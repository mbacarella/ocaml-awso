(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.gamelift
let apiVersion = "2015-10-01"
let endpointPrefix = "gamelift"
let serviceFullName = "Amazon GameLift"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let xmlNamespace = "http://gamelift.amazonaws.com/doc/"
let targetPrefix = "GameLift"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module IpProtocol =
  struct
    type nonrec t =
      | TCP 
      | UDP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | TCP -> "TCP" | UDP -> "UDP" | Non_static_id s -> s
    let of_string =
      function | "TCP" -> TCP | "UDP" -> UDP | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration IpProtocol" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"IpProtocol" j)
    let to_json = simple_to_json to_value
  end
module PortNumber =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:60000) >>=
             (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for PortNumber" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DoubleObject =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module PlayerAttributeString =
  struct
    type nonrec t = string
    let context_ = "PlayerAttributeString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PlayerAttributeString" j
    let to_json = simple_to_json to_value
  end
module ContainerDependencyCondition =
  struct
    type nonrec t =
      | START 
      | COMPLETE 
      | SUCCESS 
      | HEALTHY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | START -> "START"
      | COMPLETE -> "COMPLETE"
      | SUCCESS -> "SUCCESS"
      | HEALTHY -> "HEALTHY"
      | Non_static_id s -> s
    let of_string =
      function
      | "START" -> START
      | "COMPLETE" -> COMPLETE
      | "SUCCESS" -> SUCCESS
      | "HEALTHY" -> HEALTHY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ContainerDependencyCondition"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ContainerDependencyCondition" j)
    let to_json = simple_to_json to_value
  end
module NonZeroAnd128MaxAsciiString =
  struct
    type nonrec t = string
    let context_ = "NonZeroAnd128MaxAsciiString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9\\-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NonZeroAnd128MaxAsciiString" j
    let to_json = simple_to_json to_value
  end
module NonZeroAnd255MaxString =
  struct
    type nonrec t = string
    let context_ = "NonZeroAnd255MaxString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NonZeroAnd255MaxString" j
    let to_json = simple_to_json to_value
  end
module ContainerMountPointAccessLevel =
  struct
    type nonrec t =
      | READ_ONLY 
      | READ_AND_WRITE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | READ_ONLY -> "READ_ONLY"
      | READ_AND_WRITE -> "READ_AND_WRITE"
      | Non_static_id s -> s
    let of_string =
      function
      | "READ_ONLY" -> READ_ONLY
      | "READ_AND_WRITE" -> READ_AND_WRITE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ContainerMountPointAccessLevel"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ContainerMountPointAccessLevel" j)
    let to_json = simple_to_json to_value
  end
module ContainerPathString =
  struct
    type nonrec t = string
    let context_ = "ContainerPathString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"^(\\/+[^\\/]+\\/*)+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ContainerPathString" j
    let to_json = simple_to_json to_value
  end
module InstancePathString =
  struct
    type nonrec t = string
    let context_ = "InstancePathString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"^\\/[\\s\\S]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InstancePathString" j
    let to_json = simple_to_json to_value
  end
module ContainerPortRange =
  struct
    type nonrec t =
      {
      fromPort: PortNumber.t
        [@ocaml.doc
          "A starting value for the range of allowed port numbers."];
      toPort: PortNumber.t
        [@ocaml.doc
          "An ending value for the range of allowed port numbers. Port numbers are end-inclusive. This value must be equal to or greater than FromPort."];
      protocol: IpProtocol.t
        [@ocaml.doc "The network protocol that these ports support."]}
    let context_ = "ContainerPortRange"
    let make ~fromPort =
      fun ~toPort ->
        fun ~protocol -> fun () -> { fromPort; toPort; protocol }
    let to_value x =
      structure_to_value
        [("FromPort", (Some (PortNumber.to_value x.fromPort)));
        ("ToPort", (Some (PortNumber.to_value x.toPort)));
        ("Protocol", (Some (IpProtocol.to_value x.protocol)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protocol =
        IpProtocol.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Protocol") in
      let toPort =
        PortNumber.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ToPort") in
      let fromPort =
        PortNumber.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FromPort") in
      make ~protocol ~toPort ~fromPort ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let protocol = field_map_exn json__ "Protocol" IpProtocol.of_json in
      let toPort = field_map_exn json__ "ToPort" PortNumber.of_json in
      let fromPort = field_map_exn json__ "FromPort" PortNumber.of_json in
      make ~protocol ~toPort ~fromPort ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A set of one or more port numbers that can be opened on the container, and the supported network protocol. Part of: ContainerPortConfiguration"]
module PlayerAttributeStringDoubleMap =
  struct
    type nonrec t = (PlayerAttributeString.t * DoubleObject.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((PlayerAttributeString.of_string chopped),
                              (DoubleObject.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (PlayerAttributeString.to_value x) |>
                    (fun x -> (DoubleObject.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:PlayerAttributeString.of_string
        ~of_json:DoubleObject.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlayerAttributeStringList =
  struct
    type nonrec t = PlayerAttributeString.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PlayerAttributeString.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlayerAttributeString.of_xml)
    let of_json j =
      list_of_json ~kind:"PlayerAttributeStringList"
        ~of_json:PlayerAttributeString.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerDependency =
  struct
    type nonrec t =
      {
      containerName: NonZeroAnd128MaxAsciiString.t
        [@ocaml.doc
          "A descriptive label for the container definition that this container depends on."];
      condition: ContainerDependencyCondition.t
        [@ocaml.doc
          "The condition that the dependency container must reach before the dependent container can start. Valid conditions include: START - The dependency container must have started. COMPLETE - The dependency container has run to completion (exits). Use this condition with nonessential containers, such as those that run a script and then exit. The dependency container can't be an essential container. SUCCESS - The dependency container has run to completion and exited with a zero status. The dependency container can't be an essential container. HEALTHY - The dependency container has passed its Docker health check. Use this condition with dependency containers that have health checks configured. This condition is confirmed at container group startup only."]}
    let context_ = "ContainerDependency"
    let make ~containerName =
      fun ~condition -> fun () -> { containerName; condition }
    let to_value x =
      structure_to_value
        [("ContainerName",
           (Some (NonZeroAnd128MaxAsciiString.to_value x.containerName)));
        ("Condition",
          (Some (ContainerDependencyCondition.to_value x.condition)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let condition =
        ContainerDependencyCondition.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Condition") in
      let containerName =
        NonZeroAnd128MaxAsciiString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~condition ~containerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let condition =
        field_map_exn json__ "Condition" ContainerDependencyCondition.of_json in
      let containerName =
        field_map_exn json__ "ContainerName"
          NonZeroAnd128MaxAsciiString.of_json in
      make ~condition ~containerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A container's dependency on another container in the same container group. The dependency impacts how the dependent container is able to start or shut down based the status of the other container. For example, ContainerA is configured with the following dependency: a START dependency on ContainerB. This means that ContainerA can't start until ContainerB has started. It also means that ContainerA must shut down before ContainerB. Part of: GameServerContainerDefinition, GameServerContainerDefinitionInput, SupportContainerDefinition, SupportContainerDefinitionInput"]
module ContainerEnvironment =
  struct
    type nonrec t =
      {
      name: NonZeroAnd255MaxString.t
        [@ocaml.doc "The environment variable name."];
      value: NonZeroAnd255MaxString.t
        [@ocaml.doc "The environment variable value."]}
    let context_ = "ContainerEnvironment"
    let make ~name = fun ~value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("Name", (Some (NonZeroAnd255MaxString.to_value x.name)));
        ("Value", (Some (NonZeroAnd255MaxString.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        NonZeroAnd255MaxString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let name =
        NonZeroAnd255MaxString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~value ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map_exn json__ "Value" NonZeroAnd255MaxString.of_json in
      let name = field_map_exn json__ "Name" NonZeroAnd255MaxString.of_json in
      make ~value ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An environment variable to set inside a container, in the form of a key-value pair. Part of: GameServerContainerDefinition, GameServerContainerDefinitionInput, SupportContainerDefinition, SupportContainerDefinitionInput"]
module ContainerCommandStringList =
  struct
    type nonrec t = NonZeroAnd255MaxString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:20) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonZeroAnd255MaxString.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonZeroAnd255MaxString.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerCommandStringList"
        ~of_json:NonZeroAnd255MaxString.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerHealthCheckInterval =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:300) >>= (fun () -> check_int_min i ~min:60));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ContainerHealthCheckInterval"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ContainerHealthCheckRetries =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:10) >>= (fun () -> check_int_min i ~min:5));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ContainerHealthCheckRetries"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ContainerHealthCheckStartPeriod =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:300) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ContainerHealthCheckStartPeriod"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ContainerHealthCheckTimeout =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:60) >>= (fun () -> check_int_min i ~min:30));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ContainerHealthCheckTimeout"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ContainerMountPoint =
  struct
    type nonrec t =
      {
      instancePath: InstancePathString.t
        [@ocaml.doc "The path to the source file or directory."];
      containerPath: ContainerPathString.t option
        [@ocaml.doc
          "The mount path on the container. If this property isn't set, the instance path is used."];
      accessLevel: ContainerMountPointAccessLevel.t option
        [@ocaml.doc "The type of access for the container."]}
    let context_ = "ContainerMountPoint"
    let make ?containerPath =
      fun ?accessLevel ->
        fun ~instancePath ->
          fun () -> { containerPath; accessLevel; instancePath }
    let to_value x =
      structure_to_value
        [("InstancePath",
           (Some (InstancePathString.to_value x.instancePath)));
        ("ContainerPath",
          (Option.map x.containerPath ~f:ContainerPathString.to_value));
        ("AccessLevel",
          (Option.map x.accessLevel
             ~f:ContainerMountPointAccessLevel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accessLevel =
        (Option.map ~f:ContainerMountPointAccessLevel.of_xml)
          (Xml.child xml_arg0 "AccessLevel") in
      let containerPath =
        (Option.map ~f:ContainerPathString.of_xml)
          (Xml.child xml_arg0 "ContainerPath") in
      let instancePath =
        InstancePathString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstancePath") in
      make ?accessLevel ?containerPath ~instancePath ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let accessLevel =
        field_map json__ "AccessLevel" ContainerMountPointAccessLevel.of_json in
      let containerPath =
        field_map json__ "ContainerPath" ContainerPathString.of_json in
      let instancePath =
        field_map_exn json__ "InstancePath" InstancePathString.of_json in
      make ?accessLevel ?containerPath ~instancePath ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A mount point that binds a container to a file or directory on the host system. Part of: GameServerContainerDefinition, https://docs.aws.amazon.com/gamelift/latest/apireference/API_GameServerContainerDefinitionInput.html, SupportContainerDefinition, https://docs.aws.amazon.com/gamelift/latest/apireference/API_SupportContainerDefinitionInput.html"]
module ContainerPortRangeList =
  struct
    type nonrec t = ContainerPortRange.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContainerPortRange.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerPortRange.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerPortRangeList"
        ~of_json:ContainerPortRange.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlayerId =
  struct
    type nonrec t = string
    let context_ = "PlayerId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PlayerId" j
    let to_json = simple_to_json to_value
  end
module PlayerSessionId =
  struct
    type nonrec t = string
    let context_ = "PlayerSessionId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"^psess-\\S+"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PlayerSessionId" j
    let to_json = simple_to_json to_value
  end
module NonEmptyString =
  struct
    type nonrec t = string
    let context_ = "NonEmptyString"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NonEmptyString" j
    let to_json = simple_to_json to_value
  end
module PositiveInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for PositiveInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AttributeValue =
  struct
    type nonrec t =
      {
      s: PlayerAttributeString.t option
        [@ocaml.doc
          "For single string values. Maximum string length is 100 characters."];
      n: DoubleObject.t option
        [@ocaml.doc "For number values, expressed as double."];
      sL: PlayerAttributeStringList.t option
        [@ocaml.doc
          "For a list of up to 100 strings. Maximum length for each string is 100 characters. Duplicate values are not recognized; all occurrences of the repeated value after the first of a repeated value are ignored."];
      sDM: PlayerAttributeStringDoubleMap.t option
        [@ocaml.doc
          "For a map of up to 10 data type:value pairs. Maximum length for each string value is 100 characters."]}
    let make ?s =
      fun ?n -> fun ?sL -> fun ?sDM -> fun () -> { s; n; sL; sDM }
    let to_value x =
      structure_to_value
        [("S", (Option.map x.s ~f:PlayerAttributeString.to_value));
        ("N", (Option.map x.n ~f:DoubleObject.to_value));
        ("SL", (Option.map x.sL ~f:PlayerAttributeStringList.to_value));
        ("SDM",
          (Option.map x.sDM ~f:PlayerAttributeStringDoubleMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sDM =
        (Option.map ~f:PlayerAttributeStringDoubleMap.of_xml)
          (Xml.child xml_arg0 "SDM") in
      let sL =
        (Option.map ~f:PlayerAttributeStringList.of_xml)
          (Xml.child xml_arg0 "SL") in
      let n = (Option.map ~f:DoubleObject.of_xml) (Xml.child xml_arg0 "N") in
      let s =
        (Option.map ~f:PlayerAttributeString.of_xml) (Xml.child xml_arg0 "S") in
      make ?sDM ?sL ?n ?s ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sDM = field_map json__ "SDM" PlayerAttributeStringDoubleMap.of_json in
      let sL = field_map json__ "SL" PlayerAttributeStringList.of_json in
      let n = field_map json__ "N" DoubleObject.of_json in
      let s = field_map json__ "S" PlayerAttributeString.of_json in
      make ?sDM ?sL ?n ?s ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Values for use in player attribute key-value pairs. This object lets you specify an attribute value using any of the valid data types: string, number, string array, or data map. Each AttributeValue object can use only one of the available properties."]
module NonZeroAndMaxString =
  struct
    type nonrec t = string
    let context_ = "NonZeroAndMaxString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NonZeroAndMaxString" j
    let to_json = simple_to_json to_value
  end
module GamePropertyKey =
  struct
    type nonrec t = string
    let context_ = "GamePropertyKey"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:32); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GamePropertyKey" j
    let to_json = simple_to_json to_value
  end
module GamePropertyValue =
  struct
    type nonrec t = string
    let context_ = "GamePropertyValue"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:96); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GamePropertyValue" j
    let to_json = simple_to_json to_value
  end
module GameServerGroupInstanceType =
  struct
    type nonrec t =
      | C4_large 
      | C4_xlarge 
      | C4_2xlarge 
      | C4_4xlarge 
      | C4_8xlarge 
      | C5_large 
      | C5_xlarge 
      | C5_2xlarge 
      | C5_4xlarge 
      | C5_9xlarge 
      | C5_12xlarge 
      | C5_18xlarge 
      | C5_24xlarge 
      | C5a_large 
      | C5a_xlarge 
      | C5a_2xlarge 
      | C5a_4xlarge 
      | C5a_8xlarge 
      | C5a_12xlarge 
      | C5a_16xlarge 
      | C5a_24xlarge 
      | C6g_medium 
      | C6g_large 
      | C6g_xlarge 
      | C6g_2xlarge 
      | C6g_4xlarge 
      | C6g_8xlarge 
      | C6g_12xlarge 
      | C6g_16xlarge 
      | R4_large 
      | R4_xlarge 
      | R4_2xlarge 
      | R4_4xlarge 
      | R4_8xlarge 
      | R4_16xlarge 
      | R5_large 
      | R5_xlarge 
      | R5_2xlarge 
      | R5_4xlarge 
      | R5_8xlarge 
      | R5_12xlarge 
      | R5_16xlarge 
      | R5_24xlarge 
      | R5a_large 
      | R5a_xlarge 
      | R5a_2xlarge 
      | R5a_4xlarge 
      | R5a_8xlarge 
      | R5a_12xlarge 
      | R5a_16xlarge 
      | R5a_24xlarge 
      | R6g_medium 
      | R6g_large 
      | R6g_xlarge 
      | R6g_2xlarge 
      | R6g_4xlarge 
      | R6g_8xlarge 
      | R6g_12xlarge 
      | R6g_16xlarge 
      | M4_large 
      | M4_xlarge 
      | M4_2xlarge 
      | M4_4xlarge 
      | M4_10xlarge 
      | M5_large 
      | M5_xlarge 
      | M5_2xlarge 
      | M5_4xlarge 
      | M5_8xlarge 
      | M5_12xlarge 
      | M5_16xlarge 
      | M5_24xlarge 
      | M5a_large 
      | M5a_xlarge 
      | M5a_2xlarge 
      | M5a_4xlarge 
      | M5a_8xlarge 
      | M5a_12xlarge 
      | M5a_16xlarge 
      | M5a_24xlarge 
      | M6g_medium 
      | M6g_large 
      | M6g_xlarge 
      | M6g_2xlarge 
      | M6g_4xlarge 
      | M6g_8xlarge 
      | M6g_12xlarge 
      | M6g_16xlarge 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | C4_large -> "c4.large"
      | C4_xlarge -> "c4.xlarge"
      | C4_2xlarge -> "c4.2xlarge"
      | C4_4xlarge -> "c4.4xlarge"
      | C4_8xlarge -> "c4.8xlarge"
      | C5_large -> "c5.large"
      | C5_xlarge -> "c5.xlarge"
      | C5_2xlarge -> "c5.2xlarge"
      | C5_4xlarge -> "c5.4xlarge"
      | C5_9xlarge -> "c5.9xlarge"
      | C5_12xlarge -> "c5.12xlarge"
      | C5_18xlarge -> "c5.18xlarge"
      | C5_24xlarge -> "c5.24xlarge"
      | C5a_large -> "c5a.large"
      | C5a_xlarge -> "c5a.xlarge"
      | C5a_2xlarge -> "c5a.2xlarge"
      | C5a_4xlarge -> "c5a.4xlarge"
      | C5a_8xlarge -> "c5a.8xlarge"
      | C5a_12xlarge -> "c5a.12xlarge"
      | C5a_16xlarge -> "c5a.16xlarge"
      | C5a_24xlarge -> "c5a.24xlarge"
      | C6g_medium -> "c6g.medium"
      | C6g_large -> "c6g.large"
      | C6g_xlarge -> "c6g.xlarge"
      | C6g_2xlarge -> "c6g.2xlarge"
      | C6g_4xlarge -> "c6g.4xlarge"
      | C6g_8xlarge -> "c6g.8xlarge"
      | C6g_12xlarge -> "c6g.12xlarge"
      | C6g_16xlarge -> "c6g.16xlarge"
      | R4_large -> "r4.large"
      | R4_xlarge -> "r4.xlarge"
      | R4_2xlarge -> "r4.2xlarge"
      | R4_4xlarge -> "r4.4xlarge"
      | R4_8xlarge -> "r4.8xlarge"
      | R4_16xlarge -> "r4.16xlarge"
      | R5_large -> "r5.large"
      | R5_xlarge -> "r5.xlarge"
      | R5_2xlarge -> "r5.2xlarge"
      | R5_4xlarge -> "r5.4xlarge"
      | R5_8xlarge -> "r5.8xlarge"
      | R5_12xlarge -> "r5.12xlarge"
      | R5_16xlarge -> "r5.16xlarge"
      | R5_24xlarge -> "r5.24xlarge"
      | R5a_large -> "r5a.large"
      | R5a_xlarge -> "r5a.xlarge"
      | R5a_2xlarge -> "r5a.2xlarge"
      | R5a_4xlarge -> "r5a.4xlarge"
      | R5a_8xlarge -> "r5a.8xlarge"
      | R5a_12xlarge -> "r5a.12xlarge"
      | R5a_16xlarge -> "r5a.16xlarge"
      | R5a_24xlarge -> "r5a.24xlarge"
      | R6g_medium -> "r6g.medium"
      | R6g_large -> "r6g.large"
      | R6g_xlarge -> "r6g.xlarge"
      | R6g_2xlarge -> "r6g.2xlarge"
      | R6g_4xlarge -> "r6g.4xlarge"
      | R6g_8xlarge -> "r6g.8xlarge"
      | R6g_12xlarge -> "r6g.12xlarge"
      | R6g_16xlarge -> "r6g.16xlarge"
      | M4_large -> "m4.large"
      | M4_xlarge -> "m4.xlarge"
      | M4_2xlarge -> "m4.2xlarge"
      | M4_4xlarge -> "m4.4xlarge"
      | M4_10xlarge -> "m4.10xlarge"
      | M5_large -> "m5.large"
      | M5_xlarge -> "m5.xlarge"
      | M5_2xlarge -> "m5.2xlarge"
      | M5_4xlarge -> "m5.4xlarge"
      | M5_8xlarge -> "m5.8xlarge"
      | M5_12xlarge -> "m5.12xlarge"
      | M5_16xlarge -> "m5.16xlarge"
      | M5_24xlarge -> "m5.24xlarge"
      | M5a_large -> "m5a.large"
      | M5a_xlarge -> "m5a.xlarge"
      | M5a_2xlarge -> "m5a.2xlarge"
      | M5a_4xlarge -> "m5a.4xlarge"
      | M5a_8xlarge -> "m5a.8xlarge"
      | M5a_12xlarge -> "m5a.12xlarge"
      | M5a_16xlarge -> "m5a.16xlarge"
      | M5a_24xlarge -> "m5a.24xlarge"
      | M6g_medium -> "m6g.medium"
      | M6g_large -> "m6g.large"
      | M6g_xlarge -> "m6g.xlarge"
      | M6g_2xlarge -> "m6g.2xlarge"
      | M6g_4xlarge -> "m6g.4xlarge"
      | M6g_8xlarge -> "m6g.8xlarge"
      | M6g_12xlarge -> "m6g.12xlarge"
      | M6g_16xlarge -> "m6g.16xlarge"
      | Non_static_id s -> s
    let of_string =
      function
      | "c4.large" -> C4_large
      | "c4.xlarge" -> C4_xlarge
      | "c4.2xlarge" -> C4_2xlarge
      | "c4.4xlarge" -> C4_4xlarge
      | "c4.8xlarge" -> C4_8xlarge
      | "c5.large" -> C5_large
      | "c5.xlarge" -> C5_xlarge
      | "c5.2xlarge" -> C5_2xlarge
      | "c5.4xlarge" -> C5_4xlarge
      | "c5.9xlarge" -> C5_9xlarge
      | "c5.12xlarge" -> C5_12xlarge
      | "c5.18xlarge" -> C5_18xlarge
      | "c5.24xlarge" -> C5_24xlarge
      | "c5a.large" -> C5a_large
      | "c5a.xlarge" -> C5a_xlarge
      | "c5a.2xlarge" -> C5a_2xlarge
      | "c5a.4xlarge" -> C5a_4xlarge
      | "c5a.8xlarge" -> C5a_8xlarge
      | "c5a.12xlarge" -> C5a_12xlarge
      | "c5a.16xlarge" -> C5a_16xlarge
      | "c5a.24xlarge" -> C5a_24xlarge
      | "c6g.medium" -> C6g_medium
      | "c6g.large" -> C6g_large
      | "c6g.xlarge" -> C6g_xlarge
      | "c6g.2xlarge" -> C6g_2xlarge
      | "c6g.4xlarge" -> C6g_4xlarge
      | "c6g.8xlarge" -> C6g_8xlarge
      | "c6g.12xlarge" -> C6g_12xlarge
      | "c6g.16xlarge" -> C6g_16xlarge
      | "r4.large" -> R4_large
      | "r4.xlarge" -> R4_xlarge
      | "r4.2xlarge" -> R4_2xlarge
      | "r4.4xlarge" -> R4_4xlarge
      | "r4.8xlarge" -> R4_8xlarge
      | "r4.16xlarge" -> R4_16xlarge
      | "r5.large" -> R5_large
      | "r5.xlarge" -> R5_xlarge
      | "r5.2xlarge" -> R5_2xlarge
      | "r5.4xlarge" -> R5_4xlarge
      | "r5.8xlarge" -> R5_8xlarge
      | "r5.12xlarge" -> R5_12xlarge
      | "r5.16xlarge" -> R5_16xlarge
      | "r5.24xlarge" -> R5_24xlarge
      | "r5a.large" -> R5a_large
      | "r5a.xlarge" -> R5a_xlarge
      | "r5a.2xlarge" -> R5a_2xlarge
      | "r5a.4xlarge" -> R5a_4xlarge
      | "r5a.8xlarge" -> R5a_8xlarge
      | "r5a.12xlarge" -> R5a_12xlarge
      | "r5a.16xlarge" -> R5a_16xlarge
      | "r5a.24xlarge" -> R5a_24xlarge
      | "r6g.medium" -> R6g_medium
      | "r6g.large" -> R6g_large
      | "r6g.xlarge" -> R6g_xlarge
      | "r6g.2xlarge" -> R6g_2xlarge
      | "r6g.4xlarge" -> R6g_4xlarge
      | "r6g.8xlarge" -> R6g_8xlarge
      | "r6g.12xlarge" -> R6g_12xlarge
      | "r6g.16xlarge" -> R6g_16xlarge
      | "m4.large" -> M4_large
      | "m4.xlarge" -> M4_xlarge
      | "m4.2xlarge" -> M4_2xlarge
      | "m4.4xlarge" -> M4_4xlarge
      | "m4.10xlarge" -> M4_10xlarge
      | "m5.large" -> M5_large
      | "m5.xlarge" -> M5_xlarge
      | "m5.2xlarge" -> M5_2xlarge
      | "m5.4xlarge" -> M5_4xlarge
      | "m5.8xlarge" -> M5_8xlarge
      | "m5.12xlarge" -> M5_12xlarge
      | "m5.16xlarge" -> M5_16xlarge
      | "m5.24xlarge" -> M5_24xlarge
      | "m5a.large" -> M5a_large
      | "m5a.xlarge" -> M5a_xlarge
      | "m5a.2xlarge" -> M5a_2xlarge
      | "m5a.4xlarge" -> M5a_4xlarge
      | "m5a.8xlarge" -> M5a_8xlarge
      | "m5a.12xlarge" -> M5a_12xlarge
      | "m5a.16xlarge" -> M5a_16xlarge
      | "m5a.24xlarge" -> M5a_24xlarge
      | "m6g.medium" -> M6g_medium
      | "m6g.large" -> M6g_large
      | "m6g.xlarge" -> M6g_xlarge
      | "m6g.2xlarge" -> M6g_2xlarge
      | "m6g.4xlarge" -> M6g_4xlarge
      | "m6g.8xlarge" -> M6g_8xlarge
      | "m6g.12xlarge" -> M6g_12xlarge
      | "m6g.16xlarge" -> M6g_16xlarge
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GameServerGroupInstanceType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GameServerGroupInstanceType" j)
    let to_json = simple_to_json to_value
  end
module WeightedCapacity =
  struct
    type nonrec t = string
    let context_ = "WeightedCapacity"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:3) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^[\\u0031-\\u0039][\\u0030-\\u0039]{0,2}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"WeightedCapacity" j
    let to_json = simple_to_json to_value
  end
module BooleanModel =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ContainerDependencyList =
  struct
    type nonrec t = ContainerDependency.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContainerDependency.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerDependency.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerDependencyList"
        ~of_json:ContainerDependency.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerEnvironmentList =
  struct
    type nonrec t = ContainerEnvironment.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:20) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContainerEnvironment.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerEnvironment.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerEnvironmentList"
        ~of_json:ContainerEnvironment.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerHealthCheck =
  struct
    type nonrec t =
      {
      command: ContainerCommandStringList.t
        [@ocaml.doc
          "A string array that specifies the command that the container runs to determine if it's healthy."];
      interval: ContainerHealthCheckInterval.t option
        [@ocaml.doc
          "The time period (in seconds) between each health check."];
      retries: ContainerHealthCheckRetries.t option
        [@ocaml.doc
          "The number of times to retry a failed health check before flagging the container unhealthy. The first run of the command does not count as a retry."];
      startPeriod: ContainerHealthCheckStartPeriod.t option
        [@ocaml.doc
          "The optional grace period (in seconds) to give a container time to bootstrap before the first failed health check counts toward the number of retries."];
      timeout: ContainerHealthCheckTimeout.t option
        [@ocaml.doc
          "The time period (in seconds) to wait for a health check to succeed before counting a failed health check."]}
    let context_ = "ContainerHealthCheck"
    let make ?interval =
      fun ?retries ->
        fun ?startPeriod ->
          fun ?timeout ->
            fun ~command ->
              fun () -> { interval; retries; startPeriod; timeout; command }
    let to_value x =
      structure_to_value
        [("Command", (Some (ContainerCommandStringList.to_value x.command)));
        ("Interval",
          (Option.map x.interval ~f:ContainerHealthCheckInterval.to_value));
        ("Retries",
          (Option.map x.retries ~f:ContainerHealthCheckRetries.to_value));
        ("StartPeriod",
          (Option.map x.startPeriod
             ~f:ContainerHealthCheckStartPeriod.to_value));
        ("Timeout",
          (Option.map x.timeout ~f:ContainerHealthCheckTimeout.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timeout =
        (Option.map ~f:ContainerHealthCheckTimeout.of_xml)
          (Xml.child xml_arg0 "Timeout") in
      let startPeriod =
        (Option.map ~f:ContainerHealthCheckStartPeriod.of_xml)
          (Xml.child xml_arg0 "StartPeriod") in
      let retries =
        (Option.map ~f:ContainerHealthCheckRetries.of_xml)
          (Xml.child xml_arg0 "Retries") in
      let interval =
        (Option.map ~f:ContainerHealthCheckInterval.of_xml)
          (Xml.child xml_arg0 "Interval") in
      let command =
        ContainerCommandStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Command") in
      make ?timeout ?startPeriod ?retries ?interval ~command ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timeout =
        field_map json__ "Timeout" ContainerHealthCheckTimeout.of_json in
      let startPeriod =
        field_map json__ "StartPeriod"
          ContainerHealthCheckStartPeriod.of_json in
      let retries =
        field_map json__ "Retries" ContainerHealthCheckRetries.of_json in
      let interval =
        field_map json__ "Interval" ContainerHealthCheckInterval.of_json in
      let command =
        field_map_exn json__ "Command" ContainerCommandStringList.of_json in
      make ?timeout ?startPeriod ?retries ?interval ~command ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Instructions on when and how to check the health of a support container in a container fleet. These properties override any Docker health checks that are set in the container image. For more information on container health checks, see HealthCheck command in the Amazon Elastic Container Service API. Game server containers don't have a health check parameter; Amazon GameLift Servers automatically handles health checks for these containers. The following example instructs the container to initiate a health check command every 60 seconds and wait 10 seconds for it to succeed. If it fails, retry the command 3 times before flagging the container as unhealthy. It also tells the container to wait 100 seconds after launch before counting failed health checks. \\{\"Command\": \\[ \"CMD-SHELL\", \"ps cax | grep \"processmanager\" || exit 1\" \\], \"Interval\": 60, \"Timeout\": 10, \"Retries\": 3, \"StartPeriod\": 100 \\} Part of: SupportContainerDefinition, SupportContainerDefinitionInput"]
module ContainerMemoryLimit =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1024000) >>=
             (fun () -> check_int_min i ~min:4));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ContainerMemoryLimit" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ContainerMountPointList =
  struct
    type nonrec t = ContainerMountPoint.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContainerMountPoint.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerMountPoint.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerMountPointList"
        ~of_json:ContainerMountPoint.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerPortConfiguration =
  struct
    type nonrec t =
      {
      containerPortRanges: ContainerPortRangeList.t
        [@ocaml.doc
          "A set of one or more container port number ranges. The ranges can't overlap if the ranges' network protocols are the same. Overlapping ranges with different protocols is allowed but not recommended."]}
    let context_ = "ContainerPortConfiguration"
    let make ~containerPortRanges = fun () -> { containerPortRanges }
    let to_value x =
      structure_to_value
        [("ContainerPortRanges",
           (Some (ContainerPortRangeList.to_value x.containerPortRanges)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerPortRanges =
        ContainerPortRangeList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerPortRanges") in
      make ~containerPortRanges ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let containerPortRanges =
        field_map_exn json__ "ContainerPortRanges"
          ContainerPortRangeList.of_json in
      make ~containerPortRanges ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A set of port ranges that can be opened on the container. A process that's running in the container can bind to a port number, making it accessible to inbound traffic when it's mapped to a container fleet's connection port. Each container port range specifies a network protocol. When the configuration supports more than one protocol, we recommend that you use a different range for each protocol. If your ranges have overlapping port numbers, Amazon GameLift Servers maps a duplicated container port number to different connection ports. For example, if you include 1935 in port ranges for both TCP and UDP, it might result in the following mappings: container port 1935 (tcp) => connection port 2001 container port 1935 (udp) => connection port 2002 Part of: GameServerContainerDefinition, GameServerContainerDefinitionInput, SupportContainerDefinition, SupportContainerDefinitionInput"]
module ContainerVcpu =
  struct
    type nonrec t = float
    let make i =
      let open Result in
        ok_or_failwith
          ((check_float_min i ~min:10.) >>=
             (fun () -> check_float_min i ~min:0.125));
        i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module ImageUriString =
  struct
    type nonrec t = string
    let context_ = "ImageUriString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9-_\\.@\\/:]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ImageUriString" j
    let to_json = simple_to_json to_value
  end
module Sha256 =
  struct
    type nonrec t = string
    let context_ = "Sha256"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"^sha256:[a-fA-F0-9]{64}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Sha256" j
    let to_json = simple_to_json to_value
  end
module ContainerFleetLocationStatus =
  struct
    type nonrec t =
      | PENDING 
      | CREATING 
      | CREATED 
      | ACTIVATING 
      | ACTIVE 
      | UPDATING 
      | DELETING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | CREATING -> "CREATING"
      | CREATED -> "CREATED"
      | ACTIVATING -> "ACTIVATING"
      | ACTIVE -> "ACTIVE"
      | UPDATING -> "UPDATING"
      | DELETING -> "DELETING"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "CREATING" -> CREATING
      | "CREATED" -> CREATED
      | "ACTIVATING" -> ACTIVATING
      | "ACTIVE" -> ACTIVE
      | "UPDATING" -> UPDATING
      | "DELETING" -> DELETING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ContainerFleetLocationStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ContainerFleetLocationStatus" j)
    let to_json = simple_to_json to_value
  end
module LocationStringModel =
  struct
    type nonrec t = string
    let context_ = "LocationStringModel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"^[A-Za-z0-9\\-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LocationStringModel" j
    let to_json = simple_to_json to_value
  end
module PlayerGatewayStatus =
  struct
    type nonrec t =
      | DISABLED 
      | ENABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DISABLED -> "DISABLED"
      | ENABLED -> "ENABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "DISABLED" -> DISABLED
      | "ENABLED" -> ENABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PlayerGatewayStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PlayerGatewayStatus" j)
    let to_json = simple_to_json to_value
  end
module IpRange =
  struct
    type nonrec t = string
    let context_ = "IpRange"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[^\\s]+"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IpRange" j
    let to_json = simple_to_json to_value
  end
module IpAddress =
  struct
    type nonrec t = string
    let context_ = "IpAddress"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"^[0-9A-Fa-f\\:\\.]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IpAddress" j
    let to_json = simple_to_json to_value
  end
module MatchedPlayerSession =
  struct
    type nonrec t =
      {
      playerId: PlayerId.t option
        [@ocaml.doc "A unique identifier for a player"];
      playerSessionId: PlayerSessionId.t option
        [@ocaml.doc
          "A unique identifier for a player session. PlayerSessionId will only be populated for player sessions that are in ACTIVE or RESERVED status when the ticket is completed."]}
    let make ?playerId =
      fun ?playerSessionId -> fun () -> { playerId; playerSessionId }
    let to_value x =
      structure_to_value
        [("PlayerId", (Option.map x.playerId ~f:PlayerId.to_value));
        ("PlayerSessionId",
          (Option.map x.playerSessionId ~f:PlayerSessionId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerSessionId =
        (Option.map ~f:PlayerSessionId.of_xml)
          (Xml.child xml_arg0 "PlayerSessionId") in
      let playerId =
        (Option.map ~f:PlayerId.of_xml) (Xml.child xml_arg0 "PlayerId") in
      make ?playerSessionId ?playerId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerSessionId =
        field_map json__ "PlayerSessionId" PlayerSessionId.of_json in
      let playerId = field_map json__ "PlayerId" PlayerId.of_json in
      make ?playerSessionId ?playerId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a new player session that is created as a result of a successful FlexMatch match. A successful match automatically creates new player sessions for every player ID in the original matchmaking request. When players connect to the match's game session, they must include both player ID and player session ID in order to claim their assigned player slot."]
module LatencyMap =
  struct
    type nonrec t = (NonEmptyString.t * PositiveInteger.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((NonEmptyString.of_string chopped),
                              (PositiveInteger.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (NonEmptyString.to_value x) |>
                    (fun x ->
                       (PositiveInteger.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:NonEmptyString.of_string
        ~of_json:PositiveInteger.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlayerAttributeMap =
  struct
    type nonrec t = (NonZeroAndMaxString.t * AttributeValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types NonZeroAndMaxString AttributeValue"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (NonZeroAndMaxString.to_value x) |>
                    (fun x ->
                       (AttributeValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:NonZeroAndMaxString.of_string
        ~of_json:AttributeValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ArnStringModel =
  struct
    type nonrec t = string
    let context_ = "ArnStringModel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:512) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9:/-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ArnStringModel" j
    let to_json = simple_to_json to_value
  end
module WholeNumber =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for WholeNumber" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module PriorityType =
  struct
    type nonrec t =
      | LATENCY 
      | COST 
      | DESTINATION 
      | LOCATION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | LATENCY -> "LATENCY"
      | COST -> "COST"
      | DESTINATION -> "DESTINATION"
      | LOCATION -> "LOCATION"
      | Non_static_id s -> s
    let of_string =
      function
      | "LATENCY" -> LATENCY
      | "COST" -> COST
      | "DESTINATION" -> DESTINATION
      | "LOCATION" -> LOCATION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PriorityType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PriorityType" j)
    let to_json = simple_to_json to_value
  end
module GameProperty =
  struct
    type nonrec t =
      {
      key: GamePropertyKey.t
        [@ocaml.doc
          "The game property identifier. Avoid using periods (\".\") in property keys if you plan to search for game sessions by properties. Property keys containing periods cannot be searched and will be filtered out from search results due to search index limitations. If you use SearchGameSessions API, there is a limit of 500 game property keys across all game sessions and all fleets per region. If the limit is exceeded, there will potentially be game session entries missing from SearchGameSessions API results."];
      value: GamePropertyValue.t [@ocaml.doc "The game property value."]}
    let context_ = "GameProperty"
    let make ~key = fun ~value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Some (GamePropertyKey.to_value x.key)));
        ("Value", (Some (GamePropertyValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        GamePropertyValue.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let key =
        GamePropertyKey.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Key") in
      make ~value ~key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map_exn json__ "Value" GamePropertyValue.of_json in
      let key = field_map_exn json__ "Key" GamePropertyKey.of_json in
      make ~value ~key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This key-value pair can store custom data about a game session. For example, you might use a GameProperty to track a game session's map, level of difficulty, or remaining time. The difficulty level could be specified like this: \\{\"Key\": \"difficulty\", \"Value\":\"Novice\"\\}. You can set game properties when creating a game session. You can also modify game properties of an active game session. When searching for game sessions, you can filter on game property keys and values. You can't delete game properties from a game session. For examples of working with game properties, see Create a game session with properties."]
module LaunchParametersStringModel =
  struct
    type nonrec t = string
    let context_ = "LaunchParametersStringModel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[A-Za-z0-9_:.+\\/\\\\\\- =@;{},?'\\[\\]\"]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LaunchParametersStringModel" j
    let to_json = simple_to_json to_value
  end
module LaunchPathStringModel =
  struct
    type nonrec t = string
    let context_ = "LaunchPathStringModel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[A-Za-z0-9_:.+\\/\\\\\\- ]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LaunchPathStringModel" j
    let to_json = simple_to_json to_value
  end
module Float_ =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Float x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a float" xml_arg0)
    let of_json j = float_of_json ~kind:"a float" j
    let to_json = simple_to_json to_value
  end
module UDPEndpoint =
  struct
    type nonrec t =
      {
      domain: NonZeroAndMaxString.t option
        [@ocaml.doc "The domain name of the UDP endpoint."];
      port: PositiveInteger.t option
        [@ocaml.doc
          "The port number of the UDP endpoint. For Amazon GameLift Servers ping beacons, this is typically port 7770."]}
    let make ?domain = fun ?port -> fun () -> { domain; port }
    let to_value x =
      structure_to_value
        [("Domain", (Option.map x.domain ~f:NonZeroAndMaxString.to_value));
        ("Port", (Option.map x.port ~f:PositiveInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let port =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Port") in
      let domain =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Domain") in
      make ?port ?domain ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let port = field_map json__ "Port" PositiveInteger.of_json in
      let domain = field_map json__ "Domain" NonZeroAndMaxString.of_json in
      make ?port ?domain ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The domain name and port information for a UDP endpoint."]
module GameServerGroupAction =
  struct
    type nonrec t =
      | REPLACE_INSTANCE_TYPES 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | REPLACE_INSTANCE_TYPES -> "REPLACE_INSTANCE_TYPES"
      | Non_static_id s -> s
    let of_string =
      function
      | "REPLACE_INSTANCE_TYPES" -> REPLACE_INSTANCE_TYPES
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GameServerGroupAction" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GameServerGroupAction" j)
    let to_json = simple_to_json to_value
  end
module InstanceDefinition =
  struct
    type nonrec t =
      {
      instanceType: GameServerGroupInstanceType.t
        [@ocaml.doc "An Amazon EC2 instance type designation."];
      weightedCapacity: WeightedCapacity.t option
        [@ocaml.doc
          "Instance weighting that indicates how much this instance type contributes to the total capacity of a game server group. Instance weights are used by Amazon GameLift Servers FleetIQ to calculate the instance type's cost per unit hour and better identify the most cost-effective options. For detailed information on weighting instance capacity, see Instance Weighting in the Amazon Elastic Compute Cloud Auto Scaling User Guide. Default value is \"1\"."]}
    let context_ = "InstanceDefinition"
    let make ?weightedCapacity =
      fun ~instanceType -> fun () -> { weightedCapacity; instanceType }
    let to_value x =
      structure_to_value
        [("InstanceType",
           (Some (GameServerGroupInstanceType.to_value x.instanceType)));
        ("WeightedCapacity",
          (Option.map x.weightedCapacity ~f:WeightedCapacity.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let weightedCapacity =
        (Option.map ~f:WeightedCapacity.of_xml)
          (Xml.child xml_arg0 "WeightedCapacity") in
      let instanceType =
        GameServerGroupInstanceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceType") in
      make ?weightedCapacity ~instanceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let weightedCapacity =
        field_map json__ "WeightedCapacity" WeightedCapacity.of_json in
      let instanceType =
        field_map_exn json__ "InstanceType"
          GameServerGroupInstanceType.of_json in
      make ?weightedCapacity ~instanceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An allowed instance type for a game server group. All game server groups must have at least two instance types defined for it. Amazon GameLift Servers FleetIQ periodically evaluates each defined instance type for viability. It then updates the Auto Scaling group with the list of viable instance types."]
module DeploymentImpairmentStrategy =
  struct
    type nonrec t =
      | MAINTAIN 
      | ROLLBACK 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MAINTAIN -> "MAINTAIN"
      | ROLLBACK -> "ROLLBACK"
      | Non_static_id s -> s
    let of_string =
      function
      | "MAINTAIN" -> MAINTAIN
      | "ROLLBACK" -> ROLLBACK
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DeploymentImpairmentStrategy"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"DeploymentImpairmentStrategy" j)
    let to_json = simple_to_json to_value
  end
module DeploymentProtectionStrategy =
  struct
    type nonrec t =
      | WITH_PROTECTION 
      | IGNORE_PROTECTION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | WITH_PROTECTION -> "WITH_PROTECTION"
      | IGNORE_PROTECTION -> "IGNORE_PROTECTION"
      | Non_static_id s -> s
    let of_string =
      function
      | "WITH_PROTECTION" -> WITH_PROTECTION
      | "IGNORE_PROTECTION" -> IGNORE_PROTECTION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DeploymentProtectionStrategy"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"DeploymentProtectionStrategy" j)
    let to_json = simple_to_json to_value
  end
module MinimumHealthyPercentage =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:75) >>= (fun () -> check_int_min i ~min:30));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MinimumHealthyPercentage"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ServerSdkVersion =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Data type used for server SDK version."]
    let context_ = "ServerSdkVersion"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_pattern i ~pattern:"^\\d+\\.\\d+\\.\\d+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServerSdkVersion" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Data type used for server SDK version."]
module SupportContainerDefinition =
  struct
    type nonrec t =
      {
      containerName: NonZeroAnd128MaxAsciiString.t option
        [@ocaml.doc
          "The container definition identifier. Container names are unique within a container group definition."];
      dependsOn: ContainerDependencyList.t option
        [@ocaml.doc
          "Indicates that the container relies on the status of other containers in the same container group during its startup and shutdown sequences. A container might have dependencies on multiple containers."];
      mountPoints: ContainerMountPointList.t option
        [@ocaml.doc
          "A mount point that binds a path inside the container to a file or directory on the host system and lets it access the file or directory."];
      environmentOverride: ContainerEnvironmentList.t option
        [@ocaml.doc
          "A set of environment variables that's passed to the container on startup. See the ContainerDefinition::environment parameter in the Amazon Elastic Container Service API Reference."];
      essential: BooleanModel.t option
        [@ocaml.doc
          "Indicates whether the container is vital to the container group. If an essential container fails, the entire container group restarts."];
      healthCheck: ContainerHealthCheck.t option
        [@ocaml.doc
          "A configuration for a non-terminal health check. A support container automatically restarts if it stops functioning or if it fails this health check."];
      imageUri: ImageUriString.t option
        [@ocaml.doc
          "The URI to the image that Amazon GameLift Servers deploys to a container fleet. For a more specific identifier, see ResolvedImageDigest."];
      memoryHardLimitMebibytes: ContainerMemoryLimit.t option
        [@ocaml.doc
          "The amount of memory that Amazon GameLift Servers makes available to the container. If memory limits aren't set for an individual container, the container shares the container group's total memory allocation. Related data type: ContainerGroupDefinition TotalMemoryLimitMebibytes"];
      portConfiguration: ContainerPortConfiguration.t option
        [@ocaml.doc
          "A set of ports that allow access to the container from external users. Processes running in the container can bind to a one of these ports. Container ports aren't directly accessed by inbound traffic. Amazon GameLift Servers maps these container ports to externally accessible connection ports, which are assigned as needed from the container fleet's ConnectionPortRange."];
      resolvedImageDigest: Sha256.t option
        [@ocaml.doc
          "A unique and immutable identifier for the container image. The digest is a SHA 256 hash of the container image manifest."];
      vcpu: ContainerVcpu.t option
        [@ocaml.doc
          "The number of vCPU units that are reserved for the container. If no resources are reserved, the container shares the total vCPU limit for the container group. Related data type: ContainerGroupDefinition TotalVcpuLimit"]}
    let make ?containerName =
      fun ?dependsOn ->
        fun ?mountPoints ->
          fun ?environmentOverride ->
            fun ?essential ->
              fun ?healthCheck ->
                fun ?imageUri ->
                  fun ?memoryHardLimitMebibytes ->
                    fun ?portConfiguration ->
                      fun ?resolvedImageDigest ->
                        fun ?vcpu ->
                          fun () ->
                            {
                              containerName;
                              dependsOn;
                              mountPoints;
                              environmentOverride;
                              essential;
                              healthCheck;
                              imageUri;
                              memoryHardLimitMebibytes;
                              portConfiguration;
                              resolvedImageDigest;
                              vcpu
                            }
    let to_value x =
      structure_to_value
        [("ContainerName",
           (Option.map x.containerName
              ~f:NonZeroAnd128MaxAsciiString.to_value));
        ("DependsOn",
          (Option.map x.dependsOn ~f:ContainerDependencyList.to_value));
        ("MountPoints",
          (Option.map x.mountPoints ~f:ContainerMountPointList.to_value));
        ("EnvironmentOverride",
          (Option.map x.environmentOverride
             ~f:ContainerEnvironmentList.to_value));
        ("Essential", (Option.map x.essential ~f:BooleanModel.to_value));
        ("HealthCheck",
          (Option.map x.healthCheck ~f:ContainerHealthCheck.to_value));
        ("ImageUri", (Option.map x.imageUri ~f:ImageUriString.to_value));
        ("MemoryHardLimitMebibytes",
          (Option.map x.memoryHardLimitMebibytes
             ~f:ContainerMemoryLimit.to_value));
        ("PortConfiguration",
          (Option.map x.portConfiguration
             ~f:ContainerPortConfiguration.to_value));
        ("ResolvedImageDigest",
          (Option.map x.resolvedImageDigest ~f:Sha256.to_value));
        ("Vcpu", (Option.map x.vcpu ~f:ContainerVcpu.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vcpu =
        (Option.map ~f:ContainerVcpu.of_xml) (Xml.child xml_arg0 "Vcpu") in
      let resolvedImageDigest =
        (Option.map ~f:Sha256.of_xml)
          (Xml.child xml_arg0 "ResolvedImageDigest") in
      let portConfiguration =
        (Option.map ~f:ContainerPortConfiguration.of_xml)
          (Xml.child xml_arg0 "PortConfiguration") in
      let memoryHardLimitMebibytes =
        (Option.map ~f:ContainerMemoryLimit.of_xml)
          (Xml.child xml_arg0 "MemoryHardLimitMebibytes") in
      let imageUri =
        (Option.map ~f:ImageUriString.of_xml) (Xml.child xml_arg0 "ImageUri") in
      let healthCheck =
        (Option.map ~f:ContainerHealthCheck.of_xml)
          (Xml.child xml_arg0 "HealthCheck") in
      let essential =
        (Option.map ~f:BooleanModel.of_xml) (Xml.child xml_arg0 "Essential") in
      let environmentOverride =
        (Option.map ~f:ContainerEnvironmentList.of_xml)
          (Xml.child xml_arg0 "EnvironmentOverride") in
      let mountPoints =
        (Option.map ~f:ContainerMountPointList.of_xml)
          (Xml.child xml_arg0 "MountPoints") in
      let dependsOn =
        (Option.map ~f:ContainerDependencyList.of_xml)
          (Xml.child xml_arg0 "DependsOn") in
      let containerName =
        (Option.map ~f:NonZeroAnd128MaxAsciiString.of_xml)
          (Xml.child xml_arg0 "ContainerName") in
      make ?vcpu ?resolvedImageDigest ?portConfiguration
        ?memoryHardLimitMebibytes ?imageUri ?healthCheck ?essential
        ?environmentOverride ?mountPoints ?dependsOn ?containerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vcpu = field_map json__ "Vcpu" ContainerVcpu.of_json in
      let resolvedImageDigest =
        field_map json__ "ResolvedImageDigest" Sha256.of_json in
      let portConfiguration =
        field_map json__ "PortConfiguration"
          ContainerPortConfiguration.of_json in
      let memoryHardLimitMebibytes =
        field_map json__ "MemoryHardLimitMebibytes"
          ContainerMemoryLimit.of_json in
      let imageUri = field_map json__ "ImageUri" ImageUriString.of_json in
      let healthCheck =
        field_map json__ "HealthCheck" ContainerHealthCheck.of_json in
      let essential = field_map json__ "Essential" BooleanModel.of_json in
      let environmentOverride =
        field_map json__ "EnvironmentOverride"
          ContainerEnvironmentList.of_json in
      let mountPoints =
        field_map json__ "MountPoints" ContainerMountPointList.of_json in
      let dependsOn =
        field_map json__ "DependsOn" ContainerDependencyList.of_json in
      let containerName =
        field_map json__ "ContainerName" NonZeroAnd128MaxAsciiString.of_json in
      make ?vcpu ?resolvedImageDigest ?portConfiguration
        ?memoryHardLimitMebibytes ?imageUri ?healthCheck ?essential
        ?environmentOverride ?mountPoints ?dependsOn ?containerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a support container in a container group. A support container might be in a game server container group or a per-instance container group. Support containers don't run game server processes. You can update a support container definition and deploy the updates to an existing fleet. When creating or updating a game server container group definition, use the property GameServerContainerDefinitionInput. Part of: ContainerGroupDefinition Returned by: DescribeContainerGroupDefinition, ListContainerGroupDefinitions, UpdateContainerGroupDefinition"]
module ContainerFleetLocationAttributes =
  struct
    type nonrec t =
      {
      location: LocationStringModel.t option
        [@ocaml.doc "A location identifier."];
      status: ContainerFleetLocationStatus.t option
        [@ocaml.doc
          "The status of fleet activity in the location. PENDING -- A new container fleet has been requested. CREATING -- A new container fleet resource is being created. CREATED -- A new container fleet resource has been created. No fleet instances have been deployed. ACTIVATING -- New container fleet instances are being deployed. ACTIVE -- The container fleet has been deployed and is ready to host game sessions. UPDATING -- Updates to the container fleet is being updated. A deployment is in progress."];
      playerGatewayStatus: PlayerGatewayStatus.t option
        [@ocaml.doc
          "The current status of player gateway in this location for this container fleet. Note, even if a container fleet has PlayerGatewayMode configured as ENABLED, player gateway might not be available in a specific location. For more information about locations where player gateway is supported, see Amazon GameLift Servers service locations. Possible values include: ENABLED -- Player gateway is available for this container fleet location. DISABLED -- Player gateway is not available for this container fleet location."]}
    let make ?location =
      fun ?status ->
        fun ?playerGatewayStatus ->
          fun () -> { location; status; playerGatewayStatus }
    let to_value x =
      structure_to_value
        [("Location",
           (Option.map x.location ~f:LocationStringModel.to_value));
        ("Status",
          (Option.map x.status ~f:ContainerFleetLocationStatus.to_value));
        ("PlayerGatewayStatus",
          (Option.map x.playerGatewayStatus ~f:PlayerGatewayStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerGatewayStatus =
        (Option.map ~f:PlayerGatewayStatus.of_xml)
          (Xml.child xml_arg0 "PlayerGatewayStatus") in
      let status =
        (Option.map ~f:ContainerFleetLocationStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      make ?playerGatewayStatus ?status ?location ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerGatewayStatus =
        field_map json__ "PlayerGatewayStatus" PlayerGatewayStatus.of_json in
      let status =
        field_map json__ "Status" ContainerFleetLocationStatus.of_json in
      let location = field_map json__ "Location" LocationStringModel.of_json in
      make ?playerGatewayStatus ?status ?location ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about a location in a multi-location container fleet."]
module DeploymentId =
  struct
    type nonrec t = string
    let context_ = "DeploymentId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9\\-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DeploymentId" j
    let to_json = simple_to_json to_value
  end
module IpPermission =
  struct
    type nonrec t =
      {
      fromPort: PortNumber.t
        [@ocaml.doc
          "A starting value for a range of allowed port numbers. For fleets using Linux builds, only ports 22 and 1026-60000 are valid. For fleets using Windows builds, only ports 1026-60000 are valid."];
      toPort: PortNumber.t
        [@ocaml.doc
          "An ending value for a range of allowed port numbers. Port numbers are end-inclusive. This value must be equal to or greater than FromPort. For fleets using Linux builds, only ports 22 and 1026-60000 are valid. For fleets using Windows builds, only ports 1026-60000 are valid."];
      ipRange: IpRange.t
        [@ocaml.doc
          "A range of allowed IP addresses. This value must be expressed in CIDR notation. Example: \"000.000.000.000/\\[subnet mask\\]\" or optionally the shortened version \"0.0.0.0/\\[subnet mask\\]\"."];
      protocol: IpProtocol.t
        [@ocaml.doc "The network communication protocol used by the fleet."]}
    let context_ = "IpPermission"
    let make ~fromPort =
      fun ~toPort ->
        fun ~ipRange ->
          fun ~protocol -> fun () -> { fromPort; toPort; ipRange; protocol }
    let to_value x =
      structure_to_value
        [("FromPort", (Some (PortNumber.to_value x.fromPort)));
        ("ToPort", (Some (PortNumber.to_value x.toPort)));
        ("IpRange", (Some (IpRange.to_value x.ipRange)));
        ("Protocol", (Some (IpProtocol.to_value x.protocol)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protocol =
        IpProtocol.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Protocol") in
      let ipRange =
        IpRange.of_xml (Xml.child_exn ~context:context_ xml_arg0 "IpRange") in
      let toPort =
        PortNumber.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ToPort") in
      let fromPort =
        PortNumber.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FromPort") in
      make ~protocol ~ipRange ~toPort ~fromPort ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let protocol = field_map_exn json__ "Protocol" IpProtocol.of_json in
      let ipRange = field_map_exn json__ "IpRange" IpRange.of_json in
      let toPort = field_map_exn json__ "ToPort" PortNumber.of_json in
      let fromPort = field_map_exn json__ "FromPort" PortNumber.of_json in
      make ~protocol ~ipRange ~toPort ~fromPort ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A range of IP addresses and port settings that allow inbound traffic to connect to processes on an instance in a fleet. Processes are assigned an IP address/port number combination, which must fall into the fleet's allowed ranges. For Amazon GameLift Servers Realtime fleets, Amazon GameLift Servers automatically opens two port ranges, one for TCP messaging and one for UDP."]
module LogDestination =
  struct
    type nonrec t =
      | NONE 
      | CLOUDWATCH 
      | S3 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NONE -> "NONE"
      | CLOUDWATCH -> "CLOUDWATCH"
      | S3 -> "S3"
      | Non_static_id s -> s
    let of_string =
      function
      | "NONE" -> NONE
      | "CLOUDWATCH" -> CLOUDWATCH
      | "S3" -> S3
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LogDestination" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LogDestination" j)
    let to_json = simple_to_json to_value
  end
module LogGroupArnStringModel =
  struct
    type nonrec t = string
    let context_ = "LogGroupArnStringModel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:512) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9:/\\-\\*]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LogGroupArnStringModel" j
    let to_json = simple_to_json to_value
  end
module MetricGroup =
  struct
    type nonrec t = string
    let context_ = "MetricGroup"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MetricGroup" j
    let to_json = simple_to_json to_value
  end
module ContainerAttribute =
  struct
    type nonrec t =
      {
      containerName: NonZeroAnd128MaxAsciiString.t option
        [@ocaml.doc
          "The identifier for a container that's running in a compute."];
      containerRuntimeId: NonEmptyString.t option
        [@ocaml.doc
          "The runtime ID for the container that's running in a compute. This value is unique within the compute."]}
    let make ?containerName =
      fun ?containerRuntimeId ->
        fun () -> { containerName; containerRuntimeId }
    let to_value x =
      structure_to_value
        [("ContainerName",
           (Option.map x.containerName
              ~f:NonZeroAnd128MaxAsciiString.to_value));
        ("ContainerRuntimeId",
          (Option.map x.containerRuntimeId ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerRuntimeId =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "ContainerRuntimeId") in
      let containerName =
        (Option.map ~f:NonZeroAnd128MaxAsciiString.of_xml)
          (Xml.child xml_arg0 "ContainerName") in
      make ?containerRuntimeId ?containerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let containerRuntimeId =
        field_map json__ "ContainerRuntimeId" NonEmptyString.of_json in
      let containerName =
        field_map json__ "ContainerName" NonZeroAnd128MaxAsciiString.of_json in
      make ?containerRuntimeId ?containerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A unique identifier for a container in a container fleet compute. Returned by: DescribeCompute"]
module FleetId =
  struct
    type nonrec t = string
    let context_ = "FleetId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-z]*fleet-[a-zA-Z0-9\\-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FleetId" j
    let to_json = simple_to_json to_value
  end
module FreeText =
  struct
    type nonrec t = string
    let context_ = "FreeText"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FreeText" j
    let to_json = simple_to_json to_value
  end
module RoutingStrategyType =
  struct
    type nonrec t =
      | SIMPLE 
      | TERMINAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SIMPLE -> "SIMPLE"
      | TERMINAL -> "TERMINAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "SIMPLE" -> SIMPLE
      | "TERMINAL" -> TERMINAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RoutingStrategyType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RoutingStrategyType" j)
    let to_json = simple_to_json to_value
  end
module PlayerConnectionEndpoint =
  struct
    type nonrec t =
      {
      ipAddress: IpAddress.t option
        [@ocaml.doc
          "IP address for connecting to the game session. When player gateway is enabled, this is a player gateway IP address. When player gateway is disabled, this is the game server IP address."];
      port: PortNumber.t option
        [@ocaml.doc
          "Port number for connecting to the game session. When player gateway is enabled, this is a player gateway port. When player gateway is disabled, this is the game server port."]}
    let make ?ipAddress = fun ?port -> fun () -> { ipAddress; port }
    let to_value x =
      structure_to_value
        [("IpAddress", (Option.map x.ipAddress ~f:IpAddress.to_value));
        ("Port", (Option.map x.port ~f:PortNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let port =
        (Option.map ~f:PortNumber.of_xml) (Xml.child xml_arg0 "Port") in
      let ipAddress =
        (Option.map ~f:IpAddress.of_xml) (Xml.child xml_arg0 "IpAddress") in
      make ?port ?ipAddress ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let port = field_map json__ "Port" PortNumber.of_json in
      let ipAddress = field_map json__ "IpAddress" IpAddress.of_json in
      make ?port ?ipAddress ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Network address(es) and port(s) for connecting to a game session."]
module Double =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module DnsName =
  struct
    type nonrec t = string
    let context_ = "DnsName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DnsName" j
    let to_json = simple_to_json to_value
  end
module MatchedPlayerSessionList =
  struct
    type nonrec t = MatchedPlayerSession.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MatchedPlayerSession.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MatchedPlayerSession.of_xml)
    let of_json j =
      list_of_json ~kind:"MatchedPlayerSessionList"
        ~of_json:MatchedPlayerSession.of_json j
    let to_json v = composed_to_json to_value v
  end
module Player =
  struct
    type nonrec t =
      {
      playerId: PlayerId.t option
        [@ocaml.doc "A unique identifier for a player"];
      playerAttributes: PlayerAttributeMap.t option
        [@ocaml.doc
          "A collection of key:value pairs containing player information for use in matchmaking. Player attribute keys must match the playerAttributes used in a matchmaking rule set. Example: \"PlayerAttributes\": \\{\"skill\": \\{\"N\": \"23\"\\}, \"gameMode\": \\{\"S\": \"deathmatch\"\\}\\}. You can provide up to 10 PlayerAttributes."];
      team: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Name of the team that the player is assigned to in a match. Team names are defined in a matchmaking rule set."];
      latencyInMs: LatencyMap.t option
        [@ocaml.doc
          "A set of values, expressed in milliseconds, that indicates the amount of latency that a player experiences when connected to Amazon Web Services Regions. If this property is present, FlexMatch considers placing the match only in Regions for which latency is reported. If a matchmaker has a rule that evaluates player latency, players must report latency in order to be matched. If no latency is reported in this scenario, FlexMatch assumes that no Regions are available to the player and the ticket is not matchable."]}
    let make ?playerId =
      fun ?playerAttributes ->
        fun ?team ->
          fun ?latencyInMs ->
            fun () -> { playerId; playerAttributes; team; latencyInMs }
    let to_value x =
      structure_to_value
        [("PlayerId", (Option.map x.playerId ~f:PlayerId.to_value));
        ("PlayerAttributes",
          (Option.map x.playerAttributes ~f:PlayerAttributeMap.to_value));
        ("Team", (Option.map x.team ~f:NonZeroAndMaxString.to_value));
        ("LatencyInMs", (Option.map x.latencyInMs ~f:LatencyMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let latencyInMs =
        (Option.map ~f:LatencyMap.of_xml) (Xml.child xml_arg0 "LatencyInMs") in
      let team =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Team") in
      let playerAttributes =
        (Option.map ~f:PlayerAttributeMap.of_xml)
          (Xml.child xml_arg0 "PlayerAttributes") in
      let playerId =
        (Option.map ~f:PlayerId.of_xml) (Xml.child xml_arg0 "PlayerId") in
      make ?latencyInMs ?team ?playerAttributes ?playerId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let latencyInMs = field_map json__ "LatencyInMs" LatencyMap.of_json in
      let team = field_map json__ "Team" NonZeroAndMaxString.of_json in
      let playerAttributes =
        field_map json__ "PlayerAttributes" PlayerAttributeMap.of_json in
      let playerId = field_map json__ "PlayerId" PlayerId.of_json in
      make ?latencyInMs ?team ?playerAttributes ?playerId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a player in matchmaking. When starting a matchmaking request, a player has a player ID, attributes, and may have latency data. Team information is added after a match has been successfully completed."]
module LocationList =
  struct
    type nonrec t = LocationStringModel.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:LocationStringModel.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LocationStringModel.of_xml)
    let of_json j =
      list_of_json ~kind:"LocationList" ~of_json:LocationStringModel.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module GameSessionQueueDestination =
  struct
    type nonrec t =
      {
      destinationArn: ArnStringModel.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to fleet or fleet alias. ARNs, which include a fleet ID or alias ID and a Region name, provide a unique identifier across all Regions."]}
    let make ?destinationArn = fun () -> { destinationArn }
    let to_value x =
      structure_to_value
        [("DestinationArn",
           (Option.map x.destinationArn ~f:ArnStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let destinationArn =
        (Option.map ~f:ArnStringModel.of_xml)
          (Xml.child xml_arg0 "DestinationArn") in
      make ?destinationArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let destinationArn =
        field_map json__ "DestinationArn" ArnStringModel.of_json in
      make ?destinationArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A fleet or alias designated in a game session queue. Queues fulfill requests for new game sessions by placing a new game session on any of the queue's destinations."]
module PlayerLatencyPolicy =
  struct
    type nonrec t =
      {
      maximumIndividualPlayerLatencyMilliseconds: WholeNumber.t option
        [@ocaml.doc
          "The maximum latency value that is allowed for any player, in milliseconds. All policies must have a value set for this property."];
      policyDurationSeconds: WholeNumber.t option
        [@ocaml.doc
          "The length of time, in seconds, that the policy is enforced while placing a new game session. A null value for this property means that the policy is enforced until the queue times out."]}
    let make ?maximumIndividualPlayerLatencyMilliseconds =
      fun ?policyDurationSeconds ->
        fun () ->
          { maximumIndividualPlayerLatencyMilliseconds; policyDurationSeconds
          }
    let to_value x =
      structure_to_value
        [("MaximumIndividualPlayerLatencyMilliseconds",
           (Option.map x.maximumIndividualPlayerLatencyMilliseconds
              ~f:WholeNumber.to_value));
        ("PolicyDurationSeconds",
          (Option.map x.policyDurationSeconds ~f:WholeNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyDurationSeconds =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "PolicyDurationSeconds") in
      let maximumIndividualPlayerLatencyMilliseconds =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "MaximumIndividualPlayerLatencyMilliseconds") in
      make ?policyDurationSeconds ?maximumIndividualPlayerLatencyMilliseconds
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyDurationSeconds =
        field_map json__ "PolicyDurationSeconds" WholeNumber.of_json in
      let maximumIndividualPlayerLatencyMilliseconds =
        field_map json__ "MaximumIndividualPlayerLatencyMilliseconds"
          WholeNumber.of_json in
      make ?policyDurationSeconds ?maximumIndividualPlayerLatencyMilliseconds
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sets a latency cap for individual players when placing a game session. With a latency policy in force, a game session cannot be placed in a fleet location where a player reports latency higher than the cap. Latency policies are used only with placement request that provide player latency information. Player latency policies can be stacked to gradually relax latency requirements over time."]
module PriorityTypeList =
  struct
    type nonrec t = PriorityType.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:4) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PriorityType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PriorityType.of_xml)
    let of_json j =
      list_of_json ~kind:"PriorityTypeList" ~of_json:PriorityType.of_json j
    let to_json v = composed_to_json to_value v
  end
module ComputeName =
  struct
    type nonrec t = string
    let context_ = "ComputeName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^[a-zA-Z0-9\\-]+(\\/[a-zA-Z0-9\\-]+)?")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ComputeName" j
    let to_json = simple_to_json to_value
  end
module FleetArn =
  struct
    type nonrec t = string
    let context_ = "FleetArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:512) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:.*:[a-z]*fleet\\/[a-z]*fleet-[a-zA-Z0-9\\-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FleetArn" j
    let to_json = simple_to_json to_value
  end
module GamePropertyList =
  struct
    type nonrec t = GameProperty.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:16); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GameProperty.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GameProperty.of_xml)
    let of_json j =
      list_of_json ~kind:"GamePropertyList" ~of_json:GameProperty.of_json j
    let to_json v = composed_to_json to_value v
  end
module GameSessionStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | ACTIVATING 
      | TERMINATED 
      | TERMINATING 
      | ERROR 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | ACTIVATING -> "ACTIVATING"
      | TERMINATED -> "TERMINATED"
      | TERMINATING -> "TERMINATING"
      | ERROR -> "ERROR"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "ACTIVATING" -> ACTIVATING
      | "TERMINATED" -> TERMINATED
      | "TERMINATING" -> TERMINATING
      | "ERROR" -> ERROR
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GameSessionStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GameSessionStatus" j)
    let to_json = simple_to_json to_value
  end
module GameSessionStatusReason =
  struct
    type nonrec t =
      | INTERRUPTED 
      | TRIGGERED_ON_PROCESS_TERMINATE 
      | FORCE_TERMINATED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INTERRUPTED -> "INTERRUPTED"
      | TRIGGERED_ON_PROCESS_TERMINATE -> "TRIGGERED_ON_PROCESS_TERMINATE"
      | FORCE_TERMINATED -> "FORCE_TERMINATED"
      | Non_static_id s -> s
    let of_string =
      function
      | "INTERRUPTED" -> INTERRUPTED
      | "TRIGGERED_ON_PROCESS_TERMINATE" -> TRIGGERED_ON_PROCESS_TERMINATE
      | "FORCE_TERMINATED" -> FORCE_TERMINATED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GameSessionStatusReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GameSessionStatusReason" j)
    let to_json = simple_to_json to_value
  end
module LargeGameSessionData =
  struct
    type nonrec t = string
    let context_ = "LargeGameSessionData"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:262144) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LargeGameSessionData" j
    let to_json = simple_to_json to_value
  end
module MatchmakerData =
  struct
    type nonrec t = string
    let context_ = "MatchmakerData"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:390000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MatchmakerData" j
    let to_json = simple_to_json to_value
  end
module PlayerSessionCreationPolicy =
  struct
    type nonrec t =
      | ACCEPT_ALL 
      | DENY_ALL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACCEPT_ALL -> "ACCEPT_ALL"
      | DENY_ALL -> "DENY_ALL"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACCEPT_ALL" -> ACCEPT_ALL
      | "DENY_ALL" -> DENY_ALL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PlayerSessionCreationPolicy"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PlayerSessionCreationPolicy" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module FleetAction =
  struct
    type nonrec t =
      | AUTO_SCALING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | AUTO_SCALING -> "AUTO_SCALING" | Non_static_id s -> s
    let of_string =
      function | "AUTO_SCALING" -> AUTO_SCALING | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FleetAction" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FleetAction" j)
    let to_json = simple_to_json to_value
  end
module FleetStatus =
  struct
    type nonrec t =
      | NEW 
      | DOWNLOADING 
      | VALIDATING 
      | BUILDING 
      | ACTIVATING 
      | ACTIVE 
      | DELETING 
      | ERROR 
      | TERMINATED 
      | NOT_FOUND 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NEW -> "NEW"
      | DOWNLOADING -> "DOWNLOADING"
      | VALIDATING -> "VALIDATING"
      | BUILDING -> "BUILDING"
      | ACTIVATING -> "ACTIVATING"
      | ACTIVE -> "ACTIVE"
      | DELETING -> "DELETING"
      | ERROR -> "ERROR"
      | TERMINATED -> "TERMINATED"
      | NOT_FOUND -> "NOT_FOUND"
      | Non_static_id s -> s
    let of_string =
      function
      | "NEW" -> NEW
      | "DOWNLOADING" -> DOWNLOADING
      | "VALIDATING" -> VALIDATING
      | "BUILDING" -> BUILDING
      | "ACTIVATING" -> ACTIVATING
      | "ACTIVE" -> ACTIVE
      | "DELETING" -> DELETING
      | "ERROR" -> ERROR
      | "TERMINATED" -> TERMINATED
      | "NOT_FOUND" -> NOT_FOUND
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FleetStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FleetStatus" j)
    let to_json = simple_to_json to_value
  end
module ScaleInAfterInactivityMinutes =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1440) >>= (fun () -> check_int_min i ~min:5));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ScaleInAfterInactivityMinutes"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ZeroCapacityStrategy =
  struct
    type nonrec t =
      | MANUAL 
      | SCALE_TO_AND_FROM_ZERO 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MANUAL -> "MANUAL"
      | SCALE_TO_AND_FROM_ZERO -> "SCALE_TO_AND_FROM_ZERO"
      | Non_static_id s -> s
    let of_string =
      function
      | "MANUAL" -> MANUAL
      | "SCALE_TO_AND_FROM_ZERO" -> SCALE_TO_AND_FROM_ZERO
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ZeroCapacityStrategy" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ZeroCapacityStrategy" j)
    let to_json = simple_to_json to_value
  end
module NonNegativeLimitedLengthDouble =
  struct
    type nonrec t = string
    let context_ = "NonNegativeLimitedLengthDouble"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:11) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^\\d{1,5}(?:\\.\\d{1,5})?$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NonNegativeLimitedLengthDouble" j
    let to_json = simple_to_json to_value
  end
module CertificateType =
  struct
    type nonrec t =
      | DISABLED 
      | GENERATED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DISABLED -> "DISABLED"
      | GENERATED -> "GENERATED"
      | Non_static_id s -> s
    let of_string =
      function
      | "DISABLED" -> DISABLED
      | "GENERATED" -> GENERATED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CertificateType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CertificateType" j)
    let to_json = simple_to_json to_value
  end
module GameServerIpProtocolSupported =
  struct
    type nonrec t =
      | IPv4 
      | DUAL_STACK 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | IPv4 -> "IPv4"
      | DUAL_STACK -> "DUAL_STACK"
      | Non_static_id s -> s
    let of_string =
      function
      | "IPv4" -> IPv4
      | "DUAL_STACK" -> DUAL_STACK
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GameServerIpProtocolSupported"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GameServerIpProtocolSupported" j)
    let to_json = simple_to_json to_value
  end
module ContainerPortMapping =
  struct
    type nonrec t =
      {
      containerPort: PortNumber.t option
        [@ocaml.doc
          "The port number on the container. This port is defined in the container group definition. Container port numbers must be unique within a container group definition."];
      connectionPort: PortNumber.t option
        [@ocaml.doc
          "The port number on the fleet instance that maps to the container port. Connection ports are assigned by Amazon GameLift Servers when the container group is deployed to an instance."];
      protocol: IpProtocol.t option
        [@ocaml.doc
          "The network protocol for the port mapping. Valid values are TCP or UDP."]}
    let make ?containerPort =
      fun ?connectionPort ->
        fun ?protocol ->
          fun () -> { containerPort; connectionPort; protocol }
    let to_value x =
      structure_to_value
        [("ContainerPort",
           (Option.map x.containerPort ~f:PortNumber.to_value));
        ("ConnectionPort",
          (Option.map x.connectionPort ~f:PortNumber.to_value));
        ("Protocol", (Option.map x.protocol ~f:IpProtocol.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protocol =
        (Option.map ~f:IpProtocol.of_xml) (Xml.child xml_arg0 "Protocol") in
      let connectionPort =
        (Option.map ~f:PortNumber.of_xml)
          (Xml.child xml_arg0 "ConnectionPort") in
      let containerPort =
        (Option.map ~f:PortNumber.of_xml)
          (Xml.child xml_arg0 "ContainerPort") in
      make ?protocol ?connectionPort ?containerPort ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let protocol = field_map json__ "Protocol" IpProtocol.of_json in
      let connectionPort =
        field_map json__ "ConnectionPort" PortNumber.of_json in
      let containerPort = field_map json__ "ContainerPort" PortNumber.of_json in
      make ?protocol ?connectionPort ?containerPort ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a mapping between a container port and a connection port on a fleet instance. You define container ports in a container group definition. Amazon GameLift Servers assigns connection ports when it deploys the container group to an instance. Part of: ContainerGroupPortMapping"]
module ServerProcess =
  struct
    type nonrec t =
      {
      launchPath: LaunchPathStringModel.t
        [@ocaml.doc
          "The location of a game build executable or Realtime script. Game builds and Realtime scripts are installed on instances at the root: Windows (custom game builds only): C:\\game. Example: \"C:\\game\\MyGame\\server.exe\" Linux: /local/game. Examples: \"/local/game/MyGame/server.exe\" or \"/local/game/MyRealtimeScript.js\" Amazon GameLift Servers doesn't support the use of setup scripts that launch the game executable. For custom game builds, this parameter must indicate the executable that calls the server SDK operations initSDK() and ProcessReady()."];
      parameters: LaunchParametersStringModel.t option
        [@ocaml.doc
          "An optional list of parameters to pass to the server executable or Realtime script on launch."];
      concurrentExecutions: PositiveInteger.t
        [@ocaml.doc
          "The number of server processes using this configuration that run concurrently on each instance or compute."]}
    let context_ = "ServerProcess"
    let make ?parameters =
      fun ~launchPath ->
        fun ~concurrentExecutions ->
          fun () -> { parameters; launchPath; concurrentExecutions }
    let to_value x =
      structure_to_value
        [("LaunchPath", (Some (LaunchPathStringModel.to_value x.launchPath)));
        ("Parameters",
          (Option.map x.parameters ~f:LaunchParametersStringModel.to_value));
        ("ConcurrentExecutions",
          (Some (PositiveInteger.to_value x.concurrentExecutions)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let concurrentExecutions =
        PositiveInteger.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConcurrentExecutions") in
      let parameters =
        (Option.map ~f:LaunchParametersStringModel.of_xml)
          (Xml.child xml_arg0 "Parameters") in
      let launchPath =
        LaunchPathStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchPath") in
      make ~concurrentExecutions ?parameters ~launchPath ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let concurrentExecutions =
        field_map_exn json__ "ConcurrentExecutions" PositiveInteger.of_json in
      let parameters =
        field_map json__ "Parameters" LaunchParametersStringModel.of_json in
      let launchPath =
        field_map_exn json__ "LaunchPath" LaunchPathStringModel.of_json in
      make ~concurrentExecutions ?parameters ~launchPath ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A set of instructions for launching server processes on fleet computes. Server processes run either an executable in a custom game build or a Amazon GameLift Servers Realtime script. Server process configurations are part of a fleet's runtime configuration."]
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module PlacedPlayerSession =
  struct
    type nonrec t =
      {
      playerId: PlayerId.t option
        [@ocaml.doc
          "A unique identifier for a player that is associated with this player session."];
      playerSessionId: PlayerSessionId.t option
        [@ocaml.doc "A unique identifier for a player session."]}
    let make ?playerId =
      fun ?playerSessionId -> fun () -> { playerId; playerSessionId }
    let to_value x =
      structure_to_value
        [("PlayerId", (Option.map x.playerId ~f:PlayerId.to_value));
        ("PlayerSessionId",
          (Option.map x.playerSessionId ~f:PlayerSessionId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerSessionId =
        (Option.map ~f:PlayerSessionId.of_xml)
          (Xml.child xml_arg0 "PlayerSessionId") in
      let playerId =
        (Option.map ~f:PlayerId.of_xml) (Xml.child xml_arg0 "PlayerId") in
      make ?playerSessionId ?playerId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerSessionId =
        field_map json__ "PlayerSessionId" PlayerSessionId.of_json in
      let playerId = field_map json__ "PlayerId" PlayerId.of_json in
      make ?playerSessionId ?playerId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a player session. This object contains only the player ID and player session ID. To retrieve full details on a player session, call DescribePlayerSessions with the player session ID."]
module PlayerLatency =
  struct
    type nonrec t =
      {
      playerId: PlayerId.t option
        [@ocaml.doc
          "A unique identifier for a player associated with the latency data."];
      regionIdentifier: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Name of the Region that is associated with the latency value."];
      latencyInMilliseconds: Float_.t option
        [@ocaml.doc
          "Amount of time that represents the time lag experienced by the player when connected to the specified Region."]}
    let make ?playerId =
      fun ?regionIdentifier ->
        fun ?latencyInMilliseconds ->
          fun () -> { playerId; regionIdentifier; latencyInMilliseconds }
    let to_value x =
      structure_to_value
        [("PlayerId", (Option.map x.playerId ~f:PlayerId.to_value));
        ("RegionIdentifier",
          (Option.map x.regionIdentifier ~f:NonZeroAndMaxString.to_value));
        ("LatencyInMilliseconds",
          (Option.map x.latencyInMilliseconds ~f:Float_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let latencyInMilliseconds =
        (Option.map ~f:Float_.of_xml)
          (Xml.child xml_arg0 "LatencyInMilliseconds") in
      let regionIdentifier =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "RegionIdentifier") in
      let playerId =
        (Option.map ~f:PlayerId.of_xml) (Xml.child xml_arg0 "PlayerId") in
      make ?latencyInMilliseconds ?regionIdentifier ?playerId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let latencyInMilliseconds =
        field_map json__ "LatencyInMilliseconds" Float_.of_json in
      let regionIdentifier =
        field_map json__ "RegionIdentifier" NonZeroAndMaxString.of_json in
      let playerId = field_map json__ "PlayerId" PlayerId.of_json in
      make ?latencyInMilliseconds ?regionIdentifier ?playerId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Regional latency information for a player, used when requesting a new game session. This value indicates the amount of time lag that exists when the player is connected to a fleet in the specified Region. The relative difference between a player's latency values for multiple Regions are used to determine which fleets are best suited to place a new game session for the player."]
module LocationOrderOverrideList =
  struct
    type nonrec t = LocationStringModel.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:LocationStringModel.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LocationStringModel.of_xml)
    let of_json j =
      list_of_json ~kind:"LocationOrderOverrideList"
        ~of_json:LocationStringModel.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlacementFallbackStrategy =
  struct
    type nonrec t =
      | DEFAULT_AFTER_SINGLE_PASS 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DEFAULT_AFTER_SINGLE_PASS -> "DEFAULT_AFTER_SINGLE_PASS"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "DEFAULT_AFTER_SINGLE_PASS" -> DEFAULT_AFTER_SINGLE_PASS
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PlacementFallbackStrategy" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PlacementFallbackStrategy" j)
    let to_json = simple_to_json to_value
  end
module PlayerData =
  struct
    type nonrec t = string
    let context_ = "PlayerData"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PlayerData" j
    let to_json = simple_to_json to_value
  end
module NodeJsVersion =
  struct
    type nonrec t = string
    let context_ = "NodeJsVersion"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:16) >>=
             (fun () -> check_pattern i ~pattern:"^[0-9]+\\.(?:x|[0-9]+)$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NodeJsVersion" j
    let to_json = simple_to_json to_value
  end
module S3Location =
  struct
    type nonrec t =
      {
      bucket: NonEmptyString.t option
        [@ocaml.doc
          "An Amazon S3 bucket identifier. Thename of the S3 bucket. Amazon GameLift Servers doesn't support uploading from Amazon S3 buckets with names that contain a dot (.)."];
      key: NonEmptyString.t option
        [@ocaml.doc
          "The name of the zip file that contains the build files or script files."];
      roleArn: NonEmptyString.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) for an IAM role that allows Amazon GameLift Servers to access the S3 bucket."];
      objectVersion: NonEmptyString.t option
        [@ocaml.doc
          "The version of the file, if object versioning is turned on for the bucket. Amazon GameLift Servers uses this information when retrieving files from an S3 bucket that you own. Use this parameter to specify a specific version of the file. If not set, the latest version of the file is retrieved."]}
    let make ?bucket =
      fun ?key ->
        fun ?roleArn ->
          fun ?objectVersion ->
            fun () -> { bucket; key; roleArn; objectVersion }
    let to_value x =
      structure_to_value
        [("Bucket", (Option.map x.bucket ~f:NonEmptyString.to_value));
        ("Key", (Option.map x.key ~f:NonEmptyString.to_value));
        ("RoleArn", (Option.map x.roleArn ~f:NonEmptyString.to_value));
        ("ObjectVersion",
          (Option.map x.objectVersion ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let objectVersion =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "ObjectVersion") in
      let roleArn =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "RoleArn") in
      let key =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Key") in
      let bucket =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Bucket") in
      make ?objectVersion ?roleArn ?key ?bucket ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let objectVersion =
        field_map json__ "ObjectVersion" NonEmptyString.of_json in
      let roleArn = field_map json__ "RoleArn" NonEmptyString.of_json in
      let key = field_map json__ "Key" NonEmptyString.of_json in
      let bucket = field_map json__ "Bucket" NonEmptyString.of_json in
      make ?objectVersion ?roleArn ?key ?bucket ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The location in Amazon S3 where build or script files are stored for access by Amazon GameLift Servers."]
module ScriptArn =
  struct
    type nonrec t = string
    let context_ = "ScriptArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^arn:.*:script\\/script-\\S+");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ScriptArn" j
    let to_json = simple_to_json to_value
  end
module ScriptId =
  struct
    type nonrec t = string
    let context_ = "ScriptId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"^script-\\S+"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ScriptId" j
    let to_json = simple_to_json to_value
  end
module WholeNumberLong =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in ok_or_failwith (check_int64_min i ~min:0L); i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module LocationArnModel =
  struct
    type nonrec t = string
    let context_ = "LocationArnModel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:.*:location\\/custom-\\S+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LocationArnModel" j
    let to_json = simple_to_json to_value
  end
module PingBeacon =
  struct
    type nonrec t =
      {
      uDPEndpoint: UDPEndpoint.t option
        [@ocaml.doc
          "The domain name and port of the UDP ping beacon. Your game client can send UDP messages to this endpoint and receive responses to measure network latency."]}
    let make ?uDPEndpoint = fun () -> { uDPEndpoint }
    let to_value x =
      structure_to_value
        [("UDPEndpoint", (Option.map x.uDPEndpoint ~f:UDPEndpoint.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uDPEndpoint =
        (Option.map ~f:UDPEndpoint.of_xml) (Xml.child xml_arg0 "UDPEndpoint") in
      make ?uDPEndpoint ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uDPEndpoint = field_map json__ "UDPEndpoint" UDPEndpoint.of_json in
      make ?uDPEndpoint ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a UDP ping beacon that can be used to measure network latency between a player device and an Amazon GameLift Servers hosting location."]
module GameServerClaimStatus =
  struct
    type nonrec t =
      | CLAIMED 
      | Non_static_id of string 
    let make i = i
    let to_string = function | CLAIMED -> "CLAIMED" | Non_static_id s -> s
    let of_string = function | "CLAIMED" -> CLAIMED | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GameServerClaimStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GameServerClaimStatus" j)
    let to_json = simple_to_json to_value
  end
module GameServerConnectionInfo =
  struct
    type nonrec t = string
    let context_ = "GameServerConnectionInfo"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:512) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GameServerConnectionInfo" j
    let to_json = simple_to_json to_value
  end
module GameServerData =
  struct
    type nonrec t = string
    let context_ = "GameServerData"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GameServerData" j
    let to_json = simple_to_json to_value
  end
module GameServerGroupArn =
  struct
    type nonrec t = string
    let context_ = "GameServerGroupArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:.*:gameservergroup\\/[a-zA-Z0-9-\\.]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GameServerGroupArn" j
    let to_json = simple_to_json to_value
  end
module GameServerGroupName =
  struct
    type nonrec t = string
    let context_ = "GameServerGroupName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-\\.]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GameServerGroupName" j
    let to_json = simple_to_json to_value
  end
module GameServerId =
  struct
    type nonrec t = string
    let context_ = "GameServerId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-\\.]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GameServerId" j
    let to_json = simple_to_json to_value
  end
module GameServerInstanceId =
  struct
    type nonrec t = string
    let context_ = "GameServerInstanceId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:19) >>=
             (fun () ->
                (check_string_max i ~max:19) >>=
                  (fun () -> check_pattern i ~pattern:"^i-[0-9a-zA-Z]{17}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GameServerInstanceId" j
    let to_json = simple_to_json to_value
  end
module GameServerUtilizationStatus =
  struct
    type nonrec t =
      | AVAILABLE 
      | UTILIZED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AVAILABLE -> "AVAILABLE"
      | UTILIZED -> "UTILIZED"
      | Non_static_id s -> s
    let of_string =
      function
      | "AVAILABLE" -> AVAILABLE
      | "UTILIZED" -> UTILIZED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GameServerUtilizationStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GameServerUtilizationStatus" j)
    let to_json = simple_to_json to_value
  end
module AutoScalingGroupArn =
  struct
    type nonrec t = string
    let context_ = "AutoScalingGroupArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AutoScalingGroupArn" j
    let to_json = simple_to_json to_value
  end
module BalancingStrategy =
  struct
    type nonrec t =
      | SPOT_ONLY 
      | SPOT_PREFERRED 
      | ON_DEMAND_ONLY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SPOT_ONLY -> "SPOT_ONLY"
      | SPOT_PREFERRED -> "SPOT_PREFERRED"
      | ON_DEMAND_ONLY -> "ON_DEMAND_ONLY"
      | Non_static_id s -> s
    let of_string =
      function
      | "SPOT_ONLY" -> SPOT_ONLY
      | "SPOT_PREFERRED" -> SPOT_PREFERRED
      | "ON_DEMAND_ONLY" -> ON_DEMAND_ONLY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration BalancingStrategy" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BalancingStrategy" j)
    let to_json = simple_to_json to_value
  end
module GameServerGroupActions =
  struct
    type nonrec t = GameServerGroupAction.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GameServerGroupAction.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GameServerGroupAction.of_xml)
    let of_json j =
      list_of_json ~kind:"GameServerGroupActions"
        ~of_json:GameServerGroupAction.of_json j
    let to_json v = composed_to_json to_value v
  end
module GameServerGroupStatus =
  struct
    type nonrec t =
      | NEW 
      | ACTIVATING 
      | ACTIVE 
      | DELETE_SCHEDULED 
      | DELETING 
      | DELETED 
      | ERROR 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NEW -> "NEW"
      | ACTIVATING -> "ACTIVATING"
      | ACTIVE -> "ACTIVE"
      | DELETE_SCHEDULED -> "DELETE_SCHEDULED"
      | DELETING -> "DELETING"
      | DELETED -> "DELETED"
      | ERROR -> "ERROR"
      | Non_static_id s -> s
    let of_string =
      function
      | "NEW" -> NEW
      | "ACTIVATING" -> ACTIVATING
      | "ACTIVE" -> ACTIVE
      | "DELETE_SCHEDULED" -> DELETE_SCHEDULED
      | "DELETING" -> DELETING
      | "DELETED" -> DELETED
      | "ERROR" -> ERROR
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GameServerGroupStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GameServerGroupStatus" j)
    let to_json = simple_to_json to_value
  end
module GameServerProtectionPolicy =
  struct
    type nonrec t =
      | NO_PROTECTION 
      | FULL_PROTECTION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NO_PROTECTION -> "NO_PROTECTION"
      | FULL_PROTECTION -> "FULL_PROTECTION"
      | Non_static_id s -> s
    let of_string =
      function
      | "NO_PROTECTION" -> NO_PROTECTION
      | "FULL_PROTECTION" -> FULL_PROTECTION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GameServerProtectionPolicy"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GameServerProtectionPolicy" j)
    let to_json = simple_to_json to_value
  end
module IamRoleArn =
  struct
    type nonrec t = string
    let context_ = "IamRoleArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^arn:.*:role\\/[\\w+=,.@-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IamRoleArn" j
    let to_json = simple_to_json to_value
  end
module InstanceDefinitions =
  struct
    type nonrec t = InstanceDefinition.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:20) >>= (fun () -> check_list_min i ~min:2));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:InstanceDefinition.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceDefinition.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceDefinitions"
        ~of_json:InstanceDefinition.of_json j
    let to_json v = composed_to_json to_value v
  end
module DeploymentConfiguration =
  struct
    type nonrec t =
      {
      protectionStrategy: DeploymentProtectionStrategy.t option
        [@ocaml.doc
          "Determines how fleet deployment activity affects active game sessions on the fleet. With protection, a deployment honors game session protection, and delays actions that would interrupt a protected active game session until the game session ends. Without protection, deployment activity can shut down all running tasks, including active game sessions, regardless of game session protection."];
      minimumHealthyPercentage: MinimumHealthyPercentage.t option
        [@ocaml.doc
          "Sets a minimum level of healthy tasks to maintain during deployment activity."];
      impairmentStrategy: DeploymentImpairmentStrategy.t option
        [@ocaml.doc
          "Determines what actions to take if a deployment fails. If the fleet is multi-location, this strategy applies across all fleet locations. With a rollback strategy, updated fleet instances are rolled back to the last successful deployment. Alternatively, you can maintain a few impaired containers for the purpose of debugging, while all other tasks return to the last successful deployment."]}
    let make ?protectionStrategy =
      fun ?minimumHealthyPercentage ->
        fun ?impairmentStrategy ->
          fun () ->
            {
              protectionStrategy;
              minimumHealthyPercentage;
              impairmentStrategy
            }
    let to_value x =
      structure_to_value
        [("ProtectionStrategy",
           (Option.map x.protectionStrategy
              ~f:DeploymentProtectionStrategy.to_value));
        ("MinimumHealthyPercentage",
          (Option.map x.minimumHealthyPercentage
             ~f:MinimumHealthyPercentage.to_value));
        ("ImpairmentStrategy",
          (Option.map x.impairmentStrategy
             ~f:DeploymentImpairmentStrategy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let impairmentStrategy =
        (Option.map ~f:DeploymentImpairmentStrategy.of_xml)
          (Xml.child xml_arg0 "ImpairmentStrategy") in
      let minimumHealthyPercentage =
        (Option.map ~f:MinimumHealthyPercentage.of_xml)
          (Xml.child xml_arg0 "MinimumHealthyPercentage") in
      let protectionStrategy =
        (Option.map ~f:DeploymentProtectionStrategy.of_xml)
          (Xml.child xml_arg0 "ProtectionStrategy") in
      make ?impairmentStrategy ?minimumHealthyPercentage ?protectionStrategy
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let impairmentStrategy =
        field_map json__ "ImpairmentStrategy"
          DeploymentImpairmentStrategy.of_json in
      let minimumHealthyPercentage =
        field_map json__ "MinimumHealthyPercentage"
          MinimumHealthyPercentage.of_json in
      let protectionStrategy =
        field_map json__ "ProtectionStrategy"
          DeploymentProtectionStrategy.of_json in
      make ?impairmentStrategy ?minimumHealthyPercentage ?protectionStrategy
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Set of rules for processing a deployment for a container fleet update."]
module DeploymentStatus =
  struct
    type nonrec t =
      | IN_PROGRESS 
      | IMPAIRED 
      | COMPLETE 
      | ROLLBACK_IN_PROGRESS 
      | ROLLBACK_COMPLETE 
      | CANCELLED 
      | PENDING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | IN_PROGRESS -> "IN_PROGRESS"
      | IMPAIRED -> "IMPAIRED"
      | COMPLETE -> "COMPLETE"
      | ROLLBACK_IN_PROGRESS -> "ROLLBACK_IN_PROGRESS"
      | ROLLBACK_COMPLETE -> "ROLLBACK_COMPLETE"
      | CANCELLED -> "CANCELLED"
      | PENDING -> "PENDING"
      | Non_static_id s -> s
    let of_string =
      function
      | "IN_PROGRESS" -> IN_PROGRESS
      | "IMPAIRED" -> IMPAIRED
      | "COMPLETE" -> COMPLETE
      | "ROLLBACK_IN_PROGRESS" -> ROLLBACK_IN_PROGRESS
      | "ROLLBACK_COMPLETE" -> ROLLBACK_COMPLETE
      | "CANCELLED" -> CANCELLED
      | "PENDING" -> PENDING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DeploymentStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DeploymentStatus" j)
    let to_json = simple_to_json to_value
  end
module FleetBinaryArn =
  struct
    type nonrec t = string
    let context_ = "FleetBinaryArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9:/-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FleetBinaryArn" j
    let to_json = simple_to_json to_value
  end
module ContainerGroupDefinitionArn =
  struct
    type nonrec t = string
    let context_ = "ContainerGroupDefinitionArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:512) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:.*:containergroupdefinition\\/[a-zA-Z0-9\\-]+(:[0-9]+)?$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ContainerGroupDefinitionArn" j
    let to_json = simple_to_json to_value
  end
module ContainerGroupDefinitionName =
  struct
    type nonrec t = string
    let context_ = "ContainerGroupDefinitionName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9\\-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ContainerGroupDefinitionName" j
    let to_json = simple_to_json to_value
  end
module ContainerGroupDefinitionStatus =
  struct
    type nonrec t =
      | READY 
      | COPYING 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | READY -> "READY"
      | COPYING -> "COPYING"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "READY" -> READY
      | "COPYING" -> COPYING
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ContainerGroupDefinitionStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ContainerGroupDefinitionStatus" j)
    let to_json = simple_to_json to_value
  end
module ContainerGroupType =
  struct
    type nonrec t =
      | GAME_SERVER 
      | PER_INSTANCE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GAME_SERVER -> "GAME_SERVER"
      | PER_INSTANCE -> "PER_INSTANCE"
      | Non_static_id s -> s
    let of_string =
      function
      | "GAME_SERVER" -> GAME_SERVER
      | "PER_INSTANCE" -> PER_INSTANCE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ContainerGroupType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ContainerGroupType" j)
    let to_json = simple_to_json to_value
  end
module ContainerOperatingSystem =
  struct
    type nonrec t =
      | AMAZON_LINUX_2023 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AMAZON_LINUX_2023 -> "AMAZON_LINUX_2023"
      | Non_static_id s -> s
    let of_string =
      function
      | "AMAZON_LINUX_2023" -> AMAZON_LINUX_2023
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ContainerOperatingSystem" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ContainerOperatingSystem" j)
    let to_json = simple_to_json to_value
  end
module ContainerTotalMemoryLimit =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1024000) >>=
             (fun () -> check_int_min i ~min:4));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ContainerTotalMemoryLimit"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ContainerTotalVcpuLimit =
  struct
    type nonrec t = float
    let make i =
      let open Result in
        ok_or_failwith
          ((check_float_min i ~min:10.) >>=
             (fun () -> check_float_min i ~min:0.125));
        i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module GameServerContainerDefinition =
  struct
    type nonrec t =
      {
      containerName: NonZeroAnd128MaxAsciiString.t option
        [@ocaml.doc
          "The container definition identifier. Container names are unique within a container group definition."];
      dependsOn: ContainerDependencyList.t option
        [@ocaml.doc
          "Indicates that the container relies on the status of other containers in the same container group during startup and shutdown sequences. A container might have dependencies on multiple containers."];
      mountPoints: ContainerMountPointList.t option
        [@ocaml.doc
          "A mount point that binds a path inside the container to a file or directory on the host system and lets it access the file or directory."];
      environmentOverride: ContainerEnvironmentList.t option
        [@ocaml.doc
          "A set of environment variables that's passed to the container on startup. See the ContainerDefinition::environment parameter in the Amazon Elastic Container Service API Reference."];
      imageUri: ImageUriString.t option
        [@ocaml.doc
          "The URI to the image that Amazon GameLift Servers uses when deploying this container to a container fleet. For a more specific identifier, see ResolvedImageDigest."];
      portConfiguration: ContainerPortConfiguration.t option
        [@ocaml.doc
          "The set of ports that are available to bind to processes in the container. For example, a game server process requires a container port to allow game clients to connect to it. Container ports aren't directly accessed by inbound traffic. Amazon GameLift Servers maps these container ports to externally accessible connection ports, which are assigned as needed from the container fleet's ConnectionPortRange."];
      resolvedImageDigest: Sha256.t option
        [@ocaml.doc
          "A unique and immutable identifier for the container image. The digest is a SHA 256 hash of the container image manifest."];
      serverSdkVersion: ServerSdkVersion.t option
        [@ocaml.doc
          "The Amazon GameLift Servers server SDK version that the game server is integrated with. Only game servers using 5.2.0 or higher are compatible with container fleets."]}
    let make ?containerName =
      fun ?dependsOn ->
        fun ?mountPoints ->
          fun ?environmentOverride ->
            fun ?imageUri ->
              fun ?portConfiguration ->
                fun ?resolvedImageDigest ->
                  fun ?serverSdkVersion ->
                    fun () ->
                      {
                        containerName;
                        dependsOn;
                        mountPoints;
                        environmentOverride;
                        imageUri;
                        portConfiguration;
                        resolvedImageDigest;
                        serverSdkVersion
                      }
    let to_value x =
      structure_to_value
        [("ContainerName",
           (Option.map x.containerName
              ~f:NonZeroAnd128MaxAsciiString.to_value));
        ("DependsOn",
          (Option.map x.dependsOn ~f:ContainerDependencyList.to_value));
        ("MountPoints",
          (Option.map x.mountPoints ~f:ContainerMountPointList.to_value));
        ("EnvironmentOverride",
          (Option.map x.environmentOverride
             ~f:ContainerEnvironmentList.to_value));
        ("ImageUri", (Option.map x.imageUri ~f:ImageUriString.to_value));
        ("PortConfiguration",
          (Option.map x.portConfiguration
             ~f:ContainerPortConfiguration.to_value));
        ("ResolvedImageDigest",
          (Option.map x.resolvedImageDigest ~f:Sha256.to_value));
        ("ServerSdkVersion",
          (Option.map x.serverSdkVersion ~f:ServerSdkVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serverSdkVersion =
        (Option.map ~f:ServerSdkVersion.of_xml)
          (Xml.child xml_arg0 "ServerSdkVersion") in
      let resolvedImageDigest =
        (Option.map ~f:Sha256.of_xml)
          (Xml.child xml_arg0 "ResolvedImageDigest") in
      let portConfiguration =
        (Option.map ~f:ContainerPortConfiguration.of_xml)
          (Xml.child xml_arg0 "PortConfiguration") in
      let imageUri =
        (Option.map ~f:ImageUriString.of_xml) (Xml.child xml_arg0 "ImageUri") in
      let environmentOverride =
        (Option.map ~f:ContainerEnvironmentList.of_xml)
          (Xml.child xml_arg0 "EnvironmentOverride") in
      let mountPoints =
        (Option.map ~f:ContainerMountPointList.of_xml)
          (Xml.child xml_arg0 "MountPoints") in
      let dependsOn =
        (Option.map ~f:ContainerDependencyList.of_xml)
          (Xml.child xml_arg0 "DependsOn") in
      let containerName =
        (Option.map ~f:NonZeroAnd128MaxAsciiString.of_xml)
          (Xml.child xml_arg0 "ContainerName") in
      make ?serverSdkVersion ?resolvedImageDigest ?portConfiguration
        ?imageUri ?environmentOverride ?mountPoints ?dependsOn ?containerName
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serverSdkVersion =
        field_map json__ "ServerSdkVersion" ServerSdkVersion.of_json in
      let resolvedImageDigest =
        field_map json__ "ResolvedImageDigest" Sha256.of_json in
      let portConfiguration =
        field_map json__ "PortConfiguration"
          ContainerPortConfiguration.of_json in
      let imageUri = field_map json__ "ImageUri" ImageUriString.of_json in
      let environmentOverride =
        field_map json__ "EnvironmentOverride"
          ContainerEnvironmentList.of_json in
      let mountPoints =
        field_map json__ "MountPoints" ContainerMountPointList.of_json in
      let dependsOn =
        field_map json__ "DependsOn" ContainerDependencyList.of_json in
      let containerName =
        field_map json__ "ContainerName" NonZeroAnd128MaxAsciiString.of_json in
      make ?serverSdkVersion ?resolvedImageDigest ?portConfiguration
        ?imageUri ?environmentOverride ?mountPoints ?dependsOn ?containerName
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the game server container in an existing game server container group. A game server container identifies a container image with your game server build. A game server container is automatically considered essential; if an essential container fails, the entire container group restarts. You can update a container definition and deploy the updates to an existing fleet. When creating or updating a game server container group definition, use the property https://docs.aws.amazon.com/gamelift/latest/apireference/API_GameServerContainerDefinitionInput. Part of: ContainerGroupDefinition Returned by: DescribeContainerGroupDefinition, ListContainerGroupDefinitions, UpdateContainerGroupDefinition"]
module SupportContainerDefinitionList =
  struct
    type nonrec t = SupportContainerDefinition.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SupportContainerDefinition.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SupportContainerDefinition.of_xml)
    let of_json j =
      list_of_json ~kind:"SupportContainerDefinitionList"
        ~of_json:SupportContainerDefinition.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConnectionPortRange =
  struct
    type nonrec t =
      {
      fromPort: PortNumber.t
        [@ocaml.doc "Starting value for the port range."];
      toPort: PortNumber.t
        [@ocaml.doc
          "Ending value for the port. Port numbers are end-inclusive. This value must be equal to or greater than FromPort."]}
    let context_ = "ConnectionPortRange"
    let make ~fromPort = fun ~toPort -> fun () -> { fromPort; toPort }
    let to_value x =
      structure_to_value
        [("FromPort", (Some (PortNumber.to_value x.fromPort)));
        ("ToPort", (Some (PortNumber.to_value x.toPort)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let toPort =
        PortNumber.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ToPort") in
      let fromPort =
        PortNumber.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FromPort") in
      make ~toPort ~fromPort ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let toPort = field_map_exn json__ "ToPort" PortNumber.of_json in
      let fromPort = field_map_exn json__ "FromPort" PortNumber.of_json in
      make ~toPort ~fromPort ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The set of port numbers to open on each instance in a container fleet. Connection ports are used by inbound traffic to connect with processes that are running in containers on the fleet. The port range must not overlap with the Amazon GameLift Servers reserved port range 4092-4191. This range is reserved for internal Amazon GameLift Servers services."]
module ContainerFleetBillingType =
  struct
    type nonrec t =
      | ON_DEMAND 
      | SPOT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ON_DEMAND -> "ON_DEMAND"
      | SPOT -> "SPOT"
      | Non_static_id s -> s
    let of_string =
      function
      | "ON_DEMAND" -> ON_DEMAND
      | "SPOT" -> SPOT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ContainerFleetBillingType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ContainerFleetBillingType" j)
    let to_json = simple_to_json to_value
  end
module ContainerFleetLocationAttributesList =
  struct
    type nonrec t = ContainerFleetLocationAttributes.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContainerFleetLocationAttributes.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ContainerFleetLocationAttributes.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerFleetLocationAttributesList"
        ~of_json:ContainerFleetLocationAttributes.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerFleetStatus =
  struct
    type nonrec t =
      | PENDING 
      | CREATING 
      | CREATED 
      | ACTIVATING 
      | ACTIVE 
      | UPDATING 
      | DELETING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | CREATING -> "CREATING"
      | CREATED -> "CREATED"
      | ACTIVATING -> "ACTIVATING"
      | ACTIVE -> "ACTIVE"
      | UPDATING -> "UPDATING"
      | DELETING -> "DELETING"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "CREATING" -> CREATING
      | "CREATED" -> CREATED
      | "ACTIVATING" -> ACTIVATING
      | "ACTIVE" -> ACTIVE
      | "UPDATING" -> UPDATING
      | "DELETING" -> DELETING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ContainerFleetStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ContainerFleetStatus" j)
    let to_json = simple_to_json to_value
  end
module DeploymentDetails =
  struct
    type nonrec t =
      {
      latestDeploymentId: DeploymentId.t option
        [@ocaml.doc "A unique identifier for a fleet deployment."]}
    let make ?latestDeploymentId = fun () -> { latestDeploymentId }
    let to_value x =
      structure_to_value
        [("LatestDeploymentId",
           (Option.map x.latestDeploymentId ~f:DeploymentId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let latestDeploymentId =
        (Option.map ~f:DeploymentId.of_xml)
          (Xml.child xml_arg0 "LatestDeploymentId") in
      make ?latestDeploymentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let latestDeploymentId =
        field_map json__ "LatestDeploymentId" DeploymentId.of_json in
      make ?latestDeploymentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the most recent deployment for the container fleet."]
module GameServerContainerGroupsPerInstance =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:5000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for GameServerContainerGroupsPerInstance"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GameSessionCreationLimitPolicy =
  struct
    type nonrec t =
      {
      newGameSessionsPerCreator: WholeNumber.t option
        [@ocaml.doc
          "A policy that puts limits on the number of game sessions that a player can create within a specified span of time. With this policy, you can control players' ability to consume available resources. The policy evaluates when a player tries to create a new game session. On receiving a CreateGameSession request, Amazon GameLift Servers checks that the player (identified by CreatorId) has created fewer than game session limit in the specified time period."];
      policyPeriodInMinutes: WholeNumber.t option
        [@ocaml.doc
          "The time span used in evaluating the resource creation limit policy."]}
    let make ?newGameSessionsPerCreator =
      fun ?policyPeriodInMinutes ->
        fun () -> { newGameSessionsPerCreator; policyPeriodInMinutes }
    let to_value x =
      structure_to_value
        [("NewGameSessionsPerCreator",
           (Option.map x.newGameSessionsPerCreator ~f:WholeNumber.to_value));
        ("PolicyPeriodInMinutes",
          (Option.map x.policyPeriodInMinutes ~f:WholeNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyPeriodInMinutes =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "PolicyPeriodInMinutes") in
      let newGameSessionsPerCreator =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "NewGameSessionsPerCreator") in
      make ?policyPeriodInMinutes ?newGameSessionsPerCreator ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyPeriodInMinutes =
        field_map json__ "PolicyPeriodInMinutes" WholeNumber.of_json in
      let newGameSessionsPerCreator =
        field_map json__ "NewGameSessionsPerCreator" WholeNumber.of_json in
      make ?policyPeriodInMinutes ?newGameSessionsPerCreator ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A policy that puts limits on the number of game sessions that a player can create within a specified span of time. With this policy, you can control players' ability to consume available resources. The policy is evaluated when a player tries to create a new game session. On receiving a CreateGameSession request, Amazon GameLift Servers checks that the player (identified by CreatorId) has created fewer than game session limit in the specified time period."]
module IpPermissionsList =
  struct
    type nonrec t = IpPermission.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:50); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:IpPermission.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:IpPermission.of_xml)
    let of_json j =
      list_of_json ~kind:"IpPermissionsList" ~of_json:IpPermission.of_json j
    let to_json v = composed_to_json to_value v
  end
module LogConfiguration =
  struct
    type nonrec t =
      {
      logDestination: LogDestination.t option
        [@ocaml.doc
          "The type of log collection to use for a fleet. CLOUDWATCH -- (default value) Send logs to an Amazon CloudWatch log group that you define. Each container emits a log stream, which is organized in the log group. S3 -- Store logs in an Amazon S3 bucket that you define. This bucket must reside in the fleet's home Amazon Web Services Region. NONE -- Don't collect container logs."];
      s3BucketName: NonEmptyString.t option
        [@ocaml.doc
          "If log destination is S3, logs are sent to the specified Amazon S3 bucket name."];
      logGroupArn: LogGroupArnStringModel.t option
        [@ocaml.doc
          "If log destination is CLOUDWATCH, logs are sent to the specified log group in Amazon CloudWatch."]}
    let make ?logDestination =
      fun ?s3BucketName ->
        fun ?logGroupArn ->
          fun () -> { logDestination; s3BucketName; logGroupArn }
    let to_value x =
      structure_to_value
        [("LogDestination",
           (Option.map x.logDestination ~f:LogDestination.to_value));
        ("S3BucketName",
          (Option.map x.s3BucketName ~f:NonEmptyString.to_value));
        ("LogGroupArn",
          (Option.map x.logGroupArn ~f:LogGroupArnStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logGroupArn =
        (Option.map ~f:LogGroupArnStringModel.of_xml)
          (Xml.child xml_arg0 "LogGroupArn") in
      let s3BucketName =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "S3BucketName") in
      let logDestination =
        (Option.map ~f:LogDestination.of_xml)
          (Xml.child xml_arg0 "LogDestination") in
      make ?logGroupArn ?s3BucketName ?logDestination ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logGroupArn =
        field_map json__ "LogGroupArn" LogGroupArnStringModel.of_json in
      let s3BucketName =
        field_map json__ "S3BucketName" NonEmptyString.of_json in
      let logDestination =
        field_map json__ "LogDestination" LogDestination.of_json in
      make ?logGroupArn ?s3BucketName ?logDestination ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A method for collecting container logs for the fleet. Amazon GameLift Servers saves all standard output for each container in logs, including game session logs. You can select from the following methods:"]
module MaximumGameServerContainerGroupsPerInstance =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:5000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for MaximumGameServerContainerGroupsPerInstance"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MetricGroupList =
  struct
    type nonrec t = MetricGroup.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MetricGroup.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MetricGroup.of_xml)
    let of_json j =
      list_of_json ~kind:"MetricGroupList" ~of_json:MetricGroup.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlayerGatewayMode =
  struct
    type nonrec t =
      | DISABLED 
      | ENABLED 
      | REQUIRED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DISABLED -> "DISABLED"
      | ENABLED -> "ENABLED"
      | REQUIRED -> "REQUIRED"
      | Non_static_id s -> s
    let of_string =
      function
      | "DISABLED" -> DISABLED
      | "ENABLED" -> ENABLED
      | "REQUIRED" -> REQUIRED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PlayerGatewayMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PlayerGatewayMode" j)
    let to_json = simple_to_json to_value
  end
module ProtectionPolicy =
  struct
    type nonrec t =
      | NoProtection 
      | FullProtection 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NoProtection -> "NoProtection"
      | FullProtection -> "FullProtection"
      | Non_static_id s -> s
    let of_string =
      function
      | "NoProtection" -> NoProtection
      | "FullProtection" -> FullProtection
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ProtectionPolicy" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ProtectionPolicy" j)
    let to_json = simple_to_json to_value
  end
module ComputeArn =
  struct
    type nonrec t = string
    let context_ = "ComputeArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"^arn:.*:compute\\/[a-zA-Z0-9\\-]+(\\/[a-zA-Z0-9\\-]+)?"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ComputeArn" j
    let to_json = simple_to_json to_value
  end
module ComputeStatus =
  struct
    type nonrec t =
      | PENDING 
      | ACTIVE 
      | TERMINATING 
      | IMPAIRED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | ACTIVE -> "ACTIVE"
      | TERMINATING -> "TERMINATING"
      | IMPAIRED -> "IMPAIRED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "ACTIVE" -> ACTIVE
      | "TERMINATING" -> TERMINATING
      | "IMPAIRED" -> IMPAIRED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ComputeStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ComputeStatus" j)
    let to_json = simple_to_json to_value
  end
module ContainerAttributes =
  struct
    type nonrec t = ContainerAttribute.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContainerAttribute.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerAttribute.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerAttributes"
        ~of_json:ContainerAttribute.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerGroupDefinitionNameOrArn =
  struct
    type nonrec t = string
    let context_ = "ContainerGroupDefinitionNameOrArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:512) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^[a-zA-Z0-9\\-]+$|^arn:.*:containergroupdefinition\\/[a-zA-Z0-9\\-]+(:[0-9]+)?$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ContainerGroupDefinitionNameOrArn" j
    let to_json = simple_to_json to_value
  end
module EC2InstanceType =
  struct
    type nonrec t =
      | T2_micro 
      | T2_small 
      | T2_medium 
      | T2_large 
      | C3_large 
      | C3_xlarge 
      | C3_2xlarge 
      | C3_4xlarge 
      | C3_8xlarge 
      | C4_large 
      | C4_xlarge 
      | C4_2xlarge 
      | C4_4xlarge 
      | C4_8xlarge 
      | C5_large 
      | C5_xlarge 
      | C5_2xlarge 
      | C5_4xlarge 
      | C5_9xlarge 
      | C5_12xlarge 
      | C5_18xlarge 
      | C5_24xlarge 
      | C5a_large 
      | C5a_xlarge 
      | C5a_2xlarge 
      | C5a_4xlarge 
      | C5a_8xlarge 
      | C5a_12xlarge 
      | C5a_16xlarge 
      | C5a_24xlarge 
      | R3_large 
      | R3_xlarge 
      | R3_2xlarge 
      | R3_4xlarge 
      | R3_8xlarge 
      | R4_large 
      | R4_xlarge 
      | R4_2xlarge 
      | R4_4xlarge 
      | R4_8xlarge 
      | R4_16xlarge 
      | R5_large 
      | R5_xlarge 
      | R5_2xlarge 
      | R5_4xlarge 
      | R5_8xlarge 
      | R5_12xlarge 
      | R5_16xlarge 
      | R5_24xlarge 
      | R5a_large 
      | R5a_xlarge 
      | R5a_2xlarge 
      | R5a_4xlarge 
      | R5a_8xlarge 
      | R5a_12xlarge 
      | R5a_16xlarge 
      | R5a_24xlarge 
      | M3_medium 
      | M3_large 
      | M3_xlarge 
      | M3_2xlarge 
      | M4_large 
      | M4_xlarge 
      | M4_2xlarge 
      | M4_4xlarge 
      | M4_10xlarge 
      | M5_large 
      | M5_xlarge 
      | M5_2xlarge 
      | M5_4xlarge 
      | M5_8xlarge 
      | M5_12xlarge 
      | M5_16xlarge 
      | M5_24xlarge 
      | M5a_large 
      | M5a_xlarge 
      | M5a_2xlarge 
      | M5a_4xlarge 
      | M5a_8xlarge 
      | M5a_12xlarge 
      | M5a_16xlarge 
      | M5a_24xlarge 
      | C5d_large 
      | C5d_xlarge 
      | C5d_2xlarge 
      | C5d_4xlarge 
      | C5d_9xlarge 
      | C5d_12xlarge 
      | C5d_18xlarge 
      | C5d_24xlarge 
      | C6a_large 
      | C6a_xlarge 
      | C6a_2xlarge 
      | C6a_4xlarge 
      | C6a_8xlarge 
      | C6a_12xlarge 
      | C6a_16xlarge 
      | C6a_24xlarge 
      | C6i_large 
      | C6i_xlarge 
      | C6i_2xlarge 
      | C6i_4xlarge 
      | C6i_8xlarge 
      | C6i_12xlarge 
      | C6i_16xlarge 
      | C6i_24xlarge 
      | R5d_large 
      | R5d_xlarge 
      | R5d_2xlarge 
      | R5d_4xlarge 
      | R5d_8xlarge 
      | R5d_12xlarge 
      | R5d_16xlarge 
      | R5d_24xlarge 
      | M6g_medium 
      | M6g_large 
      | M6g_xlarge 
      | M6g_2xlarge 
      | M6g_4xlarge 
      | M6g_8xlarge 
      | M6g_12xlarge 
      | M6g_16xlarge 
      | C6g_medium 
      | C6g_large 
      | C6g_xlarge 
      | C6g_2xlarge 
      | C6g_4xlarge 
      | C6g_8xlarge 
      | C6g_12xlarge 
      | C6g_16xlarge 
      | R6g_medium 
      | R6g_large 
      | R6g_xlarge 
      | R6g_2xlarge 
      | R6g_4xlarge 
      | R6g_8xlarge 
      | R6g_12xlarge 
      | R6g_16xlarge 
      | C6gn_medium 
      | C6gn_large 
      | C6gn_xlarge 
      | C6gn_2xlarge 
      | C6gn_4xlarge 
      | C6gn_8xlarge 
      | C6gn_12xlarge 
      | C6gn_16xlarge 
      | C7g_medium 
      | C7g_large 
      | C7g_xlarge 
      | C7g_2xlarge 
      | C7g_4xlarge 
      | C7g_8xlarge 
      | C7g_12xlarge 
      | C7g_16xlarge 
      | R7g_medium 
      | R7g_large 
      | R7g_xlarge 
      | R7g_2xlarge 
      | R7g_4xlarge 
      | R7g_8xlarge 
      | R7g_12xlarge 
      | R7g_16xlarge 
      | M7g_medium 
      | M7g_large 
      | M7g_xlarge 
      | M7g_2xlarge 
      | M7g_4xlarge 
      | M7g_8xlarge 
      | M7g_12xlarge 
      | M7g_16xlarge 
      | G5g_xlarge 
      | G5g_2xlarge 
      | G5g_4xlarge 
      | G5g_8xlarge 
      | G5g_16xlarge 
      | R6i_large 
      | R6i_xlarge 
      | R6i_2xlarge 
      | R6i_4xlarge 
      | R6i_8xlarge 
      | R6i_12xlarge 
      | R6i_16xlarge 
      | C6gd_medium 
      | C6gd_large 
      | C6gd_xlarge 
      | C6gd_2xlarge 
      | C6gd_4xlarge 
      | C6gd_8xlarge 
      | C6gd_12xlarge 
      | C6gd_16xlarge 
      | C6in_large 
      | C6in_xlarge 
      | C6in_2xlarge 
      | C6in_4xlarge 
      | C6in_8xlarge 
      | C6in_12xlarge 
      | C6in_16xlarge 
      | C7a_medium 
      | C7a_large 
      | C7a_xlarge 
      | C7a_2xlarge 
      | C7a_4xlarge 
      | C7a_8xlarge 
      | C7a_12xlarge 
      | C7a_16xlarge 
      | C7gd_medium 
      | C7gd_large 
      | C7gd_xlarge 
      | C7gd_2xlarge 
      | C7gd_4xlarge 
      | C7gd_8xlarge 
      | C7gd_12xlarge 
      | C7gd_16xlarge 
      | C7gn_medium 
      | C7gn_large 
      | C7gn_xlarge 
      | C7gn_2xlarge 
      | C7gn_4xlarge 
      | C7gn_8xlarge 
      | C7gn_12xlarge 
      | C7gn_16xlarge 
      | C7i_large 
      | C7i_xlarge 
      | C7i_2xlarge 
      | C7i_4xlarge 
      | C7i_8xlarge 
      | C7i_12xlarge 
      | C7i_16xlarge 
      | M6a_large 
      | M6a_xlarge 
      | M6a_2xlarge 
      | M6a_4xlarge 
      | M6a_8xlarge 
      | M6a_12xlarge 
      | M6a_16xlarge 
      | M6gd_medium 
      | M6gd_large 
      | M6gd_xlarge 
      | M6gd_2xlarge 
      | M6gd_4xlarge 
      | M6gd_8xlarge 
      | M6gd_12xlarge 
      | M6gd_16xlarge 
      | M6i_large 
      | M6i_xlarge 
      | M6i_2xlarge 
      | M6i_4xlarge 
      | M6i_8xlarge 
      | M6i_12xlarge 
      | M6i_16xlarge 
      | M7a_medium 
      | M7a_large 
      | M7a_xlarge 
      | M7a_2xlarge 
      | M7a_4xlarge 
      | M7a_8xlarge 
      | M7a_12xlarge 
      | M7a_16xlarge 
      | M7gd_medium 
      | M7gd_large 
      | M7gd_xlarge 
      | M7gd_2xlarge 
      | M7gd_4xlarge 
      | M7gd_8xlarge 
      | M7gd_12xlarge 
      | M7gd_16xlarge 
      | M7i_large 
      | M7i_xlarge 
      | M7i_2xlarge 
      | M7i_4xlarge 
      | M7i_8xlarge 
      | M7i_12xlarge 
      | M7i_16xlarge 
      | R6gd_medium 
      | R6gd_large 
      | R6gd_xlarge 
      | R6gd_2xlarge 
      | R6gd_4xlarge 
      | R6gd_8xlarge 
      | R6gd_12xlarge 
      | R6gd_16xlarge 
      | R7a_medium 
      | R7a_large 
      | R7a_xlarge 
      | R7a_2xlarge 
      | R7a_4xlarge 
      | R7a_8xlarge 
      | R7a_12xlarge 
      | R7a_16xlarge 
      | R7gd_medium 
      | R7gd_large 
      | R7gd_xlarge 
      | R7gd_2xlarge 
      | R7gd_4xlarge 
      | R7gd_8xlarge 
      | R7gd_12xlarge 
      | R7gd_16xlarge 
      | R7i_large 
      | R7i_xlarge 
      | R7i_2xlarge 
      | R7i_4xlarge 
      | R7i_8xlarge 
      | R7i_12xlarge 
      | R7i_16xlarge 
      | R7i_24xlarge 
      | R7i_48xlarge 
      | C5ad_large 
      | C5ad_xlarge 
      | C5ad_2xlarge 
      | C5ad_4xlarge 
      | C5ad_8xlarge 
      | C5ad_12xlarge 
      | C5ad_16xlarge 
      | C5ad_24xlarge 
      | C5n_large 
      | C5n_xlarge 
      | C5n_2xlarge 
      | C5n_4xlarge 
      | C5n_9xlarge 
      | C5n_18xlarge 
      | R5ad_large 
      | R5ad_xlarge 
      | R5ad_2xlarge 
      | R5ad_4xlarge 
      | R5ad_8xlarge 
      | R5ad_12xlarge 
      | R5ad_16xlarge 
      | R5ad_24xlarge 
      | C6id_large 
      | C6id_xlarge 
      | C6id_2xlarge 
      | C6id_4xlarge 
      | C6id_8xlarge 
      | C6id_12xlarge 
      | C6id_16xlarge 
      | C6id_24xlarge 
      | C6id_32xlarge 
      | C8g_medium 
      | C8g_large 
      | C8g_xlarge 
      | C8g_2xlarge 
      | C8g_4xlarge 
      | C8g_8xlarge 
      | C8g_12xlarge 
      | C8g_16xlarge 
      | C8g_24xlarge 
      | C8g_48xlarge 
      | M5ad_large 
      | M5ad_xlarge 
      | M5ad_2xlarge 
      | M5ad_4xlarge 
      | M5ad_8xlarge 
      | M5ad_12xlarge 
      | M5ad_16xlarge 
      | M5ad_24xlarge 
      | M5d_large 
      | M5d_xlarge 
      | M5d_2xlarge 
      | M5d_4xlarge 
      | M5d_8xlarge 
      | M5d_12xlarge 
      | M5d_16xlarge 
      | M5d_24xlarge 
      | M5dn_large 
      | M5dn_xlarge 
      | M5dn_2xlarge 
      | M5dn_4xlarge 
      | M5dn_8xlarge 
      | M5dn_12xlarge 
      | M5dn_16xlarge 
      | M5dn_24xlarge 
      | M5n_large 
      | M5n_xlarge 
      | M5n_2xlarge 
      | M5n_4xlarge 
      | M5n_8xlarge 
      | M5n_12xlarge 
      | M5n_16xlarge 
      | M5n_24xlarge 
      | M6id_large 
      | M6id_xlarge 
      | M6id_2xlarge 
      | M6id_4xlarge 
      | M6id_8xlarge 
      | M6id_12xlarge 
      | M6id_16xlarge 
      | M6id_24xlarge 
      | M6id_32xlarge 
      | M6idn_large 
      | M6idn_xlarge 
      | M6idn_2xlarge 
      | M6idn_4xlarge 
      | M6idn_8xlarge 
      | M6idn_12xlarge 
      | M6idn_16xlarge 
      | M6idn_24xlarge 
      | M6idn_32xlarge 
      | M6in_large 
      | M6in_xlarge 
      | M6in_2xlarge 
      | M6in_4xlarge 
      | M6in_8xlarge 
      | M6in_12xlarge 
      | M6in_16xlarge 
      | M6in_24xlarge 
      | M6in_32xlarge 
      | M8g_medium 
      | M8g_large 
      | M8g_xlarge 
      | M8g_2xlarge 
      | M8g_4xlarge 
      | M8g_8xlarge 
      | M8g_12xlarge 
      | M8g_16xlarge 
      | M8g_24xlarge 
      | M8g_48xlarge 
      | R5dn_large 
      | R5dn_xlarge 
      | R5dn_2xlarge 
      | R5dn_4xlarge 
      | R5dn_8xlarge 
      | R5dn_12xlarge 
      | R5dn_16xlarge 
      | R5dn_24xlarge 
      | R5n_large 
      | R5n_xlarge 
      | R5n_2xlarge 
      | R5n_4xlarge 
      | R5n_8xlarge 
      | R5n_12xlarge 
      | R5n_16xlarge 
      | R5n_24xlarge 
      | R6a_large 
      | R6a_xlarge 
      | R6a_2xlarge 
      | R6a_4xlarge 
      | R6a_8xlarge 
      | R6a_12xlarge 
      | R6a_16xlarge 
      | R6a_24xlarge 
      | R6a_32xlarge 
      | R6a_48xlarge 
      | R6id_large 
      | R6id_xlarge 
      | R6id_2xlarge 
      | R6id_4xlarge 
      | R6id_8xlarge 
      | R6id_12xlarge 
      | R6id_16xlarge 
      | R6id_24xlarge 
      | R6id_32xlarge 
      | R6idn_large 
      | R6idn_xlarge 
      | R6idn_2xlarge 
      | R6idn_4xlarge 
      | R6idn_8xlarge 
      | R6idn_12xlarge 
      | R6idn_16xlarge 
      | R6idn_24xlarge 
      | R6idn_32xlarge 
      | R6in_large 
      | R6in_xlarge 
      | R6in_2xlarge 
      | R6in_4xlarge 
      | R6in_8xlarge 
      | R6in_12xlarge 
      | R6in_16xlarge 
      | R6in_24xlarge 
      | R6in_32xlarge 
      | R8g_medium 
      | R8g_large 
      | R8g_xlarge 
      | R8g_2xlarge 
      | R8g_4xlarge 
      | R8g_8xlarge 
      | R8g_12xlarge 
      | R8g_16xlarge 
      | R8g_24xlarge 
      | R8g_48xlarge 
      | M4_16xlarge 
      | C6a_32xlarge 
      | C6a_48xlarge 
      | C6i_32xlarge 
      | R6i_24xlarge 
      | R6i_32xlarge 
      | C6in_24xlarge 
      | C6in_32xlarge 
      | C7a_24xlarge 
      | C7a_32xlarge 
      | C7a_48xlarge 
      | C7i_24xlarge 
      | C7i_48xlarge 
      | M6a_24xlarge 
      | M6a_32xlarge 
      | M6a_48xlarge 
      | M6i_24xlarge 
      | M6i_32xlarge 
      | M7a_24xlarge 
      | M7a_32xlarge 
      | M7a_48xlarge 
      | M7i_24xlarge 
      | M7i_48xlarge 
      | R7a_24xlarge 
      | R7a_32xlarge 
      | R7a_48xlarge 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | T2_micro -> "t2.micro"
      | T2_small -> "t2.small"
      | T2_medium -> "t2.medium"
      | T2_large -> "t2.large"
      | C3_large -> "c3.large"
      | C3_xlarge -> "c3.xlarge"
      | C3_2xlarge -> "c3.2xlarge"
      | C3_4xlarge -> "c3.4xlarge"
      | C3_8xlarge -> "c3.8xlarge"
      | C4_large -> "c4.large"
      | C4_xlarge -> "c4.xlarge"
      | C4_2xlarge -> "c4.2xlarge"
      | C4_4xlarge -> "c4.4xlarge"
      | C4_8xlarge -> "c4.8xlarge"
      | C5_large -> "c5.large"
      | C5_xlarge -> "c5.xlarge"
      | C5_2xlarge -> "c5.2xlarge"
      | C5_4xlarge -> "c5.4xlarge"
      | C5_9xlarge -> "c5.9xlarge"
      | C5_12xlarge -> "c5.12xlarge"
      | C5_18xlarge -> "c5.18xlarge"
      | C5_24xlarge -> "c5.24xlarge"
      | C5a_large -> "c5a.large"
      | C5a_xlarge -> "c5a.xlarge"
      | C5a_2xlarge -> "c5a.2xlarge"
      | C5a_4xlarge -> "c5a.4xlarge"
      | C5a_8xlarge -> "c5a.8xlarge"
      | C5a_12xlarge -> "c5a.12xlarge"
      | C5a_16xlarge -> "c5a.16xlarge"
      | C5a_24xlarge -> "c5a.24xlarge"
      | R3_large -> "r3.large"
      | R3_xlarge -> "r3.xlarge"
      | R3_2xlarge -> "r3.2xlarge"
      | R3_4xlarge -> "r3.4xlarge"
      | R3_8xlarge -> "r3.8xlarge"
      | R4_large -> "r4.large"
      | R4_xlarge -> "r4.xlarge"
      | R4_2xlarge -> "r4.2xlarge"
      | R4_4xlarge -> "r4.4xlarge"
      | R4_8xlarge -> "r4.8xlarge"
      | R4_16xlarge -> "r4.16xlarge"
      | R5_large -> "r5.large"
      | R5_xlarge -> "r5.xlarge"
      | R5_2xlarge -> "r5.2xlarge"
      | R5_4xlarge -> "r5.4xlarge"
      | R5_8xlarge -> "r5.8xlarge"
      | R5_12xlarge -> "r5.12xlarge"
      | R5_16xlarge -> "r5.16xlarge"
      | R5_24xlarge -> "r5.24xlarge"
      | R5a_large -> "r5a.large"
      | R5a_xlarge -> "r5a.xlarge"
      | R5a_2xlarge -> "r5a.2xlarge"
      | R5a_4xlarge -> "r5a.4xlarge"
      | R5a_8xlarge -> "r5a.8xlarge"
      | R5a_12xlarge -> "r5a.12xlarge"
      | R5a_16xlarge -> "r5a.16xlarge"
      | R5a_24xlarge -> "r5a.24xlarge"
      | M3_medium -> "m3.medium"
      | M3_large -> "m3.large"
      | M3_xlarge -> "m3.xlarge"
      | M3_2xlarge -> "m3.2xlarge"
      | M4_large -> "m4.large"
      | M4_xlarge -> "m4.xlarge"
      | M4_2xlarge -> "m4.2xlarge"
      | M4_4xlarge -> "m4.4xlarge"
      | M4_10xlarge -> "m4.10xlarge"
      | M5_large -> "m5.large"
      | M5_xlarge -> "m5.xlarge"
      | M5_2xlarge -> "m5.2xlarge"
      | M5_4xlarge -> "m5.4xlarge"
      | M5_8xlarge -> "m5.8xlarge"
      | M5_12xlarge -> "m5.12xlarge"
      | M5_16xlarge -> "m5.16xlarge"
      | M5_24xlarge -> "m5.24xlarge"
      | M5a_large -> "m5a.large"
      | M5a_xlarge -> "m5a.xlarge"
      | M5a_2xlarge -> "m5a.2xlarge"
      | M5a_4xlarge -> "m5a.4xlarge"
      | M5a_8xlarge -> "m5a.8xlarge"
      | M5a_12xlarge -> "m5a.12xlarge"
      | M5a_16xlarge -> "m5a.16xlarge"
      | M5a_24xlarge -> "m5a.24xlarge"
      | C5d_large -> "c5d.large"
      | C5d_xlarge -> "c5d.xlarge"
      | C5d_2xlarge -> "c5d.2xlarge"
      | C5d_4xlarge -> "c5d.4xlarge"
      | C5d_9xlarge -> "c5d.9xlarge"
      | C5d_12xlarge -> "c5d.12xlarge"
      | C5d_18xlarge -> "c5d.18xlarge"
      | C5d_24xlarge -> "c5d.24xlarge"
      | C6a_large -> "c6a.large"
      | C6a_xlarge -> "c6a.xlarge"
      | C6a_2xlarge -> "c6a.2xlarge"
      | C6a_4xlarge -> "c6a.4xlarge"
      | C6a_8xlarge -> "c6a.8xlarge"
      | C6a_12xlarge -> "c6a.12xlarge"
      | C6a_16xlarge -> "c6a.16xlarge"
      | C6a_24xlarge -> "c6a.24xlarge"
      | C6i_large -> "c6i.large"
      | C6i_xlarge -> "c6i.xlarge"
      | C6i_2xlarge -> "c6i.2xlarge"
      | C6i_4xlarge -> "c6i.4xlarge"
      | C6i_8xlarge -> "c6i.8xlarge"
      | C6i_12xlarge -> "c6i.12xlarge"
      | C6i_16xlarge -> "c6i.16xlarge"
      | C6i_24xlarge -> "c6i.24xlarge"
      | R5d_large -> "r5d.large"
      | R5d_xlarge -> "r5d.xlarge"
      | R5d_2xlarge -> "r5d.2xlarge"
      | R5d_4xlarge -> "r5d.4xlarge"
      | R5d_8xlarge -> "r5d.8xlarge"
      | R5d_12xlarge -> "r5d.12xlarge"
      | R5d_16xlarge -> "r5d.16xlarge"
      | R5d_24xlarge -> "r5d.24xlarge"
      | M6g_medium -> "m6g.medium"
      | M6g_large -> "m6g.large"
      | M6g_xlarge -> "m6g.xlarge"
      | M6g_2xlarge -> "m6g.2xlarge"
      | M6g_4xlarge -> "m6g.4xlarge"
      | M6g_8xlarge -> "m6g.8xlarge"
      | M6g_12xlarge -> "m6g.12xlarge"
      | M6g_16xlarge -> "m6g.16xlarge"
      | C6g_medium -> "c6g.medium"
      | C6g_large -> "c6g.large"
      | C6g_xlarge -> "c6g.xlarge"
      | C6g_2xlarge -> "c6g.2xlarge"
      | C6g_4xlarge -> "c6g.4xlarge"
      | C6g_8xlarge -> "c6g.8xlarge"
      | C6g_12xlarge -> "c6g.12xlarge"
      | C6g_16xlarge -> "c6g.16xlarge"
      | R6g_medium -> "r6g.medium"
      | R6g_large -> "r6g.large"
      | R6g_xlarge -> "r6g.xlarge"
      | R6g_2xlarge -> "r6g.2xlarge"
      | R6g_4xlarge -> "r6g.4xlarge"
      | R6g_8xlarge -> "r6g.8xlarge"
      | R6g_12xlarge -> "r6g.12xlarge"
      | R6g_16xlarge -> "r6g.16xlarge"
      | C6gn_medium -> "c6gn.medium"
      | C6gn_large -> "c6gn.large"
      | C6gn_xlarge -> "c6gn.xlarge"
      | C6gn_2xlarge -> "c6gn.2xlarge"
      | C6gn_4xlarge -> "c6gn.4xlarge"
      | C6gn_8xlarge -> "c6gn.8xlarge"
      | C6gn_12xlarge -> "c6gn.12xlarge"
      | C6gn_16xlarge -> "c6gn.16xlarge"
      | C7g_medium -> "c7g.medium"
      | C7g_large -> "c7g.large"
      | C7g_xlarge -> "c7g.xlarge"
      | C7g_2xlarge -> "c7g.2xlarge"
      | C7g_4xlarge -> "c7g.4xlarge"
      | C7g_8xlarge -> "c7g.8xlarge"
      | C7g_12xlarge -> "c7g.12xlarge"
      | C7g_16xlarge -> "c7g.16xlarge"
      | R7g_medium -> "r7g.medium"
      | R7g_large -> "r7g.large"
      | R7g_xlarge -> "r7g.xlarge"
      | R7g_2xlarge -> "r7g.2xlarge"
      | R7g_4xlarge -> "r7g.4xlarge"
      | R7g_8xlarge -> "r7g.8xlarge"
      | R7g_12xlarge -> "r7g.12xlarge"
      | R7g_16xlarge -> "r7g.16xlarge"
      | M7g_medium -> "m7g.medium"
      | M7g_large -> "m7g.large"
      | M7g_xlarge -> "m7g.xlarge"
      | M7g_2xlarge -> "m7g.2xlarge"
      | M7g_4xlarge -> "m7g.4xlarge"
      | M7g_8xlarge -> "m7g.8xlarge"
      | M7g_12xlarge -> "m7g.12xlarge"
      | M7g_16xlarge -> "m7g.16xlarge"
      | G5g_xlarge -> "g5g.xlarge"
      | G5g_2xlarge -> "g5g.2xlarge"
      | G5g_4xlarge -> "g5g.4xlarge"
      | G5g_8xlarge -> "g5g.8xlarge"
      | G5g_16xlarge -> "g5g.16xlarge"
      | R6i_large -> "r6i.large"
      | R6i_xlarge -> "r6i.xlarge"
      | R6i_2xlarge -> "r6i.2xlarge"
      | R6i_4xlarge -> "r6i.4xlarge"
      | R6i_8xlarge -> "r6i.8xlarge"
      | R6i_12xlarge -> "r6i.12xlarge"
      | R6i_16xlarge -> "r6i.16xlarge"
      | C6gd_medium -> "c6gd.medium"
      | C6gd_large -> "c6gd.large"
      | C6gd_xlarge -> "c6gd.xlarge"
      | C6gd_2xlarge -> "c6gd.2xlarge"
      | C6gd_4xlarge -> "c6gd.4xlarge"
      | C6gd_8xlarge -> "c6gd.8xlarge"
      | C6gd_12xlarge -> "c6gd.12xlarge"
      | C6gd_16xlarge -> "c6gd.16xlarge"
      | C6in_large -> "c6in.large"
      | C6in_xlarge -> "c6in.xlarge"
      | C6in_2xlarge -> "c6in.2xlarge"
      | C6in_4xlarge -> "c6in.4xlarge"
      | C6in_8xlarge -> "c6in.8xlarge"
      | C6in_12xlarge -> "c6in.12xlarge"
      | C6in_16xlarge -> "c6in.16xlarge"
      | C7a_medium -> "c7a.medium"
      | C7a_large -> "c7a.large"
      | C7a_xlarge -> "c7a.xlarge"
      | C7a_2xlarge -> "c7a.2xlarge"
      | C7a_4xlarge -> "c7a.4xlarge"
      | C7a_8xlarge -> "c7a.8xlarge"
      | C7a_12xlarge -> "c7a.12xlarge"
      | C7a_16xlarge -> "c7a.16xlarge"
      | C7gd_medium -> "c7gd.medium"
      | C7gd_large -> "c7gd.large"
      | C7gd_xlarge -> "c7gd.xlarge"
      | C7gd_2xlarge -> "c7gd.2xlarge"
      | C7gd_4xlarge -> "c7gd.4xlarge"
      | C7gd_8xlarge -> "c7gd.8xlarge"
      | C7gd_12xlarge -> "c7gd.12xlarge"
      | C7gd_16xlarge -> "c7gd.16xlarge"
      | C7gn_medium -> "c7gn.medium"
      | C7gn_large -> "c7gn.large"
      | C7gn_xlarge -> "c7gn.xlarge"
      | C7gn_2xlarge -> "c7gn.2xlarge"
      | C7gn_4xlarge -> "c7gn.4xlarge"
      | C7gn_8xlarge -> "c7gn.8xlarge"
      | C7gn_12xlarge -> "c7gn.12xlarge"
      | C7gn_16xlarge -> "c7gn.16xlarge"
      | C7i_large -> "c7i.large"
      | C7i_xlarge -> "c7i.xlarge"
      | C7i_2xlarge -> "c7i.2xlarge"
      | C7i_4xlarge -> "c7i.4xlarge"
      | C7i_8xlarge -> "c7i.8xlarge"
      | C7i_12xlarge -> "c7i.12xlarge"
      | C7i_16xlarge -> "c7i.16xlarge"
      | M6a_large -> "m6a.large"
      | M6a_xlarge -> "m6a.xlarge"
      | M6a_2xlarge -> "m6a.2xlarge"
      | M6a_4xlarge -> "m6a.4xlarge"
      | M6a_8xlarge -> "m6a.8xlarge"
      | M6a_12xlarge -> "m6a.12xlarge"
      | M6a_16xlarge -> "m6a.16xlarge"
      | M6gd_medium -> "m6gd.medium"
      | M6gd_large -> "m6gd.large"
      | M6gd_xlarge -> "m6gd.xlarge"
      | M6gd_2xlarge -> "m6gd.2xlarge"
      | M6gd_4xlarge -> "m6gd.4xlarge"
      | M6gd_8xlarge -> "m6gd.8xlarge"
      | M6gd_12xlarge -> "m6gd.12xlarge"
      | M6gd_16xlarge -> "m6gd.16xlarge"
      | M6i_large -> "m6i.large"
      | M6i_xlarge -> "m6i.xlarge"
      | M6i_2xlarge -> "m6i.2xlarge"
      | M6i_4xlarge -> "m6i.4xlarge"
      | M6i_8xlarge -> "m6i.8xlarge"
      | M6i_12xlarge -> "m6i.12xlarge"
      | M6i_16xlarge -> "m6i.16xlarge"
      | M7a_medium -> "m7a.medium"
      | M7a_large -> "m7a.large"
      | M7a_xlarge -> "m7a.xlarge"
      | M7a_2xlarge -> "m7a.2xlarge"
      | M7a_4xlarge -> "m7a.4xlarge"
      | M7a_8xlarge -> "m7a.8xlarge"
      | M7a_12xlarge -> "m7a.12xlarge"
      | M7a_16xlarge -> "m7a.16xlarge"
      | M7gd_medium -> "m7gd.medium"
      | M7gd_large -> "m7gd.large"
      | M7gd_xlarge -> "m7gd.xlarge"
      | M7gd_2xlarge -> "m7gd.2xlarge"
      | M7gd_4xlarge -> "m7gd.4xlarge"
      | M7gd_8xlarge -> "m7gd.8xlarge"
      | M7gd_12xlarge -> "m7gd.12xlarge"
      | M7gd_16xlarge -> "m7gd.16xlarge"
      | M7i_large -> "m7i.large"
      | M7i_xlarge -> "m7i.xlarge"
      | M7i_2xlarge -> "m7i.2xlarge"
      | M7i_4xlarge -> "m7i.4xlarge"
      | M7i_8xlarge -> "m7i.8xlarge"
      | M7i_12xlarge -> "m7i.12xlarge"
      | M7i_16xlarge -> "m7i.16xlarge"
      | R6gd_medium -> "r6gd.medium"
      | R6gd_large -> "r6gd.large"
      | R6gd_xlarge -> "r6gd.xlarge"
      | R6gd_2xlarge -> "r6gd.2xlarge"
      | R6gd_4xlarge -> "r6gd.4xlarge"
      | R6gd_8xlarge -> "r6gd.8xlarge"
      | R6gd_12xlarge -> "r6gd.12xlarge"
      | R6gd_16xlarge -> "r6gd.16xlarge"
      | R7a_medium -> "r7a.medium"
      | R7a_large -> "r7a.large"
      | R7a_xlarge -> "r7a.xlarge"
      | R7a_2xlarge -> "r7a.2xlarge"
      | R7a_4xlarge -> "r7a.4xlarge"
      | R7a_8xlarge -> "r7a.8xlarge"
      | R7a_12xlarge -> "r7a.12xlarge"
      | R7a_16xlarge -> "r7a.16xlarge"
      | R7gd_medium -> "r7gd.medium"
      | R7gd_large -> "r7gd.large"
      | R7gd_xlarge -> "r7gd.xlarge"
      | R7gd_2xlarge -> "r7gd.2xlarge"
      | R7gd_4xlarge -> "r7gd.4xlarge"
      | R7gd_8xlarge -> "r7gd.8xlarge"
      | R7gd_12xlarge -> "r7gd.12xlarge"
      | R7gd_16xlarge -> "r7gd.16xlarge"
      | R7i_large -> "r7i.large"
      | R7i_xlarge -> "r7i.xlarge"
      | R7i_2xlarge -> "r7i.2xlarge"
      | R7i_4xlarge -> "r7i.4xlarge"
      | R7i_8xlarge -> "r7i.8xlarge"
      | R7i_12xlarge -> "r7i.12xlarge"
      | R7i_16xlarge -> "r7i.16xlarge"
      | R7i_24xlarge -> "r7i.24xlarge"
      | R7i_48xlarge -> "r7i.48xlarge"
      | C5ad_large -> "c5ad.large"
      | C5ad_xlarge -> "c5ad.xlarge"
      | C5ad_2xlarge -> "c5ad.2xlarge"
      | C5ad_4xlarge -> "c5ad.4xlarge"
      | C5ad_8xlarge -> "c5ad.8xlarge"
      | C5ad_12xlarge -> "c5ad.12xlarge"
      | C5ad_16xlarge -> "c5ad.16xlarge"
      | C5ad_24xlarge -> "c5ad.24xlarge"
      | C5n_large -> "c5n.large"
      | C5n_xlarge -> "c5n.xlarge"
      | C5n_2xlarge -> "c5n.2xlarge"
      | C5n_4xlarge -> "c5n.4xlarge"
      | C5n_9xlarge -> "c5n.9xlarge"
      | C5n_18xlarge -> "c5n.18xlarge"
      | R5ad_large -> "r5ad.large"
      | R5ad_xlarge -> "r5ad.xlarge"
      | R5ad_2xlarge -> "r5ad.2xlarge"
      | R5ad_4xlarge -> "r5ad.4xlarge"
      | R5ad_8xlarge -> "r5ad.8xlarge"
      | R5ad_12xlarge -> "r5ad.12xlarge"
      | R5ad_16xlarge -> "r5ad.16xlarge"
      | R5ad_24xlarge -> "r5ad.24xlarge"
      | C6id_large -> "c6id.large"
      | C6id_xlarge -> "c6id.xlarge"
      | C6id_2xlarge -> "c6id.2xlarge"
      | C6id_4xlarge -> "c6id.4xlarge"
      | C6id_8xlarge -> "c6id.8xlarge"
      | C6id_12xlarge -> "c6id.12xlarge"
      | C6id_16xlarge -> "c6id.16xlarge"
      | C6id_24xlarge -> "c6id.24xlarge"
      | C6id_32xlarge -> "c6id.32xlarge"
      | C8g_medium -> "c8g.medium"
      | C8g_large -> "c8g.large"
      | C8g_xlarge -> "c8g.xlarge"
      | C8g_2xlarge -> "c8g.2xlarge"
      | C8g_4xlarge -> "c8g.4xlarge"
      | C8g_8xlarge -> "c8g.8xlarge"
      | C8g_12xlarge -> "c8g.12xlarge"
      | C8g_16xlarge -> "c8g.16xlarge"
      | C8g_24xlarge -> "c8g.24xlarge"
      | C8g_48xlarge -> "c8g.48xlarge"
      | M5ad_large -> "m5ad.large"
      | M5ad_xlarge -> "m5ad.xlarge"
      | M5ad_2xlarge -> "m5ad.2xlarge"
      | M5ad_4xlarge -> "m5ad.4xlarge"
      | M5ad_8xlarge -> "m5ad.8xlarge"
      | M5ad_12xlarge -> "m5ad.12xlarge"
      | M5ad_16xlarge -> "m5ad.16xlarge"
      | M5ad_24xlarge -> "m5ad.24xlarge"
      | M5d_large -> "m5d.large"
      | M5d_xlarge -> "m5d.xlarge"
      | M5d_2xlarge -> "m5d.2xlarge"
      | M5d_4xlarge -> "m5d.4xlarge"
      | M5d_8xlarge -> "m5d.8xlarge"
      | M5d_12xlarge -> "m5d.12xlarge"
      | M5d_16xlarge -> "m5d.16xlarge"
      | M5d_24xlarge -> "m5d.24xlarge"
      | M5dn_large -> "m5dn.large"
      | M5dn_xlarge -> "m5dn.xlarge"
      | M5dn_2xlarge -> "m5dn.2xlarge"
      | M5dn_4xlarge -> "m5dn.4xlarge"
      | M5dn_8xlarge -> "m5dn.8xlarge"
      | M5dn_12xlarge -> "m5dn.12xlarge"
      | M5dn_16xlarge -> "m5dn.16xlarge"
      | M5dn_24xlarge -> "m5dn.24xlarge"
      | M5n_large -> "m5n.large"
      | M5n_xlarge -> "m5n.xlarge"
      | M5n_2xlarge -> "m5n.2xlarge"
      | M5n_4xlarge -> "m5n.4xlarge"
      | M5n_8xlarge -> "m5n.8xlarge"
      | M5n_12xlarge -> "m5n.12xlarge"
      | M5n_16xlarge -> "m5n.16xlarge"
      | M5n_24xlarge -> "m5n.24xlarge"
      | M6id_large -> "m6id.large"
      | M6id_xlarge -> "m6id.xlarge"
      | M6id_2xlarge -> "m6id.2xlarge"
      | M6id_4xlarge -> "m6id.4xlarge"
      | M6id_8xlarge -> "m6id.8xlarge"
      | M6id_12xlarge -> "m6id.12xlarge"
      | M6id_16xlarge -> "m6id.16xlarge"
      | M6id_24xlarge -> "m6id.24xlarge"
      | M6id_32xlarge -> "m6id.32xlarge"
      | M6idn_large -> "m6idn.large"
      | M6idn_xlarge -> "m6idn.xlarge"
      | M6idn_2xlarge -> "m6idn.2xlarge"
      | M6idn_4xlarge -> "m6idn.4xlarge"
      | M6idn_8xlarge -> "m6idn.8xlarge"
      | M6idn_12xlarge -> "m6idn.12xlarge"
      | M6idn_16xlarge -> "m6idn.16xlarge"
      | M6idn_24xlarge -> "m6idn.24xlarge"
      | M6idn_32xlarge -> "m6idn.32xlarge"
      | M6in_large -> "m6in.large"
      | M6in_xlarge -> "m6in.xlarge"
      | M6in_2xlarge -> "m6in.2xlarge"
      | M6in_4xlarge -> "m6in.4xlarge"
      | M6in_8xlarge -> "m6in.8xlarge"
      | M6in_12xlarge -> "m6in.12xlarge"
      | M6in_16xlarge -> "m6in.16xlarge"
      | M6in_24xlarge -> "m6in.24xlarge"
      | M6in_32xlarge -> "m6in.32xlarge"
      | M8g_medium -> "m8g.medium"
      | M8g_large -> "m8g.large"
      | M8g_xlarge -> "m8g.xlarge"
      | M8g_2xlarge -> "m8g.2xlarge"
      | M8g_4xlarge -> "m8g.4xlarge"
      | M8g_8xlarge -> "m8g.8xlarge"
      | M8g_12xlarge -> "m8g.12xlarge"
      | M8g_16xlarge -> "m8g.16xlarge"
      | M8g_24xlarge -> "m8g.24xlarge"
      | M8g_48xlarge -> "m8g.48xlarge"
      | R5dn_large -> "r5dn.large"
      | R5dn_xlarge -> "r5dn.xlarge"
      | R5dn_2xlarge -> "r5dn.2xlarge"
      | R5dn_4xlarge -> "r5dn.4xlarge"
      | R5dn_8xlarge -> "r5dn.8xlarge"
      | R5dn_12xlarge -> "r5dn.12xlarge"
      | R5dn_16xlarge -> "r5dn.16xlarge"
      | R5dn_24xlarge -> "r5dn.24xlarge"
      | R5n_large -> "r5n.large"
      | R5n_xlarge -> "r5n.xlarge"
      | R5n_2xlarge -> "r5n.2xlarge"
      | R5n_4xlarge -> "r5n.4xlarge"
      | R5n_8xlarge -> "r5n.8xlarge"
      | R5n_12xlarge -> "r5n.12xlarge"
      | R5n_16xlarge -> "r5n.16xlarge"
      | R5n_24xlarge -> "r5n.24xlarge"
      | R6a_large -> "r6a.large"
      | R6a_xlarge -> "r6a.xlarge"
      | R6a_2xlarge -> "r6a.2xlarge"
      | R6a_4xlarge -> "r6a.4xlarge"
      | R6a_8xlarge -> "r6a.8xlarge"
      | R6a_12xlarge -> "r6a.12xlarge"
      | R6a_16xlarge -> "r6a.16xlarge"
      | R6a_24xlarge -> "r6a.24xlarge"
      | R6a_32xlarge -> "r6a.32xlarge"
      | R6a_48xlarge -> "r6a.48xlarge"
      | R6id_large -> "r6id.large"
      | R6id_xlarge -> "r6id.xlarge"
      | R6id_2xlarge -> "r6id.2xlarge"
      | R6id_4xlarge -> "r6id.4xlarge"
      | R6id_8xlarge -> "r6id.8xlarge"
      | R6id_12xlarge -> "r6id.12xlarge"
      | R6id_16xlarge -> "r6id.16xlarge"
      | R6id_24xlarge -> "r6id.24xlarge"
      | R6id_32xlarge -> "r6id.32xlarge"
      | R6idn_large -> "r6idn.large"
      | R6idn_xlarge -> "r6idn.xlarge"
      | R6idn_2xlarge -> "r6idn.2xlarge"
      | R6idn_4xlarge -> "r6idn.4xlarge"
      | R6idn_8xlarge -> "r6idn.8xlarge"
      | R6idn_12xlarge -> "r6idn.12xlarge"
      | R6idn_16xlarge -> "r6idn.16xlarge"
      | R6idn_24xlarge -> "r6idn.24xlarge"
      | R6idn_32xlarge -> "r6idn.32xlarge"
      | R6in_large -> "r6in.large"
      | R6in_xlarge -> "r6in.xlarge"
      | R6in_2xlarge -> "r6in.2xlarge"
      | R6in_4xlarge -> "r6in.4xlarge"
      | R6in_8xlarge -> "r6in.8xlarge"
      | R6in_12xlarge -> "r6in.12xlarge"
      | R6in_16xlarge -> "r6in.16xlarge"
      | R6in_24xlarge -> "r6in.24xlarge"
      | R6in_32xlarge -> "r6in.32xlarge"
      | R8g_medium -> "r8g.medium"
      | R8g_large -> "r8g.large"
      | R8g_xlarge -> "r8g.xlarge"
      | R8g_2xlarge -> "r8g.2xlarge"
      | R8g_4xlarge -> "r8g.4xlarge"
      | R8g_8xlarge -> "r8g.8xlarge"
      | R8g_12xlarge -> "r8g.12xlarge"
      | R8g_16xlarge -> "r8g.16xlarge"
      | R8g_24xlarge -> "r8g.24xlarge"
      | R8g_48xlarge -> "r8g.48xlarge"
      | M4_16xlarge -> "m4.16xlarge"
      | C6a_32xlarge -> "c6a.32xlarge"
      | C6a_48xlarge -> "c6a.48xlarge"
      | C6i_32xlarge -> "c6i.32xlarge"
      | R6i_24xlarge -> "r6i.24xlarge"
      | R6i_32xlarge -> "r6i.32xlarge"
      | C6in_24xlarge -> "c6in.24xlarge"
      | C6in_32xlarge -> "c6in.32xlarge"
      | C7a_24xlarge -> "c7a.24xlarge"
      | C7a_32xlarge -> "c7a.32xlarge"
      | C7a_48xlarge -> "c7a.48xlarge"
      | C7i_24xlarge -> "c7i.24xlarge"
      | C7i_48xlarge -> "c7i.48xlarge"
      | M6a_24xlarge -> "m6a.24xlarge"
      | M6a_32xlarge -> "m6a.32xlarge"
      | M6a_48xlarge -> "m6a.48xlarge"
      | M6i_24xlarge -> "m6i.24xlarge"
      | M6i_32xlarge -> "m6i.32xlarge"
      | M7a_24xlarge -> "m7a.24xlarge"
      | M7a_32xlarge -> "m7a.32xlarge"
      | M7a_48xlarge -> "m7a.48xlarge"
      | M7i_24xlarge -> "m7i.24xlarge"
      | M7i_48xlarge -> "m7i.48xlarge"
      | R7a_24xlarge -> "r7a.24xlarge"
      | R7a_32xlarge -> "r7a.32xlarge"
      | R7a_48xlarge -> "r7a.48xlarge"
      | Non_static_id s -> s
    let of_string =
      function
      | "t2.micro" -> T2_micro
      | "t2.small" -> T2_small
      | "t2.medium" -> T2_medium
      | "t2.large" -> T2_large
      | "c3.large" -> C3_large
      | "c3.xlarge" -> C3_xlarge
      | "c3.2xlarge" -> C3_2xlarge
      | "c3.4xlarge" -> C3_4xlarge
      | "c3.8xlarge" -> C3_8xlarge
      | "c4.large" -> C4_large
      | "c4.xlarge" -> C4_xlarge
      | "c4.2xlarge" -> C4_2xlarge
      | "c4.4xlarge" -> C4_4xlarge
      | "c4.8xlarge" -> C4_8xlarge
      | "c5.large" -> C5_large
      | "c5.xlarge" -> C5_xlarge
      | "c5.2xlarge" -> C5_2xlarge
      | "c5.4xlarge" -> C5_4xlarge
      | "c5.9xlarge" -> C5_9xlarge
      | "c5.12xlarge" -> C5_12xlarge
      | "c5.18xlarge" -> C5_18xlarge
      | "c5.24xlarge" -> C5_24xlarge
      | "c5a.large" -> C5a_large
      | "c5a.xlarge" -> C5a_xlarge
      | "c5a.2xlarge" -> C5a_2xlarge
      | "c5a.4xlarge" -> C5a_4xlarge
      | "c5a.8xlarge" -> C5a_8xlarge
      | "c5a.12xlarge" -> C5a_12xlarge
      | "c5a.16xlarge" -> C5a_16xlarge
      | "c5a.24xlarge" -> C5a_24xlarge
      | "r3.large" -> R3_large
      | "r3.xlarge" -> R3_xlarge
      | "r3.2xlarge" -> R3_2xlarge
      | "r3.4xlarge" -> R3_4xlarge
      | "r3.8xlarge" -> R3_8xlarge
      | "r4.large" -> R4_large
      | "r4.xlarge" -> R4_xlarge
      | "r4.2xlarge" -> R4_2xlarge
      | "r4.4xlarge" -> R4_4xlarge
      | "r4.8xlarge" -> R4_8xlarge
      | "r4.16xlarge" -> R4_16xlarge
      | "r5.large" -> R5_large
      | "r5.xlarge" -> R5_xlarge
      | "r5.2xlarge" -> R5_2xlarge
      | "r5.4xlarge" -> R5_4xlarge
      | "r5.8xlarge" -> R5_8xlarge
      | "r5.12xlarge" -> R5_12xlarge
      | "r5.16xlarge" -> R5_16xlarge
      | "r5.24xlarge" -> R5_24xlarge
      | "r5a.large" -> R5a_large
      | "r5a.xlarge" -> R5a_xlarge
      | "r5a.2xlarge" -> R5a_2xlarge
      | "r5a.4xlarge" -> R5a_4xlarge
      | "r5a.8xlarge" -> R5a_8xlarge
      | "r5a.12xlarge" -> R5a_12xlarge
      | "r5a.16xlarge" -> R5a_16xlarge
      | "r5a.24xlarge" -> R5a_24xlarge
      | "m3.medium" -> M3_medium
      | "m3.large" -> M3_large
      | "m3.xlarge" -> M3_xlarge
      | "m3.2xlarge" -> M3_2xlarge
      | "m4.large" -> M4_large
      | "m4.xlarge" -> M4_xlarge
      | "m4.2xlarge" -> M4_2xlarge
      | "m4.4xlarge" -> M4_4xlarge
      | "m4.10xlarge" -> M4_10xlarge
      | "m5.large" -> M5_large
      | "m5.xlarge" -> M5_xlarge
      | "m5.2xlarge" -> M5_2xlarge
      | "m5.4xlarge" -> M5_4xlarge
      | "m5.8xlarge" -> M5_8xlarge
      | "m5.12xlarge" -> M5_12xlarge
      | "m5.16xlarge" -> M5_16xlarge
      | "m5.24xlarge" -> M5_24xlarge
      | "m5a.large" -> M5a_large
      | "m5a.xlarge" -> M5a_xlarge
      | "m5a.2xlarge" -> M5a_2xlarge
      | "m5a.4xlarge" -> M5a_4xlarge
      | "m5a.8xlarge" -> M5a_8xlarge
      | "m5a.12xlarge" -> M5a_12xlarge
      | "m5a.16xlarge" -> M5a_16xlarge
      | "m5a.24xlarge" -> M5a_24xlarge
      | "c5d.large" -> C5d_large
      | "c5d.xlarge" -> C5d_xlarge
      | "c5d.2xlarge" -> C5d_2xlarge
      | "c5d.4xlarge" -> C5d_4xlarge
      | "c5d.9xlarge" -> C5d_9xlarge
      | "c5d.12xlarge" -> C5d_12xlarge
      | "c5d.18xlarge" -> C5d_18xlarge
      | "c5d.24xlarge" -> C5d_24xlarge
      | "c6a.large" -> C6a_large
      | "c6a.xlarge" -> C6a_xlarge
      | "c6a.2xlarge" -> C6a_2xlarge
      | "c6a.4xlarge" -> C6a_4xlarge
      | "c6a.8xlarge" -> C6a_8xlarge
      | "c6a.12xlarge" -> C6a_12xlarge
      | "c6a.16xlarge" -> C6a_16xlarge
      | "c6a.24xlarge" -> C6a_24xlarge
      | "c6i.large" -> C6i_large
      | "c6i.xlarge" -> C6i_xlarge
      | "c6i.2xlarge" -> C6i_2xlarge
      | "c6i.4xlarge" -> C6i_4xlarge
      | "c6i.8xlarge" -> C6i_8xlarge
      | "c6i.12xlarge" -> C6i_12xlarge
      | "c6i.16xlarge" -> C6i_16xlarge
      | "c6i.24xlarge" -> C6i_24xlarge
      | "r5d.large" -> R5d_large
      | "r5d.xlarge" -> R5d_xlarge
      | "r5d.2xlarge" -> R5d_2xlarge
      | "r5d.4xlarge" -> R5d_4xlarge
      | "r5d.8xlarge" -> R5d_8xlarge
      | "r5d.12xlarge" -> R5d_12xlarge
      | "r5d.16xlarge" -> R5d_16xlarge
      | "r5d.24xlarge" -> R5d_24xlarge
      | "m6g.medium" -> M6g_medium
      | "m6g.large" -> M6g_large
      | "m6g.xlarge" -> M6g_xlarge
      | "m6g.2xlarge" -> M6g_2xlarge
      | "m6g.4xlarge" -> M6g_4xlarge
      | "m6g.8xlarge" -> M6g_8xlarge
      | "m6g.12xlarge" -> M6g_12xlarge
      | "m6g.16xlarge" -> M6g_16xlarge
      | "c6g.medium" -> C6g_medium
      | "c6g.large" -> C6g_large
      | "c6g.xlarge" -> C6g_xlarge
      | "c6g.2xlarge" -> C6g_2xlarge
      | "c6g.4xlarge" -> C6g_4xlarge
      | "c6g.8xlarge" -> C6g_8xlarge
      | "c6g.12xlarge" -> C6g_12xlarge
      | "c6g.16xlarge" -> C6g_16xlarge
      | "r6g.medium" -> R6g_medium
      | "r6g.large" -> R6g_large
      | "r6g.xlarge" -> R6g_xlarge
      | "r6g.2xlarge" -> R6g_2xlarge
      | "r6g.4xlarge" -> R6g_4xlarge
      | "r6g.8xlarge" -> R6g_8xlarge
      | "r6g.12xlarge" -> R6g_12xlarge
      | "r6g.16xlarge" -> R6g_16xlarge
      | "c6gn.medium" -> C6gn_medium
      | "c6gn.large" -> C6gn_large
      | "c6gn.xlarge" -> C6gn_xlarge
      | "c6gn.2xlarge" -> C6gn_2xlarge
      | "c6gn.4xlarge" -> C6gn_4xlarge
      | "c6gn.8xlarge" -> C6gn_8xlarge
      | "c6gn.12xlarge" -> C6gn_12xlarge
      | "c6gn.16xlarge" -> C6gn_16xlarge
      | "c7g.medium" -> C7g_medium
      | "c7g.large" -> C7g_large
      | "c7g.xlarge" -> C7g_xlarge
      | "c7g.2xlarge" -> C7g_2xlarge
      | "c7g.4xlarge" -> C7g_4xlarge
      | "c7g.8xlarge" -> C7g_8xlarge
      | "c7g.12xlarge" -> C7g_12xlarge
      | "c7g.16xlarge" -> C7g_16xlarge
      | "r7g.medium" -> R7g_medium
      | "r7g.large" -> R7g_large
      | "r7g.xlarge" -> R7g_xlarge
      | "r7g.2xlarge" -> R7g_2xlarge
      | "r7g.4xlarge" -> R7g_4xlarge
      | "r7g.8xlarge" -> R7g_8xlarge
      | "r7g.12xlarge" -> R7g_12xlarge
      | "r7g.16xlarge" -> R7g_16xlarge
      | "m7g.medium" -> M7g_medium
      | "m7g.large" -> M7g_large
      | "m7g.xlarge" -> M7g_xlarge
      | "m7g.2xlarge" -> M7g_2xlarge
      | "m7g.4xlarge" -> M7g_4xlarge
      | "m7g.8xlarge" -> M7g_8xlarge
      | "m7g.12xlarge" -> M7g_12xlarge
      | "m7g.16xlarge" -> M7g_16xlarge
      | "g5g.xlarge" -> G5g_xlarge
      | "g5g.2xlarge" -> G5g_2xlarge
      | "g5g.4xlarge" -> G5g_4xlarge
      | "g5g.8xlarge" -> G5g_8xlarge
      | "g5g.16xlarge" -> G5g_16xlarge
      | "r6i.large" -> R6i_large
      | "r6i.xlarge" -> R6i_xlarge
      | "r6i.2xlarge" -> R6i_2xlarge
      | "r6i.4xlarge" -> R6i_4xlarge
      | "r6i.8xlarge" -> R6i_8xlarge
      | "r6i.12xlarge" -> R6i_12xlarge
      | "r6i.16xlarge" -> R6i_16xlarge
      | "c6gd.medium" -> C6gd_medium
      | "c6gd.large" -> C6gd_large
      | "c6gd.xlarge" -> C6gd_xlarge
      | "c6gd.2xlarge" -> C6gd_2xlarge
      | "c6gd.4xlarge" -> C6gd_4xlarge
      | "c6gd.8xlarge" -> C6gd_8xlarge
      | "c6gd.12xlarge" -> C6gd_12xlarge
      | "c6gd.16xlarge" -> C6gd_16xlarge
      | "c6in.large" -> C6in_large
      | "c6in.xlarge" -> C6in_xlarge
      | "c6in.2xlarge" -> C6in_2xlarge
      | "c6in.4xlarge" -> C6in_4xlarge
      | "c6in.8xlarge" -> C6in_8xlarge
      | "c6in.12xlarge" -> C6in_12xlarge
      | "c6in.16xlarge" -> C6in_16xlarge
      | "c7a.medium" -> C7a_medium
      | "c7a.large" -> C7a_large
      | "c7a.xlarge" -> C7a_xlarge
      | "c7a.2xlarge" -> C7a_2xlarge
      | "c7a.4xlarge" -> C7a_4xlarge
      | "c7a.8xlarge" -> C7a_8xlarge
      | "c7a.12xlarge" -> C7a_12xlarge
      | "c7a.16xlarge" -> C7a_16xlarge
      | "c7gd.medium" -> C7gd_medium
      | "c7gd.large" -> C7gd_large
      | "c7gd.xlarge" -> C7gd_xlarge
      | "c7gd.2xlarge" -> C7gd_2xlarge
      | "c7gd.4xlarge" -> C7gd_4xlarge
      | "c7gd.8xlarge" -> C7gd_8xlarge
      | "c7gd.12xlarge" -> C7gd_12xlarge
      | "c7gd.16xlarge" -> C7gd_16xlarge
      | "c7gn.medium" -> C7gn_medium
      | "c7gn.large" -> C7gn_large
      | "c7gn.xlarge" -> C7gn_xlarge
      | "c7gn.2xlarge" -> C7gn_2xlarge
      | "c7gn.4xlarge" -> C7gn_4xlarge
      | "c7gn.8xlarge" -> C7gn_8xlarge
      | "c7gn.12xlarge" -> C7gn_12xlarge
      | "c7gn.16xlarge" -> C7gn_16xlarge
      | "c7i.large" -> C7i_large
      | "c7i.xlarge" -> C7i_xlarge
      | "c7i.2xlarge" -> C7i_2xlarge
      | "c7i.4xlarge" -> C7i_4xlarge
      | "c7i.8xlarge" -> C7i_8xlarge
      | "c7i.12xlarge" -> C7i_12xlarge
      | "c7i.16xlarge" -> C7i_16xlarge
      | "m6a.large" -> M6a_large
      | "m6a.xlarge" -> M6a_xlarge
      | "m6a.2xlarge" -> M6a_2xlarge
      | "m6a.4xlarge" -> M6a_4xlarge
      | "m6a.8xlarge" -> M6a_8xlarge
      | "m6a.12xlarge" -> M6a_12xlarge
      | "m6a.16xlarge" -> M6a_16xlarge
      | "m6gd.medium" -> M6gd_medium
      | "m6gd.large" -> M6gd_large
      | "m6gd.xlarge" -> M6gd_xlarge
      | "m6gd.2xlarge" -> M6gd_2xlarge
      | "m6gd.4xlarge" -> M6gd_4xlarge
      | "m6gd.8xlarge" -> M6gd_8xlarge
      | "m6gd.12xlarge" -> M6gd_12xlarge
      | "m6gd.16xlarge" -> M6gd_16xlarge
      | "m6i.large" -> M6i_large
      | "m6i.xlarge" -> M6i_xlarge
      | "m6i.2xlarge" -> M6i_2xlarge
      | "m6i.4xlarge" -> M6i_4xlarge
      | "m6i.8xlarge" -> M6i_8xlarge
      | "m6i.12xlarge" -> M6i_12xlarge
      | "m6i.16xlarge" -> M6i_16xlarge
      | "m7a.medium" -> M7a_medium
      | "m7a.large" -> M7a_large
      | "m7a.xlarge" -> M7a_xlarge
      | "m7a.2xlarge" -> M7a_2xlarge
      | "m7a.4xlarge" -> M7a_4xlarge
      | "m7a.8xlarge" -> M7a_8xlarge
      | "m7a.12xlarge" -> M7a_12xlarge
      | "m7a.16xlarge" -> M7a_16xlarge
      | "m7gd.medium" -> M7gd_medium
      | "m7gd.large" -> M7gd_large
      | "m7gd.xlarge" -> M7gd_xlarge
      | "m7gd.2xlarge" -> M7gd_2xlarge
      | "m7gd.4xlarge" -> M7gd_4xlarge
      | "m7gd.8xlarge" -> M7gd_8xlarge
      | "m7gd.12xlarge" -> M7gd_12xlarge
      | "m7gd.16xlarge" -> M7gd_16xlarge
      | "m7i.large" -> M7i_large
      | "m7i.xlarge" -> M7i_xlarge
      | "m7i.2xlarge" -> M7i_2xlarge
      | "m7i.4xlarge" -> M7i_4xlarge
      | "m7i.8xlarge" -> M7i_8xlarge
      | "m7i.12xlarge" -> M7i_12xlarge
      | "m7i.16xlarge" -> M7i_16xlarge
      | "r6gd.medium" -> R6gd_medium
      | "r6gd.large" -> R6gd_large
      | "r6gd.xlarge" -> R6gd_xlarge
      | "r6gd.2xlarge" -> R6gd_2xlarge
      | "r6gd.4xlarge" -> R6gd_4xlarge
      | "r6gd.8xlarge" -> R6gd_8xlarge
      | "r6gd.12xlarge" -> R6gd_12xlarge
      | "r6gd.16xlarge" -> R6gd_16xlarge
      | "r7a.medium" -> R7a_medium
      | "r7a.large" -> R7a_large
      | "r7a.xlarge" -> R7a_xlarge
      | "r7a.2xlarge" -> R7a_2xlarge
      | "r7a.4xlarge" -> R7a_4xlarge
      | "r7a.8xlarge" -> R7a_8xlarge
      | "r7a.12xlarge" -> R7a_12xlarge
      | "r7a.16xlarge" -> R7a_16xlarge
      | "r7gd.medium" -> R7gd_medium
      | "r7gd.large" -> R7gd_large
      | "r7gd.xlarge" -> R7gd_xlarge
      | "r7gd.2xlarge" -> R7gd_2xlarge
      | "r7gd.4xlarge" -> R7gd_4xlarge
      | "r7gd.8xlarge" -> R7gd_8xlarge
      | "r7gd.12xlarge" -> R7gd_12xlarge
      | "r7gd.16xlarge" -> R7gd_16xlarge
      | "r7i.large" -> R7i_large
      | "r7i.xlarge" -> R7i_xlarge
      | "r7i.2xlarge" -> R7i_2xlarge
      | "r7i.4xlarge" -> R7i_4xlarge
      | "r7i.8xlarge" -> R7i_8xlarge
      | "r7i.12xlarge" -> R7i_12xlarge
      | "r7i.16xlarge" -> R7i_16xlarge
      | "r7i.24xlarge" -> R7i_24xlarge
      | "r7i.48xlarge" -> R7i_48xlarge
      | "c5ad.large" -> C5ad_large
      | "c5ad.xlarge" -> C5ad_xlarge
      | "c5ad.2xlarge" -> C5ad_2xlarge
      | "c5ad.4xlarge" -> C5ad_4xlarge
      | "c5ad.8xlarge" -> C5ad_8xlarge
      | "c5ad.12xlarge" -> C5ad_12xlarge
      | "c5ad.16xlarge" -> C5ad_16xlarge
      | "c5ad.24xlarge" -> C5ad_24xlarge
      | "c5n.large" -> C5n_large
      | "c5n.xlarge" -> C5n_xlarge
      | "c5n.2xlarge" -> C5n_2xlarge
      | "c5n.4xlarge" -> C5n_4xlarge
      | "c5n.9xlarge" -> C5n_9xlarge
      | "c5n.18xlarge" -> C5n_18xlarge
      | "r5ad.large" -> R5ad_large
      | "r5ad.xlarge" -> R5ad_xlarge
      | "r5ad.2xlarge" -> R5ad_2xlarge
      | "r5ad.4xlarge" -> R5ad_4xlarge
      | "r5ad.8xlarge" -> R5ad_8xlarge
      | "r5ad.12xlarge" -> R5ad_12xlarge
      | "r5ad.16xlarge" -> R5ad_16xlarge
      | "r5ad.24xlarge" -> R5ad_24xlarge
      | "c6id.large" -> C6id_large
      | "c6id.xlarge" -> C6id_xlarge
      | "c6id.2xlarge" -> C6id_2xlarge
      | "c6id.4xlarge" -> C6id_4xlarge
      | "c6id.8xlarge" -> C6id_8xlarge
      | "c6id.12xlarge" -> C6id_12xlarge
      | "c6id.16xlarge" -> C6id_16xlarge
      | "c6id.24xlarge" -> C6id_24xlarge
      | "c6id.32xlarge" -> C6id_32xlarge
      | "c8g.medium" -> C8g_medium
      | "c8g.large" -> C8g_large
      | "c8g.xlarge" -> C8g_xlarge
      | "c8g.2xlarge" -> C8g_2xlarge
      | "c8g.4xlarge" -> C8g_4xlarge
      | "c8g.8xlarge" -> C8g_8xlarge
      | "c8g.12xlarge" -> C8g_12xlarge
      | "c8g.16xlarge" -> C8g_16xlarge
      | "c8g.24xlarge" -> C8g_24xlarge
      | "c8g.48xlarge" -> C8g_48xlarge
      | "m5ad.large" -> M5ad_large
      | "m5ad.xlarge" -> M5ad_xlarge
      | "m5ad.2xlarge" -> M5ad_2xlarge
      | "m5ad.4xlarge" -> M5ad_4xlarge
      | "m5ad.8xlarge" -> M5ad_8xlarge
      | "m5ad.12xlarge" -> M5ad_12xlarge
      | "m5ad.16xlarge" -> M5ad_16xlarge
      | "m5ad.24xlarge" -> M5ad_24xlarge
      | "m5d.large" -> M5d_large
      | "m5d.xlarge" -> M5d_xlarge
      | "m5d.2xlarge" -> M5d_2xlarge
      | "m5d.4xlarge" -> M5d_4xlarge
      | "m5d.8xlarge" -> M5d_8xlarge
      | "m5d.12xlarge" -> M5d_12xlarge
      | "m5d.16xlarge" -> M5d_16xlarge
      | "m5d.24xlarge" -> M5d_24xlarge
      | "m5dn.large" -> M5dn_large
      | "m5dn.xlarge" -> M5dn_xlarge
      | "m5dn.2xlarge" -> M5dn_2xlarge
      | "m5dn.4xlarge" -> M5dn_4xlarge
      | "m5dn.8xlarge" -> M5dn_8xlarge
      | "m5dn.12xlarge" -> M5dn_12xlarge
      | "m5dn.16xlarge" -> M5dn_16xlarge
      | "m5dn.24xlarge" -> M5dn_24xlarge
      | "m5n.large" -> M5n_large
      | "m5n.xlarge" -> M5n_xlarge
      | "m5n.2xlarge" -> M5n_2xlarge
      | "m5n.4xlarge" -> M5n_4xlarge
      | "m5n.8xlarge" -> M5n_8xlarge
      | "m5n.12xlarge" -> M5n_12xlarge
      | "m5n.16xlarge" -> M5n_16xlarge
      | "m5n.24xlarge" -> M5n_24xlarge
      | "m6id.large" -> M6id_large
      | "m6id.xlarge" -> M6id_xlarge
      | "m6id.2xlarge" -> M6id_2xlarge
      | "m6id.4xlarge" -> M6id_4xlarge
      | "m6id.8xlarge" -> M6id_8xlarge
      | "m6id.12xlarge" -> M6id_12xlarge
      | "m6id.16xlarge" -> M6id_16xlarge
      | "m6id.24xlarge" -> M6id_24xlarge
      | "m6id.32xlarge" -> M6id_32xlarge
      | "m6idn.large" -> M6idn_large
      | "m6idn.xlarge" -> M6idn_xlarge
      | "m6idn.2xlarge" -> M6idn_2xlarge
      | "m6idn.4xlarge" -> M6idn_4xlarge
      | "m6idn.8xlarge" -> M6idn_8xlarge
      | "m6idn.12xlarge" -> M6idn_12xlarge
      | "m6idn.16xlarge" -> M6idn_16xlarge
      | "m6idn.24xlarge" -> M6idn_24xlarge
      | "m6idn.32xlarge" -> M6idn_32xlarge
      | "m6in.large" -> M6in_large
      | "m6in.xlarge" -> M6in_xlarge
      | "m6in.2xlarge" -> M6in_2xlarge
      | "m6in.4xlarge" -> M6in_4xlarge
      | "m6in.8xlarge" -> M6in_8xlarge
      | "m6in.12xlarge" -> M6in_12xlarge
      | "m6in.16xlarge" -> M6in_16xlarge
      | "m6in.24xlarge" -> M6in_24xlarge
      | "m6in.32xlarge" -> M6in_32xlarge
      | "m8g.medium" -> M8g_medium
      | "m8g.large" -> M8g_large
      | "m8g.xlarge" -> M8g_xlarge
      | "m8g.2xlarge" -> M8g_2xlarge
      | "m8g.4xlarge" -> M8g_4xlarge
      | "m8g.8xlarge" -> M8g_8xlarge
      | "m8g.12xlarge" -> M8g_12xlarge
      | "m8g.16xlarge" -> M8g_16xlarge
      | "m8g.24xlarge" -> M8g_24xlarge
      | "m8g.48xlarge" -> M8g_48xlarge
      | "r5dn.large" -> R5dn_large
      | "r5dn.xlarge" -> R5dn_xlarge
      | "r5dn.2xlarge" -> R5dn_2xlarge
      | "r5dn.4xlarge" -> R5dn_4xlarge
      | "r5dn.8xlarge" -> R5dn_8xlarge
      | "r5dn.12xlarge" -> R5dn_12xlarge
      | "r5dn.16xlarge" -> R5dn_16xlarge
      | "r5dn.24xlarge" -> R5dn_24xlarge
      | "r5n.large" -> R5n_large
      | "r5n.xlarge" -> R5n_xlarge
      | "r5n.2xlarge" -> R5n_2xlarge
      | "r5n.4xlarge" -> R5n_4xlarge
      | "r5n.8xlarge" -> R5n_8xlarge
      | "r5n.12xlarge" -> R5n_12xlarge
      | "r5n.16xlarge" -> R5n_16xlarge
      | "r5n.24xlarge" -> R5n_24xlarge
      | "r6a.large" -> R6a_large
      | "r6a.xlarge" -> R6a_xlarge
      | "r6a.2xlarge" -> R6a_2xlarge
      | "r6a.4xlarge" -> R6a_4xlarge
      | "r6a.8xlarge" -> R6a_8xlarge
      | "r6a.12xlarge" -> R6a_12xlarge
      | "r6a.16xlarge" -> R6a_16xlarge
      | "r6a.24xlarge" -> R6a_24xlarge
      | "r6a.32xlarge" -> R6a_32xlarge
      | "r6a.48xlarge" -> R6a_48xlarge
      | "r6id.large" -> R6id_large
      | "r6id.xlarge" -> R6id_xlarge
      | "r6id.2xlarge" -> R6id_2xlarge
      | "r6id.4xlarge" -> R6id_4xlarge
      | "r6id.8xlarge" -> R6id_8xlarge
      | "r6id.12xlarge" -> R6id_12xlarge
      | "r6id.16xlarge" -> R6id_16xlarge
      | "r6id.24xlarge" -> R6id_24xlarge
      | "r6id.32xlarge" -> R6id_32xlarge
      | "r6idn.large" -> R6idn_large
      | "r6idn.xlarge" -> R6idn_xlarge
      | "r6idn.2xlarge" -> R6idn_2xlarge
      | "r6idn.4xlarge" -> R6idn_4xlarge
      | "r6idn.8xlarge" -> R6idn_8xlarge
      | "r6idn.12xlarge" -> R6idn_12xlarge
      | "r6idn.16xlarge" -> R6idn_16xlarge
      | "r6idn.24xlarge" -> R6idn_24xlarge
      | "r6idn.32xlarge" -> R6idn_32xlarge
      | "r6in.large" -> R6in_large
      | "r6in.xlarge" -> R6in_xlarge
      | "r6in.2xlarge" -> R6in_2xlarge
      | "r6in.4xlarge" -> R6in_4xlarge
      | "r6in.8xlarge" -> R6in_8xlarge
      | "r6in.12xlarge" -> R6in_12xlarge
      | "r6in.16xlarge" -> R6in_16xlarge
      | "r6in.24xlarge" -> R6in_24xlarge
      | "r6in.32xlarge" -> R6in_32xlarge
      | "r8g.medium" -> R8g_medium
      | "r8g.large" -> R8g_large
      | "r8g.xlarge" -> R8g_xlarge
      | "r8g.2xlarge" -> R8g_2xlarge
      | "r8g.4xlarge" -> R8g_4xlarge
      | "r8g.8xlarge" -> R8g_8xlarge
      | "r8g.12xlarge" -> R8g_12xlarge
      | "r8g.16xlarge" -> R8g_16xlarge
      | "r8g.24xlarge" -> R8g_24xlarge
      | "r8g.48xlarge" -> R8g_48xlarge
      | "m4.16xlarge" -> M4_16xlarge
      | "c6a.32xlarge" -> C6a_32xlarge
      | "c6a.48xlarge" -> C6a_48xlarge
      | "c6i.32xlarge" -> C6i_32xlarge
      | "r6i.24xlarge" -> R6i_24xlarge
      | "r6i.32xlarge" -> R6i_32xlarge
      | "c6in.24xlarge" -> C6in_24xlarge
      | "c6in.32xlarge" -> C6in_32xlarge
      | "c7a.24xlarge" -> C7a_24xlarge
      | "c7a.32xlarge" -> C7a_32xlarge
      | "c7a.48xlarge" -> C7a_48xlarge
      | "c7i.24xlarge" -> C7i_24xlarge
      | "c7i.48xlarge" -> C7i_48xlarge
      | "m6a.24xlarge" -> M6a_24xlarge
      | "m6a.32xlarge" -> M6a_32xlarge
      | "m6a.48xlarge" -> M6a_48xlarge
      | "m6i.24xlarge" -> M6i_24xlarge
      | "m6i.32xlarge" -> M6i_32xlarge
      | "m7a.24xlarge" -> M7a_24xlarge
      | "m7a.32xlarge" -> M7a_32xlarge
      | "m7a.48xlarge" -> M7a_48xlarge
      | "m7i.24xlarge" -> M7i_24xlarge
      | "m7i.48xlarge" -> M7i_48xlarge
      | "r7a.24xlarge" -> R7a_24xlarge
      | "r7a.32xlarge" -> R7a_32xlarge
      | "r7a.48xlarge" -> R7a_48xlarge
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EC2InstanceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EC2InstanceType" j)
    let to_json = simple_to_json to_value
  end
module GameLiftAgentEndpointOutput =
  struct
    type nonrec t = string
    let context_ = "GameLiftAgentEndpointOutput"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GameLiftAgentEndpointOutput" j
    let to_json = simple_to_json to_value
  end
module GameLiftServiceSdkEndpointOutput =
  struct
    type nonrec t = string
    let context_ = "GameLiftServiceSdkEndpointOutput"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GameLiftServiceSdkEndpointOutput" j
    let to_json = simple_to_json to_value
  end
module InstanceId =
  struct
    type nonrec t = string
    let context_ = "InstanceId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[a-zA-Z0-9\\.-]+"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InstanceId" j
    let to_json = simple_to_json to_value
  end
module OperatingSystem =
  struct
    type nonrec t =
      | WINDOWS_2012 
      | AMAZON_LINUX 
      | AMAZON_LINUX_2 
      | WINDOWS_2016 
      | AMAZON_LINUX_2023 
      | WINDOWS_2022 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | WINDOWS_2012 -> "WINDOWS_2012"
      | AMAZON_LINUX -> "AMAZON_LINUX"
      | AMAZON_LINUX_2 -> "AMAZON_LINUX_2"
      | WINDOWS_2016 -> "WINDOWS_2016"
      | AMAZON_LINUX_2023 -> "AMAZON_LINUX_2023"
      | WINDOWS_2022 -> "WINDOWS_2022"
      | Non_static_id s -> s
    let of_string =
      function
      | "WINDOWS_2012" -> WINDOWS_2012
      | "AMAZON_LINUX" -> AMAZON_LINUX
      | "AMAZON_LINUX_2" -> AMAZON_LINUX_2
      | "WINDOWS_2016" -> WINDOWS_2016
      | "AMAZON_LINUX_2023" -> AMAZON_LINUX_2023
      | "WINDOWS_2022" -> WINDOWS_2022
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration OperatingSystem" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"OperatingSystem" j)
    let to_json = simple_to_json to_value
  end
module BuildArn =
  struct
    type nonrec t = string
    let context_ = "BuildArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^arn:.*:build\\/build-\\S+");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BuildArn" j
    let to_json = simple_to_json to_value
  end
module BuildId =
  struct
    type nonrec t = string
    let context_ = "BuildId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"^build-\\S+"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BuildId" j
    let to_json = simple_to_json to_value
  end
module BuildStatus =
  struct
    type nonrec t =
      | INITIALIZED 
      | READY 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INITIALIZED -> "INITIALIZED"
      | READY -> "READY"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "INITIALIZED" -> INITIALIZED
      | "READY" -> READY
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration BuildStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BuildStatus" j)
    let to_json = simple_to_json to_value
  end
module AliasArn =
  struct
    type nonrec t = string
    let context_ = "AliasArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^arn:.*:alias\\/alias-\\S+");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AliasArn" j
    let to_json = simple_to_json to_value
  end
module AliasId =
  struct
    type nonrec t = string
    let context_ = "AliasId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"^alias-\\S+"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AliasId" j
    let to_json = simple_to_json to_value
  end
module NonBlankAndLengthConstraintString =
  struct
    type nonrec t = string
    let context_ = "NonBlankAndLengthConstraintString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"NonBlankAndLengthConstraintString" j
    let to_json = simple_to_json to_value
  end
module RoutingStrategy =
  struct
    type nonrec t =
      {
      type_: RoutingStrategyType.t option
        [@ocaml.doc
          "The type of routing strategy for the alias. Possible routing types include the following: SIMPLE - The alias resolves to one specific fleet. Use this type when routing to active fleets. TERMINAL - The alias does not resolve to a fleet but instead can be used to display a message to the user. A terminal alias throws a TerminalRoutingStrategyException with the message embedded."];
      fleetId: FleetId.t option
        [@ocaml.doc
          "A unique identifier for the fleet that the alias points to. This value is the fleet ID, not the fleet ARN."];
      message: FreeText.t option
        [@ocaml.doc
          "The message text to be used with a terminal routing strategy."]}
    let make ?type_ =
      fun ?fleetId -> fun ?message -> fun () -> { type_; fleetId; message }
    let to_value x =
      structure_to_value
        [("Type", (Option.map x.type_ ~f:RoutingStrategyType.to_value));
        ("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("Message", (Option.map x.message ~f:FreeText.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:FreeText.of_xml) (Xml.child xml_arg0 "Message") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      let type_ =
        (Option.map ~f:RoutingStrategyType.of_xml)
          (Xml.child xml_arg0 "Type") in
      make ?message ?fleetId ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" FreeText.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      let type_ = field_map json__ "Type" RoutingStrategyType.of_json in
      make ?message ?fleetId ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The routing configuration for a fleet alias. Related actions All APIs by task"]
module MaxString =
  struct
    type nonrec t = string
    let context_ = "MaxString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MaxString" j
    let to_json = simple_to_json to_value
  end
module PlayerConnectionEndpointList =
  struct
    type nonrec t = PlayerConnectionEndpoint.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PlayerConnectionEndpoint.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlayerConnectionEndpoint.of_xml)
    let of_json j =
      list_of_json ~kind:"PlayerConnectionEndpointList"
        ~of_json:PlayerConnectionEndpoint.of_json j
    let to_json v = composed_to_json to_value v
  end
module VpcPeeringConnectionStatus =
  struct
    type nonrec t =
      {
      code: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Code indicating the status of a VPC peering connection."];
      message: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Additional messaging associated with the connection status."]}
    let make ?code = fun ?message -> fun () -> { code; message }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:NonZeroAndMaxString.to_value));
        ("Message", (Option.map x.message ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Message") in
      let code =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Code") in
      make ?message ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonZeroAndMaxString.of_json in
      let code = field_map json__ "Code" NonZeroAndMaxString.of_json in
      make ?message ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents status information for a VPC peering connection. Status codes and messages are provided from EC2 (see VpcPeeringConnectionStateReason). Connection status information is also communicated as a fleet event."]
module ComparisonOperatorType =
  struct
    type nonrec t =
      | GreaterThanOrEqualToThreshold 
      | GreaterThanThreshold 
      | LessThanThreshold 
      | LessThanOrEqualToThreshold 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GreaterThanOrEqualToThreshold -> "GreaterThanOrEqualToThreshold"
      | GreaterThanThreshold -> "GreaterThanThreshold"
      | LessThanThreshold -> "LessThanThreshold"
      | LessThanOrEqualToThreshold -> "LessThanOrEqualToThreshold"
      | Non_static_id s -> s
    let of_string =
      function
      | "GreaterThanOrEqualToThreshold" -> GreaterThanOrEqualToThreshold
      | "GreaterThanThreshold" -> GreaterThanThreshold
      | "LessThanThreshold" -> LessThanThreshold
      | "LessThanOrEqualToThreshold" -> LessThanOrEqualToThreshold
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ComparisonOperatorType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ComparisonOperatorType" j)
    let to_json = simple_to_json to_value
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module LocationUpdateStatus =
  struct
    type nonrec t =
      | PENDING_UPDATE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | PENDING_UPDATE -> "PENDING_UPDATE" | Non_static_id s -> s
    let of_string =
      function | "PENDING_UPDATE" -> PENDING_UPDATE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration LocationUpdateStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LocationUpdateStatus" j)
    let to_json = simple_to_json to_value
  end
module MetricName =
  struct
    type nonrec t =
      | ActivatingGameSessions 
      | ActiveGameSessions 
      | ActiveInstances 
      | AvailableGameSessions 
      | AvailablePlayerSessions 
      | CurrentPlayerSessions 
      | IdleInstances 
      | PercentAvailableGameSessions 
      | PercentIdleInstances 
      | QueueDepth 
      | WaitTime 
      | ConcurrentActivatableGameSessions 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ActivatingGameSessions -> "ActivatingGameSessions"
      | ActiveGameSessions -> "ActiveGameSessions"
      | ActiveInstances -> "ActiveInstances"
      | AvailableGameSessions -> "AvailableGameSessions"
      | AvailablePlayerSessions -> "AvailablePlayerSessions"
      | CurrentPlayerSessions -> "CurrentPlayerSessions"
      | IdleInstances -> "IdleInstances"
      | PercentAvailableGameSessions -> "PercentAvailableGameSessions"
      | PercentIdleInstances -> "PercentIdleInstances"
      | QueueDepth -> "QueueDepth"
      | WaitTime -> "WaitTime"
      | ConcurrentActivatableGameSessions ->
          "ConcurrentActivatableGameSessions"
      | Non_static_id s -> s
    let of_string =
      function
      | "ActivatingGameSessions" -> ActivatingGameSessions
      | "ActiveGameSessions" -> ActiveGameSessions
      | "ActiveInstances" -> ActiveInstances
      | "AvailableGameSessions" -> AvailableGameSessions
      | "AvailablePlayerSessions" -> AvailablePlayerSessions
      | "CurrentPlayerSessions" -> CurrentPlayerSessions
      | "IdleInstances" -> IdleInstances
      | "PercentAvailableGameSessions" -> PercentAvailableGameSessions
      | "PercentIdleInstances" -> PercentIdleInstances
      | "QueueDepth" -> QueueDepth
      | "WaitTime" -> WaitTime
      | "ConcurrentActivatableGameSessions" ->
          ConcurrentActivatableGameSessions
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration MetricName" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"MetricName" j)
    let to_json = simple_to_json to_value
  end
module PolicyType =
  struct
    type nonrec t =
      | RuleBased 
      | TargetBased 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | RuleBased -> "RuleBased"
      | TargetBased -> "TargetBased"
      | Non_static_id s -> s
    let of_string =
      function
      | "RuleBased" -> RuleBased
      | "TargetBased" -> TargetBased
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PolicyType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PolicyType" j)
    let to_json = simple_to_json to_value
  end
module ScalingAdjustmentType =
  struct
    type nonrec t =
      | ChangeInCapacity 
      | ExactCapacity 
      | PercentChangeInCapacity 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ChangeInCapacity -> "ChangeInCapacity"
      | ExactCapacity -> "ExactCapacity"
      | PercentChangeInCapacity -> "PercentChangeInCapacity"
      | Non_static_id s -> s
    let of_string =
      function
      | "ChangeInCapacity" -> ChangeInCapacity
      | "ExactCapacity" -> ExactCapacity
      | "PercentChangeInCapacity" -> PercentChangeInCapacity
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ScalingAdjustmentType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ScalingAdjustmentType" j)
    let to_json = simple_to_json to_value
  end
module ScalingStatusType =
  struct
    type nonrec t =
      | ACTIVE 
      | UPDATE_REQUESTED 
      | UPDATING 
      | DELETE_REQUESTED 
      | DELETING 
      | DELETED 
      | ERROR 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | UPDATE_REQUESTED -> "UPDATE_REQUESTED"
      | UPDATING -> "UPDATING"
      | DELETE_REQUESTED -> "DELETE_REQUESTED"
      | DELETING -> "DELETING"
      | DELETED -> "DELETED"
      | ERROR -> "ERROR"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "UPDATE_REQUESTED" -> UPDATE_REQUESTED
      | "UPDATING" -> UPDATING
      | "DELETE_REQUESTED" -> DELETE_REQUESTED
      | "DELETING" -> DELETING
      | "DELETED" -> DELETED
      | "ERROR" -> ERROR
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ScalingStatusType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ScalingStatusType" j)
    let to_json = simple_to_json to_value
  end
module TargetConfiguration =
  struct
    type nonrec t =
      {
      targetValue: Double.t
        [@ocaml.doc
          "Desired value to use with a target-based scaling policy. The value must be relevant for whatever metric the scaling policy is using. For example, in a policy using the metric PercentAvailableGameSessions, the target value should be the preferred size of the fleet's buffer (the percent of capacity that should be idle and ready for new game sessions)."]}
    let context_ = "TargetConfiguration"
    let make ~targetValue = fun () -> { targetValue }
    let to_value x =
      structure_to_value
        [("TargetValue", (Some (Double.to_value x.targetValue)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetValue =
        Double.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetValue") in
      make ~targetValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetValue = field_map_exn json__ "TargetValue" Double.of_json in
      make ~targetValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Settings for a target-based scaling policy. A target-based policy tracks a particular fleet metric specifies a target value for the metric. As player usage changes, the policy triggers Amazon GameLift Servers to adjust capacity so that the metric returns to the target value. The target configuration specifies settings as needed for the target based policy, including the target value."]
module PlayerSessionStatus =
  struct
    type nonrec t =
      | RESERVED 
      | ACTIVE 
      | COMPLETED 
      | TIMEDOUT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | RESERVED -> "RESERVED"
      | ACTIVE -> "ACTIVE"
      | COMPLETED -> "COMPLETED"
      | TIMEDOUT -> "TIMEDOUT"
      | Non_static_id s -> s
    let of_string =
      function
      | "RESERVED" -> RESERVED
      | "ACTIVE" -> ACTIVE
      | "COMPLETED" -> COMPLETED
      | "TIMEDOUT" -> TIMEDOUT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PlayerSessionStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PlayerSessionStatus" j)
    let to_json = simple_to_json to_value
  end
module MatchmakingIdStringModel =
  struct
    type nonrec t = string
    let context_ = "MatchmakingIdStringModel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-\\.]*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MatchmakingIdStringModel" j
    let to_json = simple_to_json to_value
  end
module MatchmakingRuleSetArn =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Data type used for Matchmaking RuleSet ARN."]
    let context_ = "MatchmakingRuleSetArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"^arn:.*:matchmakingruleset\\/[a-zA-Z0-9-\\.]*");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MatchmakingRuleSetArn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Data type used for Matchmaking RuleSet ARN."]
module RuleSetBody =
  struct
    type nonrec t = string
    let context_ = "RuleSetBody"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:65535) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RuleSetBody" j
    let to_json = simple_to_json to_value
  end
module GameSessionConnectionInfo =
  struct
    type nonrec t =
      {
      gameSessionArn: ArnStringModel.t option
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>."];
      ipAddress: IpAddress.t option
        [@ocaml.doc
          "The IP address of the game session. To connect to a Amazon GameLift Servers game server, an app needs both the IP address and port number."];
      dnsName: DnsName.t option
        [@ocaml.doc
          "The DNS identifier assigned to the instance that is running the game session. Values have the following format: TLS-enabled fleets: <unique identifier>.<region identifier>.amazongamelift.com. Non-TLS-enabled fleets: ec2-<unique identifier>.compute.amazonaws.com. (See Amazon EC2 Instance IP Addressing.) When connecting to a game session that is running on a TLS-enabled fleet, you must use the DNS name, not the IP address."];
      port: PositiveInteger.t option
        [@ocaml.doc
          "The port number for the game session. To connect to a Amazon GameLift Servers game server, an app needs both the IP address and port number."];
      matchedPlayerSessions: MatchedPlayerSessionList.t option
        [@ocaml.doc
          "A collection of player session IDs, one for each player ID that was included in the original matchmaking request."];
      playerGatewayStatus: PlayerGatewayStatus.t option
        [@ocaml.doc
          "The current status of player gateway for the game session. Note, even if a fleet has PlayerGatewayMode configured as ENABLED, player gateway might not be available in a specific location. For more information about locations where player gateway is supported, see supported locations. Possible values include: ENABLED -- Player gateway is available for this game session. DISABLED -- Player gateway is not available for this game session."]}
    let make ?gameSessionArn =
      fun ?ipAddress ->
        fun ?dnsName ->
          fun ?port ->
            fun ?matchedPlayerSessions ->
              fun ?playerGatewayStatus ->
                fun () ->
                  {
                    gameSessionArn;
                    ipAddress;
                    dnsName;
                    port;
                    matchedPlayerSessions;
                    playerGatewayStatus
                  }
    let to_value x =
      structure_to_value
        [("GameSessionArn",
           (Option.map x.gameSessionArn ~f:ArnStringModel.to_value));
        ("IpAddress", (Option.map x.ipAddress ~f:IpAddress.to_value));
        ("DnsName", (Option.map x.dnsName ~f:DnsName.to_value));
        ("Port", (Option.map x.port ~f:PositiveInteger.to_value));
        ("MatchedPlayerSessions",
          (Option.map x.matchedPlayerSessions
             ~f:MatchedPlayerSessionList.to_value));
        ("PlayerGatewayStatus",
          (Option.map x.playerGatewayStatus ~f:PlayerGatewayStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerGatewayStatus =
        (Option.map ~f:PlayerGatewayStatus.of_xml)
          (Xml.child xml_arg0 "PlayerGatewayStatus") in
      let matchedPlayerSessions =
        (Option.map ~f:MatchedPlayerSessionList.of_xml)
          (Xml.child xml_arg0 "MatchedPlayerSessions") in
      let port =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Port") in
      let dnsName =
        (Option.map ~f:DnsName.of_xml) (Xml.child xml_arg0 "DnsName") in
      let ipAddress =
        (Option.map ~f:IpAddress.of_xml) (Xml.child xml_arg0 "IpAddress") in
      let gameSessionArn =
        (Option.map ~f:ArnStringModel.of_xml)
          (Xml.child xml_arg0 "GameSessionArn") in
      make ?playerGatewayStatus ?matchedPlayerSessions ?port ?dnsName
        ?ipAddress ?gameSessionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerGatewayStatus =
        field_map json__ "PlayerGatewayStatus" PlayerGatewayStatus.of_json in
      let matchedPlayerSessions =
        field_map json__ "MatchedPlayerSessions"
          MatchedPlayerSessionList.of_json in
      let port = field_map json__ "Port" PositiveInteger.of_json in
      let dnsName = field_map json__ "DnsName" DnsName.of_json in
      let ipAddress = field_map json__ "IpAddress" IpAddress.of_json in
      let gameSessionArn =
        field_map json__ "GameSessionArn" ArnStringModel.of_json in
      make ?playerGatewayStatus ?matchedPlayerSessions ?port ?dnsName
        ?ipAddress ?gameSessionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Connection information for a new game session that is created in response to a start matchmaking request. Once a match is made, the FlexMatch engine creates a new game session for it. This information, including the game session endpoint and player sessions for each player in the original matchmaking request, is added to the matchmaking ticket."]
module MatchmakingConfigurationArn =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Data type used for Matchmaking Configuration ARN."]
    let context_ = "MatchmakingConfigurationArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"^arn:.*:matchmakingconfiguration\\/[a-zA-Z0-9-\\.]*");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MatchmakingConfigurationArn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Data type used for Matchmaking Configuration ARN."]
module MatchmakingConfigurationStatus =
  struct
    type nonrec t =
      | CANCELLED 
      | COMPLETED 
      | FAILED 
      | PLACING 
      | QUEUED 
      | REQUIRES_ACCEPTANCE 
      | SEARCHING 
      | TIMED_OUT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CANCELLED -> "CANCELLED"
      | COMPLETED -> "COMPLETED"
      | FAILED -> "FAILED"
      | PLACING -> "PLACING"
      | QUEUED -> "QUEUED"
      | REQUIRES_ACCEPTANCE -> "REQUIRES_ACCEPTANCE"
      | SEARCHING -> "SEARCHING"
      | TIMED_OUT -> "TIMED_OUT"
      | Non_static_id s -> s
    let of_string =
      function
      | "CANCELLED" -> CANCELLED
      | "COMPLETED" -> COMPLETED
      | "FAILED" -> FAILED
      | "PLACING" -> PLACING
      | "QUEUED" -> QUEUED
      | "REQUIRES_ACCEPTANCE" -> REQUIRES_ACCEPTANCE
      | "SEARCHING" -> SEARCHING
      | "TIMED_OUT" -> TIMED_OUT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration MatchmakingConfigurationStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"MatchmakingConfigurationStatus" j)
    let to_json = simple_to_json to_value
  end
module PlayerList =
  struct
    type nonrec t = Player.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Player.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Player.of_xml)
    let of_json j = list_of_json ~kind:"PlayerList" ~of_json:Player.of_json j
    let to_json v = composed_to_json to_value v
  end
module StringModel =
  struct
    type nonrec t = string
    let context_ = "StringModel"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StringModel" j
    let to_json = simple_to_json to_value
  end
module BackfillMode =
  struct
    type nonrec t =
      | AUTOMATIC 
      | MANUAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AUTOMATIC -> "AUTOMATIC"
      | MANUAL -> "MANUAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "AUTOMATIC" -> AUTOMATIC
      | "MANUAL" -> MANUAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration BackfillMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BackfillMode" j)
    let to_json = simple_to_json to_value
  end
module CustomEventData =
  struct
    type nonrec t = string
    let context_ = "CustomEventData"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CustomEventData" j
    let to_json = simple_to_json to_value
  end
module FlexMatchMode =
  struct
    type nonrec t =
      | STANDALONE 
      | WITH_QUEUE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | STANDALONE -> "STANDALONE"
      | WITH_QUEUE -> "WITH_QUEUE"
      | Non_static_id s -> s
    let of_string =
      function
      | "STANDALONE" -> STANDALONE
      | "WITH_QUEUE" -> WITH_QUEUE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FlexMatchMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FlexMatchMode" j)
    let to_json = simple_to_json to_value
  end
module GameSessionData =
  struct
    type nonrec t = string
    let context_ = "GameSessionData"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:4096) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GameSessionData" j
    let to_json = simple_to_json to_value
  end
module MatchmakingAcceptanceTimeoutInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:600) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for MatchmakingAcceptanceTimeoutInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MatchmakingRequestTimeoutInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:43200) >>=
             (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for MatchmakingRequestTimeoutInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module QueueArnsList =
  struct
    type nonrec t = ArnStringModel.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ArnStringModel.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ArnStringModel.of_xml)
    let of_json j =
      list_of_json ~kind:"QueueArnsList" ~of_json:ArnStringModel.of_json j
    let to_json v = composed_to_json to_value v
  end
module SnsArnStringModel =
  struct
    type nonrec t = string
    let context_ = "SnsArnStringModel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:300) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9:_/-]*(.fifo)?")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SnsArnStringModel" j
    let to_json = simple_to_json to_value
  end
module InstanceStatus =
  struct
    type nonrec t =
      | PENDING 
      | ACTIVE 
      | TERMINATING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | ACTIVE -> "ACTIVE"
      | TERMINATING -> "TERMINATING"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "ACTIVE" -> ACTIVE
      | "TERMINATING" -> TERMINATING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration InstanceStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InstanceStatus" j)
    let to_json = simple_to_json to_value
  end
module FilterConfiguration =
  struct
    type nonrec t =
      {
      allowedLocations: LocationList.t option
        [@ocaml.doc
          "A list of locations to allow game session placement in, in the form of Amazon Web Services Region codes such as us-west-2."]}
    let make ?allowedLocations = fun () -> { allowedLocations }
    let to_value x =
      structure_to_value
        [("AllowedLocations",
           (Option.map x.allowedLocations ~f:LocationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let allowedLocations =
        (Option.map ~f:LocationList.of_xml)
          (Xml.child xml_arg0 "AllowedLocations") in
      make ?allowedLocations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let allowedLocations =
        field_map json__ "AllowedLocations" LocationList.of_json in
      make ?allowedLocations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of fleet locations where a game session queue can place new game sessions. You can use a filter to temporarily exclude specific locations from receiving placements. For queues that have multi-location fleets, you can use a filter configuration allow placement with some, but not all, of a fleet's locations."]
module GameSessionQueueArn =
  struct
    type nonrec t = string
    let context_ = "GameSessionQueueArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:.*:gamesessionqueue\\/[a-zA-Z0-9-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GameSessionQueueArn" j
    let to_json = simple_to_json to_value
  end
module GameSessionQueueDestinationList =
  struct
    type nonrec t = GameSessionQueueDestination.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GameSessionQueueDestination.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GameSessionQueueDestination.of_xml)
    let of_json j =
      list_of_json ~kind:"GameSessionQueueDestinationList"
        ~of_json:GameSessionQueueDestination.of_json j
    let to_json v = composed_to_json to_value v
  end
module GameSessionQueueName =
  struct
    type nonrec t = string
    let context_ = "GameSessionQueueName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GameSessionQueueName" j
    let to_json = simple_to_json to_value
  end
module PlayerLatencyPolicyList =
  struct
    type nonrec t = PlayerLatencyPolicy.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PlayerLatencyPolicy.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlayerLatencyPolicy.of_xml)
    let of_json j =
      list_of_json ~kind:"PlayerLatencyPolicyList"
        ~of_json:PlayerLatencyPolicy.of_json j
    let to_json v = composed_to_json to_value v
  end
module PriorityConfiguration =
  struct
    type nonrec t =
      {
      priorityOrder: PriorityTypeList.t option
        [@ocaml.doc
          "A custom sequence to use when prioritizing where to place new game sessions. Each priority type is listed once. LATENCY -- Amazon GameLift Servers prioritizes locations where the average player latency is lowest. Player latency data is provided in each game session placement request. COST -- Amazon GameLift Servers prioritizes queue destinations with the lowest current hosting costs. Cost is evaluated based on the destination's location, instance type, and fleet type (Spot or On-Demand). DESTINATION -- Amazon GameLift Servers prioritizes based on the list order of destinations in the queue configuration. LOCATION -- Amazon GameLift Servers prioritizes based on the provided order of locations, as defined in LocationOrder."];
      locationOrder: LocationList.t option
        [@ocaml.doc
          "The prioritization order to use for fleet locations, when the PriorityOrder property includes LOCATION. Locations can include Amazon Web Services Region codes (such as us-west-2), local zones, and custom locations (for Anywhere fleets). Each location must be listed only once. For details, see Amazon GameLift Servers service locations."]}
    let make ?priorityOrder =
      fun ?locationOrder -> fun () -> { priorityOrder; locationOrder }
    let to_value x =
      structure_to_value
        [("PriorityOrder",
           (Option.map x.priorityOrder ~f:PriorityTypeList.to_value));
        ("LocationOrder",
          (Option.map x.locationOrder ~f:LocationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let locationOrder =
        (Option.map ~f:LocationList.of_xml)
          (Xml.child xml_arg0 "LocationOrder") in
      let priorityOrder =
        (Option.map ~f:PriorityTypeList.of_xml)
          (Xml.child xml_arg0 "PriorityOrder") in
      make ?locationOrder ?priorityOrder ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let locationOrder =
        field_map json__ "LocationOrder" LocationList.of_json in
      let priorityOrder =
        field_map json__ "PriorityOrder" PriorityTypeList.of_json in
      make ?locationOrder ?priorityOrder ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Custom prioritization settings to use with a game session queue. Prioritization settings determine how the queue selects a game hosting resource to start a new game session. This configuration replaces the default prioritization process for queues. By default, a queue makes game session placements based on the following criteria: When a game session request does not include player latency data, Amazon GameLift Servers places game sessions based on the following priorities: (1) the queue's default destination order, and (2) for multi-location fleets, an alphabetic list of locations. When a game session request includes player latency data, Amazon GameLift Servers re-orders the queue's destinations to make placements where the average player latency is lowest. It reorders based the following priorities: (1) the lowest average latency across all players, (2) the lowest hosting cost, (3) the queue's default destination order, and (4) for multi-location fleets, an alphabetic list of locations."]
module QueueCustomEventData =
  struct
    type nonrec t = string
    let context_ = "QueueCustomEventData"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[\\s\\S]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"QueueCustomEventData" j
    let to_json = simple_to_json to_value
  end
module QueueSnsArnStringModel =
  struct
    type nonrec t = string
    let context_ = "QueueSnsArnStringModel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:300) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9:_-]*(\\.fifo)?")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"QueueSnsArnStringModel" j
    let to_json = simple_to_json to_value
  end
module GameSession =
  struct
    type nonrec t =
      {
      gameSessionId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>."];
      name: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a game session. Session names do not need to be unique."];
      fleetId: FleetId.t option
        [@ocaml.doc
          "A unique identifier for the fleet that the game session is running on."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the GameLift fleet that this game session is running on."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      terminationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was terminated. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      currentPlayerSessionCount: WholeNumber.t option
        [@ocaml.doc "Number of players currently in the game session."];
      maximumPlayerSessionCount: WholeNumber.t option
        [@ocaml.doc
          "The maximum number of players that can be connected simultaneously to the game session."];
      status: GameSessionStatus.t option
        [@ocaml.doc
          "Current status of the game session. A game session must have an ACTIVE status to have player sessions."];
      statusReason: GameSessionStatusReason.t option
        [@ocaml.doc
          "Provides additional information about game session status. INTERRUPTED -- The game session was hosted on an EC2 Spot instance that was reclaimed, causing the active game session to be stopped. TRIGGERED_ON_PROCESS_TERMINATE \226\128\147 The game session was stopped by calling TerminateGameSession with the termination mode TRIGGER_ON_PROCESS_TERMINATE. FORCE_TERMINATED \226\128\147 The game session was stopped by calling TerminateGameSession with the termination mode FORCE_TERMINATE."];
      gameProperties: GamePropertyList.t option
        [@ocaml.doc
          "A set of key-value pairs that can store custom data in a game session. For example: \\{\"Key\": \"difficulty\", \"Value\": \"novice\"\\}. Avoid using periods (\".\") in property keys if you plan to search for game sessions by properties. Property keys containing periods cannot be searched and will be filtered out from search results due to search index limitations. If you use SearchGameSessions API, there is a limit of 500 game property keys across all game sessions and all fleets per region. If the limit is exceeded, there will potentially be game session entries missing from SearchGameSessions API results."];
      ipAddress: IpAddress.t option
        [@ocaml.doc
          "The IP address of the game session. To connect to a Amazon GameLift Servers game server, an app needs both the IP address and port number."];
      dnsName: DnsName.t option
        [@ocaml.doc
          "The DNS identifier assigned to the instance that is running the game session. Values have the following format: TLS-enabled fleets: <unique identifier>.<region identifier>.amazongamelift.com. Non-TLS-enabled fleets: ec2-<unique identifier>.compute.amazonaws.com. (See Amazon EC2 Instance IP Addressing.) When connecting to a game session that is running on a TLS-enabled fleet, you must use the DNS name, not the IP address."];
      port: PortNumber.t option
        [@ocaml.doc
          "The port number for the game session. To connect to a Amazon GameLift Servers game server, an app needs both the IP address and port number."];
      playerSessionCreationPolicy: PlayerSessionCreationPolicy.t option
        [@ocaml.doc
          "Indicates whether the game session is accepting new players."];
      creatorId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A unique identifier for a player. This ID is used to enforce a resource protection policy (if one exists), that limits the number of game sessions a player can create."];
      gameSessionData: LargeGameSessionData.t option
        [@ocaml.doc
          "A set of custom game session properties, formatted as a single string value. This data is passed to a game server process with a request to start a new game session. For more information, see Start a game session."];
      matchmakerData: MatchmakerData.t option
        [@ocaml.doc
          "Information about the matchmaking process that resulted in the game session, if matchmaking was used. Data is in JSON syntax, formatted as a string. Information includes the matchmaker ID as well as player attributes and team assignments. For more details on matchmaker data, see Match Data. Matchmaker data is updated whenever new players are added during a successful backfill (see StartMatchBackfill)."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The fleet location where the game session is running. This value might specify the fleet's home Region or a remote location. Location is expressed as an Amazon Web Services Region code such as us-west-2."];
      computeName: ComputeName.t option
        [@ocaml.doc
          "A descriptive label for the compute resource. The compute resource that is hosting the game session. For EC2 fleets, this is the EC2 instance ID. For Container fleets, each game server container group on a fleet instance is assigned a compute name. For Anywhere fleets, this is the custom compute name."];
      playerGatewayStatus: PlayerGatewayStatus.t option
        [@ocaml.doc
          "Indicates whether player gateway is available for use for this game session. Note, even if a fleet has PlayerGatewayMode configured as ENABLED, player gateway might not be available in a specific location. For more information about locations where player gateway is supported, see Amazon GameLift Servers service locations. Possible values include: ENABLED -- Player gateway is available for routing player connections for this game session. DISABLED -- Player gateway is not available for this game session."]}
    let make ?gameSessionId =
      fun ?name ->
        fun ?fleetId ->
          fun ?fleetArn ->
            fun ?creationTime ->
              fun ?terminationTime ->
                fun ?currentPlayerSessionCount ->
                  fun ?maximumPlayerSessionCount ->
                    fun ?status ->
                      fun ?statusReason ->
                        fun ?gameProperties ->
                          fun ?ipAddress ->
                            fun ?dnsName ->
                              fun ?port ->
                                fun ?playerSessionCreationPolicy ->
                                  fun ?creatorId ->
                                    fun ?gameSessionData ->
                                      fun ?matchmakerData ->
                                        fun ?location ->
                                          fun ?computeName ->
                                            fun ?playerGatewayStatus ->
                                              fun () ->
                                                {
                                                  gameSessionId;
                                                  name;
                                                  fleetId;
                                                  fleetArn;
                                                  creationTime;
                                                  terminationTime;
                                                  currentPlayerSessionCount;
                                                  maximumPlayerSessionCount;
                                                  status;
                                                  statusReason;
                                                  gameProperties;
                                                  ipAddress;
                                                  dnsName;
                                                  port;
                                                  playerSessionCreationPolicy;
                                                  creatorId;
                                                  gameSessionData;
                                                  matchmakerData;
                                                  location;
                                                  computeName;
                                                  playerGatewayStatus
                                                }
    let to_value x =
      structure_to_value
        [("GameSessionId",
           (Option.map x.gameSessionId ~f:NonZeroAndMaxString.to_value));
        ("Name", (Option.map x.name ~f:NonZeroAndMaxString.to_value));
        ("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("TerminationTime",
          (Option.map x.terminationTime ~f:Timestamp.to_value));
        ("CurrentPlayerSessionCount",
          (Option.map x.currentPlayerSessionCount ~f:WholeNumber.to_value));
        ("MaximumPlayerSessionCount",
          (Option.map x.maximumPlayerSessionCount ~f:WholeNumber.to_value));
        ("Status", (Option.map x.status ~f:GameSessionStatus.to_value));
        ("StatusReason",
          (Option.map x.statusReason ~f:GameSessionStatusReason.to_value));
        ("GameProperties",
          (Option.map x.gameProperties ~f:GamePropertyList.to_value));
        ("IpAddress", (Option.map x.ipAddress ~f:IpAddress.to_value));
        ("DnsName", (Option.map x.dnsName ~f:DnsName.to_value));
        ("Port", (Option.map x.port ~f:PortNumber.to_value));
        ("PlayerSessionCreationPolicy",
          (Option.map x.playerSessionCreationPolicy
             ~f:PlayerSessionCreationPolicy.to_value));
        ("CreatorId",
          (Option.map x.creatorId ~f:NonZeroAndMaxString.to_value));
        ("GameSessionData",
          (Option.map x.gameSessionData ~f:LargeGameSessionData.to_value));
        ("MatchmakerData",
          (Option.map x.matchmakerData ~f:MatchmakerData.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value));
        ("ComputeName", (Option.map x.computeName ~f:ComputeName.to_value));
        ("PlayerGatewayStatus",
          (Option.map x.playerGatewayStatus ~f:PlayerGatewayStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerGatewayStatus =
        (Option.map ~f:PlayerGatewayStatus.of_xml)
          (Xml.child xml_arg0 "PlayerGatewayStatus") in
      let computeName =
        (Option.map ~f:ComputeName.of_xml) (Xml.child xml_arg0 "ComputeName") in
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let matchmakerData =
        (Option.map ~f:MatchmakerData.of_xml)
          (Xml.child xml_arg0 "MatchmakerData") in
      let gameSessionData =
        (Option.map ~f:LargeGameSessionData.of_xml)
          (Xml.child xml_arg0 "GameSessionData") in
      let creatorId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "CreatorId") in
      let playerSessionCreationPolicy =
        (Option.map ~f:PlayerSessionCreationPolicy.of_xml)
          (Xml.child xml_arg0 "PlayerSessionCreationPolicy") in
      let port =
        (Option.map ~f:PortNumber.of_xml) (Xml.child xml_arg0 "Port") in
      let dnsName =
        (Option.map ~f:DnsName.of_xml) (Xml.child xml_arg0 "DnsName") in
      let ipAddress =
        (Option.map ~f:IpAddress.of_xml) (Xml.child xml_arg0 "IpAddress") in
      let gameProperties =
        (Option.map ~f:GamePropertyList.of_xml)
          (Xml.child xml_arg0 "GameProperties") in
      let statusReason =
        (Option.map ~f:GameSessionStatusReason.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:GameSessionStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let maximumPlayerSessionCount =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "MaximumPlayerSessionCount") in
      let currentPlayerSessionCount =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "CurrentPlayerSessionCount") in
      let terminationTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "TerminationTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      let name =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Name") in
      let gameSessionId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "GameSessionId") in
      make ?playerGatewayStatus ?computeName ?location ?matchmakerData
        ?gameSessionData ?creatorId ?playerSessionCreationPolicy ?port
        ?dnsName ?ipAddress ?gameProperties ?statusReason ?status
        ?maximumPlayerSessionCount ?currentPlayerSessionCount
        ?terminationTime ?creationTime ?fleetArn ?fleetId ?name
        ?gameSessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerGatewayStatus =
        field_map json__ "PlayerGatewayStatus" PlayerGatewayStatus.of_json in
      let computeName = field_map json__ "ComputeName" ComputeName.of_json in
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let matchmakerData =
        field_map json__ "MatchmakerData" MatchmakerData.of_json in
      let gameSessionData =
        field_map json__ "GameSessionData" LargeGameSessionData.of_json in
      let creatorId =
        field_map json__ "CreatorId" NonZeroAndMaxString.of_json in
      let playerSessionCreationPolicy =
        field_map json__ "PlayerSessionCreationPolicy"
          PlayerSessionCreationPolicy.of_json in
      let port = field_map json__ "Port" PortNumber.of_json in
      let dnsName = field_map json__ "DnsName" DnsName.of_json in
      let ipAddress = field_map json__ "IpAddress" IpAddress.of_json in
      let gameProperties =
        field_map json__ "GameProperties" GamePropertyList.of_json in
      let statusReason =
        field_map json__ "StatusReason" GameSessionStatusReason.of_json in
      let status = field_map json__ "Status" GameSessionStatus.of_json in
      let maximumPlayerSessionCount =
        field_map json__ "MaximumPlayerSessionCount" WholeNumber.of_json in
      let currentPlayerSessionCount =
        field_map json__ "CurrentPlayerSessionCount" WholeNumber.of_json in
      let terminationTime =
        field_map json__ "TerminationTime" Timestamp.of_json in
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      let name = field_map json__ "Name" NonZeroAndMaxString.of_json in
      let gameSessionId =
        field_map json__ "GameSessionId" NonZeroAndMaxString.of_json in
      make ?playerGatewayStatus ?computeName ?location ?matchmakerData
        ?gameSessionData ?creatorId ?playerSessionCreationPolicy ?port
        ?dnsName ?ipAddress ?gameProperties ?statusReason ?status
        ?maximumPlayerSessionCount ?currentPlayerSessionCount
        ?terminationTime ?creationTime ?fleetArn ?fleetId ?name
        ?gameSessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Properties describing a game session. A game session in ACTIVE status can host players. When a game session ends, its status is set to TERMINATED. Amazon GameLift Servers retains a game session resource for 30 days after the game session ends. You can reuse idempotency token values after this time. Game session logs are retained for 14 days. All APIs by task"]
module GameServerInstanceStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | DRAINING 
      | SPOT_TERMINATING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | DRAINING -> "DRAINING"
      | SPOT_TERMINATING -> "SPOT_TERMINATING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "DRAINING" -> DRAINING
      | "SPOT_TERMINATING" -> SPOT_TERMINATING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GameServerInstanceStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GameServerInstanceStatus" j)
    let to_json = simple_to_json to_value
  end
module FleetActionList =
  struct
    type nonrec t = FleetAction.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FleetAction.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FleetAction.of_xml)
    let of_json j =
      list_of_json ~kind:"FleetActionList" ~of_json:FleetAction.of_json j
    let to_json v = composed_to_json to_value v
  end
module LocationState =
  struct
    type nonrec t =
      {
      location: LocationStringModel.t option
        [@ocaml.doc
          "The fleet location, expressed as an Amazon Web Services Region code such as us-west-2."];
      status: FleetStatus.t option
        [@ocaml.doc "The life-cycle status of a fleet location."];
      playerGatewayStatus: PlayerGatewayStatus.t option
        [@ocaml.doc
          "The current status of player gateway in this location for this fleet. Note, even if a fleet has PlayerGatewayMode configured as ENABLED, player gateway might not be available in a specific location. For more information about locations where player gateway is supported, see Amazon GameLift Servers service locations. Possible values include: ENABLED -- Player gateway is available for this fleet location. DISABLED -- Player gateway is not available for this fleet location."]}
    let make ?location =
      fun ?status ->
        fun ?playerGatewayStatus ->
          fun () -> { location; status; playerGatewayStatus }
    let to_value x =
      structure_to_value
        [("Location",
           (Option.map x.location ~f:LocationStringModel.to_value));
        ("Status", (Option.map x.status ~f:FleetStatus.to_value));
        ("PlayerGatewayStatus",
          (Option.map x.playerGatewayStatus ~f:PlayerGatewayStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerGatewayStatus =
        (Option.map ~f:PlayerGatewayStatus.of_xml)
          (Xml.child xml_arg0 "PlayerGatewayStatus") in
      let status =
        (Option.map ~f:FleetStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      make ?playerGatewayStatus ?status ?location ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerGatewayStatus =
        field_map json__ "PlayerGatewayStatus" PlayerGatewayStatus.of_json in
      let status = field_map json__ "Status" FleetStatus.of_json in
      let location = field_map json__ "Location" LocationStringModel.of_json in
      make ?playerGatewayStatus ?status ?location ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A fleet location and its life-cycle state. A location state object might be used to describe a fleet's remote location or home Region. Life-cycle state tracks the progress of launching the first instance in a new location and preparing it for game hosting, and then removing all instances and deleting the location from the fleet. NEW -- A new fleet location has been defined and desired instances is set to 1. DOWNLOADING/VALIDATING/BUILDING/ACTIVATING -- Amazon GameLift Servers is setting up the new fleet location, creating new instances with the game build or Realtime script and starting server processes. ACTIVE -- Hosts can now accept game sessions. ERROR -- An error occurred when downloading, validating, building, or activating the fleet location. DELETING -- Hosts are responding to a delete fleet location request. TERMINATED -- The fleet location no longer exists. NOT_FOUND -- The fleet location was not found. This could be because the custom location was removed or not created."]
module EventCode =
  struct
    type nonrec t =
      | GENERIC_EVENT 
      | FLEET_CREATED 
      | FLEET_DELETED 
      | FLEET_SCALING_EVENT 
      | FLEET_STATE_DOWNLOADING 
      | FLEET_STATE_VALIDATING 
      | FLEET_STATE_BUILDING 
      | FLEET_STATE_ACTIVATING 
      | FLEET_STATE_ACTIVE 
      | FLEET_STATE_ERROR 
      | FLEET_STATE_PENDING 
      | FLEET_STATE_CREATING 
      | FLEET_STATE_CREATED 
      | FLEET_STATE_UPDATING 
      | FLEET_INITIALIZATION_FAILED 
      | FLEET_BINARY_DOWNLOAD_FAILED 
      | FLEET_VALIDATION_LAUNCH_PATH_NOT_FOUND 
      | FLEET_VALIDATION_EXECUTABLE_RUNTIME_FAILURE 
      | FLEET_VALIDATION_TIMED_OUT 
      | FLEET_ACTIVATION_FAILED 
      | FLEET_ACTIVATION_FAILED_NO_INSTANCES 
      | FLEET_NEW_GAME_SESSION_PROTECTION_POLICY_UPDATED 
      | SERVER_PROCESS_INVALID_PATH 
      | SERVER_PROCESS_SDK_INITIALIZATION_TIMEOUT 
      | SERVER_PROCESS_PROCESS_READY_TIMEOUT 
      | SERVER_PROCESS_CRASHED 
      | SERVER_PROCESS_TERMINATED_UNHEALTHY 
      | SERVER_PROCESS_FORCE_TERMINATED 
      | SERVER_PROCESS_PROCESS_EXIT_TIMEOUT 
      | SERVER_PROCESS_SDK_INITIALIZATION_FAILED 
      | SERVER_PROCESS_MISCONFIGURED_CONTAINER_PORT 
      | GAME_SESSION_ACTIVATION_TIMEOUT 
      | FLEET_CREATION_EXTRACTING_BUILD 
      | FLEET_CREATION_RUNNING_INSTALLER 
      | FLEET_CREATION_VALIDATING_RUNTIME_CONFIG 
      | FLEET_VPC_PEERING_SUCCEEDED 
      | FLEET_VPC_PEERING_FAILED 
      | FLEET_VPC_PEERING_DELETED 
      | INSTANCE_INTERRUPTED 
      | INSTANCE_RECYCLED 
      | INSTANCE_REPLACED_UNHEALTHY 
      | FLEET_CREATION_COMPLETED_INSTALLER 
      | FLEET_CREATION_FAILED_INSTALLER 
      | COMPUTE_LOG_UPLOAD_FAILED 
      | GAME_SERVER_CONTAINER_GROUP_CRASHED 
      | PER_INSTANCE_CONTAINER_GROUP_CRASHED 
      | GAME_SERVER_CONTAINER_GROUP_REPLACED_UNHEALTHY 
      | LOCATION_STATE_PENDING 
      | LOCATION_STATE_CREATING 
      | LOCATION_STATE_CREATED 
      | LOCATION_STATE_ACTIVATING 
      | LOCATION_STATE_ACTIVE 
      | LOCATION_STATE_UPDATING 
      | LOCATION_STATE_ERROR 
      | LOCATION_STATE_DELETING 
      | LOCATION_STATE_DELETED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GENERIC_EVENT -> "GENERIC_EVENT"
      | FLEET_CREATED -> "FLEET_CREATED"
      | FLEET_DELETED -> "FLEET_DELETED"
      | FLEET_SCALING_EVENT -> "FLEET_SCALING_EVENT"
      | FLEET_STATE_DOWNLOADING -> "FLEET_STATE_DOWNLOADING"
      | FLEET_STATE_VALIDATING -> "FLEET_STATE_VALIDATING"
      | FLEET_STATE_BUILDING -> "FLEET_STATE_BUILDING"
      | FLEET_STATE_ACTIVATING -> "FLEET_STATE_ACTIVATING"
      | FLEET_STATE_ACTIVE -> "FLEET_STATE_ACTIVE"
      | FLEET_STATE_ERROR -> "FLEET_STATE_ERROR"
      | FLEET_STATE_PENDING -> "FLEET_STATE_PENDING"
      | FLEET_STATE_CREATING -> "FLEET_STATE_CREATING"
      | FLEET_STATE_CREATED -> "FLEET_STATE_CREATED"
      | FLEET_STATE_UPDATING -> "FLEET_STATE_UPDATING"
      | FLEET_INITIALIZATION_FAILED -> "FLEET_INITIALIZATION_FAILED"
      | FLEET_BINARY_DOWNLOAD_FAILED -> "FLEET_BINARY_DOWNLOAD_FAILED"
      | FLEET_VALIDATION_LAUNCH_PATH_NOT_FOUND ->
          "FLEET_VALIDATION_LAUNCH_PATH_NOT_FOUND"
      | FLEET_VALIDATION_EXECUTABLE_RUNTIME_FAILURE ->
          "FLEET_VALIDATION_EXECUTABLE_RUNTIME_FAILURE"
      | FLEET_VALIDATION_TIMED_OUT -> "FLEET_VALIDATION_TIMED_OUT"
      | FLEET_ACTIVATION_FAILED -> "FLEET_ACTIVATION_FAILED"
      | FLEET_ACTIVATION_FAILED_NO_INSTANCES ->
          "FLEET_ACTIVATION_FAILED_NO_INSTANCES"
      | FLEET_NEW_GAME_SESSION_PROTECTION_POLICY_UPDATED ->
          "FLEET_NEW_GAME_SESSION_PROTECTION_POLICY_UPDATED"
      | SERVER_PROCESS_INVALID_PATH -> "SERVER_PROCESS_INVALID_PATH"
      | SERVER_PROCESS_SDK_INITIALIZATION_TIMEOUT ->
          "SERVER_PROCESS_SDK_INITIALIZATION_TIMEOUT"
      | SERVER_PROCESS_PROCESS_READY_TIMEOUT ->
          "SERVER_PROCESS_PROCESS_READY_TIMEOUT"
      | SERVER_PROCESS_CRASHED -> "SERVER_PROCESS_CRASHED"
      | SERVER_PROCESS_TERMINATED_UNHEALTHY ->
          "SERVER_PROCESS_TERMINATED_UNHEALTHY"
      | SERVER_PROCESS_FORCE_TERMINATED -> "SERVER_PROCESS_FORCE_TERMINATED"
      | SERVER_PROCESS_PROCESS_EXIT_TIMEOUT ->
          "SERVER_PROCESS_PROCESS_EXIT_TIMEOUT"
      | SERVER_PROCESS_SDK_INITIALIZATION_FAILED ->
          "SERVER_PROCESS_SDK_INITIALIZATION_FAILED"
      | SERVER_PROCESS_MISCONFIGURED_CONTAINER_PORT ->
          "SERVER_PROCESS_MISCONFIGURED_CONTAINER_PORT"
      | GAME_SESSION_ACTIVATION_TIMEOUT -> "GAME_SESSION_ACTIVATION_TIMEOUT"
      | FLEET_CREATION_EXTRACTING_BUILD -> "FLEET_CREATION_EXTRACTING_BUILD"
      | FLEET_CREATION_RUNNING_INSTALLER ->
          "FLEET_CREATION_RUNNING_INSTALLER"
      | FLEET_CREATION_VALIDATING_RUNTIME_CONFIG ->
          "FLEET_CREATION_VALIDATING_RUNTIME_CONFIG"
      | FLEET_VPC_PEERING_SUCCEEDED -> "FLEET_VPC_PEERING_SUCCEEDED"
      | FLEET_VPC_PEERING_FAILED -> "FLEET_VPC_PEERING_FAILED"
      | FLEET_VPC_PEERING_DELETED -> "FLEET_VPC_PEERING_DELETED"
      | INSTANCE_INTERRUPTED -> "INSTANCE_INTERRUPTED"
      | INSTANCE_RECYCLED -> "INSTANCE_RECYCLED"
      | INSTANCE_REPLACED_UNHEALTHY -> "INSTANCE_REPLACED_UNHEALTHY"
      | FLEET_CREATION_COMPLETED_INSTALLER ->
          "FLEET_CREATION_COMPLETED_INSTALLER"
      | FLEET_CREATION_FAILED_INSTALLER -> "FLEET_CREATION_FAILED_INSTALLER"
      | COMPUTE_LOG_UPLOAD_FAILED -> "COMPUTE_LOG_UPLOAD_FAILED"
      | GAME_SERVER_CONTAINER_GROUP_CRASHED ->
          "GAME_SERVER_CONTAINER_GROUP_CRASHED"
      | PER_INSTANCE_CONTAINER_GROUP_CRASHED ->
          "PER_INSTANCE_CONTAINER_GROUP_CRASHED"
      | GAME_SERVER_CONTAINER_GROUP_REPLACED_UNHEALTHY ->
          "GAME_SERVER_CONTAINER_GROUP_REPLACED_UNHEALTHY"
      | LOCATION_STATE_PENDING -> "LOCATION_STATE_PENDING"
      | LOCATION_STATE_CREATING -> "LOCATION_STATE_CREATING"
      | LOCATION_STATE_CREATED -> "LOCATION_STATE_CREATED"
      | LOCATION_STATE_ACTIVATING -> "LOCATION_STATE_ACTIVATING"
      | LOCATION_STATE_ACTIVE -> "LOCATION_STATE_ACTIVE"
      | LOCATION_STATE_UPDATING -> "LOCATION_STATE_UPDATING"
      | LOCATION_STATE_ERROR -> "LOCATION_STATE_ERROR"
      | LOCATION_STATE_DELETING -> "LOCATION_STATE_DELETING"
      | LOCATION_STATE_DELETED -> "LOCATION_STATE_DELETED"
      | Non_static_id s -> s
    let of_string =
      function
      | "GENERIC_EVENT" -> GENERIC_EVENT
      | "FLEET_CREATED" -> FLEET_CREATED
      | "FLEET_DELETED" -> FLEET_DELETED
      | "FLEET_SCALING_EVENT" -> FLEET_SCALING_EVENT
      | "FLEET_STATE_DOWNLOADING" -> FLEET_STATE_DOWNLOADING
      | "FLEET_STATE_VALIDATING" -> FLEET_STATE_VALIDATING
      | "FLEET_STATE_BUILDING" -> FLEET_STATE_BUILDING
      | "FLEET_STATE_ACTIVATING" -> FLEET_STATE_ACTIVATING
      | "FLEET_STATE_ACTIVE" -> FLEET_STATE_ACTIVE
      | "FLEET_STATE_ERROR" -> FLEET_STATE_ERROR
      | "FLEET_STATE_PENDING" -> FLEET_STATE_PENDING
      | "FLEET_STATE_CREATING" -> FLEET_STATE_CREATING
      | "FLEET_STATE_CREATED" -> FLEET_STATE_CREATED
      | "FLEET_STATE_UPDATING" -> FLEET_STATE_UPDATING
      | "FLEET_INITIALIZATION_FAILED" -> FLEET_INITIALIZATION_FAILED
      | "FLEET_BINARY_DOWNLOAD_FAILED" -> FLEET_BINARY_DOWNLOAD_FAILED
      | "FLEET_VALIDATION_LAUNCH_PATH_NOT_FOUND" ->
          FLEET_VALIDATION_LAUNCH_PATH_NOT_FOUND
      | "FLEET_VALIDATION_EXECUTABLE_RUNTIME_FAILURE" ->
          FLEET_VALIDATION_EXECUTABLE_RUNTIME_FAILURE
      | "FLEET_VALIDATION_TIMED_OUT" -> FLEET_VALIDATION_TIMED_OUT
      | "FLEET_ACTIVATION_FAILED" -> FLEET_ACTIVATION_FAILED
      | "FLEET_ACTIVATION_FAILED_NO_INSTANCES" ->
          FLEET_ACTIVATION_FAILED_NO_INSTANCES
      | "FLEET_NEW_GAME_SESSION_PROTECTION_POLICY_UPDATED" ->
          FLEET_NEW_GAME_SESSION_PROTECTION_POLICY_UPDATED
      | "SERVER_PROCESS_INVALID_PATH" -> SERVER_PROCESS_INVALID_PATH
      | "SERVER_PROCESS_SDK_INITIALIZATION_TIMEOUT" ->
          SERVER_PROCESS_SDK_INITIALIZATION_TIMEOUT
      | "SERVER_PROCESS_PROCESS_READY_TIMEOUT" ->
          SERVER_PROCESS_PROCESS_READY_TIMEOUT
      | "SERVER_PROCESS_CRASHED" -> SERVER_PROCESS_CRASHED
      | "SERVER_PROCESS_TERMINATED_UNHEALTHY" ->
          SERVER_PROCESS_TERMINATED_UNHEALTHY
      | "SERVER_PROCESS_FORCE_TERMINATED" -> SERVER_PROCESS_FORCE_TERMINATED
      | "SERVER_PROCESS_PROCESS_EXIT_TIMEOUT" ->
          SERVER_PROCESS_PROCESS_EXIT_TIMEOUT
      | "SERVER_PROCESS_SDK_INITIALIZATION_FAILED" ->
          SERVER_PROCESS_SDK_INITIALIZATION_FAILED
      | "SERVER_PROCESS_MISCONFIGURED_CONTAINER_PORT" ->
          SERVER_PROCESS_MISCONFIGURED_CONTAINER_PORT
      | "GAME_SESSION_ACTIVATION_TIMEOUT" -> GAME_SESSION_ACTIVATION_TIMEOUT
      | "FLEET_CREATION_EXTRACTING_BUILD" -> FLEET_CREATION_EXTRACTING_BUILD
      | "FLEET_CREATION_RUNNING_INSTALLER" ->
          FLEET_CREATION_RUNNING_INSTALLER
      | "FLEET_CREATION_VALIDATING_RUNTIME_CONFIG" ->
          FLEET_CREATION_VALIDATING_RUNTIME_CONFIG
      | "FLEET_VPC_PEERING_SUCCEEDED" -> FLEET_VPC_PEERING_SUCCEEDED
      | "FLEET_VPC_PEERING_FAILED" -> FLEET_VPC_PEERING_FAILED
      | "FLEET_VPC_PEERING_DELETED" -> FLEET_VPC_PEERING_DELETED
      | "INSTANCE_INTERRUPTED" -> INSTANCE_INTERRUPTED
      | "INSTANCE_RECYCLED" -> INSTANCE_RECYCLED
      | "INSTANCE_REPLACED_UNHEALTHY" -> INSTANCE_REPLACED_UNHEALTHY
      | "FLEET_CREATION_COMPLETED_INSTALLER" ->
          FLEET_CREATION_COMPLETED_INSTALLER
      | "FLEET_CREATION_FAILED_INSTALLER" -> FLEET_CREATION_FAILED_INSTALLER
      | "COMPUTE_LOG_UPLOAD_FAILED" -> COMPUTE_LOG_UPLOAD_FAILED
      | "GAME_SERVER_CONTAINER_GROUP_CRASHED" ->
          GAME_SERVER_CONTAINER_GROUP_CRASHED
      | "PER_INSTANCE_CONTAINER_GROUP_CRASHED" ->
          PER_INSTANCE_CONTAINER_GROUP_CRASHED
      | "GAME_SERVER_CONTAINER_GROUP_REPLACED_UNHEALTHY" ->
          GAME_SERVER_CONTAINER_GROUP_REPLACED_UNHEALTHY
      | "LOCATION_STATE_PENDING" -> LOCATION_STATE_PENDING
      | "LOCATION_STATE_CREATING" -> LOCATION_STATE_CREATING
      | "LOCATION_STATE_CREATED" -> LOCATION_STATE_CREATED
      | "LOCATION_STATE_ACTIVATING" -> LOCATION_STATE_ACTIVATING
      | "LOCATION_STATE_ACTIVE" -> LOCATION_STATE_ACTIVE
      | "LOCATION_STATE_UPDATING" -> LOCATION_STATE_UPDATING
      | "LOCATION_STATE_ERROR" -> LOCATION_STATE_ERROR
      | "LOCATION_STATE_DELETING" -> LOCATION_STATE_DELETING
      | "LOCATION_STATE_DELETED" -> LOCATION_STATE_DELETED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EventCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EventCode" j)
    let to_json = simple_to_json to_value
  end
module EventCount =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int64_max i ~max:101L) >>=
             (fun () -> check_int64_min i ~min:1L));
        i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module EC2InstanceCounts =
  struct
    type nonrec t =
      {
      dESIRED: WholeNumber.t option
        [@ocaml.doc
          "Requested number of active instances. Amazon GameLift Servers takes action as needed to maintain the desired number of instances. Capacity is scaled up or down by changing the desired instances. A change in the desired instances value can take up to 1 minute to be reflected when viewing a fleet's capacity settings."];
      mINIMUM: WholeNumber.t option
        [@ocaml.doc "The minimum instance count value allowed."];
      mAXIMUM: WholeNumber.t option
        [@ocaml.doc "The maximum instance count value allowed."];
      pENDING: WholeNumber.t option
        [@ocaml.doc
          "Number of instances that are starting but not yet active."];
      aCTIVE: WholeNumber.t option
        [@ocaml.doc
          "Actual number of instances that are ready to host game sessions."];
      iDLE: WholeNumber.t option
        [@ocaml.doc
          "Number of active instances that are not currently hosting a game session."];
      tERMINATING: WholeNumber.t option
        [@ocaml.doc
          "Number of instances that are no longer active but haven't yet been terminated."]}
    let make ?dESIRED =
      fun ?mINIMUM ->
        fun ?mAXIMUM ->
          fun ?pENDING ->
            fun ?aCTIVE ->
              fun ?iDLE ->
                fun ?tERMINATING ->
                  fun () ->
                    {
                      dESIRED;
                      mINIMUM;
                      mAXIMUM;
                      pENDING;
                      aCTIVE;
                      iDLE;
                      tERMINATING
                    }
    let to_value x =
      structure_to_value
        [("DESIRED", (Option.map x.dESIRED ~f:WholeNumber.to_value));
        ("MINIMUM", (Option.map x.mINIMUM ~f:WholeNumber.to_value));
        ("MAXIMUM", (Option.map x.mAXIMUM ~f:WholeNumber.to_value));
        ("PENDING", (Option.map x.pENDING ~f:WholeNumber.to_value));
        ("ACTIVE", (Option.map x.aCTIVE ~f:WholeNumber.to_value));
        ("IDLE", (Option.map x.iDLE ~f:WholeNumber.to_value));
        ("TERMINATING", (Option.map x.tERMINATING ~f:WholeNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tERMINATING =
        (Option.map ~f:WholeNumber.of_xml) (Xml.child xml_arg0 "TERMINATING") in
      let iDLE =
        (Option.map ~f:WholeNumber.of_xml) (Xml.child xml_arg0 "IDLE") in
      let aCTIVE =
        (Option.map ~f:WholeNumber.of_xml) (Xml.child xml_arg0 "ACTIVE") in
      let pENDING =
        (Option.map ~f:WholeNumber.of_xml) (Xml.child xml_arg0 "PENDING") in
      let mAXIMUM =
        (Option.map ~f:WholeNumber.of_xml) (Xml.child xml_arg0 "MAXIMUM") in
      let mINIMUM =
        (Option.map ~f:WholeNumber.of_xml) (Xml.child xml_arg0 "MINIMUM") in
      let dESIRED =
        (Option.map ~f:WholeNumber.of_xml) (Xml.child xml_arg0 "DESIRED") in
      make ?tERMINATING ?iDLE ?aCTIVE ?pENDING ?mAXIMUM ?mINIMUM ?dESIRED ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tERMINATING = field_map json__ "TERMINATING" WholeNumber.of_json in
      let iDLE = field_map json__ "IDLE" WholeNumber.of_json in
      let aCTIVE = field_map json__ "ACTIVE" WholeNumber.of_json in
      let pENDING = field_map json__ "PENDING" WholeNumber.of_json in
      let mAXIMUM = field_map json__ "MAXIMUM" WholeNumber.of_json in
      let mINIMUM = field_map json__ "MINIMUM" WholeNumber.of_json in
      let dESIRED = field_map json__ "DESIRED" WholeNumber.of_json in
      make ?tERMINATING ?iDLE ?aCTIVE ?pENDING ?mAXIMUM ?mINIMUM ?dESIRED ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resource capacity settings. Fleet capacity is measured in Amazon EC2 instances. Pending and terminating counts are non-zero when the fleet capacity is adjusting to a scaling event or if access to resources is temporarily affected."]
module GameServerContainerGroupCounts =
  struct
    type nonrec t =
      {
      pENDING: WholeNumber.t option
        [@ocaml.doc
          "The number of container groups that are starting up but haven't yet registered."];
      aCTIVE: WholeNumber.t option
        [@ocaml.doc
          "The number of container groups that have active game sessions."];
      iDLE: WholeNumber.t option
        [@ocaml.doc
          "The number of container groups that have no active game sessions."];
      tERMINATING: WholeNumber.t option
        [@ocaml.doc
          "The number of container groups that are in the process of shutting down."]}
    let make ?pENDING =
      fun ?aCTIVE ->
        fun ?iDLE ->
          fun ?tERMINATING ->
            fun () -> { pENDING; aCTIVE; iDLE; tERMINATING }
    let to_value x =
      structure_to_value
        [("PENDING", (Option.map x.pENDING ~f:WholeNumber.to_value));
        ("ACTIVE", (Option.map x.aCTIVE ~f:WholeNumber.to_value));
        ("IDLE", (Option.map x.iDLE ~f:WholeNumber.to_value));
        ("TERMINATING", (Option.map x.tERMINATING ~f:WholeNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tERMINATING =
        (Option.map ~f:WholeNumber.of_xml) (Xml.child xml_arg0 "TERMINATING") in
      let iDLE =
        (Option.map ~f:WholeNumber.of_xml) (Xml.child xml_arg0 "IDLE") in
      let aCTIVE =
        (Option.map ~f:WholeNumber.of_xml) (Xml.child xml_arg0 "ACTIVE") in
      let pENDING =
        (Option.map ~f:WholeNumber.of_xml) (Xml.child xml_arg0 "PENDING") in
      make ?tERMINATING ?iDLE ?aCTIVE ?pENDING ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tERMINATING = field_map json__ "TERMINATING" WholeNumber.of_json in
      let iDLE = field_map json__ "IDLE" WholeNumber.of_json in
      let aCTIVE = field_map json__ "ACTIVE" WholeNumber.of_json in
      let pENDING = field_map json__ "PENDING" WholeNumber.of_json in
      make ?tERMINATING ?iDLE ?aCTIVE ?pENDING ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The number and status of game server container groups that are deployed across a container fleet. Combine this count with the number of server processes that each game server container group runs to learn how many game sessions the fleet is capable of hosting concurrently. For example, if a fleet has 50 game server container groups, and the game server container in each group runs 1 game server process, then the fleet has the capacity to run host 50 game sessions at a time. Returned by: https://docs.aws.amazon.com/gamelift/latest/apireference/API_DescribeFleetCapacity.html, https://docs.aws.amazon.com/gamelift/latest/apireference/API_DescribeFleetLocationCapacity.html"]
module ManagedCapacityConfiguration =
  struct
    type nonrec t =
      {
      zeroCapacityStrategy: ZeroCapacityStrategy.t option
        [@ocaml.doc
          "The strategy Amazon GameLift Servers will use to automatically scale your capacity to and from zero instances in response to game session activity. Game session activity refers to any active running sessions or game session requests. Possible ZeroCapacityStrategy types include: MANUAL -- (default value) Amazon GameLift Servers will not update capacity to and from zero on your behalf. SCALE_TO_AND_FROM_ZERO -- Amazon GameLift Servers will automatically scale out MinSize and DesiredInstances from 0 to 1 in response to a game session request, and will scale in MinSize and DesiredInstances to 0 after a period with no game session activity. The duration of this scale in period can be configured using ScaleInAfterInactivityMinutes."];
      scaleInAfterInactivityMinutes: ScaleInAfterInactivityMinutes.t option
        [@ocaml.doc
          "Length of time, in minutes, that Amazon GameLift Servers will wait before scaling in your MinSize and DesiredInstances to 0 after a period with no game session activity. Default: 30 minutes."]}
    let make ?zeroCapacityStrategy =
      fun ?scaleInAfterInactivityMinutes ->
        fun () -> { zeroCapacityStrategy; scaleInAfterInactivityMinutes }
    let to_value x =
      structure_to_value
        [("ZeroCapacityStrategy",
           (Option.map x.zeroCapacityStrategy
              ~f:ZeroCapacityStrategy.to_value));
        ("ScaleInAfterInactivityMinutes",
          (Option.map x.scaleInAfterInactivityMinutes
             ~f:ScaleInAfterInactivityMinutes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scaleInAfterInactivityMinutes =
        (Option.map ~f:ScaleInAfterInactivityMinutes.of_xml)
          (Xml.child xml_arg0 "ScaleInAfterInactivityMinutes") in
      let zeroCapacityStrategy =
        (Option.map ~f:ZeroCapacityStrategy.of_xml)
          (Xml.child xml_arg0 "ZeroCapacityStrategy") in
      make ?scaleInAfterInactivityMinutes ?zeroCapacityStrategy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scaleInAfterInactivityMinutes =
        field_map json__ "ScaleInAfterInactivityMinutes"
          ScaleInAfterInactivityMinutes.of_json in
      let zeroCapacityStrategy =
        field_map json__ "ZeroCapacityStrategy" ZeroCapacityStrategy.of_json in
      make ?scaleInAfterInactivityMinutes ?zeroCapacityStrategy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Use ManagedCapacityConfiguration with the \"SCALE_TO_AND_FROM_ZERO\" ZeroCapacityStrategy to enable Amazon GameLift Servers to fully manage the MinSize value, switching between 0 and 1 based on game session activity. This is ideal for eliminating compute costs during periods of no game activity. It is particularly beneficial during development when you're away from your desk, iterating on builds for extended periods, in production environments serving low-traffic locations, or for games with long, predictable downtime windows. By automatically managing capacity between 0 and 1 instances, you avoid paying for idle instances while maintaining the ability to serve game sessions when demand arrives. Note that while scale-out is triggered immediately upon receiving a game session request, actual game session availability depends on your server process startup time, so this approach works best with multi-location Fleets where cold-start latency is tolerable. With a \"MANUAL\" ZeroCapacityStrategy Amazon GameLift Servers will not modify Fleet MinSize values automatically and will not scale out from zero instances in response to game sessions."]
module AnywhereConfiguration =
  struct
    type nonrec t =
      {
      cost: NonNegativeLimitedLengthDouble.t
        [@ocaml.doc
          "The cost to run your fleet per hour. Amazon GameLift Servers uses the provided cost of your fleet to balance usage in queues. For more information about queues, see Setting up queues in the Amazon GameLift Servers Developer Guide."]}
    let context_ = "AnywhereConfiguration"
    let make ~cost = fun () -> { cost }
    let to_value x =
      structure_to_value
        [("Cost", (Some (NonNegativeLimitedLengthDouble.to_value x.cost)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cost =
        NonNegativeLimitedLengthDouble.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Cost") in
      make ~cost ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cost =
        field_map_exn json__ "Cost" NonNegativeLimitedLengthDouble.of_json in
      make ~cost ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Amazon GameLift Servers configuration options for your Anywhere fleets."]
module CertificateConfiguration =
  struct
    type nonrec t =
      {
      certificateType: CertificateType.t
        [@ocaml.doc
          "Indicates whether a TLS/SSL certificate is generated for a fleet. Valid values include: GENERATED - Generate a TLS/SSL certificate for this fleet. DISABLED - (default) Do not generate a TLS/SSL certificate for this fleet."]}
    let context_ = "CertificateConfiguration"
    let make ~certificateType = fun () -> { certificateType }
    let to_value x =
      structure_to_value
        [("CertificateType",
           (Some (CertificateType.to_value x.certificateType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let certificateType =
        CertificateType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CertificateType") in
      make ~certificateType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let certificateType =
        field_map_exn json__ "CertificateType" CertificateType.of_json in
      make ~certificateType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Determines whether a TLS/SSL certificate is generated for a fleet. This feature must be enabled when creating the fleet. All instances in a fleet share the same certificate. The certificate can be retrieved by calling the Amazon GameLift Servers Server SDK operation GetInstanceCertificate."]
module ComputeType =
  struct
    type nonrec t =
      | EC2 
      | ANYWHERE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | EC2 -> "EC2" | ANYWHERE -> "ANYWHERE" | Non_static_id s -> s
    let of_string =
      function | "EC2" -> EC2 | "ANYWHERE" -> ANYWHERE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ComputeType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ComputeType" j)
    let to_json = simple_to_json to_value
  end
module FleetType =
  struct
    type nonrec t =
      | ON_DEMAND 
      | SPOT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ON_DEMAND -> "ON_DEMAND"
      | SPOT -> "SPOT"
      | Non_static_id s -> s
    let of_string =
      function
      | "ON_DEMAND" -> ON_DEMAND
      | "SPOT" -> SPOT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FleetType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FleetType" j)
    let to_json = simple_to_json to_value
  end
module InstanceRoleCredentialsProvider =
  struct
    type nonrec t =
      | SHARED_CREDENTIAL_FILE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SHARED_CREDENTIAL_FILE -> "SHARED_CREDENTIAL_FILE"
      | Non_static_id s -> s
    let of_string =
      function
      | "SHARED_CREDENTIAL_FILE" -> SHARED_CREDENTIAL_FILE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceRoleCredentialsProvider"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"InstanceRoleCredentialsProvider" j)
    let to_json = simple_to_json to_value
  end
module PlayerGatewayConfiguration =
  struct
    type nonrec t =
      {
      gameServerIpProtocolSupported: GameServerIpProtocolSupported.t option
        [@ocaml.doc
          "The IP protocol that your game servers support for player connections through player gateway. If the value is set to IPv4, GameLift will install and execute a lightweight IP translation software on fleet instances to receive and transform incoming IPv6 traffic to IPv4. If the value is set to DUAL_STACK, the lightweight IP translation software will not be installed on fleet instances. DUAL_STACK provides slightly better performance than IPv4."]}
    let make ?gameServerIpProtocolSupported =
      fun () -> { gameServerIpProtocolSupported }
    let to_value x =
      structure_to_value
        [("GameServerIpProtocolSupported",
           (Option.map x.gameServerIpProtocolSupported
              ~f:GameServerIpProtocolSupported.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServerIpProtocolSupported =
        (Option.map ~f:GameServerIpProtocolSupported.of_xml)
          (Xml.child xml_arg0 "GameServerIpProtocolSupported") in
      make ?gameServerIpProtocolSupported ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServerIpProtocolSupported =
        field_map json__ "GameServerIpProtocolSupported"
          GameServerIpProtocolSupported.of_json in
      make ?gameServerIpProtocolSupported ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration settings for player gateway. Use these settings to specify advanced options for how player gateway handles connections."]
module ResourceCreationLimitPolicy =
  struct
    type nonrec t =
      {
      newGameSessionsPerCreator: WholeNumber.t option
        [@ocaml.doc
          "A policy that puts limits on the number of game sessions that a player can create within a specified span of time. With this policy, you can control players' ability to consume available resources. The policy is evaluated when a player tries to create a new game session. On receiving a CreateGameSession request, Amazon GameLift Servers checks that the player (identified by CreatorId) has created fewer than game session limit in the specified time period."];
      policyPeriodInMinutes: WholeNumber.t option
        [@ocaml.doc
          "The time span used in evaluating the resource creation limit policy."]}
    let make ?newGameSessionsPerCreator =
      fun ?policyPeriodInMinutes ->
        fun () -> { newGameSessionsPerCreator; policyPeriodInMinutes }
    let to_value x =
      structure_to_value
        [("NewGameSessionsPerCreator",
           (Option.map x.newGameSessionsPerCreator ~f:WholeNumber.to_value));
        ("PolicyPeriodInMinutes",
          (Option.map x.policyPeriodInMinutes ~f:WholeNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyPeriodInMinutes =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "PolicyPeriodInMinutes") in
      let newGameSessionsPerCreator =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "NewGameSessionsPerCreator") in
      make ?policyPeriodInMinutes ?newGameSessionsPerCreator ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyPeriodInMinutes =
        field_map json__ "PolicyPeriodInMinutes" WholeNumber.of_json in
      let newGameSessionsPerCreator =
        field_map json__ "NewGameSessionsPerCreator" WholeNumber.of_json in
      make ?policyPeriodInMinutes ?newGameSessionsPerCreator ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A policy that puts limits on the number of game sessions that a player can create within a specified span of time. With this policy, you can control players' ability to consume available resources. The policy is evaluated when a player tries to create a new game session. On receiving a CreateGameSession request, Amazon GameLift Servers checks that the player (identified by CreatorId) has created fewer than game session limit in the specified time period."]
module StringList =
  struct
    type nonrec t = NonZeroAndMaxString.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonZeroAndMaxString.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonZeroAndMaxString.of_xml)
    let of_json j =
      list_of_json ~kind:"StringList" ~of_json:NonZeroAndMaxString.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerPortMappingList =
  struct
    type nonrec t = ContainerPortMapping.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContainerPortMapping.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerPortMapping.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerPortMappingList"
        ~of_json:ContainerPortMapping.of_json j
    let to_json v = composed_to_json to_value v
  end
module NonNegativeDouble =
  struct
    type nonrec t = float
    let make i =
      let open Result in ok_or_failwith (check_float_min i ~min:0.); i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module FilterInstanceStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | DRAINING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | DRAINING -> "DRAINING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "DRAINING" -> DRAINING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FilterInstanceStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FilterInstanceStatus" j)
    let to_json = simple_to_json to_value
  end
module GameSessionActivationTimeoutSeconds =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:600) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for GameSessionActivationTimeoutSeconds"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MaxConcurrentGameSessionActivations =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:2147483647) >>=
             (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for MaxConcurrentGameSessionActivations"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ServerProcessList =
  struct
    type nonrec t = ServerProcess.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ServerProcess.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ServerProcess.of_xml)
    let of_json j =
      list_of_json ~kind:"ServerProcessList" ~of_json:ServerProcess.of_json j
    let to_json v = composed_to_json to_value v
  end
module SupportContainerDefinitionInput =
  struct
    type nonrec t =
      {
      containerName: NonZeroAnd128MaxAsciiString.t
        [@ocaml.doc
          "A string that uniquely identifies the container definition within a container group."];
      dependsOn: ContainerDependencyList.t option
        [@ocaml.doc
          "Establishes dependencies between this container and the status of other containers in the same container group. A container can have dependencies on multiple different containers. . You can use dependencies to establish a startup/shutdown sequence across the container group. For example, you might specify that ContainerB has a START dependency on ContainerA. This dependency means that ContainerB can't start until after ContainerA has started. This dependency is reversed on shutdown, which means that ContainerB must shut down before ContainerA can shut down."];
      mountPoints: ContainerMountPointList.t option
        [@ocaml.doc
          "A mount point that binds a path inside the container to a file or directory on the host system and lets it access the file or directory."];
      environmentOverride: ContainerEnvironmentList.t option
        [@ocaml.doc
          "A set of environment variables to pass to the container on startup. See the ContainerDefinition::environment parameter in the Amazon Elastic Container Service API Reference."];
      essential: BooleanModel.t option
        [@ocaml.doc
          "Flags the container as vital for the container group to function properly. If an essential container fails, the entire container group restarts. At least one support container in a per-instance container group must be essential. When flagging a container as essential, also configure a health check so that the container can signal that it's healthy."];
      healthCheck: ContainerHealthCheck.t option
        [@ocaml.doc
          "Configuration for a non-terminal health check. A container automatically restarts if it stops functioning. With a health check, you can define additional reasons to flag a container as unhealthy and restart it. If an essential container fails a health check, the entire container group restarts."];
      imageUri: ImageUriString.t
        [@ocaml.doc
          "The location of the container image to deploy to a container fleet. Provide an image in an Amazon Elastic Container Registry public or private repository. The repository must be in the same Amazon Web Services account and Amazon Web Services Region where you're creating the container group definition. For limits on image size, see Amazon GameLift Servers endpoints and quotas. You can use any of the following image URI formats: Image ID only: \\[AWS account\\].dkr.ecr.\\[AWS region\\].amazonaws.com/\\[repository ID\\] Image ID and digest: \\[AWS account\\].dkr.ecr.\\[AWS region\\].amazonaws.com/\\[repository ID\\]\\@\\[digest\\] Image ID and tag: \\[AWS account\\].dkr.ecr.\\[AWS region\\].amazonaws.com/\\[repository ID\\]:\\[tag\\]"];
      memoryHardLimitMebibytes: ContainerMemoryLimit.t option
        [@ocaml.doc
          "A specified amount of memory (in MiB) to reserve for this container. If you don't specify a container-specific memory limit, the container shares the container group's total memory allocation. Related data type: ContainerGroupDefinitionTotalMemoryLimitMebibytes"];
      portConfiguration: ContainerPortConfiguration.t option
        [@ocaml.doc
          "A set of ports that Amazon GameLift Servers can assign to processes in a container. The container port configuration must have enough ports for each container process that accepts inbound traffic connections. A container port configuration can have can have one or more container port ranges. Each range specifies starting and ending values as well as the supported network protocol. Container ports aren't directly accessed by inbound traffic. Amazon GameLift Servers maps each container port to an externally accessible connection port (see the container fleet property ConnectionPortRange)."];
      vcpu: ContainerVcpu.t option
        [@ocaml.doc
          "The number of vCPU units to reserve for this container. The container can use more resources when needed, if available. If you don't reserve CPU units for this container, it shares the container group's total vCPU limit. Related data type: ContainerGroupDefinition TotalCpuLimit"]}
    let context_ = "SupportContainerDefinitionInput"
    let make ?dependsOn =
      fun ?mountPoints ->
        fun ?environmentOverride ->
          fun ?essential ->
            fun ?healthCheck ->
              fun ?memoryHardLimitMebibytes ->
                fun ?portConfiguration ->
                  fun ?vcpu ->
                    fun ~containerName ->
                      fun ~imageUri ->
                        fun () ->
                          {
                            dependsOn;
                            mountPoints;
                            environmentOverride;
                            essential;
                            healthCheck;
                            memoryHardLimitMebibytes;
                            portConfiguration;
                            vcpu;
                            containerName;
                            imageUri
                          }
    let to_value x =
      structure_to_value
        [("ContainerName",
           (Some (NonZeroAnd128MaxAsciiString.to_value x.containerName)));
        ("DependsOn",
          (Option.map x.dependsOn ~f:ContainerDependencyList.to_value));
        ("MountPoints",
          (Option.map x.mountPoints ~f:ContainerMountPointList.to_value));
        ("EnvironmentOverride",
          (Option.map x.environmentOverride
             ~f:ContainerEnvironmentList.to_value));
        ("Essential", (Option.map x.essential ~f:BooleanModel.to_value));
        ("HealthCheck",
          (Option.map x.healthCheck ~f:ContainerHealthCheck.to_value));
        ("ImageUri", (Some (ImageUriString.to_value x.imageUri)));
        ("MemoryHardLimitMebibytes",
          (Option.map x.memoryHardLimitMebibytes
             ~f:ContainerMemoryLimit.to_value));
        ("PortConfiguration",
          (Option.map x.portConfiguration
             ~f:ContainerPortConfiguration.to_value));
        ("Vcpu", (Option.map x.vcpu ~f:ContainerVcpu.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vcpu =
        (Option.map ~f:ContainerVcpu.of_xml) (Xml.child xml_arg0 "Vcpu") in
      let portConfiguration =
        (Option.map ~f:ContainerPortConfiguration.of_xml)
          (Xml.child xml_arg0 "PortConfiguration") in
      let memoryHardLimitMebibytes =
        (Option.map ~f:ContainerMemoryLimit.of_xml)
          (Xml.child xml_arg0 "MemoryHardLimitMebibytes") in
      let imageUri =
        ImageUriString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ImageUri") in
      let healthCheck =
        (Option.map ~f:ContainerHealthCheck.of_xml)
          (Xml.child xml_arg0 "HealthCheck") in
      let essential =
        (Option.map ~f:BooleanModel.of_xml) (Xml.child xml_arg0 "Essential") in
      let environmentOverride =
        (Option.map ~f:ContainerEnvironmentList.of_xml)
          (Xml.child xml_arg0 "EnvironmentOverride") in
      let mountPoints =
        (Option.map ~f:ContainerMountPointList.of_xml)
          (Xml.child xml_arg0 "MountPoints") in
      let dependsOn =
        (Option.map ~f:ContainerDependencyList.of_xml)
          (Xml.child xml_arg0 "DependsOn") in
      let containerName =
        NonZeroAnd128MaxAsciiString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ?vcpu ?portConfiguration ?memoryHardLimitMebibytes ~imageUri
        ?healthCheck ?essential ?environmentOverride ?mountPoints ?dependsOn
        ~containerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vcpu = field_map json__ "Vcpu" ContainerVcpu.of_json in
      let portConfiguration =
        field_map json__ "PortConfiguration"
          ContainerPortConfiguration.of_json in
      let memoryHardLimitMebibytes =
        field_map json__ "MemoryHardLimitMebibytes"
          ContainerMemoryLimit.of_json in
      let imageUri = field_map_exn json__ "ImageUri" ImageUriString.of_json in
      let healthCheck =
        field_map json__ "HealthCheck" ContainerHealthCheck.of_json in
      let essential = field_map json__ "Essential" BooleanModel.of_json in
      let environmentOverride =
        field_map json__ "EnvironmentOverride"
          ContainerEnvironmentList.of_json in
      let mountPoints =
        field_map json__ "MountPoints" ContainerMountPointList.of_json in
      let dependsOn =
        field_map json__ "DependsOn" ContainerDependencyList.of_json in
      let containerName =
        field_map_exn json__ "ContainerName"
          NonZeroAnd128MaxAsciiString.of_json in
      make ?vcpu ?portConfiguration ?memoryHardLimitMebibytes ~imageUri
        ?healthCheck ?essential ?environmentOverride ?mountPoints ?dependsOn
        ~containerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a support container in a container group. You can define a support container in either a game server container group or a per-instance container group. Support containers don't run game server processes. This definition includes container configuration, resources, and start instructions. Use this data type when creating or updating a container group definition. For properties of a deployed support container, see SupportContainerDefinition. Use with: CreateContainerGroupDefinition, UpdateContainerGroupDefinition"]
module ContainerFleetRemoveAttribute =
  struct
    type nonrec t =
      | PER_INSTANCE_CONTAINER_GROUP_DEFINITION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PER_INSTANCE_CONTAINER_GROUP_DEFINITION ->
          "PER_INSTANCE_CONTAINER_GROUP_DEFINITION"
      | Non_static_id s -> s
    let of_string =
      function
      | "PER_INSTANCE_CONTAINER_GROUP_DEFINITION" ->
          PER_INSTANCE_CONTAINER_GROUP_DEFINITION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ContainerFleetRemoveAttribute"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ContainerFleetRemoveAttribute" j)
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t =
      {
      key: TagKey.t
        [@ocaml.doc
          "The key for a developer-defined key value pair for tagging an Amazon Web Services resource."];
      value: TagValue.t
        [@ocaml.doc
          "The value for a developer-defined key value pair for tagging an Amazon Web Services resource."]}
    let context_ = "Tag"
    let make ~key = fun ~value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Some (TagKey.to_value x.key)));
        ("Value", (Some (TagValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        TagValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let key =
        TagKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Key") in
      make ~value ~key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map_exn json__ "Value" TagValue.of_json in
      let key = field_map_exn json__ "Key" TagKey.of_json in
      make ~value ~key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A label that you can assign to a Amazon GameLift Servers resource. Learn more Tagging Amazon Web Services Resources in the Amazon Web Services General Reference Amazon Web Services Tagging Strategies Related actions All APIs by task"]
module GameSessionPlacementState =
  struct
    type nonrec t =
      | PENDING 
      | FULFILLED 
      | CANCELLED 
      | TIMED_OUT 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | FULFILLED -> "FULFILLED"
      | CANCELLED -> "CANCELLED"
      | TIMED_OUT -> "TIMED_OUT"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "FULFILLED" -> FULFILLED
      | "CANCELLED" -> CANCELLED
      | "TIMED_OUT" -> TIMED_OUT
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GameSessionPlacementState" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GameSessionPlacementState" j)
    let to_json = simple_to_json to_value
  end
module IdStringModel =
  struct
    type nonrec t = string
    let context_ = "IdStringModel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:48) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IdStringModel" j
    let to_json = simple_to_json to_value
  end
module PlacedPlayerSessionList =
  struct
    type nonrec t = PlacedPlayerSession.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PlacedPlayerSession.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlacedPlayerSession.of_xml)
    let of_json j =
      list_of_json ~kind:"PlacedPlayerSessionList"
        ~of_json:PlacedPlayerSession.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlayerLatencyList =
  struct
    type nonrec t = PlayerLatency.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PlayerLatency.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlayerLatency.of_xml)
    let of_json j =
      list_of_json ~kind:"PlayerLatencyList" ~of_json:PlayerLatency.of_json j
    let to_json v = composed_to_json to_value v
  end
module PriorityConfigurationOverride =
  struct
    type nonrec t =
      {
      placementFallbackStrategy: PlacementFallbackStrategy.t option
        [@ocaml.doc
          "Instructions for how to proceed if placement fails in every location on the priority override list. Valid strategies include: DEFAULT_AFTER_SINGLE_PASS -- After attempting to place a new game session in every location on the priority override list, try to place a game session in queue's other locations. This is the default behavior. NONE -- Limit placements to locations on the priority override list only."];
      locationOrder: LocationOrderOverrideList.t
        [@ocaml.doc
          "A prioritized list of hosting locations. The list can include Amazon Web Services Regions (such as us-west-2), local zones, and custom locations (for Anywhere fleets). Each location must be listed only once. For details, see Amazon GameLift Servers service locations."]}
    let context_ = "PriorityConfigurationOverride"
    let make ?placementFallbackStrategy =
      fun ~locationOrder ->
        fun () -> { placementFallbackStrategy; locationOrder }
    let to_value x =
      structure_to_value
        [("PlacementFallbackStrategy",
           (Option.map x.placementFallbackStrategy
              ~f:PlacementFallbackStrategy.to_value));
        ("LocationOrder",
          (Some (LocationOrderOverrideList.to_value x.locationOrder)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let locationOrder =
        LocationOrderOverrideList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LocationOrder") in
      let placementFallbackStrategy =
        (Option.map ~f:PlacementFallbackStrategy.of_xml)
          (Xml.child xml_arg0 "PlacementFallbackStrategy") in
      make ~locationOrder ?placementFallbackStrategy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let locationOrder =
        field_map_exn json__ "LocationOrder"
          LocationOrderOverrideList.of_json in
      let placementFallbackStrategy =
        field_map json__ "PlacementFallbackStrategy"
          PlacementFallbackStrategy.of_json in
      make ~locationOrder ?placementFallbackStrategy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An alternate list of prioritized locations for use with a game session queue. When this property is included in a StartGameSessionPlacement request, the alternate list overrides the queue's default location priorities, as defined in the queue's PriorityConfiguration setting (LocationOrder). The override is valid for an individual placement request only. Use this property only with queues that have a PriorityConfiguration setting that prioritizes LOCATION first. A priority configuration override list does not override a queue's FilterConfiguration setting, if the queue has one. Filter configurations are used to limit placements to a subset of the locations in a queue's destinations. If the override list includes a location that's not on in the FilterConfiguration allowed list, Amazon GameLift Servers won't attempt to place a game session there."]
module DesiredPlayerSession =
  struct
    type nonrec t =
      {
      playerId: PlayerId.t option
        [@ocaml.doc
          "A unique identifier for a player to associate with the player session."];
      playerData: PlayerData.t option
        [@ocaml.doc
          "Developer-defined information related to a player. Amazon GameLift Servers does not use this data, so it can be formatted as needed for use in the game."]}
    let make ?playerId =
      fun ?playerData -> fun () -> { playerId; playerData }
    let to_value x =
      structure_to_value
        [("PlayerId", (Option.map x.playerId ~f:PlayerId.to_value));
        ("PlayerData", (Option.map x.playerData ~f:PlayerData.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerData =
        (Option.map ~f:PlayerData.of_xml) (Xml.child xml_arg0 "PlayerData") in
      let playerId =
        (Option.map ~f:PlayerId.of_xml) (Xml.child xml_arg0 "PlayerId") in
      make ?playerData ?playerId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerData = field_map json__ "PlayerData" PlayerData.of_json in
      let playerId = field_map json__ "PlayerId" PlayerId.of_json in
      make ?playerData ?playerId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Player information for use when creating player sessions using a game session placement request."]
module Script =
  struct
    type nonrec t =
      {
      scriptId: ScriptId.t option
        [@ocaml.doc "A unique identifier for the Realtime script"];
      scriptArn: ScriptArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers script resource and uniquely identifies it. ARNs are unique across all Regions. In a GameLift script ARN, the resource ID matches the ScriptId value."];
      name: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a script. Script names do not need to be unique."];
      version: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Version information that is associated with a build or script. Version strings do not need to be unique."];
      sizeOnDisk: WholeNumberLong.t option
        [@ocaml.doc
          "The file size of the uploaded Realtime script, expressed in bytes. When files are uploaded from an S3 location, this value remains at \"0\"."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      storageLocation: S3Location.t option
        [@ocaml.doc
          "The location of the Amazon S3 bucket where a zipped file containing your Realtime scripts is stored. The storage location must specify the Amazon S3 bucket name, the zip file name (the \"key\"), and a role ARN that allows Amazon GameLift Servers to access the Amazon S3 storage location. The S3 bucket must be in the same Region where you want to create a new script. By default, Amazon GameLift Servers uploads the latest version of the zip file; if you have S3 object versioning turned on, you can use the ObjectVersion parameter to specify an earlier version."];
      nodeJsVersion: NodeJsVersion.t option
        [@ocaml.doc
          "The Node.js version used for execution of your Realtime script. The valid values are 10.x | 24.x. By default, NodeJsVersion is 10.x. This value cannot be updated later. Node.js 10 will reach end of support on September 30, 2026. See more details in the Node.js 10 FAQs. For migration guidance, see Migrating from Node.js 10 to 24."]}
    let make ?scriptId =
      fun ?scriptArn ->
        fun ?name ->
          fun ?version ->
            fun ?sizeOnDisk ->
              fun ?creationTime ->
                fun ?storageLocation ->
                  fun ?nodeJsVersion ->
                    fun () ->
                      {
                        scriptId;
                        scriptArn;
                        name;
                        version;
                        sizeOnDisk;
                        creationTime;
                        storageLocation;
                        nodeJsVersion
                      }
    let to_value x =
      structure_to_value
        [("ScriptId", (Option.map x.scriptId ~f:ScriptId.to_value));
        ("ScriptArn", (Option.map x.scriptArn ~f:ScriptArn.to_value));
        ("Name", (Option.map x.name ~f:NonZeroAndMaxString.to_value));
        ("Version", (Option.map x.version ~f:NonZeroAndMaxString.to_value));
        ("SizeOnDisk", (Option.map x.sizeOnDisk ~f:WholeNumberLong.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("StorageLocation",
          (Option.map x.storageLocation ~f:S3Location.to_value));
        ("NodeJsVersion",
          (Option.map x.nodeJsVersion ~f:NodeJsVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nodeJsVersion =
        (Option.map ~f:NodeJsVersion.of_xml)
          (Xml.child xml_arg0 "NodeJsVersion") in
      let storageLocation =
        (Option.map ~f:S3Location.of_xml)
          (Xml.child xml_arg0 "StorageLocation") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let sizeOnDisk =
        (Option.map ~f:WholeNumberLong.of_xml)
          (Xml.child xml_arg0 "SizeOnDisk") in
      let version =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Version") in
      let name =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Name") in
      let scriptArn =
        (Option.map ~f:ScriptArn.of_xml) (Xml.child xml_arg0 "ScriptArn") in
      let scriptId =
        (Option.map ~f:ScriptId.of_xml) (Xml.child xml_arg0 "ScriptId") in
      make ?nodeJsVersion ?storageLocation ?creationTime ?sizeOnDisk ?version
        ?name ?scriptArn ?scriptId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nodeJsVersion =
        field_map json__ "NodeJsVersion" NodeJsVersion.of_json in
      let storageLocation =
        field_map json__ "StorageLocation" S3Location.of_json in
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let sizeOnDisk = field_map json__ "SizeOnDisk" WholeNumberLong.of_json in
      let version = field_map json__ "Version" NonZeroAndMaxString.of_json in
      let name = field_map json__ "Name" NonZeroAndMaxString.of_json in
      let scriptArn = field_map json__ "ScriptArn" ScriptArn.of_json in
      let scriptId = field_map json__ "ScriptId" ScriptId.of_json in
      make ?nodeJsVersion ?storageLocation ?creationTime ?sizeOnDisk ?version
        ?name ?scriptArn ?scriptId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Properties describing a Realtime script. Related actions All APIs by task"]
module LocationModel =
  struct
    type nonrec t =
      {
      locationName: LocationStringModel.t option
        [@ocaml.doc "The location's name."];
      locationArn: LocationArnModel.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a custom location resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::location/location-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      pingBeacon: PingBeacon.t option
        [@ocaml.doc
          "Information about the UDP ping beacon for this location. Ping beacons are fixed endpoints that you can use to measure network latency between a player device and an Amazon GameLift Servers hosting location."]}
    let make ?locationName =
      fun ?locationArn ->
        fun ?pingBeacon ->
          fun () -> { locationName; locationArn; pingBeacon }
    let to_value x =
      structure_to_value
        [("LocationName",
           (Option.map x.locationName ~f:LocationStringModel.to_value));
        ("LocationArn",
          (Option.map x.locationArn ~f:LocationArnModel.to_value));
        ("PingBeacon", (Option.map x.pingBeacon ~f:PingBeacon.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pingBeacon =
        (Option.map ~f:PingBeacon.of_xml) (Xml.child xml_arg0 "PingBeacon") in
      let locationArn =
        (Option.map ~f:LocationArnModel.of_xml)
          (Xml.child xml_arg0 "LocationArn") in
      let locationName =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "LocationName") in
      make ?pingBeacon ?locationArn ?locationName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pingBeacon = field_map json__ "PingBeacon" PingBeacon.of_json in
      let locationArn =
        field_map json__ "LocationArn" LocationArnModel.of_json in
      let locationName =
        field_map json__ "LocationName" LocationStringModel.of_json in
      make ?pingBeacon ?locationArn ?locationName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Properties of a location, which can include its name, ARN (for custom locations), and ping beacon information."]
module LocationFilter =
  struct
    type nonrec t =
      | AWS 
      | CUSTOM 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | AWS -> "AWS" | CUSTOM -> "CUSTOM" | Non_static_id s -> s
    let of_string =
      function | "AWS" -> AWS | "CUSTOM" -> CUSTOM | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LocationFilter" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LocationFilter" j)
    let to_json = simple_to_json to_value
  end
module GameServer =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupName.t option
        [@ocaml.doc
          "A unique identifier for the game server group where the game server is running."];
      gameServerGroupArn: GameServerGroupArn.t option
        [@ocaml.doc
          "The ARN identifier for the game server group where the game server is located."];
      gameServerId: GameServerId.t option
        [@ocaml.doc
          "A custom string that uniquely identifies the game server. Game server IDs are developer-defined and are unique across all game server groups in an Amazon Web Services account."];
      instanceId: GameServerInstanceId.t option
        [@ocaml.doc
          "The unique identifier for the instance where the game server is running. This ID is available in the instance metadata. EC2 instance IDs use a 17-character format, for example: i-1234567890abcdef0."];
      connectionInfo: GameServerConnectionInfo.t option
        [@ocaml.doc
          "The port and IP address that must be used to establish a client connection to the game server."];
      gameServerData: GameServerData.t option
        [@ocaml.doc
          "A set of custom game server properties, formatted as a single string value. This data is passed to a game client or service when it requests information on game servers."];
      claimStatus: GameServerClaimStatus.t option
        [@ocaml.doc
          "Indicates when an available game server has been reserved for gameplay but has not yet started hosting a game. Once it is claimed, the game server remains in CLAIMED status for a maximum of one minute. During this time, game clients connect to the game server to start the game and trigger the game server to update its utilization status. After one minute, the game server claim status reverts to null."];
      utilizationStatus: GameServerUtilizationStatus.t option
        [@ocaml.doc
          "Indicates whether the game server is currently available for new games or is busy. Possible statuses include: AVAILABLE - The game server is available to be claimed. A game server that has been claimed remains in this status until it reports game hosting activity. UTILIZED - The game server is currently hosting a game session with players."];
      registrationTime: Timestamp.t option
        [@ocaml.doc
          "Timestamp that indicates when the game server registered. The format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      lastClaimTime: Timestamp.t option
        [@ocaml.doc
          "Timestamp that indicates the last time the game server was claimed. The format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\"). This value is used to calculate when a claimed game server's status should revert to null."];
      lastHealthCheckTime: Timestamp.t option
        [@ocaml.doc
          "Timestamp that indicates the last time the game server was updated with health status. The format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\"). After game server registration, this property is only changed when a game server update specifies a health check value."]}
    let make ?gameServerGroupName =
      fun ?gameServerGroupArn ->
        fun ?gameServerId ->
          fun ?instanceId ->
            fun ?connectionInfo ->
              fun ?gameServerData ->
                fun ?claimStatus ->
                  fun ?utilizationStatus ->
                    fun ?registrationTime ->
                      fun ?lastClaimTime ->
                        fun ?lastHealthCheckTime ->
                          fun () ->
                            {
                              gameServerGroupName;
                              gameServerGroupArn;
                              gameServerId;
                              instanceId;
                              connectionInfo;
                              gameServerData;
                              claimStatus;
                              utilizationStatus;
                              registrationTime;
                              lastClaimTime;
                              lastHealthCheckTime
                            }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Option.map x.gameServerGroupName ~f:GameServerGroupName.to_value));
        ("GameServerGroupArn",
          (Option.map x.gameServerGroupArn ~f:GameServerGroupArn.to_value));
        ("GameServerId",
          (Option.map x.gameServerId ~f:GameServerId.to_value));
        ("InstanceId",
          (Option.map x.instanceId ~f:GameServerInstanceId.to_value));
        ("ConnectionInfo",
          (Option.map x.connectionInfo ~f:GameServerConnectionInfo.to_value));
        ("GameServerData",
          (Option.map x.gameServerData ~f:GameServerData.to_value));
        ("ClaimStatus",
          (Option.map x.claimStatus ~f:GameServerClaimStatus.to_value));
        ("UtilizationStatus",
          (Option.map x.utilizationStatus
             ~f:GameServerUtilizationStatus.to_value));
        ("RegistrationTime",
          (Option.map x.registrationTime ~f:Timestamp.to_value));
        ("LastClaimTime", (Option.map x.lastClaimTime ~f:Timestamp.to_value));
        ("LastHealthCheckTime",
          (Option.map x.lastHealthCheckTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastHealthCheckTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastHealthCheckTime") in
      let lastClaimTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "LastClaimTime") in
      let registrationTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "RegistrationTime") in
      let utilizationStatus =
        (Option.map ~f:GameServerUtilizationStatus.of_xml)
          (Xml.child xml_arg0 "UtilizationStatus") in
      let claimStatus =
        (Option.map ~f:GameServerClaimStatus.of_xml)
          (Xml.child xml_arg0 "ClaimStatus") in
      let gameServerData =
        (Option.map ~f:GameServerData.of_xml)
          (Xml.child xml_arg0 "GameServerData") in
      let connectionInfo =
        (Option.map ~f:GameServerConnectionInfo.of_xml)
          (Xml.child xml_arg0 "ConnectionInfo") in
      let instanceId =
        (Option.map ~f:GameServerInstanceId.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let gameServerId =
        (Option.map ~f:GameServerId.of_xml)
          (Xml.child xml_arg0 "GameServerId") in
      let gameServerGroupArn =
        (Option.map ~f:GameServerGroupArn.of_xml)
          (Xml.child xml_arg0 "GameServerGroupArn") in
      let gameServerGroupName =
        (Option.map ~f:GameServerGroupName.of_xml)
          (Xml.child xml_arg0 "GameServerGroupName") in
      make ?lastHealthCheckTime ?lastClaimTime ?registrationTime
        ?utilizationStatus ?claimStatus ?gameServerData ?connectionInfo
        ?instanceId ?gameServerId ?gameServerGroupArn ?gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastHealthCheckTime =
        field_map json__ "LastHealthCheckTime" Timestamp.of_json in
      let lastClaimTime = field_map json__ "LastClaimTime" Timestamp.of_json in
      let registrationTime =
        field_map json__ "RegistrationTime" Timestamp.of_json in
      let utilizationStatus =
        field_map json__ "UtilizationStatus"
          GameServerUtilizationStatus.of_json in
      let claimStatus =
        field_map json__ "ClaimStatus" GameServerClaimStatus.of_json in
      let gameServerData =
        field_map json__ "GameServerData" GameServerData.of_json in
      let connectionInfo =
        field_map json__ "ConnectionInfo" GameServerConnectionInfo.of_json in
      let instanceId =
        field_map json__ "InstanceId" GameServerInstanceId.of_json in
      let gameServerId = field_map json__ "GameServerId" GameServerId.of_json in
      let gameServerGroupArn =
        field_map json__ "GameServerGroupArn" GameServerGroupArn.of_json in
      let gameServerGroupName =
        field_map json__ "GameServerGroupName" GameServerGroupName.of_json in
      make ?lastHealthCheckTime ?lastClaimTime ?registrationTime
        ?utilizationStatus ?claimStatus ?gameServerData ?connectionInfo
        ?instanceId ?gameServerId ?gameServerGroupArn ?gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Properties describing a game server that is running on an instance in a game server group. A game server is created by a successful call to RegisterGameServer and deleted by calling DeregisterGameServer. A game server is claimed to host a game session by calling ClaimGameServer."]
module GameServerGroup =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupName.t option
        [@ocaml.doc
          "A developer-defined identifier for the game server group. The name is unique for each Region in each Amazon Web Services account."];
      gameServerGroupArn: GameServerGroupArn.t option
        [@ocaml.doc "A generated unique ID for the game server group."];
      roleArn: IamRoleArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) for an IAM role that allows Amazon GameLift Servers to access your Amazon EC2 Auto Scaling groups."];
      instanceDefinitions: InstanceDefinitions.t option
        [@ocaml.doc
          "The set of Amazon EC2 instance types that Amazon GameLift Servers FleetIQ can use when balancing and automatically scaling instances in the corresponding Auto Scaling group."];
      balancingStrategy: BalancingStrategy.t option
        [@ocaml.doc
          "Indicates how Amazon GameLift Servers FleetIQ balances the use of Spot Instances and On-Demand Instances in the game server group. Method options include the following: SPOT_ONLY - Only Spot Instances are used in the game server group. If Spot Instances are unavailable or not viable for game hosting, the game server group provides no hosting capacity until Spot Instances can again be used. Until then, no new instances are started, and the existing nonviable Spot Instances are terminated (after current gameplay ends) and are not replaced. SPOT_PREFERRED - (default value) Spot Instances are used whenever available in the game server group. If Spot Instances are unavailable, the game server group continues to provide hosting capacity by falling back to On-Demand Instances. Existing nonviable Spot Instances are terminated (after current gameplay ends) and are replaced with new On-Demand Instances. ON_DEMAND_ONLY - Only On-Demand Instances are used in the game server group. No Spot Instances are used, even when available, while this balancing strategy is in force."];
      gameServerProtectionPolicy: GameServerProtectionPolicy.t option
        [@ocaml.doc
          "A flag that indicates whether instances in the game server group are protected from early termination. Unprotected instances that have active game servers running might be terminated during a scale-down event, causing players to be dropped from the game. Protected instances cannot be terminated while there are active game servers running except in the event of a forced game server group deletion (see ). An exception to this is with Spot Instances, which can be terminated by Amazon Web Services regardless of protection status."];
      autoScalingGroupArn: AutoScalingGroupArn.t option
        [@ocaml.doc
          "A generated unique ID for the Amazon EC2 Auto Scaling group that is associated with this game server group."];
      status: GameServerGroupStatus.t option
        [@ocaml.doc
          "The current status of the game server group. Possible statuses include: NEW - Amazon GameLift Servers FleetIQ has validated the CreateGameServerGroup() request. ACTIVATING - Amazon GameLift Servers FleetIQ is setting up a game server group, which includes creating an Auto Scaling group in your Amazon Web Services account. ACTIVE - The game server group has been successfully created. DELETE_SCHEDULED - A request to delete the game server group has been received. DELETING - Amazon GameLift Servers FleetIQ has received a valid DeleteGameServerGroup() request and is processing it. Amazon GameLift Servers FleetIQ must first complete and release hosts before it deletes the Auto Scaling group and the game server group. DELETED - The game server group has been successfully deleted. ERROR - The asynchronous processes of activating or deleting a game server group has failed, resulting in an error state."];
      statusReason: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Additional information about the current game server group status. This information might provide additional insight on groups that are in ERROR status."];
      suspendedActions: GameServerGroupActions.t option
        [@ocaml.doc
          "A list of activities that are currently suspended for this game server group. If this property is empty, all activities are occurring."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      lastUpdatedTime: Timestamp.t option
        [@ocaml.doc
          "A timestamp that indicates when this game server group was last updated."]}
    let make ?gameServerGroupName =
      fun ?gameServerGroupArn ->
        fun ?roleArn ->
          fun ?instanceDefinitions ->
            fun ?balancingStrategy ->
              fun ?gameServerProtectionPolicy ->
                fun ?autoScalingGroupArn ->
                  fun ?status ->
                    fun ?statusReason ->
                      fun ?suspendedActions ->
                        fun ?creationTime ->
                          fun ?lastUpdatedTime ->
                            fun () ->
                              {
                                gameServerGroupName;
                                gameServerGroupArn;
                                roleArn;
                                instanceDefinitions;
                                balancingStrategy;
                                gameServerProtectionPolicy;
                                autoScalingGroupArn;
                                status;
                                statusReason;
                                suspendedActions;
                                creationTime;
                                lastUpdatedTime
                              }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Option.map x.gameServerGroupName ~f:GameServerGroupName.to_value));
        ("GameServerGroupArn",
          (Option.map x.gameServerGroupArn ~f:GameServerGroupArn.to_value));
        ("RoleArn", (Option.map x.roleArn ~f:IamRoleArn.to_value));
        ("InstanceDefinitions",
          (Option.map x.instanceDefinitions ~f:InstanceDefinitions.to_value));
        ("BalancingStrategy",
          (Option.map x.balancingStrategy ~f:BalancingStrategy.to_value));
        ("GameServerProtectionPolicy",
          (Option.map x.gameServerProtectionPolicy
             ~f:GameServerProtectionPolicy.to_value));
        ("AutoScalingGroupArn",
          (Option.map x.autoScalingGroupArn ~f:AutoScalingGroupArn.to_value));
        ("Status", (Option.map x.status ~f:GameServerGroupStatus.to_value));
        ("StatusReason",
          (Option.map x.statusReason ~f:NonZeroAndMaxString.to_value));
        ("SuspendedActions",
          (Option.map x.suspendedActions ~f:GameServerGroupActions.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("LastUpdatedTime",
          (Option.map x.lastUpdatedTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastUpdatedTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let suspendedActions =
        (Option.map ~f:GameServerGroupActions.of_xml)
          (Xml.child xml_arg0 "SuspendedActions") in
      let statusReason =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:GameServerGroupStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let autoScalingGroupArn =
        (Option.map ~f:AutoScalingGroupArn.of_xml)
          (Xml.child xml_arg0 "AutoScalingGroupArn") in
      let gameServerProtectionPolicy =
        (Option.map ~f:GameServerProtectionPolicy.of_xml)
          (Xml.child xml_arg0 "GameServerProtectionPolicy") in
      let balancingStrategy =
        (Option.map ~f:BalancingStrategy.of_xml)
          (Xml.child xml_arg0 "BalancingStrategy") in
      let instanceDefinitions =
        (Option.map ~f:InstanceDefinitions.of_xml)
          (Xml.child xml_arg0 "InstanceDefinitions") in
      let roleArn =
        (Option.map ~f:IamRoleArn.of_xml) (Xml.child xml_arg0 "RoleArn") in
      let gameServerGroupArn =
        (Option.map ~f:GameServerGroupArn.of_xml)
          (Xml.child xml_arg0 "GameServerGroupArn") in
      let gameServerGroupName =
        (Option.map ~f:GameServerGroupName.of_xml)
          (Xml.child xml_arg0 "GameServerGroupName") in
      make ?lastUpdatedTime ?creationTime ?suspendedActions ?statusReason
        ?status ?autoScalingGroupArn ?gameServerProtectionPolicy
        ?balancingStrategy ?instanceDefinitions ?roleArn ?gameServerGroupArn
        ?gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastUpdatedTime =
        field_map json__ "LastUpdatedTime" Timestamp.of_json in
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let suspendedActions =
        field_map json__ "SuspendedActions" GameServerGroupActions.of_json in
      let statusReason =
        field_map json__ "StatusReason" NonZeroAndMaxString.of_json in
      let status = field_map json__ "Status" GameServerGroupStatus.of_json in
      let autoScalingGroupArn =
        field_map json__ "AutoScalingGroupArn" AutoScalingGroupArn.of_json in
      let gameServerProtectionPolicy =
        field_map json__ "GameServerProtectionPolicy"
          GameServerProtectionPolicy.of_json in
      let balancingStrategy =
        field_map json__ "BalancingStrategy" BalancingStrategy.of_json in
      let instanceDefinitions =
        field_map json__ "InstanceDefinitions" InstanceDefinitions.of_json in
      let roleArn = field_map json__ "RoleArn" IamRoleArn.of_json in
      let gameServerGroupArn =
        field_map json__ "GameServerGroupArn" GameServerGroupArn.of_json in
      let gameServerGroupName =
        field_map json__ "GameServerGroupName" GameServerGroupName.of_json in
      make ?lastUpdatedTime ?creationTime ?suspendedActions ?statusReason
        ?status ?autoScalingGroupArn ?gameServerProtectionPolicy
        ?balancingStrategy ?instanceDefinitions ?roleArn ?gameServerGroupArn
        ?gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Properties that describe a game server group resource. A game server group manages certain properties related to a corresponding Amazon EC2 Auto Scaling group. A game server group is created by a successful call to CreateGameServerGroup and deleted by calling DeleteGameServerGroup. Game server group activity can be temporarily suspended and resumed by calling SuspendGameServerGroup and ResumeGameServerGroup, respectively."]
module FleetDeployment =
  struct
    type nonrec t =
      {
      deploymentId: DeploymentId.t option
        [@ocaml.doc "A unique identifier for the deployment."];
      fleetId: FleetId.t option
        [@ocaml.doc "A unique identifier for the container fleet."];
      gameServerBinaryArn: FleetBinaryArn.t option
        [@ocaml.doc
          "The unique identifier for the version of the game server container group definition that is being deployed."];
      rollbackGameServerBinaryArn: FleetBinaryArn.t option
        [@ocaml.doc
          "The unique identifier for the version of the game server container group definition to roll back to if deployment fails. Amazon GameLift Servers sets this property to the container group definition version that the fleet used when it was last active."];
      perInstanceBinaryArn: FleetBinaryArn.t option
        [@ocaml.doc
          "The unique identifier for the version of the per-instance container group definition that is being deployed."];
      rollbackPerInstanceBinaryArn: FleetBinaryArn.t option
        [@ocaml.doc
          "The unique identifier for the version of the per-instance container group definition to roll back to if deployment fails. Amazon GameLift Servers sets this property to the container group definition version that the fleet used when it was last active."];
      deploymentStatus: DeploymentStatus.t option
        [@ocaml.doc
          "The status of fleet deployment activity in the location. IN_PROGRESS -- The deployment is in progress. IMPAIRED -- The deployment failed and the fleet has some impaired containers. COMPLETE -- The deployment has completed successfully. ROLLBACK_IN_PROGRESS -- The deployment failed and rollback has been initiated. ROLLBACK_IN_COMPLETE -- The deployment failed and rollback has been completed. CANCELLED -- The deployment was cancelled."];
      deploymentConfiguration: DeploymentConfiguration.t option
        [@ocaml.doc
          "Instructions for how to deploy updates to a container fleet and what actions to take if the deployment fails."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."]}
    let make ?deploymentId =
      fun ?fleetId ->
        fun ?gameServerBinaryArn ->
          fun ?rollbackGameServerBinaryArn ->
            fun ?perInstanceBinaryArn ->
              fun ?rollbackPerInstanceBinaryArn ->
                fun ?deploymentStatus ->
                  fun ?deploymentConfiguration ->
                    fun ?creationTime ->
                      fun () ->
                        {
                          deploymentId;
                          fleetId;
                          gameServerBinaryArn;
                          rollbackGameServerBinaryArn;
                          perInstanceBinaryArn;
                          rollbackPerInstanceBinaryArn;
                          deploymentStatus;
                          deploymentConfiguration;
                          creationTime
                        }
    let to_value x =
      structure_to_value
        [("DeploymentId",
           (Option.map x.deploymentId ~f:DeploymentId.to_value));
        ("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("GameServerBinaryArn",
          (Option.map x.gameServerBinaryArn ~f:FleetBinaryArn.to_value));
        ("RollbackGameServerBinaryArn",
          (Option.map x.rollbackGameServerBinaryArn
             ~f:FleetBinaryArn.to_value));
        ("PerInstanceBinaryArn",
          (Option.map x.perInstanceBinaryArn ~f:FleetBinaryArn.to_value));
        ("RollbackPerInstanceBinaryArn",
          (Option.map x.rollbackPerInstanceBinaryArn
             ~f:FleetBinaryArn.to_value));
        ("DeploymentStatus",
          (Option.map x.deploymentStatus ~f:DeploymentStatus.to_value));
        ("DeploymentConfiguration",
          (Option.map x.deploymentConfiguration
             ~f:DeploymentConfiguration.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let deploymentConfiguration =
        (Option.map ~f:DeploymentConfiguration.of_xml)
          (Xml.child xml_arg0 "DeploymentConfiguration") in
      let deploymentStatus =
        (Option.map ~f:DeploymentStatus.of_xml)
          (Xml.child xml_arg0 "DeploymentStatus") in
      let rollbackPerInstanceBinaryArn =
        (Option.map ~f:FleetBinaryArn.of_xml)
          (Xml.child xml_arg0 "RollbackPerInstanceBinaryArn") in
      let perInstanceBinaryArn =
        (Option.map ~f:FleetBinaryArn.of_xml)
          (Xml.child xml_arg0 "PerInstanceBinaryArn") in
      let rollbackGameServerBinaryArn =
        (Option.map ~f:FleetBinaryArn.of_xml)
          (Xml.child xml_arg0 "RollbackGameServerBinaryArn") in
      let gameServerBinaryArn =
        (Option.map ~f:FleetBinaryArn.of_xml)
          (Xml.child xml_arg0 "GameServerBinaryArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      let deploymentId =
        (Option.map ~f:DeploymentId.of_xml)
          (Xml.child xml_arg0 "DeploymentId") in
      make ?creationTime ?deploymentConfiguration ?deploymentStatus
        ?rollbackPerInstanceBinaryArn ?perInstanceBinaryArn
        ?rollbackGameServerBinaryArn ?gameServerBinaryArn ?fleetId
        ?deploymentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let deploymentConfiguration =
        field_map json__ "DeploymentConfiguration"
          DeploymentConfiguration.of_json in
      let deploymentStatus =
        field_map json__ "DeploymentStatus" DeploymentStatus.of_json in
      let rollbackPerInstanceBinaryArn =
        field_map json__ "RollbackPerInstanceBinaryArn"
          FleetBinaryArn.of_json in
      let perInstanceBinaryArn =
        field_map json__ "PerInstanceBinaryArn" FleetBinaryArn.of_json in
      let rollbackGameServerBinaryArn =
        field_map json__ "RollbackGameServerBinaryArn" FleetBinaryArn.of_json in
      let gameServerBinaryArn =
        field_map json__ "GameServerBinaryArn" FleetBinaryArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      let deploymentId = field_map json__ "DeploymentId" DeploymentId.of_json in
      make ?creationTime ?deploymentConfiguration ?deploymentStatus
        ?rollbackPerInstanceBinaryArn ?perInstanceBinaryArn
        ?rollbackGameServerBinaryArn ?gameServerBinaryArn ?fleetId
        ?deploymentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a container fleet deployment with updates to the fleet."]
module ContainerGroupDefinition =
  struct
    type nonrec t =
      {
      containerGroupDefinitionArn: ContainerGroupDefinitionArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to an Amazon GameLift Servers ContainerGroupDefinition resource. It uniquely identifies the resource across all Amazon Web Services Regions. Format is arn:aws:gamelift:\\[region\\]::containergroupdefinition/\\[container group definition name\\]:\\[version\\]."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      operatingSystem: ContainerOperatingSystem.t option
        [@ocaml.doc
          "The platform that all containers in the container group definition run on. Amazon Linux 2 (AL2) will reach end of support on 6/30/2026. See more details in the Amazon Linux 2 FAQs. For game servers that are hosted on AL2 and use server SDK version 4.x for Amazon GameLift Servers, first update the game server build to server SDK 5.x, and then deploy to AL2023 instances. See Migrate to server SDK version 5."];
      name: ContainerGroupDefinitionName.t option
        [@ocaml.doc
          "A descriptive identifier for the container group definition. The name value is unique in an Amazon Web Services Region."];
      containerGroupType: ContainerGroupType.t option
        [@ocaml.doc
          "The type of container group. Container group type determines how Amazon GameLift Servers deploys the container group on each fleet instance."];
      totalMemoryLimitMebibytes: ContainerTotalMemoryLimit.t option
        [@ocaml.doc
          "The amount of memory (in MiB) on a fleet instance to allocate for the container group. All containers in the group share these resources. You can set a limit for each container definition in the group. If individual containers have limits, this total value must be greater than any individual container's memory limit."];
      totalVcpuLimit: ContainerTotalVcpuLimit.t option
        [@ocaml.doc
          "The amount of vCPU units on a fleet instance to allocate for the container group (1 vCPU is equal to 1024 CPU units). All containers in the group share these resources. You can set a limit for each container definition in the group. If individual containers have limits, this total value must be equal to or greater than the sum of the limits for each container in the group."];
      gameServerContainerDefinition: GameServerContainerDefinition.t option
        [@ocaml.doc
          "The definition for the game server container in this group. This property is used only when the container group type is GAME_SERVER. This container definition specifies a container image with the game server build."];
      supportContainerDefinitions: SupportContainerDefinitionList.t option
        [@ocaml.doc
          "The set of definitions for support containers in this group. A container group definition might have zero support container definitions. Support container can be used in any type of container group."];
      versionNumber: PositiveInteger.t option
        [@ocaml.doc
          "Indicates the version of a particular container group definition. This number is incremented automatically when you update a container group definition. You can view, update, or delete individual versions or the entire container group definition."];
      versionDescription: NonZeroAndMaxString.t option
        [@ocaml.doc
          "An optional description that was provided for a container group definition update. Each version can have a unique description."];
      status: ContainerGroupDefinitionStatus.t option
        [@ocaml.doc
          "Current status of the container group definition resource. Values include: COPYING -- Amazon GameLift Servers is in the process of making copies of all container images that are defined in the group. While in this state, the resource can't be used to create a container fleet. READY -- Amazon GameLift Servers has copied the registry images for all containers that are defined in the group. You can use a container group definition in this status to create a container fleet. FAILED -- Amazon GameLift Servers failed to create a valid container group definition resource. For more details on the cause of the failure, see StatusReason. A container group definition resource in failed status will be deleted within a few minutes."];
      statusReason: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Additional information about a container group definition that's in FAILED status. Possible reasons include: An internal issue prevented Amazon GameLift Servers from creating the container group definition resource. Delete the failed resource and call CreateContainerGroupDefinitionagain. An access-denied message means that you don't have permissions to access the container image on ECR. See IAM permission examples for help setting up required IAM permissions for Amazon GameLift Servers. The ImageUri value for at least one of the containers in the container group definition was invalid or not found in the current Amazon Web Services account. At least one of the container images referenced in the container group definition exceeds the allowed size. For size limits, see Amazon GameLift Servers endpoints and quotas. At least one of the container images referenced in the container group definition uses a different operating system than the one defined for the container group."]}
    let make ?containerGroupDefinitionArn =
      fun ?creationTime ->
        fun ?operatingSystem ->
          fun ?name ->
            fun ?containerGroupType ->
              fun ?totalMemoryLimitMebibytes ->
                fun ?totalVcpuLimit ->
                  fun ?gameServerContainerDefinition ->
                    fun ?supportContainerDefinitions ->
                      fun ?versionNumber ->
                        fun ?versionDescription ->
                          fun ?status ->
                            fun ?statusReason ->
                              fun () ->
                                {
                                  containerGroupDefinitionArn;
                                  creationTime;
                                  operatingSystem;
                                  name;
                                  containerGroupType;
                                  totalMemoryLimitMebibytes;
                                  totalVcpuLimit;
                                  gameServerContainerDefinition;
                                  supportContainerDefinitions;
                                  versionNumber;
                                  versionDescription;
                                  status;
                                  statusReason
                                }
    let to_value x =
      structure_to_value
        [("ContainerGroupDefinitionArn",
           (Option.map x.containerGroupDefinitionArn
              ~f:ContainerGroupDefinitionArn.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("OperatingSystem",
          (Option.map x.operatingSystem ~f:ContainerOperatingSystem.to_value));
        ("Name",
          (Option.map x.name ~f:ContainerGroupDefinitionName.to_value));
        ("ContainerGroupType",
          (Option.map x.containerGroupType ~f:ContainerGroupType.to_value));
        ("TotalMemoryLimitMebibytes",
          (Option.map x.totalMemoryLimitMebibytes
             ~f:ContainerTotalMemoryLimit.to_value));
        ("TotalVcpuLimit",
          (Option.map x.totalVcpuLimit ~f:ContainerTotalVcpuLimit.to_value));
        ("GameServerContainerDefinition",
          (Option.map x.gameServerContainerDefinition
             ~f:GameServerContainerDefinition.to_value));
        ("SupportContainerDefinitions",
          (Option.map x.supportContainerDefinitions
             ~f:SupportContainerDefinitionList.to_value));
        ("VersionNumber",
          (Option.map x.versionNumber ~f:PositiveInteger.to_value));
        ("VersionDescription",
          (Option.map x.versionDescription ~f:NonZeroAndMaxString.to_value));
        ("Status",
          (Option.map x.status ~f:ContainerGroupDefinitionStatus.to_value));
        ("StatusReason",
          (Option.map x.statusReason ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statusReason =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:ContainerGroupDefinitionStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let versionDescription =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "VersionDescription") in
      let versionNumber =
        (Option.map ~f:PositiveInteger.of_xml)
          (Xml.child xml_arg0 "VersionNumber") in
      let supportContainerDefinitions =
        (Option.map ~f:SupportContainerDefinitionList.of_xml)
          (Xml.child xml_arg0 "SupportContainerDefinitions") in
      let gameServerContainerDefinition =
        (Option.map ~f:GameServerContainerDefinition.of_xml)
          (Xml.child xml_arg0 "GameServerContainerDefinition") in
      let totalVcpuLimit =
        (Option.map ~f:ContainerTotalVcpuLimit.of_xml)
          (Xml.child xml_arg0 "TotalVcpuLimit") in
      let totalMemoryLimitMebibytes =
        (Option.map ~f:ContainerTotalMemoryLimit.of_xml)
          (Xml.child xml_arg0 "TotalMemoryLimitMebibytes") in
      let containerGroupType =
        (Option.map ~f:ContainerGroupType.of_xml)
          (Xml.child xml_arg0 "ContainerGroupType") in
      let name =
        (Option.map ~f:ContainerGroupDefinitionName.of_xml)
          (Xml.child xml_arg0 "Name") in
      let operatingSystem =
        (Option.map ~f:ContainerOperatingSystem.of_xml)
          (Xml.child xml_arg0 "OperatingSystem") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let containerGroupDefinitionArn =
        (Option.map ~f:ContainerGroupDefinitionArn.of_xml)
          (Xml.child xml_arg0 "ContainerGroupDefinitionArn") in
      make ?statusReason ?status ?versionDescription ?versionNumber
        ?supportContainerDefinitions ?gameServerContainerDefinition
        ?totalVcpuLimit ?totalMemoryLimitMebibytes ?containerGroupType ?name
        ?operatingSystem ?creationTime ?containerGroupDefinitionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let statusReason =
        field_map json__ "StatusReason" NonZeroAndMaxString.of_json in
      let status =
        field_map json__ "Status" ContainerGroupDefinitionStatus.of_json in
      let versionDescription =
        field_map json__ "VersionDescription" NonZeroAndMaxString.of_json in
      let versionNumber =
        field_map json__ "VersionNumber" PositiveInteger.of_json in
      let supportContainerDefinitions =
        field_map json__ "SupportContainerDefinitions"
          SupportContainerDefinitionList.of_json in
      let gameServerContainerDefinition =
        field_map json__ "GameServerContainerDefinition"
          GameServerContainerDefinition.of_json in
      let totalVcpuLimit =
        field_map json__ "TotalVcpuLimit" ContainerTotalVcpuLimit.of_json in
      let totalMemoryLimitMebibytes =
        field_map json__ "TotalMemoryLimitMebibytes"
          ContainerTotalMemoryLimit.of_json in
      let containerGroupType =
        field_map json__ "ContainerGroupType" ContainerGroupType.of_json in
      let name = field_map json__ "Name" ContainerGroupDefinitionName.of_json in
      let operatingSystem =
        field_map json__ "OperatingSystem" ContainerOperatingSystem.of_json in
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let containerGroupDefinitionArn =
        field_map json__ "ContainerGroupDefinitionArn"
          ContainerGroupDefinitionArn.of_json in
      make ?statusReason ?status ?versionDescription ?versionNumber
        ?supportContainerDefinitions ?gameServerContainerDefinition
        ?totalVcpuLimit ?totalMemoryLimitMebibytes ?containerGroupType ?name
        ?operatingSystem ?creationTime ?containerGroupDefinitionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The properties that describe a container group resource. You can update all properties of a container group definition properties. Updates to a container group definition are saved as new versions. Used with: CreateContainerGroupDefinition Returned by: DescribeContainerGroupDefinition, ListContainerGroupDefinitions, UpdateContainerGroupDefinition"]
module ContainerFleet =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc
          "A unique identifier for the container fleet to retrieve."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912. In a GameLift fleet ARN, the resource ID matches the FleetId value."];
      fleetRoleArn: IamRoleArn.t option
        [@ocaml.doc
          "The unique identifier for an Identity and Access Management (IAM) role with permissions to run your containers on resources that are managed by Amazon GameLift Servers. See Set up an IAM service role. This fleet property can't be changed."];
      gameServerContainerGroupDefinitionName:
        ContainerGroupDefinitionName.t option
        [@ocaml.doc
          "The name of the fleet's game server container group definition, which describes how to deploy containers with your game server build and support software onto each fleet instance."];
      gameServerContainerGroupDefinitionArn:
        ContainerGroupDefinitionArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to the fleet's game server container group. The ARN value also identifies the specific container group definition version in use."];
      perInstanceContainerGroupDefinitionName:
        ContainerGroupDefinitionName.t option
        [@ocaml.doc
          "The name of the fleet's per-instance container group definition."];
      perInstanceContainerGroupDefinitionArn:
        ContainerGroupDefinitionArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to the fleet's per-instance container group. The ARN value also identifies the specific container group definition version in use."];
      instanceConnectionPortRange: ConnectionPortRange.t option ;
      instanceInboundPermissions: IpPermissionsList.t option
        [@ocaml.doc
          "The IP address ranges and port settings that allow inbound traffic to access game server processes and other processes on this fleet."];
      gameServerContainerGroupsPerInstance:
        GameServerContainerGroupsPerInstance.t option
        [@ocaml.doc
          "The number of times to replicate the game server container group on each fleet instance."];
      maximumGameServerContainerGroupsPerInstance:
        MaximumGameServerContainerGroupsPerInstance.t option
        [@ocaml.doc
          "The calculated maximum number of game server container group that can be deployed on each fleet instance. The calculation depends on the resource needs of the container group and the CPU and memory resources of the fleet's instance type."];
      instanceType: NonZeroAndMaxString.t option
        [@ocaml.doc
          "The Amazon EC2 instance type to use for all instances in the fleet. Instance type determines the computing resources and processing power that's available to host your game servers. This includes including CPU, memory, storage, and networking capacity. You can't update this fleet property."];
      billingType: ContainerFleetBillingType.t option
        [@ocaml.doc
          "Indicates whether the fleet uses On-Demand or Spot instances for this fleet. Learn more about when to use On-Demand versus Spot Instances. You can't update this fleet property. By default, this property is set to ON_DEMAND."];
      description: NonZeroAndMaxString.t option
        [@ocaml.doc "A meaningful description of the container fleet."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      metricGroups: MetricGroupList.t option
        [@ocaml.doc
          "The name of an Amazon Web Services CloudWatch metric group to add this fleet to. Metric groups aggregate metrics for multiple fleets."];
      newGameSessionProtectionPolicy: ProtectionPolicy.t option
        [@ocaml.doc
          "Determines whether Amazon GameLift Servers can shut down game sessions on the fleet that are actively running and hosting players. Amazon GameLift Servers might prompt an instance shutdown when scaling down fleet capacity or when retiring unhealthy instances. You can also set game session protection for individual game sessions using UpdateGameSession. NoProtection -- Game sessions can be shut down during active gameplay. FullProtection -- Game sessions in ACTIVE status can't be shut down."];
      gameSessionCreationLimitPolicy: GameSessionCreationLimitPolicy.t option
        [@ocaml.doc
          "A policy that limits the number of game sessions that each individual player can create on instances in this fleet. The limit applies for a specified span of time."];
      status: ContainerFleetStatus.t option
        [@ocaml.doc
          "The current status of the container fleet. PENDING -- A new container fleet has been requested. CREATING -- A new container fleet resource is being created. CREATED -- A new container fleet resource has been created. No fleet instances have been deployed. ACTIVATING -- New container fleet instances are being deployed. ACTIVE -- The container fleet has been deployed and is ready to host game sessions. UPDATING -- Updates to the container fleet is being updated. A deployment is in progress."];
      deploymentDetails: DeploymentDetails.t option
        [@ocaml.doc
          "Information about the most recent deployment for the container fleet."];
      logConfiguration: LogConfiguration.t option
        [@ocaml.doc
          "The method that is used to collect container logs for the fleet. Amazon GameLift Servers saves all standard output for each container in logs, including game session logs. CLOUDWATCH -- Send logs to an Amazon CloudWatch log group that you define. Each container emits a log stream, which is organized in the log group. S3 -- Store logs in an Amazon S3 bucket that you define. NONE -- Don't collect container logs."];
      locationAttributes: ContainerFleetLocationAttributesList.t option
        [@ocaml.doc
          "Information about the container fleet's remote locations where fleet instances are deployed."];
      playerGatewayMode: PlayerGatewayMode.t option
        [@ocaml.doc
          "Indicates whether player gateway is enabled for this container fleet. Player gateway provides benefits such as DDoS protection with negligible impact to latency. If ENABLED or REQUIRED, game clients can use player gateway to connect with the game server. If DISABLED, game clients cannot use player gateway. Instead, they have to directly connect to the game server."]}
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?fleetRoleArn ->
          fun ?gameServerContainerGroupDefinitionName ->
            fun ?gameServerContainerGroupDefinitionArn ->
              fun ?perInstanceContainerGroupDefinitionName ->
                fun ?perInstanceContainerGroupDefinitionArn ->
                  fun ?instanceConnectionPortRange ->
                    fun ?instanceInboundPermissions ->
                      fun ?gameServerContainerGroupsPerInstance ->
                        fun ?maximumGameServerContainerGroupsPerInstance ->
                          fun ?instanceType ->
                            fun ?billingType ->
                              fun ?description ->
                                fun ?creationTime ->
                                  fun ?metricGroups ->
                                    fun ?newGameSessionProtectionPolicy ->
                                      fun ?gameSessionCreationLimitPolicy ->
                                        fun ?status ->
                                          fun ?deploymentDetails ->
                                            fun ?logConfiguration ->
                                              fun ?locationAttributes ->
                                                fun ?playerGatewayMode ->
                                                  fun () ->
                                                    {
                                                      fleetId;
                                                      fleetArn;
                                                      fleetRoleArn;
                                                      gameServerContainerGroupDefinitionName;
                                                      gameServerContainerGroupDefinitionArn;
                                                      perInstanceContainerGroupDefinitionName;
                                                      perInstanceContainerGroupDefinitionArn;
                                                      instanceConnectionPortRange;
                                                      instanceInboundPermissions;
                                                      gameServerContainerGroupsPerInstance;
                                                      maximumGameServerContainerGroupsPerInstance;
                                                      instanceType;
                                                      billingType;
                                                      description;
                                                      creationTime;
                                                      metricGroups;
                                                      newGameSessionProtectionPolicy;
                                                      gameSessionCreationLimitPolicy;
                                                      status;
                                                      deploymentDetails;
                                                      logConfiguration;
                                                      locationAttributes;
                                                      playerGatewayMode
                                                    }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("FleetRoleArn", (Option.map x.fleetRoleArn ~f:IamRoleArn.to_value));
        ("GameServerContainerGroupDefinitionName",
          (Option.map x.gameServerContainerGroupDefinitionName
             ~f:ContainerGroupDefinitionName.to_value));
        ("GameServerContainerGroupDefinitionArn",
          (Option.map x.gameServerContainerGroupDefinitionArn
             ~f:ContainerGroupDefinitionArn.to_value));
        ("PerInstanceContainerGroupDefinitionName",
          (Option.map x.perInstanceContainerGroupDefinitionName
             ~f:ContainerGroupDefinitionName.to_value));
        ("PerInstanceContainerGroupDefinitionArn",
          (Option.map x.perInstanceContainerGroupDefinitionArn
             ~f:ContainerGroupDefinitionArn.to_value));
        ("InstanceConnectionPortRange",
          (Option.map x.instanceConnectionPortRange
             ~f:ConnectionPortRange.to_value));
        ("InstanceInboundPermissions",
          (Option.map x.instanceInboundPermissions
             ~f:IpPermissionsList.to_value));
        ("GameServerContainerGroupsPerInstance",
          (Option.map x.gameServerContainerGroupsPerInstance
             ~f:GameServerContainerGroupsPerInstance.to_value));
        ("MaximumGameServerContainerGroupsPerInstance",
          (Option.map x.maximumGameServerContainerGroupsPerInstance
             ~f:MaximumGameServerContainerGroupsPerInstance.to_value));
        ("InstanceType",
          (Option.map x.instanceType ~f:NonZeroAndMaxString.to_value));
        ("BillingType",
          (Option.map x.billingType ~f:ContainerFleetBillingType.to_value));
        ("Description",
          (Option.map x.description ~f:NonZeroAndMaxString.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("MetricGroups",
          (Option.map x.metricGroups ~f:MetricGroupList.to_value));
        ("NewGameSessionProtectionPolicy",
          (Option.map x.newGameSessionProtectionPolicy
             ~f:ProtectionPolicy.to_value));
        ("GameSessionCreationLimitPolicy",
          (Option.map x.gameSessionCreationLimitPolicy
             ~f:GameSessionCreationLimitPolicy.to_value));
        ("Status", (Option.map x.status ~f:ContainerFleetStatus.to_value));
        ("DeploymentDetails",
          (Option.map x.deploymentDetails ~f:DeploymentDetails.to_value));
        ("LogConfiguration",
          (Option.map x.logConfiguration ~f:LogConfiguration.to_value));
        ("LocationAttributes",
          (Option.map x.locationAttributes
             ~f:ContainerFleetLocationAttributesList.to_value));
        ("PlayerGatewayMode",
          (Option.map x.playerGatewayMode ~f:PlayerGatewayMode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerGatewayMode =
        (Option.map ~f:PlayerGatewayMode.of_xml)
          (Xml.child xml_arg0 "PlayerGatewayMode") in
      let locationAttributes =
        (Option.map ~f:ContainerFleetLocationAttributesList.of_xml)
          (Xml.child xml_arg0 "LocationAttributes") in
      let logConfiguration =
        (Option.map ~f:LogConfiguration.of_xml)
          (Xml.child xml_arg0 "LogConfiguration") in
      let deploymentDetails =
        (Option.map ~f:DeploymentDetails.of_xml)
          (Xml.child xml_arg0 "DeploymentDetails") in
      let status =
        (Option.map ~f:ContainerFleetStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let gameSessionCreationLimitPolicy =
        (Option.map ~f:GameSessionCreationLimitPolicy.of_xml)
          (Xml.child xml_arg0 "GameSessionCreationLimitPolicy") in
      let newGameSessionProtectionPolicy =
        (Option.map ~f:ProtectionPolicy.of_xml)
          (Xml.child xml_arg0 "NewGameSessionProtectionPolicy") in
      let metricGroups =
        (Option.map ~f:MetricGroupList.of_xml)
          (Xml.child xml_arg0 "MetricGroups") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let description =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Description") in
      let billingType =
        (Option.map ~f:ContainerFleetBillingType.of_xml)
          (Xml.child xml_arg0 "BillingType") in
      let instanceType =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let maximumGameServerContainerGroupsPerInstance =
        (Option.map ~f:MaximumGameServerContainerGroupsPerInstance.of_xml)
          (Xml.child xml_arg0 "MaximumGameServerContainerGroupsPerInstance") in
      let gameServerContainerGroupsPerInstance =
        (Option.map ~f:GameServerContainerGroupsPerInstance.of_xml)
          (Xml.child xml_arg0 "GameServerContainerGroupsPerInstance") in
      let instanceInboundPermissions =
        (Option.map ~f:IpPermissionsList.of_xml)
          (Xml.child xml_arg0 "InstanceInboundPermissions") in
      let instanceConnectionPortRange =
        (Option.map ~f:ConnectionPortRange.of_xml)
          (Xml.child xml_arg0 "InstanceConnectionPortRange") in
      let perInstanceContainerGroupDefinitionArn =
        (Option.map ~f:ContainerGroupDefinitionArn.of_xml)
          (Xml.child xml_arg0 "PerInstanceContainerGroupDefinitionArn") in
      let perInstanceContainerGroupDefinitionName =
        (Option.map ~f:ContainerGroupDefinitionName.of_xml)
          (Xml.child xml_arg0 "PerInstanceContainerGroupDefinitionName") in
      let gameServerContainerGroupDefinitionArn =
        (Option.map ~f:ContainerGroupDefinitionArn.of_xml)
          (Xml.child xml_arg0 "GameServerContainerGroupDefinitionArn") in
      let gameServerContainerGroupDefinitionName =
        (Option.map ~f:ContainerGroupDefinitionName.of_xml)
          (Xml.child xml_arg0 "GameServerContainerGroupDefinitionName") in
      let fleetRoleArn =
        (Option.map ~f:IamRoleArn.of_xml) (Xml.child xml_arg0 "FleetRoleArn") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?playerGatewayMode ?locationAttributes ?logConfiguration
        ?deploymentDetails ?status ?gameSessionCreationLimitPolicy
        ?newGameSessionProtectionPolicy ?metricGroups ?creationTime
        ?description ?billingType ?instanceType
        ?maximumGameServerContainerGroupsPerInstance
        ?gameServerContainerGroupsPerInstance ?instanceInboundPermissions
        ?instanceConnectionPortRange ?perInstanceContainerGroupDefinitionArn
        ?perInstanceContainerGroupDefinitionName
        ?gameServerContainerGroupDefinitionArn
        ?gameServerContainerGroupDefinitionName ?fleetRoleArn ?fleetArn
        ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerGatewayMode =
        field_map json__ "PlayerGatewayMode" PlayerGatewayMode.of_json in
      let locationAttributes =
        field_map json__ "LocationAttributes"
          ContainerFleetLocationAttributesList.of_json in
      let logConfiguration =
        field_map json__ "LogConfiguration" LogConfiguration.of_json in
      let deploymentDetails =
        field_map json__ "DeploymentDetails" DeploymentDetails.of_json in
      let status = field_map json__ "Status" ContainerFleetStatus.of_json in
      let gameSessionCreationLimitPolicy =
        field_map json__ "GameSessionCreationLimitPolicy"
          GameSessionCreationLimitPolicy.of_json in
      let newGameSessionProtectionPolicy =
        field_map json__ "NewGameSessionProtectionPolicy"
          ProtectionPolicy.of_json in
      let metricGroups =
        field_map json__ "MetricGroups" MetricGroupList.of_json in
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let description =
        field_map json__ "Description" NonZeroAndMaxString.of_json in
      let billingType =
        field_map json__ "BillingType" ContainerFleetBillingType.of_json in
      let instanceType =
        field_map json__ "InstanceType" NonZeroAndMaxString.of_json in
      let maximumGameServerContainerGroupsPerInstance =
        field_map json__ "MaximumGameServerContainerGroupsPerInstance"
          MaximumGameServerContainerGroupsPerInstance.of_json in
      let gameServerContainerGroupsPerInstance =
        field_map json__ "GameServerContainerGroupsPerInstance"
          GameServerContainerGroupsPerInstance.of_json in
      let instanceInboundPermissions =
        field_map json__ "InstanceInboundPermissions"
          IpPermissionsList.of_json in
      let instanceConnectionPortRange =
        field_map json__ "InstanceConnectionPortRange"
          ConnectionPortRange.of_json in
      let perInstanceContainerGroupDefinitionArn =
        field_map json__ "PerInstanceContainerGroupDefinitionArn"
          ContainerGroupDefinitionArn.of_json in
      let perInstanceContainerGroupDefinitionName =
        field_map json__ "PerInstanceContainerGroupDefinitionName"
          ContainerGroupDefinitionName.of_json in
      let gameServerContainerGroupDefinitionArn =
        field_map json__ "GameServerContainerGroupDefinitionArn"
          ContainerGroupDefinitionArn.of_json in
      let gameServerContainerGroupDefinitionName =
        field_map json__ "GameServerContainerGroupDefinitionName"
          ContainerGroupDefinitionName.of_json in
      let fleetRoleArn = field_map json__ "FleetRoleArn" IamRoleArn.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?playerGatewayMode ?locationAttributes ?logConfiguration
        ?deploymentDetails ?status ?gameSessionCreationLimitPolicy
        ?newGameSessionProtectionPolicy ?metricGroups ?creationTime
        ?description ?billingType ?instanceType
        ?maximumGameServerContainerGroupsPerInstance
        ?gameServerContainerGroupsPerInstance ?instanceInboundPermissions
        ?instanceConnectionPortRange ?perInstanceContainerGroupDefinitionArn
        ?perInstanceContainerGroupDefinitionName
        ?gameServerContainerGroupDefinitionArn
        ?gameServerContainerGroupDefinitionName ?fleetRoleArn ?fleetArn
        ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes an Amazon GameLift Servers managed container fleet."]
module Compute =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc
          "A unique identifier for the fleet that the compute belongs to."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the fleet that the compute belongs to."];
      computeName: ComputeName.t option
        [@ocaml.doc
          "A descriptive label for the compute resource. For instances in a managed EC2 fleet, the compute name is the same value as the InstanceId ID."];
      computeArn: ComputeArn.t option
        [@ocaml.doc
          "The ARN that is assigned to a compute resource and uniquely identifies it. ARNs are unique across locations. Instances in managed EC2 fleets are not assigned a Compute ARN."];
      ipAddress: IpAddress.t option
        [@ocaml.doc
          "The IP address of a compute resource. Amazon GameLift Servers requires a DNS name or IP address for a compute."];
      dnsName: DnsName.t option
        [@ocaml.doc
          "The DNS name of a compute resource. Amazon GameLift Servers requires a DNS name or IP address for a compute."];
      computeStatus: ComputeStatus.t option
        [@ocaml.doc
          "Current status of the compute. A compute must have an ACTIVE status to host game sessions. Valid values include PENDING, ACTIVE, TERMINATING, and IMPAIRED. While the ComputeStatus enum type is valid for Container based servers, the result may also include other non-enumerated string values such as \"Active\" for fleets which are not Container-based."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The name of the custom location you added to the fleet that this compute resource resides in."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      operatingSystem: OperatingSystem.t option
        [@ocaml.doc
          "The type of operating system on the compute resource. Amazon Linux 2 (AL2) will reach end of support on 6/30/2026. See more details in the Amazon Linux 2 FAQs. For game servers that are hosted on AL2 and use server SDK version 4.x for Amazon GameLift Servers, first update the game server build to server SDK 5.x, and then deploy to AL2023 instances. See Migrate to server SDK version 5."];
      type_: EC2InstanceType.t option
        [@ocaml.doc
          "The Amazon EC2 instance type that the fleet uses. For registered computes in an Amazon GameLift Servers Anywhere fleet, this property is empty."];
      gameLiftServiceSdkEndpoint: GameLiftServiceSdkEndpointOutput.t option
        [@ocaml.doc
          "The Amazon GameLift Servers SDK endpoint connection for a registered compute resource in an Anywhere fleet. The game servers on the compute use this endpoint to connect to the Amazon GameLift Servers service."];
      gameLiftAgentEndpoint: GameLiftAgentEndpointOutput.t option
        [@ocaml.doc "The endpoint of the Amazon GameLift Servers Agent."];
      instanceId: InstanceId.t option
        [@ocaml.doc
          "The InstanceID of the EC2 instance that is hosting the compute."];
      containerAttributes: ContainerAttributes.t option
        [@ocaml.doc "A set of attributes for each container in the compute."];
      gameServerContainerGroupDefinitionArn:
        ContainerGroupDefinitionNameOrArn.t option
        [@ocaml.doc
          "The game server container group definition for the compute."]}
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?computeName ->
          fun ?computeArn ->
            fun ?ipAddress ->
              fun ?dnsName ->
                fun ?computeStatus ->
                  fun ?location ->
                    fun ?creationTime ->
                      fun ?operatingSystem ->
                        fun ?type_ ->
                          fun ?gameLiftServiceSdkEndpoint ->
                            fun ?gameLiftAgentEndpoint ->
                              fun ?instanceId ->
                                fun ?containerAttributes ->
                                  fun ?gameServerContainerGroupDefinitionArn
                                    ->
                                    fun () ->
                                      {
                                        fleetId;
                                        fleetArn;
                                        computeName;
                                        computeArn;
                                        ipAddress;
                                        dnsName;
                                        computeStatus;
                                        location;
                                        creationTime;
                                        operatingSystem;
                                        type_;
                                        gameLiftServiceSdkEndpoint;
                                        gameLiftAgentEndpoint;
                                        instanceId;
                                        containerAttributes;
                                        gameServerContainerGroupDefinitionArn
                                      }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("ComputeName", (Option.map x.computeName ~f:ComputeName.to_value));
        ("ComputeArn", (Option.map x.computeArn ~f:ComputeArn.to_value));
        ("IpAddress", (Option.map x.ipAddress ~f:IpAddress.to_value));
        ("DnsName", (Option.map x.dnsName ~f:DnsName.to_value));
        ("ComputeStatus",
          (Option.map x.computeStatus ~f:ComputeStatus.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("OperatingSystem",
          (Option.map x.operatingSystem ~f:OperatingSystem.to_value));
        ("Type", (Option.map x.type_ ~f:EC2InstanceType.to_value));
        ("GameLiftServiceSdkEndpoint",
          (Option.map x.gameLiftServiceSdkEndpoint
             ~f:GameLiftServiceSdkEndpointOutput.to_value));
        ("GameLiftAgentEndpoint",
          (Option.map x.gameLiftAgentEndpoint
             ~f:GameLiftAgentEndpointOutput.to_value));
        ("InstanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("ContainerAttributes",
          (Option.map x.containerAttributes ~f:ContainerAttributes.to_value));
        ("GameServerContainerGroupDefinitionArn",
          (Option.map x.gameServerContainerGroupDefinitionArn
             ~f:ContainerGroupDefinitionNameOrArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServerContainerGroupDefinitionArn =
        (Option.map ~f:ContainerGroupDefinitionNameOrArn.of_xml)
          (Xml.child xml_arg0 "GameServerContainerGroupDefinitionArn") in
      let containerAttributes =
        (Option.map ~f:ContainerAttributes.of_xml)
          (Xml.child xml_arg0 "ContainerAttributes") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "InstanceId") in
      let gameLiftAgentEndpoint =
        (Option.map ~f:GameLiftAgentEndpointOutput.of_xml)
          (Xml.child xml_arg0 "GameLiftAgentEndpoint") in
      let gameLiftServiceSdkEndpoint =
        (Option.map ~f:GameLiftServiceSdkEndpointOutput.of_xml)
          (Xml.child xml_arg0 "GameLiftServiceSdkEndpoint") in
      let type_ =
        (Option.map ~f:EC2InstanceType.of_xml) (Xml.child xml_arg0 "Type") in
      let operatingSystem =
        (Option.map ~f:OperatingSystem.of_xml)
          (Xml.child xml_arg0 "OperatingSystem") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let computeStatus =
        (Option.map ~f:ComputeStatus.of_xml)
          (Xml.child xml_arg0 "ComputeStatus") in
      let dnsName =
        (Option.map ~f:DnsName.of_xml) (Xml.child xml_arg0 "DnsName") in
      let ipAddress =
        (Option.map ~f:IpAddress.of_xml) (Xml.child xml_arg0 "IpAddress") in
      let computeArn =
        (Option.map ~f:ComputeArn.of_xml) (Xml.child xml_arg0 "ComputeArn") in
      let computeName =
        (Option.map ~f:ComputeName.of_xml) (Xml.child xml_arg0 "ComputeName") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?gameServerContainerGroupDefinitionArn ?containerAttributes
        ?instanceId ?gameLiftAgentEndpoint ?gameLiftServiceSdkEndpoint ?type_
        ?operatingSystem ?creationTime ?location ?computeStatus ?dnsName
        ?ipAddress ?computeArn ?computeName ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServerContainerGroupDefinitionArn =
        field_map json__ "GameServerContainerGroupDefinitionArn"
          ContainerGroupDefinitionNameOrArn.of_json in
      let containerAttributes =
        field_map json__ "ContainerAttributes" ContainerAttributes.of_json in
      let instanceId = field_map json__ "InstanceId" InstanceId.of_json in
      let gameLiftAgentEndpoint =
        field_map json__ "GameLiftAgentEndpoint"
          GameLiftAgentEndpointOutput.of_json in
      let gameLiftServiceSdkEndpoint =
        field_map json__ "GameLiftServiceSdkEndpoint"
          GameLiftServiceSdkEndpointOutput.of_json in
      let type_ = field_map json__ "Type" EC2InstanceType.of_json in
      let operatingSystem =
        field_map json__ "OperatingSystem" OperatingSystem.of_json in
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let computeStatus =
        field_map json__ "ComputeStatus" ComputeStatus.of_json in
      let dnsName = field_map json__ "DnsName" DnsName.of_json in
      let ipAddress = field_map json__ "IpAddress" IpAddress.of_json in
      let computeArn = field_map json__ "ComputeArn" ComputeArn.of_json in
      let computeName = field_map json__ "ComputeName" ComputeName.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?gameServerContainerGroupDefinitionArn ?containerAttributes
        ?instanceId ?gameLiftAgentEndpoint ?gameLiftServiceSdkEndpoint ?type_
        ?operatingSystem ?creationTime ?location ?computeStatus ?dnsName
        ?ipAddress ?computeArn ?computeName ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An Amazon GameLift Servers compute resource for hosting your game servers. Computes in an Amazon GameLift Servers fleet differs depending on the fleet's compute type property as follows: For managed EC2 fleets, a compute is an EC2 instance. For Anywhere fleets, a compute is a computing resource that you provide and is registered to the fleet."]
module Build =
  struct
    type nonrec t =
      {
      buildId: BuildId.t option
        [@ocaml.doc "A unique identifier for the build."];
      buildArn: BuildArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers build resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::build/build-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912. In a GameLift build ARN, the resource ID matches the BuildId value."];
      name: FreeText.t option
        [@ocaml.doc
          "A descriptive label that is associated with a build. Build names do not need to be unique. It can be set using CreateBuild or UpdateBuild."];
      version: FreeText.t option
        [@ocaml.doc
          "Version information that is associated with a build or script. Version strings do not need to be unique."];
      status: BuildStatus.t option
        [@ocaml.doc
          "Current status of the build. Possible build statuses include the following: INITIALIZED -- A new build has been defined, but no files have been uploaded. You cannot create fleets for builds that are in this status. When a build is successfully created, the build status is set to this value. READY -- The game build has been successfully uploaded. You can now create new fleets for this build. FAILED -- The game build upload failed. You cannot create new fleets for this build."];
      sizeOnDisk: WholeNumberLong.t option
        [@ocaml.doc
          "File size of the uploaded game build, expressed in bytes. When the build status is INITIALIZED or when using a custom Amazon S3 storage location, this value is 0."];
      operatingSystem: OperatingSystem.t option
        [@ocaml.doc
          "Operating system that the game server binaries are built to run on. This value determines the type of fleet resources that you can use for this build. Amazon Linux 2 (AL2) will reach end of support on 6/30/2026. See more details in the Amazon Linux 2 FAQs. For game servers that are hosted on AL2 and use server SDK version 4.x for Amazon GameLift Servers, first update the game server build to server SDK 5.x, and then deploy to AL2023 instances. See Migrate to server SDK version 5."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      serverSdkVersion: ServerSdkVersion.t option
        [@ocaml.doc
          "The Amazon GameLift Servers Server SDK version used to develop your game server."]}
    let make ?buildId =
      fun ?buildArn ->
        fun ?name ->
          fun ?version ->
            fun ?status ->
              fun ?sizeOnDisk ->
                fun ?operatingSystem ->
                  fun ?creationTime ->
                    fun ?serverSdkVersion ->
                      fun () ->
                        {
                          buildId;
                          buildArn;
                          name;
                          version;
                          status;
                          sizeOnDisk;
                          operatingSystem;
                          creationTime;
                          serverSdkVersion
                        }
    let to_value x =
      structure_to_value
        [("BuildId", (Option.map x.buildId ~f:BuildId.to_value));
        ("BuildArn", (Option.map x.buildArn ~f:BuildArn.to_value));
        ("Name", (Option.map x.name ~f:FreeText.to_value));
        ("Version", (Option.map x.version ~f:FreeText.to_value));
        ("Status", (Option.map x.status ~f:BuildStatus.to_value));
        ("SizeOnDisk", (Option.map x.sizeOnDisk ~f:WholeNumberLong.to_value));
        ("OperatingSystem",
          (Option.map x.operatingSystem ~f:OperatingSystem.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("ServerSdkVersion",
          (Option.map x.serverSdkVersion ~f:ServerSdkVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serverSdkVersion =
        (Option.map ~f:ServerSdkVersion.of_xml)
          (Xml.child xml_arg0 "ServerSdkVersion") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let operatingSystem =
        (Option.map ~f:OperatingSystem.of_xml)
          (Xml.child xml_arg0 "OperatingSystem") in
      let sizeOnDisk =
        (Option.map ~f:WholeNumberLong.of_xml)
          (Xml.child xml_arg0 "SizeOnDisk") in
      let status =
        (Option.map ~f:BuildStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let version =
        (Option.map ~f:FreeText.of_xml) (Xml.child xml_arg0 "Version") in
      let name = (Option.map ~f:FreeText.of_xml) (Xml.child xml_arg0 "Name") in
      let buildArn =
        (Option.map ~f:BuildArn.of_xml) (Xml.child xml_arg0 "BuildArn") in
      let buildId =
        (Option.map ~f:BuildId.of_xml) (Xml.child xml_arg0 "BuildId") in
      make ?serverSdkVersion ?creationTime ?operatingSystem ?sizeOnDisk
        ?status ?version ?name ?buildArn ?buildId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serverSdkVersion =
        field_map json__ "ServerSdkVersion" ServerSdkVersion.of_json in
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let operatingSystem =
        field_map json__ "OperatingSystem" OperatingSystem.of_json in
      let sizeOnDisk = field_map json__ "SizeOnDisk" WholeNumberLong.of_json in
      let status = field_map json__ "Status" BuildStatus.of_json in
      let version = field_map json__ "Version" FreeText.of_json in
      let name = field_map json__ "Name" FreeText.of_json in
      let buildArn = field_map json__ "BuildArn" BuildArn.of_json in
      let buildId = field_map json__ "BuildId" BuildId.of_json in
      make ?serverSdkVersion ?creationTime ?operatingSystem ?sizeOnDisk
        ?status ?version ?name ?buildArn ?buildId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Properties describing a custom game build. All APIs by task"]
module Alias =
  struct
    type nonrec t =
      {
      aliasId: AliasId.t option
        [@ocaml.doc
          "A unique identifier for the alias. Alias IDs are unique within a Region."];
      name: NonBlankAndLengthConstraintString.t option
        [@ocaml.doc
          "A descriptive label that is associated with an alias. Alias names do not need to be unique."];
      aliasArn: AliasArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers alias resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::alias/alias-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912. In a GameLift alias ARN, the resource ID matches the alias ID value."];
      description: FreeText.t option
        [@ocaml.doc "A human-readable description of an alias."];
      routingStrategy: RoutingStrategy.t option
        [@ocaml.doc
          "The routing configuration, including routing type and fleet target, for the alias."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      lastUpdatedTime: Timestamp.t option
        [@ocaml.doc
          "The time that this data object was last modified. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."]}
    let make ?aliasId =
      fun ?name ->
        fun ?aliasArn ->
          fun ?description ->
            fun ?routingStrategy ->
              fun ?creationTime ->
                fun ?lastUpdatedTime ->
                  fun () ->
                    {
                      aliasId;
                      name;
                      aliasArn;
                      description;
                      routingStrategy;
                      creationTime;
                      lastUpdatedTime
                    }
    let to_value x =
      structure_to_value
        [("AliasId", (Option.map x.aliasId ~f:AliasId.to_value));
        ("Name",
          (Option.map x.name ~f:NonBlankAndLengthConstraintString.to_value));
        ("AliasArn", (Option.map x.aliasArn ~f:AliasArn.to_value));
        ("Description", (Option.map x.description ~f:FreeText.to_value));
        ("RoutingStrategy",
          (Option.map x.routingStrategy ~f:RoutingStrategy.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("LastUpdatedTime",
          (Option.map x.lastUpdatedTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastUpdatedTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let routingStrategy =
        (Option.map ~f:RoutingStrategy.of_xml)
          (Xml.child xml_arg0 "RoutingStrategy") in
      let description =
        (Option.map ~f:FreeText.of_xml) (Xml.child xml_arg0 "Description") in
      let aliasArn =
        (Option.map ~f:AliasArn.of_xml) (Xml.child xml_arg0 "AliasArn") in
      let name =
        (Option.map ~f:NonBlankAndLengthConstraintString.of_xml)
          (Xml.child xml_arg0 "Name") in
      let aliasId =
        (Option.map ~f:AliasId.of_xml) (Xml.child xml_arg0 "AliasId") in
      make ?lastUpdatedTime ?creationTime ?routingStrategy ?description
        ?aliasArn ?name ?aliasId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastUpdatedTime =
        field_map json__ "LastUpdatedTime" Timestamp.of_json in
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let routingStrategy =
        field_map json__ "RoutingStrategy" RoutingStrategy.of_json in
      let description = field_map json__ "Description" FreeText.of_json in
      let aliasArn = field_map json__ "AliasArn" AliasArn.of_json in
      let name =
        field_map json__ "Name" NonBlankAndLengthConstraintString.of_json in
      let aliasId = field_map json__ "AliasId" AliasId.of_json in
      make ?lastUpdatedTime ?creationTime ?routingStrategy ?description
        ?aliasArn ?name ?aliasId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Properties that describe an alias resource. Related actions All APIs by task"]
module PlayerConnectionDetail =
  struct
    type nonrec t =
      {
      playerId: PlayerId.t option
        [@ocaml.doc
          "A unique identifier for a player associated with this connection."];
      endpoints: PlayerConnectionEndpointList.t option
        [@ocaml.doc
          "List of connection endpoints for the game client. Your game client uses these IP address(es) and port(s) to connect to the game session. When player gateway is enabled, these are relay endpoints with benefits such as DDoS protection. When disabled, this is the game server endpoint."];
      playerGatewayToken: MaxString.t option
        [@ocaml.doc
          "Access token that your game client must prepend to all traffic sent through player gateway. Player gateway verifies identity and authorizes connection based on this token. This value is empty when player gateway is disabled."];
      expiration: Timestamp.t option
        [@ocaml.doc
          "When player gateway is enabled, this is the timestamp indicating when player gateway token expires. Your game backend should call GetPlayerConnectionDetails to retrieve fresh connection information for your game clients before this time. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\"). This value is empty when player gateway is disabled."]}
    let make ?playerId =
      fun ?endpoints ->
        fun ?playerGatewayToken ->
          fun ?expiration ->
            fun () -> { playerId; endpoints; playerGatewayToken; expiration }
    let to_value x =
      structure_to_value
        [("PlayerId", (Option.map x.playerId ~f:PlayerId.to_value));
        ("Endpoints",
          (Option.map x.endpoints ~f:PlayerConnectionEndpointList.to_value));
        ("PlayerGatewayToken",
          (Option.map x.playerGatewayToken ~f:MaxString.to_value));
        ("Expiration", (Option.map x.expiration ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expiration =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "Expiration") in
      let playerGatewayToken =
        (Option.map ~f:MaxString.of_xml)
          (Xml.child xml_arg0 "PlayerGatewayToken") in
      let endpoints =
        (Option.map ~f:PlayerConnectionEndpointList.of_xml)
          (Xml.child xml_arg0 "Endpoints") in
      let playerId =
        (Option.map ~f:PlayerId.of_xml) (Xml.child xml_arg0 "PlayerId") in
      make ?expiration ?playerGatewayToken ?endpoints ?playerId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let expiration = field_map json__ "Expiration" Timestamp.of_json in
      let playerGatewayToken =
        field_map json__ "PlayerGatewayToken" MaxString.of_json in
      let endpoints =
        field_map json__ "Endpoints" PlayerConnectionEndpointList.of_json in
      let playerId = field_map json__ "PlayerId" PlayerId.of_json in
      make ?expiration ?playerGatewayToken ?endpoints ?playerId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Connection information for a game client to connect to a game session. This object contains the IP address(es), port(s), and authentication details your game client needs to establish a connection. With player gateway enabled: Contains relay endpoints and a player gateway token. Your game client must prepend player gateway token to each payload for validation and connection through relay endpoints. With player gateway disabled: Contains game server endpoint. Player gateway token and expiration fields are empty."]
module InstanceCredentials =
  struct
    type nonrec t =
      {
      userName: NonEmptyString.t option
        [@ocaml.doc "A user name for logging in."];
      secret: NonEmptyString.t option
        [@ocaml.doc
          "Secret string. For Windows instances, the secret is a password for use with Windows Remote Desktop. For Linux instances, it's a private key for use with SSH."]}
    let make ?userName = fun ?secret -> fun () -> { userName; secret }
    let to_value x =
      structure_to_value
        [("UserName", (Option.map x.userName ~f:NonEmptyString.to_value));
        ("Secret", (Option.map x.secret ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let secret =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Secret") in
      let userName =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "UserName") in
      make ?secret ?userName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let secret = field_map json__ "Secret" NonEmptyString.of_json in
      let userName = field_map json__ "UserName" NonEmptyString.of_json in
      make ?secret ?userName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A set of credentials that allow remote access to an instance in an EC2 managed fleet. These credentials are returned in response to a call to https://docs.aws.amazon.com/gamelift/latest/apireference/API_GetInstanceAccess, which requests access for instances that are running game servers with the Amazon GameLift Servers server SDK version 4.x or earlier."]
module ContainerIdentifier =
  struct
    type nonrec t =
      {
      containerName: NonZeroAnd128MaxAsciiString.t option
        [@ocaml.doc
          "The identifier for a container that's running in a compute."];
      containerRuntimeId: NonEmptyString.t option
        [@ocaml.doc
          "The runtime ID for the container that's running in a compute. This value is unique within the compute. It is returned as a ContainerAttribute value in a Compute object."]}
    let make ?containerName =
      fun ?containerRuntimeId ->
        fun () -> { containerName; containerRuntimeId }
    let to_value x =
      structure_to_value
        [("ContainerName",
           (Option.map x.containerName
              ~f:NonZeroAnd128MaxAsciiString.to_value));
        ("ContainerRuntimeId",
          (Option.map x.containerRuntimeId ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerRuntimeId =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "ContainerRuntimeId") in
      let containerName =
        (Option.map ~f:NonZeroAnd128MaxAsciiString.of_xml)
          (Xml.child xml_arg0 "ContainerName") in
      make ?containerRuntimeId ?containerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let containerRuntimeId =
        field_map json__ "ContainerRuntimeId" NonEmptyString.of_json in
      let containerName =
        field_map json__ "ContainerName" NonZeroAnd128MaxAsciiString.of_json in
      make ?containerRuntimeId ?containerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A unique identifier for a container in a compute on a managed container fleet instance. This information makes it possible to remotely connect to a specific container on a fleet instance. Related to: ContainerAttribute Use with: GetComputeAccess"]
module VpcPeeringConnection =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc
          "A unique identifier for the fleet. This ID determines the ID of the Amazon GameLift Servers VPC for your fleet."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the GameLift fleet resource for this connection."];
      ipV4CidrBlock: NonZeroAndMaxString.t option
        [@ocaml.doc
          "CIDR block of IPv4 addresses assigned to the VPC peering connection for the GameLift VPC. The peered VPC also has an IPv4 CIDR block associated with it; these blocks cannot overlap or the peering connection cannot be created."];
      vpcPeeringConnectionId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A unique identifier that is automatically assigned to the connection record. This ID is referenced in VPC peering connection events, and is used when deleting a connection."];
      status: VpcPeeringConnectionStatus.t option
        [@ocaml.doc
          "The status information about the connection. Status indicates if a connection is pending, successful, or failed."];
      peerVpcId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A unique identifier for a VPC with resources to be accessed by your Amazon GameLift Servers fleet. The VPC must be in the same Region as your fleet. To look up a VPC ID, use the VPC Dashboard in the Amazon Web Services Management Console. Learn more about VPC peering in VPC Peering with Amazon GameLift Servers Fleets."];
      gameLiftVpcId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A unique identifier for the VPC that contains the Amazon GameLift Servers fleet for this connection. This VPC is managed by Amazon GameLift Servers and does not appear in your Amazon Web Services account."]}
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?ipV4CidrBlock ->
          fun ?vpcPeeringConnectionId ->
            fun ?status ->
              fun ?peerVpcId ->
                fun ?gameLiftVpcId ->
                  fun () ->
                    {
                      fleetId;
                      fleetArn;
                      ipV4CidrBlock;
                      vpcPeeringConnectionId;
                      status;
                      peerVpcId;
                      gameLiftVpcId
                    }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("IpV4CidrBlock",
          (Option.map x.ipV4CidrBlock ~f:NonZeroAndMaxString.to_value));
        ("VpcPeeringConnectionId",
          (Option.map x.vpcPeeringConnectionId
             ~f:NonZeroAndMaxString.to_value));
        ("Status",
          (Option.map x.status ~f:VpcPeeringConnectionStatus.to_value));
        ("PeerVpcId",
          (Option.map x.peerVpcId ~f:NonZeroAndMaxString.to_value));
        ("GameLiftVpcId",
          (Option.map x.gameLiftVpcId ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameLiftVpcId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "GameLiftVpcId") in
      let peerVpcId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "PeerVpcId") in
      let status =
        (Option.map ~f:VpcPeeringConnectionStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let vpcPeeringConnectionId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "VpcPeeringConnectionId") in
      let ipV4CidrBlock =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "IpV4CidrBlock") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?gameLiftVpcId ?peerVpcId ?status ?vpcPeeringConnectionId
        ?ipV4CidrBlock ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameLiftVpcId =
        field_map json__ "GameLiftVpcId" NonZeroAndMaxString.of_json in
      let peerVpcId =
        field_map json__ "PeerVpcId" NonZeroAndMaxString.of_json in
      let status =
        field_map json__ "Status" VpcPeeringConnectionStatus.of_json in
      let vpcPeeringConnectionId =
        field_map json__ "VpcPeeringConnectionId" NonZeroAndMaxString.of_json in
      let ipV4CidrBlock =
        field_map json__ "IpV4CidrBlock" NonZeroAndMaxString.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?gameLiftVpcId ?peerVpcId ?status ?vpcPeeringConnectionId
        ?ipV4CidrBlock ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a peering connection between a VPC on one of your Amazon Web Services accounts and the VPC for your Amazon GameLift Servers fleets. This record may be for an active peering connection or a pending connection that has not yet been established. Related actions All APIs by task"]
module VpcPeeringAuthorization =
  struct
    type nonrec t =
      {
      gameLiftAwsAccountId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A unique identifier for the Amazon Web Services account that you use to manage your Amazon GameLift Servers fleet. You can find your Account ID in the Amazon Web Services Management Console under account settings."];
      peerVpcAwsAccountId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "The authorization's peer VPC Amazon Web Services account ID."];
      peerVpcId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A unique identifier for a VPC with resources to be accessed by your Amazon GameLift Servers fleet. The VPC must be in the same Region as your fleet. To look up a VPC ID, use the VPC Dashboard in the Amazon Web Services Management Console. Learn more about VPC peering in VPC Peering with Amazon GameLift Servers Fleets."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "Time stamp indicating when this authorization was issued. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      expirationTime: Timestamp.t option
        [@ocaml.doc
          "Time stamp indicating when this authorization expires (24 hours after issuance). Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."]}
    let make ?gameLiftAwsAccountId =
      fun ?peerVpcAwsAccountId ->
        fun ?peerVpcId ->
          fun ?creationTime ->
            fun ?expirationTime ->
              fun () ->
                {
                  gameLiftAwsAccountId;
                  peerVpcAwsAccountId;
                  peerVpcId;
                  creationTime;
                  expirationTime
                }
    let to_value x =
      structure_to_value
        [("GameLiftAwsAccountId",
           (Option.map x.gameLiftAwsAccountId ~f:NonZeroAndMaxString.to_value));
        ("PeerVpcAwsAccountId",
          (Option.map x.peerVpcAwsAccountId ~f:NonZeroAndMaxString.to_value));
        ("PeerVpcId",
          (Option.map x.peerVpcId ~f:NonZeroAndMaxString.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("ExpirationTime",
          (Option.map x.expirationTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expirationTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "ExpirationTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let peerVpcId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "PeerVpcId") in
      let peerVpcAwsAccountId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "PeerVpcAwsAccountId") in
      let gameLiftAwsAccountId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "GameLiftAwsAccountId") in
      make ?expirationTime ?creationTime ?peerVpcId ?peerVpcAwsAccountId
        ?gameLiftAwsAccountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let expirationTime =
        field_map json__ "ExpirationTime" Timestamp.of_json in
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let peerVpcId =
        field_map json__ "PeerVpcId" NonZeroAndMaxString.of_json in
      let peerVpcAwsAccountId =
        field_map json__ "PeerVpcAwsAccountId" NonZeroAndMaxString.of_json in
      let gameLiftAwsAccountId =
        field_map json__ "GameLiftAwsAccountId" NonZeroAndMaxString.of_json in
      make ?expirationTime ?creationTime ?peerVpcId ?peerVpcAwsAccountId
        ?gameLiftAwsAccountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents an authorization for a VPC peering connection between the VPC for an Amazon GameLift Servers fleet and another VPC on an account you have access to. This authorization must exist and be valid for the peering connection to be established. Authorizations are valid for 24 hours after they are issued. Related actions All APIs by task"]
module ScalingPolicy =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc
          "A unique identifier for the fleet that is associated with this scaling policy."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      name: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a fleet's scaling policy. Policy names do not need to be unique."];
      status: ScalingStatusType.t option
        [@ocaml.doc
          "Current status of the scaling policy. The scaling policy can be in force only when in an ACTIVE status. Scaling policies can be suspended for individual fleets. If the policy is suspended for a fleet, the policy status does not change. ACTIVE -- The scaling policy can be used for auto-scaling a fleet. UPDATE_REQUESTED -- A request to update the scaling policy has been received. UPDATING -- A change is being made to the scaling policy. DELETE_REQUESTED -- A request to delete the scaling policy has been received. DELETING -- The scaling policy is being deleted. DELETED -- The scaling policy has been deleted. ERROR -- An error occurred in creating the policy. It should be removed and recreated."];
      scalingAdjustment: Integer.t option
        [@ocaml.doc
          "Amount of adjustment to make, based on the scaling adjustment type."];
      scalingAdjustmentType: ScalingAdjustmentType.t option
        [@ocaml.doc
          "The type of adjustment to make to a fleet's instance count. ChangeInCapacity -- add (or subtract) the scaling adjustment value from the current instance count. Positive values scale up while negative values scale down. ExactCapacity -- set the instance count to the scaling adjustment value. PercentChangeInCapacity -- increase or reduce the current instance count by the scaling adjustment, read as a percentage. Positive values scale up while negative values scale down."];
      comparisonOperator: ComparisonOperatorType.t option
        [@ocaml.doc
          "Comparison operator to use when measuring a metric against the threshold value."];
      threshold: Double.t option
        [@ocaml.doc "Metric value used to trigger a scaling event."];
      evaluationPeriods: PositiveInteger.t option
        [@ocaml.doc
          "Length of time (in minutes) the metric must be at or beyond the threshold before a scaling event is triggered."];
      metricName: MetricName.t option
        [@ocaml.doc
          "Name of the Amazon GameLift Servers-defined metric that is used to trigger a scaling adjustment. For detailed descriptions of fleet metrics, see Monitor Amazon GameLift Servers with Amazon CloudWatch. ActivatingGameSessions -- Game sessions in the process of being created. ActiveGameSessions -- Game sessions that are currently running. ActiveInstances -- Fleet instances that are currently running at least one game session. AvailableGameSessions -- Additional game sessions that fleet could host simultaneously, given current capacity. AvailablePlayerSessions -- Empty player slots in currently active game sessions. This includes game sessions that are not currently accepting players. Reserved player slots are not included. CurrentPlayerSessions -- Player slots in active game sessions that are being used by a player or are reserved for a player. IdleInstances -- Active instances that are currently hosting zero game sessions. PercentAvailableGameSessions -- Unused percentage of the total number of game sessions that a fleet could host simultaneously, given current capacity. Use this metric for a target-based scaling policy. PercentIdleInstances -- Percentage of the total number of active instances that are hosting zero game sessions. QueueDepth -- Pending game session placement requests, in any queue, where the current fleet is the top-priority destination. WaitTime -- Current wait time for pending game session placement requests, in any queue, where the current fleet is the top-priority destination."];
      policyType: PolicyType.t option
        [@ocaml.doc
          "The type of scaling policy to create. For a target-based policy, set the parameter MetricName to 'PercentAvailableGameSessions' and specify a TargetConfiguration. For a rule-based policy set the following parameters: MetricName, ComparisonOperator, Threshold, EvaluationPeriods, ScalingAdjustmentType, and ScalingAdjustment."];
      targetConfiguration: TargetConfiguration.t option
        [@ocaml.doc
          "An object that contains settings for a target-based scaling policy."];
      updateStatus: LocationUpdateStatus.t option
        [@ocaml.doc
          "The current status of the fleet's scaling policies in a requested fleet location. The status PENDING_UPDATE indicates that an update was requested for the fleet but has not yet been completed for the location."];
      location: LocationStringModel.t option
        [@ocaml.doc "The fleet location."]}
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?name ->
          fun ?status ->
            fun ?scalingAdjustment ->
              fun ?scalingAdjustmentType ->
                fun ?comparisonOperator ->
                  fun ?threshold ->
                    fun ?evaluationPeriods ->
                      fun ?metricName ->
                        fun ?policyType ->
                          fun ?targetConfiguration ->
                            fun ?updateStatus ->
                              fun ?location ->
                                fun () ->
                                  {
                                    fleetId;
                                    fleetArn;
                                    name;
                                    status;
                                    scalingAdjustment;
                                    scalingAdjustmentType;
                                    comparisonOperator;
                                    threshold;
                                    evaluationPeriods;
                                    metricName;
                                    policyType;
                                    targetConfiguration;
                                    updateStatus;
                                    location
                                  }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("Name", (Option.map x.name ~f:NonZeroAndMaxString.to_value));
        ("Status", (Option.map x.status ~f:ScalingStatusType.to_value));
        ("ScalingAdjustment",
          (Option.map x.scalingAdjustment ~f:Integer.to_value));
        ("ScalingAdjustmentType",
          (Option.map x.scalingAdjustmentType
             ~f:ScalingAdjustmentType.to_value));
        ("ComparisonOperator",
          (Option.map x.comparisonOperator ~f:ComparisonOperatorType.to_value));
        ("Threshold", (Option.map x.threshold ~f:Double.to_value));
        ("EvaluationPeriods",
          (Option.map x.evaluationPeriods ~f:PositiveInteger.to_value));
        ("MetricName", (Option.map x.metricName ~f:MetricName.to_value));
        ("PolicyType", (Option.map x.policyType ~f:PolicyType.to_value));
        ("TargetConfiguration",
          (Option.map x.targetConfiguration ~f:TargetConfiguration.to_value));
        ("UpdateStatus",
          (Option.map x.updateStatus ~f:LocationUpdateStatus.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let updateStatus =
        (Option.map ~f:LocationUpdateStatus.of_xml)
          (Xml.child xml_arg0 "UpdateStatus") in
      let targetConfiguration =
        (Option.map ~f:TargetConfiguration.of_xml)
          (Xml.child xml_arg0 "TargetConfiguration") in
      let policyType =
        (Option.map ~f:PolicyType.of_xml) (Xml.child xml_arg0 "PolicyType") in
      let metricName =
        (Option.map ~f:MetricName.of_xml) (Xml.child xml_arg0 "MetricName") in
      let evaluationPeriods =
        (Option.map ~f:PositiveInteger.of_xml)
          (Xml.child xml_arg0 "EvaluationPeriods") in
      let threshold =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "Threshold") in
      let comparisonOperator =
        (Option.map ~f:ComparisonOperatorType.of_xml)
          (Xml.child xml_arg0 "ComparisonOperator") in
      let scalingAdjustmentType =
        (Option.map ~f:ScalingAdjustmentType.of_xml)
          (Xml.child xml_arg0 "ScalingAdjustmentType") in
      let scalingAdjustment =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ScalingAdjustment") in
      let status =
        (Option.map ~f:ScalingStatusType.of_xml)
          (Xml.child xml_arg0 "Status") in
      let name =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Name") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?location ?updateStatus ?targetConfiguration ?policyType
        ?metricName ?evaluationPeriods ?threshold ?comparisonOperator
        ?scalingAdjustmentType ?scalingAdjustment ?status ?name ?fleetArn
        ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let updateStatus =
        field_map json__ "UpdateStatus" LocationUpdateStatus.of_json in
      let targetConfiguration =
        field_map json__ "TargetConfiguration" TargetConfiguration.of_json in
      let policyType = field_map json__ "PolicyType" PolicyType.of_json in
      let metricName = field_map json__ "MetricName" MetricName.of_json in
      let evaluationPeriods =
        field_map json__ "EvaluationPeriods" PositiveInteger.of_json in
      let threshold = field_map json__ "Threshold" Double.of_json in
      let comparisonOperator =
        field_map json__ "ComparisonOperator" ComparisonOperatorType.of_json in
      let scalingAdjustmentType =
        field_map json__ "ScalingAdjustmentType"
          ScalingAdjustmentType.of_json in
      let scalingAdjustment =
        field_map json__ "ScalingAdjustment" Integer.of_json in
      let status = field_map json__ "Status" ScalingStatusType.of_json in
      let name = field_map json__ "Name" NonZeroAndMaxString.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?location ?updateStatus ?targetConfiguration ?policyType
        ?metricName ?evaluationPeriods ?threshold ?comparisonOperator
        ?scalingAdjustmentType ?scalingAdjustment ?status ?name ?fleetArn
        ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Rule that controls how a fleet is scaled. Scaling policies are uniquely identified by the combination of name and fleet ID."]
module PlayerSession =
  struct
    type nonrec t =
      {
      playerSessionId: PlayerSessionId.t option
        [@ocaml.doc "A unique identifier for a player session."];
      playerId: PlayerId.t option
        [@ocaml.doc
          "A unique identifier for a player that is associated with this player session."];
      gameSessionId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions that the player session is connected to. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>."];
      fleetId: FleetId.t option
        [@ocaml.doc
          "A unique identifier for the fleet that the player's game session is running on."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the GameLift fleet that the player's game session is running on."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      terminationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was terminated. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      status: PlayerSessionStatus.t option
        [@ocaml.doc
          "Current status of the player session. Possible player session statuses include the following: RESERVED -- The player session request has been received, but the player has not yet connected to the server process and/or been validated. ACTIVE -- The player has been validated by the server process and is currently connected. COMPLETED -- The player connection has been dropped. TIMEDOUT -- A player session request was received, but the player did not connect and/or was not validated within the timeout limit (60 seconds)."];
      ipAddress: IpAddress.t option
        [@ocaml.doc
          "The IP address of the game session. To connect to a Amazon GameLift Servers game server, an app needs both the IP address and port number."];
      dnsName: DnsName.t option
        [@ocaml.doc
          "The DNS identifier assigned to the instance that is running the game session. Values have the following format: TLS-enabled fleets: <unique identifier>.<region identifier>.amazongamelift.com. Non-TLS-enabled fleets: ec2-<unique identifier>.compute.amazonaws.com. (See Amazon EC2 Instance IP Addressing.) When connecting to a game session that is running on a TLS-enabled fleet, you must use the DNS name, not the IP address."];
      port: PortNumber.t option
        [@ocaml.doc
          "Port number for the game session. To connect to a Amazon GameLift Servers server process, an app needs both the IP address and port number."];
      playerData: PlayerData.t option
        [@ocaml.doc
          "Developer-defined information related to a player. Amazon GameLift Servers does not use this data, so it can be formatted as needed for use in the game."]}
    let make ?playerSessionId =
      fun ?playerId ->
        fun ?gameSessionId ->
          fun ?fleetId ->
            fun ?fleetArn ->
              fun ?creationTime ->
                fun ?terminationTime ->
                  fun ?status ->
                    fun ?ipAddress ->
                      fun ?dnsName ->
                        fun ?port ->
                          fun ?playerData ->
                            fun () ->
                              {
                                playerSessionId;
                                playerId;
                                gameSessionId;
                                fleetId;
                                fleetArn;
                                creationTime;
                                terminationTime;
                                status;
                                ipAddress;
                                dnsName;
                                port;
                                playerData
                              }
    let to_value x =
      structure_to_value
        [("PlayerSessionId",
           (Option.map x.playerSessionId ~f:PlayerSessionId.to_value));
        ("PlayerId", (Option.map x.playerId ~f:PlayerId.to_value));
        ("GameSessionId",
          (Option.map x.gameSessionId ~f:NonZeroAndMaxString.to_value));
        ("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("TerminationTime",
          (Option.map x.terminationTime ~f:Timestamp.to_value));
        ("Status", (Option.map x.status ~f:PlayerSessionStatus.to_value));
        ("IpAddress", (Option.map x.ipAddress ~f:IpAddress.to_value));
        ("DnsName", (Option.map x.dnsName ~f:DnsName.to_value));
        ("Port", (Option.map x.port ~f:PortNumber.to_value));
        ("PlayerData", (Option.map x.playerData ~f:PlayerData.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerData =
        (Option.map ~f:PlayerData.of_xml) (Xml.child xml_arg0 "PlayerData") in
      let port =
        (Option.map ~f:PortNumber.of_xml) (Xml.child xml_arg0 "Port") in
      let dnsName =
        (Option.map ~f:DnsName.of_xml) (Xml.child xml_arg0 "DnsName") in
      let ipAddress =
        (Option.map ~f:IpAddress.of_xml) (Xml.child xml_arg0 "IpAddress") in
      let status =
        (Option.map ~f:PlayerSessionStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let terminationTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "TerminationTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      let gameSessionId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "GameSessionId") in
      let playerId =
        (Option.map ~f:PlayerId.of_xml) (Xml.child xml_arg0 "PlayerId") in
      let playerSessionId =
        (Option.map ~f:PlayerSessionId.of_xml)
          (Xml.child xml_arg0 "PlayerSessionId") in
      make ?playerData ?port ?dnsName ?ipAddress ?status ?terminationTime
        ?creationTime ?fleetArn ?fleetId ?gameSessionId ?playerId
        ?playerSessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerData = field_map json__ "PlayerData" PlayerData.of_json in
      let port = field_map json__ "Port" PortNumber.of_json in
      let dnsName = field_map json__ "DnsName" DnsName.of_json in
      let ipAddress = field_map json__ "IpAddress" IpAddress.of_json in
      let status = field_map json__ "Status" PlayerSessionStatus.of_json in
      let terminationTime =
        field_map json__ "TerminationTime" Timestamp.of_json in
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      let gameSessionId =
        field_map json__ "GameSessionId" NonZeroAndMaxString.of_json in
      let playerId = field_map json__ "PlayerId" PlayerId.of_json in
      let playerSessionId =
        field_map json__ "PlayerSessionId" PlayerSessionId.of_json in
      make ?playerData ?port ?dnsName ?ipAddress ?status ?terminationTime
        ?creationTime ?fleetArn ?fleetId ?gameSessionId ?playerId
        ?playerSessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a player session. Player sessions are created either for a specific game session, or as part of a game session placement or matchmaking request. A player session can represents a reserved player slot in a game session (when status is RESERVED) or actual player activity in a game session (when status is ACTIVE). A player session object, including player data, is automatically passed to a game session when the player connects to the game session and is validated. After the game session ends, player sessions information is retained for 30 days and then removed. Related actions All APIs by task"]
module MatchmakingRuleSet =
  struct
    type nonrec t =
      {
      ruleSetName: MatchmakingIdStringModel.t option
        [@ocaml.doc "A unique identifier for the matchmaking rule set"];
      ruleSetArn: MatchmakingRuleSetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers matchmaking rule set resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::matchmakingruleset/<ruleset name>. In a GameLift rule set ARN, the resource ID matches the RuleSetName value."];
      ruleSetBody: RuleSetBody.t option
        [@ocaml.doc
          "A collection of matchmaking rules, formatted as a JSON string. Comments are not allowed in JSON, but most elements support a description field."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."]}
    let make ?ruleSetName =
      fun ?ruleSetArn ->
        fun ?ruleSetBody ->
          fun ?creationTime ->
            fun () -> { ruleSetName; ruleSetArn; ruleSetBody; creationTime }
    let to_value x =
      structure_to_value
        [("RuleSetName",
           (Option.map x.ruleSetName ~f:MatchmakingIdStringModel.to_value));
        ("RuleSetArn",
          (Option.map x.ruleSetArn ~f:MatchmakingRuleSetArn.to_value));
        ("RuleSetBody", (Option.map x.ruleSetBody ~f:RuleSetBody.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let ruleSetBody =
        (Option.map ~f:RuleSetBody.of_xml) (Xml.child xml_arg0 "RuleSetBody") in
      let ruleSetArn =
        (Option.map ~f:MatchmakingRuleSetArn.of_xml)
          (Xml.child xml_arg0 "RuleSetArn") in
      let ruleSetName =
        (Option.map ~f:MatchmakingIdStringModel.of_xml)
          (Xml.child xml_arg0 "RuleSetName") in
      make ?creationTime ?ruleSetBody ?ruleSetArn ?ruleSetName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let ruleSetBody = field_map json__ "RuleSetBody" RuleSetBody.of_json in
      let ruleSetArn =
        field_map json__ "RuleSetArn" MatchmakingRuleSetArn.of_json in
      let ruleSetName =
        field_map json__ "RuleSetName" MatchmakingIdStringModel.of_json in
      make ?creationTime ?ruleSetBody ?ruleSetArn ?ruleSetName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Set of rule statements, used with FlexMatch, that determine how to build your player matches. Each rule set describes a type of group to be created and defines the parameters for acceptable player matches. A rule set may define the following elements for a match. For detailed information and examples showing how to construct a rule set, see Build a FlexMatch rule set. Teams -- Required. A rule set must define one or multiple teams for the match and set minimum and maximum team sizes. For example, a rule set might describe a 4x4 match that requires all eight slots to be filled. Player attributes -- Optional. These attributes specify a set of player characteristics to evaluate when looking for a match. Matchmaking requests that use a rule set with player attributes must provide the corresponding attribute values. For example, an attribute might specify a player's skill or level. Rules -- Optional. Rules define how to evaluate potential players for a match based on player attributes. A rule might specify minimum requirements for individual players, teams, or entire matches. For example, a rule might require each player to meet a certain skill level, each team to have at least one player in a certain role, or the match to have a minimum average skill level. or may describe an entire group--such as all teams must be evenly matched or have at least one player in a certain role. Expansions -- Optional. Expansions allow you to relax the rules after a period of time when no acceptable matches are found. This feature lets you balance getting players into games in a reasonable amount of time instead of making them wait indefinitely for the best possible match. For example, you might use an expansion to increase the maximum skill variance between players after 30 seconds."]
module MatchmakingRuleSetName =
  struct
    type nonrec t = string
    let context_ = "MatchmakingRuleSetName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[a-zA-Z0-9-\\.]*|^arn:.*:matchmakingruleset\\/[a-zA-Z0-9-\\.]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MatchmakingRuleSetName" j
    let to_json = simple_to_json to_value
  end
module MatchmakingTicket =
  struct
    type nonrec t =
      {
      ticketId: MatchmakingIdStringModel.t option
        [@ocaml.doc "A unique identifier for a matchmaking ticket."];
      configurationName: MatchmakingIdStringModel.t option
        [@ocaml.doc
          "Name of the matchmaking configuration that is used with this ticket. Matchmaking configurations determine how players are grouped into a match and how a new game session is created for the match."];
      configurationArn: MatchmakingConfigurationArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the GameLift matchmaking configuration resource that is used with this ticket."];
      status: MatchmakingConfigurationStatus.t option
        [@ocaml.doc
          "Current status of the matchmaking request. QUEUED -- The matchmaking request has been received and is currently waiting to be processed. SEARCHING -- The matchmaking request is currently being processed. REQUIRES_ACCEPTANCE -- A match has been proposed and the players must accept the match. This status is used only with requests that use a matchmaking configuration with a player acceptance requirement. PLACING -- The FlexMatch engine has matched players and is in the process of placing a new game session for the match. COMPLETED -- Players have been matched and a game session is ready to host the players. A ticket in this state contains the necessary connection information for players. FAILED -- The matchmaking request was not completed. CANCELLED -- The matchmaking request was canceled. This may be the result of a StopMatchmaking operation or a proposed match that one or more players failed to accept. TIMED_OUT -- The matchmaking request was not successful within the duration specified in the matchmaking configuration. Matchmaking requests that fail to successfully complete (statuses FAILED, CANCELLED, TIMED_OUT) can be resubmitted as new requests with new ticket IDs."];
      statusReason: StringModel.t option
        [@ocaml.doc
          "Code to explain the current status. For example, a status reason may indicate when a ticket has returned to SEARCHING status after a proposed match fails to receive player acceptances."];
      statusMessage: StringModel.t option
        [@ocaml.doc "Additional information about the current status."];
      startTime: Timestamp.t option
        [@ocaml.doc
          "Time stamp indicating when this matchmaking request was received. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "Time stamp indicating when the matchmaking request stopped being processed due to successful completion, timeout, or cancellation. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      players: PlayerList.t option
        [@ocaml.doc
          "A set of Player objects, each representing a player to find matches for. Players are identified by a unique player ID and may include latency data for use during matchmaking. If the ticket is in status COMPLETED, the Player objects include the team the players were assigned to in the resulting match."];
      gameSessionConnectionInfo: GameSessionConnectionInfo.t option
        [@ocaml.doc
          "Connection information for a new game session. Once a match is made, the FlexMatch engine creates a new game session for it. This information is added to the matchmaking ticket, which you can be retrieve by calling DescribeMatchmaking ."];
      estimatedWaitTime: WholeNumber.t option
        [@ocaml.doc
          "Average amount of time (in seconds) that players are currently waiting for a match. If there is not enough recent data, this property may be empty."]}
    let make ?ticketId =
      fun ?configurationName ->
        fun ?configurationArn ->
          fun ?status ->
            fun ?statusReason ->
              fun ?statusMessage ->
                fun ?startTime ->
                  fun ?endTime ->
                    fun ?players ->
                      fun ?gameSessionConnectionInfo ->
                        fun ?estimatedWaitTime ->
                          fun () ->
                            {
                              ticketId;
                              configurationName;
                              configurationArn;
                              status;
                              statusReason;
                              statusMessage;
                              startTime;
                              endTime;
                              players;
                              gameSessionConnectionInfo;
                              estimatedWaitTime
                            }
    let to_value x =
      structure_to_value
        [("TicketId",
           (Option.map x.ticketId ~f:MatchmakingIdStringModel.to_value));
        ("ConfigurationName",
          (Option.map x.configurationName
             ~f:MatchmakingIdStringModel.to_value));
        ("ConfigurationArn",
          (Option.map x.configurationArn
             ~f:MatchmakingConfigurationArn.to_value));
        ("Status",
          (Option.map x.status ~f:MatchmakingConfigurationStatus.to_value));
        ("StatusReason", (Option.map x.statusReason ~f:StringModel.to_value));
        ("StatusMessage",
          (Option.map x.statusMessage ~f:StringModel.to_value));
        ("StartTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("EndTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("Players", (Option.map x.players ~f:PlayerList.to_value));
        ("GameSessionConnectionInfo",
          (Option.map x.gameSessionConnectionInfo
             ~f:GameSessionConnectionInfo.to_value));
        ("EstimatedWaitTime",
          (Option.map x.estimatedWaitTime ~f:WholeNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let estimatedWaitTime =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "EstimatedWaitTime") in
      let gameSessionConnectionInfo =
        (Option.map ~f:GameSessionConnectionInfo.of_xml)
          (Xml.child xml_arg0 "GameSessionConnectionInfo") in
      let players =
        (Option.map ~f:PlayerList.of_xml) (Xml.child xml_arg0 "Players") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "StartTime") in
      let statusMessage =
        (Option.map ~f:StringModel.of_xml)
          (Xml.child xml_arg0 "StatusMessage") in
      let statusReason =
        (Option.map ~f:StringModel.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:MatchmakingConfigurationStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let configurationArn =
        (Option.map ~f:MatchmakingConfigurationArn.of_xml)
          (Xml.child xml_arg0 "ConfigurationArn") in
      let configurationName =
        (Option.map ~f:MatchmakingIdStringModel.of_xml)
          (Xml.child xml_arg0 "ConfigurationName") in
      let ticketId =
        (Option.map ~f:MatchmakingIdStringModel.of_xml)
          (Xml.child xml_arg0 "TicketId") in
      make ?estimatedWaitTime ?gameSessionConnectionInfo ?players ?endTime
        ?startTime ?statusMessage ?statusReason ?status ?configurationArn
        ?configurationName ?ticketId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let estimatedWaitTime =
        field_map json__ "EstimatedWaitTime" WholeNumber.of_json in
      let gameSessionConnectionInfo =
        field_map json__ "GameSessionConnectionInfo"
          GameSessionConnectionInfo.of_json in
      let players = field_map json__ "Players" PlayerList.of_json in
      let endTime = field_map json__ "EndTime" Timestamp.of_json in
      let startTime = field_map json__ "StartTime" Timestamp.of_json in
      let statusMessage =
        field_map json__ "StatusMessage" StringModel.of_json in
      let statusReason = field_map json__ "StatusReason" StringModel.of_json in
      let status =
        field_map json__ "Status" MatchmakingConfigurationStatus.of_json in
      let configurationArn =
        field_map json__ "ConfigurationArn"
          MatchmakingConfigurationArn.of_json in
      let configurationName =
        field_map json__ "ConfigurationName" MatchmakingIdStringModel.of_json in
      let ticketId =
        field_map json__ "TicketId" MatchmakingIdStringModel.of_json in
      make ?estimatedWaitTime ?gameSessionConnectionInfo ?players ?endTime
        ?startTime ?statusMessage ?statusReason ?status ?configurationArn
        ?configurationName ?ticketId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Ticket generated to track the progress of a matchmaking request. Each ticket is uniquely identified by a ticket ID, supplied by the requester, when creating a matchmaking request."]
module MatchmakingConfiguration =
  struct
    type nonrec t =
      {
      name: MatchmakingIdStringModel.t option
        [@ocaml.doc
          "A unique identifier for the matchmaking configuration. This name is used to identify the configuration associated with a matchmaking request or ticket."];
      configurationArn: MatchmakingConfigurationArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers matchmaking configuration resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::matchmakingconfiguration/<matchmaking configuration name>. In a Amazon GameLift Servers configuration ARN, the resource ID matches the Name value."];
      description: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with matchmaking configuration."];
      gameSessionQueueArns: QueueArnsList.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers game session queue resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::gamesessionqueue/<queue name>. Queues can be located in any Region. Queues are used to start new Amazon GameLift Servers-hosted game sessions for matches that are created with this matchmaking configuration. This property is not set when FlexMatchMode is set to STANDALONE."];
      requestTimeoutSeconds: MatchmakingRequestTimeoutInteger.t option
        [@ocaml.doc
          "The maximum duration, in seconds, that a matchmaking ticket can remain in process before timing out. Requests that fail due to timing out can be resubmitted as needed."];
      acceptanceTimeoutSeconds: MatchmakingAcceptanceTimeoutInteger.t option
        [@ocaml.doc
          "The length of time (in seconds) to wait for players to accept a proposed match, if acceptance is required. If any player rejects the match or fails to accept before the timeout, the ticket continues to look for an acceptable match."];
      acceptanceRequired: BooleanModel.t option
        [@ocaml.doc
          "A flag that indicates whether a match that was created with this configuration must be accepted by the matched players. To require acceptance, set to TRUE. When this option is enabled, matchmaking tickets use the status REQUIRES_ACCEPTANCE to indicate when a completed potential match is waiting for player acceptance."];
      ruleSetName: MatchmakingIdStringModel.t option
        [@ocaml.doc
          "A unique identifier for the matchmaking rule set to use with this configuration. A matchmaking configuration can only use rule sets that are defined in the same Region."];
      ruleSetArn: MatchmakingRuleSetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the GameLift matchmaking rule set resource that this configuration uses."];
      notificationTarget: SnsArnStringModel.t option
        [@ocaml.doc
          "An SNS topic ARN that is set up to receive matchmaking notifications."];
      additionalPlayerCount: WholeNumber.t option
        [@ocaml.doc
          "The number of player slots in a match to keep open for future players. For example, if the configuration's rule set specifies a match for a single 12-person team, and the additional player count is set to 2, only 10 players are selected for the match. This parameter is not used when FlexMatchMode is set to STANDALONE."];
      customEventData: CustomEventData.t option
        [@ocaml.doc
          "Information to attach to all events related to the matchmaking configuration."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      gameProperties: GamePropertyList.t option
        [@ocaml.doc
          "A set of key-value pairs that can store custom data in a game session. For example: \\{\"Key\": \"difficulty\", \"Value\": \"novice\"\\}. This information is added to the new GameSession object that is created for a successful match. This parameter is not used when FlexMatchMode is set to STANDALONE. Avoid using periods (\".\") in property keys if you plan to search for game sessions by properties. Property keys containing periods cannot be searched and will be filtered out from search results due to search index limitations. If you use SearchGameSessions API, there is a limit of 500 game property keys across all game sessions and all fleets per region. If the limit is exceeded, there will potentially be game session entries missing from SearchGameSessions API results."];
      gameSessionData: GameSessionData.t option
        [@ocaml.doc
          "A set of custom game session properties, formatted as a single string value. This data is passed to a game server process with a request to start a new game session. For more information, see Start a game session. This information is added to the new GameSession object that is created for a successful match. This parameter is not used when FlexMatchMode is set to STANDALONE."];
      backfillMode: BackfillMode.t option
        [@ocaml.doc
          "The method used to backfill game sessions created with this matchmaking configuration. MANUAL indicates that the game makes backfill requests or does not use the match backfill feature. AUTOMATIC indicates that GameLift creates backfill requests whenever a game session has one or more open slots. Learn more about manual and automatic backfill in Backfill existing games with FlexMatch. Automatic backfill is not available when FlexMatchMode is set to STANDALONE."];
      flexMatchMode: FlexMatchMode.t option
        [@ocaml.doc
          "Indicates whether this matchmaking configuration is being used with Amazon GameLift Servers hosting or as a standalone matchmaking solution. STANDALONE - FlexMatch forms matches and returns match information, including players and team assignments, in a MatchmakingSucceeded event. WITH_QUEUE - FlexMatch forms matches and uses the specified Amazon GameLift Servers queue to start a game session for the match."]}
    let make ?name =
      fun ?configurationArn ->
        fun ?description ->
          fun ?gameSessionQueueArns ->
            fun ?requestTimeoutSeconds ->
              fun ?acceptanceTimeoutSeconds ->
                fun ?acceptanceRequired ->
                  fun ?ruleSetName ->
                    fun ?ruleSetArn ->
                      fun ?notificationTarget ->
                        fun ?additionalPlayerCount ->
                          fun ?customEventData ->
                            fun ?creationTime ->
                              fun ?gameProperties ->
                                fun ?gameSessionData ->
                                  fun ?backfillMode ->
                                    fun ?flexMatchMode ->
                                      fun () ->
                                        {
                                          name;
                                          configurationArn;
                                          description;
                                          gameSessionQueueArns;
                                          requestTimeoutSeconds;
                                          acceptanceTimeoutSeconds;
                                          acceptanceRequired;
                                          ruleSetName;
                                          ruleSetArn;
                                          notificationTarget;
                                          additionalPlayerCount;
                                          customEventData;
                                          creationTime;
                                          gameProperties;
                                          gameSessionData;
                                          backfillMode;
                                          flexMatchMode
                                        }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:MatchmakingIdStringModel.to_value));
        ("ConfigurationArn",
          (Option.map x.configurationArn
             ~f:MatchmakingConfigurationArn.to_value));
        ("Description",
          (Option.map x.description ~f:NonZeroAndMaxString.to_value));
        ("GameSessionQueueArns",
          (Option.map x.gameSessionQueueArns ~f:QueueArnsList.to_value));
        ("RequestTimeoutSeconds",
          (Option.map x.requestTimeoutSeconds
             ~f:MatchmakingRequestTimeoutInteger.to_value));
        ("AcceptanceTimeoutSeconds",
          (Option.map x.acceptanceTimeoutSeconds
             ~f:MatchmakingAcceptanceTimeoutInteger.to_value));
        ("AcceptanceRequired",
          (Option.map x.acceptanceRequired ~f:BooleanModel.to_value));
        ("RuleSetName",
          (Option.map x.ruleSetName ~f:MatchmakingIdStringModel.to_value));
        ("RuleSetArn",
          (Option.map x.ruleSetArn ~f:MatchmakingRuleSetArn.to_value));
        ("NotificationTarget",
          (Option.map x.notificationTarget ~f:SnsArnStringModel.to_value));
        ("AdditionalPlayerCount",
          (Option.map x.additionalPlayerCount ~f:WholeNumber.to_value));
        ("CustomEventData",
          (Option.map x.customEventData ~f:CustomEventData.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("GameProperties",
          (Option.map x.gameProperties ~f:GamePropertyList.to_value));
        ("GameSessionData",
          (Option.map x.gameSessionData ~f:GameSessionData.to_value));
        ("BackfillMode",
          (Option.map x.backfillMode ~f:BackfillMode.to_value));
        ("FlexMatchMode",
          (Option.map x.flexMatchMode ~f:FlexMatchMode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let flexMatchMode =
        (Option.map ~f:FlexMatchMode.of_xml)
          (Xml.child xml_arg0 "FlexMatchMode") in
      let backfillMode =
        (Option.map ~f:BackfillMode.of_xml)
          (Xml.child xml_arg0 "BackfillMode") in
      let gameSessionData =
        (Option.map ~f:GameSessionData.of_xml)
          (Xml.child xml_arg0 "GameSessionData") in
      let gameProperties =
        (Option.map ~f:GamePropertyList.of_xml)
          (Xml.child xml_arg0 "GameProperties") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let customEventData =
        (Option.map ~f:CustomEventData.of_xml)
          (Xml.child xml_arg0 "CustomEventData") in
      let additionalPlayerCount =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "AdditionalPlayerCount") in
      let notificationTarget =
        (Option.map ~f:SnsArnStringModel.of_xml)
          (Xml.child xml_arg0 "NotificationTarget") in
      let ruleSetArn =
        (Option.map ~f:MatchmakingRuleSetArn.of_xml)
          (Xml.child xml_arg0 "RuleSetArn") in
      let ruleSetName =
        (Option.map ~f:MatchmakingIdStringModel.of_xml)
          (Xml.child xml_arg0 "RuleSetName") in
      let acceptanceRequired =
        (Option.map ~f:BooleanModel.of_xml)
          (Xml.child xml_arg0 "AcceptanceRequired") in
      let acceptanceTimeoutSeconds =
        (Option.map ~f:MatchmakingAcceptanceTimeoutInteger.of_xml)
          (Xml.child xml_arg0 "AcceptanceTimeoutSeconds") in
      let requestTimeoutSeconds =
        (Option.map ~f:MatchmakingRequestTimeoutInteger.of_xml)
          (Xml.child xml_arg0 "RequestTimeoutSeconds") in
      let gameSessionQueueArns =
        (Option.map ~f:QueueArnsList.of_xml)
          (Xml.child xml_arg0 "GameSessionQueueArns") in
      let description =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Description") in
      let configurationArn =
        (Option.map ~f:MatchmakingConfigurationArn.of_xml)
          (Xml.child xml_arg0 "ConfigurationArn") in
      let name =
        (Option.map ~f:MatchmakingIdStringModel.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?flexMatchMode ?backfillMode ?gameSessionData ?gameProperties
        ?creationTime ?customEventData ?additionalPlayerCount
        ?notificationTarget ?ruleSetArn ?ruleSetName ?acceptanceRequired
        ?acceptanceTimeoutSeconds ?requestTimeoutSeconds
        ?gameSessionQueueArns ?description ?configurationArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let flexMatchMode =
        field_map json__ "FlexMatchMode" FlexMatchMode.of_json in
      let backfillMode = field_map json__ "BackfillMode" BackfillMode.of_json in
      let gameSessionData =
        field_map json__ "GameSessionData" GameSessionData.of_json in
      let gameProperties =
        field_map json__ "GameProperties" GamePropertyList.of_json in
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let customEventData =
        field_map json__ "CustomEventData" CustomEventData.of_json in
      let additionalPlayerCount =
        field_map json__ "AdditionalPlayerCount" WholeNumber.of_json in
      let notificationTarget =
        field_map json__ "NotificationTarget" SnsArnStringModel.of_json in
      let ruleSetArn =
        field_map json__ "RuleSetArn" MatchmakingRuleSetArn.of_json in
      let ruleSetName =
        field_map json__ "RuleSetName" MatchmakingIdStringModel.of_json in
      let acceptanceRequired =
        field_map json__ "AcceptanceRequired" BooleanModel.of_json in
      let acceptanceTimeoutSeconds =
        field_map json__ "AcceptanceTimeoutSeconds"
          MatchmakingAcceptanceTimeoutInteger.of_json in
      let requestTimeoutSeconds =
        field_map json__ "RequestTimeoutSeconds"
          MatchmakingRequestTimeoutInteger.of_json in
      let gameSessionQueueArns =
        field_map json__ "GameSessionQueueArns" QueueArnsList.of_json in
      let description =
        field_map json__ "Description" NonZeroAndMaxString.of_json in
      let configurationArn =
        field_map json__ "ConfigurationArn"
          MatchmakingConfigurationArn.of_json in
      let name = field_map json__ "Name" MatchmakingIdStringModel.of_json in
      make ?flexMatchMode ?backfillMode ?gameSessionData ?gameProperties
        ?creationTime ?customEventData ?additionalPlayerCount
        ?notificationTarget ?ruleSetArn ?ruleSetName ?acceptanceRequired
        ?acceptanceTimeoutSeconds ?requestTimeoutSeconds
        ?gameSessionQueueArns ?description ?configurationArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Guidelines for use with FlexMatch to match players into games. All matchmaking requests must specify a matchmaking configuration."]
module MatchmakingConfigurationName =
  struct
    type nonrec t = string
    let context_ = "MatchmakingConfigurationName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[a-zA-Z0-9-\\.]*|^arn:.*:matchmakingconfiguration\\/[a-zA-Z0-9-\\.]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MatchmakingConfigurationName" j
    let to_json = simple_to_json to_value
  end
module Instance =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc
          "A unique identifier for the fleet that the instance belongs to."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      instanceId: InstanceId.t option
        [@ocaml.doc "A unique identifier for the instance."];
      ipAddress: IpAddress.t option
        [@ocaml.doc "IP address that is assigned to the instance."];
      dnsName: DnsName.t option
        [@ocaml.doc
          "The DNS identifier assigned to the instance that is running the game session. Values have the following format: TLS-enabled fleets: <unique identifier>.<region identifier>.amazongamelift.com. Non-TLS-enabled fleets: ec2-<unique identifier>.compute.amazonaws.com. (See Amazon EC2 Instance IP Addressing.) When connecting to a game session that is running on a TLS-enabled fleet, you must use the DNS name, not the IP address."];
      operatingSystem: OperatingSystem.t option
        [@ocaml.doc
          "Operating system that is running on this EC2 instance. Amazon Linux 2 (AL2) will reach end of support on 6/30/2026. See more details in the Amazon Linux 2 FAQs. For game servers that are hosted on AL2 and use server SDK version 4.x for Amazon GameLift Servers, first update the game server build to server SDK 5.x, and then deploy to AL2023 instances. See Migrate to server SDK version 5."];
      type_: EC2InstanceType.t option
        [@ocaml.doc
          "EC2 instance type that defines the computing resources of this instance."];
      status: InstanceStatus.t option
        [@ocaml.doc
          "Current status of the instance. Possible statuses include the following: PENDING -- The instance is in the process of being created and launching server processes as defined in the fleet's run-time configuration. ACTIVE -- The instance has been successfully created and at least one server process has successfully launched and reported back to Amazon GameLift Servers that it is ready to host a game session. The instance is now considered ready to host game sessions. TERMINATING -- The instance is in the process of shutting down. This may happen to reduce capacity during a scaling down event or to recycle resources in the event of a problem."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The fleet location of the instance, expressed as an Amazon Web Services Region code, such as us-west-2."]}
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?instanceId ->
          fun ?ipAddress ->
            fun ?dnsName ->
              fun ?operatingSystem ->
                fun ?type_ ->
                  fun ?status ->
                    fun ?creationTime ->
                      fun ?location ->
                        fun () ->
                          {
                            fleetId;
                            fleetArn;
                            instanceId;
                            ipAddress;
                            dnsName;
                            operatingSystem;
                            type_;
                            status;
                            creationTime;
                            location
                          }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("InstanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("IpAddress", (Option.map x.ipAddress ~f:IpAddress.to_value));
        ("DnsName", (Option.map x.dnsName ~f:DnsName.to_value));
        ("OperatingSystem",
          (Option.map x.operatingSystem ~f:OperatingSystem.to_value));
        ("Type", (Option.map x.type_ ~f:EC2InstanceType.to_value));
        ("Status", (Option.map x.status ~f:InstanceStatus.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let status =
        (Option.map ~f:InstanceStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let type_ =
        (Option.map ~f:EC2InstanceType.of_xml) (Xml.child xml_arg0 "Type") in
      let operatingSystem =
        (Option.map ~f:OperatingSystem.of_xml)
          (Xml.child xml_arg0 "OperatingSystem") in
      let dnsName =
        (Option.map ~f:DnsName.of_xml) (Xml.child xml_arg0 "DnsName") in
      let ipAddress =
        (Option.map ~f:IpAddress.of_xml) (Xml.child xml_arg0 "IpAddress") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "InstanceId") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?location ?creationTime ?status ?type_ ?operatingSystem ?dnsName
        ?ipAddress ?instanceId ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let status = field_map json__ "Status" InstanceStatus.of_json in
      let type_ = field_map json__ "Type" EC2InstanceType.of_json in
      let operatingSystem =
        field_map json__ "OperatingSystem" OperatingSystem.of_json in
      let dnsName = field_map json__ "DnsName" DnsName.of_json in
      let ipAddress = field_map json__ "IpAddress" IpAddress.of_json in
      let instanceId = field_map json__ "InstanceId" InstanceId.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?location ?creationTime ?status ?type_ ?operatingSystem ?dnsName
        ?ipAddress ?instanceId ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a virtual computing instance that runs game server processes and hosts game sessions. In Amazon GameLift Servers, one or more instances make up a managed EC2 fleet."]
module GameSessionQueue =
  struct
    type nonrec t =
      {
      name: GameSessionQueueName.t option
        [@ocaml.doc
          "A descriptive label that is associated with game session queue. Queue names must be unique within each Region."];
      gameSessionQueueArn: GameSessionQueueArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers game session queue resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::gamesessionqueue/<queue name>. In a Amazon GameLift Servers game session queue ARN, the resource ID matches the Name value."];
      timeoutInSeconds: WholeNumber.t option
        [@ocaml.doc
          "The maximum time, in seconds, that a new game session placement request remains in the queue. When a request exceeds this time, the game session placement changes to a TIMED_OUT status. The minimum value is 10 and the maximum value is 600."];
      playerLatencyPolicies: PlayerLatencyPolicyList.t option
        [@ocaml.doc
          "A set of policies that enforce a sliding cap on player latency when processing game sessions placement requests. Use multiple policies to gradually relax the cap over time if Amazon GameLift Servers can't make a placement. Policies are evaluated in order starting with the lowest maximum latency value."];
      destinations: GameSessionQueueDestinationList.t option
        [@ocaml.doc
          "A list of fleets and/or fleet aliases that can be used to fulfill game session placement requests in the queue. Destinations are identified by either a fleet ARN or a fleet alias ARN, and are listed in order of placement preference."];
      filterConfiguration: FilterConfiguration.t option
        [@ocaml.doc
          "A list of locations where a queue is allowed to place new game sessions. Locations are specified in the form of Amazon Web Services Region codes, such as us-west-2. If this parameter is not set, game sessions can be placed in any queue location."];
      priorityConfiguration: PriorityConfiguration.t option
        [@ocaml.doc
          "Custom settings to use when prioritizing destinations and locations for game session placements. This configuration replaces the FleetIQ default prioritization process. Priority types that are not explicitly named will be automatically applied at the end of the prioritization process."];
      customEventData: QueueCustomEventData.t option
        [@ocaml.doc
          "Information that is added to all events that are related to this game session queue."];
      notificationTarget: QueueSnsArnStringModel.t option
        [@ocaml.doc
          "An SNS topic ARN that is set up to receive game session placement notifications. See Setting up notifications for game session placement."]}
    let make ?name =
      fun ?gameSessionQueueArn ->
        fun ?timeoutInSeconds ->
          fun ?playerLatencyPolicies ->
            fun ?destinations ->
              fun ?filterConfiguration ->
                fun ?priorityConfiguration ->
                  fun ?customEventData ->
                    fun ?notificationTarget ->
                      fun () ->
                        {
                          name;
                          gameSessionQueueArn;
                          timeoutInSeconds;
                          playerLatencyPolicies;
                          destinations;
                          filterConfiguration;
                          priorityConfiguration;
                          customEventData;
                          notificationTarget
                        }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:GameSessionQueueName.to_value));
        ("GameSessionQueueArn",
          (Option.map x.gameSessionQueueArn ~f:GameSessionQueueArn.to_value));
        ("TimeoutInSeconds",
          (Option.map x.timeoutInSeconds ~f:WholeNumber.to_value));
        ("PlayerLatencyPolicies",
          (Option.map x.playerLatencyPolicies
             ~f:PlayerLatencyPolicyList.to_value));
        ("Destinations",
          (Option.map x.destinations
             ~f:GameSessionQueueDestinationList.to_value));
        ("FilterConfiguration",
          (Option.map x.filterConfiguration ~f:FilterConfiguration.to_value));
        ("PriorityConfiguration",
          (Option.map x.priorityConfiguration
             ~f:PriorityConfiguration.to_value));
        ("CustomEventData",
          (Option.map x.customEventData ~f:QueueCustomEventData.to_value));
        ("NotificationTarget",
          (Option.map x.notificationTarget ~f:QueueSnsArnStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notificationTarget =
        (Option.map ~f:QueueSnsArnStringModel.of_xml)
          (Xml.child xml_arg0 "NotificationTarget") in
      let customEventData =
        (Option.map ~f:QueueCustomEventData.of_xml)
          (Xml.child xml_arg0 "CustomEventData") in
      let priorityConfiguration =
        (Option.map ~f:PriorityConfiguration.of_xml)
          (Xml.child xml_arg0 "PriorityConfiguration") in
      let filterConfiguration =
        (Option.map ~f:FilterConfiguration.of_xml)
          (Xml.child xml_arg0 "FilterConfiguration") in
      let destinations =
        (Option.map ~f:GameSessionQueueDestinationList.of_xml)
          (Xml.child xml_arg0 "Destinations") in
      let playerLatencyPolicies =
        (Option.map ~f:PlayerLatencyPolicyList.of_xml)
          (Xml.child xml_arg0 "PlayerLatencyPolicies") in
      let timeoutInSeconds =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "TimeoutInSeconds") in
      let gameSessionQueueArn =
        (Option.map ~f:GameSessionQueueArn.of_xml)
          (Xml.child xml_arg0 "GameSessionQueueArn") in
      let name =
        (Option.map ~f:GameSessionQueueName.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?notificationTarget ?customEventData ?priorityConfiguration
        ?filterConfiguration ?destinations ?playerLatencyPolicies
        ?timeoutInSeconds ?gameSessionQueueArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let notificationTarget =
        field_map json__ "NotificationTarget" QueueSnsArnStringModel.of_json in
      let customEventData =
        field_map json__ "CustomEventData" QueueCustomEventData.of_json in
      let priorityConfiguration =
        field_map json__ "PriorityConfiguration"
          PriorityConfiguration.of_json in
      let filterConfiguration =
        field_map json__ "FilterConfiguration" FilterConfiguration.of_json in
      let destinations =
        field_map json__ "Destinations"
          GameSessionQueueDestinationList.of_json in
      let playerLatencyPolicies =
        field_map json__ "PlayerLatencyPolicies"
          PlayerLatencyPolicyList.of_json in
      let timeoutInSeconds =
        field_map json__ "TimeoutInSeconds" WholeNumber.of_json in
      let gameSessionQueueArn =
        field_map json__ "GameSessionQueueArn" GameSessionQueueArn.of_json in
      let name = field_map json__ "Name" GameSessionQueueName.of_json in
      make ?notificationTarget ?customEventData ?priorityConfiguration
        ?filterConfiguration ?destinations ?playerLatencyPolicies
        ?timeoutInSeconds ?gameSessionQueueArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration for a game session placement mechanism that processes requests for new game sessions. A queue can be used on its own or as part of a matchmaking solution."]
module GameSessionQueueNameOrArn =
  struct
    type nonrec t = string
    let context_ = "GameSessionQueueNameOrArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[a-zA-Z0-9-]+|^arn:.*:gamesessionqueue\\/[a-zA-Z0-9-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GameSessionQueueNameOrArn" j
    let to_json = simple_to_json to_value
  end
module GameSessionDetail =
  struct
    type nonrec t =
      {
      gameSession: GameSession.t option
        [@ocaml.doc "Object that describes a game session."];
      protectionPolicy: ProtectionPolicy.t option
        [@ocaml.doc
          "Current status of protection for the game session. NoProtection -- The game session can be terminated during a scale-down event. FullProtection -- If the game session is in an ACTIVE status, it cannot be terminated during a scale-down event."]}
    let make ?gameSession =
      fun ?protectionPolicy -> fun () -> { gameSession; protectionPolicy }
    let to_value x =
      structure_to_value
        [("GameSession", (Option.map x.gameSession ~f:GameSession.to_value));
        ("ProtectionPolicy",
          (Option.map x.protectionPolicy ~f:ProtectionPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protectionPolicy =
        (Option.map ~f:ProtectionPolicy.of_xml)
          (Xml.child xml_arg0 "ProtectionPolicy") in
      let gameSession =
        (Option.map ~f:GameSession.of_xml) (Xml.child xml_arg0 "GameSession") in
      make ?protectionPolicy ?gameSession ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let protectionPolicy =
        field_map json__ "ProtectionPolicy" ProtectionPolicy.of_json in
      let gameSession = field_map json__ "GameSession" GameSession.of_json in
      make ?protectionPolicy ?gameSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A game session's properties plus the protection policy currently in force."]
module GameServerInstance =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupName.t option
        [@ocaml.doc
          "A developer-defined identifier for the game server group that includes the game server instance. The name is unique for each Region in each Amazon Web Services account."];
      gameServerGroupArn: GameServerGroupArn.t option
        [@ocaml.doc
          "A generated unique identifier for the game server group that includes the game server instance."];
      instanceId: GameServerInstanceId.t option
        [@ocaml.doc
          "The unique identifier for the instance where the game server is running. This ID is available in the instance metadata. EC2 instance IDs use a 17-character format, for example: i-1234567890abcdef0."];
      instanceStatus: GameServerInstanceStatus.t option
        [@ocaml.doc "Current status of the game server instance"]}
    let make ?gameServerGroupName =
      fun ?gameServerGroupArn ->
        fun ?instanceId ->
          fun ?instanceStatus ->
            fun () ->
              {
                gameServerGroupName;
                gameServerGroupArn;
                instanceId;
                instanceStatus
              }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Option.map x.gameServerGroupName ~f:GameServerGroupName.to_value));
        ("GameServerGroupArn",
          (Option.map x.gameServerGroupArn ~f:GameServerGroupArn.to_value));
        ("InstanceId",
          (Option.map x.instanceId ~f:GameServerInstanceId.to_value));
        ("InstanceStatus",
          (Option.map x.instanceStatus ~f:GameServerInstanceStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceStatus =
        (Option.map ~f:GameServerInstanceStatus.of_xml)
          (Xml.child xml_arg0 "InstanceStatus") in
      let instanceId =
        (Option.map ~f:GameServerInstanceId.of_xml)
          (Xml.child xml_arg0 "InstanceId") in
      let gameServerGroupArn =
        (Option.map ~f:GameServerGroupArn.of_xml)
          (Xml.child xml_arg0 "GameServerGroupArn") in
      let gameServerGroupName =
        (Option.map ~f:GameServerGroupName.of_xml)
          (Xml.child xml_arg0 "GameServerGroupName") in
      make ?instanceStatus ?instanceId ?gameServerGroupArn
        ?gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceStatus =
        field_map json__ "InstanceStatus" GameServerInstanceStatus.of_json in
      let instanceId =
        field_map json__ "InstanceId" GameServerInstanceId.of_json in
      let gameServerGroupArn =
        field_map json__ "GameServerGroupArn" GameServerGroupArn.of_json in
      let gameServerGroupName =
        field_map json__ "GameServerGroupName" GameServerGroupName.of_json in
      make ?instanceStatus ?instanceId ?gameServerGroupArn
        ?gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Additional properties, including status, that describe an EC2 instance in a game server group. Instance configurations are set with game server group properties (see DescribeGameServerGroup and with the EC2 launch template that was used when creating the game server group. Retrieve game server instances for a game server group by calling DescribeGameServerInstances."]
module FleetUtilization =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc
          "A unique identifier for the fleet associated with the location."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      activeServerProcessCount: WholeNumber.t option
        [@ocaml.doc
          "The number of server processes in ACTIVE status that are currently running across all instances in the fleet location."];
      activeGameSessionCount: WholeNumber.t option
        [@ocaml.doc
          "The number of active game sessions that are currently being hosted across all instances in the fleet location."];
      currentPlayerSessionCount: WholeNumber.t option
        [@ocaml.doc
          "The number of active player sessions that are currently being hosted across all instances in the fleet location."];
      maximumPlayerSessionCount: WholeNumber.t option
        [@ocaml.doc
          "The maximum number of players allowed across all game sessions that are currently being hosted across all instances in the fleet location."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The fleet location for the fleet utilization information, expressed as an Amazon Web Services Region code, such as us-west-2."]}
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?activeServerProcessCount ->
          fun ?activeGameSessionCount ->
            fun ?currentPlayerSessionCount ->
              fun ?maximumPlayerSessionCount ->
                fun ?location ->
                  fun () ->
                    {
                      fleetId;
                      fleetArn;
                      activeServerProcessCount;
                      activeGameSessionCount;
                      currentPlayerSessionCount;
                      maximumPlayerSessionCount;
                      location
                    }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("ActiveServerProcessCount",
          (Option.map x.activeServerProcessCount ~f:WholeNumber.to_value));
        ("ActiveGameSessionCount",
          (Option.map x.activeGameSessionCount ~f:WholeNumber.to_value));
        ("CurrentPlayerSessionCount",
          (Option.map x.currentPlayerSessionCount ~f:WholeNumber.to_value));
        ("MaximumPlayerSessionCount",
          (Option.map x.maximumPlayerSessionCount ~f:WholeNumber.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let maximumPlayerSessionCount =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "MaximumPlayerSessionCount") in
      let currentPlayerSessionCount =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "CurrentPlayerSessionCount") in
      let activeGameSessionCount =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "ActiveGameSessionCount") in
      let activeServerProcessCount =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "ActiveServerProcessCount") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?location ?maximumPlayerSessionCount ?currentPlayerSessionCount
        ?activeGameSessionCount ?activeServerProcessCount ?fleetArn ?fleetId
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let maximumPlayerSessionCount =
        field_map json__ "MaximumPlayerSessionCount" WholeNumber.of_json in
      let currentPlayerSessionCount =
        field_map json__ "CurrentPlayerSessionCount" WholeNumber.of_json in
      let activeGameSessionCount =
        field_map json__ "ActiveGameSessionCount" WholeNumber.of_json in
      let activeServerProcessCount =
        field_map json__ "ActiveServerProcessCount" WholeNumber.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?location ?maximumPlayerSessionCount ?currentPlayerSessionCount
        ?activeGameSessionCount ?activeServerProcessCount ?fleetArn ?fleetId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Current resource utilization statistics in a specified fleet or location. The location value might refer to a fleet's remote location or its home region."]
module FleetIdOrArn =
  struct
    type nonrec t = string
    let context_ = "FleetIdOrArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:512) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^[a-z]*fleet-[a-zA-Z0-9\\-]+$|^arn:.*:[a-z]*fleet\\/[a-z]*fleet-[a-zA-Z0-9\\-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FleetIdOrArn" j
    let to_json = simple_to_json to_value
  end
module LocationAttributes =
  struct
    type nonrec t =
      {
      locationState: LocationState.t option
        [@ocaml.doc "A fleet location and its current life-cycle state."];
      stoppedActions: FleetActionList.t option
        [@ocaml.doc
          "A list of fleet actions that have been suspended in the fleet location."];
      updateStatus: LocationUpdateStatus.t option
        [@ocaml.doc
          "The status of fleet activity updates to the location. The status PENDING_UPDATE indicates that StopFleetActions or StartFleetActions has been requested but the update has not yet been completed for the location."]}
    let make ?locationState =
      fun ?stoppedActions ->
        fun ?updateStatus ->
          fun () -> { locationState; stoppedActions; updateStatus }
    let to_value x =
      structure_to_value
        [("LocationState",
           (Option.map x.locationState ~f:LocationState.to_value));
        ("StoppedActions",
          (Option.map x.stoppedActions ~f:FleetActionList.to_value));
        ("UpdateStatus",
          (Option.map x.updateStatus ~f:LocationUpdateStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updateStatus =
        (Option.map ~f:LocationUpdateStatus.of_xml)
          (Xml.child xml_arg0 "UpdateStatus") in
      let stoppedActions =
        (Option.map ~f:FleetActionList.of_xml)
          (Xml.child xml_arg0 "StoppedActions") in
      let locationState =
        (Option.map ~f:LocationState.of_xml)
          (Xml.child xml_arg0 "LocationState") in
      make ?updateStatus ?stoppedActions ?locationState ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updateStatus =
        field_map json__ "UpdateStatus" LocationUpdateStatus.of_json in
      let stoppedActions =
        field_map json__ "StoppedActions" FleetActionList.of_json in
      let locationState =
        field_map json__ "LocationState" LocationState.of_json in
      make ?updateStatus ?stoppedActions ?locationState ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Details about a location in a multi-location fleet."]
module Event =
  struct
    type nonrec t =
      {
      eventId: NonZeroAndMaxString.t option
        [@ocaml.doc "A unique identifier for a fleet event."];
      resourceId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A unique identifier for an event resource, such as a fleet ID."];
      eventCode: EventCode.t option
        [@ocaml.doc
          "The type of event being logged. Fleet state transition events: FLEET_CREATED -- A fleet resource was successfully created with a status of NEW. Event messaging includes the fleet ID. FLEET_STATE_DOWNLOADING -- Fleet status changed from NEW to DOWNLOADING. Amazon GameLift Servers is downloading the compressed build and running install scripts. FLEET_STATE_VALIDATING -- Fleet status changed from DOWNLOADING to VALIDATING. Amazon GameLift Servers has successfully installed build and is now validating the build files. FLEET_STATE_BUILDING -- Fleet status changed from VALIDATING to BUILDING. Amazon GameLift Servers has successfully verified the build files and is now launching a fleet instance. FLEET_STATE_ACTIVATING -- Fleet status changed from BUILDING to ACTIVATING. Amazon GameLift Servers is launching a game server process on the fleet instance and is testing its connectivity with the Amazon GameLift Servers service. FLEET_STATE_ACTIVE -- The fleet's status changed from ACTIVATING to ACTIVE. The fleet is now ready to host game sessions. FLEET_STATE_ERROR -- The Fleet's status changed to ERROR. Describe the fleet event message for more details. Fleet creation events (ordered by fleet creation activity): FLEET_BINARY_DOWNLOAD_FAILED -- The build failed to download to the fleet instance. FLEET_CREATION_EXTRACTING_BUILD -- The game server build was successfully downloaded to an instance, and Amazon GameLift Serversis now extracting the build files from the uploaded build. Failure at this stage prevents a fleet from moving to ACTIVE status. Logs for this stage display a list of the files that are extracted and saved on the instance. Access the logs by using the URL in PreSignedLogUrl. FLEET_CREATION_RUNNING_INSTALLER -- The game server build files were successfully extracted, and Amazon GameLift Servers is now running the build's install script (if one is included). Failure in this stage prevents a fleet from moving to ACTIVE status. Logs for this stage list the installation steps and whether or not the install completed successfully. Access the logs by using the URL in PreSignedLogUrl. FLEET_CREATION_COMPLETED_INSTALLER -- The game server build files were successfully installed and validation of the installation will begin soon. FLEET_CREATION_FAILED_INSTALLER -- The installed failed while attempting to install the build files. This event indicates that the failure occurred before Amazon GameLift Servers could start validation. FLEET_CREATION_VALIDATING_RUNTIME_CONFIG -- The build process was successful, and the GameLift is now verifying that the game server launch paths, which are specified in the fleet's runtime configuration, exist. If any listed launch path exists, Amazon GameLift Servers tries to launch a game server process and waits for the process to report ready. Failures in this stage prevent a fleet from moving to ACTIVE status. Logs for this stage list the launch paths in the runtime configuration and indicate whether each is found. Access the logs by using the URL in PreSignedLogUrl. FLEET_VALIDATION_LAUNCH_PATH_NOT_FOUND -- Validation of the runtime configuration failed because the executable specified in a launch path does not exist on the instance. FLEET_VALIDATION_EXECUTABLE_RUNTIME_FAILURE -- Validation of the runtime configuration failed because the executable specified in a launch path failed to run on the fleet instance. FLEET_VALIDATION_TIMED_OUT -- Validation of the fleet at the end of creation timed out. Try fleet creation again. FLEET_ACTIVATION_FAILED -- The fleet failed to successfully complete one of the steps in the fleet activation process. This event code indicates that the game build was successfully downloaded to a fleet instance, built, and validated, but was not able to start a server process. For more information, see Debug Fleet Creation Issues. FLEET_ACTIVATION_FAILED_NO_INSTANCES -- Fleet creation was not able to obtain any instances based on the input fleet attributes. Try again at a different time or choose a different combination of fleet attributes such as fleet type, instance type, etc. FLEET_INITIALIZATION_FAILED -- A generic exception occurred during fleet creation. Describe the fleet event message for more details. VPC peering events: FLEET_VPC_PEERING_SUCCEEDED -- A VPC peering connection has been established between the VPC for an Amazon GameLift Servers fleet and a VPC in your Amazon Web Services account. FLEET_VPC_PEERING_FAILED -- A requested VPC peering connection has failed. Event details and status information provide additional detail. A common reason for peering failure is that the two VPCs have overlapping CIDR blocks of IPv4 addresses. To resolve this, change the CIDR block for the VPC in your Amazon Web Services account. For more information on VPC peering failures, see https://docs.aws.amazon.com/AmazonVPC/latest/PeeringGuide/invalid-peering-configurations.html FLEET_VPC_PEERING_DELETED -- A VPC peering connection has been successfully deleted. Spot instance events: INSTANCE_INTERRUPTED -- A spot instance was interrupted by EC2 with a two-minute notification. INSTANCE_RECYCLED -- A spot instance was determined to have a high risk of interruption and is scheduled to be recycled once it has no active game sessions. Server process events: SERVER_PROCESS_INVALID_PATH -- The game server executable or script could not be found based on the Fleet runtime configuration. Check that the launch path is correct based on the operating system of the Fleet. SERVER_PROCESS_SDK_INITIALIZATION_TIMEOUT -- The server process did not call InitSDK() within the time expected (5 minutes). Check your game session log to see why InitSDK() was not called in time. This event is not emitted for managed container fleets and Anywhere fleets unless they're deployed with the Amazon GameLift Servers Agent. SERVER_PROCESS_PROCESS_READY_TIMEOUT -- The server process did not call ProcessReady() within the time expected (5 minutes) after calling InitSDK(). Check your game session log to see why ProcessReady() was not called in time. SERVER_PROCESS_CRASHED -- The server process exited without calling ProcessEnding(). Check your game session log to see why ProcessEnding() was not called. SERVER_PROCESS_TERMINATED_UNHEALTHY -- The server process did not report a valid health check for too long and was therefore terminated by GameLift. Check your game session log to see if the thread became stuck processing a synchronous task for too long. SERVER_PROCESS_FORCE_TERMINATED -- The server process did not exit cleanly within the time expected after OnProcessTerminate() was sent. Check your game session log to see why termination took longer than expected. SERVER_PROCESS_PROCESS_EXIT_TIMEOUT -- The server process did not exit cleanly within the time expected (30 seconds) after calling ProcessEnding(). Check your game session log to see why termination took longer than expected. Game session events: GAME_SESSION_ACTIVATION_TIMEOUT -- GameSession failed to activate within the expected time. Check your game session log to see why ActivateGameSession() took longer to complete than expected. Other fleet events: FLEET_SCALING_EVENT -- A change was made to the fleet's capacity settings (desired instances, minimum/maximum scaling limits). Event messaging includes the new capacity settings. FLEET_NEW_GAME_SESSION_PROTECTION_POLICY_UPDATED -- A change was made to the fleet's game session protection policy setting. Event messaging includes both the old and new policy setting. FLEET_DELETED -- A request to delete a fleet was initiated. GENERIC_EVENT -- An unspecified event has occurred."];
      message: NonEmptyString.t option
        [@ocaml.doc "Additional information related to the event."];
      eventTime: Timestamp.t option
        [@ocaml.doc
          "Time stamp indicating when this event occurred. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      preSignedLogUrl: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Location of stored logs with additional detail that is related to the event. This is useful for debugging issues. The URL is valid for 15 minutes. You can also access fleet creation logs through the Amazon GameLift Servers console."];
      count: EventCount.t option
        [@ocaml.doc "The number of times that this event occurred."]}
    let make ?eventId =
      fun ?resourceId ->
        fun ?eventCode ->
          fun ?message ->
            fun ?eventTime ->
              fun ?preSignedLogUrl ->
                fun ?count ->
                  fun () ->
                    {
                      eventId;
                      resourceId;
                      eventCode;
                      message;
                      eventTime;
                      preSignedLogUrl;
                      count
                    }
    let to_value x =
      structure_to_value
        [("EventId", (Option.map x.eventId ~f:NonZeroAndMaxString.to_value));
        ("ResourceId",
          (Option.map x.resourceId ~f:NonZeroAndMaxString.to_value));
        ("EventCode", (Option.map x.eventCode ~f:EventCode.to_value));
        ("Message", (Option.map x.message ~f:NonEmptyString.to_value));
        ("EventTime", (Option.map x.eventTime ~f:Timestamp.to_value));
        ("PreSignedLogUrl",
          (Option.map x.preSignedLogUrl ~f:NonZeroAndMaxString.to_value));
        ("Count", (Option.map x.count ~f:EventCount.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let count =
        (Option.map ~f:EventCount.of_xml) (Xml.child xml_arg0 "Count") in
      let preSignedLogUrl =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "PreSignedLogUrl") in
      let eventTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EventTime") in
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      let eventCode =
        (Option.map ~f:EventCode.of_xml) (Xml.child xml_arg0 "EventCode") in
      let resourceId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "ResourceId") in
      let eventId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "EventId") in
      make ?count ?preSignedLogUrl ?eventTime ?message ?eventCode ?resourceId
        ?eventId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let count = field_map json__ "Count" EventCount.of_json in
      let preSignedLogUrl =
        field_map json__ "PreSignedLogUrl" NonZeroAndMaxString.of_json in
      let eventTime = field_map json__ "EventTime" Timestamp.of_json in
      let message = field_map json__ "Message" NonEmptyString.of_json in
      let eventCode = field_map json__ "EventCode" EventCode.of_json in
      let resourceId =
        field_map json__ "ResourceId" NonZeroAndMaxString.of_json in
      let eventId = field_map json__ "EventId" NonZeroAndMaxString.of_json in
      make ?count ?preSignedLogUrl ?eventTime ?message ?eventCode ?resourceId
        ?eventId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Log entry describing an event that involves Amazon GameLift Servers resources (such as a fleet). In addition to tracking activity, event codes and messages can provide additional information for troubleshooting and debugging problems."]
module LocationalDeployment =
  struct
    type nonrec t =
      {
      deploymentStatus: DeploymentStatus.t option
        [@ocaml.doc
          "The status of fleet deployment activity in the location. IN_PROGRESS -- The deployment is in progress. IMPAIRED -- The deployment failed and the fleet has some impaired containers. COMPLETE -- The deployment has completed successfully. ROLLBACK_IN_PROGRESS -- The deployment failed and rollback has been initiated. ROLLBACK_IN_COMPLETE -- The deployment failed and rollback has been completed. CANCELLED -- The deployment was cancelled."]}
    let make ?deploymentStatus = fun () -> { deploymentStatus }
    let to_value x =
      structure_to_value
        [("DeploymentStatus",
           (Option.map x.deploymentStatus ~f:DeploymentStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deploymentStatus =
        (Option.map ~f:DeploymentStatus.of_xml)
          (Xml.child xml_arg0 "DeploymentStatus") in
      make ?deploymentStatus ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deploymentStatus =
        field_map json__ "DeploymentStatus" DeploymentStatus.of_json in
      make ?deploymentStatus ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "For a multi-location container fleet, describes the progress of a deployment across all fleet locations."]
module FleetCapacity =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc
          "A unique identifier for the fleet associated with the location."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      instanceType: EC2InstanceType.t option
        [@ocaml.doc
          "The Amazon EC2 instance type that is used for instances in a fleet. Instance type determines the computing resources in use, including CPU, memory, storage, and networking capacity. See Amazon Elastic Compute Cloud Instance Types for detailed descriptions."];
      instanceCounts: EC2InstanceCounts.t option
        [@ocaml.doc
          "The current number of instances in the fleet, listed by instance status. Counts for pending and terminating instances might be non-zero if the fleet is adjusting to a scaling event or if access to resources is temporarily affected."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The fleet location for the instance count information, expressed as an Amazon Web Services Region code, such as us-west-2."];
      gameServerContainerGroupCounts: GameServerContainerGroupCounts.t option
        [@ocaml.doc
          "The number and status of game server container groups deployed in a container fleet."];
      managedCapacityConfiguration: ManagedCapacityConfiguration.t option
        [@ocaml.doc "Configuration settings for managed capacity scaling."]}
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?instanceType ->
          fun ?instanceCounts ->
            fun ?location ->
              fun ?gameServerContainerGroupCounts ->
                fun ?managedCapacityConfiguration ->
                  fun () ->
                    {
                      fleetId;
                      fleetArn;
                      instanceType;
                      instanceCounts;
                      location;
                      gameServerContainerGroupCounts;
                      managedCapacityConfiguration
                    }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("InstanceType",
          (Option.map x.instanceType ~f:EC2InstanceType.to_value));
        ("InstanceCounts",
          (Option.map x.instanceCounts ~f:EC2InstanceCounts.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value));
        ("GameServerContainerGroupCounts",
          (Option.map x.gameServerContainerGroupCounts
             ~f:GameServerContainerGroupCounts.to_value));
        ("ManagedCapacityConfiguration",
          (Option.map x.managedCapacityConfiguration
             ~f:ManagedCapacityConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let managedCapacityConfiguration =
        (Option.map ~f:ManagedCapacityConfiguration.of_xml)
          (Xml.child xml_arg0 "ManagedCapacityConfiguration") in
      let gameServerContainerGroupCounts =
        (Option.map ~f:GameServerContainerGroupCounts.of_xml)
          (Xml.child xml_arg0 "GameServerContainerGroupCounts") in
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let instanceCounts =
        (Option.map ~f:EC2InstanceCounts.of_xml)
          (Xml.child xml_arg0 "InstanceCounts") in
      let instanceType =
        (Option.map ~f:EC2InstanceType.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?managedCapacityConfiguration ?gameServerContainerGroupCounts
        ?location ?instanceCounts ?instanceType ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let managedCapacityConfiguration =
        field_map json__ "ManagedCapacityConfiguration"
          ManagedCapacityConfiguration.of_json in
      let gameServerContainerGroupCounts =
        field_map json__ "GameServerContainerGroupCounts"
          GameServerContainerGroupCounts.of_json in
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let instanceCounts =
        field_map json__ "InstanceCounts" EC2InstanceCounts.of_json in
      let instanceType =
        field_map json__ "InstanceType" EC2InstanceType.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?managedCapacityConfiguration ?gameServerContainerGroupCounts
        ?location ?instanceCounts ?instanceType ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Current resource capacity settings for managed EC2 fleets and managed container fleets. For multi-location fleets, location values might refer to a fleet's remote location or its home Region. Returned by: DescribeFleetCapacity, DescribeFleetLocationCapacity, UpdateFleetCapacity"]
module FleetAttributes =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc "A unique identifier for the fleet."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912. In a GameLift fleet ARN, the resource ID matches the FleetId value."];
      fleetType: FleetType.t option
        [@ocaml.doc
          "Indicates whether the fleet uses On-Demand or Spot instances. For more information, see On-Demand versus Spot Instances. This fleet property can't be changed after the fleet is created."];
      instanceType: EC2InstanceType.t option
        [@ocaml.doc
          "The Amazon EC2 instance type that the fleet uses. Instance type determines the computing resources of each instance in the fleet, including CPU, memory, storage, and networking capacity. See Amazon Elastic Compute Cloud Instance Types for detailed descriptions. This attribute is used with fleets where ComputeType is EC2."];
      description: NonZeroAndMaxString.t option
        [@ocaml.doc "A human-readable description of the fleet."];
      name: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a fleet. Fleet names do not need to be unique."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was created. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      terminationTime: Timestamp.t option
        [@ocaml.doc
          "A time stamp indicating when this data object was terminated. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      status: FleetStatus.t option
        [@ocaml.doc
          "Current status of the fleet. Possible fleet statuses include the following: NEW -- A new fleet resource has been defined and Amazon GameLift Servers has started creating the fleet. Desired instances is set to 1. DOWNLOADING/VALIDATING/BUILDING -- Amazon GameLift Servers is download the game server build, running install scripts, and then validating the build files. When complete, Amazon GameLift Servers launches a fleet instance. ACTIVATING -- Amazon GameLift Servers is launching a game server process and testing its connectivity with the Amazon GameLift Servers service. ACTIVE -- The fleet is now ready to host game sessions. ERROR -- An error occurred when downloading, validating, building, or activating the fleet. DELETING -- Hosts are responding to a delete fleet request. TERMINATED -- The fleet no longer exists."];
      buildId: BuildId.t option
        [@ocaml.doc
          "A unique identifier for the build resource that is deployed on instances in this fleet. This attribute is used with fleets where ComputeType is \"EC2\"."];
      buildArn: BuildArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the Amazon GameLift Servers build resource that is deployed on instances in this fleet. In a GameLift build ARN, the resource ID matches the BuildId value. This attribute is used with fleets where ComputeType is \"EC2\"."];
      scriptId: ScriptId.t option
        [@ocaml.doc
          "A unique identifier for the Realtime script resource that is deployed on instances in this fleet. This attribute is used with fleets where ComputeType is \"EC2\"."];
      scriptArn: ScriptArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the GameLift script resource that is deployed on instances in this fleet. In a GameLift script ARN, the resource ID matches the ScriptId value."];
      serverLaunchPath: LaunchPathStringModel.t option
        [@ocaml.doc
          "This parameter is no longer used. Server launch paths are now defined using the fleet's RuntimeConfiguration. Requests that use this parameter continue to be valid."];
      serverLaunchParameters: LaunchParametersStringModel.t option
        [@ocaml.doc
          "This parameter is no longer used. Server launch parameters are now defined using the fleet's runtime configuration. Requests that use this parameter continue to be valid."];
      logPaths: StringList.t option
        [@ocaml.doc
          "This parameter is no longer used. Game session log paths are now defined using the Amazon GameLift Servers server API ProcessReady() logParameters. See more information in the Server API Reference."];
      newGameSessionProtectionPolicy: ProtectionPolicy.t option
        [@ocaml.doc
          "The type of game session protection to set on all new instances that are started in the fleet. This attribute is used with fleets where ComputeType is EC2. NoProtection -- The game session can be terminated during a scale-down event. FullProtection -- If the game session is in an ACTIVE status, it cannot be terminated during a scale-down event."];
      operatingSystem: OperatingSystem.t option
        [@ocaml.doc
          "The operating system of the fleet's computing resources. A fleet's operating system is determined by the OS of the build or script that is deployed on this fleet. This attribute is used with fleets where ComputeType is EC2. Amazon Linux 2 (AL2) will reach end of support on 6/30/2026. See more details in the Amazon Linux 2 FAQs. For game servers that are hosted on AL2 and use server SDK version 4.x for Amazon GameLift Servers, first update the game server build to server SDK 5.x, and then deploy to AL2023 instances. See Migrate to server SDK version 5."];
      resourceCreationLimitPolicy: ResourceCreationLimitPolicy.t option ;
      metricGroups: MetricGroupList.t option
        [@ocaml.doc
          "Name of a metric group that metrics for this fleet are added to. In Amazon CloudWatch, you can view aggregated metrics for fleets that are in a metric group. A fleet can be included in only one metric group at a time. This attribute is used with fleets where ComputeType is EC2."];
      stoppedActions: FleetActionList.t option
        [@ocaml.doc
          "A list of fleet activity that has been suspended using StopFleetActions. This includes fleet auto-scaling. This attribute is used with fleets where ComputeType is EC2."];
      instanceRoleArn: NonEmptyString.t option
        [@ocaml.doc
          "A unique identifier for an IAM role that manages access to your Amazon Web Services services. With an instance role ARN set, any application that runs on an instance in this fleet can assume the role, including install scripts, server processes, and daemons (background processes). Create a role or look up a role's ARN by using the IAM dashboard in the Amazon Web Services Management Console. Learn more about using on-box credentials for your game servers at Access external resources from a game server. This attribute is used with fleets where ComputeType is EC2."];
      certificateConfiguration: CertificateConfiguration.t option
        [@ocaml.doc
          "Determines whether a TLS/SSL certificate is generated for a fleet. This feature must be enabled when creating the fleet. All instances in a fleet share the same certificate."];
      computeType: ComputeType.t option
        [@ocaml.doc
          "The type of compute resource used to host your game servers. You can use your own compute resources with Amazon GameLift Servers Anywhere or use Amazon EC2 instances with managed Amazon GameLift Servers."];
      anywhereConfiguration: AnywhereConfiguration.t option
        [@ocaml.doc
          "A set of attributes that are specific to an Anywhere fleet."];
      instanceRoleCredentialsProvider:
        InstanceRoleCredentialsProvider.t option
        [@ocaml.doc
          "Indicates that fleet instances maintain a shared credentials file for the IAM role defined in InstanceRoleArn. Shared credentials allow applications that are deployed with the game server executable to communicate with other Amazon Web Services resources. This property is used only when the game server is integrated with the server SDK version 5.x. For more information about using shared credentials, see Communicate with other Amazon Web Services resources from your fleets. This attribute is used with fleets where ComputeType is EC2."];
      playerGatewayMode: PlayerGatewayMode.t option
        [@ocaml.doc
          "Indicates whether player gateway is enabled for this fleet. Player gateway provides benefits such as DDoS protection with negligible impact to latency. If ENABLED or REQUIRED, game clients can use player gateway to connect with the game server. If DISABLED, game clients cannot use player gateway. Instead, they have to directly connect to the game server."];
      playerGatewayConfiguration: PlayerGatewayConfiguration.t option
        [@ocaml.doc
          "Configuration settings for player gateway on this fleet."]}
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?fleetType ->
          fun ?instanceType ->
            fun ?description ->
              fun ?name ->
                fun ?creationTime ->
                  fun ?terminationTime ->
                    fun ?status ->
                      fun ?buildId ->
                        fun ?buildArn ->
                          fun ?scriptId ->
                            fun ?scriptArn ->
                              fun ?serverLaunchPath ->
                                fun ?serverLaunchParameters ->
                                  fun ?logPaths ->
                                    fun ?newGameSessionProtectionPolicy ->
                                      fun ?operatingSystem ->
                                        fun ?resourceCreationLimitPolicy ->
                                          fun ?metricGroups ->
                                            fun ?stoppedActions ->
                                              fun ?instanceRoleArn ->
                                                fun ?certificateConfiguration
                                                  ->
                                                  fun ?computeType ->
                                                    fun
                                                      ?anywhereConfiguration
                                                      ->
                                                      fun
                                                        ?instanceRoleCredentialsProvider
                                                        ->
                                                        fun
                                                          ?playerGatewayMode
                                                          ->
                                                          fun
                                                            ?playerGatewayConfiguration
                                                            ->
                                                            fun () ->
                                                              {
                                                                fleetId;
                                                                fleetArn;
                                                                fleetType;
                                                                instanceType;
                                                                description;
                                                                name;
                                                                creationTime;
                                                                terminationTime;
                                                                status;
                                                                buildId;
                                                                buildArn;
                                                                scriptId;
                                                                scriptArn;
                                                                serverLaunchPath;
                                                                serverLaunchParameters;
                                                                logPaths;
                                                                newGameSessionProtectionPolicy;
                                                                operatingSystem;
                                                                resourceCreationLimitPolicy;
                                                                metricGroups;
                                                                stoppedActions;
                                                                instanceRoleArn;
                                                                certificateConfiguration;
                                                                computeType;
                                                                anywhereConfiguration;
                                                                instanceRoleCredentialsProvider;
                                                                playerGatewayMode;
                                                                playerGatewayConfiguration
                                                              }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("FleetType", (Option.map x.fleetType ~f:FleetType.to_value));
        ("InstanceType",
          (Option.map x.instanceType ~f:EC2InstanceType.to_value));
        ("Description",
          (Option.map x.description ~f:NonZeroAndMaxString.to_value));
        ("Name", (Option.map x.name ~f:NonZeroAndMaxString.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("TerminationTime",
          (Option.map x.terminationTime ~f:Timestamp.to_value));
        ("Status", (Option.map x.status ~f:FleetStatus.to_value));
        ("BuildId", (Option.map x.buildId ~f:BuildId.to_value));
        ("BuildArn", (Option.map x.buildArn ~f:BuildArn.to_value));
        ("ScriptId", (Option.map x.scriptId ~f:ScriptId.to_value));
        ("ScriptArn", (Option.map x.scriptArn ~f:ScriptArn.to_value));
        ("ServerLaunchPath",
          (Option.map x.serverLaunchPath ~f:LaunchPathStringModel.to_value));
        ("ServerLaunchParameters",
          (Option.map x.serverLaunchParameters
             ~f:LaunchParametersStringModel.to_value));
        ("LogPaths", (Option.map x.logPaths ~f:StringList.to_value));
        ("NewGameSessionProtectionPolicy",
          (Option.map x.newGameSessionProtectionPolicy
             ~f:ProtectionPolicy.to_value));
        ("OperatingSystem",
          (Option.map x.operatingSystem ~f:OperatingSystem.to_value));
        ("ResourceCreationLimitPolicy",
          (Option.map x.resourceCreationLimitPolicy
             ~f:ResourceCreationLimitPolicy.to_value));
        ("MetricGroups",
          (Option.map x.metricGroups ~f:MetricGroupList.to_value));
        ("StoppedActions",
          (Option.map x.stoppedActions ~f:FleetActionList.to_value));
        ("InstanceRoleArn",
          (Option.map x.instanceRoleArn ~f:NonEmptyString.to_value));
        ("CertificateConfiguration",
          (Option.map x.certificateConfiguration
             ~f:CertificateConfiguration.to_value));
        ("ComputeType", (Option.map x.computeType ~f:ComputeType.to_value));
        ("AnywhereConfiguration",
          (Option.map x.anywhereConfiguration
             ~f:AnywhereConfiguration.to_value));
        ("InstanceRoleCredentialsProvider",
          (Option.map x.instanceRoleCredentialsProvider
             ~f:InstanceRoleCredentialsProvider.to_value));
        ("PlayerGatewayMode",
          (Option.map x.playerGatewayMode ~f:PlayerGatewayMode.to_value));
        ("PlayerGatewayConfiguration",
          (Option.map x.playerGatewayConfiguration
             ~f:PlayerGatewayConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerGatewayConfiguration =
        (Option.map ~f:PlayerGatewayConfiguration.of_xml)
          (Xml.child xml_arg0 "PlayerGatewayConfiguration") in
      let playerGatewayMode =
        (Option.map ~f:PlayerGatewayMode.of_xml)
          (Xml.child xml_arg0 "PlayerGatewayMode") in
      let instanceRoleCredentialsProvider =
        (Option.map ~f:InstanceRoleCredentialsProvider.of_xml)
          (Xml.child xml_arg0 "InstanceRoleCredentialsProvider") in
      let anywhereConfiguration =
        (Option.map ~f:AnywhereConfiguration.of_xml)
          (Xml.child xml_arg0 "AnywhereConfiguration") in
      let computeType =
        (Option.map ~f:ComputeType.of_xml) (Xml.child xml_arg0 "ComputeType") in
      let certificateConfiguration =
        (Option.map ~f:CertificateConfiguration.of_xml)
          (Xml.child xml_arg0 "CertificateConfiguration") in
      let instanceRoleArn =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "InstanceRoleArn") in
      let stoppedActions =
        (Option.map ~f:FleetActionList.of_xml)
          (Xml.child xml_arg0 "StoppedActions") in
      let metricGroups =
        (Option.map ~f:MetricGroupList.of_xml)
          (Xml.child xml_arg0 "MetricGroups") in
      let resourceCreationLimitPolicy =
        (Option.map ~f:ResourceCreationLimitPolicy.of_xml)
          (Xml.child xml_arg0 "ResourceCreationLimitPolicy") in
      let operatingSystem =
        (Option.map ~f:OperatingSystem.of_xml)
          (Xml.child xml_arg0 "OperatingSystem") in
      let newGameSessionProtectionPolicy =
        (Option.map ~f:ProtectionPolicy.of_xml)
          (Xml.child xml_arg0 "NewGameSessionProtectionPolicy") in
      let logPaths =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "LogPaths") in
      let serverLaunchParameters =
        (Option.map ~f:LaunchParametersStringModel.of_xml)
          (Xml.child xml_arg0 "ServerLaunchParameters") in
      let serverLaunchPath =
        (Option.map ~f:LaunchPathStringModel.of_xml)
          (Xml.child xml_arg0 "ServerLaunchPath") in
      let scriptArn =
        (Option.map ~f:ScriptArn.of_xml) (Xml.child xml_arg0 "ScriptArn") in
      let scriptId =
        (Option.map ~f:ScriptId.of_xml) (Xml.child xml_arg0 "ScriptId") in
      let buildArn =
        (Option.map ~f:BuildArn.of_xml) (Xml.child xml_arg0 "BuildArn") in
      let buildId =
        (Option.map ~f:BuildId.of_xml) (Xml.child xml_arg0 "BuildId") in
      let status =
        (Option.map ~f:FleetStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let terminationTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "TerminationTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let name =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Name") in
      let description =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Description") in
      let instanceType =
        (Option.map ~f:EC2InstanceType.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let fleetType =
        (Option.map ~f:FleetType.of_xml) (Xml.child xml_arg0 "FleetType") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?playerGatewayConfiguration ?playerGatewayMode
        ?instanceRoleCredentialsProvider ?anywhereConfiguration ?computeType
        ?certificateConfiguration ?instanceRoleArn ?stoppedActions
        ?metricGroups ?resourceCreationLimitPolicy ?operatingSystem
        ?newGameSessionProtectionPolicy ?logPaths ?serverLaunchParameters
        ?serverLaunchPath ?scriptArn ?scriptId ?buildArn ?buildId ?status
        ?terminationTime ?creationTime ?name ?description ?instanceType
        ?fleetType ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerGatewayConfiguration =
        field_map json__ "PlayerGatewayConfiguration"
          PlayerGatewayConfiguration.of_json in
      let playerGatewayMode =
        field_map json__ "PlayerGatewayMode" PlayerGatewayMode.of_json in
      let instanceRoleCredentialsProvider =
        field_map json__ "InstanceRoleCredentialsProvider"
          InstanceRoleCredentialsProvider.of_json in
      let anywhereConfiguration =
        field_map json__ "AnywhereConfiguration"
          AnywhereConfiguration.of_json in
      let computeType = field_map json__ "ComputeType" ComputeType.of_json in
      let certificateConfiguration =
        field_map json__ "CertificateConfiguration"
          CertificateConfiguration.of_json in
      let instanceRoleArn =
        field_map json__ "InstanceRoleArn" NonEmptyString.of_json in
      let stoppedActions =
        field_map json__ "StoppedActions" FleetActionList.of_json in
      let metricGroups =
        field_map json__ "MetricGroups" MetricGroupList.of_json in
      let resourceCreationLimitPolicy =
        field_map json__ "ResourceCreationLimitPolicy"
          ResourceCreationLimitPolicy.of_json in
      let operatingSystem =
        field_map json__ "OperatingSystem" OperatingSystem.of_json in
      let newGameSessionProtectionPolicy =
        field_map json__ "NewGameSessionProtectionPolicy"
          ProtectionPolicy.of_json in
      let logPaths = field_map json__ "LogPaths" StringList.of_json in
      let serverLaunchParameters =
        field_map json__ "ServerLaunchParameters"
          LaunchParametersStringModel.of_json in
      let serverLaunchPath =
        field_map json__ "ServerLaunchPath" LaunchPathStringModel.of_json in
      let scriptArn = field_map json__ "ScriptArn" ScriptArn.of_json in
      let scriptId = field_map json__ "ScriptId" ScriptId.of_json in
      let buildArn = field_map json__ "BuildArn" BuildArn.of_json in
      let buildId = field_map json__ "BuildId" BuildId.of_json in
      let status = field_map json__ "Status" FleetStatus.of_json in
      let terminationTime =
        field_map json__ "TerminationTime" Timestamp.of_json in
      let creationTime = field_map json__ "CreationTime" Timestamp.of_json in
      let name = field_map json__ "Name" NonZeroAndMaxString.of_json in
      let description =
        field_map json__ "Description" NonZeroAndMaxString.of_json in
      let instanceType =
        field_map json__ "InstanceType" EC2InstanceType.of_json in
      let fleetType = field_map json__ "FleetType" FleetType.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?playerGatewayConfiguration ?playerGatewayMode
        ?instanceRoleCredentialsProvider ?anywhereConfiguration ?computeType
        ?certificateConfiguration ?instanceRoleArn ?stoppedActions
        ?metricGroups ?resourceCreationLimitPolicy ?operatingSystem
        ?newGameSessionProtectionPolicy ?logPaths ?serverLaunchParameters
        ?serverLaunchPath ?scriptArn ?scriptId ?buildArn ?buildId ?status
        ?terminationTime ?creationTime ?name ?description ?instanceType
        ?fleetType ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes an Amazon GameLift Servers fleet of game hosting resources. Attributes differ based on the fleet's compute type, as follows: EC2 fleet attributes identify a Build resource (for fleets with customer game server builds) or a Script resource (for Amazon GameLift Servers Realtime fleets). Amazon GameLift Servers Anywhere fleets have an abbreviated set of attributes, because most fleet configurations are set directly on the fleet's computes. Attributes include fleet identifiers and descriptive properties, creation/termination time, and fleet status. Returned by: https://docs.aws.amazon.com/gamelift/latest/apireference/API_DescribeFleetAttributes"]
module EC2InstanceLimit =
  struct
    type nonrec t =
      {
      eC2InstanceType: EC2InstanceType.t option
        [@ocaml.doc
          "The name of an Amazon EC2 instance type. See Amazon Elastic Compute Cloud Instance Types for detailed descriptions."];
      currentInstances: WholeNumber.t option
        [@ocaml.doc
          "The number of instances for the specified type and location that are currently being used by the Amazon Web Services account."];
      instanceLimit: WholeNumber.t option
        [@ocaml.doc
          "The number of instances that is allowed for the specified instance type and location."];
      location: LocationStringModel.t option
        [@ocaml.doc "An Amazon Web Services Region code, such as us-west-2."]}
    let make ?eC2InstanceType =
      fun ?currentInstances ->
        fun ?instanceLimit ->
          fun ?location ->
            fun () ->
              { eC2InstanceType; currentInstances; instanceLimit; location }
    let to_value x =
      structure_to_value
        [("EC2InstanceType",
           (Option.map x.eC2InstanceType ~f:EC2InstanceType.to_value));
        ("CurrentInstances",
          (Option.map x.currentInstances ~f:WholeNumber.to_value));
        ("InstanceLimit",
          (Option.map x.instanceLimit ~f:WholeNumber.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let instanceLimit =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "InstanceLimit") in
      let currentInstances =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "CurrentInstances") in
      let eC2InstanceType =
        (Option.map ~f:EC2InstanceType.of_xml)
          (Xml.child xml_arg0 "EC2InstanceType") in
      make ?location ?instanceLimit ?currentInstances ?eC2InstanceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let instanceLimit =
        field_map json__ "InstanceLimit" WholeNumber.of_json in
      let currentInstances =
        field_map json__ "CurrentInstances" WholeNumber.of_json in
      let eC2InstanceType =
        field_map json__ "EC2InstanceType" EC2InstanceType.of_json in
      make ?location ?instanceLimit ?currentInstances ?eC2InstanceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Amazon GameLift Servers service limits for an Amazon EC2 instance type and current utilization. Amazon GameLift Servers allows Amazon Web Services accounts a maximum number of instances, per instance type, per Amazon Web Services Region or location, for use with Amazon GameLift Servers. You can request an limit increase for your account by using the Service limits page in the Amazon GameLift Servers console."]
module ContainerGroupPortMapping =
  struct
    type nonrec t =
      {
      containerName: NonZeroAnd128MaxAsciiString.t option
        [@ocaml.doc
          "The name of the container, as defined in the container group definition."];
      containerRuntimeId: NonEmptyString.t option
        [@ocaml.doc
          "The runtime ID for the container that's running in a compute. This value is unique within the compute."];
      containerPortMappings: ContainerPortMappingList.t option
        [@ocaml.doc
          "A list of ContainerPortMapping objects that describe the port mappings for this container."]}
    let make ?containerName =
      fun ?containerRuntimeId ->
        fun ?containerPortMappings ->
          fun () ->
            { containerName; containerRuntimeId; containerPortMappings }
    let to_value x =
      structure_to_value
        [("ContainerName",
           (Option.map x.containerName
              ~f:NonZeroAnd128MaxAsciiString.to_value));
        ("ContainerRuntimeId",
          (Option.map x.containerRuntimeId ~f:NonEmptyString.to_value));
        ("ContainerPortMappings",
          (Option.map x.containerPortMappings
             ~f:ContainerPortMappingList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerPortMappings =
        (Option.map ~f:ContainerPortMappingList.of_xml)
          (Xml.child xml_arg0 "ContainerPortMappings") in
      let containerRuntimeId =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "ContainerRuntimeId") in
      let containerName =
        (Option.map ~f:NonZeroAnd128MaxAsciiString.of_xml)
          (Xml.child xml_arg0 "ContainerName") in
      make ?containerPortMappings ?containerRuntimeId ?containerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let containerPortMappings =
        field_map json__ "ContainerPortMappings"
          ContainerPortMappingList.of_json in
      let containerRuntimeId =
        field_map json__ "ContainerRuntimeId" NonEmptyString.of_json in
      let containerName =
        field_map json__ "ContainerName" NonZeroAnd128MaxAsciiString.of_json in
      make ?containerPortMappings ?containerRuntimeId ?containerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the port mappings for a single container in a container group. Each mapping shows how a container port maps to a connection port on the fleet instance. Returned by: DescribeContainerGroupPortMappings"]
module TargetTrackingConfiguration =
  struct
    type nonrec t =
      {
      targetValue: NonNegativeDouble.t
        [@ocaml.doc
          "Desired value to use with a game server group target-based scaling policy."]}
    let context_ = "TargetTrackingConfiguration"
    let make ~targetValue = fun () -> { targetValue }
    let to_value x =
      structure_to_value
        [("TargetValue", (Some (NonNegativeDouble.to_value x.targetValue)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetValue =
        NonNegativeDouble.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetValue") in
      make ~targetValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetValue =
        field_map_exn json__ "TargetValue" NonNegativeDouble.of_json in
      make ~targetValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Settings for a target-based scaling policy as part of a GameServerGroupAutoScalingPolicy . These settings are used to create a target-based policy that tracks the Amazon GameLift Servers FleetIQ metric \"PercentUtilizedGameServers\" and specifies a target value for the metric. As player usage changes, the policy triggers to adjust the game server group capacity so that the metric returns to the target value."]
module LaunchTemplateId =
  struct
    type nonrec t = string
    let context_ = "LaunchTemplateId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LaunchTemplateId" j
    let to_json = simple_to_json to_value
  end
module LaunchTemplateName =
  struct
    type nonrec t = string
    let context_ = "LaunchTemplateName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9\\(\\)\\.\\-/_]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LaunchTemplateName" j
    let to_json = simple_to_json to_value
  end
module LaunchTemplateVersion =
  struct
    type nonrec t = string
    let context_ = "LaunchTemplateVersion"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LaunchTemplateVersion" j
    let to_json = simple_to_json to_value
  end
module VpcSubnet =
  struct
    type nonrec t = string
    let context_ = "VpcSubnet"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:15) >>=
             (fun () ->
                (check_string_max i ~max:24) >>=
                  (fun () -> check_pattern i ~pattern:"^subnet-[0-9a-z]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"VpcSubnet" j
    let to_json = simple_to_json to_value
  end
module LocationConfiguration =
  struct
    type nonrec t =
      {
      location: LocationStringModel.t
        [@ocaml.doc
          "An Amazon Web Services Region code, such as us-west-2. For a list of supported Regions and Local Zones, see Amazon GameLift Servers service locations for managed hosting."]}
    let context_ = "LocationConfiguration"
    let make ~location = fun () -> { location }
    let to_value x =
      structure_to_value
        [("Location", (Some (LocationStringModel.to_value x.location)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        LocationStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Location") in
      make ~location ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location =
        field_map_exn json__ "Location" LocationStringModel.of_json in
      make ~location ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A remote location where a multi-location fleet can deploy game servers for game hosting."]
module FilterInstanceStatuses =
  struct
    type nonrec t = FilterInstanceStatus.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FilterInstanceStatus.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FilterInstanceStatus.of_xml)
    let of_json j =
      list_of_json ~kind:"FilterInstanceStatuses"
        ~of_json:FilterInstanceStatus.of_json j
    let to_json v = composed_to_json to_value v
  end
module InternalServiceException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The service encountered an unrecoverable internal failure while processing the request. Clients can retry such requests immediately or after a waiting period."]
module InvalidRequestException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "One or more parameter values in the request are invalid. Correct the invalid parameter values before retrying."]
module UnsupportedRegionException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested operation is not supported in the Region specified."]
module NotFoundException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested resources was not found. The resource was either not created yet or deleted."]
module UnauthorizedException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The client failed authentication. Clients should not retry such requests."]
module ScriptIdOrArn =
  struct
    type nonrec t = string
    let context_ = "ScriptIdOrArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"^script-\\S+|^arn:.*:script\\/script-\\S+");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ScriptIdOrArn" j
    let to_json = simple_to_json to_value
  end
module ZipBlob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module InvalidFleetStatusException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested operation would cause a conflict with the current state of a resource associated with the request and/or the fleet. Resolve the conflict before retrying."]
module LimitExceededException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested operation would cause the resource to exceed the allowed service limit. Resolve the issue before retrying."]
module RuntimeConfiguration =
  struct
    type nonrec t =
      {
      serverProcesses: ServerProcessList.t option
        [@ocaml.doc
          "A collection of server process configurations that identify what server processes to run on fleet computes."];
      maxConcurrentGameSessionActivations:
        MaxConcurrentGameSessionActivations.t option
        [@ocaml.doc
          "The number of game sessions in status ACTIVATING to allow on an instance or compute. This setting limits the instance resources that can be used for new game activations at any one time."];
      gameSessionActivationTimeoutSeconds:
        GameSessionActivationTimeoutSeconds.t option
        [@ocaml.doc
          "The maximum amount of time (in seconds) allowed to launch a new game session and have it report ready to host players. During this time, the game session is in status ACTIVATING. If the game session does not become active before the timeout, it is ended and the game session status is changed to TERMINATED."]}
    let make ?serverProcesses =
      fun ?maxConcurrentGameSessionActivations ->
        fun ?gameSessionActivationTimeoutSeconds ->
          fun () ->
            {
              serverProcesses;
              maxConcurrentGameSessionActivations;
              gameSessionActivationTimeoutSeconds
            }
    let to_value x =
      structure_to_value
        [("ServerProcesses",
           (Option.map x.serverProcesses ~f:ServerProcessList.to_value));
        ("MaxConcurrentGameSessionActivations",
          (Option.map x.maxConcurrentGameSessionActivations
             ~f:MaxConcurrentGameSessionActivations.to_value));
        ("GameSessionActivationTimeoutSeconds",
          (Option.map x.gameSessionActivationTimeoutSeconds
             ~f:GameSessionActivationTimeoutSeconds.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameSessionActivationTimeoutSeconds =
        (Option.map ~f:GameSessionActivationTimeoutSeconds.of_xml)
          (Xml.child xml_arg0 "GameSessionActivationTimeoutSeconds") in
      let maxConcurrentGameSessionActivations =
        (Option.map ~f:MaxConcurrentGameSessionActivations.of_xml)
          (Xml.child xml_arg0 "MaxConcurrentGameSessionActivations") in
      let serverProcesses =
        (Option.map ~f:ServerProcessList.of_xml)
          (Xml.child xml_arg0 "ServerProcesses") in
      make ?gameSessionActivationTimeoutSeconds
        ?maxConcurrentGameSessionActivations ?serverProcesses ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameSessionActivationTimeoutSeconds =
        field_map json__ "GameSessionActivationTimeoutSeconds"
          GameSessionActivationTimeoutSeconds.of_json in
      let maxConcurrentGameSessionActivations =
        field_map json__ "MaxConcurrentGameSessionActivations"
          MaxConcurrentGameSessionActivations.of_json in
      let serverProcesses =
        field_map json__ "ServerProcesses" ServerProcessList.of_json in
      make ?gameSessionActivationTimeoutSeconds
        ?maxConcurrentGameSessionActivations ?serverProcesses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A set of instructions that define the set of server processes to run on computes in a fleet. Server processes run either an executable in a custom game build or a Amazon GameLift Servers Realtime script. Amazon GameLift Servers launches the processes, manages their life cycle, and replaces them as needed. Computes check regularly for an updated runtime configuration. An Amazon GameLift Servers instance is limited to 50 processes running concurrently. To calculate the total number of processes defined in a runtime configuration, add the values of the ConcurrentExecutions parameter for each server process. Learn more about Running Multiple Processes on a Fleet."]
module ConflictException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested operation would cause a conflict with the current state of a service resource associated with the request. Resolve the conflict before retrying this request."]
module InvalidGameSessionStatusException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested operation would cause a conflict with the current state of a resource associated with the request and/or the game instance. Resolve the conflict before retrying."]
module NotReadyException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The operation failed because Amazon GameLift Servers has not yet finished validating this compute. We recommend attempting 8 to 10 retries over 3 to 5 minutes with exponential backoffs and jitter."]
module GameServerGroupNameOrArn =
  struct
    type nonrec t = string
    let context_ = "GameServerGroupNameOrArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[a-zA-Z0-9-\\.]+|^arn:.*:gameservergroup\\/[a-zA-Z0-9-\\.]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GameServerGroupNameOrArn" j
    let to_json = simple_to_json to_value
  end
module GameServerHealthCheck =
  struct
    type nonrec t =
      | HEALTHY 
      | Non_static_id of string 
    let make i = i
    let to_string = function | HEALTHY -> "HEALTHY" | Non_static_id s -> s
    let of_string = function | "HEALTHY" -> HEALTHY | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GameServerHealthCheck" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GameServerHealthCheck" j)
    let to_json = simple_to_json to_value
  end
module GameServerContainerDefinitionInput =
  struct
    type nonrec t =
      {
      containerName: NonZeroAnd128MaxAsciiString.t
        [@ocaml.doc
          "A string that uniquely identifies the container definition within a container group."];
      dependsOn: ContainerDependencyList.t option
        [@ocaml.doc
          "Establishes dependencies between this container and the status of other containers in the same container group. A container can have dependencies on multiple different containers. You can use dependencies to establish a startup/shutdown sequence across the container group. For example, you might specify that ContainerB has a START dependency on ContainerA. This dependency means that ContainerB can't start until after ContainerA has started. This dependency is reversed on shutdown, which means that ContainerB must shut down before ContainerA can shut down."];
      mountPoints: ContainerMountPointList.t option
        [@ocaml.doc
          "A mount point that binds a path inside the container to a file or directory on the host system and lets it access the file or directory."];
      environmentOverride: ContainerEnvironmentList.t option
        [@ocaml.doc
          "A set of environment variables to pass to the container on startup. See the ContainerDefinition::environment parameter in the Amazon Elastic Container Service API Reference."];
      imageUri: ImageUriString.t
        [@ocaml.doc
          "The location of the container image to deploy to a container fleet. Provide an image in an Amazon Elastic Container Registry public or private repository. The repository must be in the same Amazon Web Services account and Amazon Web Services Region where you're creating the container group definition. For limits on image size, see Amazon GameLift Servers endpoints and quotas. You can use any of the following image URI formats: Image ID only: \\[AWS account\\].dkr.ecr.\\[AWS region\\].amazonaws.com/\\[repository ID\\] Image ID and digest: \\[AWS account\\].dkr.ecr.\\[AWS region\\].amazonaws.com/\\[repository ID\\]\\@\\[digest\\] Image ID and tag: \\[AWS account\\].dkr.ecr.\\[AWS region\\].amazonaws.com/\\[repository ID\\]:\\[tag\\]"];
      portConfiguration: ContainerPortConfiguration.t
        [@ocaml.doc
          "A set of ports that Amazon GameLift Servers can assign to processes in a container. The container port configuration must have enough ports for each container process that accepts inbound traffic connections. For example, a game server process requires a container port to allow game clients to connect to it. A container port configuration can have can have one or more container port ranges. Each range specifies starting and ending values as well as the supported network protocol. Container ports aren't directly accessed by inbound traffic. Amazon GameLift Servers maps each container port to an externally accessible connection port (see the container fleet property ConnectionPortRange)."];
      serverSdkVersion: ServerSdkVersion.t
        [@ocaml.doc
          "The Amazon GameLift Servers server SDK version that the game server is integrated with. Only game servers using 5.2.0 or higher are compatible with container fleets."]}
    let context_ = "GameServerContainerDefinitionInput"
    let make ?dependsOn =
      fun ?mountPoints ->
        fun ?environmentOverride ->
          fun ~containerName ->
            fun ~imageUri ->
              fun ~portConfiguration ->
                fun ~serverSdkVersion ->
                  fun () ->
                    {
                      dependsOn;
                      mountPoints;
                      environmentOverride;
                      containerName;
                      imageUri;
                      portConfiguration;
                      serverSdkVersion
                    }
    let to_value x =
      structure_to_value
        [("ContainerName",
           (Some (NonZeroAnd128MaxAsciiString.to_value x.containerName)));
        ("DependsOn",
          (Option.map x.dependsOn ~f:ContainerDependencyList.to_value));
        ("MountPoints",
          (Option.map x.mountPoints ~f:ContainerMountPointList.to_value));
        ("EnvironmentOverride",
          (Option.map x.environmentOverride
             ~f:ContainerEnvironmentList.to_value));
        ("ImageUri", (Some (ImageUriString.to_value x.imageUri)));
        ("PortConfiguration",
          (Some (ContainerPortConfiguration.to_value x.portConfiguration)));
        ("ServerSdkVersion",
          (Some (ServerSdkVersion.to_value x.serverSdkVersion)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serverSdkVersion =
        ServerSdkVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerSdkVersion") in
      let portConfiguration =
        ContainerPortConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PortConfiguration") in
      let imageUri =
        ImageUriString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ImageUri") in
      let environmentOverride =
        (Option.map ~f:ContainerEnvironmentList.of_xml)
          (Xml.child xml_arg0 "EnvironmentOverride") in
      let mountPoints =
        (Option.map ~f:ContainerMountPointList.of_xml)
          (Xml.child xml_arg0 "MountPoints") in
      let dependsOn =
        (Option.map ~f:ContainerDependencyList.of_xml)
          (Xml.child xml_arg0 "DependsOn") in
      let containerName =
        NonZeroAnd128MaxAsciiString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~serverSdkVersion ~portConfiguration ~imageUri
        ?environmentOverride ?mountPoints ?dependsOn ~containerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serverSdkVersion =
        field_map_exn json__ "ServerSdkVersion" ServerSdkVersion.of_json in
      let portConfiguration =
        field_map_exn json__ "PortConfiguration"
          ContainerPortConfiguration.of_json in
      let imageUri = field_map_exn json__ "ImageUri" ImageUriString.of_json in
      let environmentOverride =
        field_map json__ "EnvironmentOverride"
          ContainerEnvironmentList.of_json in
      let mountPoints =
        field_map json__ "MountPoints" ContainerMountPointList.of_json in
      let dependsOn =
        field_map json__ "DependsOn" ContainerDependencyList.of_json in
      let containerName =
        field_map_exn json__ "ContainerName"
          NonZeroAnd128MaxAsciiString.of_json in
      make ~serverSdkVersion ~portConfiguration ~imageUri
        ?environmentOverride ?mountPoints ?dependsOn ~containerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the configuration for a container that runs your game server executable. This definition includes container configuration, resources, and start instructions. Use this data type when creating or updating a game server container group definition. For properties of a deployed container, see GameServerContainerDefinition. A game server container is automatically considered essential; if an essential container fails, the entire container group restarts. Use with: CreateContainerGroupDefinition, UpdateContainerGroupDefinition"]
module SupportContainerDefinitionInputList =
  struct
    type nonrec t = SupportContainerDefinitionInput.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SupportContainerDefinitionInput.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SupportContainerDefinitionInput.of_xml)
    let of_json j =
      list_of_json ~kind:"SupportContainerDefinitionInputList"
        ~of_json:SupportContainerDefinitionInput.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerFleetRemoveAttributeList =
  struct
    type nonrec t = ContainerFleetRemoveAttribute.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContainerFleetRemoveAttribute.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerFleetRemoveAttribute.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerFleetRemoveAttributeList"
        ~of_json:ContainerFleetRemoveAttribute.of_json j
    let to_json v = composed_to_json to_value v
  end
module BuildIdOrArn =
  struct
    type nonrec t = string
    let context_ = "BuildIdOrArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^build-\\S+|^arn:.*:build\\/build-\\S+");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BuildIdOrArn" j
    let to_json = simple_to_json to_value
  end
module AliasIdOrArn =
  struct
    type nonrec t = string
    let context_ = "AliasIdOrArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^alias-\\S+|^arn:.*:alias\\/alias-\\S+");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AliasIdOrArn" j
    let to_json = simple_to_json to_value
  end
module TaggingFailedException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested tagging operation did not succeed. This may be due to invalid tag format or the maximum tag limit may have been exceeded. Resolve the issue before retrying."]
module AmazonResourceName =
  struct
    type nonrec t = string
    let context_ = "AmazonResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1011) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AmazonResourceName" j
    let to_json = simple_to_json to_value
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module TerminationMode =
  struct
    type nonrec t =
      | TRIGGER_ON_PROCESS_TERMINATE 
      | FORCE_TERMINATE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TRIGGER_ON_PROCESS_TERMINATE -> "TRIGGER_ON_PROCESS_TERMINATE"
      | FORCE_TERMINATE -> "FORCE_TERMINATE"
      | Non_static_id s -> s
    let of_string =
      function
      | "TRIGGER_ON_PROCESS_TERMINATE" -> TRIGGER_ON_PROCESS_TERMINATE
      | "FORCE_TERMINATE" -> FORCE_TERMINATE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TerminationMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TerminationMode" j)
    let to_json = simple_to_json to_value
  end
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module GameSessionPlacement =
  struct
    type nonrec t =
      {
      placementId: IdStringModel.t option
        [@ocaml.doc "A unique identifier for a game session placement."];
      gameSessionQueueName: GameSessionQueueName.t option
        [@ocaml.doc
          "A descriptive label that is associated with game session queue. Queue names must be unique within each Region."];
      status: GameSessionPlacementState.t option
        [@ocaml.doc
          "Current status of the game session placement request. PENDING -- The placement request is in the queue waiting to be processed. Game session properties are not yet final. FULFILLED -- A new game session has been successfully placed. Game session properties are now final. CANCELLED -- The placement request was canceled. TIMED_OUT -- A new game session was not successfully created before the time limit expired. You can resubmit the placement request as needed. FAILED -- Amazon GameLift Servers is not able to complete the process of placing the game session. Common reasons are the game session terminated before the placement process was completed, or an unexpected internal error."];
      gameProperties: GamePropertyList.t option
        [@ocaml.doc
          "A set of key-value pairs that can store custom data in a game session. For example: \\{\"Key\": \"difficulty\", \"Value\": \"novice\"\\}. Avoid using periods (\".\") in property keys if you plan to search for game sessions by properties. Property keys containing periods cannot be searched and will be filtered out from search results due to search index limitations. If you use SearchGameSessions API, there is a limit of 500 game property keys across all game sessions and all fleets per region. If the limit is exceeded, there will potentially be game session entries missing from SearchGameSessions API results."];
      maximumPlayerSessionCount: WholeNumber.t option
        [@ocaml.doc
          "The maximum number of players that can be connected simultaneously to the game session."];
      gameSessionName: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a game session. Session names do not need to be unique."];
      gameSessionId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>. This value is the same as GameSessionArn. This value isn't final until placement status is FULFILLED."];
      gameSessionArn: NonZeroAndMaxString.t option
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>. This value is the same as GameSessionId. This value isn't final until placement status is FULFILLED."];
      gameSessionRegion: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Name of the Region where the game session created by this placement request is running. This value isn't final until placement status is FULFILLED."];
      playerLatencies: PlayerLatencyList.t option
        [@ocaml.doc
          "A set of values, expressed in milliseconds, that indicates the amount of latency that a player experiences when connected to Amazon Web Services Regions."];
      startTime: Timestamp.t option
        [@ocaml.doc
          "Time stamp indicating when this request was placed in the queue. Format is a number expressed in Unix time as milliseconds (for example \"1469498468.057\")."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "Time stamp indicating when this request was completed, canceled, or timed out."];
      ipAddress: IpAddress.t option
        [@ocaml.doc
          "The IP address of the game session. To connect to a Amazon GameLift Servers game server, an app needs both the IP address and port number. This value isn't final until placement status is FULFILLED."];
      dnsName: DnsName.t option
        [@ocaml.doc
          "The DNS identifier assigned to the instance that is running the game session. Values have the following format: TLS-enabled fleets: <unique identifier>.<region identifier>.amazongamelift.com. Non-TLS-enabled fleets: ec2-<unique identifier>.compute.amazonaws.com. (See Amazon EC2 Instance IP Addressing.) When connecting to a game session that is running on a TLS-enabled fleet, you must use the DNS name, not the IP address."];
      port: PortNumber.t option
        [@ocaml.doc
          "The port number for the game session. To connect to a Amazon GameLift Servers game server, an app needs both the IP address and port number. This value isn't final until placement status is FULFILLED."];
      placedPlayerSessions: PlacedPlayerSessionList.t option
        [@ocaml.doc
          "A collection of information on player sessions created in response to the game session placement request. These player sessions are created only after a new game session is successfully placed (placement status is FULFILLED). This information includes the player ID, provided in the placement request, and a corresponding player session ID."];
      gameSessionData: LargeGameSessionData.t option
        [@ocaml.doc
          "A set of custom game session properties, formatted as a single string value. This data is passed to a game server process with a request to start a new game session. For more information, see Start a game session."];
      matchmakerData: MatchmakerData.t option
        [@ocaml.doc
          "Information on the matchmaking process for this game. Data is in JSON syntax, formatted as a string. It identifies the matchmaking configuration used to create the match, and contains data on all players assigned to the match, including player attributes and team assignments. For more details on matchmaker data, see Match Data."];
      priorityConfigurationOverride: PriorityConfigurationOverride.t option
        [@ocaml.doc
          "An alternative priority list of locations that's included with a game session placement request. When provided, the list overrides a queue's location order list for this game session placement request only. The list might include Amazon Web Services Regions, local zones, and custom locations (for Anywhere fleets). The fallback strategy tells Amazon GameLift Servers what action to take (if any) in the event that it failed to place a new game session."];
      playerGatewayStatus: PlayerGatewayStatus.t option
        [@ocaml.doc
          "The current status of player gateway for the game session placement. Note, even if a fleet has PlayerGatewayMode configured as ENABLED, player gateway might not be available in a specific location. For more information about locations where player gateway is supported, see Amazon GameLift Servers service locations. Possible values include: ENABLED -- Player gateway is available for this game session placement. DISABLED -- Player gateway is not available for this game session placement."]}
    let make ?placementId =
      fun ?gameSessionQueueName ->
        fun ?status ->
          fun ?gameProperties ->
            fun ?maximumPlayerSessionCount ->
              fun ?gameSessionName ->
                fun ?gameSessionId ->
                  fun ?gameSessionArn ->
                    fun ?gameSessionRegion ->
                      fun ?playerLatencies ->
                        fun ?startTime ->
                          fun ?endTime ->
                            fun ?ipAddress ->
                              fun ?dnsName ->
                                fun ?port ->
                                  fun ?placedPlayerSessions ->
                                    fun ?gameSessionData ->
                                      fun ?matchmakerData ->
                                        fun ?priorityConfigurationOverride ->
                                          fun ?playerGatewayStatus ->
                                            fun () ->
                                              {
                                                placementId;
                                                gameSessionQueueName;
                                                status;
                                                gameProperties;
                                                maximumPlayerSessionCount;
                                                gameSessionName;
                                                gameSessionId;
                                                gameSessionArn;
                                                gameSessionRegion;
                                                playerLatencies;
                                                startTime;
                                                endTime;
                                                ipAddress;
                                                dnsName;
                                                port;
                                                placedPlayerSessions;
                                                gameSessionData;
                                                matchmakerData;
                                                priorityConfigurationOverride;
                                                playerGatewayStatus
                                              }
    let to_value x =
      structure_to_value
        [("PlacementId",
           (Option.map x.placementId ~f:IdStringModel.to_value));
        ("GameSessionQueueName",
          (Option.map x.gameSessionQueueName ~f:GameSessionQueueName.to_value));
        ("Status",
          (Option.map x.status ~f:GameSessionPlacementState.to_value));
        ("GameProperties",
          (Option.map x.gameProperties ~f:GamePropertyList.to_value));
        ("MaximumPlayerSessionCount",
          (Option.map x.maximumPlayerSessionCount ~f:WholeNumber.to_value));
        ("GameSessionName",
          (Option.map x.gameSessionName ~f:NonZeroAndMaxString.to_value));
        ("GameSessionId",
          (Option.map x.gameSessionId ~f:NonZeroAndMaxString.to_value));
        ("GameSessionArn",
          (Option.map x.gameSessionArn ~f:NonZeroAndMaxString.to_value));
        ("GameSessionRegion",
          (Option.map x.gameSessionRegion ~f:NonZeroAndMaxString.to_value));
        ("PlayerLatencies",
          (Option.map x.playerLatencies ~f:PlayerLatencyList.to_value));
        ("StartTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("EndTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("IpAddress", (Option.map x.ipAddress ~f:IpAddress.to_value));
        ("DnsName", (Option.map x.dnsName ~f:DnsName.to_value));
        ("Port", (Option.map x.port ~f:PortNumber.to_value));
        ("PlacedPlayerSessions",
          (Option.map x.placedPlayerSessions
             ~f:PlacedPlayerSessionList.to_value));
        ("GameSessionData",
          (Option.map x.gameSessionData ~f:LargeGameSessionData.to_value));
        ("MatchmakerData",
          (Option.map x.matchmakerData ~f:MatchmakerData.to_value));
        ("PriorityConfigurationOverride",
          (Option.map x.priorityConfigurationOverride
             ~f:PriorityConfigurationOverride.to_value));
        ("PlayerGatewayStatus",
          (Option.map x.playerGatewayStatus ~f:PlayerGatewayStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerGatewayStatus =
        (Option.map ~f:PlayerGatewayStatus.of_xml)
          (Xml.child xml_arg0 "PlayerGatewayStatus") in
      let priorityConfigurationOverride =
        (Option.map ~f:PriorityConfigurationOverride.of_xml)
          (Xml.child xml_arg0 "PriorityConfigurationOverride") in
      let matchmakerData =
        (Option.map ~f:MatchmakerData.of_xml)
          (Xml.child xml_arg0 "MatchmakerData") in
      let gameSessionData =
        (Option.map ~f:LargeGameSessionData.of_xml)
          (Xml.child xml_arg0 "GameSessionData") in
      let placedPlayerSessions =
        (Option.map ~f:PlacedPlayerSessionList.of_xml)
          (Xml.child xml_arg0 "PlacedPlayerSessions") in
      let port =
        (Option.map ~f:PortNumber.of_xml) (Xml.child xml_arg0 "Port") in
      let dnsName =
        (Option.map ~f:DnsName.of_xml) (Xml.child xml_arg0 "DnsName") in
      let ipAddress =
        (Option.map ~f:IpAddress.of_xml) (Xml.child xml_arg0 "IpAddress") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "StartTime") in
      let playerLatencies =
        (Option.map ~f:PlayerLatencyList.of_xml)
          (Xml.child xml_arg0 "PlayerLatencies") in
      let gameSessionRegion =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "GameSessionRegion") in
      let gameSessionArn =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "GameSessionArn") in
      let gameSessionId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "GameSessionId") in
      let gameSessionName =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "GameSessionName") in
      let maximumPlayerSessionCount =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "MaximumPlayerSessionCount") in
      let gameProperties =
        (Option.map ~f:GamePropertyList.of_xml)
          (Xml.child xml_arg0 "GameProperties") in
      let status =
        (Option.map ~f:GameSessionPlacementState.of_xml)
          (Xml.child xml_arg0 "Status") in
      let gameSessionQueueName =
        (Option.map ~f:GameSessionQueueName.of_xml)
          (Xml.child xml_arg0 "GameSessionQueueName") in
      let placementId =
        (Option.map ~f:IdStringModel.of_xml)
          (Xml.child xml_arg0 "PlacementId") in
      make ?playerGatewayStatus ?priorityConfigurationOverride
        ?matchmakerData ?gameSessionData ?placedPlayerSessions ?port ?dnsName
        ?ipAddress ?endTime ?startTime ?playerLatencies ?gameSessionRegion
        ?gameSessionArn ?gameSessionId ?gameSessionName
        ?maximumPlayerSessionCount ?gameProperties ?status
        ?gameSessionQueueName ?placementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerGatewayStatus =
        field_map json__ "PlayerGatewayStatus" PlayerGatewayStatus.of_json in
      let priorityConfigurationOverride =
        field_map json__ "PriorityConfigurationOverride"
          PriorityConfigurationOverride.of_json in
      let matchmakerData =
        field_map json__ "MatchmakerData" MatchmakerData.of_json in
      let gameSessionData =
        field_map json__ "GameSessionData" LargeGameSessionData.of_json in
      let placedPlayerSessions =
        field_map json__ "PlacedPlayerSessions"
          PlacedPlayerSessionList.of_json in
      let port = field_map json__ "Port" PortNumber.of_json in
      let dnsName = field_map json__ "DnsName" DnsName.of_json in
      let ipAddress = field_map json__ "IpAddress" IpAddress.of_json in
      let endTime = field_map json__ "EndTime" Timestamp.of_json in
      let startTime = field_map json__ "StartTime" Timestamp.of_json in
      let playerLatencies =
        field_map json__ "PlayerLatencies" PlayerLatencyList.of_json in
      let gameSessionRegion =
        field_map json__ "GameSessionRegion" NonZeroAndMaxString.of_json in
      let gameSessionArn =
        field_map json__ "GameSessionArn" NonZeroAndMaxString.of_json in
      let gameSessionId =
        field_map json__ "GameSessionId" NonZeroAndMaxString.of_json in
      let gameSessionName =
        field_map json__ "GameSessionName" NonZeroAndMaxString.of_json in
      let maximumPlayerSessionCount =
        field_map json__ "MaximumPlayerSessionCount" WholeNumber.of_json in
      let gameProperties =
        field_map json__ "GameProperties" GamePropertyList.of_json in
      let status =
        field_map json__ "Status" GameSessionPlacementState.of_json in
      let gameSessionQueueName =
        field_map json__ "GameSessionQueueName" GameSessionQueueName.of_json in
      let placementId = field_map json__ "PlacementId" IdStringModel.of_json in
      make ?playerGatewayStatus ?priorityConfigurationOverride
        ?matchmakerData ?gameSessionData ?placedPlayerSessions ?port ?dnsName
        ?ipAddress ?endTime ?startTime ?playerLatencies ?gameSessionRegion
        ?gameSessionArn ?gameSessionId ?gameSessionName
        ?maximumPlayerSessionCount ?gameProperties ?status
        ?gameSessionQueueName ?placementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a potential game session placement, including the full details of the original placement request and the current status. If the game session placement status is PENDING, the properties for game session ID/ARN, region, IP address/DNS, and port aren't final. A game session is not active and ready to accept players until placement status reaches FULFILLED. When the placement is in PENDING status, Amazon GameLift Servers may attempt to place a game session multiple times before succeeding. With each attempt it creates a https://docs.aws.amazon.com/gamelift/latest/apireference/API_GameSession object and updates this placement object with the new game session properties."]
module DesiredPlayerSessionList =
  struct
    type nonrec t = DesiredPlayerSession.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DesiredPlayerSession.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DesiredPlayerSession.of_xml)
    let of_json j =
      list_of_json ~kind:"DesiredPlayerSessionList"
        ~of_json:DesiredPlayerSession.of_json j
    let to_json v = composed_to_json to_value v
  end
module GameSessionList =
  struct
    type nonrec t = GameSession.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GameSession.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GameSession.of_xml)
    let of_json j =
      list_of_json ~kind:"GameSessionList" ~of_json:GameSession.of_json j
    let to_json v = composed_to_json to_value v
  end
module TerminalRoutingStrategyException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The service is unable to resolve the routing for a particular alias because it has a terminal RoutingStrategy associated with it. The message returned in this exception is the message defined in the routing strategy itself. Such requests should only be retried if the routing strategy for the specified alias is modified."]
module AwsCredentials =
  struct
    type nonrec t =
      {
      accessKeyId: NonEmptyString.t option
        [@ocaml.doc
          "The access key ID that identifies the temporary security credentials."];
      secretAccessKey: NonEmptyString.t option
        [@ocaml.doc
          "The secret access key that can be used to sign requests."];
      sessionToken: NonEmptyString.t option
        [@ocaml.doc
          "The token that users must pass to the service API to use the temporary credentials."]}
    let make ?accessKeyId =
      fun ?secretAccessKey ->
        fun ?sessionToken ->
          fun () -> { accessKeyId; secretAccessKey; sessionToken }
    let to_value x =
      structure_to_value
        [("AccessKeyId",
           (Option.map x.accessKeyId ~f:NonEmptyString.to_value));
        ("SecretAccessKey",
          (Option.map x.secretAccessKey ~f:NonEmptyString.to_value));
        ("SessionToken",
          (Option.map x.sessionToken ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionToken =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "SessionToken") in
      let secretAccessKey =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "SecretAccessKey") in
      let accessKeyId =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "AccessKeyId") in
      make ?sessionToken ?secretAccessKey ?accessKeyId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionToken =
        field_map json__ "SessionToken" NonEmptyString.of_json in
      let secretAccessKey =
        field_map json__ "SecretAccessKey" NonEmptyString.of_json in
      let accessKeyId = field_map json__ "AccessKeyId" NonEmptyString.of_json in
      make ?sessionToken ?secretAccessKey ?accessKeyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Amazon Web Services account security credentials that allow interactions with Amazon GameLift Servers resources. The credentials are temporary and valid for a limited time span. You can request fresh credentials at any time. Amazon Web Services security credentials consist of three parts: an access key ID, a secret access key, and a session token. You must use all three parts together to authenticate your access requests. You need Amazon Web Services credentials for the following tasks: To upload a game server build directly to Amazon GameLift Servers S3 storage using CreateBuild. To get access for this task, call https://docs.aws.amazon.com/gamelift/latest/apireference/API_RequestUploadCredentials.html. To remotely connect to an active Amazon GameLift Servers fleet instances. To get remote access, call https://docs.aws.amazon.com/gamelift/latest/apireference/API_GetComputeAccess.html."]
module DnsNameInput =
  struct
    type nonrec t = string
    let context_ = "DnsNameInput"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"^[0-9a-zA-Z_\\-\\.]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DnsNameInput" j
    let to_json = simple_to_json to_value
  end
module ScriptList =
  struct
    type nonrec t = Script.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Script.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Script.of_xml)
    let of_json j = list_of_json ~kind:"ScriptList" ~of_json:Script.of_json j
    let to_json v = composed_to_json to_value v
  end
module LocationModelList =
  struct
    type nonrec t = LocationModel.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:LocationModel.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LocationModel.of_xml)
    let of_json j =
      list_of_json ~kind:"LocationModelList" ~of_json:LocationModel.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListLocationsLimit =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:200) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ListLocationsLimit" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module LocationFilterList =
  struct
    type nonrec t = LocationFilter.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:2) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:LocationFilter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LocationFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"LocationFilterList" ~of_json:LocationFilter.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module GameServers =
  struct
    type nonrec t = GameServer.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GameServer.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GameServer.of_xml)
    let of_json j =
      list_of_json ~kind:"GameServers" ~of_json:GameServer.of_json j
    let to_json v = composed_to_json to_value v
  end
module SortOrder =
  struct
    type nonrec t =
      | ASCENDING 
      | DESCENDING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ASCENDING -> "ASCENDING"
      | DESCENDING -> "DESCENDING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ASCENDING" -> ASCENDING
      | "DESCENDING" -> DESCENDING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SortOrder" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SortOrder" j)
    let to_json = simple_to_json to_value
  end
module GameServerGroups =
  struct
    type nonrec t = GameServerGroup.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GameServerGroup.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GameServerGroup.of_xml)
    let of_json j =
      list_of_json ~kind:"GameServerGroups" ~of_json:GameServerGroup.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module FleetIdList =
  struct
    type nonrec t = FleetId.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FleetId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FleetId.of_xml)
    let of_json j =
      list_of_json ~kind:"FleetIdList" ~of_json:FleetId.of_json j
    let to_json v = composed_to_json to_value v
  end
module FleetDeployments =
  struct
    type nonrec t = FleetDeployment.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FleetDeployment.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FleetDeployment.of_xml)
    let of_json j =
      list_of_json ~kind:"FleetDeployments" ~of_json:FleetDeployment.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ContainerGroupDefinitionList =
  struct
    type nonrec t = ContainerGroupDefinition.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContainerGroupDefinition.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerGroupDefinition.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerGroupDefinitionList"
        ~of_json:ContainerGroupDefinition.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListContainerGroupDefinitionsLimit =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListContainerGroupDefinitionsLimit" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ListContainerGroupDefinitionVersionsLimit =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListContainerGroupDefinitionVersionsLimit"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ContainerFleetList =
  struct
    type nonrec t = ContainerFleet.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContainerFleet.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerFleet.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerFleetList" ~of_json:ContainerFleet.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ComputeList =
  struct
    type nonrec t = Compute.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Compute.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Compute.of_xml)
    let of_json j =
      list_of_json ~kind:"ComputeList" ~of_json:Compute.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListComputeInputStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | IMPAIRED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | IMPAIRED -> "IMPAIRED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "IMPAIRED" -> IMPAIRED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ListComputeInputStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ListComputeInputStatus" j)
    let to_json = simple_to_json to_value
  end
module BuildList =
  struct
    type nonrec t = Build.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Build.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Build.of_xml)
    let of_json j = list_of_json ~kind:"BuildList" ~of_json:Build.of_json j
    let to_json v = composed_to_json to_value v
  end
module AliasList =
  struct
    type nonrec t = Alias.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Alias.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Alias.of_xml)
    let of_json j = list_of_json ~kind:"AliasList" ~of_json:Alias.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlayerConnectionDetailList =
  struct
    type nonrec t = PlayerConnectionDetail.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PlayerConnectionDetail.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlayerConnectionDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"PlayerConnectionDetailList"
        ~of_json:PlayerConnectionDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlayerIdList =
  struct
    type nonrec t = PlayerId.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:25) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PlayerId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlayerId.of_xml)
    let of_json j =
      list_of_json ~kind:"PlayerIdList" ~of_json:PlayerId.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceAccess =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc
          "A unique identifier for the fleet containing the instance to be accessed."];
      instanceId: InstanceId.t option
        [@ocaml.doc "A unique identifier for the instance to be accessed."];
      ipAddress: IpAddress.t option
        [@ocaml.doc "IP address assigned to the instance."];
      operatingSystem: OperatingSystem.t option
        [@ocaml.doc "Operating system that is running on the instance."];
      credentials: InstanceCredentials.t option
        [@ocaml.doc
          "Security credentials that are required to access the instance."]}
    let make ?fleetId =
      fun ?instanceId ->
        fun ?ipAddress ->
          fun ?operatingSystem ->
            fun ?credentials ->
              fun () ->
                {
                  fleetId;
                  instanceId;
                  ipAddress;
                  operatingSystem;
                  credentials
                }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("InstanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("IpAddress", (Option.map x.ipAddress ~f:IpAddress.to_value));
        ("OperatingSystem",
          (Option.map x.operatingSystem ~f:OperatingSystem.to_value));
        ("Credentials",
          (Option.map x.credentials ~f:InstanceCredentials.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let credentials =
        (Option.map ~f:InstanceCredentials.of_xml)
          (Xml.child xml_arg0 "Credentials") in
      let operatingSystem =
        (Option.map ~f:OperatingSystem.of_xml)
          (Xml.child xml_arg0 "OperatingSystem") in
      let ipAddress =
        (Option.map ~f:IpAddress.of_xml) (Xml.child xml_arg0 "IpAddress") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "InstanceId") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?credentials ?operatingSystem ?ipAddress ?instanceId ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let credentials =
        field_map json__ "Credentials" InstanceCredentials.of_json in
      let operatingSystem =
        field_map json__ "OperatingSystem" OperatingSystem.of_json in
      let ipAddress = field_map json__ "IpAddress" IpAddress.of_json in
      let instanceId = field_map json__ "InstanceId" InstanceId.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?credentials ?operatingSystem ?ipAddress ?instanceId ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information and credentials that you can use to remotely connect to an instance in an EC2 managed fleet. This data type is returned in response to a call to https://docs.aws.amazon.com/gamelift/latest/apireference/API_GetInstanceAccess."]
module ComputeAuthToken =
  struct
    type nonrec t = string
    let context_ = "ComputeAuthToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9\\-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ComputeAuthToken" j
    let to_json = simple_to_json to_value
  end
module ComputeNameOrArn =
  struct
    type nonrec t = string
    let context_ = "ComputeNameOrArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"^[a-zA-Z0-9\\-]+(\\/[a-zA-Z0-9\\-]+)?$|^arn:.*:compute\\/[a-zA-Z0-9\\-]+(\\/[a-zA-Z0-9\\-]+)?"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ComputeNameOrArn" j
    let to_json = simple_to_json to_value
  end
module ContainerIdentifierList =
  struct
    type nonrec t = ContainerIdentifier.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContainerIdentifier.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerIdentifier.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerIdentifierList"
        ~of_json:ContainerIdentifier.of_json j
    let to_json v = composed_to_json to_value v
  end
module SessionTarget =
  struct
    type nonrec t = string
    let context_ = "SessionTarget"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:400) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SessionTarget" j
    let to_json = simple_to_json to_value
  end
module VpcPeeringConnectionList =
  struct
    type nonrec t = VpcPeeringConnection.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:VpcPeeringConnection.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:VpcPeeringConnection.of_xml)
    let of_json j =
      list_of_json ~kind:"VpcPeeringConnectionList"
        ~of_json:VpcPeeringConnection.of_json j
    let to_json v = composed_to_json to_value v
  end
module VpcPeeringAuthorizationList =
  struct
    type nonrec t = VpcPeeringAuthorization.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:VpcPeeringAuthorization.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:VpcPeeringAuthorization.of_xml)
    let of_json j =
      list_of_json ~kind:"VpcPeeringAuthorizationList"
        ~of_json:VpcPeeringAuthorization.of_json j
    let to_json v = composed_to_json to_value v
  end
module ScalingPolicyList =
  struct
    type nonrec t = ScalingPolicy.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ScalingPolicy.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ScalingPolicy.of_xml)
    let of_json j =
      list_of_json ~kind:"ScalingPolicyList" ~of_json:ScalingPolicy.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlayerSessionList =
  struct
    type nonrec t = PlayerSession.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PlayerSession.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlayerSession.of_xml)
    let of_json j =
      list_of_json ~kind:"PlayerSessionList" ~of_json:PlayerSession.of_json j
    let to_json v = composed_to_json to_value v
  end
module MatchmakingRuleSetList =
  struct
    type nonrec t = MatchmakingRuleSet.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MatchmakingRuleSet.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MatchmakingRuleSet.of_xml)
    let of_json j =
      list_of_json ~kind:"MatchmakingRuleSetList"
        ~of_json:MatchmakingRuleSet.of_json j
    let to_json v = composed_to_json to_value v
  end
module MatchmakingRuleSetNameList =
  struct
    type nonrec t = MatchmakingRuleSetName.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MatchmakingRuleSetName.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MatchmakingRuleSetName.of_xml)
    let of_json j =
      list_of_json ~kind:"MatchmakingRuleSetNameList"
        ~of_json:MatchmakingRuleSetName.of_json j
    let to_json v = composed_to_json to_value v
  end
module RuleSetLimit =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:10) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for RuleSetLimit" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MatchmakingTicketList =
  struct
    type nonrec t = MatchmakingTicket.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MatchmakingTicket.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MatchmakingTicket.of_xml)
    let of_json j =
      list_of_json ~kind:"MatchmakingTicketList"
        ~of_json:MatchmakingTicket.of_json j
    let to_json v = composed_to_json to_value v
  end
module MatchmakingIdList =
  struct
    type nonrec t = MatchmakingIdStringModel.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MatchmakingIdStringModel.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MatchmakingIdStringModel.of_xml)
    let of_json j =
      list_of_json ~kind:"MatchmakingIdList"
        ~of_json:MatchmakingIdStringModel.of_json j
    let to_json v = composed_to_json to_value v
  end
module MatchmakingConfigurationList =
  struct
    type nonrec t = MatchmakingConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MatchmakingConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MatchmakingConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"MatchmakingConfigurationList"
        ~of_json:MatchmakingConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module MatchmakingConfigurationNameList =
  struct
    type nonrec t = MatchmakingConfigurationName.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MatchmakingConfigurationName.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MatchmakingConfigurationName.of_xml)
    let of_json j =
      list_of_json ~kind:"MatchmakingConfigurationNameList"
        ~of_json:MatchmakingConfigurationName.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceList =
  struct
    type nonrec t = Instance.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Instance.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Instance.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceList" ~of_json:Instance.of_json j
    let to_json v = composed_to_json to_value v
  end
module GameSessionQueueList =
  struct
    type nonrec t = GameSessionQueue.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GameSessionQueue.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GameSessionQueue.of_xml)
    let of_json j =
      list_of_json ~kind:"GameSessionQueueList"
        ~of_json:GameSessionQueue.of_json j
    let to_json v = composed_to_json to_value v
  end
module GameSessionQueueNameOrArnList =
  struct
    type nonrec t = GameSessionQueueNameOrArn.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GameSessionQueueNameOrArn.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GameSessionQueueNameOrArn.of_xml)
    let of_json j =
      list_of_json ~kind:"GameSessionQueueNameOrArnList"
        ~of_json:GameSessionQueueNameOrArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module GameSessionDetailList =
  struct
    type nonrec t = GameSessionDetail.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GameSessionDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GameSessionDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"GameSessionDetailList"
        ~of_json:GameSessionDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module GameServerInstances =
  struct
    type nonrec t = GameServerInstance.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GameServerInstance.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GameServerInstance.of_xml)
    let of_json j =
      list_of_json ~kind:"GameServerInstances"
        ~of_json:GameServerInstance.of_json j
    let to_json v = composed_to_json to_value v
  end
module GameServerInstanceIds =
  struct
    type nonrec t = GameServerInstanceId.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:20) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GameServerInstanceId.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GameServerInstanceId.of_xml)
    let of_json j =
      list_of_json ~kind:"GameServerInstanceIds"
        ~of_json:GameServerInstanceId.of_json j
    let to_json v = composed_to_json to_value v
  end
module FleetUtilizationList =
  struct
    type nonrec t = FleetUtilization.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FleetUtilization.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FleetUtilization.of_xml)
    let of_json j =
      list_of_json ~kind:"FleetUtilizationList"
        ~of_json:FleetUtilization.of_json j
    let to_json v = composed_to_json to_value v
  end
module FleetIdOrArnList =
  struct
    type nonrec t = FleetIdOrArn.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FleetIdOrArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FleetIdOrArn.of_xml)
    let of_json j =
      list_of_json ~kind:"FleetIdOrArnList" ~of_json:FleetIdOrArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module LocationAttributesList =
  struct
    type nonrec t = LocationAttributes.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:LocationAttributes.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LocationAttributes.of_xml)
    let of_json j =
      list_of_json ~kind:"LocationAttributesList"
        ~of_json:LocationAttributes.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventList =
  struct
    type nonrec t = Event.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Event.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Event.of_xml)
    let of_json j = list_of_json ~kind:"EventList" ~of_json:Event.of_json j
    let to_json v = composed_to_json to_value v
  end
module LocationalDeployments =
  struct
    type nonrec t =
      (NonZeroAnd128MaxAsciiString.t * LocationalDeployment.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types NonZeroAnd128MaxAsciiString LocationalDeployment"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (NonZeroAnd128MaxAsciiString.to_value x) |>
                    (fun x ->
                       (LocationalDeployment.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:NonZeroAnd128MaxAsciiString.of_string
        ~of_json:LocationalDeployment.of_json j
    let to_json v = composed_to_json to_value v
  end
module FleetCapacityList =
  struct
    type nonrec t = FleetCapacity.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FleetCapacity.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FleetCapacity.of_xml)
    let of_json j =
      list_of_json ~kind:"FleetCapacityList" ~of_json:FleetCapacity.of_json j
    let to_json v = composed_to_json to_value v
  end
module FleetAttributesList =
  struct
    type nonrec t = FleetAttributes.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FleetAttributes.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FleetAttributes.of_xml)
    let of_json j =
      list_of_json ~kind:"FleetAttributesList"
        ~of_json:FleetAttributes.of_json j
    let to_json v = composed_to_json to_value v
  end
module EC2InstanceLimitList =
  struct
    type nonrec t = EC2InstanceLimit.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EC2InstanceLimit.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EC2InstanceLimit.of_xml)
    let of_json j =
      list_of_json ~kind:"EC2InstanceLimitList"
        ~of_json:EC2InstanceLimit.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerGroupPortMappingList =
  struct
    type nonrec t = ContainerGroupPortMapping.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContainerGroupPortMapping.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerGroupPortMapping.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerGroupPortMappingList"
        ~of_json:ContainerGroupPortMapping.of_json j
    let to_json v = composed_to_json to_value v
  end
module CustomLocationNameOrArnModel =
  struct
    type nonrec t = string
    let context_ = "CustomLocationNameOrArnModel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^custom-[A-Za-z0-9\\-]+|^arn:.*:location\\/custom-\\S+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CustomLocationNameOrArnModel" j
    let to_json = simple_to_json to_value
  end
module GameServerGroupDeleteOption =
  struct
    type nonrec t =
      | SAFE_DELETE 
      | FORCE_DELETE 
      | RETAIN 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SAFE_DELETE -> "SAFE_DELETE"
      | FORCE_DELETE -> "FORCE_DELETE"
      | RETAIN -> "RETAIN"
      | Non_static_id s -> s
    let of_string =
      function
      | "SAFE_DELETE" -> SAFE_DELETE
      | "FORCE_DELETE" -> FORCE_DELETE
      | "RETAIN" -> RETAIN
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GameServerGroupDeleteOption"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GameServerGroupDeleteOption" j)
    let to_json = simple_to_json to_value
  end
module LocationStateList =
  struct
    type nonrec t = LocationState.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:LocationState.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LocationState.of_xml)
    let of_json j =
      list_of_json ~kind:"LocationStateList" ~of_json:LocationState.of_json j
    let to_json v = composed_to_json to_value v
  end
module GameSessionFullException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The game instance is currently full and cannot allow the requested player(s) to join. Clients can retry such requests immediately or after a waiting period."]
module PlayerDataMap =
  struct
    type nonrec t = (NonZeroAndMaxString.t * PlayerData.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((NonZeroAndMaxString.of_string chopped),
                              (PlayerData.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (NonZeroAndMaxString.to_value x) |>
                    (fun x -> (PlayerData.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:NonZeroAndMaxString.of_string
        ~of_json:PlayerData.of_json j
    let to_json v = composed_to_json to_value v
  end
module CustomInputLocationStringModel =
  struct
    type nonrec t = string
    let context_ = "CustomInputLocationStringModel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:8) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^custom-[A-Za-z0-9\\-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CustomInputLocationStringModel" j
    let to_json = simple_to_json to_value
  end
module FleetCapacityExceededException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified fleet has no available instances to fulfill a CreateGameSession request. Clients can retry such requests immediately or after a waiting period."]
module IdempotentParameterMismatchException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A game session with this custom ID string already exists in this fleet. Resolve this conflict before retrying this request."]
module GameServerGroupAutoScalingPolicy =
  struct
    type nonrec t =
      {
      estimatedInstanceWarmup: PositiveInteger.t option
        [@ocaml.doc
          "Length of time, in seconds, it takes for a new instance to start new game server processes and register with Amazon GameLift Servers FleetIQ. Specifying a warm-up time can be useful, particularly with game servers that take a long time to start up, because it avoids prematurely starting new instances."];
      targetTrackingConfiguration: TargetTrackingConfiguration.t
        [@ocaml.doc
          "Settings for a target-based scaling policy applied to Auto Scaling group. These settings are used to create a target-based policy that tracks the Amazon GameLift Servers FleetIQ metric \"PercentUtilizedGameServers\" and specifies a target value for the metric. As player usage changes, the policy triggers to adjust the game server group capacity so that the metric returns to the target value."]}
    let context_ = "GameServerGroupAutoScalingPolicy"
    let make ?estimatedInstanceWarmup =
      fun ~targetTrackingConfiguration ->
        fun () -> { estimatedInstanceWarmup; targetTrackingConfiguration }
    let to_value x =
      structure_to_value
        [("EstimatedInstanceWarmup",
           (Option.map x.estimatedInstanceWarmup ~f:PositiveInteger.to_value));
        ("TargetTrackingConfiguration",
          (Some
             (TargetTrackingConfiguration.to_value
                x.targetTrackingConfiguration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetTrackingConfiguration =
        TargetTrackingConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TargetTrackingConfiguration") in
      let estimatedInstanceWarmup =
        (Option.map ~f:PositiveInteger.of_xml)
          (Xml.child xml_arg0 "EstimatedInstanceWarmup") in
      make ~targetTrackingConfiguration ?estimatedInstanceWarmup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetTrackingConfiguration =
        field_map_exn json__ "TargetTrackingConfiguration"
          TargetTrackingConfiguration.of_json in
      let estimatedInstanceWarmup =
        field_map json__ "EstimatedInstanceWarmup" PositiveInteger.of_json in
      make ~targetTrackingConfiguration ?estimatedInstanceWarmup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration settings for intelligent automatic scaling that uses target tracking. These settings are used to add an Auto Scaling policy when creating the corresponding Auto Scaling group. After the Auto Scaling group is created, all updates to Auto Scaling policies, including changing this policy and adding or removing other policies, is done directly on the Auto Scaling group."]
module LaunchTemplateSpecification =
  struct
    type nonrec t =
      {
      launchTemplateId: LaunchTemplateId.t option
        [@ocaml.doc
          "A unique identifier for an existing Amazon EC2 launch template."];
      launchTemplateName: LaunchTemplateName.t option
        [@ocaml.doc
          "A readable identifier for an existing Amazon EC2 launch template."];
      version: LaunchTemplateVersion.t option
        [@ocaml.doc
          "The version of the Amazon EC2 launch template to use. If no version is specified, the default version will be used. With Amazon EC2, you can specify a default version for a launch template. If none is set, the default is the first version created."]}
    let make ?launchTemplateId =
      fun ?launchTemplateName ->
        fun ?version ->
          fun () -> { launchTemplateId; launchTemplateName; version }
    let to_value x =
      structure_to_value
        [("LaunchTemplateId",
           (Option.map x.launchTemplateId ~f:LaunchTemplateId.to_value));
        ("LaunchTemplateName",
          (Option.map x.launchTemplateName ~f:LaunchTemplateName.to_value));
        ("Version", (Option.map x.version ~f:LaunchTemplateVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let version =
        (Option.map ~f:LaunchTemplateVersion.of_xml)
          (Xml.child xml_arg0 "Version") in
      let launchTemplateName =
        (Option.map ~f:LaunchTemplateName.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateName") in
      let launchTemplateId =
        (Option.map ~f:LaunchTemplateId.of_xml)
          (Xml.child xml_arg0 "LaunchTemplateId") in
      make ?version ?launchTemplateName ?launchTemplateId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let version = field_map json__ "Version" LaunchTemplateVersion.of_json in
      let launchTemplateName =
        field_map json__ "LaunchTemplateName" LaunchTemplateName.of_json in
      let launchTemplateId =
        field_map json__ "LaunchTemplateId" LaunchTemplateId.of_json in
      make ?version ?launchTemplateName ?launchTemplateId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An Amazon Elastic Compute Cloud launch template that contains configuration settings and game server code to be deployed to all instances in a game server group. The launch template is specified when creating a new game server group."]
module VpcSubnets =
  struct
    type nonrec t = VpcSubnet.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:20) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:VpcSubnet.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:VpcSubnet.of_xml)
    let of_json j =
      list_of_json ~kind:"VpcSubnets" ~of_json:VpcSubnet.of_json j
    let to_json v = composed_to_json to_value v
  end
module LocationConfigurationList =
  struct
    type nonrec t = LocationConfiguration.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:LocationConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LocationConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"LocationConfigurationList"
        ~of_json:LocationConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module OutOfCapacityException =
  struct
    type nonrec t = {
      message: NonEmptyString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" NonEmptyString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified game server group has no available game servers to fulfill a ClaimGameServer request. Clients can retry such requests immediately or after a waiting period."]
module ClaimFilterOption =
  struct
    type nonrec t =
      {
      instanceStatuses: FilterInstanceStatuses.t option
        [@ocaml.doc
          "List of instance statuses that game servers may be claimed on. If provided, the list must contain the ACTIVE status."]}
    let make ?instanceStatuses = fun () -> { instanceStatuses }
    let to_value x =
      structure_to_value
        [("InstanceStatuses",
           (Option.map x.instanceStatuses ~f:FilterInstanceStatuses.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceStatuses =
        (Option.map ~f:FilterInstanceStatuses.of_xml)
          (Xml.child xml_arg0 "InstanceStatuses") in
      make ?instanceStatuses ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceStatuses =
        field_map json__ "InstanceStatuses" FilterInstanceStatuses.of_json in
      make ?instanceStatuses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Filters which game servers may be claimed when calling ClaimGameServer."]
module AcceptanceType =
  struct
    type nonrec t =
      | ACCEPT 
      | REJECT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACCEPT -> "ACCEPT"
      | REJECT -> "REJECT"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACCEPT" -> ACCEPT
      | "REJECT" -> REJECT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AcceptanceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AcceptanceType" j)
    let to_json = simple_to_json to_value
  end
module PlayerIdsForAcceptMatch =
  struct
    type nonrec t = PlayerId.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PlayerId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlayerId.of_xml)
    let of_json j =
      list_of_json ~kind:"PlayerIdsForAcceptMatch" ~of_json:PlayerId.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ValidateMatchmakingRuleSetOutput =
  struct
    type nonrec t =
      {
      valid: BooleanModel.t option
        [@ocaml.doc "A response indicating whether the rule set is valid."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?valid = fun () -> { valid }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Valid", (Option.map x.valid ~f:BooleanModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let valid =
        (Option.map ~f:BooleanModel.of_xml) (Xml.child xml_arg0 "Valid") in
      make ?valid ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let valid = field_map json__ "Valid" BooleanModel.of_json in
      make ?valid ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Validates the syntax of a matchmaking rule or rule set. This operation checks that the rule set is using syntactically correct JSON and that it conforms to allowed property expressions. To validate syntax, provide a rule set JSON string. Learn more Build a rule set"]
module ValidateMatchmakingRuleSetInput =
  struct
    type nonrec t =
      {
      ruleSetBody: RuleSetBody.t
        [@ocaml.doc
          "A collection of matchmaking rules to validate, formatted as a JSON string."]}
    let context_ = "ValidateMatchmakingRuleSetInput"
    let make ~ruleSetBody = fun () -> { ruleSetBody }
    let to_value x =
      structure_to_value
        [("RuleSetBody", (Some (RuleSetBody.to_value x.ruleSetBody)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleSetBody =
        RuleSetBody.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleSetBody") in
      make ~ruleSetBody ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleSetBody =
        field_map_exn json__ "RuleSetBody" RuleSetBody.of_json in
      make ~ruleSetBody ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Validates the syntax of a matchmaking rule or rule set. This operation checks that the rule set is using syntactically correct JSON and that it conforms to allowed property expressions. To validate syntax, provide a rule set JSON string. Learn more Build a rule set"]
module UpdateScriptOutput =
  struct
    type nonrec t =
      {
      script: Script.t option
        [@ocaml.doc
          "The newly created script record with a unique script ID. The new script's storage location reflects an Amazon S3 location: (1) If the script was uploaded from an S3 bucket under your account, the storage location reflects the information that was provided in the CreateScript request; (2) If the script file was uploaded from a local zip file, the storage location reflects an S3 location controls by the Amazon GameLift Servers service."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?script = fun () -> { script }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Script", (Option.map x.script ~f:Script.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let script =
        (Option.map ~f:Script.of_xml) (Xml.child xml_arg0 "Script") in
      make ?script ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let script = field_map json__ "Script" Script.of_json in
      make ?script ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Updates Realtime script metadata and content. To update script metadata, specify the script ID and provide updated name and/or version values. To update script content, provide an updated zip file by pointing to either a local file or an Amazon S3 bucket location. You can use either method regardless of how the original script was uploaded. Use the Version parameter to track updates to the script. If the call is successful, the updated metadata is stored in the script record and a revised script is uploaded to the Amazon GameLift Servers service. Once the script is updated and acquired by a fleet instance, the new version is used for all new game sessions. Learn more Amazon GameLift Servers Amazon GameLift Servers Realtime Related actions All APIs by task"]
module UpdateScriptInput =
  struct
    type nonrec t =
      {
      scriptId: ScriptIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the Realtime script to update. You can use either the script ID or ARN value."];
      name: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a script. Script names do not need to be unique."];
      version: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Version information that is associated with a build or script. Version strings do not need to be unique."];
      storageLocation: S3Location.t option
        [@ocaml.doc
          "The location of the Amazon S3 bucket where a zipped file containing your Realtime scripts is stored. The storage location must specify the Amazon S3 bucket name, the zip file name (the \"key\"), and a role ARN that allows Amazon GameLift Servers to access the Amazon S3 storage location. The S3 bucket must be in the same Region where you want to create a new script. By default, Amazon GameLift Servers uploads the latest version of the zip file; if you have S3 object versioning turned on, you can use the ObjectVersion parameter to specify an earlier version."];
      zipFile: ZipBlob.t option
        [@ocaml.doc
          "A data object containing your Realtime scripts and dependencies as a zip file. The zip file can have one or multiple files. Maximum size of a zip file is 5 MB. When using the Amazon Web Services CLI tool to create a script, this parameter is set to the zip file name. It must be prepended with the string \"fileb://\" to indicate that the file data is a binary object. For example: --zip-file fileb://myRealtimeScript.zip."]}
    let context_ = "UpdateScriptInput"
    let make ?name =
      fun ?version ->
        fun ?storageLocation ->
          fun ?zipFile ->
            fun ~scriptId ->
              fun () -> { name; version; storageLocation; zipFile; scriptId }
    let to_value x =
      structure_to_value
        [("ScriptId", (Some (ScriptIdOrArn.to_value x.scriptId)));
        ("Name", (Option.map x.name ~f:NonZeroAndMaxString.to_value));
        ("Version", (Option.map x.version ~f:NonZeroAndMaxString.to_value));
        ("StorageLocation",
          (Option.map x.storageLocation ~f:S3Location.to_value));
        ("ZipFile", (Option.map x.zipFile ~f:ZipBlob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let zipFile =
        (Option.map ~f:ZipBlob.of_xml) (Xml.child xml_arg0 "ZipFile") in
      let storageLocation =
        (Option.map ~f:S3Location.of_xml)
          (Xml.child xml_arg0 "StorageLocation") in
      let version =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Version") in
      let name =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Name") in
      let scriptId =
        ScriptIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScriptId") in
      make ?zipFile ?storageLocation ?version ?name ~scriptId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let zipFile = field_map json__ "ZipFile" ZipBlob.of_json in
      let storageLocation =
        field_map json__ "StorageLocation" S3Location.of_json in
      let version = field_map json__ "Version" NonZeroAndMaxString.of_json in
      let name = field_map json__ "Name" NonZeroAndMaxString.of_json in
      let scriptId = field_map_exn json__ "ScriptId" ScriptIdOrArn.of_json in
      make ?zipFile ?storageLocation ?version ?name ~scriptId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Updates Realtime script metadata and content. To update script metadata, specify the script ID and provide updated name and/or version values. To update script content, provide an updated zip file by pointing to either a local file or an Amazon S3 bucket location. You can use either method regardless of how the original script was uploaded. Use the Version parameter to track updates to the script. If the call is successful, the updated metadata is stored in the script record and a revised script is uploaded to the Amazon GameLift Servers service. Once the script is updated and acquired by a fleet instance, the new version is used for all new game sessions. Learn more Amazon GameLift Servers Amazon GameLift Servers Realtime Related actions All APIs by task"]
module UpdateRuntimeConfigurationOutput =
  struct
    type nonrec t =
      {
      runtimeConfiguration: RuntimeConfiguration.t option
        [@ocaml.doc
          "The runtime configuration currently in use by computes in the fleet. If the update is successful, all property changes are shown."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidFleetStatusException of InvalidFleetStatusException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?runtimeConfiguration = fun () -> { runtimeConfiguration }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidFleetStatusException" ->
          `InvalidFleetStatusException
            (InvalidFleetStatusException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidFleetStatusException" ->
          `InvalidFleetStatusException
            (InvalidFleetStatusException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidFleetStatusException e ->
          `Assoc
            [("error", (`String "InvalidFleetStatusException"));
            ("details", (InvalidFleetStatusException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RuntimeConfiguration",
           (Option.map x.runtimeConfiguration
              ~f:RuntimeConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let runtimeConfiguration =
        (Option.map ~f:RuntimeConfiguration.of_xml)
          (Xml.child xml_arg0 "RuntimeConfiguration") in
      make ?runtimeConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let runtimeConfiguration =
        field_map json__ "RuntimeConfiguration" RuntimeConfiguration.of_json in
      make ?runtimeConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Updates the runtime configuration for the specified fleet. The runtime configuration tells Amazon GameLift Servers how to launch server processes on computes in managed EC2 and Anywhere fleets. You can update a fleet's runtime configuration at any time after the fleet is created; it does not need to be in ACTIVE status. To update runtime configuration, specify the fleet ID and provide a RuntimeConfiguration with an updated set of server process configurations. If successful, the fleet's runtime configuration settings are updated. Fleet computes that run game server processes regularly check for and receive updated runtime configurations. The computes immediately take action to comply with the new configuration by launching new server processes or by not replacing existing processes when they shut down. Updating a fleet's runtime configuration never affects existing server processes. Learn more Setting up Amazon GameLift Servers fleets"]
module UpdateRuntimeConfigurationInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to update runtime configuration for. You can use either the fleet ID or ARN value."];
      runtimeConfiguration: RuntimeConfiguration.t
        [@ocaml.doc
          "Instructions for launching server processes on fleet computes. Server processes run either a custom game build executable or a Amazon GameLift Servers Realtime script. The runtime configuration lists the types of server processes to run, how to launch them, and the number of processes to run concurrently."]}
    let context_ = "UpdateRuntimeConfigurationInput"
    let make ~fleetId =
      fun ~runtimeConfiguration ->
        fun () -> { fleetId; runtimeConfiguration }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("RuntimeConfiguration",
          (Some (RuntimeConfiguration.to_value x.runtimeConfiguration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let runtimeConfiguration =
        RuntimeConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuntimeConfiguration") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~runtimeConfiguration ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let runtimeConfiguration =
        field_map_exn json__ "RuntimeConfiguration"
          RuntimeConfiguration.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~runtimeConfiguration ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Updates the runtime configuration for the specified fleet. The runtime configuration tells Amazon GameLift Servers how to launch server processes on computes in managed EC2 and Anywhere fleets. You can update a fleet's runtime configuration at any time after the fleet is created; it does not need to be in ACTIVE status. To update runtime configuration, specify the fleet ID and provide a RuntimeConfiguration with an updated set of server process configurations. If successful, the fleet's runtime configuration settings are updated. Fleet computes that run game server processes regularly check for and receive updated runtime configurations. The computes immediately take action to comply with the new configuration by launching new server processes or by not replacing existing processes when they shut down. Updating a fleet's runtime configuration never affects existing server processes. Learn more Setting up Amazon GameLift Servers fleets"]
module UpdateMatchmakingConfigurationOutput =
  struct
    type nonrec t =
      {
      configuration: MatchmakingConfiguration.t option
        [@ocaml.doc "The updated matchmaking configuration."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?configuration = fun () -> { configuration }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Configuration",
           (Option.map x.configuration ~f:MatchmakingConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuration =
        (Option.map ~f:MatchmakingConfiguration.of_xml)
          (Xml.child xml_arg0 "Configuration") in
      make ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let configuration =
        field_map json__ "Configuration" MatchmakingConfiguration.of_json in
      make ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Updates settings for a FlexMatch matchmaking configuration. These changes affect all matches and game sessions that are created after the update. To update settings, specify the configuration name to be updated and provide the new settings. Learn more Design a FlexMatch matchmaker"]
module UpdateMatchmakingConfigurationInput =
  struct
    type nonrec t =
      {
      name: MatchmakingConfigurationName.t
        [@ocaml.doc
          "A unique identifier for the matchmaking configuration to update. You can use either the configuration name or ARN value."];
      description: NonZeroAndMaxString.t option
        [@ocaml.doc "A description for the matchmaking configuration."];
      gameSessionQueueArns: QueueArnsList.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers game session queue resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::gamesessionqueue/<queue name>. Queues can be located in any Region. Queues are used to start new Amazon GameLift Servers-hosted game sessions for matches that are created with this matchmaking configuration. If FlexMatchMode is set to STANDALONE, do not set this parameter."];
      requestTimeoutSeconds: MatchmakingRequestTimeoutInteger.t option
        [@ocaml.doc
          "The maximum duration, in seconds, that a matchmaking ticket can remain in process before timing out. Requests that fail due to timing out can be resubmitted as needed."];
      acceptanceTimeoutSeconds: MatchmakingAcceptanceTimeoutInteger.t option
        [@ocaml.doc
          "The length of time (in seconds) to wait for players to accept a proposed match, if acceptance is required."];
      acceptanceRequired: BooleanModel.t option
        [@ocaml.doc
          "A flag that indicates whether a match that was created with this configuration must be accepted by the matched players. To require acceptance, set to TRUE. With this option enabled, matchmaking tickets use the status REQUIRES_ACCEPTANCE to indicate when a completed potential match is waiting for player acceptance."];
      ruleSetName: MatchmakingRuleSetName.t option
        [@ocaml.doc
          "A unique identifier for the matchmaking rule set to use with this configuration. You can use either the rule set name or ARN value. A matchmaking configuration can only use rule sets that are defined in the same Region."];
      notificationTarget: SnsArnStringModel.t option
        [@ocaml.doc
          "An SNS topic ARN that is set up to receive matchmaking notifications. See Setting up notifications for matchmaking for more information."];
      additionalPlayerCount: WholeNumber.t option
        [@ocaml.doc
          "The number of player slots in a match to keep open for future players. For example, if the configuration's rule set specifies a match for a single 12-person team, and the additional player count is set to 2, only 10 players are selected for the match. This parameter is not used if FlexMatchMode is set to STANDALONE."];
      customEventData: CustomEventData.t option
        [@ocaml.doc
          "Information to add to all events related to the matchmaking configuration."];
      gameProperties: GamePropertyList.t option
        [@ocaml.doc
          "A set of key-value pairs that can store custom data in a game session. For example: \\{\"Key\": \"difficulty\", \"Value\": \"novice\"\\}. This information is added to the new GameSession object that is created for a successful match. This parameter is not used if FlexMatchMode is set to STANDALONE. Avoid using periods (\".\") in property keys if you plan to search for game sessions by properties. Property keys containing periods cannot be searched and will be filtered out from search results due to search index limitations. If you use SearchGameSessions API, there is a limit of 500 game property keys across all game sessions and all fleets per region. If the limit is exceeded, there will potentially be game session entries missing from SearchGameSessions API results."];
      gameSessionData: GameSessionData.t option
        [@ocaml.doc
          "A set of custom game session properties, formatted as a single string value. This data is passed to a game server process with a request to start a new game session. For more information, see Start a game session. This information is added to the game session that is created for a successful match. This parameter is not used if FlexMatchMode is set to STANDALONE."];
      backfillMode: BackfillMode.t option
        [@ocaml.doc
          "The method that is used to backfill game sessions created with this matchmaking configuration. Specify MANUAL when your game manages backfill requests manually or does not use the match backfill feature. Specify AUTOMATIC to have GameLift create a match backfill request whenever a game session has one or more open slots. Learn more about manual and automatic backfill in Backfill Existing Games with FlexMatch. Automatic backfill is not available when FlexMatchMode is set to STANDALONE."];
      flexMatchMode: FlexMatchMode.t option
        [@ocaml.doc
          "Indicates whether this matchmaking configuration is being used with Amazon GameLift Servers hosting or as a standalone matchmaking solution. STANDALONE - FlexMatch forms matches and returns match information, including players and team assignments, in a MatchmakingSucceeded event. WITH_QUEUE - FlexMatch forms matches and uses the specified Amazon GameLift Servers queue to start a game session for the match."]}
    let context_ = "UpdateMatchmakingConfigurationInput"
    let make ?description =
      fun ?gameSessionQueueArns ->
        fun ?requestTimeoutSeconds ->
          fun ?acceptanceTimeoutSeconds ->
            fun ?acceptanceRequired ->
              fun ?ruleSetName ->
                fun ?notificationTarget ->
                  fun ?additionalPlayerCount ->
                    fun ?customEventData ->
                      fun ?gameProperties ->
                        fun ?gameSessionData ->
                          fun ?backfillMode ->
                            fun ?flexMatchMode ->
                              fun ~name ->
                                fun () ->
                                  {
                                    description;
                                    gameSessionQueueArns;
                                    requestTimeoutSeconds;
                                    acceptanceTimeoutSeconds;
                                    acceptanceRequired;
                                    ruleSetName;
                                    notificationTarget;
                                    additionalPlayerCount;
                                    customEventData;
                                    gameProperties;
                                    gameSessionData;
                                    backfillMode;
                                    flexMatchMode;
                                    name
                                  }
    let to_value x =
      structure_to_value
        [("Name", (Some (MatchmakingConfigurationName.to_value x.name)));
        ("Description",
          (Option.map x.description ~f:NonZeroAndMaxString.to_value));
        ("GameSessionQueueArns",
          (Option.map x.gameSessionQueueArns ~f:QueueArnsList.to_value));
        ("RequestTimeoutSeconds",
          (Option.map x.requestTimeoutSeconds
             ~f:MatchmakingRequestTimeoutInteger.to_value));
        ("AcceptanceTimeoutSeconds",
          (Option.map x.acceptanceTimeoutSeconds
             ~f:MatchmakingAcceptanceTimeoutInteger.to_value));
        ("AcceptanceRequired",
          (Option.map x.acceptanceRequired ~f:BooleanModel.to_value));
        ("RuleSetName",
          (Option.map x.ruleSetName ~f:MatchmakingRuleSetName.to_value));
        ("NotificationTarget",
          (Option.map x.notificationTarget ~f:SnsArnStringModel.to_value));
        ("AdditionalPlayerCount",
          (Option.map x.additionalPlayerCount ~f:WholeNumber.to_value));
        ("CustomEventData",
          (Option.map x.customEventData ~f:CustomEventData.to_value));
        ("GameProperties",
          (Option.map x.gameProperties ~f:GamePropertyList.to_value));
        ("GameSessionData",
          (Option.map x.gameSessionData ~f:GameSessionData.to_value));
        ("BackfillMode",
          (Option.map x.backfillMode ~f:BackfillMode.to_value));
        ("FlexMatchMode",
          (Option.map x.flexMatchMode ~f:FlexMatchMode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let flexMatchMode =
        (Option.map ~f:FlexMatchMode.of_xml)
          (Xml.child xml_arg0 "FlexMatchMode") in
      let backfillMode =
        (Option.map ~f:BackfillMode.of_xml)
          (Xml.child xml_arg0 "BackfillMode") in
      let gameSessionData =
        (Option.map ~f:GameSessionData.of_xml)
          (Xml.child xml_arg0 "GameSessionData") in
      let gameProperties =
        (Option.map ~f:GamePropertyList.of_xml)
          (Xml.child xml_arg0 "GameProperties") in
      let customEventData =
        (Option.map ~f:CustomEventData.of_xml)
          (Xml.child xml_arg0 "CustomEventData") in
      let additionalPlayerCount =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "AdditionalPlayerCount") in
      let notificationTarget =
        (Option.map ~f:SnsArnStringModel.of_xml)
          (Xml.child xml_arg0 "NotificationTarget") in
      let ruleSetName =
        (Option.map ~f:MatchmakingRuleSetName.of_xml)
          (Xml.child xml_arg0 "RuleSetName") in
      let acceptanceRequired =
        (Option.map ~f:BooleanModel.of_xml)
          (Xml.child xml_arg0 "AcceptanceRequired") in
      let acceptanceTimeoutSeconds =
        (Option.map ~f:MatchmakingAcceptanceTimeoutInteger.of_xml)
          (Xml.child xml_arg0 "AcceptanceTimeoutSeconds") in
      let requestTimeoutSeconds =
        (Option.map ~f:MatchmakingRequestTimeoutInteger.of_xml)
          (Xml.child xml_arg0 "RequestTimeoutSeconds") in
      let gameSessionQueueArns =
        (Option.map ~f:QueueArnsList.of_xml)
          (Xml.child xml_arg0 "GameSessionQueueArns") in
      let description =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        MatchmakingConfigurationName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?flexMatchMode ?backfillMode ?gameSessionData ?gameProperties
        ?customEventData ?additionalPlayerCount ?notificationTarget
        ?ruleSetName ?acceptanceRequired ?acceptanceTimeoutSeconds
        ?requestTimeoutSeconds ?gameSessionQueueArns ?description ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let flexMatchMode =
        field_map json__ "FlexMatchMode" FlexMatchMode.of_json in
      let backfillMode = field_map json__ "BackfillMode" BackfillMode.of_json in
      let gameSessionData =
        field_map json__ "GameSessionData" GameSessionData.of_json in
      let gameProperties =
        field_map json__ "GameProperties" GamePropertyList.of_json in
      let customEventData =
        field_map json__ "CustomEventData" CustomEventData.of_json in
      let additionalPlayerCount =
        field_map json__ "AdditionalPlayerCount" WholeNumber.of_json in
      let notificationTarget =
        field_map json__ "NotificationTarget" SnsArnStringModel.of_json in
      let ruleSetName =
        field_map json__ "RuleSetName" MatchmakingRuleSetName.of_json in
      let acceptanceRequired =
        field_map json__ "AcceptanceRequired" BooleanModel.of_json in
      let acceptanceTimeoutSeconds =
        field_map json__ "AcceptanceTimeoutSeconds"
          MatchmakingAcceptanceTimeoutInteger.of_json in
      let requestTimeoutSeconds =
        field_map json__ "RequestTimeoutSeconds"
          MatchmakingRequestTimeoutInteger.of_json in
      let gameSessionQueueArns =
        field_map json__ "GameSessionQueueArns" QueueArnsList.of_json in
      let description =
        field_map json__ "Description" NonZeroAndMaxString.of_json in
      let name =
        field_map_exn json__ "Name" MatchmakingConfigurationName.of_json in
      make ?flexMatchMode ?backfillMode ?gameSessionData ?gameProperties
        ?customEventData ?additionalPlayerCount ?notificationTarget
        ?ruleSetName ?acceptanceRequired ?acceptanceTimeoutSeconds
        ?requestTimeoutSeconds ?gameSessionQueueArns ?description ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Updates settings for a FlexMatch matchmaking configuration. These changes affect all matches and game sessions that are created after the update. To update settings, specify the configuration name to be updated and provide the new settings. Learn more Design a FlexMatch matchmaker"]
module UpdateGameSessionQueueOutput =
  struct
    type nonrec t =
      {
      gameSessionQueue: GameSessionQueue.t option
        [@ocaml.doc
          "An object that describes the newly updated game session queue."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameSessionQueue = fun () -> { gameSessionQueue }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameSessionQueue",
           (Option.map x.gameSessionQueue ~f:GameSessionQueue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameSessionQueue =
        (Option.map ~f:GameSessionQueue.of_xml)
          (Xml.child xml_arg0 "GameSessionQueue") in
      make ?gameSessionQueue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameSessionQueue =
        field_map json__ "GameSessionQueue" GameSessionQueue.of_json in
      make ?gameSessionQueue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Updates the configuration of a game session queue, which determines how the queue processes new game session requests. To update settings, specify the queue name to be updated and provide the new settings. When updating destinations, provide a complete list of destinations. Learn more Using Multi-Region Queues"]
module UpdateGameSessionQueueInput =
  struct
    type nonrec t =
      {
      name: GameSessionQueueNameOrArn.t
        [@ocaml.doc
          "A descriptive label that is associated with game session queue. Queue names must be unique within each Region. You can use either the queue ID or ARN value."];
      timeoutInSeconds: WholeNumber.t option
        [@ocaml.doc
          "The maximum time, in seconds, that a new game session placement request remains in the queue. When a request exceeds this time, the game session placement changes to a TIMED_OUT status. The minimum value is 10 and the maximum value is 600."];
      playerLatencyPolicies: PlayerLatencyPolicyList.t option
        [@ocaml.doc
          "A set of policies that enforce a sliding cap on player latency when processing game sessions placement requests. Use multiple policies to gradually relax the cap over time if Amazon GameLift Servers can't make a placement. Policies are evaluated in order starting with the lowest maximum latency value. When updating policies, provide a complete collection of policies."];
      destinations: GameSessionQueueDestinationList.t option
        [@ocaml.doc
          "A list of fleets and/or fleet aliases that can be used to fulfill game session placement requests in the queue. Destinations are identified by either a fleet ARN or a fleet alias ARN, and are listed in order of placement preference. When updating this list, provide a complete list of destinations."];
      filterConfiguration: FilterConfiguration.t option
        [@ocaml.doc
          "A list of locations where a queue is allowed to place new game sessions. Locations are specified in the form of Amazon Web Services Region codes, such as us-west-2. If this parameter is not set, game sessions can be placed in any queue location. To remove an existing filter configuration, pass in an empty set."];
      priorityConfiguration: PriorityConfiguration.t option
        [@ocaml.doc
          "Custom settings to use when prioritizing destinations and locations for game session placements. This configuration replaces the FleetIQ default prioritization process. Priority types that are not explicitly named will be automatically applied at the end of the prioritization process. To remove an existing priority configuration, pass in an empty set."];
      customEventData: QueueCustomEventData.t option
        [@ocaml.doc
          "Information to be added to all events that are related to this game session queue."];
      notificationTarget: QueueSnsArnStringModel.t option
        [@ocaml.doc
          "An SNS topic ARN that is set up to receive game session placement notifications. See Setting up notifications for game session placement."]}
    let context_ = "UpdateGameSessionQueueInput"
    let make ?timeoutInSeconds =
      fun ?playerLatencyPolicies ->
        fun ?destinations ->
          fun ?filterConfiguration ->
            fun ?priorityConfiguration ->
              fun ?customEventData ->
                fun ?notificationTarget ->
                  fun ~name ->
                    fun () ->
                      {
                        timeoutInSeconds;
                        playerLatencyPolicies;
                        destinations;
                        filterConfiguration;
                        priorityConfiguration;
                        customEventData;
                        notificationTarget;
                        name
                      }
    let to_value x =
      structure_to_value
        [("Name", (Some (GameSessionQueueNameOrArn.to_value x.name)));
        ("TimeoutInSeconds",
          (Option.map x.timeoutInSeconds ~f:WholeNumber.to_value));
        ("PlayerLatencyPolicies",
          (Option.map x.playerLatencyPolicies
             ~f:PlayerLatencyPolicyList.to_value));
        ("Destinations",
          (Option.map x.destinations
             ~f:GameSessionQueueDestinationList.to_value));
        ("FilterConfiguration",
          (Option.map x.filterConfiguration ~f:FilterConfiguration.to_value));
        ("PriorityConfiguration",
          (Option.map x.priorityConfiguration
             ~f:PriorityConfiguration.to_value));
        ("CustomEventData",
          (Option.map x.customEventData ~f:QueueCustomEventData.to_value));
        ("NotificationTarget",
          (Option.map x.notificationTarget ~f:QueueSnsArnStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notificationTarget =
        (Option.map ~f:QueueSnsArnStringModel.of_xml)
          (Xml.child xml_arg0 "NotificationTarget") in
      let customEventData =
        (Option.map ~f:QueueCustomEventData.of_xml)
          (Xml.child xml_arg0 "CustomEventData") in
      let priorityConfiguration =
        (Option.map ~f:PriorityConfiguration.of_xml)
          (Xml.child xml_arg0 "PriorityConfiguration") in
      let filterConfiguration =
        (Option.map ~f:FilterConfiguration.of_xml)
          (Xml.child xml_arg0 "FilterConfiguration") in
      let destinations =
        (Option.map ~f:GameSessionQueueDestinationList.of_xml)
          (Xml.child xml_arg0 "Destinations") in
      let playerLatencyPolicies =
        (Option.map ~f:PlayerLatencyPolicyList.of_xml)
          (Xml.child xml_arg0 "PlayerLatencyPolicies") in
      let timeoutInSeconds =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "TimeoutInSeconds") in
      let name =
        GameSessionQueueNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?notificationTarget ?customEventData ?priorityConfiguration
        ?filterConfiguration ?destinations ?playerLatencyPolicies
        ?timeoutInSeconds ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let notificationTarget =
        field_map json__ "NotificationTarget" QueueSnsArnStringModel.of_json in
      let customEventData =
        field_map json__ "CustomEventData" QueueCustomEventData.of_json in
      let priorityConfiguration =
        field_map json__ "PriorityConfiguration"
          PriorityConfiguration.of_json in
      let filterConfiguration =
        field_map json__ "FilterConfiguration" FilterConfiguration.of_json in
      let destinations =
        field_map json__ "Destinations"
          GameSessionQueueDestinationList.of_json in
      let playerLatencyPolicies =
        field_map json__ "PlayerLatencyPolicies"
          PlayerLatencyPolicyList.of_json in
      let timeoutInSeconds =
        field_map json__ "TimeoutInSeconds" WholeNumber.of_json in
      let name =
        field_map_exn json__ "Name" GameSessionQueueNameOrArn.of_json in
      make ?notificationTarget ?customEventData ?priorityConfiguration
        ?filterConfiguration ?destinations ?playerLatencyPolicies
        ?timeoutInSeconds ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Updates the configuration of a game session queue, which determines how the queue processes new game session requests. To update settings, specify the queue name to be updated and provide the new settings. When updating destinations, provide a complete list of destinations. Learn more Using Multi-Region Queues"]
module UpdateGameSessionOutput =
  struct
    type nonrec t =
      {
      gameSession: GameSession.t option
        [@ocaml.doc "The updated game session properties."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidGameSessionStatusException of
          InvalidGameSessionStatusException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `NotReadyException of NotReadyException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameSession = fun () -> { gameSession }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidGameSessionStatusException" ->
          `InvalidGameSessionStatusException
            (InvalidGameSessionStatusException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "NotReadyException" ->
          `NotReadyException (NotReadyException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidGameSessionStatusException" ->
          `InvalidGameSessionStatusException
            (InvalidGameSessionStatusException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "NotReadyException" ->
          `NotReadyException (NotReadyException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidGameSessionStatusException e ->
          `Assoc
            [("error", (`String "InvalidGameSessionStatusException"));
            ("details", (InvalidGameSessionStatusException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `NotReadyException e ->
          `Assoc
            [("error", (`String "NotReadyException"));
            ("details", (NotReadyException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameSession", (Option.map x.gameSession ~f:GameSession.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameSession =
        (Option.map ~f:GameSession.of_xml) (Xml.child xml_arg0 "GameSession") in
      make ?gameSession ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameSession = field_map json__ "GameSession" GameSession.of_json in
      make ?gameSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Updates the mutable properties of a game session. To update a game session, specify the game session ID and the values you want to change. If successful, the updated GameSession object is returned. All APIs by task"]
module UpdateGameSessionInput =
  struct
    type nonrec t =
      {
      gameSessionId: ArnStringModel.t
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions to update. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>."];
      maximumPlayerSessionCount: WholeNumber.t option
        [@ocaml.doc
          "The maximum number of players that can be connected simultaneously to the game session."];
      name: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a game session. Session names do not need to be unique."];
      playerSessionCreationPolicy: PlayerSessionCreationPolicy.t option
        [@ocaml.doc
          "A policy that determines whether the game session is accepting new players."];
      protectionPolicy: ProtectionPolicy.t option
        [@ocaml.doc
          "Game session protection policy to apply to this game session only. NoProtection -- The game session can be terminated during a scale-down event. FullProtection -- If the game session is in an ACTIVE status, it cannot be terminated during a scale-down event."];
      gameProperties: GamePropertyList.t option
        [@ocaml.doc
          "A set of key-value pairs that can store custom data in a game session. For example: \\{\"Key\": \"difficulty\", \"Value\": \"novice\"\\}. You can use this parameter to modify game properties in an active game session. This action adds new properties and modifies existing properties. There is no way to delete properties. For an example, see Update the value of a game property. Avoid using periods (\".\") in property keys if you plan to search for game sessions by properties. Property keys containing periods cannot be searched and will be filtered out from search results due to search index limitations. If you use SearchGameSessions API, there is a limit of 500 game property keys across all game sessions and all fleets per region. If the limit is exceeded, there will potentially be game session entries missing from SearchGameSessions API results."]}
    let context_ = "UpdateGameSessionInput"
    let make ?maximumPlayerSessionCount =
      fun ?name ->
        fun ?playerSessionCreationPolicy ->
          fun ?protectionPolicy ->
            fun ?gameProperties ->
              fun ~gameSessionId ->
                fun () ->
                  {
                    maximumPlayerSessionCount;
                    name;
                    playerSessionCreationPolicy;
                    protectionPolicy;
                    gameProperties;
                    gameSessionId
                  }
    let to_value x =
      structure_to_value
        [("GameSessionId", (Some (ArnStringModel.to_value x.gameSessionId)));
        ("MaximumPlayerSessionCount",
          (Option.map x.maximumPlayerSessionCount ~f:WholeNumber.to_value));
        ("Name", (Option.map x.name ~f:NonZeroAndMaxString.to_value));
        ("PlayerSessionCreationPolicy",
          (Option.map x.playerSessionCreationPolicy
             ~f:PlayerSessionCreationPolicy.to_value));
        ("ProtectionPolicy",
          (Option.map x.protectionPolicy ~f:ProtectionPolicy.to_value));
        ("GameProperties",
          (Option.map x.gameProperties ~f:GamePropertyList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameProperties =
        (Option.map ~f:GamePropertyList.of_xml)
          (Xml.child xml_arg0 "GameProperties") in
      let protectionPolicy =
        (Option.map ~f:ProtectionPolicy.of_xml)
          (Xml.child xml_arg0 "ProtectionPolicy") in
      let playerSessionCreationPolicy =
        (Option.map ~f:PlayerSessionCreationPolicy.of_xml)
          (Xml.child xml_arg0 "PlayerSessionCreationPolicy") in
      let name =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Name") in
      let maximumPlayerSessionCount =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "MaximumPlayerSessionCount") in
      let gameSessionId =
        ArnStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameSessionId") in
      make ?gameProperties ?protectionPolicy ?playerSessionCreationPolicy
        ?name ?maximumPlayerSessionCount ~gameSessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameProperties =
        field_map json__ "GameProperties" GamePropertyList.of_json in
      let protectionPolicy =
        field_map json__ "ProtectionPolicy" ProtectionPolicy.of_json in
      let playerSessionCreationPolicy =
        field_map json__ "PlayerSessionCreationPolicy"
          PlayerSessionCreationPolicy.of_json in
      let name = field_map json__ "Name" NonZeroAndMaxString.of_json in
      let maximumPlayerSessionCount =
        field_map json__ "MaximumPlayerSessionCount" WholeNumber.of_json in
      let gameSessionId =
        field_map_exn json__ "GameSessionId" ArnStringModel.of_json in
      make ?gameProperties ?protectionPolicy ?playerSessionCreationPolicy
        ?name ?maximumPlayerSessionCount ~gameSessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Updates the mutable properties of a game session. To update a game session, specify the game session ID and the values you want to change. If successful, the updated GameSession object is returned. All APIs by task"]
module UpdateGameServerOutput =
  struct
    type nonrec t =
      {
      gameServer: GameServer.t option
        [@ocaml.doc "Object that describes the newly updated game server."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameServer = fun () -> { gameServer }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameServer", (Option.map x.gameServer ~f:GameServer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServer =
        (Option.map ~f:GameServer.of_xml) (Xml.child xml_arg0 "GameServer") in
      make ?gameServer ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServer = field_map json__ "GameServer" GameServer.of_json in
      make ?gameServer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Updates information about a registered game server to help Amazon GameLift Servers FleetIQ track game server availability. This operation is called by a game server process that is running on an instance in a game server group. Use this operation to update the following types of game server information. You can make all three types of updates in the same request: To update the game server's utilization status from AVAILABLE (when the game server is available to be claimed) to UTILIZED (when the game server is currently hosting games). Identify the game server and game server group and specify the new utilization status. You can't change the status from to UTILIZED to AVAILABLE . To report health status, identify the game server and game server group and set health check to HEALTHY. If a game server does not report health status for a certain length of time, the game server is no longer considered healthy. As a result, it will be eventually deregistered from the game server group to avoid affecting utilization metrics. The best practice is to report health every 60 seconds. To change game server metadata, provide updated game server data. Once a game server is successfully updated, the relevant statuses and timestamps are updated. Learn more Amazon GameLift Servers FleetIQ Guide"]
module UpdateGameServerInput =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupNameOrArn.t
        [@ocaml.doc
          "A unique identifier for the game server group where the game server is running."];
      gameServerId: GameServerId.t
        [@ocaml.doc
          "A custom string that uniquely identifies the game server to update."];
      gameServerData: GameServerData.t option
        [@ocaml.doc
          "A set of custom game server properties, formatted as a single string value. This data is passed to a game client or service when it requests information on game servers."];
      utilizationStatus: GameServerUtilizationStatus.t option
        [@ocaml.doc
          "Indicates if the game server is available or is currently hosting gameplay. You can update a game server status from AVAILABLE to UTILIZED, but you can't change a the status from UTILIZED to AVAILABLE."];
      healthCheck: GameServerHealthCheck.t option
        [@ocaml.doc
          "Indicates health status of the game server. A request that includes this parameter updates the game server's LastHealthCheckTime timestamp."]}
    let context_ = "UpdateGameServerInput"
    let make ?gameServerData =
      fun ?utilizationStatus ->
        fun ?healthCheck ->
          fun ~gameServerGroupName ->
            fun ~gameServerId ->
              fun () ->
                {
                  gameServerData;
                  utilizationStatus;
                  healthCheck;
                  gameServerGroupName;
                  gameServerId
                }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Some (GameServerGroupNameOrArn.to_value x.gameServerGroupName)));
        ("GameServerId", (Some (GameServerId.to_value x.gameServerId)));
        ("GameServerData",
          (Option.map x.gameServerData ~f:GameServerData.to_value));
        ("UtilizationStatus",
          (Option.map x.utilizationStatus
             ~f:GameServerUtilizationStatus.to_value));
        ("HealthCheck",
          (Option.map x.healthCheck ~f:GameServerHealthCheck.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheck =
        (Option.map ~f:GameServerHealthCheck.of_xml)
          (Xml.child xml_arg0 "HealthCheck") in
      let utilizationStatus =
        (Option.map ~f:GameServerUtilizationStatus.of_xml)
          (Xml.child xml_arg0 "UtilizationStatus") in
      let gameServerData =
        (Option.map ~f:GameServerData.of_xml)
          (Xml.child xml_arg0 "GameServerData") in
      let gameServerId =
        GameServerId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerId") in
      let gameServerGroupName =
        GameServerGroupNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerGroupName") in
      make ?healthCheck ?utilizationStatus ?gameServerData ~gameServerId
        ~gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let healthCheck =
        field_map json__ "HealthCheck" GameServerHealthCheck.of_json in
      let utilizationStatus =
        field_map json__ "UtilizationStatus"
          GameServerUtilizationStatus.of_json in
      let gameServerData =
        field_map json__ "GameServerData" GameServerData.of_json in
      let gameServerId =
        field_map_exn json__ "GameServerId" GameServerId.of_json in
      let gameServerGroupName =
        field_map_exn json__ "GameServerGroupName"
          GameServerGroupNameOrArn.of_json in
      make ?healthCheck ?utilizationStatus ?gameServerData ~gameServerId
        ~gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Updates information about a registered game server to help Amazon GameLift Servers FleetIQ track game server availability. This operation is called by a game server process that is running on an instance in a game server group. Use this operation to update the following types of game server information. You can make all three types of updates in the same request: To update the game server's utilization status from AVAILABLE (when the game server is available to be claimed) to UTILIZED (when the game server is currently hosting games). Identify the game server and game server group and specify the new utilization status. You can't change the status from to UTILIZED to AVAILABLE . To report health status, identify the game server and game server group and set health check to HEALTHY. If a game server does not report health status for a certain length of time, the game server is no longer considered healthy. As a result, it will be eventually deregistered from the game server group to avoid affecting utilization metrics. The best practice is to report health every 60 seconds. To change game server metadata, provide updated game server data. Once a game server is successfully updated, the relevant statuses and timestamps are updated. Learn more Amazon GameLift Servers FleetIQ Guide"]
module UpdateGameServerGroupOutput =
  struct
    type nonrec t =
      {
      gameServerGroup: GameServerGroup.t option
        [@ocaml.doc
          "An object that describes the game server group resource with updated properties."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameServerGroup = fun () -> { gameServerGroup }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameServerGroup",
           (Option.map x.gameServerGroup ~f:GameServerGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServerGroup =
        (Option.map ~f:GameServerGroup.of_xml)
          (Xml.child xml_arg0 "GameServerGroup") in
      make ?gameServerGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServerGroup =
        field_map json__ "GameServerGroup" GameServerGroup.of_json in
      make ?gameServerGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Updates Amazon GameLift Servers FleetIQ-specific properties for a game server group. Many Auto Scaling group properties are updated on the Auto Scaling group directly, including the launch template, Auto Scaling policies, and maximum/minimum/desired instance counts. To update the game server group, specify the game server group ID and provide the updated values. Before applying the updates, the new values are validated to ensure that Amazon GameLift Servers FleetIQ can continue to perform instance balancing activity. If successful, a GameServerGroup object is returned. Learn more Amazon GameLift Servers FleetIQ Guide"]
module UpdateGameServerGroupInput =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupNameOrArn.t
        [@ocaml.doc
          "A unique identifier for the game server group. Use either the name or ARN value."];
      roleArn: IamRoleArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) for an IAM role that allows Amazon GameLift Servers to access your Amazon EC2 Auto Scaling groups."];
      instanceDefinitions: InstanceDefinitions.t option
        [@ocaml.doc
          "An updated list of Amazon EC2 instance types to use in the Auto Scaling group. The instance definitions must specify at least two different instance types that are supported by Amazon GameLift Servers FleetIQ. This updated list replaces the entire current list of instance definitions for the game server group. For more information on instance types, see EC2 Instance Types in the Amazon EC2 User Guide. You can optionally specify capacity weighting for each instance type. If no weight value is specified for an instance type, it is set to the default value \"1\". For more information about capacity weighting, see Instance Weighting for Amazon EC2 Auto Scaling in the Amazon EC2 Auto Scaling User Guide."];
      gameServerProtectionPolicy: GameServerProtectionPolicy.t option
        [@ocaml.doc
          "A flag that indicates whether instances in the game server group are protected from early termination. Unprotected instances that have active game servers running might be terminated during a scale-down event, causing players to be dropped from the game. Protected instances cannot be terminated while there are active game servers running except in the event of a forced game server group deletion (see ). An exception to this is with Spot Instances, which can be terminated by Amazon Web Services regardless of protection status. This property is set to NO_PROTECTION by default."];
      balancingStrategy: BalancingStrategy.t option
        [@ocaml.doc
          "Indicates how Amazon GameLift Servers FleetIQ balances the use of Spot Instances and On-Demand Instances in the game server group. Method options include the following: SPOT_ONLY - Only Spot Instances are used in the game server group. If Spot Instances are unavailable or not viable for game hosting, the game server group provides no hosting capacity until Spot Instances can again be used. Until then, no new instances are started, and the existing nonviable Spot Instances are terminated (after current gameplay ends) and are not replaced. SPOT_PREFERRED - (default value) Spot Instances are used whenever available in the game server group. If Spot Instances are unavailable, the game server group continues to provide hosting capacity by falling back to On-Demand Instances. Existing nonviable Spot Instances are terminated (after current gameplay ends) and are replaced with new On-Demand Instances. ON_DEMAND_ONLY - Only On-Demand Instances are used in the game server group. No Spot Instances are used, even when available, while this balancing strategy is in force."]}
    let context_ = "UpdateGameServerGroupInput"
    let make ?roleArn =
      fun ?instanceDefinitions ->
        fun ?gameServerProtectionPolicy ->
          fun ?balancingStrategy ->
            fun ~gameServerGroupName ->
              fun () ->
                {
                  roleArn;
                  instanceDefinitions;
                  gameServerProtectionPolicy;
                  balancingStrategy;
                  gameServerGroupName
                }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Some (GameServerGroupNameOrArn.to_value x.gameServerGroupName)));
        ("RoleArn", (Option.map x.roleArn ~f:IamRoleArn.to_value));
        ("InstanceDefinitions",
          (Option.map x.instanceDefinitions ~f:InstanceDefinitions.to_value));
        ("GameServerProtectionPolicy",
          (Option.map x.gameServerProtectionPolicy
             ~f:GameServerProtectionPolicy.to_value));
        ("BalancingStrategy",
          (Option.map x.balancingStrategy ~f:BalancingStrategy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let balancingStrategy =
        (Option.map ~f:BalancingStrategy.of_xml)
          (Xml.child xml_arg0 "BalancingStrategy") in
      let gameServerProtectionPolicy =
        (Option.map ~f:GameServerProtectionPolicy.of_xml)
          (Xml.child xml_arg0 "GameServerProtectionPolicy") in
      let instanceDefinitions =
        (Option.map ~f:InstanceDefinitions.of_xml)
          (Xml.child xml_arg0 "InstanceDefinitions") in
      let roleArn =
        (Option.map ~f:IamRoleArn.of_xml) (Xml.child xml_arg0 "RoleArn") in
      let gameServerGroupName =
        GameServerGroupNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerGroupName") in
      make ?balancingStrategy ?gameServerProtectionPolicy
        ?instanceDefinitions ?roleArn ~gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let balancingStrategy =
        field_map json__ "BalancingStrategy" BalancingStrategy.of_json in
      let gameServerProtectionPolicy =
        field_map json__ "GameServerProtectionPolicy"
          GameServerProtectionPolicy.of_json in
      let instanceDefinitions =
        field_map json__ "InstanceDefinitions" InstanceDefinitions.of_json in
      let roleArn = field_map json__ "RoleArn" IamRoleArn.of_json in
      let gameServerGroupName =
        field_map_exn json__ "GameServerGroupName"
          GameServerGroupNameOrArn.of_json in
      make ?balancingStrategy ?gameServerProtectionPolicy
        ?instanceDefinitions ?roleArn ~gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Updates Amazon GameLift Servers FleetIQ-specific properties for a game server group. Many Auto Scaling group properties are updated on the Auto Scaling group directly, including the launch template, Auto Scaling policies, and maximum/minimum/desired instance counts. To update the game server group, specify the game server group ID and provide the updated values. Before applying the updates, the new values are validated to ensure that Amazon GameLift Servers FleetIQ can continue to perform instance balancing activity. If successful, a GameServerGroup object is returned. Learn more Amazon GameLift Servers FleetIQ Guide"]
module UpdateFleetPortSettingsOutput =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc "A unique identifier for the fleet that was updated."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidFleetStatusException of InvalidFleetStatusException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetId = fun ?fleetArn -> fun () -> { fleetId; fleetArn }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidFleetStatusException" ->
          `InvalidFleetStatusException
            (InvalidFleetStatusException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidFleetStatusException" ->
          `InvalidFleetStatusException
            (InvalidFleetStatusException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidFleetStatusException e ->
          `Assoc
            [("error", (`String "InvalidFleetStatusException"));
            ("details", (InvalidFleetStatusException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Updates permissions that allow inbound traffic to connect to game sessions in the fleet. To update settings, specify the fleet ID to be updated and specify the changes to be made. List the permissions you want to add in InboundPermissionAuthorizations, and permissions you want to remove in InboundPermissionRevocations. Permissions to be removed must match existing fleet permissions. If successful, the fleet ID for the updated fleet is returned. For fleets with remote locations, port setting updates can take time to propagate across all locations. You can check the status of updates in each location by calling DescribeFleetPortSettings with a location name. Learn more Setting up Amazon GameLift Servers fleets"]
module UpdateFleetPortSettingsInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to update port settings for. You can use either the fleet ID or ARN value."];
      inboundPermissionAuthorizations: IpPermissionsList.t option
        [@ocaml.doc
          "A collection of port settings to be added to the fleet resource."];
      inboundPermissionRevocations: IpPermissionsList.t option
        [@ocaml.doc
          "A collection of port settings to be removed from the fleet resource."]}
    let context_ = "UpdateFleetPortSettingsInput"
    let make ?inboundPermissionAuthorizations =
      fun ?inboundPermissionRevocations ->
        fun ~fleetId ->
          fun () ->
            {
              inboundPermissionAuthorizations;
              inboundPermissionRevocations;
              fleetId
            }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("InboundPermissionAuthorizations",
          (Option.map x.inboundPermissionAuthorizations
             ~f:IpPermissionsList.to_value));
        ("InboundPermissionRevocations",
          (Option.map x.inboundPermissionRevocations
             ~f:IpPermissionsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let inboundPermissionRevocations =
        (Option.map ~f:IpPermissionsList.of_xml)
          (Xml.child xml_arg0 "InboundPermissionRevocations") in
      let inboundPermissionAuthorizations =
        (Option.map ~f:IpPermissionsList.of_xml)
          (Xml.child xml_arg0 "InboundPermissionAuthorizations") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?inboundPermissionRevocations ?inboundPermissionAuthorizations
        ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let inboundPermissionRevocations =
        field_map json__ "InboundPermissionRevocations"
          IpPermissionsList.of_json in
      let inboundPermissionAuthorizations =
        field_map json__ "InboundPermissionAuthorizations"
          IpPermissionsList.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?inboundPermissionRevocations ?inboundPermissionAuthorizations
        ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Updates permissions that allow inbound traffic to connect to game sessions in the fleet. To update settings, specify the fleet ID to be updated and specify the changes to be made. List the permissions you want to add in InboundPermissionAuthorizations, and permissions you want to remove in InboundPermissionRevocations. Permissions to be removed must match existing fleet permissions. If successful, the fleet ID for the updated fleet is returned. For fleets with remote locations, port setting updates can take time to propagate across all locations. You can check the status of updates in each location by calling DescribeFleetPortSettings with a location name. Learn more Setting up Amazon GameLift Servers fleets"]
module UpdateFleetCapacityOutput =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc "A unique identifier for the fleet that was updated."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The remote location being updated, expressed as an Amazon Web Services Region code, such as us-west-2."];
      managedCapacityConfiguration: ManagedCapacityConfiguration.t option
        [@ocaml.doc
          "Configuration for Amazon GameLift Servers-managed capacity scaling options."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidFleetStatusException of InvalidFleetStatusException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?location ->
          fun ?managedCapacityConfiguration ->
            fun () ->
              { fleetId; fleetArn; location; managedCapacityConfiguration }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidFleetStatusException" ->
          `InvalidFleetStatusException
            (InvalidFleetStatusException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidFleetStatusException" ->
          `InvalidFleetStatusException
            (InvalidFleetStatusException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidFleetStatusException e ->
          `Assoc
            [("error", (`String "InvalidFleetStatusException"));
            ("details", (InvalidFleetStatusException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value));
        ("ManagedCapacityConfiguration",
          (Option.map x.managedCapacityConfiguration
             ~f:ManagedCapacityConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let managedCapacityConfiguration =
        (Option.map ~f:ManagedCapacityConfiguration.of_xml)
          (Xml.child xml_arg0 "ManagedCapacityConfiguration") in
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?managedCapacityConfiguration ?location ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let managedCapacityConfiguration =
        field_map json__ "ManagedCapacityConfiguration"
          ManagedCapacityConfiguration.of_json in
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?managedCapacityConfiguration ?location ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Updates capacity settings for a managed EC2 fleet or managed container fleet. For these fleets, you adjust capacity by changing the number of instances in the fleet. Fleet capacity determines the number of game sessions and players that the fleet can host based on its configuration. For fleets with multiple locations, use this operation to manage capacity settings in each location individually. Minimum/maximum size: Set hard limits on the number of Amazon EC2 instances allowed. If Amazon GameLift Servers receives a request--either through manual update or automatic scaling--it won't change the capacity to a value outside of this range. Desired capacity: As an alternative to automatic scaling, manually set the number of Amazon EC2 instances to be maintained. Before changing a fleet's desired capacity, check the maximum capacity of the fleet's Amazon EC2 instance type by calling DescribeEC2InstanceLimits. To update capacity for a fleet's home Region, or if the fleet has no remote locations, omit the Location parameter. The fleet must be in ACTIVE status. To update capacity for a fleet's remote location, set the Location parameter to the location to update. The location must be in ACTIVE status. If successful, Amazon GameLift Servers updates the capacity settings and returns the identifiers for the updated fleet and/or location. If a requested change to desired capacity exceeds the instance type's limit, the LimitExceeded exception occurs. Updates often prompt an immediate change in fleet capacity, such as when current capacity is different than the new desired capacity or outside the new limits. In this scenario, Amazon GameLift Servers automatically initiates steps to add or remove instances in the fleet location. You can track a fleet's current capacity by calling DescribeFleetCapacity or DescribeFleetLocationCapacity. Use ManagedCapacityConfiguration with the \"SCALE_TO_AND_FROM_ZERO\" ZeroCapacityStrategy to enable Amazon GameLift Servers to fully manage the MinSize value, switching between 0 and 1 based on game session activity. This is ideal for eliminating compute costs during periods of no game activity. It is particularly beneficial during development when you're away from your desk, iterating on builds for extended periods, in production environments serving low-traffic locations, or for games with long, predictable downtime windows. By automatically managing capacity between 0 and 1 instances, you avoid paying for idle instances while maintaining the ability to serve game sessions when demand arrives. Note that while scale-out is triggered immediately upon receiving a game session request, actual game session availability depends on your server process startup time, so this approach works best with multi-location Fleets where cold-start latency is tolerable. With a \"MANUAL\" ZeroCapacityStrategy Amazon GameLift Servers will not modify Fleet MinSize values automatically and will not scale out from zero instances in response to game sessions. This is configurable per-location. Learn more Scaling fleet capacity"]
module UpdateFleetCapacityInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to update capacity settings for. You can use either the fleet ID or ARN value."];
      desiredInstances: WholeNumber.t option
        [@ocaml.doc
          "The number of Amazon EC2 instances you want to maintain in the specified fleet location. This value must fall between the minimum and maximum size limits. Changes in desired instance value can take up to 1 minute to be reflected when viewing the fleet's capacity settings."];
      minSize: WholeNumber.t option
        [@ocaml.doc
          "The minimum number of instances that are allowed in the specified fleet location. If this parameter is not set, the default is 0. This parameter cannot be set when using a ManagedCapacityConfiguration where ZeroCapacityStrategy has a value of SCALE_TO_AND_FROM_ZERO."];
      maxSize: WholeNumber.t option
        [@ocaml.doc
          "The maximum number of instances that are allowed in the specified fleet location. If this parameter is not set, the default is 1."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The name of a remote location to update fleet capacity settings for, in the form of an Amazon Web Services Region code such as us-west-2."];
      managedCapacityConfiguration: ManagedCapacityConfiguration.t option
        [@ocaml.doc
          "Configuration for Amazon GameLift Servers-managed capacity scaling options."]}
    let context_ = "UpdateFleetCapacityInput"
    let make ?desiredInstances =
      fun ?minSize ->
        fun ?maxSize ->
          fun ?location ->
            fun ?managedCapacityConfiguration ->
              fun ~fleetId ->
                fun () ->
                  {
                    desiredInstances;
                    minSize;
                    maxSize;
                    location;
                    managedCapacityConfiguration;
                    fleetId
                  }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("DesiredInstances",
          (Option.map x.desiredInstances ~f:WholeNumber.to_value));
        ("MinSize", (Option.map x.minSize ~f:WholeNumber.to_value));
        ("MaxSize", (Option.map x.maxSize ~f:WholeNumber.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value));
        ("ManagedCapacityConfiguration",
          (Option.map x.managedCapacityConfiguration
             ~f:ManagedCapacityConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let managedCapacityConfiguration =
        (Option.map ~f:ManagedCapacityConfiguration.of_xml)
          (Xml.child xml_arg0 "ManagedCapacityConfiguration") in
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let maxSize =
        (Option.map ~f:WholeNumber.of_xml) (Xml.child xml_arg0 "MaxSize") in
      let minSize =
        (Option.map ~f:WholeNumber.of_xml) (Xml.child xml_arg0 "MinSize") in
      let desiredInstances =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "DesiredInstances") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?managedCapacityConfiguration ?location ?maxSize ?minSize
        ?desiredInstances ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let managedCapacityConfiguration =
        field_map json__ "ManagedCapacityConfiguration"
          ManagedCapacityConfiguration.of_json in
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let maxSize = field_map json__ "MaxSize" WholeNumber.of_json in
      let minSize = field_map json__ "MinSize" WholeNumber.of_json in
      let desiredInstances =
        field_map json__ "DesiredInstances" WholeNumber.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?managedCapacityConfiguration ?location ?maxSize ?minSize
        ?desiredInstances ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Updates capacity settings for a managed EC2 fleet or managed container fleet. For these fleets, you adjust capacity by changing the number of instances in the fleet. Fleet capacity determines the number of game sessions and players that the fleet can host based on its configuration. For fleets with multiple locations, use this operation to manage capacity settings in each location individually. Minimum/maximum size: Set hard limits on the number of Amazon EC2 instances allowed. If Amazon GameLift Servers receives a request--either through manual update or automatic scaling--it won't change the capacity to a value outside of this range. Desired capacity: As an alternative to automatic scaling, manually set the number of Amazon EC2 instances to be maintained. Before changing a fleet's desired capacity, check the maximum capacity of the fleet's Amazon EC2 instance type by calling DescribeEC2InstanceLimits. To update capacity for a fleet's home Region, or if the fleet has no remote locations, omit the Location parameter. The fleet must be in ACTIVE status. To update capacity for a fleet's remote location, set the Location parameter to the location to update. The location must be in ACTIVE status. If successful, Amazon GameLift Servers updates the capacity settings and returns the identifiers for the updated fleet and/or location. If a requested change to desired capacity exceeds the instance type's limit, the LimitExceeded exception occurs. Updates often prompt an immediate change in fleet capacity, such as when current capacity is different than the new desired capacity or outside the new limits. In this scenario, Amazon GameLift Servers automatically initiates steps to add or remove instances in the fleet location. You can track a fleet's current capacity by calling DescribeFleetCapacity or DescribeFleetLocationCapacity. Use ManagedCapacityConfiguration with the \"SCALE_TO_AND_FROM_ZERO\" ZeroCapacityStrategy to enable Amazon GameLift Servers to fully manage the MinSize value, switching between 0 and 1 based on game session activity. This is ideal for eliminating compute costs during periods of no game activity. It is particularly beneficial during development when you're away from your desk, iterating on builds for extended periods, in production environments serving low-traffic locations, or for games with long, predictable downtime windows. By automatically managing capacity between 0 and 1 instances, you avoid paying for idle instances while maintaining the ability to serve game sessions when demand arrives. Note that while scale-out is triggered immediately upon receiving a game session request, actual game session availability depends on your server process startup time, so this approach works best with multi-location Fleets where cold-start latency is tolerable. With a \"MANUAL\" ZeroCapacityStrategy Amazon GameLift Servers will not modify Fleet MinSize values automatically and will not scale out from zero instances in response to game sessions. This is configurable per-location. Learn more Scaling fleet capacity"]
module UpdateFleetAttributesOutput =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc "A unique identifier for the fleet that was updated."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidFleetStatusException of InvalidFleetStatusException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetId = fun ?fleetArn -> fun () -> { fleetId; fleetArn }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidFleetStatusException" ->
          `InvalidFleetStatusException
            (InvalidFleetStatusException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidFleetStatusException" ->
          `InvalidFleetStatusException
            (InvalidFleetStatusException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidFleetStatusException e ->
          `Assoc
            [("error", (`String "InvalidFleetStatusException"));
            ("details", (InvalidFleetStatusException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Updates a fleet's mutable attributes, such as game session protection and resource creation limits. To update fleet attributes, specify the fleet ID and the property values that you want to change. If successful, Amazon GameLift Servers returns the identifiers for the updated fleet. A managed fleet's runtime environment, which depends on the fleet's Amazon Machine Image \\{AMI\\} version, can't be updated. You must create a new fleet. As a best practice, we recommend replacing your managed fleets every 30 days to maintain a secure and up-to-date runtime environment for your hosted game servers. For guidance, see Security best practices for Amazon GameLift Servers. Learn more Setting up Amazon GameLift Servers fleets"]
module UpdateFleetAttributesInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to update attribute metadata for. You can use either the fleet ID or ARN value."];
      name: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a fleet. Fleet names do not need to be unique."];
      description: NonZeroAndMaxString.t option
        [@ocaml.doc "A human-readable description of a fleet."];
      newGameSessionProtectionPolicy: ProtectionPolicy.t option
        [@ocaml.doc
          "The game session protection policy to apply to all new game sessions created in this fleet. Game sessions that already exist are not affected. You can set protection for individual game sessions using UpdateGameSession . NoProtection -- The game session can be terminated during a scale-down event. FullProtection -- If the game session is in an ACTIVE status, it cannot be terminated during a scale-down event."];
      resourceCreationLimitPolicy: ResourceCreationLimitPolicy.t option
        [@ocaml.doc
          "Policy settings that limit the number of game sessions an individual player can create over a span of time."];
      metricGroups: MetricGroupList.t option
        [@ocaml.doc
          "The name of a metric group to add this fleet to. Use a metric group in Amazon CloudWatch to aggregate the metrics from multiple fleets. Provide an existing metric group name, or create a new metric group by providing a new name. A fleet can only be in one metric group at a time."];
      anywhereConfiguration: AnywhereConfiguration.t option
        [@ocaml.doc
          "Amazon GameLift Servers Anywhere configuration options."]}
    let context_ = "UpdateFleetAttributesInput"
    let make ?name =
      fun ?description ->
        fun ?newGameSessionProtectionPolicy ->
          fun ?resourceCreationLimitPolicy ->
            fun ?metricGroups ->
              fun ?anywhereConfiguration ->
                fun ~fleetId ->
                  fun () ->
                    {
                      name;
                      description;
                      newGameSessionProtectionPolicy;
                      resourceCreationLimitPolicy;
                      metricGroups;
                      anywhereConfiguration;
                      fleetId
                    }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("Name", (Option.map x.name ~f:NonZeroAndMaxString.to_value));
        ("Description",
          (Option.map x.description ~f:NonZeroAndMaxString.to_value));
        ("NewGameSessionProtectionPolicy",
          (Option.map x.newGameSessionProtectionPolicy
             ~f:ProtectionPolicy.to_value));
        ("ResourceCreationLimitPolicy",
          (Option.map x.resourceCreationLimitPolicy
             ~f:ResourceCreationLimitPolicy.to_value));
        ("MetricGroups",
          (Option.map x.metricGroups ~f:MetricGroupList.to_value));
        ("AnywhereConfiguration",
          (Option.map x.anywhereConfiguration
             ~f:AnywhereConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let anywhereConfiguration =
        (Option.map ~f:AnywhereConfiguration.of_xml)
          (Xml.child xml_arg0 "AnywhereConfiguration") in
      let metricGroups =
        (Option.map ~f:MetricGroupList.of_xml)
          (Xml.child xml_arg0 "MetricGroups") in
      let resourceCreationLimitPolicy =
        (Option.map ~f:ResourceCreationLimitPolicy.of_xml)
          (Xml.child xml_arg0 "ResourceCreationLimitPolicy") in
      let newGameSessionProtectionPolicy =
        (Option.map ~f:ProtectionPolicy.of_xml)
          (Xml.child xml_arg0 "NewGameSessionProtectionPolicy") in
      let description =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Name") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?anywhereConfiguration ?metricGroups ?resourceCreationLimitPolicy
        ?newGameSessionProtectionPolicy ?description ?name ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let anywhereConfiguration =
        field_map json__ "AnywhereConfiguration"
          AnywhereConfiguration.of_json in
      let metricGroups =
        field_map json__ "MetricGroups" MetricGroupList.of_json in
      let resourceCreationLimitPolicy =
        field_map json__ "ResourceCreationLimitPolicy"
          ResourceCreationLimitPolicy.of_json in
      let newGameSessionProtectionPolicy =
        field_map json__ "NewGameSessionProtectionPolicy"
          ProtectionPolicy.of_json in
      let description =
        field_map json__ "Description" NonZeroAndMaxString.of_json in
      let name = field_map json__ "Name" NonZeroAndMaxString.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?anywhereConfiguration ?metricGroups ?resourceCreationLimitPolicy
        ?newGameSessionProtectionPolicy ?description ?name ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Updates a fleet's mutable attributes, such as game session protection and resource creation limits. To update fleet attributes, specify the fleet ID and the property values that you want to change. If successful, Amazon GameLift Servers returns the identifiers for the updated fleet. A managed fleet's runtime environment, which depends on the fleet's Amazon Machine Image \\{AMI\\} version, can't be updated. You must create a new fleet. As a best practice, we recommend replacing your managed fleets every 30 days to maintain a secure and up-to-date runtime environment for your hosted game servers. For guidance, see Security best practices for Amazon GameLift Servers. Learn more Setting up Amazon GameLift Servers fleets"]
module UpdateContainerGroupDefinitionOutput =
  struct
    type nonrec t =
      {
      containerGroupDefinition: ContainerGroupDefinition.t option
        [@ocaml.doc
          "The properties of the updated container group definition version."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerGroupDefinition =
      fun () -> { containerGroupDefinition }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ContainerGroupDefinition",
           (Option.map x.containerGroupDefinition
              ~f:ContainerGroupDefinition.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerGroupDefinition =
        (Option.map ~f:ContainerGroupDefinition.of_xml)
          (Xml.child xml_arg0 "ContainerGroupDefinition") in
      make ?containerGroupDefinition ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let containerGroupDefinition =
        field_map json__ "ContainerGroupDefinition"
          ContainerGroupDefinition.of_json in
      make ?containerGroupDefinition ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Updates properties in an existing container group definition. This operation doesn't replace the definition. Instead, it creates a new version of the definition and saves it separately. You can access all versions that you choose to retain. The only property you can't update is the container group type. Request options: Update based on the latest version of the container group definition. Specify the container group definition name only, or use an ARN value without a version number. Provide updated values for the properties that you want to change only. All other values remain the same as the latest version. Update based on a specific version of the container group definition. Specify the container group definition name and a source version number, or use an ARN value with a version number. Provide updated values for the properties that you want to change only. All other values remain the same as the source version. Change a game server container definition. Provide the updated container definition. Add or change a support container definition. Provide a complete set of container definitions, including the updated definition. Remove a support container definition. Provide a complete set of container definitions, excluding the definition to remove. If the container group has only one support container definition, provide an empty set. Results: If successful, this operation returns the complete properties of the new container group definition version. If the container group definition version is used in an active fleets, the update automatically initiates a new fleet deployment of the new version. You can track a fleet's deployments using ListFleetDeployments."]
module UpdateContainerGroupDefinitionInput =
  struct
    type nonrec t =
      {
      name: ContainerGroupDefinitionNameOrArn.t
        [@ocaml.doc
          "A descriptive identifier for the container group definition. The name value must be unique in an Amazon Web Services Region."];
      gameServerContainerDefinition:
        GameServerContainerDefinitionInput.t option
        [@ocaml.doc
          "An updated definition for the game server container in this group. Define a game server container only when the container group type is GAME_SERVER. You can pass in your container definitions as a JSON file."];
      supportContainerDefinitions:
        SupportContainerDefinitionInputList.t option
        [@ocaml.doc
          "One or more definitions for support containers in this group. You can define a support container in any type of container group. You can pass in your container definitions as a JSON file."];
      totalMemoryLimitMebibytes: ContainerTotalMemoryLimit.t option
        [@ocaml.doc
          "The maximum amount of memory (in MiB) to allocate to the container group. All containers in the group share this memory. If you specify memory limits for an individual container, the total value must be greater than any individual container's memory limit."];
      totalVcpuLimit: ContainerTotalVcpuLimit.t option
        [@ocaml.doc
          "The maximum amount of vCPU units to allocate to the container group (1 vCPU is equal to 1024 CPU units). All containers in the group share this memory. If you specify vCPU limits for individual containers, the total value must be equal to or greater than the sum of the CPU limits for all containers in the group."];
      versionDescription: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A description for this update to the container group definition."];
      sourceVersionNumber: PositiveInteger.t option
        [@ocaml.doc
          "The container group definition version to update. The new version starts with values from the source version, and then updates values included in this request."];
      operatingSystem: ContainerOperatingSystem.t option
        [@ocaml.doc
          "The platform that all containers in the group use. Containers in a group must run on the same operating system. Amazon Linux 2 (AL2) will reach end of support on 6/30/2026. See more details in the Amazon Linux 2 FAQs. For game servers that are hosted on AL2 and use server SDK version 4.x for Amazon GameLift Servers, first update the game server build to server SDK 5.x, and then deploy to AL2023 instances. See Migrate to server SDK version 5."]}
    let context_ = "UpdateContainerGroupDefinitionInput"
    let make ?gameServerContainerDefinition =
      fun ?supportContainerDefinitions ->
        fun ?totalMemoryLimitMebibytes ->
          fun ?totalVcpuLimit ->
            fun ?versionDescription ->
              fun ?sourceVersionNumber ->
                fun ?operatingSystem ->
                  fun ~name ->
                    fun () ->
                      {
                        gameServerContainerDefinition;
                        supportContainerDefinitions;
                        totalMemoryLimitMebibytes;
                        totalVcpuLimit;
                        versionDescription;
                        sourceVersionNumber;
                        operatingSystem;
                        name
                      }
    let to_value x =
      structure_to_value
        [("Name", (Some (ContainerGroupDefinitionNameOrArn.to_value x.name)));
        ("GameServerContainerDefinition",
          (Option.map x.gameServerContainerDefinition
             ~f:GameServerContainerDefinitionInput.to_value));
        ("SupportContainerDefinitions",
          (Option.map x.supportContainerDefinitions
             ~f:SupportContainerDefinitionInputList.to_value));
        ("TotalMemoryLimitMebibytes",
          (Option.map x.totalMemoryLimitMebibytes
             ~f:ContainerTotalMemoryLimit.to_value));
        ("TotalVcpuLimit",
          (Option.map x.totalVcpuLimit ~f:ContainerTotalVcpuLimit.to_value));
        ("VersionDescription",
          (Option.map x.versionDescription ~f:NonZeroAndMaxString.to_value));
        ("SourceVersionNumber",
          (Option.map x.sourceVersionNumber ~f:PositiveInteger.to_value));
        ("OperatingSystem",
          (Option.map x.operatingSystem ~f:ContainerOperatingSystem.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let operatingSystem =
        (Option.map ~f:ContainerOperatingSystem.of_xml)
          (Xml.child xml_arg0 "OperatingSystem") in
      let sourceVersionNumber =
        (Option.map ~f:PositiveInteger.of_xml)
          (Xml.child xml_arg0 "SourceVersionNumber") in
      let versionDescription =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "VersionDescription") in
      let totalVcpuLimit =
        (Option.map ~f:ContainerTotalVcpuLimit.of_xml)
          (Xml.child xml_arg0 "TotalVcpuLimit") in
      let totalMemoryLimitMebibytes =
        (Option.map ~f:ContainerTotalMemoryLimit.of_xml)
          (Xml.child xml_arg0 "TotalMemoryLimitMebibytes") in
      let supportContainerDefinitions =
        (Option.map ~f:SupportContainerDefinitionInputList.of_xml)
          (Xml.child xml_arg0 "SupportContainerDefinitions") in
      let gameServerContainerDefinition =
        (Option.map ~f:GameServerContainerDefinitionInput.of_xml)
          (Xml.child xml_arg0 "GameServerContainerDefinition") in
      let name =
        ContainerGroupDefinitionNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?operatingSystem ?sourceVersionNumber ?versionDescription
        ?totalVcpuLimit ?totalMemoryLimitMebibytes
        ?supportContainerDefinitions ?gameServerContainerDefinition ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let operatingSystem =
        field_map json__ "OperatingSystem" ContainerOperatingSystem.of_json in
      let sourceVersionNumber =
        field_map json__ "SourceVersionNumber" PositiveInteger.of_json in
      let versionDescription =
        field_map json__ "VersionDescription" NonZeroAndMaxString.of_json in
      let totalVcpuLimit =
        field_map json__ "TotalVcpuLimit" ContainerTotalVcpuLimit.of_json in
      let totalMemoryLimitMebibytes =
        field_map json__ "TotalMemoryLimitMebibytes"
          ContainerTotalMemoryLimit.of_json in
      let supportContainerDefinitions =
        field_map json__ "SupportContainerDefinitions"
          SupportContainerDefinitionInputList.of_json in
      let gameServerContainerDefinition =
        field_map json__ "GameServerContainerDefinition"
          GameServerContainerDefinitionInput.of_json in
      let name =
        field_map_exn json__ "Name" ContainerGroupDefinitionNameOrArn.of_json in
      make ?operatingSystem ?sourceVersionNumber ?versionDescription
        ?totalVcpuLimit ?totalMemoryLimitMebibytes
        ?supportContainerDefinitions ?gameServerContainerDefinition ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Updates properties in an existing container group definition. This operation doesn't replace the definition. Instead, it creates a new version of the definition and saves it separately. You can access all versions that you choose to retain. The only property you can't update is the container group type. Request options: Update based on the latest version of the container group definition. Specify the container group definition name only, or use an ARN value without a version number. Provide updated values for the properties that you want to change only. All other values remain the same as the latest version. Update based on a specific version of the container group definition. Specify the container group definition name and a source version number, or use an ARN value with a version number. Provide updated values for the properties that you want to change only. All other values remain the same as the source version. Change a game server container definition. Provide the updated container definition. Add or change a support container definition. Provide a complete set of container definitions, including the updated definition. Remove a support container definition. Provide a complete set of container definitions, excluding the definition to remove. If the container group has only one support container definition, provide an empty set. Results: If successful, this operation returns the complete properties of the new container group definition version. If the container group definition version is used in an active fleets, the update automatically initiates a new fleet deployment of the new version. You can track a fleet's deployments using ListFleetDeployments."]
module UpdateContainerFleetOutput =
  struct
    type nonrec t =
      {
      containerFleet: ContainerFleet.t option
        [@ocaml.doc
          "A collection of container fleet objects for all fleets that match the request criteria."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `NotReadyException of NotReadyException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerFleet = fun () -> { containerFleet }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "NotReadyException" ->
          `NotReadyException (NotReadyException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "NotReadyException" ->
          `NotReadyException (NotReadyException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `NotReadyException e ->
          `Assoc
            [("error", (`String "NotReadyException"));
            ("details", (NotReadyException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ContainerFleet",
           (Option.map x.containerFleet ~f:ContainerFleet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerFleet =
        (Option.map ~f:ContainerFleet.of_xml)
          (Xml.child xml_arg0 "ContainerFleet") in
      make ?containerFleet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let containerFleet =
        field_map json__ "ContainerFleet" ContainerFleet.of_json in
      make ?containerFleet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Updates the properties of a managed container fleet. Depending on the properties being updated, this operation might initiate a fleet deployment. You can track deployments for a fleet using https://docs.aws.amazon.com/gamelift/latest/apireference/API_DescribeFleetDeployment.html. A managed fleet's runtime environment, which depends on the fleet's Amazon Machine Image \\{AMI\\} version, can't be updated. You must create a new fleet. As a best practice, we recommend replacing your managed fleets every 30 days to maintain a secure and up-to-date runtime environment for your hosted game servers. For guidance, see Security best practices for Amazon GameLift Servers. Request options As with CreateContainerFleet, many fleet properties use common defaults or are calculated based on the fleet's container group definitions. Update fleet properties that result in a fleet deployment. Include only those properties that you want to change. Specify deployment configuration settings. Update fleet properties that don't result in a fleet deployment. Include only those properties that you want to change. Changes to the following properties initiate a fleet deployment: GameServerContainerGroupDefinition PerInstanceContainerGroupDefinition GameServerContainerGroupsPerInstance InstanceInboundPermissions InstanceConnectionPortRange LogConfiguration Results If successful, this operation updates the container fleet resource, and might initiate a new deployment of fleet resources using the deployment configuration provided. A deployment replaces existing fleet instances with new instances that are deployed with the updated fleet properties. The fleet is placed in UPDATING status until the deployment is complete, then return to ACTIVE. You can have only one update deployment active at a time for a fleet. If a second update request initiates a deployment while another deployment is in progress, the first deployment is cancelled."]
module UpdateContainerFleetInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the container fleet to update. You can use either the fleet ID or ARN value."];
      gameServerContainerGroupDefinitionName:
        ContainerGroupDefinitionNameOrArn.t option
        [@ocaml.doc
          "The name or ARN value of a new game server container group definition to deploy on the fleet. If you're updating the fleet to a specific version of a container group definition, use the ARN value and include the version number. If you're updating the fleet to the latest version of a container group definition, you can use the name value. You can't remove a fleet's game server container group definition, you can only update or replace it with another definition. Update a container group definition by calling UpdateContainerGroupDefinition. This operation creates a ContainerGroupDefinition resource with an incremented version."];
      perInstanceContainerGroupDefinitionName:
        ContainerGroupDefinitionNameOrArn.t option
        [@ocaml.doc
          "The name or ARN value of a new per-instance container group definition to deploy on the fleet. If you're updating the fleet to a specific version of a container group definition, use the ARN value and include the version number. If you're updating the fleet to the latest version of a container group definition, you can use the name value. Update a container group definition by calling UpdateContainerGroupDefinition. This operation creates a ContainerGroupDefinition resource with an incremented version. To remove a fleet's per-instance container group definition, leave this parameter empty and use the parameter RemoveAttributes."];
      gameServerContainerGroupsPerInstance:
        GameServerContainerGroupsPerInstance.t option
        [@ocaml.doc
          "The number of times to replicate the game server container group on each fleet instance. By default, Amazon GameLift Servers calculates the maximum number of game server container groups that can fit on each instance. You can remove this property value to use the calculated value, or set it manually. If you set this number manually, Amazon GameLift Servers uses your value as long as it's less than the calculated maximum."];
      instanceConnectionPortRange: ConnectionPortRange.t option
        [@ocaml.doc
          "A revised set of port numbers to open on each fleet instance. By default, Amazon GameLift Servers calculates an optimal port range based on your fleet configuration. If you previously set this parameter manually, you can't reset this to use the calculated settings. The port range must not overlap with the Amazon GameLift Servers reserved port range 4092-4191. This range is reserved for internal Amazon GameLift Servers services."];
      instanceInboundPermissionAuthorizations: IpPermissionsList.t option
        [@ocaml.doc
          "A set of ports to add to the container fleet's inbound permissions. The port range must not overlap with the Amazon GameLift Servers reserved port range 4092-4191. This range is reserved for internal Amazon GameLift Servers services."];
      instanceInboundPermissionRevocations: IpPermissionsList.t option
        [@ocaml.doc
          "A set of ports to remove from the container fleet's inbound permissions."];
      deploymentConfiguration: DeploymentConfiguration.t option
        [@ocaml.doc
          "Instructions for how to deploy updates to a container fleet, if the fleet update initiates a deployment. The deployment configuration lets you determine how to replace fleet instances and what actions to take if the deployment fails."];
      description: NonZeroAndMaxString.t option
        [@ocaml.doc "A meaningful description of the container fleet."];
      metricGroups: MetricGroupList.t option
        [@ocaml.doc
          "The name of an Amazon Web Services CloudWatch metric group to add this fleet to."];
      newGameSessionProtectionPolicy: ProtectionPolicy.t option
        [@ocaml.doc
          "The game session protection policy to apply to all new game sessions that are started in this fleet. Game sessions that already exist are not affected."];
      gameSessionCreationLimitPolicy: GameSessionCreationLimitPolicy.t option
        [@ocaml.doc
          "A policy that limits the number of game sessions that each individual player can create on instances in this fleet. The limit applies for a specified span of time."];
      logConfiguration: LogConfiguration.t option
        [@ocaml.doc
          "The method for collecting container logs for the fleet."];
      removeAttributes: ContainerFleetRemoveAttributeList.t option
        [@ocaml.doc
          "If set, this update removes a fleet's per-instance container group definition. You can't remove a fleet's game server container group definition."]}
    let context_ = "UpdateContainerFleetInput"
    let make ?gameServerContainerGroupDefinitionName =
      fun ?perInstanceContainerGroupDefinitionName ->
        fun ?gameServerContainerGroupsPerInstance ->
          fun ?instanceConnectionPortRange ->
            fun ?instanceInboundPermissionAuthorizations ->
              fun ?instanceInboundPermissionRevocations ->
                fun ?deploymentConfiguration ->
                  fun ?description ->
                    fun ?metricGroups ->
                      fun ?newGameSessionProtectionPolicy ->
                        fun ?gameSessionCreationLimitPolicy ->
                          fun ?logConfiguration ->
                            fun ?removeAttributes ->
                              fun ~fleetId ->
                                fun () ->
                                  {
                                    gameServerContainerGroupDefinitionName;
                                    perInstanceContainerGroupDefinitionName;
                                    gameServerContainerGroupsPerInstance;
                                    instanceConnectionPortRange;
                                    instanceInboundPermissionAuthorizations;
                                    instanceInboundPermissionRevocations;
                                    deploymentConfiguration;
                                    description;
                                    metricGroups;
                                    newGameSessionProtectionPolicy;
                                    gameSessionCreationLimitPolicy;
                                    logConfiguration;
                                    removeAttributes;
                                    fleetId
                                  }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("GameServerContainerGroupDefinitionName",
          (Option.map x.gameServerContainerGroupDefinitionName
             ~f:ContainerGroupDefinitionNameOrArn.to_value));
        ("PerInstanceContainerGroupDefinitionName",
          (Option.map x.perInstanceContainerGroupDefinitionName
             ~f:ContainerGroupDefinitionNameOrArn.to_value));
        ("GameServerContainerGroupsPerInstance",
          (Option.map x.gameServerContainerGroupsPerInstance
             ~f:GameServerContainerGroupsPerInstance.to_value));
        ("InstanceConnectionPortRange",
          (Option.map x.instanceConnectionPortRange
             ~f:ConnectionPortRange.to_value));
        ("InstanceInboundPermissionAuthorizations",
          (Option.map x.instanceInboundPermissionAuthorizations
             ~f:IpPermissionsList.to_value));
        ("InstanceInboundPermissionRevocations",
          (Option.map x.instanceInboundPermissionRevocations
             ~f:IpPermissionsList.to_value));
        ("DeploymentConfiguration",
          (Option.map x.deploymentConfiguration
             ~f:DeploymentConfiguration.to_value));
        ("Description",
          (Option.map x.description ~f:NonZeroAndMaxString.to_value));
        ("MetricGroups",
          (Option.map x.metricGroups ~f:MetricGroupList.to_value));
        ("NewGameSessionProtectionPolicy",
          (Option.map x.newGameSessionProtectionPolicy
             ~f:ProtectionPolicy.to_value));
        ("GameSessionCreationLimitPolicy",
          (Option.map x.gameSessionCreationLimitPolicy
             ~f:GameSessionCreationLimitPolicy.to_value));
        ("LogConfiguration",
          (Option.map x.logConfiguration ~f:LogConfiguration.to_value));
        ("RemoveAttributes",
          (Option.map x.removeAttributes
             ~f:ContainerFleetRemoveAttributeList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let removeAttributes =
        (Option.map ~f:ContainerFleetRemoveAttributeList.of_xml)
          (Xml.child xml_arg0 "RemoveAttributes") in
      let logConfiguration =
        (Option.map ~f:LogConfiguration.of_xml)
          (Xml.child xml_arg0 "LogConfiguration") in
      let gameSessionCreationLimitPolicy =
        (Option.map ~f:GameSessionCreationLimitPolicy.of_xml)
          (Xml.child xml_arg0 "GameSessionCreationLimitPolicy") in
      let newGameSessionProtectionPolicy =
        (Option.map ~f:ProtectionPolicy.of_xml)
          (Xml.child xml_arg0 "NewGameSessionProtectionPolicy") in
      let metricGroups =
        (Option.map ~f:MetricGroupList.of_xml)
          (Xml.child xml_arg0 "MetricGroups") in
      let description =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Description") in
      let deploymentConfiguration =
        (Option.map ~f:DeploymentConfiguration.of_xml)
          (Xml.child xml_arg0 "DeploymentConfiguration") in
      let instanceInboundPermissionRevocations =
        (Option.map ~f:IpPermissionsList.of_xml)
          (Xml.child xml_arg0 "InstanceInboundPermissionRevocations") in
      let instanceInboundPermissionAuthorizations =
        (Option.map ~f:IpPermissionsList.of_xml)
          (Xml.child xml_arg0 "InstanceInboundPermissionAuthorizations") in
      let instanceConnectionPortRange =
        (Option.map ~f:ConnectionPortRange.of_xml)
          (Xml.child xml_arg0 "InstanceConnectionPortRange") in
      let gameServerContainerGroupsPerInstance =
        (Option.map ~f:GameServerContainerGroupsPerInstance.of_xml)
          (Xml.child xml_arg0 "GameServerContainerGroupsPerInstance") in
      let perInstanceContainerGroupDefinitionName =
        (Option.map ~f:ContainerGroupDefinitionNameOrArn.of_xml)
          (Xml.child xml_arg0 "PerInstanceContainerGroupDefinitionName") in
      let gameServerContainerGroupDefinitionName =
        (Option.map ~f:ContainerGroupDefinitionNameOrArn.of_xml)
          (Xml.child xml_arg0 "GameServerContainerGroupDefinitionName") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?removeAttributes ?logConfiguration
        ?gameSessionCreationLimitPolicy ?newGameSessionProtectionPolicy
        ?metricGroups ?description ?deploymentConfiguration
        ?instanceInboundPermissionRevocations
        ?instanceInboundPermissionAuthorizations ?instanceConnectionPortRange
        ?gameServerContainerGroupsPerInstance
        ?perInstanceContainerGroupDefinitionName
        ?gameServerContainerGroupDefinitionName ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let removeAttributes =
        field_map json__ "RemoveAttributes"
          ContainerFleetRemoveAttributeList.of_json in
      let logConfiguration =
        field_map json__ "LogConfiguration" LogConfiguration.of_json in
      let gameSessionCreationLimitPolicy =
        field_map json__ "GameSessionCreationLimitPolicy"
          GameSessionCreationLimitPolicy.of_json in
      let newGameSessionProtectionPolicy =
        field_map json__ "NewGameSessionProtectionPolicy"
          ProtectionPolicy.of_json in
      let metricGroups =
        field_map json__ "MetricGroups" MetricGroupList.of_json in
      let description =
        field_map json__ "Description" NonZeroAndMaxString.of_json in
      let deploymentConfiguration =
        field_map json__ "DeploymentConfiguration"
          DeploymentConfiguration.of_json in
      let instanceInboundPermissionRevocations =
        field_map json__ "InstanceInboundPermissionRevocations"
          IpPermissionsList.of_json in
      let instanceInboundPermissionAuthorizations =
        field_map json__ "InstanceInboundPermissionAuthorizations"
          IpPermissionsList.of_json in
      let instanceConnectionPortRange =
        field_map json__ "InstanceConnectionPortRange"
          ConnectionPortRange.of_json in
      let gameServerContainerGroupsPerInstance =
        field_map json__ "GameServerContainerGroupsPerInstance"
          GameServerContainerGroupsPerInstance.of_json in
      let perInstanceContainerGroupDefinitionName =
        field_map json__ "PerInstanceContainerGroupDefinitionName"
          ContainerGroupDefinitionNameOrArn.of_json in
      let gameServerContainerGroupDefinitionName =
        field_map json__ "GameServerContainerGroupDefinitionName"
          ContainerGroupDefinitionNameOrArn.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?removeAttributes ?logConfiguration
        ?gameSessionCreationLimitPolicy ?newGameSessionProtectionPolicy
        ?metricGroups ?description ?deploymentConfiguration
        ?instanceInboundPermissionRevocations
        ?instanceInboundPermissionAuthorizations ?instanceConnectionPortRange
        ?gameServerContainerGroupsPerInstance
        ?perInstanceContainerGroupDefinitionName
        ?gameServerContainerGroupDefinitionName ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Updates the properties of a managed container fleet. Depending on the properties being updated, this operation might initiate a fleet deployment. You can track deployments for a fleet using https://docs.aws.amazon.com/gamelift/latest/apireference/API_DescribeFleetDeployment.html. A managed fleet's runtime environment, which depends on the fleet's Amazon Machine Image \\{AMI\\} version, can't be updated. You must create a new fleet. As a best practice, we recommend replacing your managed fleets every 30 days to maintain a secure and up-to-date runtime environment for your hosted game servers. For guidance, see Security best practices for Amazon GameLift Servers. Request options As with CreateContainerFleet, many fleet properties use common defaults or are calculated based on the fleet's container group definitions. Update fleet properties that result in a fleet deployment. Include only those properties that you want to change. Specify deployment configuration settings. Update fleet properties that don't result in a fleet deployment. Include only those properties that you want to change. Changes to the following properties initiate a fleet deployment: GameServerContainerGroupDefinition PerInstanceContainerGroupDefinition GameServerContainerGroupsPerInstance InstanceInboundPermissions InstanceConnectionPortRange LogConfiguration Results If successful, this operation updates the container fleet resource, and might initiate a new deployment of fleet resources using the deployment configuration provided. A deployment replaces existing fleet instances with new instances that are deployed with the updated fleet properties. The fleet is placed in UPDATING status until the deployment is complete, then return to ACTIVE. You can have only one update deployment active at a time for a fleet. If a second update request initiates a deployment while another deployment is in progress, the first deployment is cancelled."]
module UpdateBuildOutput =
  struct
    type nonrec t =
      {
      build: Build.t option [@ocaml.doc "The updated build resource."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?build = fun () -> { build }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Build", (Option.map x.build ~f:Build.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let build = (Option.map ~f:Build.of_xml) (Xml.child xml_arg0 "Build") in
      make ?build ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let build = field_map json__ "Build" Build.of_json in make ?build ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Updates metadata in a build resource, including the build name and version. To update the metadata, specify the build ID to update and provide the new values. If successful, a build object containing the updated metadata is returned. Learn more Upload a Custom Server Build All APIs by task"]
module UpdateBuildInput =
  struct
    type nonrec t =
      {
      buildId: BuildIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the build to update. You can use either the build ID or ARN value."];
      name: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a build. Build names do not need to be unique."];
      version: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Version information that is associated with a build or script. Version strings do not need to be unique."]}
    let context_ = "UpdateBuildInput"
    let make ?name =
      fun ?version -> fun ~buildId -> fun () -> { name; version; buildId }
    let to_value x =
      structure_to_value
        [("BuildId", (Some (BuildIdOrArn.to_value x.buildId)));
        ("Name", (Option.map x.name ~f:NonZeroAndMaxString.to_value));
        ("Version", (Option.map x.version ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let version =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Version") in
      let name =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Name") in
      let buildId =
        BuildIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BuildId") in
      make ?version ?name ~buildId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let version = field_map json__ "Version" NonZeroAndMaxString.of_json in
      let name = field_map json__ "Name" NonZeroAndMaxString.of_json in
      let buildId = field_map_exn json__ "BuildId" BuildIdOrArn.of_json in
      make ?version ?name ~buildId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Updates metadata in a build resource, including the build name and version. To update the metadata, specify the build ID to update and provide the new values. If successful, a build object containing the updated metadata is returned. Learn more Upload a Custom Server Build All APIs by task"]
module UpdateAliasOutput =
  struct
    type nonrec t =
      {
      alias: Alias.t option [@ocaml.doc "The updated alias resource."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?alias = fun () -> { alias }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Alias", (Option.map x.alias ~f:Alias.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let alias = (Option.map ~f:Alias.of_xml) (Xml.child xml_arg0 "Alias") in
      make ?alias ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let alias = field_map json__ "Alias" Alias.of_json in make ?alias ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Updates properties for an alias. Specify the unique identifier of the alias to be updated and the new property values. When reassigning an alias to a new fleet, provide an updated routing strategy. If successful, the updated alias record is returned. Related actions All APIs by task"]
module UpdateAliasInput =
  struct
    type nonrec t =
      {
      aliasId: AliasIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the alias that you want to update. You can use either the alias ID or ARN value."];
      name: NonBlankAndLengthConstraintString.t option
        [@ocaml.doc
          "A descriptive label that is associated with an alias. Alias names do not need to be unique."];
      description: NonZeroAndMaxString.t option
        [@ocaml.doc "A human-readable description of the alias."];
      routingStrategy: RoutingStrategy.t option
        [@ocaml.doc
          "The routing configuration, including routing type and fleet target, for the alias."]}
    let context_ = "UpdateAliasInput"
    let make ?name =
      fun ?description ->
        fun ?routingStrategy ->
          fun ~aliasId ->
            fun () -> { name; description; routingStrategy; aliasId }
    let to_value x =
      structure_to_value
        [("AliasId", (Some (AliasIdOrArn.to_value x.aliasId)));
        ("Name",
          (Option.map x.name ~f:NonBlankAndLengthConstraintString.to_value));
        ("Description",
          (Option.map x.description ~f:NonZeroAndMaxString.to_value));
        ("RoutingStrategy",
          (Option.map x.routingStrategy ~f:RoutingStrategy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routingStrategy =
        (Option.map ~f:RoutingStrategy.of_xml)
          (Xml.child xml_arg0 "RoutingStrategy") in
      let description =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        (Option.map ~f:NonBlankAndLengthConstraintString.of_xml)
          (Xml.child xml_arg0 "Name") in
      let aliasId =
        AliasIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AliasId") in
      make ?routingStrategy ?description ?name ~aliasId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let routingStrategy =
        field_map json__ "RoutingStrategy" RoutingStrategy.of_json in
      let description =
        field_map json__ "Description" NonZeroAndMaxString.of_json in
      let name =
        field_map json__ "Name" NonBlankAndLengthConstraintString.of_json in
      let aliasId = field_map_exn json__ "AliasId" AliasIdOrArn.of_json in
      make ?routingStrategy ?description ?name ~aliasId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Updates properties for an alias. Specify the unique identifier of the alias to be updated and the new property values. When reassigning an alias to a new fleet, provide an updated routing strategy. If successful, the updated alias record is returned. Related actions All APIs by task"]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Removes a tag assigned to a Amazon GameLift Servers resource. You can use resource tags to organize Amazon Web Services resources for a range of purposes. This operation handles the permissions necessary to manage tags for Amazon GameLift Servers resources that support tagging. To remove a tag from a resource, specify the unique ARN value for the resource and provide a string list containing one or more tags to remove. This operation succeeds even if the list includes tags that aren't assigned to the resource. Learn more Tagging Amazon Web Services Resources in the Amazon Web Services General Reference Amazon Web Services Tagging Strategies Related actions All APIs by task"]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceARN: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that uniquely identifies the Amazon GameLift Servers resource that you want to remove tags from. Amazon GameLift Servers includes resource ARNs in the data object for the resource. You can retrieve the ARN by calling a List or Describe operation for the resource type."];
      tagKeys: TagKeyList.t
        [@ocaml.doc
          "A list of one or more tag keys to remove from the specified Amazon GameLift Servers resource."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceARN =
      fun ~tagKeys -> fun () -> { resourceARN; tagKeys }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (AmazonResourceName.to_value x.resourceARN)));
        ("TagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TagKeys") in
      let resourceARN =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~tagKeys ~resourceARN ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map_exn json__ "TagKeys" TagKeyList.of_json in
      let resourceARN =
        field_map_exn json__ "ResourceARN" AmazonResourceName.of_json in
      make ~tagKeys ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Removes a tag assigned to a Amazon GameLift Servers resource. You can use resource tags to organize Amazon Web Services resources for a range of purposes. This operation handles the permissions necessary to manage tags for Amazon GameLift Servers resources that support tagging. To remove a tag from a resource, specify the unique ARN value for the resource and provide a string list containing one or more tags to remove. This operation succeeds even if the list includes tags that aren't assigned to the resource. Learn more Tagging Amazon Web Services Resources in the Amazon Web Services General Reference Amazon Web Services Tagging Strategies Related actions All APIs by task"]
module TerminateGameSessionOutput =
  struct
    type nonrec t = {
      gameSession: GameSession.t option }
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidGameSessionStatusException of
          InvalidGameSessionStatusException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `NotReadyException of NotReadyException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameSession = fun () -> { gameSession }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidGameSessionStatusException" ->
          `InvalidGameSessionStatusException
            (InvalidGameSessionStatusException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "NotReadyException" ->
          `NotReadyException (NotReadyException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidGameSessionStatusException" ->
          `InvalidGameSessionStatusException
            (InvalidGameSessionStatusException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "NotReadyException" ->
          `NotReadyException (NotReadyException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidGameSessionStatusException e ->
          `Assoc
            [("error", (`String "InvalidGameSessionStatusException"));
            ("details", (InvalidGameSessionStatusException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `NotReadyException e ->
          `Assoc
            [("error", (`String "NotReadyException"));
            ("details", (NotReadyException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameSession", (Option.map x.gameSession ~f:GameSession.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameSession =
        (Option.map ~f:GameSession.of_xml) (Xml.child xml_arg0 "GameSession") in
      make ?gameSession ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameSession = field_map json__ "GameSession" GameSession.of_json in
      make ?gameSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Ends a game session that's currently in progress. Use this action to terminate any game session that isn't in ERROR status. Terminating a game session is the most efficient way to free up a server process when it's hosting a game session that's in a bad state or not ending properly. You can use this action to terminate a game session that's being hosted on any type of Amazon GameLift Servers fleet compute, including computes for managed EC2, managed container, and Anywhere fleets. The game server must be integrated with Amazon GameLift Servers server SDK 5.x or greater. Request options Request termination for a single game session. Provide the game session ID and the termination mode. There are two potential methods for terminating a game session: Initiate a graceful termination using the normal game session shutdown sequence. With this mode, the Amazon GameLift Servers service prompts the server process that's hosting the game session by calling the server SDK callback method OnProcessTerminate(). The callback implementation is part of the custom game server code. It might involve a variety of actions to gracefully end a game session, such as notifying players, before stopping the server process. Force an immediate game session termination. With this mode, the Amazon GameLift Servers service takes action to stop the server process, which ends the game session without the normal game session shutdown sequence. Results If successful, game session termination is initiated. During this activity, the game session status is changed to TERMINATING. When completed, the server process that was hosting the game session has been stopped and replaced with a new server process that's ready to host a new game session. The old game session's status is changed to TERMINATED with a status reason that indicates the termination method used. Learn more Add Amazon GameLift Servers to your game server Amazon GameLift Servers server SDK 5 reference guide for OnProcessTerminate() (C++) (C#) (Unreal) (Go)"]
module TerminateGameSessionInput =
  struct
    type nonrec t =
      {
      gameSessionId: ArnStringModel.t
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions to be terminated. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>."];
      terminationMode: TerminationMode.t
        [@ocaml.doc
          "The method to use to terminate the game session. Available methods include: TRIGGER_ON_PROCESS_TERMINATE \226\128\147 Prompts the Amazon GameLift Servers service to send an OnProcessTerminate() callback to the server process and initiate the normal game session shutdown sequence. The OnProcessTerminate method, which is implemented in the game server code, must include a call to the server SDK action ProcessEnding(), which is how the server process signals to Amazon GameLift Servers that a game session is ending. If the server process doesn't call ProcessEnding(), the game session termination won't conclude successfully. FORCE_TERMINATE \226\128\147 Prompts the Amazon GameLift Servers service to stop the server process immediately. Amazon GameLift Servers takes action (depending on the type of fleet) to shut down the server process without the normal game session shutdown sequence. This method is not available for game sessions that are running on Anywhere fleets unless the fleet is deployed with the Amazon GameLift Servers Agent. In this scenario, a force terminate request results in an invalid or bad request exception."]}
    let context_ = "TerminateGameSessionInput"
    let make ~gameSessionId =
      fun ~terminationMode -> fun () -> { gameSessionId; terminationMode }
    let to_value x =
      structure_to_value
        [("GameSessionId", (Some (ArnStringModel.to_value x.gameSessionId)));
        ("TerminationMode",
          (Some (TerminationMode.to_value x.terminationMode)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let terminationMode =
        TerminationMode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TerminationMode") in
      let gameSessionId =
        ArnStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameSessionId") in
      make ~terminationMode ~gameSessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let terminationMode =
        field_map_exn json__ "TerminationMode" TerminationMode.of_json in
      let gameSessionId =
        field_map_exn json__ "GameSessionId" ArnStringModel.of_json in
      make ~terminationMode ~gameSessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Ends a game session that's currently in progress. Use this action to terminate any game session that isn't in ERROR status. Terminating a game session is the most efficient way to free up a server process when it's hosting a game session that's in a bad state or not ending properly. You can use this action to terminate a game session that's being hosted on any type of Amazon GameLift Servers fleet compute, including computes for managed EC2, managed container, and Anywhere fleets. The game server must be integrated with Amazon GameLift Servers server SDK 5.x or greater. Request options Request termination for a single game session. Provide the game session ID and the termination mode. There are two potential methods for terminating a game session: Initiate a graceful termination using the normal game session shutdown sequence. With this mode, the Amazon GameLift Servers service prompts the server process that's hosting the game session by calling the server SDK callback method OnProcessTerminate(). The callback implementation is part of the custom game server code. It might involve a variety of actions to gracefully end a game session, such as notifying players, before stopping the server process. Force an immediate game session termination. With this mode, the Amazon GameLift Servers service takes action to stop the server process, which ends the game session without the normal game session shutdown sequence. Results If successful, game session termination is initiated. During this activity, the game session status is changed to TERMINATING. When completed, the server process that was hosting the game session has been stopped and replaced with a new server process that's ready to host a new game session. The old game session's status is changed to TERMINATED with a status reason that indicates the termination method used. Learn more Add Amazon GameLift Servers to your game server Amazon GameLift Servers server SDK 5 reference guide for OnProcessTerminate() (C++) (C#) (Unreal) (Go)"]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Assigns a tag to an Amazon GameLift Servers resource. You can use tags to organize resources, create IAM permissions policies to manage access to groups of resources, customize Amazon Web Services cost breakdowns, and more. This operation handles the permissions necessary to manage tags for Amazon GameLift Servers resources that support tagging. To add a tag to a resource, specify the unique ARN value for the resource and provide a tag list containing one or more tags. The operation succeeds even if the list includes tags that are already assigned to the resource. Learn more Tagging Amazon Web Services Resources in the Amazon Web Services General Reference Amazon Web Services Tagging Strategies Related actions All APIs by task"]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceARN: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that uniquely identifies the Amazon GameLift Servers resource that you want to assign tags to. Amazon GameLift Servers includes resource ARNs in the data object for the resource. You can retrieve the ARN by calling a List or Describe operation for the resource type."];
      tags: TagList.t
        [@ocaml.doc
          "A list of one or more tags to assign to the specified Amazon GameLift Servers resource. Tags are developer-defined and structured as key-value pairs. The maximum tag limit may be lower than stated. See Tagging Amazon Web Services Resources for tagging limits."]}
    let context_ = "TagResourceRequest"
    let make ~resourceARN = fun ~tags -> fun () -> { resourceARN; tags }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (AmazonResourceName.to_value x.resourceARN)));
        ("Tags", (Some (TagList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resourceARN =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~tags ~resourceARN ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "Tags" TagList.of_json in
      let resourceARN =
        field_map_exn json__ "ResourceARN" AmazonResourceName.of_json in
      make ~tags ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Assigns a tag to an Amazon GameLift Servers resource. You can use tags to organize resources, create IAM permissions policies to manage access to groups of resources, customize Amazon Web Services cost breakdowns, and more. This operation handles the permissions necessary to manage tags for Amazon GameLift Servers resources that support tagging. To add a tag to a resource, specify the unique ARN value for the resource and provide a tag list containing one or more tags. The operation succeeds even if the list includes tags that are already assigned to the resource. Learn more Tagging Amazon Web Services Resources in the Amazon Web Services General Reference Amazon Web Services Tagging Strategies Related actions All APIs by task"]
module SuspendGameServerGroupOutput =
  struct
    type nonrec t =
      {
      gameServerGroup: GameServerGroup.t option
        [@ocaml.doc
          "An object that describes the game server group resource, with the SuspendedActions property updated to reflect the suspended activity."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameServerGroup = fun () -> { gameServerGroup }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameServerGroup",
           (Option.map x.gameServerGroup ~f:GameServerGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServerGroup =
        (Option.map ~f:GameServerGroup.of_xml)
          (Xml.child xml_arg0 "GameServerGroup") in
      make ?gameServerGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServerGroup =
        field_map json__ "GameServerGroup" GameServerGroup.of_json in
      make ?gameServerGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Temporarily stops activity on a game server group without terminating instances or the game server group. You can restart activity by calling ResumeGameServerGroup. You can suspend the following activity: Instance type replacement - This activity evaluates the current game hosting viability of all Spot instance types that are defined for the game server group. It updates the Auto Scaling group to remove nonviable Spot Instance types, which have a higher chance of game server interruptions. It then balances capacity across the remaining viable Spot Instance types. When this activity is suspended, the Auto Scaling group continues with its current balance, regardless of viability. Instance protection, utilization metrics, and capacity scaling activities continue to be active. To suspend activity, specify a game server group ARN and the type of activity to be suspended. If successful, a GameServerGroup object is returned showing that the activity is listed in SuspendedActions. Learn more Amazon GameLift Servers FleetIQ Guide"]
module SuspendGameServerGroupInput =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupNameOrArn.t
        [@ocaml.doc
          "A unique identifier for the game server group. Use either the name or ARN value."];
      suspendActions: GameServerGroupActions.t
        [@ocaml.doc "The activity to suspend for this game server group."]}
    let context_ = "SuspendGameServerGroupInput"
    let make ~gameServerGroupName =
      fun ~suspendActions ->
        fun () -> { gameServerGroupName; suspendActions }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Some (GameServerGroupNameOrArn.to_value x.gameServerGroupName)));
        ("SuspendActions",
          (Some (GameServerGroupActions.to_value x.suspendActions)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let suspendActions =
        GameServerGroupActions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SuspendActions") in
      let gameServerGroupName =
        GameServerGroupNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerGroupName") in
      make ~suspendActions ~gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let suspendActions =
        field_map_exn json__ "SuspendActions" GameServerGroupActions.of_json in
      let gameServerGroupName =
        field_map_exn json__ "GameServerGroupName"
          GameServerGroupNameOrArn.of_json in
      make ~suspendActions ~gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Temporarily stops activity on a game server group without terminating instances or the game server group. You can restart activity by calling ResumeGameServerGroup. You can suspend the following activity: Instance type replacement - This activity evaluates the current game hosting viability of all Spot instance types that are defined for the game server group. It updates the Auto Scaling group to remove nonviable Spot Instance types, which have a higher chance of game server interruptions. It then balances capacity across the remaining viable Spot Instance types. When this activity is suspended, the Auto Scaling group continues with its current balance, regardless of viability. Instance protection, utilization metrics, and capacity scaling activities continue to be active. To suspend activity, specify a game server group ARN and the type of activity to be suspended. If successful, a GameServerGroup object is returned showing that the activity is listed in SuspendedActions. Learn more Amazon GameLift Servers FleetIQ Guide"]
module StopMatchmakingOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Cancels a matchmaking ticket or match backfill ticket that is currently being processed. To stop the matchmaking operation, specify the ticket ID. If successful, work on the ticket is stopped, and the ticket status is changed to CANCELLED. This call is also used to turn off automatic backfill for an individual game session. This is for game sessions that are created with a matchmaking configuration that has automatic backfill enabled. The ticket ID is included in the MatchmakerData of an updated game session object, which is provided to the game server. If the operation is successful, the service sends back an empty JSON struct with the HTTP 200 response (not an empty HTTP body). Learn more Add FlexMatch to a game client"]
module StopMatchmakingInput =
  struct
    type nonrec t =
      {
      ticketId: MatchmakingIdStringModel.t
        [@ocaml.doc "A unique identifier for a matchmaking ticket."]}
    let context_ = "StopMatchmakingInput"
    let make ~ticketId = fun () -> { ticketId }
    let to_value x =
      structure_to_value
        [("TicketId", (Some (MatchmakingIdStringModel.to_value x.ticketId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ticketId =
        MatchmakingIdStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TicketId") in
      make ~ticketId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ticketId =
        field_map_exn json__ "TicketId" MatchmakingIdStringModel.of_json in
      make ~ticketId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Cancels a matchmaking ticket or match backfill ticket that is currently being processed. To stop the matchmaking operation, specify the ticket ID. If successful, work on the ticket is stopped, and the ticket status is changed to CANCELLED. This call is also used to turn off automatic backfill for an individual game session. This is for game sessions that are created with a matchmaking configuration that has automatic backfill enabled. The ticket ID is included in the MatchmakerData of an updated game session object, which is provided to the game server. If the operation is successful, the service sends back an empty JSON struct with the HTTP 200 response (not an empty HTTP body). Learn more Add FlexMatch to a game client"]
module StopGameSessionPlacementOutput =
  struct
    type nonrec t =
      {
      gameSessionPlacement: GameSessionPlacement.t option
        [@ocaml.doc
          "Object that describes the canceled game session placement, with CANCELLED status and an end time stamp."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameSessionPlacement = fun () -> { gameSessionPlacement }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameSessionPlacement",
           (Option.map x.gameSessionPlacement
              ~f:GameSessionPlacement.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameSessionPlacement =
        (Option.map ~f:GameSessionPlacement.of_xml)
          (Xml.child xml_arg0 "GameSessionPlacement") in
      make ?gameSessionPlacement ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameSessionPlacement =
        field_map json__ "GameSessionPlacement" GameSessionPlacement.of_json in
      make ?gameSessionPlacement ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Cancels a game session placement that's in PENDING status. To stop a placement, provide the placement ID value. Results If successful, this operation removes the placement request from the queue and moves the GameSessionPlacement to CANCELLED status. This operation results in an InvalidRequestExecption (400) error if a game session has already been created for this placement. You can clean up an unneeded game session by calling TerminateGameSession."]
module StopGameSessionPlacementInput =
  struct
    type nonrec t =
      {
      placementId: IdStringModel.t
        [@ocaml.doc
          "A unique identifier for a game session placement to stop."]}
    let context_ = "StopGameSessionPlacementInput"
    let make ~placementId = fun () -> { placementId }
    let to_value x =
      structure_to_value
        [("PlacementId", (Some (IdStringModel.to_value x.placementId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let placementId =
        IdStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PlacementId") in
      make ~placementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let placementId =
        field_map_exn json__ "PlacementId" IdStringModel.of_json in
      make ~placementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Cancels a game session placement that's in PENDING status. To stop a placement, provide the placement ID value. Results If successful, this operation removes the placement request from the queue and moves the GameSessionPlacement to CANCELLED status. This operation results in an InvalidRequestExecption (400) error if a game session has already been created for this placement. You can clean up an unneeded game session by calling TerminateGameSession."]
module StopFleetActionsOutput =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc "A unique identifier for the fleet to stop actions on."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetId = fun ?fleetArn -> fun () -> { fleetId; fleetArn }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Suspends certain types of activity in a fleet location. Currently, this operation is used to stop auto-scaling activity. For multi-location fleets, fleet actions are managed separately for each location. Stopping fleet actions has several potential purposes. It allows you to temporarily stop auto-scaling activity but retain your scaling policies for use in the future. For multi-location fleets, you can set up fleet-wide auto-scaling, and then opt out of it for certain locations. This operation can be used in the following ways: To stop actions on instances in the fleet's home Region, provide a fleet ID and the type of actions to suspend. To stop actions on instances in one of the fleet's remote locations, provide a fleet ID, a location name, and the type of actions to suspend. If successful, Amazon GameLift Servers no longer initiates scaling events except in response to manual changes using UpdateFleetCapacity. To restart fleet actions again, call StartFleetActions. Learn more Setting up Amazon GameLift Servers Fleets"]
module StopFleetActionsInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to stop actions on. You can use either the fleet ID or ARN value."];
      actions: FleetActionList.t
        [@ocaml.doc "List of actions to suspend on the fleet."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The fleet location to stop fleet actions for. Specify a location in the form of an Amazon Web Services Region code, such as us-west-2."]}
    let context_ = "StopFleetActionsInput"
    let make ?location =
      fun ~fleetId ->
        fun ~actions -> fun () -> { location; fleetId; actions }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("Actions", (Some (FleetActionList.to_value x.actions)));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let actions =
        FleetActionList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Actions") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?location ~actions ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let actions = field_map_exn json__ "Actions" FleetActionList.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?location ~actions ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Suspends certain types of activity in a fleet location. Currently, this operation is used to stop auto-scaling activity. For multi-location fleets, fleet actions are managed separately for each location. Stopping fleet actions has several potential purposes. It allows you to temporarily stop auto-scaling activity but retain your scaling policies for use in the future. For multi-location fleets, you can set up fleet-wide auto-scaling, and then opt out of it for certain locations. This operation can be used in the following ways: To stop actions on instances in the fleet's home Region, provide a fleet ID and the type of actions to suspend. To stop actions on instances in one of the fleet's remote locations, provide a fleet ID, a location name, and the type of actions to suspend. If successful, Amazon GameLift Servers no longer initiates scaling events except in response to manual changes using UpdateFleetCapacity. To restart fleet actions again, call StartFleetActions. Learn more Setting up Amazon GameLift Servers Fleets"]
module StartMatchmakingOutput =
  struct
    type nonrec t =
      {
      matchmakingTicket: MatchmakingTicket.t option
        [@ocaml.doc
          "Ticket representing the matchmaking request. This object include the information included in the request, ticket status, and match results as generated during the matchmaking process."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?matchmakingTicket = fun () -> { matchmakingTicket }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("MatchmakingTicket",
           (Option.map x.matchmakingTicket ~f:MatchmakingTicket.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let matchmakingTicket =
        (Option.map ~f:MatchmakingTicket.of_xml)
          (Xml.child xml_arg0 "MatchmakingTicket") in
      make ?matchmakingTicket ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let matchmakingTicket =
        field_map json__ "MatchmakingTicket" MatchmakingTicket.of_json in
      make ?matchmakingTicket ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Uses FlexMatch to create a game match for a group of players based on custom matchmaking rules. With games that use Amazon GameLift Servers managed hosting, this operation also triggers Amazon GameLift Servers to find hosting resources and start a new game session for the new match. Each matchmaking request includes information on one or more players and specifies the FlexMatch matchmaker to use. When a request is for multiple players, FlexMatch attempts to build a match that includes all players in the request, placing them in the same team and finding additional players as needed to fill the match. To start matchmaking, provide a unique ticket ID, specify a matchmaking configuration, and include the players to be matched. You must also include any player attributes that are required by the matchmaking configuration's rule set. If successful, a matchmaking ticket is returned with status set to QUEUED. Track matchmaking events to respond as needed and acquire game session connection information for successfully completed matches. Ticket status updates are tracked using event notification through Amazon Simple Notification Service, which is defined in the matchmaking configuration. Learn more Add FlexMatch to a game client Set Up FlexMatch event notification How Amazon GameLift Servers FlexMatch works"]
module StartMatchmakingInput =
  struct
    type nonrec t =
      {
      ticketId: MatchmakingIdStringModel.t option
        [@ocaml.doc
          "A unique identifier for a matchmaking ticket. If no ticket ID is specified here, Amazon GameLift Servers will generate one in the form of a UUID. Use this identifier to track the matchmaking ticket status and retrieve match results."];
      configurationName: MatchmakingConfigurationName.t
        [@ocaml.doc
          "Name of the matchmaking configuration to use for this request. Matchmaking configurations must exist in the same Region as this request. You can use either the configuration name or ARN value."];
      players: PlayerList.t
        [@ocaml.doc
          "Information on each player to be matched. This information must include a player ID, and may contain player attributes and latency data to be used in the matchmaking process. After a successful match, Player objects contain the name of the team the player is assigned to. You can include up to 10 Players in a StartMatchmaking request."]}
    let context_ = "StartMatchmakingInput"
    let make ?ticketId =
      fun ~configurationName ->
        fun ~players -> fun () -> { ticketId; configurationName; players }
    let to_value x =
      structure_to_value
        [("TicketId",
           (Option.map x.ticketId ~f:MatchmakingIdStringModel.to_value));
        ("ConfigurationName",
          (Some (MatchmakingConfigurationName.to_value x.configurationName)));
        ("Players", (Some (PlayerList.to_value x.players)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let players =
        PlayerList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Players") in
      let configurationName =
        MatchmakingConfigurationName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationName") in
      let ticketId =
        (Option.map ~f:MatchmakingIdStringModel.of_xml)
          (Xml.child xml_arg0 "TicketId") in
      make ~players ~configurationName ?ticketId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let players = field_map_exn json__ "Players" PlayerList.of_json in
      let configurationName =
        field_map_exn json__ "ConfigurationName"
          MatchmakingConfigurationName.of_json in
      let ticketId =
        field_map json__ "TicketId" MatchmakingIdStringModel.of_json in
      make ~players ~configurationName ?ticketId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Uses FlexMatch to create a game match for a group of players based on custom matchmaking rules. With games that use Amazon GameLift Servers managed hosting, this operation also triggers Amazon GameLift Servers to find hosting resources and start a new game session for the new match. Each matchmaking request includes information on one or more players and specifies the FlexMatch matchmaker to use. When a request is for multiple players, FlexMatch attempts to build a match that includes all players in the request, placing them in the same team and finding additional players as needed to fill the match. To start matchmaking, provide a unique ticket ID, specify a matchmaking configuration, and include the players to be matched. You must also include any player attributes that are required by the matchmaking configuration's rule set. If successful, a matchmaking ticket is returned with status set to QUEUED. Track matchmaking events to respond as needed and acquire game session connection information for successfully completed matches. Ticket status updates are tracked using event notification through Amazon Simple Notification Service, which is defined in the matchmaking configuration. Learn more Add FlexMatch to a game client Set Up FlexMatch event notification How Amazon GameLift Servers FlexMatch works"]
module StartMatchBackfillOutput =
  struct
    type nonrec t =
      {
      matchmakingTicket: MatchmakingTicket.t option
        [@ocaml.doc
          "Ticket representing the backfill matchmaking request. This object includes the information in the request, ticket status, and match results as generated during the matchmaking process."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?matchmakingTicket = fun () -> { matchmakingTicket }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("MatchmakingTicket",
           (Option.map x.matchmakingTicket ~f:MatchmakingTicket.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let matchmakingTicket =
        (Option.map ~f:MatchmakingTicket.of_xml)
          (Xml.child xml_arg0 "MatchmakingTicket") in
      make ?matchmakingTicket ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let matchmakingTicket =
        field_map json__ "MatchmakingTicket" MatchmakingTicket.of_json in
      make ?matchmakingTicket ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Finds new players to fill open slots in currently running game sessions. The backfill match process is essentially identical to the process of forming new matches. Backfill requests use the same matchmaker that was used to make the original match, and they provide matchmaking data for all players currently in the game session. FlexMatch uses this information to select new players so that backfilled match continues to meet the original match requirements. When using FlexMatch with Amazon GameLift Servers managed hosting, you can request a backfill match from a client service by calling this operation with a GameSessions ID. You also have the option of making backfill requests directly from your game server. In response to a request, FlexMatch creates player sessions for the new players, updates the GameSession resource, and sends updated matchmaking data to the game server. You can request a backfill match at any point after a game session is started. Each game session can have only one active backfill request at a time; a subsequent request automatically replaces the earlier request. When using FlexMatch as a standalone component, request a backfill match by calling this operation without a game session identifier. As with newly formed matches, matchmaking results are returned in a matchmaking event so that your game can update the game session that is being backfilled. To request a backfill match, specify a unique ticket ID, the original matchmaking configuration, and matchmaking data for all current players in the game session being backfilled. Optionally, specify the GameSession ARN. If successful, a match backfill ticket is created and returned with status set to QUEUED. Track the status of backfill tickets using the same method for tracking tickets for new matches. Only game sessions created by FlexMatch are supported for match backfill. Learn more Backfill existing games with FlexMatch Matchmaking events (reference) How Amazon GameLift Servers FlexMatch works"]
module StartMatchBackfillInput =
  struct
    type nonrec t =
      {
      ticketId: MatchmakingIdStringModel.t option
        [@ocaml.doc
          "A unique identifier for a matchmaking ticket. If no ticket ID is specified here, Amazon GameLift Servers will generate one in the form of a UUID. Use this identifier to track the match backfill ticket status and retrieve match results."];
      configurationName: MatchmakingConfigurationName.t
        [@ocaml.doc
          "Name of the matchmaker to use for this request. You can use either the configuration name or ARN value. The ARN of the matchmaker that was used with the original game session is listed in the GameSession object, MatchmakerData property."];
      gameSessionArn: ArnStringModel.t option
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>. When using FlexMatch as a standalone matchmaking solution, this parameter is not needed."];
      players: PlayerList.t
        [@ocaml.doc
          "Match information on all players that are currently assigned to the game session. This information is used by the matchmaker to find new players and add them to the existing game. You can include up to 199 Players in a StartMatchBackfill request. PlayerID, PlayerAttributes, Team -- This information is maintained in the GameSession object, MatchmakerData property, for all players who are currently assigned to the game session. The matchmaker data is in JSON syntax, formatted as a string. For more details, see Match Data. The backfill request must specify the team membership for every player. Do not specify team if you are not using backfill. LatencyInMs -- If the matchmaker uses player latency, include a latency value, in milliseconds, for the Region that the game session is currently in. Do not include latency values for any other Region."]}
    let context_ = "StartMatchBackfillInput"
    let make ?ticketId =
      fun ?gameSessionArn ->
        fun ~configurationName ->
          fun ~players ->
            fun () ->
              { ticketId; gameSessionArn; configurationName; players }
    let to_value x =
      structure_to_value
        [("TicketId",
           (Option.map x.ticketId ~f:MatchmakingIdStringModel.to_value));
        ("ConfigurationName",
          (Some (MatchmakingConfigurationName.to_value x.configurationName)));
        ("GameSessionArn",
          (Option.map x.gameSessionArn ~f:ArnStringModel.to_value));
        ("Players", (Some (PlayerList.to_value x.players)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let players =
        PlayerList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Players") in
      let gameSessionArn =
        (Option.map ~f:ArnStringModel.of_xml)
          (Xml.child xml_arg0 "GameSessionArn") in
      let configurationName =
        MatchmakingConfigurationName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConfigurationName") in
      let ticketId =
        (Option.map ~f:MatchmakingIdStringModel.of_xml)
          (Xml.child xml_arg0 "TicketId") in
      make ~players ?gameSessionArn ~configurationName ?ticketId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let players = field_map_exn json__ "Players" PlayerList.of_json in
      let gameSessionArn =
        field_map json__ "GameSessionArn" ArnStringModel.of_json in
      let configurationName =
        field_map_exn json__ "ConfigurationName"
          MatchmakingConfigurationName.of_json in
      let ticketId =
        field_map json__ "TicketId" MatchmakingIdStringModel.of_json in
      make ~players ?gameSessionArn ~configurationName ?ticketId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Finds new players to fill open slots in currently running game sessions. The backfill match process is essentially identical to the process of forming new matches. Backfill requests use the same matchmaker that was used to make the original match, and they provide matchmaking data for all players currently in the game session. FlexMatch uses this information to select new players so that backfilled match continues to meet the original match requirements. When using FlexMatch with Amazon GameLift Servers managed hosting, you can request a backfill match from a client service by calling this operation with a GameSessions ID. You also have the option of making backfill requests directly from your game server. In response to a request, FlexMatch creates player sessions for the new players, updates the GameSession resource, and sends updated matchmaking data to the game server. You can request a backfill match at any point after a game session is started. Each game session can have only one active backfill request at a time; a subsequent request automatically replaces the earlier request. When using FlexMatch as a standalone component, request a backfill match by calling this operation without a game session identifier. As with newly formed matches, matchmaking results are returned in a matchmaking event so that your game can update the game session that is being backfilled. To request a backfill match, specify a unique ticket ID, the original matchmaking configuration, and matchmaking data for all current players in the game session being backfilled. Optionally, specify the GameSession ARN. If successful, a match backfill ticket is created and returned with status set to QUEUED. Track the status of backfill tickets using the same method for tracking tickets for new matches. Only game sessions created by FlexMatch are supported for match backfill. Learn more Backfill existing games with FlexMatch Matchmaking events (reference) How Amazon GameLift Servers FlexMatch works"]
module StartGameSessionPlacementOutput =
  struct
    type nonrec t =
      {
      gameSessionPlacement: GameSessionPlacement.t option
        [@ocaml.doc
          "Object that describes the newly created game session placement. This object includes all the information provided in the request, as well as start/end time stamps and placement status."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameSessionPlacement = fun () -> { gameSessionPlacement }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameSessionPlacement",
           (Option.map x.gameSessionPlacement
              ~f:GameSessionPlacement.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameSessionPlacement =
        (Option.map ~f:GameSessionPlacement.of_xml)
          (Xml.child xml_arg0 "GameSessionPlacement") in
      make ?gameSessionPlacement ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameSessionPlacement =
        field_map json__ "GameSessionPlacement" GameSessionPlacement.of_json in
      make ?gameSessionPlacement ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Makes a request to start a new game session using a game session queue. When processing a placement request, Amazon GameLift Servers looks for the best possible available resource to host the game session, based on how the queue is configured to prioritize factors such as resource cost, latency, and location. After selecting an available resource, Amazon GameLift Servers prompts the resource to start a game session. A placement request can include a list of players to create a set of player sessions. The request can also include information to pass to the new game session, such as to specify a game map or other options. Request options Use this operation to make the following types of requests. Request a placement using the queue's default prioritization process (see the default prioritization described in PriorityConfiguration). Include these required parameters: GameSessionQueueName MaximumPlayerSessionCount PlacementID Request a placement and prioritize based on latency. Include these parameters: Required parameters GameSessionQueueName, MaximumPlayerSessionCount, PlacementID. PlayerLatencies. Include a set of latency values for destinations in the queue. When a request includes latency data, Amazon GameLift Servers automatically reorder the queue's locations priority list based on lowest available latency values. If a request includes latency data for multiple players, Amazon GameLift Servers calculates each location's average latency for all players and reorders to find the lowest latency across all players. Don't include PriorityConfigurationOverride. Prioritize based on a custom list of locations. If you're using a queue that's configured to prioritize location first (see PriorityConfiguration for game session queues), you can optionally use the PriorityConfigurationOverride parameter to substitute a different location priority list for this placement request. Amazon GameLift Servers searches each location on the priority override list to find an available hosting resource for the new game session. Specify a fallback strategy to use in the event that Amazon GameLift Servers fails to place the game session in any of the locations on the override list. Request a placement and prioritized based on a custom list of locations. You can request new player sessions for a group of players. Include the DesiredPlayerSessions parameter and include at minimum a unique player ID for each. You can also include player-specific data to pass to the new game session. Result If successful, this operation generates a new game session placement request and adds it to the game session queue for processing. You can track the status of individual placement requests by calling DescribeGameSessionPlacement or by monitoring queue notifications. When the request status is FULFILLED, a new game session has started and the placement request is updated with connection information for the game session (IP address and port). If the request included player session data, Amazon GameLift Servers creates a player session for each player ID in the request. The request results in a InvalidRequestException in the following situations: If the request includes both PlayerLatencies and PriorityConfigurationOverride parameters. If the request includes the PriorityConfigurationOverride parameter and specifies a queue that doesn't prioritize locations. Amazon GameLift Servers continues to retry each placement request until it reaches the queue's timeout setting. If a request times out, you can resubmit the request to the same queue or try a different queue."]
module StartGameSessionPlacementInput =
  struct
    type nonrec t =
      {
      placementId: IdStringModel.t
        [@ocaml.doc
          "A unique identifier to assign to the new game session placement. This value is developer-defined. The value must be unique across all Regions and cannot be reused."];
      gameSessionQueueName: GameSessionQueueNameOrArn.t
        [@ocaml.doc
          "Name of the queue to use to place the new game session. You can use either the queue name or ARN value."];
      gameProperties: GamePropertyList.t option
        [@ocaml.doc
          "A set of key-value pairs that can store custom data in a game session. For example: \\{\"Key\": \"difficulty\", \"Value\": \"novice\"\\}. Avoid using periods (\".\") in property keys if you plan to search for game sessions by properties. Property keys containing periods cannot be searched and will be filtered out from search results due to search index limitations. If you use SearchGameSessions API, there is a limit of 500 game property keys across all game sessions and all fleets per region. If the limit is exceeded, there will potentially be game session entries missing from SearchGameSessions API results."];
      maximumPlayerSessionCount: WholeNumber.t
        [@ocaml.doc
          "The maximum number of players that can be connected simultaneously to the game session."];
      gameSessionName: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a game session. Session names do not need to be unique."];
      playerLatencies: PlayerLatencyList.t option
        [@ocaml.doc
          "A set of values, expressed in milliseconds, that indicates the amount of latency that a player experiences when connected to Amazon Web Services Regions. This information is used to try to place the new game session where it can offer the best possible gameplay experience for the players."];
      desiredPlayerSessions: DesiredPlayerSessionList.t option
        [@ocaml.doc
          "Set of information on each player to create a player session for."];
      gameSessionData: LargeGameSessionData.t option
        [@ocaml.doc
          "A set of custom game session properties, formatted as a single string value. This data is passed to a game server process with a request to start a new game session. For more information, see Start a game session."];
      priorityConfigurationOverride: PriorityConfigurationOverride.t option
        [@ocaml.doc
          "A prioritized list of locations to use for the game session placement and instructions on how to use it. This list overrides a queue's prioritized location list for this game session placement request only. You can include Amazon Web Services Regions, local zones, and custom locations (for Anywhere fleets). You can choose to limit placements to locations on the override list only, or you can prioritize locations on the override list first and then fall back to the queue's other locations if needed. Choose a fallback strategy to use in the event that Amazon GameLift Servers fails to place a game session in any of the locations on the priority override list."]}
    let context_ = "StartGameSessionPlacementInput"
    let make ?gameProperties =
      fun ?gameSessionName ->
        fun ?playerLatencies ->
          fun ?desiredPlayerSessions ->
            fun ?gameSessionData ->
              fun ?priorityConfigurationOverride ->
                fun ~placementId ->
                  fun ~gameSessionQueueName ->
                    fun ~maximumPlayerSessionCount ->
                      fun () ->
                        {
                          gameProperties;
                          gameSessionName;
                          playerLatencies;
                          desiredPlayerSessions;
                          gameSessionData;
                          priorityConfigurationOverride;
                          placementId;
                          gameSessionQueueName;
                          maximumPlayerSessionCount
                        }
    let to_value x =
      structure_to_value
        [("PlacementId", (Some (IdStringModel.to_value x.placementId)));
        ("GameSessionQueueName",
          (Some (GameSessionQueueNameOrArn.to_value x.gameSessionQueueName)));
        ("GameProperties",
          (Option.map x.gameProperties ~f:GamePropertyList.to_value));
        ("MaximumPlayerSessionCount",
          (Some (WholeNumber.to_value x.maximumPlayerSessionCount)));
        ("GameSessionName",
          (Option.map x.gameSessionName ~f:NonZeroAndMaxString.to_value));
        ("PlayerLatencies",
          (Option.map x.playerLatencies ~f:PlayerLatencyList.to_value));
        ("DesiredPlayerSessions",
          (Option.map x.desiredPlayerSessions
             ~f:DesiredPlayerSessionList.to_value));
        ("GameSessionData",
          (Option.map x.gameSessionData ~f:LargeGameSessionData.to_value));
        ("PriorityConfigurationOverride",
          (Option.map x.priorityConfigurationOverride
             ~f:PriorityConfigurationOverride.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let priorityConfigurationOverride =
        (Option.map ~f:PriorityConfigurationOverride.of_xml)
          (Xml.child xml_arg0 "PriorityConfigurationOverride") in
      let gameSessionData =
        (Option.map ~f:LargeGameSessionData.of_xml)
          (Xml.child xml_arg0 "GameSessionData") in
      let desiredPlayerSessions =
        (Option.map ~f:DesiredPlayerSessionList.of_xml)
          (Xml.child xml_arg0 "DesiredPlayerSessions") in
      let playerLatencies =
        (Option.map ~f:PlayerLatencyList.of_xml)
          (Xml.child xml_arg0 "PlayerLatencies") in
      let gameSessionName =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "GameSessionName") in
      let maximumPlayerSessionCount =
        WholeNumber.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "MaximumPlayerSessionCount") in
      let gameProperties =
        (Option.map ~f:GamePropertyList.of_xml)
          (Xml.child xml_arg0 "GameProperties") in
      let gameSessionQueueName =
        GameSessionQueueNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameSessionQueueName") in
      let placementId =
        IdStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PlacementId") in
      make ?priorityConfigurationOverride ?gameSessionData
        ?desiredPlayerSessions ?playerLatencies ?gameSessionName
        ~maximumPlayerSessionCount ?gameProperties ~gameSessionQueueName
        ~placementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let priorityConfigurationOverride =
        field_map json__ "PriorityConfigurationOverride"
          PriorityConfigurationOverride.of_json in
      let gameSessionData =
        field_map json__ "GameSessionData" LargeGameSessionData.of_json in
      let desiredPlayerSessions =
        field_map json__ "DesiredPlayerSessions"
          DesiredPlayerSessionList.of_json in
      let playerLatencies =
        field_map json__ "PlayerLatencies" PlayerLatencyList.of_json in
      let gameSessionName =
        field_map json__ "GameSessionName" NonZeroAndMaxString.of_json in
      let maximumPlayerSessionCount =
        field_map_exn json__ "MaximumPlayerSessionCount" WholeNumber.of_json in
      let gameProperties =
        field_map json__ "GameProperties" GamePropertyList.of_json in
      let gameSessionQueueName =
        field_map_exn json__ "GameSessionQueueName"
          GameSessionQueueNameOrArn.of_json in
      let placementId =
        field_map_exn json__ "PlacementId" IdStringModel.of_json in
      make ?priorityConfigurationOverride ?gameSessionData
        ?desiredPlayerSessions ?playerLatencies ?gameSessionName
        ~maximumPlayerSessionCount ?gameProperties ~gameSessionQueueName
        ~placementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Makes a request to start a new game session using a game session queue. When processing a placement request, Amazon GameLift Servers looks for the best possible available resource to host the game session, based on how the queue is configured to prioritize factors such as resource cost, latency, and location. After selecting an available resource, Amazon GameLift Servers prompts the resource to start a game session. A placement request can include a list of players to create a set of player sessions. The request can also include information to pass to the new game session, such as to specify a game map or other options. Request options Use this operation to make the following types of requests. Request a placement using the queue's default prioritization process (see the default prioritization described in PriorityConfiguration). Include these required parameters: GameSessionQueueName MaximumPlayerSessionCount PlacementID Request a placement and prioritize based on latency. Include these parameters: Required parameters GameSessionQueueName, MaximumPlayerSessionCount, PlacementID. PlayerLatencies. Include a set of latency values for destinations in the queue. When a request includes latency data, Amazon GameLift Servers automatically reorder the queue's locations priority list based on lowest available latency values. If a request includes latency data for multiple players, Amazon GameLift Servers calculates each location's average latency for all players and reorders to find the lowest latency across all players. Don't include PriorityConfigurationOverride. Prioritize based on a custom list of locations. If you're using a queue that's configured to prioritize location first (see PriorityConfiguration for game session queues), you can optionally use the PriorityConfigurationOverride parameter to substitute a different location priority list for this placement request. Amazon GameLift Servers searches each location on the priority override list to find an available hosting resource for the new game session. Specify a fallback strategy to use in the event that Amazon GameLift Servers fails to place the game session in any of the locations on the override list. Request a placement and prioritized based on a custom list of locations. You can request new player sessions for a group of players. Include the DesiredPlayerSessions parameter and include at minimum a unique player ID for each. You can also include player-specific data to pass to the new game session. Result If successful, this operation generates a new game session placement request and adds it to the game session queue for processing. You can track the status of individual placement requests by calling DescribeGameSessionPlacement or by monitoring queue notifications. When the request status is FULFILLED, a new game session has started and the placement request is updated with connection information for the game session (IP address and port). If the request included player session data, Amazon GameLift Servers creates a player session for each player ID in the request. The request results in a InvalidRequestException in the following situations: If the request includes both PlayerLatencies and PriorityConfigurationOverride parameters. If the request includes the PriorityConfigurationOverride parameter and specifies a queue that doesn't prioritize locations. Amazon GameLift Servers continues to retry each placement request until it reaches the queue's timeout setting. If a request times out, you can resubmit the request to the same queue or try a different queue."]
module StartFleetActionsOutput =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc
          "A unique identifier for the fleet to restart actions on."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetId = fun ?fleetArn -> fun () -> { fleetId; fleetArn }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Resumes certain types of activity on fleet instances that were suspended with StopFleetActions. For multi-location fleets, fleet actions are managed separately for each location. Currently, this operation is used to restart a fleet's auto-scaling activity. This operation can be used in the following ways: To restart actions on instances in the fleet's home Region, provide a fleet ID and the type of actions to resume. To restart actions on instances in one of the fleet's remote locations, provide a fleet ID, a location name, and the type of actions to resume. If successful, Amazon GameLift Servers once again initiates scaling events as triggered by the fleet's scaling policies. If actions on the fleet location were never stopped, this operation will have no effect. Learn more Setting up Amazon GameLift Servers fleets"]
module StartFleetActionsInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to restart actions on. You can use either the fleet ID or ARN value."];
      actions: FleetActionList.t
        [@ocaml.doc "List of actions to restart on the fleet."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The fleet location to restart fleet actions for. Specify a location in the form of an Amazon Web Services Region code, such as us-west-2."]}
    let context_ = "StartFleetActionsInput"
    let make ?location =
      fun ~fleetId ->
        fun ~actions -> fun () -> { location; fleetId; actions }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("Actions", (Some (FleetActionList.to_value x.actions)));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let actions =
        FleetActionList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Actions") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?location ~actions ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let actions = field_map_exn json__ "Actions" FleetActionList.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?location ~actions ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Resumes certain types of activity on fleet instances that were suspended with StopFleetActions. For multi-location fleets, fleet actions are managed separately for each location. Currently, this operation is used to restart a fleet's auto-scaling activity. This operation can be used in the following ways: To restart actions on instances in the fleet's home Region, provide a fleet ID and the type of actions to resume. To restart actions on instances in one of the fleet's remote locations, provide a fleet ID, a location name, and the type of actions to resume. If successful, Amazon GameLift Servers once again initiates scaling events as triggered by the fleet's scaling policies. If actions on the fleet location were never stopped, this operation will have no effect. Learn more Setting up Amazon GameLift Servers fleets"]
module SearchGameSessionsOutput =
  struct
    type nonrec t =
      {
      gameSessions: GameSessionList.t option
        [@ocaml.doc
          "A collection of objects containing game session properties for each session that matches the request."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TerminalRoutingStrategyException of
          TerminalRoutingStrategyException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameSessions =
      fun ?nextToken -> fun () -> { gameSessions; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TerminalRoutingStrategyException e ->
          `Assoc
            [("error", (`String "TerminalRoutingStrategyException"));
            ("details", (TerminalRoutingStrategyException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameSessions",
           (Option.map x.gameSessions ~f:GameSessionList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let gameSessions =
        (Option.map ~f:GameSessionList.of_xml)
          (Xml.child xml_arg0 "GameSessions") in
      make ?nextToken ?gameSessions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let gameSessions =
        field_map json__ "GameSessions" GameSessionList.of_json in
      make ?nextToken ?gameSessions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves all active game sessions that match a set of search criteria and sorts them into a specified order. This operation is not designed to continually track game session status because that practice can cause you to exceed your API limit and generate errors. Instead, configure an Amazon Simple Notification Service (Amazon SNS) topic to receive notifications from a matchmaker or a game session placement queue. When searching for game sessions, you specify exactly where you want to search and provide a search filter expression, a sort expression, or both. A search request can search only one fleet, but it can search all of a fleet's locations. This operation can be used in the following ways: To search all game sessions that are currently running on all locations in a fleet, provide a fleet or alias ID. This approach returns game sessions in the fleet's home Region and all remote locations that fit the search criteria. To search all game sessions that are currently running on a specific fleet location, provide a fleet or alias ID and a location name. For location, you can specify a fleet's home Region or any remote location. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, a GameSession object is returned for each game session that matches the request. Search finds game sessions that are in ACTIVE status only. To retrieve information on game sessions in other statuses, use DescribeGameSessions. To set search and sort criteria, create a filter expression using the following game session attributes. For game session search examples, see the Examples section of this topic. gameSessionId -- An identifier for the game session that is unique across all regions. You must use the full ARN value. gameSessionName -- Name assigned to a game session. Game session names do not need to be unique to a game session. gameSessionProperties -- A set of key-value pairs that can store custom data in a game session. For example: \\{\"Key\": \"difficulty\", \"Value\": \"novice\"\\}. The filter expression must specify the https://docs.aws.amazon.com/gamelift/latest/apireference/API_GameProperty -- a Key and a string Value to search for the game sessions. For example, to search for the above key-value pair, specify the following search filter: gameSessionProperties.difficulty = \"novice\". All game property values are searched as strings. For examples of searching game sessions, see the ones below, and also see Search game sessions by game property. Avoid using periods (\".\") in property keys if you plan to search for game sessions by properties. Property keys containing periods cannot be searched and will be filtered out from search results due to search index limitations. If you use SearchGameSessions API, there is a limit of 500 game property keys across all game sessions and all fleets per region. If the limit is exceeded, there will potentially be game session entries missing from SearchGameSessions API results. maximumSessions -- Maximum number of player sessions allowed for a game session. creationTimeMillis -- Value indicating when a game session was created. It is expressed in Unix time as milliseconds. playerSessionCount -- Number of players currently connected to a game session. This value changes rapidly as players join the session or drop out. hasAvailablePlayerSessions -- Boolean value indicating whether a game session has reached its maximum number of players. It is highly recommended that all search requests include this filter attribute to optimize search performance and return only sessions that players can join. Returned values for playerSessionCount and hasAvailablePlayerSessions change quickly as players join sessions and others drop out. Results should be considered a snapshot in time. Be sure to refresh search results often, and handle sessions that fill up before a player can join. All APIs by task"]
module SearchGameSessionsInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the fleet to search for active game sessions. You can use either the fleet ID or ARN value. Each request must reference either a fleet ID or alias ID, but not both."];
      aliasId: AliasIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the alias associated with the fleet to search for active game sessions. You can use either the alias ID or ARN value. Each request must reference either a fleet ID or alias ID, but not both."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "A fleet location to search for game sessions. You can specify a fleet's home Region or a remote location. Use the Amazon Web Services Region code format, such as us-west-2."];
      filterExpression: NonZeroAndMaxString.t option
        [@ocaml.doc
          "String containing the search criteria for the session search. If no filter expression is included, the request returns results for all game sessions in the fleet that are in ACTIVE status. A filter expression can contain one or multiple conditions. Each condition consists of the following: Operand -- Name of a game session attribute. Valid values are gameSessionName, gameSessionId, gameSessionProperties, maximumSessions, creationTimeMillis, playerSessionCount, hasAvailablePlayerSessions. Comparator -- Valid comparators are: =, <>, <, >, <=, >=. Value -- Value to be searched for. Values may be numbers, boolean values (true/false) or strings depending on the operand. String values are case sensitive and must be enclosed in single quotes. Special characters must be escaped. Boolean and string values can only be used with the comparators = and <>. For example, the following filter expression searches on gameSessionName: \"FilterExpression\": \"gameSessionName = 'Matt\\\\'s Awesome Game 1'\". To chain multiple conditions in a single expression, use the logical keywords AND, OR, and NOT and parentheses as needed. For example: x AND y AND NOT z, NOT (x OR y). Session search evaluates conditions from left to right using the following precedence rules: =, <>, <, >, <=, >= Parentheses NOT AND OR For example, this filter expression retrieves game sessions hosting at least ten players that have an open player slot: \"maximumSessions>=10 AND hasAvailablePlayerSessions=true\"."];
      sortExpression: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Instructions on how to sort the search results. If no sort expression is included, the request returns results in random order. A sort expression consists of the following elements: Operand -- Name of a game session attribute. Valid values are gameSessionName, gameSessionId, gameSessionProperties, maximumSessions, creationTimeMillis, playerSessionCount, hasAvailablePlayerSessions. Order -- Valid sort orders are ASC (ascending) and DESC (descending). For example, this sort expression returns the oldest active sessions first: \"SortExpression\": \"creationTimeMillis ASC\". Results with a null value for the sort operand are returned at the end of the list."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages. The maximum number of results returned is 20, even if this value is not set or is set higher than 20."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?fleetId =
      fun ?aliasId ->
        fun ?location ->
          fun ?filterExpression ->
            fun ?sortExpression ->
              fun ?limit ->
                fun ?nextToken ->
                  fun () ->
                    {
                      fleetId;
                      aliasId;
                      location;
                      filterExpression;
                      sortExpression;
                      limit;
                      nextToken
                    }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetIdOrArn.to_value));
        ("AliasId", (Option.map x.aliasId ~f:AliasIdOrArn.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value));
        ("FilterExpression",
          (Option.map x.filterExpression ~f:NonZeroAndMaxString.to_value));
        ("SortExpression",
          (Option.map x.sortExpression ~f:NonZeroAndMaxString.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let sortExpression =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "SortExpression") in
      let filterExpression =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "FilterExpression") in
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let aliasId =
        (Option.map ~f:AliasIdOrArn.of_xml) (Xml.child xml_arg0 "AliasId") in
      let fleetId =
        (Option.map ~f:FleetIdOrArn.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?nextToken ?limit ?sortExpression ?filterExpression ?location
        ?aliasId ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let sortExpression =
        field_map json__ "SortExpression" NonZeroAndMaxString.of_json in
      let filterExpression =
        field_map json__ "FilterExpression" NonZeroAndMaxString.of_json in
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let aliasId = field_map json__ "AliasId" AliasIdOrArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetIdOrArn.of_json in
      make ?nextToken ?limit ?sortExpression ?filterExpression ?location
        ?aliasId ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves all active game sessions that match a set of search criteria and sorts them into a specified order. This operation is not designed to continually track game session status because that practice can cause you to exceed your API limit and generate errors. Instead, configure an Amazon Simple Notification Service (Amazon SNS) topic to receive notifications from a matchmaker or a game session placement queue. When searching for game sessions, you specify exactly where you want to search and provide a search filter expression, a sort expression, or both. A search request can search only one fleet, but it can search all of a fleet's locations. This operation can be used in the following ways: To search all game sessions that are currently running on all locations in a fleet, provide a fleet or alias ID. This approach returns game sessions in the fleet's home Region and all remote locations that fit the search criteria. To search all game sessions that are currently running on a specific fleet location, provide a fleet or alias ID and a location name. For location, you can specify a fleet's home Region or any remote location. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, a GameSession object is returned for each game session that matches the request. Search finds game sessions that are in ACTIVE status only. To retrieve information on game sessions in other statuses, use DescribeGameSessions. To set search and sort criteria, create a filter expression using the following game session attributes. For game session search examples, see the Examples section of this topic. gameSessionId -- An identifier for the game session that is unique across all regions. You must use the full ARN value. gameSessionName -- Name assigned to a game session. Game session names do not need to be unique to a game session. gameSessionProperties -- A set of key-value pairs that can store custom data in a game session. For example: \\{\"Key\": \"difficulty\", \"Value\": \"novice\"\\}. The filter expression must specify the https://docs.aws.amazon.com/gamelift/latest/apireference/API_GameProperty -- a Key and a string Value to search for the game sessions. For example, to search for the above key-value pair, specify the following search filter: gameSessionProperties.difficulty = \"novice\". All game property values are searched as strings. For examples of searching game sessions, see the ones below, and also see Search game sessions by game property. Avoid using periods (\".\") in property keys if you plan to search for game sessions by properties. Property keys containing periods cannot be searched and will be filtered out from search results due to search index limitations. If you use SearchGameSessions API, there is a limit of 500 game property keys across all game sessions and all fleets per region. If the limit is exceeded, there will potentially be game session entries missing from SearchGameSessions API results. maximumSessions -- Maximum number of player sessions allowed for a game session. creationTimeMillis -- Value indicating when a game session was created. It is expressed in Unix time as milliseconds. playerSessionCount -- Number of players currently connected to a game session. This value changes rapidly as players join the session or drop out. hasAvailablePlayerSessions -- Boolean value indicating whether a game session has reached its maximum number of players. It is highly recommended that all search requests include this filter attribute to optimize search performance and return only sessions that players can join. Returned values for playerSessionCount and hasAvailablePlayerSessions change quickly as players join sessions and others drop out. Results should be considered a snapshot in time. Be sure to refresh search results often, and handle sessions that fill up before a player can join. All APIs by task"]
module ResumeGameServerGroupOutput =
  struct
    type nonrec t =
      {
      gameServerGroup: GameServerGroup.t option
        [@ocaml.doc
          "An object that describes the game server group resource, with the SuspendedActions property updated to reflect the resumed activity."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameServerGroup = fun () -> { gameServerGroup }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameServerGroup",
           (Option.map x.gameServerGroup ~f:GameServerGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServerGroup =
        (Option.map ~f:GameServerGroup.of_xml)
          (Xml.child xml_arg0 "GameServerGroup") in
      make ?gameServerGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServerGroup =
        field_map json__ "GameServerGroup" GameServerGroup.of_json in
      make ?gameServerGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Reinstates activity on a game server group after it has been suspended. A game server group might be suspended by the SuspendGameServerGroup operation, or it might be suspended involuntarily due to a configuration problem. In the second case, you can manually resume activity on the group once the configuration problem has been resolved. Refer to the game server group status and status reason for more information on why group activity is suspended. To resume activity, specify a game server group ARN and the type of activity to be resumed. If successful, a GameServerGroup object is returned showing that the resumed activity is no longer listed in SuspendedActions. Learn more Amazon GameLift Servers FleetIQ Guide"]
module ResumeGameServerGroupInput =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupNameOrArn.t
        [@ocaml.doc
          "A unique identifier for the game server group. Use either the name or ARN value."];
      resumeActions: GameServerGroupActions.t
        [@ocaml.doc "The activity to resume for this game server group."]}
    let context_ = "ResumeGameServerGroupInput"
    let make ~gameServerGroupName =
      fun ~resumeActions -> fun () -> { gameServerGroupName; resumeActions }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Some (GameServerGroupNameOrArn.to_value x.gameServerGroupName)));
        ("ResumeActions",
          (Some (GameServerGroupActions.to_value x.resumeActions)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resumeActions =
        GameServerGroupActions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResumeActions") in
      let gameServerGroupName =
        GameServerGroupNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerGroupName") in
      make ~resumeActions ~gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resumeActions =
        field_map_exn json__ "ResumeActions" GameServerGroupActions.of_json in
      let gameServerGroupName =
        field_map_exn json__ "GameServerGroupName"
          GameServerGroupNameOrArn.of_json in
      make ~resumeActions ~gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Reinstates activity on a game server group after it has been suspended. A game server group might be suspended by the SuspendGameServerGroup operation, or it might be suspended involuntarily due to a configuration problem. In the second case, you can manually resume activity on the group once the configuration problem has been resolved. Refer to the game server group status and status reason for more information on why group activity is suspended. To resume activity, specify a game server group ARN and the type of activity to be resumed. If successful, a GameServerGroup object is returned showing that the resumed activity is no longer listed in SuspendedActions. Learn more Amazon GameLift Servers FleetIQ Guide"]
module ResolveAliasOutput =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc "The fleet identifier that the alias is pointing to."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the GameLift fleet resource that this alias points to."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TerminalRoutingStrategyException of
          TerminalRoutingStrategyException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetId = fun ?fleetArn -> fun () -> { fleetId; fleetArn }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TerminalRoutingStrategyException e ->
          `Assoc
            [("error", (`String "TerminalRoutingStrategyException"));
            ("details", (TerminalRoutingStrategyException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Attempts to retrieve a fleet ID that is associated with an alias. Specify a unique alias identifier. If the alias has a SIMPLE routing strategy, Amazon GameLift Servers returns a fleet ID. If the alias has a TERMINAL routing strategy, the result is a TerminalRoutingStrategyException. Related actions All APIs by task"]
module ResolveAliasInput =
  struct
    type nonrec t =
      {
      aliasId: AliasIdOrArn.t
        [@ocaml.doc
          "The unique identifier of the alias that you want to retrieve a fleet ID for. You can use either the alias ID or ARN value."]}
    let context_ = "ResolveAliasInput"
    let make ~aliasId = fun () -> { aliasId }
    let to_value x =
      structure_to_value
        [("AliasId", (Some (AliasIdOrArn.to_value x.aliasId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let aliasId =
        AliasIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AliasId") in
      make ~aliasId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let aliasId = field_map_exn json__ "AliasId" AliasIdOrArn.of_json in
      make ~aliasId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Attempts to retrieve a fleet ID that is associated with an alias. Specify a unique alias identifier. If the alias has a SIMPLE routing strategy, Amazon GameLift Servers returns a fleet ID. If the alias has a TERMINAL routing strategy, the result is a TerminalRoutingStrategyException. Related actions All APIs by task"]
module RequestUploadCredentialsOutput =
  struct
    type nonrec t =
      {
      uploadCredentials: AwsCredentials.t option
        [@ocaml.doc
          "Amazon Web Services credentials required when uploading a game build to the storage location. These credentials have a limited lifespan and are valid only for the build they were issued for."];
      storageLocation: S3Location.t option
        [@ocaml.doc
          "Amazon S3 path and key, identifying where the game build files are stored."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?uploadCredentials =
      fun ?storageLocation ->
        fun () -> { uploadCredentials; storageLocation }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("UploadCredentials",
           (Option.map x.uploadCredentials ~f:AwsCredentials.to_value));
        ("StorageLocation",
          (Option.map x.storageLocation ~f:S3Location.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let storageLocation =
        (Option.map ~f:S3Location.of_xml)
          (Xml.child xml_arg0 "StorageLocation") in
      let uploadCredentials =
        (Option.map ~f:AwsCredentials.of_xml)
          (Xml.child xml_arg0 "UploadCredentials") in
      make ?storageLocation ?uploadCredentials ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let storageLocation =
        field_map json__ "StorageLocation" S3Location.of_json in
      let uploadCredentials =
        field_map json__ "UploadCredentials" AwsCredentials.of_json in
      make ?storageLocation ?uploadCredentials ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves a fresh set of credentials for use when uploading a new set of game build files to Amazon GameLift Servers's Amazon S3. This is done as part of the build creation process; see CreateBuild. To request new credentials, specify the build ID as returned with an initial CreateBuild request. If successful, a new set of credentials are returned, along with the S3 storage location associated with the build ID. Learn more Create a Build with Files in S3 All APIs by task"]
module RequestUploadCredentialsInput =
  struct
    type nonrec t =
      {
      buildId: BuildIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the build to get credentials for. You can use either the build ID or ARN value."]}
    let context_ = "RequestUploadCredentialsInput"
    let make ~buildId = fun () -> { buildId }
    let to_value x =
      structure_to_value
        [("BuildId", (Some (BuildIdOrArn.to_value x.buildId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let buildId =
        BuildIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BuildId") in
      make ~buildId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let buildId = field_map_exn json__ "BuildId" BuildIdOrArn.of_json in
      make ~buildId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves a fresh set of credentials for use when uploading a new set of game build files to Amazon GameLift Servers's Amazon S3. This is done as part of the build creation process; see CreateBuild. To request new credentials, specify the build ID as returned with an initial CreateBuild request. If successful, a new set of credentials are returned, along with the S3 storage location associated with the build ID. Learn more Create a Build with Files in S3 All APIs by task"]
module RegisterGameServerOutput =
  struct
    type nonrec t =
      {
      gameServer: GameServer.t option
        [@ocaml.doc
          "Object that describes the newly registered game server."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameServer = fun () -> { gameServer }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameServer", (Option.map x.gameServer ~f:GameServer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServer =
        (Option.map ~f:GameServer.of_xml) (Xml.child xml_arg0 "GameServer") in
      make ?gameServer ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServer = field_map json__ "GameServer" GameServer.of_json in
      make ?gameServer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Creates a new game server resource and notifies Amazon GameLift Servers FleetIQ that the game server is ready to host gameplay and players. This operation is called by a game server process that is running on an instance in a game server group. Registering game servers enables Amazon GameLift Servers FleetIQ to track available game servers and enables game clients and services to claim a game server for a new game session. To register a game server, identify the game server group and instance where the game server is running, and provide a unique identifier for the game server. You can also include connection and game server data. Once a game server is successfully registered, it is put in status AVAILABLE. A request to register a game server may fail if the instance it is running on is in the process of shutting down as part of instance balancing or scale-down activity. Learn more Amazon GameLift Servers FleetIQ Guide"]
module RegisterGameServerInput =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupNameOrArn.t
        [@ocaml.doc
          "A unique identifier for the game server group where the game server is running."];
      gameServerId: GameServerId.t
        [@ocaml.doc
          "A custom string that uniquely identifies the game server to register. Game server IDs are developer-defined and must be unique across all game server groups in your Amazon Web Services account."];
      instanceId: GameServerInstanceId.t
        [@ocaml.doc
          "The unique identifier for the instance where the game server is running. This ID is available in the instance metadata. EC2 instance IDs use a 17-character format, for example: i-1234567890abcdef0."];
      connectionInfo: GameServerConnectionInfo.t option
        [@ocaml.doc
          "Information that is needed to make inbound client connections to the game server. This might include the IP address and port, DNS name, and other information."];
      gameServerData: GameServerData.t option
        [@ocaml.doc
          "A set of custom game server properties, formatted as a single string value. This data is passed to a game client or service when it requests information on game servers."]}
    let context_ = "RegisterGameServerInput"
    let make ?connectionInfo =
      fun ?gameServerData ->
        fun ~gameServerGroupName ->
          fun ~gameServerId ->
            fun ~instanceId ->
              fun () ->
                {
                  connectionInfo;
                  gameServerData;
                  gameServerGroupName;
                  gameServerId;
                  instanceId
                }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Some (GameServerGroupNameOrArn.to_value x.gameServerGroupName)));
        ("GameServerId", (Some (GameServerId.to_value x.gameServerId)));
        ("InstanceId", (Some (GameServerInstanceId.to_value x.instanceId)));
        ("ConnectionInfo",
          (Option.map x.connectionInfo ~f:GameServerConnectionInfo.to_value));
        ("GameServerData",
          (Option.map x.gameServerData ~f:GameServerData.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServerData =
        (Option.map ~f:GameServerData.of_xml)
          (Xml.child xml_arg0 "GameServerData") in
      let connectionInfo =
        (Option.map ~f:GameServerConnectionInfo.of_xml)
          (Xml.child xml_arg0 "ConnectionInfo") in
      let instanceId =
        GameServerInstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      let gameServerId =
        GameServerId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerId") in
      let gameServerGroupName =
        GameServerGroupNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerGroupName") in
      make ?gameServerData ?connectionInfo ~instanceId ~gameServerId
        ~gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServerData =
        field_map json__ "GameServerData" GameServerData.of_json in
      let connectionInfo =
        field_map json__ "ConnectionInfo" GameServerConnectionInfo.of_json in
      let instanceId =
        field_map_exn json__ "InstanceId" GameServerInstanceId.of_json in
      let gameServerId =
        field_map_exn json__ "GameServerId" GameServerId.of_json in
      let gameServerGroupName =
        field_map_exn json__ "GameServerGroupName"
          GameServerGroupNameOrArn.of_json in
      make ?gameServerData ?connectionInfo ~instanceId ~gameServerId
        ~gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Creates a new game server resource and notifies Amazon GameLift Servers FleetIQ that the game server is ready to host gameplay and players. This operation is called by a game server process that is running on an instance in a game server group. Registering game servers enables Amazon GameLift Servers FleetIQ to track available game servers and enables game clients and services to claim a game server for a new game session. To register a game server, identify the game server group and instance where the game server is running, and provide a unique identifier for the game server. You can also include connection and game server data. Once a game server is successfully registered, it is put in status AVAILABLE. A request to register a game server may fail if the instance it is running on is in the process of shutting down as part of instance balancing or scale-down activity. Learn more Amazon GameLift Servers FleetIQ Guide"]
module RegisterComputeOutput =
  struct
    type nonrec t =
      {
      compute: Compute.t option
        [@ocaml.doc "The details of the compute resource you registered."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotReadyException of NotReadyException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?compute = fun () -> { compute }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotReadyException" ->
          `NotReadyException (NotReadyException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotReadyException" ->
          `NotReadyException (NotReadyException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotReadyException e ->
          `Assoc
            [("error", (`String "NotReadyException"));
            ("details", (NotReadyException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Compute", (Option.map x.compute ~f:Compute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let compute =
        (Option.map ~f:Compute.of_xml) (Xml.child xml_arg0 "Compute") in
      make ?compute ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let compute = field_map json__ "Compute" Compute.of_json in
      make ?compute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Anywhere, Container Registers a compute resource in an Amazon GameLift Servers Anywhere fleet. For an Anywhere fleet that's running the Amazon GameLift Servers Agent, the Agent handles all compute registry tasks for you. For an Anywhere fleet that doesn't use the Agent, call this operation to register fleet computes. To register a compute, give the compute a name (must be unique within the fleet) and specify the compute resource's DNS name or IP address. Provide a fleet ID and a fleet location to associate with the compute being registered. You can optionally include the path to a TLS certificate on the compute resource. If successful, this operation returns compute details, including an Amazon GameLift Servers SDK endpoint or Agent endpoint. Game server processes running on the compute can use this endpoint to communicate with the Amazon GameLift Servers service. Each server process includes the SDK endpoint in its call to the Amazon GameLift Servers server SDK action InitSDK(). To view compute details, call DescribeCompute with the compute name. Learn more Create an Anywhere fleet Test your integration Server SDK reference guides (for version 5.x)"]
module RegisterComputeInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to register the compute to. You can use either the fleet ID or ARN value."];
      computeName: ComputeName.t
        [@ocaml.doc "A descriptive label for the compute resource."];
      certificatePath: NonZeroAndMaxString.t option
        [@ocaml.doc
          "The path to a TLS certificate on your compute resource. Amazon GameLift Servers doesn't validate the path and certificate."];
      dnsName: DnsNameInput.t option
        [@ocaml.doc
          "The DNS name of the compute resource. Amazon GameLift Servers requires either a DNS name or IP address."];
      ipAddress: IpAddress.t option
        [@ocaml.doc
          "The IP address of the compute resource. Amazon GameLift Servers requires either a DNS name or IP address. When registering an Anywhere fleet, an IP address is required."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The name of a custom location to associate with the compute resource being registered. This parameter is required when registering a compute for an Anywhere fleet."]}
    let context_ = "RegisterComputeInput"
    let make ?certificatePath =
      fun ?dnsName ->
        fun ?ipAddress ->
          fun ?location ->
            fun ~fleetId ->
              fun ~computeName ->
                fun () ->
                  {
                    certificatePath;
                    dnsName;
                    ipAddress;
                    location;
                    fleetId;
                    computeName
                  }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("ComputeName", (Some (ComputeName.to_value x.computeName)));
        ("CertificatePath",
          (Option.map x.certificatePath ~f:NonZeroAndMaxString.to_value));
        ("DnsName", (Option.map x.dnsName ~f:DnsNameInput.to_value));
        ("IpAddress", (Option.map x.ipAddress ~f:IpAddress.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let ipAddress =
        (Option.map ~f:IpAddress.of_xml) (Xml.child xml_arg0 "IpAddress") in
      let dnsName =
        (Option.map ~f:DnsNameInput.of_xml) (Xml.child xml_arg0 "DnsName") in
      let certificatePath =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "CertificatePath") in
      let computeName =
        ComputeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ComputeName") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?location ?ipAddress ?dnsName ?certificatePath ~computeName
        ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let ipAddress = field_map json__ "IpAddress" IpAddress.of_json in
      let dnsName = field_map json__ "DnsName" DnsNameInput.of_json in
      let certificatePath =
        field_map json__ "CertificatePath" NonZeroAndMaxString.of_json in
      let computeName =
        field_map_exn json__ "ComputeName" ComputeName.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?location ?ipAddress ?dnsName ?certificatePath ~computeName
        ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Anywhere, Container Registers a compute resource in an Amazon GameLift Servers Anywhere fleet. For an Anywhere fleet that's running the Amazon GameLift Servers Agent, the Agent handles all compute registry tasks for you. For an Anywhere fleet that doesn't use the Agent, call this operation to register fleet computes. To register a compute, give the compute a name (must be unique within the fleet) and specify the compute resource's DNS name or IP address. Provide a fleet ID and a fleet location to associate with the compute being registered. You can optionally include the path to a TLS certificate on the compute resource. If successful, this operation returns compute details, including an Amazon GameLift Servers SDK endpoint or Agent endpoint. Game server processes running on the compute can use this endpoint to communicate with the Amazon GameLift Servers service. Each server process includes the SDK endpoint in its call to the Amazon GameLift Servers server SDK action InitSDK(). To view compute details, call DescribeCompute with the compute name. Learn more Create an Anywhere fleet Test your integration Server SDK reference guides (for version 5.x)"]
module PutScalingPolicyOutput =
  struct
    type nonrec t =
      {
      name: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a fleet's scaling policy. Policy names do not need to be unique."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?name = fun () -> { name }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map json__ "Name" NonZeroAndMaxString.of_json in
      make ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Creates or updates a scaling policy for a fleet. Scaling policies are used to automatically scale a fleet's hosting capacity to meet player demand. An active scaling policy instructs Amazon GameLift Servers to track a fleet metric and automatically change the fleet's capacity when a certain threshold is reached. There are two types of scaling policies: target-based and rule-based. Use a target-based policy to quickly and efficiently manage fleet scaling; this option is the most commonly used. Use rule-based policies when you need to exert fine-grained control over auto-scaling. Fleets can have multiple scaling policies of each type in force at the same time; you can have one target-based policy, one or multiple rule-based scaling policies, or both. We recommend caution, however, because multiple auto-scaling policies can have unintended consequences. Learn more about how to work with auto-scaling in Set Up Fleet Automatic Scaling. Target-based policy A target-based policy tracks a single metric: PercentAvailableGameSessions. This metric tells us how much of a fleet's hosting capacity is ready to host game sessions but is not currently in use. This is the fleet's buffer; it measures the additional player demand that the fleet could handle at current capacity. With a target-based policy, you set your ideal buffer size and leave it to Amazon GameLift Servers to take whatever action is needed to maintain that target. For example, you might choose to maintain a 10% buffer for a fleet that has the capacity to host 100 simultaneous game sessions. This policy tells Amazon GameLift Servers to take action whenever the fleet's available capacity falls below or rises above 10 game sessions. Amazon GameLift Servers will start new instances or stop unused instances in order to return to the 10% buffer. To create or update a target-based policy, specify a fleet ID and name, and set the policy type to \"TargetBased\". Specify the metric to track (PercentAvailableGameSessions) and reference a TargetConfiguration object with your desired buffer value. Exclude all other parameters. On a successful request, the policy name is returned. The scaling policy is automatically in force as soon as it's successfully created. If the fleet's auto-scaling actions are temporarily suspended, the new policy will be in force once the fleet actions are restarted. Rule-based policy A rule-based policy tracks specified fleet metric, sets a threshold value, and specifies the type of action to initiate when triggered. With a rule-based policy, you can select from several available fleet metrics. Each policy specifies whether to scale up or scale down (and by how much), so you need one policy for each type of action. For example, a policy may make the following statement: \"If the percentage of idle instances is greater than 20% for more than 15 minutes, then reduce the fleet capacity by 10%.\" A policy's rule statement has the following structure: If \\[MetricName\\] is \\[ComparisonOperator\\] \\[Threshold\\] for \\[EvaluationPeriods\\] minutes, then \\[ScalingAdjustmentType\\] to/by \\[ScalingAdjustment\\]. To implement the example, the rule statement would look like this: If \\[PercentIdleInstances\\] is \\[GreaterThanThreshold\\] \\[20\\] for \\[15\\] minutes, then \\[PercentChangeInCapacity\\] to/by \\[10\\]. To create or update a scaling policy, specify a unique combination of name and fleet ID, and set the policy type to \"RuleBased\". Specify the parameter values for a policy rule statement. On a successful request, the policy name is returned. Scaling policies are automatically in force as soon as they're successfully created. If the fleet's auto-scaling actions are temporarily suspended, the new policy will be in force once the fleet actions are restarted."]
module PutScalingPolicyInput =
  struct
    type nonrec t =
      {
      name: NonZeroAndMaxString.t
        [@ocaml.doc
          "A descriptive label that is associated with a fleet's scaling policy. Policy names do not need to be unique. A fleet can have only one scaling policy with the same name."];
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to apply this policy to. You can use either the fleet ID or ARN value. The fleet cannot be in any of the following statuses: ERROR or DELETING."];
      scalingAdjustment: Integer.t option
        [@ocaml.doc
          "Amount of adjustment to make, based on the scaling adjustment type."];
      scalingAdjustmentType: ScalingAdjustmentType.t option
        [@ocaml.doc
          "The type of adjustment to make to a fleet's instance count: ChangeInCapacity -- add (or subtract) the scaling adjustment value from the current instance count. Positive values scale up while negative values scale down. ExactCapacity -- set the instance count to the scaling adjustment value. PercentChangeInCapacity -- increase or reduce the current instance count by the scaling adjustment, read as a percentage. Positive values scale up while negative values scale down; for example, a value of \"-10\" scales the fleet down by 10%."];
      threshold: Double.t option
        [@ocaml.doc "Metric value used to trigger a scaling event."];
      comparisonOperator: ComparisonOperatorType.t option
        [@ocaml.doc
          "Comparison operator to use when measuring the metric against the threshold value."];
      evaluationPeriods: PositiveInteger.t option
        [@ocaml.doc
          "Length of time (in minutes) the metric must be at or beyond the threshold before a scaling event is triggered."];
      metricName: MetricName.t
        [@ocaml.doc
          "Name of the Amazon GameLift Servers-defined metric that is used to trigger a scaling adjustment. For detailed descriptions of fleet metrics, see Monitor Amazon GameLift Servers with Amazon CloudWatch. ActivatingGameSessions -- Game sessions in the process of being created. ActiveGameSessions -- Game sessions that are currently running. ActiveInstances -- Fleet instances that are currently running at least one game session. AvailableGameSessions -- Additional game sessions that fleet could host simultaneously, given current capacity. AvailablePlayerSessions -- Empty player slots in currently active game sessions. This includes game sessions that are not currently accepting players. Reserved player slots are not included. CurrentPlayerSessions -- Player slots in active game sessions that are being used by a player or are reserved for a player. IdleInstances -- Active instances that are currently hosting zero game sessions. PercentAvailableGameSessions -- Unused percentage of the total number of game sessions that a fleet could host simultaneously, given current capacity. Use this metric for a target-based scaling policy. PercentIdleInstances -- Percentage of the total number of active instances that are hosting zero game sessions. QueueDepth -- Pending game session placement requests, in any queue, where the current fleet is the top-priority destination. WaitTime -- Current wait time for pending game session placement requests, in any queue, where the current fleet is the top-priority destination."];
      policyType: PolicyType.t option
        [@ocaml.doc
          "The type of scaling policy to create. For a target-based policy, set the parameter MetricName to 'PercentAvailableGameSessions' and specify a TargetConfiguration. For a rule-based policy set the following parameters: MetricName, ComparisonOperator, Threshold, EvaluationPeriods, ScalingAdjustmentType, and ScalingAdjustment."];
      targetConfiguration: TargetConfiguration.t option
        [@ocaml.doc
          "An object that contains settings for a target-based scaling policy."]}
    let context_ = "PutScalingPolicyInput"
    let make ?scalingAdjustment =
      fun ?scalingAdjustmentType ->
        fun ?threshold ->
          fun ?comparisonOperator ->
            fun ?evaluationPeriods ->
              fun ?policyType ->
                fun ?targetConfiguration ->
                  fun ~name ->
                    fun ~fleetId ->
                      fun ~metricName ->
                        fun () ->
                          {
                            scalingAdjustment;
                            scalingAdjustmentType;
                            threshold;
                            comparisonOperator;
                            evaluationPeriods;
                            policyType;
                            targetConfiguration;
                            name;
                            fleetId;
                            metricName
                          }
    let to_value x =
      structure_to_value
        [("Name", (Some (NonZeroAndMaxString.to_value x.name)));
        ("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("ScalingAdjustment",
          (Option.map x.scalingAdjustment ~f:Integer.to_value));
        ("ScalingAdjustmentType",
          (Option.map x.scalingAdjustmentType
             ~f:ScalingAdjustmentType.to_value));
        ("Threshold", (Option.map x.threshold ~f:Double.to_value));
        ("ComparisonOperator",
          (Option.map x.comparisonOperator ~f:ComparisonOperatorType.to_value));
        ("EvaluationPeriods",
          (Option.map x.evaluationPeriods ~f:PositiveInteger.to_value));
        ("MetricName", (Some (MetricName.to_value x.metricName)));
        ("PolicyType", (Option.map x.policyType ~f:PolicyType.to_value));
        ("TargetConfiguration",
          (Option.map x.targetConfiguration ~f:TargetConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetConfiguration =
        (Option.map ~f:TargetConfiguration.of_xml)
          (Xml.child xml_arg0 "TargetConfiguration") in
      let policyType =
        (Option.map ~f:PolicyType.of_xml) (Xml.child xml_arg0 "PolicyType") in
      let metricName =
        MetricName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MetricName") in
      let evaluationPeriods =
        (Option.map ~f:PositiveInteger.of_xml)
          (Xml.child xml_arg0 "EvaluationPeriods") in
      let comparisonOperator =
        (Option.map ~f:ComparisonOperatorType.of_xml)
          (Xml.child xml_arg0 "ComparisonOperator") in
      let threshold =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "Threshold") in
      let scalingAdjustmentType =
        (Option.map ~f:ScalingAdjustmentType.of_xml)
          (Xml.child xml_arg0 "ScalingAdjustmentType") in
      let scalingAdjustment =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ScalingAdjustment") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      let name =
        NonZeroAndMaxString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?targetConfiguration ?policyType ~metricName ?evaluationPeriods
        ?comparisonOperator ?threshold ?scalingAdjustmentType
        ?scalingAdjustment ~fleetId ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetConfiguration =
        field_map json__ "TargetConfiguration" TargetConfiguration.of_json in
      let policyType = field_map json__ "PolicyType" PolicyType.of_json in
      let metricName = field_map_exn json__ "MetricName" MetricName.of_json in
      let evaluationPeriods =
        field_map json__ "EvaluationPeriods" PositiveInteger.of_json in
      let comparisonOperator =
        field_map json__ "ComparisonOperator" ComparisonOperatorType.of_json in
      let threshold = field_map json__ "Threshold" Double.of_json in
      let scalingAdjustmentType =
        field_map json__ "ScalingAdjustmentType"
          ScalingAdjustmentType.of_json in
      let scalingAdjustment =
        field_map json__ "ScalingAdjustment" Integer.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      let name = field_map_exn json__ "Name" NonZeroAndMaxString.of_json in
      make ?targetConfiguration ?policyType ~metricName ?evaluationPeriods
        ?comparisonOperator ?threshold ?scalingAdjustmentType
        ?scalingAdjustment ~fleetId ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Creates or updates a scaling policy for a fleet. Scaling policies are used to automatically scale a fleet's hosting capacity to meet player demand. An active scaling policy instructs Amazon GameLift Servers to track a fleet metric and automatically change the fleet's capacity when a certain threshold is reached. There are two types of scaling policies: target-based and rule-based. Use a target-based policy to quickly and efficiently manage fleet scaling; this option is the most commonly used. Use rule-based policies when you need to exert fine-grained control over auto-scaling. Fleets can have multiple scaling policies of each type in force at the same time; you can have one target-based policy, one or multiple rule-based scaling policies, or both. We recommend caution, however, because multiple auto-scaling policies can have unintended consequences. Learn more about how to work with auto-scaling in Set Up Fleet Automatic Scaling. Target-based policy A target-based policy tracks a single metric: PercentAvailableGameSessions. This metric tells us how much of a fleet's hosting capacity is ready to host game sessions but is not currently in use. This is the fleet's buffer; it measures the additional player demand that the fleet could handle at current capacity. With a target-based policy, you set your ideal buffer size and leave it to Amazon GameLift Servers to take whatever action is needed to maintain that target. For example, you might choose to maintain a 10% buffer for a fleet that has the capacity to host 100 simultaneous game sessions. This policy tells Amazon GameLift Servers to take action whenever the fleet's available capacity falls below or rises above 10 game sessions. Amazon GameLift Servers will start new instances or stop unused instances in order to return to the 10% buffer. To create or update a target-based policy, specify a fleet ID and name, and set the policy type to \"TargetBased\". Specify the metric to track (PercentAvailableGameSessions) and reference a TargetConfiguration object with your desired buffer value. Exclude all other parameters. On a successful request, the policy name is returned. The scaling policy is automatically in force as soon as it's successfully created. If the fleet's auto-scaling actions are temporarily suspended, the new policy will be in force once the fleet actions are restarted. Rule-based policy A rule-based policy tracks specified fleet metric, sets a threshold value, and specifies the type of action to initiate when triggered. With a rule-based policy, you can select from several available fleet metrics. Each policy specifies whether to scale up or scale down (and by how much), so you need one policy for each type of action. For example, a policy may make the following statement: \"If the percentage of idle instances is greater than 20% for more than 15 minutes, then reduce the fleet capacity by 10%.\" A policy's rule statement has the following structure: If \\[MetricName\\] is \\[ComparisonOperator\\] \\[Threshold\\] for \\[EvaluationPeriods\\] minutes, then \\[ScalingAdjustmentType\\] to/by \\[ScalingAdjustment\\]. To implement the example, the rule statement would look like this: If \\[PercentIdleInstances\\] is \\[GreaterThanThreshold\\] \\[20\\] for \\[15\\] minutes, then \\[PercentChangeInCapacity\\] to/by \\[10\\]. To create or update a scaling policy, specify a unique combination of name and fleet ID, and set the policy type to \"RuleBased\". Specify the parameter values for a policy rule statement. On a successful request, the policy name is returned. Scaling policies are automatically in force as soon as they're successfully created. If the fleet's auto-scaling actions are temporarily suspended, the new policy will be in force once the fleet actions are restarted."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagList.t option
        [@ocaml.doc "The collection of tags assigned to the resource."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagList.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves all tags assigned to a Amazon GameLift Servers resource. Use resource tags to organize Amazon Web Services resources for a range of purposes. This operation handles the permissions necessary to manage tags for Amazon GameLift Servers resources that support tagging. To list tags for a resource, specify the unique ARN value for the resource. Learn more Tagging Amazon Web Services Resources in the Amazon Web Services General Reference Amazon Web Services Tagging Strategies Related actions All APIs by task"]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceARN: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that uniquely identifies the Amazon GameLift Servers resource that you want to retrieve tags for. Amazon GameLift Servers includes resource ARNs in the data object for the resource. You can retrieve the ARN by calling a List or Describe operation for the resource type."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceARN = fun () -> { resourceARN }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (AmazonResourceName.to_value x.resourceARN)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceARN =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~resourceARN ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceARN =
        field_map_exn json__ "ResourceARN" AmazonResourceName.of_json in
      make ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves all tags assigned to a Amazon GameLift Servers resource. Use resource tags to organize Amazon Web Services resources for a range of purposes. This operation handles the permissions necessary to manage tags for Amazon GameLift Servers resources that support tagging. To list tags for a resource, specify the unique ARN value for the resource. Learn more Tagging Amazon Web Services Resources in the Amazon Web Services General Reference Amazon Web Services Tagging Strategies Related actions All APIs by task"]
module ListScriptsOutput =
  struct
    type nonrec t =
      {
      scripts: ScriptList.t option
        [@ocaml.doc "A set of properties describing the requested script."];
      nextToken: NonEmptyString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?scripts = fun ?nextToken -> fun () -> { scripts; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Scripts", (Option.map x.scripts ~f:ScriptList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let scripts =
        (Option.map ~f:ScriptList.of_xml) (Xml.child xml_arg0 "Scripts") in
      make ?nextToken ?scripts ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NonEmptyString.of_json in
      let scripts = field_map json__ "Scripts" ScriptList.of_json in
      make ?nextToken ?scripts ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves script records for all Realtime scripts that are associated with the Amazon Web Services account in use. Learn more Amazon GameLift Servers Amazon GameLift Servers Realtime Related actions All APIs by task"]
module ListScriptsInput =
  struct
    type nonrec t =
      {
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonEmptyString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?limit = fun ?nextToken -> fun () -> { limit; nextToken }
    let to_value x =
      structure_to_value
        [("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      make ?nextToken ?limit ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NonEmptyString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      make ?nextToken ?limit ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves script records for all Realtime scripts that are associated with the Amazon Web Services account in use. Learn more Amazon GameLift Servers Amazon GameLift Servers Realtime Related actions All APIs by task"]
module ListLocationsOutput =
  struct
    type nonrec t =
      {
      locations: LocationModelList.t option
        [@ocaml.doc
          "A collection of locations, including both Amazon Web Services and custom locations. Each location includes a name and ping beacon information that can be used to measure network latency between player devices and the location."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?locations =
      fun ?nextToken -> fun () -> { locations; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Locations",
           (Option.map x.locations ~f:LocationModelList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let locations =
        (Option.map ~f:LocationModelList.of_xml)
          (Xml.child xml_arg0 "Locations") in
      make ?nextToken ?locations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let locations = field_map json__ "Locations" LocationModelList.of_json in
      make ?nextToken ?locations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Lists all custom and Amazon Web Services locations where Amazon GameLift Servers can host game servers. This operation also returns UDP ping beacon information for locations, which you can use to measure network latency between player devices and potential hosting locations. Learn more Service locations"]
module ListLocationsInput =
  struct
    type nonrec t =
      {
      filters: LocationFilterList.t option
        [@ocaml.doc
          "Filters the list for AWS or CUSTOM locations. Use this parameter to narrow down results to only Amazon Web Services-managed locations (Amazon EC2 or container) or only your custom locations (such as an Amazon GameLift Servers Anywhere fleet)."];
      limit: ListLocationsLimit.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?filters =
      fun ?limit -> fun ?nextToken -> fun () -> { filters; limit; nextToken }
    let to_value x =
      structure_to_value
        [("Filters", (Option.map x.filters ~f:LocationFilterList.to_value));
        ("Limit", (Option.map x.limit ~f:ListLocationsLimit.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:ListLocationsLimit.of_xml)
          (Xml.child xml_arg0 "Limit") in
      let filters =
        (Option.map ~f:LocationFilterList.of_xml)
          (Xml.child xml_arg0 "Filters") in
      make ?nextToken ?limit ?filters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" ListLocationsLimit.of_json in
      let filters = field_map json__ "Filters" LocationFilterList.of_json in
      make ?nextToken ?limit ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Lists all custom and Amazon Web Services locations where Amazon GameLift Servers can host game servers. This operation also returns UDP ping beacon information for locations, which you can use to measure network latency between player devices and potential hosting locations. Learn more Service locations"]
module ListGameServersOutput =
  struct
    type nonrec t =
      {
      gameServers: GameServers.t option
        [@ocaml.doc
          "A collection of game server objects that match the request."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameServers =
      fun ?nextToken -> fun () -> { gameServers; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameServers", (Option.map x.gameServers ~f:GameServers.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let gameServers =
        (Option.map ~f:GameServers.of_xml) (Xml.child xml_arg0 "GameServers") in
      make ?nextToken ?gameServers ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let gameServers = field_map json__ "GameServers" GameServers.of_json in
      make ?nextToken ?gameServers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Retrieves information on all game servers that are currently active in a specified game server group. You can opt to sort the list by game server age. Use the pagination parameters to retrieve results in a set of sequential segments. Learn more Amazon GameLift Servers FleetIQ Guide"]
module ListGameServersInput =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupNameOrArn.t
        [@ocaml.doc
          "An identifier for the game server group to retrieve a list of game servers from. Use either the name or ARN value."];
      sortOrder: SortOrder.t option
        [@ocaml.doc
          "Indicates how to sort the returned data based on game server registration timestamp. Use ASCENDING to retrieve oldest game servers first, or use DESCENDING to retrieve newest game servers first. If this parameter is left empty, game servers are returned in no particular order."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let context_ = "ListGameServersInput"
    let make ?sortOrder =
      fun ?limit ->
        fun ?nextToken ->
          fun ~gameServerGroupName ->
            fun () -> { sortOrder; limit; nextToken; gameServerGroupName }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Some (GameServerGroupNameOrArn.to_value x.gameServerGroupName)));
        ("SortOrder", (Option.map x.sortOrder ~f:SortOrder.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let sortOrder =
        (Option.map ~f:SortOrder.of_xml) (Xml.child xml_arg0 "SortOrder") in
      let gameServerGroupName =
        GameServerGroupNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerGroupName") in
      make ?nextToken ?limit ?sortOrder ~gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let sortOrder = field_map json__ "SortOrder" SortOrder.of_json in
      let gameServerGroupName =
        field_map_exn json__ "GameServerGroupName"
          GameServerGroupNameOrArn.of_json in
      make ?nextToken ?limit ?sortOrder ~gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Retrieves information on all game servers that are currently active in a specified game server group. You can opt to sort the list by game server age. Use the pagination parameters to retrieve results in a set of sequential segments. Learn more Amazon GameLift Servers FleetIQ Guide"]
module ListGameServerGroupsOutput =
  struct
    type nonrec t =
      {
      gameServerGroups: GameServerGroups.t option
        [@ocaml.doc "The game server groups' game server groups."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Specify the pagination token from a previous request to retrieve the next page of results."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameServerGroups =
      fun ?nextToken -> fun () -> { gameServerGroups; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameServerGroups",
           (Option.map x.gameServerGroups ~f:GameServerGroups.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let gameServerGroups =
        (Option.map ~f:GameServerGroups.of_xml)
          (Xml.child xml_arg0 "GameServerGroups") in
      make ?nextToken ?gameServerGroups ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let gameServerGroups =
        field_map json__ "GameServerGroups" GameServerGroups.of_json in
      make ?nextToken ?gameServerGroups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Lists a game server groups."]
module ListGameServerGroupsInput =
  struct
    type nonrec t =
      {
      limit: PositiveInteger.t option
        [@ocaml.doc "The game server groups' limit."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Specify the pagination token from a previous request to retrieve the next page of results."]}
    let make ?limit = fun ?nextToken -> fun () -> { limit; nextToken }
    let to_value x =
      structure_to_value
        [("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      make ?nextToken ?limit ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      make ?nextToken ?limit ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Lists a game server groups."]
module ListFleetsOutput =
  struct
    type nonrec t =
      {
      fleetIds: FleetIdList.t option
        [@ocaml.doc "A set of fleet IDs that match the list request."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetIds = fun ?nextToken -> fun () -> { fleetIds; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetIds", (Option.map x.fleetIds ~f:FleetIdList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let fleetIds =
        (Option.map ~f:FleetIdList.of_xml) (Xml.child xml_arg0 "FleetIds") in
      make ?nextToken ?fleetIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let fleetIds = field_map json__ "FleetIds" FleetIdList.of_json in
      make ?nextToken ?fleetIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves a collection of fleet resources in an Amazon Web Services Region. You can filter the result set to find only those fleets that are deployed with a specific build or script. For fleets that have multiple locations, this operation retrieves fleets based on their home Region only. You can use operation in the following ways: To get a list of all fleets in a Region, don't provide a build or script identifier. To get a list of all fleets where a specific game build is deployed, provide the build ID. To get a list of all Amazon GameLift Servers Realtime fleets with a specific configuration script, provide the script ID. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, this operation returns a list of fleet IDs that match the request parameters. A NextToken value is also returned if there are more result pages to retrieve. Fleet IDs are returned in no particular order."]
module ListFleetsInput =
  struct
    type nonrec t =
      {
      buildId: BuildIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the build to request fleets for. Use this parameter to return only fleets using a specified build. Use either the build ID or ARN value."];
      scriptId: ScriptIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the Realtime script to request fleets for. Use this parameter to return only fleets using a specified script. Use either the script ID or ARN value."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?buildId =
      fun ?scriptId ->
        fun ?limit ->
          fun ?nextToken -> fun () -> { buildId; scriptId; limit; nextToken }
    let to_value x =
      structure_to_value
        [("BuildId", (Option.map x.buildId ~f:BuildIdOrArn.to_value));
        ("ScriptId", (Option.map x.scriptId ~f:ScriptIdOrArn.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let scriptId =
        (Option.map ~f:ScriptIdOrArn.of_xml) (Xml.child xml_arg0 "ScriptId") in
      let buildId =
        (Option.map ~f:BuildIdOrArn.of_xml) (Xml.child xml_arg0 "BuildId") in
      make ?nextToken ?limit ?scriptId ?buildId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let scriptId = field_map json__ "ScriptId" ScriptIdOrArn.of_json in
      let buildId = field_map json__ "BuildId" BuildIdOrArn.of_json in
      make ?nextToken ?limit ?scriptId ?buildId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves a collection of fleet resources in an Amazon Web Services Region. You can filter the result set to find only those fleets that are deployed with a specific build or script. For fleets that have multiple locations, this operation retrieves fleets based on their home Region only. You can use operation in the following ways: To get a list of all fleets in a Region, don't provide a build or script identifier. To get a list of all fleets where a specific game build is deployed, provide the build ID. To get a list of all Amazon GameLift Servers Realtime fleets with a specific configuration script, provide the script ID. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, this operation returns a list of fleet IDs that match the request parameters. A NextToken value is also returned if there are more result pages to retrieve. Fleet IDs are returned in no particular order."]
module ListFleetDeploymentsOutput =
  struct
    type nonrec t =
      {
      fleetDeployments: FleetDeployments.t option
        [@ocaml.doc "The requested deployment information."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetDeployments =
      fun ?nextToken -> fun () -> { fleetDeployments; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetDeployments",
           (Option.map x.fleetDeployments ~f:FleetDeployments.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let fleetDeployments =
        (Option.map ~f:FleetDeployments.of_xml)
          (Xml.child xml_arg0 "FleetDeployments") in
      make ?nextToken ?fleetDeployments ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let fleetDeployments =
        field_map json__ "FleetDeployments" FleetDeployments.of_json in
      make ?nextToken ?fleetDeployments ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves a collection of container fleet deployments in an Amazon Web Services Region. Use the pagination parameters to retrieve results as a set of sequential pages. Request options Get a list of all deployments. Call this operation without specifying a fleet ID. Get a list of all deployments for a fleet. Specify the container fleet ID or ARN value. Results If successful, this operation returns a list of deployments that match the request parameters. A NextToken value is also returned if there are more result pages to retrieve. Deployments are returned starting with the latest."]
module ListFleetDeploymentsInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the container fleet. You can use either the fleet ID or ARN value."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?fleetId =
      fun ?limit -> fun ?nextToken -> fun () -> { fleetId; limit; nextToken }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetIdOrArn.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let fleetId =
        (Option.map ~f:FleetIdOrArn.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?nextToken ?limit ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let fleetId = field_map json__ "FleetId" FleetIdOrArn.of_json in
      make ?nextToken ?limit ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves a collection of container fleet deployments in an Amazon Web Services Region. Use the pagination parameters to retrieve results as a set of sequential pages. Request options Get a list of all deployments. Call this operation without specifying a fleet ID. Get a list of all deployments for a fleet. Specify the container fleet ID or ARN value. Results If successful, this operation returns a list of deployments that match the request parameters. A NextToken value is also returned if there are more result pages to retrieve. Deployments are returned starting with the latest."]
module ListContainerGroupDefinitionsOutput =
  struct
    type nonrec t =
      {
      containerGroupDefinitions: ContainerGroupDefinitionList.t option
        [@ocaml.doc
          "A result set of container group definitions that match the request."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerGroupDefinitions =
      fun ?nextToken -> fun () -> { containerGroupDefinitions; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ContainerGroupDefinitions",
           (Option.map x.containerGroupDefinitions
              ~f:ContainerGroupDefinitionList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let containerGroupDefinitions =
        (Option.map ~f:ContainerGroupDefinitionList.of_xml)
          (Xml.child xml_arg0 "ContainerGroupDefinitions") in
      make ?nextToken ?containerGroupDefinitions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let containerGroupDefinitions =
        field_map json__ "ContainerGroupDefinitions"
          ContainerGroupDefinitionList.of_json in
      make ?nextToken ?containerGroupDefinitions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves container group definitions for the Amazon Web Services account and Amazon Web Services Region. Use the pagination parameters to retrieve results in a set of sequential pages. This operation returns only the latest version of each definition. To retrieve all versions of a container group definition, use ListContainerGroupDefinitionVersions. Request options: Retrieve the most recent versions of all container group definitions. Retrieve the most recent versions of all container group definitions, filtered by type. Specify the container group type to filter on. Results: If successful, this operation returns the complete properties of a set of container group definition versions that match the request. This operation returns the list of container group definitions in no particular order."]
module ListContainerGroupDefinitionsInput =
  struct
    type nonrec t =
      {
      containerGroupType: ContainerGroupType.t option
        [@ocaml.doc
          "The type of container group to retrieve. Container group type determines how Amazon GameLift Servers deploys the container group on each fleet instance."];
      limit: ListContainerGroupDefinitionsLimit.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?containerGroupType =
      fun ?limit ->
        fun ?nextToken -> fun () -> { containerGroupType; limit; nextToken }
    let to_value x =
      structure_to_value
        [("ContainerGroupType",
           (Option.map x.containerGroupType ~f:ContainerGroupType.to_value));
        ("Limit",
          (Option.map x.limit ~f:ListContainerGroupDefinitionsLimit.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:ListContainerGroupDefinitionsLimit.of_xml)
          (Xml.child xml_arg0 "Limit") in
      let containerGroupType =
        (Option.map ~f:ContainerGroupType.of_xml)
          (Xml.child xml_arg0 "ContainerGroupType") in
      make ?nextToken ?limit ?containerGroupType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit =
        field_map json__ "Limit" ListContainerGroupDefinitionsLimit.of_json in
      let containerGroupType =
        field_map json__ "ContainerGroupType" ContainerGroupType.of_json in
      make ?nextToken ?limit ?containerGroupType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves container group definitions for the Amazon Web Services account and Amazon Web Services Region. Use the pagination parameters to retrieve results in a set of sequential pages. This operation returns only the latest version of each definition. To retrieve all versions of a container group definition, use ListContainerGroupDefinitionVersions. Request options: Retrieve the most recent versions of all container group definitions. Retrieve the most recent versions of all container group definitions, filtered by type. Specify the container group type to filter on. Results: If successful, this operation returns the complete properties of a set of container group definition versions that match the request. This operation returns the list of container group definitions in no particular order."]
module ListContainerGroupDefinitionVersionsOutput =
  struct
    type nonrec t =
      {
      containerGroupDefinitions: ContainerGroupDefinitionList.t option
        [@ocaml.doc
          "A result set of container group definitions that match the request."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerGroupDefinitions =
      fun ?nextToken -> fun () -> { containerGroupDefinitions; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ContainerGroupDefinitions",
           (Option.map x.containerGroupDefinitions
              ~f:ContainerGroupDefinitionList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let containerGroupDefinitions =
        (Option.map ~f:ContainerGroupDefinitionList.of_xml)
          (Xml.child xml_arg0 "ContainerGroupDefinitions") in
      make ?nextToken ?containerGroupDefinitions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let containerGroupDefinitions =
        field_map json__ "ContainerGroupDefinitions"
          ContainerGroupDefinitionList.of_json in
      make ?nextToken ?containerGroupDefinitions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves all versions of a container group definition. Use the pagination parameters to retrieve results in a set of sequential pages. Request options: Get all versions of a specified container group definition. Specify the container group definition name or ARN value. (If the ARN value has a version number, it's ignored.) Results: If successful, this operation returns the complete properties of a set of container group definition versions that match the request. This operation returns the list of container group definitions in descending version order (latest first). Learn more Manage a container group definition"]
module ListContainerGroupDefinitionVersionsInput =
  struct
    type nonrec t =
      {
      name: ContainerGroupDefinitionNameOrArn.t
        [@ocaml.doc
          "The unique identifier for the container group definition to retrieve properties for. You can use either the Name or ARN value."];
      limit: ListContainerGroupDefinitionVersionsLimit.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let context_ = "ListContainerGroupDefinitionVersionsInput"
    let make ?limit =
      fun ?nextToken -> fun ~name -> fun () -> { limit; nextToken; name }
    let to_value x =
      structure_to_value
        [("Name", (Some (ContainerGroupDefinitionNameOrArn.to_value x.name)));
        ("Limit",
          (Option.map x.limit
             ~f:ListContainerGroupDefinitionVersionsLimit.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:ListContainerGroupDefinitionVersionsLimit.of_xml)
          (Xml.child xml_arg0 "Limit") in
      let name =
        ContainerGroupDefinitionNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?nextToken ?limit ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit =
        field_map json__ "Limit"
          ListContainerGroupDefinitionVersionsLimit.of_json in
      let name =
        field_map_exn json__ "Name" ContainerGroupDefinitionNameOrArn.of_json in
      make ?nextToken ?limit ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves all versions of a container group definition. Use the pagination parameters to retrieve results in a set of sequential pages. Request options: Get all versions of a specified container group definition. Specify the container group definition name or ARN value. (If the ARN value has a version number, it's ignored.) Results: If successful, this operation returns the complete properties of a set of container group definition versions that match the request. This operation returns the list of container group definitions in descending version order (latest first). Learn more Manage a container group definition"]
module ListContainerFleetsOutput =
  struct
    type nonrec t =
      {
      containerFleets: ContainerFleetList.t option
        [@ocaml.doc
          "A collection of container fleet objects for all fleets that match the request criteria."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerFleets =
      fun ?nextToken -> fun () -> { containerFleets; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ContainerFleets",
           (Option.map x.containerFleets ~f:ContainerFleetList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let containerFleets =
        (Option.map ~f:ContainerFleetList.of_xml)
          (Xml.child xml_arg0 "ContainerFleets") in
      make ?nextToken ?containerFleets ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let containerFleets =
        field_map json__ "ContainerFleets" ContainerFleetList.of_json in
      make ?nextToken ?containerFleets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves a collection of container fleet resources in an Amazon Web Services Region. For fleets that have multiple locations, this operation retrieves fleets based on their home Region only. Request options Get a list of all fleets. Call this operation without specifying a container group definition. Get a list of fleets filtered by container group definition. Provide the container group definition name or ARN value. To get a list of all Amazon GameLift Servers Realtime fleets with a specific configuration script, provide the script ID. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, this operation returns a collection of container fleets that match the request parameters. A NextToken value is also returned if there are more result pages to retrieve. Fleet IDs are returned in no particular order."]
module ListContainerFleetsInput =
  struct
    type nonrec t =
      {
      containerGroupDefinitionName:
        ContainerGroupDefinitionNameOrArn.t option
        [@ocaml.doc
          "The container group definition to filter the list on. Use this parameter to retrieve only those fleets that use the specified container group definition. You can specify the container group definition's name to get fleets with the latest versions. Alternatively, provide an ARN value to get fleets with a specific version number."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?containerGroupDefinitionName =
      fun ?limit ->
        fun ?nextToken ->
          fun () -> { containerGroupDefinitionName; limit; nextToken }
    let to_value x =
      structure_to_value
        [("ContainerGroupDefinitionName",
           (Option.map x.containerGroupDefinitionName
              ~f:ContainerGroupDefinitionNameOrArn.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let containerGroupDefinitionName =
        (Option.map ~f:ContainerGroupDefinitionNameOrArn.of_xml)
          (Xml.child xml_arg0 "ContainerGroupDefinitionName") in
      make ?nextToken ?limit ?containerGroupDefinitionName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let containerGroupDefinitionName =
        field_map json__ "ContainerGroupDefinitionName"
          ContainerGroupDefinitionNameOrArn.of_json in
      make ?nextToken ?limit ?containerGroupDefinitionName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves a collection of container fleet resources in an Amazon Web Services Region. For fleets that have multiple locations, this operation retrieves fleets based on their home Region only. Request options Get a list of all fleets. Call this operation without specifying a container group definition. Get a list of fleets filtered by container group definition. Provide the container group definition name or ARN value. To get a list of all Amazon GameLift Servers Realtime fleets with a specific configuration script, provide the script ID. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, this operation returns a collection of container fleets that match the request parameters. A NextToken value is also returned if there are more result pages to retrieve. Fleet IDs are returned in no particular order."]
module ListComputeOutput =
  struct
    type nonrec t =
      {
      computeList: ComputeList.t option
        [@ocaml.doc "A list of compute resources in the specified fleet."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?computeList =
      fun ?nextToken -> fun () -> { computeList; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ComputeList", (Option.map x.computeList ~f:ComputeList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let computeList =
        (Option.map ~f:ComputeList.of_xml) (Xml.child xml_arg0 "ComputeList") in
      make ?nextToken ?computeList ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let computeList = field_map json__ "ComputeList" ComputeList.of_json in
      make ?nextToken ?computeList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves information on the compute resources in an Amazon GameLift Servers fleet. Use the pagination parameters to retrieve results in a set of sequential pages. Request options Retrieve a list of all computes in a fleet. Specify a fleet ID. Retrieve a list of all computes in a specific fleet location. Specify a fleet ID and location. Results If successful, this operation returns information on a set of computes. Depending on the type of fleet, the result includes the following information: For a managed EC2 fleet (compute type EC2), this operation returns information about the EC2 instance. Compute names are EC2 instance IDs. For an Anywhere fleet (compute type ANYWHERE), this operation returns compute names and details from when the compute was registered with RegisterCompute. This includes GameLiftServiceSdkEndpoint or GameLiftAgentEndpoint."]
module ListComputeInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to retrieve compute resources for."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The name of a location to retrieve compute resources for. For an Amazon GameLift Servers Anywhere fleet, use a custom location. For a managed fleet, provide a Amazon Web Services Region or Local Zone code (for example: us-west-2 or us-west-2-lax-1)."];
      containerGroupDefinitionName:
        ContainerGroupDefinitionNameOrArn.t option
        [@ocaml.doc
          "For computes in a managed container fleet, the name of the deployed container group definition."];
      computeStatus: ListComputeInputStatus.t option
        [@ocaml.doc
          "The status of computes in a managed container fleet, based on the success of the latest update deployment. ACTIVE -- The compute is deployed with the correct container definitions. It is ready to process game servers and host game sessions. IMPAIRED -- An update deployment to the compute failed, and the compute is deployed with incorrect container definitions."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let context_ = "ListComputeInput"
    let make ?location =
      fun ?containerGroupDefinitionName ->
        fun ?computeStatus ->
          fun ?limit ->
            fun ?nextToken ->
              fun ~fleetId ->
                fun () ->
                  {
                    location;
                    containerGroupDefinitionName;
                    computeStatus;
                    limit;
                    nextToken;
                    fleetId
                  }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value));
        ("ContainerGroupDefinitionName",
          (Option.map x.containerGroupDefinitionName
             ~f:ContainerGroupDefinitionNameOrArn.to_value));
        ("ComputeStatus",
          (Option.map x.computeStatus ~f:ListComputeInputStatus.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let computeStatus =
        (Option.map ~f:ListComputeInputStatus.of_xml)
          (Xml.child xml_arg0 "ComputeStatus") in
      let containerGroupDefinitionName =
        (Option.map ~f:ContainerGroupDefinitionNameOrArn.of_xml)
          (Xml.child xml_arg0 "ContainerGroupDefinitionName") in
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?nextToken ?limit ?computeStatus ?containerGroupDefinitionName
        ?location ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let computeStatus =
        field_map json__ "ComputeStatus" ListComputeInputStatus.of_json in
      let containerGroupDefinitionName =
        field_map json__ "ContainerGroupDefinitionName"
          ContainerGroupDefinitionNameOrArn.of_json in
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?nextToken ?limit ?computeStatus ?containerGroupDefinitionName
        ?location ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves information on the compute resources in an Amazon GameLift Servers fleet. Use the pagination parameters to retrieve results in a set of sequential pages. Request options Retrieve a list of all computes in a fleet. Specify a fleet ID. Retrieve a list of all computes in a specific fleet location. Specify a fleet ID and location. Results If successful, this operation returns information on a set of computes. Depending on the type of fleet, the result includes the following information: For a managed EC2 fleet (compute type EC2), this operation returns information about the EC2 instance. Compute names are EC2 instance IDs. For an Anywhere fleet (compute type ANYWHERE), this operation returns compute names and details from when the compute was registered with RegisterCompute. This includes GameLiftServiceSdkEndpoint or GameLiftAgentEndpoint."]
module ListBuildsOutput =
  struct
    type nonrec t =
      {
      builds: BuildList.t option
        [@ocaml.doc
          "A collection of build resources that match the request."];
      nextToken: NonEmptyString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?builds = fun ?nextToken -> fun () -> { builds; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Builds", (Option.map x.builds ~f:BuildList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let builds =
        (Option.map ~f:BuildList.of_xml) (Xml.child xml_arg0 "Builds") in
      make ?nextToken ?builds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NonEmptyString.of_json in
      let builds = field_map json__ "Builds" BuildList.of_json in
      make ?nextToken ?builds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves build resources for all builds associated with the Amazon Web Services account in use. You can limit results to builds that are in a specific status by using the Status parameter. Use the pagination parameters to retrieve results in Build resources are not listed in any particular order. Learn more Upload a Custom Server Build All APIs by task"]
module ListBuildsInput =
  struct
    type nonrec t =
      {
      status: BuildStatus.t option
        [@ocaml.doc
          "Build status to filter results by. To retrieve all builds, leave this parameter empty. Possible build statuses include the following: INITIALIZED -- A new build has been defined, but no files have been uploaded. You cannot create fleets for builds that are in this status. When a build is successfully created, the build status is set to this value. READY -- The game build has been successfully uploaded. You can now create new fleets for this build. FAILED -- The game build upload failed. You cannot create new fleets for this build."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonEmptyString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?status =
      fun ?limit -> fun ?nextToken -> fun () -> { status; limit; nextToken }
    let to_value x =
      structure_to_value
        [("Status", (Option.map x.status ~f:BuildStatus.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let status =
        (Option.map ~f:BuildStatus.of_xml) (Xml.child xml_arg0 "Status") in
      make ?nextToken ?limit ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NonEmptyString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let status = field_map json__ "Status" BuildStatus.of_json in
      make ?nextToken ?limit ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves build resources for all builds associated with the Amazon Web Services account in use. You can limit results to builds that are in a specific status by using the Status parameter. Use the pagination parameters to retrieve results in Build resources are not listed in any particular order. Learn more Upload a Custom Server Build All APIs by task"]
module ListAliasesOutput =
  struct
    type nonrec t =
      {
      aliases: AliasList.t option
        [@ocaml.doc
          "A collection of alias resources that match the request parameters."];
      nextToken: NonEmptyString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?aliases = fun ?nextToken -> fun () -> { aliases; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Aliases", (Option.map x.aliases ~f:AliasList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let aliases =
        (Option.map ~f:AliasList.of_xml) (Xml.child xml_arg0 "Aliases") in
      make ?nextToken ?aliases ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NonEmptyString.of_json in
      let aliases = field_map json__ "Aliases" AliasList.of_json in
      make ?nextToken ?aliases ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves all aliases for this Amazon Web Services account. You can filter the result set by alias name and/or routing strategy type. Use the pagination parameters to retrieve results in sequential pages. Returned aliases are not listed in any particular order. Related actions All APIs by task"]
module ListAliasesInput =
  struct
    type nonrec t =
      {
      routingStrategyType: RoutingStrategyType.t option
        [@ocaml.doc
          "The routing type to filter results on. Use this parameter to retrieve only aliases with a certain routing type. To retrieve all aliases, leave this parameter empty. Possible routing types include the following: SIMPLE -- The alias resolves to one specific fleet. Use this type when routing to active fleets. TERMINAL -- The alias does not resolve to a fleet but instead can be used to display a message to the user. A terminal alias throws a TerminalRoutingStrategyException with the RoutingStrategy message embedded."];
      name: NonEmptyString.t option
        [@ocaml.doc
          "A descriptive label that is associated with an alias. Alias names do not need to be unique."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonEmptyString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?routingStrategyType =
      fun ?name ->
        fun ?limit ->
          fun ?nextToken ->
            fun () -> { routingStrategyType; name; limit; nextToken }
    let to_value x =
      structure_to_value
        [("RoutingStrategyType",
           (Option.map x.routingStrategyType ~f:RoutingStrategyType.to_value));
        ("Name", (Option.map x.name ~f:NonEmptyString.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let name =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "Name") in
      let routingStrategyType =
        (Option.map ~f:RoutingStrategyType.of_xml)
          (Xml.child xml_arg0 "RoutingStrategyType") in
      make ?nextToken ?limit ?name ?routingStrategyType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NonEmptyString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let name = field_map json__ "Name" NonEmptyString.of_json in
      let routingStrategyType =
        field_map json__ "RoutingStrategyType" RoutingStrategyType.of_json in
      make ?nextToken ?limit ?name ?routingStrategyType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves all aliases for this Amazon Web Services account. You can filter the result set by alias name and/or routing strategy type. Use the pagination parameters to retrieve results in sequential pages. Returned aliases are not listed in any particular order. Related actions All APIs by task"]
module GetPlayerConnectionDetailsOutput =
  struct
    type nonrec t =
      {
      gameSessionId: ArnStringModel.t option
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions for which the player connection details were retrieved. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>."];
      playerConnectionDetails: PlayerConnectionDetailList.t option
        [@ocaml.doc
          "A collection of player connection detail objects, one for each requested player."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidGameSessionStatusException of
          InvalidGameSessionStatusException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameSessionId =
      fun ?playerConnectionDetails ->
        fun () -> { gameSessionId; playerConnectionDetails }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidGameSessionStatusException" ->
          `InvalidGameSessionStatusException
            (InvalidGameSessionStatusException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidGameSessionStatusException" ->
          `InvalidGameSessionStatusException
            (InvalidGameSessionStatusException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidGameSessionStatusException e ->
          `Assoc
            [("error", (`String "InvalidGameSessionStatusException"));
            ("details", (InvalidGameSessionStatusException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameSessionId",
           (Option.map x.gameSessionId ~f:ArnStringModel.to_value));
        ("PlayerConnectionDetails",
          (Option.map x.playerConnectionDetails
             ~f:PlayerConnectionDetailList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerConnectionDetails =
        (Option.map ~f:PlayerConnectionDetailList.of_xml)
          (Xml.child xml_arg0 "PlayerConnectionDetails") in
      let gameSessionId =
        (Option.map ~f:ArnStringModel.of_xml)
          (Xml.child xml_arg0 "GameSessionId") in
      make ?playerConnectionDetails ?gameSessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerConnectionDetails =
        field_map json__ "PlayerConnectionDetails"
          PlayerConnectionDetailList.of_json in
      let gameSessionId =
        field_map json__ "GameSessionId" ArnStringModel.of_json in
      make ?playerConnectionDetails ?gameSessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (server SDK 5.x or later), Container Retrieves connection details for game clients to connect to game sessions. Player gateway benefits: DDoS protection with negligible impact to latency. To enable player gateway on your fleet, set PlayerGatewayMode to ENABLED or REQUIRED when calling CreateFleet or CreateContainerFleet. How to use: After creating a game session and adding players, call this operation with the game session ID and player IDs. When player gateway is enabled, the response includes connection endpoints and player gateway tokens that your game clients can use to connect to the game session through player gateway. To learn more about player gateway integration, see DDoS protection with Amazon GameLift Servers player gateway. When player gateway is disabled or in locations where player gateway is not supported, this operation returns game server connection information without player gateway tokens, so that your game clients directly connect to the game server endpoint."]
module GetPlayerConnectionDetailsInput =
  struct
    type nonrec t =
      {
      gameSessionId: ArnStringModel.t
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions for which to retrieve player connection details. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>."];
      playerIds: PlayerIdList.t
        [@ocaml.doc
          "List of unique identifiers for players. Connection details are returned for each player in this list."]}
    let context_ = "GetPlayerConnectionDetailsInput"
    let make ~gameSessionId =
      fun ~playerIds -> fun () -> { gameSessionId; playerIds }
    let to_value x =
      structure_to_value
        [("GameSessionId", (Some (ArnStringModel.to_value x.gameSessionId)));
        ("PlayerIds", (Some (PlayerIdList.to_value x.playerIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerIds =
        PlayerIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PlayerIds") in
      let gameSessionId =
        ArnStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameSessionId") in
      make ~playerIds ~gameSessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerIds = field_map_exn json__ "PlayerIds" PlayerIdList.of_json in
      let gameSessionId =
        field_map_exn json__ "GameSessionId" ArnStringModel.of_json in
      make ~playerIds ~gameSessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (server SDK 5.x or later), Container Retrieves connection details for game clients to connect to game sessions. Player gateway benefits: DDoS protection with negligible impact to latency. To enable player gateway on your fleet, set PlayerGatewayMode to ENABLED or REQUIRED when calling CreateFleet or CreateContainerFleet. How to use: After creating a game session and adding players, call this operation with the game session ID and player IDs. When player gateway is enabled, the response includes connection endpoints and player gateway tokens that your game clients can use to connect to the game session through player gateway. To learn more about player gateway integration, see DDoS protection with Amazon GameLift Servers player gateway. When player gateway is disabled or in locations where player gateway is not supported, this operation returns game server connection information without player gateway tokens, so that your game clients directly connect to the game server endpoint."]
module GetInstanceAccessOutput =
  struct
    type nonrec t =
      {
      instanceAccess: InstanceAccess.t option
        [@ocaml.doc
          "The connection information for a fleet instance, including IP address and access credentials."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?instanceAccess = fun () -> { instanceAccess }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("InstanceAccess",
           (Option.map x.instanceAccess ~f:InstanceAccess.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceAccess =
        (Option.map ~f:InstanceAccess.of_xml)
          (Xml.child xml_arg0 "InstanceAccess") in
      make ?instanceAccess ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceAccess =
        field_map json__ "InstanceAccess" InstanceAccess.of_json in
      make ?instanceAccess ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Requests authorization to remotely connect to an instance in an Amazon GameLift Servers managed fleet. Use this operation to connect to instances with game servers that use Amazon GameLift Servers server SDK 4.x or earlier. To connect to instances with game servers that use server SDK 5.x or later, call https://docs.aws.amazon.com/gamelift/latest/apireference/API_GetComputeAccess. To request access to an instance, specify IDs for the instance and the fleet it belongs to. You can retrieve instance IDs for a fleet by calling DescribeInstances with the fleet ID. If successful, this operation returns an IP address and credentials. The returned credentials match the operating system of the instance, as follows: For a Windows instance: returns a user name and secret (password) for use with a Windows Remote Desktop client. For a Linux instance: returns a user name and secret (RSA private key) for use with an SSH client. You must save the secret to a .pem file. If you're using the CLI, see the example Get credentials for a Linux instance for tips on automatically saving the secret to a .pem file. Learn more Remotely connect to fleet instances Debug fleet issues Related actions All APIs by task"]
module GetInstanceAccessInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet that contains the instance you want to access. You can request access to instances in EC2 fleets with the following statuses: ACTIVATING, ACTIVE, or ERROR. Use either a fleet ID or an ARN value. You can access fleets in ERROR status for a short period of time before Amazon GameLift Servers deletes them."];
      instanceId: InstanceId.t
        [@ocaml.doc
          "A unique identifier for the instance you want to access. You can access an instance in any status."]}
    let context_ = "GetInstanceAccessInput"
    let make ~fleetId = fun ~instanceId -> fun () -> { fleetId; instanceId }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("InstanceId", (Some (InstanceId.to_value x.instanceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceId =
        InstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceId") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~instanceId ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceId = field_map_exn json__ "InstanceId" InstanceId.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~instanceId ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Requests authorization to remotely connect to an instance in an Amazon GameLift Servers managed fleet. Use this operation to connect to instances with game servers that use Amazon GameLift Servers server SDK 4.x or earlier. To connect to instances with game servers that use server SDK 5.x or later, call https://docs.aws.amazon.com/gamelift/latest/apireference/API_GetComputeAccess. To request access to an instance, specify IDs for the instance and the fleet it belongs to. You can retrieve instance IDs for a fleet by calling DescribeInstances with the fleet ID. If successful, this operation returns an IP address and credentials. The returned credentials match the operating system of the instance, as follows: For a Windows instance: returns a user name and secret (password) for use with a Windows Remote Desktop client. For a Linux instance: returns a user name and secret (RSA private key) for use with an SSH client. You must save the secret to a .pem file. If you're using the CLI, see the example Get credentials for a Linux instance for tips on automatically saving the secret to a .pem file. Learn more Remotely connect to fleet instances Debug fleet issues Related actions All APIs by task"]
module GetGameSessionLogUrlOutput =
  struct
    type nonrec t =
      {
      preSignedUrl: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Location of the requested game session logs, available for download. This URL is valid for 15 minutes, after which S3 will reject any download request using this URL. You can request a new URL any time within the 14-day period that the logs are retained."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?preSignedUrl = fun () -> { preSignedUrl }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PreSignedUrl",
           (Option.map x.preSignedUrl ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let preSignedUrl =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "PreSignedUrl") in
      make ?preSignedUrl ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let preSignedUrl =
        field_map json__ "PreSignedUrl" NonZeroAndMaxString.of_json in
      make ?preSignedUrl ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves the location of stored game session logs for a specified game session on Amazon GameLift Servers managed fleets. When a game session is terminated, Amazon GameLift Servers automatically stores the logs in Amazon S3 and retains them for 14 days. Use this URL to download the logs. See the Amazon Web Services Service Limits page for maximum log file sizes. Log files that exceed this limit are not saved. All APIs by task"]
module GetGameSessionLogUrlInput =
  struct
    type nonrec t =
      {
      gameSessionId: ArnStringModel.t
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions to get logs for. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>."]}
    let context_ = "GetGameSessionLogUrlInput"
    let make ~gameSessionId = fun () -> { gameSessionId }
    let to_value x =
      structure_to_value
        [("GameSessionId", (Some (ArnStringModel.to_value x.gameSessionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameSessionId =
        ArnStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameSessionId") in
      make ~gameSessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameSessionId =
        field_map_exn json__ "GameSessionId" ArnStringModel.of_json in
      make ~gameSessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves the location of stored game session logs for a specified game session on Amazon GameLift Servers managed fleets. When a game session is terminated, Amazon GameLift Servers automatically stores the logs in Amazon S3 and retains them for 14 days. Use this URL to download the logs. See the Amazon Web Services Service Limits page for maximum log file sizes. Log files that exceed this limit are not saved. All APIs by task"]
module GetComputeAuthTokenOutput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the fleet that the compute is registered to."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      computeName: ComputeNameOrArn.t option
        [@ocaml.doc
          "The name of the compute resource that the authentication token is issued to."];
      computeArn: ComputeArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to an Amazon GameLift Servers compute resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::compute/compute-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      authToken: ComputeAuthToken.t option
        [@ocaml.doc "A valid temporary authentication token."];
      expirationTimestamp: Timestamp.t option
        [@ocaml.doc
          "The amount of time until the authentication token is no longer valid."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?computeName ->
          fun ?computeArn ->
            fun ?authToken ->
              fun ?expirationTimestamp ->
                fun () ->
                  {
                    fleetId;
                    fleetArn;
                    computeName;
                    computeArn;
                    authToken;
                    expirationTimestamp
                  }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetIdOrArn.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("ComputeName",
          (Option.map x.computeName ~f:ComputeNameOrArn.to_value));
        ("ComputeArn", (Option.map x.computeArn ~f:ComputeArn.to_value));
        ("AuthToken", (Option.map x.authToken ~f:ComputeAuthToken.to_value));
        ("ExpirationTimestamp",
          (Option.map x.expirationTimestamp ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expirationTimestamp =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "ExpirationTimestamp") in
      let authToken =
        (Option.map ~f:ComputeAuthToken.of_xml)
          (Xml.child xml_arg0 "AuthToken") in
      let computeArn =
        (Option.map ~f:ComputeArn.of_xml) (Xml.child xml_arg0 "ComputeArn") in
      let computeName =
        (Option.map ~f:ComputeNameOrArn.of_xml)
          (Xml.child xml_arg0 "ComputeName") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetIdOrArn.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?expirationTimestamp ?authToken ?computeArn ?computeName ?fleetArn
        ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let expirationTimestamp =
        field_map json__ "ExpirationTimestamp" Timestamp.of_json in
      let authToken = field_map json__ "AuthToken" ComputeAuthToken.of_json in
      let computeArn = field_map json__ "ComputeArn" ComputeArn.of_json in
      let computeName =
        field_map json__ "ComputeName" ComputeNameOrArn.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetIdOrArn.of_json in
      make ?expirationTimestamp ?authToken ?computeArn ?computeName ?fleetArn
        ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Requests an authentication token from Amazon GameLift Servers for a compute resource in an Amazon GameLift Servers fleet. Game servers that are running on the compute use this token to communicate with the Amazon GameLift Servers service, such as when calling the Amazon GameLift Servers server SDK action InitSDK(). Authentication tokens are valid for a limited time span, so you need to request a fresh token before the current token expires. Request options For managed EC2 fleets (compute type EC2), auth token retrieval and refresh is handled automatically. All game servers that are running on all fleet instances have access to a valid auth token. For Anywhere fleets (compute type ANYWHERE), if you're using the Amazon GameLift Servers Agent, auth token retrieval and refresh is handled automatically for any compute where the Agent is running. If you're not using the Agent, create a mechanism to retrieve and refresh auth tokens for computes that are running game server processes. Learn more Create an Anywhere fleet Test your integration Server SDK reference guides (for version 5.x)"]
module GetComputeAuthTokenInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet that the compute is registered to."];
      computeName: ComputeNameOrArn.t
        [@ocaml.doc
          "The name of the compute resource you are requesting the authentication token for. For an Anywhere fleet compute, use the registered compute name. For an EC2 fleet instance, use the instance ID."]}
    let context_ = "GetComputeAuthTokenInput"
    let make ~fleetId =
      fun ~computeName -> fun () -> { fleetId; computeName }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("ComputeName", (Some (ComputeNameOrArn.to_value x.computeName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let computeName =
        ComputeNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ComputeName") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~computeName ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let computeName =
        field_map_exn json__ "ComputeName" ComputeNameOrArn.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~computeName ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Requests an authentication token from Amazon GameLift Servers for a compute resource in an Amazon GameLift Servers fleet. Game servers that are running on the compute use this token to communicate with the Amazon GameLift Servers service, such as when calling the Amazon GameLift Servers server SDK action InitSDK(). Authentication tokens are valid for a limited time span, so you need to request a fresh token before the current token expires. Request options For managed EC2 fleets (compute type EC2), auth token retrieval and refresh is handled automatically. All game servers that are running on all fleet instances have access to a valid auth token. For Anywhere fleets (compute type ANYWHERE), if you're using the Amazon GameLift Servers Agent, auth token retrieval and refresh is handled automatically for any compute where the Agent is running. If you're not using the Agent, create a mechanism to retrieve and refresh auth tokens for computes that are running game server processes. Learn more Create an Anywhere fleet Test your integration Server SDK reference guides (for version 5.x)"]
module GetComputeAccessOutput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t option
        [@ocaml.doc
          "The ID of the fleet that holds the compute resource to be accessed."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      computeName: ComputeNameOrArn.t option
        [@ocaml.doc
          "The identifier of the compute resource to be accessed. This value might be either a compute name or an instance ID."];
      computeArn: ComputeArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to an Amazon GameLift Servers compute resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::compute/compute-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      credentials: AwsCredentials.t option
        [@ocaml.doc
          "A set of temporary Amazon Web Services credentials for use when connecting to the compute resource with Amazon EC2 Systems Manager (SSM)."];
      target: SessionTarget.t option
        [@ocaml.doc "The instance ID where the compute resource is running."];
      containerIdentifiers: ContainerIdentifierList.t option
        [@ocaml.doc
          "For a managed container fleet, a list of containers on the compute. Use the container runtime ID with Docker commands to connect to a specific container."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?computeName ->
          fun ?computeArn ->
            fun ?credentials ->
              fun ?target ->
                fun ?containerIdentifiers ->
                  fun () ->
                    {
                      fleetId;
                      fleetArn;
                      computeName;
                      computeArn;
                      credentials;
                      target;
                      containerIdentifiers
                    }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetIdOrArn.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("ComputeName",
          (Option.map x.computeName ~f:ComputeNameOrArn.to_value));
        ("ComputeArn", (Option.map x.computeArn ~f:ComputeArn.to_value));
        ("Credentials",
          (Option.map x.credentials ~f:AwsCredentials.to_value));
        ("Target", (Option.map x.target ~f:SessionTarget.to_value));
        ("ContainerIdentifiers",
          (Option.map x.containerIdentifiers
             ~f:ContainerIdentifierList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerIdentifiers =
        (Option.map ~f:ContainerIdentifierList.of_xml)
          (Xml.child xml_arg0 "ContainerIdentifiers") in
      let target =
        (Option.map ~f:SessionTarget.of_xml) (Xml.child xml_arg0 "Target") in
      let credentials =
        (Option.map ~f:AwsCredentials.of_xml)
          (Xml.child xml_arg0 "Credentials") in
      let computeArn =
        (Option.map ~f:ComputeArn.of_xml) (Xml.child xml_arg0 "ComputeArn") in
      let computeName =
        (Option.map ~f:ComputeNameOrArn.of_xml)
          (Xml.child xml_arg0 "ComputeName") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetIdOrArn.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?containerIdentifiers ?target ?credentials ?computeArn
        ?computeName ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let containerIdentifiers =
        field_map json__ "ContainerIdentifiers"
          ContainerIdentifierList.of_json in
      let target = field_map json__ "Target" SessionTarget.of_json in
      let credentials = field_map json__ "Credentials" AwsCredentials.of_json in
      let computeArn = field_map json__ "ComputeArn" ComputeArn.of_json in
      let computeName =
        field_map json__ "ComputeName" ComputeNameOrArn.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetIdOrArn.of_json in
      make ?containerIdentifiers ?target ?credentials ?computeArn
        ?computeName ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Requests authorization to remotely connect to a hosting resource in a Amazon GameLift Servers managed fleet. This operation is not used with Amazon GameLift Servers Anywhere fleets. Request options Provide the fleet ID and compute name. The compute name varies depending on the type of fleet. For a compute in a managed EC2 fleet, provide an instance ID. Each instance in the fleet is a compute. For a compute in a managed container fleet, provide a compute name. In a container fleet, each game server container group on a fleet instance is assigned a compute name. Results If successful, this operation returns a set of temporary Amazon Web Services credentials, including a two-part access key and a session token. With a managed EC2 fleet (where compute type is EC2), use these credentials with Amazon EC2 Systems Manager (SSM) to start a session with the compute. For more details, see Starting a session (CLI) in the Amazon EC2 Systems Manager User Guide."]
module GetComputeAccessInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet that holds the compute resource that you want to connect to. You can use either the fleet ID or ARN value."];
      computeName: ComputeNameOrArn.t
        [@ocaml.doc
          "A unique identifier for the compute resource that you want to connect to. For an EC2 fleet, use an instance ID. For a managed container fleet, use a compute name. You can retrieve a fleet's compute names by calling ListCompute."]}
    let context_ = "GetComputeAccessInput"
    let make ~fleetId =
      fun ~computeName -> fun () -> { fleetId; computeName }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("ComputeName", (Some (ComputeNameOrArn.to_value x.computeName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let computeName =
        ComputeNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ComputeName") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~computeName ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let computeName =
        field_map_exn json__ "ComputeName" ComputeNameOrArn.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~computeName ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Requests authorization to remotely connect to a hosting resource in a Amazon GameLift Servers managed fleet. This operation is not used with Amazon GameLift Servers Anywhere fleets. Request options Provide the fleet ID and compute name. The compute name varies depending on the type of fleet. For a compute in a managed EC2 fleet, provide an instance ID. Each instance in the fleet is a compute. For a compute in a managed container fleet, provide a compute name. In a container fleet, each game server container group on a fleet instance is assigned a compute name. Results If successful, this operation returns a set of temporary Amazon Web Services credentials, including a two-part access key and a session token. With a managed EC2 fleet (where compute type is EC2), use these credentials with Amazon EC2 Systems Manager (SSM) to start a session with the compute. For more details, see Starting a session (CLI) in the Amazon EC2 Systems Manager User Guide."]
module DescribeVpcPeeringConnectionsOutput =
  struct
    type nonrec t =
      {
      vpcPeeringConnections: VpcPeeringConnectionList.t option
        [@ocaml.doc
          "A collection of VPC peering connection records that match the request."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?vpcPeeringConnections = fun () -> { vpcPeeringConnections }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("VpcPeeringConnections",
           (Option.map x.vpcPeeringConnections
              ~f:VpcPeeringConnectionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringConnections =
        (Option.map ~f:VpcPeeringConnectionList.of_xml)
          (Xml.child xml_arg0 "VpcPeeringConnections") in
      make ?vpcPeeringConnections ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcPeeringConnections =
        field_map json__ "VpcPeeringConnections"
          VpcPeeringConnectionList.of_json in
      make ?vpcPeeringConnections ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves information on VPC peering connections. Use this operation to get peering information for all fleets or for one specific fleet ID. To retrieve connection information, call this operation from the Amazon Web Services account that is used to manage the Amazon GameLift Servers fleets. Specify a fleet ID or leave the parameter empty to retrieve all connection records. If successful, the retrieved information includes both active and pending connections. Active connections identify the IpV4 CIDR block that the VPC uses to connect. Related actions All APIs by task"]
module DescribeVpcPeeringConnectionsInput =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc
          "A unique identifier for the fleet. You can use either the fleet ID or ARN value."]}
    let make ?fleetId = fun () -> { fleetId }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves information on VPC peering connections. Use this operation to get peering information for all fleets or for one specific fleet ID. To retrieve connection information, call this operation from the Amazon Web Services account that is used to manage the Amazon GameLift Servers fleets. Specify a fleet ID or leave the parameter empty to retrieve all connection records. If successful, the retrieved information includes both active and pending connections. Active connections identify the IpV4 CIDR block that the VPC uses to connect. Related actions All APIs by task"]
module DescribeVpcPeeringAuthorizationsOutput =
  struct
    type nonrec t =
      {
      vpcPeeringAuthorizations: VpcPeeringAuthorizationList.t option
        [@ocaml.doc
          "A collection of objects that describe all valid VPC peering operations for the current Amazon Web Services account."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?vpcPeeringAuthorizations =
      fun () -> { vpcPeeringAuthorizations }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("VpcPeeringAuthorizations",
           (Option.map x.vpcPeeringAuthorizations
              ~f:VpcPeeringAuthorizationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringAuthorizations =
        (Option.map ~f:VpcPeeringAuthorizationList.of_xml)
          (Xml.child xml_arg0 "VpcPeeringAuthorizations") in
      make ?vpcPeeringAuthorizations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcPeeringAuthorizations =
        field_map json__ "VpcPeeringAuthorizations"
          VpcPeeringAuthorizationList.of_json in
      make ?vpcPeeringAuthorizations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves valid VPC peering authorizations that are pending for the Amazon Web Services account. This operation returns all VPC peering authorizations and requests for peering. This includes those initiated and received by this account. Related actions All APIs by task"]
module DescribeVpcPeeringAuthorizationsInput =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves valid VPC peering authorizations that are pending for the Amazon Web Services account. This operation returns all VPC peering authorizations and requests for peering. This includes those initiated and received by this account. Related actions All APIs by task"]
module DescribeScriptOutput =
  struct
    type nonrec t =
      {
      script: Script.t option
        [@ocaml.doc "A set of properties describing the requested script."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?script = fun () -> { script }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Script", (Option.map x.script ~f:Script.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let script =
        (Option.map ~f:Script.of_xml) (Xml.child xml_arg0 "Script") in
      make ?script ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let script = field_map json__ "Script" Script.of_json in
      make ?script ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves properties for a Realtime script. To request a script record, specify the script ID. If successful, an object containing the script properties is returned. Learn more Amazon GameLift Servers Amazon GameLift Servers Realtime Related actions All APIs by task"]
module DescribeScriptInput =
  struct
    type nonrec t =
      {
      scriptId: ScriptIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the Realtime script to retrieve properties for. You can use either the script ID or ARN value."]}
    let context_ = "DescribeScriptInput"
    let make ~scriptId = fun () -> { scriptId }
    let to_value x =
      structure_to_value
        [("ScriptId", (Some (ScriptIdOrArn.to_value x.scriptId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scriptId =
        ScriptIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScriptId") in
      make ~scriptId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scriptId = field_map_exn json__ "ScriptId" ScriptIdOrArn.of_json in
      make ~scriptId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves properties for a Realtime script. To request a script record, specify the script ID. If successful, an object containing the script properties is returned. Learn more Amazon GameLift Servers Amazon GameLift Servers Realtime Related actions All APIs by task"]
module DescribeScalingPoliciesOutput =
  struct
    type nonrec t =
      {
      scalingPolicies: ScalingPolicyList.t option
        [@ocaml.doc
          "A collection of objects containing the scaling policies matching the request."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?scalingPolicies =
      fun ?nextToken -> fun () -> { scalingPolicies; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ScalingPolicies",
           (Option.map x.scalingPolicies ~f:ScalingPolicyList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let scalingPolicies =
        (Option.map ~f:ScalingPolicyList.of_xml)
          (Xml.child xml_arg0 "ScalingPolicies") in
      make ?nextToken ?scalingPolicies ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let scalingPolicies =
        field_map json__ "ScalingPolicies" ScalingPolicyList.of_json in
      make ?nextToken ?scalingPolicies ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves all scaling policies applied to a fleet. To get a fleet's scaling policies, specify the fleet ID. You can filter this request by policy status, such as to retrieve only active scaling policies. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, set of ScalingPolicy objects is returned for the fleet. A fleet may have all of its scaling policies suspended. This operation does not affect the status of the scaling policies, which remains ACTIVE."]
module DescribeScalingPoliciesInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet for which to retrieve scaling policies. You can use either the fleet ID or ARN value."];
      statusFilter: ScalingStatusType.t option
        [@ocaml.doc
          "Scaling policy status to filter results on. A scaling policy is only in force when in an ACTIVE status. ACTIVE -- The scaling policy is currently in force. UPDATEREQUESTED -- A request to update the scaling policy has been received. UPDATING -- A change is being made to the scaling policy. DELETEREQUESTED -- A request to delete the scaling policy has been received. DELETING -- The scaling policy is being deleted. DELETED -- The scaling policy has been deleted. ERROR -- An error occurred in creating the policy. It should be removed and recreated."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The fleet location. If you don't specify this value, the response contains the scaling policies of every location in the fleet."]}
    let context_ = "DescribeScalingPoliciesInput"
    let make ?statusFilter =
      fun ?limit ->
        fun ?nextToken ->
          fun ?location ->
            fun ~fleetId ->
              fun () -> { statusFilter; limit; nextToken; location; fleetId }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("StatusFilter",
          (Option.map x.statusFilter ~f:ScalingStatusType.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let statusFilter =
        (Option.map ~f:ScalingStatusType.of_xml)
          (Xml.child xml_arg0 "StatusFilter") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?location ?nextToken ?limit ?statusFilter ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let statusFilter =
        field_map json__ "StatusFilter" ScalingStatusType.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?location ?nextToken ?limit ?statusFilter ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves all scaling policies applied to a fleet. To get a fleet's scaling policies, specify the fleet ID. You can filter this request by policy status, such as to retrieve only active scaling policies. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, set of ScalingPolicy objects is returned for the fleet. A fleet may have all of its scaling policies suspended. This operation does not affect the status of the scaling policies, which remains ACTIVE."]
module DescribeRuntimeConfigurationOutput =
  struct
    type nonrec t =
      {
      runtimeConfiguration: RuntimeConfiguration.t option
        [@ocaml.doc
          "Instructions that describe how server processes are launched and maintained on computes in the fleet."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?runtimeConfiguration = fun () -> { runtimeConfiguration }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RuntimeConfiguration",
           (Option.map x.runtimeConfiguration
              ~f:RuntimeConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let runtimeConfiguration =
        (Option.map ~f:RuntimeConfiguration.of_xml)
          (Xml.child xml_arg0 "RuntimeConfiguration") in
      make ?runtimeConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let runtimeConfiguration =
        field_map json__ "RuntimeConfiguration" RuntimeConfiguration.of_json in
      make ?runtimeConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves a fleet's runtime configuration settings. The runtime configuration determines which server processes run, and how, on computes in the fleet. For managed EC2 fleets, the runtime configuration describes server processes that run on each fleet instance. You can update a fleet's runtime configuration at any time using UpdateRuntimeConfiguration. To get the current runtime configuration for a fleet, provide the fleet ID. If successful, a RuntimeConfiguration object is returned for the requested fleet. If the requested fleet has been deleted, the result set is empty. Learn more Setting up Amazon GameLift Servers fleets Running multiple processes on a fleet"]
module DescribeRuntimeConfigurationInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to get the runtime configuration for. You can use either the fleet ID or ARN value."]}
    let context_ = "DescribeRuntimeConfigurationInput"
    let make ~fleetId = fun () -> { fleetId }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves a fleet's runtime configuration settings. The runtime configuration determines which server processes run, and how, on computes in the fleet. For managed EC2 fleets, the runtime configuration describes server processes that run on each fleet instance. You can update a fleet's runtime configuration at any time using UpdateRuntimeConfiguration. To get the current runtime configuration for a fleet, provide the fleet ID. If successful, a RuntimeConfiguration object is returned for the requested fleet. If the requested fleet has been deleted, the result set is empty. Learn more Setting up Amazon GameLift Servers fleets Running multiple processes on a fleet"]
module DescribePlayerSessionsOutput =
  struct
    type nonrec t =
      {
      playerSessions: PlayerSessionList.t option
        [@ocaml.doc
          "A collection of objects containing properties for each player session that matches the request."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?playerSessions =
      fun ?nextToken -> fun () -> { playerSessions; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PlayerSessions",
           (Option.map x.playerSessions ~f:PlayerSessionList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let playerSessions =
        (Option.map ~f:PlayerSessionList.of_xml)
          (Xml.child xml_arg0 "PlayerSessions") in
      make ?nextToken ?playerSessions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let playerSessions =
        field_map json__ "PlayerSessions" PlayerSessionList.of_json in
      make ?nextToken ?playerSessions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves properties for one or more player sessions. This action can be used in the following ways: To retrieve a specific player session, provide the player session ID only. To retrieve all player sessions in a game session, provide the game session ID only. To retrieve all player sessions for a specific player, provide a player ID only. To request player sessions, specify either a player session ID, game session ID, or player ID. You can filter this request by player session status. If you provide a specific PlayerSessionId or PlayerId, Amazon GameLift Servers ignores the filter criteria. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, a PlayerSession object is returned for each session that matches the request. Related actions All APIs by task"]
module DescribePlayerSessionsInput =
  struct
    type nonrec t =
      {
      gameSessionId: ArnStringModel.t option
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions to retrieve player sessions for. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>."];
      playerId: PlayerId.t option
        [@ocaml.doc
          "A unique identifier for a player to retrieve player sessions for."];
      playerSessionId: PlayerSessionId.t option
        [@ocaml.doc "A unique identifier for a player session to retrieve."];
      playerSessionStatusFilter: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Player session status to filter results on. Note that when a PlayerSessionId or PlayerId is provided in a DescribePlayerSessions request, then the PlayerSessionStatusFilter has no effect on the response. Possible player session statuses include the following: RESERVED -- The player session request has been received, but the player has not yet connected to the server process and/or been validated. ACTIVE -- The player has been validated by the server process and is currently connected. COMPLETED -- The player connection has been dropped. TIMEDOUT -- A player session request was received, but the player did not connect and/or was not validated within the timeout limit (60 seconds)."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages. If a player session ID is specified, this parameter is ignored."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value. If a player session ID is specified, this parameter is ignored."]}
    let make ?gameSessionId =
      fun ?playerId ->
        fun ?playerSessionId ->
          fun ?playerSessionStatusFilter ->
            fun ?limit ->
              fun ?nextToken ->
                fun () ->
                  {
                    gameSessionId;
                    playerId;
                    playerSessionId;
                    playerSessionStatusFilter;
                    limit;
                    nextToken
                  }
    let to_value x =
      structure_to_value
        [("GameSessionId",
           (Option.map x.gameSessionId ~f:ArnStringModel.to_value));
        ("PlayerId", (Option.map x.playerId ~f:PlayerId.to_value));
        ("PlayerSessionId",
          (Option.map x.playerSessionId ~f:PlayerSessionId.to_value));
        ("PlayerSessionStatusFilter",
          (Option.map x.playerSessionStatusFilter
             ~f:NonZeroAndMaxString.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let playerSessionStatusFilter =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "PlayerSessionStatusFilter") in
      let playerSessionId =
        (Option.map ~f:PlayerSessionId.of_xml)
          (Xml.child xml_arg0 "PlayerSessionId") in
      let playerId =
        (Option.map ~f:PlayerId.of_xml) (Xml.child xml_arg0 "PlayerId") in
      let gameSessionId =
        (Option.map ~f:ArnStringModel.of_xml)
          (Xml.child xml_arg0 "GameSessionId") in
      make ?nextToken ?limit ?playerSessionStatusFilter ?playerSessionId
        ?playerId ?gameSessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let playerSessionStatusFilter =
        field_map json__ "PlayerSessionStatusFilter"
          NonZeroAndMaxString.of_json in
      let playerSessionId =
        field_map json__ "PlayerSessionId" PlayerSessionId.of_json in
      let playerId = field_map json__ "PlayerId" PlayerId.of_json in
      let gameSessionId =
        field_map json__ "GameSessionId" ArnStringModel.of_json in
      make ?nextToken ?limit ?playerSessionStatusFilter ?playerSessionId
        ?playerId ?gameSessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves properties for one or more player sessions. This action can be used in the following ways: To retrieve a specific player session, provide the player session ID only. To retrieve all player sessions in a game session, provide the game session ID only. To retrieve all player sessions for a specific player, provide a player ID only. To request player sessions, specify either a player session ID, game session ID, or player ID. You can filter this request by player session status. If you provide a specific PlayerSessionId or PlayerId, Amazon GameLift Servers ignores the filter criteria. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, a PlayerSession object is returned for each session that matches the request. Related actions All APIs by task"]
module DescribeMatchmakingRuleSetsOutput =
  struct
    type nonrec t =
      {
      ruleSets: MatchmakingRuleSetList.t option
        [@ocaml.doc
          "A collection of requested matchmaking rule set objects."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ruleSets = fun ?nextToken -> fun () -> { ruleSets; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RuleSets",
           (Option.map x.ruleSets ~f:MatchmakingRuleSetList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let ruleSets =
        (Option.map ~f:MatchmakingRuleSetList.of_xml)
          (Xml.child xml_arg0 "RuleSets") in
      make ?nextToken ?ruleSets ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let ruleSets =
        field_map json__ "RuleSets" MatchmakingRuleSetList.of_json in
      make ?nextToken ?ruleSets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves the details for FlexMatch matchmaking rule sets. You can request all existing rule sets for the Region, or provide a list of one or more rule set names. When requesting multiple items, use the pagination parameters to retrieve results as a set of sequential pages. If successful, a rule set is returned for each requested name. Learn more Build a rule set"]
module DescribeMatchmakingRuleSetsInput =
  struct
    type nonrec t =
      {
      names: MatchmakingRuleSetNameList.t option
        [@ocaml.doc
          "A list of one or more matchmaking rule set names to retrieve details for. (Note: The rule set name is different from the optional \"name\" field in the rule set body.) You can use either the rule set name or ARN value."];
      limit: RuleSetLimit.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?names =
      fun ?limit -> fun ?nextToken -> fun () -> { names; limit; nextToken }
    let to_value x =
      structure_to_value
        [("Names",
           (Option.map x.names ~f:MatchmakingRuleSetNameList.to_value));
        ("Limit", (Option.map x.limit ~f:RuleSetLimit.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:RuleSetLimit.of_xml) (Xml.child xml_arg0 "Limit") in
      let names =
        (Option.map ~f:MatchmakingRuleSetNameList.of_xml)
          (Xml.child xml_arg0 "Names") in
      make ?nextToken ?limit ?names ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" RuleSetLimit.of_json in
      let names = field_map json__ "Names" MatchmakingRuleSetNameList.of_json in
      make ?nextToken ?limit ?names ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves the details for FlexMatch matchmaking rule sets. You can request all existing rule sets for the Region, or provide a list of one or more rule set names. When requesting multiple items, use the pagination parameters to retrieve results as a set of sequential pages. If successful, a rule set is returned for each requested name. Learn more Build a rule set"]
module DescribeMatchmakingOutput =
  struct
    type nonrec t =
      {
      ticketList: MatchmakingTicketList.t option
        [@ocaml.doc
          "A collection of existing matchmaking ticket objects matching the request."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ticketList = fun () -> { ticketList }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TicketList",
           (Option.map x.ticketList ~f:MatchmakingTicketList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ticketList =
        (Option.map ~f:MatchmakingTicketList.of_xml)
          (Xml.child xml_arg0 "TicketList") in
      make ?ticketList ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ticketList =
        field_map json__ "TicketList" MatchmakingTicketList.of_json in
      make ?ticketList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves one or more matchmaking tickets. Use this operation to retrieve ticket information, including--after a successful match is made--connection information for the resulting new game session. To request matchmaking tickets, provide a list of up to 10 ticket IDs. If the request is successful, a ticket object is returned for each requested ID that currently exists. This operation is not designed to be continually called to track matchmaking ticket status. This practice can cause you to exceed your API limit, which results in errors. Instead, as a best practice, set up an Amazon Simple Notification Service to receive notifications, and provide the topic ARN in the matchmaking configuration. Learn more Add FlexMatch to a game client Set Up FlexMatch event notification"]
module DescribeMatchmakingInput =
  struct
    type nonrec t =
      {
      ticketIds: MatchmakingIdList.t
        [@ocaml.doc
          "A unique identifier for a matchmaking ticket. You can include up to 10 ID values."]}
    let context_ = "DescribeMatchmakingInput"
    let make ~ticketIds = fun () -> { ticketIds }
    let to_value x =
      structure_to_value
        [("TicketIds", (Some (MatchmakingIdList.to_value x.ticketIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ticketIds =
        MatchmakingIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TicketIds") in
      make ~ticketIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ticketIds =
        field_map_exn json__ "TicketIds" MatchmakingIdList.of_json in
      make ~ticketIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves one or more matchmaking tickets. Use this operation to retrieve ticket information, including--after a successful match is made--connection information for the resulting new game session. To request matchmaking tickets, provide a list of up to 10 ticket IDs. If the request is successful, a ticket object is returned for each requested ID that currently exists. This operation is not designed to be continually called to track matchmaking ticket status. This practice can cause you to exceed your API limit, which results in errors. Instead, as a best practice, set up an Amazon Simple Notification Service to receive notifications, and provide the topic ARN in the matchmaking configuration. Learn more Add FlexMatch to a game client Set Up FlexMatch event notification"]
module DescribeMatchmakingConfigurationsOutput =
  struct
    type nonrec t =
      {
      configurations: MatchmakingConfigurationList.t option
        [@ocaml.doc "A collection of requested matchmaking configurations."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?configurations =
      fun ?nextToken -> fun () -> { configurations; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Configurations",
           (Option.map x.configurations
              ~f:MatchmakingConfigurationList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let configurations =
        (Option.map ~f:MatchmakingConfigurationList.of_xml)
          (Xml.child xml_arg0 "Configurations") in
      make ?nextToken ?configurations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let configurations =
        field_map json__ "Configurations"
          MatchmakingConfigurationList.of_json in
      make ?nextToken ?configurations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves the details of FlexMatch matchmaking configurations. This operation offers the following options: (1) retrieve all matchmaking configurations, (2) retrieve configurations for a specified list, or (3) retrieve all configurations that use a specified rule set name. When requesting multiple items, use the pagination parameters to retrieve results as a set of sequential pages. If successful, a configuration is returned for each requested name. When specifying a list of names, only configurations that currently exist are returned. Learn more Setting up FlexMatch matchmakers"]
module DescribeMatchmakingConfigurationsInput =
  struct
    type nonrec t =
      {
      names: MatchmakingConfigurationNameList.t option
        [@ocaml.doc
          "A unique identifier for the matchmaking configuration(s) to retrieve. You can use either the configuration name or ARN value. To request all existing configurations, leave this parameter empty."];
      ruleSetName: MatchmakingRuleSetName.t option
        [@ocaml.doc
          "A unique identifier for the matchmaking rule set. You can use either the rule set name or ARN value. Use this parameter to retrieve all matchmaking configurations that use this rule set."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages. This parameter is limited to 10."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?names =
      fun ?ruleSetName ->
        fun ?limit ->
          fun ?nextToken ->
            fun () -> { names; ruleSetName; limit; nextToken }
    let to_value x =
      structure_to_value
        [("Names",
           (Option.map x.names ~f:MatchmakingConfigurationNameList.to_value));
        ("RuleSetName",
          (Option.map x.ruleSetName ~f:MatchmakingRuleSetName.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let ruleSetName =
        (Option.map ~f:MatchmakingRuleSetName.of_xml)
          (Xml.child xml_arg0 "RuleSetName") in
      let names =
        (Option.map ~f:MatchmakingConfigurationNameList.of_xml)
          (Xml.child xml_arg0 "Names") in
      make ?nextToken ?limit ?ruleSetName ?names ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let ruleSetName =
        field_map json__ "RuleSetName" MatchmakingRuleSetName.of_json in
      let names =
        field_map json__ "Names" MatchmakingConfigurationNameList.of_json in
      make ?nextToken ?limit ?ruleSetName ?names ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves the details of FlexMatch matchmaking configurations. This operation offers the following options: (1) retrieve all matchmaking configurations, (2) retrieve configurations for a specified list, or (3) retrieve all configurations that use a specified rule set name. When requesting multiple items, use the pagination parameters to retrieve results as a set of sequential pages. If successful, a configuration is returned for each requested name. When specifying a list of names, only configurations that currently exist are returned. Learn more Setting up FlexMatch matchmakers"]
module DescribeInstancesOutput =
  struct
    type nonrec t =
      {
      instances: InstanceList.t option
        [@ocaml.doc
          "A collection of objects containing properties for each instance returned."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?instances =
      fun ?nextToken -> fun () -> { instances; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Instances", (Option.map x.instances ~f:InstanceList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let instances =
        (Option.map ~f:InstanceList.of_xml) (Xml.child xml_arg0 "Instances") in
      make ?nextToken ?instances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let instances = field_map json__ "Instances" InstanceList.of_json in
      make ?nextToken ?instances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types:EC2, Container Retrieves information about the EC2 instances in an Amazon GameLift Servers managed fleet, including instance ID, connection data, and status. You can use this operation with a multi-location fleet to get location-specific instance information. As an alternative, use the operations https://docs.aws.amazon.com/gamelift/latest/apireference/API_ListCompute and https://docs.aws.amazon.com/gamelift/latest/apireference/API_DescribeCompute to retrieve information for compute resources, including EC2 and Anywhere fleets. You can call this operation in the following ways: To get information on all instances in a fleet's home Region, specify the fleet ID. To get information on all instances in a fleet's remote location, specify the fleet ID and location name. To get information on a specific instance in a fleet, specify the fleet ID and instance ID. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, this operation returns Instance objects for each requested instance, listed in no particular order. If you call this operation for an Anywhere fleet, you receive an InvalidRequestException. Learn more Remotely connect to fleet instances Debug fleet issues Related actions All APIs by task"]
module DescribeInstancesInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to retrieve instance information for. You can use either the fleet ID or ARN value."];
      instanceId: InstanceId.t option
        [@ocaml.doc
          "A unique identifier for an instance to retrieve. Specify an instance ID or leave blank to retrieve all instances in the fleet."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The name of a location to retrieve instance information for, in the form of an Amazon Web Services Region code such as us-west-2."]}
    let context_ = "DescribeInstancesInput"
    let make ?instanceId =
      fun ?limit ->
        fun ?nextToken ->
          fun ?location ->
            fun ~fleetId ->
              fun () -> { instanceId; limit; nextToken; location; fleetId }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("InstanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "InstanceId") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?location ?nextToken ?limit ?instanceId ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let instanceId = field_map json__ "InstanceId" InstanceId.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?location ?nextToken ?limit ?instanceId ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types:EC2, Container Retrieves information about the EC2 instances in an Amazon GameLift Servers managed fleet, including instance ID, connection data, and status. You can use this operation with a multi-location fleet to get location-specific instance information. As an alternative, use the operations https://docs.aws.amazon.com/gamelift/latest/apireference/API_ListCompute and https://docs.aws.amazon.com/gamelift/latest/apireference/API_DescribeCompute to retrieve information for compute resources, including EC2 and Anywhere fleets. You can call this operation in the following ways: To get information on all instances in a fleet's home Region, specify the fleet ID. To get information on all instances in a fleet's remote location, specify the fleet ID and location name. To get information on a specific instance in a fleet, specify the fleet ID and instance ID. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, this operation returns Instance objects for each requested instance, listed in no particular order. If you call this operation for an Anywhere fleet, you receive an InvalidRequestException. Learn more Remotely connect to fleet instances Debug fleet issues Related actions All APIs by task"]
module DescribeGameSessionsOutput =
  struct
    type nonrec t =
      {
      gameSessions: GameSessionList.t option
        [@ocaml.doc
          "A collection of properties for each game session that matches the request."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TerminalRoutingStrategyException of
          TerminalRoutingStrategyException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameSessions =
      fun ?nextToken -> fun () -> { gameSessions; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TerminalRoutingStrategyException e ->
          `Assoc
            [("error", (`String "TerminalRoutingStrategyException"));
            ("details", (TerminalRoutingStrategyException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameSessions",
           (Option.map x.gameSessions ~f:GameSessionList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let gameSessions =
        (Option.map ~f:GameSessionList.of_xml)
          (Xml.child xml_arg0 "GameSessions") in
      make ?nextToken ?gameSessions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let gameSessions =
        field_map json__ "GameSessions" GameSessionList.of_json in
      make ?nextToken ?gameSessions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves a set of one or more game sessions in a specific fleet location. You can optionally filter the results by current game session status. This operation can be used in the following ways: To retrieve all game sessions that are currently running on all locations in a fleet, provide a fleet or alias ID, with an optional status filter. This approach returns all game sessions in the fleet's home Region and all remote locations. To retrieve all game sessions that are currently running on a specific fleet location, provide a fleet or alias ID and a location name, with optional status filter. The location can be the fleet's home Region or any remote location. To retrieve a specific game session, provide the game session ID. This approach looks for the game session ID in all fleets that reside in the Amazon Web Services Region defined in the request. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, a GameSession object is returned for each game session that matches the request. This operation is not designed to be continually called to track game session status. This practice can cause you to exceed your API limit, which results in errors. Instead, you must configure an Amazon Simple Notification Service (SNS) topic to receive notifications from FlexMatch or queues. Continuously polling with DescribeGameSessions should only be used for games in development with low game session usage. Available in Amazon GameLift Servers Local. Learn more Find a game session All APIs by task"]
module DescribeGameSessionsInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the fleet to retrieve game sessions for. You can use either the fleet ID or ARN value."];
      gameSessionId: ArnStringModel.t option
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions to retrieve. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>."];
      aliasId: AliasIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the alias associated with the fleet to retrieve game sessions for. You can use either the alias ID or ARN value."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "A fleet location to get game sessions for. You can specify a fleet's home Region or a remote location. Use the Amazon Web Services Region code format, such as us-west-2."];
      statusFilter: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Game session status to filter results on. You can filter on the following states: ACTIVE, TERMINATED, ACTIVATING, and TERMINATING. The last two are transitory and used for only very brief periods of time."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?fleetId =
      fun ?gameSessionId ->
        fun ?aliasId ->
          fun ?location ->
            fun ?statusFilter ->
              fun ?limit ->
                fun ?nextToken ->
                  fun () ->
                    {
                      fleetId;
                      gameSessionId;
                      aliasId;
                      location;
                      statusFilter;
                      limit;
                      nextToken
                    }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetIdOrArn.to_value));
        ("GameSessionId",
          (Option.map x.gameSessionId ~f:ArnStringModel.to_value));
        ("AliasId", (Option.map x.aliasId ~f:AliasIdOrArn.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value));
        ("StatusFilter",
          (Option.map x.statusFilter ~f:NonZeroAndMaxString.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let statusFilter =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "StatusFilter") in
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let aliasId =
        (Option.map ~f:AliasIdOrArn.of_xml) (Xml.child xml_arg0 "AliasId") in
      let gameSessionId =
        (Option.map ~f:ArnStringModel.of_xml)
          (Xml.child xml_arg0 "GameSessionId") in
      let fleetId =
        (Option.map ~f:FleetIdOrArn.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?nextToken ?limit ?statusFilter ?location ?aliasId ?gameSessionId
        ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let statusFilter =
        field_map json__ "StatusFilter" NonZeroAndMaxString.of_json in
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let aliasId = field_map json__ "AliasId" AliasIdOrArn.of_json in
      let gameSessionId =
        field_map json__ "GameSessionId" ArnStringModel.of_json in
      let fleetId = field_map json__ "FleetId" FleetIdOrArn.of_json in
      make ?nextToken ?limit ?statusFilter ?location ?aliasId ?gameSessionId
        ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves a set of one or more game sessions in a specific fleet location. You can optionally filter the results by current game session status. This operation can be used in the following ways: To retrieve all game sessions that are currently running on all locations in a fleet, provide a fleet or alias ID, with an optional status filter. This approach returns all game sessions in the fleet's home Region and all remote locations. To retrieve all game sessions that are currently running on a specific fleet location, provide a fleet or alias ID and a location name, with optional status filter. The location can be the fleet's home Region or any remote location. To retrieve a specific game session, provide the game session ID. This approach looks for the game session ID in all fleets that reside in the Amazon Web Services Region defined in the request. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, a GameSession object is returned for each game session that matches the request. This operation is not designed to be continually called to track game session status. This practice can cause you to exceed your API limit, which results in errors. Instead, you must configure an Amazon Simple Notification Service (SNS) topic to receive notifications from FlexMatch or queues. Continuously polling with DescribeGameSessions should only be used for games in development with low game session usage. Available in Amazon GameLift Servers Local. Learn more Find a game session All APIs by task"]
module DescribeGameSessionQueuesOutput =
  struct
    type nonrec t =
      {
      gameSessionQueues: GameSessionQueueList.t option
        [@ocaml.doc
          "A collection of objects that describe the requested game session queues."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameSessionQueues =
      fun ?nextToken -> fun () -> { gameSessionQueues; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameSessionQueues",
           (Option.map x.gameSessionQueues ~f:GameSessionQueueList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let gameSessionQueues =
        (Option.map ~f:GameSessionQueueList.of_xml)
          (Xml.child xml_arg0 "GameSessionQueues") in
      make ?nextToken ?gameSessionQueues ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let gameSessionQueues =
        field_map json__ "GameSessionQueues" GameSessionQueueList.of_json in
      make ?nextToken ?gameSessionQueues ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves the properties for one or more game session queues. When requesting multiple queues, use the pagination parameters to retrieve results as a set of sequential pages. When specifying a list of queues, objects are returned only for queues that currently exist in the Region. Learn more View Your Queues"]
module DescribeGameSessionQueuesInput =
  struct
    type nonrec t =
      {
      names: GameSessionQueueNameOrArnList.t option
        [@ocaml.doc
          "A list of queue names to retrieve information for. You can use either the queue ID or ARN value. To request settings for all queues, leave this parameter empty."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages. You can request up to 50 results."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?names =
      fun ?limit -> fun ?nextToken -> fun () -> { names; limit; nextToken }
    let to_value x =
      structure_to_value
        [("Names",
           (Option.map x.names ~f:GameSessionQueueNameOrArnList.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let names =
        (Option.map ~f:GameSessionQueueNameOrArnList.of_xml)
          (Xml.child xml_arg0 "Names") in
      make ?nextToken ?limit ?names ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let names =
        field_map json__ "Names" GameSessionQueueNameOrArnList.of_json in
      make ?nextToken ?limit ?names ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves the properties for one or more game session queues. When requesting multiple queues, use the pagination parameters to retrieve results as a set of sequential pages. When specifying a list of queues, objects are returned only for queues that currently exist in the Region. Learn more View Your Queues"]
module DescribeGameSessionPlacementOutput =
  struct
    type nonrec t =
      {
      gameSessionPlacement: GameSessionPlacement.t option
        [@ocaml.doc
          "Object that describes the requested game session placement."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameSessionPlacement = fun () -> { gameSessionPlacement }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameSessionPlacement",
           (Option.map x.gameSessionPlacement
              ~f:GameSessionPlacement.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameSessionPlacement =
        (Option.map ~f:GameSessionPlacement.of_xml)
          (Xml.child xml_arg0 "GameSessionPlacement") in
      make ?gameSessionPlacement ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameSessionPlacement =
        field_map json__ "GameSessionPlacement" GameSessionPlacement.of_json in
      make ?gameSessionPlacement ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves information, including current status, about a game session placement request. To get game session placement details, specify the placement ID. This operation is not designed to be continually called to track game session status. This practice can cause you to exceed your API limit, which results in errors. Instead, you must configure an Amazon Simple Notification Service (SNS) topic to receive notifications from FlexMatch or queues. Continuously polling with DescribeGameSessionPlacement should only be used for games in development with low game session usage. For a reference implementation of event-based game session placement tracking, see Event-based game session placement guidance in the Amazon GameLift Toolkit."]
module DescribeGameSessionPlacementInput =
  struct
    type nonrec t =
      {
      placementId: IdStringModel.t
        [@ocaml.doc
          "A unique identifier for a game session placement to retrieve."]}
    let context_ = "DescribeGameSessionPlacementInput"
    let make ~placementId = fun () -> { placementId }
    let to_value x =
      structure_to_value
        [("PlacementId", (Some (IdStringModel.to_value x.placementId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let placementId =
        IdStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PlacementId") in
      make ~placementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let placementId =
        field_map_exn json__ "PlacementId" IdStringModel.of_json in
      make ~placementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves information, including current status, about a game session placement request. To get game session placement details, specify the placement ID. This operation is not designed to be continually called to track game session status. This practice can cause you to exceed your API limit, which results in errors. Instead, you must configure an Amazon Simple Notification Service (SNS) topic to receive notifications from FlexMatch or queues. Continuously polling with DescribeGameSessionPlacement should only be used for games in development with low game session usage. For a reference implementation of event-based game session placement tracking, see Event-based game session placement guidance in the Amazon GameLift Toolkit."]
module DescribeGameSessionDetailsOutput =
  struct
    type nonrec t =
      {
      gameSessionDetails: GameSessionDetailList.t option
        [@ocaml.doc
          "A collection of properties for each game session that matches the request."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TerminalRoutingStrategyException of
          TerminalRoutingStrategyException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameSessionDetails =
      fun ?nextToken -> fun () -> { gameSessionDetails; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TerminalRoutingStrategyException e ->
          `Assoc
            [("error", (`String "TerminalRoutingStrategyException"));
            ("details", (TerminalRoutingStrategyException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameSessionDetails",
           (Option.map x.gameSessionDetails ~f:GameSessionDetailList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let gameSessionDetails =
        (Option.map ~f:GameSessionDetailList.of_xml)
          (Xml.child xml_arg0 "GameSessionDetails") in
      make ?nextToken ?gameSessionDetails ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let gameSessionDetails =
        field_map json__ "GameSessionDetails" GameSessionDetailList.of_json in
      make ?nextToken ?gameSessionDetails ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves additional game session properties, including the game session protection policy in force, a set of one or more game sessions in a specific fleet location. You can optionally filter the results by current game session status. This operation can be used in the following ways: To retrieve details for all game sessions that are currently running on all locations in a fleet, provide a fleet or alias ID, with an optional status filter. This approach returns details from the fleet's home Region and all remote locations. To retrieve details for all game sessions that are currently running on a specific fleet location, provide a fleet or alias ID and a location name, with optional status filter. The location can be the fleet's home Region or any remote location. To retrieve details for a specific game session, provide the game session ID. This approach looks for the game session ID in all fleets that reside in the Amazon Web Services Region defined in the request. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, a GameSessionDetail object is returned for each game session that matches the request. Learn more Find a game session All APIs by task"]
module DescribeGameSessionDetailsInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the fleet to retrieve all game sessions active on the fleet. You can use either the fleet ID or ARN value."];
      gameSessionId: ArnStringModel.t option
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions to retrieve. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>."];
      aliasId: AliasIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the alias associated with the fleet to retrieve all game sessions for. You can use either the alias ID or ARN value."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "A fleet location to get game session details for. You can specify a fleet's home Region or a remote location. Use the Amazon Web Services Region code format, such as us-west-2."];
      statusFilter: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Game session status to filter results on. Possible game session statuses include ACTIVE, TERMINATED, ACTIVATING and TERMINATING (the last two are transitory)."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let make ?fleetId =
      fun ?gameSessionId ->
        fun ?aliasId ->
          fun ?location ->
            fun ?statusFilter ->
              fun ?limit ->
                fun ?nextToken ->
                  fun () ->
                    {
                      fleetId;
                      gameSessionId;
                      aliasId;
                      location;
                      statusFilter;
                      limit;
                      nextToken
                    }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetIdOrArn.to_value));
        ("GameSessionId",
          (Option.map x.gameSessionId ~f:ArnStringModel.to_value));
        ("AliasId", (Option.map x.aliasId ~f:AliasIdOrArn.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value));
        ("StatusFilter",
          (Option.map x.statusFilter ~f:NonZeroAndMaxString.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let statusFilter =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "StatusFilter") in
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let aliasId =
        (Option.map ~f:AliasIdOrArn.of_xml) (Xml.child xml_arg0 "AliasId") in
      let gameSessionId =
        (Option.map ~f:ArnStringModel.of_xml)
          (Xml.child xml_arg0 "GameSessionId") in
      let fleetId =
        (Option.map ~f:FleetIdOrArn.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?nextToken ?limit ?statusFilter ?location ?aliasId ?gameSessionId
        ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let statusFilter =
        field_map json__ "StatusFilter" NonZeroAndMaxString.of_json in
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let aliasId = field_map json__ "AliasId" AliasIdOrArn.of_json in
      let gameSessionId =
        field_map json__ "GameSessionId" ArnStringModel.of_json in
      let fleetId = field_map json__ "FleetId" FleetIdOrArn.of_json in
      make ?nextToken ?limit ?statusFilter ?location ?aliasId ?gameSessionId
        ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves additional game session properties, including the game session protection policy in force, a set of one or more game sessions in a specific fleet location. You can optionally filter the results by current game session status. This operation can be used in the following ways: To retrieve details for all game sessions that are currently running on all locations in a fleet, provide a fleet or alias ID, with an optional status filter. This approach returns details from the fleet's home Region and all remote locations. To retrieve details for all game sessions that are currently running on a specific fleet location, provide a fleet or alias ID and a location name, with optional status filter. The location can be the fleet's home Region or any remote location. To retrieve details for a specific game session, provide the game session ID. This approach looks for the game session ID in all fleets that reside in the Amazon Web Services Region defined in the request. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, a GameSessionDetail object is returned for each game session that matches the request. Learn more Find a game session All APIs by task"]
module DescribeGameServerOutput =
  struct
    type nonrec t =
      {
      gameServer: GameServer.t option
        [@ocaml.doc "Object that describes the requested game server."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameServer = fun () -> { gameServer }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameServer", (Option.map x.gameServer ~f:GameServer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServer =
        (Option.map ~f:GameServer.of_xml) (Xml.child xml_arg0 "GameServer") in
      make ?gameServer ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServer = field_map json__ "GameServer" GameServer.of_json in
      make ?gameServer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Retrieves information for a registered game server. Information includes game server status, health check info, and the instance that the game server is running on. To retrieve game server information, specify the game server ID. If successful, the requested game server object is returned. Learn more Amazon GameLift Servers FleetIQ Guide"]
module DescribeGameServerInstancesOutput =
  struct
    type nonrec t =
      {
      gameServerInstances: GameServerInstances.t option
        [@ocaml.doc "The collection of requested game server instances."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameServerInstances =
      fun ?nextToken -> fun () -> { gameServerInstances; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameServerInstances",
           (Option.map x.gameServerInstances ~f:GameServerInstances.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let gameServerInstances =
        (Option.map ~f:GameServerInstances.of_xml)
          (Xml.child xml_arg0 "GameServerInstances") in
      make ?nextToken ?gameServerInstances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let gameServerInstances =
        field_map json__ "GameServerInstances" GameServerInstances.of_json in
      make ?nextToken ?gameServerInstances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Retrieves status information about the Amazon EC2 instances associated with a Amazon GameLift Servers FleetIQ game server group. Use this operation to detect when instances are active or not available to host new game servers. To request status for all instances in the game server group, provide a game server group ID only. To request status for specific instances, provide the game server group ID and one or more instance IDs. Use the pagination parameters to retrieve results in sequential segments. If successful, a collection of GameServerInstance objects is returned. This operation is not designed to be called with every game server claim request; this practice can cause you to exceed your API limit, which results in errors. Instead, as a best practice, cache the results and refresh your cache no more than once every 10 seconds. Learn more Amazon GameLift Servers FleetIQ Guide"]
module DescribeGameServerInstancesInput =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupNameOrArn.t
        [@ocaml.doc
          "A unique identifier for the game server group. Use either the name or ARN value."];
      instanceIds: GameServerInstanceIds.t option
        [@ocaml.doc
          "The Amazon EC2 instance IDs that you want to retrieve status on. Amazon EC2 instance IDs use a 17-character format, for example: i-1234567890abcdef0. To retrieve all instances in the game server group, leave this parameter empty."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let context_ = "DescribeGameServerInstancesInput"
    let make ?instanceIds =
      fun ?limit ->
        fun ?nextToken ->
          fun ~gameServerGroupName ->
            fun () -> { instanceIds; limit; nextToken; gameServerGroupName }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Some (GameServerGroupNameOrArn.to_value x.gameServerGroupName)));
        ("InstanceIds",
          (Option.map x.instanceIds ~f:GameServerInstanceIds.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let instanceIds =
        (Option.map ~f:GameServerInstanceIds.of_xml)
          (Xml.child xml_arg0 "InstanceIds") in
      let gameServerGroupName =
        GameServerGroupNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerGroupName") in
      make ?nextToken ?limit ?instanceIds ~gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let instanceIds =
        field_map json__ "InstanceIds" GameServerInstanceIds.of_json in
      let gameServerGroupName =
        field_map_exn json__ "GameServerGroupName"
          GameServerGroupNameOrArn.of_json in
      make ?nextToken ?limit ?instanceIds ~gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Retrieves status information about the Amazon EC2 instances associated with a Amazon GameLift Servers FleetIQ game server group. Use this operation to detect when instances are active or not available to host new game servers. To request status for all instances in the game server group, provide a game server group ID only. To request status for specific instances, provide the game server group ID and one or more instance IDs. Use the pagination parameters to retrieve results in sequential segments. If successful, a collection of GameServerInstance objects is returned. This operation is not designed to be called with every game server claim request; this practice can cause you to exceed your API limit, which results in errors. Instead, as a best practice, cache the results and refresh your cache no more than once every 10 seconds. Learn more Amazon GameLift Servers FleetIQ Guide"]
module DescribeGameServerInput =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupNameOrArn.t
        [@ocaml.doc
          "A unique identifier for the game server group where the game server is running."];
      gameServerId: GameServerId.t
        [@ocaml.doc
          "A custom string that uniquely identifies the game server information to be retrieved."]}
    let context_ = "DescribeGameServerInput"
    let make ~gameServerGroupName =
      fun ~gameServerId -> fun () -> { gameServerGroupName; gameServerId }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Some (GameServerGroupNameOrArn.to_value x.gameServerGroupName)));
        ("GameServerId", (Some (GameServerId.to_value x.gameServerId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServerId =
        GameServerId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerId") in
      let gameServerGroupName =
        GameServerGroupNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerGroupName") in
      make ~gameServerId ~gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServerId =
        field_map_exn json__ "GameServerId" GameServerId.of_json in
      let gameServerGroupName =
        field_map_exn json__ "GameServerGroupName"
          GameServerGroupNameOrArn.of_json in
      make ~gameServerId ~gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Retrieves information for a registered game server. Information includes game server status, health check info, and the instance that the game server is running on. To retrieve game server information, specify the game server ID. If successful, the requested game server object is returned. Learn more Amazon GameLift Servers FleetIQ Guide"]
module DescribeGameServerGroupOutput =
  struct
    type nonrec t =
      {
      gameServerGroup: GameServerGroup.t option
        [@ocaml.doc
          "An object with the property settings for the requested game server group resource."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameServerGroup = fun () -> { gameServerGroup }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameServerGroup",
           (Option.map x.gameServerGroup ~f:GameServerGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServerGroup =
        (Option.map ~f:GameServerGroup.of_xml)
          (Xml.child xml_arg0 "GameServerGroup") in
      make ?gameServerGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServerGroup =
        field_map json__ "GameServerGroup" GameServerGroup.of_json in
      make ?gameServerGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Retrieves information on a game server group. This operation returns only properties related to Amazon GameLift Servers FleetIQ. To view or update properties for the corresponding Auto Scaling group, such as launch template, auto scaling policies, and maximum/minimum group size, access the Auto Scaling group directly. To get attributes for a game server group, provide a group name or ARN value. If successful, a GameServerGroup object is returned. Learn more Amazon GameLift Servers FleetIQ Guide"]
module DescribeGameServerGroupInput =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupNameOrArn.t
        [@ocaml.doc
          "A unique identifier for the game server group. Use either the name or ARN value."]}
    let context_ = "DescribeGameServerGroupInput"
    let make ~gameServerGroupName = fun () -> { gameServerGroupName }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Some (GameServerGroupNameOrArn.to_value x.gameServerGroupName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServerGroupName =
        GameServerGroupNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerGroupName") in
      make ~gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServerGroupName =
        field_map_exn json__ "GameServerGroupName"
          GameServerGroupNameOrArn.of_json in
      make ~gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Retrieves information on a game server group. This operation returns only properties related to Amazon GameLift Servers FleetIQ. To view or update properties for the corresponding Auto Scaling group, such as launch template, auto scaling policies, and maximum/minimum group size, access the Auto Scaling group directly. To get attributes for a game server group, provide a group name or ARN value. If successful, a GameServerGroup object is returned. Learn more Amazon GameLift Servers FleetIQ Guide"]
module DescribeFleetUtilizationOutput =
  struct
    type nonrec t =
      {
      fleetUtilization: FleetUtilizationList.t option
        [@ocaml.doc
          "A collection of objects containing utilization information for each requested fleet ID. Utilization objects are returned only for fleets that currently exist."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetUtilization =
      fun ?nextToken -> fun () -> { fleetUtilization; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetUtilization",
           (Option.map x.fleetUtilization ~f:FleetUtilizationList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let fleetUtilization =
        (Option.map ~f:FleetUtilizationList.of_xml)
          (Xml.child xml_arg0 "FleetUtilization") in
      make ?nextToken ?fleetUtilization ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let fleetUtilization =
        field_map json__ "FleetUtilization" FleetUtilizationList.of_json in
      make ?nextToken ?fleetUtilization ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Retrieves utilization statistics for one or more fleets. Utilization data provides a snapshot of how the fleet's hosting resources are currently being used. For fleets with remote locations, this operation retrieves data for the fleet's home Region only. See DescribeFleetLocationUtilization to get utilization statistics for a fleet's remote locations. This operation can be used in the following ways: To get utilization data for one or more specific fleets, provide a list of fleet IDs or fleet ARNs. To get utilization data for all fleets, do not provide a fleet identifier. When requesting multiple fleets, use the pagination parameters to retrieve results as a set of sequential pages. If successful, a FleetUtilization object is returned for each requested fleet ID, unless the fleet identifier is not found. Each fleet utilization object includes a Location property, which is set to the fleet's home Region. Some API operations may limit the number of fleet IDs allowed in one request. If a request exceeds this limit, the request fails and the error message includes the maximum allowed. Learn more Setting up Amazon GameLift Servers Fleets GameLift Metrics for Fleets"]
module DescribeFleetUtilizationInput =
  struct
    type nonrec t =
      {
      fleetIds: FleetIdOrArnList.t option
        [@ocaml.doc
          "A unique identifier for the fleet to retrieve utilization data for. You can use either the fleet ID or ARN value. To retrieve attributes for all current fleets, do not include this parameter."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages. This parameter is ignored when the request specifies one or a list of fleet IDs."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value. This parameter is ignored when the request specifies one or a list of fleet IDs."]}
    let make ?fleetIds =
      fun ?limit ->
        fun ?nextToken -> fun () -> { fleetIds; limit; nextToken }
    let to_value x =
      structure_to_value
        [("FleetIds", (Option.map x.fleetIds ~f:FleetIdOrArnList.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let fleetIds =
        (Option.map ~f:FleetIdOrArnList.of_xml)
          (Xml.child xml_arg0 "FleetIds") in
      make ?nextToken ?limit ?fleetIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let fleetIds = field_map json__ "FleetIds" FleetIdOrArnList.of_json in
      make ?nextToken ?limit ?fleetIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Retrieves utilization statistics for one or more fleets. Utilization data provides a snapshot of how the fleet's hosting resources are currently being used. For fleets with remote locations, this operation retrieves data for the fleet's home Region only. See DescribeFleetLocationUtilization to get utilization statistics for a fleet's remote locations. This operation can be used in the following ways: To get utilization data for one or more specific fleets, provide a list of fleet IDs or fleet ARNs. To get utilization data for all fleets, do not provide a fleet identifier. When requesting multiple fleets, use the pagination parameters to retrieve results as a set of sequential pages. If successful, a FleetUtilization object is returned for each requested fleet ID, unless the fleet identifier is not found. Each fleet utilization object includes a Location property, which is set to the fleet's home Region. Some API operations may limit the number of fleet IDs allowed in one request. If a request exceeds this limit, the request fails and the error message includes the maximum allowed. Learn more Setting up Amazon GameLift Servers Fleets GameLift Metrics for Fleets"]
module DescribeFleetPortSettingsOutput =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc "A unique identifier for the fleet that was requested."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      inboundPermissions: IpPermissionsList.t option
        [@ocaml.doc "The port settings for the requested fleet ID."];
      updateStatus: LocationUpdateStatus.t option
        [@ocaml.doc
          "The current status of updates to the fleet's port settings in the requested fleet location. A status of PENDING_UPDATE indicates that an update was requested for the fleet but has not yet been completed for the location."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The requested fleet location, expressed as an Amazon Web Services Region code, such as us-west-2."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?inboundPermissions ->
          fun ?updateStatus ->
            fun ?location ->
              fun () ->
                {
                  fleetId;
                  fleetArn;
                  inboundPermissions;
                  updateStatus;
                  location
                }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("InboundPermissions",
          (Option.map x.inboundPermissions ~f:IpPermissionsList.to_value));
        ("UpdateStatus",
          (Option.map x.updateStatus ~f:LocationUpdateStatus.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let updateStatus =
        (Option.map ~f:LocationUpdateStatus.of_xml)
          (Xml.child xml_arg0 "UpdateStatus") in
      let inboundPermissions =
        (Option.map ~f:IpPermissionsList.of_xml)
          (Xml.child xml_arg0 "InboundPermissions") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?location ?updateStatus ?inboundPermissions ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let updateStatus =
        field_map json__ "UpdateStatus" LocationUpdateStatus.of_json in
      let inboundPermissions =
        field_map json__ "InboundPermissions" IpPermissionsList.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?location ?updateStatus ?inboundPermissions ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Retrieves a fleet's inbound connection permissions. Connection permissions specify IP addresses and port settings that incoming traffic can use to access server processes in the fleet. Game server processes that are running in the fleet must use a port that falls within this range. Use this operation in the following ways: To retrieve the port settings for a fleet, identify the fleet's unique identifier. To check the status of recent updates to a fleet remote location, specify the fleet ID and a location. Port setting updates can take time to propagate across all locations. If successful, a set of IpPermission objects is returned for the requested fleet ID. When specifying a location, this operation returns a pending status. If the requested fleet has been deleted, the result set is empty. Learn more Setting up Amazon GameLift Servers fleets"]
module DescribeFleetPortSettingsInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to retrieve port settings for. You can use either the fleet ID or ARN value."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "A remote location to check for status of port setting updates. Use the Amazon Web Services Region code format, such as us-west-2."]}
    let context_ = "DescribeFleetPortSettingsInput"
    let make ?location = fun ~fleetId -> fun () -> { location; fleetId }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?location ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?location ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Retrieves a fleet's inbound connection permissions. Connection permissions specify IP addresses and port settings that incoming traffic can use to access server processes in the fleet. Game server processes that are running in the fleet must use a port that falls within this range. Use this operation in the following ways: To retrieve the port settings for a fleet, identify the fleet's unique identifier. To check the status of recent updates to a fleet remote location, specify the fleet ID and a location. Port setting updates can take time to propagate across all locations. If successful, a set of IpPermission objects is returned for the requested fleet ID. When specifying a location, this operation returns a pending status. If the requested fleet has been deleted, the result set is empty. Learn more Setting up Amazon GameLift Servers fleets"]
module DescribeFleetLocationUtilizationOutput =
  struct
    type nonrec t =
      {
      fleetUtilization: FleetUtilization.t option
        [@ocaml.doc
          "Utilization information for the requested fleet location. Utilization objects are returned only for fleets and locations that currently exist."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetUtilization = fun () -> { fleetUtilization }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetUtilization",
           (Option.map x.fleetUtilization ~f:FleetUtilization.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetUtilization =
        (Option.map ~f:FleetUtilization.of_xml)
          (Xml.child xml_arg0 "FleetUtilization") in
      make ?fleetUtilization ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetUtilization =
        field_map json__ "FleetUtilization" FleetUtilization.of_json in
      make ?fleetUtilization ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves current usage data for a fleet location. Utilization data provides a snapshot of current game hosting activity at the requested location. Use this operation to retrieve utilization information for a fleet's remote location or home Region (you can also retrieve home Region utilization by calling DescribeFleetUtilization). To retrieve utilization data, identify a fleet and location. If successful, a FleetUtilization object is returned for the requested fleet location. Learn more Setting up Amazon GameLift Servers fleets Amazon GameLift Servers service locations for managed hosting GameLift metrics for fleets"]
module DescribeFleetLocationUtilizationInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to request location utilization for. You can use either the fleet ID or ARN value."];
      location: LocationStringModel.t
        [@ocaml.doc
          "The fleet location to retrieve utilization information for. Specify a location in the form of an Amazon Web Services Region code, such as us-west-2."]}
    let context_ = "DescribeFleetLocationUtilizationInput"
    let make ~fleetId = fun ~location -> fun () -> { fleetId; location }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("Location", (Some (LocationStringModel.to_value x.location)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        LocationStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Location") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~location ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location =
        field_map_exn json__ "Location" LocationStringModel.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~location ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves current usage data for a fleet location. Utilization data provides a snapshot of current game hosting activity at the requested location. Use this operation to retrieve utilization information for a fleet's remote location or home Region (you can also retrieve home Region utilization by calling DescribeFleetUtilization). To retrieve utilization data, identify a fleet and location. If successful, a FleetUtilization object is returned for the requested fleet location. Learn more Setting up Amazon GameLift Servers fleets Amazon GameLift Servers service locations for managed hosting GameLift metrics for fleets"]
module DescribeFleetLocationCapacityOutput =
  struct
    type nonrec t =
      {
      fleetCapacity: FleetCapacity.t option
        [@ocaml.doc
          "Resource capacity information for the requested fleet location. Capacity objects are returned only for fleets and locations that currently exist. Changes in desired instance value can take up to 1 minute to be reflected."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetCapacity = fun () -> { fleetCapacity }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetCapacity",
           (Option.map x.fleetCapacity ~f:FleetCapacity.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetCapacity =
        (Option.map ~f:FleetCapacity.of_xml)
          (Xml.child xml_arg0 "FleetCapacity") in
      make ?fleetCapacity ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetCapacity =
        field_map json__ "FleetCapacity" FleetCapacity.of_json in
      make ?fleetCapacity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Retrieves the resource capacity settings for a fleet location. The data returned includes the current capacity (number of EC2 instances) and some scaling settings for the requested fleet location. For a managed container fleet, this operation also returns counts for game server container groups. Use this operation to retrieve capacity information for a fleet's remote location or home Region (you can also retrieve home Region capacity by calling DescribeFleetCapacity). To retrieve capacity data, identify a fleet and location. If successful, a FleetCapacity object is returned for the requested fleet location. Learn more Setting up Amazon GameLift Servers fleets Amazon GameLift Servers service locations for managed hosting GameLift metrics for fleets"]
module DescribeFleetLocationCapacityInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to request location capacity for. You can use either the fleet ID or ARN value."];
      location: LocationStringModel.t
        [@ocaml.doc
          "The fleet location to retrieve capacity information for. Specify a location in the form of an Amazon Web Services Region code, such as us-west-2."]}
    let context_ = "DescribeFleetLocationCapacityInput"
    let make ~fleetId = fun ~location -> fun () -> { fleetId; location }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("Location", (Some (LocationStringModel.to_value x.location)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        LocationStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Location") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~location ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location =
        field_map_exn json__ "Location" LocationStringModel.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~location ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Retrieves the resource capacity settings for a fleet location. The data returned includes the current capacity (number of EC2 instances) and some scaling settings for the requested fleet location. For a managed container fleet, this operation also returns counts for game server container groups. Use this operation to retrieve capacity information for a fleet's remote location or home Region (you can also retrieve home Region capacity by calling DescribeFleetCapacity). To retrieve capacity data, identify a fleet and location. If successful, a FleetCapacity object is returned for the requested fleet location. Learn more Setting up Amazon GameLift Servers fleets Amazon GameLift Servers service locations for managed hosting GameLift metrics for fleets"]
module DescribeFleetLocationAttributesOutput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the fleet that location attributes were requested for."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      locationAttributes: LocationAttributesList.t option
        [@ocaml.doc
          "Location-specific information on the requested fleet's remote locations."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?locationAttributes ->
          fun ?nextToken ->
            fun () -> { fleetId; fleetArn; locationAttributes; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetIdOrArn.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("LocationAttributes",
          (Option.map x.locationAttributes ~f:LocationAttributesList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let locationAttributes =
        (Option.map ~f:LocationAttributesList.of_xml)
          (Xml.child xml_arg0 "LocationAttributes") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetIdOrArn.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?nextToken ?locationAttributes ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let locationAttributes =
        field_map json__ "LocationAttributes" LocationAttributesList.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetIdOrArn.of_json in
      make ?nextToken ?locationAttributes ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Retrieves information on a fleet's remote locations, including life-cycle status and any suspended fleet activity. This operation can be used in the following ways: To get data for specific locations, provide a fleet identifier and a list of locations. Location data is returned in the order that it is requested. To get data for all locations, provide a fleet identifier only. Location data is returned in no particular order. When requesting attributes for multiple locations, use the pagination parameters to retrieve results as a set of sequential pages. If successful, a LocationAttributes object is returned for each requested location. If the fleet does not have a requested location, no information is returned. This operation does not return the home Region. To get information on a fleet's home Region, call DescribeFleetAttributes. Learn more Setting up Amazon GameLift Servers fleets Amazon GameLift Servers service locations for managed hosting"]
module DescribeFleetLocationAttributesInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to retrieve remote locations for. You can use either the fleet ID or ARN value."];
      locations: LocationList.t option
        [@ocaml.doc
          "A list of fleet locations to retrieve information for. Specify locations in the form of an Amazon Web Services Region code, such as us-west-2."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages. This limit is not currently enforced."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let context_ = "DescribeFleetLocationAttributesInput"
    let make ?locations =
      fun ?limit ->
        fun ?nextToken ->
          fun ~fleetId -> fun () -> { locations; limit; nextToken; fleetId }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("Locations", (Option.map x.locations ~f:LocationList.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let locations =
        (Option.map ~f:LocationList.of_xml) (Xml.child xml_arg0 "Locations") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?nextToken ?limit ?locations ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let locations = field_map json__ "Locations" LocationList.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?nextToken ?limit ?locations ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Retrieves information on a fleet's remote locations, including life-cycle status and any suspended fleet activity. This operation can be used in the following ways: To get data for specific locations, provide a fleet identifier and a list of locations. Location data is returned in the order that it is requested. To get data for all locations, provide a fleet identifier only. Location data is returned in no particular order. When requesting attributes for multiple locations, use the pagination parameters to retrieve results as a set of sequential pages. If successful, a LocationAttributes object is returned for each requested location. If the fleet does not have a requested location, no information is returned. This operation does not return the home Region. To get information on a fleet's home Region, call DescribeFleetAttributes. Learn more Setting up Amazon GameLift Servers fleets Amazon GameLift Servers service locations for managed hosting"]
module DescribeFleetEventsOutput =
  struct
    type nonrec t =
      {
      events: EventList.t option
        [@ocaml.doc
          "A collection of objects containing event log entries for the specified fleet."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?events = fun ?nextToken -> fun () -> { events; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Events", (Option.map x.events ~f:EventList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let events =
        (Option.map ~f:EventList.of_xml) (Xml.child xml_arg0 "Events") in
      make ?nextToken ?events ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let events = field_map json__ "Events" EventList.of_json in
      make ?nextToken ?events ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves entries from a fleet's event log. Fleet events are initiated by changes in status, such as during fleet creation and termination, changes in capacity, etc. If a fleet has multiple locations, events are also initiated by changes to status and capacity in remote locations. You can specify a time range to limit the result set. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, a collection of event log entries matching the request are returned. Learn more Setting up Amazon GameLift Servers fleets"]
module DescribeFleetEventsInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to get event logs for. You can use either the fleet ID or ARN value."];
      startTime: Timestamp.t option
        [@ocaml.doc
          "The earliest date to retrieve event logs for. If no start time is specified, this call returns entries starting from when the fleet was created to the specified end time. Format is a number expressed in Unix time as milliseconds (ex: \"1469498468.057\")."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "The most recent date to retrieve event logs for. If no end time is specified, this call returns entries from the specified start time up to the present. Format is a number expressed in Unix time as milliseconds (ex: \"1469498468.057\")."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value."]}
    let context_ = "DescribeFleetEventsInput"
    let make ?startTime =
      fun ?endTime ->
        fun ?limit ->
          fun ?nextToken ->
            fun ~fleetId ->
              fun () -> { startTime; endTime; limit; nextToken; fleetId }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("StartTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("EndTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "StartTime") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?nextToken ?limit ?endTime ?startTime ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let endTime = field_map json__ "EndTime" Timestamp.of_json in
      let startTime = field_map json__ "StartTime" Timestamp.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?nextToken ?limit ?endTime ?startTime ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves entries from a fleet's event log. Fleet events are initiated by changes in status, such as during fleet creation and termination, changes in capacity, etc. If a fleet has multiple locations, events are also initiated by changes to status and capacity in remote locations. You can specify a time range to limit the result set. Use the pagination parameters to retrieve results as a set of sequential pages. If successful, a collection of event log entries matching the request are returned. Learn more Setting up Amazon GameLift Servers fleets"]
module DescribeFleetDeploymentOutput =
  struct
    type nonrec t =
      {
      fleetDeployment: FleetDeployment.t option
        [@ocaml.doc "The requested deployment information."];
      locationalDeployments: LocationalDeployments.t option
        [@ocaml.doc
          "If the deployment is for a multi-location fleet, the requests returns the deployment status in each fleet location."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetDeployment =
      fun ?locationalDeployments ->
        fun () -> { fleetDeployment; locationalDeployments }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetDeployment",
           (Option.map x.fleetDeployment ~f:FleetDeployment.to_value));
        ("LocationalDeployments",
          (Option.map x.locationalDeployments
             ~f:LocationalDeployments.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let locationalDeployments =
        (Option.map ~f:LocationalDeployments.of_xml)
          (Xml.child xml_arg0 "LocationalDeployments") in
      let fleetDeployment =
        (Option.map ~f:FleetDeployment.of_xml)
          (Xml.child xml_arg0 "FleetDeployment") in
      make ?locationalDeployments ?fleetDeployment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let locationalDeployments =
        field_map json__ "LocationalDeployments"
          LocationalDeployments.of_json in
      let fleetDeployment =
        field_map json__ "FleetDeployment" FleetDeployment.of_json in
      make ?locationalDeployments ?fleetDeployment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves information about a managed container fleet deployment. Request options Get information about the latest deployment for a specific fleet. Provide the fleet ID or ARN. Get information about a specific deployment. Provide the fleet ID or ARN and the deployment ID. Results If successful, a FleetDeployment object is returned."]
module DescribeFleetDeploymentInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the container fleet. You can use either the fleet ID or ARN value."];
      deploymentId: DeploymentId.t option
        [@ocaml.doc
          "A unique identifier for the deployment to return information for."]}
    let context_ = "DescribeFleetDeploymentInput"
    let make ?deploymentId =
      fun ~fleetId -> fun () -> { deploymentId; fleetId }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("DeploymentId",
          (Option.map x.deploymentId ~f:DeploymentId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deploymentId =
        (Option.map ~f:DeploymentId.of_xml)
          (Xml.child xml_arg0 "DeploymentId") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?deploymentId ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deploymentId = field_map json__ "DeploymentId" DeploymentId.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?deploymentId ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves information about a managed container fleet deployment. Request options Get information about the latest deployment for a specific fleet. Provide the fleet ID or ARN. Get information about a specific deployment. Provide the fleet ID or ARN and the deployment ID. Results If successful, a FleetDeployment object is returned."]
module DescribeFleetCapacityOutput =
  struct
    type nonrec t =
      {
      fleetCapacity: FleetCapacityList.t option
        [@ocaml.doc
          "A collection of objects that contains capacity information for each requested fleet ID. Capacity objects are returned only for fleets that currently exist. Changes in desired instance value can take up to 1 minute to be reflected."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetCapacity =
      fun ?nextToken -> fun () -> { fleetCapacity; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetCapacity",
           (Option.map x.fleetCapacity ~f:FleetCapacityList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let fleetCapacity =
        (Option.map ~f:FleetCapacityList.of_xml)
          (Xml.child xml_arg0 "FleetCapacity") in
      make ?nextToken ?fleetCapacity ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let fleetCapacity =
        field_map json__ "FleetCapacity" FleetCapacityList.of_json in
      make ?nextToken ?fleetCapacity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Retrieves the resource capacity settings for one or more fleets. For a container fleet, this operation also returns counts for game server container groups. With multi-location fleets, this operation retrieves data for the fleet's home Region only. To retrieve capacity for remote locations, see https://docs.aws.amazon.com/gamelift/latest/apireference/API_DescribeFleetLocationCapacity.html. This operation can be used in the following ways: To get capacity data for one or more specific fleets, provide a list of fleet IDs or fleet ARNs. To get capacity data for all fleets, do not provide a fleet identifier. When requesting multiple fleets, use the pagination parameters to retrieve results as a set of sequential pages. If successful, a FleetCapacity object is returned for each requested fleet ID. Each FleetCapacity object includes a Location property, which is set to the fleet's home Region. Capacity values are returned only for fleets that currently exist. Some API operations may limit the number of fleet IDs that are allowed in one request. If a request exceeds this limit, the request fails and the error message includes the maximum allowed. Learn more Setting up Amazon GameLift Servers fleets GameLift metrics for fleets"]
module DescribeFleetCapacityInput =
  struct
    type nonrec t =
      {
      fleetIds: FleetIdOrArnList.t option
        [@ocaml.doc
          "A unique identifier for the fleet to retrieve capacity information for. You can use either the fleet ID or ARN value. Leave this parameter empty to retrieve capacity information for all fleets."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages. This parameter is ignored when the request specifies one or a list of fleet IDs."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value. This parameter is ignored when the request specifies one or a list of fleet IDs."]}
    let make ?fleetIds =
      fun ?limit ->
        fun ?nextToken -> fun () -> { fleetIds; limit; nextToken }
    let to_value x =
      structure_to_value
        [("FleetIds", (Option.map x.fleetIds ~f:FleetIdOrArnList.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let fleetIds =
        (Option.map ~f:FleetIdOrArnList.of_xml)
          (Xml.child xml_arg0 "FleetIds") in
      make ?nextToken ?limit ?fleetIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let fleetIds = field_map json__ "FleetIds" FleetIdOrArnList.of_json in
      make ?nextToken ?limit ?fleetIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Retrieves the resource capacity settings for one or more fleets. For a container fleet, this operation also returns counts for game server container groups. With multi-location fleets, this operation retrieves data for the fleet's home Region only. To retrieve capacity for remote locations, see https://docs.aws.amazon.com/gamelift/latest/apireference/API_DescribeFleetLocationCapacity.html. This operation can be used in the following ways: To get capacity data for one or more specific fleets, provide a list of fleet IDs or fleet ARNs. To get capacity data for all fleets, do not provide a fleet identifier. When requesting multiple fleets, use the pagination parameters to retrieve results as a set of sequential pages. If successful, a FleetCapacity object is returned for each requested fleet ID. Each FleetCapacity object includes a Location property, which is set to the fleet's home Region. Capacity values are returned only for fleets that currently exist. Some API operations may limit the number of fleet IDs that are allowed in one request. If a request exceeds this limit, the request fails and the error message includes the maximum allowed. Learn more Setting up Amazon GameLift Servers fleets GameLift metrics for fleets"]
module DescribeFleetAttributesOutput =
  struct
    type nonrec t =
      {
      fleetAttributes: FleetAttributesList.t option
        [@ocaml.doc
          "A collection of objects containing attribute metadata for each requested fleet ID. Attribute objects are returned only for fleets that currently exist."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates where to resume retrieving results on the next call to this operation. If no token is returned, these results represent the end of the list."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetAttributes =
      fun ?nextToken -> fun () -> { fleetAttributes; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetAttributes",
           (Option.map x.fleetAttributes ~f:FleetAttributesList.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let fleetAttributes =
        (Option.map ~f:FleetAttributesList.of_xml)
          (Xml.child xml_arg0 "FleetAttributes") in
      make ?nextToken ?fleetAttributes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let fleetAttributes =
        field_map json__ "FleetAttributes" FleetAttributesList.of_json in
      make ?nextToken ?fleetAttributes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere Retrieves core fleet-wide properties for fleets in an Amazon Web Services Region. Properties include the computing hardware and deployment configuration for instances in the fleet. You can use this operation in the following ways: To get attributes for specific fleets, provide a list of fleet IDs or fleet ARNs. To get attributes for all fleets, do not provide a fleet identifier. When requesting attributes for multiple fleets, use the pagination parameters to retrieve results as a set of sequential pages. If successful, a FleetAttributes object is returned for each fleet requested, unless the fleet identifier is not found. Some API operations limit the number of fleet IDs that allowed in one request. If a request exceeds this limit, the request fails and the error message contains the maximum allowed number. Learn more Setting up Amazon GameLift Servers fleets"]
module DescribeFleetAttributesInput =
  struct
    type nonrec t =
      {
      fleetIds: FleetIdOrArnList.t option
        [@ocaml.doc
          "A list of unique fleet identifiers to retrieve attributes for. You can use either the fleet ID or ARN value. To retrieve attributes for all current fleets, do not include this parameter."];
      limit: PositiveInteger.t option
        [@ocaml.doc
          "The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages. This parameter is ignored when the request specifies one or a list of fleet IDs."];
      nextToken: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A token that indicates the start of the next sequential page of results. Use the token that is returned with a previous call to this operation. To start at the beginning of the result set, do not specify a value. This parameter is ignored when the request specifies one or a list of fleet IDs."]}
    let make ?fleetIds =
      fun ?limit ->
        fun ?nextToken -> fun () -> { fleetIds; limit; nextToken }
    let to_value x =
      structure_to_value
        [("FleetIds", (Option.map x.fleetIds ~f:FleetIdOrArnList.to_value));
        ("Limit", (Option.map x.limit ~f:PositiveInteger.to_value));
        ("NextToken",
          (Option.map x.nextToken ~f:NonZeroAndMaxString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let limit =
        (Option.map ~f:PositiveInteger.of_xml) (Xml.child xml_arg0 "Limit") in
      let fleetIds =
        (Option.map ~f:FleetIdOrArnList.of_xml)
          (Xml.child xml_arg0 "FleetIds") in
      make ?nextToken ?limit ?fleetIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken =
        field_map json__ "NextToken" NonZeroAndMaxString.of_json in
      let limit = field_map json__ "Limit" PositiveInteger.of_json in
      let fleetIds = field_map json__ "FleetIds" FleetIdOrArnList.of_json in
      make ?nextToken ?limit ?fleetIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere Retrieves core fleet-wide properties for fleets in an Amazon Web Services Region. Properties include the computing hardware and deployment configuration for instances in the fleet. You can use this operation in the following ways: To get attributes for specific fleets, provide a list of fleet IDs or fleet ARNs. To get attributes for all fleets, do not provide a fleet identifier. When requesting attributes for multiple fleets, use the pagination parameters to retrieve results as a set of sequential pages. If successful, a FleetAttributes object is returned for each fleet requested, unless the fleet identifier is not found. Some API operations limit the number of fleet IDs that allowed in one request. If a request exceeds this limit, the request fails and the error message contains the maximum allowed number. Learn more Setting up Amazon GameLift Servers fleets"]
module DescribeEC2InstanceLimitsOutput =
  struct
    type nonrec t =
      {
      eC2InstanceLimits: EC2InstanceLimitList.t option
        [@ocaml.doc
          "The maximum number of instances for the specified instance type."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?eC2InstanceLimits = fun () -> { eC2InstanceLimits }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("EC2InstanceLimits",
           (Option.map x.eC2InstanceLimits ~f:EC2InstanceLimitList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eC2InstanceLimits =
        (Option.map ~f:EC2InstanceLimitList.of_xml)
          (Xml.child xml_arg0 "EC2InstanceLimits") in
      make ?eC2InstanceLimits ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let eC2InstanceLimits =
        field_map json__ "EC2InstanceLimits" EC2InstanceLimitList.of_json in
      make ?eC2InstanceLimits ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves the instance limits and current utilization for an Amazon Web Services Region or location. Instance limits control the number of instances, per instance type, per location, that your Amazon Web Services account can use. Learn more at Amazon EC2 Instance Types. The information returned includes the maximum number of instances allowed and your account's current usage across all fleets. This information can affect your ability to scale your Amazon GameLift Servers fleets. You can request a limit increase for your account by using the Service limits page in the Amazon GameLift Servers console. Instance limits differ based on whether the instances are deployed in a fleet's home Region or in a remote location. For remote locations, limits also differ based on the combination of home Region and remote location. All requests must specify an Amazon Web Services Region (either explicitly or as your default settings). To get the limit for a remote location, you must also specify the location. To learn more about how Amazon GameLift Servers handles locations, see Amazon GameLift Servers service locations. For example, the following requests all return different results: Request specifies the Region ap-northeast-1 with no location. The result is limits and usage data on all of the fleets that reside in ap-northeast-1, for all instance types that are deployed in ap-northeast-1. Request specifies the Region ap-northeast-1 with location us-west-2. The result is limits and usage data on all of the fleets that reside in ap-northeast-1, for all instance types that are deployed in us-west-2. Request specifies the Region us-east-1 with location ap-northeast-1. The result is limits and usage data on all of the fleets that reside in us-east-1, for all instance types that are deployed in ap-northeast-1. These limits do not affect fleets in any other Regions that deploy instances to ap-northeast-1. This operation can be used in the following ways: To get limit and usage data for all instance types that are deployed in an Amazon Web Services Region by fleets that reside in the same Region: Specify the Region only. Optionally, specify a single instance type to retrieve information for. To get limit and usage data for all instance types that are deployed to a remote location by fleets that reside in different Amazon Web Services Region: Provide both the Amazon Web Services Region and the remote location. Optionally, specify a single instance type to retrieve information for. If successful, an EC2InstanceLimits object is returned with limits and usage data for each requested instance type. Learn more Setting up Amazon GameLift Servers fleets"]
module DescribeEC2InstanceLimitsInput =
  struct
    type nonrec t =
      {
      eC2InstanceType: EC2InstanceType.t option
        [@ocaml.doc
          "Name of an Amazon EC2 instance type that is supported in Amazon GameLift Servers. A fleet instance type determines the computing resources of each instance in the fleet, including CPU, memory, storage, and networking capacity. Do not specify a value for this parameter to retrieve limits for all instance types."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The name of a remote location to request instance limits for, in the form of an Amazon Web Services Region code such as us-west-2."]}
    let make ?eC2InstanceType =
      fun ?location -> fun () -> { eC2InstanceType; location }
    let to_value x =
      structure_to_value
        [("EC2InstanceType",
           (Option.map x.eC2InstanceType ~f:EC2InstanceType.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let eC2InstanceType =
        (Option.map ~f:EC2InstanceType.of_xml)
          (Xml.child xml_arg0 "EC2InstanceType") in
      make ?location ?eC2InstanceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let eC2InstanceType =
        field_map json__ "EC2InstanceType" EC2InstanceType.of_json in
      make ?location ?eC2InstanceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves the instance limits and current utilization for an Amazon Web Services Region or location. Instance limits control the number of instances, per instance type, per location, that your Amazon Web Services account can use. Learn more at Amazon EC2 Instance Types. The information returned includes the maximum number of instances allowed and your account's current usage across all fleets. This information can affect your ability to scale your Amazon GameLift Servers fleets. You can request a limit increase for your account by using the Service limits page in the Amazon GameLift Servers console. Instance limits differ based on whether the instances are deployed in a fleet's home Region or in a remote location. For remote locations, limits also differ based on the combination of home Region and remote location. All requests must specify an Amazon Web Services Region (either explicitly or as your default settings). To get the limit for a remote location, you must also specify the location. To learn more about how Amazon GameLift Servers handles locations, see Amazon GameLift Servers service locations. For example, the following requests all return different results: Request specifies the Region ap-northeast-1 with no location. The result is limits and usage data on all of the fleets that reside in ap-northeast-1, for all instance types that are deployed in ap-northeast-1. Request specifies the Region ap-northeast-1 with location us-west-2. The result is limits and usage data on all of the fleets that reside in ap-northeast-1, for all instance types that are deployed in us-west-2. Request specifies the Region us-east-1 with location ap-northeast-1. The result is limits and usage data on all of the fleets that reside in us-east-1, for all instance types that are deployed in ap-northeast-1. These limits do not affect fleets in any other Regions that deploy instances to ap-northeast-1. This operation can be used in the following ways: To get limit and usage data for all instance types that are deployed in an Amazon Web Services Region by fleets that reside in the same Region: Specify the Region only. Optionally, specify a single instance type to retrieve information for. To get limit and usage data for all instance types that are deployed to a remote location by fleets that reside in different Amazon Web Services Region: Provide both the Amazon Web Services Region and the remote location. Optionally, specify a single instance type to retrieve information for. If successful, an EC2InstanceLimits object is returned with limits and usage data for each requested instance type. Learn more Setting up Amazon GameLift Servers fleets"]
module DescribeContainerGroupPortMappingsOutput =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t option
        [@ocaml.doc "A unique identifier for the container fleet."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "The location of the fleet instance, expressed as an Amazon Web Services Region code, such as us-west-2."];
      containerGroupDefinitionArn: ContainerGroupDefinitionArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to the container group definition. The ARN value also identifies the specific container group definition version in use."];
      containerGroupType: ContainerGroupType.t option
        [@ocaml.doc
          "The type of container group that was specified in the request. Valid values are GAME_SERVER or PER_INSTANCE."];
      computeName: ComputeName.t option
        [@ocaml.doc
          "A unique identifier for the compute resource running the game server container group. Returned when ContainerGroupType is GAME_SERVER."];
      instanceId: InstanceId.t option
        [@ocaml.doc
          "A unique identifier for the fleet instance. For GAME_SERVER requests, this is the instance running the specified compute. For PER_INSTANCE requests, this is the instance specified in the request."];
      containerGroupPortMappings: ContainerGroupPortMappingList.t option
        [@ocaml.doc
          "A list of ContainerGroupPortMapping objects that describe the port mappings for each container in the container group."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetId =
      fun ?location ->
        fun ?containerGroupDefinitionArn ->
          fun ?containerGroupType ->
            fun ?computeName ->
              fun ?instanceId ->
                fun ?containerGroupPortMappings ->
                  fun () ->
                    {
                      fleetId;
                      location;
                      containerGroupDefinitionArn;
                      containerGroupType;
                      computeName;
                      instanceId;
                      containerGroupPortMappings
                    }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetId.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value));
        ("ContainerGroupDefinitionArn",
          (Option.map x.containerGroupDefinitionArn
             ~f:ContainerGroupDefinitionArn.to_value));
        ("ContainerGroupType",
          (Option.map x.containerGroupType ~f:ContainerGroupType.to_value));
        ("ComputeName", (Option.map x.computeName ~f:ComputeName.to_value));
        ("InstanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("ContainerGroupPortMappings",
          (Option.map x.containerGroupPortMappings
             ~f:ContainerGroupPortMappingList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerGroupPortMappings =
        (Option.map ~f:ContainerGroupPortMappingList.of_xml)
          (Xml.child xml_arg0 "ContainerGroupPortMappings") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "InstanceId") in
      let computeName =
        (Option.map ~f:ComputeName.of_xml) (Xml.child xml_arg0 "ComputeName") in
      let containerGroupType =
        (Option.map ~f:ContainerGroupType.of_xml)
          (Xml.child xml_arg0 "ContainerGroupType") in
      let containerGroupDefinitionArn =
        (Option.map ~f:ContainerGroupDefinitionArn.of_xml)
          (Xml.child xml_arg0 "ContainerGroupDefinitionArn") in
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let fleetId =
        (Option.map ~f:FleetId.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?containerGroupPortMappings ?instanceId ?computeName
        ?containerGroupType ?containerGroupDefinitionArn ?location ?fleetId
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let containerGroupPortMappings =
        field_map json__ "ContainerGroupPortMappings"
          ContainerGroupPortMappingList.of_json in
      let instanceId = field_map json__ "InstanceId" InstanceId.of_json in
      let computeName = field_map json__ "ComputeName" ComputeName.of_json in
      let containerGroupType =
        field_map json__ "ContainerGroupType" ContainerGroupType.of_json in
      let containerGroupDefinitionArn =
        field_map json__ "ContainerGroupDefinitionArn"
          ContainerGroupDefinitionArn.of_json in
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let fleetId = field_map json__ "FleetId" FleetId.of_json in
      make ?containerGroupPortMappings ?instanceId ?computeName
        ?containerGroupType ?containerGroupDefinitionArn ?location ?fleetId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves the port mappings for a container group running on a container fleet. Port mappings show how container ports are mapped to connection ports on the fleet instance. Use this operation to find the connection port for a specific container on a fleet instance. Request options Get port mappings for a game server container group. Provide the fleet ID, set ContainerGroupType to GAME_SERVER, and specify the ComputeName for the game server container group. Get port mappings for a per-instance container group. Provide the fleet ID, set ContainerGroupType to PER_INSTANCE, and specify the InstanceId for the instance. Optionally filter results to a single container by providing a ContainerName. Results This operation returns the fleet ID, location, container group definition ARN, container group type, compute name (for game server container groups), instance ID, and a list of ContainerGroupPortMapping objects. Each object contains the container name, runtime ID, and a list of port mappings that show how container ports map to connection ports on the instance. Learn more Connect to containers Create a container group definition"]
module DescribeContainerGroupPortMappingsInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the container fleet. You can use either the fleet ID or ARN value."];
      containerGroupType: ContainerGroupType.t
        [@ocaml.doc
          "The type of container group to retrieve port mappings for. GAME_SERVER -- Get port mappings for a game server container group. PER_INSTANCE -- Get port mappings for a per-instance container group."];
      computeName: ComputeNameOrArn.t option
        [@ocaml.doc
          "A unique identifier for the compute resource for which to retrieve port mappings. For a container fleet, a compute represents a game server container group running on a fleet instance. You can use either the compute name or ARN value. When ContainerGroupType is GAME_SERVER, this parameter is required. When ContainerGroupType is PER_INSTANCE, do not provide this parameter. If you provide a compute name with PER_INSTANCE, the request fails with an InvalidRequestException."];
      instanceId: InstanceId.t option
        [@ocaml.doc
          "A unique identifier for the fleet instance to retrieve port mappings for. When ContainerGroupType is PER_INSTANCE, this parameter is required. When ContainerGroupType is GAME_SERVER, this parameter is optional. If you provide an instance ID, it must match the instance that's running the specified compute. If the instance ID doesn't match, the request fails with an InvalidRequestException."];
      containerName: NonZeroAnd128MaxAsciiString.t option
        [@ocaml.doc
          "A container name to filter the results. When provided, the operation returns port mappings for the specified container only. If no container with the specified name exists in the container group, the request fails with a NotFoundException. If not provided, the operation returns port mappings for all containers in the container group."]}
    let context_ = "DescribeContainerGroupPortMappingsInput"
    let make ?computeName =
      fun ?instanceId ->
        fun ?containerName ->
          fun ~fleetId ->
            fun ~containerGroupType ->
              fun () ->
                {
                  computeName;
                  instanceId;
                  containerName;
                  fleetId;
                  containerGroupType
                }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("ContainerGroupType",
          (Some (ContainerGroupType.to_value x.containerGroupType)));
        ("ComputeName",
          (Option.map x.computeName ~f:ComputeNameOrArn.to_value));
        ("InstanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("ContainerName",
          (Option.map x.containerName ~f:NonZeroAnd128MaxAsciiString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerName =
        (Option.map ~f:NonZeroAnd128MaxAsciiString.of_xml)
          (Xml.child xml_arg0 "ContainerName") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "InstanceId") in
      let computeName =
        (Option.map ~f:ComputeNameOrArn.of_xml)
          (Xml.child xml_arg0 "ComputeName") in
      let containerGroupType =
        ContainerGroupType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerGroupType") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ?containerName ?instanceId ?computeName ~containerGroupType
        ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let containerName =
        field_map json__ "ContainerName" NonZeroAnd128MaxAsciiString.of_json in
      let instanceId = field_map json__ "InstanceId" InstanceId.of_json in
      let computeName =
        field_map json__ "ComputeName" ComputeNameOrArn.of_json in
      let containerGroupType =
        field_map_exn json__ "ContainerGroupType" ContainerGroupType.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ?containerName ?instanceId ?computeName ~containerGroupType
        ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves the port mappings for a container group running on a container fleet. Port mappings show how container ports are mapped to connection ports on the fleet instance. Use this operation to find the connection port for a specific container on a fleet instance. Request options Get port mappings for a game server container group. Provide the fleet ID, set ContainerGroupType to GAME_SERVER, and specify the ComputeName for the game server container group. Get port mappings for a per-instance container group. Provide the fleet ID, set ContainerGroupType to PER_INSTANCE, and specify the InstanceId for the instance. Optionally filter results to a single container by providing a ContainerName. Results This operation returns the fleet ID, location, container group definition ARN, container group type, compute name (for game server container groups), instance ID, and a list of ContainerGroupPortMapping objects. Each object contains the container name, runtime ID, and a list of port mappings that show how container ports map to connection ports on the instance. Learn more Connect to containers Create a container group definition"]
module DescribeContainerGroupDefinitionOutput =
  struct
    type nonrec t =
      {
      containerGroupDefinition: ContainerGroupDefinition.t option
        [@ocaml.doc
          "The properties of the requested container group definition resource."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerGroupDefinition =
      fun () -> { containerGroupDefinition }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ContainerGroupDefinition",
           (Option.map x.containerGroupDefinition
              ~f:ContainerGroupDefinition.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerGroupDefinition =
        (Option.map ~f:ContainerGroupDefinition.of_xml)
          (Xml.child xml_arg0 "ContainerGroupDefinition") in
      make ?containerGroupDefinition ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let containerGroupDefinition =
        field_map json__ "ContainerGroupDefinition"
          ContainerGroupDefinition.of_json in
      make ?containerGroupDefinition ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves the properties of a container group definition, including all container definitions in the group. Request options: Retrieve the latest version of a container group definition. Specify the container group definition name only, or use an ARN value without a version number. Retrieve a particular version. Specify the container group definition name and a version number, or use an ARN value that includes the version number. Results: If successful, this operation returns the complete properties of a container group definition version. Learn more Manage a container group definition"]
module DescribeContainerGroupDefinitionInput =
  struct
    type nonrec t =
      {
      name: ContainerGroupDefinitionNameOrArn.t
        [@ocaml.doc
          "The unique identifier for the container group definition to retrieve properties for. You can use either the Name or ARN value."];
      versionNumber: PositiveInteger.t option
        [@ocaml.doc "The specific version to retrieve."]}
    let context_ = "DescribeContainerGroupDefinitionInput"
    let make ?versionNumber = fun ~name -> fun () -> { versionNumber; name }
    let to_value x =
      structure_to_value
        [("Name", (Some (ContainerGroupDefinitionNameOrArn.to_value x.name)));
        ("VersionNumber",
          (Option.map x.versionNumber ~f:PositiveInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let versionNumber =
        (Option.map ~f:PositiveInteger.of_xml)
          (Xml.child xml_arg0 "VersionNumber") in
      let name =
        ContainerGroupDefinitionNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?versionNumber ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let versionNumber =
        field_map json__ "VersionNumber" PositiveInteger.of_json in
      let name =
        field_map_exn json__ "Name" ContainerGroupDefinitionNameOrArn.of_json in
      make ?versionNumber ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves the properties of a container group definition, including all container definitions in the group. Request options: Retrieve the latest version of a container group definition. Specify the container group definition name only, or use an ARN value without a version number. Retrieve a particular version. Specify the container group definition name and a version number, or use an ARN value that includes the version number. Results: If successful, this operation returns the complete properties of a container group definition version. Learn more Manage a container group definition"]
module DescribeContainerFleetOutput =
  struct
    type nonrec t =
      {
      containerFleet: ContainerFleet.t option
        [@ocaml.doc
          "The properties for the requested container fleet, including current status."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerFleet = fun () -> { containerFleet }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ContainerFleet",
           (Option.map x.containerFleet ~f:ContainerFleet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerFleet =
        (Option.map ~f:ContainerFleet.of_xml)
          (Xml.child xml_arg0 "ContainerFleet") in
      make ?containerFleet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let containerFleet =
        field_map json__ "ContainerFleet" ContainerFleet.of_json in
      make ?containerFleet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves the properties for a container fleet. When requesting attributes for multiple fleets, use the pagination parameters to retrieve results as a set of sequential pages. Request options Get container fleet properties for a single fleet. Provide either the fleet ID or ARN value. Results If successful, a ContainerFleet object is returned. This object includes the fleet properties, including information about the most recent deployment. Some API operations limit the number of fleet IDs that allowed in one request. If a request exceeds this limit, the request fails and the error message contains the maximum allowed number."]
module DescribeContainerFleetInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the container fleet to retrieve. You can use either the fleet ID or ARN value."]}
    let context_ = "DescribeContainerFleetInput"
    let make ~fleetId = fun () -> { fleetId }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Retrieves the properties for a container fleet. When requesting attributes for multiple fleets, use the pagination parameters to retrieve results as a set of sequential pages. Request options Get container fleet properties for a single fleet. Provide either the fleet ID or ARN value. Results If successful, a ContainerFleet object is returned. This object includes the fleet properties, including information about the most recent deployment. Some API operations limit the number of fleet IDs that allowed in one request. If a request exceeds this limit, the request fails and the error message contains the maximum allowed number."]
module DescribeComputeOutput =
  struct
    type nonrec t =
      {
      compute: Compute.t option
        [@ocaml.doc
          "The set of properties for the requested compute resource."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?compute = fun () -> { compute }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Compute", (Option.map x.compute ~f:Compute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let compute =
        (Option.map ~f:Compute.of_xml) (Xml.child xml_arg0 "Compute") in
      make ?compute ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let compute = field_map json__ "Compute" Compute.of_json in
      make ?compute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves properties for a specific compute resource in an Amazon GameLift Servers fleet. You can list all computes in a fleet by calling ListCompute. Request options Provide the fleet ID and compute name. The compute name varies depending on the type of fleet. For a compute in a managed EC2 fleet, provide an instance ID. Each instance in the fleet is a compute. For a compute in a managed container fleet, provide a compute name. In a container fleet, each game server container group on a fleet instance is assigned a compute name. For a compute in an Anywhere fleet, provide a registered compute name. Anywhere fleet computes are created when you register a hosting resource with the fleet. Results If successful, this operation returns details for the requested compute resource. Depending on the fleet's compute type, the result includes the following information: For a managed EC2 fleet, this operation returns information about the EC2 instance. For an Anywhere fleet, this operation returns information about the registered compute."]
module DescribeComputeInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet that the compute belongs to. You can use either the fleet ID or ARN value."];
      computeName: ComputeNameOrArn.t
        [@ocaml.doc
          "The unique identifier of the compute resource to retrieve properties for. For a managed container fleet or Anywhere fleet, use a compute name. For an EC2 fleet, use an instance ID. To retrieve a fleet's compute identifiers, call ListCompute."]}
    let context_ = "DescribeComputeInput"
    let make ~fleetId =
      fun ~computeName -> fun () -> { fleetId; computeName }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("ComputeName", (Some (ComputeNameOrArn.to_value x.computeName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let computeName =
        ComputeNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ComputeName") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~computeName ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let computeName =
        field_map_exn json__ "ComputeName" ComputeNameOrArn.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~computeName ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves properties for a specific compute resource in an Amazon GameLift Servers fleet. You can list all computes in a fleet by calling ListCompute. Request options Provide the fleet ID and compute name. The compute name varies depending on the type of fleet. For a compute in a managed EC2 fleet, provide an instance ID. Each instance in the fleet is a compute. For a compute in a managed container fleet, provide a compute name. In a container fleet, each game server container group on a fleet instance is assigned a compute name. For a compute in an Anywhere fleet, provide a registered compute name. Anywhere fleet computes are created when you register a hosting resource with the fleet. Results If successful, this operation returns details for the requested compute resource. Depending on the fleet's compute type, the result includes the following information: For a managed EC2 fleet, this operation returns information about the EC2 instance. For an Anywhere fleet, this operation returns information about the registered compute."]
module DescribeBuildOutput =
  struct
    type nonrec t =
      {
      build: Build.t option
        [@ocaml.doc "Set of properties describing the requested build."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?build = fun () -> { build }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Build", (Option.map x.build ~f:Build.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let build = (Option.map ~f:Build.of_xml) (Xml.child xml_arg0 "Build") in
      make ?build ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let build = field_map json__ "Build" Build.of_json in make ?build ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves properties for a custom game build. To request a build resource, specify a build ID. If successful, an object containing the build properties is returned. Learn more Upload a Custom Server Build All APIs by task"]
module DescribeBuildInput =
  struct
    type nonrec t =
      {
      buildId: BuildIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the build to retrieve properties for. You can use either the build ID or ARN value."]}
    let context_ = "DescribeBuildInput"
    let make ~buildId = fun () -> { buildId }
    let to_value x =
      structure_to_value
        [("BuildId", (Some (BuildIdOrArn.to_value x.buildId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let buildId =
        BuildIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BuildId") in
      make ~buildId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let buildId = field_map_exn json__ "BuildId" BuildIdOrArn.of_json in
      make ~buildId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Retrieves properties for a custom game build. To request a build resource, specify a build ID. If successful, an object containing the build properties is returned. Learn more Upload a Custom Server Build All APIs by task"]
module DescribeAliasOutput =
  struct
    type nonrec t =
      {
      alias: Alias.t option [@ocaml.doc "The requested alias resource."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?alias = fun () -> { alias }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Alias", (Option.map x.alias ~f:Alias.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let alias = (Option.map ~f:Alias.of_xml) (Xml.child xml_arg0 "Alias") in
      make ?alias ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let alias = field_map json__ "Alias" Alias.of_json in make ?alias ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves properties for an alias. This operation returns all alias metadata and settings. To get an alias's target fleet ID only, use ResolveAlias. To get alias properties, specify the alias ID. If successful, the requested alias record is returned. Related actions All APIs by task"]
module DescribeAliasInput =
  struct
    type nonrec t =
      {
      aliasId: AliasIdOrArn.t
        [@ocaml.doc
          "The unique identifier for the fleet alias that you want to retrieve. You can use either the alias ID or ARN value."]}
    let context_ = "DescribeAliasInput"
    let make ~aliasId = fun () -> { aliasId }
    let to_value x =
      structure_to_value
        [("AliasId", (Some (AliasIdOrArn.to_value x.aliasId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let aliasId =
        AliasIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AliasId") in
      make ~aliasId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let aliasId = field_map_exn json__ "AliasId" AliasIdOrArn.of_json in
      make ~aliasId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Retrieves properties for an alias. This operation returns all alias metadata and settings. To get an alias's target fleet ID only, use ResolveAlias. To get alias properties, specify the alias ID. If successful, the requested alias record is returned. Related actions All APIs by task"]
module DeregisterGameServerInput =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupNameOrArn.t
        [@ocaml.doc
          "A unique identifier for the game server group where the game server is running."];
      gameServerId: GameServerId.t
        [@ocaml.doc
          "A custom string that uniquely identifies the game server to deregister."]}
    let context_ = "DeregisterGameServerInput"
    let make ~gameServerGroupName =
      fun ~gameServerId -> fun () -> { gameServerGroupName; gameServerId }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Some (GameServerGroupNameOrArn.to_value x.gameServerGroupName)));
        ("GameServerId", (Some (GameServerId.to_value x.gameServerId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServerId =
        GameServerId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerId") in
      let gameServerGroupName =
        GameServerGroupNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerGroupName") in
      make ~gameServerId ~gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServerId =
        field_map_exn json__ "GameServerId" GameServerId.of_json in
      let gameServerGroupName =
        field_map_exn json__ "GameServerGroupName"
          GameServerGroupNameOrArn.of_json in
      make ~gameServerId ~gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Removes the game server from a game server group. As a result of this operation, the deregistered game server can no longer be claimed and will not be returned in a list of active game servers. To deregister a game server, specify the game server group and game server ID. If successful, this operation emits a CloudWatch event with termination timestamp and reason. Learn more Amazon GameLift Servers FleetIQ Guide"]
module DeregisterComputeOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Anywhere Removes a compute resource from an Anywhere fleet. Deregistered computes can no longer host game sessions through Amazon GameLift Servers. Use this operation with an Anywhere fleet that doesn't use the Amazon GameLift Servers Agent For Anywhere fleets with the Agent, the Agent handles all compute registry tasks for you. To deregister a compute, call this operation from the compute that's being deregistered and specify the compute name and the fleet ID."]
module DeregisterComputeInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet the compute resource is currently registered to."];
      computeName: ComputeNameOrArn.t
        [@ocaml.doc
          "The unique identifier of the compute resource to deregister. For an Anywhere fleet compute, use the registered compute name."]}
    let context_ = "DeregisterComputeInput"
    let make ~fleetId =
      fun ~computeName -> fun () -> { fleetId; computeName }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("ComputeName", (Some (ComputeNameOrArn.to_value x.computeName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let computeName =
        ComputeNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ComputeName") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~computeName ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let computeName =
        field_map_exn json__ "ComputeName" ComputeNameOrArn.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~computeName ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Anywhere Removes a compute resource from an Anywhere fleet. Deregistered computes can no longer host game sessions through Amazon GameLift Servers. Use this operation with an Anywhere fleet that doesn't use the Amazon GameLift Servers Agent For Anywhere fleets with the Agent, the Agent handles all compute registry tasks for you. To deregister a compute, call this operation from the compute that's being deregistered and specify the compute name and the fleet ID."]
module DeleteVpcPeeringConnectionOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Removes a VPC peering connection. To delete the connection, you must have a valid authorization for the VPC peering connection that you want to delete.. Once a valid authorization exists, call this operation from the Amazon Web Services account that is used to manage the Amazon GameLift Servers fleets. Identify the connection to delete by the connection ID and fleet ID. If successful, the connection is removed. Related actions All APIs by task"]
module DeleteVpcPeeringConnectionInput =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t
        [@ocaml.doc
          "A unique identifier for the fleet. This fleet specified must match the fleet referenced in the VPC peering connection record. You can use either the fleet ID or ARN value."];
      vpcPeeringConnectionId: NonZeroAndMaxString.t
        [@ocaml.doc "A unique identifier for a VPC peering connection."]}
    let context_ = "DeleteVpcPeeringConnectionInput"
    let make ~fleetId =
      fun ~vpcPeeringConnectionId ->
        fun () -> { fleetId; vpcPeeringConnectionId }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetId.to_value x.fleetId)));
        ("VpcPeeringConnectionId",
          (Some (NonZeroAndMaxString.to_value x.vpcPeeringConnectionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringConnectionId =
        NonZeroAndMaxString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpcPeeringConnectionId") in
      let fleetId =
        FleetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~vpcPeeringConnectionId ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcPeeringConnectionId =
        field_map_exn json__ "VpcPeeringConnectionId"
          NonZeroAndMaxString.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetId.of_json in
      make ~vpcPeeringConnectionId ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Removes a VPC peering connection. To delete the connection, you must have a valid authorization for the VPC peering connection that you want to delete.. Once a valid authorization exists, call this operation from the Amazon Web Services account that is used to manage the Amazon GameLift Servers fleets. Identify the connection to delete by the connection ID and fleet ID. If successful, the connection is removed. Related actions All APIs by task"]
module DeleteVpcPeeringAuthorizationOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Cancels a pending VPC peering authorization for the specified VPC. If you need to delete an existing VPC peering connection, use DeleteVpcPeeringConnection. Related actions All APIs by task"]
module DeleteVpcPeeringAuthorizationInput =
  struct
    type nonrec t =
      {
      gameLiftAwsAccountId: NonZeroAndMaxString.t
        [@ocaml.doc
          "A unique identifier for the Amazon Web Services account that you use to manage your Amazon GameLift Servers fleet. You can find your Account ID in the Amazon Web Services Management Console under account settings."];
      peerVpcId: NonZeroAndMaxString.t
        [@ocaml.doc
          "A unique identifier for a VPC with resources to be accessed by your Amazon GameLift Servers fleet. The VPC must be in the same Region as your fleet. To look up a VPC ID, use the VPC Dashboard in the Amazon Web Services Management Console. Learn more about VPC peering in VPC Peering with Amazon GameLift Servers Fleets."]}
    let context_ = "DeleteVpcPeeringAuthorizationInput"
    let make ~gameLiftAwsAccountId =
      fun ~peerVpcId -> fun () -> { gameLiftAwsAccountId; peerVpcId }
    let to_value x =
      structure_to_value
        [("GameLiftAwsAccountId",
           (Some (NonZeroAndMaxString.to_value x.gameLiftAwsAccountId)));
        ("PeerVpcId", (Some (NonZeroAndMaxString.to_value x.peerVpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let peerVpcId =
        NonZeroAndMaxString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerVpcId") in
      let gameLiftAwsAccountId =
        NonZeroAndMaxString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameLiftAwsAccountId") in
      make ~peerVpcId ~gameLiftAwsAccountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let peerVpcId =
        field_map_exn json__ "PeerVpcId" NonZeroAndMaxString.of_json in
      let gameLiftAwsAccountId =
        field_map_exn json__ "GameLiftAwsAccountId"
          NonZeroAndMaxString.of_json in
      make ~peerVpcId ~gameLiftAwsAccountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Cancels a pending VPC peering authorization for the specified VPC. If you need to delete an existing VPC peering connection, use DeleteVpcPeeringConnection. Related actions All APIs by task"]
module DeleteScriptInput =
  struct
    type nonrec t =
      {
      scriptId: ScriptIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the Realtime script to delete. You can use either the script ID or ARN value."]}
    let context_ = "DeleteScriptInput"
    let make ~scriptId = fun () -> { scriptId }
    let to_value x =
      structure_to_value
        [("ScriptId", (Some (ScriptIdOrArn.to_value x.scriptId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scriptId =
        ScriptIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScriptId") in
      make ~scriptId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scriptId = field_map_exn json__ "ScriptId" ScriptIdOrArn.of_json in
      make ~scriptId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Deletes a Realtime script. This operation permanently deletes the script record. If script files were uploaded, they are also deleted (files stored in an S3 bucket are not deleted). To delete a script, specify the script ID. Before deleting a script, be sure to terminate all fleets that are deployed with the script being deleted. Fleet instances periodically check for script updates, and if the script record no longer exists, the instance will go into an error state and be unable to host game sessions. Learn more Amazon GameLift Servers Amazon GameLift Servers Realtime Related actions All APIs by task"]
module DeleteScalingPolicyInput =
  struct
    type nonrec t =
      {
      name: NonZeroAndMaxString.t
        [@ocaml.doc
          "A descriptive label that is associated with a fleet's scaling policy. Policy names do not need to be unique."];
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to be deleted. You can use either the fleet ID or ARN value."]}
    let context_ = "DeleteScalingPolicyInput"
    let make ~name = fun ~fleetId -> fun () -> { name; fleetId }
    let to_value x =
      structure_to_value
        [("Name", (Some (NonZeroAndMaxString.to_value x.name)));
        ("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      let name =
        NonZeroAndMaxString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~fleetId ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      let name = field_map_exn json__ "Name" NonZeroAndMaxString.of_json in
      make ~fleetId ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Deletes a fleet scaling policy. Once deleted, the policy is no longer in force and Amazon GameLift Servers removes all record of it. To delete a scaling policy, specify both the scaling policy name and the fleet ID it is associated with. To temporarily suspend scaling policies, use StopFleetActions. This operation suspends all policies for the fleet."]
module DeleteMatchmakingRuleSetOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Deletes an existing matchmaking rule set. To delete the rule set, provide the rule set name. Rule sets cannot be deleted if they are currently being used by a matchmaking configuration. Learn more Build a rule set"]
module DeleteMatchmakingRuleSetInput =
  struct
    type nonrec t =
      {
      name: MatchmakingRuleSetName.t
        [@ocaml.doc
          "A unique identifier for the matchmaking rule set to be deleted. (Note: The rule set name is different from the optional \"name\" field in the rule set body.) You can use either the rule set name or ARN value."]}
    let context_ = "DeleteMatchmakingRuleSetInput"
    let make ~name = fun () -> { name }
    let to_value x =
      structure_to_value
        [("Name", (Some (MatchmakingRuleSetName.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        MatchmakingRuleSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map_exn json__ "Name" MatchmakingRuleSetName.of_json in
      make ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Deletes an existing matchmaking rule set. To delete the rule set, provide the rule set name. Rule sets cannot be deleted if they are currently being used by a matchmaking configuration. Learn more Build a rule set"]
module DeleteMatchmakingConfigurationOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Permanently removes a FlexMatch matchmaking configuration. To delete, specify the configuration name. A matchmaking configuration cannot be deleted if it is being used in any active matchmaking tickets."]
module DeleteMatchmakingConfigurationInput =
  struct
    type nonrec t =
      {
      name: MatchmakingConfigurationName.t
        [@ocaml.doc
          "A unique identifier for the matchmaking configuration. You can use either the configuration name or ARN value."]}
    let context_ = "DeleteMatchmakingConfigurationInput"
    let make ~name = fun () -> { name }
    let to_value x =
      structure_to_value
        [("Name", (Some (MatchmakingConfigurationName.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        MatchmakingConfigurationName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name =
        field_map_exn json__ "Name" MatchmakingConfigurationName.of_json in
      make ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Permanently removes a FlexMatch matchmaking configuration. To delete, specify the configuration name. A matchmaking configuration cannot be deleted if it is being used in any active matchmaking tickets."]
module DeleteLocationOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Anywhere Deletes a custom location. Before deleting a custom location, review any fleets currently using the custom location and deregister the location if it is in use. For more information, see DeregisterCompute."]
module DeleteLocationInput =
  struct
    type nonrec t =
      {
      locationName: CustomLocationNameOrArnModel.t
        [@ocaml.doc
          "The location name of the custom location to be deleted."]}
    let context_ = "DeleteLocationInput"
    let make ~locationName = fun () -> { locationName }
    let to_value x =
      structure_to_value
        [("LocationName",
           (Some (CustomLocationNameOrArnModel.to_value x.locationName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let locationName =
        CustomLocationNameOrArnModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LocationName") in
      make ~locationName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let locationName =
        field_map_exn json__ "LocationName"
          CustomLocationNameOrArnModel.of_json in
      make ~locationName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Anywhere Deletes a custom location. Before deleting a custom location, review any fleets currently using the custom location and deregister the location if it is in use. For more information, see DeregisterCompute."]
module DeleteGameSessionQueueOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Deletes a game session queue. Once a queue is successfully deleted, unfulfilled StartGameSessionPlacement requests that reference the queue will fail. To delete a queue, specify the queue name."]
module DeleteGameSessionQueueInput =
  struct
    type nonrec t =
      {
      name: GameSessionQueueNameOrArn.t
        [@ocaml.doc
          "A descriptive label that is associated with game session queue. Queue names must be unique within each Region. You can use either the queue ID or ARN value."]}
    let context_ = "DeleteGameSessionQueueInput"
    let make ~name = fun () -> { name }
    let to_value x =
      structure_to_value
        [("Name", (Some (GameSessionQueueNameOrArn.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        GameSessionQueueNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name =
        field_map_exn json__ "Name" GameSessionQueueNameOrArn.of_json in
      make ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Deletes a game session queue. Once a queue is successfully deleted, unfulfilled StartGameSessionPlacement requests that reference the queue will fail. To delete a queue, specify the queue name."]
module DeleteGameServerGroupOutput =
  struct
    type nonrec t =
      {
      gameServerGroup: GameServerGroup.t option
        [@ocaml.doc
          "An object that describes the deleted game server group resource, with status updated to DELETE_SCHEDULED."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameServerGroup = fun () -> { gameServerGroup }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameServerGroup",
           (Option.map x.gameServerGroup ~f:GameServerGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServerGroup =
        (Option.map ~f:GameServerGroup.of_xml)
          (Xml.child xml_arg0 "GameServerGroup") in
      make ?gameServerGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServerGroup =
        field_map json__ "GameServerGroup" GameServerGroup.of_json in
      make ?gameServerGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Terminates a game server group and permanently deletes the game server group record. You have several options for how these resources are impacted when deleting the game server group. Depending on the type of delete operation selected, this operation might affect these resources: The game server group The corresponding Auto Scaling group All game servers that are currently running in the group To delete a game server group, identify the game server group to delete and specify the type of delete operation to initiate. Game server groups can only be deleted if they are in ACTIVE or ERROR status. If the delete request is successful, a series of operations are kicked off. The game server group status is changed to DELETE_SCHEDULED, which prevents new game servers from being registered and stops automatic scaling activity. Once all game servers in the game server group are deregistered, Amazon GameLift Servers FleetIQ can begin deleting resources. If any of the delete operations fail, the game server group is placed in ERROR status. Amazon GameLift Servers FleetIQ emits delete events to Amazon CloudWatch. Learn more Amazon GameLift Servers FleetIQ Guide"]
module DeleteGameServerGroupInput =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupNameOrArn.t
        [@ocaml.doc
          "A unique identifier for the game server group. Use either the name or ARN value."];
      deleteOption: GameServerGroupDeleteOption.t option
        [@ocaml.doc
          "The type of delete to perform. Options include the following: SAFE_DELETE \226\128\147 (default) Terminates the game server group and Amazon EC2 Auto Scaling group only when it has no game servers that are in UTILIZED status. FORCE_DELETE \226\128\147 Terminates the game server group, including all active game servers regardless of their utilization status, and the Amazon EC2 Auto Scaling group. RETAIN \226\128\147 Does a safe delete of the game server group but retains the Amazon EC2 Auto Scaling group as is."]}
    let context_ = "DeleteGameServerGroupInput"
    let make ?deleteOption =
      fun ~gameServerGroupName ->
        fun () -> { deleteOption; gameServerGroupName }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Some (GameServerGroupNameOrArn.to_value x.gameServerGroupName)));
        ("DeleteOption",
          (Option.map x.deleteOption ~f:GameServerGroupDeleteOption.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deleteOption =
        (Option.map ~f:GameServerGroupDeleteOption.of_xml)
          (Xml.child xml_arg0 "DeleteOption") in
      let gameServerGroupName =
        GameServerGroupNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerGroupName") in
      make ?deleteOption ~gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deleteOption =
        field_map json__ "DeleteOption" GameServerGroupDeleteOption.of_json in
      let gameServerGroupName =
        field_map_exn json__ "GameServerGroupName"
          GameServerGroupNameOrArn.of_json in
      make ?deleteOption ~gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Terminates a game server group and permanently deletes the game server group record. You have several options for how these resources are impacted when deleting the game server group. Depending on the type of delete operation selected, this operation might affect these resources: The game server group The corresponding Auto Scaling group All game servers that are currently running in the group To delete a game server group, identify the game server group to delete and specify the type of delete operation to initiate. Game server groups can only be deleted if they are in ACTIVE or ERROR status. If the delete request is successful, a series of operations are kicked off. The game server group status is changed to DELETE_SCHEDULED, which prevents new game servers from being registered and stops automatic scaling activity. Once all game servers in the game server group are deregistered, Amazon GameLift Servers FleetIQ can begin deleting resources. If any of the delete operations fail, the game server group is placed in ERROR status. Amazon GameLift Servers FleetIQ emits delete events to Amazon CloudWatch. Learn more Amazon GameLift Servers FleetIQ Guide"]
module DeleteFleetLocationsOutput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the fleet that location attributes are being deleted for."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      locationStates: LocationStateList.t option
        [@ocaml.doc
          "The remote locations that are being deleted, with each location status set to DELETING."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?locationStates ->
          fun () -> { fleetId; fleetArn; locationStates }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetIdOrArn.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("LocationStates",
          (Option.map x.locationStates ~f:LocationStateList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let locationStates =
        (Option.map ~f:LocationStateList.of_xml)
          (Xml.child xml_arg0 "LocationStates") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetIdOrArn.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?locationStates ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let locationStates =
        field_map json__ "LocationStates" LocationStateList.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetIdOrArn.of_json in
      make ?locationStates ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Removes locations from a multi-location fleet. When deleting a location, all game server process and all instances that are still active in the location are shut down. To delete fleet locations, identify the fleet ID and provide a list of the locations to be deleted. If successful, GameLift sets the location status to DELETING, and begins to shut down existing server processes and terminate instances in each location being deleted. When completed, the location status changes to TERMINATED. Learn more Setting up Amazon GameLift Servers fleets"]
module DeleteFleetLocationsInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to delete locations for. You can use either the fleet ID or ARN value."];
      locations: LocationList.t
        [@ocaml.doc
          "The list of fleet locations to delete. Specify locations in the form of an Amazon Web Services Region code, such as us-west-2."]}
    let context_ = "DeleteFleetLocationsInput"
    let make ~fleetId = fun ~locations -> fun () -> { fleetId; locations }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("Locations", (Some (LocationList.to_value x.locations)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let locations =
        LocationList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Locations") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~locations ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let locations = field_map_exn json__ "Locations" LocationList.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~locations ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Removes locations from a multi-location fleet. When deleting a location, all game server process and all instances that are still active in the location are shut down. To delete fleet locations, identify the fleet ID and provide a list of the locations to be deleted. If successful, GameLift sets the location status to DELETING, and begins to shut down existing server processes and terminate instances in each location being deleted. When completed, the location status changes to TERMINATED. Learn more Setting up Amazon GameLift Servers fleets"]
module DeleteFleetInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to be deleted. You can use either the fleet ID or ARN value."]}
    let context_ = "DeleteFleetInput"
    let make ~fleetId = fun () -> { fleetId }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Deletes all resources and information related to a fleet and shuts down any currently running fleet instances, including those in remote locations. If the fleet being deleted has a VPC peering connection, you first need to get a valid authorization (good for 24 hours) by calling CreateVpcPeeringAuthorization. You don't need to explicitly delete the VPC peering connection. To delete a fleet, specify the fleet ID to be terminated. During the deletion process, the fleet status is changed to DELETING. When completed, the status switches to TERMINATED and the fleet event FLEET_DELETED is emitted. Learn more Setting up Amazon GameLift Servers Fleets"]
module DeleteContainerGroupDefinitionOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Request options: Deletes a container group definition. Delete an entire container group definition, including all versions. Specify the container group definition name, or use an ARN value without the version number. Delete a particular version. Specify the container group definition name and a version number, or use an ARN value that includes the version number. Keep the newest versions and delete all older versions. Specify the container group definition name and the number of versions to retain. For example, set VersionCountToRetain to 5 to delete all but the five most recent versions. Result If successful, Amazon GameLift Servers removes the container group definition versions that you request deletion for. This request will fail for any requested versions if the following is true: If the version is being used in an active fleet If the version is being deployed to a fleet in a deployment that's currently in progress. If the version is designated as a rollback definition in a fleet deployment that's currently in progress. Learn more Manage a container group definition"]
module DeleteContainerGroupDefinitionInput =
  struct
    type nonrec t =
      {
      name: ContainerGroupDefinitionNameOrArn.t
        [@ocaml.doc
          "The unique identifier for the container group definition to delete. You can use either the Name or ARN value."];
      versionNumber: PositiveInteger.t option
        [@ocaml.doc "The specific version to delete."];
      versionCountToRetain: WholeNumber.t option
        [@ocaml.doc
          "The number of most recent versions to keep while deleting all older versions."]}
    let context_ = "DeleteContainerGroupDefinitionInput"
    let make ?versionNumber =
      fun ?versionCountToRetain ->
        fun ~name -> fun () -> { versionNumber; versionCountToRetain; name }
    let to_value x =
      structure_to_value
        [("Name", (Some (ContainerGroupDefinitionNameOrArn.to_value x.name)));
        ("VersionNumber",
          (Option.map x.versionNumber ~f:PositiveInteger.to_value));
        ("VersionCountToRetain",
          (Option.map x.versionCountToRetain ~f:WholeNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let versionCountToRetain =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "VersionCountToRetain") in
      let versionNumber =
        (Option.map ~f:PositiveInteger.of_xml)
          (Xml.child xml_arg0 "VersionNumber") in
      let name =
        ContainerGroupDefinitionNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?versionCountToRetain ?versionNumber ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let versionCountToRetain =
        field_map json__ "VersionCountToRetain" WholeNumber.of_json in
      let versionNumber =
        field_map json__ "VersionNumber" PositiveInteger.of_json in
      let name =
        field_map_exn json__ "Name" ContainerGroupDefinitionNameOrArn.of_json in
      make ?versionCountToRetain ?versionNumber ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Request options: Deletes a container group definition. Delete an entire container group definition, including all versions. Specify the container group definition name, or use an ARN value without the version number. Delete a particular version. Specify the container group definition name and a version number, or use an ARN value that includes the version number. Keep the newest versions and delete all older versions. Specify the container group definition name and the number of versions to retain. For example, set VersionCountToRetain to 5 to delete all but the five most recent versions. Result If successful, Amazon GameLift Servers removes the container group definition versions that you request deletion for. This request will fail for any requested versions if the following is true: If the version is being used in an active fleet If the version is being deployed to a fleet in a deployment that's currently in progress. If the version is designated as a rollback definition in a fleet deployment that's currently in progress. Learn more Manage a container group definition"]
module DeleteContainerFleetOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Deletes all resources and information related to a container fleet and shuts down currently running fleet instances, including those in remote locations. The container fleet must be in ACTIVE status to be deleted. To delete a fleet, specify the fleet ID to be terminated. During the deletion process, the fleet status is changed to DELETING. Learn more Setting up Amazon GameLift Servers Fleets"]
module DeleteContainerFleetInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the container fleet to delete. You can use either the fleet ID or ARN value."]}
    let context_ = "DeleteContainerFleetInput"
    let make ~fleetId = fun () -> { fleetId }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Deletes all resources and information related to a container fleet and shuts down currently running fleet instances, including those in remote locations. The container fleet must be in ACTIVE status to be deleted. To delete a fleet, specify the fleet ID to be terminated. During the deletion process, the fleet status is changed to DELETING. Learn more Setting up Amazon GameLift Servers Fleets"]
module DeleteBuildInput =
  struct
    type nonrec t =
      {
      buildId: BuildIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the build to delete. You can use either the build ID or ARN value."]}
    let context_ = "DeleteBuildInput"
    let make ~buildId = fun () -> { buildId }
    let to_value x =
      structure_to_value
        [("BuildId", (Some (BuildIdOrArn.to_value x.buildId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let buildId =
        BuildIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BuildId") in
      make ~buildId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let buildId = field_map_exn json__ "BuildId" BuildIdOrArn.of_json in
      make ~buildId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Deletes a build. This operation permanently deletes the build resource and any uploaded build files. Deleting a build does not affect the status of any active fleets using the build, but you can no longer create new fleets with the deleted build. To delete a build, specify the build ID. Learn more Upload a Custom Server Build All APIs by task"]
module DeleteAliasInput =
  struct
    type nonrec t =
      {
      aliasId: AliasIdOrArn.t
        [@ocaml.doc
          "A unique identifier of the alias that you want to delete. You can use either the alias ID or ARN value."]}
    let context_ = "DeleteAliasInput"
    let make ~aliasId = fun () -> { aliasId }
    let to_value x =
      structure_to_value
        [("AliasId", (Some (AliasIdOrArn.to_value x.aliasId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let aliasId =
        AliasIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AliasId") in
      make ~aliasId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let aliasId = field_map_exn json__ "AliasId" AliasIdOrArn.of_json in
      make ~aliasId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Deletes an alias. This operation removes all record of the alias. Game clients attempting to access a server process using the deleted alias receive an error. To delete an alias, specify the alias ID to be deleted. Related actions All APIs by task"]
module CreateVpcPeeringConnectionOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Establishes a VPC peering connection between a virtual private cloud (VPC) in an Amazon Web Services account with the VPC for your Amazon GameLift Servers fleet. VPC peering enables the game servers on your fleet to communicate directly with other Amazon Web Services resources. You can peer with VPCs in any Amazon Web Services account that you have access to, including the account that you use to manage your Amazon GameLift Servers fleets. You cannot peer with VPCs that are in different Regions. For more information, see VPC Peering with Amazon GameLift Servers Fleets. Before calling this operation to establish the peering connection, you first need to use CreateVpcPeeringAuthorization and identify the VPC you want to peer with. Once the authorization for the specified VPC is issued, you have 24 hours to establish the connection. These two operations handle all tasks necessary to peer the two VPCs, including acceptance, updating routing tables, etc. To establish the connection, call this operation from the Amazon Web Services account that is used to manage the Amazon GameLift Servers fleets. Identify the following values: (1) The ID of the fleet you want to be enable a VPC peering connection for; (2) The Amazon Web Services account with the VPC that you want to peer with; and (3) The ID of the VPC you want to peer with. This operation is asynchronous. If successful, a connection request is created. You can use continuous polling to track the request's status using DescribeVpcPeeringConnections , or by monitoring fleet events for success or failure using DescribeFleetEvents . Related actions All APIs by task"]
module CreateVpcPeeringConnectionInput =
  struct
    type nonrec t =
      {
      fleetId: FleetId.t
        [@ocaml.doc
          "A unique identifier for the fleet. You can use either the fleet ID or ARN value. This tells Amazon GameLift Servers which GameLift VPC to peer with."];
      peerVpcAwsAccountId: NonZeroAndMaxString.t
        [@ocaml.doc
          "A unique identifier for the Amazon Web Services account with the VPC that you want to peer your Amazon GameLift Servers fleet with. You can find your Account ID in the Amazon Web Services Management Console under account settings."];
      peerVpcId: NonZeroAndMaxString.t
        [@ocaml.doc
          "A unique identifier for a VPC with resources to be accessed by your Amazon GameLift Servers fleet. The VPC must be in the same Region as your fleet. To look up a VPC ID, use the VPC Dashboard in the Amazon Web Services Management Console. Learn more about VPC peering in VPC Peering with Amazon GameLift Servers Fleets."]}
    let context_ = "CreateVpcPeeringConnectionInput"
    let make ~fleetId =
      fun ~peerVpcAwsAccountId ->
        fun ~peerVpcId ->
          fun () -> { fleetId; peerVpcAwsAccountId; peerVpcId }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetId.to_value x.fleetId)));
        ("PeerVpcAwsAccountId",
          (Some (NonZeroAndMaxString.to_value x.peerVpcAwsAccountId)));
        ("PeerVpcId", (Some (NonZeroAndMaxString.to_value x.peerVpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let peerVpcId =
        NonZeroAndMaxString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerVpcId") in
      let peerVpcAwsAccountId =
        NonZeroAndMaxString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerVpcAwsAccountId") in
      let fleetId =
        FleetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~peerVpcId ~peerVpcAwsAccountId ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let peerVpcId =
        field_map_exn json__ "PeerVpcId" NonZeroAndMaxString.of_json in
      let peerVpcAwsAccountId =
        field_map_exn json__ "PeerVpcAwsAccountId"
          NonZeroAndMaxString.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetId.of_json in
      make ~peerVpcId ~peerVpcAwsAccountId ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Establishes a VPC peering connection between a virtual private cloud (VPC) in an Amazon Web Services account with the VPC for your Amazon GameLift Servers fleet. VPC peering enables the game servers on your fleet to communicate directly with other Amazon Web Services resources. You can peer with VPCs in any Amazon Web Services account that you have access to, including the account that you use to manage your Amazon GameLift Servers fleets. You cannot peer with VPCs that are in different Regions. For more information, see VPC Peering with Amazon GameLift Servers Fleets. Before calling this operation to establish the peering connection, you first need to use CreateVpcPeeringAuthorization and identify the VPC you want to peer with. Once the authorization for the specified VPC is issued, you have 24 hours to establish the connection. These two operations handle all tasks necessary to peer the two VPCs, including acceptance, updating routing tables, etc. To establish the connection, call this operation from the Amazon Web Services account that is used to manage the Amazon GameLift Servers fleets. Identify the following values: (1) The ID of the fleet you want to be enable a VPC peering connection for; (2) The Amazon Web Services account with the VPC that you want to peer with; and (3) The ID of the VPC you want to peer with. This operation is asynchronous. If successful, a connection request is created. You can use continuous polling to track the request's status using DescribeVpcPeeringConnections , or by monitoring fleet events for success or failure using DescribeFleetEvents . Related actions All APIs by task"]
module CreateVpcPeeringAuthorizationOutput =
  struct
    type nonrec t =
      {
      vpcPeeringAuthorization: VpcPeeringAuthorization.t option
        [@ocaml.doc
          "Details on the requested VPC peering authorization, including expiration."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?vpcPeeringAuthorization = fun () -> { vpcPeeringAuthorization }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("VpcPeeringAuthorization",
           (Option.map x.vpcPeeringAuthorization
              ~f:VpcPeeringAuthorization.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcPeeringAuthorization =
        (Option.map ~f:VpcPeeringAuthorization.of_xml)
          (Xml.child xml_arg0 "VpcPeeringAuthorization") in
      make ?vpcPeeringAuthorization ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcPeeringAuthorization =
        field_map json__ "VpcPeeringAuthorization"
          VpcPeeringAuthorization.of_json in
      make ?vpcPeeringAuthorization ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Requests authorization to create or delete a peer connection between the VPC for your Amazon GameLift Servers fleet and a virtual private cloud (VPC) in your Amazon Web Services account. VPC peering enables the game servers on your fleet to communicate directly with other Amazon Web Services resources. After you've received authorization, use CreateVpcPeeringConnection to establish the peering connection. For more information, see VPC Peering with Amazon GameLift Servers Fleets. You can peer with VPCs that are owned by any Amazon Web Services account you have access to, including the account that you use to manage your Amazon GameLift Servers fleets. You cannot peer with VPCs that are in different Regions. To request authorization to create a connection, call this operation from the Amazon Web Services account with the VPC that you want to peer to your Amazon GameLift Servers fleet. For example, to enable your game servers to retrieve data from a DynamoDB table, use the account that manages that DynamoDB resource. Identify the following values: (1) The ID of the VPC that you want to peer with, and (2) the ID of the Amazon Web Services account that you use to manage Amazon GameLift Servers. If successful, VPC peering is authorized for the specified VPC. To request authorization to delete a connection, call this operation from the Amazon Web Services account with the VPC that is peered with your Amazon GameLift Servers fleet. Identify the following values: (1) VPC ID that you want to delete the peering connection for, and (2) ID of the Amazon Web Services account that you use to manage Amazon GameLift Servers. The authorization remains valid for 24 hours unless it is canceled. You must create or delete the peering connection while the authorization is valid. Related actions All APIs by task"]
module CreateVpcPeeringAuthorizationInput =
  struct
    type nonrec t =
      {
      gameLiftAwsAccountId: NonZeroAndMaxString.t
        [@ocaml.doc
          "A unique identifier for the Amazon Web Services account that you use to manage your Amazon GameLift Servers fleet. You can find your Account ID in the Amazon Web Services Management Console under account settings."];
      peerVpcId: NonZeroAndMaxString.t
        [@ocaml.doc
          "A unique identifier for a VPC with resources to be accessed by your Amazon GameLift Servers fleet. The VPC must be in the same Region as your fleet. To look up a VPC ID, use the VPC Dashboard in the Amazon Web Services Management Console. Learn more about VPC peering in VPC Peering with Amazon GameLift Servers Fleets."]}
    let context_ = "CreateVpcPeeringAuthorizationInput"
    let make ~gameLiftAwsAccountId =
      fun ~peerVpcId -> fun () -> { gameLiftAwsAccountId; peerVpcId }
    let to_value x =
      structure_to_value
        [("GameLiftAwsAccountId",
           (Some (NonZeroAndMaxString.to_value x.gameLiftAwsAccountId)));
        ("PeerVpcId", (Some (NonZeroAndMaxString.to_value x.peerVpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let peerVpcId =
        NonZeroAndMaxString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeerVpcId") in
      let gameLiftAwsAccountId =
        NonZeroAndMaxString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameLiftAwsAccountId") in
      make ~peerVpcId ~gameLiftAwsAccountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let peerVpcId =
        field_map_exn json__ "PeerVpcId" NonZeroAndMaxString.of_json in
      let gameLiftAwsAccountId =
        field_map_exn json__ "GameLiftAwsAccountId"
          NonZeroAndMaxString.of_json in
      make ~peerVpcId ~gameLiftAwsAccountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 Requests authorization to create or delete a peer connection between the VPC for your Amazon GameLift Servers fleet and a virtual private cloud (VPC) in your Amazon Web Services account. VPC peering enables the game servers on your fleet to communicate directly with other Amazon Web Services resources. After you've received authorization, use CreateVpcPeeringConnection to establish the peering connection. For more information, see VPC Peering with Amazon GameLift Servers Fleets. You can peer with VPCs that are owned by any Amazon Web Services account you have access to, including the account that you use to manage your Amazon GameLift Servers fleets. You cannot peer with VPCs that are in different Regions. To request authorization to create a connection, call this operation from the Amazon Web Services account with the VPC that you want to peer to your Amazon GameLift Servers fleet. For example, to enable your game servers to retrieve data from a DynamoDB table, use the account that manages that DynamoDB resource. Identify the following values: (1) The ID of the VPC that you want to peer with, and (2) the ID of the Amazon Web Services account that you use to manage Amazon GameLift Servers. If successful, VPC peering is authorized for the specified VPC. To request authorization to delete a connection, call this operation from the Amazon Web Services account with the VPC that is peered with your Amazon GameLift Servers fleet. Identify the following values: (1) VPC ID that you want to delete the peering connection for, and (2) ID of the Amazon Web Services account that you use to manage Amazon GameLift Servers. The authorization remains valid for 24 hours unless it is canceled. You must create or delete the peering connection while the authorization is valid. Related actions All APIs by task"]
module CreateScriptOutput =
  struct
    type nonrec t =
      {
      script: Script.t option
        [@ocaml.doc
          "The newly created script record with a unique script ID and ARN. The new script's storage location reflects an Amazon S3 location: (1) If the script was uploaded from an S3 bucket under your account, the storage location reflects the information that was provided in the CreateScript request; (2) If the script file was uploaded from a local zip file, the storage location reflects an S3 location controls by the Amazon GameLift Servers service."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?script = fun () -> { script }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Script", (Option.map x.script ~f:Script.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let script =
        (Option.map ~f:Script.of_xml) (Xml.child xml_arg0 "Script") in
      make ?script ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let script = field_map json__ "Script" Script.of_json in
      make ?script ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere Creates a new script record for your Amazon GameLift Servers Realtime script. Realtime scripts are JavaScript that provide configuration settings and optional custom game logic for your game. The script is deployed when you create a Amazon GameLift Servers Realtime fleet to host your game sessions. Script logic is executed during an active game session. To create a new script record, specify a script name and provide the script file(s). The script files and all dependencies must be zipped into a single file. You can pull the zip file from either of these locations: A locally available directory. Use the ZipFile parameter for this option. An Amazon Simple Storage Service (Amazon S3) bucket under your Amazon Web Services account. Use the StorageLocation parameter for this option. You'll need to have an Identity Access Management (IAM) role that allows the Amazon GameLift Servers service to access your S3 bucket. If the call is successful, a new script record is created with a unique script ID. If the script file is provided as a local file, the file is uploaded to an Amazon GameLift Servers-owned S3 bucket and the script record's storage location reflects this location. If the script file is provided as an S3 bucket, Amazon GameLift Servers accesses the file at this storage location as needed for deployment. Learn more Amazon GameLift Servers Amazon GameLift Servers Realtime Set Up a Role for Amazon GameLift Servers Access Related actions All APIs by task"]
module CreateScriptInput =
  struct
    type nonrec t =
      {
      name: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a script. Script names do not need to be unique. You can use UpdateScript to change this value later."];
      version: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Version information that is associated with a build or script. Version strings do not need to be unique. You can use UpdateScript to change this value later."];
      storageLocation: S3Location.t option
        [@ocaml.doc
          "The location of the Amazon S3 bucket where a zipped file containing your Realtime scripts is stored. The storage location must specify the Amazon S3 bucket name, the zip file name (the \"key\"), and a role ARN that allows Amazon GameLift Servers to access the Amazon S3 storage location. The S3 bucket must be in the same Region where you want to create a new script. By default, Amazon GameLift Servers uploads the latest version of the zip file; if you have S3 object versioning turned on, you can use the ObjectVersion parameter to specify an earlier version."];
      zipFile: ZipBlob.t option
        [@ocaml.doc
          "A data object containing your Realtime scripts and dependencies as a zip file. The zip file can have one or multiple files. Maximum size of a zip file is 5 MB. When using the Amazon Web Services CLI tool to create a script, this parameter is set to the zip file name. It must be prepended with the string \"fileb://\" to indicate that the file data is a binary object. For example: --zip-file fileb://myRealtimeScript.zip."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of labels to assign to the new script resource. Tags are developer-defined key-value pairs. Tagging Amazon Web Services resources are useful for resource management, access management and cost allocation. For more information, see Tagging Amazon Web Services Resources in the Amazon Web Services General Reference. Once the resource is created, you can use TagResource, UntagResource, and ListTagsForResource to add, remove, and view tags. The maximum tag limit may be lower than stated. See the Amazon Web Services General Reference for actual tagging limits."];
      nodeJsVersion: NodeJsVersion.t option
        [@ocaml.doc
          "The Node.js version used for execution of your Realtime script. The valid values are 10.x | 24.x. By default, NodeJsVersion is 10.x. This value cannot be updated later. Node.js 10 will reach end of support on September 30, 2026. See more details in the Node.js 10 FAQs. For migration guidance, see Migrating from Node.js 10 to 24."]}
    let make ?name =
      fun ?version ->
        fun ?storageLocation ->
          fun ?zipFile ->
            fun ?tags ->
              fun ?nodeJsVersion ->
                fun () ->
                  {
                    name;
                    version;
                    storageLocation;
                    zipFile;
                    tags;
                    nodeJsVersion
                  }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:NonZeroAndMaxString.to_value));
        ("Version", (Option.map x.version ~f:NonZeroAndMaxString.to_value));
        ("StorageLocation",
          (Option.map x.storageLocation ~f:S3Location.to_value));
        ("ZipFile", (Option.map x.zipFile ~f:ZipBlob.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value));
        ("NodeJsVersion",
          (Option.map x.nodeJsVersion ~f:NodeJsVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nodeJsVersion =
        (Option.map ~f:NodeJsVersion.of_xml)
          (Xml.child xml_arg0 "NodeJsVersion") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let zipFile =
        (Option.map ~f:ZipBlob.of_xml) (Xml.child xml_arg0 "ZipFile") in
      let storageLocation =
        (Option.map ~f:S3Location.of_xml)
          (Xml.child xml_arg0 "StorageLocation") in
      let version =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Version") in
      let name =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?nodeJsVersion ?tags ?zipFile ?storageLocation ?version ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nodeJsVersion =
        field_map json__ "NodeJsVersion" NodeJsVersion.of_json in
      let tags = field_map json__ "Tags" TagList.of_json in
      let zipFile = field_map json__ "ZipFile" ZipBlob.of_json in
      let storageLocation =
        field_map json__ "StorageLocation" S3Location.of_json in
      let version = field_map json__ "Version" NonZeroAndMaxString.of_json in
      let name = field_map json__ "Name" NonZeroAndMaxString.of_json in
      make ?nodeJsVersion ?tags ?zipFile ?storageLocation ?version ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere Creates a new script record for your Amazon GameLift Servers Realtime script. Realtime scripts are JavaScript that provide configuration settings and optional custom game logic for your game. The script is deployed when you create a Amazon GameLift Servers Realtime fleet to host your game sessions. Script logic is executed during an active game session. To create a new script record, specify a script name and provide the script file(s). The script files and all dependencies must be zipped into a single file. You can pull the zip file from either of these locations: A locally available directory. Use the ZipFile parameter for this option. An Amazon Simple Storage Service (Amazon S3) bucket under your Amazon Web Services account. Use the StorageLocation parameter for this option. You'll need to have an Identity Access Management (IAM) role that allows the Amazon GameLift Servers service to access your S3 bucket. If the call is successful, a new script record is created with a unique script ID. If the script file is provided as a local file, the file is uploaded to an Amazon GameLift Servers-owned S3 bucket and the script record's storage location reflects this location. If the script file is provided as an S3 bucket, Amazon GameLift Servers accesses the file at this storage location as needed for deployment. Learn more Amazon GameLift Servers Amazon GameLift Servers Realtime Set Up a Role for Amazon GameLift Servers Access Related actions All APIs by task"]
module CreatePlayerSessionsOutput =
  struct
    type nonrec t =
      {
      playerSessions: PlayerSessionList.t option
        [@ocaml.doc
          "A collection of player session objects created for the added players."]}
    type nonrec error =
      [ `GameSessionFullException of GameSessionFullException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidGameSessionStatusException of
          InvalidGameSessionStatusException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TerminalRoutingStrategyException of
          TerminalRoutingStrategyException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?playerSessions = fun () -> { playerSessions }
    let error_of_json name json =
      match name with
      | "GameSessionFullException" ->
          `GameSessionFullException (GameSessionFullException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidGameSessionStatusException" ->
          `InvalidGameSessionStatusException
            (InvalidGameSessionStatusException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "GameSessionFullException" ->
          `GameSessionFullException (GameSessionFullException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidGameSessionStatusException" ->
          `InvalidGameSessionStatusException
            (InvalidGameSessionStatusException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `GameSessionFullException e ->
          `Assoc
            [("error", (`String "GameSessionFullException"));
            ("details", (GameSessionFullException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidGameSessionStatusException e ->
          `Assoc
            [("error", (`String "InvalidGameSessionStatusException"));
            ("details", (InvalidGameSessionStatusException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TerminalRoutingStrategyException e ->
          `Assoc
            [("error", (`String "TerminalRoutingStrategyException"));
            ("details", (TerminalRoutingStrategyException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PlayerSessions",
           (Option.map x.playerSessions ~f:PlayerSessionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerSessions =
        (Option.map ~f:PlayerSessionList.of_xml)
          (Xml.child xml_arg0 "PlayerSessions") in
      make ?playerSessions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerSessions =
        field_map json__ "PlayerSessions" PlayerSessionList.of_json in
      make ?playerSessions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Reserves open slots in a game session for a group of players. New player sessions can be created in any game session with an open slot that is in ACTIVE status and has a player creation policy of ACCEPT_ALL. To add a single player to a game session, use CreatePlayerSession To create player sessions, specify a game session ID and a list of player IDs. Optionally, provide a set of player data for each player ID. If successful, a slot is reserved in the game session for each player, and new PlayerSession objects are returned with player session IDs. Each player references their player session ID when sending a connection request to the game session, and the game server can use it to validate the player reservation with the Amazon GameLift Servers service. Player sessions cannot be updated. The maximum number of players per game session is 200. It is not adjustable. Related actions All APIs by task"]
module CreatePlayerSessionsInput =
  struct
    type nonrec t =
      {
      gameSessionId: ArnStringModel.t
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions to add players to. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>."];
      playerIds: PlayerIdList.t
        [@ocaml.doc
          "List of unique identifiers for the players to be added."];
      playerDataMap: PlayerDataMap.t option
        [@ocaml.doc
          "Map of string pairs, each specifying a player ID and a set of developer-defined information related to the player. Amazon GameLift Servers does not use this data, so it can be formatted as needed for use in the game. Any player data strings for player IDs that are not included in the PlayerIds parameter are ignored."]}
    let context_ = "CreatePlayerSessionsInput"
    let make ?playerDataMap =
      fun ~gameSessionId ->
        fun ~playerIds ->
          fun () -> { playerDataMap; gameSessionId; playerIds }
    let to_value x =
      structure_to_value
        [("GameSessionId", (Some (ArnStringModel.to_value x.gameSessionId)));
        ("PlayerIds", (Some (PlayerIdList.to_value x.playerIds)));
        ("PlayerDataMap",
          (Option.map x.playerDataMap ~f:PlayerDataMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerDataMap =
        (Option.map ~f:PlayerDataMap.of_xml)
          (Xml.child xml_arg0 "PlayerDataMap") in
      let playerIds =
        PlayerIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PlayerIds") in
      let gameSessionId =
        ArnStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameSessionId") in
      make ?playerDataMap ~playerIds ~gameSessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerDataMap =
        field_map json__ "PlayerDataMap" PlayerDataMap.of_json in
      let playerIds = field_map_exn json__ "PlayerIds" PlayerIdList.of_json in
      let gameSessionId =
        field_map_exn json__ "GameSessionId" ArnStringModel.of_json in
      make ?playerDataMap ~playerIds ~gameSessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Reserves open slots in a game session for a group of players. New player sessions can be created in any game session with an open slot that is in ACTIVE status and has a player creation policy of ACCEPT_ALL. To add a single player to a game session, use CreatePlayerSession To create player sessions, specify a game session ID and a list of player IDs. Optionally, provide a set of player data for each player ID. If successful, a slot is reserved in the game session for each player, and new PlayerSession objects are returned with player session IDs. Each player references their player session ID when sending a connection request to the game session, and the game server can use it to validate the player reservation with the Amazon GameLift Servers service. Player sessions cannot be updated. The maximum number of players per game session is 200. It is not adjustable. Related actions All APIs by task"]
module CreatePlayerSessionOutput =
  struct
    type nonrec t =
      {
      playerSession: PlayerSession.t option
        [@ocaml.doc
          "Object that describes the newly created player session record."]}
    type nonrec error =
      [ `GameSessionFullException of GameSessionFullException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidGameSessionStatusException of
          InvalidGameSessionStatusException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `TerminalRoutingStrategyException of
          TerminalRoutingStrategyException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?playerSession = fun () -> { playerSession }
    let error_of_json name json =
      match name with
      | "GameSessionFullException" ->
          `GameSessionFullException (GameSessionFullException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidGameSessionStatusException" ->
          `InvalidGameSessionStatusException
            (InvalidGameSessionStatusException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "GameSessionFullException" ->
          `GameSessionFullException (GameSessionFullException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidGameSessionStatusException" ->
          `InvalidGameSessionStatusException
            (InvalidGameSessionStatusException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `GameSessionFullException e ->
          `Assoc
            [("error", (`String "GameSessionFullException"));
            ("details", (GameSessionFullException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidGameSessionStatusException e ->
          `Assoc
            [("error", (`String "InvalidGameSessionStatusException"));
            ("details", (InvalidGameSessionStatusException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TerminalRoutingStrategyException e ->
          `Assoc
            [("error", (`String "TerminalRoutingStrategyException"));
            ("details", (TerminalRoutingStrategyException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PlayerSession",
           (Option.map x.playerSession ~f:PlayerSession.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerSession =
        (Option.map ~f:PlayerSession.of_xml)
          (Xml.child xml_arg0 "PlayerSession") in
      make ?playerSession ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerSession =
        field_map json__ "PlayerSession" PlayerSession.of_json in
      make ?playerSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Reserves an open player slot in a game session for a player. New player sessions can be created in any game session with an open slot that is in ACTIVE status and has a player creation policy of ACCEPT_ALL. You can add a group of players to a game session with CreatePlayerSessions . To create a player session, specify a game session ID, player ID, and optionally a set of player data. If successful, a slot is reserved in the game session for the player and a new PlayerSessions object is returned with a player session ID. The player references the player session ID when sending a connection request to the game session, and the game server can use it to validate the player reservation with the Amazon GameLift Servers service. Player sessions cannot be updated. The maximum number of players per game session is 200. It is not adjustable. Related actions All APIs by task"]
module CreatePlayerSessionInput =
  struct
    type nonrec t =
      {
      gameSessionId: ArnStringModel.t
        [@ocaml.doc
          "An identifier for the game session that is unique across all regions to add a player to. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>."];
      playerId: PlayerId.t
        [@ocaml.doc
          "A unique identifier for a player. Player IDs are developer-defined."];
      playerData: PlayerData.t option
        [@ocaml.doc
          "Developer-defined information related to a player. Amazon GameLift Servers does not use this data, so it can be formatted as needed for use in the game."]}
    let context_ = "CreatePlayerSessionInput"
    let make ?playerData =
      fun ~gameSessionId ->
        fun ~playerId -> fun () -> { playerData; gameSessionId; playerId }
    let to_value x =
      structure_to_value
        [("GameSessionId", (Some (ArnStringModel.to_value x.gameSessionId)));
        ("PlayerId", (Some (PlayerId.to_value x.playerId)));
        ("PlayerData", (Option.map x.playerData ~f:PlayerData.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerData =
        (Option.map ~f:PlayerData.of_xml) (Xml.child xml_arg0 "PlayerData") in
      let playerId =
        PlayerId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PlayerId") in
      let gameSessionId =
        ArnStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameSessionId") in
      make ?playerData ~playerId ~gameSessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerData = field_map json__ "PlayerData" PlayerData.of_json in
      let playerId = field_map_exn json__ "PlayerId" PlayerId.of_json in
      let gameSessionId =
        field_map_exn json__ "GameSessionId" ArnStringModel.of_json in
      make ?playerData ~playerId ~gameSessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Reserves an open player slot in a game session for a player. New player sessions can be created in any game session with an open slot that is in ACTIVE status and has a player creation policy of ACCEPT_ALL. You can add a group of players to a game session with CreatePlayerSessions . To create a player session, specify a game session ID, player ID, and optionally a set of player data. If successful, a slot is reserved in the game session for the player and a new PlayerSessions object is returned with a player session ID. The player references the player session ID when sending a connection request to the game session, and the game server can use it to validate the player reservation with the Amazon GameLift Servers service. Player sessions cannot be updated. The maximum number of players per game session is 200. It is not adjustable. Related actions All APIs by task"]
module CreateMatchmakingRuleSetOutput =
  struct
    type nonrec t =
      {
      ruleSet: MatchmakingRuleSet.t option
        [@ocaml.doc "The newly created matchmaking rule set."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ruleSet = fun () -> { ruleSet }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RuleSet", (Option.map x.ruleSet ~f:MatchmakingRuleSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleSet =
        (Option.map ~f:MatchmakingRuleSet.of_xml)
          (Xml.child xml_arg0 "RuleSet") in
      make ?ruleSet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleSet = field_map json__ "RuleSet" MatchmakingRuleSet.of_json in
      make ?ruleSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Creates a new rule set for FlexMatch matchmaking. A rule set describes the type of match to create, such as the number and size of teams. It also sets the parameters for acceptable player matches, such as minimum skill level or character type. To create a matchmaking rule set, provide unique rule set name and the rule set body in JSON format. Rule sets must be defined in the same Region as the matchmaking configuration they are used with. Since matchmaking rule sets cannot be edited, it is a good idea to check the rule set syntax using ValidateMatchmakingRuleSet before creating a new rule set. Learn more Build a rule set Design a matchmaker Matchmaking with FlexMatch"]
module CreateMatchmakingRuleSetInput =
  struct
    type nonrec t =
      {
      name: MatchmakingIdStringModel.t
        [@ocaml.doc
          "A unique identifier for the matchmaking rule set. A matchmaking configuration identifies the rule set it uses by this name value. Note that the rule set name is different from the optional name field in the rule set body."];
      ruleSetBody: RuleSetBody.t
        [@ocaml.doc
          "A collection of matchmaking rules, formatted as a JSON string. Comments are not allowed in JSON, but most elements support a description field."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of labels to assign to the new matchmaking rule set resource. Tags are developer-defined key-value pairs. Tagging Amazon Web Services resources are useful for resource management, access management and cost allocation. For more information, see Tagging Amazon Web Services Resources in the Amazon Web Services General Reference."]}
    let context_ = "CreateMatchmakingRuleSetInput"
    let make ?tags =
      fun ~name -> fun ~ruleSetBody -> fun () -> { tags; name; ruleSetBody }
    let to_value x =
      structure_to_value
        [("Name", (Some (MatchmakingIdStringModel.to_value x.name)));
        ("RuleSetBody", (Some (RuleSetBody.to_value x.ruleSetBody)));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let ruleSetBody =
        RuleSetBody.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleSetBody") in
      let name =
        MatchmakingIdStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?tags ~ruleSetBody ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagList.of_json in
      let ruleSetBody =
        field_map_exn json__ "RuleSetBody" RuleSetBody.of_json in
      let name = field_map_exn json__ "Name" MatchmakingIdStringModel.of_json in
      make ?tags ~ruleSetBody ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Creates a new rule set for FlexMatch matchmaking. A rule set describes the type of match to create, such as the number and size of teams. It also sets the parameters for acceptable player matches, such as minimum skill level or character type. To create a matchmaking rule set, provide unique rule set name and the rule set body in JSON format. Rule sets must be defined in the same Region as the matchmaking configuration they are used with. Since matchmaking rule sets cannot be edited, it is a good idea to check the rule set syntax using ValidateMatchmakingRuleSet before creating a new rule set. Learn more Build a rule set Design a matchmaker Matchmaking with FlexMatch"]
module CreateMatchmakingConfigurationOutput =
  struct
    type nonrec t =
      {
      configuration: MatchmakingConfiguration.t option
        [@ocaml.doc
          "Object that describes the newly created matchmaking configuration."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?configuration = fun () -> { configuration }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Configuration",
           (Option.map x.configuration ~f:MatchmakingConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuration =
        (Option.map ~f:MatchmakingConfiguration.of_xml)
          (Xml.child xml_arg0 "Configuration") in
      make ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let configuration =
        field_map json__ "Configuration" MatchmakingConfiguration.of_json in
      make ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Defines a new matchmaking configuration for use with FlexMatch. Whether your are using FlexMatch with Amazon GameLift Servers hosting or as a standalone matchmaking service, the matchmaking configuration sets out rules for matching players and forming teams. If you're also using Amazon GameLift Servers hosting, it defines how to start game sessions for each match. Your matchmaking system can use multiple configurations to handle different game scenarios. All matchmaking requests identify the matchmaking configuration to use and provide player attributes consistent with that configuration. To create a matchmaking configuration, you must provide the following: configuration name and FlexMatch mode (with or without Amazon GameLift Servers hosting); a rule set that specifies how to evaluate players and find acceptable matches; whether player acceptance is required; and the maximum time allowed for a matchmaking attempt. When using FlexMatch with Amazon GameLift Servers hosting, you also need to identify the game session queue to use when starting a game session for the match. In addition, you must set up an Amazon Simple Notification Service topic to receive matchmaking notifications. Provide the topic ARN in the matchmaking configuration. Learn more Design a FlexMatch matchmaker Set up FlexMatch event notification"]
module CreateMatchmakingConfigurationInput =
  struct
    type nonrec t =
      {
      name: MatchmakingIdStringModel.t
        [@ocaml.doc
          "A unique identifier for the matchmaking configuration. This name is used to identify the configuration associated with a matchmaking request or ticket."];
      description: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A human-readable description of the matchmaking configuration."];
      gameSessionQueueArns: QueueArnsList.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers game session queue resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::gamesessionqueue/<queue name>. Queues can be located in any Region. Queues are used to start new Amazon GameLift Servers-hosted game sessions for matches that are created with this matchmaking configuration. If FlexMatchMode is set to STANDALONE, do not set this parameter."];
      requestTimeoutSeconds: MatchmakingRequestTimeoutInteger.t
        [@ocaml.doc
          "The maximum duration, in seconds, that a matchmaking ticket can remain in process before timing out. Requests that fail due to timing out can be resubmitted as needed."];
      acceptanceTimeoutSeconds: MatchmakingAcceptanceTimeoutInteger.t option
        [@ocaml.doc
          "The length of time (in seconds) to wait for players to accept a proposed match, if acceptance is required."];
      acceptanceRequired: BooleanModel.t
        [@ocaml.doc
          "A flag that determines whether a match that was created with this configuration must be accepted by the matched players. To require acceptance, set to TRUE. With this option enabled, matchmaking tickets use the status REQUIRES_ACCEPTANCE to indicate when a completed potential match is waiting for player acceptance."];
      ruleSetName: MatchmakingRuleSetName.t
        [@ocaml.doc
          "A unique identifier for the matchmaking rule set to use with this configuration. You can use either the rule set name or ARN value. A matchmaking configuration can only use rule sets that are defined in the same Region."];
      notificationTarget: SnsArnStringModel.t option
        [@ocaml.doc
          "An SNS topic ARN that is set up to receive matchmaking notifications. See Setting up notifications for matchmaking for more information."];
      additionalPlayerCount: WholeNumber.t option
        [@ocaml.doc
          "The number of player slots in a match to keep open for future players. For example, if the configuration's rule set specifies a match for a single 12-person team, and the additional player count is set to 2, only 10 players are selected for the match. This parameter is not used if FlexMatchMode is set to STANDALONE."];
      customEventData: CustomEventData.t option
        [@ocaml.doc
          "Information to be added to all events related to this matchmaking configuration."];
      gameProperties: GamePropertyList.t option
        [@ocaml.doc
          "A set of key-value pairs that can store custom data in a game session. For example: \\{\"Key\": \"difficulty\", \"Value\": \"novice\"\\}. This information is added to the new GameSession object that is created for a successful match. This parameter is not used if FlexMatchMode is set to STANDALONE. Avoid using periods (\".\") in property keys if you plan to search for game sessions by properties. Property keys containing periods cannot be searched and will be filtered out from search results due to search index limitations. If you use SearchGameSessions API, there is a limit of 500 game property keys across all game sessions and all fleets per region. If the limit is exceeded, there will potentially be game session entries missing from SearchGameSessions API results."];
      gameSessionData: GameSessionData.t option
        [@ocaml.doc
          "A set of custom game session properties, formatted as a single string value. This data is passed to a game server process with a request to start a new game session. For more information, see Start a game session. This information is added to the new GameSession object that is created for a successful match. This parameter is not used if FlexMatchMode is set to STANDALONE."];
      backfillMode: BackfillMode.t option
        [@ocaml.doc
          "The method used to backfill game sessions that are created with this matchmaking configuration. Specify MANUAL when your game manages backfill requests manually or does not use the match backfill feature. Specify AUTOMATIC to have Amazon GameLift Servers create a backfill request whenever a game session has one or more open slots. Learn more about manual and automatic backfill in Backfill Existing Games with FlexMatch. Automatic backfill is not available when FlexMatchMode is set to STANDALONE."];
      flexMatchMode: FlexMatchMode.t option
        [@ocaml.doc
          "Indicates whether this matchmaking configuration is being used with Amazon GameLift Servers hosting or as a standalone matchmaking solution. STANDALONE - FlexMatch forms matches and returns match information, including players and team assignments, in a MatchmakingSucceeded event. WITH_QUEUE - FlexMatch forms matches and uses the specified Amazon GameLift Servers queue to start a game session for the match."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of labels to assign to the new matchmaking configuration resource. Tags are developer-defined key-value pairs. Tagging Amazon Web Services resources are useful for resource management, access management and cost allocation. For more information, see Tagging Amazon Web Services Resources in the Amazon Web Services General Reference."]}
    let context_ = "CreateMatchmakingConfigurationInput"
    let make ?description =
      fun ?gameSessionQueueArns ->
        fun ?acceptanceTimeoutSeconds ->
          fun ?notificationTarget ->
            fun ?additionalPlayerCount ->
              fun ?customEventData ->
                fun ?gameProperties ->
                  fun ?gameSessionData ->
                    fun ?backfillMode ->
                      fun ?flexMatchMode ->
                        fun ?tags ->
                          fun ~name ->
                            fun ~requestTimeoutSeconds ->
                              fun ~acceptanceRequired ->
                                fun ~ruleSetName ->
                                  fun () ->
                                    {
                                      description;
                                      gameSessionQueueArns;
                                      acceptanceTimeoutSeconds;
                                      notificationTarget;
                                      additionalPlayerCount;
                                      customEventData;
                                      gameProperties;
                                      gameSessionData;
                                      backfillMode;
                                      flexMatchMode;
                                      tags;
                                      name;
                                      requestTimeoutSeconds;
                                      acceptanceRequired;
                                      ruleSetName
                                    }
    let to_value x =
      structure_to_value
        [("Name", (Some (MatchmakingIdStringModel.to_value x.name)));
        ("Description",
          (Option.map x.description ~f:NonZeroAndMaxString.to_value));
        ("GameSessionQueueArns",
          (Option.map x.gameSessionQueueArns ~f:QueueArnsList.to_value));
        ("RequestTimeoutSeconds",
          (Some
             (MatchmakingRequestTimeoutInteger.to_value
                x.requestTimeoutSeconds)));
        ("AcceptanceTimeoutSeconds",
          (Option.map x.acceptanceTimeoutSeconds
             ~f:MatchmakingAcceptanceTimeoutInteger.to_value));
        ("AcceptanceRequired",
          (Some (BooleanModel.to_value x.acceptanceRequired)));
        ("RuleSetName",
          (Some (MatchmakingRuleSetName.to_value x.ruleSetName)));
        ("NotificationTarget",
          (Option.map x.notificationTarget ~f:SnsArnStringModel.to_value));
        ("AdditionalPlayerCount",
          (Option.map x.additionalPlayerCount ~f:WholeNumber.to_value));
        ("CustomEventData",
          (Option.map x.customEventData ~f:CustomEventData.to_value));
        ("GameProperties",
          (Option.map x.gameProperties ~f:GamePropertyList.to_value));
        ("GameSessionData",
          (Option.map x.gameSessionData ~f:GameSessionData.to_value));
        ("BackfillMode",
          (Option.map x.backfillMode ~f:BackfillMode.to_value));
        ("FlexMatchMode",
          (Option.map x.flexMatchMode ~f:FlexMatchMode.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let flexMatchMode =
        (Option.map ~f:FlexMatchMode.of_xml)
          (Xml.child xml_arg0 "FlexMatchMode") in
      let backfillMode =
        (Option.map ~f:BackfillMode.of_xml)
          (Xml.child xml_arg0 "BackfillMode") in
      let gameSessionData =
        (Option.map ~f:GameSessionData.of_xml)
          (Xml.child xml_arg0 "GameSessionData") in
      let gameProperties =
        (Option.map ~f:GamePropertyList.of_xml)
          (Xml.child xml_arg0 "GameProperties") in
      let customEventData =
        (Option.map ~f:CustomEventData.of_xml)
          (Xml.child xml_arg0 "CustomEventData") in
      let additionalPlayerCount =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "AdditionalPlayerCount") in
      let notificationTarget =
        (Option.map ~f:SnsArnStringModel.of_xml)
          (Xml.child xml_arg0 "NotificationTarget") in
      let ruleSetName =
        MatchmakingRuleSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleSetName") in
      let acceptanceRequired =
        BooleanModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AcceptanceRequired") in
      let acceptanceTimeoutSeconds =
        (Option.map ~f:MatchmakingAcceptanceTimeoutInteger.of_xml)
          (Xml.child xml_arg0 "AcceptanceTimeoutSeconds") in
      let requestTimeoutSeconds =
        MatchmakingRequestTimeoutInteger.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RequestTimeoutSeconds") in
      let gameSessionQueueArns =
        (Option.map ~f:QueueArnsList.of_xml)
          (Xml.child xml_arg0 "GameSessionQueueArns") in
      let description =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        MatchmakingIdStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?tags ?flexMatchMode ?backfillMode ?gameSessionData
        ?gameProperties ?customEventData ?additionalPlayerCount
        ?notificationTarget ~ruleSetName ~acceptanceRequired
        ?acceptanceTimeoutSeconds ~requestTimeoutSeconds
        ?gameSessionQueueArns ?description ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagList.of_json in
      let flexMatchMode =
        field_map json__ "FlexMatchMode" FlexMatchMode.of_json in
      let backfillMode = field_map json__ "BackfillMode" BackfillMode.of_json in
      let gameSessionData =
        field_map json__ "GameSessionData" GameSessionData.of_json in
      let gameProperties =
        field_map json__ "GameProperties" GamePropertyList.of_json in
      let customEventData =
        field_map json__ "CustomEventData" CustomEventData.of_json in
      let additionalPlayerCount =
        field_map json__ "AdditionalPlayerCount" WholeNumber.of_json in
      let notificationTarget =
        field_map json__ "NotificationTarget" SnsArnStringModel.of_json in
      let ruleSetName =
        field_map_exn json__ "RuleSetName" MatchmakingRuleSetName.of_json in
      let acceptanceRequired =
        field_map_exn json__ "AcceptanceRequired" BooleanModel.of_json in
      let acceptanceTimeoutSeconds =
        field_map json__ "AcceptanceTimeoutSeconds"
          MatchmakingAcceptanceTimeoutInteger.of_json in
      let requestTimeoutSeconds =
        field_map_exn json__ "RequestTimeoutSeconds"
          MatchmakingRequestTimeoutInteger.of_json in
      let gameSessionQueueArns =
        field_map json__ "GameSessionQueueArns" QueueArnsList.of_json in
      let description =
        field_map json__ "Description" NonZeroAndMaxString.of_json in
      let name = field_map_exn json__ "Name" MatchmakingIdStringModel.of_json in
      make ?tags ?flexMatchMode ?backfillMode ?gameSessionData
        ?gameProperties ?customEventData ?additionalPlayerCount
        ?notificationTarget ~ruleSetName ~acceptanceRequired
        ?acceptanceTimeoutSeconds ~requestTimeoutSeconds
        ?gameSessionQueueArns ?description ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Defines a new matchmaking configuration for use with FlexMatch. Whether your are using FlexMatch with Amazon GameLift Servers hosting or as a standalone matchmaking service, the matchmaking configuration sets out rules for matching players and forming teams. If you're also using Amazon GameLift Servers hosting, it defines how to start game sessions for each match. Your matchmaking system can use multiple configurations to handle different game scenarios. All matchmaking requests identify the matchmaking configuration to use and provide player attributes consistent with that configuration. To create a matchmaking configuration, you must provide the following: configuration name and FlexMatch mode (with or without Amazon GameLift Servers hosting); a rule set that specifies how to evaluate players and find acceptable matches; whether player acceptance is required; and the maximum time allowed for a matchmaking attempt. When using FlexMatch with Amazon GameLift Servers hosting, you also need to identify the game session queue to use when starting a game session for the match. In addition, you must set up an Amazon Simple Notification Service topic to receive matchmaking notifications. Provide the topic ARN in the matchmaking configuration. Learn more Design a FlexMatch matchmaker Set up FlexMatch event notification"]
module CreateLocationOutput =
  struct
    type nonrec t =
      {
      location: LocationModel.t option
        [@ocaml.doc "The details of the custom location you created."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?location = fun () -> { location }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Location", (Option.map x.location ~f:LocationModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationModel.of_xml) (Xml.child xml_arg0 "Location") in
      make ?location ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationModel.of_json in
      make ?location ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Anywhere Creates a custom location for use in an Anywhere fleet."]
module CreateLocationInput =
  struct
    type nonrec t =
      {
      locationName: CustomInputLocationStringModel.t
        [@ocaml.doc "A descriptive name for the custom location."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of labels to assign to the new resource. Tags are developer-defined key-value pairs. Tagging Amazon Web Services resources are useful for resource management, access management, and cost allocation. For more information, see Tagging Amazon Web Services Resources in the Amazon Web Services General Rareference."]}
    let context_ = "CreateLocationInput"
    let make ?tags = fun ~locationName -> fun () -> { tags; locationName }
    let to_value x =
      structure_to_value
        [("LocationName",
           (Some (CustomInputLocationStringModel.to_value x.locationName)));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let locationName =
        CustomInputLocationStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LocationName") in
      make ?tags ~locationName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagList.of_json in
      let locationName =
        field_map_exn json__ "LocationName"
          CustomInputLocationStringModel.of_json in
      make ?tags ~locationName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Anywhere Creates a custom location for use in an Anywhere fleet."]
module CreateGameSessionQueueOutput =
  struct
    type nonrec t =
      {
      gameSessionQueue: GameSessionQueue.t option
        [@ocaml.doc
          "An object that describes the newly created game session queue."]}
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameSessionQueue = fun () -> { gameSessionQueue }
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameSessionQueue",
           (Option.map x.gameSessionQueue ~f:GameSessionQueue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameSessionQueue =
        (Option.map ~f:GameSessionQueue.of_xml)
          (Xml.child xml_arg0 "GameSessionQueue") in
      make ?gameSessionQueue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameSessionQueue =
        field_map json__ "GameSessionQueue" GameSessionQueue.of_json in
      make ?gameSessionQueue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Creates a placement queue that processes requests for new game sessions. A queue uses FleetIQ algorithms to locate the best available placement locations for a new game session, and then prompts the game server process to start a new game session. A game session queue is configured with a set of destinations (Amazon GameLift Servers fleets or aliases) that determine where the queue can place new game sessions. These destinations can span multiple Amazon Web Services Regions, can use different instance types, and can include both Spot and On-Demand fleets. If the queue includes multi-location fleets, the queue can place game sessions in any of a fleet's remote locations. You can configure a queue to determine how it selects the best available placement for a new game session. Queues can prioritize placement decisions based on a combination of location, hosting cost, and player latency. You can set up the queue to use the default prioritization or provide alternate instructions using PriorityConfiguration. Request options Use this operation to make these common types of requests. Create a queue with the minimum required parameters. Name Destinations (This parameter isn't required, but a queue can't make placements without at least one destination.) Create a queue with placement notification. Queues that have high placement activity must use a notification system, such as with Amazon Simple Notification Service (Amazon SNS) or Amazon CloudWatch. Required parameters Name and Destinations NotificationTarget Create a queue with custom prioritization settings. These custom settings replace the default prioritization configuration for a queue. Required parameters Name and Destinations PriorityConfiguration Create a queue with special rules for processing player latency data. Required parameters Name and Destinations PlayerLatencyPolicies Results If successful, this operation returns a new GameSessionQueue object with an assigned queue ARN. Use the queue's name or ARN when submitting new game session requests with StartGameSessionPlacement or StartMatchmaking. Learn more Design a game session queue Create a game session queue Related actions CreateGameSessionQueue | DescribeGameSessionQueues | UpdateGameSessionQueue | DeleteGameSessionQueue | All APIs by task"]
module CreateGameSessionQueueInput =
  struct
    type nonrec t =
      {
      name: GameSessionQueueName.t
        [@ocaml.doc
          "A descriptive label that is associated with game session queue. Queue names must be unique within each Region."];
      timeoutInSeconds: WholeNumber.t option
        [@ocaml.doc
          "The maximum time, in seconds, that a new game session placement request remains in the queue. When a request exceeds this time, the game session placement changes to a TIMED_OUT status. If you don't specify a request timeout, the queue uses a default value. The minimum value is 10 and the maximum value is 600."];
      playerLatencyPolicies: PlayerLatencyPolicyList.t option
        [@ocaml.doc
          "A set of policies that enforce a sliding cap on player latency when processing game sessions placement requests. Use multiple policies to gradually relax the cap over time if Amazon GameLift Servers can't make a placement. Policies are evaluated in order starting with the lowest maximum latency value."];
      destinations: GameSessionQueueDestinationList.t option
        [@ocaml.doc
          "A list of fleets and/or fleet aliases that can be used to fulfill game session placement requests in the queue. Destinations are identified by either a fleet ARN or a fleet alias ARN, and are listed in order of placement preference."];
      filterConfiguration: FilterConfiguration.t option
        [@ocaml.doc
          "A list of locations where a queue is allowed to place new game sessions. Locations are specified in the form of Amazon Web Services Region codes, such as us-west-2. If this parameter is not set, game sessions can be placed in any queue location."];
      priorityConfiguration: PriorityConfiguration.t option
        [@ocaml.doc
          "Custom settings to use when prioritizing destinations and locations for game session placements. This configuration replaces the FleetIQ default prioritization process. Priority types that are not explicitly named will be automatically applied at the end of the prioritization process."];
      customEventData: QueueCustomEventData.t option
        [@ocaml.doc
          "Information to be added to all events that are related to this game session queue."];
      notificationTarget: QueueSnsArnStringModel.t option
        [@ocaml.doc
          "An SNS topic ARN that is set up to receive game session placement notifications. See Setting up notifications for game session placement."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of labels to assign to the new game session queue resource. Tags are developer-defined key-value pairs. Tagging Amazon Web Services resources are useful for resource management, access management and cost allocation. For more information, see Tagging Amazon Web Services Resources in the Amazon Web Services General Reference."]}
    let context_ = "CreateGameSessionQueueInput"
    let make ?timeoutInSeconds =
      fun ?playerLatencyPolicies ->
        fun ?destinations ->
          fun ?filterConfiguration ->
            fun ?priorityConfiguration ->
              fun ?customEventData ->
                fun ?notificationTarget ->
                  fun ?tags ->
                    fun ~name ->
                      fun () ->
                        {
                          timeoutInSeconds;
                          playerLatencyPolicies;
                          destinations;
                          filterConfiguration;
                          priorityConfiguration;
                          customEventData;
                          notificationTarget;
                          tags;
                          name
                        }
    let to_value x =
      structure_to_value
        [("Name", (Some (GameSessionQueueName.to_value x.name)));
        ("TimeoutInSeconds",
          (Option.map x.timeoutInSeconds ~f:WholeNumber.to_value));
        ("PlayerLatencyPolicies",
          (Option.map x.playerLatencyPolicies
             ~f:PlayerLatencyPolicyList.to_value));
        ("Destinations",
          (Option.map x.destinations
             ~f:GameSessionQueueDestinationList.to_value));
        ("FilterConfiguration",
          (Option.map x.filterConfiguration ~f:FilterConfiguration.to_value));
        ("PriorityConfiguration",
          (Option.map x.priorityConfiguration
             ~f:PriorityConfiguration.to_value));
        ("CustomEventData",
          (Option.map x.customEventData ~f:QueueCustomEventData.to_value));
        ("NotificationTarget",
          (Option.map x.notificationTarget ~f:QueueSnsArnStringModel.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let notificationTarget =
        (Option.map ~f:QueueSnsArnStringModel.of_xml)
          (Xml.child xml_arg0 "NotificationTarget") in
      let customEventData =
        (Option.map ~f:QueueCustomEventData.of_xml)
          (Xml.child xml_arg0 "CustomEventData") in
      let priorityConfiguration =
        (Option.map ~f:PriorityConfiguration.of_xml)
          (Xml.child xml_arg0 "PriorityConfiguration") in
      let filterConfiguration =
        (Option.map ~f:FilterConfiguration.of_xml)
          (Xml.child xml_arg0 "FilterConfiguration") in
      let destinations =
        (Option.map ~f:GameSessionQueueDestinationList.of_xml)
          (Xml.child xml_arg0 "Destinations") in
      let playerLatencyPolicies =
        (Option.map ~f:PlayerLatencyPolicyList.of_xml)
          (Xml.child xml_arg0 "PlayerLatencyPolicies") in
      let timeoutInSeconds =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "TimeoutInSeconds") in
      let name =
        GameSessionQueueName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?tags ?notificationTarget ?customEventData ?priorityConfiguration
        ?filterConfiguration ?destinations ?playerLatencyPolicies
        ?timeoutInSeconds ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagList.of_json in
      let notificationTarget =
        field_map json__ "NotificationTarget" QueueSnsArnStringModel.of_json in
      let customEventData =
        field_map json__ "CustomEventData" QueueCustomEventData.of_json in
      let priorityConfiguration =
        field_map json__ "PriorityConfiguration"
          PriorityConfiguration.of_json in
      let filterConfiguration =
        field_map json__ "FilterConfiguration" FilterConfiguration.of_json in
      let destinations =
        field_map json__ "Destinations"
          GameSessionQueueDestinationList.of_json in
      let playerLatencyPolicies =
        field_map json__ "PlayerLatencyPolicies"
          PlayerLatencyPolicyList.of_json in
      let timeoutInSeconds =
        field_map json__ "TimeoutInSeconds" WholeNumber.of_json in
      let name = field_map_exn json__ "Name" GameSessionQueueName.of_json in
      make ?tags ?notificationTarget ?customEventData ?priorityConfiguration
        ?filterConfiguration ?destinations ?playerLatencyPolicies
        ?timeoutInSeconds ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Creates a placement queue that processes requests for new game sessions. A queue uses FleetIQ algorithms to locate the best available placement locations for a new game session, and then prompts the game server process to start a new game session. A game session queue is configured with a set of destinations (Amazon GameLift Servers fleets or aliases) that determine where the queue can place new game sessions. These destinations can span multiple Amazon Web Services Regions, can use different instance types, and can include both Spot and On-Demand fleets. If the queue includes multi-location fleets, the queue can place game sessions in any of a fleet's remote locations. You can configure a queue to determine how it selects the best available placement for a new game session. Queues can prioritize placement decisions based on a combination of location, hosting cost, and player latency. You can set up the queue to use the default prioritization or provide alternate instructions using PriorityConfiguration. Request options Use this operation to make these common types of requests. Create a queue with the minimum required parameters. Name Destinations (This parameter isn't required, but a queue can't make placements without at least one destination.) Create a queue with placement notification. Queues that have high placement activity must use a notification system, such as with Amazon Simple Notification Service (Amazon SNS) or Amazon CloudWatch. Required parameters Name and Destinations NotificationTarget Create a queue with custom prioritization settings. These custom settings replace the default prioritization configuration for a queue. Required parameters Name and Destinations PriorityConfiguration Create a queue with special rules for processing player latency data. Required parameters Name and Destinations PlayerLatencyPolicies Results If successful, this operation returns a new GameSessionQueue object with an assigned queue ARN. Use the queue's name or ARN when submitting new game session requests with StartGameSessionPlacement or StartMatchmaking. Learn more Design a game session queue Create a game session queue Related actions CreateGameSessionQueue | DescribeGameSessionQueues | UpdateGameSessionQueue | DeleteGameSessionQueue | All APIs by task"]
module CreateGameSessionOutput =
  struct
    type nonrec t =
      {
      gameSession: GameSession.t option
        [@ocaml.doc
          "Object that describes the newly created game session record."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `FleetCapacityExceededException of FleetCapacityExceededException.t 
      | `IdempotentParameterMismatchException of
          IdempotentParameterMismatchException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidFleetStatusException of InvalidFleetStatusException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `TerminalRoutingStrategyException of
          TerminalRoutingStrategyException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameSession = fun () -> { gameSession }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "FleetCapacityExceededException" ->
          `FleetCapacityExceededException
            (FleetCapacityExceededException.of_json json)
      | "IdempotentParameterMismatchException" ->
          `IdempotentParameterMismatchException
            (IdempotentParameterMismatchException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidFleetStatusException" ->
          `InvalidFleetStatusException
            (InvalidFleetStatusException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "FleetCapacityExceededException" ->
          `FleetCapacityExceededException
            (FleetCapacityExceededException.of_xml xml)
      | "IdempotentParameterMismatchException" ->
          `IdempotentParameterMismatchException
            (IdempotentParameterMismatchException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidFleetStatusException" ->
          `InvalidFleetStatusException
            (InvalidFleetStatusException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TerminalRoutingStrategyException" ->
          `TerminalRoutingStrategyException
            (TerminalRoutingStrategyException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `FleetCapacityExceededException e ->
          `Assoc
            [("error", (`String "FleetCapacityExceededException"));
            ("details", (FleetCapacityExceededException.to_json e))]
      | `IdempotentParameterMismatchException e ->
          `Assoc
            [("error", (`String "IdempotentParameterMismatchException"));
            ("details", (IdempotentParameterMismatchException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidFleetStatusException e ->
          `Assoc
            [("error", (`String "InvalidFleetStatusException"));
            ("details", (InvalidFleetStatusException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TerminalRoutingStrategyException e ->
          `Assoc
            [("error", (`String "TerminalRoutingStrategyException"));
            ("details", (TerminalRoutingStrategyException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameSession", (Option.map x.gameSession ~f:GameSession.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameSession =
        (Option.map ~f:GameSession.of_xml) (Xml.child xml_arg0 "GameSession") in
      make ?gameSession ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameSession = field_map json__ "GameSession" GameSession.of_json in
      make ?gameSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Creates a multiplayer game session for players in a specific fleet location. This operation prompts an available server process to start a game session and retrieves connection information for the new game session. As an alternative, consider using the Amazon GameLift Servers game session placement feature with StartGameSessionPlacement, which uses the FleetIQ algorithm and queues to optimize the placement process. When creating a game session, you specify exactly where you want to place it and provide a set of game session configuration settings. The target fleet must be in ACTIVE status. You can use this operation in the following ways: To create a game session on an instance in a fleet's home Region, provide a fleet or alias ID along with your game session configuration. To create a game session on an instance in a fleet's remote location, provide a fleet or alias ID and a location name, along with your game session configuration. To create a game session on an instance in an Anywhere fleet, specify the fleet's custom location. If successful, Amazon GameLift Servers initiates a workflow to start a new game session and returns a GameSession object containing the game session configuration and status. When the game session status is ACTIVE, it is updated with connection information and you can create player sessions for the game session. By default, newly created game sessions are open to new players. You can restrict new player access by using UpdateGameSession to change the game session's player session creation policy. Amazon GameLift Servers retains logs for active for 14 days. To access the logs, call GetGameSessionLogUrl to download the log files. Available in Amazon GameLift Servers Local. Learn more Start a game session All APIs by task"]
module CreateGameSessionInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the fleet to create a game session in. You can use either the fleet ID or ARN value. Each request must reference either a fleet ID or alias ID, but not both."];
      aliasId: AliasIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the alias associated with the fleet to create a game session in. You can use either the alias ID or ARN value. Each request must reference either a fleet ID or alias ID, but not both."];
      maximumPlayerSessionCount: WholeNumber.t
        [@ocaml.doc
          "The maximum number of players that can be connected simultaneously to the game session."];
      name: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a game session. Session names do not need to be unique."];
      gameProperties: GamePropertyList.t option
        [@ocaml.doc
          "A set of key-value pairs that can store custom data in a game session. For example: \\{\"Key\": \"difficulty\", \"Value\": \"novice\"\\}. For an example, see Create a game session with custom properties. Avoid using periods (\".\") in property keys if you plan to search for game sessions by properties. Property keys containing periods cannot be searched and will be filtered out from search results due to search index limitations. If you use SearchGameSessions API, there is a limit of 500 game property keys across all game sessions and all fleets per region. If the limit is exceeded, there will potentially be game session entries missing from SearchGameSessions API results."];
      creatorId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A unique identifier for a player or entity creating the game session. If you add a resource creation limit policy to a fleet, the CreateGameSession operation requires a CreatorId. Amazon GameLift Servers limits the number of game session creation requests with the same CreatorId in a specified time period. If you your fleet doesn't have a resource creation limit policy and you provide a CreatorId in your CreateGameSession requests, Amazon GameLift Servers limits requests to one request per CreatorId per second. To not limit CreateGameSession requests with the same CreatorId, don't provide a CreatorId in your CreateGameSession request."];
      gameSessionId: IdStringModel.t option
        [@ocaml.doc
          "This parameter is deprecated. Use IdempotencyToken instead. Custom string that uniquely identifies a request for a new game session. Maximum token length is 48 characters. If provided, this string is included in the new game session's ID."];
      idempotencyToken: IdStringModel.t option
        [@ocaml.doc
          "Custom string that uniquely identifies the new game session request. This is useful for ensuring that game session requests with the same idempotency token are processed only once. Subsequent requests with the same string return the original GameSession object, with an updated status. Maximum token length is 48 characters. If provided, this string is included in the new game session's ID. The value is always a full ARN in the following format: arn:aws:gamelift:<location>::gamesession/<fleet ID>/<ID string>. Idempotency tokens remain in use for 30 days after a game session has ended; game session objects are retained for this time period and then deleted."];
      gameSessionData: LargeGameSessionData.t option
        [@ocaml.doc
          "A set of custom game session properties, formatted as a single string value. This data is passed to a game server process with a request to start a new game session. For more information, see Start a game session."];
      location: LocationStringModel.t option
        [@ocaml.doc
          "A fleet's remote location to place the new game session in. If this parameter is not set, the new game session is placed in the fleet's home Region. Specify a remote location with an Amazon Web Services Region code such as us-west-2. When using an Anywhere fleet, this parameter is required and must be set to the Anywhere fleet's custom location."]}
    let context_ = "CreateGameSessionInput"
    let make ?fleetId =
      fun ?aliasId ->
        fun ?name ->
          fun ?gameProperties ->
            fun ?creatorId ->
              fun ?gameSessionId ->
                fun ?idempotencyToken ->
                  fun ?gameSessionData ->
                    fun ?location ->
                      fun ~maximumPlayerSessionCount ->
                        fun () ->
                          {
                            fleetId;
                            aliasId;
                            name;
                            gameProperties;
                            creatorId;
                            gameSessionId;
                            idempotencyToken;
                            gameSessionData;
                            location;
                            maximumPlayerSessionCount
                          }
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetIdOrArn.to_value));
        ("AliasId", (Option.map x.aliasId ~f:AliasIdOrArn.to_value));
        ("MaximumPlayerSessionCount",
          (Some (WholeNumber.to_value x.maximumPlayerSessionCount)));
        ("Name", (Option.map x.name ~f:NonZeroAndMaxString.to_value));
        ("GameProperties",
          (Option.map x.gameProperties ~f:GamePropertyList.to_value));
        ("CreatorId",
          (Option.map x.creatorId ~f:NonZeroAndMaxString.to_value));
        ("GameSessionId",
          (Option.map x.gameSessionId ~f:IdStringModel.to_value));
        ("IdempotencyToken",
          (Option.map x.idempotencyToken ~f:IdStringModel.to_value));
        ("GameSessionData",
          (Option.map x.gameSessionData ~f:LargeGameSessionData.to_value));
        ("Location", (Option.map x.location ~f:LocationStringModel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:LocationStringModel.of_xml)
          (Xml.child xml_arg0 "Location") in
      let gameSessionData =
        (Option.map ~f:LargeGameSessionData.of_xml)
          (Xml.child xml_arg0 "GameSessionData") in
      let idempotencyToken =
        (Option.map ~f:IdStringModel.of_xml)
          (Xml.child xml_arg0 "IdempotencyToken") in
      let gameSessionId =
        (Option.map ~f:IdStringModel.of_xml)
          (Xml.child xml_arg0 "GameSessionId") in
      let creatorId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "CreatorId") in
      let gameProperties =
        (Option.map ~f:GamePropertyList.of_xml)
          (Xml.child xml_arg0 "GameProperties") in
      let name =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Name") in
      let maximumPlayerSessionCount =
        WholeNumber.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "MaximumPlayerSessionCount") in
      let aliasId =
        (Option.map ~f:AliasIdOrArn.of_xml) (Xml.child xml_arg0 "AliasId") in
      let fleetId =
        (Option.map ~f:FleetIdOrArn.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?location ?gameSessionData ?idempotencyToken ?gameSessionId
        ?creatorId ?gameProperties ?name ~maximumPlayerSessionCount ?aliasId
        ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "Location" LocationStringModel.of_json in
      let gameSessionData =
        field_map json__ "GameSessionData" LargeGameSessionData.of_json in
      let idempotencyToken =
        field_map json__ "IdempotencyToken" IdStringModel.of_json in
      let gameSessionId =
        field_map json__ "GameSessionId" IdStringModel.of_json in
      let creatorId =
        field_map json__ "CreatorId" NonZeroAndMaxString.of_json in
      let gameProperties =
        field_map json__ "GameProperties" GamePropertyList.of_json in
      let name = field_map json__ "Name" NonZeroAndMaxString.of_json in
      let maximumPlayerSessionCount =
        field_map_exn json__ "MaximumPlayerSessionCount" WholeNumber.of_json in
      let aliasId = field_map json__ "AliasId" AliasIdOrArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetIdOrArn.of_json in
      make ?location ?gameSessionData ?idempotencyToken ?gameSessionId
        ?creatorId ?gameProperties ?name ~maximumPlayerSessionCount ?aliasId
        ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Creates a multiplayer game session for players in a specific fleet location. This operation prompts an available server process to start a game session and retrieves connection information for the new game session. As an alternative, consider using the Amazon GameLift Servers game session placement feature with StartGameSessionPlacement, which uses the FleetIQ algorithm and queues to optimize the placement process. When creating a game session, you specify exactly where you want to place it and provide a set of game session configuration settings. The target fleet must be in ACTIVE status. You can use this operation in the following ways: To create a game session on an instance in a fleet's home Region, provide a fleet or alias ID along with your game session configuration. To create a game session on an instance in a fleet's remote location, provide a fleet or alias ID and a location name, along with your game session configuration. To create a game session on an instance in an Anywhere fleet, specify the fleet's custom location. If successful, Amazon GameLift Servers initiates a workflow to start a new game session and returns a GameSession object containing the game session configuration and status. When the game session status is ACTIVE, it is updated with connection information and you can create player sessions for the game session. By default, newly created game sessions are open to new players. You can restrict new player access by using UpdateGameSession to change the game session's player session creation policy. Amazon GameLift Servers retains logs for active for 14 days. To access the logs, call GetGameSessionLogUrl to download the log files. Available in Amazon GameLift Servers Local. Learn more Start a game session All APIs by task"]
module CreateGameServerGroupOutput =
  struct
    type nonrec t =
      {
      gameServerGroup: GameServerGroup.t option
        [@ocaml.doc
          "The newly created game server group object, including the new ARN value for the Amazon GameLift Servers FleetIQ game server group and the object's status. The Amazon EC2 Auto Scaling group ARN is initially null, since the group has not yet been created. This value is added once the game server group status reaches ACTIVE."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameServerGroup = fun () -> { gameServerGroup }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameServerGroup",
           (Option.map x.gameServerGroup ~f:GameServerGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServerGroup =
        (Option.map ~f:GameServerGroup.of_xml)
          (Xml.child xml_arg0 "GameServerGroup") in
      make ?gameServerGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServerGroup =
        field_map json__ "GameServerGroup" GameServerGroup.of_json in
      make ?gameServerGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Creates a Amazon GameLift Servers FleetIQ game server group for managing game hosting on a collection of Amazon Elastic Compute Cloud instances for game hosting. This operation creates the game server group, creates an Auto Scaling group in your Amazon Web Services account, and establishes a link between the two groups. You can view the status of your game server groups in the Amazon GameLift Servers console. Game server group metrics and events are emitted to Amazon CloudWatch. Before creating a new game server group, you must have the following: An Amazon Elastic Compute Cloud launch template that specifies how to launch Amazon Elastic Compute Cloud instances with your game server build. For more information, see Launching an Instance from a Launch Template in the Amazon Elastic Compute Cloud User Guide. An IAM role that extends limited access to your Amazon Web Services account to allow Amazon GameLift Servers FleetIQ to create and interact with the Auto Scaling group. For more information, see Create IAM roles for cross-service interaction in the Amazon GameLift Servers FleetIQ Developer Guide. To create a new game server group, specify a unique group name, IAM role and Amazon Elastic Compute Cloud launch template, and provide a list of instance types that can be used in the group. You must also set initial maximum and minimum limits on the group's instance count. You can optionally set an Auto Scaling policy with target tracking based on a Amazon GameLift Servers FleetIQ metric. Once the game server group and corresponding Auto Scaling group are created, you have full access to change the Auto Scaling group's configuration as needed. Several properties that are set when creating a game server group, including maximum/minimum size and auto-scaling policy settings, must be updated directly in the Auto Scaling group. Keep in mind that some Auto Scaling group properties are periodically updated by Amazon GameLift Servers FleetIQ as part of its balancing activities to optimize for availability and cost. Learn more Amazon GameLift Servers FleetIQ Guide"]
module CreateGameServerGroupInput =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupName.t
        [@ocaml.doc
          "An identifier for the new game server group. This value is used to generate unique ARN identifiers for the Amazon EC2 Auto Scaling group and the Amazon GameLift Servers FleetIQ game server group. The name must be unique per Region per Amazon Web Services account."];
      roleArn: IamRoleArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) for an IAM role that allows Amazon GameLift Servers to access your Amazon EC2 Auto Scaling groups."];
      minSize: WholeNumber.t
        [@ocaml.doc
          "The minimum number of instances allowed in the Amazon EC2 Auto Scaling group. During automatic scaling events, Amazon GameLift Servers FleetIQ and Amazon EC2 do not scale down the group below this minimum. In production, this value should be set to at least 1. After the Auto Scaling group is created, update this value directly in the Auto Scaling group using the Amazon Web Services console or APIs."];
      maxSize: PositiveInteger.t
        [@ocaml.doc
          "The maximum number of instances allowed in the Amazon EC2 Auto Scaling group. During automatic scaling events, Amazon GameLift Servers FleetIQ and EC2 do not scale up the group above this maximum. After the Auto Scaling group is created, update this value directly in the Auto Scaling group using the Amazon Web Services console or APIs."];
      launchTemplate: LaunchTemplateSpecification.t
        [@ocaml.doc
          "The Amazon EC2 launch template that contains configuration settings and game server code to be deployed to all instances in the game server group. You can specify the template using either the template name or ID. For help with creating a launch template, see Creating a Launch Template for an Auto Scaling Group in the Amazon Elastic Compute Cloud Auto Scaling User Guide. After the Auto Scaling group is created, update this value directly in the Auto Scaling group using the Amazon Web Services console or APIs. If you specify network interfaces in your launch template, you must explicitly set the property AssociatePublicIpAddress to \"true\". If no network interface is specified in the launch template, Amazon GameLift Servers FleetIQ uses your account's default VPC."];
      instanceDefinitions: InstanceDefinitions.t
        [@ocaml.doc
          "The Amazon EC2 instance types and sizes to use in the Auto Scaling group. The instance definitions must specify at least two different instance types that are supported by Amazon GameLift Servers FleetIQ. For more information on instance types, see EC2 Instance Types in the Amazon Elastic Compute Cloud User Guide. You can optionally specify capacity weighting for each instance type. If no weight value is specified for an instance type, it is set to the default value \"1\". For more information about capacity weighting, see Instance Weighting for Amazon EC2 Auto Scaling in the Amazon EC2 Auto Scaling User Guide."];
      autoScalingPolicy: GameServerGroupAutoScalingPolicy.t option
        [@ocaml.doc
          "Configuration settings to define a scaling policy for the Auto Scaling group that is optimized for game hosting. The scaling policy uses the metric \"PercentUtilizedGameServers\" to maintain a buffer of idle game servers that can immediately accommodate new games and players. After the Auto Scaling group is created, update this value directly in the Auto Scaling group using the Amazon Web Services console or APIs."];
      balancingStrategy: BalancingStrategy.t option
        [@ocaml.doc
          "Indicates how Amazon GameLift Servers FleetIQ balances the use of Spot Instances and On-Demand Instances in the game server group. Method options include the following: SPOT_ONLY - Only Spot Instances are used in the game server group. If Spot Instances are unavailable or not viable for game hosting, the game server group provides no hosting capacity until Spot Instances can again be used. Until then, no new instances are started, and the existing nonviable Spot Instances are terminated (after current gameplay ends) and are not replaced. SPOT_PREFERRED - (default value) Spot Instances are used whenever available in the game server group. If Spot Instances are unavailable, the game server group continues to provide hosting capacity by falling back to On-Demand Instances. Existing nonviable Spot Instances are terminated (after current gameplay ends) and are replaced with new On-Demand Instances. ON_DEMAND_ONLY - Only On-Demand Instances are used in the game server group. No Spot Instances are used, even when available, while this balancing strategy is in force."];
      gameServerProtectionPolicy: GameServerProtectionPolicy.t option
        [@ocaml.doc
          "A flag that indicates whether instances in the game server group are protected from early termination. Unprotected instances that have active game servers running might be terminated during a scale-down event, causing players to be dropped from the game. Protected instances cannot be terminated while there are active game servers running except in the event of a forced game server group deletion (see ). An exception to this is with Spot Instances, which can be terminated by Amazon Web Services regardless of protection status. This property is set to NO_PROTECTION by default."];
      vpcSubnets: VpcSubnets.t option
        [@ocaml.doc
          "A list of virtual private cloud (VPC) subnets to use with instances in the game server group. By default, all Amazon GameLift Servers FleetIQ-supported Availability Zones are used. You can use this parameter to specify VPCs that you've set up. This property cannot be updated after the game server group is created, and the corresponding Auto Scaling group will always use the property value that is set with this request, even if the Auto Scaling group is updated directly."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of labels to assign to the new game server group resource. Tags are developer-defined key-value pairs. Tagging Amazon Web Services resources is useful for resource management, access management, and cost allocation. For more information, see Tagging Amazon Web Services Resources in the Amazon Web Services General Reference."]}
    let context_ = "CreateGameServerGroupInput"
    let make ?autoScalingPolicy =
      fun ?balancingStrategy ->
        fun ?gameServerProtectionPolicy ->
          fun ?vpcSubnets ->
            fun ?tags ->
              fun ~gameServerGroupName ->
                fun ~roleArn ->
                  fun ~minSize ->
                    fun ~maxSize ->
                      fun ~launchTemplate ->
                        fun ~instanceDefinitions ->
                          fun () ->
                            {
                              autoScalingPolicy;
                              balancingStrategy;
                              gameServerProtectionPolicy;
                              vpcSubnets;
                              tags;
                              gameServerGroupName;
                              roleArn;
                              minSize;
                              maxSize;
                              launchTemplate;
                              instanceDefinitions
                            }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Some (GameServerGroupName.to_value x.gameServerGroupName)));
        ("RoleArn", (Some (IamRoleArn.to_value x.roleArn)));
        ("MinSize", (Some (WholeNumber.to_value x.minSize)));
        ("MaxSize", (Some (PositiveInteger.to_value x.maxSize)));
        ("LaunchTemplate",
          (Some (LaunchTemplateSpecification.to_value x.launchTemplate)));
        ("InstanceDefinitions",
          (Some (InstanceDefinitions.to_value x.instanceDefinitions)));
        ("AutoScalingPolicy",
          (Option.map x.autoScalingPolicy
             ~f:GameServerGroupAutoScalingPolicy.to_value));
        ("BalancingStrategy",
          (Option.map x.balancingStrategy ~f:BalancingStrategy.to_value));
        ("GameServerProtectionPolicy",
          (Option.map x.gameServerProtectionPolicy
             ~f:GameServerProtectionPolicy.to_value));
        ("VpcSubnets", (Option.map x.vpcSubnets ~f:VpcSubnets.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let vpcSubnets =
        (Option.map ~f:VpcSubnets.of_xml) (Xml.child xml_arg0 "VpcSubnets") in
      let gameServerProtectionPolicy =
        (Option.map ~f:GameServerProtectionPolicy.of_xml)
          (Xml.child xml_arg0 "GameServerProtectionPolicy") in
      let balancingStrategy =
        (Option.map ~f:BalancingStrategy.of_xml)
          (Xml.child xml_arg0 "BalancingStrategy") in
      let autoScalingPolicy =
        (Option.map ~f:GameServerGroupAutoScalingPolicy.of_xml)
          (Xml.child xml_arg0 "AutoScalingPolicy") in
      let instanceDefinitions =
        InstanceDefinitions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceDefinitions") in
      let launchTemplate =
        LaunchTemplateSpecification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LaunchTemplate") in
      let maxSize =
        PositiveInteger.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxSize") in
      let minSize =
        WholeNumber.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MinSize") in
      let roleArn =
        IamRoleArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RoleArn") in
      let gameServerGroupName =
        GameServerGroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerGroupName") in
      make ?tags ?vpcSubnets ?gameServerProtectionPolicy ?balancingStrategy
        ?autoScalingPolicy ~instanceDefinitions ~launchTemplate ~maxSize
        ~minSize ~roleArn ~gameServerGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagList.of_json in
      let vpcSubnets = field_map json__ "VpcSubnets" VpcSubnets.of_json in
      let gameServerProtectionPolicy =
        field_map json__ "GameServerProtectionPolicy"
          GameServerProtectionPolicy.of_json in
      let balancingStrategy =
        field_map json__ "BalancingStrategy" BalancingStrategy.of_json in
      let autoScalingPolicy =
        field_map json__ "AutoScalingPolicy"
          GameServerGroupAutoScalingPolicy.of_json in
      let instanceDefinitions =
        field_map_exn json__ "InstanceDefinitions"
          InstanceDefinitions.of_json in
      let launchTemplate =
        field_map_exn json__ "LaunchTemplate"
          LaunchTemplateSpecification.of_json in
      let maxSize = field_map_exn json__ "MaxSize" PositiveInteger.of_json in
      let minSize = field_map_exn json__ "MinSize" WholeNumber.of_json in
      let roleArn = field_map_exn json__ "RoleArn" IamRoleArn.of_json in
      let gameServerGroupName =
        field_map_exn json__ "GameServerGroupName"
          GameServerGroupName.of_json in
      make ?tags ?vpcSubnets ?gameServerProtectionPolicy ?balancingStrategy
        ?autoScalingPolicy ~instanceDefinitions ~launchTemplate ~maxSize
        ~minSize ~roleArn ~gameServerGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Creates a Amazon GameLift Servers FleetIQ game server group for managing game hosting on a collection of Amazon Elastic Compute Cloud instances for game hosting. This operation creates the game server group, creates an Auto Scaling group in your Amazon Web Services account, and establishes a link between the two groups. You can view the status of your game server groups in the Amazon GameLift Servers console. Game server group metrics and events are emitted to Amazon CloudWatch. Before creating a new game server group, you must have the following: An Amazon Elastic Compute Cloud launch template that specifies how to launch Amazon Elastic Compute Cloud instances with your game server build. For more information, see Launching an Instance from a Launch Template in the Amazon Elastic Compute Cloud User Guide. An IAM role that extends limited access to your Amazon Web Services account to allow Amazon GameLift Servers FleetIQ to create and interact with the Auto Scaling group. For more information, see Create IAM roles for cross-service interaction in the Amazon GameLift Servers FleetIQ Developer Guide. To create a new game server group, specify a unique group name, IAM role and Amazon Elastic Compute Cloud launch template, and provide a list of instance types that can be used in the group. You must also set initial maximum and minimum limits on the group's instance count. You can optionally set an Auto Scaling policy with target tracking based on a Amazon GameLift Servers FleetIQ metric. Once the game server group and corresponding Auto Scaling group are created, you have full access to change the Auto Scaling group's configuration as needed. Several properties that are set when creating a game server group, including maximum/minimum size and auto-scaling policy settings, must be updated directly in the Auto Scaling group. Keep in mind that some Auto Scaling group properties are periodically updated by Amazon GameLift Servers FleetIQ as part of its balancing activities to optimize for availability and cost. Learn more Amazon GameLift Servers FleetIQ Guide"]
module CreateFleetOutput =
  struct
    type nonrec t =
      {
      fleetAttributes: FleetAttributes.t option
        [@ocaml.doc
          "The properties for the new fleet, including the current status. All fleets are placed in NEW status on creation."];
      locationStates: LocationStateList.t option
        [@ocaml.doc
          "The fleet's locations and life-cycle status of each location. For new fleets, the status of all locations is set to NEW. During fleet creation, Amazon GameLift Servers updates each location status as instances are deployed there and prepared for game hosting. This list includes an entry for the fleet's home Region. For fleets with no remote locations, only one entry, representing the home Region, is returned."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `NotReadyException of NotReadyException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetAttributes =
      fun ?locationStates -> fun () -> { fleetAttributes; locationStates }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "NotReadyException" ->
          `NotReadyException (NotReadyException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "NotReadyException" ->
          `NotReadyException (NotReadyException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `NotReadyException e ->
          `Assoc
            [("error", (`String "NotReadyException"));
            ("details", (NotReadyException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetAttributes",
           (Option.map x.fleetAttributes ~f:FleetAttributes.to_value));
        ("LocationStates",
          (Option.map x.locationStates ~f:LocationStateList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let locationStates =
        (Option.map ~f:LocationStateList.of_xml)
          (Xml.child xml_arg0 "LocationStates") in
      let fleetAttributes =
        (Option.map ~f:FleetAttributes.of_xml)
          (Xml.child xml_arg0 "FleetAttributes") in
      make ?locationStates ?fleetAttributes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let locationStates =
        field_map json__ "LocationStates" LocationStateList.of_json in
      let fleetAttributes =
        field_map json__ "FleetAttributes" FleetAttributes.of_json in
      make ?locationStates ?fleetAttributes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Creates a fleet of compute resources to host your game servers. Use this operation to set up a fleet for the following compute types: Managed EC2 fleet An EC2 fleet is a set of Amazon Elastic Compute Cloud (Amazon EC2) instances. Your game server build is deployed to each fleet instance. Amazon GameLift Servers manages the fleet's instances and controls the lifecycle of game server processes, which host game sessions for players. EC2 fleets can have instances in multiple locations. Each instance in the fleet is designated a Compute. To create an EC2 fleet, provide these required parameters: Either BuildId or ScriptId ComputeType set to EC2 (the default value) EC2InboundPermissions EC2InstanceType FleetType Name RuntimeConfiguration with at least one ServerProcesses configuration If successful, this operation creates a new fleet resource and places it in NEW status while Amazon GameLift Servers initiates the fleet creation workflow. To debug your fleet, fetch logs, view performance metrics or other actions on the fleet, create a development fleet with port 22/3389 open. As a best practice, we recommend opening ports for remote access only when you need them and closing them when you're finished. When the fleet status is ACTIVE, you can adjust capacity settings and turn autoscaling on/off for each location. A managed fleet's runtime environment depends on the Amazon Machine Image (AMI) version it uses. When a new fleet is created, Amazon GameLift Servers assigns the latest available AMI version to the fleet, and all compute instances in that fleet are deployed with that version. To update the AMI version, you must create a new fleet. As a best practice, we recommend replacing your managed fleets every 30 days to maintain a secure and up-to-date runtime environment for your hosted game servers. For guidance, see Security best practices for Amazon GameLift Servers. Anywhere fleet An Anywhere fleet represents compute resources that are not owned or managed by Amazon GameLift Servers. You might create an Anywhere fleet with your local machine for testing, or use one to host game servers with on-premises hardware or other game hosting solutions. To create an Anywhere fleet, provide these required parameters: ComputeType set to ANYWHERE Locations specifying a custom location Name If successful, this operation creates a new fleet resource and places it in ACTIVE status. You can register computes with a fleet in ACTIVE status. Learn more Setting up fleets Debug fleet creation issues Multi-location fleets"]
module CreateFleetLocationsOutput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t option
        [@ocaml.doc
          "A unique identifier for the fleet that was updated with new locations."];
      fleetArn: FleetArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that is assigned to a Amazon GameLift Servers fleet resource and uniquely identifies it. ARNs are unique across all Regions. Format is arn:aws:gamelift:<region>::fleet/fleet-a1234567-b8c9-0d1e-2fa3-b45c6d7e8912."];
      locationStates: LocationStateList.t option
        [@ocaml.doc
          "The remote locations that are being added to the fleet, and the life-cycle status of each location. For new locations, the status is set to NEW. During location creation, Amazon GameLift Servers updates each location's status as instances are deployed there and prepared for game hosting. This list does not include the fleet home Region or any remote locations that were already added to the fleet."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidFleetStatusException of InvalidFleetStatusException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `NotReadyException of NotReadyException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?fleetId =
      fun ?fleetArn ->
        fun ?locationStates ->
          fun () -> { fleetId; fleetArn; locationStates }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidFleetStatusException" ->
          `InvalidFleetStatusException
            (InvalidFleetStatusException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "NotReadyException" ->
          `NotReadyException (NotReadyException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidFleetStatusException" ->
          `InvalidFleetStatusException
            (InvalidFleetStatusException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "NotReadyException" ->
          `NotReadyException (NotReadyException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidFleetStatusException e ->
          `Assoc
            [("error", (`String "InvalidFleetStatusException"));
            ("details", (InvalidFleetStatusException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `NotReadyException e ->
          `Assoc
            [("error", (`String "NotReadyException"));
            ("details", (NotReadyException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FleetId", (Option.map x.fleetId ~f:FleetIdOrArn.to_value));
        ("FleetArn", (Option.map x.fleetArn ~f:FleetArn.to_value));
        ("LocationStates",
          (Option.map x.locationStates ~f:LocationStateList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let locationStates =
        (Option.map ~f:LocationStateList.of_xml)
          (Xml.child xml_arg0 "LocationStates") in
      let fleetArn =
        (Option.map ~f:FleetArn.of_xml) (Xml.child xml_arg0 "FleetArn") in
      let fleetId =
        (Option.map ~f:FleetIdOrArn.of_xml) (Xml.child xml_arg0 "FleetId") in
      make ?locationStates ?fleetArn ?fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let locationStates =
        field_map json__ "LocationStates" LocationStateList.of_json in
      let fleetArn = field_map json__ "FleetArn" FleetArn.of_json in
      let fleetId = field_map json__ "FleetId" FleetIdOrArn.of_json in
      make ?locationStates ?fleetArn ?fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Adds remote locations to an EC2 and begins populating the new locations with instances. The new instances conform to the fleet's instance type, auto-scaling, and other configuration settings. You can't add remote locations to a fleet that resides in an Amazon Web Services Region that doesn't support multiple locations. Fleets created prior to March 2021 can't support multiple locations. To add fleet locations, specify the fleet to be updated and provide a list of one or more locations. If successful, this operation returns the list of added locations with their status set to NEW. Amazon GameLift Servers initiates the process of starting an instance in each added location. You can track the status of each new location by monitoring location creation events using DescribeFleetEvents. Learn more Setting up fleets Update fleet locations Amazon GameLift Servers service locations for managed hosting."]
module CreateFleetLocationsInput =
  struct
    type nonrec t =
      {
      fleetId: FleetIdOrArn.t
        [@ocaml.doc
          "A unique identifier for the fleet to add locations to. You can use either the fleet ID or ARN value."];
      locations: LocationConfigurationList.t
        [@ocaml.doc
          "A list of locations to deploy additional instances to and manage as part of the fleet. You can add any Amazon GameLift Servers-supported Amazon Web Services Region as a remote location, in the form of an Amazon Web Services Region code such as us-west-2."]}
    let context_ = "CreateFleetLocationsInput"
    let make ~fleetId = fun ~locations -> fun () -> { fleetId; locations }
    let to_value x =
      structure_to_value
        [("FleetId", (Some (FleetIdOrArn.to_value x.fleetId)));
        ("Locations",
          (Some (LocationConfigurationList.to_value x.locations)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let locations =
        LocationConfigurationList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Locations") in
      let fleetId =
        FleetIdOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetId") in
      make ~locations ~fleetId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let locations =
        field_map_exn json__ "Locations" LocationConfigurationList.of_json in
      let fleetId = field_map_exn json__ "FleetId" FleetIdOrArn.of_json in
      make ~locations ~fleetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Container Adds remote locations to an EC2 and begins populating the new locations with instances. The new instances conform to the fleet's instance type, auto-scaling, and other configuration settings. You can't add remote locations to a fleet that resides in an Amazon Web Services Region that doesn't support multiple locations. Fleets created prior to March 2021 can't support multiple locations. To add fleet locations, specify the fleet to be updated and provide a list of one or more locations. If successful, this operation returns the list of added locations with their status set to NEW. Amazon GameLift Servers initiates the process of starting an instance in each added location. You can track the status of each new location by monitoring location creation events using DescribeFleetEvents. Learn more Setting up fleets Update fleet locations Amazon GameLift Servers service locations for managed hosting."]
module CreateFleetInput =
  struct
    type nonrec t =
      {
      name: NonZeroAndMaxString.t
        [@ocaml.doc
          "A descriptive label that is associated with a fleet. Fleet names do not need to be unique."];
      description: NonZeroAndMaxString.t option
        [@ocaml.doc "A description for the fleet."];
      buildId: BuildIdOrArn.t option
        [@ocaml.doc
          "The unique identifier for a custom game server build to be deployed to a fleet with compute type EC2. You can use either the build ID or ARN. The build must be uploaded to Amazon GameLift Servers and in READY status. This fleet property can't be changed after the fleet is created."];
      scriptId: ScriptIdOrArn.t option
        [@ocaml.doc
          "The unique identifier for a Realtime configuration script to be deployed to a fleet with compute type EC2. You can use either the script ID or ARN. Scripts must be uploaded to Amazon GameLift Servers prior to creating the fleet. This fleet property can't be changed after the fleet is created."];
      serverLaunchPath: LaunchPathStringModel.t option
        [@ocaml.doc
          "This parameter is no longer used. Specify a server launch path using the RuntimeConfiguration parameter. Requests that use this parameter instead continue to be valid."];
      serverLaunchParameters: LaunchParametersStringModel.t option
        [@ocaml.doc
          "This parameter is no longer used. Specify server launch parameters using the RuntimeConfiguration parameter. Requests that use this parameter instead continue to be valid."];
      logPaths: StringList.t option
        [@ocaml.doc
          "This parameter is no longer used. To specify where Amazon GameLift Servers should store log files once a server process shuts down, use the Amazon GameLift Servers server API ProcessReady() and specify one or more directory paths in logParameters. For more information, see Initialize the server process in the Amazon GameLift Servers Developer Guide."];
      eC2InstanceType: EC2InstanceType.t option
        [@ocaml.doc
          "The Amazon GameLift Servers-supported Amazon EC2 instance type to use with managed EC2 fleets. Instance type determines the computing resources that will be used to host your game servers, including CPU, memory, storage, and networking capacity. See Amazon Elastic Compute Cloud Instance Types for detailed descriptions of Amazon EC2 instance types."];
      eC2InboundPermissions: IpPermissionsList.t option
        [@ocaml.doc
          "The IP address ranges and port settings that allow inbound traffic to access game server processes and other processes on this fleet. Set this parameter for managed EC2 fleets. You can leave this parameter empty when creating the fleet, but you must call https://docs.aws.amazon.com/gamelift/latest/apireference/API_UpdateFleetPortSettings to set it before players can connect to game sessions. As a best practice, we recommend opening ports for remote access only when you need them and closing them when you're finished. For Amazon GameLift Servers Realtime fleets, Amazon GameLift Servers automatically sets TCP and UDP ranges."];
      newGameSessionProtectionPolicy: ProtectionPolicy.t option
        [@ocaml.doc
          "The status of termination protection for active game sessions on the fleet. By default, this property is set to NoProtection. You can also set game session protection for an individual game session by calling UpdateGameSession. NoProtection - Game sessions can be terminated during active gameplay as a result of a scale-down event. FullProtection - Game sessions in ACTIVE status cannot be terminated during a scale-down event."];
      runtimeConfiguration: RuntimeConfiguration.t option
        [@ocaml.doc
          "Instructions for how to launch and run server processes on the fleet. Set runtime configuration for managed EC2 fleets. For an Anywhere fleets, set this parameter only if the fleet is running the Amazon GameLift Servers Agent. The runtime configuration defines one or more server process configurations. Each server process identifies a game executable or Realtime script file and the number of processes to run concurrently. This parameter replaces the parameters ServerLaunchPath and ServerLaunchParameters, which are still supported for backward compatibility."];
      resourceCreationLimitPolicy: ResourceCreationLimitPolicy.t option
        [@ocaml.doc
          "A policy that limits the number of game sessions that an individual player can create on instances in this fleet within a specified span of time."];
      metricGroups: MetricGroupList.t option
        [@ocaml.doc
          "The name of an Amazon Web Services CloudWatch metric group to add this fleet to. A metric group is used to aggregate the metrics for multiple fleets. You can specify an existing metric group name or set a new name to create a new metric group. A fleet can be included in only one metric group at a time."];
      peerVpcAwsAccountId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Used when peering your Amazon GameLift Servers fleet with a VPC, the unique identifier for the Amazon Web Services account that owns the VPC. You can find your account ID in the Amazon Web Services Management Console under account settings."];
      peerVpcId: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A unique identifier for a VPC with resources to be accessed by your Amazon GameLift Servers fleet. The VPC must be in the same Region as your fleet. To look up a VPC ID, use the VPC Dashboard in the Amazon Web Services Management Console. Learn more about VPC peering in VPC Peering with Amazon GameLift Servers Fleets."];
      fleetType: FleetType.t option
        [@ocaml.doc
          "Indicates whether to use On-Demand or Spot instances for this fleet. By default, this property is set to ON_DEMAND. Learn more about when to use On-Demand versus Spot Instances. This fleet property can't be changed after the fleet is created."];
      instanceRoleArn: NonEmptyString.t option
        [@ocaml.doc
          "A unique identifier for an IAM role that manages access to your Amazon Web Services services. With an instance role ARN set, any application that runs on an instance in this fleet can assume the role, including install scripts, server processes, and daemons (background processes). Create a role or look up a role's ARN by using the IAM dashboard in the Amazon Web Services Management Console. Learn more about using on-box credentials for your game servers at Access external resources from a game server. This fleet property can't be changed after the fleet is created."];
      certificateConfiguration: CertificateConfiguration.t option
        [@ocaml.doc
          "Prompts Amazon GameLift Servers to generate a TLS/SSL certificate for the fleet. Amazon GameLift Servers uses the certificates to encrypt traffic between game clients and the game servers running on Amazon GameLift Servers. By default, the CertificateConfiguration is DISABLED. You can't change this property after you create the fleet. Certificate Manager (ACM) certificates expire after 13 months. Certificate expiration can cause fleets to fail, preventing players from connecting to instances in the fleet. We recommend you replace fleets before 13 months, consider using fleet aliases for a smooth transition. ACM isn't available in all Amazon Web Services regions. A fleet creation request with certificate generation enabled in an unsupported Region, fails with a 4xx error. For more information about the supported Regions, see Supported Regions in the Certificate Manager User Guide."];
      locations: LocationConfigurationList.t option
        [@ocaml.doc
          "A set of remote locations to deploy additional instances to and manage as a multi-location fleet. Use this parameter when creating a fleet in Amazon Web Services Regions that support multiple locations. You can add any Amazon Web Services Region or Local Zone that's supported by Amazon GameLift Servers. Provide a list of one or more Amazon Web Services Region codes, such as us-west-2, or Local Zone names. When using this parameter, Amazon GameLift Servers requires you to include your home location in the request. For a list of supported Regions and Local Zones, see Amazon GameLift Servers service locations for managed hosting."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of labels to assign to the new fleet resource. Tags are developer-defined key-value pairs. Tagging Amazon Web Services resources are useful for resource management, access management and cost allocation. For more information, see Tagging Amazon Web Services Resources in the Amazon Web Services General Reference."];
      computeType: ComputeType.t option
        [@ocaml.doc
          "The type of compute resource used to host your game servers. EC2 \226\128\147 The game server build is deployed to Amazon EC2 instances for cloud hosting. This is the default setting. ANYWHERE \226\128\147 Game servers and supporting software are deployed to compute resources that you provide and manage. With this compute type, you can also set the AnywhereConfiguration parameter."];
      anywhereConfiguration: AnywhereConfiguration.t option
        [@ocaml.doc
          "Amazon GameLift Servers Anywhere configuration options."];
      instanceRoleCredentialsProvider:
        InstanceRoleCredentialsProvider.t option
        [@ocaml.doc
          "Prompts Amazon GameLift Servers to generate a shared credentials file for the IAM role that's defined in InstanceRoleArn. The shared credentials file is stored on each fleet instance and refreshed as needed. Use shared credentials for applications that are deployed along with the game server executable, if the game server is integrated with server SDK version 5.x. For more information about using shared credentials, see Communicate with other Amazon Web Services resources from your fleets."];
      playerGatewayMode: PlayerGatewayMode.t option
        [@ocaml.doc
          "Configures player gateway for your fleet. Player gateway provides benefits such as DDoS protection by rate limiting and validating tra\239\172\131c before it reaches game servers, hiding game server IP addresses from players, and providing updated endpoints when relay endpoints become unhealthy. Note, player gateway is only available for fleets using server SDK 5.x or later game server builds. How it works: When enabled, game clients connect to relay endpoints instead of to your game servers. Player gateway validates player gateway tokens and routes traffic to the appropriate game server. Your game backend calls GetPlayerConnectionDetails to retrieve relay endpoints and player gateway tokens for your game clients. To learn more about this topic, see DDoS protection with Amazon GameLift Servers player gateway. Possible values include: DISABLED (default) -- Game clients connect to the game server endpoint. Use this when you do not intend to integrate your game with player gateway. ENABLED -- Player gateway is available in fleet locations where it is supported. Your game backend can call GetPlayerConnectionDetails to obtain a player gateway token and endpoints for game clients. REQUIRED -- Player gateway is available in fleet locations where it is supported, and the fleet can only use locations that support this feature. Attempting to add a remote location to your fleet which does not support player gateway will result in an InvalidRequestException."];
      playerGatewayConfiguration: PlayerGatewayConfiguration.t option
        [@ocaml.doc
          "Configuration settings for player gateway. Use this to specify advanced options for how player gateway handles connections."]}
    let context_ = "CreateFleetInput"
    let make ?description =
      fun ?buildId ->
        fun ?scriptId ->
          fun ?serverLaunchPath ->
            fun ?serverLaunchParameters ->
              fun ?logPaths ->
                fun ?eC2InstanceType ->
                  fun ?eC2InboundPermissions ->
                    fun ?newGameSessionProtectionPolicy ->
                      fun ?runtimeConfiguration ->
                        fun ?resourceCreationLimitPolicy ->
                          fun ?metricGroups ->
                            fun ?peerVpcAwsAccountId ->
                              fun ?peerVpcId ->
                                fun ?fleetType ->
                                  fun ?instanceRoleArn ->
                                    fun ?certificateConfiguration ->
                                      fun ?locations ->
                                        fun ?tags ->
                                          fun ?computeType ->
                                            fun ?anywhereConfiguration ->
                                              fun
                                                ?instanceRoleCredentialsProvider
                                                ->
                                                fun ?playerGatewayMode ->
                                                  fun
                                                    ?playerGatewayConfiguration
                                                    ->
                                                    fun ~name ->
                                                      fun () ->
                                                        {
                                                          description;
                                                          buildId;
                                                          scriptId;
                                                          serverLaunchPath;
                                                          serverLaunchParameters;
                                                          logPaths;
                                                          eC2InstanceType;
                                                          eC2InboundPermissions;
                                                          newGameSessionProtectionPolicy;
                                                          runtimeConfiguration;
                                                          resourceCreationLimitPolicy;
                                                          metricGroups;
                                                          peerVpcAwsAccountId;
                                                          peerVpcId;
                                                          fleetType;
                                                          instanceRoleArn;
                                                          certificateConfiguration;
                                                          locations;
                                                          tags;
                                                          computeType;
                                                          anywhereConfiguration;
                                                          instanceRoleCredentialsProvider;
                                                          playerGatewayMode;
                                                          playerGatewayConfiguration;
                                                          name
                                                        }
    let to_value x =
      structure_to_value
        [("Name", (Some (NonZeroAndMaxString.to_value x.name)));
        ("Description",
          (Option.map x.description ~f:NonZeroAndMaxString.to_value));
        ("BuildId", (Option.map x.buildId ~f:BuildIdOrArn.to_value));
        ("ScriptId", (Option.map x.scriptId ~f:ScriptIdOrArn.to_value));
        ("ServerLaunchPath",
          (Option.map x.serverLaunchPath ~f:LaunchPathStringModel.to_value));
        ("ServerLaunchParameters",
          (Option.map x.serverLaunchParameters
             ~f:LaunchParametersStringModel.to_value));
        ("LogPaths", (Option.map x.logPaths ~f:StringList.to_value));
        ("EC2InstanceType",
          (Option.map x.eC2InstanceType ~f:EC2InstanceType.to_value));
        ("EC2InboundPermissions",
          (Option.map x.eC2InboundPermissions ~f:IpPermissionsList.to_value));
        ("NewGameSessionProtectionPolicy",
          (Option.map x.newGameSessionProtectionPolicy
             ~f:ProtectionPolicy.to_value));
        ("RuntimeConfiguration",
          (Option.map x.runtimeConfiguration ~f:RuntimeConfiguration.to_value));
        ("ResourceCreationLimitPolicy",
          (Option.map x.resourceCreationLimitPolicy
             ~f:ResourceCreationLimitPolicy.to_value));
        ("MetricGroups",
          (Option.map x.metricGroups ~f:MetricGroupList.to_value));
        ("PeerVpcAwsAccountId",
          (Option.map x.peerVpcAwsAccountId ~f:NonZeroAndMaxString.to_value));
        ("PeerVpcId",
          (Option.map x.peerVpcId ~f:NonZeroAndMaxString.to_value));
        ("FleetType", (Option.map x.fleetType ~f:FleetType.to_value));
        ("InstanceRoleArn",
          (Option.map x.instanceRoleArn ~f:NonEmptyString.to_value));
        ("CertificateConfiguration",
          (Option.map x.certificateConfiguration
             ~f:CertificateConfiguration.to_value));
        ("Locations",
          (Option.map x.locations ~f:LocationConfigurationList.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value));
        ("ComputeType", (Option.map x.computeType ~f:ComputeType.to_value));
        ("AnywhereConfiguration",
          (Option.map x.anywhereConfiguration
             ~f:AnywhereConfiguration.to_value));
        ("InstanceRoleCredentialsProvider",
          (Option.map x.instanceRoleCredentialsProvider
             ~f:InstanceRoleCredentialsProvider.to_value));
        ("PlayerGatewayMode",
          (Option.map x.playerGatewayMode ~f:PlayerGatewayMode.to_value));
        ("PlayerGatewayConfiguration",
          (Option.map x.playerGatewayConfiguration
             ~f:PlayerGatewayConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerGatewayConfiguration =
        (Option.map ~f:PlayerGatewayConfiguration.of_xml)
          (Xml.child xml_arg0 "PlayerGatewayConfiguration") in
      let playerGatewayMode =
        (Option.map ~f:PlayerGatewayMode.of_xml)
          (Xml.child xml_arg0 "PlayerGatewayMode") in
      let instanceRoleCredentialsProvider =
        (Option.map ~f:InstanceRoleCredentialsProvider.of_xml)
          (Xml.child xml_arg0 "InstanceRoleCredentialsProvider") in
      let anywhereConfiguration =
        (Option.map ~f:AnywhereConfiguration.of_xml)
          (Xml.child xml_arg0 "AnywhereConfiguration") in
      let computeType =
        (Option.map ~f:ComputeType.of_xml) (Xml.child xml_arg0 "ComputeType") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let locations =
        (Option.map ~f:LocationConfigurationList.of_xml)
          (Xml.child xml_arg0 "Locations") in
      let certificateConfiguration =
        (Option.map ~f:CertificateConfiguration.of_xml)
          (Xml.child xml_arg0 "CertificateConfiguration") in
      let instanceRoleArn =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "InstanceRoleArn") in
      let fleetType =
        (Option.map ~f:FleetType.of_xml) (Xml.child xml_arg0 "FleetType") in
      let peerVpcId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "PeerVpcId") in
      let peerVpcAwsAccountId =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "PeerVpcAwsAccountId") in
      let metricGroups =
        (Option.map ~f:MetricGroupList.of_xml)
          (Xml.child xml_arg0 "MetricGroups") in
      let resourceCreationLimitPolicy =
        (Option.map ~f:ResourceCreationLimitPolicy.of_xml)
          (Xml.child xml_arg0 "ResourceCreationLimitPolicy") in
      let runtimeConfiguration =
        (Option.map ~f:RuntimeConfiguration.of_xml)
          (Xml.child xml_arg0 "RuntimeConfiguration") in
      let newGameSessionProtectionPolicy =
        (Option.map ~f:ProtectionPolicy.of_xml)
          (Xml.child xml_arg0 "NewGameSessionProtectionPolicy") in
      let eC2InboundPermissions =
        (Option.map ~f:IpPermissionsList.of_xml)
          (Xml.child xml_arg0 "EC2InboundPermissions") in
      let eC2InstanceType =
        (Option.map ~f:EC2InstanceType.of_xml)
          (Xml.child xml_arg0 "EC2InstanceType") in
      let logPaths =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "LogPaths") in
      let serverLaunchParameters =
        (Option.map ~f:LaunchParametersStringModel.of_xml)
          (Xml.child xml_arg0 "ServerLaunchParameters") in
      let serverLaunchPath =
        (Option.map ~f:LaunchPathStringModel.of_xml)
          (Xml.child xml_arg0 "ServerLaunchPath") in
      let scriptId =
        (Option.map ~f:ScriptIdOrArn.of_xml) (Xml.child xml_arg0 "ScriptId") in
      let buildId =
        (Option.map ~f:BuildIdOrArn.of_xml) (Xml.child xml_arg0 "BuildId") in
      let description =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        NonZeroAndMaxString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?playerGatewayConfiguration ?playerGatewayMode
        ?instanceRoleCredentialsProvider ?anywhereConfiguration ?computeType
        ?tags ?locations ?certificateConfiguration ?instanceRoleArn
        ?fleetType ?peerVpcId ?peerVpcAwsAccountId ?metricGroups
        ?resourceCreationLimitPolicy ?runtimeConfiguration
        ?newGameSessionProtectionPolicy ?eC2InboundPermissions
        ?eC2InstanceType ?logPaths ?serverLaunchParameters ?serverLaunchPath
        ?scriptId ?buildId ?description ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerGatewayConfiguration =
        field_map json__ "PlayerGatewayConfiguration"
          PlayerGatewayConfiguration.of_json in
      let playerGatewayMode =
        field_map json__ "PlayerGatewayMode" PlayerGatewayMode.of_json in
      let instanceRoleCredentialsProvider =
        field_map json__ "InstanceRoleCredentialsProvider"
          InstanceRoleCredentialsProvider.of_json in
      let anywhereConfiguration =
        field_map json__ "AnywhereConfiguration"
          AnywhereConfiguration.of_json in
      let computeType = field_map json__ "ComputeType" ComputeType.of_json in
      let tags = field_map json__ "Tags" TagList.of_json in
      let locations =
        field_map json__ "Locations" LocationConfigurationList.of_json in
      let certificateConfiguration =
        field_map json__ "CertificateConfiguration"
          CertificateConfiguration.of_json in
      let instanceRoleArn =
        field_map json__ "InstanceRoleArn" NonEmptyString.of_json in
      let fleetType = field_map json__ "FleetType" FleetType.of_json in
      let peerVpcId =
        field_map json__ "PeerVpcId" NonZeroAndMaxString.of_json in
      let peerVpcAwsAccountId =
        field_map json__ "PeerVpcAwsAccountId" NonZeroAndMaxString.of_json in
      let metricGroups =
        field_map json__ "MetricGroups" MetricGroupList.of_json in
      let resourceCreationLimitPolicy =
        field_map json__ "ResourceCreationLimitPolicy"
          ResourceCreationLimitPolicy.of_json in
      let runtimeConfiguration =
        field_map json__ "RuntimeConfiguration" RuntimeConfiguration.of_json in
      let newGameSessionProtectionPolicy =
        field_map json__ "NewGameSessionProtectionPolicy"
          ProtectionPolicy.of_json in
      let eC2InboundPermissions =
        field_map json__ "EC2InboundPermissions" IpPermissionsList.of_json in
      let eC2InstanceType =
        field_map json__ "EC2InstanceType" EC2InstanceType.of_json in
      let logPaths = field_map json__ "LogPaths" StringList.of_json in
      let serverLaunchParameters =
        field_map json__ "ServerLaunchParameters"
          LaunchParametersStringModel.of_json in
      let serverLaunchPath =
        field_map json__ "ServerLaunchPath" LaunchPathStringModel.of_json in
      let scriptId = field_map json__ "ScriptId" ScriptIdOrArn.of_json in
      let buildId = field_map json__ "BuildId" BuildIdOrArn.of_json in
      let description =
        field_map json__ "Description" NonZeroAndMaxString.of_json in
      let name = field_map_exn json__ "Name" NonZeroAndMaxString.of_json in
      make ?playerGatewayConfiguration ?playerGatewayMode
        ?instanceRoleCredentialsProvider ?anywhereConfiguration ?computeType
        ?tags ?locations ?certificateConfiguration ?instanceRoleArn
        ?fleetType ?peerVpcId ?peerVpcAwsAccountId ?metricGroups
        ?resourceCreationLimitPolicy ?runtimeConfiguration
        ?newGameSessionProtectionPolicy ?eC2InboundPermissions
        ?eC2InstanceType ?logPaths ?serverLaunchParameters ?serverLaunchPath
        ?scriptId ?buildId ?description ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Creates a fleet of compute resources to host your game servers. Use this operation to set up a fleet for the following compute types: Managed EC2 fleet An EC2 fleet is a set of Amazon Elastic Compute Cloud (Amazon EC2) instances. Your game server build is deployed to each fleet instance. Amazon GameLift Servers manages the fleet's instances and controls the lifecycle of game server processes, which host game sessions for players. EC2 fleets can have instances in multiple locations. Each instance in the fleet is designated a Compute. To create an EC2 fleet, provide these required parameters: Either BuildId or ScriptId ComputeType set to EC2 (the default value) EC2InboundPermissions EC2InstanceType FleetType Name RuntimeConfiguration with at least one ServerProcesses configuration If successful, this operation creates a new fleet resource and places it in NEW status while Amazon GameLift Servers initiates the fleet creation workflow. To debug your fleet, fetch logs, view performance metrics or other actions on the fleet, create a development fleet with port 22/3389 open. As a best practice, we recommend opening ports for remote access only when you need them and closing them when you're finished. When the fleet status is ACTIVE, you can adjust capacity settings and turn autoscaling on/off for each location. A managed fleet's runtime environment depends on the Amazon Machine Image (AMI) version it uses. When a new fleet is created, Amazon GameLift Servers assigns the latest available AMI version to the fleet, and all compute instances in that fleet are deployed with that version. To update the AMI version, you must create a new fleet. As a best practice, we recommend replacing your managed fleets every 30 days to maintain a secure and up-to-date runtime environment for your hosted game servers. For guidance, see Security best practices for Amazon GameLift Servers. Anywhere fleet An Anywhere fleet represents compute resources that are not owned or managed by Amazon GameLift Servers. You might create an Anywhere fleet with your local machine for testing, or use one to host game servers with on-premises hardware or other game hosting solutions. To create an Anywhere fleet, provide these required parameters: ComputeType set to ANYWHERE Locations specifying a custom location Name If successful, this operation creates a new fleet resource and places it in ACTIVE status. You can register computes with a fleet in ACTIVE status. Learn more Setting up fleets Debug fleet creation issues Multi-location fleets"]
module CreateContainerGroupDefinitionOutput =
  struct
    type nonrec t =
      {
      containerGroupDefinition: ContainerGroupDefinition.t option
        [@ocaml.doc
          "The properties of the new container group definition resource. You can use this resource to create a container fleet."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerGroupDefinition =
      fun () -> { containerGroupDefinition }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ContainerGroupDefinition",
           (Option.map x.containerGroupDefinition
              ~f:ContainerGroupDefinition.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerGroupDefinition =
        (Option.map ~f:ContainerGroupDefinition.of_xml)
          (Xml.child xml_arg0 "ContainerGroupDefinition") in
      make ?containerGroupDefinition ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let containerGroupDefinition =
        field_map json__ "ContainerGroupDefinition"
          ContainerGroupDefinition.of_json in
      make ?containerGroupDefinition ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Creates a ContainerGroupDefinition that describes a set of containers for hosting your game server with Amazon GameLift Servers managed containers hosting. An Amazon GameLift Servers container group is similar to a container task or pod. Use container group definitions when you create a container fleet with CreateContainerFleet. A container group definition determines how Amazon GameLift Servers deploys your containers to each instance in a container fleet. You can maintain multiple versions of a container group definition. There are two types of container groups: A game server container group has the containers that run your game server application and supporting software. A game server container group can have these container types: Game server container. This container runs your game server. You can define one game server container in a game server container group. Support container. This container runs software in parallel with your game server. You can define up to 8 support containers in a game server group. When building a game server container group definition, you can choose to bundle your game server executable and all dependent software into a single game server container. Alternatively, you can separate the software into one game server container and one or more support containers. On a container fleet instance, a game server container group can be deployed multiple times (depending on the compute resources of the instance). This means that all containers in the container group are replicated together. A per-instance container group has containers for processes that aren't replicated on a container fleet instance. This might include background services, logging, test processes, or processes that need to persist independently of the game server container group. When building a per-instance container group, you can define up to 10 support containers. This operation requires Identity and Access Management (IAM) permissions to access container images in Amazon ECR repositories. See IAM permissions for Amazon GameLift Servers for help setting the appropriate permissions. Request options Use this operation to make the following types of requests. You can specify values for the minimum required parameters and customize optional values later. Create a game server container group definition. Provide the following required parameter values: Name ContainerGroupType (GAME_SERVER) OperatingSystem (omit to use default value) TotalMemoryLimitMebibytes (omit to use default value) TotalVcpuLimit (omit to use default value) At least one GameServerContainerDefinition ContainerName ImageUrl PortConfiguration ServerSdkVersion (omit to use default value) Create a per-instance container group definition. Provide the following required parameter values: Name ContainerGroupType (PER_INSTANCE) OperatingSystem (omit to use default value) TotalMemoryLimitMebibytes (omit to use default value) TotalVcpuLimit (omit to use default value) At least one SupportContainerDefinition ContainerName ImageUrl Results If successful, this request creates a ContainerGroupDefinition resource and assigns a unique ARN value. You can update most properties of a container group definition by calling UpdateContainerGroupDefinition, and optionally save the update as a new version."]
module CreateContainerGroupDefinitionInput =
  struct
    type nonrec t =
      {
      name: ContainerGroupDefinitionName.t
        [@ocaml.doc
          "A descriptive identifier for the container group definition. The name value must be unique in an Amazon Web Services Region."];
      containerGroupType: ContainerGroupType.t option
        [@ocaml.doc
          "The type of container group being defined. Container group type determines how Amazon GameLift Servers deploys the container group on each fleet instance. Default value: GAME_SERVER"];
      totalMemoryLimitMebibytes: ContainerTotalMemoryLimit.t
        [@ocaml.doc
          "The maximum amount of memory (in MiB) to allocate to the container group. All containers in the group share this memory. If you specify memory limits for an individual container, the total value must be greater than any individual container's memory limit. Default value: 1024"];
      totalVcpuLimit: ContainerTotalVcpuLimit.t
        [@ocaml.doc
          "The maximum amount of vCPU units to allocate to the container group (1 vCPU is equal to 1024 CPU units). All containers in the group share this memory. If you specify vCPU limits for individual containers, the total value must be equal to or greater than the sum of the CPU limits for all containers in the group. Default value: 1"];
      gameServerContainerDefinition:
        GameServerContainerDefinitionInput.t option
        [@ocaml.doc
          "The definition for the game server container in this group. Define a game server container only when the container group type is GAME_SERVER. Game server containers specify a container image with your game server build. You can pass in your container definitions as a JSON file."];
      supportContainerDefinitions:
        SupportContainerDefinitionInputList.t option
        [@ocaml.doc
          "One or more definition for support containers in this group. You can define a support container in any type of container group. You can pass in your container definitions as a JSON file."];
      operatingSystem: ContainerOperatingSystem.t
        [@ocaml.doc
          "The platform that all containers in the group use. Containers in a group must run on the same operating system. Default value: AMAZON_LINUX_2023 Amazon Linux 2 (AL2) will reach end of support on 6/30/2026. See more details in the Amazon Linux 2 FAQs. For game servers that are hosted on AL2 and use server SDK version 4.x for Amazon GameLift Servers, first update the game server build to server SDK 5.x, and then deploy to AL2023 instances. See Migrate to server SDK version 5."];
      versionDescription: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A description for the initial version of this container group definition."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of labels to assign to the container group definition resource. Tags are developer-defined key-value pairs. Tagging Amazon Web Services resources are useful for resource management, access management and cost allocation. For more information, see Tagging Amazon Web Services Resources in the Amazon Web Services General Reference."]}
    let context_ = "CreateContainerGroupDefinitionInput"
    let make ?containerGroupType =
      fun ?gameServerContainerDefinition ->
        fun ?supportContainerDefinitions ->
          fun ?versionDescription ->
            fun ?tags ->
              fun ~name ->
                fun ~totalMemoryLimitMebibytes ->
                  fun ~totalVcpuLimit ->
                    fun ~operatingSystem ->
                      fun () ->
                        {
                          containerGroupType;
                          gameServerContainerDefinition;
                          supportContainerDefinitions;
                          versionDescription;
                          tags;
                          name;
                          totalMemoryLimitMebibytes;
                          totalVcpuLimit;
                          operatingSystem
                        }
    let to_value x =
      structure_to_value
        [("Name", (Some (ContainerGroupDefinitionName.to_value x.name)));
        ("ContainerGroupType",
          (Option.map x.containerGroupType ~f:ContainerGroupType.to_value));
        ("TotalMemoryLimitMebibytes",
          (Some
             (ContainerTotalMemoryLimit.to_value x.totalMemoryLimitMebibytes)));
        ("TotalVcpuLimit",
          (Some (ContainerTotalVcpuLimit.to_value x.totalVcpuLimit)));
        ("GameServerContainerDefinition",
          (Option.map x.gameServerContainerDefinition
             ~f:GameServerContainerDefinitionInput.to_value));
        ("SupportContainerDefinitions",
          (Option.map x.supportContainerDefinitions
             ~f:SupportContainerDefinitionInputList.to_value));
        ("OperatingSystem",
          (Some (ContainerOperatingSystem.to_value x.operatingSystem)));
        ("VersionDescription",
          (Option.map x.versionDescription ~f:NonZeroAndMaxString.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let versionDescription =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "VersionDescription") in
      let operatingSystem =
        ContainerOperatingSystem.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OperatingSystem") in
      let supportContainerDefinitions =
        (Option.map ~f:SupportContainerDefinitionInputList.of_xml)
          (Xml.child xml_arg0 "SupportContainerDefinitions") in
      let gameServerContainerDefinition =
        (Option.map ~f:GameServerContainerDefinitionInput.of_xml)
          (Xml.child xml_arg0 "GameServerContainerDefinition") in
      let totalVcpuLimit =
        ContainerTotalVcpuLimit.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TotalVcpuLimit") in
      let totalMemoryLimitMebibytes =
        ContainerTotalMemoryLimit.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TotalMemoryLimitMebibytes") in
      let containerGroupType =
        (Option.map ~f:ContainerGroupType.of_xml)
          (Xml.child xml_arg0 "ContainerGroupType") in
      let name =
        ContainerGroupDefinitionName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?tags ?versionDescription ~operatingSystem
        ?supportContainerDefinitions ?gameServerContainerDefinition
        ~totalVcpuLimit ~totalMemoryLimitMebibytes ?containerGroupType ~name
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagList.of_json in
      let versionDescription =
        field_map json__ "VersionDescription" NonZeroAndMaxString.of_json in
      let operatingSystem =
        field_map_exn json__ "OperatingSystem"
          ContainerOperatingSystem.of_json in
      let supportContainerDefinitions =
        field_map json__ "SupportContainerDefinitions"
          SupportContainerDefinitionInputList.of_json in
      let gameServerContainerDefinition =
        field_map json__ "GameServerContainerDefinition"
          GameServerContainerDefinitionInput.of_json in
      let totalVcpuLimit =
        field_map_exn json__ "TotalVcpuLimit" ContainerTotalVcpuLimit.of_json in
      let totalMemoryLimitMebibytes =
        field_map_exn json__ "TotalMemoryLimitMebibytes"
          ContainerTotalMemoryLimit.of_json in
      let containerGroupType =
        field_map json__ "ContainerGroupType" ContainerGroupType.of_json in
      let name =
        field_map_exn json__ "Name" ContainerGroupDefinitionName.of_json in
      make ?tags ?versionDescription ~operatingSystem
        ?supportContainerDefinitions ?gameServerContainerDefinition
        ~totalVcpuLimit ~totalMemoryLimitMebibytes ?containerGroupType ~name
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Creates a ContainerGroupDefinition that describes a set of containers for hosting your game server with Amazon GameLift Servers managed containers hosting. An Amazon GameLift Servers container group is similar to a container task or pod. Use container group definitions when you create a container fleet with CreateContainerFleet. A container group definition determines how Amazon GameLift Servers deploys your containers to each instance in a container fleet. You can maintain multiple versions of a container group definition. There are two types of container groups: A game server container group has the containers that run your game server application and supporting software. A game server container group can have these container types: Game server container. This container runs your game server. You can define one game server container in a game server container group. Support container. This container runs software in parallel with your game server. You can define up to 8 support containers in a game server group. When building a game server container group definition, you can choose to bundle your game server executable and all dependent software into a single game server container. Alternatively, you can separate the software into one game server container and one or more support containers. On a container fleet instance, a game server container group can be deployed multiple times (depending on the compute resources of the instance). This means that all containers in the container group are replicated together. A per-instance container group has containers for processes that aren't replicated on a container fleet instance. This might include background services, logging, test processes, or processes that need to persist independently of the game server container group. When building a per-instance container group, you can define up to 10 support containers. This operation requires Identity and Access Management (IAM) permissions to access container images in Amazon ECR repositories. See IAM permissions for Amazon GameLift Servers for help setting the appropriate permissions. Request options Use this operation to make the following types of requests. You can specify values for the minimum required parameters and customize optional values later. Create a game server container group definition. Provide the following required parameter values: Name ContainerGroupType (GAME_SERVER) OperatingSystem (omit to use default value) TotalMemoryLimitMebibytes (omit to use default value) TotalVcpuLimit (omit to use default value) At least one GameServerContainerDefinition ContainerName ImageUrl PortConfiguration ServerSdkVersion (omit to use default value) Create a per-instance container group definition. Provide the following required parameter values: Name ContainerGroupType (PER_INSTANCE) OperatingSystem (omit to use default value) TotalMemoryLimitMebibytes (omit to use default value) TotalVcpuLimit (omit to use default value) At least one SupportContainerDefinition ContainerName ImageUrl Results If successful, this request creates a ContainerGroupDefinition resource and assigns a unique ARN value. You can update most properties of a container group definition by calling UpdateContainerGroupDefinition, and optionally save the update as a new version."]
module CreateContainerFleetOutput =
  struct
    type nonrec t =
      {
      containerFleet: ContainerFleet.t option
        [@ocaml.doc
          "The properties for the new container fleet, including current status. All fleets are initially placed in PENDING status."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerFleet = fun () -> { containerFleet }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ContainerFleet",
           (Option.map x.containerFleet ~f:ContainerFleet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerFleet =
        (Option.map ~f:ContainerFleet.of_xml)
          (Xml.child xml_arg0 "ContainerFleet") in
      make ?containerFleet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let containerFleet =
        field_map json__ "ContainerFleet" ContainerFleet.of_json in
      make ?containerFleet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Creates a managed fleet of Amazon Elastic Compute Cloud (Amazon EC2) instances to host your containerized game servers. Use this operation to define how to deploy a container architecture onto each fleet instance and configure fleet settings. You can create a container fleet in any Amazon Web Services Regions that Amazon GameLift Servers supports for multi-location fleets. A container fleet can be deployed to a single location or multiple locations. Container fleets are deployed with Amazon Linux 2023 as the instance operating system. Define the fleet's container architecture using container group definitions. Each fleet can have one of the following container group types: The game server container group runs your game server build and dependent software. Amazon GameLift Servers deploys one or more replicas of this container group to each fleet instance. The number of replicas depends on the computing capabilities of the fleet instance in use. An optional per-instance container group might be used to run other software that only needs to run once per instance, such as background services, logging, or test processes. One per-instance container group is deployed to each fleet instance. Each container group can include the definition for one or more containers. A container definition specifies a container image that is stored in an Amazon Elastic Container Registry (Amazon ECR) public or private repository. Request options Use this operation to make the following types of requests. Most fleet settings have default values, so you can create a working fleet with a minimal configuration and default values, which you can customize later. Create a fleet with no container groups. You can configure a container fleet and then add container group definitions later. In this scenario, no fleet instances are deployed, and the fleet can't host game sessions until you add a game server container group definition. Provide the following required parameter values: FleetRoleArn Create a fleet with a game server container group. Provide the following required parameter values: FleetRoleArn GameServerContainerGroupDefinitionName Create a fleet with a game server container group and a per-instance container group. Provide the following required parameter values: FleetRoleArn GameServerContainerGroupDefinitionName PerInstanceContainerGroupDefinitionName Results If successful, this operation creates a new container fleet resource, places it in PENDING status, and initiates the fleet creation workflow. For fleets with container groups, this workflow starts a fleet deployment and transitions the status to ACTIVE. Fleets without a container group are placed in CREATED status. You can update most of the properties of a fleet, including container group definitions, and deploy the update across all fleet instances. Use UpdateContainerFleet to deploy a new game server version update across the container fleet. A managed fleet's runtime environment depends on the Amazon Machine Image (AMI) version it uses. When a new fleet is created, Amazon GameLift Servers assigns the latest available AMI version to the fleet, and all compute instances in that fleet are deployed with that version. To update the AMI version, you must create a new fleet. As a best practice, we recommend replacing your managed fleets every 30 days to maintain a secure and up-to-date runtime environment for your hosted game servers. For guidance, see Security best practices for Amazon GameLift Servers."]
module CreateContainerFleetInput =
  struct
    type nonrec t =
      {
      fleetRoleArn: IamRoleArn.t
        [@ocaml.doc
          "The unique identifier for an Identity and Access Management (IAM) role with permissions to run your containers on resources that are managed by Amazon GameLift Servers. Use an IAM service role with the GameLiftContainerFleetPolicy managed policy attached. For more information, see Set up an IAM service role. You can't change this fleet property after the fleet is created. IAM role ARN values use the following pattern: arn:aws:iam::\\[Amazon Web Services account\\]:role/\\[role name\\]."];
      description: NonZeroAndMaxString.t option
        [@ocaml.doc "A meaningful description of the container fleet."];
      gameServerContainerGroupDefinitionName:
        ContainerGroupDefinitionNameOrArn.t option
        [@ocaml.doc
          "A container group definition resource that describes how to deploy containers with your game server build and support software onto each fleet instance. You can specify the container group definition's name to use the latest version. Alternatively, provide an ARN value with a specific version number. Create a container group definition by calling CreateContainerGroupDefinition. This operation creates a ContainerGroupDefinition resource."];
      perInstanceContainerGroupDefinitionName:
        ContainerGroupDefinitionNameOrArn.t option
        [@ocaml.doc
          "The name of a container group definition resource that describes a set of axillary software. A fleet instance has one process for executables in this container group. A per-instance container group is optional. You can update the fleet to add or remove a per-instance container group at any time. You can specify the container group definition's name to use the latest version. Alternatively, provide an ARN value with a specific version number. Create a container group definition by calling https://docs.aws.amazon.com/gamelift/latest/apireference/API_CreateContainerGroupDefinition.html. This operation creates a https://docs.aws.amazon.com/gamelift/latest/apireference/API_ContainerGroupDefinition.html resource."];
      instanceConnectionPortRange: ConnectionPortRange.t option
        [@ocaml.doc
          "The set of port numbers to open on each fleet instance. A fleet's connection ports map to container ports that are configured in the fleet's container group definitions. By default, Amazon GameLift Servers calculates an optimal port range based on your fleet configuration. To use the calculated range, don't set this parameter. The values are: Port range: 4192 to a number calculated based on your fleet configuration. Amazon GameLift Servers uses the following formula: 4192 + \\[# of game server container groups per fleet instance\\] * \\[# of container ports in the game server container group definition\\] + \\[# of container ports in the game server container group definition\\] You can also choose to manually set this parameter. When manually setting this parameter, you must use port numbers that match the fleet's inbound permissions port range. If you set values manually, Amazon GameLift Servers no longer calculates a port range for you, even if you later remove the manual settings. The port range must not overlap with the Amazon GameLift Servers reserved port range 4092-4191. This range is reserved for internal Amazon GameLift Servers services."];
      instanceInboundPermissions: IpPermissionsList.t option
        [@ocaml.doc
          "The IP address ranges and port settings that allow inbound traffic to access game server processes and other processes on this fleet. As a best practice, when remotely accessing a fleet instance, we recommend opening ports only when you need them and closing them when you're finished. By default, Amazon GameLift Servers calculates an optimal port range based on your fleet configuration. To use the calculated range, don't set this parameter. The values are: Protocol: UDP Port range: 4192 to a number calculated based on your fleet configuration. Amazon GameLift Servers uses the following formula: 4192 + \\[# of game server container groups per fleet instance\\] * \\[# of container ports in the game server container group definition\\] + \\[# of container ports in the game server container group definition\\] You can also choose to manually set this parameter. When manually setting this parameter, you must use port numbers that match the fleet's connection port range. If you set values manually, Amazon GameLift Servers no longer calculates a port range for you, even if you later remove the manual settings. The port range must not overlap with the Amazon GameLift Servers reserved port range 4092-4191. This range is reserved for internal Amazon GameLift Servers services."];
      gameServerContainerGroupsPerInstance:
        GameServerContainerGroupsPerInstance.t option
        [@ocaml.doc
          "The number of times to replicate the game server container group on each fleet instance. By default, Amazon GameLift Servers calculates the maximum number of game server container groups that can fit on each instance. This calculation is based on the CPU and memory resources of the fleet's instance type). To use the calculated maximum, don't set this parameter. If you set this number manually, Amazon GameLift Servers uses your value as long as it's less than the calculated maximum."];
      instanceType: NonZeroAndMaxString.t option
        [@ocaml.doc
          "The Amazon EC2 instance type to use for all instances in the fleet. For multi-location fleets, the instance type must be available in the home region and all remote locations. Instance type determines the computing resources and processing power that's available to host your game servers. This includes including CPU, memory, storage, and networking capacity. By default, Amazon GameLift Servers selects an instance type that fits the needs of your container groups and is available in all selected fleet locations. You can also choose to manually set this parameter. See Amazon Elastic Compute Cloud Instance Types for detailed descriptions of Amazon EC2 instance types. You can't update this fleet property later."];
      billingType: ContainerFleetBillingType.t option
        [@ocaml.doc
          "Indicates whether to use On-Demand or Spot instances for this fleet. Learn more about when to use On-Demand versus Spot Instances. This fleet property can't be changed after the fleet is created. By default, this property is set to ON_DEMAND. You can't update this fleet property later."];
      locations: LocationConfigurationList.t option
        [@ocaml.doc
          "A set of locations to deploy container fleet instances to. You can add any Amazon Web Services Region or Local Zone that's supported by Amazon GameLift Servers. Provide a list of one or more Amazon Web Services Region codes, such as us-west-2, or Local Zone names. Also include the fleet's home Region, which is the Amazon Web Services Region where the fleet is created. For a list of supported Regions and Local Zones, see Amazon GameLift Servers service locations for managed hosting."];
      metricGroups: MetricGroupList.t option
        [@ocaml.doc
          "The name of an Amazon Web Services CloudWatch metric group to add this fleet to. You can use a metric group to aggregate metrics for multiple fleets. You can specify an existing metric group name or use a new name to create a new metric group. Each fleet can have only one metric group, but you can change this value at any time."];
      newGameSessionProtectionPolicy: ProtectionPolicy.t option
        [@ocaml.doc
          "Determines whether Amazon GameLift Servers can shut down game sessions on the fleet that are actively running and hosting players. Amazon GameLift Servers might prompt an instance shutdown when scaling down fleet capacity or when retiring unhealthy instances. You can also set game session protection for individual game sessions using UpdateGameSession. NoProtection -- Game sessions can be shut down during active gameplay. FullProtection -- Game sessions in ACTIVE status can't be shut down. By default, this property is set to NoProtection."];
      gameSessionCreationLimitPolicy: GameSessionCreationLimitPolicy.t option
        [@ocaml.doc
          "A policy that limits the number of game sessions that each individual player can create on instances in this fleet. The limit applies for a specified span of time."];
      logConfiguration: LogConfiguration.t option
        [@ocaml.doc
          "A method for collecting container logs for the fleet. Amazon GameLift Servers saves all standard output for each container in logs, including game session logs. You can select from the following methods: CLOUDWATCH -- Send logs to an Amazon CloudWatch log group that you define. Each container emits a log stream, which is organized in the log group. S3 -- Store logs in an Amazon S3 bucket that you define. NONE -- Don't collect container logs. By default, this property is set to CLOUDWATCH. Amazon GameLift Servers requires permissions to send logs other Amazon Web Services services in your account. These permissions are included in the IAM fleet role for this container fleet (see FleetRoleArn)."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of labels to assign to the new fleet resource. Tags are developer-defined key-value pairs. Tagging Amazon Web Services resources are useful for resource management, access management and cost allocation. For more information, see Tagging Amazon Web Services Resources in the Amazon Web Services General Reference."];
      playerGatewayMode: PlayerGatewayMode.t option
        [@ocaml.doc
          "Configures player gateway for your fleet. Player gateway provides benefits such as DDoS protection by rate limiting and validating tra\239\172\131c before it reaches game servers, hiding game server IP addresses from players, and providing updated endpoints when relay endpoints become unhealthy. How it works: When enabled, game clients connect to relay endpoints instead of to your game servers. Player gateway validates player gateway tokens and routes traffic to the appropriate game server. Your game backend calls GetPlayerConnectionDetails to retrieve relay endpoints and player gateway tokens for your game clients. To learn more about this topic, see DDoS protection with Amazon GameLift Servers player gateway. Possible values include: DISABLED (default) -- Game clients connect to the game server endpoint. Use this when you do not intend to integrate your game with player gateway. ENABLED -- Player gateway is available in fleet locations where it is supported. Your game backend can call GetPlayerConnectionDetails to obtain a player gateway token and endpoints for game clients. REQUIRED -- Player gateway is available in fleet locations where it is supported, and the fleet can only use locations that support this feature. Attempting to add a remote location to your fleet which does not support player gateway will result in an InvalidRequestException."]}
    let context_ = "CreateContainerFleetInput"
    let make ?description =
      fun ?gameServerContainerGroupDefinitionName ->
        fun ?perInstanceContainerGroupDefinitionName ->
          fun ?instanceConnectionPortRange ->
            fun ?instanceInboundPermissions ->
              fun ?gameServerContainerGroupsPerInstance ->
                fun ?instanceType ->
                  fun ?billingType ->
                    fun ?locations ->
                      fun ?metricGroups ->
                        fun ?newGameSessionProtectionPolicy ->
                          fun ?gameSessionCreationLimitPolicy ->
                            fun ?logConfiguration ->
                              fun ?tags ->
                                fun ?playerGatewayMode ->
                                  fun ~fleetRoleArn ->
                                    fun () ->
                                      {
                                        description;
                                        gameServerContainerGroupDefinitionName;
                                        perInstanceContainerGroupDefinitionName;
                                        instanceConnectionPortRange;
                                        instanceInboundPermissions;
                                        gameServerContainerGroupsPerInstance;
                                        instanceType;
                                        billingType;
                                        locations;
                                        metricGroups;
                                        newGameSessionProtectionPolicy;
                                        gameSessionCreationLimitPolicy;
                                        logConfiguration;
                                        tags;
                                        playerGatewayMode;
                                        fleetRoleArn
                                      }
    let to_value x =
      structure_to_value
        [("FleetRoleArn", (Some (IamRoleArn.to_value x.fleetRoleArn)));
        ("Description",
          (Option.map x.description ~f:NonZeroAndMaxString.to_value));
        ("GameServerContainerGroupDefinitionName",
          (Option.map x.gameServerContainerGroupDefinitionName
             ~f:ContainerGroupDefinitionNameOrArn.to_value));
        ("PerInstanceContainerGroupDefinitionName",
          (Option.map x.perInstanceContainerGroupDefinitionName
             ~f:ContainerGroupDefinitionNameOrArn.to_value));
        ("InstanceConnectionPortRange",
          (Option.map x.instanceConnectionPortRange
             ~f:ConnectionPortRange.to_value));
        ("InstanceInboundPermissions",
          (Option.map x.instanceInboundPermissions
             ~f:IpPermissionsList.to_value));
        ("GameServerContainerGroupsPerInstance",
          (Option.map x.gameServerContainerGroupsPerInstance
             ~f:GameServerContainerGroupsPerInstance.to_value));
        ("InstanceType",
          (Option.map x.instanceType ~f:NonZeroAndMaxString.to_value));
        ("BillingType",
          (Option.map x.billingType ~f:ContainerFleetBillingType.to_value));
        ("Locations",
          (Option.map x.locations ~f:LocationConfigurationList.to_value));
        ("MetricGroups",
          (Option.map x.metricGroups ~f:MetricGroupList.to_value));
        ("NewGameSessionProtectionPolicy",
          (Option.map x.newGameSessionProtectionPolicy
             ~f:ProtectionPolicy.to_value));
        ("GameSessionCreationLimitPolicy",
          (Option.map x.gameSessionCreationLimitPolicy
             ~f:GameSessionCreationLimitPolicy.to_value));
        ("LogConfiguration",
          (Option.map x.logConfiguration ~f:LogConfiguration.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value));
        ("PlayerGatewayMode",
          (Option.map x.playerGatewayMode ~f:PlayerGatewayMode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let playerGatewayMode =
        (Option.map ~f:PlayerGatewayMode.of_xml)
          (Xml.child xml_arg0 "PlayerGatewayMode") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let logConfiguration =
        (Option.map ~f:LogConfiguration.of_xml)
          (Xml.child xml_arg0 "LogConfiguration") in
      let gameSessionCreationLimitPolicy =
        (Option.map ~f:GameSessionCreationLimitPolicy.of_xml)
          (Xml.child xml_arg0 "GameSessionCreationLimitPolicy") in
      let newGameSessionProtectionPolicy =
        (Option.map ~f:ProtectionPolicy.of_xml)
          (Xml.child xml_arg0 "NewGameSessionProtectionPolicy") in
      let metricGroups =
        (Option.map ~f:MetricGroupList.of_xml)
          (Xml.child xml_arg0 "MetricGroups") in
      let locations =
        (Option.map ~f:LocationConfigurationList.of_xml)
          (Xml.child xml_arg0 "Locations") in
      let billingType =
        (Option.map ~f:ContainerFleetBillingType.of_xml)
          (Xml.child xml_arg0 "BillingType") in
      let instanceType =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let gameServerContainerGroupsPerInstance =
        (Option.map ~f:GameServerContainerGroupsPerInstance.of_xml)
          (Xml.child xml_arg0 "GameServerContainerGroupsPerInstance") in
      let instanceInboundPermissions =
        (Option.map ~f:IpPermissionsList.of_xml)
          (Xml.child xml_arg0 "InstanceInboundPermissions") in
      let instanceConnectionPortRange =
        (Option.map ~f:ConnectionPortRange.of_xml)
          (Xml.child xml_arg0 "InstanceConnectionPortRange") in
      let perInstanceContainerGroupDefinitionName =
        (Option.map ~f:ContainerGroupDefinitionNameOrArn.of_xml)
          (Xml.child xml_arg0 "PerInstanceContainerGroupDefinitionName") in
      let gameServerContainerGroupDefinitionName =
        (Option.map ~f:ContainerGroupDefinitionNameOrArn.of_xml)
          (Xml.child xml_arg0 "GameServerContainerGroupDefinitionName") in
      let description =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Description") in
      let fleetRoleArn =
        IamRoleArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FleetRoleArn") in
      make ?playerGatewayMode ?tags ?logConfiguration
        ?gameSessionCreationLimitPolicy ?newGameSessionProtectionPolicy
        ?metricGroups ?locations ?billingType ?instanceType
        ?gameServerContainerGroupsPerInstance ?instanceInboundPermissions
        ?instanceConnectionPortRange ?perInstanceContainerGroupDefinitionName
        ?gameServerContainerGroupDefinitionName ?description ~fleetRoleArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let playerGatewayMode =
        field_map json__ "PlayerGatewayMode" PlayerGatewayMode.of_json in
      let tags = field_map json__ "Tags" TagList.of_json in
      let logConfiguration =
        field_map json__ "LogConfiguration" LogConfiguration.of_json in
      let gameSessionCreationLimitPolicy =
        field_map json__ "GameSessionCreationLimitPolicy"
          GameSessionCreationLimitPolicy.of_json in
      let newGameSessionProtectionPolicy =
        field_map json__ "NewGameSessionProtectionPolicy"
          ProtectionPolicy.of_json in
      let metricGroups =
        field_map json__ "MetricGroups" MetricGroupList.of_json in
      let locations =
        field_map json__ "Locations" LocationConfigurationList.of_json in
      let billingType =
        field_map json__ "BillingType" ContainerFleetBillingType.of_json in
      let instanceType =
        field_map json__ "InstanceType" NonZeroAndMaxString.of_json in
      let gameServerContainerGroupsPerInstance =
        field_map json__ "GameServerContainerGroupsPerInstance"
          GameServerContainerGroupsPerInstance.of_json in
      let instanceInboundPermissions =
        field_map json__ "InstanceInboundPermissions"
          IpPermissionsList.of_json in
      let instanceConnectionPortRange =
        field_map json__ "InstanceConnectionPortRange"
          ConnectionPortRange.of_json in
      let perInstanceContainerGroupDefinitionName =
        field_map json__ "PerInstanceContainerGroupDefinitionName"
          ContainerGroupDefinitionNameOrArn.of_json in
      let gameServerContainerGroupDefinitionName =
        field_map json__ "GameServerContainerGroupDefinitionName"
          ContainerGroupDefinitionNameOrArn.of_json in
      let description =
        field_map json__ "Description" NonZeroAndMaxString.of_json in
      let fleetRoleArn =
        field_map_exn json__ "FleetRoleArn" IamRoleArn.of_json in
      make ?playerGatewayMode ?tags ?logConfiguration
        ?gameSessionCreationLimitPolicy ?newGameSessionProtectionPolicy
        ?metricGroups ?locations ?billingType ?instanceType
        ?gameServerContainerGroupsPerInstance ?instanceInboundPermissions
        ?instanceConnectionPortRange ?perInstanceContainerGroupDefinitionName
        ?gameServerContainerGroupDefinitionName ?description ~fleetRoleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: Container Creates a managed fleet of Amazon Elastic Compute Cloud (Amazon EC2) instances to host your containerized game servers. Use this operation to define how to deploy a container architecture onto each fleet instance and configure fleet settings. You can create a container fleet in any Amazon Web Services Regions that Amazon GameLift Servers supports for multi-location fleets. A container fleet can be deployed to a single location or multiple locations. Container fleets are deployed with Amazon Linux 2023 as the instance operating system. Define the fleet's container architecture using container group definitions. Each fleet can have one of the following container group types: The game server container group runs your game server build and dependent software. Amazon GameLift Servers deploys one or more replicas of this container group to each fleet instance. The number of replicas depends on the computing capabilities of the fleet instance in use. An optional per-instance container group might be used to run other software that only needs to run once per instance, such as background services, logging, or test processes. One per-instance container group is deployed to each fleet instance. Each container group can include the definition for one or more containers. A container definition specifies a container image that is stored in an Amazon Elastic Container Registry (Amazon ECR) public or private repository. Request options Use this operation to make the following types of requests. Most fleet settings have default values, so you can create a working fleet with a minimal configuration and default values, which you can customize later. Create a fleet with no container groups. You can configure a container fleet and then add container group definitions later. In this scenario, no fleet instances are deployed, and the fleet can't host game sessions until you add a game server container group definition. Provide the following required parameter values: FleetRoleArn Create a fleet with a game server container group. Provide the following required parameter values: FleetRoleArn GameServerContainerGroupDefinitionName Create a fleet with a game server container group and a per-instance container group. Provide the following required parameter values: FleetRoleArn GameServerContainerGroupDefinitionName PerInstanceContainerGroupDefinitionName Results If successful, this operation creates a new container fleet resource, places it in PENDING status, and initiates the fleet creation workflow. For fleets with container groups, this workflow starts a fleet deployment and transitions the status to ACTIVE. Fleets without a container group are placed in CREATED status. You can update most of the properties of a fleet, including container group definitions, and deploy the update across all fleet instances. Use UpdateContainerFleet to deploy a new game server version update across the container fleet. A managed fleet's runtime environment depends on the Amazon Machine Image (AMI) version it uses. When a new fleet is created, Amazon GameLift Servers assigns the latest available AMI version to the fleet, and all compute instances in that fleet are deployed with that version. To update the AMI version, you must create a new fleet. As a best practice, we recommend replacing your managed fleets every 30 days to maintain a secure and up-to-date runtime environment for your hosted game servers. For guidance, see Security best practices for Amazon GameLift Servers."]
module CreateBuildOutput =
  struct
    type nonrec t =
      {
      build: Build.t option
        [@ocaml.doc
          "The newly created build resource, including a unique build IDs and status."];
      uploadCredentials: AwsCredentials.t option
        [@ocaml.doc
          "This element is returned only when the operation is called without a storage location. It contains credentials to use when you are uploading a build file to an Amazon S3 bucket that is owned by Amazon GameLift Servers. Credentials have a limited life span. To refresh these credentials, call RequestUploadCredentials."];
      storageLocation: S3Location.t option
        [@ocaml.doc
          "Amazon S3 location for your game build file, including bucket name and key."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?build =
      fun ?uploadCredentials ->
        fun ?storageLocation ->
          fun () -> { build; uploadCredentials; storageLocation }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Build", (Option.map x.build ~f:Build.to_value));
        ("UploadCredentials",
          (Option.map x.uploadCredentials ~f:AwsCredentials.to_value));
        ("StorageLocation",
          (Option.map x.storageLocation ~f:S3Location.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let storageLocation =
        (Option.map ~f:S3Location.of_xml)
          (Xml.child xml_arg0 "StorageLocation") in
      let uploadCredentials =
        (Option.map ~f:AwsCredentials.of_xml)
          (Xml.child xml_arg0 "UploadCredentials") in
      let build = (Option.map ~f:Build.of_xml) (Xml.child xml_arg0 "Build") in
      make ?storageLocation ?uploadCredentials ?build ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let storageLocation =
        field_map json__ "StorageLocation" S3Location.of_json in
      let uploadCredentials =
        field_map json__ "UploadCredentials" AwsCredentials.of_json in
      let build = field_map json__ "Build" Build.of_json in
      make ?storageLocation ?uploadCredentials ?build ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere Creates a new Amazon GameLift Servers build resource for your game server binary files. Combine game server binaries into a zip file for use with Amazon GameLift Servers. When setting up a new game build for Amazon GameLift Servers, we recommend using the CLI command upload-build . This helper command combines two tasks: (1) it uploads your build files from a file directory to an Amazon GameLift Servers Amazon S3 location, and (2) it creates a new build resource. You can use the CreateBuild operation in the following scenarios: Create a new game build with build files that are in an Amazon S3 location under an Amazon Web Services account that you control. To use this option, you give Amazon GameLift Servers access to the Amazon S3 bucket. With permissions in place, specify a build name, operating system, and the Amazon S3 storage location of your game build. Upload your build files to a Amazon GameLift Servers Amazon S3 location. To use this option, specify a build name and operating system. This operation creates a new build resource and also returns an Amazon S3 location with temporary access credentials. Use the credentials to manually upload your build files to the specified Amazon S3 location. For more information, see Uploading Objects in the Amazon S3 Developer Guide. After you upload build files to the Amazon GameLift Servers Amazon S3 location, you can't update them. If successful, this operation creates a new build resource with a unique build ID and places it in INITIALIZED status. A build must be in READY status before you can create fleets with it. Learn more Uploading Your Game Create a Build with Files in Amazon S3 All APIs by task"]
module CreateBuildInput =
  struct
    type nonrec t =
      {
      name: NonZeroAndMaxString.t option
        [@ocaml.doc
          "A descriptive label that is associated with a build. Build names do not need to be unique. You can change this value later."];
      version: NonZeroAndMaxString.t option
        [@ocaml.doc
          "Version information that is associated with a build or script. Version strings do not need to be unique. You can change this value later."];
      storageLocation: S3Location.t option
        [@ocaml.doc
          "Information indicating where your game build files are stored. Use this parameter only when creating a build with files stored in an Amazon S3 bucket that you own. The storage location must specify an Amazon S3 bucket name and key. The location must also specify a role ARN that you set up to allow Amazon GameLift Servers to access your Amazon S3 bucket. The S3 bucket and your new build must be in the same Region. If a StorageLocation is specified, the size of your file can be found in your Amazon S3 bucket. Amazon GameLift Servers will report a SizeOnDisk of 0."];
      operatingSystem: OperatingSystem.t option
        [@ocaml.doc
          "The operating system that your game server binaries run on. This value determines the type of fleet resources that you use for this build. If your game build contains multiple executables, they all must run on the same operating system. You must specify a valid operating system in this request. There is no default value. You can't change a build's operating system later. Amazon Linux 2 (AL2) will reach end of support on 6/30/2026. See more details in the Amazon Linux 2 FAQs. For game servers that are hosted on AL2 and use server SDK version 4.x for Amazon GameLift Servers, first update the game server build to server SDK 5.x, and then deploy to AL2023 instances. See Migrate to server SDK version 5. Windows Server 2016 will reach end of support on 1/12/2027. For game servers that are hosted on Windows Server 2016 and use server SDK version 4.x for Amazon GameLift Servers, first update the game server build to server SDK 5.x, and then deploy to Windows Server 2022 instances. See Migrate to server SDK version 5."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of labels to assign to the new build resource. Tags are developer defined key-value pairs. Tagging Amazon Web Services resources are useful for resource management, access management and cost allocation. For more information, see Tagging Amazon Web Services Resources in the Amazon Web Services General Reference. Once the resource is created, you can use TagResource, UntagResource, and ListTagsForResource to add, remove, and view tags. The maximum tag limit may be lower than stated. See the Amazon Web Services General Reference for actual tagging limits."];
      serverSdkVersion: ServerSdkVersion.t option
        [@ocaml.doc
          "A server SDK version you used when integrating your game server build with Amazon GameLift Servers. For more information see Integrate games with custom game servers. By default Amazon GameLift Servers sets this value to 4.0.2."]}
    let make ?name =
      fun ?version ->
        fun ?storageLocation ->
          fun ?operatingSystem ->
            fun ?tags ->
              fun ?serverSdkVersion ->
                fun () ->
                  {
                    name;
                    version;
                    storageLocation;
                    operatingSystem;
                    tags;
                    serverSdkVersion
                  }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:NonZeroAndMaxString.to_value));
        ("Version", (Option.map x.version ~f:NonZeroAndMaxString.to_value));
        ("StorageLocation",
          (Option.map x.storageLocation ~f:S3Location.to_value));
        ("OperatingSystem",
          (Option.map x.operatingSystem ~f:OperatingSystem.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value));
        ("ServerSdkVersion",
          (Option.map x.serverSdkVersion ~f:ServerSdkVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serverSdkVersion =
        (Option.map ~f:ServerSdkVersion.of_xml)
          (Xml.child xml_arg0 "ServerSdkVersion") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let operatingSystem =
        (Option.map ~f:OperatingSystem.of_xml)
          (Xml.child xml_arg0 "OperatingSystem") in
      let storageLocation =
        (Option.map ~f:S3Location.of_xml)
          (Xml.child xml_arg0 "StorageLocation") in
      let version =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Version") in
      let name =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?serverSdkVersion ?tags ?operatingSystem ?storageLocation ?version
        ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serverSdkVersion =
        field_map json__ "ServerSdkVersion" ServerSdkVersion.of_json in
      let tags = field_map json__ "Tags" TagList.of_json in
      let operatingSystem =
        field_map json__ "OperatingSystem" OperatingSystem.of_json in
      let storageLocation =
        field_map json__ "StorageLocation" S3Location.of_json in
      let version = field_map json__ "Version" NonZeroAndMaxString.of_json in
      let name = field_map json__ "Name" NonZeroAndMaxString.of_json in
      make ?serverSdkVersion ?tags ?operatingSystem ?storageLocation ?version
        ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere Creates a new Amazon GameLift Servers build resource for your game server binary files. Combine game server binaries into a zip file for use with Amazon GameLift Servers. When setting up a new game build for Amazon GameLift Servers, we recommend using the CLI command upload-build . This helper command combines two tasks: (1) it uploads your build files from a file directory to an Amazon GameLift Servers Amazon S3 location, and (2) it creates a new build resource. You can use the CreateBuild operation in the following scenarios: Create a new game build with build files that are in an Amazon S3 location under an Amazon Web Services account that you control. To use this option, you give Amazon GameLift Servers access to the Amazon S3 bucket. With permissions in place, specify a build name, operating system, and the Amazon S3 storage location of your game build. Upload your build files to a Amazon GameLift Servers Amazon S3 location. To use this option, specify a build name and operating system. This operation creates a new build resource and also returns an Amazon S3 location with temporary access credentials. Use the credentials to manually upload your build files to the specified Amazon S3 location. For more information, see Uploading Objects in the Amazon S3 Developer Guide. After you upload build files to the Amazon GameLift Servers Amazon S3 location, you can't update them. If successful, this operation creates a new build resource with a unique build ID and places it in INITIALIZED status. A build must be in READY status before you can create fleets with it. Learn more Uploading Your Game Create a Build with Files in Amazon S3 All APIs by task"]
module CreateAliasOutput =
  struct
    type nonrec t =
      {
      alias: Alias.t option [@ocaml.doc "The newly created alias resource."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `TaggingFailedException of TaggingFailedException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?alias = fun () -> { alias }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "TaggingFailedException" ->
          `TaggingFailedException (TaggingFailedException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `TaggingFailedException e ->
          `Assoc
            [("error", (`String "TaggingFailedException"));
            ("details", (TaggingFailedException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Alias", (Option.map x.alias ~f:Alias.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let alias = (Option.map ~f:Alias.of_xml) (Xml.child xml_arg0 "Alias") in
      make ?alias ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let alias = field_map json__ "Alias" Alias.of_json in make ?alias ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Creates an alias for a fleet. In most situations, you can use an alias ID in place of a fleet ID. An alias provides a level of abstraction for a fleet that is useful when redirecting player traffic from one fleet to another, such as when updating your game build. Amazon GameLift Servers supports two types of routing strategies for aliases: simple and terminal. A simple alias points to an active fleet. A terminal alias is used to display messaging or link to a URL instead of routing players to an active fleet. For example, you might use a terminal alias when a game version is no longer supported and you want to direct players to an upgrade site. To create a fleet alias, specify an alias name, routing strategy, and optional description. Each simple alias can point to only one fleet, but a fleet can have multiple aliases. If successful, a new alias record is returned, including an alias ID and an ARN. You can reassign an alias to another fleet by calling UpdateAlias. Related actions All APIs by task"]
module CreateAliasInput =
  struct
    type nonrec t =
      {
      name: NonBlankAndLengthConstraintString.t
        [@ocaml.doc
          "A descriptive label that is associated with an alias. Alias names do not need to be unique."];
      description: NonZeroAndMaxString.t option
        [@ocaml.doc "A human-readable description of the alias."];
      routingStrategy: RoutingStrategy.t
        [@ocaml.doc
          "The routing configuration, including routing type and fleet target, for the alias."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of labels to assign to the new alias resource. Tags are developer-defined key-value pairs. Tagging Amazon Web Services resources are useful for resource management, access management and cost allocation. For more information, see Tagging Amazon Web Services Resources in the Amazon Web Services General Reference."]}
    let context_ = "CreateAliasInput"
    let make ?description =
      fun ?tags ->
        fun ~name ->
          fun ~routingStrategy ->
            fun () -> { description; tags; name; routingStrategy }
    let to_value x =
      structure_to_value
        [("Name", (Some (NonBlankAndLengthConstraintString.to_value x.name)));
        ("Description",
          (Option.map x.description ~f:NonZeroAndMaxString.to_value));
        ("RoutingStrategy",
          (Some (RoutingStrategy.to_value x.routingStrategy)));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let routingStrategy =
        RoutingStrategy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RoutingStrategy") in
      let description =
        (Option.map ~f:NonZeroAndMaxString.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        NonBlankAndLengthConstraintString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?tags ~routingStrategy ?description ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagList.of_json in
      let routingStrategy =
        field_map_exn json__ "RoutingStrategy" RoutingStrategy.of_json in
      let description =
        field_map json__ "Description" NonZeroAndMaxString.of_json in
      let name =
        field_map_exn json__ "Name" NonBlankAndLengthConstraintString.of_json in
      make ?tags ~routingStrategy ?description ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Creates an alias for a fleet. In most situations, you can use an alias ID in place of a fleet ID. An alias provides a level of abstraction for a fleet that is useful when redirecting player traffic from one fleet to another, such as when updating your game build. Amazon GameLift Servers supports two types of routing strategies for aliases: simple and terminal. A simple alias points to an active fleet. A terminal alias is used to display messaging or link to a URL instead of routing players to an active fleet. For example, you might use a terminal alias when a game version is no longer supported and you want to direct players to an upgrade site. To create a fleet alias, specify an alias name, routing strategy, and optional description. Each simple alias can point to only one fleet, but a fleet can have multiple aliases. If successful, a new alias record is returned, including an alias ID and an ARN. You can reassign an alias to another fleet by calling UpdateAlias. Related actions All APIs by task"]
module ClaimGameServerOutput =
  struct
    type nonrec t =
      {
      gameServer: GameServer.t option
        [@ocaml.doc "Object that describes the newly claimed game server."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `OutOfCapacityException of OutOfCapacityException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?gameServer = fun () -> { gameServer }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "OutOfCapacityException" ->
          `OutOfCapacityException (OutOfCapacityException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "OutOfCapacityException" ->
          `OutOfCapacityException (OutOfCapacityException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `OutOfCapacityException e ->
          `Assoc
            [("error", (`String "OutOfCapacityException"));
            ("details", (OutOfCapacityException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GameServer", (Option.map x.gameServer ~f:GameServer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gameServer =
        (Option.map ~f:GameServer.of_xml) (Xml.child xml_arg0 "GameServer") in
      make ?gameServer ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let gameServer = field_map json__ "GameServer" GameServer.of_json in
      make ?gameServer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Locates an available game server and temporarily reserves it to host gameplay and players. This operation is called from a game client or client service (such as a matchmaker) to request hosting resources for a new game session. In response, Amazon GameLift Servers FleetIQ locates an available game server, places it in CLAIMED status for 60 seconds, and returns connection information that players can use to connect to the game server. To claim a game server, identify a game server group. You can also specify a game server ID, although this approach bypasses Amazon GameLift Servers FleetIQ placement optimization. Optionally, include game data to pass to the game server at the start of a game session, such as a game map or player information. Add filter options to further restrict how a game server is chosen, such as only allowing game servers on ACTIVE instances to be claimed. When a game server is successfully claimed, connection information is returned. A claimed game server's utilization status remains AVAILABLE while the claim status is set to CLAIMED for up to 60 seconds. This time period gives the game server time to update its status to UTILIZED after players join. If the game server's status is not updated within 60 seconds, the game server reverts to unclaimed status and is available to be claimed by another request. The claim time period is a fixed value and is not configurable. If you try to claim a specific game server, this request will fail in the following cases: If the game server utilization status is UTILIZED. If the game server claim status is CLAIMED. If the game server is running on an instance in DRAINING status and the provided filter option does not allow placing on DRAINING instances. Learn more Amazon GameLift Servers FleetIQ Guide"]
module ClaimGameServerInput =
  struct
    type nonrec t =
      {
      gameServerGroupName: GameServerGroupNameOrArn.t
        [@ocaml.doc
          "A unique identifier for the game server group where the game server is running. If you are not specifying a game server to claim, this value identifies where you want Amazon GameLift Servers FleetIQ to look for an available game server to claim."];
      gameServerId: GameServerId.t option
        [@ocaml.doc
          "A custom string that uniquely identifies the game server to claim. If this parameter is left empty, Amazon GameLift Servers FleetIQ searches for an available game server in the specified game server group."];
      gameServerData: GameServerData.t option
        [@ocaml.doc
          "A set of custom game server properties, formatted as a single string value. This data is passed to a game client or service when it requests information on game servers."];
      filterOption: ClaimFilterOption.t option
        [@ocaml.doc
          "Object that restricts how a claimed game server is chosen."]}
    let context_ = "ClaimGameServerInput"
    let make ?gameServerId =
      fun ?gameServerData ->
        fun ?filterOption ->
          fun ~gameServerGroupName ->
            fun () ->
              {
                gameServerId;
                gameServerData;
                filterOption;
                gameServerGroupName
              }
    let to_value x =
      structure_to_value
        [("GameServerGroupName",
           (Some (GameServerGroupNameOrArn.to_value x.gameServerGroupName)));
        ("GameServerId",
          (Option.map x.gameServerId ~f:GameServerId.to_value));
        ("GameServerData",
          (Option.map x.gameServerData ~f:GameServerData.to_value));
        ("FilterOption",
          (Option.map x.filterOption ~f:ClaimFilterOption.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filterOption =
        (Option.map ~f:ClaimFilterOption.of_xml)
          (Xml.child xml_arg0 "FilterOption") in
      let gameServerData =
        (Option.map ~f:GameServerData.of_xml)
          (Xml.child xml_arg0 "GameServerData") in
      let gameServerId =
        (Option.map ~f:GameServerId.of_xml)
          (Xml.child xml_arg0 "GameServerId") in
      let gameServerGroupName =
        GameServerGroupNameOrArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GameServerGroupName") in
      make ?filterOption ?gameServerData ?gameServerId ~gameServerGroupName
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filterOption =
        field_map json__ "FilterOption" ClaimFilterOption.of_json in
      let gameServerData =
        field_map json__ "GameServerData" GameServerData.of_json in
      let gameServerId = field_map json__ "GameServerId" GameServerId.of_json in
      let gameServerGroupName =
        field_map_exn json__ "GameServerGroupName"
          GameServerGroupNameOrArn.of_json in
      make ?filterOption ?gameServerData ?gameServerId ~gameServerGroupName
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2 (FleetIQ) Locates an available game server and temporarily reserves it to host gameplay and players. This operation is called from a game client or client service (such as a matchmaker) to request hosting resources for a new game session. In response, Amazon GameLift Servers FleetIQ locates an available game server, places it in CLAIMED status for 60 seconds, and returns connection information that players can use to connect to the game server. To claim a game server, identify a game server group. You can also specify a game server ID, although this approach bypasses Amazon GameLift Servers FleetIQ placement optimization. Optionally, include game data to pass to the game server at the start of a game session, such as a game map or player information. Add filter options to further restrict how a game server is chosen, such as only allowing game servers on ACTIVE instances to be claimed. When a game server is successfully claimed, connection information is returned. A claimed game server's utilization status remains AVAILABLE while the claim status is set to CLAIMED for up to 60 seconds. This time period gives the game server time to update its status to UTILIZED after players join. If the game server's status is not updated within 60 seconds, the game server reverts to unclaimed status and is available to be claimed by another request. The claim time period is a fixed value and is not configurable. If you try to claim a specific game server, this request will fail in the following cases: If the game server utilization status is UTILIZED. If the game server claim status is CLAIMED. If the game server is running on an instance in DRAINING status and the provided filter option does not allow placing on DRAINING instances. Learn more Amazon GameLift Servers FleetIQ Guide"]
module AcceptMatchOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServiceException of InternalServiceException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `NotFoundException of NotFoundException.t 
      | `UnsupportedRegionException of UnsupportedRegionException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException
            (UnsupportedRegionException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "UnsupportedRegionException" ->
          `UnsupportedRegionException (UnsupportedRegionException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `UnsupportedRegionException e ->
          `Assoc
            [("error", (`String "UnsupportedRegionException"));
            ("details", (UnsupportedRegionException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Registers a player's acceptance or rejection of a proposed FlexMatch match. A matchmaking configuration may require player acceptance; if so, then matches built with that configuration cannot be completed unless all players accept the proposed match within a specified time limit. When FlexMatch builds a match, all the matchmaking tickets involved in the proposed match are placed into status REQUIRES_ACCEPTANCE. This is a trigger for your game to get acceptance from all players in each ticket. Calls to this action are only valid for tickets that are in this status; calls for tickets not in this status result in an error. To register acceptance, specify the ticket ID, one or more players, and an acceptance response. When all players have accepted, Amazon GameLift Servers advances the matchmaking tickets to status PLACING, and attempts to create a new game session for the match. If any player rejects the match, or if acceptances are not received before a specified timeout, the proposed match is dropped. Each matchmaking ticket in the failed match is handled as follows: If the ticket has one or more players who rejected the match or failed to respond, the ticket status is set CANCELLED and processing is terminated. If all players in the ticket accepted the match, the ticket status is returned to SEARCHING to find a new match. Learn more Add FlexMatch to a game client FlexMatch events (reference)"]
module AcceptMatchInput =
  struct
    type nonrec t =
      {
      ticketId: MatchmakingIdStringModel.t
        [@ocaml.doc
          "A unique identifier for a matchmaking ticket. The ticket must be in status REQUIRES_ACCEPTANCE; otherwise this request will fail."];
      playerIds: PlayerIdsForAcceptMatch.t
        [@ocaml.doc
          "A unique identifier for a player delivering the response. This parameter can include one or multiple player IDs."];
      acceptanceType: AcceptanceType.t
        [@ocaml.doc "Player response to the proposed match."]}
    let context_ = "AcceptMatchInput"
    let make ~ticketId =
      fun ~playerIds ->
        fun ~acceptanceType ->
          fun () -> { ticketId; playerIds; acceptanceType }
    let to_value x =
      structure_to_value
        [("TicketId", (Some (MatchmakingIdStringModel.to_value x.ticketId)));
        ("PlayerIds", (Some (PlayerIdsForAcceptMatch.to_value x.playerIds)));
        ("AcceptanceType", (Some (AcceptanceType.to_value x.acceptanceType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptanceType =
        AcceptanceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AcceptanceType") in
      let playerIds =
        PlayerIdsForAcceptMatch.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PlayerIds") in
      let ticketId =
        MatchmakingIdStringModel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TicketId") in
      make ~acceptanceType ~playerIds ~ticketId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let acceptanceType =
        field_map_exn json__ "AcceptanceType" AcceptanceType.of_json in
      let playerIds =
        field_map_exn json__ "PlayerIds" PlayerIdsForAcceptMatch.of_json in
      let ticketId =
        field_map_exn json__ "TicketId" MatchmakingIdStringModel.of_json in
      make ~acceptanceType ~playerIds ~ticketId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This API works with the following fleet types: EC2, Anywhere, Container Registers a player's acceptance or rejection of a proposed FlexMatch match. A matchmaking configuration may require player acceptance; if so, then matches built with that configuration cannot be completed unless all players accept the proposed match within a specified time limit. When FlexMatch builds a match, all the matchmaking tickets involved in the proposed match are placed into status REQUIRES_ACCEPTANCE. This is a trigger for your game to get acceptance from all players in each ticket. Calls to this action are only valid for tickets that are in this status; calls for tickets not in this status result in an error. To register acceptance, specify the ticket ID, one or more players, and an acceptance response. When all players have accepted, Amazon GameLift Servers advances the matchmaking tickets to status PLACING, and attempts to create a new game session for the match. If any player rejects the match, or if acceptances are not received before a specified timeout, the proposed match is dropped. Each matchmaking ticket in the failed match is handled as follows: If the ticket has one or more players who rejected the match or failed to respond, the ticket status is set CANCELLED and processing is terminated. If all players in the ticket accepted the match, the ticket status is returned to SEARCHING to find a new match. Learn more Add FlexMatch to a game client FlexMatch events (reference)"]
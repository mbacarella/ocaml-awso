(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_hosted_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING HostedZoneId"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_hosted_zone
           (Values.AssociateHostedZoneInput.make ~hostedZoneId ~resourceArn
              ~name ()) (Some Values.AssociateHostedZoneOutput.to_json)
           (Some Values.AssociateHostedZoneOutput.error_to_json)])
let batch_create_firewall_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallRules =
         flag "firewall-rules" (required json_arg)
           ~doc:"JSON BatchCreateFirewallRuleInputItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_firewall_rule
           (Values.BatchCreateFirewallRuleInput.make
              ~firewallRules:(Values.BatchCreateFirewallRuleInputItems.of_json
                                firewallRules) ())
           (Some Values.BatchCreateFirewallRuleOutput.to_json)
           (Some Values.BatchCreateFirewallRuleOutput.error_to_json)])
let batch_delete_firewall_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallRules =
         flag "firewall-rules" (required json_arg)
           ~doc:"JSON BatchDeleteFirewallRuleInputItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_firewall_rule
           (Values.BatchDeleteFirewallRuleInput.make
              ~firewallRules:(Values.BatchDeleteFirewallRuleInputItems.of_json
                                firewallRules) ())
           (Some Values.BatchDeleteFirewallRuleOutput.to_json)
           (Some Values.BatchDeleteFirewallRuleOutput.error_to_json)])
let batch_update_firewall_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallRules =
         flag "firewall-rules" (required json_arg)
           ~doc:"JSON BatchUpdateFirewallRuleInputItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_firewall_rule
           (Values.BatchUpdateFirewallRuleInput.make
              ~firewallRules:(Values.BatchUpdateFirewallRuleInputItems.of_json
                                firewallRules) ())
           (Some Values.BatchUpdateFirewallRuleOutput.to_json)
           (Some Values.BatchUpdateFirewallRuleOutput.error_to_json)])
let create_access_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and name =
         flag "name" (optional string) ~doc:"STRING ResourceNameShort"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and cidr = flag "cidr" (required string) ~doc:"STRING Cidr"
       and dnsViewId =
         flag "dns-view-id" (required string) ~doc:"STRING ResourceId"
       and protocol =
         flag "protocol" (required json_arg) ~doc:"JSON DnsProtocol" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_source
           (Values.CreateAccessSourceInput.make ?clientToken
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType) ?name
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~cidr ~dnsViewId
              ~protocol:(Values.DnsProtocol.of_json protocol) ())
           (Some Values.CreateAccessSourceOutput.to_json)
           (Some Values.CreateAccessSourceOutput.error_to_json)])
let create_access_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and expiresAt =
         flag "expires-at" (optional json_arg) ~doc:"JSON ISO8601TimeString"
       and name =
         flag "name" (optional string) ~doc:"STRING ResourceNameShort"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and dnsViewId =
         flag "dns-view-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_token
           (Values.CreateAccessTokenInput.make ?clientToken
              ?expiresAt:(Option.map ~f:Values.ISO8601TimeString.of_json
                            expiresAt) ?name
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~dnsViewId ())
           (Some Values.CreateAccessTokenOutput.to_json)
           (Some Values.CreateAccessTokenOutput.error_to_json)])
let create_d_n_s_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and dnssecValidation =
         flag "dnssec-validation" (optional json_arg)
           ~doc:"JSON DnsSecValidationType"
       and ednsClientSubnet =
         flag "edns-client-subnet" (optional json_arg)
           ~doc:"JSON EdnsClientSubnetType"
       and firewallRulesFailOpen =
         flag "firewall-rules-fail-open" (optional json_arg)
           ~doc:"JSON FirewallRulesFailOpenType"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and globalResolverId =
         flag "global-resolver-id" (required string) ~doc:"STRING ResourceId"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_d_n_s_view
           (Values.CreateDNSViewInput.make ?clientToken
              ?dnssecValidation:(Option.map
                                   ~f:Values.DnsSecValidationType.of_json
                                   dnssecValidation)
              ?ednsClientSubnet:(Option.map
                                   ~f:Values.EdnsClientSubnetType.of_json
                                   ednsClientSubnet)
              ?firewallRulesFailOpen:(Option.map
                                        ~f:Values.FirewallRulesFailOpenType.of_json
                                        firewallRulesFailOpen) ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~globalResolverId ~name ())
           (Some Values.CreateDNSViewOutput.to_json)
           (Some Values.CreateDNSViewOutput.error_to_json)])
let create_firewall_domain_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and globalResolverId =
         flag "global-resolver-id" (required string) ~doc:"STRING ResourceId"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_firewall_domain_list
           (Values.CreateFirewallDomainListInput.make ?clientToken
              ?description ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~globalResolverId ~name ())
           (Some Values.CreateFirewallDomainListOutput.to_json)
           (Some Values.CreateFirewallDomainListOutput.error_to_json)])
let create_firewall_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and blockOverrideDnsType =
         flag "block-override-dns-type" (optional json_arg)
           ~doc:"JSON BlockOverrideDnsQueryType"
       and blockOverrideDomain =
         flag "block-override-domain" (optional string) ~doc:"STRING Domain"
       and blockOverrideTtl =
         flag "block-override-ttl" (optional int) ~doc:"INT BlockOverrideTtl"
       and blockResponse =
         flag "block-response" (optional json_arg)
           ~doc:"JSON FirewallBlockResponse"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and confidenceThreshold =
         flag "confidence-threshold" (optional json_arg)
           ~doc:"JSON ConfidenceThreshold"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and dnsAdvancedProtection =
         flag "dns-advanced-protection" (optional json_arg)
           ~doc:"JSON DnsAdvancedProtection"
       and firewallDomainListId =
         flag "firewall-domain-list-id" (optional string)
           ~doc:"STRING ResourceId"
       and priority =
         flag "priority" (optional json_arg) ~doc:"JSON FirewallRulePriority"
       and qType = flag "q-type" (optional string) ~doc:"STRING DnsQueryType"
       and action =
         flag "action" (required json_arg) ~doc:"JSON FirewallRuleAction"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and dnsViewId =
         flag "dns-view-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_firewall_rule
           (Values.CreateFirewallRuleInput.make
              ?blockOverrideDnsType:(Option.map
                                       ~f:Values.BlockOverrideDnsQueryType.of_json
                                       blockOverrideDnsType)
              ?blockOverrideDomain ?blockOverrideTtl
              ?blockResponse:(Option.map
                                ~f:Values.FirewallBlockResponse.of_json
                                blockResponse) ?clientToken
              ?confidenceThreshold:(Option.map
                                      ~f:Values.ConfidenceThreshold.of_json
                                      confidenceThreshold) ?description
              ?dnsAdvancedProtection:(Option.map
                                        ~f:Values.DnsAdvancedProtection.of_json
                                        dnsAdvancedProtection)
              ?firewallDomainListId
              ?priority:(Option.map ~f:Values.FirewallRulePriority.of_json
                           priority) ?qType
              ~action:(Values.FirewallRuleAction.of_json action) ~name
              ~dnsViewId ()) (Some Values.CreateFirewallRuleOutput.to_json)
           (Some Values.CreateFirewallRuleOutput.error_to_json)])
let create_global_resolver =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg)
           ~doc:"JSON GlobalResolverIpAddressType"
       and observabilityRegion =
         flag "observability-region" (optional string) ~doc:"STRING Region"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and regions = flag "regions" (required json_arg) ~doc:"JSON Regions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_global_resolver
           (Values.CreateGlobalResolverInput.make ?clientToken ?description
              ?ipAddressType:(Option.map
                                ~f:Values.GlobalResolverIpAddressType.of_json
                                ipAddressType) ?observabilityRegion
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~regions:(Values.Regions.of_json regions) ())
           (Some Values.CreateGlobalResolverOutput.to_json)
           (Some Values.CreateGlobalResolverOutput.error_to_json)])
let delete_access_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessSourceId =
         flag "access-source-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_source
           (Values.DeleteAccessSourceInput.make ~accessSourceId ())
           (Some Values.DeleteAccessSourceOutput.to_json)
           (Some Values.DeleteAccessSourceOutput.error_to_json)])
let delete_access_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessTokenId =
         flag "access-token-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_token
           (Values.DeleteAccessTokenInput.make ~accessTokenId ())
           (Some Values.DeleteAccessTokenOutput.to_json)
           (Some Values.DeleteAccessTokenOutput.error_to_json)])
let delete_d_n_s_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dnsViewId =
         flag "dns-view-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_d_n_s_view
           (Values.DeleteDNSViewInput.make ~dnsViewId ())
           (Some Values.DeleteDNSViewOutput.to_json)
           (Some Values.DeleteDNSViewOutput.error_to_json)])
let delete_firewall_domain_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallDomainListId =
         flag "firewall-domain-list-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_firewall_domain_list
           (Values.DeleteFirewallDomainListInput.make ~firewallDomainListId
              ()) (Some Values.DeleteFirewallDomainListOutput.to_json)
           (Some Values.DeleteFirewallDomainListOutput.error_to_json)])
let delete_firewall_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallRuleId =
         flag "firewall-rule-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_firewall_rule
           (Values.DeleteFirewallRuleInput.make ~firewallRuleId ())
           (Some Values.DeleteFirewallRuleOutput.to_json)
           (Some Values.DeleteFirewallRuleOutput.error_to_json)])
let delete_global_resolver =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalResolverId =
         flag "global-resolver-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_global_resolver
           (Values.DeleteGlobalResolverInput.make ~globalResolverId ())
           (Some Values.DeleteGlobalResolverOutput.to_json)
           (Some Values.DeleteGlobalResolverOutput.error_to_json)])
let disable_d_n_s_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dnsViewId =
         flag "dns-view-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_d_n_s_view
           (Values.DisableDNSViewInput.make ~dnsViewId ())
           (Some Values.DisableDNSViewOutput.to_json)
           (Some Values.DisableDNSViewOutput.error_to_json)])
let disassociate_hosted_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING HostedZoneId"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_hosted_zone
           (Values.DisassociateHostedZoneInput.make ~hostedZoneId
              ~resourceArn ())
           (Some Values.DisassociateHostedZoneOutput.to_json)
           (Some Values.DisassociateHostedZoneOutput.error_to_json)])
let enable_d_n_s_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dnsViewId =
         flag "dns-view-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_d_n_s_view
           (Values.EnableDNSViewInput.make ~dnsViewId ())
           (Some Values.EnableDNSViewOutput.to_json)
           (Some Values.EnableDNSViewOutput.error_to_json)])
let get_access_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessSourceId =
         flag "access-source-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_source
           (Values.GetAccessSourceInput.make ~accessSourceId ())
           (Some Values.GetAccessSourceOutput.to_json)
           (Some Values.GetAccessSourceOutput.error_to_json)])
let get_access_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessTokenId =
         flag "access-token-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_token
           (Values.GetAccessTokenInput.make ~accessTokenId ())
           (Some Values.GetAccessTokenOutput.to_json)
           (Some Values.GetAccessTokenOutput.error_to_json)])
let get_d_n_s_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dnsViewId =
         flag "dns-view-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_d_n_s_view (Values.GetDNSViewInput.make ~dnsViewId ())
           (Some Values.GetDNSViewOutput.to_json)
           (Some Values.GetDNSViewOutput.error_to_json)])
let get_firewall_domain_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallDomainListId =
         flag "firewall-domain-list-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_firewall_domain_list
           (Values.GetFirewallDomainListInput.make ~firewallDomainListId ())
           (Some Values.GetFirewallDomainListOutput.to_json)
           (Some Values.GetFirewallDomainListOutput.error_to_json)])
let get_firewall_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallRuleId =
         flag "firewall-rule-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_firewall_rule
           (Values.GetFirewallRuleInput.make ~firewallRuleId ())
           (Some Values.GetFirewallRuleOutput.to_json)
           (Some Values.GetFirewallRuleOutput.error_to_json)])
let get_global_resolver =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalResolverId =
         flag "global-resolver-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_global_resolver
           (Values.GetGlobalResolverInput.make ~globalResolverId ())
           (Some Values.GetGlobalResolverOutput.to_json)
           (Some Values.GetGlobalResolverOutput.error_to_json)])
let get_hosted_zone_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneAssociationId =
         flag "hosted-zone-association-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_hosted_zone_association
           (Values.GetHostedZoneAssociationInput.make
              ~hostedZoneAssociationId ())
           (Some Values.GetHostedZoneAssociationOutput.to_json)
           (Some Values.GetHostedZoneAssociationOutput.error_to_json)])
let get_managed_firewall_domain_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managedFirewallDomainListId =
         flag "managed-firewall-domain-list-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_managed_firewall_domain_list
           (Values.GetManagedFirewallDomainListInput.make
              ~managedFirewallDomainListId ())
           (Some Values.GetManagedFirewallDomainListOutput.to_json)
           (Some Values.GetManagedFirewallDomainListOutput.error_to_json)])
let import_firewall_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainFileUrl =
         flag "domain-file-url" (required string) ~doc:"STRING String"
       and firewallDomainListId =
         flag "firewall-domain-list-id" (required string)
           ~doc:"STRING ResourceId"
       and operation =
         flag "operation" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_firewall_domains
           (Values.ImportFirewallDomainsInput.make ~domainFileUrl
              ~firewallDomainListId ~operation ())
           (Some Values.ImportFirewallDomainsOutput.to_json)
           (Some Values.ImportFirewallDomainsOutput.error_to_json)])
let list_access_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_sources
           (Values.ListAccessSourcesInput.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListAccessSourcesOutput.to_json)
           (Some Values.ListAccessSourcesOutput.error_to_json)])
let list_access_tokens =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and dnsViewId =
         flag "dns-view-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_tokens
           (Values.ListAccessTokensInput.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ~dnsViewId ()) (Some Values.ListAccessTokensOutput.to_json)
           (Some Values.ListAccessTokensOutput.error_to_json)])
let list_d_n_s_views =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and globalResolverId =
         flag "global-resolver-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_d_n_s_views
           (Values.ListDNSViewsInput.make ?maxResults ?nextToken
              ~globalResolverId ()) (Some Values.ListDNSViewsOutput.to_json)
           (Some Values.ListDNSViewsOutput.error_to_json)])
let list_firewall_domain_lists =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and globalResolverId =
         flag "global-resolver-id" (optional string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_firewall_domain_lists
           (Values.ListFirewallDomainListsInput.make ?maxResults ?nextToken
              ?globalResolverId ())
           (Some Values.ListFirewallDomainListsOutput.to_json)
           (Some Values.ListFirewallDomainListsOutput.error_to_json)])
let list_firewall_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and firewallDomainListId =
         flag "firewall-domain-list-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_firewall_domains
           (Values.ListFirewallDomainsInput.make ?maxResults ?nextToken
              ~firewallDomainListId ())
           (Some Values.ListFirewallDomainsOutput.to_json)
           (Some Values.ListFirewallDomainsOutput.error_to_json)])
let list_firewall_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and dnsViewId =
         flag "dns-view-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_firewall_rules
           (Values.ListFirewallRulesInput.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ~dnsViewId ()) (Some Values.ListFirewallRulesOutput.to_json)
           (Some Values.ListFirewallRulesOutput.error_to_json)])
let list_global_resolvers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_global_resolvers
           (Values.ListGlobalResolversInput.make ?maxResults ?nextToken ())
           (Some Values.ListGlobalResolversOutput.to_json)
           (Some Values.ListGlobalResolversOutput.error_to_json)])
let list_hosted_zone_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_hosted_zone_associations
           (Values.ListHostedZoneAssociationsInput.make ?maxResults
              ?nextToken ~resourceArn ())
           (Some Values.ListHostedZoneAssociationsOutput.to_json)
           (Some Values.ListHostedZoneAssociationsOutput.error_to_json)])
let list_managed_firewall_domain_lists =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and managedFirewallDomainListType =
         flag "managed-firewall-domain-list-type" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_firewall_domain_lists
           (Values.ListManagedFirewallDomainListsInput.make ?maxResults
              ?nextToken ~managedFirewallDomainListType ())
           (Some Values.ListManagedFirewallDomainListsOutput.to_json)
           (Some Values.ListManagedFirewallDomainListsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_access_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cidr = flag "cidr" (optional string) ~doc:"STRING Cidr"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and name =
         flag "name" (optional string) ~doc:"STRING ResourceNameShort"
       and protocol =
         flag "protocol" (optional json_arg) ~doc:"JSON DnsProtocol"
       and accessSourceId =
         flag "access-source-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_access_source
           (Values.UpdateAccessSourceInput.make ?cidr
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType) ?name
              ?protocol:(Option.map ~f:Values.DnsProtocol.of_json protocol)
              ~accessSourceId ())
           (Some Values.UpdateAccessSourceOutput.to_json)
           (Some Values.UpdateAccessSourceOutput.error_to_json)])
let update_access_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessTokenId =
         flag "access-token-id" (required string) ~doc:"STRING ResourceId"
       and name =
         flag "name" (required string) ~doc:"STRING ResourceNameShort" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_access_token
           (Values.UpdateAccessTokenInput.make ~accessTokenId ~name ())
           (Some Values.UpdateAccessTokenOutput.to_json)
           (Some Values.UpdateAccessTokenOutput.error_to_json)])
let update_d_n_s_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ResourceName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and dnssecValidation =
         flag "dnssec-validation" (optional json_arg)
           ~doc:"JSON DnsSecValidationType"
       and ednsClientSubnet =
         flag "edns-client-subnet" (optional json_arg)
           ~doc:"JSON EdnsClientSubnetType"
       and firewallRulesFailOpen =
         flag "firewall-rules-fail-open" (optional json_arg)
           ~doc:"JSON FirewallRulesFailOpenType"
       and dnsViewId =
         flag "dns-view-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_d_n_s_view
           (Values.UpdateDNSViewInput.make ?name ?description
              ?dnssecValidation:(Option.map
                                   ~f:Values.DnsSecValidationType.of_json
                                   dnssecValidation)
              ?ednsClientSubnet:(Option.map
                                   ~f:Values.EdnsClientSubnetType.of_json
                                   ednsClientSubnet)
              ?firewallRulesFailOpen:(Option.map
                                        ~f:Values.FirewallRulesFailOpenType.of_json
                                        firewallRulesFailOpen) ~dnsViewId ())
           (Some Values.UpdateDNSViewOutput.to_json)
           (Some Values.UpdateDNSViewOutput.error_to_json)])
let update_firewall_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domains = flag "domains" (required json_arg) ~doc:"JSON Domains"
       and firewallDomainListId =
         flag "firewall-domain-list-id" (required string)
           ~doc:"STRING ResourceId"
       and operation =
         flag "operation" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_firewall_domains
           (Values.UpdateFirewallDomainsInput.make
              ~domains:(Values.Domains.of_json domains) ~firewallDomainListId
              ~operation ())
           (Some Values.UpdateFirewallDomainsOutput.to_json)
           (Some Values.UpdateFirewallDomainsOutput.error_to_json)])
let update_firewall_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and action =
         flag "action" (optional json_arg) ~doc:"JSON FirewallRuleAction"
       and blockOverrideDnsType =
         flag "block-override-dns-type" (optional json_arg)
           ~doc:"JSON BlockOverrideDnsQueryType"
       and blockOverrideDomain =
         flag "block-override-domain" (optional string) ~doc:"STRING Domain"
       and blockOverrideTtl =
         flag "block-override-ttl" (optional int) ~doc:"INT BlockOverrideTtl"
       and blockResponse =
         flag "block-response" (optional json_arg)
           ~doc:"JSON FirewallBlockResponse"
       and confidenceThreshold =
         flag "confidence-threshold" (optional json_arg)
           ~doc:"JSON ConfidenceThreshold"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and dnsAdvancedProtection =
         flag "dns-advanced-protection" (optional json_arg)
           ~doc:"JSON DnsAdvancedProtection"
       and name = flag "name" (optional string) ~doc:"STRING ResourceName"
       and priority =
         flag "priority" (optional json_arg) ~doc:"JSON FirewallRulePriority"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and firewallRuleId =
         flag "firewall-rule-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_firewall_rule
           (Values.UpdateFirewallRuleInput.make
              ?action:(Option.map ~f:Values.FirewallRuleAction.of_json action)
              ?blockOverrideDnsType:(Option.map
                                       ~f:Values.BlockOverrideDnsQueryType.of_json
                                       blockOverrideDnsType)
              ?blockOverrideDomain ?blockOverrideTtl
              ?blockResponse:(Option.map
                                ~f:Values.FirewallBlockResponse.of_json
                                blockResponse)
              ?confidenceThreshold:(Option.map
                                      ~f:Values.ConfidenceThreshold.of_json
                                      confidenceThreshold) ?description
              ?dnsAdvancedProtection:(Option.map
                                        ~f:Values.DnsAdvancedProtection.of_json
                                        dnsAdvancedProtection) ?name
              ?priority:(Option.map ~f:Values.FirewallRulePriority.of_json
                           priority) ~clientToken ~firewallRuleId ())
           (Some Values.UpdateFirewallRuleOutput.to_json)
           (Some Values.UpdateFirewallRuleOutput.error_to_json)])
let update_global_resolver =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ResourceName"
       and observabilityRegion =
         flag "observability-region" (optional string) ~doc:"STRING Region"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg)
           ~doc:"JSON GlobalResolverIpAddressType"
       and regions = flag "regions" (optional json_arg) ~doc:"JSON Regions"
       and globalResolverId =
         flag "global-resolver-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_global_resolver
           (Values.UpdateGlobalResolverInput.make ?name ?observabilityRegion
              ?description
              ?ipAddressType:(Option.map
                                ~f:Values.GlobalResolverIpAddressType.of_json
                                ipAddressType)
              ?regions:(Option.map ~f:Values.Regions.of_json regions)
              ~globalResolverId ())
           (Some Values.UpdateGlobalResolverOutput.to_json)
           (Some Values.UpdateGlobalResolverOutput.error_to_json)])
let update_hosted_zone_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ResourceName"
       and hostedZoneAssociationId =
         flag "hosted-zone-association-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_hosted_zone_association
           (Values.UpdateHostedZoneAssociationInput.make ?name
              ~hostedZoneAssociationId ())
           (Some Values.UpdateHostedZoneAssociationOutput.to_json)
           (Some Values.UpdateHostedZoneAssociationOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-hosted-zone", associate_hosted_zone);
    ("batch-create-firewall-rule", batch_create_firewall_rule);
    ("batch-delete-firewall-rule", batch_delete_firewall_rule);
    ("batch-update-firewall-rule", batch_update_firewall_rule);
    ("create-access-source", create_access_source);
    ("create-access-token", create_access_token);
    ("create-d-n-s-view", create_d_n_s_view);
    ("create-firewall-domain-list", create_firewall_domain_list);
    ("create-firewall-rule", create_firewall_rule);
    ("create-global-resolver", create_global_resolver);
    ("delete-access-source", delete_access_source);
    ("delete-access-token", delete_access_token);
    ("delete-d-n-s-view", delete_d_n_s_view);
    ("delete-firewall-domain-list", delete_firewall_domain_list);
    ("delete-firewall-rule", delete_firewall_rule);
    ("delete-global-resolver", delete_global_resolver);
    ("disable-d-n-s-view", disable_d_n_s_view);
    ("disassociate-hosted-zone", disassociate_hosted_zone);
    ("enable-d-n-s-view", enable_d_n_s_view);
    ("get-access-source", get_access_source);
    ("get-access-token", get_access_token);
    ("get-d-n-s-view", get_d_n_s_view);
    ("get-firewall-domain-list", get_firewall_domain_list);
    ("get-firewall-rule", get_firewall_rule);
    ("get-global-resolver", get_global_resolver);
    ("get-hosted-zone-association", get_hosted_zone_association);
    ("get-managed-firewall-domain-list", get_managed_firewall_domain_list);
    ("import-firewall-domains", import_firewall_domains);
    ("list-access-sources", list_access_sources);
    ("list-access-tokens", list_access_tokens);
    ("list-d-n-s-views", list_d_n_s_views);
    ("list-firewall-domain-lists", list_firewall_domain_lists);
    ("list-firewall-domains", list_firewall_domains);
    ("list-firewall-rules", list_firewall_rules);
    ("list-global-resolvers", list_global_resolvers);
    ("list-hosted-zone-associations", list_hosted_zone_associations);
    ("list-managed-firewall-domain-lists",
      list_managed_firewall_domain_lists);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-access-source", update_access_source);
    ("update-access-token", update_access_token);
    ("update-d-n-s-view", update_d_n_s_view);
    ("update-firewall-domains", update_firewall_domains);
    ("update-firewall-rule", update_firewall_rule);
    ("update-global-resolver", update_global_resolver);
    ("update-hosted-zone-association", update_hosted_zone_association)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_connection_proposal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ConnectionDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and attachPoint =
         flag "attach-point" (required json_arg) ~doc:"JSON AttachPoint"
       and activationKey =
         flag "activation-key" (required string) ~doc:"STRING ActivationKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_connection_proposal
           (Values.AcceptConnectionProposalRequest.make ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?clientToken
              ~attachPoint:(Values.AttachPoint.of_json attachPoint)
              ~activationKey ())
           (Some Values.AcceptConnectionProposalResponse.to_json)
           (Some Values.AcceptConnectionProposalResponse.error_to_json)])
let create_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ConnectionDescription"
       and remoteAccount =
         flag "remote-account" (optional json_arg)
           ~doc:"JSON RemoteAccountIdentifier"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and bandwidth =
         flag "bandwidth" (required string) ~doc:"STRING ConnectionBandwidth"
       and attachPoint =
         flag "attach-point" (required json_arg) ~doc:"JSON AttachPoint"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connection
           (Values.CreateConnectionRequest.make ?description
              ?remoteAccount:(Option.map
                                ~f:Values.RemoteAccountIdentifier.of_json
                                remoteAccount)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?clientToken
              ~bandwidth
              ~attachPoint:(Values.AttachPoint.of_json attachPoint)
              ~environmentId ())
           (Some Values.CreateConnectionResponse.to_json)
           (Some Values.CreateConnectionResponse.error_to_json)])
let delete_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connection
           (Values.DeleteConnectionRequest.make ?clientToken ~identifier ())
           (Some Values.DeleteConnectionResponse.to_json)
           (Some Values.DeleteConnectionResponse.error_to_json)])
let describe_connection_proposal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and activationKey =
         flag "activation-key" (required string) ~doc:"STRING ActivationKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_connection_proposal
           (Values.DescribeConnectionProposalRequest.make ~activationKey ())
           (Some Values.DescribeConnectionProposalResponse.to_json)
           (Some Values.DescribeConnectionProposalResponse.error_to_json)])
let get_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connection
           (Values.GetConnectionRequest.make ~identifier ())
           (Some Values.GetConnectionResponse.to_json)
           (Some Values.GetConnectionResponse.error_to_json)])
let get_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment (Values.GetEnvironmentRequest.make ~id ())
           (Some Values.GetEnvironmentResponse.to_json)
           (Some Values.GetEnvironmentResponse.error_to_json)])
let list_attach_points =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attach_points
           (Values.ListAttachPointsRequest.make ?maxResults ?nextToken
              ~environmentId ())
           (Some Values.ListAttachPointsResponse.to_json)
           (Some Values.ListAttachPointsResponse.error_to_json)])
let list_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and state =
         flag "state" (optional json_arg) ~doc:"JSON ConnectionState"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and provider =
         flag "provider" (optional json_arg) ~doc:"JSON Provider"
       and attachPoint =
         flag "attach-point" (optional json_arg) ~doc:"JSON AttachPoint" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connections
           (Values.ListConnectionsRequest.make ?maxResults ?nextToken
              ?state:(Option.map ~f:Values.ConnectionState.of_json state)
              ?environmentId
              ?provider:(Option.map ~f:Values.Provider.of_json provider)
              ?attachPoint:(Option.map ~f:Values.AttachPoint.of_json
                              attachPoint) ())
           (Some Values.ListConnectionsResponse.to_json)
           (Some Values.ListConnectionsResponse.error_to_json)])
let list_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and provider =
         flag "provider" (optional json_arg) ~doc:"JSON Provider"
       and location =
         flag "location" (optional string) ~doc:"STRING Location" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environments
           (Values.ListEnvironmentsRequest.make ?maxResults ?nextToken
              ?provider:(Option.map ~f:Values.Provider.of_json provider)
              ?location ()) (Some Values.ListEnvironmentsResponse.to_json)
           (Some Values.ListEnvironmentsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~arn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~arn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~arn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ConnectionDescription"
       and bandwidth =
         flag "bandwidth" (optional string) ~doc:"STRING ConnectionBandwidth"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connection
           (Values.UpdateConnectionRequest.make ?description ?bandwidth
              ?clientToken ~identifier ())
           (Some Values.UpdateConnectionResponse.to_json)
           (Some Values.UpdateConnectionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-connection-proposal", accept_connection_proposal);
    ("create-connection", create_connection);
    ("delete-connection", delete_connection);
    ("describe-connection-proposal", describe_connection_proposal);
    ("get-connection", get_connection);
    ("get-environment", get_environment);
    ("list-attach-points", list_attach_points);
    ("list-connections", list_connections);
    ("list-environments", list_environments);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-connection", update_connection)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_assessment_report_evidence_folder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and evidenceFolderId =
         flag "evidence-folder-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_assessment_report_evidence_folder
           (Values.AssociateAssessmentReportEvidenceFolderRequest.make
              ~assessmentId ~evidenceFolderId ())
           (Some
              Values.AssociateAssessmentReportEvidenceFolderResponse.to_json)
           (Some
              Values.AssociateAssessmentReportEvidenceFolderResponse.error_to_json)])
let batch_associate_assessment_report_evidence =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and evidenceFolderId =
         flag "evidence-folder-id" (required string) ~doc:"STRING UUID"
       and evidenceIds =
         flag "evidence-ids" (required json_arg) ~doc:"JSON EvidenceIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_associate_assessment_report_evidence
           (Values.BatchAssociateAssessmentReportEvidenceRequest.make
              ~assessmentId ~evidenceFolderId
              ~evidenceIds:(Values.EvidenceIds.of_json evidenceIds) ())
           (Some
              Values.BatchAssociateAssessmentReportEvidenceResponse.to_json)
           (Some
              Values.BatchAssociateAssessmentReportEvidenceResponse.error_to_json)])
let batch_create_delegation_by_assessment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and createDelegationRequests =
         flag "create-delegation-requests" (required json_arg)
           ~doc:"JSON CreateDelegationRequests"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_delegation_by_assessment
           (Values.BatchCreateDelegationByAssessmentRequest.make
              ~createDelegationRequests:(Values.CreateDelegationRequests.of_json
                                           createDelegationRequests)
              ~assessmentId ())
           (Some Values.BatchCreateDelegationByAssessmentResponse.to_json)
           (Some
              Values.BatchCreateDelegationByAssessmentResponse.error_to_json)])
let batch_delete_delegation_by_assessment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and delegationIds =
         flag "delegation-ids" (required json_arg) ~doc:"JSON DelegationIds"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_delegation_by_assessment
           (Values.BatchDeleteDelegationByAssessmentRequest.make
              ~delegationIds:(Values.DelegationIds.of_json delegationIds)
              ~assessmentId ())
           (Some Values.BatchDeleteDelegationByAssessmentResponse.to_json)
           (Some
              Values.BatchDeleteDelegationByAssessmentResponse.error_to_json)])
let batch_disassociate_assessment_report_evidence =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and evidenceFolderId =
         flag "evidence-folder-id" (required string) ~doc:"STRING UUID"
       and evidenceIds =
         flag "evidence-ids" (required json_arg) ~doc:"JSON EvidenceIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_disassociate_assessment_report_evidence
           (Values.BatchDisassociateAssessmentReportEvidenceRequest.make
              ~assessmentId ~evidenceFolderId
              ~evidenceIds:(Values.EvidenceIds.of_json evidenceIds) ())
           (Some
              Values.BatchDisassociateAssessmentReportEvidenceResponse.to_json)
           (Some
              Values.BatchDisassociateAssessmentReportEvidenceResponse.error_to_json)])
let batch_import_evidence_to_assessment_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and controlSetId =
         flag "control-set-id" (required string) ~doc:"STRING ControlSetId"
       and controlId = flag "control-id" (required string) ~doc:"STRING UUID"
       and manualEvidence =
         flag "manual-evidence" (required json_arg)
           ~doc:"JSON ManualEvidenceList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_import_evidence_to_assessment_control
           (Values.BatchImportEvidenceToAssessmentControlRequest.make
              ~assessmentId ~controlSetId ~controlId
              ~manualEvidence:(Values.ManualEvidenceList.of_json
                                 manualEvidence) ())
           (Some
              Values.BatchImportEvidenceToAssessmentControlResponse.to_json)
           (Some
              Values.BatchImportEvidenceToAssessmentControlResponse.error_to_json)])
let create_assessment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING AssessmentDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING AssessmentName"
       and assessmentReportsDestination =
         flag "assessment-reports-destination" (required json_arg)
           ~doc:"JSON AssessmentReportsDestination"
       and scope = flag "scope" (required json_arg) ~doc:"JSON Scope"
       and roles = flag "roles" (required json_arg) ~doc:"JSON Roles"
       and frameworkId =
         flag "framework-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_assessment
           (Values.CreateAssessmentRequest.make ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~assessmentReportsDestination:(Values.AssessmentReportsDestination.of_json
                                               assessmentReportsDestination)
              ~scope:(Values.Scope.of_json scope)
              ~roles:(Values.Roles.of_json roles) ~frameworkId ())
           (Some Values.CreateAssessmentResponse.to_json)
           (Some Values.CreateAssessmentResponse.error_to_json)])
let create_assessment_framework =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING FrameworkDescription"
       and complianceType =
         flag "compliance-type" (optional string)
           ~doc:"STRING ComplianceType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING FrameworkName"
       and controlSets =
         flag "control-sets" (required json_arg)
           ~doc:"JSON CreateAssessmentFrameworkControlSets" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_assessment_framework
           (Values.CreateAssessmentFrameworkRequest.make ?description
              ?complianceType
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~controlSets:(Values.CreateAssessmentFrameworkControlSets.of_json
                              controlSets) ())
           (Some Values.CreateAssessmentFrameworkResponse.to_json)
           (Some Values.CreateAssessmentFrameworkResponse.error_to_json)])
let create_assessment_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING AssessmentReportDescription"
       and name =
         flag "name" (required string) ~doc:"STRING AssessmentReportName"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_assessment_report
           (Values.CreateAssessmentReportRequest.make ?description ~name
              ~assessmentId ())
           (Some Values.CreateAssessmentReportResponse.to_json)
           (Some Values.CreateAssessmentReportResponse.error_to_json)])
let create_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ControlDescription"
       and testingInformation =
         flag "testing-information" (optional string)
           ~doc:"STRING TestingInformation"
       and actionPlanTitle =
         flag "action-plan-title" (optional string)
           ~doc:"STRING ActionPlanTitle"
       and actionPlanInstructions =
         flag "action-plan-instructions" (optional string)
           ~doc:"STRING ActionPlanInstructions"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING ControlName"
       and controlMappingSources =
         flag "control-mapping-sources" (required json_arg)
           ~doc:"JSON CreateControlMappingSources" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_control
           (Values.CreateControlRequest.make ?description ?testingInformation
              ?actionPlanTitle ?actionPlanInstructions
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~controlMappingSources:(Values.CreateControlMappingSources.of_json
                                        controlMappingSources) ())
           (Some Values.CreateControlResponse.to_json)
           (Some Values.CreateControlResponse.error_to_json)])
let delete_assessment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_assessment
           (Values.DeleteAssessmentRequest.make ~assessmentId ())
           (Some Values.DeleteAssessmentResponse.to_json)
           (Some Values.DeleteAssessmentResponse.error_to_json)])
let delete_assessment_framework =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and frameworkId =
         flag "framework-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_assessment_framework
           (Values.DeleteAssessmentFrameworkRequest.make ~frameworkId ())
           (Some Values.DeleteAssessmentFrameworkResponse.to_json)
           (Some Values.DeleteAssessmentFrameworkResponse.error_to_json)])
let delete_assessment_framework_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestId = flag "request-id" (required string) ~doc:"STRING UUID"
       and requestType =
         flag "request-type" (required json_arg) ~doc:"JSON ShareRequestType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_assessment_framework_share
           (Values.DeleteAssessmentFrameworkShareRequest.make ~requestId
              ~requestType:(Values.ShareRequestType.of_json requestType) ())
           (Some Values.DeleteAssessmentFrameworkShareResponse.to_json)
           (Some Values.DeleteAssessmentFrameworkShareResponse.error_to_json)])
let delete_assessment_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and assessmentReportId =
         flag "assessment-report-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_assessment_report
           (Values.DeleteAssessmentReportRequest.make ~assessmentId
              ~assessmentReportId ())
           (Some Values.DeleteAssessmentReportResponse.to_json)
           (Some Values.DeleteAssessmentReportResponse.error_to_json)])
let delete_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and controlId = flag "control-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_control (Values.DeleteControlRequest.make ~controlId ())
           (Some Values.DeleteControlResponse.to_json)
           (Some Values.DeleteControlResponse.error_to_json)])
let deregister_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_account (Values.DeregisterAccountRequest.make ())
           (Some Values.DeregisterAccountResponse.to_json)
           (Some Values.DeregisterAccountResponse.error_to_json)])
let deregister_organization_admin_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and adminAccountId =
         flag "admin-account-id" (optional string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_organization_admin_account
           (Values.DeregisterOrganizationAdminAccountRequest.make
              ?adminAccountId ())
           (Some Values.DeregisterOrganizationAdminAccountResponse.to_json)
           (Some
              Values.DeregisterOrganizationAdminAccountResponse.error_to_json)])
let disassociate_assessment_report_evidence_folder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and evidenceFolderId =
         flag "evidence-folder-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_assessment_report_evidence_folder
           (Values.DisassociateAssessmentReportEvidenceFolderRequest.make
              ~assessmentId ~evidenceFolderId ())
           (Some
              Values.DisassociateAssessmentReportEvidenceFolderResponse.to_json)
           (Some
              Values.DisassociateAssessmentReportEvidenceFolderResponse.error_to_json)])
let get_account_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_status (Values.GetAccountStatusRequest.make ())
           (Some Values.GetAccountStatusResponse.to_json)
           (Some Values.GetAccountStatusResponse.error_to_json)])
let get_assessment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_assessment
           (Values.GetAssessmentRequest.make ~assessmentId ())
           (Some Values.GetAssessmentResponse.to_json)
           (Some Values.GetAssessmentResponse.error_to_json)])
let get_assessment_framework =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and frameworkId =
         flag "framework-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_assessment_framework
           (Values.GetAssessmentFrameworkRequest.make ~frameworkId ())
           (Some Values.GetAssessmentFrameworkResponse.to_json)
           (Some Values.GetAssessmentFrameworkResponse.error_to_json)])
let get_assessment_report_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentReportId =
         flag "assessment-report-id" (required string) ~doc:"STRING UUID"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_assessment_report_url
           (Values.GetAssessmentReportUrlRequest.make ~assessmentReportId
              ~assessmentId ())
           (Some Values.GetAssessmentReportUrlResponse.to_json)
           (Some Values.GetAssessmentReportUrlResponse.error_to_json)])
let get_change_logs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and controlSetId =
         flag "control-set-id" (optional string) ~doc:"STRING ControlSetId"
       and controlId = flag "control-id" (optional string) ~doc:"STRING UUID"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_change_logs
           (Values.GetChangeLogsRequest.make ?controlSetId ?controlId
              ?nextToken ?maxResults ~assessmentId ())
           (Some Values.GetChangeLogsResponse.to_json)
           (Some Values.GetChangeLogsResponse.error_to_json)])
let get_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and controlId = flag "control-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_control (Values.GetControlRequest.make ~controlId ())
           (Some Values.GetControlResponse.to_json)
           (Some Values.GetControlResponse.error_to_json)])
let get_delegations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_delegations
           (Values.GetDelegationsRequest.make ?nextToken ?maxResults ())
           (Some Values.GetDelegationsResponse.to_json)
           (Some Values.GetDelegationsResponse.error_to_json)])
let get_evidence =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and controlSetId =
         flag "control-set-id" (required string) ~doc:"STRING ControlSetId"
       and evidenceFolderId =
         flag "evidence-folder-id" (required string) ~doc:"STRING UUID"
       and evidenceId =
         flag "evidence-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_evidence
           (Values.GetEvidenceRequest.make ~assessmentId ~controlSetId
              ~evidenceFolderId ~evidenceId ())
           (Some Values.GetEvidenceResponse.to_json)
           (Some Values.GetEvidenceResponse.error_to_json)])
let get_evidence_by_evidence_folder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and controlSetId =
         flag "control-set-id" (required string) ~doc:"STRING ControlSetId"
       and evidenceFolderId =
         flag "evidence-folder-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_evidence_by_evidence_folder
           (Values.GetEvidenceByEvidenceFolderRequest.make ?nextToken
              ?maxResults ~assessmentId ~controlSetId ~evidenceFolderId ())
           (Some Values.GetEvidenceByEvidenceFolderResponse.to_json)
           (Some Values.GetEvidenceByEvidenceFolderResponse.error_to_json)])
let get_evidence_folder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and controlSetId =
         flag "control-set-id" (required string) ~doc:"STRING ControlSetId"
       and evidenceFolderId =
         flag "evidence-folder-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_evidence_folder
           (Values.GetEvidenceFolderRequest.make ~assessmentId ~controlSetId
              ~evidenceFolderId ())
           (Some Values.GetEvidenceFolderResponse.to_json)
           (Some Values.GetEvidenceFolderResponse.error_to_json)])
let get_evidence_folders_by_assessment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_evidence_folders_by_assessment
           (Values.GetEvidenceFoldersByAssessmentRequest.make ?nextToken
              ?maxResults ~assessmentId ())
           (Some Values.GetEvidenceFoldersByAssessmentResponse.to_json)
           (Some Values.GetEvidenceFoldersByAssessmentResponse.error_to_json)])
let get_evidence_folders_by_assessment_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and controlSetId =
         flag "control-set-id" (required string) ~doc:"STRING ControlSetId"
       and controlId = flag "control-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_evidence_folders_by_assessment_control
           (Values.GetEvidenceFoldersByAssessmentControlRequest.make
              ?nextToken ?maxResults ~assessmentId ~controlSetId ~controlId
              ())
           (Some Values.GetEvidenceFoldersByAssessmentControlResponse.to_json)
           (Some
              Values.GetEvidenceFoldersByAssessmentControlResponse.error_to_json)])
let get_insights =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_insights (Values.GetInsightsRequest.make ())
           (Some Values.GetInsightsResponse.to_json)
           (Some Values.GetInsightsResponse.error_to_json)])
let get_insights_by_assessment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_insights_by_assessment
           (Values.GetInsightsByAssessmentRequest.make ~assessmentId ())
           (Some Values.GetInsightsByAssessmentResponse.to_json)
           (Some Values.GetInsightsByAssessmentResponse.error_to_json)])
let get_organization_admin_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_organization_admin_account
           (Values.GetOrganizationAdminAccountRequest.make ())
           (Some Values.GetOrganizationAdminAccountResponse.to_json)
           (Some Values.GetOrganizationAdminAccountResponse.error_to_json)])
let get_services_in_scope =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_services_in_scope
           (Values.GetServicesInScopeRequest.make ())
           (Some Values.GetServicesInScopeResponse.to_json)
           (Some Values.GetServicesInScopeResponse.error_to_json)])
let get_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attribute =
         flag "attribute" (required json_arg) ~doc:"JSON SettingAttribute" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_settings
           (Values.GetSettingsRequest.make
              ~attribute:(Values.SettingAttribute.of_json attribute) ())
           (Some Values.GetSettingsResponse.to_json)
           (Some Values.GetSettingsResponse.error_to_json)])
let list_assessment_control_insights_by_control_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and controlDomainId =
         flag "control-domain-id" (required string) ~doc:"STRING UUID"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_assessment_control_insights_by_control_domain
           (Values.ListAssessmentControlInsightsByControlDomainRequest.make
              ?nextToken ?maxResults ~controlDomainId ~assessmentId ())
           (Some
              Values.ListAssessmentControlInsightsByControlDomainResponse.to_json)
           (Some
              Values.ListAssessmentControlInsightsByControlDomainResponse.error_to_json)])
let list_assessment_framework_share_requests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and requestType =
         flag "request-type" (required json_arg) ~doc:"JSON ShareRequestType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_assessment_framework_share_requests
           (Values.ListAssessmentFrameworkShareRequestsRequest.make
              ?nextToken ?maxResults
              ~requestType:(Values.ShareRequestType.of_json requestType) ())
           (Some Values.ListAssessmentFrameworkShareRequestsResponse.to_json)
           (Some
              Values.ListAssessmentFrameworkShareRequestsResponse.error_to_json)])
let list_assessment_frameworks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and frameworkType =
         flag "framework-type" (required json_arg) ~doc:"JSON FrameworkType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_assessment_frameworks
           (Values.ListAssessmentFrameworksRequest.make ?nextToken
              ?maxResults
              ~frameworkType:(Values.FrameworkType.of_json frameworkType) ())
           (Some Values.ListAssessmentFrameworksResponse.to_json)
           (Some Values.ListAssessmentFrameworksResponse.error_to_json)])
let list_assessment_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_assessment_reports
           (Values.ListAssessmentReportsRequest.make ?nextToken ?maxResults
              ()) (Some Values.ListAssessmentReportsResponse.to_json)
           (Some Values.ListAssessmentReportsResponse.error_to_json)])
let list_assessments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON AssessmentStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_assessments
           (Values.ListAssessmentsRequest.make
              ?status:(Option.map ~f:Values.AssessmentStatus.of_json status)
              ?nextToken ?maxResults ())
           (Some Values.ListAssessmentsResponse.to_json)
           (Some Values.ListAssessmentsResponse.error_to_json)])
let list_control_domain_insights =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_control_domain_insights
           (Values.ListControlDomainInsightsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListControlDomainInsightsResponse.to_json)
           (Some Values.ListControlDomainInsightsResponse.error_to_json)])
let list_control_domain_insights_by_assessment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_control_domain_insights_by_assessment
           (Values.ListControlDomainInsightsByAssessmentRequest.make
              ?nextToken ?maxResults ~assessmentId ())
           (Some Values.ListControlDomainInsightsByAssessmentResponse.to_json)
           (Some
              Values.ListControlDomainInsightsByAssessmentResponse.error_to_json)])
let list_control_insights_by_control_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and controlDomainId =
         flag "control-domain-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_control_insights_by_control_domain
           (Values.ListControlInsightsByControlDomainRequest.make ?nextToken
              ?maxResults ~controlDomainId ())
           (Some Values.ListControlInsightsByControlDomainResponse.to_json)
           (Some
              Values.ListControlInsightsByControlDomainResponse.error_to_json)])
let list_controls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and controlType =
         flag "control-type" (required json_arg) ~doc:"JSON ControlType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_controls
           (Values.ListControlsRequest.make ?nextToken ?maxResults
              ~controlType:(Values.ControlType.of_json controlType) ())
           (Some Values.ListControlsResponse.to_json)
           (Some Values.ListControlsResponse.error_to_json)])
let list_keywords_for_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and source = flag "source" (required json_arg) ~doc:"JSON SourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_keywords_for_data_source
           (Values.ListKeywordsForDataSourceRequest.make ?nextToken
              ?maxResults ~source:(Values.SourceType.of_json source) ())
           (Some Values.ListKeywordsForDataSourceResponse.to_json)
           (Some Values.ListKeywordsForDataSourceResponse.error_to_json)])
let list_notifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_notifications
           (Values.ListNotificationsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListNotificationsResponse.to_json)
           (Some Values.ListNotificationsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING AuditManagerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let register_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKey = flag "kms-key" (optional string) ~doc:"STRING KmsKey"
       and delegatedAdminAccount =
         flag "delegated-admin-account" (optional string)
           ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_account
           (Values.RegisterAccountRequest.make ?kmsKey ?delegatedAdminAccount
              ()) (Some Values.RegisterAccountResponse.to_json)
           (Some Values.RegisterAccountResponse.error_to_json)])
let register_organization_admin_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and adminAccountId =
         flag "admin-account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_organization_admin_account
           (Values.RegisterOrganizationAdminAccountRequest.make
              ~adminAccountId ())
           (Some Values.RegisterOrganizationAdminAccountResponse.to_json)
           (Some
              Values.RegisterOrganizationAdminAccountResponse.error_to_json)])
let start_assessment_framework_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment =
         flag "comment" (optional string) ~doc:"STRING ShareRequestComment"
       and frameworkId =
         flag "framework-id" (required string) ~doc:"STRING UUID"
       and destinationAccount =
         flag "destination-account" (required string) ~doc:"STRING AccountId"
       and destinationRegion =
         flag "destination-region" (required string) ~doc:"STRING Region" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_assessment_framework_share
           (Values.StartAssessmentFrameworkShareRequest.make ?comment
              ~frameworkId ~destinationAccount ~destinationRegion ())
           (Some Values.StartAssessmentFrameworkShareResponse.to_json)
           (Some Values.StartAssessmentFrameworkShareResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING AuditManagerArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING AuditManagerArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_assessment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentName =
         flag "assessment-name" (optional string)
           ~doc:"STRING AssessmentName"
       and assessmentDescription =
         flag "assessment-description" (optional string)
           ~doc:"STRING AssessmentDescription"
       and assessmentReportsDestination =
         flag "assessment-reports-destination" (optional json_arg)
           ~doc:"JSON AssessmentReportsDestination"
       and roles = flag "roles" (optional json_arg) ~doc:"JSON Roles"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and scope = flag "scope" (required json_arg) ~doc:"JSON Scope" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_assessment
           (Values.UpdateAssessmentRequest.make ?assessmentName
              ?assessmentDescription
              ?assessmentReportsDestination:(Option.map
                                               ~f:Values.AssessmentReportsDestination.of_json
                                               assessmentReportsDestination)
              ?roles:(Option.map ~f:Values.Roles.of_json roles) ~assessmentId
              ~scope:(Values.Scope.of_json scope) ())
           (Some Values.UpdateAssessmentResponse.to_json)
           (Some Values.UpdateAssessmentResponse.error_to_json)])
let update_assessment_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and controlStatus =
         flag "control-status" (optional json_arg) ~doc:"JSON ControlStatus"
       and commentBody =
         flag "comment-body" (optional string)
           ~doc:"STRING ControlCommentBody"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and controlSetId =
         flag "control-set-id" (required string) ~doc:"STRING ControlSetId"
       and controlId = flag "control-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_assessment_control
           (Values.UpdateAssessmentControlRequest.make
              ?controlStatus:(Option.map ~f:Values.ControlStatus.of_json
                                controlStatus) ?commentBody ~assessmentId
              ~controlSetId ~controlId ())
           (Some Values.UpdateAssessmentControlResponse.to_json)
           (Some Values.UpdateAssessmentControlResponse.error_to_json)])
let update_assessment_control_set_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and controlSetId =
         flag "control-set-id" (required string) ~doc:"STRING String"
       and status =
         flag "status" (required json_arg) ~doc:"JSON ControlSetStatus"
       and comment =
         flag "comment" (required string) ~doc:"STRING DelegationComment" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_assessment_control_set_status
           (Values.UpdateAssessmentControlSetStatusRequest.make ~assessmentId
              ~controlSetId ~status:(Values.ControlSetStatus.of_json status)
              ~comment ())
           (Some Values.UpdateAssessmentControlSetStatusResponse.to_json)
           (Some
              Values.UpdateAssessmentControlSetStatusResponse.error_to_json)])
let update_assessment_framework =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING FrameworkDescription"
       and complianceType =
         flag "compliance-type" (optional string)
           ~doc:"STRING ComplianceType"
       and frameworkId =
         flag "framework-id" (required string) ~doc:"STRING UUID"
       and name = flag "name" (required string) ~doc:"STRING FrameworkName"
       and controlSets =
         flag "control-sets" (required json_arg)
           ~doc:"JSON UpdateAssessmentFrameworkControlSets" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_assessment_framework
           (Values.UpdateAssessmentFrameworkRequest.make ?description
              ?complianceType ~frameworkId ~name
              ~controlSets:(Values.UpdateAssessmentFrameworkControlSets.of_json
                              controlSets) ())
           (Some Values.UpdateAssessmentFrameworkResponse.to_json)
           (Some Values.UpdateAssessmentFrameworkResponse.error_to_json)])
let update_assessment_framework_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestId = flag "request-id" (required string) ~doc:"STRING UUID"
       and requestType =
         flag "request-type" (required json_arg) ~doc:"JSON ShareRequestType"
       and action =
         flag "action" (required json_arg) ~doc:"JSON ShareRequestAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_assessment_framework_share
           (Values.UpdateAssessmentFrameworkShareRequest.make ~requestId
              ~requestType:(Values.ShareRequestType.of_json requestType)
              ~action:(Values.ShareRequestAction.of_json action) ())
           (Some Values.UpdateAssessmentFrameworkShareResponse.to_json)
           (Some Values.UpdateAssessmentFrameworkShareResponse.error_to_json)])
let update_assessment_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentId =
         flag "assessment-id" (required string) ~doc:"STRING UUID"
       and status =
         flag "status" (required json_arg) ~doc:"JSON AssessmentStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_assessment_status
           (Values.UpdateAssessmentStatusRequest.make ~assessmentId
              ~status:(Values.AssessmentStatus.of_json status) ())
           (Some Values.UpdateAssessmentStatusResponse.to_json)
           (Some Values.UpdateAssessmentStatusResponse.error_to_json)])
let update_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ControlDescription"
       and testingInformation =
         flag "testing-information" (optional string)
           ~doc:"STRING TestingInformation"
       and actionPlanTitle =
         flag "action-plan-title" (optional string)
           ~doc:"STRING ActionPlanTitle"
       and actionPlanInstructions =
         flag "action-plan-instructions" (optional string)
           ~doc:"STRING ActionPlanInstructions"
       and controlId = flag "control-id" (required string) ~doc:"STRING UUID"
       and name = flag "name" (required string) ~doc:"STRING ControlName"
       and controlMappingSources =
         flag "control-mapping-sources" (required json_arg)
           ~doc:"JSON ControlMappingSources" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_control
           (Values.UpdateControlRequest.make ?description ?testingInformation
              ?actionPlanTitle ?actionPlanInstructions ~controlId ~name
              ~controlMappingSources:(Values.ControlMappingSources.of_json
                                        controlMappingSources) ())
           (Some Values.UpdateControlResponse.to_json)
           (Some Values.UpdateControlResponse.error_to_json)])
let update_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snsTopic = flag "sns-topic" (optional string) ~doc:"STRING SnsArn"
       and defaultAssessmentReportsDestination =
         flag "default-assessment-reports-destination" (optional json_arg)
           ~doc:"JSON AssessmentReportsDestination"
       and defaultProcessOwners =
         flag "default-process-owners" (optional json_arg) ~doc:"JSON Roles"
       and kmsKey = flag "kms-key" (optional string) ~doc:"STRING KmsKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_settings
           (Values.UpdateSettingsRequest.make ?snsTopic
              ?defaultAssessmentReportsDestination:(Option.map
                                                      ~f:Values.AssessmentReportsDestination.of_json
                                                      defaultAssessmentReportsDestination)
              ?defaultProcessOwners:(Option.map ~f:Values.Roles.of_json
                                       defaultProcessOwners) ?kmsKey ())
           (Some Values.UpdateSettingsResponse.to_json)
           (Some Values.UpdateSettingsResponse.error_to_json)])
let validate_assessment_report_integrity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3RelativePath =
         flag "s3-relative-path" (required string) ~doc:"STRING S3Url" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.validate_assessment_report_integrity
           (Values.ValidateAssessmentReportIntegrityRequest.make
              ~s3RelativePath ())
           (Some Values.ValidateAssessmentReportIntegrityResponse.to_json)
           (Some
              Values.ValidateAssessmentReportIntegrityResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-assessment-report-evidence-folder",
       associate_assessment_report_evidence_folder);
    ("batch-associate-assessment-report-evidence",
      batch_associate_assessment_report_evidence);
    ("batch-create-delegation-by-assessment",
      batch_create_delegation_by_assessment);
    ("batch-delete-delegation-by-assessment",
      batch_delete_delegation_by_assessment);
    ("batch-disassociate-assessment-report-evidence",
      batch_disassociate_assessment_report_evidence);
    ("batch-import-evidence-to-assessment-control",
      batch_import_evidence_to_assessment_control);
    ("create-assessment", create_assessment);
    ("create-assessment-framework", create_assessment_framework);
    ("create-assessment-report", create_assessment_report);
    ("create-control", create_control);
    ("delete-assessment", delete_assessment);
    ("delete-assessment-framework", delete_assessment_framework);
    ("delete-assessment-framework-share", delete_assessment_framework_share);
    ("delete-assessment-report", delete_assessment_report);
    ("delete-control", delete_control);
    ("deregister-account", deregister_account);
    ("deregister-organization-admin-account",
      deregister_organization_admin_account);
    ("disassociate-assessment-report-evidence-folder",
      disassociate_assessment_report_evidence_folder);
    ("get-account-status", get_account_status);
    ("get-assessment", get_assessment);
    ("get-assessment-framework", get_assessment_framework);
    ("get-assessment-report-url", get_assessment_report_url);
    ("get-change-logs", get_change_logs);
    ("get-control", get_control);
    ("get-delegations", get_delegations);
    ("get-evidence", get_evidence);
    ("get-evidence-by-evidence-folder", get_evidence_by_evidence_folder);
    ("get-evidence-folder", get_evidence_folder);
    ("get-evidence-folders-by-assessment",
      get_evidence_folders_by_assessment);
    ("get-evidence-folders-by-assessment-control",
      get_evidence_folders_by_assessment_control);
    ("get-insights", get_insights);
    ("get-insights-by-assessment", get_insights_by_assessment);
    ("get-organization-admin-account", get_organization_admin_account);
    ("get-services-in-scope", get_services_in_scope);
    ("get-settings", get_settings);
    ("list-assessment-control-insights-by-control-domain",
      list_assessment_control_insights_by_control_domain);
    ("list-assessment-framework-share-requests",
      list_assessment_framework_share_requests);
    ("list-assessment-frameworks", list_assessment_frameworks);
    ("list-assessment-reports", list_assessment_reports);
    ("list-assessments", list_assessments);
    ("list-control-domain-insights", list_control_domain_insights);
    ("list-control-domain-insights-by-assessment",
      list_control_domain_insights_by_assessment);
    ("list-control-insights-by-control-domain",
      list_control_insights_by_control_domain);
    ("list-controls", list_controls);
    ("list-keywords-for-data-source", list_keywords_for_data_source);
    ("list-notifications", list_notifications);
    ("list-tags-for-resource", list_tags_for_resource);
    ("register-account", register_account);
    ("register-organization-admin-account",
      register_organization_admin_account);
    ("start-assessment-framework-share", start_assessment_framework_share);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-assessment", update_assessment);
    ("update-assessment-control", update_assessment_control);
    ("update-assessment-control-set-status",
      update_assessment_control_set_status);
    ("update-assessment-framework", update_assessment_framework);
    ("update-assessment-framework-share", update_assessment_framework_share);
    ("update-assessment-status", update_assessment_status);
    ("update-control", update_control);
    ("update-settings", update_settings);
    ("validate-assessment-report-integrity",
      validate_assessment_report_integrity)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.auditmanager
let apiVersion = "2017-07-25"
let endpointPrefix = "auditmanager"
let serviceFullName = "AWS Audit Manager"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module ControlCommentBody =
  struct
    type nonrec t = string
    let context_ = "ControlCommentBody"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:500) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ControlCommentBody" j
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module Username =
  struct
    type nonrec t = string
    let context_ = "Username"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9-_()\\s\\+=,.@]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Username" j
    let to_json = simple_to_json to_value
  end
module KeywordInputType =
  struct
    type nonrec t =
      | SELECT_FROM_LIST 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SELECT_FROM_LIST -> "SELECT_FROM_LIST"
      | Non_static_id s -> s
    let of_string =
      function
      | "SELECT_FROM_LIST" -> SELECT_FROM_LIST
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration KeywordInputType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"KeywordInputType" j)
    let to_json = simple_to_json to_value
  end
module KeywordValue =
  struct
    type nonrec t = string
    let context_ = "KeywordValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z_0-9-\\s().]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KeywordValue" j
    let to_json = simple_to_json to_value
  end
module ControlComment =
  struct
    type nonrec t =
      {
      authorName: Username.t option ;
      commentBody: ControlCommentBody.t option ;
      postedDate: Timestamp.t option }
    let make ?authorName =
      fun ?commentBody ->
        fun ?postedDate -> fun () -> { authorName; commentBody; postedDate }
    let to_value x =
      structure_to_value
        [("authorName", (Option.map x.authorName ~f:Username.to_value));
        ("commentBody",
          (Option.map x.commentBody ~f:ControlCommentBody.to_value));
        ("postedDate", (Option.map x.postedDate ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let postedDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "postedDate") in
      let commentBody =
        (Option.map ~f:ControlCommentBody.of_xml)
          (Xml.child xml_arg0 "commentBody") in
      let authorName =
        (Option.map ~f:Username.of_xml) (Xml.child xml_arg0 "authorName") in
      make ?postedDate ?commentBody ?authorName ()
    let of_json json =
      let postedDate = field_map json "postedDate" Timestamp.of_json in
      let commentBody =
        field_map json "commentBody" ControlCommentBody.of_json in
      let authorName = field_map json "authorName" Username.of_json in
      make ?postedDate ?commentBody ?authorName ()
    let to_json v = composed_to_json to_value v
  end
module NonEmptyString =
  struct
    type nonrec t = string
    let context_ = "NonEmptyString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NonEmptyString" j
    let to_json = simple_to_json to_value
  end
module SourceDescription =
  struct
    type nonrec t = string
    let context_ = "SourceDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SourceDescription" j
    let to_json = simple_to_json to_value
  end
module SourceFrequency =
  struct
    type nonrec t =
      | DAILY 
      | WEEKLY 
      | MONTHLY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DAILY -> "DAILY"
      | WEEKLY -> "WEEKLY"
      | MONTHLY -> "MONTHLY"
      | Non_static_id s -> s
    let of_string =
      function
      | "DAILY" -> DAILY
      | "WEEKLY" -> WEEKLY
      | "MONTHLY" -> MONTHLY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SourceFrequency" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SourceFrequency" j)
    let to_json = simple_to_json to_value
  end
module SourceKeyword =
  struct
    type nonrec t =
      {
      keywordInputType: KeywordInputType.t option ;
      keywordValue: KeywordValue.t option }
    let make ?keywordInputType =
      fun ?keywordValue -> fun () -> { keywordInputType; keywordValue }
    let to_value x =
      structure_to_value
        [("keywordInputType",
           (Option.map x.keywordInputType ~f:KeywordInputType.to_value));
        ("keywordValue",
          (Option.map x.keywordValue ~f:KeywordValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keywordValue =
        (Option.map ~f:KeywordValue.of_xml)
          (Xml.child xml_arg0 "keywordValue") in
      let keywordInputType =
        (Option.map ~f:KeywordInputType.of_xml)
          (Xml.child xml_arg0 "keywordInputType") in
      make ?keywordValue ?keywordInputType ()
    let of_json json =
      let keywordValue = field_map json "keywordValue" KeywordValue.of_json in
      let keywordInputType =
        field_map json "keywordInputType" KeywordInputType.of_json in
      make ?keywordValue ?keywordInputType ()
    let to_json v = composed_to_json to_value v
  end
module SourceName =
  struct
    type nonrec t = string
    let context_ = "SourceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SourceName" j
    let to_json = simple_to_json to_value
  end
module SourceSetUpOption =
  struct
    type nonrec t =
      | System_Controls_Mapping 
      | Procedural_Controls_Mapping 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | System_Controls_Mapping -> "System_Controls_Mapping"
      | Procedural_Controls_Mapping -> "Procedural_Controls_Mapping"
      | Non_static_id s -> s
    let of_string =
      function
      | "System_Controls_Mapping" -> System_Controls_Mapping
      | "Procedural_Controls_Mapping" -> Procedural_Controls_Mapping
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SourceSetUpOption" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SourceSetUpOption" j)
    let to_json = simple_to_json to_value
  end
module SourceType =
  struct
    type nonrec t =
      | AWS_Cloudtrail 
      | AWS_Config 
      | AWS_Security_Hub 
      | AWS_API_Call 
      | MANUAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AWS_Cloudtrail -> "AWS_Cloudtrail"
      | AWS_Config -> "AWS_Config"
      | AWS_Security_Hub -> "AWS_Security_Hub"
      | AWS_API_Call -> "AWS_API_Call"
      | MANUAL -> "MANUAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "AWS_Cloudtrail" -> AWS_Cloudtrail
      | "AWS_Config" -> AWS_Config
      | "AWS_Security_Hub" -> AWS_Security_Hub
      | "AWS_API_Call" -> AWS_API_Call
      | "MANUAL" -> MANUAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SourceType" j)
    let to_json = simple_to_json to_value
  end
module TroubleshootingText =
  struct
    type nonrec t = string
    let context_ = "TroubleshootingText"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TroubleshootingText" j
    let to_json = simple_to_json to_value
  end
module UUID =
  struct
    type nonrec t = string
    let context_ = "UUID"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:36) >>=
             (fun () ->
                (check_string_max i ~max:36) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"UUID" j
    let to_json = simple_to_json to_value
  end
module ControlComments =
  struct
    type nonrec t = ControlComment.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ControlComment.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ControlComment.of_xml)
    let of_json j =
      list_of_json ~kind:"ControlComments" ~of_json:ControlComment.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlDescription =
  struct
    type nonrec t = string
    let context_ = "ControlDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ControlDescription" j
    let to_json = simple_to_json to_value
  end
module ControlName =
  struct
    type nonrec t = string
    let context_ = "ControlName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:300) >>=
                  (fun () -> check_pattern i ~pattern:"^[^\\\\]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ControlName" j
    let to_json = simple_to_json to_value
  end
module ControlResponse =
  struct
    type nonrec t =
      | MANUAL 
      | AUTOMATE 
      | DEFER 
      | IGNORE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MANUAL -> "MANUAL"
      | AUTOMATE -> "AUTOMATE"
      | DEFER -> "DEFER"
      | IGNORE -> "IGNORE"
      | Non_static_id s -> s
    let of_string =
      function
      | "MANUAL" -> MANUAL
      | "AUTOMATE" -> AUTOMATE
      | "DEFER" -> DEFER
      | "IGNORE" -> IGNORE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ControlResponse" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ControlResponse" j)
    let to_json = simple_to_json to_value
  end
module ControlStatus =
  struct
    type nonrec t =
      | UNDER_REVIEW 
      | REVIEWED 
      | INACTIVE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UNDER_REVIEW -> "UNDER_REVIEW"
      | REVIEWED -> "REVIEWED"
      | INACTIVE -> "INACTIVE"
      | Non_static_id s -> s
    let of_string =
      function
      | "UNDER_REVIEW" -> UNDER_REVIEW
      | "REVIEWED" -> REVIEWED
      | "INACTIVE" -> INACTIVE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ControlStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ControlStatus" j)
    let to_json = simple_to_json to_value
  end
module EvidenceSources =
  struct
    type nonrec t = NonEmptyString.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"EvidenceSources" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AssessmentName =
  struct
    type nonrec t = string
    let context_ = "AssessmentName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:300) >>=
                  (fun () -> check_pattern i ~pattern:"^[^\\\\]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AssessmentName" j
    let to_json = simple_to_json to_value
  end
module ControlSetId =
  struct
    type nonrec t = string
    let context_ = "ControlSetId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:300) >>=
                  (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ControlSetId" j
    let to_json = simple_to_json to_value
  end
module CreatedBy =
  struct
    type nonrec t = string
    let context_ = "CreatedBy"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9\\s-_()\\[\\]]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CreatedBy" j
    let to_json = simple_to_json to_value
  end
module DelegationComment =
  struct
    type nonrec t = string
    let context_ = "DelegationComment"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:350) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DelegationComment" j
    let to_json = simple_to_json to_value
  end
module DelegationStatus =
  struct
    type nonrec t =
      | IN_PROGRESS 
      | UNDER_REVIEW 
      | COMPLETE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | IN_PROGRESS -> "IN_PROGRESS"
      | UNDER_REVIEW -> "UNDER_REVIEW"
      | COMPLETE -> "COMPLETE"
      | Non_static_id s -> s
    let of_string =
      function
      | "IN_PROGRESS" -> IN_PROGRESS
      | "UNDER_REVIEW" -> UNDER_REVIEW
      | "COMPLETE" -> COMPLETE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DelegationStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DelegationStatus" j)
    let to_json = simple_to_json to_value
  end
module IamArn =
  struct
    type nonrec t = string
    let context_ = "IamArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"^arn:.*:iam:.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IamArn" j
    let to_json = simple_to_json to_value
  end
module RoleType =
  struct
    type nonrec t =
      | PROCESS_OWNER 
      | RESOURCE_OWNER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PROCESS_OWNER -> "PROCESS_OWNER"
      | RESOURCE_OWNER -> "RESOURCE_OWNER"
      | Non_static_id s -> s
    let of_string =
      function
      | "PROCESS_OWNER" -> PROCESS_OWNER
      | "RESOURCE_OWNER" -> RESOURCE_OWNER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration RoleType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RoleType" j)
    let to_json = simple_to_json to_value
  end
module ControlMappingSource =
  struct
    type nonrec t =
      {
      sourceId: UUID.t option ;
      sourceName: SourceName.t option ;
      sourceDescription: SourceDescription.t option ;
      sourceSetUpOption: SourceSetUpOption.t option ;
      sourceType: SourceType.t option ;
      sourceKeyword: SourceKeyword.t option ;
      sourceFrequency: SourceFrequency.t option ;
      troubleshootingText: TroubleshootingText.t option }
    let make ?sourceId =
      fun ?sourceName ->
        fun ?sourceDescription ->
          fun ?sourceSetUpOption ->
            fun ?sourceType ->
              fun ?sourceKeyword ->
                fun ?sourceFrequency ->
                  fun ?troubleshootingText ->
                    fun () ->
                      {
                        sourceId;
                        sourceName;
                        sourceDescription;
                        sourceSetUpOption;
                        sourceType;
                        sourceKeyword;
                        sourceFrequency;
                        troubleshootingText
                      }
    let to_value x =
      structure_to_value
        [("sourceId", (Option.map x.sourceId ~f:UUID.to_value));
        ("sourceName", (Option.map x.sourceName ~f:SourceName.to_value));
        ("sourceDescription",
          (Option.map x.sourceDescription ~f:SourceDescription.to_value));
        ("sourceSetUpOption",
          (Option.map x.sourceSetUpOption ~f:SourceSetUpOption.to_value));
        ("sourceType", (Option.map x.sourceType ~f:SourceType.to_value));
        ("sourceKeyword",
          (Option.map x.sourceKeyword ~f:SourceKeyword.to_value));
        ("sourceFrequency",
          (Option.map x.sourceFrequency ~f:SourceFrequency.to_value));
        ("troubleshootingText",
          (Option.map x.troubleshootingText ~f:TroubleshootingText.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let troubleshootingText =
        (Option.map ~f:TroubleshootingText.of_xml)
          (Xml.child xml_arg0 "troubleshootingText") in
      let sourceFrequency =
        (Option.map ~f:SourceFrequency.of_xml)
          (Xml.child xml_arg0 "sourceFrequency") in
      let sourceKeyword =
        (Option.map ~f:SourceKeyword.of_xml)
          (Xml.child xml_arg0 "sourceKeyword") in
      let sourceType =
        (Option.map ~f:SourceType.of_xml) (Xml.child xml_arg0 "sourceType") in
      let sourceSetUpOption =
        (Option.map ~f:SourceSetUpOption.of_xml)
          (Xml.child xml_arg0 "sourceSetUpOption") in
      let sourceDescription =
        (Option.map ~f:SourceDescription.of_xml)
          (Xml.child xml_arg0 "sourceDescription") in
      let sourceName =
        (Option.map ~f:SourceName.of_xml) (Xml.child xml_arg0 "sourceName") in
      let sourceId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "sourceId") in
      make ?troubleshootingText ?sourceFrequency ?sourceKeyword ?sourceType
        ?sourceSetUpOption ?sourceDescription ?sourceName ?sourceId ()
    let of_json json =
      let troubleshootingText =
        field_map json "troubleshootingText" TroubleshootingText.of_json in
      let sourceFrequency =
        field_map json "sourceFrequency" SourceFrequency.of_json in
      let sourceKeyword =
        field_map json "sourceKeyword" SourceKeyword.of_json in
      let sourceType = field_map json "sourceType" SourceType.of_json in
      let sourceSetUpOption =
        field_map json "sourceSetUpOption" SourceSetUpOption.of_json in
      let sourceDescription =
        field_map json "sourceDescription" SourceDescription.of_json in
      let sourceName = field_map json "sourceName" SourceName.of_json in
      let sourceId = field_map json "sourceId" UUID.of_json in
      make ?troubleshootingText ?sourceFrequency ?sourceKeyword ?sourceType
        ?sourceSetUpOption ?sourceDescription ?sourceName ?sourceId ()
    let to_json v = composed_to_json to_value v
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^(?!aws:)[a-zA-Z+-=._:/]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:".{0,255}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module AssessmentControl =
  struct
    type nonrec t =
      {
      id: UUID.t option ;
      name: ControlName.t option ;
      description: ControlDescription.t option ;
      status: ControlStatus.t option ;
      response: ControlResponse.t option ;
      comments: ControlComments.t option ;
      evidenceSources: EvidenceSources.t option ;
      evidenceCount: Integer.t option ;
      assessmentReportEvidenceCount: Integer.t option }
    let make ?id =
      fun ?name ->
        fun ?description ->
          fun ?status ->
            fun ?response ->
              fun ?comments ->
                fun ?evidenceSources ->
                  fun ?evidenceCount ->
                    fun ?assessmentReportEvidenceCount ->
                      fun () ->
                        {
                          id;
                          name;
                          description;
                          status;
                          response;
                          comments;
                          evidenceSources;
                          evidenceCount;
                          assessmentReportEvidenceCount
                        }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:UUID.to_value));
        ("name", (Option.map x.name ~f:ControlName.to_value));
        ("description",
          (Option.map x.description ~f:ControlDescription.to_value));
        ("status", (Option.map x.status ~f:ControlStatus.to_value));
        ("response", (Option.map x.response ~f:ControlResponse.to_value));
        ("comments", (Option.map x.comments ~f:ControlComments.to_value));
        ("evidenceSources",
          (Option.map x.evidenceSources ~f:EvidenceSources.to_value));
        ("evidenceCount", (Option.map x.evidenceCount ~f:Integer.to_value));
        ("assessmentReportEvidenceCount",
          (Option.map x.assessmentReportEvidenceCount ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessmentReportEvidenceCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "assessmentReportEvidenceCount") in
      let evidenceCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "evidenceCount") in
      let evidenceSources =
        (Option.map ~f:EvidenceSources.of_xml)
          (Xml.child xml_arg0 "evidenceSources") in
      let comments =
        (Option.map ~f:ControlComments.of_xml)
          (Xml.child xml_arg0 "comments") in
      let response =
        (Option.map ~f:ControlResponse.of_xml)
          (Xml.child xml_arg0 "response") in
      let status =
        (Option.map ~f:ControlStatus.of_xml) (Xml.child xml_arg0 "status") in
      let description =
        (Option.map ~f:ControlDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:ControlName.of_xml) (Xml.child xml_arg0 "name") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      make ?assessmentReportEvidenceCount ?evidenceCount ?evidenceSources
        ?comments ?response ?status ?description ?name ?id ()
    let of_json json =
      let assessmentReportEvidenceCount =
        field_map json "assessmentReportEvidenceCount" Integer.of_json in
      let evidenceCount = field_map json "evidenceCount" Integer.of_json in
      let evidenceSources =
        field_map json "evidenceSources" EvidenceSources.of_json in
      let comments = field_map json "comments" ControlComments.of_json in
      let response = field_map json "response" ControlResponse.of_json in
      let status = field_map json "status" ControlStatus.of_json in
      let description =
        field_map json "description" ControlDescription.of_json in
      let name = field_map json "name" ControlName.of_json in
      let id = field_map json "id" UUID.of_json in
      make ?assessmentReportEvidenceCount ?evidenceCount ?evidenceSources
        ?comments ?response ?status ?description ?name ?id ()
    let to_json v = composed_to_json to_value v
  end
module Delegation =
  struct
    type nonrec t =
      {
      id: UUID.t option ;
      assessmentName: AssessmentName.t option ;
      assessmentId: UUID.t option ;
      status: DelegationStatus.t option ;
      roleArn: IamArn.t option ;
      roleType: RoleType.t option ;
      creationTime: Timestamp.t option ;
      lastUpdated: Timestamp.t option ;
      controlSetId: ControlSetId.t option ;
      comment: DelegationComment.t option ;
      createdBy: CreatedBy.t option }
    let make ?id =
      fun ?assessmentName ->
        fun ?assessmentId ->
          fun ?status ->
            fun ?roleArn ->
              fun ?roleType ->
                fun ?creationTime ->
                  fun ?lastUpdated ->
                    fun ?controlSetId ->
                      fun ?comment ->
                        fun ?createdBy ->
                          fun () ->
                            {
                              id;
                              assessmentName;
                              assessmentId;
                              status;
                              roleArn;
                              roleType;
                              creationTime;
                              lastUpdated;
                              controlSetId;
                              comment;
                              createdBy
                            }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:UUID.to_value));
        ("assessmentName",
          (Option.map x.assessmentName ~f:AssessmentName.to_value));
        ("assessmentId", (Option.map x.assessmentId ~f:UUID.to_value));
        ("status", (Option.map x.status ~f:DelegationStatus.to_value));
        ("roleArn", (Option.map x.roleArn ~f:IamArn.to_value));
        ("roleType", (Option.map x.roleType ~f:RoleType.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("lastUpdated", (Option.map x.lastUpdated ~f:Timestamp.to_value));
        ("controlSetId",
          (Option.map x.controlSetId ~f:ControlSetId.to_value));
        ("comment", (Option.map x.comment ~f:DelegationComment.to_value));
        ("createdBy", (Option.map x.createdBy ~f:CreatedBy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let createdBy =
        (Option.map ~f:CreatedBy.of_xml) (Xml.child xml_arg0 "createdBy") in
      let comment =
        (Option.map ~f:DelegationComment.of_xml)
          (Xml.child xml_arg0 "comment") in
      let controlSetId =
        (Option.map ~f:ControlSetId.of_xml)
          (Xml.child xml_arg0 "controlSetId") in
      let lastUpdated =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdated") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let roleType =
        (Option.map ~f:RoleType.of_xml) (Xml.child xml_arg0 "roleType") in
      let roleArn =
        (Option.map ~f:IamArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let status =
        (Option.map ~f:DelegationStatus.of_xml) (Xml.child xml_arg0 "status") in
      let assessmentId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "assessmentId") in
      let assessmentName =
        (Option.map ~f:AssessmentName.of_xml)
          (Xml.child xml_arg0 "assessmentName") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      make ?createdBy ?comment ?controlSetId ?lastUpdated ?creationTime
        ?roleType ?roleArn ?status ?assessmentId ?assessmentName ?id ()
    let of_json json =
      let createdBy = field_map json "createdBy" CreatedBy.of_json in
      let comment = field_map json "comment" DelegationComment.of_json in
      let controlSetId = field_map json "controlSetId" ControlSetId.of_json in
      let lastUpdated = field_map json "lastUpdated" Timestamp.of_json in
      let creationTime = field_map json "creationTime" Timestamp.of_json in
      let roleType = field_map json "roleType" RoleType.of_json in
      let roleArn = field_map json "roleArn" IamArn.of_json in
      let status = field_map json "status" DelegationStatus.of_json in
      let assessmentId = field_map json "assessmentId" UUID.of_json in
      let assessmentName =
        field_map json "assessmentName" AssessmentName.of_json in
      let id = field_map json "id" UUID.of_json in
      make ?createdBy ?comment ?controlSetId ?lastUpdated ?creationTime
        ?roleType ?roleArn ?status ?assessmentId ?assessmentName ?id ()
    let to_json v = composed_to_json to_value v
  end
module Role =
  struct
    type nonrec t = {
      roleType: RoleType.t option ;
      roleArn: IamArn.t option }
    let make ?roleType = fun ?roleArn -> fun () -> { roleType; roleArn }
    let to_value x =
      structure_to_value
        [("roleType", (Option.map x.roleType ~f:RoleType.to_value));
        ("roleArn", (Option.map x.roleArn ~f:IamArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let roleArn =
        (Option.map ~f:IamArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let roleType =
        (Option.map ~f:RoleType.of_xml) (Xml.child xml_arg0 "roleType") in
      make ?roleArn ?roleType ()
    let of_json json =
      let roleArn = field_map json "roleArn" IamArn.of_json in
      let roleType = field_map json "roleType" RoleType.of_json in
      make ?roleArn ?roleType ()
    let to_json v = composed_to_json to_value v
  end
module AccountId =
  struct
    type nonrec t = string
    let context_ = "AccountId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:12) >>=
             (fun () ->
                (check_string_max i ~max:12) >>=
                  (fun () -> check_pattern i ~pattern:"^[0-9]{12}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AccountId" j
    let to_json = simple_to_json to_value
  end
module AccountName =
  struct
    type nonrec t = string
    let context_ = "AccountName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:50) >>=
                  (fun () -> check_pattern i ~pattern:"^[\\u0020-\\u007E]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AccountName" j
    let to_json = simple_to_json to_value
  end
module EmailAddress =
  struct
    type nonrec t = string
    let context_ = "EmailAddress"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:320) >>=
                  (fun () -> check_pattern i ~pattern:"^.*@.*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EmailAddress" j
    let to_json = simple_to_json to_value
  end
module AWSServiceName =
  struct
    type nonrec t = string
    let context_ = "AWSServiceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:40) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9-\\s().]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AWSServiceName" j
    let to_json = simple_to_json to_value
  end
module ActionPlanInstructions =
  struct
    type nonrec t = string
    let context_ = "ActionPlanInstructions"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ActionPlanInstructions" j
    let to_json = simple_to_json to_value
  end
module ActionPlanTitle =
  struct
    type nonrec t = string
    let context_ = "ActionPlanTitle"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:300) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ActionPlanTitle" j
    let to_json = simple_to_json to_value
  end
module AuditManagerArn =
  struct
    type nonrec t = string
    let context_ = "AuditManagerArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^arn:.*:auditmanager:.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AuditManagerArn" j
    let to_json = simple_to_json to_value
  end
module ControlMappingSources =
  struct
    type nonrec t = ControlMappingSource.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:ControlMappingSource.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ControlMappingSource.of_xml)
    let of_json j =
      list_of_json ~kind:"ControlMappingSources"
        ~of_json:ControlMappingSource.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlSources =
  struct
    type nonrec t = string
    let context_ = "ControlSources"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z_0-9-\\s.,]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ControlSources" j
    let to_json = simple_to_json to_value
  end
module ControlType =
  struct
    type nonrec t =
      | Standard 
      | Custom 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Standard -> "Standard"
      | Custom -> "Custom"
      | Non_static_id s -> s
    let of_string =
      function
      | "Standard" -> Standard
      | "Custom" -> Custom
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ControlType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ControlType" j)
    let to_json = simple_to_json to_value
  end
module LastUpdatedBy =
  struct
    type nonrec t = string
    let context_ = "LastUpdatedBy"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9\\s-_()\\[\\]]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LastUpdatedBy" j
    let to_json = simple_to_json to_value
  end
module TagMap =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module TestingInformation =
  struct
    type nonrec t = string
    let context_ = "TestingInformation"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TestingInformation" j
    let to_json = simple_to_json to_value
  end
module AssessmentControls =
  struct
    type nonrec t = AssessmentControl.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AssessmentControl.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AssessmentControl.of_xml)
    let of_json j =
      list_of_json ~kind:"AssessmentControls"
        ~of_json:AssessmentControl.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlSetStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | UNDER_REVIEW 
      | REVIEWED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | UNDER_REVIEW -> "UNDER_REVIEW"
      | REVIEWED -> "REVIEWED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "UNDER_REVIEW" -> UNDER_REVIEW
      | "REVIEWED" -> REVIEWED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ControlSetStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ControlSetStatus" j)
    let to_json = simple_to_json to_value
  end
module Delegations =
  struct
    type nonrec t = Delegation.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Delegation.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Delegation.of_xml)
    let of_json j =
      list_of_json ~kind:"Delegations" ~of_json:Delegation.of_json j
    let to_json v = composed_to_json to_value v
  end
module Roles =
  struct
    type nonrec t = Role.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Role.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Role.of_xml)
    let of_json j = list_of_json ~kind:"Roles" ~of_json:Role.of_json j
    let to_json v = composed_to_json to_value v
  end
module AWSAccount =
  struct
    type nonrec t =
      {
      id: AccountId.t option ;
      emailAddress: EmailAddress.t option ;
      name: AccountName.t option }
    let make ?id =
      fun ?emailAddress -> fun ?name -> fun () -> { id; emailAddress; name }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:AccountId.to_value));
        ("emailAddress",
          (Option.map x.emailAddress ~f:EmailAddress.to_value));
        ("name", (Option.map x.name ~f:AccountName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:AccountName.of_xml) (Xml.child xml_arg0 "name") in
      let emailAddress =
        (Option.map ~f:EmailAddress.of_xml)
          (Xml.child xml_arg0 "emailAddress") in
      let id = (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "id") in
      make ?name ?emailAddress ?id ()
    let of_json json =
      let name = field_map json "name" AccountName.of_json in
      let emailAddress = field_map json "emailAddress" EmailAddress.of_json in
      let id = field_map json "id" AccountId.of_json in
      make ?name ?emailAddress ?id ()
    let to_json v = composed_to_json to_value v
  end
module AWSService =
  struct
    type nonrec t = {
      serviceName: AWSServiceName.t option }
    let make ?serviceName = fun () -> { serviceName }
    let to_value x =
      structure_to_value
        [("serviceName",
           (Option.map x.serviceName ~f:AWSServiceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceName =
        (Option.map ~f:AWSServiceName.of_xml)
          (Xml.child xml_arg0 "serviceName") in
      make ?serviceName ()
    let of_json json =
      let serviceName = field_map json "serviceName" AWSServiceName.of_json in
      make ?serviceName ()
    let to_json v = composed_to_json to_value v
  end
module Control =
  struct
    type nonrec t =
      {
      arn: AuditManagerArn.t option ;
      id: UUID.t option ;
      type_: ControlType.t option ;
      name: ControlName.t option ;
      description: ControlDescription.t option ;
      testingInformation: TestingInformation.t option ;
      actionPlanTitle: ActionPlanTitle.t option ;
      actionPlanInstructions: ActionPlanInstructions.t option ;
      controlSources: ControlSources.t option ;
      controlMappingSources: ControlMappingSources.t option ;
      createdAt: Timestamp.t option ;
      lastUpdatedAt: Timestamp.t option ;
      createdBy: CreatedBy.t option ;
      lastUpdatedBy: LastUpdatedBy.t option ;
      tags: TagMap.t option }
    let make ?arn =
      fun ?id ->
        fun ?type_ ->
          fun ?name ->
            fun ?description ->
              fun ?testingInformation ->
                fun ?actionPlanTitle ->
                  fun ?actionPlanInstructions ->
                    fun ?controlSources ->
                      fun ?controlMappingSources ->
                        fun ?createdAt ->
                          fun ?lastUpdatedAt ->
                            fun ?createdBy ->
                              fun ?lastUpdatedBy ->
                                fun ?tags ->
                                  fun () ->
                                    {
                                      arn;
                                      id;
                                      type_;
                                      name;
                                      description;
                                      testingInformation;
                                      actionPlanTitle;
                                      actionPlanInstructions;
                                      controlSources;
                                      controlMappingSources;
                                      createdAt;
                                      lastUpdatedAt;
                                      createdBy;
                                      lastUpdatedBy;
                                      tags
                                    }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AuditManagerArn.to_value));
        ("id", (Option.map x.id ~f:UUID.to_value));
        ("type", (Option.map x.type_ ~f:ControlType.to_value));
        ("name", (Option.map x.name ~f:ControlName.to_value));
        ("description",
          (Option.map x.description ~f:ControlDescription.to_value));
        ("testingInformation",
          (Option.map x.testingInformation ~f:TestingInformation.to_value));
        ("actionPlanTitle",
          (Option.map x.actionPlanTitle ~f:ActionPlanTitle.to_value));
        ("actionPlanInstructions",
          (Option.map x.actionPlanInstructions
             ~f:ActionPlanInstructions.to_value));
        ("controlSources",
          (Option.map x.controlSources ~f:ControlSources.to_value));
        ("controlMappingSources",
          (Option.map x.controlMappingSources
             ~f:ControlMappingSources.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("lastUpdatedAt", (Option.map x.lastUpdatedAt ~f:Timestamp.to_value));
        ("createdBy", (Option.map x.createdBy ~f:CreatedBy.to_value));
        ("lastUpdatedBy",
          (Option.map x.lastUpdatedBy ~f:LastUpdatedBy.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let lastUpdatedBy =
        (Option.map ~f:LastUpdatedBy.of_xml)
          (Xml.child xml_arg0 "lastUpdatedBy") in
      let createdBy =
        (Option.map ~f:CreatedBy.of_xml) (Xml.child xml_arg0 "createdBy") in
      let lastUpdatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let controlMappingSources =
        (Option.map ~f:ControlMappingSources.of_xml)
          (Xml.child xml_arg0 "controlMappingSources") in
      let controlSources =
        (Option.map ~f:ControlSources.of_xml)
          (Xml.child xml_arg0 "controlSources") in
      let actionPlanInstructions =
        (Option.map ~f:ActionPlanInstructions.of_xml)
          (Xml.child xml_arg0 "actionPlanInstructions") in
      let actionPlanTitle =
        (Option.map ~f:ActionPlanTitle.of_xml)
          (Xml.child xml_arg0 "actionPlanTitle") in
      let testingInformation =
        (Option.map ~f:TestingInformation.of_xml)
          (Xml.child xml_arg0 "testingInformation") in
      let description =
        (Option.map ~f:ControlDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:ControlName.of_xml) (Xml.child xml_arg0 "name") in
      let type_ =
        (Option.map ~f:ControlType.of_xml) (Xml.child xml_arg0 "type") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      let arn =
        (Option.map ~f:AuditManagerArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?tags ?lastUpdatedBy ?createdBy ?lastUpdatedAt ?createdAt
        ?controlMappingSources ?controlSources ?actionPlanInstructions
        ?actionPlanTitle ?testingInformation ?description ?name ?type_ ?id
        ?arn ()
    let of_json json =
      let tags = field_map json "tags" TagMap.of_json in
      let lastUpdatedBy =
        field_map json "lastUpdatedBy" LastUpdatedBy.of_json in
      let createdBy = field_map json "createdBy" CreatedBy.of_json in
      let lastUpdatedAt = field_map json "lastUpdatedAt" Timestamp.of_json in
      let createdAt = field_map json "createdAt" Timestamp.of_json in
      let controlMappingSources =
        field_map json "controlMappingSources" ControlMappingSources.of_json in
      let controlSources =
        field_map json "controlSources" ControlSources.of_json in
      let actionPlanInstructions =
        field_map json "actionPlanInstructions"
          ActionPlanInstructions.of_json in
      let actionPlanTitle =
        field_map json "actionPlanTitle" ActionPlanTitle.of_json in
      let testingInformation =
        field_map json "testingInformation" TestingInformation.of_json in
      let description =
        field_map json "description" ControlDescription.of_json in
      let name = field_map json "name" ControlName.of_json in
      let type_ = field_map json "type" ControlType.of_json in
      let id = field_map json "id" UUID.of_json in
      let arn = field_map json "arn" AuditManagerArn.of_json in
      make ?tags ?lastUpdatedBy ?createdBy ?lastUpdatedAt ?createdAt
        ?controlMappingSources ?controlSources ?actionPlanInstructions
        ?actionPlanTitle ?testingInformation ?description ?name ?type_ ?id
        ?arn ()
    let to_json v = composed_to_json to_value v
  end
module GenericArn =
  struct
    type nonrec t = string
    let context_ = "GenericArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"^arn:.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GenericArn" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:".*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module AssessmentControlSet =
  struct
    type nonrec t =
      {
      id: ControlSetId.t option ;
      description: NonEmptyString.t option ;
      status: ControlSetStatus.t option ;
      roles: Roles.t option ;
      controls: AssessmentControls.t option ;
      delegations: Delegations.t option ;
      systemEvidenceCount: Integer.t option ;
      manualEvidenceCount: Integer.t option }
    let make ?id =
      fun ?description ->
        fun ?status ->
          fun ?roles ->
            fun ?controls ->
              fun ?delegations ->
                fun ?systemEvidenceCount ->
                  fun ?manualEvidenceCount ->
                    fun () ->
                      {
                        id;
                        description;
                        status;
                        roles;
                        controls;
                        delegations;
                        systemEvidenceCount;
                        manualEvidenceCount
                      }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:ControlSetId.to_value));
        ("description",
          (Option.map x.description ~f:NonEmptyString.to_value));
        ("status", (Option.map x.status ~f:ControlSetStatus.to_value));
        ("roles", (Option.map x.roles ~f:Roles.to_value));
        ("controls", (Option.map x.controls ~f:AssessmentControls.to_value));
        ("delegations", (Option.map x.delegations ~f:Delegations.to_value));
        ("systemEvidenceCount",
          (Option.map x.systemEvidenceCount ~f:Integer.to_value));
        ("manualEvidenceCount",
          (Option.map x.manualEvidenceCount ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let manualEvidenceCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "manualEvidenceCount") in
      let systemEvidenceCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "systemEvidenceCount") in
      let delegations =
        (Option.map ~f:Delegations.of_xml) (Xml.child xml_arg0 "delegations") in
      let controls =
        (Option.map ~f:AssessmentControls.of_xml)
          (Xml.child xml_arg0 "controls") in
      let roles = (Option.map ~f:Roles.of_xml) (Xml.child xml_arg0 "roles") in
      let status =
        (Option.map ~f:ControlSetStatus.of_xml) (Xml.child xml_arg0 "status") in
      let description =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "description") in
      let id = (Option.map ~f:ControlSetId.of_xml) (Xml.child xml_arg0 "id") in
      make ?manualEvidenceCount ?systemEvidenceCount ?delegations ?controls
        ?roles ?status ?description ?id ()
    let of_json json =
      let manualEvidenceCount =
        field_map json "manualEvidenceCount" Integer.of_json in
      let systemEvidenceCount =
        field_map json "systemEvidenceCount" Integer.of_json in
      let delegations = field_map json "delegations" Delegations.of_json in
      let controls = field_map json "controls" AssessmentControls.of_json in
      let roles = field_map json "roles" Roles.of_json in
      let status = field_map json "status" ControlSetStatus.of_json in
      let description = field_map json "description" NonEmptyString.of_json in
      let id = field_map json "id" ControlSetId.of_json in
      make ?manualEvidenceCount ?systemEvidenceCount ?delegations ?controls
        ?roles ?status ?description ?id ()
    let to_json v = composed_to_json to_value v
  end
module AssessmentFrameworkDescription =
  struct
    type nonrec t = string
    let context_ = "AssessmentFrameworkDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:200) >>=
                  (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AssessmentFrameworkDescription" j
    let to_json = simple_to_json to_value
  end
module ComplianceType =
  struct
    type nonrec t = string
    let context_ = "ComplianceType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ComplianceType" j
    let to_json = simple_to_json to_value
  end
module Filename =
  struct
    type nonrec t = string
    let context_ = "Filename"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[\\w,\\s-]+\\.[A-Za-z]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Filename" j
    let to_json = simple_to_json to_value
  end
module AssessmentReportDestinationType =
  struct
    type nonrec t =
      | S3 
      | Non_static_id of string 
    let make i = i
    let to_string = function | S3 -> "S3" | Non_static_id s -> s
    let of_string = function | "S3" -> S3 | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AssessmentReportDestinationType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"AssessmentReportDestinationType" j)
    let to_json = simple_to_json to_value
  end
module S3Url =
  struct
    type nonrec t = string
    let context_ = "S3Url"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(S|s)3:\\/\\/[a-zA-Z0-9\\-\\.\\(\\)\\'\\*\\_\\!\\/]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3Url" j
    let to_json = simple_to_json to_value
  end
module AWSAccounts =
  struct
    type nonrec t = AWSAccount.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AWSAccount.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AWSAccount.of_xml)
    let of_json j =
      list_of_json ~kind:"AWSAccounts" ~of_json:AWSAccount.of_json j
    let to_json v = composed_to_json to_value v
  end
module AWSServices =
  struct
    type nonrec t = AWSService.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AWSService.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AWSService.of_xml)
    let of_json j =
      list_of_json ~kind:"AWSServices" ~of_json:AWSService.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlSetName =
  struct
    type nonrec t = string
    let context_ = "ControlSetName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:300) >>=
                  (fun () -> check_pattern i ~pattern:"^[^\\\\\\_]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ControlSetName" j
    let to_json = simple_to_json to_value
  end
module Controls =
  struct
    type nonrec t = Control.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:Control.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Control.of_xml)
    let of_json j = list_of_json ~kind:"Controls" ~of_json:Control.of_json j
    let to_json v = composed_to_json to_value v
  end
module CreateAssessmentFrameworkControl =
  struct
    type nonrec t = {
      id: UUID.t }
    let context_ = "CreateAssessmentFrameworkControl"
    let make ~id = fun () -> { id }
    let to_value x = structure_to_value [("id", (Some (UUID.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_json json =
      let id = field_map_exn json "id" UUID.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end
module NullableInteger =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for NullableInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module EvidenceAttributeKey =
  struct
    type nonrec t = string
    let context_ = "EvidenceAttributeKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EvidenceAttributeKey" j
    let to_json = simple_to_json to_value
  end
module EvidenceAttributeValue =
  struct
    type nonrec t = string
    let context_ = "EvidenceAttributeValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:200) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EvidenceAttributeValue" j
    let to_json = simple_to_json to_value
  end
module Resource =
  struct
    type nonrec t = {
      arn: GenericArn.t option ;
      value: String_.t option }
    let make ?arn = fun ?value -> fun () -> { arn; value }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:GenericArn.to_value));
        ("value", (Option.map x.value ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let arn = (Option.map ~f:GenericArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?value ?arn ()
    let of_json json =
      let value = field_map json "value" String_.of_json in
      let arn = field_map json "arn" GenericArn.of_json in
      make ?value ?arn ()
    let to_json v = composed_to_json to_value v
  end
module ValidationExceptionField =
  struct
    type nonrec t = {
      name: String_.t ;
      message: String_.t }
    let context_ = "ValidationExceptionField"
    let make ~name = fun ~message -> fun () -> { name; message }
    let to_value x =
      structure_to_value
        [("name", (Some (String_.to_value x.name)));
        ("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      let name =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~message ~name ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      let name = field_map_exn json "name" String_.of_json in
      make ~message ~name ()
    let to_json v = composed_to_json to_value v
  end
module AssessmentControlSets =
  struct
    type nonrec t = AssessmentControlSet.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AssessmentControlSet.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AssessmentControlSet.of_xml)
    let of_json j =
      list_of_json ~kind:"AssessmentControlSets"
        ~of_json:AssessmentControlSet.of_json j
    let to_json v = composed_to_json to_value v
  end
module FrameworkMetadata =
  struct
    type nonrec t =
      {
      name: AssessmentName.t option ;
      description: AssessmentFrameworkDescription.t option ;
      logo: Filename.t option ;
      complianceType: ComplianceType.t option }
    let make ?name =
      fun ?description ->
        fun ?logo ->
          fun ?complianceType ->
            fun () -> { name; description; logo; complianceType }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:AssessmentName.to_value));
        ("description",
          (Option.map x.description
             ~f:AssessmentFrameworkDescription.to_value));
        ("logo", (Option.map x.logo ~f:Filename.to_value));
        ("complianceType",
          (Option.map x.complianceType ~f:ComplianceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let complianceType =
        (Option.map ~f:ComplianceType.of_xml)
          (Xml.child xml_arg0 "complianceType") in
      let logo = (Option.map ~f:Filename.of_xml) (Xml.child xml_arg0 "logo") in
      let description =
        (Option.map ~f:AssessmentFrameworkDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:AssessmentName.of_xml) (Xml.child xml_arg0 "name") in
      make ?complianceType ?logo ?description ?name ()
    let of_json json =
      let complianceType =
        field_map json "complianceType" ComplianceType.of_json in
      let logo = field_map json "logo" Filename.of_json in
      let description =
        field_map json "description" AssessmentFrameworkDescription.of_json in
      let name = field_map json "name" AssessmentName.of_json in
      make ?complianceType ?logo ?description ?name ()
    let to_json v = composed_to_json to_value v
  end
module AssessmentDescription =
  struct
    type nonrec t = string
    let context_ = "AssessmentDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AssessmentDescription" j
    let to_json = simple_to_json to_value
  end
module AssessmentReportsDestination =
  struct
    type nonrec t =
      {
      destinationType: AssessmentReportDestinationType.t option ;
      destination: S3Url.t option }
    let make ?destinationType =
      fun ?destination -> fun () -> { destinationType; destination }
    let to_value x =
      structure_to_value
        [("destinationType",
           (Option.map x.destinationType
              ~f:AssessmentReportDestinationType.to_value));
        ("destination", (Option.map x.destination ~f:S3Url.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let destination =
        (Option.map ~f:S3Url.of_xml) (Xml.child xml_arg0 "destination") in
      let destinationType =
        (Option.map ~f:AssessmentReportDestinationType.of_xml)
          (Xml.child xml_arg0 "destinationType") in
      make ?destination ?destinationType ()
    let of_json json =
      let destination = field_map json "destination" S3Url.of_json in
      let destinationType =
        field_map json "destinationType"
          AssessmentReportDestinationType.of_json in
      make ?destination ?destinationType ()
    let to_json v = composed_to_json to_value v
  end
module AssessmentStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | INACTIVE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | INACTIVE -> "INACTIVE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "INACTIVE" -> INACTIVE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AssessmentStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AssessmentStatus" j)
    let to_json = simple_to_json to_value
  end
module Scope =
  struct
    type nonrec t =
      {
      awsAccounts: AWSAccounts.t option ;
      awsServices: AWSServices.t option }
    let make ?awsAccounts =
      fun ?awsServices -> fun () -> { awsAccounts; awsServices }
    let to_value x =
      structure_to_value
        [("awsAccounts", (Option.map x.awsAccounts ~f:AWSAccounts.to_value));
        ("awsServices", (Option.map x.awsServices ~f:AWSServices.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let awsServices =
        (Option.map ~f:AWSServices.of_xml) (Xml.child xml_arg0 "awsServices") in
      let awsAccounts =
        (Option.map ~f:AWSAccounts.of_xml) (Xml.child xml_arg0 "awsAccounts") in
      make ?awsServices ?awsAccounts ()
    let of_json json =
      let awsServices = field_map json "awsServices" AWSServices.of_json in
      let awsAccounts = field_map json "awsAccounts" AWSAccounts.of_json in
      make ?awsServices ?awsAccounts ()
    let to_json v = composed_to_json to_value v
  end
module ControlSet =
  struct
    type nonrec t =
      {
      id: UUID.t option ;
      name: ControlSetName.t option ;
      controls: Controls.t option }
    let make ?id =
      fun ?name -> fun ?controls -> fun () -> { id; name; controls }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:UUID.to_value));
        ("name", (Option.map x.name ~f:ControlSetName.to_value));
        ("controls", (Option.map x.controls ~f:Controls.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let controls =
        (Option.map ~f:Controls.of_xml) (Xml.child xml_arg0 "controls") in
      let name =
        (Option.map ~f:ControlSetName.of_xml) (Xml.child xml_arg0 "name") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      make ?controls ?name ?id ()
    let of_json json =
      let controls = field_map json "controls" Controls.of_json in
      let name = field_map json "name" ControlSetName.of_json in
      let id = field_map json "id" UUID.of_json in
      make ?controls ?name ?id ()
    let to_json v = composed_to_json to_value v
  end
module CreateAssessmentFrameworkControls =
  struct
    type nonrec t = CreateAssessmentFrameworkControl.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:CreateAssessmentFrameworkControl.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:CreateAssessmentFrameworkControl.of_xml)
    let of_json j =
      list_of_json ~kind:"CreateAssessmentFrameworkControls"
        ~of_json:CreateAssessmentFrameworkControl.of_json j
    let to_json v = composed_to_json to_value v
  end
module TimestampUUID =
  struct
    type nonrec t = string
    let context_ = "TimestampUUID"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:47) >>=
             (fun () ->
                (check_string_max i ~max:50) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^[0-9]{10,13}_[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TimestampUUID" j
    let to_json = simple_to_json to_value
  end
module EvidenceInsights =
  struct
    type nonrec t =
      {
      noncompliantEvidenceCount: NullableInteger.t option ;
      compliantEvidenceCount: NullableInteger.t option ;
      inconclusiveEvidenceCount: NullableInteger.t option }
    let make ?noncompliantEvidenceCount =
      fun ?compliantEvidenceCount ->
        fun ?inconclusiveEvidenceCount ->
          fun () ->
            {
              noncompliantEvidenceCount;
              compliantEvidenceCount;
              inconclusiveEvidenceCount
            }
    let to_value x =
      structure_to_value
        [("noncompliantEvidenceCount",
           (Option.map x.noncompliantEvidenceCount
              ~f:NullableInteger.to_value));
        ("compliantEvidenceCount",
          (Option.map x.compliantEvidenceCount ~f:NullableInteger.to_value));
        ("inconclusiveEvidenceCount",
          (Option.map x.inconclusiveEvidenceCount ~f:NullableInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let inconclusiveEvidenceCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "inconclusiveEvidenceCount") in
      let compliantEvidenceCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "compliantEvidenceCount") in
      let noncompliantEvidenceCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "noncompliantEvidenceCount") in
      make ?inconclusiveEvidenceCount ?compliantEvidenceCount
        ?noncompliantEvidenceCount ()
    let of_json json =
      let inconclusiveEvidenceCount =
        field_map json "inconclusiveEvidenceCount" NullableInteger.of_json in
      let compliantEvidenceCount =
        field_map json "compliantEvidenceCount" NullableInteger.of_json in
      let noncompliantEvidenceCount =
        field_map json "noncompliantEvidenceCount" NullableInteger.of_json in
      make ?inconclusiveEvidenceCount ?compliantEvidenceCount
        ?noncompliantEvidenceCount ()
    let to_json v = composed_to_json to_value v
  end
module AssessmentReportDescription =
  struct
    type nonrec t = string
    let context_ = "AssessmentReportDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AssessmentReportDescription" j
    let to_json = simple_to_json to_value
  end
module AssessmentReportName =
  struct
    type nonrec t = string
    let context_ = "AssessmentReportName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:300) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9-_\\.]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AssessmentReportName" j
    let to_json = simple_to_json to_value
  end
module AssessmentReportStatus =
  struct
    type nonrec t =
      | COMPLETE 
      | IN_PROGRESS 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | COMPLETE -> "COMPLETE"
      | IN_PROGRESS -> "IN_PROGRESS"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "COMPLETE" -> COMPLETE
      | "IN_PROGRESS" -> IN_PROGRESS
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AssessmentReportStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"AssessmentReportStatus" j)
    let to_json = simple_to_json to_value
  end
module ControlSetsCount =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ControlSetsCount" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ControlsCount =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ControlsCount" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module FrameworkDescription =
  struct
    type nonrec t = string
    let context_ = "FrameworkDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1000) >>=
                  (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FrameworkDescription" j
    let to_json = simple_to_json to_value
  end
module FrameworkName =
  struct
    type nonrec t = string
    let context_ = "FrameworkName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:300) >>=
                  (fun () -> check_pattern i ~pattern:"^[^\\\\]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FrameworkName" j
    let to_json = simple_to_json to_value
  end
module FrameworkType =
  struct
    type nonrec t =
      | Standard 
      | Custom 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Standard -> "Standard"
      | Custom -> "Custom"
      | Non_static_id s -> s
    let of_string =
      function
      | "Standard" -> Standard
      | "Custom" -> Custom
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FrameworkType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FrameworkType" j)
    let to_json = simple_to_json to_value
  end
module Region =
  struct
    type nonrec t = string
    let context_ = "Region"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^[a-z]{2}-[a-z]+-[0-9]{1}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Region" j
    let to_json = simple_to_json to_value
  end
module ShareRequestComment =
  struct
    type nonrec t = string
    let context_ = "ShareRequestComment"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:500) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ShareRequestComment" j
    let to_json = simple_to_json to_value
  end
module ShareRequestStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | REPLICATING 
      | SHARED 
      | EXPIRING 
      | FAILED 
      | EXPIRED 
      | DECLINED 
      | REVOKED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | REPLICATING -> "REPLICATING"
      | SHARED -> "SHARED"
      | EXPIRING -> "EXPIRING"
      | FAILED -> "FAILED"
      | EXPIRED -> "EXPIRED"
      | DECLINED -> "DECLINED"
      | REVOKED -> "REVOKED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "REPLICATING" -> REPLICATING
      | "SHARED" -> SHARED
      | "EXPIRING" -> EXPIRING
      | "FAILED" -> FAILED
      | "EXPIRED" -> EXPIRED
      | "DECLINED" -> DECLINED
      | "REVOKED" -> REVOKED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ShareRequestStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ShareRequestStatus" j)
    let to_json = simple_to_json to_value
  end
module AssessmentEvidenceFolderName =
  struct
    type nonrec t = string
    let context_ = "AssessmentEvidenceFolderName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:300) >>=
                  (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AssessmentEvidenceFolderName" j
    let to_json = simple_to_json to_value
  end
module EventName =
  struct
    type nonrec t = string
    let context_ = "EventName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EventName" j
    let to_json = simple_to_json to_value
  end
module EvidenceAttributes =
  struct
    type nonrec t = (EvidenceAttributeKey.t * EvidenceAttributeValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((EvidenceAttributeKey.of_string chopped),
                              (EvidenceAttributeValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (EvidenceAttributeKey.to_value x) |>
                    (fun x ->
                       (EvidenceAttributeValue.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:EvidenceAttributeKey.of_string
        ~of_json:EvidenceAttributeValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module Resources =
  struct
    type nonrec t = Resource.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Resource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Resource.of_xml)
    let of_json j =
      list_of_json ~kind:"Resources" ~of_json:Resource.of_json j
    let to_json v = composed_to_json to_value v
  end
module ActionEnum =
  struct
    type nonrec t =
      | CREATE 
      | UPDATE_METADATA 
      | ACTIVE 
      | INACTIVE 
      | DELETE 
      | UNDER_REVIEW 
      | REVIEWED 
      | IMPORT_EVIDENCE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATE -> "CREATE"
      | UPDATE_METADATA -> "UPDATE_METADATA"
      | ACTIVE -> "ACTIVE"
      | INACTIVE -> "INACTIVE"
      | DELETE -> "DELETE"
      | UNDER_REVIEW -> "UNDER_REVIEW"
      | REVIEWED -> "REVIEWED"
      | IMPORT_EVIDENCE -> "IMPORT_EVIDENCE"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATE" -> CREATE
      | "UPDATE_METADATA" -> UPDATE_METADATA
      | "ACTIVE" -> ACTIVE
      | "INACTIVE" -> INACTIVE
      | "DELETE" -> DELETE
      | "UNDER_REVIEW" -> UNDER_REVIEW
      | "REVIEWED" -> REVIEWED
      | "IMPORT_EVIDENCE" -> IMPORT_EVIDENCE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ActionEnum" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ActionEnum" j)
    let to_json = simple_to_json to_value
  end
module ObjectTypeEnum =
  struct
    type nonrec t =
      | ASSESSMENT 
      | CONTROL_SET 
      | CONTROL 
      | DELEGATION 
      | ASSESSMENT_REPORT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ASSESSMENT -> "ASSESSMENT"
      | CONTROL_SET -> "CONTROL_SET"
      | CONTROL -> "CONTROL"
      | DELEGATION -> "DELEGATION"
      | ASSESSMENT_REPORT -> "ASSESSMENT_REPORT"
      | Non_static_id s -> s
    let of_string =
      function
      | "ASSESSMENT" -> ASSESSMENT
      | "CONTROL_SET" -> CONTROL_SET
      | "CONTROL" -> CONTROL
      | "DELEGATION" -> DELEGATION
      | "ASSESSMENT_REPORT" -> ASSESSMENT_REPORT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ObjectTypeEnum" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ObjectTypeEnum" j)
    let to_json = simple_to_json to_value
  end
module ErrorCode =
  struct
    type nonrec t = string
    let context_ = "ErrorCode"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:3) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9]{3}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorCode" j
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:300) >>=
             (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module ManualEvidence =
  struct
    type nonrec t = {
      s3ResourcePath: S3Url.t option }
    let make ?s3ResourcePath = fun () -> { s3ResourcePath }
    let to_value x =
      structure_to_value
        [("s3ResourcePath", (Option.map x.s3ResourcePath ~f:S3Url.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3ResourcePath =
        (Option.map ~f:S3Url.of_xml) (Xml.child xml_arg0 "s3ResourcePath") in
      make ?s3ResourcePath ()
    let of_json json =
      let s3ResourcePath = field_map json "s3ResourcePath" S3Url.of_json in
      make ?s3ResourcePath ()
    let to_json v = composed_to_json to_value v
  end
module CreateDelegationRequest =
  struct
    type nonrec t =
      {
      comment: DelegationComment.t option ;
      controlSetId: ControlSetId.t option ;
      roleArn: IamArn.t option ;
      roleType: RoleType.t option }
    let make ?comment =
      fun ?controlSetId ->
        fun ?roleArn ->
          fun ?roleType ->
            fun () -> { comment; controlSetId; roleArn; roleType }
    let to_value x =
      structure_to_value
        [("comment", (Option.map x.comment ~f:DelegationComment.to_value));
        ("controlSetId",
          (Option.map x.controlSetId ~f:ControlSetId.to_value));
        ("roleArn", (Option.map x.roleArn ~f:IamArn.to_value));
        ("roleType", (Option.map x.roleType ~f:RoleType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let roleType =
        (Option.map ~f:RoleType.of_xml) (Xml.child xml_arg0 "roleType") in
      let roleArn =
        (Option.map ~f:IamArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let controlSetId =
        (Option.map ~f:ControlSetId.of_xml)
          (Xml.child xml_arg0 "controlSetId") in
      let comment =
        (Option.map ~f:DelegationComment.of_xml)
          (Xml.child xml_arg0 "comment") in
      make ?roleType ?roleArn ?controlSetId ?comment ()
    let of_json json =
      let roleType = field_map json "roleType" RoleType.of_json in
      let roleArn = field_map json "roleArn" IamArn.of_json in
      let controlSetId = field_map json "controlSetId" ControlSetId.of_json in
      let comment = field_map json "comment" DelegationComment.of_json in
      make ?roleType ?roleArn ?controlSetId ?comment ()
    let to_json v = composed_to_json to_value v
  end
module ValidationExceptionFieldList =
  struct
    type nonrec t = ValidationExceptionField.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ValidationExceptionField.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ValidationExceptionField.of_xml)
    let of_json j =
      list_of_json ~kind:"ValidationExceptionFieldList"
        ~of_json:ValidationExceptionField.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidationExceptionReason =
  struct
    type nonrec t =
      | UnknownOperation 
      | CannotParse 
      | FieldValidationFailed 
      | Other 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UnknownOperation -> "unknownOperation"
      | CannotParse -> "cannotParse"
      | FieldValidationFailed -> "fieldValidationFailed"
      | Other -> "other"
      | Non_static_id s -> s
    let of_string =
      function
      | "unknownOperation" -> UnknownOperation
      | "cannotParse" -> CannotParse
      | "fieldValidationFailed" -> FieldValidationFailed
      | "other" -> Other
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module KmsKey =
  struct
    type nonrec t = string
    let context_ = "KmsKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:7) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^arn:.*:kms:.*|DEFAULT")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KmsKey" j
    let to_json = simple_to_json to_value
  end
module SNSTopic =
  struct
    type nonrec t = string
    let context_ = "SNSTopic"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9-_\\(\\)\\[\\]]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SNSTopic" j
    let to_json = simple_to_json to_value
  end
module AssessmentFramework =
  struct
    type nonrec t =
      {
      id: UUID.t option ;
      arn: AuditManagerArn.t option ;
      metadata: FrameworkMetadata.t option ;
      controlSets: AssessmentControlSets.t option }
    let make ?id =
      fun ?arn ->
        fun ?metadata ->
          fun ?controlSets -> fun () -> { id; arn; metadata; controlSets }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:UUID.to_value));
        ("arn", (Option.map x.arn ~f:AuditManagerArn.to_value));
        ("metadata", (Option.map x.metadata ~f:FrameworkMetadata.to_value));
        ("controlSets",
          (Option.map x.controlSets ~f:AssessmentControlSets.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let controlSets =
        (Option.map ~f:AssessmentControlSets.of_xml)
          (Xml.child xml_arg0 "controlSets") in
      let metadata =
        (Option.map ~f:FrameworkMetadata.of_xml)
          (Xml.child xml_arg0 "metadata") in
      let arn =
        (Option.map ~f:AuditManagerArn.of_xml) (Xml.child xml_arg0 "arn") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      make ?controlSets ?metadata ?arn ?id ()
    let of_json json =
      let controlSets =
        field_map json "controlSets" AssessmentControlSets.of_json in
      let metadata = field_map json "metadata" FrameworkMetadata.of_json in
      let arn = field_map json "arn" AuditManagerArn.of_json in
      let id = field_map json "id" UUID.of_json in
      make ?controlSets ?metadata ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end
module AssessmentMetadata =
  struct
    type nonrec t =
      {
      name: AssessmentName.t option ;
      id: UUID.t option ;
      description: AssessmentDescription.t option ;
      complianceType: ComplianceType.t option ;
      status: AssessmentStatus.t option ;
      assessmentReportsDestination: AssessmentReportsDestination.t option ;
      scope: Scope.t option ;
      roles: Roles.t option ;
      delegations: Delegations.t option ;
      creationTime: Timestamp.t option ;
      lastUpdated: Timestamp.t option }
    let make ?name =
      fun ?id ->
        fun ?description ->
          fun ?complianceType ->
            fun ?status ->
              fun ?assessmentReportsDestination ->
                fun ?scope ->
                  fun ?roles ->
                    fun ?delegations ->
                      fun ?creationTime ->
                        fun ?lastUpdated ->
                          fun () ->
                            {
                              name;
                              id;
                              description;
                              complianceType;
                              status;
                              assessmentReportsDestination;
                              scope;
                              roles;
                              delegations;
                              creationTime;
                              lastUpdated
                            }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:AssessmentName.to_value));
        ("id", (Option.map x.id ~f:UUID.to_value));
        ("description",
          (Option.map x.description ~f:AssessmentDescription.to_value));
        ("complianceType",
          (Option.map x.complianceType ~f:ComplianceType.to_value));
        ("status", (Option.map x.status ~f:AssessmentStatus.to_value));
        ("assessmentReportsDestination",
          (Option.map x.assessmentReportsDestination
             ~f:AssessmentReportsDestination.to_value));
        ("scope", (Option.map x.scope ~f:Scope.to_value));
        ("roles", (Option.map x.roles ~f:Roles.to_value));
        ("delegations", (Option.map x.delegations ~f:Delegations.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("lastUpdated", (Option.map x.lastUpdated ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdated =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdated") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let delegations =
        (Option.map ~f:Delegations.of_xml) (Xml.child xml_arg0 "delegations") in
      let roles = (Option.map ~f:Roles.of_xml) (Xml.child xml_arg0 "roles") in
      let scope = (Option.map ~f:Scope.of_xml) (Xml.child xml_arg0 "scope") in
      let assessmentReportsDestination =
        (Option.map ~f:AssessmentReportsDestination.of_xml)
          (Xml.child xml_arg0 "assessmentReportsDestination") in
      let status =
        (Option.map ~f:AssessmentStatus.of_xml) (Xml.child xml_arg0 "status") in
      let complianceType =
        (Option.map ~f:ComplianceType.of_xml)
          (Xml.child xml_arg0 "complianceType") in
      let description =
        (Option.map ~f:AssessmentDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      let name =
        (Option.map ~f:AssessmentName.of_xml) (Xml.child xml_arg0 "name") in
      make ?lastUpdated ?creationTime ?delegations ?roles ?scope
        ?assessmentReportsDestination ?status ?complianceType ?description
        ?id ?name ()
    let of_json json =
      let lastUpdated = field_map json "lastUpdated" Timestamp.of_json in
      let creationTime = field_map json "creationTime" Timestamp.of_json in
      let delegations = field_map json "delegations" Delegations.of_json in
      let roles = field_map json "roles" Roles.of_json in
      let scope = field_map json "scope" Scope.of_json in
      let assessmentReportsDestination =
        field_map json "assessmentReportsDestination"
          AssessmentReportsDestination.of_json in
      let status = field_map json "status" AssessmentStatus.of_json in
      let complianceType =
        field_map json "complianceType" ComplianceType.of_json in
      let description =
        field_map json "description" AssessmentDescription.of_json in
      let id = field_map json "id" UUID.of_json in
      let name = field_map json "name" AssessmentName.of_json in
      make ?lastUpdated ?creationTime ?delegations ?roles ?scope
        ?assessmentReportsDestination ?status ?complianceType ?description
        ?id ?name ()
    let to_json v = composed_to_json to_value v
  end
module ControlSets =
  struct
    type nonrec t = ControlSet.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:ControlSet.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ControlSet.of_xml)
    let of_json j =
      list_of_json ~kind:"ControlSets" ~of_json:ControlSet.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentFrameworkControlSet =
  struct
    type nonrec t =
      {
      id: ControlSetName.t option ;
      name: ControlSetName.t ;
      controls: CreateAssessmentFrameworkControls.t }
    let context_ = "UpdateAssessmentFrameworkControlSet"
    let make ?id =
      fun ~name -> fun ~controls -> fun () -> { id; name; controls }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:ControlSetName.to_value));
        ("name", (Some (ControlSetName.to_value x.name)));
        ("controls",
          (Some (CreateAssessmentFrameworkControls.to_value x.controls)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let controls =
        CreateAssessmentFrameworkControls.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controls") in
      let name =
        ControlSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let id =
        (Option.map ~f:ControlSetName.of_xml) (Xml.child xml_arg0 "id") in
      make ~controls ~name ?id ()
    let of_json json =
      let controls =
        field_map_exn json "controls"
          CreateAssessmentFrameworkControls.of_json in
      let name = field_map_exn json "name" ControlSetName.of_json in
      let id = field_map json "id" ControlSetName.of_json in
      make ~controls ~name ?id ()
    let to_json v = composed_to_json to_value v
  end
module Notification =
  struct
    type nonrec t =
      {
      id: TimestampUUID.t option ;
      assessmentId: UUID.t option ;
      assessmentName: AssessmentName.t option ;
      controlSetId: ControlSetId.t option ;
      controlSetName: NonEmptyString.t option ;
      description: NonEmptyString.t option ;
      eventTime: Timestamp.t option ;
      source: NonEmptyString.t option }
    let make ?id =
      fun ?assessmentId ->
        fun ?assessmentName ->
          fun ?controlSetId ->
            fun ?controlSetName ->
              fun ?description ->
                fun ?eventTime ->
                  fun ?source ->
                    fun () ->
                      {
                        id;
                        assessmentId;
                        assessmentName;
                        controlSetId;
                        controlSetName;
                        description;
                        eventTime;
                        source
                      }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:TimestampUUID.to_value));
        ("assessmentId", (Option.map x.assessmentId ~f:UUID.to_value));
        ("assessmentName",
          (Option.map x.assessmentName ~f:AssessmentName.to_value));
        ("controlSetId",
          (Option.map x.controlSetId ~f:ControlSetId.to_value));
        ("controlSetName",
          (Option.map x.controlSetName ~f:NonEmptyString.to_value));
        ("description",
          (Option.map x.description ~f:NonEmptyString.to_value));
        ("eventTime", (Option.map x.eventTime ~f:Timestamp.to_value));
        ("source", (Option.map x.source ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let source =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "source") in
      let eventTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "eventTime") in
      let description =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "description") in
      let controlSetName =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "controlSetName") in
      let controlSetId =
        (Option.map ~f:ControlSetId.of_xml)
          (Xml.child xml_arg0 "controlSetId") in
      let assessmentName =
        (Option.map ~f:AssessmentName.of_xml)
          (Xml.child xml_arg0 "assessmentName") in
      let assessmentId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "assessmentId") in
      let id = (Option.map ~f:TimestampUUID.of_xml) (Xml.child xml_arg0 "id") in
      make ?source ?eventTime ?description ?controlSetName ?controlSetId
        ?assessmentName ?assessmentId ?id ()
    let of_json json =
      let source = field_map json "source" NonEmptyString.of_json in
      let eventTime = field_map json "eventTime" Timestamp.of_json in
      let description = field_map json "description" NonEmptyString.of_json in
      let controlSetName =
        field_map json "controlSetName" NonEmptyString.of_json in
      let controlSetId = field_map json "controlSetId" ControlSetId.of_json in
      let assessmentName =
        field_map json "assessmentName" AssessmentName.of_json in
      let assessmentId = field_map json "assessmentId" UUID.of_json in
      let id = field_map json "id" TimestampUUID.of_json in
      make ?source ?eventTime ?description ?controlSetName ?controlSetId
        ?assessmentName ?assessmentId ?id ()
    let to_json v = composed_to_json to_value v
  end
module ControlMetadata =
  struct
    type nonrec t =
      {
      arn: AuditManagerArn.t option ;
      id: UUID.t option ;
      name: ControlName.t option ;
      controlSources: ControlSources.t option ;
      createdAt: Timestamp.t option ;
      lastUpdatedAt: Timestamp.t option }
    let make ?arn =
      fun ?id ->
        fun ?name ->
          fun ?controlSources ->
            fun ?createdAt ->
              fun ?lastUpdatedAt ->
                fun () ->
                  { arn; id; name; controlSources; createdAt; lastUpdatedAt }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AuditManagerArn.to_value));
        ("id", (Option.map x.id ~f:UUID.to_value));
        ("name", (Option.map x.name ~f:ControlName.to_value));
        ("controlSources",
          (Option.map x.controlSources ~f:ControlSources.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("lastUpdatedAt", (Option.map x.lastUpdatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let controlSources =
        (Option.map ~f:ControlSources.of_xml)
          (Xml.child xml_arg0 "controlSources") in
      let name =
        (Option.map ~f:ControlName.of_xml) (Xml.child xml_arg0 "name") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      let arn =
        (Option.map ~f:AuditManagerArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?lastUpdatedAt ?createdAt ?controlSources ?name ?id ?arn ()
    let of_json json =
      let lastUpdatedAt = field_map json "lastUpdatedAt" Timestamp.of_json in
      let createdAt = field_map json "createdAt" Timestamp.of_json in
      let controlSources =
        field_map json "controlSources" ControlSources.of_json in
      let name = field_map json "name" ControlName.of_json in
      let id = field_map json "id" UUID.of_json in
      let arn = field_map json "arn" AuditManagerArn.of_json in
      make ?lastUpdatedAt ?createdAt ?controlSources ?name ?id ?arn ()
    let to_json v = composed_to_json to_value v
  end
module ControlInsightsMetadataItem =
  struct
    type nonrec t =
      {
      name: NonEmptyString.t option ;
      id: UUID.t option ;
      evidenceInsights: EvidenceInsights.t option ;
      lastUpdated: Timestamp.t option }
    let make ?name =
      fun ?id ->
        fun ?evidenceInsights ->
          fun ?lastUpdated ->
            fun () -> { name; id; evidenceInsights; lastUpdated }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:NonEmptyString.to_value));
        ("id", (Option.map x.id ~f:UUID.to_value));
        ("evidenceInsights",
          (Option.map x.evidenceInsights ~f:EvidenceInsights.to_value));
        ("lastUpdated", (Option.map x.lastUpdated ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdated =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdated") in
      let evidenceInsights =
        (Option.map ~f:EvidenceInsights.of_xml)
          (Xml.child xml_arg0 "evidenceInsights") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      let name =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "name") in
      make ?lastUpdated ?evidenceInsights ?id ?name ()
    let of_json json =
      let lastUpdated = field_map json "lastUpdated" Timestamp.of_json in
      let evidenceInsights =
        field_map json "evidenceInsights" EvidenceInsights.of_json in
      let id = field_map json "id" UUID.of_json in
      let name = field_map json "name" NonEmptyString.of_json in
      make ?lastUpdated ?evidenceInsights ?id ?name ()
    let to_json v = composed_to_json to_value v
  end
module ControlDomainInsights =
  struct
    type nonrec t =
      {
      name: NonEmptyString.t option ;
      id: UUID.t option ;
      controlsCountByNoncompliantEvidence: NullableInteger.t option ;
      totalControlsCount: NullableInteger.t option ;
      evidenceInsights: EvidenceInsights.t option ;
      lastUpdated: Timestamp.t option }
    let make ?name =
      fun ?id ->
        fun ?controlsCountByNoncompliantEvidence ->
          fun ?totalControlsCount ->
            fun ?evidenceInsights ->
              fun ?lastUpdated ->
                fun () ->
                  {
                    name;
                    id;
                    controlsCountByNoncompliantEvidence;
                    totalControlsCount;
                    evidenceInsights;
                    lastUpdated
                  }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:NonEmptyString.to_value));
        ("id", (Option.map x.id ~f:UUID.to_value));
        ("controlsCountByNoncompliantEvidence",
          (Option.map x.controlsCountByNoncompliantEvidence
             ~f:NullableInteger.to_value));
        ("totalControlsCount",
          (Option.map x.totalControlsCount ~f:NullableInteger.to_value));
        ("evidenceInsights",
          (Option.map x.evidenceInsights ~f:EvidenceInsights.to_value));
        ("lastUpdated", (Option.map x.lastUpdated ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdated =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdated") in
      let evidenceInsights =
        (Option.map ~f:EvidenceInsights.of_xml)
          (Xml.child xml_arg0 "evidenceInsights") in
      let totalControlsCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "totalControlsCount") in
      let controlsCountByNoncompliantEvidence =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "controlsCountByNoncompliantEvidence") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      let name =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "name") in
      make ?lastUpdated ?evidenceInsights ?totalControlsCount
        ?controlsCountByNoncompliantEvidence ?id ?name ()
    let of_json json =
      let lastUpdated = field_map json "lastUpdated" Timestamp.of_json in
      let evidenceInsights =
        field_map json "evidenceInsights" EvidenceInsights.of_json in
      let totalControlsCount =
        field_map json "totalControlsCount" NullableInteger.of_json in
      let controlsCountByNoncompliantEvidence =
        field_map json "controlsCountByNoncompliantEvidence"
          NullableInteger.of_json in
      let id = field_map json "id" UUID.of_json in
      let name = field_map json "name" NonEmptyString.of_json in
      make ?lastUpdated ?evidenceInsights ?totalControlsCount
        ?controlsCountByNoncompliantEvidence ?id ?name ()
    let to_json v = composed_to_json to_value v
  end
module AssessmentMetadataItem =
  struct
    type nonrec t =
      {
      name: AssessmentName.t option ;
      id: UUID.t option ;
      complianceType: ComplianceType.t option ;
      status: AssessmentStatus.t option ;
      roles: Roles.t option ;
      delegations: Delegations.t option ;
      creationTime: Timestamp.t option ;
      lastUpdated: Timestamp.t option }
    let make ?name =
      fun ?id ->
        fun ?complianceType ->
          fun ?status ->
            fun ?roles ->
              fun ?delegations ->
                fun ?creationTime ->
                  fun ?lastUpdated ->
                    fun () ->
                      {
                        name;
                        id;
                        complianceType;
                        status;
                        roles;
                        delegations;
                        creationTime;
                        lastUpdated
                      }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:AssessmentName.to_value));
        ("id", (Option.map x.id ~f:UUID.to_value));
        ("complianceType",
          (Option.map x.complianceType ~f:ComplianceType.to_value));
        ("status", (Option.map x.status ~f:AssessmentStatus.to_value));
        ("roles", (Option.map x.roles ~f:Roles.to_value));
        ("delegations", (Option.map x.delegations ~f:Delegations.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("lastUpdated", (Option.map x.lastUpdated ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdated =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdated") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let delegations =
        (Option.map ~f:Delegations.of_xml) (Xml.child xml_arg0 "delegations") in
      let roles = (Option.map ~f:Roles.of_xml) (Xml.child xml_arg0 "roles") in
      let status =
        (Option.map ~f:AssessmentStatus.of_xml) (Xml.child xml_arg0 "status") in
      let complianceType =
        (Option.map ~f:ComplianceType.of_xml)
          (Xml.child xml_arg0 "complianceType") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      let name =
        (Option.map ~f:AssessmentName.of_xml) (Xml.child xml_arg0 "name") in
      make ?lastUpdated ?creationTime ?delegations ?roles ?status
        ?complianceType ?id ?name ()
    let of_json json =
      let lastUpdated = field_map json "lastUpdated" Timestamp.of_json in
      let creationTime = field_map json "creationTime" Timestamp.of_json in
      let delegations = field_map json "delegations" Delegations.of_json in
      let roles = field_map json "roles" Roles.of_json in
      let status = field_map json "status" AssessmentStatus.of_json in
      let complianceType =
        field_map json "complianceType" ComplianceType.of_json in
      let id = field_map json "id" UUID.of_json in
      let name = field_map json "name" AssessmentName.of_json in
      make ?lastUpdated ?creationTime ?delegations ?roles ?status
        ?complianceType ?id ?name ()
    let to_json v = composed_to_json to_value v
  end
module AssessmentReportMetadata =
  struct
    type nonrec t =
      {
      id: UUID.t option ;
      name: AssessmentReportName.t option ;
      description: AssessmentReportDescription.t option ;
      assessmentId: UUID.t option ;
      assessmentName: AssessmentName.t option ;
      author: Username.t option ;
      status: AssessmentReportStatus.t option ;
      creationTime: Timestamp.t option }
    let make ?id =
      fun ?name ->
        fun ?description ->
          fun ?assessmentId ->
            fun ?assessmentName ->
              fun ?author ->
                fun ?status ->
                  fun ?creationTime ->
                    fun () ->
                      {
                        id;
                        name;
                        description;
                        assessmentId;
                        assessmentName;
                        author;
                        status;
                        creationTime
                      }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:UUID.to_value));
        ("name", (Option.map x.name ~f:AssessmentReportName.to_value));
        ("description",
          (Option.map x.description ~f:AssessmentReportDescription.to_value));
        ("assessmentId", (Option.map x.assessmentId ~f:UUID.to_value));
        ("assessmentName",
          (Option.map x.assessmentName ~f:AssessmentName.to_value));
        ("author", (Option.map x.author ~f:Username.to_value));
        ("status", (Option.map x.status ~f:AssessmentReportStatus.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let status =
        (Option.map ~f:AssessmentReportStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let author =
        (Option.map ~f:Username.of_xml) (Xml.child xml_arg0 "author") in
      let assessmentName =
        (Option.map ~f:AssessmentName.of_xml)
          (Xml.child xml_arg0 "assessmentName") in
      let assessmentId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "assessmentId") in
      let description =
        (Option.map ~f:AssessmentReportDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:AssessmentReportName.of_xml)
          (Xml.child xml_arg0 "name") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      make ?creationTime ?status ?author ?assessmentName ?assessmentId
        ?description ?name ?id ()
    let of_json json =
      let creationTime = field_map json "creationTime" Timestamp.of_json in
      let status = field_map json "status" AssessmentReportStatus.of_json in
      let author = field_map json "author" Username.of_json in
      let assessmentName =
        field_map json "assessmentName" AssessmentName.of_json in
      let assessmentId = field_map json "assessmentId" UUID.of_json in
      let description =
        field_map json "description" AssessmentReportDescription.of_json in
      let name = field_map json "name" AssessmentReportName.of_json in
      let id = field_map json "id" UUID.of_json in
      make ?creationTime ?status ?author ?assessmentName ?assessmentId
        ?description ?name ?id ()
    let to_json v = composed_to_json to_value v
  end
module AssessmentFrameworkMetadata =
  struct
    type nonrec t =
      {
      arn: AuditManagerArn.t option ;
      id: UUID.t option ;
      type_: FrameworkType.t option ;
      name: FrameworkName.t option ;
      description: FrameworkDescription.t option ;
      logo: Filename.t option ;
      complianceType: ComplianceType.t option ;
      controlsCount: ControlsCount.t option ;
      controlSetsCount: ControlSetsCount.t option ;
      createdAt: Timestamp.t option ;
      lastUpdatedAt: Timestamp.t option }
    let make ?arn =
      fun ?id ->
        fun ?type_ ->
          fun ?name ->
            fun ?description ->
              fun ?logo ->
                fun ?complianceType ->
                  fun ?controlsCount ->
                    fun ?controlSetsCount ->
                      fun ?createdAt ->
                        fun ?lastUpdatedAt ->
                          fun () ->
                            {
                              arn;
                              id;
                              type_;
                              name;
                              description;
                              logo;
                              complianceType;
                              controlsCount;
                              controlSetsCount;
                              createdAt;
                              lastUpdatedAt
                            }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AuditManagerArn.to_value));
        ("id", (Option.map x.id ~f:UUID.to_value));
        ("type", (Option.map x.type_ ~f:FrameworkType.to_value));
        ("name", (Option.map x.name ~f:FrameworkName.to_value));
        ("description",
          (Option.map x.description ~f:FrameworkDescription.to_value));
        ("logo", (Option.map x.logo ~f:Filename.to_value));
        ("complianceType",
          (Option.map x.complianceType ~f:ComplianceType.to_value));
        ("controlsCount",
          (Option.map x.controlsCount ~f:ControlsCount.to_value));
        ("controlSetsCount",
          (Option.map x.controlSetsCount ~f:ControlSetsCount.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("lastUpdatedAt", (Option.map x.lastUpdatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let controlSetsCount =
        (Option.map ~f:ControlSetsCount.of_xml)
          (Xml.child xml_arg0 "controlSetsCount") in
      let controlsCount =
        (Option.map ~f:ControlsCount.of_xml)
          (Xml.child xml_arg0 "controlsCount") in
      let complianceType =
        (Option.map ~f:ComplianceType.of_xml)
          (Xml.child xml_arg0 "complianceType") in
      let logo = (Option.map ~f:Filename.of_xml) (Xml.child xml_arg0 "logo") in
      let description =
        (Option.map ~f:FrameworkDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:FrameworkName.of_xml) (Xml.child xml_arg0 "name") in
      let type_ =
        (Option.map ~f:FrameworkType.of_xml) (Xml.child xml_arg0 "type") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      let arn =
        (Option.map ~f:AuditManagerArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?lastUpdatedAt ?createdAt ?controlSetsCount ?controlsCount
        ?complianceType ?logo ?description ?name ?type_ ?id ?arn ()
    let of_json json =
      let lastUpdatedAt = field_map json "lastUpdatedAt" Timestamp.of_json in
      let createdAt = field_map json "createdAt" Timestamp.of_json in
      let controlSetsCount =
        field_map json "controlSetsCount" ControlSetsCount.of_json in
      let controlsCount =
        field_map json "controlsCount" ControlsCount.of_json in
      let complianceType =
        field_map json "complianceType" ComplianceType.of_json in
      let logo = field_map json "logo" Filename.of_json in
      let description =
        field_map json "description" FrameworkDescription.of_json in
      let name = field_map json "name" FrameworkName.of_json in
      let type_ = field_map json "type" FrameworkType.of_json in
      let id = field_map json "id" UUID.of_json in
      let arn = field_map json "arn" AuditManagerArn.of_json in
      make ?lastUpdatedAt ?createdAt ?controlSetsCount ?controlsCount
        ?complianceType ?logo ?description ?name ?type_ ?id ?arn ()
    let to_json v = composed_to_json to_value v
  end
module AssessmentFrameworkShareRequest =
  struct
    type nonrec t =
      {
      id: UUID.t option ;
      frameworkId: UUID.t option ;
      frameworkName: FrameworkName.t option ;
      frameworkDescription: FrameworkDescription.t option ;
      status: ShareRequestStatus.t option ;
      sourceAccount: AccountId.t option ;
      destinationAccount: AccountId.t option ;
      destinationRegion: Region.t option ;
      expirationTime: Timestamp.t option ;
      creationTime: Timestamp.t option ;
      lastUpdated: Timestamp.t option ;
      comment: ShareRequestComment.t option ;
      standardControlsCount: NullableInteger.t option ;
      customControlsCount: NullableInteger.t option ;
      complianceType: ComplianceType.t option }
    let make ?id =
      fun ?frameworkId ->
        fun ?frameworkName ->
          fun ?frameworkDescription ->
            fun ?status ->
              fun ?sourceAccount ->
                fun ?destinationAccount ->
                  fun ?destinationRegion ->
                    fun ?expirationTime ->
                      fun ?creationTime ->
                        fun ?lastUpdated ->
                          fun ?comment ->
                            fun ?standardControlsCount ->
                              fun ?customControlsCount ->
                                fun ?complianceType ->
                                  fun () ->
                                    {
                                      id;
                                      frameworkId;
                                      frameworkName;
                                      frameworkDescription;
                                      status;
                                      sourceAccount;
                                      destinationAccount;
                                      destinationRegion;
                                      expirationTime;
                                      creationTime;
                                      lastUpdated;
                                      comment;
                                      standardControlsCount;
                                      customControlsCount;
                                      complianceType
                                    }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:UUID.to_value));
        ("frameworkId", (Option.map x.frameworkId ~f:UUID.to_value));
        ("frameworkName",
          (Option.map x.frameworkName ~f:FrameworkName.to_value));
        ("frameworkDescription",
          (Option.map x.frameworkDescription ~f:FrameworkDescription.to_value));
        ("status", (Option.map x.status ~f:ShareRequestStatus.to_value));
        ("sourceAccount", (Option.map x.sourceAccount ~f:AccountId.to_value));
        ("destinationAccount",
          (Option.map x.destinationAccount ~f:AccountId.to_value));
        ("destinationRegion",
          (Option.map x.destinationRegion ~f:Region.to_value));
        ("expirationTime",
          (Option.map x.expirationTime ~f:Timestamp.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("lastUpdated", (Option.map x.lastUpdated ~f:Timestamp.to_value));
        ("comment", (Option.map x.comment ~f:ShareRequestComment.to_value));
        ("standardControlsCount",
          (Option.map x.standardControlsCount ~f:NullableInteger.to_value));
        ("customControlsCount",
          (Option.map x.customControlsCount ~f:NullableInteger.to_value));
        ("complianceType",
          (Option.map x.complianceType ~f:ComplianceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let complianceType =
        (Option.map ~f:ComplianceType.of_xml)
          (Xml.child xml_arg0 "complianceType") in
      let customControlsCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "customControlsCount") in
      let standardControlsCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "standardControlsCount") in
      let comment =
        (Option.map ~f:ShareRequestComment.of_xml)
          (Xml.child xml_arg0 "comment") in
      let lastUpdated =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdated") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let expirationTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "expirationTime") in
      let destinationRegion =
        (Option.map ~f:Region.of_xml)
          (Xml.child xml_arg0 "destinationRegion") in
      let destinationAccount =
        (Option.map ~f:AccountId.of_xml)
          (Xml.child xml_arg0 "destinationAccount") in
      let sourceAccount =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "sourceAccount") in
      let status =
        (Option.map ~f:ShareRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let frameworkDescription =
        (Option.map ~f:FrameworkDescription.of_xml)
          (Xml.child xml_arg0 "frameworkDescription") in
      let frameworkName =
        (Option.map ~f:FrameworkName.of_xml)
          (Xml.child xml_arg0 "frameworkName") in
      let frameworkId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "frameworkId") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      make ?complianceType ?customControlsCount ?standardControlsCount
        ?comment ?lastUpdated ?creationTime ?expirationTime
        ?destinationRegion ?destinationAccount ?sourceAccount ?status
        ?frameworkDescription ?frameworkName ?frameworkId ?id ()
    let of_json json =
      let complianceType =
        field_map json "complianceType" ComplianceType.of_json in
      let customControlsCount =
        field_map json "customControlsCount" NullableInteger.of_json in
      let standardControlsCount =
        field_map json "standardControlsCount" NullableInteger.of_json in
      let comment = field_map json "comment" ShareRequestComment.of_json in
      let lastUpdated = field_map json "lastUpdated" Timestamp.of_json in
      let creationTime = field_map json "creationTime" Timestamp.of_json in
      let expirationTime = field_map json "expirationTime" Timestamp.of_json in
      let destinationRegion =
        field_map json "destinationRegion" Region.of_json in
      let destinationAccount =
        field_map json "destinationAccount" AccountId.of_json in
      let sourceAccount = field_map json "sourceAccount" AccountId.of_json in
      let status = field_map json "status" ShareRequestStatus.of_json in
      let frameworkDescription =
        field_map json "frameworkDescription" FrameworkDescription.of_json in
      let frameworkName =
        field_map json "frameworkName" FrameworkName.of_json in
      let frameworkId = field_map json "frameworkId" UUID.of_json in
      let id = field_map json "id" UUID.of_json in
      make ?complianceType ?customControlsCount ?standardControlsCount
        ?comment ?lastUpdated ?creationTime ?expirationTime
        ?destinationRegion ?destinationAccount ?sourceAccount ?status
        ?frameworkDescription ?frameworkName ?frameworkId ?id ()
    let to_json v = composed_to_json to_value v
  end
module ControlInsightsMetadataByAssessmentItem =
  struct
    type nonrec t =
      {
      name: NonEmptyString.t option ;
      id: UUID.t option ;
      evidenceInsights: EvidenceInsights.t option ;
      controlSetName: NonEmptyString.t option ;
      lastUpdated: Timestamp.t option }
    let make ?name =
      fun ?id ->
        fun ?evidenceInsights ->
          fun ?controlSetName ->
            fun ?lastUpdated ->
              fun () ->
                { name; id; evidenceInsights; controlSetName; lastUpdated }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:NonEmptyString.to_value));
        ("id", (Option.map x.id ~f:UUID.to_value));
        ("evidenceInsights",
          (Option.map x.evidenceInsights ~f:EvidenceInsights.to_value));
        ("controlSetName",
          (Option.map x.controlSetName ~f:NonEmptyString.to_value));
        ("lastUpdated", (Option.map x.lastUpdated ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdated =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdated") in
      let controlSetName =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "controlSetName") in
      let evidenceInsights =
        (Option.map ~f:EvidenceInsights.of_xml)
          (Xml.child xml_arg0 "evidenceInsights") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      let name =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "name") in
      make ?lastUpdated ?controlSetName ?evidenceInsights ?id ?name ()
    let of_json json =
      let lastUpdated = field_map json "lastUpdated" Timestamp.of_json in
      let controlSetName =
        field_map json "controlSetName" NonEmptyString.of_json in
      let evidenceInsights =
        field_map json "evidenceInsights" EvidenceInsights.of_json in
      let id = field_map json "id" UUID.of_json in
      let name = field_map json "name" NonEmptyString.of_json in
      make ?lastUpdated ?controlSetName ?evidenceInsights ?id ?name ()
    let to_json v = composed_to_json to_value v
  end
module ServiceMetadata =
  struct
    type nonrec t =
      {
      name: AWSServiceName.t option ;
      displayName: NonEmptyString.t option ;
      description: NonEmptyString.t option ;
      category: NonEmptyString.t option }
    let make ?name =
      fun ?displayName ->
        fun ?description ->
          fun ?category ->
            fun () -> { name; displayName; description; category }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:AWSServiceName.to_value));
        ("displayName",
          (Option.map x.displayName ~f:NonEmptyString.to_value));
        ("description",
          (Option.map x.description ~f:NonEmptyString.to_value));
        ("category", (Option.map x.category ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let category =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "category") in
      let description =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "description") in
      let displayName =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "displayName") in
      let name =
        (Option.map ~f:AWSServiceName.of_xml) (Xml.child xml_arg0 "name") in
      make ?category ?description ?displayName ?name ()
    let of_json json =
      let category = field_map json "category" NonEmptyString.of_json in
      let description = field_map json "description" NonEmptyString.of_json in
      let displayName = field_map json "displayName" NonEmptyString.of_json in
      let name = field_map json "name" AWSServiceName.of_json in
      make ?category ?description ?displayName ?name ()
    let to_json v = composed_to_json to_value v
  end
module AssessmentEvidenceFolder =
  struct
    type nonrec t =
      {
      name: AssessmentEvidenceFolderName.t option ;
      date: Timestamp.t option ;
      assessmentId: UUID.t option ;
      controlSetId: ControlSetId.t option ;
      controlId: UUID.t option ;
      id: UUID.t option ;
      dataSource: String_.t option ;
      author: String_.t option ;
      totalEvidence: Integer.t option ;
      assessmentReportSelectionCount: Integer.t option ;
      controlName: ControlName.t option ;
      evidenceResourcesIncludedCount: Integer.t option ;
      evidenceByTypeConfigurationDataCount: Integer.t option ;
      evidenceByTypeManualCount: Integer.t option ;
      evidenceByTypeComplianceCheckCount: Integer.t option ;
      evidenceByTypeComplianceCheckIssuesCount: Integer.t option ;
      evidenceByTypeUserActivityCount: Integer.t option ;
      evidenceAwsServiceSourceCount: Integer.t option }
    let make ?name =
      fun ?date ->
        fun ?assessmentId ->
          fun ?controlSetId ->
            fun ?controlId ->
              fun ?id ->
                fun ?dataSource ->
                  fun ?author ->
                    fun ?totalEvidence ->
                      fun ?assessmentReportSelectionCount ->
                        fun ?controlName ->
                          fun ?evidenceResourcesIncludedCount ->
                            fun ?evidenceByTypeConfigurationDataCount ->
                              fun ?evidenceByTypeManualCount ->
                                fun ?evidenceByTypeComplianceCheckCount ->
                                  fun
                                    ?evidenceByTypeComplianceCheckIssuesCount
                                    ->
                                    fun ?evidenceByTypeUserActivityCount ->
                                      fun ?evidenceAwsServiceSourceCount ->
                                        fun () ->
                                          {
                                            name;
                                            date;
                                            assessmentId;
                                            controlSetId;
                                            controlId;
                                            id;
                                            dataSource;
                                            author;
                                            totalEvidence;
                                            assessmentReportSelectionCount;
                                            controlName;
                                            evidenceResourcesIncludedCount;
                                            evidenceByTypeConfigurationDataCount;
                                            evidenceByTypeManualCount;
                                            evidenceByTypeComplianceCheckCount;
                                            evidenceByTypeComplianceCheckIssuesCount;
                                            evidenceByTypeUserActivityCount;
                                            evidenceAwsServiceSourceCount
                                          }
    let to_value x =
      structure_to_value
        [("name",
           (Option.map x.name ~f:AssessmentEvidenceFolderName.to_value));
        ("date", (Option.map x.date ~f:Timestamp.to_value));
        ("assessmentId", (Option.map x.assessmentId ~f:UUID.to_value));
        ("controlSetId",
          (Option.map x.controlSetId ~f:ControlSetId.to_value));
        ("controlId", (Option.map x.controlId ~f:UUID.to_value));
        ("id", (Option.map x.id ~f:UUID.to_value));
        ("dataSource", (Option.map x.dataSource ~f:String_.to_value));
        ("author", (Option.map x.author ~f:String_.to_value));
        ("totalEvidence", (Option.map x.totalEvidence ~f:Integer.to_value));
        ("assessmentReportSelectionCount",
          (Option.map x.assessmentReportSelectionCount ~f:Integer.to_value));
        ("controlName", (Option.map x.controlName ~f:ControlName.to_value));
        ("evidenceResourcesIncludedCount",
          (Option.map x.evidenceResourcesIncludedCount ~f:Integer.to_value));
        ("evidenceByTypeConfigurationDataCount",
          (Option.map x.evidenceByTypeConfigurationDataCount
             ~f:Integer.to_value));
        ("evidenceByTypeManualCount",
          (Option.map x.evidenceByTypeManualCount ~f:Integer.to_value));
        ("evidenceByTypeComplianceCheckCount",
          (Option.map x.evidenceByTypeComplianceCheckCount
             ~f:Integer.to_value));
        ("evidenceByTypeComplianceCheckIssuesCount",
          (Option.map x.evidenceByTypeComplianceCheckIssuesCount
             ~f:Integer.to_value));
        ("evidenceByTypeUserActivityCount",
          (Option.map x.evidenceByTypeUserActivityCount ~f:Integer.to_value));
        ("evidenceAwsServiceSourceCount",
          (Option.map x.evidenceAwsServiceSourceCount ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let evidenceAwsServiceSourceCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "evidenceAwsServiceSourceCount") in
      let evidenceByTypeUserActivityCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "evidenceByTypeUserActivityCount") in
      let evidenceByTypeComplianceCheckIssuesCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "evidenceByTypeComplianceCheckIssuesCount") in
      let evidenceByTypeComplianceCheckCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "evidenceByTypeComplianceCheckCount") in
      let evidenceByTypeManualCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "evidenceByTypeManualCount") in
      let evidenceByTypeConfigurationDataCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "evidenceByTypeConfigurationDataCount") in
      let evidenceResourcesIncludedCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "evidenceResourcesIncludedCount") in
      let controlName =
        (Option.map ~f:ControlName.of_xml) (Xml.child xml_arg0 "controlName") in
      let assessmentReportSelectionCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "assessmentReportSelectionCount") in
      let totalEvidence =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "totalEvidence") in
      let author =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "author") in
      let dataSource =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "dataSource") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      let controlId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "controlId") in
      let controlSetId =
        (Option.map ~f:ControlSetId.of_xml)
          (Xml.child xml_arg0 "controlSetId") in
      let assessmentId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "assessmentId") in
      let date = (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "date") in
      let name =
        (Option.map ~f:AssessmentEvidenceFolderName.of_xml)
          (Xml.child xml_arg0 "name") in
      make ?evidenceAwsServiceSourceCount ?evidenceByTypeUserActivityCount
        ?evidenceByTypeComplianceCheckIssuesCount
        ?evidenceByTypeComplianceCheckCount ?evidenceByTypeManualCount
        ?evidenceByTypeConfigurationDataCount ?evidenceResourcesIncludedCount
        ?controlName ?assessmentReportSelectionCount ?totalEvidence ?author
        ?dataSource ?id ?controlId ?controlSetId ?assessmentId ?date ?name ()
    let of_json json =
      let evidenceAwsServiceSourceCount =
        field_map json "evidenceAwsServiceSourceCount" Integer.of_json in
      let evidenceByTypeUserActivityCount =
        field_map json "evidenceByTypeUserActivityCount" Integer.of_json in
      let evidenceByTypeComplianceCheckIssuesCount =
        field_map json "evidenceByTypeComplianceCheckIssuesCount"
          Integer.of_json in
      let evidenceByTypeComplianceCheckCount =
        field_map json "evidenceByTypeComplianceCheckCount" Integer.of_json in
      let evidenceByTypeManualCount =
        field_map json "evidenceByTypeManualCount" Integer.of_json in
      let evidenceByTypeConfigurationDataCount =
        field_map json "evidenceByTypeConfigurationDataCount" Integer.of_json in
      let evidenceResourcesIncludedCount =
        field_map json "evidenceResourcesIncludedCount" Integer.of_json in
      let controlName = field_map json "controlName" ControlName.of_json in
      let assessmentReportSelectionCount =
        field_map json "assessmentReportSelectionCount" Integer.of_json in
      let totalEvidence = field_map json "totalEvidence" Integer.of_json in
      let author = field_map json "author" String_.of_json in
      let dataSource = field_map json "dataSource" String_.of_json in
      let id = field_map json "id" UUID.of_json in
      let controlId = field_map json "controlId" UUID.of_json in
      let controlSetId = field_map json "controlSetId" ControlSetId.of_json in
      let assessmentId = field_map json "assessmentId" UUID.of_json in
      let date = field_map json "date" Timestamp.of_json in
      let name = field_map json "name" AssessmentEvidenceFolderName.of_json in
      make ?evidenceAwsServiceSourceCount ?evidenceByTypeUserActivityCount
        ?evidenceByTypeComplianceCheckIssuesCount
        ?evidenceByTypeComplianceCheckCount ?evidenceByTypeManualCount
        ?evidenceByTypeConfigurationDataCount ?evidenceResourcesIncludedCount
        ?controlName ?assessmentReportSelectionCount ?totalEvidence ?author
        ?dataSource ?id ?controlId ?controlSetId ?assessmentId ?date ?name ()
    let to_json v = composed_to_json to_value v
  end
module Evidence =
  struct
    type nonrec t =
      {
      dataSource: String_.t option ;
      evidenceAwsAccountId: AccountId.t option ;
      time: Timestamp.t option ;
      eventSource: AWSServiceName.t option ;
      eventName: EventName.t option ;
      evidenceByType: String_.t option ;
      resourcesIncluded: Resources.t option ;
      attributes: EvidenceAttributes.t option ;
      iamId: IamArn.t option ;
      complianceCheck: String_.t option ;
      awsOrganization: String_.t option ;
      awsAccountId: AccountId.t option ;
      evidenceFolderId: UUID.t option ;
      id: UUID.t option ;
      assessmentReportSelection: String_.t option }
    let make ?dataSource =
      fun ?evidenceAwsAccountId ->
        fun ?time ->
          fun ?eventSource ->
            fun ?eventName ->
              fun ?evidenceByType ->
                fun ?resourcesIncluded ->
                  fun ?attributes ->
                    fun ?iamId ->
                      fun ?complianceCheck ->
                        fun ?awsOrganization ->
                          fun ?awsAccountId ->
                            fun ?evidenceFolderId ->
                              fun ?id ->
                                fun ?assessmentReportSelection ->
                                  fun () ->
                                    {
                                      dataSource;
                                      evidenceAwsAccountId;
                                      time;
                                      eventSource;
                                      eventName;
                                      evidenceByType;
                                      resourcesIncluded;
                                      attributes;
                                      iamId;
                                      complianceCheck;
                                      awsOrganization;
                                      awsAccountId;
                                      evidenceFolderId;
                                      id;
                                      assessmentReportSelection
                                    }
    let to_value x =
      structure_to_value
        [("dataSource", (Option.map x.dataSource ~f:String_.to_value));
        ("evidenceAwsAccountId",
          (Option.map x.evidenceAwsAccountId ~f:AccountId.to_value));
        ("time", (Option.map x.time ~f:Timestamp.to_value));
        ("eventSource",
          (Option.map x.eventSource ~f:AWSServiceName.to_value));
        ("eventName", (Option.map x.eventName ~f:EventName.to_value));
        ("evidenceByType", (Option.map x.evidenceByType ~f:String_.to_value));
        ("resourcesIncluded",
          (Option.map x.resourcesIncluded ~f:Resources.to_value));
        ("attributes",
          (Option.map x.attributes ~f:EvidenceAttributes.to_value));
        ("iamId", (Option.map x.iamId ~f:IamArn.to_value));
        ("complianceCheck",
          (Option.map x.complianceCheck ~f:String_.to_value));
        ("awsOrganization",
          (Option.map x.awsOrganization ~f:String_.to_value));
        ("awsAccountId", (Option.map x.awsAccountId ~f:AccountId.to_value));
        ("evidenceFolderId",
          (Option.map x.evidenceFolderId ~f:UUID.to_value));
        ("id", (Option.map x.id ~f:UUID.to_value));
        ("assessmentReportSelection",
          (Option.map x.assessmentReportSelection ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessmentReportSelection =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "assessmentReportSelection") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      let evidenceFolderId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "evidenceFolderId") in
      let awsAccountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "awsAccountId") in
      let awsOrganization =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "awsOrganization") in
      let complianceCheck =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "complianceCheck") in
      let iamId = (Option.map ~f:IamArn.of_xml) (Xml.child xml_arg0 "iamId") in
      let attributes =
        (Option.map ~f:EvidenceAttributes.of_xml)
          (Xml.child xml_arg0 "attributes") in
      let resourcesIncluded =
        (Option.map ~f:Resources.of_xml)
          (Xml.child xml_arg0 "resourcesIncluded") in
      let evidenceByType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "evidenceByType") in
      let eventName =
        (Option.map ~f:EventName.of_xml) (Xml.child xml_arg0 "eventName") in
      let eventSource =
        (Option.map ~f:AWSServiceName.of_xml)
          (Xml.child xml_arg0 "eventSource") in
      let time = (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "time") in
      let evidenceAwsAccountId =
        (Option.map ~f:AccountId.of_xml)
          (Xml.child xml_arg0 "evidenceAwsAccountId") in
      let dataSource =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "dataSource") in
      make ?assessmentReportSelection ?id ?evidenceFolderId ?awsAccountId
        ?awsOrganization ?complianceCheck ?iamId ?attributes
        ?resourcesIncluded ?evidenceByType ?eventName ?eventSource ?time
        ?evidenceAwsAccountId ?dataSource ()
    let of_json json =
      let assessmentReportSelection =
        field_map json "assessmentReportSelection" String_.of_json in
      let id = field_map json "id" UUID.of_json in
      let evidenceFolderId = field_map json "evidenceFolderId" UUID.of_json in
      let awsAccountId = field_map json "awsAccountId" AccountId.of_json in
      let awsOrganization = field_map json "awsOrganization" String_.of_json in
      let complianceCheck = field_map json "complianceCheck" String_.of_json in
      let iamId = field_map json "iamId" IamArn.of_json in
      let attributes = field_map json "attributes" EvidenceAttributes.of_json in
      let resourcesIncluded =
        field_map json "resourcesIncluded" Resources.of_json in
      let evidenceByType = field_map json "evidenceByType" String_.of_json in
      let eventName = field_map json "eventName" EventName.of_json in
      let eventSource = field_map json "eventSource" AWSServiceName.of_json in
      let time = field_map json "time" Timestamp.of_json in
      let evidenceAwsAccountId =
        field_map json "evidenceAwsAccountId" AccountId.of_json in
      let dataSource = field_map json "dataSource" String_.of_json in
      make ?assessmentReportSelection ?id ?evidenceFolderId ?awsAccountId
        ?awsOrganization ?complianceCheck ?iamId ?attributes
        ?resourcesIncluded ?evidenceByType ?eventName ?eventSource ?time
        ?evidenceAwsAccountId ?dataSource ()
    let to_json v = composed_to_json to_value v
  end
module DelegationMetadata =
  struct
    type nonrec t =
      {
      id: UUID.t option ;
      assessmentName: AssessmentName.t option ;
      assessmentId: UUID.t option ;
      status: DelegationStatus.t option ;
      roleArn: IamArn.t option ;
      creationTime: Timestamp.t option ;
      controlSetName: NonEmptyString.t option }
    let make ?id =
      fun ?assessmentName ->
        fun ?assessmentId ->
          fun ?status ->
            fun ?roleArn ->
              fun ?creationTime ->
                fun ?controlSetName ->
                  fun () ->
                    {
                      id;
                      assessmentName;
                      assessmentId;
                      status;
                      roleArn;
                      creationTime;
                      controlSetName
                    }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:UUID.to_value));
        ("assessmentName",
          (Option.map x.assessmentName ~f:AssessmentName.to_value));
        ("assessmentId", (Option.map x.assessmentId ~f:UUID.to_value));
        ("status", (Option.map x.status ~f:DelegationStatus.to_value));
        ("roleArn", (Option.map x.roleArn ~f:IamArn.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("controlSetName",
          (Option.map x.controlSetName ~f:NonEmptyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let controlSetName =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "controlSetName") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let roleArn =
        (Option.map ~f:IamArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let status =
        (Option.map ~f:DelegationStatus.of_xml) (Xml.child xml_arg0 "status") in
      let assessmentId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "assessmentId") in
      let assessmentName =
        (Option.map ~f:AssessmentName.of_xml)
          (Xml.child xml_arg0 "assessmentName") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      make ?controlSetName ?creationTime ?roleArn ?status ?assessmentId
        ?assessmentName ?id ()
    let of_json json =
      let controlSetName =
        field_map json "controlSetName" NonEmptyString.of_json in
      let creationTime = field_map json "creationTime" Timestamp.of_json in
      let roleArn = field_map json "roleArn" IamArn.of_json in
      let status = field_map json "status" DelegationStatus.of_json in
      let assessmentId = field_map json "assessmentId" UUID.of_json in
      let assessmentName =
        field_map json "assessmentName" AssessmentName.of_json in
      let id = field_map json "id" UUID.of_json in
      make ?controlSetName ?creationTime ?roleArn ?status ?assessmentId
        ?assessmentName ?id ()
    let to_json v = composed_to_json to_value v
  end
module ChangeLog =
  struct
    type nonrec t =
      {
      objectType: ObjectTypeEnum.t option ;
      objectName: NonEmptyString.t option ;
      action: ActionEnum.t option ;
      createdAt: Timestamp.t option ;
      createdBy: IamArn.t option }
    let make ?objectType =
      fun ?objectName ->
        fun ?action ->
          fun ?createdAt ->
            fun ?createdBy ->
              fun () ->
                { objectType; objectName; action; createdAt; createdBy }
    let to_value x =
      structure_to_value
        [("objectType", (Option.map x.objectType ~f:ObjectTypeEnum.to_value));
        ("objectName", (Option.map x.objectName ~f:NonEmptyString.to_value));
        ("action", (Option.map x.action ~f:ActionEnum.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("createdBy", (Option.map x.createdBy ~f:IamArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let createdBy =
        (Option.map ~f:IamArn.of_xml) (Xml.child xml_arg0 "createdBy") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let action =
        (Option.map ~f:ActionEnum.of_xml) (Xml.child xml_arg0 "action") in
      let objectName =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "objectName") in
      let objectType =
        (Option.map ~f:ObjectTypeEnum.of_xml)
          (Xml.child xml_arg0 "objectType") in
      make ?createdBy ?createdAt ?action ?objectName ?objectType ()
    let of_json json =
      let createdBy = field_map json "createdBy" IamArn.of_json in
      let createdAt = field_map json "createdAt" Timestamp.of_json in
      let action = field_map json "action" ActionEnum.of_json in
      let objectName = field_map json "objectName" NonEmptyString.of_json in
      let objectType = field_map json "objectType" ObjectTypeEnum.of_json in
      make ?createdBy ?createdAt ?action ?objectName ?objectType ()
    let to_json v = composed_to_json to_value v
  end
module HyperlinkName =
  struct
    type nonrec t = string
    let context_ = "HyperlinkName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:200) >>=
                  (fun () -> check_pattern i ~pattern:"^[\\w\\W\\s\\S]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HyperlinkName" j
    let to_json = simple_to_json to_value
  end
module UrlLink =
  struct
    type nonrec t = string
    let context_ = "UrlLink"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:8192) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(https?:\\/\\/)?(www\\.)?[a-zA-Z0-9-_]+([\\.]+[a-zA-Z]+)+[\\/\\w]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"UrlLink" j
    let to_json = simple_to_json to_value
  end
module CreateControlMappingSource =
  struct
    type nonrec t =
      {
      sourceName: SourceName.t option ;
      sourceDescription: SourceDescription.t option ;
      sourceSetUpOption: SourceSetUpOption.t option ;
      sourceType: SourceType.t option ;
      sourceKeyword: SourceKeyword.t option ;
      sourceFrequency: SourceFrequency.t option ;
      troubleshootingText: TroubleshootingText.t option }
    let make ?sourceName =
      fun ?sourceDescription ->
        fun ?sourceSetUpOption ->
          fun ?sourceType ->
            fun ?sourceKeyword ->
              fun ?sourceFrequency ->
                fun ?troubleshootingText ->
                  fun () ->
                    {
                      sourceName;
                      sourceDescription;
                      sourceSetUpOption;
                      sourceType;
                      sourceKeyword;
                      sourceFrequency;
                      troubleshootingText
                    }
    let to_value x =
      structure_to_value
        [("sourceName", (Option.map x.sourceName ~f:SourceName.to_value));
        ("sourceDescription",
          (Option.map x.sourceDescription ~f:SourceDescription.to_value));
        ("sourceSetUpOption",
          (Option.map x.sourceSetUpOption ~f:SourceSetUpOption.to_value));
        ("sourceType", (Option.map x.sourceType ~f:SourceType.to_value));
        ("sourceKeyword",
          (Option.map x.sourceKeyword ~f:SourceKeyword.to_value));
        ("sourceFrequency",
          (Option.map x.sourceFrequency ~f:SourceFrequency.to_value));
        ("troubleshootingText",
          (Option.map x.troubleshootingText ~f:TroubleshootingText.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let troubleshootingText =
        (Option.map ~f:TroubleshootingText.of_xml)
          (Xml.child xml_arg0 "troubleshootingText") in
      let sourceFrequency =
        (Option.map ~f:SourceFrequency.of_xml)
          (Xml.child xml_arg0 "sourceFrequency") in
      let sourceKeyword =
        (Option.map ~f:SourceKeyword.of_xml)
          (Xml.child xml_arg0 "sourceKeyword") in
      let sourceType =
        (Option.map ~f:SourceType.of_xml) (Xml.child xml_arg0 "sourceType") in
      let sourceSetUpOption =
        (Option.map ~f:SourceSetUpOption.of_xml)
          (Xml.child xml_arg0 "sourceSetUpOption") in
      let sourceDescription =
        (Option.map ~f:SourceDescription.of_xml)
          (Xml.child xml_arg0 "sourceDescription") in
      let sourceName =
        (Option.map ~f:SourceName.of_xml) (Xml.child xml_arg0 "sourceName") in
      make ?troubleshootingText ?sourceFrequency ?sourceKeyword ?sourceType
        ?sourceSetUpOption ?sourceDescription ?sourceName ()
    let of_json json =
      let troubleshootingText =
        field_map json "troubleshootingText" TroubleshootingText.of_json in
      let sourceFrequency =
        field_map json "sourceFrequency" SourceFrequency.of_json in
      let sourceKeyword =
        field_map json "sourceKeyword" SourceKeyword.of_json in
      let sourceType = field_map json "sourceType" SourceType.of_json in
      let sourceSetUpOption =
        field_map json "sourceSetUpOption" SourceSetUpOption.of_json in
      let sourceDescription =
        field_map json "sourceDescription" SourceDescription.of_json in
      let sourceName = field_map json "sourceName" SourceName.of_json in
      make ?troubleshootingText ?sourceFrequency ?sourceKeyword ?sourceType
        ?sourceSetUpOption ?sourceDescription ?sourceName ()
    let to_json v = composed_to_json to_value v
  end
module CreateAssessmentFrameworkControlSet =
  struct
    type nonrec t =
      {
      name: ControlSetName.t ;
      controls: CreateAssessmentFrameworkControls.t option }
    let context_ = "CreateAssessmentFrameworkControlSet"
    let make ?controls = fun ~name -> fun () -> { controls; name }
    let to_value x =
      structure_to_value
        [("name", (Some (ControlSetName.to_value x.name)));
        ("controls",
          (Option.map x.controls
             ~f:CreateAssessmentFrameworkControls.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let controls =
        (Option.map ~f:CreateAssessmentFrameworkControls.of_xml)
          (Xml.child xml_arg0 "controls") in
      let name =
        ControlSetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?controls ~name ()
    let of_json json =
      let controls =
        field_map json "controls" CreateAssessmentFrameworkControls.of_json in
      let name = field_map_exn json "name" ControlSetName.of_json in
      make ?controls ~name ()
    let to_json v = composed_to_json to_value v
  end
module BatchImportEvidenceToAssessmentControlError =
  struct
    type nonrec t =
      {
      manualEvidence: ManualEvidence.t option ;
      errorCode: ErrorCode.t option ;
      errorMessage: ErrorMessage.t option }
    let make ?manualEvidence =
      fun ?errorCode ->
        fun ?errorMessage ->
          fun () -> { manualEvidence; errorCode; errorMessage }
    let to_value x =
      structure_to_value
        [("manualEvidence",
           (Option.map x.manualEvidence ~f:ManualEvidence.to_value));
        ("errorCode", (Option.map x.errorCode ~f:ErrorCode.to_value));
        ("errorMessage",
          (Option.map x.errorMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "errorMessage") in
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      let manualEvidence =
        (Option.map ~f:ManualEvidence.of_xml)
          (Xml.child xml_arg0 "manualEvidence") in
      make ?errorMessage ?errorCode ?manualEvidence ()
    let of_json json =
      let errorMessage = field_map json "errorMessage" ErrorMessage.of_json in
      let errorCode = field_map json "errorCode" ErrorCode.of_json in
      let manualEvidence =
        field_map json "manualEvidence" ManualEvidence.of_json in
      make ?errorMessage ?errorCode ?manualEvidence ()
    let to_json v = composed_to_json to_value v
  end
module AssessmentReportEvidenceError =
  struct
    type nonrec t =
      {
      evidenceId: UUID.t option ;
      errorCode: ErrorCode.t option ;
      errorMessage: ErrorMessage.t option }
    let make ?evidenceId =
      fun ?errorCode ->
        fun ?errorMessage ->
          fun () -> { evidenceId; errorCode; errorMessage }
    let to_value x =
      structure_to_value
        [("evidenceId", (Option.map x.evidenceId ~f:UUID.to_value));
        ("errorCode", (Option.map x.errorCode ~f:ErrorCode.to_value));
        ("errorMessage",
          (Option.map x.errorMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "errorMessage") in
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      let evidenceId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "evidenceId") in
      make ?errorMessage ?errorCode ?evidenceId ()
    let of_json json =
      let errorMessage = field_map json "errorMessage" ErrorMessage.of_json in
      let errorCode = field_map json "errorCode" ErrorCode.of_json in
      let evidenceId = field_map json "evidenceId" UUID.of_json in
      make ?errorMessage ?errorCode ?evidenceId ()
    let to_json v = composed_to_json to_value v
  end
module BatchDeleteDelegationByAssessmentError =
  struct
    type nonrec t =
      {
      delegationId: UUID.t option ;
      errorCode: ErrorCode.t option ;
      errorMessage: ErrorMessage.t option }
    let make ?delegationId =
      fun ?errorCode ->
        fun ?errorMessage ->
          fun () -> { delegationId; errorCode; errorMessage }
    let to_value x =
      structure_to_value
        [("delegationId", (Option.map x.delegationId ~f:UUID.to_value));
        ("errorCode", (Option.map x.errorCode ~f:ErrorCode.to_value));
        ("errorMessage",
          (Option.map x.errorMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "errorMessage") in
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      let delegationId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "delegationId") in
      make ?errorMessage ?errorCode ?delegationId ()
    let of_json json =
      let errorMessage = field_map json "errorMessage" ErrorMessage.of_json in
      let errorCode = field_map json "errorCode" ErrorCode.of_json in
      let delegationId = field_map json "delegationId" UUID.of_json in
      make ?errorMessage ?errorCode ?delegationId ()
    let to_json v = composed_to_json to_value v
  end
module BatchCreateDelegationByAssessmentError =
  struct
    type nonrec t =
      {
      createDelegationRequest: CreateDelegationRequest.t option ;
      errorCode: ErrorCode.t option ;
      errorMessage: ErrorMessage.t option }
    let make ?createDelegationRequest =
      fun ?errorCode ->
        fun ?errorMessage ->
          fun () -> { createDelegationRequest; errorCode; errorMessage }
    let to_value x =
      structure_to_value
        [("createDelegationRequest",
           (Option.map x.createDelegationRequest
              ~f:CreateDelegationRequest.to_value));
        ("errorCode", (Option.map x.errorCode ~f:ErrorCode.to_value));
        ("errorMessage",
          (Option.map x.errorMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "errorMessage") in
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      let createDelegationRequest =
        (Option.map ~f:CreateDelegationRequest.of_xml)
          (Xml.child xml_arg0 "createDelegationRequest") in
      make ?errorMessage ?errorCode ?createDelegationRequest ()
    let of_json json =
      let errorMessage = field_map json "errorMessage" ErrorMessage.of_json in
      let errorCode = field_map json "errorCode" ErrorCode.of_json in
      let createDelegationRequest =
        field_map json "createDelegationRequest"
          CreateDelegationRequest.of_json in
      make ?errorMessage ?errorCode ?createDelegationRequest ()
    let to_json v = composed_to_json to_value v
  end
module AccessDeniedException =
  struct
    type nonrec t = {
      message: String_.t }
    let context_ = "AccessDeniedException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value [("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end
module InternalServerException =
  struct
    type nonrec t = {
      message: String_.t }
    let context_ = "InternalServerException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value [("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: String_.t ;
      resourceId: String_.t ;
      resourceType: String_.t }
    let context_ = "ResourceNotFoundException"
    let make ~message =
      fun ~resourceId ->
        fun ~resourceType -> fun () -> { message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("message", (Some (String_.to_value x.message)));
        ("resourceId", (Some (String_.to_value x.resourceId)));
        ("resourceType", (Some (String_.to_value x.resourceType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceType") in
      let resourceId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceId") in
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ~resourceType ~resourceId ~message ()
    let of_json json =
      let resourceType = field_map_exn json "resourceType" String_.of_json in
      let resourceId = field_map_exn json "resourceId" String_.of_json in
      let message = field_map_exn json "message" String_.of_json in
      make ~resourceType ~resourceId ~message ()
    let to_json v = composed_to_json to_value v
  end
module ValidationErrors =
  struct
    type nonrec t = NonEmptyString.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"ValidationErrors" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidationException =
  struct
    type nonrec t =
      {
      message: String_.t ;
      reason: ValidationExceptionReason.t option ;
      fields: ValidationExceptionFieldList.t option }
    let context_ = "ValidationException"
    let make ?reason =
      fun ?fields -> fun ~message -> fun () -> { reason; fields; message }
    let to_value x =
      structure_to_value
        [("message", (Some (String_.to_value x.message)));
        ("reason",
          (Option.map x.reason ~f:ValidationExceptionReason.to_value));
        ("fields",
          (Option.map x.fields ~f:ValidationExceptionFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fields =
        (Option.map ~f:ValidationExceptionFieldList.of_xml)
          (Xml.child xml_arg0 "fields") in
      let reason =
        (Option.map ~f:ValidationExceptionReason.of_xml)
          (Xml.child xml_arg0 "reason") in
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ?fields ?reason ~message ()
    let of_json json =
      let fields =
        field_map json "fields" ValidationExceptionFieldList.of_json in
      let reason = field_map json "reason" ValidationExceptionReason.of_json in
      let message = field_map_exn json "message" String_.of_json in
      make ?fields ?reason ~message ()
    let to_json v = composed_to_json to_value v
  end
module Settings =
  struct
    type nonrec t =
      {
      isAwsOrgEnabled: Boolean.t option ;
      snsTopic: SNSTopic.t option ;
      defaultAssessmentReportsDestination:
        AssessmentReportsDestination.t option ;
      defaultProcessOwners: Roles.t option ;
      kmsKey: KmsKey.t option }
    let make ?isAwsOrgEnabled =
      fun ?snsTopic ->
        fun ?defaultAssessmentReportsDestination ->
          fun ?defaultProcessOwners ->
            fun ?kmsKey ->
              fun () ->
                {
                  isAwsOrgEnabled;
                  snsTopic;
                  defaultAssessmentReportsDestination;
                  defaultProcessOwners;
                  kmsKey
                }
    let to_value x =
      structure_to_value
        [("isAwsOrgEnabled",
           (Option.map x.isAwsOrgEnabled ~f:Boolean.to_value));
        ("snsTopic", (Option.map x.snsTopic ~f:SNSTopic.to_value));
        ("defaultAssessmentReportsDestination",
          (Option.map x.defaultAssessmentReportsDestination
             ~f:AssessmentReportsDestination.to_value));
        ("defaultProcessOwners",
          (Option.map x.defaultProcessOwners ~f:Roles.to_value));
        ("kmsKey", (Option.map x.kmsKey ~f:KmsKey.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKey =
        (Option.map ~f:KmsKey.of_xml) (Xml.child xml_arg0 "kmsKey") in
      let defaultProcessOwners =
        (Option.map ~f:Roles.of_xml)
          (Xml.child xml_arg0 "defaultProcessOwners") in
      let defaultAssessmentReportsDestination =
        (Option.map ~f:AssessmentReportsDestination.of_xml)
          (Xml.child xml_arg0 "defaultAssessmentReportsDestination") in
      let snsTopic =
        (Option.map ~f:SNSTopic.of_xml) (Xml.child xml_arg0 "snsTopic") in
      let isAwsOrgEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isAwsOrgEnabled") in
      make ?kmsKey ?defaultProcessOwners ?defaultAssessmentReportsDestination
        ?snsTopic ?isAwsOrgEnabled ()
    let of_json json =
      let kmsKey = field_map json "kmsKey" KmsKey.of_json in
      let defaultProcessOwners =
        field_map json "defaultProcessOwners" Roles.of_json in
      let defaultAssessmentReportsDestination =
        field_map json "defaultAssessmentReportsDestination"
          AssessmentReportsDestination.of_json in
      let snsTopic = field_map json "snsTopic" SNSTopic.of_json in
      let isAwsOrgEnabled = field_map json "isAwsOrgEnabled" Boolean.of_json in
      make ?kmsKey ?defaultProcessOwners ?defaultAssessmentReportsDestination
        ?snsTopic ?isAwsOrgEnabled ()
    let to_json v = composed_to_json to_value v
  end
module SnsArn =
  struct
    type nonrec t = string
    let context_ = "SnsArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:4) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"^arn:.*:sns:.*|NONE")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SnsArn" j
    let to_json = simple_to_json to_value
  end
module Assessment =
  struct
    type nonrec t =
      {
      arn: AuditManagerArn.t option ;
      awsAccount: AWSAccount.t option ;
      metadata: AssessmentMetadata.t option ;
      framework: AssessmentFramework.t option ;
      tags: TagMap.t option }
    let make ?arn =
      fun ?awsAccount ->
        fun ?metadata ->
          fun ?framework ->
            fun ?tags ->
              fun () -> { arn; awsAccount; metadata; framework; tags }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AuditManagerArn.to_value));
        ("awsAccount", (Option.map x.awsAccount ~f:AWSAccount.to_value));
        ("metadata", (Option.map x.metadata ~f:AssessmentMetadata.to_value));
        ("framework",
          (Option.map x.framework ~f:AssessmentFramework.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let framework =
        (Option.map ~f:AssessmentFramework.of_xml)
          (Xml.child xml_arg0 "framework") in
      let metadata =
        (Option.map ~f:AssessmentMetadata.of_xml)
          (Xml.child xml_arg0 "metadata") in
      let awsAccount =
        (Option.map ~f:AWSAccount.of_xml) (Xml.child xml_arg0 "awsAccount") in
      let arn =
        (Option.map ~f:AuditManagerArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?tags ?framework ?metadata ?awsAccount ?arn ()
    let of_json json =
      let tags = field_map json "tags" TagMap.of_json in
      let framework = field_map json "framework" AssessmentFramework.of_json in
      let metadata = field_map json "metadata" AssessmentMetadata.of_json in
      let awsAccount = field_map json "awsAccount" AWSAccount.of_json in
      let arn = field_map json "arn" AuditManagerArn.of_json in
      make ?tags ?framework ?metadata ?awsAccount ?arn ()
    let to_json v = composed_to_json to_value v
  end
module ShareRequestAction =
  struct
    type nonrec t =
      | ACCEPT 
      | DECLINE 
      | REVOKE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACCEPT -> "ACCEPT"
      | DECLINE -> "DECLINE"
      | REVOKE -> "REVOKE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACCEPT" -> ACCEPT
      | "DECLINE" -> DECLINE
      | "REVOKE" -> REVOKE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ShareRequestAction" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ShareRequestAction" j)
    let to_json = simple_to_json to_value
  end
module ShareRequestType =
  struct
    type nonrec t =
      | SENT 
      | RECEIVED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SENT -> "SENT"
      | RECEIVED -> "RECEIVED"
      | Non_static_id s -> s
    let of_string =
      function
      | "SENT" -> SENT
      | "RECEIVED" -> RECEIVED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ShareRequestType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ShareRequestType" j)
    let to_json = simple_to_json to_value
  end
module Framework =
  struct
    type nonrec t =
      {
      arn: AuditManagerArn.t option ;
      id: UUID.t option ;
      name: FrameworkName.t option ;
      type_: FrameworkType.t option ;
      complianceType: ComplianceType.t option ;
      description: FrameworkDescription.t option ;
      logo: Filename.t option ;
      controlSources: ControlSources.t option ;
      controlSets: ControlSets.t option ;
      createdAt: Timestamp.t option ;
      lastUpdatedAt: Timestamp.t option ;
      createdBy: CreatedBy.t option ;
      lastUpdatedBy: LastUpdatedBy.t option ;
      tags: TagMap.t option }
    let make ?arn =
      fun ?id ->
        fun ?name ->
          fun ?type_ ->
            fun ?complianceType ->
              fun ?description ->
                fun ?logo ->
                  fun ?controlSources ->
                    fun ?controlSets ->
                      fun ?createdAt ->
                        fun ?lastUpdatedAt ->
                          fun ?createdBy ->
                            fun ?lastUpdatedBy ->
                              fun ?tags ->
                                fun () ->
                                  {
                                    arn;
                                    id;
                                    name;
                                    type_;
                                    complianceType;
                                    description;
                                    logo;
                                    controlSources;
                                    controlSets;
                                    createdAt;
                                    lastUpdatedAt;
                                    createdBy;
                                    lastUpdatedBy;
                                    tags
                                  }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AuditManagerArn.to_value));
        ("id", (Option.map x.id ~f:UUID.to_value));
        ("name", (Option.map x.name ~f:FrameworkName.to_value));
        ("type", (Option.map x.type_ ~f:FrameworkType.to_value));
        ("complianceType",
          (Option.map x.complianceType ~f:ComplianceType.to_value));
        ("description",
          (Option.map x.description ~f:FrameworkDescription.to_value));
        ("logo", (Option.map x.logo ~f:Filename.to_value));
        ("controlSources",
          (Option.map x.controlSources ~f:ControlSources.to_value));
        ("controlSets", (Option.map x.controlSets ~f:ControlSets.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("lastUpdatedAt", (Option.map x.lastUpdatedAt ~f:Timestamp.to_value));
        ("createdBy", (Option.map x.createdBy ~f:CreatedBy.to_value));
        ("lastUpdatedBy",
          (Option.map x.lastUpdatedBy ~f:LastUpdatedBy.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let lastUpdatedBy =
        (Option.map ~f:LastUpdatedBy.of_xml)
          (Xml.child xml_arg0 "lastUpdatedBy") in
      let createdBy =
        (Option.map ~f:CreatedBy.of_xml) (Xml.child xml_arg0 "createdBy") in
      let lastUpdatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let controlSets =
        (Option.map ~f:ControlSets.of_xml) (Xml.child xml_arg0 "controlSets") in
      let controlSources =
        (Option.map ~f:ControlSources.of_xml)
          (Xml.child xml_arg0 "controlSources") in
      let logo = (Option.map ~f:Filename.of_xml) (Xml.child xml_arg0 "logo") in
      let description =
        (Option.map ~f:FrameworkDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let complianceType =
        (Option.map ~f:ComplianceType.of_xml)
          (Xml.child xml_arg0 "complianceType") in
      let type_ =
        (Option.map ~f:FrameworkType.of_xml) (Xml.child xml_arg0 "type") in
      let name =
        (Option.map ~f:FrameworkName.of_xml) (Xml.child xml_arg0 "name") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      let arn =
        (Option.map ~f:AuditManagerArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?tags ?lastUpdatedBy ?createdBy ?lastUpdatedAt ?createdAt
        ?controlSets ?controlSources ?logo ?description ?complianceType
        ?type_ ?name ?id ?arn ()
    let of_json json =
      let tags = field_map json "tags" TagMap.of_json in
      let lastUpdatedBy =
        field_map json "lastUpdatedBy" LastUpdatedBy.of_json in
      let createdBy = field_map json "createdBy" CreatedBy.of_json in
      let lastUpdatedAt = field_map json "lastUpdatedAt" Timestamp.of_json in
      let createdAt = field_map json "createdAt" Timestamp.of_json in
      let controlSets = field_map json "controlSets" ControlSets.of_json in
      let controlSources =
        field_map json "controlSources" ControlSources.of_json in
      let logo = field_map json "logo" Filename.of_json in
      let description =
        field_map json "description" FrameworkDescription.of_json in
      let complianceType =
        field_map json "complianceType" ComplianceType.of_json in
      let type_ = field_map json "type" FrameworkType.of_json in
      let name = field_map json "name" FrameworkName.of_json in
      let id = field_map json "id" UUID.of_json in
      let arn = field_map json "arn" AuditManagerArn.of_json in
      make ?tags ?lastUpdatedBy ?createdBy ?lastUpdatedAt ?createdAt
        ?controlSets ?controlSources ?logo ?description ?complianceType
        ?type_ ?name ?id ?arn ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentFrameworkControlSets =
  struct
    type nonrec t = UpdateAssessmentFrameworkControlSet.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:UpdateAssessmentFrameworkControlSet.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:UpdateAssessmentFrameworkControlSet.of_xml)
    let of_json j =
      list_of_json ~kind:"UpdateAssessmentFrameworkControlSets"
        ~of_json:UpdateAssessmentFrameworkControlSet.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module OrganizationId =
  struct
    type nonrec t = string
    let context_ = "organizationId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:12) >>=
             (fun () ->
                (check_string_max i ~max:34) >>=
                  (fun () -> check_pattern i ~pattern:"o-[a-z0-9]{10,32}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"organizationId" j
    let to_json = simple_to_json to_value
  end
module AccountStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | INACTIVE 
      | PENDING_ACTIVATION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | INACTIVE -> "INACTIVE"
      | PENDING_ACTIVATION -> "PENDING_ACTIVATION"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "INACTIVE" -> INACTIVE
      | "PENDING_ACTIVATION" -> PENDING_ACTIVATION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AccountStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AccountStatus" j)
    let to_json = simple_to_json to_value
  end
module Notifications =
  struct
    type nonrec t = Notification.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Notification.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Notification.of_xml)
    let of_json j =
      list_of_json ~kind:"Notifications" ~of_json:Notification.of_json j
    let to_json v = composed_to_json to_value v
  end
module Token =
  struct
    type nonrec t = string
    let context_ = "Token"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1000) >>=
                  (fun () -> check_pattern i ~pattern:"^[A-Za-z0-9+\\/=]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Token" j
    let to_json = simple_to_json to_value
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Keywords =
  struct
    type nonrec t = KeywordValue.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:KeywordValue.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:KeywordValue.of_xml)
    let of_json j =
      list_of_json ~kind:"Keywords" ~of_json:KeywordValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlMetadataList =
  struct
    type nonrec t = ControlMetadata.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ControlMetadata.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ControlMetadata.of_xml)
    let of_json j =
      list_of_json ~kind:"ControlMetadataList"
        ~of_json:ControlMetadata.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlInsightsMetadata =
  struct
    type nonrec t = ControlInsightsMetadataItem.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ControlInsightsMetadataItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ControlInsightsMetadataItem.of_xml)
    let of_json j =
      list_of_json ~kind:"ControlInsightsMetadata"
        ~of_json:ControlInsightsMetadataItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlDomainInsightsList =
  struct
    type nonrec t = ControlDomainInsights.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ControlDomainInsights.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ControlDomainInsights.of_xml)
    let of_json j =
      list_of_json ~kind:"ControlDomainInsightsList"
        ~of_json:ControlDomainInsights.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListAssessmentMetadata =
  struct
    type nonrec t = AssessmentMetadataItem.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AssessmentMetadataItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AssessmentMetadataItem.of_xml)
    let of_json j =
      list_of_json ~kind:"ListAssessmentMetadata"
        ~of_json:AssessmentMetadataItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module AssessmentReportsMetadata =
  struct
    type nonrec t = AssessmentReportMetadata.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AssessmentReportMetadata.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AssessmentReportMetadata.of_xml)
    let of_json j =
      list_of_json ~kind:"AssessmentReportsMetadata"
        ~of_json:AssessmentReportMetadata.of_json j
    let to_json v = composed_to_json to_value v
  end
module FrameworkMetadataList =
  struct
    type nonrec t = AssessmentFrameworkMetadata.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AssessmentFrameworkMetadata.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AssessmentFrameworkMetadata.of_xml)
    let of_json j =
      list_of_json ~kind:"FrameworkMetadataList"
        ~of_json:AssessmentFrameworkMetadata.of_json j
    let to_json v = composed_to_json to_value v
  end
module AssessmentFrameworkShareRequestList =
  struct
    type nonrec t = AssessmentFrameworkShareRequest.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AssessmentFrameworkShareRequest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AssessmentFrameworkShareRequest.of_xml)
    let of_json j =
      list_of_json ~kind:"AssessmentFrameworkShareRequestList"
        ~of_json:AssessmentFrameworkShareRequest.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlInsightsMetadataByAssessment =
  struct
    type nonrec t = ControlInsightsMetadataByAssessmentItem.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ControlInsightsMetadataByAssessmentItem.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ControlInsightsMetadataByAssessmentItem.of_xml)
    let of_json j =
      list_of_json ~kind:"ControlInsightsMetadataByAssessment"
        ~of_json:ControlInsightsMetadataByAssessmentItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module SettingAttribute =
  struct
    type nonrec t =
      | ALL 
      | IS_AWS_ORG_ENABLED 
      | SNS_TOPIC 
      | DEFAULT_ASSESSMENT_REPORTS_DESTINATION 
      | DEFAULT_PROCESS_OWNERS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ALL -> "ALL"
      | IS_AWS_ORG_ENABLED -> "IS_AWS_ORG_ENABLED"
      | SNS_TOPIC -> "SNS_TOPIC"
      | DEFAULT_ASSESSMENT_REPORTS_DESTINATION ->
          "DEFAULT_ASSESSMENT_REPORTS_DESTINATION"
      | DEFAULT_PROCESS_OWNERS -> "DEFAULT_PROCESS_OWNERS"
      | Non_static_id s -> s
    let of_string =
      function
      | "ALL" -> ALL
      | "IS_AWS_ORG_ENABLED" -> IS_AWS_ORG_ENABLED
      | "SNS_TOPIC" -> SNS_TOPIC
      | "DEFAULT_ASSESSMENT_REPORTS_DESTINATION" ->
          DEFAULT_ASSESSMENT_REPORTS_DESTINATION
      | "DEFAULT_PROCESS_OWNERS" -> DEFAULT_PROCESS_OWNERS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SettingAttribute" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SettingAttribute" j)
    let to_json = simple_to_json to_value
  end
module ServiceMetadataList =
  struct
    type nonrec t = ServiceMetadata.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ServiceMetadata.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ServiceMetadata.of_xml)
    let of_json j =
      list_of_json ~kind:"ServiceMetadataList"
        ~of_json:ServiceMetadata.of_json j
    let to_json v = composed_to_json to_value v
  end
module Insights =
  struct
    type nonrec t =
      {
      activeAssessmentsCount: NullableInteger.t option ;
      noncompliantEvidenceCount: NullableInteger.t option ;
      compliantEvidenceCount: NullableInteger.t option ;
      inconclusiveEvidenceCount: NullableInteger.t option ;
      assessmentControlsCountByNoncompliantEvidence: NullableInteger.t option ;
      totalAssessmentControlsCount: NullableInteger.t option ;
      lastUpdated: Timestamp.t option }
    let make ?activeAssessmentsCount =
      fun ?noncompliantEvidenceCount ->
        fun ?compliantEvidenceCount ->
          fun ?inconclusiveEvidenceCount ->
            fun ?assessmentControlsCountByNoncompliantEvidence ->
              fun ?totalAssessmentControlsCount ->
                fun ?lastUpdated ->
                  fun () ->
                    {
                      activeAssessmentsCount;
                      noncompliantEvidenceCount;
                      compliantEvidenceCount;
                      inconclusiveEvidenceCount;
                      assessmentControlsCountByNoncompliantEvidence;
                      totalAssessmentControlsCount;
                      lastUpdated
                    }
    let to_value x =
      structure_to_value
        [("activeAssessmentsCount",
           (Option.map x.activeAssessmentsCount ~f:NullableInteger.to_value));
        ("noncompliantEvidenceCount",
          (Option.map x.noncompliantEvidenceCount ~f:NullableInteger.to_value));
        ("compliantEvidenceCount",
          (Option.map x.compliantEvidenceCount ~f:NullableInteger.to_value));
        ("inconclusiveEvidenceCount",
          (Option.map x.inconclusiveEvidenceCount ~f:NullableInteger.to_value));
        ("assessmentControlsCountByNoncompliantEvidence",
          (Option.map x.assessmentControlsCountByNoncompliantEvidence
             ~f:NullableInteger.to_value));
        ("totalAssessmentControlsCount",
          (Option.map x.totalAssessmentControlsCount
             ~f:NullableInteger.to_value));
        ("lastUpdated", (Option.map x.lastUpdated ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdated =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdated") in
      let totalAssessmentControlsCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "totalAssessmentControlsCount") in
      let assessmentControlsCountByNoncompliantEvidence =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "assessmentControlsCountByNoncompliantEvidence") in
      let inconclusiveEvidenceCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "inconclusiveEvidenceCount") in
      let compliantEvidenceCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "compliantEvidenceCount") in
      let noncompliantEvidenceCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "noncompliantEvidenceCount") in
      let activeAssessmentsCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "activeAssessmentsCount") in
      make ?lastUpdated ?totalAssessmentControlsCount
        ?assessmentControlsCountByNoncompliantEvidence
        ?inconclusiveEvidenceCount ?compliantEvidenceCount
        ?noncompliantEvidenceCount ?activeAssessmentsCount ()
    let of_json json =
      let lastUpdated = field_map json "lastUpdated" Timestamp.of_json in
      let totalAssessmentControlsCount =
        field_map json "totalAssessmentControlsCount" NullableInteger.of_json in
      let assessmentControlsCountByNoncompliantEvidence =
        field_map json "assessmentControlsCountByNoncompliantEvidence"
          NullableInteger.of_json in
      let inconclusiveEvidenceCount =
        field_map json "inconclusiveEvidenceCount" NullableInteger.of_json in
      let compliantEvidenceCount =
        field_map json "compliantEvidenceCount" NullableInteger.of_json in
      let noncompliantEvidenceCount =
        field_map json "noncompliantEvidenceCount" NullableInteger.of_json in
      let activeAssessmentsCount =
        field_map json "activeAssessmentsCount" NullableInteger.of_json in
      make ?lastUpdated ?totalAssessmentControlsCount
        ?assessmentControlsCountByNoncompliantEvidence
        ?inconclusiveEvidenceCount ?compliantEvidenceCount
        ?noncompliantEvidenceCount ?activeAssessmentsCount ()
    let to_json v = composed_to_json to_value v
  end
module InsightsByAssessment =
  struct
    type nonrec t =
      {
      noncompliantEvidenceCount: NullableInteger.t option ;
      compliantEvidenceCount: NullableInteger.t option ;
      inconclusiveEvidenceCount: NullableInteger.t option ;
      assessmentControlsCountByNoncompliantEvidence: NullableInteger.t option ;
      totalAssessmentControlsCount: NullableInteger.t option ;
      lastUpdated: Timestamp.t option }
    let make ?noncompliantEvidenceCount =
      fun ?compliantEvidenceCount ->
        fun ?inconclusiveEvidenceCount ->
          fun ?assessmentControlsCountByNoncompliantEvidence ->
            fun ?totalAssessmentControlsCount ->
              fun ?lastUpdated ->
                fun () ->
                  {
                    noncompliantEvidenceCount;
                    compliantEvidenceCount;
                    inconclusiveEvidenceCount;
                    assessmentControlsCountByNoncompliantEvidence;
                    totalAssessmentControlsCount;
                    lastUpdated
                  }
    let to_value x =
      structure_to_value
        [("noncompliantEvidenceCount",
           (Option.map x.noncompliantEvidenceCount
              ~f:NullableInteger.to_value));
        ("compliantEvidenceCount",
          (Option.map x.compliantEvidenceCount ~f:NullableInteger.to_value));
        ("inconclusiveEvidenceCount",
          (Option.map x.inconclusiveEvidenceCount ~f:NullableInteger.to_value));
        ("assessmentControlsCountByNoncompliantEvidence",
          (Option.map x.assessmentControlsCountByNoncompliantEvidence
             ~f:NullableInteger.to_value));
        ("totalAssessmentControlsCount",
          (Option.map x.totalAssessmentControlsCount
             ~f:NullableInteger.to_value));
        ("lastUpdated", (Option.map x.lastUpdated ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdated =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdated") in
      let totalAssessmentControlsCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "totalAssessmentControlsCount") in
      let assessmentControlsCountByNoncompliantEvidence =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "assessmentControlsCountByNoncompliantEvidence") in
      let inconclusiveEvidenceCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "inconclusiveEvidenceCount") in
      let compliantEvidenceCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "compliantEvidenceCount") in
      let noncompliantEvidenceCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "noncompliantEvidenceCount") in
      make ?lastUpdated ?totalAssessmentControlsCount
        ?assessmentControlsCountByNoncompliantEvidence
        ?inconclusiveEvidenceCount ?compliantEvidenceCount
        ?noncompliantEvidenceCount ()
    let of_json json =
      let lastUpdated = field_map json "lastUpdated" Timestamp.of_json in
      let totalAssessmentControlsCount =
        field_map json "totalAssessmentControlsCount" NullableInteger.of_json in
      let assessmentControlsCountByNoncompliantEvidence =
        field_map json "assessmentControlsCountByNoncompliantEvidence"
          NullableInteger.of_json in
      let inconclusiveEvidenceCount =
        field_map json "inconclusiveEvidenceCount" NullableInteger.of_json in
      let compliantEvidenceCount =
        field_map json "compliantEvidenceCount" NullableInteger.of_json in
      let noncompliantEvidenceCount =
        field_map json "noncompliantEvidenceCount" NullableInteger.of_json in
      make ?lastUpdated ?totalAssessmentControlsCount
        ?assessmentControlsCountByNoncompliantEvidence
        ?inconclusiveEvidenceCount ?compliantEvidenceCount
        ?noncompliantEvidenceCount ()
    let to_json v = composed_to_json to_value v
  end
module AssessmentEvidenceFolders =
  struct
    type nonrec t = AssessmentEvidenceFolder.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AssessmentEvidenceFolder.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AssessmentEvidenceFolder.of_xml)
    let of_json j =
      list_of_json ~kind:"AssessmentEvidenceFolders"
        ~of_json:AssessmentEvidenceFolder.of_json j
    let to_json v = composed_to_json to_value v
  end
module EvidenceList =
  struct
    type nonrec t = Evidence.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Evidence.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Evidence.of_xml)
    let of_json j =
      list_of_json ~kind:"EvidenceList" ~of_json:Evidence.of_json j
    let to_json v = composed_to_json to_value v
  end
module DelegationMetadataList =
  struct
    type nonrec t = DelegationMetadata.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:DelegationMetadata.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DelegationMetadata.of_xml)
    let of_json j =
      list_of_json ~kind:"DelegationMetadataList"
        ~of_json:DelegationMetadata.of_json j
    let to_json v = composed_to_json to_value v
  end
module ChangeLogs =
  struct
    type nonrec t = ChangeLog.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ChangeLog.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ChangeLog.of_xml)
    let of_json j =
      list_of_json ~kind:"ChangeLogs" ~of_json:ChangeLog.of_json j
    let to_json v = composed_to_json to_value v
  end
module URL =
  struct
    type nonrec t =
      {
      hyperlinkName: HyperlinkName.t option ;
      link: UrlLink.t option }
    let make ?hyperlinkName = fun ?link -> fun () -> { hyperlinkName; link }
    let to_value x =
      structure_to_value
        [("hyperlinkName",
           (Option.map x.hyperlinkName ~f:HyperlinkName.to_value));
        ("link", (Option.map x.link ~f:UrlLink.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let link = (Option.map ~f:UrlLink.of_xml) (Xml.child xml_arg0 "link") in
      let hyperlinkName =
        (Option.map ~f:HyperlinkName.of_xml)
          (Xml.child xml_arg0 "hyperlinkName") in
      make ?link ?hyperlinkName ()
    let of_json json =
      let link = field_map json "link" UrlLink.of_json in
      let hyperlinkName =
        field_map json "hyperlinkName" HyperlinkName.of_json in
      make ?link ?hyperlinkName ()
    let to_json v = composed_to_json to_value v
  end
module CreateControlMappingSources =
  struct
    type nonrec t = CreateControlMappingSource.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:CreateControlMappingSource.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CreateControlMappingSource.of_xml)
    let of_json j =
      list_of_json ~kind:"CreateControlMappingSources"
        ~of_json:CreateControlMappingSource.of_json j
    let to_json v = composed_to_json to_value v
  end
module AssessmentReport =
  struct
    type nonrec t =
      {
      id: UUID.t option ;
      name: AssessmentReportName.t option ;
      description: AssessmentReportDescription.t option ;
      awsAccountId: AccountId.t option ;
      assessmentId: UUID.t option ;
      assessmentName: AssessmentName.t option ;
      author: Username.t option ;
      status: AssessmentReportStatus.t option ;
      creationTime: Timestamp.t option }
    let make ?id =
      fun ?name ->
        fun ?description ->
          fun ?awsAccountId ->
            fun ?assessmentId ->
              fun ?assessmentName ->
                fun ?author ->
                  fun ?status ->
                    fun ?creationTime ->
                      fun () ->
                        {
                          id;
                          name;
                          description;
                          awsAccountId;
                          assessmentId;
                          assessmentName;
                          author;
                          status;
                          creationTime
                        }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:UUID.to_value));
        ("name", (Option.map x.name ~f:AssessmentReportName.to_value));
        ("description",
          (Option.map x.description ~f:AssessmentReportDescription.to_value));
        ("awsAccountId", (Option.map x.awsAccountId ~f:AccountId.to_value));
        ("assessmentId", (Option.map x.assessmentId ~f:UUID.to_value));
        ("assessmentName",
          (Option.map x.assessmentName ~f:AssessmentName.to_value));
        ("author", (Option.map x.author ~f:Username.to_value));
        ("status", (Option.map x.status ~f:AssessmentReportStatus.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let status =
        (Option.map ~f:AssessmentReportStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let author =
        (Option.map ~f:Username.of_xml) (Xml.child xml_arg0 "author") in
      let assessmentName =
        (Option.map ~f:AssessmentName.of_xml)
          (Xml.child xml_arg0 "assessmentName") in
      let assessmentId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "assessmentId") in
      let awsAccountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "awsAccountId") in
      let description =
        (Option.map ~f:AssessmentReportDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:AssessmentReportName.of_xml)
          (Xml.child xml_arg0 "name") in
      let id = (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "id") in
      make ?creationTime ?status ?author ?assessmentName ?assessmentId
        ?awsAccountId ?description ?name ?id ()
    let of_json json =
      let creationTime = field_map json "creationTime" Timestamp.of_json in
      let status = field_map json "status" AssessmentReportStatus.of_json in
      let author = field_map json "author" Username.of_json in
      let assessmentName =
        field_map json "assessmentName" AssessmentName.of_json in
      let assessmentId = field_map json "assessmentId" UUID.of_json in
      let awsAccountId = field_map json "awsAccountId" AccountId.of_json in
      let description =
        field_map json "description" AssessmentReportDescription.of_json in
      let name = field_map json "name" AssessmentReportName.of_json in
      let id = field_map json "id" UUID.of_json in
      make ?creationTime ?status ?author ?assessmentName ?assessmentId
        ?awsAccountId ?description ?name ?id ()
    let to_json v = composed_to_json to_value v
  end
module CreateAssessmentFrameworkControlSets =
  struct
    type nonrec t = CreateAssessmentFrameworkControlSet.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:CreateAssessmentFrameworkControlSet.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:CreateAssessmentFrameworkControlSet.of_xml)
    let of_json j =
      list_of_json ~kind:"CreateAssessmentFrameworkControlSets"
        ~of_json:CreateAssessmentFrameworkControlSet.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchImportEvidenceToAssessmentControlErrors =
  struct
    type nonrec t = BatchImportEvidenceToAssessmentControlError.t list
    let make i = i
    let to_value xs =
      (xs |>
         (List.map ~f:BatchImportEvidenceToAssessmentControlError.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:BatchImportEvidenceToAssessmentControlError.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchImportEvidenceToAssessmentControlErrors"
        ~of_json:BatchImportEvidenceToAssessmentControlError.of_json j
    let to_json v = composed_to_json to_value v
  end
module ManualEvidenceList =
  struct
    type nonrec t = ManualEvidence.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:ManualEvidence.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ManualEvidence.of_xml)
    let of_json j =
      list_of_json ~kind:"ManualEvidenceList" ~of_json:ManualEvidence.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module AssessmentReportEvidenceErrors =
  struct
    type nonrec t = AssessmentReportEvidenceError.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AssessmentReportEvidenceError.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AssessmentReportEvidenceError.of_xml)
    let of_json j =
      list_of_json ~kind:"AssessmentReportEvidenceErrors"
        ~of_json:AssessmentReportEvidenceError.of_json j
    let to_json v = composed_to_json to_value v
  end
module EvidenceIds =
  struct
    type nonrec t = UUID.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:UUID.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UUID.of_xml)
    let of_json j = list_of_json ~kind:"EvidenceIds" ~of_json:UUID.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchDeleteDelegationByAssessmentErrors =
  struct
    type nonrec t = BatchDeleteDelegationByAssessmentError.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BatchDeleteDelegationByAssessmentError.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:BatchDeleteDelegationByAssessmentError.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchDeleteDelegationByAssessmentErrors"
        ~of_json:BatchDeleteDelegationByAssessmentError.of_json j
    let to_json v = composed_to_json to_value v
  end
module DelegationIds =
  struct
    type nonrec t = UUID.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:UUID.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UUID.of_xml)
    let of_json j =
      list_of_json ~kind:"DelegationIds" ~of_json:UUID.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchCreateDelegationByAssessmentErrors =
  struct
    type nonrec t = BatchCreateDelegationByAssessmentError.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BatchCreateDelegationByAssessmentError.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:BatchCreateDelegationByAssessmentError.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchCreateDelegationByAssessmentErrors"
        ~of_json:BatchCreateDelegationByAssessmentError.of_json j
    let to_json v = composed_to_json to_value v
  end
module CreateDelegationRequests =
  struct
    type nonrec t = CreateDelegationRequest.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:CreateDelegationRequest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CreateDelegationRequest.of_xml)
    let of_json j =
      list_of_json ~kind:"CreateDelegationRequests"
        ~of_json:CreateDelegationRequest.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidateAssessmentReportIntegrityResponse =
  struct
    type nonrec t =
      {
      signatureValid: Boolean.t option ;
      signatureAlgorithm: String_.t option ;
      signatureDateTime: String_.t option ;
      signatureKeyId: String_.t option ;
      validationErrors: ValidationErrors.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?signatureValid =
      fun ?signatureAlgorithm ->
        fun ?signatureDateTime ->
          fun ?signatureKeyId ->
            fun ?validationErrors ->
              fun () ->
                {
                  signatureValid;
                  signatureAlgorithm;
                  signatureDateTime;
                  signatureKeyId;
                  validationErrors
                }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("signatureValid",
           (Option.map x.signatureValid ~f:Boolean.to_value));
        ("signatureAlgorithm",
          (Option.map x.signatureAlgorithm ~f:String_.to_value));
        ("signatureDateTime",
          (Option.map x.signatureDateTime ~f:String_.to_value));
        ("signatureKeyId", (Option.map x.signatureKeyId ~f:String_.to_value));
        ("validationErrors",
          (Option.map x.validationErrors ~f:ValidationErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let validationErrors =
        (Option.map ~f:ValidationErrors.of_xml)
          (Xml.child xml_arg0 "validationErrors") in
      let signatureKeyId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "signatureKeyId") in
      let signatureDateTime =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "signatureDateTime") in
      let signatureAlgorithm =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "signatureAlgorithm") in
      let signatureValid =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "signatureValid") in
      make ?validationErrors ?signatureKeyId ?signatureDateTime
        ?signatureAlgorithm ?signatureValid ()
    let of_json json =
      let validationErrors =
        field_map json "validationErrors" ValidationErrors.of_json in
      let signatureKeyId = field_map json "signatureKeyId" String_.of_json in
      let signatureDateTime =
        field_map json "signatureDateTime" String_.of_json in
      let signatureAlgorithm =
        field_map json "signatureAlgorithm" String_.of_json in
      let signatureValid = field_map json "signatureValid" Boolean.of_json in
      make ?validationErrors ?signatureKeyId ?signatureDateTime
        ?signatureAlgorithm ?signatureValid ()
    let to_json v = composed_to_json to_value v
  end
module ValidateAssessmentReportIntegrityRequest =
  struct
    type nonrec t = {
      s3RelativePath: S3Url.t }
    let context_ = "ValidateAssessmentReportIntegrityRequest"
    let make ~s3RelativePath = fun () -> { s3RelativePath }
    let to_value x =
      structure_to_value
        [("s3RelativePath", (Some (S3Url.to_value x.s3RelativePath)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3RelativePath =
        S3Url.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "s3RelativePath") in
      make ~s3RelativePath ()
    let of_json json =
      let s3RelativePath = field_map_exn json "s3RelativePath" S3Url.of_json in
      make ~s3RelativePath ()
    let to_json v = composed_to_json to_value v
  end
module UpdateSettingsResponse =
  struct
    type nonrec t = {
      settings: Settings.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?settings = fun () -> { settings }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("settings", (Option.map x.settings ~f:Settings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let settings =
        (Option.map ~f:Settings.of_xml) (Xml.child xml_arg0 "settings") in
      make ?settings ()
    let of_json json =
      let settings = field_map json "settings" Settings.of_json in
      make ?settings ()
    let to_json v = composed_to_json to_value v
  end
module UpdateSettingsRequest =
  struct
    type nonrec t =
      {
      snsTopic: SnsArn.t option ;
      defaultAssessmentReportsDestination:
        AssessmentReportsDestination.t option ;
      defaultProcessOwners: Roles.t option ;
      kmsKey: KmsKey.t option }
    let make ?snsTopic =
      fun ?defaultAssessmentReportsDestination ->
        fun ?defaultProcessOwners ->
          fun ?kmsKey ->
            fun () ->
              {
                snsTopic;
                defaultAssessmentReportsDestination;
                defaultProcessOwners;
                kmsKey
              }
    let to_value x =
      structure_to_value
        [("snsTopic", (Option.map x.snsTopic ~f:SnsArn.to_value));
        ("defaultAssessmentReportsDestination",
          (Option.map x.defaultAssessmentReportsDestination
             ~f:AssessmentReportsDestination.to_value));
        ("defaultProcessOwners",
          (Option.map x.defaultProcessOwners ~f:Roles.to_value));
        ("kmsKey", (Option.map x.kmsKey ~f:KmsKey.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKey =
        (Option.map ~f:KmsKey.of_xml) (Xml.child xml_arg0 "kmsKey") in
      let defaultProcessOwners =
        (Option.map ~f:Roles.of_xml)
          (Xml.child xml_arg0 "defaultProcessOwners") in
      let defaultAssessmentReportsDestination =
        (Option.map ~f:AssessmentReportsDestination.of_xml)
          (Xml.child xml_arg0 "defaultAssessmentReportsDestination") in
      let snsTopic =
        (Option.map ~f:SnsArn.of_xml) (Xml.child xml_arg0 "snsTopic") in
      make ?kmsKey ?defaultProcessOwners ?defaultAssessmentReportsDestination
        ?snsTopic ()
    let of_json json =
      let kmsKey = field_map json "kmsKey" KmsKey.of_json in
      let defaultProcessOwners =
        field_map json "defaultProcessOwners" Roles.of_json in
      let defaultAssessmentReportsDestination =
        field_map json "defaultAssessmentReportsDestination"
          AssessmentReportsDestination.of_json in
      let snsTopic = field_map json "snsTopic" SnsArn.of_json in
      make ?kmsKey ?defaultProcessOwners ?defaultAssessmentReportsDestination
        ?snsTopic ()
    let to_json v = composed_to_json to_value v
  end
module UpdateControlResponse =
  struct
    type nonrec t = {
      control: Control.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?control = fun () -> { control }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("control", (Option.map x.control ~f:Control.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let control =
        (Option.map ~f:Control.of_xml) (Xml.child xml_arg0 "control") in
      make ?control ()
    let of_json json =
      let control = field_map json "control" Control.of_json in
      make ?control ()
    let to_json v = composed_to_json to_value v
  end
module UpdateControlRequest =
  struct
    type nonrec t =
      {
      controlId: UUID.t ;
      name: ControlName.t ;
      description: ControlDescription.t option ;
      testingInformation: TestingInformation.t option ;
      actionPlanTitle: ActionPlanTitle.t option ;
      actionPlanInstructions: ActionPlanInstructions.t option ;
      controlMappingSources: ControlMappingSources.t }
    let context_ = "UpdateControlRequest"
    let make ?description =
      fun ?testingInformation ->
        fun ?actionPlanTitle ->
          fun ?actionPlanInstructions ->
            fun ~controlId ->
              fun ~name ->
                fun ~controlMappingSources ->
                  fun () ->
                    {
                      description;
                      testingInformation;
                      actionPlanTitle;
                      actionPlanInstructions;
                      controlId;
                      name;
                      controlMappingSources
                    }
    let to_value x =
      structure_to_value
        [("controlId", (Some (UUID.to_value x.controlId)));
        ("name", (Some (ControlName.to_value x.name)));
        ("description",
          (Option.map x.description ~f:ControlDescription.to_value));
        ("testingInformation",
          (Option.map x.testingInformation ~f:TestingInformation.to_value));
        ("actionPlanTitle",
          (Option.map x.actionPlanTitle ~f:ActionPlanTitle.to_value));
        ("actionPlanInstructions",
          (Option.map x.actionPlanInstructions
             ~f:ActionPlanInstructions.to_value));
        ("controlMappingSources",
          (Some (ControlMappingSources.to_value x.controlMappingSources)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let controlMappingSources =
        ControlMappingSources.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlMappingSources") in
      let actionPlanInstructions =
        (Option.map ~f:ActionPlanInstructions.of_xml)
          (Xml.child xml_arg0 "actionPlanInstructions") in
      let actionPlanTitle =
        (Option.map ~f:ActionPlanTitle.of_xml)
          (Xml.child xml_arg0 "actionPlanTitle") in
      let testingInformation =
        (Option.map ~f:TestingInformation.of_xml)
          (Xml.child xml_arg0 "testingInformation") in
      let description =
        (Option.map ~f:ControlDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        ControlName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let controlId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "controlId") in
      make ~controlMappingSources ?actionPlanInstructions ?actionPlanTitle
        ?testingInformation ?description ~name ~controlId ()
    let of_json json =
      let controlMappingSources =
        field_map_exn json "controlMappingSources"
          ControlMappingSources.of_json in
      let actionPlanInstructions =
        field_map json "actionPlanInstructions"
          ActionPlanInstructions.of_json in
      let actionPlanTitle =
        field_map json "actionPlanTitle" ActionPlanTitle.of_json in
      let testingInformation =
        field_map json "testingInformation" TestingInformation.of_json in
      let description =
        field_map json "description" ControlDescription.of_json in
      let name = field_map_exn json "name" ControlName.of_json in
      let controlId = field_map_exn json "controlId" UUID.of_json in
      make ~controlMappingSources ?actionPlanInstructions ?actionPlanTitle
        ?testingInformation ?description ~name ~controlId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentStatusResponse =
  struct
    type nonrec t = {
      assessment: Assessment.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?assessment = fun () -> { assessment }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("assessment", (Option.map x.assessment ~f:Assessment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessment =
        (Option.map ~f:Assessment.of_xml) (Xml.child xml_arg0 "assessment") in
      make ?assessment ()
    let of_json json =
      let assessment = field_map json "assessment" Assessment.of_json in
      make ?assessment ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentStatusRequest =
  struct
    type nonrec t = {
      assessmentId: UUID.t ;
      status: AssessmentStatus.t }
    let context_ = "UpdateAssessmentStatusRequest"
    let make ~assessmentId =
      fun ~status -> fun () -> { assessmentId; status }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("status", (Some (AssessmentStatus.to_value x.status)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        AssessmentStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "status") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ~status ~assessmentId ()
    let of_json json =
      let status = field_map_exn json "status" AssessmentStatus.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ~status ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentResponse =
  struct
    type nonrec t = {
      assessment: Assessment.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?assessment = fun () -> { assessment }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("assessment", (Option.map x.assessment ~f:Assessment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessment =
        (Option.map ~f:Assessment.of_xml) (Xml.child xml_arg0 "assessment") in
      make ?assessment ()
    let of_json json =
      let assessment = field_map json "assessment" Assessment.of_json in
      make ?assessment ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentRequest =
  struct
    type nonrec t =
      {
      assessmentId: UUID.t ;
      assessmentName: AssessmentName.t option ;
      assessmentDescription: AssessmentDescription.t option ;
      scope: Scope.t ;
      assessmentReportsDestination: AssessmentReportsDestination.t option ;
      roles: Roles.t option }
    let context_ = "UpdateAssessmentRequest"
    let make ?assessmentName =
      fun ?assessmentDescription ->
        fun ?assessmentReportsDestination ->
          fun ?roles ->
            fun ~assessmentId ->
              fun ~scope ->
                fun () ->
                  {
                    assessmentName;
                    assessmentDescription;
                    assessmentReportsDestination;
                    roles;
                    assessmentId;
                    scope
                  }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("assessmentName",
          (Option.map x.assessmentName ~f:AssessmentName.to_value));
        ("assessmentDescription",
          (Option.map x.assessmentDescription
             ~f:AssessmentDescription.to_value));
        ("scope", (Some (Scope.to_value x.scope)));
        ("assessmentReportsDestination",
          (Option.map x.assessmentReportsDestination
             ~f:AssessmentReportsDestination.to_value));
        ("roles", (Option.map x.roles ~f:Roles.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let roles = (Option.map ~f:Roles.of_xml) (Xml.child xml_arg0 "roles") in
      let assessmentReportsDestination =
        (Option.map ~f:AssessmentReportsDestination.of_xml)
          (Xml.child xml_arg0 "assessmentReportsDestination") in
      let scope =
        Scope.of_xml (Xml.child_exn ~context:context_ xml_arg0 "scope") in
      let assessmentDescription =
        (Option.map ~f:AssessmentDescription.of_xml)
          (Xml.child xml_arg0 "assessmentDescription") in
      let assessmentName =
        (Option.map ~f:AssessmentName.of_xml)
          (Xml.child xml_arg0 "assessmentName") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ?roles ?assessmentReportsDestination ~scope ?assessmentDescription
        ?assessmentName ~assessmentId ()
    let of_json json =
      let roles = field_map json "roles" Roles.of_json in
      let assessmentReportsDestination =
        field_map json "assessmentReportsDestination"
          AssessmentReportsDestination.of_json in
      let scope = field_map_exn json "scope" Scope.of_json in
      let assessmentDescription =
        field_map json "assessmentDescription" AssessmentDescription.of_json in
      let assessmentName =
        field_map json "assessmentName" AssessmentName.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ?roles ?assessmentReportsDestination ~scope ?assessmentDescription
        ?assessmentName ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentFrameworkShareResponse =
  struct
    type nonrec t =
      {
      assessmentFrameworkShareRequest:
        AssessmentFrameworkShareRequest.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?assessmentFrameworkShareRequest =
      fun () -> { assessmentFrameworkShareRequest }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("assessmentFrameworkShareRequest",
           (Option.map x.assessmentFrameworkShareRequest
              ~f:AssessmentFrameworkShareRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessmentFrameworkShareRequest =
        (Option.map ~f:AssessmentFrameworkShareRequest.of_xml)
          (Xml.child xml_arg0 "assessmentFrameworkShareRequest") in
      make ?assessmentFrameworkShareRequest ()
    let of_json json =
      let assessmentFrameworkShareRequest =
        field_map json "assessmentFrameworkShareRequest"
          AssessmentFrameworkShareRequest.of_json in
      make ?assessmentFrameworkShareRequest ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentFrameworkShareRequest =
  struct
    type nonrec t =
      {
      requestId: UUID.t ;
      requestType: ShareRequestType.t ;
      action: ShareRequestAction.t }
    let context_ = "UpdateAssessmentFrameworkShareRequest"
    let make ~requestId =
      fun ~requestType ->
        fun ~action -> fun () -> { requestId; requestType; action }
    let to_value x =
      structure_to_value
        [("requestId", (Some (UUID.to_value x.requestId)));
        ("requestType", (Some (ShareRequestType.to_value x.requestType)));
        ("action", (Some (ShareRequestAction.to_value x.action)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let action =
        ShareRequestAction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "action") in
      let requestType =
        ShareRequestType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "requestType") in
      let requestId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "requestId") in
      make ~action ~requestType ~requestId ()
    let of_json json =
      let action = field_map_exn json "action" ShareRequestAction.of_json in
      let requestType =
        field_map_exn json "requestType" ShareRequestType.of_json in
      let requestId = field_map_exn json "requestId" UUID.of_json in
      make ~action ~requestType ~requestId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentFrameworkResponse =
  struct
    type nonrec t = {
      framework: Framework.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?framework = fun () -> { framework }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("framework", (Option.map x.framework ~f:Framework.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let framework =
        (Option.map ~f:Framework.of_xml) (Xml.child xml_arg0 "framework") in
      make ?framework ()
    let of_json json =
      let framework = field_map json "framework" Framework.of_json in
      make ?framework ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentFrameworkRequest =
  struct
    type nonrec t =
      {
      frameworkId: UUID.t ;
      name: FrameworkName.t ;
      description: FrameworkDescription.t option ;
      complianceType: ComplianceType.t option ;
      controlSets: UpdateAssessmentFrameworkControlSets.t }
    let context_ = "UpdateAssessmentFrameworkRequest"
    let make ?description =
      fun ?complianceType ->
        fun ~frameworkId ->
          fun ~name ->
            fun ~controlSets ->
              fun () ->
                { description; complianceType; frameworkId; name; controlSets
                }
    let to_value x =
      structure_to_value
        [("frameworkId", (Some (UUID.to_value x.frameworkId)));
        ("name", (Some (FrameworkName.to_value x.name)));
        ("description",
          (Option.map x.description ~f:FrameworkDescription.to_value));
        ("complianceType",
          (Option.map x.complianceType ~f:ComplianceType.to_value));
        ("controlSets",
          (Some (UpdateAssessmentFrameworkControlSets.to_value x.controlSets)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let controlSets =
        UpdateAssessmentFrameworkControlSets.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlSets") in
      let complianceType =
        (Option.map ~f:ComplianceType.of_xml)
          (Xml.child xml_arg0 "complianceType") in
      let description =
        (Option.map ~f:FrameworkDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        FrameworkName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let frameworkId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "frameworkId") in
      make ~controlSets ?complianceType ?description ~name ~frameworkId ()
    let of_json json =
      let controlSets =
        field_map_exn json "controlSets"
          UpdateAssessmentFrameworkControlSets.of_json in
      let complianceType =
        field_map json "complianceType" ComplianceType.of_json in
      let description =
        field_map json "description" FrameworkDescription.of_json in
      let name = field_map_exn json "name" FrameworkName.of_json in
      let frameworkId = field_map_exn json "frameworkId" UUID.of_json in
      make ~controlSets ?complianceType ?description ~name ~frameworkId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentControlSetStatusResponse =
  struct
    type nonrec t = {
      controlSet: AssessmentControlSet.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?controlSet = fun () -> { controlSet }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("controlSet",
           (Option.map x.controlSet ~f:AssessmentControlSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let controlSet =
        (Option.map ~f:AssessmentControlSet.of_xml)
          (Xml.child xml_arg0 "controlSet") in
      make ?controlSet ()
    let of_json json =
      let controlSet =
        field_map json "controlSet" AssessmentControlSet.of_json in
      make ?controlSet ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentControlSetStatusRequest =
  struct
    type nonrec t =
      {
      assessmentId: UUID.t ;
      controlSetId: String_.t ;
      status: ControlSetStatus.t ;
      comment: DelegationComment.t }
    let context_ = "UpdateAssessmentControlSetStatusRequest"
    let make ~assessmentId =
      fun ~controlSetId ->
        fun ~status ->
          fun ~comment ->
            fun () -> { assessmentId; controlSetId; status; comment }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("controlSetId", (Some (String_.to_value x.controlSetId)));
        ("status", (Some (ControlSetStatus.to_value x.status)));
        ("comment", (Some (DelegationComment.to_value x.comment)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let comment =
        DelegationComment.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "comment") in
      let status =
        ControlSetStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "status") in
      let controlSetId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlSetId") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ~comment ~status ~controlSetId ~assessmentId ()
    let of_json json =
      let comment = field_map_exn json "comment" DelegationComment.of_json in
      let status = field_map_exn json "status" ControlSetStatus.of_json in
      let controlSetId = field_map_exn json "controlSetId" String_.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ~comment ~status ~controlSetId ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentControlResponse =
  struct
    type nonrec t = {
      control: AssessmentControl.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?control = fun () -> { control }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("control", (Option.map x.control ~f:AssessmentControl.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let control =
        (Option.map ~f:AssessmentControl.of_xml)
          (Xml.child xml_arg0 "control") in
      make ?control ()
    let of_json json =
      let control = field_map json "control" AssessmentControl.of_json in
      make ?control ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAssessmentControlRequest =
  struct
    type nonrec t =
      {
      assessmentId: UUID.t ;
      controlSetId: ControlSetId.t ;
      controlId: UUID.t ;
      controlStatus: ControlStatus.t option ;
      commentBody: ControlCommentBody.t option }
    let context_ = "UpdateAssessmentControlRequest"
    let make ?controlStatus =
      fun ?commentBody ->
        fun ~assessmentId ->
          fun ~controlSetId ->
            fun ~controlId ->
              fun () ->
                {
                  controlStatus;
                  commentBody;
                  assessmentId;
                  controlSetId;
                  controlId
                }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("controlSetId", (Some (ControlSetId.to_value x.controlSetId)));
        ("controlId", (Some (UUID.to_value x.controlId)));
        ("controlStatus",
          (Option.map x.controlStatus ~f:ControlStatus.to_value));
        ("commentBody",
          (Option.map x.commentBody ~f:ControlCommentBody.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let commentBody =
        (Option.map ~f:ControlCommentBody.of_xml)
          (Xml.child xml_arg0 "commentBody") in
      let controlStatus =
        (Option.map ~f:ControlStatus.of_xml)
          (Xml.child xml_arg0 "controlStatus") in
      let controlId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "controlId") in
      let controlSetId =
        ControlSetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlSetId") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ?commentBody ?controlStatus ~controlId ~controlSetId ~assessmentId
        ()
    let of_json json =
      let commentBody =
        field_map json "commentBody" ControlCommentBody.of_json in
      let controlStatus =
        field_map json "controlStatus" ControlStatus.of_json in
      let controlId = field_map_exn json "controlId" UUID.of_json in
      let controlSetId =
        field_map_exn json "controlSetId" ControlSetId.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ?commentBody ?controlStatus ~controlId ~controlSetId ~assessmentId
        ()
    let to_json v = composed_to_json to_value v
  end
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module UntagResourceRequest =
  struct
    type nonrec t = {
      resourceArn: AuditManagerArn.t ;
      tagKeys: TagKeyList.t }
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (AuditManagerArn.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        AuditManagerArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_json json =
      let tagKeys = field_map_exn json "tagKeys" TagKeyList.of_json in
      let resourceArn =
        field_map_exn json "resourceArn" AuditManagerArn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module TagResourceRequest =
  struct
    type nonrec t = {
      resourceArn: AuditManagerArn.t ;
      tags: TagMap.t }
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (AuditManagerArn.to_value x.resourceArn)));
        ("tags", (Some (TagMap.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagMap.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        AuditManagerArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_json json =
      let tags = field_map_exn json "tags" TagMap.of_json in
      let resourceArn =
        field_map_exn json "resourceArn" AuditManagerArn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end
module StartAssessmentFrameworkShareResponse =
  struct
    type nonrec t =
      {
      assessmentFrameworkShareRequest:
        AssessmentFrameworkShareRequest.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?assessmentFrameworkShareRequest =
      fun () -> { assessmentFrameworkShareRequest }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("assessmentFrameworkShareRequest",
           (Option.map x.assessmentFrameworkShareRequest
              ~f:AssessmentFrameworkShareRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessmentFrameworkShareRequest =
        (Option.map ~f:AssessmentFrameworkShareRequest.of_xml)
          (Xml.child xml_arg0 "assessmentFrameworkShareRequest") in
      make ?assessmentFrameworkShareRequest ()
    let of_json json =
      let assessmentFrameworkShareRequest =
        field_map json "assessmentFrameworkShareRequest"
          AssessmentFrameworkShareRequest.of_json in
      make ?assessmentFrameworkShareRequest ()
    let to_json v = composed_to_json to_value v
  end
module StartAssessmentFrameworkShareRequest =
  struct
    type nonrec t =
      {
      frameworkId: UUID.t ;
      destinationAccount: AccountId.t ;
      destinationRegion: Region.t ;
      comment: ShareRequestComment.t option }
    let context_ = "StartAssessmentFrameworkShareRequest"
    let make ?comment =
      fun ~frameworkId ->
        fun ~destinationAccount ->
          fun ~destinationRegion ->
            fun () ->
              { comment; frameworkId; destinationAccount; destinationRegion }
    let to_value x =
      structure_to_value
        [("frameworkId", (Some (UUID.to_value x.frameworkId)));
        ("destinationAccount",
          (Some (AccountId.to_value x.destinationAccount)));
        ("destinationRegion", (Some (Region.to_value x.destinationRegion)));
        ("comment", (Option.map x.comment ~f:ShareRequestComment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let comment =
        (Option.map ~f:ShareRequestComment.of_xml)
          (Xml.child xml_arg0 "comment") in
      let destinationRegion =
        Region.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "destinationRegion") in
      let destinationAccount =
        AccountId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "destinationAccount") in
      let frameworkId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "frameworkId") in
      make ?comment ~destinationRegion ~destinationAccount ~frameworkId ()
    let of_json json =
      let comment = field_map json "comment" ShareRequestComment.of_json in
      let destinationRegion =
        field_map_exn json "destinationRegion" Region.of_json in
      let destinationAccount =
        field_map_exn json "destinationAccount" AccountId.of_json in
      let frameworkId = field_map_exn json "frameworkId" UUID.of_json in
      make ?comment ~destinationRegion ~destinationAccount ~frameworkId ()
    let to_json v = composed_to_json to_value v
  end
module RegisterOrganizationAdminAccountResponse =
  struct
    type nonrec t =
      {
      adminAccountId: AccountId.t option ;
      organizationId: OrganizationId.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?adminAccountId =
      fun ?organizationId -> fun () -> { adminAccountId; organizationId }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("adminAccountId",
           (Option.map x.adminAccountId ~f:AccountId.to_value));
        ("organizationId",
          (Option.map x.organizationId ~f:OrganizationId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let organizationId =
        (Option.map ~f:OrganizationId.of_xml)
          (Xml.child xml_arg0 "organizationId") in
      let adminAccountId =
        (Option.map ~f:AccountId.of_xml)
          (Xml.child xml_arg0 "adminAccountId") in
      make ?organizationId ?adminAccountId ()
    let of_json json =
      let organizationId =
        field_map json "organizationId" OrganizationId.of_json in
      let adminAccountId = field_map json "adminAccountId" AccountId.of_json in
      make ?organizationId ?adminAccountId ()
    let to_json v = composed_to_json to_value v
  end
module RegisterOrganizationAdminAccountRequest =
  struct
    type nonrec t = {
      adminAccountId: AccountId.t }
    let context_ = "RegisterOrganizationAdminAccountRequest"
    let make ~adminAccountId = fun () -> { adminAccountId }
    let to_value x =
      structure_to_value
        [("adminAccountId", (Some (AccountId.to_value x.adminAccountId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let adminAccountId =
        AccountId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "adminAccountId") in
      make ~adminAccountId ()
    let of_json json =
      let adminAccountId =
        field_map_exn json "adminAccountId" AccountId.of_json in
      make ~adminAccountId ()
    let to_json v = composed_to_json to_value v
  end
module RegisterAccountResponse =
  struct
    type nonrec t = {
      status: AccountStatus.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:AccountStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:AccountStatus.of_xml) (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_json json =
      let status = field_map json "status" AccountStatus.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end
module RegisterAccountRequest =
  struct
    type nonrec t =
      {
      kmsKey: KmsKey.t option ;
      delegatedAdminAccount: AccountId.t option }
    let make ?kmsKey =
      fun ?delegatedAdminAccount ->
        fun () -> { kmsKey; delegatedAdminAccount }
    let to_value x =
      structure_to_value
        [("kmsKey", (Option.map x.kmsKey ~f:KmsKey.to_value));
        ("delegatedAdminAccount",
          (Option.map x.delegatedAdminAccount ~f:AccountId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let delegatedAdminAccount =
        (Option.map ~f:AccountId.of_xml)
          (Xml.child xml_arg0 "delegatedAdminAccount") in
      let kmsKey =
        (Option.map ~f:KmsKey.of_xml) (Xml.child xml_arg0 "kmsKey") in
      make ?delegatedAdminAccount ?kmsKey ()
    let of_json json =
      let delegatedAdminAccount =
        field_map json "delegatedAdminAccount" AccountId.of_json in
      let kmsKey = field_map json "kmsKey" KmsKey.of_json in
      make ?delegatedAdminAccount ?kmsKey ()
    let to_json v = composed_to_json to_value v
  end
module ListTagsForResourceResponse =
  struct
    type nonrec t = {
      tags: TagMap.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_json json =
      let tags = field_map json "tags" TagMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end
module ListTagsForResourceRequest =
  struct
    type nonrec t = {
      resourceArn: AuditManagerArn.t }
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (AuditManagerArn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        AuditManagerArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_json json =
      let resourceArn =
        field_map_exn json "resourceArn" AuditManagerArn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end
module ListNotificationsResponse =
  struct
    type nonrec t =
      {
      notifications: Notifications.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?notifications =
      fun ?nextToken -> fun () -> { notifications; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("notifications",
           (Option.map x.notifications ~f:Notifications.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let notifications =
        (Option.map ~f:Notifications.of_xml)
          (Xml.child xml_arg0 "notifications") in
      make ?nextToken ?notifications ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let notifications =
        field_map json "notifications" Notifications.of_json in
      make ?nextToken ?notifications ()
    let to_json v = composed_to_json to_value v
  end
module ListNotificationsRequest =
  struct
    type nonrec t =
      {
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module ListKeywordsForDataSourceResponse =
  struct
    type nonrec t = {
      keywords: Keywords.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?keywords = fun ?nextToken -> fun () -> { keywords; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("keywords", (Option.map x.keywords ~f:Keywords.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let keywords =
        (Option.map ~f:Keywords.of_xml) (Xml.child xml_arg0 "keywords") in
      make ?nextToken ?keywords ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let keywords = field_map json "keywords" Keywords.of_json in
      make ?nextToken ?keywords ()
    let to_json v = composed_to_json to_value v
  end
module ListKeywordsForDataSourceRequest =
  struct
    type nonrec t =
      {
      source: SourceType.t ;
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let context_ = "ListKeywordsForDataSourceRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~source -> fun () -> { nextToken; maxResults; source }
    let to_value x =
      structure_to_value
        [("source", (Some (SourceType.to_value x.source)));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let source =
        SourceType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "source") in
      make ?maxResults ?nextToken ~source ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      let source = field_map_exn json "source" SourceType.of_json in
      make ?maxResults ?nextToken ~source ()
    let to_json v = composed_to_json to_value v
  end
module ListControlsResponse =
  struct
    type nonrec t =
      {
      controlMetadataList: ControlMetadataList.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?controlMetadataList =
      fun ?nextToken -> fun () -> { controlMetadataList; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("controlMetadataList",
           (Option.map x.controlMetadataList ~f:ControlMetadataList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let controlMetadataList =
        (Option.map ~f:ControlMetadataList.of_xml)
          (Xml.child xml_arg0 "controlMetadataList") in
      make ?nextToken ?controlMetadataList ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let controlMetadataList =
        field_map json "controlMetadataList" ControlMetadataList.of_json in
      make ?nextToken ?controlMetadataList ()
    let to_json v = composed_to_json to_value v
  end
module ListControlsRequest =
  struct
    type nonrec t =
      {
      controlType: ControlType.t ;
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let context_ = "ListControlsRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~controlType -> fun () -> { nextToken; maxResults; controlType }
    let to_value x =
      structure_to_value
        [("controlType", (Some (ControlType.to_value x.controlType)));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let controlType =
        ControlType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlType") in
      make ?maxResults ?nextToken ~controlType ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      let controlType = field_map_exn json "controlType" ControlType.of_json in
      make ?maxResults ?nextToken ~controlType ()
    let to_json v = composed_to_json to_value v
  end
module ListControlInsightsByControlDomainResponse =
  struct
    type nonrec t =
      {
      controlInsightsMetadata: ControlInsightsMetadata.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?controlInsightsMetadata =
      fun ?nextToken -> fun () -> { controlInsightsMetadata; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("controlInsightsMetadata",
           (Option.map x.controlInsightsMetadata
              ~f:ControlInsightsMetadata.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let controlInsightsMetadata =
        (Option.map ~f:ControlInsightsMetadata.of_xml)
          (Xml.child xml_arg0 "controlInsightsMetadata") in
      make ?nextToken ?controlInsightsMetadata ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let controlInsightsMetadata =
        field_map json "controlInsightsMetadata"
          ControlInsightsMetadata.of_json in
      make ?nextToken ?controlInsightsMetadata ()
    let to_json v = composed_to_json to_value v
  end
module ListControlInsightsByControlDomainRequest =
  struct
    type nonrec t =
      {
      controlDomainId: UUID.t ;
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let context_ = "ListControlInsightsByControlDomainRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~controlDomainId ->
          fun () -> { nextToken; maxResults; controlDomainId }
    let to_value x =
      structure_to_value
        [("controlDomainId", (Some (UUID.to_value x.controlDomainId)));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let controlDomainId =
        UUID.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlDomainId") in
      make ?maxResults ?nextToken ~controlDomainId ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      let controlDomainId = field_map_exn json "controlDomainId" UUID.of_json in
      make ?maxResults ?nextToken ~controlDomainId ()
    let to_json v = composed_to_json to_value v
  end
module ListControlDomainInsightsResponse =
  struct
    type nonrec t =
      {
      controlDomainInsights: ControlDomainInsightsList.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?controlDomainInsights =
      fun ?nextToken -> fun () -> { controlDomainInsights; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("controlDomainInsights",
           (Option.map x.controlDomainInsights
              ~f:ControlDomainInsightsList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let controlDomainInsights =
        (Option.map ~f:ControlDomainInsightsList.of_xml)
          (Xml.child xml_arg0 "controlDomainInsights") in
      make ?nextToken ?controlDomainInsights ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let controlDomainInsights =
        field_map json "controlDomainInsights"
          ControlDomainInsightsList.of_json in
      make ?nextToken ?controlDomainInsights ()
    let to_json v = composed_to_json to_value v
  end
module ListControlDomainInsightsRequest =
  struct
    type nonrec t =
      {
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module ListControlDomainInsightsByAssessmentResponse =
  struct
    type nonrec t =
      {
      controlDomainInsights: ControlDomainInsightsList.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?controlDomainInsights =
      fun ?nextToken -> fun () -> { controlDomainInsights; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("controlDomainInsights",
           (Option.map x.controlDomainInsights
              ~f:ControlDomainInsightsList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let controlDomainInsights =
        (Option.map ~f:ControlDomainInsightsList.of_xml)
          (Xml.child xml_arg0 "controlDomainInsights") in
      make ?nextToken ?controlDomainInsights ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let controlDomainInsights =
        field_map json "controlDomainInsights"
          ControlDomainInsightsList.of_json in
      make ?nextToken ?controlDomainInsights ()
    let to_json v = composed_to_json to_value v
  end
module ListControlDomainInsightsByAssessmentRequest =
  struct
    type nonrec t =
      {
      assessmentId: UUID.t ;
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let context_ = "ListControlDomainInsightsByAssessmentRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~assessmentId ->
          fun () -> { nextToken; maxResults; assessmentId }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ?maxResults ?nextToken ~assessmentId ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ?maxResults ?nextToken ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module ListAssessmentsResponse =
  struct
    type nonrec t =
      {
      assessmentMetadata: ListAssessmentMetadata.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?assessmentMetadata =
      fun ?nextToken -> fun () -> { assessmentMetadata; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("assessmentMetadata",
           (Option.map x.assessmentMetadata
              ~f:ListAssessmentMetadata.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let assessmentMetadata =
        (Option.map ~f:ListAssessmentMetadata.of_xml)
          (Xml.child xml_arg0 "assessmentMetadata") in
      make ?nextToken ?assessmentMetadata ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let assessmentMetadata =
        field_map json "assessmentMetadata" ListAssessmentMetadata.of_json in
      make ?nextToken ?assessmentMetadata ()
    let to_json v = composed_to_json to_value v
  end
module ListAssessmentsRequest =
  struct
    type nonrec t =
      {
      status: AssessmentStatus.t option ;
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let make ?status =
      fun ?nextToken ->
        fun ?maxResults -> fun () -> { status; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:AssessmentStatus.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let status =
        (Option.map ~f:AssessmentStatus.of_xml) (Xml.child xml_arg0 "status") in
      make ?maxResults ?nextToken ?status ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      let status = field_map json "status" AssessmentStatus.of_json in
      make ?maxResults ?nextToken ?status ()
    let to_json v = composed_to_json to_value v
  end
module ListAssessmentReportsResponse =
  struct
    type nonrec t =
      {
      assessmentReports: AssessmentReportsMetadata.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?assessmentReports =
      fun ?nextToken -> fun () -> { assessmentReports; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("assessmentReports",
           (Option.map x.assessmentReports
              ~f:AssessmentReportsMetadata.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let assessmentReports =
        (Option.map ~f:AssessmentReportsMetadata.of_xml)
          (Xml.child xml_arg0 "assessmentReports") in
      make ?nextToken ?assessmentReports ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let assessmentReports =
        field_map json "assessmentReports" AssessmentReportsMetadata.of_json in
      make ?nextToken ?assessmentReports ()
    let to_json v = composed_to_json to_value v
  end
module ListAssessmentReportsRequest =
  struct
    type nonrec t =
      {
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module ListAssessmentFrameworksResponse =
  struct
    type nonrec t =
      {
      frameworkMetadataList: FrameworkMetadataList.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?frameworkMetadataList =
      fun ?nextToken -> fun () -> { frameworkMetadataList; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("frameworkMetadataList",
           (Option.map x.frameworkMetadataList
              ~f:FrameworkMetadataList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let frameworkMetadataList =
        (Option.map ~f:FrameworkMetadataList.of_xml)
          (Xml.child xml_arg0 "frameworkMetadataList") in
      make ?nextToken ?frameworkMetadataList ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let frameworkMetadataList =
        field_map json "frameworkMetadataList" FrameworkMetadataList.of_json in
      make ?nextToken ?frameworkMetadataList ()
    let to_json v = composed_to_json to_value v
  end
module ListAssessmentFrameworksRequest =
  struct
    type nonrec t =
      {
      frameworkType: FrameworkType.t ;
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let context_ = "ListAssessmentFrameworksRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~frameworkType ->
          fun () -> { nextToken; maxResults; frameworkType }
    let to_value x =
      structure_to_value
        [("frameworkType", (Some (FrameworkType.to_value x.frameworkType)));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let frameworkType =
        FrameworkType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "frameworkType") in
      make ?maxResults ?nextToken ~frameworkType ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      let frameworkType =
        field_map_exn json "frameworkType" FrameworkType.of_json in
      make ?maxResults ?nextToken ~frameworkType ()
    let to_json v = composed_to_json to_value v
  end
module ListAssessmentFrameworkShareRequestsResponse =
  struct
    type nonrec t =
      {
      assessmentFrameworkShareRequests:
        AssessmentFrameworkShareRequestList.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?assessmentFrameworkShareRequests =
      fun ?nextToken ->
        fun () -> { assessmentFrameworkShareRequests; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("assessmentFrameworkShareRequests",
           (Option.map x.assessmentFrameworkShareRequests
              ~f:AssessmentFrameworkShareRequestList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let assessmentFrameworkShareRequests =
        (Option.map ~f:AssessmentFrameworkShareRequestList.of_xml)
          (Xml.child xml_arg0 "assessmentFrameworkShareRequests") in
      make ?nextToken ?assessmentFrameworkShareRequests ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let assessmentFrameworkShareRequests =
        field_map json "assessmentFrameworkShareRequests"
          AssessmentFrameworkShareRequestList.of_json in
      make ?nextToken ?assessmentFrameworkShareRequests ()
    let to_json v = composed_to_json to_value v
  end
module ListAssessmentFrameworkShareRequestsRequest =
  struct
    type nonrec t =
      {
      requestType: ShareRequestType.t ;
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let context_ = "ListAssessmentFrameworkShareRequestsRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~requestType -> fun () -> { nextToken; maxResults; requestType }
    let to_value x =
      structure_to_value
        [("requestType", (Some (ShareRequestType.to_value x.requestType)));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let requestType =
        ShareRequestType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "requestType") in
      make ?maxResults ?nextToken ~requestType ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      let requestType =
        field_map_exn json "requestType" ShareRequestType.of_json in
      make ?maxResults ?nextToken ~requestType ()
    let to_json v = composed_to_json to_value v
  end
module ListAssessmentControlInsightsByControlDomainResponse =
  struct
    type nonrec t =
      {
      controlInsightsByAssessment:
        ControlInsightsMetadataByAssessment.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?controlInsightsByAssessment =
      fun ?nextToken -> fun () -> { controlInsightsByAssessment; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("controlInsightsByAssessment",
           (Option.map x.controlInsightsByAssessment
              ~f:ControlInsightsMetadataByAssessment.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let controlInsightsByAssessment =
        (Option.map ~f:ControlInsightsMetadataByAssessment.of_xml)
          (Xml.child xml_arg0 "controlInsightsByAssessment") in
      make ?nextToken ?controlInsightsByAssessment ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let controlInsightsByAssessment =
        field_map json "controlInsightsByAssessment"
          ControlInsightsMetadataByAssessment.of_json in
      make ?nextToken ?controlInsightsByAssessment ()
    let to_json v = composed_to_json to_value v
  end
module ListAssessmentControlInsightsByControlDomainRequest =
  struct
    type nonrec t =
      {
      controlDomainId: UUID.t ;
      assessmentId: UUID.t ;
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let context_ = "ListAssessmentControlInsightsByControlDomainRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~controlDomainId ->
          fun ~assessmentId ->
            fun () ->
              { nextToken; maxResults; controlDomainId; assessmentId }
    let to_value x =
      structure_to_value
        [("controlDomainId", (Some (UUID.to_value x.controlDomainId)));
        ("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      let controlDomainId =
        UUID.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlDomainId") in
      make ?maxResults ?nextToken ~assessmentId ~controlDomainId ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      let controlDomainId = field_map_exn json "controlDomainId" UUID.of_json in
      make ?maxResults ?nextToken ~assessmentId ~controlDomainId ()
    let to_json v = composed_to_json to_value v
  end
module GetSettingsResponse =
  struct
    type nonrec t = {
      settings: Settings.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?settings = fun () -> { settings }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("settings", (Option.map x.settings ~f:Settings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let settings =
        (Option.map ~f:Settings.of_xml) (Xml.child xml_arg0 "settings") in
      make ?settings ()
    let of_json json =
      let settings = field_map json "settings" Settings.of_json in
      make ?settings ()
    let to_json v = composed_to_json to_value v
  end
module GetSettingsRequest =
  struct
    type nonrec t = {
      attribute: SettingAttribute.t }
    let context_ = "GetSettingsRequest"
    let make ~attribute = fun () -> { attribute }
    let to_value x =
      structure_to_value
        [("attribute", (Some (SettingAttribute.to_value x.attribute)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attribute =
        SettingAttribute.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "attribute") in
      make ~attribute ()
    let of_json json =
      let attribute = field_map_exn json "attribute" SettingAttribute.of_json in
      make ~attribute ()
    let to_json v = composed_to_json to_value v
  end
module GetServicesInScopeResponse =
  struct
    type nonrec t = {
      serviceMetadata: ServiceMetadataList.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?serviceMetadata = fun () -> { serviceMetadata }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("serviceMetadata",
           (Option.map x.serviceMetadata ~f:ServiceMetadataList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceMetadata =
        (Option.map ~f:ServiceMetadataList.of_xml)
          (Xml.child xml_arg0 "serviceMetadata") in
      make ?serviceMetadata ()
    let of_json json =
      let serviceMetadata =
        field_map json "serviceMetadata" ServiceMetadataList.of_json in
      make ?serviceMetadata ()
    let to_json v = composed_to_json to_value v
  end
module GetServicesInScopeRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module GetOrganizationAdminAccountResponse =
  struct
    type nonrec t =
      {
      adminAccountId: AccountId.t option ;
      organizationId: OrganizationId.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?adminAccountId =
      fun ?organizationId -> fun () -> { adminAccountId; organizationId }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("adminAccountId",
           (Option.map x.adminAccountId ~f:AccountId.to_value));
        ("organizationId",
          (Option.map x.organizationId ~f:OrganizationId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let organizationId =
        (Option.map ~f:OrganizationId.of_xml)
          (Xml.child xml_arg0 "organizationId") in
      let adminAccountId =
        (Option.map ~f:AccountId.of_xml)
          (Xml.child xml_arg0 "adminAccountId") in
      make ?organizationId ?adminAccountId ()
    let of_json json =
      let organizationId =
        field_map json "organizationId" OrganizationId.of_json in
      let adminAccountId = field_map json "adminAccountId" AccountId.of_json in
      make ?organizationId ?adminAccountId ()
    let to_json v = composed_to_json to_value v
  end
module GetOrganizationAdminAccountRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module GetInsightsResponse =
  struct
    type nonrec t = {
      insights: Insights.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?insights = fun () -> { insights }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("insights", (Option.map x.insights ~f:Insights.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let insights =
        (Option.map ~f:Insights.of_xml) (Xml.child xml_arg0 "insights") in
      make ?insights ()
    let of_json json =
      let insights = field_map json "insights" Insights.of_json in
      make ?insights ()
    let to_json v = composed_to_json to_value v
  end
module GetInsightsRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module GetInsightsByAssessmentResponse =
  struct
    type nonrec t = {
      insights: InsightsByAssessment.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?insights = fun () -> { insights }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("insights",
           (Option.map x.insights ~f:InsightsByAssessment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let insights =
        (Option.map ~f:InsightsByAssessment.of_xml)
          (Xml.child xml_arg0 "insights") in
      make ?insights ()
    let of_json json =
      let insights = field_map json "insights" InsightsByAssessment.of_json in
      make ?insights ()
    let to_json v = composed_to_json to_value v
  end
module GetInsightsByAssessmentRequest =
  struct
    type nonrec t = {
      assessmentId: UUID.t }
    let context_ = "GetInsightsByAssessmentRequest"
    let make ~assessmentId = fun () -> { assessmentId }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ~assessmentId ()
    let of_json json =
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module GetEvidenceResponse =
  struct
    type nonrec t = {
      evidence: Evidence.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?evidence = fun () -> { evidence }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("evidence", (Option.map x.evidence ~f:Evidence.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let evidence =
        (Option.map ~f:Evidence.of_xml) (Xml.child xml_arg0 "evidence") in
      make ?evidence ()
    let of_json json =
      let evidence = field_map json "evidence" Evidence.of_json in
      make ?evidence ()
    let to_json v = composed_to_json to_value v
  end
module GetEvidenceRequest =
  struct
    type nonrec t =
      {
      assessmentId: UUID.t ;
      controlSetId: ControlSetId.t ;
      evidenceFolderId: UUID.t ;
      evidenceId: UUID.t }
    let context_ = "GetEvidenceRequest"
    let make ~assessmentId =
      fun ~controlSetId ->
        fun ~evidenceFolderId ->
          fun ~evidenceId ->
            fun () ->
              { assessmentId; controlSetId; evidenceFolderId; evidenceId }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("controlSetId", (Some (ControlSetId.to_value x.controlSetId)));
        ("evidenceFolderId", (Some (UUID.to_value x.evidenceFolderId)));
        ("evidenceId", (Some (UUID.to_value x.evidenceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let evidenceId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "evidenceId") in
      let evidenceFolderId =
        UUID.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "evidenceFolderId") in
      let controlSetId =
        ControlSetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlSetId") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ~evidenceId ~evidenceFolderId ~controlSetId ~assessmentId ()
    let of_json json =
      let evidenceId = field_map_exn json "evidenceId" UUID.of_json in
      let evidenceFolderId =
        field_map_exn json "evidenceFolderId" UUID.of_json in
      let controlSetId =
        field_map_exn json "controlSetId" ControlSetId.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ~evidenceId ~evidenceFolderId ~controlSetId ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module GetEvidenceFoldersByAssessmentResponse =
  struct
    type nonrec t =
      {
      evidenceFolders: AssessmentEvidenceFolders.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?evidenceFolders =
      fun ?nextToken -> fun () -> { evidenceFolders; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("evidenceFolders",
           (Option.map x.evidenceFolders
              ~f:AssessmentEvidenceFolders.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let evidenceFolders =
        (Option.map ~f:AssessmentEvidenceFolders.of_xml)
          (Xml.child xml_arg0 "evidenceFolders") in
      make ?nextToken ?evidenceFolders ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let evidenceFolders =
        field_map json "evidenceFolders" AssessmentEvidenceFolders.of_json in
      make ?nextToken ?evidenceFolders ()
    let to_json v = composed_to_json to_value v
  end
module GetEvidenceFoldersByAssessmentRequest =
  struct
    type nonrec t =
      {
      assessmentId: UUID.t ;
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let context_ = "GetEvidenceFoldersByAssessmentRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~assessmentId ->
          fun () -> { nextToken; maxResults; assessmentId }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ?maxResults ?nextToken ~assessmentId ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ?maxResults ?nextToken ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module GetEvidenceFoldersByAssessmentControlResponse =
  struct
    type nonrec t =
      {
      evidenceFolders: AssessmentEvidenceFolders.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?evidenceFolders =
      fun ?nextToken -> fun () -> { evidenceFolders; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("evidenceFolders",
           (Option.map x.evidenceFolders
              ~f:AssessmentEvidenceFolders.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let evidenceFolders =
        (Option.map ~f:AssessmentEvidenceFolders.of_xml)
          (Xml.child xml_arg0 "evidenceFolders") in
      make ?nextToken ?evidenceFolders ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let evidenceFolders =
        field_map json "evidenceFolders" AssessmentEvidenceFolders.of_json in
      make ?nextToken ?evidenceFolders ()
    let to_json v = composed_to_json to_value v
  end
module GetEvidenceFoldersByAssessmentControlRequest =
  struct
    type nonrec t =
      {
      assessmentId: UUID.t ;
      controlSetId: ControlSetId.t ;
      controlId: UUID.t ;
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let context_ = "GetEvidenceFoldersByAssessmentControlRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~assessmentId ->
          fun ~controlSetId ->
            fun ~controlId ->
              fun () ->
                {
                  nextToken;
                  maxResults;
                  assessmentId;
                  controlSetId;
                  controlId
                }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("controlSetId", (Some (ControlSetId.to_value x.controlSetId)));
        ("controlId", (Some (UUID.to_value x.controlId)));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let controlId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "controlId") in
      let controlSetId =
        ControlSetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlSetId") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ?maxResults ?nextToken ~controlId ~controlSetId ~assessmentId ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      let controlId = field_map_exn json "controlId" UUID.of_json in
      let controlSetId =
        field_map_exn json "controlSetId" ControlSetId.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ?maxResults ?nextToken ~controlId ~controlSetId ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module GetEvidenceFolderResponse =
  struct
    type nonrec t = {
      evidenceFolder: AssessmentEvidenceFolder.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?evidenceFolder = fun () -> { evidenceFolder }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("evidenceFolder",
           (Option.map x.evidenceFolder ~f:AssessmentEvidenceFolder.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let evidenceFolder =
        (Option.map ~f:AssessmentEvidenceFolder.of_xml)
          (Xml.child xml_arg0 "evidenceFolder") in
      make ?evidenceFolder ()
    let of_json json =
      let evidenceFolder =
        field_map json "evidenceFolder" AssessmentEvidenceFolder.of_json in
      make ?evidenceFolder ()
    let to_json v = composed_to_json to_value v
  end
module GetEvidenceFolderRequest =
  struct
    type nonrec t =
      {
      assessmentId: UUID.t ;
      controlSetId: ControlSetId.t ;
      evidenceFolderId: UUID.t }
    let context_ = "GetEvidenceFolderRequest"
    let make ~assessmentId =
      fun ~controlSetId ->
        fun ~evidenceFolderId ->
          fun () -> { assessmentId; controlSetId; evidenceFolderId }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("controlSetId", (Some (ControlSetId.to_value x.controlSetId)));
        ("evidenceFolderId", (Some (UUID.to_value x.evidenceFolderId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let evidenceFolderId =
        UUID.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "evidenceFolderId") in
      let controlSetId =
        ControlSetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlSetId") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ~evidenceFolderId ~controlSetId ~assessmentId ()
    let of_json json =
      let evidenceFolderId =
        field_map_exn json "evidenceFolderId" UUID.of_json in
      let controlSetId =
        field_map_exn json "controlSetId" ControlSetId.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ~evidenceFolderId ~controlSetId ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module GetEvidenceByEvidenceFolderResponse =
  struct
    type nonrec t =
      {
      evidence: EvidenceList.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?evidence = fun ?nextToken -> fun () -> { evidence; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("evidence", (Option.map x.evidence ~f:EvidenceList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let evidence =
        (Option.map ~f:EvidenceList.of_xml) (Xml.child xml_arg0 "evidence") in
      make ?nextToken ?evidence ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let evidence = field_map json "evidence" EvidenceList.of_json in
      make ?nextToken ?evidence ()
    let to_json v = composed_to_json to_value v
  end
module GetEvidenceByEvidenceFolderRequest =
  struct
    type nonrec t =
      {
      assessmentId: UUID.t ;
      controlSetId: ControlSetId.t ;
      evidenceFolderId: UUID.t ;
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let context_ = "GetEvidenceByEvidenceFolderRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~assessmentId ->
          fun ~controlSetId ->
            fun ~evidenceFolderId ->
              fun () ->
                {
                  nextToken;
                  maxResults;
                  assessmentId;
                  controlSetId;
                  evidenceFolderId
                }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("controlSetId", (Some (ControlSetId.to_value x.controlSetId)));
        ("evidenceFolderId", (Some (UUID.to_value x.evidenceFolderId)));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let evidenceFolderId =
        UUID.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "evidenceFolderId") in
      let controlSetId =
        ControlSetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlSetId") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ?maxResults ?nextToken ~evidenceFolderId ~controlSetId
        ~assessmentId ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      let evidenceFolderId =
        field_map_exn json "evidenceFolderId" UUID.of_json in
      let controlSetId =
        field_map_exn json "controlSetId" ControlSetId.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ?maxResults ?nextToken ~evidenceFolderId ~controlSetId
        ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module GetDelegationsResponse =
  struct
    type nonrec t =
      {
      delegations: DelegationMetadataList.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?delegations =
      fun ?nextToken -> fun () -> { delegations; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("delegations",
           (Option.map x.delegations ~f:DelegationMetadataList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let delegations =
        (Option.map ~f:DelegationMetadataList.of_xml)
          (Xml.child xml_arg0 "delegations") in
      make ?nextToken ?delegations ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let delegations =
        field_map json "delegations" DelegationMetadataList.of_json in
      make ?nextToken ?delegations ()
    let to_json v = composed_to_json to_value v
  end
module GetDelegationsRequest =
  struct
    type nonrec t =
      {
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module GetControlResponse =
  struct
    type nonrec t = {
      control: Control.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?control = fun () -> { control }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("control", (Option.map x.control ~f:Control.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let control =
        (Option.map ~f:Control.of_xml) (Xml.child xml_arg0 "control") in
      make ?control ()
    let of_json json =
      let control = field_map json "control" Control.of_json in
      make ?control ()
    let to_json v = composed_to_json to_value v
  end
module GetControlRequest =
  struct
    type nonrec t = {
      controlId: UUID.t }
    let context_ = "GetControlRequest"
    let make ~controlId = fun () -> { controlId }
    let to_value x =
      structure_to_value [("controlId", (Some (UUID.to_value x.controlId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let controlId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "controlId") in
      make ~controlId ()
    let of_json json =
      let controlId = field_map_exn json "controlId" UUID.of_json in
      make ~controlId ()
    let to_json v = composed_to_json to_value v
  end
module GetChangeLogsResponse =
  struct
    type nonrec t =
      {
      changeLogs: ChangeLogs.t option ;
      nextToken: Token.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?changeLogs =
      fun ?nextToken -> fun () -> { changeLogs; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("changeLogs", (Option.map x.changeLogs ~f:ChangeLogs.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let changeLogs =
        (Option.map ~f:ChangeLogs.of_xml) (Xml.child xml_arg0 "changeLogs") in
      make ?nextToken ?changeLogs ()
    let of_json json =
      let nextToken = field_map json "nextToken" Token.of_json in
      let changeLogs = field_map json "changeLogs" ChangeLogs.of_json in
      make ?nextToken ?changeLogs ()
    let to_json v = composed_to_json to_value v
  end
module GetChangeLogsRequest =
  struct
    type nonrec t =
      {
      assessmentId: UUID.t ;
      controlSetId: ControlSetId.t option ;
      controlId: UUID.t option ;
      nextToken: Token.t option ;
      maxResults: MaxResults.t option }
    let context_ = "GetChangeLogsRequest"
    let make ?controlSetId =
      fun ?controlId ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ~assessmentId ->
              fun () ->
                {
                  controlSetId;
                  controlId;
                  nextToken;
                  maxResults;
                  assessmentId
                }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("controlSetId",
          (Option.map x.controlSetId ~f:ControlSetId.to_value));
        ("controlId", (Option.map x.controlId ~f:UUID.to_value));
        ("nextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "nextToken") in
      let controlId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "controlId") in
      let controlSetId =
        (Option.map ~f:ControlSetId.of_xml)
          (Xml.child xml_arg0 "controlSetId") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ?maxResults ?nextToken ?controlId ?controlSetId ~assessmentId ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" Token.of_json in
      let controlId = field_map json "controlId" UUID.of_json in
      let controlSetId = field_map json "controlSetId" ControlSetId.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ?maxResults ?nextToken ?controlId ?controlSetId ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module GetAssessmentResponse =
  struct
    type nonrec t =
      {
      assessment: Assessment.t option ;
      userRole: Role.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?assessment =
      fun ?userRole -> fun () -> { assessment; userRole }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("assessment", (Option.map x.assessment ~f:Assessment.to_value));
        ("userRole", (Option.map x.userRole ~f:Role.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userRole =
        (Option.map ~f:Role.of_xml) (Xml.child xml_arg0 "userRole") in
      let assessment =
        (Option.map ~f:Assessment.of_xml) (Xml.child xml_arg0 "assessment") in
      make ?userRole ?assessment ()
    let of_json json =
      let userRole = field_map json "userRole" Role.of_json in
      let assessment = field_map json "assessment" Assessment.of_json in
      make ?userRole ?assessment ()
    let to_json v = composed_to_json to_value v
  end
module GetAssessmentRequest =
  struct
    type nonrec t = {
      assessmentId: UUID.t }
    let context_ = "GetAssessmentRequest"
    let make ~assessmentId = fun () -> { assessmentId }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ~assessmentId ()
    let of_json json =
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module GetAssessmentReportUrlResponse =
  struct
    type nonrec t = {
      preSignedUrl: URL.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?preSignedUrl = fun () -> { preSignedUrl }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("preSignedUrl", (Option.map x.preSignedUrl ~f:URL.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let preSignedUrl =
        (Option.map ~f:URL.of_xml) (Xml.child xml_arg0 "preSignedUrl") in
      make ?preSignedUrl ()
    let of_json json =
      let preSignedUrl = field_map json "preSignedUrl" URL.of_json in
      make ?preSignedUrl ()
    let to_json v = composed_to_json to_value v
  end
module GetAssessmentReportUrlRequest =
  struct
    type nonrec t = {
      assessmentReportId: UUID.t ;
      assessmentId: UUID.t }
    let context_ = "GetAssessmentReportUrlRequest"
    let make ~assessmentReportId =
      fun ~assessmentId -> fun () -> { assessmentReportId; assessmentId }
    let to_value x =
      structure_to_value
        [("assessmentReportId", (Some (UUID.to_value x.assessmentReportId)));
        ("assessmentId", (Some (UUID.to_value x.assessmentId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      let assessmentReportId =
        UUID.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "assessmentReportId") in
      make ~assessmentId ~assessmentReportId ()
    let of_json json =
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      let assessmentReportId =
        field_map_exn json "assessmentReportId" UUID.of_json in
      make ~assessmentId ~assessmentReportId ()
    let to_json v = composed_to_json to_value v
  end
module GetAssessmentFrameworkResponse =
  struct
    type nonrec t = {
      framework: Framework.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?framework = fun () -> { framework }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("framework", (Option.map x.framework ~f:Framework.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let framework =
        (Option.map ~f:Framework.of_xml) (Xml.child xml_arg0 "framework") in
      make ?framework ()
    let of_json json =
      let framework = field_map json "framework" Framework.of_json in
      make ?framework ()
    let to_json v = composed_to_json to_value v
  end
module GetAssessmentFrameworkRequest =
  struct
    type nonrec t = {
      frameworkId: UUID.t }
    let context_ = "GetAssessmentFrameworkRequest"
    let make ~frameworkId = fun () -> { frameworkId }
    let to_value x =
      structure_to_value
        [("frameworkId", (Some (UUID.to_value x.frameworkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let frameworkId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "frameworkId") in
      make ~frameworkId ()
    let of_json json =
      let frameworkId = field_map_exn json "frameworkId" UUID.of_json in
      make ~frameworkId ()
    let to_json v = composed_to_json to_value v
  end
module GetAccountStatusResponse =
  struct
    type nonrec t = {
      status: AccountStatus.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:AccountStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:AccountStatus.of_xml) (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_json json =
      let status = field_map json "status" AccountStatus.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end
module GetAccountStatusRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateAssessmentReportEvidenceFolderResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateAssessmentReportEvidenceFolderRequest =
  struct
    type nonrec t = {
      assessmentId: UUID.t ;
      evidenceFolderId: UUID.t }
    let context_ = "DisassociateAssessmentReportEvidenceFolderRequest"
    let make ~assessmentId =
      fun ~evidenceFolderId -> fun () -> { assessmentId; evidenceFolderId }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("evidenceFolderId", (Some (UUID.to_value x.evidenceFolderId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let evidenceFolderId =
        UUID.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "evidenceFolderId") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ~evidenceFolderId ~assessmentId ()
    let of_json json =
      let evidenceFolderId =
        field_map_exn json "evidenceFolderId" UUID.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ~evidenceFolderId ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module DeregisterOrganizationAdminAccountResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeregisterOrganizationAdminAccountRequest =
  struct
    type nonrec t = {
      adminAccountId: AccountId.t option }
    let make ?adminAccountId = fun () -> { adminAccountId }
    let to_value x =
      structure_to_value
        [("adminAccountId",
           (Option.map x.adminAccountId ~f:AccountId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let adminAccountId =
        (Option.map ~f:AccountId.of_xml)
          (Xml.child xml_arg0 "adminAccountId") in
      make ?adminAccountId ()
    let of_json json =
      let adminAccountId = field_map json "adminAccountId" AccountId.of_json in
      make ?adminAccountId ()
    let to_json v = composed_to_json to_value v
  end
module DeregisterAccountResponse =
  struct
    type nonrec t = {
      status: AccountStatus.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:AccountStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:AccountStatus.of_xml) (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_json json =
      let status = field_map json "status" AccountStatus.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end
module DeregisterAccountRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteControlResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteControlRequest =
  struct
    type nonrec t = {
      controlId: UUID.t }
    let context_ = "DeleteControlRequest"
    let make ~controlId = fun () -> { controlId }
    let to_value x =
      structure_to_value [("controlId", (Some (UUID.to_value x.controlId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let controlId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "controlId") in
      make ~controlId ()
    let of_json json =
      let controlId = field_map_exn json "controlId" UUID.of_json in
      make ~controlId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteAssessmentResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteAssessmentRequest =
  struct
    type nonrec t = {
      assessmentId: UUID.t }
    let context_ = "DeleteAssessmentRequest"
    let make ~assessmentId = fun () -> { assessmentId }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ~assessmentId ()
    let of_json json =
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteAssessmentReportResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteAssessmentReportRequest =
  struct
    type nonrec t = {
      assessmentId: UUID.t ;
      assessmentReportId: UUID.t }
    let context_ = "DeleteAssessmentReportRequest"
    let make ~assessmentId =
      fun ~assessmentReportId ->
        fun () -> { assessmentId; assessmentReportId }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("assessmentReportId", (Some (UUID.to_value x.assessmentReportId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessmentReportId =
        UUID.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "assessmentReportId") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ~assessmentReportId ~assessmentId ()
    let of_json json =
      let assessmentReportId =
        field_map_exn json "assessmentReportId" UUID.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ~assessmentReportId ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteAssessmentFrameworkShareResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteAssessmentFrameworkShareRequest =
  struct
    type nonrec t = {
      requestId: UUID.t ;
      requestType: ShareRequestType.t }
    let context_ = "DeleteAssessmentFrameworkShareRequest"
    let make ~requestId =
      fun ~requestType -> fun () -> { requestId; requestType }
    let to_value x =
      structure_to_value
        [("requestId", (Some (UUID.to_value x.requestId)));
        ("requestType", (Some (ShareRequestType.to_value x.requestType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let requestType =
        ShareRequestType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "requestType") in
      let requestId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "requestId") in
      make ~requestType ~requestId ()
    let of_json json =
      let requestType =
        field_map_exn json "requestType" ShareRequestType.of_json in
      let requestId = field_map_exn json "requestId" UUID.of_json in
      make ~requestType ~requestId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteAssessmentFrameworkResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteAssessmentFrameworkRequest =
  struct
    type nonrec t = {
      frameworkId: UUID.t }
    let context_ = "DeleteAssessmentFrameworkRequest"
    let make ~frameworkId = fun () -> { frameworkId }
    let to_value x =
      structure_to_value
        [("frameworkId", (Some (UUID.to_value x.frameworkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let frameworkId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "frameworkId") in
      make ~frameworkId ()
    let of_json json =
      let frameworkId = field_map_exn json "frameworkId" UUID.of_json in
      make ~frameworkId ()
    let to_json v = composed_to_json to_value v
  end
module CreateControlResponse =
  struct
    type nonrec t = {
      control: Control.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?control = fun () -> { control }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("control", (Option.map x.control ~f:Control.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let control =
        (Option.map ~f:Control.of_xml) (Xml.child xml_arg0 "control") in
      make ?control ()
    let of_json json =
      let control = field_map json "control" Control.of_json in
      make ?control ()
    let to_json v = composed_to_json to_value v
  end
module CreateControlRequest =
  struct
    type nonrec t =
      {
      name: ControlName.t ;
      description: ControlDescription.t option ;
      testingInformation: TestingInformation.t option ;
      actionPlanTitle: ActionPlanTitle.t option ;
      actionPlanInstructions: ActionPlanInstructions.t option ;
      controlMappingSources: CreateControlMappingSources.t ;
      tags: TagMap.t option }
    let context_ = "CreateControlRequest"
    let make ?description =
      fun ?testingInformation ->
        fun ?actionPlanTitle ->
          fun ?actionPlanInstructions ->
            fun ?tags ->
              fun ~name ->
                fun ~controlMappingSources ->
                  fun () ->
                    {
                      description;
                      testingInformation;
                      actionPlanTitle;
                      actionPlanInstructions;
                      tags;
                      name;
                      controlMappingSources
                    }
    let to_value x =
      structure_to_value
        [("name", (Some (ControlName.to_value x.name)));
        ("description",
          (Option.map x.description ~f:ControlDescription.to_value));
        ("testingInformation",
          (Option.map x.testingInformation ~f:TestingInformation.to_value));
        ("actionPlanTitle",
          (Option.map x.actionPlanTitle ~f:ActionPlanTitle.to_value));
        ("actionPlanInstructions",
          (Option.map x.actionPlanInstructions
             ~f:ActionPlanInstructions.to_value));
        ("controlMappingSources",
          (Some
             (CreateControlMappingSources.to_value x.controlMappingSources)));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let controlMappingSources =
        CreateControlMappingSources.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlMappingSources") in
      let actionPlanInstructions =
        (Option.map ~f:ActionPlanInstructions.of_xml)
          (Xml.child xml_arg0 "actionPlanInstructions") in
      let actionPlanTitle =
        (Option.map ~f:ActionPlanTitle.of_xml)
          (Xml.child xml_arg0 "actionPlanTitle") in
      let testingInformation =
        (Option.map ~f:TestingInformation.of_xml)
          (Xml.child xml_arg0 "testingInformation") in
      let description =
        (Option.map ~f:ControlDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        ControlName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ~controlMappingSources ?actionPlanInstructions
        ?actionPlanTitle ?testingInformation ?description ~name ()
    let of_json json =
      let tags = field_map json "tags" TagMap.of_json in
      let controlMappingSources =
        field_map_exn json "controlMappingSources"
          CreateControlMappingSources.of_json in
      let actionPlanInstructions =
        field_map json "actionPlanInstructions"
          ActionPlanInstructions.of_json in
      let actionPlanTitle =
        field_map json "actionPlanTitle" ActionPlanTitle.of_json in
      let testingInformation =
        field_map json "testingInformation" TestingInformation.of_json in
      let description =
        field_map json "description" ControlDescription.of_json in
      let name = field_map_exn json "name" ControlName.of_json in
      make ?tags ~controlMappingSources ?actionPlanInstructions
        ?actionPlanTitle ?testingInformation ?description ~name ()
    let to_json v = composed_to_json to_value v
  end
module CreateAssessmentResponse =
  struct
    type nonrec t = {
      assessment: Assessment.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?assessment = fun () -> { assessment }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("assessment", (Option.map x.assessment ~f:Assessment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessment =
        (Option.map ~f:Assessment.of_xml) (Xml.child xml_arg0 "assessment") in
      make ?assessment ()
    let of_json json =
      let assessment = field_map json "assessment" Assessment.of_json in
      make ?assessment ()
    let to_json v = composed_to_json to_value v
  end
module CreateAssessmentRequest =
  struct
    type nonrec t =
      {
      name: AssessmentName.t ;
      description: AssessmentDescription.t option ;
      assessmentReportsDestination: AssessmentReportsDestination.t ;
      scope: Scope.t ;
      roles: Roles.t ;
      frameworkId: UUID.t ;
      tags: TagMap.t option }
    let context_ = "CreateAssessmentRequest"
    let make ?description =
      fun ?tags ->
        fun ~name ->
          fun ~assessmentReportsDestination ->
            fun ~scope ->
              fun ~roles ->
                fun ~frameworkId ->
                  fun () ->
                    {
                      description;
                      tags;
                      name;
                      assessmentReportsDestination;
                      scope;
                      roles;
                      frameworkId
                    }
    let to_value x =
      structure_to_value
        [("name", (Some (AssessmentName.to_value x.name)));
        ("description",
          (Option.map x.description ~f:AssessmentDescription.to_value));
        ("assessmentReportsDestination",
          (Some
             (AssessmentReportsDestination.to_value
                x.assessmentReportsDestination)));
        ("scope", (Some (Scope.to_value x.scope)));
        ("roles", (Some (Roles.to_value x.roles)));
        ("frameworkId", (Some (UUID.to_value x.frameworkId)));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let frameworkId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "frameworkId") in
      let roles =
        Roles.of_xml (Xml.child_exn ~context:context_ xml_arg0 "roles") in
      let scope =
        Scope.of_xml (Xml.child_exn ~context:context_ xml_arg0 "scope") in
      let assessmentReportsDestination =
        AssessmentReportsDestination.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "assessmentReportsDestination") in
      let description =
        (Option.map ~f:AssessmentDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        AssessmentName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ~frameworkId ~roles ~scope ~assessmentReportsDestination
        ?description ~name ()
    let of_json json =
      let tags = field_map json "tags" TagMap.of_json in
      let frameworkId = field_map_exn json "frameworkId" UUID.of_json in
      let roles = field_map_exn json "roles" Roles.of_json in
      let scope = field_map_exn json "scope" Scope.of_json in
      let assessmentReportsDestination =
        field_map_exn json "assessmentReportsDestination"
          AssessmentReportsDestination.of_json in
      let description =
        field_map json "description" AssessmentDescription.of_json in
      let name = field_map_exn json "name" AssessmentName.of_json in
      make ?tags ~frameworkId ~roles ~scope ~assessmentReportsDestination
        ?description ~name ()
    let to_json v = composed_to_json to_value v
  end
module CreateAssessmentReportResponse =
  struct
    type nonrec t = {
      assessmentReport: AssessmentReport.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?assessmentReport = fun () -> { assessmentReport }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("assessmentReport",
           (Option.map x.assessmentReport ~f:AssessmentReport.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessmentReport =
        (Option.map ~f:AssessmentReport.of_xml)
          (Xml.child xml_arg0 "assessmentReport") in
      make ?assessmentReport ()
    let of_json json =
      let assessmentReport =
        field_map json "assessmentReport" AssessmentReport.of_json in
      make ?assessmentReport ()
    let to_json v = composed_to_json to_value v
  end
module CreateAssessmentReportRequest =
  struct
    type nonrec t =
      {
      name: AssessmentReportName.t ;
      description: AssessmentReportDescription.t option ;
      assessmentId: UUID.t }
    let context_ = "CreateAssessmentReportRequest"
    let make ?description =
      fun ~name ->
        fun ~assessmentId -> fun () -> { description; name; assessmentId }
    let to_value x =
      structure_to_value
        [("name", (Some (AssessmentReportName.to_value x.name)));
        ("description",
          (Option.map x.description ~f:AssessmentReportDescription.to_value));
        ("assessmentId", (Some (UUID.to_value x.assessmentId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      let description =
        (Option.map ~f:AssessmentReportDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        AssessmentReportName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~assessmentId ?description ~name ()
    let of_json json =
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      let description =
        field_map json "description" AssessmentReportDescription.of_json in
      let name = field_map_exn json "name" AssessmentReportName.of_json in
      make ~assessmentId ?description ~name ()
    let to_json v = composed_to_json to_value v
  end
module CreateAssessmentFrameworkResponse =
  struct
    type nonrec t = {
      framework: Framework.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?framework = fun () -> { framework }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("framework", (Option.map x.framework ~f:Framework.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let framework =
        (Option.map ~f:Framework.of_xml) (Xml.child xml_arg0 "framework") in
      make ?framework ()
    let of_json json =
      let framework = field_map json "framework" Framework.of_json in
      make ?framework ()
    let to_json v = composed_to_json to_value v
  end
module CreateAssessmentFrameworkRequest =
  struct
    type nonrec t =
      {
      name: FrameworkName.t ;
      description: FrameworkDescription.t option ;
      complianceType: ComplianceType.t option ;
      controlSets: CreateAssessmentFrameworkControlSets.t ;
      tags: TagMap.t option }
    let context_ = "CreateAssessmentFrameworkRequest"
    let make ?description =
      fun ?complianceType ->
        fun ?tags ->
          fun ~name ->
            fun ~controlSets ->
              fun () ->
                { description; complianceType; tags; name; controlSets }
    let to_value x =
      structure_to_value
        [("name", (Some (FrameworkName.to_value x.name)));
        ("description",
          (Option.map x.description ~f:FrameworkDescription.to_value));
        ("complianceType",
          (Option.map x.complianceType ~f:ComplianceType.to_value));
        ("controlSets",
          (Some (CreateAssessmentFrameworkControlSets.to_value x.controlSets)));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let controlSets =
        CreateAssessmentFrameworkControlSets.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlSets") in
      let complianceType =
        (Option.map ~f:ComplianceType.of_xml)
          (Xml.child xml_arg0 "complianceType") in
      let description =
        (Option.map ~f:FrameworkDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        FrameworkName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ~controlSets ?complianceType ?description ~name ()
    let of_json json =
      let tags = field_map json "tags" TagMap.of_json in
      let controlSets =
        field_map_exn json "controlSets"
          CreateAssessmentFrameworkControlSets.of_json in
      let complianceType =
        field_map json "complianceType" ComplianceType.of_json in
      let description =
        field_map json "description" FrameworkDescription.of_json in
      let name = field_map_exn json "name" FrameworkName.of_json in
      make ?tags ~controlSets ?complianceType ?description ~name ()
    let to_json v = composed_to_json to_value v
  end
module BatchImportEvidenceToAssessmentControlResponse =
  struct
    type nonrec t =
      {
      errors: BatchImportEvidenceToAssessmentControlErrors.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?errors = fun () -> { errors }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("errors",
           (Option.map x.errors
              ~f:BatchImportEvidenceToAssessmentControlErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errors =
        (Option.map ~f:BatchImportEvidenceToAssessmentControlErrors.of_xml)
          (Xml.child xml_arg0 "errors") in
      make ?errors ()
    let of_json json =
      let errors =
        field_map json "errors"
          BatchImportEvidenceToAssessmentControlErrors.of_json in
      make ?errors ()
    let to_json v = composed_to_json to_value v
  end
module BatchImportEvidenceToAssessmentControlRequest =
  struct
    type nonrec t =
      {
      assessmentId: UUID.t ;
      controlSetId: ControlSetId.t ;
      controlId: UUID.t ;
      manualEvidence: ManualEvidenceList.t }
    let context_ = "BatchImportEvidenceToAssessmentControlRequest"
    let make ~assessmentId =
      fun ~controlSetId ->
        fun ~controlId ->
          fun ~manualEvidence ->
            fun () ->
              { assessmentId; controlSetId; controlId; manualEvidence }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("controlSetId", (Some (ControlSetId.to_value x.controlSetId)));
        ("controlId", (Some (UUID.to_value x.controlId)));
        ("manualEvidence",
          (Some (ManualEvidenceList.to_value x.manualEvidence)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let manualEvidence =
        ManualEvidenceList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "manualEvidence") in
      let controlId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "controlId") in
      let controlSetId =
        ControlSetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "controlSetId") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ~manualEvidence ~controlId ~controlSetId ~assessmentId ()
    let of_json json =
      let manualEvidence =
        field_map_exn json "manualEvidence" ManualEvidenceList.of_json in
      let controlId = field_map_exn json "controlId" UUID.of_json in
      let controlSetId =
        field_map_exn json "controlSetId" ControlSetId.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ~manualEvidence ~controlId ~controlSetId ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module BatchDisassociateAssessmentReportEvidenceResponse =
  struct
    type nonrec t =
      {
      evidenceIds: EvidenceIds.t option ;
      errors: AssessmentReportEvidenceErrors.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?evidenceIds = fun ?errors -> fun () -> { evidenceIds; errors }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("evidenceIds", (Option.map x.evidenceIds ~f:EvidenceIds.to_value));
        ("errors",
          (Option.map x.errors ~f:AssessmentReportEvidenceErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errors =
        (Option.map ~f:AssessmentReportEvidenceErrors.of_xml)
          (Xml.child xml_arg0 "errors") in
      let evidenceIds =
        (Option.map ~f:EvidenceIds.of_xml) (Xml.child xml_arg0 "evidenceIds") in
      make ?errors ?evidenceIds ()
    let of_json json =
      let errors =
        field_map json "errors" AssessmentReportEvidenceErrors.of_json in
      let evidenceIds = field_map json "evidenceIds" EvidenceIds.of_json in
      make ?errors ?evidenceIds ()
    let to_json v = composed_to_json to_value v
  end
module BatchDisassociateAssessmentReportEvidenceRequest =
  struct
    type nonrec t =
      {
      assessmentId: UUID.t ;
      evidenceFolderId: UUID.t ;
      evidenceIds: EvidenceIds.t }
    let context_ = "BatchDisassociateAssessmentReportEvidenceRequest"
    let make ~assessmentId =
      fun ~evidenceFolderId ->
        fun ~evidenceIds ->
          fun () -> { assessmentId; evidenceFolderId; evidenceIds }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("evidenceFolderId", (Some (UUID.to_value x.evidenceFolderId)));
        ("evidenceIds", (Some (EvidenceIds.to_value x.evidenceIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let evidenceIds =
        EvidenceIds.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "evidenceIds") in
      let evidenceFolderId =
        UUID.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "evidenceFolderId") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ~evidenceIds ~evidenceFolderId ~assessmentId ()
    let of_json json =
      let evidenceIds = field_map_exn json "evidenceIds" EvidenceIds.of_json in
      let evidenceFolderId =
        field_map_exn json "evidenceFolderId" UUID.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ~evidenceIds ~evidenceFolderId ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module BatchDeleteDelegationByAssessmentResponse =
  struct
    type nonrec t =
      {
      errors: BatchDeleteDelegationByAssessmentErrors.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?errors = fun () -> { errors }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("errors",
           (Option.map x.errors
              ~f:BatchDeleteDelegationByAssessmentErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errors =
        (Option.map ~f:BatchDeleteDelegationByAssessmentErrors.of_xml)
          (Xml.child xml_arg0 "errors") in
      make ?errors ()
    let of_json json =
      let errors =
        field_map json "errors"
          BatchDeleteDelegationByAssessmentErrors.of_json in
      make ?errors ()
    let to_json v = composed_to_json to_value v
  end
module BatchDeleteDelegationByAssessmentRequest =
  struct
    type nonrec t = {
      delegationIds: DelegationIds.t ;
      assessmentId: UUID.t }
    let context_ = "BatchDeleteDelegationByAssessmentRequest"
    let make ~delegationIds =
      fun ~assessmentId -> fun () -> { delegationIds; assessmentId }
    let to_value x =
      structure_to_value
        [("delegationIds", (Some (DelegationIds.to_value x.delegationIds)));
        ("assessmentId", (Some (UUID.to_value x.assessmentId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      let delegationIds =
        DelegationIds.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "delegationIds") in
      make ~assessmentId ~delegationIds ()
    let of_json json =
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      let delegationIds =
        field_map_exn json "delegationIds" DelegationIds.of_json in
      make ~assessmentId ~delegationIds ()
    let to_json v = composed_to_json to_value v
  end
module BatchCreateDelegationByAssessmentResponse =
  struct
    type nonrec t =
      {
      delegations: Delegations.t option ;
      errors: BatchCreateDelegationByAssessmentErrors.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?delegations = fun ?errors -> fun () -> { delegations; errors }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("delegations", (Option.map x.delegations ~f:Delegations.to_value));
        ("errors",
          (Option.map x.errors
             ~f:BatchCreateDelegationByAssessmentErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errors =
        (Option.map ~f:BatchCreateDelegationByAssessmentErrors.of_xml)
          (Xml.child xml_arg0 "errors") in
      let delegations =
        (Option.map ~f:Delegations.of_xml) (Xml.child xml_arg0 "delegations") in
      make ?errors ?delegations ()
    let of_json json =
      let errors =
        field_map json "errors"
          BatchCreateDelegationByAssessmentErrors.of_json in
      let delegations = field_map json "delegations" Delegations.of_json in
      make ?errors ?delegations ()
    let to_json v = composed_to_json to_value v
  end
module BatchCreateDelegationByAssessmentRequest =
  struct
    type nonrec t =
      {
      createDelegationRequests: CreateDelegationRequests.t ;
      assessmentId: UUID.t }
    let context_ = "BatchCreateDelegationByAssessmentRequest"
    let make ~createDelegationRequests =
      fun ~assessmentId ->
        fun () -> { createDelegationRequests; assessmentId }
    let to_value x =
      structure_to_value
        [("createDelegationRequests",
           (Some
              (CreateDelegationRequests.to_value x.createDelegationRequests)));
        ("assessmentId", (Some (UUID.to_value x.assessmentId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      let createDelegationRequests =
        CreateDelegationRequests.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "createDelegationRequests") in
      make ~assessmentId ~createDelegationRequests ()
    let of_json json =
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      let createDelegationRequests =
        field_map_exn json "createDelegationRequests"
          CreateDelegationRequests.of_json in
      make ~assessmentId ~createDelegationRequests ()
    let to_json v = composed_to_json to_value v
  end
module BatchAssociateAssessmentReportEvidenceResponse =
  struct
    type nonrec t =
      {
      evidenceIds: EvidenceIds.t option ;
      errors: AssessmentReportEvidenceErrors.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?evidenceIds = fun ?errors -> fun () -> { evidenceIds; errors }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("evidenceIds", (Option.map x.evidenceIds ~f:EvidenceIds.to_value));
        ("errors",
          (Option.map x.errors ~f:AssessmentReportEvidenceErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errors =
        (Option.map ~f:AssessmentReportEvidenceErrors.of_xml)
          (Xml.child xml_arg0 "errors") in
      let evidenceIds =
        (Option.map ~f:EvidenceIds.of_xml) (Xml.child xml_arg0 "evidenceIds") in
      make ?errors ?evidenceIds ()
    let of_json json =
      let errors =
        field_map json "errors" AssessmentReportEvidenceErrors.of_json in
      let evidenceIds = field_map json "evidenceIds" EvidenceIds.of_json in
      make ?errors ?evidenceIds ()
    let to_json v = composed_to_json to_value v
  end
module BatchAssociateAssessmentReportEvidenceRequest =
  struct
    type nonrec t =
      {
      assessmentId: UUID.t ;
      evidenceFolderId: UUID.t ;
      evidenceIds: EvidenceIds.t }
    let context_ = "BatchAssociateAssessmentReportEvidenceRequest"
    let make ~assessmentId =
      fun ~evidenceFolderId ->
        fun ~evidenceIds ->
          fun () -> { assessmentId; evidenceFolderId; evidenceIds }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("evidenceFolderId", (Some (UUID.to_value x.evidenceFolderId)));
        ("evidenceIds", (Some (EvidenceIds.to_value x.evidenceIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let evidenceIds =
        EvidenceIds.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "evidenceIds") in
      let evidenceFolderId =
        UUID.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "evidenceFolderId") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ~evidenceIds ~evidenceFolderId ~assessmentId ()
    let of_json json =
      let evidenceIds = field_map_exn json "evidenceIds" EvidenceIds.of_json in
      let evidenceFolderId =
        field_map_exn json "evidenceFolderId" UUID.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ~evidenceIds ~evidenceFolderId ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
module AssociateAssessmentReportEvidenceFolderResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module AssociateAssessmentReportEvidenceFolderRequest =
  struct
    type nonrec t = {
      assessmentId: UUID.t ;
      evidenceFolderId: UUID.t }
    let context_ = "AssociateAssessmentReportEvidenceFolderRequest"
    let make ~assessmentId =
      fun ~evidenceFolderId -> fun () -> { assessmentId; evidenceFolderId }
    let to_value x =
      structure_to_value
        [("assessmentId", (Some (UUID.to_value x.assessmentId)));
        ("evidenceFolderId", (Some (UUID.to_value x.evidenceFolderId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let evidenceFolderId =
        UUID.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "evidenceFolderId") in
      let assessmentId =
        UUID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "assessmentId") in
      make ~evidenceFolderId ~assessmentId ()
    let of_json json =
      let evidenceFolderId =
        field_map_exn json "evidenceFolderId" UUID.of_json in
      let assessmentId = field_map_exn json "assessmentId" UUID.of_json in
      make ~evidenceFolderId ~assessmentId ()
    let to_json v = composed_to_json to_value v
  end
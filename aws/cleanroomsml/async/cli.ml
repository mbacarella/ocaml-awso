(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_trained_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionIdentifier =
         flag "version-identifier" (optional string) ~doc:"STRING UUID"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID"
       and trainedModelArn =
         flag "trained-model-arn" (required string)
           ~doc:"STRING TrainedModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_trained_model
           (Values.CancelTrainedModelRequest.make ?versionIdentifier
              ~membershipIdentifier ~trainedModelArn ()) None None])
let cancel_trained_model_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID"
       and trainedModelInferenceJobArn =
         flag "trained-model-inference-job-arn" (required string)
           ~doc:"STRING TrainedModelInferenceJobArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_trained_model_inference_job
           (Values.CancelTrainedModelInferenceJobRequest.make
              ~membershipIdentifier ~trainedModelInferenceJobArn ()) None
           None])
let create_audience_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trainingDataStartTime =
         flag "training-data-start-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and trainingDataEndTime =
         flag "training-data-end-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and trainingDatasetArn =
         flag "training-dataset-arn" (required string)
           ~doc:"STRING TrainingDatasetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_audience_model
           (Values.CreateAudienceModelRequest.make
              ?trainingDataStartTime:(Option.map
                                        ~f:Values.SyntheticTimestamp_date_time.of_json
                                        trainingDataStartTime)
              ?trainingDataEndTime:(Option.map
                                      ~f:Values.SyntheticTimestamp_date_time.of_json
                                      trainingDataEndTime) ?kmsKeyArn
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?description
              ~name ~trainingDatasetArn ())
           (Some Values.CreateAudienceModelResponse.to_json)
           (Some Values.CreateAudienceModelResponse.error_to_json)])
let create_configured_audience_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and minMatchingSeedSize =
         flag "min-matching-seed-size" (optional int)
           ~doc:"INT MinMatchingSeedSize"
       and audienceSizeConfig =
         flag "audience-size-config" (optional json_arg)
           ~doc:"JSON AudienceSizeConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and childResourceTagOnCreatePolicy =
         flag "child-resource-tag-on-create-policy" (optional json_arg)
           ~doc:"JSON TagOnCreatePolicy"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and audienceModelArn =
         flag "audience-model-arn" (required string)
           ~doc:"STRING AudienceModelArn"
       and outputConfig =
         flag "output-config" (required json_arg)
           ~doc:"JSON ConfiguredAudienceModelOutputConfig"
       and sharedAudienceMetrics =
         flag "shared-audience-metrics" (required json_arg)
           ~doc:"JSON MetricsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configured_audience_model
           (Values.CreateConfiguredAudienceModelRequest.make ?description
              ?minMatchingSeedSize
              ?audienceSizeConfig:(Option.map
                                     ~f:Values.AudienceSizeConfig.of_json
                                     audienceSizeConfig)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?childResourceTagOnCreatePolicy:(Option.map
                                                 ~f:Values.TagOnCreatePolicy.of_json
                                                 childResourceTagOnCreatePolicy)
              ~name ~audienceModelArn
              ~outputConfig:(Values.ConfiguredAudienceModelOutputConfig.of_json
                               outputConfig)
              ~sharedAudienceMetrics:(Values.MetricsList.of_json
                                        sharedAudienceMetrics) ())
           (Some Values.CreateConfiguredAudienceModelResponse.to_json)
           (Some Values.CreateConfiguredAudienceModelResponse.error_to_json)])
let create_configured_model_algorithm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and trainingContainerConfig =
         flag "training-container-config" (optional json_arg)
           ~doc:"JSON ContainerConfig"
       and inferenceContainerConfig =
         flag "inference-container-config" (optional json_arg)
           ~doc:"JSON InferenceContainerConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING IamRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configured_model_algorithm
           (Values.CreateConfiguredModelAlgorithmRequest.make ?description
              ?trainingContainerConfig:(Option.map
                                          ~f:Values.ContainerConfig.of_json
                                          trainingContainerConfig)
              ?inferenceContainerConfig:(Option.map
                                           ~f:Values.InferenceContainerConfig.of_json
                                           inferenceContainerConfig)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?kmsKeyArn
              ~name ~roleArn ())
           (Some Values.CreateConfiguredModelAlgorithmResponse.to_json)
           (Some Values.CreateConfiguredModelAlgorithmResponse.error_to_json)])
let create_configured_model_algorithm_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and privacyConfiguration =
         flag "privacy-configuration" (optional json_arg)
           ~doc:"JSON PrivacyConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID"
       and configuredModelAlgorithmArn =
         flag "configured-model-algorithm-arn" (required string)
           ~doc:"STRING ConfiguredModelAlgorithmArn"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configured_model_algorithm_association
           (Values.CreateConfiguredModelAlgorithmAssociationRequest.make
              ?description
              ?privacyConfiguration:(Option.map
                                       ~f:Values.PrivacyConfiguration.of_json
                                       privacyConfiguration)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~membershipIdentifier ~configuredModelAlgorithmArn ~name ())
           (Some
              Values.CreateConfiguredModelAlgorithmAssociationResponse.to_json)
           (Some
              Values.CreateConfiguredModelAlgorithmAssociationResponse.error_to_json)])
let create_m_l_input_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID"
       and configuredModelAlgorithmAssociations =
         flag "configured-model-algorithm-associations" (required json_arg)
           ~doc:"JSON CreateMLInputChannelRequestConfiguredModelAlgorithmAssociationsList"
       and inputChannel =
         flag "input-channel" (required json_arg) ~doc:"JSON InputChannel"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and retentionInDays =
         flag "retention-in-days" (required int)
           ~doc:"INT CreateMLInputChannelRequestRetentionInDaysInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_m_l_input_channel
           (Values.CreateMLInputChannelRequest.make ?description ?kmsKeyArn
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~membershipIdentifier
              ~configuredModelAlgorithmAssociations:(Values.CreateMLInputChannelRequestConfiguredModelAlgorithmAssociationsList.of_json
                                                       configuredModelAlgorithmAssociations)
              ~inputChannel:(Values.InputChannel.of_json inputChannel) ~name
              ~retentionInDays ())
           (Some Values.CreateMLInputChannelResponse.to_json)
           (Some Values.CreateMLInputChannelResponse.error_to_json)])
let create_trained_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hyperparameters =
         flag "hyperparameters" (optional json_arg)
           ~doc:"JSON HyperParameters"
       and environment =
         flag "environment" (optional json_arg) ~doc:"JSON Environment"
       and stoppingCondition =
         flag "stopping-condition" (optional json_arg)
           ~doc:"JSON StoppingCondition"
       and incrementalTrainingDataChannels =
         flag "incremental-training-data-channels" (optional json_arg)
           ~doc:"JSON IncrementalTrainingDataChannels"
       and trainingInputMode =
         flag "training-input-mode" (optional json_arg)
           ~doc:"JSON TrainingInputMode"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and configuredModelAlgorithmAssociationArn =
         flag "configured-model-algorithm-association-arn" (required string)
           ~doc:"STRING ConfiguredModelAlgorithmAssociationArn"
       and resourceConfig =
         flag "resource-config" (required json_arg)
           ~doc:"JSON ResourceConfig"
       and dataChannels =
         flag "data-channels" (required json_arg)
           ~doc:"JSON ModelTrainingDataChannels" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_trained_model
           (Values.CreateTrainedModelRequest.make
              ?hyperparameters:(Option.map ~f:Values.HyperParameters.of_json
                                  hyperparameters)
              ?environment:(Option.map ~f:Values.Environment.of_json
                              environment)
              ?stoppingCondition:(Option.map
                                    ~f:Values.StoppingCondition.of_json
                                    stoppingCondition)
              ?incrementalTrainingDataChannels:(Option.map
                                                  ~f:Values.IncrementalTrainingDataChannels.of_json
                                                  incrementalTrainingDataChannels)
              ?trainingInputMode:(Option.map
                                    ~f:Values.TrainingInputMode.of_json
                                    trainingInputMode) ?description
              ?kmsKeyArn ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~membershipIdentifier ~name
              ~configuredModelAlgorithmAssociationArn
              ~resourceConfig:(Values.ResourceConfig.of_json resourceConfig)
              ~dataChannels:(Values.ModelTrainingDataChannels.of_json
                               dataChannels) ())
           (Some Values.CreateTrainedModelResponse.to_json)
           (Some Values.CreateTrainedModelResponse.error_to_json)])
let create_training_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING IamRoleArn"
       and trainingData =
         flag "training-data" (required json_arg)
           ~doc:"JSON CreateTrainingDatasetRequestTrainingDataList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_training_dataset
           (Values.CreateTrainingDatasetRequest.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?description
              ~name ~roleArn
              ~trainingData:(Values.CreateTrainingDatasetRequestTrainingDataList.of_json
                               trainingData) ())
           (Some Values.CreateTrainingDatasetResponse.to_json)
           (Some Values.CreateTrainingDatasetResponse.error_to_json)])
let delete_audience_generation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and audienceGenerationJobArn =
         flag "audience-generation-job-arn" (required string)
           ~doc:"STRING AudienceGenerationJobArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_audience_generation_job
           (Values.DeleteAudienceGenerationJobRequest.make
              ~audienceGenerationJobArn ()) None None])
let delete_audience_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and audienceModelArn =
         flag "audience-model-arn" (required string)
           ~doc:"STRING AudienceModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_audience_model
           (Values.DeleteAudienceModelRequest.make ~audienceModelArn ()) None
           None])
let delete_configured_audience_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuredAudienceModelArn =
         flag "configured-audience-model-arn" (required string)
           ~doc:"STRING ConfiguredAudienceModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configured_audience_model
           (Values.DeleteConfiguredAudienceModelRequest.make
              ~configuredAudienceModelArn ()) None None])
let delete_configured_audience_model_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuredAudienceModelArn =
         flag "configured-audience-model-arn" (required string)
           ~doc:"STRING ConfiguredAudienceModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configured_audience_model_policy
           (Values.DeleteConfiguredAudienceModelPolicyRequest.make
              ~configuredAudienceModelArn ()) None None])
let delete_configured_model_algorithm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuredModelAlgorithmArn =
         flag "configured-model-algorithm-arn" (required string)
           ~doc:"STRING ConfiguredModelAlgorithmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configured_model_algorithm
           (Values.DeleteConfiguredModelAlgorithmRequest.make
              ~configuredModelAlgorithmArn ()) None None])
let delete_configured_model_algorithm_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuredModelAlgorithmAssociationArn =
         flag "configured-model-algorithm-association-arn" (required string)
           ~doc:"STRING ConfiguredModelAlgorithmAssociationArn"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configured_model_algorithm_association
           (Values.DeleteConfiguredModelAlgorithmAssociationRequest.make
              ~configuredModelAlgorithmAssociationArn ~membershipIdentifier
              ()) None None])
let delete_m_l_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_m_l_configuration
           (Values.DeleteMLConfigurationRequest.make ~membershipIdentifier ())
           None None])
let delete_m_l_input_channel_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mlInputChannelArn =
         flag "ml-input-channel-arn" (required string)
           ~doc:"STRING MLInputChannelArn"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_m_l_input_channel_data
           (Values.DeleteMLInputChannelDataRequest.make ~mlInputChannelArn
              ~membershipIdentifier ()) None None])
let delete_trained_model_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionIdentifier =
         flag "version-identifier" (optional string) ~doc:"STRING UUID"
       and trainedModelArn =
         flag "trained-model-arn" (required string)
           ~doc:"STRING TrainedModelArn"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_trained_model_output
           (Values.DeleteTrainedModelOutputRequest.make ?versionIdentifier
              ~trainedModelArn ~membershipIdentifier ()) None None])
let delete_training_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trainingDatasetArn =
         flag "training-dataset-arn" (required string)
           ~doc:"STRING TrainingDatasetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_training_dataset
           (Values.DeleteTrainingDatasetRequest.make ~trainingDatasetArn ())
           None None])
let get_audience_generation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and audienceGenerationJobArn =
         flag "audience-generation-job-arn" (required string)
           ~doc:"STRING AudienceGenerationJobArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_audience_generation_job
           (Values.GetAudienceGenerationJobRequest.make
              ~audienceGenerationJobArn ())
           (Some Values.GetAudienceGenerationJobResponse.to_json)
           (Some Values.GetAudienceGenerationJobResponse.error_to_json)])
let get_audience_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and audienceModelArn =
         flag "audience-model-arn" (required string)
           ~doc:"STRING AudienceModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_audience_model
           (Values.GetAudienceModelRequest.make ~audienceModelArn ())
           (Some Values.GetAudienceModelResponse.to_json)
           (Some Values.GetAudienceModelResponse.error_to_json)])
let get_collaboration_configured_model_algorithm_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuredModelAlgorithmAssociationArn =
         flag "configured-model-algorithm-association-arn" (required string)
           ~doc:"STRING ConfiguredModelAlgorithmAssociationArn"
       and collaborationIdentifier =
         flag "collaboration-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_collaboration_configured_model_algorithm_association
           (Values.GetCollaborationConfiguredModelAlgorithmAssociationRequest.make
              ~configuredModelAlgorithmAssociationArn
              ~collaborationIdentifier ())
           (Some
              Values.GetCollaborationConfiguredModelAlgorithmAssociationResponse.to_json)
           (Some
              Values.GetCollaborationConfiguredModelAlgorithmAssociationResponse.error_to_json)])
let get_collaboration_m_l_input_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mlInputChannelArn =
         flag "ml-input-channel-arn" (required string)
           ~doc:"STRING MLInputChannelArn"
       and collaborationIdentifier =
         flag "collaboration-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_collaboration_m_l_input_channel
           (Values.GetCollaborationMLInputChannelRequest.make
              ~mlInputChannelArn ~collaborationIdentifier ())
           (Some Values.GetCollaborationMLInputChannelResponse.to_json)
           (Some Values.GetCollaborationMLInputChannelResponse.error_to_json)])
let get_collaboration_trained_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionIdentifier =
         flag "version-identifier" (optional string) ~doc:"STRING UUID"
       and trainedModelArn =
         flag "trained-model-arn" (required string)
           ~doc:"STRING TrainedModelArn"
       and collaborationIdentifier =
         flag "collaboration-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_collaboration_trained_model
           (Values.GetCollaborationTrainedModelRequest.make
              ?versionIdentifier ~trainedModelArn ~collaborationIdentifier ())
           (Some Values.GetCollaborationTrainedModelResponse.to_json)
           (Some Values.GetCollaborationTrainedModelResponse.error_to_json)])
let get_configured_audience_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuredAudienceModelArn =
         flag "configured-audience-model-arn" (required string)
           ~doc:"STRING ConfiguredAudienceModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configured_audience_model
           (Values.GetConfiguredAudienceModelRequest.make
              ~configuredAudienceModelArn ())
           (Some Values.GetConfiguredAudienceModelResponse.to_json)
           (Some Values.GetConfiguredAudienceModelResponse.error_to_json)])
let get_configured_audience_model_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuredAudienceModelArn =
         flag "configured-audience-model-arn" (required string)
           ~doc:"STRING ConfiguredAudienceModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configured_audience_model_policy
           (Values.GetConfiguredAudienceModelPolicyRequest.make
              ~configuredAudienceModelArn ())
           (Some Values.GetConfiguredAudienceModelPolicyResponse.to_json)
           (Some
              Values.GetConfiguredAudienceModelPolicyResponse.error_to_json)])
let get_configured_model_algorithm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuredModelAlgorithmArn =
         flag "configured-model-algorithm-arn" (required string)
           ~doc:"STRING ConfiguredModelAlgorithmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configured_model_algorithm
           (Values.GetConfiguredModelAlgorithmRequest.make
              ~configuredModelAlgorithmArn ())
           (Some Values.GetConfiguredModelAlgorithmResponse.to_json)
           (Some Values.GetConfiguredModelAlgorithmResponse.error_to_json)])
let get_configured_model_algorithm_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuredModelAlgorithmAssociationArn =
         flag "configured-model-algorithm-association-arn" (required string)
           ~doc:"STRING ConfiguredModelAlgorithmAssociationArn"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configured_model_algorithm_association
           (Values.GetConfiguredModelAlgorithmAssociationRequest.make
              ~configuredModelAlgorithmAssociationArn ~membershipIdentifier
              ())
           (Some
              Values.GetConfiguredModelAlgorithmAssociationResponse.to_json)
           (Some
              Values.GetConfiguredModelAlgorithmAssociationResponse.error_to_json)])
let get_m_l_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_m_l_configuration
           (Values.GetMLConfigurationRequest.make ~membershipIdentifier ())
           (Some Values.GetMLConfigurationResponse.to_json)
           (Some Values.GetMLConfigurationResponse.error_to_json)])
let get_m_l_input_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mlInputChannelArn =
         flag "ml-input-channel-arn" (required string)
           ~doc:"STRING MLInputChannelArn"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_m_l_input_channel
           (Values.GetMLInputChannelRequest.make ~mlInputChannelArn
              ~membershipIdentifier ())
           (Some Values.GetMLInputChannelResponse.to_json)
           (Some Values.GetMLInputChannelResponse.error_to_json)])
let get_trained_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionIdentifier =
         flag "version-identifier" (optional string) ~doc:"STRING UUID"
       and trainedModelArn =
         flag "trained-model-arn" (required string)
           ~doc:"STRING TrainedModelArn"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_trained_model
           (Values.GetTrainedModelRequest.make ?versionIdentifier
              ~trainedModelArn ~membershipIdentifier ())
           (Some Values.GetTrainedModelResponse.to_json)
           (Some Values.GetTrainedModelResponse.error_to_json)])
let get_trained_model_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID"
       and trainedModelInferenceJobArn =
         flag "trained-model-inference-job-arn" (required string)
           ~doc:"STRING TrainedModelInferenceJobArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_trained_model_inference_job
           (Values.GetTrainedModelInferenceJobRequest.make
              ~membershipIdentifier ~trainedModelInferenceJobArn ())
           (Some Values.GetTrainedModelInferenceJobResponse.to_json)
           (Some Values.GetTrainedModelInferenceJobResponse.error_to_json)])
let get_training_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trainingDatasetArn =
         flag "training-dataset-arn" (required string)
           ~doc:"STRING TrainingDatasetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_training_dataset
           (Values.GetTrainingDatasetRequest.make ~trainingDatasetArn ())
           (Some Values.GetTrainingDatasetResponse.to_json)
           (Some Values.GetTrainingDatasetResponse.error_to_json)])
let list_audience_export_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and audienceGenerationJobArn =
         flag "audience-generation-job-arn" (optional string)
           ~doc:"STRING AudienceGenerationJobArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_audience_export_jobs
           (Values.ListAudienceExportJobsRequest.make ?nextToken ?maxResults
              ?audienceGenerationJobArn ())
           (Some Values.ListAudienceExportJobsResponse.to_json)
           (Some Values.ListAudienceExportJobsResponse.error_to_json)])
let list_audience_generation_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and configuredAudienceModelArn =
         flag "configured-audience-model-arn" (optional string)
           ~doc:"STRING ConfiguredAudienceModelArn"
       and collaborationId =
         flag "collaboration-id" (optional string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_audience_generation_jobs
           (Values.ListAudienceGenerationJobsRequest.make ?nextToken
              ?maxResults ?configuredAudienceModelArn ?collaborationId ())
           (Some Values.ListAudienceGenerationJobsResponse.to_json)
           (Some Values.ListAudienceGenerationJobsResponse.error_to_json)])
let list_audience_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_audience_models
           (Values.ListAudienceModelsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListAudienceModelsResponse.to_json)
           (Some Values.ListAudienceModelsResponse.error_to_json)])
let list_collaboration_configured_model_algorithm_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and collaborationIdentifier =
         flag "collaboration-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_collaboration_configured_model_algorithm_associations
           (Values.ListCollaborationConfiguredModelAlgorithmAssociationsRequest.make
              ?nextToken ?maxResults ~collaborationIdentifier ())
           (Some
              Values.ListCollaborationConfiguredModelAlgorithmAssociationsResponse.to_json)
           (Some
              Values.ListCollaborationConfiguredModelAlgorithmAssociationsResponse.error_to_json)])
let list_collaboration_m_l_input_channels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and collaborationIdentifier =
         flag "collaboration-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_collaboration_m_l_input_channels
           (Values.ListCollaborationMLInputChannelsRequest.make ?nextToken
              ?maxResults ~collaborationIdentifier ())
           (Some Values.ListCollaborationMLInputChannelsResponse.to_json)
           (Some
              Values.ListCollaborationMLInputChannelsResponse.error_to_json)])
let list_collaboration_trained_model_export_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and trainedModelVersionIdentifier =
         flag "trained-model-version-identifier" (optional string)
           ~doc:"STRING UUID"
       and collaborationIdentifier =
         flag "collaboration-identifier" (required string) ~doc:"STRING UUID"
       and trainedModelArn =
         flag "trained-model-arn" (required string)
           ~doc:"STRING TrainedModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_collaboration_trained_model_export_jobs
           (Values.ListCollaborationTrainedModelExportJobsRequest.make
              ?nextToken ?maxResults ?trainedModelVersionIdentifier
              ~collaborationIdentifier ~trainedModelArn ())
           (Some
              Values.ListCollaborationTrainedModelExportJobsResponse.to_json)
           (Some
              Values.ListCollaborationTrainedModelExportJobsResponse.error_to_json)])
let list_collaboration_trained_model_inference_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and trainedModelArn =
         flag "trained-model-arn" (optional string)
           ~doc:"STRING TrainedModelArn"
       and trainedModelVersionIdentifier =
         flag "trained-model-version-identifier" (optional string)
           ~doc:"STRING UUID"
       and collaborationIdentifier =
         flag "collaboration-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_collaboration_trained_model_inference_jobs
           (Values.ListCollaborationTrainedModelInferenceJobsRequest.make
              ?nextToken ?maxResults ?trainedModelArn
              ?trainedModelVersionIdentifier ~collaborationIdentifier ())
           (Some
              Values.ListCollaborationTrainedModelInferenceJobsResponse.to_json)
           (Some
              Values.ListCollaborationTrainedModelInferenceJobsResponse.error_to_json)])
let list_collaboration_trained_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and collaborationIdentifier =
         flag "collaboration-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_collaboration_trained_models
           (Values.ListCollaborationTrainedModelsRequest.make ?nextToken
              ?maxResults ~collaborationIdentifier ())
           (Some Values.ListCollaborationTrainedModelsResponse.to_json)
           (Some Values.ListCollaborationTrainedModelsResponse.error_to_json)])
let list_configured_audience_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configured_audience_models
           (Values.ListConfiguredAudienceModelsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListConfiguredAudienceModelsResponse.to_json)
           (Some Values.ListConfiguredAudienceModelsResponse.error_to_json)])
let list_configured_model_algorithm_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configured_model_algorithm_associations
           (Values.ListConfiguredModelAlgorithmAssociationsRequest.make
              ?nextToken ?maxResults ~membershipIdentifier ())
           (Some
              Values.ListConfiguredModelAlgorithmAssociationsResponse.to_json)
           (Some
              Values.ListConfiguredModelAlgorithmAssociationsResponse.error_to_json)])
let list_configured_model_algorithms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configured_model_algorithms
           (Values.ListConfiguredModelAlgorithmsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListConfiguredModelAlgorithmsResponse.to_json)
           (Some Values.ListConfiguredModelAlgorithmsResponse.error_to_json)])
let list_m_l_input_channels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_m_l_input_channels
           (Values.ListMLInputChannelsRequest.make ?nextToken ?maxResults
              ~membershipIdentifier ())
           (Some Values.ListMLInputChannelsResponse.to_json)
           (Some Values.ListMLInputChannelsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TaggableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_trained_model_inference_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and trainedModelArn =
         flag "trained-model-arn" (optional string)
           ~doc:"STRING TrainedModelArn"
       and trainedModelVersionIdentifier =
         flag "trained-model-version-identifier" (optional string)
           ~doc:"STRING UUID"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_trained_model_inference_jobs
           (Values.ListTrainedModelInferenceJobsRequest.make ?nextToken
              ?maxResults ?trainedModelArn ?trainedModelVersionIdentifier
              ~membershipIdentifier ())
           (Some Values.ListTrainedModelInferenceJobsResponse.to_json)
           (Some Values.ListTrainedModelInferenceJobsResponse.error_to_json)])
let list_trained_model_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON TrainedModelStatus"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID"
       and trainedModelArn =
         flag "trained-model-arn" (required string)
           ~doc:"STRING TrainedModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_trained_model_versions
           (Values.ListTrainedModelVersionsRequest.make ?nextToken
              ?maxResults
              ?status:(Option.map ~f:Values.TrainedModelStatus.of_json status)
              ~membershipIdentifier ~trainedModelArn ())
           (Some Values.ListTrainedModelVersionsResponse.to_json)
           (Some Values.ListTrainedModelVersionsResponse.error_to_json)])
let list_trained_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_trained_models
           (Values.ListTrainedModelsRequest.make ?nextToken ?maxResults
              ~membershipIdentifier ())
           (Some Values.ListTrainedModelsResponse.to_json)
           (Some Values.ListTrainedModelsResponse.error_to_json)])
let list_training_datasets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_training_datasets
           (Values.ListTrainingDatasetsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListTrainingDatasetsResponse.to_json)
           (Some Values.ListTrainingDatasetsResponse.error_to_json)])
let put_configured_audience_model_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and previousPolicyHash =
         flag "previous-policy-hash" (optional string) ~doc:"STRING Hash"
       and policyExistenceCondition =
         flag "policy-existence-condition" (optional json_arg)
           ~doc:"JSON PolicyExistenceCondition"
       and configuredAudienceModelArn =
         flag "configured-audience-model-arn" (required string)
           ~doc:"STRING ConfiguredAudienceModelArn"
       and configuredAudienceModelPolicy =
         flag "configured-audience-model-policy" (required string)
           ~doc:"STRING ResourcePolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_configured_audience_model_policy
           (Values.PutConfiguredAudienceModelPolicyRequest.make
              ?previousPolicyHash
              ?policyExistenceCondition:(Option.map
                                           ~f:Values.PolicyExistenceCondition.of_json
                                           policyExistenceCondition)
              ~configuredAudienceModelArn ~configuredAudienceModelPolicy ())
           (Some Values.PutConfiguredAudienceModelPolicyResponse.to_json)
           (Some
              Values.PutConfiguredAudienceModelPolicyResponse.error_to_json)])
let put_m_l_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID"
       and defaultOutputLocation =
         flag "default-output-location" (required json_arg)
           ~doc:"JSON MLOutputConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_m_l_configuration
           (Values.PutMLConfigurationRequest.make ~membershipIdentifier
              ~defaultOutputLocation:(Values.MLOutputConfiguration.of_json
                                        defaultOutputLocation) ()) None None])
let start_audience_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and audienceGenerationJobArn =
         flag "audience-generation-job-arn" (required string)
           ~doc:"STRING AudienceGenerationJobArn"
       and audienceSize =
         flag "audience-size" (required json_arg) ~doc:"JSON AudienceSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_audience_export_job
           (Values.StartAudienceExportJobRequest.make ?description ~name
              ~audienceGenerationJobArn
              ~audienceSize:(Values.AudienceSize.of_json audienceSize) ())
           None None])
let start_audience_generation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeSeedInOutput =
         flag "include-seed-in-output" (optional bool) ~doc:"BOOL Boolean"
       and collaborationId =
         flag "collaboration-id" (optional string) ~doc:"STRING UUID"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and configuredAudienceModelArn =
         flag "configured-audience-model-arn" (required string)
           ~doc:"STRING ConfiguredAudienceModelArn"
       and seedAudience =
         flag "seed-audience" (required json_arg)
           ~doc:"JSON AudienceGenerationJobDataSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_audience_generation_job
           (Values.StartAudienceGenerationJobRequest.make
              ?includeSeedInOutput ?collaborationId ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~configuredAudienceModelArn
              ~seedAudience:(Values.AudienceGenerationJobDataSource.of_json
                               seedAudience) ())
           (Some Values.StartAudienceGenerationJobResponse.to_json)
           (Some Values.StartAudienceGenerationJobResponse.error_to_json)])
let start_trained_model_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trainedModelVersionIdentifier =
         flag "trained-model-version-identifier" (optional string)
           ~doc:"STRING UUID"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and trainedModelArn =
         flag "trained-model-arn" (required string)
           ~doc:"STRING TrainedModelArn"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID"
       and outputConfiguration =
         flag "output-configuration" (required json_arg)
           ~doc:"JSON TrainedModelExportOutputConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_trained_model_export_job
           (Values.StartTrainedModelExportJobRequest.make
              ?trainedModelVersionIdentifier ?description ~name
              ~trainedModelArn ~membershipIdentifier
              ~outputConfiguration:(Values.TrainedModelExportOutputConfiguration.of_json
                                      outputConfiguration) ()) None None])
let start_trained_model_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trainedModelVersionIdentifier =
         flag "trained-model-version-identifier" (optional string)
           ~doc:"STRING UUID"
       and configuredModelAlgorithmAssociationArn =
         flag "configured-model-algorithm-association-arn" (optional string)
           ~doc:"STRING ConfiguredModelAlgorithmAssociationArn"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and containerExecutionParameters =
         flag "container-execution-parameters" (optional json_arg)
           ~doc:"JSON InferenceContainerExecutionParameters"
       and environment =
         flag "environment" (optional json_arg)
           ~doc:"JSON InferenceEnvironmentMap"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and membershipIdentifier =
         flag "membership-identifier" (required string) ~doc:"STRING UUID"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and trainedModelArn =
         flag "trained-model-arn" (required string)
           ~doc:"STRING TrainedModelArn"
       and resourceConfig =
         flag "resource-config" (required json_arg)
           ~doc:"JSON InferenceResourceConfig"
       and outputConfiguration =
         flag "output-configuration" (required json_arg)
           ~doc:"JSON InferenceOutputConfiguration"
       and dataSource =
         flag "data-source" (required json_arg)
           ~doc:"JSON ModelInferenceDataSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_trained_model_inference_job
           (Values.StartTrainedModelInferenceJobRequest.make
              ?trainedModelVersionIdentifier
              ?configuredModelAlgorithmAssociationArn ?description
              ?containerExecutionParameters:(Option.map
                                               ~f:Values.InferenceContainerExecutionParameters.of_json
                                               containerExecutionParameters)
              ?environment:(Option.map
                              ~f:Values.InferenceEnvironmentMap.of_json
                              environment) ?kmsKeyArn
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~membershipIdentifier ~name ~trainedModelArn
              ~resourceConfig:(Values.InferenceResourceConfig.of_json
                                 resourceConfig)
              ~outputConfiguration:(Values.InferenceOutputConfiguration.of_json
                                      outputConfiguration)
              ~dataSource:(Values.ModelInferenceDataSource.of_json dataSource)
              ()) (Some Values.StartTrainedModelInferenceJobResponse.to_json)
           (Some Values.StartTrainedModelInferenceJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TaggableArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TaggableArn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_configured_audience_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and outputConfig =
         flag "output-config" (optional json_arg)
           ~doc:"JSON ConfiguredAudienceModelOutputConfig"
       and audienceModelArn =
         flag "audience-model-arn" (optional string)
           ~doc:"STRING AudienceModelArn"
       and sharedAudienceMetrics =
         flag "shared-audience-metrics" (optional json_arg)
           ~doc:"JSON MetricsList"
       and minMatchingSeedSize =
         flag "min-matching-seed-size" (optional int)
           ~doc:"INT MinMatchingSeedSize"
       and audienceSizeConfig =
         flag "audience-size-config" (optional json_arg)
           ~doc:"JSON AudienceSizeConfig"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and configuredAudienceModelArn =
         flag "configured-audience-model-arn" (required string)
           ~doc:"STRING ConfiguredAudienceModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_configured_audience_model
           (Values.UpdateConfiguredAudienceModelRequest.make
              ?outputConfig:(Option.map
                               ~f:Values.ConfiguredAudienceModelOutputConfig.of_json
                               outputConfig) ?audienceModelArn
              ?sharedAudienceMetrics:(Option.map
                                        ~f:Values.MetricsList.of_json
                                        sharedAudienceMetrics)
              ?minMatchingSeedSize
              ?audienceSizeConfig:(Option.map
                                     ~f:Values.AudienceSizeConfig.of_json
                                     audienceSizeConfig) ?description
              ~configuredAudienceModelArn ())
           (Some Values.UpdateConfiguredAudienceModelResponse.to_json)
           (Some Values.UpdateConfiguredAudienceModelResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("cancel-trained-model", cancel_trained_model);
    ("cancel-trained-model-inference-job",
      cancel_trained_model_inference_job);
    ("create-audience-model", create_audience_model);
    ("create-configured-audience-model", create_configured_audience_model);
    ("create-configured-model-algorithm", create_configured_model_algorithm);
    ("create-configured-model-algorithm-association",
      create_configured_model_algorithm_association);
    ("create-m-l-input-channel", create_m_l_input_channel);
    ("create-trained-model", create_trained_model);
    ("create-training-dataset", create_training_dataset);
    ("delete-audience-generation-job", delete_audience_generation_job);
    ("delete-audience-model", delete_audience_model);
    ("delete-configured-audience-model", delete_configured_audience_model);
    ("delete-configured-audience-model-policy",
      delete_configured_audience_model_policy);
    ("delete-configured-model-algorithm", delete_configured_model_algorithm);
    ("delete-configured-model-algorithm-association",
      delete_configured_model_algorithm_association);
    ("delete-m-l-configuration", delete_m_l_configuration);
    ("delete-m-l-input-channel-data", delete_m_l_input_channel_data);
    ("delete-trained-model-output", delete_trained_model_output);
    ("delete-training-dataset", delete_training_dataset);
    ("get-audience-generation-job", get_audience_generation_job);
    ("get-audience-model", get_audience_model);
    ("get-collaboration-configured-model-algorithm-association",
      get_collaboration_configured_model_algorithm_association);
    ("get-collaboration-m-l-input-channel",
      get_collaboration_m_l_input_channel);
    ("get-collaboration-trained-model", get_collaboration_trained_model);
    ("get-configured-audience-model", get_configured_audience_model);
    ("get-configured-audience-model-policy",
      get_configured_audience_model_policy);
    ("get-configured-model-algorithm", get_configured_model_algorithm);
    ("get-configured-model-algorithm-association",
      get_configured_model_algorithm_association);
    ("get-m-l-configuration", get_m_l_configuration);
    ("get-m-l-input-channel", get_m_l_input_channel);
    ("get-trained-model", get_trained_model);
    ("get-trained-model-inference-job", get_trained_model_inference_job);
    ("get-training-dataset", get_training_dataset);
    ("list-audience-export-jobs", list_audience_export_jobs);
    ("list-audience-generation-jobs", list_audience_generation_jobs);
    ("list-audience-models", list_audience_models);
    ("list-collaboration-configured-model-algorithm-associations",
      list_collaboration_configured_model_algorithm_associations);
    ("list-collaboration-m-l-input-channels",
      list_collaboration_m_l_input_channels);
    ("list-collaboration-trained-model-export-jobs",
      list_collaboration_trained_model_export_jobs);
    ("list-collaboration-trained-model-inference-jobs",
      list_collaboration_trained_model_inference_jobs);
    ("list-collaboration-trained-models", list_collaboration_trained_models);
    ("list-configured-audience-models", list_configured_audience_models);
    ("list-configured-model-algorithm-associations",
      list_configured_model_algorithm_associations);
    ("list-configured-model-algorithms", list_configured_model_algorithms);
    ("list-m-l-input-channels", list_m_l_input_channels);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-trained-model-inference-jobs", list_trained_model_inference_jobs);
    ("list-trained-model-versions", list_trained_model_versions);
    ("list-trained-models", list_trained_models);
    ("list-training-datasets", list_training_datasets);
    ("put-configured-audience-model-policy",
      put_configured_audience_model_policy);
    ("put-m-l-configuration", put_m_l_configuration);
    ("start-audience-export-job", start_audience_export_job);
    ("start-audience-generation-job", start_audience_generation_job);
    ("start-trained-model-export-job", start_trained_model_export_job);
    ("start-trained-model-inference-job", start_trained_model_inference_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-configured-audience-model", update_configured_audience_model)]
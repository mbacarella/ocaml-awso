(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and vectorBucketArn =
         flag "vector-bucket-arn" (optional string)
           ~doc:"STRING VectorBucketArn"
       and metadataConfiguration =
         flag "metadata-configuration" (optional json_arg)
           ~doc:"JSON MetadataConfiguration"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING IndexName"
       and dataType =
         flag "data-type" (required json_arg) ~doc:"JSON DataType"
       and dimension = flag "dimension" (required int) ~doc:"INT Dimension"
       and distanceMetric =
         flag "distance-metric" (required json_arg)
           ~doc:"JSON DistanceMetric" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_index
           (Values.CreateIndexInput.make ?vectorBucketName ?vectorBucketArn
              ?metadataConfiguration:(Option.map
                                        ~f:Values.MetadataConfiguration.of_json
                                        metadataConfiguration)
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~indexName
              ~dataType:(Values.DataType.of_json dataType) ~dimension
              ~distanceMetric:(Values.DistanceMetric.of_json distanceMetric)
              ()) (Some Values.CreateIndexOutput.to_json)
           (Some Values.CreateIndexOutput.error_to_json)])
let create_vector_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and vectorBucketName =
         flag "vector-bucket-name" (required string)
           ~doc:"STRING VectorBucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vector_bucket
           (Values.CreateVectorBucketInput.make
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~vectorBucketName ())
           (Some Values.CreateVectorBucketOutput.to_json)
           (Some Values.CreateVectorBucketOutput.error_to_json)])
let delete_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and indexArn =
         flag "index-arn" (optional string) ~doc:"STRING IndexArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_index
           (Values.DeleteIndexInput.make ?vectorBucketName ?indexName
              ?indexArn ()) (Some Values.DeleteIndexOutput.to_json)
           (Some Values.DeleteIndexOutput.error_to_json)])
let delete_vector_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and vectorBucketArn =
         flag "vector-bucket-arn" (optional string)
           ~doc:"STRING VectorBucketArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vector_bucket
           (Values.DeleteVectorBucketInput.make ?vectorBucketName
              ?vectorBucketArn ())
           (Some Values.DeleteVectorBucketOutput.to_json)
           (Some Values.DeleteVectorBucketOutput.error_to_json)])
let delete_vector_bucket_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and vectorBucketArn =
         flag "vector-bucket-arn" (optional string)
           ~doc:"STRING VectorBucketArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vector_bucket_policy
           (Values.DeleteVectorBucketPolicyInput.make ?vectorBucketName
              ?vectorBucketArn ())
           (Some Values.DeleteVectorBucketPolicyOutput.to_json)
           (Some Values.DeleteVectorBucketPolicyOutput.error_to_json)])
let delete_vectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and indexArn =
         flag "index-arn" (optional string) ~doc:"STRING IndexArn"
       and keys =
         flag "keys" (required json_arg) ~doc:"JSON DeleteVectorsInputList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vectors
           (Values.DeleteVectorsInput.make ?vectorBucketName ?indexName
              ?indexArn ~keys:(Values.DeleteVectorsInputList.of_json keys) ())
           (Some Values.DeleteVectorsOutput.to_json)
           (Some Values.DeleteVectorsOutput.error_to_json)])
let get_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and indexArn =
         flag "index-arn" (optional string) ~doc:"STRING IndexArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_index
           (Values.GetIndexInput.make ?vectorBucketName ?indexName ?indexArn
              ()) (Some Values.GetIndexOutput.to_json)
           (Some Values.GetIndexOutput.error_to_json)])
let get_vector_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and vectorBucketArn =
         flag "vector-bucket-arn" (optional string)
           ~doc:"STRING VectorBucketArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vector_bucket
           (Values.GetVectorBucketInput.make ?vectorBucketName
              ?vectorBucketArn ())
           (Some Values.GetVectorBucketOutput.to_json)
           (Some Values.GetVectorBucketOutput.error_to_json)])
let get_vector_bucket_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and vectorBucketArn =
         flag "vector-bucket-arn" (optional string)
           ~doc:"STRING VectorBucketArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vector_bucket_policy
           (Values.GetVectorBucketPolicyInput.make ?vectorBucketName
              ?vectorBucketArn ())
           (Some Values.GetVectorBucketPolicyOutput.to_json)
           (Some Values.GetVectorBucketPolicyOutput.error_to_json)])
let get_vectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and indexArn =
         flag "index-arn" (optional string) ~doc:"STRING IndexArn"
       and returnData =
         flag "return-data" (optional bool) ~doc:"BOOL Boolean"
       and returnMetadata =
         flag "return-metadata" (optional bool) ~doc:"BOOL Boolean"
       and keys =
         flag "keys" (required json_arg) ~doc:"JSON GetVectorsInputList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vectors
           (Values.GetVectorsInput.make ?vectorBucketName ?indexName
              ?indexArn ?returnData ?returnMetadata
              ~keys:(Values.GetVectorsInputList.of_json keys) ())
           (Some Values.GetVectorsOutput.to_json)
           (Some Values.GetVectorsOutput.error_to_json)])
let list_indexes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and vectorBucketArn =
         flag "vector-bucket-arn" (optional string)
           ~doc:"STRING VectorBucketArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListIndexesMaxResults"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListIndexesNextToken"
       and prefix =
         flag "prefix" (optional string) ~doc:"STRING ListIndexesPrefix" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_indexes
           (Values.ListIndexesInput.make ?vectorBucketName ?vectorBucketArn
              ?maxResults ?nextToken ?prefix ())
           (Some Values.ListIndexesOutput.to_json)
           (Some Values.ListIndexesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_vector_buckets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListVectorBucketsMaxResults"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListVectorBucketsNextToken"
       and prefix =
         flag "prefix" (optional string)
           ~doc:"STRING ListVectorBucketsPrefix" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_vector_buckets
           (Values.ListVectorBucketsInput.make ?maxResults ?nextToken ?prefix
              ()) (Some Values.ListVectorBucketsOutput.to_json)
           (Some Values.ListVectorBucketsOutput.error_to_json)])
let list_vectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and indexArn =
         flag "index-arn" (optional string) ~doc:"STRING IndexArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListVectorsMaxResults"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListVectorsNextToken"
       and segmentCount =
         flag "segment-count" (optional int)
           ~doc:"INT ListVectorsSegmentCount"
       and segmentIndex =
         flag "segment-index" (optional int)
           ~doc:"INT ListVectorsSegmentIndex"
       and returnData =
         flag "return-data" (optional bool) ~doc:"BOOL Boolean"
       and returnMetadata =
         flag "return-metadata" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_vectors
           (Values.ListVectorsInput.make ?vectorBucketName ?indexName
              ?indexArn ?maxResults ?nextToken ?segmentCount ?segmentIndex
              ?returnData ?returnMetadata ())
           (Some Values.ListVectorsOutput.to_json)
           (Some Values.ListVectorsOutput.error_to_json)])
let put_vector_bucket_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and vectorBucketArn =
         flag "vector-bucket-arn" (optional string)
           ~doc:"STRING VectorBucketArn"
       and policy =
         flag "policy" (required string) ~doc:"STRING VectorBucketPolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_vector_bucket_policy
           (Values.PutVectorBucketPolicyInput.make ?vectorBucketName
              ?vectorBucketArn ~policy ())
           (Some Values.PutVectorBucketPolicyOutput.to_json)
           (Some Values.PutVectorBucketPolicyOutput.error_to_json)])
let put_vectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and indexArn =
         flag "index-arn" (optional string) ~doc:"STRING IndexArn"
       and vectors =
         flag "vectors" (required json_arg) ~doc:"JSON PutVectorsInputList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_vectors
           (Values.PutVectorsInput.make ?vectorBucketName ?indexName
              ?indexArn ~vectors:(Values.PutVectorsInputList.of_json vectors)
              ()) (Some Values.PutVectorsOutput.to_json)
           (Some Values.PutVectorsOutput.error_to_json)])
let query_vectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vectorBucketName =
         flag "vector-bucket-name" (optional string)
           ~doc:"STRING VectorBucketName"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and indexArn =
         flag "index-arn" (optional string) ~doc:"STRING IndexArn"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Document"
       and returnMetadata =
         flag "return-metadata" (optional bool) ~doc:"BOOL Boolean"
       and returnDistance =
         flag "return-distance" (optional bool) ~doc:"BOOL Boolean"
       and topK = flag "top-k" (required int) ~doc:"INT TopK"
       and queryVector =
         flag "query-vector" (required json_arg) ~doc:"JSON VectorData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.query_vectors
           (Values.QueryVectorsInput.make ?vectorBucketName ?indexName
              ?indexArn
              ?filter:(Option.map ~f:Values.Document.of_json filter)
              ?returnMetadata ?returnDistance ~topK
              ~queryVector:(Values.VectorData.of_json queryVector) ())
           (Some Values.QueryVectorsOutput.to_json)
           (Some Values.QueryVectorsOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceARN"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagsMap.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceARN"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-index", create_index);
    ("create-vector-bucket", create_vector_bucket);
    ("delete-index", delete_index);
    ("delete-vector-bucket", delete_vector_bucket);
    ("delete-vector-bucket-policy", delete_vector_bucket_policy);
    ("delete-vectors", delete_vectors);
    ("get-index", get_index);
    ("get-vector-bucket", get_vector_bucket);
    ("get-vector-bucket-policy", get_vector_bucket_policy);
    ("get-vectors", get_vectors);
    ("list-indexes", list_indexes);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-vector-buckets", list_vector_buckets);
    ("list-vectors", list_vectors);
    ("put-vector-bucket-policy", put_vector_bucket_policy);
    ("put-vectors", put_vectors);
    ("query-vectors", query_vectors);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
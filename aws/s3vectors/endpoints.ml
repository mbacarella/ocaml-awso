(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateIndex: (CreateIndexInput.t, CreateIndexOutput.t,
  CreateIndexOutput.error) t 
  | CreateVectorBucket: (CreateVectorBucketInput.t,
  CreateVectorBucketOutput.t, CreateVectorBucketOutput.error) t 
  | DeleteIndex: (DeleteIndexInput.t, DeleteIndexOutput.t,
  DeleteIndexOutput.error) t 
  | DeleteVectorBucket: (DeleteVectorBucketInput.t,
  DeleteVectorBucketOutput.t, DeleteVectorBucketOutput.error) t 
  | DeleteVectorBucketPolicy: (DeleteVectorBucketPolicyInput.t,
  DeleteVectorBucketPolicyOutput.t, DeleteVectorBucketPolicyOutput.error) t 
  | DeleteVectors: (DeleteVectorsInput.t, DeleteVectorsOutput.t,
  DeleteVectorsOutput.error) t 
  | GetIndex: (GetIndexInput.t, GetIndexOutput.t, GetIndexOutput.error) t 
  | GetVectorBucket: (GetVectorBucketInput.t, GetVectorBucketOutput.t,
  GetVectorBucketOutput.error) t 
  | GetVectorBucketPolicy: (GetVectorBucketPolicyInput.t,
  GetVectorBucketPolicyOutput.t, GetVectorBucketPolicyOutput.error) t 
  | GetVectors: (GetVectorsInput.t, GetVectorsOutput.t,
  GetVectorsOutput.error) t 
  | ListIndexes: (ListIndexesInput.t, ListIndexesOutput.t,
  ListIndexesOutput.error) t 
  | ListTagsForResource: (ListTagsForResourceInput.t,
  ListTagsForResourceOutput.t, ListTagsForResourceOutput.error) t 
  | ListVectorBuckets: (ListVectorBucketsInput.t, ListVectorBucketsOutput.t,
  ListVectorBucketsOutput.error) t 
  | ListVectors: (ListVectorsInput.t, ListVectorsOutput.t,
  ListVectorsOutput.error) t 
  | PutVectorBucketPolicy: (PutVectorBucketPolicyInput.t,
  PutVectorBucketPolicyOutput.t, PutVectorBucketPolicyOutput.error) t 
  | PutVectors: (PutVectorsInput.t, PutVectorsOutput.t,
  PutVectorsOutput.error) t 
  | QueryVectors: (QueryVectorsInput.t, QueryVectorsOutput.t,
  QueryVectorsOutput.error) t 
  | TagResource: (TagResourceInput.t, TagResourceOutput.t,
  TagResourceOutput.error) t 
  | UntagResource: (UntagResourceInput.t, UntagResourceOutput.t,
  UntagResourceOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateIndex -> `POST
  | CreateVectorBucket -> `POST
  | DeleteIndex -> `POST
  | DeleteVectorBucket -> `POST
  | DeleteVectorBucketPolicy -> `POST
  | DeleteVectors -> `POST
  | GetIndex -> `POST
  | GetVectorBucket -> `POST
  | GetVectorBucketPolicy -> `POST
  | GetVectors -> `POST
  | ListIndexes -> `POST
  | ListTagsForResource -> `GET
  | ListVectorBuckets -> `POST
  | ListVectors -> `POST
  | PutVectorBucketPolicy -> `POST
  | PutVectors -> `POST
  | QueryVectors -> `POST
  | TagResource -> `POST
  | UntagResource -> `DELETE
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateIndex -> (Format.kasprintf Uri.of_string) "/CreateIndex"
      | CreateVectorBucket ->
          (Format.kasprintf Uri.of_string) "/CreateVectorBucket"
      | DeleteIndex -> (Format.kasprintf Uri.of_string) "/DeleteIndex"
      | DeleteVectorBucket ->
          (Format.kasprintf Uri.of_string) "/DeleteVectorBucket"
      | DeleteVectorBucketPolicy ->
          (Format.kasprintf Uri.of_string) "/DeleteVectorBucketPolicy"
      | DeleteVectors -> (Format.kasprintf Uri.of_string) "/DeleteVectors"
      | GetIndex -> (Format.kasprintf Uri.of_string) "/GetIndex"
      | GetVectorBucket ->
          (Format.kasprintf Uri.of_string) "/GetVectorBucket"
      | GetVectorBucketPolicy ->
          (Format.kasprintf Uri.of_string) "/GetVectorBucketPolicy"
      | GetVectors -> (Format.kasprintf Uri.of_string) "/GetVectors"
      | ListIndexes -> (Format.kasprintf Uri.of_string) "/ListIndexes"
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (ResourceARN.to_header x.ListTagsForResourceInput.resourceArn)
      | ListVectorBuckets ->
          (Format.kasprintf Uri.of_string) "/ListVectorBuckets"
      | ListVectors -> (Format.kasprintf Uri.of_string) "/ListVectors"
      | PutVectorBucketPolicy ->
          (Format.kasprintf Uri.of_string) "/PutVectorBucketPolicy"
      | PutVectors -> (Format.kasprintf Uri.of_string) "/PutVectors"
      | QueryVectors -> (Format.kasprintf Uri.of_string) "/QueryVectors"
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (ResourceARN.to_header x.TagResourceInput.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (ResourceARN.to_header x.UntagResourceInput.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateIndex ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CreateIndexInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map req.CreateIndexInput.vectorBucketArn
                        ~f:(fun x ->
                              ("vectorBucketArn",
                                (VectorBucketArn.to_value x)));
                      Some
                        ("indexName",
                          (IndexName.to_value req.CreateIndexInput.indexName));
                      Some
                        ("dataType",
                          (DataType.to_value req.CreateIndexInput.dataType));
                      Some
                        ("dimension",
                          (Dimension.to_value req.CreateIndexInput.dimension));
                      Some
                        ("distanceMetric",
                          (DistanceMetric.to_value
                             req.CreateIndexInput.distanceMetric));
                      Option.map req.CreateIndexInput.metadataConfiguration
                        ~f:(fun x ->
                              ("metadataConfiguration",
                                (MetadataConfiguration.to_value x)));
                      Option.map req.CreateIndexInput.encryptionConfiguration
                        ~f:(fun x ->
                              ("encryptionConfiguration",
                                (EncryptionConfiguration.to_value x)));
                      Option.map req.CreateIndexInput.tags
                        ~f:(fun x -> ("tags", (TagsMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateVectorBucket ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("vectorBucketName",
                           (VectorBucketName.to_value
                              req.CreateVectorBucketInput.vectorBucketName));
                      Option.map
                        req.CreateVectorBucketInput.encryptionConfiguration
                        ~f:(fun x ->
                              ("encryptionConfiguration",
                                (EncryptionConfiguration.to_value x)));
                      Option.map req.CreateVectorBucketInput.tags
                        ~f:(fun x -> ("tags", (TagsMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteIndex ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.DeleteIndexInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map req.DeleteIndexInput.indexName
                        ~f:(fun x -> ("indexName", (IndexName.to_value x)));
                      Option.map req.DeleteIndexInput.indexArn
                        ~f:(fun x -> ("indexArn", (IndexArn.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteVectorBucket ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.DeleteVectorBucketInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map req.DeleteVectorBucketInput.vectorBucketArn
                        ~f:(fun x ->
                              ("vectorBucketArn",
                                (VectorBucketArn.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteVectorBucketPolicy ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.DeleteVectorBucketPolicyInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map
                        req.DeleteVectorBucketPolicyInput.vectorBucketArn
                        ~f:(fun x ->
                              ("vectorBucketArn",
                                (VectorBucketArn.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteVectors ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.DeleteVectorsInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map req.DeleteVectorsInput.indexName
                        ~f:(fun x -> ("indexName", (IndexName.to_value x)));
                      Option.map req.DeleteVectorsInput.indexArn
                        ~f:(fun x -> ("indexArn", (IndexArn.to_value x)));
                      Some
                        ("keys",
                          (DeleteVectorsInputList.to_value
                             req.DeleteVectorsInput.keys))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetIndex ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GetIndexInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map req.GetIndexInput.indexName
                        ~f:(fun x -> ("indexName", (IndexName.to_value x)));
                      Option.map req.GetIndexInput.indexArn
                        ~f:(fun x -> ("indexArn", (IndexArn.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetVectorBucket ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GetVectorBucketInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map req.GetVectorBucketInput.vectorBucketArn
                        ~f:(fun x ->
                              ("vectorBucketArn",
                                (VectorBucketArn.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetVectorBucketPolicy ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.GetVectorBucketPolicyInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map
                        req.GetVectorBucketPolicyInput.vectorBucketArn
                        ~f:(fun x ->
                              ("vectorBucketArn",
                                (VectorBucketArn.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetVectors ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GetVectorsInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map req.GetVectorsInput.indexName
                        ~f:(fun x -> ("indexName", (IndexName.to_value x)));
                      Option.map req.GetVectorsInput.indexArn
                        ~f:(fun x -> ("indexArn", (IndexArn.to_value x)));
                      Some
                        ("keys",
                          (GetVectorsInputList.to_value
                             req.GetVectorsInput.keys));
                      Option.map req.GetVectorsInput.returnData
                        ~f:(fun x -> ("returnData", (Boolean.to_value x)));
                      Option.map req.GetVectorsInput.returnMetadata
                        ~f:(fun x -> ("returnMetadata", (Boolean.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListIndexes ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListIndexesInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map req.ListIndexesInput.vectorBucketArn
                        ~f:(fun x ->
                              ("vectorBucketArn",
                                (VectorBucketArn.to_value x)));
                      Option.map req.ListIndexesInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListIndexesMaxResults.to_value x)));
                      Option.map req.ListIndexesInput.nextToken
                        ~f:(fun x ->
                              ("nextToken",
                                (ListIndexesNextToken.to_value x)));
                      Option.map req.ListIndexesInput.prefix
                        ~f:(fun x ->
                              ("prefix", (ListIndexesPrefix.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListVectorBuckets ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListVectorBucketsInput.maxResults
                         ~f:(fun x ->
                               ("maxResults",
                                 (ListVectorBucketsMaxResults.to_value x)));
                      Option.map req.ListVectorBucketsInput.nextToken
                        ~f:(fun x ->
                              ("nextToken",
                                (ListVectorBucketsNextToken.to_value x)));
                      Option.map req.ListVectorBucketsInput.prefix
                        ~f:(fun x ->
                              ("prefix",
                                (ListVectorBucketsPrefix.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListVectors ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListVectorsInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map req.ListVectorsInput.indexName
                        ~f:(fun x -> ("indexName", (IndexName.to_value x)));
                      Option.map req.ListVectorsInput.indexArn
                        ~f:(fun x -> ("indexArn", (IndexArn.to_value x)));
                      Option.map req.ListVectorsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListVectorsMaxResults.to_value x)));
                      Option.map req.ListVectorsInput.nextToken
                        ~f:(fun x ->
                              ("nextToken",
                                (ListVectorsNextToken.to_value x)));
                      Option.map req.ListVectorsInput.segmentCount
                        ~f:(fun x ->
                              ("segmentCount",
                                (ListVectorsSegmentCount.to_value x)));
                      Option.map req.ListVectorsInput.segmentIndex
                        ~f:(fun x ->
                              ("segmentIndex",
                                (ListVectorsSegmentIndex.to_value x)));
                      Option.map req.ListVectorsInput.returnData
                        ~f:(fun x -> ("returnData", (Boolean.to_value x)));
                      Option.map req.ListVectorsInput.returnMetadata
                        ~f:(fun x -> ("returnMetadata", (Boolean.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutVectorBucketPolicy ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.PutVectorBucketPolicyInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map
                        req.PutVectorBucketPolicyInput.vectorBucketArn
                        ~f:(fun x ->
                              ("vectorBucketArn",
                                (VectorBucketArn.to_value x)));
                      Some
                        ("policy",
                          (VectorBucketPolicy.to_value
                             req.PutVectorBucketPolicyInput.policy))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutVectors ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.PutVectorsInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map req.PutVectorsInput.indexName
                        ~f:(fun x -> ("indexName", (IndexName.to_value x)));
                      Option.map req.PutVectorsInput.indexArn
                        ~f:(fun x -> ("indexArn", (IndexArn.to_value x)));
                      Some
                        ("vectors",
                          (PutVectorsInputList.to_value
                             req.PutVectorsInput.vectors))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | QueryVectors ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.QueryVectorsInput.vectorBucketName
                         ~f:(fun x ->
                               ("vectorBucketName",
                                 (VectorBucketName.to_value x)));
                      Option.map req.QueryVectorsInput.indexName
                        ~f:(fun x -> ("indexName", (IndexName.to_value x)));
                      Option.map req.QueryVectorsInput.indexArn
                        ~f:(fun x -> ("indexArn", (IndexArn.to_value x)));
                      Some
                        ("topK", (TopK.to_value req.QueryVectorsInput.topK));
                      Some
                        ("queryVector",
                          (VectorData.to_value
                             req.QueryVectorsInput.queryVector));
                      Option.map req.QueryVectorsInput.filter
                        ~f:(fun x -> ("filter", (Document.to_value x)));
                      Option.map req.QueryVectorsInput.returnMetadata
                        ~f:(fun x -> ("returnMetadata", (Boolean.to_value x)));
                      Option.map req.QueryVectorsInput.returnDistance
                        ~f:(fun x -> ("returnDistance", (Boolean.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tags",
                           (TagsMap.to_value req.TagResourceInput.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CreateIndex ->
      if is_success
      then Ok (CreateIndexOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateIndexOutput.error_of_json))
  | CreateVectorBucket ->
      if is_success
      then Ok (CreateVectorBucketOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some CreateVectorBucketOutput.error_of_json))
  | DeleteIndex ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteIndexOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteIndexOutput.error_of_json))
  | DeleteVectorBucket ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteVectorBucketOutput.of_header_and_body (headers, ()))
      else
        Error (parse_aws_error (Some DeleteVectorBucketOutput.error_of_json))
  | DeleteVectorBucketPolicy ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteVectorBucketPolicyOutput.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error
             (Some DeleteVectorBucketPolicyOutput.error_of_json))
  | DeleteVectors ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteVectorsOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteVectorsOutput.error_of_json))
  | GetIndex ->
      if is_success
      then Ok (GetIndexOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetIndexOutput.error_of_json))
  | GetVectorBucket ->
      if is_success
      then Ok (GetVectorBucketOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetVectorBucketOutput.error_of_json))
  | GetVectorBucketPolicy ->
      if is_success
      then Ok (GetVectorBucketPolicyOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetVectorBucketPolicyOutput.error_of_json))
  | GetVectors ->
      if is_success
      then Ok (GetVectorsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetVectorsOutput.error_of_json))
  | ListIndexes ->
      if is_success
      then Ok (ListIndexesOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListIndexesOutput.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceOutput.error_of_json))
  | ListVectorBuckets ->
      if is_success
      then Ok (ListVectorBucketsOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListVectorBucketsOutput.error_of_json))
  | ListVectors ->
      if is_success
      then Ok (ListVectorsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListVectorsOutput.error_of_json))
  | PutVectorBucketPolicy ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (PutVectorBucketPolicyOutput.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error (Some PutVectorBucketPolicyOutput.error_of_json))
  | PutVectors ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (PutVectorsOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some PutVectorsOutput.error_of_json))
  | QueryVectors ->
      if is_success
      then Ok (QueryVectorsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some QueryVectorsOutput.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceOutput.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceOutput.error_of_json))
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and topicArn =
         flag "topic-arn" (required string) ~doc:"STRING topicARN"
       and label = flag "label" (required string) ~doc:"STRING label"
       and aWSAccountId =
         flag "a-w-s-account-id" (required json_arg)
           ~doc:"JSON DelegatesList"
       and actionName =
         flag "action-name" (required json_arg) ~doc:"JSON ActionsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_permission
           (Values.AddPermissionInput.make ~topicArn ~label
              ~aWSAccountId:(Values.DelegatesList.of_json aWSAccountId)
              ~actionName:(Values.ActionsList.of_json actionName) ()) None
           None])
let check_if_phone_number_is_opted_out =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumber =
         flag "phone-number" (required string) ~doc:"STRING PhoneNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.check_if_phone_number_is_opted_out
           (Values.CheckIfPhoneNumberIsOptedOutInput.make ~phoneNumber ())
           (Some Values.CheckIfPhoneNumberIsOptedOutResponse.to_json)
           (Some Values.CheckIfPhoneNumberIsOptedOutResponse.error_to_json)])
let confirm_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticateOnUnsubscribe =
         flag "authenticate-on-unsubscribe" (optional string)
           ~doc:"STRING authenticateOnUnsubscribe"
       and topicArn =
         flag "topic-arn" (required string) ~doc:"STRING topicARN"
       and token = flag "token" (required string) ~doc:"STRING token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.confirm_subscription
           (Values.ConfirmSubscriptionInput.make ?authenticateOnUnsubscribe
              ~topicArn ~token ())
           (Some Values.ConfirmSubscriptionResponse.to_json)
           (Some Values.ConfirmSubscriptionResponse.error_to_json)])
let create_platform_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String"
       and platform = flag "platform" (required string) ~doc:"STRING String"
       and attributes =
         flag "attributes" (required json_arg) ~doc:"JSON MapStringToString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_platform_application
           (Values.CreatePlatformApplicationInput.make ~name ~platform
              ~attributes:(Values.MapStringToString.of_json attributes) ())
           (Some Values.CreatePlatformApplicationResponse.to_json)
           (Some Values.CreatePlatformApplicationResponse.error_to_json)])
let create_platform_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customUserData =
         flag "custom-user-data" (optional string) ~doc:"STRING String"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON MapStringToString"
       and platformApplicationArn =
         flag "platform-application-arn" (required string)
           ~doc:"STRING String"
       and token = flag "token" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_platform_endpoint
           (Values.CreatePlatformEndpointInput.make ?customUserData
              ?attributes:(Option.map ~f:Values.MapStringToString.of_json
                             attributes) ~platformApplicationArn ~token ())
           (Some Values.CreateEndpointResponse.to_json)
           (Some Values.CreateEndpointResponse.error_to_json)])
let create_s_m_s_sandbox_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and languageCode =
         flag "language-code" (optional json_arg)
           ~doc:"JSON LanguageCodeString"
       and phoneNumber =
         flag "phone-number" (required string)
           ~doc:"STRING PhoneNumberString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_s_m_s_sandbox_phone_number
           (Values.CreateSMSSandboxPhoneNumberInput.make
              ?languageCode:(Option.map ~f:Values.LanguageCodeString.of_json
                               languageCode) ~phoneNumber ())
           (Some Values.CreateSMSSandboxPhoneNumberResult.to_json)
           (Some Values.CreateSMSSandboxPhoneNumberResult.error_to_json)])
let create_topic =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON TopicAttributesMap"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING topicName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_topic
           (Values.CreateTopicInput.make
              ?attributes:(Option.map ~f:Values.TopicAttributesMap.of_json
                             attributes)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.CreateTopicResponse.to_json)
           (Some Values.CreateTopicResponse.error_to_json)])
let delete_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_endpoint
           (Values.DeleteEndpointInput.make ~endpointArn ()) None None])
let delete_platform_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and platformApplicationArn =
         flag "platform-application-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_platform_application
           (Values.DeletePlatformApplicationInput.make
              ~platformApplicationArn ()) None None])
let delete_s_m_s_sandbox_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumber =
         flag "phone-number" (required string)
           ~doc:"STRING PhoneNumberString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_s_m_s_sandbox_phone_number
           (Values.DeleteSMSSandboxPhoneNumberInput.make ~phoneNumber ())
           (Some Values.DeleteSMSSandboxPhoneNumberResult.to_json)
           (Some Values.DeleteSMSSandboxPhoneNumberResult.error_to_json)])
let delete_topic =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and topicArn =
         flag "topic-arn" (required string) ~doc:"STRING topicARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_topic (Values.DeleteTopicInput.make ~topicArn ()) None
           None])
let get_endpoint_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_endpoint_attributes
           (Values.GetEndpointAttributesInput.make ~endpointArn ())
           (Some Values.GetEndpointAttributesResponse.to_json)
           (Some Values.GetEndpointAttributesResponse.error_to_json)])
let get_platform_application_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and platformApplicationArn =
         flag "platform-application-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_platform_application_attributes
           (Values.GetPlatformApplicationAttributesInput.make
              ~platformApplicationArn ())
           (Some Values.GetPlatformApplicationAttributesResponse.to_json)
           (Some
              Values.GetPlatformApplicationAttributesResponse.error_to_json)])
let get_s_m_s_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON ListString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_s_m_s_attributes
           (Values.GetSMSAttributesInput.make
              ?attributes:(Option.map ~f:Values.ListString.of_json attributes)
              ()) (Some Values.GetSMSAttributesResponse.to_json)
           (Some Values.GetSMSAttributesResponse.error_to_json)])
let get_s_m_s_sandbox_account_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_s_m_s_sandbox_account_status
           (Values.GetSMSSandboxAccountStatusInput.make ())
           (Some Values.GetSMSSandboxAccountStatusResult.to_json)
           (Some Values.GetSMSSandboxAccountStatusResult.error_to_json)])
let get_subscription_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionArn =
         flag "subscription-arn" (required string)
           ~doc:"STRING subscriptionARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_subscription_attributes
           (Values.GetSubscriptionAttributesInput.make ~subscriptionArn ())
           (Some Values.GetSubscriptionAttributesResponse.to_json)
           (Some Values.GetSubscriptionAttributesResponse.error_to_json)])
let get_topic_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and topicArn =
         flag "topic-arn" (required string) ~doc:"STRING topicARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_topic_attributes
           (Values.GetTopicAttributesInput.make ~topicArn ())
           (Some Values.GetTopicAttributesResponse.to_json)
           (Some Values.GetTopicAttributesResponse.error_to_json)])
let list_endpoints_by_platform_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and platformApplicationArn =
         flag "platform-application-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_endpoints_by_platform_application
           (Values.ListEndpointsByPlatformApplicationInput.make ?nextToken
              ~platformApplicationArn ())
           (Some Values.ListEndpointsByPlatformApplicationResponse.to_json)
           (Some
              Values.ListEndpointsByPlatformApplicationResponse.error_to_json)])
let list_origination_numbers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxItemsListOriginationNumbers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_origination_numbers
           (Values.ListOriginationNumbersRequest.make ?nextToken ?maxResults
              ()) (Some Values.ListOriginationNumbersResult.to_json)
           (Some Values.ListOriginationNumbersResult.error_to_json)])
let list_phone_numbers_opted_out =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String__lc1" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_phone_numbers_opted_out
           (Values.ListPhoneNumbersOptedOutInput.make ?nextToken ())
           (Some Values.ListPhoneNumbersOptedOutResponse.to_json)
           (Some Values.ListPhoneNumbersOptedOutResponse.error_to_json)])
let list_platform_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_platform_applications
           (Values.ListPlatformApplicationsInput.make ?nextToken ())
           (Some Values.ListPlatformApplicationsResponse.to_json)
           (Some Values.ListPlatformApplicationsResponse.error_to_json)])
let list_s_m_s_sandbox_phone_numbers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_s_m_s_sandbox_phone_numbers
           (Values.ListSMSSandboxPhoneNumbersInput.make ?nextToken
              ?maxResults ())
           (Some Values.ListSMSSandboxPhoneNumbersResult.to_json)
           (Some Values.ListSMSSandboxPhoneNumbersResult.error_to_json)])
let list_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_subscriptions
           (Values.ListSubscriptionsInput.make ?nextToken ())
           (Some Values.ListSubscriptionsResponse.to_json)
           (Some Values.ListSubscriptionsResponse.error_to_json)])
let list_subscriptions_by_topic =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and topicArn =
         flag "topic-arn" (required string) ~doc:"STRING topicARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_subscriptions_by_topic
           (Values.ListSubscriptionsByTopicInput.make ?nextToken ~topicArn ())
           (Some Values.ListSubscriptionsByTopicResponse.to_json)
           (Some Values.ListSubscriptionsByTopicResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_topics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_topics (Values.ListTopicsInput.make ?nextToken ())
           (Some Values.ListTopicsResponse.to_json)
           (Some Values.ListTopicsResponse.error_to_json)])
let opt_in_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumber =
         flag "phone-number" (required string) ~doc:"STRING PhoneNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.opt_in_phone_number
           (Values.OptInPhoneNumberInput.make ~phoneNumber ())
           (Some Values.OptInPhoneNumberResponse.to_json)
           (Some Values.OptInPhoneNumberResponse.error_to_json)])
let publish =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and topicArn =
         flag "topic-arn" (optional string) ~doc:"STRING topicARN"
       and targetArn =
         flag "target-arn" (optional string) ~doc:"STRING String"
       and phoneNumber =
         flag "phone-number" (optional string) ~doc:"STRING String"
       and subject = flag "subject" (optional string) ~doc:"STRING subject"
       and messageStructure =
         flag "message-structure" (optional string)
           ~doc:"STRING messageStructure"
       and messageAttributes =
         flag "message-attributes" (optional json_arg)
           ~doc:"JSON MessageAttributeMap"
       and messageDeduplicationId =
         flag "message-deduplication-id" (optional string)
           ~doc:"STRING String"
       and messageGroupId =
         flag "message-group-id" (optional string) ~doc:"STRING String"
       and message = flag "message" (required string) ~doc:"STRING message" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.publish
           (Values.PublishInput.make ?topicArn ?targetArn ?phoneNumber
              ?subject ?messageStructure
              ?messageAttributes:(Option.map
                                    ~f:Values.MessageAttributeMap.of_json
                                    messageAttributes)
              ?messageDeduplicationId ?messageGroupId ~message ())
           (Some Values.PublishResponse.to_json)
           (Some Values.PublishResponse.error_to_json)])
let publish_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and topicArn =
         flag "topic-arn" (required string) ~doc:"STRING topicARN"
       and publishBatchRequestEntries =
         flag "publish-batch-request-entries" (required json_arg)
           ~doc:"JSON PublishBatchRequestEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.publish_batch
           (Values.PublishBatchInput.make ~topicArn
              ~publishBatchRequestEntries:(Values.PublishBatchRequestEntryList.of_json
                                             publishBatchRequestEntries) ())
           (Some Values.PublishBatchResponse.to_json)
           (Some Values.PublishBatchResponse.error_to_json)])
let remove_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and topicArn =
         flag "topic-arn" (required string) ~doc:"STRING topicARN"
       and label = flag "label" (required string) ~doc:"STRING label" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_permission
           (Values.RemovePermissionInput.make ~topicArn ~label ()) None None])
let set_endpoint_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING String"
       and attributes =
         flag "attributes" (required json_arg) ~doc:"JSON MapStringToString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_endpoint_attributes
           (Values.SetEndpointAttributesInput.make ~endpointArn
              ~attributes:(Values.MapStringToString.of_json attributes) ())
           None None])
let set_platform_application_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and platformApplicationArn =
         flag "platform-application-arn" (required string)
           ~doc:"STRING String"
       and attributes =
         flag "attributes" (required json_arg) ~doc:"JSON MapStringToString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_platform_application_attributes
           (Values.SetPlatformApplicationAttributesInput.make
              ~platformApplicationArn
              ~attributes:(Values.MapStringToString.of_json attributes) ())
           None None])
let set_s_m_s_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (required json_arg) ~doc:"JSON MapStringToString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_s_m_s_attributes
           (Values.SetSMSAttributesInput.make
              ~attributes:(Values.MapStringToString.of_json attributes) ())
           (Some Values.SetSMSAttributesResponse.to_json)
           (Some Values.SetSMSAttributesResponse.error_to_json)])
let set_subscription_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeValue =
         flag "attribute-value" (optional string)
           ~doc:"STRING attributeValue"
       and subscriptionArn =
         flag "subscription-arn" (required string)
           ~doc:"STRING subscriptionARN"
       and attributeName =
         flag "attribute-name" (required string) ~doc:"STRING attributeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_subscription_attributes
           (Values.SetSubscriptionAttributesInput.make ?attributeValue
              ~subscriptionArn ~attributeName ()) None None])
let set_topic_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeValue =
         flag "attribute-value" (optional string)
           ~doc:"STRING attributeValue"
       and topicArn =
         flag "topic-arn" (required string) ~doc:"STRING topicARN"
       and attributeName =
         flag "attribute-name" (required string) ~doc:"STRING attributeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_topic_attributes
           (Values.SetTopicAttributesInput.make ?attributeValue ~topicArn
              ~attributeName ()) None None])
let subscribe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpoint =
         flag "endpoint" (optional string) ~doc:"STRING Endpoint__lc1"
       and attributes =
         flag "attributes" (optional json_arg)
           ~doc:"JSON SubscriptionAttributesMap"
       and returnSubscriptionArn =
         flag "return-subscription-arn" (optional bool) ~doc:"BOOL boolean"
       and topicArn =
         flag "topic-arn" (required string) ~doc:"STRING topicARN"
       and protocol =
         flag "protocol" (required string) ~doc:"STRING protocol" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.subscribe
           (Values.SubscribeInput.make ?endpoint
              ?attributes:(Option.map
                             ~f:Values.SubscriptionAttributesMap.of_json
                             attributes) ?returnSubscriptionArn ~topicArn
              ~protocol ()) (Some Values.SubscribeResponse.to_json)
           (Some Values.SubscribeResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let unsubscribe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionArn =
         flag "subscription-arn" (required string)
           ~doc:"STRING subscriptionARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.unsubscribe (Values.UnsubscribeInput.make ~subscriptionArn ())
           None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let verify_s_m_s_sandbox_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumber =
         flag "phone-number" (required string)
           ~doc:"STRING PhoneNumberString"
       and oneTimePassword =
         flag "one-time-password" (required string) ~doc:"STRING OTPCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_s_m_s_sandbox_phone_number
           (Values.VerifySMSSandboxPhoneNumberInput.make ~phoneNumber
              ~oneTimePassword ())
           (Some Values.VerifySMSSandboxPhoneNumberResult.to_json)
           (Some Values.VerifySMSSandboxPhoneNumberResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("add-permission", add_permission);
    ("check-if-phone-number-is-opted-out",
      check_if_phone_number_is_opted_out);
    ("confirm-subscription", confirm_subscription);
    ("create-platform-application", create_platform_application);
    ("create-platform-endpoint", create_platform_endpoint);
    ("create-s-m-s-sandbox-phone-number", create_s_m_s_sandbox_phone_number);
    ("create-topic", create_topic);
    ("delete-endpoint", delete_endpoint);
    ("delete-platform-application", delete_platform_application);
    ("delete-s-m-s-sandbox-phone-number", delete_s_m_s_sandbox_phone_number);
    ("delete-topic", delete_topic);
    ("get-endpoint-attributes", get_endpoint_attributes);
    ("get-platform-application-attributes",
      get_platform_application_attributes);
    ("get-s-m-s-attributes", get_s_m_s_attributes);
    ("get-s-m-s-sandbox-account-status", get_s_m_s_sandbox_account_status);
    ("get-subscription-attributes", get_subscription_attributes);
    ("get-topic-attributes", get_topic_attributes);
    ("list-endpoints-by-platform-application",
      list_endpoints_by_platform_application);
    ("list-origination-numbers", list_origination_numbers);
    ("list-phone-numbers-opted-out", list_phone_numbers_opted_out);
    ("list-platform-applications", list_platform_applications);
    ("list-s-m-s-sandbox-phone-numbers", list_s_m_s_sandbox_phone_numbers);
    ("list-subscriptions", list_subscriptions);
    ("list-subscriptions-by-topic", list_subscriptions_by_topic);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-topics", list_topics);
    ("opt-in-phone-number", opt_in_phone_number);
    ("publish", publish);
    ("publish-batch", publish_batch);
    ("remove-permission", remove_permission);
    ("set-endpoint-attributes", set_endpoint_attributes);
    ("set-platform-application-attributes",
      set_platform_application_attributes);
    ("set-s-m-s-attributes", set_s_m_s_attributes);
    ("set-subscription-attributes", set_subscription_attributes);
    ("set-topic-attributes", set_topic_attributes);
    ("subscribe", subscribe);
    ("tag-resource", tag_resource);
    ("unsubscribe", unsubscribe);
    ("untag-resource", untag_resource);
    ("verify-s-m-s-sandbox-phone-number", verify_s_m_s_sandbox_phone_number)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_inbound_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionId =
         flag "connection-id" (required string) ~doc:"STRING ConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_inbound_connection
           (Values.AcceptInboundConnectionRequest.make ~connectionId ())
           (Some Values.AcceptInboundConnectionResponse.to_json)
           (Some Values.AcceptInboundConnectionResponse.error_to_json)])
let add_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aRN = flag "a-r-n" (required string) ~doc:"STRING ARN"
       and tagList = flag "tag-list" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags
           (Values.AddTagsRequest.make ~aRN
              ~tagList:(Values.TagList.of_json tagList) ()) None None])
let associate_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and packageID =
         flag "package-i-d" (required string) ~doc:"STRING PackageID"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_package
           (Values.AssociatePackageRequest.make ~packageID ~domainName ())
           (Some Values.AssociatePackageResponse.to_json)
           (Some Values.AssociatePackageResponse.error_to_json)])
let cancel_service_software_update =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_service_software_update
           (Values.CancelServiceSoftwareUpdateRequest.make ~domainName ())
           (Some Values.CancelServiceSoftwareUpdateResponse.to_json)
           (Some Values.CancelServiceSoftwareUpdateResponse.error_to_json)])
let create_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING VersionString"
       and clusterConfig =
         flag "cluster-config" (optional json_arg) ~doc:"JSON ClusterConfig"
       and eBSOptions =
         flag "e-b-s-options" (optional json_arg) ~doc:"JSON EBSOptions"
       and accessPolicies =
         flag "access-policies" (optional string)
           ~doc:"STRING PolicyDocument"
       and snapshotOptions =
         flag "snapshot-options" (optional json_arg)
           ~doc:"JSON SnapshotOptions"
       and vPCOptions =
         flag "v-p-c-options" (optional json_arg) ~doc:"JSON VPCOptions"
       and cognitoOptions =
         flag "cognito-options" (optional json_arg)
           ~doc:"JSON CognitoOptions"
       and encryptionAtRestOptions =
         flag "encryption-at-rest-options" (optional json_arg)
           ~doc:"JSON EncryptionAtRestOptions"
       and nodeToNodeEncryptionOptions =
         flag "node-to-node-encryption-options" (optional json_arg)
           ~doc:"JSON NodeToNodeEncryptionOptions"
       and advancedOptions =
         flag "advanced-options" (optional json_arg)
           ~doc:"JSON AdvancedOptions"
       and logPublishingOptions =
         flag "log-publishing-options" (optional json_arg)
           ~doc:"JSON LogPublishingOptions"
       and domainEndpointOptions =
         flag "domain-endpoint-options" (optional json_arg)
           ~doc:"JSON DomainEndpointOptions"
       and advancedSecurityOptions =
         flag "advanced-security-options" (optional json_arg)
           ~doc:"JSON AdvancedSecurityOptionsInput"
       and tagList = flag "tag-list" (optional json_arg) ~doc:"JSON TagList"
       and autoTuneOptions =
         flag "auto-tune-options" (optional json_arg)
           ~doc:"JSON AutoTuneOptionsInput"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain
           (Values.CreateDomainRequest.make ?engineVersion
              ?clusterConfig:(Option.map ~f:Values.ClusterConfig.of_json
                                clusterConfig)
              ?eBSOptions:(Option.map ~f:Values.EBSOptions.of_json eBSOptions)
              ?accessPolicies
              ?snapshotOptions:(Option.map ~f:Values.SnapshotOptions.of_json
                                  snapshotOptions)
              ?vPCOptions:(Option.map ~f:Values.VPCOptions.of_json vPCOptions)
              ?cognitoOptions:(Option.map ~f:Values.CognitoOptions.of_json
                                 cognitoOptions)
              ?encryptionAtRestOptions:(Option.map
                                          ~f:Values.EncryptionAtRestOptions.of_json
                                          encryptionAtRestOptions)
              ?nodeToNodeEncryptionOptions:(Option.map
                                              ~f:Values.NodeToNodeEncryptionOptions.of_json
                                              nodeToNodeEncryptionOptions)
              ?advancedOptions:(Option.map ~f:Values.AdvancedOptions.of_json
                                  advancedOptions)
              ?logPublishingOptions:(Option.map
                                       ~f:Values.LogPublishingOptions.of_json
                                       logPublishingOptions)
              ?domainEndpointOptions:(Option.map
                                        ~f:Values.DomainEndpointOptions.of_json
                                        domainEndpointOptions)
              ?advancedSecurityOptions:(Option.map
                                          ~f:Values.AdvancedSecurityOptionsInput.of_json
                                          advancedSecurityOptions)
              ?tagList:(Option.map ~f:Values.TagList.of_json tagList)
              ?autoTuneOptions:(Option.map
                                  ~f:Values.AutoTuneOptionsInput.of_json
                                  autoTuneOptions) ~domainName ())
           (Some Values.CreateDomainResponse.to_json)
           (Some Values.CreateDomainResponse.error_to_json)])
let create_outbound_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and localDomainInfo =
         flag "local-domain-info" (required json_arg)
           ~doc:"JSON DomainInformationContainer"
       and remoteDomainInfo =
         flag "remote-domain-info" (required json_arg)
           ~doc:"JSON DomainInformationContainer"
       and connectionAlias =
         flag "connection-alias" (required string)
           ~doc:"STRING ConnectionAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_outbound_connection
           (Values.CreateOutboundConnectionRequest.make
              ~localDomainInfo:(Values.DomainInformationContainer.of_json
                                  localDomainInfo)
              ~remoteDomainInfo:(Values.DomainInformationContainer.of_json
                                   remoteDomainInfo) ~connectionAlias ())
           (Some Values.CreateOutboundConnectionResponse.to_json)
           (Some Values.CreateOutboundConnectionResponse.error_to_json)])
let create_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and packageDescription =
         flag "package-description" (optional string)
           ~doc:"STRING PackageDescription"
       and packageName =
         flag "package-name" (required string) ~doc:"STRING PackageName"
       and packageType =
         flag "package-type" (required json_arg) ~doc:"JSON PackageType"
       and packageSource =
         flag "package-source" (required json_arg) ~doc:"JSON PackageSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_package
           (Values.CreatePackageRequest.make ?packageDescription ~packageName
              ~packageType:(Values.PackageType.of_json packageType)
              ~packageSource:(Values.PackageSource.of_json packageSource) ())
           (Some Values.CreatePackageResponse.to_json)
           (Some Values.CreatePackageResponse.error_to_json)])
let delete_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain (Values.DeleteDomainRequest.make ~domainName ())
           (Some Values.DeleteDomainResponse.to_json)
           (Some Values.DeleteDomainResponse.error_to_json)])
let delete_inbound_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionId =
         flag "connection-id" (required string) ~doc:"STRING ConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_inbound_connection
           (Values.DeleteInboundConnectionRequest.make ~connectionId ())
           (Some Values.DeleteInboundConnectionResponse.to_json)
           (Some Values.DeleteInboundConnectionResponse.error_to_json)])
let delete_outbound_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionId =
         flag "connection-id" (required string) ~doc:"STRING ConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_outbound_connection
           (Values.DeleteOutboundConnectionRequest.make ~connectionId ())
           (Some Values.DeleteOutboundConnectionResponse.to_json)
           (Some Values.DeleteOutboundConnectionResponse.error_to_json)])
let delete_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and packageID =
         flag "package-i-d" (required string) ~doc:"STRING PackageID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_package (Values.DeletePackageRequest.make ~packageID ())
           (Some Values.DeletePackageResponse.to_json)
           (Some Values.DeletePackageResponse.error_to_json)])
let describe_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain
           (Values.DescribeDomainRequest.make ~domainName ())
           (Some Values.DescribeDomainResponse.to_json)
           (Some Values.DescribeDomainResponse.error_to_json)])
let describe_domain_auto_tunes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain_auto_tunes
           (Values.DescribeDomainAutoTunesRequest.make ?maxResults ?nextToken
              ~domainName ())
           (Some Values.DescribeDomainAutoTunesResponse.to_json)
           (Some Values.DescribeDomainAutoTunesResponse.error_to_json)])
let describe_domain_change_progress =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and changeId = flag "change-id" (optional string) ~doc:"STRING GUID"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain_change_progress
           (Values.DescribeDomainChangeProgressRequest.make ?changeId
              ~domainName ())
           (Some Values.DescribeDomainChangeProgressResponse.to_json)
           (Some Values.DescribeDomainChangeProgressResponse.error_to_json)])
let describe_domain_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain_config
           (Values.DescribeDomainConfigRequest.make ~domainName ())
           (Some Values.DescribeDomainConfigResponse.to_json)
           (Some Values.DescribeDomainConfigResponse.error_to_json)])
let describe_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNames =
         flag "domain-names" (required json_arg) ~doc:"JSON DomainNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domains
           (Values.DescribeDomainsRequest.make
              ~domainNames:(Values.DomainNameList.of_json domainNames) ())
           (Some Values.DescribeDomainsResponse.to_json)
           (Some Values.DescribeDomainsResponse.error_to_json)])
let describe_inbound_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_inbound_connections
           (Values.DescribeInboundConnectionsRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.DescribeInboundConnectionsResponse.to_json)
           (Some Values.DescribeInboundConnectionsResponse.error_to_json)])
let describe_instance_type_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName"
       and instanceType =
         flag "instance-type" (required json_arg)
           ~doc:"JSON OpenSearchPartitionInstanceType"
       and engineVersion =
         flag "engine-version" (required string) ~doc:"STRING VersionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_type_limits
           (Values.DescribeInstanceTypeLimitsRequest.make ?domainName
              ~instanceType:(Values.OpenSearchPartitionInstanceType.of_json
                               instanceType) ~engineVersion ())
           (Some Values.DescribeInstanceTypeLimitsResponse.to_json)
           (Some Values.DescribeInstanceTypeLimitsResponse.error_to_json)])
let describe_outbound_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_outbound_connections
           (Values.DescribeOutboundConnectionsRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.DescribeOutboundConnectionsResponse.to_json)
           (Some Values.DescribeOutboundConnectionsResponse.error_to_json)])
let describe_packages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON DescribePackagesFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_packages
           (Values.DescribePackagesRequest.make
              ?filters:(Option.map
                          ~f:Values.DescribePackagesFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.DescribePackagesResponse.to_json)
           (Some Values.DescribePackagesResponse.error_to_json)])
let describe_reserved_instance_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedInstanceOfferingId =
         flag "reserved-instance-offering-id" (optional string)
           ~doc:"STRING GUID"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_instance_offerings
           (Values.DescribeReservedInstanceOfferingsRequest.make
              ?reservedInstanceOfferingId ?maxResults ?nextToken ())
           (Some Values.DescribeReservedInstanceOfferingsResponse.to_json)
           (Some
              Values.DescribeReservedInstanceOfferingsResponse.error_to_json)])
let describe_reserved_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedInstanceId =
         flag "reserved-instance-id" (optional string) ~doc:"STRING GUID"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_instances
           (Values.DescribeReservedInstancesRequest.make ?reservedInstanceId
              ?maxResults ?nextToken ())
           (Some Values.DescribeReservedInstancesResponse.to_json)
           (Some Values.DescribeReservedInstancesResponse.error_to_json)])
let dissociate_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and packageID =
         flag "package-i-d" (required string) ~doc:"STRING PackageID"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.dissociate_package
           (Values.DissociatePackageRequest.make ~packageID ~domainName ())
           (Some Values.DissociatePackageResponse.to_json)
           (Some Values.DissociatePackageResponse.error_to_json)])
let get_compatible_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_compatible_versions
           (Values.GetCompatibleVersionsRequest.make ?domainName ())
           (Some Values.GetCompatibleVersionsResponse.to_json)
           (Some Values.GetCompatibleVersionsResponse.error_to_json)])
let get_package_version_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and packageID =
         flag "package-i-d" (required string) ~doc:"STRING PackageID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_package_version_history
           (Values.GetPackageVersionHistoryRequest.make ?maxResults
              ?nextToken ~packageID ())
           (Some Values.GetPackageVersionHistoryResponse.to_json)
           (Some Values.GetPackageVersionHistoryResponse.error_to_json)])
let get_upgrade_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_upgrade_history
           (Values.GetUpgradeHistoryRequest.make ?maxResults ?nextToken
              ~domainName ()) (Some Values.GetUpgradeHistoryResponse.to_json)
           (Some Values.GetUpgradeHistoryResponse.error_to_json)])
let get_upgrade_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_upgrade_status
           (Values.GetUpgradeStatusRequest.make ~domainName ())
           (Some Values.GetUpgradeStatusResponse.to_json)
           (Some Values.GetUpgradeStatusResponse.error_to_json)])
let list_domain_names =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engineType =
         flag "engine-type" (optional json_arg) ~doc:"JSON EngineType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domain_names
           (Values.ListDomainNamesRequest.make
              ?engineType:(Option.map ~f:Values.EngineType.of_json engineType)
              ()) (Some Values.ListDomainNamesResponse.to_json)
           (Some Values.ListDomainNamesResponse.error_to_json)])
let list_domains_for_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and packageID =
         flag "package-i-d" (required string) ~doc:"STRING PackageID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domains_for_package
           (Values.ListDomainsForPackageRequest.make ?maxResults ?nextToken
              ~packageID ())
           (Some Values.ListDomainsForPackageResponse.to_json)
           (Some Values.ListDomainsForPackageResponse.error_to_json)])
let list_instance_type_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and engineVersion =
         flag "engine-version" (required string) ~doc:"STRING VersionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_instance_type_details
           (Values.ListInstanceTypeDetailsRequest.make ?domainName
              ?maxResults ?nextToken ~engineVersion ())
           (Some Values.ListInstanceTypeDetailsResponse.to_json)
           (Some Values.ListInstanceTypeDetailsResponse.error_to_json)])
let list_packages_for_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_packages_for_domain
           (Values.ListPackagesForDomainRequest.make ?maxResults ?nextToken
              ~domainName ())
           (Some Values.ListPackagesForDomainResponse.to_json)
           (Some Values.ListPackagesForDomainResponse.error_to_json)])
let list_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aRN = flag "a-r-n" (required string) ~doc:"STRING ARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags (Values.ListTagsRequest.make ~aRN ())
           (Some Values.ListTagsResponse.to_json)
           (Some Values.ListTagsResponse.error_to_json)])
let list_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_versions
           (Values.ListVersionsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListVersionsResponse.to_json)
           (Some Values.ListVersionsResponse.error_to_json)])
let purchase_reserved_instance_offering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceCount =
         flag "instance-count" (optional int) ~doc:"INT InstanceCount"
       and reservedInstanceOfferingId =
         flag "reserved-instance-offering-id" (required string)
           ~doc:"STRING GUID"
       and reservationName =
         flag "reservation-name" (required string)
           ~doc:"STRING ReservationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.purchase_reserved_instance_offering
           (Values.PurchaseReservedInstanceOfferingRequest.make
              ?instanceCount ~reservedInstanceOfferingId ~reservationName ())
           (Some Values.PurchaseReservedInstanceOfferingResponse.to_json)
           (Some
              Values.PurchaseReservedInstanceOfferingResponse.error_to_json)])
let reject_inbound_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionId =
         flag "connection-id" (required string) ~doc:"STRING ConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_inbound_connection
           (Values.RejectInboundConnectionRequest.make ~connectionId ())
           (Some Values.RejectInboundConnectionResponse.to_json)
           (Some Values.RejectInboundConnectionResponse.error_to_json)])
let remove_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aRN = flag "a-r-n" (required string) ~doc:"STRING ARN"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_tags
           (Values.RemoveTagsRequest.make ~aRN
              ~tagKeys:(Values.StringList.of_json tagKeys) ()) None None])
let start_service_software_update =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_service_software_update
           (Values.StartServiceSoftwareUpdateRequest.make ~domainName ())
           (Some Values.StartServiceSoftwareUpdateResponse.to_json)
           (Some Values.StartServiceSoftwareUpdateResponse.error_to_json)])
let update_domain_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterConfig =
         flag "cluster-config" (optional json_arg) ~doc:"JSON ClusterConfig"
       and eBSOptions =
         flag "e-b-s-options" (optional json_arg) ~doc:"JSON EBSOptions"
       and snapshotOptions =
         flag "snapshot-options" (optional json_arg)
           ~doc:"JSON SnapshotOptions"
       and vPCOptions =
         flag "v-p-c-options" (optional json_arg) ~doc:"JSON VPCOptions"
       and cognitoOptions =
         flag "cognito-options" (optional json_arg)
           ~doc:"JSON CognitoOptions"
       and advancedOptions =
         flag "advanced-options" (optional json_arg)
           ~doc:"JSON AdvancedOptions"
       and accessPolicies =
         flag "access-policies" (optional string)
           ~doc:"STRING PolicyDocument"
       and logPublishingOptions =
         flag "log-publishing-options" (optional json_arg)
           ~doc:"JSON LogPublishingOptions"
       and encryptionAtRestOptions =
         flag "encryption-at-rest-options" (optional json_arg)
           ~doc:"JSON EncryptionAtRestOptions"
       and domainEndpointOptions =
         flag "domain-endpoint-options" (optional json_arg)
           ~doc:"JSON DomainEndpointOptions"
       and nodeToNodeEncryptionOptions =
         flag "node-to-node-encryption-options" (optional json_arg)
           ~doc:"JSON NodeToNodeEncryptionOptions"
       and advancedSecurityOptions =
         flag "advanced-security-options" (optional json_arg)
           ~doc:"JSON AdvancedSecurityOptionsInput"
       and autoTuneOptions =
         flag "auto-tune-options" (optional json_arg)
           ~doc:"JSON AutoTuneOptions"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL DryRun"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain_config
           (Values.UpdateDomainConfigRequest.make
              ?clusterConfig:(Option.map ~f:Values.ClusterConfig.of_json
                                clusterConfig)
              ?eBSOptions:(Option.map ~f:Values.EBSOptions.of_json eBSOptions)
              ?snapshotOptions:(Option.map ~f:Values.SnapshotOptions.of_json
                                  snapshotOptions)
              ?vPCOptions:(Option.map ~f:Values.VPCOptions.of_json vPCOptions)
              ?cognitoOptions:(Option.map ~f:Values.CognitoOptions.of_json
                                 cognitoOptions)
              ?advancedOptions:(Option.map ~f:Values.AdvancedOptions.of_json
                                  advancedOptions) ?accessPolicies
              ?logPublishingOptions:(Option.map
                                       ~f:Values.LogPublishingOptions.of_json
                                       logPublishingOptions)
              ?encryptionAtRestOptions:(Option.map
                                          ~f:Values.EncryptionAtRestOptions.of_json
                                          encryptionAtRestOptions)
              ?domainEndpointOptions:(Option.map
                                        ~f:Values.DomainEndpointOptions.of_json
                                        domainEndpointOptions)
              ?nodeToNodeEncryptionOptions:(Option.map
                                              ~f:Values.NodeToNodeEncryptionOptions.of_json
                                              nodeToNodeEncryptionOptions)
              ?advancedSecurityOptions:(Option.map
                                          ~f:Values.AdvancedSecurityOptionsInput.of_json
                                          advancedSecurityOptions)
              ?autoTuneOptions:(Option.map ~f:Values.AutoTuneOptions.of_json
                                  autoTuneOptions) ?dryRun ~domainName ())
           (Some Values.UpdateDomainConfigResponse.to_json)
           (Some Values.UpdateDomainConfigResponse.error_to_json)])
let update_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and packageDescription =
         flag "package-description" (optional string)
           ~doc:"STRING PackageDescription"
       and commitMessage =
         flag "commit-message" (optional string) ~doc:"STRING CommitMessage"
       and packageID =
         flag "package-i-d" (required string) ~doc:"STRING PackageID"
       and packageSource =
         flag "package-source" (required json_arg) ~doc:"JSON PackageSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_package
           (Values.UpdatePackageRequest.make ?packageDescription
              ?commitMessage ~packageID
              ~packageSource:(Values.PackageSource.of_json packageSource) ())
           (Some Values.UpdatePackageResponse.to_json)
           (Some Values.UpdatePackageResponse.error_to_json)])
let upgrade_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and performCheckOnly =
         flag "perform-check-only" (optional bool) ~doc:"BOOL Boolean"
       and advancedOptions =
         flag "advanced-options" (optional json_arg)
           ~doc:"JSON AdvancedOptions"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and targetVersion =
         flag "target-version" (required string) ~doc:"STRING VersionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upgrade_domain
           (Values.UpgradeDomainRequest.make ?performCheckOnly
              ?advancedOptions:(Option.map ~f:Values.AdvancedOptions.of_json
                                  advancedOptions) ~domainName ~targetVersion
              ()) (Some Values.UpgradeDomainResponse.to_json)
           (Some Values.UpgradeDomainResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-inbound-connection", accept_inbound_connection);
    ("add-tags", add_tags);
    ("associate-package", associate_package);
    ("cancel-service-software-update", cancel_service_software_update);
    ("create-domain", create_domain);
    ("create-outbound-connection", create_outbound_connection);
    ("create-package", create_package);
    ("delete-domain", delete_domain);
    ("delete-inbound-connection", delete_inbound_connection);
    ("delete-outbound-connection", delete_outbound_connection);
    ("delete-package", delete_package);
    ("describe-domain", describe_domain);
    ("describe-domain-auto-tunes", describe_domain_auto_tunes);
    ("describe-domain-change-progress", describe_domain_change_progress);
    ("describe-domain-config", describe_domain_config);
    ("describe-domains", describe_domains);
    ("describe-inbound-connections", describe_inbound_connections);
    ("describe-instance-type-limits", describe_instance_type_limits);
    ("describe-outbound-connections", describe_outbound_connections);
    ("describe-packages", describe_packages);
    ("describe-reserved-instance-offerings",
      describe_reserved_instance_offerings);
    ("describe-reserved-instances", describe_reserved_instances);
    ("dissociate-package", dissociate_package);
    ("get-compatible-versions", get_compatible_versions);
    ("get-package-version-history", get_package_version_history);
    ("get-upgrade-history", get_upgrade_history);
    ("get-upgrade-status", get_upgrade_status);
    ("list-domain-names", list_domain_names);
    ("list-domains-for-package", list_domains_for_package);
    ("list-instance-type-details", list_instance_type_details);
    ("list-packages-for-domain", list_packages_for_domain);
    ("list-tags", list_tags);
    ("list-versions", list_versions);
    ("purchase-reserved-instance-offering",
      purchase_reserved_instance_offering);
    ("reject-inbound-connection", reject_inbound_connection);
    ("remove-tags", remove_tags);
    ("start-service-software-update", start_service_software_update);
    ("update-domain-config", update_domain_config);
    ("update-package", update_package);
    ("upgrade-domain", upgrade_domain)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_account_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and name =
         flag "name" (optional string) ~doc:"STRING AccountAssociationName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING AccountAssociationDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and generalAuthorization =
         flag "general-authorization" (optional json_arg)
           ~doc:"JSON GeneralAuthorizationName"
       and connectorDestinationId =
         flag "connector-destination-id" (required string)
           ~doc:"STRING ConnectorDestinationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_account_association
           (Values.CreateAccountAssociationRequest.make ?clientToken ?name
              ?description ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?generalAuthorization:(Option.map
                                       ~f:Values.GeneralAuthorizationName.of_json
                                       generalAuthorization)
              ~connectorDestinationId ())
           (Some Values.CreateAccountAssociationResponse.to_json)
           (Some Values.CreateAccountAssociationResponse.error_to_json)])
let create_cloud_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CloudConnectorDescription"
       and endpointType =
         flag "endpoint-type" (optional json_arg) ~doc:"JSON EndpointType"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and name = flag "name" (required string) ~doc:"STRING DisplayName"
       and endpointConfig =
         flag "endpoint-config" (required json_arg)
           ~doc:"JSON EndpointConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cloud_connector
           (Values.CreateCloudConnectorRequest.make ?description
              ?endpointType:(Option.map ~f:Values.EndpointType.of_json
                               endpointType) ?clientToken ~name
              ~endpointConfig:(Values.EndpointConfig.of_json endpointConfig)
              ()) (Some Values.CreateCloudConnectorResponse.to_json)
           (Some Values.CreateCloudConnectorResponse.error_to_json)])
let create_connector_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING ConnectorDestinationName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ConnectorDestinationDescription"
       and authType =
         flag "auth-type" (optional json_arg) ~doc:"JSON AuthType"
       and secretsManager =
         flag "secrets-manager" (optional json_arg)
           ~doc:"JSON SecretsManager"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and cloudConnectorId =
         flag "cloud-connector-id" (required string)
           ~doc:"STRING CloudConnectorId"
       and authConfig =
         flag "auth-config" (required json_arg) ~doc:"JSON AuthConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connector_destination
           (Values.CreateConnectorDestinationRequest.make ?name ?description
              ?authType:(Option.map ~f:Values.AuthType.of_json authType)
              ?secretsManager:(Option.map ~f:Values.SecretsManager.of_json
                                 secretsManager) ?clientToken
              ~cloudConnectorId
              ~authConfig:(Values.AuthConfig.of_json authConfig) ())
           (Some Values.CreateConnectorDestinationResponse.to_json)
           (Some Values.CreateConnectorDestinationResponse.error_to_json)])
let create_credential_locker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING CredentialLockerName"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_credential_locker
           (Values.CreateCredentialLockerRequest.make ?name ?clientToken
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ())
           (Some Values.CreateCredentialLockerResponse.to_json)
           (Some Values.CreateCredentialLockerResponse.error_to_json)])
let create_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING DestinationDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and deliveryDestinationArn =
         flag "delivery-destination-arn" (required string)
           ~doc:"STRING DeliveryDestinationArn"
       and deliveryDestinationType =
         flag "delivery-destination-type" (required json_arg)
           ~doc:"JSON DeliveryDestinationType"
       and name = flag "name" (required string) ~doc:"STRING DestinationName"
       and roleArn =
         flag "role-arn" (required string)
           ~doc:"STRING DeliveryDestinationRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_destination
           (Values.CreateDestinationRequest.make ?clientToken ?description
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~deliveryDestinationArn
              ~deliveryDestinationType:(Values.DeliveryDestinationType.of_json
                                          deliveryDestinationType) ~name
              ~roleArn ()) (Some Values.CreateDestinationResponse.to_json)
           (Some Values.CreateDestinationResponse.error_to_json)])
let create_event_log_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (optional string)
           ~doc:"STRING SmartHomeResourceId"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and resourceType =
         flag "resource-type" (required string)
           ~doc:"STRING SmartHomeResourceType"
       and eventLogLevel =
         flag "event-log-level" (required json_arg) ~doc:"JSON LogLevel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_event_log_configuration
           (Values.CreateEventLogConfigurationRequest.make ?resourceId
              ?clientToken ~resourceType
              ~eventLogLevel:(Values.LogLevel.of_json eventLogLevel) ())
           (Some Values.CreateEventLogConfigurationResponse.to_json)
           (Some Values.CreateEventLogConfigurationResponse.error_to_json)])
let create_managed_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and owner = flag "owner" (optional string) ~doc:"STRING Owner"
       and credentialLockerId =
         flag "credential-locker-id" (optional string)
           ~doc:"STRING CredentialLockerId"
       and wiFiSimpleSetupConfiguration =
         flag "wi-fi-simple-setup-configuration" (optional json_arg)
           ~doc:"JSON WiFiSimpleSetupConfiguration"
       and serialNumber =
         flag "serial-number" (optional string) ~doc:"STRING SerialNumber"
       and brand = flag "brand" (optional string) ~doc:"STRING Brand"
       and model = flag "model" (optional string) ~doc:"STRING Model"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and capabilityReport =
         flag "capability-report" (optional json_arg)
           ~doc:"JSON CapabilityReport"
       and capabilitySchemas =
         flag "capability-schemas" (optional json_arg)
           ~doc:"JSON CapabilitySchemas"
       and capabilities =
         flag "capabilities" (optional string) ~doc:"STRING Capabilities"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and classification =
         flag "classification" (optional string) ~doc:"STRING Classification"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and metaData =
         flag "meta-data" (optional json_arg) ~doc:"JSON MetaData"
       and role = flag "role" (required json_arg) ~doc:"JSON Role"
       and authenticationMaterial =
         flag "authentication-material" (required string)
           ~doc:"STRING AuthMaterialString"
       and authenticationMaterialType =
         flag "authentication-material-type" (required json_arg)
           ~doc:"JSON AuthMaterialType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_managed_thing
           (Values.CreateManagedThingRequest.make ?owner ?credentialLockerId
              ?wiFiSimpleSetupConfiguration:(Option.map
                                               ~f:Values.WiFiSimpleSetupConfiguration.of_json
                                               wiFiSimpleSetupConfiguration)
              ?serialNumber ?brand ?model ?name
              ?capabilityReport:(Option.map
                                   ~f:Values.CapabilityReport.of_json
                                   capabilityReport)
              ?capabilitySchemas:(Option.map
                                    ~f:Values.CapabilitySchemas.of_json
                                    capabilitySchemas) ?capabilities
              ?clientToken ?classification
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?metaData:(Option.map ~f:Values.MetaData.of_json metaData)
              ~role:(Values.Role.of_json role) ~authenticationMaterial
              ~authenticationMaterialType:(Values.AuthMaterialType.of_json
                                             authenticationMaterialType) ())
           (Some Values.CreateManagedThingResponse.to_json)
           (Some Values.CreateManagedThingResponse.error_to_json)])
let create_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and eventType =
         flag "event-type" (required json_arg) ~doc:"JSON EventType"
       and destinationName =
         flag "destination-name" (required string)
           ~doc:"STRING DestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_notification_configuration
           (Values.CreateNotificationConfigurationRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~eventType:(Values.EventType.of_json eventType)
              ~destinationName ())
           (Some Values.CreateNotificationConfigurationResponse.to_json)
           (Some Values.CreateNotificationConfigurationResponse.error_to_json)])
let create_ota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING OtaDescription"
       and protocol =
         flag "protocol" (optional json_arg) ~doc:"JSON OtaProtocol"
       and target = flag "target" (optional json_arg) ~doc:"JSON Target"
       and taskConfigurationId =
         flag "task-configuration-id" (optional string)
           ~doc:"STRING OtaTaskConfigurationId"
       and otaMechanism =
         flag "ota-mechanism" (optional json_arg) ~doc:"JSON OtaMechanism"
       and otaTargetQueryString =
         flag "ota-target-query-string" (optional string)
           ~doc:"STRING OtaTargetQueryString"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and otaSchedulingConfig =
         flag "ota-scheduling-config" (optional json_arg)
           ~doc:"JSON OtaTaskSchedulingConfig"
       and otaTaskExecutionRetryConfig =
         flag "ota-task-execution-retry-config" (optional json_arg)
           ~doc:"JSON OtaTaskExecutionRetryConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and s3Url = flag "s3-url" (required string) ~doc:"STRING S3Url"
       and otaType = flag "ota-type" (required json_arg) ~doc:"JSON OtaType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ota_task
           (Values.CreateOtaTaskRequest.make ?description
              ?protocol:(Option.map ~f:Values.OtaProtocol.of_json protocol)
              ?target:(Option.map ~f:Values.Target.of_json target)
              ?taskConfigurationId
              ?otaMechanism:(Option.map ~f:Values.OtaMechanism.of_json
                               otaMechanism) ?otaTargetQueryString
              ?clientToken
              ?otaSchedulingConfig:(Option.map
                                      ~f:Values.OtaTaskSchedulingConfig.of_json
                                      otaSchedulingConfig)
              ?otaTaskExecutionRetryConfig:(Option.map
                                              ~f:Values.OtaTaskExecutionRetryConfig.of_json
                                              otaTaskExecutionRetryConfig)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~s3Url
              ~otaType:(Values.OtaType.of_json otaType) ())
           (Some Values.CreateOtaTaskResponse.to_json)
           (Some Values.CreateOtaTaskResponse.error_to_json)])
let create_ota_task_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING OtaDescription"
       and name =
         flag "name" (optional string) ~doc:"STRING OtaTaskConfigurationName"
       and pushConfig =
         flag "push-config" (optional json_arg) ~doc:"JSON PushConfig"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ota_task_configuration
           (Values.CreateOtaTaskConfigurationRequest.make ?description ?name
              ?pushConfig:(Option.map ~f:Values.PushConfig.of_json pushConfig)
              ?clientToken ())
           (Some Values.CreateOtaTaskConfigurationResponse.to_json)
           (Some Values.CreateOtaTaskConfigurationResponse.error_to_json)])
let create_provisioning_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and caCertificate =
         flag "ca-certificate" (optional string) ~doc:"STRING CaCertificate"
       and claimCertificate =
         flag "claim-certificate" (optional string)
           ~doc:"STRING ClaimCertificate"
       and name =
         flag "name" (optional string) ~doc:"STRING ProvisioningProfileName"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and provisioningType =
         flag "provisioning-type" (required json_arg)
           ~doc:"JSON ProvisioningType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_provisioning_profile
           (Values.CreateProvisioningProfileRequest.make ?caCertificate
              ?claimCertificate ?name ?clientToken
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~provisioningType:(Values.ProvisioningType.of_json
                                   provisioningType) ())
           (Some Values.CreateProvisioningProfileResponse.to_json)
           (Some Values.CreateProvisioningProfileResponse.error_to_json)])
let delete_account_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountAssociationId =
         flag "account-association-id" (required string)
           ~doc:"STRING AccountAssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_account_association
           (Values.DeleteAccountAssociationRequest.make ~accountAssociationId
              ()) None None])
let delete_cloud_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING CloudConnectorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cloud_connector
           (Values.DeleteCloudConnectorRequest.make ~identifier ()) None None])
let delete_connector_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ConnectorDestinationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connector_destination
           (Values.DeleteConnectorDestinationRequest.make ~identifier ())
           None None])
let delete_credential_locker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING CredentialLockerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_credential_locker
           (Values.DeleteCredentialLockerRequest.make ~identifier ()) None
           None])
let delete_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_destination
           (Values.DeleteDestinationRequest.make ~name ()) None None])
let delete_event_log_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING LogConfigurationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event_log_configuration
           (Values.DeleteEventLogConfigurationRequest.make ~id ()) None None])
let delete_managed_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ManagedThingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_managed_thing
           (Values.DeleteManagedThingRequest.make ?force ~identifier ()) None
           None])
let delete_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventType =
         flag "event-type" (required json_arg) ~doc:"JSON EventType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_notification_configuration
           (Values.DeleteNotificationConfigurationRequest.make
              ~eventType:(Values.EventType.of_json eventType) ()) None None])
let delete_ota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING OtaTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_ota_task
           (Values.DeleteOtaTaskRequest.make ~identifier ()) None None])
let delete_ota_task_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING OtaTaskConfigurationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_ota_task_configuration
           (Values.DeleteOtaTaskConfigurationRequest.make ~identifier ())
           None None])
let delete_provisioning_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ProvisioningProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_provisioning_profile
           (Values.DeleteProvisioningProfileRequest.make ~identifier ()) None
           None])
let deregister_account_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managedThingId =
         flag "managed-thing-id" (required string)
           ~doc:"STRING ManagedThingId"
       and accountAssociationId =
         flag "account-association-id" (required string)
           ~doc:"STRING AccountAssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_account_association
           (Values.DeregisterAccountAssociationRequest.make ~managedThingId
              ~accountAssociationId ()) None None])
let get_account_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountAssociationId =
         flag "account-association-id" (required string)
           ~doc:"STRING AccountAssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_association
           (Values.GetAccountAssociationRequest.make ~accountAssociationId ())
           (Some Values.GetAccountAssociationResponse.to_json)
           (Some Values.GetAccountAssociationResponse.error_to_json)])
let get_cloud_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING CloudConnectorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cloud_connector
           (Values.GetCloudConnectorRequest.make ~identifier ())
           (Some Values.GetCloudConnectorResponse.to_json)
           (Some Values.GetCloudConnectorResponse.error_to_json)])
let get_connector_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ConnectorDestinationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connector_destination
           (Values.GetConnectorDestinationRequest.make ~identifier ())
           (Some Values.GetConnectorDestinationResponse.to_json)
           (Some Values.GetConnectorDestinationResponse.error_to_json)])
let get_credential_locker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING CredentialLockerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_credential_locker
           (Values.GetCredentialLockerRequest.make ~identifier ())
           (Some Values.GetCredentialLockerResponse.to_json)
           (Some Values.GetCredentialLockerResponse.error_to_json)])
let get_custom_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_custom_endpoint (Values.GetCustomEndpointRequest.make ())
           (Some Values.GetCustomEndpointResponse.to_json)
           (Some Values.GetCustomEndpointResponse.error_to_json)])
let get_default_encryption_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_default_encryption_configuration
           (Values.GetDefaultEncryptionConfigurationRequest.make ())
           (Some Values.GetDefaultEncryptionConfigurationResponse.to_json)
           (Some
              Values.GetDefaultEncryptionConfigurationResponse.error_to_json)])
let get_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_destination (Values.GetDestinationRequest.make ~name ())
           (Some Values.GetDestinationResponse.to_json)
           (Some Values.GetDestinationResponse.error_to_json)])
let get_device_discovery =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DeviceDiscoveryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device_discovery
           (Values.GetDeviceDiscoveryRequest.make ~identifier ())
           (Some Values.GetDeviceDiscoveryResponse.to_json)
           (Some Values.GetDeviceDiscoveryResponse.error_to_json)])
let get_event_log_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING LogConfigurationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_event_log_configuration
           (Values.GetEventLogConfigurationRequest.make ~id ())
           (Some Values.GetEventLogConfigurationResponse.to_json)
           (Some Values.GetEventLogConfigurationResponse.error_to_json)])
let get_hub_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_hub_configuration
           (Values.GetHubConfigurationRequest.make ())
           (Some Values.GetHubConfigurationResponse.to_json)
           (Some Values.GetHubConfigurationResponse.error_to_json)])
let get_managed_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ManagedThingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_managed_thing
           (Values.GetManagedThingRequest.make ~identifier ())
           (Some Values.GetManagedThingResponse.to_json)
           (Some Values.GetManagedThingResponse.error_to_json)])
let get_managed_thing_capabilities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ManagedThingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_managed_thing_capabilities
           (Values.GetManagedThingCapabilitiesRequest.make ~identifier ())
           (Some Values.GetManagedThingCapabilitiesResponse.to_json)
           (Some Values.GetManagedThingCapabilitiesResponse.error_to_json)])
let get_managed_thing_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ManagedThingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_managed_thing_certificate
           (Values.GetManagedThingCertificateRequest.make ~identifier ())
           (Some Values.GetManagedThingCertificateResponse.to_json)
           (Some Values.GetManagedThingCertificateResponse.error_to_json)])
let get_managed_thing_connectivity_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ManagedThingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_managed_thing_connectivity_data
           (Values.GetManagedThingConnectivityDataRequest.make ~identifier ())
           (Some Values.GetManagedThingConnectivityDataResponse.to_json)
           (Some Values.GetManagedThingConnectivityDataResponse.error_to_json)])
let get_managed_thing_meta_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ManagedThingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_managed_thing_meta_data
           (Values.GetManagedThingMetaDataRequest.make ~identifier ())
           (Some Values.GetManagedThingMetaDataResponse.to_json)
           (Some Values.GetManagedThingMetaDataResponse.error_to_json)])
let get_managed_thing_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managedThingId =
         flag "managed-thing-id" (required string)
           ~doc:"STRING ManagedThingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_managed_thing_state
           (Values.GetManagedThingStateRequest.make ~managedThingId ())
           (Some Values.GetManagedThingStateResponse.to_json)
           (Some Values.GetManagedThingStateResponse.error_to_json)])
let get_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventType =
         flag "event-type" (required json_arg) ~doc:"JSON EventType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_notification_configuration
           (Values.GetNotificationConfigurationRequest.make
              ~eventType:(Values.EventType.of_json eventType) ())
           (Some Values.GetNotificationConfigurationResponse.to_json)
           (Some Values.GetNotificationConfigurationResponse.error_to_json)])
let get_ota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING OtaTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_ota_task (Values.GetOtaTaskRequest.make ~identifier ())
           (Some Values.GetOtaTaskResponse.to_json)
           (Some Values.GetOtaTaskResponse.error_to_json)])
let get_ota_task_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING OtaTaskConfigurationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_ota_task_configuration
           (Values.GetOtaTaskConfigurationRequest.make ~identifier ())
           (Some Values.GetOtaTaskConfigurationResponse.to_json)
           (Some Values.GetOtaTaskConfigurationResponse.error_to_json)])
let get_provisioning_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ProvisioningProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_provisioning_profile
           (Values.GetProvisioningProfileRequest.make ~identifier ())
           (Some Values.GetProvisioningProfileResponse.to_json)
           (Some Values.GetProvisioningProfileResponse.error_to_json)])
let get_runtime_log_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managedThingId =
         flag "managed-thing-id" (required string)
           ~doc:"STRING ManagedThingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_runtime_log_configuration
           (Values.GetRuntimeLogConfigurationRequest.make ~managedThingId ())
           (Some Values.GetRuntimeLogConfigurationResponse.to_json)
           (Some Values.GetRuntimeLogConfigurationResponse.error_to_json)])
let get_schema_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and format =
         flag "format" (optional json_arg) ~doc:"JSON SchemaVersionFormat"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON SchemaVersionType"
       and schemaVersionedId =
         flag "schema-versioned-id" (required string)
           ~doc:"STRING SchemaVersionedId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_schema_version
           (Values.GetSchemaVersionRequest.make
              ?format:(Option.map ~f:Values.SchemaVersionFormat.of_json
                         format)
              ~type_:(Values.SchemaVersionType.of_json type_)
              ~schemaVersionedId ())
           (Some Values.GetSchemaVersionResponse.to_json)
           (Some Values.GetSchemaVersionResponse.error_to_json)])
let list_account_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorDestinationId =
         flag "connector-destination-id" (optional string)
           ~doc:"STRING ConnectorDestinationId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_account_associations
           (Values.ListAccountAssociationsRequest.make
              ?connectorDestinationId ?maxResults ?nextToken ())
           (Some Values.ListAccountAssociationsResponse.to_json)
           (Some Values.ListAccountAssociationsResponse.error_to_json)])
let list_cloud_connectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON CloudConnectorType"
       and lambdaArn =
         flag "lambda-arn" (optional string) ~doc:"STRING LambdaArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_cloud_connectors
           (Values.ListCloudConnectorsRequest.make
              ?type_:(Option.map ~f:Values.CloudConnectorType.of_json type_)
              ?lambdaArn ?maxResults ?nextToken ())
           (Some Values.ListCloudConnectorsResponse.to_json)
           (Some Values.ListCloudConnectorsResponse.error_to_json)])
let list_connector_destinations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cloudConnectorId =
         flag "cloud-connector-id" (optional string)
           ~doc:"STRING CloudConnectorId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connector_destinations
           (Values.ListConnectorDestinationsRequest.make ?cloudConnectorId
              ?nextToken ?maxResults ())
           (Some Values.ListConnectorDestinationsResponse.to_json)
           (Some Values.ListConnectorDestinationsResponse.error_to_json)])
let list_credential_lockers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_credential_lockers
           (Values.ListCredentialLockersRequest.make ?nextToken ?maxResults
              ()) (Some Values.ListCredentialLockersResponse.to_json)
           (Some Values.ListCredentialLockersResponse.error_to_json)])
let list_destinations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_destinations
           (Values.ListDestinationsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListDestinationsResponse.to_json)
           (Some Values.ListDestinationsResponse.error_to_json)])
let list_device_discoveries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and typeFilter =
         flag "type-filter" (optional json_arg) ~doc:"JSON DiscoveryType"
       and statusFilter =
         flag "status-filter" (optional json_arg)
           ~doc:"JSON DeviceDiscoveryStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_device_discoveries
           (Values.ListDeviceDiscoveriesRequest.make ?nextToken ?maxResults
              ?typeFilter:(Option.map ~f:Values.DiscoveryType.of_json
                             typeFilter)
              ?statusFilter:(Option.map
                               ~f:Values.DeviceDiscoveryStatus.of_json
                               statusFilter) ())
           (Some Values.ListDeviceDiscoveriesResponse.to_json)
           (Some Values.ListDeviceDiscoveriesResponse.error_to_json)])
let list_discovered_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DeviceDiscoveryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_discovered_devices
           (Values.ListDiscoveredDevicesRequest.make ?nextToken ?maxResults
              ~identifier ())
           (Some Values.ListDiscoveredDevicesResponse.to_json)
           (Some Values.ListDiscoveredDevicesResponse.error_to_json)])
let list_event_log_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_event_log_configurations
           (Values.ListEventLogConfigurationsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListEventLogConfigurationsResponse.to_json)
           (Some Values.ListEventLogConfigurationsResponse.error_to_json)])
let list_managed_thing_account_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managedThingId =
         flag "managed-thing-id" (optional string)
           ~doc:"STRING ManagedThingId"
       and accountAssociationId =
         flag "account-association-id" (optional string)
           ~doc:"STRING AccountAssociationId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_thing_account_associations
           (Values.ListManagedThingAccountAssociationsRequest.make
              ?managedThingId ?accountAssociationId ?maxResults ?nextToken ())
           (Some Values.ListManagedThingAccountAssociationsResponse.to_json)
           (Some
              Values.ListManagedThingAccountAssociationsResponse.error_to_json)])
let list_managed_thing_schemas =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointIdFilter =
         flag "endpoint-id-filter" (optional string) ~doc:"STRING EndpointId"
       and capabilityIdFilter =
         flag "capability-id-filter" (optional string)
           ~doc:"STRING CapabilityId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ManagedThingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_thing_schemas
           (Values.ListManagedThingSchemasRequest.make ?endpointIdFilter
              ?capabilityIdFilter ?nextToken ?maxResults ~identifier ())
           (Some Values.ListManagedThingSchemasResponse.to_json)
           (Some Values.ListManagedThingSchemasResponse.error_to_json)])
let list_managed_things =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ownerFilter =
         flag "owner-filter" (optional string) ~doc:"STRING Owner"
       and credentialLockerFilter =
         flag "credential-locker-filter" (optional string)
           ~doc:"STRING CredentialLockerId"
       and roleFilter =
         flag "role-filter" (optional json_arg) ~doc:"JSON Role"
       and parentControllerIdentifierFilter =
         flag "parent-controller-identifier-filter" (optional string)
           ~doc:"STRING ParentControllerId"
       and connectorPolicyIdFilter =
         flag "connector-policy-id-filter" (optional string)
           ~doc:"STRING ConnectorPolicyId"
       and connectorDestinationIdFilter =
         flag "connector-destination-id-filter" (optional string)
           ~doc:"STRING ConnectorDestinationId"
       and connectorDeviceIdFilter =
         flag "connector-device-id-filter" (optional string)
           ~doc:"STRING ConnectorDeviceId"
       and serialNumberFilter =
         flag "serial-number-filter" (optional string)
           ~doc:"STRING SerialNumber"
       and provisioningStatusFilter =
         flag "provisioning-status-filter" (optional json_arg)
           ~doc:"JSON ProvisioningStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_things
           (Values.ListManagedThingsRequest.make ?ownerFilter
              ?credentialLockerFilter
              ?roleFilter:(Option.map ~f:Values.Role.of_json roleFilter)
              ?parentControllerIdentifierFilter ?connectorPolicyIdFilter
              ?connectorDestinationIdFilter ?connectorDeviceIdFilter
              ?serialNumberFilter
              ?provisioningStatusFilter:(Option.map
                                           ~f:Values.ProvisioningStatus.of_json
                                           provisioningStatusFilter)
              ?nextToken ?maxResults ())
           (Some Values.ListManagedThingsResponse.to_json)
           (Some Values.ListManagedThingsResponse.error_to_json)])
let list_notification_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_notification_configurations
           (Values.ListNotificationConfigurationsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListNotificationConfigurationsResponse.to_json)
           (Some Values.ListNotificationConfigurationsResponse.error_to_json)])
let list_ota_task_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ota_task_configurations
           (Values.ListOtaTaskConfigurationsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListOtaTaskConfigurationsResponse.to_json)
           (Some Values.ListOtaTaskConfigurationsResponse.error_to_json)])
let list_ota_task_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING OtaNextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING OtaTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ota_task_executions
           (Values.ListOtaTaskExecutionsRequest.make ?nextToken ?maxResults
              ~identifier ())
           (Some Values.ListOtaTaskExecutionsResponse.to_json)
           (Some Values.ListOtaTaskExecutionsResponse.error_to_json)])
let list_ota_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING OtaNextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ota_tasks
           (Values.ListOtaTasksRequest.make ?nextToken ?maxResults ())
           (Some Values.ListOtaTasksResponse.to_json)
           (Some Values.ListOtaTasksResponse.error_to_json)])
let list_provisioning_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_provisioning_profiles
           (Values.ListProvisioningProfilesRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListProvisioningProfilesResponse.to_json)
           (Some Values.ListProvisioningProfilesResponse.error_to_json)])
let list_schema_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and schemaId =
         flag "schema-id" (optional string) ~doc:"STRING SchemaId"
       and namespace =
         flag "namespace" (optional string)
           ~doc:"STRING SchemaVersionNamespaceName"
       and visibility =
         flag "visibility" (optional json_arg)
           ~doc:"JSON SchemaVersionVisibility"
       and semanticVersion =
         flag "semantic-version" (optional string)
           ~doc:"STRING SchemaVersionVersion"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON SchemaVersionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_schema_versions
           (Values.ListSchemaVersionsRequest.make ?maxResults ?nextToken
              ?schemaId ?namespace
              ?visibility:(Option.map
                             ~f:Values.SchemaVersionVisibility.of_json
                             visibility) ?semanticVersion
              ~type_:(Values.SchemaVersionType.of_json type_) ())
           (Some Values.ListSchemaVersionsResponse.to_json)
           (Some Values.ListSchemaVersionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING IoTManagedIntegrationsResourceARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_default_encryption_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and encryptionType =
         flag "encryption-type" (required json_arg)
           ~doc:"JSON EncryptionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_default_encryption_configuration
           (Values.PutDefaultEncryptionConfigurationRequest.make ?kmsKeyArn
              ~encryptionType:(Values.EncryptionType.of_json encryptionType)
              ())
           (Some Values.PutDefaultEncryptionConfigurationResponse.to_json)
           (Some
              Values.PutDefaultEncryptionConfigurationResponse.error_to_json)])
let put_hub_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hubTokenTimerExpirySettingInSeconds =
         flag "hub-token-timer-expiry-setting-in-seconds" (required json_arg)
           ~doc:"JSON HubTokenTimerExpirySettingInSeconds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_hub_configuration
           (Values.PutHubConfigurationRequest.make
              ~hubTokenTimerExpirySettingInSeconds:(Values.HubTokenTimerExpirySettingInSeconds.of_json
                                                      hubTokenTimerExpirySettingInSeconds)
              ()) (Some Values.PutHubConfigurationResponse.to_json)
           (Some Values.PutHubConfigurationResponse.error_to_json)])
let put_runtime_log_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managedThingId =
         flag "managed-thing-id" (required string)
           ~doc:"STRING ManagedThingId"
       and runtimeLogConfigurations =
         flag "runtime-log-configurations" (required json_arg)
           ~doc:"JSON RuntimeLogConfigurations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_runtime_log_configuration
           (Values.PutRuntimeLogConfigurationRequest.make ~managedThingId
              ~runtimeLogConfigurations:(Values.RuntimeLogConfigurations.of_json
                                           runtimeLogConfigurations) ()) None
           None])
let register_account_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managedThingId =
         flag "managed-thing-id" (required string)
           ~doc:"STRING ManagedThingId"
       and accountAssociationId =
         flag "account-association-id" (required string)
           ~doc:"STRING AccountAssociationId"
       and deviceDiscoveryId =
         flag "device-discovery-id" (required string)
           ~doc:"STRING DeviceDiscoveryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_account_association
           (Values.RegisterAccountAssociationRequest.make ~managedThingId
              ~accountAssociationId ~deviceDiscoveryId ())
           (Some Values.RegisterAccountAssociationResponse.to_json)
           (Some Values.RegisterAccountAssociationResponse.error_to_json)])
let register_custom_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_custom_endpoint
           (Values.RegisterCustomEndpointRequest.make ())
           (Some Values.RegisterCustomEndpointResponse.to_json)
           (Some Values.RegisterCustomEndpointResponse.error_to_json)])
let reset_runtime_log_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managedThingId =
         flag "managed-thing-id" (required string)
           ~doc:"STRING ManagedThingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_runtime_log_configuration
           (Values.ResetRuntimeLogConfigurationRequest.make ~managedThingId
              ()) None None])
let send_connector_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId =
         flag "user-id" (optional string) ~doc:"STRING ThirdPartyUserId"
       and operationVersion =
         flag "operation-version" (optional string)
           ~doc:"STRING ConnectorEventOperationVersion"
       and statusCode =
         flag "status-code" (optional int)
           ~doc:"INT ConnectorEventStatusCode"
       and message =
         flag "message" (optional string) ~doc:"STRING ConnectorEventMessage"
       and deviceDiscoveryId =
         flag "device-discovery-id" (optional string)
           ~doc:"STRING DeviceDiscoveryId"
       and connectorDeviceId =
         flag "connector-device-id" (optional string)
           ~doc:"STRING ConnectorDeviceId"
       and traceId = flag "trace-id" (optional string) ~doc:"STRING TraceId"
       and devices = flag "devices" (optional json_arg) ~doc:"JSON Devices"
       and matterEndpoint =
         flag "matter-endpoint" (optional json_arg)
           ~doc:"JSON MatterEndpoint"
       and connectorId =
         flag "connector-id" (required string) ~doc:"STRING ConnectorId"
       and operation =
         flag "operation" (required json_arg)
           ~doc:"JSON ConnectorEventOperation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_connector_event
           (Values.SendConnectorEventRequest.make ?userId ?operationVersion
              ?statusCode ?message ?deviceDiscoveryId ?connectorDeviceId
              ?traceId
              ?devices:(Option.map ~f:Values.Devices.of_json devices)
              ?matterEndpoint:(Option.map ~f:Values.MatterEndpoint.of_json
                                 matterEndpoint) ~connectorId
              ~operation:(Values.ConnectorEventOperation.of_json operation)
              ()) (Some Values.SendConnectorEventResponse.to_json)
           (Some Values.SendConnectorEventResponse.error_to_json)])
let send_managed_thing_command =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorAssociationId =
         flag "connector-association-id" (optional string)
           ~doc:"STRING ConnectorAssociationId"
       and accountAssociationId =
         flag "account-association-id" (optional string)
           ~doc:"STRING AccountAssociationId"
       and managedThingId =
         flag "managed-thing-id" (required string)
           ~doc:"STRING ManagedThingId"
       and endpoints =
         flag "endpoints" (required json_arg) ~doc:"JSON CommandEndpoints" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_managed_thing_command
           (Values.SendManagedThingCommandRequest.make
              ?connectorAssociationId ?accountAssociationId ~managedThingId
              ~endpoints:(Values.CommandEndpoints.of_json endpoints) ())
           (Some Values.SendManagedThingCommandResponse.to_json)
           (Some Values.SendManagedThingCommandResponse.error_to_json)])
let start_account_association_refresh =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountAssociationId =
         flag "account-association-id" (required string)
           ~doc:"STRING AccountAssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_account_association_refresh
           (Values.StartAccountAssociationRefreshRequest.make
              ~accountAssociationId ())
           (Some Values.StartAccountAssociationRefreshResponse.to_json)
           (Some Values.StartAccountAssociationRefreshResponse.error_to_json)])
let start_device_discovery =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customProtocolDetail =
         flag "custom-protocol-detail" (optional json_arg)
           ~doc:"JSON CustomProtocolDetail"
       and controllerIdentifier =
         flag "controller-identifier" (optional string)
           ~doc:"STRING ManagedThingId"
       and connectorAssociationIdentifier =
         flag "connector-association-identifier" (optional string)
           ~doc:"STRING ConnectorAssociationId"
       and accountAssociationId =
         flag "account-association-id" (optional string)
           ~doc:"STRING AccountAssociationId"
       and authenticationMaterial =
         flag "authentication-material" (optional string)
           ~doc:"STRING DiscoveryAuthMaterialString"
       and authenticationMaterialType =
         flag "authentication-material-type" (optional json_arg)
           ~doc:"JSON DiscoveryAuthMaterialType"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and connectorDeviceIdList =
         flag "connector-device-id-list" (optional json_arg)
           ~doc:"JSON ConnectorDeviceIdList"
       and protocol =
         flag "protocol" (optional json_arg) ~doc:"JSON ProtocolType"
       and endDeviceIdentifier =
         flag "end-device-identifier" (optional string)
           ~doc:"STRING ManagedThingId"
       and discoveryType =
         flag "discovery-type" (required json_arg) ~doc:"JSON DiscoveryType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_device_discovery
           (Values.StartDeviceDiscoveryRequest.make
              ?customProtocolDetail:(Option.map
                                       ~f:Values.CustomProtocolDetail.of_json
                                       customProtocolDetail)
              ?controllerIdentifier ?connectorAssociationIdentifier
              ?accountAssociationId ?authenticationMaterial
              ?authenticationMaterialType:(Option.map
                                             ~f:Values.DiscoveryAuthMaterialType.of_json
                                             authenticationMaterialType)
              ?clientToken ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?connectorDeviceIdList:(Option.map
                                        ~f:Values.ConnectorDeviceIdList.of_json
                                        connectorDeviceIdList)
              ?protocol:(Option.map ~f:Values.ProtocolType.of_json protocol)
              ?endDeviceIdentifier
              ~discoveryType:(Values.DiscoveryType.of_json discoveryType) ())
           (Some Values.StartDeviceDiscoveryResponse.to_json)
           (Some Values.StartDeviceDiscoveryResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING IoTManagedIntegrationsResourceARN"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagsMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING IoTManagedIntegrationsResourceARN"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_account_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING AccountAssociationName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING AccountAssociationDescription"
       and accountAssociationId =
         flag "account-association-id" (required string)
           ~doc:"STRING AccountAssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account_association
           (Values.UpdateAccountAssociationRequest.make ?name ?description
              ~accountAssociationId ()) None None])
let update_cloud_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING DisplayName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CloudConnectorDescription"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING CloudConnectorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cloud_connector
           (Values.UpdateCloudConnectorRequest.make ?name ?description
              ~identifier ()) None None])
let update_connector_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ConnectorDestinationDescription"
       and name =
         flag "name" (optional string) ~doc:"STRING ConnectorDestinationName"
       and authType =
         flag "auth-type" (optional json_arg) ~doc:"JSON AuthType"
       and authConfig =
         flag "auth-config" (optional json_arg) ~doc:"JSON AuthConfigUpdate"
       and secretsManager =
         flag "secrets-manager" (optional json_arg)
           ~doc:"JSON SecretsManager"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ConnectorDestinationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connector_destination
           (Values.UpdateConnectorDestinationRequest.make ?description ?name
              ?authType:(Option.map ~f:Values.AuthType.of_json authType)
              ?authConfig:(Option.map ~f:Values.AuthConfigUpdate.of_json
                             authConfig)
              ?secretsManager:(Option.map ~f:Values.SecretsManager.of_json
                                 secretsManager) ~identifier ()) None None])
let update_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deliveryDestinationArn =
         flag "delivery-destination-arn" (optional string)
           ~doc:"STRING DeliveryDestinationArn"
       and deliveryDestinationType =
         flag "delivery-destination-type" (optional json_arg)
           ~doc:"JSON DeliveryDestinationType"
       and roleArn =
         flag "role-arn" (optional string)
           ~doc:"STRING DeliveryDestinationRoleArn"
       and description =
         flag "description" (optional string)
           ~doc:"STRING DestinationDescription"
       and name = flag "name" (required string) ~doc:"STRING DestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_destination
           (Values.UpdateDestinationRequest.make ?deliveryDestinationArn
              ?deliveryDestinationType:(Option.map
                                          ~f:Values.DeliveryDestinationType.of_json
                                          deliveryDestinationType) ?roleArn
              ?description ~name ()) None None])
let update_event_log_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING LogConfigurationId"
       and eventLogLevel =
         flag "event-log-level" (required json_arg) ~doc:"JSON LogLevel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_event_log_configuration
           (Values.UpdateEventLogConfigurationRequest.make ~id
              ~eventLogLevel:(Values.LogLevel.of_json eventLogLevel) ()) None
           None])
let update_managed_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and owner = flag "owner" (optional string) ~doc:"STRING Owner"
       and credentialLockerId =
         flag "credential-locker-id" (optional string)
           ~doc:"STRING CredentialLockerId"
       and serialNumber =
         flag "serial-number" (optional string) ~doc:"STRING SerialNumber"
       and wiFiSimpleSetupConfiguration =
         flag "wi-fi-simple-setup-configuration" (optional json_arg)
           ~doc:"JSON WiFiSimpleSetupConfiguration"
       and brand = flag "brand" (optional string) ~doc:"STRING Brand"
       and model = flag "model" (optional string) ~doc:"STRING Model"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and capabilityReport =
         flag "capability-report" (optional json_arg)
           ~doc:"JSON CapabilityReport"
       and capabilitySchemas =
         flag "capability-schemas" (optional json_arg)
           ~doc:"JSON CapabilitySchemas"
       and capabilities =
         flag "capabilities" (optional string) ~doc:"STRING Capabilities"
       and classification =
         flag "classification" (optional string) ~doc:"STRING Classification"
       and hubNetworkMode =
         flag "hub-network-mode" (optional json_arg)
           ~doc:"JSON HubNetworkMode"
       and metaData =
         flag "meta-data" (optional json_arg) ~doc:"JSON MetaData"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ManagedThingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_managed_thing
           (Values.UpdateManagedThingRequest.make ?owner ?credentialLockerId
              ?serialNumber
              ?wiFiSimpleSetupConfiguration:(Option.map
                                               ~f:Values.WiFiSimpleSetupConfiguration.of_json
                                               wiFiSimpleSetupConfiguration)
              ?brand ?model ?name
              ?capabilityReport:(Option.map
                                   ~f:Values.CapabilityReport.of_json
                                   capabilityReport)
              ?capabilitySchemas:(Option.map
                                    ~f:Values.CapabilitySchemas.of_json
                                    capabilitySchemas) ?capabilities
              ?classification
              ?hubNetworkMode:(Option.map ~f:Values.HubNetworkMode.of_json
                                 hubNetworkMode)
              ?metaData:(Option.map ~f:Values.MetaData.of_json metaData)
              ~identifier ()) None None])
let update_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventType =
         flag "event-type" (required json_arg) ~doc:"JSON EventType"
       and destinationName =
         flag "destination-name" (required string)
           ~doc:"STRING DestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_notification_configuration
           (Values.UpdateNotificationConfigurationRequest.make
              ~eventType:(Values.EventType.of_json eventType)
              ~destinationName ()) None None])
let update_ota_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING OtaDescription"
       and taskConfigurationId =
         flag "task-configuration-id" (optional string)
           ~doc:"STRING OtaTaskConfigurationId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING OtaTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_ota_task
           (Values.UpdateOtaTaskRequest.make ?description
              ?taskConfigurationId ~identifier ()) None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-account-association", create_account_association);
    ("create-cloud-connector", create_cloud_connector);
    ("create-connector-destination", create_connector_destination);
    ("create-credential-locker", create_credential_locker);
    ("create-destination", create_destination);
    ("create-event-log-configuration", create_event_log_configuration);
    ("create-managed-thing", create_managed_thing);
    ("create-notification-configuration", create_notification_configuration);
    ("create-ota-task", create_ota_task);
    ("create-ota-task-configuration", create_ota_task_configuration);
    ("create-provisioning-profile", create_provisioning_profile);
    ("delete-account-association", delete_account_association);
    ("delete-cloud-connector", delete_cloud_connector);
    ("delete-connector-destination", delete_connector_destination);
    ("delete-credential-locker", delete_credential_locker);
    ("delete-destination", delete_destination);
    ("delete-event-log-configuration", delete_event_log_configuration);
    ("delete-managed-thing", delete_managed_thing);
    ("delete-notification-configuration", delete_notification_configuration);
    ("delete-ota-task", delete_ota_task);
    ("delete-ota-task-configuration", delete_ota_task_configuration);
    ("delete-provisioning-profile", delete_provisioning_profile);
    ("deregister-account-association", deregister_account_association);
    ("get-account-association", get_account_association);
    ("get-cloud-connector", get_cloud_connector);
    ("get-connector-destination", get_connector_destination);
    ("get-credential-locker", get_credential_locker);
    ("get-custom-endpoint", get_custom_endpoint);
    ("get-default-encryption-configuration",
      get_default_encryption_configuration);
    ("get-destination", get_destination);
    ("get-device-discovery", get_device_discovery);
    ("get-event-log-configuration", get_event_log_configuration);
    ("get-hub-configuration", get_hub_configuration);
    ("get-managed-thing", get_managed_thing);
    ("get-managed-thing-capabilities", get_managed_thing_capabilities);
    ("get-managed-thing-certificate", get_managed_thing_certificate);
    ("get-managed-thing-connectivity-data",
      get_managed_thing_connectivity_data);
    ("get-managed-thing-meta-data", get_managed_thing_meta_data);
    ("get-managed-thing-state", get_managed_thing_state);
    ("get-notification-configuration", get_notification_configuration);
    ("get-ota-task", get_ota_task);
    ("get-ota-task-configuration", get_ota_task_configuration);
    ("get-provisioning-profile", get_provisioning_profile);
    ("get-runtime-log-configuration", get_runtime_log_configuration);
    ("get-schema-version", get_schema_version);
    ("list-account-associations", list_account_associations);
    ("list-cloud-connectors", list_cloud_connectors);
    ("list-connector-destinations", list_connector_destinations);
    ("list-credential-lockers", list_credential_lockers);
    ("list-destinations", list_destinations);
    ("list-device-discoveries", list_device_discoveries);
    ("list-discovered-devices", list_discovered_devices);
    ("list-event-log-configurations", list_event_log_configurations);
    ("list-managed-thing-account-associations",
      list_managed_thing_account_associations);
    ("list-managed-thing-schemas", list_managed_thing_schemas);
    ("list-managed-things", list_managed_things);
    ("list-notification-configurations", list_notification_configurations);
    ("list-ota-task-configurations", list_ota_task_configurations);
    ("list-ota-task-executions", list_ota_task_executions);
    ("list-ota-tasks", list_ota_tasks);
    ("list-provisioning-profiles", list_provisioning_profiles);
    ("list-schema-versions", list_schema_versions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-default-encryption-configuration",
      put_default_encryption_configuration);
    ("put-hub-configuration", put_hub_configuration);
    ("put-runtime-log-configuration", put_runtime_log_configuration);
    ("register-account-association", register_account_association);
    ("register-custom-endpoint", register_custom_endpoint);
    ("reset-runtime-log-configuration", reset_runtime_log_configuration);
    ("send-connector-event", send_connector_event);
    ("send-managed-thing-command", send_managed_thing_command);
    ("start-account-association-refresh", start_account_association_refresh);
    ("start-device-discovery", start_device_discovery);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-account-association", update_account_association);
    ("update-cloud-connector", update_cloud_connector);
    ("update-connector-destination", update_connector_destination);
    ("update-destination", update_destination);
    ("update-event-log-configuration", update_event_log_configuration);
    ("update-managed-thing", update_managed_thing);
    ("update-notification-configuration", update_notification_configuration);
    ("update-ota-task", update_ota_task)]
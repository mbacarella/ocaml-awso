(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_handshake =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and handshakeId =
         flag "handshake-id" (required string) ~doc:"STRING HandshakeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_handshake
           (Values.AcceptHandshakeRequest.make ~handshakeId ())
           (Some Values.AcceptHandshakeResponse.to_json)
           (Some Values.AcceptHandshakeResponse.error_to_json)])
let attach_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId"
       and targetId =
         flag "target-id" (required string) ~doc:"STRING PolicyTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_policy
           (Values.AttachPolicyRequest.make ~policyId ~targetId ()) None None])
let cancel_handshake =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and handshakeId =
         flag "handshake-id" (required string) ~doc:"STRING HandshakeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_handshake
           (Values.CancelHandshakeRequest.make ~handshakeId ())
           (Some Values.CancelHandshakeResponse.to_json)
           (Some Values.CancelHandshakeResponse.error_to_json)])
let close_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.close_account (Values.CloseAccountRequest.make ~accountId ())
           None None])
let create_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (optional string) ~doc:"STRING RoleName"
       and iamUserAccessToBilling =
         flag "iam-user-access-to-billing" (optional json_arg)
           ~doc:"JSON IAMUserAccessToBilling"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and email = flag "email" (required string) ~doc:"STRING Email"
       and accountName =
         flag "account-name" (required string)
           ~doc:"STRING CreateAccountName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_account
           (Values.CreateAccountRequest.make ?roleName
              ?iamUserAccessToBilling:(Option.map
                                         ~f:Values.IAMUserAccessToBilling.of_json
                                         iamUserAccessToBilling)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~email
              ~accountName ()) (Some Values.CreateAccountResponse.to_json)
           (Some Values.CreateAccountResponse.error_to_json)])
let create_gov_cloud_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (optional string) ~doc:"STRING RoleName"
       and iamUserAccessToBilling =
         flag "iam-user-access-to-billing" (optional json_arg)
           ~doc:"JSON IAMUserAccessToBilling"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and email = flag "email" (required string) ~doc:"STRING Email"
       and accountName =
         flag "account-name" (required string)
           ~doc:"STRING CreateAccountName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_gov_cloud_account
           (Values.CreateGovCloudAccountRequest.make ?roleName
              ?iamUserAccessToBilling:(Option.map
                                         ~f:Values.IAMUserAccessToBilling.of_json
                                         iamUserAccessToBilling)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~email
              ~accountName ())
           (Some Values.CreateGovCloudAccountResponse.to_json)
           (Some Values.CreateGovCloudAccountResponse.error_to_json)])
let create_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and featureSet =
         flag "feature-set" (optional json_arg)
           ~doc:"JSON OrganizationFeatureSet" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_organization
           (Values.CreateOrganizationRequest.make
              ?featureSet:(Option.map
                             ~f:Values.OrganizationFeatureSet.of_json
                             featureSet) ())
           (Some Values.CreateOrganizationResponse.to_json)
           (Some Values.CreateOrganizationResponse.error_to_json)])
let create_organizational_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and parentId =
         flag "parent-id" (required string) ~doc:"STRING ParentId"
       and name =
         flag "name" (required string) ~doc:"STRING OrganizationalUnitName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_organizational_unit
           (Values.CreateOrganizationalUnitRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~parentId ~name
              ()) (Some Values.CreateOrganizationalUnitResponse.to_json)
           (Some Values.CreateOrganizationalUnitResponse.error_to_json)])
let create_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and content =
         flag "content" (required string) ~doc:"STRING PolicyContent"
       and description =
         flag "description" (required string) ~doc:"STRING PolicyDescription"
       and name = flag "name" (required string) ~doc:"STRING PolicyName"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON PolicyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_policy
           (Values.CreatePolicyRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~content
              ~description ~name ~type_:(Values.PolicyType.of_json type_) ())
           (Some Values.CreatePolicyResponse.to_json)
           (Some Values.CreatePolicyResponse.error_to_json)])
let decline_handshake =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and handshakeId =
         flag "handshake-id" (required string) ~doc:"STRING HandshakeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.decline_handshake
           (Values.DeclineHandshakeRequest.make ~handshakeId ())
           (Some Values.DeclineHandshakeResponse.to_json)
           (Some Values.DeclineHandshakeResponse.error_to_json)])
let delete_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_organization (Fn.id ()) None None])
let delete_organizational_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationalUnitId =
         flag "organizational-unit-id" (required string)
           ~doc:"STRING OrganizationalUnitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_organizational_unit
           (Values.DeleteOrganizationalUnitRequest.make ~organizationalUnitId
              ()) None None])
let delete_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy (Values.DeletePolicyRequest.make ~policyId ())
           None None])
let deregister_delegated_administrator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and servicePrincipal =
         flag "service-principal" (required string)
           ~doc:"STRING ServicePrincipal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_delegated_administrator
           (Values.DeregisterDelegatedAdministratorRequest.make ~accountId
              ~servicePrincipal ()) None None])
let describe_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account
           (Values.DescribeAccountRequest.make ~accountId ())
           (Some Values.DescribeAccountResponse.to_json)
           (Some Values.DescribeAccountResponse.error_to_json)])
let describe_create_account_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and createAccountRequestId =
         flag "create-account-request-id" (required string)
           ~doc:"STRING CreateAccountRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_create_account_status
           (Values.DescribeCreateAccountStatusRequest.make
              ~createAccountRequestId ())
           (Some Values.DescribeCreateAccountStatusResponse.to_json)
           (Some Values.DescribeCreateAccountStatusResponse.error_to_json)])
let describe_effective_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetId =
         flag "target-id" (optional string) ~doc:"STRING PolicyTargetId"
       and policyType =
         flag "policy-type" (required json_arg)
           ~doc:"JSON EffectivePolicyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_effective_policy
           (Values.DescribeEffectivePolicyRequest.make ?targetId
              ~policyType:(Values.EffectivePolicyType.of_json policyType) ())
           (Some Values.DescribeEffectivePolicyResponse.to_json)
           (Some Values.DescribeEffectivePolicyResponse.error_to_json)])
let describe_handshake =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and handshakeId =
         flag "handshake-id" (required string) ~doc:"STRING HandshakeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_handshake
           (Values.DescribeHandshakeRequest.make ~handshakeId ())
           (Some Values.DescribeHandshakeResponse.to_json)
           (Some Values.DescribeHandshakeResponse.error_to_json)])
let describe_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_organization (Fn.id ())
           (Some Values.DescribeOrganizationResponse.to_json)
           (Some Values.DescribeOrganizationResponse.error_to_json)])
let describe_organizational_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationalUnitId =
         flag "organizational-unit-id" (required string)
           ~doc:"STRING OrganizationalUnitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_organizational_unit
           (Values.DescribeOrganizationalUnitRequest.make
              ~organizationalUnitId ())
           (Some Values.DescribeOrganizationalUnitResponse.to_json)
           (Some Values.DescribeOrganizationalUnitResponse.error_to_json)])
let describe_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_policy
           (Values.DescribePolicyRequest.make ~policyId ())
           (Some Values.DescribePolicyResponse.to_json)
           (Some Values.DescribePolicyResponse.error_to_json)])
let detach_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId"
       and targetId =
         flag "target-id" (required string) ~doc:"STRING PolicyTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_policy
           (Values.DetachPolicyRequest.make ~policyId ~targetId ()) None None])
let disable_a_w_s_service_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and servicePrincipal =
         flag "service-principal" (required string)
           ~doc:"STRING ServicePrincipal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_a_w_s_service_access
           (Values.DisableAWSServiceAccessRequest.make ~servicePrincipal ())
           None None])
let disable_policy_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and rootId = flag "root-id" (required string) ~doc:"STRING RootId"
       and policyType =
         flag "policy-type" (required json_arg) ~doc:"JSON PolicyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_policy_type
           (Values.DisablePolicyTypeRequest.make ~rootId
              ~policyType:(Values.PolicyType.of_json policyType) ())
           (Some Values.DisablePolicyTypeResponse.to_json)
           (Some Values.DisablePolicyTypeResponse.error_to_json)])
let enable_a_w_s_service_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and servicePrincipal =
         flag "service-principal" (required string)
           ~doc:"STRING ServicePrincipal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_a_w_s_service_access
           (Values.EnableAWSServiceAccessRequest.make ~servicePrincipal ())
           None None])
let enable_all_features =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_all_features (Values.EnableAllFeaturesRequest.make ())
           (Some Values.EnableAllFeaturesResponse.to_json)
           (Some Values.EnableAllFeaturesResponse.error_to_json)])
let enable_policy_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and rootId = flag "root-id" (required string) ~doc:"STRING RootId"
       and policyType =
         flag "policy-type" (required json_arg) ~doc:"JSON PolicyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_policy_type
           (Values.EnablePolicyTypeRequest.make ~rootId
              ~policyType:(Values.PolicyType.of_json policyType) ())
           (Some Values.EnablePolicyTypeResponse.to_json)
           (Some Values.EnablePolicyTypeResponse.error_to_json)])
let invite_account_to_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notes =
         flag "notes" (optional string) ~doc:"STRING HandshakeNotes"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and target =
         flag "target" (required json_arg) ~doc:"JSON HandshakeParty" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invite_account_to_organization
           (Values.InviteAccountToOrganizationRequest.make ?notes
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~target:(Values.HandshakeParty.of_json target) ())
           (Some Values.InviteAccountToOrganizationResponse.to_json)
           (Some Values.InviteAccountToOrganizationResponse.error_to_json)])
let leave_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.leave_organization (Fn.id ()) None None])
let list_a_w_s_service_access_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_a_w_s_service_access_for_organization
           (Values.ListAWSServiceAccessForOrganizationRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListAWSServiceAccessForOrganizationResponse.to_json)
           (Some
              Values.ListAWSServiceAccessForOrganizationResponse.error_to_json)])
let list_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_accounts
           (Values.ListAccountsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListAccountsResponse.to_json)
           (Some Values.ListAccountsResponse.error_to_json)])
let list_accounts_for_parent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and parentId =
         flag "parent-id" (required string) ~doc:"STRING ParentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_accounts_for_parent
           (Values.ListAccountsForParentRequest.make ?nextToken ?maxResults
              ~parentId ())
           (Some Values.ListAccountsForParentResponse.to_json)
           (Some Values.ListAccountsForParentResponse.error_to_json)])
let list_children =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and parentId =
         flag "parent-id" (required string) ~doc:"STRING ParentId"
       and childType =
         flag "child-type" (required json_arg) ~doc:"JSON ChildType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_children
           (Values.ListChildrenRequest.make ?nextToken ?maxResults ~parentId
              ~childType:(Values.ChildType.of_json childType) ())
           (Some Values.ListChildrenResponse.to_json)
           (Some Values.ListChildrenResponse.error_to_json)])
let list_create_account_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and states =
         flag "states" (optional json_arg) ~doc:"JSON CreateAccountStates"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_create_account_status
           (Values.ListCreateAccountStatusRequest.make
              ?states:(Option.map ~f:Values.CreateAccountStates.of_json
                         states) ?nextToken ?maxResults ())
           (Some Values.ListCreateAccountStatusResponse.to_json)
           (Some Values.ListCreateAccountStatusResponse.error_to_json)])
let list_delegated_administrators =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and servicePrincipal =
         flag "service-principal" (optional string)
           ~doc:"STRING ServicePrincipal"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_delegated_administrators
           (Values.ListDelegatedAdministratorsRequest.make ?servicePrincipal
              ?nextToken ?maxResults ())
           (Some Values.ListDelegatedAdministratorsResponse.to_json)
           (Some Values.ListDelegatedAdministratorsResponse.error_to_json)])
let list_delegated_services_for_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_delegated_services_for_account
           (Values.ListDelegatedServicesForAccountRequest.make ?nextToken
              ?maxResults ~accountId ())
           (Some Values.ListDelegatedServicesForAccountResponse.to_json)
           (Some Values.ListDelegatedServicesForAccountResponse.error_to_json)])
let list_handshakes_for_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON HandshakeFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_handshakes_for_account
           (Values.ListHandshakesForAccountRequest.make
              ?filter:(Option.map ~f:Values.HandshakeFilter.of_json filter)
              ?nextToken ?maxResults ())
           (Some Values.ListHandshakesForAccountResponse.to_json)
           (Some Values.ListHandshakesForAccountResponse.error_to_json)])
let list_handshakes_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON HandshakeFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_handshakes_for_organization
           (Values.ListHandshakesForOrganizationRequest.make
              ?filter:(Option.map ~f:Values.HandshakeFilter.of_json filter)
              ?nextToken ?maxResults ())
           (Some Values.ListHandshakesForOrganizationResponse.to_json)
           (Some Values.ListHandshakesForOrganizationResponse.error_to_json)])
let list_organizational_units_for_parent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and parentId =
         flag "parent-id" (required string) ~doc:"STRING ParentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_organizational_units_for_parent
           (Values.ListOrganizationalUnitsForParentRequest.make ?nextToken
              ?maxResults ~parentId ())
           (Some Values.ListOrganizationalUnitsForParentResponse.to_json)
           (Some
              Values.ListOrganizationalUnitsForParentResponse.error_to_json)])
let list_parents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and childId = flag "child-id" (required string) ~doc:"STRING ChildId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_parents
           (Values.ListParentsRequest.make ?nextToken ?maxResults ~childId ())
           (Some Values.ListParentsResponse.to_json)
           (Some Values.ListParentsResponse.error_to_json)])
let list_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filter = flag "filter" (required json_arg) ~doc:"JSON PolicyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policies
           (Values.ListPoliciesRequest.make ?nextToken ?maxResults
              ~filter:(Values.PolicyType.of_json filter) ())
           (Some Values.ListPoliciesResponse.to_json)
           (Some Values.ListPoliciesResponse.error_to_json)])
let list_policies_for_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and targetId =
         flag "target-id" (required string) ~doc:"STRING PolicyTargetId"
       and filter = flag "filter" (required json_arg) ~doc:"JSON PolicyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policies_for_target
           (Values.ListPoliciesForTargetRequest.make ?nextToken ?maxResults
              ~targetId ~filter:(Values.PolicyType.of_json filter) ())
           (Some Values.ListPoliciesForTargetResponse.to_json)
           (Some Values.ListPoliciesForTargetResponse.error_to_json)])
let list_roots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_roots
           (Values.ListRootsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListRootsResponse.to_json)
           (Some Values.ListRootsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and resourceId =
         flag "resource-id" (required string)
           ~doc:"STRING TaggableResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?nextToken ~resourceId ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_targets_for_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_targets_for_policy
           (Values.ListTargetsForPolicyRequest.make ?nextToken ?maxResults
              ~policyId ())
           (Some Values.ListTargetsForPolicyResponse.to_json)
           (Some Values.ListTargetsForPolicyResponse.error_to_json)])
let move_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and sourceParentId =
         flag "source-parent-id" (required string) ~doc:"STRING ParentId"
       and destinationParentId =
         flag "destination-parent-id" (required string)
           ~doc:"STRING ParentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.move_account
           (Values.MoveAccountRequest.make ~accountId ~sourceParentId
              ~destinationParentId ()) None None])
let register_delegated_administrator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and servicePrincipal =
         flag "service-principal" (required string)
           ~doc:"STRING ServicePrincipal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_delegated_administrator
           (Values.RegisterDelegatedAdministratorRequest.make ~accountId
              ~servicePrincipal ()) None None])
let remove_account_from_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_account_from_organization
           (Values.RemoveAccountFromOrganizationRequest.make ~accountId ())
           None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string)
           ~doc:"STRING TaggableResourceId"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceId
              ~tags:(Values.Tags.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string)
           ~doc:"STRING TaggableResourceId"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceId
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ()) None None])
let update_organizational_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING OrganizationalUnitName"
       and organizationalUnitId =
         flag "organizational-unit-id" (required string)
           ~doc:"STRING OrganizationalUnitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_organizational_unit
           (Values.UpdateOrganizationalUnitRequest.make ?name
              ~organizationalUnitId ())
           (Some Values.UpdateOrganizationalUnitResponse.to_json)
           (Some Values.UpdateOrganizationalUnitResponse.error_to_json)])
let update_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING PolicyName"
       and description =
         flag "description" (optional string) ~doc:"STRING PolicyDescription"
       and content =
         flag "content" (optional string) ~doc:"STRING PolicyContent"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_policy
           (Values.UpdatePolicyRequest.make ?name ?description ?content
              ~policyId ()) (Some Values.UpdatePolicyResponse.to_json)
           (Some Values.UpdatePolicyResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("accept-handshake", accept_handshake);
    ("attach-policy", attach_policy);
    ("cancel-handshake", cancel_handshake);
    ("close-account", close_account);
    ("create-account", create_account);
    ("create-gov-cloud-account", create_gov_cloud_account);
    ("create-organization", create_organization);
    ("create-organizational-unit", create_organizational_unit);
    ("create-policy", create_policy);
    ("decline-handshake", decline_handshake);
    ("delete-organization", delete_organization);
    ("delete-organizational-unit", delete_organizational_unit);
    ("delete-policy", delete_policy);
    ("deregister-delegated-administrator",
      deregister_delegated_administrator);
    ("describe-account", describe_account);
    ("describe-create-account-status", describe_create_account_status);
    ("describe-effective-policy", describe_effective_policy);
    ("describe-handshake", describe_handshake);
    ("describe-organization", describe_organization);
    ("describe-organizational-unit", describe_organizational_unit);
    ("describe-policy", describe_policy);
    ("detach-policy", detach_policy);
    ("disable-a-w-s-service-access", disable_a_w_s_service_access);
    ("disable-policy-type", disable_policy_type);
    ("enable-a-w-s-service-access", enable_a_w_s_service_access);
    ("enable-all-features", enable_all_features);
    ("enable-policy-type", enable_policy_type);
    ("invite-account-to-organization", invite_account_to_organization);
    ("leave-organization", leave_organization);
    ("list-a-w-s-service-access-for-organization",
      list_a_w_s_service_access_for_organization);
    ("list-accounts", list_accounts);
    ("list-accounts-for-parent", list_accounts_for_parent);
    ("list-children", list_children);
    ("list-create-account-status", list_create_account_status);
    ("list-delegated-administrators", list_delegated_administrators);
    ("list-delegated-services-for-account",
      list_delegated_services_for_account);
    ("list-handshakes-for-account", list_handshakes_for_account);
    ("list-handshakes-for-organization", list_handshakes_for_organization);
    ("list-organizational-units-for-parent",
      list_organizational_units_for_parent);
    ("list-parents", list_parents);
    ("list-policies", list_policies);
    ("list-policies-for-target", list_policies_for_target);
    ("list-roots", list_roots);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-targets-for-policy", list_targets_for_policy);
    ("move-account", move_account);
    ("register-delegated-administrator", register_delegated_administrator);
    ("remove-account-from-organization", remove_account_from_organization);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-organizational-unit", update_organizational_unit);
    ("update-policy", update_policy)]
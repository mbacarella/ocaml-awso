(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let activate_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayType =
         flag "gateway-type" (optional string) ~doc:"STRING GatewayType"
       and tapeDriveType =
         flag "tape-drive-type" (optional string) ~doc:"STRING TapeDriveType"
       and mediumChangerType =
         flag "medium-changer-type" (optional string)
           ~doc:"STRING MediumChangerType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and activationKey =
         flag "activation-key" (required string) ~doc:"STRING ActivationKey"
       and gatewayName =
         flag "gateway-name" (required string) ~doc:"STRING GatewayName"
       and gatewayTimezone =
         flag "gateway-timezone" (required string)
           ~doc:"STRING GatewayTimezone"
       and gatewayRegion =
         flag "gateway-region" (required string) ~doc:"STRING RegionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.activate_gateway
           (Values.ActivateGatewayInput.make ?gatewayType ?tapeDriveType
              ?mediumChangerType
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~activationKey
              ~gatewayName ~gatewayTimezone ~gatewayRegion ())
           (Some Values.ActivateGatewayOutput.to_json)
           (Some Values.ActivateGatewayOutput.error_to_json)])
let add_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and diskIds = flag "disk-ids" (required json_arg) ~doc:"JSON DiskIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_cache
           (Values.AddCacheInput.make ~gatewayARN
              ~diskIds:(Values.DiskIds.of_json diskIds) ())
           (Some Values.AddCacheOutput.to_json)
           (Some Values.AddCacheOutput.error_to_json)])
let add_tags_to_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceARN"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags_to_resource
           (Values.AddTagsToResourceInput.make ~resourceARN
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.AddTagsToResourceOutput.to_json)
           (Some Values.AddTagsToResourceOutput.error_to_json)])
let add_upload_buffer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and diskIds = flag "disk-ids" (required json_arg) ~doc:"JSON DiskIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_upload_buffer
           (Values.AddUploadBufferInput.make ~gatewayARN
              ~diskIds:(Values.DiskIds.of_json diskIds) ())
           (Some Values.AddUploadBufferOutput.to_json)
           (Some Values.AddUploadBufferOutput.error_to_json)])
let add_working_storage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and diskIds = flag "disk-ids" (required json_arg) ~doc:"JSON DiskIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_working_storage
           (Values.AddWorkingStorageInput.make ~gatewayARN
              ~diskIds:(Values.DiskIds.of_json diskIds) ())
           (Some Values.AddWorkingStorageOutput.to_json)
           (Some Values.AddWorkingStorageOutput.error_to_json)])
let assign_tape_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bypassGovernanceRetention =
         flag "bypass-governance-retention" (optional bool)
           ~doc:"BOOL Boolean__lc1"
       and tapeARN =
         flag "tape-a-r-n" (required string) ~doc:"STRING TapeARN"
       and poolId = flag "pool-id" (required string) ~doc:"STRING PoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assign_tape_pool
           (Values.AssignTapePoolInput.make ?bypassGovernanceRetention
              ~tapeARN ~poolId ()) (Some Values.AssignTapePoolOutput.to_json)
           (Some Values.AssignTapePoolOutput.error_to_json)])
let associate_file_system =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and auditDestinationARN =
         flag "audit-destination-a-r-n" (optional string)
           ~doc:"STRING AuditDestinationARN"
       and cacheAttributes =
         flag "cache-attributes" (optional json_arg)
           ~doc:"JSON CacheAttributes"
       and endpointNetworkConfiguration =
         flag "endpoint-network-configuration" (optional json_arg)
           ~doc:"JSON EndpointNetworkConfiguration"
       and userName =
         flag "user-name" (required string) ~doc:"STRING DomainUserName"
       and password =
         flag "password" (required string) ~doc:"STRING DomainUserPassword"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and locationARN =
         flag "location-a-r-n" (required string)
           ~doc:"STRING FileSystemLocationARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_file_system
           (Values.AssociateFileSystemInput.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?auditDestinationARN
              ?cacheAttributes:(Option.map ~f:Values.CacheAttributes.of_json
                                  cacheAttributes)
              ?endpointNetworkConfiguration:(Option.map
                                               ~f:Values.EndpointNetworkConfiguration.of_json
                                               endpointNetworkConfiguration)
              ~userName ~password ~clientToken ~gatewayARN ~locationARN ())
           (Some Values.AssociateFileSystemOutput.to_json)
           (Some Values.AssociateFileSystemOutput.error_to_json)])
let attach_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetName =
         flag "target-name" (optional string) ~doc:"STRING TargetName"
       and diskId = flag "disk-id" (optional string) ~doc:"STRING DiskId"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and volumeARN =
         flag "volume-a-r-n" (required string) ~doc:"STRING VolumeARN"
       and networkInterfaceId =
         flag "network-interface-id" (required string)
           ~doc:"STRING NetworkInterfaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_volume
           (Values.AttachVolumeInput.make ?targetName ?diskId ~gatewayARN
              ~volumeARN ~networkInterfaceId ())
           (Some Values.AttachVolumeOutput.to_json)
           (Some Values.AttachVolumeOutput.error_to_json)])
let cancel_archival =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and tapeARN =
         flag "tape-a-r-n" (required string) ~doc:"STRING TapeARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_archival
           (Values.CancelArchivalInput.make ~gatewayARN ~tapeARN ())
           (Some Values.CancelArchivalOutput.to_json)
           (Some Values.CancelArchivalOutput.error_to_json)])
let cancel_retrieval =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and tapeARN =
         flag "tape-a-r-n" (required string) ~doc:"STRING TapeARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_retrieval
           (Values.CancelRetrievalInput.make ~gatewayARN ~tapeARN ())
           (Some Values.CancelRetrievalOutput.to_json)
           (Some Values.CancelRetrievalOutput.error_to_json)])
let create_cachedi_s_c_s_i_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotId =
         flag "snapshot-id" (optional string) ~doc:"STRING SnapshotId"
       and sourceVolumeARN =
         flag "source-volume-a-r-n" (optional string) ~doc:"STRING VolumeARN"
       and kMSEncrypted =
         flag "k-m-s-encrypted" (optional bool) ~doc:"BOOL Boolean"
       and kMSKey = flag "k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and volumeSizeInBytes =
         flag "volume-size-in-bytes" (required json_arg) ~doc:"JSON long"
       and targetName =
         flag "target-name" (required string) ~doc:"STRING TargetName"
       and networkInterfaceId =
         flag "network-interface-id" (required string)
           ~doc:"STRING NetworkInterfaceId"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cachedi_s_c_s_i_volume
           (Values.CreateCachediSCSIVolumeInput.make ?snapshotId
              ?sourceVolumeARN ?kMSEncrypted ?kMSKey
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~gatewayARN
              ~volumeSizeInBytes:(Values.Long.of_json volumeSizeInBytes)
              ~targetName ~networkInterfaceId ~clientToken ())
           (Some Values.CreateCachediSCSIVolumeOutput.to_json)
           (Some Values.CreateCachediSCSIVolumeOutput.error_to_json)])
let create_n_f_s_file_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nFSFileShareDefaults =
         flag "n-f-s-file-share-defaults" (optional json_arg)
           ~doc:"JSON NFSFileShareDefaults"
       and kMSEncrypted =
         flag "k-m-s-encrypted" (optional bool) ~doc:"BOOL Boolean"
       and kMSKey = flag "k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and defaultStorageClass =
         flag "default-storage-class" (optional string)
           ~doc:"STRING StorageClass"
       and objectACL =
         flag "object-a-c-l" (optional json_arg) ~doc:"JSON ObjectACL"
       and clientList =
         flag "client-list" (optional json_arg)
           ~doc:"JSON FileShareClientList"
       and squash = flag "squash" (optional string) ~doc:"STRING Squash"
       and readOnly = flag "read-only" (optional bool) ~doc:"BOOL Boolean"
       and guessMIMETypeEnabled =
         flag "guess-m-i-m-e-type-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and requesterPays =
         flag "requester-pays" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and fileShareName =
         flag "file-share-name" (optional string) ~doc:"STRING FileShareName"
       and cacheAttributes =
         flag "cache-attributes" (optional json_arg)
           ~doc:"JSON CacheAttributes"
       and notificationPolicy =
         flag "notification-policy" (optional string)
           ~doc:"STRING NotificationPolicy"
       and vPCEndpointDNSName =
         flag "v-p-c-endpoint-d-n-s-name" (optional string)
           ~doc:"STRING DNSHostName"
       and bucketRegion =
         flag "bucket-region" (optional string) ~doc:"STRING RegionId"
       and auditDestinationARN =
         flag "audit-destination-a-r-n" (optional string)
           ~doc:"STRING AuditDestinationARN"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and role = flag "role" (required string) ~doc:"STRING Role"
       and locationARN =
         flag "location-a-r-n" (required string) ~doc:"STRING LocationARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_n_f_s_file_share
           (Values.CreateNFSFileShareInput.make
              ?nFSFileShareDefaults:(Option.map
                                       ~f:Values.NFSFileShareDefaults.of_json
                                       nFSFileShareDefaults) ?kMSEncrypted
              ?kMSKey ?defaultStorageClass
              ?objectACL:(Option.map ~f:Values.ObjectACL.of_json objectACL)
              ?clientList:(Option.map ~f:Values.FileShareClientList.of_json
                             clientList) ?squash ?readOnly
              ?guessMIMETypeEnabled ?requesterPays
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?fileShareName
              ?cacheAttributes:(Option.map ~f:Values.CacheAttributes.of_json
                                  cacheAttributes) ?notificationPolicy
              ?vPCEndpointDNSName ?bucketRegion ?auditDestinationARN
              ~clientToken ~gatewayARN ~role ~locationARN ())
           (Some Values.CreateNFSFileShareOutput.to_json)
           (Some Values.CreateNFSFileShareOutput.error_to_json)])
let create_s_m_b_file_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kMSEncrypted =
         flag "k-m-s-encrypted" (optional bool) ~doc:"BOOL Boolean"
       and kMSKey = flag "k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and defaultStorageClass =
         flag "default-storage-class" (optional string)
           ~doc:"STRING StorageClass"
       and objectACL =
         flag "object-a-c-l" (optional json_arg) ~doc:"JSON ObjectACL"
       and readOnly = flag "read-only" (optional bool) ~doc:"BOOL Boolean"
       and guessMIMETypeEnabled =
         flag "guess-m-i-m-e-type-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and requesterPays =
         flag "requester-pays" (optional bool) ~doc:"BOOL Boolean"
       and sMBACLEnabled =
         flag "s-m-b-a-c-l-enabled" (optional bool) ~doc:"BOOL Boolean"
       and accessBasedEnumeration =
         flag "access-based-enumeration" (optional bool) ~doc:"BOOL Boolean"
       and adminUserList =
         flag "admin-user-list" (optional json_arg) ~doc:"JSON UserList"
       and validUserList =
         flag "valid-user-list" (optional json_arg) ~doc:"JSON UserList"
       and invalidUserList =
         flag "invalid-user-list" (optional json_arg) ~doc:"JSON UserList"
       and auditDestinationARN =
         flag "audit-destination-a-r-n" (optional string)
           ~doc:"STRING AuditDestinationARN"
       and authentication =
         flag "authentication" (optional string) ~doc:"STRING Authentication"
       and caseSensitivity =
         flag "case-sensitivity" (optional json_arg)
           ~doc:"JSON CaseSensitivity"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and fileShareName =
         flag "file-share-name" (optional string) ~doc:"STRING FileShareName"
       and cacheAttributes =
         flag "cache-attributes" (optional json_arg)
           ~doc:"JSON CacheAttributes"
       and notificationPolicy =
         flag "notification-policy" (optional string)
           ~doc:"STRING NotificationPolicy"
       and vPCEndpointDNSName =
         flag "v-p-c-endpoint-d-n-s-name" (optional string)
           ~doc:"STRING DNSHostName"
       and bucketRegion =
         flag "bucket-region" (optional string) ~doc:"STRING RegionId"
       and oplocksEnabled =
         flag "oplocks-enabled" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and role = flag "role" (required string) ~doc:"STRING Role"
       and locationARN =
         flag "location-a-r-n" (required string) ~doc:"STRING LocationARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_s_m_b_file_share
           (Values.CreateSMBFileShareInput.make ?kMSEncrypted ?kMSKey
              ?defaultStorageClass
              ?objectACL:(Option.map ~f:Values.ObjectACL.of_json objectACL)
              ?readOnly ?guessMIMETypeEnabled ?requesterPays ?sMBACLEnabled
              ?accessBasedEnumeration
              ?adminUserList:(Option.map ~f:Values.UserList.of_json
                                adminUserList)
              ?validUserList:(Option.map ~f:Values.UserList.of_json
                                validUserList)
              ?invalidUserList:(Option.map ~f:Values.UserList.of_json
                                  invalidUserList) ?auditDestinationARN
              ?authentication
              ?caseSensitivity:(Option.map ~f:Values.CaseSensitivity.of_json
                                  caseSensitivity)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?fileShareName
              ?cacheAttributes:(Option.map ~f:Values.CacheAttributes.of_json
                                  cacheAttributes) ?notificationPolicy
              ?vPCEndpointDNSName ?bucketRegion ?oplocksEnabled ~clientToken
              ~gatewayARN ~role ~locationARN ())
           (Some Values.CreateSMBFileShareOutput.to_json)
           (Some Values.CreateSMBFileShareOutput.error_to_json)])
let create_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and volumeARN =
         flag "volume-a-r-n" (required string) ~doc:"STRING VolumeARN"
       and snapshotDescription =
         flag "snapshot-description" (required string)
           ~doc:"STRING SnapshotDescription" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_snapshot
           (Values.CreateSnapshotInput.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~volumeARN
              ~snapshotDescription ())
           (Some Values.CreateSnapshotOutput.to_json)
           (Some Values.CreateSnapshotOutput.error_to_json)])
let create_snapshot_from_volume_recovery_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and volumeARN =
         flag "volume-a-r-n" (required string) ~doc:"STRING VolumeARN"
       and snapshotDescription =
         flag "snapshot-description" (required string)
           ~doc:"STRING SnapshotDescription" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_snapshot_from_volume_recovery_point
           (Values.CreateSnapshotFromVolumeRecoveryPointInput.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~volumeARN
              ~snapshotDescription ())
           (Some Values.CreateSnapshotFromVolumeRecoveryPointOutput.to_json)
           (Some
              Values.CreateSnapshotFromVolumeRecoveryPointOutput.error_to_json)])
let create_storedi_s_c_s_i_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotId =
         flag "snapshot-id" (optional string) ~doc:"STRING SnapshotId"
       and kMSEncrypted =
         flag "k-m-s-encrypted" (optional bool) ~doc:"BOOL Boolean"
       and kMSKey = flag "k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and diskId = flag "disk-id" (required string) ~doc:"STRING DiskId"
       and preserveExistingData =
         flag "preserve-existing-data" (required bool)
           ~doc:"BOOL Boolean__lc1"
       and targetName =
         flag "target-name" (required string) ~doc:"STRING TargetName"
       and networkInterfaceId =
         flag "network-interface-id" (required string)
           ~doc:"STRING NetworkInterfaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_storedi_s_c_s_i_volume
           (Values.CreateStorediSCSIVolumeInput.make ?snapshotId
              ?kMSEncrypted ?kMSKey
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~gatewayARN
              ~diskId ~preserveExistingData ~targetName ~networkInterfaceId
              ()) (Some Values.CreateStorediSCSIVolumeOutput.to_json)
           (Some Values.CreateStorediSCSIVolumeOutput.error_to_json)])
let create_tape_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retentionLockType =
         flag "retention-lock-type" (optional json_arg)
           ~doc:"JSON RetentionLockType"
       and retentionLockTimeInDays =
         flag "retention-lock-time-in-days" (optional int)
           ~doc:"INT RetentionLockTimeInDays"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and poolName =
         flag "pool-name" (required string) ~doc:"STRING PoolName"
       and storageClass =
         flag "storage-class" (required json_arg)
           ~doc:"JSON TapeStorageClass" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_tape_pool
           (Values.CreateTapePoolInput.make
              ?retentionLockType:(Option.map
                                    ~f:Values.RetentionLockType.of_json
                                    retentionLockType)
              ?retentionLockTimeInDays
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~poolName
              ~storageClass:(Values.TapeStorageClass.of_json storageClass) ())
           (Some Values.CreateTapePoolOutput.to_json)
           (Some Values.CreateTapePoolOutput.error_to_json)])
let create_tape_with_barcode =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kMSEncrypted =
         flag "k-m-s-encrypted" (optional bool) ~doc:"BOOL Boolean"
       and kMSKey = flag "k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and poolId = flag "pool-id" (optional string) ~doc:"STRING PoolId"
       and worm = flag "worm" (optional bool) ~doc:"BOOL Boolean__lc1"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and tapeSizeInBytes =
         flag "tape-size-in-bytes" (required json_arg) ~doc:"JSON TapeSize"
       and tapeBarcode =
         flag "tape-barcode" (required string) ~doc:"STRING TapeBarcode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_tape_with_barcode
           (Values.CreateTapeWithBarcodeInput.make ?kMSEncrypted ?kMSKey
              ?poolId ?worm ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~gatewayARN
              ~tapeSizeInBytes:(Values.TapeSize.of_json tapeSizeInBytes)
              ~tapeBarcode ())
           (Some Values.CreateTapeWithBarcodeOutput.to_json)
           (Some Values.CreateTapeWithBarcodeOutput.error_to_json)])
let create_tapes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kMSEncrypted =
         flag "k-m-s-encrypted" (optional bool) ~doc:"BOOL Boolean"
       and kMSKey = flag "k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and poolId = flag "pool-id" (optional string) ~doc:"STRING PoolId"
       and worm = flag "worm" (optional bool) ~doc:"BOOL Boolean__lc1"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and tapeSizeInBytes =
         flag "tape-size-in-bytes" (required json_arg) ~doc:"JSON TapeSize"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and numTapesToCreate =
         flag "num-tapes-to-create" (required int)
           ~doc:"INT NumTapesToCreate"
       and tapeBarcodePrefix =
         flag "tape-barcode-prefix" (required string)
           ~doc:"STRING TapeBarcodePrefix" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_tapes
           (Values.CreateTapesInput.make ?kMSEncrypted ?kMSKey ?poolId ?worm
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~gatewayARN
              ~tapeSizeInBytes:(Values.TapeSize.of_json tapeSizeInBytes)
              ~clientToken ~numTapesToCreate ~tapeBarcodePrefix ())
           (Some Values.CreateTapesOutput.to_json)
           (Some Values.CreateTapesOutput.error_to_json)])
let delete_automatic_tape_creation_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_automatic_tape_creation_policy
           (Values.DeleteAutomaticTapeCreationPolicyInput.make ~gatewayARN ())
           (Some Values.DeleteAutomaticTapeCreationPolicyOutput.to_json)
           (Some Values.DeleteAutomaticTapeCreationPolicyOutput.error_to_json)])
let delete_bandwidth_rate_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and bandwidthType =
         flag "bandwidth-type" (required string) ~doc:"STRING BandwidthType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bandwidth_rate_limit
           (Values.DeleteBandwidthRateLimitInput.make ~gatewayARN
              ~bandwidthType ())
           (Some Values.DeleteBandwidthRateLimitOutput.to_json)
           (Some Values.DeleteBandwidthRateLimitOutput.error_to_json)])
let delete_chap_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetARN =
         flag "target-a-r-n" (required string) ~doc:"STRING TargetARN"
       and initiatorName =
         flag "initiator-name" (required string) ~doc:"STRING IqnName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_chap_credentials
           (Values.DeleteChapCredentialsInput.make ~targetARN ~initiatorName
              ()) (Some Values.DeleteChapCredentialsOutput.to_json)
           (Some Values.DeleteChapCredentialsOutput.error_to_json)])
let delete_file_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL Boolean__lc1"
       and fileShareARN =
         flag "file-share-a-r-n" (required string) ~doc:"STRING FileShareARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_file_share
           (Values.DeleteFileShareInput.make ?forceDelete ~fileShareARN ())
           (Some Values.DeleteFileShareOutput.to_json)
           (Some Values.DeleteFileShareOutput.error_to_json)])
let delete_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_gateway (Values.DeleteGatewayInput.make ~gatewayARN ())
           (Some Values.DeleteGatewayOutput.to_json)
           (Some Values.DeleteGatewayOutput.error_to_json)])
let delete_snapshot_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and volumeARN =
         flag "volume-a-r-n" (required string) ~doc:"STRING VolumeARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_snapshot_schedule
           (Values.DeleteSnapshotScheduleInput.make ~volumeARN ())
           (Some Values.DeleteSnapshotScheduleOutput.to_json)
           (Some Values.DeleteSnapshotScheduleOutput.error_to_json)])
let delete_tape =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bypassGovernanceRetention =
         flag "bypass-governance-retention" (optional bool)
           ~doc:"BOOL Boolean__lc1"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and tapeARN =
         flag "tape-a-r-n" (required string) ~doc:"STRING TapeARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tape
           (Values.DeleteTapeInput.make ?bypassGovernanceRetention
              ~gatewayARN ~tapeARN ()) (Some Values.DeleteTapeOutput.to_json)
           (Some Values.DeleteTapeOutput.error_to_json)])
let delete_tape_archive =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bypassGovernanceRetention =
         flag "bypass-governance-retention" (optional bool)
           ~doc:"BOOL Boolean__lc1"
       and tapeARN =
         flag "tape-a-r-n" (required string) ~doc:"STRING TapeARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tape_archive
           (Values.DeleteTapeArchiveInput.make ?bypassGovernanceRetention
              ~tapeARN ()) (Some Values.DeleteTapeArchiveOutput.to_json)
           (Some Values.DeleteTapeArchiveOutput.error_to_json)])
let delete_tape_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and poolARN =
         flag "pool-a-r-n" (required string) ~doc:"STRING PoolARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tape_pool (Values.DeleteTapePoolInput.make ~poolARN ())
           (Some Values.DeleteTapePoolOutput.to_json)
           (Some Values.DeleteTapePoolOutput.error_to_json)])
let delete_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and volumeARN =
         flag "volume-a-r-n" (required string) ~doc:"STRING VolumeARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_volume (Values.DeleteVolumeInput.make ~volumeARN ())
           (Some Values.DeleteVolumeOutput.to_json)
           (Some Values.DeleteVolumeOutput.error_to_json)])
let describe_availability_monitor_test =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_availability_monitor_test
           (Values.DescribeAvailabilityMonitorTestInput.make ~gatewayARN ())
           (Some Values.DescribeAvailabilityMonitorTestOutput.to_json)
           (Some Values.DescribeAvailabilityMonitorTestOutput.error_to_json)])
let describe_bandwidth_rate_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_bandwidth_rate_limit
           (Values.DescribeBandwidthRateLimitInput.make ~gatewayARN ())
           (Some Values.DescribeBandwidthRateLimitOutput.to_json)
           (Some Values.DescribeBandwidthRateLimitOutput.error_to_json)])
let describe_bandwidth_rate_limit_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_bandwidth_rate_limit_schedule
           (Values.DescribeBandwidthRateLimitScheduleInput.make ~gatewayARN
              ())
           (Some Values.DescribeBandwidthRateLimitScheduleOutput.to_json)
           (Some
              Values.DescribeBandwidthRateLimitScheduleOutput.error_to_json)])
let describe_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cache (Values.DescribeCacheInput.make ~gatewayARN ())
           (Some Values.DescribeCacheOutput.to_json)
           (Some Values.DescribeCacheOutput.error_to_json)])
let describe_cachedi_s_c_s_i_volumes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and volumeARNs =
         flag "volume-a-r-ns" (required json_arg) ~doc:"JSON VolumeARNs" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cachedi_s_c_s_i_volumes
           (Values.DescribeCachediSCSIVolumesInput.make
              ~volumeARNs:(Values.VolumeARNs.of_json volumeARNs) ())
           (Some Values.DescribeCachediSCSIVolumesOutput.to_json)
           (Some Values.DescribeCachediSCSIVolumesOutput.error_to_json)])
let describe_chap_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetARN =
         flag "target-a-r-n" (required string) ~doc:"STRING TargetARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_chap_credentials
           (Values.DescribeChapCredentialsInput.make ~targetARN ())
           (Some Values.DescribeChapCredentialsOutput.to_json)
           (Some Values.DescribeChapCredentialsOutput.error_to_json)])
let describe_file_system_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemAssociationARNList =
         flag "file-system-association-a-r-n-list" (required json_arg)
           ~doc:"JSON FileSystemAssociationARNList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_file_system_associations
           (Values.DescribeFileSystemAssociationsInput.make
              ~fileSystemAssociationARNList:(Values.FileSystemAssociationARNList.of_json
                                               fileSystemAssociationARNList)
              ()) (Some Values.DescribeFileSystemAssociationsOutput.to_json)
           (Some Values.DescribeFileSystemAssociationsOutput.error_to_json)])
let describe_gateway_information =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_gateway_information
           (Values.DescribeGatewayInformationInput.make ~gatewayARN ())
           (Some Values.DescribeGatewayInformationOutput.to_json)
           (Some Values.DescribeGatewayInformationOutput.error_to_json)])
let describe_maintenance_start_time =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_maintenance_start_time
           (Values.DescribeMaintenanceStartTimeInput.make ~gatewayARN ())
           (Some Values.DescribeMaintenanceStartTimeOutput.to_json)
           (Some Values.DescribeMaintenanceStartTimeOutput.error_to_json)])
let describe_n_f_s_file_shares =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileShareARNList =
         flag "file-share-a-r-n-list" (required json_arg)
           ~doc:"JSON FileShareARNList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_n_f_s_file_shares
           (Values.DescribeNFSFileSharesInput.make
              ~fileShareARNList:(Values.FileShareARNList.of_json
                                   fileShareARNList) ())
           (Some Values.DescribeNFSFileSharesOutput.to_json)
           (Some Values.DescribeNFSFileSharesOutput.error_to_json)])
let describe_s_m_b_file_shares =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileShareARNList =
         flag "file-share-a-r-n-list" (required json_arg)
           ~doc:"JSON FileShareARNList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_s_m_b_file_shares
           (Values.DescribeSMBFileSharesInput.make
              ~fileShareARNList:(Values.FileShareARNList.of_json
                                   fileShareARNList) ())
           (Some Values.DescribeSMBFileSharesOutput.to_json)
           (Some Values.DescribeSMBFileSharesOutput.error_to_json)])
let describe_s_m_b_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_s_m_b_settings
           (Values.DescribeSMBSettingsInput.make ~gatewayARN ())
           (Some Values.DescribeSMBSettingsOutput.to_json)
           (Some Values.DescribeSMBSettingsOutput.error_to_json)])
let describe_snapshot_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and volumeARN =
         flag "volume-a-r-n" (required string) ~doc:"STRING VolumeARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_snapshot_schedule
           (Values.DescribeSnapshotScheduleInput.make ~volumeARN ())
           (Some Values.DescribeSnapshotScheduleOutput.to_json)
           (Some Values.DescribeSnapshotScheduleOutput.error_to_json)])
let describe_storedi_s_c_s_i_volumes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and volumeARNs =
         flag "volume-a-r-ns" (required json_arg) ~doc:"JSON VolumeARNs" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_storedi_s_c_s_i_volumes
           (Values.DescribeStorediSCSIVolumesInput.make
              ~volumeARNs:(Values.VolumeARNs.of_json volumeARNs) ())
           (Some Values.DescribeStorediSCSIVolumesOutput.to_json)
           (Some Values.DescribeStorediSCSIVolumesOutput.error_to_json)])
let describe_tape_archives =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tapeARNs =
         flag "tape-a-r-ns" (optional json_arg) ~doc:"JSON TapeARNs"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveIntObject" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_tape_archives
           (Values.DescribeTapeArchivesInput.make
              ?tapeARNs:(Option.map ~f:Values.TapeARNs.of_json tapeARNs)
              ?marker ?limit ())
           (Some Values.DescribeTapeArchivesOutput.to_json)
           (Some Values.DescribeTapeArchivesOutput.error_to_json)])
let describe_tape_recovery_points =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveIntObject"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_tape_recovery_points
           (Values.DescribeTapeRecoveryPointsInput.make ?marker ?limit
              ~gatewayARN ())
           (Some Values.DescribeTapeRecoveryPointsOutput.to_json)
           (Some Values.DescribeTapeRecoveryPointsOutput.error_to_json)])
let describe_tapes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tapeARNs =
         flag "tape-a-r-ns" (optional json_arg) ~doc:"JSON TapeARNs"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveIntObject"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_tapes
           (Values.DescribeTapesInput.make
              ?tapeARNs:(Option.map ~f:Values.TapeARNs.of_json tapeARNs)
              ?marker ?limit ~gatewayARN ())
           (Some Values.DescribeTapesOutput.to_json)
           (Some Values.DescribeTapesOutput.error_to_json)])
let describe_upload_buffer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_upload_buffer
           (Values.DescribeUploadBufferInput.make ~gatewayARN ())
           (Some Values.DescribeUploadBufferOutput.to_json)
           (Some Values.DescribeUploadBufferOutput.error_to_json)])
let describe_v_t_l_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vTLDeviceARNs =
         flag "v-t-l-device-a-r-ns" (optional json_arg)
           ~doc:"JSON VTLDeviceARNs"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveIntObject"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_v_t_l_devices
           (Values.DescribeVTLDevicesInput.make
              ?vTLDeviceARNs:(Option.map ~f:Values.VTLDeviceARNs.of_json
                                vTLDeviceARNs) ?marker ?limit ~gatewayARN ())
           (Some Values.DescribeVTLDevicesOutput.to_json)
           (Some Values.DescribeVTLDevicesOutput.error_to_json)])
let describe_working_storage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_working_storage
           (Values.DescribeWorkingStorageInput.make ~gatewayARN ())
           (Some Values.DescribeWorkingStorageOutput.to_json)
           (Some Values.DescribeWorkingStorageOutput.error_to_json)])
let detach_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDetach =
         flag "force-detach" (optional bool) ~doc:"BOOL Boolean"
       and volumeARN =
         flag "volume-a-r-n" (required string) ~doc:"STRING VolumeARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_volume
           (Values.DetachVolumeInput.make ?forceDetach ~volumeARN ())
           (Some Values.DetachVolumeOutput.to_json)
           (Some Values.DetachVolumeOutput.error_to_json)])
let disable_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_gateway
           (Values.DisableGatewayInput.make ~gatewayARN ())
           (Some Values.DisableGatewayOutput.to_json)
           (Some Values.DisableGatewayOutput.error_to_json)])
let disassociate_file_system =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL Boolean__lc1"
       and fileSystemAssociationARN =
         flag "file-system-association-a-r-n" (required string)
           ~doc:"STRING FileSystemAssociationARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_file_system
           (Values.DisassociateFileSystemInput.make ?forceDelete
              ~fileSystemAssociationARN ())
           (Some Values.DisassociateFileSystemOutput.to_json)
           (Some Values.DisassociateFileSystemOutput.error_to_json)])
let join_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationalUnit =
         flag "organizational-unit" (optional string)
           ~doc:"STRING OrganizationalUnit"
       and domainControllers =
         flag "domain-controllers" (optional json_arg) ~doc:"JSON Hosts"
       and timeoutInSeconds =
         flag "timeout-in-seconds" (optional int) ~doc:"INT TimeoutInSeconds"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and userName =
         flag "user-name" (required string) ~doc:"STRING DomainUserName"
       and password =
         flag "password" (required string) ~doc:"STRING DomainUserPassword" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.join_domain
           (Values.JoinDomainInput.make ?organizationalUnit
              ?domainControllers:(Option.map ~f:Values.Hosts.of_json
                                    domainControllers) ?timeoutInSeconds
              ~gatewayARN ~domainName ~userName ~password ())
           (Some Values.JoinDomainOutput.to_json)
           (Some Values.JoinDomainOutput.error_to_json)])
let list_automatic_tape_creation_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (optional string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_automatic_tape_creation_policies
           (Values.ListAutomaticTapeCreationPoliciesInput.make ?gatewayARN ())
           (Some Values.ListAutomaticTapeCreationPoliciesOutput.to_json)
           (Some Values.ListAutomaticTapeCreationPoliciesOutput.error_to_json)])
let list_file_shares =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (optional string) ~doc:"STRING GatewayARN"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveIntObject"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_file_shares
           (Values.ListFileSharesInput.make ?gatewayARN ?limit ?marker ())
           (Some Values.ListFileSharesOutput.to_json)
           (Some Values.ListFileSharesOutput.error_to_json)])
let list_file_system_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (optional string) ~doc:"STRING GatewayARN"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveIntObject"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_file_system_associations
           (Values.ListFileSystemAssociationsInput.make ?gatewayARN ?limit
              ?marker ())
           (Some Values.ListFileSystemAssociationsOutput.to_json)
           (Some Values.ListFileSystemAssociationsOutput.error_to_json)])
let list_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveIntObject" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_gateways (Values.ListGatewaysInput.make ?marker ?limit ())
           (Some Values.ListGatewaysOutput.to_json)
           (Some Values.ListGatewaysOutput.error_to_json)])
let list_local_disks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_local_disks
           (Values.ListLocalDisksInput.make ~gatewayARN ())
           (Some Values.ListLocalDisksOutput.to_json)
           (Some Values.ListLocalDisksOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveIntObject"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ?marker ?limit ~resourceARN
              ()) (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_tape_pools =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and poolARNs =
         flag "pool-a-r-ns" (optional json_arg) ~doc:"JSON PoolARNs"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveIntObject" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tape_pools
           (Values.ListTapePoolsInput.make
              ?poolARNs:(Option.map ~f:Values.PoolARNs.of_json poolARNs)
              ?marker ?limit ()) (Some Values.ListTapePoolsOutput.to_json)
           (Some Values.ListTapePoolsOutput.error_to_json)])
let list_tapes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tapeARNs =
         flag "tape-a-r-ns" (optional json_arg) ~doc:"JSON TapeARNs"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveIntObject" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tapes
           (Values.ListTapesInput.make
              ?tapeARNs:(Option.map ~f:Values.TapeARNs.of_json tapeARNs)
              ?marker ?limit ()) (Some Values.ListTapesOutput.to_json)
           (Some Values.ListTapesOutput.error_to_json)])
let list_volume_initiators =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and volumeARN =
         flag "volume-a-r-n" (required string) ~doc:"STRING VolumeARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_volume_initiators
           (Values.ListVolumeInitiatorsInput.make ~volumeARN ())
           (Some Values.ListVolumeInitiatorsOutput.to_json)
           (Some Values.ListVolumeInitiatorsOutput.error_to_json)])
let list_volume_recovery_points =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_volume_recovery_points
           (Values.ListVolumeRecoveryPointsInput.make ~gatewayARN ())
           (Some Values.ListVolumeRecoveryPointsOutput.to_json)
           (Some Values.ListVolumeRecoveryPointsOutput.error_to_json)])
let list_volumes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (optional string) ~doc:"STRING GatewayARN"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveIntObject" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_volumes
           (Values.ListVolumesInput.make ?gatewayARN ?marker ?limit ())
           (Some Values.ListVolumesOutput.to_json)
           (Some Values.ListVolumesOutput.error_to_json)])
let notify_when_uploaded =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileShareARN =
         flag "file-share-a-r-n" (required string) ~doc:"STRING FileShareARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.notify_when_uploaded
           (Values.NotifyWhenUploadedInput.make ~fileShareARN ())
           (Some Values.NotifyWhenUploadedOutput.to_json)
           (Some Values.NotifyWhenUploadedOutput.error_to_json)])
let refresh_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and folderList =
         flag "folder-list" (optional json_arg) ~doc:"JSON FolderList"
       and recursive = flag "recursive" (optional bool) ~doc:"BOOL Boolean"
       and fileShareARN =
         flag "file-share-a-r-n" (required string) ~doc:"STRING FileShareARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.refresh_cache
           (Values.RefreshCacheInput.make
              ?folderList:(Option.map ~f:Values.FolderList.of_json folderList)
              ?recursive ~fileShareARN ())
           (Some Values.RefreshCacheOutput.to_json)
           (Some Values.RefreshCacheOutput.error_to_json)])
let remove_tags_from_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceARN"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_tags_from_resource
           (Values.RemoveTagsFromResourceInput.make ~resourceARN
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.RemoveTagsFromResourceOutput.to_json)
           (Some Values.RemoveTagsFromResourceOutput.error_to_json)])
let reset_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_cache (Values.ResetCacheInput.make ~gatewayARN ())
           (Some Values.ResetCacheOutput.to_json)
           (Some Values.ResetCacheOutput.error_to_json)])
let retrieve_tape_archive =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tapeARN =
         flag "tape-a-r-n" (required string) ~doc:"STRING TapeARN"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.retrieve_tape_archive
           (Values.RetrieveTapeArchiveInput.make ~tapeARN ~gatewayARN ())
           (Some Values.RetrieveTapeArchiveOutput.to_json)
           (Some Values.RetrieveTapeArchiveOutput.error_to_json)])
let retrieve_tape_recovery_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tapeARN =
         flag "tape-a-r-n" (required string) ~doc:"STRING TapeARN"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.retrieve_tape_recovery_point
           (Values.RetrieveTapeRecoveryPointInput.make ~tapeARN ~gatewayARN
              ()) (Some Values.RetrieveTapeRecoveryPointOutput.to_json)
           (Some Values.RetrieveTapeRecoveryPointOutput.error_to_json)])
let set_local_console_password =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and localConsolePassword =
         flag "local-console-password" (required string)
           ~doc:"STRING LocalConsolePassword" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_local_console_password
           (Values.SetLocalConsolePasswordInput.make ~gatewayARN
              ~localConsolePassword ())
           (Some Values.SetLocalConsolePasswordOutput.to_json)
           (Some Values.SetLocalConsolePasswordOutput.error_to_json)])
let set_s_m_b_guest_password =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and password =
         flag "password" (required string) ~doc:"STRING SMBGuestPassword" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_s_m_b_guest_password
           (Values.SetSMBGuestPasswordInput.make ~gatewayARN ~password ())
           (Some Values.SetSMBGuestPasswordOutput.to_json)
           (Some Values.SetSMBGuestPasswordOutput.error_to_json)])
let shutdown_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.shutdown_gateway
           (Values.ShutdownGatewayInput.make ~gatewayARN ())
           (Some Values.ShutdownGatewayOutput.to_json)
           (Some Values.ShutdownGatewayOutput.error_to_json)])
let start_availability_monitor_test =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_availability_monitor_test
           (Values.StartAvailabilityMonitorTestInput.make ~gatewayARN ())
           (Some Values.StartAvailabilityMonitorTestOutput.to_json)
           (Some Values.StartAvailabilityMonitorTestOutput.error_to_json)])
let start_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_gateway (Values.StartGatewayInput.make ~gatewayARN ())
           (Some Values.StartGatewayOutput.to_json)
           (Some Values.StartGatewayOutput.error_to_json)])
let update_automatic_tape_creation_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and automaticTapeCreationRules =
         flag "automatic-tape-creation-rules" (required json_arg)
           ~doc:"JSON AutomaticTapeCreationRules"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_automatic_tape_creation_policy
           (Values.UpdateAutomaticTapeCreationPolicyInput.make
              ~automaticTapeCreationRules:(Values.AutomaticTapeCreationRules.of_json
                                             automaticTapeCreationRules)
              ~gatewayARN ())
           (Some Values.UpdateAutomaticTapeCreationPolicyOutput.to_json)
           (Some Values.UpdateAutomaticTapeCreationPolicyOutput.error_to_json)])
let update_bandwidth_rate_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and averageUploadRateLimitInBitsPerSec =
         flag "average-upload-rate-limit-in-bits-per-sec" (optional json_arg)
           ~doc:"JSON BandwidthUploadRateLimit"
       and averageDownloadRateLimitInBitsPerSec =
         flag "average-download-rate-limit-in-bits-per-sec"
           (optional json_arg) ~doc:"JSON BandwidthDownloadRateLimit"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bandwidth_rate_limit
           (Values.UpdateBandwidthRateLimitInput.make
              ?averageUploadRateLimitInBitsPerSec:(Option.map
                                                     ~f:Values.BandwidthUploadRateLimit.of_json
                                                     averageUploadRateLimitInBitsPerSec)
              ?averageDownloadRateLimitInBitsPerSec:(Option.map
                                                       ~f:Values.BandwidthDownloadRateLimit.of_json
                                                       averageDownloadRateLimitInBitsPerSec)
              ~gatewayARN ())
           (Some Values.UpdateBandwidthRateLimitOutput.to_json)
           (Some Values.UpdateBandwidthRateLimitOutput.error_to_json)])
let update_bandwidth_rate_limit_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and bandwidthRateLimitIntervals =
         flag "bandwidth-rate-limit-intervals" (required json_arg)
           ~doc:"JSON BandwidthRateLimitIntervals" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bandwidth_rate_limit_schedule
           (Values.UpdateBandwidthRateLimitScheduleInput.make ~gatewayARN
              ~bandwidthRateLimitIntervals:(Values.BandwidthRateLimitIntervals.of_json
                                              bandwidthRateLimitIntervals) ())
           (Some Values.UpdateBandwidthRateLimitScheduleOutput.to_json)
           (Some Values.UpdateBandwidthRateLimitScheduleOutput.error_to_json)])
let update_chap_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and secretToAuthenticateTarget =
         flag "secret-to-authenticate-target" (optional string)
           ~doc:"STRING ChapSecret"
       and targetARN =
         flag "target-a-r-n" (required string) ~doc:"STRING TargetARN"
       and secretToAuthenticateInitiator =
         flag "secret-to-authenticate-initiator" (required string)
           ~doc:"STRING ChapSecret"
       and initiatorName =
         flag "initiator-name" (required string) ~doc:"STRING IqnName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_chap_credentials
           (Values.UpdateChapCredentialsInput.make
              ?secretToAuthenticateTarget ~targetARN
              ~secretToAuthenticateInitiator ~initiatorName ())
           (Some Values.UpdateChapCredentialsOutput.to_json)
           (Some Values.UpdateChapCredentialsOutput.error_to_json)])
let update_file_system_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string) ~doc:"STRING DomainUserName"
       and password =
         flag "password" (optional string) ~doc:"STRING DomainUserPassword"
       and auditDestinationARN =
         flag "audit-destination-a-r-n" (optional string)
           ~doc:"STRING AuditDestinationARN"
       and cacheAttributes =
         flag "cache-attributes" (optional json_arg)
           ~doc:"JSON CacheAttributes"
       and fileSystemAssociationARN =
         flag "file-system-association-a-r-n" (required string)
           ~doc:"STRING FileSystemAssociationARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_file_system_association
           (Values.UpdateFileSystemAssociationInput.make ?userName ?password
              ?auditDestinationARN
              ?cacheAttributes:(Option.map ~f:Values.CacheAttributes.of_json
                                  cacheAttributes) ~fileSystemAssociationARN
              ()) (Some Values.UpdateFileSystemAssociationOutput.to_json)
           (Some Values.UpdateFileSystemAssociationOutput.error_to_json)])
let update_gateway_information =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayName =
         flag "gateway-name" (optional string) ~doc:"STRING GatewayName"
       and gatewayTimezone =
         flag "gateway-timezone" (optional string)
           ~doc:"STRING GatewayTimezone"
       and cloudWatchLogGroupARN =
         flag "cloud-watch-log-group-a-r-n" (optional string)
           ~doc:"STRING CloudWatchLogGroupARN"
       and gatewayCapacity =
         flag "gateway-capacity" (optional json_arg)
           ~doc:"JSON GatewayCapacity"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_gateway_information
           (Values.UpdateGatewayInformationInput.make ?gatewayName
              ?gatewayTimezone ?cloudWatchLogGroupARN
              ?gatewayCapacity:(Option.map ~f:Values.GatewayCapacity.of_json
                                  gatewayCapacity) ~gatewayARN ())
           (Some Values.UpdateGatewayInformationOutput.to_json)
           (Some Values.UpdateGatewayInformationOutput.error_to_json)])
let update_gateway_software_now =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_gateway_software_now
           (Values.UpdateGatewaySoftwareNowInput.make ~gatewayARN ())
           (Some Values.UpdateGatewaySoftwareNowOutput.to_json)
           (Some Values.UpdateGatewaySoftwareNowOutput.error_to_json)])
let update_maintenance_start_time =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dayOfWeek = flag "day-of-week" (optional int) ~doc:"INT DayOfWeek"
       and dayOfMonth =
         flag "day-of-month" (optional int) ~doc:"INT DayOfMonth"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and hourOfDay = flag "hour-of-day" (required int) ~doc:"INT HourOfDay"
       and minuteOfHour =
         flag "minute-of-hour" (required int) ~doc:"INT MinuteOfHour" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_maintenance_start_time
           (Values.UpdateMaintenanceStartTimeInput.make ?dayOfWeek
              ?dayOfMonth ~gatewayARN ~hourOfDay ~minuteOfHour ())
           (Some Values.UpdateMaintenanceStartTimeOutput.to_json)
           (Some Values.UpdateMaintenanceStartTimeOutput.error_to_json)])
let update_n_f_s_file_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kMSEncrypted =
         flag "k-m-s-encrypted" (optional bool) ~doc:"BOOL Boolean"
       and kMSKey = flag "k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and nFSFileShareDefaults =
         flag "n-f-s-file-share-defaults" (optional json_arg)
           ~doc:"JSON NFSFileShareDefaults"
       and defaultStorageClass =
         flag "default-storage-class" (optional string)
           ~doc:"STRING StorageClass"
       and objectACL =
         flag "object-a-c-l" (optional json_arg) ~doc:"JSON ObjectACL"
       and clientList =
         flag "client-list" (optional json_arg)
           ~doc:"JSON FileShareClientList"
       and squash = flag "squash" (optional string) ~doc:"STRING Squash"
       and readOnly = flag "read-only" (optional bool) ~doc:"BOOL Boolean"
       and guessMIMETypeEnabled =
         flag "guess-m-i-m-e-type-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and requesterPays =
         flag "requester-pays" (optional bool) ~doc:"BOOL Boolean"
       and fileShareName =
         flag "file-share-name" (optional string) ~doc:"STRING FileShareName"
       and cacheAttributes =
         flag "cache-attributes" (optional json_arg)
           ~doc:"JSON CacheAttributes"
       and notificationPolicy =
         flag "notification-policy" (optional string)
           ~doc:"STRING NotificationPolicy"
       and auditDestinationARN =
         flag "audit-destination-a-r-n" (optional string)
           ~doc:"STRING AuditDestinationARN"
       and fileShareARN =
         flag "file-share-a-r-n" (required string) ~doc:"STRING FileShareARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_n_f_s_file_share
           (Values.UpdateNFSFileShareInput.make ?kMSEncrypted ?kMSKey
              ?nFSFileShareDefaults:(Option.map
                                       ~f:Values.NFSFileShareDefaults.of_json
                                       nFSFileShareDefaults)
              ?defaultStorageClass
              ?objectACL:(Option.map ~f:Values.ObjectACL.of_json objectACL)
              ?clientList:(Option.map ~f:Values.FileShareClientList.of_json
                             clientList) ?squash ?readOnly
              ?guessMIMETypeEnabled ?requesterPays ?fileShareName
              ?cacheAttributes:(Option.map ~f:Values.CacheAttributes.of_json
                                  cacheAttributes) ?notificationPolicy
              ?auditDestinationARN ~fileShareARN ())
           (Some Values.UpdateNFSFileShareOutput.to_json)
           (Some Values.UpdateNFSFileShareOutput.error_to_json)])
let update_s_m_b_file_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kMSEncrypted =
         flag "k-m-s-encrypted" (optional bool) ~doc:"BOOL Boolean"
       and kMSKey = flag "k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and defaultStorageClass =
         flag "default-storage-class" (optional string)
           ~doc:"STRING StorageClass"
       and objectACL =
         flag "object-a-c-l" (optional json_arg) ~doc:"JSON ObjectACL"
       and readOnly = flag "read-only" (optional bool) ~doc:"BOOL Boolean"
       and guessMIMETypeEnabled =
         flag "guess-m-i-m-e-type-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and requesterPays =
         flag "requester-pays" (optional bool) ~doc:"BOOL Boolean"
       and sMBACLEnabled =
         flag "s-m-b-a-c-l-enabled" (optional bool) ~doc:"BOOL Boolean"
       and accessBasedEnumeration =
         flag "access-based-enumeration" (optional bool) ~doc:"BOOL Boolean"
       and adminUserList =
         flag "admin-user-list" (optional json_arg) ~doc:"JSON UserList"
       and validUserList =
         flag "valid-user-list" (optional json_arg) ~doc:"JSON UserList"
       and invalidUserList =
         flag "invalid-user-list" (optional json_arg) ~doc:"JSON UserList"
       and auditDestinationARN =
         flag "audit-destination-a-r-n" (optional string)
           ~doc:"STRING AuditDestinationARN"
       and caseSensitivity =
         flag "case-sensitivity" (optional json_arg)
           ~doc:"JSON CaseSensitivity"
       and fileShareName =
         flag "file-share-name" (optional string) ~doc:"STRING FileShareName"
       and cacheAttributes =
         flag "cache-attributes" (optional json_arg)
           ~doc:"JSON CacheAttributes"
       and notificationPolicy =
         flag "notification-policy" (optional string)
           ~doc:"STRING NotificationPolicy"
       and oplocksEnabled =
         flag "oplocks-enabled" (optional bool) ~doc:"BOOL Boolean"
       and fileShareARN =
         flag "file-share-a-r-n" (required string) ~doc:"STRING FileShareARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_s_m_b_file_share
           (Values.UpdateSMBFileShareInput.make ?kMSEncrypted ?kMSKey
              ?defaultStorageClass
              ?objectACL:(Option.map ~f:Values.ObjectACL.of_json objectACL)
              ?readOnly ?guessMIMETypeEnabled ?requesterPays ?sMBACLEnabled
              ?accessBasedEnumeration
              ?adminUserList:(Option.map ~f:Values.UserList.of_json
                                adminUserList)
              ?validUserList:(Option.map ~f:Values.UserList.of_json
                                validUserList)
              ?invalidUserList:(Option.map ~f:Values.UserList.of_json
                                  invalidUserList) ?auditDestinationARN
              ?caseSensitivity:(Option.map ~f:Values.CaseSensitivity.of_json
                                  caseSensitivity) ?fileShareName
              ?cacheAttributes:(Option.map ~f:Values.CacheAttributes.of_json
                                  cacheAttributes) ?notificationPolicy
              ?oplocksEnabled ~fileShareARN ())
           (Some Values.UpdateSMBFileShareOutput.to_json)
           (Some Values.UpdateSMBFileShareOutput.error_to_json)])
let update_s_m_b_file_share_visibility =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and fileSharesVisible =
         flag "file-shares-visible" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_s_m_b_file_share_visibility
           (Values.UpdateSMBFileShareVisibilityInput.make ~gatewayARN
              ~fileSharesVisible ())
           (Some Values.UpdateSMBFileShareVisibilityOutput.to_json)
           (Some Values.UpdateSMBFileShareVisibilityOutput.error_to_json)])
let update_s_m_b_local_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and sMBLocalGroups =
         flag "s-m-b-local-groups" (required json_arg)
           ~doc:"JSON SMBLocalGroups" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_s_m_b_local_groups
           (Values.UpdateSMBLocalGroupsInput.make ~gatewayARN
              ~sMBLocalGroups:(Values.SMBLocalGroups.of_json sMBLocalGroups)
              ()) (Some Values.UpdateSMBLocalGroupsOutput.to_json)
           (Some Values.UpdateSMBLocalGroupsOutput.error_to_json)])
let update_s_m_b_security_strategy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayARN =
         flag "gateway-a-r-n" (required string) ~doc:"STRING GatewayARN"
       and sMBSecurityStrategy =
         flag "s-m-b-security-strategy" (required json_arg)
           ~doc:"JSON SMBSecurityStrategy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_s_m_b_security_strategy
           (Values.UpdateSMBSecurityStrategyInput.make ~gatewayARN
              ~sMBSecurityStrategy:(Values.SMBSecurityStrategy.of_json
                                      sMBSecurityStrategy) ())
           (Some Values.UpdateSMBSecurityStrategyOutput.to_json)
           (Some Values.UpdateSMBSecurityStrategyOutput.error_to_json)])
let update_snapshot_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and volumeARN =
         flag "volume-a-r-n" (required string) ~doc:"STRING VolumeARN"
       and startAt = flag "start-at" (required int) ~doc:"INT HourOfDay"
       and recurrenceInHours =
         flag "recurrence-in-hours" (required int)
           ~doc:"INT RecurrenceInHours" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_snapshot_schedule
           (Values.UpdateSnapshotScheduleInput.make ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~volumeARN
              ~startAt ~recurrenceInHours ())
           (Some Values.UpdateSnapshotScheduleOutput.to_json)
           (Some Values.UpdateSnapshotScheduleOutput.error_to_json)])
let update_v_t_l_device_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vTLDeviceARN =
         flag "v-t-l-device-a-r-n" (required string)
           ~doc:"STRING VTLDeviceARN"
       and deviceType =
         flag "device-type" (required string) ~doc:"STRING DeviceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_v_t_l_device_type
           (Values.UpdateVTLDeviceTypeInput.make ~vTLDeviceARN ~deviceType ())
           (Some Values.UpdateVTLDeviceTypeOutput.to_json)
           (Some Values.UpdateVTLDeviceTypeOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("activate-gateway", activate_gateway);
    ("add-cache", add_cache);
    ("add-tags-to-resource", add_tags_to_resource);
    ("add-upload-buffer", add_upload_buffer);
    ("add-working-storage", add_working_storage);
    ("assign-tape-pool", assign_tape_pool);
    ("associate-file-system", associate_file_system);
    ("attach-volume", attach_volume);
    ("cancel-archival", cancel_archival);
    ("cancel-retrieval", cancel_retrieval);
    ("create-cachedi-s-c-s-i-volume", create_cachedi_s_c_s_i_volume);
    ("create-n-f-s-file-share", create_n_f_s_file_share);
    ("create-s-m-b-file-share", create_s_m_b_file_share);
    ("create-snapshot", create_snapshot);
    ("create-snapshot-from-volume-recovery-point",
      create_snapshot_from_volume_recovery_point);
    ("create-storedi-s-c-s-i-volume", create_storedi_s_c_s_i_volume);
    ("create-tape-pool", create_tape_pool);
    ("create-tape-with-barcode", create_tape_with_barcode);
    ("create-tapes", create_tapes);
    ("delete-automatic-tape-creation-policy",
      delete_automatic_tape_creation_policy);
    ("delete-bandwidth-rate-limit", delete_bandwidth_rate_limit);
    ("delete-chap-credentials", delete_chap_credentials);
    ("delete-file-share", delete_file_share);
    ("delete-gateway", delete_gateway);
    ("delete-snapshot-schedule", delete_snapshot_schedule);
    ("delete-tape", delete_tape);
    ("delete-tape-archive", delete_tape_archive);
    ("delete-tape-pool", delete_tape_pool);
    ("delete-volume", delete_volume);
    ("describe-availability-monitor-test",
      describe_availability_monitor_test);
    ("describe-bandwidth-rate-limit", describe_bandwidth_rate_limit);
    ("describe-bandwidth-rate-limit-schedule",
      describe_bandwidth_rate_limit_schedule);
    ("describe-cache", describe_cache);
    ("describe-cachedi-s-c-s-i-volumes", describe_cachedi_s_c_s_i_volumes);
    ("describe-chap-credentials", describe_chap_credentials);
    ("describe-file-system-associations", describe_file_system_associations);
    ("describe-gateway-information", describe_gateway_information);
    ("describe-maintenance-start-time", describe_maintenance_start_time);
    ("describe-n-f-s-file-shares", describe_n_f_s_file_shares);
    ("describe-s-m-b-file-shares", describe_s_m_b_file_shares);
    ("describe-s-m-b-settings", describe_s_m_b_settings);
    ("describe-snapshot-schedule", describe_snapshot_schedule);
    ("describe-storedi-s-c-s-i-volumes", describe_storedi_s_c_s_i_volumes);
    ("describe-tape-archives", describe_tape_archives);
    ("describe-tape-recovery-points", describe_tape_recovery_points);
    ("describe-tapes", describe_tapes);
    ("describe-upload-buffer", describe_upload_buffer);
    ("describe-v-t-l-devices", describe_v_t_l_devices);
    ("describe-working-storage", describe_working_storage);
    ("detach-volume", detach_volume);
    ("disable-gateway", disable_gateway);
    ("disassociate-file-system", disassociate_file_system);
    ("join-domain", join_domain);
    ("list-automatic-tape-creation-policies",
      list_automatic_tape_creation_policies);
    ("list-file-shares", list_file_shares);
    ("list-file-system-associations", list_file_system_associations);
    ("list-gateways", list_gateways);
    ("list-local-disks", list_local_disks);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-tape-pools", list_tape_pools);
    ("list-tapes", list_tapes);
    ("list-volume-initiators", list_volume_initiators);
    ("list-volume-recovery-points", list_volume_recovery_points);
    ("list-volumes", list_volumes);
    ("notify-when-uploaded", notify_when_uploaded);
    ("refresh-cache", refresh_cache);
    ("remove-tags-from-resource", remove_tags_from_resource);
    ("reset-cache", reset_cache);
    ("retrieve-tape-archive", retrieve_tape_archive);
    ("retrieve-tape-recovery-point", retrieve_tape_recovery_point);
    ("set-local-console-password", set_local_console_password);
    ("set-s-m-b-guest-password", set_s_m_b_guest_password);
    ("shutdown-gateway", shutdown_gateway);
    ("start-availability-monitor-test", start_availability_monitor_test);
    ("start-gateway", start_gateway);
    ("update-automatic-tape-creation-policy",
      update_automatic_tape_creation_policy);
    ("update-bandwidth-rate-limit", update_bandwidth_rate_limit);
    ("update-bandwidth-rate-limit-schedule",
      update_bandwidth_rate_limit_schedule);
    ("update-chap-credentials", update_chap_credentials);
    ("update-file-system-association", update_file_system_association);
    ("update-gateway-information", update_gateway_information);
    ("update-gateway-software-now", update_gateway_software_now);
    ("update-maintenance-start-time", update_maintenance_start_time);
    ("update-n-f-s-file-share", update_n_f_s_file_share);
    ("update-s-m-b-file-share", update_s_m_b_file_share);
    ("update-s-m-b-file-share-visibility",
      update_s_m_b_file_share_visibility);
    ("update-s-m-b-local-groups", update_s_m_b_local_groups);
    ("update-s-m-b-security-strategy", update_s_m_b_security_strategy);
    ("update-snapshot-schedule", update_snapshot_schedule);
    ("update-v-t-l-device-type", update_v_t_l_device_type)]
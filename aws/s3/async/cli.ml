(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let abort_multipart_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING MultipartUploadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.abort_multipart_upload
           (Values.AbortMultipartUploadRequest.make
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?expectedBucketOwner ~bucket
              ~key ~uploadId ())
           (Some Values.AbortMultipartUploadOutput.to_json)
           (Some Values.AbortMultipartUploadOutput.error_to_json)])
let complete_multipart_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and multipartUpload =
         flag "multipart-upload" (optional json_arg)
           ~doc:"JSON CompletedMultipartUpload"
       and checksumCRC32 =
         flag "checksum-c-r-c32" (optional string)
           ~doc:"STRING ChecksumCRC32"
       and checksumCRC32C =
         flag "checksum-c-r-c32-c" (optional string)
           ~doc:"STRING ChecksumCRC32C"
       and checksumSHA1 =
         flag "checksum-s-h-a1" (optional string) ~doc:"STRING ChecksumSHA1"
       and checksumSHA256 =
         flag "checksum-s-h-a256" (optional string)
           ~doc:"STRING ChecksumSHA256"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and sSECustomerAlgorithm =
         flag "s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING SSECustomerAlgorithm"
       and sSECustomerKey =
         flag "s-s-e-customer-key" (optional string)
           ~doc:"STRING SSECustomerKey"
       and sSECustomerKeyMD5 =
         flag "s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING SSECustomerKeyMD5"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING MultipartUploadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.complete_multipart_upload
           (Values.CompleteMultipartUploadRequest.make
              ?multipartUpload:(Option.map
                                  ~f:Values.CompletedMultipartUpload.of_json
                                  multipartUpload) ?checksumCRC32
              ?checksumCRC32C ?checksumSHA1 ?checksumSHA256
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?expectedBucketOwner
              ?sSECustomerAlgorithm ?sSECustomerKey ?sSECustomerKeyMD5
              ~bucket ~key ~uploadId ())
           (Some Values.CompleteMultipartUploadOutput.to_json)
           (Some Values.CompleteMultipartUploadOutput.error_to_json)])
let copy_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aCL = flag "a-c-l" (optional json_arg) ~doc:"JSON ObjectCannedACL"
       and cacheControl =
         flag "cache-control" (optional string) ~doc:"STRING CacheControl"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and contentDisposition =
         flag "content-disposition" (optional string)
           ~doc:"STRING ContentDisposition"
       and contentEncoding =
         flag "content-encoding" (optional string)
           ~doc:"STRING ContentEncoding"
       and contentLanguage =
         flag "content-language" (optional string)
           ~doc:"STRING ContentLanguage"
       and contentType =
         flag "content-type" (optional string) ~doc:"STRING ContentType"
       and copySourceIfMatch =
         flag "copy-source-if-match" (optional string)
           ~doc:"STRING CopySourceIfMatch"
       and copySourceIfModifiedSince =
         flag "copy-source-if-modified-since" (optional json_arg)
           ~doc:"JSON CopySourceIfModifiedSince"
       and copySourceIfNoneMatch =
         flag "copy-source-if-none-match" (optional string)
           ~doc:"STRING CopySourceIfNoneMatch"
       and copySourceIfUnmodifiedSince =
         flag "copy-source-if-unmodified-since" (optional json_arg)
           ~doc:"JSON CopySourceIfUnmodifiedSince"
       and expires = flag "expires" (optional json_arg) ~doc:"JSON Expires"
       and grantFullControl =
         flag "grant-full-control" (optional string)
           ~doc:"STRING GrantFullControl"
       and grantRead =
         flag "grant-read" (optional string) ~doc:"STRING GrantRead"
       and grantReadACP =
         flag "grant-read-a-c-p" (optional string) ~doc:"STRING GrantReadACP"
       and grantWriteACP =
         flag "grant-write-a-c-p" (optional string)
           ~doc:"STRING GrantWriteACP"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON Metadata"
       and metadataDirective =
         flag "metadata-directive" (optional json_arg)
           ~doc:"JSON MetadataDirective"
       and taggingDirective =
         flag "tagging-directive" (optional json_arg)
           ~doc:"JSON TaggingDirective"
       and serverSideEncryption =
         flag "server-side-encryption" (optional json_arg)
           ~doc:"JSON ServerSideEncryption"
       and storageClass =
         flag "storage-class" (optional json_arg) ~doc:"JSON StorageClass"
       and websiteRedirectLocation =
         flag "website-redirect-location" (optional string)
           ~doc:"STRING WebsiteRedirectLocation"
       and sSECustomerAlgorithm =
         flag "s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING SSECustomerAlgorithm"
       and sSECustomerKey =
         flag "s-s-e-customer-key" (optional string)
           ~doc:"STRING SSECustomerKey"
       and sSECustomerKeyMD5 =
         flag "s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING SSECustomerKeyMD5"
       and sSEKMSKeyId =
         flag "s-s-e-k-m-s-key-id" (optional string)
           ~doc:"STRING SSEKMSKeyId"
       and sSEKMSEncryptionContext =
         flag "s-s-e-k-m-s-encryption-context" (optional string)
           ~doc:"STRING SSEKMSEncryptionContext"
       and bucketKeyEnabled =
         flag "bucket-key-enabled" (optional bool)
           ~doc:"BOOL BucketKeyEnabled"
       and copySourceSSECustomerAlgorithm =
         flag "copy-source-s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING CopySourceSSECustomerAlgorithm"
       and copySourceSSECustomerKey =
         flag "copy-source-s-s-e-customer-key" (optional string)
           ~doc:"STRING CopySourceSSECustomerKey"
       and copySourceSSECustomerKeyMD5 =
         flag "copy-source-s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING CopySourceSSECustomerKeyMD5"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and tagging =
         flag "tagging" (optional string) ~doc:"STRING TaggingHeader"
       and objectLockMode =
         flag "object-lock-mode" (optional json_arg)
           ~doc:"JSON ObjectLockMode"
       and objectLockRetainUntilDate =
         flag "object-lock-retain-until-date" (optional json_arg)
           ~doc:"JSON ObjectLockRetainUntilDate"
       and objectLockLegalHoldStatus =
         flag "object-lock-legal-hold-status" (optional json_arg)
           ~doc:"JSON ObjectLockLegalHoldStatus"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and expectedSourceBucketOwner =
         flag "expected-source-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and copySource =
         flag "copy-source" (required string) ~doc:"STRING CopySource"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_object
           (Values.CopyObjectRequest.make
              ?aCL:(Option.map ~f:Values.ObjectCannedACL.of_json aCL)
              ?cacheControl
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?contentDisposition
              ?contentEncoding ?contentLanguage ?contentType
              ?copySourceIfMatch
              ?copySourceIfModifiedSince:(Option.map
                                            ~f:Values.CopySourceIfModifiedSince.of_json
                                            copySourceIfModifiedSince)
              ?copySourceIfNoneMatch
              ?copySourceIfUnmodifiedSince:(Option.map
                                              ~f:Values.CopySourceIfUnmodifiedSince.of_json
                                              copySourceIfUnmodifiedSince)
              ?expires:(Option.map ~f:Values.Expires.of_json expires)
              ?grantFullControl ?grantRead ?grantReadACP ?grantWriteACP
              ?metadata:(Option.map ~f:Values.Metadata.of_json metadata)
              ?metadataDirective:(Option.map
                                    ~f:Values.MetadataDirective.of_json
                                    metadataDirective)
              ?taggingDirective:(Option.map
                                   ~f:Values.TaggingDirective.of_json
                                   taggingDirective)
              ?serverSideEncryption:(Option.map
                                       ~f:Values.ServerSideEncryption.of_json
                                       serverSideEncryption)
              ?storageClass:(Option.map ~f:Values.StorageClass.of_json
                               storageClass) ?websiteRedirectLocation
              ?sSECustomerAlgorithm ?sSECustomerKey ?sSECustomerKeyMD5
              ?sSEKMSKeyId ?sSEKMSEncryptionContext ?bucketKeyEnabled
              ?copySourceSSECustomerAlgorithm ?copySourceSSECustomerKey
              ?copySourceSSECustomerKeyMD5
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?tagging
              ?objectLockMode:(Option.map ~f:Values.ObjectLockMode.of_json
                                 objectLockMode)
              ?objectLockRetainUntilDate:(Option.map
                                            ~f:Values.ObjectLockRetainUntilDate.of_json
                                            objectLockRetainUntilDate)
              ?objectLockLegalHoldStatus:(Option.map
                                            ~f:Values.ObjectLockLegalHoldStatus.of_json
                                            objectLockLegalHoldStatus)
              ?expectedBucketOwner ?expectedSourceBucketOwner ~bucket
              ~copySource ~key ()) (Some Values.CopyObjectOutput.to_json)
           (Some Values.CopyObjectOutput.error_to_json)])
let create_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aCL = flag "a-c-l" (optional json_arg) ~doc:"JSON BucketCannedACL"
       and createBucketConfiguration =
         flag "create-bucket-configuration" (optional json_arg)
           ~doc:"JSON CreateBucketConfiguration"
       and grantFullControl =
         flag "grant-full-control" (optional string)
           ~doc:"STRING GrantFullControl"
       and grantRead =
         flag "grant-read" (optional string) ~doc:"STRING GrantRead"
       and grantReadACP =
         flag "grant-read-a-c-p" (optional string) ~doc:"STRING GrantReadACP"
       and grantWrite =
         flag "grant-write" (optional string) ~doc:"STRING GrantWrite"
       and grantWriteACP =
         flag "grant-write-a-c-p" (optional string)
           ~doc:"STRING GrantWriteACP"
       and objectLockEnabledForBucket =
         flag "object-lock-enabled-for-bucket" (optional bool)
           ~doc:"BOOL ObjectLockEnabledForBucket"
       and objectOwnership =
         flag "object-ownership" (optional json_arg)
           ~doc:"JSON ObjectOwnership"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bucket
           (Values.CreateBucketRequest.make
              ?aCL:(Option.map ~f:Values.BucketCannedACL.of_json aCL)
              ?createBucketConfiguration:(Option.map
                                            ~f:Values.CreateBucketConfiguration.of_json
                                            createBucketConfiguration)
              ?grantFullControl ?grantRead ?grantReadACP ?grantWrite
              ?grantWriteACP ?objectLockEnabledForBucket
              ?objectOwnership:(Option.map ~f:Values.ObjectOwnership.of_json
                                  objectOwnership) ~bucket ())
           (Some Values.CreateBucketOutput.to_json)
           (Some Values.CreateBucketOutput.error_to_json)])
let create_multipart_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aCL = flag "a-c-l" (optional json_arg) ~doc:"JSON ObjectCannedACL"
       and cacheControl =
         flag "cache-control" (optional string) ~doc:"STRING CacheControl"
       and contentDisposition =
         flag "content-disposition" (optional string)
           ~doc:"STRING ContentDisposition"
       and contentEncoding =
         flag "content-encoding" (optional string)
           ~doc:"STRING ContentEncoding"
       and contentLanguage =
         flag "content-language" (optional string)
           ~doc:"STRING ContentLanguage"
       and contentType =
         flag "content-type" (optional string) ~doc:"STRING ContentType"
       and expires = flag "expires" (optional json_arg) ~doc:"JSON Expires"
       and grantFullControl =
         flag "grant-full-control" (optional string)
           ~doc:"STRING GrantFullControl"
       and grantRead =
         flag "grant-read" (optional string) ~doc:"STRING GrantRead"
       and grantReadACP =
         flag "grant-read-a-c-p" (optional string) ~doc:"STRING GrantReadACP"
       and grantWriteACP =
         flag "grant-write-a-c-p" (optional string)
           ~doc:"STRING GrantWriteACP"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON Metadata"
       and serverSideEncryption =
         flag "server-side-encryption" (optional json_arg)
           ~doc:"JSON ServerSideEncryption"
       and storageClass =
         flag "storage-class" (optional json_arg) ~doc:"JSON StorageClass"
       and websiteRedirectLocation =
         flag "website-redirect-location" (optional string)
           ~doc:"STRING WebsiteRedirectLocation"
       and sSECustomerAlgorithm =
         flag "s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING SSECustomerAlgorithm"
       and sSECustomerKey =
         flag "s-s-e-customer-key" (optional string)
           ~doc:"STRING SSECustomerKey"
       and sSECustomerKeyMD5 =
         flag "s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING SSECustomerKeyMD5"
       and sSEKMSKeyId =
         flag "s-s-e-k-m-s-key-id" (optional string)
           ~doc:"STRING SSEKMSKeyId"
       and sSEKMSEncryptionContext =
         flag "s-s-e-k-m-s-encryption-context" (optional string)
           ~doc:"STRING SSEKMSEncryptionContext"
       and bucketKeyEnabled =
         flag "bucket-key-enabled" (optional bool)
           ~doc:"BOOL BucketKeyEnabled"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and tagging =
         flag "tagging" (optional string) ~doc:"STRING TaggingHeader"
       and objectLockMode =
         flag "object-lock-mode" (optional json_arg)
           ~doc:"JSON ObjectLockMode"
       and objectLockRetainUntilDate =
         flag "object-lock-retain-until-date" (optional json_arg)
           ~doc:"JSON ObjectLockRetainUntilDate"
       and objectLockLegalHoldStatus =
         flag "object-lock-legal-hold-status" (optional json_arg)
           ~doc:"JSON ObjectLockLegalHoldStatus"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_multipart_upload
           (Values.CreateMultipartUploadRequest.make
              ?aCL:(Option.map ~f:Values.ObjectCannedACL.of_json aCL)
              ?cacheControl ?contentDisposition ?contentEncoding
              ?contentLanguage ?contentType
              ?expires:(Option.map ~f:Values.Expires.of_json expires)
              ?grantFullControl ?grantRead ?grantReadACP ?grantWriteACP
              ?metadata:(Option.map ~f:Values.Metadata.of_json metadata)
              ?serverSideEncryption:(Option.map
                                       ~f:Values.ServerSideEncryption.of_json
                                       serverSideEncryption)
              ?storageClass:(Option.map ~f:Values.StorageClass.of_json
                               storageClass) ?websiteRedirectLocation
              ?sSECustomerAlgorithm ?sSECustomerKey ?sSECustomerKeyMD5
              ?sSEKMSKeyId ?sSEKMSEncryptionContext ?bucketKeyEnabled
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?tagging
              ?objectLockMode:(Option.map ~f:Values.ObjectLockMode.of_json
                                 objectLockMode)
              ?objectLockRetainUntilDate:(Option.map
                                            ~f:Values.ObjectLockRetainUntilDate.of_json
                                            objectLockRetainUntilDate)
              ?objectLockLegalHoldStatus:(Option.map
                                            ~f:Values.ObjectLockLegalHoldStatus.of_json
                                            objectLockLegalHoldStatus)
              ?expectedBucketOwner
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ~bucket ~key ())
           (Some Values.CreateMultipartUploadOutput.to_json)
           (Some Values.CreateMultipartUploadOutput.error_to_json)])
let delete_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket
           (Values.DeleteBucketRequest.make ?expectedBucketOwner ~bucket ())
           None None])
let delete_bucket_analytics_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and id = flag "id" (required string) ~doc:"STRING AnalyticsId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_analytics_configuration
           (Values.DeleteBucketAnalyticsConfigurationRequest.make
              ?expectedBucketOwner ~bucket ~id ()) None None])
let delete_bucket_cors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_cors
           (Values.DeleteBucketCorsRequest.make ?expectedBucketOwner ~bucket
              ()) None None])
let delete_bucket_encryption =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_encryption
           (Values.DeleteBucketEncryptionRequest.make ?expectedBucketOwner
              ~bucket ()) None None])
let delete_bucket_intelligent_tiering_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and id =
         flag "id" (required string) ~doc:"STRING IntelligentTieringId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_intelligent_tiering_configuration
           (Values.DeleteBucketIntelligentTieringConfigurationRequest.make
              ~bucket ~id ()) None None])
let delete_bucket_inventory_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and id = flag "id" (required string) ~doc:"STRING InventoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_inventory_configuration
           (Values.DeleteBucketInventoryConfigurationRequest.make
              ?expectedBucketOwner ~bucket ~id ()) None None])
let delete_bucket_lifecycle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_lifecycle
           (Values.DeleteBucketLifecycleRequest.make ?expectedBucketOwner
              ~bucket ()) None None])
let delete_bucket_metrics_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and id = flag "id" (required string) ~doc:"STRING MetricsId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_metrics_configuration
           (Values.DeleteBucketMetricsConfigurationRequest.make
              ?expectedBucketOwner ~bucket ~id ()) None None])
let delete_bucket_ownership_controls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_ownership_controls
           (Values.DeleteBucketOwnershipControlsRequest.make
              ?expectedBucketOwner ~bucket ()) None None])
let delete_bucket_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_policy
           (Values.DeleteBucketPolicyRequest.make ?expectedBucketOwner
              ~bucket ()) None None])
let delete_bucket_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_replication
           (Values.DeleteBucketReplicationRequest.make ?expectedBucketOwner
              ~bucket ()) None None])
let delete_bucket_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_tagging
           (Values.DeleteBucketTaggingRequest.make ?expectedBucketOwner
              ~bucket ()) None None])
let delete_bucket_website =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_website
           (Values.DeleteBucketWebsiteRequest.make ?expectedBucketOwner
              ~bucket ()) None None])
let delete_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mFA = flag "m-f-a" (optional string) ~doc:"STRING MFA"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and bypassGovernanceRetention =
         flag "bypass-governance-retention" (optional bool)
           ~doc:"BOOL BypassGovernanceRetention"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_object
           (Values.DeleteObjectRequest.make ?mFA ?versionId
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?bypassGovernanceRetention
              ?expectedBucketOwner ~bucket ~key ())
           (Some Values.DeleteObjectOutput.to_json)
           (Some Values.DeleteObjectOutput.error_to_json)])
let delete_object_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_object_tagging
           (Values.DeleteObjectTaggingRequest.make ?versionId
              ?expectedBucketOwner ~bucket ~key ())
           (Some Values.DeleteObjectTaggingOutput.to_json)
           (Some Values.DeleteObjectTaggingOutput.error_to_json)])
let delete_objects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mFA = flag "m-f-a" (optional string) ~doc:"STRING MFA"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and bypassGovernanceRetention =
         flag "bypass-governance-retention" (optional bool)
           ~doc:"BOOL BypassGovernanceRetention"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and delete = flag "delete" (required json_arg) ~doc:"JSON Delete" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_objects
           (Values.DeleteObjectsRequest.make ?mFA
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?bypassGovernanceRetention
              ?expectedBucketOwner
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ~bucket
              ~delete:(Values.Delete.of_json delete) ())
           (Some Values.DeleteObjectsOutput.to_json)
           (Some Values.DeleteObjectsOutput.error_to_json)])
let delete_public_access_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_public_access_block
           (Values.DeletePublicAccessBlockRequest.make ?expectedBucketOwner
              ~bucket ()) None None])
let get_bucket_accelerate_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_accelerate_configuration
           (Values.GetBucketAccelerateConfigurationRequest.make
              ?expectedBucketOwner ~bucket ())
           (Some Values.GetBucketAccelerateConfigurationOutput.to_json)
           (Some Values.GetBucketAccelerateConfigurationOutput.error_to_json)])
let get_bucket_acl =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_acl
           (Values.GetBucketAclRequest.make ?expectedBucketOwner ~bucket ())
           (Some Values.GetBucketAclOutput.to_json)
           (Some Values.GetBucketAclOutput.error_to_json)])
let get_bucket_analytics_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and id = flag "id" (required string) ~doc:"STRING AnalyticsId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_analytics_configuration
           (Values.GetBucketAnalyticsConfigurationRequest.make
              ?expectedBucketOwner ~bucket ~id ())
           (Some Values.GetBucketAnalyticsConfigurationOutput.to_json)
           (Some Values.GetBucketAnalyticsConfigurationOutput.error_to_json)])
let get_bucket_cors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_cors
           (Values.GetBucketCorsRequest.make ?expectedBucketOwner ~bucket ())
           (Some Values.GetBucketCorsOutput.to_json)
           (Some Values.GetBucketCorsOutput.error_to_json)])
let get_bucket_encryption =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_encryption
           (Values.GetBucketEncryptionRequest.make ?expectedBucketOwner
              ~bucket ()) (Some Values.GetBucketEncryptionOutput.to_json)
           (Some Values.GetBucketEncryptionOutput.error_to_json)])
let get_bucket_intelligent_tiering_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and id =
         flag "id" (required string) ~doc:"STRING IntelligentTieringId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_intelligent_tiering_configuration
           (Values.GetBucketIntelligentTieringConfigurationRequest.make
              ~bucket ~id ())
           (Some
              Values.GetBucketIntelligentTieringConfigurationOutput.to_json)
           (Some
              Values.GetBucketIntelligentTieringConfigurationOutput.error_to_json)])
let get_bucket_inventory_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and id = flag "id" (required string) ~doc:"STRING InventoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_inventory_configuration
           (Values.GetBucketInventoryConfigurationRequest.make
              ?expectedBucketOwner ~bucket ~id ())
           (Some Values.GetBucketInventoryConfigurationOutput.to_json)
           (Some Values.GetBucketInventoryConfigurationOutput.error_to_json)])
let get_bucket_lifecycle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_lifecycle
           (Values.GetBucketLifecycleRequest.make ?expectedBucketOwner
              ~bucket ()) (Some Values.GetBucketLifecycleOutput.to_json)
           (Some Values.GetBucketLifecycleOutput.error_to_json)])
let get_bucket_lifecycle_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_lifecycle_configuration
           (Values.GetBucketLifecycleConfigurationRequest.make
              ?expectedBucketOwner ~bucket ())
           (Some Values.GetBucketLifecycleConfigurationOutput.to_json)
           (Some Values.GetBucketLifecycleConfigurationOutput.error_to_json)])
let get_bucket_location =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_location
           (Values.GetBucketLocationRequest.make ?expectedBucketOwner ~bucket
              ()) (Some Values.GetBucketLocationOutput.to_json)
           (Some Values.GetBucketLocationOutput.error_to_json)])
let get_bucket_logging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_logging
           (Values.GetBucketLoggingRequest.make ?expectedBucketOwner ~bucket
              ()) (Some Values.GetBucketLoggingOutput.to_json)
           (Some Values.GetBucketLoggingOutput.error_to_json)])
let get_bucket_metrics_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and id = flag "id" (required string) ~doc:"STRING MetricsId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_metrics_configuration
           (Values.GetBucketMetricsConfigurationRequest.make
              ?expectedBucketOwner ~bucket ~id ())
           (Some Values.GetBucketMetricsConfigurationOutput.to_json)
           (Some Values.GetBucketMetricsConfigurationOutput.error_to_json)])
let get_bucket_notification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_notification
           (Values.GetBucketNotificationConfigurationRequest.make
              ?expectedBucketOwner ~bucket ())
           (Some Values.NotificationConfigurationDeprecated.to_json)
           (Some Values.NotificationConfigurationDeprecated.error_to_json)])
let get_bucket_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_notification_configuration
           (Values.GetBucketNotificationConfigurationRequest.make
              ?expectedBucketOwner ~bucket ())
           (Some Values.NotificationConfiguration.to_json)
           (Some Values.NotificationConfiguration.error_to_json)])
let get_bucket_ownership_controls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_ownership_controls
           (Values.GetBucketOwnershipControlsRequest.make
              ?expectedBucketOwner ~bucket ())
           (Some Values.GetBucketOwnershipControlsOutput.to_json)
           (Some Values.GetBucketOwnershipControlsOutput.error_to_json)])
let get_bucket_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_policy
           (Values.GetBucketPolicyRequest.make ?expectedBucketOwner ~bucket
              ()) (Some Values.GetBucketPolicyOutput.to_json)
           (Some Values.GetBucketPolicyOutput.error_to_json)])
let get_bucket_policy_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_policy_status
           (Values.GetBucketPolicyStatusRequest.make ?expectedBucketOwner
              ~bucket ()) (Some Values.GetBucketPolicyStatusOutput.to_json)
           (Some Values.GetBucketPolicyStatusOutput.error_to_json)])
let get_bucket_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_replication
           (Values.GetBucketReplicationRequest.make ?expectedBucketOwner
              ~bucket ()) (Some Values.GetBucketReplicationOutput.to_json)
           (Some Values.GetBucketReplicationOutput.error_to_json)])
let get_bucket_request_payment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_request_payment
           (Values.GetBucketRequestPaymentRequest.make ?expectedBucketOwner
              ~bucket ()) (Some Values.GetBucketRequestPaymentOutput.to_json)
           (Some Values.GetBucketRequestPaymentOutput.error_to_json)])
let get_bucket_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_tagging
           (Values.GetBucketTaggingRequest.make ?expectedBucketOwner ~bucket
              ()) (Some Values.GetBucketTaggingOutput.to_json)
           (Some Values.GetBucketTaggingOutput.error_to_json)])
let get_bucket_versioning =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_versioning
           (Values.GetBucketVersioningRequest.make ?expectedBucketOwner
              ~bucket ()) (Some Values.GetBucketVersioningOutput.to_json)
           (Some Values.GetBucketVersioningOutput.error_to_json)])
let get_bucket_website =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_website
           (Values.GetBucketWebsiteRequest.make ?expectedBucketOwner ~bucket
              ()) (Some Values.GetBucketWebsiteOutput.to_json)
           (Some Values.GetBucketWebsiteOutput.error_to_json)])
let get_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ifMatch = flag "if-match" (optional string) ~doc:"STRING IfMatch"
       and ifModifiedSince =
         flag "if-modified-since" (optional json_arg)
           ~doc:"JSON IfModifiedSince"
       and ifNoneMatch =
         flag "if-none-match" (optional string) ~doc:"STRING IfNoneMatch"
       and ifUnmodifiedSince =
         flag "if-unmodified-since" (optional json_arg)
           ~doc:"JSON IfUnmodifiedSince"
       and range = flag "range" (optional string) ~doc:"STRING Range"
       and responseCacheControl =
         flag "response-cache-control" (optional string)
           ~doc:"STRING ResponseCacheControl"
       and responseContentDisposition =
         flag "response-content-disposition" (optional string)
           ~doc:"STRING ResponseContentDisposition"
       and responseContentEncoding =
         flag "response-content-encoding" (optional string)
           ~doc:"STRING ResponseContentEncoding"
       and responseContentLanguage =
         flag "response-content-language" (optional string)
           ~doc:"STRING ResponseContentLanguage"
       and responseContentType =
         flag "response-content-type" (optional string)
           ~doc:"STRING ResponseContentType"
       and responseExpires =
         flag "response-expires" (optional json_arg)
           ~doc:"JSON ResponseExpires"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and sSECustomerAlgorithm =
         flag "s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING SSECustomerAlgorithm"
       and sSECustomerKey =
         flag "s-s-e-customer-key" (optional string)
           ~doc:"STRING SSECustomerKey"
       and sSECustomerKeyMD5 =
         flag "s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING SSECustomerKeyMD5"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and partNumber =
         flag "part-number" (optional int) ~doc:"INT PartNumber"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and checksumMode =
         flag "checksum-mode" (optional json_arg) ~doc:"JSON ChecksumMode"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_object
           (Values.GetObjectRequest.make ?ifMatch
              ?ifModifiedSince:(Option.map ~f:Values.IfModifiedSince.of_json
                                  ifModifiedSince) ?ifNoneMatch
              ?ifUnmodifiedSince:(Option.map
                                    ~f:Values.IfUnmodifiedSince.of_json
                                    ifUnmodifiedSince) ?range
              ?responseCacheControl ?responseContentDisposition
              ?responseContentEncoding ?responseContentLanguage
              ?responseContentType
              ?responseExpires:(Option.map ~f:Values.ResponseExpires.of_json
                                  responseExpires) ?versionId
              ?sSECustomerAlgorithm ?sSECustomerKey ?sSECustomerKeyMD5
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?partNumber ?expectedBucketOwner
              ?checksumMode:(Option.map ~f:Values.ChecksumMode.of_json
                               checksumMode) ~bucket ~key ())
           (Some Values.GetObjectOutput.to_json)
           (Some Values.GetObjectOutput.error_to_json)])
let get_object_acl =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_object_acl
           (Values.GetObjectAclRequest.make ?versionId
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?expectedBucketOwner ~bucket
              ~key ()) (Some Values.GetObjectAclOutput.to_json)
           (Some Values.GetObjectAclOutput.error_to_json)])
let get_object_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and maxParts = flag "max-parts" (optional int) ~doc:"INT MaxParts"
       and partNumberMarker =
         flag "part-number-marker" (optional int) ~doc:"INT PartNumberMarker"
       and sSECustomerAlgorithm =
         flag "s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING SSECustomerAlgorithm"
       and sSECustomerKey =
         flag "s-s-e-customer-key" (optional string)
           ~doc:"STRING SSECustomerKey"
       and sSECustomerKeyMD5 =
         flag "s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING SSECustomerKeyMD5"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey"
       and objectAttributes =
         flag "object-attributes" (required json_arg)
           ~doc:"JSON ObjectAttributesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_object_attributes
           (Values.GetObjectAttributesRequest.make ?versionId ?maxParts
              ?partNumberMarker ?sSECustomerAlgorithm ?sSECustomerKey
              ?sSECustomerKeyMD5
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?expectedBucketOwner ~bucket
              ~key
              ~objectAttributes:(Values.ObjectAttributesList.of_json
                                   objectAttributes) ())
           (Some Values.GetObjectAttributesOutput.to_json)
           (Some Values.GetObjectAttributesOutput.error_to_json)])
let get_object_legal_hold =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_object_legal_hold
           (Values.GetObjectLegalHoldRequest.make ?versionId
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?expectedBucketOwner ~bucket
              ~key ()) (Some Values.GetObjectLegalHoldOutput.to_json)
           (Some Values.GetObjectLegalHoldOutput.error_to_json)])
let get_object_lock_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_object_lock_configuration
           (Values.GetObjectLockConfigurationRequest.make
              ?expectedBucketOwner ~bucket ())
           (Some Values.GetObjectLockConfigurationOutput.to_json)
           (Some Values.GetObjectLockConfigurationOutput.error_to_json)])
let get_object_retention =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_object_retention
           (Values.GetObjectRetentionRequest.make ?versionId
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?expectedBucketOwner ~bucket
              ~key ()) (Some Values.GetObjectRetentionOutput.to_json)
           (Some Values.GetObjectRetentionOutput.error_to_json)])
let get_object_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_object_tagging
           (Values.GetObjectTaggingRequest.make ?versionId
              ?expectedBucketOwner
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ~bucket ~key ())
           (Some Values.GetObjectTaggingOutput.to_json)
           (Some Values.GetObjectTaggingOutput.error_to_json)])
let get_object_torrent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_object_torrent
           (Values.GetObjectTorrentRequest.make
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?expectedBucketOwner ~bucket
              ~key ()) (Some Values.GetObjectTorrentOutput.to_json)
           (Some Values.GetObjectTorrentOutput.error_to_json)])
let get_public_access_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_public_access_block
           (Values.GetPublicAccessBlockRequest.make ?expectedBucketOwner
              ~bucket ()) (Some Values.GetPublicAccessBlockOutput.to_json)
           (Some Values.GetPublicAccessBlockOutput.error_to_json)])
let head_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.head_bucket
           (Values.HeadBucketRequest.make ?expectedBucketOwner ~bucket ())
           None None])
let head_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ifMatch = flag "if-match" (optional string) ~doc:"STRING IfMatch"
       and ifModifiedSince =
         flag "if-modified-since" (optional json_arg)
           ~doc:"JSON IfModifiedSince"
       and ifNoneMatch =
         flag "if-none-match" (optional string) ~doc:"STRING IfNoneMatch"
       and ifUnmodifiedSince =
         flag "if-unmodified-since" (optional json_arg)
           ~doc:"JSON IfUnmodifiedSince"
       and range = flag "range" (optional string) ~doc:"STRING Range"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and sSECustomerAlgorithm =
         flag "s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING SSECustomerAlgorithm"
       and sSECustomerKey =
         flag "s-s-e-customer-key" (optional string)
           ~doc:"STRING SSECustomerKey"
       and sSECustomerKeyMD5 =
         flag "s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING SSECustomerKeyMD5"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and partNumber =
         flag "part-number" (optional int) ~doc:"INT PartNumber"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and checksumMode =
         flag "checksum-mode" (optional json_arg) ~doc:"JSON ChecksumMode"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.head_object
           (Values.HeadObjectRequest.make ?ifMatch
              ?ifModifiedSince:(Option.map ~f:Values.IfModifiedSince.of_json
                                  ifModifiedSince) ?ifNoneMatch
              ?ifUnmodifiedSince:(Option.map
                                    ~f:Values.IfUnmodifiedSince.of_json
                                    ifUnmodifiedSince) ?range ?versionId
              ?sSECustomerAlgorithm ?sSECustomerKey ?sSECustomerKeyMD5
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?partNumber ?expectedBucketOwner
              ?checksumMode:(Option.map ~f:Values.ChecksumMode.of_json
                               checksumMode) ~bucket ~key ())
           (Some Values.HeadObjectOutput.to_json)
           (Some Values.HeadObjectOutput.error_to_json)])
let list_bucket_analytics_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and continuationToken =
         flag "continuation-token" (optional string) ~doc:"STRING Token"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bucket_analytics_configurations
           (Values.ListBucketAnalyticsConfigurationsRequest.make
              ?continuationToken ?expectedBucketOwner ~bucket ())
           (Some Values.ListBucketAnalyticsConfigurationsOutput.to_json)
           (Some Values.ListBucketAnalyticsConfigurationsOutput.error_to_json)])
let list_bucket_intelligent_tiering_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and continuationToken =
         flag "continuation-token" (optional string) ~doc:"STRING Token"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bucket_intelligent_tiering_configurations
           (Values.ListBucketIntelligentTieringConfigurationsRequest.make
              ?continuationToken ~bucket ())
           (Some
              Values.ListBucketIntelligentTieringConfigurationsOutput.to_json)
           (Some
              Values.ListBucketIntelligentTieringConfigurationsOutput.error_to_json)])
let list_bucket_inventory_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and continuationToken =
         flag "continuation-token" (optional string) ~doc:"STRING Token"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bucket_inventory_configurations
           (Values.ListBucketInventoryConfigurationsRequest.make
              ?continuationToken ?expectedBucketOwner ~bucket ())
           (Some Values.ListBucketInventoryConfigurationsOutput.to_json)
           (Some Values.ListBucketInventoryConfigurationsOutput.error_to_json)])
let list_bucket_metrics_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and continuationToken =
         flag "continuation-token" (optional string) ~doc:"STRING Token"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bucket_metrics_configurations
           (Values.ListBucketMetricsConfigurationsRequest.make
              ?continuationToken ?expectedBucketOwner ~bucket ())
           (Some Values.ListBucketMetricsConfigurationsOutput.to_json)
           (Some Values.ListBucketMetricsConfigurationsOutput.error_to_json)])
let list_buckets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_buckets (Fn.id ()) (Some Values.ListBucketsOutput.to_json)
           (Some Values.ListBucketsOutput.error_to_json)])
let list_multipart_uploads =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and delimiter =
         flag "delimiter" (optional string) ~doc:"STRING Delimiter"
       and encodingType =
         flag "encoding-type" (optional json_arg) ~doc:"JSON EncodingType"
       and keyMarker =
         flag "key-marker" (optional string) ~doc:"STRING KeyMarker"
       and maxUploads =
         flag "max-uploads" (optional int) ~doc:"INT MaxUploads"
       and prefix = flag "prefix" (optional string) ~doc:"STRING Prefix"
       and uploadIdMarker =
         flag "upload-id-marker" (optional string)
           ~doc:"STRING UploadIdMarker"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_multipart_uploads
           (Values.ListMultipartUploadsRequest.make ?delimiter
              ?encodingType:(Option.map ~f:Values.EncodingType.of_json
                               encodingType) ?keyMarker ?maxUploads ?prefix
              ?uploadIdMarker ?expectedBucketOwner ~bucket ())
           (Some Values.ListMultipartUploadsOutput.to_json)
           (Some Values.ListMultipartUploadsOutput.error_to_json)])
let list_object_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and delimiter =
         flag "delimiter" (optional string) ~doc:"STRING Delimiter"
       and encodingType =
         flag "encoding-type" (optional json_arg) ~doc:"JSON EncodingType"
       and keyMarker =
         flag "key-marker" (optional string) ~doc:"STRING KeyMarker"
       and maxKeys = flag "max-keys" (optional int) ~doc:"INT MaxKeys"
       and prefix = flag "prefix" (optional string) ~doc:"STRING Prefix"
       and versionIdMarker =
         flag "version-id-marker" (optional string)
           ~doc:"STRING VersionIdMarker"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_object_versions
           (Values.ListObjectVersionsRequest.make ?delimiter
              ?encodingType:(Option.map ~f:Values.EncodingType.of_json
                               encodingType) ?keyMarker ?maxKeys ?prefix
              ?versionIdMarker ?expectedBucketOwner ~bucket ())
           (Some Values.ListObjectVersionsOutput.to_json)
           (Some Values.ListObjectVersionsOutput.error_to_json)])
let list_objects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and delimiter =
         flag "delimiter" (optional string) ~doc:"STRING Delimiter"
       and encodingType =
         flag "encoding-type" (optional json_arg) ~doc:"JSON EncodingType"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and maxKeys = flag "max-keys" (optional int) ~doc:"INT MaxKeys"
       and prefix = flag "prefix" (optional string) ~doc:"STRING Prefix"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_objects
           (Values.ListObjectsRequest.make ?delimiter
              ?encodingType:(Option.map ~f:Values.EncodingType.of_json
                               encodingType) ?marker ?maxKeys ?prefix
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?expectedBucketOwner ~bucket ())
           (Some Values.ListObjectsOutput.to_json)
           (Some Values.ListObjectsOutput.error_to_json)])
let list_objects_v2 =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and delimiter =
         flag "delimiter" (optional string) ~doc:"STRING Delimiter"
       and encodingType =
         flag "encoding-type" (optional json_arg) ~doc:"JSON EncodingType"
       and maxKeys = flag "max-keys" (optional int) ~doc:"INT MaxKeys"
       and prefix = flag "prefix" (optional string) ~doc:"STRING Prefix"
       and continuationToken =
         flag "continuation-token" (optional string) ~doc:"STRING Token"
       and fetchOwner =
         flag "fetch-owner" (optional bool) ~doc:"BOOL FetchOwner"
       and startAfter =
         flag "start-after" (optional string) ~doc:"STRING StartAfter"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_objects_v2
           (Values.ListObjectsV2Request.make ?delimiter
              ?encodingType:(Option.map ~f:Values.EncodingType.of_json
                               encodingType) ?maxKeys ?prefix
              ?continuationToken ?fetchOwner ?startAfter
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?expectedBucketOwner ~bucket ())
           (Some Values.ListObjectsV2Output.to_json)
           (Some Values.ListObjectsV2Output.error_to_json)])
let list_parts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxParts = flag "max-parts" (optional int) ~doc:"INT MaxParts"
       and partNumberMarker =
         flag "part-number-marker" (optional int) ~doc:"INT PartNumberMarker"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and sSECustomerAlgorithm =
         flag "s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING SSECustomerAlgorithm"
       and sSECustomerKey =
         flag "s-s-e-customer-key" (optional string)
           ~doc:"STRING SSECustomerKey"
       and sSECustomerKeyMD5 =
         flag "s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING SSECustomerKeyMD5"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING MultipartUploadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_parts
           (Values.ListPartsRequest.make ?maxParts ?partNumberMarker
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?expectedBucketOwner
              ?sSECustomerAlgorithm ?sSECustomerKey ?sSECustomerKeyMD5
              ~bucket ~key ~uploadId ())
           (Some Values.ListPartsOutput.to_json)
           (Some Values.ListPartsOutput.error_to_json)])
let put_bucket_accelerate_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and accelerateConfiguration =
         flag "accelerate-configuration" (required json_arg)
           ~doc:"JSON AccelerateConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_accelerate_configuration
           (Values.PutBucketAccelerateConfigurationRequest.make
              ?expectedBucketOwner
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ~bucket
              ~accelerateConfiguration:(Values.AccelerateConfiguration.of_json
                                          accelerateConfiguration) ()) None
           None])
let put_bucket_acl =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aCL = flag "a-c-l" (optional json_arg) ~doc:"JSON BucketCannedACL"
       and accessControlPolicy =
         flag "access-control-policy" (optional json_arg)
           ~doc:"JSON AccessControlPolicy"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and grantFullControl =
         flag "grant-full-control" (optional string)
           ~doc:"STRING GrantFullControl"
       and grantRead =
         flag "grant-read" (optional string) ~doc:"STRING GrantRead"
       and grantReadACP =
         flag "grant-read-a-c-p" (optional string) ~doc:"STRING GrantReadACP"
       and grantWrite =
         flag "grant-write" (optional string) ~doc:"STRING GrantWrite"
       and grantWriteACP =
         flag "grant-write-a-c-p" (optional string)
           ~doc:"STRING GrantWriteACP"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_acl
           (Values.PutBucketAclRequest.make
              ?aCL:(Option.map ~f:Values.BucketCannedACL.of_json aCL)
              ?accessControlPolicy:(Option.map
                                      ~f:Values.AccessControlPolicy.of_json
                                      accessControlPolicy) ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?grantFullControl
              ?grantRead ?grantReadACP ?grantWrite ?grantWriteACP
              ?expectedBucketOwner ~bucket ()) None None])
let put_bucket_analytics_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and id = flag "id" (required string) ~doc:"STRING AnalyticsId"
       and analyticsConfiguration =
         flag "analytics-configuration" (required json_arg)
           ~doc:"JSON AnalyticsConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_analytics_configuration
           (Values.PutBucketAnalyticsConfigurationRequest.make
              ?expectedBucketOwner ~bucket ~id
              ~analyticsConfiguration:(Values.AnalyticsConfiguration.of_json
                                         analyticsConfiguration) ()) None
           None])
let put_bucket_cors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and cORSConfiguration =
         flag "c-o-r-s-configuration" (required json_arg)
           ~doc:"JSON CORSConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_cors
           (Values.PutBucketCorsRequest.make ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?expectedBucketOwner
              ~bucket
              ~cORSConfiguration:(Values.CORSConfiguration.of_json
                                    cORSConfiguration) ()) None None])
let put_bucket_encryption =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and serverSideEncryptionConfiguration =
         flag "server-side-encryption-configuration" (required json_arg)
           ~doc:"JSON ServerSideEncryptionConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_encryption
           (Values.PutBucketEncryptionRequest.make ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?expectedBucketOwner
              ~bucket
              ~serverSideEncryptionConfiguration:(Values.ServerSideEncryptionConfiguration.of_json
                                                    serverSideEncryptionConfiguration)
              ()) None None])
let put_bucket_intelligent_tiering_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and id =
         flag "id" (required string) ~doc:"STRING IntelligentTieringId"
       and intelligentTieringConfiguration =
         flag "intelligent-tiering-configuration" (required json_arg)
           ~doc:"JSON IntelligentTieringConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_intelligent_tiering_configuration
           (Values.PutBucketIntelligentTieringConfigurationRequest.make
              ~bucket ~id
              ~intelligentTieringConfiguration:(Values.IntelligentTieringConfiguration.of_json
                                                  intelligentTieringConfiguration)
              ()) None None])
let put_bucket_inventory_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and id = flag "id" (required string) ~doc:"STRING InventoryId"
       and inventoryConfiguration =
         flag "inventory-configuration" (required json_arg)
           ~doc:"JSON InventoryConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_inventory_configuration
           (Values.PutBucketInventoryConfigurationRequest.make
              ?expectedBucketOwner ~bucket ~id
              ~inventoryConfiguration:(Values.InventoryConfiguration.of_json
                                         inventoryConfiguration) ()) None
           None])
let put_bucket_lifecycle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and lifecycleConfiguration =
         flag "lifecycle-configuration" (optional json_arg)
           ~doc:"JSON LifecycleConfiguration"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_lifecycle
           (Values.PutBucketLifecycleRequest.make ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm)
              ?lifecycleConfiguration:(Option.map
                                         ~f:Values.LifecycleConfiguration.of_json
                                         lifecycleConfiguration)
              ?expectedBucketOwner ~bucket ()) None None])
let put_bucket_lifecycle_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and lifecycleConfiguration =
         flag "lifecycle-configuration" (optional json_arg)
           ~doc:"JSON BucketLifecycleConfiguration"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_lifecycle_configuration
           (Values.PutBucketLifecycleConfigurationRequest.make
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm)
              ?lifecycleConfiguration:(Option.map
                                         ~f:Values.BucketLifecycleConfiguration.of_json
                                         lifecycleConfiguration)
              ?expectedBucketOwner ~bucket ()) None None])
let put_bucket_logging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and bucketLoggingStatus =
         flag "bucket-logging-status" (required json_arg)
           ~doc:"JSON BucketLoggingStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_logging
           (Values.PutBucketLoggingRequest.make ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?expectedBucketOwner
              ~bucket
              ~bucketLoggingStatus:(Values.BucketLoggingStatus.of_json
                                      bucketLoggingStatus) ()) None None])
let put_bucket_metrics_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and id = flag "id" (required string) ~doc:"STRING MetricsId"
       and metricsConfiguration =
         flag "metrics-configuration" (required json_arg)
           ~doc:"JSON MetricsConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_metrics_configuration
           (Values.PutBucketMetricsConfigurationRequest.make
              ?expectedBucketOwner ~bucket ~id
              ~metricsConfiguration:(Values.MetricsConfiguration.of_json
                                       metricsConfiguration) ()) None None])
let put_bucket_notification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and notificationConfiguration =
         flag "notification-configuration" (required json_arg)
           ~doc:"JSON NotificationConfigurationDeprecated" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_notification
           (Values.PutBucketNotificationRequest.make ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?expectedBucketOwner
              ~bucket
              ~notificationConfiguration:(Values.NotificationConfigurationDeprecated.of_json
                                            notificationConfiguration) ())
           None None])
let put_bucket_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and skipDestinationValidation =
         flag "skip-destination-validation" (optional bool)
           ~doc:"BOOL SkipValidation"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and notificationConfiguration =
         flag "notification-configuration" (required json_arg)
           ~doc:"JSON NotificationConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_notification_configuration
           (Values.PutBucketNotificationConfigurationRequest.make
              ?expectedBucketOwner ?skipDestinationValidation ~bucket
              ~notificationConfiguration:(Values.NotificationConfiguration.of_json
                                            notificationConfiguration) ())
           None None])
let put_bucket_ownership_controls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and ownershipControls =
         flag "ownership-controls" (required json_arg)
           ~doc:"JSON OwnershipControls" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_ownership_controls
           (Values.PutBucketOwnershipControlsRequest.make ?contentMD5
              ?expectedBucketOwner ~bucket
              ~ownershipControls:(Values.OwnershipControls.of_json
                                    ownershipControls) ()) None None])
let put_bucket_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and confirmRemoveSelfBucketAccess =
         flag "confirm-remove-self-bucket-access" (optional bool)
           ~doc:"BOOL ConfirmRemoveSelfBucketAccess"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and policy = flag "policy" (required string) ~doc:"STRING Policy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_policy
           (Values.PutBucketPolicyRequest.make ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm)
              ?confirmRemoveSelfBucketAccess ?expectedBucketOwner ~bucket
              ~policy ()) None None])
let put_bucket_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and token =
         flag "token" (optional string) ~doc:"STRING ObjectLockToken"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and replicationConfiguration =
         flag "replication-configuration" (required json_arg)
           ~doc:"JSON ReplicationConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_replication
           (Values.PutBucketReplicationRequest.make ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?token
              ?expectedBucketOwner ~bucket
              ~replicationConfiguration:(Values.ReplicationConfiguration.of_json
                                           replicationConfiguration) ()) None
           None])
let put_bucket_request_payment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and requestPaymentConfiguration =
         flag "request-payment-configuration" (required json_arg)
           ~doc:"JSON RequestPaymentConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_request_payment
           (Values.PutBucketRequestPaymentRequest.make ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?expectedBucketOwner
              ~bucket
              ~requestPaymentConfiguration:(Values.RequestPaymentConfiguration.of_json
                                              requestPaymentConfiguration) ())
           None None])
let put_bucket_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and tagging = flag "tagging" (required json_arg) ~doc:"JSON Tagging" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_tagging
           (Values.PutBucketTaggingRequest.make ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?expectedBucketOwner
              ~bucket ~tagging:(Values.Tagging.of_json tagging) ()) None None])
let put_bucket_versioning =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and mFA = flag "m-f-a" (optional string) ~doc:"STRING MFA"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and versioningConfiguration =
         flag "versioning-configuration" (required json_arg)
           ~doc:"JSON VersioningConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_versioning
           (Values.PutBucketVersioningRequest.make ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?mFA
              ?expectedBucketOwner ~bucket
              ~versioningConfiguration:(Values.VersioningConfiguration.of_json
                                          versioningConfiguration) ()) None
           None])
let put_bucket_website =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and websiteConfiguration =
         flag "website-configuration" (required json_arg)
           ~doc:"JSON WebsiteConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_website
           (Values.PutBucketWebsiteRequest.make ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?expectedBucketOwner
              ~bucket
              ~websiteConfiguration:(Values.WebsiteConfiguration.of_json
                                       websiteConfiguration) ()) None None])
let put_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aCL = flag "a-c-l" (optional json_arg) ~doc:"JSON ObjectCannedACL"
       and body = flag "body" (optional json_arg) ~doc:"JSON Body"
       and cacheControl =
         flag "cache-control" (optional string) ~doc:"STRING CacheControl"
       and contentDisposition =
         flag "content-disposition" (optional string)
           ~doc:"STRING ContentDisposition"
       and contentEncoding =
         flag "content-encoding" (optional string)
           ~doc:"STRING ContentEncoding"
       and contentLanguage =
         flag "content-language" (optional string)
           ~doc:"STRING ContentLanguage"
       and contentLength =
         flag "content-length" (optional json_arg) ~doc:"JSON ContentLength"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and contentType =
         flag "content-type" (optional string) ~doc:"STRING ContentType"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and checksumCRC32 =
         flag "checksum-c-r-c32" (optional string)
           ~doc:"STRING ChecksumCRC32"
       and checksumCRC32C =
         flag "checksum-c-r-c32-c" (optional string)
           ~doc:"STRING ChecksumCRC32C"
       and checksumSHA1 =
         flag "checksum-s-h-a1" (optional string) ~doc:"STRING ChecksumSHA1"
       and checksumSHA256 =
         flag "checksum-s-h-a256" (optional string)
           ~doc:"STRING ChecksumSHA256"
       and expires = flag "expires" (optional json_arg) ~doc:"JSON Expires"
       and grantFullControl =
         flag "grant-full-control" (optional string)
           ~doc:"STRING GrantFullControl"
       and grantRead =
         flag "grant-read" (optional string) ~doc:"STRING GrantRead"
       and grantReadACP =
         flag "grant-read-a-c-p" (optional string) ~doc:"STRING GrantReadACP"
       and grantWriteACP =
         flag "grant-write-a-c-p" (optional string)
           ~doc:"STRING GrantWriteACP"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON Metadata"
       and serverSideEncryption =
         flag "server-side-encryption" (optional json_arg)
           ~doc:"JSON ServerSideEncryption"
       and storageClass =
         flag "storage-class" (optional json_arg) ~doc:"JSON StorageClass"
       and websiteRedirectLocation =
         flag "website-redirect-location" (optional string)
           ~doc:"STRING WebsiteRedirectLocation"
       and sSECustomerAlgorithm =
         flag "s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING SSECustomerAlgorithm"
       and sSECustomerKey =
         flag "s-s-e-customer-key" (optional string)
           ~doc:"STRING SSECustomerKey"
       and sSECustomerKeyMD5 =
         flag "s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING SSECustomerKeyMD5"
       and sSEKMSKeyId =
         flag "s-s-e-k-m-s-key-id" (optional string)
           ~doc:"STRING SSEKMSKeyId"
       and sSEKMSEncryptionContext =
         flag "s-s-e-k-m-s-encryption-context" (optional string)
           ~doc:"STRING SSEKMSEncryptionContext"
       and bucketKeyEnabled =
         flag "bucket-key-enabled" (optional bool)
           ~doc:"BOOL BucketKeyEnabled"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and tagging =
         flag "tagging" (optional string) ~doc:"STRING TaggingHeader"
       and objectLockMode =
         flag "object-lock-mode" (optional json_arg)
           ~doc:"JSON ObjectLockMode"
       and objectLockRetainUntilDate =
         flag "object-lock-retain-until-date" (optional json_arg)
           ~doc:"JSON ObjectLockRetainUntilDate"
       and objectLockLegalHoldStatus =
         flag "object-lock-legal-hold-status" (optional json_arg)
           ~doc:"JSON ObjectLockLegalHoldStatus"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_object
           (Values.PutObjectRequest.make
              ?aCL:(Option.map ~f:Values.ObjectCannedACL.of_json aCL)
              ?body:(Option.map ~f:Values.Body.of_json body) ?cacheControl
              ?contentDisposition ?contentEncoding ?contentLanguage
              ?contentLength:(Option.map ~f:Values.ContentLength.of_json
                                contentLength) ?contentMD5 ?contentType
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?checksumCRC32
              ?checksumCRC32C ?checksumSHA1 ?checksumSHA256
              ?expires:(Option.map ~f:Values.Expires.of_json expires)
              ?grantFullControl ?grantRead ?grantReadACP ?grantWriteACP
              ?metadata:(Option.map ~f:Values.Metadata.of_json metadata)
              ?serverSideEncryption:(Option.map
                                       ~f:Values.ServerSideEncryption.of_json
                                       serverSideEncryption)
              ?storageClass:(Option.map ~f:Values.StorageClass.of_json
                               storageClass) ?websiteRedirectLocation
              ?sSECustomerAlgorithm ?sSECustomerKey ?sSECustomerKeyMD5
              ?sSEKMSKeyId ?sSEKMSEncryptionContext ?bucketKeyEnabled
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?tagging
              ?objectLockMode:(Option.map ~f:Values.ObjectLockMode.of_json
                                 objectLockMode)
              ?objectLockRetainUntilDate:(Option.map
                                            ~f:Values.ObjectLockRetainUntilDate.of_json
                                            objectLockRetainUntilDate)
              ?objectLockLegalHoldStatus:(Option.map
                                            ~f:Values.ObjectLockLegalHoldStatus.of_json
                                            objectLockLegalHoldStatus)
              ?expectedBucketOwner ~bucket ~key ())
           (Some Values.PutObjectOutput.to_json)
           (Some Values.PutObjectOutput.error_to_json)])
let put_object_acl =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aCL = flag "a-c-l" (optional json_arg) ~doc:"JSON ObjectCannedACL"
       and accessControlPolicy =
         flag "access-control-policy" (optional json_arg)
           ~doc:"JSON AccessControlPolicy"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and grantFullControl =
         flag "grant-full-control" (optional string)
           ~doc:"STRING GrantFullControl"
       and grantRead =
         flag "grant-read" (optional string) ~doc:"STRING GrantRead"
       and grantReadACP =
         flag "grant-read-a-c-p" (optional string) ~doc:"STRING GrantReadACP"
       and grantWrite =
         flag "grant-write" (optional string) ~doc:"STRING GrantWrite"
       and grantWriteACP =
         flag "grant-write-a-c-p" (optional string)
           ~doc:"STRING GrantWriteACP"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_object_acl
           (Values.PutObjectAclRequest.make
              ?aCL:(Option.map ~f:Values.ObjectCannedACL.of_json aCL)
              ?accessControlPolicy:(Option.map
                                      ~f:Values.AccessControlPolicy.of_json
                                      accessControlPolicy) ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?grantFullControl
              ?grantRead ?grantReadACP ?grantWrite ?grantWriteACP
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?versionId ?expectedBucketOwner
              ~bucket ~key ()) (Some Values.PutObjectAclOutput.to_json)
           (Some Values.PutObjectAclOutput.error_to_json)])
let put_object_legal_hold =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and legalHold =
         flag "legal-hold" (optional json_arg)
           ~doc:"JSON ObjectLockLegalHold"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_object_legal_hold
           (Values.PutObjectLegalHoldRequest.make
              ?legalHold:(Option.map ~f:Values.ObjectLockLegalHold.of_json
                            legalHold)
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?versionId ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?expectedBucketOwner
              ~bucket ~key ()) (Some Values.PutObjectLegalHoldOutput.to_json)
           (Some Values.PutObjectLegalHoldOutput.error_to_json)])
let put_object_lock_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and objectLockConfiguration =
         flag "object-lock-configuration" (optional json_arg)
           ~doc:"JSON ObjectLockConfiguration"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and token =
         flag "token" (optional string) ~doc:"STRING ObjectLockToken"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_object_lock_configuration
           (Values.PutObjectLockConfigurationRequest.make
              ?objectLockConfiguration:(Option.map
                                          ~f:Values.ObjectLockConfiguration.of_json
                                          objectLockConfiguration)
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?token ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?expectedBucketOwner
              ~bucket ())
           (Some Values.PutObjectLockConfigurationOutput.to_json)
           (Some Values.PutObjectLockConfigurationOutput.error_to_json)])
let put_object_retention =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retention =
         flag "retention" (optional json_arg) ~doc:"JSON ObjectLockRetention"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and bypassGovernanceRetention =
         flag "bypass-governance-retention" (optional bool)
           ~doc:"BOOL BypassGovernanceRetention"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_object_retention
           (Values.PutObjectRetentionRequest.make
              ?retention:(Option.map ~f:Values.ObjectLockRetention.of_json
                            retention)
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?versionId
              ?bypassGovernanceRetention ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?expectedBucketOwner
              ~bucket ~key ()) (Some Values.PutObjectRetentionOutput.to_json)
           (Some Values.PutObjectRetentionOutput.error_to_json)])
let put_object_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey"
       and tagging = flag "tagging" (required json_arg) ~doc:"JSON Tagging" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_object_tagging
           (Values.PutObjectTaggingRequest.make ?versionId ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?expectedBucketOwner
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ~bucket ~key
              ~tagging:(Values.Tagging.of_json tagging) ())
           (Some Values.PutObjectTaggingOutput.to_json)
           (Some Values.PutObjectTaggingOutput.error_to_json)])
let put_public_access_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and publicAccessBlockConfiguration =
         flag "public-access-block-configuration" (required json_arg)
           ~doc:"JSON PublicAccessBlockConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_public_access_block
           (Values.PutPublicAccessBlockRequest.make ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?expectedBucketOwner
              ~bucket
              ~publicAccessBlockConfiguration:(Values.PublicAccessBlockConfiguration.of_json
                                                 publicAccessBlockConfiguration)
              ()) None None])
let restore_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and restoreRequest =
         flag "restore-request" (optional json_arg)
           ~doc:"JSON RestoreRequest"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_object
           (Values.RestoreObjectRequest.make ?versionId
              ?restoreRequest:(Option.map ~f:Values.RestoreRequest.of_json
                                 restoreRequest)
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer)
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?expectedBucketOwner
              ~bucket ~key ()) (Some Values.RestoreObjectOutput.to_json)
           (Some Values.RestoreObjectOutput.error_to_json)])
let select_object_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sSECustomerAlgorithm =
         flag "s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING SSECustomerAlgorithm"
       and sSECustomerKey =
         flag "s-s-e-customer-key" (optional string)
           ~doc:"STRING SSECustomerKey"
       and sSECustomerKeyMD5 =
         flag "s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING SSECustomerKeyMD5"
       and requestProgress =
         flag "request-progress" (optional json_arg)
           ~doc:"JSON RequestProgress"
       and scanRange =
         flag "scan-range" (optional json_arg) ~doc:"JSON ScanRange"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey"
       and expression =
         flag "expression" (required string) ~doc:"STRING Expression"
       and expressionType =
         flag "expression-type" (required json_arg)
           ~doc:"JSON ExpressionType"
       and inputSerialization =
         flag "input-serialization" (required json_arg)
           ~doc:"JSON InputSerialization"
       and outputSerialization =
         flag "output-serialization" (required json_arg)
           ~doc:"JSON OutputSerialization" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.select_object_content
           (Values.SelectObjectContentRequest.make ?sSECustomerAlgorithm
              ?sSECustomerKey ?sSECustomerKeyMD5
              ?requestProgress:(Option.map ~f:Values.RequestProgress.of_json
                                  requestProgress)
              ?scanRange:(Option.map ~f:Values.ScanRange.of_json scanRange)
              ?expectedBucketOwner ~bucket ~key ~expression
              ~expressionType:(Values.ExpressionType.of_json expressionType)
              ~inputSerialization:(Values.InputSerialization.of_json
                                     inputSerialization)
              ~outputSerialization:(Values.OutputSerialization.of_json
                                      outputSerialization) ())
           (Some Values.SelectObjectContentOutput.to_json)
           (Some Values.SelectObjectContentOutput.error_to_json)])
let upload_part =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and body = flag "body" (optional json_arg) ~doc:"JSON Body"
       and contentLength =
         flag "content-length" (optional json_arg) ~doc:"JSON ContentLength"
       and contentMD5 =
         flag "content-m-d5" (optional string) ~doc:"STRING ContentMD5"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and checksumCRC32 =
         flag "checksum-c-r-c32" (optional string)
           ~doc:"STRING ChecksumCRC32"
       and checksumCRC32C =
         flag "checksum-c-r-c32-c" (optional string)
           ~doc:"STRING ChecksumCRC32C"
       and checksumSHA1 =
         flag "checksum-s-h-a1" (optional string) ~doc:"STRING ChecksumSHA1"
       and checksumSHA256 =
         flag "checksum-s-h-a256" (optional string)
           ~doc:"STRING ChecksumSHA256"
       and sSECustomerAlgorithm =
         flag "s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING SSECustomerAlgorithm"
       and sSECustomerKey =
         flag "s-s-e-customer-key" (optional string)
           ~doc:"STRING SSECustomerKey"
       and sSECustomerKeyMD5 =
         flag "s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING SSECustomerKeyMD5"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey"
       and partNumber =
         flag "part-number" (required int) ~doc:"INT PartNumber"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING MultipartUploadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upload_part
           (Values.UploadPartRequest.make
              ?body:(Option.map ~f:Values.Body.of_json body)
              ?contentLength:(Option.map ~f:Values.ContentLength.of_json
                                contentLength) ?contentMD5
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ?checksumCRC32
              ?checksumCRC32C ?checksumSHA1 ?checksumSHA256
              ?sSECustomerAlgorithm ?sSECustomerKey ?sSECustomerKeyMD5
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?expectedBucketOwner ~bucket
              ~key ~partNumber ~uploadId ())
           (Some Values.UploadPartOutput.to_json)
           (Some Values.UploadPartOutput.error_to_json)])
let upload_part_copy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and copySourceIfMatch =
         flag "copy-source-if-match" (optional string)
           ~doc:"STRING CopySourceIfMatch"
       and copySourceIfModifiedSince =
         flag "copy-source-if-modified-since" (optional json_arg)
           ~doc:"JSON CopySourceIfModifiedSince"
       and copySourceIfNoneMatch =
         flag "copy-source-if-none-match" (optional string)
           ~doc:"STRING CopySourceIfNoneMatch"
       and copySourceIfUnmodifiedSince =
         flag "copy-source-if-unmodified-since" (optional json_arg)
           ~doc:"JSON CopySourceIfUnmodifiedSince"
       and copySourceRange =
         flag "copy-source-range" (optional string)
           ~doc:"STRING CopySourceRange"
       and sSECustomerAlgorithm =
         flag "s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING SSECustomerAlgorithm"
       and sSECustomerKey =
         flag "s-s-e-customer-key" (optional string)
           ~doc:"STRING SSECustomerKey"
       and sSECustomerKeyMD5 =
         flag "s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING SSECustomerKeyMD5"
       and copySourceSSECustomerAlgorithm =
         flag "copy-source-s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING CopySourceSSECustomerAlgorithm"
       and copySourceSSECustomerKey =
         flag "copy-source-s-s-e-customer-key" (optional string)
           ~doc:"STRING CopySourceSSECustomerKey"
       and copySourceSSECustomerKeyMD5 =
         flag "copy-source-s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING CopySourceSSECustomerKeyMD5"
       and requestPayer =
         flag "request-payer" (optional json_arg) ~doc:"JSON RequestPayer"
       and expectedBucketOwner =
         flag "expected-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and expectedSourceBucketOwner =
         flag "expected-source-bucket-owner" (optional string)
           ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and copySource =
         flag "copy-source" (required string) ~doc:"STRING CopySource"
       and key = flag "key" (required string) ~doc:"STRING ObjectKey"
       and partNumber =
         flag "part-number" (required int) ~doc:"INT PartNumber"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING MultipartUploadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upload_part_copy
           (Values.UploadPartCopyRequest.make ?copySourceIfMatch
              ?copySourceIfModifiedSince:(Option.map
                                            ~f:Values.CopySourceIfModifiedSince.of_json
                                            copySourceIfModifiedSince)
              ?copySourceIfNoneMatch
              ?copySourceIfUnmodifiedSince:(Option.map
                                              ~f:Values.CopySourceIfUnmodifiedSince.of_json
                                              copySourceIfUnmodifiedSince)
              ?copySourceRange ?sSECustomerAlgorithm ?sSECustomerKey
              ?sSECustomerKeyMD5 ?copySourceSSECustomerAlgorithm
              ?copySourceSSECustomerKey ?copySourceSSECustomerKeyMD5
              ?requestPayer:(Option.map ~f:Values.RequestPayer.of_json
                               requestPayer) ?expectedBucketOwner
              ?expectedSourceBucketOwner ~bucket ~copySource ~key ~partNumber
              ~uploadId ()) (Some Values.UploadPartCopyOutput.to_json)
           (Some Values.UploadPartCopyOutput.error_to_json)])
let write_get_object_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and body = flag "body" (optional json_arg) ~doc:"JSON Body"
       and statusCode =
         flag "status-code" (optional int)
           ~doc:"INT GetObjectResponseStatusCode"
       and errorCode =
         flag "error-code" (optional string) ~doc:"STRING ErrorCode"
       and errorMessage =
         flag "error-message" (optional string) ~doc:"STRING ErrorMessage"
       and acceptRanges =
         flag "accept-ranges" (optional string) ~doc:"STRING AcceptRanges"
       and cacheControl =
         flag "cache-control" (optional string) ~doc:"STRING CacheControl"
       and contentDisposition =
         flag "content-disposition" (optional string)
           ~doc:"STRING ContentDisposition"
       and contentEncoding =
         flag "content-encoding" (optional string)
           ~doc:"STRING ContentEncoding"
       and contentLanguage =
         flag "content-language" (optional string)
           ~doc:"STRING ContentLanguage"
       and contentLength =
         flag "content-length" (optional json_arg) ~doc:"JSON ContentLength"
       and contentRange =
         flag "content-range" (optional string) ~doc:"STRING ContentRange"
       and contentType =
         flag "content-type" (optional string) ~doc:"STRING ContentType"
       and checksumCRC32 =
         flag "checksum-c-r-c32" (optional string)
           ~doc:"STRING ChecksumCRC32"
       and checksumCRC32C =
         flag "checksum-c-r-c32-c" (optional string)
           ~doc:"STRING ChecksumCRC32C"
       and checksumSHA1 =
         flag "checksum-s-h-a1" (optional string) ~doc:"STRING ChecksumSHA1"
       and checksumSHA256 =
         flag "checksum-s-h-a256" (optional string)
           ~doc:"STRING ChecksumSHA256"
       and deleteMarker =
         flag "delete-marker" (optional bool) ~doc:"BOOL DeleteMarker"
       and eTag = flag "e-tag" (optional string) ~doc:"STRING ETag"
       and expires = flag "expires" (optional json_arg) ~doc:"JSON Expires"
       and expiration =
         flag "expiration" (optional string) ~doc:"STRING Expiration"
       and lastModified =
         flag "last-modified" (optional json_arg) ~doc:"JSON LastModified"
       and missingMeta =
         flag "missing-meta" (optional int) ~doc:"INT MissingMeta"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON Metadata"
       and objectLockMode =
         flag "object-lock-mode" (optional json_arg)
           ~doc:"JSON ObjectLockMode"
       and objectLockLegalHoldStatus =
         flag "object-lock-legal-hold-status" (optional json_arg)
           ~doc:"JSON ObjectLockLegalHoldStatus"
       and objectLockRetainUntilDate =
         flag "object-lock-retain-until-date" (optional json_arg)
           ~doc:"JSON ObjectLockRetainUntilDate"
       and partsCount =
         flag "parts-count" (optional int) ~doc:"INT PartsCount"
       and replicationStatus =
         flag "replication-status" (optional json_arg)
           ~doc:"JSON ReplicationStatus"
       and requestCharged =
         flag "request-charged" (optional json_arg)
           ~doc:"JSON RequestCharged"
       and restore = flag "restore" (optional string) ~doc:"STRING Restore"
       and serverSideEncryption =
         flag "server-side-encryption" (optional json_arg)
           ~doc:"JSON ServerSideEncryption"
       and sSECustomerAlgorithm =
         flag "s-s-e-customer-algorithm" (optional string)
           ~doc:"STRING SSECustomerAlgorithm"
       and sSEKMSKeyId =
         flag "s-s-e-k-m-s-key-id" (optional string)
           ~doc:"STRING SSEKMSKeyId"
       and sSECustomerKeyMD5 =
         flag "s-s-e-customer-key-m-d5" (optional string)
           ~doc:"STRING SSECustomerKeyMD5"
       and storageClass =
         flag "storage-class" (optional json_arg) ~doc:"JSON StorageClass"
       and tagCount = flag "tag-count" (optional int) ~doc:"INT TagCount"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING ObjectVersionId"
       and bucketKeyEnabled =
         flag "bucket-key-enabled" (optional bool)
           ~doc:"BOOL BucketKeyEnabled"
       and requestRoute =
         flag "request-route" (required string) ~doc:"STRING RequestRoute"
       and requestToken =
         flag "request-token" (required string) ~doc:"STRING RequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.write_get_object_response
           (Values.WriteGetObjectResponseRequest.make
              ?body:(Option.map ~f:Values.Body.of_json body) ?statusCode
              ?errorCode ?errorMessage ?acceptRanges ?cacheControl
              ?contentDisposition ?contentEncoding ?contentLanguage
              ?contentLength:(Option.map ~f:Values.ContentLength.of_json
                                contentLength) ?contentRange ?contentType
              ?checksumCRC32 ?checksumCRC32C ?checksumSHA1 ?checksumSHA256
              ?deleteMarker ?eTag
              ?expires:(Option.map ~f:Values.Expires.of_json expires)
              ?expiration
              ?lastModified:(Option.map ~f:Values.LastModified.of_json
                               lastModified) ?missingMeta
              ?metadata:(Option.map ~f:Values.Metadata.of_json metadata)
              ?objectLockMode:(Option.map ~f:Values.ObjectLockMode.of_json
                                 objectLockMode)
              ?objectLockLegalHoldStatus:(Option.map
                                            ~f:Values.ObjectLockLegalHoldStatus.of_json
                                            objectLockLegalHoldStatus)
              ?objectLockRetainUntilDate:(Option.map
                                            ~f:Values.ObjectLockRetainUntilDate.of_json
                                            objectLockRetainUntilDate)
              ?partsCount
              ?replicationStatus:(Option.map
                                    ~f:Values.ReplicationStatus.of_json
                                    replicationStatus)
              ?requestCharged:(Option.map ~f:Values.RequestCharged.of_json
                                 requestCharged) ?restore
              ?serverSideEncryption:(Option.map
                                       ~f:Values.ServerSideEncryption.of_json
                                       serverSideEncryption)
              ?sSECustomerAlgorithm ?sSEKMSKeyId ?sSECustomerKeyMD5
              ?storageClass:(Option.map ~f:Values.StorageClass.of_json
                               storageClass) ?tagCount ?versionId
              ?bucketKeyEnabled ~requestRoute ~requestToken ()) None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("abort-multipart-upload", abort_multipart_upload);
    ("complete-multipart-upload", complete_multipart_upload);
    ("copy-object", copy_object);
    ("create-bucket", create_bucket);
    ("create-multipart-upload", create_multipart_upload);
    ("delete-bucket", delete_bucket);
    ("delete-bucket-analytics-configuration",
      delete_bucket_analytics_configuration);
    ("delete-bucket-cors", delete_bucket_cors);
    ("delete-bucket-encryption", delete_bucket_encryption);
    ("delete-bucket-intelligent-tiering-configuration",
      delete_bucket_intelligent_tiering_configuration);
    ("delete-bucket-inventory-configuration",
      delete_bucket_inventory_configuration);
    ("delete-bucket-lifecycle", delete_bucket_lifecycle);
    ("delete-bucket-metrics-configuration",
      delete_bucket_metrics_configuration);
    ("delete-bucket-ownership-controls", delete_bucket_ownership_controls);
    ("delete-bucket-policy", delete_bucket_policy);
    ("delete-bucket-replication", delete_bucket_replication);
    ("delete-bucket-tagging", delete_bucket_tagging);
    ("delete-bucket-website", delete_bucket_website);
    ("delete-object", delete_object);
    ("delete-object-tagging", delete_object_tagging);
    ("delete-objects", delete_objects);
    ("delete-public-access-block", delete_public_access_block);
    ("get-bucket-accelerate-configuration",
      get_bucket_accelerate_configuration);
    ("get-bucket-acl", get_bucket_acl);
    ("get-bucket-analytics-configuration",
      get_bucket_analytics_configuration);
    ("get-bucket-cors", get_bucket_cors);
    ("get-bucket-encryption", get_bucket_encryption);
    ("get-bucket-intelligent-tiering-configuration",
      get_bucket_intelligent_tiering_configuration);
    ("get-bucket-inventory-configuration",
      get_bucket_inventory_configuration);
    ("get-bucket-lifecycle", get_bucket_lifecycle);
    ("get-bucket-lifecycle-configuration",
      get_bucket_lifecycle_configuration);
    ("get-bucket-location", get_bucket_location);
    ("get-bucket-logging", get_bucket_logging);
    ("get-bucket-metrics-configuration", get_bucket_metrics_configuration);
    ("get-bucket-notification", get_bucket_notification);
    ("get-bucket-notification-configuration",
      get_bucket_notification_configuration);
    ("get-bucket-ownership-controls", get_bucket_ownership_controls);
    ("get-bucket-policy", get_bucket_policy);
    ("get-bucket-policy-status", get_bucket_policy_status);
    ("get-bucket-replication", get_bucket_replication);
    ("get-bucket-request-payment", get_bucket_request_payment);
    ("get-bucket-tagging", get_bucket_tagging);
    ("get-bucket-versioning", get_bucket_versioning);
    ("get-bucket-website", get_bucket_website);
    ("get-object", get_object);
    ("get-object-acl", get_object_acl);
    ("get-object-attributes", get_object_attributes);
    ("get-object-legal-hold", get_object_legal_hold);
    ("get-object-lock-configuration", get_object_lock_configuration);
    ("get-object-retention", get_object_retention);
    ("get-object-tagging", get_object_tagging);
    ("get-object-torrent", get_object_torrent);
    ("get-public-access-block", get_public_access_block);
    ("head-bucket", head_bucket);
    ("head-object", head_object);
    ("list-bucket-analytics-configurations",
      list_bucket_analytics_configurations);
    ("list-bucket-intelligent-tiering-configurations",
      list_bucket_intelligent_tiering_configurations);
    ("list-bucket-inventory-configurations",
      list_bucket_inventory_configurations);
    ("list-bucket-metrics-configurations",
      list_bucket_metrics_configurations);
    ("list-buckets", list_buckets);
    ("list-multipart-uploads", list_multipart_uploads);
    ("list-object-versions", list_object_versions);
    ("list-objects", list_objects);
    ("list-objects-v2", list_objects_v2);
    ("list-parts", list_parts);
    ("put-bucket-accelerate-configuration",
      put_bucket_accelerate_configuration);
    ("put-bucket-acl", put_bucket_acl);
    ("put-bucket-analytics-configuration",
      put_bucket_analytics_configuration);
    ("put-bucket-cors", put_bucket_cors);
    ("put-bucket-encryption", put_bucket_encryption);
    ("put-bucket-intelligent-tiering-configuration",
      put_bucket_intelligent_tiering_configuration);
    ("put-bucket-inventory-configuration",
      put_bucket_inventory_configuration);
    ("put-bucket-lifecycle", put_bucket_lifecycle);
    ("put-bucket-lifecycle-configuration",
      put_bucket_lifecycle_configuration);
    ("put-bucket-logging", put_bucket_logging);
    ("put-bucket-metrics-configuration", put_bucket_metrics_configuration);
    ("put-bucket-notification", put_bucket_notification);
    ("put-bucket-notification-configuration",
      put_bucket_notification_configuration);
    ("put-bucket-ownership-controls", put_bucket_ownership_controls);
    ("put-bucket-policy", put_bucket_policy);
    ("put-bucket-replication", put_bucket_replication);
    ("put-bucket-request-payment", put_bucket_request_payment);
    ("put-bucket-tagging", put_bucket_tagging);
    ("put-bucket-versioning", put_bucket_versioning);
    ("put-bucket-website", put_bucket_website);
    ("put-object", put_object);
    ("put-object-acl", put_object_acl);
    ("put-object-legal-hold", put_object_legal_hold);
    ("put-object-lock-configuration", put_object_lock_configuration);
    ("put-object-retention", put_object_retention);
    ("put-object-tagging", put_object_tagging);
    ("put-public-access-block", put_public_access_block);
    ("restore-object", restore_object);
    ("select-object-content", select_object_content);
    ("upload-part", upload_part);
    ("upload-part-copy", upload_part_copy);
    ("write-get-object-response", write_get_object_response)]
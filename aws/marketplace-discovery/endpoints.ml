(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | GetListing: (GetListingInput.t, GetListingOutput.t,
  GetListingOutput.error) t 
  | GetOffer: (GetOfferInput.t, GetOfferOutput.t, GetOfferOutput.error) t 
  | GetOfferSet: (GetOfferSetInput.t, GetOfferSetOutput.t,
  GetOfferSetOutput.error) t 
  | GetOfferTerms: (GetOfferTermsInput.t, GetOfferTermsOutput.t,
  GetOfferTermsOutput.error) t 
  | GetProduct: (GetProductInput.t, GetProductOutput.t,
  GetProductOutput.error) t 
  | ListFulfillmentOptions: (ListFulfillmentOptionsInput.t,
  ListFulfillmentOptionsOutput.t, ListFulfillmentOptionsOutput.error) t 
  | ListPurchaseOptions: (ListPurchaseOptionsInput.t,
  ListPurchaseOptionsOutput.t, ListPurchaseOptionsOutput.error) t 
  | SearchFacets: (SearchFacetsInput.t, SearchFacetsOutput.t,
  SearchFacetsOutput.error) t 
  | SearchListings: (SearchListingsInput.t, SearchListingsOutput.t,
  SearchListingsOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | GetListing -> `POST
  | GetOffer -> `POST
  | GetOfferSet -> `POST
  | GetOfferTerms -> `POST
  | GetProduct -> `POST
  | ListFulfillmentOptions -> `POST
  | ListPurchaseOptions -> `POST
  | SearchFacets -> `POST
  | SearchListings -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | GetListing ->
          (Format.kasprintf Uri.of_string) "/2026-02-05/getListing"
      | GetOffer -> (Format.kasprintf Uri.of_string) "/2026-02-05/getOffer"
      | GetOfferSet ->
          (Format.kasprintf Uri.of_string) "/2026-02-05/getOfferSet"
      | GetOfferTerms ->
          (Format.kasprintf Uri.of_string) "/2026-02-05/getOfferTerms"
      | GetProduct ->
          (Format.kasprintf Uri.of_string) "/2026-02-05/getProduct"
      | ListFulfillmentOptions ->
          (Format.kasprintf Uri.of_string)
            "/2026-02-05/listFulfillmentOptions"
      | ListPurchaseOptions ->
          (Format.kasprintf Uri.of_string) "/2026-02-05/listPurchaseOptions"
      | SearchFacets ->
          (Format.kasprintf Uri.of_string) "/2026-02-05/searchFacets"
      | SearchListings ->
          (Format.kasprintf Uri.of_string) "/2026-02-05/searchListings")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | GetListing ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("listingId",
                           (ListingId.to_value req.GetListingInput.listingId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetOffer ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("offerId",
                           (OfferId.to_value req.GetOfferInput.offerId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetOfferSet ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("offerSetId",
                           (OfferSetId.to_value
                              req.GetOfferSetInput.offerSetId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetOfferTerms ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("offerId",
                           (OfferId.to_value req.GetOfferTermsInput.offerId));
                      Option.map req.GetOfferTermsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (GetOfferTermsInputMaxResultsInteger.to_value
                                   x)));
                      Option.map req.GetOfferTermsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetProduct ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("productId",
                           (ProductId.to_value req.GetProductInput.productId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListFulfillmentOptions ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("productId",
                           (ProductId.to_value
                              req.ListFulfillmentOptionsInput.productId));
                      Option.map req.ListFulfillmentOptionsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListFulfillmentOptionsInputMaxResultsInteger.to_value
                                   x)));
                      Option.map req.ListFulfillmentOptionsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListPurchaseOptions ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListPurchaseOptionsInput.filters
                         ~f:(fun x ->
                               ("filters",
                                 (PurchaseOptionFilterList.to_value x)));
                      Option.map req.ListPurchaseOptionsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListPurchaseOptionsInputMaxResultsInteger.to_value
                                   x)));
                      Option.map req.ListPurchaseOptionsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SearchFacets ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.SearchFacetsInput.searchText
                         ~f:(fun x -> ("searchText", (SearchText.to_value x)));
                      Option.map req.SearchFacetsInput.filters
                        ~f:(fun x ->
                              ("filters", (SearchFilterList.to_value x)));
                      Option.map req.SearchFacetsInput.facetTypes
                        ~f:(fun x ->
                              ("facetTypes", (FacetTypeList.to_value x)));
                      Option.map req.SearchFacetsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SearchListings ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.SearchListingsInput.searchText
                         ~f:(fun x -> ("searchText", (SearchText.to_value x)));
                      Option.map req.SearchListingsInput.filters
                        ~f:(fun x ->
                              ("filters", (SearchFilterList.to_value x)));
                      Option.map req.SearchListingsInput.maxResults
                        ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.SearchListingsInput.sortBy
                        ~f:(fun x ->
                              ("sortBy", (SearchListingsSortBy.to_value x)));
                      Option.map req.SearchListingsInput.sortOrder
                        ~f:(fun x ->
                              ("sortOrder",
                                (SearchListingsSortOrder.to_value x)));
                      Option.map req.SearchListingsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | GetListing ->
      if is_success
      then Ok (GetListingOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetListingOutput.error_of_json))
  | GetOffer ->
      if is_success
      then Ok (GetOfferOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetOfferOutput.error_of_json))
  | GetOfferSet ->
      if is_success
      then Ok (GetOfferSetOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetOfferSetOutput.error_of_json))
  | GetOfferTerms ->
      if is_success
      then Ok (GetOfferTermsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetOfferTermsOutput.error_of_json))
  | GetProduct ->
      if is_success
      then Ok (GetProductOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetProductOutput.error_of_json))
  | ListFulfillmentOptions ->
      if is_success
      then Ok (ListFulfillmentOptionsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListFulfillmentOptionsOutput.error_of_json))
  | ListPurchaseOptions ->
      if is_success
      then Ok (ListPurchaseOptionsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListPurchaseOptionsOutput.error_of_json))
  | SearchFacets ->
      if is_success
      then Ok (SearchFacetsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SearchFacetsOutput.error_of_json))
  | SearchListings ->
      if is_success
      then Ok (SearchListingsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SearchListingsOutput.error_of_json))
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let get_listing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listingId =
         flag "listing-id" (required string) ~doc:"STRING ListingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_listing (Values.GetListingInput.make ~listingId ())
           (Some Values.GetListingOutput.to_json)
           (Some Values.GetListingOutput.error_to_json)])
let get_offer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and offerId = flag "offer-id" (required string) ~doc:"STRING OfferId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_offer (Values.GetOfferInput.make ~offerId ())
           (Some Values.GetOfferOutput.to_json)
           (Some Values.GetOfferOutput.error_to_json)])
let get_offer_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and offerSetId =
         flag "offer-set-id" (required string) ~doc:"STRING OfferSetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_offer_set (Values.GetOfferSetInput.make ~offerSetId ())
           (Some Values.GetOfferSetOutput.to_json)
           (Some Values.GetOfferSetOutput.error_to_json)])
let get_offer_terms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT GetOfferTermsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and offerId = flag "offer-id" (required string) ~doc:"STRING OfferId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_offer_terms
           (Values.GetOfferTermsInput.make ?maxResults ?nextToken ~offerId ())
           (Some Values.GetOfferTermsOutput.to_json)
           (Some Values.GetOfferTermsOutput.error_to_json)])
let get_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and productId =
         flag "product-id" (required string) ~doc:"STRING ProductId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_product (Values.GetProductInput.make ~productId ())
           (Some Values.GetProductOutput.to_json)
           (Some Values.GetProductOutput.error_to_json)])
let list_fulfillment_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListFulfillmentOptionsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and productId =
         flag "product-id" (required string) ~doc:"STRING ProductId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_fulfillment_options
           (Values.ListFulfillmentOptionsInput.make ?maxResults ?nextToken
              ~productId ())
           (Some Values.ListFulfillmentOptionsOutput.to_json)
           (Some Values.ListFulfillmentOptionsOutput.error_to_json)])
let list_purchase_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON PurchaseOptionFilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListPurchaseOptionsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_purchase_options
           (Values.ListPurchaseOptionsInput.make
              ?filters:(Option.map ~f:Values.PurchaseOptionFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.ListPurchaseOptionsOutput.to_json)
           (Some Values.ListPurchaseOptionsOutput.error_to_json)])
let search_facets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchText =
         flag "search-text" (optional string) ~doc:"STRING SearchText"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SearchFilterList"
       and facetTypes =
         flag "facet-types" (optional json_arg) ~doc:"JSON FacetTypeList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_facets
           (Values.SearchFacetsInput.make ?searchText
              ?filters:(Option.map ~f:Values.SearchFilterList.of_json filters)
              ?facetTypes:(Option.map ~f:Values.FacetTypeList.of_json
                             facetTypes) ?nextToken ())
           (Some Values.SearchFacetsOutput.to_json)
           (Some Values.SearchFacetsOutput.error_to_json)])
let search_listings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchText =
         flag "search-text" (optional string) ~doc:"STRING SearchText"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SearchFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SearchListingsSortBy"
       and sortOrder =
         flag "sort-order" (optional json_arg)
           ~doc:"JSON SearchListingsSortOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_listings
           (Values.SearchListingsInput.make ?searchText
              ?filters:(Option.map ~f:Values.SearchFilterList.of_json filters)
              ?maxResults
              ?sortBy:(Option.map ~f:Values.SearchListingsSortBy.of_json
                         sortBy)
              ?sortOrder:(Option.map
                            ~f:Values.SearchListingsSortOrder.of_json
                            sortOrder) ?nextToken ())
           (Some Values.SearchListingsOutput.to_json)
           (Some Values.SearchListingsOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("get-listing", get_listing);
    ("get-offer", get_offer);
    ("get-offer-set", get_offer_set);
    ("get-offer-terms", get_offer_terms);
    ("get-product", get_product);
    ("list-fulfillment-options", list_fulfillment_options);
    ("list-purchase-options", list_purchase_options);
    ("search-facets", search_facets);
    ("search-listings", search_listings)]
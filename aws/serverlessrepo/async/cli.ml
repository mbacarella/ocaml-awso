(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and homePageUrl =
         flag "home-page-url" (optional string) ~doc:"STRING __string"
       and labels =
         flag "labels" (optional json_arg) ~doc:"JSON __listOf__string"
       and licenseBody =
         flag "license-body" (optional string) ~doc:"STRING __string"
       and licenseUrl =
         flag "license-url" (optional string) ~doc:"STRING __string"
       and readmeBody =
         flag "readme-body" (optional string) ~doc:"STRING __string"
       and readmeUrl =
         flag "readme-url" (optional string) ~doc:"STRING __string"
       and semanticVersion =
         flag "semantic-version" (optional string) ~doc:"STRING __string"
       and sourceCodeArchiveUrl =
         flag "source-code-archive-url" (optional string)
           ~doc:"STRING __string"
       and sourceCodeUrl =
         flag "source-code-url" (optional string) ~doc:"STRING __string"
       and spdxLicenseId =
         flag "spdx-license-id" (optional string) ~doc:"STRING __string"
       and templateBody =
         flag "template-body" (optional string) ~doc:"STRING __string"
       and templateUrl =
         flag "template-url" (optional string) ~doc:"STRING __string"
       and author = flag "author" (required string) ~doc:"STRING __string"
       and description =
         flag "description" (required string) ~doc:"STRING __string"
       and name = flag "name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application
           (Values.CreateApplicationRequest.make ?homePageUrl
              ?labels:(Option.map ~f:Values.Zz__listOf__string.of_json labels)
              ?licenseBody ?licenseUrl ?readmeBody ?readmeUrl
              ?semanticVersion ?sourceCodeArchiveUrl ?sourceCodeUrl
              ?spdxLicenseId ?templateBody ?templateUrl ~author ~description
              ~name ()) (Some Values.CreateApplicationResponse.to_json)
           (Some Values.CreateApplicationResponse.error_to_json)])
let create_application_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceCodeArchiveUrl =
         flag "source-code-archive-url" (optional string)
           ~doc:"STRING __string"
       and sourceCodeUrl =
         flag "source-code-url" (optional string) ~doc:"STRING __string"
       and templateBody =
         flag "template-body" (optional string) ~doc:"STRING __string"
       and templateUrl =
         flag "template-url" (optional string) ~doc:"STRING __string"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING __string"
       and semanticVersion =
         flag "semantic-version" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application_version
           (Values.CreateApplicationVersionRequest.make ?sourceCodeArchiveUrl
              ?sourceCodeUrl ?templateBody ?templateUrl ~applicationId
              ~semanticVersion ())
           (Some Values.CreateApplicationVersionResponse.to_json)
           (Some Values.CreateApplicationVersionResponse.error_to_json)])
let create_cloud_formation_change_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capabilities =
         flag "capabilities" (optional json_arg) ~doc:"JSON __listOf__string"
       and changeSetName =
         flag "change-set-name" (optional string) ~doc:"STRING __string"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING __string"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and notificationArns =
         flag "notification-arns" (optional json_arg)
           ~doc:"JSON __listOf__string"
       and parameterOverrides =
         flag "parameter-overrides" (optional json_arg)
           ~doc:"JSON __listOfParameterValue"
       and resourceTypes =
         flag "resource-types" (optional json_arg)
           ~doc:"JSON __listOf__string"
       and rollbackConfiguration =
         flag "rollback-configuration" (optional json_arg)
           ~doc:"JSON RollbackConfiguration"
       and semanticVersion =
         flag "semantic-version" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON __listOfTag"
       and templateId =
         flag "template-id" (optional string) ~doc:"STRING __string"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING __string"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cloud_formation_change_set
           (Values.CreateCloudFormationChangeSetRequest.make
              ?capabilities:(Option.map ~f:Values.Zz__listOf__string.of_json
                               capabilities) ?changeSetName ?clientToken
              ?description
              ?notificationArns:(Option.map
                                   ~f:Values.Zz__listOf__string.of_json
                                   notificationArns)
              ?parameterOverrides:(Option.map
                                     ~f:Values.Zz__listOfParameterValue.of_json
                                     parameterOverrides)
              ?resourceTypes:(Option.map ~f:Values.Zz__listOf__string.of_json
                                resourceTypes)
              ?rollbackConfiguration:(Option.map
                                        ~f:Values.RollbackConfiguration.of_json
                                        rollbackConfiguration)
              ?semanticVersion
              ?tags:(Option.map ~f:Values.Zz__listOfTag.of_json tags)
              ?templateId ~applicationId ~stackName ())
           (Some Values.CreateCloudFormationChangeSetResponse.to_json)
           (Some Values.CreateCloudFormationChangeSetResponse.error_to_json)])
let create_cloud_formation_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and semanticVersion =
         flag "semantic-version" (optional string) ~doc:"STRING __string"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cloud_formation_template
           (Values.CreateCloudFormationTemplateRequest.make ?semanticVersion
              ~applicationId ())
           (Some Values.CreateCloudFormationTemplateResponse.to_json)
           (Some Values.CreateCloudFormationTemplateResponse.error_to_json)])
let delete_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_application
           (Values.DeleteApplicationRequest.make ~applicationId ()) None None])
let get_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and semanticVersion =
         flag "semantic-version" (optional string) ~doc:"STRING __string"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_application
           (Values.GetApplicationRequest.make ?semanticVersion ~applicationId
              ()) (Some Values.GetApplicationResponse.to_json)
           (Some Values.GetApplicationResponse.error_to_json)])
let get_application_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_application_policy
           (Values.GetApplicationPolicyRequest.make ~applicationId ())
           (Some Values.GetApplicationPolicyResponse.to_json)
           (Some Values.GetApplicationPolicyResponse.error_to_json)])
let get_cloud_formation_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING __string"
       and templateId =
         flag "template-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cloud_formation_template
           (Values.GetCloudFormationTemplateRequest.make ~applicationId
              ~templateId ())
           (Some Values.GetCloudFormationTemplateResponse.to_json)
           (Some Values.GetCloudFormationTemplateResponse.error_to_json)])
let list_application_dependencies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxItems"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and semanticVersion =
         flag "semantic-version" (optional string) ~doc:"STRING __string"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_application_dependencies
           (Values.ListApplicationDependenciesRequest.make ?maxItems
              ?nextToken ?semanticVersion ~applicationId ())
           (Some Values.ListApplicationDependenciesResponse.to_json)
           (Some Values.ListApplicationDependenciesResponse.error_to_json)])
let list_application_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxItems"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_application_versions
           (Values.ListApplicationVersionsRequest.make ?maxItems ?nextToken
              ~applicationId ())
           (Some Values.ListApplicationVersionsResponse.to_json)
           (Some Values.ListApplicationVersionsResponse.error_to_json)])
let list_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxItems"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_applications
           (Values.ListApplicationsRequest.make ?maxItems ?nextToken ())
           (Some Values.ListApplicationsResponse.to_json)
           (Some Values.ListApplicationsResponse.error_to_json)])
let put_application_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING __string"
       and statements =
         flag "statements" (required json_arg)
           ~doc:"JSON __listOfApplicationPolicyStatement" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_application_policy
           (Values.PutApplicationPolicyRequest.make ~applicationId
              ~statements:(Values.Zz__listOfApplicationPolicyStatement.of_json
                             statements) ())
           (Some Values.PutApplicationPolicyResponse.to_json)
           (Some Values.PutApplicationPolicyResponse.error_to_json)])
let unshare_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING __string"
       and organizationId =
         flag "organization-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.unshare_application
           (Values.UnshareApplicationRequest.make ~applicationId
              ~organizationId ()) None None])
let update_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and author = flag "author" (optional string) ~doc:"STRING __string"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and homePageUrl =
         flag "home-page-url" (optional string) ~doc:"STRING __string"
       and labels =
         flag "labels" (optional json_arg) ~doc:"JSON __listOf__string"
       and readmeBody =
         flag "readme-body" (optional string) ~doc:"STRING __string"
       and readmeUrl =
         flag "readme-url" (optional string) ~doc:"STRING __string"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application
           (Values.UpdateApplicationRequest.make ?author ?description
              ?homePageUrl
              ?labels:(Option.map ~f:Values.Zz__listOf__string.of_json labels)
              ?readmeBody ?readmeUrl ~applicationId ())
           (Some Values.UpdateApplicationResponse.to_json)
           (Some Values.UpdateApplicationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-application", create_application);
    ("create-application-version", create_application_version);
    ("create-cloud-formation-change-set", create_cloud_formation_change_set);
    ("create-cloud-formation-template", create_cloud_formation_template);
    ("delete-application", delete_application);
    ("get-application", get_application);
    ("get-application-policy", get_application_policy);
    ("get-cloud-formation-template", get_cloud_formation_template);
    ("list-application-dependencies", list_application_dependencies);
    ("list-application-versions", list_application_versions);
    ("list-applications", list_applications);
    ("put-application-policy", put_application_policy);
    ("unshare-application", unshare_application);
    ("update-application", update_application)]
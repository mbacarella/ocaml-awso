(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let bulk_publish =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.bulk_publish
           (Values.BulkPublishRequest.make ~identityPoolId ())
           (Some Values.BulkPublishResponse.to_json)
           (Some Values.BulkPublishResponse.error_to_json)])
let delete_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dataset
           (Values.DeleteDatasetRequest.make ~identityPoolId ~identityId
              ~datasetName ()) (Some Values.DeleteDatasetResponse.to_json)
           (Some Values.DeleteDatasetResponse.error_to_json)])
let describe_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dataset
           (Values.DescribeDatasetRequest.make ~identityPoolId ~identityId
              ~datasetName ()) (Some Values.DescribeDatasetResponse.to_json)
           (Some Values.DescribeDatasetResponse.error_to_json)])
let describe_identity_pool_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_identity_pool_usage
           (Values.DescribeIdentityPoolUsageRequest.make ~identityPoolId ())
           (Some Values.DescribeIdentityPoolUsageResponse.to_json)
           (Some Values.DescribeIdentityPoolUsageResponse.error_to_json)])
let describe_identity_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_identity_usage
           (Values.DescribeIdentityUsageRequest.make ~identityPoolId
              ~identityId ())
           (Some Values.DescribeIdentityUsageResponse.to_json)
           (Some Values.DescribeIdentityUsageResponse.error_to_json)])
let get_bulk_publish_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bulk_publish_details
           (Values.GetBulkPublishDetailsRequest.make ~identityPoolId ())
           (Some Values.GetBulkPublishDetailsResponse.to_json)
           (Some Values.GetBulkPublishDetailsResponse.error_to_json)])
let get_cognito_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cognito_events
           (Values.GetCognitoEventsRequest.make ~identityPoolId ())
           (Some Values.GetCognitoEventsResponse.to_json)
           (Some Values.GetCognitoEventsResponse.error_to_json)])
let get_identity_pool_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_identity_pool_configuration
           (Values.GetIdentityPoolConfigurationRequest.make ~identityPoolId
              ()) (Some Values.GetIdentityPoolConfigurationResponse.to_json)
           (Some Values.GetIdentityPoolConfigurationResponse.error_to_json)])
let list_datasets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IntegerString"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_datasets
           (Values.ListDatasetsRequest.make ?nextToken ?maxResults
              ~identityPoolId ~identityId ())
           (Some Values.ListDatasetsResponse.to_json)
           (Some Values.ListDatasetsResponse.error_to_json)])
let list_identity_pool_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IntegerString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_identity_pool_usage
           (Values.ListIdentityPoolUsageRequest.make ?nextToken ?maxResults
              ()) (Some Values.ListIdentityPoolUsageResponse.to_json)
           (Some Values.ListIdentityPoolUsageResponse.error_to_json)])
let list_records =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lastSyncCount =
         flag "last-sync-count" (optional json_arg) ~doc:"JSON Long"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IntegerString"
       and syncSessionToken =
         flag "sync-session-token" (optional string)
           ~doc:"STRING SyncSessionToken"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_records
           (Values.ListRecordsRequest.make
              ?lastSyncCount:(Option.map ~f:Values.Long.of_json lastSyncCount)
              ?nextToken ?maxResults ?syncSessionToken ~identityPoolId
              ~identityId ~datasetName ())
           (Some Values.ListRecordsResponse.to_json)
           (Some Values.ListRecordsResponse.error_to_json)])
let register_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId"
       and platform =
         flag "platform" (required json_arg) ~doc:"JSON Platform"
       and token = flag "token" (required string) ~doc:"STRING PushToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_device
           (Values.RegisterDeviceRequest.make ~identityPoolId ~identityId
              ~platform:(Values.Platform.of_json platform) ~token ())
           (Some Values.RegisterDeviceResponse.to_json)
           (Some Values.RegisterDeviceResponse.error_to_json)])
let set_cognito_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and events = flag "events" (required json_arg) ~doc:"JSON Events" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_cognito_events
           (Values.SetCognitoEventsRequest.make ~identityPoolId
              ~events:(Values.Events.of_json events) ()) None None])
let set_identity_pool_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pushSync =
         flag "push-sync" (optional json_arg) ~doc:"JSON PushSync"
       and cognitoStreams =
         flag "cognito-streams" (optional json_arg)
           ~doc:"JSON CognitoStreams"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_identity_pool_configuration
           (Values.SetIdentityPoolConfigurationRequest.make
              ?pushSync:(Option.map ~f:Values.PushSync.of_json pushSync)
              ?cognitoStreams:(Option.map ~f:Values.CognitoStreams.of_json
                                 cognitoStreams) ~identityPoolId ())
           (Some Values.SetIdentityPoolConfigurationResponse.to_json)
           (Some Values.SetIdentityPoolConfigurationResponse.error_to_json)])
let subscribe_to_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.subscribe_to_dataset
           (Values.SubscribeToDatasetRequest.make ~identityPoolId ~identityId
              ~datasetName ~deviceId ())
           (Some Values.SubscribeToDatasetResponse.to_json)
           (Some Values.SubscribeToDatasetResponse.error_to_json)])
let unsubscribe_from_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.unsubscribe_from_dataset
           (Values.UnsubscribeFromDatasetRequest.make ~identityPoolId
              ~identityId ~datasetName ~deviceId ())
           (Some Values.UnsubscribeFromDatasetResponse.to_json)
           (Some Values.UnsubscribeFromDatasetResponse.error_to_json)])
let update_records =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceId =
         flag "device-id" (optional string) ~doc:"STRING DeviceId"
       and recordPatches =
         flag "record-patches" (optional json_arg)
           ~doc:"JSON RecordPatchList"
       and clientContext =
         flag "client-context" (optional string) ~doc:"STRING ClientContext"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName"
       and syncSessionToken =
         flag "sync-session-token" (required string)
           ~doc:"STRING SyncSessionToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_records
           (Values.UpdateRecordsRequest.make ?deviceId
              ?recordPatches:(Option.map ~f:Values.RecordPatchList.of_json
                                recordPatches) ?clientContext ~identityPoolId
              ~identityId ~datasetName ~syncSessionToken ())
           (Some Values.UpdateRecordsResponse.to_json)
           (Some Values.UpdateRecordsResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("bulk-publish", bulk_publish);
    ("delete-dataset", delete_dataset);
    ("describe-dataset", describe_dataset);
    ("describe-identity-pool-usage", describe_identity_pool_usage);
    ("describe-identity-usage", describe_identity_usage);
    ("get-bulk-publish-details", get_bulk_publish_details);
    ("get-cognito-events", get_cognito_events);
    ("get-identity-pool-configuration", get_identity_pool_configuration);
    ("list-datasets", list_datasets);
    ("list-identity-pool-usage", list_identity_pool_usage);
    ("list-records", list_records);
    ("register-device", register_device);
    ("set-cognito-events", set_cognito_events);
    ("set-identity-pool-configuration", set_identity_pool_configuration);
    ("subscribe-to-dataset", subscribe_to_dataset);
    ("unsubscribe-from-dataset", unsubscribe_from_dataset);
    ("update-records", update_records)]
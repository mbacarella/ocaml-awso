(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_api_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Boolean"
       and generateDistinctId =
         flag "generate-distinct-id" (optional bool) ~doc:"BOOL Boolean"
       and value = flag "value" (optional string) ~doc:"STRING String"
       and stageKeys =
         flag "stage-keys" (optional json_arg) ~doc:"JSON ListOfStageKeys"
       and customerId =
         flag "customer-id" (optional string) ~doc:"STRING String"
       and tags =
         flag "tags" (optional json_arg) ~doc:"JSON MapOfStringToString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_api_key
           (Values.CreateApiKeyRequest.make ?name ?description ?enabled
              ?generateDistinctId ?value
              ?stageKeys:(Option.map ~f:Values.ListOfStageKeys.of_json
                            stageKeys) ?customerId
              ?tags:(Option.map ~f:Values.MapOfStringToString.of_json tags)
              ()) (Some Values.ApiKey.to_json)
           (Some Values.ApiKey.error_to_json)])
let create_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and providerARNs =
         flag "provider-a-r-ns" (optional json_arg) ~doc:"JSON ListOfARNs"
       and authType = flag "auth-type" (optional string) ~doc:"STRING String"
       and authorizerUri =
         flag "authorizer-uri" (optional string) ~doc:"STRING String"
       and authorizerCredentials =
         flag "authorizer-credentials" (optional string) ~doc:"STRING String"
       and identitySource =
         flag "identity-source" (optional string) ~doc:"STRING String"
       and identityValidationExpression =
         flag "identity-validation-expression" (optional string)
           ~doc:"STRING String"
       and authorizerResultTtlInSeconds =
         flag "authorizer-result-ttl-in-seconds" (optional int)
           ~doc:"INT NullableInteger"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and name = flag "name" (required string) ~doc:"STRING String"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON AuthorizerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_authorizer
           (Values.CreateAuthorizerRequest.make
              ?providerARNs:(Option.map ~f:Values.ListOfARNs.of_json
                               providerARNs) ?authType ?authorizerUri
              ?authorizerCredentials ?identitySource
              ?identityValidationExpression ?authorizerResultTtlInSeconds
              ~restApiId ~name ~type_:(Values.AuthorizerType.of_json type_)
              ()) (Some Values.Authorizer.to_json)
           (Some Values.Authorizer.error_to_json)])
let create_base_path_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameId =
         flag "domain-name-id" (optional string) ~doc:"STRING String"
       and basePath = flag "base-path" (optional string) ~doc:"STRING String"
       and stage = flag "stage" (optional string) ~doc:"STRING String"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_base_path_mapping
           (Values.CreateBasePathMappingRequest.make ?domainNameId ?basePath
              ?stage ~domainName ~restApiId ())
           (Some Values.BasePathMapping.to_json)
           (Some Values.BasePathMapping.error_to_json)])
let create_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stageName =
         flag "stage-name" (optional string) ~doc:"STRING String"
       and stageDescription =
         flag "stage-description" (optional string) ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and cacheClusterEnabled =
         flag "cache-cluster-enabled" (optional bool)
           ~doc:"BOOL NullableBoolean"
       and cacheClusterSize =
         flag "cache-cluster-size" (optional json_arg)
           ~doc:"JSON CacheClusterSize"
       and variables =
         flag "variables" (optional json_arg) ~doc:"JSON MapOfStringToString"
       and canarySettings =
         flag "canary-settings" (optional json_arg)
           ~doc:"JSON DeploymentCanarySettings"
       and tracingEnabled =
         flag "tracing-enabled" (optional bool) ~doc:"BOOL NullableBoolean"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_deployment
           (Values.CreateDeploymentRequest.make ?stageName ?stageDescription
              ?description ?cacheClusterEnabled
              ?cacheClusterSize:(Option.map
                                   ~f:Values.CacheClusterSize.of_json
                                   cacheClusterSize)
              ?variables:(Option.map ~f:Values.MapOfStringToString.of_json
                            variables)
              ?canarySettings:(Option.map
                                 ~f:Values.DeploymentCanarySettings.of_json
                                 canarySettings) ?tracingEnabled ~restApiId
              ()) (Some Values.Deployment.to_json)
           (Some Values.Deployment.error_to_json)])
let create_documentation_part =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and location =
         flag "location" (required json_arg)
           ~doc:"JSON DocumentationPartLocation"
       and properties =
         flag "properties" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_documentation_part
           (Values.CreateDocumentationPartRequest.make ~restApiId
              ~location:(Values.DocumentationPartLocation.of_json location)
              ~properties ()) (Some Values.DocumentationPart.to_json)
           (Some Values.DocumentationPart.error_to_json)])
let create_documentation_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stageName =
         flag "stage-name" (optional string) ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and documentationVersion =
         flag "documentation-version" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_documentation_version
           (Values.CreateDocumentationVersionRequest.make ?stageName
              ?description ~restApiId ~documentationVersion ())
           (Some Values.DocumentationVersion.to_json)
           (Some Values.DocumentationVersion.error_to_json)])
let create_domain_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateName =
         flag "certificate-name" (optional string) ~doc:"STRING String"
       and certificateBody =
         flag "certificate-body" (optional string) ~doc:"STRING String"
       and certificatePrivateKey =
         flag "certificate-private-key" (optional string)
           ~doc:"STRING String"
       and certificateChain =
         flag "certificate-chain" (optional string) ~doc:"STRING String"
       and certificateArn =
         flag "certificate-arn" (optional string) ~doc:"STRING String"
       and regionalCertificateName =
         flag "regional-certificate-name" (optional string)
           ~doc:"STRING String"
       and regionalCertificateArn =
         flag "regional-certificate-arn" (optional string)
           ~doc:"STRING String"
       and endpointConfiguration =
         flag "endpoint-configuration" (optional json_arg)
           ~doc:"JSON EndpointConfiguration"
       and tags =
         flag "tags" (optional json_arg) ~doc:"JSON MapOfStringToString"
       and securityPolicy =
         flag "security-policy" (optional json_arg)
           ~doc:"JSON SecurityPolicy"
       and endpointAccessMode =
         flag "endpoint-access-mode" (optional json_arg)
           ~doc:"JSON EndpointAccessMode"
       and mutualTlsAuthentication =
         flag "mutual-tls-authentication" (optional json_arg)
           ~doc:"JSON MutualTlsAuthenticationInput"
       and ownershipVerificationCertificateArn =
         flag "ownership-verification-certificate-arn" (optional string)
           ~doc:"STRING String"
       and policy = flag "policy" (optional string) ~doc:"STRING String"
       and routingMode =
         flag "routing-mode" (optional json_arg) ~doc:"JSON RoutingMode"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain_name
           (Values.CreateDomainNameRequest.make ?certificateName
              ?certificateBody ?certificatePrivateKey ?certificateChain
              ?certificateArn ?regionalCertificateName
              ?regionalCertificateArn
              ?endpointConfiguration:(Option.map
                                        ~f:Values.EndpointConfiguration.of_json
                                        endpointConfiguration)
              ?tags:(Option.map ~f:Values.MapOfStringToString.of_json tags)
              ?securityPolicy:(Option.map ~f:Values.SecurityPolicy.of_json
                                 securityPolicy)
              ?endpointAccessMode:(Option.map
                                     ~f:Values.EndpointAccessMode.of_json
                                     endpointAccessMode)
              ?mutualTlsAuthentication:(Option.map
                                          ~f:Values.MutualTlsAuthenticationInput.of_json
                                          mutualTlsAuthentication)
              ?ownershipVerificationCertificateArn ?policy
              ?routingMode:(Option.map ~f:Values.RoutingMode.of_json
                              routingMode) ~domainName ())
           (Some Values.DomainName.to_json)
           (Some Values.DomainName.error_to_json)])
let create_domain_name_access_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags =
         flag "tags" (optional json_arg) ~doc:"JSON MapOfStringToString"
       and domainNameArn =
         flag "domain-name-arn" (required string) ~doc:"STRING String"
       and accessAssociationSourceType =
         flag "access-association-source-type" (required json_arg)
           ~doc:"JSON AccessAssociationSourceType"
       and accessAssociationSource =
         flag "access-association-source" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain_name_access_association
           (Values.CreateDomainNameAccessAssociationRequest.make
              ?tags:(Option.map ~f:Values.MapOfStringToString.of_json tags)
              ~domainNameArn
              ~accessAssociationSourceType:(Values.AccessAssociationSourceType.of_json
                                              accessAssociationSourceType)
              ~accessAssociationSource ())
           (Some Values.DomainNameAccessAssociation.to_json)
           (Some Values.DomainNameAccessAssociation.error_to_json)])
let create_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and schema = flag "schema" (optional string) ~doc:"STRING String"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and name = flag "name" (required string) ~doc:"STRING String"
       and contentType =
         flag "content-type" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model
           (Values.CreateModelRequest.make ?description ?schema ~restApiId
              ~name ~contentType ()) (Some Values.Model.to_json)
           (Some Values.Model.error_to_json)])
let create_request_validator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and validateRequestBody =
         flag "validate-request-body" (optional bool) ~doc:"BOOL Boolean"
       and validateRequestParameters =
         flag "validate-request-parameters" (optional bool)
           ~doc:"BOOL Boolean"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_request_validator
           (Values.CreateRequestValidatorRequest.make ?name
              ?validateRequestBody ?validateRequestParameters ~restApiId ())
           (Some Values.RequestValidator.to_json)
           (Some Values.RequestValidator.error_to_json)])
let create_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and parentId = flag "parent-id" (required string) ~doc:"STRING String"
       and pathPart = flag "path-part" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource
           (Values.CreateResourceRequest.make ~restApiId ~parentId ~pathPart
              ()) (Some Values.Resource.to_json)
           (Some Values.Resource.error_to_json)])
let create_rest_api =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and version = flag "version" (optional string) ~doc:"STRING String"
       and cloneFrom =
         flag "clone-from" (optional string) ~doc:"STRING String"
       and binaryMediaTypes =
         flag "binary-media-types" (optional json_arg)
           ~doc:"JSON ListOfString"
       and minimumCompressionSize =
         flag "minimum-compression-size" (optional int)
           ~doc:"INT NullableInteger"
       and apiKeySource =
         flag "api-key-source" (optional json_arg)
           ~doc:"JSON ApiKeySourceType"
       and endpointConfiguration =
         flag "endpoint-configuration" (optional json_arg)
           ~doc:"JSON EndpointConfiguration"
       and policy = flag "policy" (optional string) ~doc:"STRING String"
       and tags =
         flag "tags" (optional json_arg) ~doc:"JSON MapOfStringToString"
       and disableExecuteApiEndpoint =
         flag "disable-execute-api-endpoint" (optional bool)
           ~doc:"BOOL Boolean"
       and securityPolicy =
         flag "security-policy" (optional json_arg)
           ~doc:"JSON SecurityPolicy"
       and endpointAccessMode =
         flag "endpoint-access-mode" (optional json_arg)
           ~doc:"JSON EndpointAccessMode"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_rest_api
           (Values.CreateRestApiRequest.make ?description ?version ?cloneFrom
              ?binaryMediaTypes:(Option.map ~f:Values.ListOfString.of_json
                                   binaryMediaTypes) ?minimumCompressionSize
              ?apiKeySource:(Option.map ~f:Values.ApiKeySourceType.of_json
                               apiKeySource)
              ?endpointConfiguration:(Option.map
                                        ~f:Values.EndpointConfiguration.of_json
                                        endpointConfiguration) ?policy
              ?tags:(Option.map ~f:Values.MapOfStringToString.of_json tags)
              ?disableExecuteApiEndpoint
              ?securityPolicy:(Option.map ~f:Values.SecurityPolicy.of_json
                                 securityPolicy)
              ?endpointAccessMode:(Option.map
                                     ~f:Values.EndpointAccessMode.of_json
                                     endpointAccessMode) ~name ())
           (Some Values.RestApi.to_json) (Some Values.RestApi.error_to_json)])
let create_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and cacheClusterEnabled =
         flag "cache-cluster-enabled" (optional bool) ~doc:"BOOL Boolean"
       and cacheClusterSize =
         flag "cache-cluster-size" (optional json_arg)
           ~doc:"JSON CacheClusterSize"
       and variables =
         flag "variables" (optional json_arg) ~doc:"JSON MapOfStringToString"
       and documentationVersion =
         flag "documentation-version" (optional string) ~doc:"STRING String"
       and canarySettings =
         flag "canary-settings" (optional json_arg)
           ~doc:"JSON CanarySettings"
       and tracingEnabled =
         flag "tracing-enabled" (optional bool) ~doc:"BOOL Boolean"
       and tags =
         flag "tags" (optional json_arg) ~doc:"JSON MapOfStringToString"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING String"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_stage
           (Values.CreateStageRequest.make ?description ?cacheClusterEnabled
              ?cacheClusterSize:(Option.map
                                   ~f:Values.CacheClusterSize.of_json
                                   cacheClusterSize)
              ?variables:(Option.map ~f:Values.MapOfStringToString.of_json
                            variables) ?documentationVersion
              ?canarySettings:(Option.map ~f:Values.CanarySettings.of_json
                                 canarySettings) ?tracingEnabled
              ?tags:(Option.map ~f:Values.MapOfStringToString.of_json tags)
              ~restApiId ~stageName ~deploymentId ())
           (Some Values.Stage.to_json) (Some Values.Stage.error_to_json)])
let create_usage_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and apiStages =
         flag "api-stages" (optional json_arg) ~doc:"JSON ListOfApiStage"
       and throttle =
         flag "throttle" (optional json_arg) ~doc:"JSON ThrottleSettings"
       and quota = flag "quota" (optional json_arg) ~doc:"JSON QuotaSettings"
       and tags =
         flag "tags" (optional json_arg) ~doc:"JSON MapOfStringToString"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_usage_plan
           (Values.CreateUsagePlanRequest.make ?description
              ?apiStages:(Option.map ~f:Values.ListOfApiStage.of_json
                            apiStages)
              ?throttle:(Option.map ~f:Values.ThrottleSettings.of_json
                           throttle)
              ?quota:(Option.map ~f:Values.QuotaSettings.of_json quota)
              ?tags:(Option.map ~f:Values.MapOfStringToString.of_json tags)
              ~name ()) (Some Values.UsagePlan.to_json)
           (Some Values.UsagePlan.error_to_json)])
let create_usage_plan_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and usagePlanId =
         flag "usage-plan-id" (required string) ~doc:"STRING String"
       and keyId = flag "key-id" (required string) ~doc:"STRING String"
       and keyType = flag "key-type" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_usage_plan_key
           (Values.CreateUsagePlanKeyRequest.make ~usagePlanId ~keyId
              ~keyType ()) (Some Values.UsagePlanKey.to_json)
           (Some Values.UsagePlanKey.error_to_json)])
let create_vpc_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and tags =
         flag "tags" (optional json_arg) ~doc:"JSON MapOfStringToString"
       and name = flag "name" (required string) ~doc:"STRING String"
       and targetArns =
         flag "target-arns" (required json_arg) ~doc:"JSON ListOfString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpc_link
           (Values.CreateVpcLinkRequest.make ?description
              ?tags:(Option.map ~f:Values.MapOfStringToString.of_json tags)
              ~name ~targetArns:(Values.ListOfString.of_json targetArns) ())
           (Some Values.VpcLink.to_json) (Some Values.VpcLink.error_to_json)])
let delete_api_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiKey = flag "api-key" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_api_key (Values.DeleteApiKeyRequest.make ~apiKey ())
           None None])
let delete_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and authorizerId =
         flag "authorizer-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_authorizer
           (Values.DeleteAuthorizerRequest.make ~restApiId ~authorizerId ())
           None None])
let delete_base_path_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameId =
         flag "domain-name-id" (optional string) ~doc:"STRING String"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String"
       and basePath = flag "base-path" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_base_path_mapping
           (Values.DeleteBasePathMappingRequest.make ?domainNameId
              ~domainName ~basePath ()) None None])
let delete_client_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientCertificateId =
         flag "client-certificate-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_client_certificate
           (Values.DeleteClientCertificateRequest.make ~clientCertificateId
              ()) None None])
let delete_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_deployment
           (Values.DeleteDeploymentRequest.make ~restApiId ~deploymentId ())
           None None])
let delete_documentation_part =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and documentationPartId =
         flag "documentation-part-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_documentation_part
           (Values.DeleteDocumentationPartRequest.make ~restApiId
              ~documentationPartId ()) None None])
let delete_documentation_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and documentationVersion =
         flag "documentation-version" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_documentation_version
           (Values.DeleteDocumentationVersionRequest.make ~restApiId
              ~documentationVersion ()) None None])
let delete_domain_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameId =
         flag "domain-name-id" (optional string) ~doc:"STRING String"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain_name
           (Values.DeleteDomainNameRequest.make ?domainNameId ~domainName ())
           None None])
let delete_domain_name_access_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameAccessAssociationArn =
         flag "domain-name-access-association-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain_name_access_association
           (Values.DeleteDomainNameAccessAssociationRequest.make
              ~domainNameAccessAssociationArn ()) None None])
let delete_gateway_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and responseType =
         flag "response-type" (required json_arg)
           ~doc:"JSON GatewayResponseType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_gateway_response
           (Values.DeleteGatewayResponseRequest.make ~restApiId
              ~responseType:(Values.GatewayResponseType.of_json responseType)
              ()) None None])
let delete_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_integration
           (Values.DeleteIntegrationRequest.make ~restApiId ~resourceId
              ~httpMethod ()) None None])
let delete_integration_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String"
       and statusCode =
         flag "status-code" (required string) ~doc:"STRING StatusCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_integration_response
           (Values.DeleteIntegrationResponseRequest.make ~restApiId
              ~resourceId ~httpMethod ~statusCode ()) None None])
let delete_method =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_method
           (Values.DeleteMethodRequest.make ~restApiId ~resourceId
              ~httpMethod ()) None None])
let delete_method_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String"
       and statusCode =
         flag "status-code" (required string) ~doc:"STRING StatusCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_method_response
           (Values.DeleteMethodResponseRequest.make ~restApiId ~resourceId
              ~httpMethod ~statusCode ()) None None])
let delete_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model
           (Values.DeleteModelRequest.make ~restApiId ~modelName ()) None
           None])
let delete_request_validator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and requestValidatorId =
         flag "request-validator-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_request_validator
           (Values.DeleteRequestValidatorRequest.make ~restApiId
              ~requestValidatorId ()) None None])
let delete_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource
           (Values.DeleteResourceRequest.make ~restApiId ~resourceId ()) None
           None])
let delete_rest_api =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_rest_api
           (Values.DeleteRestApiRequest.make ~restApiId ()) None None])
let delete_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_stage
           (Values.DeleteStageRequest.make ~restApiId ~stageName ()) None
           None])
let delete_usage_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and usagePlanId =
         flag "usage-plan-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_usage_plan
           (Values.DeleteUsagePlanRequest.make ~usagePlanId ()) None None])
let delete_usage_plan_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and usagePlanId =
         flag "usage-plan-id" (required string) ~doc:"STRING String"
       and keyId = flag "key-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_usage_plan_key
           (Values.DeleteUsagePlanKeyRequest.make ~usagePlanId ~keyId ())
           None None])
let delete_vpc_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcLinkId =
         flag "vpc-link-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_link
           (Values.DeleteVpcLinkRequest.make ~vpcLinkId ()) None None])
let flush_stage_authorizers_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.flush_stage_authorizers_cache
           (Values.FlushStageAuthorizersCacheRequest.make ~restApiId
              ~stageName ()) None None])
let flush_stage_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.flush_stage_cache
           (Values.FlushStageCacheRequest.make ~restApiId ~stageName ()) None
           None])
let generate_client_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and tags =
         flag "tags" (optional json_arg) ~doc:"JSON MapOfStringToString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_client_certificate
           (Values.GenerateClientCertificateRequest.make ?description
              ?tags:(Option.map ~f:Values.MapOfStringToString.of_json tags)
              ()) (Some Values.ClientCertificate.to_json)
           (Some Values.ClientCertificate.error_to_json)])
let get_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account (Values.GetAccountRequest.make ())
           (Some Values.Account.to_json) (Some Values.Account.error_to_json)])
let get_api_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeValue =
         flag "include-value" (optional bool) ~doc:"BOOL NullableBoolean"
       and apiKey = flag "api-key" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_api_key
           (Values.GetApiKeyRequest.make ?includeValue ~apiKey ())
           (Some Values.ApiKey.to_json) (Some Values.ApiKey.error_to_json)])
let get_api_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and nameQuery =
         flag "name-query" (optional string) ~doc:"STRING String"
       and customerId =
         flag "customer-id" (optional string) ~doc:"STRING String"
       and includeValues =
         flag "include-values" (optional bool) ~doc:"BOOL NullableBoolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_api_keys
           (Values.GetApiKeysRequest.make ?position ?limit ?nameQuery
              ?customerId ?includeValues ()) (Some Values.ApiKeys.to_json)
           (Some Values.ApiKeys.error_to_json)])
let get_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and authorizerId =
         flag "authorizer-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_authorizer
           (Values.GetAuthorizerRequest.make ~restApiId ~authorizerId ())
           (Some Values.Authorizer.to_json)
           (Some Values.Authorizer.error_to_json)])
let get_authorizers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_authorizers
           (Values.GetAuthorizersRequest.make ?position ?limit ~restApiId ())
           (Some Values.Authorizers.to_json)
           (Some Values.Authorizers.error_to_json)])
let get_base_path_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameId =
         flag "domain-name-id" (optional string) ~doc:"STRING String"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String"
       and basePath = flag "base-path" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_base_path_mapping
           (Values.GetBasePathMappingRequest.make ?domainNameId ~domainName
              ~basePath ()) (Some Values.BasePathMapping.to_json)
           (Some Values.BasePathMapping.error_to_json)])
let get_base_path_mappings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameId =
         flag "domain-name-id" (optional string) ~doc:"STRING String"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_base_path_mappings
           (Values.GetBasePathMappingsRequest.make ?domainNameId ?position
              ?limit ~domainName ()) (Some Values.BasePathMappings.to_json)
           (Some Values.BasePathMappings.error_to_json)])
let get_client_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientCertificateId =
         flag "client-certificate-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_client_certificate
           (Values.GetClientCertificateRequest.make ~clientCertificateId ())
           (Some Values.ClientCertificate.to_json)
           (Some Values.ClientCertificate.error_to_json)])
let get_client_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_client_certificates
           (Values.GetClientCertificatesRequest.make ?position ?limit ())
           (Some Values.ClientCertificates.to_json)
           (Some Values.ClientCertificates.error_to_json)])
let get_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and embed = flag "embed" (optional json_arg) ~doc:"JSON ListOfString"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deployment
           (Values.GetDeploymentRequest.make
              ?embed:(Option.map ~f:Values.ListOfString.of_json embed)
              ~restApiId ~deploymentId ()) (Some Values.Deployment.to_json)
           (Some Values.Deployment.error_to_json)])
let get_deployments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deployments
           (Values.GetDeploymentsRequest.make ?position ?limit ~restApiId ())
           (Some Values.Deployments.to_json)
           (Some Values.Deployments.error_to_json)])
let get_documentation_part =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and documentationPartId =
         flag "documentation-part-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_documentation_part
           (Values.GetDocumentationPartRequest.make ~restApiId
              ~documentationPartId ())
           (Some Values.DocumentationPart.to_json)
           (Some Values.DocumentationPart.error_to_json)])
let get_documentation_parts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON DocumentationPartType"
       and nameQuery =
         flag "name-query" (optional string) ~doc:"STRING String"
       and path = flag "path" (optional string) ~doc:"STRING String"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and locationStatus =
         flag "location-status" (optional json_arg)
           ~doc:"JSON LocationStatusType"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_documentation_parts
           (Values.GetDocumentationPartsRequest.make
              ?type_:(Option.map ~f:Values.DocumentationPartType.of_json
                        type_) ?nameQuery ?path ?position ?limit
              ?locationStatus:(Option.map
                                 ~f:Values.LocationStatusType.of_json
                                 locationStatus) ~restApiId ())
           (Some Values.DocumentationParts.to_json)
           (Some Values.DocumentationParts.error_to_json)])
let get_documentation_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and documentationVersion =
         flag "documentation-version" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_documentation_version
           (Values.GetDocumentationVersionRequest.make ~restApiId
              ~documentationVersion ())
           (Some Values.DocumentationVersion.to_json)
           (Some Values.DocumentationVersion.error_to_json)])
let get_documentation_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_documentation_versions
           (Values.GetDocumentationVersionsRequest.make ?position ?limit
              ~restApiId ()) (Some Values.DocumentationVersions.to_json)
           (Some Values.DocumentationVersions.error_to_json)])
let get_domain_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameId =
         flag "domain-name-id" (optional string) ~doc:"STRING String"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_name
           (Values.GetDomainNameRequest.make ?domainNameId ~domainName ())
           (Some Values.DomainName.to_json)
           (Some Values.DomainName.error_to_json)])
let get_domain_name_access_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and resourceOwner =
         flag "resource-owner" (optional json_arg) ~doc:"JSON ResourceOwner" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_name_access_associations
           (Values.GetDomainNameAccessAssociationsRequest.make ?position
              ?limit
              ?resourceOwner:(Option.map ~f:Values.ResourceOwner.of_json
                                resourceOwner) ())
           (Some Values.DomainNameAccessAssociations.to_json)
           (Some Values.DomainNameAccessAssociations.error_to_json)])
let get_domain_names =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and resourceOwner =
         flag "resource-owner" (optional json_arg) ~doc:"JSON ResourceOwner" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_names
           (Values.GetDomainNamesRequest.make ?position ?limit
              ?resourceOwner:(Option.map ~f:Values.ResourceOwner.of_json
                                resourceOwner) ())
           (Some Values.DomainNames.to_json)
           (Some Values.DomainNames.error_to_json)])
let get_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and accepts = flag "accepts" (optional string) ~doc:"STRING String"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING String"
       and exportType =
         flag "export-type" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_export
           (Values.GetExportRequest.make
              ?parameters:(Option.map ~f:Values.MapOfStringToString.of_json
                             parameters) ?accepts ~restApiId ~stageName
              ~exportType ()) (Some Values.ExportResponse.to_json)
           (Some Values.ExportResponse.error_to_json)])
let get_gateway_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and responseType =
         flag "response-type" (required json_arg)
           ~doc:"JSON GatewayResponseType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_gateway_response
           (Values.GetGatewayResponseRequest.make ~restApiId
              ~responseType:(Values.GatewayResponseType.of_json responseType)
              ()) (Some Values.GatewayResponse.to_json)
           (Some Values.GatewayResponse.error_to_json)])
let get_gateway_responses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_gateway_responses
           (Values.GetGatewayResponsesRequest.make ?position ?limit
              ~restApiId ()) (Some Values.GatewayResponses.to_json)
           (Some Values.GatewayResponses.error_to_json)])
let get_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_integration
           (Values.GetIntegrationRequest.make ~restApiId ~resourceId
              ~httpMethod ()) (Some Values.Integration.to_json)
           (Some Values.Integration.error_to_json)])
let get_integration_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String"
       and statusCode =
         flag "status-code" (required string) ~doc:"STRING StatusCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_integration_response
           (Values.GetIntegrationResponseRequest.make ~restApiId ~resourceId
              ~httpMethod ~statusCode ())
           (Some Values.IntegrationResponse.to_json)
           (Some Values.IntegrationResponse.error_to_json)])
let get_method =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_method
           (Values.GetMethodRequest.make ~restApiId ~resourceId ~httpMethod
              ()) (Some Values.Method.to_json)
           (Some Values.Method.error_to_json)])
let get_method_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String"
       and statusCode =
         flag "status-code" (required string) ~doc:"STRING StatusCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_method_response
           (Values.GetMethodResponseRequest.make ~restApiId ~resourceId
              ~httpMethod ~statusCode ())
           (Some Values.MethodResponse.to_json)
           (Some Values.MethodResponse.error_to_json)])
let get_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flatten = flag "flatten" (optional bool) ~doc:"BOOL Boolean"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_model
           (Values.GetModelRequest.make ?flatten ~restApiId ~modelName ())
           (Some Values.Model.to_json) (Some Values.Model.error_to_json)])
let get_model_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_model_template
           (Values.GetModelTemplateRequest.make ~restApiId ~modelName ())
           (Some Values.Template.to_json)
           (Some Values.Template.error_to_json)])
let get_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_models
           (Values.GetModelsRequest.make ?position ?limit ~restApiId ())
           (Some Values.Models.to_json) (Some Values.Models.error_to_json)])
let get_request_validator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and requestValidatorId =
         flag "request-validator-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_request_validator
           (Values.GetRequestValidatorRequest.make ~restApiId
              ~requestValidatorId ()) (Some Values.RequestValidator.to_json)
           (Some Values.RequestValidator.error_to_json)])
let get_request_validators =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_request_validators
           (Values.GetRequestValidatorsRequest.make ?position ?limit
              ~restApiId ()) (Some Values.RequestValidators.to_json)
           (Some Values.RequestValidators.error_to_json)])
let get_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and embed = flag "embed" (optional json_arg) ~doc:"JSON ListOfString"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource
           (Values.GetResourceRequest.make
              ?embed:(Option.map ~f:Values.ListOfString.of_json embed)
              ~restApiId ~resourceId ()) (Some Values.Resource.to_json)
           (Some Values.Resource.error_to_json)])
let get_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and embed = flag "embed" (optional json_arg) ~doc:"JSON ListOfString"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resources
           (Values.GetResourcesRequest.make ?position ?limit
              ?embed:(Option.map ~f:Values.ListOfString.of_json embed)
              ~restApiId ()) (Some Values.Resources.to_json)
           (Some Values.Resources.error_to_json)])
let get_rest_api =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_rest_api (Values.GetRestApiRequest.make ~restApiId ())
           (Some Values.RestApi.to_json) (Some Values.RestApi.error_to_json)])
let get_rest_apis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_rest_apis
           (Values.GetRestApisRequest.make ?position ?limit ())
           (Some Values.RestApis.to_json)
           (Some Values.RestApis.error_to_json)])
let get_sdk =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING String"
       and sdkType = flag "sdk-type" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sdk
           (Values.GetSdkRequest.make
              ?parameters:(Option.map ~f:Values.MapOfStringToString.of_json
                             parameters) ~restApiId ~stageName ~sdkType ())
           (Some Values.SdkResponse.to_json)
           (Some Values.SdkResponse.error_to_json)])
let get_sdk_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sdk_type (Values.GetSdkTypeRequest.make ~id ())
           (Some Values.SdkType.to_json) (Some Values.SdkType.error_to_json)])
let get_sdk_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sdk_types
           (Values.GetSdkTypesRequest.make ?position ?limit ())
           (Some Values.SdkTypes.to_json)
           (Some Values.SdkTypes.error_to_json)])
let get_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_stage
           (Values.GetStageRequest.make ~restApiId ~stageName ())
           (Some Values.Stage.to_json) (Some Values.Stage.error_to_json)])
let get_stages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deploymentId =
         flag "deployment-id" (optional string) ~doc:"STRING String"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_stages
           (Values.GetStagesRequest.make ?deploymentId ~restApiId ())
           (Some Values.Stages.to_json) (Some Values.Stages.error_to_json)])
let get_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tags
           (Values.GetTagsRequest.make ?position ?limit ~resourceArn ())
           (Some Values.Tags.to_json) (Some Values.Tags.error_to_json)])
let get_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (optional string) ~doc:"STRING String"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and usagePlanId =
         flag "usage-plan-id" (required string) ~doc:"STRING String"
       and startDate =
         flag "start-date" (required string) ~doc:"STRING String"
       and endDate = flag "end-date" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_usage
           (Values.GetUsageRequest.make ?keyId ?position ?limit ~usagePlanId
              ~startDate ~endDate ()) (Some Values.Usage.to_json)
           (Some Values.Usage.error_to_json)])
let get_usage_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and usagePlanId =
         flag "usage-plan-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_usage_plan
           (Values.GetUsagePlanRequest.make ~usagePlanId ())
           (Some Values.UsagePlan.to_json)
           (Some Values.UsagePlan.error_to_json)])
let get_usage_plan_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and usagePlanId =
         flag "usage-plan-id" (required string) ~doc:"STRING String"
       and keyId = flag "key-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_usage_plan_key
           (Values.GetUsagePlanKeyRequest.make ~usagePlanId ~keyId ())
           (Some Values.UsagePlanKey.to_json)
           (Some Values.UsagePlanKey.error_to_json)])
let get_usage_plan_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger"
       and nameQuery =
         flag "name-query" (optional string) ~doc:"STRING String"
       and usagePlanId =
         flag "usage-plan-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_usage_plan_keys
           (Values.GetUsagePlanKeysRequest.make ?position ?limit ?nameQuery
              ~usagePlanId ()) (Some Values.UsagePlanKeys.to_json)
           (Some Values.UsagePlanKeys.error_to_json)])
let get_usage_plans =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and keyId = flag "key-id" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_usage_plans
           (Values.GetUsagePlansRequest.make ?position ?keyId ?limit ())
           (Some Values.UsagePlans.to_json)
           (Some Values.UsagePlans.error_to_json)])
let get_vpc_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcLinkId =
         flag "vpc-link-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vpc_link (Values.GetVpcLinkRequest.make ~vpcLinkId ())
           (Some Values.VpcLink.to_json) (Some Values.VpcLink.error_to_json)])
let get_vpc_links =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and position = flag "position" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT NullableInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vpc_links
           (Values.GetVpcLinksRequest.make ?position ?limit ())
           (Some Values.VpcLinks.to_json)
           (Some Values.VpcLinks.error_to_json)])
let import_api_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and failOnWarnings =
         flag "fail-on-warnings" (optional bool) ~doc:"BOOL Boolean"
       and body = flag "body" (required json_arg) ~doc:"JSON Blob"
       and format =
         flag "format" (required json_arg) ~doc:"JSON ApiKeysFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_api_keys
           (Values.ImportApiKeysRequest.make ?failOnWarnings
              ~body:(Values.Blob.of_json body)
              ~format:(Values.ApiKeysFormat.of_json format) ())
           (Some Values.ApiKeyIds.to_json)
           (Some Values.ApiKeyIds.error_to_json)])
let import_documentation_parts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mode = flag "mode" (optional json_arg) ~doc:"JSON PutMode"
       and failOnWarnings =
         flag "fail-on-warnings" (optional bool) ~doc:"BOOL Boolean"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and body = flag "body" (required json_arg) ~doc:"JSON Blob" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_documentation_parts
           (Values.ImportDocumentationPartsRequest.make
              ?mode:(Option.map ~f:Values.PutMode.of_json mode)
              ?failOnWarnings ~restApiId ~body:(Values.Blob.of_json body) ())
           (Some Values.DocumentationPartIds.to_json)
           (Some Values.DocumentationPartIds.error_to_json)])
let import_rest_api =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and failOnWarnings =
         flag "fail-on-warnings" (optional bool) ~doc:"BOOL Boolean"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and body = flag "body" (required json_arg) ~doc:"JSON Blob" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_rest_api
           (Values.ImportRestApiRequest.make ?failOnWarnings
              ?parameters:(Option.map ~f:Values.MapOfStringToString.of_json
                             parameters) ~body:(Values.Blob.of_json body) ())
           (Some Values.RestApi.to_json) (Some Values.RestApi.error_to_json)])
let put_gateway_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statusCode =
         flag "status-code" (optional string) ~doc:"STRING StatusCode"
       and responseParameters =
         flag "response-parameters" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and responseTemplates =
         flag "response-templates" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and responseType =
         flag "response-type" (required json_arg)
           ~doc:"JSON GatewayResponseType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_gateway_response
           (Values.PutGatewayResponseRequest.make ?statusCode
              ?responseParameters:(Option.map
                                     ~f:Values.MapOfStringToString.of_json
                                     responseParameters)
              ?responseTemplates:(Option.map
                                    ~f:Values.MapOfStringToString.of_json
                                    responseTemplates) ~restApiId
              ~responseType:(Values.GatewayResponseType.of_json responseType)
              ()) (Some Values.GatewayResponse.to_json)
           (Some Values.GatewayResponse.error_to_json)])
let put_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and integrationHttpMethod =
         flag "integration-http-method" (optional string)
           ~doc:"STRING String"
       and uri = flag "uri" (optional string) ~doc:"STRING String"
       and connectionType =
         flag "connection-type" (optional json_arg)
           ~doc:"JSON ConnectionType"
       and connectionId =
         flag "connection-id" (optional string) ~doc:"STRING String"
       and credentials =
         flag "credentials" (optional string) ~doc:"STRING String"
       and requestParameters =
         flag "request-parameters" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and requestTemplates =
         flag "request-templates" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and passthroughBehavior =
         flag "passthrough-behavior" (optional string) ~doc:"STRING String"
       and cacheNamespace =
         flag "cache-namespace" (optional string) ~doc:"STRING String"
       and cacheKeyParameters =
         flag "cache-key-parameters" (optional json_arg)
           ~doc:"JSON ListOfString"
       and contentHandling =
         flag "content-handling" (optional json_arg)
           ~doc:"JSON ContentHandlingStrategy"
       and timeoutInMillis =
         flag "timeout-in-millis" (optional int) ~doc:"INT NullableInteger"
       and tlsConfig =
         flag "tls-config" (optional json_arg) ~doc:"JSON TlsConfig"
       and responseTransferMode =
         flag "response-transfer-mode" (optional json_arg)
           ~doc:"JSON ResponseTransferMode"
       and integrationTarget =
         flag "integration-target" (optional string) ~doc:"STRING String"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON IntegrationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_integration
           (Values.PutIntegrationRequest.make ?integrationHttpMethod ?uri
              ?connectionType:(Option.map ~f:Values.ConnectionType.of_json
                                 connectionType) ?connectionId ?credentials
              ?requestParameters:(Option.map
                                    ~f:Values.MapOfStringToString.of_json
                                    requestParameters)
              ?requestTemplates:(Option.map
                                   ~f:Values.MapOfStringToString.of_json
                                   requestTemplates) ?passthroughBehavior
              ?cacheNamespace
              ?cacheKeyParameters:(Option.map ~f:Values.ListOfString.of_json
                                     cacheKeyParameters)
              ?contentHandling:(Option.map
                                  ~f:Values.ContentHandlingStrategy.of_json
                                  contentHandling) ?timeoutInMillis
              ?tlsConfig:(Option.map ~f:Values.TlsConfig.of_json tlsConfig)
              ?responseTransferMode:(Option.map
                                       ~f:Values.ResponseTransferMode.of_json
                                       responseTransferMode)
              ?integrationTarget ~restApiId ~resourceId ~httpMethod
              ~type_:(Values.IntegrationType.of_json type_) ())
           (Some Values.Integration.to_json)
           (Some Values.Integration.error_to_json)])
let put_integration_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and selectionPattern =
         flag "selection-pattern" (optional string) ~doc:"STRING String"
       and responseParameters =
         flag "response-parameters" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and responseTemplates =
         flag "response-templates" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and contentHandling =
         flag "content-handling" (optional json_arg)
           ~doc:"JSON ContentHandlingStrategy"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String"
       and statusCode =
         flag "status-code" (required string) ~doc:"STRING StatusCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_integration_response
           (Values.PutIntegrationResponseRequest.make ?selectionPattern
              ?responseParameters:(Option.map
                                     ~f:Values.MapOfStringToString.of_json
                                     responseParameters)
              ?responseTemplates:(Option.map
                                    ~f:Values.MapOfStringToString.of_json
                                    responseTemplates)
              ?contentHandling:(Option.map
                                  ~f:Values.ContentHandlingStrategy.of_json
                                  contentHandling) ~restApiId ~resourceId
              ~httpMethod ~statusCode ())
           (Some Values.IntegrationResponse.to_json)
           (Some Values.IntegrationResponse.error_to_json)])
let put_method =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorizerId =
         flag "authorizer-id" (optional string) ~doc:"STRING String"
       and apiKeyRequired =
         flag "api-key-required" (optional bool) ~doc:"BOOL Boolean"
       and operationName =
         flag "operation-name" (optional string) ~doc:"STRING String"
       and requestParameters =
         flag "request-parameters" (optional json_arg)
           ~doc:"JSON MapOfStringToBoolean"
       and requestModels =
         flag "request-models" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and requestValidatorId =
         flag "request-validator-id" (optional string) ~doc:"STRING String"
       and authorizationScopes =
         flag "authorization-scopes" (optional json_arg)
           ~doc:"JSON ListOfString"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String"
       and authorizationType =
         flag "authorization-type" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_method
           (Values.PutMethodRequest.make ?authorizerId ?apiKeyRequired
              ?operationName
              ?requestParameters:(Option.map
                                    ~f:Values.MapOfStringToBoolean.of_json
                                    requestParameters)
              ?requestModels:(Option.map
                                ~f:Values.MapOfStringToString.of_json
                                requestModels) ?requestValidatorId
              ?authorizationScopes:(Option.map ~f:Values.ListOfString.of_json
                                      authorizationScopes) ~restApiId
              ~resourceId ~httpMethod ~authorizationType ())
           (Some Values.Method.to_json) (Some Values.Method.error_to_json)])
let put_method_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and responseParameters =
         flag "response-parameters" (optional json_arg)
           ~doc:"JSON MapOfStringToBoolean"
       and responseModels =
         flag "response-models" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String"
       and statusCode =
         flag "status-code" (required string) ~doc:"STRING StatusCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_method_response
           (Values.PutMethodResponseRequest.make
              ?responseParameters:(Option.map
                                     ~f:Values.MapOfStringToBoolean.of_json
                                     responseParameters)
              ?responseModels:(Option.map
                                 ~f:Values.MapOfStringToString.of_json
                                 responseModels) ~restApiId ~resourceId
              ~httpMethod ~statusCode ())
           (Some Values.MethodResponse.to_json)
           (Some Values.MethodResponse.error_to_json)])
let put_rest_api =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mode = flag "mode" (optional json_arg) ~doc:"JSON PutMode"
       and failOnWarnings =
         flag "fail-on-warnings" (optional bool) ~doc:"BOOL Boolean"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and body = flag "body" (required json_arg) ~doc:"JSON Blob" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_rest_api
           (Values.PutRestApiRequest.make
              ?mode:(Option.map ~f:Values.PutMode.of_json mode)
              ?failOnWarnings
              ?parameters:(Option.map ~f:Values.MapOfStringToString.of_json
                             parameters) ~restApiId
              ~body:(Values.Blob.of_json body) ())
           (Some Values.RestApi.to_json) (Some Values.RestApi.error_to_json)])
let reject_domain_name_access_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameAccessAssociationArn =
         flag "domain-name-access-association-arn" (required string)
           ~doc:"STRING String"
       and domainNameArn =
         flag "domain-name-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_domain_name_access_association
           (Values.RejectDomainNameAccessAssociationRequest.make
              ~domainNameAccessAssociationArn ~domainNameArn ()) None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags =
         flag "tags" (required json_arg) ~doc:"JSON MapOfStringToString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.MapOfStringToString.of_json tags) ()) None None])
let test_invoke_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and headers =
         flag "headers" (optional json_arg) ~doc:"JSON MapOfStringToString"
       and multiValueHeaders =
         flag "multi-value-headers" (optional json_arg)
           ~doc:"JSON MapOfStringToList"
       and pathWithQueryString =
         flag "path-with-query-string" (optional string) ~doc:"STRING String"
       and body = flag "body" (optional string) ~doc:"STRING String"
       and stageVariables =
         flag "stage-variables" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and additionalContext =
         flag "additional-context" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and authorizerId =
         flag "authorizer-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_invoke_authorizer
           (Values.TestInvokeAuthorizerRequest.make
              ?headers:(Option.map ~f:Values.MapOfStringToString.of_json
                          headers)
              ?multiValueHeaders:(Option.map
                                    ~f:Values.MapOfStringToList.of_json
                                    multiValueHeaders) ?pathWithQueryString
              ?body
              ?stageVariables:(Option.map
                                 ~f:Values.MapOfStringToString.of_json
                                 stageVariables)
              ?additionalContext:(Option.map
                                    ~f:Values.MapOfStringToString.of_json
                                    additionalContext) ~restApiId
              ~authorizerId ())
           (Some Values.TestInvokeAuthorizerResponse.to_json)
           (Some Values.TestInvokeAuthorizerResponse.error_to_json)])
let test_invoke_method =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pathWithQueryString =
         flag "path-with-query-string" (optional string) ~doc:"STRING String"
       and body = flag "body" (optional string) ~doc:"STRING String"
       and headers =
         flag "headers" (optional json_arg) ~doc:"JSON MapOfStringToString"
       and multiValueHeaders =
         flag "multi-value-headers" (optional json_arg)
           ~doc:"JSON MapOfStringToList"
       and clientCertificateId =
         flag "client-certificate-id" (optional string) ~doc:"STRING String"
       and stageVariables =
         flag "stage-variables" (optional json_arg)
           ~doc:"JSON MapOfStringToString"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_invoke_method
           (Values.TestInvokeMethodRequest.make ?pathWithQueryString ?body
              ?headers:(Option.map ~f:Values.MapOfStringToString.of_json
                          headers)
              ?multiValueHeaders:(Option.map
                                    ~f:Values.MapOfStringToList.of_json
                                    multiValueHeaders) ?clientCertificateId
              ?stageVariables:(Option.map
                                 ~f:Values.MapOfStringToString.of_json
                                 stageVariables) ~restApiId ~resourceId
              ~httpMethod ()) (Some Values.TestInvokeMethodResponse.to_json)
           (Some Values.TestInvokeMethodResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON ListOfString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.ListOfString.of_json tagKeys) ()) None None])
let update_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account
           (Values.UpdateAccountRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ())
           (Some Values.Account.to_json) (Some Values.Account.error_to_json)])
let update_api_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and apiKey = flag "api-key" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_api_key
           (Values.UpdateApiKeyRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~apiKey ())
           (Some Values.ApiKey.to_json) (Some Values.ApiKey.error_to_json)])
let update_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and authorizerId =
         flag "authorizer-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_authorizer
           (Values.UpdateAuthorizerRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId ~authorizerId
              ()) (Some Values.Authorizer.to_json)
           (Some Values.Authorizer.error_to_json)])
let update_base_path_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameId =
         flag "domain-name-id" (optional string) ~doc:"STRING String"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String"
       and basePath = flag "base-path" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_base_path_mapping
           (Values.UpdateBasePathMappingRequest.make ?domainNameId
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~domainName ~basePath ())
           (Some Values.BasePathMapping.to_json)
           (Some Values.BasePathMapping.error_to_json)])
let update_client_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and clientCertificateId =
         flag "client-certificate-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_client_certificate
           (Values.UpdateClientCertificateRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~clientCertificateId ())
           (Some Values.ClientCertificate.to_json)
           (Some Values.ClientCertificate.error_to_json)])
let update_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_deployment
           (Values.UpdateDeploymentRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId ~deploymentId
              ()) (Some Values.Deployment.to_json)
           (Some Values.Deployment.error_to_json)])
let update_documentation_part =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and documentationPartId =
         flag "documentation-part-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_documentation_part
           (Values.UpdateDocumentationPartRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId
              ~documentationPartId ())
           (Some Values.DocumentationPart.to_json)
           (Some Values.DocumentationPart.error_to_json)])
let update_documentation_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and documentationVersion =
         flag "documentation-version" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_documentation_version
           (Values.UpdateDocumentationVersionRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId
              ~documentationVersion ())
           (Some Values.DocumentationVersion.to_json)
           (Some Values.DocumentationVersion.error_to_json)])
let update_domain_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameId =
         flag "domain-name-id" (optional string) ~doc:"STRING String"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain_name
           (Values.UpdateDomainNameRequest.make ?domainNameId
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~domainName ())
           (Some Values.DomainName.to_json)
           (Some Values.DomainName.error_to_json)])
let update_gateway_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and responseType =
         flag "response-type" (required json_arg)
           ~doc:"JSON GatewayResponseType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_gateway_response
           (Values.UpdateGatewayResponseRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId
              ~responseType:(Values.GatewayResponseType.of_json responseType)
              ()) (Some Values.GatewayResponse.to_json)
           (Some Values.GatewayResponse.error_to_json)])
let update_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_integration
           (Values.UpdateIntegrationRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId ~resourceId
              ~httpMethod ()) (Some Values.Integration.to_json)
           (Some Values.Integration.error_to_json)])
let update_integration_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String"
       and statusCode =
         flag "status-code" (required string) ~doc:"STRING StatusCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_integration_response
           (Values.UpdateIntegrationResponseRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId ~resourceId
              ~httpMethod ~statusCode ())
           (Some Values.IntegrationResponse.to_json)
           (Some Values.IntegrationResponse.error_to_json)])
let update_method =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_method
           (Values.UpdateMethodRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId ~resourceId
              ~httpMethod ()) (Some Values.Method.to_json)
           (Some Values.Method.error_to_json)])
let update_method_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and httpMethod =
         flag "http-method" (required string) ~doc:"STRING String"
       and statusCode =
         flag "status-code" (required string) ~doc:"STRING StatusCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_method_response
           (Values.UpdateMethodResponseRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId ~resourceId
              ~httpMethod ~statusCode ())
           (Some Values.MethodResponse.to_json)
           (Some Values.MethodResponse.error_to_json)])
let update_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_model
           (Values.UpdateModelRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId ~modelName ())
           (Some Values.Model.to_json) (Some Values.Model.error_to_json)])
let update_request_validator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and requestValidatorId =
         flag "request-validator-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_request_validator
           (Values.UpdateRequestValidatorRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId
              ~requestValidatorId ()) (Some Values.RequestValidator.to_json)
           (Some Values.RequestValidator.error_to_json)])
let update_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource
           (Values.UpdateResourceRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId ~resourceId ())
           (Some Values.Resource.to_json)
           (Some Values.Resource.error_to_json)])
let update_rest_api =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_rest_api
           (Values.UpdateRestApiRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId ())
           (Some Values.RestApi.to_json) (Some Values.RestApi.error_to_json)])
let update_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and restApiId =
         flag "rest-api-id" (required string) ~doc:"STRING String"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_stage
           (Values.UpdateStageRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~restApiId ~stageName ())
           (Some Values.Stage.to_json) (Some Values.Stage.error_to_json)])
let update_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and usagePlanId =
         flag "usage-plan-id" (required string) ~doc:"STRING String"
       and keyId = flag "key-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_usage
           (Values.UpdateUsageRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~usagePlanId ~keyId ())
           (Some Values.Usage.to_json) (Some Values.Usage.error_to_json)])
let update_usage_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and usagePlanId =
         flag "usage-plan-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_usage_plan
           (Values.UpdateUsagePlanRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~usagePlanId ())
           (Some Values.UsagePlan.to_json)
           (Some Values.UsagePlan.error_to_json)])
let update_vpc_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchOperations =
         flag "patch-operations" (optional json_arg)
           ~doc:"JSON ListOfPatchOperation"
       and vpcLinkId =
         flag "vpc-link-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_vpc_link
           (Values.UpdateVpcLinkRequest.make
              ?patchOperations:(Option.map
                                  ~f:Values.ListOfPatchOperation.of_json
                                  patchOperations) ~vpcLinkId ())
           (Some Values.VpcLink.to_json) (Some Values.VpcLink.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-api-key", create_api_key);
    ("create-authorizer", create_authorizer);
    ("create-base-path-mapping", create_base_path_mapping);
    ("create-deployment", create_deployment);
    ("create-documentation-part", create_documentation_part);
    ("create-documentation-version", create_documentation_version);
    ("create-domain-name", create_domain_name);
    ("create-domain-name-access-association",
      create_domain_name_access_association);
    ("create-model", create_model);
    ("create-request-validator", create_request_validator);
    ("create-resource", create_resource);
    ("create-rest-api", create_rest_api);
    ("create-stage", create_stage);
    ("create-usage-plan", create_usage_plan);
    ("create-usage-plan-key", create_usage_plan_key);
    ("create-vpc-link", create_vpc_link);
    ("delete-api-key", delete_api_key);
    ("delete-authorizer", delete_authorizer);
    ("delete-base-path-mapping", delete_base_path_mapping);
    ("delete-client-certificate", delete_client_certificate);
    ("delete-deployment", delete_deployment);
    ("delete-documentation-part", delete_documentation_part);
    ("delete-documentation-version", delete_documentation_version);
    ("delete-domain-name", delete_domain_name);
    ("delete-domain-name-access-association",
      delete_domain_name_access_association);
    ("delete-gateway-response", delete_gateway_response);
    ("delete-integration", delete_integration);
    ("delete-integration-response", delete_integration_response);
    ("delete-method", delete_method);
    ("delete-method-response", delete_method_response);
    ("delete-model", delete_model);
    ("delete-request-validator", delete_request_validator);
    ("delete-resource", delete_resource);
    ("delete-rest-api", delete_rest_api);
    ("delete-stage", delete_stage);
    ("delete-usage-plan", delete_usage_plan);
    ("delete-usage-plan-key", delete_usage_plan_key);
    ("delete-vpc-link", delete_vpc_link);
    ("flush-stage-authorizers-cache", flush_stage_authorizers_cache);
    ("flush-stage-cache", flush_stage_cache);
    ("generate-client-certificate", generate_client_certificate);
    ("get-account", get_account);
    ("get-api-key", get_api_key);
    ("get-api-keys", get_api_keys);
    ("get-authorizer", get_authorizer);
    ("get-authorizers", get_authorizers);
    ("get-base-path-mapping", get_base_path_mapping);
    ("get-base-path-mappings", get_base_path_mappings);
    ("get-client-certificate", get_client_certificate);
    ("get-client-certificates", get_client_certificates);
    ("get-deployment", get_deployment);
    ("get-deployments", get_deployments);
    ("get-documentation-part", get_documentation_part);
    ("get-documentation-parts", get_documentation_parts);
    ("get-documentation-version", get_documentation_version);
    ("get-documentation-versions", get_documentation_versions);
    ("get-domain-name", get_domain_name);
    ("get-domain-name-access-associations",
      get_domain_name_access_associations);
    ("get-domain-names", get_domain_names);
    ("get-export", get_export);
    ("get-gateway-response", get_gateway_response);
    ("get-gateway-responses", get_gateway_responses);
    ("get-integration", get_integration);
    ("get-integration-response", get_integration_response);
    ("get-method", get_method);
    ("get-method-response", get_method_response);
    ("get-model", get_model);
    ("get-model-template", get_model_template);
    ("get-models", get_models);
    ("get-request-validator", get_request_validator);
    ("get-request-validators", get_request_validators);
    ("get-resource", get_resource);
    ("get-resources", get_resources);
    ("get-rest-api", get_rest_api);
    ("get-rest-apis", get_rest_apis);
    ("get-sdk", get_sdk);
    ("get-sdk-type", get_sdk_type);
    ("get-sdk-types", get_sdk_types);
    ("get-stage", get_stage);
    ("get-stages", get_stages);
    ("get-tags", get_tags);
    ("get-usage", get_usage);
    ("get-usage-plan", get_usage_plan);
    ("get-usage-plan-key", get_usage_plan_key);
    ("get-usage-plan-keys", get_usage_plan_keys);
    ("get-usage-plans", get_usage_plans);
    ("get-vpc-link", get_vpc_link);
    ("get-vpc-links", get_vpc_links);
    ("import-api-keys", import_api_keys);
    ("import-documentation-parts", import_documentation_parts);
    ("import-rest-api", import_rest_api);
    ("put-gateway-response", put_gateway_response);
    ("put-integration", put_integration);
    ("put-integration-response", put_integration_response);
    ("put-method", put_method);
    ("put-method-response", put_method_response);
    ("put-rest-api", put_rest_api);
    ("reject-domain-name-access-association",
      reject_domain_name_access_association);
    ("tag-resource", tag_resource);
    ("test-invoke-authorizer", test_invoke_authorizer);
    ("test-invoke-method", test_invoke_method);
    ("untag-resource", untag_resource);
    ("update-account", update_account);
    ("update-api-key", update_api_key);
    ("update-authorizer", update_authorizer);
    ("update-base-path-mapping", update_base_path_mapping);
    ("update-client-certificate", update_client_certificate);
    ("update-deployment", update_deployment);
    ("update-documentation-part", update_documentation_part);
    ("update-documentation-version", update_documentation_version);
    ("update-domain-name", update_domain_name);
    ("update-gateway-response", update_gateway_response);
    ("update-integration", update_integration);
    ("update-integration-response", update_integration_response);
    ("update-method", update_method);
    ("update-method-response", update_method_response);
    ("update-model", update_model);
    ("update-request-validator", update_request_validator);
    ("update-resource", update_resource);
    ("update-rest-api", update_rest_api);
    ("update-stage", update_stage);
    ("update-usage", update_usage);
    ("update-usage-plan", update_usage_plan);
    ("update-vpc-link", update_vpc_link)]
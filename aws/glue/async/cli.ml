(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_create_partition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and partitionInputList =
         flag "partition-input-list" (required json_arg)
           ~doc:"JSON PartitionInputList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_partition
           (Values.BatchCreatePartitionRequest.make ?catalogId ~databaseName
              ~tableName
              ~partitionInputList:(Values.PartitionInputList.of_json
                                     partitionInputList) ())
           (Some Values.BatchCreatePartitionResponse.to_json)
           (Some Values.BatchCreatePartitionResponse.error_to_json)])
let batch_delete_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and connectionNameList =
         flag "connection-name-list" (required json_arg)
           ~doc:"JSON DeleteConnectionNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_connection
           (Values.BatchDeleteConnectionRequest.make ?catalogId
              ~connectionNameList:(Values.DeleteConnectionNameList.of_json
                                     connectionNameList) ())
           (Some Values.BatchDeleteConnectionResponse.to_json)
           (Some Values.BatchDeleteConnectionResponse.error_to_json)])
let batch_delete_partition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and partitionsToDelete =
         flag "partitions-to-delete" (required json_arg)
           ~doc:"JSON BatchDeletePartitionValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_partition
           (Values.BatchDeletePartitionRequest.make ?catalogId ~databaseName
              ~tableName
              ~partitionsToDelete:(Values.BatchDeletePartitionValueList.of_json
                                     partitionsToDelete) ())
           (Some Values.BatchDeletePartitionResponse.to_json)
           (Some Values.BatchDeletePartitionResponse.error_to_json)])
let batch_delete_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and transactionId =
         flag "transaction-id" (optional string)
           ~doc:"STRING TransactionIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tablesToDelete =
         flag "tables-to-delete" (required json_arg)
           ~doc:"JSON BatchDeleteTableNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_table
           (Values.BatchDeleteTableRequest.make ?catalogId ?transactionId
              ~databaseName
              ~tablesToDelete:(Values.BatchDeleteTableNameList.of_json
                                 tablesToDelete) ())
           (Some Values.BatchDeleteTableResponse.to_json)
           (Some Values.BatchDeleteTableResponse.error_to_json)])
let batch_delete_table_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and versionIds =
         flag "version-ids" (required json_arg)
           ~doc:"JSON BatchDeleteTableVersionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_table_version
           (Values.BatchDeleteTableVersionRequest.make ?catalogId
              ~databaseName ~tableName
              ~versionIds:(Values.BatchDeleteTableVersionList.of_json
                             versionIds) ())
           (Some Values.BatchDeleteTableVersionResponse.to_json)
           (Some Values.BatchDeleteTableVersionResponse.error_to_json)])
let batch_get_blueprints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeBlueprint =
         flag "include-blueprint" (optional bool) ~doc:"BOOL NullableBoolean"
       and includeParameterSpec =
         flag "include-parameter-spec" (optional bool)
           ~doc:"BOOL NullableBoolean"
       and names =
         flag "names" (required json_arg) ~doc:"JSON BatchGetBlueprintNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_blueprints
           (Values.BatchGetBlueprintsRequest.make ?includeBlueprint
              ?includeParameterSpec
              ~names:(Values.BatchGetBlueprintNames.of_json names) ())
           (Some Values.BatchGetBlueprintsResponse.to_json)
           (Some Values.BatchGetBlueprintsResponse.error_to_json)])
let batch_get_crawlers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crawlerNames =
         flag "crawler-names" (required json_arg) ~doc:"JSON CrawlerNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_crawlers
           (Values.BatchGetCrawlersRequest.make
              ~crawlerNames:(Values.CrawlerNameList.of_json crawlerNames) ())
           (Some Values.BatchGetCrawlersResponse.to_json)
           (Some Values.BatchGetCrawlersResponse.error_to_json)])
let batch_get_custom_entity_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names =
         flag "names" (required json_arg) ~doc:"JSON CustomEntityTypeNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_custom_entity_types
           (Values.BatchGetCustomEntityTypesRequest.make
              ~names:(Values.CustomEntityTypeNames.of_json names) ())
           (Some Values.BatchGetCustomEntityTypesResponse.to_json)
           (Some Values.BatchGetCustomEntityTypesResponse.error_to_json)])
let batch_get_data_quality_result =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resultIds =
         flag "result-ids" (required json_arg)
           ~doc:"JSON DataQualityResultIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_data_quality_result
           (Values.BatchGetDataQualityResultRequest.make
              ~resultIds:(Values.DataQualityResultIds.of_json resultIds) ())
           (Some Values.BatchGetDataQualityResultResponse.to_json)
           (Some Values.BatchGetDataQualityResultResponse.error_to_json)])
let batch_get_dev_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and devEndpointNames =
         flag "dev-endpoint-names" (required json_arg)
           ~doc:"JSON DevEndpointNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_dev_endpoints
           (Values.BatchGetDevEndpointsRequest.make
              ~devEndpointNames:(Values.DevEndpointNames.of_json
                                   devEndpointNames) ())
           (Some Values.BatchGetDevEndpointsResponse.to_json)
           (Some Values.BatchGetDevEndpointsResponse.error_to_json)])
let batch_get_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobNames =
         flag "job-names" (required json_arg) ~doc:"JSON JobNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_jobs
           (Values.BatchGetJobsRequest.make
              ~jobNames:(Values.JobNameList.of_json jobNames) ())
           (Some Values.BatchGetJobsResponse.to_json)
           (Some Values.BatchGetJobsResponse.error_to_json)])
let batch_get_partition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and auditContext =
         flag "audit-context" (optional json_arg) ~doc:"JSON AuditContext"
       and querySessionContext =
         flag "query-session-context" (optional json_arg)
           ~doc:"JSON QuerySessionContext"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and partitionsToGet =
         flag "partitions-to-get" (required json_arg)
           ~doc:"JSON BatchGetPartitionValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_partition
           (Values.BatchGetPartitionRequest.make ?catalogId
              ?auditContext:(Option.map ~f:Values.AuditContext.of_json
                               auditContext)
              ?querySessionContext:(Option.map
                                      ~f:Values.QuerySessionContext.of_json
                                      querySessionContext) ~databaseName
              ~tableName
              ~partitionsToGet:(Values.BatchGetPartitionValueList.of_json
                                  partitionsToGet) ())
           (Some Values.BatchGetPartitionResponse.to_json)
           (Some Values.BatchGetPartitionResponse.error_to_json)])
let batch_get_table_optimizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entries =
         flag "entries" (required json_arg)
           ~doc:"JSON BatchGetTableOptimizerEntries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_table_optimizer
           (Values.BatchGetTableOptimizerRequest.make
              ~entries:(Values.BatchGetTableOptimizerEntries.of_json entries)
              ()) (Some Values.BatchGetTableOptimizerResponse.to_json)
           (Some Values.BatchGetTableOptimizerResponse.error_to_json)])
let batch_get_triggers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and triggerNames =
         flag "trigger-names" (required json_arg) ~doc:"JSON TriggerNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_triggers
           (Values.BatchGetTriggersRequest.make
              ~triggerNames:(Values.TriggerNameList.of_json triggerNames) ())
           (Some Values.BatchGetTriggersResponse.to_json)
           (Some Values.BatchGetTriggersResponse.error_to_json)])
let batch_get_workflows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeGraph =
         flag "include-graph" (optional bool) ~doc:"BOOL NullableBoolean"
       and names = flag "names" (required json_arg) ~doc:"JSON WorkflowNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_workflows
           (Values.BatchGetWorkflowsRequest.make ?includeGraph
              ~names:(Values.WorkflowNames.of_json names) ())
           (Some Values.BatchGetWorkflowsResponse.to_json)
           (Some Values.BatchGetWorkflowsResponse.error_to_json)])
let batch_put_data_quality_statistic_annotation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING HashString"
       and inclusionAnnotations =
         flag "inclusion-annotations" (required json_arg)
           ~doc:"JSON InclusionAnnotationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_put_data_quality_statistic_annotation
           (Values.BatchPutDataQualityStatisticAnnotationRequest.make
              ?clientToken
              ~inclusionAnnotations:(Values.InclusionAnnotationList.of_json
                                       inclusionAnnotations) ())
           (Some
              Values.BatchPutDataQualityStatisticAnnotationResponse.to_json)
           (Some
              Values.BatchPutDataQualityStatisticAnnotationResponse.error_to_json)])
let batch_stop_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName =
         flag "job-name" (required string) ~doc:"STRING NameString"
       and jobRunIds =
         flag "job-run-ids" (required json_arg)
           ~doc:"JSON BatchStopJobRunJobRunIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_stop_job_run
           (Values.BatchStopJobRunRequest.make ~jobName
              ~jobRunIds:(Values.BatchStopJobRunJobRunIdList.of_json
                            jobRunIds) ())
           (Some Values.BatchStopJobRunResponse.to_json)
           (Some Values.BatchStopJobRunResponse.error_to_json)])
let batch_update_partition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and entries =
         flag "entries" (required json_arg)
           ~doc:"JSON BatchUpdatePartitionRequestEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_partition
           (Values.BatchUpdatePartitionRequest.make ?catalogId ~databaseName
              ~tableName
              ~entries:(Values.BatchUpdatePartitionRequestEntryList.of_json
                          entries) ())
           (Some Values.BatchUpdatePartitionResponse.to_json)
           (Some Values.BatchUpdatePartitionResponse.error_to_json)])
let cancel_data_quality_rule_recommendation_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and runId = flag "run-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_data_quality_rule_recommendation_run
           (Values.CancelDataQualityRuleRecommendationRunRequest.make ~runId
              ())
           (Some
              Values.CancelDataQualityRuleRecommendationRunResponse.to_json)
           (Some
              Values.CancelDataQualityRuleRecommendationRunResponse.error_to_json)])
let cancel_data_quality_ruleset_evaluation_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and runId = flag "run-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_data_quality_ruleset_evaluation_run
           (Values.CancelDataQualityRulesetEvaluationRunRequest.make ~runId
              ())
           (Some Values.CancelDataQualityRulesetEvaluationRunResponse.to_json)
           (Some
              Values.CancelDataQualityRulesetEvaluationRunResponse.error_to_json)])
let cancel_m_l_task_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transformId =
         flag "transform-id" (required string) ~doc:"STRING HashString"
       and taskRunId =
         flag "task-run-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_m_l_task_run
           (Values.CancelMLTaskRunRequest.make ~transformId ~taskRunId ())
           (Some Values.CancelMLTaskRunResponse.to_json)
           (Some Values.CancelMLTaskRunResponse.error_to_json)])
let cancel_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestOrigin =
         flag "request-origin" (optional string)
           ~doc:"STRING OrchestrationNameString"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING NameString"
       and id = flag "id" (required int) ~doc:"INT IntegerValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_statement
           (Values.CancelStatementRequest.make ?requestOrigin ~sessionId ~id
              ()) (Some Values.CancelStatementResponse.to_json)
           (Some Values.CancelStatementResponse.error_to_json)])
let check_schema_version_validity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataFormat =
         flag "data-format" (required json_arg) ~doc:"JSON DataFormat"
       and schemaDefinition =
         flag "schema-definition" (required string)
           ~doc:"STRING SchemaDefinitionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.check_schema_version_validity
           (Values.CheckSchemaVersionValidityInput.make
              ~dataFormat:(Values.DataFormat.of_json dataFormat)
              ~schemaDefinition ())
           (Some Values.CheckSchemaVersionValidityResponse.to_json)
           (Some Values.CheckSchemaVersionValidityResponse.error_to_json)])
let create_blueprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING Generic512CharString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name =
         flag "name" (required string) ~doc:"STRING OrchestrationNameString"
       and blueprintLocation =
         flag "blueprint-location" (required string)
           ~doc:"STRING OrchestrationS3Location" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_blueprint
           (Values.CreateBlueprintRequest.make ?description
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name
              ~blueprintLocation ())
           (Some Values.CreateBlueprintResponse.to_json)
           (Some Values.CreateBlueprintResponse.error_to_json)])
let create_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name =
         flag "name" (required string) ~doc:"STRING CatalogNameString"
       and catalogInput =
         flag "catalog-input" (required json_arg) ~doc:"JSON CatalogInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_catalog
           (Values.CreateCatalogRequest.make
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name
              ~catalogInput:(Values.CatalogInput.of_json catalogInput) ())
           (Some Values.CreateCatalogResponse.to_json)
           (Some Values.CreateCatalogResponse.error_to_json)])
let create_classifier =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grokClassifier =
         flag "grok-classifier" (optional json_arg)
           ~doc:"JSON CreateGrokClassifierRequest"
       and xMLClassifier =
         flag "x-m-l-classifier" (optional json_arg)
           ~doc:"JSON CreateXMLClassifierRequest"
       and jsonClassifier =
         flag "json-classifier" (optional json_arg)
           ~doc:"JSON CreateJsonClassifierRequest"
       and csvClassifier =
         flag "csv-classifier" (optional json_arg)
           ~doc:"JSON CreateCsvClassifierRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_classifier
           (Values.CreateClassifierRequest.make
              ?grokClassifier:(Option.map
                                 ~f:Values.CreateGrokClassifierRequest.of_json
                                 grokClassifier)
              ?xMLClassifier:(Option.map
                                ~f:Values.CreateXMLClassifierRequest.of_json
                                xMLClassifier)
              ?jsonClassifier:(Option.map
                                 ~f:Values.CreateJsonClassifierRequest.of_json
                                 jsonClassifier)
              ?csvClassifier:(Option.map
                                ~f:Values.CreateCsvClassifierRequest.of_json
                                csvClassifier) ())
           (Some Values.CreateClassifierResponse.to_json)
           (Some Values.CreateClassifierResponse.error_to_json)])
let create_column_statistics_task_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schedule =
         flag "schedule" (optional string) ~doc:"STRING CronExpression"
       and columnNameList =
         flag "column-name-list" (optional json_arg)
           ~doc:"JSON ColumnNameList"
       and sampleSize =
         flag "sample-size" (optional float)
           ~doc:"FLOAT SampleSizePercentage"
       and catalogID =
         flag "catalog-i-d" (optional string) ~doc:"STRING NameString"
       and securityConfiguration =
         flag "security-configuration" (optional string)
           ~doc:"STRING NameString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and role = flag "role" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_column_statistics_task_settings
           (Values.CreateColumnStatisticsTaskSettingsRequest.make ?schedule
              ?columnNameList:(Option.map ~f:Values.ColumnNameList.of_json
                                 columnNameList) ?sampleSize ?catalogID
              ?securityConfiguration
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~databaseName
              ~tableName ~role ())
           (Some Values.CreateColumnStatisticsTaskSettingsResponse.to_json)
           (Some
              Values.CreateColumnStatisticsTaskSettingsResponse.error_to_json)])
let create_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and connectionInput =
         flag "connection-input" (required json_arg)
           ~doc:"JSON ConnectionInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connection
           (Values.CreateConnectionRequest.make ?catalogId
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~connectionInput:(Values.ConnectionInput.of_json
                                  connectionInput) ())
           (Some Values.CreateConnectionResponse.to_json)
           (Some Values.CreateConnectionResponse.error_to_json)])
let create_crawler =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (optional string) ~doc:"STRING DatabaseName"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and schedule =
         flag "schedule" (optional string) ~doc:"STRING CronExpression"
       and classifiers =
         flag "classifiers" (optional json_arg)
           ~doc:"JSON ClassifierNameList"
       and tablePrefix =
         flag "table-prefix" (optional string) ~doc:"STRING TablePrefix"
       and schemaChangePolicy =
         flag "schema-change-policy" (optional json_arg)
           ~doc:"JSON SchemaChangePolicy"
       and recrawlPolicy =
         flag "recrawl-policy" (optional json_arg) ~doc:"JSON RecrawlPolicy"
       and lineageConfiguration =
         flag "lineage-configuration" (optional json_arg)
           ~doc:"JSON LineageConfiguration"
       and lakeFormationConfiguration =
         flag "lake-formation-configuration" (optional json_arg)
           ~doc:"JSON LakeFormationConfiguration"
       and configuration =
         flag "configuration" (optional string)
           ~doc:"STRING CrawlerConfiguration"
       and crawlerSecurityConfiguration =
         flag "crawler-security-configuration" (optional string)
           ~doc:"STRING CrawlerSecurityConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and role = flag "role" (required string) ~doc:"STRING Role"
       and targets =
         flag "targets" (required json_arg) ~doc:"JSON CrawlerTargets" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_crawler
           (Values.CreateCrawlerRequest.make ?databaseName ?description
              ?schedule
              ?classifiers:(Option.map ~f:Values.ClassifierNameList.of_json
                              classifiers) ?tablePrefix
              ?schemaChangePolicy:(Option.map
                                     ~f:Values.SchemaChangePolicy.of_json
                                     schemaChangePolicy)
              ?recrawlPolicy:(Option.map ~f:Values.RecrawlPolicy.of_json
                                recrawlPolicy)
              ?lineageConfiguration:(Option.map
                                       ~f:Values.LineageConfiguration.of_json
                                       lineageConfiguration)
              ?lakeFormationConfiguration:(Option.map
                                             ~f:Values.LakeFormationConfiguration.of_json
                                             lakeFormationConfiguration)
              ?configuration ?crawlerSecurityConfiguration
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name ~role
              ~targets:(Values.CrawlerTargets.of_json targets) ())
           (Some Values.CreateCrawlerResponse.to_json)
           (Some Values.CreateCrawlerResponse.error_to_json)])
let create_custom_entity_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contextWords =
         flag "context-words" (optional json_arg) ~doc:"JSON ContextWords"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and regexString =
         flag "regex-string" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_entity_type
           (Values.CreateCustomEntityTypeRequest.make
              ?contextWords:(Option.map ~f:Values.ContextWords.of_json
                               contextWords)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name
              ~regexString ())
           (Some Values.CreateCustomEntityTypeResponse.to_json)
           (Some Values.CreateCustomEntityTypeResponse.error_to_json)])
let create_data_quality_ruleset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and targetTable =
         flag "target-table" (optional json_arg)
           ~doc:"JSON DataQualityTargetTable"
       and dataQualitySecurityConfiguration =
         flag "data-quality-security-configuration" (optional string)
           ~doc:"STRING NameString"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING HashString"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and ruleset =
         flag "ruleset" (required string)
           ~doc:"STRING DataQualityRulesetString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_quality_ruleset
           (Values.CreateDataQualityRulesetRequest.make ?description
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?targetTable:(Option.map
                              ~f:Values.DataQualityTargetTable.of_json
                              targetTable) ?dataQualitySecurityConfiguration
              ?clientToken ~name ~ruleset ())
           (Some Values.CreateDataQualityRulesetResponse.to_json)
           (Some Values.CreateDataQualityRulesetResponse.error_to_json)])
let create_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and databaseInput =
         flag "database-input" (required json_arg) ~doc:"JSON DatabaseInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_database
           (Values.CreateDatabaseRequest.make ?catalogId
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~databaseInput:(Values.DatabaseInput.of_json databaseInput) ())
           (Some Values.CreateDatabaseResponse.to_json)
           (Some Values.CreateDatabaseResponse.error_to_json)])
let create_dev_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg) ~doc:"JSON StringList"
       and subnetId =
         flag "subnet-id" (optional string) ~doc:"STRING GenericString"
       and publicKey =
         flag "public-key" (optional string) ~doc:"STRING GenericString"
       and publicKeys =
         flag "public-keys" (optional json_arg) ~doc:"JSON PublicKeysList"
       and numberOfNodes =
         flag "number-of-nodes" (optional int) ~doc:"INT IntegerValue"
       and workerType =
         flag "worker-type" (optional json_arg) ~doc:"JSON WorkerType"
       and glueVersion =
         flag "glue-version" (optional string)
           ~doc:"STRING GlueVersionString"
       and numberOfWorkers =
         flag "number-of-workers" (optional int) ~doc:"INT NullableInteger"
       and extraPythonLibsS3Path =
         flag "extra-python-libs-s3-path" (optional string)
           ~doc:"STRING GenericString"
       and extraJarsS3Path =
         flag "extra-jars-s3-path" (optional string)
           ~doc:"STRING GenericString"
       and securityConfiguration =
         flag "security-configuration" (optional string)
           ~doc:"STRING NameString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and arguments =
         flag "arguments" (optional json_arg) ~doc:"JSON MapValue"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING GenericString"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dev_endpoint
           (Values.CreateDevEndpointRequest.make
              ?securityGroupIds:(Option.map ~f:Values.StringList.of_json
                                   securityGroupIds) ?subnetId ?publicKey
              ?publicKeys:(Option.map ~f:Values.PublicKeysList.of_json
                             publicKeys) ?numberOfNodes
              ?workerType:(Option.map ~f:Values.WorkerType.of_json workerType)
              ?glueVersion ?numberOfWorkers ?extraPythonLibsS3Path
              ?extraJarsS3Path ?securityConfiguration
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?arguments:(Option.map ~f:Values.MapValue.of_json arguments)
              ~endpointName ~roleArn ())
           (Some Values.CreateDevEndpointResponse.to_json)
           (Some Values.CreateDevEndpointResponse.error_to_json)])
let create_glue_identity_center_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scopes =
         flag "scopes" (optional json_arg)
           ~doc:"JSON IdentityCenterScopesList"
       and userBackgroundSessionsEnabled =
         flag "user-background-sessions-enabled" (optional bool)
           ~doc:"BOOL NullableBoolean"
       and instanceArn =
         flag "instance-arn" (required string)
           ~doc:"STRING IdentityCenterInstanceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_glue_identity_center_configuration
           (Values.CreateGlueIdentityCenterConfigurationRequest.make
              ?scopes:(Option.map ~f:Values.IdentityCenterScopesList.of_json
                         scopes) ?userBackgroundSessionsEnabled ~instanceArn
              ())
           (Some Values.CreateGlueIdentityCenterConfigurationResponse.to_json)
           (Some
              Values.CreateGlueIdentityCenterConfigurationResponse.error_to_json)])
let create_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING IntegrationDescription"
       and dataFilter =
         flag "data-filter" (optional string) ~doc:"STRING String2048"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String2048"
       and additionalEncryptionContext =
         flag "additional-encryption-context" (optional json_arg)
           ~doc:"JSON IntegrationAdditionalEncryptionContextMap"
       and tags =
         flag "tags" (optional json_arg) ~doc:"JSON IntegrationTagsList"
       and integrationConfig =
         flag "integration-config" (optional json_arg)
           ~doc:"JSON IntegrationConfig"
       and integrationName =
         flag "integration-name" (required string) ~doc:"STRING String128"
       and sourceArn =
         flag "source-arn" (required string) ~doc:"STRING String512"
       and targetArn =
         flag "target-arn" (required string) ~doc:"STRING String512" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_integration
           (Values.CreateIntegrationRequest.make ?description ?dataFilter
              ?kmsKeyId
              ?additionalEncryptionContext:(Option.map
                                              ~f:Values.IntegrationAdditionalEncryptionContextMap.of_json
                                              additionalEncryptionContext)
              ?tags:(Option.map ~f:Values.IntegrationTagsList.of_json tags)
              ?integrationConfig:(Option.map
                                    ~f:Values.IntegrationConfig.of_json
                                    integrationConfig) ~integrationName
              ~sourceArn ~targetArn ())
           (Some Values.CreateIntegrationResponse.to_json)
           (Some Values.CreateIntegrationResponse.error_to_json)])
let create_integration_resource_property =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceProcessingProperties =
         flag "source-processing-properties" (optional json_arg)
           ~doc:"JSON SourceProcessingProperties"
       and targetProcessingProperties =
         flag "target-processing-properties" (optional json_arg)
           ~doc:"JSON TargetProcessingProperties"
       and tags =
         flag "tags" (optional json_arg) ~doc:"JSON IntegrationTagsList"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String512" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_integration_resource_property
           (Values.CreateIntegrationResourcePropertyRequest.make
              ?sourceProcessingProperties:(Option.map
                                             ~f:Values.SourceProcessingProperties.of_json
                                             sourceProcessingProperties)
              ?targetProcessingProperties:(Option.map
                                             ~f:Values.TargetProcessingProperties.of_json
                                             targetProcessingProperties)
              ?tags:(Option.map ~f:Values.IntegrationTagsList.of_json tags)
              ~resourceArn ())
           (Some Values.CreateIntegrationResourcePropertyResponse.to_json)
           (Some
              Values.CreateIntegrationResourcePropertyResponse.error_to_json)])
let create_integration_table_properties =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceTableConfig =
         flag "source-table-config" (optional json_arg)
           ~doc:"JSON SourceTableConfig"
       and targetTableConfig =
         flag "target-table-config" (optional json_arg)
           ~doc:"JSON TargetTableConfig"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String512"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING String128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_integration_table_properties
           (Values.CreateIntegrationTablePropertiesRequest.make
              ?sourceTableConfig:(Option.map
                                    ~f:Values.SourceTableConfig.of_json
                                    sourceTableConfig)
              ?targetTableConfig:(Option.map
                                    ~f:Values.TargetTableConfig.of_json
                                    targetTableConfig) ~resourceArn
              ~tableName ())
           (Some Values.CreateIntegrationTablePropertiesResponse.to_json)
           (Some
              Values.CreateIntegrationTablePropertiesResponse.error_to_json)])
let create_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobMode = flag "job-mode" (optional json_arg) ~doc:"JSON JobMode"
       and jobRunQueuingEnabled =
         flag "job-run-queuing-enabled" (optional bool)
           ~doc:"BOOL NullableBoolean"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and logUri = flag "log-uri" (optional string) ~doc:"STRING UriString"
       and executionProperty =
         flag "execution-property" (optional json_arg)
           ~doc:"JSON ExecutionProperty"
       and defaultArguments =
         flag "default-arguments" (optional json_arg) ~doc:"JSON GenericMap"
       and nonOverridableArguments =
         flag "non-overridable-arguments" (optional json_arg)
           ~doc:"JSON GenericMap"
       and connections =
         flag "connections" (optional json_arg) ~doc:"JSON ConnectionsList"
       and maxRetries =
         flag "max-retries" (optional int) ~doc:"INT MaxRetries"
       and allocatedCapacity =
         flag "allocated-capacity" (optional int) ~doc:"INT IntegerValue"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and maxCapacity =
         flag "max-capacity" (optional float) ~doc:"FLOAT NullableDouble"
       and securityConfiguration =
         flag "security-configuration" (optional string)
           ~doc:"STRING NameString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and notificationProperty =
         flag "notification-property" (optional json_arg)
           ~doc:"JSON NotificationProperty"
       and glueVersion =
         flag "glue-version" (optional string)
           ~doc:"STRING GlueVersionString"
       and numberOfWorkers =
         flag "number-of-workers" (optional int) ~doc:"INT NullableInteger"
       and workerType =
         flag "worker-type" (optional json_arg) ~doc:"JSON WorkerType"
       and codeGenConfigurationNodes =
         flag "code-gen-configuration-nodes" (optional json_arg)
           ~doc:"JSON CodeGenConfigurationNodes"
       and executionClass =
         flag "execution-class" (optional json_arg)
           ~doc:"JSON ExecutionClass"
       and sourceControlDetails =
         flag "source-control-details" (optional json_arg)
           ~doc:"JSON SourceControlDetails"
       and maintenanceWindow =
         flag "maintenance-window" (optional string)
           ~doc:"STRING MaintenanceWindow"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and role = flag "role" (required string) ~doc:"STRING RoleString"
       and command =
         flag "command" (required json_arg) ~doc:"JSON JobCommand" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_job
           (Values.CreateJobRequest.make
              ?jobMode:(Option.map ~f:Values.JobMode.of_json jobMode)
              ?jobRunQueuingEnabled ?description ?logUri
              ?executionProperty:(Option.map
                                    ~f:Values.ExecutionProperty.of_json
                                    executionProperty)
              ?defaultArguments:(Option.map ~f:Values.GenericMap.of_json
                                   defaultArguments)
              ?nonOverridableArguments:(Option.map
                                          ~f:Values.GenericMap.of_json
                                          nonOverridableArguments)
              ?connections:(Option.map ~f:Values.ConnectionsList.of_json
                              connections) ?maxRetries ?allocatedCapacity
              ?timeout ?maxCapacity ?securityConfiguration
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?notificationProperty:(Option.map
                                       ~f:Values.NotificationProperty.of_json
                                       notificationProperty) ?glueVersion
              ?numberOfWorkers
              ?workerType:(Option.map ~f:Values.WorkerType.of_json workerType)
              ?codeGenConfigurationNodes:(Option.map
                                            ~f:Values.CodeGenConfigurationNodes.of_json
                                            codeGenConfigurationNodes)
              ?executionClass:(Option.map ~f:Values.ExecutionClass.of_json
                                 executionClass)
              ?sourceControlDetails:(Option.map
                                       ~f:Values.SourceControlDetails.of_json
                                       sourceControlDetails)
              ?maintenanceWindow ~name ~role
              ~command:(Values.JobCommand.of_json command) ())
           (Some Values.CreateJobResponse.to_json)
           (Some Values.CreateJobResponse.error_to_json)])
let create_m_l_transform =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and glueVersion =
         flag "glue-version" (optional string)
           ~doc:"STRING GlueVersionString"
       and maxCapacity =
         flag "max-capacity" (optional float) ~doc:"FLOAT NullableDouble"
       and workerType =
         flag "worker-type" (optional json_arg) ~doc:"JSON WorkerType"
       and numberOfWorkers =
         flag "number-of-workers" (optional int) ~doc:"INT NullableInteger"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and maxRetries =
         flag "max-retries" (optional int) ~doc:"INT NullableInteger"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and transformEncryption =
         flag "transform-encryption" (optional json_arg)
           ~doc:"JSON TransformEncryption"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and inputRecordTables =
         flag "input-record-tables" (required json_arg)
           ~doc:"JSON GlueTables"
       and parameters =
         flag "parameters" (required json_arg)
           ~doc:"JSON TransformParameters"
       and role = flag "role" (required string) ~doc:"STRING RoleString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_m_l_transform
           (Values.CreateMLTransformRequest.make ?description ?glueVersion
              ?maxCapacity
              ?workerType:(Option.map ~f:Values.WorkerType.of_json workerType)
              ?numberOfWorkers ?timeout ?maxRetries
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?transformEncryption:(Option.map
                                      ~f:Values.TransformEncryption.of_json
                                      transformEncryption) ~name
              ~inputRecordTables:(Values.GlueTables.of_json inputRecordTables)
              ~parameters:(Values.TransformParameters.of_json parameters)
              ~role ()) (Some Values.CreateMLTransformResponse.to_json)
           (Some Values.CreateMLTransformResponse.error_to_json)])
let create_partition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and partitionInput =
         flag "partition-input" (required json_arg)
           ~doc:"JSON PartitionInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_partition
           (Values.CreatePartitionRequest.make ?catalogId ~databaseName
              ~tableName
              ~partitionInput:(Values.PartitionInput.of_json partitionInput)
              ()) (Some Values.CreatePartitionResponse.to_json)
           (Some Values.CreatePartitionResponse.error_to_json)])
let create_partition_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and partitionIndex =
         flag "partition-index" (required json_arg)
           ~doc:"JSON PartitionIndex" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_partition_index
           (Values.CreatePartitionIndexRequest.make ?catalogId ~databaseName
              ~tableName
              ~partitionIndex:(Values.PartitionIndex.of_json partitionIndex)
              ()) (Some Values.CreatePartitionIndexResponse.to_json)
           (Some Values.CreatePartitionIndexResponse.error_to_json)])
let create_registry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and registryName =
         flag "registry-name" (required string)
           ~doc:"STRING SchemaRegistryNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_registry
           (Values.CreateRegistryInput.make ?description
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~registryName
              ()) (Some Values.CreateRegistryResponse.to_json)
           (Some Values.CreateRegistryResponse.error_to_json)])
let create_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional json_arg) ~doc:"JSON RegistryId"
       and compatibility =
         flag "compatibility" (optional json_arg) ~doc:"JSON Compatibility"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and schemaDefinition =
         flag "schema-definition" (optional string)
           ~doc:"STRING SchemaDefinitionString"
       and schemaName =
         flag "schema-name" (required string)
           ~doc:"STRING SchemaRegistryNameString"
       and dataFormat =
         flag "data-format" (required json_arg) ~doc:"JSON DataFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_schema
           (Values.CreateSchemaInput.make
              ?registryId:(Option.map ~f:Values.RegistryId.of_json registryId)
              ?compatibility:(Option.map ~f:Values.Compatibility.of_json
                                compatibility) ?description
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?schemaDefinition ~schemaName
              ~dataFormat:(Values.DataFormat.of_json dataFormat) ())
           (Some Values.CreateSchemaResponse.to_json)
           (Some Values.CreateSchemaResponse.error_to_json)])
let create_script =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dagNodes =
         flag "dag-nodes" (optional json_arg) ~doc:"JSON DagNodes"
       and dagEdges =
         flag "dag-edges" (optional json_arg) ~doc:"JSON DagEdges"
       and language =
         flag "language" (optional json_arg) ~doc:"JSON Language" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_script
           (Values.CreateScriptRequest.make
              ?dagNodes:(Option.map ~f:Values.DagNodes.of_json dagNodes)
              ?dagEdges:(Option.map ~f:Values.DagEdges.of_json dagEdges)
              ?language:(Option.map ~f:Values.Language.of_json language) ())
           (Some Values.CreateScriptResponse.to_json)
           (Some Values.CreateScriptResponse.error_to_json)])
let create_security_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and encryptionConfiguration =
         flag "encryption-configuration" (required json_arg)
           ~doc:"JSON EncryptionConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_security_configuration
           (Values.CreateSecurityConfigurationRequest.make ~name
              ~encryptionConfiguration:(Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration) ())
           (Some Values.CreateSecurityConfigurationResponse.to_json)
           (Some Values.CreateSecurityConfigurationResponse.error_to_json)])
let create_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and idleTimeout =
         flag "idle-timeout" (optional int) ~doc:"INT Timeout"
       and defaultArguments =
         flag "default-arguments" (optional json_arg)
           ~doc:"JSON OrchestrationArgumentsMap"
       and connections =
         flag "connections" (optional json_arg) ~doc:"JSON ConnectionsList"
       and maxCapacity =
         flag "max-capacity" (optional float) ~doc:"FLOAT NullableDouble"
       and numberOfWorkers =
         flag "number-of-workers" (optional int) ~doc:"INT NullableInteger"
       and workerType =
         flag "worker-type" (optional json_arg) ~doc:"JSON WorkerType"
       and securityConfiguration =
         flag "security-configuration" (optional string)
           ~doc:"STRING NameString"
       and glueVersion =
         flag "glue-version" (optional string)
           ~doc:"STRING GlueVersionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and requestOrigin =
         flag "request-origin" (optional string)
           ~doc:"STRING OrchestrationNameString"
       and id = flag "id" (required string) ~doc:"STRING NameString"
       and role =
         flag "role" (required string) ~doc:"STRING OrchestrationRoleArn"
       and command =
         flag "command" (required json_arg) ~doc:"JSON SessionCommand" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_session
           (Values.CreateSessionRequest.make ?description ?timeout
              ?idleTimeout
              ?defaultArguments:(Option.map
                                   ~f:Values.OrchestrationArgumentsMap.of_json
                                   defaultArguments)
              ?connections:(Option.map ~f:Values.ConnectionsList.of_json
                              connections) ?maxCapacity ?numberOfWorkers
              ?workerType:(Option.map ~f:Values.WorkerType.of_json workerType)
              ?securityConfiguration ?glueVersion
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?requestOrigin ~id ~role
              ~command:(Values.SessionCommand.of_json command) ())
           (Some Values.CreateSessionResponse.to_json)
           (Some Values.CreateSessionResponse.error_to_json)])
let create_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and name = flag "name" (optional string) ~doc:"STRING NameString"
       and tableInput =
         flag "table-input" (optional json_arg) ~doc:"JSON TableInput"
       and partitionIndexes =
         flag "partition-indexes" (optional json_arg)
           ~doc:"JSON PartitionIndexList"
       and transactionId =
         flag "transaction-id" (optional string)
           ~doc:"STRING TransactionIdString"
       and openTableFormatInput =
         flag "open-table-format-input" (optional json_arg)
           ~doc:"JSON OpenTableFormatInput"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_table
           (Values.CreateTableRequest.make ?catalogId ?name
              ?tableInput:(Option.map ~f:Values.TableInput.of_json tableInput)
              ?partitionIndexes:(Option.map
                                   ~f:Values.PartitionIndexList.of_json
                                   partitionIndexes) ?transactionId
              ?openTableFormatInput:(Option.map
                                       ~f:Values.OpenTableFormatInput.of_json
                                       openTableFormatInput) ~databaseName ())
           (Some Values.CreateTableResponse.to_json)
           (Some Values.CreateTableResponse.error_to_json)])
let create_table_optimizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON TableOptimizerType"
       and tableOptimizerConfiguration =
         flag "table-optimizer-configuration" (required json_arg)
           ~doc:"JSON TableOptimizerConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_table_optimizer
           (Values.CreateTableOptimizerRequest.make ~catalogId ~databaseName
              ~tableName ~type_:(Values.TableOptimizerType.of_json type_)
              ~tableOptimizerConfiguration:(Values.TableOptimizerConfiguration.of_json
                                              tableOptimizerConfiguration) ())
           (Some Values.CreateTableOptimizerResponse.to_json)
           (Some Values.CreateTableOptimizerResponse.error_to_json)])
let create_trigger =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowName =
         flag "workflow-name" (optional string) ~doc:"STRING NameString"
       and schedule =
         flag "schedule" (optional string) ~doc:"STRING GenericString"
       and predicate =
         flag "predicate" (optional json_arg) ~doc:"JSON Predicate"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and startOnCreation =
         flag "start-on-creation" (optional bool) ~doc:"BOOL BooleanValue"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and eventBatchingCondition =
         flag "event-batching-condition" (optional json_arg)
           ~doc:"JSON EventBatchingCondition"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON TriggerType"
       and actions =
         flag "actions" (required json_arg) ~doc:"JSON ActionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_trigger
           (Values.CreateTriggerRequest.make ?workflowName ?schedule
              ?predicate:(Option.map ~f:Values.Predicate.of_json predicate)
              ?description ?startOnCreation
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?eventBatchingCondition:(Option.map
                                         ~f:Values.EventBatchingCondition.of_json
                                         eventBatchingCondition) ~name
              ~type_:(Values.TriggerType.of_json type_)
              ~actions:(Values.ActionList.of_json actions) ())
           (Some Values.CreateTriggerResponse.to_json)
           (Some Values.CreateTriggerResponse.error_to_json)])
let create_usage_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON ProfileConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_usage_profile
           (Values.CreateUsageProfileRequest.make ?description
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name
              ~configuration:(Values.ProfileConfiguration.of_json
                                configuration) ())
           (Some Values.CreateUsageProfileResponse.to_json)
           (Some Values.CreateUsageProfileResponse.error_to_json)])
let create_user_defined_function =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and functionInput =
         flag "function-input" (required json_arg)
           ~doc:"JSON UserDefinedFunctionInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user_defined_function
           (Values.CreateUserDefinedFunctionRequest.make ?catalogId
              ~databaseName
              ~functionInput:(Values.UserDefinedFunctionInput.of_json
                                functionInput) ())
           (Some Values.CreateUserDefinedFunctionResponse.to_json)
           (Some Values.CreateUserDefinedFunctionResponse.error_to_json)])
let create_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING WorkflowDescriptionString"
       and defaultRunProperties =
         flag "default-run-properties" (optional json_arg)
           ~doc:"JSON WorkflowRunProperties"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and maxConcurrentRuns =
         flag "max-concurrent-runs" (optional int) ~doc:"INT NullableInteger"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workflow
           (Values.CreateWorkflowRequest.make ?description
              ?defaultRunProperties:(Option.map
                                       ~f:Values.WorkflowRunProperties.of_json
                                       defaultRunProperties)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?maxConcurrentRuns ~name ())
           (Some Values.CreateWorkflowResponse.to_json)
           (Some Values.CreateWorkflowResponse.error_to_json)])
let delete_blueprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_blueprint (Values.DeleteBlueprintRequest.make ~name ())
           (Some Values.DeleteBlueprintResponse.to_json)
           (Some Values.DeleteBlueprintResponse.error_to_json)])
let delete_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING CatalogIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_catalog (Values.DeleteCatalogRequest.make ~catalogId ())
           (Some Values.DeleteCatalogResponse.to_json)
           (Some Values.DeleteCatalogResponse.error_to_json)])
let delete_classifier =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_classifier
           (Values.DeleteClassifierRequest.make ~name ())
           (Some Values.DeleteClassifierResponse.to_json)
           (Some Values.DeleteClassifierResponse.error_to_json)])
let delete_column_statistics_for_partition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and partitionValues =
         flag "partition-values" (required json_arg)
           ~doc:"JSON ValueStringList"
       and columnName =
         flag "column-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_column_statistics_for_partition
           (Values.DeleteColumnStatisticsForPartitionRequest.make ?catalogId
              ~databaseName ~tableName
              ~partitionValues:(Values.ValueStringList.of_json
                                  partitionValues) ~columnName ())
           (Some Values.DeleteColumnStatisticsForPartitionResponse.to_json)
           (Some
              Values.DeleteColumnStatisticsForPartitionResponse.error_to_json)])
let delete_column_statistics_for_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and columnName =
         flag "column-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_column_statistics_for_table
           (Values.DeleteColumnStatisticsForTableRequest.make ?catalogId
              ~databaseName ~tableName ~columnName ())
           (Some Values.DeleteColumnStatisticsForTableResponse.to_json)
           (Some Values.DeleteColumnStatisticsForTableResponse.error_to_json)])
let delete_column_statistics_task_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_column_statistics_task_settings
           (Values.DeleteColumnStatisticsTaskSettingsRequest.make
              ~databaseName ~tableName ())
           (Some Values.DeleteColumnStatisticsTaskSettingsResponse.to_json)
           (Some
              Values.DeleteColumnStatisticsTaskSettingsResponse.error_to_json)])
let delete_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and connectionName =
         flag "connection-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connection
           (Values.DeleteConnectionRequest.make ?catalogId ~connectionName ())
           (Some Values.DeleteConnectionResponse.to_json)
           (Some Values.DeleteConnectionResponse.error_to_json)])
let delete_connection_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionType =
         flag "connection-type" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connection_type
           (Values.DeleteConnectionTypeRequest.make ~connectionType ())
           (Some Values.DeleteConnectionTypeResponse.to_json)
           (Some Values.DeleteConnectionTypeResponse.error_to_json)])
let delete_crawler =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_crawler (Values.DeleteCrawlerRequest.make ~name ())
           (Some Values.DeleteCrawlerResponse.to_json)
           (Some Values.DeleteCrawlerResponse.error_to_json)])
let delete_custom_entity_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_entity_type
           (Values.DeleteCustomEntityTypeRequest.make ~name ())
           (Some Values.DeleteCustomEntityTypeResponse.to_json)
           (Some Values.DeleteCustomEntityTypeResponse.error_to_json)])
let delete_data_quality_ruleset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_quality_ruleset
           (Values.DeleteDataQualityRulesetRequest.make ~name ())
           (Some Values.DeleteDataQualityRulesetResponse.to_json)
           (Some Values.DeleteDataQualityRulesetResponse.error_to_json)])
let delete_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_database
           (Values.DeleteDatabaseRequest.make ?catalogId ~name ())
           (Some Values.DeleteDatabaseResponse.to_json)
           (Some Values.DeleteDatabaseResponse.error_to_json)])
let delete_dev_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dev_endpoint
           (Values.DeleteDevEndpointRequest.make ~endpointName ())
           (Some Values.DeleteDevEndpointResponse.to_json)
           (Some Values.DeleteDevEndpointResponse.error_to_json)])
let delete_glue_identity_center_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_glue_identity_center_configuration
           (Values.DeleteGlueIdentityCenterConfigurationRequest.make ())
           (Some Values.DeleteGlueIdentityCenterConfigurationResponse.to_json)
           (Some
              Values.DeleteGlueIdentityCenterConfigurationResponse.error_to_json)])
let delete_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and integrationIdentifier =
         flag "integration-identifier" (required string)
           ~doc:"STRING String128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_integration
           (Values.DeleteIntegrationRequest.make ~integrationIdentifier ())
           (Some Values.DeleteIntegrationResponse.to_json)
           (Some Values.DeleteIntegrationResponse.error_to_json)])
let delete_integration_resource_property =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String512" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_integration_resource_property
           (Values.DeleteIntegrationResourcePropertyRequest.make ~resourceArn
              ())
           (Some Values.DeleteIntegrationResourcePropertyResponse.to_json)
           (Some
              Values.DeleteIntegrationResourcePropertyResponse.error_to_json)])
let delete_integration_table_properties =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String512"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING String128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_integration_table_properties
           (Values.DeleteIntegrationTablePropertiesRequest.make ~resourceArn
              ~tableName ())
           (Some Values.DeleteIntegrationTablePropertiesResponse.to_json)
           (Some
              Values.DeleteIntegrationTablePropertiesResponse.error_to_json)])
let delete_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName =
         flag "job-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_job (Values.DeleteJobRequest.make ~jobName ())
           (Some Values.DeleteJobResponse.to_json)
           (Some Values.DeleteJobResponse.error_to_json)])
let delete_m_l_transform =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transformId =
         flag "transform-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_m_l_transform
           (Values.DeleteMLTransformRequest.make ~transformId ())
           (Some Values.DeleteMLTransformResponse.to_json)
           (Some Values.DeleteMLTransformResponse.error_to_json)])
let delete_partition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and partitionValues =
         flag "partition-values" (required json_arg)
           ~doc:"JSON ValueStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_partition
           (Values.DeletePartitionRequest.make ?catalogId ~databaseName
              ~tableName
              ~partitionValues:(Values.ValueStringList.of_json
                                  partitionValues) ())
           (Some Values.DeletePartitionResponse.to_json)
           (Some Values.DeletePartitionResponse.error_to_json)])
let delete_partition_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_partition_index
           (Values.DeletePartitionIndexRequest.make ?catalogId ~databaseName
              ~tableName ~indexName ())
           (Some Values.DeletePartitionIndexResponse.to_json)
           (Some Values.DeletePartitionIndexResponse.error_to_json)])
let delete_registry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (required json_arg) ~doc:"JSON RegistryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_registry
           (Values.DeleteRegistryInput.make
              ~registryId:(Values.RegistryId.of_json registryId) ())
           (Some Values.DeleteRegistryResponse.to_json)
           (Some Values.DeleteRegistryResponse.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyHashCondition =
         flag "policy-hash-condition" (optional string)
           ~doc:"STRING HashString"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING GlueResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyRequest.make ?policyHashCondition
              ?resourceArn ())
           (Some Values.DeleteResourcePolicyResponse.to_json)
           (Some Values.DeleteResourcePolicyResponse.error_to_json)])
let delete_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaId =
         flag "schema-id" (required json_arg) ~doc:"JSON SchemaId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_schema
           (Values.DeleteSchemaInput.make
              ~schemaId:(Values.SchemaId.of_json schemaId) ())
           (Some Values.DeleteSchemaResponse.to_json)
           (Some Values.DeleteSchemaResponse.error_to_json)])
let delete_schema_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaId =
         flag "schema-id" (required json_arg) ~doc:"JSON SchemaId"
       and versions =
         flag "versions" (required string) ~doc:"STRING VersionsString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_schema_versions
           (Values.DeleteSchemaVersionsInput.make
              ~schemaId:(Values.SchemaId.of_json schemaId) ~versions ())
           (Some Values.DeleteSchemaVersionsResponse.to_json)
           (Some Values.DeleteSchemaVersionsResponse.error_to_json)])
let delete_security_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_security_configuration
           (Values.DeleteSecurityConfigurationRequest.make ~name ())
           (Some Values.DeleteSecurityConfigurationResponse.to_json)
           (Some Values.DeleteSecurityConfigurationResponse.error_to_json)])
let delete_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestOrigin =
         flag "request-origin" (optional string)
           ~doc:"STRING OrchestrationNameString"
       and id = flag "id" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_session
           (Values.DeleteSessionRequest.make ?requestOrigin ~id ())
           (Some Values.DeleteSessionResponse.to_json)
           (Some Values.DeleteSessionResponse.error_to_json)])
let delete_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and transactionId =
         flag "transaction-id" (optional string)
           ~doc:"STRING TransactionIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table
           (Values.DeleteTableRequest.make ?catalogId ?transactionId
              ~databaseName ~name ())
           (Some Values.DeleteTableResponse.to_json)
           (Some Values.DeleteTableResponse.error_to_json)])
let delete_table_optimizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON TableOptimizerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table_optimizer
           (Values.DeleteTableOptimizerRequest.make ~catalogId ~databaseName
              ~tableName ~type_:(Values.TableOptimizerType.of_json type_) ())
           (Some Values.DeleteTableOptimizerResponse.to_json)
           (Some Values.DeleteTableOptimizerResponse.error_to_json)])
let delete_table_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and versionId =
         flag "version-id" (required string) ~doc:"STRING VersionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table_version
           (Values.DeleteTableVersionRequest.make ?catalogId ~databaseName
              ~tableName ~versionId ())
           (Some Values.DeleteTableVersionResponse.to_json)
           (Some Values.DeleteTableVersionResponse.error_to_json)])
let delete_trigger =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_trigger (Values.DeleteTriggerRequest.make ~name ())
           (Some Values.DeleteTriggerResponse.to_json)
           (Some Values.DeleteTriggerResponse.error_to_json)])
let delete_usage_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_usage_profile
           (Values.DeleteUsageProfileRequest.make ~name ())
           (Some Values.DeleteUsageProfileResponse.to_json)
           (Some Values.DeleteUsageProfileResponse.error_to_json)])
let delete_user_defined_function =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user_defined_function
           (Values.DeleteUserDefinedFunctionRequest.make ?catalogId
              ~databaseName ~functionName ())
           (Some Values.DeleteUserDefinedFunctionResponse.to_json)
           (Some Values.DeleteUserDefinedFunctionResponse.error_to_json)])
let delete_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workflow (Values.DeleteWorkflowRequest.make ~name ())
           (Some Values.DeleteWorkflowResponse.to_json)
           (Some Values.DeleteWorkflowResponse.error_to_json)])
let describe_connection_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionType =
         flag "connection-type" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_connection_type
           (Values.DescribeConnectionTypeRequest.make ~connectionType ())
           (Some Values.DescribeConnectionTypeResponse.to_json)
           (Some Values.DescribeConnectionTypeResponse.error_to_json)])
let describe_entity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and dataStoreApiVersion =
         flag "data-store-api-version" (optional string)
           ~doc:"STRING ApiVersion"
       and connectionName =
         flag "connection-name" (required string) ~doc:"STRING NameString"
       and entityName =
         flag "entity-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_entity
           (Values.DescribeEntityRequest.make ?catalogId ?nextToken
              ?dataStoreApiVersion ~connectionName ~entityName ())
           (Some Values.DescribeEntityResponse.to_json)
           (Some Values.DescribeEntityResponse.error_to_json)])
let describe_inbound_integrations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and integrationArn =
         flag "integration-arn" (optional string) ~doc:"STRING String128"
       and marker = flag "marker" (optional string) ~doc:"STRING String128"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegrationInteger"
       and targetArn =
         flag "target-arn" (optional string) ~doc:"STRING String512" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_inbound_integrations
           (Values.DescribeInboundIntegrationsRequest.make ?integrationArn
              ?marker ?maxRecords ?targetArn ())
           (Some Values.DescribeInboundIntegrationsResponse.to_json)
           (Some Values.DescribeInboundIntegrationsResponse.error_to_json)])
let describe_integrations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and integrationIdentifier =
         flag "integration-identifier" (optional string)
           ~doc:"STRING String128"
       and marker = flag "marker" (optional string) ~doc:"STRING String128"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegrationInteger"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON IntegrationFilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_integrations
           (Values.DescribeIntegrationsRequest.make ?integrationIdentifier
              ?marker ?maxRecords
              ?filters:(Option.map ~f:Values.IntegrationFilterList.of_json
                          filters) ())
           (Some Values.DescribeIntegrationsResponse.to_json)
           (Some Values.DescribeIntegrationsResponse.error_to_json)])
let get_blueprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeBlueprint =
         flag "include-blueprint" (optional bool) ~doc:"BOOL NullableBoolean"
       and includeParameterSpec =
         flag "include-parameter-spec" (optional bool)
           ~doc:"BOOL NullableBoolean"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_blueprint
           (Values.GetBlueprintRequest.make ?includeBlueprint
              ?includeParameterSpec ~name ())
           (Some Values.GetBlueprintResponse.to_json)
           (Some Values.GetBlueprintResponse.error_to_json)])
let get_blueprint_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and blueprintName =
         flag "blueprint-name" (required string)
           ~doc:"STRING OrchestrationNameString"
       and runId = flag "run-id" (required string) ~doc:"STRING IdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_blueprint_run
           (Values.GetBlueprintRunRequest.make ~blueprintName ~runId ())
           (Some Values.GetBlueprintRunResponse.to_json)
           (Some Values.GetBlueprintRunResponse.error_to_json)])
let get_blueprint_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and blueprintName =
         flag "blueprint-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_blueprint_runs
           (Values.GetBlueprintRunsRequest.make ?nextToken ?maxResults
              ~blueprintName ())
           (Some Values.GetBlueprintRunsResponse.to_json)
           (Some Values.GetBlueprintRunsResponse.error_to_json)])
let get_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING CatalogIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_catalog (Values.GetCatalogRequest.make ~catalogId ())
           (Some Values.GetCatalogResponse.to_json)
           (Some Values.GetCatalogResponse.error_to_json)])
let get_catalog_import_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_catalog_import_status
           (Values.GetCatalogImportStatusRequest.make ?catalogId ())
           (Some Values.GetCatalogImportStatusResponse.to_json)
           (Some Values.GetCatalogImportStatusResponse.error_to_json)])
let get_catalogs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parentCatalogId =
         flag "parent-catalog-id" (optional string)
           ~doc:"STRING CatalogIdString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and recursive = flag "recursive" (optional bool) ~doc:"BOOL Boolean"
       and includeRoot =
         flag "include-root" (optional bool) ~doc:"BOOL NullableBoolean"
       and hasDatabases =
         flag "has-databases" (optional bool) ~doc:"BOOL NullableBoolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_catalogs
           (Values.GetCatalogsRequest.make ?parentCatalogId ?nextToken
              ?maxResults ?recursive ?includeRoot ?hasDatabases ())
           (Some Values.GetCatalogsResponse.to_json)
           (Some Values.GetCatalogsResponse.error_to_json)])
let get_classifier =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_classifier (Values.GetClassifierRequest.make ~name ())
           (Some Values.GetClassifierResponse.to_json)
           (Some Values.GetClassifierResponse.error_to_json)])
let get_classifiers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_classifiers
           (Values.GetClassifiersRequest.make ?maxResults ?nextToken ())
           (Some Values.GetClassifiersResponse.to_json)
           (Some Values.GetClassifiersResponse.error_to_json)])
let get_column_statistics_for_partition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and partitionValues =
         flag "partition-values" (required json_arg)
           ~doc:"JSON ValueStringList"
       and columnNames =
         flag "column-names" (required json_arg)
           ~doc:"JSON GetColumnNamesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_column_statistics_for_partition
           (Values.GetColumnStatisticsForPartitionRequest.make ?catalogId
              ~databaseName ~tableName
              ~partitionValues:(Values.ValueStringList.of_json
                                  partitionValues)
              ~columnNames:(Values.GetColumnNamesList.of_json columnNames) ())
           (Some Values.GetColumnStatisticsForPartitionResponse.to_json)
           (Some Values.GetColumnStatisticsForPartitionResponse.error_to_json)])
let get_column_statistics_for_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and columnNames =
         flag "column-names" (required json_arg)
           ~doc:"JSON GetColumnNamesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_column_statistics_for_table
           (Values.GetColumnStatisticsForTableRequest.make ?catalogId
              ~databaseName ~tableName
              ~columnNames:(Values.GetColumnNamesList.of_json columnNames) ())
           (Some Values.GetColumnStatisticsForTableResponse.to_json)
           (Some Values.GetColumnStatisticsForTableResponse.error_to_json)])
let get_column_statistics_task_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and columnStatisticsTaskRunId =
         flag "column-statistics-task-run-id" (required string)
           ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_column_statistics_task_run
           (Values.GetColumnStatisticsTaskRunRequest.make
              ~columnStatisticsTaskRunId ())
           (Some Values.GetColumnStatisticsTaskRunResponse.to_json)
           (Some Values.GetColumnStatisticsTaskRunResponse.error_to_json)])
let get_column_statistics_task_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING DatabaseName"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_column_statistics_task_runs
           (Values.GetColumnStatisticsTaskRunsRequest.make ?maxResults
              ?nextToken ~databaseName ~tableName ())
           (Some Values.GetColumnStatisticsTaskRunsResponse.to_json)
           (Some Values.GetColumnStatisticsTaskRunsResponse.error_to_json)])
let get_column_statistics_task_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_column_statistics_task_settings
           (Values.GetColumnStatisticsTaskSettingsRequest.make ~databaseName
              ~tableName ())
           (Some Values.GetColumnStatisticsTaskSettingsResponse.to_json)
           (Some Values.GetColumnStatisticsTaskSettingsResponse.error_to_json)])
let get_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and hidePassword =
         flag "hide-password" (optional bool) ~doc:"BOOL Boolean"
       and applyOverrideForComputeEnvironment =
         flag "apply-override-for-compute-environment" (optional json_arg)
           ~doc:"JSON ComputeEnvironment"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connection
           (Values.GetConnectionRequest.make ?catalogId ?hidePassword
              ?applyOverrideForComputeEnvironment:(Option.map
                                                     ~f:Values.ComputeEnvironment.of_json
                                                     applyOverrideForComputeEnvironment)
              ~name ()) (Some Values.GetConnectionResponse.to_json)
           (Some Values.GetConnectionResponse.error_to_json)])
let get_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON GetConnectionsFilter"
       and hidePassword =
         flag "hide-password" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connections
           (Values.GetConnectionsRequest.make ?catalogId
              ?filter:(Option.map ~f:Values.GetConnectionsFilter.of_json
                         filter) ?hidePassword ?nextToken ?maxResults ())
           (Some Values.GetConnectionsResponse.to_json)
           (Some Values.GetConnectionsResponse.error_to_json)])
let get_crawler =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_crawler (Values.GetCrawlerRequest.make ~name ())
           (Some Values.GetCrawlerResponse.to_json)
           (Some Values.GetCrawlerResponse.error_to_json)])
let get_crawler_metrics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crawlerNameList =
         flag "crawler-name-list" (optional json_arg)
           ~doc:"JSON CrawlerNameList"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_crawler_metrics
           (Values.GetCrawlerMetricsRequest.make
              ?crawlerNameList:(Option.map ~f:Values.CrawlerNameList.of_json
                                  crawlerNameList) ?maxResults ?nextToken ())
           (Some Values.GetCrawlerMetricsResponse.to_json)
           (Some Values.GetCrawlerMetricsResponse.error_to_json)])
let get_crawlers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_crawlers
           (Values.GetCrawlersRequest.make ?maxResults ?nextToken ())
           (Some Values.GetCrawlersResponse.to_json)
           (Some Values.GetCrawlersResponse.error_to_json)])
let get_custom_entity_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_custom_entity_type
           (Values.GetCustomEntityTypeRequest.make ~name ())
           (Some Values.GetCustomEntityTypeResponse.to_json)
           (Some Values.GetCustomEntityTypeResponse.error_to_json)])
let get_data_catalog_encryption_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_catalog_encryption_settings
           (Values.GetDataCatalogEncryptionSettingsRequest.make ?catalogId ())
           (Some Values.GetDataCatalogEncryptionSettingsResponse.to_json)
           (Some
              Values.GetDataCatalogEncryptionSettingsResponse.error_to_json)])
let get_data_quality_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statisticId =
         flag "statistic-id" (optional string) ~doc:"STRING HashString"
       and profileId =
         flag "profile-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_quality_model
           (Values.GetDataQualityModelRequest.make ?statisticId ~profileId ())
           (Some Values.GetDataQualityModelResponse.to_json)
           (Some Values.GetDataQualityModelResponse.error_to_json)])
let get_data_quality_model_result =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statisticId =
         flag "statistic-id" (required string) ~doc:"STRING HashString"
       and profileId =
         flag "profile-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_quality_model_result
           (Values.GetDataQualityModelResultRequest.make ~statisticId
              ~profileId ())
           (Some Values.GetDataQualityModelResultResponse.to_json)
           (Some Values.GetDataQualityModelResultResponse.error_to_json)])
let get_data_quality_result =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resultId =
         flag "result-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_quality_result
           (Values.GetDataQualityResultRequest.make ~resultId ())
           (Some Values.GetDataQualityResultResponse.to_json)
           (Some Values.GetDataQualityResultResponse.error_to_json)])
let get_data_quality_rule_recommendation_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and runId = flag "run-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_quality_rule_recommendation_run
           (Values.GetDataQualityRuleRecommendationRunRequest.make ~runId ())
           (Some Values.GetDataQualityRuleRecommendationRunResponse.to_json)
           (Some
              Values.GetDataQualityRuleRecommendationRunResponse.error_to_json)])
let get_data_quality_ruleset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_quality_ruleset
           (Values.GetDataQualityRulesetRequest.make ~name ())
           (Some Values.GetDataQualityRulesetResponse.to_json)
           (Some Values.GetDataQualityRulesetResponse.error_to_json)])
let get_data_quality_ruleset_evaluation_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and runId = flag "run-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_quality_ruleset_evaluation_run
           (Values.GetDataQualityRulesetEvaluationRunRequest.make ~runId ())
           (Some Values.GetDataQualityRulesetEvaluationRunResponse.to_json)
           (Some
              Values.GetDataQualityRulesetEvaluationRunResponse.error_to_json)])
let get_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_database
           (Values.GetDatabaseRequest.make ?catalogId ~name ())
           (Some Values.GetDatabaseResponse.to_json)
           (Some Values.GetDatabaseResponse.error_to_json)])
let get_databases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT CatalogGetterPageSize"
       and resourceShareType =
         flag "resource-share-type" (optional json_arg)
           ~doc:"JSON ResourceShareType"
       and attributesToGet =
         flag "attributes-to-get" (optional json_arg)
           ~doc:"JSON DatabaseAttributesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_databases
           (Values.GetDatabasesRequest.make ?catalogId ?nextToken ?maxResults
              ?resourceShareType:(Option.map
                                    ~f:Values.ResourceShareType.of_json
                                    resourceShareType)
              ?attributesToGet:(Option.map
                                  ~f:Values.DatabaseAttributesList.of_json
                                  attributesToGet) ())
           (Some Values.GetDatabasesResponse.to_json)
           (Some Values.GetDatabasesResponse.error_to_json)])
let get_dataflow_graph =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pythonScript =
         flag "python-script" (optional string) ~doc:"STRING PythonScript" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dataflow_graph
           (Values.GetDataflowGraphRequest.make ?pythonScript ())
           (Some Values.GetDataflowGraphResponse.to_json)
           (Some Values.GetDataflowGraphResponse.error_to_json)])
let get_dev_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dev_endpoint
           (Values.GetDevEndpointRequest.make ~endpointName ())
           (Some Values.GetDevEndpointResponse.to_json)
           (Some Values.GetDevEndpointResponse.error_to_json)])
let get_dev_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dev_endpoints
           (Values.GetDevEndpointsRequest.make ?maxResults ?nextToken ())
           (Some Values.GetDevEndpointsResponse.to_json)
           (Some Values.GetDevEndpointsResponse.error_to_json)])
let get_entity_records =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionName =
         flag "connection-name" (optional string) ~doc:"STRING NameString"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and dataStoreApiVersion =
         flag "data-store-api-version" (optional string)
           ~doc:"STRING ApiVersion"
       and connectionOptions =
         flag "connection-options" (optional json_arg)
           ~doc:"JSON ConnectionOptions"
       and filterPredicate =
         flag "filter-predicate" (optional string)
           ~doc:"STRING FilterPredicate"
       and orderBy = flag "order-by" (optional string) ~doc:"STRING String"
       and selectedFields =
         flag "selected-fields" (optional json_arg)
           ~doc:"JSON SelectedFields"
       and entityName =
         flag "entity-name" (required string) ~doc:"STRING EntityName"
       and limit = flag "limit" (required json_arg) ~doc:"JSON Limit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_entity_records
           (Values.GetEntityRecordsRequest.make ?connectionName ?catalogId
              ?nextToken ?dataStoreApiVersion
              ?connectionOptions:(Option.map
                                    ~f:Values.ConnectionOptions.of_json
                                    connectionOptions) ?filterPredicate
              ?orderBy
              ?selectedFields:(Option.map ~f:Values.SelectedFields.of_json
                                 selectedFields) ~entityName
              ~limit:(Values.Limit.of_json limit) ())
           (Some Values.GetEntityRecordsResponse.to_json)
           (Some Values.GetEntityRecordsResponse.error_to_json)])
let get_glue_identity_center_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_glue_identity_center_configuration
           (Values.GetGlueIdentityCenterConfigurationRequest.make ())
           (Some Values.GetGlueIdentityCenterConfigurationResponse.to_json)
           (Some
              Values.GetGlueIdentityCenterConfigurationResponse.error_to_json)])
let get_integration_resource_property =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String512" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_integration_resource_property
           (Values.GetIntegrationResourcePropertyRequest.make ~resourceArn ())
           (Some Values.GetIntegrationResourcePropertyResponse.to_json)
           (Some Values.GetIntegrationResourcePropertyResponse.error_to_json)])
let get_integration_table_properties =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String512"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING String128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_integration_table_properties
           (Values.GetIntegrationTablePropertiesRequest.make ~resourceArn
              ~tableName ())
           (Some Values.GetIntegrationTablePropertiesResponse.to_json)
           (Some Values.GetIntegrationTablePropertiesResponse.error_to_json)])
let get_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName =
         flag "job-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job (Values.GetJobRequest.make ~jobName ())
           (Some Values.GetJobResponse.to_json)
           (Some Values.GetJobResponse.error_to_json)])
let get_job_bookmark =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and runId = flag "run-id" (optional string) ~doc:"STRING RunId"
       and jobName = flag "job-name" (required string) ~doc:"STRING JobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job_bookmark
           (Values.GetJobBookmarkRequest.make ?runId ~jobName ())
           (Some Values.GetJobBookmarkResponse.to_json)
           (Some Values.GetJobBookmarkResponse.error_to_json)])
let get_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and predecessorsIncluded =
         flag "predecessors-included" (optional bool)
           ~doc:"BOOL BooleanValue"
       and jobName =
         flag "job-name" (required string) ~doc:"STRING NameString"
       and runId = flag "run-id" (required string) ~doc:"STRING IdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job_run
           (Values.GetJobRunRequest.make ?predecessorsIncluded ~jobName
              ~runId ()) (Some Values.GetJobRunResponse.to_json)
           (Some Values.GetJobRunResponse.error_to_json)])
let get_job_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT OrchestrationPageSize200"
       and jobName =
         flag "job-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job_runs
           (Values.GetJobRunsRequest.make ?nextToken ?maxResults ~jobName ())
           (Some Values.GetJobRunsResponse.to_json)
           (Some Values.GetJobRunsResponse.error_to_json)])
let get_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_jobs (Values.GetJobsRequest.make ?nextToken ?maxResults ())
           (Some Values.GetJobsResponse.to_json)
           (Some Values.GetJobsResponse.error_to_json)])
let get_m_l_task_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transformId =
         flag "transform-id" (required string) ~doc:"STRING HashString"
       and taskRunId =
         flag "task-run-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_m_l_task_run
           (Values.GetMLTaskRunRequest.make ~transformId ~taskRunId ())
           (Some Values.GetMLTaskRunResponse.to_json)
           (Some Values.GetMLTaskRunResponse.error_to_json)])
let get_m_l_task_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON TaskRunFilterCriteria"
       and sort =
         flag "sort" (optional json_arg) ~doc:"JSON TaskRunSortCriteria"
       and transformId =
         flag "transform-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_m_l_task_runs
           (Values.GetMLTaskRunsRequest.make ?nextToken ?maxResults
              ?filter:(Option.map ~f:Values.TaskRunFilterCriteria.of_json
                         filter)
              ?sort:(Option.map ~f:Values.TaskRunSortCriteria.of_json sort)
              ~transformId ()) (Some Values.GetMLTaskRunsResponse.to_json)
           (Some Values.GetMLTaskRunsResponse.error_to_json)])
let get_m_l_transform =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transformId =
         flag "transform-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_m_l_transform
           (Values.GetMLTransformRequest.make ~transformId ())
           (Some Values.GetMLTransformResponse.to_json)
           (Some Values.GetMLTransformResponse.error_to_json)])
let get_m_l_transforms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON TransformFilterCriteria"
       and sort =
         flag "sort" (optional json_arg) ~doc:"JSON TransformSortCriteria" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_m_l_transforms
           (Values.GetMLTransformsRequest.make ?nextToken ?maxResults
              ?filter:(Option.map ~f:Values.TransformFilterCriteria.of_json
                         filter)
              ?sort:(Option.map ~f:Values.TransformSortCriteria.of_json sort)
              ()) (Some Values.GetMLTransformsResponse.to_json)
           (Some Values.GetMLTransformsResponse.error_to_json)])
let get_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sinks =
         flag "sinks" (optional json_arg) ~doc:"JSON CatalogEntries"
       and location =
         flag "location" (optional json_arg) ~doc:"JSON Location"
       and source =
         flag "source" (required json_arg) ~doc:"JSON CatalogEntry" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_mapping
           (Values.GetMappingRequest.make
              ?sinks:(Option.map ~f:Values.CatalogEntries.of_json sinks)
              ?location:(Option.map ~f:Values.Location.of_json location)
              ~source:(Values.CatalogEntry.of_json source) ())
           (Some Values.GetMappingResponse.to_json)
           (Some Values.GetMappingResponse.error_to_json)])
let get_materialized_view_refresh_task_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING NameString"
       and materializedViewRefreshTaskRunId =
         flag "materialized-view-refresh-task-run-id" (required string)
           ~doc:"STRING UUIDv4" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_materialized_view_refresh_task_run
           (Values.GetMaterializedViewRefreshTaskRunRequest.make ~catalogId
              ~materializedViewRefreshTaskRunId ())
           (Some Values.GetMaterializedViewRefreshTaskRunResponse.to_json)
           (Some
              Values.GetMaterializedViewRefreshTaskRunResponse.error_to_json)])
let get_partition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and auditContext =
         flag "audit-context" (optional json_arg) ~doc:"JSON AuditContext"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and partitionValues =
         flag "partition-values" (required json_arg)
           ~doc:"JSON ValueStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_partition
           (Values.GetPartitionRequest.make ?catalogId
              ?auditContext:(Option.map ~f:Values.AuditContext.of_json
                               auditContext) ~databaseName ~tableName
              ~partitionValues:(Values.ValueStringList.of_json
                                  partitionValues) ())
           (Some Values.GetPartitionResponse.to_json)
           (Some Values.GetPartitionResponse.error_to_json)])
let get_partition_indexes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_partition_indexes
           (Values.GetPartitionIndexesRequest.make ?catalogId ?nextToken
              ~databaseName ~tableName ())
           (Some Values.GetPartitionIndexesResponse.to_json)
           (Some Values.GetPartitionIndexesResponse.error_to_json)])
let get_partitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and expression =
         flag "expression" (optional string) ~doc:"STRING PredicateString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and segment = flag "segment" (optional json_arg) ~doc:"JSON Segment"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and excludeColumnSchema =
         flag "exclude-column-schema" (optional bool)
           ~doc:"BOOL BooleanNullable"
       and transactionId =
         flag "transaction-id" (optional string)
           ~doc:"STRING TransactionIdString"
       and queryAsOfTime =
         flag "query-as-of-time" (optional json_arg) ~doc:"JSON Timestamp"
       and auditContext =
         flag "audit-context" (optional json_arg) ~doc:"JSON AuditContext"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_partitions
           (Values.GetPartitionsRequest.make ?catalogId ?expression
              ?nextToken
              ?segment:(Option.map ~f:Values.Segment.of_json segment)
              ?maxResults ?excludeColumnSchema ?transactionId
              ?queryAsOfTime:(Option.map ~f:Values.Timestamp.of_json
                                queryAsOfTime)
              ?auditContext:(Option.map ~f:Values.AuditContext.of_json
                               auditContext) ~databaseName ~tableName ())
           (Some Values.GetPartitionsResponse.to_json)
           (Some Values.GetPartitionsResponse.error_to_json)])
let get_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sinks =
         flag "sinks" (optional json_arg) ~doc:"JSON CatalogEntries"
       and location =
         flag "location" (optional json_arg) ~doc:"JSON Location"
       and language =
         flag "language" (optional json_arg) ~doc:"JSON Language"
       and additionalPlanOptionsMap =
         flag "additional-plan-options-map" (optional json_arg)
           ~doc:"JSON AdditionalPlanOptionsMap"
       and mapping =
         flag "mapping" (required json_arg) ~doc:"JSON MappingList"
       and source =
         flag "source" (required json_arg) ~doc:"JSON CatalogEntry" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_plan
           (Values.GetPlanRequest.make
              ?sinks:(Option.map ~f:Values.CatalogEntries.of_json sinks)
              ?location:(Option.map ~f:Values.Location.of_json location)
              ?language:(Option.map ~f:Values.Language.of_json language)
              ?additionalPlanOptionsMap:(Option.map
                                           ~f:Values.AdditionalPlanOptionsMap.of_json
                                           additionalPlanOptionsMap)
              ~mapping:(Values.MappingList.of_json mapping)
              ~source:(Values.CatalogEntry.of_json source) ())
           (Some Values.GetPlanResponse.to_json)
           (Some Values.GetPlanResponse.error_to_json)])
let get_registry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (required json_arg) ~doc:"JSON RegistryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_registry
           (Values.GetRegistryInput.make
              ~registryId:(Values.RegistryId.of_json registryId) ())
           (Some Values.GetRegistryResponse.to_json)
           (Some Values.GetRegistryResponse.error_to_json)])
let get_resource_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policies
           (Values.GetResourcePoliciesRequest.make ?nextToken ?maxResults ())
           (Some Values.GetResourcePoliciesResponse.to_json)
           (Some Values.GetResourcePoliciesResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING GlueResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ?resourceArn ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let get_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaId =
         flag "schema-id" (required json_arg) ~doc:"JSON SchemaId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_schema
           (Values.GetSchemaInput.make
              ~schemaId:(Values.SchemaId.of_json schemaId) ())
           (Some Values.GetSchemaResponse.to_json)
           (Some Values.GetSchemaResponse.error_to_json)])
let get_schema_by_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaId =
         flag "schema-id" (required json_arg) ~doc:"JSON SchemaId"
       and schemaDefinition =
         flag "schema-definition" (required string)
           ~doc:"STRING SchemaDefinitionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_schema_by_definition
           (Values.GetSchemaByDefinitionInput.make
              ~schemaId:(Values.SchemaId.of_json schemaId) ~schemaDefinition
              ()) (Some Values.GetSchemaByDefinitionResponse.to_json)
           (Some Values.GetSchemaByDefinitionResponse.error_to_json)])
let get_schema_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaId =
         flag "schema-id" (optional json_arg) ~doc:"JSON SchemaId"
       and schemaVersionId =
         flag "schema-version-id" (optional string)
           ~doc:"STRING SchemaVersionIdString"
       and schemaVersionNumber =
         flag "schema-version-number" (optional json_arg)
           ~doc:"JSON SchemaVersionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_schema_version
           (Values.GetSchemaVersionInput.make
              ?schemaId:(Option.map ~f:Values.SchemaId.of_json schemaId)
              ?schemaVersionId
              ?schemaVersionNumber:(Option.map
                                      ~f:Values.SchemaVersionNumber.of_json
                                      schemaVersionNumber) ())
           (Some Values.GetSchemaVersionResponse.to_json)
           (Some Values.GetSchemaVersionResponse.error_to_json)])
let get_schema_versions_diff =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaId =
         flag "schema-id" (required json_arg) ~doc:"JSON SchemaId"
       and firstSchemaVersionNumber =
         flag "first-schema-version-number" (required json_arg)
           ~doc:"JSON SchemaVersionNumber"
       and secondSchemaVersionNumber =
         flag "second-schema-version-number" (required json_arg)
           ~doc:"JSON SchemaVersionNumber"
       and schemaDiffType =
         flag "schema-diff-type" (required json_arg)
           ~doc:"JSON SchemaDiffType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_schema_versions_diff
           (Values.GetSchemaVersionsDiffInput.make
              ~schemaId:(Values.SchemaId.of_json schemaId)
              ~firstSchemaVersionNumber:(Values.SchemaVersionNumber.of_json
                                           firstSchemaVersionNumber)
              ~secondSchemaVersionNumber:(Values.SchemaVersionNumber.of_json
                                            secondSchemaVersionNumber)
              ~schemaDiffType:(Values.SchemaDiffType.of_json schemaDiffType)
              ()) (Some Values.GetSchemaVersionsDiffResponse.to_json)
           (Some Values.GetSchemaVersionsDiffResponse.error_to_json)])
let get_security_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_security_configuration
           (Values.GetSecurityConfigurationRequest.make ~name ())
           (Some Values.GetSecurityConfigurationResponse.to_json)
           (Some Values.GetSecurityConfigurationResponse.error_to_json)])
let get_security_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_security_configurations
           (Values.GetSecurityConfigurationsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.GetSecurityConfigurationsResponse.to_json)
           (Some Values.GetSecurityConfigurationsResponse.error_to_json)])
let get_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestOrigin =
         flag "request-origin" (optional string)
           ~doc:"STRING OrchestrationNameString"
       and id = flag "id" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_session
           (Values.GetSessionRequest.make ?requestOrigin ~id ())
           (Some Values.GetSessionResponse.to_json)
           (Some Values.GetSessionResponse.error_to_json)])
let get_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestOrigin =
         flag "request-origin" (optional string)
           ~doc:"STRING OrchestrationNameString"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING NameString"
       and id = flag "id" (required int) ~doc:"INT IntegerValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_statement
           (Values.GetStatementRequest.make ?requestOrigin ~sessionId ~id ())
           (Some Values.GetStatementResponse.to_json)
           (Some Values.GetStatementResponse.error_to_json)])
let get_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and transactionId =
         flag "transaction-id" (optional string)
           ~doc:"STRING TransactionIdString"
       and queryAsOfTime =
         flag "query-as-of-time" (optional json_arg) ~doc:"JSON Timestamp"
       and auditContext =
         flag "audit-context" (optional json_arg) ~doc:"JSON AuditContext"
       and includeStatusDetails =
         flag "include-status-details" (optional bool)
           ~doc:"BOOL BooleanNullable"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table
           (Values.GetTableRequest.make ?catalogId ?transactionId
              ?queryAsOfTime:(Option.map ~f:Values.Timestamp.of_json
                                queryAsOfTime)
              ?auditContext:(Option.map ~f:Values.AuditContext.of_json
                               auditContext) ?includeStatusDetails
              ~databaseName ~name ()) (Some Values.GetTableResponse.to_json)
           (Some Values.GetTableResponse.error_to_json)])
let get_table_optimizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON TableOptimizerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_optimizer
           (Values.GetTableOptimizerRequest.make ~catalogId ~databaseName
              ~tableName ~type_:(Values.TableOptimizerType.of_json type_) ())
           (Some Values.GetTableOptimizerResponse.to_json)
           (Some Values.GetTableOptimizerResponse.error_to_json)])
let get_table_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING VersionString"
       and auditContext =
         flag "audit-context" (optional json_arg) ~doc:"JSON AuditContext"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_version
           (Values.GetTableVersionRequest.make ?catalogId ?versionId
              ?auditContext:(Option.map ~f:Values.AuditContext.of_json
                               auditContext) ~databaseName ~tableName ())
           (Some Values.GetTableVersionResponse.to_json)
           (Some Values.GetTableVersionResponse.error_to_json)])
let get_table_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT CatalogGetterPageSize"
       and auditContext =
         flag "audit-context" (optional json_arg) ~doc:"JSON AuditContext"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_versions
           (Values.GetTableVersionsRequest.make ?catalogId ?nextToken
              ?maxResults
              ?auditContext:(Option.map ~f:Values.AuditContext.of_json
                               auditContext) ~databaseName ~tableName ())
           (Some Values.GetTableVersionsResponse.to_json)
           (Some Values.GetTableVersionsResponse.error_to_json)])
let get_tables =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and expression =
         flag "expression" (optional string) ~doc:"STRING FilterString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT CatalogGetterPageSize"
       and transactionId =
         flag "transaction-id" (optional string)
           ~doc:"STRING TransactionIdString"
       and queryAsOfTime =
         flag "query-as-of-time" (optional json_arg) ~doc:"JSON Timestamp"
       and auditContext =
         flag "audit-context" (optional json_arg) ~doc:"JSON AuditContext"
       and includeStatusDetails =
         flag "include-status-details" (optional bool)
           ~doc:"BOOL BooleanNullable"
       and attributesToGet =
         flag "attributes-to-get" (optional json_arg)
           ~doc:"JSON TableAttributesList"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tables
           (Values.GetTablesRequest.make ?catalogId ?expression ?nextToken
              ?maxResults ?transactionId
              ?queryAsOfTime:(Option.map ~f:Values.Timestamp.of_json
                                queryAsOfTime)
              ?auditContext:(Option.map ~f:Values.AuditContext.of_json
                               auditContext) ?includeStatusDetails
              ?attributesToGet:(Option.map
                                  ~f:Values.TableAttributesList.of_json
                                  attributesToGet) ~databaseName ())
           (Some Values.GetTablesResponse.to_json)
           (Some Values.GetTablesResponse.error_to_json)])
let get_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING GlueResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tags (Values.GetTagsRequest.make ~resourceArn ())
           (Some Values.GetTagsResponse.to_json)
           (Some Values.GetTagsResponse.error_to_json)])
let get_trigger =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_trigger (Values.GetTriggerRequest.make ~name ())
           (Some Values.GetTriggerResponse.to_json)
           (Some Values.GetTriggerResponse.error_to_json)])
let get_triggers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and dependentJobName =
         flag "dependent-job-name" (optional string) ~doc:"STRING NameString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT OrchestrationPageSize200" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_triggers
           (Values.GetTriggersRequest.make ?nextToken ?dependentJobName
              ?maxResults ()) (Some Values.GetTriggersResponse.to_json)
           (Some Values.GetTriggersResponse.error_to_json)])
let get_unfiltered_partition_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and region = flag "region" (optional string) ~doc:"STRING ValueString"
       and auditContext =
         flag "audit-context" (optional json_arg) ~doc:"JSON AuditContext"
       and querySessionContext =
         flag "query-session-context" (optional json_arg)
           ~doc:"JSON QuerySessionContext"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and partitionValues =
         flag "partition-values" (required json_arg)
           ~doc:"JSON ValueStringList"
       and supportedPermissionTypes =
         flag "supported-permission-types" (required json_arg)
           ~doc:"JSON PermissionTypeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_unfiltered_partition_metadata
           (Values.GetUnfilteredPartitionMetadataRequest.make ?region
              ?auditContext:(Option.map ~f:Values.AuditContext.of_json
                               auditContext)
              ?querySessionContext:(Option.map
                                      ~f:Values.QuerySessionContext.of_json
                                      querySessionContext) ~catalogId
              ~databaseName ~tableName
              ~partitionValues:(Values.ValueStringList.of_json
                                  partitionValues)
              ~supportedPermissionTypes:(Values.PermissionTypeList.of_json
                                           supportedPermissionTypes) ())
           (Some Values.GetUnfilteredPartitionMetadataResponse.to_json)
           (Some Values.GetUnfilteredPartitionMetadataResponse.error_to_json)])
let get_unfiltered_partitions_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and region = flag "region" (optional string) ~doc:"STRING ValueString"
       and expression =
         flag "expression" (optional string) ~doc:"STRING PredicateString"
       and auditContext =
         flag "audit-context" (optional json_arg) ~doc:"JSON AuditContext"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and segment = flag "segment" (optional json_arg) ~doc:"JSON Segment"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and querySessionContext =
         flag "query-session-context" (optional json_arg)
           ~doc:"JSON QuerySessionContext"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and supportedPermissionTypes =
         flag "supported-permission-types" (required json_arg)
           ~doc:"JSON PermissionTypeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_unfiltered_partitions_metadata
           (Values.GetUnfilteredPartitionsMetadataRequest.make ?region
              ?expression
              ?auditContext:(Option.map ~f:Values.AuditContext.of_json
                               auditContext) ?nextToken
              ?segment:(Option.map ~f:Values.Segment.of_json segment)
              ?maxResults
              ?querySessionContext:(Option.map
                                      ~f:Values.QuerySessionContext.of_json
                                      querySessionContext) ~catalogId
              ~databaseName ~tableName
              ~supportedPermissionTypes:(Values.PermissionTypeList.of_json
                                           supportedPermissionTypes) ())
           (Some Values.GetUnfilteredPartitionsMetadataResponse.to_json)
           (Some Values.GetUnfilteredPartitionsMetadataResponse.error_to_json)])
let get_unfiltered_table_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and region = flag "region" (optional string) ~doc:"STRING ValueString"
       and auditContext =
         flag "audit-context" (optional json_arg) ~doc:"JSON AuditContext"
       and parentResourceArn =
         flag "parent-resource-arn" (optional string) ~doc:"STRING ArnString"
       and rootResourceArn =
         flag "root-resource-arn" (optional string) ~doc:"STRING ArnString"
       and supportedDialect =
         flag "supported-dialect" (optional json_arg)
           ~doc:"JSON SupportedDialect"
       and permissions =
         flag "permissions" (optional json_arg) ~doc:"JSON PermissionList"
       and querySessionContext =
         flag "query-session-context" (optional json_arg)
           ~doc:"JSON QuerySessionContext"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and supportedPermissionTypes =
         flag "supported-permission-types" (required json_arg)
           ~doc:"JSON PermissionTypeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_unfiltered_table_metadata
           (Values.GetUnfilteredTableMetadataRequest.make ?region
              ?auditContext:(Option.map ~f:Values.AuditContext.of_json
                               auditContext) ?parentResourceArn
              ?rootResourceArn
              ?supportedDialect:(Option.map
                                   ~f:Values.SupportedDialect.of_json
                                   supportedDialect)
              ?permissions:(Option.map ~f:Values.PermissionList.of_json
                              permissions)
              ?querySessionContext:(Option.map
                                      ~f:Values.QuerySessionContext.of_json
                                      querySessionContext) ~catalogId
              ~databaseName ~name
              ~supportedPermissionTypes:(Values.PermissionTypeList.of_json
                                           supportedPermissionTypes) ())
           (Some Values.GetUnfilteredTableMetadataResponse.to_json)
           (Some Values.GetUnfilteredTableMetadataResponse.error_to_json)])
let get_usage_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_usage_profile (Values.GetUsageProfileRequest.make ~name ())
           (Some Values.GetUsageProfileResponse.to_json)
           (Some Values.GetUsageProfileResponse.error_to_json)])
let get_user_defined_function =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user_defined_function
           (Values.GetUserDefinedFunctionRequest.make ?catalogId
              ~databaseName ~functionName ())
           (Some Values.GetUserDefinedFunctionResponse.to_json)
           (Some Values.GetUserDefinedFunctionResponse.error_to_json)])
let get_user_defined_functions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (optional string) ~doc:"STRING NameString"
       and functionType =
         flag "function-type" (optional json_arg) ~doc:"JSON FunctionType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT CatalogGetterPageSize"
       and pattern =
         flag "pattern" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user_defined_functions
           (Values.GetUserDefinedFunctionsRequest.make ?catalogId
              ?databaseName
              ?functionType:(Option.map ~f:Values.FunctionType.of_json
                               functionType) ?nextToken ?maxResults ~pattern
              ()) (Some Values.GetUserDefinedFunctionsResponse.to_json)
           (Some Values.GetUserDefinedFunctionsResponse.error_to_json)])
let get_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeGraph =
         flag "include-graph" (optional bool) ~doc:"BOOL NullableBoolean"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow
           (Values.GetWorkflowRequest.make ?includeGraph ~name ())
           (Some Values.GetWorkflowResponse.to_json)
           (Some Values.GetWorkflowResponse.error_to_json)])
let get_workflow_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeGraph =
         flag "include-graph" (optional bool) ~doc:"BOOL NullableBoolean"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and runId = flag "run-id" (required string) ~doc:"STRING IdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow_run
           (Values.GetWorkflowRunRequest.make ?includeGraph ~name ~runId ())
           (Some Values.GetWorkflowRunResponse.to_json)
           (Some Values.GetWorkflowRunResponse.error_to_json)])
let get_workflow_run_properties =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and runId = flag "run-id" (required string) ~doc:"STRING IdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow_run_properties
           (Values.GetWorkflowRunPropertiesRequest.make ~name ~runId ())
           (Some Values.GetWorkflowRunPropertiesResponse.to_json)
           (Some Values.GetWorkflowRunPropertiesResponse.error_to_json)])
let get_workflow_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeGraph =
         flag "include-graph" (optional bool) ~doc:"BOOL NullableBoolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow_runs
           (Values.GetWorkflowRunsRequest.make ?includeGraph ?nextToken
              ?maxResults ~name ())
           (Some Values.GetWorkflowRunsResponse.to_json)
           (Some Values.GetWorkflowRunsResponse.error_to_json)])
let import_catalog_to_glue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_catalog_to_glue
           (Values.ImportCatalogToGlueRequest.make ?catalogId ())
           (Some Values.ImportCatalogToGlueResponse.to_json)
           (Some Values.ImportCatalogToGlueResponse.error_to_json)])
let list_blueprints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT OrchestrationPageSize25"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_blueprints
           (Values.ListBlueprintsRequest.make ?nextToken ?maxResults
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ())
           (Some Values.ListBlueprintsResponse.to_json)
           (Some Values.ListBlueprintsResponse.error_to_json)])
let list_column_statistics_task_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_column_statistics_task_runs
           (Values.ListColumnStatisticsTaskRunsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListColumnStatisticsTaskRunsResponse.to_json)
           (Some Values.ListColumnStatisticsTaskRunsResponse.error_to_json)])
let list_connection_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connection_types
           (Values.ListConnectionTypesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListConnectionTypesResponse.to_json)
           (Some Values.ListConnectionTypesResponse.error_to_json)])
let list_crawlers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_crawlers
           (Values.ListCrawlersRequest.make ?maxResults ?nextToken
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ())
           (Some Values.ListCrawlersResponse.to_json)
           (Some Values.ListCrawlersResponse.error_to_json)])
let list_crawls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON CrawlsFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and crawlerName =
         flag "crawler-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_crawls
           (Values.ListCrawlsRequest.make ?maxResults
              ?filters:(Option.map ~f:Values.CrawlsFilterList.of_json filters)
              ?nextToken ~crawlerName ())
           (Some Values.ListCrawlsResponse.to_json)
           (Some Values.ListCrawlsResponse.error_to_json)])
let list_custom_entity_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_entity_types
           (Values.ListCustomEntityTypesRequest.make ?nextToken ?maxResults
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ())
           (Some Values.ListCustomEntityTypesResponse.to_json)
           (Some Values.ListCustomEntityTypesResponse.error_to_json)])
let list_data_quality_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON DataQualityResultFilterCriteria"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_quality_results
           (Values.ListDataQualityResultsRequest.make
              ?filter:(Option.map
                         ~f:Values.DataQualityResultFilterCriteria.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListDataQualityResultsResponse.to_json)
           (Some Values.ListDataQualityResultsResponse.error_to_json)])
let list_data_quality_rule_recommendation_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON DataQualityRuleRecommendationRunFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_quality_rule_recommendation_runs
           (Values.ListDataQualityRuleRecommendationRunsRequest.make
              ?filter:(Option.map
                         ~f:Values.DataQualityRuleRecommendationRunFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListDataQualityRuleRecommendationRunsResponse.to_json)
           (Some
              Values.ListDataQualityRuleRecommendationRunsResponse.error_to_json)])
let list_data_quality_ruleset_evaluation_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON DataQualityRulesetEvaluationRunFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_quality_ruleset_evaluation_runs
           (Values.ListDataQualityRulesetEvaluationRunsRequest.make
              ?filter:(Option.map
                         ~f:Values.DataQualityRulesetEvaluationRunFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListDataQualityRulesetEvaluationRunsResponse.to_json)
           (Some
              Values.ListDataQualityRulesetEvaluationRunsResponse.error_to_json)])
let list_data_quality_rulesets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON DataQualityRulesetFilterCriteria"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_quality_rulesets
           (Values.ListDataQualityRulesetsRequest.make ?nextToken ?maxResults
              ?filter:(Option.map
                         ~f:Values.DataQualityRulesetFilterCriteria.of_json
                         filter)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ())
           (Some Values.ListDataQualityRulesetsResponse.to_json)
           (Some Values.ListDataQualityRulesetsResponse.error_to_json)])
let list_data_quality_statistic_annotations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statisticId =
         flag "statistic-id" (optional string) ~doc:"STRING HashString"
       and profileId =
         flag "profile-id" (optional string) ~doc:"STRING HashString"
       and timestampFilter =
         flag "timestamp-filter" (optional json_arg)
           ~doc:"JSON TimestampFilter"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_quality_statistic_annotations
           (Values.ListDataQualityStatisticAnnotationsRequest.make
              ?statisticId ?profileId
              ?timestampFilter:(Option.map ~f:Values.TimestampFilter.of_json
                                  timestampFilter) ?maxResults ?nextToken ())
           (Some Values.ListDataQualityStatisticAnnotationsResponse.to_json)
           (Some
              Values.ListDataQualityStatisticAnnotationsResponse.error_to_json)])
let list_data_quality_statistics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statisticId =
         flag "statistic-id" (optional string) ~doc:"STRING HashString"
       and profileId =
         flag "profile-id" (optional string) ~doc:"STRING HashString"
       and timestampFilter =
         flag "timestamp-filter" (optional json_arg)
           ~doc:"JSON TimestampFilter"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_quality_statistics
           (Values.ListDataQualityStatisticsRequest.make ?statisticId
              ?profileId
              ?timestampFilter:(Option.map ~f:Values.TimestampFilter.of_json
                                  timestampFilter) ?maxResults ?nextToken ())
           (Some Values.ListDataQualityStatisticsResponse.to_json)
           (Some Values.ListDataQualityStatisticsResponse.error_to_json)])
let list_dev_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dev_endpoints
           (Values.ListDevEndpointsRequest.make ?nextToken ?maxResults
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ())
           (Some Values.ListDevEndpointsResponse.to_json)
           (Some Values.ListDevEndpointsResponse.error_to_json)])
let list_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionName =
         flag "connection-name" (optional string) ~doc:"STRING NameString"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and parentEntityName =
         flag "parent-entity-name" (optional string) ~doc:"STRING EntityName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and dataStoreApiVersion =
         flag "data-store-api-version" (optional string)
           ~doc:"STRING ApiVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_entities
           (Values.ListEntitiesRequest.make ?connectionName ?catalogId
              ?parentEntityName ?nextToken ?dataStoreApiVersion ())
           (Some Values.ListEntitiesResponse.to_json)
           (Some Values.ListEntitiesResponse.error_to_json)])
let list_integration_resource_properties =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING String1024"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON IntegrationResourcePropertyFilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegrationInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_integration_resource_properties
           (Values.ListIntegrationResourcePropertiesRequest.make ?marker
              ?filters:(Option.map
                          ~f:Values.IntegrationResourcePropertyFilterList.of_json
                          filters) ?maxRecords ())
           (Some Values.ListIntegrationResourcePropertiesResponse.to_json)
           (Some
              Values.ListIntegrationResourcePropertiesResponse.error_to_json)])
let list_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_jobs
           (Values.ListJobsRequest.make ?nextToken ?maxResults
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ())
           (Some Values.ListJobsResponse.to_json)
           (Some Values.ListJobsResponse.error_to_json)])
let list_m_l_transforms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON TransformFilterCriteria"
       and sort =
         flag "sort" (optional json_arg) ~doc:"JSON TransformSortCriteria"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_m_l_transforms
           (Values.ListMLTransformsRequest.make ?nextToken ?maxResults
              ?filter:(Option.map ~f:Values.TransformFilterCriteria.of_json
                         filter)
              ?sort:(Option.map ~f:Values.TransformSortCriteria.of_json sort)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ())
           (Some Values.ListMLTransformsResponse.to_json)
           (Some Values.ListMLTransformsResponse.error_to_json)])
let list_materialized_view_refresh_task_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (optional string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (optional string) ~doc:"STRING NameString"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_materialized_view_refresh_task_runs
           (Values.ListMaterializedViewRefreshTaskRunsRequest.make
              ?databaseName ?tableName ?maxResults ?nextToken ~catalogId ())
           (Some Values.ListMaterializedViewRefreshTaskRunsResponse.to_json)
           (Some
              Values.ListMaterializedViewRefreshTaskRunsResponse.error_to_json)])
let list_registries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsNumber"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING SchemaRegistryTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_registries
           (Values.ListRegistriesInput.make ?maxResults ?nextToken ())
           (Some Values.ListRegistriesResponse.to_json)
           (Some Values.ListRegistriesResponse.error_to_json)])
let list_schema_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsNumber"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING SchemaRegistryTokenString"
       and schemaId =
         flag "schema-id" (required json_arg) ~doc:"JSON SchemaId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_schema_versions
           (Values.ListSchemaVersionsInput.make ?maxResults ?nextToken
              ~schemaId:(Values.SchemaId.of_json schemaId) ())
           (Some Values.ListSchemaVersionsResponse.to_json)
           (Some Values.ListSchemaVersionsResponse.error_to_json)])
let list_schemas =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional json_arg) ~doc:"JSON RegistryId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsNumber"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING SchemaRegistryTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_schemas
           (Values.ListSchemasInput.make
              ?registryId:(Option.map ~f:Values.RegistryId.of_json registryId)
              ?maxResults ?nextToken ())
           (Some Values.ListSchemasResponse.to_json)
           (Some Values.ListSchemasResponse.error_to_json)])
let list_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING OrchestrationToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and requestOrigin =
         flag "request-origin" (optional string)
           ~doc:"STRING OrchestrationNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sessions
           (Values.ListSessionsRequest.make ?nextToken ?maxResults
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?requestOrigin ()) (Some Values.ListSessionsResponse.to_json)
           (Some Values.ListSessionsResponse.error_to_json)])
let list_statements =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestOrigin =
         flag "request-origin" (optional string)
           ~doc:"STRING OrchestrationNameString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING OrchestrationToken"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_statements
           (Values.ListStatementsRequest.make ?requestOrigin ?nextToken
              ~sessionId ()) (Some Values.ListStatementsResponse.to_json)
           (Some Values.ListStatementsResponse.error_to_json)])
let list_table_optimizer_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxListTableOptimizerRunsTokenResults"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListTableOptimizerRunsToken"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON TableOptimizerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_table_optimizer_runs
           (Values.ListTableOptimizerRunsRequest.make ?maxResults ?nextToken
              ~catalogId ~databaseName ~tableName
              ~type_:(Values.TableOptimizerType.of_json type_) ())
           (Some Values.ListTableOptimizerRunsResponse.to_json)
           (Some Values.ListTableOptimizerRunsResponse.error_to_json)])
let list_triggers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and dependentJobName =
         flag "dependent-job-name" (optional string) ~doc:"STRING NameString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT OrchestrationPageSize200"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_triggers
           (Values.ListTriggersRequest.make ?nextToken ?dependentJobName
              ?maxResults ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ()) (Some Values.ListTriggersResponse.to_json)
           (Some Values.ListTriggersResponse.error_to_json)])
let list_usage_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING OrchestrationToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT OrchestrationPageSize200" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_usage_profiles
           (Values.ListUsageProfilesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListUsageProfilesResponse.to_json)
           (Some Values.ListUsageProfilesResponse.error_to_json)])
let list_workflows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT OrchestrationPageSize25" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflows
           (Values.ListWorkflowsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListWorkflowsResponse.to_json)
           (Some Values.ListWorkflowsResponse.error_to_json)])
let modify_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING IntegrationDescription"
       and dataFilter =
         flag "data-filter" (optional string) ~doc:"STRING String2048"
       and integrationConfig =
         flag "integration-config" (optional json_arg)
           ~doc:"JSON IntegrationConfig"
       and integrationName =
         flag "integration-name" (optional string) ~doc:"STRING String128"
       and integrationIdentifier =
         flag "integration-identifier" (required string)
           ~doc:"STRING String128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_integration
           (Values.ModifyIntegrationRequest.make ?description ?dataFilter
              ?integrationConfig:(Option.map
                                    ~f:Values.IntegrationConfig.of_json
                                    integrationConfig) ?integrationName
              ~integrationIdentifier ())
           (Some Values.ModifyIntegrationResponse.to_json)
           (Some Values.ModifyIntegrationResponse.error_to_json)])
let put_data_catalog_encryption_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and dataCatalogEncryptionSettings =
         flag "data-catalog-encryption-settings" (required json_arg)
           ~doc:"JSON DataCatalogEncryptionSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_data_catalog_encryption_settings
           (Values.PutDataCatalogEncryptionSettingsRequest.make ?catalogId
              ~dataCatalogEncryptionSettings:(Values.DataCatalogEncryptionSettings.of_json
                                                dataCatalogEncryptionSettings)
              ())
           (Some Values.PutDataCatalogEncryptionSettingsResponse.to_json)
           (Some
              Values.PutDataCatalogEncryptionSettingsResponse.error_to_json)])
let put_data_quality_profile_annotation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId =
         flag "profile-id" (required string) ~doc:"STRING HashString"
       and inclusionAnnotation =
         flag "inclusion-annotation" (required json_arg)
           ~doc:"JSON InclusionAnnotationValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_data_quality_profile_annotation
           (Values.PutDataQualityProfileAnnotationRequest.make ~profileId
              ~inclusionAnnotation:(Values.InclusionAnnotationValue.of_json
                                      inclusionAnnotation) ())
           (Some Values.PutDataQualityProfileAnnotationResponse.to_json)
           (Some Values.PutDataQualityProfileAnnotationResponse.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING GlueResourceArn"
       and policyHashCondition =
         flag "policy-hash-condition" (optional string)
           ~doc:"STRING HashString"
       and policyExistsCondition =
         flag "policy-exists-condition" (optional json_arg)
           ~doc:"JSON ExistCondition"
       and enableHybrid =
         flag "enable-hybrid" (optional json_arg)
           ~doc:"JSON EnableHybridValues"
       and policyInJson =
         flag "policy-in-json" (required string)
           ~doc:"STRING PolicyJsonString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyRequest.make ?resourceArn
              ?policyHashCondition
              ?policyExistsCondition:(Option.map
                                        ~f:Values.ExistCondition.of_json
                                        policyExistsCondition)
              ?enableHybrid:(Option.map ~f:Values.EnableHybridValues.of_json
                               enableHybrid) ~policyInJson ())
           (Some Values.PutResourcePolicyResponse.to_json)
           (Some Values.PutResourcePolicyResponse.error_to_json)])
let put_schema_version_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaId =
         flag "schema-id" (optional json_arg) ~doc:"JSON SchemaId"
       and schemaVersionNumber =
         flag "schema-version-number" (optional json_arg)
           ~doc:"JSON SchemaVersionNumber"
       and schemaVersionId =
         flag "schema-version-id" (optional string)
           ~doc:"STRING SchemaVersionIdString"
       and metadataKeyValue =
         flag "metadata-key-value" (required json_arg)
           ~doc:"JSON MetadataKeyValuePair" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_schema_version_metadata
           (Values.PutSchemaVersionMetadataInput.make
              ?schemaId:(Option.map ~f:Values.SchemaId.of_json schemaId)
              ?schemaVersionNumber:(Option.map
                                      ~f:Values.SchemaVersionNumber.of_json
                                      schemaVersionNumber) ?schemaVersionId
              ~metadataKeyValue:(Values.MetadataKeyValuePair.of_json
                                   metadataKeyValue) ())
           (Some Values.PutSchemaVersionMetadataResponse.to_json)
           (Some Values.PutSchemaVersionMetadataResponse.error_to_json)])
let put_workflow_run_properties =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and runId = flag "run-id" (required string) ~doc:"STRING IdString"
       and runProperties =
         flag "run-properties" (required json_arg)
           ~doc:"JSON WorkflowRunProperties" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_workflow_run_properties
           (Values.PutWorkflowRunPropertiesRequest.make ~name ~runId
              ~runProperties:(Values.WorkflowRunProperties.of_json
                                runProperties) ())
           (Some Values.PutWorkflowRunPropertiesResponse.to_json)
           (Some Values.PutWorkflowRunPropertiesResponse.error_to_json)])
let query_schema_version_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaId =
         flag "schema-id" (optional json_arg) ~doc:"JSON SchemaId"
       and schemaVersionNumber =
         flag "schema-version-number" (optional json_arg)
           ~doc:"JSON SchemaVersionNumber"
       and schemaVersionId =
         flag "schema-version-id" (optional string)
           ~doc:"STRING SchemaVersionIdString"
       and metadataList =
         flag "metadata-list" (optional json_arg) ~doc:"JSON MetadataList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT QuerySchemaVersionMetadataMaxResults"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING SchemaRegistryTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.query_schema_version_metadata
           (Values.QuerySchemaVersionMetadataInput.make
              ?schemaId:(Option.map ~f:Values.SchemaId.of_json schemaId)
              ?schemaVersionNumber:(Option.map
                                      ~f:Values.SchemaVersionNumber.of_json
                                      schemaVersionNumber) ?schemaVersionId
              ?metadataList:(Option.map ~f:Values.MetadataList.of_json
                               metadataList) ?maxResults ?nextToken ())
           (Some Values.QuerySchemaVersionMetadataResponse.to_json)
           (Some Values.QuerySchemaVersionMetadataResponse.error_to_json)])
let register_connection_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and connectionType =
         flag "connection-type" (required string) ~doc:"STRING NameString"
       and integrationType =
         flag "integration-type" (required json_arg)
           ~doc:"JSON IntegrationType"
       and connectionProperties =
         flag "connection-properties" (required json_arg)
           ~doc:"JSON ConnectionPropertiesConfiguration"
       and connectorAuthenticationConfiguration =
         flag "connector-authentication-configuration" (required json_arg)
           ~doc:"JSON ConnectorAuthenticationConfiguration"
       and restConfiguration =
         flag "rest-configuration" (required json_arg)
           ~doc:"JSON RestConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_connection_type
           (Values.RegisterConnectionTypeRequest.make ?description
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~connectionType
              ~integrationType:(Values.IntegrationType.of_json
                                  integrationType)
              ~connectionProperties:(Values.ConnectionPropertiesConfiguration.of_json
                                       connectionProperties)
              ~connectorAuthenticationConfiguration:(Values.ConnectorAuthenticationConfiguration.of_json
                                                       connectorAuthenticationConfiguration)
              ~restConfiguration:(Values.RestConfiguration.of_json
                                    restConfiguration) ())
           (Some Values.RegisterConnectionTypeResponse.to_json)
           (Some Values.RegisterConnectionTypeResponse.error_to_json)])
let register_schema_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaId =
         flag "schema-id" (required json_arg) ~doc:"JSON SchemaId"
       and schemaDefinition =
         flag "schema-definition" (required string)
           ~doc:"STRING SchemaDefinitionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_schema_version
           (Values.RegisterSchemaVersionInput.make
              ~schemaId:(Values.SchemaId.of_json schemaId) ~schemaDefinition
              ()) (Some Values.RegisterSchemaVersionResponse.to_json)
           (Some Values.RegisterSchemaVersionResponse.error_to_json)])
let remove_schema_version_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaId =
         flag "schema-id" (optional json_arg) ~doc:"JSON SchemaId"
       and schemaVersionNumber =
         flag "schema-version-number" (optional json_arg)
           ~doc:"JSON SchemaVersionNumber"
       and schemaVersionId =
         flag "schema-version-id" (optional string)
           ~doc:"STRING SchemaVersionIdString"
       and metadataKeyValue =
         flag "metadata-key-value" (required json_arg)
           ~doc:"JSON MetadataKeyValuePair" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_schema_version_metadata
           (Values.RemoveSchemaVersionMetadataInput.make
              ?schemaId:(Option.map ~f:Values.SchemaId.of_json schemaId)
              ?schemaVersionNumber:(Option.map
                                      ~f:Values.SchemaVersionNumber.of_json
                                      schemaVersionNumber) ?schemaVersionId
              ~metadataKeyValue:(Values.MetadataKeyValuePair.of_json
                                   metadataKeyValue) ())
           (Some Values.RemoveSchemaVersionMetadataResponse.to_json)
           (Some Values.RemoveSchemaVersionMetadataResponse.error_to_json)])
let reset_job_bookmark =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and runId = flag "run-id" (optional string) ~doc:"STRING RunId"
       and jobName = flag "job-name" (required string) ~doc:"STRING JobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_job_bookmark
           (Values.ResetJobBookmarkRequest.make ?runId ~jobName ())
           (Some Values.ResetJobBookmarkResponse.to_json)
           (Some Values.ResetJobBookmarkResponse.error_to_json)])
let resume_workflow_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and runId = flag "run-id" (required string) ~doc:"STRING IdString"
       and nodeIds =
         flag "node-ids" (required json_arg) ~doc:"JSON NodeIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resume_workflow_run
           (Values.ResumeWorkflowRunRequest.make ~name ~runId
              ~nodeIds:(Values.NodeIdList.of_json nodeIds) ())
           (Some Values.ResumeWorkflowRunResponse.to_json)
           (Some Values.ResumeWorkflowRunResponse.error_to_json)])
let run_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestOrigin =
         flag "request-origin" (optional string)
           ~doc:"STRING OrchestrationNameString"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING NameString"
       and code =
         flag "code" (required string)
           ~doc:"STRING OrchestrationStatementCodeString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.run_statement
           (Values.RunStatementRequest.make ?requestOrigin ~sessionId ~code
              ()) (Some Values.RunStatementResponse.to_json)
           (Some Values.RunStatementResponse.error_to_json)])
let search_tables =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON SearchPropertyPredicates"
       and searchText =
         flag "search-text" (optional string) ~doc:"STRING ValueString"
       and sortCriteria =
         flag "sort-criteria" (optional json_arg) ~doc:"JSON SortCriteria"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and resourceShareType =
         flag "resource-share-type" (optional json_arg)
           ~doc:"JSON ResourceShareType"
       and includeStatusDetails =
         flag "include-status-details" (optional bool)
           ~doc:"BOOL BooleanNullable" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_tables
           (Values.SearchTablesRequest.make ?catalogId ?nextToken
              ?filters:(Option.map ~f:Values.SearchPropertyPredicates.of_json
                          filters) ?searchText
              ?sortCriteria:(Option.map ~f:Values.SortCriteria.of_json
                               sortCriteria) ?maxResults
              ?resourceShareType:(Option.map
                                    ~f:Values.ResourceShareType.of_json
                                    resourceShareType) ?includeStatusDetails
              ()) (Some Values.SearchTablesResponse.to_json)
           (Some Values.SearchTablesResponse.error_to_json)])
let start_blueprint_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional string)
           ~doc:"STRING BlueprintParameters"
       and blueprintName =
         flag "blueprint-name" (required string)
           ~doc:"STRING OrchestrationNameString"
       and roleArn =
         flag "role-arn" (required string)
           ~doc:"STRING OrchestrationIAMRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_blueprint_run
           (Values.StartBlueprintRunRequest.make ?parameters ~blueprintName
              ~roleArn ()) (Some Values.StartBlueprintRunResponse.to_json)
           (Some Values.StartBlueprintRunResponse.error_to_json)])
let start_column_statistics_task_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and columnNameList =
         flag "column-name-list" (optional json_arg)
           ~doc:"JSON ColumnNameList"
       and sampleSize =
         flag "sample-size" (optional float)
           ~doc:"FLOAT SampleSizePercentage"
       and catalogID =
         flag "catalog-i-d" (optional string) ~doc:"STRING NameString"
       and securityConfiguration =
         flag "security-configuration" (optional string)
           ~doc:"STRING NameString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and role = flag "role" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_column_statistics_task_run
           (Values.StartColumnStatisticsTaskRunRequest.make
              ?columnNameList:(Option.map ~f:Values.ColumnNameList.of_json
                                 columnNameList) ?sampleSize ?catalogID
              ?securityConfiguration ~databaseName ~tableName ~role ())
           (Some Values.StartColumnStatisticsTaskRunResponse.to_json)
           (Some Values.StartColumnStatisticsTaskRunResponse.error_to_json)])
let start_column_statistics_task_run_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_column_statistics_task_run_schedule
           (Values.StartColumnStatisticsTaskRunScheduleRequest.make
              ~databaseName ~tableName ())
           (Some Values.StartColumnStatisticsTaskRunScheduleResponse.to_json)
           (Some
              Values.StartColumnStatisticsTaskRunScheduleResponse.error_to_json)])
let start_crawler =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_crawler (Values.StartCrawlerRequest.make ~name ())
           (Some Values.StartCrawlerResponse.to_json)
           (Some Values.StartCrawlerResponse.error_to_json)])
let start_crawler_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crawlerName =
         flag "crawler-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_crawler_schedule
           (Values.StartCrawlerScheduleRequest.make ~crawlerName ())
           (Some Values.StartCrawlerScheduleResponse.to_json)
           (Some Values.StartCrawlerScheduleResponse.error_to_json)])
let start_data_quality_rule_recommendation_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and numberOfWorkers =
         flag "number-of-workers" (optional int) ~doc:"INT NullableInteger"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and createdRulesetName =
         flag "created-ruleset-name" (optional string)
           ~doc:"STRING NameString"
       and dataQualitySecurityConfiguration =
         flag "data-quality-security-configuration" (optional string)
           ~doc:"STRING NameString"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING HashString"
       and dataSource =
         flag "data-source" (required json_arg) ~doc:"JSON DataSource"
       and role = flag "role" (required string) ~doc:"STRING RoleString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_data_quality_rule_recommendation_run
           (Values.StartDataQualityRuleRecommendationRunRequest.make
              ?numberOfWorkers ?timeout ?createdRulesetName
              ?dataQualitySecurityConfiguration ?clientToken
              ~dataSource:(Values.DataSource.of_json dataSource) ~role ())
           (Some Values.StartDataQualityRuleRecommendationRunResponse.to_json)
           (Some
              Values.StartDataQualityRuleRecommendationRunResponse.error_to_json)])
let start_data_quality_ruleset_evaluation_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and numberOfWorkers =
         flag "number-of-workers" (optional int) ~doc:"INT NullableInteger"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING HashString"
       and additionalRunOptions =
         flag "additional-run-options" (optional json_arg)
           ~doc:"JSON DataQualityEvaluationRunAdditionalRunOptions"
       and additionalDataSources =
         flag "additional-data-sources" (optional json_arg)
           ~doc:"JSON DataSourceMap"
       and dataSource =
         flag "data-source" (required json_arg) ~doc:"JSON DataSource"
       and role = flag "role" (required string) ~doc:"STRING RoleString"
       and rulesetNames =
         flag "ruleset-names" (required json_arg) ~doc:"JSON RulesetNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_data_quality_ruleset_evaluation_run
           (Values.StartDataQualityRulesetEvaluationRunRequest.make
              ?numberOfWorkers ?timeout ?clientToken
              ?additionalRunOptions:(Option.map
                                       ~f:Values.DataQualityEvaluationRunAdditionalRunOptions.of_json
                                       additionalRunOptions)
              ?additionalDataSources:(Option.map
                                        ~f:Values.DataSourceMap.of_json
                                        additionalDataSources)
              ~dataSource:(Values.DataSource.of_json dataSource) ~role
              ~rulesetNames:(Values.RulesetNames.of_json rulesetNames) ())
           (Some Values.StartDataQualityRulesetEvaluationRunResponse.to_json)
           (Some
              Values.StartDataQualityRulesetEvaluationRunResponse.error_to_json)])
let start_export_labels_task_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transformId =
         flag "transform-id" (required string) ~doc:"STRING HashString"
       and outputS3Path =
         flag "output-s3-path" (required string) ~doc:"STRING UriString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_export_labels_task_run
           (Values.StartExportLabelsTaskRunRequest.make ~transformId
              ~outputS3Path ())
           (Some Values.StartExportLabelsTaskRunResponse.to_json)
           (Some Values.StartExportLabelsTaskRunResponse.error_to_json)])
let start_import_labels_task_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replaceAllLabels =
         flag "replace-all-labels" (optional bool) ~doc:"BOOL ReplaceBoolean"
       and transformId =
         flag "transform-id" (required string) ~doc:"STRING HashString"
       and inputS3Path =
         flag "input-s3-path" (required string) ~doc:"STRING UriString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_import_labels_task_run
           (Values.StartImportLabelsTaskRunRequest.make ?replaceAllLabels
              ~transformId ~inputS3Path ())
           (Some Values.StartImportLabelsTaskRunResponse.to_json)
           (Some Values.StartImportLabelsTaskRunResponse.error_to_json)])
let start_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobRunQueuingEnabled =
         flag "job-run-queuing-enabled" (optional bool)
           ~doc:"BOOL NullableBoolean"
       and jobRunId =
         flag "job-run-id" (optional string) ~doc:"STRING IdString"
       and arguments =
         flag "arguments" (optional json_arg) ~doc:"JSON GenericMap"
       and allocatedCapacity =
         flag "allocated-capacity" (optional int) ~doc:"INT IntegerValue"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and maxCapacity =
         flag "max-capacity" (optional float) ~doc:"FLOAT NullableDouble"
       and securityConfiguration =
         flag "security-configuration" (optional string)
           ~doc:"STRING NameString"
       and notificationProperty =
         flag "notification-property" (optional json_arg)
           ~doc:"JSON NotificationProperty"
       and workerType =
         flag "worker-type" (optional json_arg) ~doc:"JSON WorkerType"
       and numberOfWorkers =
         flag "number-of-workers" (optional int) ~doc:"INT NullableInteger"
       and executionClass =
         flag "execution-class" (optional json_arg)
           ~doc:"JSON ExecutionClass"
       and executionRoleSessionPolicy =
         flag "execution-role-session-policy" (optional string)
           ~doc:"STRING OrchestrationPolicyJsonString"
       and jobName =
         flag "job-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_job_run
           (Values.StartJobRunRequest.make ?jobRunQueuingEnabled ?jobRunId
              ?arguments:(Option.map ~f:Values.GenericMap.of_json arguments)
              ?allocatedCapacity ?timeout ?maxCapacity ?securityConfiguration
              ?notificationProperty:(Option.map
                                       ~f:Values.NotificationProperty.of_json
                                       notificationProperty)
              ?workerType:(Option.map ~f:Values.WorkerType.of_json workerType)
              ?numberOfWorkers
              ?executionClass:(Option.map ~f:Values.ExecutionClass.of_json
                                 executionClass) ?executionRoleSessionPolicy
              ~jobName ()) (Some Values.StartJobRunResponse.to_json)
           (Some Values.StartJobRunResponse.error_to_json)])
let start_m_l_evaluation_task_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transformId =
         flag "transform-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_m_l_evaluation_task_run
           (Values.StartMLEvaluationTaskRunRequest.make ~transformId ())
           (Some Values.StartMLEvaluationTaskRunResponse.to_json)
           (Some Values.StartMLEvaluationTaskRunResponse.error_to_json)])
let start_m_l_labeling_set_generation_task_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transformId =
         flag "transform-id" (required string) ~doc:"STRING HashString"
       and outputS3Path =
         flag "output-s3-path" (required string) ~doc:"STRING UriString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_m_l_labeling_set_generation_task_run
           (Values.StartMLLabelingSetGenerationTaskRunRequest.make
              ~transformId ~outputS3Path ())
           (Some Values.StartMLLabelingSetGenerationTaskRunResponse.to_json)
           (Some
              Values.StartMLLabelingSetGenerationTaskRunResponse.error_to_json)])
let start_materialized_view_refresh_task_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fullRefresh =
         flag "full-refresh" (optional bool) ~doc:"BOOL NullableBoolean"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING NameString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_materialized_view_refresh_task_run
           (Values.StartMaterializedViewRefreshTaskRunRequest.make
              ?fullRefresh ~catalogId ~databaseName ~tableName ())
           (Some Values.StartMaterializedViewRefreshTaskRunResponse.to_json)
           (Some
              Values.StartMaterializedViewRefreshTaskRunResponse.error_to_json)])
let start_trigger =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_trigger (Values.StartTriggerRequest.make ~name ())
           (Some Values.StartTriggerResponse.to_json)
           (Some Values.StartTriggerResponse.error_to_json)])
let start_workflow_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and runProperties =
         flag "run-properties" (optional json_arg)
           ~doc:"JSON WorkflowRunProperties"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_workflow_run
           (Values.StartWorkflowRunRequest.make
              ?runProperties:(Option.map
                                ~f:Values.WorkflowRunProperties.of_json
                                runProperties) ~name ())
           (Some Values.StartWorkflowRunResponse.to_json)
           (Some Values.StartWorkflowRunResponse.error_to_json)])
let stop_column_statistics_task_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING DatabaseName"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_column_statistics_task_run
           (Values.StopColumnStatisticsTaskRunRequest.make ~databaseName
              ~tableName ())
           (Some Values.StopColumnStatisticsTaskRunResponse.to_json)
           (Some Values.StopColumnStatisticsTaskRunResponse.error_to_json)])
let stop_column_statistics_task_run_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_column_statistics_task_run_schedule
           (Values.StopColumnStatisticsTaskRunScheduleRequest.make
              ~databaseName ~tableName ())
           (Some Values.StopColumnStatisticsTaskRunScheduleResponse.to_json)
           (Some
              Values.StopColumnStatisticsTaskRunScheduleResponse.error_to_json)])
let stop_crawler =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_crawler (Values.StopCrawlerRequest.make ~name ())
           (Some Values.StopCrawlerResponse.to_json)
           (Some Values.StopCrawlerResponse.error_to_json)])
let stop_crawler_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crawlerName =
         flag "crawler-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_crawler_schedule
           (Values.StopCrawlerScheduleRequest.make ~crawlerName ())
           (Some Values.StopCrawlerScheduleResponse.to_json)
           (Some Values.StopCrawlerScheduleResponse.error_to_json)])
let stop_materialized_view_refresh_task_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING NameString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_materialized_view_refresh_task_run
           (Values.StopMaterializedViewRefreshTaskRunRequest.make ~catalogId
              ~databaseName ~tableName ())
           (Some Values.StopMaterializedViewRefreshTaskRunResponse.to_json)
           (Some
              Values.StopMaterializedViewRefreshTaskRunResponse.error_to_json)])
let stop_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestOrigin =
         flag "request-origin" (optional string)
           ~doc:"STRING OrchestrationNameString"
       and id = flag "id" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_session
           (Values.StopSessionRequest.make ?requestOrigin ~id ())
           (Some Values.StopSessionResponse.to_json)
           (Some Values.StopSessionResponse.error_to_json)])
let stop_trigger =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_trigger (Values.StopTriggerRequest.make ~name ())
           (Some Values.StopTriggerResponse.to_json)
           (Some Values.StopTriggerResponse.error_to_json)])
let stop_workflow_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and runId = flag "run-id" (required string) ~doc:"STRING IdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_workflow_run
           (Values.StopWorkflowRunRequest.make ~name ~runId ())
           (Some Values.StopWorkflowRunResponse.to_json)
           (Some Values.StopWorkflowRunResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING GlueResourceArn"
       and tagsToAdd =
         flag "tags-to-add" (required json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tagsToAdd:(Values.TagsMap.of_json tagsToAdd) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let test_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionName =
         flag "connection-name" (optional string) ~doc:"STRING NameString"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and testConnectionInput =
         flag "test-connection-input" (optional json_arg)
           ~doc:"JSON TestConnectionInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_connection
           (Values.TestConnectionRequest.make ?connectionName ?catalogId
              ?testConnectionInput:(Option.map
                                      ~f:Values.TestConnectionInput.of_json
                                      testConnectionInput) ())
           (Some Values.TestConnectionResponse.to_json)
           (Some Values.TestConnectionResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING GlueResourceArn"
       and tagsToRemove =
         flag "tags-to-remove" (required json_arg) ~doc:"JSON TagKeysList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagsToRemove:(Values.TagKeysList.of_json tagsToRemove) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_blueprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING Generic512CharString"
       and name =
         flag "name" (required string) ~doc:"STRING OrchestrationNameString"
       and blueprintLocation =
         flag "blueprint-location" (required string)
           ~doc:"STRING OrchestrationS3Location" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_blueprint
           (Values.UpdateBlueprintRequest.make ?description ~name
              ~blueprintLocation ())
           (Some Values.UpdateBlueprintResponse.to_json)
           (Some Values.UpdateBlueprintResponse.error_to_json)])
let update_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING CatalogIdString"
       and catalogInput =
         flag "catalog-input" (required json_arg) ~doc:"JSON CatalogInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_catalog
           (Values.UpdateCatalogRequest.make ~catalogId
              ~catalogInput:(Values.CatalogInput.of_json catalogInput) ())
           (Some Values.UpdateCatalogResponse.to_json)
           (Some Values.UpdateCatalogResponse.error_to_json)])
let update_classifier =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grokClassifier =
         flag "grok-classifier" (optional json_arg)
           ~doc:"JSON UpdateGrokClassifierRequest"
       and xMLClassifier =
         flag "x-m-l-classifier" (optional json_arg)
           ~doc:"JSON UpdateXMLClassifierRequest"
       and jsonClassifier =
         flag "json-classifier" (optional json_arg)
           ~doc:"JSON UpdateJsonClassifierRequest"
       and csvClassifier =
         flag "csv-classifier" (optional json_arg)
           ~doc:"JSON UpdateCsvClassifierRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_classifier
           (Values.UpdateClassifierRequest.make
              ?grokClassifier:(Option.map
                                 ~f:Values.UpdateGrokClassifierRequest.of_json
                                 grokClassifier)
              ?xMLClassifier:(Option.map
                                ~f:Values.UpdateXMLClassifierRequest.of_json
                                xMLClassifier)
              ?jsonClassifier:(Option.map
                                 ~f:Values.UpdateJsonClassifierRequest.of_json
                                 jsonClassifier)
              ?csvClassifier:(Option.map
                                ~f:Values.UpdateCsvClassifierRequest.of_json
                                csvClassifier) ())
           (Some Values.UpdateClassifierResponse.to_json)
           (Some Values.UpdateClassifierResponse.error_to_json)])
let update_column_statistics_for_partition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and partitionValues =
         flag "partition-values" (required json_arg)
           ~doc:"JSON ValueStringList"
       and columnStatisticsList =
         flag "column-statistics-list" (required json_arg)
           ~doc:"JSON UpdateColumnStatisticsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_column_statistics_for_partition
           (Values.UpdateColumnStatisticsForPartitionRequest.make ?catalogId
              ~databaseName ~tableName
              ~partitionValues:(Values.ValueStringList.of_json
                                  partitionValues)
              ~columnStatisticsList:(Values.UpdateColumnStatisticsList.of_json
                                       columnStatisticsList) ())
           (Some Values.UpdateColumnStatisticsForPartitionResponse.to_json)
           (Some
              Values.UpdateColumnStatisticsForPartitionResponse.error_to_json)])
let update_column_statistics_for_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and columnStatisticsList =
         flag "column-statistics-list" (required json_arg)
           ~doc:"JSON UpdateColumnStatisticsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_column_statistics_for_table
           (Values.UpdateColumnStatisticsForTableRequest.make ?catalogId
              ~databaseName ~tableName
              ~columnStatisticsList:(Values.UpdateColumnStatisticsList.of_json
                                       columnStatisticsList) ())
           (Some Values.UpdateColumnStatisticsForTableResponse.to_json)
           (Some Values.UpdateColumnStatisticsForTableResponse.error_to_json)])
let update_column_statistics_task_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and role = flag "role" (optional string) ~doc:"STRING NameString"
       and schedule =
         flag "schedule" (optional string) ~doc:"STRING CronExpression"
       and columnNameList =
         flag "column-name-list" (optional json_arg)
           ~doc:"JSON ColumnNameList"
       and sampleSize =
         flag "sample-size" (optional float)
           ~doc:"FLOAT SampleSizePercentage"
       and catalogID =
         flag "catalog-i-d" (optional string) ~doc:"STRING NameString"
       and securityConfiguration =
         flag "security-configuration" (optional string)
           ~doc:"STRING NameString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_column_statistics_task_settings
           (Values.UpdateColumnStatisticsTaskSettingsRequest.make ?role
              ?schedule
              ?columnNameList:(Option.map ~f:Values.ColumnNameList.of_json
                                 columnNameList) ?sampleSize ?catalogID
              ?securityConfiguration ~databaseName ~tableName ())
           (Some Values.UpdateColumnStatisticsTaskSettingsResponse.to_json)
           (Some
              Values.UpdateColumnStatisticsTaskSettingsResponse.error_to_json)])
let update_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and connectionInput =
         flag "connection-input" (required json_arg)
           ~doc:"JSON ConnectionInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connection
           (Values.UpdateConnectionRequest.make ?catalogId ~name
              ~connectionInput:(Values.ConnectionInput.of_json
                                  connectionInput) ())
           (Some Values.UpdateConnectionResponse.to_json)
           (Some Values.UpdateConnectionResponse.error_to_json)])
let update_crawler =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and role = flag "role" (optional string) ~doc:"STRING Role"
       and databaseName =
         flag "database-name" (optional string) ~doc:"STRING DatabaseName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING DescriptionStringRemovable"
       and targets =
         flag "targets" (optional json_arg) ~doc:"JSON CrawlerTargets"
       and schedule =
         flag "schedule" (optional string) ~doc:"STRING CronExpression"
       and classifiers =
         flag "classifiers" (optional json_arg)
           ~doc:"JSON ClassifierNameList"
       and tablePrefix =
         flag "table-prefix" (optional string) ~doc:"STRING TablePrefix"
       and schemaChangePolicy =
         flag "schema-change-policy" (optional json_arg)
           ~doc:"JSON SchemaChangePolicy"
       and recrawlPolicy =
         flag "recrawl-policy" (optional json_arg) ~doc:"JSON RecrawlPolicy"
       and lineageConfiguration =
         flag "lineage-configuration" (optional json_arg)
           ~doc:"JSON LineageConfiguration"
       and lakeFormationConfiguration =
         flag "lake-formation-configuration" (optional json_arg)
           ~doc:"JSON LakeFormationConfiguration"
       and configuration =
         flag "configuration" (optional string)
           ~doc:"STRING CrawlerConfiguration"
       and crawlerSecurityConfiguration =
         flag "crawler-security-configuration" (optional string)
           ~doc:"STRING CrawlerSecurityConfiguration"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_crawler
           (Values.UpdateCrawlerRequest.make ?role ?databaseName ?description
              ?targets:(Option.map ~f:Values.CrawlerTargets.of_json targets)
              ?schedule
              ?classifiers:(Option.map ~f:Values.ClassifierNameList.of_json
                              classifiers) ?tablePrefix
              ?schemaChangePolicy:(Option.map
                                     ~f:Values.SchemaChangePolicy.of_json
                                     schemaChangePolicy)
              ?recrawlPolicy:(Option.map ~f:Values.RecrawlPolicy.of_json
                                recrawlPolicy)
              ?lineageConfiguration:(Option.map
                                       ~f:Values.LineageConfiguration.of_json
                                       lineageConfiguration)
              ?lakeFormationConfiguration:(Option.map
                                             ~f:Values.LakeFormationConfiguration.of_json
                                             lakeFormationConfiguration)
              ?configuration ?crawlerSecurityConfiguration ~name ())
           (Some Values.UpdateCrawlerResponse.to_json)
           (Some Values.UpdateCrawlerResponse.error_to_json)])
let update_crawler_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schedule =
         flag "schedule" (optional string) ~doc:"STRING CronExpression"
       and crawlerName =
         flag "crawler-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_crawler_schedule
           (Values.UpdateCrawlerScheduleRequest.make ?schedule ~crawlerName
              ()) (Some Values.UpdateCrawlerScheduleResponse.to_json)
           (Some Values.UpdateCrawlerScheduleResponse.error_to_json)])
let update_data_quality_ruleset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and ruleset =
         flag "ruleset" (optional string)
           ~doc:"STRING DataQualityRulesetString"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_quality_ruleset
           (Values.UpdateDataQualityRulesetRequest.make ?description ?ruleset
              ~name ())
           (Some Values.UpdateDataQualityRulesetResponse.to_json)
           (Some Values.UpdateDataQualityRulesetResponse.error_to_json)])
let update_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and databaseInput =
         flag "database-input" (required json_arg) ~doc:"JSON DatabaseInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_database
           (Values.UpdateDatabaseRequest.make ?catalogId ~name
              ~databaseInput:(Values.DatabaseInput.of_json databaseInput) ())
           (Some Values.UpdateDatabaseResponse.to_json)
           (Some Values.UpdateDatabaseResponse.error_to_json)])
let update_dev_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and publicKey =
         flag "public-key" (optional string) ~doc:"STRING GenericString"
       and addPublicKeys =
         flag "add-public-keys" (optional json_arg)
           ~doc:"JSON PublicKeysList"
       and deletePublicKeys =
         flag "delete-public-keys" (optional json_arg)
           ~doc:"JSON PublicKeysList"
       and customLibraries =
         flag "custom-libraries" (optional json_arg)
           ~doc:"JSON DevEndpointCustomLibraries"
       and updateEtlLibraries =
         flag "update-etl-libraries" (optional bool) ~doc:"BOOL BooleanValue"
       and deleteArguments =
         flag "delete-arguments" (optional json_arg) ~doc:"JSON StringList"
       and addArguments =
         flag "add-arguments" (optional json_arg) ~doc:"JSON MapValue"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_dev_endpoint
           (Values.UpdateDevEndpointRequest.make ?publicKey
              ?addPublicKeys:(Option.map ~f:Values.PublicKeysList.of_json
                                addPublicKeys)
              ?deletePublicKeys:(Option.map ~f:Values.PublicKeysList.of_json
                                   deletePublicKeys)
              ?customLibraries:(Option.map
                                  ~f:Values.DevEndpointCustomLibraries.of_json
                                  customLibraries) ?updateEtlLibraries
              ?deleteArguments:(Option.map ~f:Values.StringList.of_json
                                  deleteArguments)
              ?addArguments:(Option.map ~f:Values.MapValue.of_json
                               addArguments) ~endpointName ())
           (Some Values.UpdateDevEndpointResponse.to_json)
           (Some Values.UpdateDevEndpointResponse.error_to_json)])
let update_glue_identity_center_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scopes =
         flag "scopes" (optional json_arg)
           ~doc:"JSON IdentityCenterScopesList"
       and userBackgroundSessionsEnabled =
         flag "user-background-sessions-enabled" (optional bool)
           ~doc:"BOOL NullableBoolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_glue_identity_center_configuration
           (Values.UpdateGlueIdentityCenterConfigurationRequest.make
              ?scopes:(Option.map ~f:Values.IdentityCenterScopesList.of_json
                         scopes) ?userBackgroundSessionsEnabled ())
           (Some Values.UpdateGlueIdentityCenterConfigurationResponse.to_json)
           (Some
              Values.UpdateGlueIdentityCenterConfigurationResponse.error_to_json)])
let update_integration_resource_property =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceProcessingProperties =
         flag "source-processing-properties" (optional json_arg)
           ~doc:"JSON SourceProcessingProperties"
       and targetProcessingProperties =
         flag "target-processing-properties" (optional json_arg)
           ~doc:"JSON TargetProcessingProperties"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String512" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_integration_resource_property
           (Values.UpdateIntegrationResourcePropertyRequest.make
              ?sourceProcessingProperties:(Option.map
                                             ~f:Values.SourceProcessingProperties.of_json
                                             sourceProcessingProperties)
              ?targetProcessingProperties:(Option.map
                                             ~f:Values.TargetProcessingProperties.of_json
                                             targetProcessingProperties)
              ~resourceArn ())
           (Some Values.UpdateIntegrationResourcePropertyResponse.to_json)
           (Some
              Values.UpdateIntegrationResourcePropertyResponse.error_to_json)])
let update_integration_table_properties =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceTableConfig =
         flag "source-table-config" (optional json_arg)
           ~doc:"JSON SourceTableConfig"
       and targetTableConfig =
         flag "target-table-config" (optional json_arg)
           ~doc:"JSON TargetTableConfig"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String512"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING String128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_integration_table_properties
           (Values.UpdateIntegrationTablePropertiesRequest.make
              ?sourceTableConfig:(Option.map
                                    ~f:Values.SourceTableConfig.of_json
                                    sourceTableConfig)
              ?targetTableConfig:(Option.map
                                    ~f:Values.TargetTableConfig.of_json
                                    targetTableConfig) ~resourceArn
              ~tableName ())
           (Some Values.UpdateIntegrationTablePropertiesResponse.to_json)
           (Some
              Values.UpdateIntegrationTablePropertiesResponse.error_to_json)])
let update_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName =
         flag "job-name" (required string) ~doc:"STRING NameString"
       and jobUpdate =
         flag "job-update" (required json_arg) ~doc:"JSON JobUpdate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_job
           (Values.UpdateJobRequest.make ~jobName
              ~jobUpdate:(Values.JobUpdate.of_json jobUpdate) ())
           (Some Values.UpdateJobResponse.to_json)
           (Some Values.UpdateJobResponse.error_to_json)])
let update_job_from_source_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName =
         flag "job-name" (optional string) ~doc:"STRING NameString"
       and provider =
         flag "provider" (optional json_arg)
           ~doc:"JSON SourceControlProvider"
       and repositoryName =
         flag "repository-name" (optional string) ~doc:"STRING NameString"
       and repositoryOwner =
         flag "repository-owner" (optional string) ~doc:"STRING NameString"
       and branchName =
         flag "branch-name" (optional string) ~doc:"STRING NameString"
       and folder = flag "folder" (optional string) ~doc:"STRING NameString"
       and commitId =
         flag "commit-id" (optional string) ~doc:"STRING CommitIdString"
       and authStrategy =
         flag "auth-strategy" (optional json_arg)
           ~doc:"JSON SourceControlAuthStrategy"
       and authToken =
         flag "auth-token" (optional string) ~doc:"STRING AuthTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_job_from_source_control
           (Values.UpdateJobFromSourceControlRequest.make ?jobName
              ?provider:(Option.map ~f:Values.SourceControlProvider.of_json
                           provider) ?repositoryName ?repositoryOwner
              ?branchName ?folder ?commitId
              ?authStrategy:(Option.map
                               ~f:Values.SourceControlAuthStrategy.of_json
                               authStrategy) ?authToken ())
           (Some Values.UpdateJobFromSourceControlResponse.to_json)
           (Some Values.UpdateJobFromSourceControlResponse.error_to_json)])
let update_m_l_transform =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING NameString"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON TransformParameters"
       and role = flag "role" (optional string) ~doc:"STRING RoleString"
       and glueVersion =
         flag "glue-version" (optional string)
           ~doc:"STRING GlueVersionString"
       and maxCapacity =
         flag "max-capacity" (optional float) ~doc:"FLOAT NullableDouble"
       and workerType =
         flag "worker-type" (optional json_arg) ~doc:"JSON WorkerType"
       and numberOfWorkers =
         flag "number-of-workers" (optional int) ~doc:"INT NullableInteger"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and maxRetries =
         flag "max-retries" (optional int) ~doc:"INT NullableInteger"
       and transformId =
         flag "transform-id" (required string) ~doc:"STRING HashString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_m_l_transform
           (Values.UpdateMLTransformRequest.make ?name ?description
              ?parameters:(Option.map ~f:Values.TransformParameters.of_json
                             parameters) ?role ?glueVersion ?maxCapacity
              ?workerType:(Option.map ~f:Values.WorkerType.of_json workerType)
              ?numberOfWorkers ?timeout ?maxRetries ~transformId ())
           (Some Values.UpdateMLTransformResponse.to_json)
           (Some Values.UpdateMLTransformResponse.error_to_json)])
let update_partition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and partitionValueList =
         flag "partition-value-list" (required json_arg)
           ~doc:"JSON BoundedPartitionValueList"
       and partitionInput =
         flag "partition-input" (required json_arg)
           ~doc:"JSON PartitionInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_partition
           (Values.UpdatePartitionRequest.make ?catalogId ~databaseName
              ~tableName
              ~partitionValueList:(Values.BoundedPartitionValueList.of_json
                                     partitionValueList)
              ~partitionInput:(Values.PartitionInput.of_json partitionInput)
              ()) (Some Values.UpdatePartitionResponse.to_json)
           (Some Values.UpdatePartitionResponse.error_to_json)])
let update_registry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (required json_arg) ~doc:"JSON RegistryId"
       and description =
         flag "description" (required string) ~doc:"STRING DescriptionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_registry
           (Values.UpdateRegistryInput.make
              ~registryId:(Values.RegistryId.of_json registryId) ~description
              ()) (Some Values.UpdateRegistryResponse.to_json)
           (Some Values.UpdateRegistryResponse.error_to_json)])
let update_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaVersionNumber =
         flag "schema-version-number" (optional json_arg)
           ~doc:"JSON SchemaVersionNumber"
       and compatibility =
         flag "compatibility" (optional json_arg) ~doc:"JSON Compatibility"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and schemaId =
         flag "schema-id" (required json_arg) ~doc:"JSON SchemaId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_schema
           (Values.UpdateSchemaInput.make
              ?schemaVersionNumber:(Option.map
                                      ~f:Values.SchemaVersionNumber.of_json
                                      schemaVersionNumber)
              ?compatibility:(Option.map ~f:Values.Compatibility.of_json
                                compatibility) ?description
              ~schemaId:(Values.SchemaId.of_json schemaId) ())
           (Some Values.UpdateSchemaResponse.to_json)
           (Some Values.UpdateSchemaResponse.error_to_json)])
let update_source_control_from_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName =
         flag "job-name" (optional string) ~doc:"STRING NameString"
       and provider =
         flag "provider" (optional json_arg)
           ~doc:"JSON SourceControlProvider"
       and repositoryName =
         flag "repository-name" (optional string) ~doc:"STRING NameString"
       and repositoryOwner =
         flag "repository-owner" (optional string) ~doc:"STRING NameString"
       and branchName =
         flag "branch-name" (optional string) ~doc:"STRING NameString"
       and folder = flag "folder" (optional string) ~doc:"STRING NameString"
       and commitId =
         flag "commit-id" (optional string) ~doc:"STRING CommitIdString"
       and authStrategy =
         flag "auth-strategy" (optional json_arg)
           ~doc:"JSON SourceControlAuthStrategy"
       and authToken =
         flag "auth-token" (optional string) ~doc:"STRING AuthTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_source_control_from_job
           (Values.UpdateSourceControlFromJobRequest.make ?jobName
              ?provider:(Option.map ~f:Values.SourceControlProvider.of_json
                           provider) ?repositoryName ?repositoryOwner
              ?branchName ?folder ?commitId
              ?authStrategy:(Option.map
                               ~f:Values.SourceControlAuthStrategy.of_json
                               authStrategy) ?authToken ())
           (Some Values.UpdateSourceControlFromJobResponse.to_json)
           (Some Values.UpdateSourceControlFromJobResponse.error_to_json)])
let update_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and name = flag "name" (optional string) ~doc:"STRING NameString"
       and tableInput =
         flag "table-input" (optional json_arg) ~doc:"JSON TableInput"
       and skipArchive =
         flag "skip-archive" (optional bool) ~doc:"BOOL BooleanNullable"
       and transactionId =
         flag "transaction-id" (optional string)
           ~doc:"STRING TransactionIdString"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING VersionString"
       and viewUpdateAction =
         flag "view-update-action" (optional json_arg)
           ~doc:"JSON ViewUpdateAction"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and updateOpenTableFormatInput =
         flag "update-open-table-format-input" (optional json_arg)
           ~doc:"JSON UpdateOpenTableFormatInput"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_table
           (Values.UpdateTableRequest.make ?catalogId ?name
              ?tableInput:(Option.map ~f:Values.TableInput.of_json tableInput)
              ?skipArchive ?transactionId ?versionId
              ?viewUpdateAction:(Option.map
                                   ~f:Values.ViewUpdateAction.of_json
                                   viewUpdateAction) ?force
              ?updateOpenTableFormatInput:(Option.map
                                             ~f:Values.UpdateOpenTableFormatInput.of_json
                                             updateOpenTableFormatInput)
              ~databaseName ()) (Some Values.UpdateTableResponse.to_json)
           (Some Values.UpdateTableResponse.error_to_json)])
let update_table_optimizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (required string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON TableOptimizerType"
       and tableOptimizerConfiguration =
         flag "table-optimizer-configuration" (required json_arg)
           ~doc:"JSON TableOptimizerConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_table_optimizer
           (Values.UpdateTableOptimizerRequest.make ~catalogId ~databaseName
              ~tableName ~type_:(Values.TableOptimizerType.of_json type_)
              ~tableOptimizerConfiguration:(Values.TableOptimizerConfiguration.of_json
                                              tableOptimizerConfiguration) ())
           (Some Values.UpdateTableOptimizerResponse.to_json)
           (Some Values.UpdateTableOptimizerResponse.error_to_json)])
let update_trigger =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and triggerUpdate =
         flag "trigger-update" (required json_arg) ~doc:"JSON TriggerUpdate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_trigger
           (Values.UpdateTriggerRequest.make ~name
              ~triggerUpdate:(Values.TriggerUpdate.of_json triggerUpdate) ())
           (Some Values.UpdateTriggerResponse.to_json)
           (Some Values.UpdateTriggerResponse.error_to_json)])
let update_usage_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON ProfileConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_usage_profile
           (Values.UpdateUsageProfileRequest.make ?description ~name
              ~configuration:(Values.ProfileConfiguration.of_json
                                configuration) ())
           (Some Values.UpdateUsageProfileResponse.to_json)
           (Some Values.UpdateUsageProfileResponse.error_to_json)])
let update_user_defined_function =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogId =
         flag "catalog-id" (optional string) ~doc:"STRING CatalogIdString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING NameString"
       and functionInput =
         flag "function-input" (required json_arg)
           ~doc:"JSON UserDefinedFunctionInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_defined_function
           (Values.UpdateUserDefinedFunctionRequest.make ?catalogId
              ~databaseName ~functionName
              ~functionInput:(Values.UserDefinedFunctionInput.of_json
                                functionInput) ())
           (Some Values.UpdateUserDefinedFunctionResponse.to_json)
           (Some Values.UpdateUserDefinedFunctionResponse.error_to_json)])
let update_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING WorkflowDescriptionString"
       and defaultRunProperties =
         flag "default-run-properties" (optional json_arg)
           ~doc:"JSON WorkflowRunProperties"
       and maxConcurrentRuns =
         flag "max-concurrent-runs" (optional int) ~doc:"INT NullableInteger"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workflow
           (Values.UpdateWorkflowRequest.make ?description
              ?defaultRunProperties:(Option.map
                                       ~f:Values.WorkflowRunProperties.of_json
                                       defaultRunProperties)
              ?maxConcurrentRuns ~name ())
           (Some Values.UpdateWorkflowResponse.to_json)
           (Some Values.UpdateWorkflowResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-create-partition", batch_create_partition);
    ("batch-delete-connection", batch_delete_connection);
    ("batch-delete-partition", batch_delete_partition);
    ("batch-delete-table", batch_delete_table);
    ("batch-delete-table-version", batch_delete_table_version);
    ("batch-get-blueprints", batch_get_blueprints);
    ("batch-get-crawlers", batch_get_crawlers);
    ("batch-get-custom-entity-types", batch_get_custom_entity_types);
    ("batch-get-data-quality-result", batch_get_data_quality_result);
    ("batch-get-dev-endpoints", batch_get_dev_endpoints);
    ("batch-get-jobs", batch_get_jobs);
    ("batch-get-partition", batch_get_partition);
    ("batch-get-table-optimizer", batch_get_table_optimizer);
    ("batch-get-triggers", batch_get_triggers);
    ("batch-get-workflows", batch_get_workflows);
    ("batch-put-data-quality-statistic-annotation",
      batch_put_data_quality_statistic_annotation);
    ("batch-stop-job-run", batch_stop_job_run);
    ("batch-update-partition", batch_update_partition);
    ("cancel-data-quality-rule-recommendation-run",
      cancel_data_quality_rule_recommendation_run);
    ("cancel-data-quality-ruleset-evaluation-run",
      cancel_data_quality_ruleset_evaluation_run);
    ("cancel-m-l-task-run", cancel_m_l_task_run);
    ("cancel-statement", cancel_statement);
    ("check-schema-version-validity", check_schema_version_validity);
    ("create-blueprint", create_blueprint);
    ("create-catalog", create_catalog);
    ("create-classifier", create_classifier);
    ("create-column-statistics-task-settings",
      create_column_statistics_task_settings);
    ("create-connection", create_connection);
    ("create-crawler", create_crawler);
    ("create-custom-entity-type", create_custom_entity_type);
    ("create-data-quality-ruleset", create_data_quality_ruleset);
    ("create-database", create_database);
    ("create-dev-endpoint", create_dev_endpoint);
    ("create-glue-identity-center-configuration",
      create_glue_identity_center_configuration);
    ("create-integration", create_integration);
    ("create-integration-resource-property",
      create_integration_resource_property);
    ("create-integration-table-properties",
      create_integration_table_properties);
    ("create-job", create_job);
    ("create-m-l-transform", create_m_l_transform);
    ("create-partition", create_partition);
    ("create-partition-index", create_partition_index);
    ("create-registry", create_registry);
    ("create-schema", create_schema);
    ("create-script", create_script);
    ("create-security-configuration", create_security_configuration);
    ("create-session", create_session);
    ("create-table", create_table);
    ("create-table-optimizer", create_table_optimizer);
    ("create-trigger", create_trigger);
    ("create-usage-profile", create_usage_profile);
    ("create-user-defined-function", create_user_defined_function);
    ("create-workflow", create_workflow);
    ("delete-blueprint", delete_blueprint);
    ("delete-catalog", delete_catalog);
    ("delete-classifier", delete_classifier);
    ("delete-column-statistics-for-partition",
      delete_column_statistics_for_partition);
    ("delete-column-statistics-for-table",
      delete_column_statistics_for_table);
    ("delete-column-statistics-task-settings",
      delete_column_statistics_task_settings);
    ("delete-connection", delete_connection);
    ("delete-connection-type", delete_connection_type);
    ("delete-crawler", delete_crawler);
    ("delete-custom-entity-type", delete_custom_entity_type);
    ("delete-data-quality-ruleset", delete_data_quality_ruleset);
    ("delete-database", delete_database);
    ("delete-dev-endpoint", delete_dev_endpoint);
    ("delete-glue-identity-center-configuration",
      delete_glue_identity_center_configuration);
    ("delete-integration", delete_integration);
    ("delete-integration-resource-property",
      delete_integration_resource_property);
    ("delete-integration-table-properties",
      delete_integration_table_properties);
    ("delete-job", delete_job);
    ("delete-m-l-transform", delete_m_l_transform);
    ("delete-partition", delete_partition);
    ("delete-partition-index", delete_partition_index);
    ("delete-registry", delete_registry);
    ("delete-resource-policy", delete_resource_policy);
    ("delete-schema", delete_schema);
    ("delete-schema-versions", delete_schema_versions);
    ("delete-security-configuration", delete_security_configuration);
    ("delete-session", delete_session);
    ("delete-table", delete_table);
    ("delete-table-optimizer", delete_table_optimizer);
    ("delete-table-version", delete_table_version);
    ("delete-trigger", delete_trigger);
    ("delete-usage-profile", delete_usage_profile);
    ("delete-user-defined-function", delete_user_defined_function);
    ("delete-workflow", delete_workflow);
    ("describe-connection-type", describe_connection_type);
    ("describe-entity", describe_entity);
    ("describe-inbound-integrations", describe_inbound_integrations);
    ("describe-integrations", describe_integrations);
    ("get-blueprint", get_blueprint);
    ("get-blueprint-run", get_blueprint_run);
    ("get-blueprint-runs", get_blueprint_runs);
    ("get-catalog", get_catalog);
    ("get-catalog-import-status", get_catalog_import_status);
    ("get-catalogs", get_catalogs);
    ("get-classifier", get_classifier);
    ("get-classifiers", get_classifiers);
    ("get-column-statistics-for-partition",
      get_column_statistics_for_partition);
    ("get-column-statistics-for-table", get_column_statistics_for_table);
    ("get-column-statistics-task-run", get_column_statistics_task_run);
    ("get-column-statistics-task-runs", get_column_statistics_task_runs);
    ("get-column-statistics-task-settings",
      get_column_statistics_task_settings);
    ("get-connection", get_connection);
    ("get-connections", get_connections);
    ("get-crawler", get_crawler);
    ("get-crawler-metrics", get_crawler_metrics);
    ("get-crawlers", get_crawlers);
    ("get-custom-entity-type", get_custom_entity_type);
    ("get-data-catalog-encryption-settings",
      get_data_catalog_encryption_settings);
    ("get-data-quality-model", get_data_quality_model);
    ("get-data-quality-model-result", get_data_quality_model_result);
    ("get-data-quality-result", get_data_quality_result);
    ("get-data-quality-rule-recommendation-run",
      get_data_quality_rule_recommendation_run);
    ("get-data-quality-ruleset", get_data_quality_ruleset);
    ("get-data-quality-ruleset-evaluation-run",
      get_data_quality_ruleset_evaluation_run);
    ("get-database", get_database);
    ("get-databases", get_databases);
    ("get-dataflow-graph", get_dataflow_graph);
    ("get-dev-endpoint", get_dev_endpoint);
    ("get-dev-endpoints", get_dev_endpoints);
    ("get-entity-records", get_entity_records);
    ("get-glue-identity-center-configuration",
      get_glue_identity_center_configuration);
    ("get-integration-resource-property", get_integration_resource_property);
    ("get-integration-table-properties", get_integration_table_properties);
    ("get-job", get_job);
    ("get-job-bookmark", get_job_bookmark);
    ("get-job-run", get_job_run);
    ("get-job-runs", get_job_runs);
    ("get-jobs", get_jobs);
    ("get-m-l-task-run", get_m_l_task_run);
    ("get-m-l-task-runs", get_m_l_task_runs);
    ("get-m-l-transform", get_m_l_transform);
    ("get-m-l-transforms", get_m_l_transforms);
    ("get-mapping", get_mapping);
    ("get-materialized-view-refresh-task-run",
      get_materialized_view_refresh_task_run);
    ("get-partition", get_partition);
    ("get-partition-indexes", get_partition_indexes);
    ("get-partitions", get_partitions);
    ("get-plan", get_plan);
    ("get-registry", get_registry);
    ("get-resource-policies", get_resource_policies);
    ("get-resource-policy", get_resource_policy);
    ("get-schema", get_schema);
    ("get-schema-by-definition", get_schema_by_definition);
    ("get-schema-version", get_schema_version);
    ("get-schema-versions-diff", get_schema_versions_diff);
    ("get-security-configuration", get_security_configuration);
    ("get-security-configurations", get_security_configurations);
    ("get-session", get_session);
    ("get-statement", get_statement);
    ("get-table", get_table);
    ("get-table-optimizer", get_table_optimizer);
    ("get-table-version", get_table_version);
    ("get-table-versions", get_table_versions);
    ("get-tables", get_tables);
    ("get-tags", get_tags);
    ("get-trigger", get_trigger);
    ("get-triggers", get_triggers);
    ("get-unfiltered-partition-metadata", get_unfiltered_partition_metadata);
    ("get-unfiltered-partitions-metadata",
      get_unfiltered_partitions_metadata);
    ("get-unfiltered-table-metadata", get_unfiltered_table_metadata);
    ("get-usage-profile", get_usage_profile);
    ("get-user-defined-function", get_user_defined_function);
    ("get-user-defined-functions", get_user_defined_functions);
    ("get-workflow", get_workflow);
    ("get-workflow-run", get_workflow_run);
    ("get-workflow-run-properties", get_workflow_run_properties);
    ("get-workflow-runs", get_workflow_runs);
    ("import-catalog-to-glue", import_catalog_to_glue);
    ("list-blueprints", list_blueprints);
    ("list-column-statistics-task-runs", list_column_statistics_task_runs);
    ("list-connection-types", list_connection_types);
    ("list-crawlers", list_crawlers);
    ("list-crawls", list_crawls);
    ("list-custom-entity-types", list_custom_entity_types);
    ("list-data-quality-results", list_data_quality_results);
    ("list-data-quality-rule-recommendation-runs",
      list_data_quality_rule_recommendation_runs);
    ("list-data-quality-ruleset-evaluation-runs",
      list_data_quality_ruleset_evaluation_runs);
    ("list-data-quality-rulesets", list_data_quality_rulesets);
    ("list-data-quality-statistic-annotations",
      list_data_quality_statistic_annotations);
    ("list-data-quality-statistics", list_data_quality_statistics);
    ("list-dev-endpoints", list_dev_endpoints);
    ("list-entities", list_entities);
    ("list-integration-resource-properties",
      list_integration_resource_properties);
    ("list-jobs", list_jobs);
    ("list-m-l-transforms", list_m_l_transforms);
    ("list-materialized-view-refresh-task-runs",
      list_materialized_view_refresh_task_runs);
    ("list-registries", list_registries);
    ("list-schema-versions", list_schema_versions);
    ("list-schemas", list_schemas);
    ("list-sessions", list_sessions);
    ("list-statements", list_statements);
    ("list-table-optimizer-runs", list_table_optimizer_runs);
    ("list-triggers", list_triggers);
    ("list-usage-profiles", list_usage_profiles);
    ("list-workflows", list_workflows);
    ("modify-integration", modify_integration);
    ("put-data-catalog-encryption-settings",
      put_data_catalog_encryption_settings);
    ("put-data-quality-profile-annotation",
      put_data_quality_profile_annotation);
    ("put-resource-policy", put_resource_policy);
    ("put-schema-version-metadata", put_schema_version_metadata);
    ("put-workflow-run-properties", put_workflow_run_properties);
    ("query-schema-version-metadata", query_schema_version_metadata);
    ("register-connection-type", register_connection_type);
    ("register-schema-version", register_schema_version);
    ("remove-schema-version-metadata", remove_schema_version_metadata);
    ("reset-job-bookmark", reset_job_bookmark);
    ("resume-workflow-run", resume_workflow_run);
    ("run-statement", run_statement);
    ("search-tables", search_tables);
    ("start-blueprint-run", start_blueprint_run);
    ("start-column-statistics-task-run", start_column_statistics_task_run);
    ("start-column-statistics-task-run-schedule",
      start_column_statistics_task_run_schedule);
    ("start-crawler", start_crawler);
    ("start-crawler-schedule", start_crawler_schedule);
    ("start-data-quality-rule-recommendation-run",
      start_data_quality_rule_recommendation_run);
    ("start-data-quality-ruleset-evaluation-run",
      start_data_quality_ruleset_evaluation_run);
    ("start-export-labels-task-run", start_export_labels_task_run);
    ("start-import-labels-task-run", start_import_labels_task_run);
    ("start-job-run", start_job_run);
    ("start-m-l-evaluation-task-run", start_m_l_evaluation_task_run);
    ("start-m-l-labeling-set-generation-task-run",
      start_m_l_labeling_set_generation_task_run);
    ("start-materialized-view-refresh-task-run",
      start_materialized_view_refresh_task_run);
    ("start-trigger", start_trigger);
    ("start-workflow-run", start_workflow_run);
    ("stop-column-statistics-task-run", stop_column_statistics_task_run);
    ("stop-column-statistics-task-run-schedule",
      stop_column_statistics_task_run_schedule);
    ("stop-crawler", stop_crawler);
    ("stop-crawler-schedule", stop_crawler_schedule);
    ("stop-materialized-view-refresh-task-run",
      stop_materialized_view_refresh_task_run);
    ("stop-session", stop_session);
    ("stop-trigger", stop_trigger);
    ("stop-workflow-run", stop_workflow_run);
    ("tag-resource", tag_resource);
    ("test-connection", test_connection);
    ("untag-resource", untag_resource);
    ("update-blueprint", update_blueprint);
    ("update-catalog", update_catalog);
    ("update-classifier", update_classifier);
    ("update-column-statistics-for-partition",
      update_column_statistics_for_partition);
    ("update-column-statistics-for-table",
      update_column_statistics_for_table);
    ("update-column-statistics-task-settings",
      update_column_statistics_task_settings);
    ("update-connection", update_connection);
    ("update-crawler", update_crawler);
    ("update-crawler-schedule", update_crawler_schedule);
    ("update-data-quality-ruleset", update_data_quality_ruleset);
    ("update-database", update_database);
    ("update-dev-endpoint", update_dev_endpoint);
    ("update-glue-identity-center-configuration",
      update_glue_identity_center_configuration);
    ("update-integration-resource-property",
      update_integration_resource_property);
    ("update-integration-table-properties",
      update_integration_table_properties);
    ("update-job", update_job);
    ("update-job-from-source-control", update_job_from_source_control);
    ("update-m-l-transform", update_m_l_transform);
    ("update-partition", update_partition);
    ("update-registry", update_registry);
    ("update-schema", update_schema);
    ("update-source-control-from-job", update_source_control_from_job);
    ("update-table", update_table);
    ("update-table-optimizer", update_table_optimizer);
    ("update-trigger", update_trigger);
    ("update-usage-profile", update_usage_profile);
    ("update-user-defined-function", update_user_defined_function);
    ("update-workflow", update_workflow)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workspaceInstanceId =
         flag "workspace-instance-id" (required string)
           ~doc:"STRING WorkspaceInstanceId"
       and volumeId =
         flag "volume-id" (required string) ~doc:"STRING VolumeId"
       and device = flag "device" (required string) ~doc:"STRING DeviceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_volume
           (Values.AssociateVolumeRequest.make ~workspaceInstanceId ~volumeId
              ~device ()) (Some Values.AssociateVolumeResponse.to_json)
           (Some Values.AssociateVolumeResponse.error_to_json)])
let create_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and encrypted = flag "encrypted" (optional bool) ~doc:"BOOL Boolean"
       and iops = flag "iops" (optional int) ~doc:"INT NonNegativeInteger"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and sizeInGB =
         flag "size-in-g-b" (optional int) ~doc:"INT NonNegativeInteger"
       and snapshotId =
         flag "snapshot-id" (optional string) ~doc:"STRING SnapshotId"
       and tagSpecifications =
         flag "tag-specifications" (optional json_arg)
           ~doc:"JSON TagSpecifications"
       and throughput =
         flag "throughput" (optional int) ~doc:"INT NonNegativeInteger"
       and volumeType =
         flag "volume-type" (optional json_arg) ~doc:"JSON VolumeTypeEnum"
       and availabilityZone =
         flag "availability-zone" (required string) ~doc:"STRING String64" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_volume
           (Values.CreateVolumeRequest.make ?clientToken ?encrypted ?iops
              ?kmsKeyId ?sizeInGB ?snapshotId
              ?tagSpecifications:(Option.map
                                    ~f:Values.TagSpecifications.of_json
                                    tagSpecifications) ?throughput
              ?volumeType:(Option.map ~f:Values.VolumeTypeEnum.of_json
                             volumeType) ~availabilityZone ())
           (Some Values.CreateVolumeResponse.to_json)
           (Some Values.CreateVolumeResponse.error_to_json)])
let create_workspace_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and billingConfiguration =
         flag "billing-configuration" (optional json_arg)
           ~doc:"JSON BillingConfiguration"
       and managedInstance =
         flag "managed-instance" (required json_arg)
           ~doc:"JSON ManagedInstanceRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workspace_instance
           (Values.CreateWorkspaceInstanceRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?billingConfiguration:(Option.map
                                       ~f:Values.BillingConfiguration.of_json
                                       billingConfiguration)
              ~managedInstance:(Values.ManagedInstanceRequest.of_json
                                  managedInstance) ())
           (Some Values.CreateWorkspaceInstanceResponse.to_json)
           (Some Values.CreateWorkspaceInstanceResponse.error_to_json)])
let delete_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and volumeId =
         flag "volume-id" (required string) ~doc:"STRING VolumeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_volume (Values.DeleteVolumeRequest.make ~volumeId ())
           (Some Values.DeleteVolumeResponse.to_json)
           (Some Values.DeleteVolumeResponse.error_to_json)])
let delete_workspace_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workspaceInstanceId =
         flag "workspace-instance-id" (required string)
           ~doc:"STRING WorkspaceInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workspace_instance
           (Values.DeleteWorkspaceInstanceRequest.make ~workspaceInstanceId
              ()) (Some Values.DeleteWorkspaceInstanceResponse.to_json)
           (Some Values.DeleteWorkspaceInstanceResponse.error_to_json)])
let disassociate_volume =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and device = flag "device" (optional string) ~doc:"STRING DeviceName"
       and disassociateMode =
         flag "disassociate-mode" (optional json_arg)
           ~doc:"JSON DisassociateModeEnum"
       and workspaceInstanceId =
         flag "workspace-instance-id" (required string)
           ~doc:"STRING WorkspaceInstanceId"
       and volumeId =
         flag "volume-id" (required string) ~doc:"STRING VolumeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_volume
           (Values.DisassociateVolumeRequest.make ?device
              ?disassociateMode:(Option.map
                                   ~f:Values.DisassociateModeEnum.of_json
                                   disassociateMode) ~workspaceInstanceId
              ~volumeId ()) (Some Values.DisassociateVolumeResponse.to_json)
           (Some Values.DisassociateVolumeResponse.error_to_json)])
let get_workspace_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workspaceInstanceId =
         flag "workspace-instance-id" (required string)
           ~doc:"STRING WorkspaceInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workspace_instance
           (Values.GetWorkspaceInstanceRequest.make ~workspaceInstanceId ())
           (Some Values.GetWorkspaceInstanceResponse.to_json)
           (Some Values.GetWorkspaceInstanceResponse.error_to_json)])
let list_instance_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListInstanceTypesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and instanceConfigurationFilter =
         flag "instance-configuration-filter" (optional json_arg)
           ~doc:"JSON InstanceConfigurationFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_instance_types
           (Values.ListInstanceTypesRequest.make ?maxResults ?nextToken
              ?instanceConfigurationFilter:(Option.map
                                              ~f:Values.InstanceConfigurationFilter.of_json
                                              instanceConfigurationFilter) ())
           (Some Values.ListInstanceTypesResponse.to_json)
           (Some Values.ListInstanceTypesResponse.error_to_json)])
let list_regions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_regions
           (Values.ListRegionsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListRegionsResponse.to_json)
           (Some Values.ListRegionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workspaceInstanceId =
         flag "workspace-instance-id" (required string)
           ~doc:"STRING WorkspaceInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~workspaceInstanceId ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_workspace_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and provisionStates =
         flag "provision-states" (optional json_arg)
           ~doc:"JSON ProvisionStates"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workspace_instances
           (Values.ListWorkspaceInstancesRequest.make
              ?provisionStates:(Option.map ~f:Values.ProvisionStates.of_json
                                  provisionStates) ?maxResults ?nextToken ())
           (Some Values.ListWorkspaceInstancesResponse.to_json)
           (Some Values.ListWorkspaceInstancesResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workspaceInstanceId =
         flag "workspace-instance-id" (required string)
           ~doc:"STRING WorkspaceInstanceId"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~workspaceInstanceId
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workspaceInstanceId =
         flag "workspace-instance-id" (required string)
           ~doc:"STRING WorkspaceInstanceId"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~workspaceInstanceId
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-volume", associate_volume);
    ("create-volume", create_volume);
    ("create-workspace-instance", create_workspace_instance);
    ("delete-volume", delete_volume);
    ("delete-workspace-instance", delete_workspace_instance);
    ("disassociate-volume", disassociate_volume);
    ("get-workspace-instance", get_workspace_instance);
    ("list-instance-types", list_instance_types);
    ("list-regions", list_regions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-workspace-instances", list_workspace_instances);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
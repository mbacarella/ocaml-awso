(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | GetGlyphs: (GetGlyphsRequest.t, GetGlyphsResponse.t,
  GetGlyphsResponse.error) t 
  | GetSprites: (GetSpritesRequest.t, GetSpritesResponse.t,
  GetSpritesResponse.error) t 
  | GetStaticMap: (GetStaticMapRequest.t, GetStaticMapResponse.t,
  GetStaticMapResponse.error) t 
  | GetStyleDescriptor: (GetStyleDescriptorRequest.t,
  GetStyleDescriptorResponse.t, GetStyleDescriptorResponse.error) t 
  | GetTile: (GetTileRequest.t, GetTileResponse.t, GetTileResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | GetGlyphs -> `GET
  | GetSprites -> `GET
  | GetStaticMap -> `GET
  | GetStyleDescriptor -> `GET
  | GetTile -> `GET
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | GetGlyphs ->
          (Format.kasprintf Uri.of_string) "/glyphs/%s/%s"
            (GetGlyphsRequestFontStackString.to_header
               x.GetGlyphsRequest.fontStack)
            (GetGlyphsRequestFontUnicodeRangeString.to_header
               x.GetGlyphsRequest.fontUnicodeRange)
      | GetSprites ->
          (Format.kasprintf Uri.of_string) "/styles/%s/%s/%s/sprites/%s"
            (MapStyle.to_header x.GetSpritesRequest.style)
            (ColorScheme.to_header x.GetSpritesRequest.colorScheme)
            (Variant.to_header x.GetSpritesRequest.variant)
            (GetSpritesRequestFileNameString.to_header
               x.GetSpritesRequest.fileName)
      | GetStaticMap ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/static/%s"
               (GetStaticMapRequestFileNameString.to_header
                  x.GetStaticMapRequest.fileName))
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("bounding-box",
                          (GetStaticMapRequestBoundingBoxString.to_header v)))
                  x.boundingBox;
               Option.map
                 ~f:(fun v ->
                       ("bounded-positions",
                         (GetStaticMapRequestBoundedPositionsString.to_header
                            v))) x.boundedPositions;
               Option.map
                 ~f:(fun v -> ("center", (PositionString.to_header v)))
                 x.center;
               Option.map
                 ~f:(fun v -> ("color-scheme", (ColorScheme.to_header v)))
                 x.colorScheme;
               Option.map
                 ~f:(fun v ->
                       ("compact-overlay", (CompactOverlay.to_header v)))
                 x.compactOverlay;
               Option.map
                 ~f:(fun v -> ("crop-labels", (Boolean.to_header v)))
                 x.cropLabels;
               Option.map
                 ~f:(fun v ->
                       ("geojson-overlay", (GeoJsonOverlay.to_header v)))
                 x.geoJsonOverlay;
               Some
                 ("height",
                   (GetStaticMapRequestHeightInteger.to_header x.height));
               Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key;
               Option.map
                 ~f:(fun v -> ("label-size", (LabelSize.to_header v)))
                 x.labelSize;
               Option.map ~f:(fun v -> ("lang", (LanguageTag.to_header v)))
                 x.language;
               Option.map
                 ~f:(fun v ->
                       ("padding",
                         (GetStaticMapRequestPaddingInteger.to_header v)))
                 x.padding;
               Option.map
                 ~f:(fun v -> ("political-view", (CountryCode.to_header v)))
                 x.politicalView;
               Option.map
                 ~f:(fun v -> ("pois", (MapFeatureMode.to_header v)))
                 x.pointsOfInterests;
               Option.map
                 ~f:(fun v -> ("radius", (DistanceMeters.to_header v)))
                 x.radius;
               Option.map
                 ~f:(fun v -> ("scale-unit", (ScaleBarUnit.to_header v)))
                 x.scaleBarUnit;
               Option.map
                 ~f:(fun v -> ("style", (StaticMapStyle.to_header v)))
                 x.style;
               Some
                 ("width",
                   (GetStaticMapRequestWidthInteger.to_header x.width));
               Option.map
                 ~f:(fun v ->
                       ("zoom", (GetStaticMapRequestZoomFloat.to_header v)))
                 x.zoom])
      | GetStyleDescriptor ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/styles/%s/descriptor"
               (MapStyle.to_header x.GetStyleDescriptorRequest.style))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("color-scheme", (ColorScheme.to_header v)))
                  x.colorScheme;
               Option.map
                 ~f:(fun v -> ("political-view", (CountryCode.to_header v)))
                 x.politicalView;
               Option.map ~f:(fun v -> ("terrain", (Terrain.to_header v)))
                 x.terrain;
               Option.map
                 ~f:(fun v ->
                       ("contour-density", (ContourDensity.to_header v)))
                 x.contourDensity;
               Option.map ~f:(fun v -> ("traffic", (Traffic.to_header v)))
                 x.traffic;
               Option.map
                 ~f:(fun v -> ("travel-modes", (TravelModeList.to_header v)))
                 x.travelModes;
               Option.map
                 ~f:(fun v -> ("buildings", (Buildings.to_header v)))
                 x.buildings;
               Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key])
      | GetTile ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tiles/%s/%s/%s/%s"
               (Tileset.to_header x.GetTileRequest.tileset)
               (GetTileRequestZString.to_header x.GetTileRequest.z)
               (GetTileRequestXString.to_header x.GetTileRequest.x)
               (GetTileRequestYString.to_header x.GetTileRequest.y))
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("additional-features",
                          (TileAdditionalFeatureList.to_header v)))
                  x.additionalFeatures;
               Option.map ~f:(fun v -> ("key", (ApiKey.to_header v))) x.key]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | GetGlyphs ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetSprites ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetStaticMap ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetStyleDescriptor ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetTile ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | GetGlyphs ->
      if is_success
      then
        let body = Blob.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (GetGlyphsResponse.of_header_and_body (headers, body))
      else Error (parse_aws_error None)
  | GetSprites ->
      if is_success
      then
        let body = Blob.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (GetSpritesResponse.of_header_and_body (headers, body))
      else Error (parse_aws_error None)
  | GetStaticMap ->
      if is_success
      then
        let body = Blob.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (GetStaticMapResponse.of_header_and_body (headers, body))
      else Error (parse_aws_error (Some GetStaticMapResponse.error_of_json))
  | GetStyleDescriptor ->
      if is_success
      then
        let body = Blob.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (GetStyleDescriptorResponse.of_header_and_body (headers, body))
      else Error (parse_aws_error None)
  | GetTile ->
      if is_success
      then
        let body = Blob.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (GetTileResponse.of_header_and_body (headers, body))
      else Error (parse_aws_error (Some GetTileResponse.error_of_json))
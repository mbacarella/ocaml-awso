(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let get_glyphs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fontStack =
         flag "font-stack" (required string)
           ~doc:"STRING GetGlyphsRequestFontStackString"
       and fontUnicodeRange =
         flag "font-unicode-range" (required string)
           ~doc:"STRING GetGlyphsRequestFontUnicodeRangeString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_glyphs
           (Values.GetGlyphsRequest.make ~fontStack ~fontUnicodeRange ())
           (Some Values.GetGlyphsResponse.to_json)
           (Some Values.GetGlyphsResponse.error_to_json)])
let get_sprites =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileName =
         flag "file-name" (required string)
           ~doc:"STRING GetSpritesRequestFileNameString"
       and style = flag "style" (required json_arg) ~doc:"JSON MapStyle"
       and colorScheme =
         flag "color-scheme" (required json_arg) ~doc:"JSON ColorScheme"
       and variant = flag "variant" (required json_arg) ~doc:"JSON Variant" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sprites
           (Values.GetSpritesRequest.make ~fileName
              ~style:(Values.MapStyle.of_json style)
              ~colorScheme:(Values.ColorScheme.of_json colorScheme)
              ~variant:(Values.Variant.of_json variant) ())
           (Some Values.GetSpritesResponse.to_json)
           (Some Values.GetSpritesResponse.error_to_json)])
let get_static_map =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and boundingBox =
         flag "bounding-box" (optional string)
           ~doc:"STRING GetStaticMapRequestBoundingBoxString"
       and boundedPositions =
         flag "bounded-positions" (optional string)
           ~doc:"STRING GetStaticMapRequestBoundedPositionsString"
       and center =
         flag "center" (optional string) ~doc:"STRING PositionString"
       and colorScheme =
         flag "color-scheme" (optional json_arg) ~doc:"JSON ColorScheme"
       and compactOverlay =
         flag "compact-overlay" (optional string)
           ~doc:"STRING CompactOverlay"
       and cropLabels =
         flag "crop-labels" (optional bool) ~doc:"BOOL Boolean"
       and geoJsonOverlay =
         flag "geo-json-overlay" (optional string)
           ~doc:"STRING GeoJsonOverlay"
       and key = flag "key" (optional string) ~doc:"STRING ApiKey"
       and labelSize =
         flag "label-size" (optional json_arg) ~doc:"JSON LabelSize"
       and language =
         flag "language" (optional string) ~doc:"STRING LanguageTag"
       and padding =
         flag "padding" (optional int)
           ~doc:"INT GetStaticMapRequestPaddingInteger"
       and politicalView =
         flag "political-view" (optional string) ~doc:"STRING CountryCode"
       and pointsOfInterests =
         flag "points-of-interests" (optional json_arg)
           ~doc:"JSON MapFeatureMode"
       and radius =
         flag "radius" (optional json_arg) ~doc:"JSON DistanceMeters"
       and scaleBarUnit =
         flag "scale-bar-unit" (optional json_arg) ~doc:"JSON ScaleBarUnit"
       and style =
         flag "style" (optional json_arg) ~doc:"JSON StaticMapStyle"
       and zoom =
         flag "zoom" (optional float)
           ~doc:"FLOAT GetStaticMapRequestZoomFloat"
       and height =
         flag "height" (required int)
           ~doc:"INT GetStaticMapRequestHeightInteger"
       and fileName =
         flag "file-name" (required string)
           ~doc:"STRING GetStaticMapRequestFileNameString"
       and width =
         flag "width" (required int)
           ~doc:"INT GetStaticMapRequestWidthInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_static_map
           (Values.GetStaticMapRequest.make ?boundingBox ?boundedPositions
              ?center
              ?colorScheme:(Option.map ~f:Values.ColorScheme.of_json
                              colorScheme) ?compactOverlay ?cropLabels
              ?geoJsonOverlay ?key
              ?labelSize:(Option.map ~f:Values.LabelSize.of_json labelSize)
              ?language ?padding ?politicalView
              ?pointsOfInterests:(Option.map ~f:Values.MapFeatureMode.of_json
                                    pointsOfInterests)
              ?radius:(Option.map ~f:Values.DistanceMeters.of_json radius)
              ?scaleBarUnit:(Option.map ~f:Values.ScaleBarUnit.of_json
                               scaleBarUnit)
              ?style:(Option.map ~f:Values.StaticMapStyle.of_json style)
              ?zoom ~height ~fileName ~width ())
           (Some Values.GetStaticMapResponse.to_json)
           (Some Values.GetStaticMapResponse.error_to_json)])
let get_style_descriptor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and colorScheme =
         flag "color-scheme" (optional json_arg) ~doc:"JSON ColorScheme"
       and politicalView =
         flag "political-view" (optional string) ~doc:"STRING CountryCode"
       and terrain = flag "terrain" (optional json_arg) ~doc:"JSON Terrain"
       and contourDensity =
         flag "contour-density" (optional json_arg)
           ~doc:"JSON ContourDensity"
       and traffic = flag "traffic" (optional json_arg) ~doc:"JSON Traffic"
       and travelModes =
         flag "travel-modes" (optional json_arg) ~doc:"JSON TravelModeList"
       and buildings =
         flag "buildings" (optional json_arg) ~doc:"JSON Buildings"
       and key = flag "key" (optional string) ~doc:"STRING ApiKey"
       and style = flag "style" (required json_arg) ~doc:"JSON MapStyle" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_style_descriptor
           (Values.GetStyleDescriptorRequest.make
              ?colorScheme:(Option.map ~f:Values.ColorScheme.of_json
                              colorScheme) ?politicalView
              ?terrain:(Option.map ~f:Values.Terrain.of_json terrain)
              ?contourDensity:(Option.map ~f:Values.ContourDensity.of_json
                                 contourDensity)
              ?traffic:(Option.map ~f:Values.Traffic.of_json traffic)
              ?travelModes:(Option.map ~f:Values.TravelModeList.of_json
                              travelModes)
              ?buildings:(Option.map ~f:Values.Buildings.of_json buildings)
              ?key ~style:(Values.MapStyle.of_json style) ())
           (Some Values.GetStyleDescriptorResponse.to_json)
           (Some Values.GetStyleDescriptorResponse.error_to_json)])
let get_tile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and additionalFeatures =
         flag "additional-features" (optional json_arg)
           ~doc:"JSON TileAdditionalFeatureList"
       and key = flag "key" (optional string) ~doc:"STRING ApiKey"
       and tileset = flag "tileset" (required string) ~doc:"STRING Tileset"
       and z = flag "z" (required string) ~doc:"STRING GetTileRequestZString"
       and x = flag "x" (required string) ~doc:"STRING GetTileRequestXString"
       and y = flag "y" (required string) ~doc:"STRING GetTileRequestYString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tile
           (Values.GetTileRequest.make
              ?additionalFeatures:(Option.map
                                     ~f:Values.TileAdditionalFeatureList.of_json
                                     additionalFeatures) ?key ~tileset ~z ~x
              ~y ()) (Some Values.GetTileResponse.to_json)
           (Some Values.GetTileResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("get-glyphs", get_glyphs);
    ("get-sprites", get_sprites);
    ("get-static-map", get_static_map);
    ("get-style-descriptor", get_style_descriptor);
    ("get-tile", get_tile)]
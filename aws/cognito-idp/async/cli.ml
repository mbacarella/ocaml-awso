(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_custom_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and customAttributes =
         flag "custom-attributes" (required json_arg)
           ~doc:"JSON CustomAttributesListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_custom_attributes
           (Values.AddCustomAttributesRequest.make ~userPoolId
              ~customAttributes:(Values.CustomAttributesListType.of_json
                                   customAttributes) ())
           (Some Values.AddCustomAttributesResponse.to_json)
           (Some Values.AddCustomAttributesResponse.error_to_json)])
let admin_add_user_to_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_add_user_to_group
           (Values.AdminAddUserToGroupRequest.make ~userPoolId ~username
              ~groupName ()) None None])
let admin_confirm_sign_up =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_confirm_sign_up
           (Values.AdminConfirmSignUpRequest.make
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata) ~userPoolId ~username ())
           (Some Values.AdminConfirmSignUpResponse.to_json)
           (Some Values.AdminConfirmSignUpResponse.error_to_json)])
let admin_create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userAttributes =
         flag "user-attributes" (optional json_arg)
           ~doc:"JSON AttributeListType"
       and validationData =
         flag "validation-data" (optional json_arg)
           ~doc:"JSON AttributeListType"
       and temporaryPassword =
         flag "temporary-password" (optional string)
           ~doc:"STRING PasswordType"
       and forceAliasCreation =
         flag "force-alias-creation" (optional bool)
           ~doc:"BOOL ForceAliasCreation"
       and messageAction =
         flag "message-action" (optional json_arg)
           ~doc:"JSON MessageActionType"
       and desiredDeliveryMediums =
         flag "desired-delivery-mediums" (optional json_arg)
           ~doc:"JSON DeliveryMediumListType"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_create_user
           (Values.AdminCreateUserRequest.make
              ?userAttributes:(Option.map ~f:Values.AttributeListType.of_json
                                 userAttributes)
              ?validationData:(Option.map ~f:Values.AttributeListType.of_json
                                 validationData) ?temporaryPassword
              ?forceAliasCreation
              ?messageAction:(Option.map ~f:Values.MessageActionType.of_json
                                messageAction)
              ?desiredDeliveryMediums:(Option.map
                                         ~f:Values.DeliveryMediumListType.of_json
                                         desiredDeliveryMediums)
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata) ~userPoolId ~username ())
           (Some Values.AdminCreateUserResponse.to_json)
           (Some Values.AdminCreateUserResponse.error_to_json)])
let admin_delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_delete_user
           (Values.AdminDeleteUserRequest.make ~userPoolId ~username ()) None
           None])
let admin_delete_user_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and userAttributeNames =
         flag "user-attribute-names" (required json_arg)
           ~doc:"JSON AttributeNameListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_delete_user_attributes
           (Values.AdminDeleteUserAttributesRequest.make ~userPoolId
              ~username
              ~userAttributeNames:(Values.AttributeNameListType.of_json
                                     userAttributeNames) ())
           (Some Values.AdminDeleteUserAttributesResponse.to_json)
           (Some Values.AdminDeleteUserAttributesResponse.error_to_json)])
let admin_disable_provider_for_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING StringType"
       and user =
         flag "user" (required json_arg)
           ~doc:"JSON ProviderUserIdentifierType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_disable_provider_for_user
           (Values.AdminDisableProviderForUserRequest.make ~userPoolId
              ~user:(Values.ProviderUserIdentifierType.of_json user) ())
           (Some Values.AdminDisableProviderForUserResponse.to_json)
           (Some Values.AdminDisableProviderForUserResponse.error_to_json)])
let admin_disable_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_disable_user
           (Values.AdminDisableUserRequest.make ~userPoolId ~username ())
           (Some Values.AdminDisableUserResponse.to_json)
           (Some Values.AdminDisableUserResponse.error_to_json)])
let admin_enable_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_enable_user
           (Values.AdminEnableUserRequest.make ~userPoolId ~username ())
           (Some Values.AdminEnableUserResponse.to_json)
           (Some Values.AdminEnableUserResponse.error_to_json)])
let admin_forget_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and deviceKey =
         flag "device-key" (required string) ~doc:"STRING DeviceKeyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_forget_device
           (Values.AdminForgetDeviceRequest.make ~userPoolId ~username
              ~deviceKey ()) None None])
let admin_get_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceKey =
         flag "device-key" (required string) ~doc:"STRING DeviceKeyType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_get_device
           (Values.AdminGetDeviceRequest.make ~deviceKey ~userPoolId
              ~username ()) (Some Values.AdminGetDeviceResponse.to_json)
           (Some Values.AdminGetDeviceResponse.error_to_json)])
let admin_get_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_get_user
           (Values.AdminGetUserRequest.make ~userPoolId ~username ())
           (Some Values.AdminGetUserResponse.to_json)
           (Some Values.AdminGetUserResponse.error_to_json)])
let admin_initiate_auth =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authParameters =
         flag "auth-parameters" (optional json_arg)
           ~doc:"JSON AuthParametersType"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and analyticsMetadata =
         flag "analytics-metadata" (optional json_arg)
           ~doc:"JSON AnalyticsMetadataType"
       and contextData =
         flag "context-data" (optional json_arg) ~doc:"JSON ContextDataType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientIdType"
       and authFlow =
         flag "auth-flow" (required json_arg) ~doc:"JSON AuthFlowType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_initiate_auth
           (Values.AdminInitiateAuthRequest.make
              ?authParameters:(Option.map
                                 ~f:Values.AuthParametersType.of_json
                                 authParameters)
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata)
              ?analyticsMetadata:(Option.map
                                    ~f:Values.AnalyticsMetadataType.of_json
                                    analyticsMetadata)
              ?contextData:(Option.map ~f:Values.ContextDataType.of_json
                              contextData) ~userPoolId ~clientId
              ~authFlow:(Values.AuthFlowType.of_json authFlow) ())
           (Some Values.AdminInitiateAuthResponse.to_json)
           (Some Values.AdminInitiateAuthResponse.error_to_json)])
let admin_link_provider_for_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING StringType"
       and destinationUser =
         flag "destination-user" (required json_arg)
           ~doc:"JSON ProviderUserIdentifierType"
       and sourceUser =
         flag "source-user" (required json_arg)
           ~doc:"JSON ProviderUserIdentifierType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_link_provider_for_user
           (Values.AdminLinkProviderForUserRequest.make ~userPoolId
              ~destinationUser:(Values.ProviderUserIdentifierType.of_json
                                  destinationUser)
              ~sourceUser:(Values.ProviderUserIdentifierType.of_json
                             sourceUser) ())
           (Some Values.AdminLinkProviderForUserResponse.to_json)
           (Some Values.AdminLinkProviderForUserResponse.error_to_json)])
let admin_list_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT QueryLimitType"
       and paginationToken =
         flag "pagination-token" (optional string)
           ~doc:"STRING SearchPaginationTokenType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_list_devices
           (Values.AdminListDevicesRequest.make ?limit ?paginationToken
              ~userPoolId ~username ())
           (Some Values.AdminListDevicesResponse.to_json)
           (Some Values.AdminListDevicesResponse.error_to_json)])
let admin_list_groups_for_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT QueryLimitType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationKey"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_list_groups_for_user
           (Values.AdminListGroupsForUserRequest.make ?limit ?nextToken
              ~username ~userPoolId ())
           (Some Values.AdminListGroupsForUserResponse.to_json)
           (Some Values.AdminListGroupsForUserResponse.error_to_json)])
let admin_list_user_auth_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT QueryLimitType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationKey"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_list_user_auth_events
           (Values.AdminListUserAuthEventsRequest.make ?maxResults ?nextToken
              ~userPoolId ~username ())
           (Some Values.AdminListUserAuthEventsResponse.to_json)
           (Some Values.AdminListUserAuthEventsResponse.error_to_json)])
let admin_remove_user_from_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_remove_user_from_group
           (Values.AdminRemoveUserFromGroupRequest.make ~userPoolId ~username
              ~groupName ()) None None])
let admin_reset_user_password =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_reset_user_password
           (Values.AdminResetUserPasswordRequest.make
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata) ~userPoolId ~username ())
           (Some Values.AdminResetUserPasswordResponse.to_json)
           (Some Values.AdminResetUserPasswordResponse.error_to_json)])
let admin_respond_to_auth_challenge =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and challengeResponses =
         flag "challenge-responses" (optional json_arg)
           ~doc:"JSON ChallengeResponsesType"
       and session =
         flag "session" (optional string) ~doc:"STRING SessionType"
       and analyticsMetadata =
         flag "analytics-metadata" (optional json_arg)
           ~doc:"JSON AnalyticsMetadataType"
       and contextData =
         flag "context-data" (optional json_arg) ~doc:"JSON ContextDataType"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientIdType"
       and challengeName =
         flag "challenge-name" (required json_arg)
           ~doc:"JSON ChallengeNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_respond_to_auth_challenge
           (Values.AdminRespondToAuthChallengeRequest.make
              ?challengeResponses:(Option.map
                                     ~f:Values.ChallengeResponsesType.of_json
                                     challengeResponses) ?session
              ?analyticsMetadata:(Option.map
                                    ~f:Values.AnalyticsMetadataType.of_json
                                    analyticsMetadata)
              ?contextData:(Option.map ~f:Values.ContextDataType.of_json
                              contextData)
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata) ~userPoolId ~clientId
              ~challengeName:(Values.ChallengeNameType.of_json challengeName)
              ()) (Some Values.AdminRespondToAuthChallengeResponse.to_json)
           (Some Values.AdminRespondToAuthChallengeResponse.error_to_json)])
let admin_set_user_m_f_a_preference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sMSMfaSettings =
         flag "s-m-s-mfa-settings" (optional json_arg)
           ~doc:"JSON SMSMfaSettingsType"
       and softwareTokenMfaSettings =
         flag "software-token-mfa-settings" (optional json_arg)
           ~doc:"JSON SoftwareTokenMfaSettingsType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_set_user_m_f_a_preference
           (Values.AdminSetUserMFAPreferenceRequest.make
              ?sMSMfaSettings:(Option.map
                                 ~f:Values.SMSMfaSettingsType.of_json
                                 sMSMfaSettings)
              ?softwareTokenMfaSettings:(Option.map
                                           ~f:Values.SoftwareTokenMfaSettingsType.of_json
                                           softwareTokenMfaSettings)
              ~username ~userPoolId ())
           (Some Values.AdminSetUserMFAPreferenceResponse.to_json)
           (Some Values.AdminSetUserMFAPreferenceResponse.error_to_json)])
let admin_set_user_password =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and permanent =
         flag "permanent" (optional bool) ~doc:"BOOL BooleanType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and password =
         flag "password" (required string) ~doc:"STRING PasswordType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_set_user_password
           (Values.AdminSetUserPasswordRequest.make ?permanent ~userPoolId
              ~username ~password ())
           (Some Values.AdminSetUserPasswordResponse.to_json)
           (Some Values.AdminSetUserPasswordResponse.error_to_json)])
let admin_set_user_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and mFAOptions =
         flag "m-f-a-options" (required json_arg)
           ~doc:"JSON MFAOptionListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_set_user_settings
           (Values.AdminSetUserSettingsRequest.make ~userPoolId ~username
              ~mFAOptions:(Values.MFAOptionListType.of_json mFAOptions) ())
           (Some Values.AdminSetUserSettingsResponse.to_json)
           (Some Values.AdminSetUserSettingsResponse.error_to_json)])
let admin_update_auth_event_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and eventId =
         flag "event-id" (required string) ~doc:"STRING EventIdType"
       and feedbackValue =
         flag "feedback-value" (required json_arg)
           ~doc:"JSON FeedbackValueType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_update_auth_event_feedback
           (Values.AdminUpdateAuthEventFeedbackRequest.make ~userPoolId
              ~username ~eventId
              ~feedbackValue:(Values.FeedbackValueType.of_json feedbackValue)
              ()) (Some Values.AdminUpdateAuthEventFeedbackResponse.to_json)
           (Some Values.AdminUpdateAuthEventFeedbackResponse.error_to_json)])
let admin_update_device_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceRememberedStatus =
         flag "device-remembered-status" (optional json_arg)
           ~doc:"JSON DeviceRememberedStatusType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and deviceKey =
         flag "device-key" (required string) ~doc:"STRING DeviceKeyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_update_device_status
           (Values.AdminUpdateDeviceStatusRequest.make
              ?deviceRememberedStatus:(Option.map
                                         ~f:Values.DeviceRememberedStatusType.of_json
                                         deviceRememberedStatus) ~userPoolId
              ~username ~deviceKey ())
           (Some Values.AdminUpdateDeviceStatusResponse.to_json)
           (Some Values.AdminUpdateDeviceStatusResponse.error_to_json)])
let admin_update_user_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and userAttributes =
         flag "user-attributes" (required json_arg)
           ~doc:"JSON AttributeListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_update_user_attributes
           (Values.AdminUpdateUserAttributesRequest.make
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata) ~userPoolId ~username
              ~userAttributes:(Values.AttributeListType.of_json
                                 userAttributes) ())
           (Some Values.AdminUpdateUserAttributesResponse.to_json)
           (Some Values.AdminUpdateUserAttributesResponse.error_to_json)])
let admin_user_global_sign_out =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.admin_user_global_sign_out
           (Values.AdminUserGlobalSignOutRequest.make ~userPoolId ~username
              ()) (Some Values.AdminUserGlobalSignOutResponse.to_json)
           (Some Values.AdminUserGlobalSignOutResponse.error_to_json)])
let associate_software_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessToken =
         flag "access-token" (optional string) ~doc:"STRING TokenModelType"
       and session =
         flag "session" (optional string) ~doc:"STRING SessionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_software_token
           (Values.AssociateSoftwareTokenRequest.make ?accessToken ?session
              ()) (Some Values.AssociateSoftwareTokenResponse.to_json)
           (Some Values.AssociateSoftwareTokenResponse.error_to_json)])
let change_password =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and previousPassword =
         flag "previous-password" (required string)
           ~doc:"STRING PasswordType"
       and proposedPassword =
         flag "proposed-password" (required string)
           ~doc:"STRING PasswordType"
       and accessToken =
         flag "access-token" (required string) ~doc:"STRING TokenModelType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.change_password
           (Values.ChangePasswordRequest.make ~previousPassword
              ~proposedPassword ~accessToken ())
           (Some Values.ChangePasswordResponse.to_json)
           (Some Values.ChangePasswordResponse.error_to_json)])
let confirm_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceSecretVerifierConfig =
         flag "device-secret-verifier-config" (optional json_arg)
           ~doc:"JSON DeviceSecretVerifierConfigType"
       and deviceName =
         flag "device-name" (optional string) ~doc:"STRING DeviceNameType"
       and accessToken =
         flag "access-token" (required string) ~doc:"STRING TokenModelType"
       and deviceKey =
         flag "device-key" (required string) ~doc:"STRING DeviceKeyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.confirm_device
           (Values.ConfirmDeviceRequest.make
              ?deviceSecretVerifierConfig:(Option.map
                                             ~f:Values.DeviceSecretVerifierConfigType.of_json
                                             deviceSecretVerifierConfig)
              ?deviceName ~accessToken ~deviceKey ())
           (Some Values.ConfirmDeviceResponse.to_json)
           (Some Values.ConfirmDeviceResponse.error_to_json)])
let confirm_forgot_password =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and secretHash =
         flag "secret-hash" (optional string) ~doc:"STRING SecretHashType"
       and analyticsMetadata =
         flag "analytics-metadata" (optional json_arg)
           ~doc:"JSON AnalyticsMetadataType"
       and userContextData =
         flag "user-context-data" (optional json_arg)
           ~doc:"JSON UserContextDataType"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and confirmationCode =
         flag "confirmation-code" (required string)
           ~doc:"STRING ConfirmationCodeType"
       and password =
         flag "password" (required string) ~doc:"STRING PasswordType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.confirm_forgot_password
           (Values.ConfirmForgotPasswordRequest.make ?secretHash
              ?analyticsMetadata:(Option.map
                                    ~f:Values.AnalyticsMetadataType.of_json
                                    analyticsMetadata)
              ?userContextData:(Option.map
                                  ~f:Values.UserContextDataType.of_json
                                  userContextData)
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata) ~clientId ~username
              ~confirmationCode ~password ())
           (Some Values.ConfirmForgotPasswordResponse.to_json)
           (Some Values.ConfirmForgotPasswordResponse.error_to_json)])
let confirm_sign_up =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and secretHash =
         flag "secret-hash" (optional string) ~doc:"STRING SecretHashType"
       and forceAliasCreation =
         flag "force-alias-creation" (optional bool)
           ~doc:"BOOL ForceAliasCreation"
       and analyticsMetadata =
         flag "analytics-metadata" (optional json_arg)
           ~doc:"JSON AnalyticsMetadataType"
       and userContextData =
         flag "user-context-data" (optional json_arg)
           ~doc:"JSON UserContextDataType"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and confirmationCode =
         flag "confirmation-code" (required string)
           ~doc:"STRING ConfirmationCodeType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.confirm_sign_up
           (Values.ConfirmSignUpRequest.make ?secretHash ?forceAliasCreation
              ?analyticsMetadata:(Option.map
                                    ~f:Values.AnalyticsMetadataType.of_json
                                    analyticsMetadata)
              ?userContextData:(Option.map
                                  ~f:Values.UserContextDataType.of_json
                                  userContextData)
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata) ~clientId ~username
              ~confirmationCode ())
           (Some Values.ConfirmSignUpResponse.to_json)
           (Some Values.ConfirmSignUpResponse.error_to_json)])
let create_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionType"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING ArnType"
       and precedence =
         flag "precedence" (optional int) ~doc:"INT PrecedenceType"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupNameType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_group
           (Values.CreateGroupRequest.make ?description ?roleArn ?precedence
              ~groupName ~userPoolId ())
           (Some Values.CreateGroupResponse.to_json)
           (Some Values.CreateGroupResponse.error_to_json)])
let create_identity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeMapping =
         flag "attribute-mapping" (optional json_arg)
           ~doc:"JSON AttributeMappingType"
       and idpIdentifiers =
         flag "idp-identifiers" (optional json_arg)
           ~doc:"JSON IdpIdentifiersListType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and providerName =
         flag "provider-name" (required string)
           ~doc:"STRING ProviderNameTypeV1"
       and providerType =
         flag "provider-type" (required json_arg)
           ~doc:"JSON IdentityProviderTypeType"
       and providerDetails =
         flag "provider-details" (required json_arg)
           ~doc:"JSON ProviderDetailsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_identity_provider
           (Values.CreateIdentityProviderRequest.make
              ?attributeMapping:(Option.map
                                   ~f:Values.AttributeMappingType.of_json
                                   attributeMapping)
              ?idpIdentifiers:(Option.map
                                 ~f:Values.IdpIdentifiersListType.of_json
                                 idpIdentifiers) ~userPoolId ~providerName
              ~providerType:(Values.IdentityProviderTypeType.of_json
                               providerType)
              ~providerDetails:(Values.ProviderDetailsType.of_json
                                  providerDetails) ())
           (Some Values.CreateIdentityProviderResponse.to_json)
           (Some Values.CreateIdentityProviderResponse.error_to_json)])
let create_resource_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scopes =
         flag "scopes" (optional json_arg)
           ~doc:"JSON ResourceServerScopeListType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ResourceServerIdentifierType"
       and name =
         flag "name" (required string) ~doc:"STRING ResourceServerNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_server
           (Values.CreateResourceServerRequest.make
              ?scopes:(Option.map
                         ~f:Values.ResourceServerScopeListType.of_json scopes)
              ~userPoolId ~identifier ~name ())
           (Some Values.CreateResourceServerResponse.to_json)
           (Some Values.CreateResourceServerResponse.error_to_json)])
let create_user_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName =
         flag "job-name" (required string)
           ~doc:"STRING UserImportJobNameType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and cloudWatchLogsRoleArn =
         flag "cloud-watch-logs-role-arn" (required string)
           ~doc:"STRING ArnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user_import_job
           (Values.CreateUserImportJobRequest.make ~jobName ~userPoolId
              ~cloudWatchLogsRoleArn ())
           (Some Values.CreateUserImportJobResponse.to_json)
           (Some Values.CreateUserImportJobResponse.error_to_json)])
let create_user_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policies =
         flag "policies" (optional json_arg) ~doc:"JSON UserPoolPolicyType"
       and lambdaConfig =
         flag "lambda-config" (optional json_arg)
           ~doc:"JSON LambdaConfigType"
       and autoVerifiedAttributes =
         flag "auto-verified-attributes" (optional json_arg)
           ~doc:"JSON VerifiedAttributesListType"
       and aliasAttributes =
         flag "alias-attributes" (optional json_arg)
           ~doc:"JSON AliasAttributesListType"
       and usernameAttributes =
         flag "username-attributes" (optional json_arg)
           ~doc:"JSON UsernameAttributesListType"
       and smsVerificationMessage =
         flag "sms-verification-message" (optional string)
           ~doc:"STRING SmsVerificationMessageType"
       and emailVerificationMessage =
         flag "email-verification-message" (optional string)
           ~doc:"STRING EmailVerificationMessageType"
       and emailVerificationSubject =
         flag "email-verification-subject" (optional string)
           ~doc:"STRING EmailVerificationSubjectType"
       and verificationMessageTemplate =
         flag "verification-message-template" (optional json_arg)
           ~doc:"JSON VerificationMessageTemplateType"
       and smsAuthenticationMessage =
         flag "sms-authentication-message" (optional string)
           ~doc:"STRING SmsVerificationMessageType"
       and mfaConfiguration =
         flag "mfa-configuration" (optional json_arg)
           ~doc:"JSON UserPoolMfaType"
       and deviceConfiguration =
         flag "device-configuration" (optional json_arg)
           ~doc:"JSON DeviceConfigurationType"
       and emailConfiguration =
         flag "email-configuration" (optional json_arg)
           ~doc:"JSON EmailConfigurationType"
       and smsConfiguration =
         flag "sms-configuration" (optional json_arg)
           ~doc:"JSON SmsConfigurationType"
       and userPoolTags =
         flag "user-pool-tags" (optional json_arg)
           ~doc:"JSON UserPoolTagsType"
       and adminCreateUserConfig =
         flag "admin-create-user-config" (optional json_arg)
           ~doc:"JSON AdminCreateUserConfigType"
       and schema =
         flag "schema" (optional json_arg)
           ~doc:"JSON SchemaAttributesListType"
       and userPoolAddOns =
         flag "user-pool-add-ons" (optional json_arg)
           ~doc:"JSON UserPoolAddOnsType"
       and usernameConfiguration =
         flag "username-configuration" (optional json_arg)
           ~doc:"JSON UsernameConfigurationType"
       and accountRecoverySetting =
         flag "account-recovery-setting" (optional json_arg)
           ~doc:"JSON AccountRecoverySettingType"
       and poolName =
         flag "pool-name" (required string) ~doc:"STRING UserPoolNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user_pool
           (Values.CreateUserPoolRequest.make
              ?policies:(Option.map ~f:Values.UserPoolPolicyType.of_json
                           policies)
              ?lambdaConfig:(Option.map ~f:Values.LambdaConfigType.of_json
                               lambdaConfig)
              ?autoVerifiedAttributes:(Option.map
                                         ~f:Values.VerifiedAttributesListType.of_json
                                         autoVerifiedAttributes)
              ?aliasAttributes:(Option.map
                                  ~f:Values.AliasAttributesListType.of_json
                                  aliasAttributes)
              ?usernameAttributes:(Option.map
                                     ~f:Values.UsernameAttributesListType.of_json
                                     usernameAttributes)
              ?smsVerificationMessage ?emailVerificationMessage
              ?emailVerificationSubject
              ?verificationMessageTemplate:(Option.map
                                              ~f:Values.VerificationMessageTemplateType.of_json
                                              verificationMessageTemplate)
              ?smsAuthenticationMessage
              ?mfaConfiguration:(Option.map ~f:Values.UserPoolMfaType.of_json
                                   mfaConfiguration)
              ?deviceConfiguration:(Option.map
                                      ~f:Values.DeviceConfigurationType.of_json
                                      deviceConfiguration)
              ?emailConfiguration:(Option.map
                                     ~f:Values.EmailConfigurationType.of_json
                                     emailConfiguration)
              ?smsConfiguration:(Option.map
                                   ~f:Values.SmsConfigurationType.of_json
                                   smsConfiguration)
              ?userPoolTags:(Option.map ~f:Values.UserPoolTagsType.of_json
                               userPoolTags)
              ?adminCreateUserConfig:(Option.map
                                        ~f:Values.AdminCreateUserConfigType.of_json
                                        adminCreateUserConfig)
              ?schema:(Option.map ~f:Values.SchemaAttributesListType.of_json
                         schema)
              ?userPoolAddOns:(Option.map
                                 ~f:Values.UserPoolAddOnsType.of_json
                                 userPoolAddOns)
              ?usernameConfiguration:(Option.map
                                        ~f:Values.UsernameConfigurationType.of_json
                                        usernameConfiguration)
              ?accountRecoverySetting:(Option.map
                                         ~f:Values.AccountRecoverySettingType.of_json
                                         accountRecoverySetting) ~poolName ())
           (Some Values.CreateUserPoolResponse.to_json)
           (Some Values.CreateUserPoolResponse.error_to_json)])
let create_user_pool_client =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and generateSecret =
         flag "generate-secret" (optional bool) ~doc:"BOOL GenerateSecret"
       and refreshTokenValidity =
         flag "refresh-token-validity" (optional int)
           ~doc:"INT RefreshTokenValidityType"
       and accessTokenValidity =
         flag "access-token-validity" (optional int)
           ~doc:"INT AccessTokenValidityType"
       and idTokenValidity =
         flag "id-token-validity" (optional int)
           ~doc:"INT IdTokenValidityType"
       and tokenValidityUnits =
         flag "token-validity-units" (optional json_arg)
           ~doc:"JSON TokenValidityUnitsType"
       and readAttributes =
         flag "read-attributes" (optional json_arg)
           ~doc:"JSON ClientPermissionListType"
       and writeAttributes =
         flag "write-attributes" (optional json_arg)
           ~doc:"JSON ClientPermissionListType"
       and explicitAuthFlows =
         flag "explicit-auth-flows" (optional json_arg)
           ~doc:"JSON ExplicitAuthFlowsListType"
       and supportedIdentityProviders =
         flag "supported-identity-providers" (optional json_arg)
           ~doc:"JSON SupportedIdentityProvidersListType"
       and callbackURLs =
         flag "callback-u-r-ls" (optional json_arg)
           ~doc:"JSON CallbackURLsListType"
       and logoutURLs =
         flag "logout-u-r-ls" (optional json_arg)
           ~doc:"JSON LogoutURLsListType"
       and defaultRedirectURI =
         flag "default-redirect-u-r-i" (optional string)
           ~doc:"STRING RedirectUrlType"
       and allowedOAuthFlows =
         flag "allowed-o-auth-flows" (optional json_arg)
           ~doc:"JSON OAuthFlowsType"
       and allowedOAuthScopes =
         flag "allowed-o-auth-scopes" (optional json_arg)
           ~doc:"JSON ScopeListType"
       and allowedOAuthFlowsUserPoolClient =
         flag "allowed-o-auth-flows-user-pool-client" (optional bool)
           ~doc:"BOOL BooleanType"
       and analyticsConfiguration =
         flag "analytics-configuration" (optional json_arg)
           ~doc:"JSON AnalyticsConfigurationType"
       and preventUserExistenceErrors =
         flag "prevent-user-existence-errors" (optional json_arg)
           ~doc:"JSON PreventUserExistenceErrorTypes"
       and enableTokenRevocation =
         flag "enable-token-revocation" (optional bool)
           ~doc:"BOOL WrappedBooleanType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and clientName =
         flag "client-name" (required string) ~doc:"STRING ClientNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user_pool_client
           (Values.CreateUserPoolClientRequest.make ?generateSecret
              ?refreshTokenValidity ?accessTokenValidity ?idTokenValidity
              ?tokenValidityUnits:(Option.map
                                     ~f:Values.TokenValidityUnitsType.of_json
                                     tokenValidityUnits)
              ?readAttributes:(Option.map
                                 ~f:Values.ClientPermissionListType.of_json
                                 readAttributes)
              ?writeAttributes:(Option.map
                                  ~f:Values.ClientPermissionListType.of_json
                                  writeAttributes)
              ?explicitAuthFlows:(Option.map
                                    ~f:Values.ExplicitAuthFlowsListType.of_json
                                    explicitAuthFlows)
              ?supportedIdentityProviders:(Option.map
                                             ~f:Values.SupportedIdentityProvidersListType.of_json
                                             supportedIdentityProviders)
              ?callbackURLs:(Option.map
                               ~f:Values.CallbackURLsListType.of_json
                               callbackURLs)
              ?logoutURLs:(Option.map ~f:Values.LogoutURLsListType.of_json
                             logoutURLs) ?defaultRedirectURI
              ?allowedOAuthFlows:(Option.map ~f:Values.OAuthFlowsType.of_json
                                    allowedOAuthFlows)
              ?allowedOAuthScopes:(Option.map ~f:Values.ScopeListType.of_json
                                     allowedOAuthScopes)
              ?allowedOAuthFlowsUserPoolClient
              ?analyticsConfiguration:(Option.map
                                         ~f:Values.AnalyticsConfigurationType.of_json
                                         analyticsConfiguration)
              ?preventUserExistenceErrors:(Option.map
                                             ~f:Values.PreventUserExistenceErrorTypes.of_json
                                             preventUserExistenceErrors)
              ?enableTokenRevocation ~userPoolId ~clientName ())
           (Some Values.CreateUserPoolClientResponse.to_json)
           (Some Values.CreateUserPoolClientResponse.error_to_json)])
let create_user_pool_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customDomainConfig =
         flag "custom-domain-config" (optional json_arg)
           ~doc:"JSON CustomDomainConfigType"
       and domain = flag "domain" (required string) ~doc:"STRING DomainType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user_pool_domain
           (Values.CreateUserPoolDomainRequest.make
              ?customDomainConfig:(Option.map
                                     ~f:Values.CustomDomainConfigType.of_json
                                     customDomainConfig) ~domain ~userPoolId
              ()) (Some Values.CreateUserPoolDomainResponse.to_json)
           (Some Values.CreateUserPoolDomainResponse.error_to_json)])
let delete_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupNameType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_group
           (Values.DeleteGroupRequest.make ~groupName ~userPoolId ()) None
           None])
let delete_identity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and providerName =
         flag "provider-name" (required string)
           ~doc:"STRING ProviderNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_identity_provider
           (Values.DeleteIdentityProviderRequest.make ~userPoolId
              ~providerName ()) None None])
let delete_resource_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ResourceServerIdentifierType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_server
           (Values.DeleteResourceServerRequest.make ~userPoolId ~identifier
              ()) None None])
let delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessToken =
         flag "access-token" (required string) ~doc:"STRING TokenModelType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user (Values.DeleteUserRequest.make ~accessToken ())
           None None])
let delete_user_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userAttributeNames =
         flag "user-attribute-names" (required json_arg)
           ~doc:"JSON AttributeNameListType"
       and accessToken =
         flag "access-token" (required string) ~doc:"STRING TokenModelType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user_attributes
           (Values.DeleteUserAttributesRequest.make
              ~userAttributeNames:(Values.AttributeNameListType.of_json
                                     userAttributeNames) ~accessToken ())
           (Some Values.DeleteUserAttributesResponse.to_json)
           (Some Values.DeleteUserAttributesResponse.error_to_json)])
let delete_user_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user_pool
           (Values.DeleteUserPoolRequest.make ~userPoolId ()) None None])
let delete_user_pool_client =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user_pool_client
           (Values.DeleteUserPoolClientRequest.make ~userPoolId ~clientId ())
           None None])
let delete_user_pool_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING DomainType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user_pool_domain
           (Values.DeleteUserPoolDomainRequest.make ~domain ~userPoolId ())
           (Some Values.DeleteUserPoolDomainResponse.to_json)
           (Some Values.DeleteUserPoolDomainResponse.error_to_json)])
let describe_identity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and providerName =
         flag "provider-name" (required string)
           ~doc:"STRING ProviderNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_identity_provider
           (Values.DescribeIdentityProviderRequest.make ~userPoolId
              ~providerName ())
           (Some Values.DescribeIdentityProviderResponse.to_json)
           (Some Values.DescribeIdentityProviderResponse.error_to_json)])
let describe_resource_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ResourceServerIdentifierType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_resource_server
           (Values.DescribeResourceServerRequest.make ~userPoolId ~identifier
              ()) (Some Values.DescribeResourceServerResponse.to_json)
           (Some Values.DescribeResourceServerResponse.error_to_json)])
let describe_risk_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientId =
         flag "client-id" (optional string) ~doc:"STRING ClientIdType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_risk_configuration
           (Values.DescribeRiskConfigurationRequest.make ?clientId
              ~userPoolId ())
           (Some Values.DescribeRiskConfigurationResponse.to_json)
           (Some Values.DescribeRiskConfigurationResponse.error_to_json)])
let describe_user_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and jobId =
         flag "job-id" (required string) ~doc:"STRING UserImportJobIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user_import_job
           (Values.DescribeUserImportJobRequest.make ~userPoolId ~jobId ())
           (Some Values.DescribeUserImportJobResponse.to_json)
           (Some Values.DescribeUserImportJobResponse.error_to_json)])
let describe_user_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user_pool
           (Values.DescribeUserPoolRequest.make ~userPoolId ())
           (Some Values.DescribeUserPoolResponse.to_json)
           (Some Values.DescribeUserPoolResponse.error_to_json)])
let describe_user_pool_client =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user_pool_client
           (Values.DescribeUserPoolClientRequest.make ~userPoolId ~clientId
              ()) (Some Values.DescribeUserPoolClientResponse.to_json)
           (Some Values.DescribeUserPoolClientResponse.error_to_json)])
let describe_user_pool_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING DomainType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user_pool_domain
           (Values.DescribeUserPoolDomainRequest.make ~domain ())
           (Some Values.DescribeUserPoolDomainResponse.to_json)
           (Some Values.DescribeUserPoolDomainResponse.error_to_json)])
let forget_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessToken =
         flag "access-token" (optional string) ~doc:"STRING TokenModelType"
       and deviceKey =
         flag "device-key" (required string) ~doc:"STRING DeviceKeyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.forget_device
           (Values.ForgetDeviceRequest.make ?accessToken ~deviceKey ()) None
           None])
let forgot_password =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and secretHash =
         flag "secret-hash" (optional string) ~doc:"STRING SecretHashType"
       and userContextData =
         flag "user-context-data" (optional json_arg)
           ~doc:"JSON UserContextDataType"
       and analyticsMetadata =
         flag "analytics-metadata" (optional json_arg)
           ~doc:"JSON AnalyticsMetadataType"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.forgot_password
           (Values.ForgotPasswordRequest.make ?secretHash
              ?userContextData:(Option.map
                                  ~f:Values.UserContextDataType.of_json
                                  userContextData)
              ?analyticsMetadata:(Option.map
                                    ~f:Values.AnalyticsMetadataType.of_json
                                    analyticsMetadata)
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata) ~clientId ~username ())
           (Some Values.ForgotPasswordResponse.to_json)
           (Some Values.ForgotPasswordResponse.error_to_json)])
let get_c_s_v_header =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_c_s_v_header
           (Values.GetCSVHeaderRequest.make ~userPoolId ())
           (Some Values.GetCSVHeaderResponse.to_json)
           (Some Values.GetCSVHeaderResponse.error_to_json)])
let get_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessToken =
         flag "access-token" (optional string) ~doc:"STRING TokenModelType"
       and deviceKey =
         flag "device-key" (required string) ~doc:"STRING DeviceKeyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device
           (Values.GetDeviceRequest.make ?accessToken ~deviceKey ())
           (Some Values.GetDeviceResponse.to_json)
           (Some Values.GetDeviceResponse.error_to_json)])
let get_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupNameType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_group
           (Values.GetGroupRequest.make ~groupName ~userPoolId ())
           (Some Values.GetGroupResponse.to_json)
           (Some Values.GetGroupResponse.error_to_json)])
let get_identity_provider_by_identifier =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and idpIdentifier =
         flag "idp-identifier" (required string)
           ~doc:"STRING IdpIdentifierType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_identity_provider_by_identifier
           (Values.GetIdentityProviderByIdentifierRequest.make ~userPoolId
              ~idpIdentifier ())
           (Some Values.GetIdentityProviderByIdentifierResponse.to_json)
           (Some Values.GetIdentityProviderByIdentifierResponse.error_to_json)])
let get_signing_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_signing_certificate
           (Values.GetSigningCertificateRequest.make ~userPoolId ())
           (Some Values.GetSigningCertificateResponse.to_json)
           (Some Values.GetSigningCertificateResponse.error_to_json)])
let get_u_i_customization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientId =
         flag "client-id" (optional string) ~doc:"STRING ClientIdType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_u_i_customization
           (Values.GetUICustomizationRequest.make ?clientId ~userPoolId ())
           (Some Values.GetUICustomizationResponse.to_json)
           (Some Values.GetUICustomizationResponse.error_to_json)])
let get_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessToken =
         flag "access-token" (required string) ~doc:"STRING TokenModelType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user (Values.GetUserRequest.make ~accessToken ())
           (Some Values.GetUserResponse.to_json)
           (Some Values.GetUserResponse.error_to_json)])
let get_user_attribute_verification_code =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and accessToken =
         flag "access-token" (required string) ~doc:"STRING TokenModelType"
       and attributeName =
         flag "attribute-name" (required string)
           ~doc:"STRING AttributeNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user_attribute_verification_code
           (Values.GetUserAttributeVerificationCodeRequest.make
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata) ~accessToken ~attributeName
              ())
           (Some Values.GetUserAttributeVerificationCodeResponse.to_json)
           (Some
              Values.GetUserAttributeVerificationCodeResponse.error_to_json)])
let get_user_pool_mfa_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user_pool_mfa_config
           (Values.GetUserPoolMfaConfigRequest.make ~userPoolId ())
           (Some Values.GetUserPoolMfaConfigResponse.to_json)
           (Some Values.GetUserPoolMfaConfigResponse.error_to_json)])
let global_sign_out =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessToken =
         flag "access-token" (required string) ~doc:"STRING TokenModelType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.global_sign_out
           (Values.GlobalSignOutRequest.make ~accessToken ())
           (Some Values.GlobalSignOutResponse.to_json)
           (Some Values.GlobalSignOutResponse.error_to_json)])
let initiate_auth =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authParameters =
         flag "auth-parameters" (optional json_arg)
           ~doc:"JSON AuthParametersType"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and analyticsMetadata =
         flag "analytics-metadata" (optional json_arg)
           ~doc:"JSON AnalyticsMetadataType"
       and userContextData =
         flag "user-context-data" (optional json_arg)
           ~doc:"JSON UserContextDataType"
       and authFlow =
         flag "auth-flow" (required json_arg) ~doc:"JSON AuthFlowType"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.initiate_auth
           (Values.InitiateAuthRequest.make
              ?authParameters:(Option.map
                                 ~f:Values.AuthParametersType.of_json
                                 authParameters)
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata)
              ?analyticsMetadata:(Option.map
                                    ~f:Values.AnalyticsMetadataType.of_json
                                    analyticsMetadata)
              ?userContextData:(Option.map
                                  ~f:Values.UserContextDataType.of_json
                                  userContextData)
              ~authFlow:(Values.AuthFlowType.of_json authFlow) ~clientId ())
           (Some Values.InitiateAuthResponse.to_json)
           (Some Values.InitiateAuthResponse.error_to_json)])
let list_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT QueryLimitType"
       and paginationToken =
         flag "pagination-token" (optional string)
           ~doc:"STRING SearchPaginationTokenType"
       and accessToken =
         flag "access-token" (required string) ~doc:"STRING TokenModelType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_devices
           (Values.ListDevicesRequest.make ?limit ?paginationToken
              ~accessToken ()) (Some Values.ListDevicesResponse.to_json)
           (Some Values.ListDevicesResponse.error_to_json)])
let list_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT QueryLimitType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationKey"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_groups
           (Values.ListGroupsRequest.make ?limit ?nextToken ~userPoolId ())
           (Some Values.ListGroupsResponse.to_json)
           (Some Values.ListGroupsResponse.error_to_json)])
let list_identity_providers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListProvidersLimitType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationKeyType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_identity_providers
           (Values.ListIdentityProvidersRequest.make ?maxResults ?nextToken
              ~userPoolId ())
           (Some Values.ListIdentityProvidersResponse.to_json)
           (Some Values.ListIdentityProvidersResponse.error_to_json)])
let list_resource_servers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListResourceServersLimitType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationKeyType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_servers
           (Values.ListResourceServersRequest.make ?maxResults ?nextToken
              ~userPoolId ())
           (Some Values.ListResourceServersResponse.to_json)
           (Some Values.ListResourceServersResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ArnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_user_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and paginationToken =
         flag "pagination-token" (optional string)
           ~doc:"STRING PaginationKeyType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and maxResults =
         flag "max-results" (required int) ~doc:"INT PoolQueryLimitType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_user_import_jobs
           (Values.ListUserImportJobsRequest.make ?paginationToken
              ~userPoolId ~maxResults ())
           (Some Values.ListUserImportJobsResponse.to_json)
           (Some Values.ListUserImportJobsResponse.error_to_json)])
let list_user_pool_clients =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT QueryLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationKey"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_user_pool_clients
           (Values.ListUserPoolClientsRequest.make ?maxResults ?nextToken
              ~userPoolId ())
           (Some Values.ListUserPoolClientsResponse.to_json)
           (Some Values.ListUserPoolClientsResponse.error_to_json)])
let list_user_pools =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationKeyType"
       and maxResults =
         flag "max-results" (required int) ~doc:"INT PoolQueryLimitType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_user_pools
           (Values.ListUserPoolsRequest.make ?nextToken ~maxResults ())
           (Some Values.ListUserPoolsResponse.to_json)
           (Some Values.ListUserPoolsResponse.error_to_json)])
let list_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributesToGet =
         flag "attributes-to-get" (optional json_arg)
           ~doc:"JSON SearchedAttributeNamesListType"
       and limit = flag "limit" (optional int) ~doc:"INT QueryLimitType"
       and paginationToken =
         flag "pagination-token" (optional string)
           ~doc:"STRING SearchPaginationTokenType"
       and filter =
         flag "filter" (optional string) ~doc:"STRING UserFilterType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_users
           (Values.ListUsersRequest.make
              ?attributesToGet:(Option.map
                                  ~f:Values.SearchedAttributeNamesListType.of_json
                                  attributesToGet) ?limit ?paginationToken
              ?filter ~userPoolId ()) (Some Values.ListUsersResponse.to_json)
           (Some Values.ListUsersResponse.error_to_json)])
let list_users_in_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT QueryLimitType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationKey"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_users_in_group
           (Values.ListUsersInGroupRequest.make ?limit ?nextToken ~userPoolId
              ~groupName ()) (Some Values.ListUsersInGroupResponse.to_json)
           (Some Values.ListUsersInGroupResponse.error_to_json)])
let resend_confirmation_code =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and secretHash =
         flag "secret-hash" (optional string) ~doc:"STRING SecretHashType"
       and userContextData =
         flag "user-context-data" (optional json_arg)
           ~doc:"JSON UserContextDataType"
       and analyticsMetadata =
         flag "analytics-metadata" (optional json_arg)
           ~doc:"JSON AnalyticsMetadataType"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resend_confirmation_code
           (Values.ResendConfirmationCodeRequest.make ?secretHash
              ?userContextData:(Option.map
                                  ~f:Values.UserContextDataType.of_json
                                  userContextData)
              ?analyticsMetadata:(Option.map
                                    ~f:Values.AnalyticsMetadataType.of_json
                                    analyticsMetadata)
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata) ~clientId ~username ())
           (Some Values.ResendConfirmationCodeResponse.to_json)
           (Some Values.ResendConfirmationCodeResponse.error_to_json)])
let respond_to_auth_challenge =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and session =
         flag "session" (optional string) ~doc:"STRING SessionType"
       and challengeResponses =
         flag "challenge-responses" (optional json_arg)
           ~doc:"JSON ChallengeResponsesType"
       and analyticsMetadata =
         flag "analytics-metadata" (optional json_arg)
           ~doc:"JSON AnalyticsMetadataType"
       and userContextData =
         flag "user-context-data" (optional json_arg)
           ~doc:"JSON UserContextDataType"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientIdType"
       and challengeName =
         flag "challenge-name" (required json_arg)
           ~doc:"JSON ChallengeNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.respond_to_auth_challenge
           (Values.RespondToAuthChallengeRequest.make ?session
              ?challengeResponses:(Option.map
                                     ~f:Values.ChallengeResponsesType.of_json
                                     challengeResponses)
              ?analyticsMetadata:(Option.map
                                    ~f:Values.AnalyticsMetadataType.of_json
                                    analyticsMetadata)
              ?userContextData:(Option.map
                                  ~f:Values.UserContextDataType.of_json
                                  userContextData)
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata) ~clientId
              ~challengeName:(Values.ChallengeNameType.of_json challengeName)
              ()) (Some Values.RespondToAuthChallengeResponse.to_json)
           (Some Values.RespondToAuthChallengeResponse.error_to_json)])
let revoke_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientSecret =
         flag "client-secret" (optional string)
           ~doc:"STRING ClientSecretType"
       and token =
         flag "token" (required string) ~doc:"STRING TokenModelType"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_token
           (Values.RevokeTokenRequest.make ?clientSecret ~token ~clientId ())
           (Some Values.RevokeTokenResponse.to_json)
           (Some Values.RevokeTokenResponse.error_to_json)])
let set_risk_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientId =
         flag "client-id" (optional string) ~doc:"STRING ClientIdType"
       and compromisedCredentialsRiskConfiguration =
         flag "compromised-credentials-risk-configuration"
           (optional json_arg)
           ~doc:"JSON CompromisedCredentialsRiskConfigurationType"
       and accountTakeoverRiskConfiguration =
         flag "account-takeover-risk-configuration" (optional json_arg)
           ~doc:"JSON AccountTakeoverRiskConfigurationType"
       and riskExceptionConfiguration =
         flag "risk-exception-configuration" (optional json_arg)
           ~doc:"JSON RiskExceptionConfigurationType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_risk_configuration
           (Values.SetRiskConfigurationRequest.make ?clientId
              ?compromisedCredentialsRiskConfiguration:(Option.map
                                                          ~f:Values.CompromisedCredentialsRiskConfigurationType.of_json
                                                          compromisedCredentialsRiskConfiguration)
              ?accountTakeoverRiskConfiguration:(Option.map
                                                   ~f:Values.AccountTakeoverRiskConfigurationType.of_json
                                                   accountTakeoverRiskConfiguration)
              ?riskExceptionConfiguration:(Option.map
                                             ~f:Values.RiskExceptionConfigurationType.of_json
                                             riskExceptionConfiguration)
              ~userPoolId ())
           (Some Values.SetRiskConfigurationResponse.to_json)
           (Some Values.SetRiskConfigurationResponse.error_to_json)])
let set_u_i_customization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientId =
         flag "client-id" (optional string) ~doc:"STRING ClientIdType"
       and cSS = flag "c-s-s" (optional string) ~doc:"STRING CSSType"
       and imageFile =
         flag "image-file" (optional json_arg) ~doc:"JSON ImageFileType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_u_i_customization
           (Values.SetUICustomizationRequest.make ?clientId ?cSS
              ?imageFile:(Option.map ~f:Values.ImageFileType.of_json
                            imageFile) ~userPoolId ())
           (Some Values.SetUICustomizationResponse.to_json)
           (Some Values.SetUICustomizationResponse.error_to_json)])
let set_user_m_f_a_preference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sMSMfaSettings =
         flag "s-m-s-mfa-settings" (optional json_arg)
           ~doc:"JSON SMSMfaSettingsType"
       and softwareTokenMfaSettings =
         flag "software-token-mfa-settings" (optional json_arg)
           ~doc:"JSON SoftwareTokenMfaSettingsType"
       and accessToken =
         flag "access-token" (required string) ~doc:"STRING TokenModelType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_user_m_f_a_preference
           (Values.SetUserMFAPreferenceRequest.make
              ?sMSMfaSettings:(Option.map
                                 ~f:Values.SMSMfaSettingsType.of_json
                                 sMSMfaSettings)
              ?softwareTokenMfaSettings:(Option.map
                                           ~f:Values.SoftwareTokenMfaSettingsType.of_json
                                           softwareTokenMfaSettings)
              ~accessToken ())
           (Some Values.SetUserMFAPreferenceResponse.to_json)
           (Some Values.SetUserMFAPreferenceResponse.error_to_json)])
let set_user_pool_mfa_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and smsMfaConfiguration =
         flag "sms-mfa-configuration" (optional json_arg)
           ~doc:"JSON SmsMfaConfigType"
       and softwareTokenMfaConfiguration =
         flag "software-token-mfa-configuration" (optional json_arg)
           ~doc:"JSON SoftwareTokenMfaConfigType"
       and mfaConfiguration =
         flag "mfa-configuration" (optional json_arg)
           ~doc:"JSON UserPoolMfaType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_user_pool_mfa_config
           (Values.SetUserPoolMfaConfigRequest.make
              ?smsMfaConfiguration:(Option.map
                                      ~f:Values.SmsMfaConfigType.of_json
                                      smsMfaConfiguration)
              ?softwareTokenMfaConfiguration:(Option.map
                                                ~f:Values.SoftwareTokenMfaConfigType.of_json
                                                softwareTokenMfaConfiguration)
              ?mfaConfiguration:(Option.map ~f:Values.UserPoolMfaType.of_json
                                   mfaConfiguration) ~userPoolId ())
           (Some Values.SetUserPoolMfaConfigResponse.to_json)
           (Some Values.SetUserPoolMfaConfigResponse.error_to_json)])
let set_user_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessToken =
         flag "access-token" (required string) ~doc:"STRING TokenModelType"
       and mFAOptions =
         flag "m-f-a-options" (required json_arg)
           ~doc:"JSON MFAOptionListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_user_settings
           (Values.SetUserSettingsRequest.make ~accessToken
              ~mFAOptions:(Values.MFAOptionListType.of_json mFAOptions) ())
           (Some Values.SetUserSettingsResponse.to_json)
           (Some Values.SetUserSettingsResponse.error_to_json)])
let sign_up =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and secretHash =
         flag "secret-hash" (optional string) ~doc:"STRING SecretHashType"
       and userAttributes =
         flag "user-attributes" (optional json_arg)
           ~doc:"JSON AttributeListType"
       and validationData =
         flag "validation-data" (optional json_arg)
           ~doc:"JSON AttributeListType"
       and analyticsMetadata =
         flag "analytics-metadata" (optional json_arg)
           ~doc:"JSON AnalyticsMetadataType"
       and userContextData =
         flag "user-context-data" (optional json_arg)
           ~doc:"JSON UserContextDataType"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and password =
         flag "password" (required string) ~doc:"STRING PasswordType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.sign_up
           (Values.SignUpRequest.make ?secretHash
              ?userAttributes:(Option.map ~f:Values.AttributeListType.of_json
                                 userAttributes)
              ?validationData:(Option.map ~f:Values.AttributeListType.of_json
                                 validationData)
              ?analyticsMetadata:(Option.map
                                    ~f:Values.AnalyticsMetadataType.of_json
                                    analyticsMetadata)
              ?userContextData:(Option.map
                                  ~f:Values.UserContextDataType.of_json
                                  userContextData)
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata) ~clientId ~username
              ~password ()) (Some Values.SignUpResponse.to_json)
           (Some Values.SignUpResponse.error_to_json)])
let start_user_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and jobId =
         flag "job-id" (required string) ~doc:"STRING UserImportJobIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_user_import_job
           (Values.StartUserImportJobRequest.make ~userPoolId ~jobId ())
           (Some Values.StartUserImportJobResponse.to_json)
           (Some Values.StartUserImportJobResponse.error_to_json)])
let stop_user_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and jobId =
         flag "job-id" (required string) ~doc:"STRING UserImportJobIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_user_import_job
           (Values.StopUserImportJobRequest.make ~userPoolId ~jobId ())
           (Some Values.StopUserImportJobResponse.to_json)
           (Some Values.StopUserImportJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ArnType"
       and tags =
         flag "tags" (required json_arg) ~doc:"JSON UserPoolTagsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.UserPoolTagsType.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ArnType"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON UserPoolTagsListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.UserPoolTagsListType.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_auth_event_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and eventId =
         flag "event-id" (required string) ~doc:"STRING EventIdType"
       and feedbackToken =
         flag "feedback-token" (required string) ~doc:"STRING TokenModelType"
       and feedbackValue =
         flag "feedback-value" (required json_arg)
           ~doc:"JSON FeedbackValueType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_auth_event_feedback
           (Values.UpdateAuthEventFeedbackRequest.make ~userPoolId ~username
              ~eventId ~feedbackToken
              ~feedbackValue:(Values.FeedbackValueType.of_json feedbackValue)
              ()) (Some Values.UpdateAuthEventFeedbackResponse.to_json)
           (Some Values.UpdateAuthEventFeedbackResponse.error_to_json)])
let update_device_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceRememberedStatus =
         flag "device-remembered-status" (optional json_arg)
           ~doc:"JSON DeviceRememberedStatusType"
       and accessToken =
         flag "access-token" (required string) ~doc:"STRING TokenModelType"
       and deviceKey =
         flag "device-key" (required string) ~doc:"STRING DeviceKeyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_device_status
           (Values.UpdateDeviceStatusRequest.make
              ?deviceRememberedStatus:(Option.map
                                         ~f:Values.DeviceRememberedStatusType.of_json
                                         deviceRememberedStatus) ~accessToken
              ~deviceKey ()) (Some Values.UpdateDeviceStatusResponse.to_json)
           (Some Values.UpdateDeviceStatusResponse.error_to_json)])
let update_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionType"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING ArnType"
       and precedence =
         flag "precedence" (optional int) ~doc:"INT PrecedenceType"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupNameType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_group
           (Values.UpdateGroupRequest.make ?description ?roleArn ?precedence
              ~groupName ~userPoolId ())
           (Some Values.UpdateGroupResponse.to_json)
           (Some Values.UpdateGroupResponse.error_to_json)])
let update_identity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and providerDetails =
         flag "provider-details" (optional json_arg)
           ~doc:"JSON ProviderDetailsType"
       and attributeMapping =
         flag "attribute-mapping" (optional json_arg)
           ~doc:"JSON AttributeMappingType"
       and idpIdentifiers =
         flag "idp-identifiers" (optional json_arg)
           ~doc:"JSON IdpIdentifiersListType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and providerName =
         flag "provider-name" (required string)
           ~doc:"STRING ProviderNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_identity_provider
           (Values.UpdateIdentityProviderRequest.make
              ?providerDetails:(Option.map
                                  ~f:Values.ProviderDetailsType.of_json
                                  providerDetails)
              ?attributeMapping:(Option.map
                                   ~f:Values.AttributeMappingType.of_json
                                   attributeMapping)
              ?idpIdentifiers:(Option.map
                                 ~f:Values.IdpIdentifiersListType.of_json
                                 idpIdentifiers) ~userPoolId ~providerName ())
           (Some Values.UpdateIdentityProviderResponse.to_json)
           (Some Values.UpdateIdentityProviderResponse.error_to_json)])
let update_resource_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scopes =
         flag "scopes" (optional json_arg)
           ~doc:"JSON ResourceServerScopeListType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ResourceServerIdentifierType"
       and name =
         flag "name" (required string) ~doc:"STRING ResourceServerNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource_server
           (Values.UpdateResourceServerRequest.make
              ?scopes:(Option.map
                         ~f:Values.ResourceServerScopeListType.of_json scopes)
              ~userPoolId ~identifier ~name ())
           (Some Values.UpdateResourceServerResponse.to_json)
           (Some Values.UpdateResourceServerResponse.error_to_json)])
let update_user_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientMetadata =
         flag "client-metadata" (optional json_arg)
           ~doc:"JSON ClientMetadataType"
       and userAttributes =
         flag "user-attributes" (required json_arg)
           ~doc:"JSON AttributeListType"
       and accessToken =
         flag "access-token" (required string) ~doc:"STRING TokenModelType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_attributes
           (Values.UpdateUserAttributesRequest.make
              ?clientMetadata:(Option.map
                                 ~f:Values.ClientMetadataType.of_json
                                 clientMetadata)
              ~userAttributes:(Values.AttributeListType.of_json
                                 userAttributes) ~accessToken ())
           (Some Values.UpdateUserAttributesResponse.to_json)
           (Some Values.UpdateUserAttributesResponse.error_to_json)])
let update_user_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policies =
         flag "policies" (optional json_arg) ~doc:"JSON UserPoolPolicyType"
       and lambdaConfig =
         flag "lambda-config" (optional json_arg)
           ~doc:"JSON LambdaConfigType"
       and autoVerifiedAttributes =
         flag "auto-verified-attributes" (optional json_arg)
           ~doc:"JSON VerifiedAttributesListType"
       and smsVerificationMessage =
         flag "sms-verification-message" (optional string)
           ~doc:"STRING SmsVerificationMessageType"
       and emailVerificationMessage =
         flag "email-verification-message" (optional string)
           ~doc:"STRING EmailVerificationMessageType"
       and emailVerificationSubject =
         flag "email-verification-subject" (optional string)
           ~doc:"STRING EmailVerificationSubjectType"
       and verificationMessageTemplate =
         flag "verification-message-template" (optional json_arg)
           ~doc:"JSON VerificationMessageTemplateType"
       and smsAuthenticationMessage =
         flag "sms-authentication-message" (optional string)
           ~doc:"STRING SmsVerificationMessageType"
       and mfaConfiguration =
         flag "mfa-configuration" (optional json_arg)
           ~doc:"JSON UserPoolMfaType"
       and deviceConfiguration =
         flag "device-configuration" (optional json_arg)
           ~doc:"JSON DeviceConfigurationType"
       and emailConfiguration =
         flag "email-configuration" (optional json_arg)
           ~doc:"JSON EmailConfigurationType"
       and smsConfiguration =
         flag "sms-configuration" (optional json_arg)
           ~doc:"JSON SmsConfigurationType"
       and userPoolTags =
         flag "user-pool-tags" (optional json_arg)
           ~doc:"JSON UserPoolTagsType"
       and adminCreateUserConfig =
         flag "admin-create-user-config" (optional json_arg)
           ~doc:"JSON AdminCreateUserConfigType"
       and userPoolAddOns =
         flag "user-pool-add-ons" (optional json_arg)
           ~doc:"JSON UserPoolAddOnsType"
       and accountRecoverySetting =
         flag "account-recovery-setting" (optional json_arg)
           ~doc:"JSON AccountRecoverySettingType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_pool
           (Values.UpdateUserPoolRequest.make
              ?policies:(Option.map ~f:Values.UserPoolPolicyType.of_json
                           policies)
              ?lambdaConfig:(Option.map ~f:Values.LambdaConfigType.of_json
                               lambdaConfig)
              ?autoVerifiedAttributes:(Option.map
                                         ~f:Values.VerifiedAttributesListType.of_json
                                         autoVerifiedAttributes)
              ?smsVerificationMessage ?emailVerificationMessage
              ?emailVerificationSubject
              ?verificationMessageTemplate:(Option.map
                                              ~f:Values.VerificationMessageTemplateType.of_json
                                              verificationMessageTemplate)
              ?smsAuthenticationMessage
              ?mfaConfiguration:(Option.map ~f:Values.UserPoolMfaType.of_json
                                   mfaConfiguration)
              ?deviceConfiguration:(Option.map
                                      ~f:Values.DeviceConfigurationType.of_json
                                      deviceConfiguration)
              ?emailConfiguration:(Option.map
                                     ~f:Values.EmailConfigurationType.of_json
                                     emailConfiguration)
              ?smsConfiguration:(Option.map
                                   ~f:Values.SmsConfigurationType.of_json
                                   smsConfiguration)
              ?userPoolTags:(Option.map ~f:Values.UserPoolTagsType.of_json
                               userPoolTags)
              ?adminCreateUserConfig:(Option.map
                                        ~f:Values.AdminCreateUserConfigType.of_json
                                        adminCreateUserConfig)
              ?userPoolAddOns:(Option.map
                                 ~f:Values.UserPoolAddOnsType.of_json
                                 userPoolAddOns)
              ?accountRecoverySetting:(Option.map
                                         ~f:Values.AccountRecoverySettingType.of_json
                                         accountRecoverySetting) ~userPoolId
              ()) (Some Values.UpdateUserPoolResponse.to_json)
           (Some Values.UpdateUserPoolResponse.error_to_json)])
let update_user_pool_client =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientName =
         flag "client-name" (optional string) ~doc:"STRING ClientNameType"
       and refreshTokenValidity =
         flag "refresh-token-validity" (optional int)
           ~doc:"INT RefreshTokenValidityType"
       and accessTokenValidity =
         flag "access-token-validity" (optional int)
           ~doc:"INT AccessTokenValidityType"
       and idTokenValidity =
         flag "id-token-validity" (optional int)
           ~doc:"INT IdTokenValidityType"
       and tokenValidityUnits =
         flag "token-validity-units" (optional json_arg)
           ~doc:"JSON TokenValidityUnitsType"
       and readAttributes =
         flag "read-attributes" (optional json_arg)
           ~doc:"JSON ClientPermissionListType"
       and writeAttributes =
         flag "write-attributes" (optional json_arg)
           ~doc:"JSON ClientPermissionListType"
       and explicitAuthFlows =
         flag "explicit-auth-flows" (optional json_arg)
           ~doc:"JSON ExplicitAuthFlowsListType"
       and supportedIdentityProviders =
         flag "supported-identity-providers" (optional json_arg)
           ~doc:"JSON SupportedIdentityProvidersListType"
       and callbackURLs =
         flag "callback-u-r-ls" (optional json_arg)
           ~doc:"JSON CallbackURLsListType"
       and logoutURLs =
         flag "logout-u-r-ls" (optional json_arg)
           ~doc:"JSON LogoutURLsListType"
       and defaultRedirectURI =
         flag "default-redirect-u-r-i" (optional string)
           ~doc:"STRING RedirectUrlType"
       and allowedOAuthFlows =
         flag "allowed-o-auth-flows" (optional json_arg)
           ~doc:"JSON OAuthFlowsType"
       and allowedOAuthScopes =
         flag "allowed-o-auth-scopes" (optional json_arg)
           ~doc:"JSON ScopeListType"
       and allowedOAuthFlowsUserPoolClient =
         flag "allowed-o-auth-flows-user-pool-client" (optional bool)
           ~doc:"BOOL BooleanType"
       and analyticsConfiguration =
         flag "analytics-configuration" (optional json_arg)
           ~doc:"JSON AnalyticsConfigurationType"
       and preventUserExistenceErrors =
         flag "prevent-user-existence-errors" (optional json_arg)
           ~doc:"JSON PreventUserExistenceErrorTypes"
       and enableTokenRevocation =
         flag "enable-token-revocation" (optional bool)
           ~doc:"BOOL WrappedBooleanType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_pool_client
           (Values.UpdateUserPoolClientRequest.make ?clientName
              ?refreshTokenValidity ?accessTokenValidity ?idTokenValidity
              ?tokenValidityUnits:(Option.map
                                     ~f:Values.TokenValidityUnitsType.of_json
                                     tokenValidityUnits)
              ?readAttributes:(Option.map
                                 ~f:Values.ClientPermissionListType.of_json
                                 readAttributes)
              ?writeAttributes:(Option.map
                                  ~f:Values.ClientPermissionListType.of_json
                                  writeAttributes)
              ?explicitAuthFlows:(Option.map
                                    ~f:Values.ExplicitAuthFlowsListType.of_json
                                    explicitAuthFlows)
              ?supportedIdentityProviders:(Option.map
                                             ~f:Values.SupportedIdentityProvidersListType.of_json
                                             supportedIdentityProviders)
              ?callbackURLs:(Option.map
                               ~f:Values.CallbackURLsListType.of_json
                               callbackURLs)
              ?logoutURLs:(Option.map ~f:Values.LogoutURLsListType.of_json
                             logoutURLs) ?defaultRedirectURI
              ?allowedOAuthFlows:(Option.map ~f:Values.OAuthFlowsType.of_json
                                    allowedOAuthFlows)
              ?allowedOAuthScopes:(Option.map ~f:Values.ScopeListType.of_json
                                     allowedOAuthScopes)
              ?allowedOAuthFlowsUserPoolClient
              ?analyticsConfiguration:(Option.map
                                         ~f:Values.AnalyticsConfigurationType.of_json
                                         analyticsConfiguration)
              ?preventUserExistenceErrors:(Option.map
                                             ~f:Values.PreventUserExistenceErrorTypes.of_json
                                             preventUserExistenceErrors)
              ?enableTokenRevocation ~userPoolId ~clientId ())
           (Some Values.UpdateUserPoolClientResponse.to_json)
           (Some Values.UpdateUserPoolClientResponse.error_to_json)])
let update_user_pool_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING DomainType"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING UserPoolIdType"
       and customDomainConfig =
         flag "custom-domain-config" (required json_arg)
           ~doc:"JSON CustomDomainConfigType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_pool_domain
           (Values.UpdateUserPoolDomainRequest.make ~domain ~userPoolId
              ~customDomainConfig:(Values.CustomDomainConfigType.of_json
                                     customDomainConfig) ())
           (Some Values.UpdateUserPoolDomainResponse.to_json)
           (Some Values.UpdateUserPoolDomainResponse.error_to_json)])
let verify_software_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessToken =
         flag "access-token" (optional string) ~doc:"STRING TokenModelType"
       and session =
         flag "session" (optional string) ~doc:"STRING SessionType"
       and friendlyDeviceName =
         flag "friendly-device-name" (optional string)
           ~doc:"STRING StringType"
       and userCode =
         flag "user-code" (required string)
           ~doc:"STRING SoftwareTokenMFAUserCodeType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_software_token
           (Values.VerifySoftwareTokenRequest.make ?accessToken ?session
              ?friendlyDeviceName ~userCode ())
           (Some Values.VerifySoftwareTokenResponse.to_json)
           (Some Values.VerifySoftwareTokenResponse.error_to_json)])
let verify_user_attribute =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessToken =
         flag "access-token" (required string) ~doc:"STRING TokenModelType"
       and attributeName =
         flag "attribute-name" (required string)
           ~doc:"STRING AttributeNameType"
       and code =
         flag "code" (required string) ~doc:"STRING ConfirmationCodeType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_user_attribute
           (Values.VerifyUserAttributeRequest.make ~accessToken
              ~attributeName ~code ())
           (Some Values.VerifyUserAttributeResponse.to_json)
           (Some Values.VerifyUserAttributeResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-custom-attributes", add_custom_attributes);
    ("admin-add-user-to-group", admin_add_user_to_group);
    ("admin-confirm-sign-up", admin_confirm_sign_up);
    ("admin-create-user", admin_create_user);
    ("admin-delete-user", admin_delete_user);
    ("admin-delete-user-attributes", admin_delete_user_attributes);
    ("admin-disable-provider-for-user", admin_disable_provider_for_user);
    ("admin-disable-user", admin_disable_user);
    ("admin-enable-user", admin_enable_user);
    ("admin-forget-device", admin_forget_device);
    ("admin-get-device", admin_get_device);
    ("admin-get-user", admin_get_user);
    ("admin-initiate-auth", admin_initiate_auth);
    ("admin-link-provider-for-user", admin_link_provider_for_user);
    ("admin-list-devices", admin_list_devices);
    ("admin-list-groups-for-user", admin_list_groups_for_user);
    ("admin-list-user-auth-events", admin_list_user_auth_events);
    ("admin-remove-user-from-group", admin_remove_user_from_group);
    ("admin-reset-user-password", admin_reset_user_password);
    ("admin-respond-to-auth-challenge", admin_respond_to_auth_challenge);
    ("admin-set-user-m-f-a-preference", admin_set_user_m_f_a_preference);
    ("admin-set-user-password", admin_set_user_password);
    ("admin-set-user-settings", admin_set_user_settings);
    ("admin-update-auth-event-feedback", admin_update_auth_event_feedback);
    ("admin-update-device-status", admin_update_device_status);
    ("admin-update-user-attributes", admin_update_user_attributes);
    ("admin-user-global-sign-out", admin_user_global_sign_out);
    ("associate-software-token", associate_software_token);
    ("change-password", change_password);
    ("confirm-device", confirm_device);
    ("confirm-forgot-password", confirm_forgot_password);
    ("confirm-sign-up", confirm_sign_up);
    ("create-group", create_group);
    ("create-identity-provider", create_identity_provider);
    ("create-resource-server", create_resource_server);
    ("create-user-import-job", create_user_import_job);
    ("create-user-pool", create_user_pool);
    ("create-user-pool-client", create_user_pool_client);
    ("create-user-pool-domain", create_user_pool_domain);
    ("delete-group", delete_group);
    ("delete-identity-provider", delete_identity_provider);
    ("delete-resource-server", delete_resource_server);
    ("delete-user", delete_user);
    ("delete-user-attributes", delete_user_attributes);
    ("delete-user-pool", delete_user_pool);
    ("delete-user-pool-client", delete_user_pool_client);
    ("delete-user-pool-domain", delete_user_pool_domain);
    ("describe-identity-provider", describe_identity_provider);
    ("describe-resource-server", describe_resource_server);
    ("describe-risk-configuration", describe_risk_configuration);
    ("describe-user-import-job", describe_user_import_job);
    ("describe-user-pool", describe_user_pool);
    ("describe-user-pool-client", describe_user_pool_client);
    ("describe-user-pool-domain", describe_user_pool_domain);
    ("forget-device", forget_device);
    ("forgot-password", forgot_password);
    ("get-c-s-v-header", get_c_s_v_header);
    ("get-device", get_device);
    ("get-group", get_group);
    ("get-identity-provider-by-identifier",
      get_identity_provider_by_identifier);
    ("get-signing-certificate", get_signing_certificate);
    ("get-u-i-customization", get_u_i_customization);
    ("get-user", get_user);
    ("get-user-attribute-verification-code",
      get_user_attribute_verification_code);
    ("get-user-pool-mfa-config", get_user_pool_mfa_config);
    ("global-sign-out", global_sign_out);
    ("initiate-auth", initiate_auth);
    ("list-devices", list_devices);
    ("list-groups", list_groups);
    ("list-identity-providers", list_identity_providers);
    ("list-resource-servers", list_resource_servers);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-user-import-jobs", list_user_import_jobs);
    ("list-user-pool-clients", list_user_pool_clients);
    ("list-user-pools", list_user_pools);
    ("list-users", list_users);
    ("list-users-in-group", list_users_in_group);
    ("resend-confirmation-code", resend_confirmation_code);
    ("respond-to-auth-challenge", respond_to_auth_challenge);
    ("revoke-token", revoke_token);
    ("set-risk-configuration", set_risk_configuration);
    ("set-u-i-customization", set_u_i_customization);
    ("set-user-m-f-a-preference", set_user_m_f_a_preference);
    ("set-user-pool-mfa-config", set_user_pool_mfa_config);
    ("set-user-settings", set_user_settings);
    ("sign-up", sign_up);
    ("start-user-import-job", start_user_import_job);
    ("stop-user-import-job", stop_user_import_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-auth-event-feedback", update_auth_event_feedback);
    ("update-device-status", update_device_status);
    ("update-group", update_group);
    ("update-identity-provider", update_identity_provider);
    ("update-resource-server", update_resource_server);
    ("update-user-attributes", update_user_attributes);
    ("update-user-pool", update_user_pool);
    ("update-user-pool-client", update_user_pool_client);
    ("update-user-pool-domain", update_user_pool_domain);
    ("verify-software-token", verify_software_token);
    ("verify-user-attribute", verify_user_attribute)]
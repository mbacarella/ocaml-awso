(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and users =
         flag "users" (required json_arg)
           ~doc:"JSON BatchCreateUserRequestItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_user
           (Values.BatchCreateUserRequest.make ?clientToken ~networkId
              ~users:(Values.BatchCreateUserRequestItems.of_json users) ())
           (Some Values.BatchCreateUserResponse.to_json)
           (Some Values.BatchCreateUserResponse.error_to_json)])
let batch_delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and userIds = flag "user-ids" (required json_arg) ~doc:"JSON UserIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_user
           (Values.BatchDeleteUserRequest.make ?clientToken ~networkId
              ~userIds:(Values.UserIds.of_json userIds) ())
           (Some Values.BatchDeleteUserResponse.to_json)
           (Some Values.BatchDeleteUserResponse.error_to_json)])
let batch_lookup_user_uname =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and unames = flag "unames" (required json_arg) ~doc:"JSON Unames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_lookup_user_uname
           (Values.BatchLookupUserUnameRequest.make ?clientToken ~networkId
              ~unames:(Values.Unames.of_json unames) ())
           (Some Values.BatchLookupUserUnameResponse.to_json)
           (Some Values.BatchLookupUserUnameResponse.error_to_json)])
let batch_reinvite_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and userIds = flag "user-ids" (required json_arg) ~doc:"JSON UserIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_reinvite_user
           (Values.BatchReinviteUserRequest.make ?clientToken ~networkId
              ~userIds:(Values.UserIds.of_json userIds) ())
           (Some Values.BatchReinviteUserResponse.to_json)
           (Some Values.BatchReinviteUserResponse.error_to_json)])
let batch_reset_devices_for_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId"
       and appIds = flag "app-ids" (required json_arg) ~doc:"JSON AppIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_reset_devices_for_user
           (Values.BatchResetDevicesForUserRequest.make ?clientToken
              ~networkId ~userId ~appIds:(Values.AppIds.of_json appIds) ())
           (Some Values.BatchResetDevicesForUserResponse.to_json)
           (Some Values.BatchResetDevicesForUserResponse.error_to_json)])
let batch_toggle_user_suspend_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and suspend = flag "suspend" (required bool) ~doc:"BOOL Boolean"
       and userIds = flag "user-ids" (required json_arg) ~doc:"JSON UserIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_toggle_user_suspend_status
           (Values.BatchToggleUserSuspendStatusRequest.make ?clientToken
              ~networkId ~suspend ~userIds:(Values.UserIds.of_json userIds)
              ()) (Some Values.BatchToggleUserSuspendStatusResponse.to_json)
           (Some Values.BatchToggleUserSuspendStatusResponse.error_to_json)])
let create_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING GenericString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and username =
         flag "username" (required string) ~doc:"STRING GenericString"
       and groupId =
         flag "group-id" (required string) ~doc:"STRING GenericString"
       and challenge =
         flag "challenge" (required string) ~doc:"STRING SensitiveString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bot
           (Values.CreateBotRequest.make ?displayName ~networkId ~username
              ~groupId ~challenge ()) (Some Values.CreateBotResponse.to_json)
           (Some Values.CreateBotResponse.error_to_json)])
let create_data_retention_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_retention_bot
           (Values.CreateDataRetentionBotRequest.make ~networkId ())
           (Some Values.CreateDataRetentionBotResponse.to_json)
           (Some Values.CreateDataRetentionBotResponse.error_to_json)])
let create_data_retention_bot_challenge =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_retention_bot_challenge
           (Values.CreateDataRetentionBotChallengeRequest.make ~networkId ())
           (Some Values.CreateDataRetentionBotChallengeResponse.to_json)
           (Some Values.CreateDataRetentionBotChallengeResponse.error_to_json)])
let create_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enablePremiumFreeTrial =
         flag "enable-premium-free-trial" (optional bool) ~doc:"BOOL Boolean"
       and encryptionKeyArn =
         flag "encryption-key-arn" (optional string)
           ~doc:"STRING GenericString"
       and networkName =
         flag "network-name" (required string) ~doc:"STRING GenericString"
       and accessLevel =
         flag "access-level" (required json_arg) ~doc:"JSON AccessLevel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_network
           (Values.CreateNetworkRequest.make ?enablePremiumFreeTrial
              ?encryptionKeyArn ~networkName
              ~accessLevel:(Values.AccessLevel.of_json accessLevel) ())
           (Some Values.CreateNetworkResponse.to_json)
           (Some Values.CreateNetworkResponse.error_to_json)])
let create_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and name = flag "name" (required string) ~doc:"STRING GenericString"
       and securityGroupSettings =
         flag "security-group-settings" (required json_arg)
           ~doc:"JSON SecurityGroupSettingsRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_security_group
           (Values.CreateSecurityGroupRequest.make ?clientToken ~networkId
              ~name
              ~securityGroupSettings:(Values.SecurityGroupSettingsRequest.of_json
                                        securityGroupSettings) ())
           (Some Values.CreateSecurityGroupResponse.to_json)
           (Some Values.CreateSecurityGroupResponse.error_to_json)])
let delete_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and botId = flag "bot-id" (required string) ~doc:"STRING BotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bot (Values.DeleteBotRequest.make ~networkId ~botId ())
           (Some Values.DeleteBotResponse.to_json)
           (Some Values.DeleteBotResponse.error_to_json)])
let delete_data_retention_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_retention_bot
           (Values.DeleteDataRetentionBotRequest.make ~networkId ())
           (Some Values.DeleteDataRetentionBotResponse.to_json)
           (Some Values.DeleteDataRetentionBotResponse.error_to_json)])
let delete_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network
           (Values.DeleteNetworkRequest.make ?clientToken ~networkId ())
           (Some Values.DeleteNetworkResponse.to_json)
           (Some Values.DeleteNetworkResponse.error_to_json)])
let delete_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and groupId =
         flag "group-id" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_security_group
           (Values.DeleteSecurityGroupRequest.make ~networkId ~groupId ())
           (Some Values.DeleteSecurityGroupResponse.to_json)
           (Some Values.DeleteSecurityGroupResponse.error_to_json)])
let get_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and botId = flag "bot-id" (required string) ~doc:"STRING BotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bot (Values.GetBotRequest.make ~networkId ~botId ())
           (Some Values.GetBotResponse.to_json)
           (Some Values.GetBotResponse.error_to_json)])
let get_bots_count =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bots_count (Values.GetBotsCountRequest.make ~networkId ())
           (Some Values.GetBotsCountResponse.to_json)
           (Some Values.GetBotsCountResponse.error_to_json)])
let get_data_retention_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_retention_bot
           (Values.GetDataRetentionBotRequest.make ~networkId ())
           (Some Values.GetDataRetentionBotResponse.to_json)
           (Some Values.GetDataRetentionBotResponse.error_to_json)])
let get_guest_user_history_count =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_guest_user_history_count
           (Values.GetGuestUserHistoryCountRequest.make ~networkId ())
           (Some Values.GetGuestUserHistoryCountResponse.to_json)
           (Some Values.GetGuestUserHistoryCountResponse.error_to_json)])
let get_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_network (Values.GetNetworkRequest.make ~networkId ())
           (Some Values.GetNetworkResponse.to_json)
           (Some Values.GetNetworkResponse.error_to_json)])
let get_network_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_network_settings
           (Values.GetNetworkSettingsRequest.make ~networkId ())
           (Some Values.GetNetworkSettingsResponse.to_json)
           (Some Values.GetNetworkSettingsResponse.error_to_json)])
let get_oidc_info =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientId =
         flag "client-id" (optional string) ~doc:"STRING GenericString"
       and code = flag "code" (optional string) ~doc:"STRING GenericString"
       and grantType =
         flag "grant-type" (optional string) ~doc:"STRING GenericString"
       and redirectUri =
         flag "redirect-uri" (optional string) ~doc:"STRING GenericString"
       and url = flag "url" (optional string) ~doc:"STRING GenericString"
       and clientSecret =
         flag "client-secret" (optional string) ~doc:"STRING SensitiveString"
       and codeVerifier =
         flag "code-verifier" (optional string) ~doc:"STRING GenericString"
       and certificate =
         flag "certificate" (optional string) ~doc:"STRING GenericString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_oidc_info
           (Values.GetOidcInfoRequest.make ?clientId ?code ?grantType
              ?redirectUri ?url ?clientSecret ?codeVerifier ?certificate
              ~networkId ()) (Some Values.GetOidcInfoResponse.to_json)
           (Some Values.GetOidcInfoResponse.error_to_json)])
let get_opentdf_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_opentdf_config
           (Values.GetOpentdfConfigRequest.make ~networkId ())
           (Some Values.GetOpentdfConfigResponse.to_json)
           (Some Values.GetOpentdfConfigResponse.error_to_json)])
let get_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and groupId =
         flag "group-id" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_security_group
           (Values.GetSecurityGroupRequest.make ~networkId ~groupId ())
           (Some Values.GetSecurityGroupResponse.to_json)
           (Some Values.GetSecurityGroupResponse.error_to_json)])
let get_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTime =
         flag "start-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_epoch_seconds"
       and endTime =
         flag "end-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_epoch_seconds"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user
           (Values.GetUserRequest.make
              ?startTime:(Option.map
                            ~f:Values.SyntheticTimestamp_epoch_seconds.of_json
                            startTime)
              ?endTime:(Option.map
                          ~f:Values.SyntheticTimestamp_epoch_seconds.of_json
                          endTime) ~networkId ~userId ())
           (Some Values.GetUserResponse.to_json)
           (Some Values.GetUserResponse.error_to_json)])
let get_users_count =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_users_count
           (Values.GetUsersCountRequest.make ~networkId ())
           (Some Values.GetUsersCountResponse.to_json)
           (Some Values.GetUsersCountResponse.error_to_json)])
let list_blocked_guest_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and sortDirection =
         flag "sort-direction" (optional json_arg) ~doc:"JSON SortDirection"
       and sortFields =
         flag "sort-fields" (optional string) ~doc:"STRING GenericString"
       and username =
         flag "username" (optional string) ~doc:"STRING GenericString"
       and admin = flag "admin" (optional string) ~doc:"STRING GenericString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_blocked_guest_users
           (Values.ListBlockedGuestUsersRequest.make ?maxResults
              ?sortDirection:(Option.map ~f:Values.SortDirection.of_json
                                sortDirection) ?sortFields ?username ?admin
              ?nextToken ~networkId ())
           (Some Values.ListBlockedGuestUsersResponse.to_json)
           (Some Values.ListBlockedGuestUsersResponse.error_to_json)])
let list_bots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and sortFields =
         flag "sort-fields" (optional string) ~doc:"STRING GenericString"
       and sortDirection =
         flag "sort-direction" (optional json_arg) ~doc:"JSON SortDirection"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING GenericString"
       and username =
         flag "username" (optional string) ~doc:"STRING GenericString"
       and status = flag "status" (optional int) ~doc:"INT BotStatus"
       and groupId =
         flag "group-id" (optional string) ~doc:"STRING GenericString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bots
           (Values.ListBotsRequest.make ?nextToken ?maxResults ?sortFields
              ?sortDirection:(Option.map ~f:Values.SortDirection.of_json
                                sortDirection) ?displayName ?username ?status
              ?groupId ~networkId ()) (Some Values.ListBotsResponse.to_json)
           (Some Values.ListBotsResponse.error_to_json)])
let list_devices_for_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and sortFields =
         flag "sort-fields" (optional string) ~doc:"STRING GenericString"
       and sortDirection =
         flag "sort-direction" (optional json_arg) ~doc:"JSON SortDirection"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_devices_for_user
           (Values.ListDevicesForUserRequest.make ?nextToken ?maxResults
              ?sortFields
              ?sortDirection:(Option.map ~f:Values.SortDirection.of_json
                                sortDirection) ~networkId ~userId ())
           (Some Values.ListDevicesForUserResponse.to_json)
           (Some Values.ListDevicesForUserResponse.error_to_json)])
let list_guest_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and sortDirection =
         flag "sort-direction" (optional json_arg) ~doc:"JSON SortDirection"
       and sortFields =
         flag "sort-fields" (optional string) ~doc:"STRING GenericString"
       and username =
         flag "username" (optional string) ~doc:"STRING GenericString"
       and billingPeriod =
         flag "billing-period" (optional string) ~doc:"STRING GenericString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_guest_users
           (Values.ListGuestUsersRequest.make ?maxResults
              ?sortDirection:(Option.map ~f:Values.SortDirection.of_json
                                sortDirection) ?sortFields ?username
              ?billingPeriod ?nextToken ~networkId ())
           (Some Values.ListGuestUsersResponse.to_json)
           (Some Values.ListGuestUsersResponse.error_to_json)])
let list_networks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and sortFields =
         flag "sort-fields" (optional string) ~doc:"STRING GenericString"
       and sortDirection =
         flag "sort-direction" (optional json_arg) ~doc:"JSON SortDirection"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_networks
           (Values.ListNetworksRequest.make ?maxResults ?sortFields
              ?sortDirection:(Option.map ~f:Values.SortDirection.of_json
                                sortDirection) ?nextToken ())
           (Some Values.ListNetworksResponse.to_json)
           (Some Values.ListNetworksResponse.error_to_json)])
let list_security_group_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and sortFields =
         flag "sort-fields" (optional string) ~doc:"STRING GenericString"
       and sortDirection =
         flag "sort-direction" (optional json_arg) ~doc:"JSON SortDirection"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and groupId =
         flag "group-id" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_security_group_users
           (Values.ListSecurityGroupUsersRequest.make ?nextToken ?maxResults
              ?sortFields
              ?sortDirection:(Option.map ~f:Values.SortDirection.of_json
                                sortDirection) ~networkId ~groupId ())
           (Some Values.ListSecurityGroupUsersResponse.to_json)
           (Some Values.ListSecurityGroupUsersResponse.error_to_json)])
let list_security_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and sortFields =
         flag "sort-fields" (optional string) ~doc:"STRING GenericString"
       and sortDirection =
         flag "sort-direction" (optional json_arg) ~doc:"JSON SortDirection"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_security_groups
           (Values.ListSecurityGroupsRequest.make ?nextToken ?maxResults
              ?sortFields
              ?sortDirection:(Option.map ~f:Values.SortDirection.of_json
                                sortDirection) ~networkId ())
           (Some Values.ListSecurityGroupsResponse.to_json)
           (Some Values.ListSecurityGroupsResponse.error_to_json)])
let list_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and sortFields =
         flag "sort-fields" (optional string) ~doc:"STRING GenericString"
       and sortDirection =
         flag "sort-direction" (optional json_arg) ~doc:"JSON SortDirection"
       and firstName =
         flag "first-name" (optional string) ~doc:"STRING SensitiveString"
       and lastName =
         flag "last-name" (optional string) ~doc:"STRING SensitiveString"
       and username =
         flag "username" (optional string) ~doc:"STRING GenericString"
       and status = flag "status" (optional int) ~doc:"INT UserStatus"
       and groupId =
         flag "group-id" (optional string) ~doc:"STRING GenericString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_users
           (Values.ListUsersRequest.make ?nextToken ?maxResults ?sortFields
              ?sortDirection:(Option.map ~f:Values.SortDirection.of_json
                                sortDirection) ?firstName ?lastName ?username
              ?status ?groupId ~networkId ())
           (Some Values.ListUsersResponse.to_json)
           (Some Values.ListUsersResponse.error_to_json)])
let register_oidc_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customUsername =
         flag "custom-username" (optional string) ~doc:"STRING GenericString"
       and extraAuthParams =
         flag "extra-auth-params" (optional string)
           ~doc:"STRING GenericString"
       and secret =
         flag "secret" (optional string) ~doc:"STRING SensitiveString"
       and ssoTokenBufferMinutes =
         flag "sso-token-buffer-minutes" (optional int) ~doc:"INT Integer"
       and userId =
         flag "user-id" (optional string) ~doc:"STRING GenericString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and companyId =
         flag "company-id" (required string) ~doc:"STRING GenericString"
       and issuer =
         flag "issuer" (required string) ~doc:"STRING GenericString"
       and scopes =
         flag "scopes" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_oidc_config
           (Values.RegisterOidcConfigRequest.make ?customUsername
              ?extraAuthParams ?secret ?ssoTokenBufferMinutes ?userId
              ~networkId ~companyId ~issuer ~scopes ())
           (Some Values.RegisterOidcConfigResponse.to_json)
           (Some Values.RegisterOidcConfigResponse.error_to_json)])
let register_oidc_config_test =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and extraAuthParams =
         flag "extra-auth-params" (optional string)
           ~doc:"STRING GenericString"
       and certificate =
         flag "certificate" (optional string) ~doc:"STRING GenericString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and issuer =
         flag "issuer" (required string) ~doc:"STRING GenericString"
       and scopes =
         flag "scopes" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_oidc_config_test
           (Values.RegisterOidcConfigTestRequest.make ?extraAuthParams
              ?certificate ~networkId ~issuer ~scopes ())
           (Some Values.RegisterOidcConfigTestResponse.to_json)
           (Some Values.RegisterOidcConfigTestResponse.error_to_json)])
let register_opentdf_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING GenericString"
       and clientSecret =
         flag "client-secret" (required string) ~doc:"STRING SensitiveString"
       and domain =
         flag "domain" (required string) ~doc:"STRING GenericString"
       and provider =
         flag "provider" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_opentdf_config
           (Values.RegisterOpentdfConfigRequest.make ?dryRun ~networkId
              ~clientId ~clientSecret ~domain ~provider ())
           (Some Values.RegisterOpentdfConfigResponse.to_json)
           (Some Values.RegisterOpentdfConfigResponse.error_to_json)])
let update_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING GenericString"
       and groupId =
         flag "group-id" (optional string) ~doc:"STRING GenericString"
       and challenge =
         flag "challenge" (optional string) ~doc:"STRING SensitiveString"
       and suspend = flag "suspend" (optional bool) ~doc:"BOOL Boolean"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and botId = flag "bot-id" (required string) ~doc:"STRING BotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bot
           (Values.UpdateBotRequest.make ?displayName ?groupId ?challenge
              ?suspend ~networkId ~botId ())
           (Some Values.UpdateBotResponse.to_json)
           (Some Values.UpdateBotResponse.error_to_json)])
let update_data_retention =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and actionType =
         flag "action-type" (required json_arg)
           ~doc:"JSON DataRetentionActionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_retention
           (Values.UpdateDataRetentionRequest.make ~networkId
              ~actionType:(Values.DataRetentionActionType.of_json actionType)
              ()) (Some Values.UpdateDataRetentionResponse.to_json)
           (Some Values.UpdateDataRetentionResponse.error_to_json)])
let update_guest_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and usernameHash =
         flag "username-hash" (required string) ~doc:"STRING GenericString"
       and block = flag "block" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_guest_user
           (Values.UpdateGuestUserRequest.make ~networkId ~usernameHash
              ~block ()) (Some Values.UpdateGuestUserResponse.to_json)
           (Some Values.UpdateGuestUserResponse.error_to_json)])
let update_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and encryptionKeyArn =
         flag "encryption-key-arn" (optional string)
           ~doc:"STRING GenericString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and networkName =
         flag "network-name" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_network
           (Values.UpdateNetworkRequest.make ?clientToken ?encryptionKeyArn
              ~networkId ~networkName ())
           (Some Values.UpdateNetworkResponse.to_json)
           (Some Values.UpdateNetworkResponse.error_to_json)])
let update_network_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and settings =
         flag "settings" (required json_arg) ~doc:"JSON NetworkSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_network_settings
           (Values.UpdateNetworkSettingsRequest.make ~networkId
              ~settings:(Values.NetworkSettings.of_json settings) ())
           (Some Values.UpdateNetworkSettingsResponse.to_json)
           (Some Values.UpdateNetworkSettingsResponse.error_to_json)])
let update_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING GenericString"
       and securityGroupSettings =
         flag "security-group-settings" (optional json_arg)
           ~doc:"JSON SecurityGroupSettings"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and groupId =
         flag "group-id" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_security_group
           (Values.UpdateSecurityGroupRequest.make ?name
              ?securityGroupSettings:(Option.map
                                        ~f:Values.SecurityGroupSettings.of_json
                                        securityGroupSettings) ~networkId
              ~groupId ()) (Some Values.UpdateSecurityGroupResponse.to_json)
           (Some Values.UpdateSecurityGroupResponse.error_to_json)])
let update_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userDetails =
         flag "user-details" (optional json_arg)
           ~doc:"JSON UpdateUserDetails"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING NetworkId"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user
           (Values.UpdateUserRequest.make
              ?userDetails:(Option.map ~f:Values.UpdateUserDetails.of_json
                              userDetails) ~networkId ~userId ())
           (Some Values.UpdateUserResponse.to_json)
           (Some Values.UpdateUserResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-create-user", batch_create_user);
    ("batch-delete-user", batch_delete_user);
    ("batch-lookup-user-uname", batch_lookup_user_uname);
    ("batch-reinvite-user", batch_reinvite_user);
    ("batch-reset-devices-for-user", batch_reset_devices_for_user);
    ("batch-toggle-user-suspend-status", batch_toggle_user_suspend_status);
    ("create-bot", create_bot);
    ("create-data-retention-bot", create_data_retention_bot);
    ("create-data-retention-bot-challenge",
      create_data_retention_bot_challenge);
    ("create-network", create_network);
    ("create-security-group", create_security_group);
    ("delete-bot", delete_bot);
    ("delete-data-retention-bot", delete_data_retention_bot);
    ("delete-network", delete_network);
    ("delete-security-group", delete_security_group);
    ("get-bot", get_bot);
    ("get-bots-count", get_bots_count);
    ("get-data-retention-bot", get_data_retention_bot);
    ("get-guest-user-history-count", get_guest_user_history_count);
    ("get-network", get_network);
    ("get-network-settings", get_network_settings);
    ("get-oidc-info", get_oidc_info);
    ("get-opentdf-config", get_opentdf_config);
    ("get-security-group", get_security_group);
    ("get-user", get_user);
    ("get-users-count", get_users_count);
    ("list-blocked-guest-users", list_blocked_guest_users);
    ("list-bots", list_bots);
    ("list-devices-for-user", list_devices_for_user);
    ("list-guest-users", list_guest_users);
    ("list-networks", list_networks);
    ("list-security-group-users", list_security_group_users);
    ("list-security-groups", list_security_groups);
    ("list-users", list_users);
    ("register-oidc-config", register_oidc_config);
    ("register-oidc-config-test", register_oidc_config_test);
    ("register-opentdf-config", register_opentdf_config);
    ("update-bot", update_bot);
    ("update-data-retention", update_data_retention);
    ("update-guest-user", update_guest_user);
    ("update-network", update_network);
    ("update-network-settings", update_network_settings);
    ("update-security-group", update_security_group);
    ("update-user", update_user)]
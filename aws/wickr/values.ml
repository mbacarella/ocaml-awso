(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.wickr
let apiVersion = "2024-02-01"
let endpointPrefix = "admin.wickr"
let serviceFullName = "AWS Wickr Admin API"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module GenericString =
  struct
    type nonrec t = string
    let context_ = "GenericString"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[\\S\\s]*"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GenericString" j
    let to_json = simple_to_json to_value
  end
module NetworkId =
  struct
    type nonrec t = string
    let context_ = "NetworkId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:8) >>=
             (fun () ->
                (check_string_max i ~max:8) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9]{8}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NetworkId" j
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module PermittedWickrEnterpriseNetwork =
  struct
    type nonrec t =
      {
      domain: GenericString.t
        [@ocaml.doc
          "The domain identifier for the permitted Wickr enterprise network."];
      networkId: NetworkId.t
        [@ocaml.doc
          "The network ID of the permitted Wickr enterprise network."]}
    let context_ = "PermittedWickrEnterpriseNetwork"
    let make ~domain = fun ~networkId -> fun () -> { domain; networkId }
    let to_value x =
      structure_to_value
        [("domain", (Some (GenericString.to_value x.domain)));
        ("networkId", (Some (NetworkId.to_value x.networkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      let domain =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "domain") in
      make ~networkId ~domain ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      let domain = field_map_exn json__ "domain" GenericString.of_json in
      make ~networkId ~domain ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Identifies a Wickr enterprise network that is permitted for global federation, allowing users to communicate with members of the specified network."]
module WickrAwsNetworks =
  struct
    type nonrec t =
      {
      region: GenericString.t
        [@ocaml.doc
          "The Amazon Web Services region identifier where the network is hosted (e.g., 'us-east-1')."];
      networkId: NetworkId.t
        [@ocaml.doc
          "The network ID of the Wickr Amazon Web Services network."]}
    let context_ = "WickrAwsNetworks"
    let make ~region = fun ~networkId -> fun () -> { region; networkId }
    let to_value x =
      structure_to_value
        [("region", (Some (GenericString.to_value x.region)));
        ("networkId", (Some (NetworkId.to_value x.networkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      let region =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "region") in
      make ~networkId ~region ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      let region = field_map_exn json__ "region" GenericString.of_json in
      make ~networkId ~region ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Identifies a Amazon Web Services Wickr network by region and network ID, used for configuring permitted networks for global federation."]
module SecurityGroupId =
  struct
    type nonrec t = string
    let context_ = "SecurityGroupId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[\\S]+"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SecurityGroupId" j
    let to_json = simple_to_json to_value
  end
module CallingSettings =
  struct
    type nonrec t =
      {
      canStart11Call: Boolean.t option
        [@ocaml.doc "Specifies whether users can start one-to-one calls."];
      canVideoCall: Boolean.t option
        [@ocaml.doc
          "Specifies whether users can make video calls (as opposed to audio-only calls). Valid only when audio call(canStart11Call) is enabled."];
      forceTcpCall: Boolean.t option
        [@ocaml.doc
          "When enabled, forces all calls to use TCP protocol instead of UDP for network traversal."]}
    let make ?canStart11Call =
      fun ?canVideoCall ->
        fun ?forceTcpCall ->
          fun () -> { canStart11Call; canVideoCall; forceTcpCall }
    let to_value x =
      structure_to_value
        [("canStart11Call",
           (Option.map x.canStart11Call ~f:Boolean.to_value));
        ("canVideoCall", (Option.map x.canVideoCall ~f:Boolean.to_value));
        ("forceTcpCall", (Option.map x.forceTcpCall ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let forceTcpCall =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "forceTcpCall") in
      let canVideoCall =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "canVideoCall") in
      let canStart11Call =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "canStart11Call") in
      make ?forceTcpCall ?canVideoCall ?canStart11Call ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let forceTcpCall = field_map json__ "forceTcpCall" Boolean.of_json in
      let canVideoCall = field_map json__ "canVideoCall" Boolean.of_json in
      let canStart11Call = field_map json__ "canStart11Call" Boolean.of_json in
      make ?forceTcpCall ?canVideoCall ?canStart11Call ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines the calling feature permissions and settings for users in a security group, controlling what types of calls users can initiate and participate in."]
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module PasswordRequirements =
  struct
    type nonrec t =
      {
      lowercase: Integer.t option
        [@ocaml.doc
          "The minimum number of lowercase letters required in passwords."];
      minLength: Integer.t option
        [@ocaml.doc "The minimum password length in characters."];
      numbers: Integer.t option
        [@ocaml.doc
          "The minimum number of numeric characters required in passwords."];
      symbols: Integer.t option
        [@ocaml.doc
          "The minimum number of special symbol characters required in passwords."];
      uppercase: Integer.t option
        [@ocaml.doc
          "The minimum number of uppercase letters required in passwords."]}
    let make ?lowercase =
      fun ?minLength ->
        fun ?numbers ->
          fun ?symbols ->
            fun ?uppercase ->
              fun () -> { lowercase; minLength; numbers; symbols; uppercase }
    let to_value x =
      structure_to_value
        [("lowercase", (Option.map x.lowercase ~f:Integer.to_value));
        ("minLength", (Option.map x.minLength ~f:Integer.to_value));
        ("numbers", (Option.map x.numbers ~f:Integer.to_value));
        ("symbols", (Option.map x.symbols ~f:Integer.to_value));
        ("uppercase", (Option.map x.uppercase ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uppercase =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "uppercase") in
      let symbols =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "symbols") in
      let numbers =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "numbers") in
      let minLength =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "minLength") in
      let lowercase =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "lowercase") in
      make ?uppercase ?symbols ?numbers ?minLength ?lowercase ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uppercase = field_map json__ "uppercase" Integer.of_json in
      let symbols = field_map json__ "symbols" Integer.of_json in
      let numbers = field_map json__ "numbers" Integer.of_json in
      let minLength = field_map json__ "minLength" Integer.of_json in
      let lowercase = field_map json__ "lowercase" Integer.of_json in
      make ?uppercase ?symbols ?numbers ?minLength ?lowercase ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines password complexity requirements for users in a security group, including minimum length and character type requirements."]
module PermittedNetworksList =
  struct
    type nonrec t = NetworkId.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NetworkId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NetworkId.of_xml)
    let of_json j =
      list_of_json ~kind:"PermittedNetworksList" ~of_json:NetworkId.of_json j
    let to_json v = composed_to_json to_value v
  end
module PermittedWickrEnterpriseNetworksList =
  struct
    type nonrec t = PermittedWickrEnterpriseNetwork.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PermittedWickrEnterpriseNetwork.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PermittedWickrEnterpriseNetwork.of_xml)
    let of_json j =
      list_of_json ~kind:"PermittedWickrEnterpriseNetworksList"
        ~of_json:PermittedWickrEnterpriseNetwork.of_json j
    let to_json v = composed_to_json to_value v
  end
module SecurityGroupStringList =
  struct
    type nonrec t = GenericString.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GenericString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GenericString.of_xml)
    let of_json j =
      list_of_json ~kind:"SecurityGroupStringList"
        ~of_json:GenericString.of_json j
    let to_json v = composed_to_json to_value v
  end
module ShredderSettings =
  struct
    type nonrec t =
      {
      canProcessManually: Boolean.t option
        [@ocaml.doc
          "Specifies whether users can manually trigger the shredder to delete content."];
      intensity: Integer.t option
        [@ocaml.doc
          "Prevents Wickr data from being recovered by overwriting deleted Wickr data. Valid Values: Must be one of \\[0, 20, 60, 100\\]"]}
    let make ?canProcessManually =
      fun ?intensity -> fun () -> { canProcessManually; intensity }
    let to_value x =
      structure_to_value
        [("canProcessManually",
           (Option.map x.canProcessManually ~f:Boolean.to_value));
        ("intensity", (Option.map x.intensity ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let intensity =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "intensity") in
      let canProcessManually =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "canProcessManually") in
      make ?intensity ?canProcessManually ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let intensity = field_map json__ "intensity" Integer.of_json in
      let canProcessManually =
        field_map json__ "canProcessManually" Boolean.of_json in
      make ?intensity ?canProcessManually ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration for the message shredder feature, which securely deletes messages and files from devices to prevent data recovery."]
module WickrAwsNetworksList =
  struct
    type nonrec t = WickrAwsNetworks.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:WickrAwsNetworks.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:WickrAwsNetworks.of_xml)
    let of_json j =
      list_of_json ~kind:"WickrAwsNetworksList"
        ~of_json:WickrAwsNetworks.of_json j
    let to_json v = composed_to_json to_value v
  end
module ErrorDetail =
  struct
    type nonrec t =
      {
      field: GenericString.t option
        [@ocaml.doc
          "The name of the field that contains an error or warning."];
      reason: GenericString.t option
        [@ocaml.doc "A detailed description of the error or warning."]}
    let make ?field = fun ?reason -> fun () -> { field; reason }
    let to_value x =
      structure_to_value
        [("field", (Option.map x.field ~f:GenericString.to_value));
        ("reason", (Option.map x.reason ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "reason") in
      let field =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "field") in
      make ?reason ?field ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reason = field_map json__ "reason" GenericString.of_json in
      let field = field_map json__ "field" GenericString.of_json in
      make ?reason ?field ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains detailed error information explaining why an operation failed, including which field caused the error and the reason for the failure."]
module Status =
  struct
    type nonrec t =
      | DISABLED 
      | ENABLED 
      | FORCE_ENABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DISABLED -> "DISABLED"
      | ENABLED -> "ENABLED"
      | FORCE_ENABLED -> "FORCE_ENABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "DISABLED" -> DISABLED
      | "ENABLED" -> ENABLED
      | "FORCE_ENABLED" -> FORCE_ENABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Status" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Status" j)
    let to_json = simple_to_json to_value
  end
module SecurityGroupIdList =
  struct
    type nonrec t = SecurityGroupId.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SecurityGroupId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SecurityGroupId.of_xml)
    let of_json j =
      list_of_json ~kind:"SecurityGroupIdList"
        ~of_json:SecurityGroupId.of_json j
    let to_json v = composed_to_json to_value v
  end
module SensitiveString =
  struct
    type nonrec t = string
    let context_ = "SensitiveString"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[\\S\\s]*"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SensitiveString" j
    let to_json = simple_to_json to_value
  end
module UserId =
  struct
    type nonrec t = string
    let context_ = "UserId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:10) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"UserId" j
    let to_json = simple_to_json to_value
  end
module SecurityGroupSettings =
  struct
    type nonrec t =
      {
      alwaysReauthenticate: Boolean.t option
        [@ocaml.doc
          "Requires users to reauthenticate every time they return to the application, providing an additional layer of security."];
      atakPackageValues: SecurityGroupStringList.t option
        [@ocaml.doc
          "Configuration values for ATAK (Android Team Awareness Kit) package integration, when ATAK is enabled."];
      calling: CallingSettings.t option
        [@ocaml.doc
          "The calling feature permissions and settings that control what types of calls users can initiate and participate in."];
      checkForUpdates: Boolean.t option
        [@ocaml.doc
          "Enables automatic checking for Wickr client updates to ensure users stay current with the latest version."];
      enableAtak: Boolean.t option
        [@ocaml.doc
          "Enables ATAK (Android Team Awareness Kit) integration for tactical communication and situational awareness."];
      enableCrashReports: Boolean.t option
        [@ocaml.doc "Allow users to report crashes."];
      enableFileDownload: Boolean.t option
        [@ocaml.doc
          "Specifies whether users can download files from messages to their devices."];
      enableGuestFederation: Boolean.t option
        [@ocaml.doc
          "Allows users to communicate with guest users from other Wickr networks and federated external networks."];
      enableNotificationPreview: Boolean.t option
        [@ocaml.doc
          "Enables message preview text in push notifications, allowing users to see message content before opening the app."];
      enableOpenAccessOption: Boolean.t option
        [@ocaml.doc
          "Allow users to avoid censorship when they are geo-blocked or have network limitations."];
      enableRestrictedGlobalFederation: Boolean.t option
        [@ocaml.doc
          "Enables restricted global federation, limiting external communication to only specified permitted networks."];
      filesEnabled: Boolean.t option
        [@ocaml.doc
          "Enables file sharing capabilities, allowing users to send and receive files in conversations."];
      forceDeviceLockout: Integer.t option
        [@ocaml.doc
          "Defines the number of failed login attempts before data stored on the device is reset. Should be less than lockoutThreshold."];
      forceOpenAccess: Boolean.t option
        [@ocaml.doc
          "Automatically enable and enforce Wickr open access on all devices. Valid only if enableOpenAccessOption settings is enabled."];
      forceReadReceipts: Boolean.t option
        [@ocaml.doc
          "Allow user approved bots to read messages in rooms without using a slash command."];
      globalFederation: Boolean.t option
        [@ocaml.doc
          "Allows users to communicate with users on other Wickr instances (Wickr Enterprise) outside the current network."];
      isAtoEnabled: Boolean.t option
        [@ocaml.doc
          "Enforces a two-factor authentication when a user adds a new device to their account."];
      isLinkPreviewEnabled: Boolean.t option
        [@ocaml.doc
          "Enables automatic preview of links shared in messages, showing webpage thumbnails and descriptions."];
      locationAllowMaps: Boolean.t option
        [@ocaml.doc
          "Allows map integration in location sharing, enabling users to view shared locations on interactive maps. Only allowed when location setting is enabled."];
      locationEnabled: Boolean.t option
        [@ocaml.doc
          "Enables location sharing features, allowing users to share their current location with others."];
      maxAutoDownloadSize: Long.t option
        [@ocaml.doc
          "The maximum file size in bytes that will be automatically downloaded without user confirmation. Only allowed if fileDownload is enabled. Valid Values \\[512000 (low_quality), 7340032 (high_quality) \\]"];
      maxBor: Integer.t option
        [@ocaml.doc
          "The maximum burn-on-read (BOR) time in seconds, which determines how long messages remain visible before auto-deletion after being read."];
      maxTtl: Long.t option
        [@ocaml.doc
          "The maximum time-to-live (TTL) in seconds for messages, after which they will be automatically deleted from all devices."];
      messageForwardingEnabled: Boolean.t option
        [@ocaml.doc
          "Enables message forwarding, allowing users to forward messages from one conversation to another."];
      passwordRequirements: PasswordRequirements.t option
        [@ocaml.doc
          "The password complexity requirements that users must follow when creating or changing passwords."];
      presenceEnabled: Boolean.t option
        [@ocaml.doc
          "Enables presence indicators that show whether users are online, away, or offline."];
      quickResponses: SecurityGroupStringList.t option
        [@ocaml.doc
          "A list of pre-defined quick response message templates that users can send with a single tap."];
      showMasterRecoveryKey: Boolean.t option
        [@ocaml.doc
          "Users will get a master recovery key that can be used to securely sign in to their Wickr account without having access to their primary device for authentication. Available in SSO enabled network."];
      shredder: ShredderSettings.t option
        [@ocaml.doc
          "The message shredder configuration that controls secure deletion of messages and files from devices."];
      ssoMaxIdleMinutes: Integer.t option
        [@ocaml.doc
          "The duration for which users SSO session remains inactive before automatically logging them out for security. Available in SSO enabled network."];
      federationMode: Integer.t option
        [@ocaml.doc
          "The local federation mode controlling how users can communicate with other networks. Values: 0 (none), 1 (federated), 2 (restricted)."];
      lockoutThreshold: Integer.t option
        [@ocaml.doc
          "The number of failed password attempts before a user account is locked out."];
      permittedNetworks: PermittedNetworksList.t option
        [@ocaml.doc
          "A list of network IDs that are permitted for local federation when federation mode is set to restricted."];
      permittedWickrAwsNetworks: WickrAwsNetworksList.t option
        [@ocaml.doc
          "A list of permitted Wickr networks for global federation, restricting communication to specific approved networks."];
      permittedWickrEnterpriseNetworks:
        PermittedWickrEnterpriseNetworksList.t option
        [@ocaml.doc
          "A list of permitted Wickr Enterprise networks for global federation, restricting communication to specific approved networks."]}
    let make ?alwaysReauthenticate =
      fun ?atakPackageValues ->
        fun ?calling ->
          fun ?checkForUpdates ->
            fun ?enableAtak ->
              fun ?enableCrashReports ->
                fun ?enableFileDownload ->
                  fun ?enableGuestFederation ->
                    fun ?enableNotificationPreview ->
                      fun ?enableOpenAccessOption ->
                        fun ?enableRestrictedGlobalFederation ->
                          fun ?filesEnabled ->
                            fun ?forceDeviceLockout ->
                              fun ?forceOpenAccess ->
                                fun ?forceReadReceipts ->
                                  fun ?globalFederation ->
                                    fun ?isAtoEnabled ->
                                      fun ?isLinkPreviewEnabled ->
                                        fun ?locationAllowMaps ->
                                          fun ?locationEnabled ->
                                            fun ?maxAutoDownloadSize ->
                                              fun ?maxBor ->
                                                fun ?maxTtl ->
                                                  fun
                                                    ?messageForwardingEnabled
                                                    ->
                                                    fun ?passwordRequirements
                                                      ->
                                                      fun ?presenceEnabled ->
                                                        fun ?quickResponses
                                                          ->
                                                          fun
                                                            ?showMasterRecoveryKey
                                                            ->
                                                            fun ?shredder ->
                                                              fun
                                                                ?ssoMaxIdleMinutes
                                                                ->
                                                                fun
                                                                  ?federationMode
                                                                  ->
                                                                  fun
                                                                    ?lockoutThreshold
                                                                    ->
                                                                    fun
                                                                    ?permittedNetworks
                                                                    ->
                                                                    fun
                                                                    ?permittedWickrAwsNetworks
                                                                    ->
                                                                    fun
                                                                    ?permittedWickrEnterpriseNetworks
                                                                    ->
                                                                    fun () ->
                                                                    {
                                                                    alwaysReauthenticate;
                                                                    atakPackageValues;
                                                                    calling;
                                                                    checkForUpdates;
                                                                    enableAtak;
                                                                    enableCrashReports;
                                                                    enableFileDownload;
                                                                    enableGuestFederation;
                                                                    enableNotificationPreview;
                                                                    enableOpenAccessOption;
                                                                    enableRestrictedGlobalFederation;
                                                                    filesEnabled;
                                                                    forceDeviceLockout;
                                                                    forceOpenAccess;
                                                                    forceReadReceipts;
                                                                    globalFederation;
                                                                    isAtoEnabled;
                                                                    isLinkPreviewEnabled;
                                                                    locationAllowMaps;
                                                                    locationEnabled;
                                                                    maxAutoDownloadSize;
                                                                    maxBor;
                                                                    maxTtl;
                                                                    messageForwardingEnabled;
                                                                    passwordRequirements;
                                                                    presenceEnabled;
                                                                    quickResponses;
                                                                    showMasterRecoveryKey;
                                                                    shredder;
                                                                    ssoMaxIdleMinutes;
                                                                    federationMode;
                                                                    lockoutThreshold;
                                                                    permittedNetworks;
                                                                    permittedWickrAwsNetworks;
                                                                    permittedWickrEnterpriseNetworks
                                                                    }
    let to_value x =
      structure_to_value
        [("alwaysReauthenticate",
           (Option.map x.alwaysReauthenticate ~f:Boolean.to_value));
        ("atakPackageValues",
          (Option.map x.atakPackageValues ~f:SecurityGroupStringList.to_value));
        ("calling", (Option.map x.calling ~f:CallingSettings.to_value));
        ("checkForUpdates",
          (Option.map x.checkForUpdates ~f:Boolean.to_value));
        ("enableAtak", (Option.map x.enableAtak ~f:Boolean.to_value));
        ("enableCrashReports",
          (Option.map x.enableCrashReports ~f:Boolean.to_value));
        ("enableFileDownload",
          (Option.map x.enableFileDownload ~f:Boolean.to_value));
        ("enableGuestFederation",
          (Option.map x.enableGuestFederation ~f:Boolean.to_value));
        ("enableNotificationPreview",
          (Option.map x.enableNotificationPreview ~f:Boolean.to_value));
        ("enableOpenAccessOption",
          (Option.map x.enableOpenAccessOption ~f:Boolean.to_value));
        ("enableRestrictedGlobalFederation",
          (Option.map x.enableRestrictedGlobalFederation ~f:Boolean.to_value));
        ("filesEnabled", (Option.map x.filesEnabled ~f:Boolean.to_value));
        ("forceDeviceLockout",
          (Option.map x.forceDeviceLockout ~f:Integer.to_value));
        ("forceOpenAccess",
          (Option.map x.forceOpenAccess ~f:Boolean.to_value));
        ("forceReadReceipts",
          (Option.map x.forceReadReceipts ~f:Boolean.to_value));
        ("globalFederation",
          (Option.map x.globalFederation ~f:Boolean.to_value));
        ("isAtoEnabled", (Option.map x.isAtoEnabled ~f:Boolean.to_value));
        ("isLinkPreviewEnabled",
          (Option.map x.isLinkPreviewEnabled ~f:Boolean.to_value));
        ("locationAllowMaps",
          (Option.map x.locationAllowMaps ~f:Boolean.to_value));
        ("locationEnabled",
          (Option.map x.locationEnabled ~f:Boolean.to_value));
        ("maxAutoDownloadSize",
          (Option.map x.maxAutoDownloadSize ~f:Long.to_value));
        ("maxBor", (Option.map x.maxBor ~f:Integer.to_value));
        ("maxTtl", (Option.map x.maxTtl ~f:Long.to_value));
        ("messageForwardingEnabled",
          (Option.map x.messageForwardingEnabled ~f:Boolean.to_value));
        ("passwordRequirements",
          (Option.map x.passwordRequirements ~f:PasswordRequirements.to_value));
        ("presenceEnabled",
          (Option.map x.presenceEnabled ~f:Boolean.to_value));
        ("quickResponses",
          (Option.map x.quickResponses ~f:SecurityGroupStringList.to_value));
        ("showMasterRecoveryKey",
          (Option.map x.showMasterRecoveryKey ~f:Boolean.to_value));
        ("shredder", (Option.map x.shredder ~f:ShredderSettings.to_value));
        ("ssoMaxIdleMinutes",
          (Option.map x.ssoMaxIdleMinutes ~f:Integer.to_value));
        ("federationMode", (Option.map x.federationMode ~f:Integer.to_value));
        ("lockoutThreshold",
          (Option.map x.lockoutThreshold ~f:Integer.to_value));
        ("permittedNetworks",
          (Option.map x.permittedNetworks ~f:PermittedNetworksList.to_value));
        ("permittedWickrAwsNetworks",
          (Option.map x.permittedWickrAwsNetworks
             ~f:WickrAwsNetworksList.to_value));
        ("permittedWickrEnterpriseNetworks",
          (Option.map x.permittedWickrEnterpriseNetworks
             ~f:PermittedWickrEnterpriseNetworksList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let permittedWickrEnterpriseNetworks =
        (Option.map ~f:PermittedWickrEnterpriseNetworksList.of_xml)
          (Xml.child xml_arg0 "permittedWickrEnterpriseNetworks") in
      let permittedWickrAwsNetworks =
        (Option.map ~f:WickrAwsNetworksList.of_xml)
          (Xml.child xml_arg0 "permittedWickrAwsNetworks") in
      let permittedNetworks =
        (Option.map ~f:PermittedNetworksList.of_xml)
          (Xml.child xml_arg0 "permittedNetworks") in
      let lockoutThreshold =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "lockoutThreshold") in
      let federationMode =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "federationMode") in
      let ssoMaxIdleMinutes =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ssoMaxIdleMinutes") in
      let shredder =
        (Option.map ~f:ShredderSettings.of_xml)
          (Xml.child xml_arg0 "shredder") in
      let showMasterRecoveryKey =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "showMasterRecoveryKey") in
      let quickResponses =
        (Option.map ~f:SecurityGroupStringList.of_xml)
          (Xml.child xml_arg0 "quickResponses") in
      let presenceEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "presenceEnabled") in
      let passwordRequirements =
        (Option.map ~f:PasswordRequirements.of_xml)
          (Xml.child xml_arg0 "passwordRequirements") in
      let messageForwardingEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "messageForwardingEnabled") in
      let maxTtl = (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "maxTtl") in
      let maxBor =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxBor") in
      let maxAutoDownloadSize =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "maxAutoDownloadSize") in
      let locationEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "locationEnabled") in
      let locationAllowMaps =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "locationAllowMaps") in
      let isLinkPreviewEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "isLinkPreviewEnabled") in
      let isAtoEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isAtoEnabled") in
      let globalFederation =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "globalFederation") in
      let forceReadReceipts =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "forceReadReceipts") in
      let forceOpenAccess =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "forceOpenAccess") in
      let forceDeviceLockout =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "forceDeviceLockout") in
      let filesEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "filesEnabled") in
      let enableRestrictedGlobalFederation =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableRestrictedGlobalFederation") in
      let enableOpenAccessOption =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableOpenAccessOption") in
      let enableNotificationPreview =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableNotificationPreview") in
      let enableGuestFederation =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableGuestFederation") in
      let enableFileDownload =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableFileDownload") in
      let enableCrashReports =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableCrashReports") in
      let enableAtak =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enableAtak") in
      let checkForUpdates =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "checkForUpdates") in
      let calling =
        (Option.map ~f:CallingSettings.of_xml) (Xml.child xml_arg0 "calling") in
      let atakPackageValues =
        (Option.map ~f:SecurityGroupStringList.of_xml)
          (Xml.child xml_arg0 "atakPackageValues") in
      let alwaysReauthenticate =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "alwaysReauthenticate") in
      make ?permittedWickrEnterpriseNetworks ?permittedWickrAwsNetworks
        ?permittedNetworks ?lockoutThreshold ?federationMode
        ?ssoMaxIdleMinutes ?shredder ?showMasterRecoveryKey ?quickResponses
        ?presenceEnabled ?passwordRequirements ?messageForwardingEnabled
        ?maxTtl ?maxBor ?maxAutoDownloadSize ?locationEnabled
        ?locationAllowMaps ?isLinkPreviewEnabled ?isAtoEnabled
        ?globalFederation ?forceReadReceipts ?forceOpenAccess
        ?forceDeviceLockout ?filesEnabled ?enableRestrictedGlobalFederation
        ?enableOpenAccessOption ?enableNotificationPreview
        ?enableGuestFederation ?enableFileDownload ?enableCrashReports
        ?enableAtak ?checkForUpdates ?calling ?atakPackageValues
        ?alwaysReauthenticate ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let permittedWickrEnterpriseNetworks =
        field_map json__ "permittedWickrEnterpriseNetworks"
          PermittedWickrEnterpriseNetworksList.of_json in
      let permittedWickrAwsNetworks =
        field_map json__ "permittedWickrAwsNetworks"
          WickrAwsNetworksList.of_json in
      let permittedNetworks =
        field_map json__ "permittedNetworks" PermittedNetworksList.of_json in
      let lockoutThreshold =
        field_map json__ "lockoutThreshold" Integer.of_json in
      let federationMode = field_map json__ "federationMode" Integer.of_json in
      let ssoMaxIdleMinutes =
        field_map json__ "ssoMaxIdleMinutes" Integer.of_json in
      let shredder = field_map json__ "shredder" ShredderSettings.of_json in
      let showMasterRecoveryKey =
        field_map json__ "showMasterRecoveryKey" Boolean.of_json in
      let quickResponses =
        field_map json__ "quickResponses" SecurityGroupStringList.of_json in
      let presenceEnabled =
        field_map json__ "presenceEnabled" Boolean.of_json in
      let passwordRequirements =
        field_map json__ "passwordRequirements" PasswordRequirements.of_json in
      let messageForwardingEnabled =
        field_map json__ "messageForwardingEnabled" Boolean.of_json in
      let maxTtl = field_map json__ "maxTtl" Long.of_json in
      let maxBor = field_map json__ "maxBor" Integer.of_json in
      let maxAutoDownloadSize =
        field_map json__ "maxAutoDownloadSize" Long.of_json in
      let locationEnabled =
        field_map json__ "locationEnabled" Boolean.of_json in
      let locationAllowMaps =
        field_map json__ "locationAllowMaps" Boolean.of_json in
      let isLinkPreviewEnabled =
        field_map json__ "isLinkPreviewEnabled" Boolean.of_json in
      let isAtoEnabled = field_map json__ "isAtoEnabled" Boolean.of_json in
      let globalFederation =
        field_map json__ "globalFederation" Boolean.of_json in
      let forceReadReceipts =
        field_map json__ "forceReadReceipts" Boolean.of_json in
      let forceOpenAccess =
        field_map json__ "forceOpenAccess" Boolean.of_json in
      let forceDeviceLockout =
        field_map json__ "forceDeviceLockout" Integer.of_json in
      let filesEnabled = field_map json__ "filesEnabled" Boolean.of_json in
      let enableRestrictedGlobalFederation =
        field_map json__ "enableRestrictedGlobalFederation" Boolean.of_json in
      let enableOpenAccessOption =
        field_map json__ "enableOpenAccessOption" Boolean.of_json in
      let enableNotificationPreview =
        field_map json__ "enableNotificationPreview" Boolean.of_json in
      let enableGuestFederation =
        field_map json__ "enableGuestFederation" Boolean.of_json in
      let enableFileDownload =
        field_map json__ "enableFileDownload" Boolean.of_json in
      let enableCrashReports =
        field_map json__ "enableCrashReports" Boolean.of_json in
      let enableAtak = field_map json__ "enableAtak" Boolean.of_json in
      let checkForUpdates =
        field_map json__ "checkForUpdates" Boolean.of_json in
      let calling = field_map json__ "calling" CallingSettings.of_json in
      let atakPackageValues =
        field_map json__ "atakPackageValues" SecurityGroupStringList.of_json in
      let alwaysReauthenticate =
        field_map json__ "alwaysReauthenticate" Boolean.of_json in
      make ?permittedWickrEnterpriseNetworks ?permittedWickrAwsNetworks
        ?permittedNetworks ?lockoutThreshold ?federationMode
        ?ssoMaxIdleMinutes ?shredder ?showMasterRecoveryKey ?quickResponses
        ?presenceEnabled ?passwordRequirements ?messageForwardingEnabled
        ?maxTtl ?maxBor ?maxAutoDownloadSize ?locationEnabled
        ?locationAllowMaps ?isLinkPreviewEnabled ?isAtoEnabled
        ?globalFederation ?forceReadReceipts ?forceOpenAccess
        ?forceDeviceLockout ?filesEnabled ?enableRestrictedGlobalFederation
        ?enableOpenAccessOption ?enableNotificationPreview
        ?enableGuestFederation ?enableFileDownload ?enableCrashReports
        ?enableAtak ?checkForUpdates ?calling ?atakPackageValues
        ?alwaysReauthenticate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Comprehensive configuration settings that define all user capabilities, restrictions, and features for members of a security group. These settings control everything from calling permissions to federation settings to security policies."]
module AccessLevel =
  struct
    type nonrec t =
      | STANDARD 
      | PREMIUM 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | STANDARD -> "STANDARD"
      | PREMIUM -> "PREMIUM"
      | Non_static_id s -> s
    let of_string =
      function
      | "STANDARD" -> STANDARD
      | "PREMIUM" -> PREMIUM
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AccessLevel" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AccessLevel" j)
    let to_json = simple_to_json to_value
  end
module BotStatus =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for BotStatus" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Uname =
  struct
    type nonrec t = string
    let context_ = "Uname"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Uname" j
    let to_json = simple_to_json to_value
  end
module ErrorDetailList =
  struct
    type nonrec t = ErrorDetail.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ErrorDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ErrorDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"ErrorDetailList" ~of_json:ErrorDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module Setting =
  struct
    type nonrec t =
      {
      optionName: GenericString.t option
        [@ocaml.doc
          "The name of the network setting (e.g., 'enableClientMetrics', 'dataRetention')."];
      value: GenericString.t option
        [@ocaml.doc
          "The current value of the setting as a string. Boolean values are represented as 'true' or 'false'."];
      type_: GenericString.t option
        [@ocaml.doc
          "The data type of the setting value (e.g., 'boolean', 'string', 'number')."]}
    let make ?optionName =
      fun ?value -> fun ?type_ -> fun () -> { optionName; value; type_ }
    let to_value x =
      structure_to_value
        [("optionName", (Option.map x.optionName ~f:GenericString.to_value));
        ("value", (Option.map x.value ~f:GenericString.to_value));
        ("type", (Option.map x.type_ ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "type") in
      let value =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "value") in
      let optionName =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "optionName") in
      make ?type_ ?value ?optionName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" GenericString.of_json in
      let value = field_map json__ "value" GenericString.of_json in
      let optionName = field_map json__ "optionName" GenericString.of_json in
      make ?type_ ?value ?optionName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a single network-level configuration setting with its name, value, and data type. Settings control network-wide behaviors and features."]
module ReadReceiptConfig =
  struct
    type nonrec t =
      {
      status: Status.t option
        [@ocaml.doc "The read receipt status mode for the network."]}
    let make ?status = fun () -> { status }
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" Status.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration for read receipts at the network level, controlling whether senders can see when their messages have been read."]
module User =
  struct
    type nonrec t =
      {
      userId: UserId.t option
        [@ocaml.doc "The unique identifier for the user within the network."];
      firstName: SensitiveString.t option
        [@ocaml.doc "The first name of the user."];
      lastName: SensitiveString.t option
        [@ocaml.doc "The last name of the user."];
      username: GenericString.t option
        [@ocaml.doc
          "The email address or username of the user. For bots, this must end in 'bot'."];
      securityGroups: SecurityGroupIdList.t option
        [@ocaml.doc
          "A list of security group IDs to which the user is assigned, determining their permissions and feature access."];
      isAdmin: Boolean.t option
        [@ocaml.doc
          "Indicates whether the user has administrator privileges in the network."];
      suspended: Boolean.t option
        [@ocaml.doc
          "Indicates whether the user is currently suspended and unable to access the network."];
      status: Integer.t option
        [@ocaml.doc
          "The current status of the user (1 for pending invitation, 2 for active)."];
      otpEnabled: Boolean.t option
        [@ocaml.doc
          "Indicates whether one-time password (OTP) authentication is enabled for the user."];
      scimId: GenericString.t option
        [@ocaml.doc
          "The SCIM (System for Cross-domain Identity Management) identifier for the user, used for identity synchronization. Currently not used."];
      type_: GenericString.t option
        [@ocaml.doc
          "The descriptive type of the user account (e.g., 'user')."];
      cell: GenericString.t option
        [@ocaml.doc
          "The phone number minus country code, used for cloud deployments."];
      countryCode: GenericString.t option
        [@ocaml.doc
          "The country code for the user's phone number, used for cloud deployments."];
      challengeFailures: Integer.t option
        [@ocaml.doc
          "The number of failed password attempts for enterprise deployments, used for account lockout policies."];
      isInviteExpired: Boolean.t option
        [@ocaml.doc
          "Indicates whether the user's email invitation code has expired, applicable to cloud deployments."];
      isUser: Boolean.t option
        [@ocaml.doc
          "Indicates whether this account is a user (as opposed to a bot or other account type)."];
      inviteCode: GenericString.t option
        [@ocaml.doc
          "The invitation code for this user, used during registration to join the network."];
      codeValidation: Boolean.t option
        [@ocaml.doc
          "Indicates whether the user can be verified through a custom invite code."];
      uname: GenericString.t option
        [@ocaml.doc "The unique identifier for the user."]}
    let make ?userId =
      fun ?firstName ->
        fun ?lastName ->
          fun ?username ->
            fun ?securityGroups ->
              fun ?isAdmin ->
                fun ?suspended ->
                  fun ?status ->
                    fun ?otpEnabled ->
                      fun ?scimId ->
                        fun ?type_ ->
                          fun ?cell ->
                            fun ?countryCode ->
                              fun ?challengeFailures ->
                                fun ?isInviteExpired ->
                                  fun ?isUser ->
                                    fun ?inviteCode ->
                                      fun ?codeValidation ->
                                        fun ?uname ->
                                          fun () ->
                                            {
                                              userId;
                                              firstName;
                                              lastName;
                                              username;
                                              securityGroups;
                                              isAdmin;
                                              suspended;
                                              status;
                                              otpEnabled;
                                              scimId;
                                              type_;
                                              cell;
                                              countryCode;
                                              challengeFailures;
                                              isInviteExpired;
                                              isUser;
                                              inviteCode;
                                              codeValidation;
                                              uname
                                            }
    let to_value x =
      structure_to_value
        [("userId", (Option.map x.userId ~f:UserId.to_value));
        ("firstName", (Option.map x.firstName ~f:SensitiveString.to_value));
        ("lastName", (Option.map x.lastName ~f:SensitiveString.to_value));
        ("username", (Option.map x.username ~f:GenericString.to_value));
        ("securityGroups",
          (Option.map x.securityGroups ~f:SecurityGroupIdList.to_value));
        ("isAdmin", (Option.map x.isAdmin ~f:Boolean.to_value));
        ("suspended", (Option.map x.suspended ~f:Boolean.to_value));
        ("status", (Option.map x.status ~f:Integer.to_value));
        ("otpEnabled", (Option.map x.otpEnabled ~f:Boolean.to_value));
        ("scimId", (Option.map x.scimId ~f:GenericString.to_value));
        ("type", (Option.map x.type_ ~f:GenericString.to_value));
        ("cell", (Option.map x.cell ~f:GenericString.to_value));
        ("countryCode", (Option.map x.countryCode ~f:GenericString.to_value));
        ("challengeFailures",
          (Option.map x.challengeFailures ~f:Integer.to_value));
        ("isInviteExpired",
          (Option.map x.isInviteExpired ~f:Boolean.to_value));
        ("isUser", (Option.map x.isUser ~f:Boolean.to_value));
        ("inviteCode", (Option.map x.inviteCode ~f:GenericString.to_value));
        ("codeValidation", (Option.map x.codeValidation ~f:Boolean.to_value));
        ("uname", (Option.map x.uname ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uname =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "uname") in
      let codeValidation =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "codeValidation") in
      let inviteCode =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "inviteCode") in
      let isUser =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isUser") in
      let isInviteExpired =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isInviteExpired") in
      let challengeFailures =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "challengeFailures") in
      let countryCode =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "countryCode") in
      let cell =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "cell") in
      let type_ =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "type") in
      let scimId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "scimId") in
      let otpEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "otpEnabled") in
      let status =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "status") in
      let suspended =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "suspended") in
      let isAdmin =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isAdmin") in
      let securityGroups =
        (Option.map ~f:SecurityGroupIdList.of_xml)
          (Xml.child xml_arg0 "securityGroups") in
      let username =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "username") in
      let lastName =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "lastName") in
      let firstName =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "firstName") in
      let userId =
        (Option.map ~f:UserId.of_xml) (Xml.child xml_arg0 "userId") in
      make ?uname ?codeValidation ?inviteCode ?isUser ?isInviteExpired
        ?challengeFailures ?countryCode ?cell ?type_ ?scimId ?otpEnabled
        ?status ?suspended ?isAdmin ?securityGroups ?username ?lastName
        ?firstName ?userId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uname = field_map json__ "uname" GenericString.of_json in
      let codeValidation = field_map json__ "codeValidation" Boolean.of_json in
      let inviteCode = field_map json__ "inviteCode" GenericString.of_json in
      let isUser = field_map json__ "isUser" Boolean.of_json in
      let isInviteExpired =
        field_map json__ "isInviteExpired" Boolean.of_json in
      let challengeFailures =
        field_map json__ "challengeFailures" Integer.of_json in
      let countryCode = field_map json__ "countryCode" GenericString.of_json in
      let cell = field_map json__ "cell" GenericString.of_json in
      let type_ = field_map json__ "type" GenericString.of_json in
      let scimId = field_map json__ "scimId" GenericString.of_json in
      let otpEnabled = field_map json__ "otpEnabled" Boolean.of_json in
      let status = field_map json__ "status" Integer.of_json in
      let suspended = field_map json__ "suspended" Boolean.of_json in
      let isAdmin = field_map json__ "isAdmin" Boolean.of_json in
      let securityGroups =
        field_map json__ "securityGroups" SecurityGroupIdList.of_json in
      let username = field_map json__ "username" GenericString.of_json in
      let lastName = field_map json__ "lastName" SensitiveString.of_json in
      let firstName = field_map json__ "firstName" SensitiveString.of_json in
      let userId = field_map json__ "userId" UserId.of_json in
      make ?uname ?codeValidation ?inviteCode ?isUser ?isInviteExpired
        ?challengeFailures ?countryCode ?cell ?type_ ?scimId ?otpEnabled
        ?status ?suspended ?isAdmin ?securityGroups ?username ?lastName
        ?firstName ?userId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a user account in a Wickr network with detailed profile information, status, security settings, and authentication details. codeValidation, inviteCode and inviteCodeTtl are restricted to networks under preview only."]
module SecurityGroup =
  struct
    type nonrec t =
      {
      activeMembers: Integer.t option
        [@ocaml.doc
          "The number of active user members currently in the security group."];
      botMembers: Integer.t option
        [@ocaml.doc
          "The number of bot members currently in the security group."];
      activeDirectoryGuid: GenericString.t option
        [@ocaml.doc
          "The GUID of the Active Directory group associated with this security group, if synchronized with LDAP."];
      id: GenericString.t option
        [@ocaml.doc "The unique identifier of the security group."];
      isDefault: Boolean.t option
        [@ocaml.doc
          "Indicates whether this is the default security group for the network. Each network has only one default group."];
      name: GenericString.t option
        [@ocaml.doc "The human-readable name of the security group."];
      modified: Integer.t option
        [@ocaml.doc
          "The timestamp when the security group was last modified, specified in epoch seconds."];
      securityGroupSettings: SecurityGroupSettings.t option
        [@ocaml.doc
          "The comprehensive configuration settings that define capabilities and restrictions for members of this security group."]}
    let make ?activeMembers =
      fun ?botMembers ->
        fun ?activeDirectoryGuid ->
          fun ?id ->
            fun ?isDefault ->
              fun ?name ->
                fun ?modified ->
                  fun ?securityGroupSettings ->
                    fun () ->
                      {
                        activeMembers;
                        botMembers;
                        activeDirectoryGuid;
                        id;
                        isDefault;
                        name;
                        modified;
                        securityGroupSettings
                      }
    let to_value x =
      structure_to_value
        [("activeMembers", (Option.map x.activeMembers ~f:Integer.to_value));
        ("botMembers", (Option.map x.botMembers ~f:Integer.to_value));
        ("activeDirectoryGuid",
          (Option.map x.activeDirectoryGuid ~f:GenericString.to_value));
        ("id", (Option.map x.id ~f:GenericString.to_value));
        ("isDefault", (Option.map x.isDefault ~f:Boolean.to_value));
        ("name", (Option.map x.name ~f:GenericString.to_value));
        ("modified", (Option.map x.modified ~f:Integer.to_value));
        ("securityGroupSettings",
          (Option.map x.securityGroupSettings
             ~f:SecurityGroupSettings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupSettings =
        (Option.map ~f:SecurityGroupSettings.of_xml)
          (Xml.child xml_arg0 "securityGroupSettings") in
      let modified =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "modified") in
      let name =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "name") in
      let isDefault =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isDefault") in
      let id = (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "id") in
      let activeDirectoryGuid =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "activeDirectoryGuid") in
      let botMembers =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "botMembers") in
      let activeMembers =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "activeMembers") in
      make ?securityGroupSettings ?modified ?name ?isDefault ?id
        ?activeDirectoryGuid ?botMembers ?activeMembers ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroupSettings =
        field_map json__ "securityGroupSettings"
          SecurityGroupSettings.of_json in
      let modified = field_map json__ "modified" Integer.of_json in
      let name = field_map json__ "name" GenericString.of_json in
      let isDefault = field_map json__ "isDefault" Boolean.of_json in
      let id = field_map json__ "id" GenericString.of_json in
      let activeDirectoryGuid =
        field_map json__ "activeDirectoryGuid" GenericString.of_json in
      let botMembers = field_map json__ "botMembers" Integer.of_json in
      let activeMembers = field_map json__ "activeMembers" Integer.of_json in
      make ?securityGroupSettings ?modified ?name ?isDefault ?id
        ?activeDirectoryGuid ?botMembers ?activeMembers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a security group in a Wickr network, containing membership statistics, configuration, and all permission settings that apply to its members."]
module Network =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t option
        [@ocaml.doc "The unique identifier of the network."];
      networkName: GenericString.t option
        [@ocaml.doc "The name of the network."];
      accessLevel: AccessLevel.t option
        [@ocaml.doc
          "The access level of the network (STANDARD or PREMIUM), which determines available features and capabilities."];
      awsAccountId: GenericString.t option
        [@ocaml.doc
          "The Amazon Web Services account ID that owns the network."];
      networkArn: GenericString.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the network."];
      standing: Integer.t option
        [@ocaml.doc "The current standing or status of the network."];
      freeTrialExpiration: GenericString.t option
        [@ocaml.doc
          "The expiration date and time for the network's free trial period, if applicable."];
      migrationState: Integer.t option
        [@ocaml.doc
          "The SSO redirect URI migration state, managed by the SSO redirect migration wizard. Values: 0 (not started), 1 (in progress), or 2 (completed)."];
      encryptionKeyArn: GenericString.t option
        [@ocaml.doc
          "The ARN of the Amazon Web Services KMS customer managed key used for encrypting sensitive data in the network."]}
    let make ?networkId =
      fun ?networkName ->
        fun ?accessLevel ->
          fun ?awsAccountId ->
            fun ?networkArn ->
              fun ?standing ->
                fun ?freeTrialExpiration ->
                  fun ?migrationState ->
                    fun ?encryptionKeyArn ->
                      fun () ->
                        {
                          networkId;
                          networkName;
                          accessLevel;
                          awsAccountId;
                          networkArn;
                          standing;
                          freeTrialExpiration;
                          migrationState;
                          encryptionKeyArn
                        }
    let to_value x =
      structure_to_value
        [("networkId", (Option.map x.networkId ~f:NetworkId.to_value));
        ("networkName", (Option.map x.networkName ~f:GenericString.to_value));
        ("accessLevel", (Option.map x.accessLevel ~f:AccessLevel.to_value));
        ("awsAccountId",
          (Option.map x.awsAccountId ~f:GenericString.to_value));
        ("networkArn", (Option.map x.networkArn ~f:GenericString.to_value));
        ("standing", (Option.map x.standing ~f:Integer.to_value));
        ("freeTrialExpiration",
          (Option.map x.freeTrialExpiration ~f:GenericString.to_value));
        ("migrationState", (Option.map x.migrationState ~f:Integer.to_value));
        ("encryptionKeyArn",
          (Option.map x.encryptionKeyArn ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionKeyArn =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "encryptionKeyArn") in
      let migrationState =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "migrationState") in
      let freeTrialExpiration =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "freeTrialExpiration") in
      let standing =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "standing") in
      let networkArn =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "networkArn") in
      let awsAccountId =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "awsAccountId") in
      let accessLevel =
        (Option.map ~f:AccessLevel.of_xml) (Xml.child xml_arg0 "accessLevel") in
      let networkName =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "networkName") in
      let networkId =
        (Option.map ~f:NetworkId.of_xml) (Xml.child xml_arg0 "networkId") in
      make ?encryptionKeyArn ?migrationState ?freeTrialExpiration ?standing
        ?networkArn ?awsAccountId ?accessLevel ?networkName ?networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryptionKeyArn =
        field_map json__ "encryptionKeyArn" GenericString.of_json in
      let migrationState = field_map json__ "migrationState" Integer.of_json in
      let freeTrialExpiration =
        field_map json__ "freeTrialExpiration" GenericString.of_json in
      let standing = field_map json__ "standing" Integer.of_json in
      let networkArn = field_map json__ "networkArn" GenericString.of_json in
      let awsAccountId =
        field_map json__ "awsAccountId" GenericString.of_json in
      let accessLevel = field_map json__ "accessLevel" AccessLevel.of_json in
      let networkName = field_map json__ "networkName" GenericString.of_json in
      let networkId = field_map json__ "networkId" NetworkId.of_json in
      make ?encryptionKeyArn ?migrationState ?freeTrialExpiration ?standing
        ?networkArn ?awsAccountId ?accessLevel ?networkName ?networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a Wickr network with all its configuration and status information."]
module GuestUser =
  struct
    type nonrec t =
      {
      billingPeriod: GenericString.t option
        [@ocaml.doc
          "The billing period when this guest user accessed the network (e.g., '2024-01')."];
      username: GenericString.t option
        [@ocaml.doc "The username of the guest user."];
      usernameHash: GenericString.t option
        [@ocaml.doc
          "The unique username hash identifier for the guest user."]}
    let make ?billingPeriod =
      fun ?username ->
        fun ?usernameHash ->
          fun () -> { billingPeriod; username; usernameHash }
    let to_value x =
      structure_to_value
        [("billingPeriod",
           (Option.map x.billingPeriod ~f:GenericString.to_value));
        ("username", (Option.map x.username ~f:GenericString.to_value));
        ("usernameHash",
          (Option.map x.usernameHash ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let usernameHash =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "usernameHash") in
      let username =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "username") in
      let billingPeriod =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "billingPeriod") in
      make ?usernameHash ?username ?billingPeriod ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let usernameHash =
        field_map json__ "usernameHash" GenericString.of_json in
      let username = field_map json__ "username" GenericString.of_json in
      let billingPeriod =
        field_map json__ "billingPeriod" GenericString.of_json in
      make ?usernameHash ?username ?billingPeriod ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a guest user who has accessed the network from a federated Wickr network."]
module BasicDeviceObject =
  struct
    type nonrec t =
      {
      appId: GenericString.t option
        [@ocaml.doc
          "The unique application ID for the Wickr app on this device."];
      created: GenericString.t option
        [@ocaml.doc
          "The timestamp when the device first appeared in the Wickr database."];
      lastLogin: GenericString.t option
        [@ocaml.doc
          "The timestamp when the device last successfully logged into Wickr. This is also used to determine SSO idle time."];
      statusText: GenericString.t option
        [@ocaml.doc
          "The current status of the device, either 'Active' or 'Reset' depending on whether the device is currently active or has been marked for reset."];
      suspend: Boolean.t option
        [@ocaml.doc "Indicates whether the device is suspended."];
      type_: GenericString.t option
        [@ocaml.doc
          "The operating system of the device (e.g., 'MacOSX', 'Windows', 'iOS', 'Android')."]}
    let make ?appId =
      fun ?created ->
        fun ?lastLogin ->
          fun ?statusText ->
            fun ?suspend ->
              fun ?type_ ->
                fun () ->
                  { appId; created; lastLogin; statusText; suspend; type_ }
    let to_value x =
      structure_to_value
        [("appId", (Option.map x.appId ~f:GenericString.to_value));
        ("created", (Option.map x.created ~f:GenericString.to_value));
        ("lastLogin", (Option.map x.lastLogin ~f:GenericString.to_value));
        ("statusText", (Option.map x.statusText ~f:GenericString.to_value));
        ("suspend", (Option.map x.suspend ~f:Boolean.to_value));
        ("type", (Option.map x.type_ ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "type") in
      let suspend =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "suspend") in
      let statusText =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "statusText") in
      let lastLogin =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "lastLogin") in
      let created =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "created") in
      let appId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "appId") in
      make ?type_ ?suspend ?statusText ?lastLogin ?created ?appId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" GenericString.of_json in
      let suspend = field_map json__ "suspend" Boolean.of_json in
      let statusText = field_map json__ "statusText" GenericString.of_json in
      let lastLogin = field_map json__ "lastLogin" GenericString.of_json in
      let created = field_map json__ "created" GenericString.of_json in
      let appId = field_map json__ "appId" GenericString.of_json in
      make ?type_ ?suspend ?statusText ?lastLogin ?created ?appId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a device where a user has logged into Wickr, containing information about the device's type, status, and login history."]
module Bot =
  struct
    type nonrec t =
      {
      botId: GenericString.t option
        [@ocaml.doc "The unique identifier of the bot."];
      displayName: GenericString.t option
        [@ocaml.doc "The display name of the bot that is visible to users."];
      username: GenericString.t option
        [@ocaml.doc "The username of the bot."];
      uname: GenericString.t option
        [@ocaml.doc "The unique username hash identifier for the bot."];
      pubkey: GenericString.t option
        [@ocaml.doc "The public key of the bot used for encryption."];
      status: BotStatus.t option
        [@ocaml.doc
          "The current status of the bot (1 for pending, 2 for active)."];
      groupId: GenericString.t option
        [@ocaml.doc "The ID of the security group to which the bot belongs."];
      hasChallenge: Boolean.t option
        [@ocaml.doc "Indicates whether the bot has a password set."];
      suspended: Boolean.t option
        [@ocaml.doc "Indicates whether the bot is currently suspended."];
      lastLogin: GenericString.t option
        [@ocaml.doc "The timestamp of the bot's last login."]}
    let make ?botId =
      fun ?displayName ->
        fun ?username ->
          fun ?uname ->
            fun ?pubkey ->
              fun ?status ->
                fun ?groupId ->
                  fun ?hasChallenge ->
                    fun ?suspended ->
                      fun ?lastLogin ->
                        fun () ->
                          {
                            botId;
                            displayName;
                            username;
                            uname;
                            pubkey;
                            status;
                            groupId;
                            hasChallenge;
                            suspended;
                            lastLogin
                          }
    let to_value x =
      structure_to_value
        [("botId", (Option.map x.botId ~f:GenericString.to_value));
        ("displayName", (Option.map x.displayName ~f:GenericString.to_value));
        ("username", (Option.map x.username ~f:GenericString.to_value));
        ("uname", (Option.map x.uname ~f:GenericString.to_value));
        ("pubkey", (Option.map x.pubkey ~f:GenericString.to_value));
        ("status", (Option.map x.status ~f:BotStatus.to_value));
        ("groupId", (Option.map x.groupId ~f:GenericString.to_value));
        ("hasChallenge", (Option.map x.hasChallenge ~f:Boolean.to_value));
        ("suspended", (Option.map x.suspended ~f:Boolean.to_value));
        ("lastLogin", (Option.map x.lastLogin ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastLogin =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "lastLogin") in
      let suspended =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "suspended") in
      let hasChallenge =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "hasChallenge") in
      let groupId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "groupId") in
      let status =
        (Option.map ~f:BotStatus.of_xml) (Xml.child xml_arg0 "status") in
      let pubkey =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "pubkey") in
      let uname =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "uname") in
      let username =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "username") in
      let displayName =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "displayName") in
      let botId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "botId") in
      make ?lastLogin ?suspended ?hasChallenge ?groupId ?status ?pubkey
        ?uname ?username ?displayName ?botId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastLogin = field_map json__ "lastLogin" GenericString.of_json in
      let suspended = field_map json__ "suspended" Boolean.of_json in
      let hasChallenge = field_map json__ "hasChallenge" Boolean.of_json in
      let groupId = field_map json__ "groupId" GenericString.of_json in
      let status = field_map json__ "status" BotStatus.of_json in
      let pubkey = field_map json__ "pubkey" GenericString.of_json in
      let uname = field_map json__ "uname" GenericString.of_json in
      let username = field_map json__ "username" GenericString.of_json in
      let displayName = field_map json__ "displayName" GenericString.of_json in
      let botId = field_map json__ "botId" GenericString.of_json in
      make ?lastLogin ?suspended ?hasChallenge ?groupId ?status ?pubkey
        ?uname ?username ?displayName ?botId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a bot account in a Wickr network with all its informational fields."]
module BlockedGuestUser =
  struct
    type nonrec t =
      {
      username: GenericString.t option
        [@ocaml.doc "The username of the blocked guest user."];
      admin: GenericString.t option
        [@ocaml.doc
          "The username of the administrator who blocked this guest user."];
      modified: GenericString.t option
        [@ocaml.doc
          "The timestamp when the guest user was blocked or last modified."];
      usernameHash: GenericString.t option
        [@ocaml.doc
          "The unique username hash identifier for the blocked guest user."]}
    let make ?username =
      fun ?admin ->
        fun ?modified ->
          fun ?usernameHash ->
            fun () -> { username; admin; modified; usernameHash }
    let to_value x =
      structure_to_value
        [("username", (Option.map x.username ~f:GenericString.to_value));
        ("admin", (Option.map x.admin ~f:GenericString.to_value));
        ("modified", (Option.map x.modified ~f:GenericString.to_value));
        ("usernameHash",
          (Option.map x.usernameHash ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let usernameHash =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "usernameHash") in
      let modified =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "modified") in
      let admin =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "admin") in
      let username =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "username") in
      make ?usernameHash ?modified ?admin ?username ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let usernameHash =
        field_map json__ "usernameHash" GenericString.of_json in
      let modified = field_map json__ "modified" GenericString.of_json in
      let admin = field_map json__ "admin" GenericString.of_json in
      let username = field_map json__ "username" GenericString.of_json in
      make ?usernameHash ?modified ?admin ?username ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a guest user who has been blocked from accessing a Wickr network."]
module OidcConfigInfoApplicationIdInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:10) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for OidcConfigInfoApplicationIdInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GuestUserHistoryCount =
  struct
    type nonrec t =
      {
      month: GenericString.t option
        [@ocaml.doc
          "The month and billing period in YYYY_MM format (e.g., '2024_01')."];
      count: GenericString.t option
        [@ocaml.doc
          "The number of guest users who have communicated with your Wickr network during this billing period."]}
    let make ?month = fun ?count -> fun () -> { month; count }
    let to_value x =
      structure_to_value
        [("month", (Option.map x.month ~f:GenericString.to_value));
        ("count", (Option.map x.count ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let count =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "count") in
      let month =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "month") in
      make ?count ?month ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let count = field_map json__ "count" GenericString.of_json in
      let month = field_map json__ "month" GenericString.of_json in
      make ?count ?month ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the count of guest users for a specific billing period, used for tracking historical guest user activity."]
module BatchUserErrorResponseItem =
  struct
    type nonrec t =
      {
      field: GenericString.t option
        [@ocaml.doc "The field that caused the error."];
      reason: GenericString.t option
        [@ocaml.doc "A description of why the user operation failed."];
      userId: UserId.t option
        [@ocaml.doc "The user ID associated with the failed operation."]}
    let make ?field =
      fun ?reason -> fun ?userId -> fun () -> { field; reason; userId }
    let to_value x =
      structure_to_value
        [("field", (Option.map x.field ~f:GenericString.to_value));
        ("reason", (Option.map x.reason ~f:GenericString.to_value));
        ("userId", (Option.map x.userId ~f:UserId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userId =
        (Option.map ~f:UserId.of_xml) (Xml.child xml_arg0 "userId") in
      let reason =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "reason") in
      let field =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "field") in
      make ?userId ?reason ?field ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let userId = field_map json__ "userId" UserId.of_json in
      let reason = field_map json__ "reason" GenericString.of_json in
      let field = field_map json__ "field" GenericString.of_json in
      make ?userId ?reason ?field ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains error information for a user operation that failed in a batch user request."]
module BatchUserSuccessResponseItem =
  struct
    type nonrec t =
      {
      userId: UserId.t option
        [@ocaml.doc "The user ID that was successfully processed."]}
    let make ?userId = fun () -> { userId }
    let to_value x =
      structure_to_value
        [("userId", (Option.map x.userId ~f:UserId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userId =
        (Option.map ~f:UserId.of_xml) (Xml.child xml_arg0 "userId") in
      make ?userId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let userId = field_map json__ "userId" UserId.of_json in
      make ?userId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a user that was successfully processed in a batch user operation."]
module BatchDeviceErrorResponseItem =
  struct
    type nonrec t =
      {
      field: GenericString.t option
        [@ocaml.doc "The field that caused the error."];
      reason: GenericString.t option
        [@ocaml.doc "A description of why the device operation failed."];
      appId: GenericString.t option
        [@ocaml.doc
          "The application ID of the device that failed to be processed."]}
    let make ?field =
      fun ?reason -> fun ?appId -> fun () -> { field; reason; appId }
    let to_value x =
      structure_to_value
        [("field", (Option.map x.field ~f:GenericString.to_value));
        ("reason", (Option.map x.reason ~f:GenericString.to_value));
        ("appId", (Option.map x.appId ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let appId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "appId") in
      let reason =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "reason") in
      let field =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "field") in
      make ?appId ?reason ?field ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let appId = field_map json__ "appId" GenericString.of_json in
      let reason = field_map json__ "reason" GenericString.of_json in
      let field = field_map json__ "field" GenericString.of_json in
      make ?appId ?reason ?field ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains error information for a device operation that failed in a batch device request."]
module BatchDeviceSuccessResponseItem =
  struct
    type nonrec t =
      {
      appId: GenericString.t option
        [@ocaml.doc
          "The application ID of the device that was successfully processed."]}
    let make ?appId = fun () -> { appId }
    let to_value x =
      structure_to_value
        [("appId", (Option.map x.appId ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let appId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "appId") in
      make ?appId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let appId = field_map json__ "appId" GenericString.of_json in
      make ?appId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a device that was successfully processed in a batch device operation."]
module BatchUnameErrorResponseItem =
  struct
    type nonrec t =
      {
      field: GenericString.t option
        [@ocaml.doc "The field that caused the error."];
      reason: GenericString.t option
        [@ocaml.doc "A description of why the username hash lookup failed."];
      uname: Uname.t option
        [@ocaml.doc "The username hash that failed to be looked up."]}
    let make ?field =
      fun ?reason -> fun ?uname -> fun () -> { field; reason; uname }
    let to_value x =
      structure_to_value
        [("field", (Option.map x.field ~f:GenericString.to_value));
        ("reason", (Option.map x.reason ~f:GenericString.to_value));
        ("uname", (Option.map x.uname ~f:Uname.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uname = (Option.map ~f:Uname.of_xml) (Xml.child xml_arg0 "uname") in
      let reason =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "reason") in
      let field =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "field") in
      make ?uname ?reason ?field ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uname = field_map json__ "uname" Uname.of_json in
      let reason = field_map json__ "reason" GenericString.of_json in
      let field = field_map json__ "field" GenericString.of_json in
      make ?uname ?reason ?field ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains error information for a username hash lookup that failed in a batch uname lookup request."]
module BatchUnameSuccessResponseItem =
  struct
    type nonrec t =
      {
      uname: Uname.t option
        [@ocaml.doc "The username hash that was successfully resolved."];
      username: GenericString.t option
        [@ocaml.doc
          "The email address or username corresponding to the username hash."]}
    let make ?uname = fun ?username -> fun () -> { uname; username }
    let to_value x =
      structure_to_value
        [("uname", (Option.map x.uname ~f:Uname.to_value));
        ("username", (Option.map x.username ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let username =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "username") in
      let uname = (Option.map ~f:Uname.of_xml) (Xml.child xml_arg0 "uname") in
      make ?username ?uname ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let username = field_map json__ "username" GenericString.of_json in
      let uname = field_map json__ "uname" Uname.of_json in
      make ?username ?uname ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a username hash that was successfully resolved in a batch uname lookup operation."]
module BatchCreateUserRequestItem =
  struct
    type nonrec t =
      {
      firstName: SensitiveString.t option
        [@ocaml.doc "The first name of the user."];
      lastName: SensitiveString.t option
        [@ocaml.doc "The last name of the user."];
      securityGroupIds: SecurityGroupIdList.t
        [@ocaml.doc
          "A list of security group IDs to which the user should be assigned."];
      username: GenericString.t
        [@ocaml.doc
          "The email address or username for the user. Must be unique within the network."];
      inviteCode: GenericString.t option
        [@ocaml.doc
          "A custom invite code for the user. If not provided, one will be generated automatically."];
      inviteCodeTtl: Integer.t option
        [@ocaml.doc
          "The time-to-live for the invite code in days. After this period, the invite code will expire."];
      codeValidation: Boolean.t option
        [@ocaml.doc
          "Indicates whether the user can be verified through a custom invite code."]}
    let context_ = "BatchCreateUserRequestItem"
    let make ?firstName =
      fun ?lastName ->
        fun ?inviteCode ->
          fun ?inviteCodeTtl ->
            fun ?codeValidation ->
              fun ~securityGroupIds ->
                fun ~username ->
                  fun () ->
                    {
                      firstName;
                      lastName;
                      inviteCode;
                      inviteCodeTtl;
                      codeValidation;
                      securityGroupIds;
                      username
                    }
    let to_value x =
      structure_to_value
        [("firstName", (Option.map x.firstName ~f:SensitiveString.to_value));
        ("lastName", (Option.map x.lastName ~f:SensitiveString.to_value));
        ("securityGroupIds",
          (Some (SecurityGroupIdList.to_value x.securityGroupIds)));
        ("username", (Some (GenericString.to_value x.username)));
        ("inviteCode", (Option.map x.inviteCode ~f:GenericString.to_value));
        ("inviteCodeTtl", (Option.map x.inviteCodeTtl ~f:Integer.to_value));
        ("codeValidation", (Option.map x.codeValidation ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let codeValidation =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "codeValidation") in
      let inviteCodeTtl =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "inviteCodeTtl") in
      let inviteCode =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "inviteCode") in
      let username =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "username") in
      let securityGroupIds =
        SecurityGroupIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "securityGroupIds") in
      let lastName =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "lastName") in
      let firstName =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "firstName") in
      make ?codeValidation ?inviteCodeTtl ?inviteCode ~username
        ~securityGroupIds ?lastName ?firstName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let codeValidation = field_map json__ "codeValidation" Boolean.of_json in
      let inviteCodeTtl = field_map json__ "inviteCodeTtl" Integer.of_json in
      let inviteCode = field_map json__ "inviteCode" GenericString.of_json in
      let username = field_map_exn json__ "username" GenericString.of_json in
      let securityGroupIds =
        field_map_exn json__ "securityGroupIds" SecurityGroupIdList.of_json in
      let lastName = field_map json__ "lastName" SensitiveString.of_json in
      let firstName = field_map json__ "firstName" SensitiveString.of_json in
      make ?codeValidation ?inviteCodeTtl ?inviteCode ~username
        ~securityGroupIds ?lastName ?firstName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the details for a single user to be created in a batch user creation request. A user can only be assigned to a single security group. Attempting to add a user to multiple security groups is not supported and will result in an error. codeValidation, inviteCode, and inviteCodeTtl are restricted to networks under preview only."]
module BadRequestError =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A detailed message explaining what was wrong with the request and how to correct it."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request was invalid or malformed. This error occurs when the request parameters do not meet the API requirements, such as invalid field values, missing required parameters, or improperly formatted data."]
module ForbiddenError =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message explaining why access was denied and what permissions are required."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Access to the requested resource is forbidden. This error occurs when the authenticated user does not have the necessary permissions to perform the requested operation, even though they are authenticated."]
module InternalServerError =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message describing the internal server error that occurred."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An unexpected error occurred on the server while processing the request. This indicates a problem with the Wickr service itself rather than with the request. If this error persists, contact Amazon Web Services Support."]
module RateLimitError =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating that the rate limit was exceeded and suggesting when to retry."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request was throttled because too many requests were sent in a short period of time. Wait a moment and retry the request. Consider implementing exponential backoff in your application."]
module ResourceNotFoundError =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc "A message identifying which resource was not found."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested resource could not be found. This error occurs when you try to access or modify a network, user, bot, security group, or other resource that doesn't exist or has been deleted."]
module UnauthorizedError =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc "A message explaining why the authentication failed."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request was not authenticated or the authentication credentials were invalid. This error occurs when the request lacks valid authentication credentials or the credentials have expired."]
module ValidationError =
  struct
    type nonrec t =
      {
      reasons: ErrorDetailList.t option
        [@ocaml.doc
          "A list of validation error details, where each item identifies a specific field that failed validation and explains the reason for the failure."];
      message: GenericString.t option
        [@ocaml.doc
          "A message describing the validation error error that occurred."]}
    let make ?reasons = fun ?message -> fun () -> { reasons; message }
    let to_value x =
      structure_to_value
        [("reasons", (Option.map x.reasons ~f:ErrorDetailList.to_value));
        ("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      let reasons =
        (Option.map ~f:ErrorDetailList.of_xml) (Xml.child xml_arg0 "reasons") in
      make ?message ?reasons ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      let reasons = field_map json__ "reasons" ErrorDetailList.of_json in
      make ?message ?reasons ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "One or more fields in the request failed validation. This error provides detailed information about which fields were invalid and why, allowing you to correct the request and retry."]
module UpdateUserDetails =
  struct
    type nonrec t =
      {
      firstName: SensitiveString.t option
        [@ocaml.doc "The new first name for the user."];
      lastName: SensitiveString.t option
        [@ocaml.doc "The new last name for the user."];
      username: GenericString.t option
        [@ocaml.doc "The new username or email address for the user."];
      securityGroupIds: SecurityGroupIdList.t option
        [@ocaml.doc
          "The updated list of security group IDs to which the user should belong."];
      inviteCode: GenericString.t option
        [@ocaml.doc "A new custom invite code for the user."];
      inviteCodeTtl: Integer.t option
        [@ocaml.doc "The new time-to-live for the invite code in days."];
      codeValidation: Boolean.t option
        [@ocaml.doc
          "Indicates whether the user can be verified through a custom invite code."]}
    let make ?firstName =
      fun ?lastName ->
        fun ?username ->
          fun ?securityGroupIds ->
            fun ?inviteCode ->
              fun ?inviteCodeTtl ->
                fun ?codeValidation ->
                  fun () ->
                    {
                      firstName;
                      lastName;
                      username;
                      securityGroupIds;
                      inviteCode;
                      inviteCodeTtl;
                      codeValidation
                    }
    let to_value x =
      structure_to_value
        [("firstName", (Option.map x.firstName ~f:SensitiveString.to_value));
        ("lastName", (Option.map x.lastName ~f:SensitiveString.to_value));
        ("username", (Option.map x.username ~f:GenericString.to_value));
        ("securityGroupIds",
          (Option.map x.securityGroupIds ~f:SecurityGroupIdList.to_value));
        ("inviteCode", (Option.map x.inviteCode ~f:GenericString.to_value));
        ("inviteCodeTtl", (Option.map x.inviteCodeTtl ~f:Integer.to_value));
        ("codeValidation", (Option.map x.codeValidation ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let codeValidation =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "codeValidation") in
      let inviteCodeTtl =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "inviteCodeTtl") in
      let inviteCode =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "inviteCode") in
      let securityGroupIds =
        (Option.map ~f:SecurityGroupIdList.of_xml)
          (Xml.child xml_arg0 "securityGroupIds") in
      let username =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "username") in
      let lastName =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "lastName") in
      let firstName =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "firstName") in
      make ?codeValidation ?inviteCodeTtl ?inviteCode ?securityGroupIds
        ?username ?lastName ?firstName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let codeValidation = field_map json__ "codeValidation" Boolean.of_json in
      let inviteCodeTtl = field_map json__ "inviteCodeTtl" Integer.of_json in
      let inviteCode = field_map json__ "inviteCode" GenericString.of_json in
      let securityGroupIds =
        field_map json__ "securityGroupIds" SecurityGroupIdList.of_json in
      let username = field_map json__ "username" GenericString.of_json in
      let lastName = field_map json__ "lastName" SensitiveString.of_json in
      let firstName = field_map json__ "firstName" SensitiveString.of_json in
      make ?codeValidation ?inviteCodeTtl ?inviteCode ?securityGroupIds
        ?username ?lastName ?firstName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the modifiable details for updating an existing user, including name, password, security group membership, and invitation settings. A user can only be assigned to a single security group. Attempting to add a user to multiple security groups is not supported and will result in an error."]
module SettingsList =
  struct
    type nonrec t = Setting.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Setting.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Setting.of_xml)
    let of_json j =
      list_of_json ~kind:"SettingsList" ~of_json:Setting.of_json j
    let to_json v = composed_to_json to_value v
  end
module NetworkSettings =
  struct
    type nonrec t =
      {
      enableClientMetrics: Boolean.t option
        [@ocaml.doc
          "Allows Wickr clients to send anonymized performance and usage metrics to the Wickr backend server for service improvement and troubleshooting."];
      readReceiptConfig: ReadReceiptConfig.t option
        [@ocaml.doc
          "Configuration for read receipts at the network level, controlling the default behavior for whether senders can see when their messages have been read."];
      dataRetention: Boolean.t option
        [@ocaml.doc
          "Indicates whether the data retention feature is enabled for the network. When true, messages are captured by the data retention bot for compliance and archiving purposes."];
      enableTrustedDataFormat: Boolean.t option
        [@ocaml.doc
          "Configuration for OpenTDF integration at the network level, enforcing ABAC decision making when operating in TDF enabled rooms."]}
    let make ?enableClientMetrics =
      fun ?readReceiptConfig ->
        fun ?dataRetention ->
          fun ?enableTrustedDataFormat ->
            fun () ->
              {
                enableClientMetrics;
                readReceiptConfig;
                dataRetention;
                enableTrustedDataFormat
              }
    let to_value x =
      structure_to_value
        [("enableClientMetrics",
           (Option.map x.enableClientMetrics ~f:Boolean.to_value));
        ("readReceiptConfig",
          (Option.map x.readReceiptConfig ~f:ReadReceiptConfig.to_value));
        ("dataRetention", (Option.map x.dataRetention ~f:Boolean.to_value));
        ("enableTrustedDataFormat",
          (Option.map x.enableTrustedDataFormat ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enableTrustedDataFormat =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableTrustedDataFormat") in
      let dataRetention =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dataRetention") in
      let readReceiptConfig =
        (Option.map ~f:ReadReceiptConfig.of_xml)
          (Xml.child xml_arg0 "readReceiptConfig") in
      let enableClientMetrics =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableClientMetrics") in
      make ?enableTrustedDataFormat ?dataRetention ?readReceiptConfig
        ?enableClientMetrics ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let enableTrustedDataFormat =
        field_map json__ "enableTrustedDataFormat" Boolean.of_json in
      let dataRetention = field_map json__ "dataRetention" Boolean.of_json in
      let readReceiptConfig =
        field_map json__ "readReceiptConfig" ReadReceiptConfig.of_json in
      let enableClientMetrics =
        field_map json__ "enableClientMetrics" Boolean.of_json in
      make ?enableTrustedDataFormat ?dataRetention ?readReceiptConfig
        ?enableClientMetrics ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains network-level configuration settings that apply to all users and security groups within a Wickr network."]
module ClientToken =
  struct
    type nonrec t = string
    let context_ = "ClientToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-_:]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientToken" j
    let to_json = simple_to_json to_value
  end
module DataRetentionActionType =
  struct
    type nonrec t =
      | ENABLE 
      | DISABLE 
      | PUBKEY_MSG_ACK 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLE -> "ENABLE"
      | DISABLE -> "DISABLE"
      | PUBKEY_MSG_ACK -> "PUBKEY_MSG_ACK"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLE" -> ENABLE
      | "DISABLE" -> DISABLE
      | "PUBKEY_MSG_ACK" -> PUBKEY_MSG_ACK
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DataRetentionActionType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"DataRetentionActionType" j)
    let to_json = simple_to_json to_value
  end
module BotId =
  struct
    type nonrec t = string
    let context_ = "BotId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:10) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BotId" j
    let to_json = simple_to_json to_value
  end
module StringList =
  struct
    type nonrec t = GenericString.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GenericString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GenericString.of_xml)
    let of_json j =
      list_of_json ~kind:"StringList" ~of_json:GenericString.of_json j
    let to_json v = composed_to_json to_value v
  end
module RegisterOidcConfigResponseApplicationIdInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:10) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for RegisterOidcConfigResponseApplicationIdInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Users =
  struct
    type nonrec t = User.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:User.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:User.of_xml)
    let of_json j = list_of_json ~kind:"Users" ~of_json:User.of_json j
    let to_json v = composed_to_json to_value v
  end
module SortDirection =
  struct
    type nonrec t =
      | ASC 
      | DESC 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | ASC -> "ASC" | DESC -> "DESC" | Non_static_id s -> s
    let of_string =
      function | "ASC" -> ASC | "DESC" -> DESC | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SortDirection" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SortDirection" j)
    let to_json = simple_to_json to_value
  end
module UserStatus =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for UserStatus" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SecurityGroupList =
  struct
    type nonrec t = SecurityGroup.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SecurityGroup.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SecurityGroup.of_xml)
    let of_json j =
      list_of_json ~kind:"SecurityGroupList" ~of_json:SecurityGroup.of_json j
    let to_json v = composed_to_json to_value v
  end
module NetworkList =
  struct
    type nonrec t = Network.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Network.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Network.of_xml)
    let of_json j =
      list_of_json ~kind:"NetworkList" ~of_json:Network.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuestUserList =
  struct
    type nonrec t = GuestUser.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuestUser.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuestUser.of_xml)
    let of_json j =
      list_of_json ~kind:"GuestUserList" ~of_json:GuestUser.of_json j
    let to_json v = composed_to_json to_value v
  end
module Devices =
  struct
    type nonrec t = BasicDeviceObject.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BasicDeviceObject.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BasicDeviceObject.of_xml)
    let of_json j =
      list_of_json ~kind:"Devices" ~of_json:BasicDeviceObject.of_json j
    let to_json v = composed_to_json to_value v
  end
module Bots =
  struct
    type nonrec t = Bot.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Bot.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Bot.of_xml)
    let of_json j = list_of_json ~kind:"Bots" ~of_json:Bot.of_json j
    let to_json v = composed_to_json to_value v
  end
module BlockedGuestUserList =
  struct
    type nonrec t = BlockedGuestUser.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BlockedGuestUser.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BlockedGuestUser.of_xml)
    let of_json j =
      list_of_json ~kind:"BlockedGuestUserList"
        ~of_json:BlockedGuestUser.of_json j
    let to_json v = composed_to_json to_value v
  end
module SyntheticTimestamp_epoch_seconds =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module OidcConfigInfo =
  struct
    type nonrec t =
      {
      applicationName: GenericString.t option
        [@ocaml.doc
          "The name of the OIDC application as registered with the identity provider."];
      clientId: GenericString.t option
        [@ocaml.doc
          "The OAuth client ID assigned by the identity provider for authentication requests."];
      companyId: GenericString.t option
        [@ocaml.doc
          "Custom identifier your end users will use to sign in with SSO."];
      scopes: GenericString.t option
        [@ocaml.doc
          "The OAuth scopes requested from the identity provider, which determine what user information is accessible (e.g., 'openid profile email')."];
      issuer: GenericString.t option
        [@ocaml.doc
          "The issuer URL of the identity provider, which serves as the base URL for OIDC endpoints and configuration discovery."];
      clientSecret: SensitiveString.t option
        [@ocaml.doc
          "The OAuth client secret used to authenticate the application with the identity provider."];
      secret: SensitiveString.t option
        [@ocaml.doc
          "An additional secret credential used by the identity provider for authentication."];
      redirectUrl: GenericString.t option
        [@ocaml.doc
          "The callback URL where the identity provider redirects users after successful authentication. This URL must be registered with the identity provider."];
      userId: GenericString.t option
        [@ocaml.doc
          "The claim field from the OIDC token to use as the unique user identifier (e.g., 'email', 'sub', or a custom claim)."];
      customUsername: GenericString.t option
        [@ocaml.doc
          "A custom field mapping to extract the username from the OIDC token when the standard username claim is insufficient."];
      caCertificate: GenericString.t option
        [@ocaml.doc
          "The X.509 CA certificate for validating SSL/TLS connections to the identity provider when using self-signed or enterprise certificates."];
      applicationId: OidcConfigInfoApplicationIdInteger.t option
        [@ocaml.doc
          "The unique identifier for the registered OIDC application. Valid range is 1-10."];
      ssoTokenBufferMinutes: Integer.t option
        [@ocaml.doc
          "The grace period in minutes before the SSO token expires when the system should proactively refresh the token to maintain seamless user access."];
      extraAuthParams: GenericString.t option
        [@ocaml.doc
          "Additional authentication parameters to include in the OIDC authorization request as a query string. Useful for provider-specific extensions."]}
    let make ?applicationName =
      fun ?clientId ->
        fun ?companyId ->
          fun ?scopes ->
            fun ?issuer ->
              fun ?clientSecret ->
                fun ?secret ->
                  fun ?redirectUrl ->
                    fun ?userId ->
                      fun ?customUsername ->
                        fun ?caCertificate ->
                          fun ?applicationId ->
                            fun ?ssoTokenBufferMinutes ->
                              fun ?extraAuthParams ->
                                fun () ->
                                  {
                                    applicationName;
                                    clientId;
                                    companyId;
                                    scopes;
                                    issuer;
                                    clientSecret;
                                    secret;
                                    redirectUrl;
                                    userId;
                                    customUsername;
                                    caCertificate;
                                    applicationId;
                                    ssoTokenBufferMinutes;
                                    extraAuthParams
                                  }
    let to_value x =
      structure_to_value
        [("applicationName",
           (Option.map x.applicationName ~f:GenericString.to_value));
        ("clientId", (Option.map x.clientId ~f:GenericString.to_value));
        ("companyId", (Option.map x.companyId ~f:GenericString.to_value));
        ("scopes", (Option.map x.scopes ~f:GenericString.to_value));
        ("issuer", (Option.map x.issuer ~f:GenericString.to_value));
        ("clientSecret",
          (Option.map x.clientSecret ~f:SensitiveString.to_value));
        ("secret", (Option.map x.secret ~f:SensitiveString.to_value));
        ("redirectUrl", (Option.map x.redirectUrl ~f:GenericString.to_value));
        ("userId", (Option.map x.userId ~f:GenericString.to_value));
        ("customUsername",
          (Option.map x.customUsername ~f:GenericString.to_value));
        ("caCertificate",
          (Option.map x.caCertificate ~f:GenericString.to_value));
        ("applicationId",
          (Option.map x.applicationId
             ~f:OidcConfigInfoApplicationIdInteger.to_value));
        ("ssoTokenBufferMinutes",
          (Option.map x.ssoTokenBufferMinutes ~f:Integer.to_value));
        ("extraAuthParams",
          (Option.map x.extraAuthParams ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let extraAuthParams =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "extraAuthParams") in
      let ssoTokenBufferMinutes =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ssoTokenBufferMinutes") in
      let applicationId =
        (Option.map ~f:OidcConfigInfoApplicationIdInteger.of_xml)
          (Xml.child xml_arg0 "applicationId") in
      let caCertificate =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "caCertificate") in
      let customUsername =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "customUsername") in
      let userId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "userId") in
      let redirectUrl =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "redirectUrl") in
      let secret =
        (Option.map ~f:SensitiveString.of_xml) (Xml.child xml_arg0 "secret") in
      let clientSecret =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "clientSecret") in
      let issuer =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "issuer") in
      let scopes =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "scopes") in
      let companyId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "companyId") in
      let clientId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "clientId") in
      let applicationName =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "applicationName") in
      make ?extraAuthParams ?ssoTokenBufferMinutes ?applicationId
        ?caCertificate ?customUsername ?userId ?redirectUrl ?secret
        ?clientSecret ?issuer ?scopes ?companyId ?clientId ?applicationName
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let extraAuthParams =
        field_map json__ "extraAuthParams" GenericString.of_json in
      let ssoTokenBufferMinutes =
        field_map json__ "ssoTokenBufferMinutes" Integer.of_json in
      let applicationId =
        field_map json__ "applicationId"
          OidcConfigInfoApplicationIdInteger.of_json in
      let caCertificate =
        field_map json__ "caCertificate" GenericString.of_json in
      let customUsername =
        field_map json__ "customUsername" GenericString.of_json in
      let userId = field_map json__ "userId" GenericString.of_json in
      let redirectUrl = field_map json__ "redirectUrl" GenericString.of_json in
      let secret = field_map json__ "secret" SensitiveString.of_json in
      let clientSecret =
        field_map json__ "clientSecret" SensitiveString.of_json in
      let issuer = field_map json__ "issuer" GenericString.of_json in
      let scopes = field_map json__ "scopes" GenericString.of_json in
      let companyId = field_map json__ "companyId" GenericString.of_json in
      let clientId = field_map json__ "clientId" GenericString.of_json in
      let applicationName =
        field_map json__ "applicationName" GenericString.of_json in
      make ?extraAuthParams ?ssoTokenBufferMinutes ?applicationId
        ?caCertificate ?customUsername ?userId ?redirectUrl ?secret
        ?clientSecret ?issuer ?scopes ?companyId ?clientId ?applicationName
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the OpenID Connect (OIDC) configuration information for Single Sign-On (SSO) authentication, including identity provider settings and client credentials."]
module OidcTokenInfo =
  struct
    type nonrec t =
      {
      codeVerifier: GenericString.t option
        [@ocaml.doc
          "The PKCE (Proof Key for Code Exchange) code verifier, a cryptographically random string used to enhance security in the OAuth flow."];
      codeChallenge: GenericString.t option
        [@ocaml.doc
          "The PKCE code challenge, a transformed version of the code verifier sent during the authorization request for verification."];
      accessToken: GenericString.t option
        [@ocaml.doc
          "The OAuth access token that can be used to access protected resources on behalf of the authenticated user."];
      idToken: GenericString.t option
        [@ocaml.doc
          "The OpenID Connect ID token containing user identity information and authentication context as a signed JWT."];
      refreshToken: GenericString.t option
        [@ocaml.doc
          "The OAuth refresh token that can be used to obtain new access tokens without requiring the user to re-authenticate."];
      tokenType: GenericString.t option
        [@ocaml.doc
          "The type of access token issued, typically 'Bearer', which indicates how the token should be used in API requests."];
      expiresIn: Long.t option
        [@ocaml.doc
          "The lifetime of the access token in seconds, indicating when the token will expire and need to be refreshed."]}
    let make ?codeVerifier =
      fun ?codeChallenge ->
        fun ?accessToken ->
          fun ?idToken ->
            fun ?refreshToken ->
              fun ?tokenType ->
                fun ?expiresIn ->
                  fun () ->
                    {
                      codeVerifier;
                      codeChallenge;
                      accessToken;
                      idToken;
                      refreshToken;
                      tokenType;
                      expiresIn
                    }
    let to_value x =
      structure_to_value
        [("codeVerifier",
           (Option.map x.codeVerifier ~f:GenericString.to_value));
        ("codeChallenge",
          (Option.map x.codeChallenge ~f:GenericString.to_value));
        ("accessToken", (Option.map x.accessToken ~f:GenericString.to_value));
        ("idToken", (Option.map x.idToken ~f:GenericString.to_value));
        ("refreshToken",
          (Option.map x.refreshToken ~f:GenericString.to_value));
        ("tokenType", (Option.map x.tokenType ~f:GenericString.to_value));
        ("expiresIn", (Option.map x.expiresIn ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expiresIn =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "expiresIn") in
      let tokenType =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "tokenType") in
      let refreshToken =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "refreshToken") in
      let idToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "idToken") in
      let accessToken =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "accessToken") in
      let codeChallenge =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "codeChallenge") in
      let codeVerifier =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "codeVerifier") in
      make ?expiresIn ?tokenType ?refreshToken ?idToken ?accessToken
        ?codeChallenge ?codeVerifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let expiresIn = field_map json__ "expiresIn" Long.of_json in
      let tokenType = field_map json__ "tokenType" GenericString.of_json in
      let refreshToken =
        field_map json__ "refreshToken" GenericString.of_json in
      let idToken = field_map json__ "idToken" GenericString.of_json in
      let accessToken = field_map json__ "accessToken" GenericString.of_json in
      let codeChallenge =
        field_map json__ "codeChallenge" GenericString.of_json in
      let codeVerifier =
        field_map json__ "codeVerifier" GenericString.of_json in
      make ?expiresIn ?tokenType ?refreshToken ?idToken ?accessToken
        ?codeChallenge ?codeVerifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains OAuth token information returned from the identity provider, including access tokens, ID tokens, and PKCE parameters used for secure authentication."]
module GuestUserHistoryCountList =
  struct
    type nonrec t = GuestUserHistoryCount.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuestUserHistoryCount.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuestUserHistoryCount.of_xml)
    let of_json j =
      list_of_json ~kind:"GuestUserHistoryCountList"
        ~of_json:GuestUserHistoryCount.of_json j
    let to_json v = composed_to_json to_value v
  end
module SecurityGroupSettingsRequest =
  struct
    type nonrec t =
      {
      lockoutThreshold: Integer.t option
        [@ocaml.doc
          "The number of failed password attempts before a user account is locked out."];
      permittedNetworks: PermittedNetworksList.t option
        [@ocaml.doc
          "A list of network IDs that are permitted for local federation when federation mode is set to restricted."];
      enableGuestFederation: Boolean.t option
        [@ocaml.doc
          "Guest users let you work with people outside your organization that only have limited access to Wickr. Only valid when federationMode is set to Global."];
      globalFederation: Boolean.t option
        [@ocaml.doc
          "Allow users to securely federate with all Amazon Web Services Wickr networks and Amazon Web Services Enterprise networks."];
      federationMode: Integer.t option
        [@ocaml.doc
          "The local federation mode. Values: 0 (none), 1 (federated - all networks), 2 (restricted - only permitted networks)."];
      enableRestrictedGlobalFederation: Boolean.t option
        [@ocaml.doc
          "Enables restricted global federation to limit communication to specific permitted networks only. Requires globalFederation to be enabled."];
      permittedWickrAwsNetworks: WickrAwsNetworksList.t option
        [@ocaml.doc
          "A list of permitted Amazon Web Services Wickr networks for restricted global federation."];
      permittedWickrEnterpriseNetworks:
        PermittedWickrEnterpriseNetworksList.t option
        [@ocaml.doc
          "A list of permitted Wickr Enterprise networks for restricted global federation."]}
    let make ?lockoutThreshold =
      fun ?permittedNetworks ->
        fun ?enableGuestFederation ->
          fun ?globalFederation ->
            fun ?federationMode ->
              fun ?enableRestrictedGlobalFederation ->
                fun ?permittedWickrAwsNetworks ->
                  fun ?permittedWickrEnterpriseNetworks ->
                    fun () ->
                      {
                        lockoutThreshold;
                        permittedNetworks;
                        enableGuestFederation;
                        globalFederation;
                        federationMode;
                        enableRestrictedGlobalFederation;
                        permittedWickrAwsNetworks;
                        permittedWickrEnterpriseNetworks
                      }
    let to_value x =
      structure_to_value
        [("lockoutThreshold",
           (Option.map x.lockoutThreshold ~f:Integer.to_value));
        ("permittedNetworks",
          (Option.map x.permittedNetworks ~f:PermittedNetworksList.to_value));
        ("enableGuestFederation",
          (Option.map x.enableGuestFederation ~f:Boolean.to_value));
        ("globalFederation",
          (Option.map x.globalFederation ~f:Boolean.to_value));
        ("federationMode", (Option.map x.federationMode ~f:Integer.to_value));
        ("enableRestrictedGlobalFederation",
          (Option.map x.enableRestrictedGlobalFederation ~f:Boolean.to_value));
        ("permittedWickrAwsNetworks",
          (Option.map x.permittedWickrAwsNetworks
             ~f:WickrAwsNetworksList.to_value));
        ("permittedWickrEnterpriseNetworks",
          (Option.map x.permittedWickrEnterpriseNetworks
             ~f:PermittedWickrEnterpriseNetworksList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let permittedWickrEnterpriseNetworks =
        (Option.map ~f:PermittedWickrEnterpriseNetworksList.of_xml)
          (Xml.child xml_arg0 "permittedWickrEnterpriseNetworks") in
      let permittedWickrAwsNetworks =
        (Option.map ~f:WickrAwsNetworksList.of_xml)
          (Xml.child xml_arg0 "permittedWickrAwsNetworks") in
      let enableRestrictedGlobalFederation =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableRestrictedGlobalFederation") in
      let federationMode =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "federationMode") in
      let globalFederation =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "globalFederation") in
      let enableGuestFederation =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableGuestFederation") in
      let permittedNetworks =
        (Option.map ~f:PermittedNetworksList.of_xml)
          (Xml.child xml_arg0 "permittedNetworks") in
      let lockoutThreshold =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "lockoutThreshold") in
      make ?permittedWickrEnterpriseNetworks ?permittedWickrAwsNetworks
        ?enableRestrictedGlobalFederation ?federationMode ?globalFederation
        ?enableGuestFederation ?permittedNetworks ?lockoutThreshold ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let permittedWickrEnterpriseNetworks =
        field_map json__ "permittedWickrEnterpriseNetworks"
          PermittedWickrEnterpriseNetworksList.of_json in
      let permittedWickrAwsNetworks =
        field_map json__ "permittedWickrAwsNetworks"
          WickrAwsNetworksList.of_json in
      let enableRestrictedGlobalFederation =
        field_map json__ "enableRestrictedGlobalFederation" Boolean.of_json in
      let federationMode = field_map json__ "federationMode" Integer.of_json in
      let globalFederation =
        field_map json__ "globalFederation" Boolean.of_json in
      let enableGuestFederation =
        field_map json__ "enableGuestFederation" Boolean.of_json in
      let permittedNetworks =
        field_map json__ "permittedNetworks" PermittedNetworksList.of_json in
      let lockoutThreshold =
        field_map json__ "lockoutThreshold" Integer.of_json in
      make ?permittedWickrEnterpriseNetworks ?permittedWickrAwsNetworks
        ?enableRestrictedGlobalFederation ?federationMode ?globalFederation
        ?enableGuestFederation ?permittedNetworks ?lockoutThreshold ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the security group configuration settings that can be specified when creating or updating a security group. This is a subset of SecurityGroupSettings containing only the modifiable federation and security settings."]
module BatchUserErrorResponseItems =
  struct
    type nonrec t = BatchUserErrorResponseItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchUserErrorResponseItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchUserErrorResponseItem.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchUserErrorResponseItems"
        ~of_json:BatchUserErrorResponseItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchUserSuccessResponseItems =
  struct
    type nonrec t = BatchUserSuccessResponseItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchUserSuccessResponseItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchUserSuccessResponseItem.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchUserSuccessResponseItems"
        ~of_json:BatchUserSuccessResponseItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module UserIds =
  struct
    type nonrec t = UserId.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:UserId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UserId.of_xml)
    let of_json j = list_of_json ~kind:"UserIds" ~of_json:UserId.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchDeviceErrorResponseItems =
  struct
    type nonrec t = BatchDeviceErrorResponseItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchDeviceErrorResponseItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchDeviceErrorResponseItem.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchDeviceErrorResponseItems"
        ~of_json:BatchDeviceErrorResponseItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchDeviceSuccessResponseItems =
  struct
    type nonrec t = BatchDeviceSuccessResponseItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchDeviceSuccessResponseItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchDeviceSuccessResponseItem.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchDeviceSuccessResponseItems"
        ~of_json:BatchDeviceSuccessResponseItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module AppIds =
  struct
    type nonrec t = GenericString.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GenericString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GenericString.of_xml)
    let of_json j =
      list_of_json ~kind:"AppIds" ~of_json:GenericString.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchUnameErrorResponseItems =
  struct
    type nonrec t = BatchUnameErrorResponseItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchUnameErrorResponseItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchUnameErrorResponseItem.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchUnameErrorResponseItems"
        ~of_json:BatchUnameErrorResponseItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchUnameSuccessResponseItems =
  struct
    type nonrec t = BatchUnameSuccessResponseItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchUnameSuccessResponseItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchUnameSuccessResponseItem.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchUnameSuccessResponseItems"
        ~of_json:BatchUnameSuccessResponseItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module Unames =
  struct
    type nonrec t = GenericString.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GenericString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GenericString.of_xml)
    let of_json j =
      list_of_json ~kind:"Unames" ~of_json:GenericString.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchCreateUserRequestItems =
  struct
    type nonrec t = BatchCreateUserRequestItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchCreateUserRequestItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchCreateUserRequestItem.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchCreateUserRequestItems"
        ~of_json:BatchCreateUserRequestItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateUserResponse =
  struct
    type nonrec t =
      {
      userId: UserId.t option
        [@ocaml.doc "The unique identifier of the updated user."];
      networkId: NetworkId.t option
        [@ocaml.doc "The ID of the network where the user was updated."];
      securityGroupIds: SecurityGroupIdList.t option
        [@ocaml.doc
          "The list of security group IDs to which the user now belongs after the update."];
      firstName: SensitiveString.t option
        [@ocaml.doc "The updated first name of the user."];
      lastName: SensitiveString.t option
        [@ocaml.doc "The updated last name of the user."];
      middleName: GenericString.t option
        [@ocaml.doc "The middle name of the user (currently not used)."];
      suspended: Boolean.t option
        [@ocaml.doc
          "Indicates whether the user is suspended after the update."];
      modified: Integer.t option
        [@ocaml.doc
          "The timestamp when the user was last modified, specified in epoch seconds."];
      status: Integer.t option
        [@ocaml.doc "The user's status after the update."];
      inviteCode: GenericString.t option
        [@ocaml.doc "The updated invite code for the user, if applicable."];
      inviteExpiration: Integer.t option
        [@ocaml.doc
          "The expiration time of the user's invite code, specified in epoch seconds."];
      codeValidation: Boolean.t option
        [@ocaml.doc
          "Indicates whether the user can be verified through a custom invite code."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?userId =
      fun ?networkId ->
        fun ?securityGroupIds ->
          fun ?firstName ->
            fun ?lastName ->
              fun ?middleName ->
                fun ?suspended ->
                  fun ?modified ->
                    fun ?status ->
                      fun ?inviteCode ->
                        fun ?inviteExpiration ->
                          fun ?codeValidation ->
                            fun () ->
                              {
                                userId;
                                networkId;
                                securityGroupIds;
                                firstName;
                                lastName;
                                middleName;
                                suspended;
                                modified;
                                status;
                                inviteCode;
                                inviteExpiration;
                                codeValidation
                              }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("userId", (Option.map x.userId ~f:UserId.to_value));
        ("networkId", (Option.map x.networkId ~f:NetworkId.to_value));
        ("securityGroupIds",
          (Option.map x.securityGroupIds ~f:SecurityGroupIdList.to_value));
        ("firstName", (Option.map x.firstName ~f:SensitiveString.to_value));
        ("lastName", (Option.map x.lastName ~f:SensitiveString.to_value));
        ("middleName", (Option.map x.middleName ~f:GenericString.to_value));
        ("suspended", (Option.map x.suspended ~f:Boolean.to_value));
        ("modified", (Option.map x.modified ~f:Integer.to_value));
        ("status", (Option.map x.status ~f:Integer.to_value));
        ("inviteCode", (Option.map x.inviteCode ~f:GenericString.to_value));
        ("inviteExpiration",
          (Option.map x.inviteExpiration ~f:Integer.to_value));
        ("codeValidation", (Option.map x.codeValidation ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let codeValidation =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "codeValidation") in
      let inviteExpiration =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "inviteExpiration") in
      let inviteCode =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "inviteCode") in
      let status =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "status") in
      let modified =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "modified") in
      let suspended =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "suspended") in
      let middleName =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "middleName") in
      let lastName =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "lastName") in
      let firstName =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "firstName") in
      let securityGroupIds =
        (Option.map ~f:SecurityGroupIdList.of_xml)
          (Xml.child xml_arg0 "securityGroupIds") in
      let networkId =
        (Option.map ~f:NetworkId.of_xml) (Xml.child xml_arg0 "networkId") in
      let userId =
        (Option.map ~f:UserId.of_xml) (Xml.child xml_arg0 "userId") in
      make ?codeValidation ?inviteExpiration ?inviteCode ?status ?modified
        ?suspended ?middleName ?lastName ?firstName ?securityGroupIds
        ?networkId ?userId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let codeValidation = field_map json__ "codeValidation" Boolean.of_json in
      let inviteExpiration =
        field_map json__ "inviteExpiration" Integer.of_json in
      let inviteCode = field_map json__ "inviteCode" GenericString.of_json in
      let status = field_map json__ "status" Integer.of_json in
      let modified = field_map json__ "modified" Integer.of_json in
      let suspended = field_map json__ "suspended" Boolean.of_json in
      let middleName = field_map json__ "middleName" GenericString.of_json in
      let lastName = field_map json__ "lastName" SensitiveString.of_json in
      let firstName = field_map json__ "firstName" SensitiveString.of_json in
      let securityGroupIds =
        field_map json__ "securityGroupIds" SecurityGroupIdList.of_json in
      let networkId = field_map json__ "networkId" NetworkId.of_json in
      let userId = field_map json__ "userId" UserId.of_json in
      make ?codeValidation ?inviteExpiration ?inviteCode ?status ?modified
        ?suspended ?middleName ?lastName ?firstName ?securityGroupIds
        ?networkId ?userId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the properties of an existing user in a Wickr network. This operation allows you to modify the user's name, password, security group membership, and invite code settings. codeValidation, inviteCode, and inviteCodeTtl are restricted to networks under preview only."]
module UpdateUserRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network containing the user to update."];
      userId: UserId.t
        [@ocaml.doc "The unique identifier of the user to update."];
      userDetails: UpdateUserDetails.t option
        [@ocaml.doc
          "An object containing the user details to be updated, such as name, password, security groups, and invite code settings."]}
    let context_ = "UpdateUserRequest"
    let make ?userDetails =
      fun ~networkId ->
        fun ~userId -> fun () -> { userDetails; networkId; userId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("userId", (Some (UserId.to_value x.userId)));
        ("userDetails",
          (Option.map x.userDetails ~f:UpdateUserDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userDetails =
        (Option.map ~f:UpdateUserDetails.of_xml)
          (Xml.child xml_arg0 "userDetails") in
      let userId =
        UserId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "userId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?userDetails ~userId ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let userDetails =
        field_map json__ "userDetails" UpdateUserDetails.of_json in
      let userId = field_map_exn json__ "userId" UserId.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?userDetails ~userId ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the properties of an existing user in a Wickr network. This operation allows you to modify the user's name, password, security group membership, and invite code settings. codeValidation, inviteCode, and inviteCodeTtl are restricted to networks under preview only."]
module UpdateSecurityGroupResponse =
  struct
    type nonrec t =
      {
      securityGroup: SecurityGroup.t option
        [@ocaml.doc
          "The updated security group details, including the new settings."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?securityGroup = fun () -> { securityGroup }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("securityGroup",
           (Option.map x.securityGroup ~f:SecurityGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroup =
        (Option.map ~f:SecurityGroup.of_xml)
          (Xml.child xml_arg0 "securityGroup") in
      make ?securityGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroup =
        field_map json__ "securityGroup" SecurityGroup.of_json in
      make ?securityGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the properties of an existing security group in a Wickr network, such as its name or settings."]
module UpdateSecurityGroupRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network containing the security group to update."];
      groupId: GenericString.t
        [@ocaml.doc "The unique identifier of the security group to update."];
      name: GenericString.t option
        [@ocaml.doc "The new name for the security group."];
      securityGroupSettings: SecurityGroupSettings.t option
        [@ocaml.doc
          "The updated configuration settings for the security group. Federation mode - 0 (Local federation), 1 (Restricted federation), 2 (Global federation)"]}
    let context_ = "UpdateSecurityGroupRequest"
    let make ?name =
      fun ?securityGroupSettings ->
        fun ~networkId ->
          fun ~groupId ->
            fun () -> { name; securityGroupSettings; networkId; groupId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("groupId", (Some (GenericString.to_value x.groupId)));
        ("name", (Option.map x.name ~f:GenericString.to_value));
        ("securityGroupSettings",
          (Option.map x.securityGroupSettings
             ~f:SecurityGroupSettings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupSettings =
        (Option.map ~f:SecurityGroupSettings.of_xml)
          (Xml.child xml_arg0 "securityGroupSettings") in
      let name =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "name") in
      let groupId =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "groupId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?securityGroupSettings ?name ~groupId ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroupSettings =
        field_map json__ "securityGroupSettings"
          SecurityGroupSettings.of_json in
      let name = field_map json__ "name" GenericString.of_json in
      let groupId = field_map_exn json__ "groupId" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?securityGroupSettings ?name ~groupId ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the properties of an existing security group in a Wickr network, such as its name or settings."]
module UpdateNetworkSettingsResponse =
  struct
    type nonrec t =
      {
      settings: SettingsList.t option
        [@ocaml.doc
          "A list of the updated network settings, showing the new values for each modified setting."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?settings = fun () -> { settings }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("settings", (Option.map x.settings ~f:SettingsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let settings =
        (Option.map ~f:SettingsList.of_xml) (Xml.child xml_arg0 "settings") in
      make ?settings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let settings = field_map json__ "settings" SettingsList.of_json in
      make ?settings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates network-level settings for a Wickr network. You can modify settings such as client metrics, data retention, and other network-wide options."]
module UpdateNetworkSettingsRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network whose settings will be updated."];
      settings: NetworkSettings.t
        [@ocaml.doc
          "A map of setting names to their new values. Each setting should be provided with its appropriate type (boolean, string, number, etc.)."]}
    let context_ = "UpdateNetworkSettingsRequest"
    let make ~networkId = fun ~settings -> fun () -> { networkId; settings }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("settings", (Some (NetworkSettings.to_value x.settings)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let settings =
        NetworkSettings.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "settings") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~settings ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let settings = field_map_exn json__ "settings" NetworkSettings.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~settings ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates network-level settings for a Wickr network. You can modify settings such as client metrics, data retention, and other network-wide options."]
module UpdateNetworkResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating that the network was updated successfully."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message = fun () -> { message }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the properties of an existing Wickr network, such as its name or encryption key configuration."]
module UpdateNetworkRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc "The ID of the Wickr network to update."];
      networkName: GenericString.t
        [@ocaml.doc
          "The new name for the network. Must be between 1 and 20 characters."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique identifier for this request to ensure idempotency."];
      encryptionKeyArn: GenericString.t option
        [@ocaml.doc
          "The ARN of the Amazon Web Services KMS customer managed key to use for encrypting sensitive data in the network."]}
    let context_ = "UpdateNetworkRequest"
    let make ?clientToken =
      fun ?encryptionKeyArn ->
        fun ~networkId ->
          fun ~networkName ->
            fun () ->
              { clientToken; encryptionKeyArn; networkId; networkName }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("networkName", (Some (GenericString.to_value x.networkName)));
        ("X-Client-Token",
          (Option.map x.clientToken ~f:ClientToken.to_value));
        ("encryptionKeyArn",
          (Option.map x.encryptionKeyArn ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionKeyArn =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "encryptionKeyArn") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml)
          (Xml.child xml_arg0 "X-Client-Token") in
      let networkName =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkName") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?encryptionKeyArn ?clientToken ~networkName ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryptionKeyArn =
        field_map json__ "encryptionKeyArn" GenericString.of_json in
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let networkName =
        field_map_exn json__ "networkName" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?encryptionKeyArn ?clientToken ~networkName ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the properties of an existing Wickr network, such as its name or encryption key configuration."]
module UpdateGuestUserResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating the result of the update operation."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message = fun () -> { message }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the block status of a guest user in a Wickr network. This operation allows you to block or unblock a guest user from accessing the network."]
module UpdateGuestUserRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network where the guest user's status will be updated."];
      usernameHash: GenericString.t
        [@ocaml.doc
          "The username hash (unique identifier) of the guest user to update."];
      block: Boolean.t
        [@ocaml.doc
          "Set to true to block the guest user or false to unblock them."]}
    let context_ = "UpdateGuestUserRequest"
    let make ~networkId =
      fun ~usernameHash ->
        fun ~block -> fun () -> { networkId; usernameHash; block }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("usernameHash", (Some (GenericString.to_value x.usernameHash)));
        ("block", (Some (Boolean.to_value x.block)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let block =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "block") in
      let usernameHash =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "usernameHash") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~block ~usernameHash ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let block = field_map_exn json__ "block" Boolean.of_json in
      let usernameHash =
        field_map_exn json__ "usernameHash" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~block ~usernameHash ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the block status of a guest user in a Wickr network. This operation allows you to block or unblock a guest user from accessing the network."]
module UpdateDataRetentionResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating the result of the update operation."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message = fun () -> { message }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the data retention bot settings, allowing you to enable or disable the data retention service, or acknowledge the public key message."]
module UpdateDataRetentionRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network containing the data retention bot."];
      actionType: DataRetentionActionType.t
        [@ocaml.doc
          "The action to perform. Valid values are 'ENABLE' (to enable the data retention service), 'DISABLE' (to disable the service), or 'PUBKEY_MSG_ACK' (to acknowledge the public key message)."]}
    let context_ = "UpdateDataRetentionRequest"
    let make ~networkId =
      fun ~actionType -> fun () -> { networkId; actionType }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("actionType",
          (Some (DataRetentionActionType.to_value x.actionType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let actionType =
        DataRetentionActionType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "actionType") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~actionType ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let actionType =
        field_map_exn json__ "actionType" DataRetentionActionType.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~actionType ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the data retention bot settings, allowing you to enable or disable the data retention service, or acknowledge the public key message."]
module UpdateBotResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating the result of the bot update operation."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message = fun () -> { message }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the properties of an existing bot in a Wickr network. This operation allows you to modify the bot's display name, security group, password, or suspension status."]
module UpdateBotRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network containing the bot to update."];
      botId: BotId.t
        [@ocaml.doc "The unique identifier of the bot to update."];
      displayName: GenericString.t option
        [@ocaml.doc "The new display name for the bot."];
      groupId: GenericString.t option
        [@ocaml.doc "The ID of the new security group to assign the bot to."];
      challenge: SensitiveString.t option
        [@ocaml.doc "The new password for the bot account."];
      suspend: Boolean.t option
        [@ocaml.doc
          "Set to true to suspend the bot or false to unsuspend it. Omit this field for standard updates that don't affect suspension status."]}
    let context_ = "UpdateBotRequest"
    let make ?displayName =
      fun ?groupId ->
        fun ?challenge ->
          fun ?suspend ->
            fun ~networkId ->
              fun ~botId ->
                fun () ->
                  {
                    displayName;
                    groupId;
                    challenge;
                    suspend;
                    networkId;
                    botId
                  }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("botId", (Some (BotId.to_value x.botId)));
        ("displayName", (Option.map x.displayName ~f:GenericString.to_value));
        ("groupId", (Option.map x.groupId ~f:GenericString.to_value));
        ("challenge", (Option.map x.challenge ~f:SensitiveString.to_value));
        ("suspend", (Option.map x.suspend ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let suspend =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "suspend") in
      let challenge =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "challenge") in
      let groupId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "groupId") in
      let displayName =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "displayName") in
      let botId =
        BotId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "botId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?suspend ?challenge ?groupId ?displayName ~botId ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let suspend = field_map json__ "suspend" Boolean.of_json in
      let challenge = field_map json__ "challenge" SensitiveString.of_json in
      let groupId = field_map json__ "groupId" GenericString.of_json in
      let displayName = field_map json__ "displayName" GenericString.of_json in
      let botId = field_map_exn json__ "botId" BotId.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?suspend ?challenge ?groupId ?displayName ~botId ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the properties of an existing bot in a Wickr network. This operation allows you to modify the bot's display name, security group, password, or suspension status."]
module RegisterOpentdfConfigResponse =
  struct
    type nonrec t =
      {
      clientId: GenericString.t option
        [@ocaml.doc
          "The OIDC client ID used for authenticating with the OpenTDF provider."];
      domain: GenericString.t option
        [@ocaml.doc "The domain of the OpenTDF server."];
      clientSecret: SensitiveString.t option
        [@ocaml.doc
          "The OIDC client secret used for authenticating with the OpenTDF provider."];
      provider: GenericString.t option
        [@ocaml.doc "The provider of the OpenTDF platform."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?clientId =
      fun ?domain ->
        fun ?clientSecret ->
          fun ?provider ->
            fun () -> { clientId; domain; clientSecret; provider }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientId", (Option.map x.clientId ~f:GenericString.to_value));
        ("domain", (Option.map x.domain ~f:GenericString.to_value));
        ("clientSecret",
          (Option.map x.clientSecret ~f:SensitiveString.to_value));
        ("provider", (Option.map x.provider ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let provider =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "provider") in
      let clientSecret =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "clientSecret") in
      let domain =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "domain") in
      let clientId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "clientId") in
      make ?provider ?clientSecret ?domain ?clientId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let provider = field_map json__ "provider" GenericString.of_json in
      let clientSecret =
        field_map json__ "clientSecret" SensitiveString.of_json in
      let domain = field_map json__ "domain" GenericString.of_json in
      let clientId = field_map json__ "clientId" GenericString.of_json in
      make ?provider ?clientSecret ?domain ?clientId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Registers and saves OpenTDF configuration for a Wickr network, enabling attribute-based access control for Wickr through an OpenTDF provider."]
module RegisterOpentdfConfigRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network for which OpenTDF integration will be configured."];
      clientId: GenericString.t
        [@ocaml.doc
          "The OIDC client ID used for authenticating with the OpenTDF provider."];
      clientSecret: SensitiveString.t
        [@ocaml.doc
          "The OIDC client secret used for authenticating with the OpenTDF provider"];
      domain: GenericString.t
        [@ocaml.doc "The domain of the OpenTDF server."];
      provider: GenericString.t
        [@ocaml.doc
          "The provider of the OpenTDF platform. Currently only Virtru is supported as the OpenTDF provider."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Perform dry-run test connection of OpenTDF configuration (optional)."]}
    let context_ = "RegisterOpentdfConfigRequest"
    let make ?dryRun =
      fun ~networkId ->
        fun ~clientId ->
          fun ~clientSecret ->
            fun ~domain ->
              fun ~provider ->
                fun () ->
                  {
                    dryRun;
                    networkId;
                    clientId;
                    clientSecret;
                    domain;
                    provider
                  }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("clientId", (Some (GenericString.to_value x.clientId)));
        ("clientSecret", (Some (SensitiveString.to_value x.clientSecret)));
        ("domain", (Some (GenericString.to_value x.domain)));
        ("provider", (Some (GenericString.to_value x.provider)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let provider =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "provider") in
      let domain =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "domain") in
      let clientSecret =
        SensitiveString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "clientSecret") in
      let clientId =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "clientId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?dryRun ~provider ~domain ~clientSecret ~clientId ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "dryRun" Boolean.of_json in
      let provider = field_map_exn json__ "provider" GenericString.of_json in
      let domain = field_map_exn json__ "domain" GenericString.of_json in
      let clientSecret =
        field_map_exn json__ "clientSecret" SensitiveString.of_json in
      let clientId = field_map_exn json__ "clientId" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?dryRun ~provider ~domain ~clientSecret ~clientId ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Registers and saves OpenTDF configuration for a Wickr network, enabling attribute-based access control for Wickr through an OpenTDF provider."]
module RegisterOidcConfigTestResponse =
  struct
    type nonrec t =
      {
      tokenEndpoint: GenericString.t option
        [@ocaml.doc
          "The token endpoint URL discovered from the OIDC provider."];
      userinfoEndpoint: GenericString.t option
        [@ocaml.doc
          "The user info endpoint URL discovered from the OIDC provider."];
      responseTypesSupported: StringList.t option
        [@ocaml.doc
          "The OAuth response types supported by the OIDC provider."];
      scopesSupported: StringList.t option
        [@ocaml.doc "The OAuth scopes supported by the OIDC provider."];
      issuer: GenericString.t option
        [@ocaml.doc "The issuer URL confirmed by the OIDC provider."];
      authorizationEndpoint: GenericString.t option
        [@ocaml.doc
          "The authorization endpoint URL discovered from the OIDC provider."];
      endSessionEndpoint: GenericString.t option
        [@ocaml.doc
          "The end session endpoint URL for logging out users from the OIDC provider."];
      logoutEndpoint: GenericString.t option
        [@ocaml.doc "The logout endpoint URL for terminating user sessions."];
      grantTypesSupported: StringList.t option
        [@ocaml.doc "The OAuth grant types supported by the OIDC provider."];
      revocationEndpoint: GenericString.t option
        [@ocaml.doc
          "The token revocation endpoint URL for invalidating tokens."];
      tokenEndpointAuthMethodsSupported: StringList.t option
        [@ocaml.doc
          "The authentication methods supported by the token endpoint."];
      microsoftMultiRefreshToken: Boolean.t option
        [@ocaml.doc
          "Indicates whether the provider supports Microsoft multi-refresh tokens."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tokenEndpoint =
      fun ?userinfoEndpoint ->
        fun ?responseTypesSupported ->
          fun ?scopesSupported ->
            fun ?issuer ->
              fun ?authorizationEndpoint ->
                fun ?endSessionEndpoint ->
                  fun ?logoutEndpoint ->
                    fun ?grantTypesSupported ->
                      fun ?revocationEndpoint ->
                        fun ?tokenEndpointAuthMethodsSupported ->
                          fun ?microsoftMultiRefreshToken ->
                            fun () ->
                              {
                                tokenEndpoint;
                                userinfoEndpoint;
                                responseTypesSupported;
                                scopesSupported;
                                issuer;
                                authorizationEndpoint;
                                endSessionEndpoint;
                                logoutEndpoint;
                                grantTypesSupported;
                                revocationEndpoint;
                                tokenEndpointAuthMethodsSupported;
                                microsoftMultiRefreshToken
                              }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("tokenEndpoint",
           (Option.map x.tokenEndpoint ~f:GenericString.to_value));
        ("userinfoEndpoint",
          (Option.map x.userinfoEndpoint ~f:GenericString.to_value));
        ("responseTypesSupported",
          (Option.map x.responseTypesSupported ~f:StringList.to_value));
        ("scopesSupported",
          (Option.map x.scopesSupported ~f:StringList.to_value));
        ("issuer", (Option.map x.issuer ~f:GenericString.to_value));
        ("authorizationEndpoint",
          (Option.map x.authorizationEndpoint ~f:GenericString.to_value));
        ("endSessionEndpoint",
          (Option.map x.endSessionEndpoint ~f:GenericString.to_value));
        ("logoutEndpoint",
          (Option.map x.logoutEndpoint ~f:GenericString.to_value));
        ("grantTypesSupported",
          (Option.map x.grantTypesSupported ~f:StringList.to_value));
        ("revocationEndpoint",
          (Option.map x.revocationEndpoint ~f:GenericString.to_value));
        ("tokenEndpointAuthMethodsSupported",
          (Option.map x.tokenEndpointAuthMethodsSupported
             ~f:StringList.to_value));
        ("microsoftMultiRefreshToken",
          (Option.map x.microsoftMultiRefreshToken ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let microsoftMultiRefreshToken =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "microsoftMultiRefreshToken") in
      let tokenEndpointAuthMethodsSupported =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "tokenEndpointAuthMethodsSupported") in
      let revocationEndpoint =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "revocationEndpoint") in
      let grantTypesSupported =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "grantTypesSupported") in
      let logoutEndpoint =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "logoutEndpoint") in
      let endSessionEndpoint =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "endSessionEndpoint") in
      let authorizationEndpoint =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "authorizationEndpoint") in
      let issuer =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "issuer") in
      let scopesSupported =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "scopesSupported") in
      let responseTypesSupported =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "responseTypesSupported") in
      let userinfoEndpoint =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "userinfoEndpoint") in
      let tokenEndpoint =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "tokenEndpoint") in
      make ?microsoftMultiRefreshToken ?tokenEndpointAuthMethodsSupported
        ?revocationEndpoint ?grantTypesSupported ?logoutEndpoint
        ?endSessionEndpoint ?authorizationEndpoint ?issuer ?scopesSupported
        ?responseTypesSupported ?userinfoEndpoint ?tokenEndpoint ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let microsoftMultiRefreshToken =
        field_map json__ "microsoftMultiRefreshToken" Boolean.of_json in
      let tokenEndpointAuthMethodsSupported =
        field_map json__ "tokenEndpointAuthMethodsSupported"
          StringList.of_json in
      let revocationEndpoint =
        field_map json__ "revocationEndpoint" GenericString.of_json in
      let grantTypesSupported =
        field_map json__ "grantTypesSupported" StringList.of_json in
      let logoutEndpoint =
        field_map json__ "logoutEndpoint" GenericString.of_json in
      let endSessionEndpoint =
        field_map json__ "endSessionEndpoint" GenericString.of_json in
      let authorizationEndpoint =
        field_map json__ "authorizationEndpoint" GenericString.of_json in
      let issuer = field_map json__ "issuer" GenericString.of_json in
      let scopesSupported =
        field_map json__ "scopesSupported" StringList.of_json in
      let responseTypesSupported =
        field_map json__ "responseTypesSupported" StringList.of_json in
      let userinfoEndpoint =
        field_map json__ "userinfoEndpoint" GenericString.of_json in
      let tokenEndpoint =
        field_map json__ "tokenEndpoint" GenericString.of_json in
      make ?microsoftMultiRefreshToken ?tokenEndpointAuthMethodsSupported
        ?revocationEndpoint ?grantTypesSupported ?logoutEndpoint
        ?endSessionEndpoint ?authorizationEndpoint ?issuer ?scopesSupported
        ?responseTypesSupported ?userinfoEndpoint ?tokenEndpoint ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Tests an OpenID Connect (OIDC) configuration for a Wickr network by validating the connection to the identity provider and retrieving its supported capabilities."]
module RegisterOidcConfigTestRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network for which the OIDC configuration will be tested."];
      extraAuthParams: GenericString.t option
        [@ocaml.doc
          "Additional authentication parameters to include in the test (optional)."];
      issuer: GenericString.t
        [@ocaml.doc "The issuer URL of the OIDC provider to test."];
      scopes: GenericString.t
        [@ocaml.doc "The OAuth scopes to test with the OIDC provider."];
      certificate: GenericString.t option
        [@ocaml.doc
          "The CA certificate for secure communication with the OIDC provider (optional)."]}
    let context_ = "RegisterOidcConfigTestRequest"
    let make ?extraAuthParams =
      fun ?certificate ->
        fun ~networkId ->
          fun ~issuer ->
            fun ~scopes ->
              fun () ->
                { extraAuthParams; certificate; networkId; issuer; scopes }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("extraAuthParams",
          (Option.map x.extraAuthParams ~f:GenericString.to_value));
        ("issuer", (Some (GenericString.to_value x.issuer)));
        ("scopes", (Some (GenericString.to_value x.scopes)));
        ("certificate", (Option.map x.certificate ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let certificate =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "certificate") in
      let scopes =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "scopes") in
      let issuer =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "issuer") in
      let extraAuthParams =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "extraAuthParams") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?certificate ~scopes ~issuer ?extraAuthParams ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let certificate = field_map json__ "certificate" GenericString.of_json in
      let scopes = field_map_exn json__ "scopes" GenericString.of_json in
      let issuer = field_map_exn json__ "issuer" GenericString.of_json in
      let extraAuthParams =
        field_map json__ "extraAuthParams" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?certificate ~scopes ~issuer ?extraAuthParams ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Tests an OpenID Connect (OIDC) configuration for a Wickr network by validating the connection to the identity provider and retrieving its supported capabilities."]
module RegisterOidcConfigResponse =
  struct
    type nonrec t =
      {
      applicationName: GenericString.t option
        [@ocaml.doc "The name of the registered OIDC application."];
      clientId: GenericString.t option
        [@ocaml.doc "The OAuth client ID assigned to the application."];
      companyId: GenericString.t option
        [@ocaml.doc
          "Custom identifier your end users will use to sign in with SSO."];
      scopes: GenericString.t option
        [@ocaml.doc "The OAuth scopes configured for the application."];
      issuer: GenericString.t option
        [@ocaml.doc "The issuer URL of the OIDC provider."];
      clientSecret: SensitiveString.t option
        [@ocaml.doc "The OAuth client secret for the application."];
      secret: SensitiveString.t option
        [@ocaml.doc
          "The client secret for authenticating with the OIDC provider."];
      redirectUrl: GenericString.t option
        [@ocaml.doc "The redirect URL configured for the OAuth flow."];
      userId: GenericString.t option
        [@ocaml.doc "The claim field being used as the user identifier."];
      customUsername: GenericString.t option
        [@ocaml.doc
          "The custom field mapping used for extracting the username."];
      caCertificate: GenericString.t option
        [@ocaml.doc
          "The CA certificate used for secure communication with the OIDC provider."];
      applicationId: RegisterOidcConfigResponseApplicationIdInteger.t option
        [@ocaml.doc
          "The unique identifier for the registered OIDC application."];
      ssoTokenBufferMinutes: Integer.t option
        [@ocaml.doc
          "The buffer time in minutes before the SSO token expires."];
      extraAuthParams: GenericString.t option
        [@ocaml.doc
          "The additional authentication parameters configured for the OIDC flow."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?applicationName =
      fun ?clientId ->
        fun ?companyId ->
          fun ?scopes ->
            fun ?issuer ->
              fun ?clientSecret ->
                fun ?secret ->
                  fun ?redirectUrl ->
                    fun ?userId ->
                      fun ?customUsername ->
                        fun ?caCertificate ->
                          fun ?applicationId ->
                            fun ?ssoTokenBufferMinutes ->
                              fun ?extraAuthParams ->
                                fun () ->
                                  {
                                    applicationName;
                                    clientId;
                                    companyId;
                                    scopes;
                                    issuer;
                                    clientSecret;
                                    secret;
                                    redirectUrl;
                                    userId;
                                    customUsername;
                                    caCertificate;
                                    applicationId;
                                    ssoTokenBufferMinutes;
                                    extraAuthParams
                                  }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("applicationName",
           (Option.map x.applicationName ~f:GenericString.to_value));
        ("clientId", (Option.map x.clientId ~f:GenericString.to_value));
        ("companyId", (Option.map x.companyId ~f:GenericString.to_value));
        ("scopes", (Option.map x.scopes ~f:GenericString.to_value));
        ("issuer", (Option.map x.issuer ~f:GenericString.to_value));
        ("clientSecret",
          (Option.map x.clientSecret ~f:SensitiveString.to_value));
        ("secret", (Option.map x.secret ~f:SensitiveString.to_value));
        ("redirectUrl", (Option.map x.redirectUrl ~f:GenericString.to_value));
        ("userId", (Option.map x.userId ~f:GenericString.to_value));
        ("customUsername",
          (Option.map x.customUsername ~f:GenericString.to_value));
        ("caCertificate",
          (Option.map x.caCertificate ~f:GenericString.to_value));
        ("applicationId",
          (Option.map x.applicationId
             ~f:RegisterOidcConfigResponseApplicationIdInteger.to_value));
        ("ssoTokenBufferMinutes",
          (Option.map x.ssoTokenBufferMinutes ~f:Integer.to_value));
        ("extraAuthParams",
          (Option.map x.extraAuthParams ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let extraAuthParams =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "extraAuthParams") in
      let ssoTokenBufferMinutes =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ssoTokenBufferMinutes") in
      let applicationId =
        (Option.map ~f:RegisterOidcConfigResponseApplicationIdInteger.of_xml)
          (Xml.child xml_arg0 "applicationId") in
      let caCertificate =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "caCertificate") in
      let customUsername =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "customUsername") in
      let userId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "userId") in
      let redirectUrl =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "redirectUrl") in
      let secret =
        (Option.map ~f:SensitiveString.of_xml) (Xml.child xml_arg0 "secret") in
      let clientSecret =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "clientSecret") in
      let issuer =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "issuer") in
      let scopes =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "scopes") in
      let companyId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "companyId") in
      let clientId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "clientId") in
      let applicationName =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "applicationName") in
      make ?extraAuthParams ?ssoTokenBufferMinutes ?applicationId
        ?caCertificate ?customUsername ?userId ?redirectUrl ?secret
        ?clientSecret ?issuer ?scopes ?companyId ?clientId ?applicationName
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let extraAuthParams =
        field_map json__ "extraAuthParams" GenericString.of_json in
      let ssoTokenBufferMinutes =
        field_map json__ "ssoTokenBufferMinutes" Integer.of_json in
      let applicationId =
        field_map json__ "applicationId"
          RegisterOidcConfigResponseApplicationIdInteger.of_json in
      let caCertificate =
        field_map json__ "caCertificate" GenericString.of_json in
      let customUsername =
        field_map json__ "customUsername" GenericString.of_json in
      let userId = field_map json__ "userId" GenericString.of_json in
      let redirectUrl = field_map json__ "redirectUrl" GenericString.of_json in
      let secret = field_map json__ "secret" SensitiveString.of_json in
      let clientSecret =
        field_map json__ "clientSecret" SensitiveString.of_json in
      let issuer = field_map json__ "issuer" GenericString.of_json in
      let scopes = field_map json__ "scopes" GenericString.of_json in
      let companyId = field_map json__ "companyId" GenericString.of_json in
      let clientId = field_map json__ "clientId" GenericString.of_json in
      let applicationName =
        field_map json__ "applicationName" GenericString.of_json in
      make ?extraAuthParams ?ssoTokenBufferMinutes ?applicationId
        ?caCertificate ?customUsername ?userId ?redirectUrl ?secret
        ?clientSecret ?issuer ?scopes ?companyId ?clientId ?applicationName
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Registers and saves an OpenID Connect (OIDC) configuration for a Wickr network, enabling Single Sign-On (SSO) authentication through an identity provider."]
module RegisterOidcConfigRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network for which OIDC will be configured."];
      companyId: GenericString.t
        [@ocaml.doc
          "Custom identifier your end users will use to sign in with SSO."];
      customUsername: GenericString.t option
        [@ocaml.doc
          "A custom field mapping to extract the username from the OIDC token (optional). The customUsername is only required if you use something other than email as the username field."];
      extraAuthParams: GenericString.t option
        [@ocaml.doc
          "Additional authentication parameters to include in the OIDC flow (optional)."];
      issuer: GenericString.t
        [@ocaml.doc
          "The issuer URL of the OIDC provider (e.g., 'https://login.example.com')."];
      scopes: GenericString.t
        [@ocaml.doc
          "The OAuth scopes to request from the OIDC provider (e.g., 'openid profile email')."];
      secret: SensitiveString.t option
        [@ocaml.doc
          "The client secret for authenticating with the OIDC provider (optional)."];
      ssoTokenBufferMinutes: Integer.t option
        [@ocaml.doc
          "The buffer time in minutes before the SSO token expires to refresh it (optional)."];
      userId: GenericString.t option
        [@ocaml.doc
          "Unique identifier provided by your identity provider to authenticate the access request. Also referred to as clientID."]}
    let context_ = "RegisterOidcConfigRequest"
    let make ?customUsername =
      fun ?extraAuthParams ->
        fun ?secret ->
          fun ?ssoTokenBufferMinutes ->
            fun ?userId ->
              fun ~networkId ->
                fun ~companyId ->
                  fun ~issuer ->
                    fun ~scopes ->
                      fun () ->
                        {
                          customUsername;
                          extraAuthParams;
                          secret;
                          ssoTokenBufferMinutes;
                          userId;
                          networkId;
                          companyId;
                          issuer;
                          scopes
                        }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("companyId", (Some (GenericString.to_value x.companyId)));
        ("customUsername",
          (Option.map x.customUsername ~f:GenericString.to_value));
        ("extraAuthParams",
          (Option.map x.extraAuthParams ~f:GenericString.to_value));
        ("issuer", (Some (GenericString.to_value x.issuer)));
        ("scopes", (Some (GenericString.to_value x.scopes)));
        ("secret", (Option.map x.secret ~f:SensitiveString.to_value));
        ("ssoTokenBufferMinutes",
          (Option.map x.ssoTokenBufferMinutes ~f:Integer.to_value));
        ("userId", (Option.map x.userId ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "userId") in
      let ssoTokenBufferMinutes =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ssoTokenBufferMinutes") in
      let secret =
        (Option.map ~f:SensitiveString.of_xml) (Xml.child xml_arg0 "secret") in
      let scopes =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "scopes") in
      let issuer =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "issuer") in
      let extraAuthParams =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "extraAuthParams") in
      let customUsername =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "customUsername") in
      let companyId =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "companyId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?userId ?ssoTokenBufferMinutes ?secret ~scopes ~issuer
        ?extraAuthParams ?customUsername ~companyId ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let userId = field_map json__ "userId" GenericString.of_json in
      let ssoTokenBufferMinutes =
        field_map json__ "ssoTokenBufferMinutes" Integer.of_json in
      let secret = field_map json__ "secret" SensitiveString.of_json in
      let scopes = field_map_exn json__ "scopes" GenericString.of_json in
      let issuer = field_map_exn json__ "issuer" GenericString.of_json in
      let extraAuthParams =
        field_map json__ "extraAuthParams" GenericString.of_json in
      let customUsername =
        field_map json__ "customUsername" GenericString.of_json in
      let companyId = field_map_exn json__ "companyId" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?userId ?ssoTokenBufferMinutes ?secret ~scopes ~issuer
        ?extraAuthParams ?customUsername ~companyId ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Registers and saves an OpenID Connect (OIDC) configuration for a Wickr network, enabling Single Sign-On (SSO) authentication through an identity provider."]
module ListUsersResponse =
  struct
    type nonrec t =
      {
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token to use for retrieving the next page of results. If this is not present, there are no more results."];
      users: Users.t option
        [@ocaml.doc
          "A list of user objects matching the specified filters and within the current page."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?users -> fun () -> { nextToken; users }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:GenericString.to_value));
        ("users", (Option.map x.users ~f:Users.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let users = (Option.map ~f:Users.of_xml) (Xml.child xml_arg0 "users") in
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?users ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let users = field_map json__ "users" Users.of_json in
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      make ?users ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of users in a specified Wickr network. You can filter and sort the results based on various criteria such as name, status, or security group membership."]
module ListUsersRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc "The ID of the Wickr network from which to list users."];
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token for retrieving the next page of results. This is returned from a previous request when there are more results available."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of users to return in a single page. Valid range is 1-100. Default is 10."];
      sortFields: GenericString.t option
        [@ocaml.doc
          "The fields to sort users by. Multiple fields can be specified by separating them with '+'. Accepted values include 'username', 'firstName', 'lastName', 'status', and 'groupId'."];
      sortDirection: SortDirection.t option
        [@ocaml.doc
          "The direction to sort results. Valid values are 'ASC' (ascending) or 'DESC' (descending). Default is 'DESC'."];
      firstName: SensitiveString.t option
        [@ocaml.doc
          "Filter results to only include users with first names matching this value."];
      lastName: SensitiveString.t option
        [@ocaml.doc
          "Filter results to only include users with last names matching this value."];
      username: GenericString.t option
        [@ocaml.doc
          "Filter results to only include users with usernames matching this value."];
      status: UserStatus.t option
        [@ocaml.doc
          "Filter results to only include users with this status (1 for pending, 2 for active)."];
      groupId: GenericString.t option
        [@ocaml.doc
          "Filter results to only include users belonging to this security group."]}
    let context_ = "ListUsersRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ?sortFields ->
          fun ?sortDirection ->
            fun ?firstName ->
              fun ?lastName ->
                fun ?username ->
                  fun ?status ->
                    fun ?groupId ->
                      fun ~networkId ->
                        fun () ->
                          {
                            nextToken;
                            maxResults;
                            sortFields;
                            sortDirection;
                            firstName;
                            lastName;
                            username;
                            status;
                            groupId;
                            networkId
                          }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("nextToken", (Option.map x.nextToken ~f:GenericString.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("sortFields", (Option.map x.sortFields ~f:GenericString.to_value));
        ("sortDirection",
          (Option.map x.sortDirection ~f:SortDirection.to_value));
        ("firstName", (Option.map x.firstName ~f:SensitiveString.to_value));
        ("lastName", (Option.map x.lastName ~f:SensitiveString.to_value));
        ("username", (Option.map x.username ~f:GenericString.to_value));
        ("status", (Option.map x.status ~f:UserStatus.to_value));
        ("groupId", (Option.map x.groupId ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "groupId") in
      let status =
        (Option.map ~f:UserStatus.of_xml) (Xml.child xml_arg0 "status") in
      let username =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "username") in
      let lastName =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "lastName") in
      let firstName =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "firstName") in
      let sortDirection =
        (Option.map ~f:SortDirection.of_xml)
          (Xml.child xml_arg0 "sortDirection") in
      let sortFields =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "sortFields") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?groupId ?status ?username ?lastName ?firstName ?sortDirection
        ?sortFields ?maxResults ?nextToken ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let groupId = field_map json__ "groupId" GenericString.of_json in
      let status = field_map json__ "status" UserStatus.of_json in
      let username = field_map json__ "username" GenericString.of_json in
      let lastName = field_map json__ "lastName" SensitiveString.of_json in
      let firstName = field_map json__ "firstName" SensitiveString.of_json in
      let sortDirection =
        field_map json__ "sortDirection" SortDirection.of_json in
      let sortFields = field_map json__ "sortFields" GenericString.of_json in
      let maxResults = field_map json__ "maxResults" Integer.of_json in
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?groupId ?status ?username ?lastName ?firstName ?sortDirection
        ?sortFields ?maxResults ?nextToken ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of users in a specified Wickr network. You can filter and sort the results based on various criteria such as name, status, or security group membership."]
module ListSecurityGroupsResponse =
  struct
    type nonrec t =
      {
      securityGroups: SecurityGroupList.t option
        [@ocaml.doc "A list of security group objects in the current page."];
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token to use for retrieving the next page of results. If this is not present, there are no more results."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?securityGroups =
      fun ?nextToken -> fun () -> { securityGroups; nextToken }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("securityGroups",
           (Option.map x.securityGroups ~f:SecurityGroupList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      let securityGroups =
        (Option.map ~f:SecurityGroupList.of_xml)
          (Xml.child xml_arg0 "securityGroups") in
      make ?nextToken ?securityGroups ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      let securityGroups =
        field_map json__ "securityGroups" SecurityGroupList.of_json in
      make ?nextToken ?securityGroups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of security groups in a specified Wickr network. You can sort the results by various criteria."]
module ListSecurityGroupsRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network from which to list security groups."];
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token for retrieving the next page of results. This is returned from a previous request when there are more results available."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of security groups to return in a single page. Valid range is 1-100. Default is 10."];
      sortFields: GenericString.t option
        [@ocaml.doc
          "The field to sort security groups by. Accepted values include 'id' and 'name'."];
      sortDirection: SortDirection.t option
        [@ocaml.doc
          "The direction to sort results. Valid values are 'ASC' (ascending) or 'DESC' (descending). Default is 'DESC'."]}
    let context_ = "ListSecurityGroupsRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ?sortFields ->
          fun ?sortDirection ->
            fun ~networkId ->
              fun () ->
                { nextToken; maxResults; sortFields; sortDirection; networkId
                }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("nextToken", (Option.map x.nextToken ~f:GenericString.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("sortFields", (Option.map x.sortFields ~f:GenericString.to_value));
        ("sortDirection",
          (Option.map x.sortDirection ~f:SortDirection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortDirection =
        (Option.map ~f:SortDirection.of_xml)
          (Xml.child xml_arg0 "sortDirection") in
      let sortFields =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "sortFields") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?sortDirection ?sortFields ?maxResults ?nextToken ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortDirection =
        field_map json__ "sortDirection" SortDirection.of_json in
      let sortFields = field_map json__ "sortFields" GenericString.of_json in
      let maxResults = field_map json__ "maxResults" Integer.of_json in
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?sortDirection ?sortFields ?maxResults ?nextToken ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of security groups in a specified Wickr network. You can sort the results by various criteria."]
module ListSecurityGroupUsersResponse =
  struct
    type nonrec t =
      {
      users: Users.t option
        [@ocaml.doc
          "A list of user objects belonging to the security group within the current page."];
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token to use for retrieving the next page of results. If this is not present, there are no more results."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?users = fun ?nextToken -> fun () -> { users; nextToken }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("users", (Option.map x.users ~f:Users.to_value));
        ("nextToken", (Option.map x.nextToken ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      let users = (Option.map ~f:Users.of_xml) (Xml.child xml_arg0 "users") in
      make ?nextToken ?users ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      let users = field_map json__ "users" Users.of_json in
      make ?nextToken ?users ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of users who belong to a specific security group in a Wickr network."]
module ListSecurityGroupUsersRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network containing the security group."];
      groupId: GenericString.t
        [@ocaml.doc
          "The unique identifier of the security group whose users will be listed."];
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token for retrieving the next page of results. This is returned from a previous request when there are more results available."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of users to return in a single page. Valid range is 1-100. Default is 10."];
      sortFields: GenericString.t option
        [@ocaml.doc
          "The field to sort users by. Multiple fields can be specified by separating them with '+'. Accepted values include 'username', 'firstName', and 'lastName'."];
      sortDirection: SortDirection.t option
        [@ocaml.doc
          "The direction to sort results. Valid values are 'ASC' (ascending) or 'DESC' (descending). Default is 'DESC'."]}
    let context_ = "ListSecurityGroupUsersRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ?sortFields ->
          fun ?sortDirection ->
            fun ~networkId ->
              fun ~groupId ->
                fun () ->
                  {
                    nextToken;
                    maxResults;
                    sortFields;
                    sortDirection;
                    networkId;
                    groupId
                  }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("groupId", (Some (GenericString.to_value x.groupId)));
        ("nextToken", (Option.map x.nextToken ~f:GenericString.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("sortFields", (Option.map x.sortFields ~f:GenericString.to_value));
        ("sortDirection",
          (Option.map x.sortDirection ~f:SortDirection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortDirection =
        (Option.map ~f:SortDirection.of_xml)
          (Xml.child xml_arg0 "sortDirection") in
      let sortFields =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "sortFields") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      let groupId =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "groupId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?sortDirection ?sortFields ?maxResults ?nextToken ~groupId
        ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortDirection =
        field_map json__ "sortDirection" SortDirection.of_json in
      let sortFields = field_map json__ "sortFields" GenericString.of_json in
      let maxResults = field_map json__ "maxResults" Integer.of_json in
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      let groupId = field_map_exn json__ "groupId" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?sortDirection ?sortFields ?maxResults ?nextToken ~groupId
        ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of users who belong to a specific security group in a Wickr network."]
module ListNetworksResponse =
  struct
    type nonrec t =
      {
      networks: NetworkList.t option
        [@ocaml.doc
          "A list of network objects for the Amazon Web Services account."];
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token to use for retrieving the next page of results. If this is not present, there are no more results."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?networks = fun ?nextToken -> fun () -> { networks; nextToken }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networks", (Option.map x.networks ~f:NetworkList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networks =
        (Option.map ~f:NetworkList.of_xml) (Xml.child xml_arg0 "networks") in
      make ?nextToken ?networks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      let networks = field_map json__ "networks" NetworkList.of_json in
      make ?nextToken ?networks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of all Wickr networks associated with your Amazon Web Services account. You can sort the results by network ID or name."]
module ListNetworksRequest =
  struct
    type nonrec t =
      {
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of networks to return in a single page. Valid range is 1-100. Default is 10."];
      sortFields: GenericString.t option
        [@ocaml.doc
          "The field to sort networks by. Accepted values are 'networkId' and 'networkName'. Default is 'networkId'."];
      sortDirection: SortDirection.t option
        [@ocaml.doc
          "The direction to sort results. Valid values are 'ASC' (ascending) or 'DESC' (descending). Default is 'DESC'."];
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token for retrieving the next page of results. This is returned from a previous request when there are more results available."]}
    let make ?maxResults =
      fun ?sortFields ->
        fun ?sortDirection ->
          fun ?nextToken ->
            fun () -> { maxResults; sortFields; sortDirection; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("sortFields", (Option.map x.sortFields ~f:GenericString.to_value));
        ("sortDirection",
          (Option.map x.sortDirection ~f:SortDirection.to_value));
        ("nextToken", (Option.map x.nextToken ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      let sortDirection =
        (Option.map ~f:SortDirection.of_xml)
          (Xml.child xml_arg0 "sortDirection") in
      let sortFields =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "sortFields") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?sortDirection ?sortFields ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      let sortDirection =
        field_map json__ "sortDirection" SortDirection.of_json in
      let sortFields = field_map json__ "sortFields" GenericString.of_json in
      let maxResults = field_map json__ "maxResults" Integer.of_json in
      make ?nextToken ?sortDirection ?sortFields ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of all Wickr networks associated with your Amazon Web Services account. You can sort the results by network ID or name."]
module ListGuestUsersResponse =
  struct
    type nonrec t =
      {
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token to use for retrieving the next page of results. If this is not present, there are no more results."];
      guestlist: GuestUserList.t option
        [@ocaml.doc "A list of guest user objects within the current page."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?guestlist -> fun () -> { nextToken; guestlist }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:GenericString.to_value));
        ("guestlist", (Option.map x.guestlist ~f:GuestUserList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let guestlist =
        (Option.map ~f:GuestUserList.of_xml) (Xml.child xml_arg0 "guestlist") in
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?guestlist ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let guestlist = field_map json__ "guestlist" GuestUserList.of_json in
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      make ?guestlist ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of guest users who have communicated with your Wickr network. Guest users are external users from federated networks who can communicate with network members."]
module ListGuestUsersRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network from which to list guest users."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of guest users to return in a single page. Valid range is 1-100. Default is 10."];
      sortDirection: SortDirection.t option
        [@ocaml.doc
          "The direction to sort results. Valid values are 'ASC' (ascending) or 'DESC' (descending). Default is 'DESC'."];
      sortFields: GenericString.t option
        [@ocaml.doc
          "The field to sort guest users by. Accepted values include 'username' and 'billingPeriod'."];
      username: GenericString.t option
        [@ocaml.doc
          "Filter results to only include guest users with usernames matching this value."];
      billingPeriod: GenericString.t option
        [@ocaml.doc
          "Filter results to only include guest users from this billing period (e.g., '2024-01')."];
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token for retrieving the next page of results. This is returned from a previous request when there are more results available."]}
    let context_ = "ListGuestUsersRequest"
    let make ?maxResults =
      fun ?sortDirection ->
        fun ?sortFields ->
          fun ?username ->
            fun ?billingPeriod ->
              fun ?nextToken ->
                fun ~networkId ->
                  fun () ->
                    {
                      maxResults;
                      sortDirection;
                      sortFields;
                      username;
                      billingPeriod;
                      nextToken;
                      networkId
                    }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("sortDirection",
          (Option.map x.sortDirection ~f:SortDirection.to_value));
        ("sortFields", (Option.map x.sortFields ~f:GenericString.to_value));
        ("username", (Option.map x.username ~f:GenericString.to_value));
        ("billingPeriod",
          (Option.map x.billingPeriod ~f:GenericString.to_value));
        ("nextToken", (Option.map x.nextToken ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      let billingPeriod =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "billingPeriod") in
      let username =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "username") in
      let sortFields =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "sortFields") in
      let sortDirection =
        (Option.map ~f:SortDirection.of_xml)
          (Xml.child xml_arg0 "sortDirection") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?nextToken ?billingPeriod ?username ?sortFields ?sortDirection
        ?maxResults ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      let billingPeriod =
        field_map json__ "billingPeriod" GenericString.of_json in
      let username = field_map json__ "username" GenericString.of_json in
      let sortFields = field_map json__ "sortFields" GenericString.of_json in
      let sortDirection =
        field_map json__ "sortDirection" SortDirection.of_json in
      let maxResults = field_map json__ "maxResults" Integer.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?nextToken ?billingPeriod ?username ?sortFields ?sortDirection
        ?maxResults ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of guest users who have communicated with your Wickr network. Guest users are external users from federated networks who can communicate with network members."]
module ListDevicesForUserResponse =
  struct
    type nonrec t =
      {
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token to use for retrieving the next page of results. If this is not present, there are no more results."];
      devices: Devices.t option
        [@ocaml.doc
          "A list of device objects associated with the user within the current page."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?devices -> fun () -> { nextToken; devices }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:GenericString.to_value));
        ("devices", (Option.map x.devices ~f:Devices.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let devices =
        (Option.map ~f:Devices.of_xml) (Xml.child xml_arg0 "devices") in
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?devices ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let devices = field_map json__ "devices" Devices.of_json in
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      make ?devices ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of devices associated with a specific user in a Wickr network. This operation returns information about all devices where the user has logged into Wickr."]
module ListDevicesForUserRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc "The ID of the Wickr network containing the user."];
      userId: UserId.t
        [@ocaml.doc
          "The unique identifier of the user whose devices will be listed."];
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token for retrieving the next page of results. This is returned from a previous request when there are more results available."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of devices to return in a single page. Valid range is 1-100. Default is 10."];
      sortFields: GenericString.t option
        [@ocaml.doc
          "The fields to sort devices by. Multiple fields can be specified by separating them with '+'. Accepted values include 'lastlogin', 'type', 'suspend', and 'created'."];
      sortDirection: SortDirection.t option
        [@ocaml.doc
          "The direction to sort results. Valid values are 'ASC' (ascending) or 'DESC' (descending). Default is 'DESC'."]}
    let context_ = "ListDevicesForUserRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ?sortFields ->
          fun ?sortDirection ->
            fun ~networkId ->
              fun ~userId ->
                fun () ->
                  {
                    nextToken;
                    maxResults;
                    sortFields;
                    sortDirection;
                    networkId;
                    userId
                  }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("userId", (Some (UserId.to_value x.userId)));
        ("nextToken", (Option.map x.nextToken ~f:GenericString.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("sortFields", (Option.map x.sortFields ~f:GenericString.to_value));
        ("sortDirection",
          (Option.map x.sortDirection ~f:SortDirection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortDirection =
        (Option.map ~f:SortDirection.of_xml)
          (Xml.child xml_arg0 "sortDirection") in
      let sortFields =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "sortFields") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      let userId =
        UserId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "userId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?sortDirection ?sortFields ?maxResults ?nextToken ~userId
        ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortDirection =
        field_map json__ "sortDirection" SortDirection.of_json in
      let sortFields = field_map json__ "sortFields" GenericString.of_json in
      let maxResults = field_map json__ "maxResults" Integer.of_json in
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      let userId = field_map_exn json__ "userId" UserId.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?sortDirection ?sortFields ?maxResults ?nextToken ~userId
        ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of devices associated with a specific user in a Wickr network. This operation returns information about all devices where the user has logged into Wickr."]
module ListBotsResponse =
  struct
    type nonrec t =
      {
      bots: Bots.t option
        [@ocaml.doc
          "A list of bot objects matching the specified filters and within the current page."];
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token to use for retrieving the next page of results. If this is not present, there are no more results."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?bots = fun ?nextToken -> fun () -> { bots; nextToken }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("bots", (Option.map x.bots ~f:Bots.to_value));
        ("nextToken", (Option.map x.nextToken ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      let bots = (Option.map ~f:Bots.of_xml) (Xml.child xml_arg0 "bots") in
      make ?nextToken ?bots ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      let bots = field_map json__ "bots" Bots.of_json in
      make ?nextToken ?bots ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of bots in a specified Wickr network. You can filter and sort the results based on various criteria."]
module ListBotsRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc "The ID of the Wickr network from which to list bots."];
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token for retrieving the next page of results. This is returned from a previous request when there are more results available."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of bots to return in a single page. Valid range is 1-100. Default is 10."];
      sortFields: GenericString.t option
        [@ocaml.doc
          "The fields to sort bots by. Multiple fields can be specified by separating them with '+'. Accepted values include 'username', 'firstName', 'displayName', 'status', and 'groupId'."];
      sortDirection: SortDirection.t option
        [@ocaml.doc
          "The direction to sort results. Valid values are 'ASC' (ascending) or 'DESC' (descending). Default is 'DESC'."];
      displayName: GenericString.t option
        [@ocaml.doc
          "Filter results to only include bots with display names matching this value."];
      username: GenericString.t option
        [@ocaml.doc
          "Filter results to only include bots with usernames matching this value."];
      status: BotStatus.t option
        [@ocaml.doc
          "Filter results to only include bots with this status (1 for pending, 2 for active)."];
      groupId: GenericString.t option
        [@ocaml.doc
          "Filter results to only include bots belonging to this security group."]}
    let context_ = "ListBotsRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ?sortFields ->
          fun ?sortDirection ->
            fun ?displayName ->
              fun ?username ->
                fun ?status ->
                  fun ?groupId ->
                    fun ~networkId ->
                      fun () ->
                        {
                          nextToken;
                          maxResults;
                          sortFields;
                          sortDirection;
                          displayName;
                          username;
                          status;
                          groupId;
                          networkId
                        }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("nextToken", (Option.map x.nextToken ~f:GenericString.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("sortFields", (Option.map x.sortFields ~f:GenericString.to_value));
        ("sortDirection",
          (Option.map x.sortDirection ~f:SortDirection.to_value));
        ("displayName", (Option.map x.displayName ~f:GenericString.to_value));
        ("username", (Option.map x.username ~f:GenericString.to_value));
        ("status", (Option.map x.status ~f:BotStatus.to_value));
        ("groupId", (Option.map x.groupId ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "groupId") in
      let status =
        (Option.map ~f:BotStatus.of_xml) (Xml.child xml_arg0 "status") in
      let username =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "username") in
      let displayName =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "displayName") in
      let sortDirection =
        (Option.map ~f:SortDirection.of_xml)
          (Xml.child xml_arg0 "sortDirection") in
      let sortFields =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "sortFields") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?groupId ?status ?username ?displayName ?sortDirection ?sortFields
        ?maxResults ?nextToken ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let groupId = field_map json__ "groupId" GenericString.of_json in
      let status = field_map json__ "status" BotStatus.of_json in
      let username = field_map json__ "username" GenericString.of_json in
      let displayName = field_map json__ "displayName" GenericString.of_json in
      let sortDirection =
        field_map json__ "sortDirection" SortDirection.of_json in
      let sortFields = field_map json__ "sortFields" GenericString.of_json in
      let maxResults = field_map json__ "maxResults" Integer.of_json in
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?groupId ?status ?username ?displayName ?sortDirection ?sortFields
        ?maxResults ?nextToken ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of bots in a specified Wickr network. You can filter and sort the results based on various criteria."]
module ListBlockedGuestUsersResponse =
  struct
    type nonrec t =
      {
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token to use for retrieving the next page of results. If this is not present, there are no more results."];
      blocklist: BlockedGuestUserList.t option
        [@ocaml.doc
          "A list of blocked guest user objects within the current page."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?blocklist -> fun () -> { nextToken; blocklist }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:GenericString.to_value));
        ("blocklist",
          (Option.map x.blocklist ~f:BlockedGuestUserList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let blocklist =
        (Option.map ~f:BlockedGuestUserList.of_xml)
          (Xml.child xml_arg0 "blocklist") in
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?blocklist ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let blocklist =
        field_map json__ "blocklist" BlockedGuestUserList.of_json in
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      make ?blocklist ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of guest users who have been blocked from a Wickr network. You can filter and sort the results."]
module ListBlockedGuestUsersRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network from which to list blocked guest users."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of blocked guest users to return in a single page. Valid range is 1-100. Default is 10."];
      sortDirection: SortDirection.t option
        [@ocaml.doc
          "The direction to sort results. Valid values are 'ASC' (ascending) or 'DESC' (descending). Default is 'DESC'."];
      sortFields: GenericString.t option
        [@ocaml.doc
          "The field to sort blocked guest users by. Accepted values include 'username', 'admin', and 'modified'."];
      username: GenericString.t option
        [@ocaml.doc
          "Filter results to only include blocked guest users with usernames matching this value."];
      admin: GenericString.t option
        [@ocaml.doc
          "Filter results to only include blocked guest users that were blocked by this administrator."];
      nextToken: GenericString.t option
        [@ocaml.doc
          "The token for retrieving the next page of results. This is returned from a previous request when there are more results available."]}
    let context_ = "ListBlockedGuestUsersRequest"
    let make ?maxResults =
      fun ?sortDirection ->
        fun ?sortFields ->
          fun ?username ->
            fun ?admin ->
              fun ?nextToken ->
                fun ~networkId ->
                  fun () ->
                    {
                      maxResults;
                      sortDirection;
                      sortFields;
                      username;
                      admin;
                      nextToken;
                      networkId
                    }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("sortDirection",
          (Option.map x.sortDirection ~f:SortDirection.to_value));
        ("sortFields", (Option.map x.sortFields ~f:GenericString.to_value));
        ("username", (Option.map x.username ~f:GenericString.to_value));
        ("admin", (Option.map x.admin ~f:GenericString.to_value));
        ("nextToken", (Option.map x.nextToken ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "nextToken") in
      let admin =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "admin") in
      let username =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "username") in
      let sortFields =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "sortFields") in
      let sortDirection =
        (Option.map ~f:SortDirection.of_xml)
          (Xml.child xml_arg0 "sortDirection") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?nextToken ?admin ?username ?sortFields ?sortDirection ?maxResults
        ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" GenericString.of_json in
      let admin = field_map json__ "admin" GenericString.of_json in
      let username = field_map json__ "username" GenericString.of_json in
      let sortFields = field_map json__ "sortFields" GenericString.of_json in
      let sortDirection =
        field_map json__ "sortDirection" SortDirection.of_json in
      let maxResults = field_map json__ "maxResults" Integer.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?nextToken ?admin ?username ?sortFields ?sortDirection ?maxResults
        ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a paginated list of guest users who have been blocked from a Wickr network. You can filter and sort the results."]
module GetUsersCountResponse =
  struct
    type nonrec t =
      {
      pending: Integer.t option
        [@ocaml.doc
          "The number of users with pending status (invited but not yet accepted)."];
      active: Integer.t option
        [@ocaml.doc "The number of users with active status in the network."];
      rejected: Integer.t option
        [@ocaml.doc
          "The number of users who have rejected network invitations."];
      remaining: Integer.t option
        [@ocaml.doc
          "The number of additional users that can be added to the network while maintaining premium free trial eligibility."];
      total: Integer.t option
        [@ocaml.doc
          "The total number of users in the network (active and pending combined)."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?pending =
      fun ?active ->
        fun ?rejected ->
          fun ?remaining ->
            fun ?total ->
              fun () -> { pending; active; rejected; remaining; total }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("pending", (Option.map x.pending ~f:Integer.to_value));
        ("active", (Option.map x.active ~f:Integer.to_value));
        ("rejected", (Option.map x.rejected ~f:Integer.to_value));
        ("remaining", (Option.map x.remaining ~f:Integer.to_value));
        ("total", (Option.map x.total ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let total = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "total") in
      let remaining =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "remaining") in
      let rejected =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "rejected") in
      let active =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "active") in
      let pending =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "pending") in
      make ?total ?remaining ?rejected ?active ?pending ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let total = field_map json__ "total" Integer.of_json in
      let remaining = field_map json__ "remaining" Integer.of_json in
      let rejected = field_map json__ "rejected" Integer.of_json in
      let active = field_map json__ "active" Integer.of_json in
      let pending = field_map json__ "pending" Integer.of_json in
      make ?total ?remaining ?rejected ?active ?pending ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the count of users in a Wickr network, categorized by their status (pending, active, rejected) and showing how many users can still be added."]
module GetUsersCountRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network for which to retrieve user counts."]}
    let context_ = "GetUsersCountRequest"
    let make ~networkId = fun () -> { networkId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the count of users in a Wickr network, categorized by their status (pending, active, rejected) and showing how many users can still be added."]
module GetUserResponse =
  struct
    type nonrec t =
      {
      userId: UserId.t option
        [@ocaml.doc "The unique identifier of the user."];
      firstName: SensitiveString.t option
        [@ocaml.doc "The first name of the user."];
      lastName: SensitiveString.t option
        [@ocaml.doc "The last name of the user."];
      username: GenericString.t option
        [@ocaml.doc "The email address or username of the user."];
      isAdmin: Boolean.t option
        [@ocaml.doc
          "Indicates whether the user has administrator privileges in the network."];
      suspended: Boolean.t option
        [@ocaml.doc "Indicates whether the user is currently suspended."];
      status: Integer.t option
        [@ocaml.doc
          "The current status of the user (1 for pending, 2 for active)."];
      lastActivity: Integer.t option
        [@ocaml.doc
          "The timestamp of the user's last activity in the network, specified in epoch seconds."];
      lastLogin: Integer.t option
        [@ocaml.doc
          "The timestamp of the user's last login to the network, specified in epoch seconds."];
      securityGroupIds: SecurityGroupIdList.t option
        [@ocaml.doc
          "A list of security group IDs to which the user belongs."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?userId =
      fun ?firstName ->
        fun ?lastName ->
          fun ?username ->
            fun ?isAdmin ->
              fun ?suspended ->
                fun ?status ->
                  fun ?lastActivity ->
                    fun ?lastLogin ->
                      fun ?securityGroupIds ->
                        fun () ->
                          {
                            userId;
                            firstName;
                            lastName;
                            username;
                            isAdmin;
                            suspended;
                            status;
                            lastActivity;
                            lastLogin;
                            securityGroupIds
                          }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("userId", (Option.map x.userId ~f:UserId.to_value));
        ("firstName", (Option.map x.firstName ~f:SensitiveString.to_value));
        ("lastName", (Option.map x.lastName ~f:SensitiveString.to_value));
        ("username", (Option.map x.username ~f:GenericString.to_value));
        ("isAdmin", (Option.map x.isAdmin ~f:Boolean.to_value));
        ("suspended", (Option.map x.suspended ~f:Boolean.to_value));
        ("status", (Option.map x.status ~f:Integer.to_value));
        ("lastActivity", (Option.map x.lastActivity ~f:Integer.to_value));
        ("lastLogin", (Option.map x.lastLogin ~f:Integer.to_value));
        ("securityGroupIds",
          (Option.map x.securityGroupIds ~f:SecurityGroupIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupIds =
        (Option.map ~f:SecurityGroupIdList.of_xml)
          (Xml.child xml_arg0 "securityGroupIds") in
      let lastLogin =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "lastLogin") in
      let lastActivity =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "lastActivity") in
      let status =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "status") in
      let suspended =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "suspended") in
      let isAdmin =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isAdmin") in
      let username =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "username") in
      let lastName =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "lastName") in
      let firstName =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "firstName") in
      let userId =
        (Option.map ~f:UserId.of_xml) (Xml.child xml_arg0 "userId") in
      make ?securityGroupIds ?lastLogin ?lastActivity ?status ?suspended
        ?isAdmin ?username ?lastName ?firstName ?userId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroupIds =
        field_map json__ "securityGroupIds" SecurityGroupIdList.of_json in
      let lastLogin = field_map json__ "lastLogin" Integer.of_json in
      let lastActivity = field_map json__ "lastActivity" Integer.of_json in
      let status = field_map json__ "status" Integer.of_json in
      let suspended = field_map json__ "suspended" Boolean.of_json in
      let isAdmin = field_map json__ "isAdmin" Boolean.of_json in
      let username = field_map json__ "username" GenericString.of_json in
      let lastName = field_map json__ "lastName" SensitiveString.of_json in
      let firstName = field_map json__ "firstName" SensitiveString.of_json in
      let userId = field_map json__ "userId" UserId.of_json in
      make ?securityGroupIds ?lastLogin ?lastActivity ?status ?suspended
        ?isAdmin ?username ?lastName ?firstName ?userId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific user in a Wickr network, including their profile, status, and activity history."]
module GetUserRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc "The ID of the Wickr network containing the user."];
      userId: UserId.t
        [@ocaml.doc "The unique identifier of the user to retrieve."];
      startTime: SyntheticTimestamp_epoch_seconds.t option
        [@ocaml.doc
          "The start time for filtering the user's last activity. Only activity after this timestamp will be considered. Time is specified in epoch seconds."];
      endTime: SyntheticTimestamp_epoch_seconds.t option
        [@ocaml.doc
          "The end time for filtering the user's last activity. Only activity before this timestamp will be considered. Time is specified in epoch seconds."]}
    let context_ = "GetUserRequest"
    let make ?startTime =
      fun ?endTime ->
        fun ~networkId ->
          fun ~userId -> fun () -> { startTime; endTime; networkId; userId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("userId", (Some (UserId.to_value x.userId)));
        ("startTime",
          (Option.map x.startTime
             ~f:SyntheticTimestamp_epoch_seconds.to_value));
        ("endTime",
          (Option.map x.endTime ~f:SyntheticTimestamp_epoch_seconds.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endTime =
        (Option.map ~f:SyntheticTimestamp_epoch_seconds.of_xml)
          (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:SyntheticTimestamp_epoch_seconds.of_xml)
          (Xml.child xml_arg0 "startTime") in
      let userId =
        UserId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "userId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?endTime ?startTime ~userId ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let endTime =
        field_map json__ "endTime" SyntheticTimestamp_epoch_seconds.of_json in
      let startTime =
        field_map json__ "startTime" SyntheticTimestamp_epoch_seconds.of_json in
      let userId = field_map_exn json__ "userId" UserId.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?endTime ?startTime ~userId ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific user in a Wickr network, including their profile, status, and activity history."]
module GetSecurityGroupResponse =
  struct
    type nonrec t =
      {
      securityGroup: SecurityGroup.t option
        [@ocaml.doc
          "The detailed information about the security group, including all its settings and member counts."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?securityGroup = fun () -> { securityGroup }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("securityGroup",
           (Option.map x.securityGroup ~f:SecurityGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroup =
        (Option.map ~f:SecurityGroup.of_xml)
          (Xml.child xml_arg0 "securityGroup") in
      make ?securityGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroup =
        field_map json__ "securityGroup" SecurityGroup.of_json in
      make ?securityGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific security group in a Wickr network, including its settings, member counts, and configuration."]
module GetSecurityGroupRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network containing the security group."];
      groupId: GenericString.t
        [@ocaml.doc
          "The unique identifier of the security group to retrieve."]}
    let context_ = "GetSecurityGroupRequest"
    let make ~networkId = fun ~groupId -> fun () -> { networkId; groupId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("groupId", (Some (GenericString.to_value x.groupId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupId =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "groupId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~groupId ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let groupId = field_map_exn json__ "groupId" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~groupId ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific security group in a Wickr network, including its settings, member counts, and configuration."]
module GetOpentdfConfigResponse =
  struct
    type nonrec t =
      {
      clientId: GenericString.t option
        [@ocaml.doc
          "The OIDC client ID used for authenticating with the OpenTDF provider."];
      domain: GenericString.t option
        [@ocaml.doc "The domain of the OpenTDF server."];
      clientSecret: SensitiveString.t option
        [@ocaml.doc
          "The OIDC client secret used for authenticating with the OpenTDF provider."];
      provider: GenericString.t option
        [@ocaml.doc "The provider of the OpenTDF platform."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?clientId =
      fun ?domain ->
        fun ?clientSecret ->
          fun ?provider ->
            fun () -> { clientId; domain; clientSecret; provider }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clientId", (Option.map x.clientId ~f:GenericString.to_value));
        ("domain", (Option.map x.domain ~f:GenericString.to_value));
        ("clientSecret",
          (Option.map x.clientSecret ~f:SensitiveString.to_value));
        ("provider", (Option.map x.provider ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let provider =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "provider") in
      let clientSecret =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "clientSecret") in
      let domain =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "domain") in
      let clientId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "clientId") in
      make ?provider ?clientSecret ?domain ?clientId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let provider = field_map json__ "provider" GenericString.of_json in
      let clientSecret =
        field_map json__ "clientSecret" SensitiveString.of_json in
      let domain = field_map json__ "domain" GenericString.of_json in
      let clientId = field_map json__ "clientId" GenericString.of_json in
      make ?provider ?clientSecret ?domain ?clientId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the OpenTDF integration configuration for a Wickr network."]
module GetOpentdfConfigRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network for which OpenTDF integration will be retrieved."]}
    let context_ = "GetOpentdfConfigRequest"
    let make ~networkId = fun () -> { networkId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the OpenTDF integration configuration for a Wickr network."]
module GetOidcInfoResponse =
  struct
    type nonrec t =
      {
      openidConnectInfo: OidcConfigInfo.t option
        [@ocaml.doc
          "The OpenID Connect configuration information for the network, including issuer, client ID, scopes, and other SSO settings."];
      tokenInfo: OidcTokenInfo.t option
        [@ocaml.doc
          "OAuth token information including access token, refresh token, and expiration details (only present if token parameters were provided in the request)."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?openidConnectInfo =
      fun ?tokenInfo -> fun () -> { openidConnectInfo; tokenInfo }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("openidConnectInfo",
           (Option.map x.openidConnectInfo ~f:OidcConfigInfo.to_value));
        ("tokenInfo", (Option.map x.tokenInfo ~f:OidcTokenInfo.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tokenInfo =
        (Option.map ~f:OidcTokenInfo.of_xml) (Xml.child xml_arg0 "tokenInfo") in
      let openidConnectInfo =
        (Option.map ~f:OidcConfigInfo.of_xml)
          (Xml.child xml_arg0 "openidConnectInfo") in
      make ?tokenInfo ?openidConnectInfo ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tokenInfo = field_map json__ "tokenInfo" OidcTokenInfo.of_json in
      let openidConnectInfo =
        field_map json__ "openidConnectInfo" OidcConfigInfo.of_json in
      make ?tokenInfo ?openidConnectInfo ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the OpenID Connect (OIDC) configuration for a Wickr network, including SSO settings and optional token information if access token parameters are provided."]
module GetOidcInfoRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network whose OIDC configuration will be retrieved."];
      clientId: GenericString.t option
        [@ocaml.doc
          "The OAuth client ID for retrieving access tokens (optional)."];
      code: GenericString.t option
        [@ocaml.doc
          "The authorization code for retrieving access tokens (optional)."];
      grantType: GenericString.t option
        [@ocaml.doc
          "The OAuth grant type for retrieving access tokens (optional)."];
      redirectUri: GenericString.t option
        [@ocaml.doc "The redirect URI for the OAuth flow (optional)."];
      url: GenericString.t option
        [@ocaml.doc "The URL for the OIDC provider (optional)."];
      clientSecret: SensitiveString.t option
        [@ocaml.doc
          "The OAuth client secret for retrieving access tokens (optional)."];
      codeVerifier: GenericString.t option
        [@ocaml.doc
          "The PKCE code verifier for enhanced security in the OAuth flow (optional)."];
      certificate: GenericString.t option
        [@ocaml.doc
          "The CA certificate for secure communication with the OIDC provider (optional)."]}
    let context_ = "GetOidcInfoRequest"
    let make ?clientId =
      fun ?code ->
        fun ?grantType ->
          fun ?redirectUri ->
            fun ?url ->
              fun ?clientSecret ->
                fun ?codeVerifier ->
                  fun ?certificate ->
                    fun ~networkId ->
                      fun () ->
                        {
                          clientId;
                          code;
                          grantType;
                          redirectUri;
                          url;
                          clientSecret;
                          codeVerifier;
                          certificate;
                          networkId
                        }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("clientId", (Option.map x.clientId ~f:GenericString.to_value));
        ("code", (Option.map x.code ~f:GenericString.to_value));
        ("grantType", (Option.map x.grantType ~f:GenericString.to_value));
        ("redirectUri", (Option.map x.redirectUri ~f:GenericString.to_value));
        ("url", (Option.map x.url ~f:GenericString.to_value));
        ("clientSecret",
          (Option.map x.clientSecret ~f:SensitiveString.to_value));
        ("codeVerifier",
          (Option.map x.codeVerifier ~f:GenericString.to_value));
        ("certificate", (Option.map x.certificate ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let certificate =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "certificate") in
      let codeVerifier =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "codeVerifier") in
      let clientSecret =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "clientSecret") in
      let url =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "url") in
      let redirectUri =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "redirectUri") in
      let grantType =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "grantType") in
      let code =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "code") in
      let clientId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "clientId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?certificate ?codeVerifier ?clientSecret ?url ?redirectUri
        ?grantType ?code ?clientId ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let certificate = field_map json__ "certificate" GenericString.of_json in
      let codeVerifier =
        field_map json__ "codeVerifier" GenericString.of_json in
      let clientSecret =
        field_map json__ "clientSecret" SensitiveString.of_json in
      let url = field_map json__ "url" GenericString.of_json in
      let redirectUri = field_map json__ "redirectUri" GenericString.of_json in
      let grantType = field_map json__ "grantType" GenericString.of_json in
      let code = field_map json__ "code" GenericString.of_json in
      let clientId = field_map json__ "clientId" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?certificate ?codeVerifier ?clientSecret ?url ?redirectUri
        ?grantType ?code ?clientId ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the OpenID Connect (OIDC) configuration for a Wickr network, including SSO settings and optional token information if access token parameters are provided."]
module GetNetworkSettingsResponse =
  struct
    type nonrec t =
      {
      settings: SettingsList.t option
        [@ocaml.doc
          "A list of network settings, where each setting includes a name, value, and type."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?settings = fun () -> { settings }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("settings", (Option.map x.settings ~f:SettingsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let settings =
        (Option.map ~f:SettingsList.of_xml) (Xml.child xml_arg0 "settings") in
      make ?settings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let settings = field_map json__ "settings" SettingsList.of_json in
      make ?settings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves all network-level settings for a Wickr network, including client metrics, data retention, and other configuration options."]
module GetNetworkSettingsRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network whose settings will be retrieved."]}
    let context_ = "GetNetworkSettingsRequest"
    let make ~networkId = fun () -> { networkId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves all network-level settings for a Wickr network, including client metrics, data retention, and other configuration options."]
module GetNetworkResponse =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t option
        [@ocaml.doc "The unique identifier of the network."];
      networkName: GenericString.t option
        [@ocaml.doc "The name of the network."];
      accessLevel: AccessLevel.t option
        [@ocaml.doc
          "The access level of the network (STANDARD or PREMIUM), which determines available features and capabilities."];
      awsAccountId: GenericString.t option
        [@ocaml.doc
          "The Amazon Web Services account ID that owns the network."];
      networkArn: GenericString.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the network."];
      standing: Integer.t option
        [@ocaml.doc "The current standing or status of the network."];
      freeTrialExpiration: GenericString.t option
        [@ocaml.doc
          "The expiration date and time for the network's free trial period, if applicable."];
      migrationState: Integer.t option
        [@ocaml.doc
          "The SSO redirect URI migration state, managed by the SSO redirect migration wizard. Values: 0 (not started), 1 (in progress), or 2 (completed)."];
      encryptionKeyArn: GenericString.t option
        [@ocaml.doc
          "The ARN of the Amazon Web Services KMS customer managed key used for encrypting sensitive data in the network."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?networkId =
      fun ?networkName ->
        fun ?accessLevel ->
          fun ?awsAccountId ->
            fun ?networkArn ->
              fun ?standing ->
                fun ?freeTrialExpiration ->
                  fun ?migrationState ->
                    fun ?encryptionKeyArn ->
                      fun () ->
                        {
                          networkId;
                          networkName;
                          accessLevel;
                          awsAccountId;
                          networkArn;
                          standing;
                          freeTrialExpiration;
                          migrationState;
                          encryptionKeyArn
                        }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkId", (Option.map x.networkId ~f:NetworkId.to_value));
        ("networkName", (Option.map x.networkName ~f:GenericString.to_value));
        ("accessLevel", (Option.map x.accessLevel ~f:AccessLevel.to_value));
        ("awsAccountId",
          (Option.map x.awsAccountId ~f:GenericString.to_value));
        ("networkArn", (Option.map x.networkArn ~f:GenericString.to_value));
        ("standing", (Option.map x.standing ~f:Integer.to_value));
        ("freeTrialExpiration",
          (Option.map x.freeTrialExpiration ~f:GenericString.to_value));
        ("migrationState", (Option.map x.migrationState ~f:Integer.to_value));
        ("encryptionKeyArn",
          (Option.map x.encryptionKeyArn ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionKeyArn =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "encryptionKeyArn") in
      let migrationState =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "migrationState") in
      let freeTrialExpiration =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "freeTrialExpiration") in
      let standing =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "standing") in
      let networkArn =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "networkArn") in
      let awsAccountId =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "awsAccountId") in
      let accessLevel =
        (Option.map ~f:AccessLevel.of_xml) (Xml.child xml_arg0 "accessLevel") in
      let networkName =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "networkName") in
      let networkId =
        (Option.map ~f:NetworkId.of_xml) (Xml.child xml_arg0 "networkId") in
      make ?encryptionKeyArn ?migrationState ?freeTrialExpiration ?standing
        ?networkArn ?awsAccountId ?accessLevel ?networkName ?networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryptionKeyArn =
        field_map json__ "encryptionKeyArn" GenericString.of_json in
      let migrationState = field_map json__ "migrationState" Integer.of_json in
      let freeTrialExpiration =
        field_map json__ "freeTrialExpiration" GenericString.of_json in
      let standing = field_map json__ "standing" Integer.of_json in
      let networkArn = field_map json__ "networkArn" GenericString.of_json in
      let awsAccountId =
        field_map json__ "awsAccountId" GenericString.of_json in
      let accessLevel = field_map json__ "accessLevel" AccessLevel.of_json in
      let networkName = field_map json__ "networkName" GenericString.of_json in
      let networkId = field_map json__ "networkId" NetworkId.of_json in
      make ?encryptionKeyArn ?migrationState ?freeTrialExpiration ?standing
        ?networkArn ?awsAccountId ?accessLevel ?networkName ?networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific Wickr network, including its configuration, access level, and status."]
module GetNetworkRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc "The ID of the Wickr network to retrieve."]}
    let context_ = "GetNetworkRequest"
    let make ~networkId = fun () -> { networkId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific Wickr network, including its configuration, access level, and status."]
module GetGuestUserHistoryCountResponse =
  struct
    type nonrec t =
      {
      history: GuestUserHistoryCountList.t option
        [@ocaml.doc
          "A list of historical guest user counts, organized by month and billing period."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?history = fun () -> { history }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("history",
           (Option.map x.history ~f:GuestUserHistoryCountList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let history =
        (Option.map ~f:GuestUserHistoryCountList.of_xml)
          (Xml.child xml_arg0 "history") in
      make ?history ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let history =
        field_map json__ "history" GuestUserHistoryCountList.of_json in
      make ?history ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves historical guest user count data for a Wickr network, showing the number of guest users per billing period over the past 90 days."]
module GetGuestUserHistoryCountRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network for which to retrieve guest user history."]}
    let context_ = "GetGuestUserHistoryCountRequest"
    let make ~networkId = fun () -> { networkId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves historical guest user count data for a Wickr network, showing the number of guest users per billing period over the past 90 days."]
module GetDataRetentionBotResponse =
  struct
    type nonrec t =
      {
      botName: GenericString.t option
        [@ocaml.doc "The name of the data retention bot."];
      botExists: Boolean.t option
        [@ocaml.doc
          "Indicates whether a data retention bot exists in the network."];
      isBotActive: Boolean.t option
        [@ocaml.doc
          "Indicates whether the data retention bot is active and operational."];
      isDataRetentionBotRegistered: Boolean.t option
        [@ocaml.doc
          "Indicates whether the data retention bot has been registered with the network."];
      isDataRetentionServiceEnabled: Boolean.t option
        [@ocaml.doc
          "Indicates whether the data retention service is enabled for the network."];
      isPubkeyMsgAcked: Boolean.t option
        [@ocaml.doc
          "Indicates whether the public key message has been acknowledged by the bot."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?botName =
      fun ?botExists ->
        fun ?isBotActive ->
          fun ?isDataRetentionBotRegistered ->
            fun ?isDataRetentionServiceEnabled ->
              fun ?isPubkeyMsgAcked ->
                fun () ->
                  {
                    botName;
                    botExists;
                    isBotActive;
                    isDataRetentionBotRegistered;
                    isDataRetentionServiceEnabled;
                    isPubkeyMsgAcked
                  }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("botName", (Option.map x.botName ~f:GenericString.to_value));
        ("botExists", (Option.map x.botExists ~f:Boolean.to_value));
        ("isBotActive", (Option.map x.isBotActive ~f:Boolean.to_value));
        ("isDataRetentionBotRegistered",
          (Option.map x.isDataRetentionBotRegistered ~f:Boolean.to_value));
        ("isDataRetentionServiceEnabled",
          (Option.map x.isDataRetentionServiceEnabled ~f:Boolean.to_value));
        ("isPubkeyMsgAcked",
          (Option.map x.isPubkeyMsgAcked ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let isPubkeyMsgAcked =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "isPubkeyMsgAcked") in
      let isDataRetentionServiceEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "isDataRetentionServiceEnabled") in
      let isDataRetentionBotRegistered =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "isDataRetentionBotRegistered") in
      let isBotActive =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isBotActive") in
      let botExists =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "botExists") in
      let botName =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "botName") in
      make ?isPubkeyMsgAcked ?isDataRetentionServiceEnabled
        ?isDataRetentionBotRegistered ?isBotActive ?botExists ?botName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let isPubkeyMsgAcked =
        field_map json__ "isPubkeyMsgAcked" Boolean.of_json in
      let isDataRetentionServiceEnabled =
        field_map json__ "isDataRetentionServiceEnabled" Boolean.of_json in
      let isDataRetentionBotRegistered =
        field_map json__ "isDataRetentionBotRegistered" Boolean.of_json in
      let isBotActive = field_map json__ "isBotActive" Boolean.of_json in
      let botExists = field_map json__ "botExists" Boolean.of_json in
      let botName = field_map json__ "botName" GenericString.of_json in
      make ?isPubkeyMsgAcked ?isDataRetentionServiceEnabled
        ?isDataRetentionBotRegistered ?isBotActive ?botExists ?botName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves information about the data retention bot in a Wickr network, including its status and whether the data retention service is enabled."]
module GetDataRetentionBotRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network containing the data retention bot."]}
    let context_ = "GetDataRetentionBotRequest"
    let make ~networkId = fun () -> { networkId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves information about the data retention bot in a Wickr network, including its status and whether the data retention service is enabled."]
module GetBotsCountResponse =
  struct
    type nonrec t =
      {
      pending: Integer.t option
        [@ocaml.doc
          "The number of bots with pending status (invited but not yet activated)."];
      active: Integer.t option
        [@ocaml.doc "The number of bots with active status."];
      total: Integer.t option
        [@ocaml.doc
          "The total number of bots in the network (active and pending)."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?pending =
      fun ?active -> fun ?total -> fun () -> { pending; active; total }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("pending", (Option.map x.pending ~f:Integer.to_value));
        ("active", (Option.map x.active ~f:Integer.to_value));
        ("total", (Option.map x.total ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let total = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "total") in
      let active =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "active") in
      let pending =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "pending") in
      make ?total ?active ?pending ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let total = field_map json__ "total" Integer.of_json in
      let active = field_map json__ "active" Integer.of_json in
      let pending = field_map json__ "pending" Integer.of_json in
      make ?total ?active ?pending ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the count of bots in a Wickr network, categorized by their status (pending, active, and total)."]
module GetBotsCountRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network for which to retrieve bot counts."]}
    let context_ = "GetBotsCountRequest"
    let make ~networkId = fun () -> { networkId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the count of bots in a Wickr network, categorized by their status (pending, active, and total)."]
module GetBotResponse =
  struct
    type nonrec t =
      {
      botId: GenericString.t option
        [@ocaml.doc "The unique identifier of the bot."];
      displayName: GenericString.t option
        [@ocaml.doc "The display name of the bot that is visible to users."];
      username: GenericString.t option
        [@ocaml.doc "The username of the bot."];
      uname: GenericString.t option
        [@ocaml.doc "The unique username hash identifier for the bot."];
      pubkey: GenericString.t option
        [@ocaml.doc "The public key of the bot used for encryption."];
      status: BotStatus.t option
        [@ocaml.doc
          "The current status of the bot (1 for pending, 2 for active)."];
      groupId: GenericString.t option
        [@ocaml.doc "The ID of the security group to which the bot belongs."];
      hasChallenge: Boolean.t option
        [@ocaml.doc "Indicates whether the bot has a password set."];
      suspended: Boolean.t option
        [@ocaml.doc "Indicates whether the bot is currently suspended."];
      lastLogin: GenericString.t option
        [@ocaml.doc "The timestamp of the bot's last login."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?botId =
      fun ?displayName ->
        fun ?username ->
          fun ?uname ->
            fun ?pubkey ->
              fun ?status ->
                fun ?groupId ->
                  fun ?hasChallenge ->
                    fun ?suspended ->
                      fun ?lastLogin ->
                        fun () ->
                          {
                            botId;
                            displayName;
                            username;
                            uname;
                            pubkey;
                            status;
                            groupId;
                            hasChallenge;
                            suspended;
                            lastLogin
                          }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("botId", (Option.map x.botId ~f:GenericString.to_value));
        ("displayName", (Option.map x.displayName ~f:GenericString.to_value));
        ("username", (Option.map x.username ~f:GenericString.to_value));
        ("uname", (Option.map x.uname ~f:GenericString.to_value));
        ("pubkey", (Option.map x.pubkey ~f:GenericString.to_value));
        ("status", (Option.map x.status ~f:BotStatus.to_value));
        ("groupId", (Option.map x.groupId ~f:GenericString.to_value));
        ("hasChallenge", (Option.map x.hasChallenge ~f:Boolean.to_value));
        ("suspended", (Option.map x.suspended ~f:Boolean.to_value));
        ("lastLogin", (Option.map x.lastLogin ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastLogin =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "lastLogin") in
      let suspended =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "suspended") in
      let hasChallenge =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "hasChallenge") in
      let groupId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "groupId") in
      let status =
        (Option.map ~f:BotStatus.of_xml) (Xml.child xml_arg0 "status") in
      let pubkey =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "pubkey") in
      let uname =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "uname") in
      let username =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "username") in
      let displayName =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "displayName") in
      let botId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "botId") in
      make ?lastLogin ?suspended ?hasChallenge ?groupId ?status ?pubkey
        ?uname ?username ?displayName ?botId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastLogin = field_map json__ "lastLogin" GenericString.of_json in
      let suspended = field_map json__ "suspended" Boolean.of_json in
      let hasChallenge = field_map json__ "hasChallenge" Boolean.of_json in
      let groupId = field_map json__ "groupId" GenericString.of_json in
      let status = field_map json__ "status" BotStatus.of_json in
      let pubkey = field_map json__ "pubkey" GenericString.of_json in
      let uname = field_map json__ "uname" GenericString.of_json in
      let username = field_map json__ "username" GenericString.of_json in
      let displayName = field_map json__ "displayName" GenericString.of_json in
      let botId = field_map json__ "botId" GenericString.of_json in
      make ?lastLogin ?suspended ?hasChallenge ?groupId ?status ?pubkey
        ?uname ?username ?displayName ?botId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific bot in a Wickr network, including its status, group membership, and authentication details."]
module GetBotRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc "The ID of the Wickr network containing the bot."];
      botId: BotId.t
        [@ocaml.doc "The unique identifier of the bot to retrieve."]}
    let context_ = "GetBotRequest"
    let make ~networkId = fun ~botId -> fun () -> { networkId; botId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("botId", (Some (BotId.to_value x.botId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let botId =
        BotId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "botId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~botId ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let botId = field_map_exn json__ "botId" BotId.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~botId ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific bot in a Wickr network, including its status, group membership, and authentication details."]
module DeleteSecurityGroupResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating the result of the security group deletion operation."];
      networkId: NetworkId.t option
        [@ocaml.doc
          "The ID of the network from which the security group was deleted."];
      groupId: GenericString.t option
        [@ocaml.doc "The ID of the security group that was deleted."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message =
      fun ?networkId ->
        fun ?groupId -> fun () -> { message; networkId; groupId }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value));
        ("networkId", (Option.map x.networkId ~f:NetworkId.to_value));
        ("groupId", (Option.map x.groupId ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "groupId") in
      let networkId =
        (Option.map ~f:NetworkId.of_xml) (Xml.child xml_arg0 "networkId") in
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?groupId ?networkId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let groupId = field_map json__ "groupId" GenericString.of_json in
      let networkId = field_map json__ "networkId" NetworkId.of_json in
      let message = field_map json__ "message" GenericString.of_json in
      make ?groupId ?networkId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a security group from a Wickr network. This operation cannot be performed on the default security group."]
module DeleteSecurityGroupRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network from which the security group will be deleted."];
      groupId: GenericString.t
        [@ocaml.doc "The unique identifier of the security group to delete."]}
    let context_ = "DeleteSecurityGroupRequest"
    let make ~networkId = fun ~groupId -> fun () -> { networkId; groupId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("groupId", (Some (GenericString.to_value x.groupId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupId =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "groupId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~groupId ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let groupId = field_map_exn json__ "groupId" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~groupId ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a security group from a Wickr network. This operation cannot be performed on the default security group."]
module DeleteNetworkResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating that the network deletion has been initiated successfully."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message = fun () -> { message }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a Wickr network and all its associated resources, including users, bots, security groups, and settings. This operation is permanent and cannot be undone."]
module DeleteNetworkRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc "The ID of the Wickr network to delete."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique identifier for this request to ensure idempotency. If you retry a request with the same client token, the service will return the same response without attempting to delete the network again."]}
    let context_ = "DeleteNetworkRequest"
    let make ?clientToken =
      fun ~networkId -> fun () -> { clientToken; networkId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("X-Client-Token",
          (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml)
          (Xml.child xml_arg0 "X-Client-Token") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?clientToken ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?clientToken ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a Wickr network and all its associated resources, including users, bots, security groups, and settings. This operation is permanent and cannot be undone."]
module DeleteDataRetentionBotResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating that the data retention bot and all associated data were successfully deleted."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message = fun () -> { message }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the data retention bot from a Wickr network. This operation permanently removes the bot and all its associated data from the database."]
module DeleteDataRetentionBotRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network from which the data retention bot will be deleted."]}
    let context_ = "DeleteDataRetentionBotRequest"
    let make ~networkId = fun () -> { networkId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the data retention bot from a Wickr network. This operation permanently removes the bot and all its associated data from the database."]
module DeleteBotResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating the result of the bot deletion operation."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message = fun () -> { message }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a bot from a specified Wickr network. This operation permanently removes the bot account and its associated data from the network."]
module DeleteBotRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network from which the bot will be deleted."];
      botId: BotId.t
        [@ocaml.doc "The unique identifier of the bot to be deleted."]}
    let context_ = "DeleteBotRequest"
    let make ~networkId = fun ~botId -> fun () -> { networkId; botId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("botId", (Some (BotId.to_value x.botId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let botId =
        BotId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "botId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~botId ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let botId = field_map_exn json__ "botId" BotId.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~botId ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a bot from a specified Wickr network. This operation permanently removes the bot account and its associated data from the network."]
module CreateSecurityGroupResponse =
  struct
    type nonrec t =
      {
      securityGroup: SecurityGroup.t option
        [@ocaml.doc
          "The details of the newly created security group, including its ID, name, and settings."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?securityGroup = fun () -> { securityGroup }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("securityGroup",
           (Option.map x.securityGroup ~f:SecurityGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroup =
        (Option.map ~f:SecurityGroup.of_xml)
          (Xml.child xml_arg0 "securityGroup") in
      make ?securityGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroup =
        field_map json__ "securityGroup" SecurityGroup.of_json in
      make ?securityGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new security group in a Wickr network. Security groups allow you to organize users and control their permissions, features, and security settings."]
module CreateSecurityGroupRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network where the security group will be created."];
      name: GenericString.t
        [@ocaml.doc "The name for the new security group."];
      securityGroupSettings: SecurityGroupSettingsRequest.t
        [@ocaml.doc
          "The configuration settings for the security group, including permissions, federation settings, and feature controls."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique identifier for this request to ensure idempotency."]}
    let context_ = "CreateSecurityGroupRequest"
    let make ?clientToken =
      fun ~networkId ->
        fun ~name ->
          fun ~securityGroupSettings ->
            fun () -> { clientToken; networkId; name; securityGroupSettings }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("name", (Some (GenericString.to_value x.name)));
        ("securityGroupSettings",
          (Some
             (SecurityGroupSettingsRequest.to_value x.securityGroupSettings)));
        ("X-Client-Token",
          (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml)
          (Xml.child xml_arg0 "X-Client-Token") in
      let securityGroupSettings =
        SecurityGroupSettingsRequest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "securityGroupSettings") in
      let name =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?clientToken ~securityGroupSettings ~name ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let securityGroupSettings =
        field_map_exn json__ "securityGroupSettings"
          SecurityGroupSettingsRequest.of_json in
      let name = field_map_exn json__ "name" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?clientToken ~securityGroupSettings ~name ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new security group in a Wickr network. Security groups allow you to organize users and control their permissions, features, and security settings."]
module CreateNetworkResponse =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t option
        [@ocaml.doc
          "The unique identifier assigned to the newly created network."];
      networkName: GenericString.t option
        [@ocaml.doc "The name of the newly created network."];
      encryptionKeyArn: GenericString.t option
        [@ocaml.doc
          "The ARN of the KMS key being used to encrypt sensitive data in the network."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?networkId =
      fun ?networkName ->
        fun ?encryptionKeyArn ->
          fun () -> { networkId; networkName; encryptionKeyArn }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkId", (Option.map x.networkId ~f:NetworkId.to_value));
        ("networkName", (Option.map x.networkName ~f:GenericString.to_value));
        ("encryptionKeyArn",
          (Option.map x.encryptionKeyArn ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionKeyArn =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "encryptionKeyArn") in
      let networkName =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "networkName") in
      let networkId =
        (Option.map ~f:NetworkId.of_xml) (Xml.child xml_arg0 "networkId") in
      make ?encryptionKeyArn ?networkName ?networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryptionKeyArn =
        field_map json__ "encryptionKeyArn" GenericString.of_json in
      let networkName = field_map json__ "networkName" GenericString.of_json in
      let networkId = field_map json__ "networkId" NetworkId.of_json in
      make ?encryptionKeyArn ?networkName ?networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new Wickr network with specified access level and configuration. This operation provisions a new communication network for your organization."]
module CreateNetworkRequest =
  struct
    type nonrec t =
      {
      networkName: GenericString.t
        [@ocaml.doc
          "The name for the new network. Must be between 1 and 20 characters."];
      accessLevel: AccessLevel.t
        [@ocaml.doc
          "The access level for the network. Valid values are STANDARD or PREMIUM, which determine the features and capabilities available to network members."];
      enablePremiumFreeTrial: Boolean.t option
        [@ocaml.doc
          "Specifies whether to enable a premium free trial for the network. It is optional and has a default value as false. When set to true, the network starts with premium features for a limited trial period."];
      encryptionKeyArn: GenericString.t option
        [@ocaml.doc
          "The ARN of the Amazon Web Services KMS customer managed key to use for encrypting sensitive data in the network."]}
    let context_ = "CreateNetworkRequest"
    let make ?enablePremiumFreeTrial =
      fun ?encryptionKeyArn ->
        fun ~networkName ->
          fun ~accessLevel ->
            fun () ->
              {
                enablePremiumFreeTrial;
                encryptionKeyArn;
                networkName;
                accessLevel
              }
    let to_value x =
      structure_to_value
        [("networkName", (Some (GenericString.to_value x.networkName)));
        ("accessLevel", (Some (AccessLevel.to_value x.accessLevel)));
        ("enablePremiumFreeTrial",
          (Option.map x.enablePremiumFreeTrial ~f:Boolean.to_value));
        ("encryptionKeyArn",
          (Option.map x.encryptionKeyArn ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionKeyArn =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "encryptionKeyArn") in
      let enablePremiumFreeTrial =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enablePremiumFreeTrial") in
      let accessLevel =
        AccessLevel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "accessLevel") in
      let networkName =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkName") in
      make ?encryptionKeyArn ?enablePremiumFreeTrial ~accessLevel
        ~networkName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryptionKeyArn =
        field_map json__ "encryptionKeyArn" GenericString.of_json in
      let enablePremiumFreeTrial =
        field_map json__ "enablePremiumFreeTrial" Boolean.of_json in
      let accessLevel =
        field_map_exn json__ "accessLevel" AccessLevel.of_json in
      let networkName =
        field_map_exn json__ "networkName" GenericString.of_json in
      make ?encryptionKeyArn ?enablePremiumFreeTrial ~accessLevel
        ~networkName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new Wickr network with specified access level and configuration. This operation provisions a new communication network for your organization."]
module CreateDataRetentionBotResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating that the data retention bot was successfully provisioned."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message = fun () -> { message }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" GenericString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a data retention bot in a Wickr network. Data retention bots are specialized bots that handle message archiving and compliance by capturing and storing messages for regulatory or organizational requirements."]
module CreateDataRetentionBotRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network where the data retention bot will be created."]}
    let context_ = "CreateDataRetentionBotRequest"
    let make ~networkId = fun () -> { networkId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a data retention bot in a Wickr network. Data retention bots are specialized bots that handle message archiving and compliance by capturing and storing messages for regulatory or organizational requirements."]
module CreateDataRetentionBotChallengeResponse =
  struct
    type nonrec t =
      {
      challenge: SensitiveString.t option
        [@ocaml.doc
          "The newly generated challenge password for the data retention bot."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?challenge = fun () -> { challenge }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("challenge", (Option.map x.challenge ~f:SensitiveString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let challenge =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "challenge") in
      make ?challenge ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let challenge = field_map json__ "challenge" SensitiveString.of_json in
      make ?challenge ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new challenge password for the data retention bot. This password is used for authentication when the bot connects to the network."]
module CreateDataRetentionBotChallengeRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network containing the data retention bot."]}
    let context_ = "CreateDataRetentionBotChallengeRequest"
    let make ~networkId = fun () -> { networkId }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new challenge password for the data retention bot. This password is used for authentication when the bot connects to the network."]
module CreateBotResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating the result of the bot creation operation."];
      botId: BotId.t option
        [@ocaml.doc
          "The unique identifier assigned to the newly created bot."];
      networkId: NetworkId.t option
        [@ocaml.doc "The ID of the network where the bot was created."];
      username: GenericString.t option
        [@ocaml.doc "The username of the newly created bot."];
      displayName: GenericString.t option
        [@ocaml.doc "The display name of the newly created bot."];
      groupId: GenericString.t option
        [@ocaml.doc
          "The ID of the security group to which the bot was assigned."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message =
      fun ?botId ->
        fun ?networkId ->
          fun ?username ->
            fun ?displayName ->
              fun ?groupId ->
                fun () ->
                  { message; botId; networkId; username; displayName; groupId
                  }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value));
        ("botId", (Option.map x.botId ~f:BotId.to_value));
        ("networkId", (Option.map x.networkId ~f:NetworkId.to_value));
        ("username", (Option.map x.username ~f:GenericString.to_value));
        ("displayName", (Option.map x.displayName ~f:GenericString.to_value));
        ("groupId", (Option.map x.groupId ~f:GenericString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupId =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "groupId") in
      let displayName =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "displayName") in
      let username =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "username") in
      let networkId =
        (Option.map ~f:NetworkId.of_xml) (Xml.child xml_arg0 "networkId") in
      let botId = (Option.map ~f:BotId.of_xml) (Xml.child xml_arg0 "botId") in
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?groupId ?displayName ?username ?networkId ?botId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let groupId = field_map json__ "groupId" GenericString.of_json in
      let displayName = field_map json__ "displayName" GenericString.of_json in
      let username = field_map json__ "username" GenericString.of_json in
      let networkId = field_map json__ "networkId" NetworkId.of_json in
      let botId = field_map json__ "botId" BotId.of_json in
      let message = field_map json__ "message" GenericString.of_json in
      make ?groupId ?displayName ?username ?networkId ?botId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new bot in a specified Wickr network. Bots are automated accounts that can send and receive messages, enabling integration with external systems and automation of tasks."]
module CreateBotRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network where the bot will be created."];
      username: GenericString.t
        [@ocaml.doc
          "The username for the bot. This must be unique within the network and follow the network's naming conventions."];
      displayName: GenericString.t option
        [@ocaml.doc
          "The display name for the bot that will be visible to users in the network."];
      groupId: GenericString.t
        [@ocaml.doc
          "The ID of the security group to which the bot will be assigned."];
      challenge: SensitiveString.t
        [@ocaml.doc "The password for the bot account."]}
    let context_ = "CreateBotRequest"
    let make ?displayName =
      fun ~networkId ->
        fun ~username ->
          fun ~groupId ->
            fun ~challenge ->
              fun () ->
                { displayName; networkId; username; groupId; challenge }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("username", (Some (GenericString.to_value x.username)));
        ("displayName", (Option.map x.displayName ~f:GenericString.to_value));
        ("groupId", (Some (GenericString.to_value x.groupId)));
        ("challenge", (Some (SensitiveString.to_value x.challenge)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let challenge =
        SensitiveString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "challenge") in
      let groupId =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "groupId") in
      let displayName =
        (Option.map ~f:GenericString.of_xml)
          (Xml.child xml_arg0 "displayName") in
      let username =
        GenericString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "username") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ~challenge ~groupId ?displayName ~username ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let challenge =
        field_map_exn json__ "challenge" SensitiveString.of_json in
      let groupId = field_map_exn json__ "groupId" GenericString.of_json in
      let displayName = field_map json__ "displayName" GenericString.of_json in
      let username = field_map_exn json__ "username" GenericString.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ~challenge ~groupId ?displayName ~username ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new bot in a specified Wickr network. Bots are automated accounts that can send and receive messages, enabling integration with external systems and automation of tasks."]
module BatchToggleUserSuspendStatusResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating the overall result of the batch suspend status toggle operation."];
      successful: BatchUserSuccessResponseItems.t option
        [@ocaml.doc
          "A list of user IDs whose suspend status was successfully toggled."];
      failed: BatchUserErrorResponseItems.t option
        [@ocaml.doc
          "A list of suspend status toggle attempts that failed, including error details explaining why each user's status could not be changed."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message =
      fun ?successful ->
        fun ?failed -> fun () -> { message; successful; failed }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value));
        ("successful",
          (Option.map x.successful ~f:BatchUserSuccessResponseItems.to_value));
        ("failed",
          (Option.map x.failed ~f:BatchUserErrorResponseItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failed =
        (Option.map ~f:BatchUserErrorResponseItems.of_xml)
          (Xml.child xml_arg0 "failed") in
      let successful =
        (Option.map ~f:BatchUserSuccessResponseItems.of_xml)
          (Xml.child xml_arg0 "successful") in
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?failed ?successful ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let failed =
        field_map json__ "failed" BatchUserErrorResponseItems.of_json in
      let successful =
        field_map json__ "successful" BatchUserSuccessResponseItems.of_json in
      let message = field_map json__ "message" GenericString.of_json in
      make ?failed ?successful ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Suspends or unsuspends multiple users in a Wickr network. Suspended users cannot access the network until they are unsuspended. This operation is useful for temporarily restricting access without deleting user accounts."]
module BatchToggleUserSuspendStatusRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network where users will be suspended or unsuspended."];
      suspend: Boolean.t
        [@ocaml.doc
          "A boolean value indicating whether to suspend (true) or unsuspend (false) the specified users."];
      userIds: UserIds.t
        [@ocaml.doc
          "A list of user IDs identifying the users whose suspend status will be toggled. Maximum 50 users per batch request."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique identifier for this request to ensure idempotency."]}
    let context_ = "BatchToggleUserSuspendStatusRequest"
    let make ?clientToken =
      fun ~networkId ->
        fun ~suspend ->
          fun ~userIds ->
            fun () -> { clientToken; networkId; suspend; userIds }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("suspend", (Some (Boolean.to_value x.suspend)));
        ("userIds", (Some (UserIds.to_value x.userIds)));
        ("X-Client-Token",
          (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml)
          (Xml.child xml_arg0 "X-Client-Token") in
      let userIds =
        UserIds.of_xml (Xml.child_exn ~context:context_ xml_arg0 "userIds") in
      let suspend =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "suspend") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?clientToken ~userIds ~suspend ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let userIds = field_map_exn json__ "userIds" UserIds.of_json in
      let suspend = field_map_exn json__ "suspend" Boolean.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?clientToken ~userIds ~suspend ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Suspends or unsuspends multiple users in a Wickr network. Suspended users cannot access the network until they are unsuspended. This operation is useful for temporarily restricting access without deleting user accounts."]
module BatchResetDevicesForUserResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating the overall result of the batch device reset operation."];
      successful: BatchDeviceSuccessResponseItems.t option
        [@ocaml.doc
          "A list of application IDs that were successfully reset."];
      failed: BatchDeviceErrorResponseItems.t option
        [@ocaml.doc
          "A list of device reset attempts that failed, including error details explaining why each device could not be reset."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message =
      fun ?successful ->
        fun ?failed -> fun () -> { message; successful; failed }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value));
        ("successful",
          (Option.map x.successful
             ~f:BatchDeviceSuccessResponseItems.to_value));
        ("failed",
          (Option.map x.failed ~f:BatchDeviceErrorResponseItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failed =
        (Option.map ~f:BatchDeviceErrorResponseItems.of_xml)
          (Xml.child xml_arg0 "failed") in
      let successful =
        (Option.map ~f:BatchDeviceSuccessResponseItems.of_xml)
          (Xml.child xml_arg0 "successful") in
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?failed ?successful ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let failed =
        field_map json__ "failed" BatchDeviceErrorResponseItems.of_json in
      let successful =
        field_map json__ "successful" BatchDeviceSuccessResponseItems.of_json in
      let message = field_map json__ "message" GenericString.of_json in
      make ?failed ?successful ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resets multiple devices for a specific user in a Wickr network. This operation forces the selected devices to log out and requires users to re-authenticate, which is useful for security purposes or when devices need to be revoked."]
module BatchResetDevicesForUserRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network containing the user whose devices will be reset."];
      userId: UserId.t
        [@ocaml.doc "The ID of the user whose devices will be reset."];
      appIds: AppIds.t
        [@ocaml.doc
          "A list of application IDs identifying the specific devices to be reset for the user. Maximum 50 devices per batch request."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique identifier for this request to ensure idempotency."]}
    let context_ = "BatchResetDevicesForUserRequest"
    let make ?clientToken =
      fun ~networkId ->
        fun ~userId ->
          fun ~appIds -> fun () -> { clientToken; networkId; userId; appIds }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("userId", (Some (UserId.to_value x.userId)));
        ("appIds", (Some (AppIds.to_value x.appIds)));
        ("X-Client-Token",
          (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml)
          (Xml.child xml_arg0 "X-Client-Token") in
      let appIds =
        AppIds.of_xml (Xml.child_exn ~context:context_ xml_arg0 "appIds") in
      let userId =
        UserId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "userId") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?clientToken ~appIds ~userId ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let appIds = field_map_exn json__ "appIds" AppIds.of_json in
      let userId = field_map_exn json__ "userId" UserId.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?clientToken ~appIds ~userId ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resets multiple devices for a specific user in a Wickr network. This operation forces the selected devices to log out and requires users to re-authenticate, which is useful for security purposes or when devices need to be revoked."]
module BatchReinviteUserResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating the overall result of the batch reinvitation operation."];
      successful: BatchUserSuccessResponseItems.t option
        [@ocaml.doc "A list of user IDs that were successfully reinvited."];
      failed: BatchUserErrorResponseItems.t option
        [@ocaml.doc
          "A list of reinvitation attempts that failed, including error details explaining why each user could not be reinvited."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message =
      fun ?successful ->
        fun ?failed -> fun () -> { message; successful; failed }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value));
        ("successful",
          (Option.map x.successful ~f:BatchUserSuccessResponseItems.to_value));
        ("failed",
          (Option.map x.failed ~f:BatchUserErrorResponseItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failed =
        (Option.map ~f:BatchUserErrorResponseItems.of_xml)
          (Xml.child xml_arg0 "failed") in
      let successful =
        (Option.map ~f:BatchUserSuccessResponseItems.of_xml)
          (Xml.child xml_arg0 "successful") in
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?failed ?successful ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let failed =
        field_map json__ "failed" BatchUserErrorResponseItems.of_json in
      let successful =
        field_map json__ "successful" BatchUserSuccessResponseItems.of_json in
      let message = field_map json__ "message" GenericString.of_json in
      make ?failed ?successful ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resends invitation codes to multiple users who have pending invitations in a Wickr network. This operation is useful when users haven't accepted their initial invitations or when invitations have expired."]
module BatchReinviteUserRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network where users will be reinvited."];
      userIds: UserIds.t
        [@ocaml.doc
          "A list of user IDs identifying the users to be reinvited to the network. Maximum 50 users per batch request."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique identifier for this request to ensure idempotency."]}
    let context_ = "BatchReinviteUserRequest"
    let make ?clientToken =
      fun ~networkId ->
        fun ~userIds -> fun () -> { clientToken; networkId; userIds }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("userIds", (Some (UserIds.to_value x.userIds)));
        ("X-Client-Token",
          (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml)
          (Xml.child xml_arg0 "X-Client-Token") in
      let userIds =
        UserIds.of_xml (Xml.child_exn ~context:context_ xml_arg0 "userIds") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?clientToken ~userIds ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let userIds = field_map_exn json__ "userIds" UserIds.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?clientToken ~userIds ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resends invitation codes to multiple users who have pending invitations in a Wickr network. This operation is useful when users haven't accepted their initial invitations or when invitations have expired."]
module BatchLookupUserUnameResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating the overall result of the batch lookup operation."];
      successful: BatchUnameSuccessResponseItems.t option
        [@ocaml.doc
          "A list of successfully resolved username hashes with their corresponding email addresses."];
      failed: BatchUnameErrorResponseItems.t option
        [@ocaml.doc
          "A list of username hash lookup attempts that failed, including error details explaining why each lookup failed."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message =
      fun ?successful ->
        fun ?failed -> fun () -> { message; successful; failed }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value));
        ("successful",
          (Option.map x.successful ~f:BatchUnameSuccessResponseItems.to_value));
        ("failed",
          (Option.map x.failed ~f:BatchUnameErrorResponseItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failed =
        (Option.map ~f:BatchUnameErrorResponseItems.of_xml)
          (Xml.child xml_arg0 "failed") in
      let successful =
        (Option.map ~f:BatchUnameSuccessResponseItems.of_xml)
          (Xml.child xml_arg0 "successful") in
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?failed ?successful ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let failed =
        field_map json__ "failed" BatchUnameErrorResponseItems.of_json in
      let successful =
        field_map json__ "successful" BatchUnameSuccessResponseItems.of_json in
      let message = field_map json__ "message" GenericString.of_json in
      make ?failed ?successful ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Looks up multiple user usernames from their unique username hashes (unames). This operation allows you to retrieve the email addresses associated with a list of username hashes."]
module BatchLookupUserUnameRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network where the users will be looked up."];
      unames: Unames.t
        [@ocaml.doc
          "A list of username hashes (unames) to look up. Each uname is a unique identifier for a user's username. Maximum 50 unames per batch request."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique identifier for this request to ensure idempotency."]}
    let context_ = "BatchLookupUserUnameRequest"
    let make ?clientToken =
      fun ~networkId ->
        fun ~unames -> fun () -> { clientToken; networkId; unames }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("unames", (Some (Unames.to_value x.unames)));
        ("X-Client-Token",
          (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml)
          (Xml.child xml_arg0 "X-Client-Token") in
      let unames =
        Unames.of_xml (Xml.child_exn ~context:context_ xml_arg0 "unames") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?clientToken ~unames ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let unames = field_map_exn json__ "unames" Unames.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?clientToken ~unames ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Looks up multiple user usernames from their unique username hashes (unames). This operation allows you to retrieve the email addresses associated with a list of username hashes."]
module BatchDeleteUserResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating the overall result of the batch deletion operation."];
      successful: BatchUserSuccessResponseItems.t option
        [@ocaml.doc
          "A list of user IDs that were successfully deleted from the network."];
      failed: BatchUserErrorResponseItems.t option
        [@ocaml.doc
          "A list of user deletion attempts that failed, including error details explaining why each user could not be deleted."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message =
      fun ?successful ->
        fun ?failed -> fun () -> { message; successful; failed }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value));
        ("successful",
          (Option.map x.successful ~f:BatchUserSuccessResponseItems.to_value));
        ("failed",
          (Option.map x.failed ~f:BatchUserErrorResponseItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failed =
        (Option.map ~f:BatchUserErrorResponseItems.of_xml)
          (Xml.child xml_arg0 "failed") in
      let successful =
        (Option.map ~f:BatchUserSuccessResponseItems.of_xml)
          (Xml.child xml_arg0 "successful") in
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?failed ?successful ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let failed =
        field_map json__ "failed" BatchUserErrorResponseItems.of_json in
      let successful =
        field_map json__ "successful" BatchUserSuccessResponseItems.of_json in
      let message = field_map json__ "message" GenericString.of_json in
      make ?failed ?successful ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes multiple users from a specified Wickr network. This operation permanently removes user accounts and their associated data from the network."]
module BatchDeleteUserRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network from which users will be deleted."];
      userIds: UserIds.t
        [@ocaml.doc
          "A list of user IDs identifying the users to be deleted from the network. Maximum 50 users per batch request."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique identifier for this request to ensure idempotency. If you retry a request with the same client token, the service will return the same response without attempting to delete users again."]}
    let context_ = "BatchDeleteUserRequest"
    let make ?clientToken =
      fun ~networkId ->
        fun ~userIds -> fun () -> { clientToken; networkId; userIds }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("userIds", (Some (UserIds.to_value x.userIds)));
        ("X-Client-Token",
          (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml)
          (Xml.child xml_arg0 "X-Client-Token") in
      let userIds =
        UserIds.of_xml (Xml.child_exn ~context:context_ xml_arg0 "userIds") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?clientToken ~userIds ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let userIds = field_map_exn json__ "userIds" UserIds.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?clientToken ~userIds ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes multiple users from a specified Wickr network. This operation permanently removes user accounts and their associated data from the network."]
module BatchCreateUserResponse =
  struct
    type nonrec t =
      {
      message: GenericString.t option
        [@ocaml.doc
          "A message indicating the overall result of the batch operation."];
      successful: Users.t option
        [@ocaml.doc
          "A list of user objects that were successfully created, including their assigned user IDs and invite codes."];
      failed: BatchUserErrorResponseItems.t option
        [@ocaml.doc
          "A list of user creation attempts that failed, including error details explaining why each user could not be created."]}
    type nonrec error =
      [ `BadRequestError of BadRequestError.t 
      | `ForbiddenError of ForbiddenError.t 
      | `InternalServerError of InternalServerError.t 
      | `RateLimitError of RateLimitError.t 
      | `ResourceNotFoundError of ResourceNotFoundError.t 
      | `UnauthorizedError of UnauthorizedError.t 
      | `ValidationError of ValidationError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?message =
      fun ?successful ->
        fun ?failed -> fun () -> { message; successful; failed }
    let error_of_json name json =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_json json)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_json json)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_json json)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_json json)
      | "ValidationError" -> `ValidationError (ValidationError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestError" -> `BadRequestError (BadRequestError.of_xml xml)
      | "ForbiddenError" -> `ForbiddenError (ForbiddenError.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "RateLimitError" -> `RateLimitError (RateLimitError.of_xml xml)
      | "ResourceNotFoundError" ->
          `ResourceNotFoundError (ResourceNotFoundError.of_xml xml)
      | "UnauthorizedError" ->
          `UnauthorizedError (UnauthorizedError.of_xml xml)
      | "ValidationError" -> `ValidationError (ValidationError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestError e ->
          `Assoc
            [("error", (`String "BadRequestError"));
            ("details", (BadRequestError.to_json e))]
      | `ForbiddenError e ->
          `Assoc
            [("error", (`String "ForbiddenError"));
            ("details", (ForbiddenError.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `RateLimitError e ->
          `Assoc
            [("error", (`String "RateLimitError"));
            ("details", (RateLimitError.to_json e))]
      | `ResourceNotFoundError e ->
          `Assoc
            [("error", (`String "ResourceNotFoundError"));
            ("details", (ResourceNotFoundError.to_json e))]
      | `UnauthorizedError e ->
          `Assoc
            [("error", (`String "UnauthorizedError"));
            ("details", (UnauthorizedError.to_json e))]
      | `ValidationError e ->
          `Assoc
            [("error", (`String "ValidationError"));
            ("details", (ValidationError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:GenericString.to_value));
        ("successful", (Option.map x.successful ~f:Users.to_value));
        ("failed",
          (Option.map x.failed ~f:BatchUserErrorResponseItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failed =
        (Option.map ~f:BatchUserErrorResponseItems.of_xml)
          (Xml.child xml_arg0 "failed") in
      let successful =
        (Option.map ~f:Users.of_xml) (Xml.child xml_arg0 "successful") in
      let message =
        (Option.map ~f:GenericString.of_xml) (Xml.child xml_arg0 "message") in
      make ?failed ?successful ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let failed =
        field_map json__ "failed" BatchUserErrorResponseItems.of_json in
      let successful = field_map json__ "successful" Users.of_json in
      let message = field_map json__ "message" GenericString.of_json in
      make ?failed ?successful ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates multiple users in a specified Wickr network. This operation allows you to provision multiple user accounts simultaneously, optionally specifying security groups, and validation requirements for each user. codeValidation, inviteCode, and inviteCodeTtl are restricted to networks under preview only."]
module BatchCreateUserRequest =
  struct
    type nonrec t =
      {
      networkId: NetworkId.t
        [@ocaml.doc
          "The ID of the Wickr network where users will be created."];
      users: BatchCreateUserRequestItems.t
        [@ocaml.doc
          "A list of user objects containing the details for each user to be created, including username, name, security groups, and optional invite codes. Maximum 50 users per batch request."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique identifier for this request to ensure idempotency. If you retry a request with the same client token, the service will return the same response without creating duplicate users."]}
    let context_ = "BatchCreateUserRequest"
    let make ?clientToken =
      fun ~networkId ->
        fun ~users -> fun () -> { clientToken; networkId; users }
    let to_value x =
      structure_to_value
        [("networkId", (Some (NetworkId.to_value x.networkId)));
        ("users", (Some (BatchCreateUserRequestItems.to_value x.users)));
        ("X-Client-Token",
          (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml)
          (Xml.child xml_arg0 "X-Client-Token") in
      let users =
        BatchCreateUserRequestItems.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "users") in
      let networkId =
        NetworkId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "networkId") in
      make ?clientToken ~users ~networkId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let users =
        field_map_exn json__ "users" BatchCreateUserRequestItems.of_json in
      let networkId = field_map_exn json__ "networkId" NetworkId.of_json in
      make ?clientToken ~users ~networkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates multiple users in a specified Wickr network. This operation allows you to provision multiple user accounts simultaneously, optionally specifying security groups, and validation requirements for each user. codeValidation, inviteCode, and inviteCodeTtl are restricted to networks under preview only."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let build_bot_locale =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.build_bot_locale
           (Values.BuildBotLocaleRequest.make ~botId ~botVersion ~localeId ())
           (Some Values.BuildBotLocaleResponse.to_json)
           (Some Values.BuildBotLocaleResponse.error_to_json)])
let create_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and botTags = flag "bot-tags" (optional json_arg) ~doc:"JSON TagMap"
       and testBotAliasTags =
         flag "test-bot-alias-tags" (optional json_arg) ~doc:"JSON TagMap"
       and botName = flag "bot-name" (required string) ~doc:"STRING Name"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and dataPrivacy =
         flag "data-privacy" (required json_arg) ~doc:"JSON DataPrivacy"
       and idleSessionTTLInSeconds =
         flag "idle-session-t-t-l-in-seconds" (required int)
           ~doc:"INT SessionTTL" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bot
           (Values.CreateBotRequest.make ?description
              ?botTags:(Option.map ~f:Values.TagMap.of_json botTags)
              ?testBotAliasTags:(Option.map ~f:Values.TagMap.of_json
                                   testBotAliasTags) ~botName ~roleArn
              ~dataPrivacy:(Values.DataPrivacy.of_json dataPrivacy)
              ~idleSessionTTLInSeconds ())
           (Some Values.CreateBotResponse.to_json)
           (Some Values.CreateBotResponse.error_to_json)])
let create_bot_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and botVersion =
         flag "bot-version" (optional string)
           ~doc:"STRING NumericalBotVersion"
       and botAliasLocaleSettings =
         flag "bot-alias-locale-settings" (optional json_arg)
           ~doc:"JSON BotAliasLocaleSettingsMap"
       and conversationLogSettings =
         flag "conversation-log-settings" (optional json_arg)
           ~doc:"JSON ConversationLogSettings"
       and sentimentAnalysisSettings =
         flag "sentiment-analysis-settings" (optional json_arg)
           ~doc:"JSON SentimentAnalysisSettings"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and botAliasName =
         flag "bot-alias-name" (required string) ~doc:"STRING Name"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bot_alias
           (Values.CreateBotAliasRequest.make ?description ?botVersion
              ?botAliasLocaleSettings:(Option.map
                                         ~f:Values.BotAliasLocaleSettingsMap.of_json
                                         botAliasLocaleSettings)
              ?conversationLogSettings:(Option.map
                                          ~f:Values.ConversationLogSettings.of_json
                                          conversationLogSettings)
              ?sentimentAnalysisSettings:(Option.map
                                            ~f:Values.SentimentAnalysisSettings.of_json
                                            sentimentAnalysisSettings)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~botAliasName
              ~botId ()) (Some Values.CreateBotAliasResponse.to_json)
           (Some Values.CreateBotAliasResponse.error_to_json)])
let create_bot_locale =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and voiceSettings =
         flag "voice-settings" (optional json_arg) ~doc:"JSON VoiceSettings"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId"
       and nluIntentConfidenceThreshold =
         flag "nlu-intent-confidence-threshold" (required float)
           ~doc:"FLOAT ConfidenceThreshold" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bot_locale
           (Values.CreateBotLocaleRequest.make ?description
              ?voiceSettings:(Option.map ~f:Values.VoiceSettings.of_json
                                voiceSettings) ~botId ~botVersion ~localeId
              ~nluIntentConfidenceThreshold ())
           (Some Values.CreateBotLocaleResponse.to_json)
           (Some Values.CreateBotLocaleResponse.error_to_json)])
let create_bot_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersionLocaleSpecification =
         flag "bot-version-locale-specification" (required json_arg)
           ~doc:"JSON BotVersionLocaleSpecification" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bot_version
           (Values.CreateBotVersionRequest.make ?description ~botId
              ~botVersionLocaleSpecification:(Values.BotVersionLocaleSpecification.of_json
                                                botVersionLocaleSpecification)
              ()) (Some Values.CreateBotVersionResponse.to_json)
           (Some Values.CreateBotVersionResponse.error_to_json)])
let create_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filePassword =
         flag "file-password" (optional string)
           ~doc:"STRING ImportExportFilePassword"
       and resourceSpecification =
         flag "resource-specification" (required json_arg)
           ~doc:"JSON ExportResourceSpecification"
       and fileFormat =
         flag "file-format" (required json_arg)
           ~doc:"JSON ImportExportFileFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_export
           (Values.CreateExportRequest.make ?filePassword
              ~resourceSpecification:(Values.ExportResourceSpecification.of_json
                                        resourceSpecification)
              ~fileFormat:(Values.ImportExportFileFormat.of_json fileFormat)
              ()) (Some Values.CreateExportResponse.to_json)
           (Some Values.CreateExportResponse.error_to_json)])
let create_intent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and parentIntentSignature =
         flag "parent-intent-signature" (optional string)
           ~doc:"STRING IntentSignature"
       and sampleUtterances =
         flag "sample-utterances" (optional json_arg)
           ~doc:"JSON SampleUtterancesList"
       and dialogCodeHook =
         flag "dialog-code-hook" (optional json_arg)
           ~doc:"JSON DialogCodeHookSettings"
       and fulfillmentCodeHook =
         flag "fulfillment-code-hook" (optional json_arg)
           ~doc:"JSON FulfillmentCodeHookSettings"
       and intentConfirmationSetting =
         flag "intent-confirmation-setting" (optional json_arg)
           ~doc:"JSON IntentConfirmationSetting"
       and intentClosingSetting =
         flag "intent-closing-setting" (optional json_arg)
           ~doc:"JSON IntentClosingSetting"
       and inputContexts =
         flag "input-contexts" (optional json_arg)
           ~doc:"JSON InputContextsList"
       and outputContexts =
         flag "output-contexts" (optional json_arg)
           ~doc:"JSON OutputContextsList"
       and kendraConfiguration =
         flag "kendra-configuration" (optional json_arg)
           ~doc:"JSON KendraConfiguration"
       and intentName =
         flag "intent-name" (required string) ~doc:"STRING Name"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_intent
           (Values.CreateIntentRequest.make ?description
              ?parentIntentSignature
              ?sampleUtterances:(Option.map
                                   ~f:Values.SampleUtterancesList.of_json
                                   sampleUtterances)
              ?dialogCodeHook:(Option.map
                                 ~f:Values.DialogCodeHookSettings.of_json
                                 dialogCodeHook)
              ?fulfillmentCodeHook:(Option.map
                                      ~f:Values.FulfillmentCodeHookSettings.of_json
                                      fulfillmentCodeHook)
              ?intentConfirmationSetting:(Option.map
                                            ~f:Values.IntentConfirmationSetting.of_json
                                            intentConfirmationSetting)
              ?intentClosingSetting:(Option.map
                                       ~f:Values.IntentClosingSetting.of_json
                                       intentClosingSetting)
              ?inputContexts:(Option.map ~f:Values.InputContextsList.of_json
                                inputContexts)
              ?outputContexts:(Option.map
                                 ~f:Values.OutputContextsList.of_json
                                 outputContexts)
              ?kendraConfiguration:(Option.map
                                      ~f:Values.KendraConfiguration.of_json
                                      kendraConfiguration) ~intentName ~botId
              ~botVersion ~localeId ())
           (Some Values.CreateIntentResponse.to_json)
           (Some Values.CreateIntentResponse.error_to_json)])
let create_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and policy = flag "policy" (required string) ~doc:"STRING Policy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_policy
           (Values.CreateResourcePolicyRequest.make ~resourceArn ~policy ())
           (Some Values.CreateResourcePolicyResponse.to_json)
           (Some Values.CreateResourcePolicyResponse.error_to_json)])
let create_resource_policy_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and condition =
         flag "condition" (optional json_arg) ~doc:"JSON ConditionMap"
       and expectedRevisionId =
         flag "expected-revision-id" (optional string)
           ~doc:"STRING RevisionId"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and statementId =
         flag "statement-id" (required string) ~doc:"STRING Name"
       and effect_ = flag "effect-" (required json_arg) ~doc:"JSON Effect"
       and principal =
         flag "principal" (required json_arg) ~doc:"JSON PrincipalList"
       and action =
         flag "action" (required json_arg) ~doc:"JSON OperationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_policy_statement
           (Values.CreateResourcePolicyStatementRequest.make
              ?condition:(Option.map ~f:Values.ConditionMap.of_json condition)
              ?expectedRevisionId ~resourceArn ~statementId
              ~effect_:(Values.Effect.of_json effect_)
              ~principal:(Values.PrincipalList.of_json principal)
              ~action:(Values.OperationList.of_json action) ())
           (Some Values.CreateResourcePolicyStatementResponse.to_json)
           (Some Values.CreateResourcePolicyStatementResponse.error_to_json)])
let create_slot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and slotTypeId =
         flag "slot-type-id" (optional string)
           ~doc:"STRING BuiltInOrCustomSlotTypeId"
       and obfuscationSetting =
         flag "obfuscation-setting" (optional json_arg)
           ~doc:"JSON ObfuscationSetting"
       and multipleValuesSetting =
         flag "multiple-values-setting" (optional json_arg)
           ~doc:"JSON MultipleValuesSetting"
       and slotName = flag "slot-name" (required string) ~doc:"STRING Name"
       and valueElicitationSetting =
         flag "value-elicitation-setting" (required json_arg)
           ~doc:"JSON SlotValueElicitationSetting"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId"
       and intentId = flag "intent-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_slot
           (Values.CreateSlotRequest.make ?description ?slotTypeId
              ?obfuscationSetting:(Option.map
                                     ~f:Values.ObfuscationSetting.of_json
                                     obfuscationSetting)
              ?multipleValuesSetting:(Option.map
                                        ~f:Values.MultipleValuesSetting.of_json
                                        multipleValuesSetting) ~slotName
              ~valueElicitationSetting:(Values.SlotValueElicitationSetting.of_json
                                          valueElicitationSetting) ~botId
              ~botVersion ~localeId ~intentId ())
           (Some Values.CreateSlotResponse.to_json)
           (Some Values.CreateSlotResponse.error_to_json)])
let create_slot_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and slotTypeValues =
         flag "slot-type-values" (optional json_arg)
           ~doc:"JSON SlotTypeValues"
       and valueSelectionSetting =
         flag "value-selection-setting" (optional json_arg)
           ~doc:"JSON SlotValueSelectionSetting"
       and parentSlotTypeSignature =
         flag "parent-slot-type-signature" (optional string)
           ~doc:"STRING SlotTypeSignature"
       and externalSourceSetting =
         flag "external-source-setting" (optional json_arg)
           ~doc:"JSON ExternalSourceSetting"
       and slotTypeName =
         flag "slot-type-name" (required string) ~doc:"STRING Name"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_slot_type
           (Values.CreateSlotTypeRequest.make ?description
              ?slotTypeValues:(Option.map ~f:Values.SlotTypeValues.of_json
                                 slotTypeValues)
              ?valueSelectionSetting:(Option.map
                                        ~f:Values.SlotValueSelectionSetting.of_json
                                        valueSelectionSetting)
              ?parentSlotTypeSignature
              ?externalSourceSetting:(Option.map
                                        ~f:Values.ExternalSourceSetting.of_json
                                        externalSourceSetting) ~slotTypeName
              ~botId ~botVersion ~localeId ())
           (Some Values.CreateSlotTypeResponse.to_json)
           (Some Values.CreateSlotTypeResponse.error_to_json)])
let create_upload_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_upload_url (Values.CreateUploadUrlRequest.make ())
           (Some Values.CreateUploadUrlResponse.to_json)
           (Some Values.CreateUploadUrlResponse.error_to_json)])
let delete_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipResourceInUseCheck =
         flag "skip-resource-in-use-check" (optional bool)
           ~doc:"BOOL SkipResourceInUseCheck"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bot
           (Values.DeleteBotRequest.make ?skipResourceInUseCheck ~botId ())
           (Some Values.DeleteBotResponse.to_json)
           (Some Values.DeleteBotResponse.error_to_json)])
let delete_bot_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipResourceInUseCheck =
         flag "skip-resource-in-use-check" (optional bool)
           ~doc:"BOOL SkipResourceInUseCheck"
       and botAliasId =
         flag "bot-alias-id" (required string) ~doc:"STRING BotAliasId"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bot_alias
           (Values.DeleteBotAliasRequest.make ?skipResourceInUseCheck
              ~botAliasId ~botId ())
           (Some Values.DeleteBotAliasResponse.to_json)
           (Some Values.DeleteBotAliasResponse.error_to_json)])
let delete_bot_locale =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bot_locale
           (Values.DeleteBotLocaleRequest.make ~botId ~botVersion ~localeId
              ()) (Some Values.DeleteBotLocaleResponse.to_json)
           (Some Values.DeleteBotLocaleResponse.error_to_json)])
let delete_bot_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipResourceInUseCheck =
         flag "skip-resource-in-use-check" (optional bool)
           ~doc:"BOOL SkipResourceInUseCheck"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string)
           ~doc:"STRING NumericalBotVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bot_version
           (Values.DeleteBotVersionRequest.make ?skipResourceInUseCheck
              ~botId ~botVersion ())
           (Some Values.DeleteBotVersionResponse.to_json)
           (Some Values.DeleteBotVersionResponse.error_to_json)])
let delete_custom_vocabulary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_vocabulary
           (Values.DeleteCustomVocabularyRequest.make ~botId ~botVersion
              ~localeId ())
           (Some Values.DeleteCustomVocabularyResponse.to_json)
           (Some Values.DeleteCustomVocabularyResponse.error_to_json)])
let delete_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportId = flag "export-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_export (Values.DeleteExportRequest.make ~exportId ())
           (Some Values.DeleteExportResponse.to_json)
           (Some Values.DeleteExportResponse.error_to_json)])
let delete_import =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and importId = flag "import-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_import (Values.DeleteImportRequest.make ~importId ())
           (Some Values.DeleteImportResponse.to_json)
           (Some Values.DeleteImportResponse.error_to_json)])
let delete_intent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and intentId = flag "intent-id" (required string) ~doc:"STRING Id"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_intent
           (Values.DeleteIntentRequest.make ~intentId ~botId ~botVersion
              ~localeId ()) None None])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedRevisionId =
         flag "expected-revision-id" (optional string)
           ~doc:"STRING RevisionId"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyRequest.make ?expectedRevisionId
              ~resourceArn ())
           (Some Values.DeleteResourcePolicyResponse.to_json)
           (Some Values.DeleteResourcePolicyResponse.error_to_json)])
let delete_resource_policy_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedRevisionId =
         flag "expected-revision-id" (optional string)
           ~doc:"STRING RevisionId"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and statementId =
         flag "statement-id" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy_statement
           (Values.DeleteResourcePolicyStatementRequest.make
              ?expectedRevisionId ~resourceArn ~statementId ())
           (Some Values.DeleteResourcePolicyStatementResponse.to_json)
           (Some Values.DeleteResourcePolicyStatementResponse.error_to_json)])
let delete_slot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and slotId = flag "slot-id" (required string) ~doc:"STRING Id"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId"
       and intentId = flag "intent-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_slot
           (Values.DeleteSlotRequest.make ~slotId ~botId ~botVersion
              ~localeId ~intentId ()) None None])
let delete_slot_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipResourceInUseCheck =
         flag "skip-resource-in-use-check" (optional bool)
           ~doc:"BOOL SkipResourceInUseCheck"
       and slotTypeId =
         flag "slot-type-id" (required string) ~doc:"STRING Id"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_slot_type
           (Values.DeleteSlotTypeRequest.make ?skipResourceInUseCheck
              ~slotTypeId ~botId ~botVersion ~localeId ()) None None])
let delete_utterances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and localeId =
         flag "locale-id" (optional string) ~doc:"STRING LocaleId"
       and sessionId =
         flag "session-id" (optional string) ~doc:"STRING SessionId"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_utterances
           (Values.DeleteUtterancesRequest.make ?localeId ?sessionId ~botId
              ()) (Some Values.DeleteUtterancesResponse.to_json)
           (Some Values.DeleteUtterancesResponse.error_to_json)])
let describe_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_bot (Values.DescribeBotRequest.make ~botId ())
           (Some Values.DescribeBotResponse.to_json)
           (Some Values.DescribeBotResponse.error_to_json)])
let describe_bot_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botAliasId =
         flag "bot-alias-id" (required string) ~doc:"STRING BotAliasId"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_bot_alias
           (Values.DescribeBotAliasRequest.make ~botAliasId ~botId ())
           (Some Values.DescribeBotAliasResponse.to_json)
           (Some Values.DescribeBotAliasResponse.error_to_json)])
let describe_bot_locale =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING BotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_bot_locale
           (Values.DescribeBotLocaleRequest.make ~botId ~botVersion ~localeId
              ()) (Some Values.DescribeBotLocaleResponse.to_json)
           (Some Values.DescribeBotLocaleResponse.error_to_json)])
let describe_bot_recommendation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId"
       and botRecommendationId =
         flag "bot-recommendation-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_bot_recommendation
           (Values.DescribeBotRecommendationRequest.make ~botId ~botVersion
              ~localeId ~botRecommendationId ())
           (Some Values.DescribeBotRecommendationResponse.to_json)
           (Some Values.DescribeBotRecommendationResponse.error_to_json)])
let describe_bot_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string)
           ~doc:"STRING NumericalBotVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_bot_version
           (Values.DescribeBotVersionRequest.make ~botId ~botVersion ())
           (Some Values.DescribeBotVersionResponse.to_json)
           (Some Values.DescribeBotVersionResponse.error_to_json)])
let describe_custom_vocabulary_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING BotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_custom_vocabulary_metadata
           (Values.DescribeCustomVocabularyMetadataRequest.make ~botId
              ~botVersion ~localeId ())
           (Some Values.DescribeCustomVocabularyMetadataResponse.to_json)
           (Some
              Values.DescribeCustomVocabularyMetadataResponse.error_to_json)])
let describe_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportId = flag "export-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_export
           (Values.DescribeExportRequest.make ~exportId ())
           (Some Values.DescribeExportResponse.to_json)
           (Some Values.DescribeExportResponse.error_to_json)])
let describe_import =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and importId = flag "import-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_import
           (Values.DescribeImportRequest.make ~importId ())
           (Some Values.DescribeImportResponse.to_json)
           (Some Values.DescribeImportResponse.error_to_json)])
let describe_intent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and intentId = flag "intent-id" (required string) ~doc:"STRING Id"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING BotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_intent
           (Values.DescribeIntentRequest.make ~intentId ~botId ~botVersion
              ~localeId ()) (Some Values.DescribeIntentResponse.to_json)
           (Some Values.DescribeIntentResponse.error_to_json)])
let describe_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_resource_policy
           (Values.DescribeResourcePolicyRequest.make ~resourceArn ())
           (Some Values.DescribeResourcePolicyResponse.to_json)
           (Some Values.DescribeResourcePolicyResponse.error_to_json)])
let describe_slot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and slotId = flag "slot-id" (required string) ~doc:"STRING Id"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING BotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId"
       and intentId = flag "intent-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_slot
           (Values.DescribeSlotRequest.make ~slotId ~botId ~botVersion
              ~localeId ~intentId ())
           (Some Values.DescribeSlotResponse.to_json)
           (Some Values.DescribeSlotResponse.error_to_json)])
let describe_slot_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and slotTypeId =
         flag "slot-type-id" (required string) ~doc:"STRING Id"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING BotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_slot_type
           (Values.DescribeSlotTypeRequest.make ~slotTypeId ~botId
              ~botVersion ~localeId ())
           (Some Values.DescribeSlotTypeResponse.to_json)
           (Some Values.DescribeSlotTypeResponse.error_to_json)])
let list_aggregated_utterances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botAliasId =
         flag "bot-alias-id" (optional string) ~doc:"STRING BotAliasId"
       and botVersion =
         flag "bot-version" (optional string) ~doc:"STRING BotVersion"
       and sortBy =
         flag "sort-by" (optional json_arg)
           ~doc:"JSON AggregatedUtterancesSortBy"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON AggregatedUtterancesFilters"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId"
       and aggregationDuration =
         flag "aggregation-duration" (required json_arg)
           ~doc:"JSON UtteranceAggregationDuration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_aggregated_utterances
           (Values.ListAggregatedUtterancesRequest.make ?botAliasId
              ?botVersion
              ?sortBy:(Option.map
                         ~f:Values.AggregatedUtterancesSortBy.of_json sortBy)
              ?filters:(Option.map
                          ~f:Values.AggregatedUtterancesFilters.of_json
                          filters) ?maxResults ?nextToken ~botId ~localeId
              ~aggregationDuration:(Values.UtteranceAggregationDuration.of_json
                                      aggregationDuration) ())
           (Some Values.ListAggregatedUtterancesResponse.to_json)
           (Some Values.ListAggregatedUtterancesResponse.error_to_json)])
let list_bot_aliases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bot_aliases
           (Values.ListBotAliasesRequest.make ?maxResults ?nextToken ~botId
              ()) (Some Values.ListBotAliasesResponse.to_json)
           (Some Values.ListBotAliasesResponse.error_to_json)])
let list_bot_locales =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON BotLocaleSortBy"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON BotLocaleFilters"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING BotVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bot_locales
           (Values.ListBotLocalesRequest.make
              ?sortBy:(Option.map ~f:Values.BotLocaleSortBy.of_json sortBy)
              ?filters:(Option.map ~f:Values.BotLocaleFilters.of_json filters)
              ?maxResults ?nextToken ~botId ~botVersion ())
           (Some Values.ListBotLocalesResponse.to_json)
           (Some Values.ListBotLocalesResponse.error_to_json)])
let list_bot_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bot_recommendations
           (Values.ListBotRecommendationsRequest.make ?maxResults ?nextToken
              ~botId ~botVersion ~localeId ())
           (Some Values.ListBotRecommendationsResponse.to_json)
           (Some Values.ListBotRecommendationsResponse.error_to_json)])
let list_bot_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON BotVersionSortBy"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bot_versions
           (Values.ListBotVersionsRequest.make
              ?sortBy:(Option.map ~f:Values.BotVersionSortBy.of_json sortBy)
              ?maxResults ?nextToken ~botId ())
           (Some Values.ListBotVersionsResponse.to_json)
           (Some Values.ListBotVersionsResponse.error_to_json)])
let list_bots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON BotSortBy"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON BotFilters"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bots
           (Values.ListBotsRequest.make
              ?sortBy:(Option.map ~f:Values.BotSortBy.of_json sortBy)
              ?filters:(Option.map ~f:Values.BotFilters.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.ListBotsResponse.to_json)
           (Some Values.ListBotsResponse.error_to_json)])
let list_built_in_intents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON BuiltInIntentSortBy"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BuiltInsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_built_in_intents
           (Values.ListBuiltInIntentsRequest.make
              ?sortBy:(Option.map ~f:Values.BuiltInIntentSortBy.of_json
                         sortBy) ?maxResults ?nextToken ~localeId ())
           (Some Values.ListBuiltInIntentsResponse.to_json)
           (Some Values.ListBuiltInIntentsResponse.error_to_json)])
let list_built_in_slot_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON BuiltInSlotTypeSortBy"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BuiltInsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_built_in_slot_types
           (Values.ListBuiltInSlotTypesRequest.make
              ?sortBy:(Option.map ~f:Values.BuiltInSlotTypeSortBy.of_json
                         sortBy) ?maxResults ?nextToken ~localeId ())
           (Some Values.ListBuiltInSlotTypesResponse.to_json)
           (Some Values.ListBuiltInSlotTypesResponse.error_to_json)])
let list_exports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botId = flag "bot-id" (optional string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (optional string) ~doc:"STRING BotVersion"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON ExportSortBy"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ExportFilters"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and localeId =
         flag "locale-id" (optional string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_exports
           (Values.ListExportsRequest.make ?botId ?botVersion
              ?sortBy:(Option.map ~f:Values.ExportSortBy.of_json sortBy)
              ?filters:(Option.map ~f:Values.ExportFilters.of_json filters)
              ?maxResults ?nextToken ?localeId ())
           (Some Values.ListExportsResponse.to_json)
           (Some Values.ListExportsResponse.error_to_json)])
let list_imports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botId = flag "bot-id" (optional string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (optional string) ~doc:"STRING DraftBotVersion"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON ImportSortBy"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ImportFilters"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and localeId =
         flag "locale-id" (optional string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_imports
           (Values.ListImportsRequest.make ?botId ?botVersion
              ?sortBy:(Option.map ~f:Values.ImportSortBy.of_json sortBy)
              ?filters:(Option.map ~f:Values.ImportFilters.of_json filters)
              ?maxResults ?nextToken ?localeId ())
           (Some Values.ListImportsResponse.to_json)
           (Some Values.ListImportsResponse.error_to_json)])
let list_intents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON IntentSortBy"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON IntentFilters"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING BotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_intents
           (Values.ListIntentsRequest.make
              ?sortBy:(Option.map ~f:Values.IntentSortBy.of_json sortBy)
              ?filters:(Option.map ~f:Values.IntentFilters.of_json filters)
              ?maxResults ?nextToken ~botId ~botVersion ~localeId ())
           (Some Values.ListIntentsResponse.to_json)
           (Some Values.ListIntentsResponse.error_to_json)])
let list_recommended_intents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId"
       and botRecommendationId =
         flag "bot-recommendation-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recommended_intents
           (Values.ListRecommendedIntentsRequest.make ?nextToken ?maxResults
              ~botId ~botVersion ~localeId ~botRecommendationId ())
           (Some Values.ListRecommendedIntentsResponse.to_json)
           (Some Values.ListRecommendedIntentsResponse.error_to_json)])
let list_slot_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SlotTypeSortBy"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SlotTypeFilters"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING BotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_slot_types
           (Values.ListSlotTypesRequest.make
              ?sortBy:(Option.map ~f:Values.SlotTypeSortBy.of_json sortBy)
              ?filters:(Option.map ~f:Values.SlotTypeFilters.of_json filters)
              ?maxResults ?nextToken ~botId ~botVersion ~localeId ())
           (Some Values.ListSlotTypesResponse.to_json)
           (Some Values.ListSlotTypesResponse.error_to_json)])
let list_slots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SlotSortBy"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SlotFilters"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING BotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId"
       and intentId = flag "intent-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_slots
           (Values.ListSlotsRequest.make
              ?sortBy:(Option.map ~f:Values.SlotSortBy.of_json sortBy)
              ?filters:(Option.map ~f:Values.SlotFilters.of_json filters)
              ?maxResults ?nextToken ~botId ~botVersion ~localeId ~intentId
              ()) (Some Values.ListSlotsResponse.to_json)
           (Some Values.ListSlotsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let search_associated_transcripts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchOrder =
         flag "search-order" (optional json_arg) ~doc:"JSON SearchOrder"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextIndex = flag "next-index" (optional int) ~doc:"INT NextIndex"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING BotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId"
       and botRecommendationId =
         flag "bot-recommendation-id" (required string) ~doc:"STRING Id"
       and filters =
         flag "filters" (required json_arg)
           ~doc:"JSON AssociatedTranscriptFilters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_associated_transcripts
           (Values.SearchAssociatedTranscriptsRequest.make
              ?searchOrder:(Option.map ~f:Values.SearchOrder.of_json
                              searchOrder) ?maxResults ?nextIndex ~botId
              ~botVersion ~localeId ~botRecommendationId
              ~filters:(Values.AssociatedTranscriptFilters.of_json filters)
              ()) (Some Values.SearchAssociatedTranscriptsResponse.to_json)
           (Some Values.SearchAssociatedTranscriptsResponse.error_to_json)])
let start_bot_recommendation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionSetting =
         flag "encryption-setting" (optional json_arg)
           ~doc:"JSON EncryptionSetting"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId"
       and transcriptSourceSetting =
         flag "transcript-source-setting" (required json_arg)
           ~doc:"JSON TranscriptSourceSetting" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_bot_recommendation
           (Values.StartBotRecommendationRequest.make
              ?encryptionSetting:(Option.map
                                    ~f:Values.EncryptionSetting.of_json
                                    encryptionSetting) ~botId ~botVersion
              ~localeId
              ~transcriptSourceSetting:(Values.TranscriptSourceSetting.of_json
                                          transcriptSourceSetting) ())
           (Some Values.StartBotRecommendationResponse.to_json)
           (Some Values.StartBotRecommendationResponse.error_to_json)])
let start_import =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filePassword =
         flag "file-password" (optional string)
           ~doc:"STRING ImportExportFilePassword"
       and importId = flag "import-id" (required string) ~doc:"STRING Id"
       and resourceSpecification =
         flag "resource-specification" (required json_arg)
           ~doc:"JSON ImportResourceSpecification"
       and mergeStrategy =
         flag "merge-strategy" (required json_arg) ~doc:"JSON MergeStrategy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_import
           (Values.StartImportRequest.make ?filePassword ~importId
              ~resourceSpecification:(Values.ImportResourceSpecification.of_json
                                        resourceSpecification)
              ~mergeStrategy:(Values.MergeStrategy.of_json mergeStrategy) ())
           (Some Values.StartImportResponse.to_json)
           (Some Values.StartImportResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botName = flag "bot-name" (required string) ~doc:"STRING Name"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and dataPrivacy =
         flag "data-privacy" (required json_arg) ~doc:"JSON DataPrivacy"
       and idleSessionTTLInSeconds =
         flag "idle-session-t-t-l-in-seconds" (required int)
           ~doc:"INT SessionTTL" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bot
           (Values.UpdateBotRequest.make ?description ~botId ~botName
              ~roleArn ~dataPrivacy:(Values.DataPrivacy.of_json dataPrivacy)
              ~idleSessionTTLInSeconds ())
           (Some Values.UpdateBotResponse.to_json)
           (Some Values.UpdateBotResponse.error_to_json)])
let update_bot_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and botVersion =
         flag "bot-version" (optional string) ~doc:"STRING BotVersion"
       and botAliasLocaleSettings =
         flag "bot-alias-locale-settings" (optional json_arg)
           ~doc:"JSON BotAliasLocaleSettingsMap"
       and conversationLogSettings =
         flag "conversation-log-settings" (optional json_arg)
           ~doc:"JSON ConversationLogSettings"
       and sentimentAnalysisSettings =
         flag "sentiment-analysis-settings" (optional json_arg)
           ~doc:"JSON SentimentAnalysisSettings"
       and botAliasId =
         flag "bot-alias-id" (required string) ~doc:"STRING BotAliasId"
       and botAliasName =
         flag "bot-alias-name" (required string) ~doc:"STRING Name"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bot_alias
           (Values.UpdateBotAliasRequest.make ?description ?botVersion
              ?botAliasLocaleSettings:(Option.map
                                         ~f:Values.BotAliasLocaleSettingsMap.of_json
                                         botAliasLocaleSettings)
              ?conversationLogSettings:(Option.map
                                          ~f:Values.ConversationLogSettings.of_json
                                          conversationLogSettings)
              ?sentimentAnalysisSettings:(Option.map
                                            ~f:Values.SentimentAnalysisSettings.of_json
                                            sentimentAnalysisSettings)
              ~botAliasId ~botAliasName ~botId ())
           (Some Values.UpdateBotAliasResponse.to_json)
           (Some Values.UpdateBotAliasResponse.error_to_json)])
let update_bot_locale =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and voiceSettings =
         flag "voice-settings" (optional json_arg) ~doc:"JSON VoiceSettings"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId"
       and nluIntentConfidenceThreshold =
         flag "nlu-intent-confidence-threshold" (required float)
           ~doc:"FLOAT ConfidenceThreshold" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bot_locale
           (Values.UpdateBotLocaleRequest.make ?description
              ?voiceSettings:(Option.map ~f:Values.VoiceSettings.of_json
                                voiceSettings) ~botId ~botVersion ~localeId
              ~nluIntentConfidenceThreshold ())
           (Some Values.UpdateBotLocaleResponse.to_json)
           (Some Values.UpdateBotLocaleResponse.error_to_json)])
let update_bot_recommendation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId"
       and botRecommendationId =
         flag "bot-recommendation-id" (required string) ~doc:"STRING Id"
       and encryptionSetting =
         flag "encryption-setting" (required json_arg)
           ~doc:"JSON EncryptionSetting" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bot_recommendation
           (Values.UpdateBotRecommendationRequest.make ~botId ~botVersion
              ~localeId ~botRecommendationId
              ~encryptionSetting:(Values.EncryptionSetting.of_json
                                    encryptionSetting) ())
           (Some Values.UpdateBotRecommendationResponse.to_json)
           (Some Values.UpdateBotRecommendationResponse.error_to_json)])
let update_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filePassword =
         flag "file-password" (optional string)
           ~doc:"STRING ImportExportFilePassword"
       and exportId = flag "export-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_export
           (Values.UpdateExportRequest.make ?filePassword ~exportId ())
           (Some Values.UpdateExportResponse.to_json)
           (Some Values.UpdateExportResponse.error_to_json)])
let update_intent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and parentIntentSignature =
         flag "parent-intent-signature" (optional string)
           ~doc:"STRING IntentSignature"
       and sampleUtterances =
         flag "sample-utterances" (optional json_arg)
           ~doc:"JSON SampleUtterancesList"
       and dialogCodeHook =
         flag "dialog-code-hook" (optional json_arg)
           ~doc:"JSON DialogCodeHookSettings"
       and fulfillmentCodeHook =
         flag "fulfillment-code-hook" (optional json_arg)
           ~doc:"JSON FulfillmentCodeHookSettings"
       and slotPriorities =
         flag "slot-priorities" (optional json_arg)
           ~doc:"JSON SlotPrioritiesList"
       and intentConfirmationSetting =
         flag "intent-confirmation-setting" (optional json_arg)
           ~doc:"JSON IntentConfirmationSetting"
       and intentClosingSetting =
         flag "intent-closing-setting" (optional json_arg)
           ~doc:"JSON IntentClosingSetting"
       and inputContexts =
         flag "input-contexts" (optional json_arg)
           ~doc:"JSON InputContextsList"
       and outputContexts =
         flag "output-contexts" (optional json_arg)
           ~doc:"JSON OutputContextsList"
       and kendraConfiguration =
         flag "kendra-configuration" (optional json_arg)
           ~doc:"JSON KendraConfiguration"
       and intentId = flag "intent-id" (required string) ~doc:"STRING Id"
       and intentName =
         flag "intent-name" (required string) ~doc:"STRING Name"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_intent
           (Values.UpdateIntentRequest.make ?description
              ?parentIntentSignature
              ?sampleUtterances:(Option.map
                                   ~f:Values.SampleUtterancesList.of_json
                                   sampleUtterances)
              ?dialogCodeHook:(Option.map
                                 ~f:Values.DialogCodeHookSettings.of_json
                                 dialogCodeHook)
              ?fulfillmentCodeHook:(Option.map
                                      ~f:Values.FulfillmentCodeHookSettings.of_json
                                      fulfillmentCodeHook)
              ?slotPriorities:(Option.map
                                 ~f:Values.SlotPrioritiesList.of_json
                                 slotPriorities)
              ?intentConfirmationSetting:(Option.map
                                            ~f:Values.IntentConfirmationSetting.of_json
                                            intentConfirmationSetting)
              ?intentClosingSetting:(Option.map
                                       ~f:Values.IntentClosingSetting.of_json
                                       intentClosingSetting)
              ?inputContexts:(Option.map ~f:Values.InputContextsList.of_json
                                inputContexts)
              ?outputContexts:(Option.map
                                 ~f:Values.OutputContextsList.of_json
                                 outputContexts)
              ?kendraConfiguration:(Option.map
                                      ~f:Values.KendraConfiguration.of_json
                                      kendraConfiguration) ~intentId
              ~intentName ~botId ~botVersion ~localeId ())
           (Some Values.UpdateIntentResponse.to_json)
           (Some Values.UpdateIntentResponse.error_to_json)])
let update_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedRevisionId =
         flag "expected-revision-id" (optional string)
           ~doc:"STRING RevisionId"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and policy = flag "policy" (required string) ~doc:"STRING Policy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource_policy
           (Values.UpdateResourcePolicyRequest.make ?expectedRevisionId
              ~resourceArn ~policy ())
           (Some Values.UpdateResourcePolicyResponse.to_json)
           (Some Values.UpdateResourcePolicyResponse.error_to_json)])
let update_slot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and slotTypeId =
         flag "slot-type-id" (optional string)
           ~doc:"STRING BuiltInOrCustomSlotTypeId"
       and obfuscationSetting =
         flag "obfuscation-setting" (optional json_arg)
           ~doc:"JSON ObfuscationSetting"
       and multipleValuesSetting =
         flag "multiple-values-setting" (optional json_arg)
           ~doc:"JSON MultipleValuesSetting"
       and slotId = flag "slot-id" (required string) ~doc:"STRING Id"
       and slotName = flag "slot-name" (required string) ~doc:"STRING Name"
       and valueElicitationSetting =
         flag "value-elicitation-setting" (required json_arg)
           ~doc:"JSON SlotValueElicitationSetting"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId"
       and intentId = flag "intent-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_slot
           (Values.UpdateSlotRequest.make ?description ?slotTypeId
              ?obfuscationSetting:(Option.map
                                     ~f:Values.ObfuscationSetting.of_json
                                     obfuscationSetting)
              ?multipleValuesSetting:(Option.map
                                        ~f:Values.MultipleValuesSetting.of_json
                                        multipleValuesSetting) ~slotId
              ~slotName
              ~valueElicitationSetting:(Values.SlotValueElicitationSetting.of_json
                                          valueElicitationSetting) ~botId
              ~botVersion ~localeId ~intentId ())
           (Some Values.UpdateSlotResponse.to_json)
           (Some Values.UpdateSlotResponse.error_to_json)])
let update_slot_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and slotTypeValues =
         flag "slot-type-values" (optional json_arg)
           ~doc:"JSON SlotTypeValues"
       and valueSelectionSetting =
         flag "value-selection-setting" (optional json_arg)
           ~doc:"JSON SlotValueSelectionSetting"
       and parentSlotTypeSignature =
         flag "parent-slot-type-signature" (optional string)
           ~doc:"STRING SlotTypeSignature"
       and externalSourceSetting =
         flag "external-source-setting" (optional json_arg)
           ~doc:"JSON ExternalSourceSetting"
       and slotTypeId =
         flag "slot-type-id" (required string) ~doc:"STRING Id"
       and slotTypeName =
         flag "slot-type-name" (required string) ~doc:"STRING Name"
       and botId = flag "bot-id" (required string) ~doc:"STRING Id"
       and botVersion =
         flag "bot-version" (required string) ~doc:"STRING DraftBotVersion"
       and localeId =
         flag "locale-id" (required string) ~doc:"STRING LocaleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_slot_type
           (Values.UpdateSlotTypeRequest.make ?description
              ?slotTypeValues:(Option.map ~f:Values.SlotTypeValues.of_json
                                 slotTypeValues)
              ?valueSelectionSetting:(Option.map
                                        ~f:Values.SlotValueSelectionSetting.of_json
                                        valueSelectionSetting)
              ?parentSlotTypeSignature
              ?externalSourceSetting:(Option.map
                                        ~f:Values.ExternalSourceSetting.of_json
                                        externalSourceSetting) ~slotTypeId
              ~slotTypeName ~botId ~botVersion ~localeId ())
           (Some Values.UpdateSlotTypeResponse.to_json)
           (Some Values.UpdateSlotTypeResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("build-bot-locale", build_bot_locale);
    ("create-bot", create_bot);
    ("create-bot-alias", create_bot_alias);
    ("create-bot-locale", create_bot_locale);
    ("create-bot-version", create_bot_version);
    ("create-export", create_export);
    ("create-intent", create_intent);
    ("create-resource-policy", create_resource_policy);
    ("create-resource-policy-statement", create_resource_policy_statement);
    ("create-slot", create_slot);
    ("create-slot-type", create_slot_type);
    ("create-upload-url", create_upload_url);
    ("delete-bot", delete_bot);
    ("delete-bot-alias", delete_bot_alias);
    ("delete-bot-locale", delete_bot_locale);
    ("delete-bot-version", delete_bot_version);
    ("delete-custom-vocabulary", delete_custom_vocabulary);
    ("delete-export", delete_export);
    ("delete-import", delete_import);
    ("delete-intent", delete_intent);
    ("delete-resource-policy", delete_resource_policy);
    ("delete-resource-policy-statement", delete_resource_policy_statement);
    ("delete-slot", delete_slot);
    ("delete-slot-type", delete_slot_type);
    ("delete-utterances", delete_utterances);
    ("describe-bot", describe_bot);
    ("describe-bot-alias", describe_bot_alias);
    ("describe-bot-locale", describe_bot_locale);
    ("describe-bot-recommendation", describe_bot_recommendation);
    ("describe-bot-version", describe_bot_version);
    ("describe-custom-vocabulary-metadata",
      describe_custom_vocabulary_metadata);
    ("describe-export", describe_export);
    ("describe-import", describe_import);
    ("describe-intent", describe_intent);
    ("describe-resource-policy", describe_resource_policy);
    ("describe-slot", describe_slot);
    ("describe-slot-type", describe_slot_type);
    ("list-aggregated-utterances", list_aggregated_utterances);
    ("list-bot-aliases", list_bot_aliases);
    ("list-bot-locales", list_bot_locales);
    ("list-bot-recommendations", list_bot_recommendations);
    ("list-bot-versions", list_bot_versions);
    ("list-bots", list_bots);
    ("list-built-in-intents", list_built_in_intents);
    ("list-built-in-slot-types", list_built_in_slot_types);
    ("list-exports", list_exports);
    ("list-imports", list_imports);
    ("list-intents", list_intents);
    ("list-recommended-intents", list_recommended_intents);
    ("list-slot-types", list_slot_types);
    ("list-slots", list_slots);
    ("list-tags-for-resource", list_tags_for_resource);
    ("search-associated-transcripts", search_associated_transcripts);
    ("start-bot-recommendation", start_bot_recommendation);
    ("start-import", start_import);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-bot", update_bot);
    ("update-bot-alias", update_bot_alias);
    ("update-bot-locale", update_bot_locale);
    ("update-bot-recommendation", update_bot_recommendation);
    ("update-export", update_export);
    ("update-intent", update_intent);
    ("update-resource-policy", update_resource_policy);
    ("update-slot", update_slot);
    ("update-slot-type", update_slot_type)]
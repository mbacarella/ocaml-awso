(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | GetControl: (GetControlRequest.t, GetControlResponse.t,
  GetControlResponse.error) t 
  | ListCommonControls: (ListCommonControlsRequest.t,
  ListCommonControlsResponse.t, ListCommonControlsResponse.error) t 
  | ListControlMappings: (ListControlMappingsRequest.t,
  ListControlMappingsResponse.t, ListControlMappingsResponse.error) t 
  | ListControls: (ListControlsRequest.t, ListControlsResponse.t,
  ListControlsResponse.error) t 
  | ListDomains: (ListDomainsRequest.t, ListDomainsResponse.t,
  ListDomainsResponse.error) t 
  | ListObjectives: (ListObjectivesRequest.t, ListObjectivesResponse.t,
  ListObjectivesResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | GetControl -> `POST
  | ListCommonControls -> `POST
  | ListControlMappings -> `POST
  | ListControls -> `POST
  | ListDomains -> `POST
  | ListObjectives -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | GetControl -> (Format.kasprintf Uri.of_string) "/get-control"
      | ListCommonControls ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/common-controls")
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("maxResults",
                          (MaxListCommonControlsResults.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (PaginationToken.to_header v)))
                 x.nextToken])
      | ListControlMappings ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/list-control-mappings")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("nextToken", (PaginationToken.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v ->
                       ("maxResults",
                         (MaxListControlMappingsResults.to_header v)))
                 x.maxResults])
      | ListControls ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/list-controls")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("nextToken", (PaginationToken.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v ->
                       ("maxResults", (MaxListControlsResults.to_header v)))
                 x.maxResults])
      | ListDomains ->
          Uri.add_query_params' ((Format.kasprintf Uri.of_string) "/domains")
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("maxResults", (MaxListDomainsResults.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (PaginationToken.to_header v)))
                 x.nextToken])
      | ListObjectives ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/objectives")
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("maxResults",
                          (MaxListObjectivesResults.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (PaginationToken.to_header v)))
                 x.nextToken]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | GetControl ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ControlArn",
                           (ControlArn.to_value
                              req.GetControlRequest.controlArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListCommonControls ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListCommonControlsRequest.commonControlFilter
                         ~f:(fun x ->
                               ("CommonControlFilter",
                                 (CommonControlFilter.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListControlMappings ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListControlMappingsRequest.filter
                         ~f:(fun x ->
                               ("Filter", (ControlMappingFilter.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListControls ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListControlsRequest.filter
                         ~f:(fun x -> ("Filter", (ControlFilter.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListDomains ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListObjectives ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListObjectivesRequest.objectiveFilter
                         ~f:(fun x ->
                               ("ObjectiveFilter",
                                 (ObjectiveFilter.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | GetControl ->
      if is_success
      then Ok (GetControlResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetControlResponse.error_of_json))
  | ListCommonControls ->
      if is_success
      then Ok (ListCommonControlsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListCommonControlsResponse.error_of_json))
  | ListControlMappings ->
      if is_success
      then Ok (ListControlMappingsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListControlMappingsResponse.error_of_json))
  | ListControls ->
      if is_success
      then Ok (ListControlsResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListControlsResponse.error_of_json))
  | ListDomains ->
      if is_success
      then Ok (ListDomainsResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListDomainsResponse.error_of_json))
  | ListObjectives ->
      if is_success
      then Ok (ListObjectivesResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListObjectivesResponse.error_of_json))
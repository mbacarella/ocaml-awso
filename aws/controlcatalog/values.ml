(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.controlcatalog
let apiVersion = "2018-05-10"
let endpointPrefix = "controlcatalog"
let serviceFullName = "AWS Control Catalog"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module CommonControlArn =
  struct
    type nonrec t = string
    let context_ = "CommonControlArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:41) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:(aws(?:[-a-z]*)?):controlcatalog:::common-control/[0-9a-z]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CommonControlArn" j
    let to_json = simple_to_json to_value
  end
module FrameworkItem =
  struct
    type nonrec t = string
    let context_ = "FrameworkItem"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:250) >>=
             (fun () -> check_string_min i ~min:3));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FrameworkItem" j
    let to_json = simple_to_json to_value
  end
module FrameworkName =
  struct
    type nonrec t = string
    let context_ = "FrameworkName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:250) >>=
             (fun () -> check_string_min i ~min:3));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FrameworkName" j
    let to_json = simple_to_json to_value
  end
module ControlArn =
  struct
    type nonrec t = string
    let context_ = "ControlArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:34) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:(aws(?:[-a-z]*)?):(controlcatalog|controltower):[a-zA-Z0-9-]*::control/[0-9a-zA-Z_\\-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ControlArn" j
    let to_json = simple_to_json to_value
  end
module ControlRelationType =
  struct
    type nonrec t =
      | COMPLEMENTARY 
      | ALTERNATIVE 
      | MUTUALLY_EXCLUSIVE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | COMPLEMENTARY -> "COMPLEMENTARY"
      | ALTERNATIVE -> "ALTERNATIVE"
      | MUTUALLY_EXCLUSIVE -> "MUTUALLY_EXCLUSIVE"
      | Non_static_id s -> s
    let of_string =
      function
      | "COMPLEMENTARY" -> COMPLEMENTARY
      | "ALTERNATIVE" -> ALTERNATIVE
      | "MUTUALLY_EXCLUSIVE" -> MUTUALLY_EXCLUSIVE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ControlRelationType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ControlRelationType" j)
    let to_json = simple_to_json to_value
  end
module DomainArn =
  struct
    type nonrec t = string
    let context_ = "DomainArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:33) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:(aws(?:[-a-z]*)?):controlcatalog:::domain/[0-9a-z]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DomainArn" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module ControlAlias =
  struct
    type nonrec t = string
    let context_ = "ControlAlias"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"[a-zA-Z0-9](?:[a-zA-Z0-9_.-]{0,254}[a-zA-Z0-9])");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ControlAlias" j
    let to_json = simple_to_json to_value
  end
module GovernedResource =
  struct
    type nonrec t = string
    let context_ = "GovernedResource"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"[A-Za-z0-9]{2,64}::[A-Za-z0-9]{2,64}::[A-Za-z0-9]{2,64}");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GovernedResource" j
    let to_json = simple_to_json to_value
  end
module ImplementationIdentifier =
  struct
    type nonrec t = string
    let context_ = "ImplementationIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9_\\.-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ImplementationIdentifier" j
    let to_json = simple_to_json to_value
  end
module ImplementationType =
  struct
    type nonrec t = string
    let context_ = "ImplementationType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:7) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[A-Za-z0-9]+(::[A-Za-z0-9_]+){2,3}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ImplementationType" j
    let to_json = simple_to_json to_value
  end
module CommonControlMappingDetails =
  struct
    type nonrec t =
      {
      commonControlArn: CommonControlArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that identifies the common control in the mapping."]}
    let make ?commonControlArn = fun () -> { commonControlArn }
    let to_value x =
      structure_to_value
        [("CommonControlArn",
           (Option.map x.commonControlArn ~f:CommonControlArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let commonControlArn =
        (Option.map ~f:CommonControlArn.of_xml)
          (Xml.child xml_arg0 "CommonControlArn") in
      make ?commonControlArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let commonControlArn =
        field_map json__ "CommonControlArn" CommonControlArn.of_json in
      make ?commonControlArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that contains details about a common control mapping. In particular, it returns the Amazon Resource Name (ARN) of the common control."]
module FrameworkMappingDetails =
  struct
    type nonrec t =
      {
      name: FrameworkName.t option
        [@ocaml.doc
          "The name of the compliance framework that the control maps to."];
      item: FrameworkItem.t option
        [@ocaml.doc
          "The specific item or requirement within the framework that the control maps to."]}
    let make ?name = fun ?item -> fun () -> { name; item }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:FrameworkName.to_value));
        ("Item", (Option.map x.item ~f:FrameworkItem.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let item =
        (Option.map ~f:FrameworkItem.of_xml) (Xml.child xml_arg0 "Item") in
      let name =
        (Option.map ~f:FrameworkName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?item ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let item = field_map json__ "Item" FrameworkItem.of_json in
      let name = field_map json__ "Name" FrameworkName.of_json in
      make ?item ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that contains details about a framework mapping, including the framework name and specific item within the framework that the control maps to."]
module RelatedControlMappingDetails =
  struct
    type nonrec t =
      {
      controlArn: ControlArn.t option
        [@ocaml.doc "The unique identifier of a control."];
      relationType: ControlRelationType.t option
        [@ocaml.doc
          "Returns an enumerated value that represents the relationship between two or more controls."]}
    let make ?controlArn =
      fun ?relationType -> fun () -> { controlArn; relationType }
    let to_value x =
      structure_to_value
        [("ControlArn", (Option.map x.controlArn ~f:ControlArn.to_value));
        ("RelationType",
          (Option.map x.relationType ~f:ControlRelationType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let relationType =
        (Option.map ~f:ControlRelationType.of_xml)
          (Xml.child xml_arg0 "RelationType") in
      let controlArn =
        (Option.map ~f:ControlArn.of_xml) (Xml.child xml_arg0 "ControlArn") in
      make ?relationType ?controlArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let relationType =
        field_map json__ "RelationType" ControlRelationType.of_json in
      let controlArn = field_map json__ "ControlArn" ControlArn.of_json in
      make ?relationType ?controlArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that describes a control's relationship status with other controls."]
module ObjectiveArn =
  struct
    type nonrec t = string
    let context_ = "ObjectiveArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:36) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:(aws(?:[-a-z]*)?):controlcatalog:::objective/[0-9a-z]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ObjectiveArn" j
    let to_json = simple_to_json to_value
  end
module AssociatedDomainSummary =
  struct
    type nonrec t =
      {
      arn: DomainArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the related domain."];
      name: String_.t option [@ocaml.doc "The name of the related domain."]}
    let make ?arn = fun ?name -> fun () -> { arn; name }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:DomainArn.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let arn = (Option.map ~f:DomainArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?name ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map json__ "Name" String_.of_json in
      let arn = field_map json__ "Arn" DomainArn.of_json in
      make ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A summary of the domain that a common control or an objective belongs to."]
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module DomainResourceFilter =
  struct
    type nonrec t =
      {
      arn: DomainArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the domain."]}
    let make ?arn = fun () -> { arn }
    let to_value x =
      structure_to_value [("Arn", (Option.map x.arn ~f:DomainArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn = (Option.map ~f:DomainArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let arn = field_map json__ "Arn" DomainArn.of_json in make ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The domain resource that's being used as a filter."]
module ControlAliases =
  struct
    type nonrec t = ControlAlias.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ControlAlias.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ControlAlias.of_xml)
    let of_json j =
      list_of_json ~kind:"ControlAliases" ~of_json:ControlAlias.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlBehavior =
  struct
    type nonrec t =
      | PREVENTIVE 
      | PROACTIVE 
      | DETECTIVE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PREVENTIVE -> "PREVENTIVE"
      | PROACTIVE -> "PROACTIVE"
      | DETECTIVE -> "DETECTIVE"
      | Non_static_id s -> s
    let of_string =
      function
      | "PREVENTIVE" -> PREVENTIVE
      | "PROACTIVE" -> PROACTIVE
      | "DETECTIVE" -> DETECTIVE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ControlBehavior" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ControlBehavior" j)
    let to_json = simple_to_json to_value
  end
module ControlSeverity =
  struct
    type nonrec t =
      | LOW 
      | MEDIUM 
      | HIGH 
      | CRITICAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | LOW -> "LOW"
      | MEDIUM -> "MEDIUM"
      | HIGH -> "HIGH"
      | CRITICAL -> "CRITICAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "LOW" -> LOW
      | "MEDIUM" -> MEDIUM
      | "HIGH" -> HIGH
      | "CRITICAL" -> CRITICAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ControlSeverity" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ControlSeverity" j)
    let to_json = simple_to_json to_value
  end
module GovernedResources =
  struct
    type nonrec t = GovernedResource.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GovernedResource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GovernedResource.of_xml)
    let of_json j =
      list_of_json ~kind:"GovernedResources"
        ~of_json:GovernedResource.of_json j
    let to_json v = composed_to_json to_value v
  end
module ImplementationSummary =
  struct
    type nonrec t =
      {
      type_: ImplementationType.t option
        [@ocaml.doc
          "A string that represents the Amazon Web Services service that implements this control. For example, a value of AWS::Config::ConfigRule indicates that the control is implemented by Amazon Web Services Config, and AWS::SecurityHub::SecurityControl indicates implementation by Amazon Web Services Security Hub."];
      identifier: ImplementationIdentifier.t option
        [@ocaml.doc
          "The identifier originally assigned by the Amazon Web Services service that implements the control. For example, CODEPIPELINE_DEPLOYMENT_COUNT_CHECK."]}
    let make ?type_ = fun ?identifier -> fun () -> { type_; identifier }
    let to_value x =
      structure_to_value
        [("Type", (Option.map x.type_ ~f:ImplementationType.to_value));
        ("Identifier",
          (Option.map x.identifier ~f:ImplementationIdentifier.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identifier =
        (Option.map ~f:ImplementationIdentifier.of_xml)
          (Xml.child xml_arg0 "Identifier") in
      let type_ =
        (Option.map ~f:ImplementationType.of_xml) (Xml.child xml_arg0 "Type") in
      make ?identifier ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let identifier =
        field_map json__ "Identifier" ImplementationIdentifier.of_json in
      let type_ = field_map json__ "Type" ImplementationType.of_json in
      make ?identifier ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A summary of how the control is implemented, including the Amazon Web Services service that enforces the control and its service-specific identifier. For example, the value of this field could indicate that the control is implemented as an Amazon Web Services Config Rule or an Amazon Web Services Security Hub control."]
module ImplementationIdentifierFilterList =
  struct
    type nonrec t = ImplementationIdentifier.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ImplementationIdentifier.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ImplementationIdentifier.of_xml)
    let of_json j =
      list_of_json ~kind:"ImplementationIdentifierFilterList"
        ~of_json:ImplementationIdentifier.of_json j
    let to_json v = composed_to_json to_value v
  end
module ImplementationTypeFilterList =
  struct
    type nonrec t = ImplementationType.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ImplementationType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ImplementationType.of_xml)
    let of_json j =
      list_of_json ~kind:"ImplementationTypeFilterList"
        ~of_json:ImplementationType.of_json j
    let to_json v = composed_to_json to_value v
  end
module Mapping =
  struct
    type nonrec t =
      {
      framework: FrameworkMappingDetails.t option
        [@ocaml.doc
          "The framework mapping details when the mapping type relates to a compliance framework."];
      commonControl: CommonControlMappingDetails.t option
        [@ocaml.doc
          "The common control mapping details when the mapping type relates to a common control."];
      relatedControl: RelatedControlMappingDetails.t option
        [@ocaml.doc
          "Returns information about controls that are related to the specified control."]}
    let make ?framework =
      fun ?commonControl ->
        fun ?relatedControl ->
          fun () -> { framework; commonControl; relatedControl }
    let to_value x =
      structure_to_value
        [("Framework",
           (Option.map x.framework ~f:FrameworkMappingDetails.to_value));
        ("CommonControl",
          (Option.map x.commonControl ~f:CommonControlMappingDetails.to_value));
        ("RelatedControl",
          (Option.map x.relatedControl
             ~f:RelatedControlMappingDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let relatedControl =
        (Option.map ~f:RelatedControlMappingDetails.of_xml)
          (Xml.child xml_arg0 "RelatedControl") in
      let commonControl =
        (Option.map ~f:CommonControlMappingDetails.of_xml)
          (Xml.child xml_arg0 "CommonControl") in
      let framework =
        (Option.map ~f:FrameworkMappingDetails.of_xml)
          (Xml.child xml_arg0 "Framework") in
      make ?relatedControl ?commonControl ?framework ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let relatedControl =
        field_map json__ "RelatedControl"
          RelatedControlMappingDetails.of_json in
      let commonControl =
        field_map json__ "CommonControl" CommonControlMappingDetails.of_json in
      let framework =
        field_map json__ "Framework" FrameworkMappingDetails.of_json in
      make ?relatedControl ?commonControl ?framework ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that contains the details of a mapping relationship, which can be either to a framework or to a common control."]
module MappingType =
  struct
    type nonrec t =
      | FRAMEWORK 
      | COMMON_CONTROL 
      | RELATED_CONTROL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | FRAMEWORK -> "FRAMEWORK"
      | COMMON_CONTROL -> "COMMON_CONTROL"
      | RELATED_CONTROL -> "RELATED_CONTROL"
      | Non_static_id s -> s
    let of_string =
      function
      | "FRAMEWORK" -> FRAMEWORK
      | "COMMON_CONTROL" -> COMMON_CONTROL
      | "RELATED_CONTROL" -> RELATED_CONTROL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration MappingType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"MappingType" j)
    let to_json = simple_to_json to_value
  end
module AssociatedObjectiveSummary =
  struct
    type nonrec t =
      {
      arn: ObjectiveArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the related objective."];
      name: String_.t option
        [@ocaml.doc "The name of the related objective."]}
    let make ?arn = fun ?name -> fun () -> { arn; name }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ObjectiveArn.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let arn =
        (Option.map ~f:ObjectiveArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?name ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map json__ "Name" String_.of_json in
      let arn = field_map json__ "Arn" ObjectiveArn.of_json in
      make ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A summary of the objective that a common control supports."]
module ObjectiveResourceFilter =
  struct
    type nonrec t =
      {
      arn: ObjectiveArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the objective."]}
    let make ?arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ObjectiveArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        (Option.map ~f:ObjectiveArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let arn = field_map json__ "Arn" ObjectiveArn.of_json in make ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The objective resource that's being used as a filter."]
module RegionCode =
  struct
    type nonrec t = string
    let context_ = "RegionCode"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[a-zA-Z0-9-]{1,128}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RegionCode" j
    let to_json = simple_to_json to_value
  end
module ObjectiveSummary =
  struct
    type nonrec t =
      {
      arn: ObjectiveArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that identifies the objective."];
      name: String_.t option [@ocaml.doc "The name of the objective."];
      description: String_.t option
        [@ocaml.doc "The description of the objective."];
      domain: AssociatedDomainSummary.t option
        [@ocaml.doc "The domain that the objective belongs to."];
      createTime: Timestamp.t option
        [@ocaml.doc "The time when the objective was created."];
      lastUpdateTime: Timestamp.t option
        [@ocaml.doc "The time when the objective was most recently updated."]}
    let make ?arn =
      fun ?name ->
        fun ?description ->
          fun ?domain ->
            fun ?createTime ->
              fun ?lastUpdateTime ->
                fun () ->
                  {
                    arn;
                    name;
                    description;
                    domain;
                    createTime;
                    lastUpdateTime
                  }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ObjectiveArn.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("Domain", (Option.map x.domain ~f:AssociatedDomainSummary.to_value));
        ("CreateTime", (Option.map x.createTime ~f:Timestamp.to_value));
        ("LastUpdateTime",
          (Option.map x.lastUpdateTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastUpdateTime") in
      let createTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreateTime") in
      let domain =
        (Option.map ~f:AssociatedDomainSummary.of_xml)
          (Xml.child xml_arg0 "Domain") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let arn =
        (Option.map ~f:ObjectiveArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?lastUpdateTime ?createTime ?domain ?description ?name ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastUpdateTime =
        field_map json__ "LastUpdateTime" Timestamp.of_json in
      let createTime = field_map json__ "CreateTime" Timestamp.of_json in
      let domain = field_map json__ "Domain" AssociatedDomainSummary.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let name = field_map json__ "Name" String_.of_json in
      let arn = field_map json__ "Arn" ObjectiveArn.of_json in
      make ?lastUpdateTime ?createTime ?domain ?description ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A summary of metadata for an objective."]
module DomainResourceFilterList =
  struct
    type nonrec t = DomainResourceFilter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DomainResourceFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DomainResourceFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"DomainResourceFilterList"
        ~of_json:DomainResourceFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module DomainSummary =
  struct
    type nonrec t =
      {
      arn: DomainArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that identifies the domain."];
      name: String_.t option [@ocaml.doc "The name of the domain."];
      description: String_.t option
        [@ocaml.doc "The description of the domain."];
      createTime: Timestamp.t option
        [@ocaml.doc "The time when the domain was created."];
      lastUpdateTime: Timestamp.t option
        [@ocaml.doc "The time when the domain was most recently updated."]}
    let make ?arn =
      fun ?name ->
        fun ?description ->
          fun ?createTime ->
            fun ?lastUpdateTime ->
              fun () ->
                { arn; name; description; createTime; lastUpdateTime }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:DomainArn.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("CreateTime", (Option.map x.createTime ~f:Timestamp.to_value));
        ("LastUpdateTime",
          (Option.map x.lastUpdateTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastUpdateTime") in
      let createTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreateTime") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let arn = (Option.map ~f:DomainArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?lastUpdateTime ?createTime ?description ?name ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastUpdateTime =
        field_map json__ "LastUpdateTime" Timestamp.of_json in
      let createTime = field_map json__ "CreateTime" Timestamp.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let name = field_map json__ "Name" String_.of_json in
      let arn = field_map json__ "Arn" DomainArn.of_json in
      make ?lastUpdateTime ?createTime ?description ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A summary of metadata for a domain."]
module ControlSummary =
  struct
    type nonrec t =
      {
      arn: ControlArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the control."];
      aliases: ControlAliases.t option
        [@ocaml.doc
          "A list of alternative identifiers for the control. These are human-readable designators, such as SH.S3.1. Several aliases can refer to the same control across different Amazon Web Services services or compliance frameworks."];
      name: String_.t option [@ocaml.doc "The display name of the control."];
      description: String_.t option
        [@ocaml.doc
          "A description of the control, as it may appear in the console. Describes the functionality of the control."];
      behavior: ControlBehavior.t option
        [@ocaml.doc
          "An enumerated type, with the following possible values:"];
      severity: ControlSeverity.t option
        [@ocaml.doc
          "An enumerated type, with the following possible values:"];
      implementation: ImplementationSummary.t option
        [@ocaml.doc
          "An object of type ImplementationSummary that describes how the control is implemented."];
      createTime: Timestamp.t option
        [@ocaml.doc
          "A timestamp that notes the time when the control was released (start of its life) as a governance capability in Amazon Web Services."];
      governedResources: GovernedResources.t option
        [@ocaml.doc
          "A list of Amazon Web Services resource types that are governed by this control. This information helps you understand which controls can govern certain types of resources, and conversely, which resources are affected when the control is implemented. The resources are represented as Amazon Web Services CloudFormation resource types. If GovernedResources cannot be represented by available CloudFormation resource types, it\226\128\153s returned as an empty list."]}
    let make ?arn =
      fun ?aliases ->
        fun ?name ->
          fun ?description ->
            fun ?behavior ->
              fun ?severity ->
                fun ?implementation ->
                  fun ?createTime ->
                    fun ?governedResources ->
                      fun () ->
                        {
                          arn;
                          aliases;
                          name;
                          description;
                          behavior;
                          severity;
                          implementation;
                          createTime;
                          governedResources
                        }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ControlArn.to_value));
        ("Aliases", (Option.map x.aliases ~f:ControlAliases.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("Behavior", (Option.map x.behavior ~f:ControlBehavior.to_value));
        ("Severity", (Option.map x.severity ~f:ControlSeverity.to_value));
        ("Implementation",
          (Option.map x.implementation ~f:ImplementationSummary.to_value));
        ("CreateTime", (Option.map x.createTime ~f:Timestamp.to_value));
        ("GovernedResources",
          (Option.map x.governedResources ~f:GovernedResources.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let governedResources =
        (Option.map ~f:GovernedResources.of_xml)
          (Xml.child xml_arg0 "GovernedResources") in
      let createTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreateTime") in
      let implementation =
        (Option.map ~f:ImplementationSummary.of_xml)
          (Xml.child xml_arg0 "Implementation") in
      let severity =
        (Option.map ~f:ControlSeverity.of_xml)
          (Xml.child xml_arg0 "Severity") in
      let behavior =
        (Option.map ~f:ControlBehavior.of_xml)
          (Xml.child xml_arg0 "Behavior") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let aliases =
        (Option.map ~f:ControlAliases.of_xml) (Xml.child xml_arg0 "Aliases") in
      let arn = (Option.map ~f:ControlArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?governedResources ?createTime ?implementation ?severity ?behavior
        ?description ?name ?aliases ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let governedResources =
        field_map json__ "GovernedResources" GovernedResources.of_json in
      let createTime = field_map json__ "CreateTime" Timestamp.of_json in
      let implementation =
        field_map json__ "Implementation" ImplementationSummary.of_json in
      let severity = field_map json__ "Severity" ControlSeverity.of_json in
      let behavior = field_map json__ "Behavior" ControlBehavior.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let name = field_map json__ "Name" String_.of_json in
      let aliases = field_map json__ "Aliases" ControlAliases.of_json in
      let arn = field_map json__ "Arn" ControlArn.of_json in
      make ?governedResources ?createTime ?implementation ?severity ?behavior
        ?description ?name ?aliases ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Overview of information about a control."]
module ImplementationFilter =
  struct
    type nonrec t =
      {
      types: ImplementationTypeFilterList.t option
        [@ocaml.doc
          "A list of implementation types that can serve as filters. For example, you can filter for controls implemented as Amazon Web Services Config Rules by specifying AWS::Config::ConfigRule as a type."];
      identifiers: ImplementationIdentifierFilterList.t option
        [@ocaml.doc
          "A list of service-specific identifiers that can serve as filters. For example, you can filter for controls with specific Amazon Web Services Config Rule IDs or Security Hub Control IDs."]}
    let make ?types = fun ?identifiers -> fun () -> { types; identifiers }
    let to_value x =
      structure_to_value
        [("Types",
           (Option.map x.types ~f:ImplementationTypeFilterList.to_value));
        ("Identifiers",
          (Option.map x.identifiers
             ~f:ImplementationIdentifierFilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identifiers =
        (Option.map ~f:ImplementationIdentifierFilterList.of_xml)
          (Xml.child xml_arg0 "Identifiers") in
      let types =
        (Option.map ~f:ImplementationTypeFilterList.of_xml)
          (Xml.child xml_arg0 "Types") in
      make ?identifiers ?types ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let identifiers =
        field_map json__ "Identifiers"
          ImplementationIdentifierFilterList.of_json in
      let types =
        field_map json__ "Types" ImplementationTypeFilterList.of_json in
      make ?identifiers ?types ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that defines filtering criteria for control implementations. You can use this filter to find controls that are implemented by specific Amazon Web Services services or with specific service identifiers."]
module ControlMapping =
  struct
    type nonrec t =
      {
      controlArn: ControlArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that identifies the control in the mapping."];
      mappingType: MappingType.t option
        [@ocaml.doc
          "The type of mapping relationship between the control and other entities."];
      mapping: Mapping.t option
        [@ocaml.doc
          "The details of the mapping relationship, for example, containing framework, common control, or related control information."]}
    let make ?controlArn =
      fun ?mappingType ->
        fun ?mapping -> fun () -> { controlArn; mappingType; mapping }
    let to_value x =
      structure_to_value
        [("ControlArn", (Option.map x.controlArn ~f:ControlArn.to_value));
        ("MappingType", (Option.map x.mappingType ~f:MappingType.to_value));
        ("Mapping", (Option.map x.mapping ~f:Mapping.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let mapping =
        (Option.map ~f:Mapping.of_xml) (Xml.child xml_arg0 "Mapping") in
      let mappingType =
        (Option.map ~f:MappingType.of_xml) (Xml.child xml_arg0 "MappingType") in
      let controlArn =
        (Option.map ~f:ControlArn.of_xml) (Xml.child xml_arg0 "ControlArn") in
      make ?mapping ?mappingType ?controlArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let mapping = field_map json__ "Mapping" Mapping.of_json in
      let mappingType = field_map json__ "MappingType" MappingType.of_json in
      let controlArn = field_map json__ "ControlArn" ControlArn.of_json in
      make ?mapping ?mappingType ?controlArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that contains information about a control mapping, including the control ARN, mapping type, and mapping details."]
module CommonControlArnFilterList =
  struct
    type nonrec t = CommonControlArn.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CommonControlArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CommonControlArn.of_xml)
    let of_json j =
      list_of_json ~kind:"CommonControlArnFilterList"
        ~of_json:CommonControlArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlArnFilterList =
  struct
    type nonrec t = ControlArn.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ControlArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ControlArn.of_xml)
    let of_json j =
      list_of_json ~kind:"ControlArnFilterList" ~of_json:ControlArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module MappingTypeFilterList =
  struct
    type nonrec t = MappingType.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MappingType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MappingType.of_xml)
    let of_json j =
      list_of_json ~kind:"MappingTypeFilterList" ~of_json:MappingType.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module CommonControlSummary =
  struct
    type nonrec t =
      {
      arn: CommonControlArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that identifies the common control."];
      name: String_.t option [@ocaml.doc "The name of the common control."];
      description: String_.t option
        [@ocaml.doc "The description of the common control."];
      domain: AssociatedDomainSummary.t option
        [@ocaml.doc "The domain that the common control belongs to."];
      objective: AssociatedObjectiveSummary.t option
        [@ocaml.doc "The objective that the common control belongs to."];
      createTime: Timestamp.t option
        [@ocaml.doc "The time when the common control was created."];
      lastUpdateTime: Timestamp.t option
        [@ocaml.doc
          "The time when the common control was most recently updated."]}
    let make ?arn =
      fun ?name ->
        fun ?description ->
          fun ?domain ->
            fun ?objective ->
              fun ?createTime ->
                fun ?lastUpdateTime ->
                  fun () ->
                    {
                      arn;
                      name;
                      description;
                      domain;
                      objective;
                      createTime;
                      lastUpdateTime
                    }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:CommonControlArn.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("Domain", (Option.map x.domain ~f:AssociatedDomainSummary.to_value));
        ("Objective",
          (Option.map x.objective ~f:AssociatedObjectiveSummary.to_value));
        ("CreateTime", (Option.map x.createTime ~f:Timestamp.to_value));
        ("LastUpdateTime",
          (Option.map x.lastUpdateTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastUpdateTime") in
      let createTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreateTime") in
      let objective =
        (Option.map ~f:AssociatedObjectiveSummary.of_xml)
          (Xml.child xml_arg0 "Objective") in
      let domain =
        (Option.map ~f:AssociatedDomainSummary.of_xml)
          (Xml.child xml_arg0 "Domain") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let arn =
        (Option.map ~f:CommonControlArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?lastUpdateTime ?createTime ?objective ?domain ?description ?name
        ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastUpdateTime =
        field_map json__ "LastUpdateTime" Timestamp.of_json in
      let createTime = field_map json__ "CreateTime" Timestamp.of_json in
      let objective =
        field_map json__ "Objective" AssociatedObjectiveSummary.of_json in
      let domain = field_map json__ "Domain" AssociatedDomainSummary.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let name = field_map json__ "Name" String_.of_json in
      let arn = field_map json__ "Arn" CommonControlArn.of_json in
      make ?lastUpdateTime ?createTime ?objective ?domain ?description ?name
        ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A summary of metadata for a common control."]
module ObjectiveResourceFilterList =
  struct
    type nonrec t = ObjectiveResourceFilter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ObjectiveResourceFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ObjectiveResourceFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"ObjectiveResourceFilterList"
        ~of_json:ObjectiveResourceFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlParameter =
  struct
    type nonrec t =
      {
      name: String_.t option
        [@ocaml.doc
          "The parameter name. This name is the parameter key when you call EnableControl or UpdateEnabledControl ."]}
    let make ?name = fun () -> { name }
    let to_value x =
      structure_to_value [("Name", (Option.map x.name ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map json__ "Name" String_.of_json in make ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Five types of control parameters are supported. AllowedRegions: List of Amazon Web Services Regions exempted from the control. Each string is expected to be an Amazon Web Services Region code. This parameter is mandatory for the OU Region deny control, CT.MULTISERVICE.PV.1. Example: \\[\"us-east-1\",\"us-west-2\"\\] ExemptedActions: List of Amazon Web Services IAM actions exempted from the control. Each string is expected to be an IAM action. Example: \\[\"logs:DescribeLogGroups\",\"logs:StartQuery\",\"logs:GetQueryResults\"\\] ExemptedPrincipalArns: List of Amazon Web Services IAM principal ARNs exempted from the control. Each string is expected to be an IAM principal that follows the format arn:partition:service::account:resource Example: \\[\"arn:aws:iam::*:role/ReadOnly\",\"arn:aws:sts::*:assumed-role/ReadOnly/*\"\\] ExemptedResourceArns: List of resource ARNs exempted from the control. Each string is expected to be a resource ARN. Example: \\[\"arn:aws:s3:::my-bucket-name\"\\] ExemptAssumeRoot: A parameter that lets you choose whether to exempt requests made with AssumeRoot from this control, for this OU. For member accounts, the AssumeRoot property is included in requests initiated by IAM centralized root access. This parameter applies only to the AWS-GR_RESTRICT_ROOT_USER control. If you add the parameter when enabling the control, the AssumeRoot exemption is allowed. If you omit the parameter, the AssumeRoot exception is not permitted. The parameter does not accept False as a value. Example: Enabling the control and allowing AssumeRoot \\{ \"controlIdentifier\": \"arn:aws:controlcatalog:::control/5kvme4m5d2b4d7if2fs5yg2ui\", \"parameters\": \\[ \\{ \"key\": \"ExemptAssumeRoot\", \"value\": true \\} \\], \"targetIdentifier\": \"arn:aws:organizations::8633900XXXXX:ou/o-6jmn81636m/ou-qsah-jtiihcla\" \\}"]
module ControlScope =
  struct
    type nonrec t =
      | GLOBAL 
      | REGIONAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GLOBAL -> "GLOBAL"
      | REGIONAL -> "REGIONAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "GLOBAL" -> GLOBAL
      | "REGIONAL" -> REGIONAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ControlScope" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ControlScope" j)
    let to_json = simple_to_json to_value
  end
module DeployableRegions =
  struct
    type nonrec t = RegionCode.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:RegionCode.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RegionCode.of_xml)
    let of_json j =
      list_of_json ~kind:"DeployableRegions" ~of_json:RegionCode.of_json j
    let to_json v = composed_to_json to_value v
  end
module AccessDeniedException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You do not have sufficient access to perform this action."]
module InternalServerException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An internal service error occurred during the processing of your request. Try again later."]
module ObjectiveSummaryList =
  struct
    type nonrec t = ObjectiveSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ObjectiveSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ObjectiveSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ObjectiveSummaryList"
        ~of_json:ObjectiveSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module PaginationToken =
  struct
    type nonrec t = string
    let context_ = "PaginationToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PaginationToken" j
    let to_json = simple_to_json to_value
  end
module ThrottlingException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request was denied due to request throttling."]
module ValidationException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request has invalid or missing parameters."]
module MaxListObjectivesResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxListObjectivesResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ObjectiveFilter =
  struct
    type nonrec t =
      {
      domains: DomainResourceFilterList.t option
        [@ocaml.doc
          "The domain that's used as filter criteria. You can use this parameter to specify one domain ARN at a time. Passing multiple ARNs in the ObjectiveFilter isn\226\128\153t supported."]}
    let make ?domains = fun () -> { domains }
    let to_value x =
      structure_to_value
        [("Domains",
           (Option.map x.domains ~f:DomainResourceFilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domains =
        (Option.map ~f:DomainResourceFilterList.of_xml)
          (Xml.child xml_arg0 "Domains") in
      make ?domains ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domains =
        field_map json__ "Domains" DomainResourceFilterList.of_json in
      make ?domains ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An optional filter that narrows the list of objectives to a specific domain."]
module DomainSummaryList =
  struct
    type nonrec t = DomainSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DomainSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DomainSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"DomainSummaryList" ~of_json:DomainSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxListDomainsResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxListDomainsResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Controls =
  struct
    type nonrec t = ControlSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ControlSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ControlSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"Controls" ~of_json:ControlSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlFilter =
  struct
    type nonrec t =
      {
      implementations: ImplementationFilter.t option
        [@ocaml.doc
          "A filter that narrows the results to controls with specific implementation types or identifiers. This field allows you to find controls that are implemented by specific Amazon Web Services services or with specific service identifiers."]}
    let make ?implementations = fun () -> { implementations }
    let to_value x =
      structure_to_value
        [("Implementations",
           (Option.map x.implementations ~f:ImplementationFilter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let implementations =
        (Option.map ~f:ImplementationFilter.of_xml)
          (Xml.child xml_arg0 "Implementations") in
      make ?implementations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let implementations =
        field_map json__ "Implementations" ImplementationFilter.of_json in
      make ?implementations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that defines filtering criteria for the ListControls operation. You can use this filter to narrow down the list of controls based on their implementation details."]
module MaxListControlsResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxListControlsResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ControlMappings =
  struct
    type nonrec t = ControlMapping.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ControlMapping.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ControlMapping.of_xml)
    let of_json j =
      list_of_json ~kind:"ControlMappings" ~of_json:ControlMapping.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlMappingFilter =
  struct
    type nonrec t =
      {
      controlArns: ControlArnFilterList.t option
        [@ocaml.doc
          "A list of control ARNs to filter the mappings. When specified, only mappings associated with these controls are returned."];
      commonControlArns: CommonControlArnFilterList.t option
        [@ocaml.doc
          "A list of common control ARNs to filter the mappings. When specified, only mappings associated with these common controls are returned."];
      mappingTypes: MappingTypeFilterList.t option
        [@ocaml.doc
          "A list of mapping types to filter the mappings. When specified, only mappings of these types are returned."]}
    let make ?controlArns =
      fun ?commonControlArns ->
        fun ?mappingTypes ->
          fun () -> { controlArns; commonControlArns; mappingTypes }
    let to_value x =
      structure_to_value
        [("ControlArns",
           (Option.map x.controlArns ~f:ControlArnFilterList.to_value));
        ("CommonControlArns",
          (Option.map x.commonControlArns
             ~f:CommonControlArnFilterList.to_value));
        ("MappingTypes",
          (Option.map x.mappingTypes ~f:MappingTypeFilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let mappingTypes =
        (Option.map ~f:MappingTypeFilterList.of_xml)
          (Xml.child xml_arg0 "MappingTypes") in
      let commonControlArns =
        (Option.map ~f:CommonControlArnFilterList.of_xml)
          (Xml.child xml_arg0 "CommonControlArns") in
      let controlArns =
        (Option.map ~f:ControlArnFilterList.of_xml)
          (Xml.child xml_arg0 "ControlArns") in
      make ?mappingTypes ?commonControlArns ?controlArns ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let mappingTypes =
        field_map json__ "MappingTypes" MappingTypeFilterList.of_json in
      let commonControlArns =
        field_map json__ "CommonControlArns"
          CommonControlArnFilterList.of_json in
      let controlArns =
        field_map json__ "ControlArns" ControlArnFilterList.of_json in
      make ?mappingTypes ?commonControlArns ?controlArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that defines filtering criteria for the ListControlMappings operation. You can use this filter to narrow down the list of control mappings based on control ARNs, common control ARNs, or mapping types."]
module MaxListControlMappingsResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxListControlMappingsResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CommonControlSummaryList =
  struct
    type nonrec t = CommonControlSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CommonControlSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CommonControlSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"CommonControlSummaryList"
        ~of_json:CommonControlSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module CommonControlFilter =
  struct
    type nonrec t =
      {
      objectives: ObjectiveResourceFilterList.t option
        [@ocaml.doc
          "The objective that's used as filter criteria. You can use this parameter to specify one objective ARN at a time. Passing multiple ARNs in the CommonControlFilter isn\226\128\153t supported."]}
    let make ?objectives = fun () -> { objectives }
    let to_value x =
      structure_to_value
        [("Objectives",
           (Option.map x.objectives ~f:ObjectiveResourceFilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let objectives =
        (Option.map ~f:ObjectiveResourceFilterList.of_xml)
          (Xml.child xml_arg0 "Objectives") in
      make ?objectives ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let objectives =
        field_map json__ "Objectives" ObjectiveResourceFilterList.of_json in
      make ?objectives ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An optional filter that narrows the results to a specific objective."]
module MaxListCommonControlsResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxListCommonControlsResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ControlParameters =
  struct
    type nonrec t = ControlParameter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ControlParameter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ControlParameter.of_xml)
    let of_json j =
      list_of_json ~kind:"ControlParameters"
        ~of_json:ControlParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ImplementationDetails =
  struct
    type nonrec t =
      {
      type_: ImplementationType.t option
        [@ocaml.doc
          "A string that describes a control's implementation type."];
      identifier: ImplementationIdentifier.t option
        [@ocaml.doc
          "A service-specific identifier for the control, assigned by the service that implemented the control. For example, this identifier could be an Amazon Web Services Config Rule ID or a Security Hub Control ID."]}
    let make ?type_ = fun ?identifier -> fun () -> { type_; identifier }
    let to_value x =
      structure_to_value
        [("Type", (Option.map x.type_ ~f:ImplementationType.to_value));
        ("Identifier",
          (Option.map x.identifier ~f:ImplementationIdentifier.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identifier =
        (Option.map ~f:ImplementationIdentifier.of_xml)
          (Xml.child xml_arg0 "Identifier") in
      let type_ =
        (Option.map ~f:ImplementationType.of_xml) (Xml.child xml_arg0 "Type") in
      make ?identifier ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let identifier =
        field_map json__ "Identifier" ImplementationIdentifier.of_json in
      let type_ = field_map json__ "Type" ImplementationType.of_json in
      make ?identifier ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object that describes the implementation type for a control. Our ImplementationDetails Type format has three required segments: SERVICE-PROVIDER::SERVICE-NAME::RESOURCE-NAME For example, AWS::Config::ConfigRule or AWS::SecurityHub::SecurityControl resources have the format with three required segments. Our ImplementationDetails Type format has an optional fourth segment, which is present for applicable implementation types. The format is as follows: SERVICE-PROVIDER::SERVICE-NAME::RESOURCE-NAME::RESOURCE-TYPE-DESCRIPTION For example, AWS::Organizations::Policy::SERVICE_CONTROL_POLICY or AWS::CloudFormation::Type::HOOK have the format with four segments. Although the format is similar, the values for the Type field do not match any Amazon Web Services CloudFormation values."]
module RegionConfiguration =
  struct
    type nonrec t =
      {
      scope: ControlScope.t option
        [@ocaml.doc
          "The coverage of the control, if deployed. Scope is an enumerated type, with value Regional, or Global. A control with Global scope is effective in all Amazon Web Services Regions, regardless of the Region from which it is enabled, or to which it is deployed. A control implemented by an SCP is usually Global in scope. A control with Regional scope has operations that are restricted specifically to the Region from which it is enabled and to which it is deployed. Controls implemented by Config rules and CloudFormation hooks usually are Regional in scope. Security Hub controls usually are Regional in scope."];
      deployableRegions: DeployableRegions.t option
        [@ocaml.doc
          "Regions in which the control is available to be deployed."]}
    let make ?scope =
      fun ?deployableRegions -> fun () -> { scope; deployableRegions }
    let to_value x =
      structure_to_value
        [("Scope", (Option.map x.scope ~f:ControlScope.to_value));
        ("DeployableRegions",
          (Option.map x.deployableRegions ~f:DeployableRegions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deployableRegions =
        (Option.map ~f:DeployableRegions.of_xml)
          (Xml.child xml_arg0 "DeployableRegions") in
      let scope =
        (Option.map ~f:ControlScope.of_xml) (Xml.child xml_arg0 "Scope") in
      make ?deployableRegions ?scope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deployableRegions =
        field_map json__ "DeployableRegions" DeployableRegions.of_json in
      let scope = field_map json__ "Scope" ControlScope.of_json in
      make ?deployableRegions ?scope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about the control, including the scope of the control, if enabled, and the Regions in which the control is available for deployment. For more information about scope, see Global services. If you are applying controls through an Amazon Web Services Control Tower landing zone environment, remember that the values returned in the RegionConfiguration API operation are not related to the governed Regions in your landing zone. For example, if you are governing Regions A,B,and C while the control is available in Regions A, B, C, and D, you'd see a response with DeployableRegions of A, B, C, and D for a control with REGIONAL scope, even though you may not intend to deploy the control in Region D, because you do not govern it through your landing zone."]
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The requested resource does not exist."]
module ListObjectivesResponse =
  struct
    type nonrec t =
      {
      objectives: ObjectiveSummaryList.t option
        [@ocaml.doc
          "The list of objectives that the ListObjectives API returns."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The pagination token that's used to fetch the next set of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?objectives =
      fun ?nextToken -> fun () -> { objectives; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Objectives",
           (Option.map x.objectives ~f:ObjectiveSummaryList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let objectives =
        (Option.map ~f:ObjectiveSummaryList.of_xml)
          (Xml.child xml_arg0 "Objectives") in
      make ?nextToken ?objectives ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" PaginationToken.of_json in
      let objectives =
        field_map json__ "Objectives" ObjectiveSummaryList.of_json in
      make ?nextToken ?objectives ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a paginated list of objectives from the Control Catalog. You can apply an optional filter to see the objectives that belong to a specific domain. If you don\226\128\153t provide a filter, the operation returns all objectives."]
module ListObjectivesRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxListObjectivesResults.t option
        [@ocaml.doc
          "The maximum number of results on a page or for an API request call."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The pagination token that's used to fetch the next set of results."];
      objectiveFilter: ObjectiveFilter.t option
        [@ocaml.doc
          "An optional filter that narrows the results to a specific domain. This filter allows you to specify one domain ARN at a time. Passing multiple ARNs in the ObjectiveFilter isn\226\128\153t supported."]}
    let make ?maxResults =
      fun ?nextToken ->
        fun ?objectiveFilter ->
          fun () -> { maxResults; nextToken; objectiveFilter }
    let to_value x =
      structure_to_value
        [("maxResults",
           (Option.map x.maxResults ~f:MaxListObjectivesResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("ObjectiveFilter",
          (Option.map x.objectiveFilter ~f:ObjectiveFilter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let objectiveFilter =
        (Option.map ~f:ObjectiveFilter.of_xml)
          (Xml.child xml_arg0 "ObjectiveFilter") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxListObjectivesResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      make ?objectiveFilter ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let objectiveFilter =
        field_map json__ "ObjectiveFilter" ObjectiveFilter.of_json in
      let nextToken = field_map json__ "NextToken" PaginationToken.of_json in
      let maxResults =
        field_map json__ "MaxResults" MaxListObjectivesResults.of_json in
      make ?objectiveFilter ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a paginated list of objectives from the Control Catalog. You can apply an optional filter to see the objectives that belong to a specific domain. If you don\226\128\153t provide a filter, the operation returns all objectives."]
module ListDomainsResponse =
  struct
    type nonrec t =
      {
      domains: DomainSummaryList.t option
        [@ocaml.doc "The list of domains that the ListDomains API returns."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The pagination token that's used to fetch the next set of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?domains = fun ?nextToken -> fun () -> { domains; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Domains", (Option.map x.domains ~f:DomainSummaryList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let domains =
        (Option.map ~f:DomainSummaryList.of_xml)
          (Xml.child xml_arg0 "Domains") in
      make ?nextToken ?domains ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" PaginationToken.of_json in
      let domains = field_map json__ "Domains" DomainSummaryList.of_json in
      make ?nextToken ?domains ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a paginated list of domains from the Control Catalog."]
module ListDomainsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxListDomainsResults.t option
        [@ocaml.doc
          "The maximum number of results on a page or for an API request call."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The pagination token that's used to fetch the next set of results."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults",
           (Option.map x.maxResults ~f:MaxListDomainsResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxListDomainsResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" PaginationToken.of_json in
      let maxResults =
        field_map json__ "MaxResults" MaxListDomainsResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a paginated list of domains from the Control Catalog."]
module ListControlsResponse =
  struct
    type nonrec t =
      {
      controls: Controls.t option
        [@ocaml.doc
          "Returns a list of controls, given as structures of type controlSummary."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The pagination token that's used to fetch the next set of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?controls = fun ?nextToken -> fun () -> { controls; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Controls", (Option.map x.controls ~f:Controls.to_value));
        ("NextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let controls =
        (Option.map ~f:Controls.of_xml) (Xml.child xml_arg0 "Controls") in
      make ?nextToken ?controls ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" PaginationToken.of_json in
      let controls = field_map json__ "Controls" Controls.of_json in
      make ?nextToken ?controls ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a paginated list of all available controls in the Control Catalog library. Allows you to discover available controls. The list of controls is given as structures of type controlSummary. The ARN is returned in the global controlcatalog format, as shown in the examples."]
module ListControlsRequest =
  struct
    type nonrec t =
      {
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The pagination token that's used to fetch the next set of results."];
      maxResults: MaxListControlsResults.t option
        [@ocaml.doc
          "The maximum number of results on a page or for an API request call."];
      filter: ControlFilter.t option
        [@ocaml.doc
          "An optional filter that narrows the results to controls with specific implementation types or identifiers. If you don't provide a filter, the operation returns all available controls."]}
    let make ?nextToken =
      fun ?maxResults ->
        fun ?filter -> fun () -> { nextToken; maxResults; filter }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("maxResults",
          (Option.map x.maxResults ~f:MaxListControlsResults.to_value));
        ("Filter", (Option.map x.filter ~f:ControlFilter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filter =
        (Option.map ~f:ControlFilter.of_xml) (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:MaxListControlsResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ?filter ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filter = field_map json__ "Filter" ControlFilter.of_json in
      let maxResults =
        field_map json__ "MaxResults" MaxListControlsResults.of_json in
      let nextToken = field_map json__ "NextToken" PaginationToken.of_json in
      make ?filter ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a paginated list of all available controls in the Control Catalog library. Allows you to discover available controls. The list of controls is given as structures of type controlSummary. The ARN is returned in the global controlcatalog format, as shown in the examples."]
module ListControlMappingsResponse =
  struct
    type nonrec t =
      {
      controlMappings: ControlMappings.t option
        [@ocaml.doc
          "The list of control mappings that the ListControlMappings API returns."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The pagination token that's used to fetch the next set of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?controlMappings =
      fun ?nextToken -> fun () -> { controlMappings; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ControlMappings",
           (Option.map x.controlMappings ~f:ControlMappings.to_value));
        ("NextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let controlMappings =
        (Option.map ~f:ControlMappings.of_xml)
          (Xml.child xml_arg0 "ControlMappings") in
      make ?nextToken ?controlMappings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" PaginationToken.of_json in
      let controlMappings =
        field_map json__ "ControlMappings" ControlMappings.of_json in
      make ?nextToken ?controlMappings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a paginated list of control mappings from the Control Catalog. Control mappings show relationships between controls and other entities, such as common controls or compliance frameworks."]
module ListControlMappingsRequest =
  struct
    type nonrec t =
      {
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The pagination token that's used to fetch the next set of results."];
      maxResults: MaxListControlMappingsResults.t option
        [@ocaml.doc
          "The maximum number of results on a page or for an API request call."];
      filter: ControlMappingFilter.t option
        [@ocaml.doc
          "An optional filter that narrows the results to specific control mappings based on control ARNs, common control ARNs, or mapping types."]}
    let make ?nextToken =
      fun ?maxResults ->
        fun ?filter -> fun () -> { nextToken; maxResults; filter }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("maxResults",
          (Option.map x.maxResults ~f:MaxListControlMappingsResults.to_value));
        ("Filter", (Option.map x.filter ~f:ControlMappingFilter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filter =
        (Option.map ~f:ControlMappingFilter.of_xml)
          (Xml.child xml_arg0 "Filter") in
      let maxResults =
        (Option.map ~f:MaxListControlMappingsResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ?filter ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filter = field_map json__ "Filter" ControlMappingFilter.of_json in
      let maxResults =
        field_map json__ "MaxResults" MaxListControlMappingsResults.of_json in
      let nextToken = field_map json__ "NextToken" PaginationToken.of_json in
      make ?filter ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a paginated list of control mappings from the Control Catalog. Control mappings show relationships between controls and other entities, such as common controls or compliance frameworks."]
module ListCommonControlsResponse =
  struct
    type nonrec t =
      {
      commonControls: CommonControlSummaryList.t option
        [@ocaml.doc
          "The list of common controls that the ListCommonControls API returns."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The pagination token that's used to fetch the next set of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?commonControls =
      fun ?nextToken -> fun () -> { commonControls; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("CommonControls",
           (Option.map x.commonControls ~f:CommonControlSummaryList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let commonControls =
        (Option.map ~f:CommonControlSummaryList.of_xml)
          (Xml.child xml_arg0 "CommonControls") in
      make ?nextToken ?commonControls ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" PaginationToken.of_json in
      let commonControls =
        field_map json__ "CommonControls" CommonControlSummaryList.of_json in
      make ?nextToken ?commonControls ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a paginated list of common controls from the Amazon Web Services Control Catalog. You can apply an optional filter to see common controls that have a specific objective. If you don\226\128\153t provide a filter, the operation returns all common controls."]
module ListCommonControlsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxListCommonControlsResults.t option
        [@ocaml.doc
          "The maximum number of results on a page or for an API request call."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The pagination token that's used to fetch the next set of results."];
      commonControlFilter: CommonControlFilter.t option
        [@ocaml.doc
          "An optional filter that narrows the results to a specific objective. This filter allows you to specify one objective ARN at a time. Passing multiple ARNs in the CommonControlFilter isn\226\128\153t supported."]}
    let make ?maxResults =
      fun ?nextToken ->
        fun ?commonControlFilter ->
          fun () -> { maxResults; nextToken; commonControlFilter }
    let to_value x =
      structure_to_value
        [("maxResults",
           (Option.map x.maxResults ~f:MaxListCommonControlsResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("CommonControlFilter",
          (Option.map x.commonControlFilter ~f:CommonControlFilter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let commonControlFilter =
        (Option.map ~f:CommonControlFilter.of_xml)
          (Xml.child xml_arg0 "CommonControlFilter") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxListCommonControlsResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      make ?commonControlFilter ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let commonControlFilter =
        field_map json__ "CommonControlFilter" CommonControlFilter.of_json in
      let nextToken = field_map json__ "NextToken" PaginationToken.of_json in
      let maxResults =
        field_map json__ "MaxResults" MaxListCommonControlsResults.of_json in
      make ?commonControlFilter ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a paginated list of common controls from the Amazon Web Services Control Catalog. You can apply an optional filter to see common controls that have a specific objective. If you don\226\128\153t provide a filter, the operation returns all common controls."]
module GetControlResponse =
  struct
    type nonrec t =
      {
      arn: ControlArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the control."];
      aliases: ControlAliases.t option
        [@ocaml.doc
          "A list of alternative identifiers for the control. These are human-readable designators, such as SH.S3.1. Several aliases can refer to the same control across different Amazon Web Services services or compliance frameworks."];
      name: String_.t option [@ocaml.doc "The display name of the control."];
      description: String_.t option
        [@ocaml.doc "A description of what the control does."];
      behavior: ControlBehavior.t option
        [@ocaml.doc
          "A term that identifies the control's functional behavior. One of Preventive, Detective, Proactive"];
      severity: ControlSeverity.t option
        [@ocaml.doc
          "An enumerated type, with the following possible values:"];
      regionConfiguration: RegionConfiguration.t option ;
      implementation: ImplementationDetails.t option
        [@ocaml.doc
          "Returns information about the control, as an ImplementationDetails object that shows the underlying implementation type for a control."];
      parameters: ControlParameters.t option
        [@ocaml.doc
          "Returns an array of ControlParameter objects that specify the parameters a control supports. An empty list is returned for controls that don\226\128\153t support parameters."];
      createTime: Timestamp.t option
        [@ocaml.doc
          "A timestamp that notes the time when the control was released (start of its life) as a governance capability in Amazon Web Services."];
      governedResources: GovernedResources.t option
        [@ocaml.doc
          "A list of Amazon Web Services resource types that are governed by this control. This information helps you understand which controls can govern certain types of resources, and conversely, which resources are affected when the control is implemented. The resources are represented as Amazon Web Services CloudFormation resource types. If GovernedResources cannot be represented by available CloudFormation resource types, it\226\128\153s returned as an empty list."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn =
      fun ?aliases ->
        fun ?name ->
          fun ?description ->
            fun ?behavior ->
              fun ?severity ->
                fun ?regionConfiguration ->
                  fun ?implementation ->
                    fun ?parameters ->
                      fun ?createTime ->
                        fun ?governedResources ->
                          fun () ->
                            {
                              arn;
                              aliases;
                              name;
                              description;
                              behavior;
                              severity;
                              regionConfiguration;
                              implementation;
                              parameters;
                              createTime;
                              governedResources
                            }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ControlArn.to_value));
        ("Aliases", (Option.map x.aliases ~f:ControlAliases.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("Behavior", (Option.map x.behavior ~f:ControlBehavior.to_value));
        ("Severity", (Option.map x.severity ~f:ControlSeverity.to_value));
        ("RegionConfiguration",
          (Option.map x.regionConfiguration ~f:RegionConfiguration.to_value));
        ("Implementation",
          (Option.map x.implementation ~f:ImplementationDetails.to_value));
        ("Parameters",
          (Option.map x.parameters ~f:ControlParameters.to_value));
        ("CreateTime", (Option.map x.createTime ~f:Timestamp.to_value));
        ("GovernedResources",
          (Option.map x.governedResources ~f:GovernedResources.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let governedResources =
        (Option.map ~f:GovernedResources.of_xml)
          (Xml.child xml_arg0 "GovernedResources") in
      let createTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreateTime") in
      let parameters =
        (Option.map ~f:ControlParameters.of_xml)
          (Xml.child xml_arg0 "Parameters") in
      let implementation =
        (Option.map ~f:ImplementationDetails.of_xml)
          (Xml.child xml_arg0 "Implementation") in
      let regionConfiguration =
        (Option.map ~f:RegionConfiguration.of_xml)
          (Xml.child xml_arg0 "RegionConfiguration") in
      let severity =
        (Option.map ~f:ControlSeverity.of_xml)
          (Xml.child xml_arg0 "Severity") in
      let behavior =
        (Option.map ~f:ControlBehavior.of_xml)
          (Xml.child xml_arg0 "Behavior") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let aliases =
        (Option.map ~f:ControlAliases.of_xml) (Xml.child xml_arg0 "Aliases") in
      let arn = (Option.map ~f:ControlArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?governedResources ?createTime ?parameters ?implementation
        ?regionConfiguration ?severity ?behavior ?description ?name ?aliases
        ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let governedResources =
        field_map json__ "GovernedResources" GovernedResources.of_json in
      let createTime = field_map json__ "CreateTime" Timestamp.of_json in
      let parameters =
        field_map json__ "Parameters" ControlParameters.of_json in
      let implementation =
        field_map json__ "Implementation" ImplementationDetails.of_json in
      let regionConfiguration =
        field_map json__ "RegionConfiguration" RegionConfiguration.of_json in
      let severity = field_map json__ "Severity" ControlSeverity.of_json in
      let behavior = field_map json__ "Behavior" ControlBehavior.of_json in
      let description = field_map json__ "Description" String_.of_json in
      let name = field_map json__ "Name" String_.of_json in
      let aliases = field_map json__ "Aliases" ControlAliases.of_json in
      let arn = field_map json__ "Arn" ControlArn.of_json in
      make ?governedResources ?createTime ?parameters ?implementation
        ?regionConfiguration ?severity ?behavior ?description ?name ?aliases
        ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns details about a specific control, most notably a list of Amazon Web Services Regions where this control is supported. Input a value for the ControlArn parameter, in ARN form. GetControl accepts controltower or controlcatalog control ARNs as input. Returns a controlcatalog ARN format. In the API response, controls that have the value GLOBAL in the Scope field do not show the DeployableRegions field, because it does not apply. Controls that have the value REGIONAL in the Scope field return a value for the DeployableRegions field, as shown in the example."]
module GetControlRequest =
  struct
    type nonrec t =
      {
      controlArn: ControlArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the control. It has one of the following formats: Global format arn:\\{PARTITION\\}:controlcatalog:::control/\\{CONTROL_CATALOG_OPAQUE_ID\\} Or Regional format arn:\\{PARTITION\\}:controltower:\\{REGION\\}::control/\\{CONTROL_TOWER_OPAQUE_ID\\} Here is a more general pattern that covers Amazon Web Services Control Tower and Control Catalog ARNs: ^arn:(aws(?:\\[-a-z\\]*)?):(controlcatalog|controltower):\\[a-zA-Z0-9-\\]*::control/\\[0-9a-zA-Z_\\\\-\\]+$"]}
    let context_ = "GetControlRequest"
    let make ~controlArn = fun () -> { controlArn }
    let to_value x =
      structure_to_value
        [("ControlArn", (Some (ControlArn.to_value x.controlArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let controlArn =
        ControlArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ControlArn") in
      make ~controlArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let controlArn = field_map_exn json__ "ControlArn" ControlArn.of_json in
      make ~controlArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns details about a specific control, most notably a list of Amazon Web Services Regions where this control is supported. Input a value for the ControlArn parameter, in ARN form. GetControl accepts controltower or controlcatalog control ARNs as input. Returns a controlcatalog ARN format. In the API response, controls that have the value GLOBAL in the Scope field do not show the DeployableRegions field, because it does not apply. Controls that have the value REGIONAL in the Scope field return a value for the DeployableRegions field, as shown in the example."]
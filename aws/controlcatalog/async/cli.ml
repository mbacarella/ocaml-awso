(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let get_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and controlArn =
         flag "control-arn" (required string) ~doc:"STRING ControlArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_control (Values.GetControlRequest.make ~controlArn ())
           (Some Values.GetControlResponse.to_json)
           (Some Values.GetControlResponse.error_to_json)])
let list_common_controls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxListCommonControlsResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and commonControlFilter =
         flag "common-control-filter" (optional json_arg)
           ~doc:"JSON CommonControlFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_common_controls
           (Values.ListCommonControlsRequest.make ?maxResults ?nextToken
              ?commonControlFilter:(Option.map
                                      ~f:Values.CommonControlFilter.of_json
                                      commonControlFilter) ())
           (Some Values.ListCommonControlsResponse.to_json)
           (Some Values.ListCommonControlsResponse.error_to_json)])
let list_control_mappings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxListControlMappingsResults"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ControlMappingFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_control_mappings
           (Values.ListControlMappingsRequest.make ?nextToken ?maxResults
              ?filter:(Option.map ~f:Values.ControlMappingFilter.of_json
                         filter) ())
           (Some Values.ListControlMappingsResponse.to_json)
           (Some Values.ListControlMappingsResponse.error_to_json)])
let list_controls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxListControlsResults"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ControlFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_controls
           (Values.ListControlsRequest.make ?nextToken ?maxResults
              ?filter:(Option.map ~f:Values.ControlFilter.of_json filter) ())
           (Some Values.ListControlsResponse.to_json)
           (Some Values.ListControlsResponse.error_to_json)])
let list_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxListDomainsResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domains
           (Values.ListDomainsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListDomainsResponse.to_json)
           (Some Values.ListDomainsResponse.error_to_json)])
let list_objectives =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxListObjectivesResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and objectiveFilter =
         flag "objective-filter" (optional json_arg)
           ~doc:"JSON ObjectiveFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_objectives
           (Values.ListObjectivesRequest.make ?maxResults ?nextToken
              ?objectiveFilter:(Option.map ~f:Values.ObjectiveFilter.of_json
                                  objectiveFilter) ())
           (Some Values.ListObjectivesResponse.to_json)
           (Some Values.ListObjectivesResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("get-control", get_control);
    ("list-common-controls", list_common_controls);
    ("list-control-mappings", list_control_mappings);
    ("list-controls", list_controls);
    ("list-domains", list_domains);
    ("list-objectives", list_objectives)]
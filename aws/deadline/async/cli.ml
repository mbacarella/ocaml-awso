(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_member_to_farm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and principalType =
         flag "principal-type" (required json_arg) ~doc:"JSON PrincipalType"
       and identityStoreId =
         flag "identity-store-id" (required string)
           ~doc:"STRING IdentityStoreId"
       and membershipLevel =
         flag "membership-level" (required json_arg)
           ~doc:"JSON MembershipLevel"
       and principalId =
         flag "principal-id" (required string)
           ~doc:"STRING IdentityCenterPrincipalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_member_to_farm
           (Values.AssociateMemberToFarmRequest.make ~farmId
              ~principalType:(Values.PrincipalType.of_json principalType)
              ~identityStoreId
              ~membershipLevel:(Values.MembershipLevel.of_json
                                  membershipLevel) ~principalId ())
           (Some Values.AssociateMemberToFarmResponse.to_json)
           (Some Values.AssociateMemberToFarmResponse.error_to_json)])
let associate_member_to_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and principalType =
         flag "principal-type" (required json_arg) ~doc:"JSON PrincipalType"
       and identityStoreId =
         flag "identity-store-id" (required string)
           ~doc:"STRING IdentityStoreId"
       and membershipLevel =
         flag "membership-level" (required json_arg)
           ~doc:"JSON MembershipLevel"
       and principalId =
         flag "principal-id" (required string)
           ~doc:"STRING IdentityCenterPrincipalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_member_to_fleet
           (Values.AssociateMemberToFleetRequest.make ~farmId ~fleetId
              ~principalType:(Values.PrincipalType.of_json principalType)
              ~identityStoreId
              ~membershipLevel:(Values.MembershipLevel.of_json
                                  membershipLevel) ~principalId ())
           (Some Values.AssociateMemberToFleetResponse.to_json)
           (Some Values.AssociateMemberToFleetResponse.error_to_json)])
let associate_member_to_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and principalType =
         flag "principal-type" (required json_arg) ~doc:"JSON PrincipalType"
       and identityStoreId =
         flag "identity-store-id" (required string)
           ~doc:"STRING IdentityStoreId"
       and membershipLevel =
         flag "membership-level" (required json_arg)
           ~doc:"JSON MembershipLevel"
       and principalId =
         flag "principal-id" (required string)
           ~doc:"STRING IdentityCenterPrincipalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_member_to_job
           (Values.AssociateMemberToJobRequest.make ~farmId ~queueId ~jobId
              ~principalType:(Values.PrincipalType.of_json principalType)
              ~identityStoreId
              ~membershipLevel:(Values.MembershipLevel.of_json
                                  membershipLevel) ~principalId ())
           (Some Values.AssociateMemberToJobResponse.to_json)
           (Some Values.AssociateMemberToJobResponse.error_to_json)])
let associate_member_to_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and principalType =
         flag "principal-type" (required json_arg) ~doc:"JSON PrincipalType"
       and identityStoreId =
         flag "identity-store-id" (required string)
           ~doc:"STRING IdentityStoreId"
       and membershipLevel =
         flag "membership-level" (required json_arg)
           ~doc:"JSON MembershipLevel"
       and principalId =
         flag "principal-id" (required string)
           ~doc:"STRING IdentityCenterPrincipalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_member_to_queue
           (Values.AssociateMemberToQueueRequest.make ~farmId ~queueId
              ~principalType:(Values.PrincipalType.of_json principalType)
              ~identityStoreId
              ~membershipLevel:(Values.MembershipLevel.of_json
                                  membershipLevel) ~principalId ())
           (Some Values.AssociateMemberToQueueResponse.to_json)
           (Some Values.AssociateMemberToQueueResponse.error_to_json)])
let assume_fleet_role_for_read =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assume_fleet_role_for_read
           (Values.AssumeFleetRoleForReadRequest.make ~farmId ~fleetId ())
           (Some Values.AssumeFleetRoleForReadResponse.to_json)
           (Some Values.AssumeFleetRoleForReadResponse.error_to_json)])
let assume_fleet_role_for_worker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING WorkerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assume_fleet_role_for_worker
           (Values.AssumeFleetRoleForWorkerRequest.make ~farmId ~fleetId
              ~workerId ())
           (Some Values.AssumeFleetRoleForWorkerResponse.to_json)
           (Some Values.AssumeFleetRoleForWorkerResponse.error_to_json)])
let assume_queue_role_for_read =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assume_queue_role_for_read
           (Values.AssumeQueueRoleForReadRequest.make ~farmId ~queueId ())
           (Some Values.AssumeQueueRoleForReadResponse.to_json)
           (Some Values.AssumeQueueRoleForReadResponse.error_to_json)])
let assume_queue_role_for_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assume_queue_role_for_user
           (Values.AssumeQueueRoleForUserRequest.make ~farmId ~queueId ())
           (Some Values.AssumeQueueRoleForUserResponse.to_json)
           (Some Values.AssumeQueueRoleForUserResponse.error_to_json)])
let assume_queue_role_for_worker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING WorkerId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assume_queue_role_for_worker
           (Values.AssumeQueueRoleForWorkerRequest.make ~farmId ~fleetId
              ~workerId ~queueId ())
           (Some Values.AssumeQueueRoleForWorkerResponse.to_json)
           (Some Values.AssumeQueueRoleForWorkerResponse.error_to_json)])
let batch_get_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifiers =
         flag "identifiers" (required json_arg)
           ~doc:"JSON BatchGetJobIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_job
           (Values.BatchGetJobRequest.make
              ~identifiers:(Values.BatchGetJobIdentifiers.of_json identifiers)
              ()) (Some Values.BatchGetJobResponse.to_json)
           (Some Values.BatchGetJobResponse.error_to_json)])
let batch_get_job_entity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING WorkerId"
       and identifiers =
         flag "identifiers" (required json_arg)
           ~doc:"JSON JobEntityIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_job_entity
           (Values.BatchGetJobEntityRequest.make ~farmId ~fleetId ~workerId
              ~identifiers:(Values.JobEntityIdentifiers.of_json identifiers)
              ()) (Some Values.BatchGetJobEntityResponse.to_json)
           (Some Values.BatchGetJobEntityResponse.error_to_json)])
let batch_get_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifiers =
         flag "identifiers" (required json_arg)
           ~doc:"JSON BatchGetSessionIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_session
           (Values.BatchGetSessionRequest.make
              ~identifiers:(Values.BatchGetSessionIdentifiers.of_json
                              identifiers) ())
           (Some Values.BatchGetSessionResponse.to_json)
           (Some Values.BatchGetSessionResponse.error_to_json)])
let batch_get_session_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifiers =
         flag "identifiers" (required json_arg)
           ~doc:"JSON BatchGetSessionActionIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_session_action
           (Values.BatchGetSessionActionRequest.make
              ~identifiers:(Values.BatchGetSessionActionIdentifiers.of_json
                              identifiers) ())
           (Some Values.BatchGetSessionActionResponse.to_json)
           (Some Values.BatchGetSessionActionResponse.error_to_json)])
let batch_get_step =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifiers =
         flag "identifiers" (required json_arg)
           ~doc:"JSON BatchGetStepIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_step
           (Values.BatchGetStepRequest.make
              ~identifiers:(Values.BatchGetStepIdentifiers.of_json
                              identifiers) ())
           (Some Values.BatchGetStepResponse.to_json)
           (Some Values.BatchGetStepResponse.error_to_json)])
let batch_get_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifiers =
         flag "identifiers" (required json_arg)
           ~doc:"JSON BatchGetTaskIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_task
           (Values.BatchGetTaskRequest.make
              ~identifiers:(Values.BatchGetTaskIdentifiers.of_json
                              identifiers) ())
           (Some Values.BatchGetTaskResponse.to_json)
           (Some Values.BatchGetTaskResponse.error_to_json)])
let batch_get_worker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifiers =
         flag "identifiers" (required json_arg)
           ~doc:"JSON BatchGetWorkerIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_worker
           (Values.BatchGetWorkerRequest.make
              ~identifiers:(Values.BatchGetWorkerIdentifiers.of_json
                              identifiers) ())
           (Some Values.BatchGetWorkerResponse.to_json)
           (Some Values.BatchGetWorkerResponse.error_to_json)])
let batch_update_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and jobs =
         flag "jobs" (required json_arg) ~doc:"JSON BatchUpdateJobItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_job
           (Values.BatchUpdateJobRequest.make ?clientToken
              ~jobs:(Values.BatchUpdateJobItems.of_json jobs) ())
           (Some Values.BatchUpdateJobResponse.to_json)
           (Some Values.BatchUpdateJobResponse.error_to_json)])
let batch_update_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tasks =
         flag "tasks" (required json_arg) ~doc:"JSON BatchUpdateTaskItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_task
           (Values.BatchUpdateTaskRequest.make ?clientToken
              ~tasks:(Values.BatchUpdateTaskItems.of_json tasks) ())
           (Some Values.BatchUpdateTaskResponse.to_json)
           (Some Values.BatchUpdateTaskResponse.error_to_json)])
let copy_job_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and targetS3Location =
         flag "target-s3-location" (required json_arg) ~doc:"JSON S3Location" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_job_template
           (Values.CopyJobTemplateRequest.make ~farmId ~queueId ~jobId
              ~targetS3Location:(Values.S3Location.of_json targetS3Location)
              ()) (Some Values.CopyJobTemplateResponse.to_json)
           (Some Values.CopyJobTemplateResponse.error_to_json)])
let create_budget =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING ResourceName"
       and usageTrackingResource =
         flag "usage-tracking-resource" (required json_arg)
           ~doc:"JSON UsageTrackingResource"
       and approximateDollarLimit =
         flag "approximate-dollar-limit" (required float)
           ~doc:"FLOAT ConsumedUsageLimit"
       and actions =
         flag "actions" (required json_arg) ~doc:"JSON BudgetActionsToAdd"
       and schedule =
         flag "schedule" (required json_arg) ~doc:"JSON BudgetSchedule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_budget
           (Values.CreateBudgetRequest.make ?description ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~farmId
              ~displayName
              ~usageTrackingResource:(Values.UsageTrackingResource.of_json
                                        usageTrackingResource)
              ~approximateDollarLimit
              ~actions:(Values.BudgetActionsToAdd.of_json actions)
              ~schedule:(Values.BudgetSchedule.of_json schedule) ())
           (Some Values.CreateBudgetResponse.to_json)
           (Some Values.CreateBudgetResponse.error_to_json)])
let create_farm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and costScaleFactor =
         flag "cost-scale-factor" (optional float)
           ~doc:"FLOAT CostScaleFactor"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_farm
           (Values.CreateFarmRequest.make ?clientToken ?description
              ?kmsKeyArn ?costScaleFactor
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~displayName ())
           (Some Values.CreateFarmResponse.to_json)
           (Some Values.CreateFarmResponse.error_to_json)])
let create_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and minWorkerCount =
         flag "min-worker-count" (optional int) ~doc:"INT MinZeroMaxInteger"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and hostConfiguration =
         flag "host-configuration" (optional json_arg)
           ~doc:"JSON HostConfiguration"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING ResourceName"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING IamRoleArn"
       and maxWorkerCount =
         flag "max-worker-count" (required int) ~doc:"INT MinZeroMaxInteger"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON FleetConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_fleet
           (Values.CreateFleetRequest.make ?clientToken ?description
              ?minWorkerCount ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?hostConfiguration:(Option.map
                                    ~f:Values.HostConfiguration.of_json
                                    hostConfiguration) ~farmId ~displayName
              ~roleArn ~maxWorkerCount
              ~configuration:(Values.FleetConfiguration.of_json configuration)
              ()) (Some Values.CreateFleetResponse.to_json)
           (Some Values.CreateFleetResponse.error_to_json)])
let create_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and template =
         flag "template" (optional string) ~doc:"STRING JobTemplate"
       and templateType =
         flag "template-type" (optional json_arg) ~doc:"JSON JobTemplateType"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON JobParameters"
       and attachments =
         flag "attachments" (optional json_arg) ~doc:"JSON Attachments"
       and storageProfileId =
         flag "storage-profile-id" (optional string)
           ~doc:"STRING StorageProfileId"
       and targetTaskRunStatus =
         flag "target-task-run-status" (optional json_arg)
           ~doc:"JSON CreateJobTargetTaskRunStatus"
       and maxFailedTasksCount =
         flag "max-failed-tasks-count" (optional int)
           ~doc:"INT MaxFailedTasksCount"
       and maxRetriesPerTask =
         flag "max-retries-per-task" (optional int)
           ~doc:"INT MaxRetriesPerTask"
       and maxWorkerCount =
         flag "max-worker-count" (optional int) ~doc:"INT MaxWorkerCount"
       and sourceJobId =
         flag "source-job-id" (optional string) ~doc:"STRING JobId"
       and nameOverride =
         flag "name-override" (optional string) ~doc:"STRING JobName"
       and descriptionOverride =
         flag "description-override" (optional string)
           ~doc:"STRING JobDescriptionOverride"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and priority = flag "priority" (required int) ~doc:"INT JobPriority" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_job
           (Values.CreateJobRequest.make ?clientToken ?template
              ?templateType:(Option.map ~f:Values.JobTemplateType.of_json
                               templateType)
              ?parameters:(Option.map ~f:Values.JobParameters.of_json
                             parameters)
              ?attachments:(Option.map ~f:Values.Attachments.of_json
                              attachments) ?storageProfileId
              ?targetTaskRunStatus:(Option.map
                                      ~f:Values.CreateJobTargetTaskRunStatus.of_json
                                      targetTaskRunStatus)
              ?maxFailedTasksCount ?maxRetriesPerTask ?maxWorkerCount
              ?sourceJobId ?nameOverride ?descriptionOverride
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~farmId ~queueId
              ~priority ()) (Some Values.CreateJobResponse.to_json)
           (Some Values.CreateJobResponse.error_to_json)])
let create_license_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId"
       and subnetIds =
         flag "subnet-ids" (required json_arg)
           ~doc:"JSON CreateLicenseEndpointRequestSubnetIdsList"
       and securityGroupIds =
         flag "security-group-ids" (required json_arg)
           ~doc:"JSON CreateLicenseEndpointRequestSecurityGroupIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_license_endpoint
           (Values.CreateLicenseEndpointRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~vpcId
              ~subnetIds:(Values.CreateLicenseEndpointRequestSubnetIdsList.of_json
                            subnetIds)
              ~securityGroupIds:(Values.CreateLicenseEndpointRequestSecurityGroupIdsList.of_json
                                   securityGroupIds) ())
           (Some Values.CreateLicenseEndpointResponse.to_json)
           (Some Values.CreateLicenseEndpointResponse.error_to_json)])
let create_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING ResourceName"
       and amountRequirementName =
         flag "amount-requirement-name" (required string)
           ~doc:"STRING AmountRequirementName"
       and maxCount = flag "max-count" (required int) ~doc:"INT MaxCount" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_limit
           (Values.CreateLimitRequest.make ?clientToken ?description ~farmId
              ~displayName ~amountRequirementName ~maxCount ())
           (Some Values.CreateLimitResponse.to_json)
           (Some Values.CreateLimitResponse.error_to_json)])
let create_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and identityCenterRegion =
         flag "identity-center-region" (optional string) ~doc:"STRING Region"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING ResourceName"
       and identityCenterInstanceArn =
         flag "identity-center-instance-arn" (required string)
           ~doc:"STRING IdentityCenterInstanceArn"
       and subdomain =
         flag "subdomain" (required string) ~doc:"STRING Subdomain"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING IamRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_monitor
           (Values.CreateMonitorRequest.make ?clientToken
              ?identityCenterRegion
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~displayName
              ~identityCenterInstanceArn ~subdomain ~roleArn ())
           (Some Values.CreateMonitorResponse.to_json)
           (Some Values.CreateMonitorResponse.error_to_json)])
let create_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and defaultBudgetAction =
         flag "default-budget-action" (optional json_arg)
           ~doc:"JSON DefaultQueueBudgetAction"
       and jobAttachmentSettings =
         flag "job-attachment-settings" (optional json_arg)
           ~doc:"JSON JobAttachmentSettings"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and jobRunAsUser =
         flag "job-run-as-user" (optional json_arg) ~doc:"JSON JobRunAsUser"
       and requiredFileSystemLocationNames =
         flag "required-file-system-location-names" (optional json_arg)
           ~doc:"JSON RequiredFileSystemLocationNames"
       and allowedStorageProfileIds =
         flag "allowed-storage-profile-ids" (optional json_arg)
           ~doc:"JSON AllowedStorageProfileIds"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and schedulingConfiguration =
         flag "scheduling-configuration" (optional json_arg)
           ~doc:"JSON SchedulingConfiguration"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_queue
           (Values.CreateQueueRequest.make ?clientToken ?description
              ?defaultBudgetAction:(Option.map
                                      ~f:Values.DefaultQueueBudgetAction.of_json
                                      defaultBudgetAction)
              ?jobAttachmentSettings:(Option.map
                                        ~f:Values.JobAttachmentSettings.of_json
                                        jobAttachmentSettings) ?roleArn
              ?jobRunAsUser:(Option.map ~f:Values.JobRunAsUser.of_json
                               jobRunAsUser)
              ?requiredFileSystemLocationNames:(Option.map
                                                  ~f:Values.RequiredFileSystemLocationNames.of_json
                                                  requiredFileSystemLocationNames)
              ?allowedStorageProfileIds:(Option.map
                                           ~f:Values.AllowedStorageProfileIds.of_json
                                           allowedStorageProfileIds)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?schedulingConfiguration:(Option.map
                                          ~f:Values.SchedulingConfiguration.of_json
                                          schedulingConfiguration) ~farmId
              ~displayName ()) (Some Values.CreateQueueResponse.to_json)
           (Some Values.CreateQueueResponse.error_to_json)])
let create_queue_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and priority = flag "priority" (required int) ~doc:"INT Priority"
       and templateType =
         flag "template-type" (required json_arg)
           ~doc:"JSON EnvironmentTemplateType"
       and template =
         flag "template" (required string) ~doc:"STRING EnvironmentTemplate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_queue_environment
           (Values.CreateQueueEnvironmentRequest.make ?clientToken ~farmId
              ~queueId ~priority
              ~templateType:(Values.EnvironmentTemplateType.of_json
                               templateType) ~template ())
           (Some Values.CreateQueueEnvironmentResponse.to_json)
           (Some Values.CreateQueueEnvironmentResponse.error_to_json)])
let create_queue_fleet_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_queue_fleet_association
           (Values.CreateQueueFleetAssociationRequest.make ~farmId ~queueId
              ~fleetId ())
           (Some Values.CreateQueueFleetAssociationResponse.to_json)
           (Some Values.CreateQueueFleetAssociationResponse.error_to_json)])
let create_queue_limit_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and limitId = flag "limit-id" (required string) ~doc:"STRING LimitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_queue_limit_association
           (Values.CreateQueueLimitAssociationRequest.make ~farmId ~queueId
              ~limitId ())
           (Some Values.CreateQueueLimitAssociationResponse.to_json)
           (Some Values.CreateQueueLimitAssociationResponse.error_to_json)])
let create_storage_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and fileSystemLocations =
         flag "file-system-locations" (optional json_arg)
           ~doc:"JSON FileSystemLocationsList"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING ResourceName"
       and osFamily =
         flag "os-family" (required json_arg)
           ~doc:"JSON StorageProfileOperatingSystemFamily" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_storage_profile
           (Values.CreateStorageProfileRequest.make ?clientToken
              ?fileSystemLocations:(Option.map
                                      ~f:Values.FileSystemLocationsList.of_json
                                      fileSystemLocations) ~farmId
              ~displayName
              ~osFamily:(Values.StorageProfileOperatingSystemFamily.of_json
                           osFamily) ())
           (Some Values.CreateStorageProfileResponse.to_json)
           (Some Values.CreateStorageProfileResponse.error_to_json)])
let create_worker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostProperties =
         flag "host-properties" (optional json_arg)
           ~doc:"JSON HostPropertiesRequest"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_worker
           (Values.CreateWorkerRequest.make
              ?hostProperties:(Option.map
                                 ~f:Values.HostPropertiesRequest.of_json
                                 hostProperties) ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~farmId ~fleetId
              ()) (Some Values.CreateWorkerResponse.to_json)
           (Some Values.CreateWorkerResponse.error_to_json)])
let delete_budget =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and budgetId =
         flag "budget-id" (required string) ~doc:"STRING BudgetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_budget
           (Values.DeleteBudgetRequest.make ~farmId ~budgetId ())
           (Some Values.DeleteBudgetResponse.to_json)
           (Some Values.DeleteBudgetResponse.error_to_json)])
let delete_farm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_farm (Values.DeleteFarmRequest.make ~farmId ())
           (Some Values.DeleteFarmResponse.to_json)
           (Some Values.DeleteFarmResponse.error_to_json)])
let delete_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_fleet
           (Values.DeleteFleetRequest.make ?clientToken ~farmId ~fleetId ())
           (Some Values.DeleteFleetResponse.to_json)
           (Some Values.DeleteFleetResponse.error_to_json)])
let delete_license_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseEndpointId =
         flag "license-endpoint-id" (required string)
           ~doc:"STRING LicenseEndpointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_license_endpoint
           (Values.DeleteLicenseEndpointRequest.make ~licenseEndpointId ())
           (Some Values.DeleteLicenseEndpointResponse.to_json)
           (Some Values.DeleteLicenseEndpointResponse.error_to_json)])
let delete_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and limitId = flag "limit-id" (required string) ~doc:"STRING LimitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_limit
           (Values.DeleteLimitRequest.make ~farmId ~limitId ())
           (Some Values.DeleteLimitResponse.to_json)
           (Some Values.DeleteLimitResponse.error_to_json)])
let delete_metered_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseEndpointId =
         flag "license-endpoint-id" (required string)
           ~doc:"STRING LicenseEndpointId"
       and productId =
         flag "product-id" (required string) ~doc:"STRING MeteredProductId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_metered_product
           (Values.DeleteMeteredProductRequest.make ~licenseEndpointId
              ~productId ())
           (Some Values.DeleteMeteredProductResponse.to_json)
           (Some Values.DeleteMeteredProductResponse.error_to_json)])
let delete_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorId =
         flag "monitor-id" (required string) ~doc:"STRING MonitorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_monitor (Values.DeleteMonitorRequest.make ~monitorId ())
           (Some Values.DeleteMonitorResponse.to_json)
           (Some Values.DeleteMonitorResponse.error_to_json)])
let delete_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_queue
           (Values.DeleteQueueRequest.make ~farmId ~queueId ())
           (Some Values.DeleteQueueResponse.to_json)
           (Some Values.DeleteQueueResponse.error_to_json)])
let delete_queue_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and queueEnvironmentId =
         flag "queue-environment-id" (required string)
           ~doc:"STRING QueueEnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_queue_environment
           (Values.DeleteQueueEnvironmentRequest.make ~farmId ~queueId
              ~queueEnvironmentId ())
           (Some Values.DeleteQueueEnvironmentResponse.to_json)
           (Some Values.DeleteQueueEnvironmentResponse.error_to_json)])
let delete_queue_fleet_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_queue_fleet_association
           (Values.DeleteQueueFleetAssociationRequest.make ~farmId ~queueId
              ~fleetId ())
           (Some Values.DeleteQueueFleetAssociationResponse.to_json)
           (Some Values.DeleteQueueFleetAssociationResponse.error_to_json)])
let delete_queue_limit_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and limitId = flag "limit-id" (required string) ~doc:"STRING LimitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_queue_limit_association
           (Values.DeleteQueueLimitAssociationRequest.make ~farmId ~queueId
              ~limitId ())
           (Some Values.DeleteQueueLimitAssociationResponse.to_json)
           (Some Values.DeleteQueueLimitAssociationResponse.error_to_json)])
let delete_storage_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and storageProfileId =
         flag "storage-profile-id" (required string)
           ~doc:"STRING StorageProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_storage_profile
           (Values.DeleteStorageProfileRequest.make ~farmId ~storageProfileId
              ()) (Some Values.DeleteStorageProfileResponse.to_json)
           (Some Values.DeleteStorageProfileResponse.error_to_json)])
let delete_worker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING WorkerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_worker
           (Values.DeleteWorkerRequest.make ~farmId ~fleetId ~workerId ())
           (Some Values.DeleteWorkerResponse.to_json)
           (Some Values.DeleteWorkerResponse.error_to_json)])
let disassociate_member_from_farm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and principalId =
         flag "principal-id" (required string)
           ~doc:"STRING IdentityCenterPrincipalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_member_from_farm
           (Values.DisassociateMemberFromFarmRequest.make ~farmId
              ~principalId ())
           (Some Values.DisassociateMemberFromFarmResponse.to_json)
           (Some Values.DisassociateMemberFromFarmResponse.error_to_json)])
let disassociate_member_from_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and principalId =
         flag "principal-id" (required string)
           ~doc:"STRING IdentityCenterPrincipalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_member_from_fleet
           (Values.DisassociateMemberFromFleetRequest.make ~farmId ~fleetId
              ~principalId ())
           (Some Values.DisassociateMemberFromFleetResponse.to_json)
           (Some Values.DisassociateMemberFromFleetResponse.error_to_json)])
let disassociate_member_from_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and principalId =
         flag "principal-id" (required string)
           ~doc:"STRING IdentityCenterPrincipalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_member_from_job
           (Values.DisassociateMemberFromJobRequest.make ~farmId ~queueId
              ~jobId ~principalId ())
           (Some Values.DisassociateMemberFromJobResponse.to_json)
           (Some Values.DisassociateMemberFromJobResponse.error_to_json)])
let disassociate_member_from_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and principalId =
         flag "principal-id" (required string)
           ~doc:"STRING IdentityCenterPrincipalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_member_from_queue
           (Values.DisassociateMemberFromQueueRequest.make ~farmId ~queueId
              ~principalId ())
           (Some Values.DisassociateMemberFromQueueResponse.to_json)
           (Some Values.DisassociateMemberFromQueueResponse.error_to_json)])
let get_budget =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and budgetId =
         flag "budget-id" (required string) ~doc:"STRING BudgetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_budget (Values.GetBudgetRequest.make ~farmId ~budgetId ())
           (Some Values.GetBudgetResponse.to_json)
           (Some Values.GetBudgetResponse.error_to_json)])
let get_farm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_farm (Values.GetFarmRequest.make ~farmId ())
           (Some Values.GetFarmResponse.to_json)
           (Some Values.GetFarmResponse.error_to_json)])
let get_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_fleet (Values.GetFleetRequest.make ~farmId ~fleetId ())
           (Some Values.GetFleetResponse.to_json)
           (Some Values.GetFleetResponse.error_to_json)])
let get_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job (Values.GetJobRequest.make ~farmId ~queueId ~jobId ())
           (Some Values.GetJobResponse.to_json)
           (Some Values.GetJobResponse.error_to_json)])
let get_license_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseEndpointId =
         flag "license-endpoint-id" (required string)
           ~doc:"STRING LicenseEndpointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_license_endpoint
           (Values.GetLicenseEndpointRequest.make ~licenseEndpointId ())
           (Some Values.GetLicenseEndpointResponse.to_json)
           (Some Values.GetLicenseEndpointResponse.error_to_json)])
let get_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and limitId = flag "limit-id" (required string) ~doc:"STRING LimitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_limit (Values.GetLimitRequest.make ~farmId ~limitId ())
           (Some Values.GetLimitResponse.to_json)
           (Some Values.GetLimitResponse.error_to_json)])
let get_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorId =
         flag "monitor-id" (required string) ~doc:"STRING MonitorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_monitor (Values.GetMonitorRequest.make ~monitorId ())
           (Some Values.GetMonitorResponse.to_json)
           (Some Values.GetMonitorResponse.error_to_json)])
let get_monitor_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorId =
         flag "monitor-id" (required string) ~doc:"STRING MonitorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_monitor_settings
           (Values.GetMonitorSettingsRequest.make ~monitorId ())
           (Some Values.GetMonitorSettingsResponse.to_json)
           (Some Values.GetMonitorSettingsResponse.error_to_json)])
let get_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_queue (Values.GetQueueRequest.make ~farmId ~queueId ())
           (Some Values.GetQueueResponse.to_json)
           (Some Values.GetQueueResponse.error_to_json)])
let get_queue_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and queueEnvironmentId =
         flag "queue-environment-id" (required string)
           ~doc:"STRING QueueEnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_queue_environment
           (Values.GetQueueEnvironmentRequest.make ~farmId ~queueId
              ~queueEnvironmentId ())
           (Some Values.GetQueueEnvironmentResponse.to_json)
           (Some Values.GetQueueEnvironmentResponse.error_to_json)])
let get_queue_fleet_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_queue_fleet_association
           (Values.GetQueueFleetAssociationRequest.make ~farmId ~queueId
              ~fleetId ())
           (Some Values.GetQueueFleetAssociationResponse.to_json)
           (Some Values.GetQueueFleetAssociationResponse.error_to_json)])
let get_queue_limit_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and limitId = flag "limit-id" (required string) ~doc:"STRING LimitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_queue_limit_association
           (Values.GetQueueLimitAssociationRequest.make ~farmId ~queueId
              ~limitId ())
           (Some Values.GetQueueLimitAssociationResponse.to_json)
           (Some Values.GetQueueLimitAssociationResponse.error_to_json)])
let get_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING SessionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_session
           (Values.GetSessionRequest.make ~farmId ~queueId ~jobId ~sessionId
              ()) (Some Values.GetSessionResponse.to_json)
           (Some Values.GetSessionResponse.error_to_json)])
let get_session_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and sessionActionId =
         flag "session-action-id" (required string)
           ~doc:"STRING SessionActionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_session_action
           (Values.GetSessionActionRequest.make ~farmId ~queueId ~jobId
              ~sessionActionId ())
           (Some Values.GetSessionActionResponse.to_json)
           (Some Values.GetSessionActionResponse.error_to_json)])
let get_sessions_statistics_aggregation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and aggregationId =
         flag "aggregation-id" (required string) ~doc:"STRING AggregationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sessions_statistics_aggregation
           (Values.GetSessionsStatisticsAggregationRequest.make ?nextToken
              ?maxResults ~farmId ~aggregationId ())
           (Some Values.GetSessionsStatisticsAggregationResponse.to_json)
           (Some
              Values.GetSessionsStatisticsAggregationResponse.error_to_json)])
let get_step =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and stepId = flag "step-id" (required string) ~doc:"STRING StepId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_step
           (Values.GetStepRequest.make ~farmId ~queueId ~jobId ~stepId ())
           (Some Values.GetStepResponse.to_json)
           (Some Values.GetStepResponse.error_to_json)])
let get_storage_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and storageProfileId =
         flag "storage-profile-id" (required string)
           ~doc:"STRING StorageProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_storage_profile
           (Values.GetStorageProfileRequest.make ~farmId ~storageProfileId ())
           (Some Values.GetStorageProfileResponse.to_json)
           (Some Values.GetStorageProfileResponse.error_to_json)])
let get_storage_profile_for_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and storageProfileId =
         flag "storage-profile-id" (required string)
           ~doc:"STRING StorageProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_storage_profile_for_queue
           (Values.GetStorageProfileForQueueRequest.make ~farmId ~queueId
              ~storageProfileId ())
           (Some Values.GetStorageProfileForQueueResponse.to_json)
           (Some Values.GetStorageProfileForQueueResponse.error_to_json)])
let get_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and stepId = flag "step-id" (required string) ~doc:"STRING StepId"
       and taskId = flag "task-id" (required string) ~doc:"STRING TaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_task
           (Values.GetTaskRequest.make ~farmId ~queueId ~jobId ~stepId
              ~taskId ()) (Some Values.GetTaskResponse.to_json)
           (Some Values.GetTaskResponse.error_to_json)])
let get_worker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING WorkerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_worker
           (Values.GetWorkerRequest.make ~farmId ~fleetId ~workerId ())
           (Some Values.GetWorkerResponse.to_json)
           (Some Values.GetWorkerResponse.error_to_json)])
let list_available_metered_products =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_available_metered_products
           (Values.ListAvailableMeteredProductsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListAvailableMeteredProductsResponse.to_json)
           (Some Values.ListAvailableMeteredProductsResponse.error_to_json)])
let list_budgets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON BudgetStatus"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_budgets
           (Values.ListBudgetsRequest.make ?nextToken ?maxResults
              ?status:(Option.map ~f:Values.BudgetStatus.of_json status)
              ~farmId ()) (Some Values.ListBudgetsResponse.to_json)
           (Some Values.ListBudgetsResponse.error_to_json)])
let list_farm_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_farm_members
           (Values.ListFarmMembersRequest.make ?nextToken ?maxResults ~farmId
              ()) (Some Values.ListFarmMembersResponse.to_json)
           (Some Values.ListFarmMembersResponse.error_to_json)])
let list_farms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and principalId =
         flag "principal-id" (optional string)
           ~doc:"STRING IdentityCenterPrincipalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_farms
           (Values.ListFarmsRequest.make ?nextToken ?maxResults ?principalId
              ()) (Some Values.ListFarmsResponse.to_json)
           (Some Values.ListFarmsResponse.error_to_json)])
let list_fleet_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_fleet_members
           (Values.ListFleetMembersRequest.make ?nextToken ?maxResults
              ~farmId ~fleetId ())
           (Some Values.ListFleetMembersResponse.to_json)
           (Some Values.ListFleetMembersResponse.error_to_json)])
let list_fleets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and principalId =
         flag "principal-id" (optional string)
           ~doc:"STRING IdentityCenterPrincipalId"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ResourceName"
       and status = flag "status" (optional json_arg) ~doc:"JSON FleetStatus"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_fleets
           (Values.ListFleetsRequest.make ?nextToken ?maxResults ?principalId
              ?displayName
              ?status:(Option.map ~f:Values.FleetStatus.of_json status)
              ~farmId ()) (Some Values.ListFleetsResponse.to_json)
           (Some Values.ListFleetsResponse.error_to_json)])
let list_job_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_job_members
           (Values.ListJobMembersRequest.make ?nextToken ?maxResults ~farmId
              ~queueId ~jobId ())
           (Some Values.ListJobMembersResponse.to_json)
           (Some Values.ListJobMembersResponse.error_to_json)])
let list_job_parameter_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_job_parameter_definitions
           (Values.ListJobParameterDefinitionsRequest.make ?nextToken
              ?maxResults ~farmId ~queueId ~jobId ())
           (Some Values.ListJobParameterDefinitionsResponse.to_json)
           (Some Values.ListJobParameterDefinitionsResponse.error_to_json)])
let list_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and principalId =
         flag "principal-id" (optional string)
           ~doc:"STRING IdentityCenterPrincipalId"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_jobs
           (Values.ListJobsRequest.make ?nextToken ?maxResults ?principalId
              ~farmId ~queueId ()) (Some Values.ListJobsResponse.to_json)
           (Some Values.ListJobsResponse.error_to_json)])
let list_license_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_license_endpoints
           (Values.ListLicenseEndpointsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListLicenseEndpointsResponse.to_json)
           (Some Values.ListLicenseEndpointsResponse.error_to_json)])
let list_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_limits
           (Values.ListLimitsRequest.make ?nextToken ?maxResults ~farmId ())
           (Some Values.ListLimitsResponse.to_json)
           (Some Values.ListLimitsResponse.error_to_json)])
let list_metered_products =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and licenseEndpointId =
         flag "license-endpoint-id" (required string)
           ~doc:"STRING LicenseEndpointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_metered_products
           (Values.ListMeteredProductsRequest.make ?nextToken ?maxResults
              ~licenseEndpointId ())
           (Some Values.ListMeteredProductsResponse.to_json)
           (Some Values.ListMeteredProductsResponse.error_to_json)])
let list_monitors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_monitors
           (Values.ListMonitorsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListMonitorsResponse.to_json)
           (Some Values.ListMonitorsResponse.error_to_json)])
let list_queue_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queue_environments
           (Values.ListQueueEnvironmentsRequest.make ?nextToken ?maxResults
              ~farmId ~queueId ())
           (Some Values.ListQueueEnvironmentsResponse.to_json)
           (Some Values.ListQueueEnvironmentsResponse.error_to_json)])
let list_queue_fleet_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and queueId = flag "queue-id" (optional string) ~doc:"STRING QueueId"
       and fleetId = flag "fleet-id" (optional string) ~doc:"STRING FleetId"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queue_fleet_associations
           (Values.ListQueueFleetAssociationsRequest.make ?nextToken
              ?maxResults ?queueId ?fleetId ~farmId ())
           (Some Values.ListQueueFleetAssociationsResponse.to_json)
           (Some Values.ListQueueFleetAssociationsResponse.error_to_json)])
let list_queue_limit_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and queueId = flag "queue-id" (optional string) ~doc:"STRING QueueId"
       and limitId = flag "limit-id" (optional string) ~doc:"STRING LimitId"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queue_limit_associations
           (Values.ListQueueLimitAssociationsRequest.make ?nextToken
              ?maxResults ?queueId ?limitId ~farmId ())
           (Some Values.ListQueueLimitAssociationsResponse.to_json)
           (Some Values.ListQueueLimitAssociationsResponse.error_to_json)])
let list_queue_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queue_members
           (Values.ListQueueMembersRequest.make ?nextToken ?maxResults
              ~farmId ~queueId ())
           (Some Values.ListQueueMembersResponse.to_json)
           (Some Values.ListQueueMembersResponse.error_to_json)])
let list_queues =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and principalId =
         flag "principal-id" (optional string)
           ~doc:"STRING IdentityCenterPrincipalId"
       and status = flag "status" (optional json_arg) ~doc:"JSON QueueStatus"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queues
           (Values.ListQueuesRequest.make ?nextToken ?maxResults ?principalId
              ?status:(Option.map ~f:Values.QueueStatus.of_json status)
              ~farmId ()) (Some Values.ListQueuesResponse.to_json)
           (Some Values.ListQueuesResponse.error_to_json)])
let list_session_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and sessionId =
         flag "session-id" (optional string) ~doc:"STRING SessionId"
       and taskId = flag "task-id" (optional string) ~doc:"STRING TaskId"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_session_actions
           (Values.ListSessionActionsRequest.make ?nextToken ?maxResults
              ?sessionId ?taskId ~farmId ~queueId ~jobId ())
           (Some Values.ListSessionActionsResponse.to_json)
           (Some Values.ListSessionActionsResponse.error_to_json)])
let list_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sessions
           (Values.ListSessionsRequest.make ?nextToken ?maxResults ~farmId
              ~queueId ~jobId ()) (Some Values.ListSessionsResponse.to_json)
           (Some Values.ListSessionsResponse.error_to_json)])
let list_sessions_for_worker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING WorkerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sessions_for_worker
           (Values.ListSessionsForWorkerRequest.make ?nextToken ?maxResults
              ~farmId ~fleetId ~workerId ())
           (Some Values.ListSessionsForWorkerResponse.to_json)
           (Some Values.ListSessionsForWorkerResponse.error_to_json)])
let list_step_consumers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListStepConsumersRequestMaxResultsInteger"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and stepId = flag "step-id" (required string) ~doc:"STRING StepId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_step_consumers
           (Values.ListStepConsumersRequest.make ?nextToken ?maxResults
              ~farmId ~queueId ~jobId ~stepId ())
           (Some Values.ListStepConsumersResponse.to_json)
           (Some Values.ListStepConsumersResponse.error_to_json)])
let list_step_dependencies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListStepDependenciesRequestMaxResultsInteger"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and stepId = flag "step-id" (required string) ~doc:"STRING StepId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_step_dependencies
           (Values.ListStepDependenciesRequest.make ?nextToken ?maxResults
              ~farmId ~queueId ~jobId ~stepId ())
           (Some Values.ListStepDependenciesResponse.to_json)
           (Some Values.ListStepDependenciesResponse.error_to_json)])
let list_steps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_steps
           (Values.ListStepsRequest.make ?nextToken ?maxResults ~farmId
              ~queueId ~jobId ()) (Some Values.ListStepsResponse.to_json)
           (Some Values.ListStepsResponse.error_to_json)])
let list_storage_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_storage_profiles
           (Values.ListStorageProfilesRequest.make ?nextToken ?maxResults
              ~farmId ()) (Some Values.ListStorageProfilesResponse.to_json)
           (Some Values.ListStorageProfilesResponse.error_to_json)])
let list_storage_profiles_for_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_storage_profiles_for_queue
           (Values.ListStorageProfilesForQueueRequest.make ?nextToken
              ?maxResults ~farmId ~queueId ())
           (Some Values.ListStorageProfilesForQueueResponse.to_json)
           (Some Values.ListStorageProfilesForQueueResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and stepId = flag "step-id" (required string) ~doc:"STRING StepId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tasks
           (Values.ListTasksRequest.make ?nextToken ?maxResults ~farmId
              ~queueId ~jobId ~stepId ())
           (Some Values.ListTasksResponse.to_json)
           (Some Values.ListTasksResponse.error_to_json)])
let list_workers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workers
           (Values.ListWorkersRequest.make ?nextToken ?maxResults ~farmId
              ~fleetId ()) (Some Values.ListWorkersResponse.to_json)
           (Some Values.ListWorkersResponse.error_to_json)])
let put_metered_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseEndpointId =
         flag "license-endpoint-id" (required string)
           ~doc:"STRING LicenseEndpointId"
       and productId =
         flag "product-id" (required string) ~doc:"STRING MeteredProductId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_metered_product
           (Values.PutMeteredProductRequest.make ~licenseEndpointId
              ~productId ()) (Some Values.PutMeteredProductResponse.to_json)
           (Some Values.PutMeteredProductResponse.error_to_json)])
let search_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterExpressions =
         flag "filter-expressions" (optional json_arg)
           ~doc:"JSON SearchGroupedFilterExpressions"
       and sortExpressions =
         flag "sort-expressions" (optional json_arg)
           ~doc:"JSON SearchSortExpressions"
       and pageSize =
         flag "page-size" (optional int)
           ~doc:"INT SearchJobsRequestPageSizeInteger"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and itemOffset =
         flag "item-offset" (required int)
           ~doc:"INT SearchJobsRequestItemOffsetInteger"
       and queueIds =
         flag "queue-ids" (required json_arg)
           ~doc:"JSON SearchJobsRequestQueueIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_jobs
           (Values.SearchJobsRequest.make
              ?filterExpressions:(Option.map
                                    ~f:Values.SearchGroupedFilterExpressions.of_json
                                    filterExpressions)
              ?sortExpressions:(Option.map
                                  ~f:Values.SearchSortExpressions.of_json
                                  sortExpressions) ?pageSize ~farmId
              ~itemOffset
              ~queueIds:(Values.SearchJobsRequestQueueIdsList.of_json
                           queueIds) ())
           (Some Values.SearchJobsResponse.to_json)
           (Some Values.SearchJobsResponse.error_to_json)])
let search_steps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterExpressions =
         flag "filter-expressions" (optional json_arg)
           ~doc:"JSON SearchGroupedFilterExpressions"
       and sortExpressions =
         flag "sort-expressions" (optional json_arg)
           ~doc:"JSON SearchSortExpressions"
       and pageSize =
         flag "page-size" (optional int)
           ~doc:"INT SearchStepsRequestPageSizeInteger"
       and jobId = flag "job-id" (optional string) ~doc:"STRING JobId"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and itemOffset =
         flag "item-offset" (required int)
           ~doc:"INT SearchStepsRequestItemOffsetInteger"
       and queueIds =
         flag "queue-ids" (required json_arg)
           ~doc:"JSON SearchStepsRequestQueueIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_steps
           (Values.SearchStepsRequest.make
              ?filterExpressions:(Option.map
                                    ~f:Values.SearchGroupedFilterExpressions.of_json
                                    filterExpressions)
              ?sortExpressions:(Option.map
                                  ~f:Values.SearchSortExpressions.of_json
                                  sortExpressions) ?pageSize ?jobId ~farmId
              ~itemOffset
              ~queueIds:(Values.SearchStepsRequestQueueIdsList.of_json
                           queueIds) ())
           (Some Values.SearchStepsResponse.to_json)
           (Some Values.SearchStepsResponse.error_to_json)])
let search_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterExpressions =
         flag "filter-expressions" (optional json_arg)
           ~doc:"JSON SearchGroupedFilterExpressions"
       and sortExpressions =
         flag "sort-expressions" (optional json_arg)
           ~doc:"JSON SearchSortExpressions"
       and pageSize =
         flag "page-size" (optional int)
           ~doc:"INT SearchTasksRequestPageSizeInteger"
       and jobId = flag "job-id" (optional string) ~doc:"STRING JobId"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and itemOffset =
         flag "item-offset" (required int)
           ~doc:"INT SearchTasksRequestItemOffsetInteger"
       and queueIds =
         flag "queue-ids" (required json_arg)
           ~doc:"JSON SearchTasksRequestQueueIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_tasks
           (Values.SearchTasksRequest.make
              ?filterExpressions:(Option.map
                                    ~f:Values.SearchGroupedFilterExpressions.of_json
                                    filterExpressions)
              ?sortExpressions:(Option.map
                                  ~f:Values.SearchSortExpressions.of_json
                                  sortExpressions) ?pageSize ?jobId ~farmId
              ~itemOffset
              ~queueIds:(Values.SearchTasksRequestQueueIdsList.of_json
                           queueIds) ())
           (Some Values.SearchTasksResponse.to_json)
           (Some Values.SearchTasksResponse.error_to_json)])
let search_workers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterExpressions =
         flag "filter-expressions" (optional json_arg)
           ~doc:"JSON SearchGroupedFilterExpressions"
       and sortExpressions =
         flag "sort-expressions" (optional json_arg)
           ~doc:"JSON SearchSortExpressions"
       and pageSize =
         flag "page-size" (optional int)
           ~doc:"INT SearchWorkersRequestPageSizeInteger"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and itemOffset =
         flag "item-offset" (required int)
           ~doc:"INT SearchWorkersRequestItemOffsetInteger"
       and fleetIds =
         flag "fleet-ids" (required json_arg)
           ~doc:"JSON SearchWorkersRequestFleetIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_workers
           (Values.SearchWorkersRequest.make
              ?filterExpressions:(Option.map
                                    ~f:Values.SearchGroupedFilterExpressions.of_json
                                    filterExpressions)
              ?sortExpressions:(Option.map
                                  ~f:Values.SearchSortExpressions.of_json
                                  sortExpressions) ?pageSize ~farmId
              ~itemOffset
              ~fleetIds:(Values.SearchWorkersRequestFleetIdsList.of_json
                           fleetIds) ())
           (Some Values.SearchWorkersResponse.to_json)
           (Some Values.SearchWorkersResponse.error_to_json)])
let start_sessions_statistics_aggregation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timezone =
         flag "timezone" (optional string) ~doc:"STRING Timezone"
       and period = flag "period" (optional json_arg) ~doc:"JSON Period"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and resourceIds =
         flag "resource-ids" (required json_arg)
           ~doc:"JSON SessionsStatisticsResources"
       and startTime =
         flag "start-time" (required json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and endTime =
         flag "end-time" (required json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and groupBy =
         flag "group-by" (required json_arg) ~doc:"JSON UsageGroupBy"
       and statistics =
         flag "statistics" (required json_arg) ~doc:"JSON UsageStatistics" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_sessions_statistics_aggregation
           (Values.StartSessionsStatisticsAggregationRequest.make ?timezone
              ?period:(Option.map ~f:Values.Period.of_json period) ~farmId
              ~resourceIds:(Values.SessionsStatisticsResources.of_json
                              resourceIds)
              ~startTime:(Values.SyntheticTimestamp_date_time.of_json
                            startTime)
              ~endTime:(Values.SyntheticTimestamp_date_time.of_json endTime)
              ~groupBy:(Values.UsageGroupBy.of_json groupBy)
              ~statistics:(Values.UsageStatistics.of_json statistics) ())
           (Some Values.StartSessionsStatisticsAggregationResponse.to_json)
           (Some
              Values.StartSessionsStatisticsAggregationResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~resourceArn ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.StringList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_budget =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ResourceName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON BudgetStatus"
       and approximateDollarLimit =
         flag "approximate-dollar-limit" (optional float)
           ~doc:"FLOAT ConsumedUsageLimit"
       and actionsToAdd =
         flag "actions-to-add" (optional json_arg)
           ~doc:"JSON BudgetActionsToAdd"
       and actionsToRemove =
         flag "actions-to-remove" (optional json_arg)
           ~doc:"JSON BudgetActionsToRemove"
       and schedule =
         flag "schedule" (optional json_arg) ~doc:"JSON BudgetSchedule"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and budgetId =
         flag "budget-id" (required string) ~doc:"STRING BudgetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_budget
           (Values.UpdateBudgetRequest.make ?clientToken ?displayName
              ?description
              ?status:(Option.map ~f:Values.BudgetStatus.of_json status)
              ?approximateDollarLimit
              ?actionsToAdd:(Option.map ~f:Values.BudgetActionsToAdd.of_json
                               actionsToAdd)
              ?actionsToRemove:(Option.map
                                  ~f:Values.BudgetActionsToRemove.of_json
                                  actionsToRemove)
              ?schedule:(Option.map ~f:Values.BudgetSchedule.of_json schedule)
              ~farmId ~budgetId ())
           (Some Values.UpdateBudgetResponse.to_json)
           (Some Values.UpdateBudgetResponse.error_to_json)])
let update_farm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ResourceName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and costScaleFactor =
         flag "cost-scale-factor" (optional float)
           ~doc:"FLOAT CostScaleFactor"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_farm
           (Values.UpdateFarmRequest.make ?displayName ?description
              ?costScaleFactor ~farmId ())
           (Some Values.UpdateFarmResponse.to_json)
           (Some Values.UpdateFarmResponse.error_to_json)])
let update_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ResourceName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and minWorkerCount =
         flag "min-worker-count" (optional int) ~doc:"INT MinZeroMaxInteger"
       and maxWorkerCount =
         flag "max-worker-count" (optional int) ~doc:"INT MinZeroMaxInteger"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON FleetConfiguration"
       and hostConfiguration =
         flag "host-configuration" (optional json_arg)
           ~doc:"JSON HostConfiguration"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_fleet
           (Values.UpdateFleetRequest.make ?clientToken ?displayName
              ?description ?roleArn ?minWorkerCount ?maxWorkerCount
              ?configuration:(Option.map ~f:Values.FleetConfiguration.of_json
                                configuration)
              ?hostConfiguration:(Option.map
                                    ~f:Values.HostConfiguration.of_json
                                    hostConfiguration) ~farmId ~fleetId ())
           (Some Values.UpdateFleetResponse.to_json)
           (Some Values.UpdateFleetResponse.error_to_json)])
let update_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and targetTaskRunStatus =
         flag "target-task-run-status" (optional json_arg)
           ~doc:"JSON JobTargetTaskRunStatus"
       and priority = flag "priority" (optional int) ~doc:"INT JobPriority"
       and maxFailedTasksCount =
         flag "max-failed-tasks-count" (optional int)
           ~doc:"INT MaxFailedTasksCount"
       and maxRetriesPerTask =
         flag "max-retries-per-task" (optional int)
           ~doc:"INT MaxRetriesPerTask"
       and lifecycleStatus =
         flag "lifecycle-status" (optional json_arg)
           ~doc:"JSON UpdateJobLifecycleStatus"
       and maxWorkerCount =
         flag "max-worker-count" (optional int) ~doc:"INT MaxWorkerCount"
       and name = flag "name" (optional string) ~doc:"STRING JobName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING JobDescriptionOverride"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_job
           (Values.UpdateJobRequest.make ?clientToken
              ?targetTaskRunStatus:(Option.map
                                      ~f:Values.JobTargetTaskRunStatus.of_json
                                      targetTaskRunStatus) ?priority
              ?maxFailedTasksCount ?maxRetriesPerTask
              ?lifecycleStatus:(Option.map
                                  ~f:Values.UpdateJobLifecycleStatus.of_json
                                  lifecycleStatus) ?maxWorkerCount ?name
              ?description ~farmId ~queueId ~jobId ())
           (Some Values.UpdateJobResponse.to_json)
           (Some Values.UpdateJobResponse.error_to_json)])
let update_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ResourceName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and maxCount = flag "max-count" (optional int) ~doc:"INT MaxCount"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and limitId = flag "limit-id" (required string) ~doc:"STRING LimitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_limit
           (Values.UpdateLimitRequest.make ?displayName ?description
              ?maxCount ~farmId ~limitId ())
           (Some Values.UpdateLimitResponse.to_json)
           (Some Values.UpdateLimitResponse.error_to_json)])
let update_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdomain =
         flag "subdomain" (optional string) ~doc:"STRING Subdomain"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ResourceName"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and monitorId =
         flag "monitor-id" (required string) ~doc:"STRING MonitorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_monitor
           (Values.UpdateMonitorRequest.make ?subdomain ?displayName ?roleArn
              ~monitorId ()) (Some Values.UpdateMonitorResponse.to_json)
           (Some Values.UpdateMonitorResponse.error_to_json)])
let update_monitor_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorId =
         flag "monitor-id" (required string) ~doc:"STRING MonitorId"
       and settings =
         flag "settings" (required json_arg) ~doc:"JSON SettingsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_monitor_settings
           (Values.UpdateMonitorSettingsRequest.make ~monitorId
              ~settings:(Values.SettingsMap.of_json settings) ())
           (Some Values.UpdateMonitorSettingsResponse.to_json)
           (Some Values.UpdateMonitorSettingsResponse.error_to_json)])
let update_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ResourceName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and defaultBudgetAction =
         flag "default-budget-action" (optional json_arg)
           ~doc:"JSON DefaultQueueBudgetAction"
       and jobAttachmentSettings =
         flag "job-attachment-settings" (optional json_arg)
           ~doc:"JSON JobAttachmentSettings"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and jobRunAsUser =
         flag "job-run-as-user" (optional json_arg) ~doc:"JSON JobRunAsUser"
       and requiredFileSystemLocationNamesToAdd =
         flag "required-file-system-location-names-to-add"
           (optional json_arg) ~doc:"JSON RequiredFileSystemLocationNames"
       and requiredFileSystemLocationNamesToRemove =
         flag "required-file-system-location-names-to-remove"
           (optional json_arg) ~doc:"JSON RequiredFileSystemLocationNames"
       and allowedStorageProfileIdsToAdd =
         flag "allowed-storage-profile-ids-to-add" (optional json_arg)
           ~doc:"JSON AllowedStorageProfileIds"
       and allowedStorageProfileIdsToRemove =
         flag "allowed-storage-profile-ids-to-remove" (optional json_arg)
           ~doc:"JSON AllowedStorageProfileIds"
       and schedulingConfiguration =
         flag "scheduling-configuration" (optional json_arg)
           ~doc:"JSON SchedulingConfiguration"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_queue
           (Values.UpdateQueueRequest.make ?clientToken ?displayName
              ?description
              ?defaultBudgetAction:(Option.map
                                      ~f:Values.DefaultQueueBudgetAction.of_json
                                      defaultBudgetAction)
              ?jobAttachmentSettings:(Option.map
                                        ~f:Values.JobAttachmentSettings.of_json
                                        jobAttachmentSettings) ?roleArn
              ?jobRunAsUser:(Option.map ~f:Values.JobRunAsUser.of_json
                               jobRunAsUser)
              ?requiredFileSystemLocationNamesToAdd:(Option.map
                                                       ~f:Values.RequiredFileSystemLocationNames.of_json
                                                       requiredFileSystemLocationNamesToAdd)
              ?requiredFileSystemLocationNamesToRemove:(Option.map
                                                          ~f:Values.RequiredFileSystemLocationNames.of_json
                                                          requiredFileSystemLocationNamesToRemove)
              ?allowedStorageProfileIdsToAdd:(Option.map
                                                ~f:Values.AllowedStorageProfileIds.of_json
                                                allowedStorageProfileIdsToAdd)
              ?allowedStorageProfileIdsToRemove:(Option.map
                                                   ~f:Values.AllowedStorageProfileIds.of_json
                                                   allowedStorageProfileIdsToRemove)
              ?schedulingConfiguration:(Option.map
                                          ~f:Values.SchedulingConfiguration.of_json
                                          schedulingConfiguration) ~farmId
              ~queueId ()) (Some Values.UpdateQueueResponse.to_json)
           (Some Values.UpdateQueueResponse.error_to_json)])
let update_queue_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and priority = flag "priority" (optional int) ~doc:"INT Priority"
       and templateType =
         flag "template-type" (optional json_arg)
           ~doc:"JSON EnvironmentTemplateType"
       and template =
         flag "template" (optional string) ~doc:"STRING EnvironmentTemplate"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and queueEnvironmentId =
         flag "queue-environment-id" (required string)
           ~doc:"STRING QueueEnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_queue_environment
           (Values.UpdateQueueEnvironmentRequest.make ?clientToken ?priority
              ?templateType:(Option.map
                               ~f:Values.EnvironmentTemplateType.of_json
                               templateType) ?template ~farmId ~queueId
              ~queueEnvironmentId ())
           (Some Values.UpdateQueueEnvironmentResponse.to_json)
           (Some Values.UpdateQueueEnvironmentResponse.error_to_json)])
let update_queue_fleet_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and status =
         flag "status" (required json_arg)
           ~doc:"JSON UpdateQueueFleetAssociationStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_queue_fleet_association
           (Values.UpdateQueueFleetAssociationRequest.make ~farmId ~queueId
              ~fleetId
              ~status:(Values.UpdateQueueFleetAssociationStatus.of_json
                         status) ())
           (Some Values.UpdateQueueFleetAssociationResponse.to_json)
           (Some Values.UpdateQueueFleetAssociationResponse.error_to_json)])
let update_queue_limit_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and limitId = flag "limit-id" (required string) ~doc:"STRING LimitId"
       and status =
         flag "status" (required json_arg)
           ~doc:"JSON UpdateQueueLimitAssociationStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_queue_limit_association
           (Values.UpdateQueueLimitAssociationRequest.make ~farmId ~queueId
              ~limitId
              ~status:(Values.UpdateQueueLimitAssociationStatus.of_json
                         status) ())
           (Some Values.UpdateQueueLimitAssociationResponse.to_json)
           (Some Values.UpdateQueueLimitAssociationResponse.error_to_json)])
let update_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING SessionId"
       and targetLifecycleStatus =
         flag "target-lifecycle-status" (required json_arg)
           ~doc:"JSON SessionLifecycleTargetStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_session
           (Values.UpdateSessionRequest.make ?clientToken ~farmId ~queueId
              ~jobId ~sessionId
              ~targetLifecycleStatus:(Values.SessionLifecycleTargetStatus.of_json
                                        targetLifecycleStatus) ())
           (Some Values.UpdateSessionResponse.to_json)
           (Some Values.UpdateSessionResponse.error_to_json)])
let update_step =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and stepId = flag "step-id" (required string) ~doc:"STRING StepId"
       and targetTaskRunStatus =
         flag "target-task-run-status" (required json_arg)
           ~doc:"JSON StepTargetTaskRunStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_step
           (Values.UpdateStepRequest.make ?clientToken ~farmId ~queueId
              ~jobId ~stepId
              ~targetTaskRunStatus:(Values.StepTargetTaskRunStatus.of_json
                                      targetTaskRunStatus) ())
           (Some Values.UpdateStepResponse.to_json)
           (Some Values.UpdateStepResponse.error_to_json)])
let update_storage_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ResourceName"
       and osFamily =
         flag "os-family" (optional json_arg)
           ~doc:"JSON StorageProfileOperatingSystemFamily"
       and fileSystemLocationsToAdd =
         flag "file-system-locations-to-add" (optional json_arg)
           ~doc:"JSON FileSystemLocationsList"
       and fileSystemLocationsToRemove =
         flag "file-system-locations-to-remove" (optional json_arg)
           ~doc:"JSON FileSystemLocationsList"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and storageProfileId =
         flag "storage-profile-id" (required string)
           ~doc:"STRING StorageProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_storage_profile
           (Values.UpdateStorageProfileRequest.make ?clientToken ?displayName
              ?osFamily:(Option.map
                           ~f:Values.StorageProfileOperatingSystemFamily.of_json
                           osFamily)
              ?fileSystemLocationsToAdd:(Option.map
                                           ~f:Values.FileSystemLocationsList.of_json
                                           fileSystemLocationsToAdd)
              ?fileSystemLocationsToRemove:(Option.map
                                              ~f:Values.FileSystemLocationsList.of_json
                                              fileSystemLocationsToRemove)
              ~farmId ~storageProfileId ())
           (Some Values.UpdateStorageProfileResponse.to_json)
           (Some Values.UpdateStorageProfileResponse.error_to_json)])
let update_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and queueId = flag "queue-id" (required string) ~doc:"STRING QueueId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and stepId = flag "step-id" (required string) ~doc:"STRING StepId"
       and taskId = flag "task-id" (required string) ~doc:"STRING TaskId"
       and targetRunStatus =
         flag "target-run-status" (required json_arg)
           ~doc:"JSON TaskTargetRunStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_task
           (Values.UpdateTaskRequest.make ?clientToken ~farmId ~queueId
              ~jobId ~stepId ~taskId
              ~targetRunStatus:(Values.TaskTargetRunStatus.of_json
                                  targetRunStatus) ())
           (Some Values.UpdateTaskResponse.to_json)
           (Some Values.UpdateTaskResponse.error_to_json)])
let update_worker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON UpdatedWorkerStatus"
       and capabilities =
         flag "capabilities" (optional json_arg)
           ~doc:"JSON WorkerCapabilities"
       and hostProperties =
         flag "host-properties" (optional json_arg)
           ~doc:"JSON HostPropertiesRequest"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING WorkerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_worker
           (Values.UpdateWorkerRequest.make
              ?status:(Option.map ~f:Values.UpdatedWorkerStatus.of_json
                         status)
              ?capabilities:(Option.map ~f:Values.WorkerCapabilities.of_json
                               capabilities)
              ?hostProperties:(Option.map
                                 ~f:Values.HostPropertiesRequest.of_json
                                 hostProperties) ~farmId ~fleetId ~workerId
              ()) (Some Values.UpdateWorkerResponse.to_json)
           (Some Values.UpdateWorkerResponse.error_to_json)])
let update_worker_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updatedSessionActions =
         flag "updated-session-actions" (optional json_arg)
           ~doc:"JSON UpdatedSessionActions"
       and farmId = flag "farm-id" (required string) ~doc:"STRING FarmId"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING WorkerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_worker_schedule
           (Values.UpdateWorkerScheduleRequest.make
              ?updatedSessionActions:(Option.map
                                        ~f:Values.UpdatedSessionActions.of_json
                                        updatedSessionActions) ~farmId
              ~fleetId ~workerId ())
           (Some Values.UpdateWorkerScheduleResponse.to_json)
           (Some Values.UpdateWorkerScheduleResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-member-to-farm", associate_member_to_farm);
    ("associate-member-to-fleet", associate_member_to_fleet);
    ("associate-member-to-job", associate_member_to_job);
    ("associate-member-to-queue", associate_member_to_queue);
    ("assume-fleet-role-for-read", assume_fleet_role_for_read);
    ("assume-fleet-role-for-worker", assume_fleet_role_for_worker);
    ("assume-queue-role-for-read", assume_queue_role_for_read);
    ("assume-queue-role-for-user", assume_queue_role_for_user);
    ("assume-queue-role-for-worker", assume_queue_role_for_worker);
    ("batch-get-job", batch_get_job);
    ("batch-get-job-entity", batch_get_job_entity);
    ("batch-get-session", batch_get_session);
    ("batch-get-session-action", batch_get_session_action);
    ("batch-get-step", batch_get_step);
    ("batch-get-task", batch_get_task);
    ("batch-get-worker", batch_get_worker);
    ("batch-update-job", batch_update_job);
    ("batch-update-task", batch_update_task);
    ("copy-job-template", copy_job_template);
    ("create-budget", create_budget);
    ("create-farm", create_farm);
    ("create-fleet", create_fleet);
    ("create-job", create_job);
    ("create-license-endpoint", create_license_endpoint);
    ("create-limit", create_limit);
    ("create-monitor", create_monitor);
    ("create-queue", create_queue);
    ("create-queue-environment", create_queue_environment);
    ("create-queue-fleet-association", create_queue_fleet_association);
    ("create-queue-limit-association", create_queue_limit_association);
    ("create-storage-profile", create_storage_profile);
    ("create-worker", create_worker);
    ("delete-budget", delete_budget);
    ("delete-farm", delete_farm);
    ("delete-fleet", delete_fleet);
    ("delete-license-endpoint", delete_license_endpoint);
    ("delete-limit", delete_limit);
    ("delete-metered-product", delete_metered_product);
    ("delete-monitor", delete_monitor);
    ("delete-queue", delete_queue);
    ("delete-queue-environment", delete_queue_environment);
    ("delete-queue-fleet-association", delete_queue_fleet_association);
    ("delete-queue-limit-association", delete_queue_limit_association);
    ("delete-storage-profile", delete_storage_profile);
    ("delete-worker", delete_worker);
    ("disassociate-member-from-farm", disassociate_member_from_farm);
    ("disassociate-member-from-fleet", disassociate_member_from_fleet);
    ("disassociate-member-from-job", disassociate_member_from_job);
    ("disassociate-member-from-queue", disassociate_member_from_queue);
    ("get-budget", get_budget);
    ("get-farm", get_farm);
    ("get-fleet", get_fleet);
    ("get-job", get_job);
    ("get-license-endpoint", get_license_endpoint);
    ("get-limit", get_limit);
    ("get-monitor", get_monitor);
    ("get-monitor-settings", get_monitor_settings);
    ("get-queue", get_queue);
    ("get-queue-environment", get_queue_environment);
    ("get-queue-fleet-association", get_queue_fleet_association);
    ("get-queue-limit-association", get_queue_limit_association);
    ("get-session", get_session);
    ("get-session-action", get_session_action);
    ("get-sessions-statistics-aggregation",
      get_sessions_statistics_aggregation);
    ("get-step", get_step);
    ("get-storage-profile", get_storage_profile);
    ("get-storage-profile-for-queue", get_storage_profile_for_queue);
    ("get-task", get_task);
    ("get-worker", get_worker);
    ("list-available-metered-products", list_available_metered_products);
    ("list-budgets", list_budgets);
    ("list-farm-members", list_farm_members);
    ("list-farms", list_farms);
    ("list-fleet-members", list_fleet_members);
    ("list-fleets", list_fleets);
    ("list-job-members", list_job_members);
    ("list-job-parameter-definitions", list_job_parameter_definitions);
    ("list-jobs", list_jobs);
    ("list-license-endpoints", list_license_endpoints);
    ("list-limits", list_limits);
    ("list-metered-products", list_metered_products);
    ("list-monitors", list_monitors);
    ("list-queue-environments", list_queue_environments);
    ("list-queue-fleet-associations", list_queue_fleet_associations);
    ("list-queue-limit-associations", list_queue_limit_associations);
    ("list-queue-members", list_queue_members);
    ("list-queues", list_queues);
    ("list-session-actions", list_session_actions);
    ("list-sessions", list_sessions);
    ("list-sessions-for-worker", list_sessions_for_worker);
    ("list-step-consumers", list_step_consumers);
    ("list-step-dependencies", list_step_dependencies);
    ("list-steps", list_steps);
    ("list-storage-profiles", list_storage_profiles);
    ("list-storage-profiles-for-queue", list_storage_profiles_for_queue);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-tasks", list_tasks);
    ("list-workers", list_workers);
    ("put-metered-product", put_metered_product);
    ("search-jobs", search_jobs);
    ("search-steps", search_steps);
    ("search-tasks", search_tasks);
    ("search-workers", search_workers);
    ("start-sessions-statistics-aggregation",
      start_sessions_statistics_aggregation);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-budget", update_budget);
    ("update-farm", update_farm);
    ("update-fleet", update_fleet);
    ("update-job", update_job);
    ("update-limit", update_limit);
    ("update-monitor", update_monitor);
    ("update-monitor-settings", update_monitor_settings);
    ("update-queue", update_queue);
    ("update-queue-environment", update_queue_environment);
    ("update-queue-fleet-association", update_queue_fleet_association);
    ("update-queue-limit-association", update_queue_limit_association);
    ("update-session", update_session);
    ("update-step", update_step);
    ("update-storage-profile", update_storage_profile);
    ("update-task", update_task);
    ("update-worker", update_worker);
    ("update-worker-schedule", update_worker_schedule)]
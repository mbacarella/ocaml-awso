(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_pipe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING PipeDescription"
       and desiredState =
         flag "desired-state" (optional json_arg)
           ~doc:"JSON RequestedPipeState"
       and sourceParameters =
         flag "source-parameters" (optional json_arg)
           ~doc:"JSON PipeSourceParameters"
       and enrichment =
         flag "enrichment" (optional string) ~doc:"STRING OptionalArn"
       and enrichmentParameters =
         flag "enrichment-parameters" (optional json_arg)
           ~doc:"JSON PipeEnrichmentParameters"
       and targetParameters =
         flag "target-parameters" (optional json_arg)
           ~doc:"JSON PipeTargetParameters"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and logConfiguration =
         flag "log-configuration" (optional json_arg)
           ~doc:"JSON PipeLogConfigurationParameters"
       and kmsKeyIdentifier =
         flag "kms-key-identifier" (optional string)
           ~doc:"STRING KmsKeyIdentifier"
       and name = flag "name" (required string) ~doc:"STRING PipeName"
       and source = flag "source" (required string) ~doc:"STRING ArnOrUrl"
       and target = flag "target" (required string) ~doc:"STRING Arn"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pipe
           (Values.CreatePipeRequest.make ?description
              ?desiredState:(Option.map ~f:Values.RequestedPipeState.of_json
                               desiredState)
              ?sourceParameters:(Option.map
                                   ~f:Values.PipeSourceParameters.of_json
                                   sourceParameters) ?enrichment
              ?enrichmentParameters:(Option.map
                                       ~f:Values.PipeEnrichmentParameters.of_json
                                       enrichmentParameters)
              ?targetParameters:(Option.map
                                   ~f:Values.PipeTargetParameters.of_json
                                   targetParameters)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?logConfiguration:(Option.map
                                   ~f:Values.PipeLogConfigurationParameters.of_json
                                   logConfiguration) ?kmsKeyIdentifier ~name
              ~source ~target ~roleArn ())
           (Some Values.CreatePipeResponse.to_json)
           (Some Values.CreatePipeResponse.error_to_json)])
let delete_pipe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING PipeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_pipe (Values.DeletePipeRequest.make ~name ())
           (Some Values.DeletePipeResponse.to_json)
           (Some Values.DeletePipeResponse.error_to_json)])
let describe_pipe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING PipeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_pipe (Values.DescribePipeRequest.make ~name ())
           (Some Values.DescribePipeResponse.to_json)
           (Some Values.DescribePipeResponse.error_to_json)])
let list_pipes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namePrefix =
         flag "name-prefix" (optional string) ~doc:"STRING PipeName"
       and desiredState =
         flag "desired-state" (optional json_arg)
           ~doc:"JSON RequestedPipeState"
       and currentState =
         flag "current-state" (optional json_arg) ~doc:"JSON PipeState"
       and sourcePrefix =
         flag "source-prefix" (optional string) ~doc:"STRING ResourceArn"
       and targetPrefix =
         flag "target-prefix" (optional string) ~doc:"STRING ResourceArn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT LimitMax100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pipes
           (Values.ListPipesRequest.make ?namePrefix
              ?desiredState:(Option.map ~f:Values.RequestedPipeState.of_json
                               desiredState)
              ?currentState:(Option.map ~f:Values.PipeState.of_json
                               currentState) ?sourcePrefix ?targetPrefix
              ?nextToken ?limit ()) (Some Values.ListPipesResponse.to_json)
           (Some Values.ListPipesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING PipeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_pipe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING PipeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_pipe (Values.StartPipeRequest.make ~name ())
           (Some Values.StartPipeResponse.to_json)
           (Some Values.StartPipeResponse.error_to_json)])
let stop_pipe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING PipeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_pipe (Values.StopPipeRequest.make ~name ())
           (Some Values.StopPipeResponse.to_json)
           (Some Values.StopPipeResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING PipeArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING PipeArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_pipe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING PipeDescription"
       and desiredState =
         flag "desired-state" (optional json_arg)
           ~doc:"JSON RequestedPipeState"
       and sourceParameters =
         flag "source-parameters" (optional json_arg)
           ~doc:"JSON UpdatePipeSourceParameters"
       and enrichment =
         flag "enrichment" (optional string) ~doc:"STRING OptionalArn"
       and enrichmentParameters =
         flag "enrichment-parameters" (optional json_arg)
           ~doc:"JSON PipeEnrichmentParameters"
       and target = flag "target" (optional string) ~doc:"STRING Arn"
       and targetParameters =
         flag "target-parameters" (optional json_arg)
           ~doc:"JSON PipeTargetParameters"
       and logConfiguration =
         flag "log-configuration" (optional json_arg)
           ~doc:"JSON PipeLogConfigurationParameters"
       and kmsKeyIdentifier =
         flag "kms-key-identifier" (optional string)
           ~doc:"STRING KmsKeyIdentifier"
       and name = flag "name" (required string) ~doc:"STRING PipeName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pipe
           (Values.UpdatePipeRequest.make ?description
              ?desiredState:(Option.map ~f:Values.RequestedPipeState.of_json
                               desiredState)
              ?sourceParameters:(Option.map
                                   ~f:Values.UpdatePipeSourceParameters.of_json
                                   sourceParameters) ?enrichment
              ?enrichmentParameters:(Option.map
                                       ~f:Values.PipeEnrichmentParameters.of_json
                                       enrichmentParameters) ?target
              ?targetParameters:(Option.map
                                   ~f:Values.PipeTargetParameters.of_json
                                   targetParameters)
              ?logConfiguration:(Option.map
                                   ~f:Values.PipeLogConfigurationParameters.of_json
                                   logConfiguration) ?kmsKeyIdentifier ~name
              ~roleArn ()) (Some Values.UpdatePipeResponse.to_json)
           (Some Values.UpdatePipeResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-pipe", create_pipe);
    ("delete-pipe", delete_pipe);
    ("describe-pipe", describe_pipe);
    ("list-pipes", list_pipes);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-pipe", start_pipe);
    ("stop-pipe", stop_pipe);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-pipe", update_pipe)]
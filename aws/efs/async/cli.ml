(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_access_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and posixUser =
         flag "posix-user" (optional json_arg) ~doc:"JSON PosixUser"
       and rootDirectory =
         flag "root-directory" (optional json_arg) ~doc:"JSON RootDirectory"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_point
           (Values.CreateAccessPointRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?posixUser:(Option.map ~f:Values.PosixUser.of_json posixUser)
              ?rootDirectory:(Option.map ~f:Values.RootDirectory.of_json
                                rootDirectory) ~clientToken ~fileSystemId ())
           (Some Values.AccessPointDescription.to_json)
           (Some Values.AccessPointDescription.error_to_json)])
let create_file_system =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and performanceMode =
         flag "performance-mode" (optional json_arg)
           ~doc:"JSON PerformanceMode"
       and encrypted = flag "encrypted" (optional bool) ~doc:"BOOL Encrypted"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and throughputMode =
         flag "throughput-mode" (optional json_arg)
           ~doc:"JSON ThroughputMode"
       and provisionedThroughputInMibps =
         flag "provisioned-throughput-in-mibps" (optional float)
           ~doc:"FLOAT ProvisionedThroughputInMibps"
       and availabilityZoneName =
         flag "availability-zone-name" (optional string)
           ~doc:"STRING AvailabilityZoneName"
       and backup = flag "backup" (optional bool) ~doc:"BOOL Backup"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and creationToken =
         flag "creation-token" (required string) ~doc:"STRING CreationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_file_system
           (Values.CreateFileSystemRequest.make
              ?performanceMode:(Option.map ~f:Values.PerformanceMode.of_json
                                  performanceMode) ?encrypted ?kmsKeyId
              ?throughputMode:(Option.map ~f:Values.ThroughputMode.of_json
                                 throughputMode)
              ?provisionedThroughputInMibps ?availabilityZoneName ?backup
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~creationToken
              ()) (Some Values.FileSystemDescription.to_json)
           (Some Values.FileSystemDescription.error_to_json)])
let create_mount_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ipAddress =
         flag "ip-address" (optional string) ~doc:"STRING IpAddress"
       and securityGroups =
         flag "security-groups" (optional json_arg)
           ~doc:"JSON SecurityGroups"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId"
       and subnetId =
         flag "subnet-id" (required string) ~doc:"STRING SubnetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_mount_target
           (Values.CreateMountTargetRequest.make ?ipAddress
              ?securityGroups:(Option.map ~f:Values.SecurityGroups.of_json
                                 securityGroups) ~fileSystemId ~subnetId ())
           (Some Values.MountTargetDescription.to_json)
           (Some Values.MountTargetDescription.error_to_json)])
let create_replication_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceFileSystemId =
         flag "source-file-system-id" (required string)
           ~doc:"STRING FileSystemId"
       and destinations =
         flag "destinations" (required json_arg)
           ~doc:"JSON DestinationsToCreate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_replication_configuration
           (Values.CreateReplicationConfigurationRequest.make
              ~sourceFileSystemId
              ~destinations:(Values.DestinationsToCreate.of_json destinations)
              ()) (Some Values.ReplicationConfigurationDescription.to_json)
           (Some Values.ReplicationConfigurationDescription.error_to_json)])
let create_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_tags
           (Values.CreateTagsRequest.make ~fileSystemId
              ~tags:(Values.Tags.of_json tags) ()) None None])
let delete_access_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessPointId =
         flag "access-point-id" (required string) ~doc:"STRING AccessPointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_point
           (Values.DeleteAccessPointRequest.make ~accessPointId ()) None None])
let delete_file_system =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_file_system
           (Values.DeleteFileSystemRequest.make ~fileSystemId ()) None None])
let delete_file_system_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_file_system_policy
           (Values.DeleteFileSystemPolicyRequest.make ~fileSystemId ()) None
           None])
let delete_mount_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mountTargetId =
         flag "mount-target-id" (required string) ~doc:"STRING MountTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_mount_target
           (Values.DeleteMountTargetRequest.make ~mountTargetId ()) None None])
let delete_replication_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceFileSystemId =
         flag "source-file-system-id" (required string)
           ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_replication_configuration
           (Values.DeleteReplicationConfigurationRequest.make
              ~sourceFileSystemId ()) None None])
let delete_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tags
           (Values.DeleteTagsRequest.make ~fileSystemId
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ()) None None])
let describe_access_points =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and accessPointId =
         flag "access-point-id" (optional string) ~doc:"STRING AccessPointId"
       and fileSystemId =
         flag "file-system-id" (optional string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_access_points
           (Values.DescribeAccessPointsRequest.make ?maxResults ?nextToken
              ?accessPointId ?fileSystemId ())
           (Some Values.DescribeAccessPointsResponse.to_json)
           (Some Values.DescribeAccessPointsResponse.error_to_json)])
let describe_account_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_preferences
           (Values.DescribeAccountPreferencesRequest.make ?nextToken
              ?maxResults ())
           (Some Values.DescribeAccountPreferencesResponse.to_json)
           (Some Values.DescribeAccountPreferencesResponse.error_to_json)])
let describe_backup_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_backup_policy
           (Values.DescribeBackupPolicyRequest.make ~fileSystemId ())
           (Some Values.BackupPolicyDescription.to_json)
           (Some Values.BackupPolicyDescription.error_to_json)])
let describe_file_system_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_file_system_policy
           (Values.DescribeFileSystemPolicyRequest.make ~fileSystemId ())
           (Some Values.FileSystemPolicyDescription.to_json)
           (Some Values.FileSystemPolicyDescription.error_to_json)])
let describe_file_systems =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxItems"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and creationToken =
         flag "creation-token" (optional string) ~doc:"STRING CreationToken"
       and fileSystemId =
         flag "file-system-id" (optional string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_file_systems
           (Values.DescribeFileSystemsRequest.make ?maxItems ?marker
              ?creationToken ?fileSystemId ())
           (Some Values.DescribeFileSystemsResponse.to_json)
           (Some Values.DescribeFileSystemsResponse.error_to_json)])
let describe_lifecycle_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_lifecycle_configuration
           (Values.DescribeLifecycleConfigurationRequest.make ~fileSystemId
              ()) (Some Values.LifecycleConfigurationDescription.to_json)
           (Some Values.LifecycleConfigurationDescription.error_to_json)])
let describe_mount_target_security_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mountTargetId =
         flag "mount-target-id" (required string) ~doc:"STRING MountTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_mount_target_security_groups
           (Values.DescribeMountTargetSecurityGroupsRequest.make
              ~mountTargetId ())
           (Some Values.DescribeMountTargetSecurityGroupsResponse.to_json)
           (Some
              Values.DescribeMountTargetSecurityGroupsResponse.error_to_json)])
let describe_mount_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxItems"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and fileSystemId =
         flag "file-system-id" (optional string) ~doc:"STRING FileSystemId"
       and mountTargetId =
         flag "mount-target-id" (optional string) ~doc:"STRING MountTargetId"
       and accessPointId =
         flag "access-point-id" (optional string) ~doc:"STRING AccessPointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_mount_targets
           (Values.DescribeMountTargetsRequest.make ?maxItems ?marker
              ?fileSystemId ?mountTargetId ?accessPointId ())
           (Some Values.DescribeMountTargetsResponse.to_json)
           (Some Values.DescribeMountTargetsResponse.error_to_json)])
let describe_replication_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (optional string) ~doc:"STRING FileSystemId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_replication_configurations
           (Values.DescribeReplicationConfigurationsRequest.make
              ?fileSystemId ?nextToken ?maxResults ())
           (Some Values.DescribeReplicationConfigurationsResponse.to_json)
           (Some
              Values.DescribeReplicationConfigurationsResponse.error_to_json)])
let describe_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxItems"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_tags
           (Values.DescribeTagsRequest.make ?maxItems ?marker ~fileSystemId
              ()) (Some Values.DescribeTagsResponse.to_json)
           (Some Values.DescribeTagsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?maxResults ?nextToken
              ~resourceId ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let modify_mount_target_security_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityGroups =
         flag "security-groups" (optional json_arg)
           ~doc:"JSON SecurityGroups"
       and mountTargetId =
         flag "mount-target-id" (required string) ~doc:"STRING MountTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_mount_target_security_groups
           (Values.ModifyMountTargetSecurityGroupsRequest.make
              ?securityGroups:(Option.map ~f:Values.SecurityGroups.of_json
                                 securityGroups) ~mountTargetId ()) None None])
let put_account_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdType =
         flag "resource-id-type" (required json_arg)
           ~doc:"JSON ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_account_preferences
           (Values.PutAccountPreferencesRequest.make
              ~resourceIdType:(Values.ResourceIdType.of_json resourceIdType)
              ()) (Some Values.PutAccountPreferencesResponse.to_json)
           (Some Values.PutAccountPreferencesResponse.error_to_json)])
let put_backup_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId"
       and backupPolicy =
         flag "backup-policy" (required json_arg) ~doc:"JSON BackupPolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_backup_policy
           (Values.PutBackupPolicyRequest.make ~fileSystemId
              ~backupPolicy:(Values.BackupPolicy.of_json backupPolicy) ())
           (Some Values.BackupPolicyDescription.to_json)
           (Some Values.BackupPolicyDescription.error_to_json)])
let put_file_system_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bypassPolicyLockoutSafetyCheck =
         flag "bypass-policy-lockout-safety-check" (optional bool)
           ~doc:"BOOL BypassPolicyLockoutSafetyCheck"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId"
       and policy = flag "policy" (required string) ~doc:"STRING Policy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_file_system_policy
           (Values.PutFileSystemPolicyRequest.make
              ?bypassPolicyLockoutSafetyCheck ~fileSystemId ~policy ())
           (Some Values.FileSystemPolicyDescription.to_json)
           (Some Values.FileSystemPolicyDescription.error_to_json)])
let put_lifecycle_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId"
       and lifecyclePolicies =
         flag "lifecycle-policies" (required json_arg)
           ~doc:"JSON LifecyclePolicies" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_lifecycle_configuration
           (Values.PutLifecycleConfigurationRequest.make ~fileSystemId
              ~lifecyclePolicies:(Values.LifecyclePolicies.of_json
                                    lifecyclePolicies) ())
           (Some Values.LifecycleConfigurationDescription.to_json)
           (Some Values.LifecycleConfigurationDescription.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceId
              ~tags:(Values.Tags.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceId
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ()) None None])
let update_file_system =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and throughputMode =
         flag "throughput-mode" (optional json_arg)
           ~doc:"JSON ThroughputMode"
       and provisionedThroughputInMibps =
         flag "provisioned-throughput-in-mibps" (optional float)
           ~doc:"FLOAT ProvisionedThroughputInMibps"
       and fileSystemId =
         flag "file-system-id" (required string) ~doc:"STRING FileSystemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_file_system
           (Values.UpdateFileSystemRequest.make
              ?throughputMode:(Option.map ~f:Values.ThroughputMode.of_json
                                 throughputMode)
              ?provisionedThroughputInMibps ~fileSystemId ())
           (Some Values.FileSystemDescription.to_json)
           (Some Values.FileSystemDescription.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-access-point", create_access_point);
    ("create-file-system", create_file_system);
    ("create-mount-target", create_mount_target);
    ("create-replication-configuration", create_replication_configuration);
    ("create-tags", create_tags);
    ("delete-access-point", delete_access_point);
    ("delete-file-system", delete_file_system);
    ("delete-file-system-policy", delete_file_system_policy);
    ("delete-mount-target", delete_mount_target);
    ("delete-replication-configuration", delete_replication_configuration);
    ("delete-tags", delete_tags);
    ("describe-access-points", describe_access_points);
    ("describe-account-preferences", describe_account_preferences);
    ("describe-backup-policy", describe_backup_policy);
    ("describe-file-system-policy", describe_file_system_policy);
    ("describe-file-systems", describe_file_systems);
    ("describe-lifecycle-configuration", describe_lifecycle_configuration);
    ("describe-mount-target-security-groups",
      describe_mount_target_security_groups);
    ("describe-mount-targets", describe_mount_targets);
    ("describe-replication-configurations",
      describe_replication_configurations);
    ("describe-tags", describe_tags);
    ("list-tags-for-resource", list_tags_for_resource);
    ("modify-mount-target-security-groups",
      modify_mount_target_security_groups);
    ("put-account-preferences", put_account_preferences);
    ("put-backup-policy", put_backup_policy);
    ("put-file-system-policy", put_file_system_policy);
    ("put-lifecycle-configuration", put_lifecycle_configuration);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-file-system", update_file_system)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_eulas =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and eulaIds =
         flag "eula-ids" (optional json_arg) ~doc:"JSON EulaIdList"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_eulas
           (Values.AcceptEulasRequest.make ?clientToken
              ?eulaIds:(Option.map ~f:Values.EulaIdList.of_json eulaIds)
              ~studioId ()) (Some Values.AcceptEulasResponse.to_json)
           (Some Values.AcceptEulasResponse.error_to_json)])
let create_launch_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING LaunchProfileDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and ec2SubnetIds =
         flag "ec2-subnet-ids" (required json_arg)
           ~doc:"JSON EC2SubnetIdList"
       and launchProfileProtocolVersions =
         flag "launch-profile-protocol-versions" (required json_arg)
           ~doc:"JSON LaunchProfileProtocolVersionList"
       and name =
         flag "name" (required string) ~doc:"STRING LaunchProfileName"
       and streamConfiguration =
         flag "stream-configuration" (required json_arg)
           ~doc:"JSON StreamConfigurationCreate"
       and studioComponentIds =
         flag "studio-component-ids" (required json_arg)
           ~doc:"JSON LaunchProfileStudioComponentIdList"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_launch_profile
           (Values.CreateLaunchProfileRequest.make ?clientToken ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~ec2SubnetIds:(Values.EC2SubnetIdList.of_json ec2SubnetIds)
              ~launchProfileProtocolVersions:(Values.LaunchProfileProtocolVersionList.of_json
                                                launchProfileProtocolVersions)
              ~name
              ~streamConfiguration:(Values.StreamConfigurationCreate.of_json
                                      streamConfiguration)
              ~studioComponentIds:(Values.LaunchProfileStudioComponentIdList.of_json
                                     studioComponentIds) ~studioId ())
           (Some Values.CreateLaunchProfileResponse.to_json)
           (Some Values.CreateLaunchProfileResponse.error_to_json)])
let create_streaming_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING SyntheticCreateStreamingImageRequestStreamingImageDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and ec2ImageId =
         flag "ec2-image-id" (required string) ~doc:"STRING EC2ImageId"
       and name =
         flag "name" (required string)
           ~doc:"STRING SyntheticCreateStreamingImageRequestStreamingImageName"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_streaming_image
           (Values.CreateStreamingImageRequest.make ?clientToken ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~ec2ImageId
              ~name ~studioId ())
           (Some Values.CreateStreamingImageResponse.to_json)
           (Some Values.CreateStreamingImageResponse.error_to_json)])
let create_streaming_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and ec2InstanceType =
         flag "ec2-instance-type" (optional json_arg)
           ~doc:"JSON StreamingInstanceType"
       and launchProfileId =
         flag "launch-profile-id" (optional string) ~doc:"STRING String"
       and ownedBy = flag "owned-by" (optional string) ~doc:"STRING String"
       and streamingImageId =
         flag "streaming-image-id" (optional string)
           ~doc:"STRING StreamingImageId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_streaming_session
           (Values.CreateStreamingSessionRequest.make ?clientToken
              ?ec2InstanceType:(Option.map
                                  ~f:Values.StreamingInstanceType.of_json
                                  ec2InstanceType) ?launchProfileId ?ownedBy
              ?streamingImageId
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~studioId ())
           (Some Values.CreateStreamingSessionResponse.to_json)
           (Some Values.CreateStreamingSessionResponse.error_to_json)])
let create_streaming_session_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and expirationInSeconds =
         flag "expiration-in-seconds" (optional int)
           ~doc:"INT StreamingSessionStreamExpirationInSeconds"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_streaming_session_stream
           (Values.CreateStreamingSessionStreamRequest.make ?clientToken
              ?expirationInSeconds ~sessionId ~studioId ())
           (Some Values.CreateStreamingSessionStreamResponse.to_json)
           (Some Values.CreateStreamingSessionStreamResponse.error_to_json)])
let create_studio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and studioEncryptionConfiguration =
         flag "studio-encryption-configuration" (optional json_arg)
           ~doc:"JSON StudioEncryptionConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and adminRoleArn =
         flag "admin-role-arn" (required string) ~doc:"STRING String"
       and displayName =
         flag "display-name" (required string)
           ~doc:"STRING SyntheticCreateStudioRequestStudioDisplayName"
       and studioName =
         flag "studio-name" (required string) ~doc:"STRING StudioName"
       and userRoleArn =
         flag "user-role-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_studio
           (Values.CreateStudioRequest.make ?clientToken
              ?studioEncryptionConfiguration:(Option.map
                                                ~f:Values.StudioEncryptionConfiguration.of_json
                                                studioEncryptionConfiguration)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~adminRoleArn
              ~displayName ~studioName ~userRoleArn ())
           (Some Values.CreateStudioResponse.to_json)
           (Some Values.CreateStudioResponse.error_to_json)])
let create_studio_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON StudioComponentConfiguration"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StudioComponentDescription"
       and ec2SecurityGroupIds =
         flag "ec2-security-group-ids" (optional json_arg)
           ~doc:"JSON StudioComponentSecurityGroupIdList"
       and initializationScripts =
         flag "initialization-scripts" (optional json_arg)
           ~doc:"JSON StudioComponentInitializationScriptList"
       and scriptParameters =
         flag "script-parameters" (optional json_arg)
           ~doc:"JSON StudioComponentScriptParameterKeyValueList"
       and subtype =
         flag "subtype" (optional json_arg)
           ~doc:"JSON StudioComponentSubtype"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name =
         flag "name" (required string) ~doc:"STRING StudioComponentName"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON StudioComponentType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_studio_component
           (Values.CreateStudioComponentRequest.make ?clientToken
              ?configuration:(Option.map
                                ~f:Values.StudioComponentConfiguration.of_json
                                configuration) ?description
              ?ec2SecurityGroupIds:(Option.map
                                      ~f:Values.StudioComponentSecurityGroupIdList.of_json
                                      ec2SecurityGroupIds)
              ?initializationScripts:(Option.map
                                        ~f:Values.StudioComponentInitializationScriptList.of_json
                                        initializationScripts)
              ?scriptParameters:(Option.map
                                   ~f:Values.StudioComponentScriptParameterKeyValueList.of_json
                                   scriptParameters)
              ?subtype:(Option.map ~f:Values.StudioComponentSubtype.of_json
                          subtype)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name ~studioId
              ~type_:(Values.StudioComponentType.of_json type_) ())
           (Some Values.CreateStudioComponentResponse.to_json)
           (Some Values.CreateStudioComponentResponse.error_to_json)])
let delete_launch_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and launchProfileId =
         flag "launch-profile-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_launch_profile
           (Values.DeleteLaunchProfileRequest.make ?clientToken
              ~launchProfileId ~studioId ())
           (Some Values.DeleteLaunchProfileResponse.to_json)
           (Some Values.DeleteLaunchProfileResponse.error_to_json)])
let delete_launch_profile_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and launchProfileId =
         flag "launch-profile-id" (required string) ~doc:"STRING String"
       and principalId =
         flag "principal-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_launch_profile_member
           (Values.DeleteLaunchProfileMemberRequest.make ?clientToken
              ~launchProfileId ~principalId ~studioId ())
           (Some Values.DeleteLaunchProfileMemberResponse.to_json)
           (Some Values.DeleteLaunchProfileMemberResponse.error_to_json)])
let delete_streaming_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and streamingImageId =
         flag "streaming-image-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_streaming_image
           (Values.DeleteStreamingImageRequest.make ?clientToken
              ~streamingImageId ~studioId ())
           (Some Values.DeleteStreamingImageResponse.to_json)
           (Some Values.DeleteStreamingImageResponse.error_to_json)])
let delete_streaming_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_streaming_session
           (Values.DeleteStreamingSessionRequest.make ?clientToken ~sessionId
              ~studioId ())
           (Some Values.DeleteStreamingSessionResponse.to_json)
           (Some Values.DeleteStreamingSessionResponse.error_to_json)])
let delete_studio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_studio
           (Values.DeleteStudioRequest.make ?clientToken ~studioId ())
           (Some Values.DeleteStudioResponse.to_json)
           (Some Values.DeleteStudioResponse.error_to_json)])
let delete_studio_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and studioComponentId =
         flag "studio-component-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_studio_component
           (Values.DeleteStudioComponentRequest.make ?clientToken
              ~studioComponentId ~studioId ())
           (Some Values.DeleteStudioComponentResponse.to_json)
           (Some Values.DeleteStudioComponentResponse.error_to_json)])
let delete_studio_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and principalId =
         flag "principal-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_studio_member
           (Values.DeleteStudioMemberRequest.make ?clientToken ~principalId
              ~studioId ()) (Some Values.DeleteStudioMemberResponse.to_json)
           (Some Values.DeleteStudioMemberResponse.error_to_json)])
let get_eula =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eulaId = flag "eula-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_eula (Values.GetEulaRequest.make ~eulaId ())
           (Some Values.GetEulaResponse.to_json)
           (Some Values.GetEulaResponse.error_to_json)])
let get_launch_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and launchProfileId =
         flag "launch-profile-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_launch_profile
           (Values.GetLaunchProfileRequest.make ~launchProfileId ~studioId ())
           (Some Values.GetLaunchProfileResponse.to_json)
           (Some Values.GetLaunchProfileResponse.error_to_json)])
let get_launch_profile_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and launchProfileId =
         flag "launch-profile-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_launch_profile_details
           (Values.GetLaunchProfileDetailsRequest.make ~launchProfileId
              ~studioId ())
           (Some Values.GetLaunchProfileDetailsResponse.to_json)
           (Some Values.GetLaunchProfileDetailsResponse.error_to_json)])
let get_launch_profile_initialization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and launchProfileId =
         flag "launch-profile-id" (required string) ~doc:"STRING String"
       and launchProfileProtocolVersions =
         flag "launch-profile-protocol-versions" (required json_arg)
           ~doc:"JSON StringList"
       and launchPurpose =
         flag "launch-purpose" (required string) ~doc:"STRING String"
       and platform = flag "platform" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_launch_profile_initialization
           (Values.GetLaunchProfileInitializationRequest.make
              ~launchProfileId
              ~launchProfileProtocolVersions:(Values.StringList.of_json
                                                launchProfileProtocolVersions)
              ~launchPurpose ~platform ~studioId ())
           (Some Values.GetLaunchProfileInitializationResponse.to_json)
           (Some Values.GetLaunchProfileInitializationResponse.error_to_json)])
let get_launch_profile_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and launchProfileId =
         flag "launch-profile-id" (required string) ~doc:"STRING String"
       and principalId =
         flag "principal-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_launch_profile_member
           (Values.GetLaunchProfileMemberRequest.make ~launchProfileId
              ~principalId ~studioId ())
           (Some Values.GetLaunchProfileMemberResponse.to_json)
           (Some Values.GetLaunchProfileMemberResponse.error_to_json)])
let get_streaming_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and streamingImageId =
         flag "streaming-image-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_streaming_image
           (Values.GetStreamingImageRequest.make ~streamingImageId ~studioId
              ()) (Some Values.GetStreamingImageResponse.to_json)
           (Some Values.GetStreamingImageResponse.error_to_json)])
let get_streaming_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_streaming_session
           (Values.GetStreamingSessionRequest.make ~sessionId ~studioId ())
           (Some Values.GetStreamingSessionResponse.to_json)
           (Some Values.GetStreamingSessionResponse.error_to_json)])
let get_streaming_session_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING String"
       and streamId = flag "stream-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_streaming_session_stream
           (Values.GetStreamingSessionStreamRequest.make ~sessionId ~streamId
              ~studioId ())
           (Some Values.GetStreamingSessionStreamResponse.to_json)
           (Some Values.GetStreamingSessionStreamResponse.error_to_json)])
let get_studio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_studio (Values.GetStudioRequest.make ~studioId ())
           (Some Values.GetStudioResponse.to_json)
           (Some Values.GetStudioResponse.error_to_json)])
let get_studio_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and studioComponentId =
         flag "studio-component-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_studio_component
           (Values.GetStudioComponentRequest.make ~studioComponentId
              ~studioId ()) (Some Values.GetStudioComponentResponse.to_json)
           (Some Values.GetStudioComponentResponse.error_to_json)])
let get_studio_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and principalId =
         flag "principal-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_studio_member
           (Values.GetStudioMemberRequest.make ~principalId ~studioId ())
           (Some Values.GetStudioMemberResponse.to_json)
           (Some Values.GetStudioMemberResponse.error_to_json)])
let list_eula_acceptances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eulaIds =
         flag "eula-ids" (optional json_arg) ~doc:"JSON StringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_eula_acceptances
           (Values.ListEulaAcceptancesRequest.make
              ?eulaIds:(Option.map ~f:Values.StringList.of_json eulaIds)
              ?nextToken ~studioId ())
           (Some Values.ListEulaAcceptancesResponse.to_json)
           (Some Values.ListEulaAcceptancesResponse.error_to_json)])
let list_eulas =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eulaIds =
         flag "eula-ids" (optional json_arg) ~doc:"JSON StringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_eulas
           (Values.ListEulasRequest.make
              ?eulaIds:(Option.map ~f:Values.StringList.of_json eulaIds)
              ?nextToken ()) (Some Values.ListEulasResponse.to_json)
           (Some Values.ListEulasResponse.error_to_json)])
let list_launch_profile_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and launchProfileId =
         flag "launch-profile-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_launch_profile_members
           (Values.ListLaunchProfileMembersRequest.make ?maxResults
              ?nextToken ~launchProfileId ~studioId ())
           (Some Values.ListLaunchProfileMembersResponse.to_json)
           (Some Values.ListLaunchProfileMembersResponse.error_to_json)])
let list_launch_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and principalId =
         flag "principal-id" (optional string) ~doc:"STRING String"
       and states =
         flag "states" (optional json_arg) ~doc:"JSON LaunchProfileStateList"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_launch_profiles
           (Values.ListLaunchProfilesRequest.make ?maxResults ?nextToken
              ?principalId
              ?states:(Option.map ~f:Values.LaunchProfileStateList.of_json
                         states) ~studioId ())
           (Some Values.ListLaunchProfilesResponse.to_json)
           (Some Values.ListLaunchProfilesResponse.error_to_json)])
let list_streaming_images =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and owner = flag "owner" (optional string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_streaming_images
           (Values.ListStreamingImagesRequest.make ?nextToken ?owner
              ~studioId ()) (Some Values.ListStreamingImagesResponse.to_json)
           (Some Values.ListStreamingImagesResponse.error_to_json)])
let list_streaming_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and createdBy =
         flag "created-by" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and ownedBy = flag "owned-by" (optional string) ~doc:"STRING String"
       and sessionIds =
         flag "session-ids" (optional string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_streaming_sessions
           (Values.ListStreamingSessionsRequest.make ?createdBy ?nextToken
              ?ownedBy ?sessionIds ~studioId ())
           (Some Values.ListStreamingSessionsResponse.to_json)
           (Some Values.ListStreamingSessionsResponse.error_to_json)])
let list_studio_components =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and states =
         flag "states" (optional json_arg)
           ~doc:"JSON StudioComponentStateList"
       and types =
         flag "types" (optional json_arg) ~doc:"JSON StudioComponentTypeList"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_studio_components
           (Values.ListStudioComponentsRequest.make ?maxResults ?nextToken
              ?states:(Option.map ~f:Values.StudioComponentStateList.of_json
                         states)
              ?types:(Option.map ~f:Values.StudioComponentTypeList.of_json
                        types) ~studioId ())
           (Some Values.ListStudioComponentsResponse.to_json)
           (Some Values.ListStudioComponentsResponse.error_to_json)])
let list_studio_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_studio_members
           (Values.ListStudioMembersRequest.make ?maxResults ?nextToken
              ~studioId ()) (Some Values.ListStudioMembersResponse.to_json)
           (Some Values.ListStudioMembersResponse.error_to_json)])
let list_studios =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_studios (Values.ListStudiosRequest.make ?nextToken ())
           (Some Values.ListStudiosResponse.to_json)
           (Some Values.ListStudiosResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_launch_profile_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and identityStoreId =
         flag "identity-store-id" (required string) ~doc:"STRING String"
       and launchProfileId =
         flag "launch-profile-id" (required string) ~doc:"STRING String"
       and members =
         flag "members" (required json_arg)
           ~doc:"JSON NewLaunchProfileMemberList"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_launch_profile_members
           (Values.PutLaunchProfileMembersRequest.make ?clientToken
              ~identityStoreId ~launchProfileId
              ~members:(Values.NewLaunchProfileMemberList.of_json members)
              ~studioId ())
           (Some Values.PutLaunchProfileMembersResponse.to_json)
           (Some Values.PutLaunchProfileMembersResponse.error_to_json)])
let put_studio_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and identityStoreId =
         flag "identity-store-id" (required string) ~doc:"STRING String"
       and members =
         flag "members" (required json_arg) ~doc:"JSON NewStudioMemberList"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_studio_members
           (Values.PutStudioMembersRequest.make ?clientToken ~identityStoreId
              ~members:(Values.NewStudioMemberList.of_json members) ~studioId
              ()) (Some Values.PutStudioMembersResponse.to_json)
           (Some Values.PutStudioMembersResponse.error_to_json)])
let start_streaming_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_streaming_session
           (Values.StartStreamingSessionRequest.make ?clientToken ~sessionId
              ~studioId ())
           (Some Values.StartStreamingSessionResponse.to_json)
           (Some Values.StartStreamingSessionResponse.error_to_json)])
let start_studio_s_s_o_configuration_repair =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_studio_s_s_o_configuration_repair
           (Values.StartStudioSSOConfigurationRepairRequest.make ?clientToken
              ~studioId ())
           (Some Values.StartStudioSSOConfigurationRepairResponse.to_json)
           (Some
              Values.StartStudioSSOConfigurationRepairResponse.error_to_json)])
let stop_streaming_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_streaming_session
           (Values.StopStreamingSessionRequest.make ?clientToken ~sessionId
              ~studioId ())
           (Some Values.StopStreamingSessionResponse.to_json)
           (Some Values.StopStreamingSessionResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~resourceArn ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.StringList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_launch_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING LaunchProfileDescription"
       and launchProfileProtocolVersions =
         flag "launch-profile-protocol-versions" (optional json_arg)
           ~doc:"JSON LaunchProfileProtocolVersionList"
       and name =
         flag "name" (optional string) ~doc:"STRING LaunchProfileName"
       and streamConfiguration =
         flag "stream-configuration" (optional json_arg)
           ~doc:"JSON StreamConfigurationCreate"
       and studioComponentIds =
         flag "studio-component-ids" (optional json_arg)
           ~doc:"JSON LaunchProfileStudioComponentIdList"
       and launchProfileId =
         flag "launch-profile-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_launch_profile
           (Values.UpdateLaunchProfileRequest.make ?clientToken ?description
              ?launchProfileProtocolVersions:(Option.map
                                                ~f:Values.LaunchProfileProtocolVersionList.of_json
                                                launchProfileProtocolVersions)
              ?name
              ?streamConfiguration:(Option.map
                                      ~f:Values.StreamConfigurationCreate.of_json
                                      streamConfiguration)
              ?studioComponentIds:(Option.map
                                     ~f:Values.LaunchProfileStudioComponentIdList.of_json
                                     studioComponentIds) ~launchProfileId
              ~studioId ()) (Some Values.UpdateLaunchProfileResponse.to_json)
           (Some Values.UpdateLaunchProfileResponse.error_to_json)])
let update_launch_profile_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and launchProfileId =
         flag "launch-profile-id" (required string) ~doc:"STRING String"
       and persona =
         flag "persona" (required json_arg) ~doc:"JSON LaunchProfilePersona"
       and principalId =
         flag "principal-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_launch_profile_member
           (Values.UpdateLaunchProfileMemberRequest.make ?clientToken
              ~launchProfileId
              ~persona:(Values.LaunchProfilePersona.of_json persona)
              ~principalId ~studioId ())
           (Some Values.UpdateLaunchProfileMemberResponse.to_json)
           (Some Values.UpdateLaunchProfileMemberResponse.error_to_json)])
let update_streaming_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING SyntheticUpdateStreamingImageRequestStreamingImageDescription"
       and name =
         flag "name" (optional string)
           ~doc:"STRING SyntheticUpdateStreamingImageRequestStreamingImageName"
       and streamingImageId =
         flag "streaming-image-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_streaming_image
           (Values.UpdateStreamingImageRequest.make ?clientToken ?description
              ?name ~streamingImageId ~studioId ())
           (Some Values.UpdateStreamingImageResponse.to_json)
           (Some Values.UpdateStreamingImageResponse.error_to_json)])
let update_studio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and adminRoleArn =
         flag "admin-role-arn" (optional string) ~doc:"STRING String"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING SyntheticUpdateStudioRequestStudioDisplayName"
       and userRoleArn =
         flag "user-role-arn" (optional string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_studio
           (Values.UpdateStudioRequest.make ?adminRoleArn ?clientToken
              ?displayName ?userRoleArn ~studioId ())
           (Some Values.UpdateStudioResponse.to_json)
           (Some Values.UpdateStudioResponse.error_to_json)])
let update_studio_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON StudioComponentConfiguration"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StudioComponentDescription"
       and ec2SecurityGroupIds =
         flag "ec2-security-group-ids" (optional json_arg)
           ~doc:"JSON StudioComponentSecurityGroupIdList"
       and initializationScripts =
         flag "initialization-scripts" (optional json_arg)
           ~doc:"JSON StudioComponentInitializationScriptList"
       and name =
         flag "name" (optional string) ~doc:"STRING StudioComponentName"
       and scriptParameters =
         flag "script-parameters" (optional json_arg)
           ~doc:"JSON StudioComponentScriptParameterKeyValueList"
       and subtype =
         flag "subtype" (optional json_arg)
           ~doc:"JSON StudioComponentSubtype"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON StudioComponentType"
       and studioComponentId =
         flag "studio-component-id" (required string) ~doc:"STRING String"
       and studioId = flag "studio-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_studio_component
           (Values.UpdateStudioComponentRequest.make ?clientToken
              ?configuration:(Option.map
                                ~f:Values.StudioComponentConfiguration.of_json
                                configuration) ?description
              ?ec2SecurityGroupIds:(Option.map
                                      ~f:Values.StudioComponentSecurityGroupIdList.of_json
                                      ec2SecurityGroupIds)
              ?initializationScripts:(Option.map
                                        ~f:Values.StudioComponentInitializationScriptList.of_json
                                        initializationScripts) ?name
              ?scriptParameters:(Option.map
                                   ~f:Values.StudioComponentScriptParameterKeyValueList.of_json
                                   scriptParameters)
              ?subtype:(Option.map ~f:Values.StudioComponentSubtype.of_json
                          subtype)
              ?type_:(Option.map ~f:Values.StudioComponentType.of_json type_)
              ~studioComponentId ~studioId ())
           (Some Values.UpdateStudioComponentResponse.to_json)
           (Some Values.UpdateStudioComponentResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("accept-eulas", accept_eulas);
    ("create-launch-profile", create_launch_profile);
    ("create-streaming-image", create_streaming_image);
    ("create-streaming-session", create_streaming_session);
    ("create-streaming-session-stream", create_streaming_session_stream);
    ("create-studio", create_studio);
    ("create-studio-component", create_studio_component);
    ("delete-launch-profile", delete_launch_profile);
    ("delete-launch-profile-member", delete_launch_profile_member);
    ("delete-streaming-image", delete_streaming_image);
    ("delete-streaming-session", delete_streaming_session);
    ("delete-studio", delete_studio);
    ("delete-studio-component", delete_studio_component);
    ("delete-studio-member", delete_studio_member);
    ("get-eula", get_eula);
    ("get-launch-profile", get_launch_profile);
    ("get-launch-profile-details", get_launch_profile_details);
    ("get-launch-profile-initialization", get_launch_profile_initialization);
    ("get-launch-profile-member", get_launch_profile_member);
    ("get-streaming-image", get_streaming_image);
    ("get-streaming-session", get_streaming_session);
    ("get-streaming-session-stream", get_streaming_session_stream);
    ("get-studio", get_studio);
    ("get-studio-component", get_studio_component);
    ("get-studio-member", get_studio_member);
    ("list-eula-acceptances", list_eula_acceptances);
    ("list-eulas", list_eulas);
    ("list-launch-profile-members", list_launch_profile_members);
    ("list-launch-profiles", list_launch_profiles);
    ("list-streaming-images", list_streaming_images);
    ("list-streaming-sessions", list_streaming_sessions);
    ("list-studio-components", list_studio_components);
    ("list-studio-members", list_studio_members);
    ("list-studios", list_studios);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-launch-profile-members", put_launch_profile_members);
    ("put-studio-members", put_studio_members);
    ("start-streaming-session", start_streaming_session);
    ("start-studio-s-s-o-configuration-repair",
      start_studio_s_s_o_configuration_repair);
    ("stop-streaming-session", stop_streaming_session);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-launch-profile", update_launch_profile);
    ("update-launch-profile-member", update_launch_profile_member);
    ("update-streaming-image", update_streaming_image);
    ("update-studio", update_studio);
    ("update-studio-component", update_studio_component)]
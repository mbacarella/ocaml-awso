(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_budget =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notificationsWithSubscribers =
         flag "notifications-with-subscribers" (optional json_arg)
           ~doc:"JSON NotificationWithSubscribersList"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budget = flag "budget" (required json_arg) ~doc:"JSON Budget" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_budget
           (Values.CreateBudgetRequest.make
              ?notificationsWithSubscribers:(Option.map
                                               ~f:Values.NotificationWithSubscribersList.of_json
                                               notificationsWithSubscribers)
              ~accountId ~budget:(Values.Budget.of_json budget) ())
           (Some Values.CreateBudgetResponse.to_json)
           (Some Values.CreateBudgetResponse.error_to_json)])
let create_budget_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and notificationType =
         flag "notification-type" (required json_arg)
           ~doc:"JSON NotificationType"
       and actionType =
         flag "action-type" (required json_arg) ~doc:"JSON ActionType"
       and actionThreshold =
         flag "action-threshold" (required json_arg)
           ~doc:"JSON ActionThreshold"
       and definition =
         flag "definition" (required json_arg) ~doc:"JSON Definition"
       and executionRoleArn =
         flag "execution-role-arn" (required string) ~doc:"STRING RoleArn"
       and approvalModel =
         flag "approval-model" (required json_arg) ~doc:"JSON ApprovalModel"
       and subscribers =
         flag "subscribers" (required json_arg) ~doc:"JSON Subscribers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_budget_action
           (Values.CreateBudgetActionRequest.make ~accountId ~budgetName
              ~notificationType:(Values.NotificationType.of_json
                                   notificationType)
              ~actionType:(Values.ActionType.of_json actionType)
              ~actionThreshold:(Values.ActionThreshold.of_json
                                  actionThreshold)
              ~definition:(Values.Definition.of_json definition)
              ~executionRoleArn
              ~approvalModel:(Values.ApprovalModel.of_json approvalModel)
              ~subscribers:(Values.Subscribers.of_json subscribers) ())
           (Some Values.CreateBudgetActionResponse.to_json)
           (Some Values.CreateBudgetActionResponse.error_to_json)])
let create_notification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and notification =
         flag "notification" (required json_arg) ~doc:"JSON Notification"
       and subscribers =
         flag "subscribers" (required json_arg) ~doc:"JSON Subscribers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_notification
           (Values.CreateNotificationRequest.make ~accountId ~budgetName
              ~notification:(Values.Notification.of_json notification)
              ~subscribers:(Values.Subscribers.of_json subscribers) ())
           (Some Values.CreateNotificationResponse.to_json)
           (Some Values.CreateNotificationResponse.error_to_json)])
let create_subscriber =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and notification =
         flag "notification" (required json_arg) ~doc:"JSON Notification"
       and subscriber =
         flag "subscriber" (required json_arg) ~doc:"JSON Subscriber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subscriber
           (Values.CreateSubscriberRequest.make ~accountId ~budgetName
              ~notification:(Values.Notification.of_json notification)
              ~subscriber:(Values.Subscriber.of_json subscriber) ())
           (Some Values.CreateSubscriberResponse.to_json)
           (Some Values.CreateSubscriberResponse.error_to_json)])
let delete_budget =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_budget
           (Values.DeleteBudgetRequest.make ~accountId ~budgetName ())
           (Some Values.DeleteBudgetResponse.to_json)
           (Some Values.DeleteBudgetResponse.error_to_json)])
let delete_budget_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and actionId =
         flag "action-id" (required string) ~doc:"STRING ActionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_budget_action
           (Values.DeleteBudgetActionRequest.make ~accountId ~budgetName
              ~actionId ()) (Some Values.DeleteBudgetActionResponse.to_json)
           (Some Values.DeleteBudgetActionResponse.error_to_json)])
let delete_notification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and notification =
         flag "notification" (required json_arg) ~doc:"JSON Notification" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_notification
           (Values.DeleteNotificationRequest.make ~accountId ~budgetName
              ~notification:(Values.Notification.of_json notification) ())
           (Some Values.DeleteNotificationResponse.to_json)
           (Some Values.DeleteNotificationResponse.error_to_json)])
let delete_subscriber =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and notification =
         flag "notification" (required json_arg) ~doc:"JSON Notification"
       and subscriber =
         flag "subscriber" (required json_arg) ~doc:"JSON Subscriber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_subscriber
           (Values.DeleteSubscriberRequest.make ~accountId ~budgetName
              ~notification:(Values.Notification.of_json notification)
              ~subscriber:(Values.Subscriber.of_json subscriber) ())
           (Some Values.DeleteSubscriberResponse.to_json)
           (Some Values.DeleteSubscriberResponse.error_to_json)])
let describe_budget =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_budget
           (Values.DescribeBudgetRequest.make ~accountId ~budgetName ())
           (Some Values.DescribeBudgetResponse.to_json)
           (Some Values.DescribeBudgetResponse.error_to_json)])
let describe_budget_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and actionId =
         flag "action-id" (required string) ~doc:"STRING ActionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_budget_action
           (Values.DescribeBudgetActionRequest.make ~accountId ~budgetName
              ~actionId ())
           (Some Values.DescribeBudgetActionResponse.to_json)
           (Some Values.DescribeBudgetActionResponse.error_to_json)])
let describe_budget_action_histories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timePeriod =
         flag "time-period" (optional json_arg) ~doc:"JSON TimePeriod"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and actionId =
         flag "action-id" (required string) ~doc:"STRING ActionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_budget_action_histories
           (Values.DescribeBudgetActionHistoriesRequest.make
              ?timePeriod:(Option.map ~f:Values.TimePeriod.of_json timePeriod)
              ?maxResults ?nextToken ~accountId ~budgetName ~actionId ())
           (Some Values.DescribeBudgetActionHistoriesResponse.to_json)
           (Some Values.DescribeBudgetActionHistoriesResponse.error_to_json)])
let describe_budget_actions_for_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_budget_actions_for_account
           (Values.DescribeBudgetActionsForAccountRequest.make ?maxResults
              ?nextToken ~accountId ())
           (Some Values.DescribeBudgetActionsForAccountResponse.to_json)
           (Some Values.DescribeBudgetActionsForAccountResponse.error_to_json)])
let describe_budget_actions_for_budget =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_budget_actions_for_budget
           (Values.DescribeBudgetActionsForBudgetRequest.make ?maxResults
              ?nextToken ~accountId ~budgetName ())
           (Some Values.DescribeBudgetActionsForBudgetResponse.to_json)
           (Some Values.DescribeBudgetActionsForBudgetResponse.error_to_json)])
let describe_budget_notifications_for_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsBudgetNotifications"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_budget_notifications_for_account
           (Values.DescribeBudgetNotificationsForAccountRequest.make
              ?maxResults ?nextToken ~accountId ())
           (Some Values.DescribeBudgetNotificationsForAccountResponse.to_json)
           (Some
              Values.DescribeBudgetNotificationsForAccountResponse.error_to_json)])
let describe_budget_performance_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timePeriod =
         flag "time-period" (optional json_arg) ~doc:"JSON TimePeriod"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_budget_performance_history
           (Values.DescribeBudgetPerformanceHistoryRequest.make
              ?timePeriod:(Option.map ~f:Values.TimePeriod.of_json timePeriod)
              ?maxResults ?nextToken ~accountId ~budgetName ())
           (Some Values.DescribeBudgetPerformanceHistoryResponse.to_json)
           (Some
              Values.DescribeBudgetPerformanceHistoryResponse.error_to_json)])
let describe_budgets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_budgets
           (Values.DescribeBudgetsRequest.make ?maxResults ?nextToken
              ~accountId ()) (Some Values.DescribeBudgetsResponse.to_json)
           (Some Values.DescribeBudgetsResponse.error_to_json)])
let describe_notifications_for_budget =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_notifications_for_budget
           (Values.DescribeNotificationsForBudgetRequest.make ?maxResults
              ?nextToken ~accountId ~budgetName ())
           (Some Values.DescribeNotificationsForBudgetResponse.to_json)
           (Some Values.DescribeNotificationsForBudgetResponse.error_to_json)])
let describe_subscribers_for_notification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and notification =
         flag "notification" (required json_arg) ~doc:"JSON Notification" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_subscribers_for_notification
           (Values.DescribeSubscribersForNotificationRequest.make ?maxResults
              ?nextToken ~accountId ~budgetName
              ~notification:(Values.Notification.of_json notification) ())
           (Some Values.DescribeSubscribersForNotificationResponse.to_json)
           (Some
              Values.DescribeSubscribersForNotificationResponse.error_to_json)])
let execute_budget_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and actionId =
         flag "action-id" (required string) ~doc:"STRING ActionId"
       and executionType =
         flag "execution-type" (required json_arg) ~doc:"JSON ExecutionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_budget_action
           (Values.ExecuteBudgetActionRequest.make ~accountId ~budgetName
              ~actionId
              ~executionType:(Values.ExecutionType.of_json executionType) ())
           (Some Values.ExecuteBudgetActionResponse.to_json)
           (Some Values.ExecuteBudgetActionResponse.error_to_json)])
let update_budget =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and newBudget =
         flag "new-budget" (required json_arg) ~doc:"JSON Budget" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_budget
           (Values.UpdateBudgetRequest.make ~accountId
              ~newBudget:(Values.Budget.of_json newBudget) ())
           (Some Values.UpdateBudgetResponse.to_json)
           (Some Values.UpdateBudgetResponse.error_to_json)])
let update_budget_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notificationType =
         flag "notification-type" (optional json_arg)
           ~doc:"JSON NotificationType"
       and actionThreshold =
         flag "action-threshold" (optional json_arg)
           ~doc:"JSON ActionThreshold"
       and definition =
         flag "definition" (optional json_arg) ~doc:"JSON Definition"
       and executionRoleArn =
         flag "execution-role-arn" (optional string) ~doc:"STRING RoleArn"
       and approvalModel =
         flag "approval-model" (optional json_arg) ~doc:"JSON ApprovalModel"
       and subscribers =
         flag "subscribers" (optional json_arg) ~doc:"JSON Subscribers"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and actionId =
         flag "action-id" (required string) ~doc:"STRING ActionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_budget_action
           (Values.UpdateBudgetActionRequest.make
              ?notificationType:(Option.map
                                   ~f:Values.NotificationType.of_json
                                   notificationType)
              ?actionThreshold:(Option.map ~f:Values.ActionThreshold.of_json
                                  actionThreshold)
              ?definition:(Option.map ~f:Values.Definition.of_json definition)
              ?executionRoleArn
              ?approvalModel:(Option.map ~f:Values.ApprovalModel.of_json
                                approvalModel)
              ?subscribers:(Option.map ~f:Values.Subscribers.of_json
                              subscribers) ~accountId ~budgetName ~actionId
              ()) (Some Values.UpdateBudgetActionResponse.to_json)
           (Some Values.UpdateBudgetActionResponse.error_to_json)])
let update_notification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and oldNotification =
         flag "old-notification" (required json_arg) ~doc:"JSON Notification"
       and newNotification =
         flag "new-notification" (required json_arg) ~doc:"JSON Notification" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_notification
           (Values.UpdateNotificationRequest.make ~accountId ~budgetName
              ~oldNotification:(Values.Notification.of_json oldNotification)
              ~newNotification:(Values.Notification.of_json newNotification)
              ()) (Some Values.UpdateNotificationResponse.to_json)
           (Some Values.UpdateNotificationResponse.error_to_json)])
let update_subscriber =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and notification =
         flag "notification" (required json_arg) ~doc:"JSON Notification"
       and oldSubscriber =
         flag "old-subscriber" (required json_arg) ~doc:"JSON Subscriber"
       and newSubscriber =
         flag "new-subscriber" (required json_arg) ~doc:"JSON Subscriber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_subscriber
           (Values.UpdateSubscriberRequest.make ~accountId ~budgetName
              ~notification:(Values.Notification.of_json notification)
              ~oldSubscriber:(Values.Subscriber.of_json oldSubscriber)
              ~newSubscriber:(Values.Subscriber.of_json newSubscriber) ())
           (Some Values.UpdateSubscriberResponse.to_json)
           (Some Values.UpdateSubscriberResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-budget", create_budget);
    ("create-budget-action", create_budget_action);
    ("create-notification", create_notification);
    ("create-subscriber", create_subscriber);
    ("delete-budget", delete_budget);
    ("delete-budget-action", delete_budget_action);
    ("delete-notification", delete_notification);
    ("delete-subscriber", delete_subscriber);
    ("describe-budget", describe_budget);
    ("describe-budget-action", describe_budget_action);
    ("describe-budget-action-histories", describe_budget_action_histories);
    ("describe-budget-actions-for-account",
      describe_budget_actions_for_account);
    ("describe-budget-actions-for-budget",
      describe_budget_actions_for_budget);
    ("describe-budget-notifications-for-account",
      describe_budget_notifications_for_account);
    ("describe-budget-performance-history",
      describe_budget_performance_history);
    ("describe-budgets", describe_budgets);
    ("describe-notifications-for-budget", describe_notifications_for_budget);
    ("describe-subscribers-for-notification",
      describe_subscribers_for_notification);
    ("execute-budget-action", execute_budget_action);
    ("update-budget", update_budget);
    ("update-budget-action", update_budget_action);
    ("update-notification", update_notification);
    ("update-subscriber", update_subscriber)]
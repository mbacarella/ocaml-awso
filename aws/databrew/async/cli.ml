(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_delete_recipe_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING RecipeName"
       and recipeVersions =
         flag "recipe-versions" (required json_arg)
           ~doc:"JSON RecipeVersionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_recipe_version
           (Values.BatchDeleteRecipeVersionRequest.make ~name
              ~recipeVersions:(Values.RecipeVersionList.of_json
                                 recipeVersions) ())
           (Some Values.BatchDeleteRecipeVersionResponse.to_json)
           (Some Values.BatchDeleteRecipeVersionResponse.error_to_json)])
let create_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and format = flag "format" (optional json_arg) ~doc:"JSON InputFormat"
       and formatOptions =
         flag "format-options" (optional json_arg) ~doc:"JSON FormatOptions"
       and pathOptions =
         flag "path-options" (optional json_arg) ~doc:"JSON PathOptions"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING DatasetName"
       and input = flag "input" (required json_arg) ~doc:"JSON Input" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dataset
           (Values.CreateDatasetRequest.make
              ?format:(Option.map ~f:Values.InputFormat.of_json format)
              ?formatOptions:(Option.map ~f:Values.FormatOptions.of_json
                                formatOptions)
              ?pathOptions:(Option.map ~f:Values.PathOptions.of_json
                              pathOptions)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~input:(Values.Input.of_json input) ())
           (Some Values.CreateDatasetResponse.to_json)
           (Some Values.CreateDatasetResponse.error_to_json)])
let create_profile_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionKeyArn =
         flag "encryption-key-arn" (optional string)
           ~doc:"STRING EncryptionKeyArn"
       and encryptionMode =
         flag "encryption-mode" (optional json_arg)
           ~doc:"JSON EncryptionMode"
       and logSubscription =
         flag "log-subscription" (optional json_arg)
           ~doc:"JSON LogSubscription"
       and maxCapacity =
         flag "max-capacity" (optional int) ~doc:"INT MaxCapacity"
       and maxRetries =
         flag "max-retries" (optional int) ~doc:"INT MaxRetries"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON ProfileConfiguration"
       and validationConfigurations =
         flag "validation-configurations" (optional json_arg)
           ~doc:"JSON ValidationConfigurationList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and jobSample =
         flag "job-sample" (optional json_arg) ~doc:"JSON JobSample"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName"
       and name = flag "name" (required string) ~doc:"STRING JobName"
       and outputLocation =
         flag "output-location" (required json_arg) ~doc:"JSON S3Location"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_profile_job
           (Values.CreateProfileJobRequest.make ?encryptionKeyArn
              ?encryptionMode:(Option.map ~f:Values.EncryptionMode.of_json
                                 encryptionMode)
              ?logSubscription:(Option.map ~f:Values.LogSubscription.of_json
                                  logSubscription) ?maxCapacity ?maxRetries
              ?configuration:(Option.map
                                ~f:Values.ProfileConfiguration.of_json
                                configuration)
              ?validationConfigurations:(Option.map
                                           ~f:Values.ValidationConfigurationList.of_json
                                           validationConfigurations)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?timeout
              ?jobSample:(Option.map ~f:Values.JobSample.of_json jobSample)
              ~datasetName ~name
              ~outputLocation:(Values.S3Location.of_json outputLocation)
              ~roleArn ()) (Some Values.CreateProfileJobResponse.to_json)
           (Some Values.CreateProfileJobResponse.error_to_json)])
let create_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sample = flag "sample" (optional json_arg) ~doc:"JSON Sample"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName"
       and name = flag "name" (required string) ~doc:"STRING ProjectName"
       and recipeName =
         flag "recipe-name" (required string) ~doc:"STRING RecipeName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_project
           (Values.CreateProjectRequest.make
              ?sample:(Option.map ~f:Values.Sample.of_json sample)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~datasetName
              ~name ~recipeName ~roleArn ())
           (Some Values.CreateProjectResponse.to_json)
           (Some Values.CreateProjectResponse.error_to_json)])
let create_recipe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING RecipeDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING RecipeName"
       and steps =
         flag "steps" (required json_arg) ~doc:"JSON RecipeStepList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_recipe
           (Values.CreateRecipeRequest.make ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~steps:(Values.RecipeStepList.of_json steps) ())
           (Some Values.CreateRecipeResponse.to_json)
           (Some Values.CreateRecipeResponse.error_to_json)])
let create_recipe_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetName =
         flag "dataset-name" (optional string) ~doc:"STRING DatasetName"
       and encryptionKeyArn =
         flag "encryption-key-arn" (optional string)
           ~doc:"STRING EncryptionKeyArn"
       and encryptionMode =
         flag "encryption-mode" (optional json_arg)
           ~doc:"JSON EncryptionMode"
       and logSubscription =
         flag "log-subscription" (optional json_arg)
           ~doc:"JSON LogSubscription"
       and maxCapacity =
         flag "max-capacity" (optional int) ~doc:"INT MaxCapacity"
       and maxRetries =
         flag "max-retries" (optional int) ~doc:"INT MaxRetries"
       and outputs =
         flag "outputs" (optional json_arg) ~doc:"JSON OutputList"
       and dataCatalogOutputs =
         flag "data-catalog-outputs" (optional json_arg)
           ~doc:"JSON DataCatalogOutputList"
       and databaseOutputs =
         flag "database-outputs" (optional json_arg)
           ~doc:"JSON DatabaseOutputList"
       and projectName =
         flag "project-name" (optional string) ~doc:"STRING ProjectName"
       and recipeReference =
         flag "recipe-reference" (optional json_arg)
           ~doc:"JSON RecipeReference"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and name = flag "name" (required string) ~doc:"STRING JobName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_recipe_job
           (Values.CreateRecipeJobRequest.make ?datasetName ?encryptionKeyArn
              ?encryptionMode:(Option.map ~f:Values.EncryptionMode.of_json
                                 encryptionMode)
              ?logSubscription:(Option.map ~f:Values.LogSubscription.of_json
                                  logSubscription) ?maxCapacity ?maxRetries
              ?outputs:(Option.map ~f:Values.OutputList.of_json outputs)
              ?dataCatalogOutputs:(Option.map
                                     ~f:Values.DataCatalogOutputList.of_json
                                     dataCatalogOutputs)
              ?databaseOutputs:(Option.map
                                  ~f:Values.DatabaseOutputList.of_json
                                  databaseOutputs) ?projectName
              ?recipeReference:(Option.map ~f:Values.RecipeReference.of_json
                                  recipeReference)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?timeout ~name
              ~roleArn ()) (Some Values.CreateRecipeJobResponse.to_json)
           (Some Values.CreateRecipeJobResponse.error_to_json)])
let create_ruleset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING RulesetDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING RulesetName"
       and targetArn = flag "target-arn" (required string) ~doc:"STRING Arn"
       and rules = flag "rules" (required json_arg) ~doc:"JSON RuleList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ruleset
           (Values.CreateRulesetRequest.make ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~targetArn ~rules:(Values.RuleList.of_json rules) ())
           (Some Values.CreateRulesetResponse.to_json)
           (Some Values.CreateRulesetResponse.error_to_json)])
let create_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobNames =
         flag "job-names" (optional json_arg) ~doc:"JSON JobNameList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and cronExpression =
         flag "cron-expression" (required string)
           ~doc:"STRING CronExpression"
       and name = flag "name" (required string) ~doc:"STRING ScheduleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_schedule
           (Values.CreateScheduleRequest.make
              ?jobNames:(Option.map ~f:Values.JobNameList.of_json jobNames)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~cronExpression ~name ())
           (Some Values.CreateScheduleResponse.to_json)
           (Some Values.CreateScheduleResponse.error_to_json)])
let delete_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dataset (Values.DeleteDatasetRequest.make ~name ())
           (Some Values.DeleteDatasetResponse.to_json)
           (Some Values.DeleteDatasetResponse.error_to_json)])
let delete_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING JobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_job (Values.DeleteJobRequest.make ~name ())
           (Some Values.DeleteJobResponse.to_json)
           (Some Values.DeleteJobResponse.error_to_json)])
let delete_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_project (Values.DeleteProjectRequest.make ~name ())
           (Some Values.DeleteProjectResponse.to_json)
           (Some Values.DeleteProjectResponse.error_to_json)])
let delete_recipe_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING RecipeName"
       and recipeVersion =
         flag "recipe-version" (required string) ~doc:"STRING RecipeVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_recipe_version
           (Values.DeleteRecipeVersionRequest.make ~name ~recipeVersion ())
           (Some Values.DeleteRecipeVersionResponse.to_json)
           (Some Values.DeleteRecipeVersionResponse.error_to_json)])
let delete_ruleset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING RulesetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_ruleset (Values.DeleteRulesetRequest.make ~name ())
           (Some Values.DeleteRulesetResponse.to_json)
           (Some Values.DeleteRulesetResponse.error_to_json)])
let delete_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ScheduleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_schedule (Values.DeleteScheduleRequest.make ~name ())
           (Some Values.DeleteScheduleResponse.to_json)
           (Some Values.DeleteScheduleResponse.error_to_json)])
let describe_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dataset (Values.DescribeDatasetRequest.make ~name ())
           (Some Values.DescribeDatasetResponse.to_json)
           (Some Values.DescribeDatasetResponse.error_to_json)])
let describe_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING JobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_job (Values.DescribeJobRequest.make ~name ())
           (Some Values.DescribeJobResponse.to_json)
           (Some Values.DescribeJobResponse.error_to_json)])
let describe_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING JobName"
       and runId = flag "run-id" (required string) ~doc:"STRING JobRunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_job_run
           (Values.DescribeJobRunRequest.make ~name ~runId ())
           (Some Values.DescribeJobRunResponse.to_json)
           (Some Values.DescribeJobRunResponse.error_to_json)])
let describe_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_project (Values.DescribeProjectRequest.make ~name ())
           (Some Values.DescribeProjectResponse.to_json)
           (Some Values.DescribeProjectResponse.error_to_json)])
let describe_recipe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recipeVersion =
         flag "recipe-version" (optional string) ~doc:"STRING RecipeVersion"
       and name = flag "name" (required string) ~doc:"STRING RecipeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_recipe
           (Values.DescribeRecipeRequest.make ?recipeVersion ~name ())
           (Some Values.DescribeRecipeResponse.to_json)
           (Some Values.DescribeRecipeResponse.error_to_json)])
let describe_ruleset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING RulesetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ruleset (Values.DescribeRulesetRequest.make ~name ())
           (Some Values.DescribeRulesetResponse.to_json)
           (Some Values.DescribeRulesetResponse.error_to_json)])
let describe_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ScheduleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_schedule
           (Values.DescribeScheduleRequest.make ~name ())
           (Some Values.DescribeScheduleResponse.to_json)
           (Some Values.DescribeScheduleResponse.error_to_json)])
let list_datasets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults100"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_datasets
           (Values.ListDatasetsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListDatasetsResponse.to_json)
           (Some Values.ListDatasetsResponse.error_to_json)])
let list_job_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults100"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and name = flag "name" (required string) ~doc:"STRING JobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_job_runs
           (Values.ListJobRunsRequest.make ?maxResults ?nextToken ~name ())
           (Some Values.ListJobRunsResponse.to_json)
           (Some Values.ListJobRunsResponse.error_to_json)])
let list_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetName =
         flag "dataset-name" (optional string) ~doc:"STRING DatasetName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults100"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and projectName =
         flag "project-name" (optional string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_jobs
           (Values.ListJobsRequest.make ?datasetName ?maxResults ?nextToken
              ?projectName ()) (Some Values.ListJobsResponse.to_json)
           (Some Values.ListJobsResponse.error_to_json)])
let list_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_projects
           (Values.ListProjectsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListProjectsResponse.to_json)
           (Some Values.ListProjectsResponse.error_to_json)])
let list_recipe_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults100"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and name = flag "name" (required string) ~doc:"STRING RecipeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recipe_versions
           (Values.ListRecipeVersionsRequest.make ?maxResults ?nextToken
              ~name ()) (Some Values.ListRecipeVersionsResponse.to_json)
           (Some Values.ListRecipeVersionsResponse.error_to_json)])
let list_recipes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults100"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and recipeVersion =
         flag "recipe-version" (optional string) ~doc:"STRING RecipeVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recipes
           (Values.ListRecipesRequest.make ?maxResults ?nextToken
              ?recipeVersion ()) (Some Values.ListRecipesResponse.to_json)
           (Some Values.ListRecipesResponse.error_to_json)])
let list_rulesets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetArn = flag "target-arn" (optional string) ~doc:"STRING Arn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults100"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rulesets
           (Values.ListRulesetsRequest.make ?targetArn ?maxResults ?nextToken
              ()) (Some Values.ListRulesetsResponse.to_json)
           (Some Values.ListRulesetsResponse.error_to_json)])
let list_schedules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults100"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_schedules
           (Values.ListSchedulesRequest.make ?jobName ?maxResults ?nextToken
              ()) (Some Values.ListSchedulesResponse.to_json)
           (Some Values.ListSchedulesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let publish_recipe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING RecipeDescription"
       and name = flag "name" (required string) ~doc:"STRING RecipeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.publish_recipe
           (Values.PublishRecipeRequest.make ?description ~name ())
           (Some Values.PublishRecipeResponse.to_json)
           (Some Values.PublishRecipeResponse.error_to_json)])
let send_project_session_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and preview = flag "preview" (optional bool) ~doc:"BOOL Preview"
       and recipeStep =
         flag "recipe-step" (optional json_arg) ~doc:"JSON RecipeStep"
       and stepIndex = flag "step-index" (optional int) ~doc:"INT StepIndex"
       and clientSessionId =
         flag "client-session-id" (optional string)
           ~doc:"STRING ClientSessionId"
       and viewFrame =
         flag "view-frame" (optional json_arg) ~doc:"JSON ViewFrame"
       and name = flag "name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_project_session_action
           (Values.SendProjectSessionActionRequest.make ?preview
              ?recipeStep:(Option.map ~f:Values.RecipeStep.of_json recipeStep)
              ?stepIndex ?clientSessionId
              ?viewFrame:(Option.map ~f:Values.ViewFrame.of_json viewFrame)
              ~name ())
           (Some Values.SendProjectSessionActionResponse.to_json)
           (Some Values.SendProjectSessionActionResponse.error_to_json)])
let start_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING JobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_job_run (Values.StartJobRunRequest.make ~name ())
           (Some Values.StartJobRunResponse.to_json)
           (Some Values.StartJobRunResponse.error_to_json)])
let start_project_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assumeControl =
         flag "assume-control" (optional bool) ~doc:"BOOL AssumeControl"
       and name = flag "name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_project_session
           (Values.StartProjectSessionRequest.make ?assumeControl ~name ())
           (Some Values.StartProjectSessionResponse.to_json)
           (Some Values.StartProjectSessionResponse.error_to_json)])
let stop_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING JobName"
       and runId = flag "run-id" (required string) ~doc:"STRING JobRunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_job_run (Values.StopJobRunRequest.make ~name ~runId ())
           (Some Values.StopJobRunResponse.to_json)
           (Some Values.StopJobRunResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and format = flag "format" (optional json_arg) ~doc:"JSON InputFormat"
       and formatOptions =
         flag "format-options" (optional json_arg) ~doc:"JSON FormatOptions"
       and pathOptions =
         flag "path-options" (optional json_arg) ~doc:"JSON PathOptions"
       and name = flag "name" (required string) ~doc:"STRING DatasetName"
       and input = flag "input" (required json_arg) ~doc:"JSON Input" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_dataset
           (Values.UpdateDatasetRequest.make
              ?format:(Option.map ~f:Values.InputFormat.of_json format)
              ?formatOptions:(Option.map ~f:Values.FormatOptions.of_json
                                formatOptions)
              ?pathOptions:(Option.map ~f:Values.PathOptions.of_json
                              pathOptions) ~name
              ~input:(Values.Input.of_json input) ())
           (Some Values.UpdateDatasetResponse.to_json)
           (Some Values.UpdateDatasetResponse.error_to_json)])
let update_profile_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON ProfileConfiguration"
       and encryptionKeyArn =
         flag "encryption-key-arn" (optional string)
           ~doc:"STRING EncryptionKeyArn"
       and encryptionMode =
         flag "encryption-mode" (optional json_arg)
           ~doc:"JSON EncryptionMode"
       and logSubscription =
         flag "log-subscription" (optional json_arg)
           ~doc:"JSON LogSubscription"
       and maxCapacity =
         flag "max-capacity" (optional int) ~doc:"INT MaxCapacity"
       and maxRetries =
         flag "max-retries" (optional int) ~doc:"INT MaxRetries"
       and validationConfigurations =
         flag "validation-configurations" (optional json_arg)
           ~doc:"JSON ValidationConfigurationList"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and jobSample =
         flag "job-sample" (optional json_arg) ~doc:"JSON JobSample"
       and name = flag "name" (required string) ~doc:"STRING JobName"
       and outputLocation =
         flag "output-location" (required json_arg) ~doc:"JSON S3Location"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_profile_job
           (Values.UpdateProfileJobRequest.make
              ?configuration:(Option.map
                                ~f:Values.ProfileConfiguration.of_json
                                configuration) ?encryptionKeyArn
              ?encryptionMode:(Option.map ~f:Values.EncryptionMode.of_json
                                 encryptionMode)
              ?logSubscription:(Option.map ~f:Values.LogSubscription.of_json
                                  logSubscription) ?maxCapacity ?maxRetries
              ?validationConfigurations:(Option.map
                                           ~f:Values.ValidationConfigurationList.of_json
                                           validationConfigurations) ?timeout
              ?jobSample:(Option.map ~f:Values.JobSample.of_json jobSample)
              ~name
              ~outputLocation:(Values.S3Location.of_json outputLocation)
              ~roleArn ()) (Some Values.UpdateProfileJobResponse.to_json)
           (Some Values.UpdateProfileJobResponse.error_to_json)])
let update_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sample = flag "sample" (optional json_arg) ~doc:"JSON Sample"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING Arn"
       and name = flag "name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_project
           (Values.UpdateProjectRequest.make
              ?sample:(Option.map ~f:Values.Sample.of_json sample) ~roleArn
              ~name ()) (Some Values.UpdateProjectResponse.to_json)
           (Some Values.UpdateProjectResponse.error_to_json)])
let update_recipe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING RecipeDescription"
       and steps =
         flag "steps" (optional json_arg) ~doc:"JSON RecipeStepList"
       and name = flag "name" (required string) ~doc:"STRING RecipeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_recipe
           (Values.UpdateRecipeRequest.make ?description
              ?steps:(Option.map ~f:Values.RecipeStepList.of_json steps)
              ~name ()) (Some Values.UpdateRecipeResponse.to_json)
           (Some Values.UpdateRecipeResponse.error_to_json)])
let update_recipe_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionKeyArn =
         flag "encryption-key-arn" (optional string)
           ~doc:"STRING EncryptionKeyArn"
       and encryptionMode =
         flag "encryption-mode" (optional json_arg)
           ~doc:"JSON EncryptionMode"
       and logSubscription =
         flag "log-subscription" (optional json_arg)
           ~doc:"JSON LogSubscription"
       and maxCapacity =
         flag "max-capacity" (optional int) ~doc:"INT MaxCapacity"
       and maxRetries =
         flag "max-retries" (optional int) ~doc:"INT MaxRetries"
       and outputs =
         flag "outputs" (optional json_arg) ~doc:"JSON OutputList"
       and dataCatalogOutputs =
         flag "data-catalog-outputs" (optional json_arg)
           ~doc:"JSON DataCatalogOutputList"
       and databaseOutputs =
         flag "database-outputs" (optional json_arg)
           ~doc:"JSON DatabaseOutputList"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and name = flag "name" (required string) ~doc:"STRING JobName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_recipe_job
           (Values.UpdateRecipeJobRequest.make ?encryptionKeyArn
              ?encryptionMode:(Option.map ~f:Values.EncryptionMode.of_json
                                 encryptionMode)
              ?logSubscription:(Option.map ~f:Values.LogSubscription.of_json
                                  logSubscription) ?maxCapacity ?maxRetries
              ?outputs:(Option.map ~f:Values.OutputList.of_json outputs)
              ?dataCatalogOutputs:(Option.map
                                     ~f:Values.DataCatalogOutputList.of_json
                                     dataCatalogOutputs)
              ?databaseOutputs:(Option.map
                                  ~f:Values.DatabaseOutputList.of_json
                                  databaseOutputs) ?timeout ~name ~roleArn ())
           (Some Values.UpdateRecipeJobResponse.to_json)
           (Some Values.UpdateRecipeJobResponse.error_to_json)])
let update_ruleset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING RulesetDescription"
       and name = flag "name" (required string) ~doc:"STRING RulesetName"
       and rules = flag "rules" (required json_arg) ~doc:"JSON RuleList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_ruleset
           (Values.UpdateRulesetRequest.make ?description ~name
              ~rules:(Values.RuleList.of_json rules) ())
           (Some Values.UpdateRulesetResponse.to_json)
           (Some Values.UpdateRulesetResponse.error_to_json)])
let update_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobNames =
         flag "job-names" (optional json_arg) ~doc:"JSON JobNameList"
       and cronExpression =
         flag "cron-expression" (required string)
           ~doc:"STRING CronExpression"
       and name = flag "name" (required string) ~doc:"STRING ScheduleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_schedule
           (Values.UpdateScheduleRequest.make
              ?jobNames:(Option.map ~f:Values.JobNameList.of_json jobNames)
              ~cronExpression ~name ())
           (Some Values.UpdateScheduleResponse.to_json)
           (Some Values.UpdateScheduleResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("batch-delete-recipe-version", batch_delete_recipe_version);
    ("create-dataset", create_dataset);
    ("create-profile-job", create_profile_job);
    ("create-project", create_project);
    ("create-recipe", create_recipe);
    ("create-recipe-job", create_recipe_job);
    ("create-ruleset", create_ruleset);
    ("create-schedule", create_schedule);
    ("delete-dataset", delete_dataset);
    ("delete-job", delete_job);
    ("delete-project", delete_project);
    ("delete-recipe-version", delete_recipe_version);
    ("delete-ruleset", delete_ruleset);
    ("delete-schedule", delete_schedule);
    ("describe-dataset", describe_dataset);
    ("describe-job", describe_job);
    ("describe-job-run", describe_job_run);
    ("describe-project", describe_project);
    ("describe-recipe", describe_recipe);
    ("describe-ruleset", describe_ruleset);
    ("describe-schedule", describe_schedule);
    ("list-datasets", list_datasets);
    ("list-job-runs", list_job_runs);
    ("list-jobs", list_jobs);
    ("list-projects", list_projects);
    ("list-recipe-versions", list_recipe_versions);
    ("list-recipes", list_recipes);
    ("list-rulesets", list_rulesets);
    ("list-schedules", list_schedules);
    ("list-tags-for-resource", list_tags_for_resource);
    ("publish-recipe", publish_recipe);
    ("send-project-session-action", send_project_session_action);
    ("start-job-run", start_job_run);
    ("start-project-session", start_project_session);
    ("stop-job-run", stop_job_run);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-dataset", update_dataset);
    ("update-profile-job", update_profile_job);
    ("update-project", update_project);
    ("update-recipe", update_recipe);
    ("update-recipe-job", update_recipe_job);
    ("update-ruleset", update_ruleset);
    ("update-schedule", update_schedule)]
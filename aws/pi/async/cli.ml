(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let describe_dimension_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and periodInSeconds =
         flag "period-in-seconds" (optional int) ~doc:"INT Integer"
       and additionalMetrics =
         flag "additional-metrics" (optional json_arg)
           ~doc:"JSON AdditionalMetricsList"
       and partitionBy =
         flag "partition-by" (optional json_arg) ~doc:"JSON DimensionGroup"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON MetricQueryFilterMap"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and serviceType =
         flag "service-type" (required json_arg) ~doc:"JSON ServiceType"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING RequestString"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON ISOTimestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON ISOTimestamp"
       and metric =
         flag "metric" (required string) ~doc:"STRING RequestString"
       and groupBy =
         flag "group-by" (required json_arg) ~doc:"JSON DimensionGroup" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dimension_keys
           (Values.DescribeDimensionKeysRequest.make ?periodInSeconds
              ?additionalMetrics:(Option.map
                                    ~f:Values.AdditionalMetricsList.of_json
                                    additionalMetrics)
              ?partitionBy:(Option.map ~f:Values.DimensionGroup.of_json
                              partitionBy)
              ?filter:(Option.map ~f:Values.MetricQueryFilterMap.of_json
                         filter) ?maxResults ?nextToken
              ~serviceType:(Values.ServiceType.of_json serviceType)
              ~identifier ~startTime:(Values.ISOTimestamp.of_json startTime)
              ~endTime:(Values.ISOTimestamp.of_json endTime) ~metric
              ~groupBy:(Values.DimensionGroup.of_json groupBy) ())
           (Some Values.DescribeDimensionKeysResponse.to_json)
           (Some Values.DescribeDimensionKeysResponse.error_to_json)])
let get_dimension_key_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestedDimensions =
         flag "requested-dimensions" (optional json_arg)
           ~doc:"JSON RequestedDimensionList"
       and serviceType =
         flag "service-type" (required json_arg) ~doc:"JSON ServiceType"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING IdentifierString"
       and group = flag "group" (required string) ~doc:"STRING RequestString"
       and groupIdentifier =
         flag "group-identifier" (required string)
           ~doc:"STRING RequestString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dimension_key_details
           (Values.GetDimensionKeyDetailsRequest.make
              ?requestedDimensions:(Option.map
                                      ~f:Values.RequestedDimensionList.of_json
                                      requestedDimensions)
              ~serviceType:(Values.ServiceType.of_json serviceType)
              ~identifier ~group ~groupIdentifier ())
           (Some Values.GetDimensionKeyDetailsResponse.to_json)
           (Some Values.GetDimensionKeyDetailsResponse.error_to_json)])
let get_resource_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceType =
         flag "service-type" (required json_arg) ~doc:"JSON ServiceType"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING RequestString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_metadata
           (Values.GetResourceMetadataRequest.make
              ~serviceType:(Values.ServiceType.of_json serviceType)
              ~identifier ())
           (Some Values.GetResourceMetadataResponse.to_json)
           (Some Values.GetResourceMetadataResponse.error_to_json)])
let get_resource_metrics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and periodInSeconds =
         flag "period-in-seconds" (optional int) ~doc:"INT Integer"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and serviceType =
         flag "service-type" (required json_arg) ~doc:"JSON ServiceType"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING RequestString"
       and metricQueries =
         flag "metric-queries" (required json_arg)
           ~doc:"JSON MetricQueryList"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON ISOTimestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON ISOTimestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_metrics
           (Values.GetResourceMetricsRequest.make ?periodInSeconds
              ?maxResults ?nextToken
              ~serviceType:(Values.ServiceType.of_json serviceType)
              ~identifier
              ~metricQueries:(Values.MetricQueryList.of_json metricQueries)
              ~startTime:(Values.ISOTimestamp.of_json startTime)
              ~endTime:(Values.ISOTimestamp.of_json endTime) ())
           (Some Values.GetResourceMetricsResponse.to_json)
           (Some Values.GetResourceMetricsResponse.error_to_json)])
let list_available_resource_dimensions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and serviceType =
         flag "service-type" (required json_arg) ~doc:"JSON ServiceType"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING RequestString"
       and metrics =
         flag "metrics" (required json_arg) ~doc:"JSON DimensionsMetricList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_available_resource_dimensions
           (Values.ListAvailableResourceDimensionsRequest.make ?maxResults
              ?nextToken
              ~serviceType:(Values.ServiceType.of_json serviceType)
              ~identifier
              ~metrics:(Values.DimensionsMetricList.of_json metrics) ())
           (Some Values.ListAvailableResourceDimensionsResponse.to_json)
           (Some Values.ListAvailableResourceDimensionsResponse.error_to_json)])
let list_available_resource_metrics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and serviceType =
         flag "service-type" (required json_arg) ~doc:"JSON ServiceType"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING RequestString"
       and metricTypes =
         flag "metric-types" (required json_arg) ~doc:"JSON MetricTypeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_available_resource_metrics
           (Values.ListAvailableResourceMetricsRequest.make ?nextToken
              ?maxResults
              ~serviceType:(Values.ServiceType.of_json serviceType)
              ~identifier
              ~metricTypes:(Values.MetricTypeList.of_json metricTypes) ())
           (Some Values.ListAvailableResourceMetricsResponse.to_json)
           (Some Values.ListAvailableResourceMetricsResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("describe-dimension-keys", describe_dimension_keys);
    ("get-dimension-key-details", get_dimension_key_details);
    ("get-resource-metadata", get_resource_metadata);
    ("get-resource-metrics", get_resource_metrics);
    ("list-available-resource-dimensions",
      list_available_resource_dimensions);
    ("list-available-resource-metrics", list_available_resource_metrics)]
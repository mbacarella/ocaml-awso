(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | DescribeDimensionKeys: (DescribeDimensionKeysRequest.t,
  DescribeDimensionKeysResponse.t, DescribeDimensionKeysResponse.error) t 
  | GetDimensionKeyDetails: (GetDimensionKeyDetailsRequest.t,
  GetDimensionKeyDetailsResponse.t, GetDimensionKeyDetailsResponse.error) t 
  | GetResourceMetadata: (GetResourceMetadataRequest.t,
  GetResourceMetadataResponse.t, GetResourceMetadataResponse.error) t 
  | GetResourceMetrics: (GetResourceMetricsRequest.t,
  GetResourceMetricsResponse.t, GetResourceMetricsResponse.error) t 
  | ListAvailableResourceDimensions:
  (ListAvailableResourceDimensionsRequest.t,
  ListAvailableResourceDimensionsResponse.t,
  ListAvailableResourceDimensionsResponse.error) t 
  | ListAvailableResourceMetrics: (ListAvailableResourceMetricsRequest.t,
  ListAvailableResourceMetricsResponse.t,
  ListAvailableResourceMetricsResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | DescribeDimensionKeys -> `POST
  | GetDimensionKeyDetails -> `POST
  | GetResourceMetadata -> `POST
  | GetResourceMetrics -> `POST
  | ListAvailableResourceDimensions -> `POST
  | ListAvailableResourceMetrics -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | DescribeDimensionKeys -> (Format.kasprintf Uri.of_string) "/"
      | GetDimensionKeyDetails -> (Format.kasprintf Uri.of_string) "/"
      | GetResourceMetadata -> (Format.kasprintf Uri.of_string) "/"
      | GetResourceMetrics -> (Format.kasprintf Uri.of_string) "/"
      | ListAvailableResourceDimensions ->
          (Format.kasprintf Uri.of_string) "/"
      | ListAvailableResourceMetrics -> (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  match endp with
  | DescribeDimensionKeys ->
      let json = DescribeDimensionKeysRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target",
            "PerformanceInsightsv20180227.DescribeDimensionKeys")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetDimensionKeyDetails ->
      let json = GetDimensionKeyDetailsRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target",
            "PerformanceInsightsv20180227.GetDimensionKeyDetails")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetResourceMetadata ->
      let json = GetResourceMetadataRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target",
            "PerformanceInsightsv20180227.GetResourceMetadata")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetResourceMetrics ->
      let json = GetResourceMetricsRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "PerformanceInsightsv20180227.GetResourceMetrics")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListAvailableResourceDimensions ->
      let json = ListAvailableResourceDimensionsRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target",
            "PerformanceInsightsv20180227.ListAvailableResourceDimensions")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListAvailableResourceMetrics ->
      let json = ListAvailableResourceMetricsRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target",
            "PerformanceInsightsv20180227.ListAvailableResourceMetrics")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : (Awso.Http.Response.t, Awso.Http.Io.Error.call) result) :
  (o, [ `AWS of e  | `Transport of Awso.Http.Io.Error.call ]) result=
  let handle_error err error_of_json =
    let generic_error () = Error (`Transport err) in
    match err with
    | `Too_many_redirects -> generic_error ()
    | `Bad_response
        { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = _ } ->
        (match (error_of_json, ((code >= 400) && (code <= 599))) with
         | (Some error_of_json, true) ->
             let json = Yojson.Safe.from_string body in
             (match json |> (Yojson.Safe.Util.member "__type") with
              | `String error_type ->
                  Error (`AWS (error_of_json error_type json))
              | `Null -> generic_error ()
              | _ ->
                  failwith
                    (sprintf "Error '__type' did not have string type: %s"
                       body))
         | (None, _) | (_, false) -> generic_error ()) in
  match endpoint with
  | DescribeDimensionKeys ->
      (match resp with
       | Error err ->
           handle_error err
             (Some DescribeDimensionKeysResponse.error_of_json)
       | Ok resp ->
           let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
           Ok (DescribeDimensionKeysResponse.of_json json))
  | GetDimensionKeyDetails ->
      (match resp with
       | Error err ->
           handle_error err
             (Some GetDimensionKeyDetailsResponse.error_of_json)
       | Ok resp ->
           let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
           Ok (GetDimensionKeyDetailsResponse.of_json json))
  | GetResourceMetadata ->
      (match resp with
       | Error err ->
           handle_error err (Some GetResourceMetadataResponse.error_of_json)
       | Ok resp ->
           let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
           Ok (GetResourceMetadataResponse.of_json json))
  | GetResourceMetrics ->
      (match resp with
       | Error err ->
           handle_error err (Some GetResourceMetricsResponse.error_of_json)
       | Ok resp ->
           let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
           Ok (GetResourceMetricsResponse.of_json json))
  | ListAvailableResourceDimensions ->
      (match resp with
       | Error err ->
           handle_error err
             (Some ListAvailableResourceDimensionsResponse.error_of_json)
       | Ok resp ->
           let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
           Ok (ListAvailableResourceDimensionsResponse.of_json json))
  | ListAvailableResourceMetrics ->
      (match resp with
       | Error err ->
           handle_error err
             (Some ListAvailableResourceMetricsResponse.error_of_json)
       | Ok resp ->
           let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
           Ok (ListAvailableResourceMetricsResponse.of_json json))
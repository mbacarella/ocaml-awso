(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_create_table_rows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and workbookId =
         flag "workbook-id" (required string) ~doc:"STRING ResourceId"
       and tableId =
         flag "table-id" (required string) ~doc:"STRING ResourceId"
       and rowsToCreate =
         flag "rows-to-create" (required json_arg)
           ~doc:"JSON CreateRowDataList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_table_rows
           (Values.BatchCreateTableRowsRequest.make ?clientRequestToken
              ~workbookId ~tableId
              ~rowsToCreate:(Values.CreateRowDataList.of_json rowsToCreate)
              ()) (Some Values.BatchCreateTableRowsResult.to_json)
           (Some Values.BatchCreateTableRowsResult.error_to_json)])
let batch_delete_table_rows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and workbookId =
         flag "workbook-id" (required string) ~doc:"STRING ResourceId"
       and tableId =
         flag "table-id" (required string) ~doc:"STRING ResourceId"
       and rowIds = flag "row-ids" (required json_arg) ~doc:"JSON RowIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_table_rows
           (Values.BatchDeleteTableRowsRequest.make ?clientRequestToken
              ~workbookId ~tableId ~rowIds:(Values.RowIdList.of_json rowIds)
              ()) (Some Values.BatchDeleteTableRowsResult.to_json)
           (Some Values.BatchDeleteTableRowsResult.error_to_json)])
let batch_update_table_rows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and workbookId =
         flag "workbook-id" (required string) ~doc:"STRING ResourceId"
       and tableId =
         flag "table-id" (required string) ~doc:"STRING ResourceId"
       and rowsToUpdate =
         flag "rows-to-update" (required json_arg)
           ~doc:"JSON UpdateRowDataList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_table_rows
           (Values.BatchUpdateTableRowsRequest.make ?clientRequestToken
              ~workbookId ~tableId
              ~rowsToUpdate:(Values.UpdateRowDataList.of_json rowsToUpdate)
              ()) (Some Values.BatchUpdateTableRowsResult.to_json)
           (Some Values.BatchUpdateTableRowsResult.error_to_json)])
let batch_upsert_table_rows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and workbookId =
         flag "workbook-id" (required string) ~doc:"STRING ResourceId"
       and tableId =
         flag "table-id" (required string) ~doc:"STRING ResourceId"
       and rowsToUpsert =
         flag "rows-to-upsert" (required json_arg)
           ~doc:"JSON UpsertRowDataList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_upsert_table_rows
           (Values.BatchUpsertTableRowsRequest.make ?clientRequestToken
              ~workbookId ~tableId
              ~rowsToUpsert:(Values.UpsertRowDataList.of_json rowsToUpsert)
              ()) (Some Values.BatchUpsertTableRowsResult.to_json)
           (Some Values.BatchUpsertTableRowsResult.error_to_json)])
let describe_table_data_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workbookId =
         flag "workbook-id" (required string) ~doc:"STRING ResourceId"
       and tableId =
         flag "table-id" (required string) ~doc:"STRING ResourceId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_table_data_import_job
           (Values.DescribeTableDataImportJobRequest.make ~workbookId
              ~tableId ~jobId ())
           (Some Values.DescribeTableDataImportJobResult.to_json)
           (Some Values.DescribeTableDataImportJobResult.error_to_json)])
let get_screen_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and variables =
         flag "variables" (optional json_arg) ~doc:"JSON VariableValueMap"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and workbookId =
         flag "workbook-id" (required string) ~doc:"STRING ResourceId"
       and appId = flag "app-id" (required string) ~doc:"STRING ResourceId"
       and screenId =
         flag "screen-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_screen_data
           (Values.GetScreenDataRequest.make
              ?variables:(Option.map ~f:Values.VariableValueMap.of_json
                            variables) ?maxResults ?nextToken ~workbookId
              ~appId ~screenId ()) (Some Values.GetScreenDataResult.to_json)
           (Some Values.GetScreenDataResult.error_to_json)])
let invoke_screen_automation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and variables =
         flag "variables" (optional json_arg) ~doc:"JSON VariableValueMap"
       and rowId = flag "row-id" (optional string) ~doc:"STRING RowId"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and workbookId =
         flag "workbook-id" (required string) ~doc:"STRING ResourceId"
       and appId = flag "app-id" (required string) ~doc:"STRING ResourceId"
       and screenId =
         flag "screen-id" (required string) ~doc:"STRING ResourceId"
       and screenAutomationId =
         flag "screen-automation-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invoke_screen_automation
           (Values.InvokeScreenAutomationRequest.make
              ?variables:(Option.map ~f:Values.VariableValueMap.of_json
                            variables) ?rowId ?clientRequestToken ~workbookId
              ~appId ~screenId ~screenAutomationId ())
           (Some Values.InvokeScreenAutomationResult.to_json)
           (Some Values.InvokeScreenAutomationResult.error_to_json)])
let list_table_columns =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and workbookId =
         flag "workbook-id" (required string) ~doc:"STRING ResourceId"
       and tableId =
         flag "table-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_table_columns
           (Values.ListTableColumnsRequest.make ?nextToken ~workbookId
              ~tableId ()) (Some Values.ListTableColumnsResult.to_json)
           (Some Values.ListTableColumnsResult.error_to_json)])
let list_table_rows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and rowIds = flag "row-ids" (optional json_arg) ~doc:"JSON RowIdList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and workbookId =
         flag "workbook-id" (required string) ~doc:"STRING ResourceId"
       and tableId =
         flag "table-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_table_rows
           (Values.ListTableRowsRequest.make
              ?rowIds:(Option.map ~f:Values.RowIdList.of_json rowIds)
              ?maxResults ?nextToken ~workbookId ~tableId ())
           (Some Values.ListTableRowsResult.to_json)
           (Some Values.ListTableRowsResult.error_to_json)])
let list_tables =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and workbookId =
         flag "workbook-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tables
           (Values.ListTablesRequest.make ?maxResults ?nextToken ~workbookId
              ()) (Some Values.ListTablesResult.to_json)
           (Some Values.ListTablesResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResult.to_json)
           (Some Values.ListTagsForResourceResult.error_to_json)])
let query_table_rows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and workbookId =
         flag "workbook-id" (required string) ~doc:"STRING ResourceId"
       and tableId =
         flag "table-id" (required string) ~doc:"STRING ResourceId"
       and filterFormula =
         flag "filter-formula" (required json_arg) ~doc:"JSON Filter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.query_table_rows
           (Values.QueryTableRowsRequest.make ?maxResults ?nextToken
              ~workbookId ~tableId
              ~filterFormula:(Values.Filter.of_json filterFormula) ())
           (Some Values.QueryTableRowsResult.to_json)
           (Some Values.QueryTableRowsResult.error_to_json)])
let start_table_data_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workbookId =
         flag "workbook-id" (required string) ~doc:"STRING ResourceId"
       and dataSource =
         flag "data-source" (required json_arg) ~doc:"JSON ImportDataSource"
       and dataFormat =
         flag "data-format" (required json_arg)
           ~doc:"JSON ImportSourceDataFormat"
       and destinationTableId =
         flag "destination-table-id" (required string)
           ~doc:"STRING ResourceId"
       and importOptions =
         flag "import-options" (required json_arg) ~doc:"JSON ImportOptions"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_table_data_import_job
           (Values.StartTableDataImportJobRequest.make ~workbookId
              ~dataSource:(Values.ImportDataSource.of_json dataSource)
              ~dataFormat:(Values.ImportSourceDataFormat.of_json dataFormat)
              ~destinationTableId
              ~importOptions:(Values.ImportOptions.of_json importOptions)
              ~clientRequestToken ())
           (Some Values.StartTableDataImportJobResult.to_json)
           (Some Values.StartTableDataImportJobResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagsMap.of_json tags) ())
           (Some Values.TagResourceResult.to_json)
           (Some Values.TagResourceResult.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeysList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeysList.of_json tagKeys) ())
           (Some Values.UntagResourceResult.to_json)
           (Some Values.UntagResourceResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-create-table-rows", batch_create_table_rows);
    ("batch-delete-table-rows", batch_delete_table_rows);
    ("batch-update-table-rows", batch_update_table_rows);
    ("batch-upsert-table-rows", batch_upsert_table_rows);
    ("describe-table-data-import-job", describe_table_data_import_job);
    ("get-screen-data", get_screen_data);
    ("invoke-screen-automation", invoke_screen_automation);
    ("list-table-columns", list_table_columns);
    ("list-table-rows", list_table_rows);
    ("list-tables", list_tables);
    ("list-tags-for-resource", list_tags_for_resource);
    ("query-table-rows", query_table_rows);
    ("start-table-data-import-job", start_table_data_import_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
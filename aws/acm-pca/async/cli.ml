(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_certificate_authority =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revocationConfiguration =
         flag "revocation-configuration" (optional json_arg)
           ~doc:"JSON RevocationConfiguration"
       and idempotencyToken =
         flag "idempotency-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and keyStorageSecurityStandard =
         flag "key-storage-security-standard" (optional json_arg)
           ~doc:"JSON KeyStorageSecurityStandard"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and certificateAuthorityConfiguration =
         flag "certificate-authority-configuration" (required json_arg)
           ~doc:"JSON CertificateAuthorityConfiguration"
       and certificateAuthorityType =
         flag "certificate-authority-type" (required json_arg)
           ~doc:"JSON CertificateAuthorityType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_certificate_authority
           (Values.CreateCertificateAuthorityRequest.make
              ?revocationConfiguration:(Option.map
                                          ~f:Values.RevocationConfiguration.of_json
                                          revocationConfiguration)
              ?idempotencyToken
              ?keyStorageSecurityStandard:(Option.map
                                             ~f:Values.KeyStorageSecurityStandard.of_json
                                             keyStorageSecurityStandard)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~certificateAuthorityConfiguration:(Values.CertificateAuthorityConfiguration.of_json
                                                    certificateAuthorityConfiguration)
              ~certificateAuthorityType:(Values.CertificateAuthorityType.of_json
                                           certificateAuthorityType) ())
           (Some Values.CreateCertificateAuthorityResponse.to_json)
           (Some Values.CreateCertificateAuthorityResponse.error_to_json)])
let create_certificate_authority_audit_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn"
       and s3BucketName =
         flag "s3-bucket-name" (required string) ~doc:"STRING S3BucketName"
       and auditReportResponseFormat =
         flag "audit-report-response-format" (required json_arg)
           ~doc:"JSON AuditReportResponseFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_certificate_authority_audit_report
           (Values.CreateCertificateAuthorityAuditReportRequest.make
              ~certificateAuthorityArn ~s3BucketName
              ~auditReportResponseFormat:(Values.AuditReportResponseFormat.of_json
                                            auditReportResponseFormat) ())
           (Some Values.CreateCertificateAuthorityAuditReportResponse.to_json)
           (Some
              Values.CreateCertificateAuthorityAuditReportResponse.error_to_json)])
let create_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceAccount =
         flag "source-account" (optional string) ~doc:"STRING AccountId"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn"
       and principal =
         flag "principal" (required string) ~doc:"STRING Principal"
       and actions =
         flag "actions" (required json_arg) ~doc:"JSON ActionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_permission
           (Values.CreatePermissionRequest.make ?sourceAccount
              ~certificateAuthorityArn ~principal
              ~actions:(Values.ActionList.of_json actions) ()) None None])
let delete_certificate_authority =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and permanentDeletionTimeInDays =
         flag "permanent-deletion-time-in-days" (optional int)
           ~doc:"INT PermanentDeletionTimeInDays"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_certificate_authority
           (Values.DeleteCertificateAuthorityRequest.make
              ?permanentDeletionTimeInDays ~certificateAuthorityArn ()) None
           None])
let delete_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceAccount =
         flag "source-account" (optional string) ~doc:"STRING AccountId"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn"
       and principal =
         flag "principal" (required string) ~doc:"STRING Principal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_permission
           (Values.DeletePermissionRequest.make ?sourceAccount
              ~certificateAuthorityArn ~principal ()) None None])
let delete_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy (Values.DeletePolicyRequest.make ~resourceArn ())
           None None])
let describe_certificate_authority =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_certificate_authority
           (Values.DescribeCertificateAuthorityRequest.make
              ~certificateAuthorityArn ())
           (Some Values.DescribeCertificateAuthorityResponse.to_json)
           (Some Values.DescribeCertificateAuthorityResponse.error_to_json)])
let describe_certificate_authority_audit_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn"
       and auditReportId =
         flag "audit-report-id" (required string) ~doc:"STRING AuditReportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_certificate_authority_audit_report
           (Values.DescribeCertificateAuthorityAuditReportRequest.make
              ~certificateAuthorityArn ~auditReportId ())
           (Some
              Values.DescribeCertificateAuthorityAuditReportResponse.to_json)
           (Some
              Values.DescribeCertificateAuthorityAuditReportResponse.error_to_json)])
let get_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn"
       and certificateArn =
         flag "certificate-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_certificate
           (Values.GetCertificateRequest.make ~certificateAuthorityArn
              ~certificateArn ())
           (Some Values.GetCertificateResponse.to_json)
           (Some Values.GetCertificateResponse.error_to_json)])
let get_certificate_authority_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_certificate_authority_certificate
           (Values.GetCertificateAuthorityCertificateRequest.make
              ~certificateAuthorityArn ())
           (Some Values.GetCertificateAuthorityCertificateResponse.to_json)
           (Some
              Values.GetCertificateAuthorityCertificateResponse.error_to_json)])
let get_certificate_authority_csr =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_certificate_authority_csr
           (Values.GetCertificateAuthorityCsrRequest.make
              ~certificateAuthorityArn ())
           (Some Values.GetCertificateAuthorityCsrResponse.to_json)
           (Some Values.GetCertificateAuthorityCsrResponse.error_to_json)])
let get_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy (Values.GetPolicyRequest.make ~resourceArn ())
           (Some Values.GetPolicyResponse.to_json)
           (Some Values.GetPolicyResponse.error_to_json)])
let import_certificate_authority_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateChain =
         flag "certificate-chain" (optional json_arg)
           ~doc:"JSON CertificateChainBlob"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn"
       and certificate =
         flag "certificate" (required json_arg)
           ~doc:"JSON CertificateBodyBlob" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_certificate_authority_certificate
           (Values.ImportCertificateAuthorityCertificateRequest.make
              ?certificateChain:(Option.map
                                   ~f:Values.CertificateChainBlob.of_json
                                   certificateChain) ~certificateAuthorityArn
              ~certificate:(Values.CertificateBodyBlob.of_json certificate)
              ()) None None])
let issue_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiPassthrough =
         flag "api-passthrough" (optional json_arg)
           ~doc:"JSON ApiPassthrough"
       and templateArn =
         flag "template-arn" (optional string) ~doc:"STRING Arn"
       and validityNotBefore =
         flag "validity-not-before" (optional json_arg) ~doc:"JSON Validity"
       and idempotencyToken =
         flag "idempotency-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn"
       and csr = flag "csr" (required json_arg) ~doc:"JSON CsrBlob"
       and signingAlgorithm =
         flag "signing-algorithm" (required json_arg)
           ~doc:"JSON SigningAlgorithm"
       and validity =
         flag "validity" (required json_arg) ~doc:"JSON Validity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.issue_certificate
           (Values.IssueCertificateRequest.make
              ?apiPassthrough:(Option.map ~f:Values.ApiPassthrough.of_json
                                 apiPassthrough) ?templateArn
              ?validityNotBefore:(Option.map ~f:Values.Validity.of_json
                                    validityNotBefore) ?idempotencyToken
              ~certificateAuthorityArn ~csr:(Values.CsrBlob.of_json csr)
              ~signingAlgorithm:(Values.SigningAlgorithm.of_json
                                   signingAlgorithm)
              ~validity:(Values.Validity.of_json validity) ())
           (Some Values.IssueCertificateResponse.to_json)
           (Some Values.IssueCertificateResponse.error_to_json)])
let list_certificate_authorities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceOwner =
         flag "resource-owner" (optional json_arg) ~doc:"JSON ResourceOwner" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_certificate_authorities
           (Values.ListCertificateAuthoritiesRequest.make ?nextToken
              ?maxResults
              ?resourceOwner:(Option.map ~f:Values.ResourceOwner.of_json
                                resourceOwner) ())
           (Some Values.ListCertificateAuthoritiesResponse.to_json)
           (Some Values.ListCertificateAuthoritiesResponse.error_to_json)])
let list_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_permissions
           (Values.ListPermissionsRequest.make ?nextToken ?maxResults
              ~certificateAuthorityArn ())
           (Some Values.ListPermissionsResponse.to_json)
           (Some Values.ListPermissionsResponse.error_to_json)])
let list_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags
           (Values.ListTagsRequest.make ?nextToken ?maxResults
              ~certificateAuthorityArn ())
           (Some Values.ListTagsResponse.to_json)
           (Some Values.ListTagsResponse.error_to_json)])
let put_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and policy = flag "policy" (required string) ~doc:"STRING AWSPolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_policy
           (Values.PutPolicyRequest.make ~resourceArn ~policy ()) None None])
let restore_certificate_authority =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_certificate_authority
           (Values.RestoreCertificateAuthorityRequest.make
              ~certificateAuthorityArn ()) None None])
let revoke_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn"
       and certificateSerial =
         flag "certificate-serial" (required string) ~doc:"STRING String128"
       and revocationReason =
         flag "revocation-reason" (required json_arg)
           ~doc:"JSON RevocationReason" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_certificate
           (Values.RevokeCertificateRequest.make ~certificateAuthorityArn
              ~certificateSerial
              ~revocationReason:(Values.RevocationReason.of_json
                                   revocationReason) ()) None None])
let tag_certificate_authority =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_certificate_authority
           (Values.TagCertificateAuthorityRequest.make
              ~certificateAuthorityArn ~tags:(Values.TagList.of_json tags) ())
           None None])
let untag_certificate_authority =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_certificate_authority
           (Values.UntagCertificateAuthorityRequest.make
              ~certificateAuthorityArn ~tags:(Values.TagList.of_json tags) ())
           None None])
let update_certificate_authority =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revocationConfiguration =
         flag "revocation-configuration" (optional json_arg)
           ~doc:"JSON RevocationConfiguration"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON CertificateAuthorityStatus"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_certificate_authority
           (Values.UpdateCertificateAuthorityRequest.make
              ?revocationConfiguration:(Option.map
                                          ~f:Values.RevocationConfiguration.of_json
                                          revocationConfiguration)
              ?status:(Option.map
                         ~f:Values.CertificateAuthorityStatus.of_json status)
              ~certificateAuthorityArn ()) None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-certificate-authority", create_certificate_authority);
    ("create-certificate-authority-audit-report",
      create_certificate_authority_audit_report);
    ("create-permission", create_permission);
    ("delete-certificate-authority", delete_certificate_authority);
    ("delete-permission", delete_permission);
    ("delete-policy", delete_policy);
    ("describe-certificate-authority", describe_certificate_authority);
    ("describe-certificate-authority-audit-report",
      describe_certificate_authority_audit_report);
    ("get-certificate", get_certificate);
    ("get-certificate-authority-certificate",
      get_certificate_authority_certificate);
    ("get-certificate-authority-csr", get_certificate_authority_csr);
    ("get-policy", get_policy);
    ("import-certificate-authority-certificate",
      import_certificate_authority_certificate);
    ("issue-certificate", issue_certificate);
    ("list-certificate-authorities", list_certificate_authorities);
    ("list-permissions", list_permissions);
    ("list-tags", list_tags);
    ("put-policy", put_policy);
    ("restore-certificate-authority", restore_certificate_authority);
    ("revoke-certificate", revoke_certificate);
    ("tag-certificate-authority", tag_certificate_authority);
    ("untag-certificate-authority", untag_certificate_authority);
    ("update-certificate-authority", update_certificate_authority)]
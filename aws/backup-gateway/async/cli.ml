(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_gateway_to_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayArn =
         flag "gateway-arn" (required string) ~doc:"STRING GatewayArn"
       and serverArn =
         flag "server-arn" (required string) ~doc:"STRING ServerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_gateway_to_server
           (Values.AssociateGatewayToServerInput.make ~gatewayArn ~serverArn
              ()) (Some Values.AssociateGatewayToServerOutput.to_json)
           (Some Values.AssociateGatewayToServerOutput.error_to_json)])
let create_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and activationKey =
         flag "activation-key" (required string) ~doc:"STRING ActivationKey"
       and gatewayDisplayName =
         flag "gateway-display-name" (required string) ~doc:"STRING Name"
       and gatewayType =
         flag "gateway-type" (required json_arg) ~doc:"JSON GatewayType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_gateway
           (Values.CreateGatewayInput.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~activationKey
              ~gatewayDisplayName
              ~gatewayType:(Values.GatewayType.of_json gatewayType) ())
           (Some Values.CreateGatewayOutput.to_json)
           (Some Values.CreateGatewayOutput.error_to_json)])
let delete_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayArn =
         flag "gateway-arn" (required string) ~doc:"STRING GatewayArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_gateway (Values.DeleteGatewayInput.make ~gatewayArn ())
           (Some Values.DeleteGatewayOutput.to_json)
           (Some Values.DeleteGatewayOutput.error_to_json)])
let delete_hypervisor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hypervisorArn =
         flag "hypervisor-arn" (required string) ~doc:"STRING ServerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_hypervisor
           (Values.DeleteHypervisorInput.make ~hypervisorArn ())
           (Some Values.DeleteHypervisorOutput.to_json)
           (Some Values.DeleteHypervisorOutput.error_to_json)])
let disassociate_gateway_from_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayArn =
         flag "gateway-arn" (required string) ~doc:"STRING GatewayArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_gateway_from_server
           (Values.DisassociateGatewayFromServerInput.make ~gatewayArn ())
           (Some Values.DisassociateGatewayFromServerOutput.to_json)
           (Some Values.DisassociateGatewayFromServerOutput.error_to_json)])
let import_hypervisor_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and password =
         flag "password" (optional string) ~doc:"STRING Password"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and username =
         flag "username" (optional string) ~doc:"STRING Username"
       and host = flag "host" (required string) ~doc:"STRING Host"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_hypervisor_configuration
           (Values.ImportHypervisorConfigurationInput.make ?kmsKeyArn
              ?password ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?username ~host ~name ())
           (Some Values.ImportHypervisorConfigurationOutput.to_json)
           (Some Values.ImportHypervisorConfigurationOutput.error_to_json)])
let list_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_gateways
           (Values.ListGatewaysInput.make ?maxResults ?nextToken ())
           (Some Values.ListGatewaysOutput.to_json)
           (Some Values.ListGatewaysOutput.error_to_json)])
let list_hypervisors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_hypervisors
           (Values.ListHypervisorsInput.make ?maxResults ?nextToken ())
           (Some Values.ListHypervisorsOutput.to_json)
           (Some Values.ListHypervisorsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_virtual_machines =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_virtual_machines
           (Values.ListVirtualMachinesInput.make ?maxResults ?nextToken ())
           (Some Values.ListVirtualMachinesOutput.to_json)
           (Some Values.ListVirtualMachinesOutput.error_to_json)])
let put_maintenance_start_time =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dayOfMonth =
         flag "day-of-month" (optional int) ~doc:"INT DayOfMonth"
       and dayOfWeek = flag "day-of-week" (optional int) ~doc:"INT DayOfWeek"
       and gatewayArn =
         flag "gateway-arn" (required string) ~doc:"STRING GatewayArn"
       and hourOfDay = flag "hour-of-day" (required int) ~doc:"INT HourOfDay"
       and minuteOfHour =
         flag "minute-of-hour" (required int) ~doc:"INT MinuteOfHour" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_maintenance_start_time
           (Values.PutMaintenanceStartTimeInput.make ?dayOfMonth ?dayOfWeek
              ~gatewayArn ~hourOfDay ~minuteOfHour ())
           (Some Values.PutMaintenanceStartTimeOutput.to_json)
           (Some Values.PutMaintenanceStartTimeOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceARN
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let test_hypervisor_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and password =
         flag "password" (optional string) ~doc:"STRING Password"
       and username =
         flag "username" (optional string) ~doc:"STRING Username"
       and gatewayArn =
         flag "gateway-arn" (required string) ~doc:"STRING GatewayArn"
       and host = flag "host" (required string) ~doc:"STRING Host" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_hypervisor_configuration
           (Values.TestHypervisorConfigurationInput.make ?password ?username
              ~gatewayArn ~host ())
           (Some Values.TestHypervisorConfigurationOutput.to_json)
           (Some Values.TestHypervisorConfigurationOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceArn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceARN
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_gateway_information =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayDisplayName =
         flag "gateway-display-name" (optional string) ~doc:"STRING Name"
       and gatewayArn =
         flag "gateway-arn" (required string) ~doc:"STRING GatewayArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_gateway_information
           (Values.UpdateGatewayInformationInput.make ?gatewayDisplayName
              ~gatewayArn ())
           (Some Values.UpdateGatewayInformationOutput.to_json)
           (Some Values.UpdateGatewayInformationOutput.error_to_json)])
let update_hypervisor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and host = flag "host" (optional string) ~doc:"STRING Host"
       and password =
         flag "password" (optional string) ~doc:"STRING Password"
       and username =
         flag "username" (optional string) ~doc:"STRING Username"
       and hypervisorArn =
         flag "hypervisor-arn" (required string) ~doc:"STRING ServerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_hypervisor
           (Values.UpdateHypervisorInput.make ?host ?password ?username
              ~hypervisorArn ()) (Some Values.UpdateHypervisorOutput.to_json)
           (Some Values.UpdateHypervisorOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-gateway-to-server", associate_gateway_to_server);
    ("create-gateway", create_gateway);
    ("delete-gateway", delete_gateway);
    ("delete-hypervisor", delete_hypervisor);
    ("disassociate-gateway-from-server", disassociate_gateway_from_server);
    ("import-hypervisor-configuration", import_hypervisor_configuration);
    ("list-gateways", list_gateways);
    ("list-hypervisors", list_hypervisors);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-virtual-machines", list_virtual_machines);
    ("put-maintenance-start-time", put_maintenance_start_time);
    ("tag-resource", tag_resource);
    ("test-hypervisor-configuration", test_hypervisor_configuration);
    ("untag-resource", untag_resource);
    ("update-gateway-information", update_gateway_information);
    ("update-hypervisor", update_hypervisor)]
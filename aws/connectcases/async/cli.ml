(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_get_case_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and caseRules =
         flag "case-rules" (required json_arg)
           ~doc:"JSON CaseRuleIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_case_rule
           (Values.BatchGetCaseRuleRequest.make ~domainId
              ~caseRules:(Values.CaseRuleIdentifierList.of_json caseRules) ())
           (Some Values.BatchGetCaseRuleResponse.to_json)
           (Some Values.BatchGetCaseRuleResponse.error_to_json)])
let batch_get_field =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and fields =
         flag "fields" (required json_arg)
           ~doc:"JSON BatchGetFieldIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_field
           (Values.BatchGetFieldRequest.make ~domainId
              ~fields:(Values.BatchGetFieldIdentifierList.of_json fields) ())
           (Some Values.BatchGetFieldResponse.to_json)
           (Some Values.BatchGetFieldResponse.error_to_json)])
let batch_put_field_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and fieldId = flag "field-id" (required string) ~doc:"STRING FieldId"
       and options =
         flag "options" (required json_arg)
           ~doc:"JSON BatchPutFieldOptionsRequestOptionsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_put_field_options
           (Values.BatchPutFieldOptionsRequest.make ~domainId ~fieldId
              ~options:(Values.BatchPutFieldOptionsRequestOptionsList.of_json
                          options) ())
           (Some Values.BatchPutFieldOptionsResponse.to_json)
           (Some Values.BatchPutFieldOptionsResponse.error_to_json)])
let create_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING CreateCaseRequestClientTokenString"
       and performedBy =
         flag "performed-by" (optional json_arg) ~doc:"JSON UserUnion"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON MutableTags"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and templateId =
         flag "template-id" (required string) ~doc:"STRING TemplateId"
       and fields =
         flag "fields" (required json_arg)
           ~doc:"JSON CreateCaseRequestFieldsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_case
           (Values.CreateCaseRequest.make ?clientToken
              ?performedBy:(Option.map ~f:Values.UserUnion.of_json
                              performedBy)
              ?tags:(Option.map ~f:Values.MutableTags.of_json tags) ~domainId
              ~templateId
              ~fields:(Values.CreateCaseRequestFieldsList.of_json fields) ())
           (Some Values.CreateCaseResponse.to_json)
           (Some Values.CreateCaseResponse.error_to_json)])
let create_case_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CaseRuleDescription"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING CaseRuleName"
       and rule = flag "rule" (required json_arg) ~doc:"JSON CaseRuleDetails" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_case_rule
           (Values.CreateCaseRuleRequest.make ?description ~domainId ~name
              ~rule:(Values.CaseRuleDetails.of_json rule) ())
           (Some Values.CreateCaseRuleResponse.to_json)
           (Some Values.CreateCaseRuleResponse.error_to_json)])
let create_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain (Values.CreateDomainRequest.make ~name ())
           (Some Values.CreateDomainResponse.to_json)
           (Some Values.CreateDomainResponse.error_to_json)])
let create_field =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING FieldDescription"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON FieldAttributes"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING FieldName"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON FieldType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_field
           (Values.CreateFieldRequest.make ?description
              ?attributes:(Option.map ~f:Values.FieldAttributes.of_json
                             attributes) ~domainId ~name
              ~type_:(Values.FieldType.of_json type_) ())
           (Some Values.CreateFieldResponse.to_json)
           (Some Values.CreateFieldResponse.error_to_json)])
let create_layout =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING LayoutName"
       and content =
         flag "content" (required json_arg) ~doc:"JSON LayoutContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_layout
           (Values.CreateLayoutRequest.make ~domainId ~name
              ~content:(Values.LayoutContent.of_json content) ())
           (Some Values.CreateLayoutResponse.to_json)
           (Some Values.CreateLayoutResponse.error_to_json)])
let create_related_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and performedBy =
         flag "performed-by" (optional json_arg) ~doc:"JSON UserUnion"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON RelatedItemType"
       and content =
         flag "content" (required json_arg)
           ~doc:"JSON RelatedItemInputContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_related_item
           (Values.CreateRelatedItemRequest.make
              ?performedBy:(Option.map ~f:Values.UserUnion.of_json
                              performedBy) ~domainId ~caseId
              ~type_:(Values.RelatedItemType.of_json type_)
              ~content:(Values.RelatedItemInputContent.of_json content) ())
           (Some Values.CreateRelatedItemResponse.to_json)
           (Some Values.CreateRelatedItemResponse.error_to_json)])
let create_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING TemplateDescription"
       and layoutConfiguration =
         flag "layout-configuration" (optional json_arg)
           ~doc:"JSON LayoutConfiguration"
       and requiredFields =
         flag "required-fields" (optional json_arg)
           ~doc:"JSON RequiredFieldList"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON TemplateStatus"
       and rules =
         flag "rules" (optional json_arg) ~doc:"JSON TemplateCaseRuleList"
       and tagPropagationConfigurations =
         flag "tag-propagation-configurations" (optional json_arg)
           ~doc:"JSON TagPropagationConfigurationList"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_template
           (Values.CreateTemplateRequest.make ?description
              ?layoutConfiguration:(Option.map
                                      ~f:Values.LayoutConfiguration.of_json
                                      layoutConfiguration)
              ?requiredFields:(Option.map ~f:Values.RequiredFieldList.of_json
                                 requiredFields)
              ?status:(Option.map ~f:Values.TemplateStatus.of_json status)
              ?rules:(Option.map ~f:Values.TemplateCaseRuleList.of_json rules)
              ?tagPropagationConfigurations:(Option.map
                                               ~f:Values.TagPropagationConfigurationList.of_json
                                               tagPropagationConfigurations)
              ~domainId ~name ())
           (Some Values.CreateTemplateResponse.to_json)
           (Some Values.CreateTemplateResponse.error_to_json)])
let delete_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_case
           (Values.DeleteCaseRequest.make ~domainId ~caseId ())
           (Some Values.DeleteCaseResponse.to_json)
           (Some Values.DeleteCaseResponse.error_to_json)])
let delete_case_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and caseRuleId =
         flag "case-rule-id" (required string) ~doc:"STRING CaseRuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_case_rule
           (Values.DeleteCaseRuleRequest.make ~domainId ~caseRuleId ())
           (Some Values.DeleteCaseRuleResponse.to_json)
           (Some Values.DeleteCaseRuleResponse.error_to_json)])
let delete_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain (Values.DeleteDomainRequest.make ~domainId ())
           (Some Values.DeleteDomainResponse.to_json)
           (Some Values.DeleteDomainResponse.error_to_json)])
let delete_field =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and fieldId = flag "field-id" (required string) ~doc:"STRING FieldId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_field
           (Values.DeleteFieldRequest.make ~domainId ~fieldId ())
           (Some Values.DeleteFieldResponse.to_json)
           (Some Values.DeleteFieldResponse.error_to_json)])
let delete_layout =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and layoutId =
         flag "layout-id" (required string) ~doc:"STRING LayoutId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_layout
           (Values.DeleteLayoutRequest.make ~domainId ~layoutId ())
           (Some Values.DeleteLayoutResponse.to_json)
           (Some Values.DeleteLayoutResponse.error_to_json)])
let delete_related_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId"
       and relatedItemId =
         flag "related-item-id" (required string) ~doc:"STRING RelatedItemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_related_item
           (Values.DeleteRelatedItemRequest.make ~domainId ~caseId
              ~relatedItemId ())
           (Some Values.DeleteRelatedItemResponse.to_json)
           (Some Values.DeleteRelatedItemResponse.error_to_json)])
let delete_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and templateId =
         flag "template-id" (required string) ~doc:"STRING TemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_template
           (Values.DeleteTemplateRequest.make ~domainId ~templateId ())
           (Some Values.DeleteTemplateResponse.to_json)
           (Some Values.DeleteTemplateResponse.error_to_json)])
let get_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and fields =
         flag "fields" (required json_arg)
           ~doc:"JSON GetCaseRequestFieldsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_case
           (Values.GetCaseRequest.make ?nextToken ~caseId ~domainId
              ~fields:(Values.GetCaseRequestFieldsList.of_json fields) ())
           (Some Values.GetCaseResponse.to_json)
           (Some Values.GetCaseResponse.error_to_json)])
let get_case_audit_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT GetCaseAuditEventsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_case_audit_events
           (Values.GetCaseAuditEventsRequest.make ?maxResults ?nextToken
              ~caseId ~domainId ())
           (Some Values.GetCaseAuditEventsResponse.to_json)
           (Some Values.GetCaseAuditEventsResponse.error_to_json)])
let get_case_event_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_case_event_configuration
           (Values.GetCaseEventConfigurationRequest.make ~domainId ())
           (Some Values.GetCaseEventConfigurationResponse.to_json)
           (Some Values.GetCaseEventConfigurationResponse.error_to_json)])
let get_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain (Values.GetDomainRequest.make ~domainId ())
           (Some Values.GetDomainResponse.to_json)
           (Some Values.GetDomainResponse.error_to_json)])
let get_layout =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and layoutId =
         flag "layout-id" (required string) ~doc:"STRING LayoutId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_layout
           (Values.GetLayoutRequest.make ~domainId ~layoutId ())
           (Some Values.GetLayoutResponse.to_json)
           (Some Values.GetLayoutResponse.error_to_json)])
let get_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and templateId =
         flag "template-id" (required string) ~doc:"STRING TemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_template
           (Values.GetTemplateRequest.make ~domainId ~templateId ())
           (Some Values.GetTemplateResponse.to_json)
           (Some Values.GetTemplateResponse.error_to_json)])
let list_case_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_case_rules
           (Values.ListCaseRulesRequest.make ?maxResults ?nextToken ~domainId
              ()) (Some Values.ListCaseRulesResponse.to_json)
           (Some Values.ListCaseRulesResponse.error_to_json)])
let list_cases_for_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListCasesForContactRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and contactArn =
         flag "contact-arn" (required string) ~doc:"STRING ContactArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_cases_for_contact
           (Values.ListCasesForContactRequest.make ?maxResults ?nextToken
              ~domainId ~contactArn ())
           (Some Values.ListCasesForContactResponse.to_json)
           (Some Values.ListCasesForContactResponse.error_to_json)])
let list_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListDomainsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domains
           (Values.ListDomainsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListDomainsResponse.to_json)
           (Some Values.ListDomainsResponse.error_to_json)])
let list_field_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and values = flag "values" (optional json_arg) ~doc:"JSON ValuesList"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and fieldId = flag "field-id" (required string) ~doc:"STRING FieldId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_field_options
           (Values.ListFieldOptionsRequest.make ?maxResults ?nextToken
              ?values:(Option.map ~f:Values.ValuesList.of_json values)
              ~domainId ~fieldId ())
           (Some Values.ListFieldOptionsResponse.to_json)
           (Some Values.ListFieldOptionsResponse.error_to_json)])
let list_fields =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_fields
           (Values.ListFieldsRequest.make ?maxResults ?nextToken ~domainId ())
           (Some Values.ListFieldsResponse.to_json)
           (Some Values.ListFieldsResponse.error_to_json)])
let list_layouts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_layouts
           (Values.ListLayoutsRequest.make ?maxResults ?nextToken ~domainId
              ()) (Some Values.ListLayoutsResponse.to_json)
           (Some Values.ListLayoutsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~arn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON TemplateStatusFilters"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_templates
           (Values.ListTemplatesRequest.make ?maxResults ?nextToken
              ?status:(Option.map ~f:Values.TemplateStatusFilters.of_json
                         status) ~domainId ())
           (Some Values.ListTemplatesResponse.to_json)
           (Some Values.ListTemplatesResponse.error_to_json)])
let put_case_event_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and eventBridge =
         flag "event-bridge" (required json_arg)
           ~doc:"JSON EventBridgeConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_case_event_configuration
           (Values.PutCaseEventConfigurationRequest.make ~domainId
              ~eventBridge:(Values.EventBridgeConfiguration.of_json
                              eventBridge) ())
           (Some Values.PutCaseEventConfigurationResponse.to_json)
           (Some Values.PutCaseEventConfigurationResponse.error_to_json)])
let search_all_related_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT SearchAllRelatedItemsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON SearchAllRelatedItemsRequestFiltersList"
       and sorts =
         flag "sorts" (optional json_arg)
           ~doc:"JSON SearchAllRelatedItemsRequestSortsList"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_all_related_items
           (Values.SearchAllRelatedItemsRequest.make ?maxResults ?nextToken
              ?filters:(Option.map
                          ~f:Values.SearchAllRelatedItemsRequestFiltersList.of_json
                          filters)
              ?sorts:(Option.map
                        ~f:Values.SearchAllRelatedItemsRequestSortsList.of_json
                        sorts) ~domainId ())
           (Some Values.SearchAllRelatedItemsResponse.to_json)
           (Some Values.SearchAllRelatedItemsResponse.error_to_json)])
let search_cases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT SearchCasesRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and searchTerm =
         flag "search-term" (optional string)
           ~doc:"STRING SearchCasesRequestSearchTermString"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON CaseFilter"
       and sorts =
         flag "sorts" (optional json_arg)
           ~doc:"JSON SearchCasesRequestSortsList"
       and fields =
         flag "fields" (optional json_arg)
           ~doc:"JSON SearchCasesRequestFieldsList"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_cases
           (Values.SearchCasesRequest.make ?maxResults ?nextToken ?searchTerm
              ?filter:(Option.map ~f:Values.CaseFilter.of_json filter)
              ?sorts:(Option.map
                        ~f:Values.SearchCasesRequestSortsList.of_json sorts)
              ?fields:(Option.map
                         ~f:Values.SearchCasesRequestFieldsList.of_json
                         fields) ~domainId ())
           (Some Values.SearchCasesResponse.to_json)
           (Some Values.SearchCasesResponse.error_to_json)])
let search_related_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT SearchRelatedItemsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON SearchRelatedItemsRequestFiltersList"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_related_items
           (Values.SearchRelatedItemsRequest.make ?maxResults ?nextToken
              ?filters:(Option.map
                          ~f:Values.SearchRelatedItemsRequestFiltersList.of_json
                          filters) ~domainId ~caseId ())
           (Some Values.SearchRelatedItemsResponse.to_json)
           (Some Values.SearchRelatedItemsResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~arn
              ~tags:(Values.Tags.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~arn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and performedBy =
         flag "performed-by" (optional json_arg) ~doc:"JSON UserUnion"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId"
       and fields =
         flag "fields" (required json_arg)
           ~doc:"JSON UpdateCaseRequestFieldsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_case
           (Values.UpdateCaseRequest.make
              ?performedBy:(Option.map ~f:Values.UserUnion.of_json
                              performedBy) ~domainId ~caseId
              ~fields:(Values.UpdateCaseRequestFieldsList.of_json fields) ())
           (Some Values.UpdateCaseResponse.to_json)
           (Some Values.UpdateCaseResponse.error_to_json)])
let update_case_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING CaseRuleName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CaseRuleDescription"
       and rule = flag "rule" (optional json_arg) ~doc:"JSON CaseRuleDetails"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and caseRuleId =
         flag "case-rule-id" (required string) ~doc:"STRING CaseRuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_case_rule
           (Values.UpdateCaseRuleRequest.make ?name ?description
              ?rule:(Option.map ~f:Values.CaseRuleDetails.of_json rule)
              ~domainId ~caseRuleId ())
           (Some Values.UpdateCaseRuleResponse.to_json)
           (Some Values.UpdateCaseRuleResponse.error_to_json)])
let update_field =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING FieldName"
       and description =
         flag "description" (optional string) ~doc:"STRING FieldDescription"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON FieldAttributes"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and fieldId = flag "field-id" (required string) ~doc:"STRING FieldId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_field
           (Values.UpdateFieldRequest.make ?name ?description
              ?attributes:(Option.map ~f:Values.FieldAttributes.of_json
                             attributes) ~domainId ~fieldId ())
           (Some Values.UpdateFieldResponse.to_json)
           (Some Values.UpdateFieldResponse.error_to_json)])
let update_layout =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING LayoutName"
       and content =
         flag "content" (optional json_arg) ~doc:"JSON LayoutContent"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and layoutId =
         flag "layout-id" (required string) ~doc:"STRING LayoutId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_layout
           (Values.UpdateLayoutRequest.make ?name
              ?content:(Option.map ~f:Values.LayoutContent.of_json content)
              ~domainId ~layoutId ())
           (Some Values.UpdateLayoutResponse.to_json)
           (Some Values.UpdateLayoutResponse.error_to_json)])
let update_related_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and performedBy =
         flag "performed-by" (optional json_arg) ~doc:"JSON UserUnion"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId"
       and relatedItemId =
         flag "related-item-id" (required string) ~doc:"STRING RelatedItemId"
       and content =
         flag "content" (required json_arg)
           ~doc:"JSON RelatedItemUpdateContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_related_item
           (Values.UpdateRelatedItemRequest.make
              ?performedBy:(Option.map ~f:Values.UserUnion.of_json
                              performedBy) ~domainId ~caseId ~relatedItemId
              ~content:(Values.RelatedItemUpdateContent.of_json content) ())
           (Some Values.UpdateRelatedItemResponse.to_json)
           (Some Values.UpdateRelatedItemResponse.error_to_json)])
let update_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING TemplateName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING TemplateDescription"
       and layoutConfiguration =
         flag "layout-configuration" (optional json_arg)
           ~doc:"JSON LayoutConfiguration"
       and requiredFields =
         flag "required-fields" (optional json_arg)
           ~doc:"JSON RequiredFieldList"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON TemplateStatus"
       and rules =
         flag "rules" (optional json_arg) ~doc:"JSON TemplateCaseRuleList"
       and tagPropagationConfigurations =
         flag "tag-propagation-configurations" (optional json_arg)
           ~doc:"JSON TagPropagationConfigurationList"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and templateId =
         flag "template-id" (required string) ~doc:"STRING TemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_template
           (Values.UpdateTemplateRequest.make ?name ?description
              ?layoutConfiguration:(Option.map
                                      ~f:Values.LayoutConfiguration.of_json
                                      layoutConfiguration)
              ?requiredFields:(Option.map ~f:Values.RequiredFieldList.of_json
                                 requiredFields)
              ?status:(Option.map ~f:Values.TemplateStatus.of_json status)
              ?rules:(Option.map ~f:Values.TemplateCaseRuleList.of_json rules)
              ?tagPropagationConfigurations:(Option.map
                                               ~f:Values.TagPropagationConfigurationList.of_json
                                               tagPropagationConfigurations)
              ~domainId ~templateId ())
           (Some Values.UpdateTemplateResponse.to_json)
           (Some Values.UpdateTemplateResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-get-case-rule", batch_get_case_rule);
    ("batch-get-field", batch_get_field);
    ("batch-put-field-options", batch_put_field_options);
    ("create-case", create_case);
    ("create-case-rule", create_case_rule);
    ("create-domain", create_domain);
    ("create-field", create_field);
    ("create-layout", create_layout);
    ("create-related-item", create_related_item);
    ("create-template", create_template);
    ("delete-case", delete_case);
    ("delete-case-rule", delete_case_rule);
    ("delete-domain", delete_domain);
    ("delete-field", delete_field);
    ("delete-layout", delete_layout);
    ("delete-related-item", delete_related_item);
    ("delete-template", delete_template);
    ("get-case", get_case);
    ("get-case-audit-events", get_case_audit_events);
    ("get-case-event-configuration", get_case_event_configuration);
    ("get-domain", get_domain);
    ("get-layout", get_layout);
    ("get-template", get_template);
    ("list-case-rules", list_case_rules);
    ("list-cases-for-contact", list_cases_for_contact);
    ("list-domains", list_domains);
    ("list-field-options", list_field_options);
    ("list-fields", list_fields);
    ("list-layouts", list_layouts);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-templates", list_templates);
    ("put-case-event-configuration", put_case_event_configuration);
    ("search-all-related-items", search_all_related_items);
    ("search-cases", search_cases);
    ("search-related-items", search_related_items);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-case", update_case);
    ("update-case-rule", update_case_rule);
    ("update-field", update_field);
    ("update-layout", update_layout);
    ("update-related-item", update_related_item);
    ("update-template", update_template)]
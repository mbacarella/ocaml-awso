(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let delete_report_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportId =
         flag "report-id" (required string) ~doc:"STRING ReportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_report_definition
           (Values.DeleteReportDefinitionRequest.make ~reportId ())
           (Some Values.DeleteReportDefinitionResult.to_json)
           (Some Values.DeleteReportDefinitionResult.error_to_json)])
let get_report_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportId =
         flag "report-id" (required string) ~doc:"STRING ReportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_report_definition
           (Values.GetReportDefinitionRequest.make ~reportId ())
           (Some Values.GetReportDefinitionResult.to_json)
           (Some Values.GetReportDefinitionResult.error_to_json)])
let import_application_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceS3Location =
         flag "source-s3-location" (required json_arg)
           ~doc:"JSON SourceS3Location" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_application_usage
           (Values.ImportApplicationUsageRequest.make
              ~sourceS3Location:(Values.SourceS3Location.of_json
                                   sourceS3Location) ())
           (Some Values.ImportApplicationUsageResult.to_json)
           (Some Values.ImportApplicationUsageResult.error_to_json)])
let list_report_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_report_definitions
           (Values.ListReportDefinitionsRequest.make ?nextToken ?maxResults
              ()) (Some Values.ListReportDefinitionsResult.to_json)
           (Some Values.ListReportDefinitionsResult.error_to_json)])
let put_report_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportId =
         flag "report-id" (required string) ~doc:"STRING ReportId"
       and reportDescription =
         flag "report-description" (required string)
           ~doc:"STRING ReportDescription"
       and reportFrequency =
         flag "report-frequency" (required json_arg)
           ~doc:"JSON ReportFrequency"
       and format = flag "format" (required json_arg) ~doc:"JSON Format"
       and destinationS3Location =
         flag "destination-s3-location" (required json_arg)
           ~doc:"JSON S3Location" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_report_definition
           (Values.PutReportDefinitionRequest.make ~reportId
              ~reportDescription
              ~reportFrequency:(Values.ReportFrequency.of_json
                                  reportFrequency)
              ~format:(Values.Format_.of_json format)
              ~destinationS3Location:(Values.S3Location.of_json
                                        destinationS3Location) ())
           (Some Values.PutReportDefinitionResult.to_json)
           (Some Values.PutReportDefinitionResult.error_to_json)])
let update_report_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportId =
         flag "report-id" (required string) ~doc:"STRING ReportId"
       and reportDescription =
         flag "report-description" (required string)
           ~doc:"STRING ReportDescription"
       and reportFrequency =
         flag "report-frequency" (required json_arg)
           ~doc:"JSON ReportFrequency"
       and format = flag "format" (required json_arg) ~doc:"JSON Format"
       and destinationS3Location =
         flag "destination-s3-location" (required json_arg)
           ~doc:"JSON S3Location" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_report_definition
           (Values.UpdateReportDefinitionRequest.make ~reportId
              ~reportDescription
              ~reportFrequency:(Values.ReportFrequency.of_json
                                  reportFrequency)
              ~format:(Values.Format_.of_json format)
              ~destinationS3Location:(Values.S3Location.of_json
                                        destinationS3Location) ())
           (Some Values.UpdateReportDefinitionResult.to_json)
           (Some Values.UpdateReportDefinitionResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("delete-report-definition", delete_report_definition);
    ("get-report-definition", get_report_definition);
    ("import-application-usage", import_application_usage);
    ("list-report-definitions", list_report_definitions);
    ("put-report-definition", put_report_definition);
    ("update-report-definition", update_report_definition)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.elb
let apiVersion = "2012-06-01"
let endpointPrefix = "elasticloadbalancing"
let serviceFullName = "Elastic Load Balancing"
let signatureVersion = "v4"
let protocol = "query"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let xmlNamespace =
  "http://elasticloadbalancing.amazonaws.com/doc/2012-06-01/"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module PolicyName =
  struct
    type nonrec t = string
    let context_ = "PolicyName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PolicyName" j
    let to_json = simple_to_json to_value
  end
module AccessPointPort =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for AccessPointPort" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module InstancePort =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:65535) >>=
             (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for InstancePort" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Protocol =
  struct
    type nonrec t = string
    let context_ = "Protocol"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Protocol" j
    let to_json = simple_to_json to_value
  end
module SSLCertificateId =
  struct
    type nonrec t = string
    let context_ = "SSLCertificateId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SSLCertificateId" j
    let to_json = simple_to_json to_value
  end
module CookieName =
  struct
    type nonrec t = string
    let context_ = "CookieName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CookieName" j
    let to_json = simple_to_json to_value
  end
module CookieExpirationPeriod =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module AttributeName =
  struct
    type nonrec t = string
    let context_ = "AttributeName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttributeName" j
    let to_json = simple_to_json to_value
  end
module AttributeType =
  struct
    type nonrec t = string
    let context_ = "AttributeType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttributeType" j
    let to_json = simple_to_json to_value
  end
module Cardinality =
  struct
    type nonrec t = string
    let context_ = "Cardinality"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Cardinality" j
    let to_json = simple_to_json to_value
  end
module DefaultValue =
  struct
    type nonrec t = string
    let context_ = "DefaultValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DefaultValue" j
    let to_json = simple_to_json to_value
  end
module Description =
  struct
    type nonrec t = string
    let context_ = "Description"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Description" j
    let to_json = simple_to_json to_value
  end
module AttributeValue =
  struct
    type nonrec t = string
    let context_ = "AttributeValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttributeValue" j
    let to_json = simple_to_json to_value
  end
module PolicyNames =
  struct
    type nonrec t = PolicyName.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PolicyName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PolicyName.of_xml)
    let of_json j =
      list_of_json ~kind:"PolicyNames" ~of_json:PolicyName.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceId =
  struct
    type nonrec t = string
    let context_ = "InstanceId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InstanceId" j
    let to_json = simple_to_json to_value
  end
module Listener =
  struct
    type nonrec t =
      {
      protocol: Protocol.t
        [@ocaml.doc
          "The load balancer transport protocol to use for routing: HTTP, HTTPS, TCP, or SSL."];
      loadBalancerPort: AccessPointPort.t
        [@ocaml.doc
          "The port on which the load balancer is listening. On EC2-VPC, you can specify any port from the range 1-65535. On EC2-Classic, you can specify any port from the following list: 25, 80, 443, 465, 587, 1024-65535."];
      instanceProtocol: Protocol.t option
        [@ocaml.doc
          "The protocol to use for routing traffic to instances: HTTP, HTTPS, TCP, or SSL. If the front-end protocol is TCP or SSL, the back-end protocol must be TCP or SSL. If the front-end protocol is HTTP or HTTPS, the back-end protocol must be HTTP or HTTPS. If there is another listener with the same InstancePort whose InstanceProtocol is secure, (HTTPS or SSL), the listener's InstanceProtocol must also be secure. If there is another listener with the same InstancePort whose InstanceProtocol is HTTP or TCP, the listener's InstanceProtocol must be HTTP or TCP."];
      instancePort: InstancePort.t
        [@ocaml.doc "The port on which the instance is listening."];
      sSLCertificateId: SSLCertificateId.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the server certificate."]}
    let context_ = "Listener"
    let make ?instanceProtocol =
      fun ?sSLCertificateId ->
        fun ~protocol ->
          fun ~loadBalancerPort ->
            fun ~instancePort ->
              fun () ->
                {
                  instanceProtocol;
                  sSLCertificateId;
                  protocol;
                  loadBalancerPort;
                  instancePort
                }
    let to_value x =
      structure_to_value
        [("Protocol", (Some (Protocol.to_value x.protocol)));
        ("LoadBalancerPort",
          (Some (AccessPointPort.to_value x.loadBalancerPort)));
        ("InstanceProtocol",
          (Option.map x.instanceProtocol ~f:Protocol.to_value));
        ("InstancePort", (Some (InstancePort.to_value x.instancePort)));
        ("SSLCertificateId",
          (Option.map x.sSLCertificateId ~f:SSLCertificateId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sSLCertificateId =
        (Option.map ~f:SSLCertificateId.of_xml)
          (Xml.child xml_arg0 "SSLCertificateId") in
      let instancePort =
        InstancePort.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstancePort") in
      let instanceProtocol =
        (Option.map ~f:Protocol.of_xml)
          (Xml.child xml_arg0 "InstanceProtocol") in
      let loadBalancerPort =
        AccessPointPort.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerPort") in
      let protocol =
        Protocol.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Protocol") in
      make ?sSLCertificateId ~instancePort ?instanceProtocol
        ~loadBalancerPort ~protocol ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sSLCertificateId =
        field_map json__ "SSLCertificateId" SSLCertificateId.of_json in
      let instancePort =
        field_map_exn json__ "InstancePort" InstancePort.of_json in
      let instanceProtocol =
        field_map json__ "InstanceProtocol" Protocol.of_json in
      let loadBalancerPort =
        field_map_exn json__ "LoadBalancerPort" AccessPointPort.of_json in
      let protocol = field_map_exn json__ "Protocol" Protocol.of_json in
      make ?sSLCertificateId ~instancePort ?instanceProtocol
        ~loadBalancerPort ~protocol ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a listener. For information about the protocols and the ports supported by Elastic Load Balancing, see Listeners for Your Classic Load Balancer in the Classic Load Balancers Guide."]
module AppCookieStickinessPolicy =
  struct
    type nonrec t =
      {
      policyName: PolicyName.t option
        [@ocaml.doc
          "The mnemonic name for the policy being created. The name must be unique within a set of policies for this load balancer."];
      cookieName: CookieName.t option
        [@ocaml.doc
          "The name of the application cookie used for stickiness."]}
    let make ?policyName =
      fun ?cookieName -> fun () -> { policyName; cookieName }
    let to_value x =
      structure_to_value
        [("PolicyName", (Option.map x.policyName ~f:PolicyName.to_value));
        ("CookieName", (Option.map x.cookieName ~f:CookieName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cookieName =
        (Option.map ~f:CookieName.of_xml) (Xml.child xml_arg0 "CookieName") in
      let policyName =
        (Option.map ~f:PolicyName.of_xml) (Xml.child xml_arg0 "PolicyName") in
      make ?cookieName ?policyName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cookieName = field_map json__ "CookieName" CookieName.of_json in
      let policyName = field_map json__ "PolicyName" PolicyName.of_json in
      make ?cookieName ?policyName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a policy for application-controlled session stickiness."]
module LBCookieStickinessPolicy =
  struct
    type nonrec t =
      {
      policyName: PolicyName.t option
        [@ocaml.doc
          "The name of the policy. This name must be unique within the set of policies for this load balancer."];
      cookieExpirationPeriod: CookieExpirationPeriod.t option
        [@ocaml.doc
          "The time period, in seconds, after which the cookie should be considered stale. If this parameter is not specified, the stickiness session lasts for the duration of the browser session."]}
    let make ?policyName =
      fun ?cookieExpirationPeriod ->
        fun () -> { policyName; cookieExpirationPeriod }
    let to_value x =
      structure_to_value
        [("PolicyName", (Option.map x.policyName ~f:PolicyName.to_value));
        ("CookieExpirationPeriod",
          (Option.map x.cookieExpirationPeriod
             ~f:CookieExpirationPeriod.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cookieExpirationPeriod =
        (Option.map ~f:CookieExpirationPeriod.of_xml)
          (Xml.child xml_arg0 "CookieExpirationPeriod") in
      let policyName =
        (Option.map ~f:PolicyName.of_xml) (Xml.child xml_arg0 "PolicyName") in
      make ?cookieExpirationPeriod ?policyName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cookieExpirationPeriod =
        field_map json__ "CookieExpirationPeriod"
          CookieExpirationPeriod.of_json in
      let policyName = field_map json__ "PolicyName" PolicyName.of_json in
      make ?cookieExpirationPeriod ?policyName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a policy for duration-based session stickiness."]
module AdditionalAttributeKey =
  struct
    type nonrec t = string
    let context_ = "AdditionalAttributeKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9.]+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AdditionalAttributeKey" j
    let to_json = simple_to_json to_value
  end
module AdditionalAttributeValue =
  struct
    type nonrec t = string
    let context_ = "AdditionalAttributeValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9.]+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AdditionalAttributeValue" j
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t =
      {
      key: TagKey.t [@ocaml.doc "The key of the tag."];
      value: TagValue.t option [@ocaml.doc "The value of the tag."]}
    let context_ = "Tag"
    let make ?value = fun ~key -> fun () -> { value; key }
    let to_value x =
      structure_to_value
        [("Key", (Some (TagKey.to_value x.key)));
        ("Value", (Option.map x.value ~f:TagValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:TagValue.of_xml) (Xml.child xml_arg0 "Value") in
      let key =
        TagKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Key") in
      make ?value ~key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "Value" TagValue.of_json in
      let key = field_map_exn json__ "Key" TagKey.of_json in
      make ?value ~key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a tag."]
module PolicyAttributeTypeDescription =
  struct
    type nonrec t =
      {
      attributeName: AttributeName.t option
        [@ocaml.doc "The name of the attribute."];
      attributeType: AttributeType.t option
        [@ocaml.doc
          "The type of the attribute. For example, Boolean or Integer."];
      description: Description.t option
        [@ocaml.doc "A description of the attribute."];
      defaultValue: DefaultValue.t option
        [@ocaml.doc "The default value of the attribute, if applicable."];
      cardinality: Cardinality.t option
        [@ocaml.doc
          "The cardinality of the attribute. Valid values: ONE(1) : Single value required ZERO_OR_ONE(0..1) : Up to one value is allowed ZERO_OR_MORE(0..*) : Optional. Multiple values are allowed ONE_OR_MORE(1..*0) : Required. Multiple values are allowed"]}
    let make ?attributeName =
      fun ?attributeType ->
        fun ?description ->
          fun ?defaultValue ->
            fun ?cardinality ->
              fun () ->
                {
                  attributeName;
                  attributeType;
                  description;
                  defaultValue;
                  cardinality
                }
    let to_value x =
      structure_to_value
        [("AttributeName",
           (Option.map x.attributeName ~f:AttributeName.to_value));
        ("AttributeType",
          (Option.map x.attributeType ~f:AttributeType.to_value));
        ("Description", (Option.map x.description ~f:Description.to_value));
        ("DefaultValue",
          (Option.map x.defaultValue ~f:DefaultValue.to_value));
        ("Cardinality", (Option.map x.cardinality ~f:Cardinality.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cardinality =
        (Option.map ~f:Cardinality.of_xml) (Xml.child xml_arg0 "Cardinality") in
      let defaultValue =
        (Option.map ~f:DefaultValue.of_xml)
          (Xml.child xml_arg0 "DefaultValue") in
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      let attributeType =
        (Option.map ~f:AttributeType.of_xml)
          (Xml.child xml_arg0 "AttributeType") in
      let attributeName =
        (Option.map ~f:AttributeName.of_xml)
          (Xml.child xml_arg0 "AttributeName") in
      make ?cardinality ?defaultValue ?description ?attributeType
        ?attributeName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cardinality = field_map json__ "Cardinality" Cardinality.of_json in
      let defaultValue = field_map json__ "DefaultValue" DefaultValue.of_json in
      let description = field_map json__ "Description" Description.of_json in
      let attributeType =
        field_map json__ "AttributeType" AttributeType.of_json in
      let attributeName =
        field_map json__ "AttributeName" AttributeName.of_json in
      make ?cardinality ?defaultValue ?description ?attributeType
        ?attributeName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a policy attribute type."]
module PolicyAttributeDescription =
  struct
    type nonrec t =
      {
      attributeName: AttributeName.t option
        [@ocaml.doc "The name of the attribute."];
      attributeValue: AttributeValue.t option
        [@ocaml.doc "The value of the attribute."]}
    let make ?attributeName =
      fun ?attributeValue -> fun () -> { attributeName; attributeValue }
    let to_value x =
      structure_to_value
        [("AttributeName",
           (Option.map x.attributeName ~f:AttributeName.to_value));
        ("AttributeValue",
          (Option.map x.attributeValue ~f:AttributeValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributeValue =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "AttributeValue") in
      let attributeName =
        (Option.map ~f:AttributeName.of_xml)
          (Xml.child xml_arg0 "AttributeName") in
      make ?attributeValue ?attributeName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let attributeValue =
        field_map json__ "AttributeValue" AttributeValue.of_json in
      let attributeName =
        field_map json__ "AttributeName" AttributeName.of_json in
      make ?attributeValue ?attributeName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a policy attribute."]
module AvailabilityZone =
  struct
    type nonrec t = string
    let context_ = "AvailabilityZone"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AvailabilityZone" j
    let to_json = simple_to_json to_value
  end
module BackendServerDescription =
  struct
    type nonrec t =
      {
      instancePort: InstancePort.t option
        [@ocaml.doc "The port on which the EC2 instance is listening."];
      policyNames: PolicyNames.t option
        [@ocaml.doc
          "The names of the policies enabled for the EC2 instance."]}
    let make ?instancePort =
      fun ?policyNames -> fun () -> { instancePort; policyNames }
    let to_value x =
      structure_to_value
        [("InstancePort",
           (Option.map x.instancePort ~f:InstancePort.to_value));
        ("PolicyNames", (Option.map x.policyNames ~f:PolicyNames.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyNames =
        (Option.map ~f:PolicyNames.of_xml) (Xml.child xml_arg0 "PolicyNames") in
      let instancePort =
        (Option.map ~f:InstancePort.of_xml)
          (Xml.child xml_arg0 "InstancePort") in
      make ?policyNames ?instancePort ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyNames = field_map json__ "PolicyNames" PolicyNames.of_json in
      let instancePort = field_map json__ "InstancePort" InstancePort.of_json in
      make ?policyNames ?instancePort ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about the configuration of an EC2 instance."]
module HealthCheckInterval =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:300) >>= (fun () -> check_int_min i ~min:5));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for HealthCheckInterval" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module HealthCheckTarget =
  struct
    type nonrec t = string
    let context_ = "HealthCheckTarget"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HealthCheckTarget" j
    let to_json = simple_to_json to_value
  end
module HealthCheckTimeout =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:60) >>= (fun () -> check_int_min i ~min:2));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for HealthCheckTimeout" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module HealthyThreshold =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:10) >>= (fun () -> check_int_min i ~min:2));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for HealthyThreshold" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module UnhealthyThreshold =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:10) >>= (fun () -> check_int_min i ~min:2));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for UnhealthyThreshold" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Instance =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t option [@ocaml.doc "The instance ID."]}
    let make ?instanceId = fun () -> { instanceId }
    let to_value x =
      structure_to_value
        [("InstanceId", (Option.map x.instanceId ~f:InstanceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "InstanceId") in
      make ?instanceId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceId = field_map json__ "InstanceId" InstanceId.of_json in
      make ?instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The ID of an EC2 instance."]
module ListenerDescription =
  struct
    type nonrec t =
      {
      listener: Listener.t option [@ocaml.doc "The listener."];
      policyNames: PolicyNames.t option
        [@ocaml.doc
          "The policies. If there are no policies enabled, the list is empty."]}
    let make ?listener =
      fun ?policyNames -> fun () -> { listener; policyNames }
    let to_value x =
      structure_to_value
        [("Listener", (Option.map x.listener ~f:Listener.to_value));
        ("PolicyNames", (Option.map x.policyNames ~f:PolicyNames.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyNames =
        (Option.map ~f:PolicyNames.of_xml) (Xml.child xml_arg0 "PolicyNames") in
      let listener =
        (Option.map ~f:Listener.of_xml) (Xml.child xml_arg0 "Listener") in
      make ?policyNames ?listener ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyNames = field_map json__ "PolicyNames" PolicyNames.of_json in
      let listener = field_map json__ "Listener" Listener.of_json in
      make ?policyNames ?listener ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The policies enabled for a listener."]
module AppCookieStickinessPolicies =
  struct
    type nonrec t = AppCookieStickinessPolicy.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AppCookieStickinessPolicy.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AppCookieStickinessPolicy.of_xml)
    let of_json j =
      list_of_json ~kind:"AppCookieStickinessPolicies"
        ~of_json:AppCookieStickinessPolicy.of_json j
    let to_json v = composed_to_json to_value v
  end
module LBCookieStickinessPolicies =
  struct
    type nonrec t = LBCookieStickinessPolicy.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:LBCookieStickinessPolicy.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LBCookieStickinessPolicy.of_xml)
    let of_json j =
      list_of_json ~kind:"LBCookieStickinessPolicies"
        ~of_json:LBCookieStickinessPolicy.of_json j
    let to_json v = composed_to_json to_value v
  end
module SecurityGroupId =
  struct
    type nonrec t = string
    let context_ = "SecurityGroupId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SecurityGroupId" j
    let to_json = simple_to_json to_value
  end
module SecurityGroupName =
  struct
    type nonrec t = string
    let context_ = "SecurityGroupName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SecurityGroupName" j
    let to_json = simple_to_json to_value
  end
module SecurityGroupOwnerAlias =
  struct
    type nonrec t = string
    let context_ = "SecurityGroupOwnerAlias"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SecurityGroupOwnerAlias" j
    let to_json = simple_to_json to_value
  end
module SubnetId =
  struct
    type nonrec t = string
    let context_ = "SubnetId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SubnetId" j
    let to_json = simple_to_json to_value
  end
module AccessLogEnabled =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module AccessLogInterval =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for AccessLogInterval" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AccessLogPrefix =
  struct
    type nonrec t = string
    let context_ = "AccessLogPrefix"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AccessLogPrefix" j
    let to_json = simple_to_json to_value
  end
module S3BucketName =
  struct
    type nonrec t = string
    let context_ = "S3BucketName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3BucketName" j
    let to_json = simple_to_json to_value
  end
module AdditionalAttribute =
  struct
    type nonrec t =
      {
      key: AdditionalAttributeKey.t option
        [@ocaml.doc
          "The name of the attribute. The following attribute is supported. elb.http.desyncmitigationmode - Determines how the load balancer handles requests that might pose a security risk to your application. The possible values are monitor, defensive, and strictest. The default is defensive."];
      value: AdditionalAttributeValue.t option
        [@ocaml.doc "This value of the attribute."]}
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:AdditionalAttributeKey.to_value));
        ("Value", (Option.map x.value ~f:AdditionalAttributeValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:AdditionalAttributeValue.of_xml)
          (Xml.child xml_arg0 "Value") in
      let key =
        (Option.map ~f:AdditionalAttributeKey.of_xml)
          (Xml.child xml_arg0 "Key") in
      make ?value ?key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "Value" AdditionalAttributeValue.of_json in
      let key = field_map json__ "Key" AdditionalAttributeKey.of_json in
      make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about additional load balancer attributes."]
module ConnectionDrainingEnabled =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ConnectionDrainingTimeout =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ConnectionDrainingTimeout"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module IdleTimeout =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:3600) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for IdleTimeout" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CrossZoneLoadBalancingEnabled =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module AccessPointName =
  struct
    type nonrec t = string
    let context_ = "AccessPointName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AccessPointName" j
    let to_json = simple_to_json to_value
  end
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module PolicyAttributeTypeDescriptions =
  struct
    type nonrec t = PolicyAttributeTypeDescription.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PolicyAttributeTypeDescription.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PolicyAttributeTypeDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"PolicyAttributeTypeDescriptions"
        ~of_json:PolicyAttributeTypeDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module PolicyTypeName =
  struct
    type nonrec t = string
    let context_ = "PolicyTypeName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PolicyTypeName" j
    let to_json = simple_to_json to_value
  end
module PolicyAttributeDescriptions =
  struct
    type nonrec t = PolicyAttributeDescription.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PolicyAttributeDescription.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PolicyAttributeDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"PolicyAttributeDescriptions"
        ~of_json:PolicyAttributeDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReasonCode =
  struct
    type nonrec t = string
    let context_ = "ReasonCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ReasonCode" j
    let to_json = simple_to_json to_value
  end
module State =
  struct
    type nonrec t = string
    let context_ = "State"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"State" j
    let to_json = simple_to_json to_value
  end
module Max =
  struct
    type nonrec t = string
    let context_ = "Max"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Max" j
    let to_json = simple_to_json to_value
  end
module Name =
  struct
    type nonrec t = string
    let context_ = "Name"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Name" j
    let to_json = simple_to_json to_value
  end
module AvailabilityZones =
  struct
    type nonrec t = AvailabilityZone.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AvailabilityZone.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AvailabilityZone.of_xml)
    let of_json j =
      list_of_json ~kind:"AvailabilityZones"
        ~of_json:AvailabilityZone.of_json j
    let to_json v = composed_to_json to_value v
  end
module BackendServerDescriptions =
  struct
    type nonrec t = BackendServerDescription.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BackendServerDescription.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BackendServerDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"BackendServerDescriptions"
        ~of_json:BackendServerDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module CreatedTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module DNSName =
  struct
    type nonrec t = string
    let context_ = "DNSName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DNSName" j
    let to_json = simple_to_json to_value
  end
module HealthCheck =
  struct
    type nonrec t =
      {
      target: HealthCheckTarget.t
        [@ocaml.doc
          "The instance being checked. The protocol is either TCP, HTTP, HTTPS, or SSL. The range of valid ports is one (1) through 65535. TCP is the default, specified as a TCP: port pair, for example \"TCP:5000\". In this case, a health check simply attempts to open a TCP connection to the instance on the specified port. Failure to connect within the configured timeout is considered unhealthy. SSL is also specified as SSL: port pair, for example, SSL:5000. For HTTP/HTTPS, you must include a ping path in the string. HTTP is specified as a HTTP:port;/;PathToPing; grouping, for example \"HTTP:80/weather/us/wa/seattle\". In this case, a HTTP GET request is issued to the instance on the given port and path. Any answer other than \"200 OK\" within the timeout period is considered unhealthy. The total length of the HTTP ping target must be 1024 16-bit Unicode characters or less."];
      interval: HealthCheckInterval.t
        [@ocaml.doc
          "The approximate interval, in seconds, between health checks of an individual instance."];
      timeout: HealthCheckTimeout.t
        [@ocaml.doc
          "The amount of time, in seconds, during which no response means a failed health check. This value must be less than the Interval value."];
      unhealthyThreshold: UnhealthyThreshold.t
        [@ocaml.doc
          "The number of consecutive health check failures required before moving the instance to the Unhealthy state."];
      healthyThreshold: HealthyThreshold.t
        [@ocaml.doc
          "The number of consecutive health checks successes required before moving the instance to the Healthy state."]}
    let context_ = "HealthCheck"
    let make ~target =
      fun ~interval ->
        fun ~timeout ->
          fun ~unhealthyThreshold ->
            fun ~healthyThreshold ->
              fun () ->
                {
                  target;
                  interval;
                  timeout;
                  unhealthyThreshold;
                  healthyThreshold
                }
    let to_value x =
      structure_to_value
        [("Target", (Some (HealthCheckTarget.to_value x.target)));
        ("Interval", (Some (HealthCheckInterval.to_value x.interval)));
        ("Timeout", (Some (HealthCheckTimeout.to_value x.timeout)));
        ("UnhealthyThreshold",
          (Some (UnhealthyThreshold.to_value x.unhealthyThreshold)));
        ("HealthyThreshold",
          (Some (HealthyThreshold.to_value x.healthyThreshold)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthyThreshold =
        HealthyThreshold.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthyThreshold") in
      let unhealthyThreshold =
        UnhealthyThreshold.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "UnhealthyThreshold") in
      let timeout =
        HealthCheckTimeout.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Timeout") in
      let interval =
        HealthCheckInterval.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Interval") in
      let target =
        HealthCheckTarget.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Target") in
      make ~healthyThreshold ~unhealthyThreshold ~timeout ~interval ~target
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let healthyThreshold =
        field_map_exn json__ "HealthyThreshold" HealthyThreshold.of_json in
      let unhealthyThreshold =
        field_map_exn json__ "UnhealthyThreshold" UnhealthyThreshold.of_json in
      let timeout = field_map_exn json__ "Timeout" HealthCheckTimeout.of_json in
      let interval =
        field_map_exn json__ "Interval" HealthCheckInterval.of_json in
      let target = field_map_exn json__ "Target" HealthCheckTarget.of_json in
      make ~healthyThreshold ~unhealthyThreshold ~timeout ~interval ~target
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a health check."]
module Instances =
  struct
    type nonrec t = Instance.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Instance.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Instance.of_xml)
    let of_json j =
      list_of_json ~kind:"Instances" ~of_json:Instance.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListenerDescriptions =
  struct
    type nonrec t = ListenerDescription.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ListenerDescription.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ListenerDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"ListenerDescriptions"
        ~of_json:ListenerDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module LoadBalancerScheme =
  struct
    type nonrec t = string
    let context_ = "LoadBalancerScheme"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LoadBalancerScheme" j
    let to_json = simple_to_json to_value
  end
module Policies =
  struct
    type nonrec t =
      {
      appCookieStickinessPolicies: AppCookieStickinessPolicies.t option
        [@ocaml.doc
          "The stickiness policies created using CreateAppCookieStickinessPolicy."];
      lBCookieStickinessPolicies: LBCookieStickinessPolicies.t option
        [@ocaml.doc
          "The stickiness policies created using CreateLBCookieStickinessPolicy."];
      otherPolicies: PolicyNames.t option
        [@ocaml.doc "The policies other than the stickiness policies."]}
    let make ?appCookieStickinessPolicies =
      fun ?lBCookieStickinessPolicies ->
        fun ?otherPolicies ->
          fun () ->
            {
              appCookieStickinessPolicies;
              lBCookieStickinessPolicies;
              otherPolicies
            }
    let to_value x =
      structure_to_value
        [("AppCookieStickinessPolicies",
           (Option.map x.appCookieStickinessPolicies
              ~f:AppCookieStickinessPolicies.to_value));
        ("LBCookieStickinessPolicies",
          (Option.map x.lBCookieStickinessPolicies
             ~f:LBCookieStickinessPolicies.to_value));
        ("OtherPolicies",
          (Option.map x.otherPolicies ~f:PolicyNames.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let otherPolicies =
        (Option.map ~f:PolicyNames.of_xml)
          (Xml.child xml_arg0 "OtherPolicies") in
      let lBCookieStickinessPolicies =
        (Option.map ~f:LBCookieStickinessPolicies.of_xml)
          (Xml.child xml_arg0 "LBCookieStickinessPolicies") in
      let appCookieStickinessPolicies =
        (Option.map ~f:AppCookieStickinessPolicies.of_xml)
          (Xml.child xml_arg0 "AppCookieStickinessPolicies") in
      make ?otherPolicies ?lBCookieStickinessPolicies
        ?appCookieStickinessPolicies ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let otherPolicies =
        field_map json__ "OtherPolicies" PolicyNames.of_json in
      let lBCookieStickinessPolicies =
        field_map json__ "LBCookieStickinessPolicies"
          LBCookieStickinessPolicies.of_json in
      let appCookieStickinessPolicies =
        field_map json__ "AppCookieStickinessPolicies"
          AppCookieStickinessPolicies.of_json in
      make ?otherPolicies ?lBCookieStickinessPolicies
        ?appCookieStickinessPolicies ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The policies for a load balancer."]
module SecurityGroups =
  struct
    type nonrec t = SecurityGroupId.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SecurityGroupId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SecurityGroupId.of_xml)
    let of_json j =
      list_of_json ~kind:"SecurityGroups" ~of_json:SecurityGroupId.of_json j
    let to_json v = composed_to_json to_value v
  end
module SourceSecurityGroup =
  struct
    type nonrec t =
      {
      ownerAlias: SecurityGroupOwnerAlias.t option
        [@ocaml.doc "The owner of the security group."];
      groupName: SecurityGroupName.t option
        [@ocaml.doc "The name of the security group."]}
    let make ?ownerAlias =
      fun ?groupName -> fun () -> { ownerAlias; groupName }
    let to_value x =
      structure_to_value
        [("OwnerAlias",
           (Option.map x.ownerAlias ~f:SecurityGroupOwnerAlias.to_value));
        ("GroupName", (Option.map x.groupName ~f:SecurityGroupName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupName =
        (Option.map ~f:SecurityGroupName.of_xml)
          (Xml.child xml_arg0 "GroupName") in
      let ownerAlias =
        (Option.map ~f:SecurityGroupOwnerAlias.of_xml)
          (Xml.child xml_arg0 "OwnerAlias") in
      make ?groupName ?ownerAlias ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let groupName = field_map json__ "GroupName" SecurityGroupName.of_json in
      let ownerAlias =
        field_map json__ "OwnerAlias" SecurityGroupOwnerAlias.of_json in
      make ?groupName ?ownerAlias ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a source security group."]
module Subnets =
  struct
    type nonrec t = SubnetId.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SubnetId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SubnetId.of_xml)
    let of_json j = list_of_json ~kind:"Subnets" ~of_json:SubnetId.of_json j
    let to_json v = composed_to_json to_value v
  end
module VPCId =
  struct
    type nonrec t = string
    let context_ = "VPCId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"VPCId" j
    let to_json = simple_to_json to_value
  end
module TagKeyOnly =
  struct
    type nonrec t =
      {
      key: TagKey.t option [@ocaml.doc "The name of the key."]}
    let make ?key = fun () -> { key }
    let to_value x =
      structure_to_value [("Key", (Option.map x.key ~f:TagKey.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let key = (Option.map ~f:TagKey.of_xml) (Xml.child xml_arg0 "Key") in
      make ?key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let key = field_map json__ "Key" TagKey.of_json in make ?key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The key of a tag."]
module AccessLog =
  struct
    type nonrec t =
      {
      enabled: AccessLogEnabled.t
        [@ocaml.doc
          "Specifies whether access logs are enabled for the load balancer."];
      s3BucketName: S3BucketName.t option
        [@ocaml.doc
          "The name of the Amazon S3 bucket where the access logs are stored."];
      emitInterval: AccessLogInterval.t option
        [@ocaml.doc
          "The interval for publishing the access logs. You can specify an interval of either 5 minutes or 60 minutes. Default: 60 minutes"];
      s3BucketPrefix: AccessLogPrefix.t option
        [@ocaml.doc
          "The logical hierarchy you created for your Amazon S3 bucket, for example my-bucket-prefix/prod. If the prefix is not provided, the log is placed at the root level of the bucket."]}
    let context_ = "AccessLog"
    let make ?s3BucketName =
      fun ?emitInterval ->
        fun ?s3BucketPrefix ->
          fun ~enabled ->
            fun () -> { s3BucketName; emitInterval; s3BucketPrefix; enabled }
    let to_value x =
      structure_to_value
        [("Enabled", (Some (AccessLogEnabled.to_value x.enabled)));
        ("S3BucketName",
          (Option.map x.s3BucketName ~f:S3BucketName.to_value));
        ("EmitInterval",
          (Option.map x.emitInterval ~f:AccessLogInterval.to_value));
        ("S3BucketPrefix",
          (Option.map x.s3BucketPrefix ~f:AccessLogPrefix.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3BucketPrefix =
        (Option.map ~f:AccessLogPrefix.of_xml)
          (Xml.child xml_arg0 "S3BucketPrefix") in
      let emitInterval =
        (Option.map ~f:AccessLogInterval.of_xml)
          (Xml.child xml_arg0 "EmitInterval") in
      let s3BucketName =
        (Option.map ~f:S3BucketName.of_xml)
          (Xml.child xml_arg0 "S3BucketName") in
      let enabled =
        AccessLogEnabled.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Enabled") in
      make ?s3BucketPrefix ?emitInterval ?s3BucketName ~enabled ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3BucketPrefix =
        field_map json__ "S3BucketPrefix" AccessLogPrefix.of_json in
      let emitInterval =
        field_map json__ "EmitInterval" AccessLogInterval.of_json in
      let s3BucketName = field_map json__ "S3BucketName" S3BucketName.of_json in
      let enabled = field_map_exn json__ "Enabled" AccessLogEnabled.of_json in
      make ?s3BucketPrefix ?emitInterval ?s3BucketName ~enabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about the AccessLog attribute."]
module AdditionalAttributes =
  struct
    type nonrec t = AdditionalAttribute.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:10); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AdditionalAttribute.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AdditionalAttribute.of_xml)
    let of_json j =
      list_of_json ~kind:"AdditionalAttributes"
        ~of_json:AdditionalAttribute.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConnectionDraining =
  struct
    type nonrec t =
      {
      enabled: ConnectionDrainingEnabled.t
        [@ocaml.doc
          "Specifies whether connection draining is enabled for the load balancer."];
      timeout: ConnectionDrainingTimeout.t option
        [@ocaml.doc
          "The maximum time, in seconds, to keep the existing connections open before deregistering the instances."]}
    let context_ = "ConnectionDraining"
    let make ?timeout = fun ~enabled -> fun () -> { timeout; enabled }
    let to_value x =
      structure_to_value
        [("Enabled", (Some (ConnectionDrainingEnabled.to_value x.enabled)));
        ("Timeout",
          (Option.map x.timeout ~f:ConnectionDrainingTimeout.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timeout =
        (Option.map ~f:ConnectionDrainingTimeout.of_xml)
          (Xml.child xml_arg0 "Timeout") in
      let enabled =
        ConnectionDrainingEnabled.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Enabled") in
      make ?timeout ~enabled ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timeout =
        field_map json__ "Timeout" ConnectionDrainingTimeout.of_json in
      let enabled =
        field_map_exn json__ "Enabled" ConnectionDrainingEnabled.of_json in
      make ?timeout ~enabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about the ConnectionDraining attribute."]
module ConnectionSettings =
  struct
    type nonrec t =
      {
      idleTimeout: IdleTimeout.t
        [@ocaml.doc
          "The time, in seconds, that the connection is allowed to be idle (no data has been sent over the connection) before it is closed by the load balancer."]}
    let context_ = "ConnectionSettings"
    let make ~idleTimeout = fun () -> { idleTimeout }
    let to_value x =
      structure_to_value
        [("IdleTimeout", (Some (IdleTimeout.to_value x.idleTimeout)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idleTimeout =
        IdleTimeout.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdleTimeout") in
      make ~idleTimeout ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let idleTimeout =
        field_map_exn json__ "IdleTimeout" IdleTimeout.of_json in
      make ~idleTimeout ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about the ConnectionSettings attribute."]
module CrossZoneLoadBalancing =
  struct
    type nonrec t =
      {
      enabled: CrossZoneLoadBalancingEnabled.t
        [@ocaml.doc
          "Specifies whether cross-zone load balancing is enabled for the load balancer."]}
    let context_ = "CrossZoneLoadBalancing"
    let make ~enabled = fun () -> { enabled }
    let to_value x =
      structure_to_value
        [("Enabled",
           (Some (CrossZoneLoadBalancingEnabled.to_value x.enabled)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enabled =
        CrossZoneLoadBalancingEnabled.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Enabled") in
      make ~enabled ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let enabled =
        field_map_exn json__ "Enabled" CrossZoneLoadBalancingEnabled.of_json in
      make ~enabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about the CrossZoneLoadBalancing attribute."]
module TagDescription =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t option
        [@ocaml.doc "The name of the load balancer."];
      tags: TagList.t option [@ocaml.doc "The tags."]}
    let make ?loadBalancerName =
      fun ?tags -> fun () -> { loadBalancerName; tags }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Option.map x.loadBalancerName ~f:AccessPointName.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let loadBalancerName =
        (Option.map ~f:AccessPointName.of_xml)
          (Xml.child xml_arg0 "LoadBalancerName") in
      make ?tags ?loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagList.of_json in
      let loadBalancerName =
        field_map json__ "LoadBalancerName" AccessPointName.of_json in
      make ?tags ?loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The tags associated with a load balancer."]
module PolicyTypeDescription =
  struct
    type nonrec t =
      {
      policyTypeName: PolicyTypeName.t option
        [@ocaml.doc "The name of the policy type."];
      description: Description.t option
        [@ocaml.doc "A description of the policy type."];
      policyAttributeTypeDescriptions:
        PolicyAttributeTypeDescriptions.t option
        [@ocaml.doc
          "The description of the policy attributes associated with the policies defined by Elastic Load Balancing."]}
    let make ?policyTypeName =
      fun ?description ->
        fun ?policyAttributeTypeDescriptions ->
          fun () ->
            { policyTypeName; description; policyAttributeTypeDescriptions }
    let to_value x =
      structure_to_value
        [("PolicyTypeName",
           (Option.map x.policyTypeName ~f:PolicyTypeName.to_value));
        ("Description", (Option.map x.description ~f:Description.to_value));
        ("PolicyAttributeTypeDescriptions",
          (Option.map x.policyAttributeTypeDescriptions
             ~f:PolicyAttributeTypeDescriptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyAttributeTypeDescriptions =
        (Option.map ~f:PolicyAttributeTypeDescriptions.of_xml)
          (Xml.child xml_arg0 "PolicyAttributeTypeDescriptions") in
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      let policyTypeName =
        (Option.map ~f:PolicyTypeName.of_xml)
          (Xml.child xml_arg0 "PolicyTypeName") in
      make ?policyAttributeTypeDescriptions ?description ?policyTypeName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyAttributeTypeDescriptions =
        field_map json__ "PolicyAttributeTypeDescriptions"
          PolicyAttributeTypeDescriptions.of_json in
      let description = field_map json__ "Description" Description.of_json in
      let policyTypeName =
        field_map json__ "PolicyTypeName" PolicyTypeName.of_json in
      make ?policyAttributeTypeDescriptions ?description ?policyTypeName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a policy type."]
module PolicyDescription =
  struct
    type nonrec t =
      {
      policyName: PolicyName.t option [@ocaml.doc "The name of the policy."];
      policyTypeName: PolicyTypeName.t option
        [@ocaml.doc "The name of the policy type."];
      policyAttributeDescriptions: PolicyAttributeDescriptions.t option
        [@ocaml.doc "The policy attributes."]}
    let make ?policyName =
      fun ?policyTypeName ->
        fun ?policyAttributeDescriptions ->
          fun () ->
            { policyName; policyTypeName; policyAttributeDescriptions }
    let to_value x =
      structure_to_value
        [("PolicyName", (Option.map x.policyName ~f:PolicyName.to_value));
        ("PolicyTypeName",
          (Option.map x.policyTypeName ~f:PolicyTypeName.to_value));
        ("PolicyAttributeDescriptions",
          (Option.map x.policyAttributeDescriptions
             ~f:PolicyAttributeDescriptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyAttributeDescriptions =
        (Option.map ~f:PolicyAttributeDescriptions.of_xml)
          (Xml.child xml_arg0 "PolicyAttributeDescriptions") in
      let policyTypeName =
        (Option.map ~f:PolicyTypeName.of_xml)
          (Xml.child xml_arg0 "PolicyTypeName") in
      let policyName =
        (Option.map ~f:PolicyName.of_xml) (Xml.child xml_arg0 "PolicyName") in
      make ?policyAttributeDescriptions ?policyTypeName ?policyName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyAttributeDescriptions =
        field_map json__ "PolicyAttributeDescriptions"
          PolicyAttributeDescriptions.of_json in
      let policyTypeName =
        field_map json__ "PolicyTypeName" PolicyTypeName.of_json in
      let policyName = field_map json__ "PolicyName" PolicyName.of_json in
      make ?policyAttributeDescriptions ?policyTypeName ?policyName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a policy."]
module InstanceState =
  struct
    type nonrec t =
      {
      instanceId: InstanceId.t option [@ocaml.doc "The ID of the instance."];
      state: State.t option
        [@ocaml.doc
          "The current state of the instance. Valid values: InService | OutOfService | Unknown"];
      reasonCode: ReasonCode.t option
        [@ocaml.doc
          "Information about the cause of OutOfService instances. Specifically, whether the cause is Elastic Load Balancing or the instance. Valid values: ELB | Instance | N/A"];
      description: Description.t option
        [@ocaml.doc
          "A description of the instance state. This string can contain one or more of the following messages. N/A A transient error occurred. Please try again later. Instance has failed at least the UnhealthyThreshold number of health checks consecutively. Instance has not passed the configured HealthyThreshold number of health checks consecutively. Instance registration is still in progress. Instance is in the EC2 Availability Zone for which LoadBalancer is not configured to route traffic to. Instance is not currently registered with the LoadBalancer. Instance deregistration currently in progress. Disable Availability Zone is currently in progress. Instance is in pending state. Instance is in stopped state. Instance is in terminated state."]}
    let make ?instanceId =
      fun ?state ->
        fun ?reasonCode ->
          fun ?description ->
            fun () -> { instanceId; state; reasonCode; description }
    let to_value x =
      structure_to_value
        [("InstanceId", (Option.map x.instanceId ~f:InstanceId.to_value));
        ("State", (Option.map x.state ~f:State.to_value));
        ("ReasonCode", (Option.map x.reasonCode ~f:ReasonCode.to_value));
        ("Description", (Option.map x.description ~f:Description.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      let reasonCode =
        (Option.map ~f:ReasonCode.of_xml) (Xml.child xml_arg0 "ReasonCode") in
      let state = (Option.map ~f:State.of_xml) (Xml.child xml_arg0 "State") in
      let instanceId =
        (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "InstanceId") in
      make ?description ?reasonCode ?state ?instanceId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description = field_map json__ "Description" Description.of_json in
      let reasonCode = field_map json__ "ReasonCode" ReasonCode.of_json in
      let state = field_map json__ "State" State.of_json in
      let instanceId = field_map json__ "InstanceId" InstanceId.of_json in
      make ?description ?reasonCode ?state ?instanceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about the state of an EC2 instance."]
module Limit =
  struct
    type nonrec t =
      {
      name: Name.t option
        [@ocaml.doc
          "The name of the limit. The possible values are: classic-listeners classic-load-balancers classic-registered-instances"];
      max: Max.t option [@ocaml.doc "The maximum value of the limit."]}
    let make ?name = fun ?max -> fun () -> { name; max }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:Name.to_value));
        ("Max", (Option.map x.max ~f:Max.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let max = (Option.map ~f:Max.of_xml) (Xml.child xml_arg0 "Max") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "Name") in
      make ?max ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let max = field_map json__ "Max" Max.of_json in
      let name = field_map json__ "Name" Name.of_json in make ?max ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about an Elastic Load Balancing resource limit for your AWS account."]
module LoadBalancerDescription =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t option
        [@ocaml.doc "The name of the load balancer."];
      dNSName: DNSName.t option
        [@ocaml.doc "The DNS name of the load balancer."];
      canonicalHostedZoneName: DNSName.t option
        [@ocaml.doc
          "The DNS name of the load balancer. For more information, see Configure a Custom Domain Name in the Classic Load Balancers Guide."];
      canonicalHostedZoneNameID: DNSName.t option
        [@ocaml.doc
          "The ID of the Amazon Route 53 hosted zone for the load balancer."];
      listenerDescriptions: ListenerDescriptions.t option
        [@ocaml.doc "The listeners for the load balancer."];
      policies: Policies.t option
        [@ocaml.doc "The policies defined for the load balancer."];
      backendServerDescriptions: BackendServerDescriptions.t option
        [@ocaml.doc "Information about your EC2 instances."];
      availabilityZones: AvailabilityZones.t option
        [@ocaml.doc "The Availability Zones for the load balancer."];
      subnets: Subnets.t option
        [@ocaml.doc "The IDs of the subnets for the load balancer."];
      vPCId: VPCId.t option
        [@ocaml.doc "The ID of the VPC for the load balancer."];
      instances: Instances.t option
        [@ocaml.doc "The IDs of the instances for the load balancer."];
      healthCheck: HealthCheck.t option
        [@ocaml.doc
          "Information about the health checks conducted on the load balancer."];
      sourceSecurityGroup: SourceSecurityGroup.t option
        [@ocaml.doc
          "The security group for the load balancer, which you can use as part of your inbound rules for your registered instances. To only allow traffic from load balancers, add a security group rule that specifies this source security group as the inbound source."];
      securityGroups: SecurityGroups.t option
        [@ocaml.doc
          "The security groups for the load balancer. Valid only for load balancers in a VPC."];
      createdTime: CreatedTime.t option
        [@ocaml.doc "The date and time the load balancer was created."];
      scheme: LoadBalancerScheme.t option
        [@ocaml.doc
          "The type of load balancer. Valid only for load balancers in a VPC. If Scheme is internet-facing, the load balancer has a public DNS name that resolves to a public IP address. If Scheme is internal, the load balancer has a public DNS name that resolves to a private IP address."]}
    let make ?loadBalancerName =
      fun ?dNSName ->
        fun ?canonicalHostedZoneName ->
          fun ?canonicalHostedZoneNameID ->
            fun ?listenerDescriptions ->
              fun ?policies ->
                fun ?backendServerDescriptions ->
                  fun ?availabilityZones ->
                    fun ?subnets ->
                      fun ?vPCId ->
                        fun ?instances ->
                          fun ?healthCheck ->
                            fun ?sourceSecurityGroup ->
                              fun ?securityGroups ->
                                fun ?createdTime ->
                                  fun ?scheme ->
                                    fun () ->
                                      {
                                        loadBalancerName;
                                        dNSName;
                                        canonicalHostedZoneName;
                                        canonicalHostedZoneNameID;
                                        listenerDescriptions;
                                        policies;
                                        backendServerDescriptions;
                                        availabilityZones;
                                        subnets;
                                        vPCId;
                                        instances;
                                        healthCheck;
                                        sourceSecurityGroup;
                                        securityGroups;
                                        createdTime;
                                        scheme
                                      }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Option.map x.loadBalancerName ~f:AccessPointName.to_value));
        ("DNSName", (Option.map x.dNSName ~f:DNSName.to_value));
        ("CanonicalHostedZoneName",
          (Option.map x.canonicalHostedZoneName ~f:DNSName.to_value));
        ("CanonicalHostedZoneNameID",
          (Option.map x.canonicalHostedZoneNameID ~f:DNSName.to_value));
        ("ListenerDescriptions",
          (Option.map x.listenerDescriptions ~f:ListenerDescriptions.to_value));
        ("Policies", (Option.map x.policies ~f:Policies.to_value));
        ("BackendServerDescriptions",
          (Option.map x.backendServerDescriptions
             ~f:BackendServerDescriptions.to_value));
        ("AvailabilityZones",
          (Option.map x.availabilityZones ~f:AvailabilityZones.to_value));
        ("Subnets", (Option.map x.subnets ~f:Subnets.to_value));
        ("VPCId", (Option.map x.vPCId ~f:VPCId.to_value));
        ("Instances", (Option.map x.instances ~f:Instances.to_value));
        ("HealthCheck", (Option.map x.healthCheck ~f:HealthCheck.to_value));
        ("SourceSecurityGroup",
          (Option.map x.sourceSecurityGroup ~f:SourceSecurityGroup.to_value));
        ("SecurityGroups",
          (Option.map x.securityGroups ~f:SecurityGroups.to_value));
        ("CreatedTime", (Option.map x.createdTime ~f:CreatedTime.to_value));
        ("Scheme", (Option.map x.scheme ~f:LoadBalancerScheme.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scheme =
        (Option.map ~f:LoadBalancerScheme.of_xml)
          (Xml.child xml_arg0 "Scheme") in
      let createdTime =
        (Option.map ~f:CreatedTime.of_xml) (Xml.child xml_arg0 "CreatedTime") in
      let securityGroups =
        (Option.map ~f:SecurityGroups.of_xml)
          (Xml.child xml_arg0 "SecurityGroups") in
      let sourceSecurityGroup =
        (Option.map ~f:SourceSecurityGroup.of_xml)
          (Xml.child xml_arg0 "SourceSecurityGroup") in
      let healthCheck =
        (Option.map ~f:HealthCheck.of_xml) (Xml.child xml_arg0 "HealthCheck") in
      let instances =
        (Option.map ~f:Instances.of_xml) (Xml.child xml_arg0 "Instances") in
      let vPCId = (Option.map ~f:VPCId.of_xml) (Xml.child xml_arg0 "VPCId") in
      let subnets =
        (Option.map ~f:Subnets.of_xml) (Xml.child xml_arg0 "Subnets") in
      let availabilityZones =
        (Option.map ~f:AvailabilityZones.of_xml)
          (Xml.child xml_arg0 "AvailabilityZones") in
      let backendServerDescriptions =
        (Option.map ~f:BackendServerDescriptions.of_xml)
          (Xml.child xml_arg0 "BackendServerDescriptions") in
      let policies =
        (Option.map ~f:Policies.of_xml) (Xml.child xml_arg0 "Policies") in
      let listenerDescriptions =
        (Option.map ~f:ListenerDescriptions.of_xml)
          (Xml.child xml_arg0 "ListenerDescriptions") in
      let canonicalHostedZoneNameID =
        (Option.map ~f:DNSName.of_xml)
          (Xml.child xml_arg0 "CanonicalHostedZoneNameID") in
      let canonicalHostedZoneName =
        (Option.map ~f:DNSName.of_xml)
          (Xml.child xml_arg0 "CanonicalHostedZoneName") in
      let dNSName =
        (Option.map ~f:DNSName.of_xml) (Xml.child xml_arg0 "DNSName") in
      let loadBalancerName =
        (Option.map ~f:AccessPointName.of_xml)
          (Xml.child xml_arg0 "LoadBalancerName") in
      make ?scheme ?createdTime ?securityGroups ?sourceSecurityGroup
        ?healthCheck ?instances ?vPCId ?subnets ?availabilityZones
        ?backendServerDescriptions ?policies ?listenerDescriptions
        ?canonicalHostedZoneNameID ?canonicalHostedZoneName ?dNSName
        ?loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scheme = field_map json__ "Scheme" LoadBalancerScheme.of_json in
      let createdTime = field_map json__ "CreatedTime" CreatedTime.of_json in
      let securityGroups =
        field_map json__ "SecurityGroups" SecurityGroups.of_json in
      let sourceSecurityGroup =
        field_map json__ "SourceSecurityGroup" SourceSecurityGroup.of_json in
      let healthCheck = field_map json__ "HealthCheck" HealthCheck.of_json in
      let instances = field_map json__ "Instances" Instances.of_json in
      let vPCId = field_map json__ "VPCId" VPCId.of_json in
      let subnets = field_map json__ "Subnets" Subnets.of_json in
      let availabilityZones =
        field_map json__ "AvailabilityZones" AvailabilityZones.of_json in
      let backendServerDescriptions =
        field_map json__ "BackendServerDescriptions"
          BackendServerDescriptions.of_json in
      let policies = field_map json__ "Policies" Policies.of_json in
      let listenerDescriptions =
        field_map json__ "ListenerDescriptions" ListenerDescriptions.of_json in
      let canonicalHostedZoneNameID =
        field_map json__ "CanonicalHostedZoneNameID" DNSName.of_json in
      let canonicalHostedZoneName =
        field_map json__ "CanonicalHostedZoneName" DNSName.of_json in
      let dNSName = field_map json__ "DNSName" DNSName.of_json in
      let loadBalancerName =
        field_map json__ "LoadBalancerName" AccessPointName.of_json in
      make ?scheme ?createdTime ?securityGroups ?sourceSecurityGroup
        ?healthCheck ?instances ?vPCId ?subnets ?availabilityZones
        ?backendServerDescriptions ?policies ?listenerDescriptions
        ?canonicalHostedZoneNameID ?canonicalHostedZoneName ?dNSName
        ?loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a load balancer."]
module PolicyAttribute =
  struct
    type nonrec t =
      {
      attributeName: AttributeName.t option
        [@ocaml.doc "The name of the attribute."];
      attributeValue: AttributeValue.t option
        [@ocaml.doc "The value of the attribute."]}
    let make ?attributeName =
      fun ?attributeValue -> fun () -> { attributeName; attributeValue }
    let to_value x =
      structure_to_value
        [("AttributeName",
           (Option.map x.attributeName ~f:AttributeName.to_value));
        ("AttributeValue",
          (Option.map x.attributeValue ~f:AttributeValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributeValue =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "AttributeValue") in
      let attributeName =
        (Option.map ~f:AttributeName.of_xml)
          (Xml.child xml_arg0 "AttributeName") in
      make ?attributeValue ?attributeName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let attributeValue =
        field_map json__ "AttributeValue" AttributeValue.of_json in
      let attributeName =
        field_map json__ "AttributeName" AttributeName.of_json in
      make ?attributeValue ?attributeName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a policy attribute."]
module AccessPointNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified load balancer does not exist."]
module InvalidConfigurationRequestException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The requested configuration change is not valid."]
module ListenerNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The load balancer does not have a listener configured at the specified port."]
module PolicyNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "One or more of the specified policies do not exist."]
module EndPointPort =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for EndPointPort" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CertificateNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified ARN does not refer to a valid SSL certificate in AWS Identity and Access Management (IAM) or AWS Certificate Manager (ACM). Note that if you recently uploaded the certificate to IAM, this error might indicate that the certificate is not fully available yet."]
module UnsupportedProtocolException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified protocol or signature version is not supported."]
module LoadBalancerNames =
  struct
    type nonrec t = AccessPointName.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AccessPointName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccessPointName.of_xml)
    let of_json j =
      list_of_json ~kind:"LoadBalancerNames" ~of_json:AccessPointName.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module TagKeyList =
  struct
    type nonrec t = TagKeyOnly.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagKeyOnly.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKeyOnly.of_xml)
    let of_json j =
      list_of_json ~kind:"TagKeyList" ~of_json:TagKeyOnly.of_json j
    let to_json v = composed_to_json to_value v
  end
module InvalidEndPointException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified endpoint is not valid."]
module LoadBalancerAttributeNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified load balancer attribute does not exist."]
module LoadBalancerAttributes =
  struct
    type nonrec t =
      {
      crossZoneLoadBalancing: CrossZoneLoadBalancing.t option
        [@ocaml.doc
          "If enabled, the load balancer routes the request traffic evenly across all instances regardless of the Availability Zones. For more information, see Configure Cross-Zone Load Balancing in the Classic Load Balancers Guide."];
      accessLog: AccessLog.t option
        [@ocaml.doc
          "If enabled, the load balancer captures detailed information of all requests and delivers the information to the Amazon S3 bucket that you specify. For more information, see Enable Access Logs in the Classic Load Balancers Guide."];
      connectionDraining: ConnectionDraining.t option
        [@ocaml.doc
          "If enabled, the load balancer allows existing requests to complete before the load balancer shifts traffic away from a deregistered or unhealthy instance. For more information, see Configure Connection Draining in the Classic Load Balancers Guide."];
      connectionSettings: ConnectionSettings.t option
        [@ocaml.doc
          "If enabled, the load balancer allows the connections to remain idle (no data is sent over the connection) for the specified duration. By default, Elastic Load Balancing maintains a 60-second idle connection timeout for both front-end and back-end connections of your load balancer. For more information, see Configure Idle Connection Timeout in the Classic Load Balancers Guide."];
      additionalAttributes: AdditionalAttributes.t option
        [@ocaml.doc "Any additional attributes."]}
    let make ?crossZoneLoadBalancing =
      fun ?accessLog ->
        fun ?connectionDraining ->
          fun ?connectionSettings ->
            fun ?additionalAttributes ->
              fun () ->
                {
                  crossZoneLoadBalancing;
                  accessLog;
                  connectionDraining;
                  connectionSettings;
                  additionalAttributes
                }
    let to_value x =
      structure_to_value
        [("CrossZoneLoadBalancing",
           (Option.map x.crossZoneLoadBalancing
              ~f:CrossZoneLoadBalancing.to_value));
        ("AccessLog", (Option.map x.accessLog ~f:AccessLog.to_value));
        ("ConnectionDraining",
          (Option.map x.connectionDraining ~f:ConnectionDraining.to_value));
        ("ConnectionSettings",
          (Option.map x.connectionSettings ~f:ConnectionSettings.to_value));
        ("AdditionalAttributes",
          (Option.map x.additionalAttributes ~f:AdditionalAttributes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let additionalAttributes =
        (Option.map ~f:AdditionalAttributes.of_xml)
          (Xml.child xml_arg0 "AdditionalAttributes") in
      let connectionSettings =
        (Option.map ~f:ConnectionSettings.of_xml)
          (Xml.child xml_arg0 "ConnectionSettings") in
      let connectionDraining =
        (Option.map ~f:ConnectionDraining.of_xml)
          (Xml.child xml_arg0 "ConnectionDraining") in
      let accessLog =
        (Option.map ~f:AccessLog.of_xml) (Xml.child xml_arg0 "AccessLog") in
      let crossZoneLoadBalancing =
        (Option.map ~f:CrossZoneLoadBalancing.of_xml)
          (Xml.child xml_arg0 "CrossZoneLoadBalancing") in
      make ?additionalAttributes ?connectionSettings ?connectionDraining
        ?accessLog ?crossZoneLoadBalancing ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let additionalAttributes =
        field_map json__ "AdditionalAttributes" AdditionalAttributes.of_json in
      let connectionSettings =
        field_map json__ "ConnectionSettings" ConnectionSettings.of_json in
      let connectionDraining =
        field_map json__ "ConnectionDraining" ConnectionDraining.of_json in
      let accessLog = field_map json__ "AccessLog" AccessLog.of_json in
      let crossZoneLoadBalancing =
        field_map json__ "CrossZoneLoadBalancing"
          CrossZoneLoadBalancing.of_json in
      make ?additionalAttributes ?connectionSettings ?connectionDraining
        ?accessLog ?crossZoneLoadBalancing ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The attributes for a load balancer."]
module TagDescriptions =
  struct
    type nonrec t = TagDescription.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagDescription.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"TagDescriptions" ~of_json:TagDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module LoadBalancerNamesMax20 =
  struct
    type nonrec t = AccessPointName.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:20) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AccessPointName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccessPointName.of_xml)
    let of_json j =
      list_of_json ~kind:"LoadBalancerNamesMax20"
        ~of_json:AccessPointName.of_json j
    let to_json v = composed_to_json to_value v
  end
module PolicyTypeDescriptions =
  struct
    type nonrec t = PolicyTypeDescription.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PolicyTypeDescription.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PolicyTypeDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"PolicyTypeDescriptions"
        ~of_json:PolicyTypeDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module PolicyTypeNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "One or more of the specified policy types do not exist."]
module PolicyTypeNames =
  struct
    type nonrec t = PolicyTypeName.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PolicyTypeName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PolicyTypeName.of_xml)
    let of_json j =
      list_of_json ~kind:"PolicyTypeNames" ~of_json:PolicyTypeName.of_json j
    let to_json v = composed_to_json to_value v
  end
module PolicyDescriptions =
  struct
    type nonrec t = PolicyDescription.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PolicyDescription.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PolicyDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"PolicyDescriptions"
        ~of_json:PolicyDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceStates =
  struct
    type nonrec t = InstanceState.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:InstanceState.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceState.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceStates" ~of_json:InstanceState.of_json j
    let to_json v = composed_to_json to_value v
  end
module Limits =
  struct
    type nonrec t = Limit.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Limit.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Limit.of_xml)
    let of_json j = list_of_json ~kind:"Limits" ~of_json:Limit.of_json j
    let to_json v = composed_to_json to_value v
  end
module Marker =
  struct
    type nonrec t = string
    let context_ = "Marker"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Marker" j
    let to_json = simple_to_json to_value
  end
module PageSize =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:400) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for PageSize" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DependencyThrottleException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request made by Elastic Load Balancing to another service exceeds the maximum request rate permitted for your account."]
module LoadBalancerDescriptions =
  struct
    type nonrec t = LoadBalancerDescription.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:LoadBalancerDescription.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LoadBalancerDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"LoadBalancerDescriptions"
        ~of_json:LoadBalancerDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module Ports =
  struct
    type nonrec t = AccessPointPort.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AccessPointPort.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccessPointPort.of_xml)
    let of_json j =
      list_of_json ~kind:"Ports" ~of_json:AccessPointPort.of_json j
    let to_json v = composed_to_json to_value v
  end
module DuplicatePolicyNameException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A policy with the specified name already exists for this load balancer."]
module TooManyPoliciesException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The quota for the number of policies for this load balancer has been reached."]
module PolicyAttributes =
  struct
    type nonrec t = PolicyAttribute.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PolicyAttribute.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PolicyAttribute.of_xml)
    let of_json j =
      list_of_json ~kind:"PolicyAttributes" ~of_json:PolicyAttribute.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module DuplicateListenerException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A listener already exists for the specified load balancer name and port, but with a different instance port, protocol, or SSL certificate."]
module Listeners =
  struct
    type nonrec t = Listener.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Listener.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Listener.of_xml)
    let of_json j =
      list_of_json ~kind:"Listeners" ~of_json:Listener.of_json j
    let to_json v = composed_to_json to_value v
  end
module DuplicateAccessPointNameException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified load balancer name already exists for this account."]
module DuplicateTagKeysException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A tag key was specified more than once."]
module InvalidSchemeException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified value for the schema is not valid. You can only specify a scheme for load balancers in a VPC."]
module InvalidSecurityGroupException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "One or more of the specified security groups do not exist."]
module InvalidSubnetException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified VPC has no associated Internet gateway."]
module OperationNotPermittedException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "This operation is not allowed."]
module SubnetNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "One or more of the specified subnets do not exist."]
module TooManyAccessPointsException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The quota for the number of load balancers has been reached."]
module TooManyTagsException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The quota for the number of tags that can be assigned to a load balancer has been reached."]
module SetLoadBalancerPoliciesOfListenerOutput =
  struct
    type setLoadBalancerPoliciesOfListenerResult = unit
    and responseMetaData = unit
    and t =
      {
      setLoadBalancerPoliciesOfListenerResult:
        setLoadBalancerPoliciesOfListenerResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `ListenerNotFoundException of ListenerNotFoundException.t 
      | `PolicyNotFoundException of PolicyNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { setLoadBalancerPoliciesOfListenerResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | "ListenerNotFoundException" ->
          `ListenerNotFoundException (ListenerNotFoundException.of_json json)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | "ListenerNotFoundException" ->
          `ListenerNotFoundException (ListenerNotFoundException.of_xml xml)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `ListenerNotFoundException e ->
          `Assoc
            [("error", (`String "ListenerNotFoundException"));
            ("details", (ListenerNotFoundException.to_json e))]
      | `PolicyNotFoundException e ->
          `Assoc
            [("error", (`String "PolicyNotFoundException"));
            ("details", (PolicyNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of SetLoadBalancePoliciesOfListener."]
module SetLoadBalancerPoliciesOfListenerInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      loadBalancerPort: AccessPointPort.t
        [@ocaml.doc "The external port of the load balancer."];
      policyNames: PolicyNames.t
        [@ocaml.doc
          "The names of the policies. This list must include all policies to be enabled. If you omit a policy that is currently enabled, it is disabled. If the list is empty, all current policies are disabled."]}
    let context_ = "SetLoadBalancerPoliciesOfListenerInput"
    let make ~loadBalancerName =
      fun ~loadBalancerPort ->
        fun ~policyNames ->
          fun () -> { loadBalancerName; loadBalancerPort; policyNames }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("LoadBalancerPort",
          (Some (AccessPointPort.to_value x.loadBalancerPort)));
        ("PolicyNames", (Some (PolicyNames.to_value x.policyNames)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyNames =
        PolicyNames.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyNames") in
      let loadBalancerPort =
        AccessPointPort.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerPort") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~policyNames ~loadBalancerPort ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyNames =
        field_map_exn json__ "PolicyNames" PolicyNames.of_json in
      let loadBalancerPort =
        field_map_exn json__ "LoadBalancerPort" AccessPointPort.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~policyNames ~loadBalancerPort ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for SetLoadBalancePoliciesOfListener."]
module SetLoadBalancerPoliciesForBackendServerOutput =
  struct
    type setLoadBalancerPoliciesForBackendServerResult = unit
    and responseMetaData = unit
    and t =
      {
      setLoadBalancerPoliciesForBackendServerResult:
        setLoadBalancerPoliciesForBackendServerResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `PolicyNotFoundException of PolicyNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      {
        setLoadBalancerPoliciesForBackendServerResult = ();
        responseMetaData = ()
      }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `PolicyNotFoundException e ->
          `Assoc
            [("error", (`String "PolicyNotFoundException"));
            ("details", (PolicyNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output of SetLoadBalancerPoliciesForBackendServer."]
module SetLoadBalancerPoliciesForBackendServerInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      instancePort: EndPointPort.t
        [@ocaml.doc "The port number associated with the EC2 instance."];
      policyNames: PolicyNames.t
        [@ocaml.doc
          "The names of the policies. If the list is empty, then all current polices are removed from the EC2 instance."]}
    let context_ = "SetLoadBalancerPoliciesForBackendServerInput"
    let make ~loadBalancerName =
      fun ~instancePort ->
        fun ~policyNames ->
          fun () -> { loadBalancerName; instancePort; policyNames }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("InstancePort", (Some (EndPointPort.to_value x.instancePort)));
        ("PolicyNames", (Some (PolicyNames.to_value x.policyNames)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyNames =
        PolicyNames.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyNames") in
      let instancePort =
        EndPointPort.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstancePort") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~policyNames ~instancePort ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyNames =
        field_map_exn json__ "PolicyNames" PolicyNames.of_json in
      let instancePort =
        field_map_exn json__ "InstancePort" EndPointPort.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~policyNames ~instancePort ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for SetLoadBalancerPoliciesForBackendServer."]
module SetLoadBalancerListenerSSLCertificateOutput =
  struct
    type setLoadBalancerListenerSSLCertificateResult = unit
    and responseMetaData = unit
    and t =
      {
      setLoadBalancerListenerSSLCertificateResult:
        setLoadBalancerListenerSSLCertificateResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `CertificateNotFoundException of CertificateNotFoundException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `ListenerNotFoundException of ListenerNotFoundException.t 
      | `UnsupportedProtocolException of UnsupportedProtocolException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      {
        setLoadBalancerListenerSSLCertificateResult = ();
        responseMetaData = ()
      }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "CertificateNotFoundException" ->
          `CertificateNotFoundException
            (CertificateNotFoundException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | "ListenerNotFoundException" ->
          `ListenerNotFoundException (ListenerNotFoundException.of_json json)
      | "UnsupportedProtocolException" ->
          `UnsupportedProtocolException
            (UnsupportedProtocolException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "CertificateNotFoundException" ->
          `CertificateNotFoundException
            (CertificateNotFoundException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | "ListenerNotFoundException" ->
          `ListenerNotFoundException (ListenerNotFoundException.of_xml xml)
      | "UnsupportedProtocolException" ->
          `UnsupportedProtocolException
            (UnsupportedProtocolException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `CertificateNotFoundException e ->
          `Assoc
            [("error", (`String "CertificateNotFoundException"));
            ("details", (CertificateNotFoundException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `ListenerNotFoundException e ->
          `Assoc
            [("error", (`String "ListenerNotFoundException"));
            ("details", (ListenerNotFoundException.to_json e))]
      | `UnsupportedProtocolException e ->
          `Assoc
            [("error", (`String "UnsupportedProtocolException"));
            ("details", (UnsupportedProtocolException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output of SetLoadBalancerListenerSSLCertificate."]
module SetLoadBalancerListenerSSLCertificateInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      loadBalancerPort: AccessPointPort.t
        [@ocaml.doc "The port that uses the specified SSL certificate."];
      sSLCertificateId: SSLCertificateId.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the SSL certificate."]}
    let context_ = "SetLoadBalancerListenerSSLCertificateInput"
    let make ~loadBalancerName =
      fun ~loadBalancerPort ->
        fun ~sSLCertificateId ->
          fun () -> { loadBalancerName; loadBalancerPort; sSLCertificateId }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("LoadBalancerPort",
          (Some (AccessPointPort.to_value x.loadBalancerPort)));
        ("SSLCertificateId",
          (Some (SSLCertificateId.to_value x.sSLCertificateId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sSLCertificateId =
        SSLCertificateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SSLCertificateId") in
      let loadBalancerPort =
        AccessPointPort.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerPort") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~sSLCertificateId ~loadBalancerPort ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sSLCertificateId =
        field_map_exn json__ "SSLCertificateId" SSLCertificateId.of_json in
      let loadBalancerPort =
        field_map_exn json__ "LoadBalancerPort" AccessPointPort.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~sSLCertificateId ~loadBalancerPort ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for SetLoadBalancerListenerSSLCertificate."]
module RemoveTagsOutput =
  struct
    type removeTagsResult = unit
    and responseMetaData = unit
    and t =
      {
      removeTagsResult: removeTagsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { removeTagsResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of RemoveTags."]
module RemoveTagsInput =
  struct
    type nonrec t =
      {
      loadBalancerNames: LoadBalancerNames.t
        [@ocaml.doc
          "The name of the load balancer. You can specify a maximum of one load balancer name."];
      tags: TagKeyList.t [@ocaml.doc "The list of tag keys to remove."]}
    let context_ = "RemoveTagsInput"
    let make ~loadBalancerNames =
      fun ~tags -> fun () -> { loadBalancerNames; tags }
    let to_value x =
      structure_to_value
        [("LoadBalancerNames",
           (Some (LoadBalancerNames.to_value x.loadBalancerNames)));
        ("Tags", (Some (TagKeyList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagKeyList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let loadBalancerNames =
        LoadBalancerNames.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerNames") in
      make ~tags ~loadBalancerNames ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "Tags" TagKeyList.of_json in
      let loadBalancerNames =
        field_map_exn json__ "LoadBalancerNames" LoadBalancerNames.of_json in
      make ~tags ~loadBalancerNames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for RemoveTags."]
module RemoveAvailabilityZonesOutput =
  struct
    type disableAvailabilityZonesForLoadBalancerResult =
      {
      availabilityZones: AvailabilityZones.t option
        [@ocaml.doc
          "The remaining Availability Zones for the load balancer."]}
    and responseMetaData = unit
    and t =
      {
      disableAvailabilityZonesForLoadBalancerResult:
        disableAvailabilityZonesForLoadBalancerResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "RemoveAvailabilityZonesOutput"
    let make ?availabilityZones =
      fun () ->
        {
          disableAvailabilityZonesForLoadBalancerResult =
            { availabilityZones };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.disableAvailabilityZonesForLoadBalancerResult in
      structure_to_wrapped_value
        [("AvailabilityZones",
           (Option.map x.availabilityZones ~f:AvailabilityZones.to_value))]
        ~wrapper:"DisableAvailabilityZonesForLoadBalancerResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DisableAvailabilityZonesForLoadBalancerResult" in
      let availabilityZones =
        (Option.map ~f:AvailabilityZones.of_xml)
          (Xml.child xml_arg0 "AvailabilityZones") in
      make ?availabilityZones ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let availabilityZones =
        field_map json__ "AvailabilityZones" AvailabilityZones.of_json in
      make ?availabilityZones ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output for DisableAvailabilityZonesForLoadBalancer."]
module RemoveAvailabilityZonesInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      availabilityZones: AvailabilityZones.t
        [@ocaml.doc "The Availability Zones."]}
    let context_ = "RemoveAvailabilityZonesInput"
    let make ~loadBalancerName =
      fun ~availabilityZones ->
        fun () -> { loadBalancerName; availabilityZones }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("AvailabilityZones",
          (Some (AvailabilityZones.to_value x.availabilityZones)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availabilityZones =
        AvailabilityZones.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AvailabilityZones") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~availabilityZones ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let availabilityZones =
        field_map_exn json__ "AvailabilityZones" AvailabilityZones.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~availabilityZones ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DisableAvailabilityZonesForLoadBalancer."]
module RegisterEndPointsOutput =
  struct
    type registerInstancesWithLoadBalancerResult =
      {
      instances: Instances.t option
        [@ocaml.doc "The updated list of instances for the load balancer."]}
    and responseMetaData = unit
    and t =
      {
      registerInstancesWithLoadBalancerResult:
        registerInstancesWithLoadBalancerResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `InvalidEndPointException of InvalidEndPointException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "RegisterEndPointsOutput"
    let make ?instances =
      fun () ->
        {
          registerInstancesWithLoadBalancerResult = { instances };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "InvalidEndPointException" ->
          `InvalidEndPointException (InvalidEndPointException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "InvalidEndPointException" ->
          `InvalidEndPointException (InvalidEndPointException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `InvalidEndPointException e ->
          `Assoc
            [("error", (`String "InvalidEndPointException"));
            ("details", (InvalidEndPointException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.registerInstancesWithLoadBalancerResult in
      structure_to_wrapped_value
        [("Instances", (Option.map x.instances ~f:Instances.to_value))]
        ~wrapper:"RegisterInstancesWithLoadBalancerResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "RegisterInstancesWithLoadBalancerResult" in
      let instances =
        (Option.map ~f:Instances.of_xml) (Xml.child xml_arg0 "Instances") in
      make ?instances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instances = field_map json__ "Instances" Instances.of_json in
      make ?instances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output of RegisterInstancesWithLoadBalancer."]
module RegisterEndPointsInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      instances: Instances.t [@ocaml.doc "The IDs of the instances."]}
    let context_ = "RegisterEndPointsInput"
    let make ~loadBalancerName =
      fun ~instances -> fun () -> { loadBalancerName; instances }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("Instances", (Some (Instances.to_value x.instances)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instances =
        Instances.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Instances") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~instances ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instances = field_map_exn json__ "Instances" Instances.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~instances ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for RegisterInstancesWithLoadBalancer."]
module ModifyLoadBalancerAttributesOutput =
  struct
    type modifyLoadBalancerAttributesResult =
      {
      loadBalancerName: AccessPointName.t option
        [@ocaml.doc "The name of the load balancer."];
      loadBalancerAttributes: LoadBalancerAttributes.t option
        [@ocaml.doc "Information about the load balancer attributes."]}
    and responseMetaData = unit
    and t =
      {
      modifyLoadBalancerAttributesResult: modifyLoadBalancerAttributesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `LoadBalancerAttributeNotFoundException of
          LoadBalancerAttributeNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ModifyLoadBalancerAttributesOutput"
    let make ?loadBalancerName =
      fun ?loadBalancerAttributes ->
        fun () ->
          {
            modifyLoadBalancerAttributesResult =
              { loadBalancerName; loadBalancerAttributes };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | "LoadBalancerAttributeNotFoundException" ->
          `LoadBalancerAttributeNotFoundException
            (LoadBalancerAttributeNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | "LoadBalancerAttributeNotFoundException" ->
          `LoadBalancerAttributeNotFoundException
            (LoadBalancerAttributeNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `LoadBalancerAttributeNotFoundException e ->
          `Assoc
            [("error", (`String "LoadBalancerAttributeNotFoundException"));
            ("details", (LoadBalancerAttributeNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.modifyLoadBalancerAttributesResult in
      structure_to_wrapped_value
        [("LoadBalancerName",
           (Option.map x.loadBalancerName ~f:AccessPointName.to_value));
        ("LoadBalancerAttributes",
          (Option.map x.loadBalancerAttributes
             ~f:LoadBalancerAttributes.to_value))]
        ~wrapper:"ModifyLoadBalancerAttributesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "ModifyLoadBalancerAttributesResult" in
      let loadBalancerAttributes =
        (Option.map ~f:LoadBalancerAttributes.of_xml)
          (Xml.child xml_arg0 "LoadBalancerAttributes") in
      let loadBalancerName =
        (Option.map ~f:AccessPointName.of_xml)
          (Xml.child xml_arg0 "LoadBalancerName") in
      make ?loadBalancerAttributes ?loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let loadBalancerAttributes =
        field_map json__ "LoadBalancerAttributes"
          LoadBalancerAttributes.of_json in
      let loadBalancerName =
        field_map json__ "LoadBalancerName" AccessPointName.of_json in
      make ?loadBalancerAttributes ?loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of ModifyLoadBalancerAttributes."]
module ModifyLoadBalancerAttributesInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      loadBalancerAttributes: LoadBalancerAttributes.t
        [@ocaml.doc "The attributes for the load balancer."]}
    let context_ = "ModifyLoadBalancerAttributesInput"
    let make ~loadBalancerName =
      fun ~loadBalancerAttributes ->
        fun () -> { loadBalancerName; loadBalancerAttributes }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("LoadBalancerAttributes",
          (Some (LoadBalancerAttributes.to_value x.loadBalancerAttributes)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let loadBalancerAttributes =
        LoadBalancerAttributes.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerAttributes") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~loadBalancerAttributes ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let loadBalancerAttributes =
        field_map_exn json__ "LoadBalancerAttributes"
          LoadBalancerAttributes.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~loadBalancerAttributes ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for ModifyLoadBalancerAttributes."]
module DetachLoadBalancerFromSubnetsOutput =
  struct
    type detachLoadBalancerFromSubnetsResult =
      {
      subnets: Subnets.t option
        [@ocaml.doc
          "The IDs of the remaining subnets for the load balancer."]}
    and responseMetaData = unit
    and t =
      {
      detachLoadBalancerFromSubnetsResult:
        detachLoadBalancerFromSubnetsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DetachLoadBalancerFromSubnetsOutput"
    let make ?subnets =
      fun () ->
        {
          detachLoadBalancerFromSubnetsResult = { subnets };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.detachLoadBalancerFromSubnetsResult in
      structure_to_wrapped_value
        [("Subnets", (Option.map x.subnets ~f:Subnets.to_value))]
        ~wrapper:"DetachLoadBalancerFromSubnetsResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DetachLoadBalancerFromSubnetsResult" in
      let subnets =
        (Option.map ~f:Subnets.of_xml) (Xml.child xml_arg0 "Subnets") in
      make ?subnets ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let subnets = field_map json__ "Subnets" Subnets.of_json in
      make ?subnets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of DetachLoadBalancerFromSubnets."]
module DetachLoadBalancerFromSubnetsInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      subnets: Subnets.t [@ocaml.doc "The IDs of the subnets."]}
    let context_ = "DetachLoadBalancerFromSubnetsInput"
    let make ~loadBalancerName =
      fun ~subnets -> fun () -> { loadBalancerName; subnets }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("Subnets", (Some (Subnets.to_value x.subnets)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subnets =
        Subnets.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Subnets") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~subnets ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let subnets = field_map_exn json__ "Subnets" Subnets.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~subnets ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DetachLoadBalancerFromSubnets."]
module DescribeTagsOutput =
  struct
    type describeTagsResult =
      {
      tagDescriptions: TagDescriptions.t option
        [@ocaml.doc "Information about the tags."]}
    and responseMetaData = unit
    and t =
      {
      describeTagsResult: describeTagsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeTagsOutput"
    let make ?tagDescriptions =
      fun () ->
        { describeTagsResult = { tagDescriptions }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeTagsResult in
      structure_to_wrapped_value
        [("TagDescriptions",
           (Option.map x.tagDescriptions ~f:TagDescriptions.to_value))]
        ~wrapper:"DescribeTagsResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "DescribeTagsResult" in
      let tagDescriptions =
        (Option.map ~f:TagDescriptions.of_xml)
          (Xml.child xml_arg0 "TagDescriptions") in
      make ?tagDescriptions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagDescriptions =
        field_map json__ "TagDescriptions" TagDescriptions.of_json in
      make ?tagDescriptions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output for DescribeTags."]
module DescribeTagsInput =
  struct
    type nonrec t =
      {
      loadBalancerNames: LoadBalancerNamesMax20.t
        [@ocaml.doc "The names of the load balancers."]}
    let context_ = "DescribeTagsInput"
    let make ~loadBalancerNames = fun () -> { loadBalancerNames }
    let to_value x =
      structure_to_value
        [("LoadBalancerNames",
           (Some (LoadBalancerNamesMax20.to_value x.loadBalancerNames)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let loadBalancerNames =
        LoadBalancerNamesMax20.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerNames") in
      make ~loadBalancerNames ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let loadBalancerNames =
        field_map_exn json__ "LoadBalancerNames"
          LoadBalancerNamesMax20.of_json in
      make ~loadBalancerNames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DescribeTags."]
module DescribeLoadBalancerPolicyTypesOutput =
  struct
    type describeLoadBalancerPolicyTypesResult =
      {
      policyTypeDescriptions: PolicyTypeDescriptions.t option
        [@ocaml.doc "Information about the policy types."]}
    and responseMetaData = unit
    and t =
      {
      describeLoadBalancerPolicyTypesResult:
        describeLoadBalancerPolicyTypesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `PolicyTypeNotFoundException of PolicyTypeNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeLoadBalancerPolicyTypesOutput"
    let make ?policyTypeDescriptions =
      fun () ->
        {
          describeLoadBalancerPolicyTypesResult = { policyTypeDescriptions };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "PolicyTypeNotFoundException" ->
          `PolicyTypeNotFoundException
            (PolicyTypeNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "PolicyTypeNotFoundException" ->
          `PolicyTypeNotFoundException
            (PolicyTypeNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `PolicyTypeNotFoundException e ->
          `Assoc
            [("error", (`String "PolicyTypeNotFoundException"));
            ("details", (PolicyTypeNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeLoadBalancerPolicyTypesResult in
      structure_to_wrapped_value
        [("PolicyTypeDescriptions",
           (Option.map x.policyTypeDescriptions
              ~f:PolicyTypeDescriptions.to_value))]
        ~wrapper:"DescribeLoadBalancerPolicyTypesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DescribeLoadBalancerPolicyTypesResult" in
      let policyTypeDescriptions =
        (Option.map ~f:PolicyTypeDescriptions.of_xml)
          (Xml.child xml_arg0 "PolicyTypeDescriptions") in
      make ?policyTypeDescriptions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyTypeDescriptions =
        field_map json__ "PolicyTypeDescriptions"
          PolicyTypeDescriptions.of_json in
      make ?policyTypeDescriptions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of DescribeLoadBalancerPolicyTypes."]
module DescribeLoadBalancerPolicyTypesInput =
  struct
    type nonrec t =
      {
      policyTypeNames: PolicyTypeNames.t option
        [@ocaml.doc
          "The names of the policy types. If no names are specified, describes all policy types defined by Elastic Load Balancing."]}
    let make ?policyTypeNames = fun () -> { policyTypeNames }
    let to_value x =
      structure_to_value
        [("PolicyTypeNames",
           (Option.map x.policyTypeNames ~f:PolicyTypeNames.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyTypeNames =
        (Option.map ~f:PolicyTypeNames.of_xml)
          (Xml.child xml_arg0 "PolicyTypeNames") in
      make ?policyTypeNames ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyTypeNames =
        field_map json__ "PolicyTypeNames" PolicyTypeNames.of_json in
      make ?policyTypeNames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DescribeLoadBalancerPolicyTypes."]
module DescribeLoadBalancerPoliciesOutput =
  struct
    type describeLoadBalancerPoliciesResult =
      {
      policyDescriptions: PolicyDescriptions.t option
        [@ocaml.doc "Information about the policies."]}
    and responseMetaData = unit
    and t =
      {
      describeLoadBalancerPoliciesResult: describeLoadBalancerPoliciesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `PolicyNotFoundException of PolicyNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeLoadBalancerPoliciesOutput"
    let make ?policyDescriptions =
      fun () ->
        {
          describeLoadBalancerPoliciesResult = { policyDescriptions };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `PolicyNotFoundException e ->
          `Assoc
            [("error", (`String "PolicyNotFoundException"));
            ("details", (PolicyNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeLoadBalancerPoliciesResult in
      structure_to_wrapped_value
        [("PolicyDescriptions",
           (Option.map x.policyDescriptions ~f:PolicyDescriptions.to_value))]
        ~wrapper:"DescribeLoadBalancerPoliciesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DescribeLoadBalancerPoliciesResult" in
      let policyDescriptions =
        (Option.map ~f:PolicyDescriptions.of_xml)
          (Xml.child xml_arg0 "PolicyDescriptions") in
      make ?policyDescriptions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyDescriptions =
        field_map json__ "PolicyDescriptions" PolicyDescriptions.of_json in
      make ?policyDescriptions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of DescribeLoadBalancerPolicies."]
module DescribeLoadBalancerPoliciesInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t option
        [@ocaml.doc "The name of the load balancer."];
      policyNames: PolicyNames.t option
        [@ocaml.doc "The names of the policies."]}
    let make ?loadBalancerName =
      fun ?policyNames -> fun () -> { loadBalancerName; policyNames }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Option.map x.loadBalancerName ~f:AccessPointName.to_value));
        ("PolicyNames", (Option.map x.policyNames ~f:PolicyNames.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyNames =
        (Option.map ~f:PolicyNames.of_xml) (Xml.child xml_arg0 "PolicyNames") in
      let loadBalancerName =
        (Option.map ~f:AccessPointName.of_xml)
          (Xml.child xml_arg0 "LoadBalancerName") in
      make ?policyNames ?loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyNames = field_map json__ "PolicyNames" PolicyNames.of_json in
      let loadBalancerName =
        field_map json__ "LoadBalancerName" AccessPointName.of_json in
      make ?policyNames ?loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DescribeLoadBalancerPolicies."]
module DescribeLoadBalancerAttributesOutput =
  struct
    type describeLoadBalancerAttributesResult =
      {
      loadBalancerAttributes: LoadBalancerAttributes.t option
        [@ocaml.doc "Information about the load balancer attributes."]}
    and responseMetaData = unit
    and t =
      {
      describeLoadBalancerAttributesResult:
        describeLoadBalancerAttributesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `LoadBalancerAttributeNotFoundException of
          LoadBalancerAttributeNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeLoadBalancerAttributesOutput"
    let make ?loadBalancerAttributes =
      fun () ->
        {
          describeLoadBalancerAttributesResult = { loadBalancerAttributes };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "LoadBalancerAttributeNotFoundException" ->
          `LoadBalancerAttributeNotFoundException
            (LoadBalancerAttributeNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "LoadBalancerAttributeNotFoundException" ->
          `LoadBalancerAttributeNotFoundException
            (LoadBalancerAttributeNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `LoadBalancerAttributeNotFoundException e ->
          `Assoc
            [("error", (`String "LoadBalancerAttributeNotFoundException"));
            ("details", (LoadBalancerAttributeNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeLoadBalancerAttributesResult in
      structure_to_wrapped_value
        [("LoadBalancerAttributes",
           (Option.map x.loadBalancerAttributes
              ~f:LoadBalancerAttributes.to_value))]
        ~wrapper:"DescribeLoadBalancerAttributesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DescribeLoadBalancerAttributesResult" in
      let loadBalancerAttributes =
        (Option.map ~f:LoadBalancerAttributes.of_xml)
          (Xml.child xml_arg0 "LoadBalancerAttributes") in
      make ?loadBalancerAttributes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let loadBalancerAttributes =
        field_map json__ "LoadBalancerAttributes"
          LoadBalancerAttributes.of_json in
      make ?loadBalancerAttributes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of DescribeLoadBalancerAttributes."]
module DescribeLoadBalancerAttributesInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."]}
    let context_ = "DescribeLoadBalancerAttributesInput"
    let make ~loadBalancerName = fun () -> { loadBalancerName }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DescribeLoadBalancerAttributes."]
module DescribeEndPointStateOutput =
  struct
    type describeInstanceHealthResult =
      {
      instanceStates: InstanceStates.t option
        [@ocaml.doc "Information about the health of the instances."]}
    and responseMetaData = unit
    and t =
      {
      describeInstanceHealthResult: describeInstanceHealthResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `InvalidEndPointException of InvalidEndPointException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeEndPointStateOutput"
    let make ?instanceStates =
      fun () ->
        {
          describeInstanceHealthResult = { instanceStates };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "InvalidEndPointException" ->
          `InvalidEndPointException (InvalidEndPointException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "InvalidEndPointException" ->
          `InvalidEndPointException (InvalidEndPointException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `InvalidEndPointException e ->
          `Assoc
            [("error", (`String "InvalidEndPointException"));
            ("details", (InvalidEndPointException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeInstanceHealthResult in
      structure_to_wrapped_value
        [("InstanceStates",
           (Option.map x.instanceStates ~f:InstanceStates.to_value))]
        ~wrapper:"DescribeInstanceHealthResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeInstanceHealthResult" in
      let instanceStates =
        (Option.map ~f:InstanceStates.of_xml)
          (Xml.child xml_arg0 "InstanceStates") in
      make ?instanceStates ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceStates =
        field_map json__ "InstanceStates" InstanceStates.of_json in
      make ?instanceStates ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output for DescribeInstanceHealth."]
module DescribeEndPointStateInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      instances: Instances.t option [@ocaml.doc "The IDs of the instances."]}
    let context_ = "DescribeEndPointStateInput"
    let make ?instances =
      fun ~loadBalancerName -> fun () -> { instances; loadBalancerName }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("Instances", (Option.map x.instances ~f:Instances.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instances =
        (Option.map ~f:Instances.of_xml) (Xml.child xml_arg0 "Instances") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ?instances ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instances = field_map json__ "Instances" Instances.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ?instances ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DescribeInstanceHealth."]
module DescribeAccountLimitsOutput =
  struct
    type describeAccountLimitsResult =
      {
      limits: Limits.t option [@ocaml.doc "Information about the limits."];
      nextMarker: Marker.t option
        [@ocaml.doc
          "The marker to use when requesting the next set of results. If there are no additional results, the string is empty."]}
    and responseMetaData = unit
    and t =
      {
      describeAccountLimitsResult: describeAccountLimitsResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeAccountLimitsOutput"
    let make ?limits =
      fun ?nextMarker ->
        fun () ->
          {
            describeAccountLimitsResult = { limits; nextMarker };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeAccountLimitsResult in
      structure_to_wrapped_value
        [("Limits", (Option.map x.limits ~f:Limits.to_value));
        ("NextMarker", (Option.map x.nextMarker ~f:Marker.to_value))]
        ~wrapper:"DescribeAccountLimitsResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeAccountLimitsResult" in
      let nextMarker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "NextMarker") in
      let limits =
        (Option.map ~f:Limits.of_xml) (Xml.child xml_arg0 "Limits") in
      make ?nextMarker ?limits ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextMarker = field_map json__ "NextMarker" Marker.of_json in
      let limits = field_map json__ "Limits" Limits.of_json in
      make ?nextMarker ?limits ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the current Elastic Load Balancing resource limits for your AWS account. For more information, see Limits for Your Classic Load Balancer in the Classic Load Balancers Guide."]
module DescribeAccountLimitsInput =
  struct
    type nonrec t =
      {
      marker: Marker.t option
        [@ocaml.doc
          "The marker for the next set of results. (You received this marker from a previous call.)"];
      pageSize: PageSize.t option
        [@ocaml.doc
          "The maximum number of results to return with this call."]}
    let make ?marker = fun ?pageSize -> fun () -> { marker; pageSize }
    let to_value x =
      structure_to_value
        [("Marker", (Option.map x.marker ~f:Marker.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      make ?pageSize ?marker ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pageSize = field_map json__ "PageSize" PageSize.of_json in
      let marker = field_map json__ "Marker" Marker.of_json in
      make ?pageSize ?marker ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the current Elastic Load Balancing resource limits for your AWS account. For more information, see Limits for Your Classic Load Balancer in the Classic Load Balancers Guide."]
module DescribeAccessPointsOutput =
  struct
    type describeLoadBalancersResult =
      {
      loadBalancerDescriptions: LoadBalancerDescriptions.t option
        [@ocaml.doc "Information about the load balancers."];
      nextMarker: Marker.t option
        [@ocaml.doc
          "The marker to use when requesting the next set of results. If there are no additional results, the string is empty."]}
    and responseMetaData = unit
    and t =
      {
      describeLoadBalancersResult: describeLoadBalancersResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `DependencyThrottleException of DependencyThrottleException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeAccessPointsOutput"
    let make ?loadBalancerDescriptions =
      fun ?nextMarker ->
        fun () ->
          {
            describeLoadBalancersResult =
              { loadBalancerDescriptions; nextMarker };
            responseMetaData = ()
          }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "DependencyThrottleException" ->
          `DependencyThrottleException
            (DependencyThrottleException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "DependencyThrottleException" ->
          `DependencyThrottleException
            (DependencyThrottleException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `DependencyThrottleException e ->
          `Assoc
            [("error", (`String "DependencyThrottleException"));
            ("details", (DependencyThrottleException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeLoadBalancersResult in
      structure_to_wrapped_value
        [("LoadBalancerDescriptions",
           (Option.map x.loadBalancerDescriptions
              ~f:LoadBalancerDescriptions.to_value));
        ("NextMarker", (Option.map x.nextMarker ~f:Marker.to_value))]
        ~wrapper:"DescribeLoadBalancersResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeLoadBalancersResult" in
      let nextMarker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "NextMarker") in
      let loadBalancerDescriptions =
        (Option.map ~f:LoadBalancerDescriptions.of_xml)
          (Xml.child xml_arg0 "LoadBalancerDescriptions") in
      make ?nextMarker ?loadBalancerDescriptions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextMarker = field_map json__ "NextMarker" Marker.of_json in
      let loadBalancerDescriptions =
        field_map json__ "LoadBalancerDescriptions"
          LoadBalancerDescriptions.of_json in
      make ?nextMarker ?loadBalancerDescriptions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DescribeLoadBalancers."]
module DescribeAccessPointsInput =
  struct
    type nonrec t =
      {
      loadBalancerNames: LoadBalancerNames.t option
        [@ocaml.doc "The names of the load balancers."];
      marker: Marker.t option
        [@ocaml.doc
          "The marker for the next set of results. (You received this marker from a previous call.)"];
      pageSize: PageSize.t option
        [@ocaml.doc
          "The maximum number of results to return with this call (a number from 1 to 400). The default is 400."]}
    let make ?loadBalancerNames =
      fun ?marker ->
        fun ?pageSize -> fun () -> { loadBalancerNames; marker; pageSize }
    let to_value x =
      structure_to_value
        [("LoadBalancerNames",
           (Option.map x.loadBalancerNames ~f:LoadBalancerNames.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let loadBalancerNames =
        (Option.map ~f:LoadBalancerNames.of_xml)
          (Xml.child xml_arg0 "LoadBalancerNames") in
      make ?pageSize ?marker ?loadBalancerNames ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pageSize = field_map json__ "PageSize" PageSize.of_json in
      let marker = field_map json__ "Marker" Marker.of_json in
      let loadBalancerNames =
        field_map json__ "LoadBalancerNames" LoadBalancerNames.of_json in
      make ?pageSize ?marker ?loadBalancerNames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DescribeLoadBalancers."]
module DeregisterEndPointsOutput =
  struct
    type deregisterInstancesFromLoadBalancerResult =
      {
      instances: Instances.t option
        [@ocaml.doc
          "The remaining instances registered with the load balancer."]}
    and responseMetaData = unit
    and t =
      {
      deregisterInstancesFromLoadBalancerResult:
        deregisterInstancesFromLoadBalancerResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `InvalidEndPointException of InvalidEndPointException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DeregisterEndPointsOutput"
    let make ?instances =
      fun () ->
        {
          deregisterInstancesFromLoadBalancerResult = { instances };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "InvalidEndPointException" ->
          `InvalidEndPointException (InvalidEndPointException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "InvalidEndPointException" ->
          `InvalidEndPointException (InvalidEndPointException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `InvalidEndPointException e ->
          `Assoc
            [("error", (`String "InvalidEndPointException"));
            ("details", (InvalidEndPointException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.deregisterInstancesFromLoadBalancerResult in
      structure_to_wrapped_value
        [("Instances", (Option.map x.instances ~f:Instances.to_value))]
        ~wrapper:"DeregisterInstancesFromLoadBalancerResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DeregisterInstancesFromLoadBalancerResult" in
      let instances =
        (Option.map ~f:Instances.of_xml) (Xml.child xml_arg0 "Instances") in
      make ?instances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instances = field_map json__ "Instances" Instances.of_json in
      make ?instances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output of DeregisterInstancesFromLoadBalancer."]
module DeregisterEndPointsInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      instances: Instances.t [@ocaml.doc "The IDs of the instances."]}
    let context_ = "DeregisterEndPointsInput"
    let make ~loadBalancerName =
      fun ~instances -> fun () -> { loadBalancerName; instances }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("Instances", (Some (Instances.to_value x.instances)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instances =
        Instances.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Instances") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~instances ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instances = field_map_exn json__ "Instances" Instances.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~instances ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for DeregisterInstancesFromLoadBalancer."]
module DeleteLoadBalancerPolicyOutput =
  struct
    type deleteLoadBalancerPolicyResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteLoadBalancerPolicyResult: deleteLoadBalancerPolicyResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { deleteLoadBalancerPolicyResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of DeleteLoadBalancerPolicy."]
module DeleteLoadBalancerPolicyInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      policyName: PolicyName.t [@ocaml.doc "The name of the policy."]}
    let context_ = "DeleteLoadBalancerPolicyInput"
    let make ~loadBalancerName =
      fun ~policyName -> fun () -> { loadBalancerName; policyName }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("PolicyName", (Some (PolicyName.to_value x.policyName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyName =
        PolicyName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyName") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~policyName ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyName = field_map_exn json__ "PolicyName" PolicyName.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~policyName ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DeleteLoadBalancerPolicy."]
module DeleteLoadBalancerListenerOutput =
  struct
    type deleteLoadBalancerListenersResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteLoadBalancerListenersResult: deleteLoadBalancerListenersResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { deleteLoadBalancerListenersResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of DeleteLoadBalancerListeners."]
module DeleteLoadBalancerListenerInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      loadBalancerPorts: Ports.t
        [@ocaml.doc "The client port numbers of the listeners."]}
    let context_ = "DeleteLoadBalancerListenerInput"
    let make ~loadBalancerName =
      fun ~loadBalancerPorts ->
        fun () -> { loadBalancerName; loadBalancerPorts }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("LoadBalancerPorts", (Some (Ports.to_value x.loadBalancerPorts)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let loadBalancerPorts =
        Ports.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerPorts") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~loadBalancerPorts ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let loadBalancerPorts =
        field_map_exn json__ "LoadBalancerPorts" Ports.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~loadBalancerPorts ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DeleteLoadBalancerListeners."]
module DeleteAccessPointOutput =
  struct
    type deleteLoadBalancerResult = unit
    and responseMetaData = unit
    and t =
      {
      deleteLoadBalancerResult: deleteLoadBalancerResult ;
      responseMetaData: responseMetaData }
    type error = [ `Unknown_operation_error of (string * string option) ]
    let make () = { deleteLoadBalancerResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of DeleteLoadBalancer."]
module DeleteAccessPointInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."]}
    let context_ = "DeleteAccessPointInput"
    let make ~loadBalancerName = fun () -> { loadBalancerName }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for DeleteLoadBalancer."]
module CreateLoadBalancerPolicyOutput =
  struct
    type createLoadBalancerPolicyResult = unit
    and responseMetaData = unit
    and t =
      {
      createLoadBalancerPolicyResult: createLoadBalancerPolicyResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `DuplicatePolicyNameException of DuplicatePolicyNameException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `PolicyTypeNotFoundException of PolicyTypeNotFoundException.t 
      | `TooManyPoliciesException of TooManyPoliciesException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { createLoadBalancerPolicyResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "DuplicatePolicyNameException" ->
          `DuplicatePolicyNameException
            (DuplicatePolicyNameException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | "PolicyTypeNotFoundException" ->
          `PolicyTypeNotFoundException
            (PolicyTypeNotFoundException.of_json json)
      | "TooManyPoliciesException" ->
          `TooManyPoliciesException (TooManyPoliciesException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "DuplicatePolicyNameException" ->
          `DuplicatePolicyNameException
            (DuplicatePolicyNameException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | "PolicyTypeNotFoundException" ->
          `PolicyTypeNotFoundException
            (PolicyTypeNotFoundException.of_xml xml)
      | "TooManyPoliciesException" ->
          `TooManyPoliciesException (TooManyPoliciesException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `DuplicatePolicyNameException e ->
          `Assoc
            [("error", (`String "DuplicatePolicyNameException"));
            ("details", (DuplicatePolicyNameException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `PolicyTypeNotFoundException e ->
          `Assoc
            [("error", (`String "PolicyTypeNotFoundException"));
            ("details", (PolicyTypeNotFoundException.to_json e))]
      | `TooManyPoliciesException e ->
          `Assoc
            [("error", (`String "TooManyPoliciesException"));
            ("details", (TooManyPoliciesException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of CreateLoadBalancerPolicy."]
module CreateLoadBalancerPolicyInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      policyName: PolicyName.t
        [@ocaml.doc
          "The name of the load balancer policy to be created. This name must be unique within the set of policies for this load balancer."];
      policyTypeName: PolicyTypeName.t
        [@ocaml.doc
          "The name of the base policy type. To get the list of policy types, use DescribeLoadBalancerPolicyTypes."];
      policyAttributes: PolicyAttributes.t option
        [@ocaml.doc "The policy attributes."]}
    let context_ = "CreateLoadBalancerPolicyInput"
    let make ?policyAttributes =
      fun ~loadBalancerName ->
        fun ~policyName ->
          fun ~policyTypeName ->
            fun () ->
              {
                policyAttributes;
                loadBalancerName;
                policyName;
                policyTypeName
              }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("PolicyName", (Some (PolicyName.to_value x.policyName)));
        ("PolicyTypeName", (Some (PolicyTypeName.to_value x.policyTypeName)));
        ("PolicyAttributes",
          (Option.map x.policyAttributes ~f:PolicyAttributes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyAttributes =
        (Option.map ~f:PolicyAttributes.of_xml)
          (Xml.child xml_arg0 "PolicyAttributes") in
      let policyTypeName =
        PolicyTypeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyTypeName") in
      let policyName =
        PolicyName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyName") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ?policyAttributes ~policyTypeName ~policyName ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyAttributes =
        field_map json__ "PolicyAttributes" PolicyAttributes.of_json in
      let policyTypeName =
        field_map_exn json__ "PolicyTypeName" PolicyTypeName.of_json in
      let policyName = field_map_exn json__ "PolicyName" PolicyName.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ?policyAttributes ~policyTypeName ~policyName ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for CreateLoadBalancerPolicy."]
module CreateLoadBalancerListenerOutput =
  struct
    type createLoadBalancerListenersResult = unit
    and responseMetaData = unit
    and t =
      {
      createLoadBalancerListenersResult: createLoadBalancerListenersResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `CertificateNotFoundException of CertificateNotFoundException.t 
      | `DuplicateListenerException of DuplicateListenerException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `UnsupportedProtocolException of UnsupportedProtocolException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { createLoadBalancerListenersResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "CertificateNotFoundException" ->
          `CertificateNotFoundException
            (CertificateNotFoundException.of_json json)
      | "DuplicateListenerException" ->
          `DuplicateListenerException
            (DuplicateListenerException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | "UnsupportedProtocolException" ->
          `UnsupportedProtocolException
            (UnsupportedProtocolException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "CertificateNotFoundException" ->
          `CertificateNotFoundException
            (CertificateNotFoundException.of_xml xml)
      | "DuplicateListenerException" ->
          `DuplicateListenerException (DuplicateListenerException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | "UnsupportedProtocolException" ->
          `UnsupportedProtocolException
            (UnsupportedProtocolException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `CertificateNotFoundException e ->
          `Assoc
            [("error", (`String "CertificateNotFoundException"));
            ("details", (CertificateNotFoundException.to_json e))]
      | `DuplicateListenerException e ->
          `Assoc
            [("error", (`String "DuplicateListenerException"));
            ("details", (DuplicateListenerException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `UnsupportedProtocolException e ->
          `Assoc
            [("error", (`String "UnsupportedProtocolException"));
            ("details", (UnsupportedProtocolException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for CreateLoadBalancerListener."]
module CreateLoadBalancerListenerInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      listeners: Listeners.t [@ocaml.doc "The listeners."]}
    let context_ = "CreateLoadBalancerListenerInput"
    let make ~loadBalancerName =
      fun ~listeners -> fun () -> { loadBalancerName; listeners }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("Listeners", (Some (Listeners.to_value x.listeners)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let listeners =
        Listeners.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Listeners") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~listeners ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let listeners = field_map_exn json__ "Listeners" Listeners.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~listeners ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for CreateLoadBalancerListeners."]
module CreateLBCookieStickinessPolicyOutput =
  struct
    type createLBCookieStickinessPolicyResult = unit
    and responseMetaData = unit
    and t =
      {
      createLBCookieStickinessPolicyResult:
        createLBCookieStickinessPolicyResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `DuplicatePolicyNameException of DuplicatePolicyNameException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `TooManyPoliciesException of TooManyPoliciesException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { createLBCookieStickinessPolicyResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "DuplicatePolicyNameException" ->
          `DuplicatePolicyNameException
            (DuplicatePolicyNameException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | "TooManyPoliciesException" ->
          `TooManyPoliciesException (TooManyPoliciesException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "DuplicatePolicyNameException" ->
          `DuplicatePolicyNameException
            (DuplicatePolicyNameException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | "TooManyPoliciesException" ->
          `TooManyPoliciesException (TooManyPoliciesException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `DuplicatePolicyNameException e ->
          `Assoc
            [("error", (`String "DuplicatePolicyNameException"));
            ("details", (DuplicatePolicyNameException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `TooManyPoliciesException e ->
          `Assoc
            [("error", (`String "TooManyPoliciesException"));
            ("details", (TooManyPoliciesException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output for CreateLBCookieStickinessPolicy."]
module CreateLBCookieStickinessPolicyInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      policyName: PolicyName.t
        [@ocaml.doc
          "The name of the policy being created. Policy names must consist of alphanumeric characters and dashes (-). This name must be unique within the set of policies for this load balancer."];
      cookieExpirationPeriod: CookieExpirationPeriod.t option
        [@ocaml.doc
          "The time period, in seconds, after which the cookie should be considered stale. If you do not specify this parameter, the default value is 0, which indicates that the sticky session should last for the duration of the browser session."]}
    let context_ = "CreateLBCookieStickinessPolicyInput"
    let make ?cookieExpirationPeriod =
      fun ~loadBalancerName ->
        fun ~policyName ->
          fun () -> { cookieExpirationPeriod; loadBalancerName; policyName }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("PolicyName", (Some (PolicyName.to_value x.policyName)));
        ("CookieExpirationPeriod",
          (Option.map x.cookieExpirationPeriod
             ~f:CookieExpirationPeriod.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cookieExpirationPeriod =
        (Option.map ~f:CookieExpirationPeriod.of_xml)
          (Xml.child xml_arg0 "CookieExpirationPeriod") in
      let policyName =
        PolicyName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyName") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ?cookieExpirationPeriod ~policyName ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cookieExpirationPeriod =
        field_map json__ "CookieExpirationPeriod"
          CookieExpirationPeriod.of_json in
      let policyName = field_map_exn json__ "PolicyName" PolicyName.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ?cookieExpirationPeriod ~policyName ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for CreateLBCookieStickinessPolicy."]
module CreateAppCookieStickinessPolicyOutput =
  struct
    type createAppCookieStickinessPolicyResult = unit
    and responseMetaData = unit
    and t =
      {
      createAppCookieStickinessPolicyResult:
        createAppCookieStickinessPolicyResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `DuplicatePolicyNameException of DuplicatePolicyNameException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `TooManyPoliciesException of TooManyPoliciesException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () =
      { createAppCookieStickinessPolicyResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "DuplicatePolicyNameException" ->
          `DuplicatePolicyNameException
            (DuplicatePolicyNameException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | "TooManyPoliciesException" ->
          `TooManyPoliciesException (TooManyPoliciesException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "DuplicatePolicyNameException" ->
          `DuplicatePolicyNameException
            (DuplicatePolicyNameException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | "TooManyPoliciesException" ->
          `TooManyPoliciesException (TooManyPoliciesException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `DuplicatePolicyNameException e ->
          `Assoc
            [("error", (`String "DuplicatePolicyNameException"));
            ("details", (DuplicatePolicyNameException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `TooManyPoliciesException e ->
          `Assoc
            [("error", (`String "TooManyPoliciesException"));
            ("details", (TooManyPoliciesException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output for CreateAppCookieStickinessPolicy."]
module CreateAppCookieStickinessPolicyInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      policyName: PolicyName.t
        [@ocaml.doc
          "The name of the policy being created. Policy names must consist of alphanumeric characters and dashes (-). This name must be unique within the set of policies for this load balancer."];
      cookieName: CookieName.t
        [@ocaml.doc
          "The name of the application cookie used for stickiness."]}
    let context_ = "CreateAppCookieStickinessPolicyInput"
    let make ~loadBalancerName =
      fun ~policyName ->
        fun ~cookieName ->
          fun () -> { loadBalancerName; policyName; cookieName }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("PolicyName", (Some (PolicyName.to_value x.policyName)));
        ("CookieName", (Some (CookieName.to_value x.cookieName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cookieName =
        CookieName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CookieName") in
      let policyName =
        PolicyName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyName") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~cookieName ~policyName ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cookieName = field_map_exn json__ "CookieName" CookieName.of_json in
      let policyName = field_map_exn json__ "PolicyName" PolicyName.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~cookieName ~policyName ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for CreateAppCookieStickinessPolicy."]
module CreateAccessPointOutput =
  struct
    type createLoadBalancerResult =
      {
      dNSName: DNSName.t option
        [@ocaml.doc "The DNS name of the load balancer."]}
    and responseMetaData = unit
    and t =
      {
      createLoadBalancerResult: createLoadBalancerResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `CertificateNotFoundException of CertificateNotFoundException.t 
      | `DuplicateAccessPointNameException of
          DuplicateAccessPointNameException.t 
      | `DuplicateTagKeysException of DuplicateTagKeysException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `InvalidSchemeException of InvalidSchemeException.t 
      | `InvalidSecurityGroupException of InvalidSecurityGroupException.t 
      | `InvalidSubnetException of InvalidSubnetException.t 
      | `OperationNotPermittedException of OperationNotPermittedException.t 
      | `SubnetNotFoundException of SubnetNotFoundException.t 
      | `TooManyAccessPointsException of TooManyAccessPointsException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `UnsupportedProtocolException of UnsupportedProtocolException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CreateAccessPointOutput"
    let make ?dNSName =
      fun () ->
        { createLoadBalancerResult = { dNSName }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "CertificateNotFoundException" ->
          `CertificateNotFoundException
            (CertificateNotFoundException.of_json json)
      | "DuplicateAccessPointNameException" ->
          `DuplicateAccessPointNameException
            (DuplicateAccessPointNameException.of_json json)
      | "DuplicateTagKeysException" ->
          `DuplicateTagKeysException (DuplicateTagKeysException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | "InvalidSchemeException" ->
          `InvalidSchemeException (InvalidSchemeException.of_json json)
      | "InvalidSecurityGroupException" ->
          `InvalidSecurityGroupException
            (InvalidSecurityGroupException.of_json json)
      | "InvalidSubnetException" ->
          `InvalidSubnetException (InvalidSubnetException.of_json json)
      | "OperationNotPermittedException" ->
          `OperationNotPermittedException
            (OperationNotPermittedException.of_json json)
      | "SubnetNotFoundException" ->
          `SubnetNotFoundException (SubnetNotFoundException.of_json json)
      | "TooManyAccessPointsException" ->
          `TooManyAccessPointsException
            (TooManyAccessPointsException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | "UnsupportedProtocolException" ->
          `UnsupportedProtocolException
            (UnsupportedProtocolException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "CertificateNotFoundException" ->
          `CertificateNotFoundException
            (CertificateNotFoundException.of_xml xml)
      | "DuplicateAccessPointNameException" ->
          `DuplicateAccessPointNameException
            (DuplicateAccessPointNameException.of_xml xml)
      | "DuplicateTagKeysException" ->
          `DuplicateTagKeysException (DuplicateTagKeysException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | "InvalidSchemeException" ->
          `InvalidSchemeException (InvalidSchemeException.of_xml xml)
      | "InvalidSecurityGroupException" ->
          `InvalidSecurityGroupException
            (InvalidSecurityGroupException.of_xml xml)
      | "InvalidSubnetException" ->
          `InvalidSubnetException (InvalidSubnetException.of_xml xml)
      | "OperationNotPermittedException" ->
          `OperationNotPermittedException
            (OperationNotPermittedException.of_xml xml)
      | "SubnetNotFoundException" ->
          `SubnetNotFoundException (SubnetNotFoundException.of_xml xml)
      | "TooManyAccessPointsException" ->
          `TooManyAccessPointsException
            (TooManyAccessPointsException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | "UnsupportedProtocolException" ->
          `UnsupportedProtocolException
            (UnsupportedProtocolException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `CertificateNotFoundException e ->
          `Assoc
            [("error", (`String "CertificateNotFoundException"));
            ("details", (CertificateNotFoundException.to_json e))]
      | `DuplicateAccessPointNameException e ->
          `Assoc
            [("error", (`String "DuplicateAccessPointNameException"));
            ("details", (DuplicateAccessPointNameException.to_json e))]
      | `DuplicateTagKeysException e ->
          `Assoc
            [("error", (`String "DuplicateTagKeysException"));
            ("details", (DuplicateTagKeysException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `InvalidSchemeException e ->
          `Assoc
            [("error", (`String "InvalidSchemeException"));
            ("details", (InvalidSchemeException.to_json e))]
      | `InvalidSecurityGroupException e ->
          `Assoc
            [("error", (`String "InvalidSecurityGroupException"));
            ("details", (InvalidSecurityGroupException.to_json e))]
      | `InvalidSubnetException e ->
          `Assoc
            [("error", (`String "InvalidSubnetException"));
            ("details", (InvalidSubnetException.to_json e))]
      | `OperationNotPermittedException e ->
          `Assoc
            [("error", (`String "OperationNotPermittedException"));
            ("details", (OperationNotPermittedException.to_json e))]
      | `SubnetNotFoundException e ->
          `Assoc
            [("error", (`String "SubnetNotFoundException"));
            ("details", (SubnetNotFoundException.to_json e))]
      | `TooManyAccessPointsException e ->
          `Assoc
            [("error", (`String "TooManyAccessPointsException"));
            ("details", (TooManyAccessPointsException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `UnsupportedProtocolException e ->
          `Assoc
            [("error", (`String "UnsupportedProtocolException"));
            ("details", (UnsupportedProtocolException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.createLoadBalancerResult in
      structure_to_wrapped_value
        [("DNSName", (Option.map x.dNSName ~f:DNSName.to_value))]
        ~wrapper:"CreateLoadBalancerResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "CreateLoadBalancerResult" in
      let dNSName =
        (Option.map ~f:DNSName.of_xml) (Xml.child xml_arg0 "DNSName") in
      make ?dNSName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dNSName = field_map json__ "DNSName" DNSName.of_json in
      make ?dNSName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output for CreateLoadBalancer."]
module CreateAccessPointInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc
          "The name of the load balancer. This name must be unique within your set of load balancers for the region, must have a maximum of 32 characters, must contain only alphanumeric characters or hyphens, and cannot begin or end with a hyphen."];
      listeners: Listeners.t
        [@ocaml.doc
          "The listeners. For more information, see Listeners for Your Classic Load Balancer in the Classic Load Balancers Guide."];
      availabilityZones: AvailabilityZones.t option
        [@ocaml.doc
          "One or more Availability Zones from the same region as the load balancer. You must specify at least one Availability Zone. You can add more Availability Zones after you create the load balancer using EnableAvailabilityZonesForLoadBalancer."];
      subnets: Subnets.t option
        [@ocaml.doc
          "The IDs of the subnets in your VPC to attach to the load balancer. Specify one subnet per Availability Zone specified in AvailabilityZones."];
      securityGroups: SecurityGroups.t option
        [@ocaml.doc
          "The IDs of the security groups to assign to the load balancer."];
      scheme: LoadBalancerScheme.t option
        [@ocaml.doc
          "The type of a load balancer. Valid only for load balancers in a VPC. By default, Elastic Load Balancing creates an Internet-facing load balancer with a DNS name that resolves to public IP addresses. For more information about Internet-facing and Internal load balancers, see Load Balancer Scheme in the Elastic Load Balancing User Guide. Specify internal to create a load balancer with a DNS name that resolves to private IP addresses."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of tags to assign to the load balancer. For more information about tagging your load balancer, see Tag Your Classic Load Balancer in the Classic Load Balancers Guide."]}
    let context_ = "CreateAccessPointInput"
    let make ?availabilityZones =
      fun ?subnets ->
        fun ?securityGroups ->
          fun ?scheme ->
            fun ?tags ->
              fun ~loadBalancerName ->
                fun ~listeners ->
                  fun () ->
                    {
                      availabilityZones;
                      subnets;
                      securityGroups;
                      scheme;
                      tags;
                      loadBalancerName;
                      listeners
                    }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("Listeners", (Some (Listeners.to_value x.listeners)));
        ("AvailabilityZones",
          (Option.map x.availabilityZones ~f:AvailabilityZones.to_value));
        ("Subnets", (Option.map x.subnets ~f:Subnets.to_value));
        ("SecurityGroups",
          (Option.map x.securityGroups ~f:SecurityGroups.to_value));
        ("Scheme", (Option.map x.scheme ~f:LoadBalancerScheme.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let scheme =
        (Option.map ~f:LoadBalancerScheme.of_xml)
          (Xml.child xml_arg0 "Scheme") in
      let securityGroups =
        (Option.map ~f:SecurityGroups.of_xml)
          (Xml.child xml_arg0 "SecurityGroups") in
      let subnets =
        (Option.map ~f:Subnets.of_xml) (Xml.child xml_arg0 "Subnets") in
      let availabilityZones =
        (Option.map ~f:AvailabilityZones.of_xml)
          (Xml.child xml_arg0 "AvailabilityZones") in
      let listeners =
        Listeners.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Listeners") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ?tags ?scheme ?securityGroups ?subnets ?availabilityZones
        ~listeners ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagList.of_json in
      let scheme = field_map json__ "Scheme" LoadBalancerScheme.of_json in
      let securityGroups =
        field_map json__ "SecurityGroups" SecurityGroups.of_json in
      let subnets = field_map json__ "Subnets" Subnets.of_json in
      let availabilityZones =
        field_map json__ "AvailabilityZones" AvailabilityZones.of_json in
      let listeners = field_map_exn json__ "Listeners" Listeners.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ?tags ?scheme ?securityGroups ?subnets ?availabilityZones
        ~listeners ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for CreateLoadBalancer."]
module ConfigureHealthCheckOutput =
  struct
    type configureHealthCheckResult =
      {
      healthCheck: HealthCheck.t option
        [@ocaml.doc "The updated health check."]}
    and responseMetaData = unit
    and t =
      {
      configureHealthCheckResult: configureHealthCheckResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ConfigureHealthCheckOutput"
    let make ?healthCheck =
      fun () ->
        { configureHealthCheckResult = { healthCheck }; responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.configureHealthCheckResult in
      structure_to_wrapped_value
        [("HealthCheck", (Option.map x.healthCheck ~f:HealthCheck.to_value))]
        ~wrapper:"ConfigureHealthCheckResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "ConfigureHealthCheckResult" in
      let healthCheck =
        (Option.map ~f:HealthCheck.of_xml) (Xml.child xml_arg0 "HealthCheck") in
      make ?healthCheck ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let healthCheck = field_map json__ "HealthCheck" HealthCheck.of_json in
      make ?healthCheck ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of ConfigureHealthCheck."]
module ConfigureHealthCheckInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      healthCheck: HealthCheck.t
        [@ocaml.doc "The configuration information."]}
    let context_ = "ConfigureHealthCheckInput"
    let make ~loadBalancerName =
      fun ~healthCheck -> fun () -> { loadBalancerName; healthCheck }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("HealthCheck", (Some (HealthCheck.to_value x.healthCheck)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheck =
        HealthCheck.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheck") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~healthCheck ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let healthCheck =
        field_map_exn json__ "HealthCheck" HealthCheck.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~healthCheck ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for ConfigureHealthCheck."]
module AttachLoadBalancerToSubnetsOutput =
  struct
    type attachLoadBalancerToSubnetsResult =
      {
      subnets: Subnets.t option
        [@ocaml.doc "The IDs of the subnets attached to the load balancer."]}
    and responseMetaData = unit
    and t =
      {
      attachLoadBalancerToSubnetsResult: attachLoadBalancerToSubnetsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `InvalidSubnetException of InvalidSubnetException.t 
      | `SubnetNotFoundException of SubnetNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "AttachLoadBalancerToSubnetsOutput"
    let make ?subnets =
      fun () ->
        {
          attachLoadBalancerToSubnetsResult = { subnets };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | "InvalidSubnetException" ->
          `InvalidSubnetException (InvalidSubnetException.of_json json)
      | "SubnetNotFoundException" ->
          `SubnetNotFoundException (SubnetNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | "InvalidSubnetException" ->
          `InvalidSubnetException (InvalidSubnetException.of_xml xml)
      | "SubnetNotFoundException" ->
          `SubnetNotFoundException (SubnetNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `InvalidSubnetException e ->
          `Assoc
            [("error", (`String "InvalidSubnetException"));
            ("details", (InvalidSubnetException.to_json e))]
      | `SubnetNotFoundException e ->
          `Assoc
            [("error", (`String "SubnetNotFoundException"));
            ("details", (SubnetNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.attachLoadBalancerToSubnetsResult in
      structure_to_wrapped_value
        [("Subnets", (Option.map x.subnets ~f:Subnets.to_value))]
        ~wrapper:"AttachLoadBalancerToSubnetsResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "AttachLoadBalancerToSubnetsResult" in
      let subnets =
        (Option.map ~f:Subnets.of_xml) (Xml.child xml_arg0 "Subnets") in
      make ?subnets ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let subnets = field_map json__ "Subnets" Subnets.of_json in
      make ?subnets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of AttachLoadBalancerToSubnets."]
module AttachLoadBalancerToSubnetsInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      subnets: Subnets.t
        [@ocaml.doc
          "The IDs of the subnets to add. You can add only one subnet per Availability Zone."]}
    let context_ = "AttachLoadBalancerToSubnetsInput"
    let make ~loadBalancerName =
      fun ~subnets -> fun () -> { loadBalancerName; subnets }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("Subnets", (Some (Subnets.to_value x.subnets)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subnets =
        Subnets.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Subnets") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~subnets ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let subnets = field_map_exn json__ "Subnets" Subnets.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~subnets ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for AttachLoaBalancerToSubnets."]
module ApplySecurityGroupsToLoadBalancerOutput =
  struct
    type applySecurityGroupsToLoadBalancerResult =
      {
      securityGroups: SecurityGroups.t option
        [@ocaml.doc
          "The IDs of the security groups associated with the load balancer."]}
    and responseMetaData = unit
    and t =
      {
      applySecurityGroupsToLoadBalancerResult:
        applySecurityGroupsToLoadBalancerResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `InvalidConfigurationRequestException of
          InvalidConfigurationRequestException.t 
      | `InvalidSecurityGroupException of InvalidSecurityGroupException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ApplySecurityGroupsToLoadBalancerOutput"
    let make ?securityGroups =
      fun () ->
        {
          applySecurityGroupsToLoadBalancerResult = { securityGroups };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_json json)
      | "InvalidSecurityGroupException" ->
          `InvalidSecurityGroupException
            (InvalidSecurityGroupException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "InvalidConfigurationRequestException" ->
          `InvalidConfigurationRequestException
            (InvalidConfigurationRequestException.of_xml xml)
      | "InvalidSecurityGroupException" ->
          `InvalidSecurityGroupException
            (InvalidSecurityGroupException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `InvalidConfigurationRequestException e ->
          `Assoc
            [("error", (`String "InvalidConfigurationRequestException"));
            ("details", (InvalidConfigurationRequestException.to_json e))]
      | `InvalidSecurityGroupException e ->
          `Assoc
            [("error", (`String "InvalidSecurityGroupException"));
            ("details", (InvalidSecurityGroupException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.applySecurityGroupsToLoadBalancerResult in
      structure_to_wrapped_value
        [("SecurityGroups",
           (Option.map x.securityGroups ~f:SecurityGroups.to_value))]
        ~wrapper:"ApplySecurityGroupsToLoadBalancerResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "ApplySecurityGroupsToLoadBalancerResult" in
      let securityGroups =
        (Option.map ~f:SecurityGroups.of_xml)
          (Xml.child xml_arg0 "SecurityGroups") in
      make ?securityGroups ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroups =
        field_map json__ "SecurityGroups" SecurityGroups.of_json in
      make ?securityGroups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output of ApplySecurityGroupsToLoadBalancer."]
module ApplySecurityGroupsToLoadBalancerInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      securityGroups: SecurityGroups.t
        [@ocaml.doc
          "The IDs of the security groups to associate with the load balancer. Note that you cannot specify the name of the security group."]}
    let context_ = "ApplySecurityGroupsToLoadBalancerInput"
    let make ~loadBalancerName =
      fun ~securityGroups -> fun () -> { loadBalancerName; securityGroups }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("SecurityGroups", (Some (SecurityGroups.to_value x.securityGroups)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroups =
        SecurityGroups.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SecurityGroups") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~securityGroups ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroups =
        field_map_exn json__ "SecurityGroups" SecurityGroups.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~securityGroups ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for ApplySecurityGroupsToLoadBalancer."]
module AddTagsOutput =
  struct
    type addTagsResult = unit
    and responseMetaData = unit
    and t =
      {
      addTagsResult: addTagsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `DuplicateTagKeysException of DuplicateTagKeysException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = { addTagsResult = (); responseMetaData = () }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | "DuplicateTagKeysException" ->
          `DuplicateTagKeysException (DuplicateTagKeysException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | "DuplicateTagKeysException" ->
          `DuplicateTagKeysException (DuplicateTagKeysException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `DuplicateTagKeysException e ->
          `Assoc
            [("error", (`String "DuplicateTagKeysException"));
            ("details", (DuplicateTagKeysException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the output of AddTags."]
module AddTagsInput =
  struct
    type nonrec t =
      {
      loadBalancerNames: LoadBalancerNames.t
        [@ocaml.doc
          "The name of the load balancer. You can specify one load balancer only."];
      tags: TagList.t [@ocaml.doc "The tags."]}
    let context_ = "AddTagsInput"
    let make ~loadBalancerNames =
      fun ~tags -> fun () -> { loadBalancerNames; tags }
    let to_value x =
      structure_to_value
        [("LoadBalancerNames",
           (Some (LoadBalancerNames.to_value x.loadBalancerNames)));
        ("Tags", (Some (TagList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let loadBalancerNames =
        LoadBalancerNames.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerNames") in
      make ~tags ~loadBalancerNames ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "Tags" TagList.of_json in
      let loadBalancerNames =
        field_map_exn json__ "LoadBalancerNames" LoadBalancerNames.of_json in
      make ~tags ~loadBalancerNames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters for AddTags."]
module AddAvailabilityZonesOutput =
  struct
    type enableAvailabilityZonesForLoadBalancerResult =
      {
      availabilityZones: AvailabilityZones.t option
        [@ocaml.doc
          "The updated list of Availability Zones for the load balancer."]}
    and responseMetaData = unit
    and t =
      {
      enableAvailabilityZonesForLoadBalancerResult:
        enableAvailabilityZonesForLoadBalancerResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `AccessPointNotFoundException of AccessPointNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "AddAvailabilityZonesOutput"
    let make ?availabilityZones =
      fun () ->
        {
          enableAvailabilityZonesForLoadBalancerResult =
            { availabilityZones };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessPointNotFoundException" ->
          `AccessPointNotFoundException
            (AccessPointNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessPointNotFoundException e ->
          `Assoc
            [("error", (`String "AccessPointNotFoundException"));
            ("details", (AccessPointNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.enableAvailabilityZonesForLoadBalancerResult in
      structure_to_wrapped_value
        [("AvailabilityZones",
           (Option.map x.availabilityZones ~f:AvailabilityZones.to_value))]
        ~wrapper:"EnableAvailabilityZonesForLoadBalancerResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "EnableAvailabilityZonesForLoadBalancerResult" in
      let availabilityZones =
        (Option.map ~f:AvailabilityZones.of_xml)
          (Xml.child xml_arg0 "AvailabilityZones") in
      make ?availabilityZones ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let availabilityZones =
        field_map json__ "AvailabilityZones" AvailabilityZones.of_json in
      make ?availabilityZones ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output of EnableAvailabilityZonesForLoadBalancer."]
module AddAvailabilityZonesInput =
  struct
    type nonrec t =
      {
      loadBalancerName: AccessPointName.t
        [@ocaml.doc "The name of the load balancer."];
      availabilityZones: AvailabilityZones.t
        [@ocaml.doc
          "The Availability Zones. These must be in the same region as the load balancer."]}
    let context_ = "AddAvailabilityZonesInput"
    let make ~loadBalancerName =
      fun ~availabilityZones ->
        fun () -> { loadBalancerName; availabilityZones }
    let to_value x =
      structure_to_value
        [("LoadBalancerName",
           (Some (AccessPointName.to_value x.loadBalancerName)));
        ("AvailabilityZones",
          (Some (AvailabilityZones.to_value x.availabilityZones)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availabilityZones =
        AvailabilityZones.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AvailabilityZones") in
      let loadBalancerName =
        AccessPointName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LoadBalancerName") in
      make ~availabilityZones ~loadBalancerName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let availabilityZones =
        field_map_exn json__ "AvailabilityZones" AvailabilityZones.of_json in
      let loadBalancerName =
        field_map_exn json__ "LoadBalancerName" AccessPointName.of_json in
      make ~availabilityZones ~loadBalancerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the parameters for EnableAvailabilityZonesForLoadBalancer."]
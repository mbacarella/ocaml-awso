(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | AddTags: (AddTagsInput.t, AddTagsOutput.t, AddTagsOutput.error) t 
  | ApplySecurityGroupsToLoadBalancer:
  (ApplySecurityGroupsToLoadBalancerInput.t,
  ApplySecurityGroupsToLoadBalancerOutput.t,
  ApplySecurityGroupsToLoadBalancerOutput.error) t 
  | AttachLoadBalancerToSubnets: (AttachLoadBalancerToSubnetsInput.t,
  AttachLoadBalancerToSubnetsOutput.t,
  AttachLoadBalancerToSubnetsOutput.error) t 
  | ConfigureHealthCheck: (ConfigureHealthCheckInput.t,
  ConfigureHealthCheckOutput.t, ConfigureHealthCheckOutput.error) t 
  | CreateAppCookieStickinessPolicy: (CreateAppCookieStickinessPolicyInput.t,
  CreateAppCookieStickinessPolicyOutput.t,
  CreateAppCookieStickinessPolicyOutput.error) t 
  | CreateLBCookieStickinessPolicy: (CreateLBCookieStickinessPolicyInput.t,
  CreateLBCookieStickinessPolicyOutput.t,
  CreateLBCookieStickinessPolicyOutput.error) t 
  | CreateLoadBalancer: (CreateAccessPointInput.t, CreateAccessPointOutput.t,
  CreateAccessPointOutput.error) t 
  | CreateLoadBalancerListeners: (CreateLoadBalancerListenerInput.t,
  CreateLoadBalancerListenerOutput.t, CreateLoadBalancerListenerOutput.error)
  t 
  | CreateLoadBalancerPolicy: (CreateLoadBalancerPolicyInput.t,
  CreateLoadBalancerPolicyOutput.t, CreateLoadBalancerPolicyOutput.error) t 
  | DeleteLoadBalancer: (DeleteAccessPointInput.t, DeleteAccessPointOutput.t,
  DeleteAccessPointOutput.error) t 
  | DeleteLoadBalancerListeners: (DeleteLoadBalancerListenerInput.t,
  DeleteLoadBalancerListenerOutput.t, DeleteLoadBalancerListenerOutput.error)
  t 
  | DeleteLoadBalancerPolicy: (DeleteLoadBalancerPolicyInput.t,
  DeleteLoadBalancerPolicyOutput.t, DeleteLoadBalancerPolicyOutput.error) t 
  | DeregisterInstancesFromLoadBalancer: (DeregisterEndPointsInput.t,
  DeregisterEndPointsOutput.t, DeregisterEndPointsOutput.error) t 
  | DescribeAccountLimits: (DescribeAccountLimitsInput.t,
  DescribeAccountLimitsOutput.t, DescribeAccountLimitsOutput.error) t 
  | DescribeInstanceHealth: (DescribeEndPointStateInput.t,
  DescribeEndPointStateOutput.t, DescribeEndPointStateOutput.error) t 
  | DescribeLoadBalancerAttributes: (DescribeLoadBalancerAttributesInput.t,
  DescribeLoadBalancerAttributesOutput.t,
  DescribeLoadBalancerAttributesOutput.error) t 
  | DescribeLoadBalancerPolicies: (DescribeLoadBalancerPoliciesInput.t,
  DescribeLoadBalancerPoliciesOutput.t,
  DescribeLoadBalancerPoliciesOutput.error) t 
  | DescribeLoadBalancerPolicyTypes: (DescribeLoadBalancerPolicyTypesInput.t,
  DescribeLoadBalancerPolicyTypesOutput.t,
  DescribeLoadBalancerPolicyTypesOutput.error) t 
  | DescribeLoadBalancers: (DescribeAccessPointsInput.t,
  DescribeAccessPointsOutput.t, DescribeAccessPointsOutput.error) t 
  | DescribeTags: (DescribeTagsInput.t, DescribeTagsOutput.t,
  DescribeTagsOutput.error) t 
  | DetachLoadBalancerFromSubnets: (DetachLoadBalancerFromSubnetsInput.t,
  DetachLoadBalancerFromSubnetsOutput.t,
  DetachLoadBalancerFromSubnetsOutput.error) t 
  | DisableAvailabilityZonesForLoadBalancer: (RemoveAvailabilityZonesInput.t,
  RemoveAvailabilityZonesOutput.t, RemoveAvailabilityZonesOutput.error) t 
  | EnableAvailabilityZonesForLoadBalancer: (AddAvailabilityZonesInput.t,
  AddAvailabilityZonesOutput.t, AddAvailabilityZonesOutput.error) t 
  | ModifyLoadBalancerAttributes: (ModifyLoadBalancerAttributesInput.t,
  ModifyLoadBalancerAttributesOutput.t,
  ModifyLoadBalancerAttributesOutput.error) t 
  | RegisterInstancesWithLoadBalancer: (RegisterEndPointsInput.t,
  RegisterEndPointsOutput.t, RegisterEndPointsOutput.error) t 
  | RemoveTags: (RemoveTagsInput.t, RemoveTagsOutput.t,
  RemoveTagsOutput.error) t 
  | SetLoadBalancerListenerSSLCertificate:
  (SetLoadBalancerListenerSSLCertificateInput.t,
  SetLoadBalancerListenerSSLCertificateOutput.t,
  SetLoadBalancerListenerSSLCertificateOutput.error) t 
  | SetLoadBalancerPoliciesForBackendServer:
  (SetLoadBalancerPoliciesForBackendServerInput.t,
  SetLoadBalancerPoliciesForBackendServerOutput.t,
  SetLoadBalancerPoliciesForBackendServerOutput.error) t 
  | SetLoadBalancerPoliciesOfListener:
  (SetLoadBalancerPoliciesOfListenerInput.t,
  SetLoadBalancerPoliciesOfListenerOutput.t,
  SetLoadBalancerPoliciesOfListenerOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | AddTags -> `POST
  | ApplySecurityGroupsToLoadBalancer -> `POST
  | AttachLoadBalancerToSubnets -> `POST
  | ConfigureHealthCheck -> `POST
  | CreateAppCookieStickinessPolicy -> `POST
  | CreateLBCookieStickinessPolicy -> `POST
  | CreateLoadBalancer -> `POST
  | CreateLoadBalancerListeners -> `POST
  | CreateLoadBalancerPolicy -> `POST
  | DeleteLoadBalancer -> `POST
  | DeleteLoadBalancerListeners -> `POST
  | DeleteLoadBalancerPolicy -> `POST
  | DeregisterInstancesFromLoadBalancer -> `POST
  | DescribeAccountLimits -> `POST
  | DescribeInstanceHealth -> `POST
  | DescribeLoadBalancerAttributes -> `POST
  | DescribeLoadBalancerPolicies -> `POST
  | DescribeLoadBalancerPolicyTypes -> `POST
  | DescribeLoadBalancers -> `POST
  | DescribeTags -> `POST
  | DetachLoadBalancerFromSubnets -> `POST
  | DisableAvailabilityZonesForLoadBalancer -> `POST
  | EnableAvailabilityZonesForLoadBalancer -> `POST
  | ModifyLoadBalancerAttributes -> `POST
  | RegisterInstancesWithLoadBalancer -> `POST
  | RemoveTags -> `POST
  | SetLoadBalancerListenerSSLCertificate -> `POST
  | SetLoadBalancerPoliciesForBackendServer -> `POST
  | SetLoadBalancerPoliciesOfListener -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | AddTags -> (Format.kasprintf Uri.of_string) "/"
      | ApplySecurityGroupsToLoadBalancer ->
          (Format.kasprintf Uri.of_string) "/"
      | AttachLoadBalancerToSubnets -> (Format.kasprintf Uri.of_string) "/"
      | ConfigureHealthCheck -> (Format.kasprintf Uri.of_string) "/"
      | CreateAppCookieStickinessPolicy ->
          (Format.kasprintf Uri.of_string) "/"
      | CreateLBCookieStickinessPolicy ->
          (Format.kasprintf Uri.of_string) "/"
      | CreateLoadBalancer -> (Format.kasprintf Uri.of_string) "/"
      | CreateLoadBalancerListeners -> (Format.kasprintf Uri.of_string) "/"
      | CreateLoadBalancerPolicy -> (Format.kasprintf Uri.of_string) "/"
      | DeleteLoadBalancer -> (Format.kasprintf Uri.of_string) "/"
      | DeleteLoadBalancerListeners -> (Format.kasprintf Uri.of_string) "/"
      | DeleteLoadBalancerPolicy -> (Format.kasprintf Uri.of_string) "/"
      | DeregisterInstancesFromLoadBalancer ->
          (Format.kasprintf Uri.of_string) "/"
      | DescribeAccountLimits -> (Format.kasprintf Uri.of_string) "/"
      | DescribeInstanceHealth -> (Format.kasprintf Uri.of_string) "/"
      | DescribeLoadBalancerAttributes ->
          (Format.kasprintf Uri.of_string) "/"
      | DescribeLoadBalancerPolicies -> (Format.kasprintf Uri.of_string) "/"
      | DescribeLoadBalancerPolicyTypes ->
          (Format.kasprintf Uri.of_string) "/"
      | DescribeLoadBalancers -> (Format.kasprintf Uri.of_string) "/"
      | DescribeTags -> (Format.kasprintf Uri.of_string) "/"
      | DetachLoadBalancerFromSubnets -> (Format.kasprintf Uri.of_string) "/"
      | DisableAvailabilityZonesForLoadBalancer ->
          (Format.kasprintf Uri.of_string) "/"
      | EnableAvailabilityZonesForLoadBalancer ->
          (Format.kasprintf Uri.of_string) "/"
      | ModifyLoadBalancerAttributes -> (Format.kasprintf Uri.of_string) "/"
      | RegisterInstancesWithLoadBalancer ->
          (Format.kasprintf Uri.of_string) "/"
      | RemoveTags -> (Format.kasprintf Uri.of_string) "/"
      | SetLoadBalancerListenerSSLCertificate ->
          (Format.kasprintf Uri.of_string) "/"
      | SetLoadBalancerPoliciesForBackendServer ->
          (Format.kasprintf Uri.of_string) "/"
      | SetLoadBalancerPoliciesOfListener ->
          (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | AddTags ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["AddTags"]); ("Version", [apiVersion])] in
        let query = (AddTagsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ApplySecurityGroupsToLoadBalancer ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ApplySecurityGroupsToLoadBalancer"]);
          ("Version", [apiVersion])] in
        let query =
          (ApplySecurityGroupsToLoadBalancerInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | AttachLoadBalancerToSubnets ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["AttachLoadBalancerToSubnets"]);
          ("Version", [apiVersion])] in
        let query =
          (AttachLoadBalancerToSubnetsInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ConfigureHealthCheck ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ConfigureHealthCheck"]); ("Version", [apiVersion])] in
        let query =
          (ConfigureHealthCheckInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateAppCookieStickinessPolicy ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["CreateAppCookieStickinessPolicy"]);
          ("Version", [apiVersion])] in
        let query =
          (CreateAppCookieStickinessPolicyInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateLBCookieStickinessPolicy ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["CreateLBCookieStickinessPolicy"]);
          ("Version", [apiVersion])] in
        let query =
          (CreateLBCookieStickinessPolicyInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateLoadBalancer ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["CreateLoadBalancer"]); ("Version", [apiVersion])] in
        let query =
          (CreateAccessPointInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateLoadBalancerListeners ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["CreateLoadBalancerListeners"]);
          ("Version", [apiVersion])] in
        let query =
          (CreateLoadBalancerListenerInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateLoadBalancerPolicy ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["CreateLoadBalancerPolicy"]);
          ("Version", [apiVersion])] in
        let query =
          (CreateLoadBalancerPolicyInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeleteLoadBalancer ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeleteLoadBalancer"]); ("Version", [apiVersion])] in
        let query =
          (DeleteAccessPointInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeleteLoadBalancerListeners ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeleteLoadBalancerListeners"]);
          ("Version", [apiVersion])] in
        let query =
          (DeleteLoadBalancerListenerInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeleteLoadBalancerPolicy ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeleteLoadBalancerPolicy"]);
          ("Version", [apiVersion])] in
        let query =
          (DeleteLoadBalancerPolicyInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeregisterInstancesFromLoadBalancer ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeregisterInstancesFromLoadBalancer"]);
          ("Version", [apiVersion])] in
        let query =
          (DeregisterEndPointsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeAccountLimits ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeAccountLimits"]); ("Version", [apiVersion])] in
        let query =
          (DescribeAccountLimitsInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeInstanceHealth ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeInstanceHealth"]); ("Version", [apiVersion])] in
        let query =
          (DescribeEndPointStateInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeLoadBalancerAttributes ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeLoadBalancerAttributes"]);
          ("Version", [apiVersion])] in
        let query =
          (DescribeLoadBalancerAttributesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeLoadBalancerPolicies ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeLoadBalancerPolicies"]);
          ("Version", [apiVersion])] in
        let query =
          (DescribeLoadBalancerPoliciesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeLoadBalancerPolicyTypes ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeLoadBalancerPolicyTypes"]);
          ("Version", [apiVersion])] in
        let query =
          (DescribeLoadBalancerPolicyTypesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeLoadBalancers ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeLoadBalancers"]); ("Version", [apiVersion])] in
        let query =
          (DescribeAccessPointsInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeTags ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["DescribeTags"]); ("Version", [apiVersion])] in
        let query =
          (DescribeTagsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DetachLoadBalancerFromSubnets ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DetachLoadBalancerFromSubnets"]);
          ("Version", [apiVersion])] in
        let query =
          (DetachLoadBalancerFromSubnetsInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DisableAvailabilityZonesForLoadBalancer ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DisableAvailabilityZonesForLoadBalancer"]);
          ("Version", [apiVersion])] in
        let query =
          (RemoveAvailabilityZonesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | EnableAvailabilityZonesForLoadBalancer ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["EnableAvailabilityZonesForLoadBalancer"]);
          ("Version", [apiVersion])] in
        let query =
          (AddAvailabilityZonesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ModifyLoadBalancerAttributes ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ModifyLoadBalancerAttributes"]);
          ("Version", [apiVersion])] in
        let query =
          (ModifyLoadBalancerAttributesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | RegisterInstancesWithLoadBalancer ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["RegisterInstancesWithLoadBalancer"]);
          ("Version", [apiVersion])] in
        let query =
          (RegisterEndPointsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | RemoveTags ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["RemoveTags"]); ("Version", [apiVersion])] in
        let query =
          (RemoveTagsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | SetLoadBalancerListenerSSLCertificate ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["SetLoadBalancerListenerSSLCertificate"]);
          ("Version", [apiVersion])] in
        let query =
          (SetLoadBalancerListenerSSLCertificateInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | SetLoadBalancerPoliciesForBackendServer ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["SetLoadBalancerPoliciesForBackendServer"]);
          ("Version", [apiVersion])] in
        let query =
          (SetLoadBalancerPoliciesForBackendServerInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | SetLoadBalancerPoliciesOfListener ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["SetLoadBalancerPoliciesOfListener"]);
          ("Version", [apiVersion])] in
        let query =
          (SetLoadBalancerPoliciesOfListenerInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let parse_aws_error error_of_xml =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = None }) in
    match (error_of_xml, ((code >= 400) && (code <= 599))) with
    | (None, _) | (_, false) -> bail ()
    | (Some error_of_xml, true) ->
        (match Awso.Xml.parse_response body with
         | `Data _ -> bail ()
         | `El (((_, "ErrorResponse"), _), _) as error_response_xml ->
             let error_xml = Awso.Xml.child_exn error_response_xml "Error" in
             (try
                let error_code =
                  match Awso.Xml.child_exn error_xml "Code" with
                  | `Data error_code -> error_code
                  | `El (_, children) ->
                      (List.map children
                         ~f:(function | `Data s -> s | `El _ -> ""))
                        |> (String.concat ~sep:"") in
                error_of_xml (String.strip error_code) error_xml
              with | Failure _ -> bail ())
         | `El _ -> bail ()) in
  let _ = parse_aws_error in
  let _ = resp in
  match endpoint with
  | AddTags ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (AddTagsOutput.of_xml xml)
      else Error (parse_aws_error (Some AddTagsOutput.error_of_xml))
  | ApplySecurityGroupsToLoadBalancer ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ApplySecurityGroupsToLoadBalancerOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some ApplySecurityGroupsToLoadBalancerOutput.error_of_xml))
  | AttachLoadBalancerToSubnets ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (AttachLoadBalancerToSubnetsOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some AttachLoadBalancerToSubnetsOutput.error_of_xml))
  | ConfigureHealthCheck ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ConfigureHealthCheckOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some ConfigureHealthCheckOutput.error_of_xml))
  | CreateAppCookieStickinessPolicy ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (CreateAppCookieStickinessPolicyOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some CreateAppCookieStickinessPolicyOutput.error_of_xml))
  | CreateLBCookieStickinessPolicy ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (CreateLBCookieStickinessPolicyOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some CreateLBCookieStickinessPolicyOutput.error_of_xml))
  | CreateLoadBalancer ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (CreateAccessPointOutput.of_xml xml)
      else
        Error (parse_aws_error (Some CreateAccessPointOutput.error_of_xml))
  | CreateLoadBalancerListeners ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (CreateLoadBalancerListenerOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some CreateLoadBalancerListenerOutput.error_of_xml))
  | CreateLoadBalancerPolicy ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (CreateLoadBalancerPolicyOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some CreateLoadBalancerPolicyOutput.error_of_xml))
  | DeleteLoadBalancer ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeleteAccessPointOutput.of_xml xml)
      else Error (parse_aws_error None)
  | DeleteLoadBalancerListeners ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeleteLoadBalancerListenerOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some DeleteLoadBalancerListenerOutput.error_of_xml))
  | DeleteLoadBalancerPolicy ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeleteLoadBalancerPolicyOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some DeleteLoadBalancerPolicyOutput.error_of_xml))
  | DeregisterInstancesFromLoadBalancer ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeregisterEndPointsOutput.of_xml xml)
      else
        Error (parse_aws_error (Some DeregisterEndPointsOutput.error_of_xml))
  | DescribeAccountLimits ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeAccountLimitsOutput.of_xml xml)
      else Error (parse_aws_error None)
  | DescribeInstanceHealth ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeEndPointStateOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some DescribeEndPointStateOutput.error_of_xml))
  | DescribeLoadBalancerAttributes ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeLoadBalancerAttributesOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some DescribeLoadBalancerAttributesOutput.error_of_xml))
  | DescribeLoadBalancerPolicies ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeLoadBalancerPoliciesOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some DescribeLoadBalancerPoliciesOutput.error_of_xml))
  | DescribeLoadBalancerPolicyTypes ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeLoadBalancerPolicyTypesOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some DescribeLoadBalancerPolicyTypesOutput.error_of_xml))
  | DescribeLoadBalancers ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeAccessPointsOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some DescribeAccessPointsOutput.error_of_xml))
  | DescribeTags ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeTagsOutput.of_xml xml)
      else Error (parse_aws_error (Some DescribeTagsOutput.error_of_xml))
  | DetachLoadBalancerFromSubnets ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DetachLoadBalancerFromSubnetsOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some DetachLoadBalancerFromSubnetsOutput.error_of_xml))
  | DisableAvailabilityZonesForLoadBalancer ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (RemoveAvailabilityZonesOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some RemoveAvailabilityZonesOutput.error_of_xml))
  | EnableAvailabilityZonesForLoadBalancer ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (AddAvailabilityZonesOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some AddAvailabilityZonesOutput.error_of_xml))
  | ModifyLoadBalancerAttributes ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ModifyLoadBalancerAttributesOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some ModifyLoadBalancerAttributesOutput.error_of_xml))
  | RegisterInstancesWithLoadBalancer ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (RegisterEndPointsOutput.of_xml xml)
      else
        Error (parse_aws_error (Some RegisterEndPointsOutput.error_of_xml))
  | RemoveTags ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (RemoveTagsOutput.of_xml xml)
      else Error (parse_aws_error (Some RemoveTagsOutput.error_of_xml))
  | SetLoadBalancerListenerSSLCertificate ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (SetLoadBalancerListenerSSLCertificateOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some SetLoadBalancerListenerSSLCertificateOutput.error_of_xml))
  | SetLoadBalancerPoliciesForBackendServer ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (SetLoadBalancerPoliciesForBackendServerOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some SetLoadBalancerPoliciesForBackendServerOutput.error_of_xml))
  | SetLoadBalancerPoliciesOfListener ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (SetLoadBalancerPoliciesOfListenerOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some SetLoadBalancerPoliciesOfListenerOutput.error_of_xml))
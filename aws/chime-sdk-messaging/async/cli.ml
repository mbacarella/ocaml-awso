(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_channel_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and channelFlowArn =
         flag "channel-flow-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_channel_flow
           (Values.AssociateChannelFlowRequest.make ~channelArn
              ~channelFlowArn ~chimeBearer ()) None None])
let batch_create_channel_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON ChannelMembershipType"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArns =
         flag "member-arns" (required json_arg) ~doc:"JSON MemberArns"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_channel_membership
           (Values.BatchCreateChannelMembershipRequest.make
              ?type_:(Option.map ~f:Values.ChannelMembershipType.of_json
                        type_) ~channelArn
              ~memberArns:(Values.MemberArns.of_json memberArns) ~chimeBearer
              ()) (Some Values.BatchCreateChannelMembershipResponse.to_json)
           (Some Values.BatchCreateChannelMembershipResponse.error_to_json)])
let channel_flow_callback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deleteResource =
         flag "delete-resource" (optional bool)
           ~doc:"BOOL NonNullableBoolean"
       and callbackId =
         flag "callback-id" (required string) ~doc:"STRING CallbackIdType"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and channelMessage =
         flag "channel-message" (required json_arg)
           ~doc:"JSON ChannelMessageCallback" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.channel_flow_callback
           (Values.ChannelFlowCallbackRequest.make ?deleteResource
              ~callbackId ~channelArn
              ~channelMessage:(Values.ChannelMessageCallback.of_json
                                 channelMessage) ())
           (Some Values.ChannelFlowCallbackResponse.to_json)
           (Some Values.ChannelFlowCallbackResponse.error_to_json)])
let create_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mode = flag "mode" (optional json_arg) ~doc:"JSON ChannelMode"
       and privacy =
         flag "privacy" (optional json_arg) ~doc:"JSON ChannelPrivacy"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn"
       and name =
         flag "name" (required string) ~doc:"STRING NonEmptyResourceName"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel
           (Values.CreateChannelRequest.make
              ?mode:(Option.map ~f:Values.ChannelMode.of_json mode)
              ?privacy:(Option.map ~f:Values.ChannelPrivacy.of_json privacy)
              ?metadata ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~appInstanceArn ~name ~clientRequestToken ~chimeBearer ())
           (Some Values.CreateChannelResponse.to_json)
           (Some Values.CreateChannelResponse.error_to_json)])
let create_channel_ban =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel_ban
           (Values.CreateChannelBanRequest.make ~channelArn ~memberArn
              ~chimeBearer ()) (Some Values.CreateChannelBanResponse.to_json)
           (Some Values.CreateChannelBanResponse.error_to_json)])
let create_channel_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn"
       and processors =
         flag "processors" (required json_arg) ~doc:"JSON ProcessorList"
       and name =
         flag "name" (required string) ~doc:"STRING NonEmptyResourceName"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel_flow
           (Values.CreateChannelFlowRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~appInstanceArn
              ~processors:(Values.ProcessorList.of_json processors) ~name
              ~clientRequestToken ())
           (Some Values.CreateChannelFlowResponse.to_json)
           (Some Values.CreateChannelFlowResponse.error_to_json)])
let create_channel_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON ChannelMembershipType"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel_membership
           (Values.CreateChannelMembershipRequest.make ~channelArn ~memberArn
              ~type_:(Values.ChannelMembershipType.of_json type_)
              ~chimeBearer ())
           (Some Values.CreateChannelMembershipResponse.to_json)
           (Some Values.CreateChannelMembershipResponse.error_to_json)])
let create_channel_moderator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and channelModeratorArn =
         flag "channel-moderator-arn" (required string)
           ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel_moderator
           (Values.CreateChannelModeratorRequest.make ~channelArn
              ~channelModeratorArn ~chimeBearer ())
           (Some Values.CreateChannelModeratorResponse.to_json)
           (Some Values.CreateChannelModeratorResponse.error_to_json)])
let delete_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel
           (Values.DeleteChannelRequest.make ~channelArn ~chimeBearer ())
           None None])
let delete_channel_ban =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel_ban
           (Values.DeleteChannelBanRequest.make ~channelArn ~memberArn
              ~chimeBearer ()) None None])
let delete_channel_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelFlowArn =
         flag "channel-flow-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel_flow
           (Values.DeleteChannelFlowRequest.make ~channelFlowArn ()) None
           None])
let delete_channel_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel_membership
           (Values.DeleteChannelMembershipRequest.make ~channelArn ~memberArn
              ~chimeBearer ()) None None])
let delete_channel_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING MessageId"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel_message
           (Values.DeleteChannelMessageRequest.make ~channelArn ~messageId
              ~chimeBearer ()) None None])
let delete_channel_moderator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and channelModeratorArn =
         flag "channel-moderator-arn" (required string)
           ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel_moderator
           (Values.DeleteChannelModeratorRequest.make ~channelArn
              ~channelModeratorArn ~chimeBearer ()) None None])
let describe_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel
           (Values.DescribeChannelRequest.make ~channelArn ~chimeBearer ())
           (Some Values.DescribeChannelResponse.to_json)
           (Some Values.DescribeChannelResponse.error_to_json)])
let describe_channel_ban =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel_ban
           (Values.DescribeChannelBanRequest.make ~channelArn ~memberArn
              ~chimeBearer ())
           (Some Values.DescribeChannelBanResponse.to_json)
           (Some Values.DescribeChannelBanResponse.error_to_json)])
let describe_channel_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelFlowArn =
         flag "channel-flow-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel_flow
           (Values.DescribeChannelFlowRequest.make ~channelFlowArn ())
           (Some Values.DescribeChannelFlowResponse.to_json)
           (Some Values.DescribeChannelFlowResponse.error_to_json)])
let describe_channel_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel_membership
           (Values.DescribeChannelMembershipRequest.make ~channelArn
              ~memberArn ~chimeBearer ())
           (Some Values.DescribeChannelMembershipResponse.to_json)
           (Some Values.DescribeChannelMembershipResponse.error_to_json)])
let describe_channel_membership_for_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel_membership_for_app_instance_user
           (Values.DescribeChannelMembershipForAppInstanceUserRequest.make
              ~channelArn ~appInstanceUserArn ~chimeBearer ())
           (Some
              Values.DescribeChannelMembershipForAppInstanceUserResponse.to_json)
           (Some
              Values.DescribeChannelMembershipForAppInstanceUserResponse.error_to_json)])
let describe_channel_moderated_by_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel_moderated_by_app_instance_user
           (Values.DescribeChannelModeratedByAppInstanceUserRequest.make
              ~channelArn ~appInstanceUserArn ~chimeBearer ())
           (Some
              Values.DescribeChannelModeratedByAppInstanceUserResponse.to_json)
           (Some
              Values.DescribeChannelModeratedByAppInstanceUserResponse.error_to_json)])
let describe_channel_moderator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and channelModeratorArn =
         flag "channel-moderator-arn" (required string)
           ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel_moderator
           (Values.DescribeChannelModeratorRequest.make ~channelArn
              ~channelModeratorArn ~chimeBearer ())
           (Some Values.DescribeChannelModeratorResponse.to_json)
           (Some Values.DescribeChannelModeratorResponse.error_to_json)])
let disassociate_channel_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and channelFlowArn =
         flag "channel-flow-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_channel_flow
           (Values.DisassociateChannelFlowRequest.make ~channelArn
              ~channelFlowArn ~chimeBearer ()) None None])
let get_channel_membership_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_channel_membership_preferences
           (Values.GetChannelMembershipPreferencesRequest.make ~channelArn
              ~memberArn ~chimeBearer ())
           (Some Values.GetChannelMembershipPreferencesResponse.to_json)
           (Some Values.GetChannelMembershipPreferencesResponse.error_to_json)])
let get_channel_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING MessageId"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_channel_message
           (Values.GetChannelMessageRequest.make ~channelArn ~messageId
              ~chimeBearer ())
           (Some Values.GetChannelMessageResponse.to_json)
           (Some Values.GetChannelMessageResponse.error_to_json)])
let get_channel_message_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING MessageId"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_channel_message_status
           (Values.GetChannelMessageStatusRequest.make ~channelArn ~messageId
              ~chimeBearer ())
           (Some Values.GetChannelMessageStatusResponse.to_json)
           (Some Values.GetChannelMessageStatusResponse.error_to_json)])
let get_messaging_session_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_messaging_session_endpoint
           (Values.GetMessagingSessionEndpointRequest.make ())
           (Some Values.GetMessagingSessionEndpointResponse.to_json)
           (Some Values.GetMessagingSessionEndpointResponse.error_to_json)])
let list_channel_bans =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_bans
           (Values.ListChannelBansRequest.make ?maxResults ?nextToken
              ~channelArn ~chimeBearer ())
           (Some Values.ListChannelBansResponse.to_json)
           (Some Values.ListChannelBansResponse.error_to_json)])
let list_channel_flows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_flows
           (Values.ListChannelFlowsRequest.make ?maxResults ?nextToken
              ~appInstanceArn ())
           (Some Values.ListChannelFlowsResponse.to_json)
           (Some Values.ListChannelFlowsResponse.error_to_json)])
let list_channel_memberships =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON ChannelMembershipType"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_memberships
           (Values.ListChannelMembershipsRequest.make
              ?type_:(Option.map ~f:Values.ChannelMembershipType.of_json
                        type_) ?maxResults ?nextToken ~channelArn
              ~chimeBearer ())
           (Some Values.ListChannelMembershipsResponse.to_json)
           (Some Values.ListChannelMembershipsResponse.error_to_json)])
let list_channel_memberships_for_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (optional string)
           ~doc:"STRING ChimeArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_memberships_for_app_instance_user
           (Values.ListChannelMembershipsForAppInstanceUserRequest.make
              ?appInstanceUserArn ?maxResults ?nextToken ~chimeBearer ())
           (Some
              Values.ListChannelMembershipsForAppInstanceUserResponse.to_json)
           (Some
              Values.ListChannelMembershipsForAppInstanceUserResponse.error_to_json)])
let list_channel_messages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and notBefore =
         flag "not-before" (optional json_arg) ~doc:"JSON Timestamp"
       and notAfter =
         flag "not-after" (optional json_arg) ~doc:"JSON Timestamp"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_messages
           (Values.ListChannelMessagesRequest.make
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?notBefore:(Option.map ~f:Values.Timestamp.of_json notBefore)
              ?notAfter:(Option.map ~f:Values.Timestamp.of_json notAfter)
              ?maxResults ?nextToken ~channelArn ~chimeBearer ())
           (Some Values.ListChannelMessagesResponse.to_json)
           (Some Values.ListChannelMessagesResponse.error_to_json)])
let list_channel_moderators =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_moderators
           (Values.ListChannelModeratorsRequest.make ?maxResults ?nextToken
              ~channelArn ~chimeBearer ())
           (Some Values.ListChannelModeratorsResponse.to_json)
           (Some Values.ListChannelModeratorsResponse.error_to_json)])
let list_channels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and privacy =
         flag "privacy" (optional json_arg) ~doc:"JSON ChannelPrivacy"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channels
           (Values.ListChannelsRequest.make
              ?privacy:(Option.map ~f:Values.ChannelPrivacy.of_json privacy)
              ?maxResults ?nextToken ~appInstanceArn ~chimeBearer ())
           (Some Values.ListChannelsResponse.to_json)
           (Some Values.ListChannelsResponse.error_to_json)])
let list_channels_associated_with_channel_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and channelFlowArn =
         flag "channel-flow-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channels_associated_with_channel_flow
           (Values.ListChannelsAssociatedWithChannelFlowRequest.make
              ?maxResults ?nextToken ~channelFlowArn ())
           (Some Values.ListChannelsAssociatedWithChannelFlowResponse.to_json)
           (Some
              Values.ListChannelsAssociatedWithChannelFlowResponse.error_to_json)])
let list_channels_moderated_by_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (optional string)
           ~doc:"STRING ChimeArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channels_moderated_by_app_instance_user
           (Values.ListChannelsModeratedByAppInstanceUserRequest.make
              ?appInstanceUserArn ?maxResults ?nextToken ~chimeBearer ())
           (Some
              Values.ListChannelsModeratedByAppInstanceUserResponse.to_json)
           (Some
              Values.ListChannelsModeratedByAppInstanceUserResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_channel_membership_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn"
       and preferences =
         flag "preferences" (required json_arg)
           ~doc:"JSON ChannelMembershipPreferences" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_channel_membership_preferences
           (Values.PutChannelMembershipPreferencesRequest.make ~channelArn
              ~memberArn ~chimeBearer
              ~preferences:(Values.ChannelMembershipPreferences.of_json
                              preferences) ())
           (Some Values.PutChannelMembershipPreferencesResponse.to_json)
           (Some Values.PutChannelMembershipPreferencesResponse.error_to_json)])
let redact_channel_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING MessageId"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.redact_channel_message
           (Values.RedactChannelMessageRequest.make ~channelArn ~messageId
              ~chimeBearer ())
           (Some Values.RedactChannelMessageResponse.to_json)
           (Some Values.RedactChannelMessageResponse.error_to_json)])
let send_channel_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and pushNotification =
         flag "push-notification" (optional json_arg)
           ~doc:"JSON PushNotificationConfiguration"
       and messageAttributes =
         flag "message-attributes" (optional json_arg)
           ~doc:"JSON MessageAttributeMap"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and content =
         flag "content" (required string) ~doc:"STRING NonEmptyContent"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON ChannelMessageType"
       and persistence =
         flag "persistence" (required json_arg)
           ~doc:"JSON ChannelMessagePersistenceType"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_channel_message
           (Values.SendChannelMessageRequest.make ?metadata
              ?pushNotification:(Option.map
                                   ~f:Values.PushNotificationConfiguration.of_json
                                   pushNotification)
              ?messageAttributes:(Option.map
                                    ~f:Values.MessageAttributeMap.of_json
                                    messageAttributes) ~channelArn ~content
              ~type_:(Values.ChannelMessageType.of_json type_)
              ~persistence:(Values.ChannelMessagePersistenceType.of_json
                              persistence) ~clientRequestToken ~chimeBearer
              ()) (Some Values.SendChannelMessageResponse.to_json)
           (Some Values.SendChannelMessageResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ChimeArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ChimeArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and name =
         flag "name" (required string) ~doc:"STRING NonEmptyResourceName"
       and mode = flag "mode" (required json_arg) ~doc:"JSON ChannelMode"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel
           (Values.UpdateChannelRequest.make ?metadata ~channelArn ~name
              ~mode:(Values.ChannelMode.of_json mode) ~chimeBearer ())
           (Some Values.UpdateChannelResponse.to_json)
           (Some Values.UpdateChannelResponse.error_to_json)])
let update_channel_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelFlowArn =
         flag "channel-flow-arn" (required string) ~doc:"STRING ChimeArn"
       and processors =
         flag "processors" (required json_arg) ~doc:"JSON ProcessorList"
       and name =
         flag "name" (required string) ~doc:"STRING NonEmptyResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel_flow
           (Values.UpdateChannelFlowRequest.make ~channelFlowArn
              ~processors:(Values.ProcessorList.of_json processors) ~name ())
           (Some Values.UpdateChannelFlowResponse.to_json)
           (Some Values.UpdateChannelFlowResponse.error_to_json)])
let update_channel_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and content = flag "content" (optional string) ~doc:"STRING Content"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING MessageId"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel_message
           (Values.UpdateChannelMessageRequest.make ?content ?metadata
              ~channelArn ~messageId ~chimeBearer ())
           (Some Values.UpdateChannelMessageResponse.to_json)
           (Some Values.UpdateChannelMessageResponse.error_to_json)])
let update_channel_read_marker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and chimeBearer =
         flag "chime-bearer" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel_read_marker
           (Values.UpdateChannelReadMarkerRequest.make ~channelArn
              ~chimeBearer ())
           (Some Values.UpdateChannelReadMarkerResponse.to_json)
           (Some Values.UpdateChannelReadMarkerResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-channel-flow", associate_channel_flow);
    ("batch-create-channel-membership", batch_create_channel_membership);
    ("channel-flow-callback", channel_flow_callback);
    ("create-channel", create_channel);
    ("create-channel-ban", create_channel_ban);
    ("create-channel-flow", create_channel_flow);
    ("create-channel-membership", create_channel_membership);
    ("create-channel-moderator", create_channel_moderator);
    ("delete-channel", delete_channel);
    ("delete-channel-ban", delete_channel_ban);
    ("delete-channel-flow", delete_channel_flow);
    ("delete-channel-membership", delete_channel_membership);
    ("delete-channel-message", delete_channel_message);
    ("delete-channel-moderator", delete_channel_moderator);
    ("describe-channel", describe_channel);
    ("describe-channel-ban", describe_channel_ban);
    ("describe-channel-flow", describe_channel_flow);
    ("describe-channel-membership", describe_channel_membership);
    ("describe-channel-membership-for-app-instance-user",
      describe_channel_membership_for_app_instance_user);
    ("describe-channel-moderated-by-app-instance-user",
      describe_channel_moderated_by_app_instance_user);
    ("describe-channel-moderator", describe_channel_moderator);
    ("disassociate-channel-flow", disassociate_channel_flow);
    ("get-channel-membership-preferences",
      get_channel_membership_preferences);
    ("get-channel-message", get_channel_message);
    ("get-channel-message-status", get_channel_message_status);
    ("get-messaging-session-endpoint", get_messaging_session_endpoint);
    ("list-channel-bans", list_channel_bans);
    ("list-channel-flows", list_channel_flows);
    ("list-channel-memberships", list_channel_memberships);
    ("list-channel-memberships-for-app-instance-user",
      list_channel_memberships_for_app_instance_user);
    ("list-channel-messages", list_channel_messages);
    ("list-channel-moderators", list_channel_moderators);
    ("list-channels", list_channels);
    ("list-channels-associated-with-channel-flow",
      list_channels_associated_with_channel_flow);
    ("list-channels-moderated-by-app-instance-user",
      list_channels_moderated_by_app_instance_user);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-channel-membership-preferences",
      put_channel_membership_preferences);
    ("redact-channel-message", redact_channel_message);
    ("send-channel-message", send_channel_message);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-channel", update_channel);
    ("update-channel-flow", update_channel_flow);
    ("update-channel-message", update_channel_message);
    ("update-channel-read-marker", update_channel_read_marker)]
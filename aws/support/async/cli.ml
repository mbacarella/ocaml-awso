(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_attachments_to_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachmentSetId =
         flag "attachment-set-id" (optional string)
           ~doc:"STRING AttachmentSetId"
       and attachments =
         flag "attachments" (required json_arg) ~doc:"JSON Attachments" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_attachments_to_set
           (Values.AddAttachmentsToSetRequest.make ?attachmentSetId
              ~attachments:(Values.Attachments.of_json attachments) ())
           (Some Values.AddAttachmentsToSetResponse.to_json)
           (Some Values.AddAttachmentsToSetResponse.error_to_json)])
let add_communication_to_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and caseId = flag "case-id" (optional string) ~doc:"STRING CaseId"
       and ccEmailAddresses =
         flag "cc-email-addresses" (optional json_arg)
           ~doc:"JSON CcEmailAddressList"
       and attachmentSetId =
         flag "attachment-set-id" (optional string)
           ~doc:"STRING AttachmentSetId"
       and communicationBody =
         flag "communication-body" (required string)
           ~doc:"STRING CommunicationBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_communication_to_case
           (Values.AddCommunicationToCaseRequest.make ?caseId
              ?ccEmailAddresses:(Option.map
                                   ~f:Values.CcEmailAddressList.of_json
                                   ccEmailAddresses) ?attachmentSetId
              ~communicationBody ())
           (Some Values.AddCommunicationToCaseResponse.to_json)
           (Some Values.AddCommunicationToCaseResponse.error_to_json)])
let create_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceCode =
         flag "service-code" (optional string) ~doc:"STRING ServiceCode"
       and severityCode =
         flag "severity-code" (optional string) ~doc:"STRING SeverityCode"
       and categoryCode =
         flag "category-code" (optional string) ~doc:"STRING CategoryCode"
       and ccEmailAddresses =
         flag "cc-email-addresses" (optional json_arg)
           ~doc:"JSON CcEmailAddressList"
       and language =
         flag "language" (optional string) ~doc:"STRING Language"
       and issueType =
         flag "issue-type" (optional string) ~doc:"STRING IssueType"
       and attachmentSetId =
         flag "attachment-set-id" (optional string)
           ~doc:"STRING AttachmentSetId"
       and subject = flag "subject" (required string) ~doc:"STRING Subject"
       and communicationBody =
         flag "communication-body" (required string)
           ~doc:"STRING CommunicationBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_case
           (Values.CreateCaseRequest.make ?serviceCode ?severityCode
              ?categoryCode
              ?ccEmailAddresses:(Option.map
                                   ~f:Values.CcEmailAddressList.of_json
                                   ccEmailAddresses) ?language ?issueType
              ?attachmentSetId ~subject ~communicationBody ())
           (Some Values.CreateCaseResponse.to_json)
           (Some Values.CreateCaseResponse.error_to_json)])
let describe_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_attachment
           (Values.DescribeAttachmentRequest.make ~attachmentId ())
           (Some Values.DescribeAttachmentResponse.to_json)
           (Some Values.DescribeAttachmentResponse.error_to_json)])
let describe_cases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and caseIdList =
         flag "case-id-list" (optional json_arg) ~doc:"JSON CaseIdList"
       and displayId =
         flag "display-id" (optional string) ~doc:"STRING DisplayId"
       and afterTime =
         flag "after-time" (optional string) ~doc:"STRING AfterTime"
       and beforeTime =
         flag "before-time" (optional string) ~doc:"STRING BeforeTime"
       and includeResolvedCases =
         flag "include-resolved-cases" (optional bool)
           ~doc:"BOOL IncludeResolvedCases"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and language =
         flag "language" (optional string) ~doc:"STRING Language"
       and includeCommunications =
         flag "include-communications" (optional bool)
           ~doc:"BOOL IncludeCommunications" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cases
           (Values.DescribeCasesRequest.make
              ?caseIdList:(Option.map ~f:Values.CaseIdList.of_json caseIdList)
              ?displayId ?afterTime ?beforeTime ?includeResolvedCases
              ?nextToken ?maxResults ?language ?includeCommunications ())
           (Some Values.DescribeCasesResponse.to_json)
           (Some Values.DescribeCasesResponse.error_to_json)])
let describe_communications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and beforeTime =
         flag "before-time" (optional string) ~doc:"STRING BeforeTime"
       and afterTime =
         flag "after-time" (optional string) ~doc:"STRING AfterTime"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_communications
           (Values.DescribeCommunicationsRequest.make ?beforeTime ?afterTime
              ?nextToken ?maxResults ~caseId ())
           (Some Values.DescribeCommunicationsResponse.to_json)
           (Some Values.DescribeCommunicationsResponse.error_to_json)])
let describe_create_case_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and issueType =
         flag "issue-type" (required string) ~doc:"STRING IssueType"
       and serviceCode =
         flag "service-code" (required string) ~doc:"STRING ServiceCode"
       and language =
         flag "language" (required string) ~doc:"STRING Language"
       and categoryCode =
         flag "category-code" (required string) ~doc:"STRING CategoryCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_create_case_options
           (Values.DescribeCreateCaseOptionsRequest.make ~issueType
              ~serviceCode ~language ~categoryCode ())
           (Some Values.DescribeCreateCaseOptionsResponse.to_json)
           (Some Values.DescribeCreateCaseOptionsResponse.error_to_json)])
let describe_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceCodeList =
         flag "service-code-list" (optional json_arg)
           ~doc:"JSON ServiceCodeList"
       and language =
         flag "language" (optional string) ~doc:"STRING Language" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_services
           (Values.DescribeServicesRequest.make
              ?serviceCodeList:(Option.map ~f:Values.ServiceCodeList.of_json
                                  serviceCodeList) ?language ())
           (Some Values.DescribeServicesResponse.to_json)
           (Some Values.DescribeServicesResponse.error_to_json)])
let describe_severity_levels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and language =
         flag "language" (optional string) ~doc:"STRING Language" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_severity_levels
           (Values.DescribeSeverityLevelsRequest.make ?language ())
           (Some Values.DescribeSeverityLevelsResponse.to_json)
           (Some Values.DescribeSeverityLevelsResponse.error_to_json)])
let describe_supported_languages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and issueType =
         flag "issue-type" (required string)
           ~doc:"STRING ValidatedIssueTypeString"
       and serviceCode =
         flag "service-code" (required string)
           ~doc:"STRING ValidatedServiceCode"
       and categoryCode =
         flag "category-code" (required string)
           ~doc:"STRING ValidatedCategoryCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_supported_languages
           (Values.DescribeSupportedLanguagesRequest.make ~issueType
              ~serviceCode ~categoryCode ())
           (Some Values.DescribeSupportedLanguagesResponse.to_json)
           (Some Values.DescribeSupportedLanguagesResponse.error_to_json)])
let describe_trusted_advisor_check_refresh_statuses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and checkIds =
         flag "check-ids" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_trusted_advisor_check_refresh_statuses
           (Values.DescribeTrustedAdvisorCheckRefreshStatusesRequest.make
              ~checkIds:(Values.StringList.of_json checkIds) ())
           (Some
              Values.DescribeTrustedAdvisorCheckRefreshStatusesResponse.to_json)
           (Some
              Values.DescribeTrustedAdvisorCheckRefreshStatusesResponse.error_to_json)])
let describe_trusted_advisor_check_result =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and language = flag "language" (optional string) ~doc:"STRING String"
       and checkId = flag "check-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_trusted_advisor_check_result
           (Values.DescribeTrustedAdvisorCheckResultRequest.make ?language
              ~checkId ())
           (Some Values.DescribeTrustedAdvisorCheckResultResponse.to_json)
           (Some
              Values.DescribeTrustedAdvisorCheckResultResponse.error_to_json)])
let describe_trusted_advisor_check_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and checkIds =
         flag "check-ids" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_trusted_advisor_check_summaries
           (Values.DescribeTrustedAdvisorCheckSummariesRequest.make
              ~checkIds:(Values.StringList.of_json checkIds) ())
           (Some Values.DescribeTrustedAdvisorCheckSummariesResponse.to_json)
           (Some
              Values.DescribeTrustedAdvisorCheckSummariesResponse.error_to_json)])
let describe_trusted_advisor_checks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and language = flag "language" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_trusted_advisor_checks
           (Values.DescribeTrustedAdvisorChecksRequest.make ~language ())
           (Some Values.DescribeTrustedAdvisorChecksResponse.to_json)
           (Some Values.DescribeTrustedAdvisorChecksResponse.error_to_json)])
let refresh_trusted_advisor_check =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and checkId = flag "check-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.refresh_trusted_advisor_check
           (Values.RefreshTrustedAdvisorCheckRequest.make ~checkId ())
           (Some Values.RefreshTrustedAdvisorCheckResponse.to_json)
           (Some Values.RefreshTrustedAdvisorCheckResponse.error_to_json)])
let resolve_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and caseId = flag "case-id" (optional string) ~doc:"STRING CaseId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resolve_case (Values.ResolveCaseRequest.make ?caseId ())
           (Some Values.ResolveCaseResponse.to_json)
           (Some Values.ResolveCaseResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-attachments-to-set", add_attachments_to_set);
    ("add-communication-to-case", add_communication_to_case);
    ("create-case", create_case);
    ("describe-attachment", describe_attachment);
    ("describe-cases", describe_cases);
    ("describe-communications", describe_communications);
    ("describe-create-case-options", describe_create_case_options);
    ("describe-services", describe_services);
    ("describe-severity-levels", describe_severity_levels);
    ("describe-supported-languages", describe_supported_languages);
    ("describe-trusted-advisor-check-refresh-statuses",
      describe_trusted_advisor_check_refresh_statuses);
    ("describe-trusted-advisor-check-result",
      describe_trusted_advisor_check_result);
    ("describe-trusted-advisor-check-summaries",
      describe_trusted_advisor_check_summaries);
    ("describe-trusted-advisor-checks", describe_trusted_advisor_checks);
    ("refresh-trusted-advisor-check", refresh_trusted_advisor_check);
    ("resolve-case", resolve_case)]
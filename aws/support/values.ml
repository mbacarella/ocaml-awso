(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.support
let apiVersion = "2013-04-15"
let endpointPrefix = "support"
let serviceFullName = "AWS Support"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let targetPrefix = "AWSSupport_20130415"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module AttachmentId =
  struct
    type nonrec t = string
    let context_ = "AttachmentId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttachmentId" j
    let to_json = simple_to_json to_value
  end
module FileName =
  struct
    type nonrec t = string
    let context_ = "FileName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FileName" j
    let to_json = simple_to_json to_value
  end
module AttachmentDetails =
  struct
    type nonrec t =
      {
      attachmentId: AttachmentId.t option
        [@ocaml.doc "The ID of the attachment."];
      fileName: FileName.t option
        [@ocaml.doc "The file name of the attachment."]}
    let make ?attachmentId =
      fun ?fileName -> fun () -> { attachmentId; fileName }
    let to_value x =
      structure_to_value
        [("attachmentId",
           (Option.map x.attachmentId ~f:AttachmentId.to_value));
        ("fileName", (Option.map x.fileName ~f:FileName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fileName =
        (Option.map ~f:FileName.of_xml) (Xml.child xml_arg0 "fileName") in
      let attachmentId =
        (Option.map ~f:AttachmentId.of_xml)
          (Xml.child xml_arg0 "attachmentId") in
      make ?fileName ?attachmentId ()
    let of_json json =
      let fileName = field_map json "fileName" FileName.of_json in
      let attachmentId = field_map json "attachmentId" AttachmentId.of_json in
      make ?fileName ?attachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The file name and ID of an attachment to a case communication. You can use the ID to retrieve the attachment with the DescribeAttachment operation."]
module AttachmentSet =
  struct
    type nonrec t = AttachmentDetails.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AttachmentDetails.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AttachmentDetails.of_xml)
    let of_json j =
      list_of_json ~kind:"AttachmentSet" ~of_json:AttachmentDetails.of_json j
    let to_json v = composed_to_json to_value v
  end
module CaseId =
  struct
    type nonrec t = string
    let context_ = "CaseId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CaseId" j
    let to_json = simple_to_json to_value
  end
module CommunicationBody =
  struct
    type nonrec t = string
    let context_ = "CommunicationBody"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:8000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CommunicationBody" j
    let to_json = simple_to_json to_value
  end
module SubmittedBy =
  struct
    type nonrec t = string
    let context_ = "SubmittedBy"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SubmittedBy" j
    let to_json = simple_to_json to_value
  end
module TimeCreated =
  struct
    type nonrec t = string
    let context_ = "TimeCreated"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TimeCreated" j
    let to_json = simple_to_json to_value
  end
module Double =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module CategoryCode =
  struct
    type nonrec t = string
    let context_ = "CategoryCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CategoryCode" j
    let to_json = simple_to_json to_value
  end
module CategoryName =
  struct
    type nonrec t = string
    let context_ = "CategoryName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CategoryName" j
    let to_json = simple_to_json to_value
  end
module Communication =
  struct
    type nonrec t =
      {
      caseId: CaseId.t option
        [@ocaml.doc
          "The support case ID requested or returned in the call. The case ID is an alphanumeric string formatted as shown in this example: case-12345678910-2013-c4c1d2bf33c5cf47"];
      body: CommunicationBody.t option
        [@ocaml.doc
          "The text of the communication between the customer and Amazon Web Services Support."];
      submittedBy: SubmittedBy.t option
        [@ocaml.doc
          "The identity of the account that submitted, or responded to, the support case. Customer entries include the role or IAM user as well as the email address. For example, \"AdminRole (Role) <janedoe\\@example.com>. Entries from the Amazon Web Services Support team display \"Amazon Web Services,\" and don't show an email address."];
      timeCreated: TimeCreated.t option
        [@ocaml.doc "The time the communication was created."];
      attachmentSet: AttachmentSet.t option
        [@ocaml.doc
          "Information about the attachments to the case communication."]}
    let make ?caseId =
      fun ?body ->
        fun ?submittedBy ->
          fun ?timeCreated ->
            fun ?attachmentSet ->
              fun () ->
                { caseId; body; submittedBy; timeCreated; attachmentSet }
    let to_value x =
      structure_to_value
        [("caseId", (Option.map x.caseId ~f:CaseId.to_value));
        ("body", (Option.map x.body ~f:CommunicationBody.to_value));
        ("submittedBy", (Option.map x.submittedBy ~f:SubmittedBy.to_value));
        ("timeCreated", (Option.map x.timeCreated ~f:TimeCreated.to_value));
        ("attachmentSet",
          (Option.map x.attachmentSet ~f:AttachmentSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attachmentSet =
        (Option.map ~f:AttachmentSet.of_xml)
          (Xml.child xml_arg0 "attachmentSet") in
      let timeCreated =
        (Option.map ~f:TimeCreated.of_xml) (Xml.child xml_arg0 "timeCreated") in
      let submittedBy =
        (Option.map ~f:SubmittedBy.of_xml) (Xml.child xml_arg0 "submittedBy") in
      let body =
        (Option.map ~f:CommunicationBody.of_xml) (Xml.child xml_arg0 "body") in
      let caseId =
        (Option.map ~f:CaseId.of_xml) (Xml.child xml_arg0 "caseId") in
      make ?attachmentSet ?timeCreated ?submittedBy ?body ?caseId ()
    let of_json json =
      let attachmentSet =
        field_map json "attachmentSet" AttachmentSet.of_json in
      let timeCreated = field_map json "timeCreated" TimeCreated.of_json in
      let submittedBy = field_map json "submittedBy" SubmittedBy.of_json in
      let body = field_map json "body" CommunicationBody.of_json in
      let caseId = field_map json "caseId" CaseId.of_json in
      make ?attachmentSet ?timeCreated ?submittedBy ?body ?caseId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A communication associated with a support case. The communication consists of the case ID, the message body, attachment information, the submitter of the communication, and the date and time of the communication."]
module TrustedAdvisorCostOptimizingSummary =
  struct
    type nonrec t =
      {
      estimatedMonthlySavings: Double.t
        [@ocaml.doc
          "The estimated monthly savings that might be realized if the recommended operations are taken."];
      estimatedPercentMonthlySavings: Double.t
        [@ocaml.doc
          "The estimated percentage of savings that might be realized if the recommended operations are taken."]}
    let context_ = "TrustedAdvisorCostOptimizingSummary"
    let make ~estimatedMonthlySavings =
      fun ~estimatedPercentMonthlySavings ->
        fun () -> { estimatedMonthlySavings; estimatedPercentMonthlySavings }
    let to_value x =
      structure_to_value
        [("estimatedMonthlySavings",
           (Some (Double.to_value x.estimatedMonthlySavings)));
        ("estimatedPercentMonthlySavings",
          (Some (Double.to_value x.estimatedPercentMonthlySavings)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let estimatedPercentMonthlySavings =
        Double.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "estimatedPercentMonthlySavings") in
      let estimatedMonthlySavings =
        Double.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "estimatedMonthlySavings") in
      make ~estimatedPercentMonthlySavings ~estimatedMonthlySavings ()
    let of_json json =
      let estimatedPercentMonthlySavings =
        field_map_exn json "estimatedPercentMonthlySavings" Double.of_json in
      let estimatedMonthlySavings =
        field_map_exn json "estimatedMonthlySavings" Double.of_json in
      make ~estimatedPercentMonthlySavings ~estimatedMonthlySavings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The estimated cost savings that might be realized if the recommended operations are taken."]
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module StringList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"StringList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module Category =
  struct
    type nonrec t =
      {
      code: CategoryCode.t option
        [@ocaml.doc "The category code for the support case."];
      name: CategoryName.t option
        [@ocaml.doc "The category name for the support case."]}
    let make ?code = fun ?name -> fun () -> { code; name }
    let to_value x =
      structure_to_value
        [("code", (Option.map x.code ~f:CategoryCode.to_value));
        ("name", (Option.map x.name ~f:CategoryName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:CategoryName.of_xml) (Xml.child xml_arg0 "name") in
      let code =
        (Option.map ~f:CategoryCode.of_xml) (Xml.child xml_arg0 "code") in
      make ?name ?code ()
    let of_json json =
      let name = field_map json "name" CategoryName.of_json in
      let code = field_map json "code" CategoryCode.of_json in
      make ?name ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A JSON-formatted name/value pair that represents the category name and category code of the problem, selected from the DescribeServices response for each Amazon Web Services service."]
module CcEmailAddress =
  struct
    type nonrec t = string
    let context_ = "CcEmailAddress"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CcEmailAddress" j
    let to_json = simple_to_json to_value
  end
module CommunicationList =
  struct
    type nonrec t = Communication.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Communication.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Communication.of_xml)
    let of_json j =
      list_of_json ~kind:"CommunicationList" ~of_json:Communication.of_json j
    let to_json v = composed_to_json to_value v
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module TrustedAdvisorCategorySpecificSummary =
  struct
    type nonrec t =
      {
      costOptimizing: TrustedAdvisorCostOptimizingSummary.t option
        [@ocaml.doc
          "The summary information about cost savings for a Trusted Advisor check that is in the Cost Optimizing category."]}
    let make ?costOptimizing = fun () -> { costOptimizing }
    let to_value x =
      structure_to_value
        [("costOptimizing",
           (Option.map x.costOptimizing
              ~f:TrustedAdvisorCostOptimizingSummary.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costOptimizing =
        (Option.map ~f:TrustedAdvisorCostOptimizingSummary.of_xml)
          (Xml.child xml_arg0 "costOptimizing") in
      make ?costOptimizing ()
    let of_json json =
      let costOptimizing =
        field_map json "costOptimizing"
          TrustedAdvisorCostOptimizingSummary.of_json in
      make ?costOptimizing ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The container for summary information that relates to the category of the Trusted Advisor check."]
module TrustedAdvisorResourcesSummary =
  struct
    type nonrec t =
      {
      resourcesProcessed: Long.t
        [@ocaml.doc
          "The number of Amazon Web Services resources that were analyzed by the Trusted Advisor check."];
      resourcesFlagged: Long.t
        [@ocaml.doc
          "The number of Amazon Web Services resources that were flagged (listed) by the Trusted Advisor check."];
      resourcesIgnored: Long.t
        [@ocaml.doc
          "The number of Amazon Web Services resources ignored by Trusted Advisor because information was unavailable."];
      resourcesSuppressed: Long.t
        [@ocaml.doc
          "The number of Amazon Web Services resources ignored by Trusted Advisor because they were marked as suppressed by the user."]}
    let context_ = "TrustedAdvisorResourcesSummary"
    let make ~resourcesProcessed =
      fun ~resourcesFlagged ->
        fun ~resourcesIgnored ->
          fun ~resourcesSuppressed ->
            fun () ->
              {
                resourcesProcessed;
                resourcesFlagged;
                resourcesIgnored;
                resourcesSuppressed
              }
    let to_value x =
      structure_to_value
        [("resourcesProcessed", (Some (Long.to_value x.resourcesProcessed)));
        ("resourcesFlagged", (Some (Long.to_value x.resourcesFlagged)));
        ("resourcesIgnored", (Some (Long.to_value x.resourcesIgnored)));
        ("resourcesSuppressed", (Some (Long.to_value x.resourcesSuppressed)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourcesSuppressed =
        Long.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourcesSuppressed") in
      let resourcesIgnored =
        Long.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourcesIgnored") in
      let resourcesFlagged =
        Long.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourcesFlagged") in
      let resourcesProcessed =
        Long.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourcesProcessed") in
      make ~resourcesSuppressed ~resourcesIgnored ~resourcesFlagged
        ~resourcesProcessed ()
    let of_json json =
      let resourcesSuppressed =
        field_map_exn json "resourcesSuppressed" Long.of_json in
      let resourcesIgnored =
        field_map_exn json "resourcesIgnored" Long.of_json in
      let resourcesFlagged =
        field_map_exn json "resourcesFlagged" Long.of_json in
      let resourcesProcessed =
        field_map_exn json "resourcesProcessed" Long.of_json in
      make ~resourcesSuppressed ~resourcesIgnored ~resourcesFlagged
        ~resourcesProcessed ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about Amazon Web Services resources that were analyzed in a call to Trusted Advisor DescribeTrustedAdvisorCheckSummaries."]
module TrustedAdvisorResourceDetail =
  struct
    type nonrec t =
      {
      status: String_.t
        [@ocaml.doc
          "The status code for the resource identified in the Trusted Advisor check."];
      region: String_.t option
        [@ocaml.doc
          "The Amazon Web Services Region in which the identified resource is located."];
      resourceId: String_.t
        [@ocaml.doc "The unique identifier for the identified resource."];
      isSuppressed: Boolean.t option
        [@ocaml.doc
          "Specifies whether the Amazon Web Services resource was ignored by Trusted Advisor because it was marked as suppressed by the user."];
      metadata: StringList.t
        [@ocaml.doc
          "Additional information about the identified resource. The exact metadata and its order can be obtained by inspecting the TrustedAdvisorCheckDescription object returned by the call to DescribeTrustedAdvisorChecks. Metadata contains all the data that is shown in the Excel download, even in those cases where the UI shows just summary data."]}
    let context_ = "TrustedAdvisorResourceDetail"
    let make ?region =
      fun ?isSuppressed ->
        fun ~status ->
          fun ~resourceId ->
            fun ~metadata ->
              fun () ->
                { region; isSuppressed; status; resourceId; metadata }
    let to_value x =
      structure_to_value
        [("status", (Some (String_.to_value x.status)));
        ("region", (Option.map x.region ~f:String_.to_value));
        ("resourceId", (Some (String_.to_value x.resourceId)));
        ("isSuppressed", (Option.map x.isSuppressed ~f:Boolean.to_value));
        ("metadata", (Some (StringList.to_value x.metadata)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metadata =
        StringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "metadata") in
      let isSuppressed =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isSuppressed") in
      let resourceId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceId") in
      let region =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "region") in
      let status =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "status") in
      make ~metadata ?isSuppressed ~resourceId ?region ~status ()
    let of_json json =
      let metadata = field_map_exn json "metadata" StringList.of_json in
      let isSuppressed = field_map json "isSuppressed" Boolean.of_json in
      let resourceId = field_map_exn json "resourceId" String_.of_json in
      let region = field_map json "region" String_.of_json in
      let status = field_map_exn json "status" String_.of_json in
      make ~metadata ?isSuppressed ~resourceId ?region ~status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a resource identified by a Trusted Advisor check."]
module SeverityLevelCode =
  struct
    type nonrec t = string
    let context_ = "SeverityLevelCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SeverityLevelCode" j
    let to_json = simple_to_json to_value
  end
module SeverityLevelName =
  struct
    type nonrec t = string
    let context_ = "SeverityLevelName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SeverityLevelName" j
    let to_json = simple_to_json to_value
  end
module CategoryList =
  struct
    type nonrec t = Category.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Category.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Category.of_xml)
    let of_json j =
      list_of_json ~kind:"CategoryList" ~of_json:Category.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServiceCode =
  struct
    type nonrec t = string
    let context_ = "ServiceCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServiceCode" j
    let to_json = simple_to_json to_value
  end
module ServiceName =
  struct
    type nonrec t = string
    let context_ = "ServiceName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServiceName" j
    let to_json = simple_to_json to_value
  end
module CcEmailAddressList =
  struct
    type nonrec t = CcEmailAddress.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:CcEmailAddress.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CcEmailAddress.of_xml)
    let of_json j =
      list_of_json ~kind:"CcEmailAddressList" ~of_json:CcEmailAddress.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module DisplayId =
  struct
    type nonrec t = string
    let context_ = "DisplayId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DisplayId" j
    let to_json = simple_to_json to_value
  end
module Language =
  struct
    type nonrec t = string
    let context_ = "Language"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Language" j
    let to_json = simple_to_json to_value
  end
module RecentCaseCommunications =
  struct
    type nonrec t =
      {
      communications: CommunicationList.t option
        [@ocaml.doc
          "The five most recent communications associated with the case."];
      nextToken: NextToken.t option
        [@ocaml.doc "A resumption point for pagination."]}
    let make ?communications =
      fun ?nextToken -> fun () -> { communications; nextToken }
    let to_value x =
      structure_to_value
        [("communications",
           (Option.map x.communications ~f:CommunicationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let communications =
        (Option.map ~f:CommunicationList.of_xml)
          (Xml.child xml_arg0 "communications") in
      make ?nextToken ?communications ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let communications =
        field_map json "communications" CommunicationList.of_json in
      make ?nextToken ?communications ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The five most recent communications associated with the case."]
module SeverityCode =
  struct
    type nonrec t = string
    let context_ = "SeverityCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SeverityCode" j
    let to_json = simple_to_json to_value
  end
module Status =
  struct
    type nonrec t = string
    let context_ = "Status"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Status" j
    let to_json = simple_to_json to_value
  end
module Subject =
  struct
    type nonrec t = string
    let context_ = "Subject"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Subject" j
    let to_json = simple_to_json to_value
  end
module Data =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module TrustedAdvisorCheckDescription =
  struct
    type nonrec t =
      {
      id: String_.t
        [@ocaml.doc "The unique identifier for the Trusted Advisor check."];
      name: String_.t
        [@ocaml.doc "The display name for the Trusted Advisor check."];
      description: String_.t
        [@ocaml.doc
          "The description of the Trusted Advisor check, which includes the alert criteria and recommended operations (contains HTML markup)."];
      category: String_.t
        [@ocaml.doc "The category of the Trusted Advisor check."];
      metadata: StringList.t
        [@ocaml.doc
          "The column headings for the data returned by the Trusted Advisor check. The order of the headings corresponds to the order of the data in the Metadata element of the TrustedAdvisorResourceDetail for the check. Metadata contains all the data that is shown in the Excel download, even in those cases where the UI shows just summary data."]}
    let context_ = "TrustedAdvisorCheckDescription"
    let make ~id =
      fun ~name ->
        fun ~description ->
          fun ~category ->
            fun ~metadata ->
              fun () -> { id; name; description; category; metadata }
    let to_value x =
      structure_to_value
        [("id", (Some (String_.to_value x.id)));
        ("name", (Some (String_.to_value x.name)));
        ("description", (Some (String_.to_value x.description)));
        ("category", (Some (String_.to_value x.category)));
        ("metadata", (Some (StringList.to_value x.metadata)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metadata =
        StringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "metadata") in
      let category =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "category") in
      let description =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "description") in
      let name =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let id = String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~metadata ~category ~description ~name ~id ()
    let of_json json =
      let metadata = field_map_exn json "metadata" StringList.of_json in
      let category = field_map_exn json "category" String_.of_json in
      let description = field_map_exn json "description" String_.of_json in
      let name = field_map_exn json "name" String_.of_json in
      let id = field_map_exn json "id" String_.of_json in
      make ~metadata ~category ~description ~name ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The description and metadata for a Trusted Advisor check."]
module TrustedAdvisorCheckSummary =
  struct
    type nonrec t =
      {
      checkId: String_.t
        [@ocaml.doc "The unique identifier for the Trusted Advisor check."];
      timestamp: String_.t
        [@ocaml.doc "The time of the last refresh of the check."];
      status: String_.t
        [@ocaml.doc
          "The alert status of the check: \"ok\" (green), \"warning\" (yellow), \"error\" (red), or \"not_available\"."];
      hasFlaggedResources: Boolean.t option
        [@ocaml.doc
          "Specifies whether the Trusted Advisor check has flagged resources."];
      resourcesSummary: TrustedAdvisorResourcesSummary.t ;
      categorySpecificSummary: TrustedAdvisorCategorySpecificSummary.t
        [@ocaml.doc
          "Summary information that relates to the category of the check. Cost Optimizing is the only category that is currently supported."]}
    let context_ = "TrustedAdvisorCheckSummary"
    let make ?hasFlaggedResources =
      fun ~checkId ->
        fun ~timestamp ->
          fun ~status ->
            fun ~resourcesSummary ->
              fun ~categorySpecificSummary ->
                fun () ->
                  {
                    hasFlaggedResources;
                    checkId;
                    timestamp;
                    status;
                    resourcesSummary;
                    categorySpecificSummary
                  }
    let to_value x =
      structure_to_value
        [("checkId", (Some (String_.to_value x.checkId)));
        ("timestamp", (Some (String_.to_value x.timestamp)));
        ("status", (Some (String_.to_value x.status)));
        ("hasFlaggedResources",
          (Option.map x.hasFlaggedResources ~f:Boolean.to_value));
        ("resourcesSummary",
          (Some (TrustedAdvisorResourcesSummary.to_value x.resourcesSummary)));
        ("categorySpecificSummary",
          (Some
             (TrustedAdvisorCategorySpecificSummary.to_value
                x.categorySpecificSummary)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let categorySpecificSummary =
        TrustedAdvisorCategorySpecificSummary.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "categorySpecificSummary") in
      let resourcesSummary =
        TrustedAdvisorResourcesSummary.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourcesSummary") in
      let hasFlaggedResources =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "hasFlaggedResources") in
      let status =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "status") in
      let timestamp =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "timestamp") in
      let checkId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "checkId") in
      make ~categorySpecificSummary ~resourcesSummary ?hasFlaggedResources
        ~status ~timestamp ~checkId ()
    let of_json json =
      let categorySpecificSummary =
        field_map_exn json "categorySpecificSummary"
          TrustedAdvisorCategorySpecificSummary.of_json in
      let resourcesSummary =
        field_map_exn json "resourcesSummary"
          TrustedAdvisorResourcesSummary.of_json in
      let hasFlaggedResources =
        field_map json "hasFlaggedResources" Boolean.of_json in
      let status = field_map_exn json "status" String_.of_json in
      let timestamp = field_map_exn json "timestamp" String_.of_json in
      let checkId = field_map_exn json "checkId" String_.of_json in
      make ~categorySpecificSummary ~resourcesSummary ?hasFlaggedResources
        ~status ~timestamp ~checkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A summary of a Trusted Advisor check result, including the alert status, last refresh, and number of resources examined."]
module TrustedAdvisorResourceDetailList =
  struct
    type nonrec t = TrustedAdvisorResourceDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TrustedAdvisorResourceDetail.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TrustedAdvisorResourceDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"TrustedAdvisorResourceDetailList"
        ~of_json:TrustedAdvisorResourceDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module TrustedAdvisorCheckRefreshStatus =
  struct
    type nonrec t =
      {
      checkId: String_.t
        [@ocaml.doc "The unique identifier for the Trusted Advisor check."];
      status: String_.t
        [@ocaml.doc
          "The status of the Trusted Advisor check for which a refresh has been requested: none - The check is not refreshed or the non-success status exceeds the timeout enqueued - The check refresh requests has entered the refresh queue processing - The check refresh request is picked up by the rule processing engine success - The check is successfully refreshed abandoned - The check refresh has failed"];
      millisUntilNextRefreshable: Long.t
        [@ocaml.doc
          "The amount of time, in milliseconds, until the Trusted Advisor check is eligible for refresh."]}
    let context_ = "TrustedAdvisorCheckRefreshStatus"
    let make ~checkId =
      fun ~status ->
        fun ~millisUntilNextRefreshable ->
          fun () -> { checkId; status; millisUntilNextRefreshable }
    let to_value x =
      structure_to_value
        [("checkId", (Some (String_.to_value x.checkId)));
        ("status", (Some (String_.to_value x.status)));
        ("millisUntilNextRefreshable",
          (Some (Long.to_value x.millisUntilNextRefreshable)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let millisUntilNextRefreshable =
        Long.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "millisUntilNextRefreshable") in
      let status =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "status") in
      let checkId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "checkId") in
      make ~millisUntilNextRefreshable ~status ~checkId ()
    let of_json json =
      let millisUntilNextRefreshable =
        field_map_exn json "millisUntilNextRefreshable" Long.of_json in
      let status = field_map_exn json "status" String_.of_json in
      let checkId = field_map_exn json "checkId" String_.of_json in
      make ~millisUntilNextRefreshable ~status ~checkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The refresh status of a Trusted Advisor check."]
module SeverityLevel =
  struct
    type nonrec t =
      {
      code: SeverityLevelCode.t option
        [@ocaml.doc
          "The code for case severity level. Valid values: low | normal | high | urgent | critical"];
      name: SeverityLevelName.t option
        [@ocaml.doc
          "The name of the severity level that corresponds to the severity level code. The values returned by the API are different from the values that appear in the Amazon Web Services Support Center. For example, the API uses the code low, but the name appears as General guidance in Support Center. The following are the API code names and how they appear in the console: low - General guidance normal - System impaired high - Production system impaired urgent - Production system down critical - Business-critical system down For more information, see Choosing a severity in the Amazon Web Services Support User Guide."]}
    let make ?code = fun ?name -> fun () -> { code; name }
    let to_value x =
      structure_to_value
        [("code", (Option.map x.code ~f:SeverityLevelCode.to_value));
        ("name", (Option.map x.name ~f:SeverityLevelName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:SeverityLevelName.of_xml) (Xml.child xml_arg0 "name") in
      let code =
        (Option.map ~f:SeverityLevelCode.of_xml) (Xml.child xml_arg0 "code") in
      make ?name ?code ()
    let of_json json =
      let name = field_map json "name" SeverityLevelName.of_json in
      let code = field_map json "code" SeverityLevelCode.of_json in
      make ?name ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A code and name pair that represents the severity level of a support case. The available values depend on the support plan for the account. For more information, see Choosing a severity in the Amazon Web Services Support User Guide."]
module Service =
  struct
    type nonrec t =
      {
      code: ServiceCode.t option
        [@ocaml.doc
          "The code for an Amazon Web Services service returned by the DescribeServices response. The name element contains the corresponding friendly name."];
      name: ServiceName.t option
        [@ocaml.doc
          "The friendly name for an Amazon Web Services service. The code element contains the corresponding code."];
      categories: CategoryList.t option
        [@ocaml.doc
          "A list of categories that describe the type of support issue a case describes. Categories consist of a category name and a category code. Category names and codes are passed to Amazon Web Services Support when you call CreateCase."]}
    let make ?code =
      fun ?name -> fun ?categories -> fun () -> { code; name; categories }
    let to_value x =
      structure_to_value
        [("code", (Option.map x.code ~f:ServiceCode.to_value));
        ("name", (Option.map x.name ~f:ServiceName.to_value));
        ("categories", (Option.map x.categories ~f:CategoryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let categories =
        (Option.map ~f:CategoryList.of_xml) (Xml.child xml_arg0 "categories") in
      let name =
        (Option.map ~f:ServiceName.of_xml) (Xml.child xml_arg0 "name") in
      let code =
        (Option.map ~f:ServiceCode.of_xml) (Xml.child xml_arg0 "code") in
      make ?categories ?name ?code ()
    let of_json json =
      let categories = field_map json "categories" CategoryList.of_json in
      let name = field_map json "name" ServiceName.of_json in
      let code = field_map json "code" ServiceCode.of_json in
      make ?categories ?name ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about an Amazon Web Services service returned by the DescribeServices operation."]
module CaseDetails =
  struct
    type nonrec t =
      {
      caseId: CaseId.t option
        [@ocaml.doc
          "The support case ID requested or returned in the call. The case ID is an alphanumeric string formatted as shown in this example: case-12345678910-2013-c4c1d2bf33c5cf47"];
      displayId: DisplayId.t option
        [@ocaml.doc
          "The ID displayed for the case in the Amazon Web Services Support Center. This is a numeric string."];
      subject: Subject.t option
        [@ocaml.doc
          "The subject line for the case in the Amazon Web Services Support Center."];
      status: Status.t option
        [@ocaml.doc
          "The status of the case. Valid values: opened pending-customer-action reopened resolved unassigned work-in-progress"];
      serviceCode: ServiceCode.t option
        [@ocaml.doc
          "The code for the Amazon Web Services service. You can get a list of codes and the corresponding service names by calling DescribeServices."];
      categoryCode: CategoryCode.t option
        [@ocaml.doc "The category of problem for the support case."];
      severityCode: SeverityCode.t option
        [@ocaml.doc
          "The code for the severity level returned by the call to DescribeSeverityLevels."];
      submittedBy: SubmittedBy.t option
        [@ocaml.doc
          "The email address of the account that submitted the case."];
      timeCreated: TimeCreated.t option
        [@ocaml.doc
          "The time that the case was created in the Amazon Web Services Support Center."];
      recentCommunications: RecentCaseCommunications.t option
        [@ocaml.doc
          "The five most recent communications between you and Amazon Web Services Support Center, including the IDs of any attachments to the communications. Also includes a nextToken that you can use to retrieve earlier communications."];
      ccEmailAddresses: CcEmailAddressList.t option
        [@ocaml.doc
          "The email addresses that receive copies of communication about the case."];
      language: Language.t option
        [@ocaml.doc
          "The ISO 639-1 code for the language in which Amazon Web Services provides support. Amazon Web Services Support currently supports English (\"en\") and Japanese (\"ja\"). Language parameters must be passed explicitly for operations that take them."]}
    let make ?caseId =
      fun ?displayId ->
        fun ?subject ->
          fun ?status ->
            fun ?serviceCode ->
              fun ?categoryCode ->
                fun ?severityCode ->
                  fun ?submittedBy ->
                    fun ?timeCreated ->
                      fun ?recentCommunications ->
                        fun ?ccEmailAddresses ->
                          fun ?language ->
                            fun () ->
                              {
                                caseId;
                                displayId;
                                subject;
                                status;
                                serviceCode;
                                categoryCode;
                                severityCode;
                                submittedBy;
                                timeCreated;
                                recentCommunications;
                                ccEmailAddresses;
                                language
                              }
    let to_value x =
      structure_to_value
        [("caseId", (Option.map x.caseId ~f:CaseId.to_value));
        ("displayId", (Option.map x.displayId ~f:DisplayId.to_value));
        ("subject", (Option.map x.subject ~f:Subject.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("serviceCode", (Option.map x.serviceCode ~f:ServiceCode.to_value));
        ("categoryCode",
          (Option.map x.categoryCode ~f:CategoryCode.to_value));
        ("severityCode",
          (Option.map x.severityCode ~f:SeverityCode.to_value));
        ("submittedBy", (Option.map x.submittedBy ~f:SubmittedBy.to_value));
        ("timeCreated", (Option.map x.timeCreated ~f:TimeCreated.to_value));
        ("recentCommunications",
          (Option.map x.recentCommunications
             ~f:RecentCaseCommunications.to_value));
        ("ccEmailAddresses",
          (Option.map x.ccEmailAddresses ~f:CcEmailAddressList.to_value));
        ("language", (Option.map x.language ~f:Language.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let language =
        (Option.map ~f:Language.of_xml) (Xml.child xml_arg0 "language") in
      let ccEmailAddresses =
        (Option.map ~f:CcEmailAddressList.of_xml)
          (Xml.child xml_arg0 "ccEmailAddresses") in
      let recentCommunications =
        (Option.map ~f:RecentCaseCommunications.of_xml)
          (Xml.child xml_arg0 "recentCommunications") in
      let timeCreated =
        (Option.map ~f:TimeCreated.of_xml) (Xml.child xml_arg0 "timeCreated") in
      let submittedBy =
        (Option.map ~f:SubmittedBy.of_xml) (Xml.child xml_arg0 "submittedBy") in
      let severityCode =
        (Option.map ~f:SeverityCode.of_xml)
          (Xml.child xml_arg0 "severityCode") in
      let categoryCode =
        (Option.map ~f:CategoryCode.of_xml)
          (Xml.child xml_arg0 "categoryCode") in
      let serviceCode =
        (Option.map ~f:ServiceCode.of_xml) (Xml.child xml_arg0 "serviceCode") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let subject =
        (Option.map ~f:Subject.of_xml) (Xml.child xml_arg0 "subject") in
      let displayId =
        (Option.map ~f:DisplayId.of_xml) (Xml.child xml_arg0 "displayId") in
      let caseId =
        (Option.map ~f:CaseId.of_xml) (Xml.child xml_arg0 "caseId") in
      make ?language ?ccEmailAddresses ?recentCommunications ?timeCreated
        ?submittedBy ?severityCode ?categoryCode ?serviceCode ?status
        ?subject ?displayId ?caseId ()
    let of_json json =
      let language = field_map json "language" Language.of_json in
      let ccEmailAddresses =
        field_map json "ccEmailAddresses" CcEmailAddressList.of_json in
      let recentCommunications =
        field_map json "recentCommunications"
          RecentCaseCommunications.of_json in
      let timeCreated = field_map json "timeCreated" TimeCreated.of_json in
      let submittedBy = field_map json "submittedBy" SubmittedBy.of_json in
      let severityCode = field_map json "severityCode" SeverityCode.of_json in
      let categoryCode = field_map json "categoryCode" CategoryCode.of_json in
      let serviceCode = field_map json "serviceCode" ServiceCode.of_json in
      let status = field_map json "status" Status.of_json in
      let subject = field_map json "subject" Subject.of_json in
      let displayId = field_map json "displayId" DisplayId.of_json in
      let caseId = field_map json "caseId" CaseId.of_json in
      make ?language ?ccEmailAddresses ?recentCommunications ?timeCreated
        ?submittedBy ?severityCode ?categoryCode ?serviceCode ?status
        ?subject ?displayId ?caseId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A JSON-formatted object that contains the metadata for a support case. It is contained in the response from a DescribeCases request. CaseDetails contains the following fields: caseId - The support case ID requested or returned in the call. The case ID is an alphanumeric string formatted as shown in this example: case-12345678910-2013-c4c1d2bf33c5cf47. categoryCode - The category of problem for the support case. Corresponds to the CategoryCode values returned by a call to DescribeServices. displayId - The identifier for the case on pages in the Amazon Web Services Support Center. language - The ISO 639-1 code for the language in which Amazon Web Services provides support. Amazon Web Services Support currently supports English (\"en\") and Japanese (\"ja\"). Language parameters must be passed explicitly for operations that take them. nextToken - A resumption point for pagination. recentCommunications - One or more Communication objects. Fields of these objects are attachments, body, caseId, submittedBy, and timeCreated. serviceCode - The identifier for the Amazon Web Services service that corresponds to the service code defined in the call to DescribeServices. severityCode - The severity code assigned to the case. Contains one of the values returned by the call to DescribeSeverityLevels. The possible values are: low, normal, high, urgent, and critical. status - The status of the case in the Amazon Web Services Support Center. Valid values: opened pending-customer-action reopened resolved unassigned work-in-progress subject - The subject line of the case. submittedBy - The email address of the account that submitted the case. timeCreated - The time the case was created, in ISO-8601 format."]
module Attachment =
  struct
    type nonrec t =
      {
      fileName: FileName.t option
        [@ocaml.doc "The name of the attachment file."];
      data: Data.t option [@ocaml.doc "The content of the attachment file."]}
    let make ?fileName = fun ?data -> fun () -> { fileName; data }
    let to_value x =
      structure_to_value
        [("fileName", (Option.map x.fileName ~f:FileName.to_value));
        ("data", (Option.map x.data ~f:Data.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let data = (Option.map ~f:Data.of_xml) (Xml.child xml_arg0 "data") in
      let fileName =
        (Option.map ~f:FileName.of_xml) (Xml.child xml_arg0 "fileName") in
      make ?data ?fileName ()
    let of_json json =
      let data = field_map json "data" Data.of_json in
      let fileName = field_map json "fileName" FileName.of_json in
      make ?data ?fileName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An attachment to a case communication. The attachment consists of the file name and the content of the file."]
module CaseIdNotFound =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc "The requested CaseId could not be located."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The requested caseId couldn't be located."]
module CaseStatus =
  struct
    type nonrec t = string
    let context_ = "CaseStatus"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CaseStatus" j
    let to_json = simple_to_json to_value
  end
module InternalServerError =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc "An internal server error occurred."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An internal server error occurred."]
module TrustedAdvisorCheckList =
  struct
    type nonrec t = TrustedAdvisorCheckDescription.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TrustedAdvisorCheckDescription.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TrustedAdvisorCheckDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"TrustedAdvisorCheckList"
        ~of_json:TrustedAdvisorCheckDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module TrustedAdvisorCheckSummaryList =
  struct
    type nonrec t = TrustedAdvisorCheckSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TrustedAdvisorCheckSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TrustedAdvisorCheckSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"TrustedAdvisorCheckSummaryList"
        ~of_json:TrustedAdvisorCheckSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module TrustedAdvisorCheckResult =
  struct
    type nonrec t =
      {
      checkId: String_.t
        [@ocaml.doc "The unique identifier for the Trusted Advisor check."];
      timestamp: String_.t
        [@ocaml.doc "The time of the last refresh of the check."];
      status: String_.t
        [@ocaml.doc
          "The alert status of the check: \"ok\" (green), \"warning\" (yellow), \"error\" (red), or \"not_available\"."];
      resourcesSummary: TrustedAdvisorResourcesSummary.t ;
      categorySpecificSummary: TrustedAdvisorCategorySpecificSummary.t
        [@ocaml.doc
          "Summary information that relates to the category of the check. Cost Optimizing is the only category that is currently supported."];
      flaggedResources: TrustedAdvisorResourceDetailList.t
        [@ocaml.doc
          "The details about each resource listed in the check result."]}
    let context_ = "TrustedAdvisorCheckResult"
    let make ~checkId =
      fun ~timestamp ->
        fun ~status ->
          fun ~resourcesSummary ->
            fun ~categorySpecificSummary ->
              fun ~flaggedResources ->
                fun () ->
                  {
                    checkId;
                    timestamp;
                    status;
                    resourcesSummary;
                    categorySpecificSummary;
                    flaggedResources
                  }
    let to_value x =
      structure_to_value
        [("checkId", (Some (String_.to_value x.checkId)));
        ("timestamp", (Some (String_.to_value x.timestamp)));
        ("status", (Some (String_.to_value x.status)));
        ("resourcesSummary",
          (Some (TrustedAdvisorResourcesSummary.to_value x.resourcesSummary)));
        ("categorySpecificSummary",
          (Some
             (TrustedAdvisorCategorySpecificSummary.to_value
                x.categorySpecificSummary)));
        ("flaggedResources",
          (Some
             (TrustedAdvisorResourceDetailList.to_value x.flaggedResources)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let flaggedResources =
        TrustedAdvisorResourceDetailList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flaggedResources") in
      let categorySpecificSummary =
        TrustedAdvisorCategorySpecificSummary.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "categorySpecificSummary") in
      let resourcesSummary =
        TrustedAdvisorResourcesSummary.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourcesSummary") in
      let status =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "status") in
      let timestamp =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "timestamp") in
      let checkId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "checkId") in
      make ~flaggedResources ~categorySpecificSummary ~resourcesSummary
        ~status ~timestamp ~checkId ()
    let of_json json =
      let flaggedResources =
        field_map_exn json "flaggedResources"
          TrustedAdvisorResourceDetailList.of_json in
      let categorySpecificSummary =
        field_map_exn json "categorySpecificSummary"
          TrustedAdvisorCategorySpecificSummary.of_json in
      let resourcesSummary =
        field_map_exn json "resourcesSummary"
          TrustedAdvisorResourcesSummary.of_json in
      let status = field_map_exn json "status" String_.of_json in
      let timestamp = field_map_exn json "timestamp" String_.of_json in
      let checkId = field_map_exn json "checkId" String_.of_json in
      make ~flaggedResources ~categorySpecificSummary ~resourcesSummary
        ~status ~timestamp ~checkId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The results of a Trusted Advisor check returned by DescribeTrustedAdvisorCheckResult."]
module TrustedAdvisorCheckRefreshStatusList =
  struct
    type nonrec t = TrustedAdvisorCheckRefreshStatus.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TrustedAdvisorCheckRefreshStatus.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:TrustedAdvisorCheckRefreshStatus.of_xml)
    let of_json j =
      list_of_json ~kind:"TrustedAdvisorCheckRefreshStatusList"
        ~of_json:TrustedAdvisorCheckRefreshStatus.of_json j
    let to_json v = composed_to_json to_value v
  end
module SeverityLevelsList =
  struct
    type nonrec t = SeverityLevel.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SeverityLevel.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SeverityLevel.of_xml)
    let of_json j =
      list_of_json ~kind:"SeverityLevelsList" ~of_json:SeverityLevel.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ServiceList =
  struct
    type nonrec t = Service.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Service.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Service.of_xml)
    let of_json j =
      list_of_json ~kind:"ServiceList" ~of_json:Service.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServiceCodeList =
  struct
    type nonrec t = ServiceCode.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:ServiceCode.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ServiceCode.of_xml)
    let of_json j =
      list_of_json ~kind:"ServiceCodeList" ~of_json:ServiceCode.of_json j
    let to_json v = composed_to_json to_value v
  end
module AfterTime =
  struct
    type nonrec t = string
    let context_ = "AfterTime"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AfterTime" j
    let to_json = simple_to_json to_value
  end
module BeforeTime =
  struct
    type nonrec t = string
    let context_ = "BeforeTime"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BeforeTime" j
    let to_json = simple_to_json to_value
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:10));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CaseList =
  struct
    type nonrec t = CaseDetails.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CaseDetails.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CaseDetails.of_xml)
    let of_json j =
      list_of_json ~kind:"CaseList" ~of_json:CaseDetails.of_json j
    let to_json v = composed_to_json to_value v
  end
module CaseIdList =
  struct
    type nonrec t = CaseId.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:CaseId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CaseId.of_xml)
    let of_json j = list_of_json ~kind:"CaseIdList" ~of_json:CaseId.of_json j
    let to_json v = composed_to_json to_value v
  end
module IncludeCommunications =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module IncludeResolvedCases =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module AttachmentIdNotFound =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc
          "An attachment with the specified ID could not be found."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An attachment with the specified ID could not be found."]
module DescribeAttachmentLimitExceeded =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc
          "The limit for the number of DescribeAttachment requests in a short period of time has been exceeded."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The limit for the number of DescribeAttachment requests in a short period of time has been exceeded."]
module AttachmentSetExpired =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc
          "The expiration time of the attachment set has passed. The set expires one hour after it is created."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The expiration time of the attachment set has passed. The set expires 1 hour after it is created."]
module AttachmentSetIdNotFound =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc
          "An attachment set with the specified ID could not be found."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An attachment set with the specified ID could not be found."]
module CaseCreationLimitExceeded =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc
          "An error message that indicates that you have exceeded the number of cases you can have open."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The case creation limit for the account has been exceeded."]
module AttachmentSetId =
  struct
    type nonrec t = string
    let context_ = "AttachmentSetId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttachmentSetId" j
    let to_json = simple_to_json to_value
  end
module IssueType =
  struct
    type nonrec t = string
    let context_ = "IssueType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IssueType" j
    let to_json = simple_to_json to_value
  end
module Result_ =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module AttachmentLimitExceeded =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc
          "The limit for the number of attachment sets created in a short period of time has been exceeded."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The limit for the number of attachment sets created in a short period of time has been exceeded."]
module AttachmentSetSizeLimitExceeded =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc
          "A limit for the size of an attachment set has been exceeded. The limits are three attachments and 5 MB per attachment."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A limit for the size of an attachment set has been exceeded. The limits are three attachments and 5 MB per attachment."]
module ExpiryTime =
  struct
    type nonrec t = string
    let context_ = "ExpiryTime"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExpiryTime" j
    let to_json = simple_to_json to_value
  end
module Attachments =
  struct
    type nonrec t = Attachment.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Attachment.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Attachment.of_xml)
    let of_json j =
      list_of_json ~kind:"Attachments" ~of_json:Attachment.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResolveCaseResponse =
  struct
    type nonrec t =
      {
      initialCaseStatus: CaseStatus.t option
        [@ocaml.doc
          "The status of the case when the ResolveCase request was sent."];
      finalCaseStatus: CaseStatus.t option
        [@ocaml.doc
          "The status of the case after the ResolveCase request was processed."]}
    type nonrec error =
      [ `CaseIdNotFound of CaseIdNotFound.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?initialCaseStatus =
      fun ?finalCaseStatus ->
        fun () -> { initialCaseStatus; finalCaseStatus }
    let error_of_json name json =
      match name with
      | "CaseIdNotFound" -> `CaseIdNotFound (CaseIdNotFound.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "CaseIdNotFound" -> `CaseIdNotFound (CaseIdNotFound.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `CaseIdNotFound e ->
          `Assoc
            [("error", (`String "CaseIdNotFound"));
            ("details", (CaseIdNotFound.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("initialCaseStatus",
           (Option.map x.initialCaseStatus ~f:CaseStatus.to_value));
        ("finalCaseStatus",
          (Option.map x.finalCaseStatus ~f:CaseStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let finalCaseStatus =
        (Option.map ~f:CaseStatus.of_xml)
          (Xml.child xml_arg0 "finalCaseStatus") in
      let initialCaseStatus =
        (Option.map ~f:CaseStatus.of_xml)
          (Xml.child xml_arg0 "initialCaseStatus") in
      make ?finalCaseStatus ?initialCaseStatus ()
    let of_json json =
      let finalCaseStatus =
        field_map json "finalCaseStatus" CaseStatus.of_json in
      let initialCaseStatus =
        field_map json "initialCaseStatus" CaseStatus.of_json in
      make ?finalCaseStatus ?initialCaseStatus ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The status of the case returned by the ResolveCase operation."]
module ResolveCaseRequest =
  struct
    type nonrec t =
      {
      caseId: CaseId.t option
        [@ocaml.doc
          "The support case ID requested or returned in the call. The case ID is an alphanumeric string formatted as shown in this example: case-12345678910-2013-c4c1d2bf33c5cf47"]}
    let make ?caseId = fun () -> { caseId }
    let to_value x =
      structure_to_value
        [("caseId", (Option.map x.caseId ~f:CaseId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let caseId =
        (Option.map ~f:CaseId.of_xml) (Xml.child xml_arg0 "caseId") in
      make ?caseId ()
    let of_json json =
      let caseId = field_map json "caseId" CaseId.of_json in make ?caseId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resolves a support case. This operation takes a caseId and returns the initial and final state of the case. You must have a Business, Enterprise On-Ramp, or Enterprise Support plan to use the Amazon Web Services Support API. If you call the Amazon Web Services Support API from an account that does not have a Business, Enterprise On-Ramp, or Enterprise Support plan, the SubscriptionRequiredException error message appears. For information about changing your support plan, see Amazon Web Services Support."]
module RefreshTrustedAdvisorCheckResponse =
  struct
    type nonrec t =
      {
      status: TrustedAdvisorCheckRefreshStatus.t
        [@ocaml.doc
          "The current refresh status for a check, including the amount of time until the check is eligible for refresh."]}
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "RefreshTrustedAdvisorCheckResponse"
    let make ~status = fun () -> { status }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status",
           (Some (TrustedAdvisorCheckRefreshStatus.to_value x.status)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        TrustedAdvisorCheckRefreshStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "status") in
      make ~status ()
    let of_json json =
      let status =
        field_map_exn json "status" TrustedAdvisorCheckRefreshStatus.of_json in
      make ~status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The current refresh status of a Trusted Advisor check."]
module RefreshTrustedAdvisorCheckRequest =
  struct
    type nonrec t =
      {
      checkId: String_.t
        [@ocaml.doc
          "The unique identifier for the Trusted Advisor check to refresh. Specifying the check ID of a check that is automatically refreshed causes an InvalidParameterValue error."]}
    let context_ = "RefreshTrustedAdvisorCheckRequest"
    let make ~checkId = fun () -> { checkId }
    let to_value x =
      structure_to_value [("checkId", (Some (String_.to_value x.checkId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let checkId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "checkId") in
      make ~checkId ()
    let of_json json =
      let checkId = field_map_exn json "checkId" String_.of_json in
      make ~checkId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTrustedAdvisorChecksResponse =
  struct
    type nonrec t =
      {
      checks: TrustedAdvisorCheckList.t
        [@ocaml.doc
          "Information about all available Trusted Advisor checks."]}
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeTrustedAdvisorChecksResponse"
    let make ~checks = fun () -> { checks }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("checks", (Some (TrustedAdvisorCheckList.to_value x.checks)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let checks =
        TrustedAdvisorCheckList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "checks") in
      make ~checks ()
    let of_json json =
      let checks =
        field_map_exn json "checks" TrustedAdvisorCheckList.of_json in
      make ~checks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the Trusted Advisor checks returned by the DescribeTrustedAdvisorChecks operation."]
module DescribeTrustedAdvisorChecksRequest =
  struct
    type nonrec t =
      {
      language: String_.t
        [@ocaml.doc
          "The ISO 639-1 code for the language in which Amazon Web Services provides support. Amazon Web Services Support currently supports English (\"en\") and Japanese (\"ja\"). Language parameters must be passed explicitly for operations that take them."]}
    let context_ = "DescribeTrustedAdvisorChecksRequest"
    let make ~language = fun () -> { language }
    let to_value x =
      structure_to_value [("language", (Some (String_.to_value x.language)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let language =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "language") in
      make ~language ()
    let of_json json =
      let language = field_map_exn json "language" String_.of_json in
      make ~language ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about all available Trusted Advisor checks, including the name, ID, category, description, and metadata. You must specify a language code. The Amazon Web Services Support API currently supports English (\"en\") and Japanese (\"ja\"). The response contains a TrustedAdvisorCheckDescription object for each check. You must set the Amazon Web Services Region to us-east-1. You must have a Business, Enterprise On-Ramp, or Enterprise Support plan to use the Amazon Web Services Support API. If you call the Amazon Web Services Support API from an account that does not have a Business, Enterprise On-Ramp, or Enterprise Support plan, the SubscriptionRequiredException error message appears. For information about changing your support plan, see Amazon Web Services Support. The names and descriptions for Trusted Advisor checks are subject to change. We recommend that you specify the check ID in your code to uniquely identify a check."]
module DescribeTrustedAdvisorCheckSummariesResponse =
  struct
    type nonrec t =
      {
      summaries: TrustedAdvisorCheckSummaryList.t
        [@ocaml.doc
          "The summary information for the requested Trusted Advisor checks."]}
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeTrustedAdvisorCheckSummariesResponse"
    let make ~summaries = fun () -> { summaries }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("summaries",
           (Some (TrustedAdvisorCheckSummaryList.to_value x.summaries)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let summaries =
        TrustedAdvisorCheckSummaryList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "summaries") in
      make ~summaries ()
    let of_json json =
      let summaries =
        field_map_exn json "summaries" TrustedAdvisorCheckSummaryList.of_json in
      make ~summaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The summaries of the Trusted Advisor checks returned by the DescribeTrustedAdvisorCheckSummaries operation."]
module DescribeTrustedAdvisorCheckSummariesRequest =
  struct
    type nonrec t =
      {
      checkIds: StringList.t
        [@ocaml.doc "The IDs of the Trusted Advisor checks."]}
    let context_ = "DescribeTrustedAdvisorCheckSummariesRequest"
    let make ~checkIds = fun () -> { checkIds }
    let to_value x =
      structure_to_value
        [("checkIds", (Some (StringList.to_value x.checkIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let checkIds =
        StringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "checkIds") in
      make ~checkIds ()
    let of_json json =
      let checkIds = field_map_exn json "checkIds" StringList.of_json in
      make ~checkIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the results for the Trusted Advisor check summaries for the check IDs that you specified. You can get the check IDs by calling the DescribeTrustedAdvisorChecks operation. The response contains an array of TrustedAdvisorCheckSummary objects. You must have a Business, Enterprise On-Ramp, or Enterprise Support plan to use the Amazon Web Services Support API. If you call the Amazon Web Services Support API from an account that does not have a Business, Enterprise On-Ramp, or Enterprise Support plan, the SubscriptionRequiredException error message appears. For information about changing your support plan, see Amazon Web Services Support."]
module DescribeTrustedAdvisorCheckResultResponse =
  struct
    type nonrec t =
      {
      result: TrustedAdvisorCheckResult.t option
        [@ocaml.doc "The detailed results of the Trusted Advisor check."]}
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?result = fun () -> { result }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("result",
           (Option.map x.result ~f:TrustedAdvisorCheckResult.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let result =
        (Option.map ~f:TrustedAdvisorCheckResult.of_xml)
          (Xml.child xml_arg0 "result") in
      make ?result ()
    let of_json json =
      let result = field_map json "result" TrustedAdvisorCheckResult.of_json in
      make ?result ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of the Trusted Advisor check returned by the DescribeTrustedAdvisorCheckResult operation."]
module DescribeTrustedAdvisorCheckResultRequest =
  struct
    type nonrec t =
      {
      checkId: String_.t
        [@ocaml.doc "The unique identifier for the Trusted Advisor check."];
      language: String_.t option
        [@ocaml.doc
          "The ISO 639-1 code for the language in which Amazon Web Services provides support. Amazon Web Services Support currently supports English (\"en\") and Japanese (\"ja\"). Language parameters must be passed explicitly for operations that take them."]}
    let context_ = "DescribeTrustedAdvisorCheckResultRequest"
    let make ?language = fun ~checkId -> fun () -> { language; checkId }
    let to_value x =
      structure_to_value
        [("checkId", (Some (String_.to_value x.checkId)));
        ("language", (Option.map x.language ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let language =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "language") in
      let checkId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "checkId") in
      make ?language ~checkId ()
    let of_json json =
      let language = field_map json "language" String_.of_json in
      let checkId = field_map_exn json "checkId" String_.of_json in
      make ?language ~checkId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTrustedAdvisorCheckRefreshStatusesResponse =
  struct
    type nonrec t =
      {
      statuses: TrustedAdvisorCheckRefreshStatusList.t
        [@ocaml.doc
          "The refresh status of the specified Trusted Advisor checks."]}
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeTrustedAdvisorCheckRefreshStatusesResponse"
    let make ~statuses = fun () -> { statuses }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("statuses",
           (Some (TrustedAdvisorCheckRefreshStatusList.to_value x.statuses)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statuses =
        TrustedAdvisorCheckRefreshStatusList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "statuses") in
      make ~statuses ()
    let of_json json =
      let statuses =
        field_map_exn json "statuses"
          TrustedAdvisorCheckRefreshStatusList.of_json in
      make ~statuses ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The statuses of the Trusted Advisor checks returned by the DescribeTrustedAdvisorCheckRefreshStatuses operation."]
module DescribeTrustedAdvisorCheckRefreshStatusesRequest =
  struct
    type nonrec t =
      {
      checkIds: StringList.t
        [@ocaml.doc
          "The IDs of the Trusted Advisor checks to get the status. If you specify the check ID of a check that is automatically refreshed, you might see an InvalidParameterValue error."]}
    let context_ = "DescribeTrustedAdvisorCheckRefreshStatusesRequest"
    let make ~checkIds = fun () -> { checkIds }
    let to_value x =
      structure_to_value
        [("checkIds", (Some (StringList.to_value x.checkIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let checkIds =
        StringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "checkIds") in
      make ~checkIds ()
    let of_json json =
      let checkIds = field_map_exn json "checkIds" StringList.of_json in
      make ~checkIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the refresh status of the Trusted Advisor checks that have the specified check IDs. You can get the check IDs by calling the DescribeTrustedAdvisorChecks operation. Some checks are refreshed automatically, and you can't return their refresh statuses by using the DescribeTrustedAdvisorCheckRefreshStatuses operation. If you call this operation for these checks, you might see an InvalidParameterValue error. You must have a Business, Enterprise On-Ramp, or Enterprise Support plan to use the Amazon Web Services Support API. If you call the Amazon Web Services Support API from an account that does not have a Business, Enterprise On-Ramp, or Enterprise Support plan, the SubscriptionRequiredException error message appears. For information about changing your support plan, see Amazon Web Services Support."]
module DescribeSeverityLevelsResponse =
  struct
    type nonrec t =
      {
      severityLevels: SeverityLevelsList.t option
        [@ocaml.doc
          "The available severity levels for the support case. Available severity levels are defined by your service level agreement with Amazon Web Services."]}
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?severityLevels = fun () -> { severityLevels }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("severityLevels",
           (Option.map x.severityLevels ~f:SeverityLevelsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let severityLevels =
        (Option.map ~f:SeverityLevelsList.of_xml)
          (Xml.child xml_arg0 "severityLevels") in
      make ?severityLevels ()
    let of_json json =
      let severityLevels =
        field_map json "severityLevels" SeverityLevelsList.of_json in
      make ?severityLevels ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The list of severity levels returned by the DescribeSeverityLevels operation."]
module DescribeSeverityLevelsRequest =
  struct
    type nonrec t =
      {
      language: Language.t option
        [@ocaml.doc
          "The ISO 639-1 code for the language in which Amazon Web Services provides support. Amazon Web Services Support currently supports English (\"en\") and Japanese (\"ja\"). Language parameters must be passed explicitly for operations that take them."]}
    let make ?language = fun () -> { language }
    let to_value x =
      structure_to_value
        [("language", (Option.map x.language ~f:Language.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let language =
        (Option.map ~f:Language.of_xml) (Xml.child xml_arg0 "language") in
      make ?language ()
    let of_json json =
      let language = field_map json "language" Language.of_json in
      make ?language ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the list of severity levels that you can assign to a support case. The severity level for a case is also a field in the CaseDetails data type that you include for a CreateCase request. You must have a Business, Enterprise On-Ramp, or Enterprise Support plan to use the Amazon Web Services Support API. If you call the Amazon Web Services Support API from an account that does not have a Business, Enterprise On-Ramp, or Enterprise Support plan, the SubscriptionRequiredException error message appears. For information about changing your support plan, see Amazon Web Services Support."]
module DescribeServicesResponse =
  struct
    type nonrec t =
      {
      services: ServiceList.t option
        [@ocaml.doc "A JSON-formatted list of Amazon Web Services services."]}
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?services = fun () -> { services }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("services", (Option.map x.services ~f:ServiceList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let services =
        (Option.map ~f:ServiceList.of_xml) (Xml.child xml_arg0 "services") in
      make ?services ()
    let of_json json =
      let services = field_map json "services" ServiceList.of_json in
      make ?services ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The list of Amazon Web Services services returned by the DescribeServices operation."]
module DescribeServicesRequest =
  struct
    type nonrec t =
      {
      serviceCodeList: ServiceCodeList.t option
        [@ocaml.doc
          "A JSON-formatted list of service codes available for Amazon Web Services services."];
      language: Language.t option
        [@ocaml.doc
          "The ISO 639-1 code for the language in which Amazon Web Services provides support. Amazon Web Services Support currently supports English (\"en\") and Japanese (\"ja\"). Language parameters must be passed explicitly for operations that take them."]}
    let make ?serviceCodeList =
      fun ?language -> fun () -> { serviceCodeList; language }
    let to_value x =
      structure_to_value
        [("serviceCodeList",
           (Option.map x.serviceCodeList ~f:ServiceCodeList.to_value));
        ("language", (Option.map x.language ~f:Language.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let language =
        (Option.map ~f:Language.of_xml) (Xml.child xml_arg0 "language") in
      let serviceCodeList =
        (Option.map ~f:ServiceCodeList.of_xml)
          (Xml.child xml_arg0 "serviceCodeList") in
      make ?language ?serviceCodeList ()
    let of_json json =
      let language = field_map json "language" Language.of_json in
      let serviceCodeList =
        field_map json "serviceCodeList" ServiceCodeList.of_json in
      make ?language ?serviceCodeList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the current list of Amazon Web Services services and a list of service categories for each service. You then use service names and categories in your CreateCase requests. Each Amazon Web Services service has its own set of categories. The service codes and category codes correspond to the values that appear in the Service and Category lists on the Amazon Web Services Support Center Create Case page. The values in those fields don't necessarily match the service codes and categories returned by the DescribeServices operation. Always use the service codes and categories that the DescribeServices operation returns, so that you have the most recent set of service and category codes. You must have a Business, Enterprise On-Ramp, or Enterprise Support plan to use the Amazon Web Services Support API. If you call the Amazon Web Services Support API from an account that does not have a Business, Enterprise On-Ramp, or Enterprise Support plan, the SubscriptionRequiredException error message appears. For information about changing your support plan, see Amazon Web Services Support."]
module DescribeCommunicationsResponse =
  struct
    type nonrec t =
      {
      communications: CommunicationList.t option
        [@ocaml.doc "The communications for the case."];
      nextToken: NextToken.t option
        [@ocaml.doc "A resumption point for pagination."]}
    type nonrec error =
      [ `CaseIdNotFound of CaseIdNotFound.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?communications =
      fun ?nextToken -> fun () -> { communications; nextToken }
    let error_of_json name json =
      match name with
      | "CaseIdNotFound" -> `CaseIdNotFound (CaseIdNotFound.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "CaseIdNotFound" -> `CaseIdNotFound (CaseIdNotFound.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `CaseIdNotFound e ->
          `Assoc
            [("error", (`String "CaseIdNotFound"));
            ("details", (CaseIdNotFound.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("communications",
           (Option.map x.communications ~f:CommunicationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let communications =
        (Option.map ~f:CommunicationList.of_xml)
          (Xml.child xml_arg0 "communications") in
      make ?nextToken ?communications ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let communications =
        field_map json "communications" CommunicationList.of_json in
      make ?nextToken ?communications ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The communications returned by the DescribeCommunications operation."]
module DescribeCommunicationsRequest =
  struct
    type nonrec t =
      {
      caseId: CaseId.t
        [@ocaml.doc
          "The support case ID requested or returned in the call. The case ID is an alphanumeric string formatted as shown in this example: case-12345678910-2013-c4c1d2bf33c5cf47"];
      beforeTime: BeforeTime.t option
        [@ocaml.doc
          "The end date for a filtered date search on support case communications. Case communications are available for 12 months after creation."];
      afterTime: AfterTime.t option
        [@ocaml.doc
          "The start date for a filtered date search on support case communications. Case communications are available for 12 months after creation."];
      nextToken: NextToken.t option
        [@ocaml.doc "A resumption point for pagination."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return before paginating."]}
    let context_ = "DescribeCommunicationsRequest"
    let make ?beforeTime =
      fun ?afterTime ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ~caseId ->
              fun () ->
                { beforeTime; afterTime; nextToken; maxResults; caseId }
    let to_value x =
      structure_to_value
        [("caseId", (Some (CaseId.to_value x.caseId)));
        ("beforeTime", (Option.map x.beforeTime ~f:BeforeTime.to_value));
        ("afterTime", (Option.map x.afterTime ~f:AfterTime.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let afterTime =
        (Option.map ~f:AfterTime.of_xml) (Xml.child xml_arg0 "afterTime") in
      let beforeTime =
        (Option.map ~f:BeforeTime.of_xml) (Xml.child xml_arg0 "beforeTime") in
      let caseId =
        CaseId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "caseId") in
      make ?maxResults ?nextToken ?afterTime ?beforeTime ~caseId ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let afterTime = field_map json "afterTime" AfterTime.of_json in
      let beforeTime = field_map json "beforeTime" BeforeTime.of_json in
      let caseId = field_map_exn json "caseId" CaseId.of_json in
      make ?maxResults ?nextToken ?afterTime ?beforeTime ~caseId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns communications and attachments for one or more support cases. Use the afterTime and beforeTime parameters to filter by date. You can use the caseId parameter to restrict the results to a specific case. Case data is available for 12 months after creation. If a case was created more than 12 months ago, a request for data might cause an error. You can use the maxResults and nextToken parameters to control the pagination of the results. Set maxResults to the number of cases that you want to display on each page, and use nextToken to specify the resumption of pagination. You must have a Business, Enterprise On-Ramp, or Enterprise Support plan to use the Amazon Web Services Support API. If you call the Amazon Web Services Support API from an account that does not have a Business, Enterprise On-Ramp, or Enterprise Support plan, the SubscriptionRequiredException error message appears. For information about changing your support plan, see Amazon Web Services Support."]
module DescribeCasesResponse =
  struct
    type nonrec t =
      {
      cases: CaseList.t option
        [@ocaml.doc "The details for the cases that match the request."];
      nextToken: NextToken.t option
        [@ocaml.doc "A resumption point for pagination."]}
    type nonrec error =
      [ `CaseIdNotFound of CaseIdNotFound.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?cases = fun ?nextToken -> fun () -> { cases; nextToken }
    let error_of_json name json =
      match name with
      | "CaseIdNotFound" -> `CaseIdNotFound (CaseIdNotFound.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "CaseIdNotFound" -> `CaseIdNotFound (CaseIdNotFound.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `CaseIdNotFound e ->
          `Assoc
            [("error", (`String "CaseIdNotFound"));
            ("details", (CaseIdNotFound.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("cases", (Option.map x.cases ~f:CaseList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let cases =
        (Option.map ~f:CaseList.of_xml) (Xml.child xml_arg0 "cases") in
      make ?nextToken ?cases ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let cases = field_map json "cases" CaseList.of_json in
      make ?nextToken ?cases ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns an array of CaseDetails objects and a nextToken that defines a point for pagination in the result set."]
module DescribeCasesRequest =
  struct
    type nonrec t =
      {
      caseIdList: CaseIdList.t option
        [@ocaml.doc
          "A list of ID numbers of the support cases you want returned. The maximum number of cases is 100."];
      displayId: DisplayId.t option
        [@ocaml.doc
          "The ID displayed for a case in the Amazon Web Services Support Center user interface."];
      afterTime: AfterTime.t option
        [@ocaml.doc
          "The start date for a filtered date search on support case communications. Case communications are available for 12 months after creation."];
      beforeTime: BeforeTime.t option
        [@ocaml.doc
          "The end date for a filtered date search on support case communications. Case communications are available for 12 months after creation."];
      includeResolvedCases: IncludeResolvedCases.t option
        [@ocaml.doc
          "Specifies whether to include resolved support cases in the DescribeCases response. By default, resolved cases aren't included."];
      nextToken: NextToken.t option
        [@ocaml.doc "A resumption point for pagination."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return before paginating."];
      language: Language.t option
        [@ocaml.doc
          "The ISO 639-1 code for the language in which Amazon Web Services provides support. Amazon Web Services Support currently supports English (\"en\") and Japanese (\"ja\"). Language parameters must be passed explicitly for operations that take them."];
      includeCommunications: IncludeCommunications.t option
        [@ocaml.doc
          "Specifies whether to include communications in the DescribeCases response. By default, communications are included."]}
    let make ?caseIdList =
      fun ?displayId ->
        fun ?afterTime ->
          fun ?beforeTime ->
            fun ?includeResolvedCases ->
              fun ?nextToken ->
                fun ?maxResults ->
                  fun ?language ->
                    fun ?includeCommunications ->
                      fun () ->
                        {
                          caseIdList;
                          displayId;
                          afterTime;
                          beforeTime;
                          includeResolvedCases;
                          nextToken;
                          maxResults;
                          language;
                          includeCommunications
                        }
    let to_value x =
      structure_to_value
        [("caseIdList", (Option.map x.caseIdList ~f:CaseIdList.to_value));
        ("displayId", (Option.map x.displayId ~f:DisplayId.to_value));
        ("afterTime", (Option.map x.afterTime ~f:AfterTime.to_value));
        ("beforeTime", (Option.map x.beforeTime ~f:BeforeTime.to_value));
        ("includeResolvedCases",
          (Option.map x.includeResolvedCases ~f:IncludeResolvedCases.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("language", (Option.map x.language ~f:Language.to_value));
        ("includeCommunications",
          (Option.map x.includeCommunications
             ~f:IncludeCommunications.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let includeCommunications =
        (Option.map ~f:IncludeCommunications.of_xml)
          (Xml.child xml_arg0 "includeCommunications") in
      let language =
        (Option.map ~f:Language.of_xml) (Xml.child xml_arg0 "language") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let includeResolvedCases =
        (Option.map ~f:IncludeResolvedCases.of_xml)
          (Xml.child xml_arg0 "includeResolvedCases") in
      let beforeTime =
        (Option.map ~f:BeforeTime.of_xml) (Xml.child xml_arg0 "beforeTime") in
      let afterTime =
        (Option.map ~f:AfterTime.of_xml) (Xml.child xml_arg0 "afterTime") in
      let displayId =
        (Option.map ~f:DisplayId.of_xml) (Xml.child xml_arg0 "displayId") in
      let caseIdList =
        (Option.map ~f:CaseIdList.of_xml) (Xml.child xml_arg0 "caseIdList") in
      make ?includeCommunications ?language ?maxResults ?nextToken
        ?includeResolvedCases ?beforeTime ?afterTime ?displayId ?caseIdList
        ()
    let of_json json =
      let includeCommunications =
        field_map json "includeCommunications" IncludeCommunications.of_json in
      let language = field_map json "language" Language.of_json in
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let includeResolvedCases =
        field_map json "includeResolvedCases" IncludeResolvedCases.of_json in
      let beforeTime = field_map json "beforeTime" BeforeTime.of_json in
      let afterTime = field_map json "afterTime" AfterTime.of_json in
      let displayId = field_map json "displayId" DisplayId.of_json in
      let caseIdList = field_map json "caseIdList" CaseIdList.of_json in
      make ?includeCommunications ?language ?maxResults ?nextToken
        ?includeResolvedCases ?beforeTime ?afterTime ?displayId ?caseIdList
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of cases that you specify by passing one or more case IDs. You can use the afterTime and beforeTime parameters to filter the cases by date. You can set values for the includeResolvedCases and includeCommunications parameters to specify how much information to return. The response returns the following in JSON format: One or more CaseDetails data types. One or more nextToken values, which specify where to paginate the returned records represented by the CaseDetails objects. Case data is available for 12 months after creation. If a case was created more than 12 months ago, a request might return an error. You must have a Business, Enterprise On-Ramp, or Enterprise Support plan to use the Amazon Web Services Support API. If you call the Amazon Web Services Support API from an account that does not have a Business, Enterprise On-Ramp, or Enterprise Support plan, the SubscriptionRequiredException error message appears. For information about changing your support plan, see Amazon Web Services Support."]
module DescribeAttachmentResponse =
  struct
    type nonrec t =
      {
      attachment: Attachment.t option
        [@ocaml.doc
          "This object includes the attachment content and file name. In the previous response syntax, the value for the data parameter appears as blob, which is represented as a base64-encoded string. The value for fileName is the name of the attachment, such as troubleshoot-screenshot.png."]}
    type nonrec error =
      [ `AttachmentIdNotFound of AttachmentIdNotFound.t 
      | `DescribeAttachmentLimitExceeded of DescribeAttachmentLimitExceeded.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?attachment = fun () -> { attachment }
    let error_of_json name json =
      match name with
      | "AttachmentIdNotFound" ->
          `AttachmentIdNotFound (AttachmentIdNotFound.of_json json)
      | "DescribeAttachmentLimitExceeded" ->
          `DescribeAttachmentLimitExceeded
            (DescribeAttachmentLimitExceeded.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AttachmentIdNotFound" ->
          `AttachmentIdNotFound (AttachmentIdNotFound.of_xml xml)
      | "DescribeAttachmentLimitExceeded" ->
          `DescribeAttachmentLimitExceeded
            (DescribeAttachmentLimitExceeded.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AttachmentIdNotFound e ->
          `Assoc
            [("error", (`String "AttachmentIdNotFound"));
            ("details", (AttachmentIdNotFound.to_json e))]
      | `DescribeAttachmentLimitExceeded e ->
          `Assoc
            [("error", (`String "DescribeAttachmentLimitExceeded"));
            ("details", (DescribeAttachmentLimitExceeded.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("attachment", (Option.map x.attachment ~f:Attachment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attachment =
        (Option.map ~f:Attachment.of_xml) (Xml.child xml_arg0 "attachment") in
      make ?attachment ()
    let of_json json =
      let attachment = field_map json "attachment" Attachment.of_json in
      make ?attachment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The content and file name of the attachment returned by the DescribeAttachment operation."]
module DescribeAttachmentRequest =
  struct
    type nonrec t =
      {
      attachmentId: AttachmentId.t
        [@ocaml.doc
          "The ID of the attachment to return. Attachment IDs are returned by the DescribeCommunications operation."]}
    let context_ = "DescribeAttachmentRequest"
    let make ~attachmentId = fun () -> { attachmentId }
    let to_value x =
      structure_to_value
        [("attachmentId", (Some (AttachmentId.to_value x.attachmentId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attachmentId =
        AttachmentId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "attachmentId") in
      make ~attachmentId ()
    let of_json json =
      let attachmentId =
        field_map_exn json "attachmentId" AttachmentId.of_json in
      make ~attachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the attachment that has the specified ID. Attachments can include screenshots, error logs, or other files that describe your issue. Attachment IDs are generated by the case management system when you add an attachment to a case or case communication. Attachment IDs are returned in the AttachmentDetails objects that are returned by the DescribeCommunications operation. You must have a Business, Enterprise On-Ramp, or Enterprise Support plan to use the Amazon Web Services Support API. If you call the Amazon Web Services Support API from an account that does not have a Business, Enterprise On-Ramp, or Enterprise Support plan, the SubscriptionRequiredException error message appears. For information about changing your support plan, see Amazon Web Services Support."]
module CreateCaseResponse =
  struct
    type nonrec t =
      {
      caseId: CaseId.t option
        [@ocaml.doc
          "The support case ID requested or returned in the call. The case ID is an alphanumeric string in the following format: case-12345678910-2013-c4c1d2bf33c5cf47"]}
    type nonrec error =
      [ `AttachmentSetExpired of AttachmentSetExpired.t 
      | `AttachmentSetIdNotFound of AttachmentSetIdNotFound.t 
      | `CaseCreationLimitExceeded of CaseCreationLimitExceeded.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?caseId = fun () -> { caseId }
    let error_of_json name json =
      match name with
      | "AttachmentSetExpired" ->
          `AttachmentSetExpired (AttachmentSetExpired.of_json json)
      | "AttachmentSetIdNotFound" ->
          `AttachmentSetIdNotFound (AttachmentSetIdNotFound.of_json json)
      | "CaseCreationLimitExceeded" ->
          `CaseCreationLimitExceeded (CaseCreationLimitExceeded.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AttachmentSetExpired" ->
          `AttachmentSetExpired (AttachmentSetExpired.of_xml xml)
      | "AttachmentSetIdNotFound" ->
          `AttachmentSetIdNotFound (AttachmentSetIdNotFound.of_xml xml)
      | "CaseCreationLimitExceeded" ->
          `CaseCreationLimitExceeded (CaseCreationLimitExceeded.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AttachmentSetExpired e ->
          `Assoc
            [("error", (`String "AttachmentSetExpired"));
            ("details", (AttachmentSetExpired.to_json e))]
      | `AttachmentSetIdNotFound e ->
          `Assoc
            [("error", (`String "AttachmentSetIdNotFound"));
            ("details", (AttachmentSetIdNotFound.to_json e))]
      | `CaseCreationLimitExceeded e ->
          `Assoc
            [("error", (`String "CaseCreationLimitExceeded"));
            ("details", (CaseCreationLimitExceeded.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("caseId", (Option.map x.caseId ~f:CaseId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let caseId =
        (Option.map ~f:CaseId.of_xml) (Xml.child xml_arg0 "caseId") in
      make ?caseId ()
    let of_json json =
      let caseId = field_map json "caseId" CaseId.of_json in make ?caseId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The support case ID returned by a successful completion of the CreateCase operation."]
module CreateCaseRequest =
  struct
    type nonrec t =
      {
      subject: Subject.t
        [@ocaml.doc
          "The title of the support case. The title appears in the Subject field on the Amazon Web Services Support Center Create Case page."];
      serviceCode: ServiceCode.t option
        [@ocaml.doc
          "The code for the Amazon Web Services service. You can use the DescribeServices operation to get the possible serviceCode values."];
      severityCode: SeverityCode.t option
        [@ocaml.doc
          "A value that indicates the urgency of the case. This value determines the response time according to your service level agreement with Amazon Web Services Support. You can use the DescribeSeverityLevels operation to get the possible values for severityCode. For more information, see SeverityLevel and Choosing a Severity in the Amazon Web Services Support User Guide. The availability of severity levels depends on the support plan for the Amazon Web Services account."];
      categoryCode: CategoryCode.t option
        [@ocaml.doc
          "The category of problem for the support case. You also use the DescribeServices operation to get the category code for a service. Each Amazon Web Services service defines its own set of category codes."];
      communicationBody: CommunicationBody.t
        [@ocaml.doc
          "The communication body text that describes the issue. This text appears in the Description field on the Amazon Web Services Support Center Create Case page."];
      ccEmailAddresses: CcEmailAddressList.t option
        [@ocaml.doc
          "A list of email addresses that Amazon Web Services Support copies on case correspondence. Amazon Web Services Support identifies the account that creates the case when you specify your Amazon Web Services credentials in an HTTP POST method or use the Amazon Web Services SDKs."];
      language: Language.t option
        [@ocaml.doc
          "The language in which Amazon Web Services Support handles the case. You must specify the ISO 639-1 code for the language parameter if you want support in that language. Currently, English (\"en\") and Japanese (\"ja\") are supported."];
      issueType: IssueType.t option
        [@ocaml.doc
          "The type of issue for the case. You can specify customer-service or technical. If you don't specify a value, the default is technical."];
      attachmentSetId: AttachmentSetId.t option
        [@ocaml.doc
          "The ID of a set of one or more attachments for the case. Create the set by using the AddAttachmentsToSet operation."]}
    let context_ = "CreateCaseRequest"
    let make ?serviceCode =
      fun ?severityCode ->
        fun ?categoryCode ->
          fun ?ccEmailAddresses ->
            fun ?language ->
              fun ?issueType ->
                fun ?attachmentSetId ->
                  fun ~subject ->
                    fun ~communicationBody ->
                      fun () ->
                        {
                          serviceCode;
                          severityCode;
                          categoryCode;
                          ccEmailAddresses;
                          language;
                          issueType;
                          attachmentSetId;
                          subject;
                          communicationBody
                        }
    let to_value x =
      structure_to_value
        [("subject", (Some (Subject.to_value x.subject)));
        ("serviceCode", (Option.map x.serviceCode ~f:ServiceCode.to_value));
        ("severityCode",
          (Option.map x.severityCode ~f:SeverityCode.to_value));
        ("categoryCode",
          (Option.map x.categoryCode ~f:CategoryCode.to_value));
        ("communicationBody",
          (Some (CommunicationBody.to_value x.communicationBody)));
        ("ccEmailAddresses",
          (Option.map x.ccEmailAddresses ~f:CcEmailAddressList.to_value));
        ("language", (Option.map x.language ~f:Language.to_value));
        ("issueType", (Option.map x.issueType ~f:IssueType.to_value));
        ("attachmentSetId",
          (Option.map x.attachmentSetId ~f:AttachmentSetId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attachmentSetId =
        (Option.map ~f:AttachmentSetId.of_xml)
          (Xml.child xml_arg0 "attachmentSetId") in
      let issueType =
        (Option.map ~f:IssueType.of_xml) (Xml.child xml_arg0 "issueType") in
      let language =
        (Option.map ~f:Language.of_xml) (Xml.child xml_arg0 "language") in
      let ccEmailAddresses =
        (Option.map ~f:CcEmailAddressList.of_xml)
          (Xml.child xml_arg0 "ccEmailAddresses") in
      let communicationBody =
        CommunicationBody.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "communicationBody") in
      let categoryCode =
        (Option.map ~f:CategoryCode.of_xml)
          (Xml.child xml_arg0 "categoryCode") in
      let severityCode =
        (Option.map ~f:SeverityCode.of_xml)
          (Xml.child xml_arg0 "severityCode") in
      let serviceCode =
        (Option.map ~f:ServiceCode.of_xml) (Xml.child xml_arg0 "serviceCode") in
      let subject =
        Subject.of_xml (Xml.child_exn ~context:context_ xml_arg0 "subject") in
      make ?attachmentSetId ?issueType ?language ?ccEmailAddresses
        ~communicationBody ?categoryCode ?severityCode ?serviceCode ~subject
        ()
    let of_json json =
      let attachmentSetId =
        field_map json "attachmentSetId" AttachmentSetId.of_json in
      let issueType = field_map json "issueType" IssueType.of_json in
      let language = field_map json "language" Language.of_json in
      let ccEmailAddresses =
        field_map json "ccEmailAddresses" CcEmailAddressList.of_json in
      let communicationBody =
        field_map_exn json "communicationBody" CommunicationBody.of_json in
      let categoryCode = field_map json "categoryCode" CategoryCode.of_json in
      let severityCode = field_map json "severityCode" SeverityCode.of_json in
      let serviceCode = field_map json "serviceCode" ServiceCode.of_json in
      let subject = field_map_exn json "subject" Subject.of_json in
      make ?attachmentSetId ?issueType ?language ?ccEmailAddresses
        ~communicationBody ?categoryCode ?severityCode ?serviceCode ~subject
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a case in the Amazon Web Services Support Center. This operation is similar to how you create a case in the Amazon Web Services Support Center Create Case page. The Amazon Web Services Support API doesn't support requesting service limit increases. You can submit a service limit increase in the following ways: Submit a request from the Amazon Web Services Support Center Create Case page. Use the Service Quotas RequestServiceQuotaIncrease operation. A successful CreateCase request returns an Amazon Web Services Support case number. You can use the DescribeCases operation and specify the case number to get existing Amazon Web Services Support cases. After you create a case, use the AddCommunicationToCase operation to add additional communication or attachments to an existing case. The caseId is separate from the displayId that appears in the Amazon Web Services Support Center. Use the DescribeCases operation to get the displayId. You must have a Business, Enterprise On-Ramp, or Enterprise Support plan to use the Amazon Web Services Support API. If you call the Amazon Web Services Support API from an account that does not have a Business, Enterprise On-Ramp, or Enterprise Support plan, the SubscriptionRequiredException error message appears. For information about changing your support plan, see Amazon Web Services Support."]
module AddCommunicationToCaseResponse =
  struct
    type nonrec t =
      {
      result: Result_.t option
        [@ocaml.doc
          "True if AddCommunicationToCase succeeds. Otherwise, returns an error."]}
    type nonrec error =
      [ `AttachmentSetExpired of AttachmentSetExpired.t 
      | `AttachmentSetIdNotFound of AttachmentSetIdNotFound.t 
      | `CaseIdNotFound of CaseIdNotFound.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?result = fun () -> { result }
    let error_of_json name json =
      match name with
      | "AttachmentSetExpired" ->
          `AttachmentSetExpired (AttachmentSetExpired.of_json json)
      | "AttachmentSetIdNotFound" ->
          `AttachmentSetIdNotFound (AttachmentSetIdNotFound.of_json json)
      | "CaseIdNotFound" -> `CaseIdNotFound (CaseIdNotFound.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AttachmentSetExpired" ->
          `AttachmentSetExpired (AttachmentSetExpired.of_xml xml)
      | "AttachmentSetIdNotFound" ->
          `AttachmentSetIdNotFound (AttachmentSetIdNotFound.of_xml xml)
      | "CaseIdNotFound" -> `CaseIdNotFound (CaseIdNotFound.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AttachmentSetExpired e ->
          `Assoc
            [("error", (`String "AttachmentSetExpired"));
            ("details", (AttachmentSetExpired.to_json e))]
      | `AttachmentSetIdNotFound e ->
          `Assoc
            [("error", (`String "AttachmentSetIdNotFound"));
            ("details", (AttachmentSetIdNotFound.to_json e))]
      | `CaseIdNotFound e ->
          `Assoc
            [("error", (`String "CaseIdNotFound"));
            ("details", (CaseIdNotFound.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("result", (Option.map x.result ~f:Result_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let result =
        (Option.map ~f:Result_.of_xml) (Xml.child xml_arg0 "result") in
      make ?result ()
    let of_json json =
      let result = field_map json "result" Result_.of_json in make ?result ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The result of the AddCommunicationToCase operation."]
module AddCommunicationToCaseRequest =
  struct
    type nonrec t =
      {
      caseId: CaseId.t option
        [@ocaml.doc
          "The support case ID requested or returned in the call. The case ID is an alphanumeric string formatted as shown in this example: case-12345678910-2013-c4c1d2bf33c5cf47"];
      communicationBody: CommunicationBody.t
        [@ocaml.doc
          "The body of an email communication to add to the support case."];
      ccEmailAddresses: CcEmailAddressList.t option
        [@ocaml.doc
          "The email addresses in the CC line of an email to be added to the support case."];
      attachmentSetId: AttachmentSetId.t option
        [@ocaml.doc
          "The ID of a set of one or more attachments for the communication to add to the case. Create the set by calling AddAttachmentsToSet"]}
    let context_ = "AddCommunicationToCaseRequest"
    let make ?caseId =
      fun ?ccEmailAddresses ->
        fun ?attachmentSetId ->
          fun ~communicationBody ->
            fun () ->
              { caseId; ccEmailAddresses; attachmentSetId; communicationBody
              }
    let to_value x =
      structure_to_value
        [("caseId", (Option.map x.caseId ~f:CaseId.to_value));
        ("communicationBody",
          (Some (CommunicationBody.to_value x.communicationBody)));
        ("ccEmailAddresses",
          (Option.map x.ccEmailAddresses ~f:CcEmailAddressList.to_value));
        ("attachmentSetId",
          (Option.map x.attachmentSetId ~f:AttachmentSetId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attachmentSetId =
        (Option.map ~f:AttachmentSetId.of_xml)
          (Xml.child xml_arg0 "attachmentSetId") in
      let ccEmailAddresses =
        (Option.map ~f:CcEmailAddressList.of_xml)
          (Xml.child xml_arg0 "ccEmailAddresses") in
      let communicationBody =
        CommunicationBody.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "communicationBody") in
      let caseId =
        (Option.map ~f:CaseId.of_xml) (Xml.child xml_arg0 "caseId") in
      make ?attachmentSetId ?ccEmailAddresses ~communicationBody ?caseId ()
    let of_json json =
      let attachmentSetId =
        field_map json "attachmentSetId" AttachmentSetId.of_json in
      let ccEmailAddresses =
        field_map json "ccEmailAddresses" CcEmailAddressList.of_json in
      let communicationBody =
        field_map_exn json "communicationBody" CommunicationBody.of_json in
      let caseId = field_map json "caseId" CaseId.of_json in
      make ?attachmentSetId ?ccEmailAddresses ~communicationBody ?caseId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds additional customer communication to an Amazon Web Services Support case. Use the caseId parameter to identify the case to which to add communication. You can list a set of email addresses to copy on the communication by using the ccEmailAddresses parameter. The communicationBody value contains the text of the communication. You must have a Business, Enterprise On-Ramp, or Enterprise Support plan to use the Amazon Web Services Support API. If you call the Amazon Web Services Support API from an account that does not have a Business, Enterprise On-Ramp, or Enterprise Support plan, the SubscriptionRequiredException error message appears. For information about changing your support plan, see Amazon Web Services Support."]
module AddAttachmentsToSetResponse =
  struct
    type nonrec t =
      {
      attachmentSetId: AttachmentSetId.t option
        [@ocaml.doc
          "The ID of the attachment set. If an attachmentSetId was not specified, a new attachment set is created, and the ID of the set is returned in the response. If an attachmentSetId was specified, the attachments are added to the specified set, if it exists."];
      expiryTime: ExpiryTime.t option
        [@ocaml.doc "The time and date when the attachment set expires."]}
    type nonrec error =
      [ `AttachmentLimitExceeded of AttachmentLimitExceeded.t 
      | `AttachmentSetExpired of AttachmentSetExpired.t 
      | `AttachmentSetIdNotFound of AttachmentSetIdNotFound.t 
      | `AttachmentSetSizeLimitExceeded of AttachmentSetSizeLimitExceeded.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?attachmentSetId =
      fun ?expiryTime -> fun () -> { attachmentSetId; expiryTime }
    let error_of_json name json =
      match name with
      | "AttachmentLimitExceeded" ->
          `AttachmentLimitExceeded (AttachmentLimitExceeded.of_json json)
      | "AttachmentSetExpired" ->
          `AttachmentSetExpired (AttachmentSetExpired.of_json json)
      | "AttachmentSetIdNotFound" ->
          `AttachmentSetIdNotFound (AttachmentSetIdNotFound.of_json json)
      | "AttachmentSetSizeLimitExceeded" ->
          `AttachmentSetSizeLimitExceeded
            (AttachmentSetSizeLimitExceeded.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AttachmentLimitExceeded" ->
          `AttachmentLimitExceeded (AttachmentLimitExceeded.of_xml xml)
      | "AttachmentSetExpired" ->
          `AttachmentSetExpired (AttachmentSetExpired.of_xml xml)
      | "AttachmentSetIdNotFound" ->
          `AttachmentSetIdNotFound (AttachmentSetIdNotFound.of_xml xml)
      | "AttachmentSetSizeLimitExceeded" ->
          `AttachmentSetSizeLimitExceeded
            (AttachmentSetSizeLimitExceeded.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AttachmentLimitExceeded e ->
          `Assoc
            [("error", (`String "AttachmentLimitExceeded"));
            ("details", (AttachmentLimitExceeded.to_json e))]
      | `AttachmentSetExpired e ->
          `Assoc
            [("error", (`String "AttachmentSetExpired"));
            ("details", (AttachmentSetExpired.to_json e))]
      | `AttachmentSetIdNotFound e ->
          `Assoc
            [("error", (`String "AttachmentSetIdNotFound"));
            ("details", (AttachmentSetIdNotFound.to_json e))]
      | `AttachmentSetSizeLimitExceeded e ->
          `Assoc
            [("error", (`String "AttachmentSetSizeLimitExceeded"));
            ("details", (AttachmentSetSizeLimitExceeded.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("attachmentSetId",
           (Option.map x.attachmentSetId ~f:AttachmentSetId.to_value));
        ("expiryTime", (Option.map x.expiryTime ~f:ExpiryTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expiryTime =
        (Option.map ~f:ExpiryTime.of_xml) (Xml.child xml_arg0 "expiryTime") in
      let attachmentSetId =
        (Option.map ~f:AttachmentSetId.of_xml)
          (Xml.child xml_arg0 "attachmentSetId") in
      make ?expiryTime ?attachmentSetId ()
    let of_json json =
      let expiryTime = field_map json "expiryTime" ExpiryTime.of_json in
      let attachmentSetId =
        field_map json "attachmentSetId" AttachmentSetId.of_json in
      make ?expiryTime ?attachmentSetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The ID and expiry time of the attachment set returned by the AddAttachmentsToSet operation."]
module AddAttachmentsToSetRequest =
  struct
    type nonrec t =
      {
      attachmentSetId: AttachmentSetId.t option
        [@ocaml.doc
          "The ID of the attachment set. If an attachmentSetId is not specified, a new attachment set is created, and the ID of the set is returned in the response. If an attachmentSetId is specified, the attachments are added to the specified set, if it exists."];
      attachments: Attachments.t
        [@ocaml.doc
          "One or more attachments to add to the set. You can add up to three attachments per set. The size limit is 5 MB per attachment. In the Attachment object, use the data parameter to specify the contents of the attachment file. In the previous request syntax, the value for data appear as blob, which is represented as a base64-encoded string. The value for fileName is the name of the attachment, such as troubleshoot-screenshot.png."]}
    let context_ = "AddAttachmentsToSetRequest"
    let make ?attachmentSetId =
      fun ~attachments -> fun () -> { attachmentSetId; attachments }
    let to_value x =
      structure_to_value
        [("attachmentSetId",
           (Option.map x.attachmentSetId ~f:AttachmentSetId.to_value));
        ("attachments", (Some (Attachments.to_value x.attachments)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attachments =
        Attachments.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "attachments") in
      let attachmentSetId =
        (Option.map ~f:AttachmentSetId.of_xml)
          (Xml.child xml_arg0 "attachmentSetId") in
      make ~attachments ?attachmentSetId ()
    let of_json json =
      let attachments = field_map_exn json "attachments" Attachments.of_json in
      let attachmentSetId =
        field_map json "attachmentSetId" AttachmentSetId.of_json in
      make ~attachments ?attachmentSetId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds one or more attachments to an attachment set. An attachment set is a temporary container for attachments that you add to a case or case communication. The set is available for 1 hour after it's created. The expiryTime returned in the response is when the set expires. You must have a Business, Enterprise On-Ramp, or Enterprise Support plan to use the Amazon Web Services Support API. If you call the Amazon Web Services Support API from an account that does not have a Business, Enterprise On-Ramp, or Enterprise Support plan, the SubscriptionRequiredException error message appears. For information about changing your support plan, see Amazon Web Services Support."]
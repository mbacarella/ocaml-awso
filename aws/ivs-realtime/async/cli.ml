(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_encoder_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING EncoderConfigurationName"
       and video = flag "video" (optional json_arg) ~doc:"JSON Video"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_encoder_configuration
           (Values.CreateEncoderConfigurationRequest.make ?name
              ?video:(Option.map ~f:Values.Video.of_json video)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ())
           (Some Values.CreateEncoderConfigurationResponse.to_json)
           (Some Values.CreateEncoderConfigurationResponse.error_to_json)])
let create_ingest_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING IngestConfigurationName"
       and stageArn =
         flag "stage-arn" (optional string)
           ~doc:"STRING IngestConfigurationStageArn"
       and userId = flag "user-id" (optional string) ~doc:"STRING UserId"
       and attributes =
         flag "attributes" (optional json_arg)
           ~doc:"JSON ParticipantAttributes"
       and insecureIngest =
         flag "insecure-ingest" (optional bool) ~doc:"BOOL InsecureIngest"
       and redundantIngest =
         flag "redundant-ingest" (optional bool) ~doc:"BOOL RedundantIngest"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and ingestProtocol =
         flag "ingest-protocol" (required json_arg)
           ~doc:"JSON IngestProtocol" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ingest_configuration
           (Values.CreateIngestConfigurationRequest.make ?name ?stageArn
              ?userId
              ?attributes:(Option.map ~f:Values.ParticipantAttributes.of_json
                             attributes) ?insecureIngest ?redundantIngest
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~ingestProtocol:(Values.IngestProtocol.of_json ingestProtocol)
              ()) (Some Values.CreateIngestConfigurationResponse.to_json)
           (Some Values.CreateIngestConfigurationResponse.error_to_json)])
let create_participant_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and duration =
         flag "duration" (optional int)
           ~doc:"INT ParticipantTokenDurationMinutes"
       and userId =
         flag "user-id" (optional string)
           ~doc:"STRING ParticipantTokenUserId"
       and attributes =
         flag "attributes" (optional json_arg)
           ~doc:"JSON ParticipantTokenAttributes"
       and capabilities =
         flag "capabilities" (optional json_arg)
           ~doc:"JSON ParticipantTokenCapabilities"
       and stageArn =
         flag "stage-arn" (required string) ~doc:"STRING StageArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_participant_token
           (Values.CreateParticipantTokenRequest.make ?duration ?userId
              ?attributes:(Option.map
                             ~f:Values.ParticipantTokenAttributes.of_json
                             attributes)
              ?capabilities:(Option.map
                               ~f:Values.ParticipantTokenCapabilities.of_json
                               capabilities) ~stageArn ())
           (Some Values.CreateParticipantTokenResponse.to_json)
           (Some Values.CreateParticipantTokenResponse.error_to_json)])
let create_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING StageName"
       and participantTokenConfigurations =
         flag "participant-token-configurations" (optional json_arg)
           ~doc:"JSON ParticipantTokenConfigurations"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and autoParticipantRecordingConfiguration =
         flag "auto-participant-recording-configuration" (optional json_arg)
           ~doc:"JSON AutoParticipantRecordingConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_stage
           (Values.CreateStageRequest.make ?name
              ?participantTokenConfigurations:(Option.map
                                                 ~f:Values.ParticipantTokenConfigurations.of_json
                                                 participantTokenConfigurations)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?autoParticipantRecordingConfiguration:(Option.map
                                                        ~f:Values.AutoParticipantRecordingConfiguration.of_json
                                                        autoParticipantRecordingConfiguration)
              ()) (Some Values.CreateStageResponse.to_json)
           (Some Values.CreateStageResponse.error_to_json)])
let create_storage_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING StorageConfigurationName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and s3 =
         flag "s3" (required json_arg) ~doc:"JSON S3StorageConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_storage_configuration
           (Values.CreateStorageConfigurationRequest.make ?name
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~s3:(Values.S3StorageConfiguration.of_json s3) ())
           (Some Values.CreateStorageConfigurationResponse.to_json)
           (Some Values.CreateStorageConfigurationResponse.error_to_json)])
let delete_encoder_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING EncoderConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_encoder_configuration
           (Values.DeleteEncoderConfigurationRequest.make ~arn ())
           (Some Values.DeleteEncoderConfigurationResponse.to_json)
           (Some Values.DeleteEncoderConfigurationResponse.error_to_json)])
let delete_ingest_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and arn =
         flag "arn" (required string) ~doc:"STRING IngestConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_ingest_configuration
           (Values.DeleteIngestConfigurationRequest.make ?force ~arn ())
           (Some Values.DeleteIngestConfigurationResponse.to_json)
           (Some Values.DeleteIngestConfigurationResponse.error_to_json)])
let delete_public_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PublicKeyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_public_key (Values.DeletePublicKeyRequest.make ~arn ())
           (Some Values.DeletePublicKeyResponse.to_json)
           (Some Values.DeletePublicKeyResponse.error_to_json)])
let delete_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING StageArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_stage (Values.DeleteStageRequest.make ~arn ())
           (Some Values.DeleteStageResponse.to_json)
           (Some Values.DeleteStageResponse.error_to_json)])
let delete_storage_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING StorageConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_storage_configuration
           (Values.DeleteStorageConfigurationRequest.make ~arn ())
           (Some Values.DeleteStorageConfigurationResponse.to_json)
           (Some Values.DeleteStorageConfigurationResponse.error_to_json)])
let disconnect_participant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reason =
         flag "reason" (optional string)
           ~doc:"STRING DisconnectParticipantReason"
       and stageArn =
         flag "stage-arn" (required string) ~doc:"STRING StageArn"
       and participantId =
         flag "participant-id" (required string)
           ~doc:"STRING ParticipantTokenId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disconnect_participant
           (Values.DisconnectParticipantRequest.make ?reason ~stageArn
              ~participantId ())
           (Some Values.DisconnectParticipantResponse.to_json)
           (Some Values.DisconnectParticipantResponse.error_to_json)])
let get_composition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING CompositionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_composition (Values.GetCompositionRequest.make ~arn ())
           (Some Values.GetCompositionResponse.to_json)
           (Some Values.GetCompositionResponse.error_to_json)])
let get_encoder_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING EncoderConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_encoder_configuration
           (Values.GetEncoderConfigurationRequest.make ~arn ())
           (Some Values.GetEncoderConfigurationResponse.to_json)
           (Some Values.GetEncoderConfigurationResponse.error_to_json)])
let get_ingest_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING IngestConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_ingest_configuration
           (Values.GetIngestConfigurationRequest.make ~arn ())
           (Some Values.GetIngestConfigurationResponse.to_json)
           (Some Values.GetIngestConfigurationResponse.error_to_json)])
let get_participant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stageArn =
         flag "stage-arn" (required string) ~doc:"STRING StageArn"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING StageSessionId"
       and participantId =
         flag "participant-id" (required string) ~doc:"STRING ParticipantId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_participant
           (Values.GetParticipantRequest.make ~stageArn ~sessionId
              ~participantId ()) (Some Values.GetParticipantResponse.to_json)
           (Some Values.GetParticipantResponse.error_to_json)])
let get_public_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PublicKeyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_public_key (Values.GetPublicKeyRequest.make ~arn ())
           (Some Values.GetPublicKeyResponse.to_json)
           (Some Values.GetPublicKeyResponse.error_to_json)])
let get_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING StageArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_stage (Values.GetStageRequest.make ~arn ())
           (Some Values.GetStageResponse.to_json)
           (Some Values.GetStageResponse.error_to_json)])
let get_stage_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stageArn =
         flag "stage-arn" (required string) ~doc:"STRING StageArn"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING StageSessionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_stage_session
           (Values.GetStageSessionRequest.make ~stageArn ~sessionId ())
           (Some Values.GetStageSessionResponse.to_json)
           (Some Values.GetStageSessionResponse.error_to_json)])
let get_storage_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING StorageConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_storage_configuration
           (Values.GetStorageConfigurationRequest.make ~arn ())
           (Some Values.GetStorageConfigurationResponse.to_json)
           (Some Values.GetStorageConfigurationResponse.error_to_json)])
let import_public_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING PublicKeyName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and publicKeyMaterial =
         flag "public-key-material" (required string)
           ~doc:"STRING PublicKeyMaterial" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_public_key
           (Values.ImportPublicKeyRequest.make ?name
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~publicKeyMaterial ())
           (Some Values.ImportPublicKeyResponse.to_json)
           (Some Values.ImportPublicKeyResponse.error_to_json)])
let list_compositions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterByStageArn =
         flag "filter-by-stage-arn" (optional string) ~doc:"STRING StageArn"
       and filterByEncoderConfigurationArn =
         flag "filter-by-encoder-configuration-arn" (optional string)
           ~doc:"STRING EncoderConfigurationArn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxCompositionResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_compositions
           (Values.ListCompositionsRequest.make ?filterByStageArn
              ?filterByEncoderConfigurationArn ?nextToken ?maxResults ())
           (Some Values.ListCompositionsResponse.to_json)
           (Some Values.ListCompositionsResponse.error_to_json)])
let list_encoder_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxEncoderConfigurationResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_encoder_configurations
           (Values.ListEncoderConfigurationsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListEncoderConfigurationsResponse.to_json)
           (Some Values.ListEncoderConfigurationsResponse.error_to_json)])
let list_ingest_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterByStageArn =
         flag "filter-by-stage-arn" (optional string) ~doc:"STRING StageArn"
       and filterByState =
         flag "filter-by-state" (optional json_arg)
           ~doc:"JSON IngestConfigurationState"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxIngestConfigurationResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ingest_configurations
           (Values.ListIngestConfigurationsRequest.make ?filterByStageArn
              ?filterByState:(Option.map
                                ~f:Values.IngestConfigurationState.of_json
                                filterByState) ?nextToken ?maxResults ())
           (Some Values.ListIngestConfigurationsResponse.to_json)
           (Some Values.ListIngestConfigurationsResponse.error_to_json)])
let list_participant_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxParticipantEventResults"
       and stageArn =
         flag "stage-arn" (required string) ~doc:"STRING StageArn"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING StageSessionId"
       and participantId =
         flag "participant-id" (required string) ~doc:"STRING ParticipantId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_participant_events
           (Values.ListParticipantEventsRequest.make ?nextToken ?maxResults
              ~stageArn ~sessionId ~participantId ())
           (Some Values.ListParticipantEventsResponse.to_json)
           (Some Values.ListParticipantEventsResponse.error_to_json)])
let list_participant_replicas =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxParticipantReplicaResults"
       and sourceStageArn =
         flag "source-stage-arn" (required string) ~doc:"STRING StageArn"
       and participantId =
         flag "participant-id" (required string) ~doc:"STRING ParticipantId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_participant_replicas
           (Values.ListParticipantReplicasRequest.make ?nextToken ?maxResults
              ~sourceStageArn ~participantId ())
           (Some Values.ListParticipantReplicasResponse.to_json)
           (Some Values.ListParticipantReplicasResponse.error_to_json)])
let list_participants =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterByUserId =
         flag "filter-by-user-id" (optional string) ~doc:"STRING UserId"
       and filterByPublished =
         flag "filter-by-published" (optional bool) ~doc:"BOOL Published"
       and filterByState =
         flag "filter-by-state" (optional json_arg)
           ~doc:"JSON ParticipantState"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxParticipantResults"
       and filterByRecordingState =
         flag "filter-by-recording-state" (optional json_arg)
           ~doc:"JSON ParticipantRecordingFilterByRecordingState"
       and stageArn =
         flag "stage-arn" (required string) ~doc:"STRING StageArn"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING StageSessionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_participants
           (Values.ListParticipantsRequest.make ?filterByUserId
              ?filterByPublished
              ?filterByState:(Option.map ~f:Values.ParticipantState.of_json
                                filterByState) ?nextToken ?maxResults
              ?filterByRecordingState:(Option.map
                                         ~f:Values.ParticipantRecordingFilterByRecordingState.of_json
                                         filterByRecordingState) ~stageArn
              ~sessionId ()) (Some Values.ListParticipantsResponse.to_json)
           (Some Values.ListParticipantsResponse.error_to_json)])
let list_public_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPublicKeyResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_public_keys
           (Values.ListPublicKeysRequest.make ?nextToken ?maxResults ())
           (Some Values.ListPublicKeysResponse.to_json)
           (Some Values.ListPublicKeysResponse.error_to_json)])
let list_stage_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxStageSessionResults"
       and stageArn =
         flag "stage-arn" (required string) ~doc:"STRING StageArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stage_sessions
           (Values.ListStageSessionsRequest.make ?nextToken ?maxResults
              ~stageArn ()) (Some Values.ListStageSessionsResponse.to_json)
           (Some Values.ListStageSessionsResponse.error_to_json)])
let list_stages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxStageResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stages
           (Values.ListStagesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListStagesResponse.to_json)
           (Some Values.ListStagesResponse.error_to_json)])
let list_storage_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxStorageConfigurationResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_storage_configurations
           (Values.ListStorageConfigurationsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListStorageConfigurationsResponse.to_json)
           (Some Values.ListStorageConfigurationsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_composition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and idempotencyToken =
         flag "idempotency-token" (optional string)
           ~doc:"STRING CompositionClientToken"
       and layout =
         flag "layout" (optional json_arg) ~doc:"JSON LayoutConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and stageArn =
         flag "stage-arn" (required string) ~doc:"STRING StageArn"
       and destinations =
         flag "destinations" (required json_arg)
           ~doc:"JSON DestinationConfigurationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_composition
           (Values.StartCompositionRequest.make ?idempotencyToken
              ?layout:(Option.map ~f:Values.LayoutConfiguration.of_json
                         layout)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~stageArn
              ~destinations:(Values.DestinationConfigurationList.of_json
                               destinations) ())
           (Some Values.StartCompositionResponse.to_json)
           (Some Values.StartCompositionResponse.error_to_json)])
let start_participant_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reconnectWindowSeconds =
         flag "reconnect-window-seconds" (optional int)
           ~doc:"INT ReconnectWindowSeconds"
       and attributes =
         flag "attributes" (optional json_arg)
           ~doc:"JSON ParticipantAttributes"
       and sourceStageArn =
         flag "source-stage-arn" (required string) ~doc:"STRING StageArn"
       and destinationStageArn =
         flag "destination-stage-arn" (required string)
           ~doc:"STRING StageArn"
       and participantId =
         flag "participant-id" (required string) ~doc:"STRING ParticipantId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_participant_replication
           (Values.StartParticipantReplicationRequest.make
              ?reconnectWindowSeconds
              ?attributes:(Option.map ~f:Values.ParticipantAttributes.of_json
                             attributes) ~sourceStageArn ~destinationStageArn
              ~participantId ())
           (Some Values.StartParticipantReplicationResponse.to_json)
           (Some Values.StartParticipantReplicationResponse.error_to_json)])
let stop_composition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING CompositionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_composition (Values.StopCompositionRequest.make ~arn ())
           (Some Values.StopCompositionResponse.to_json)
           (Some Values.StopCompositionResponse.error_to_json)])
let stop_participant_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceStageArn =
         flag "source-stage-arn" (required string) ~doc:"STRING StageArn"
       and destinationStageArn =
         flag "destination-stage-arn" (required string)
           ~doc:"STRING StageArn"
       and participantId =
         flag "participant-id" (required string) ~doc:"STRING ParticipantId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_participant_replication
           (Values.StopParticipantReplicationRequest.make ~sourceStageArn
              ~destinationStageArn ~participantId ())
           (Some Values.StopParticipantReplicationResponse.to_json)
           (Some Values.StopParticipantReplicationResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_ingest_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stageArn =
         flag "stage-arn" (optional string)
           ~doc:"STRING IngestConfigurationStageArn"
       and redundantIngest =
         flag "redundant-ingest" (optional bool) ~doc:"BOOL RedundantIngest"
       and arn =
         flag "arn" (required string) ~doc:"STRING IngestConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_ingest_configuration
           (Values.UpdateIngestConfigurationRequest.make ?stageArn
              ?redundantIngest ~arn ())
           (Some Values.UpdateIngestConfigurationResponse.to_json)
           (Some Values.UpdateIngestConfigurationResponse.error_to_json)])
let update_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING StageName"
       and autoParticipantRecordingConfiguration =
         flag "auto-participant-recording-configuration" (optional json_arg)
           ~doc:"JSON AutoParticipantRecordingConfiguration"
       and arn = flag "arn" (required string) ~doc:"STRING StageArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_stage
           (Values.UpdateStageRequest.make ?name
              ?autoParticipantRecordingConfiguration:(Option.map
                                                        ~f:Values.AutoParticipantRecordingConfiguration.of_json
                                                        autoParticipantRecordingConfiguration)
              ~arn ()) (Some Values.UpdateStageResponse.to_json)
           (Some Values.UpdateStageResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-encoder-configuration", create_encoder_configuration);
    ("create-ingest-configuration", create_ingest_configuration);
    ("create-participant-token", create_participant_token);
    ("create-stage", create_stage);
    ("create-storage-configuration", create_storage_configuration);
    ("delete-encoder-configuration", delete_encoder_configuration);
    ("delete-ingest-configuration", delete_ingest_configuration);
    ("delete-public-key", delete_public_key);
    ("delete-stage", delete_stage);
    ("delete-storage-configuration", delete_storage_configuration);
    ("disconnect-participant", disconnect_participant);
    ("get-composition", get_composition);
    ("get-encoder-configuration", get_encoder_configuration);
    ("get-ingest-configuration", get_ingest_configuration);
    ("get-participant", get_participant);
    ("get-public-key", get_public_key);
    ("get-stage", get_stage);
    ("get-stage-session", get_stage_session);
    ("get-storage-configuration", get_storage_configuration);
    ("import-public-key", import_public_key);
    ("list-compositions", list_compositions);
    ("list-encoder-configurations", list_encoder_configurations);
    ("list-ingest-configurations", list_ingest_configurations);
    ("list-participant-events", list_participant_events);
    ("list-participant-replicas", list_participant_replicas);
    ("list-participants", list_participants);
    ("list-public-keys", list_public_keys);
    ("list-stage-sessions", list_stage_sessions);
    ("list-stages", list_stages);
    ("list-storage-configurations", list_storage_configurations);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-composition", start_composition);
    ("start-participant-replication", start_participant_replication);
    ("stop-composition", stop_composition);
    ("stop-participant-replication", stop_participant_replication);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-ingest-configuration", update_ingest_configuration);
    ("update-stage", update_stage)]
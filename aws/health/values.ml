(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.health
let apiVersion = "2016-08-04"
let endpointPrefix = "health"
let serviceFullName = "AWS Health APIs and Notifications"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "AWSHealth"
let targetPrefix = "AWSHealth_20160804"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module Count =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for count" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module EntityStatusCode =
  struct
    type nonrec t =
      | IMPAIRED 
      | UNIMPAIRED 
      | UNKNOWN 
      | PENDING 
      | RESOLVED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | IMPAIRED -> "IMPAIRED"
      | UNIMPAIRED -> "UNIMPAIRED"
      | UNKNOWN -> "UNKNOWN"
      | PENDING -> "PENDING"
      | RESOLVED -> "RESOLVED"
      | Non_static_id s -> s
    let of_string =
      function
      | "IMPAIRED" -> IMPAIRED
      | "UNIMPAIRED" -> UNIMPAIRED
      | "UNKNOWN" -> UNKNOWN
      | "PENDING" -> PENDING
      | "RESOLVED" -> RESOLVED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration entityStatusCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"entityStatusCode" j)
    let to_json = simple_to_json to_value
  end
module EventPersona =
  struct
    type nonrec t =
      | OPERATIONS 
      | SECURITY 
      | BILLING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | OPERATIONS -> "OPERATIONS"
      | SECURITY -> "SECURITY"
      | BILLING -> "BILLING"
      | Non_static_id s -> s
    let of_string =
      function
      | "OPERATIONS" -> OPERATIONS
      | "SECURITY" -> SECURITY
      | "BILLING" -> BILLING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EventPersona" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EventPersona" j)
    let to_json = simple_to_json to_value
  end
module EntityStatuses =
  struct
    type nonrec t = (EntityStatusCode.t * Count.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((EntityStatusCode.of_string chopped),
                              (Count.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (EntityStatusCode.to_value x) |>
                    (fun x -> (Count.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:EntityStatusCode.of_string
        ~of_json:Count.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventArn =
  struct
    type nonrec t = string
    let context_ = "eventArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1600) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"arn:aws(-[a-z]+(-[a-z]+)?)?:health:[^:]*:[^:]*:event(?:/[\\w-]+){3}"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"eventArn" j
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "tagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:127) >>=
             (fun () -> check_pattern i ~pattern:".{0,127}"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"tagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "tagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_pattern i ~pattern:".{0,255}"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"tagValue" j
    let to_json = simple_to_json to_value
  end
module EventTypePersona =
  struct
    type nonrec t =
      | OPERATIONS 
      | SECURITY 
      | BILLING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | OPERATIONS -> "OPERATIONS"
      | SECURITY -> "SECURITY"
      | BILLING -> "BILLING"
      | Non_static_id s -> s
    let of_string =
      function
      | "OPERATIONS" -> OPERATIONS
      | "SECURITY" -> SECURITY
      | "BILLING" -> BILLING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EventTypePersona" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EventTypePersona" j)
    let to_json = simple_to_json to_value
  end
module EventActionability =
  struct
    type nonrec t =
      | ACTION_REQUIRED 
      | ACTION_MAY_BE_REQUIRED 
      | INFORMATIONAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTION_REQUIRED -> "ACTION_REQUIRED"
      | ACTION_MAY_BE_REQUIRED -> "ACTION_MAY_BE_REQUIRED"
      | INFORMATIONAL -> "INFORMATIONAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTION_REQUIRED" -> ACTION_REQUIRED
      | "ACTION_MAY_BE_REQUIRED" -> ACTION_MAY_BE_REQUIRED
      | "INFORMATIONAL" -> INFORMATIONAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration EventActionability" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EventActionability" j)
    let to_json = simple_to_json to_value
  end
module EventPersonaList =
  struct
    type nonrec t = EventPersona.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:3) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventPersona.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventPersona.of_xml)
    let of_json j =
      list_of_json ~kind:"EventPersonaList" ~of_json:EventPersona.of_json j
    let to_json v = composed_to_json to_value v
  end
module AvailabilityZone =
  struct
    type nonrec t = string
    let context_ = "availabilityZone"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:6) >>=
             (fun () ->
                (check_string_max i ~max:18) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[a-z]{2,4}\\-[0-9a-z\\-]{4,16}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"availabilityZone" j
    let to_json = simple_to_json to_value
  end
module EventScopeCode =
  struct
    type nonrec t =
      | PUBLIC 
      | ACCOUNT_SPECIFIC 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PUBLIC -> "PUBLIC"
      | ACCOUNT_SPECIFIC -> "ACCOUNT_SPECIFIC"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "PUBLIC" -> PUBLIC
      | "ACCOUNT_SPECIFIC" -> ACCOUNT_SPECIFIC
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration eventScopeCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"eventScopeCode" j)
    let to_json = simple_to_json to_value
  end
module EventStatusCode =
  struct
    type nonrec t =
      | Open 
      | Closed 
      | Upcoming 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Open -> "open"
      | Closed -> "closed"
      | Upcoming -> "upcoming"
      | Non_static_id s -> s
    let of_string =
      function
      | "open" -> Open
      | "closed" -> Closed
      | "upcoming" -> Upcoming
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration eventStatusCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"eventStatusCode" j)
    let to_json = simple_to_json to_value
  end
module EventTypeCategory =
  struct
    type nonrec t =
      | Issue 
      | AccountNotification 
      | ScheduledChange 
      | Investigation 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Issue -> "issue"
      | AccountNotification -> "accountNotification"
      | ScheduledChange -> "scheduledChange"
      | Investigation -> "investigation"
      | Non_static_id s -> s
    let of_string =
      function
      | "issue" -> Issue
      | "accountNotification" -> AccountNotification
      | "scheduledChange" -> ScheduledChange
      | "investigation" -> Investigation
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration eventTypeCategory" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"eventTypeCategory" j)
    let to_json = simple_to_json to_value
  end
module EventTypeCode =
  struct
    type nonrec t = string
    let context_ = "eventTypeCode"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9\\_\\-]{3,100}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"eventTypeCode" j
    let to_json = simple_to_json to_value
  end
module Region =
  struct
    type nonrec t = string
    let context_ = "region"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:2) >>=
             (fun () ->
                (check_string_max i ~max:25) >>=
                  (fun () -> check_pattern i ~pattern:"[^:/]{2,25}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"region" j
    let to_json = simple_to_json to_value
  end
module Service =
  struct
    type nonrec t = string
    let context_ = "service"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:2) >>=
             (fun () ->
                (check_string_max i ~max:30) >>=
                  (fun () -> check_pattern i ~pattern:"[^:/]{2,30}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"service" j
    let to_json = simple_to_json to_value
  end
module EventDescription__lc1 =
  struct
    type nonrec t = string
    let context_ = "EventDescription__lc1"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EventDescription__lc1" j
    let to_json = simple_to_json to_value
  end
module MetadataKey =
  struct
    type nonrec t = string
    let context_ = "metadataKey"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:32766); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"metadataKey" j
    let to_json = simple_to_json to_value
  end
module MetadataValue =
  struct
    type nonrec t = string
    let context_ = "metadataValue"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:32766); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"metadataValue" j
    let to_json = simple_to_json to_value
  end
module AccountEntityAggregate =
  struct
    type nonrec t =
      {
      accountId: EventArn.t option
        [@ocaml.doc
          "The 12-digit Amazon Web Services account numbers that contains the affected entities."];
      count: Count.t option
        [@ocaml.doc
          "The number of entities that match the filter criteria for the specified events."];
      statuses: EntityStatuses.t option
        [@ocaml.doc
          "The number of affected entities aggregated by the entity status codes."]}
    let make ?accountId =
      fun ?count -> fun ?statuses -> fun () -> { accountId; count; statuses }
    let to_value x =
      structure_to_value
        [("accountId", (Option.map x.accountId ~f:EventArn.to_value));
        ("count", (Option.map x.count ~f:Count.to_value));
        ("statuses", (Option.map x.statuses ~f:EntityStatuses.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statuses =
        (Option.map ~f:EntityStatuses.of_xml) (Xml.child xml_arg0 "statuses") in
      let count = (Option.map ~f:Count.of_xml) (Xml.child xml_arg0 "count") in
      let accountId =
        (Option.map ~f:EventArn.of_xml) (Xml.child xml_arg0 "accountId") in
      make ?statuses ?count ?accountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let statuses = field_map json__ "statuses" EntityStatuses.of_json in
      let count = field_map json__ "count" Count.of_json in
      let accountId = field_map json__ "accountId" EventArn.of_json in
      make ?statuses ?count ?accountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The number of entities in an account that are impacted by a specific event aggregated by the entity status codes."]
module EntityMetadataKey =
  struct
    type nonrec t = string
    let context_ = "entityMetadataKey"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1024); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"entityMetadataKey" j
    let to_json = simple_to_json to_value
  end
module EntityMetadataValue =
  struct
    type nonrec t = string
    let context_ = "entityMetadataValue"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1024); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"entityMetadataValue" j
    let to_json = simple_to_json to_value
  end
module EventType__lc1 =
  struct
    type nonrec t = string
    let context_ = "EventType__lc1"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"[^:/]{3,100}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EventType__lc1" j
    let to_json = simple_to_json to_value
  end
module DateTimeRange =
  struct
    type nonrec t =
      {
      from: Timestamp.t option
        [@ocaml.doc "The starting date and time of a time range."];
      to_: Timestamp.t option
        [@ocaml.doc "The ending date and time of a time range."]}
    let make ?from = fun ?to_ -> fun () -> { from; to_ }
    let to_value x =
      structure_to_value
        [("from", (Option.map x.from ~f:Timestamp.to_value));
        ("to", (Option.map x.to_ ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let to_ = (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "to") in
      let from = (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "from") in
      make ?to_ ?from ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let to_ = field_map json__ "to" Timestamp.of_json in
      let from = field_map json__ "from" Timestamp.of_json in
      make ?to_ ?from ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A range of dates and times that is used by the EventFilter and EntityFilter objects. If from is set and to is set: match items where the timestamp (startTime, endTime, or lastUpdatedTime) is between from and to inclusive. If from is set and to is not set: match items where the timestamp value is equal to or after from. If from is not set and to is set: match items where the timestamp value is equal to or before to."]
module EntityArn =
  struct
    type nonrec t = string
    let context_ = "entityArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1600) >>=
             (fun () -> check_pattern i ~pattern:".{0,1600}"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"entityArn" j
    let to_json = simple_to_json to_value
  end
module EntityValue =
  struct
    type nonrec t = string
    let context_ = "entityValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1224) >>=
             (fun () -> check_pattern i ~pattern:".{0,1224}"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"entityValue" j
    let to_json = simple_to_json to_value
  end
module TagSet =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:50); i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module AccountId =
  struct
    type nonrec t = string
    let context_ = "accountId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:12) >>=
             (fun () -> check_pattern i ~pattern:"^\\S+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"accountId" j
    let to_json = simple_to_json to_value
  end
module EventTypeActionability =
  struct
    type nonrec t =
      | ACTION_REQUIRED 
      | ACTION_MAY_BE_REQUIRED 
      | INFORMATIONAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTION_REQUIRED -> "ACTION_REQUIRED"
      | ACTION_MAY_BE_REQUIRED -> "ACTION_MAY_BE_REQUIRED"
      | INFORMATIONAL -> "INFORMATIONAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTION_REQUIRED" -> ACTION_REQUIRED
      | "ACTION_MAY_BE_REQUIRED" -> ACTION_MAY_BE_REQUIRED
      | "INFORMATIONAL" -> INFORMATIONAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration EventTypeActionability" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"EventTypeActionability" j)
    let to_json = simple_to_json to_value
  end
module EventTypePersonaList =
  struct
    type nonrec t = EventTypePersona.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:3) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventTypePersona.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventTypePersona.of_xml)
    let of_json j =
      list_of_json ~kind:"EventTypePersonaList"
        ~of_json:EventTypePersona.of_json j
    let to_json v = composed_to_json to_value v
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "string"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"string" j
    let to_json = simple_to_json to_value
  end
module Event =
  struct
    type nonrec t =
      {
      arn: EventArn.t option
        [@ocaml.doc
          "The unique identifier for the event. The event ARN has the arn:aws:health:event-region::event/SERVICE/EVENT_TYPE_CODE/EVENT_TYPE_PLUS_ID format. For example, an event ARN might look like the following: arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-DEF456"];
      service: Service.t option
        [@ocaml.doc
          "The Amazon Web Services service that is affected by the event. For example, EC2, RDS."];
      eventTypeCode: EventTypeCode.t option
        [@ocaml.doc
          "The unique identifier for the event type. The format is AWS_SERVICE_DESCRIPTION ; for example, AWS_EC2_SYSTEM_MAINTENANCE_EVENT."];
      eventTypeCategory: EventTypeCategory.t option
        [@ocaml.doc
          "A list of event type category codes. Possible values are issue, accountNotification, or scheduledChange. Currently, the investigation value isn't supported at this time."];
      region: Region.t option
        [@ocaml.doc "The Amazon Web Services Region name of the event."];
      availabilityZone: AvailabilityZone.t option
        [@ocaml.doc
          "The Amazon Web Services Availability Zone of the event. For example, us-east-1a."];
      startTime: Timestamp.t option
        [@ocaml.doc "The date and time that the event began."];
      endTime: Timestamp.t option
        [@ocaml.doc "The date and time that the event ended."];
      lastUpdatedTime: Timestamp.t option
        [@ocaml.doc
          "The most recent date and time that the event was updated."];
      statusCode: EventStatusCode.t option
        [@ocaml.doc
          "The most recent status of the event. Possible values are open, closed, and upcoming."];
      eventScopeCode: EventScopeCode.t option
        [@ocaml.doc
          "This parameter specifies if the Health event is a public Amazon Web Services service event or an account-specific event. If the eventScopeCode value is PUBLIC, then the affectedAccounts value is always empty. If the eventScopeCode value is ACCOUNT_SPECIFIC, then the affectedAccounts value lists the affected Amazon Web Services accounts in your organization. For example, if an event affects a service such as Amazon Elastic Compute Cloud and you have Amazon Web Services accounts that use that service, those account IDs appear in the response. If the eventScopeCode value is NONE, then the eventArn that you specified in the request is invalid or doesn't exist."];
      actionability: EventActionability.t option
        [@ocaml.doc
          "The actionability classification of the event. Possible values are ACTION_REQUIRED, ACTION_MAY_BE_REQUIRED and INFORMATIONAL. Events with ACTION_REQUIRED actionability require customer action to resolve or mitigate the event. Events with ACTION_MAY_BE_REQUIRED actionability indicates that the current status is unknown or conditional and inspection is needed to determine if action is required. Events with INFORMATIONAL actionability are provided for awareness and do not require immediate action."];
      personas: EventPersonaList.t option
        [@ocaml.doc
          "A list of persona classifications that indicate the target audience for the event. Possible values are OPERATIONS, SECURITY, and BILLING. Events can be associated with multiple personas to indicate relevance to different teams or roles within an organization."]}
    let make ?arn =
      fun ?service ->
        fun ?eventTypeCode ->
          fun ?eventTypeCategory ->
            fun ?region ->
              fun ?availabilityZone ->
                fun ?startTime ->
                  fun ?endTime ->
                    fun ?lastUpdatedTime ->
                      fun ?statusCode ->
                        fun ?eventScopeCode ->
                          fun ?actionability ->
                            fun ?personas ->
                              fun () ->
                                {
                                  arn;
                                  service;
                                  eventTypeCode;
                                  eventTypeCategory;
                                  region;
                                  availabilityZone;
                                  startTime;
                                  endTime;
                                  lastUpdatedTime;
                                  statusCode;
                                  eventScopeCode;
                                  actionability;
                                  personas
                                }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:EventArn.to_value));
        ("service", (Option.map x.service ~f:Service.to_value));
        ("eventTypeCode",
          (Option.map x.eventTypeCode ~f:EventTypeCode.to_value));
        ("eventTypeCategory",
          (Option.map x.eventTypeCategory ~f:EventTypeCategory.to_value));
        ("region", (Option.map x.region ~f:Region.to_value));
        ("availabilityZone",
          (Option.map x.availabilityZone ~f:AvailabilityZone.to_value));
        ("startTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("lastUpdatedTime",
          (Option.map x.lastUpdatedTime ~f:Timestamp.to_value));
        ("statusCode", (Option.map x.statusCode ~f:EventStatusCode.to_value));
        ("eventScopeCode",
          (Option.map x.eventScopeCode ~f:EventScopeCode.to_value));
        ("actionability",
          (Option.map x.actionability ~f:EventActionability.to_value));
        ("personas", (Option.map x.personas ~f:EventPersonaList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let personas =
        (Option.map ~f:EventPersonaList.of_xml)
          (Xml.child xml_arg0 "personas") in
      let actionability =
        (Option.map ~f:EventActionability.of_xml)
          (Xml.child xml_arg0 "actionability") in
      let eventScopeCode =
        (Option.map ~f:EventScopeCode.of_xml)
          (Xml.child xml_arg0 "eventScopeCode") in
      let statusCode =
        (Option.map ~f:EventStatusCode.of_xml)
          (Xml.child xml_arg0 "statusCode") in
      let lastUpdatedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastUpdatedTime") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startTime") in
      let availabilityZone =
        (Option.map ~f:AvailabilityZone.of_xml)
          (Xml.child xml_arg0 "availabilityZone") in
      let region =
        (Option.map ~f:Region.of_xml) (Xml.child xml_arg0 "region") in
      let eventTypeCategory =
        (Option.map ~f:EventTypeCategory.of_xml)
          (Xml.child xml_arg0 "eventTypeCategory") in
      let eventTypeCode =
        (Option.map ~f:EventTypeCode.of_xml)
          (Xml.child xml_arg0 "eventTypeCode") in
      let service =
        (Option.map ~f:Service.of_xml) (Xml.child xml_arg0 "service") in
      let arn = (Option.map ~f:EventArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?personas ?actionability ?eventScopeCode ?statusCode
        ?lastUpdatedTime ?endTime ?startTime ?availabilityZone ?region
        ?eventTypeCategory ?eventTypeCode ?service ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let personas = field_map json__ "personas" EventPersonaList.of_json in
      let actionability =
        field_map json__ "actionability" EventActionability.of_json in
      let eventScopeCode =
        field_map json__ "eventScopeCode" EventScopeCode.of_json in
      let statusCode = field_map json__ "statusCode" EventStatusCode.of_json in
      let lastUpdatedTime =
        field_map json__ "lastUpdatedTime" Timestamp.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let startTime = field_map json__ "startTime" Timestamp.of_json in
      let availabilityZone =
        field_map json__ "availabilityZone" AvailabilityZone.of_json in
      let region = field_map json__ "region" Region.of_json in
      let eventTypeCategory =
        field_map json__ "eventTypeCategory" EventTypeCategory.of_json in
      let eventTypeCode =
        field_map json__ "eventTypeCode" EventTypeCode.of_json in
      let service = field_map json__ "service" Service.of_json in
      let arn = field_map json__ "arn" EventArn.of_json in
      make ?personas ?actionability ?eventScopeCode ?statusCode
        ?lastUpdatedTime ?endTime ?startTime ?availabilityZone ?region
        ?eventTypeCategory ?eventTypeCode ?service ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Summary information about an Health event. Health events can be public or account-specific: Public events might be service events that are not specific to an Amazon Web Services account. For example, if there is an issue with an Amazon Web Services Region, Health provides information about the event, even if you don't use services or resources in that Region. Account-specific events are specific to either your Amazon Web Services account or an account in your organization. For example, if there's an issue with Amazon Elastic Compute Cloud in a Region that you use, Health provides information about the event and the affected resources in the account. You can determine if an event is public or account-specific by using the eventScopeCode parameter. For more information, see eventScopeCode."]
module EventDescription =
  struct
    type nonrec t =
      {
      latestDescription: EventDescription__lc1.t option
        [@ocaml.doc "The most recent description of the event."]}
    let make ?latestDescription = fun () -> { latestDescription }
    let to_value x =
      structure_to_value
        [("latestDescription",
           (Option.map x.latestDescription ~f:EventDescription__lc1.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let latestDescription =
        (Option.map ~f:EventDescription__lc1.of_xml)
          (Xml.child xml_arg0 "latestDescription") in
      make ?latestDescription ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let latestDescription =
        field_map json__ "latestDescription" EventDescription__lc1.of_json in
      make ?latestDescription ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The detailed description of the event. Included in the information returned by the DescribeEventDetails operation."]
module EventMetadata =
  struct
    type nonrec t = (MetadataKey.t * MetadataValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((MetadataKey.of_string chopped),
                              (MetadataValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (MetadataKey.to_value x) |>
                    (fun x -> (MetadataValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:MetadataKey.of_string
        ~of_json:MetadataValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module AggregateValue =
  struct
    type nonrec t = string
    let context_ = "aggregateValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"aggregateValue" j
    let to_json = simple_to_json to_value
  end
module AccountEntityAggregatesList =
  struct
    type nonrec t = AccountEntityAggregate.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AccountEntityAggregate.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccountEntityAggregate.of_xml)
    let of_json j =
      list_of_json ~kind:"AccountEntityAggregatesList"
        ~of_json:AccountEntityAggregate.of_json j
    let to_json v = composed_to_json to_value v
  end
module EntityMetadata =
  struct
    type nonrec t = (EntityMetadataKey.t * EntityMetadataValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((EntityMetadataKey.of_string chopped),
                              (EntityMetadataValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (EntityMetadataKey.to_value x) |>
                    (fun x ->
                       (EntityMetadataValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:EntityMetadataKey.of_string
        ~of_json:EntityMetadataValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module EntityUrl =
  struct
    type nonrec t = string
    let context_ = "entityUrl"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"entityUrl" j
    let to_json = simple_to_json to_value
  end
module EntityStatusCodeList =
  struct
    type nonrec t = EntityStatusCode.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EntityStatusCode.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EntityStatusCode.of_xml)
    let of_json j =
      list_of_json ~kind:"entityStatusCodeList"
        ~of_json:EntityStatusCode.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventActionabilityList =
  struct
    type nonrec t = EventActionability.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:3) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventActionability.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventActionability.of_xml)
    let of_json j =
      list_of_json ~kind:"EventActionabilityList"
        ~of_json:EventActionability.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventTypeCategoryList__lc1 =
  struct
    type nonrec t = EventTypeCategory.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventTypeCategory.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventTypeCategory.of_xml)
    let of_json j =
      list_of_json ~kind:"EventTypeCategoryList__lc1"
        ~of_json:EventTypeCategory.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventTypeList__lc1 =
  struct
    type nonrec t = EventType__lc1.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventType__lc1.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventType__lc1.of_xml)
    let of_json j =
      list_of_json ~kind:"EventTypeList__lc1" ~of_json:EventType__lc1.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module AvailabilityZones =
  struct
    type nonrec t = AvailabilityZone.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AvailabilityZone.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AvailabilityZone.of_xml)
    let of_json j =
      list_of_json ~kind:"availabilityZones"
        ~of_json:AvailabilityZone.of_json j
    let to_json v = composed_to_json to_value v
  end
module DateTimeRangeList =
  struct
    type nonrec t = DateTimeRange.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DateTimeRange.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DateTimeRange.of_xml)
    let of_json j =
      list_of_json ~kind:"dateTimeRangeList" ~of_json:DateTimeRange.of_json j
    let to_json v = composed_to_json to_value v
  end
module EntityArnList =
  struct
    type nonrec t = EntityArn.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:99) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EntityArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EntityArn.of_xml)
    let of_json j =
      list_of_json ~kind:"entityArnList" ~of_json:EntityArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module EntityValueList =
  struct
    type nonrec t = EntityValue.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:99) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EntityValue.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EntityValue.of_xml)
    let of_json j =
      list_of_json ~kind:"entityValueList" ~of_json:EntityValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventArnList =
  struct
    type nonrec t = EventArn.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventArn.of_xml)
    let of_json j =
      list_of_json ~kind:"eventArnList" ~of_json:EventArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventStatusCodeList =
  struct
    type nonrec t = EventStatusCode.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:6) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventStatusCode.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventStatusCode.of_xml)
    let of_json j =
      list_of_json ~kind:"eventStatusCodeList"
        ~of_json:EventStatusCode.of_json j
    let to_json v = composed_to_json to_value v
  end
module RegionList =
  struct
    type nonrec t = Region.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Region.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Region.of_xml)
    let of_json j = list_of_json ~kind:"regionList" ~of_json:Region.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServiceList =
  struct
    type nonrec t = Service.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Service.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Service.of_xml)
    let of_json j =
      list_of_json ~kind:"serviceList" ~of_json:Service.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagFilter =
  struct
    type nonrec t = TagSet.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:50); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagSet.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagSet.of_xml)
    let of_json j = list_of_json ~kind:"tagFilter" ~of_json:TagSet.of_json j
    let to_json v = composed_to_json to_value v
  end
module OrganizationEvent =
  struct
    type nonrec t =
      {
      arn: EventArn.t option
        [@ocaml.doc
          "The unique identifier for the event. The event ARN has the arn:aws:health:event-region::event/SERVICE/EVENT_TYPE_CODE/EVENT_TYPE_PLUS_ID format. For example, an event ARN might look like the following: arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-DEF456"];
      service: Service.t option
        [@ocaml.doc
          "The Amazon Web Services service that is affected by the event, such as EC2 and RDS."];
      eventTypeCode: EventTypeCode.t option
        [@ocaml.doc
          "The unique identifier for the event type. The format is AWS_SERVICE_DESCRIPTION. For example, AWS_EC2_SYSTEM_MAINTENANCE_EVENT."];
      eventTypeCategory: EventTypeCategory.t option
        [@ocaml.doc
          "A list of event type category codes. Possible values are issue, accountNotification, or scheduledChange. Currently, the investigation value isn't supported at this time."];
      eventScopeCode: EventScopeCode.t option
        [@ocaml.doc
          "This parameter specifies if the Health event is a public Amazon Web Services service event or an account-specific event. If the eventScopeCode value is PUBLIC, then the affectedAccounts value is always empty. If the eventScopeCode value is ACCOUNT_SPECIFIC, then the affectedAccounts value lists the affected Amazon Web Services accounts in your organization. For example, if an event affects a service such as Amazon Elastic Compute Cloud and you have Amazon Web Services accounts that use that service, those account IDs appear in the response. If the eventScopeCode value is NONE, then the eventArn that you specified in the request is invalid or doesn't exist."];
      region: Region.t option
        [@ocaml.doc "The Amazon Web Services Region name of the event."];
      startTime: Timestamp.t option
        [@ocaml.doc "The date and time that the event began."];
      endTime: Timestamp.t option
        [@ocaml.doc "The date and time that the event ended."];
      lastUpdatedTime: Timestamp.t option
        [@ocaml.doc
          "The most recent date and time that the event was updated."];
      statusCode: EventStatusCode.t option
        [@ocaml.doc
          "The most recent status of the event. Possible values are open, closed, and upcoming."];
      actionability: EventActionability.t option
        [@ocaml.doc
          "The actionability classification of the event. Possible values are ACTION_REQUIRED, ACTION_MAY_BE_REQUIRED and INFORMATIONAL. Events with ACTION_REQUIRED actionability require customer action to resolve or mitigate the event. Events with ACTION_MAY_BE_REQUIRED actionability indicates that the current status is unknown or conditional and inspection is needed to determine if action is required. Events with INFORMATIONAL actionability are provided for awareness and do not require immediate action."];
      personas: EventPersonaList.t option
        [@ocaml.doc
          "A list of persona classifications that indicate the target audience for the event. Possible values are OPERATIONS, SECURITY, and BILLING. Events can be associated with multiple personas to indicate relevance to different teams or roles within an organization."]}
    let make ?arn =
      fun ?service ->
        fun ?eventTypeCode ->
          fun ?eventTypeCategory ->
            fun ?eventScopeCode ->
              fun ?region ->
                fun ?startTime ->
                  fun ?endTime ->
                    fun ?lastUpdatedTime ->
                      fun ?statusCode ->
                        fun ?actionability ->
                          fun ?personas ->
                            fun () ->
                              {
                                arn;
                                service;
                                eventTypeCode;
                                eventTypeCategory;
                                eventScopeCode;
                                region;
                                startTime;
                                endTime;
                                lastUpdatedTime;
                                statusCode;
                                actionability;
                                personas
                              }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:EventArn.to_value));
        ("service", (Option.map x.service ~f:Service.to_value));
        ("eventTypeCode",
          (Option.map x.eventTypeCode ~f:EventTypeCode.to_value));
        ("eventTypeCategory",
          (Option.map x.eventTypeCategory ~f:EventTypeCategory.to_value));
        ("eventScopeCode",
          (Option.map x.eventScopeCode ~f:EventScopeCode.to_value));
        ("region", (Option.map x.region ~f:Region.to_value));
        ("startTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("lastUpdatedTime",
          (Option.map x.lastUpdatedTime ~f:Timestamp.to_value));
        ("statusCode", (Option.map x.statusCode ~f:EventStatusCode.to_value));
        ("actionability",
          (Option.map x.actionability ~f:EventActionability.to_value));
        ("personas", (Option.map x.personas ~f:EventPersonaList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let personas =
        (Option.map ~f:EventPersonaList.of_xml)
          (Xml.child xml_arg0 "personas") in
      let actionability =
        (Option.map ~f:EventActionability.of_xml)
          (Xml.child xml_arg0 "actionability") in
      let statusCode =
        (Option.map ~f:EventStatusCode.of_xml)
          (Xml.child xml_arg0 "statusCode") in
      let lastUpdatedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastUpdatedTime") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startTime") in
      let region =
        (Option.map ~f:Region.of_xml) (Xml.child xml_arg0 "region") in
      let eventScopeCode =
        (Option.map ~f:EventScopeCode.of_xml)
          (Xml.child xml_arg0 "eventScopeCode") in
      let eventTypeCategory =
        (Option.map ~f:EventTypeCategory.of_xml)
          (Xml.child xml_arg0 "eventTypeCategory") in
      let eventTypeCode =
        (Option.map ~f:EventTypeCode.of_xml)
          (Xml.child xml_arg0 "eventTypeCode") in
      let service =
        (Option.map ~f:Service.of_xml) (Xml.child xml_arg0 "service") in
      let arn = (Option.map ~f:EventArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?personas ?actionability ?statusCode ?lastUpdatedTime ?endTime
        ?startTime ?region ?eventScopeCode ?eventTypeCategory ?eventTypeCode
        ?service ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let personas = field_map json__ "personas" EventPersonaList.of_json in
      let actionability =
        field_map json__ "actionability" EventActionability.of_json in
      let statusCode = field_map json__ "statusCode" EventStatusCode.of_json in
      let lastUpdatedTime =
        field_map json__ "lastUpdatedTime" Timestamp.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let startTime = field_map json__ "startTime" Timestamp.of_json in
      let region = field_map json__ "region" Region.of_json in
      let eventScopeCode =
        field_map json__ "eventScopeCode" EventScopeCode.of_json in
      let eventTypeCategory =
        field_map json__ "eventTypeCategory" EventTypeCategory.of_json in
      let eventTypeCode =
        field_map json__ "eventTypeCode" EventTypeCode.of_json in
      let service = field_map json__ "service" Service.of_json in
      let arn = field_map json__ "arn" EventArn.of_json in
      make ?personas ?actionability ?statusCode ?lastUpdatedTime ?endTime
        ?startTime ?region ?eventScopeCode ?eventTypeCategory ?eventTypeCode
        ?service ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Summary information about an event, returned by the DescribeEventsForOrganization operation."]
module AwsAccountIdsList =
  struct
    type nonrec t = AccountId.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AccountId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccountId.of_xml)
    let of_json j =
      list_of_json ~kind:"awsAccountIdsList" ~of_json:AccountId.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventType =
  struct
    type nonrec t =
      {
      service: Service.t option
        [@ocaml.doc
          "The Amazon Web Services service that is affected by the event. For example, EC2, RDS."];
      code: EventTypeCode.t option
        [@ocaml.doc
          "The unique identifier for the event type. The format is AWS_SERVICE_DESCRIPTION ; for example, AWS_EC2_SYSTEM_MAINTENANCE_EVENT."];
      category: EventTypeCategory.t option
        [@ocaml.doc
          "A list of event type category codes. Possible values are issue, accountNotification, or scheduledChange. Currently, the investigation value isn't supported at this time."];
      actionability: EventTypeActionability.t option
        [@ocaml.doc
          "The actionability classification of the event. Possible values are ACTION_REQUIRED, ACTION_MAY_BE_REQUIRED and INFORMATIONAL. Events with ACTION_REQUIRED actionability require customer action to resolve or mitigate the event. Events with ACTION_MAY_BE_REQUIRED actionability indicates that the current status is unknown or conditional and inspection is needed to determine if action is required. Events with INFORMATIONAL actionability are provided for awareness and do not require immediate action."];
      personas: EventTypePersonaList.t option
        [@ocaml.doc
          "A list of persona classifications that indicate the target audience for the event. Possible values are OPERATIONS, SECURITY, and BILLING. Events can be associated with multiple personas to indicate relevance to different teams or roles within an organization."]}
    let make ?service =
      fun ?code ->
        fun ?category ->
          fun ?actionability ->
            fun ?personas ->
              fun () -> { service; code; category; actionability; personas }
    let to_value x =
      structure_to_value
        [("service", (Option.map x.service ~f:Service.to_value));
        ("code", (Option.map x.code ~f:EventTypeCode.to_value));
        ("category", (Option.map x.category ~f:EventTypeCategory.to_value));
        ("actionability",
          (Option.map x.actionability ~f:EventTypeActionability.to_value));
        ("personas",
          (Option.map x.personas ~f:EventTypePersonaList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let personas =
        (Option.map ~f:EventTypePersonaList.of_xml)
          (Xml.child xml_arg0 "personas") in
      let actionability =
        (Option.map ~f:EventTypeActionability.of_xml)
          (Xml.child xml_arg0 "actionability") in
      let category =
        (Option.map ~f:EventTypeCategory.of_xml)
          (Xml.child xml_arg0 "category") in
      let code =
        (Option.map ~f:EventTypeCode.of_xml) (Xml.child xml_arg0 "code") in
      let service =
        (Option.map ~f:Service.of_xml) (Xml.child xml_arg0 "service") in
      make ?personas ?actionability ?category ?code ?service ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let personas = field_map json__ "personas" EventTypePersonaList.of_json in
      let actionability =
        field_map json__ "actionability" EventTypeActionability.of_json in
      let category = field_map json__ "category" EventTypeCategory.of_json in
      let code = field_map json__ "code" EventTypeCode.of_json in
      let service = field_map json__ "service" Service.of_json in
      make ?personas ?actionability ?category ?code ?service ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the metadata about a type of event that is reported by Health. The EventType shows the category, service, and the event type code of the event. For example, an issue might be the category, EC2 the service, and AWS_EC2_SYSTEM_MAINTENANCE_EVENT the event type code. You can use the DescribeEventTypes API operation to return this information about an event. You can also use the Amazon CloudWatch Events console to create a rule so that you can get notified or take action when Health delivers a specific event to your Amazon Web Services account. For more information, see Monitor for Health events with Amazon CloudWatch Events in the Health User Guide."]
module EventTypeActionabilityList =
  struct
    type nonrec t = EventTypeActionability.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:3) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventTypeActionability.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventTypeActionability.of_xml)
    let of_json j =
      list_of_json ~kind:"EventTypeActionabilityList"
        ~of_json:EventTypeActionability.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventTypeCategoryList =
  struct
    type nonrec t = EventTypeCategory.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventTypeCategory.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventTypeCategory.of_xml)
    let of_json j =
      list_of_json ~kind:"EventTypeCategoryList"
        ~of_json:EventTypeCategory.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventTypeCodeList =
  struct
    type nonrec t = EventTypeCode.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventTypeCode.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventTypeCode.of_xml)
    let of_json j =
      list_of_json ~kind:"EventTypeCodeList" ~of_json:EventTypeCode.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventDetailsErrorItem =
  struct
    type nonrec t =
      {
      eventArn: EventArn.t option
        [@ocaml.doc
          "The unique identifier for the event. The event ARN has the arn:aws:health:event-region::event/SERVICE/EVENT_TYPE_CODE/EVENT_TYPE_PLUS_ID format. For example, an event ARN might look like the following: arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-DEF456"];
      errorName: String_.t option [@ocaml.doc "The name of the error."];
      errorMessage: String_.t option
        [@ocaml.doc "A message that describes the error."]}
    let make ?eventArn =
      fun ?errorName ->
        fun ?errorMessage -> fun () -> { eventArn; errorName; errorMessage }
    let to_value x =
      structure_to_value
        [("eventArn", (Option.map x.eventArn ~f:EventArn.to_value));
        ("errorName", (Option.map x.errorName ~f:String_.to_value));
        ("errorMessage", (Option.map x.errorMessage ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorMessage") in
      let errorName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorName") in
      let eventArn =
        (Option.map ~f:EventArn.of_xml) (Xml.child xml_arg0 "eventArn") in
      make ?errorMessage ?errorName ?eventArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let errorMessage = field_map json__ "errorMessage" String_.of_json in
      let errorName = field_map json__ "errorName" String_.of_json in
      let eventArn = field_map json__ "eventArn" EventArn.of_json in
      make ?errorMessage ?errorName ?eventArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Error information returned when a DescribeEventDetails operation can't find a specified event."]
module EventDetails =
  struct
    type nonrec t =
      {
      event: Event.t option
        [@ocaml.doc "Summary information about the event."];
      eventDescription__lc1: EventDescription.t option
        [@ocaml.doc "The most recent description of the event."];
      eventMetadata: EventMetadata.t option
        [@ocaml.doc "Additional metadata about the event."]}
    let make ?event =
      fun ?eventDescription__lc1 ->
        fun ?eventMetadata ->
          fun () -> { event; eventDescription__lc1; eventMetadata }
    let to_value x =
      structure_to_value
        [("event", (Option.map x.event ~f:Event.to_value));
        ("EventDescription__lc1",
          (Option.map x.eventDescription__lc1 ~f:EventDescription.to_value));
        ("eventMetadata",
          (Option.map x.eventMetadata ~f:EventMetadata.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventMetadata =
        (Option.map ~f:EventMetadata.of_xml)
          (Xml.child xml_arg0 "eventMetadata") in
      let eventDescription__lc1 =
        (Option.map ~f:EventDescription.of_xml)
          (Xml.child xml_arg0 "EventDescription__lc1") in
      let event = (Option.map ~f:Event.of_xml) (Xml.child xml_arg0 "event") in
      make ?eventMetadata ?eventDescription__lc1 ?event ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let eventMetadata =
        field_map json__ "eventMetadata" EventMetadata.of_json in
      let eventDescription__lc1 =
        field_map json__ "EventDescription__lc1" EventDescription.of_json in
      let event = field_map json__ "event" Event.of_json in
      make ?eventMetadata ?eventDescription__lc1 ?event ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Detailed information about an event. A combination of an Event object, an EventDescription object, and additional metadata about the event. Returned by the DescribeEventDetails operation."]
module OrganizationEventDetailsErrorItem =
  struct
    type nonrec t =
      {
      awsAccountId: AccountId.t option
        [@ocaml.doc
          "Error information returned when a DescribeEventDetailsForOrganization operation can't find a specified event."];
      eventArn: EventArn.t option
        [@ocaml.doc
          "The unique identifier for the event. The event ARN has the arn:aws:health:event-region::event/SERVICE/EVENT_TYPE_CODE/EVENT_TYPE_PLUS_ID format. For example, an event ARN might look like the following: arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-DEF456"];
      errorName: String_.t option [@ocaml.doc "The name of the error."];
      errorMessage: String_.t option
        [@ocaml.doc
          "A message that describes the error. If you call the DescribeEventDetailsForOrganization operation and receive one of the following errors, follow the recommendations in the message: We couldn't find a public event that matches your request. To find an event that is account specific, you must enter an Amazon Web Services account ID in the request. We couldn't find an account specific event for the specified Amazon Web Services account. To find an event that is public, you must enter a null value for the Amazon Web Services account ID in the request. Your Amazon Web Services account doesn't include the Amazon Web Services Support plan required to use the Health API. You must have either a Business, Enterprise On-Ramp, or Enterprise Support plan."]}
    let make ?awsAccountId =
      fun ?eventArn ->
        fun ?errorName ->
          fun ?errorMessage ->
            fun () -> { awsAccountId; eventArn; errorName; errorMessage }
    let to_value x =
      structure_to_value
        [("awsAccountId", (Option.map x.awsAccountId ~f:AccountId.to_value));
        ("eventArn", (Option.map x.eventArn ~f:EventArn.to_value));
        ("errorName", (Option.map x.errorName ~f:String_.to_value));
        ("errorMessage", (Option.map x.errorMessage ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorMessage") in
      let errorName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorName") in
      let eventArn =
        (Option.map ~f:EventArn.of_xml) (Xml.child xml_arg0 "eventArn") in
      let awsAccountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "awsAccountId") in
      make ?errorMessage ?errorName ?eventArn ?awsAccountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let errorMessage = field_map json__ "errorMessage" String_.of_json in
      let errorName = field_map json__ "errorName" String_.of_json in
      let eventArn = field_map json__ "eventArn" EventArn.of_json in
      let awsAccountId = field_map json__ "awsAccountId" AccountId.of_json in
      make ?errorMessage ?errorName ?eventArn ?awsAccountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Error information returned when a DescribeEventDetailsForOrganization operation can't find a specified event."]
module OrganizationEventDetails =
  struct
    type nonrec t =
      {
      awsAccountId: AccountId.t option
        [@ocaml.doc
          "The 12-digit Amazon Web Services account numbers that contains the affected entities."];
      event: Event.t option ;
      eventDescription__lc1: EventDescription.t option ;
      eventMetadata: EventMetadata.t option
        [@ocaml.doc "Additional metadata about the event."]}
    let make ?awsAccountId =
      fun ?event ->
        fun ?eventDescription__lc1 ->
          fun ?eventMetadata ->
            fun () ->
              { awsAccountId; event; eventDescription__lc1; eventMetadata }
    let to_value x =
      structure_to_value
        [("awsAccountId", (Option.map x.awsAccountId ~f:AccountId.to_value));
        ("event", (Option.map x.event ~f:Event.to_value));
        ("EventDescription__lc1",
          (Option.map x.eventDescription__lc1 ~f:EventDescription.to_value));
        ("eventMetadata",
          (Option.map x.eventMetadata ~f:EventMetadata.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventMetadata =
        (Option.map ~f:EventMetadata.of_xml)
          (Xml.child xml_arg0 "eventMetadata") in
      let eventDescription__lc1 =
        (Option.map ~f:EventDescription.of_xml)
          (Xml.child xml_arg0 "EventDescription__lc1") in
      let event = (Option.map ~f:Event.of_xml) (Xml.child xml_arg0 "event") in
      let awsAccountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "awsAccountId") in
      make ?eventMetadata ?eventDescription__lc1 ?event ?awsAccountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let eventMetadata =
        field_map json__ "eventMetadata" EventMetadata.of_json in
      let eventDescription__lc1 =
        field_map json__ "EventDescription__lc1" EventDescription.of_json in
      let event = field_map json__ "event" Event.of_json in
      let awsAccountId = field_map json__ "awsAccountId" AccountId.of_json in
      make ?eventMetadata ?eventDescription__lc1 ?event ?awsAccountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Detailed information about an event. A combination of an Event object, an EventDescription object, and additional metadata about the event. Returned by the DescribeEventDetailsForOrganization operation."]
module EventAccountFilter =
  struct
    type nonrec t =
      {
      eventArn: EventArn.t
        [@ocaml.doc
          "The unique identifier for the event. The event ARN has the arn:aws:health:event-region::event/SERVICE/EVENT_TYPE_CODE/EVENT_TYPE_PLUS_ID format. For example, an event ARN might look like the following: arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-DEF456"];
      awsAccountId: AccountId.t option
        [@ocaml.doc
          "The 12-digit Amazon Web Services account numbers that contains the affected entities."]}
    let context_ = "EventAccountFilter"
    let make ?awsAccountId =
      fun ~eventArn -> fun () -> { awsAccountId; eventArn }
    let to_value x =
      structure_to_value
        [("eventArn", (Some (EventArn.to_value x.eventArn)));
        ("awsAccountId", (Option.map x.awsAccountId ~f:AccountId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let awsAccountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "awsAccountId") in
      let eventArn =
        EventArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "eventArn") in
      make ?awsAccountId ~eventArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let awsAccountId = field_map json__ "awsAccountId" AccountId.of_json in
      let eventArn = field_map_exn json__ "eventArn" EventArn.of_json in
      make ?awsAccountId ~eventArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The values used to filter results from the DescribeEventDetailsForOrganization and DescribeAffectedEntitiesForOrganization operations."]
module EventAggregate =
  struct
    type nonrec t =
      {
      aggregateValue: AggregateValue.t option
        [@ocaml.doc "The issue type for the associated count."];
      count: Count.t option
        [@ocaml.doc "The number of events of the associated issue type."]}
    let make ?aggregateValue =
      fun ?count -> fun () -> { aggregateValue; count }
    let to_value x =
      structure_to_value
        [("aggregateValue",
           (Option.map x.aggregateValue ~f:AggregateValue.to_value));
        ("count", (Option.map x.count ~f:Count.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let count = (Option.map ~f:Count.of_xml) (Xml.child xml_arg0 "count") in
      let aggregateValue =
        (Option.map ~f:AggregateValue.of_xml)
          (Xml.child xml_arg0 "aggregateValue") in
      make ?count ?aggregateValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let count = field_map json__ "count" Count.of_json in
      let aggregateValue =
        field_map json__ "aggregateValue" AggregateValue.of_json in
      make ?count ?aggregateValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The number of events of each issue type. Returned by the DescribeEventAggregates operation."]
module EntityAggregate =
  struct
    type nonrec t =
      {
      eventArn: EventArn.t option
        [@ocaml.doc
          "The unique identifier for the event. The event ARN has the arn:aws:health:event-region::event/SERVICE/EVENT_TYPE_CODE/EVENT_TYPE_PLUS_ID format. For example, an event ARN might look like the following: arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-DEF456"];
      count: Count.t option
        [@ocaml.doc
          "The number of entities that match the criteria for the specified events."];
      statuses: EntityStatuses.t option
        [@ocaml.doc
          "The number of affected entities aggregated by the entity status codes."]}
    let make ?eventArn =
      fun ?count -> fun ?statuses -> fun () -> { eventArn; count; statuses }
    let to_value x =
      structure_to_value
        [("eventArn", (Option.map x.eventArn ~f:EventArn.to_value));
        ("count", (Option.map x.count ~f:Count.to_value));
        ("statuses", (Option.map x.statuses ~f:EntityStatuses.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statuses =
        (Option.map ~f:EntityStatuses.of_xml) (Xml.child xml_arg0 "statuses") in
      let count = (Option.map ~f:Count.of_xml) (Xml.child xml_arg0 "count") in
      let eventArn =
        (Option.map ~f:EventArn.of_xml) (Xml.child xml_arg0 "eventArn") in
      make ?statuses ?count ?eventArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let statuses = field_map json__ "statuses" EntityStatuses.of_json in
      let count = field_map json__ "count" Count.of_json in
      let eventArn = field_map json__ "eventArn" EventArn.of_json in
      make ?statuses ?count ?eventArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The number of entities that are affected by one or more events. Returned by the DescribeEntityAggregates operation."]
module OrganizationEntityAggregate =
  struct
    type nonrec t =
      {
      eventArn: EventArn.t option
        [@ocaml.doc
          "A list of event ARNs (unique identifiers). For example: \"arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-CDE456\", \"arn:aws:health:us-west-1::event/EBS/AWS_EBS_LOST_VOLUME/AWS_EBS_LOST_VOLUME_CHI789_JKL101\""];
      count: Count.t option
        [@ocaml.doc
          "The number of entities for the organization that match the filter criteria for the specified events."];
      statuses: EntityStatuses.t option
        [@ocaml.doc
          "The number of affected entities aggregated by the entitiy status codes."];
      accounts: AccountEntityAggregatesList.t option
        [@ocaml.doc
          "A list of entity aggregates for each of the specified accounts in your organization that are affected by a specific event. If there are no awsAccountIds provided in the request, this field will be empty in the response."]}
    let make ?eventArn =
      fun ?count ->
        fun ?statuses ->
          fun ?accounts -> fun () -> { eventArn; count; statuses; accounts }
    let to_value x =
      structure_to_value
        [("eventArn", (Option.map x.eventArn ~f:EventArn.to_value));
        ("count", (Option.map x.count ~f:Count.to_value));
        ("statuses", (Option.map x.statuses ~f:EntityStatuses.to_value));
        ("accounts",
          (Option.map x.accounts ~f:AccountEntityAggregatesList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accounts =
        (Option.map ~f:AccountEntityAggregatesList.of_xml)
          (Xml.child xml_arg0 "accounts") in
      let statuses =
        (Option.map ~f:EntityStatuses.of_xml) (Xml.child xml_arg0 "statuses") in
      let count = (Option.map ~f:Count.of_xml) (Xml.child xml_arg0 "count") in
      let eventArn =
        (Option.map ~f:EventArn.of_xml) (Xml.child xml_arg0 "eventArn") in
      make ?accounts ?statuses ?count ?eventArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let accounts =
        field_map json__ "accounts" AccountEntityAggregatesList.of_json in
      let statuses = field_map json__ "statuses" EntityStatuses.of_json in
      let count = field_map json__ "count" Count.of_json in
      let eventArn = field_map json__ "eventArn" EventArn.of_json in
      make ?accounts ?statuses ?count ?eventArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The aggregate results of entities affected by the specified event in your organization. The results are aggregated by the entity status codes for the specified set of accountsIDs."]
module AffectedEntity =
  struct
    type nonrec t =
      {
      entityArn: EntityArn.t option
        [@ocaml.doc
          "The unique identifier for the entity. Format: arn:aws:health:entity-region:aws-account:entity/entity-id . Example: arn:aws:health:us-east-1:111222333444:entity/AVh5GGT7ul1arKr1sE1K"];
      eventArn: EventArn.t option
        [@ocaml.doc
          "The unique identifier for the event. The event ARN has the arn:aws:health:event-region::event/SERVICE/EVENT_TYPE_CODE/EVENT_TYPE_PLUS_ID format. For example, an event ARN might look like the following: arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-DEF456"];
      entityValue: EntityValue.t option
        [@ocaml.doc "The ID of the affected entity."];
      entityUrl: EntityUrl.t option
        [@ocaml.doc "The URL of the affected entity."];
      awsAccountId: AccountId.t option
        [@ocaml.doc
          "The 12-digit Amazon Web Services account number that contains the affected entity."];
      lastUpdatedTime: Timestamp.t option
        [@ocaml.doc "The most recent time that the entity was updated."];
      statusCode: EntityStatusCode.t option
        [@ocaml.doc
          "The most recent status of the entity affected by the event. The possible values are IMPAIRED, UNIMPAIRED, UNKNOWN, PENDING, and RESOLVED."];
      tags: TagSet.t option
        [@ocaml.doc
          "A map of entity tags attached to the affected entity. Currently, the tags property isn't supported."];
      entityMetadata: EntityMetadata.t option
        [@ocaml.doc "Additional metadata about the affected entity."]}
    let make ?entityArn =
      fun ?eventArn ->
        fun ?entityValue ->
          fun ?entityUrl ->
            fun ?awsAccountId ->
              fun ?lastUpdatedTime ->
                fun ?statusCode ->
                  fun ?tags ->
                    fun ?entityMetadata ->
                      fun () ->
                        {
                          entityArn;
                          eventArn;
                          entityValue;
                          entityUrl;
                          awsAccountId;
                          lastUpdatedTime;
                          statusCode;
                          tags;
                          entityMetadata
                        }
    let to_value x =
      structure_to_value
        [("entityArn", (Option.map x.entityArn ~f:EntityArn.to_value));
        ("eventArn", (Option.map x.eventArn ~f:EventArn.to_value));
        ("entityValue", (Option.map x.entityValue ~f:EntityValue.to_value));
        ("entityUrl", (Option.map x.entityUrl ~f:EntityUrl.to_value));
        ("awsAccountId", (Option.map x.awsAccountId ~f:AccountId.to_value));
        ("lastUpdatedTime",
          (Option.map x.lastUpdatedTime ~f:Timestamp.to_value));
        ("statusCode",
          (Option.map x.statusCode ~f:EntityStatusCode.to_value));
        ("tags", (Option.map x.tags ~f:TagSet.to_value));
        ("entityMetadata",
          (Option.map x.entityMetadata ~f:EntityMetadata.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let entityMetadata =
        (Option.map ~f:EntityMetadata.of_xml)
          (Xml.child xml_arg0 "entityMetadata") in
      let tags = (Option.map ~f:TagSet.of_xml) (Xml.child xml_arg0 "tags") in
      let statusCode =
        (Option.map ~f:EntityStatusCode.of_xml)
          (Xml.child xml_arg0 "statusCode") in
      let lastUpdatedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastUpdatedTime") in
      let awsAccountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "awsAccountId") in
      let entityUrl =
        (Option.map ~f:EntityUrl.of_xml) (Xml.child xml_arg0 "entityUrl") in
      let entityValue =
        (Option.map ~f:EntityValue.of_xml) (Xml.child xml_arg0 "entityValue") in
      let eventArn =
        (Option.map ~f:EventArn.of_xml) (Xml.child xml_arg0 "eventArn") in
      let entityArn =
        (Option.map ~f:EntityArn.of_xml) (Xml.child xml_arg0 "entityArn") in
      make ?entityMetadata ?tags ?statusCode ?lastUpdatedTime ?awsAccountId
        ?entityUrl ?entityValue ?eventArn ?entityArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let entityMetadata =
        field_map json__ "entityMetadata" EntityMetadata.of_json in
      let tags = field_map json__ "tags" TagSet.of_json in
      let statusCode = field_map json__ "statusCode" EntityStatusCode.of_json in
      let lastUpdatedTime =
        field_map json__ "lastUpdatedTime" Timestamp.of_json in
      let awsAccountId = field_map json__ "awsAccountId" AccountId.of_json in
      let entityUrl = field_map json__ "entityUrl" EntityUrl.of_json in
      let entityValue = field_map json__ "entityValue" EntityValue.of_json in
      let eventArn = field_map json__ "eventArn" EventArn.of_json in
      let entityArn = field_map json__ "entityArn" EntityArn.of_json in
      make ?entityMetadata ?tags ?statusCode ?lastUpdatedTime ?awsAccountId
        ?entityUrl ?entityValue ?eventArn ?entityArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about an entity that is affected by a Health event."]
module OrganizationAffectedEntitiesErrorItem =
  struct
    type nonrec t =
      {
      awsAccountId: AccountId.t option
        [@ocaml.doc
          "The 12-digit Amazon Web Services account numbers that contains the affected entities."];
      eventArn: EventArn.t option
        [@ocaml.doc
          "The unique identifier for the event. The event ARN has the arn:aws:health:event-region::event/SERVICE/EVENT_TYPE_CODE/EVENT_TYPE_PLUS_ID format. For example, an event ARN might look like the following: arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-DEF456"];
      errorName: String_.t option [@ocaml.doc "The name of the error."];
      errorMessage: String_.t option
        [@ocaml.doc
          "A message that describes the error. Follow the error message and retry your request. For example, the InvalidAccountInputError error message appears if you call the DescribeAffectedEntitiesForOrganization operation and specify the AccountSpecific value for the EventScopeCode parameter, but don't specify an Amazon Web Services account."]}
    let make ?awsAccountId =
      fun ?eventArn ->
        fun ?errorName ->
          fun ?errorMessage ->
            fun () -> { awsAccountId; eventArn; errorName; errorMessage }
    let to_value x =
      structure_to_value
        [("awsAccountId", (Option.map x.awsAccountId ~f:AccountId.to_value));
        ("eventArn", (Option.map x.eventArn ~f:EventArn.to_value));
        ("errorName", (Option.map x.errorName ~f:String_.to_value));
        ("errorMessage", (Option.map x.errorMessage ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorMessage") in
      let errorName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorName") in
      let eventArn =
        (Option.map ~f:EventArn.of_xml) (Xml.child xml_arg0 "eventArn") in
      let awsAccountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "awsAccountId") in
      make ?errorMessage ?errorName ?eventArn ?awsAccountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let errorMessage = field_map json__ "errorMessage" String_.of_json in
      let errorName = field_map json__ "errorName" String_.of_json in
      let eventArn = field_map json__ "eventArn" EventArn.of_json in
      let awsAccountId = field_map json__ "awsAccountId" AccountId.of_json in
      make ?errorMessage ?errorName ?eventArn ?awsAccountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Error information returned when a DescribeAffectedEntitiesForOrganization operation can't find or process a specific entity."]
module EntityAccountFilter =
  struct
    type nonrec t =
      {
      eventArn: EventArn.t
        [@ocaml.doc
          "The unique identifier for the event. The event ARN has the arn:aws:health:event-region::event/SERVICE/EVENT_TYPE_CODE/EVENT_TYPE_PLUS_ID format. For example, an event ARN might look like the following: arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-DEF456"];
      awsAccountId: AccountId.t option
        [@ocaml.doc
          "The 12-digit Amazon Web Services account numbers that contains the affected entities."];
      statusCodes: EntityStatusCodeList.t option
        [@ocaml.doc "A list of entity status codes."]}
    let context_ = "EntityAccountFilter"
    let make ?awsAccountId =
      fun ?statusCodes ->
        fun ~eventArn -> fun () -> { awsAccountId; statusCodes; eventArn }
    let to_value x =
      structure_to_value
        [("eventArn", (Some (EventArn.to_value x.eventArn)));
        ("awsAccountId", (Option.map x.awsAccountId ~f:AccountId.to_value));
        ("statusCodes",
          (Option.map x.statusCodes ~f:EntityStatusCodeList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statusCodes =
        (Option.map ~f:EntityStatusCodeList.of_xml)
          (Xml.child xml_arg0 "statusCodes") in
      let awsAccountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "awsAccountId") in
      let eventArn =
        EventArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "eventArn") in
      make ?statusCodes ?awsAccountId ~eventArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let statusCodes =
        field_map json__ "statusCodes" EntityStatusCodeList.of_json in
      let awsAccountId = field_map json__ "awsAccountId" AccountId.of_json in
      let eventArn = field_map_exn json__ "eventArn" EventArn.of_json in
      make ?statusCodes ?awsAccountId ~eventArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A JSON set of elements including the awsAccountId, eventArn and a set of statusCodes."]
module HealthServiceAccessStatusForOrganization =
  struct
    type nonrec t = string
    let context_ = "healthServiceAccessStatusForOrganization"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"healthServiceAccessStatusForOrganization" j
    let to_json = simple_to_json to_value
  end
module EventList =
  struct
    type nonrec t = Event.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Event.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Event.of_xml)
    let of_json j = list_of_json ~kind:"EventList" ~of_json:Event.of_json j
    let to_json v = composed_to_json to_value v
  end
module InvalidPaginationToken =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified pagination token (nextToken) is not valid."]
module UnsupportedLocale =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified locale is not supported."]
module NextToken =
  struct
    type nonrec t = string
    let context_ = "nextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:4) >>=
             (fun () ->
                (check_string_max i ~max:10000) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9=/+_.-]{4,10000}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"nextToken" j
    let to_json = simple_to_json to_value
  end
module EventFilter =
  struct
    type nonrec t =
      {
      actionabilities: EventActionabilityList.t option
        [@ocaml.doc
          "A list of actionability values to filter events. Use this to filter events based on whether they require action (ACTION_REQUIRED), may require action (ACTION_MAY_BE_REQUIRED) or are informational (INFORMATIONAL)."];
      eventArns: EventArnList.t option
        [@ocaml.doc
          "A list of event ARNs (unique identifiers). For example: \"arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-CDE456\", \"arn:aws:health:us-west-1::event/EBS/AWS_EBS_LOST_VOLUME/AWS_EBS_LOST_VOLUME_CHI789_JKL101\""];
      eventTypeCodes: EventTypeList__lc1.t option
        [@ocaml.doc
          "A list of unique identifiers for event types. For example, \"AWS_EC2_SYSTEM_MAINTENANCE_EVENT\",\"AWS_RDS_MAINTENANCE_SCHEDULED\"."];
      services: ServiceList.t option
        [@ocaml.doc
          "The Amazon Web Services services associated with the event. For example, EC2, RDS."];
      regions: RegionList.t option
        [@ocaml.doc "A list of Amazon Web Services Regions."];
      availabilityZones: AvailabilityZones.t option
        [@ocaml.doc "A list of Amazon Web Services Availability Zones."];
      startTimes: DateTimeRangeList.t option
        [@ocaml.doc "A list of dates and times that the event began."];
      endTimes: DateTimeRangeList.t option
        [@ocaml.doc "A list of dates and times that the event ended."];
      lastUpdatedTimes: DateTimeRangeList.t option
        [@ocaml.doc
          "A list of dates and times that the event was last updated."];
      entityArns: EntityArnList.t option
        [@ocaml.doc "A list of entity ARNs (unique identifiers)."];
      entityValues: EntityValueList.t option
        [@ocaml.doc
          "A list of entity identifiers, such as EC2 instance IDs (i-34ab692e) or EBS volumes (vol-426ab23e)."];
      eventTypeCategories: EventTypeCategoryList__lc1.t option
        [@ocaml.doc
          "A list of event type category codes. Possible values are issue, accountNotification, or scheduledChange. Currently, the investigation value isn't supported at this time."];
      tags: TagFilter.t option
        [@ocaml.doc
          "A map of entity tags attached to the affected entity. Currently, the tags property isn't supported."];
      eventStatusCodes: EventStatusCodeList.t option
        [@ocaml.doc "A list of event status codes."];
      personas: EventPersonaList.t option
        [@ocaml.doc
          "A list of persona values to filter events. Use this to filter events based on their target audience: OPERATIONS, SECURITY, or BILLING."]}
    let make ?actionabilities =
      fun ?eventArns ->
        fun ?eventTypeCodes ->
          fun ?services ->
            fun ?regions ->
              fun ?availabilityZones ->
                fun ?startTimes ->
                  fun ?endTimes ->
                    fun ?lastUpdatedTimes ->
                      fun ?entityArns ->
                        fun ?entityValues ->
                          fun ?eventTypeCategories ->
                            fun ?tags ->
                              fun ?eventStatusCodes ->
                                fun ?personas ->
                                  fun () ->
                                    {
                                      actionabilities;
                                      eventArns;
                                      eventTypeCodes;
                                      services;
                                      regions;
                                      availabilityZones;
                                      startTimes;
                                      endTimes;
                                      lastUpdatedTimes;
                                      entityArns;
                                      entityValues;
                                      eventTypeCategories;
                                      tags;
                                      eventStatusCodes;
                                      personas
                                    }
    let to_value x =
      structure_to_value
        [("actionabilities",
           (Option.map x.actionabilities ~f:EventActionabilityList.to_value));
        ("eventArns", (Option.map x.eventArns ~f:EventArnList.to_value));
        ("eventTypeCodes",
          (Option.map x.eventTypeCodes ~f:EventTypeList__lc1.to_value));
        ("services", (Option.map x.services ~f:ServiceList.to_value));
        ("regions", (Option.map x.regions ~f:RegionList.to_value));
        ("availabilityZones",
          (Option.map x.availabilityZones ~f:AvailabilityZones.to_value));
        ("startTimes",
          (Option.map x.startTimes ~f:DateTimeRangeList.to_value));
        ("endTimes", (Option.map x.endTimes ~f:DateTimeRangeList.to_value));
        ("lastUpdatedTimes",
          (Option.map x.lastUpdatedTimes ~f:DateTimeRangeList.to_value));
        ("entityArns", (Option.map x.entityArns ~f:EntityArnList.to_value));
        ("entityValues",
          (Option.map x.entityValues ~f:EntityValueList.to_value));
        ("eventTypeCategories",
          (Option.map x.eventTypeCategories
             ~f:EventTypeCategoryList__lc1.to_value));
        ("tags", (Option.map x.tags ~f:TagFilter.to_value));
        ("eventStatusCodes",
          (Option.map x.eventStatusCodes ~f:EventStatusCodeList.to_value));
        ("personas", (Option.map x.personas ~f:EventPersonaList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let personas =
        (Option.map ~f:EventPersonaList.of_xml)
          (Xml.child xml_arg0 "personas") in
      let eventStatusCodes =
        (Option.map ~f:EventStatusCodeList.of_xml)
          (Xml.child xml_arg0 "eventStatusCodes") in
      let tags = (Option.map ~f:TagFilter.of_xml) (Xml.child xml_arg0 "tags") in
      let eventTypeCategories =
        (Option.map ~f:EventTypeCategoryList__lc1.of_xml)
          (Xml.child xml_arg0 "eventTypeCategories") in
      let entityValues =
        (Option.map ~f:EntityValueList.of_xml)
          (Xml.child xml_arg0 "entityValues") in
      let entityArns =
        (Option.map ~f:EntityArnList.of_xml)
          (Xml.child xml_arg0 "entityArns") in
      let lastUpdatedTimes =
        (Option.map ~f:DateTimeRangeList.of_xml)
          (Xml.child xml_arg0 "lastUpdatedTimes") in
      let endTimes =
        (Option.map ~f:DateTimeRangeList.of_xml)
          (Xml.child xml_arg0 "endTimes") in
      let startTimes =
        (Option.map ~f:DateTimeRangeList.of_xml)
          (Xml.child xml_arg0 "startTimes") in
      let availabilityZones =
        (Option.map ~f:AvailabilityZones.of_xml)
          (Xml.child xml_arg0 "availabilityZones") in
      let regions =
        (Option.map ~f:RegionList.of_xml) (Xml.child xml_arg0 "regions") in
      let services =
        (Option.map ~f:ServiceList.of_xml) (Xml.child xml_arg0 "services") in
      let eventTypeCodes =
        (Option.map ~f:EventTypeList__lc1.of_xml)
          (Xml.child xml_arg0 "eventTypeCodes") in
      let eventArns =
        (Option.map ~f:EventArnList.of_xml) (Xml.child xml_arg0 "eventArns") in
      let actionabilities =
        (Option.map ~f:EventActionabilityList.of_xml)
          (Xml.child xml_arg0 "actionabilities") in
      make ?personas ?eventStatusCodes ?tags ?eventTypeCategories
        ?entityValues ?entityArns ?lastUpdatedTimes ?endTimes ?startTimes
        ?availabilityZones ?regions ?services ?eventTypeCodes ?eventArns
        ?actionabilities ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let personas = field_map json__ "personas" EventPersonaList.of_json in
      let eventStatusCodes =
        field_map json__ "eventStatusCodes" EventStatusCodeList.of_json in
      let tags = field_map json__ "tags" TagFilter.of_json in
      let eventTypeCategories =
        field_map json__ "eventTypeCategories"
          EventTypeCategoryList__lc1.of_json in
      let entityValues =
        field_map json__ "entityValues" EntityValueList.of_json in
      let entityArns = field_map json__ "entityArns" EntityArnList.of_json in
      let lastUpdatedTimes =
        field_map json__ "lastUpdatedTimes" DateTimeRangeList.of_json in
      let endTimes = field_map json__ "endTimes" DateTimeRangeList.of_json in
      let startTimes =
        field_map json__ "startTimes" DateTimeRangeList.of_json in
      let availabilityZones =
        field_map json__ "availabilityZones" AvailabilityZones.of_json in
      let regions = field_map json__ "regions" RegionList.of_json in
      let services = field_map json__ "services" ServiceList.of_json in
      let eventTypeCodes =
        field_map json__ "eventTypeCodes" EventTypeList__lc1.of_json in
      let eventArns = field_map json__ "eventArns" EventArnList.of_json in
      let actionabilities =
        field_map json__ "actionabilities" EventActionabilityList.of_json in
      make ?personas ?eventStatusCodes ?tags ?eventTypeCategories
        ?entityValues ?entityArns ?lastUpdatedTimes ?endTimes ?startTimes
        ?availabilityZones ?regions ?services ?eventTypeCodes ?eventArns
        ?actionabilities ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The values to use to filter results from the DescribeEvents and DescribeEventAggregates operations."]
module Locale =
  struct
    type nonrec t = string
    let context_ = "locale"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:2) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:".{2,256}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"locale" j
    let to_json = simple_to_json to_value
  end
module MaxResultsLowerRange =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for maxResultsLowerRange" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module OrganizationEventList =
  struct
    type nonrec t = OrganizationEvent.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:OrganizationEvent.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:OrganizationEvent.of_xml)
    let of_json j =
      list_of_json ~kind:"OrganizationEventList"
        ~of_json:OrganizationEvent.of_json j
    let to_json v = composed_to_json to_value v
  end
module OrganizationEventFilter =
  struct
    type nonrec t =
      {
      actionabilities: EventActionabilityList.t option
        [@ocaml.doc
          "A list of actionability values to filter events. Use this to filter events based on whether they require action (ACTION_REQUIRED), may require action (ACTION_MAY_BE_REQUIRED) or are informational (INFORMATIONAL)."];
      eventTypeCodes: EventTypeList__lc1.t option
        [@ocaml.doc
          "A list of unique identifiers for event types. For example, \"AWS_EC2_SYSTEM_MAINTENANCE_EVENT\",\"AWS_RDS_MAINTENANCE_SCHEDULED\"."];
      awsAccountIds: AwsAccountIdsList.t option
        [@ocaml.doc
          "A list of 12-digit Amazon Web Services account numbers that contains the affected entities."];
      services: ServiceList.t option
        [@ocaml.doc
          "The Amazon Web Services services associated with the event. For example, EC2, RDS."];
      regions: RegionList.t option
        [@ocaml.doc "A list of Amazon Web Services Regions."];
      startTime: DateTimeRange.t option ;
      endTime: DateTimeRange.t option ;
      lastUpdatedTime: DateTimeRange.t option ;
      entityArns: EntityArnList.t option
        [@ocaml.doc "A list of entity ARNs (unique identifiers)."];
      entityValues: EntityValueList.t option
        [@ocaml.doc
          "A list of entity identifiers, such as EC2 instance IDs (i-34ab692e) or EBS volumes (vol-426ab23e)."];
      eventTypeCategories: EventTypeCategoryList__lc1.t option
        [@ocaml.doc
          "A list of event type category codes. Possible values are issue, accountNotification, or scheduledChange. Currently, the investigation value isn't supported at this time."];
      eventStatusCodes: EventStatusCodeList.t option
        [@ocaml.doc "A list of event status codes."];
      personas: EventPersonaList.t option
        [@ocaml.doc
          "A list of persona values to filter events. Use this to filter events based on their target audience: OPERATIONS, SECURITY, or BILLING."]}
    let make ?actionabilities =
      fun ?eventTypeCodes ->
        fun ?awsAccountIds ->
          fun ?services ->
            fun ?regions ->
              fun ?startTime ->
                fun ?endTime ->
                  fun ?lastUpdatedTime ->
                    fun ?entityArns ->
                      fun ?entityValues ->
                        fun ?eventTypeCategories ->
                          fun ?eventStatusCodes ->
                            fun ?personas ->
                              fun () ->
                                {
                                  actionabilities;
                                  eventTypeCodes;
                                  awsAccountIds;
                                  services;
                                  regions;
                                  startTime;
                                  endTime;
                                  lastUpdatedTime;
                                  entityArns;
                                  entityValues;
                                  eventTypeCategories;
                                  eventStatusCodes;
                                  personas
                                }
    let to_value x =
      structure_to_value
        [("actionabilities",
           (Option.map x.actionabilities ~f:EventActionabilityList.to_value));
        ("eventTypeCodes",
          (Option.map x.eventTypeCodes ~f:EventTypeList__lc1.to_value));
        ("awsAccountIds",
          (Option.map x.awsAccountIds ~f:AwsAccountIdsList.to_value));
        ("services", (Option.map x.services ~f:ServiceList.to_value));
        ("regions", (Option.map x.regions ~f:RegionList.to_value));
        ("startTime", (Option.map x.startTime ~f:DateTimeRange.to_value));
        ("endTime", (Option.map x.endTime ~f:DateTimeRange.to_value));
        ("lastUpdatedTime",
          (Option.map x.lastUpdatedTime ~f:DateTimeRange.to_value));
        ("entityArns", (Option.map x.entityArns ~f:EntityArnList.to_value));
        ("entityValues",
          (Option.map x.entityValues ~f:EntityValueList.to_value));
        ("eventTypeCategories",
          (Option.map x.eventTypeCategories
             ~f:EventTypeCategoryList__lc1.to_value));
        ("eventStatusCodes",
          (Option.map x.eventStatusCodes ~f:EventStatusCodeList.to_value));
        ("personas", (Option.map x.personas ~f:EventPersonaList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let personas =
        (Option.map ~f:EventPersonaList.of_xml)
          (Xml.child xml_arg0 "personas") in
      let eventStatusCodes =
        (Option.map ~f:EventStatusCodeList.of_xml)
          (Xml.child xml_arg0 "eventStatusCodes") in
      let eventTypeCategories =
        (Option.map ~f:EventTypeCategoryList__lc1.of_xml)
          (Xml.child xml_arg0 "eventTypeCategories") in
      let entityValues =
        (Option.map ~f:EntityValueList.of_xml)
          (Xml.child xml_arg0 "entityValues") in
      let entityArns =
        (Option.map ~f:EntityArnList.of_xml)
          (Xml.child xml_arg0 "entityArns") in
      let lastUpdatedTime =
        (Option.map ~f:DateTimeRange.of_xml)
          (Xml.child xml_arg0 "lastUpdatedTime") in
      let endTime =
        (Option.map ~f:DateTimeRange.of_xml) (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:DateTimeRange.of_xml) (Xml.child xml_arg0 "startTime") in
      let regions =
        (Option.map ~f:RegionList.of_xml) (Xml.child xml_arg0 "regions") in
      let services =
        (Option.map ~f:ServiceList.of_xml) (Xml.child xml_arg0 "services") in
      let awsAccountIds =
        (Option.map ~f:AwsAccountIdsList.of_xml)
          (Xml.child xml_arg0 "awsAccountIds") in
      let eventTypeCodes =
        (Option.map ~f:EventTypeList__lc1.of_xml)
          (Xml.child xml_arg0 "eventTypeCodes") in
      let actionabilities =
        (Option.map ~f:EventActionabilityList.of_xml)
          (Xml.child xml_arg0 "actionabilities") in
      make ?personas ?eventStatusCodes ?eventTypeCategories ?entityValues
        ?entityArns ?lastUpdatedTime ?endTime ?startTime ?regions ?services
        ?awsAccountIds ?eventTypeCodes ?actionabilities ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let personas = field_map json__ "personas" EventPersonaList.of_json in
      let eventStatusCodes =
        field_map json__ "eventStatusCodes" EventStatusCodeList.of_json in
      let eventTypeCategories =
        field_map json__ "eventTypeCategories"
          EventTypeCategoryList__lc1.of_json in
      let entityValues =
        field_map json__ "entityValues" EntityValueList.of_json in
      let entityArns = field_map json__ "entityArns" EntityArnList.of_json in
      let lastUpdatedTime =
        field_map json__ "lastUpdatedTime" DateTimeRange.of_json in
      let endTime = field_map json__ "endTime" DateTimeRange.of_json in
      let startTime = field_map json__ "startTime" DateTimeRange.of_json in
      let regions = field_map json__ "regions" RegionList.of_json in
      let services = field_map json__ "services" ServiceList.of_json in
      let awsAccountIds =
        field_map json__ "awsAccountIds" AwsAccountIdsList.of_json in
      let eventTypeCodes =
        field_map json__ "eventTypeCodes" EventTypeList__lc1.of_json in
      let actionabilities =
        field_map json__ "actionabilities" EventActionabilityList.of_json in
      make ?personas ?eventStatusCodes ?eventTypeCategories ?entityValues
        ?entityArns ?lastUpdatedTime ?endTime ?startTime ?regions ?services
        ?awsAccountIds ?eventTypeCodes ?actionabilities ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The values to filter results from the DescribeEventsForOrganization operation."]
module EventTypeList =
  struct
    type nonrec t = EventType.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventType.of_xml)
    let of_json j =
      list_of_json ~kind:"EventTypeList" ~of_json:EventType.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventTypeFilter =
  struct
    type nonrec t =
      {
      eventTypeCodes: EventTypeCodeList.t option
        [@ocaml.doc "A list of event type codes."];
      services: ServiceList.t option
        [@ocaml.doc
          "The Amazon Web Services services associated with the event. For example, EC2, RDS."];
      eventTypeCategories: EventTypeCategoryList.t option
        [@ocaml.doc
          "A list of event type category codes. Possible values are issue, accountNotification, or scheduledChange. Currently, the investigation value isn't supported at this time."];
      actionabilities: EventTypeActionabilityList.t option
        [@ocaml.doc
          "A list of actionability values to filter event types. Possible values are ACTION_REQUIRED, ACTION_MAY_BE_REQUIRED and INFORMATIONAL."];
      personas: EventTypePersonaList.t option
        [@ocaml.doc
          "A list of persona classifications to filter event types. Possible values are OPERATIONS, SECURITY, and BILLING."]}
    let make ?eventTypeCodes =
      fun ?services ->
        fun ?eventTypeCategories ->
          fun ?actionabilities ->
            fun ?personas ->
              fun () ->
                {
                  eventTypeCodes;
                  services;
                  eventTypeCategories;
                  actionabilities;
                  personas
                }
    let to_value x =
      structure_to_value
        [("eventTypeCodes",
           (Option.map x.eventTypeCodes ~f:EventTypeCodeList.to_value));
        ("services", (Option.map x.services ~f:ServiceList.to_value));
        ("eventTypeCategories",
          (Option.map x.eventTypeCategories ~f:EventTypeCategoryList.to_value));
        ("actionabilities",
          (Option.map x.actionabilities
             ~f:EventTypeActionabilityList.to_value));
        ("personas",
          (Option.map x.personas ~f:EventTypePersonaList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let personas =
        (Option.map ~f:EventTypePersonaList.of_xml)
          (Xml.child xml_arg0 "personas") in
      let actionabilities =
        (Option.map ~f:EventTypeActionabilityList.of_xml)
          (Xml.child xml_arg0 "actionabilities") in
      let eventTypeCategories =
        (Option.map ~f:EventTypeCategoryList.of_xml)
          (Xml.child xml_arg0 "eventTypeCategories") in
      let services =
        (Option.map ~f:ServiceList.of_xml) (Xml.child xml_arg0 "services") in
      let eventTypeCodes =
        (Option.map ~f:EventTypeCodeList.of_xml)
          (Xml.child xml_arg0 "eventTypeCodes") in
      make ?personas ?actionabilities ?eventTypeCategories ?services
        ?eventTypeCodes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let personas = field_map json__ "personas" EventTypePersonaList.of_json in
      let actionabilities =
        field_map json__ "actionabilities" EventTypeActionabilityList.of_json in
      let eventTypeCategories =
        field_map json__ "eventTypeCategories" EventTypeCategoryList.of_json in
      let services = field_map json__ "services" ServiceList.of_json in
      let eventTypeCodes =
        field_map json__ "eventTypeCodes" EventTypeCodeList.of_json in
      make ?personas ?actionabilities ?eventTypeCategories ?services
        ?eventTypeCodes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The values to use to filter results from the DescribeEventTypes operation."]
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:10));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for maxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DescribeEventDetailsFailedSet =
  struct
    type nonrec t = EventDetailsErrorItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventDetailsErrorItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventDetailsErrorItem.of_xml)
    let of_json j =
      list_of_json ~kind:"DescribeEventDetailsFailedSet"
        ~of_json:EventDetailsErrorItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module DescribeEventDetailsSuccessfulSet =
  struct
    type nonrec t = EventDetails.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventDetails.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventDetails.of_xml)
    let of_json j =
      list_of_json ~kind:"DescribeEventDetailsSuccessfulSet"
        ~of_json:EventDetails.of_json j
    let to_json v = composed_to_json to_value v
  end
module DescribeEventDetailsForOrganizationFailedSet =
  struct
    type nonrec t = OrganizationEventDetailsErrorItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:OrganizationEventDetailsErrorItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:OrganizationEventDetailsErrorItem.of_xml)
    let of_json j =
      list_of_json ~kind:"DescribeEventDetailsForOrganizationFailedSet"
        ~of_json:OrganizationEventDetailsErrorItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module DescribeEventDetailsForOrganizationSuccessfulSet =
  struct
    type nonrec t = OrganizationEventDetails.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:OrganizationEventDetails.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:OrganizationEventDetails.of_xml)
    let of_json j =
      list_of_json ~kind:"DescribeEventDetailsForOrganizationSuccessfulSet"
        ~of_json:OrganizationEventDetails.of_json j
    let to_json v = composed_to_json to_value v
  end
module OrganizationEventDetailFiltersList =
  struct
    type nonrec t = EventAccountFilter.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventAccountFilter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventAccountFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"OrganizationEventDetailFiltersList"
        ~of_json:EventAccountFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventAggregateList =
  struct
    type nonrec t = EventAggregate.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventAggregate.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventAggregate.of_xml)
    let of_json j =
      list_of_json ~kind:"EventAggregateList" ~of_json:EventAggregate.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module EventAggregateField =
  struct
    type nonrec t =
      | EventTypeCategory 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | EventTypeCategory -> "eventTypeCategory"
      | Non_static_id s -> s
    let of_string =
      function
      | "eventTypeCategory" -> EventTypeCategory
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration eventAggregateField" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"eventAggregateField" j)
    let to_json = simple_to_json to_value
  end
module EntityAggregateList =
  struct
    type nonrec t = EntityAggregate.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EntityAggregate.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EntityAggregate.of_xml)
    let of_json j =
      list_of_json ~kind:"EntityAggregateList"
        ~of_json:EntityAggregate.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventArnsList =
  struct
    type nonrec t = EventArn.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventArn.of_xml)
    let of_json j =
      list_of_json ~kind:"EventArnsList" ~of_json:EventArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module OrganizationEntityAggregatesList =
  struct
    type nonrec t = OrganizationEntityAggregate.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:OrganizationEntityAggregate.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:OrganizationEntityAggregate.of_xml)
    let of_json j =
      list_of_json ~kind:"OrganizationEntityAggregatesList"
        ~of_json:OrganizationEntityAggregate.of_json j
    let to_json v = composed_to_json to_value v
  end
module OrganizationAccountIdsList =
  struct
    type nonrec t = AccountId.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:25) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AccountId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccountId.of_xml)
    let of_json j =
      list_of_json ~kind:"OrganizationAccountIdsList"
        ~of_json:AccountId.of_json j
    let to_json v = composed_to_json to_value v
  end
module OrganizationEventArnsList =
  struct
    type nonrec t = EventArn.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:25) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventArn.of_xml)
    let of_json j =
      list_of_json ~kind:"OrganizationEventArnsList"
        ~of_json:EventArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module EntityList =
  struct
    type nonrec t = AffectedEntity.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AffectedEntity.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AffectedEntity.of_xml)
    let of_json j =
      list_of_json ~kind:"EntityList" ~of_json:AffectedEntity.of_json j
    let to_json v = composed_to_json to_value v
  end
module EntityFilter =
  struct
    type nonrec t =
      {
      eventArns: EventArnList.t
        [@ocaml.doc
          "A list of event ARNs (unique identifiers). For example: \"arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-CDE456\", \"arn:aws:health:us-west-1::event/EBS/AWS_EBS_LOST_VOLUME/AWS_EBS_LOST_VOLUME_CHI789_JKL101\""];
      entityArns: EntityArnList.t option
        [@ocaml.doc "A list of entity ARNs (unique identifiers)."];
      entityValues: EntityValueList.t option
        [@ocaml.doc "A list of IDs for affected entities."];
      lastUpdatedTimes: DateTimeRangeList.t option
        [@ocaml.doc
          "A list of the most recent dates and times that the entity was updated."];
      tags: TagFilter.t option
        [@ocaml.doc
          "A map of entity tags attached to the affected entity. Currently, the tags property isn't supported."];
      statusCodes: EntityStatusCodeList.t option
        [@ocaml.doc
          "A list of entity status codes (IMPAIRED, UNIMPAIRED, or UNKNOWN)."]}
    let context_ = "EntityFilter"
    let make ?entityArns =
      fun ?entityValues ->
        fun ?lastUpdatedTimes ->
          fun ?tags ->
            fun ?statusCodes ->
              fun ~eventArns ->
                fun () ->
                  {
                    entityArns;
                    entityValues;
                    lastUpdatedTimes;
                    tags;
                    statusCodes;
                    eventArns
                  }
    let to_value x =
      structure_to_value
        [("eventArns", (Some (EventArnList.to_value x.eventArns)));
        ("entityArns", (Option.map x.entityArns ~f:EntityArnList.to_value));
        ("entityValues",
          (Option.map x.entityValues ~f:EntityValueList.to_value));
        ("lastUpdatedTimes",
          (Option.map x.lastUpdatedTimes ~f:DateTimeRangeList.to_value));
        ("tags", (Option.map x.tags ~f:TagFilter.to_value));
        ("statusCodes",
          (Option.map x.statusCodes ~f:EntityStatusCodeList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statusCodes =
        (Option.map ~f:EntityStatusCodeList.of_xml)
          (Xml.child xml_arg0 "statusCodes") in
      let tags = (Option.map ~f:TagFilter.of_xml) (Xml.child xml_arg0 "tags") in
      let lastUpdatedTimes =
        (Option.map ~f:DateTimeRangeList.of_xml)
          (Xml.child xml_arg0 "lastUpdatedTimes") in
      let entityValues =
        (Option.map ~f:EntityValueList.of_xml)
          (Xml.child xml_arg0 "entityValues") in
      let entityArns =
        (Option.map ~f:EntityArnList.of_xml)
          (Xml.child xml_arg0 "entityArns") in
      let eventArns =
        EventArnList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "eventArns") in
      make ?statusCodes ?tags ?lastUpdatedTimes ?entityValues ?entityArns
        ~eventArns ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let statusCodes =
        field_map json__ "statusCodes" EntityStatusCodeList.of_json in
      let tags = field_map json__ "tags" TagFilter.of_json in
      let lastUpdatedTimes =
        field_map json__ "lastUpdatedTimes" DateTimeRangeList.of_json in
      let entityValues =
        field_map json__ "entityValues" EntityValueList.of_json in
      let entityArns = field_map json__ "entityArns" EntityArnList.of_json in
      let eventArns = field_map_exn json__ "eventArns" EventArnList.of_json in
      make ?statusCodes ?tags ?lastUpdatedTimes ?entityValues ?entityArns
        ~eventArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The values to use to filter results from the DescribeAffectedEntities operation."]
module DescribeAffectedEntitiesForOrganizationFailedSet =
  struct
    type nonrec t = OrganizationAffectedEntitiesErrorItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:OrganizationAffectedEntitiesErrorItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:OrganizationAffectedEntitiesErrorItem.of_xml)
    let of_json j =
      list_of_json ~kind:"DescribeAffectedEntitiesForOrganizationFailedSet"
        ~of_json:OrganizationAffectedEntitiesErrorItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module OrganizationEntityAccountFiltersList =
  struct
    type nonrec t = EntityAccountFilter.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EntityAccountFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EntityAccountFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"OrganizationEntityAccountFiltersList"
        ~of_json:EntityAccountFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module OrganizationEntityFiltersList =
  struct
    type nonrec t = EventAccountFilter.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EventAccountFilter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventAccountFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"OrganizationEntityFiltersList"
        ~of_json:EventAccountFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module AffectedAccountsList =
  struct
    type nonrec t = AccountId.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AccountId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccountId.of_xml)
    let of_json j =
      list_of_json ~kind:"affectedAccountsList" ~of_json:AccountId.of_json j
    let to_json v = composed_to_json to_value v
  end
module DescribeHealthServiceStatusForOrganizationResponse =
  struct
    type nonrec t =
      {
      healthServiceAccessStatusForOrganization:
        HealthServiceAccessStatusForOrganization.t option
        [@ocaml.doc
          "Information about the status of enabling or disabling the Health organizational view feature in your organization. Valid values are ENABLED | DISABLED | PENDING."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?healthServiceAccessStatusForOrganization =
      fun () -> { healthServiceAccessStatusForOrganization }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("healthServiceAccessStatusForOrganization",
           (Option.map x.healthServiceAccessStatusForOrganization
              ~f:HealthServiceAccessStatusForOrganization.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthServiceAccessStatusForOrganization =
        (Option.map ~f:HealthServiceAccessStatusForOrganization.of_xml)
          (Xml.child xml_arg0 "healthServiceAccessStatusForOrganization") in
      make ?healthServiceAccessStatusForOrganization ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let healthServiceAccessStatusForOrganization =
        field_map json__ "healthServiceAccessStatusForOrganization"
          HealthServiceAccessStatusForOrganization.of_json in
      make ?healthServiceAccessStatusForOrganization ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This operation provides status information on enabling or disabling Health to work with your organization. To call this operation, you must use the organization's management account."]
module DescribeEventsResponse =
  struct
    type nonrec t =
      {
      events: EventList.t option
        [@ocaml.doc "The events that match the specified filter criteria."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."]}
    type nonrec error =
      [ `InvalidPaginationToken of InvalidPaginationToken.t 
      | `UnsupportedLocale of UnsupportedLocale.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?events = fun ?nextToken -> fun () -> { events; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_json json)
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_xml xml)
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidPaginationToken e ->
          `Assoc
            [("error", (`String "InvalidPaginationToken"));
            ("details", (InvalidPaginationToken.to_json e))]
      | `UnsupportedLocale e ->
          `Assoc
            [("error", (`String "UnsupportedLocale"));
            ("details", (UnsupportedLocale.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("events", (Option.map x.events ~f:EventList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let events =
        (Option.map ~f:EventList.of_xml) (Xml.child xml_arg0 "events") in
      make ?nextToken ?events ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let events = field_map json__ "events" EventList.of_json in
      make ?nextToken ?events ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about events that meet the specified filter criteria. Events are returned in a summary form and do not include the detailed description, any additional metadata that depends on the event type, or any affected resources. To retrieve that information, use the DescribeEventDetails and DescribeAffectedEntities operations. If no filter criteria are specified, all events are returned. Results are sorted by lastModifiedTime, starting with the most recent event. When you call the DescribeEvents operation and specify an entity for the entityValues parameter, Health might return public events that aren't specific to that resource. For example, if you call DescribeEvents and specify an ID for an Amazon Elastic Compute Cloud (Amazon EC2) instance, Health might return events that aren't specific to that resource or service. To get events that are specific to a service, use the services parameter in the filter object. For more information, see Event. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results."]
module DescribeEventsRequest =
  struct
    type nonrec t =
      {
      filter: EventFilter.t option
        [@ocaml.doc "Values to narrow the results returned."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."];
      maxResults: MaxResultsLowerRange.t option
        [@ocaml.doc
          "The maximum number of items to return in one batch, between 1 and 100, inclusive."];
      locale: Locale.t option
        [@ocaml.doc
          "The locale (language) to return information in. English (en) is the default and the only supported value at this time."]}
    let make ?filter =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?locale -> fun () -> { filter; nextToken; maxResults; locale }
    let to_value x =
      structure_to_value
        [("filter", (Option.map x.filter ~f:EventFilter.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults",
          (Option.map x.maxResults ~f:MaxResultsLowerRange.to_value));
        ("locale", (Option.map x.locale ~f:Locale.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let locale =
        (Option.map ~f:Locale.of_xml) (Xml.child xml_arg0 "locale") in
      let maxResults =
        (Option.map ~f:MaxResultsLowerRange.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let filter =
        (Option.map ~f:EventFilter.of_xml) (Xml.child xml_arg0 "filter") in
      make ?locale ?maxResults ?nextToken ?filter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let locale = field_map json__ "locale" Locale.of_json in
      let maxResults =
        field_map json__ "maxResults" MaxResultsLowerRange.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let filter = field_map json__ "filter" EventFilter.of_json in
      make ?locale ?maxResults ?nextToken ?filter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about events that meet the specified filter criteria. Events are returned in a summary form and do not include the detailed description, any additional metadata that depends on the event type, or any affected resources. To retrieve that information, use the DescribeEventDetails and DescribeAffectedEntities operations. If no filter criteria are specified, all events are returned. Results are sorted by lastModifiedTime, starting with the most recent event. When you call the DescribeEvents operation and specify an entity for the entityValues parameter, Health might return public events that aren't specific to that resource. For example, if you call DescribeEvents and specify an ID for an Amazon Elastic Compute Cloud (Amazon EC2) instance, Health might return events that aren't specific to that resource or service. To get events that are specific to a service, use the services parameter in the filter object. For more information, see Event. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results."]
module DescribeEventsForOrganizationResponse =
  struct
    type nonrec t =
      {
      events: OrganizationEventList.t option
        [@ocaml.doc "The events that match the specified filter criteria."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."]}
    type nonrec error =
      [ `InvalidPaginationToken of InvalidPaginationToken.t 
      | `UnsupportedLocale of UnsupportedLocale.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?events = fun ?nextToken -> fun () -> { events; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_json json)
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_xml xml)
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidPaginationToken e ->
          `Assoc
            [("error", (`String "InvalidPaginationToken"));
            ("details", (InvalidPaginationToken.to_json e))]
      | `UnsupportedLocale e ->
          `Assoc
            [("error", (`String "UnsupportedLocale"));
            ("details", (UnsupportedLocale.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("events", (Option.map x.events ~f:OrganizationEventList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let events =
        (Option.map ~f:OrganizationEventList.of_xml)
          (Xml.child xml_arg0 "events") in
      make ?nextToken ?events ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let events = field_map json__ "events" OrganizationEventList.of_json in
      make ?nextToken ?events ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about events across your organization in Organizations. You can use thefilters parameter to specify the events that you want to return. Events are returned in a summary form and don't include the affected accounts, detailed description, any additional metadata that depends on the event type, or any affected resources. To retrieve that information, use the following operations: DescribeAffectedAccountsForOrganization DescribeEventDetailsForOrganization DescribeAffectedEntitiesForOrganization If you don't specify a filter, the DescribeEventsForOrganizations returns all events across your organization. Results are sorted by lastModifiedTime, starting with the most recent event. For more information about the different types of Health events, see Event. Before you can call this operation, you must first enable Health to work with Organizations. To do this, call the EnableHealthServiceAccessForOrganization operation from your organization's management account. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results."]
module DescribeEventsForOrganizationRequest =
  struct
    type nonrec t =
      {
      filter: OrganizationEventFilter.t option
        [@ocaml.doc "Values to narrow the results returned."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."];
      maxResults: MaxResultsLowerRange.t option
        [@ocaml.doc
          "The maximum number of items to return in one batch, between 1 and 100, inclusive."];
      locale: Locale.t option
        [@ocaml.doc
          "The locale (language) to return information in. English (en) is the default and the only supported value at this time."]}
    let make ?filter =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?locale -> fun () -> { filter; nextToken; maxResults; locale }
    let to_value x =
      structure_to_value
        [("filter",
           (Option.map x.filter ~f:OrganizationEventFilter.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults",
          (Option.map x.maxResults ~f:MaxResultsLowerRange.to_value));
        ("locale", (Option.map x.locale ~f:Locale.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let locale =
        (Option.map ~f:Locale.of_xml) (Xml.child xml_arg0 "locale") in
      let maxResults =
        (Option.map ~f:MaxResultsLowerRange.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let filter =
        (Option.map ~f:OrganizationEventFilter.of_xml)
          (Xml.child xml_arg0 "filter") in
      make ?locale ?maxResults ?nextToken ?filter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let locale = field_map json__ "locale" Locale.of_json in
      let maxResults =
        field_map json__ "maxResults" MaxResultsLowerRange.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let filter = field_map json__ "filter" OrganizationEventFilter.of_json in
      make ?locale ?maxResults ?nextToken ?filter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about events across your organization in Organizations. You can use thefilters parameter to specify the events that you want to return. Events are returned in a summary form and don't include the affected accounts, detailed description, any additional metadata that depends on the event type, or any affected resources. To retrieve that information, use the following operations: DescribeAffectedAccountsForOrganization DescribeEventDetailsForOrganization DescribeAffectedEntitiesForOrganization If you don't specify a filter, the DescribeEventsForOrganizations returns all events across your organization. Results are sorted by lastModifiedTime, starting with the most recent event. For more information about the different types of Health events, see Event. Before you can call this operation, you must first enable Health to work with Organizations. To do this, call the EnableHealthServiceAccessForOrganization operation from your organization's management account. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results."]
module DescribeEventTypesResponse =
  struct
    type nonrec t =
      {
      eventTypes: EventTypeList.t option
        [@ocaml.doc
          "A list of event types that match the filter criteria. Event types have a category (issue, accountNotification, or scheduledChange), a service (for example, EC2, RDS, DATAPIPELINE, BILLING), and a code (in the format AWS_SERVICE_DESCRIPTION ; for example, AWS_EC2_SYSTEM_MAINTENANCE_EVENT)."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."]}
    type nonrec error =
      [ `InvalidPaginationToken of InvalidPaginationToken.t 
      | `UnsupportedLocale of UnsupportedLocale.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?eventTypes =
      fun ?nextToken -> fun () -> { eventTypes; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_json json)
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_xml xml)
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidPaginationToken e ->
          `Assoc
            [("error", (`String "InvalidPaginationToken"));
            ("details", (InvalidPaginationToken.to_json e))]
      | `UnsupportedLocale e ->
          `Assoc
            [("error", (`String "UnsupportedLocale"));
            ("details", (UnsupportedLocale.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("eventTypes", (Option.map x.eventTypes ~f:EventTypeList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let eventTypes =
        (Option.map ~f:EventTypeList.of_xml)
          (Xml.child xml_arg0 "eventTypes") in
      make ?nextToken ?eventTypes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let eventTypes = field_map json__ "eventTypes" EventTypeList.of_json in
      make ?nextToken ?eventTypes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the event types that meet the specified filter criteria. You can use this API operation to find information about the Health event, such as the category, Amazon Web Services service, and event code. The metadata for each event appears in the EventType object. If you don't specify a filter criteria, the API operation returns all event types, in no particular order. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results."]
module DescribeEventTypesRequest =
  struct
    type nonrec t =
      {
      filter: EventTypeFilter.t option
        [@ocaml.doc "Values to narrow the results returned."];
      locale: Locale.t option
        [@ocaml.doc
          "The locale (language) to return information in. English (en) is the default and the only supported value at this time."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return in one batch, between 10 and 100, inclusive. If you don't specify the maxResults parameter, this operation returns a maximum of 30 items by default."]}
    let make ?filter =
      fun ?locale ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun () -> { filter; locale; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("filter", (Option.map x.filter ~f:EventTypeFilter.to_value));
        ("locale", (Option.map x.locale ~f:Locale.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let locale =
        (Option.map ~f:Locale.of_xml) (Xml.child xml_arg0 "locale") in
      let filter =
        (Option.map ~f:EventTypeFilter.of_xml) (Xml.child xml_arg0 "filter") in
      make ?maxResults ?nextToken ?locale ?filter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let locale = field_map json__ "locale" Locale.of_json in
      let filter = field_map json__ "filter" EventTypeFilter.of_json in
      make ?maxResults ?nextToken ?locale ?filter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the event types that meet the specified filter criteria. You can use this API operation to find information about the Health event, such as the category, Amazon Web Services service, and event code. The metadata for each event appears in the EventType object. If you don't specify a filter criteria, the API operation returns all event types, in no particular order. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results."]
module DescribeEventDetailsResponse =
  struct
    type nonrec t =
      {
      successfulSet: DescribeEventDetailsSuccessfulSet.t option
        [@ocaml.doc "Information about the events that could be retrieved."];
      failedSet: DescribeEventDetailsFailedSet.t option
        [@ocaml.doc
          "Error messages for any events that could not be retrieved."]}
    type nonrec error =
      [ `UnsupportedLocale of UnsupportedLocale.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?successfulSet =
      fun ?failedSet -> fun () -> { successfulSet; failedSet }
    let error_of_json name json =
      match name with
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `UnsupportedLocale e ->
          `Assoc
            [("error", (`String "UnsupportedLocale"));
            ("details", (UnsupportedLocale.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successfulSet",
           (Option.map x.successfulSet
              ~f:DescribeEventDetailsSuccessfulSet.to_value));
        ("failedSet",
          (Option.map x.failedSet ~f:DescribeEventDetailsFailedSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failedSet =
        (Option.map ~f:DescribeEventDetailsFailedSet.of_xml)
          (Xml.child xml_arg0 "failedSet") in
      let successfulSet =
        (Option.map ~f:DescribeEventDetailsSuccessfulSet.of_xml)
          (Xml.child xml_arg0 "successfulSet") in
      make ?failedSet ?successfulSet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let failedSet =
        field_map json__ "failedSet" DescribeEventDetailsFailedSet.of_json in
      let successfulSet =
        field_map json__ "successfulSet"
          DescribeEventDetailsSuccessfulSet.of_json in
      make ?failedSet ?successfulSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns detailed information about one or more specified events. Information includes standard event data (Amazon Web Services Region, service, and so on, as returned by DescribeEvents), a detailed event description, and possible additional metadata that depends upon the nature of the event. Affected entities are not included. To retrieve the entities, use the DescribeAffectedEntities operation. If a specified event can't be retrieved, an error message is returned for that event. This operation supports resource-level permissions. You can use this operation to allow or deny access to specific Health events. For more information, see Resource- and action-based conditions in the Health User Guide."]
module DescribeEventDetailsRequest =
  struct
    type nonrec t =
      {
      eventArns: EventArnList.t
        [@ocaml.doc
          "A list of event ARNs (unique identifiers). For example: \"arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-CDE456\", \"arn:aws:health:us-west-1::event/EBS/AWS_EBS_LOST_VOLUME/AWS_EBS_LOST_VOLUME_CHI789_JKL101\""];
      locale: Locale.t option
        [@ocaml.doc
          "The locale (language) to return information in. English (en) is the default and the only supported value at this time."]}
    let context_ = "DescribeEventDetailsRequest"
    let make ?locale = fun ~eventArns -> fun () -> { locale; eventArns }
    let to_value x =
      structure_to_value
        [("eventArns", (Some (EventArnList.to_value x.eventArns)));
        ("locale", (Option.map x.locale ~f:Locale.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let locale =
        (Option.map ~f:Locale.of_xml) (Xml.child xml_arg0 "locale") in
      let eventArns =
        EventArnList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "eventArns") in
      make ?locale ~eventArns ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let locale = field_map json__ "locale" Locale.of_json in
      let eventArns = field_map_exn json__ "eventArns" EventArnList.of_json in
      make ?locale ~eventArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns detailed information about one or more specified events. Information includes standard event data (Amazon Web Services Region, service, and so on, as returned by DescribeEvents), a detailed event description, and possible additional metadata that depends upon the nature of the event. Affected entities are not included. To retrieve the entities, use the DescribeAffectedEntities operation. If a specified event can't be retrieved, an error message is returned for that event. This operation supports resource-level permissions. You can use this operation to allow or deny access to specific Health events. For more information, see Resource- and action-based conditions in the Health User Guide."]
module DescribeEventDetailsForOrganizationResponse =
  struct
    type nonrec t =
      {
      successfulSet:
        DescribeEventDetailsForOrganizationSuccessfulSet.t option
        [@ocaml.doc "Information about the events that could be retrieved."];
      failedSet: DescribeEventDetailsForOrganizationFailedSet.t option
        [@ocaml.doc
          "Error messages for any events that could not be retrieved."]}
    type nonrec error =
      [ `UnsupportedLocale of UnsupportedLocale.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?successfulSet =
      fun ?failedSet -> fun () -> { successfulSet; failedSet }
    let error_of_json name json =
      match name with
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `UnsupportedLocale e ->
          `Assoc
            [("error", (`String "UnsupportedLocale"));
            ("details", (UnsupportedLocale.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("successfulSet",
           (Option.map x.successfulSet
              ~f:DescribeEventDetailsForOrganizationSuccessfulSet.to_value));
        ("failedSet",
          (Option.map x.failedSet
             ~f:DescribeEventDetailsForOrganizationFailedSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failedSet =
        (Option.map ~f:DescribeEventDetailsForOrganizationFailedSet.of_xml)
          (Xml.child xml_arg0 "failedSet") in
      let successfulSet =
        (Option.map
           ~f:DescribeEventDetailsForOrganizationSuccessfulSet.of_xml)
          (Xml.child xml_arg0 "successfulSet") in
      make ?failedSet ?successfulSet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let failedSet =
        field_map json__ "failedSet"
          DescribeEventDetailsForOrganizationFailedSet.of_json in
      let successfulSet =
        field_map json__ "successfulSet"
          DescribeEventDetailsForOrganizationSuccessfulSet.of_json in
      make ?failedSet ?successfulSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns detailed information about one or more specified events for one or more Amazon Web Services accounts in your organization. This information includes standard event data (such as the Amazon Web Services Region and service), an event description, and (depending on the event) possible metadata. This operation doesn't return affected entities, such as the resources related to the event. To return affected entities, use the DescribeAffectedEntitiesForOrganization operation. Before you can call this operation, you must first enable Health to work with Organizations. To do this, call the EnableHealthServiceAccessForOrganization operation from your organization's management account. When you call the DescribeEventDetailsForOrganization operation, specify the organizationEventDetailFilters object in the request. Depending on the Health event type, note the following differences: To return event details for a public event, you must specify a null value for the awsAccountId parameter. If you specify an account ID for a public event, Health returns an error message because public events aren't specific to an account. To return event details for an event that is specific to an account in your organization, you must specify the awsAccountId parameter in the request. If you don't specify an account ID, Health returns an error message because the event is specific to an account in your organization. For more information, see Event. This operation doesn't support resource-level permissions. You can't use this operation to allow or deny access to specific Health events. For more information, see Resource- and action-based conditions in the Health User Guide."]
module DescribeEventDetailsForOrganizationRequest =
  struct
    type nonrec t =
      {
      organizationEventDetailFilters: OrganizationEventDetailFiltersList.t
        [@ocaml.doc
          "A set of JSON elements that includes the awsAccountId and the eventArn."];
      locale: Locale.t option
        [@ocaml.doc
          "The locale (language) to return information in. English (en) is the default and the only supported value at this time."]}
    let context_ = "DescribeEventDetailsForOrganizationRequest"
    let make ?locale =
      fun ~organizationEventDetailFilters ->
        fun () -> { locale; organizationEventDetailFilters }
    let to_value x =
      structure_to_value
        [("organizationEventDetailFilters",
           (Some
              (OrganizationEventDetailFiltersList.to_value
                 x.organizationEventDetailFilters)));
        ("locale", (Option.map x.locale ~f:Locale.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let locale =
        (Option.map ~f:Locale.of_xml) (Xml.child xml_arg0 "locale") in
      let organizationEventDetailFilters =
        OrganizationEventDetailFiltersList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "organizationEventDetailFilters") in
      make ?locale ~organizationEventDetailFilters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let locale = field_map json__ "locale" Locale.of_json in
      let organizationEventDetailFilters =
        field_map_exn json__ "organizationEventDetailFilters"
          OrganizationEventDetailFiltersList.of_json in
      make ?locale ~organizationEventDetailFilters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns detailed information about one or more specified events for one or more Amazon Web Services accounts in your organization. This information includes standard event data (such as the Amazon Web Services Region and service), an event description, and (depending on the event) possible metadata. This operation doesn't return affected entities, such as the resources related to the event. To return affected entities, use the DescribeAffectedEntitiesForOrganization operation. Before you can call this operation, you must first enable Health to work with Organizations. To do this, call the EnableHealthServiceAccessForOrganization operation from your organization's management account. When you call the DescribeEventDetailsForOrganization operation, specify the organizationEventDetailFilters object in the request. Depending on the Health event type, note the following differences: To return event details for a public event, you must specify a null value for the awsAccountId parameter. If you specify an account ID for a public event, Health returns an error message because public events aren't specific to an account. To return event details for an event that is specific to an account in your organization, you must specify the awsAccountId parameter in the request. If you don't specify an account ID, Health returns an error message because the event is specific to an account in your organization. For more information, see Event. This operation doesn't support resource-level permissions. You can't use this operation to allow or deny access to specific Health events. For more information, see Resource- and action-based conditions in the Health User Guide."]
module DescribeEventAggregatesResponse =
  struct
    type nonrec t =
      {
      eventAggregates: EventAggregateList.t option
        [@ocaml.doc
          "The number of events in each category that meet the optional filter criteria."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."]}
    type nonrec error =
      [ `InvalidPaginationToken of InvalidPaginationToken.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?eventAggregates =
      fun ?nextToken -> fun () -> { eventAggregates; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidPaginationToken e ->
          `Assoc
            [("error", (`String "InvalidPaginationToken"));
            ("details", (InvalidPaginationToken.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("eventAggregates",
           (Option.map x.eventAggregates ~f:EventAggregateList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let eventAggregates =
        (Option.map ~f:EventAggregateList.of_xml)
          (Xml.child xml_arg0 "eventAggregates") in
      make ?nextToken ?eventAggregates ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let eventAggregates =
        field_map json__ "eventAggregates" EventAggregateList.of_json in
      make ?nextToken ?eventAggregates ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the number of events of each event type (issue, scheduled change, and account notification). If no filter is specified, the counts of all events in each category are returned. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results."]
module DescribeEventAggregatesRequest =
  struct
    type nonrec t =
      {
      filter: EventFilter.t option
        [@ocaml.doc "Values to narrow the results returned."];
      aggregateField: EventAggregateField.t
        [@ocaml.doc
          "The only currently supported value is eventTypeCategory."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return in one batch, between 10 and 100, inclusive."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."]}
    let context_ = "DescribeEventAggregatesRequest"
    let make ?filter =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ~aggregateField ->
            fun () -> { filter; maxResults; nextToken; aggregateField }
    let to_value x =
      structure_to_value
        [("filter", (Option.map x.filter ~f:EventFilter.to_value));
        ("aggregateField",
          (Some (EventAggregateField.to_value x.aggregateField)));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let aggregateField =
        EventAggregateField.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "aggregateField") in
      let filter =
        (Option.map ~f:EventFilter.of_xml) (Xml.child xml_arg0 "filter") in
      make ?nextToken ?maxResults ~aggregateField ?filter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let aggregateField =
        field_map_exn json__ "aggregateField" EventAggregateField.of_json in
      let filter = field_map json__ "filter" EventFilter.of_json in
      make ?nextToken ?maxResults ~aggregateField ?filter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the number of events of each event type (issue, scheduled change, and account notification). If no filter is specified, the counts of all events in each category are returned. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results."]
module DescribeEntityAggregatesResponse =
  struct
    type nonrec t =
      {
      entityAggregates: EntityAggregateList.t option
        [@ocaml.doc
          "The number of entities that are affected by each of the specified events."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?entityAggregates = fun () -> { entityAggregates }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("entityAggregates",
           (Option.map x.entityAggregates ~f:EntityAggregateList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let entityAggregates =
        (Option.map ~f:EntityAggregateList.of_xml)
          (Xml.child xml_arg0 "entityAggregates") in
      make ?entityAggregates ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let entityAggregates =
        field_map json__ "entityAggregates" EntityAggregateList.of_json in
      make ?entityAggregates ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the number of entities that are affected by each of the specified events."]
module DescribeEntityAggregatesRequest =
  struct
    type nonrec t =
      {
      eventArns: EventArnsList.t option
        [@ocaml.doc
          "A list of event ARNs (unique identifiers). For example: \"arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-CDE456\", \"arn:aws:health:us-west-1::event/EBS/AWS_EBS_LOST_VOLUME/AWS_EBS_LOST_VOLUME_CHI789_JKL101\""]}
    let make ?eventArns = fun () -> { eventArns }
    let to_value x =
      structure_to_value
        [("eventArns", (Option.map x.eventArns ~f:EventArnsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventArns =
        (Option.map ~f:EventArnsList.of_xml) (Xml.child xml_arg0 "eventArns") in
      make ?eventArns ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let eventArns = field_map json__ "eventArns" EventArnsList.of_json in
      make ?eventArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the number of entities that are affected by each of the specified events."]
module DescribeEntityAggregatesForOrganizationResponse =
  struct
    type nonrec t =
      {
      organizationEntityAggregates: OrganizationEntityAggregatesList.t option
        [@ocaml.doc
          "The list of entity aggregates for each of the specified accounts that are affected by each of the specified events."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?organizationEntityAggregates =
      fun () -> { organizationEntityAggregates }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("organizationEntityAggregates",
           (Option.map x.organizationEntityAggregates
              ~f:OrganizationEntityAggregatesList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let organizationEntityAggregates =
        (Option.map ~f:OrganizationEntityAggregatesList.of_xml)
          (Xml.child xml_arg0 "organizationEntityAggregates") in
      make ?organizationEntityAggregates ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let organizationEntityAggregates =
        field_map json__ "organizationEntityAggregates"
          OrganizationEntityAggregatesList.of_json in
      make ?organizationEntityAggregates ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of entity aggregates for your Organizations that are affected by each of the specified events."]
module DescribeEntityAggregatesForOrganizationRequest =
  struct
    type nonrec t =
      {
      eventArns: OrganizationEventArnsList.t
        [@ocaml.doc
          "A list of event ARNs (unique identifiers). For example: \"arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-CDE456\", \"arn:aws:health:us-west-1::event/EBS/AWS_EBS_LOST_VOLUME/AWS_EBS_LOST_VOLUME_CHI789_JKL101\""];
      awsAccountIds: OrganizationAccountIdsList.t option
        [@ocaml.doc
          "A list of 12-digit Amazon Web Services account numbers that contains the affected entities."]}
    let context_ = "DescribeEntityAggregatesForOrganizationRequest"
    let make ?awsAccountIds =
      fun ~eventArns -> fun () -> { awsAccountIds; eventArns }
    let to_value x =
      structure_to_value
        [("eventArns",
           (Some (OrganizationEventArnsList.to_value x.eventArns)));
        ("awsAccountIds",
          (Option.map x.awsAccountIds ~f:OrganizationAccountIdsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let awsAccountIds =
        (Option.map ~f:OrganizationAccountIdsList.of_xml)
          (Xml.child xml_arg0 "awsAccountIds") in
      let eventArns =
        OrganizationEventArnsList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "eventArns") in
      make ?awsAccountIds ~eventArns ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let awsAccountIds =
        field_map json__ "awsAccountIds" OrganizationAccountIdsList.of_json in
      let eventArns =
        field_map_exn json__ "eventArns" OrganizationEventArnsList.of_json in
      make ?awsAccountIds ~eventArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of entity aggregates for your Organizations that are affected by each of the specified events."]
module DescribeAffectedEntitiesResponse =
  struct
    type nonrec t =
      {
      entities: EntityList.t option
        [@ocaml.doc "The entities that match the filter criteria."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."]}
    type nonrec error =
      [ `InvalidPaginationToken of InvalidPaginationToken.t 
      | `UnsupportedLocale of UnsupportedLocale.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?entities = fun ?nextToken -> fun () -> { entities; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_json json)
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_xml xml)
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidPaginationToken e ->
          `Assoc
            [("error", (`String "InvalidPaginationToken"));
            ("details", (InvalidPaginationToken.to_json e))]
      | `UnsupportedLocale e ->
          `Assoc
            [("error", (`String "UnsupportedLocale"));
            ("details", (UnsupportedLocale.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("entities", (Option.map x.entities ~f:EntityList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let entities =
        (Option.map ~f:EntityList.of_xml) (Xml.child xml_arg0 "entities") in
      make ?nextToken ?entities ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let entities = field_map json__ "entities" EntityList.of_json in
      make ?nextToken ?entities ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of entities that have been affected by the specified events, based on the specified filter criteria. Entities can refer to individual customer resources, groups of customer resources, or any other construct, depending on the Amazon Web Services service. Events that have impact beyond that of the affected entities, or where the extent of impact is unknown, include at least one entity indicating this. At least one event ARN is required. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results. This operation supports resource-level permissions. You can use this operation to allow or deny access to specific Health events. For more information, see Resource- and action-based conditions in the Health User Guide."]
module DescribeAffectedEntitiesRequest =
  struct
    type nonrec t =
      {
      filter: EntityFilter.t
        [@ocaml.doc
          "Values to narrow the results returned. At least one event ARN is required."];
      locale: Locale.t option
        [@ocaml.doc
          "The locale (language) to return information in. English (en) is the default and the only supported value at this time."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."];
      maxResults: MaxResultsLowerRange.t option
        [@ocaml.doc
          "The maximum number of items to return in one batch, between 1 and 100, inclusive."]}
    let context_ = "DescribeAffectedEntitiesRequest"
    let make ?locale =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ~filter -> fun () -> { locale; nextToken; maxResults; filter }
    let to_value x =
      structure_to_value
        [("filter", (Some (EntityFilter.to_value x.filter)));
        ("locale", (Option.map x.locale ~f:Locale.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults",
          (Option.map x.maxResults ~f:MaxResultsLowerRange.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResultsLowerRange.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let locale =
        (Option.map ~f:Locale.of_xml) (Xml.child xml_arg0 "locale") in
      let filter =
        EntityFilter.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "filter") in
      make ?maxResults ?nextToken ?locale ~filter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "maxResults" MaxResultsLowerRange.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let locale = field_map json__ "locale" Locale.of_json in
      let filter = field_map_exn json__ "filter" EntityFilter.of_json in
      make ?maxResults ?nextToken ?locale ~filter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of entities that have been affected by the specified events, based on the specified filter criteria. Entities can refer to individual customer resources, groups of customer resources, or any other construct, depending on the Amazon Web Services service. Events that have impact beyond that of the affected entities, or where the extent of impact is unknown, include at least one entity indicating this. At least one event ARN is required. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results. This operation supports resource-level permissions. You can use this operation to allow or deny access to specific Health events. For more information, see Resource- and action-based conditions in the Health User Guide."]
module DescribeAffectedEntitiesForOrganizationResponse =
  struct
    type nonrec t =
      {
      entities: EntityList.t option
        [@ocaml.doc
          "A JSON set of elements including the awsAccountId and its entityArn, entityValue and its entityArn, lastUpdatedTime, and statusCode."];
      failedSet: DescribeAffectedEntitiesForOrganizationFailedSet.t option
        [@ocaml.doc
          "A JSON set of elements of the failed response, including the awsAccountId, errorMessage, errorName, and eventArn."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."]}
    type nonrec error =
      [ `InvalidPaginationToken of InvalidPaginationToken.t 
      | `UnsupportedLocale of UnsupportedLocale.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?entities =
      fun ?failedSet ->
        fun ?nextToken -> fun () -> { entities; failedSet; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_json json)
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_xml xml)
      | "UnsupportedLocale" ->
          `UnsupportedLocale (UnsupportedLocale.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidPaginationToken e ->
          `Assoc
            [("error", (`String "InvalidPaginationToken"));
            ("details", (InvalidPaginationToken.to_json e))]
      | `UnsupportedLocale e ->
          `Assoc
            [("error", (`String "UnsupportedLocale"));
            ("details", (UnsupportedLocale.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("entities", (Option.map x.entities ~f:EntityList.to_value));
        ("failedSet",
          (Option.map x.failedSet
             ~f:DescribeAffectedEntitiesForOrganizationFailedSet.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let failedSet =
        (Option.map
           ~f:DescribeAffectedEntitiesForOrganizationFailedSet.of_xml)
          (Xml.child xml_arg0 "failedSet") in
      let entities =
        (Option.map ~f:EntityList.of_xml) (Xml.child xml_arg0 "entities") in
      make ?nextToken ?failedSet ?entities ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let failedSet =
        field_map json__ "failedSet"
          DescribeAffectedEntitiesForOrganizationFailedSet.of_json in
      let entities = field_map json__ "entities" EntityList.of_json in
      make ?nextToken ?failedSet ?entities ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of entities that have been affected by one or more events for one or more accounts in your organization in Organizations, based on the filter criteria. Entities can refer to individual customer resources, groups of customer resources, or any other construct, depending on the Amazon Web Services service. At least one event Amazon Resource Name (ARN) and account ID are required. Before you can call this operation, you must first enable Health to work with Organizations. To do this, call the EnableHealthServiceAccessForOrganization operation from your organization's management account. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results. This operation doesn't support resource-level permissions. You can't use this operation to allow or deny access to specific Health events. For more information, see Resource- and action-based conditions in the Health User Guide."]
module DescribeAffectedEntitiesForOrganizationRequest =
  struct
    type nonrec t =
      {
      organizationEntityFilters: OrganizationEntityFiltersList.t option
        [@ocaml.doc
          "A JSON set of elements including the awsAccountId and the eventArn."];
      locale: Locale.t option
        [@ocaml.doc
          "The locale (language) to return information in. English (en) is the default and the only supported value at this time."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."];
      maxResults: MaxResultsLowerRange.t option
        [@ocaml.doc
          "The maximum number of items to return in one batch, between 1 and 100, inclusive."];
      organizationEntityAccountFilters:
        OrganizationEntityAccountFiltersList.t option
        [@ocaml.doc
          "A JSON set of elements including the awsAccountId, eventArn and a set of statusCodes."]}
    let make ?organizationEntityFilters =
      fun ?locale ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ?organizationEntityAccountFilters ->
              fun () ->
                {
                  organizationEntityFilters;
                  locale;
                  nextToken;
                  maxResults;
                  organizationEntityAccountFilters
                }
    let to_value x =
      structure_to_value
        [("organizationEntityFilters",
           (Option.map x.organizationEntityFilters
              ~f:OrganizationEntityFiltersList.to_value));
        ("locale", (Option.map x.locale ~f:Locale.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults",
          (Option.map x.maxResults ~f:MaxResultsLowerRange.to_value));
        ("organizationEntityAccountFilters",
          (Option.map x.organizationEntityAccountFilters
             ~f:OrganizationEntityAccountFiltersList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let organizationEntityAccountFilters =
        (Option.map ~f:OrganizationEntityAccountFiltersList.of_xml)
          (Xml.child xml_arg0 "organizationEntityAccountFilters") in
      let maxResults =
        (Option.map ~f:MaxResultsLowerRange.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let locale =
        (Option.map ~f:Locale.of_xml) (Xml.child xml_arg0 "locale") in
      let organizationEntityFilters =
        (Option.map ~f:OrganizationEntityFiltersList.of_xml)
          (Xml.child xml_arg0 "organizationEntityFilters") in
      make ?organizationEntityAccountFilters ?maxResults ?nextToken ?locale
        ?organizationEntityFilters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let organizationEntityAccountFilters =
        field_map json__ "organizationEntityAccountFilters"
          OrganizationEntityAccountFiltersList.of_json in
      let maxResults =
        field_map json__ "maxResults" MaxResultsLowerRange.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let locale = field_map json__ "locale" Locale.of_json in
      let organizationEntityFilters =
        field_map json__ "organizationEntityFilters"
          OrganizationEntityFiltersList.of_json in
      make ?organizationEntityAccountFilters ?maxResults ?nextToken ?locale
        ?organizationEntityFilters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of entities that have been affected by one or more events for one or more accounts in your organization in Organizations, based on the filter criteria. Entities can refer to individual customer resources, groups of customer resources, or any other construct, depending on the Amazon Web Services service. At least one event Amazon Resource Name (ARN) and account ID are required. Before you can call this operation, you must first enable Health to work with Organizations. To do this, call the EnableHealthServiceAccessForOrganization operation from your organization's management account. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results. This operation doesn't support resource-level permissions. You can't use this operation to allow or deny access to specific Health events. For more information, see Resource- and action-based conditions in the Health User Guide."]
module DescribeAffectedAccountsForOrganizationResponse =
  struct
    type nonrec t =
      {
      affectedAccounts: AffectedAccountsList.t option
        [@ocaml.doc "A JSON set of elements of the affected accounts."];
      eventScopeCode: EventScopeCode.t option
        [@ocaml.doc
          "This parameter specifies if the Health event is a public Amazon Web Services service event or an account-specific event. If the eventScopeCode value is PUBLIC, then the affectedAccounts value is always empty. If the eventScopeCode value is ACCOUNT_SPECIFIC, then the affectedAccounts value lists the affected Amazon Web Services accounts in your organization. For example, if an event affects a service such as Amazon Elastic Compute Cloud and you have Amazon Web Services accounts that use that service, those account IDs appear in the response. If the eventScopeCode value is NONE, then the eventArn that you specified in the request is invalid or doesn't exist."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."]}
    type nonrec error =
      [ `InvalidPaginationToken of InvalidPaginationToken.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?affectedAccounts =
      fun ?eventScopeCode ->
        fun ?nextToken ->
          fun () -> { affectedAccounts; eventScopeCode; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidPaginationToken e ->
          `Assoc
            [("error", (`String "InvalidPaginationToken"));
            ("details", (InvalidPaginationToken.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("affectedAccounts",
           (Option.map x.affectedAccounts ~f:AffectedAccountsList.to_value));
        ("eventScopeCode",
          (Option.map x.eventScopeCode ~f:EventScopeCode.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let eventScopeCode =
        (Option.map ~f:EventScopeCode.of_xml)
          (Xml.child xml_arg0 "eventScopeCode") in
      let affectedAccounts =
        (Option.map ~f:AffectedAccountsList.of_xml)
          (Xml.child xml_arg0 "affectedAccounts") in
      make ?nextToken ?eventScopeCode ?affectedAccounts ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let eventScopeCode =
        field_map json__ "eventScopeCode" EventScopeCode.of_json in
      let affectedAccounts =
        field_map json__ "affectedAccounts" AffectedAccountsList.of_json in
      make ?nextToken ?eventScopeCode ?affectedAccounts ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of accounts in the organization from Organizations that are affected by the provided event. For more information about the different types of Health events, see Event. Before you can call this operation, you must first enable Health to work with Organizations. To do this, call the EnableHealthServiceAccessForOrganization operation from your organization's management account. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results."]
module DescribeAffectedAccountsForOrganizationRequest =
  struct
    type nonrec t =
      {
      eventArn: EventArn.t
        [@ocaml.doc
          "The unique identifier for the event. The event ARN has the arn:aws:health:event-region::event/SERVICE/EVENT_TYPE_CODE/EVENT_TYPE_PLUS_ID format. For example, an event ARN might look like the following: arn:aws:health:us-east-1::event/EC2/EC2_INSTANCE_RETIREMENT_SCHEDULED/EC2_INSTANCE_RETIREMENT_SCHEDULED_ABC123-DEF456"];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the results of a search are large, only a portion of the results are returned, and a nextToken pagination token is returned in the response. To retrieve the next batch of results, reissue the search request and include the returned token. When all results have been returned, the response does not contain a pagination token value."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return in one batch, between 10 and 100, inclusive."]}
    let context_ = "DescribeAffectedAccountsForOrganizationRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~eventArn -> fun () -> { nextToken; maxResults; eventArn }
    let to_value x =
      structure_to_value
        [("eventArn", (Some (EventArn.to_value x.eventArn)));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let eventArn =
        EventArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "eventArn") in
      make ?maxResults ?nextToken ~eventArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let eventArn = field_map_exn json__ "eventArn" EventArn.of_json in
      make ?maxResults ?nextToken ~eventArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of accounts in the organization from Organizations that are affected by the provided event. For more information about the different types of Health events, see Event. Before you can call this operation, you must first enable Health to work with Organizations. To do this, call the EnableHealthServiceAccessForOrganization operation from your organization's management account. This API operation uses pagination. Specify the nextToken parameter in the next request to return more results."]
module ConcurrentModificationException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "EnableHealthServiceAccessForOrganization is already in progress. Wait for the action to complete before trying again. To get the current status, use the DescribeHealthServiceStatusForOrganization operation."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let describe_affected_accounts_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and eventArn =
         flag "event-arn" (required string) ~doc:"STRING eventArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_affected_accounts_for_organization
           (Values.DescribeAffectedAccountsForOrganizationRequest.make
              ?nextToken ?maxResults ~eventArn ())
           (Some
              Values.DescribeAffectedAccountsForOrganizationResponse.to_json)
           (Some
              Values.DescribeAffectedAccountsForOrganizationResponse.error_to_json)])
let describe_affected_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locale = flag "locale" (optional string) ~doc:"STRING locale"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResultsLowerRange"
       and filter =
         flag "filter" (required json_arg) ~doc:"JSON EntityFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_affected_entities
           (Values.DescribeAffectedEntitiesRequest.make ?locale ?nextToken
              ?maxResults ~filter:(Values.EntityFilter.of_json filter) ())
           (Some Values.DescribeAffectedEntitiesResponse.to_json)
           (Some Values.DescribeAffectedEntitiesResponse.error_to_json)])
let describe_affected_entities_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationEntityFilters =
         flag "organization-entity-filters" (optional json_arg)
           ~doc:"JSON OrganizationEntityFiltersList"
       and locale = flag "locale" (optional string) ~doc:"STRING locale"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResultsLowerRange"
       and organizationEntityAccountFilters =
         flag "organization-entity-account-filters" (optional json_arg)
           ~doc:"JSON OrganizationEntityAccountFiltersList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_affected_entities_for_organization
           (Values.DescribeAffectedEntitiesForOrganizationRequest.make
              ?organizationEntityFilters:(Option.map
                                            ~f:Values.OrganizationEntityFiltersList.of_json
                                            organizationEntityFilters)
              ?locale ?nextToken ?maxResults
              ?organizationEntityAccountFilters:(Option.map
                                                   ~f:Values.OrganizationEntityAccountFiltersList.of_json
                                                   organizationEntityAccountFilters)
              ())
           (Some
              Values.DescribeAffectedEntitiesForOrganizationResponse.to_json)
           (Some
              Values.DescribeAffectedEntitiesForOrganizationResponse.error_to_json)])
let describe_entity_aggregates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventArns =
         flag "event-arns" (optional json_arg) ~doc:"JSON EventArnsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_entity_aggregates
           (Values.DescribeEntityAggregatesRequest.make
              ?eventArns:(Option.map ~f:Values.EventArnsList.of_json
                            eventArns) ())
           (Some Values.DescribeEntityAggregatesResponse.to_json)
           (Some Values.DescribeEntityAggregatesResponse.error_to_json)])
let describe_entity_aggregates_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountIds =
         flag "aws-account-ids" (optional json_arg)
           ~doc:"JSON OrganizationAccountIdsList"
       and eventArns =
         flag "event-arns" (required json_arg)
           ~doc:"JSON OrganizationEventArnsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_entity_aggregates_for_organization
           (Values.DescribeEntityAggregatesForOrganizationRequest.make
              ?awsAccountIds:(Option.map
                                ~f:Values.OrganizationAccountIdsList.of_json
                                awsAccountIds)
              ~eventArns:(Values.OrganizationEventArnsList.of_json eventArns)
              ())
           (Some
              Values.DescribeEntityAggregatesForOrganizationResponse.to_json)
           (Some
              Values.DescribeEntityAggregatesForOrganizationResponse.error_to_json)])
let describe_event_aggregates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON EventFilter"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and aggregateField =
         flag "aggregate-field" (required json_arg)
           ~doc:"JSON eventAggregateField" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_aggregates
           (Values.DescribeEventAggregatesRequest.make
              ?filter:(Option.map ~f:Values.EventFilter.of_json filter)
              ?maxResults ?nextToken
              ~aggregateField:(Values.EventAggregateField.of_json
                                 aggregateField) ())
           (Some Values.DescribeEventAggregatesResponse.to_json)
           (Some Values.DescribeEventAggregatesResponse.error_to_json)])
let describe_event_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locale = flag "locale" (optional string) ~doc:"STRING locale"
       and eventArns =
         flag "event-arns" (required json_arg) ~doc:"JSON eventArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_details
           (Values.DescribeEventDetailsRequest.make ?locale
              ~eventArns:(Values.EventArnList.of_json eventArns) ())
           (Some Values.DescribeEventDetailsResponse.to_json)
           (Some Values.DescribeEventDetailsResponse.error_to_json)])
let describe_event_details_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locale = flag "locale" (optional string) ~doc:"STRING locale"
       and organizationEventDetailFilters =
         flag "organization-event-detail-filters" (required json_arg)
           ~doc:"JSON OrganizationEventDetailFiltersList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_details_for_organization
           (Values.DescribeEventDetailsForOrganizationRequest.make ?locale
              ~organizationEventDetailFilters:(Values.OrganizationEventDetailFiltersList.of_json
                                                 organizationEventDetailFilters)
              ())
           (Some Values.DescribeEventDetailsForOrganizationResponse.to_json)
           (Some
              Values.DescribeEventDetailsForOrganizationResponse.error_to_json)])
let describe_event_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON EventTypeFilter"
       and locale = flag "locale" (optional string) ~doc:"STRING locale"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_types
           (Values.DescribeEventTypesRequest.make
              ?filter:(Option.map ~f:Values.EventTypeFilter.of_json filter)
              ?locale ?nextToken ?maxResults ())
           (Some Values.DescribeEventTypesResponse.to_json)
           (Some Values.DescribeEventTypesResponse.error_to_json)])
let describe_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON EventFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResultsLowerRange"
       and locale = flag "locale" (optional string) ~doc:"STRING locale" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_events
           (Values.DescribeEventsRequest.make
              ?filter:(Option.map ~f:Values.EventFilter.of_json filter)
              ?nextToken ?maxResults ?locale ())
           (Some Values.DescribeEventsResponse.to_json)
           (Some Values.DescribeEventsResponse.error_to_json)])
let describe_events_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON OrganizationEventFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResultsLowerRange"
       and locale = flag "locale" (optional string) ~doc:"STRING locale" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_events_for_organization
           (Values.DescribeEventsForOrganizationRequest.make
              ?filter:(Option.map ~f:Values.OrganizationEventFilter.of_json
                         filter) ?nextToken ?maxResults ?locale ())
           (Some Values.DescribeEventsForOrganizationResponse.to_json)
           (Some Values.DescribeEventsForOrganizationResponse.error_to_json)])
let describe_health_service_status_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_health_service_status_for_organization (Fn.id ())
           (Some
              Values.DescribeHealthServiceStatusForOrganizationResponse.to_json)
           (Some
              Values.DescribeHealthServiceStatusForOrganizationResponse.error_to_json)])
let disable_health_service_access_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_health_service_access_for_organization (Fn.id ()) None
           None])
let enable_health_service_access_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_health_service_access_for_organization (Fn.id ()) None
           None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("describe-affected-accounts-for-organization",
       describe_affected_accounts_for_organization);
    ("describe-affected-entities", describe_affected_entities);
    ("describe-affected-entities-for-organization",
      describe_affected_entities_for_organization);
    ("describe-entity-aggregates", describe_entity_aggregates);
    ("describe-entity-aggregates-for-organization",
      describe_entity_aggregates_for_organization);
    ("describe-event-aggregates", describe_event_aggregates);
    ("describe-event-details", describe_event_details);
    ("describe-event-details-for-organization",
      describe_event_details_for_organization);
    ("describe-event-types", describe_event_types);
    ("describe-events", describe_events);
    ("describe-events-for-organization", describe_events_for_organization);
    ("describe-health-service-status-for-organization",
      describe_health_service_status_for_organization);
    ("disable-health-service-access-for-organization",
      disable_health_service_access_for_organization);
    ("enable-health-service-access-for-organization",
      enable_health_service_access_for_organization)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_tags_to_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagList = flag "tag-list" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags_to_resource
           (Values.AddTagsToResourceRequest.make ~resourceArn
              ~tagList:(Values.TagList.of_json tagList) ())
           (Some Values.AddTagsToResourceResponse.to_json)
           (Some Values.AddTagsToResourceResponse.error_to_json)])
let create_hapg =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and label = flag "label" (required string) ~doc:"STRING Label" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_hapg (Values.CreateHapgRequest.make ~label ())
           (Some Values.CreateHapgResponse.to_json)
           (Some Values.CreateHapgResponse.error_to_json)])
let create_hsm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eniIp = flag "eni-ip" (optional string) ~doc:"STRING IpAddress"
       and externalId =
         flag "external-id" (optional string) ~doc:"STRING ExternalId"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and syslogIp =
         flag "syslog-ip" (optional string) ~doc:"STRING IpAddress"
       and subnetId =
         flag "subnet-id" (required string) ~doc:"STRING SubnetId"
       and sshKey = flag "ssh-key" (required string) ~doc:"STRING SshKey"
       and iamRoleArn =
         flag "iam-role-arn" (required string) ~doc:"STRING IamRoleArn"
       and subscriptionType =
         flag "subscription-type" (required json_arg)
           ~doc:"JSON SubscriptionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_hsm
           (Values.CreateHsmRequest.make ?eniIp ?externalId ?clientToken
              ?syslogIp ~subnetId ~sshKey ~iamRoleArn
              ~subscriptionType:(Values.SubscriptionType.of_json
                                   subscriptionType) ())
           (Some Values.CreateHsmResponse.to_json)
           (Some Values.CreateHsmResponse.error_to_json)])
let create_luna_client =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and label = flag "label" (optional string) ~doc:"STRING ClientLabel"
       and certificate =
         flag "certificate" (required string) ~doc:"STRING Certificate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_luna_client
           (Values.CreateLunaClientRequest.make ?label ~certificate ())
           (Some Values.CreateLunaClientResponse.to_json)
           (Some Values.CreateLunaClientResponse.error_to_json)])
let delete_hapg =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hapgArn = flag "hapg-arn" (required string) ~doc:"STRING HapgArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_hapg (Values.DeleteHapgRequest.make ~hapgArn ())
           (Some Values.DeleteHapgResponse.to_json)
           (Some Values.DeleteHapgResponse.error_to_json)])
let delete_hsm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hsmArn = flag "hsm-arn" (required string) ~doc:"STRING HsmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_hsm (Values.DeleteHsmRequest.make ~hsmArn ())
           (Some Values.DeleteHsmResponse.to_json)
           (Some Values.DeleteHsmResponse.error_to_json)])
let delete_luna_client =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientArn =
         flag "client-arn" (required string) ~doc:"STRING ClientArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_luna_client
           (Values.DeleteLunaClientRequest.make ~clientArn ())
           (Some Values.DeleteLunaClientResponse.to_json)
           (Some Values.DeleteLunaClientResponse.error_to_json)])
let describe_hapg =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hapgArn = flag "hapg-arn" (required string) ~doc:"STRING HapgArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_hapg (Values.DescribeHapgRequest.make ~hapgArn ())
           (Some Values.DescribeHapgResponse.to_json)
           (Some Values.DescribeHapgResponse.error_to_json)])
let describe_hsm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hsmArn = flag "hsm-arn" (optional string) ~doc:"STRING HsmArn"
       and hsmSerialNumber =
         flag "hsm-serial-number" (optional string)
           ~doc:"STRING HsmSerialNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_hsm
           (Values.DescribeHsmRequest.make ?hsmArn ?hsmSerialNumber ())
           (Some Values.DescribeHsmResponse.to_json)
           (Some Values.DescribeHsmResponse.error_to_json)])
let describe_luna_client =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientArn =
         flag "client-arn" (optional string) ~doc:"STRING ClientArn"
       and certificateFingerprint =
         flag "certificate-fingerprint" (optional string)
           ~doc:"STRING CertificateFingerprint" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_luna_client
           (Values.DescribeLunaClientRequest.make ?clientArn
              ?certificateFingerprint ())
           (Some Values.DescribeLunaClientResponse.to_json)
           (Some Values.DescribeLunaClientResponse.error_to_json)])
let get_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientArn =
         flag "client-arn" (required string) ~doc:"STRING ClientArn"
       and clientVersion =
         flag "client-version" (required json_arg) ~doc:"JSON ClientVersion"
       and hapgList =
         flag "hapg-list" (required json_arg) ~doc:"JSON HapgList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_config
           (Values.GetConfigRequest.make ~clientArn
              ~clientVersion:(Values.ClientVersion.of_json clientVersion)
              ~hapgList:(Values.HapgList.of_json hapgList) ())
           (Some Values.GetConfigResponse.to_json)
           (Some Values.GetConfigResponse.error_to_json)])
let list_available_zones =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_available_zones (Values.ListAvailableZonesRequest.make ())
           (Some Values.ListAvailableZonesResponse.to_json)
           (Some Values.ListAvailableZonesResponse.error_to_json)])
let list_hapgs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_hapgs (Values.ListHapgsRequest.make ?nextToken ())
           (Some Values.ListHapgsResponse.to_json)
           (Some Values.ListHapgsResponse.error_to_json)])
let list_hsms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_hsms (Values.ListHsmsRequest.make ?nextToken ())
           (Some Values.ListHsmsResponse.to_json)
           (Some Values.ListHsmsResponse.error_to_json)])
let list_luna_clients =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_luna_clients
           (Values.ListLunaClientsRequest.make ?nextToken ())
           (Some Values.ListLunaClientsResponse.to_json)
           (Some Values.ListLunaClientsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let modify_hapg =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and label = flag "label" (optional string) ~doc:"STRING Label"
       and partitionSerialList =
         flag "partition-serial-list" (optional json_arg)
           ~doc:"JSON PartitionSerialList"
       and hapgArn = flag "hapg-arn" (required string) ~doc:"STRING HapgArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_hapg
           (Values.ModifyHapgRequest.make ?label
              ?partitionSerialList:(Option.map
                                      ~f:Values.PartitionSerialList.of_json
                                      partitionSerialList) ~hapgArn ())
           (Some Values.ModifyHapgResponse.to_json)
           (Some Values.ModifyHapgResponse.error_to_json)])
let modify_hsm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subnetId =
         flag "subnet-id" (optional string) ~doc:"STRING SubnetId"
       and eniIp = flag "eni-ip" (optional string) ~doc:"STRING IpAddress"
       and iamRoleArn =
         flag "iam-role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and externalId =
         flag "external-id" (optional string) ~doc:"STRING ExternalId"
       and syslogIp =
         flag "syslog-ip" (optional string) ~doc:"STRING IpAddress"
       and hsmArn = flag "hsm-arn" (required string) ~doc:"STRING HsmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_hsm
           (Values.ModifyHsmRequest.make ?subnetId ?eniIp ?iamRoleArn
              ?externalId ?syslogIp ~hsmArn ())
           (Some Values.ModifyHsmResponse.to_json)
           (Some Values.ModifyHsmResponse.error_to_json)])
let modify_luna_client =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientArn =
         flag "client-arn" (required string) ~doc:"STRING ClientArn"
       and certificate =
         flag "certificate" (required string) ~doc:"STRING Certificate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_luna_client
           (Values.ModifyLunaClientRequest.make ~clientArn ~certificate ())
           (Some Values.ModifyLunaClientResponse.to_json)
           (Some Values.ModifyLunaClientResponse.error_to_json)])
let remove_tags_from_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeyList =
         flag "tag-key-list" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_tags_from_resource
           (Values.RemoveTagsFromResourceRequest.make ~resourceArn
              ~tagKeyList:(Values.TagKeyList.of_json tagKeyList) ())
           (Some Values.RemoveTagsFromResourceResponse.to_json)
           (Some Values.RemoveTagsFromResourceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-tags-to-resource", add_tags_to_resource);
    ("create-hapg", create_hapg);
    ("create-hsm", create_hsm);
    ("create-luna-client", create_luna_client);
    ("delete-hapg", delete_hapg);
    ("delete-hsm", delete_hsm);
    ("delete-luna-client", delete_luna_client);
    ("describe-hapg", describe_hapg);
    ("describe-hsm", describe_hsm);
    ("describe-luna-client", describe_luna_client);
    ("get-config", get_config);
    ("list-available-zones", list_available_zones);
    ("list-hapgs", list_hapgs);
    ("list-hsms", list_hsms);
    ("list-luna-clients", list_luna_clients);
    ("list-tags-for-resource", list_tags_for_resource);
    ("modify-hapg", modify_hapg);
    ("modify-hsm", modify_hsm);
    ("modify-luna-client", modify_luna_client);
    ("remove-tags-from-resource", remove_tags_from_resource)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | GetEstimatedCarbonEmissions: (GetEstimatedCarbonEmissionsRequest.t,
  GetEstimatedCarbonEmissionsResponse.t,
  GetEstimatedCarbonEmissionsResponse.error) t 
  | GetEstimatedCarbonEmissionsDimensionValues:
  (GetEstimatedCarbonEmissionsDimensionValuesRequest.t,
  GetEstimatedCarbonEmissionsDimensionValuesResponse.t,
  GetEstimatedCarbonEmissionsDimensionValuesResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | GetEstimatedCarbonEmissions -> `POST
  | GetEstimatedCarbonEmissionsDimensionValues -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | GetEstimatedCarbonEmissions ->
          (Format.kasprintf Uri.of_string) "/v1/estimated-carbon-emissions"
      | GetEstimatedCarbonEmissionsDimensionValues ->
          (Format.kasprintf Uri.of_string)
            "/v1/estimated-carbon-emissions-dimension-values")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | GetEstimatedCarbonEmissions ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("TimePeriod",
                           (TimePeriod.to_value
                              req.GetEstimatedCarbonEmissionsRequest.timePeriod));
                      Option.map
                        req.GetEstimatedCarbonEmissionsRequest.groupBy
                        ~f:(fun x -> ("GroupBy", (DimensionList.to_value x)));
                      Option.map
                        req.GetEstimatedCarbonEmissionsRequest.filterBy
                        ~f:(fun x ->
                              ("FilterBy", (FilterExpression.to_value x)));
                      Option.map
                        req.GetEstimatedCarbonEmissionsRequest.emissionsTypes
                        ~f:(fun x ->
                              ("EmissionsTypes",
                                (EmissionsTypeList.to_value x)));
                      Option.map
                        req.GetEstimatedCarbonEmissionsRequest.granularity
                        ~f:(fun x ->
                              ("Granularity", (TimeGranularity.to_value x)));
                      Option.map
                        req.GetEstimatedCarbonEmissionsRequest.granularityConfiguration
                        ~f:(fun x ->
                              ("GranularityConfiguration",
                                (GranularityConfiguration.to_value x)));
                      Option.map
                        req.GetEstimatedCarbonEmissionsRequest.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map
                        req.GetEstimatedCarbonEmissionsRequest.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetEstimatedCarbonEmissionsDimensionValues ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("TimePeriod",
                           (TimePeriod.to_value
                              req.GetEstimatedCarbonEmissionsDimensionValuesRequest.timePeriod));
                      Some
                        ("Dimensions",
                          (DimensionList.to_value
                             req.GetEstimatedCarbonEmissionsDimensionValuesRequest.dimensions));
                      Option.map
                        req.GetEstimatedCarbonEmissionsDimensionValuesRequest.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map
                        req.GetEstimatedCarbonEmissionsDimensionValuesRequest.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | GetEstimatedCarbonEmissions ->
      if is_success
      then
        Ok
          (GetEstimatedCarbonEmissionsResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetEstimatedCarbonEmissionsResponse.error_of_json))
  | GetEstimatedCarbonEmissionsDimensionValues ->
      if is_success
      then
        Ok
          (GetEstimatedCarbonEmissionsDimensionValuesResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some
                GetEstimatedCarbonEmissionsDimensionValuesResponse.error_of_json))
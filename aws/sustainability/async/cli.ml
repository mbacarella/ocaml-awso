(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let get_estimated_carbon_emissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupBy =
         flag "group-by" (optional json_arg) ~doc:"JSON DimensionList"
       and filterBy =
         flag "filter-by" (optional json_arg) ~doc:"JSON FilterExpression"
       and emissionsTypes =
         flag "emissions-types" (optional json_arg)
           ~doc:"JSON EmissionsTypeList"
       and granularity =
         flag "granularity" (optional json_arg) ~doc:"JSON TimeGranularity"
       and granularityConfiguration =
         flag "granularity-configuration" (optional json_arg)
           ~doc:"JSON GranularityConfiguration"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON TimePeriod" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_estimated_carbon_emissions
           (Values.GetEstimatedCarbonEmissionsRequest.make
              ?groupBy:(Option.map ~f:Values.DimensionList.of_json groupBy)
              ?filterBy:(Option.map ~f:Values.FilterExpression.of_json
                           filterBy)
              ?emissionsTypes:(Option.map ~f:Values.EmissionsTypeList.of_json
                                 emissionsTypes)
              ?granularity:(Option.map ~f:Values.TimeGranularity.of_json
                              granularity)
              ?granularityConfiguration:(Option.map
                                           ~f:Values.GranularityConfiguration.of_json
                                           granularityConfiguration)
              ?maxResults ?nextToken
              ~timePeriod:(Values.TimePeriod.of_json timePeriod) ())
           (Some Values.GetEstimatedCarbonEmissionsResponse.to_json)
           (Some Values.GetEstimatedCarbonEmissionsResponse.error_to_json)])
let get_estimated_carbon_emissions_dimension_values =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON TimePeriod"
       and dimensions =
         flag "dimensions" (required json_arg) ~doc:"JSON DimensionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_estimated_carbon_emissions_dimension_values
           (Values.GetEstimatedCarbonEmissionsDimensionValuesRequest.make
              ?maxResults ?nextToken
              ~timePeriod:(Values.TimePeriod.of_json timePeriod)
              ~dimensions:(Values.DimensionList.of_json dimensions) ())
           (Some
              Values.GetEstimatedCarbonEmissionsDimensionValuesResponse.to_json)
           (Some
              Values.GetEstimatedCarbonEmissionsDimensionValuesResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("get-estimated-carbon-emissions", get_estimated_carbon_emissions);
    ("get-estimated-carbon-emissions-dimension-values",
      get_estimated_carbon_emissions_dimension_values)]
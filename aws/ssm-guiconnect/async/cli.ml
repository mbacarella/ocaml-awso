(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let delete_connection_recording_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connection_recording_preferences
           (Values.DeleteConnectionRecordingPreferencesRequest.make
              ?clientToken ())
           (Some Values.DeleteConnectionRecordingPreferencesResponse.to_json)
           (Some
              Values.DeleteConnectionRecordingPreferencesResponse.error_to_json)])
let get_connection_recording_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connection_recording_preferences (Fn.id ())
           (Some Values.GetConnectionRecordingPreferencesResponse.to_json)
           (Some
              Values.GetConnectionRecordingPreferencesResponse.error_to_json)])
let update_connection_recording_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and connectionRecordingPreferences =
         flag "connection-recording-preferences" (required json_arg)
           ~doc:"JSON ConnectionRecordingPreferences" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connection_recording_preferences
           (Values.UpdateConnectionRecordingPreferencesRequest.make
              ?clientToken
              ~connectionRecordingPreferences:(Values.ConnectionRecordingPreferences.of_json
                                                 connectionRecordingPreferences)
              ())
           (Some Values.UpdateConnectionRecordingPreferencesResponse.to_json)
           (Some
              Values.UpdateConnectionRecordingPreferencesResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("delete-connection-recording-preferences",
       delete_connection_recording_preferences);
    ("get-connection-recording-preferences",
      get_connection_recording_preferences);
    ("update-connection-recording-preferences",
      update_connection_recording_preferences)]
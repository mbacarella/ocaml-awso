(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_application_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationInstanceIdToReplace =
         flag "application-instance-id-to-replace" (optional string)
           ~doc:"STRING ApplicationInstanceId"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and manifestOverridesPayload =
         flag "manifest-overrides-payload" (optional json_arg)
           ~doc:"JSON ManifestOverridesPayload"
       and name =
         flag "name" (optional string) ~doc:"STRING ApplicationInstanceName"
       and runtimeRoleArn =
         flag "runtime-role-arn" (optional string)
           ~doc:"STRING RuntimeRoleArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and defaultRuntimeContextDevice =
         flag "default-runtime-context-device" (required string)
           ~doc:"STRING DefaultRuntimeContextDevice"
       and manifestPayload =
         flag "manifest-payload" (required json_arg)
           ~doc:"JSON ManifestPayload" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application_instance
           (Values.CreateApplicationInstanceRequest.make
              ?applicationInstanceIdToReplace ?description
              ?manifestOverridesPayload:(Option.map
                                           ~f:Values.ManifestOverridesPayload.of_json
                                           manifestOverridesPayload) ?name
              ?runtimeRoleArn
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~defaultRuntimeContextDevice
              ~manifestPayload:(Values.ManifestPayload.of_json
                                  manifestPayload) ())
           (Some Values.CreateApplicationInstanceResponse.to_json)
           (Some Values.CreateApplicationInstanceResponse.error_to_json)])
let create_job_for_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceIds =
         flag "device-ids" (required json_arg) ~doc:"JSON DeviceIdList"
       and deviceJobConfig =
         flag "device-job-config" (required json_arg)
           ~doc:"JSON DeviceJobConfig"
       and jobType = flag "job-type" (required json_arg) ~doc:"JSON JobType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_job_for_devices
           (Values.CreateJobForDevicesRequest.make
              ~deviceIds:(Values.DeviceIdList.of_json deviceIds)
              ~deviceJobConfig:(Values.DeviceJobConfig.of_json
                                  deviceJobConfig)
              ~jobType:(Values.JobType.of_json jobType) ())
           (Some Values.CreateJobForDevicesResponse.to_json)
           (Some Values.CreateJobForDevicesResponse.error_to_json)])
let create_node_from_template_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobTags =
         flag "job-tags" (optional json_arg) ~doc:"JSON JobTagsList"
       and nodeDescription =
         flag "node-description" (optional string) ~doc:"STRING Description"
       and nodeName =
         flag "node-name" (required string) ~doc:"STRING NodeName"
       and outputPackageName =
         flag "output-package-name" (required string)
           ~doc:"STRING NodePackageName"
       and outputPackageVersion =
         flag "output-package-version" (required string)
           ~doc:"STRING NodePackageVersion"
       and templateParameters =
         flag "template-parameters" (required json_arg)
           ~doc:"JSON TemplateParametersMap"
       and templateType =
         flag "template-type" (required json_arg) ~doc:"JSON TemplateType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_node_from_template_job
           (Values.CreateNodeFromTemplateJobRequest.make
              ?jobTags:(Option.map ~f:Values.JobTagsList.of_json jobTags)
              ?nodeDescription ~nodeName ~outputPackageName
              ~outputPackageVersion
              ~templateParameters:(Values.TemplateParametersMap.of_json
                                     templateParameters)
              ~templateType:(Values.TemplateType.of_json templateType) ())
           (Some Values.CreateNodeFromTemplateJobResponse.to_json)
           (Some Values.CreateNodeFromTemplateJobResponse.error_to_json)])
let create_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and packageName =
         flag "package-name" (required string) ~doc:"STRING NodePackageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_package
           (Values.CreatePackageRequest.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~packageName
              ()) (Some Values.CreatePackageResponse.to_json)
           (Some Values.CreatePackageResponse.error_to_json)])
let create_package_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobTags =
         flag "job-tags" (optional json_arg) ~doc:"JSON JobTagsList"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and inputConfig =
         flag "input-config" (required json_arg)
           ~doc:"JSON PackageImportJobInputConfig"
       and jobType =
         flag "job-type" (required json_arg) ~doc:"JSON PackageImportJobType"
       and outputConfig =
         flag "output-config" (required json_arg)
           ~doc:"JSON PackageImportJobOutputConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_package_import_job
           (Values.CreatePackageImportJobRequest.make
              ?jobTags:(Option.map ~f:Values.JobTagsList.of_json jobTags)
              ~clientToken
              ~inputConfig:(Values.PackageImportJobInputConfig.of_json
                              inputConfig)
              ~jobType:(Values.PackageImportJobType.of_json jobType)
              ~outputConfig:(Values.PackageImportJobOutputConfig.of_json
                               outputConfig) ())
           (Some Values.CreatePackageImportJobResponse.to_json)
           (Some Values.CreatePackageImportJobResponse.error_to_json)])
let delete_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_device (Values.DeleteDeviceRequest.make ~deviceId ())
           (Some Values.DeleteDeviceResponse.to_json)
           (Some Values.DeleteDeviceResponse.error_to_json)])
let delete_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL Boolean"
       and packageId =
         flag "package-id" (required string) ~doc:"STRING NodePackageId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_package
           (Values.DeletePackageRequest.make ?forceDelete ~packageId ())
           (Some Values.DeletePackageResponse.to_json)
           (Some Values.DeletePackageResponse.error_to_json)])
let deregister_package_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ownerAccount =
         flag "owner-account" (optional string)
           ~doc:"STRING PackageOwnerAccount"
       and updatedLatestPatchVersion =
         flag "updated-latest-patch-version" (optional string)
           ~doc:"STRING NodePackagePatchVersion"
       and packageId =
         flag "package-id" (required string) ~doc:"STRING NodePackageId"
       and packageVersion =
         flag "package-version" (required string)
           ~doc:"STRING NodePackageVersion"
       and patchVersion =
         flag "patch-version" (required string)
           ~doc:"STRING NodePackagePatchVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_package_version
           (Values.DeregisterPackageVersionRequest.make ?ownerAccount
              ?updatedLatestPatchVersion ~packageId ~packageVersion
              ~patchVersion ())
           (Some Values.DeregisterPackageVersionResponse.to_json)
           (Some Values.DeregisterPackageVersionResponse.error_to_json)])
let describe_application_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationInstanceId =
         flag "application-instance-id" (required string)
           ~doc:"STRING ApplicationInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_application_instance
           (Values.DescribeApplicationInstanceRequest.make
              ~applicationInstanceId ())
           (Some Values.DescribeApplicationInstanceResponse.to_json)
           (Some Values.DescribeApplicationInstanceResponse.error_to_json)])
let describe_application_instance_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationInstanceId =
         flag "application-instance-id" (required string)
           ~doc:"STRING ApplicationInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_application_instance_details
           (Values.DescribeApplicationInstanceDetailsRequest.make
              ~applicationInstanceId ())
           (Some Values.DescribeApplicationInstanceDetailsResponse.to_json)
           (Some
              Values.DescribeApplicationInstanceDetailsResponse.error_to_json)])
let describe_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_device
           (Values.DescribeDeviceRequest.make ~deviceId ())
           (Some Values.DescribeDeviceResponse.to_json)
           (Some Values.DescribeDeviceResponse.error_to_json)])
let describe_device_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_device_job
           (Values.DescribeDeviceJobRequest.make ~jobId ())
           (Some Values.DescribeDeviceJobResponse.to_json)
           (Some Values.DescribeDeviceJobResponse.error_to_json)])
let describe_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ownerAccount =
         flag "owner-account" (optional string)
           ~doc:"STRING PackageOwnerAccount"
       and nodeId = flag "node-id" (required string) ~doc:"STRING NodeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_node
           (Values.DescribeNodeRequest.make ?ownerAccount ~nodeId ())
           (Some Values.DescribeNodeResponse.to_json)
           (Some Values.DescribeNodeResponse.error_to_json)])
let describe_node_from_template_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_node_from_template_job
           (Values.DescribeNodeFromTemplateJobRequest.make ~jobId ())
           (Some Values.DescribeNodeFromTemplateJobResponse.to_json)
           (Some Values.DescribeNodeFromTemplateJobResponse.error_to_json)])
let describe_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and packageId =
         flag "package-id" (required string) ~doc:"STRING NodePackageId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_package
           (Values.DescribePackageRequest.make ~packageId ())
           (Some Values.DescribePackageResponse.to_json)
           (Some Values.DescribePackageResponse.error_to_json)])
let describe_package_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_package_import_job
           (Values.DescribePackageImportJobRequest.make ~jobId ())
           (Some Values.DescribePackageImportJobResponse.to_json)
           (Some Values.DescribePackageImportJobResponse.error_to_json)])
let describe_package_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ownerAccount =
         flag "owner-account" (optional string)
           ~doc:"STRING PackageOwnerAccount"
       and patchVersion =
         flag "patch-version" (optional string)
           ~doc:"STRING NodePackagePatchVersion"
       and packageId =
         flag "package-id" (required string) ~doc:"STRING NodePackageId"
       and packageVersion =
         flag "package-version" (required string)
           ~doc:"STRING NodePackageVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_package_version
           (Values.DescribePackageVersionRequest.make ?ownerAccount
              ?patchVersion ~packageId ~packageVersion ())
           (Some Values.DescribePackageVersionResponse.to_json)
           (Some Values.DescribePackageVersionResponse.error_to_json)])
let list_application_instance_dependencies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize25"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and applicationInstanceId =
         flag "application-instance-id" (required string)
           ~doc:"STRING ApplicationInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_application_instance_dependencies
           (Values.ListApplicationInstanceDependenciesRequest.make
              ?maxResults ?nextToken ~applicationInstanceId ())
           (Some Values.ListApplicationInstanceDependenciesResponse.to_json)
           (Some
              Values.ListApplicationInstanceDependenciesResponse.error_to_json)])
let list_application_instance_node_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize25"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and applicationInstanceId =
         flag "application-instance-id" (required string)
           ~doc:"STRING ApplicationInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_application_instance_node_instances
           (Values.ListApplicationInstanceNodeInstancesRequest.make
              ?maxResults ?nextToken ~applicationInstanceId ())
           (Some Values.ListApplicationInstanceNodeInstancesResponse.to_json)
           (Some
              Values.ListApplicationInstanceNodeInstancesResponse.error_to_json)])
let list_application_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceId =
         flag "device-id" (optional string) ~doc:"STRING DeviceId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize25"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and statusFilter =
         flag "status-filter" (optional json_arg) ~doc:"JSON StatusFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_application_instances
           (Values.ListApplicationInstancesRequest.make ?deviceId ?maxResults
              ?nextToken
              ?statusFilter:(Option.map ~f:Values.StatusFilter.of_json
                               statusFilter) ())
           (Some Values.ListApplicationInstancesResponse.to_json)
           (Some Values.ListApplicationInstancesResponse.error_to_json)])
let list_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize25"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_devices
           (Values.ListDevicesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListDevicesResponse.to_json)
           (Some Values.ListDevicesResponse.error_to_json)])
let list_devices_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceId =
         flag "device-id" (optional string) ~doc:"STRING DeviceId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize25"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_devices_jobs
           (Values.ListDevicesJobsRequest.make ?deviceId ?maxResults
              ?nextToken ()) (Some Values.ListDevicesJobsResponse.to_json)
           (Some Values.ListDevicesJobsResponse.error_to_json)])
let list_node_from_template_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize25"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_node_from_template_jobs
           (Values.ListNodeFromTemplateJobsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListNodeFromTemplateJobsResponse.to_json)
           (Some Values.ListNodeFromTemplateJobsResponse.error_to_json)])
let list_nodes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and category =
         flag "category" (optional json_arg) ~doc:"JSON NodeCategory"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize25"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and ownerAccount =
         flag "owner-account" (optional string)
           ~doc:"STRING PackageOwnerAccount"
       and packageName =
         flag "package-name" (optional string) ~doc:"STRING NodePackageName"
       and packageVersion =
         flag "package-version" (optional string)
           ~doc:"STRING NodePackageVersion"
       and patchVersion =
         flag "patch-version" (optional string)
           ~doc:"STRING NodePackagePatchVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_nodes
           (Values.ListNodesRequest.make
              ?category:(Option.map ~f:Values.NodeCategory.of_json category)
              ?maxResults ?nextToken ?ownerAccount ?packageName
              ?packageVersion ?patchVersion ())
           (Some Values.ListNodesResponse.to_json)
           (Some Values.ListNodesResponse.error_to_json)])
let list_package_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize25"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_package_import_jobs
           (Values.ListPackageImportJobsRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListPackageImportJobsResponse.to_json)
           (Some Values.ListPackageImportJobsResponse.error_to_json)])
let list_packages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize25"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_packages
           (Values.ListPackagesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListPackagesResponse.to_json)
           (Some Values.ListPackagesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let provision_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and networkingConfiguration =
         flag "networking-configuration" (optional json_arg)
           ~doc:"JSON NetworkPayload"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING DeviceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.provision_device
           (Values.ProvisionDeviceRequest.make ?description
              ?networkingConfiguration:(Option.map
                                          ~f:Values.NetworkPayload.of_json
                                          networkingConfiguration)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name ())
           (Some Values.ProvisionDeviceResponse.to_json)
           (Some Values.ProvisionDeviceResponse.error_to_json)])
let register_package_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and markLatest =
         flag "mark-latest" (optional bool) ~doc:"BOOL MarkLatestPatch"
       and ownerAccount =
         flag "owner-account" (optional string)
           ~doc:"STRING PackageOwnerAccount"
       and packageId =
         flag "package-id" (required string) ~doc:"STRING NodePackageId"
       and packageVersion =
         flag "package-version" (required string)
           ~doc:"STRING NodePackageVersion"
       and patchVersion =
         flag "patch-version" (required string)
           ~doc:"STRING NodePackagePatchVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_package_version
           (Values.RegisterPackageVersionRequest.make ?markLatest
              ?ownerAccount ~packageId ~packageVersion ~patchVersion ())
           (Some Values.RegisterPackageVersionResponse.to_json)
           (Some Values.RegisterPackageVersionResponse.error_to_json)])
let remove_application_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationInstanceId =
         flag "application-instance-id" (required string)
           ~doc:"STRING ApplicationInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_application_instance
           (Values.RemoveApplicationInstanceRequest.make
              ~applicationInstanceId ())
           (Some Values.RemoveApplicationInstanceResponse.to_json)
           (Some Values.RemoveApplicationInstanceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_device_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_device_metadata
           (Values.UpdateDeviceMetadataRequest.make ?description ~deviceId ())
           (Some Values.UpdateDeviceMetadataResponse.to_json)
           (Some Values.UpdateDeviceMetadataResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-application-instance", create_application_instance);
    ("create-job-for-devices", create_job_for_devices);
    ("create-node-from-template-job", create_node_from_template_job);
    ("create-package", create_package);
    ("create-package-import-job", create_package_import_job);
    ("delete-device", delete_device);
    ("delete-package", delete_package);
    ("deregister-package-version", deregister_package_version);
    ("describe-application-instance", describe_application_instance);
    ("describe-application-instance-details",
      describe_application_instance_details);
    ("describe-device", describe_device);
    ("describe-device-job", describe_device_job);
    ("describe-node", describe_node);
    ("describe-node-from-template-job", describe_node_from_template_job);
    ("describe-package", describe_package);
    ("describe-package-import-job", describe_package_import_job);
    ("describe-package-version", describe_package_version);
    ("list-application-instance-dependencies",
      list_application_instance_dependencies);
    ("list-application-instance-node-instances",
      list_application_instance_node_instances);
    ("list-application-instances", list_application_instances);
    ("list-devices", list_devices);
    ("list-devices-jobs", list_devices_jobs);
    ("list-node-from-template-jobs", list_node_from_template_jobs);
    ("list-nodes", list_nodes);
    ("list-package-import-jobs", list_package_import_jobs);
    ("list-packages", list_packages);
    ("list-tags-for-resource", list_tags_for_resource);
    ("provision-device", provision_device);
    ("register-package-version", register_package_version);
    ("remove-application-instance", remove_application_instance);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-device-metadata", update_device_metadata)]
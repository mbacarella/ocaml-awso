(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_game =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING GameDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_game
           (Values.CreateGameRequest.make ?clientToken ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~gameName ())
           (Some Values.CreateGameResult.to_json)
           (Some Values.CreateGameResult.error_to_json)])
let create_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING SnapshotDescription"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_snapshot
           (Values.CreateSnapshotRequest.make ?description ~gameName ())
           (Some Values.CreateSnapshotResult.to_json)
           (Some Values.CreateSnapshotResult.error_to_json)])
let create_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING StageDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and role = flag "role" (required string) ~doc:"STRING RoleARN"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_stage
           (Values.CreateStageRequest.make ?clientToken ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~gameName
              ~role ~stageName ()) (Some Values.CreateStageResult.to_json)
           (Some Values.CreateStageResult.error_to_json)])
let delete_game =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_game (Values.DeleteGameRequest.make ~gameName ())
           (Some Values.DeleteGameResult.to_json)
           (Some Values.DeleteGameResult.error_to_json)])
let delete_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_stage
           (Values.DeleteStageRequest.make ~gameName ~stageName ())
           (Some Values.DeleteStageResult.to_json)
           (Some Values.DeleteStageResult.error_to_json)])
let disconnect_player =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and playerId =
         flag "player-id" (required string) ~doc:"STRING PlayerId"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disconnect_player
           (Values.DisconnectPlayerRequest.make ~gameName ~playerId
              ~stageName ()) (Some Values.DisconnectPlayerResult.to_json)
           (Some Values.DisconnectPlayerResult.error_to_json)])
let export_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_snapshot
           (Values.ExportSnapshotRequest.make ~gameName ~snapshotId ())
           (Some Values.ExportSnapshotResult.to_json)
           (Some Values.ExportSnapshotResult.error_to_json)])
let get_extension =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ExtensionName"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING ExtensionNamespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_extension
           (Values.GetExtensionRequest.make ~name ~namespace ())
           (Some Values.GetExtensionResult.to_json)
           (Some Values.GetExtensionResult.error_to_json)])
let get_extension_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and extensionVersion =
         flag "extension-version" (required string)
           ~doc:"STRING ExtensionVersion"
       and name = flag "name" (required string) ~doc:"STRING ExtensionName"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING ExtensionNamespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_extension_version
           (Values.GetExtensionVersionRequest.make ~extensionVersion ~name
              ~namespace ()) (Some Values.GetExtensionVersionResult.to_json)
           (Some Values.GetExtensionVersionResult.error_to_json)])
let get_game =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_game (Values.GetGameRequest.make ~gameName ())
           (Some Values.GetGameResult.to_json)
           (Some Values.GetGameResult.error_to_json)])
let get_game_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sections =
         flag "sections" (optional json_arg) ~doc:"JSON SectionList"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_game_configuration
           (Values.GetGameConfigurationRequest.make
              ?sections:(Option.map ~f:Values.SectionList.of_json sections)
              ~gameName ()) (Some Values.GetGameConfigurationResult.to_json)
           (Some Values.GetGameConfigurationResult.error_to_json)])
let get_generated_code_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and jobId =
         flag "job-id" (required string) ~doc:"STRING GeneratedCodeJobId"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_generated_code_job
           (Values.GetGeneratedCodeJobRequest.make ~gameName ~jobId
              ~snapshotId ()) (Some Values.GetGeneratedCodeJobResult.to_json)
           (Some Values.GetGeneratedCodeJobResult.error_to_json)])
let get_player_connection_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and playerId =
         flag "player-id" (required string) ~doc:"STRING PlayerId"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_player_connection_status
           (Values.GetPlayerConnectionStatusRequest.make ~gameName ~playerId
              ~stageName ())
           (Some Values.GetPlayerConnectionStatusResult.to_json)
           (Some Values.GetPlayerConnectionStatusResult.error_to_json)])
let get_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sections =
         flag "sections" (optional json_arg) ~doc:"JSON SectionList"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_snapshot
           (Values.GetSnapshotRequest.make
              ?sections:(Option.map ~f:Values.SectionList.of_json sections)
              ~gameName ~snapshotId ())
           (Some Values.GetSnapshotResult.to_json)
           (Some Values.GetSnapshotResult.error_to_json)])
let get_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_stage (Values.GetStageRequest.make ~gameName ~stageName ())
           (Some Values.GetStageResult.to_json)
           (Some Values.GetStageResult.error_to_json)])
let get_stage_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deploymentId =
         flag "deployment-id" (optional string) ~doc:"STRING DeploymentId"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_stage_deployment
           (Values.GetStageDeploymentRequest.make ?deploymentId ~gameName
              ~stageName ()) (Some Values.GetStageDeploymentResult.to_json)
           (Some Values.GetStageDeploymentResult.error_to_json)])
let import_game_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and importSource =
         flag "import-source" (required json_arg)
           ~doc:"JSON ImportGameConfigurationSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_game_configuration
           (Values.ImportGameConfigurationRequest.make ~gameName
              ~importSource:(Values.ImportGameConfigurationSource.of_json
                               importSource) ())
           (Some Values.ImportGameConfigurationResult.to_json)
           (Some Values.ImportGameConfigurationResult.error_to_json)])
let list_extension_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and name = flag "name" (required string) ~doc:"STRING ExtensionName"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING ExtensionNamespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_extension_versions
           (Values.ListExtensionVersionsRequest.make ?maxResults ?nextToken
              ~name ~namespace ())
           (Some Values.ListExtensionVersionsResult.to_json)
           (Some Values.ListExtensionVersionsResult.error_to_json)])
let list_extensions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_extensions
           (Values.ListExtensionsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListExtensionsResult.to_json)
           (Some Values.ListExtensionsResult.error_to_json)])
let list_games =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_games
           (Values.ListGamesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListGamesResult.to_json)
           (Some Values.ListGamesResult.error_to_json)])
let list_generated_code_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_generated_code_jobs
           (Values.ListGeneratedCodeJobsRequest.make ?maxResults ?nextToken
              ~gameName ~snapshotId ())
           (Some Values.ListGeneratedCodeJobsResult.to_json)
           (Some Values.ListGeneratedCodeJobsResult.error_to_json)])
let list_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_snapshots
           (Values.ListSnapshotsRequest.make ?maxResults ?nextToken ~gameName
              ()) (Some Values.ListSnapshotsResult.to_json)
           (Some Values.ListSnapshotsResult.error_to_json)])
let list_stage_deployments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stage_deployments
           (Values.ListStageDeploymentsRequest.make ?maxResults ?nextToken
              ~gameName ~stageName ())
           (Some Values.ListStageDeploymentsResult.to_json)
           (Some Values.ListStageDeploymentsResult.error_to_json)])
let list_stages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stages
           (Values.ListStagesRequest.make ?maxResults ?nextToken ~gameName ())
           (Some Values.ListStagesResult.to_json)
           (Some Values.ListStagesResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResult.to_json)
           (Some Values.ListTagsForResourceResult.error_to_json)])
let start_generated_code_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and generator =
         flag "generator" (required json_arg) ~doc:"JSON Generator"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_generated_code_job
           (Values.StartGeneratedCodeJobRequest.make ~gameName
              ~generator:(Values.Generator.of_json generator) ~snapshotId ())
           (Some Values.StartGeneratedCodeJobResult.to_json)
           (Some Values.StartGeneratedCodeJobResult.error_to_json)])
let start_stage_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_stage_deployment
           (Values.StartStageDeploymentRequest.make ?clientToken ~gameName
              ~snapshotId ~stageName ())
           (Some Values.StartStageDeploymentResult.to_json)
           (Some Values.StartStageDeploymentResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResult.to_json)
           (Some Values.TagResourceResult.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResult.to_json)
           (Some Values.UntagResourceResult.error_to_json)])
let update_game =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING GameDescription"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_game
           (Values.UpdateGameRequest.make ?description ~gameName ())
           (Some Values.UpdateGameResult.to_json)
           (Some Values.UpdateGameResult.error_to_json)])
let update_game_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and modifications =
         flag "modifications" (required json_arg)
           ~doc:"JSON SectionModificationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_game_configuration
           (Values.UpdateGameConfigurationRequest.make ~gameName
              ~modifications:(Values.SectionModificationList.of_json
                                modifications) ())
           (Some Values.UpdateGameConfigurationResult.to_json)
           (Some Values.UpdateGameConfigurationResult.error_to_json)])
let update_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING SnapshotDescription"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_snapshot
           (Values.UpdateSnapshotRequest.make ?description ~gameName
              ~snapshotId ()) (Some Values.UpdateSnapshotResult.to_json)
           (Some Values.UpdateSnapshotResult.error_to_json)])
let update_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING StageDescription"
       and role = flag "role" (optional string) ~doc:"STRING RoleARN"
       and gameName =
         flag "game-name" (required string) ~doc:"STRING GameName"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING StageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_stage
           (Values.UpdateStageRequest.make ?description ?role ~gameName
              ~stageName ()) (Some Values.UpdateStageResult.to_json)
           (Some Values.UpdateStageResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-game", create_game);
    ("create-snapshot", create_snapshot);
    ("create-stage", create_stage);
    ("delete-game", delete_game);
    ("delete-stage", delete_stage);
    ("disconnect-player", disconnect_player);
    ("export-snapshot", export_snapshot);
    ("get-extension", get_extension);
    ("get-extension-version", get_extension_version);
    ("get-game", get_game);
    ("get-game-configuration", get_game_configuration);
    ("get-generated-code-job", get_generated_code_job);
    ("get-player-connection-status", get_player_connection_status);
    ("get-snapshot", get_snapshot);
    ("get-stage", get_stage);
    ("get-stage-deployment", get_stage_deployment);
    ("import-game-configuration", import_game_configuration);
    ("list-extension-versions", list_extension_versions);
    ("list-extensions", list_extensions);
    ("list-games", list_games);
    ("list-generated-code-jobs", list_generated_code_jobs);
    ("list-snapshots", list_snapshots);
    ("list-stage-deployments", list_stage_deployments);
    ("list-stages", list_stages);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-generated-code-job", start_generated_code_job);
    ("start-stage-deployment", start_stage_deployment);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-game", update_game);
    ("update-game-configuration", update_game_configuration);
    ("update-snapshot", update_snapshot);
    ("update-stage", update_stage)]
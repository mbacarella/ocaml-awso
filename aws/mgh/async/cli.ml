(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_created_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL DryRun"
       and progressUpdateStream =
         flag "progress-update-stream" (required string)
           ~doc:"STRING ProgressUpdateStream"
       and migrationTaskName =
         flag "migration-task-name" (required string)
           ~doc:"STRING MigrationTaskName"
       and createdArtifact =
         flag "created-artifact" (required json_arg)
           ~doc:"JSON CreatedArtifact" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_created_artifact
           (Values.AssociateCreatedArtifactRequest.make ?dryRun
              ~progressUpdateStream ~migrationTaskName
              ~createdArtifact:(Values.CreatedArtifact.of_json
                                  createdArtifact) ())
           (Some Values.AssociateCreatedArtifactResult.to_json)
           (Some Values.AssociateCreatedArtifactResult.error_to_json)])
let associate_discovered_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL DryRun"
       and progressUpdateStream =
         flag "progress-update-stream" (required string)
           ~doc:"STRING ProgressUpdateStream"
       and migrationTaskName =
         flag "migration-task-name" (required string)
           ~doc:"STRING MigrationTaskName"
       and discoveredResource =
         flag "discovered-resource" (required json_arg)
           ~doc:"JSON DiscoveredResource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_discovered_resource
           (Values.AssociateDiscoveredResourceRequest.make ?dryRun
              ~progressUpdateStream ~migrationTaskName
              ~discoveredResource:(Values.DiscoveredResource.of_json
                                     discoveredResource) ())
           (Some Values.AssociateDiscoveredResourceResult.to_json)
           (Some Values.AssociateDiscoveredResourceResult.error_to_json)])
let create_progress_update_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL DryRun"
       and progressUpdateStreamName =
         flag "progress-update-stream-name" (required string)
           ~doc:"STRING ProgressUpdateStream" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_progress_update_stream
           (Values.CreateProgressUpdateStreamRequest.make ?dryRun
              ~progressUpdateStreamName ())
           (Some Values.CreateProgressUpdateStreamResult.to_json)
           (Some Values.CreateProgressUpdateStreamResult.error_to_json)])
let delete_progress_update_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL DryRun"
       and progressUpdateStreamName =
         flag "progress-update-stream-name" (required string)
           ~doc:"STRING ProgressUpdateStream" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_progress_update_stream
           (Values.DeleteProgressUpdateStreamRequest.make ?dryRun
              ~progressUpdateStreamName ())
           (Some Values.DeleteProgressUpdateStreamResult.to_json)
           (Some Values.DeleteProgressUpdateStreamResult.error_to_json)])
let describe_application_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_application_state
           (Values.DescribeApplicationStateRequest.make ~applicationId ())
           (Some Values.DescribeApplicationStateResult.to_json)
           (Some Values.DescribeApplicationStateResult.error_to_json)])
let describe_migration_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and progressUpdateStream =
         flag "progress-update-stream" (required string)
           ~doc:"STRING ProgressUpdateStream"
       and migrationTaskName =
         flag "migration-task-name" (required string)
           ~doc:"STRING MigrationTaskName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_migration_task
           (Values.DescribeMigrationTaskRequest.make ~progressUpdateStream
              ~migrationTaskName ())
           (Some Values.DescribeMigrationTaskResult.to_json)
           (Some Values.DescribeMigrationTaskResult.error_to_json)])
let disassociate_created_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL DryRun"
       and progressUpdateStream =
         flag "progress-update-stream" (required string)
           ~doc:"STRING ProgressUpdateStream"
       and migrationTaskName =
         flag "migration-task-name" (required string)
           ~doc:"STRING MigrationTaskName"
       and createdArtifactName =
         flag "created-artifact-name" (required string)
           ~doc:"STRING CreatedArtifactName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_created_artifact
           (Values.DisassociateCreatedArtifactRequest.make ?dryRun
              ~progressUpdateStream ~migrationTaskName ~createdArtifactName
              ()) (Some Values.DisassociateCreatedArtifactResult.to_json)
           (Some Values.DisassociateCreatedArtifactResult.error_to_json)])
let disassociate_discovered_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL DryRun"
       and progressUpdateStream =
         flag "progress-update-stream" (required string)
           ~doc:"STRING ProgressUpdateStream"
       and migrationTaskName =
         flag "migration-task-name" (required string)
           ~doc:"STRING MigrationTaskName"
       and configurationId =
         flag "configuration-id" (required string)
           ~doc:"STRING ConfigurationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_discovered_resource
           (Values.DisassociateDiscoveredResourceRequest.make ?dryRun
              ~progressUpdateStream ~migrationTaskName ~configurationId ())
           (Some Values.DisassociateDiscoveredResourceResult.to_json)
           (Some Values.DisassociateDiscoveredResourceResult.error_to_json)])
let import_migration_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL DryRun"
       and progressUpdateStream =
         flag "progress-update-stream" (required string)
           ~doc:"STRING ProgressUpdateStream"
       and migrationTaskName =
         flag "migration-task-name" (required string)
           ~doc:"STRING MigrationTaskName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_migration_task
           (Values.ImportMigrationTaskRequest.make ?dryRun
              ~progressUpdateStream ~migrationTaskName ())
           (Some Values.ImportMigrationTaskResult.to_json)
           (Some Values.ImportMigrationTaskResult.error_to_json)])
let list_application_states =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationIds =
         flag "application-ids" (optional json_arg)
           ~doc:"JSON ApplicationIds"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_application_states
           (Values.ListApplicationStatesRequest.make
              ?applicationIds:(Option.map ~f:Values.ApplicationIds.of_json
                                 applicationIds) ?nextToken ?maxResults ())
           (Some Values.ListApplicationStatesResult.to_json)
           (Some Values.ListApplicationStatesResult.error_to_json)])
let list_created_artifacts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsCreatedArtifacts"
       and progressUpdateStream =
         flag "progress-update-stream" (required string)
           ~doc:"STRING ProgressUpdateStream"
       and migrationTaskName =
         flag "migration-task-name" (required string)
           ~doc:"STRING MigrationTaskName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_created_artifacts
           (Values.ListCreatedArtifactsRequest.make ?nextToken ?maxResults
              ~progressUpdateStream ~migrationTaskName ())
           (Some Values.ListCreatedArtifactsResult.to_json)
           (Some Values.ListCreatedArtifactsResult.error_to_json)])
let list_discovered_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsResources"
       and progressUpdateStream =
         flag "progress-update-stream" (required string)
           ~doc:"STRING ProgressUpdateStream"
       and migrationTaskName =
         flag "migration-task-name" (required string)
           ~doc:"STRING MigrationTaskName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_discovered_resources
           (Values.ListDiscoveredResourcesRequest.make ?nextToken ?maxResults
              ~progressUpdateStream ~migrationTaskName ())
           (Some Values.ListDiscoveredResourcesResult.to_json)
           (Some Values.ListDiscoveredResourcesResult.error_to_json)])
let list_migration_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceName =
         flag "resource-name" (optional string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_migration_tasks
           (Values.ListMigrationTasksRequest.make ?nextToken ?maxResults
              ?resourceName ())
           (Some Values.ListMigrationTasksResult.to_json)
           (Some Values.ListMigrationTasksResult.error_to_json)])
let list_progress_update_streams =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_progress_update_streams
           (Values.ListProgressUpdateStreamsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListProgressUpdateStreamsResult.to_json)
           (Some Values.ListProgressUpdateStreamsResult.error_to_json)])
let notify_application_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updateDateTime =
         flag "update-date-time" (optional json_arg)
           ~doc:"JSON UpdateDateTime"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL DryRun"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and status =
         flag "status" (required json_arg) ~doc:"JSON ApplicationStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.notify_application_state
           (Values.NotifyApplicationStateRequest.make
              ?updateDateTime:(Option.map ~f:Values.UpdateDateTime.of_json
                                 updateDateTime) ?dryRun ~applicationId
              ~status:(Values.ApplicationStatus.of_json status) ())
           (Some Values.NotifyApplicationStateResult.to_json)
           (Some Values.NotifyApplicationStateResult.error_to_json)])
let notify_migration_task_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL DryRun"
       and progressUpdateStream =
         flag "progress-update-stream" (required string)
           ~doc:"STRING ProgressUpdateStream"
       and migrationTaskName =
         flag "migration-task-name" (required string)
           ~doc:"STRING MigrationTaskName"
       and task = flag "task" (required json_arg) ~doc:"JSON Task"
       and updateDateTime =
         flag "update-date-time" (required json_arg)
           ~doc:"JSON UpdateDateTime"
       and nextUpdateSeconds =
         flag "next-update-seconds" (required int)
           ~doc:"INT NextUpdateSeconds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.notify_migration_task_state
           (Values.NotifyMigrationTaskStateRequest.make ?dryRun
              ~progressUpdateStream ~migrationTaskName
              ~task:(Values.Task.of_json task)
              ~updateDateTime:(Values.UpdateDateTime.of_json updateDateTime)
              ~nextUpdateSeconds ())
           (Some Values.NotifyMigrationTaskStateResult.to_json)
           (Some Values.NotifyMigrationTaskStateResult.error_to_json)])
let put_resource_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL DryRun"
       and progressUpdateStream =
         flag "progress-update-stream" (required string)
           ~doc:"STRING ProgressUpdateStream"
       and migrationTaskName =
         flag "migration-task-name" (required string)
           ~doc:"STRING MigrationTaskName"
       and resourceAttributeList =
         flag "resource-attribute-list" (required json_arg)
           ~doc:"JSON ResourceAttributeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_attributes
           (Values.PutResourceAttributesRequest.make ?dryRun
              ~progressUpdateStream ~migrationTaskName
              ~resourceAttributeList:(Values.ResourceAttributeList.of_json
                                        resourceAttributeList) ())
           (Some Values.PutResourceAttributesResult.to_json)
           (Some Values.PutResourceAttributesResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-created-artifact", associate_created_artifact);
    ("associate-discovered-resource", associate_discovered_resource);
    ("create-progress-update-stream", create_progress_update_stream);
    ("delete-progress-update-stream", delete_progress_update_stream);
    ("describe-application-state", describe_application_state);
    ("describe-migration-task", describe_migration_task);
    ("disassociate-created-artifact", disassociate_created_artifact);
    ("disassociate-discovered-resource", disassociate_discovered_resource);
    ("import-migration-task", import_migration_task);
    ("list-application-states", list_application_states);
    ("list-created-artifacts", list_created_artifacts);
    ("list-discovered-resources", list_discovered_resources);
    ("list-migration-tasks", list_migration_tasks);
    ("list-progress-update-streams", list_progress_update_streams);
    ("notify-application-state", notify_application_state);
    ("notify-migration-task-state", notify_migration_task_state);
    ("put-resource-attributes", put_resource_attributes)]
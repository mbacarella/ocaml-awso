(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | ActivateType: (ActivateTypeInput.t, ActivateTypeOutput.t,
  ActivateTypeOutput.error) t 
  | BatchDescribeTypeConfigurations: (BatchDescribeTypeConfigurationsInput.t,
  BatchDescribeTypeConfigurationsOutput.t,
  BatchDescribeTypeConfigurationsOutput.error) t 
  | CancelUpdateStack: (CancelUpdateStackInput.t, unit, unit) t 
  | ContinueUpdateRollback: (ContinueUpdateRollbackInput.t,
  ContinueUpdateRollbackOutput.t, ContinueUpdateRollbackOutput.error) t 
  | CreateChangeSet: (CreateChangeSetInput.t, CreateChangeSetOutput.t,
  CreateChangeSetOutput.error) t 
  | CreateStack: (CreateStackInput.t, CreateStackOutput.t,
  CreateStackOutput.error) t 
  | CreateStackInstances: (CreateStackInstancesInput.t,
  CreateStackInstancesOutput.t, CreateStackInstancesOutput.error) t 
  | CreateStackSet: (CreateStackSetInput.t, CreateStackSetOutput.t,
  CreateStackSetOutput.error) t 
  | DeactivateType: (DeactivateTypeInput.t, DeactivateTypeOutput.t,
  DeactivateTypeOutput.error) t 
  | DeleteChangeSet: (DeleteChangeSetInput.t, DeleteChangeSetOutput.t,
  DeleteChangeSetOutput.error) t 
  | DeleteStack: (DeleteStackInput.t, unit, unit) t 
  | DeleteStackInstances: (DeleteStackInstancesInput.t,
  DeleteStackInstancesOutput.t, DeleteStackInstancesOutput.error) t 
  | DeleteStackSet: (DeleteStackSetInput.t, DeleteStackSetOutput.t,
  DeleteStackSetOutput.error) t 
  | DeregisterType: (DeregisterTypeInput.t, DeregisterTypeOutput.t,
  DeregisterTypeOutput.error) t 
  | DescribeAccountLimits: (DescribeAccountLimitsInput.t,
  DescribeAccountLimitsOutput.t, DescribeAccountLimitsOutput.error) t 
  | DescribeChangeSet: (DescribeChangeSetInput.t, DescribeChangeSetOutput.t,
  DescribeChangeSetOutput.error) t 
  | DescribeChangeSetHooks: (DescribeChangeSetHooksInput.t,
  DescribeChangeSetHooksOutput.t, DescribeChangeSetHooksOutput.error) t 
  | DescribePublisher: (DescribePublisherInput.t, DescribePublisherOutput.t,
  DescribePublisherOutput.error) t 
  | DescribeStackDriftDetectionStatus:
  (DescribeStackDriftDetectionStatusInput.t,
  DescribeStackDriftDetectionStatusOutput.t,
  DescribeStackDriftDetectionStatusOutput.error) t 
  | DescribeStackEvents: (DescribeStackEventsInput.t,
  DescribeStackEventsOutput.t, DescribeStackEventsOutput.error) t 
  | DescribeStackInstance: (DescribeStackInstanceInput.t,
  DescribeStackInstanceOutput.t, DescribeStackInstanceOutput.error) t 
  | DescribeStackResource: (DescribeStackResourceInput.t,
  DescribeStackResourceOutput.t, DescribeStackResourceOutput.error) t 
  | DescribeStackResourceDrifts: (DescribeStackResourceDriftsInput.t,
  DescribeStackResourceDriftsOutput.t,
  DescribeStackResourceDriftsOutput.error) t 
  | DescribeStackResources: (DescribeStackResourcesInput.t,
  DescribeStackResourcesOutput.t, DescribeStackResourcesOutput.error) t 
  | DescribeStackSet: (DescribeStackSetInput.t, DescribeStackSetOutput.t,
  DescribeStackSetOutput.error) t 
  | DescribeStackSetOperation: (DescribeStackSetOperationInput.t,
  DescribeStackSetOperationOutput.t, DescribeStackSetOperationOutput.error) t
  
  | DescribeStacks: (DescribeStacksInput.t, DescribeStacksOutput.t,
  DescribeStacksOutput.error) t 
  | DescribeType: (DescribeTypeInput.t, DescribeTypeOutput.t,
  DescribeTypeOutput.error) t 
  | DescribeTypeRegistration: (DescribeTypeRegistrationInput.t,
  DescribeTypeRegistrationOutput.t, DescribeTypeRegistrationOutput.error) t 
  | DetectStackDrift: (DetectStackDriftInput.t, DetectStackDriftOutput.t,
  DetectStackDriftOutput.error) t 
  | DetectStackResourceDrift: (DetectStackResourceDriftInput.t,
  DetectStackResourceDriftOutput.t, DetectStackResourceDriftOutput.error) t 
  | DetectStackSetDrift: (DetectStackSetDriftInput.t,
  DetectStackSetDriftOutput.t, DetectStackSetDriftOutput.error) t 
  | EstimateTemplateCost: (EstimateTemplateCostInput.t,
  EstimateTemplateCostOutput.t, EstimateTemplateCostOutput.error) t 
  | ExecuteChangeSet: (ExecuteChangeSetInput.t, ExecuteChangeSetOutput.t,
  ExecuteChangeSetOutput.error) t 
  | GetStackPolicy: (GetStackPolicyInput.t, GetStackPolicyOutput.t,
  GetStackPolicyOutput.error) t 
  | GetTemplate: (GetTemplateInput.t, GetTemplateOutput.t,
  GetTemplateOutput.error) t 
  | GetTemplateSummary: (GetTemplateSummaryInput.t,
  GetTemplateSummaryOutput.t, GetTemplateSummaryOutput.error) t 
  | ImportStacksToStackSet: (ImportStacksToStackSetInput.t,
  ImportStacksToStackSetOutput.t, ImportStacksToStackSetOutput.error) t 
  | ListChangeSets: (ListChangeSetsInput.t, ListChangeSetsOutput.t,
  ListChangeSetsOutput.error) t 
  | ListExports: (ListExportsInput.t, ListExportsOutput.t,
  ListExportsOutput.error) t 
  | ListImports: (ListImportsInput.t, ListImportsOutput.t,
  ListImportsOutput.error) t 
  | ListStackInstances: (ListStackInstancesInput.t,
  ListStackInstancesOutput.t, ListStackInstancesOutput.error) t 
  | ListStackResources: (ListStackResourcesInput.t,
  ListStackResourcesOutput.t, ListStackResourcesOutput.error) t 
  | ListStackSetOperationResults: (ListStackSetOperationResultsInput.t,
  ListStackSetOperationResultsOutput.t,
  ListStackSetOperationResultsOutput.error) t 
  | ListStackSetOperations: (ListStackSetOperationsInput.t,
  ListStackSetOperationsOutput.t, ListStackSetOperationsOutput.error) t 
  | ListStackSets: (ListStackSetsInput.t, ListStackSetsOutput.t,
  ListStackSetsOutput.error) t 
  | ListStacks: (ListStacksInput.t, ListStacksOutput.t,
  ListStacksOutput.error) t 
  | ListTypeRegistrations: (ListTypeRegistrationsInput.t,
  ListTypeRegistrationsOutput.t, ListTypeRegistrationsOutput.error) t 
  | ListTypeVersions: (ListTypeVersionsInput.t, ListTypeVersionsOutput.t,
  ListTypeVersionsOutput.error) t 
  | ListTypes: (ListTypesInput.t, ListTypesOutput.t, ListTypesOutput.error) t
  
  | PublishType: (PublishTypeInput.t, PublishTypeOutput.t,
  PublishTypeOutput.error) t 
  | RecordHandlerProgress: (RecordHandlerProgressInput.t,
  RecordHandlerProgressOutput.t, RecordHandlerProgressOutput.error) t 
  | RegisterPublisher: (RegisterPublisherInput.t, RegisterPublisherOutput.t,
  RegisterPublisherOutput.error) t 
  | RegisterType: (RegisterTypeInput.t, RegisterTypeOutput.t,
  RegisterTypeOutput.error) t 
  | RollbackStack: (RollbackStackInput.t, RollbackStackOutput.t,
  RollbackStackOutput.error) t 
  | SetStackPolicy: (SetStackPolicyInput.t, unit, unit) t 
  | SetTypeConfiguration: (SetTypeConfigurationInput.t,
  SetTypeConfigurationOutput.t, SetTypeConfigurationOutput.error) t 
  | SetTypeDefaultVersion: (SetTypeDefaultVersionInput.t,
  SetTypeDefaultVersionOutput.t, SetTypeDefaultVersionOutput.error) t 
  | SignalResource: (SignalResourceInput.t, unit, unit) t 
  | StopStackSetOperation: (StopStackSetOperationInput.t,
  StopStackSetOperationOutput.t, StopStackSetOperationOutput.error) t 
  | TestType: (TestTypeInput.t, TestTypeOutput.t, TestTypeOutput.error) t 
  | UpdateStack: (UpdateStackInput.t, UpdateStackOutput.t,
  UpdateStackOutput.error) t 
  | UpdateStackInstances: (UpdateStackInstancesInput.t,
  UpdateStackInstancesOutput.t, UpdateStackInstancesOutput.error) t 
  | UpdateStackSet: (UpdateStackSetInput.t, UpdateStackSetOutput.t,
  UpdateStackSetOutput.error) t 
  | UpdateTerminationProtection: (UpdateTerminationProtectionInput.t,
  UpdateTerminationProtectionOutput.t,
  UpdateTerminationProtectionOutput.error) t 
  | ValidateTemplate: (ValidateTemplateInput.t, ValidateTemplateOutput.t,
  ValidateTemplateOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | ActivateType -> `POST
  | BatchDescribeTypeConfigurations -> `POST
  | CancelUpdateStack -> `POST
  | ContinueUpdateRollback -> `POST
  | CreateChangeSet -> `POST
  | CreateStack -> `POST
  | CreateStackInstances -> `POST
  | CreateStackSet -> `POST
  | DeactivateType -> `POST
  | DeleteChangeSet -> `POST
  | DeleteStack -> `POST
  | DeleteStackInstances -> `POST
  | DeleteStackSet -> `POST
  | DeregisterType -> `POST
  | DescribeAccountLimits -> `POST
  | DescribeChangeSet -> `POST
  | DescribeChangeSetHooks -> `POST
  | DescribePublisher -> `POST
  | DescribeStackDriftDetectionStatus -> `POST
  | DescribeStackEvents -> `POST
  | DescribeStackInstance -> `POST
  | DescribeStackResource -> `POST
  | DescribeStackResourceDrifts -> `POST
  | DescribeStackResources -> `POST
  | DescribeStackSet -> `POST
  | DescribeStackSetOperation -> `POST
  | DescribeStacks -> `POST
  | DescribeType -> `POST
  | DescribeTypeRegistration -> `POST
  | DetectStackDrift -> `POST
  | DetectStackResourceDrift -> `POST
  | DetectStackSetDrift -> `POST
  | EstimateTemplateCost -> `POST
  | ExecuteChangeSet -> `POST
  | GetStackPolicy -> `POST
  | GetTemplate -> `POST
  | GetTemplateSummary -> `POST
  | ImportStacksToStackSet -> `POST
  | ListChangeSets -> `POST
  | ListExports -> `POST
  | ListImports -> `POST
  | ListStackInstances -> `POST
  | ListStackResources -> `POST
  | ListStackSetOperationResults -> `POST
  | ListStackSetOperations -> `POST
  | ListStackSets -> `POST
  | ListStacks -> `POST
  | ListTypeRegistrations -> `POST
  | ListTypeVersions -> `POST
  | ListTypes -> `POST
  | PublishType -> `POST
  | RecordHandlerProgress -> `POST
  | RegisterPublisher -> `POST
  | RegisterType -> `POST
  | RollbackStack -> `POST
  | SetStackPolicy -> `POST
  | SetTypeConfiguration -> `POST
  | SetTypeDefaultVersion -> `POST
  | SignalResource -> `POST
  | StopStackSetOperation -> `POST
  | TestType -> `POST
  | UpdateStack -> `POST
  | UpdateStackInstances -> `POST
  | UpdateStackSet -> `POST
  | UpdateTerminationProtection -> `POST
  | ValidateTemplate -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | ActivateType -> (Format.kasprintf Uri.of_string) "/"
      | BatchDescribeTypeConfigurations ->
          (Format.kasprintf Uri.of_string) "/"
      | CancelUpdateStack -> (Format.kasprintf Uri.of_string) "/"
      | ContinueUpdateRollback -> (Format.kasprintf Uri.of_string) "/"
      | CreateChangeSet -> (Format.kasprintf Uri.of_string) "/"
      | CreateStack -> (Format.kasprintf Uri.of_string) "/"
      | CreateStackInstances -> (Format.kasprintf Uri.of_string) "/"
      | CreateStackSet -> (Format.kasprintf Uri.of_string) "/"
      | DeactivateType -> (Format.kasprintf Uri.of_string) "/"
      | DeleteChangeSet -> (Format.kasprintf Uri.of_string) "/"
      | DeleteStack -> (Format.kasprintf Uri.of_string) "/"
      | DeleteStackInstances -> (Format.kasprintf Uri.of_string) "/"
      | DeleteStackSet -> (Format.kasprintf Uri.of_string) "/"
      | DeregisterType -> (Format.kasprintf Uri.of_string) "/"
      | DescribeAccountLimits -> (Format.kasprintf Uri.of_string) "/"
      | DescribeChangeSet -> (Format.kasprintf Uri.of_string) "/"
      | DescribeChangeSetHooks -> (Format.kasprintf Uri.of_string) "/"
      | DescribePublisher -> (Format.kasprintf Uri.of_string) "/"
      | DescribeStackDriftDetectionStatus ->
          (Format.kasprintf Uri.of_string) "/"
      | DescribeStackEvents -> (Format.kasprintf Uri.of_string) "/"
      | DescribeStackInstance -> (Format.kasprintf Uri.of_string) "/"
      | DescribeStackResource -> (Format.kasprintf Uri.of_string) "/"
      | DescribeStackResourceDrifts -> (Format.kasprintf Uri.of_string) "/"
      | DescribeStackResources -> (Format.kasprintf Uri.of_string) "/"
      | DescribeStackSet -> (Format.kasprintf Uri.of_string) "/"
      | DescribeStackSetOperation -> (Format.kasprintf Uri.of_string) "/"
      | DescribeStacks -> (Format.kasprintf Uri.of_string) "/"
      | DescribeType -> (Format.kasprintf Uri.of_string) "/"
      | DescribeTypeRegistration -> (Format.kasprintf Uri.of_string) "/"
      | DetectStackDrift -> (Format.kasprintf Uri.of_string) "/"
      | DetectStackResourceDrift -> (Format.kasprintf Uri.of_string) "/"
      | DetectStackSetDrift -> (Format.kasprintf Uri.of_string) "/"
      | EstimateTemplateCost -> (Format.kasprintf Uri.of_string) "/"
      | ExecuteChangeSet -> (Format.kasprintf Uri.of_string) "/"
      | GetStackPolicy -> (Format.kasprintf Uri.of_string) "/"
      | GetTemplate -> (Format.kasprintf Uri.of_string) "/"
      | GetTemplateSummary -> (Format.kasprintf Uri.of_string) "/"
      | ImportStacksToStackSet -> (Format.kasprintf Uri.of_string) "/"
      | ListChangeSets -> (Format.kasprintf Uri.of_string) "/"
      | ListExports -> (Format.kasprintf Uri.of_string) "/"
      | ListImports -> (Format.kasprintf Uri.of_string) "/"
      | ListStackInstances -> (Format.kasprintf Uri.of_string) "/"
      | ListStackResources -> (Format.kasprintf Uri.of_string) "/"
      | ListStackSetOperationResults -> (Format.kasprintf Uri.of_string) "/"
      | ListStackSetOperations -> (Format.kasprintf Uri.of_string) "/"
      | ListStackSets -> (Format.kasprintf Uri.of_string) "/"
      | ListStacks -> (Format.kasprintf Uri.of_string) "/"
      | ListTypeRegistrations -> (Format.kasprintf Uri.of_string) "/"
      | ListTypeVersions -> (Format.kasprintf Uri.of_string) "/"
      | ListTypes -> (Format.kasprintf Uri.of_string) "/"
      | PublishType -> (Format.kasprintf Uri.of_string) "/"
      | RecordHandlerProgress -> (Format.kasprintf Uri.of_string) "/"
      | RegisterPublisher -> (Format.kasprintf Uri.of_string) "/"
      | RegisterType -> (Format.kasprintf Uri.of_string) "/"
      | RollbackStack -> (Format.kasprintf Uri.of_string) "/"
      | SetStackPolicy -> (Format.kasprintf Uri.of_string) "/"
      | SetTypeConfiguration -> (Format.kasprintf Uri.of_string) "/"
      | SetTypeDefaultVersion -> (Format.kasprintf Uri.of_string) "/"
      | SignalResource -> (Format.kasprintf Uri.of_string) "/"
      | StopStackSetOperation -> (Format.kasprintf Uri.of_string) "/"
      | TestType -> (Format.kasprintf Uri.of_string) "/"
      | UpdateStack -> (Format.kasprintf Uri.of_string) "/"
      | UpdateStackInstances -> (Format.kasprintf Uri.of_string) "/"
      | UpdateStackSet -> (Format.kasprintf Uri.of_string) "/"
      | UpdateTerminationProtection -> (Format.kasprintf Uri.of_string) "/"
      | ValidateTemplate -> (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | ActivateType ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["ActivateType"]); ("Version", [apiVersion])] in
        let query =
          (ActivateTypeInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | BatchDescribeTypeConfigurations ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["BatchDescribeTypeConfigurations"]);
          ("Version", [apiVersion])] in
        let query =
          (BatchDescribeTypeConfigurationsInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CancelUpdateStack ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["CancelUpdateStack"]); ("Version", [apiVersion])] in
        let query =
          (CancelUpdateStackInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ContinueUpdateRollback ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ContinueUpdateRollback"]); ("Version", [apiVersion])] in
        let query =
          (ContinueUpdateRollbackInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateChangeSet ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["CreateChangeSet"]); ("Version", [apiVersion])] in
        let query =
          (CreateChangeSetInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateStack ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["CreateStack"]); ("Version", [apiVersion])] in
        let query =
          (CreateStackInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateStackInstances ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["CreateStackInstances"]); ("Version", [apiVersion])] in
        let query =
          (CreateStackInstancesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateStackSet ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["CreateStackSet"]); ("Version", [apiVersion])] in
        let query =
          (CreateStackSetInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeactivateType ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeactivateType"]); ("Version", [apiVersion])] in
        let query =
          (DeactivateTypeInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeleteChangeSet ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeleteChangeSet"]); ("Version", [apiVersion])] in
        let query =
          (DeleteChangeSetInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeleteStack ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["DeleteStack"]); ("Version", [apiVersion])] in
        let query =
          (DeleteStackInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeleteStackInstances ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeleteStackInstances"]); ("Version", [apiVersion])] in
        let query =
          (DeleteStackInstancesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeleteStackSet ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeleteStackSet"]); ("Version", [apiVersion])] in
        let query =
          (DeleteStackSetInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeregisterType ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeregisterType"]); ("Version", [apiVersion])] in
        let query =
          (DeregisterTypeInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeAccountLimits ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeAccountLimits"]); ("Version", [apiVersion])] in
        let query =
          (DescribeAccountLimitsInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeChangeSet ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeChangeSet"]); ("Version", [apiVersion])] in
        let query =
          (DescribeChangeSetInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeChangeSetHooks ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeChangeSetHooks"]); ("Version", [apiVersion])] in
        let query =
          (DescribeChangeSetHooksInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribePublisher ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribePublisher"]); ("Version", [apiVersion])] in
        let query =
          (DescribePublisherInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeStackDriftDetectionStatus ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeStackDriftDetectionStatus"]);
          ("Version", [apiVersion])] in
        let query =
          (DescribeStackDriftDetectionStatusInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeStackEvents ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeStackEvents"]); ("Version", [apiVersion])] in
        let query =
          (DescribeStackEventsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeStackInstance ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeStackInstance"]); ("Version", [apiVersion])] in
        let query =
          (DescribeStackInstanceInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeStackResource ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeStackResource"]); ("Version", [apiVersion])] in
        let query =
          (DescribeStackResourceInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeStackResourceDrifts ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeStackResourceDrifts"]);
          ("Version", [apiVersion])] in
        let query =
          (DescribeStackResourceDriftsInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeStackResources ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeStackResources"]); ("Version", [apiVersion])] in
        let query =
          (DescribeStackResourcesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeStackSet ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeStackSet"]); ("Version", [apiVersion])] in
        let query =
          (DescribeStackSetInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeStackSetOperation ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeStackSetOperation"]);
          ("Version", [apiVersion])] in
        let query =
          (DescribeStackSetOperationInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeStacks ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeStacks"]); ("Version", [apiVersion])] in
        let query =
          (DescribeStacksInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeType ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["DescribeType"]); ("Version", [apiVersion])] in
        let query =
          (DescribeTypeInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeTypeRegistration ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeTypeRegistration"]);
          ("Version", [apiVersion])] in
        let query =
          (DescribeTypeRegistrationInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DetectStackDrift ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DetectStackDrift"]); ("Version", [apiVersion])] in
        let query =
          (DetectStackDriftInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DetectStackResourceDrift ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DetectStackResourceDrift"]);
          ("Version", [apiVersion])] in
        let query =
          (DetectStackResourceDriftInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DetectStackSetDrift ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DetectStackSetDrift"]); ("Version", [apiVersion])] in
        let query =
          (DetectStackSetDriftInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | EstimateTemplateCost ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["EstimateTemplateCost"]); ("Version", [apiVersion])] in
        let query =
          (EstimateTemplateCostInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ExecuteChangeSet ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ExecuteChangeSet"]); ("Version", [apiVersion])] in
        let query =
          (ExecuteChangeSetInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | GetStackPolicy ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["GetStackPolicy"]); ("Version", [apiVersion])] in
        let query =
          (GetStackPolicyInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | GetTemplate ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["GetTemplate"]); ("Version", [apiVersion])] in
        let query =
          (GetTemplateInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | GetTemplateSummary ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["GetTemplateSummary"]); ("Version", [apiVersion])] in
        let query =
          (GetTemplateSummaryInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ImportStacksToStackSet ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ImportStacksToStackSet"]); ("Version", [apiVersion])] in
        let query =
          (ImportStacksToStackSetInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ListChangeSets ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ListChangeSets"]); ("Version", [apiVersion])] in
        let query =
          (ListChangeSetsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ListExports ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["ListExports"]); ("Version", [apiVersion])] in
        let query =
          (ListExportsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ListImports ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["ListImports"]); ("Version", [apiVersion])] in
        let query =
          (ListImportsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ListStackInstances ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ListStackInstances"]); ("Version", [apiVersion])] in
        let query =
          (ListStackInstancesInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ListStackResources ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ListStackResources"]); ("Version", [apiVersion])] in
        let query =
          (ListStackResourcesInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ListStackSetOperationResults ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ListStackSetOperationResults"]);
          ("Version", [apiVersion])] in
        let query =
          (ListStackSetOperationResultsInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ListStackSetOperations ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ListStackSetOperations"]); ("Version", [apiVersion])] in
        let query =
          (ListStackSetOperationsInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ListStackSets ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["ListStackSets"]); ("Version", [apiVersion])] in
        let query =
          (ListStackSetsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ListStacks ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["ListStacks"]); ("Version", [apiVersion])] in
        let query =
          (ListStacksInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ListTypeRegistrations ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ListTypeRegistrations"]); ("Version", [apiVersion])] in
        let query =
          (ListTypeRegistrationsInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ListTypeVersions ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ListTypeVersions"]); ("Version", [apiVersion])] in
        let query =
          (ListTypeVersionsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ListTypes ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["ListTypes"]); ("Version", [apiVersion])] in
        let query = (ListTypesInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | PublishType ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["PublishType"]); ("Version", [apiVersion])] in
        let query =
          (PublishTypeInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | RecordHandlerProgress ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["RecordHandlerProgress"]); ("Version", [apiVersion])] in
        let query =
          (RecordHandlerProgressInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | RegisterPublisher ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["RegisterPublisher"]); ("Version", [apiVersion])] in
        let query =
          (RegisterPublisherInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | RegisterType ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["RegisterType"]); ("Version", [apiVersion])] in
        let query =
          (RegisterTypeInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | RollbackStack ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["RollbackStack"]); ("Version", [apiVersion])] in
        let query =
          (RollbackStackInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | SetStackPolicy ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["SetStackPolicy"]); ("Version", [apiVersion])] in
        let query =
          (SetStackPolicyInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | SetTypeConfiguration ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["SetTypeConfiguration"]); ("Version", [apiVersion])] in
        let query =
          (SetTypeConfigurationInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | SetTypeDefaultVersion ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["SetTypeDefaultVersion"]); ("Version", [apiVersion])] in
        let query =
          (SetTypeDefaultVersionInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | SignalResource ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["SignalResource"]); ("Version", [apiVersion])] in
        let query =
          (SignalResourceInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | StopStackSetOperation ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["StopStackSetOperation"]); ("Version", [apiVersion])] in
        let query =
          (StopStackSetOperationInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | TestType ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["TestType"]); ("Version", [apiVersion])] in
        let query = (TestTypeInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | UpdateStack ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["UpdateStack"]); ("Version", [apiVersion])] in
        let query =
          (UpdateStackInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | UpdateStackInstances ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["UpdateStackInstances"]); ("Version", [apiVersion])] in
        let query =
          (UpdateStackInstancesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | UpdateStackSet ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["UpdateStackSet"]); ("Version", [apiVersion])] in
        let query =
          (UpdateStackSetInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | UpdateTerminationProtection ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["UpdateTerminationProtection"]);
          ("Version", [apiVersion])] in
        let query =
          (UpdateTerminationProtectionInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ValidateTemplate ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ValidateTemplate"]); ("Version", [apiVersion])] in
        let query =
          (ValidateTemplateInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let parse_aws_error error_of_xml =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = None }) in
    match (error_of_xml, ((code >= 400) && (code <= 599))) with
    | (None, _) | (_, false) -> bail ()
    | (Some error_of_xml, true) ->
        (match Awso.Xml.parse_response body with
         | `Data _ -> bail ()
         | `El (((_, "ErrorResponse"), _), _) as error_response_xml ->
             let error_xml = Awso.Xml.child_exn error_response_xml "Error" in
             (try
                let error_code =
                  match Awso.Xml.child_exn error_xml "Code" with
                  | `Data error_code -> error_code
                  | `El (_, children) ->
                      (List.map children
                         ~f:(function | `Data s -> s | `El _ -> ""))
                        |> (String.concat ~sep:"") in
                error_of_xml (String.strip error_code) error_xml
              with | Failure _ -> bail ())
         | `El _ -> bail ()) in
  let _ = parse_aws_error in
  let _ = resp in
  match endpoint with
  | ActivateType ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ActivateTypeOutput.of_xml xml)
      else Error (parse_aws_error (Some ActivateTypeOutput.error_of_xml))
  | BatchDescribeTypeConfigurations ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (BatchDescribeTypeConfigurationsOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some BatchDescribeTypeConfigurationsOutput.error_of_xml))
  | CancelUpdateStack ->
      if is_success then Ok () else Error (parse_aws_error None)
  | ContinueUpdateRollback ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ContinueUpdateRollbackOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some ContinueUpdateRollbackOutput.error_of_xml))
  | CreateChangeSet ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (CreateChangeSetOutput.of_xml xml)
      else Error (parse_aws_error (Some CreateChangeSetOutput.error_of_xml))
  | CreateStack ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (CreateStackOutput.of_xml xml)
      else Error (parse_aws_error (Some CreateStackOutput.error_of_xml))
  | CreateStackInstances ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (CreateStackInstancesOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some CreateStackInstancesOutput.error_of_xml))
  | CreateStackSet ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (CreateStackSetOutput.of_xml xml)
      else Error (parse_aws_error (Some CreateStackSetOutput.error_of_xml))
  | DeactivateType ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeactivateTypeOutput.of_xml xml)
      else Error (parse_aws_error (Some DeactivateTypeOutput.error_of_xml))
  | DeleteChangeSet ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeleteChangeSetOutput.of_xml xml)
      else Error (parse_aws_error (Some DeleteChangeSetOutput.error_of_xml))
  | DeleteStack -> if is_success then Ok () else Error (parse_aws_error None)
  | DeleteStackInstances ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeleteStackInstancesOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some DeleteStackInstancesOutput.error_of_xml))
  | DeleteStackSet ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeleteStackSetOutput.of_xml xml)
      else Error (parse_aws_error (Some DeleteStackSetOutput.error_of_xml))
  | DeregisterType ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeregisterTypeOutput.of_xml xml)
      else Error (parse_aws_error (Some DeregisterTypeOutput.error_of_xml))
  | DescribeAccountLimits ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeAccountLimitsOutput.of_xml xml)
      else Error (parse_aws_error None)
  | DescribeChangeSet ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeChangeSetOutput.of_xml xml)
      else
        Error (parse_aws_error (Some DescribeChangeSetOutput.error_of_xml))
  | DescribeChangeSetHooks ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeChangeSetHooksOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some DescribeChangeSetHooksOutput.error_of_xml))
  | DescribePublisher ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribePublisherOutput.of_xml xml)
      else
        Error (parse_aws_error (Some DescribePublisherOutput.error_of_xml))
  | DescribeStackDriftDetectionStatus ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeStackDriftDetectionStatusOutput.of_xml xml)
      else Error (parse_aws_error None)
  | DescribeStackEvents ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeStackEventsOutput.of_xml xml)
      else Error (parse_aws_error None)
  | DescribeStackInstance ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeStackInstanceOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some DescribeStackInstanceOutput.error_of_xml))
  | DescribeStackResource ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeStackResourceOutput.of_xml xml)
      else Error (parse_aws_error None)
  | DescribeStackResourceDrifts ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeStackResourceDriftsOutput.of_xml xml)
      else Error (parse_aws_error None)
  | DescribeStackResources ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeStackResourcesOutput.of_xml xml)
      else Error (parse_aws_error None)
  | DescribeStackSet ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeStackSetOutput.of_xml xml)
      else Error (parse_aws_error (Some DescribeStackSetOutput.error_of_xml))
  | DescribeStackSetOperation ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeStackSetOperationOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some DescribeStackSetOperationOutput.error_of_xml))
  | DescribeStacks ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeStacksOutput.of_xml xml)
      else Error (parse_aws_error None)
  | DescribeType ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeTypeOutput.of_xml xml)
      else Error (parse_aws_error (Some DescribeTypeOutput.error_of_xml))
  | DescribeTypeRegistration ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeTypeRegistrationOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some DescribeTypeRegistrationOutput.error_of_xml))
  | DetectStackDrift ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DetectStackDriftOutput.of_xml xml)
      else Error (parse_aws_error None)
  | DetectStackResourceDrift ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DetectStackResourceDriftOutput.of_xml xml)
      else Error (parse_aws_error None)
  | DetectStackSetDrift ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DetectStackSetDriftOutput.of_xml xml)
      else
        Error (parse_aws_error (Some DetectStackSetDriftOutput.error_of_xml))
  | EstimateTemplateCost ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (EstimateTemplateCostOutput.of_xml xml)
      else Error (parse_aws_error None)
  | ExecuteChangeSet ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ExecuteChangeSetOutput.of_xml xml)
      else Error (parse_aws_error (Some ExecuteChangeSetOutput.error_of_xml))
  | GetStackPolicy ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (GetStackPolicyOutput.of_xml xml)
      else Error (parse_aws_error None)
  | GetTemplate ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (GetTemplateOutput.of_xml xml)
      else Error (parse_aws_error (Some GetTemplateOutput.error_of_xml))
  | GetTemplateSummary ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (GetTemplateSummaryOutput.of_xml xml)
      else
        Error (parse_aws_error (Some GetTemplateSummaryOutput.error_of_xml))
  | ImportStacksToStackSet ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ImportStacksToStackSetOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some ImportStacksToStackSetOutput.error_of_xml))
  | ListChangeSets ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ListChangeSetsOutput.of_xml xml)
      else Error (parse_aws_error None)
  | ListExports ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ListExportsOutput.of_xml xml)
      else Error (parse_aws_error None)
  | ListImports ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ListImportsOutput.of_xml xml)
      else Error (parse_aws_error None)
  | ListStackInstances ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ListStackInstancesOutput.of_xml xml)
      else
        Error (parse_aws_error (Some ListStackInstancesOutput.error_of_xml))
  | ListStackResources ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ListStackResourcesOutput.of_xml xml)
      else Error (parse_aws_error None)
  | ListStackSetOperationResults ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ListStackSetOperationResultsOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some ListStackSetOperationResultsOutput.error_of_xml))
  | ListStackSetOperations ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ListStackSetOperationsOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some ListStackSetOperationsOutput.error_of_xml))
  | ListStackSets ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ListStackSetsOutput.of_xml xml)
      else Error (parse_aws_error None)
  | ListStacks ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ListStacksOutput.of_xml xml)
      else Error (parse_aws_error None)
  | ListTypeRegistrations ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ListTypeRegistrationsOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some ListTypeRegistrationsOutput.error_of_xml))
  | ListTypeVersions ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ListTypeVersionsOutput.of_xml xml)
      else Error (parse_aws_error (Some ListTypeVersionsOutput.error_of_xml))
  | ListTypes ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ListTypesOutput.of_xml xml)
      else Error (parse_aws_error (Some ListTypesOutput.error_of_xml))
  | PublishType ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (PublishTypeOutput.of_xml xml)
      else Error (parse_aws_error (Some PublishTypeOutput.error_of_xml))
  | RecordHandlerProgress ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (RecordHandlerProgressOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some RecordHandlerProgressOutput.error_of_xml))
  | RegisterPublisher ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (RegisterPublisherOutput.of_xml xml)
      else
        Error (parse_aws_error (Some RegisterPublisherOutput.error_of_xml))
  | RegisterType ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (RegisterTypeOutput.of_xml xml)
      else Error (parse_aws_error (Some RegisterTypeOutput.error_of_xml))
  | RollbackStack ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (RollbackStackOutput.of_xml xml)
      else Error (parse_aws_error (Some RollbackStackOutput.error_of_xml))
  | SetStackPolicy ->
      if is_success then Ok () else Error (parse_aws_error None)
  | SetTypeConfiguration ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (SetTypeConfigurationOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some SetTypeConfigurationOutput.error_of_xml))
  | SetTypeDefaultVersion ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (SetTypeDefaultVersionOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some SetTypeDefaultVersionOutput.error_of_xml))
  | SignalResource ->
      if is_success then Ok () else Error (parse_aws_error None)
  | StopStackSetOperation ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (StopStackSetOperationOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some StopStackSetOperationOutput.error_of_xml))
  | TestType ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (TestTypeOutput.of_xml xml)
      else Error (parse_aws_error (Some TestTypeOutput.error_of_xml))
  | UpdateStack ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (UpdateStackOutput.of_xml xml)
      else Error (parse_aws_error (Some UpdateStackOutput.error_of_xml))
  | UpdateStackInstances ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (UpdateStackInstancesOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some UpdateStackInstancesOutput.error_of_xml))
  | UpdateStackSet ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (UpdateStackSetOutput.of_xml xml)
      else Error (parse_aws_error (Some UpdateStackSetOutput.error_of_xml))
  | UpdateTerminationProtection ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (UpdateTerminationProtectionOutput.of_xml xml)
      else Error (parse_aws_error None)
  | ValidateTemplate ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ValidateTemplateOutput.of_xml xml)
      else Error (parse_aws_error None)
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let activate_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON ThirdPartyType"
       and publicTypeArn =
         flag "public-type-arn" (optional string)
           ~doc:"STRING ThirdPartyTypeArn"
       and publisherId =
         flag "publisher-id" (optional string) ~doc:"STRING PublisherId"
       and typeName =
         flag "type-name" (optional string) ~doc:"STRING TypeName"
       and typeNameAlias =
         flag "type-name-alias" (optional string) ~doc:"STRING TypeName"
       and autoUpdate =
         flag "auto-update" (optional bool) ~doc:"BOOL AutoUpdate"
       and loggingConfig =
         flag "logging-config" (optional json_arg) ~doc:"JSON LoggingConfig"
       and executionRoleArn =
         flag "execution-role-arn" (optional string) ~doc:"STRING RoleArn"
       and versionBump =
         flag "version-bump" (optional json_arg) ~doc:"JSON VersionBump"
       and majorVersion =
         flag "major-version" (optional json_arg) ~doc:"JSON MajorVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.activate_type
           (Values.ActivateTypeInput.make
              ?type_:(Option.map ~f:Values.ThirdPartyType.of_json type_)
              ?publicTypeArn ?publisherId ?typeName ?typeNameAlias
              ?autoUpdate
              ?loggingConfig:(Option.map ~f:Values.LoggingConfig.of_json
                                loggingConfig) ?executionRoleArn
              ?versionBump:(Option.map ~f:Values.VersionBump.of_json
                              versionBump)
              ?majorVersion:(Option.map ~f:Values.MajorVersion.of_json
                               majorVersion) ())
           (Some Values.ActivateTypeOutput.to_json)
           (Some Values.ActivateTypeOutput.error_to_json)])
let batch_describe_type_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and typeConfigurationIdentifiers =
         flag "type-configuration-identifiers" (required json_arg)
           ~doc:"JSON TypeConfigurationIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_describe_type_configurations
           (Values.BatchDescribeTypeConfigurationsInput.make
              ~typeConfigurationIdentifiers:(Values.TypeConfigurationIdentifiers.of_json
                                               typeConfigurationIdentifiers)
              ()) (Some Values.BatchDescribeTypeConfigurationsOutput.to_json)
           (Some Values.BatchDescribeTypeConfigurationsOutput.error_to_json)])
let cancel_update_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_update_stack
           (Values.CancelUpdateStackInput.make ?clientRequestToken ~stackName
              ()) None None])
let continue_update_rollback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleARN =
         flag "role-a-r-n" (optional string) ~doc:"STRING RoleARN"
       and resourcesToSkip =
         flag "resources-to-skip" (optional json_arg)
           ~doc:"JSON ResourcesToSkip"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackNameOrId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.continue_update_rollback
           (Values.ContinueUpdateRollbackInput.make ?roleARN
              ?resourcesToSkip:(Option.map ~f:Values.ResourcesToSkip.of_json
                                  resourcesToSkip) ?clientRequestToken
              ~stackName ())
           (Some Values.ContinueUpdateRollbackOutput.to_json)
           (Some Values.ContinueUpdateRollbackOutput.error_to_json)])
let create_change_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateBody =
         flag "template-body" (optional string) ~doc:"STRING TemplateBody"
       and templateURL =
         flag "template-u-r-l" (optional string) ~doc:"STRING TemplateURL"
       and usePreviousTemplate =
         flag "use-previous-template" (optional bool)
           ~doc:"BOOL UsePreviousTemplate"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and capabilities =
         flag "capabilities" (optional json_arg) ~doc:"JSON Capabilities"
       and resourceTypes =
         flag "resource-types" (optional json_arg) ~doc:"JSON ResourceTypes"
       and roleARN =
         flag "role-a-r-n" (optional string) ~doc:"STRING RoleARN"
       and rollbackConfiguration =
         flag "rollback-configuration" (optional json_arg)
           ~doc:"JSON RollbackConfiguration"
       and notificationARNs =
         flag "notification-a-r-ns" (optional json_arg)
           ~doc:"JSON NotificationARNs"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and changeSetType =
         flag "change-set-type" (optional json_arg) ~doc:"JSON ChangeSetType"
       and resourcesToImport =
         flag "resources-to-import" (optional json_arg)
           ~doc:"JSON ResourcesToImport"
       and includeNestedStacks =
         flag "include-nested-stacks" (optional bool)
           ~doc:"BOOL IncludeNestedStacks"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackNameOrId"
       and changeSetName =
         flag "change-set-name" (required string) ~doc:"STRING ChangeSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_change_set
           (Values.CreateChangeSetInput.make ?templateBody ?templateURL
              ?usePreviousTemplate
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?capabilities:(Option.map ~f:Values.Capabilities.of_json
                               capabilities)
              ?resourceTypes:(Option.map ~f:Values.ResourceTypes.of_json
                                resourceTypes) ?roleARN
              ?rollbackConfiguration:(Option.map
                                        ~f:Values.RollbackConfiguration.of_json
                                        rollbackConfiguration)
              ?notificationARNs:(Option.map
                                   ~f:Values.NotificationARNs.of_json
                                   notificationARNs)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?clientToken
              ?description
              ?changeSetType:(Option.map ~f:Values.ChangeSetType.of_json
                                changeSetType)
              ?resourcesToImport:(Option.map
                                    ~f:Values.ResourcesToImport.of_json
                                    resourcesToImport) ?includeNestedStacks
              ~stackName ~changeSetName ())
           (Some Values.CreateChangeSetOutput.to_json)
           (Some Values.CreateChangeSetOutput.error_to_json)])
let create_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateBody =
         flag "template-body" (optional string) ~doc:"STRING TemplateBody"
       and templateURL =
         flag "template-u-r-l" (optional string) ~doc:"STRING TemplateURL"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and disableRollback =
         flag "disable-rollback" (optional bool) ~doc:"BOOL DisableRollback"
       and rollbackConfiguration =
         flag "rollback-configuration" (optional json_arg)
           ~doc:"JSON RollbackConfiguration"
       and timeoutInMinutes =
         flag "timeout-in-minutes" (optional int) ~doc:"INT TimeoutMinutes"
       and notificationARNs =
         flag "notification-a-r-ns" (optional json_arg)
           ~doc:"JSON NotificationARNs"
       and capabilities =
         flag "capabilities" (optional json_arg) ~doc:"JSON Capabilities"
       and resourceTypes =
         flag "resource-types" (optional json_arg) ~doc:"JSON ResourceTypes"
       and roleARN =
         flag "role-a-r-n" (optional string) ~doc:"STRING RoleARN"
       and onFailure =
         flag "on-failure" (optional json_arg) ~doc:"JSON OnFailure"
       and stackPolicyBody =
         flag "stack-policy-body" (optional string)
           ~doc:"STRING StackPolicyBody"
       and stackPolicyURL =
         flag "stack-policy-u-r-l" (optional string)
           ~doc:"STRING StackPolicyURL"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and enableTerminationProtection =
         flag "enable-termination-protection" (optional bool)
           ~doc:"BOOL EnableTerminationProtection"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_stack
           (Values.CreateStackInput.make ?templateBody ?templateURL
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?disableRollback
              ?rollbackConfiguration:(Option.map
                                        ~f:Values.RollbackConfiguration.of_json
                                        rollbackConfiguration)
              ?timeoutInMinutes
              ?notificationARNs:(Option.map
                                   ~f:Values.NotificationARNs.of_json
                                   notificationARNs)
              ?capabilities:(Option.map ~f:Values.Capabilities.of_json
                               capabilities)
              ?resourceTypes:(Option.map ~f:Values.ResourceTypes.of_json
                                resourceTypes) ?roleARN
              ?onFailure:(Option.map ~f:Values.OnFailure.of_json onFailure)
              ?stackPolicyBody ?stackPolicyURL
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?clientRequestToken ?enableTerminationProtection ~stackName ())
           (Some Values.CreateStackOutput.to_json)
           (Some Values.CreateStackOutput.error_to_json)])
let create_stack_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accounts =
         flag "accounts" (optional json_arg) ~doc:"JSON AccountList"
       and deploymentTargets =
         flag "deployment-targets" (optional json_arg)
           ~doc:"JSON DeploymentTargets"
       and parameterOverrides =
         flag "parameter-overrides" (optional json_arg)
           ~doc:"JSON Parameters"
       and operationPreferences =
         flag "operation-preferences" (optional json_arg)
           ~doc:"JSON StackSetOperationPreferences"
       and operationId =
         flag "operation-id" (optional string)
           ~doc:"STRING ClientRequestToken"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and stackSetName =
         flag "stack-set-name" (required string) ~doc:"STRING StackSetName"
       and regions =
         flag "regions" (required json_arg) ~doc:"JSON RegionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_stack_instances
           (Values.CreateStackInstancesInput.make
              ?accounts:(Option.map ~f:Values.AccountList.of_json accounts)
              ?deploymentTargets:(Option.map
                                    ~f:Values.DeploymentTargets.of_json
                                    deploymentTargets)
              ?parameterOverrides:(Option.map ~f:Values.Parameters.of_json
                                     parameterOverrides)
              ?operationPreferences:(Option.map
                                       ~f:Values.StackSetOperationPreferences.of_json
                                       operationPreferences) ?operationId
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ~stackSetName ~regions:(Values.RegionList.of_json regions) ())
           (Some Values.CreateStackInstancesOutput.to_json)
           (Some Values.CreateStackInstancesOutput.error_to_json)])
let create_stack_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and templateBody =
         flag "template-body" (optional string) ~doc:"STRING TemplateBody"
       and templateURL =
         flag "template-u-r-l" (optional string) ~doc:"STRING TemplateURL"
       and stackId = flag "stack-id" (optional string) ~doc:"STRING StackId"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and capabilities =
         flag "capabilities" (optional json_arg) ~doc:"JSON Capabilities"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and administrationRoleARN =
         flag "administration-role-a-r-n" (optional string)
           ~doc:"STRING RoleARN"
       and executionRoleName =
         flag "execution-role-name" (optional string)
           ~doc:"STRING ExecutionRoleName"
       and permissionModel =
         flag "permission-model" (optional json_arg)
           ~doc:"JSON PermissionModels"
       and autoDeployment =
         flag "auto-deployment" (optional json_arg)
           ~doc:"JSON AutoDeployment"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and managedExecution =
         flag "managed-execution" (optional json_arg)
           ~doc:"JSON ManagedExecution"
       and stackSetName =
         flag "stack-set-name" (required string) ~doc:"STRING StackSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_stack_set
           (Values.CreateStackSetInput.make ?description ?templateBody
              ?templateURL ?stackId
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?capabilities:(Option.map ~f:Values.Capabilities.of_json
                               capabilities)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?administrationRoleARN ?executionRoleName
              ?permissionModel:(Option.map ~f:Values.PermissionModels.of_json
                                  permissionModel)
              ?autoDeployment:(Option.map ~f:Values.AutoDeployment.of_json
                                 autoDeployment)
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ?clientRequestToken
              ?managedExecution:(Option.map
                                   ~f:Values.ManagedExecution.of_json
                                   managedExecution) ~stackSetName ())
           (Some Values.CreateStackSetOutput.to_json)
           (Some Values.CreateStackSetOutput.error_to_json)])
let deactivate_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and typeName =
         flag "type-name" (optional string) ~doc:"STRING TypeName"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON ThirdPartyType"
       and arn = flag "arn" (optional string) ~doc:"STRING PrivateTypeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deactivate_type
           (Values.DeactivateTypeInput.make ?typeName
              ?type_:(Option.map ~f:Values.ThirdPartyType.of_json type_) ?arn
              ()) (Some Values.DeactivateTypeOutput.to_json)
           (Some Values.DeactivateTypeOutput.error_to_json)])
let delete_change_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (optional string) ~doc:"STRING StackNameOrId"
       and changeSetName =
         flag "change-set-name" (required string)
           ~doc:"STRING ChangeSetNameOrId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_change_set
           (Values.DeleteChangeSetInput.make ?stackName ~changeSetName ())
           (Some Values.DeleteChangeSetOutput.to_json)
           (Some Values.DeleteChangeSetOutput.error_to_json)])
let delete_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retainResources =
         flag "retain-resources" (optional json_arg)
           ~doc:"JSON RetainResources"
       and roleARN =
         flag "role-a-r-n" (optional string) ~doc:"STRING RoleARN"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_stack
           (Values.DeleteStackInput.make
              ?retainResources:(Option.map ~f:Values.RetainResources.of_json
                                  retainResources) ?roleARN
              ?clientRequestToken ~stackName ()) None None])
let delete_stack_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accounts =
         flag "accounts" (optional json_arg) ~doc:"JSON AccountList"
       and deploymentTargets =
         flag "deployment-targets" (optional json_arg)
           ~doc:"JSON DeploymentTargets"
       and operationPreferences =
         flag "operation-preferences" (optional json_arg)
           ~doc:"JSON StackSetOperationPreferences"
       and operationId =
         flag "operation-id" (optional string)
           ~doc:"STRING ClientRequestToken"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and stackSetName =
         flag "stack-set-name" (required string) ~doc:"STRING StackSetName"
       and regions =
         flag "regions" (required json_arg) ~doc:"JSON RegionList"
       and retainStacks =
         flag "retain-stacks" (required bool) ~doc:"BOOL RetainStacks" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_stack_instances
           (Values.DeleteStackInstancesInput.make
              ?accounts:(Option.map ~f:Values.AccountList.of_json accounts)
              ?deploymentTargets:(Option.map
                                    ~f:Values.DeploymentTargets.of_json
                                    deploymentTargets)
              ?operationPreferences:(Option.map
                                       ~f:Values.StackSetOperationPreferences.of_json
                                       operationPreferences) ?operationId
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ~stackSetName ~regions:(Values.RegionList.of_json regions)
              ~retainStacks ())
           (Some Values.DeleteStackInstancesOutput.to_json)
           (Some Values.DeleteStackInstancesOutput.error_to_json)])
let delete_stack_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and stackSetName =
         flag "stack-set-name" (required string) ~doc:"STRING StackSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_stack_set
           (Values.DeleteStackSetInput.make
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ~stackSetName ()) (Some Values.DeleteStackSetOutput.to_json)
           (Some Values.DeleteStackSetOutput.error_to_json)])
let deregister_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (optional string) ~doc:"STRING PrivateTypeArn"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON RegistryType"
       and typeName =
         flag "type-name" (optional string) ~doc:"STRING TypeName"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING TypeVersionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_type
           (Values.DeregisterTypeInput.make ?arn
              ?type_:(Option.map ~f:Values.RegistryType.of_json type_)
              ?typeName ?versionId ())
           (Some Values.DeregisterTypeOutput.to_json)
           (Some Values.DeregisterTypeOutput.error_to_json)])
let describe_account_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_limits
           (Values.DescribeAccountLimitsInput.make ?nextToken ())
           (Some Values.DescribeAccountLimitsOutput.to_json)
           (Some Values.DescribeAccountLimitsOutput.error_to_json)])
let describe_change_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (optional string) ~doc:"STRING StackNameOrId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and changeSetName =
         flag "change-set-name" (required string)
           ~doc:"STRING ChangeSetNameOrId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_change_set
           (Values.DescribeChangeSetInput.make ?stackName ?nextToken
              ~changeSetName ())
           (Some Values.DescribeChangeSetOutput.to_json)
           (Some Values.DescribeChangeSetOutput.error_to_json)])
let describe_change_set_hooks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (optional string) ~doc:"STRING StackNameOrId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and logicalResourceId =
         flag "logical-resource-id" (optional string)
           ~doc:"STRING LogicalResourceId"
       and changeSetName =
         flag "change-set-name" (required string)
           ~doc:"STRING ChangeSetNameOrId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_change_set_hooks
           (Values.DescribeChangeSetHooksInput.make ?stackName ?nextToken
              ?logicalResourceId ~changeSetName ())
           (Some Values.DescribeChangeSetHooksOutput.to_json)
           (Some Values.DescribeChangeSetHooksOutput.error_to_json)])
let describe_publisher =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and publisherId =
         flag "publisher-id" (optional string) ~doc:"STRING PublisherId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_publisher
           (Values.DescribePublisherInput.make ?publisherId ())
           (Some Values.DescribePublisherOutput.to_json)
           (Some Values.DescribePublisherOutput.error_to_json)])
let describe_stack_drift_detection_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackDriftDetectionId =
         flag "stack-drift-detection-id" (required string)
           ~doc:"STRING StackDriftDetectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stack_drift_detection_status
           (Values.DescribeStackDriftDetectionStatusInput.make
              ~stackDriftDetectionId ())
           (Some Values.DescribeStackDriftDetectionStatusOutput.to_json)
           (Some Values.DescribeStackDriftDetectionStatusOutput.error_to_json)])
let describe_stack_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (optional string) ~doc:"STRING StackName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stack_events
           (Values.DescribeStackEventsInput.make ?stackName ?nextToken ())
           (Some Values.DescribeStackEventsOutput.to_json)
           (Some Values.DescribeStackEventsOutput.error_to_json)])
let describe_stack_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and stackSetName =
         flag "stack-set-name" (required string) ~doc:"STRING StackSetName"
       and stackInstanceAccount =
         flag "stack-instance-account" (required string)
           ~doc:"STRING Account"
       and stackInstanceRegion =
         flag "stack-instance-region" (required string) ~doc:"STRING Region" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stack_instance
           (Values.DescribeStackInstanceInput.make
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ~stackSetName ~stackInstanceAccount ~stackInstanceRegion ())
           (Some Values.DescribeStackInstanceOutput.to_json)
           (Some Values.DescribeStackInstanceOutput.error_to_json)])
let describe_stack_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackName"
       and logicalResourceId =
         flag "logical-resource-id" (required string)
           ~doc:"STRING LogicalResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stack_resource
           (Values.DescribeStackResourceInput.make ~stackName
              ~logicalResourceId ())
           (Some Values.DescribeStackResourceOutput.to_json)
           (Some Values.DescribeStackResourceOutput.error_to_json)])
let describe_stack_resource_drifts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackResourceDriftStatusFilters =
         flag "stack-resource-drift-status-filters" (optional json_arg)
           ~doc:"JSON StackResourceDriftStatusFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxedMaxResults"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackNameOrId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stack_resource_drifts
           (Values.DescribeStackResourceDriftsInput.make
              ?stackResourceDriftStatusFilters:(Option.map
                                                  ~f:Values.StackResourceDriftStatusFilters.of_json
                                                  stackResourceDriftStatusFilters)
              ?nextToken ?maxResults ~stackName ())
           (Some Values.DescribeStackResourceDriftsOutput.to_json)
           (Some Values.DescribeStackResourceDriftsOutput.error_to_json)])
let describe_stack_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (optional string) ~doc:"STRING StackName"
       and logicalResourceId =
         flag "logical-resource-id" (optional string)
           ~doc:"STRING LogicalResourceId"
       and physicalResourceId =
         flag "physical-resource-id" (optional string)
           ~doc:"STRING PhysicalResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stack_resources
           (Values.DescribeStackResourcesInput.make ?stackName
              ?logicalResourceId ?physicalResourceId ())
           (Some Values.DescribeStackResourcesOutput.to_json)
           (Some Values.DescribeStackResourcesOutput.error_to_json)])
let describe_stack_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and stackSetName =
         flag "stack-set-name" (required string) ~doc:"STRING StackSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stack_set
           (Values.DescribeStackSetInput.make
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ~stackSetName ()) (Some Values.DescribeStackSetOutput.to_json)
           (Some Values.DescribeStackSetOutput.error_to_json)])
let describe_stack_set_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and stackSetName =
         flag "stack-set-name" (required string) ~doc:"STRING StackSetName"
       and operationId =
         flag "operation-id" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stack_set_operation
           (Values.DescribeStackSetOperationInput.make
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ~stackSetName ~operationId ())
           (Some Values.DescribeStackSetOperationOutput.to_json)
           (Some Values.DescribeStackSetOperationOutput.error_to_json)])
let describe_stacks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (optional string) ~doc:"STRING StackName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stacks
           (Values.DescribeStacksInput.make ?stackName ?nextToken ())
           (Some Values.DescribeStacksOutput.to_json)
           (Some Values.DescribeStacksOutput.error_to_json)])
let describe_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON RegistryType"
       and typeName =
         flag "type-name" (optional string) ~doc:"STRING TypeName"
       and arn = flag "arn" (optional string) ~doc:"STRING TypeArn"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING TypeVersionId"
       and publisherId =
         flag "publisher-id" (optional string) ~doc:"STRING PublisherId"
       and publicVersionNumber =
         flag "public-version-number" (optional string)
           ~doc:"STRING PublicVersionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_type
           (Values.DescribeTypeInput.make
              ?type_:(Option.map ~f:Values.RegistryType.of_json type_)
              ?typeName ?arn ?versionId ?publisherId ?publicVersionNumber ())
           (Some Values.DescribeTypeOutput.to_json)
           (Some Values.DescribeTypeOutput.error_to_json)])
let describe_type_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registrationToken =
         flag "registration-token" (required string)
           ~doc:"STRING RegistrationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_type_registration
           (Values.DescribeTypeRegistrationInput.make ~registrationToken ())
           (Some Values.DescribeTypeRegistrationOutput.to_json)
           (Some Values.DescribeTypeRegistrationOutput.error_to_json)])
let detect_stack_drift =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and logicalResourceIds =
         flag "logical-resource-ids" (optional json_arg)
           ~doc:"JSON LogicalResourceIds"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackNameOrId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_stack_drift
           (Values.DetectStackDriftInput.make
              ?logicalResourceIds:(Option.map
                                     ~f:Values.LogicalResourceIds.of_json
                                     logicalResourceIds) ~stackName ())
           (Some Values.DetectStackDriftOutput.to_json)
           (Some Values.DetectStackDriftOutput.error_to_json)])
let detect_stack_resource_drift =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackNameOrId"
       and logicalResourceId =
         flag "logical-resource-id" (required string)
           ~doc:"STRING LogicalResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_stack_resource_drift
           (Values.DetectStackResourceDriftInput.make ~stackName
              ~logicalResourceId ())
           (Some Values.DetectStackResourceDriftOutput.to_json)
           (Some Values.DetectStackResourceDriftOutput.error_to_json)])
let detect_stack_set_drift =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operationPreferences =
         flag "operation-preferences" (optional json_arg)
           ~doc:"JSON StackSetOperationPreferences"
       and operationId =
         flag "operation-id" (optional string)
           ~doc:"STRING ClientRequestToken"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and stackSetName =
         flag "stack-set-name" (required string)
           ~doc:"STRING StackSetNameOrId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_stack_set_drift
           (Values.DetectStackSetDriftInput.make
              ?operationPreferences:(Option.map
                                       ~f:Values.StackSetOperationPreferences.of_json
                                       operationPreferences) ?operationId
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ~stackSetName ())
           (Some Values.DetectStackSetDriftOutput.to_json)
           (Some Values.DetectStackSetDriftOutput.error_to_json)])
let estimate_template_cost =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateBody =
         flag "template-body" (optional string) ~doc:"STRING TemplateBody"
       and templateURL =
         flag "template-u-r-l" (optional string) ~doc:"STRING TemplateURL"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.estimate_template_cost
           (Values.EstimateTemplateCostInput.make ?templateBody ?templateURL
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ()) (Some Values.EstimateTemplateCostOutput.to_json)
           (Some Values.EstimateTemplateCostOutput.error_to_json)])
let execute_change_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (optional string) ~doc:"STRING StackNameOrId"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and disableRollback =
         flag "disable-rollback" (optional bool) ~doc:"BOOL DisableRollback"
       and changeSetName =
         flag "change-set-name" (required string)
           ~doc:"STRING ChangeSetNameOrId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_change_set
           (Values.ExecuteChangeSetInput.make ?stackName ?clientRequestToken
              ?disableRollback ~changeSetName ())
           (Some Values.ExecuteChangeSetOutput.to_json)
           (Some Values.ExecuteChangeSetOutput.error_to_json)])
let get_stack_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_stack_policy
           (Values.GetStackPolicyInput.make ~stackName ())
           (Some Values.GetStackPolicyOutput.to_json)
           (Some Values.GetStackPolicyOutput.error_to_json)])
let get_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (optional string) ~doc:"STRING StackName"
       and changeSetName =
         flag "change-set-name" (optional string)
           ~doc:"STRING ChangeSetNameOrId"
       and templateStage =
         flag "template-stage" (optional json_arg) ~doc:"JSON TemplateStage" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_template
           (Values.GetTemplateInput.make ?stackName ?changeSetName
              ?templateStage:(Option.map ~f:Values.TemplateStage.of_json
                                templateStage) ())
           (Some Values.GetTemplateOutput.to_json)
           (Some Values.GetTemplateOutput.error_to_json)])
let get_template_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateBody =
         flag "template-body" (optional string) ~doc:"STRING TemplateBody"
       and templateURL =
         flag "template-u-r-l" (optional string) ~doc:"STRING TemplateURL"
       and stackName =
         flag "stack-name" (optional string) ~doc:"STRING StackNameOrId"
       and stackSetName =
         flag "stack-set-name" (optional string)
           ~doc:"STRING StackSetNameOrId"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_template_summary
           (Values.GetTemplateSummaryInput.make ?templateBody ?templateURL
              ?stackName ?stackSetName
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs) ())
           (Some Values.GetTemplateSummaryOutput.to_json)
           (Some Values.GetTemplateSummaryOutput.error_to_json)])
let import_stacks_to_stack_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackIds =
         flag "stack-ids" (optional json_arg) ~doc:"JSON StackIdList"
       and stackIdsUrl =
         flag "stack-ids-url" (optional string) ~doc:"STRING StackIdsUrl"
       and organizationalUnitIds =
         flag "organizational-unit-ids" (optional json_arg)
           ~doc:"JSON OrganizationalUnitIdList"
       and operationPreferences =
         flag "operation-preferences" (optional json_arg)
           ~doc:"JSON StackSetOperationPreferences"
       and operationId =
         flag "operation-id" (optional string)
           ~doc:"STRING ClientRequestToken"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and stackSetName =
         flag "stack-set-name" (required string)
           ~doc:"STRING StackSetNameOrId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_stacks_to_stack_set
           (Values.ImportStacksToStackSetInput.make
              ?stackIds:(Option.map ~f:Values.StackIdList.of_json stackIds)
              ?stackIdsUrl
              ?organizationalUnitIds:(Option.map
                                        ~f:Values.OrganizationalUnitIdList.of_json
                                        organizationalUnitIds)
              ?operationPreferences:(Option.map
                                       ~f:Values.StackSetOperationPreferences.of_json
                                       operationPreferences) ?operationId
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ~stackSetName ())
           (Some Values.ImportStacksToStackSetOutput.to_json)
           (Some Values.ImportStacksToStackSetOutput.error_to_json)])
let list_change_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackNameOrId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_change_sets
           (Values.ListChangeSetsInput.make ?nextToken ~stackName ())
           (Some Values.ListChangeSetsOutput.to_json)
           (Some Values.ListChangeSetsOutput.error_to_json)])
let list_exports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_exports (Values.ListExportsInput.make ?nextToken ())
           (Some Values.ListExportsOutput.to_json)
           (Some Values.ListExportsOutput.error_to_json)])
let list_imports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and exportName =
         flag "export-name" (required string) ~doc:"STRING ExportName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_imports
           (Values.ListImportsInput.make ?nextToken ~exportName ())
           (Some Values.ListImportsOutput.to_json)
           (Some Values.ListImportsOutput.error_to_json)])
let list_stack_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON StackInstanceFilters"
       and stackInstanceAccount =
         flag "stack-instance-account" (optional string)
           ~doc:"STRING Account"
       and stackInstanceRegion =
         flag "stack-instance-region" (optional string) ~doc:"STRING Region"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and stackSetName =
         flag "stack-set-name" (required string) ~doc:"STRING StackSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stack_instances
           (Values.ListStackInstancesInput.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.StackInstanceFilters.of_json
                          filters) ?stackInstanceAccount ?stackInstanceRegion
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ~stackSetName ())
           (Some Values.ListStackInstancesOutput.to_json)
           (Some Values.ListStackInstancesOutput.error_to_json)])
let list_stack_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stack_resources
           (Values.ListStackResourcesInput.make ?nextToken ~stackName ())
           (Some Values.ListStackResourcesOutput.to_json)
           (Some Values.ListStackResourcesOutput.error_to_json)])
let list_stack_set_operation_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and stackSetName =
         flag "stack-set-name" (required string) ~doc:"STRING StackSetName"
       and operationId =
         flag "operation-id" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stack_set_operation_results
           (Values.ListStackSetOperationResultsInput.make ?nextToken
              ?maxResults
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ~stackSetName ~operationId ())
           (Some Values.ListStackSetOperationResultsOutput.to_json)
           (Some Values.ListStackSetOperationResultsOutput.error_to_json)])
let list_stack_set_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and stackSetName =
         flag "stack-set-name" (required string) ~doc:"STRING StackSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stack_set_operations
           (Values.ListStackSetOperationsInput.make ?nextToken ?maxResults
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ~stackSetName ())
           (Some Values.ListStackSetOperationsOutput.to_json)
           (Some Values.ListStackSetOperationsOutput.error_to_json)])
let list_stack_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON StackSetStatus"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stack_sets
           (Values.ListStackSetsInput.make ?nextToken ?maxResults
              ?status:(Option.map ~f:Values.StackSetStatus.of_json status)
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs) ())
           (Some Values.ListStackSetsOutput.to_json)
           (Some Values.ListStackSetsOutput.error_to_json)])
let list_stacks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and stackStatusFilter =
         flag "stack-status-filter" (optional json_arg)
           ~doc:"JSON StackStatusFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stacks
           (Values.ListStacksInput.make ?nextToken
              ?stackStatusFilter:(Option.map
                                    ~f:Values.StackStatusFilter.of_json
                                    stackStatusFilter) ())
           (Some Values.ListStacksOutput.to_json)
           (Some Values.ListStacksOutput.error_to_json)])
let list_type_registrations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON RegistryType"
       and typeName =
         flag "type-name" (optional string) ~doc:"STRING TypeName"
       and typeArn = flag "type-arn" (optional string) ~doc:"STRING TypeArn"
       and registrationStatusFilter =
         flag "registration-status-filter" (optional json_arg)
           ~doc:"JSON RegistrationStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_type_registrations
           (Values.ListTypeRegistrationsInput.make
              ?type_:(Option.map ~f:Values.RegistryType.of_json type_)
              ?typeName ?typeArn
              ?registrationStatusFilter:(Option.map
                                           ~f:Values.RegistrationStatus.of_json
                                           registrationStatusFilter)
              ?maxResults ?nextToken ())
           (Some Values.ListTypeRegistrationsOutput.to_json)
           (Some Values.ListTypeRegistrationsOutput.error_to_json)])
let list_type_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON RegistryType"
       and typeName =
         flag "type-name" (optional string) ~doc:"STRING TypeName"
       and arn = flag "arn" (optional string) ~doc:"STRING TypeArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and deprecatedStatus =
         flag "deprecated-status" (optional json_arg)
           ~doc:"JSON DeprecatedStatus"
       and publisherId =
         flag "publisher-id" (optional string) ~doc:"STRING PublisherId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_type_versions
           (Values.ListTypeVersionsInput.make
              ?type_:(Option.map ~f:Values.RegistryType.of_json type_)
              ?typeName ?arn ?maxResults ?nextToken
              ?deprecatedStatus:(Option.map
                                   ~f:Values.DeprecatedStatus.of_json
                                   deprecatedStatus) ?publisherId ())
           (Some Values.ListTypeVersionsOutput.to_json)
           (Some Values.ListTypeVersionsOutput.error_to_json)])
let list_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and visibility =
         flag "visibility" (optional json_arg) ~doc:"JSON Visibility"
       and provisioningType =
         flag "provisioning-type" (optional json_arg)
           ~doc:"JSON ProvisioningType"
       and deprecatedStatus =
         flag "deprecated-status" (optional json_arg)
           ~doc:"JSON DeprecatedStatus"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON RegistryType"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON TypeFilters"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_types
           (Values.ListTypesInput.make
              ?visibility:(Option.map ~f:Values.Visibility.of_json visibility)
              ?provisioningType:(Option.map
                                   ~f:Values.ProvisioningType.of_json
                                   provisioningType)
              ?deprecatedStatus:(Option.map
                                   ~f:Values.DeprecatedStatus.of_json
                                   deprecatedStatus)
              ?type_:(Option.map ~f:Values.RegistryType.of_json type_)
              ?filters:(Option.map ~f:Values.TypeFilters.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.ListTypesOutput.to_json)
           (Some Values.ListTypesOutput.error_to_json)])
let publish_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON ThirdPartyType"
       and arn = flag "arn" (optional string) ~doc:"STRING PrivateTypeArn"
       and typeName =
         flag "type-name" (optional string) ~doc:"STRING TypeName"
       and publicVersionNumber =
         flag "public-version-number" (optional string)
           ~doc:"STRING PublicVersionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.publish_type
           (Values.PublishTypeInput.make
              ?type_:(Option.map ~f:Values.ThirdPartyType.of_json type_) ?arn
              ?typeName ?publicVersionNumber ())
           (Some Values.PublishTypeOutput.to_json)
           (Some Values.PublishTypeOutput.error_to_json)])
let record_handler_progress =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and currentOperationStatus =
         flag "current-operation-status" (optional json_arg)
           ~doc:"JSON OperationStatus"
       and statusMessage =
         flag "status-message" (optional string) ~doc:"STRING StatusMessage"
       and errorCode =
         flag "error-code" (optional json_arg) ~doc:"JSON HandlerErrorCode"
       and resourceModel =
         flag "resource-model" (optional string) ~doc:"STRING ResourceModel"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and bearerToken =
         flag "bearer-token" (required string) ~doc:"STRING ClientToken"
       and operationStatus =
         flag "operation-status" (required json_arg)
           ~doc:"JSON OperationStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.record_handler_progress
           (Values.RecordHandlerProgressInput.make
              ?currentOperationStatus:(Option.map
                                         ~f:Values.OperationStatus.of_json
                                         currentOperationStatus)
              ?statusMessage
              ?errorCode:(Option.map ~f:Values.HandlerErrorCode.of_json
                            errorCode) ?resourceModel ?clientRequestToken
              ~bearerToken
              ~operationStatus:(Values.OperationStatus.of_json
                                  operationStatus) ())
           (Some Values.RecordHandlerProgressOutput.to_json)
           (Some Values.RecordHandlerProgressOutput.error_to_json)])
let register_publisher =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptTermsAndConditions =
         flag "accept-terms-and-conditions" (optional bool)
           ~doc:"BOOL AcceptTermsAndConditions"
       and connectionArn =
         flag "connection-arn" (optional string) ~doc:"STRING ConnectionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_publisher
           (Values.RegisterPublisherInput.make ?acceptTermsAndConditions
              ?connectionArn ())
           (Some Values.RegisterPublisherOutput.to_json)
           (Some Values.RegisterPublisherOutput.error_to_json)])
let register_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON RegistryType"
       and loggingConfig =
         flag "logging-config" (optional json_arg) ~doc:"JSON LoggingConfig"
       and executionRoleArn =
         flag "execution-role-arn" (optional string) ~doc:"STRING RoleArn"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING RequestToken"
       and typeName =
         flag "type-name" (required string) ~doc:"STRING TypeName"
       and schemaHandlerPackage =
         flag "schema-handler-package" (required string) ~doc:"STRING S3Url" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_type
           (Values.RegisterTypeInput.make
              ?type_:(Option.map ~f:Values.RegistryType.of_json type_)
              ?loggingConfig:(Option.map ~f:Values.LoggingConfig.of_json
                                loggingConfig) ?executionRoleArn
              ?clientRequestToken ~typeName ~schemaHandlerPackage ())
           (Some Values.RegisterTypeOutput.to_json)
           (Some Values.RegisterTypeOutput.error_to_json)])
let rollback_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleARN =
         flag "role-a-r-n" (optional string) ~doc:"STRING RoleARN"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackNameOrId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.rollback_stack
           (Values.RollbackStackInput.make ?roleARN ?clientRequestToken
              ~stackName ()) (Some Values.RollbackStackOutput.to_json)
           (Some Values.RollbackStackOutput.error_to_json)])
let set_stack_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackPolicyBody =
         flag "stack-policy-body" (optional string)
           ~doc:"STRING StackPolicyBody"
       and stackPolicyURL =
         flag "stack-policy-u-r-l" (optional string)
           ~doc:"STRING StackPolicyURL"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_stack_policy
           (Values.SetStackPolicyInput.make ?stackPolicyBody ?stackPolicyURL
              ~stackName ()) None None])
let set_type_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and typeArn = flag "type-arn" (optional string) ~doc:"STRING TypeArn"
       and configurationAlias =
         flag "configuration-alias" (optional string)
           ~doc:"STRING TypeConfigurationAlias"
       and typeName =
         flag "type-name" (optional string) ~doc:"STRING TypeName"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON ThirdPartyType"
       and configuration =
         flag "configuration" (required string)
           ~doc:"STRING TypeConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_type_configuration
           (Values.SetTypeConfigurationInput.make ?typeArn
              ?configurationAlias ?typeName
              ?type_:(Option.map ~f:Values.ThirdPartyType.of_json type_)
              ~configuration ())
           (Some Values.SetTypeConfigurationOutput.to_json)
           (Some Values.SetTypeConfigurationOutput.error_to_json)])
let set_type_default_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (optional string) ~doc:"STRING PrivateTypeArn"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON RegistryType"
       and typeName =
         flag "type-name" (optional string) ~doc:"STRING TypeName"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING TypeVersionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_type_default_version
           (Values.SetTypeDefaultVersionInput.make ?arn
              ?type_:(Option.map ~f:Values.RegistryType.of_json type_)
              ?typeName ?versionId ())
           (Some Values.SetTypeDefaultVersionOutput.to_json)
           (Some Values.SetTypeDefaultVersionOutput.error_to_json)])
let signal_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackNameOrId"
       and logicalResourceId =
         flag "logical-resource-id" (required string)
           ~doc:"STRING LogicalResourceId"
       and uniqueId =
         flag "unique-id" (required string)
           ~doc:"STRING ResourceSignalUniqueId"
       and status =
         flag "status" (required json_arg) ~doc:"JSON ResourceSignalStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.signal_resource
           (Values.SignalResourceInput.make ~stackName ~logicalResourceId
              ~uniqueId ~status:(Values.ResourceSignalStatus.of_json status)
              ()) None None])
let stop_stack_set_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and stackSetName =
         flag "stack-set-name" (required string) ~doc:"STRING StackSetName"
       and operationId =
         flag "operation-id" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_stack_set_operation
           (Values.StopStackSetOperationInput.make
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ~stackSetName ~operationId ())
           (Some Values.StopStackSetOperationOutput.to_json)
           (Some Values.StopStackSetOperationOutput.error_to_json)])
let test_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (optional string) ~doc:"STRING TypeArn"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON ThirdPartyType"
       and typeName =
         flag "type-name" (optional string) ~doc:"STRING TypeName"
       and versionId =
         flag "version-id" (optional string) ~doc:"STRING TypeVersionId"
       and logDeliveryBucket =
         flag "log-delivery-bucket" (optional string) ~doc:"STRING S3Bucket" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_type
           (Values.TestTypeInput.make ?arn
              ?type_:(Option.map ~f:Values.ThirdPartyType.of_json type_)
              ?typeName ?versionId ?logDeliveryBucket ())
           (Some Values.TestTypeOutput.to_json)
           (Some Values.TestTypeOutput.error_to_json)])
let update_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateBody =
         flag "template-body" (optional string) ~doc:"STRING TemplateBody"
       and templateURL =
         flag "template-u-r-l" (optional string) ~doc:"STRING TemplateURL"
       and usePreviousTemplate =
         flag "use-previous-template" (optional bool)
           ~doc:"BOOL UsePreviousTemplate"
       and stackPolicyDuringUpdateBody =
         flag "stack-policy-during-update-body" (optional string)
           ~doc:"STRING StackPolicyDuringUpdateBody"
       and stackPolicyDuringUpdateURL =
         flag "stack-policy-during-update-u-r-l" (optional string)
           ~doc:"STRING StackPolicyDuringUpdateURL"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and capabilities =
         flag "capabilities" (optional json_arg) ~doc:"JSON Capabilities"
       and resourceTypes =
         flag "resource-types" (optional json_arg) ~doc:"JSON ResourceTypes"
       and roleARN =
         flag "role-a-r-n" (optional string) ~doc:"STRING RoleARN"
       and rollbackConfiguration =
         flag "rollback-configuration" (optional json_arg)
           ~doc:"JSON RollbackConfiguration"
       and stackPolicyBody =
         flag "stack-policy-body" (optional string)
           ~doc:"STRING StackPolicyBody"
       and stackPolicyURL =
         flag "stack-policy-u-r-l" (optional string)
           ~doc:"STRING StackPolicyURL"
       and notificationARNs =
         flag "notification-a-r-ns" (optional json_arg)
           ~doc:"JSON NotificationARNs"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and disableRollback =
         flag "disable-rollback" (optional bool) ~doc:"BOOL DisableRollback"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_stack
           (Values.UpdateStackInput.make ?templateBody ?templateURL
              ?usePreviousTemplate ?stackPolicyDuringUpdateBody
              ?stackPolicyDuringUpdateURL
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?capabilities:(Option.map ~f:Values.Capabilities.of_json
                               capabilities)
              ?resourceTypes:(Option.map ~f:Values.ResourceTypes.of_json
                                resourceTypes) ?roleARN
              ?rollbackConfiguration:(Option.map
                                        ~f:Values.RollbackConfiguration.of_json
                                        rollbackConfiguration)
              ?stackPolicyBody ?stackPolicyURL
              ?notificationARNs:(Option.map
                                   ~f:Values.NotificationARNs.of_json
                                   notificationARNs)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?disableRollback
              ?clientRequestToken ~stackName ())
           (Some Values.UpdateStackOutput.to_json)
           (Some Values.UpdateStackOutput.error_to_json)])
let update_stack_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accounts =
         flag "accounts" (optional json_arg) ~doc:"JSON AccountList"
       and deploymentTargets =
         flag "deployment-targets" (optional json_arg)
           ~doc:"JSON DeploymentTargets"
       and parameterOverrides =
         flag "parameter-overrides" (optional json_arg)
           ~doc:"JSON Parameters"
       and operationPreferences =
         flag "operation-preferences" (optional json_arg)
           ~doc:"JSON StackSetOperationPreferences"
       and operationId =
         flag "operation-id" (optional string)
           ~doc:"STRING ClientRequestToken"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and stackSetName =
         flag "stack-set-name" (required string)
           ~doc:"STRING StackSetNameOrId"
       and regions =
         flag "regions" (required json_arg) ~doc:"JSON RegionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_stack_instances
           (Values.UpdateStackInstancesInput.make
              ?accounts:(Option.map ~f:Values.AccountList.of_json accounts)
              ?deploymentTargets:(Option.map
                                    ~f:Values.DeploymentTargets.of_json
                                    deploymentTargets)
              ?parameterOverrides:(Option.map ~f:Values.Parameters.of_json
                                     parameterOverrides)
              ?operationPreferences:(Option.map
                                       ~f:Values.StackSetOperationPreferences.of_json
                                       operationPreferences) ?operationId
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ~stackSetName ~regions:(Values.RegionList.of_json regions) ())
           (Some Values.UpdateStackInstancesOutput.to_json)
           (Some Values.UpdateStackInstancesOutput.error_to_json)])
let update_stack_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and templateBody =
         flag "template-body" (optional string) ~doc:"STRING TemplateBody"
       and templateURL =
         flag "template-u-r-l" (optional string) ~doc:"STRING TemplateURL"
       and usePreviousTemplate =
         flag "use-previous-template" (optional bool)
           ~doc:"BOOL UsePreviousTemplate"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and capabilities =
         flag "capabilities" (optional json_arg) ~doc:"JSON Capabilities"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and operationPreferences =
         flag "operation-preferences" (optional json_arg)
           ~doc:"JSON StackSetOperationPreferences"
       and administrationRoleARN =
         flag "administration-role-a-r-n" (optional string)
           ~doc:"STRING RoleARN"
       and executionRoleName =
         flag "execution-role-name" (optional string)
           ~doc:"STRING ExecutionRoleName"
       and deploymentTargets =
         flag "deployment-targets" (optional json_arg)
           ~doc:"JSON DeploymentTargets"
       and permissionModel =
         flag "permission-model" (optional json_arg)
           ~doc:"JSON PermissionModels"
       and autoDeployment =
         flag "auto-deployment" (optional json_arg)
           ~doc:"JSON AutoDeployment"
       and operationId =
         flag "operation-id" (optional string)
           ~doc:"STRING ClientRequestToken"
       and accounts =
         flag "accounts" (optional json_arg) ~doc:"JSON AccountList"
       and regions =
         flag "regions" (optional json_arg) ~doc:"JSON RegionList"
       and callAs = flag "call-as" (optional json_arg) ~doc:"JSON CallAs"
       and managedExecution =
         flag "managed-execution" (optional json_arg)
           ~doc:"JSON ManagedExecution"
       and stackSetName =
         flag "stack-set-name" (required string) ~doc:"STRING StackSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_stack_set
           (Values.UpdateStackSetInput.make ?description ?templateBody
              ?templateURL ?usePreviousTemplate
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?capabilities:(Option.map ~f:Values.Capabilities.of_json
                               capabilities)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?operationPreferences:(Option.map
                                       ~f:Values.StackSetOperationPreferences.of_json
                                       operationPreferences)
              ?administrationRoleARN ?executionRoleName
              ?deploymentTargets:(Option.map
                                    ~f:Values.DeploymentTargets.of_json
                                    deploymentTargets)
              ?permissionModel:(Option.map ~f:Values.PermissionModels.of_json
                                  permissionModel)
              ?autoDeployment:(Option.map ~f:Values.AutoDeployment.of_json
                                 autoDeployment) ?operationId
              ?accounts:(Option.map ~f:Values.AccountList.of_json accounts)
              ?regions:(Option.map ~f:Values.RegionList.of_json regions)
              ?callAs:(Option.map ~f:Values.CallAs.of_json callAs)
              ?managedExecution:(Option.map
                                   ~f:Values.ManagedExecution.of_json
                                   managedExecution) ~stackSetName ())
           (Some Values.UpdateStackSetOutput.to_json)
           (Some Values.UpdateStackSetOutput.error_to_json)])
let update_termination_protection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enableTerminationProtection =
         flag "enable-termination-protection" (required bool)
           ~doc:"BOOL EnableTerminationProtection"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING StackNameOrId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_termination_protection
           (Values.UpdateTerminationProtectionInput.make
              ~enableTerminationProtection ~stackName ())
           (Some Values.UpdateTerminationProtectionOutput.to_json)
           (Some Values.UpdateTerminationProtectionOutput.error_to_json)])
let validate_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateBody =
         flag "template-body" (optional string) ~doc:"STRING TemplateBody"
       and templateURL =
         flag "template-u-r-l" (optional string) ~doc:"STRING TemplateURL" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.validate_template
           (Values.ValidateTemplateInput.make ?templateBody ?templateURL ())
           (Some Values.ValidateTemplateOutput.to_json)
           (Some Values.ValidateTemplateOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("activate-type", activate_type);
    ("batch-describe-type-configurations",
      batch_describe_type_configurations);
    ("cancel-update-stack", cancel_update_stack);
    ("continue-update-rollback", continue_update_rollback);
    ("create-change-set", create_change_set);
    ("create-stack", create_stack);
    ("create-stack-instances", create_stack_instances);
    ("create-stack-set", create_stack_set);
    ("deactivate-type", deactivate_type);
    ("delete-change-set", delete_change_set);
    ("delete-stack", delete_stack);
    ("delete-stack-instances", delete_stack_instances);
    ("delete-stack-set", delete_stack_set);
    ("deregister-type", deregister_type);
    ("describe-account-limits", describe_account_limits);
    ("describe-change-set", describe_change_set);
    ("describe-change-set-hooks", describe_change_set_hooks);
    ("describe-publisher", describe_publisher);
    ("describe-stack-drift-detection-status",
      describe_stack_drift_detection_status);
    ("describe-stack-events", describe_stack_events);
    ("describe-stack-instance", describe_stack_instance);
    ("describe-stack-resource", describe_stack_resource);
    ("describe-stack-resource-drifts", describe_stack_resource_drifts);
    ("describe-stack-resources", describe_stack_resources);
    ("describe-stack-set", describe_stack_set);
    ("describe-stack-set-operation", describe_stack_set_operation);
    ("describe-stacks", describe_stacks);
    ("describe-type", describe_type);
    ("describe-type-registration", describe_type_registration);
    ("detect-stack-drift", detect_stack_drift);
    ("detect-stack-resource-drift", detect_stack_resource_drift);
    ("detect-stack-set-drift", detect_stack_set_drift);
    ("estimate-template-cost", estimate_template_cost);
    ("execute-change-set", execute_change_set);
    ("get-stack-policy", get_stack_policy);
    ("get-template", get_template);
    ("get-template-summary", get_template_summary);
    ("import-stacks-to-stack-set", import_stacks_to_stack_set);
    ("list-change-sets", list_change_sets);
    ("list-exports", list_exports);
    ("list-imports", list_imports);
    ("list-stack-instances", list_stack_instances);
    ("list-stack-resources", list_stack_resources);
    ("list-stack-set-operation-results", list_stack_set_operation_results);
    ("list-stack-set-operations", list_stack_set_operations);
    ("list-stack-sets", list_stack_sets);
    ("list-stacks", list_stacks);
    ("list-type-registrations", list_type_registrations);
    ("list-type-versions", list_type_versions);
    ("list-types", list_types);
    ("publish-type", publish_type);
    ("record-handler-progress", record_handler_progress);
    ("register-publisher", register_publisher);
    ("register-type", register_type);
    ("rollback-stack", rollback_stack);
    ("set-stack-policy", set_stack_policy);
    ("set-type-configuration", set_type_configuration);
    ("set-type-default-version", set_type_default_version);
    ("signal-resource", signal_resource);
    ("stop-stack-set-operation", stop_stack_set_operation);
    ("test-type", test_type);
    ("update-stack", update_stack);
    ("update-stack-instances", update_stack_instances);
    ("update-stack-set", update_stack_set);
    ("update-termination-protection", update_termination_protection);
    ("validate-template", validate_template)]
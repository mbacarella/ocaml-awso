(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_journal_kinesis_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ledgerName =
         flag "ledger-name" (required string) ~doc:"STRING LedgerName"
       and streamId =
         flag "stream-id" (required string) ~doc:"STRING UniqueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_journal_kinesis_stream
           (Values.CancelJournalKinesisStreamRequest.make ~ledgerName
              ~streamId ())
           (Some Values.CancelJournalKinesisStreamResponse.to_json)
           (Some Values.CancelJournalKinesisStreamResponse.error_to_json)])
let create_ledger =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL DeletionProtection"
       and kmsKey = flag "kms-key" (optional string) ~doc:"STRING KmsKey"
       and name = flag "name" (required string) ~doc:"STRING LedgerName"
       and permissionsMode =
         flag "permissions-mode" (required json_arg)
           ~doc:"JSON PermissionsMode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ledger
           (Values.CreateLedgerRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?deletionProtection ?kmsKey ~name
              ~permissionsMode:(Values.PermissionsMode.of_json
                                  permissionsMode) ())
           (Some Values.CreateLedgerResponse.to_json)
           (Some Values.CreateLedgerResponse.error_to_json)])
let delete_ledger =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING LedgerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_ledger (Values.DeleteLedgerRequest.make ~name ()) None
           None])
let describe_journal_kinesis_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ledgerName =
         flag "ledger-name" (required string) ~doc:"STRING LedgerName"
       and streamId =
         flag "stream-id" (required string) ~doc:"STRING UniqueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_journal_kinesis_stream
           (Values.DescribeJournalKinesisStreamRequest.make ~ledgerName
              ~streamId ())
           (Some Values.DescribeJournalKinesisStreamResponse.to_json)
           (Some Values.DescribeJournalKinesisStreamResponse.error_to_json)])
let describe_journal_s3_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING LedgerName"
       and exportId =
         flag "export-id" (required string) ~doc:"STRING UniqueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_journal_s3_export
           (Values.DescribeJournalS3ExportRequest.make ~name ~exportId ())
           (Some Values.DescribeJournalS3ExportResponse.to_json)
           (Some Values.DescribeJournalS3ExportResponse.error_to_json)])
let describe_ledger =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING LedgerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ledger (Values.DescribeLedgerRequest.make ~name ())
           (Some Values.DescribeLedgerResponse.to_json)
           (Some Values.DescribeLedgerResponse.error_to_json)])
let export_journal_to_s3 =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and outputFormat =
         flag "output-format" (optional json_arg) ~doc:"JSON OutputFormat"
       and name = flag "name" (required string) ~doc:"STRING LedgerName"
       and inclusiveStartTime =
         flag "inclusive-start-time" (required json_arg)
           ~doc:"JSON Timestamp"
       and exclusiveEndTime =
         flag "exclusive-end-time" (required json_arg) ~doc:"JSON Timestamp"
       and s3ExportConfiguration =
         flag "s3-export-configuration" (required json_arg)
           ~doc:"JSON S3ExportConfiguration"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_journal_to_s3
           (Values.ExportJournalToS3Request.make
              ?outputFormat:(Option.map ~f:Values.OutputFormat.of_json
                               outputFormat) ~name
              ~inclusiveStartTime:(Values.Timestamp.of_json
                                     inclusiveStartTime)
              ~exclusiveEndTime:(Values.Timestamp.of_json exclusiveEndTime)
              ~s3ExportConfiguration:(Values.S3ExportConfiguration.of_json
                                        s3ExportConfiguration) ~roleArn ())
           (Some Values.ExportJournalToS3Response.to_json)
           (Some Values.ExportJournalToS3Response.error_to_json)])
let get_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and digestTipAddress =
         flag "digest-tip-address" (optional json_arg)
           ~doc:"JSON ValueHolder"
       and name = flag "name" (required string) ~doc:"STRING LedgerName"
       and blockAddress =
         flag "block-address" (required json_arg) ~doc:"JSON ValueHolder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_block
           (Values.GetBlockRequest.make
              ?digestTipAddress:(Option.map ~f:Values.ValueHolder.of_json
                                   digestTipAddress) ~name
              ~blockAddress:(Values.ValueHolder.of_json blockAddress) ())
           (Some Values.GetBlockResponse.to_json)
           (Some Values.GetBlockResponse.error_to_json)])
let get_digest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING LedgerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_digest (Values.GetDigestRequest.make ~name ())
           (Some Values.GetDigestResponse.to_json)
           (Some Values.GetDigestResponse.error_to_json)])
let get_revision =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and digestTipAddress =
         flag "digest-tip-address" (optional json_arg)
           ~doc:"JSON ValueHolder"
       and name = flag "name" (required string) ~doc:"STRING LedgerName"
       and blockAddress =
         flag "block-address" (required json_arg) ~doc:"JSON ValueHolder"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING UniqueId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_revision
           (Values.GetRevisionRequest.make
              ?digestTipAddress:(Option.map ~f:Values.ValueHolder.of_json
                                   digestTipAddress) ~name
              ~blockAddress:(Values.ValueHolder.of_json blockAddress)
              ~documentId ()) (Some Values.GetRevisionResponse.to_json)
           (Some Values.GetRevisionResponse.error_to_json)])
let list_journal_kinesis_streams_for_ledger =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and ledgerName =
         flag "ledger-name" (required string) ~doc:"STRING LedgerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_journal_kinesis_streams_for_ledger
           (Values.ListJournalKinesisStreamsForLedgerRequest.make ?maxResults
              ?nextToken ~ledgerName ())
           (Some Values.ListJournalKinesisStreamsForLedgerResponse.to_json)
           (Some
              Values.ListJournalKinesisStreamsForLedgerResponse.error_to_json)])
let list_journal_s3_exports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_journal_s3_exports
           (Values.ListJournalS3ExportsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListJournalS3ExportsResponse.to_json)
           (Some Values.ListJournalS3ExportsResponse.error_to_json)])
let list_journal_s3_exports_for_ledger =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and name = flag "name" (required string) ~doc:"STRING LedgerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_journal_s3_exports_for_ledger
           (Values.ListJournalS3ExportsForLedgerRequest.make ?maxResults
              ?nextToken ~name ())
           (Some Values.ListJournalS3ExportsForLedgerResponse.to_json)
           (Some Values.ListJournalS3ExportsForLedgerResponse.error_to_json)])
let list_ledgers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ledgers
           (Values.ListLedgersRequest.make ?maxResults ?nextToken ())
           (Some Values.ListLedgersResponse.to_json)
           (Some Values.ListLedgersResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let stream_journal_to_kinesis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and exclusiveEndTime =
         flag "exclusive-end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and ledgerName =
         flag "ledger-name" (required string) ~doc:"STRING LedgerName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING Arn"
       and inclusiveStartTime =
         flag "inclusive-start-time" (required json_arg)
           ~doc:"JSON Timestamp"
       and kinesisConfiguration =
         flag "kinesis-configuration" (required json_arg)
           ~doc:"JSON KinesisConfiguration"
       and streamName =
         flag "stream-name" (required string) ~doc:"STRING StreamName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stream_journal_to_kinesis
           (Values.StreamJournalToKinesisRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?exclusiveEndTime:(Option.map ~f:Values.Timestamp.of_json
                                   exclusiveEndTime) ~ledgerName ~roleArn
              ~inclusiveStartTime:(Values.Timestamp.of_json
                                     inclusiveStartTime)
              ~kinesisConfiguration:(Values.KinesisConfiguration.of_json
                                       kinesisConfiguration) ~streamName ())
           (Some Values.StreamJournalToKinesisResponse.to_json)
           (Some Values.StreamJournalToKinesisResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_ledger =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL DeletionProtection"
       and kmsKey = flag "kms-key" (optional string) ~doc:"STRING KmsKey"
       and name = flag "name" (required string) ~doc:"STRING LedgerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_ledger
           (Values.UpdateLedgerRequest.make ?deletionProtection ?kmsKey ~name
              ()) (Some Values.UpdateLedgerResponse.to_json)
           (Some Values.UpdateLedgerResponse.error_to_json)])
let update_ledger_permissions_mode =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING LedgerName"
       and permissionsMode =
         flag "permissions-mode" (required json_arg)
           ~doc:"JSON PermissionsMode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_ledger_permissions_mode
           (Values.UpdateLedgerPermissionsModeRequest.make ~name
              ~permissionsMode:(Values.PermissionsMode.of_json
                                  permissionsMode) ())
           (Some Values.UpdateLedgerPermissionsModeResponse.to_json)
           (Some Values.UpdateLedgerPermissionsModeResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("cancel-journal-kinesis-stream", cancel_journal_kinesis_stream);
    ("create-ledger", create_ledger);
    ("delete-ledger", delete_ledger);
    ("describe-journal-kinesis-stream", describe_journal_kinesis_stream);
    ("describe-journal-s3-export", describe_journal_s3_export);
    ("describe-ledger", describe_ledger);
    ("export-journal-to-s3", export_journal_to_s3);
    ("get-block", get_block);
    ("get-digest", get_digest);
    ("get-revision", get_revision);
    ("list-journal-kinesis-streams-for-ledger",
      list_journal_kinesis_streams_for_ledger);
    ("list-journal-s3-exports", list_journal_s3_exports);
    ("list-journal-s3-exports-for-ledger",
      list_journal_s3_exports_for_ledger);
    ("list-ledgers", list_ledgers);
    ("list-tags-for-resource", list_tags_for_resource);
    ("stream-journal-to-kinesis", stream_journal_to_kinesis);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-ledger", update_ledger);
    ("update-ledger-permissions-mode", update_ledger_permissions_mode)]
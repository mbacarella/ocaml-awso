(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.qldb
let apiVersion = "2019-01-02"
let endpointPrefix = "qldb"
let serviceFullName = "Amazon QLDB"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "QLDB"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module Arn =
  struct
    type nonrec t = string
    let context_ = "Arn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1600) >>=
             (fun () -> check_string_min i ~min:20));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Arn" j
    let to_json = simple_to_json to_value
  end
module S3ObjectEncryptionType =
  struct
    type nonrec t =
      | SSE_KMS 
      | SSE_S3 
      | NO_ENCRYPTION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SSE_KMS -> "SSE_KMS"
      | SSE_S3 -> "SSE_S3"
      | NO_ENCRYPTION -> "NO_ENCRYPTION"
      | Non_static_id s -> s
    let of_string =
      function
      | "SSE_KMS" -> SSE_KMS
      | "SSE_S3" -> SSE_S3
      | "NO_ENCRYPTION" -> NO_ENCRYPTION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration S3ObjectEncryptionType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"S3ObjectEncryptionType" j)
    let to_json = simple_to_json to_value
  end
module S3Bucket =
  struct
    type nonrec t = string
    let context_ = "S3Bucket"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () -> check_pattern i ~pattern:"^[A-Za-z-0-9-_.]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3Bucket" j
    let to_json = simple_to_json to_value
  end
module S3EncryptionConfiguration =
  struct
    type nonrec t =
      {
      objectEncryptionType: S3ObjectEncryptionType.t
        [@ocaml.doc
          "The Amazon S3 object encryption type. To learn more about server-side encryption options in Amazon S3, see Protecting Data Using Server-Side Encryption in the Amazon S3 Developer Guide."];
      kmsKeyArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of a symmetric key in Key Management Service (KMS). Amazon S3 does not support asymmetric KMS keys. You must provide a KmsKeyArn if you specify SSE_KMS as the ObjectEncryptionType. KmsKeyArn is not required if you specify SSE_S3 as the ObjectEncryptionType."]}
    let context_ = "S3EncryptionConfiguration"
    let make ?kmsKeyArn =
      fun ~objectEncryptionType ->
        fun () -> { kmsKeyArn; objectEncryptionType }
    let to_value x =
      structure_to_value
        [("ObjectEncryptionType",
           (Some (S3ObjectEncryptionType.to_value x.objectEncryptionType)));
        ("KmsKeyArn", (Option.map x.kmsKeyArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKeyArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "KmsKeyArn") in
      let objectEncryptionType =
        S3ObjectEncryptionType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ObjectEncryptionType") in
      make ?kmsKeyArn ~objectEncryptionType ()
    let of_json json =
      let kmsKeyArn = field_map json "KmsKeyArn" Arn.of_json in
      let objectEncryptionType =
        field_map_exn json "ObjectEncryptionType"
          S3ObjectEncryptionType.of_json in
      make ?kmsKeyArn ~objectEncryptionType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The encryption settings that are used by a journal export job to write data in an Amazon Simple Storage Service (Amazon S3) bucket."]
module S3Prefix =
  struct
    type nonrec t = string
    let context_ = "S3Prefix"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3Prefix" j
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module LedgerName =
  struct
    type nonrec t = string
    let context_ = "LedgerName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:32) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(?!^.*--)(?!^[0-9]+$)(?!^-)(?!.*-$)^[A-Za-z0-9-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LedgerName" j
    let to_json = simple_to_json to_value
  end
module LedgerState =
  struct
    type nonrec t =
      | CREATING 
      | ACTIVE 
      | DELETING 
      | DELETED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATING -> "CREATING"
      | ACTIVE -> "ACTIVE"
      | DELETING -> "DELETING"
      | DELETED -> "DELETED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATING" -> CREATING
      | "ACTIVE" -> ACTIVE
      | "DELETING" -> DELETING
      | "DELETED" -> DELETED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LedgerState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LedgerState" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ExportStatus =
  struct
    type nonrec t =
      | IN_PROGRESS 
      | COMPLETED 
      | CANCELLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | IN_PROGRESS -> "IN_PROGRESS"
      | COMPLETED -> "COMPLETED"
      | CANCELLED -> "CANCELLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "IN_PROGRESS" -> IN_PROGRESS
      | "COMPLETED" -> COMPLETED
      | "CANCELLED" -> CANCELLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ExportStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ExportStatus" j)
    let to_json = simple_to_json to_value
  end
module OutputFormat =
  struct
    type nonrec t =
      | ION_BINARY 
      | ION_TEXT 
      | JSON 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ION_BINARY -> "ION_BINARY"
      | ION_TEXT -> "ION_TEXT"
      | JSON -> "JSON"
      | Non_static_id s -> s
    let of_string =
      function
      | "ION_BINARY" -> ION_BINARY
      | "ION_TEXT" -> ION_TEXT
      | "JSON" -> JSON
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration OutputFormat" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"OutputFormat" j)
    let to_json = simple_to_json to_value
  end
module S3ExportConfiguration =
  struct
    type nonrec t =
      {
      bucket: S3Bucket.t
        [@ocaml.doc
          "The Amazon S3 bucket name in which a journal export job writes the journal contents. The bucket name must comply with the Amazon S3 bucket naming conventions. For more information, see Bucket Restrictions and Limitations in the Amazon S3 Developer Guide."];
      prefix: S3Prefix.t
        [@ocaml.doc
          "The prefix for the Amazon S3 bucket in which a journal export job writes the journal contents. The prefix must comply with Amazon S3 key naming rules and restrictions. For more information, see Object Key and Metadata in the Amazon S3 Developer Guide. The following are examples of valid Prefix values: JournalExports-ForMyLedger/Testing/ JournalExports My:Tests/"];
      encryptionConfiguration: S3EncryptionConfiguration.t
        [@ocaml.doc
          "The encryption settings that are used by a journal export job to write data in an Amazon S3 bucket."]}
    let context_ = "S3ExportConfiguration"
    let make ~bucket =
      fun ~prefix ->
        fun ~encryptionConfiguration ->
          fun () -> { bucket; prefix; encryptionConfiguration }
    let to_value x =
      structure_to_value
        [("Bucket", (Some (S3Bucket.to_value x.bucket)));
        ("Prefix", (Some (S3Prefix.to_value x.prefix)));
        ("EncryptionConfiguration",
          (Some
             (S3EncryptionConfiguration.to_value x.encryptionConfiguration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionConfiguration =
        S3EncryptionConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EncryptionConfiguration") in
      let prefix =
        S3Prefix.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Prefix") in
      let bucket =
        S3Bucket.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Bucket") in
      make ~encryptionConfiguration ~prefix ~bucket ()
    let of_json json =
      let encryptionConfiguration =
        field_map_exn json "EncryptionConfiguration"
          S3EncryptionConfiguration.of_json in
      let prefix = field_map_exn json "Prefix" S3Prefix.of_json in
      let bucket = field_map_exn json "Bucket" S3Bucket.of_json in
      make ~encryptionConfiguration ~prefix ~bucket ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Amazon Simple Storage Service (Amazon S3) bucket location in which a journal export job writes the journal contents."]
module UniqueId =
  struct
    type nonrec t = string
    let context_ = "UniqueId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:22) >>=
             (fun () ->
                (check_string_max i ~max:22) >>=
                  (fun () -> check_pattern i ~pattern:"^[A-Za-z-0-9]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"UniqueId" j
    let to_json = simple_to_json to_value
  end
module ErrorCause =
  struct
    type nonrec t =
      | KINESIS_STREAM_NOT_FOUND 
      | IAM_PERMISSION_REVOKED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | KINESIS_STREAM_NOT_FOUND -> "KINESIS_STREAM_NOT_FOUND"
      | IAM_PERMISSION_REVOKED -> "IAM_PERMISSION_REVOKED"
      | Non_static_id s -> s
    let of_string =
      function
      | "KINESIS_STREAM_NOT_FOUND" -> KINESIS_STREAM_NOT_FOUND
      | "IAM_PERMISSION_REVOKED" -> IAM_PERMISSION_REVOKED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ErrorCause" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ErrorCause" j)
    let to_json = simple_to_json to_value
  end
module KinesisConfiguration =
  struct
    type nonrec t =
      {
      streamArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Kinesis Data Streams resource."];
      aggregationEnabled: Boolean.t option
        [@ocaml.doc
          "Enables QLDB to publish multiple data records in a single Kinesis Data Streams record, increasing the number of records sent per API call. This option is enabled by default. Record aggregation has important implications for processing records and requires de-aggregation in your stream consumer. To learn more, see KPL Key Concepts and Consumer De-aggregation in the Amazon Kinesis Data Streams Developer Guide."]}
    let context_ = "KinesisConfiguration"
    let make ?aggregationEnabled =
      fun ~streamArn -> fun () -> { aggregationEnabled; streamArn }
    let to_value x =
      structure_to_value
        [("StreamArn", (Some (Arn.to_value x.streamArn)));
        ("AggregationEnabled",
          (Option.map x.aggregationEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let aggregationEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "AggregationEnabled") in
      let streamArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "StreamArn") in
      make ?aggregationEnabled ~streamArn ()
    let of_json json =
      let aggregationEnabled =
        field_map json "AggregationEnabled" Boolean.of_json in
      let streamArn = field_map_exn json "StreamArn" Arn.of_json in
      make ?aggregationEnabled ~streamArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration settings of the Amazon Kinesis Data Streams destination for an Amazon QLDB journal stream."]
module StreamName =
  struct
    type nonrec t = string
    let context_ = "StreamName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:32) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(?!^.*--)(?!^[0-9]+$)(?!^-)(?!.*-$)^[A-Za-z0-9-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StreamName" j
    let to_json = simple_to_json to_value
  end
module StreamStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | COMPLETED 
      | CANCELED 
      | FAILED 
      | IMPAIRED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | COMPLETED -> "COMPLETED"
      | CANCELED -> "CANCELED"
      | FAILED -> "FAILED"
      | IMPAIRED -> "IMPAIRED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "COMPLETED" -> COMPLETED
      | "CANCELED" -> CANCELED
      | "FAILED" -> FAILED
      | "IMPAIRED" -> IMPAIRED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StreamStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StreamStatus" j)
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module ParameterName =
  struct
    type nonrec t = string
    let context_ = "ParameterName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ParameterName" j
    let to_json = simple_to_json to_value
  end
module EncryptionStatus =
  struct
    type nonrec t =
      | ENABLED 
      | UPDATING 
      | KMS_KEY_INACCESSIBLE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | UPDATING -> "UPDATING"
      | KMS_KEY_INACCESSIBLE -> "KMS_KEY_INACCESSIBLE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "UPDATING" -> UPDATING
      | "KMS_KEY_INACCESSIBLE" -> KMS_KEY_INACCESSIBLE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EncryptionStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EncryptionStatus" j)
    let to_json = simple_to_json to_value
  end
module ResourceName =
  struct
    type nonrec t = string
    let context_ = "ResourceName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceName" j
    let to_json = simple_to_json to_value
  end
module ResourceType =
  struct
    type nonrec t = string
    let context_ = "ResourceType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceType" j
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module LedgerSummary =
  struct
    type nonrec t =
      {
      name: LedgerName.t option [@ocaml.doc "The name of the ledger."];
      state: LedgerState.t option
        [@ocaml.doc "The current status of the ledger."];
      creationDateTime: Timestamp.t option
        [@ocaml.doc
          "The date and time, in epoch time format, when the ledger was created. (Epoch time format is the number of seconds elapsed since 12:00:00 AM January 1, 1970 UTC.)"]}
    let make ?name =
      fun ?state ->
        fun ?creationDateTime -> fun () -> { name; state; creationDateTime }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:LedgerName.to_value));
        ("State", (Option.map x.state ~f:LedgerState.to_value));
        ("CreationDateTime",
          (Option.map x.creationDateTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationDateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CreationDateTime") in
      let state =
        (Option.map ~f:LedgerState.of_xml) (Xml.child xml_arg0 "State") in
      let name =
        (Option.map ~f:LedgerName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?creationDateTime ?state ?name ()
    let of_json json =
      let creationDateTime =
        field_map json "CreationDateTime" Timestamp.of_json in
      let state = field_map json "State" LedgerState.of_json in
      let name = field_map json "Name" LedgerName.of_json in
      make ?creationDateTime ?state ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a ledger, including its name, state, and when it was created."]
module JournalS3ExportDescription =
  struct
    type nonrec t =
      {
      ledgerName: LedgerName.t [@ocaml.doc "The name of the ledger."];
      exportId: UniqueId.t
        [@ocaml.doc
          "The UUID (represented in Base62-encoded text) of the journal export job."];
      exportCreationTime: Timestamp.t
        [@ocaml.doc
          "The date and time, in epoch time format, when the export job was created. (Epoch time format is the number of seconds elapsed since 12:00:00 AM January 1, 1970 UTC.)"];
      status: ExportStatus.t
        [@ocaml.doc "The current state of the journal export job."];
      inclusiveStartTime: Timestamp.t
        [@ocaml.doc
          "The inclusive start date and time for the range of journal contents that was specified in the original export request."];
      exclusiveEndTime: Timestamp.t
        [@ocaml.doc
          "The exclusive end date and time for the range of journal contents that was specified in the original export request."];
      s3ExportConfiguration: S3ExportConfiguration.t ;
      roleArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM role that grants QLDB permissions for a journal export job to do the following: Write objects into your Amazon Simple Storage Service (Amazon S3) bucket. (Optional) Use your customer managed key in Key Management Service (KMS) for server-side encryption of your exported data."];
      outputFormat: OutputFormat.t option
        [@ocaml.doc "The output format of the exported journal data."]}
    let context_ = "JournalS3ExportDescription"
    let make ?outputFormat =
      fun ~ledgerName ->
        fun ~exportId ->
          fun ~exportCreationTime ->
            fun ~status ->
              fun ~inclusiveStartTime ->
                fun ~exclusiveEndTime ->
                  fun ~s3ExportConfiguration ->
                    fun ~roleArn ->
                      fun () ->
                        {
                          outputFormat;
                          ledgerName;
                          exportId;
                          exportCreationTime;
                          status;
                          inclusiveStartTime;
                          exclusiveEndTime;
                          s3ExportConfiguration;
                          roleArn
                        }
    let to_value x =
      structure_to_value
        [("LedgerName", (Some (LedgerName.to_value x.ledgerName)));
        ("ExportId", (Some (UniqueId.to_value x.exportId)));
        ("ExportCreationTime",
          (Some (Timestamp.to_value x.exportCreationTime)));
        ("Status", (Some (ExportStatus.to_value x.status)));
        ("InclusiveStartTime",
          (Some (Timestamp.to_value x.inclusiveStartTime)));
        ("ExclusiveEndTime", (Some (Timestamp.to_value x.exclusiveEndTime)));
        ("S3ExportConfiguration",
          (Some (S3ExportConfiguration.to_value x.s3ExportConfiguration)));
        ("RoleArn", (Some (Arn.to_value x.roleArn)));
        ("OutputFormat",
          (Option.map x.outputFormat ~f:OutputFormat.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputFormat =
        (Option.map ~f:OutputFormat.of_xml)
          (Xml.child xml_arg0 "OutputFormat") in
      let roleArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "RoleArn") in
      let s3ExportConfiguration =
        S3ExportConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "S3ExportConfiguration") in
      let exclusiveEndTime =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExclusiveEndTime") in
      let inclusiveStartTime =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InclusiveStartTime") in
      let status =
        ExportStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Status") in
      let exportCreationTime =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExportCreationTime") in
      let exportId =
        UniqueId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ExportId") in
      let ledgerName =
        LedgerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LedgerName") in
      make ?outputFormat ~roleArn ~s3ExportConfiguration ~exclusiveEndTime
        ~inclusiveStartTime ~status ~exportCreationTime ~exportId ~ledgerName
        ()
    let of_json json =
      let outputFormat = field_map json "OutputFormat" OutputFormat.of_json in
      let roleArn = field_map_exn json "RoleArn" Arn.of_json in
      let s3ExportConfiguration =
        field_map_exn json "S3ExportConfiguration"
          S3ExportConfiguration.of_json in
      let exclusiveEndTime =
        field_map_exn json "ExclusiveEndTime" Timestamp.of_json in
      let inclusiveStartTime =
        field_map_exn json "InclusiveStartTime" Timestamp.of_json in
      let status = field_map_exn json "Status" ExportStatus.of_json in
      let exportCreationTime =
        field_map_exn json "ExportCreationTime" Timestamp.of_json in
      let exportId = field_map_exn json "ExportId" UniqueId.of_json in
      let ledgerName = field_map_exn json "LedgerName" LedgerName.of_json in
      make ?outputFormat ~roleArn ~s3ExportConfiguration ~exclusiveEndTime
        ~inclusiveStartTime ~status ~exportCreationTime ~exportId ~ledgerName
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a journal export job, including the ledger name, export ID, creation time, current status, and the parameters of the original export creation request."]
module JournalKinesisStreamDescription =
  struct
    type nonrec t =
      {
      ledgerName: LedgerName.t [@ocaml.doc "The name of the ledger."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "The date and time, in epoch time format, when the QLDB journal stream was created. (Epoch time format is the number of seconds elapsed since 12:00:00 AM January 1, 1970 UTC.)"];
      inclusiveStartTime: Timestamp.t option
        [@ocaml.doc
          "The inclusive start date and time from which to start streaming journal data."];
      exclusiveEndTime: Timestamp.t option
        [@ocaml.doc
          "The exclusive date and time that specifies when the stream ends. If this parameter is undefined, the stream runs indefinitely until you cancel it."];
      roleArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM role that grants QLDB permissions for a journal stream to write data records to a Kinesis Data Streams resource."];
      streamId: UniqueId.t
        [@ocaml.doc
          "The UUID (represented in Base62-encoded text) of the QLDB journal stream."];
      arn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the QLDB journal stream."];
      status: StreamStatus.t
        [@ocaml.doc "The current state of the QLDB journal stream."];
      kinesisConfiguration: KinesisConfiguration.t
        [@ocaml.doc
          "The configuration settings of the Amazon Kinesis Data Streams destination for a QLDB journal stream."];
      errorCause: ErrorCause.t option
        [@ocaml.doc
          "The error message that describes the reason that a stream has a status of IMPAIRED or FAILED. This is not applicable to streams that have other status values."];
      streamName: StreamName.t
        [@ocaml.doc "The user-defined name of the QLDB journal stream."]}
    let context_ = "JournalKinesisStreamDescription"
    let make ?creationTime =
      fun ?inclusiveStartTime ->
        fun ?exclusiveEndTime ->
          fun ?arn ->
            fun ?errorCause ->
              fun ~ledgerName ->
                fun ~roleArn ->
                  fun ~streamId ->
                    fun ~status ->
                      fun ~kinesisConfiguration ->
                        fun ~streamName ->
                          fun () ->
                            {
                              creationTime;
                              inclusiveStartTime;
                              exclusiveEndTime;
                              arn;
                              errorCause;
                              ledgerName;
                              roleArn;
                              streamId;
                              status;
                              kinesisConfiguration;
                              streamName
                            }
    let to_value x =
      structure_to_value
        [("LedgerName", (Some (LedgerName.to_value x.ledgerName)));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("InclusiveStartTime",
          (Option.map x.inclusiveStartTime ~f:Timestamp.to_value));
        ("ExclusiveEndTime",
          (Option.map x.exclusiveEndTime ~f:Timestamp.to_value));
        ("RoleArn", (Some (Arn.to_value x.roleArn)));
        ("StreamId", (Some (UniqueId.to_value x.streamId)));
        ("Arn", (Option.map x.arn ~f:Arn.to_value));
        ("Status", (Some (StreamStatus.to_value x.status)));
        ("KinesisConfiguration",
          (Some (KinesisConfiguration.to_value x.kinesisConfiguration)));
        ("ErrorCause", (Option.map x.errorCause ~f:ErrorCause.to_value));
        ("StreamName", (Some (StreamName.to_value x.streamName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamName =
        StreamName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StreamName") in
      let errorCause =
        (Option.map ~f:ErrorCause.of_xml) (Xml.child xml_arg0 "ErrorCause") in
      let kinesisConfiguration =
        KinesisConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "KinesisConfiguration") in
      let status =
        StreamStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Status") in
      let arn = (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "Arn") in
      let streamId =
        UniqueId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "StreamId") in
      let roleArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "RoleArn") in
      let exclusiveEndTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "ExclusiveEndTime") in
      let inclusiveStartTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "InclusiveStartTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let ledgerName =
        LedgerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LedgerName") in
      make ~streamName ?errorCause ~kinesisConfiguration ~status ?arn
        ~streamId ~roleArn ?exclusiveEndTime ?inclusiveStartTime
        ?creationTime ~ledgerName ()
    let of_json json =
      let streamName = field_map_exn json "StreamName" StreamName.of_json in
      let errorCause = field_map json "ErrorCause" ErrorCause.of_json in
      let kinesisConfiguration =
        field_map_exn json "KinesisConfiguration"
          KinesisConfiguration.of_json in
      let status = field_map_exn json "Status" StreamStatus.of_json in
      let arn = field_map json "Arn" Arn.of_json in
      let streamId = field_map_exn json "StreamId" UniqueId.of_json in
      let roleArn = field_map_exn json "RoleArn" Arn.of_json in
      let exclusiveEndTime =
        field_map json "ExclusiveEndTime" Timestamp.of_json in
      let inclusiveStartTime =
        field_map json "InclusiveStartTime" Timestamp.of_json in
      let creationTime = field_map json "CreationTime" Timestamp.of_json in
      let ledgerName = field_map_exn json "LedgerName" LedgerName.of_json in
      make ~streamName ?errorCause ~kinesisConfiguration ~status ?arn
        ~streamId ~roleArn ?exclusiveEndTime ?inclusiveStartTime
        ?creationTime ~ledgerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about an Amazon QLDB journal stream, including the Amazon Resource Name (ARN), stream name, creation time, current status, and the parameters of the original stream creation request."]
module IonText =
  struct
    type nonrec t = string
    let context_ = "IonText"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1048576) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IonText" j
    let to_json = simple_to_json to_value
  end
module DeletionProtection =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module InvalidParameterException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      parameterName: ParameterName.t option
        [@ocaml.doc "The name of the invalid parameter."]}
    let make ?message =
      fun ?parameterName -> fun () -> { message; parameterName }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("ParameterName",
          (Option.map x.parameterName ~f:ParameterName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameterName =
        (Option.map ~f:ParameterName.of_xml)
          (Xml.child xml_arg0 "ParameterName") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?parameterName ?message ()
    let of_json json =
      let parameterName =
        field_map json "ParameterName" ParameterName.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?parameterName ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "One or more parameters in the request aren't valid."]
module LedgerEncryptionDescription =
  struct
    type nonrec t =
      {
      kmsKeyArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the customer managed KMS key that the ledger uses for encryption at rest. If this parameter is undefined, the ledger uses an Amazon Web Services owned KMS key for encryption."];
      encryptionStatus: EncryptionStatus.t
        [@ocaml.doc
          "The current state of encryption at rest for the ledger. This can be one of the following values: ENABLED: Encryption is fully enabled using the specified key. UPDATING: The ledger is actively processing the specified key change. Key changes in QLDB are asynchronous. The ledger is fully accessible without any performance impact while the key change is being processed. The amount of time it takes to update a key varies depending on the ledger size. KMS_KEY_INACCESSIBLE: The specified customer managed KMS key is not accessible, and the ledger is impaired. Either the key was disabled or deleted, or the grants on the key were revoked. When a ledger is impaired, it is not accessible and does not accept any read or write requests. An impaired ledger automatically returns to an active state after you restore the grants on the key, or re-enable the key that was disabled. However, deleting a customer managed KMS key is irreversible. After a key is deleted, you can no longer access the ledgers that are protected with that key, and the data becomes unrecoverable permanently."];
      inaccessibleKmsKeyDateTime: Timestamp.t option
        [@ocaml.doc
          "The date and time, in epoch time format, when the KMS key first became inaccessible, in the case of an error. (Epoch time format is the number of seconds that have elapsed since 12:00:00 AM January 1, 1970 UTC.) This parameter is undefined if the KMS key is accessible."]}
    let context_ = "LedgerEncryptionDescription"
    let make ?inaccessibleKmsKeyDateTime =
      fun ~kmsKeyArn ->
        fun ~encryptionStatus ->
          fun () ->
            { inaccessibleKmsKeyDateTime; kmsKeyArn; encryptionStatus }
    let to_value x =
      structure_to_value
        [("KmsKeyArn", (Some (Arn.to_value x.kmsKeyArn)));
        ("EncryptionStatus",
          (Some (EncryptionStatus.to_value x.encryptionStatus)));
        ("InaccessibleKmsKeyDateTime",
          (Option.map x.inaccessibleKmsKeyDateTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let inaccessibleKmsKeyDateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "InaccessibleKmsKeyDateTime") in
      let encryptionStatus =
        EncryptionStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EncryptionStatus") in
      let kmsKeyArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "KmsKeyArn") in
      make ?inaccessibleKmsKeyDateTime ~encryptionStatus ~kmsKeyArn ()
    let of_json json =
      let inaccessibleKmsKeyDateTime =
        field_map json "InaccessibleKmsKeyDateTime" Timestamp.of_json in
      let encryptionStatus =
        field_map_exn json "EncryptionStatus" EncryptionStatus.of_json in
      let kmsKeyArn = field_map_exn json "KmsKeyArn" Arn.of_json in
      make ?inaccessibleKmsKeyDateTime ~encryptionStatus ~kmsKeyArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the encryption of data at rest in an Amazon QLDB ledger. This includes the current status, the key in Key Management Service (KMS), and when the key became inaccessible (in the case of an error). For more information, see Encryption at rest in the Amazon QLDB Developer Guide."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      resourceType: ResourceType.t option
        [@ocaml.doc "The type of resource."];
      resourceName: ResourceName.t option
        [@ocaml.doc "The name of the resource."]}
    let make ?message =
      fun ?resourceType ->
        fun ?resourceName ->
          fun () -> { message; resourceType; resourceName }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("ResourceName",
          (Option.map x.resourceName ~f:ResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ResourceName") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceName ?resourceType ?message ()
    let of_json json =
      let resourceName = field_map json "ResourceName" ResourceName.of_json in
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?resourceName ?resourceType ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified resource doesn't exist."]
module KmsKey =
  struct
    type nonrec t = string
    let context_ = "KmsKey"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1600); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KmsKey" j
    let to_json = simple_to_json to_value
  end
module PermissionsMode =
  struct
    type nonrec t =
      | ALLOW_ALL 
      | STANDARD 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ALLOW_ALL -> "ALLOW_ALL"
      | STANDARD -> "STANDARD"
      | Non_static_id s -> s
    let of_string =
      function
      | "ALLOW_ALL" -> ALLOW_ALL
      | "STANDARD" -> STANDARD
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PermissionsMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PermissionsMode" j)
    let to_json = simple_to_json to_value
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module Tags =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourcePreconditionNotMetException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      resourceType: ResourceType.t option
        [@ocaml.doc "The type of resource."];
      resourceName: ResourceName.t option
        [@ocaml.doc "The name of the resource."]}
    let make ?message =
      fun ?resourceType ->
        fun ?resourceName ->
          fun () -> { message; resourceType; resourceName }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("ResourceName",
          (Option.map x.resourceName ~f:ResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ResourceName") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceName ?resourceType ?message ()
    let of_json json =
      let resourceName = field_map json "ResourceName" ResourceName.of_json in
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?resourceName ?resourceType ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The operation failed because a condition wasn't satisfied in advance."]
module LedgerList =
  struct
    type nonrec t = LedgerSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LedgerSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LedgerSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"LedgerList" ~of_json:LedgerSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:4) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"^[A-Za-z-0-9+/=]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module JournalS3ExportList =
  struct
    type nonrec t = JournalS3ExportDescription.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:JournalS3ExportDescription.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:JournalS3ExportDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"JournalS3ExportList"
        ~of_json:JournalS3ExportDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module JournalKinesisStreamDescriptionList =
  struct
    type nonrec t = JournalKinesisStreamDescription.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:JournalKinesisStreamDescription.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:JournalKinesisStreamDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"JournalKinesisStreamDescriptionList"
        ~of_json:JournalKinesisStreamDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValueHolder =
  struct
    type nonrec t =
      {
      ionText: IonText.t option
        [@ocaml.doc
          "An Amazon Ion plaintext value contained in a ValueHolder structure."]}
    let make ?ionText = fun () -> { ionText }
    let to_value x =
      structure_to_value
        [("IonText", (Option.map x.ionText ~f:IonText.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ionText =
        (Option.map ~f:IonText.of_xml) (Xml.child xml_arg0 "IonText") in
      make ?ionText ()
    let of_json json =
      let ionText = field_map json "IonText" IonText.of_json in
      make ?ionText ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that can contain a value in multiple encoding formats."]
module Digest =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module LimitExceededException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      resourceType: ResourceType.t option
        [@ocaml.doc "The type of resource."]}
    let make ?message =
      fun ?resourceType -> fun () -> { message; resourceType }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceType ?message ()
    let of_json json =
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?resourceType ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You have reached the limit on the maximum number of resources allowed."]
module ResourceAlreadyExistsException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      resourceType: ResourceType.t option
        [@ocaml.doc "The type of resource."];
      resourceName: ResourceName.t option
        [@ocaml.doc "The name of the resource."]}
    let make ?message =
      fun ?resourceType ->
        fun ?resourceName ->
          fun () -> { message; resourceType; resourceName }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("ResourceName",
          (Option.map x.resourceName ~f:ResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ResourceName") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceName ?resourceType ?message ()
    let of_json json =
      let resourceName = field_map json "ResourceName" ResourceName.of_json in
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?resourceName ?resourceType ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified resource already exists."]
module ResourceInUseException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      resourceType: ResourceType.t option
        [@ocaml.doc "The type of resource."];
      resourceName: ResourceName.t option
        [@ocaml.doc "The name of the resource."]}
    let make ?message =
      fun ?resourceType ->
        fun ?resourceName ->
          fun () -> { message; resourceType; resourceName }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("ResourceName",
          (Option.map x.resourceName ~f:ResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ResourceName") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceName ?resourceType ?message ()
    let of_json json =
      let resourceName = field_map json "ResourceName" ResourceName.of_json in
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?resourceName ?resourceType ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified resource can't be modified at this time."]
module UpdateLedgerResponse =
  struct
    type nonrec t =
      {
      name: LedgerName.t option [@ocaml.doc "The name of the ledger."];
      arn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) for the ledger."];
      state: LedgerState.t option
        [@ocaml.doc "The current status of the ledger."];
      creationDateTime: Timestamp.t option
        [@ocaml.doc
          "The date and time, in epoch time format, when the ledger was created. (Epoch time format is the number of seconds elapsed since 12:00:00 AM January 1, 1970 UTC.)"];
      deletionProtection: DeletionProtection.t option
        [@ocaml.doc
          "The flag that prevents a ledger from being deleted by any user. If not provided on ledger creation, this feature is enabled (true) by default. If deletion protection is enabled, you must first disable it before you can delete the ledger. You can disable it by calling the UpdateLedger operation to set the flag to false."];
      encryptionDescription: LedgerEncryptionDescription.t option
        [@ocaml.doc
          "Information about the encryption of data at rest in the ledger. This includes the current status, the KMS key, and when the key became inaccessible (in the case of an error)."]}
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?name =
      fun ?arn ->
        fun ?state ->
          fun ?creationDateTime ->
            fun ?deletionProtection ->
              fun ?encryptionDescription ->
                fun () ->
                  {
                    name;
                    arn;
                    state;
                    creationDateTime;
                    deletionProtection;
                    encryptionDescription
                  }
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:LedgerName.to_value));
        ("Arn", (Option.map x.arn ~f:Arn.to_value));
        ("State", (Option.map x.state ~f:LedgerState.to_value));
        ("CreationDateTime",
          (Option.map x.creationDateTime ~f:Timestamp.to_value));
        ("DeletionProtection",
          (Option.map x.deletionProtection ~f:DeletionProtection.to_value));
        ("EncryptionDescription",
          (Option.map x.encryptionDescription
             ~f:LedgerEncryptionDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionDescription =
        (Option.map ~f:LedgerEncryptionDescription.of_xml)
          (Xml.child xml_arg0 "EncryptionDescription") in
      let deletionProtection =
        (Option.map ~f:DeletionProtection.of_xml)
          (Xml.child xml_arg0 "DeletionProtection") in
      let creationDateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CreationDateTime") in
      let state =
        (Option.map ~f:LedgerState.of_xml) (Xml.child xml_arg0 "State") in
      let arn = (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "Arn") in
      let name =
        (Option.map ~f:LedgerName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?encryptionDescription ?deletionProtection ?creationDateTime
        ?state ?arn ?name ()
    let of_json json =
      let encryptionDescription =
        field_map json "EncryptionDescription"
          LedgerEncryptionDescription.of_json in
      let deletionProtection =
        field_map json "DeletionProtection" DeletionProtection.of_json in
      let creationDateTime =
        field_map json "CreationDateTime" Timestamp.of_json in
      let state = field_map json "State" LedgerState.of_json in
      let arn = field_map json "Arn" Arn.of_json in
      let name = field_map json "Name" LedgerName.of_json in
      make ?encryptionDescription ?deletionProtection ?creationDateTime
        ?state ?arn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates properties on a ledger."]
module UpdateLedgerRequest =
  struct
    type nonrec t =
      {
      name: LedgerName.t [@ocaml.doc "The name of the ledger."];
      deletionProtection: DeletionProtection.t option
        [@ocaml.doc
          "The flag that prevents a ledger from being deleted by any user. If not provided on ledger creation, this feature is enabled (true) by default. If deletion protection is enabled, you must first disable it before you can delete the ledger. You can disable it by calling the UpdateLedger operation to set the flag to false."];
      kmsKey: KmsKey.t option
        [@ocaml.doc
          "The key in Key Management Service (KMS) to use for encryption of data at rest in the ledger. For more information, see Encryption at rest in the Amazon QLDB Developer Guide. Use one of the following options to specify this parameter: AWS_OWNED_KMS_KEY: Use an KMS key that is owned and managed by Amazon Web Services on your behalf. Undefined: Make no changes to the KMS key of the ledger. A valid symmetric customer managed KMS key: Use the specified KMS key in your account that you create, own, and manage. Amazon QLDB does not support asymmetric keys. For more information, see Using symmetric and asymmetric keys in the Key Management Service Developer Guide. To specify a customer managed KMS key, you can use its key ID, Amazon Resource Name (ARN), alias name, or alias ARN. When using an alias name, prefix it with \"alias/\". To specify a key in a different Amazon Web Services account, you must use the key ARN or alias ARN. For example: Key ID: 1234abcd-12ab-34cd-56ef-1234567890ab Key ARN: arn:aws:kms:us-east-2:111122223333:key/1234abcd-12ab-34cd-56ef-1234567890ab Alias name: alias/ExampleAlias Alias ARN: arn:aws:kms:us-east-2:111122223333:alias/ExampleAlias For more information, see Key identifiers (KeyId) in the Key Management Service Developer Guide."]}
    let context_ = "UpdateLedgerRequest"
    let make ?deletionProtection =
      fun ?kmsKey ->
        fun ~name -> fun () -> { deletionProtection; kmsKey; name }
    let to_value x =
      structure_to_value
        [("name", (Some (LedgerName.to_value x.name)));
        ("DeletionProtection",
          (Option.map x.deletionProtection ~f:DeletionProtection.to_value));
        ("KmsKey", (Option.map x.kmsKey ~f:KmsKey.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKey =
        (Option.map ~f:KmsKey.of_xml) (Xml.child xml_arg0 "KmsKey") in
      let deletionProtection =
        (Option.map ~f:DeletionProtection.of_xml)
          (Xml.child xml_arg0 "DeletionProtection") in
      let name =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?kmsKey ?deletionProtection ~name ()
    let of_json json =
      let kmsKey = field_map json "KmsKey" KmsKey.of_json in
      let deletionProtection =
        field_map json "DeletionProtection" DeletionProtection.of_json in
      let name = field_map_exn json "Name" LedgerName.of_json in
      make ?kmsKey ?deletionProtection ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates properties on a ledger."]
module UpdateLedgerPermissionsModeResponse =
  struct
    type nonrec t =
      {
      name: LedgerName.t option [@ocaml.doc "The name of the ledger."];
      arn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) for the ledger."];
      permissionsMode: PermissionsMode.t option
        [@ocaml.doc "The current permissions mode of the ledger."]}
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?name =
      fun ?arn ->
        fun ?permissionsMode -> fun () -> { name; arn; permissionsMode }
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:LedgerName.to_value));
        ("Arn", (Option.map x.arn ~f:Arn.to_value));
        ("PermissionsMode",
          (Option.map x.permissionsMode ~f:PermissionsMode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let permissionsMode =
        (Option.map ~f:PermissionsMode.of_xml)
          (Xml.child xml_arg0 "PermissionsMode") in
      let arn = (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "Arn") in
      let name =
        (Option.map ~f:LedgerName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?permissionsMode ?arn ?name ()
    let of_json json =
      let permissionsMode =
        field_map json "PermissionsMode" PermissionsMode.of_json in
      let arn = field_map json "Arn" Arn.of_json in
      let name = field_map json "Name" LedgerName.of_json in
      make ?permissionsMode ?arn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the permissions mode of a ledger. Before you switch to the STANDARD permissions mode, you must first create all required IAM policies and table tags to avoid disruption to your users. To learn more, see Migrating to the standard permissions mode in the Amazon QLDB Developer Guide."]
module UpdateLedgerPermissionsModeRequest =
  struct
    type nonrec t =
      {
      name: LedgerName.t [@ocaml.doc "The name of the ledger."];
      permissionsMode: PermissionsMode.t
        [@ocaml.doc
          "The permissions mode to assign to the ledger. This parameter can have one of the following values: ALLOW_ALL: A legacy permissions mode that enables access control with API-level granularity for ledgers. This mode allows users who have the SendCommand API permission for this ledger to run all PartiQL commands (hence, ALLOW_ALL) on any tables in the specified ledger. This mode disregards any table-level or command-level IAM permissions policies that you create for the ledger. STANDARD: (Recommended) A permissions mode that enables access control with finer granularity for ledgers, tables, and PartiQL commands. By default, this mode denies all user requests to run any PartiQL commands on any tables in this ledger. To allow PartiQL commands to run, you must create IAM permissions policies for specific table resources and PartiQL actions, in addition to the SendCommand API permission for the ledger. For information, see Getting started with the standard permissions mode in the Amazon QLDB Developer Guide. We strongly recommend using the STANDARD permissions mode to maximize the security of your ledger data."]}
    let context_ = "UpdateLedgerPermissionsModeRequest"
    let make ~name =
      fun ~permissionsMode -> fun () -> { name; permissionsMode }
    let to_value x =
      structure_to_value
        [("name", (Some (LedgerName.to_value x.name)));
        ("PermissionsMode",
          (Some (PermissionsMode.to_value x.permissionsMode)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let permissionsMode =
        PermissionsMode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PermissionsMode") in
      let name =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~permissionsMode ~name ()
    let of_json json =
      let permissionsMode =
        field_map_exn json "PermissionsMode" PermissionsMode.of_json in
      let name = field_map_exn json "Name" LedgerName.of_json in
      make ~permissionsMode ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the permissions mode of a ledger. Before you switch to the STANDARD permissions mode, you must first create all required IAM policies and table tags to avoid disruption to your users. To learn more, see Migrating to the standard permissions mode in the Amazon QLDB Developer Guide."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes one or more tags from a specified Amazon QLDB resource. You can specify up to 50 tag keys to remove."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) from which to remove the tags. For example: arn:aws:qldb:us-east-1:123456789012:ledger/exampleLedger"];
      tagKeys: TagKeyList.t [@ocaml.doc "The list of tag keys to remove."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_json json =
      let tagKeys = field_map_exn json "TagKeys" TagKeyList.of_json in
      let resourceArn = field_map_exn json "ResourceArn" Arn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes one or more tags from a specified Amazon QLDB resource. You can specify up to 50 tag keys to remove."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds one or more tags to a specified Amazon QLDB resource. A resource can have up to 50 tags. If you try to create more than 50 tags for a resource, your request fails and returns an error."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) to which you want to add the tags. For example: arn:aws:qldb:us-east-1:123456789012:ledger/exampleLedger"];
      tags: Tags.t
        [@ocaml.doc
          "The key-value pairs to add as tags to the specified QLDB resource. Tag keys are case sensitive. If you specify a key that already exists for the resource, your request fails and returns an error. Tag values are case sensitive and can be null."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)));
        ("Tags", (Some (Tags.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        Tags.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_json json =
      let tags = field_map_exn json "Tags" Tags.of_json in
      let resourceArn = field_map_exn json "ResourceArn" Arn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds one or more tags to a specified Amazon QLDB resource. A resource can have up to 50 tags. If you try to create more than 50 tags for a resource, your request fails and returns an error."]
module StreamJournalToKinesisResponse =
  struct
    type nonrec t =
      {
      streamId: UniqueId.t option
        [@ocaml.doc
          "The UUID (represented in Base62-encoded text) that QLDB assigns to each QLDB journal stream."]}
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ResourcePreconditionNotMetException of
          ResourcePreconditionNotMetException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?streamId = fun () -> { streamId }
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ResourcePreconditionNotMetException e ->
          `Assoc
            [("error", (`String "ResourcePreconditionNotMetException"));
            ("details", (ResourcePreconditionNotMetException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("StreamId", (Option.map x.streamId ~f:UniqueId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamId =
        (Option.map ~f:UniqueId.of_xml) (Xml.child xml_arg0 "StreamId") in
      make ?streamId ()
    let of_json json =
      let streamId = field_map json "StreamId" UniqueId.of_json in
      make ?streamId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a journal stream for a given Amazon QLDB ledger. The stream captures every document revision that is committed to the ledger's journal and delivers the data to a specified Amazon Kinesis Data Streams resource."]
module StreamJournalToKinesisRequest =
  struct
    type nonrec t =
      {
      ledgerName: LedgerName.t [@ocaml.doc "The name of the ledger."];
      roleArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM role that grants QLDB permissions for a journal stream to write data records to a Kinesis Data Streams resource. To pass a role to QLDB when requesting a journal stream, you must have permissions to perform the iam:PassRole action on the IAM role resource. This is required for all journal stream requests."];
      tags: Tags.t option
        [@ocaml.doc
          "The key-value pairs to add as tags to the stream that you want to create. Tag keys are case sensitive. Tag values are case sensitive and can be null."];
      inclusiveStartTime: Timestamp.t
        [@ocaml.doc
          "The inclusive start date and time from which to start streaming journal data. This parameter must be in ISO 8601 date and time format and in Universal Coordinated Time (UTC). For example: 2019-06-13T21:36:34Z. The InclusiveStartTime cannot be in the future and must be before ExclusiveEndTime. If you provide an InclusiveStartTime that is before the ledger's CreationDateTime, QLDB effectively defaults it to the ledger's CreationDateTime."];
      exclusiveEndTime: Timestamp.t option
        [@ocaml.doc
          "The exclusive date and time that specifies when the stream ends. If you don't define this parameter, the stream runs indefinitely until you cancel it. The ExclusiveEndTime must be in ISO 8601 date and time format and in Universal Coordinated Time (UTC). For example: 2019-06-13T21:36:34Z."];
      kinesisConfiguration: KinesisConfiguration.t
        [@ocaml.doc
          "The configuration settings of the Kinesis Data Streams destination for your stream request."];
      streamName: StreamName.t
        [@ocaml.doc
          "The name that you want to assign to the QLDB journal stream. User-defined names can help identify and indicate the purpose of a stream. Your stream name must be unique among other active streams for a given ledger. Stream names have the same naming constraints as ledger names, as defined in Quotas in Amazon QLDB in the Amazon QLDB Developer Guide."]}
    let context_ = "StreamJournalToKinesisRequest"
    let make ?tags =
      fun ?exclusiveEndTime ->
        fun ~ledgerName ->
          fun ~roleArn ->
            fun ~inclusiveStartTime ->
              fun ~kinesisConfiguration ->
                fun ~streamName ->
                  fun () ->
                    {
                      tags;
                      exclusiveEndTime;
                      ledgerName;
                      roleArn;
                      inclusiveStartTime;
                      kinesisConfiguration;
                      streamName
                    }
    let to_value x =
      structure_to_value
        [("name", (Some (LedgerName.to_value x.ledgerName)));
        ("RoleArn", (Some (Arn.to_value x.roleArn)));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("InclusiveStartTime",
          (Some (Timestamp.to_value x.inclusiveStartTime)));
        ("ExclusiveEndTime",
          (Option.map x.exclusiveEndTime ~f:Timestamp.to_value));
        ("KinesisConfiguration",
          (Some (KinesisConfiguration.to_value x.kinesisConfiguration)));
        ("StreamName", (Some (StreamName.to_value x.streamName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamName =
        StreamName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StreamName") in
      let kinesisConfiguration =
        KinesisConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "KinesisConfiguration") in
      let exclusiveEndTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "ExclusiveEndTime") in
      let inclusiveStartTime =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InclusiveStartTime") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let roleArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "RoleArn") in
      let ledgerName =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~streamName ~kinesisConfiguration ?exclusiveEndTime
        ~inclusiveStartTime ?tags ~roleArn ~ledgerName ()
    let of_json json =
      let streamName = field_map_exn json "StreamName" StreamName.of_json in
      let kinesisConfiguration =
        field_map_exn json "KinesisConfiguration"
          KinesisConfiguration.of_json in
      let exclusiveEndTime =
        field_map json "ExclusiveEndTime" Timestamp.of_json in
      let inclusiveStartTime =
        field_map_exn json "InclusiveStartTime" Timestamp.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let roleArn = field_map_exn json "RoleArn" Arn.of_json in
      let ledgerName = field_map_exn json "LedgerName" LedgerName.of_json in
      make ~streamName ~kinesisConfiguration ?exclusiveEndTime
        ~inclusiveStartTime ?tags ~roleArn ~ledgerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a journal stream for a given Amazon QLDB ledger. The stream captures every document revision that is committed to the ledger's journal and delivers the data to a specified Amazon Kinesis Data Streams resource."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: Tags.t option
        [@ocaml.doc
          "The tags that are currently associated with the specified Amazon QLDB resource."]}
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?tags ()
    let of_json json =
      let tags = field_map json "Tags" Tags.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns all tags for a specified Amazon QLDB resource."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) for which to list the tags. For example: arn:aws:qldb:us-east-1:123456789012:ledger/exampleLedger"]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_json json =
      let resourceArn = field_map_exn json "ResourceArn" Arn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns all tags for a specified Amazon QLDB resource."]
module ListLedgersResponse =
  struct
    type nonrec t =
      {
      ledgers: LedgerList.t option
        [@ocaml.doc
          "The array of ledger summaries that are associated with the current Amazon Web Services account and Region."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token, indicating whether there are more results available: If NextToken is empty, then the last page of results has been processed and there are no more results to be retrieved. If NextToken is not empty, then there are more results available. To retrieve the next page of results, use the value of NextToken in a subsequent ListLedgers call."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?ledgers = fun ?nextToken -> fun () -> { ledgers; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Ledgers", (Option.map x.ledgers ~f:LedgerList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let ledgers =
        (Option.map ~f:LedgerList.of_xml) (Xml.child xml_arg0 "Ledgers") in
      make ?nextToken ?ledgers ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let ledgers = field_map json "Ledgers" LedgerList.of_json in
      make ?nextToken ?ledgers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns an array of ledger summaries that are associated with the current Amazon Web Services account and Region. This action returns a maximum of 100 items and is paginated so that you can retrieve all the items by calling ListLedgers multiple times."]
module ListLedgersRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in a single ListLedgers request. (The actual number of results returned might be fewer.)"];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token, indicating that you want to retrieve the next page of results. If you received a value for NextToken in the response from a previous ListLedgers call, then you should use that value as input here."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("max_results", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("next_token", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "next_token") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "max_results") in
      make ?nextToken ?maxResults ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns an array of ledger summaries that are associated with the current Amazon Web Services account and Region. This action returns a maximum of 100 items and is paginated so that you can retrieve all the items by calling ListLedgers multiple times."]
module ListJournalS3ExportsResponse =
  struct
    type nonrec t =
      {
      journalS3Exports: JournalS3ExportList.t option
        [@ocaml.doc
          "The array of journal export job descriptions for all ledgers that are associated with the current Amazon Web Services account and Region."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If NextToken is empty, then the last page of results has been processed and there are no more results to be retrieved. If NextToken is not empty, then there are more results available. To retrieve the next page of results, use the value of NextToken in a subsequent ListJournalS3Exports call."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?journalS3Exports =
      fun ?nextToken -> fun () -> { journalS3Exports; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("JournalS3Exports",
           (Option.map x.journalS3Exports ~f:JournalS3ExportList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let journalS3Exports =
        (Option.map ~f:JournalS3ExportList.of_xml)
          (Xml.child xml_arg0 "JournalS3Exports") in
      make ?nextToken ?journalS3Exports ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let journalS3Exports =
        field_map json "JournalS3Exports" JournalS3ExportList.of_json in
      make ?nextToken ?journalS3Exports ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns an array of journal export job descriptions for all ledgers that are associated with the current Amazon Web Services account and Region. This action returns a maximum of MaxResults items, and is paginated so that you can retrieve all the items by calling ListJournalS3Exports multiple times. This action does not return any expired export jobs. For more information, see Export job expiration in the Amazon QLDB Developer Guide."]
module ListJournalS3ExportsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in a single ListJournalS3Exports request. (The actual number of results returned might be fewer.)"];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token, indicating that you want to retrieve the next page of results. If you received a value for NextToken in the response from a previous ListJournalS3Exports call, then you should use that value as input here."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("max_results", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("next_token", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "next_token") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "max_results") in
      make ?nextToken ?maxResults ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns an array of journal export job descriptions for all ledgers that are associated with the current Amazon Web Services account and Region. This action returns a maximum of MaxResults items, and is paginated so that you can retrieve all the items by calling ListJournalS3Exports multiple times. This action does not return any expired export jobs. For more information, see Export job expiration in the Amazon QLDB Developer Guide."]
module ListJournalS3ExportsForLedgerResponse =
  struct
    type nonrec t =
      {
      journalS3Exports: JournalS3ExportList.t option
        [@ocaml.doc
          "The array of journal export job descriptions that are associated with the specified ledger."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If NextToken is empty, then the last page of results has been processed and there are no more results to be retrieved. If NextToken is not empty, then there are more results available. To retrieve the next page of results, use the value of NextToken in a subsequent ListJournalS3ExportsForLedger call."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?journalS3Exports =
      fun ?nextToken -> fun () -> { journalS3Exports; nextToken }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("JournalS3Exports",
           (Option.map x.journalS3Exports ~f:JournalS3ExportList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let journalS3Exports =
        (Option.map ~f:JournalS3ExportList.of_xml)
          (Xml.child xml_arg0 "JournalS3Exports") in
      make ?nextToken ?journalS3Exports ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let journalS3Exports =
        field_map json "JournalS3Exports" JournalS3ExportList.of_json in
      make ?nextToken ?journalS3Exports ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns an array of journal export job descriptions for a specified ledger. This action returns a maximum of MaxResults items, and is paginated so that you can retrieve all the items by calling ListJournalS3ExportsForLedger multiple times. This action does not return any expired export jobs. For more information, see Export job expiration in the Amazon QLDB Developer Guide."]
module ListJournalS3ExportsForLedgerRequest =
  struct
    type nonrec t =
      {
      name: LedgerName.t [@ocaml.doc "The name of the ledger."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in a single ListJournalS3ExportsForLedger request. (The actual number of results returned might be fewer.)"];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token, indicating that you want to retrieve the next page of results. If you received a value for NextToken in the response from a previous ListJournalS3ExportsForLedger call, then you should use that value as input here."]}
    let context_ = "ListJournalS3ExportsForLedgerRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~name -> fun () -> { maxResults; nextToken; name }
    let to_value x =
      structure_to_value
        [("name", (Some (LedgerName.to_value x.name)));
        ("max_results", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("next_token", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "next_token") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "max_results") in
      let name =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?nextToken ?maxResults ~name ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let name = field_map_exn json "Name" LedgerName.of_json in
      make ?nextToken ?maxResults ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns an array of journal export job descriptions for a specified ledger. This action returns a maximum of MaxResults items, and is paginated so that you can retrieve all the items by calling ListJournalS3ExportsForLedger multiple times. This action does not return any expired export jobs. For more information, see Export job expiration in the Amazon QLDB Developer Guide."]
module ListJournalKinesisStreamsForLedgerResponse =
  struct
    type nonrec t =
      {
      streams: JournalKinesisStreamDescriptionList.t option
        [@ocaml.doc
          "The array of QLDB journal stream descriptors that are associated with the given ledger."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If NextToken is empty, the last page of results has been processed and there are no more results to be retrieved. If NextToken is not empty, more results are available. To retrieve the next page of results, use the value of NextToken in a subsequent ListJournalKinesisStreamsForLedger call."]}
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ResourcePreconditionNotMetException of
          ResourcePreconditionNotMetException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?streams = fun ?nextToken -> fun () -> { streams; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ResourcePreconditionNotMetException e ->
          `Assoc
            [("error", (`String "ResourcePreconditionNotMetException"));
            ("details", (ResourcePreconditionNotMetException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Streams",
           (Option.map x.streams
              ~f:JournalKinesisStreamDescriptionList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let streams =
        (Option.map ~f:JournalKinesisStreamDescriptionList.of_xml)
          (Xml.child xml_arg0 "Streams") in
      make ?nextToken ?streams ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let streams =
        field_map json "Streams" JournalKinesisStreamDescriptionList.of_json in
      make ?nextToken ?streams ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns an array of all Amazon QLDB journal stream descriptors for a given ledger. The output of each stream descriptor includes the same details that are returned by DescribeJournalKinesisStream. This action does not return any expired journal streams. For more information, see Expiration for terminal streams in the Amazon QLDB Developer Guide. This action returns a maximum of MaxResults items. It is paginated so that you can retrieve all the items by calling ListJournalKinesisStreamsForLedger multiple times."]
module ListJournalKinesisStreamsForLedgerRequest =
  struct
    type nonrec t =
      {
      ledgerName: LedgerName.t [@ocaml.doc "The name of the ledger."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in a single ListJournalKinesisStreamsForLedger request. (The actual number of results returned might be fewer.)"];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token, indicating that you want to retrieve the next page of results. If you received a value for NextToken in the response from a previous ListJournalKinesisStreamsForLedger call, you should use that value as input here."]}
    let context_ = "ListJournalKinesisStreamsForLedgerRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~ledgerName -> fun () -> { maxResults; nextToken; ledgerName }
    let to_value x =
      structure_to_value
        [("name", (Some (LedgerName.to_value x.ledgerName)));
        ("max_results", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("next_token", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "next_token") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "max_results") in
      let ledgerName =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?nextToken ?maxResults ~ledgerName ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let ledgerName = field_map_exn json "LedgerName" LedgerName.of_json in
      make ?nextToken ?maxResults ~ledgerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns an array of all Amazon QLDB journal stream descriptors for a given ledger. The output of each stream descriptor includes the same details that are returned by DescribeJournalKinesisStream. This action does not return any expired journal streams. For more information, see Expiration for terminal streams in the Amazon QLDB Developer Guide. This action returns a maximum of MaxResults items. It is paginated so that you can retrieve all the items by calling ListJournalKinesisStreamsForLedger multiple times."]
module GetRevisionResponse =
  struct
    type nonrec t =
      {
      proof: ValueHolder.t option
        [@ocaml.doc
          "The proof object in Amazon Ion format returned by a GetRevision request. A proof contains the list of hash values that are required to recalculate the specified digest using a Merkle tree, starting with the specified document revision."];
      revision: ValueHolder.t
        [@ocaml.doc
          "The document revision data object in Amazon Ion format."]}
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ResourcePreconditionNotMetException of
          ResourcePreconditionNotMetException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetRevisionResponse"
    let make ?proof = fun ~revision -> fun () -> { proof; revision }
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ResourcePreconditionNotMetException e ->
          `Assoc
            [("error", (`String "ResourcePreconditionNotMetException"));
            ("details", (ResourcePreconditionNotMetException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Proof", (Option.map x.proof ~f:ValueHolder.to_value));
        ("Revision", (Some (ValueHolder.to_value x.revision)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let revision =
        ValueHolder.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Revision") in
      let proof =
        (Option.map ~f:ValueHolder.of_xml) (Xml.child xml_arg0 "Proof") in
      make ~revision ?proof ()
    let of_json json =
      let revision = field_map_exn json "Revision" ValueHolder.of_json in
      let proof = field_map json "Proof" ValueHolder.of_json in
      make ~revision ?proof ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a revision data object for a specified document ID and block address. Also returns a proof of the specified revision for verification if DigestTipAddress is provided."]
module GetRevisionRequest =
  struct
    type nonrec t =
      {
      name: LedgerName.t [@ocaml.doc "The name of the ledger."];
      blockAddress: ValueHolder.t
        [@ocaml.doc
          "The block location of the document revision to be verified. An address is an Amazon Ion structure that has two fields: strandId and sequenceNo. For example: \\{strandId:\"BlFTjlSXze9BIh1KOszcE3\",sequenceNo:14\\}."];
      documentId: UniqueId.t
        [@ocaml.doc
          "The UUID (represented in Base62-encoded text) of the document to be verified."];
      digestTipAddress: ValueHolder.t option
        [@ocaml.doc
          "The latest block location covered by the digest for which to request a proof. An address is an Amazon Ion structure that has two fields: strandId and sequenceNo. For example: \\{strandId:\"BlFTjlSXze9BIh1KOszcE3\",sequenceNo:49\\}."]}
    let context_ = "GetRevisionRequest"
    let make ?digestTipAddress =
      fun ~name ->
        fun ~blockAddress ->
          fun ~documentId ->
            fun () -> { digestTipAddress; name; blockAddress; documentId }
    let to_value x =
      structure_to_value
        [("name", (Some (LedgerName.to_value x.name)));
        ("BlockAddress", (Some (ValueHolder.to_value x.blockAddress)));
        ("DocumentId", (Some (UniqueId.to_value x.documentId)));
        ("DigestTipAddress",
          (Option.map x.digestTipAddress ~f:ValueHolder.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let digestTipAddress =
        (Option.map ~f:ValueHolder.of_xml)
          (Xml.child xml_arg0 "DigestTipAddress") in
      let documentId =
        UniqueId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DocumentId") in
      let blockAddress =
        ValueHolder.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BlockAddress") in
      let name =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?digestTipAddress ~documentId ~blockAddress ~name ()
    let of_json json =
      let digestTipAddress =
        field_map json "DigestTipAddress" ValueHolder.of_json in
      let documentId = field_map_exn json "DocumentId" UniqueId.of_json in
      let blockAddress =
        field_map_exn json "BlockAddress" ValueHolder.of_json in
      let name = field_map_exn json "Name" LedgerName.of_json in
      make ?digestTipAddress ~documentId ~blockAddress ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a revision data object for a specified document ID and block address. Also returns a proof of the specified revision for verification if DigestTipAddress is provided."]
module GetDigestResponse =
  struct
    type nonrec t =
      {
      digest: Digest.t
        [@ocaml.doc
          "The 256-bit hash value representing the digest returned by a GetDigest request."];
      digestTipAddress: ValueHolder.t
        [@ocaml.doc
          "The latest block location covered by the digest that you requested. An address is an Amazon Ion structure that has two fields: strandId and sequenceNo."]}
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ResourcePreconditionNotMetException of
          ResourcePreconditionNotMetException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetDigestResponse"
    let make ~digest =
      fun ~digestTipAddress -> fun () -> { digest; digestTipAddress }
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ResourcePreconditionNotMetException e ->
          `Assoc
            [("error", (`String "ResourcePreconditionNotMetException"));
            ("details", (ResourcePreconditionNotMetException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Digest", (Some (Digest.to_value x.digest)));
        ("DigestTipAddress",
          (Some (ValueHolder.to_value x.digestTipAddress)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let digestTipAddress =
        ValueHolder.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DigestTipAddress") in
      let digest =
        Digest.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Digest") in
      make ~digestTipAddress ~digest ()
    let of_json json =
      let digestTipAddress =
        field_map_exn json "DigestTipAddress" ValueHolder.of_json in
      let digest = field_map_exn json "Digest" Digest.of_json in
      make ~digestTipAddress ~digest ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the digest of a ledger at the latest committed block in the journal. The response includes a 256-bit hash value and a block address."]
module GetDigestRequest =
  struct
    type nonrec t =
      {
      name: LedgerName.t [@ocaml.doc "The name of the ledger."]}
    let context_ = "GetDigestRequest"
    let make ~name = fun () -> { name }
    let to_value x =
      structure_to_value [("name", (Some (LedgerName.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~name ()
    let of_json json =
      let name = field_map_exn json "Name" LedgerName.of_json in
      make ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the digest of a ledger at the latest committed block in the journal. The response includes a 256-bit hash value and a block address."]
module GetBlockResponse =
  struct
    type nonrec t =
      {
      block: ValueHolder.t
        [@ocaml.doc "The block data object in Amazon Ion format."];
      proof: ValueHolder.t option
        [@ocaml.doc
          "The proof object in Amazon Ion format returned by a GetBlock request. A proof contains the list of hash values required to recalculate the specified digest using a Merkle tree, starting with the specified block."]}
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ResourcePreconditionNotMetException of
          ResourcePreconditionNotMetException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetBlockResponse"
    let make ?proof = fun ~block -> fun () -> { proof; block }
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ResourcePreconditionNotMetException e ->
          `Assoc
            [("error", (`String "ResourcePreconditionNotMetException"));
            ("details", (ResourcePreconditionNotMetException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Block", (Some (ValueHolder.to_value x.block)));
        ("Proof", (Option.map x.proof ~f:ValueHolder.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let proof =
        (Option.map ~f:ValueHolder.of_xml) (Xml.child xml_arg0 "Proof") in
      let block =
        ValueHolder.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Block") in
      make ?proof ~block ()
    let of_json json =
      let proof = field_map json "Proof" ValueHolder.of_json in
      let block = field_map_exn json "Block" ValueHolder.of_json in
      make ?proof ~block ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a block object at a specified address in a journal. Also returns a proof of the specified block for verification if DigestTipAddress is provided. For information about the data contents in a block, see Journal contents in the Amazon QLDB Developer Guide. If the specified ledger doesn't exist or is in DELETING status, then throws ResourceNotFoundException. If the specified ledger is in CREATING status, then throws ResourcePreconditionNotMetException. If no block exists with the specified address, then throws InvalidParameterException."]
module GetBlockRequest =
  struct
    type nonrec t =
      {
      name: LedgerName.t [@ocaml.doc "The name of the ledger."];
      blockAddress: ValueHolder.t
        [@ocaml.doc
          "The location of the block that you want to request. An address is an Amazon Ion structure that has two fields: strandId and sequenceNo. For example: \\{strandId:\"BlFTjlSXze9BIh1KOszcE3\",sequenceNo:14\\}."];
      digestTipAddress: ValueHolder.t option
        [@ocaml.doc
          "The latest block location covered by the digest for which to request a proof. An address is an Amazon Ion structure that has two fields: strandId and sequenceNo. For example: \\{strandId:\"BlFTjlSXze9BIh1KOszcE3\",sequenceNo:49\\}."]}
    let context_ = "GetBlockRequest"
    let make ?digestTipAddress =
      fun ~name ->
        fun ~blockAddress ->
          fun () -> { digestTipAddress; name; blockAddress }
    let to_value x =
      structure_to_value
        [("name", (Some (LedgerName.to_value x.name)));
        ("BlockAddress", (Some (ValueHolder.to_value x.blockAddress)));
        ("DigestTipAddress",
          (Option.map x.digestTipAddress ~f:ValueHolder.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let digestTipAddress =
        (Option.map ~f:ValueHolder.of_xml)
          (Xml.child xml_arg0 "DigestTipAddress") in
      let blockAddress =
        ValueHolder.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BlockAddress") in
      let name =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?digestTipAddress ~blockAddress ~name ()
    let of_json json =
      let digestTipAddress =
        field_map json "DigestTipAddress" ValueHolder.of_json in
      let blockAddress =
        field_map_exn json "BlockAddress" ValueHolder.of_json in
      let name = field_map_exn json "Name" LedgerName.of_json in
      make ?digestTipAddress ~blockAddress ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a block object at a specified address in a journal. Also returns a proof of the specified block for verification if DigestTipAddress is provided. For information about the data contents in a block, see Journal contents in the Amazon QLDB Developer Guide. If the specified ledger doesn't exist or is in DELETING status, then throws ResourceNotFoundException. If the specified ledger is in CREATING status, then throws ResourcePreconditionNotMetException. If no block exists with the specified address, then throws InvalidParameterException."]
module ExportJournalToS3Response =
  struct
    type nonrec t =
      {
      exportId: UniqueId.t
        [@ocaml.doc
          "The UUID (represented in Base62-encoded text) that QLDB assigns to each journal export job. To describe your export request and check the status of the job, you can use ExportId to call DescribeJournalS3Export."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ResourcePreconditionNotMetException of
          ResourcePreconditionNotMetException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ExportJournalToS3Response"
    let make ~exportId = fun () -> { exportId }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ResourcePreconditionNotMetException e ->
          `Assoc
            [("error", (`String "ResourcePreconditionNotMetException"));
            ("details", (ResourcePreconditionNotMetException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ExportId", (Some (UniqueId.to_value x.exportId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exportId =
        UniqueId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ExportId") in
      make ~exportId ()
    let of_json json =
      let exportId = field_map_exn json "ExportId" UniqueId.of_json in
      make ~exportId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exports journal contents within a date and time range from a ledger into a specified Amazon Simple Storage Service (Amazon S3) bucket. A journal export job can write the data objects in either the text or binary representation of Amazon Ion format, or in JSON Lines text format. In JSON Lines format, each journal block in the exported data object is a valid JSON object that is delimited by a newline. You can use this format to easily integrate JSON exports with analytics tools such as Glue and Amazon Athena because these services can parse newline-delimited JSON automatically. For more information about the format, see JSON Lines. If the ledger with the given Name doesn't exist, then throws ResourceNotFoundException. If the ledger with the given Name is in CREATING status, then throws ResourcePreconditionNotMetException. You can initiate up to two concurrent journal export requests for each ledger. Beyond this limit, journal export requests throw LimitExceededException."]
module ExportJournalToS3Request =
  struct
    type nonrec t =
      {
      name: LedgerName.t [@ocaml.doc "The name of the ledger."];
      inclusiveStartTime: Timestamp.t
        [@ocaml.doc
          "The inclusive start date and time for the range of journal contents to export. The InclusiveStartTime must be in ISO 8601 date and time format and in Universal Coordinated Time (UTC). For example: 2019-06-13T21:36:34Z. The InclusiveStartTime must be before ExclusiveEndTime. If you provide an InclusiveStartTime that is before the ledger's CreationDateTime, Amazon QLDB defaults it to the ledger's CreationDateTime."];
      exclusiveEndTime: Timestamp.t
        [@ocaml.doc
          "The exclusive end date and time for the range of journal contents to export. The ExclusiveEndTime must be in ISO 8601 date and time format and in Universal Coordinated Time (UTC). For example: 2019-06-13T21:36:34Z. The ExclusiveEndTime must be less than or equal to the current UTC date and time."];
      s3ExportConfiguration: S3ExportConfiguration.t
        [@ocaml.doc
          "The configuration settings of the Amazon S3 bucket destination for your export request."];
      roleArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM role that grants QLDB permissions for a journal export job to do the following: Write objects into your Amazon Simple Storage Service (Amazon S3) bucket. (Optional) Use your customer managed key in Key Management Service (KMS) for server-side encryption of your exported data. To pass a role to QLDB when requesting a journal export, you must have permissions to perform the iam:PassRole action on the IAM role resource. This is required for all journal export requests."];
      outputFormat: OutputFormat.t option
        [@ocaml.doc
          "The output format of your exported journal data. If this parameter is not specified, the exported data defaults to ION_TEXT format."]}
    let context_ = "ExportJournalToS3Request"
    let make ?outputFormat =
      fun ~name ->
        fun ~inclusiveStartTime ->
          fun ~exclusiveEndTime ->
            fun ~s3ExportConfiguration ->
              fun ~roleArn ->
                fun () ->
                  {
                    outputFormat;
                    name;
                    inclusiveStartTime;
                    exclusiveEndTime;
                    s3ExportConfiguration;
                    roleArn
                  }
    let to_value x =
      structure_to_value
        [("name", (Some (LedgerName.to_value x.name)));
        ("InclusiveStartTime",
          (Some (Timestamp.to_value x.inclusiveStartTime)));
        ("ExclusiveEndTime", (Some (Timestamp.to_value x.exclusiveEndTime)));
        ("S3ExportConfiguration",
          (Some (S3ExportConfiguration.to_value x.s3ExportConfiguration)));
        ("RoleArn", (Some (Arn.to_value x.roleArn)));
        ("OutputFormat",
          (Option.map x.outputFormat ~f:OutputFormat.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputFormat =
        (Option.map ~f:OutputFormat.of_xml)
          (Xml.child xml_arg0 "OutputFormat") in
      let roleArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "RoleArn") in
      let s3ExportConfiguration =
        S3ExportConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "S3ExportConfiguration") in
      let exclusiveEndTime =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExclusiveEndTime") in
      let inclusiveStartTime =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InclusiveStartTime") in
      let name =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?outputFormat ~roleArn ~s3ExportConfiguration ~exclusiveEndTime
        ~inclusiveStartTime ~name ()
    let of_json json =
      let outputFormat = field_map json "OutputFormat" OutputFormat.of_json in
      let roleArn = field_map_exn json "RoleArn" Arn.of_json in
      let s3ExportConfiguration =
        field_map_exn json "S3ExportConfiguration"
          S3ExportConfiguration.of_json in
      let exclusiveEndTime =
        field_map_exn json "ExclusiveEndTime" Timestamp.of_json in
      let inclusiveStartTime =
        field_map_exn json "InclusiveStartTime" Timestamp.of_json in
      let name = field_map_exn json "Name" LedgerName.of_json in
      make ?outputFormat ~roleArn ~s3ExportConfiguration ~exclusiveEndTime
        ~inclusiveStartTime ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exports journal contents within a date and time range from a ledger into a specified Amazon Simple Storage Service (Amazon S3) bucket. A journal export job can write the data objects in either the text or binary representation of Amazon Ion format, or in JSON Lines text format. In JSON Lines format, each journal block in the exported data object is a valid JSON object that is delimited by a newline. You can use this format to easily integrate JSON exports with analytics tools such as Glue and Amazon Athena because these services can parse newline-delimited JSON automatically. For more information about the format, see JSON Lines. If the ledger with the given Name doesn't exist, then throws ResourceNotFoundException. If the ledger with the given Name is in CREATING status, then throws ResourcePreconditionNotMetException. You can initiate up to two concurrent journal export requests for each ledger. Beyond this limit, journal export requests throw LimitExceededException."]
module DescribeLedgerResponse =
  struct
    type nonrec t =
      {
      name: LedgerName.t option [@ocaml.doc "The name of the ledger."];
      arn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) for the ledger."];
      state: LedgerState.t option
        [@ocaml.doc "The current status of the ledger."];
      creationDateTime: Timestamp.t option
        [@ocaml.doc
          "The date and time, in epoch time format, when the ledger was created. (Epoch time format is the number of seconds elapsed since 12:00:00 AM January 1, 1970 UTC.)"];
      permissionsMode: PermissionsMode.t option
        [@ocaml.doc "The permissions mode of the ledger."];
      deletionProtection: DeletionProtection.t option
        [@ocaml.doc
          "The flag that prevents a ledger from being deleted by any user. If not provided on ledger creation, this feature is enabled (true) by default. If deletion protection is enabled, you must first disable it before you can delete the ledger. You can disable it by calling the UpdateLedger operation to set the flag to false."];
      encryptionDescription: LedgerEncryptionDescription.t option
        [@ocaml.doc
          "Information about the encryption of data at rest in the ledger. This includes the current status, the KMS key, and when the key became inaccessible (in the case of an error)."]}
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?name =
      fun ?arn ->
        fun ?state ->
          fun ?creationDateTime ->
            fun ?permissionsMode ->
              fun ?deletionProtection ->
                fun ?encryptionDescription ->
                  fun () ->
                    {
                      name;
                      arn;
                      state;
                      creationDateTime;
                      permissionsMode;
                      deletionProtection;
                      encryptionDescription
                    }
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:LedgerName.to_value));
        ("Arn", (Option.map x.arn ~f:Arn.to_value));
        ("State", (Option.map x.state ~f:LedgerState.to_value));
        ("CreationDateTime",
          (Option.map x.creationDateTime ~f:Timestamp.to_value));
        ("PermissionsMode",
          (Option.map x.permissionsMode ~f:PermissionsMode.to_value));
        ("DeletionProtection",
          (Option.map x.deletionProtection ~f:DeletionProtection.to_value));
        ("EncryptionDescription",
          (Option.map x.encryptionDescription
             ~f:LedgerEncryptionDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionDescription =
        (Option.map ~f:LedgerEncryptionDescription.of_xml)
          (Xml.child xml_arg0 "EncryptionDescription") in
      let deletionProtection =
        (Option.map ~f:DeletionProtection.of_xml)
          (Xml.child xml_arg0 "DeletionProtection") in
      let permissionsMode =
        (Option.map ~f:PermissionsMode.of_xml)
          (Xml.child xml_arg0 "PermissionsMode") in
      let creationDateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CreationDateTime") in
      let state =
        (Option.map ~f:LedgerState.of_xml) (Xml.child xml_arg0 "State") in
      let arn = (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "Arn") in
      let name =
        (Option.map ~f:LedgerName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?encryptionDescription ?deletionProtection ?permissionsMode
        ?creationDateTime ?state ?arn ?name ()
    let of_json json =
      let encryptionDescription =
        field_map json "EncryptionDescription"
          LedgerEncryptionDescription.of_json in
      let deletionProtection =
        field_map json "DeletionProtection" DeletionProtection.of_json in
      let permissionsMode =
        field_map json "PermissionsMode" PermissionsMode.of_json in
      let creationDateTime =
        field_map json "CreationDateTime" Timestamp.of_json in
      let state = field_map json "State" LedgerState.of_json in
      let arn = field_map json "Arn" Arn.of_json in
      let name = field_map json "Name" LedgerName.of_json in
      make ?encryptionDescription ?deletionProtection ?permissionsMode
        ?creationDateTime ?state ?arn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about a ledger, including its state, permissions mode, encryption at rest settings, and when it was created."]
module DescribeLedgerRequest =
  struct
    type nonrec t =
      {
      name: LedgerName.t
        [@ocaml.doc "The name of the ledger that you want to describe."]}
    let context_ = "DescribeLedgerRequest"
    let make ~name = fun () -> { name }
    let to_value x =
      structure_to_value [("name", (Some (LedgerName.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~name ()
    let of_json json =
      let name = field_map_exn json "Name" LedgerName.of_json in
      make ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about a ledger, including its state, permissions mode, encryption at rest settings, and when it was created."]
module DescribeJournalS3ExportResponse =
  struct
    type nonrec t =
      {
      exportDescription: JournalS3ExportDescription.t
        [@ocaml.doc
          "Information about the journal export job returned by a DescribeJournalS3Export request."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeJournalS3ExportResponse"
    let make ~exportDescription = fun () -> { exportDescription }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ExportDescription",
           (Some (JournalS3ExportDescription.to_value x.exportDescription)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exportDescription =
        JournalS3ExportDescription.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExportDescription") in
      make ~exportDescription ()
    let of_json json =
      let exportDescription =
        field_map_exn json "ExportDescription"
          JournalS3ExportDescription.of_json in
      make ~exportDescription ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about a journal export job, including the ledger name, export ID, creation time, current status, and the parameters of the original export creation request. This action does not return any expired export jobs. For more information, see Export job expiration in the Amazon QLDB Developer Guide. If the export job with the given ExportId doesn't exist, then throws ResourceNotFoundException. If the ledger with the given Name doesn't exist, then throws ResourceNotFoundException."]
module DescribeJournalS3ExportRequest =
  struct
    type nonrec t =
      {
      name: LedgerName.t [@ocaml.doc "The name of the ledger."];
      exportId: UniqueId.t
        [@ocaml.doc
          "The UUID (represented in Base62-encoded text) of the journal export job to describe."]}
    let context_ = "DescribeJournalS3ExportRequest"
    let make ~name = fun ~exportId -> fun () -> { name; exportId }
    let to_value x =
      structure_to_value
        [("name", (Some (LedgerName.to_value x.name)));
        ("exportId", (Some (UniqueId.to_value x.exportId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exportId =
        UniqueId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "exportId") in
      let name =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~exportId ~name ()
    let of_json json =
      let exportId = field_map_exn json "ExportId" UniqueId.of_json in
      let name = field_map_exn json "Name" LedgerName.of_json in
      make ~exportId ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about a journal export job, including the ledger name, export ID, creation time, current status, and the parameters of the original export creation request. This action does not return any expired export jobs. For more information, see Export job expiration in the Amazon QLDB Developer Guide. If the export job with the given ExportId doesn't exist, then throws ResourceNotFoundException. If the ledger with the given Name doesn't exist, then throws ResourceNotFoundException."]
module DescribeJournalKinesisStreamResponse =
  struct
    type nonrec t =
      {
      stream: JournalKinesisStreamDescription.t option
        [@ocaml.doc
          "Information about the QLDB journal stream returned by a DescribeJournalS3Export request."]}
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ResourcePreconditionNotMetException of
          ResourcePreconditionNotMetException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?stream = fun () -> { stream }
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ResourcePreconditionNotMetException e ->
          `Assoc
            [("error", (`String "ResourcePreconditionNotMetException"));
            ("details", (ResourcePreconditionNotMetException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Stream",
           (Option.map x.stream ~f:JournalKinesisStreamDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stream =
        (Option.map ~f:JournalKinesisStreamDescription.of_xml)
          (Xml.child xml_arg0 "Stream") in
      make ?stream ()
    let of_json json =
      let stream =
        field_map json "Stream" JournalKinesisStreamDescription.of_json in
      make ?stream ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns detailed information about a given Amazon QLDB journal stream. The output includes the Amazon Resource Name (ARN), stream name, current status, creation time, and the parameters of the original stream creation request. This action does not return any expired journal streams. For more information, see Expiration for terminal streams in the Amazon QLDB Developer Guide."]
module DescribeJournalKinesisStreamRequest =
  struct
    type nonrec t =
      {
      ledgerName: LedgerName.t [@ocaml.doc "The name of the ledger."];
      streamId: UniqueId.t
        [@ocaml.doc
          "The UUID (represented in Base62-encoded text) of the QLDB journal stream to describe."]}
    let context_ = "DescribeJournalKinesisStreamRequest"
    let make ~ledgerName =
      fun ~streamId -> fun () -> { ledgerName; streamId }
    let to_value x =
      structure_to_value
        [("name", (Some (LedgerName.to_value x.ledgerName)));
        ("streamId", (Some (UniqueId.to_value x.streamId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamId =
        UniqueId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "streamId") in
      let ledgerName =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~streamId ~ledgerName ()
    let of_json json =
      let streamId = field_map_exn json "StreamId" UniqueId.of_json in
      let ledgerName = field_map_exn json "LedgerName" LedgerName.of_json in
      make ~streamId ~ledgerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns detailed information about a given Amazon QLDB journal stream. The output includes the Amazon Resource Name (ARN), stream name, current status, creation time, and the parameters of the original stream creation request. This action does not return any expired journal streams. For more information, see Expiration for terminal streams in the Amazon QLDB Developer Guide."]
module DeleteLedgerRequest =
  struct
    type nonrec t =
      {
      name: LedgerName.t
        [@ocaml.doc "The name of the ledger that you want to delete."]}
    let context_ = "DeleteLedgerRequest"
    let make ~name = fun () -> { name }
    let to_value x =
      structure_to_value [("name", (Some (LedgerName.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~name ()
    let of_json json =
      let name = field_map_exn json "Name" LedgerName.of_json in
      make ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a ledger and all of its contents. This action is irreversible. If deletion protection is enabled, you must first disable it before you can delete the ledger. You can disable it by calling the UpdateLedger operation to set the flag to false."]
module CreateLedgerResponse =
  struct
    type nonrec t =
      {
      name: LedgerName.t option [@ocaml.doc "The name of the ledger."];
      arn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) for the ledger."];
      state: LedgerState.t option
        [@ocaml.doc "The current status of the ledger."];
      creationDateTime: Timestamp.t option
        [@ocaml.doc
          "The date and time, in epoch time format, when the ledger was created. (Epoch time format is the number of seconds elapsed since 12:00:00 AM January 1, 1970 UTC.)"];
      permissionsMode: PermissionsMode.t option
        [@ocaml.doc "The permissions mode of the ledger that you created."];
      deletionProtection: DeletionProtection.t option
        [@ocaml.doc
          "The flag that prevents a ledger from being deleted by any user. If not provided on ledger creation, this feature is enabled (true) by default. If deletion protection is enabled, you must first disable it before you can delete the ledger. You can disable it by calling the UpdateLedger operation to set the flag to false."];
      kmsKeyArn: Arn.t option
        [@ocaml.doc
          "The ARN of the customer managed KMS key that the ledger uses for encryption at rest. If this parameter is undefined, the ledger uses an Amazon Web Services owned KMS key for encryption."]}
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?name =
      fun ?arn ->
        fun ?state ->
          fun ?creationDateTime ->
            fun ?permissionsMode ->
              fun ?deletionProtection ->
                fun ?kmsKeyArn ->
                  fun () ->
                    {
                      name;
                      arn;
                      state;
                      creationDateTime;
                      permissionsMode;
                      deletionProtection;
                      kmsKeyArn
                    }
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:LedgerName.to_value));
        ("Arn", (Option.map x.arn ~f:Arn.to_value));
        ("State", (Option.map x.state ~f:LedgerState.to_value));
        ("CreationDateTime",
          (Option.map x.creationDateTime ~f:Timestamp.to_value));
        ("PermissionsMode",
          (Option.map x.permissionsMode ~f:PermissionsMode.to_value));
        ("DeletionProtection",
          (Option.map x.deletionProtection ~f:DeletionProtection.to_value));
        ("KmsKeyArn", (Option.map x.kmsKeyArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKeyArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "KmsKeyArn") in
      let deletionProtection =
        (Option.map ~f:DeletionProtection.of_xml)
          (Xml.child xml_arg0 "DeletionProtection") in
      let permissionsMode =
        (Option.map ~f:PermissionsMode.of_xml)
          (Xml.child xml_arg0 "PermissionsMode") in
      let creationDateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CreationDateTime") in
      let state =
        (Option.map ~f:LedgerState.of_xml) (Xml.child xml_arg0 "State") in
      let arn = (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "Arn") in
      let name =
        (Option.map ~f:LedgerName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?kmsKeyArn ?deletionProtection ?permissionsMode ?creationDateTime
        ?state ?arn ?name ()
    let of_json json =
      let kmsKeyArn = field_map json "KmsKeyArn" Arn.of_json in
      let deletionProtection =
        field_map json "DeletionProtection" DeletionProtection.of_json in
      let permissionsMode =
        field_map json "PermissionsMode" PermissionsMode.of_json in
      let creationDateTime =
        field_map json "CreationDateTime" Timestamp.of_json in
      let state = field_map json "State" LedgerState.of_json in
      let arn = field_map json "Arn" Arn.of_json in
      let name = field_map json "Name" LedgerName.of_json in
      make ?kmsKeyArn ?deletionProtection ?permissionsMode ?creationDateTime
        ?state ?arn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new ledger in your Amazon Web Services account in the current Region."]
module CreateLedgerRequest =
  struct
    type nonrec t =
      {
      name: LedgerName.t
        [@ocaml.doc
          "The name of the ledger that you want to create. The name must be unique among all of the ledgers in your Amazon Web Services account in the current Region. Naming constraints for ledger names are defined in Quotas in Amazon QLDB in the Amazon QLDB Developer Guide."];
      tags: Tags.t option
        [@ocaml.doc
          "The key-value pairs to add as tags to the ledger that you want to create. Tag keys are case sensitive. Tag values are case sensitive and can be null."];
      permissionsMode: PermissionsMode.t
        [@ocaml.doc
          "The permissions mode to assign to the ledger that you want to create. This parameter can have one of the following values: ALLOW_ALL: A legacy permissions mode that enables access control with API-level granularity for ledgers. This mode allows users who have the SendCommand API permission for this ledger to run all PartiQL commands (hence, ALLOW_ALL) on any tables in the specified ledger. This mode disregards any table-level or command-level IAM permissions policies that you create for the ledger. STANDARD: (Recommended) A permissions mode that enables access control with finer granularity for ledgers, tables, and PartiQL commands. By default, this mode denies all user requests to run any PartiQL commands on any tables in this ledger. To allow PartiQL commands to run, you must create IAM permissions policies for specific table resources and PartiQL actions, in addition to the SendCommand API permission for the ledger. For information, see Getting started with the standard permissions mode in the Amazon QLDB Developer Guide. We strongly recommend using the STANDARD permissions mode to maximize the security of your ledger data."];
      deletionProtection: DeletionProtection.t option
        [@ocaml.doc
          "The flag that prevents a ledger from being deleted by any user. If not provided on ledger creation, this feature is enabled (true) by default. If deletion protection is enabled, you must first disable it before you can delete the ledger. You can disable it by calling the UpdateLedger operation to set the flag to false."];
      kmsKey: KmsKey.t option
        [@ocaml.doc
          "The key in Key Management Service (KMS) to use for encryption of data at rest in the ledger. For more information, see Encryption at rest in the Amazon QLDB Developer Guide. Use one of the following options to specify this parameter: AWS_OWNED_KMS_KEY: Use an KMS key that is owned and managed by Amazon Web Services on your behalf. Undefined: By default, use an Amazon Web Services owned KMS key. A valid symmetric customer managed KMS key: Use the specified KMS key in your account that you create, own, and manage. Amazon QLDB does not support asymmetric keys. For more information, see Using symmetric and asymmetric keys in the Key Management Service Developer Guide. To specify a customer managed KMS key, you can use its key ID, Amazon Resource Name (ARN), alias name, or alias ARN. When using an alias name, prefix it with \"alias/\". To specify a key in a different Amazon Web Services account, you must use the key ARN or alias ARN. For example: Key ID: 1234abcd-12ab-34cd-56ef-1234567890ab Key ARN: arn:aws:kms:us-east-2:111122223333:key/1234abcd-12ab-34cd-56ef-1234567890ab Alias name: alias/ExampleAlias Alias ARN: arn:aws:kms:us-east-2:111122223333:alias/ExampleAlias For more information, see Key identifiers (KeyId) in the Key Management Service Developer Guide."]}
    let context_ = "CreateLedgerRequest"
    let make ?tags =
      fun ?deletionProtection ->
        fun ?kmsKey ->
          fun ~name ->
            fun ~permissionsMode ->
              fun () ->
                { tags; deletionProtection; kmsKey; name; permissionsMode }
    let to_value x =
      structure_to_value
        [("Name", (Some (LedgerName.to_value x.name)));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("PermissionsMode",
          (Some (PermissionsMode.to_value x.permissionsMode)));
        ("DeletionProtection",
          (Option.map x.deletionProtection ~f:DeletionProtection.to_value));
        ("KmsKey", (Option.map x.kmsKey ~f:KmsKey.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKey =
        (Option.map ~f:KmsKey.of_xml) (Xml.child xml_arg0 "KmsKey") in
      let deletionProtection =
        (Option.map ~f:DeletionProtection.of_xml)
          (Xml.child xml_arg0 "DeletionProtection") in
      let permissionsMode =
        PermissionsMode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PermissionsMode") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let name =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?kmsKey ?deletionProtection ~permissionsMode ?tags ~name ()
    let of_json json =
      let kmsKey = field_map json "KmsKey" KmsKey.of_json in
      let deletionProtection =
        field_map json "DeletionProtection" DeletionProtection.of_json in
      let permissionsMode =
        field_map_exn json "PermissionsMode" PermissionsMode.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let name = field_map_exn json "Name" LedgerName.of_json in
      make ?kmsKey ?deletionProtection ~permissionsMode ?tags ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new ledger in your Amazon Web Services account in the current Region."]
module CancelJournalKinesisStreamResponse =
  struct
    type nonrec t =
      {
      streamId: UniqueId.t option
        [@ocaml.doc
          "The UUID (Base62-encoded text) of the canceled QLDB journal stream."]}
    type nonrec error =
      [ `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ResourcePreconditionNotMetException of
          ResourcePreconditionNotMetException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?streamId = fun () -> { streamId }
    let error_of_json name json =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ResourcePreconditionNotMetException" ->
          `ResourcePreconditionNotMetException
            (ResourcePreconditionNotMetException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ResourcePreconditionNotMetException e ->
          `Assoc
            [("error", (`String "ResourcePreconditionNotMetException"));
            ("details", (ResourcePreconditionNotMetException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("StreamId", (Option.map x.streamId ~f:UniqueId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamId =
        (Option.map ~f:UniqueId.of_xml) (Xml.child xml_arg0 "StreamId") in
      make ?streamId ()
    let of_json json =
      let streamId = field_map json "StreamId" UniqueId.of_json in
      make ?streamId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Ends a given Amazon QLDB journal stream. Before a stream can be canceled, its current status must be ACTIVE. You can't restart a stream after you cancel it. Canceled QLDB stream resources are subject to a 7-day retention period, so they are automatically deleted after this limit expires."]
module CancelJournalKinesisStreamRequest =
  struct
    type nonrec t =
      {
      ledgerName: LedgerName.t [@ocaml.doc "The name of the ledger."];
      streamId: UniqueId.t
        [@ocaml.doc
          "The UUID (represented in Base62-encoded text) of the QLDB journal stream to be canceled."]}
    let context_ = "CancelJournalKinesisStreamRequest"
    let make ~ledgerName =
      fun ~streamId -> fun () -> { ledgerName; streamId }
    let to_value x =
      structure_to_value
        [("name", (Some (LedgerName.to_value x.ledgerName)));
        ("streamId", (Some (UniqueId.to_value x.streamId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamId =
        UniqueId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "streamId") in
      let ledgerName =
        LedgerName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~streamId ~ledgerName ()
    let of_json json =
      let streamId = field_map_exn json "StreamId" UniqueId.of_json in
      let ledgerName = field_map_exn json "LedgerName" LedgerName.of_json in
      make ~streamId ~ledgerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Ends a given Amazon QLDB journal stream. Before a stream can be canceled, its current status must be ACTIVE. You can't restart a stream after you cancel it. Canceled QLDB stream resources are subject to a 7-day retention period, so they are automatically deleted after this limit expires."]
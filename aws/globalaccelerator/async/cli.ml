(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_custom_routing_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointConfigurations =
         flag "endpoint-configurations" (required json_arg)
           ~doc:"JSON CustomRoutingEndpointConfigurations"
       and endpointGroupArn =
         flag "endpoint-group-arn" (required string)
           ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_custom_routing_endpoints
           (Values.AddCustomRoutingEndpointsRequest.make
              ~endpointConfigurations:(Values.CustomRoutingEndpointConfigurations.of_json
                                         endpointConfigurations)
              ~endpointGroupArn ())
           (Some Values.AddCustomRoutingEndpointsResponse.to_json)
           (Some Values.AddCustomRoutingEndpointsResponse.error_to_json)])
let advertise_byoip_cidr =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cidr = flag "cidr" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.advertise_byoip_cidr
           (Values.AdvertiseByoipCidrRequest.make ~cidr ())
           (Some Values.AdvertiseByoipCidrResponse.to_json)
           (Some Values.AdvertiseByoipCidrResponse.error_to_json)])
let allow_custom_routing_traffic =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationAddresses =
         flag "destination-addresses" (optional json_arg)
           ~doc:"JSON DestinationAddresses"
       and destinationPorts =
         flag "destination-ports" (optional json_arg)
           ~doc:"JSON DestinationPorts"
       and allowAllTrafficToEndpoint =
         flag "allow-all-traffic-to-endpoint" (optional bool)
           ~doc:"BOOL GenericBoolean"
       and endpointGroupArn =
         flag "endpoint-group-arn" (required string)
           ~doc:"STRING GenericString"
       and endpointId =
         flag "endpoint-id" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.allow_custom_routing_traffic
           (Values.AllowCustomRoutingTrafficRequest.make
              ?destinationAddresses:(Option.map
                                       ~f:Values.DestinationAddresses.of_json
                                       destinationAddresses)
              ?destinationPorts:(Option.map
                                   ~f:Values.DestinationPorts.of_json
                                   destinationPorts)
              ?allowAllTrafficToEndpoint ~endpointGroupArn ~endpointId ())
           None None])
let create_accelerator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and ipAddresses =
         flag "ip-addresses" (optional json_arg) ~doc:"JSON IpAddresses"
       and enabled =
         flag "enabled" (optional bool) ~doc:"BOOL GenericBoolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING GenericString"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_accelerator
           (Values.CreateAcceleratorRequest.make
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType)
              ?ipAddresses:(Option.map ~f:Values.IpAddresses.of_json
                              ipAddresses) ?enabled
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~idempotencyToken ())
           (Some Values.CreateAcceleratorResponse.to_json)
           (Some Values.CreateAcceleratorResponse.error_to_json)])
let create_custom_routing_accelerator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and ipAddresses =
         flag "ip-addresses" (optional json_arg) ~doc:"JSON IpAddresses"
       and enabled =
         flag "enabled" (optional bool) ~doc:"BOOL GenericBoolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING GenericString"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_routing_accelerator
           (Values.CreateCustomRoutingAcceleratorRequest.make
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType)
              ?ipAddresses:(Option.map ~f:Values.IpAddresses.of_json
                              ipAddresses) ?enabled
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~idempotencyToken ())
           (Some Values.CreateCustomRoutingAcceleratorResponse.to_json)
           (Some Values.CreateCustomRoutingAcceleratorResponse.error_to_json)])
let create_custom_routing_endpoint_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING GenericString"
       and endpointGroupRegion =
         flag "endpoint-group-region" (required string)
           ~doc:"STRING GenericString"
       and destinationConfigurations =
         flag "destination-configurations" (required json_arg)
           ~doc:"JSON CustomRoutingDestinationConfigurations"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_routing_endpoint_group
           (Values.CreateCustomRoutingEndpointGroupRequest.make ~listenerArn
              ~endpointGroupRegion
              ~destinationConfigurations:(Values.CustomRoutingDestinationConfigurations.of_json
                                            destinationConfigurations)
              ~idempotencyToken ())
           (Some Values.CreateCustomRoutingEndpointGroupResponse.to_json)
           (Some
              Values.CreateCustomRoutingEndpointGroupResponse.error_to_json)])
let create_custom_routing_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString"
       and portRanges =
         flag "port-ranges" (required json_arg) ~doc:"JSON PortRanges"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_routing_listener
           (Values.CreateCustomRoutingListenerRequest.make ~acceleratorArn
              ~portRanges:(Values.PortRanges.of_json portRanges)
              ~idempotencyToken ())
           (Some Values.CreateCustomRoutingListenerResponse.to_json)
           (Some Values.CreateCustomRoutingListenerResponse.error_to_json)])
let create_endpoint_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointConfigurations =
         flag "endpoint-configurations" (optional json_arg)
           ~doc:"JSON EndpointConfigurations"
       and trafficDialPercentage =
         flag "traffic-dial-percentage" (optional float)
           ~doc:"FLOAT TrafficDialPercentage"
       and healthCheckPort =
         flag "health-check-port" (optional int) ~doc:"INT HealthCheckPort"
       and healthCheckProtocol =
         flag "health-check-protocol" (optional json_arg)
           ~doc:"JSON HealthCheckProtocol"
       and healthCheckPath =
         flag "health-check-path" (optional string)
           ~doc:"STRING HealthCheckPath"
       and healthCheckIntervalSeconds =
         flag "health-check-interval-seconds" (optional int)
           ~doc:"INT HealthCheckIntervalSeconds"
       and thresholdCount =
         flag "threshold-count" (optional int) ~doc:"INT ThresholdCount"
       and portOverrides =
         flag "port-overrides" (optional json_arg) ~doc:"JSON PortOverrides"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING GenericString"
       and endpointGroupRegion =
         flag "endpoint-group-region" (required string)
           ~doc:"STRING GenericString"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_endpoint_group
           (Values.CreateEndpointGroupRequest.make
              ?endpointConfigurations:(Option.map
                                         ~f:Values.EndpointConfigurations.of_json
                                         endpointConfigurations)
              ?trafficDialPercentage ?healthCheckPort
              ?healthCheckProtocol:(Option.map
                                      ~f:Values.HealthCheckProtocol.of_json
                                      healthCheckProtocol) ?healthCheckPath
              ?healthCheckIntervalSeconds ?thresholdCount
              ?portOverrides:(Option.map ~f:Values.PortOverrides.of_json
                                portOverrides) ~listenerArn
              ~endpointGroupRegion ~idempotencyToken ())
           (Some Values.CreateEndpointGroupResponse.to_json)
           (Some Values.CreateEndpointGroupResponse.error_to_json)])
let create_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientAffinity =
         flag "client-affinity" (optional json_arg)
           ~doc:"JSON ClientAffinity"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString"
       and portRanges =
         flag "port-ranges" (required json_arg) ~doc:"JSON PortRanges"
       and protocol =
         flag "protocol" (required json_arg) ~doc:"JSON Protocol"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_listener
           (Values.CreateListenerRequest.make
              ?clientAffinity:(Option.map ~f:Values.ClientAffinity.of_json
                                 clientAffinity) ~acceleratorArn
              ~portRanges:(Values.PortRanges.of_json portRanges)
              ~protocol:(Values.Protocol.of_json protocol) ~idempotencyToken
              ()) (Some Values.CreateListenerResponse.to_json)
           (Some Values.CreateListenerResponse.error_to_json)])
let delete_accelerator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_accelerator
           (Values.DeleteAcceleratorRequest.make ~acceleratorArn ()) None
           None])
let delete_custom_routing_accelerator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_routing_accelerator
           (Values.DeleteCustomRoutingAcceleratorRequest.make ~acceleratorArn
              ()) None None])
let delete_custom_routing_endpoint_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointGroupArn =
         flag "endpoint-group-arn" (required string)
           ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_routing_endpoint_group
           (Values.DeleteCustomRoutingEndpointGroupRequest.make
              ~endpointGroupArn ()) None None])
let delete_custom_routing_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_routing_listener
           (Values.DeleteCustomRoutingListenerRequest.make ~listenerArn ())
           None None])
let delete_endpoint_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointGroupArn =
         flag "endpoint-group-arn" (required string)
           ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_endpoint_group
           (Values.DeleteEndpointGroupRequest.make ~endpointGroupArn ()) None
           None])
let delete_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_listener
           (Values.DeleteListenerRequest.make ~listenerArn ()) None None])
let deny_custom_routing_traffic =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationAddresses =
         flag "destination-addresses" (optional json_arg)
           ~doc:"JSON DestinationAddresses"
       and destinationPorts =
         flag "destination-ports" (optional json_arg)
           ~doc:"JSON DestinationPorts"
       and denyAllTrafficToEndpoint =
         flag "deny-all-traffic-to-endpoint" (optional bool)
           ~doc:"BOOL GenericBoolean"
       and endpointGroupArn =
         flag "endpoint-group-arn" (required string)
           ~doc:"STRING GenericString"
       and endpointId =
         flag "endpoint-id" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deny_custom_routing_traffic
           (Values.DenyCustomRoutingTrafficRequest.make
              ?destinationAddresses:(Option.map
                                       ~f:Values.DestinationAddresses.of_json
                                       destinationAddresses)
              ?destinationPorts:(Option.map
                                   ~f:Values.DestinationPorts.of_json
                                   destinationPorts)
              ?denyAllTrafficToEndpoint ~endpointGroupArn ~endpointId ())
           None None])
let deprovision_byoip_cidr =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cidr = flag "cidr" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deprovision_byoip_cidr
           (Values.DeprovisionByoipCidrRequest.make ~cidr ())
           (Some Values.DeprovisionByoipCidrResponse.to_json)
           (Some Values.DeprovisionByoipCidrResponse.error_to_json)])
let describe_accelerator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_accelerator
           (Values.DescribeAcceleratorRequest.make ~acceleratorArn ())
           (Some Values.DescribeAcceleratorResponse.to_json)
           (Some Values.DescribeAcceleratorResponse.error_to_json)])
let describe_accelerator_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_accelerator_attributes
           (Values.DescribeAcceleratorAttributesRequest.make ~acceleratorArn
              ()) (Some Values.DescribeAcceleratorAttributesResponse.to_json)
           (Some Values.DescribeAcceleratorAttributesResponse.error_to_json)])
let describe_custom_routing_accelerator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_custom_routing_accelerator
           (Values.DescribeCustomRoutingAcceleratorRequest.make
              ~acceleratorArn ())
           (Some Values.DescribeCustomRoutingAcceleratorResponse.to_json)
           (Some
              Values.DescribeCustomRoutingAcceleratorResponse.error_to_json)])
let describe_custom_routing_accelerator_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_custom_routing_accelerator_attributes
           (Values.DescribeCustomRoutingAcceleratorAttributesRequest.make
              ~acceleratorArn ())
           (Some
              Values.DescribeCustomRoutingAcceleratorAttributesResponse.to_json)
           (Some
              Values.DescribeCustomRoutingAcceleratorAttributesResponse.error_to_json)])
let describe_custom_routing_endpoint_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointGroupArn =
         flag "endpoint-group-arn" (required string)
           ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_custom_routing_endpoint_group
           (Values.DescribeCustomRoutingEndpointGroupRequest.make
              ~endpointGroupArn ())
           (Some Values.DescribeCustomRoutingEndpointGroupResponse.to_json)
           (Some
              Values.DescribeCustomRoutingEndpointGroupResponse.error_to_json)])
let describe_custom_routing_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_custom_routing_listener
           (Values.DescribeCustomRoutingListenerRequest.make ~listenerArn ())
           (Some Values.DescribeCustomRoutingListenerResponse.to_json)
           (Some Values.DescribeCustomRoutingListenerResponse.error_to_json)])
let describe_endpoint_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointGroupArn =
         flag "endpoint-group-arn" (required string)
           ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoint_group
           (Values.DescribeEndpointGroupRequest.make ~endpointGroupArn ())
           (Some Values.DescribeEndpointGroupResponse.to_json)
           (Some Values.DescribeEndpointGroupResponse.error_to_json)])
let describe_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_listener
           (Values.DescribeListenerRequest.make ~listenerArn ())
           (Some Values.DescribeListenerResponse.to_json)
           (Some Values.DescribeListenerResponse.error_to_json)])
let list_accelerators =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_accelerators
           (Values.ListAcceleratorsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListAcceleratorsResponse.to_json)
           (Some Values.ListAcceleratorsResponse.error_to_json)])
let list_byoip_cidrs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_byoip_cidrs
           (Values.ListByoipCidrsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListByoipCidrsResponse.to_json)
           (Some Values.ListByoipCidrsResponse.error_to_json)])
let list_custom_routing_accelerators =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_routing_accelerators
           (Values.ListCustomRoutingAcceleratorsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListCustomRoutingAcceleratorsResponse.to_json)
           (Some Values.ListCustomRoutingAcceleratorsResponse.error_to_json)])
let list_custom_routing_endpoint_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_routing_endpoint_groups
           (Values.ListCustomRoutingEndpointGroupsRequest.make ?maxResults
              ?nextToken ~listenerArn ())
           (Some Values.ListCustomRoutingEndpointGroupsResponse.to_json)
           (Some Values.ListCustomRoutingEndpointGroupsResponse.error_to_json)])
let list_custom_routing_listeners =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_routing_listeners
           (Values.ListCustomRoutingListenersRequest.make ?maxResults
              ?nextToken ~acceleratorArn ())
           (Some Values.ListCustomRoutingListenersResponse.to_json)
           (Some Values.ListCustomRoutingListenersResponse.error_to_json)])
let list_custom_routing_port_mappings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointGroupArn =
         flag "endpoint-group-arn" (optional string)
           ~doc:"STRING GenericString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PortMappingsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_routing_port_mappings
           (Values.ListCustomRoutingPortMappingsRequest.make
              ?endpointGroupArn ?maxResults ?nextToken ~acceleratorArn ())
           (Some Values.ListCustomRoutingPortMappingsResponse.to_json)
           (Some Values.ListCustomRoutingPortMappingsResponse.error_to_json)])
let list_custom_routing_port_mappings_by_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PortMappingsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and endpointId =
         flag "endpoint-id" (required string) ~doc:"STRING GenericString"
       and destinationAddress =
         flag "destination-address" (required string)
           ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_routing_port_mappings_by_destination
           (Values.ListCustomRoutingPortMappingsByDestinationRequest.make
              ?maxResults ?nextToken ~endpointId ~destinationAddress ())
           (Some
              Values.ListCustomRoutingPortMappingsByDestinationResponse.to_json)
           (Some
              Values.ListCustomRoutingPortMappingsByDestinationResponse.error_to_json)])
let list_endpoint_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_endpoint_groups
           (Values.ListEndpointGroupsRequest.make ?maxResults ?nextToken
              ~listenerArn ())
           (Some Values.ListEndpointGroupsResponse.to_json)
           (Some Values.ListEndpointGroupsResponse.error_to_json)])
let list_listeners =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_listeners
           (Values.ListListenersRequest.make ?maxResults ?nextToken
              ~acceleratorArn ()) (Some Values.ListListenersResponse.to_json)
           (Some Values.ListListenersResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let provision_byoip_cidr =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cidr = flag "cidr" (required string) ~doc:"STRING GenericString"
       and cidrAuthorizationContext =
         flag "cidr-authorization-context" (required json_arg)
           ~doc:"JSON CidrAuthorizationContext" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.provision_byoip_cidr
           (Values.ProvisionByoipCidrRequest.make ~cidr
              ~cidrAuthorizationContext:(Values.CidrAuthorizationContext.of_json
                                           cidrAuthorizationContext) ())
           (Some Values.ProvisionByoipCidrResponse.to_json)
           (Some Values.ProvisionByoipCidrResponse.error_to_json)])
let remove_custom_routing_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointIds =
         flag "endpoint-ids" (required json_arg) ~doc:"JSON EndpointIds"
       and endpointGroupArn =
         flag "endpoint-group-arn" (required string)
           ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_custom_routing_endpoints
           (Values.RemoveCustomRoutingEndpointsRequest.make
              ~endpointIds:(Values.EndpointIds.of_json endpointIds)
              ~endpointGroupArn ()) None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_accelerator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING GenericString"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and enabled =
         flag "enabled" (optional bool) ~doc:"BOOL GenericBoolean"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_accelerator
           (Values.UpdateAcceleratorRequest.make ?name
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType) ?enabled ~acceleratorArn ())
           (Some Values.UpdateAcceleratorResponse.to_json)
           (Some Values.UpdateAcceleratorResponse.error_to_json)])
let update_accelerator_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowLogsEnabled =
         flag "flow-logs-enabled" (optional bool) ~doc:"BOOL GenericBoolean"
       and flowLogsS3Bucket =
         flag "flow-logs-s3-bucket" (optional string)
           ~doc:"STRING GenericString"
       and flowLogsS3Prefix =
         flag "flow-logs-s3-prefix" (optional string)
           ~doc:"STRING GenericString"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_accelerator_attributes
           (Values.UpdateAcceleratorAttributesRequest.make ?flowLogsEnabled
              ?flowLogsS3Bucket ?flowLogsS3Prefix ~acceleratorArn ())
           (Some Values.UpdateAcceleratorAttributesResponse.to_json)
           (Some Values.UpdateAcceleratorAttributesResponse.error_to_json)])
let update_custom_routing_accelerator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING GenericString"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and enabled =
         flag "enabled" (optional bool) ~doc:"BOOL GenericBoolean"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_custom_routing_accelerator
           (Values.UpdateCustomRoutingAcceleratorRequest.make ?name
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType) ?enabled ~acceleratorArn ())
           (Some Values.UpdateCustomRoutingAcceleratorResponse.to_json)
           (Some Values.UpdateCustomRoutingAcceleratorResponse.error_to_json)])
let update_custom_routing_accelerator_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowLogsEnabled =
         flag "flow-logs-enabled" (optional bool) ~doc:"BOOL GenericBoolean"
       and flowLogsS3Bucket =
         flag "flow-logs-s3-bucket" (optional string)
           ~doc:"STRING GenericString"
       and flowLogsS3Prefix =
         flag "flow-logs-s3-prefix" (optional string)
           ~doc:"STRING GenericString"
       and acceleratorArn =
         flag "accelerator-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_custom_routing_accelerator_attributes
           (Values.UpdateCustomRoutingAcceleratorAttributesRequest.make
              ?flowLogsEnabled ?flowLogsS3Bucket ?flowLogsS3Prefix
              ~acceleratorArn ())
           (Some
              Values.UpdateCustomRoutingAcceleratorAttributesResponse.to_json)
           (Some
              Values.UpdateCustomRoutingAcceleratorAttributesResponse.error_to_json)])
let update_custom_routing_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING GenericString"
       and portRanges =
         flag "port-ranges" (required json_arg) ~doc:"JSON PortRanges" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_custom_routing_listener
           (Values.UpdateCustomRoutingListenerRequest.make ~listenerArn
              ~portRanges:(Values.PortRanges.of_json portRanges) ())
           (Some Values.UpdateCustomRoutingListenerResponse.to_json)
           (Some Values.UpdateCustomRoutingListenerResponse.error_to_json)])
let update_endpoint_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointConfigurations =
         flag "endpoint-configurations" (optional json_arg)
           ~doc:"JSON EndpointConfigurations"
       and trafficDialPercentage =
         flag "traffic-dial-percentage" (optional float)
           ~doc:"FLOAT TrafficDialPercentage"
       and healthCheckPort =
         flag "health-check-port" (optional int) ~doc:"INT HealthCheckPort"
       and healthCheckProtocol =
         flag "health-check-protocol" (optional json_arg)
           ~doc:"JSON HealthCheckProtocol"
       and healthCheckPath =
         flag "health-check-path" (optional string)
           ~doc:"STRING HealthCheckPath"
       and healthCheckIntervalSeconds =
         flag "health-check-interval-seconds" (optional int)
           ~doc:"INT HealthCheckIntervalSeconds"
       and thresholdCount =
         flag "threshold-count" (optional int) ~doc:"INT ThresholdCount"
       and portOverrides =
         flag "port-overrides" (optional json_arg) ~doc:"JSON PortOverrides"
       and endpointGroupArn =
         flag "endpoint-group-arn" (required string)
           ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_endpoint_group
           (Values.UpdateEndpointGroupRequest.make
              ?endpointConfigurations:(Option.map
                                         ~f:Values.EndpointConfigurations.of_json
                                         endpointConfigurations)
              ?trafficDialPercentage ?healthCheckPort
              ?healthCheckProtocol:(Option.map
                                      ~f:Values.HealthCheckProtocol.of_json
                                      healthCheckProtocol) ?healthCheckPath
              ?healthCheckIntervalSeconds ?thresholdCount
              ?portOverrides:(Option.map ~f:Values.PortOverrides.of_json
                                portOverrides) ~endpointGroupArn ())
           (Some Values.UpdateEndpointGroupResponse.to_json)
           (Some Values.UpdateEndpointGroupResponse.error_to_json)])
let update_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portRanges =
         flag "port-ranges" (optional json_arg) ~doc:"JSON PortRanges"
       and protocol =
         flag "protocol" (optional json_arg) ~doc:"JSON Protocol"
       and clientAffinity =
         flag "client-affinity" (optional json_arg)
           ~doc:"JSON ClientAffinity"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_listener
           (Values.UpdateListenerRequest.make
              ?portRanges:(Option.map ~f:Values.PortRanges.of_json portRanges)
              ?protocol:(Option.map ~f:Values.Protocol.of_json protocol)
              ?clientAffinity:(Option.map ~f:Values.ClientAffinity.of_json
                                 clientAffinity) ~listenerArn ())
           (Some Values.UpdateListenerResponse.to_json)
           (Some Values.UpdateListenerResponse.error_to_json)])
let withdraw_byoip_cidr =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cidr = flag "cidr" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.withdraw_byoip_cidr
           (Values.WithdrawByoipCidrRequest.make ~cidr ())
           (Some Values.WithdrawByoipCidrResponse.to_json)
           (Some Values.WithdrawByoipCidrResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("add-custom-routing-endpoints", add_custom_routing_endpoints);
    ("advertise-byoip-cidr", advertise_byoip_cidr);
    ("allow-custom-routing-traffic", allow_custom_routing_traffic);
    ("create-accelerator", create_accelerator);
    ("create-custom-routing-accelerator", create_custom_routing_accelerator);
    ("create-custom-routing-endpoint-group",
      create_custom_routing_endpoint_group);
    ("create-custom-routing-listener", create_custom_routing_listener);
    ("create-endpoint-group", create_endpoint_group);
    ("create-listener", create_listener);
    ("delete-accelerator", delete_accelerator);
    ("delete-custom-routing-accelerator", delete_custom_routing_accelerator);
    ("delete-custom-routing-endpoint-group",
      delete_custom_routing_endpoint_group);
    ("delete-custom-routing-listener", delete_custom_routing_listener);
    ("delete-endpoint-group", delete_endpoint_group);
    ("delete-listener", delete_listener);
    ("deny-custom-routing-traffic", deny_custom_routing_traffic);
    ("deprovision-byoip-cidr", deprovision_byoip_cidr);
    ("describe-accelerator", describe_accelerator);
    ("describe-accelerator-attributes", describe_accelerator_attributes);
    ("describe-custom-routing-accelerator",
      describe_custom_routing_accelerator);
    ("describe-custom-routing-accelerator-attributes",
      describe_custom_routing_accelerator_attributes);
    ("describe-custom-routing-endpoint-group",
      describe_custom_routing_endpoint_group);
    ("describe-custom-routing-listener", describe_custom_routing_listener);
    ("describe-endpoint-group", describe_endpoint_group);
    ("describe-listener", describe_listener);
    ("list-accelerators", list_accelerators);
    ("list-byoip-cidrs", list_byoip_cidrs);
    ("list-custom-routing-accelerators", list_custom_routing_accelerators);
    ("list-custom-routing-endpoint-groups",
      list_custom_routing_endpoint_groups);
    ("list-custom-routing-listeners", list_custom_routing_listeners);
    ("list-custom-routing-port-mappings", list_custom_routing_port_mappings);
    ("list-custom-routing-port-mappings-by-destination",
      list_custom_routing_port_mappings_by_destination);
    ("list-endpoint-groups", list_endpoint_groups);
    ("list-listeners", list_listeners);
    ("list-tags-for-resource", list_tags_for_resource);
    ("provision-byoip-cidr", provision_byoip_cidr);
    ("remove-custom-routing-endpoints", remove_custom_routing_endpoints);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-accelerator", update_accelerator);
    ("update-accelerator-attributes", update_accelerator_attributes);
    ("update-custom-routing-accelerator", update_custom_routing_accelerator);
    ("update-custom-routing-accelerator-attributes",
      update_custom_routing_accelerator_attributes);
    ("update-custom-routing-listener", update_custom_routing_listener);
    ("update-endpoint-group", update_endpoint_group);
    ("update-listener", update_listener);
    ("withdraw-byoip-cidr", withdraw_byoip_cidr)]
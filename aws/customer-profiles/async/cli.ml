(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_profile_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId = flag "profile-id" (required string) ~doc:"STRING uuid"
       and keyName = flag "key-name" (required string) ~doc:"STRING name"
       and values =
         flag "values" (required json_arg) ~doc:"JSON requestValueList"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_profile_key
           (Values.AddProfileKeyRequest.make ~profileId ~keyName
              ~values:(Values.RequestValueList.of_json values) ~domainName ())
           (Some Values.AddProfileKeyResponse.to_json)
           (Some Values.AddProfileKeyResponse.error_to_json)])
let create_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and defaultEncryptionKey =
         flag "default-encryption-key" (optional string)
           ~doc:"STRING encryptionKey"
       and deadLetterQueueUrl =
         flag "dead-letter-queue-url" (optional string)
           ~doc:"STRING sqsQueueUrl"
       and matching =
         flag "matching" (optional json_arg) ~doc:"JSON MatchingRequest"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and defaultExpirationDays =
         flag "default-expiration-days" (required int)
           ~doc:"INT expirationDaysInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain
           (Values.CreateDomainRequest.make ?defaultEncryptionKey
              ?deadLetterQueueUrl
              ?matching:(Option.map ~f:Values.MatchingRequest.of_json
                           matching)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~domainName
              ~defaultExpirationDays ())
           (Some Values.CreateDomainResponse.to_json)
           (Some Values.CreateDomainResponse.error_to_json)])
let create_integration_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and workflowType =
         flag "workflow-type" (required json_arg) ~doc:"JSON WorkflowType"
       and integrationConfig =
         flag "integration-config" (required json_arg)
           ~doc:"JSON IntegrationConfig"
       and objectTypeName =
         flag "object-type-name" (required string) ~doc:"STRING typeName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_integration_workflow
           (Values.CreateIntegrationWorkflowRequest.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~domainName
              ~workflowType:(Values.WorkflowType.of_json workflowType)
              ~integrationConfig:(Values.IntegrationConfig.of_json
                                    integrationConfig) ~objectTypeName
              ~roleArn ())
           (Some Values.CreateIntegrationWorkflowResponse.to_json)
           (Some Values.CreateIntegrationWorkflowResponse.error_to_json)])
let create_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountNumber =
         flag "account-number" (optional string) ~doc:"STRING string1To255"
       and additionalInformation =
         flag "additional-information" (optional string)
           ~doc:"STRING string1To1000"
       and partyType =
         flag "party-type" (optional json_arg) ~doc:"JSON PartyType"
       and businessName =
         flag "business-name" (optional string) ~doc:"STRING string1To255"
       and firstName =
         flag "first-name" (optional string) ~doc:"STRING string1To255"
       and middleName =
         flag "middle-name" (optional string) ~doc:"STRING string1To255"
       and lastName =
         flag "last-name" (optional string) ~doc:"STRING string1To255"
       and birthDate =
         flag "birth-date" (optional string) ~doc:"STRING string1To255"
       and gender = flag "gender" (optional json_arg) ~doc:"JSON Gender"
       and phoneNumber =
         flag "phone-number" (optional string) ~doc:"STRING string1To255"
       and mobilePhoneNumber =
         flag "mobile-phone-number" (optional string)
           ~doc:"STRING string1To255"
       and homePhoneNumber =
         flag "home-phone-number" (optional string)
           ~doc:"STRING string1To255"
       and businessPhoneNumber =
         flag "business-phone-number" (optional string)
           ~doc:"STRING string1To255"
       and emailAddress =
         flag "email-address" (optional string) ~doc:"STRING string1To255"
       and personalEmailAddress =
         flag "personal-email-address" (optional string)
           ~doc:"STRING string1To255"
       and businessEmailAddress =
         flag "business-email-address" (optional string)
           ~doc:"STRING string1To255"
       and address = flag "address" (optional json_arg) ~doc:"JSON Address"
       and shippingAddress =
         flag "shipping-address" (optional json_arg) ~doc:"JSON Address"
       and mailingAddress =
         flag "mailing-address" (optional json_arg) ~doc:"JSON Address"
       and billingAddress =
         flag "billing-address" (optional json_arg) ~doc:"JSON Address"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON Attributes"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_profile
           (Values.CreateProfileRequest.make ?accountNumber
              ?additionalInformation
              ?partyType:(Option.map ~f:Values.PartyType.of_json partyType)
              ?businessName ?firstName ?middleName ?lastName ?birthDate
              ?gender:(Option.map ~f:Values.Gender.of_json gender)
              ?phoneNumber ?mobilePhoneNumber ?homePhoneNumber
              ?businessPhoneNumber ?emailAddress ?personalEmailAddress
              ?businessEmailAddress
              ?address:(Option.map ~f:Values.Address.of_json address)
              ?shippingAddress:(Option.map ~f:Values.Address.of_json
                                  shippingAddress)
              ?mailingAddress:(Option.map ~f:Values.Address.of_json
                                 mailingAddress)
              ?billingAddress:(Option.map ~f:Values.Address.of_json
                                 billingAddress)
              ?attributes:(Option.map ~f:Values.Attributes.of_json attributes)
              ~domainName ()) (Some Values.CreateProfileResponse.to_json)
           (Some Values.CreateProfileResponse.error_to_json)])
let delete_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain (Values.DeleteDomainRequest.make ~domainName ())
           (Some Values.DeleteDomainResponse.to_json)
           (Some Values.DeleteDomainResponse.error_to_json)])
let delete_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and uri = flag "uri" (required string) ~doc:"STRING string1To255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_integration
           (Values.DeleteIntegrationRequest.make ~domainName ~uri ())
           (Some Values.DeleteIntegrationResponse.to_json)
           (Some Values.DeleteIntegrationResponse.error_to_json)])
let delete_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId = flag "profile-id" (required string) ~doc:"STRING uuid"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_profile
           (Values.DeleteProfileRequest.make ~profileId ~domainName ())
           (Some Values.DeleteProfileResponse.to_json)
           (Some Values.DeleteProfileResponse.error_to_json)])
let delete_profile_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId = flag "profile-id" (required string) ~doc:"STRING uuid"
       and keyName = flag "key-name" (required string) ~doc:"STRING name"
       and values =
         flag "values" (required json_arg) ~doc:"JSON requestValueList"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_profile_key
           (Values.DeleteProfileKeyRequest.make ~profileId ~keyName
              ~values:(Values.RequestValueList.of_json values) ~domainName ())
           (Some Values.DeleteProfileKeyResponse.to_json)
           (Some Values.DeleteProfileKeyResponse.error_to_json)])
let delete_profile_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId = flag "profile-id" (required string) ~doc:"STRING uuid"
       and profileObjectUniqueKey =
         flag "profile-object-unique-key" (required string)
           ~doc:"STRING string1To255"
       and objectTypeName =
         flag "object-type-name" (required string) ~doc:"STRING typeName"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_profile_object
           (Values.DeleteProfileObjectRequest.make ~profileId
              ~profileObjectUniqueKey ~objectTypeName ~domainName ())
           (Some Values.DeleteProfileObjectResponse.to_json)
           (Some Values.DeleteProfileObjectResponse.error_to_json)])
let delete_profile_object_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and objectTypeName =
         flag "object-type-name" (required string) ~doc:"STRING typeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_profile_object_type
           (Values.DeleteProfileObjectTypeRequest.make ~domainName
              ~objectTypeName ())
           (Some Values.DeleteProfileObjectTypeResponse.to_json)
           (Some Values.DeleteProfileObjectTypeResponse.error_to_json)])
let delete_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING string1To255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workflow
           (Values.DeleteWorkflowRequest.make ~domainName ~workflowId ())
           (Some Values.DeleteWorkflowResponse.to_json)
           (Some Values.DeleteWorkflowResponse.error_to_json)])
let get_auto_merging_preview =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and consolidation =
         flag "consolidation" (required json_arg) ~doc:"JSON Consolidation"
       and conflictResolution =
         flag "conflict-resolution" (required json_arg)
           ~doc:"JSON ConflictResolution" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_auto_merging_preview
           (Values.GetAutoMergingPreviewRequest.make ~domainName
              ~consolidation:(Values.Consolidation.of_json consolidation)
              ~conflictResolution:(Values.ConflictResolution.of_json
                                     conflictResolution) ())
           (Some Values.GetAutoMergingPreviewResponse.to_json)
           (Some Values.GetAutoMergingPreviewResponse.error_to_json)])
let get_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain (Values.GetDomainRequest.make ~domainName ())
           (Some Values.GetDomainResponse.to_json)
           (Some Values.GetDomainResponse.error_to_json)])
let get_identity_resolution_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and jobId = flag "job-id" (required string) ~doc:"STRING uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_identity_resolution_job
           (Values.GetIdentityResolutionJobRequest.make ~domainName ~jobId ())
           (Some Values.GetIdentityResolutionJobResponse.to_json)
           (Some Values.GetIdentityResolutionJobResponse.error_to_json)])
let get_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and uri = flag "uri" (required string) ~doc:"STRING string1To255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_integration
           (Values.GetIntegrationRequest.make ~domainName ~uri ())
           (Some Values.GetIntegrationResponse.to_json)
           (Some Values.GetIntegrationResponse.error_to_json)])
let get_matches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxSize100"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_matches
           (Values.GetMatchesRequest.make ?nextToken ?maxResults ~domainName
              ()) (Some Values.GetMatchesResponse.to_json)
           (Some Values.GetMatchesResponse.error_to_json)])
let get_profile_object_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and objectTypeName =
         flag "object-type-name" (required string) ~doc:"STRING typeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_profile_object_type
           (Values.GetProfileObjectTypeRequest.make ~domainName
              ~objectTypeName ())
           (Some Values.GetProfileObjectTypeResponse.to_json)
           (Some Values.GetProfileObjectTypeResponse.error_to_json)])
let get_profile_object_type_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateId =
         flag "template-id" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_profile_object_type_template
           (Values.GetProfileObjectTypeTemplateRequest.make ~templateId ())
           (Some Values.GetProfileObjectTypeTemplateResponse.to_json)
           (Some Values.GetProfileObjectTypeTemplateResponse.error_to_json)])
let get_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow
           (Values.GetWorkflowRequest.make ~domainName ~workflowId ())
           (Some Values.GetWorkflowResponse.to_json)
           (Some Values.GetWorkflowResponse.error_to_json)])
let get_workflow_steps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxSize100"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow_steps
           (Values.GetWorkflowStepsRequest.make ?nextToken ?maxResults
              ~domainName ~workflowId ())
           (Some Values.GetWorkflowStepsResponse.to_json)
           (Some Values.GetWorkflowStepsResponse.error_to_json)])
let list_account_integrations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxSize100"
       and includeHidden =
         flag "include-hidden" (optional bool) ~doc:"BOOL optionalBoolean"
       and uri = flag "uri" (required string) ~doc:"STRING string1To255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_account_integrations
           (Values.ListAccountIntegrationsRequest.make ?nextToken ?maxResults
              ?includeHidden ~uri ())
           (Some Values.ListAccountIntegrationsResponse.to_json)
           (Some Values.ListAccountIntegrationsResponse.error_to_json)])
let list_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxSize100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domains
           (Values.ListDomainsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListDomainsResponse.to_json)
           (Some Values.ListDomainsResponse.error_to_json)])
let list_identity_resolution_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxSize100"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_identity_resolution_jobs
           (Values.ListIdentityResolutionJobsRequest.make ?nextToken
              ?maxResults ~domainName ())
           (Some Values.ListIdentityResolutionJobsResponse.to_json)
           (Some Values.ListIdentityResolutionJobsResponse.error_to_json)])
let list_integrations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxSize100"
       and includeHidden =
         flag "include-hidden" (optional bool) ~doc:"BOOL optionalBoolean"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_integrations
           (Values.ListIntegrationsRequest.make ?nextToken ?maxResults
              ?includeHidden ~domainName ())
           (Some Values.ListIntegrationsResponse.to_json)
           (Some Values.ListIntegrationsResponse.error_to_json)])
let list_profile_object_type_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxSize100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_profile_object_type_templates
           (Values.ListProfileObjectTypeTemplatesRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListProfileObjectTypeTemplatesResponse.to_json)
           (Some Values.ListProfileObjectTypeTemplatesResponse.error_to_json)])
let list_profile_object_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxSize100"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_profile_object_types
           (Values.ListProfileObjectTypesRequest.make ?nextToken ?maxResults
              ~domainName ())
           (Some Values.ListProfileObjectTypesResponse.to_json)
           (Some Values.ListProfileObjectTypesResponse.error_to_json)])
let list_profile_objects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxSize100"
       and objectFilter =
         flag "object-filter" (optional json_arg) ~doc:"JSON ObjectFilter"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and objectTypeName =
         flag "object-type-name" (required string) ~doc:"STRING typeName"
       and profileId = flag "profile-id" (required string) ~doc:"STRING uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_profile_objects
           (Values.ListProfileObjectsRequest.make ?nextToken ?maxResults
              ?objectFilter:(Option.map ~f:Values.ObjectFilter.of_json
                               objectFilter) ~domainName ~objectTypeName
              ~profileId ()) (Some Values.ListProfileObjectsResponse.to_json)
           (Some Values.ListProfileObjectsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_workflows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowType =
         flag "workflow-type" (optional json_arg) ~doc:"JSON WorkflowType"
       and status = flag "status" (optional json_arg) ~doc:"JSON Status"
       and queryStartDate =
         flag "query-start-date" (optional json_arg) ~doc:"JSON timestamp"
       and queryEndDate =
         flag "query-end-date" (optional json_arg) ~doc:"JSON timestamp"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxSize100"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflows
           (Values.ListWorkflowsRequest.make
              ?workflowType:(Option.map ~f:Values.WorkflowType.of_json
                               workflowType)
              ?status:(Option.map ~f:Values.Status.of_json status)
              ?queryStartDate:(Option.map ~f:Values.Timestamp.of_json
                                 queryStartDate)
              ?queryEndDate:(Option.map ~f:Values.Timestamp.of_json
                               queryEndDate) ?nextToken ?maxResults
              ~domainName ()) (Some Values.ListWorkflowsResponse.to_json)
           (Some Values.ListWorkflowsResponse.error_to_json)])
let merge_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fieldSourceProfileIds =
         flag "field-source-profile-ids" (optional json_arg)
           ~doc:"JSON FieldSourceProfileIds"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and mainProfileId =
         flag "main-profile-id" (required string) ~doc:"STRING uuid"
       and profileIdsToBeMerged =
         flag "profile-ids-to-be-merged" (required json_arg)
           ~doc:"JSON ProfileIdToBeMergedList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.merge_profiles
           (Values.MergeProfilesRequest.make
              ?fieldSourceProfileIds:(Option.map
                                        ~f:Values.FieldSourceProfileIds.of_json
                                        fieldSourceProfileIds) ~domainName
              ~mainProfileId
              ~profileIdsToBeMerged:(Values.ProfileIdToBeMergedList.of_json
                                       profileIdsToBeMerged) ())
           (Some Values.MergeProfilesResponse.to_json)
           (Some Values.MergeProfilesResponse.error_to_json)])
let put_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and uri = flag "uri" (optional string) ~doc:"STRING string1To255"
       and objectTypeName =
         flag "object-type-name" (optional string) ~doc:"STRING typeName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and flowDefinition =
         flag "flow-definition" (optional json_arg)
           ~doc:"JSON FlowDefinition"
       and objectTypeNames =
         flag "object-type-names" (optional json_arg)
           ~doc:"JSON ObjectTypeNames"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_integration
           (Values.PutIntegrationRequest.make ?uri ?objectTypeName
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?flowDefinition:(Option.map ~f:Values.FlowDefinition.of_json
                                 flowDefinition)
              ?objectTypeNames:(Option.map ~f:Values.ObjectTypeNames.of_json
                                  objectTypeNames) ~domainName ())
           (Some Values.PutIntegrationResponse.to_json)
           (Some Values.PutIntegrationResponse.error_to_json)])
let put_profile_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and objectTypeName =
         flag "object-type-name" (required string) ~doc:"STRING typeName"
       and object_ =
         flag "object-" (required string) ~doc:"STRING stringifiedJson"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_profile_object
           (Values.PutProfileObjectRequest.make ~objectTypeName ~object_
              ~domainName ()) (Some Values.PutProfileObjectResponse.to_json)
           (Some Values.PutProfileObjectResponse.error_to_json)])
let put_profile_object_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateId =
         flag "template-id" (optional string) ~doc:"STRING name"
       and expirationDays =
         flag "expiration-days" (optional int)
           ~doc:"INT expirationDaysInteger"
       and encryptionKey =
         flag "encryption-key" (optional string) ~doc:"STRING encryptionKey"
       and allowProfileCreation =
         flag "allow-profile-creation" (optional bool) ~doc:"BOOL boolean"
       and sourceLastUpdatedTimestampFormat =
         flag "source-last-updated-timestamp-format" (optional string)
           ~doc:"STRING string1To255"
       and fields = flag "fields" (optional json_arg) ~doc:"JSON FieldMap"
       and keys = flag "keys" (optional json_arg) ~doc:"JSON KeyMap"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and objectTypeName =
         flag "object-type-name" (required string) ~doc:"STRING typeName"
       and description =
         flag "description" (required string) ~doc:"STRING text" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_profile_object_type
           (Values.PutProfileObjectTypeRequest.make ?templateId
              ?expirationDays ?encryptionKey ?allowProfileCreation
              ?sourceLastUpdatedTimestampFormat
              ?fields:(Option.map ~f:Values.FieldMap.of_json fields)
              ?keys:(Option.map ~f:Values.KeyMap.of_json keys)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~domainName
              ~objectTypeName ~description ())
           (Some Values.PutProfileObjectTypeResponse.to_json)
           (Some Values.PutProfileObjectTypeResponse.error_to_json)])
let search_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxSize100"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and keyName = flag "key-name" (required string) ~doc:"STRING name"
       and values =
         flag "values" (required json_arg) ~doc:"JSON requestValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_profiles
           (Values.SearchProfilesRequest.make ?nextToken ?maxResults
              ~domainName ~keyName
              ~values:(Values.RequestValueList.of_json values) ())
           (Some Values.SearchProfilesResponse.to_json)
           (Some Values.SearchProfilesResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and defaultExpirationDays =
         flag "default-expiration-days" (optional int)
           ~doc:"INT expirationDaysInteger"
       and defaultEncryptionKey =
         flag "default-encryption-key" (optional string)
           ~doc:"STRING encryptionKey"
       and deadLetterQueueUrl =
         flag "dead-letter-queue-url" (optional string)
           ~doc:"STRING sqsQueueUrl"
       and matching =
         flag "matching" (optional json_arg) ~doc:"JSON MatchingRequest"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain
           (Values.UpdateDomainRequest.make ?defaultExpirationDays
              ?defaultEncryptionKey ?deadLetterQueueUrl
              ?matching:(Option.map ~f:Values.MatchingRequest.of_json
                           matching)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~domainName ())
           (Some Values.UpdateDomainResponse.to_json)
           (Some Values.UpdateDomainResponse.error_to_json)])
let update_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and additionalInformation =
         flag "additional-information" (optional string)
           ~doc:"STRING string0To1000"
       and accountNumber =
         flag "account-number" (optional string) ~doc:"STRING string0To255"
       and partyType =
         flag "party-type" (optional json_arg) ~doc:"JSON PartyType"
       and businessName =
         flag "business-name" (optional string) ~doc:"STRING string0To255"
       and firstName =
         flag "first-name" (optional string) ~doc:"STRING string0To255"
       and middleName =
         flag "middle-name" (optional string) ~doc:"STRING string0To255"
       and lastName =
         flag "last-name" (optional string) ~doc:"STRING string0To255"
       and birthDate =
         flag "birth-date" (optional string) ~doc:"STRING string0To255"
       and gender = flag "gender" (optional json_arg) ~doc:"JSON Gender"
       and phoneNumber =
         flag "phone-number" (optional string) ~doc:"STRING string0To255"
       and mobilePhoneNumber =
         flag "mobile-phone-number" (optional string)
           ~doc:"STRING string0To255"
       and homePhoneNumber =
         flag "home-phone-number" (optional string)
           ~doc:"STRING string0To255"
       and businessPhoneNumber =
         flag "business-phone-number" (optional string)
           ~doc:"STRING string0To255"
       and emailAddress =
         flag "email-address" (optional string) ~doc:"STRING string0To255"
       and personalEmailAddress =
         flag "personal-email-address" (optional string)
           ~doc:"STRING string0To255"
       and businessEmailAddress =
         flag "business-email-address" (optional string)
           ~doc:"STRING string0To255"
       and address =
         flag "address" (optional json_arg) ~doc:"JSON UpdateAddress"
       and shippingAddress =
         flag "shipping-address" (optional json_arg)
           ~doc:"JSON UpdateAddress"
       and mailingAddress =
         flag "mailing-address" (optional json_arg) ~doc:"JSON UpdateAddress"
       and billingAddress =
         flag "billing-address" (optional json_arg) ~doc:"JSON UpdateAddress"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON UpdateAttributes"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING name"
       and profileId = flag "profile-id" (required string) ~doc:"STRING uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_profile
           (Values.UpdateProfileRequest.make ?additionalInformation
              ?accountNumber
              ?partyType:(Option.map ~f:Values.PartyType.of_json partyType)
              ?businessName ?firstName ?middleName ?lastName ?birthDate
              ?gender:(Option.map ~f:Values.Gender.of_json gender)
              ?phoneNumber ?mobilePhoneNumber ?homePhoneNumber
              ?businessPhoneNumber ?emailAddress ?personalEmailAddress
              ?businessEmailAddress
              ?address:(Option.map ~f:Values.UpdateAddress.of_json address)
              ?shippingAddress:(Option.map ~f:Values.UpdateAddress.of_json
                                  shippingAddress)
              ?mailingAddress:(Option.map ~f:Values.UpdateAddress.of_json
                                 mailingAddress)
              ?billingAddress:(Option.map ~f:Values.UpdateAddress.of_json
                                 billingAddress)
              ?attributes:(Option.map ~f:Values.UpdateAttributes.of_json
                             attributes) ~domainName ~profileId ())
           (Some Values.UpdateProfileResponse.to_json)
           (Some Values.UpdateProfileResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("add-profile-key", add_profile_key);
    ("create-domain", create_domain);
    ("create-integration-workflow", create_integration_workflow);
    ("create-profile", create_profile);
    ("delete-domain", delete_domain);
    ("delete-integration", delete_integration);
    ("delete-profile", delete_profile);
    ("delete-profile-key", delete_profile_key);
    ("delete-profile-object", delete_profile_object);
    ("delete-profile-object-type", delete_profile_object_type);
    ("delete-workflow", delete_workflow);
    ("get-auto-merging-preview", get_auto_merging_preview);
    ("get-domain", get_domain);
    ("get-identity-resolution-job", get_identity_resolution_job);
    ("get-integration", get_integration);
    ("get-matches", get_matches);
    ("get-profile-object-type", get_profile_object_type);
    ("get-profile-object-type-template", get_profile_object_type_template);
    ("get-workflow", get_workflow);
    ("get-workflow-steps", get_workflow_steps);
    ("list-account-integrations", list_account_integrations);
    ("list-domains", list_domains);
    ("list-identity-resolution-jobs", list_identity_resolution_jobs);
    ("list-integrations", list_integrations);
    ("list-profile-object-type-templates",
      list_profile_object_type_templates);
    ("list-profile-object-types", list_profile_object_types);
    ("list-profile-objects", list_profile_objects);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-workflows", list_workflows);
    ("merge-profiles", merge_profiles);
    ("put-integration", put_integration);
    ("put-profile-object", put_profile_object);
    ("put-profile-object-type", put_profile_object_type);
    ("search-profiles", search_profiles);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-domain", update_domain);
    ("update-profile", update_profile)]
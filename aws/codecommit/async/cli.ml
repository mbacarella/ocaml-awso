(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_approval_rule_template_with_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and approvalRuleTemplateName =
         flag "approval-rule-template-name" (required string)
           ~doc:"STRING ApprovalRuleTemplateName"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_approval_rule_template_with_repository
           (Values.AssociateApprovalRuleTemplateWithRepositoryInput.make
              ~approvalRuleTemplateName ~repositoryName ()) None None])
let batch_associate_approval_rule_template_with_repositories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and approvalRuleTemplateName =
         flag "approval-rule-template-name" (required string)
           ~doc:"STRING ApprovalRuleTemplateName"
       and repositoryNames =
         flag "repository-names" (required json_arg)
           ~doc:"JSON RepositoryNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_associate_approval_rule_template_with_repositories
           (Values.BatchAssociateApprovalRuleTemplateWithRepositoriesInput.make
              ~approvalRuleTemplateName
              ~repositoryNames:(Values.RepositoryNameList.of_json
                                  repositoryNames) ())
           (Some
              Values.BatchAssociateApprovalRuleTemplateWithRepositoriesOutput.to_json)
           (Some
              Values.BatchAssociateApprovalRuleTemplateWithRepositoriesOutput.error_to_json)])
let batch_describe_merge_conflicts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxMergeHunks =
         flag "max-merge-hunks" (optional int) ~doc:"INT MaxResults"
       and maxConflictFiles =
         flag "max-conflict-files" (optional int) ~doc:"INT MaxResults"
       and filePaths =
         flag "file-paths" (optional json_arg) ~doc:"JSON FilePaths"
       and conflictDetailLevel =
         flag "conflict-detail-level" (optional json_arg)
           ~doc:"JSON ConflictDetailLevelTypeEnum"
       and conflictResolutionStrategy =
         flag "conflict-resolution-strategy" (optional json_arg)
           ~doc:"JSON ConflictResolutionStrategyTypeEnum"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and destinationCommitSpecifier =
         flag "destination-commit-specifier" (required string)
           ~doc:"STRING CommitName"
       and sourceCommitSpecifier =
         flag "source-commit-specifier" (required string)
           ~doc:"STRING CommitName"
       and mergeOption =
         flag "merge-option" (required json_arg)
           ~doc:"JSON MergeOptionTypeEnum" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_describe_merge_conflicts
           (Values.BatchDescribeMergeConflictsInput.make ?maxMergeHunks
              ?maxConflictFiles
              ?filePaths:(Option.map ~f:Values.FilePaths.of_json filePaths)
              ?conflictDetailLevel:(Option.map
                                      ~f:Values.ConflictDetailLevelTypeEnum.of_json
                                      conflictDetailLevel)
              ?conflictResolutionStrategy:(Option.map
                                             ~f:Values.ConflictResolutionStrategyTypeEnum.of_json
                                             conflictResolutionStrategy)
              ?nextToken ~repositoryName ~destinationCommitSpecifier
              ~sourceCommitSpecifier
              ~mergeOption:(Values.MergeOptionTypeEnum.of_json mergeOption)
              ()) (Some Values.BatchDescribeMergeConflictsOutput.to_json)
           (Some Values.BatchDescribeMergeConflictsOutput.error_to_json)])
let batch_disassociate_approval_rule_template_from_repositories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and approvalRuleTemplateName =
         flag "approval-rule-template-name" (required string)
           ~doc:"STRING ApprovalRuleTemplateName"
       and repositoryNames =
         flag "repository-names" (required json_arg)
           ~doc:"JSON RepositoryNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_disassociate_approval_rule_template_from_repositories
           (Values.BatchDisassociateApprovalRuleTemplateFromRepositoriesInput.make
              ~approvalRuleTemplateName
              ~repositoryNames:(Values.RepositoryNameList.of_json
                                  repositoryNames) ())
           (Some
              Values.BatchDisassociateApprovalRuleTemplateFromRepositoriesOutput.to_json)
           (Some
              Values.BatchDisassociateApprovalRuleTemplateFromRepositoriesOutput.error_to_json)])
let batch_get_commits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and commitIds =
         flag "commit-ids" (required json_arg) ~doc:"JSON CommitIdsInputList"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_commits
           (Values.BatchGetCommitsInput.make
              ~commitIds:(Values.CommitIdsInputList.of_json commitIds)
              ~repositoryName ()) (Some Values.BatchGetCommitsOutput.to_json)
           (Some Values.BatchGetCommitsOutput.error_to_json)])
let batch_get_repositories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryNames =
         flag "repository-names" (required json_arg)
           ~doc:"JSON RepositoryNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_repositories
           (Values.BatchGetRepositoriesInput.make
              ~repositoryNames:(Values.RepositoryNameList.of_json
                                  repositoryNames) ())
           (Some Values.BatchGetRepositoriesOutput.to_json)
           (Some Values.BatchGetRepositoriesOutput.error_to_json)])
let create_approval_rule_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and approvalRuleTemplateDescription =
         flag "approval-rule-template-description" (optional string)
           ~doc:"STRING ApprovalRuleTemplateDescription"
       and approvalRuleTemplateName =
         flag "approval-rule-template-name" (required string)
           ~doc:"STRING ApprovalRuleTemplateName"
       and approvalRuleTemplateContent =
         flag "approval-rule-template-content" (required string)
           ~doc:"STRING ApprovalRuleTemplateContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_approval_rule_template
           (Values.CreateApprovalRuleTemplateInput.make
              ?approvalRuleTemplateDescription ~approvalRuleTemplateName
              ~approvalRuleTemplateContent ())
           (Some Values.CreateApprovalRuleTemplateOutput.to_json)
           (Some Values.CreateApprovalRuleTemplateOutput.error_to_json)])
let create_branch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName"
       and commitId =
         flag "commit-id" (required string) ~doc:"STRING CommitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_branch
           (Values.CreateBranchInput.make ~repositoryName ~branchName
              ~commitId ()) None None])
let create_commit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parentCommitId =
         flag "parent-commit-id" (optional string) ~doc:"STRING CommitId"
       and authorName =
         flag "author-name" (optional string) ~doc:"STRING Name"
       and email = flag "email" (optional string) ~doc:"STRING Email"
       and commitMessage =
         flag "commit-message" (optional string) ~doc:"STRING Message"
       and keepEmptyFolders =
         flag "keep-empty-folders" (optional bool)
           ~doc:"BOOL KeepEmptyFolders"
       and putFiles =
         flag "put-files" (optional json_arg) ~doc:"JSON PutFileEntries"
       and deleteFiles =
         flag "delete-files" (optional json_arg)
           ~doc:"JSON DeleteFileEntries"
       and setFileModes =
         flag "set-file-modes" (optional json_arg)
           ~doc:"JSON SetFileModeEntries"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_commit
           (Values.CreateCommitInput.make ?parentCommitId ?authorName ?email
              ?commitMessage ?keepEmptyFolders
              ?putFiles:(Option.map ~f:Values.PutFileEntries.of_json putFiles)
              ?deleteFiles:(Option.map ~f:Values.DeleteFileEntries.of_json
                              deleteFiles)
              ?setFileModes:(Option.map ~f:Values.SetFileModeEntries.of_json
                               setFileModes) ~repositoryName ~branchName ())
           (Some Values.CreateCommitOutput.to_json)
           (Some Values.CreateCommitOutput.error_to_json)])
let create_pull_request =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and title = flag "title" (required string) ~doc:"STRING Title"
       and targets =
         flag "targets" (required json_arg) ~doc:"JSON TargetList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pull_request
           (Values.CreatePullRequestInput.make ?description
              ?clientRequestToken ~title
              ~targets:(Values.TargetList.of_json targets) ())
           (Some Values.CreatePullRequestOutput.to_json)
           (Some Values.CreatePullRequestOutput.error_to_json)])
let create_pull_request_approval_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and approvalRuleName =
         flag "approval-rule-name" (required string)
           ~doc:"STRING ApprovalRuleName"
       and approvalRuleContent =
         flag "approval-rule-content" (required string)
           ~doc:"STRING ApprovalRuleContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pull_request_approval_rule
           (Values.CreatePullRequestApprovalRuleInput.make ~pullRequestId
              ~approvalRuleName ~approvalRuleContent ())
           (Some Values.CreatePullRequestApprovalRuleOutput.to_json)
           (Some Values.CreatePullRequestApprovalRuleOutput.error_to_json)])
let create_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryDescription =
         flag "repository-description" (optional string)
           ~doc:"STRING RepositoryDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_repository
           (Values.CreateRepositoryInput.make ?repositoryDescription
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~repositoryName ())
           (Some Values.CreateRepositoryOutput.to_json)
           (Some Values.CreateRepositoryOutput.error_to_json)])
let create_unreferenced_merge_commit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and conflictDetailLevel =
         flag "conflict-detail-level" (optional json_arg)
           ~doc:"JSON ConflictDetailLevelTypeEnum"
       and conflictResolutionStrategy =
         flag "conflict-resolution-strategy" (optional json_arg)
           ~doc:"JSON ConflictResolutionStrategyTypeEnum"
       and authorName =
         flag "author-name" (optional string) ~doc:"STRING Name"
       and email = flag "email" (optional string) ~doc:"STRING Email"
       and commitMessage =
         flag "commit-message" (optional string) ~doc:"STRING Message"
       and keepEmptyFolders =
         flag "keep-empty-folders" (optional bool)
           ~doc:"BOOL KeepEmptyFolders"
       and conflictResolution =
         flag "conflict-resolution" (optional json_arg)
           ~doc:"JSON ConflictResolution"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and sourceCommitSpecifier =
         flag "source-commit-specifier" (required string)
           ~doc:"STRING CommitName"
       and destinationCommitSpecifier =
         flag "destination-commit-specifier" (required string)
           ~doc:"STRING CommitName"
       and mergeOption =
         flag "merge-option" (required json_arg)
           ~doc:"JSON MergeOptionTypeEnum" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_unreferenced_merge_commit
           (Values.CreateUnreferencedMergeCommitInput.make
              ?conflictDetailLevel:(Option.map
                                      ~f:Values.ConflictDetailLevelTypeEnum.of_json
                                      conflictDetailLevel)
              ?conflictResolutionStrategy:(Option.map
                                             ~f:Values.ConflictResolutionStrategyTypeEnum.of_json
                                             conflictResolutionStrategy)
              ?authorName ?email ?commitMessage ?keepEmptyFolders
              ?conflictResolution:(Option.map
                                     ~f:Values.ConflictResolution.of_json
                                     conflictResolution) ~repositoryName
              ~sourceCommitSpecifier ~destinationCommitSpecifier
              ~mergeOption:(Values.MergeOptionTypeEnum.of_json mergeOption)
              ()) (Some Values.CreateUnreferencedMergeCommitOutput.to_json)
           (Some Values.CreateUnreferencedMergeCommitOutput.error_to_json)])
let delete_approval_rule_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and approvalRuleTemplateName =
         flag "approval-rule-template-name" (required string)
           ~doc:"STRING ApprovalRuleTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_approval_rule_template
           (Values.DeleteApprovalRuleTemplateInput.make
              ~approvalRuleTemplateName ())
           (Some Values.DeleteApprovalRuleTemplateOutput.to_json)
           (Some Values.DeleteApprovalRuleTemplateOutput.error_to_json)])
let delete_branch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_branch
           (Values.DeleteBranchInput.make ~repositoryName ~branchName ())
           (Some Values.DeleteBranchOutput.to_json)
           (Some Values.DeleteBranchOutput.error_to_json)])
let delete_comment_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and commentId =
         flag "comment-id" (required string) ~doc:"STRING CommentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_comment_content
           (Values.DeleteCommentContentInput.make ~commentId ())
           (Some Values.DeleteCommentContentOutput.to_json)
           (Some Values.DeleteCommentContentOutput.error_to_json)])
let delete_file =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keepEmptyFolders =
         flag "keep-empty-folders" (optional bool)
           ~doc:"BOOL KeepEmptyFolders"
       and commitMessage =
         flag "commit-message" (optional string) ~doc:"STRING Message"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and email = flag "email" (optional string) ~doc:"STRING Email"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName"
       and filePath = flag "file-path" (required string) ~doc:"STRING Path"
       and parentCommitId =
         flag "parent-commit-id" (required string) ~doc:"STRING CommitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_file
           (Values.DeleteFileInput.make ?keepEmptyFolders ?commitMessage
              ?name ?email ~repositoryName ~branchName ~filePath
              ~parentCommitId ()) (Some Values.DeleteFileOutput.to_json)
           (Some Values.DeleteFileOutput.error_to_json)])
let delete_pull_request_approval_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and approvalRuleName =
         flag "approval-rule-name" (required string)
           ~doc:"STRING ApprovalRuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_pull_request_approval_rule
           (Values.DeletePullRequestApprovalRuleInput.make ~pullRequestId
              ~approvalRuleName ())
           (Some Values.DeletePullRequestApprovalRuleOutput.to_json)
           (Some Values.DeletePullRequestApprovalRuleOutput.error_to_json)])
let delete_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_repository
           (Values.DeleteRepositoryInput.make ~repositoryName ())
           (Some Values.DeleteRepositoryOutput.to_json)
           (Some Values.DeleteRepositoryOutput.error_to_json)])
let describe_merge_conflicts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxMergeHunks =
         flag "max-merge-hunks" (optional int) ~doc:"INT MaxResults"
       and conflictDetailLevel =
         flag "conflict-detail-level" (optional json_arg)
           ~doc:"JSON ConflictDetailLevelTypeEnum"
       and conflictResolutionStrategy =
         flag "conflict-resolution-strategy" (optional json_arg)
           ~doc:"JSON ConflictResolutionStrategyTypeEnum"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and destinationCommitSpecifier =
         flag "destination-commit-specifier" (required string)
           ~doc:"STRING CommitName"
       and sourceCommitSpecifier =
         flag "source-commit-specifier" (required string)
           ~doc:"STRING CommitName"
       and mergeOption =
         flag "merge-option" (required json_arg)
           ~doc:"JSON MergeOptionTypeEnum"
       and filePath = flag "file-path" (required string) ~doc:"STRING Path" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_merge_conflicts
           (Values.DescribeMergeConflictsInput.make ?maxMergeHunks
              ?conflictDetailLevel:(Option.map
                                      ~f:Values.ConflictDetailLevelTypeEnum.of_json
                                      conflictDetailLevel)
              ?conflictResolutionStrategy:(Option.map
                                             ~f:Values.ConflictResolutionStrategyTypeEnum.of_json
                                             conflictResolutionStrategy)
              ?nextToken ~repositoryName ~destinationCommitSpecifier
              ~sourceCommitSpecifier
              ~mergeOption:(Values.MergeOptionTypeEnum.of_json mergeOption)
              ~filePath ())
           (Some Values.DescribeMergeConflictsOutput.to_json)
           (Some Values.DescribeMergeConflictsOutput.error_to_json)])
let describe_pull_request_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pullRequestEventType =
         flag "pull-request-event-type" (optional json_arg)
           ~doc:"JSON PullRequestEventType"
       and actorArn = flag "actor-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_pull_request_events
           (Values.DescribePullRequestEventsInput.make
              ?pullRequestEventType:(Option.map
                                       ~f:Values.PullRequestEventType.of_json
                                       pullRequestEventType) ?actorArn
              ?nextToken ?maxResults ~pullRequestId ())
           (Some Values.DescribePullRequestEventsOutput.to_json)
           (Some Values.DescribePullRequestEventsOutput.error_to_json)])
let disassociate_approval_rule_template_from_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and approvalRuleTemplateName =
         flag "approval-rule-template-name" (required string)
           ~doc:"STRING ApprovalRuleTemplateName"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_approval_rule_template_from_repository
           (Values.DisassociateApprovalRuleTemplateFromRepositoryInput.make
              ~approvalRuleTemplateName ~repositoryName ()) None None])
let evaluate_pull_request_approval_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and revisionId =
         flag "revision-id" (required string) ~doc:"STRING RevisionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.evaluate_pull_request_approval_rules
           (Values.EvaluatePullRequestApprovalRulesInput.make ~pullRequestId
              ~revisionId ())
           (Some Values.EvaluatePullRequestApprovalRulesOutput.to_json)
           (Some Values.EvaluatePullRequestApprovalRulesOutput.error_to_json)])
let get_approval_rule_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and approvalRuleTemplateName =
         flag "approval-rule-template-name" (required string)
           ~doc:"STRING ApprovalRuleTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_approval_rule_template
           (Values.GetApprovalRuleTemplateInput.make
              ~approvalRuleTemplateName ())
           (Some Values.GetApprovalRuleTemplateOutput.to_json)
           (Some Values.GetApprovalRuleTemplateOutput.error_to_json)])
let get_blob =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and blobId = flag "blob-id" (required string) ~doc:"STRING ObjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_blob (Values.GetBlobInput.make ~repositoryName ~blobId ())
           (Some Values.GetBlobOutput.to_json)
           (Some Values.GetBlobOutput.error_to_json)])
let get_branch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryName =
         flag "repository-name" (optional string)
           ~doc:"STRING RepositoryName"
       and branchName =
         flag "branch-name" (optional string) ~doc:"STRING BranchName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_branch
           (Values.GetBranchInput.make ?repositoryName ?branchName ())
           (Some Values.GetBranchOutput.to_json)
           (Some Values.GetBranchOutput.error_to_json)])
let get_comment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and commentId =
         flag "comment-id" (required string) ~doc:"STRING CommentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_comment (Values.GetCommentInput.make ~commentId ())
           (Some Values.GetCommentOutput.to_json)
           (Some Values.GetCommentOutput.error_to_json)])
let get_comment_reactions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reactionUserArn =
         flag "reaction-user-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and commentId =
         flag "comment-id" (required string) ~doc:"STRING CommentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_comment_reactions
           (Values.GetCommentReactionsInput.make ?reactionUserArn ?nextToken
              ?maxResults ~commentId ())
           (Some Values.GetCommentReactionsOutput.to_json)
           (Some Values.GetCommentReactionsOutput.error_to_json)])
let get_comments_for_compared_commit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and beforeCommitId =
         flag "before-commit-id" (optional string) ~doc:"STRING CommitId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and afterCommitId =
         flag "after-commit-id" (required string) ~doc:"STRING CommitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_comments_for_compared_commit
           (Values.GetCommentsForComparedCommitInput.make ?beforeCommitId
              ?nextToken ?maxResults ~repositoryName ~afterCommitId ())
           (Some Values.GetCommentsForComparedCommitOutput.to_json)
           (Some Values.GetCommentsForComparedCommitOutput.error_to_json)])
let get_comments_for_pull_request =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryName =
         flag "repository-name" (optional string)
           ~doc:"STRING RepositoryName"
       and beforeCommitId =
         flag "before-commit-id" (optional string) ~doc:"STRING CommitId"
       and afterCommitId =
         flag "after-commit-id" (optional string) ~doc:"STRING CommitId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_comments_for_pull_request
           (Values.GetCommentsForPullRequestInput.make ?repositoryName
              ?beforeCommitId ?afterCommitId ?nextToken ?maxResults
              ~pullRequestId ())
           (Some Values.GetCommentsForPullRequestOutput.to_json)
           (Some Values.GetCommentsForPullRequestOutput.error_to_json)])
let get_commit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and commitId =
         flag "commit-id" (required string) ~doc:"STRING ObjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_commit
           (Values.GetCommitInput.make ~repositoryName ~commitId ())
           (Some Values.GetCommitOutput.to_json)
           (Some Values.GetCommitOutput.error_to_json)])
let get_differences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and beforeCommitSpecifier =
         flag "before-commit-specifier" (optional string)
           ~doc:"STRING CommitName"
       and beforePath =
         flag "before-path" (optional string) ~doc:"STRING Path"
       and afterPath = flag "after-path" (optional string) ~doc:"STRING Path"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Limit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and afterCommitSpecifier =
         flag "after-commit-specifier" (required string)
           ~doc:"STRING CommitName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_differences
           (Values.GetDifferencesInput.make ?beforeCommitSpecifier
              ?beforePath ?afterPath ?maxResults ?nextToken ~repositoryName
              ~afterCommitSpecifier ())
           (Some Values.GetDifferencesOutput.to_json)
           (Some Values.GetDifferencesOutput.error_to_json)])
let get_file =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and commitSpecifier =
         flag "commit-specifier" (optional string) ~doc:"STRING CommitName"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and filePath = flag "file-path" (required string) ~doc:"STRING Path" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_file
           (Values.GetFileInput.make ?commitSpecifier ~repositoryName
              ~filePath ()) (Some Values.GetFileOutput.to_json)
           (Some Values.GetFileOutput.error_to_json)])
let get_folder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and commitSpecifier =
         flag "commit-specifier" (optional string) ~doc:"STRING CommitName"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and folderPath =
         flag "folder-path" (required string) ~doc:"STRING Path" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_folder
           (Values.GetFolderInput.make ?commitSpecifier ~repositoryName
              ~folderPath ()) (Some Values.GetFolderOutput.to_json)
           (Some Values.GetFolderOutput.error_to_json)])
let get_merge_commit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and conflictDetailLevel =
         flag "conflict-detail-level" (optional json_arg)
           ~doc:"JSON ConflictDetailLevelTypeEnum"
       and conflictResolutionStrategy =
         flag "conflict-resolution-strategy" (optional json_arg)
           ~doc:"JSON ConflictResolutionStrategyTypeEnum"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and sourceCommitSpecifier =
         flag "source-commit-specifier" (required string)
           ~doc:"STRING CommitName"
       and destinationCommitSpecifier =
         flag "destination-commit-specifier" (required string)
           ~doc:"STRING CommitName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_merge_commit
           (Values.GetMergeCommitInput.make
              ?conflictDetailLevel:(Option.map
                                      ~f:Values.ConflictDetailLevelTypeEnum.of_json
                                      conflictDetailLevel)
              ?conflictResolutionStrategy:(Option.map
                                             ~f:Values.ConflictResolutionStrategyTypeEnum.of_json
                                             conflictResolutionStrategy)
              ~repositoryName ~sourceCommitSpecifier
              ~destinationCommitSpecifier ())
           (Some Values.GetMergeCommitOutput.to_json)
           (Some Values.GetMergeCommitOutput.error_to_json)])
let get_merge_conflicts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and conflictDetailLevel =
         flag "conflict-detail-level" (optional json_arg)
           ~doc:"JSON ConflictDetailLevelTypeEnum"
       and maxConflictFiles =
         flag "max-conflict-files" (optional int) ~doc:"INT MaxResults"
       and conflictResolutionStrategy =
         flag "conflict-resolution-strategy" (optional json_arg)
           ~doc:"JSON ConflictResolutionStrategyTypeEnum"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and destinationCommitSpecifier =
         flag "destination-commit-specifier" (required string)
           ~doc:"STRING CommitName"
       and sourceCommitSpecifier =
         flag "source-commit-specifier" (required string)
           ~doc:"STRING CommitName"
       and mergeOption =
         flag "merge-option" (required json_arg)
           ~doc:"JSON MergeOptionTypeEnum" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_merge_conflicts
           (Values.GetMergeConflictsInput.make
              ?conflictDetailLevel:(Option.map
                                      ~f:Values.ConflictDetailLevelTypeEnum.of_json
                                      conflictDetailLevel) ?maxConflictFiles
              ?conflictResolutionStrategy:(Option.map
                                             ~f:Values.ConflictResolutionStrategyTypeEnum.of_json
                                             conflictResolutionStrategy)
              ?nextToken ~repositoryName ~destinationCommitSpecifier
              ~sourceCommitSpecifier
              ~mergeOption:(Values.MergeOptionTypeEnum.of_json mergeOption)
              ()) (Some Values.GetMergeConflictsOutput.to_json)
           (Some Values.GetMergeConflictsOutput.error_to_json)])
let get_merge_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and conflictDetailLevel =
         flag "conflict-detail-level" (optional json_arg)
           ~doc:"JSON ConflictDetailLevelTypeEnum"
       and conflictResolutionStrategy =
         flag "conflict-resolution-strategy" (optional json_arg)
           ~doc:"JSON ConflictResolutionStrategyTypeEnum"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and sourceCommitSpecifier =
         flag "source-commit-specifier" (required string)
           ~doc:"STRING CommitName"
       and destinationCommitSpecifier =
         flag "destination-commit-specifier" (required string)
           ~doc:"STRING CommitName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_merge_options
           (Values.GetMergeOptionsInput.make
              ?conflictDetailLevel:(Option.map
                                      ~f:Values.ConflictDetailLevelTypeEnum.of_json
                                      conflictDetailLevel)
              ?conflictResolutionStrategy:(Option.map
                                             ~f:Values.ConflictResolutionStrategyTypeEnum.of_json
                                             conflictResolutionStrategy)
              ~repositoryName ~sourceCommitSpecifier
              ~destinationCommitSpecifier ())
           (Some Values.GetMergeOptionsOutput.to_json)
           (Some Values.GetMergeOptionsOutput.error_to_json)])
let get_pull_request =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_pull_request
           (Values.GetPullRequestInput.make ~pullRequestId ())
           (Some Values.GetPullRequestOutput.to_json)
           (Some Values.GetPullRequestOutput.error_to_json)])
let get_pull_request_approval_states =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and revisionId =
         flag "revision-id" (required string) ~doc:"STRING RevisionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_pull_request_approval_states
           (Values.GetPullRequestApprovalStatesInput.make ~pullRequestId
              ~revisionId ())
           (Some Values.GetPullRequestApprovalStatesOutput.to_json)
           (Some Values.GetPullRequestApprovalStatesOutput.error_to_json)])
let get_pull_request_override_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and revisionId =
         flag "revision-id" (required string) ~doc:"STRING RevisionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_pull_request_override_state
           (Values.GetPullRequestOverrideStateInput.make ~pullRequestId
              ~revisionId ())
           (Some Values.GetPullRequestOverrideStateOutput.to_json)
           (Some Values.GetPullRequestOverrideStateOutput.error_to_json)])
let get_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_repository
           (Values.GetRepositoryInput.make ~repositoryName ())
           (Some Values.GetRepositoryOutput.to_json)
           (Some Values.GetRepositoryOutput.error_to_json)])
let get_repository_triggers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_repository_triggers
           (Values.GetRepositoryTriggersInput.make ~repositoryName ())
           (Some Values.GetRepositoryTriggersOutput.to_json)
           (Some Values.GetRepositoryTriggersOutput.error_to_json)])
let list_approval_rule_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_approval_rule_templates
           (Values.ListApprovalRuleTemplatesInput.make ?nextToken ?maxResults
              ()) (Some Values.ListApprovalRuleTemplatesOutput.to_json)
           (Some Values.ListApprovalRuleTemplatesOutput.error_to_json)])
let list_associated_approval_rule_templates_for_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_associated_approval_rule_templates_for_repository
           (Values.ListAssociatedApprovalRuleTemplatesForRepositoryInput.make
              ?nextToken ?maxResults ~repositoryName ())
           (Some
              Values.ListAssociatedApprovalRuleTemplatesForRepositoryOutput.to_json)
           (Some
              Values.ListAssociatedApprovalRuleTemplatesForRepositoryOutput.error_to_json)])
let list_branches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_branches
           (Values.ListBranchesInput.make ?nextToken ~repositoryName ())
           (Some Values.ListBranchesOutput.to_json)
           (Some Values.ListBranchesOutput.error_to_json)])
let list_pull_requests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorArn = flag "author-arn" (optional string) ~doc:"STRING Arn"
       and pullRequestStatus =
         flag "pull-request-status" (optional json_arg)
           ~doc:"JSON PullRequestStatusEnum"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pull_requests
           (Values.ListPullRequestsInput.make ?authorArn
              ?pullRequestStatus:(Option.map
                                    ~f:Values.PullRequestStatusEnum.of_json
                                    pullRequestStatus) ?nextToken ?maxResults
              ~repositoryName ())
           (Some Values.ListPullRequestsOutput.to_json)
           (Some Values.ListPullRequestsOutput.error_to_json)])
let list_repositories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SortByEnum"
       and order = flag "order" (optional json_arg) ~doc:"JSON OrderEnum" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_repositories
           (Values.ListRepositoriesInput.make ?nextToken
              ?sortBy:(Option.map ~f:Values.SortByEnum.of_json sortBy)
              ?order:(Option.map ~f:Values.OrderEnum.of_json order) ())
           (Some Values.ListRepositoriesOutput.to_json)
           (Some Values.ListRepositoriesOutput.error_to_json)])
let list_repositories_for_approval_rule_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and approvalRuleTemplateName =
         flag "approval-rule-template-name" (required string)
           ~doc:"STRING ApprovalRuleTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_repositories_for_approval_rule_template
           (Values.ListRepositoriesForApprovalRuleTemplateInput.make
              ?nextToken ?maxResults ~approvalRuleTemplateName ())
           (Some Values.ListRepositoriesForApprovalRuleTemplateOutput.to_json)
           (Some
              Values.ListRepositoriesForApprovalRuleTemplateOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ?nextToken ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let merge_branches_by_fast_forward =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetBranch =
         flag "target-branch" (optional string) ~doc:"STRING BranchName"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and sourceCommitSpecifier =
         flag "source-commit-specifier" (required string)
           ~doc:"STRING CommitName"
       and destinationCommitSpecifier =
         flag "destination-commit-specifier" (required string)
           ~doc:"STRING CommitName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.merge_branches_by_fast_forward
           (Values.MergeBranchesByFastForwardInput.make ?targetBranch
              ~repositoryName ~sourceCommitSpecifier
              ~destinationCommitSpecifier ())
           (Some Values.MergeBranchesByFastForwardOutput.to_json)
           (Some Values.MergeBranchesByFastForwardOutput.error_to_json)])
let merge_branches_by_squash =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetBranch =
         flag "target-branch" (optional string) ~doc:"STRING BranchName"
       and conflictDetailLevel =
         flag "conflict-detail-level" (optional json_arg)
           ~doc:"JSON ConflictDetailLevelTypeEnum"
       and conflictResolutionStrategy =
         flag "conflict-resolution-strategy" (optional json_arg)
           ~doc:"JSON ConflictResolutionStrategyTypeEnum"
       and authorName =
         flag "author-name" (optional string) ~doc:"STRING Name"
       and email = flag "email" (optional string) ~doc:"STRING Email"
       and commitMessage =
         flag "commit-message" (optional string) ~doc:"STRING Message"
       and keepEmptyFolders =
         flag "keep-empty-folders" (optional bool)
           ~doc:"BOOL KeepEmptyFolders"
       and conflictResolution =
         flag "conflict-resolution" (optional json_arg)
           ~doc:"JSON ConflictResolution"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and sourceCommitSpecifier =
         flag "source-commit-specifier" (required string)
           ~doc:"STRING CommitName"
       and destinationCommitSpecifier =
         flag "destination-commit-specifier" (required string)
           ~doc:"STRING CommitName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.merge_branches_by_squash
           (Values.MergeBranchesBySquashInput.make ?targetBranch
              ?conflictDetailLevel:(Option.map
                                      ~f:Values.ConflictDetailLevelTypeEnum.of_json
                                      conflictDetailLevel)
              ?conflictResolutionStrategy:(Option.map
                                             ~f:Values.ConflictResolutionStrategyTypeEnum.of_json
                                             conflictResolutionStrategy)
              ?authorName ?email ?commitMessage ?keepEmptyFolders
              ?conflictResolution:(Option.map
                                     ~f:Values.ConflictResolution.of_json
                                     conflictResolution) ~repositoryName
              ~sourceCommitSpecifier ~destinationCommitSpecifier ())
           (Some Values.MergeBranchesBySquashOutput.to_json)
           (Some Values.MergeBranchesBySquashOutput.error_to_json)])
let merge_branches_by_three_way =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetBranch =
         flag "target-branch" (optional string) ~doc:"STRING BranchName"
       and conflictDetailLevel =
         flag "conflict-detail-level" (optional json_arg)
           ~doc:"JSON ConflictDetailLevelTypeEnum"
       and conflictResolutionStrategy =
         flag "conflict-resolution-strategy" (optional json_arg)
           ~doc:"JSON ConflictResolutionStrategyTypeEnum"
       and authorName =
         flag "author-name" (optional string) ~doc:"STRING Name"
       and email = flag "email" (optional string) ~doc:"STRING Email"
       and commitMessage =
         flag "commit-message" (optional string) ~doc:"STRING Message"
       and keepEmptyFolders =
         flag "keep-empty-folders" (optional bool)
           ~doc:"BOOL KeepEmptyFolders"
       and conflictResolution =
         flag "conflict-resolution" (optional json_arg)
           ~doc:"JSON ConflictResolution"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and sourceCommitSpecifier =
         flag "source-commit-specifier" (required string)
           ~doc:"STRING CommitName"
       and destinationCommitSpecifier =
         flag "destination-commit-specifier" (required string)
           ~doc:"STRING CommitName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.merge_branches_by_three_way
           (Values.MergeBranchesByThreeWayInput.make ?targetBranch
              ?conflictDetailLevel:(Option.map
                                      ~f:Values.ConflictDetailLevelTypeEnum.of_json
                                      conflictDetailLevel)
              ?conflictResolutionStrategy:(Option.map
                                             ~f:Values.ConflictResolutionStrategyTypeEnum.of_json
                                             conflictResolutionStrategy)
              ?authorName ?email ?commitMessage ?keepEmptyFolders
              ?conflictResolution:(Option.map
                                     ~f:Values.ConflictResolution.of_json
                                     conflictResolution) ~repositoryName
              ~sourceCommitSpecifier ~destinationCommitSpecifier ())
           (Some Values.MergeBranchesByThreeWayOutput.to_json)
           (Some Values.MergeBranchesByThreeWayOutput.error_to_json)])
let merge_pull_request_by_fast_forward =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceCommitId =
         flag "source-commit-id" (optional string) ~doc:"STRING ObjectId"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.merge_pull_request_by_fast_forward
           (Values.MergePullRequestByFastForwardInput.make ?sourceCommitId
              ~pullRequestId ~repositoryName ())
           (Some Values.MergePullRequestByFastForwardOutput.to_json)
           (Some Values.MergePullRequestByFastForwardOutput.error_to_json)])
let merge_pull_request_by_squash =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceCommitId =
         flag "source-commit-id" (optional string) ~doc:"STRING ObjectId"
       and conflictDetailLevel =
         flag "conflict-detail-level" (optional json_arg)
           ~doc:"JSON ConflictDetailLevelTypeEnum"
       and conflictResolutionStrategy =
         flag "conflict-resolution-strategy" (optional json_arg)
           ~doc:"JSON ConflictResolutionStrategyTypeEnum"
       and commitMessage =
         flag "commit-message" (optional string) ~doc:"STRING Message"
       and authorName =
         flag "author-name" (optional string) ~doc:"STRING Name"
       and email = flag "email" (optional string) ~doc:"STRING Email"
       and keepEmptyFolders =
         flag "keep-empty-folders" (optional bool)
           ~doc:"BOOL KeepEmptyFolders"
       and conflictResolution =
         flag "conflict-resolution" (optional json_arg)
           ~doc:"JSON ConflictResolution"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.merge_pull_request_by_squash
           (Values.MergePullRequestBySquashInput.make ?sourceCommitId
              ?conflictDetailLevel:(Option.map
                                      ~f:Values.ConflictDetailLevelTypeEnum.of_json
                                      conflictDetailLevel)
              ?conflictResolutionStrategy:(Option.map
                                             ~f:Values.ConflictResolutionStrategyTypeEnum.of_json
                                             conflictResolutionStrategy)
              ?commitMessage ?authorName ?email ?keepEmptyFolders
              ?conflictResolution:(Option.map
                                     ~f:Values.ConflictResolution.of_json
                                     conflictResolution) ~pullRequestId
              ~repositoryName ())
           (Some Values.MergePullRequestBySquashOutput.to_json)
           (Some Values.MergePullRequestBySquashOutput.error_to_json)])
let merge_pull_request_by_three_way =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceCommitId =
         flag "source-commit-id" (optional string) ~doc:"STRING ObjectId"
       and conflictDetailLevel =
         flag "conflict-detail-level" (optional json_arg)
           ~doc:"JSON ConflictDetailLevelTypeEnum"
       and conflictResolutionStrategy =
         flag "conflict-resolution-strategy" (optional json_arg)
           ~doc:"JSON ConflictResolutionStrategyTypeEnum"
       and commitMessage =
         flag "commit-message" (optional string) ~doc:"STRING Message"
       and authorName =
         flag "author-name" (optional string) ~doc:"STRING Name"
       and email = flag "email" (optional string) ~doc:"STRING Email"
       and keepEmptyFolders =
         flag "keep-empty-folders" (optional bool)
           ~doc:"BOOL KeepEmptyFolders"
       and conflictResolution =
         flag "conflict-resolution" (optional json_arg)
           ~doc:"JSON ConflictResolution"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.merge_pull_request_by_three_way
           (Values.MergePullRequestByThreeWayInput.make ?sourceCommitId
              ?conflictDetailLevel:(Option.map
                                      ~f:Values.ConflictDetailLevelTypeEnum.of_json
                                      conflictDetailLevel)
              ?conflictResolutionStrategy:(Option.map
                                             ~f:Values.ConflictResolutionStrategyTypeEnum.of_json
                                             conflictResolutionStrategy)
              ?commitMessage ?authorName ?email ?keepEmptyFolders
              ?conflictResolution:(Option.map
                                     ~f:Values.ConflictResolution.of_json
                                     conflictResolution) ~pullRequestId
              ~repositoryName ())
           (Some Values.MergePullRequestByThreeWayOutput.to_json)
           (Some Values.MergePullRequestByThreeWayOutput.error_to_json)])
let override_pull_request_approval_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and revisionId =
         flag "revision-id" (required string) ~doc:"STRING RevisionId"
       and overrideStatus =
         flag "override-status" (required json_arg)
           ~doc:"JSON OverrideStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.override_pull_request_approval_rules
           (Values.OverridePullRequestApprovalRulesInput.make ~pullRequestId
              ~revisionId
              ~overrideStatus:(Values.OverrideStatus.of_json overrideStatus)
              ()) None None])
let post_comment_for_compared_commit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and beforeCommitId =
         flag "before-commit-id" (optional string) ~doc:"STRING CommitId"
       and location =
         flag "location" (optional json_arg) ~doc:"JSON Location"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and afterCommitId =
         flag "after-commit-id" (required string) ~doc:"STRING CommitId"
       and content = flag "content" (required string) ~doc:"STRING Content" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.post_comment_for_compared_commit
           (Values.PostCommentForComparedCommitInput.make ?beforeCommitId
              ?location:(Option.map ~f:Values.Location.of_json location)
              ?clientRequestToken ~repositoryName ~afterCommitId ~content ())
           (Some Values.PostCommentForComparedCommitOutput.to_json)
           (Some Values.PostCommentForComparedCommitOutput.error_to_json)])
let post_comment_for_pull_request =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and location =
         flag "location" (optional json_arg) ~doc:"JSON Location"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and beforeCommitId =
         flag "before-commit-id" (required string) ~doc:"STRING CommitId"
       and afterCommitId =
         flag "after-commit-id" (required string) ~doc:"STRING CommitId"
       and content = flag "content" (required string) ~doc:"STRING Content" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.post_comment_for_pull_request
           (Values.PostCommentForPullRequestInput.make
              ?location:(Option.map ~f:Values.Location.of_json location)
              ?clientRequestToken ~pullRequestId ~repositoryName
              ~beforeCommitId ~afterCommitId ~content ())
           (Some Values.PostCommentForPullRequestOutput.to_json)
           (Some Values.PostCommentForPullRequestOutput.error_to_json)])
let post_comment_reply =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and inReplyTo =
         flag "in-reply-to" (required string) ~doc:"STRING CommentId"
       and content = flag "content" (required string) ~doc:"STRING Content" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.post_comment_reply
           (Values.PostCommentReplyInput.make ?clientRequestToken ~inReplyTo
              ~content ()) (Some Values.PostCommentReplyOutput.to_json)
           (Some Values.PostCommentReplyOutput.error_to_json)])
let put_comment_reaction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and commentId =
         flag "comment-id" (required string) ~doc:"STRING CommentId"
       and reactionValue =
         flag "reaction-value" (required string) ~doc:"STRING ReactionValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_comment_reaction
           (Values.PutCommentReactionInput.make ~commentId ~reactionValue ())
           None None])
let put_file =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileMode =
         flag "file-mode" (optional json_arg) ~doc:"JSON FileModeTypeEnum"
       and parentCommitId =
         flag "parent-commit-id" (optional string) ~doc:"STRING CommitId"
       and commitMessage =
         flag "commit-message" (optional string) ~doc:"STRING Message"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and email = flag "email" (optional string) ~doc:"STRING Email"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName"
       and fileContent =
         flag "file-content" (required json_arg) ~doc:"JSON FileContent"
       and filePath = flag "file-path" (required string) ~doc:"STRING Path" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_file
           (Values.PutFileInput.make
              ?fileMode:(Option.map ~f:Values.FileModeTypeEnum.of_json
                           fileMode) ?parentCommitId ?commitMessage ?name
              ?email ~repositoryName ~branchName
              ~fileContent:(Values.FileContent.of_json fileContent) ~filePath
              ()) (Some Values.PutFileOutput.to_json)
           (Some Values.PutFileOutput.error_to_json)])
let put_repository_triggers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and triggers =
         flag "triggers" (required json_arg)
           ~doc:"JSON RepositoryTriggersList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_repository_triggers
           (Values.PutRepositoryTriggersInput.make ~repositoryName
              ~triggers:(Values.RepositoryTriggersList.of_json triggers) ())
           (Some Values.PutRepositoryTriggersOutput.to_json)
           (Some Values.PutRepositoryTriggersOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagsMap.of_json tags) ()) None None])
let test_repository_triggers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and triggers =
         flag "triggers" (required json_arg)
           ~doc:"JSON RepositoryTriggersList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_repository_triggers
           (Values.TestRepositoryTriggersInput.make ~repositoryName
              ~triggers:(Values.RepositoryTriggersList.of_json triggers) ())
           (Some Values.TestRepositoryTriggersOutput.to_json)
           (Some Values.TestRepositoryTriggersOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeysList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeysList.of_json tagKeys) ()) None None])
let update_approval_rule_template_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and existingRuleContentSha256 =
         flag "existing-rule-content-sha256" (optional string)
           ~doc:"STRING RuleContentSha256"
       and approvalRuleTemplateName =
         flag "approval-rule-template-name" (required string)
           ~doc:"STRING ApprovalRuleTemplateName"
       and newRuleContent =
         flag "new-rule-content" (required string)
           ~doc:"STRING ApprovalRuleTemplateContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_approval_rule_template_content
           (Values.UpdateApprovalRuleTemplateContentInput.make
              ?existingRuleContentSha256 ~approvalRuleTemplateName
              ~newRuleContent ())
           (Some Values.UpdateApprovalRuleTemplateContentOutput.to_json)
           (Some Values.UpdateApprovalRuleTemplateContentOutput.error_to_json)])
let update_approval_rule_template_description =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and approvalRuleTemplateName =
         flag "approval-rule-template-name" (required string)
           ~doc:"STRING ApprovalRuleTemplateName"
       and approvalRuleTemplateDescription =
         flag "approval-rule-template-description" (required string)
           ~doc:"STRING ApprovalRuleTemplateDescription" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_approval_rule_template_description
           (Values.UpdateApprovalRuleTemplateDescriptionInput.make
              ~approvalRuleTemplateName ~approvalRuleTemplateDescription ())
           (Some Values.UpdateApprovalRuleTemplateDescriptionOutput.to_json)
           (Some
              Values.UpdateApprovalRuleTemplateDescriptionOutput.error_to_json)])
let update_approval_rule_template_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and oldApprovalRuleTemplateName =
         flag "old-approval-rule-template-name" (required string)
           ~doc:"STRING ApprovalRuleTemplateName"
       and newApprovalRuleTemplateName =
         flag "new-approval-rule-template-name" (required string)
           ~doc:"STRING ApprovalRuleTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_approval_rule_template_name
           (Values.UpdateApprovalRuleTemplateNameInput.make
              ~oldApprovalRuleTemplateName ~newApprovalRuleTemplateName ())
           (Some Values.UpdateApprovalRuleTemplateNameOutput.to_json)
           (Some Values.UpdateApprovalRuleTemplateNameOutput.error_to_json)])
let update_comment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and commentId =
         flag "comment-id" (required string) ~doc:"STRING CommentId"
       and content = flag "content" (required string) ~doc:"STRING Content" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_comment
           (Values.UpdateCommentInput.make ~commentId ~content ())
           (Some Values.UpdateCommentOutput.to_json)
           (Some Values.UpdateCommentOutput.error_to_json)])
let update_default_branch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and defaultBranchName =
         flag "default-branch-name" (required string)
           ~doc:"STRING BranchName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_default_branch
           (Values.UpdateDefaultBranchInput.make ~repositoryName
              ~defaultBranchName ()) None None])
let update_pull_request_approval_rule_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and existingRuleContentSha256 =
         flag "existing-rule-content-sha256" (optional string)
           ~doc:"STRING RuleContentSha256"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and approvalRuleName =
         flag "approval-rule-name" (required string)
           ~doc:"STRING ApprovalRuleName"
       and newRuleContent =
         flag "new-rule-content" (required string)
           ~doc:"STRING ApprovalRuleContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pull_request_approval_rule_content
           (Values.UpdatePullRequestApprovalRuleContentInput.make
              ?existingRuleContentSha256 ~pullRequestId ~approvalRuleName
              ~newRuleContent ())
           (Some Values.UpdatePullRequestApprovalRuleContentOutput.to_json)
           (Some
              Values.UpdatePullRequestApprovalRuleContentOutput.error_to_json)])
let update_pull_request_approval_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and revisionId =
         flag "revision-id" (required string) ~doc:"STRING RevisionId"
       and approvalState =
         flag "approval-state" (required json_arg) ~doc:"JSON ApprovalState" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pull_request_approval_state
           (Values.UpdatePullRequestApprovalStateInput.make ~pullRequestId
              ~revisionId
              ~approvalState:(Values.ApprovalState.of_json approvalState) ())
           None None])
let update_pull_request_description =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and description =
         flag "description" (required string) ~doc:"STRING Description" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pull_request_description
           (Values.UpdatePullRequestDescriptionInput.make ~pullRequestId
              ~description ())
           (Some Values.UpdatePullRequestDescriptionOutput.to_json)
           (Some Values.UpdatePullRequestDescriptionOutput.error_to_json)])
let update_pull_request_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and pullRequestStatus =
         flag "pull-request-status" (required json_arg)
           ~doc:"JSON PullRequestStatusEnum" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pull_request_status
           (Values.UpdatePullRequestStatusInput.make ~pullRequestId
              ~pullRequestStatus:(Values.PullRequestStatusEnum.of_json
                                    pullRequestStatus) ())
           (Some Values.UpdatePullRequestStatusOutput.to_json)
           (Some Values.UpdatePullRequestStatusOutput.error_to_json)])
let update_pull_request_title =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pullRequestId =
         flag "pull-request-id" (required string) ~doc:"STRING PullRequestId"
       and title = flag "title" (required string) ~doc:"STRING Title" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pull_request_title
           (Values.UpdatePullRequestTitleInput.make ~pullRequestId ~title ())
           (Some Values.UpdatePullRequestTitleOutput.to_json)
           (Some Values.UpdatePullRequestTitleOutput.error_to_json)])
let update_repository_description =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryDescription =
         flag "repository-description" (optional string)
           ~doc:"STRING RepositoryDescription"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_repository_description
           (Values.UpdateRepositoryDescriptionInput.make
              ?repositoryDescription ~repositoryName ()) None None])
let update_repository_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and oldName =
         flag "old-name" (required string) ~doc:"STRING RepositoryName"
       and newName =
         flag "new-name" (required string) ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_repository_name
           (Values.UpdateRepositoryNameInput.make ~oldName ~newName ()) None
           None])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-approval-rule-template-with-repository",
       associate_approval_rule_template_with_repository);
    ("batch-associate-approval-rule-template-with-repositories",
      batch_associate_approval_rule_template_with_repositories);
    ("batch-describe-merge-conflicts", batch_describe_merge_conflicts);
    ("batch-disassociate-approval-rule-template-from-repositories",
      batch_disassociate_approval_rule_template_from_repositories);
    ("batch-get-commits", batch_get_commits);
    ("batch-get-repositories", batch_get_repositories);
    ("create-approval-rule-template", create_approval_rule_template);
    ("create-branch", create_branch);
    ("create-commit", create_commit);
    ("create-pull-request", create_pull_request);
    ("create-pull-request-approval-rule", create_pull_request_approval_rule);
    ("create-repository", create_repository);
    ("create-unreferenced-merge-commit", create_unreferenced_merge_commit);
    ("delete-approval-rule-template", delete_approval_rule_template);
    ("delete-branch", delete_branch);
    ("delete-comment-content", delete_comment_content);
    ("delete-file", delete_file);
    ("delete-pull-request-approval-rule", delete_pull_request_approval_rule);
    ("delete-repository", delete_repository);
    ("describe-merge-conflicts", describe_merge_conflicts);
    ("describe-pull-request-events", describe_pull_request_events);
    ("disassociate-approval-rule-template-from-repository",
      disassociate_approval_rule_template_from_repository);
    ("evaluate-pull-request-approval-rules",
      evaluate_pull_request_approval_rules);
    ("get-approval-rule-template", get_approval_rule_template);
    ("get-blob", get_blob);
    ("get-branch", get_branch);
    ("get-comment", get_comment);
    ("get-comment-reactions", get_comment_reactions);
    ("get-comments-for-compared-commit", get_comments_for_compared_commit);
    ("get-comments-for-pull-request", get_comments_for_pull_request);
    ("get-commit", get_commit);
    ("get-differences", get_differences);
    ("get-file", get_file);
    ("get-folder", get_folder);
    ("get-merge-commit", get_merge_commit);
    ("get-merge-conflicts", get_merge_conflicts);
    ("get-merge-options", get_merge_options);
    ("get-pull-request", get_pull_request);
    ("get-pull-request-approval-states", get_pull_request_approval_states);
    ("get-pull-request-override-state", get_pull_request_override_state);
    ("get-repository", get_repository);
    ("get-repository-triggers", get_repository_triggers);
    ("list-approval-rule-templates", list_approval_rule_templates);
    ("list-associated-approval-rule-templates-for-repository",
      list_associated_approval_rule_templates_for_repository);
    ("list-branches", list_branches);
    ("list-pull-requests", list_pull_requests);
    ("list-repositories", list_repositories);
    ("list-repositories-for-approval-rule-template",
      list_repositories_for_approval_rule_template);
    ("list-tags-for-resource", list_tags_for_resource);
    ("merge-branches-by-fast-forward", merge_branches_by_fast_forward);
    ("merge-branches-by-squash", merge_branches_by_squash);
    ("merge-branches-by-three-way", merge_branches_by_three_way);
    ("merge-pull-request-by-fast-forward",
      merge_pull_request_by_fast_forward);
    ("merge-pull-request-by-squash", merge_pull_request_by_squash);
    ("merge-pull-request-by-three-way", merge_pull_request_by_three_way);
    ("override-pull-request-approval-rules",
      override_pull_request_approval_rules);
    ("post-comment-for-compared-commit", post_comment_for_compared_commit);
    ("post-comment-for-pull-request", post_comment_for_pull_request);
    ("post-comment-reply", post_comment_reply);
    ("put-comment-reaction", put_comment_reaction);
    ("put-file", put_file);
    ("put-repository-triggers", put_repository_triggers);
    ("tag-resource", tag_resource);
    ("test-repository-triggers", test_repository_triggers);
    ("untag-resource", untag_resource);
    ("update-approval-rule-template-content",
      update_approval_rule_template_content);
    ("update-approval-rule-template-description",
      update_approval_rule_template_description);
    ("update-approval-rule-template-name",
      update_approval_rule_template_name);
    ("update-comment", update_comment);
    ("update-default-branch", update_default_branch);
    ("update-pull-request-approval-rule-content",
      update_pull_request_approval_rule_content);
    ("update-pull-request-approval-state",
      update_pull_request_approval_state);
    ("update-pull-request-description", update_pull_request_description);
    ("update-pull-request-status", update_pull_request_status);
    ("update-pull-request-title", update_pull_request_title);
    ("update-repository-description", update_repository_description);
    ("update-repository-name", update_repository_name)]
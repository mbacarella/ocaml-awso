(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_lenses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and lensAliases =
         flag "lens-aliases" (required json_arg) ~doc:"JSON LensAliases" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_lenses
           (Values.AssociateLensesInput.make ~workloadId
              ~lensAliases:(Values.LensAliases.of_json lensAliases) ()) None
           None])
let create_lens_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias"
       and sharedWith =
         flag "shared-with" (required string) ~doc:"STRING SharedWith"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_lens_share
           (Values.CreateLensShareInput.make ~lensAlias ~sharedWith
              ~clientRequestToken ())
           (Some Values.CreateLensShareOutput.to_json)
           (Some Values.CreateLensShareOutput.error_to_json)])
let create_lens_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and isMajorVersion =
         flag "is-major-version" (optional bool) ~doc:"BOOL IsMajorVersion"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias"
       and lensVersion =
         flag "lens-version" (required string) ~doc:"STRING LensVersion"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_lens_version
           (Values.CreateLensVersionInput.make ?isMajorVersion ~lensAlias
              ~lensVersion ~clientRequestToken ())
           (Some Values.CreateLensVersionOutput.to_json)
           (Some Values.CreateLensVersionOutput.error_to_json)])
let create_milestone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and milestoneName =
         flag "milestone-name" (required string) ~doc:"STRING MilestoneName"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_milestone
           (Values.CreateMilestoneInput.make ~workloadId ~milestoneName
              ~clientRequestToken ())
           (Some Values.CreateMilestoneOutput.to_json)
           (Some Values.CreateMilestoneOutput.error_to_json)])
let create_workload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg)
           ~doc:"JSON WorkloadAccountIds"
       and awsRegions =
         flag "aws-regions" (optional json_arg)
           ~doc:"JSON WorkloadAwsRegions"
       and nonAwsRegions =
         flag "non-aws-regions" (optional json_arg)
           ~doc:"JSON WorkloadNonAwsRegions"
       and pillarPriorities =
         flag "pillar-priorities" (optional json_arg)
           ~doc:"JSON WorkloadPillarPriorities"
       and architecturalDesign =
         flag "architectural-design" (optional string)
           ~doc:"STRING WorkloadArchitecturalDesign"
       and industryType =
         flag "industry-type" (optional string)
           ~doc:"STRING WorkloadIndustryType"
       and industry =
         flag "industry" (optional string) ~doc:"STRING WorkloadIndustry"
       and notes = flag "notes" (optional string) ~doc:"STRING Notes"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and workloadName =
         flag "workload-name" (required string) ~doc:"STRING WorkloadName"
       and description =
         flag "description" (required string)
           ~doc:"STRING WorkloadDescription"
       and environment =
         flag "environment" (required json_arg)
           ~doc:"JSON WorkloadEnvironment"
       and reviewOwner =
         flag "review-owner" (required string)
           ~doc:"STRING WorkloadReviewOwner"
       and lenses =
         flag "lenses" (required json_arg) ~doc:"JSON WorkloadLenses"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workload
           (Values.CreateWorkloadInput.make
              ?accountIds:(Option.map ~f:Values.WorkloadAccountIds.of_json
                             accountIds)
              ?awsRegions:(Option.map ~f:Values.WorkloadAwsRegions.of_json
                             awsRegions)
              ?nonAwsRegions:(Option.map
                                ~f:Values.WorkloadNonAwsRegions.of_json
                                nonAwsRegions)
              ?pillarPriorities:(Option.map
                                   ~f:Values.WorkloadPillarPriorities.of_json
                                   pillarPriorities) ?architecturalDesign
              ?industryType ?industry ?notes
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~workloadName
              ~description
              ~environment:(Values.WorkloadEnvironment.of_json environment)
              ~reviewOwner ~lenses:(Values.WorkloadLenses.of_json lenses)
              ~clientRequestToken ())
           (Some Values.CreateWorkloadOutput.to_json)
           (Some Values.CreateWorkloadOutput.error_to_json)])
let create_workload_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and sharedWith =
         flag "shared-with" (required string) ~doc:"STRING SharedWith"
       and permissionType =
         flag "permission-type" (required json_arg)
           ~doc:"JSON PermissionType"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workload_share
           (Values.CreateWorkloadShareInput.make ~workloadId ~sharedWith
              ~permissionType:(Values.PermissionType.of_json permissionType)
              ~clientRequestToken ())
           (Some Values.CreateWorkloadShareOutput.to_json)
           (Some Values.CreateWorkloadShareOutput.error_to_json)])
let delete_lens =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken"
       and lensStatus =
         flag "lens-status" (required json_arg) ~doc:"JSON LensStatusType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_lens
           (Values.DeleteLensInput.make ~lensAlias ~clientRequestToken
              ~lensStatus:(Values.LensStatusType.of_json lensStatus) ()) None
           None])
let delete_lens_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shareId = flag "share-id" (required string) ~doc:"STRING ShareId"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_lens_share
           (Values.DeleteLensShareInput.make ~shareId ~lensAlias
              ~clientRequestToken ()) None None])
let delete_workload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workload
           (Values.DeleteWorkloadInput.make ~workloadId ~clientRequestToken
              ()) None None])
let delete_workload_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shareId = flag "share-id" (required string) ~doc:"STRING ShareId"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workload_share
           (Values.DeleteWorkloadShareInput.make ~shareId ~workloadId
              ~clientRequestToken ()) None None])
let disassociate_lenses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and lensAliases =
         flag "lens-aliases" (required json_arg) ~doc:"JSON LensAliases" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_lenses
           (Values.DisassociateLensesInput.make ~workloadId
              ~lensAliases:(Values.LensAliases.of_json lensAliases) ()) None
           None])
let export_lens =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lensVersion =
         flag "lens-version" (optional string) ~doc:"STRING LensVersion"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_lens
           (Values.ExportLensInput.make ?lensVersion ~lensAlias ())
           (Some Values.ExportLensOutput.to_json)
           (Some Values.ExportLensOutput.error_to_json)])
let get_answer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and milestoneNumber =
         flag "milestone-number" (optional int) ~doc:"INT MilestoneNumber"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias"
       and questionId =
         flag "question-id" (required string) ~doc:"STRING QuestionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_answer
           (Values.GetAnswerInput.make ?milestoneNumber ~workloadId
              ~lensAlias ~questionId ())
           (Some Values.GetAnswerOutput.to_json)
           (Some Values.GetAnswerOutput.error_to_json)])
let get_lens =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lensVersion =
         flag "lens-version" (optional string) ~doc:"STRING LensVersion"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_lens (Values.GetLensInput.make ?lensVersion ~lensAlias ())
           (Some Values.GetLensOutput.to_json)
           (Some Values.GetLensOutput.error_to_json)])
let get_lens_review =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and milestoneNumber =
         flag "milestone-number" (optional int) ~doc:"INT MilestoneNumber"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_lens_review
           (Values.GetLensReviewInput.make ?milestoneNumber ~workloadId
              ~lensAlias ()) (Some Values.GetLensReviewOutput.to_json)
           (Some Values.GetLensReviewOutput.error_to_json)])
let get_lens_review_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and milestoneNumber =
         flag "milestone-number" (optional int) ~doc:"INT MilestoneNumber"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_lens_review_report
           (Values.GetLensReviewReportInput.make ?milestoneNumber ~workloadId
              ~lensAlias ()) (Some Values.GetLensReviewReportOutput.to_json)
           (Some Values.GetLensReviewReportOutput.error_to_json)])
let get_lens_version_difference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and baseLensVersion =
         flag "base-lens-version" (optional string) ~doc:"STRING LensVersion"
       and targetLensVersion =
         flag "target-lens-version" (optional string)
           ~doc:"STRING LensVersion"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_lens_version_difference
           (Values.GetLensVersionDifferenceInput.make ?baseLensVersion
              ?targetLensVersion ~lensAlias ())
           (Some Values.GetLensVersionDifferenceOutput.to_json)
           (Some Values.GetLensVersionDifferenceOutput.error_to_json)])
let get_milestone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and milestoneNumber =
         flag "milestone-number" (required int) ~doc:"INT MilestoneNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_milestone
           (Values.GetMilestoneInput.make ~workloadId ~milestoneNumber ())
           (Some Values.GetMilestoneOutput.to_json)
           (Some Values.GetMilestoneOutput.error_to_json)])
let get_workload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workload (Values.GetWorkloadInput.make ~workloadId ())
           (Some Values.GetWorkloadOutput.to_json)
           (Some Values.GetWorkloadOutput.error_to_json)])
let import_lens =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lensAlias =
         flag "lens-alias" (optional string) ~doc:"STRING LensAlias"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and jSONString =
         flag "j-s-o-n-string" (required string) ~doc:"STRING LensJSON"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_lens
           (Values.ImportLensInput.make ?lensAlias
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~jSONString
              ~clientRequestToken ()) (Some Values.ImportLensOutput.to_json)
           (Some Values.ImportLensOutput.error_to_json)])
let list_answers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pillarId =
         flag "pillar-id" (optional string) ~doc:"STRING PillarId"
       and milestoneNumber =
         flag "milestone-number" (optional int) ~doc:"INT MilestoneNumber"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListAnswersMaxResults"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_answers
           (Values.ListAnswersInput.make ?pillarId ?milestoneNumber
              ?nextToken ?maxResults ~workloadId ~lensAlias ())
           (Some Values.ListAnswersOutput.to_json)
           (Some Values.ListAnswersOutput.error_to_json)])
let list_lens_review_improvements =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pillarId =
         flag "pillar-id" (optional string) ~doc:"STRING PillarId"
       and milestoneNumber =
         flag "milestone-number" (optional int) ~doc:"INT MilestoneNumber"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListLensReviewImprovementsMaxResults"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_lens_review_improvements
           (Values.ListLensReviewImprovementsInput.make ?pillarId
              ?milestoneNumber ?nextToken ?maxResults ~workloadId ~lensAlias
              ()) (Some Values.ListLensReviewImprovementsOutput.to_json)
           (Some Values.ListLensReviewImprovementsOutput.error_to_json)])
let list_lens_reviews =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and milestoneNumber =
         flag "milestone-number" (optional int) ~doc:"INT MilestoneNumber"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_lens_reviews
           (Values.ListLensReviewsInput.make ?milestoneNumber ?nextToken
              ?maxResults ~workloadId ())
           (Some Values.ListLensReviewsOutput.to_json)
           (Some Values.ListLensReviewsOutput.error_to_json)])
let list_lens_shares =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sharedWithPrefix =
         flag "shared-with-prefix" (optional string)
           ~doc:"STRING SharedWithPrefix"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListWorkloadSharesMaxResults"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_lens_shares
           (Values.ListLensSharesInput.make ?sharedWithPrefix ?nextToken
              ?maxResults ~lensAlias ())
           (Some Values.ListLensSharesOutput.to_json)
           (Some Values.ListLensSharesOutput.error_to_json)])
let list_lenses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and lensType =
         flag "lens-type" (optional json_arg) ~doc:"JSON LensType"
       and lensStatus =
         flag "lens-status" (optional json_arg) ~doc:"JSON LensStatusType"
       and lensName =
         flag "lens-name" (optional string) ~doc:"STRING LensName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_lenses
           (Values.ListLensesInput.make ?nextToken ?maxResults
              ?lensType:(Option.map ~f:Values.LensType.of_json lensType)
              ?lensStatus:(Option.map ~f:Values.LensStatusType.of_json
                             lensStatus) ?lensName ())
           (Some Values.ListLensesOutput.to_json)
           (Some Values.ListLensesOutput.error_to_json)])
let list_milestones =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_milestones
           (Values.ListMilestonesInput.make ?nextToken ?maxResults
              ~workloadId ()) (Some Values.ListMilestonesOutput.to_json)
           (Some Values.ListMilestonesOutput.error_to_json)])
let list_notifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadId =
         flag "workload-id" (optional string) ~doc:"STRING WorkloadId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListNotificationsMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_notifications
           (Values.ListNotificationsInput.make ?workloadId ?nextToken
              ?maxResults ()) (Some Values.ListNotificationsOutput.to_json)
           (Some Values.ListNotificationsOutput.error_to_json)])
let list_share_invitations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadNamePrefix =
         flag "workload-name-prefix" (optional string)
           ~doc:"STRING WorkloadNamePrefix"
       and lensNamePrefix =
         flag "lens-name-prefix" (optional string)
           ~doc:"STRING LensNamePrefix"
       and shareResourceType =
         flag "share-resource-type" (optional json_arg)
           ~doc:"JSON ShareResourceType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListShareInvitationsMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_share_invitations
           (Values.ListShareInvitationsInput.make ?workloadNamePrefix
              ?lensNamePrefix
              ?shareResourceType:(Option.map
                                    ~f:Values.ShareResourceType.of_json
                                    shareResourceType) ?nextToken ?maxResults
              ()) (Some Values.ListShareInvitationsOutput.to_json)
           (Some Values.ListShareInvitationsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadArn =
         flag "workload-arn" (required string) ~doc:"STRING WorkloadArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~workloadArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_workload_shares =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sharedWithPrefix =
         flag "shared-with-prefix" (optional string)
           ~doc:"STRING SharedWithPrefix"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListWorkloadSharesMaxResults"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workload_shares
           (Values.ListWorkloadSharesInput.make ?sharedWithPrefix ?nextToken
              ?maxResults ~workloadId ())
           (Some Values.ListWorkloadSharesOutput.to_json)
           (Some Values.ListWorkloadSharesOutput.error_to_json)])
let list_workloads =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadNamePrefix =
         flag "workload-name-prefix" (optional string)
           ~doc:"STRING WorkloadNamePrefix"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListWorkloadsMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workloads
           (Values.ListWorkloadsInput.make ?workloadNamePrefix ?nextToken
              ?maxResults ()) (Some Values.ListWorkloadsOutput.to_json)
           (Some Values.ListWorkloadsOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadArn =
         flag "workload-arn" (required string) ~doc:"STRING WorkloadArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~workloadArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadArn =
         flag "workload-arn" (required string) ~doc:"STRING WorkloadArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~workloadArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_answer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and selectedChoices =
         flag "selected-choices" (optional json_arg)
           ~doc:"JSON SelectedChoices"
       and choiceUpdates =
         flag "choice-updates" (optional json_arg) ~doc:"JSON ChoiceUpdates"
       and notes = flag "notes" (optional string) ~doc:"STRING Notes"
       and isApplicable =
         flag "is-applicable" (optional bool) ~doc:"BOOL IsApplicable"
       and reason =
         flag "reason" (optional json_arg) ~doc:"JSON AnswerReason"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias"
       and questionId =
         flag "question-id" (required string) ~doc:"STRING QuestionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_answer
           (Values.UpdateAnswerInput.make
              ?selectedChoices:(Option.map ~f:Values.SelectedChoices.of_json
                                  selectedChoices)
              ?choiceUpdates:(Option.map ~f:Values.ChoiceUpdates.of_json
                                choiceUpdates) ?notes ?isApplicable
              ?reason:(Option.map ~f:Values.AnswerReason.of_json reason)
              ~workloadId ~lensAlias ~questionId ())
           (Some Values.UpdateAnswerOutput.to_json)
           (Some Values.UpdateAnswerOutput.error_to_json)])
let update_lens_review =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lensNotes =
         flag "lens-notes" (optional string) ~doc:"STRING Notes"
       and pillarNotes =
         flag "pillar-notes" (optional json_arg) ~doc:"JSON PillarNotes"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_lens_review
           (Values.UpdateLensReviewInput.make ?lensNotes
              ?pillarNotes:(Option.map ~f:Values.PillarNotes.of_json
                              pillarNotes) ~workloadId ~lensAlias ())
           (Some Values.UpdateLensReviewOutput.to_json)
           (Some Values.UpdateLensReviewOutput.error_to_json)])
let update_share_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shareInvitationId =
         flag "share-invitation-id" (required string)
           ~doc:"STRING ShareInvitationId"
       and shareInvitationAction =
         flag "share-invitation-action" (required json_arg)
           ~doc:"JSON ShareInvitationAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_share_invitation
           (Values.UpdateShareInvitationInput.make ~shareInvitationId
              ~shareInvitationAction:(Values.ShareInvitationAction.of_json
                                        shareInvitationAction) ())
           (Some Values.UpdateShareInvitationOutput.to_json)
           (Some Values.UpdateShareInvitationOutput.error_to_json)])
let update_workload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadName =
         flag "workload-name" (optional string) ~doc:"STRING WorkloadName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING WorkloadDescription"
       and environment =
         flag "environment" (optional json_arg)
           ~doc:"JSON WorkloadEnvironment"
       and accountIds =
         flag "account-ids" (optional json_arg)
           ~doc:"JSON WorkloadAccountIds"
       and awsRegions =
         flag "aws-regions" (optional json_arg)
           ~doc:"JSON WorkloadAwsRegions"
       and nonAwsRegions =
         flag "non-aws-regions" (optional json_arg)
           ~doc:"JSON WorkloadNonAwsRegions"
       and pillarPriorities =
         flag "pillar-priorities" (optional json_arg)
           ~doc:"JSON WorkloadPillarPriorities"
       and architecturalDesign =
         flag "architectural-design" (optional string)
           ~doc:"STRING WorkloadArchitecturalDesign"
       and reviewOwner =
         flag "review-owner" (optional string)
           ~doc:"STRING WorkloadReviewOwner"
       and isReviewOwnerUpdateAcknowledged =
         flag "is-review-owner-update-acknowledged" (optional bool)
           ~doc:"BOOL IsReviewOwnerUpdateAcknowledged"
       and industryType =
         flag "industry-type" (optional string)
           ~doc:"STRING WorkloadIndustryType"
       and industry =
         flag "industry" (optional string) ~doc:"STRING WorkloadIndustry"
       and notes = flag "notes" (optional string) ~doc:"STRING Notes"
       and improvementStatus =
         flag "improvement-status" (optional json_arg)
           ~doc:"JSON WorkloadImprovementStatus"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workload
           (Values.UpdateWorkloadInput.make ?workloadName ?description
              ?environment:(Option.map ~f:Values.WorkloadEnvironment.of_json
                              environment)
              ?accountIds:(Option.map ~f:Values.WorkloadAccountIds.of_json
                             accountIds)
              ?awsRegions:(Option.map ~f:Values.WorkloadAwsRegions.of_json
                             awsRegions)
              ?nonAwsRegions:(Option.map
                                ~f:Values.WorkloadNonAwsRegions.of_json
                                nonAwsRegions)
              ?pillarPriorities:(Option.map
                                   ~f:Values.WorkloadPillarPriorities.of_json
                                   pillarPriorities) ?architecturalDesign
              ?reviewOwner ?isReviewOwnerUpdateAcknowledged ?industryType
              ?industry ?notes
              ?improvementStatus:(Option.map
                                    ~f:Values.WorkloadImprovementStatus.of_json
                                    improvementStatus) ~workloadId ())
           (Some Values.UpdateWorkloadOutput.to_json)
           (Some Values.UpdateWorkloadOutput.error_to_json)])
let update_workload_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shareId = flag "share-id" (required string) ~doc:"STRING ShareId"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and permissionType =
         flag "permission-type" (required json_arg)
           ~doc:"JSON PermissionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workload_share
           (Values.UpdateWorkloadShareInput.make ~shareId ~workloadId
              ~permissionType:(Values.PermissionType.of_json permissionType)
              ()) (Some Values.UpdateWorkloadShareOutput.to_json)
           (Some Values.UpdateWorkloadShareOutput.error_to_json)])
let upgrade_lens_review =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and workloadId =
         flag "workload-id" (required string) ~doc:"STRING WorkloadId"
       and lensAlias =
         flag "lens-alias" (required string) ~doc:"STRING LensAlias"
       and milestoneName =
         flag "milestone-name" (required string) ~doc:"STRING MilestoneName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upgrade_lens_review
           (Values.UpgradeLensReviewInput.make ?clientRequestToken
              ~workloadId ~lensAlias ~milestoneName ()) None None])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-lenses", associate_lenses);
    ("create-lens-share", create_lens_share);
    ("create-lens-version", create_lens_version);
    ("create-milestone", create_milestone);
    ("create-workload", create_workload);
    ("create-workload-share", create_workload_share);
    ("delete-lens", delete_lens);
    ("delete-lens-share", delete_lens_share);
    ("delete-workload", delete_workload);
    ("delete-workload-share", delete_workload_share);
    ("disassociate-lenses", disassociate_lenses);
    ("export-lens", export_lens);
    ("get-answer", get_answer);
    ("get-lens", get_lens);
    ("get-lens-review", get_lens_review);
    ("get-lens-review-report", get_lens_review_report);
    ("get-lens-version-difference", get_lens_version_difference);
    ("get-milestone", get_milestone);
    ("get-workload", get_workload);
    ("import-lens", import_lens);
    ("list-answers", list_answers);
    ("list-lens-review-improvements", list_lens_review_improvements);
    ("list-lens-reviews", list_lens_reviews);
    ("list-lens-shares", list_lens_shares);
    ("list-lenses", list_lenses);
    ("list-milestones", list_milestones);
    ("list-notifications", list_notifications);
    ("list-share-invitations", list_share_invitations);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-workload-shares", list_workload_shares);
    ("list-workloads", list_workloads);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-answer", update_answer);
    ("update-lens-review", update_lens_review);
    ("update-share-invitation", update_share_invitation);
    ("update-workload", update_workload);
    ("update-workload-share", update_workload_share);
    ("upgrade-lens-review", upgrade_lens_review)]
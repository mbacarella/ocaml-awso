(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_centralization_rule_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMapInput"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING RuleName"
       and rule =
         flag "rule" (required json_arg) ~doc:"JSON CentralizationRule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_centralization_rule_for_organization
           (Values.CreateCentralizationRuleForOrganizationInput.make
              ?tags:(Option.map ~f:Values.TagMapInput.of_json tags) ~ruleName
              ~rule:(Values.CentralizationRule.of_json rule) ())
           (Some Values.CreateCentralizationRuleForOrganizationOutput.to_json)
           (Some
              Values.CreateCentralizationRuleForOrganizationOutput.error_to_json)])
let create_s3_table_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMapInput"
       and encryption =
         flag "encryption" (required json_arg) ~doc:"JSON Encryption"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_s3_table_integration
           (Values.CreateS3TableIntegrationInput.make
              ?tags:(Option.map ~f:Values.TagMapInput.of_json tags)
              ~encryption:(Values.Encryption.of_json encryption) ~roleArn ())
           (Some Values.CreateS3TableIntegrationOutput.to_json)
           (Some Values.CreateS3TableIntegrationOutput.error_to_json)])
let create_telemetry_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMapInput"
       and name =
         flag "name" (required string) ~doc:"STRING TelemetryPipelineName"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON TelemetryPipelineConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_telemetry_pipeline
           (Values.CreateTelemetryPipelineInput.make
              ?tags:(Option.map ~f:Values.TagMapInput.of_json tags) ~name
              ~configuration:(Values.TelemetryPipelineConfiguration.of_json
                                configuration) ())
           (Some Values.CreateTelemetryPipelineOutput.to_json)
           (Some Values.CreateTelemetryPipelineOutput.error_to_json)])
let create_telemetry_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMapInput"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING RuleName"
       and rule = flag "rule" (required json_arg) ~doc:"JSON TelemetryRule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_telemetry_rule
           (Values.CreateTelemetryRuleInput.make
              ?tags:(Option.map ~f:Values.TagMapInput.of_json tags) ~ruleName
              ~rule:(Values.TelemetryRule.of_json rule) ())
           (Some Values.CreateTelemetryRuleOutput.to_json)
           (Some Values.CreateTelemetryRuleOutput.error_to_json)])
let create_telemetry_rule_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMapInput"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING RuleName"
       and rule = flag "rule" (required json_arg) ~doc:"JSON TelemetryRule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_telemetry_rule_for_organization
           (Values.CreateTelemetryRuleForOrganizationInput.make
              ?tags:(Option.map ~f:Values.TagMapInput.of_json tags) ~ruleName
              ~rule:(Values.TelemetryRule.of_json rule) ())
           (Some Values.CreateTelemetryRuleForOrganizationOutput.to_json)
           (Some
              Values.CreateTelemetryRuleForOrganizationOutput.error_to_json)])
let delete_centralization_rule_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleIdentifier =
         flag "rule-identifier" (required string)
           ~doc:"STRING RuleIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_centralization_rule_for_organization
           (Values.DeleteCentralizationRuleForOrganizationInput.make
              ~ruleIdentifier ()) None None])
let delete_s3_table_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_s3_table_integration
           (Values.DeleteS3TableIntegrationInput.make ~arn ()) None None])
let delete_telemetry_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineIdentifier =
         flag "pipeline-identifier" (required string)
           ~doc:"STRING TelemetryPipelineIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_telemetry_pipeline
           (Values.DeleteTelemetryPipelineInput.make ~pipelineIdentifier ())
           (Some Values.DeleteTelemetryPipelineOutput.to_json)
           (Some Values.DeleteTelemetryPipelineOutput.error_to_json)])
let delete_telemetry_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleIdentifier =
         flag "rule-identifier" (required string)
           ~doc:"STRING RuleIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_telemetry_rule
           (Values.DeleteTelemetryRuleInput.make ~ruleIdentifier ()) None
           None])
let delete_telemetry_rule_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleIdentifier =
         flag "rule-identifier" (required string)
           ~doc:"STRING RuleIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_telemetry_rule_for_organization
           (Values.DeleteTelemetryRuleForOrganizationInput.make
              ~ruleIdentifier ()) None None])
let get_centralization_rule_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleIdentifier =
         flag "rule-identifier" (required string)
           ~doc:"STRING RuleIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_centralization_rule_for_organization
           (Values.GetCentralizationRuleForOrganizationInput.make
              ~ruleIdentifier ())
           (Some Values.GetCentralizationRuleForOrganizationOutput.to_json)
           (Some
              Values.GetCentralizationRuleForOrganizationOutput.error_to_json)])
let get_s3_table_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_s3_table_integration
           (Values.GetS3TableIntegrationInput.make ~arn ())
           (Some Values.GetS3TableIntegrationOutput.to_json)
           (Some Values.GetS3TableIntegrationOutput.error_to_json)])
let get_telemetry_enrichment_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_telemetry_enrichment_status (Fn.id ())
           (Some Values.GetTelemetryEnrichmentStatusOutput.to_json)
           (Some Values.GetTelemetryEnrichmentStatusOutput.error_to_json)])
let get_telemetry_evaluation_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_telemetry_evaluation_status (Fn.id ())
           (Some Values.GetTelemetryEvaluationStatusOutput.to_json)
           (Some Values.GetTelemetryEvaluationStatusOutput.error_to_json)])
let get_telemetry_evaluation_status_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_telemetry_evaluation_status_for_organization (Fn.id ())
           (Some
              Values.GetTelemetryEvaluationStatusForOrganizationOutput.to_json)
           (Some
              Values.GetTelemetryEvaluationStatusForOrganizationOutput.error_to_json)])
let get_telemetry_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineIdentifier =
         flag "pipeline-identifier" (required string)
           ~doc:"STRING TelemetryPipelineIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_telemetry_pipeline
           (Values.GetTelemetryPipelineInput.make ~pipelineIdentifier ())
           (Some Values.GetTelemetryPipelineOutput.to_json)
           (Some Values.GetTelemetryPipelineOutput.error_to_json)])
let get_telemetry_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleIdentifier =
         flag "rule-identifier" (required string)
           ~doc:"STRING RuleIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_telemetry_rule
           (Values.GetTelemetryRuleInput.make ~ruleIdentifier ())
           (Some Values.GetTelemetryRuleOutput.to_json)
           (Some Values.GetTelemetryRuleOutput.error_to_json)])
let get_telemetry_rule_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleIdentifier =
         flag "rule-identifier" (required string)
           ~doc:"STRING RuleIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_telemetry_rule_for_organization
           (Values.GetTelemetryRuleForOrganizationInput.make ~ruleIdentifier
              ()) (Some Values.GetTelemetryRuleForOrganizationOutput.to_json)
           (Some Values.GetTelemetryRuleForOrganizationOutput.error_to_json)])
let list_centralization_rules_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleNamePrefix =
         flag "rule-name-prefix" (optional string)
           ~doc:"STRING ListCentralizationRulesForOrganizationInputRuleNamePrefixString"
       and allRegions =
         flag "all-regions" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListCentralizationRulesForOrganizationMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_centralization_rules_for_organization
           (Values.ListCentralizationRulesForOrganizationInput.make
              ?ruleNamePrefix ?allRegions ?maxResults ?nextToken ())
           (Some Values.ListCentralizationRulesForOrganizationOutput.to_json)
           (Some
              Values.ListCentralizationRulesForOrganizationOutput.error_to_json)])
let list_resource_telemetry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifierPrefix =
         flag "resource-identifier-prefix" (optional string)
           ~doc:"STRING ResourceIdentifierPrefix"
       and resourceTypes =
         flag "resource-types" (optional json_arg) ~doc:"JSON ResourceTypes"
       and telemetryConfigurationState =
         flag "telemetry-configuration-state" (optional json_arg)
           ~doc:"JSON TelemetryConfigurationState"
       and resourceTags =
         flag "resource-tags" (optional json_arg) ~doc:"JSON TagMapInput"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListResourceTelemetryMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_telemetry
           (Values.ListResourceTelemetryInput.make ?resourceIdentifierPrefix
              ?resourceTypes:(Option.map ~f:Values.ResourceTypes.of_json
                                resourceTypes)
              ?telemetryConfigurationState:(Option.map
                                              ~f:Values.TelemetryConfigurationState.of_json
                                              telemetryConfigurationState)
              ?resourceTags:(Option.map ~f:Values.TagMapInput.of_json
                               resourceTags) ?maxResults ?nextToken ())
           (Some Values.ListResourceTelemetryOutput.to_json)
           (Some Values.ListResourceTelemetryOutput.error_to_json)])
let list_resource_telemetry_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIdentifiers =
         flag "account-identifiers" (optional json_arg)
           ~doc:"JSON AccountIdentifiers"
       and resourceIdentifierPrefix =
         flag "resource-identifier-prefix" (optional string)
           ~doc:"STRING ResourceIdentifierPrefix"
       and resourceTypes =
         flag "resource-types" (optional json_arg) ~doc:"JSON ResourceTypes"
       and telemetryConfigurationState =
         flag "telemetry-configuration-state" (optional json_arg)
           ~doc:"JSON TelemetryConfigurationState"
       and resourceTags =
         flag "resource-tags" (optional json_arg) ~doc:"JSON TagMapInput"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListResourceTelemetryForOrganizationMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_telemetry_for_organization
           (Values.ListResourceTelemetryForOrganizationInput.make
              ?accountIdentifiers:(Option.map
                                     ~f:Values.AccountIdentifiers.of_json
                                     accountIdentifiers)
              ?resourceIdentifierPrefix
              ?resourceTypes:(Option.map ~f:Values.ResourceTypes.of_json
                                resourceTypes)
              ?telemetryConfigurationState:(Option.map
                                              ~f:Values.TelemetryConfigurationState.of_json
                                              telemetryConfigurationState)
              ?resourceTags:(Option.map ~f:Values.TagMapInput.of_json
                               resourceTags) ?maxResults ?nextToken ())
           (Some Values.ListResourceTelemetryForOrganizationOutput.to_json)
           (Some
              Values.ListResourceTelemetryForOrganizationOutput.error_to_json)])
let list_s3_table_integrations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListS3TableIntegrationsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_s3_table_integrations
           (Values.ListS3TableIntegrationsInput.make ?maxResults ?nextToken
              ()) (Some Values.ListS3TableIntegrationsOutput.to_json)
           (Some Values.ListS3TableIntegrationsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceARN ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_telemetry_pipelines =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListTelemetryPipelinesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_telemetry_pipelines
           (Values.ListTelemetryPipelinesInput.make ?maxResults ?nextToken ())
           (Some Values.ListTelemetryPipelinesOutput.to_json)
           (Some Values.ListTelemetryPipelinesOutput.error_to_json)])
let list_telemetry_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleNamePrefix =
         flag "rule-name-prefix" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListTelemetryRulesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_telemetry_rules
           (Values.ListTelemetryRulesInput.make ?ruleNamePrefix ?maxResults
              ?nextToken ()) (Some Values.ListTelemetryRulesOutput.to_json)
           (Some Values.ListTelemetryRulesOutput.error_to_json)])
let list_telemetry_rules_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleNamePrefix =
         flag "rule-name-prefix" (optional string) ~doc:"STRING String"
       and sourceAccountIds =
         flag "source-account-ids" (optional json_arg)
           ~doc:"JSON AccountIdentifiers"
       and sourceOrganizationUnitIds =
         flag "source-organization-unit-ids" (optional json_arg)
           ~doc:"JSON OrganizationUnitIdentifiers"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListTelemetryRulesForOrganizationMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_telemetry_rules_for_organization
           (Values.ListTelemetryRulesForOrganizationInput.make
              ?ruleNamePrefix
              ?sourceAccountIds:(Option.map
                                   ~f:Values.AccountIdentifiers.of_json
                                   sourceAccountIds)
              ?sourceOrganizationUnitIds:(Option.map
                                            ~f:Values.OrganizationUnitIdentifiers.of_json
                                            sourceOrganizationUnitIds)
              ?maxResults ?nextToken ())
           (Some Values.ListTelemetryRulesForOrganizationOutput.to_json)
           (Some Values.ListTelemetryRulesForOrganizationOutput.error_to_json)])
let start_telemetry_enrichment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_telemetry_enrichment (Fn.id ())
           (Some Values.StartTelemetryEnrichmentOutput.to_json)
           (Some Values.StartTelemetryEnrichmentOutput.error_to_json)])
let start_telemetry_evaluation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regions = flag "regions" (optional json_arg) ~doc:"JSON Regions"
       and allRegions =
         flag "all-regions" (optional bool) ~doc:"BOOL AllRegions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_telemetry_evaluation
           (Values.StartTelemetryEvaluationInput.make
              ?regions:(Option.map ~f:Values.Regions.of_json regions)
              ?allRegions ()) None None])
let start_telemetry_evaluation_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regions = flag "regions" (optional json_arg) ~doc:"JSON Regions"
       and allRegions =
         flag "all-regions" (optional bool) ~doc:"BOOL AllRegions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_telemetry_evaluation_for_organization
           (Values.StartTelemetryEvaluationForOrganizationInput.make
              ?regions:(Option.map ~f:Values.Regions.of_json regions)
              ?allRegions ()) None None])
let stop_telemetry_enrichment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_telemetry_enrichment (Fn.id ())
           (Some Values.StopTelemetryEnrichmentOutput.to_json)
           (Some Values.StopTelemetryEnrichmentOutput.error_to_json)])
let stop_telemetry_evaluation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_telemetry_evaluation (Fn.id ()) None None])
let stop_telemetry_evaluation_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_telemetry_evaluation_for_organization (Fn.id ()) None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMapInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceARN
              ~tags:(Values.TagMapInput.of_json tags) ()) None None])
let test_telemetry_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and records = flag "records" (required json_arg) ~doc:"JSON Records"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON TelemetryPipelineConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_telemetry_pipeline
           (Values.TestTelemetryPipelineInput.make
              ~records:(Values.Records.of_json records)
              ~configuration:(Values.TelemetryPipelineConfiguration.of_json
                                configuration) ())
           (Some Values.TestTelemetryPipelineOutput.to_json)
           (Some Values.TestTelemetryPipelineOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_centralization_rule_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleIdentifier =
         flag "rule-identifier" (required string)
           ~doc:"STRING RuleIdentifier"
       and rule =
         flag "rule" (required json_arg) ~doc:"JSON CentralizationRule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_centralization_rule_for_organization
           (Values.UpdateCentralizationRuleForOrganizationInput.make
              ~ruleIdentifier ~rule:(Values.CentralizationRule.of_json rule)
              ())
           (Some Values.UpdateCentralizationRuleForOrganizationOutput.to_json)
           (Some
              Values.UpdateCentralizationRuleForOrganizationOutput.error_to_json)])
let update_telemetry_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineIdentifier =
         flag "pipeline-identifier" (required string)
           ~doc:"STRING TelemetryPipelineIdentifier"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON TelemetryPipelineConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_telemetry_pipeline
           (Values.UpdateTelemetryPipelineInput.make ~pipelineIdentifier
              ~configuration:(Values.TelemetryPipelineConfiguration.of_json
                                configuration) ())
           (Some Values.UpdateTelemetryPipelineOutput.to_json)
           (Some Values.UpdateTelemetryPipelineOutput.error_to_json)])
let update_telemetry_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleIdentifier =
         flag "rule-identifier" (required string)
           ~doc:"STRING RuleIdentifier"
       and rule = flag "rule" (required json_arg) ~doc:"JSON TelemetryRule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_telemetry_rule
           (Values.UpdateTelemetryRuleInput.make ~ruleIdentifier
              ~rule:(Values.TelemetryRule.of_json rule) ())
           (Some Values.UpdateTelemetryRuleOutput.to_json)
           (Some Values.UpdateTelemetryRuleOutput.error_to_json)])
let update_telemetry_rule_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleIdentifier =
         flag "rule-identifier" (required string)
           ~doc:"STRING RuleIdentifier"
       and rule = flag "rule" (required json_arg) ~doc:"JSON TelemetryRule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_telemetry_rule_for_organization
           (Values.UpdateTelemetryRuleForOrganizationInput.make
              ~ruleIdentifier ~rule:(Values.TelemetryRule.of_json rule) ())
           (Some Values.UpdateTelemetryRuleForOrganizationOutput.to_json)
           (Some
              Values.UpdateTelemetryRuleForOrganizationOutput.error_to_json)])
let validate_telemetry_pipeline_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON TelemetryPipelineConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.validate_telemetry_pipeline_configuration
           (Values.ValidateTelemetryPipelineConfigurationInput.make
              ~configuration:(Values.TelemetryPipelineConfiguration.of_json
                                configuration) ())
           (Some Values.ValidateTelemetryPipelineConfigurationOutput.to_json)
           (Some
              Values.ValidateTelemetryPipelineConfigurationOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-centralization-rule-for-organization",
       create_centralization_rule_for_organization);
    ("create-s3-table-integration", create_s3_table_integration);
    ("create-telemetry-pipeline", create_telemetry_pipeline);
    ("create-telemetry-rule", create_telemetry_rule);
    ("create-telemetry-rule-for-organization",
      create_telemetry_rule_for_organization);
    ("delete-centralization-rule-for-organization",
      delete_centralization_rule_for_organization);
    ("delete-s3-table-integration", delete_s3_table_integration);
    ("delete-telemetry-pipeline", delete_telemetry_pipeline);
    ("delete-telemetry-rule", delete_telemetry_rule);
    ("delete-telemetry-rule-for-organization",
      delete_telemetry_rule_for_organization);
    ("get-centralization-rule-for-organization",
      get_centralization_rule_for_organization);
    ("get-s3-table-integration", get_s3_table_integration);
    ("get-telemetry-enrichment-status", get_telemetry_enrichment_status);
    ("get-telemetry-evaluation-status", get_telemetry_evaluation_status);
    ("get-telemetry-evaluation-status-for-organization",
      get_telemetry_evaluation_status_for_organization);
    ("get-telemetry-pipeline", get_telemetry_pipeline);
    ("get-telemetry-rule", get_telemetry_rule);
    ("get-telemetry-rule-for-organization",
      get_telemetry_rule_for_organization);
    ("list-centralization-rules-for-organization",
      list_centralization_rules_for_organization);
    ("list-resource-telemetry", list_resource_telemetry);
    ("list-resource-telemetry-for-organization",
      list_resource_telemetry_for_organization);
    ("list-s3-table-integrations", list_s3_table_integrations);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-telemetry-pipelines", list_telemetry_pipelines);
    ("list-telemetry-rules", list_telemetry_rules);
    ("list-telemetry-rules-for-organization",
      list_telemetry_rules_for_organization);
    ("start-telemetry-enrichment", start_telemetry_enrichment);
    ("start-telemetry-evaluation", start_telemetry_evaluation);
    ("start-telemetry-evaluation-for-organization",
      start_telemetry_evaluation_for_organization);
    ("stop-telemetry-enrichment", stop_telemetry_enrichment);
    ("stop-telemetry-evaluation", stop_telemetry_evaluation);
    ("stop-telemetry-evaluation-for-organization",
      stop_telemetry_evaluation_for_organization);
    ("tag-resource", tag_resource);
    ("test-telemetry-pipeline", test_telemetry_pipeline);
    ("untag-resource", untag_resource);
    ("update-centralization-rule-for-organization",
      update_centralization_rule_for_organization);
    ("update-telemetry-pipeline", update_telemetry_pipeline);
    ("update-telemetry-rule", update_telemetry_rule);
    ("update-telemetry-rule-for-organization",
      update_telemetry_rule_for_organization);
    ("validate-telemetry-pipeline-configuration",
      validate_telemetry_pipeline_configuration)]
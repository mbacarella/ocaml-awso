(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.observabilityadmin
let apiVersion = "2018-05-10"
let endpointPrefix = "observabilityadmin"
let serviceFullName = "CloudWatch Observability Admin Service"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module Action =
  struct
    type nonrec t =
      | ALLOW 
      | BLOCK 
      | COUNT 
      | CAPTCHA 
      | CHALLENGE 
      | EXCLUDED_AS_COUNT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ALLOW -> "ALLOW"
      | BLOCK -> "BLOCK"
      | COUNT -> "COUNT"
      | CAPTCHA -> "CAPTCHA"
      | CHALLENGE -> "CHALLENGE"
      | EXCLUDED_AS_COUNT -> "EXCLUDED_AS_COUNT"
      | Non_static_id s -> s
    let of_string =
      function
      | "ALLOW" -> ALLOW
      | "BLOCK" -> BLOCK
      | "COUNT" -> COUNT
      | "CAPTCHA" -> CAPTCHA
      | "CHALLENGE" -> CHALLENGE
      | "EXCLUDED_AS_COUNT" -> EXCLUDED_AS_COUNT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Action" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Action" j)
    let to_json = simple_to_json to_value
  end
module LabelNameConditionLabelNameString =
  struct
    type nonrec t = string
    let context_ = "LabelNameConditionLabelNameString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9A-Za-z_\\-:]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"LabelNameConditionLabelNameString" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module ActionCondition =
  struct
    type nonrec t =
      {
      action: Action.t option
        [@ocaml.doc
          "The WAF action to match against (ALLOW, BLOCK, COUNT, CAPTCHA, CHALLENGE, EXCLUDED_AS_COUNT)."]}
    let make ?action = fun () -> { action }
    let to_value x =
      structure_to_value
        [("Action", (Option.map x.action ~f:Action.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let action =
        (Option.map ~f:Action.of_xml) (Xml.child xml_arg0 "Action") in
      make ?action ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let action = field_map json__ "Action" Action.of_json in
      make ?action ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Condition that matches based on the specific WAF action taken on the request."]
module LabelNameCondition =
  struct
    type nonrec t =
      {
      labelName: LabelNameConditionLabelNameString.t option
        [@ocaml.doc
          "The label name to match, supporting alphanumeric characters, underscores, hyphens, and colons."]}
    let make ?labelName = fun () -> { labelName }
    let to_value x =
      structure_to_value
        [("LabelName",
           (Option.map x.labelName
              ~f:LabelNameConditionLabelNameString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let labelName =
        (Option.map ~f:LabelNameConditionLabelNameString.of_xml)
          (Xml.child xml_arg0 "LabelName") in
      make ?labelName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let labelName =
        field_map json__ "LabelName"
          LabelNameConditionLabelNameString.of_json in
      make ?labelName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Condition that matches based on WAF rule labels, with label names limited to 1024 characters."]
module StringList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"StringList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module Condition =
  struct
    type nonrec t =
      {
      actionCondition: ActionCondition.t option
        [@ocaml.doc
          "Matches log records based on the WAF rule action taken (ALLOW, BLOCK, COUNT, etc.)."];
      labelNameCondition: LabelNameCondition.t option
        [@ocaml.doc
          "Matches log records based on WAF rule labels applied to the request."]}
    let make ?actionCondition =
      fun ?labelNameCondition ->
        fun () -> { actionCondition; labelNameCondition }
    let to_value x =
      structure_to_value
        [("ActionCondition",
           (Option.map x.actionCondition ~f:ActionCondition.to_value));
        ("LabelNameCondition",
          (Option.map x.labelNameCondition ~f:LabelNameCondition.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let labelNameCondition =
        (Option.map ~f:LabelNameCondition.of_xml)
          (Xml.child xml_arg0 "LabelNameCondition") in
      let actionCondition =
        (Option.map ~f:ActionCondition.of_xml)
          (Xml.child xml_arg0 "ActionCondition") in
      make ?labelNameCondition ?actionCondition ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let labelNameCondition =
        field_map json__ "LabelNameCondition" LabelNameCondition.of_json in
      let actionCondition =
        field_map json__ "ActionCondition" ActionCondition.of_json in
      make ?labelNameCondition ?actionCondition ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A single condition that can match based on WAF rule action or label name."]
module AdvancedFieldSelector =
  struct
    type nonrec t =
      {
      field: String_.t
        [@ocaml.doc "The name of the field to use for selection."];
      equals: StringList.t option
        [@ocaml.doc "Matches if the field value equals the specified value."];
      startsWith: StringList.t option
        [@ocaml.doc
          "Matches if the field value starts with the specified value."];
      endsWith: StringList.t option
        [@ocaml.doc
          "Matches if the field value ends with the specified value."];
      notEquals: StringList.t option
        [@ocaml.doc
          "Matches if the field value does not equal the specified value."];
      notStartsWith: StringList.t option
        [@ocaml.doc
          "Matches if the field value does not start with the specified value."];
      notEndsWith: StringList.t option
        [@ocaml.doc
          "Matches if the field value does not end with the specified value."]}
    let context_ = "AdvancedFieldSelector"
    let make ?equals =
      fun ?startsWith ->
        fun ?endsWith ->
          fun ?notEquals ->
            fun ?notStartsWith ->
              fun ?notEndsWith ->
                fun ~field ->
                  fun () ->
                    {
                      equals;
                      startsWith;
                      endsWith;
                      notEquals;
                      notStartsWith;
                      notEndsWith;
                      field
                    }
    let to_value x =
      structure_to_value
        [("Field", (Some (String_.to_value x.field)));
        ("Equals", (Option.map x.equals ~f:StringList.to_value));
        ("StartsWith", (Option.map x.startsWith ~f:StringList.to_value));
        ("EndsWith", (Option.map x.endsWith ~f:StringList.to_value));
        ("NotEquals", (Option.map x.notEquals ~f:StringList.to_value));
        ("NotStartsWith",
          (Option.map x.notStartsWith ~f:StringList.to_value));
        ("NotEndsWith", (Option.map x.notEndsWith ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notEndsWith =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "NotEndsWith") in
      let notStartsWith =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "NotStartsWith") in
      let notEquals =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "NotEquals") in
      let endsWith =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "EndsWith") in
      let startsWith =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "StartsWith") in
      let equals =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "Equals") in
      let field =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Field") in
      make ?notEndsWith ?notStartsWith ?notEquals ?endsWith ?startsWith
        ?equals ~field ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let notEndsWith = field_map json__ "NotEndsWith" StringList.of_json in
      let notStartsWith = field_map json__ "NotStartsWith" StringList.of_json in
      let notEquals = field_map json__ "NotEquals" StringList.of_json in
      let endsWith = field_map json__ "EndsWith" StringList.of_json in
      let startsWith = field_map json__ "StartsWith" StringList.of_json in
      let equals = field_map json__ "Equals" StringList.of_json in
      let field = field_map_exn json__ "Field" String_.of_json in
      make ?notEndsWith ?notStartsWith ?notEquals ?endsWith ?startsWith
        ?equals ~field ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines criteria for selecting resources based on field values."]
module Conditions =
  struct
    type nonrec t = Condition.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Condition.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Condition.of_xml)
    let of_json j =
      list_of_json ~kind:"Conditions" ~of_json:Condition.of_json j
    let to_json v = composed_to_json to_value v
  end
module FilterBehavior =
  struct
    type nonrec t =
      | KEEP 
      | DROP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | KEEP -> "KEEP" | DROP -> "DROP" | Non_static_id s -> s
    let of_string =
      function | "KEEP" -> KEEP | "DROP" -> DROP | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FilterBehavior" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FilterBehavior" j)
    let to_json = simple_to_json to_value
  end
module FilterRequirement =
  struct
    type nonrec t =
      | MEETS_ALL 
      | MEETS_ANY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MEETS_ALL -> "MEETS_ALL"
      | MEETS_ANY -> "MEETS_ANY"
      | Non_static_id s -> s
    let of_string =
      function
      | "MEETS_ALL" -> MEETS_ALL
      | "MEETS_ANY" -> MEETS_ANY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FilterRequirement" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FilterRequirement" j)
    let to_json = simple_to_json to_value
  end
module SingleHeaderNameString =
  struct
    type nonrec t = string
    let context_ = "SingleHeaderNameString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SingleHeaderNameString" j
    let to_json = simple_to_json to_value
  end
module FieldSelectors =
  struct
    type nonrec t = AdvancedFieldSelector.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AdvancedFieldSelector.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AdvancedFieldSelector.of_xml)
    let of_json j =
      list_of_json ~kind:"FieldSelectors"
        ~of_json:AdvancedFieldSelector.of_json j
    let to_json v = composed_to_json to_value v
  end
module Filter =
  struct
    type nonrec t =
      {
      behavior: FilterBehavior.t option
        [@ocaml.doc
          "The action to take for log records matching this filter (KEEP or DROP)."];
      requirement: FilterRequirement.t option
        [@ocaml.doc
          "Whether the log record must meet all conditions (MEETS_ALL) or any condition (MEETS_ANY) to match this filter."];
      conditions: Conditions.t option
        [@ocaml.doc
          "The list of conditions that determine if a log record matches this filter."]}
    let make ?behavior =
      fun ?requirement ->
        fun ?conditions -> fun () -> { behavior; requirement; conditions }
    let to_value x =
      structure_to_value
        [("Behavior", (Option.map x.behavior ~f:FilterBehavior.to_value));
        ("Requirement",
          (Option.map x.requirement ~f:FilterRequirement.to_value));
        ("Conditions", (Option.map x.conditions ~f:Conditions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let conditions =
        (Option.map ~f:Conditions.of_xml) (Xml.child xml_arg0 "Conditions") in
      let requirement =
        (Option.map ~f:FilterRequirement.of_xml)
          (Xml.child xml_arg0 "Requirement") in
      let behavior =
        (Option.map ~f:FilterBehavior.of_xml) (Xml.child xml_arg0 "Behavior") in
      make ?conditions ?requirement ?behavior ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let conditions = field_map json__ "Conditions" Conditions.of_json in
      let requirement =
        field_map json__ "Requirement" FilterRequirement.of_json in
      let behavior = field_map json__ "Behavior" FilterBehavior.of_json in
      make ?conditions ?requirement ?behavior ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A single filter condition that specifies behavior, requirement, and matching conditions for WAF log records."]
module SingleHeader =
  struct
    type nonrec t =
      {
      name: SingleHeaderNameString.t option
        [@ocaml.doc "The name value, limited to 64 characters."]}
    let make ?name = fun () -> { name }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:SingleHeaderNameString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:SingleHeaderNameString.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map json__ "Name" SingleHeaderNameString.of_json in
      make ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Structure containing a name field limited to 64 characters for header or query parameter identification."]
module AdvancedEventSelector =
  struct
    type nonrec t =
      {
      name: String_.t option
        [@ocaml.doc
          "An optional, descriptive name for an advanced event selector, such as \"Log data events for only two S3 buckets\"."];
      fieldSelectors: FieldSelectors.t
        [@ocaml.doc
          "Contains all selector statements in an advanced event selector."]}
    let context_ = "AdvancedEventSelector"
    let make ?name =
      fun ~fieldSelectors -> fun () -> { name; fieldSelectors }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("FieldSelectors", (Some (FieldSelectors.to_value x.fieldSelectors)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fieldSelectors =
        FieldSelectors.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FieldSelectors") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ~fieldSelectors ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fieldSelectors =
        field_map_exn json__ "FieldSelectors" FieldSelectors.of_json in
      let name = field_map json__ "Name" String_.of_json in
      make ~fieldSelectors ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Advanced event selectors let you create fine-grained selectors for management, data, and network activity events."]
module LogType =
  struct
    type nonrec t =
      | APPLICATION_LOGS 
      | USAGE_LOGS 
      | SECURITY_FINDING_LOGS 
      | ACCESS_LOGS 
      | CONNECTION_LOGS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | APPLICATION_LOGS -> "APPLICATION_LOGS"
      | USAGE_LOGS -> "USAGE_LOGS"
      | SECURITY_FINDING_LOGS -> "SECURITY_FINDING_LOGS"
      | ACCESS_LOGS -> "ACCESS_LOGS"
      | CONNECTION_LOGS -> "CONNECTION_LOGS"
      | Non_static_id s -> s
    let of_string =
      function
      | "APPLICATION_LOGS" -> APPLICATION_LOGS
      | "USAGE_LOGS" -> USAGE_LOGS
      | "SECURITY_FINDING_LOGS" -> SECURITY_FINDING_LOGS
      | "ACCESS_LOGS" -> ACCESS_LOGS
      | "CONNECTION_LOGS" -> CONNECTION_LOGS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LogType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LogType" j)
    let to_json = simple_to_json to_value
  end
module Filters =
  struct
    type nonrec t = Filter.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Filter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Filter.of_xml)
    let of_json j = list_of_json ~kind:"Filters" ~of_json:Filter.of_json j
    let to_json v = composed_to_json to_value v
  end
module FieldToMatch =
  struct
    type nonrec t =
      {
      singleHeader: SingleHeader.t option
        [@ocaml.doc "Redacts a specific header field by name from WAF logs."];
      uriPath: String_.t option
        [@ocaml.doc "Redacts the URI path from WAF logs."];
      queryString: String_.t option
        [@ocaml.doc "Redacts the entire query string from WAF logs."];
      method_: String_.t option
        [@ocaml.doc "Redacts the HTTP method from WAF logs."]}
    let make ?singleHeader =
      fun ?uriPath ->
        fun ?queryString ->
          fun ?method_ ->
            fun () -> { singleHeader; uriPath; queryString; method_ }
    let to_value x =
      structure_to_value
        [("SingleHeader",
           (Option.map x.singleHeader ~f:SingleHeader.to_value));
        ("UriPath", (Option.map x.uriPath ~f:String_.to_value));
        ("QueryString", (Option.map x.queryString ~f:String_.to_value));
        ("Method", (Option.map x.method_ ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let method_ =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Method") in
      let queryString =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "QueryString") in
      let uriPath =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "UriPath") in
      let singleHeader =
        (Option.map ~f:SingleHeader.of_xml)
          (Xml.child xml_arg0 "SingleHeader") in
      make ?method_ ?queryString ?uriPath ?singleHeader ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let method_ = field_map json__ "Method" String_.of_json in
      let queryString = field_map json__ "QueryString" String_.of_json in
      let uriPath = field_map json__ "UriPath" String_.of_json in
      let singleHeader = field_map json__ "SingleHeader" SingleHeader.of_json in
      make ?method_ ?queryString ?uriPath ?singleHeader ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a field in the request to redact from WAF logs, such as headers, query parameters, or body content."]
module LogGroupNamePattern =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The pattern used to generate destination log group names during centralization. The pattern can contain static text and dynamic variables that are replaced with source attributes. If a variable cannot be resolved, it inherits the value from its parent variable in the hierarchy. The pattern must be between 1 and 512 characters. Supported variables: $\\{source.logGroup\\} \226\128\148 The original log group name from the source account. $\\{source.accountId\\} \226\128\148 The Amazon Web Services account ID where the log originated. $\\{source.region\\} \226\128\148 The Amazon Web Services Region where the log originated. $\\{source.org.id\\} \226\128\148 The Amazon Web Services Organization ID of the source account. $\\{source.org.ouId\\} \226\128\148 The organizational unit ID of the source account. $\\{source.org.rootId\\} \226\128\148 The organization Root ID. $\\{source.org.path\\} \226\128\148 The organizational path from account to root."]
    let context_ = "LogGroupNamePattern"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:512) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(?:[\\._\\-/#A-Za-z0-9]+|\\$\\{[A-Za-z]+(?:\\.[A-Za-z]+){1,2}\\})+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LogGroupNamePattern" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "The pattern used to generate destination log group names during centralization. The pattern can contain static text and dynamic variables that are replaced with source attributes. If a variable cannot be resolved, it inherits the value from its parent variable in the hierarchy. The pattern must be between 1 and 512 characters. Supported variables: $\\{source.logGroup\\} \226\128\148 The original log group name from the source account. $\\{source.accountId\\} \226\128\148 The Amazon Web Services account ID where the log originated. $\\{source.region\\} \226\128\148 The Amazon Web Services Region where the log originated. $\\{source.org.id\\} \226\128\148 The Amazon Web Services Organization ID of the source account. $\\{source.org.ouId\\} \226\128\148 The organizational unit ID of the source account. $\\{source.org.rootId\\} \226\128\148 The organization Root ID. $\\{source.org.path\\} \226\128\148 The organizational path from account to root."]
module Region =
  struct
    type nonrec t = string
    let context_ = "Region"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Region" j
    let to_json = simple_to_json to_value
  end
module ResourceArn =
  struct
    type nonrec t = string
    let context_ = "ResourceArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1011) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws([a-z0-9\\-]+)?:([a-zA-Z0-9\\-]+):([a-z0-9\\-]+)?:([0-9]{12})?:(.+)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceArn" j
    let to_json = simple_to_json to_value
  end
module EncryptionConflictResolutionStrategy =
  struct
    type nonrec t =
      | ALLOW 
      | SKIP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | ALLOW -> "ALLOW" | SKIP -> "SKIP" | Non_static_id s -> s
    let of_string =
      function | "ALLOW" -> ALLOW | "SKIP" -> SKIP | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration EncryptionConflictResolutionStrategy" xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"EncryptionConflictResolutionStrategy" j)
    let to_json = simple_to_json to_value
  end
module EncryptionStrategy =
  struct
    type nonrec t =
      | CUSTOMER_MANAGED 
      | AWS_OWNED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CUSTOMER_MANAGED -> "CUSTOMER_MANAGED"
      | AWS_OWNED -> "AWS_OWNED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CUSTOMER_MANAGED" -> CUSTOMER_MANAGED
      | "AWS_OWNED" -> AWS_OWNED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration EncryptionStrategy" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EncryptionStrategy" j)
    let to_json = simple_to_json to_value
  end
module DataSource =
  struct
    type nonrec t =
      {
      name: String_.t option
        [@ocaml.doc
          "The name of the data source. For CloudWatch Logs sources, this corresponds to the data_source_name from the log event metadata. For third-party sources, this is either the configured data_source_name or defaults to the plugin name if not specified."];
      type_: String_.t option
        [@ocaml.doc
          "The type of the data source. For CloudWatch Logs sources, this corresponds to the data_source_type from the log event metadata. For third-party sources, this field is empty."]}
    let make ?name = fun ?type_ -> fun () -> { name; type_ }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?type_ ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "Type" String_.of_json in
      let name = field_map json__ "Name" String_.of_json in
      make ?type_ ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a data source associated with the telemetry pipeline. For CloudWatch Logs sources, this includes both a name and type extracted from the log event metadata. For third-party sources (such as S3), this includes only a name, with the type field left empty."]
module Source =
  struct
    type nonrec t =
      {
      type_: String_.t option
        [@ocaml.doc
          "The plugin name of the source, such as cloudwatch_logs or s3."]}
    let make ?type_ = fun () -> { type_ }
    let to_value x =
      structure_to_value [("Type", (Option.map x.type_ ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      make ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "Type" String_.of_json in make ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of source plugin types used in the pipeline configuration (such as cloudwatch_logs or s3). Currently supports a single source per pipeline, but is structured as a list to accommodate multiple pipelines in the configuration."]
module FieldMap =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module AdvancedEventSelectors =
  struct
    type nonrec t = AdvancedEventSelector.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AdvancedEventSelector.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AdvancedEventSelector.of_xml)
    let of_json j =
      list_of_json ~kind:"AdvancedEventSelectors"
        ~of_json:AdvancedEventSelector.of_json j
    let to_json v = composed_to_json to_value v
  end
module OutputFormat =
  struct
    type nonrec t =
      | Plain 
      | Json 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Plain -> "plain" | Json -> "json" | Non_static_id s -> s
    let of_string =
      function | "plain" -> Plain | "json" -> Json | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration OutputFormat" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"OutputFormat" j)
    let to_json = simple_to_json to_value
  end
module LogTypes =
  struct
    type nonrec t = LogType.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:LogType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LogType.of_xml)
    let of_json j = list_of_json ~kind:"LogTypes" ~of_json:LogType.of_json j
    let to_json v = composed_to_json to_value v
  end
module MskEnhancedMonitoringLevel =
  struct
    type nonrec t =
      | DEFAULT 
      | PER_BROKER 
      | PER_TOPIC_PER_BROKER 
      | PER_TOPIC_PER_PARTITION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DEFAULT -> "DEFAULT"
      | PER_BROKER -> "PER_BROKER"
      | PER_TOPIC_PER_BROKER -> "PER_TOPIC_PER_BROKER"
      | PER_TOPIC_PER_PARTITION -> "PER_TOPIC_PER_PARTITION"
      | Non_static_id s -> s
    let of_string =
      function
      | "DEFAULT" -> DEFAULT
      | "PER_BROKER" -> PER_BROKER
      | "PER_TOPIC_PER_BROKER" -> PER_TOPIC_PER_BROKER
      | "PER_TOPIC_PER_PARTITION" -> PER_TOPIC_PER_PARTITION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration MskEnhancedMonitoringLevel"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"MskEnhancedMonitoringLevel" j)
    let to_json = simple_to_json to_value
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module LoggingFilter =
  struct
    type nonrec t =
      {
      filters: Filters.t option
        [@ocaml.doc
          "A list of filter conditions that determine log record handling behavior."];
      defaultBehavior: FilterBehavior.t option
        [@ocaml.doc
          "The default action (KEEP or DROP) for log records that don't match any filter conditions."]}
    let make ?filters =
      fun ?defaultBehavior -> fun () -> { filters; defaultBehavior }
    let to_value x =
      structure_to_value
        [("Filters", (Option.map x.filters ~f:Filters.to_value));
        ("DefaultBehavior",
          (Option.map x.defaultBehavior ~f:FilterBehavior.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let defaultBehavior =
        (Option.map ~f:FilterBehavior.of_xml)
          (Xml.child xml_arg0 "DefaultBehavior") in
      let filters =
        (Option.map ~f:Filters.of_xml) (Xml.child xml_arg0 "Filters") in
      make ?defaultBehavior ?filters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let defaultBehavior =
        field_map json__ "DefaultBehavior" FilterBehavior.of_json in
      let filters = field_map json__ "Filters" Filters.of_json in
      make ?defaultBehavior ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration that determines which WAF log records to keep or drop based on specified conditions."]
module RedactedFields =
  struct
    type nonrec t = FieldToMatch.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FieldToMatch.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FieldToMatch.of_xml)
    let of_json j =
      list_of_json ~kind:"RedactedFields" ~of_json:FieldToMatch.of_json j
    let to_json v = composed_to_json to_value v
  end
module WAFLogType =
  struct
    type nonrec t =
      | WAF_LOGS 
      | Non_static_id of string 
    let make i = i
    let to_string = function | WAF_LOGS -> "WAF_LOGS" | Non_static_id s -> s
    let of_string = function | "WAF_LOGS" -> WAF_LOGS | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration WAFLogType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"WAFLogType" j)
    let to_json = simple_to_json to_value
  end
module LogGroupNameConfiguration =
  struct
    type nonrec t =
      {
      logGroupNamePattern: LogGroupNamePattern.t
        [@ocaml.doc
          "The pattern used to generate destination log group names during centralization. The pattern can contain static text and dynamic variables that are replaced with source attributes. If a variable cannot be resolved, it inherits the value from its parent variable in the hierarchy. The pattern must be between 1 and 512 characters. Supported variables: $\\{source.logGroup\\} \226\128\148 The original log group name from the source account. $\\{source.accountId\\} \226\128\148 The Amazon Web Services account ID where the log originated. $\\{source.region\\} \226\128\148 The Amazon Web Services Region where the log originated. $\\{source.org.id\\} \226\128\148 The Amazon Web Services Organization ID of the source account. $\\{source.org.ouId\\} \226\128\148 The organizational unit ID of the source account. $\\{source.org.rootId\\} \226\128\148 The organization Root ID. $\\{source.org.path\\} \226\128\148 The organizational path from account to root."]}
    let context_ = "LogGroupNameConfiguration"
    let make ~logGroupNamePattern = fun () -> { logGroupNamePattern }
    let to_value x =
      structure_to_value
        [("LogGroupNamePattern",
           (Some (LogGroupNamePattern.to_value x.logGroupNamePattern)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logGroupNamePattern =
        LogGroupNamePattern.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LogGroupNamePattern") in
      make ~logGroupNamePattern ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logGroupNamePattern =
        field_map_exn json__ "LogGroupNamePattern"
          LogGroupNamePattern.of_json in
      make ~logGroupNamePattern ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration that specifies a naming pattern for destination log groups created during centralization. The pattern supports static text and dynamic variables that are replaced with source attributes when log groups are created."]
module LogsBackupConfiguration =
  struct
    type nonrec t =
      {
      region: Region.t
        [@ocaml.doc
          "Logs specific backup destination region within the primary destination account to which log data should be centralized."];
      kmsKeyArn: ResourceArn.t option
        [@ocaml.doc
          "KMS Key ARN belonging to the primary destination account and backup region, to encrypt newly created central log groups in the backup destination."]}
    let context_ = "LogsBackupConfiguration"
    let make ?kmsKeyArn = fun ~region -> fun () -> { kmsKeyArn; region }
    let to_value x =
      structure_to_value
        [("Region", (Some (Region.to_value x.region)));
        ("KmsKeyArn", (Option.map x.kmsKeyArn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKeyArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "KmsKeyArn") in
      let region =
        Region.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Region") in
      make ?kmsKeyArn ~region ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let kmsKeyArn = field_map json__ "KmsKeyArn" ResourceArn.of_json in
      let region = field_map_exn json__ "Region" Region.of_json in
      make ?kmsKeyArn ~region ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration for backing up centralized log data to a secondary region."]
module LogsEncryptionConfiguration =
  struct
    type nonrec t =
      {
      encryptionStrategy: EncryptionStrategy.t
        [@ocaml.doc
          "Configuration that determines the encryption strategy of the destination log groups. CUSTOMER_MANAGED uses the configured KmsKeyArn to encrypt newly created destination log groups."];
      kmsKeyArn: ResourceArn.t option
        [@ocaml.doc
          "KMS Key ARN belonging to the primary destination account and region, to encrypt newly created central log groups in the primary destination."];
      encryptionConflictResolutionStrategy:
        EncryptionConflictResolutionStrategy.t option
        [@ocaml.doc
          "Conflict resolution strategy for centralization if the encryption strategy is set to CUSTOMER_MANAGED and the destination log group is encrypted with an AWS_OWNED KMS Key. ALLOW lets centralization go through while SKIP prevents centralization into the destination log group."]}
    let context_ = "LogsEncryptionConfiguration"
    let make ?kmsKeyArn =
      fun ?encryptionConflictResolutionStrategy ->
        fun ~encryptionStrategy ->
          fun () ->
            {
              kmsKeyArn;
              encryptionConflictResolutionStrategy;
              encryptionStrategy
            }
    let to_value x =
      structure_to_value
        [("EncryptionStrategy",
           (Some (EncryptionStrategy.to_value x.encryptionStrategy)));
        ("KmsKeyArn", (Option.map x.kmsKeyArn ~f:ResourceArn.to_value));
        ("EncryptionConflictResolutionStrategy",
          (Option.map x.encryptionConflictResolutionStrategy
             ~f:EncryptionConflictResolutionStrategy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionConflictResolutionStrategy =
        (Option.map ~f:EncryptionConflictResolutionStrategy.of_xml)
          (Xml.child xml_arg0 "EncryptionConflictResolutionStrategy") in
      let kmsKeyArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "KmsKeyArn") in
      let encryptionStrategy =
        EncryptionStrategy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EncryptionStrategy") in
      make ?encryptionConflictResolutionStrategy ?kmsKeyArn
        ~encryptionStrategy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryptionConflictResolutionStrategy =
        field_map json__ "EncryptionConflictResolutionStrategy"
          EncryptionConflictResolutionStrategy.of_json in
      let kmsKeyArn = field_map json__ "KmsKeyArn" ResourceArn.of_json in
      let encryptionStrategy =
        field_map_exn json__ "EncryptionStrategy" EncryptionStrategy.of_json in
      make ?encryptionConflictResolutionStrategy ?kmsKeyArn
        ~encryptionStrategy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration for encrypting centralized log groups. This configuration is only applied to destination log groups for which the corresponding source log groups are encrypted using Customer Managed KMS Keys."]
module DataSourceFilterString =
  struct
    type nonrec t = string
    let context_ = "DataSourceFilterString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DataSourceFilterString" j
    let to_json = simple_to_json to_value
  end
module EncryptedLogGroupStrategy =
  struct
    type nonrec t =
      | ALLOW 
      | SKIP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | ALLOW -> "ALLOW" | SKIP -> "SKIP" | Non_static_id s -> s
    let of_string =
      function | "ALLOW" -> ALLOW | "SKIP" -> SKIP | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration EncryptedLogGroupStrategy" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"EncryptedLogGroupStrategy" j)
    let to_json = simple_to_json to_value
  end
module LogsFilterString =
  struct
    type nonrec t = string
    let context_ = "LogsFilterString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LogsFilterString" j
    let to_json = simple_to_json to_value
  end
module RecordFormat =
  struct
    type nonrec t =
      | STRING 
      | JSON 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | STRING -> "STRING" | JSON -> "JSON" | Non_static_id s -> s
    let of_string =
      function | "STRING" -> STRING | "JSON" -> JSON | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration RecordFormat" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RecordFormat" j)
    let to_json = simple_to_json to_value
  end
module TelemetrySourceType =
  struct
    type nonrec t =
      | VPC_FLOW_LOGS 
      | ROUTE53_RESOLVER_QUERY_LOGS 
      | EKS_AUDIT_LOGS 
      | EKS_AUTHENTICATOR_LOGS 
      | EKS_CONTROLLER_MANAGER_LOGS 
      | EKS_SCHEDULER_LOGS 
      | EKS_API_LOGS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | VPC_FLOW_LOGS -> "VPC_FLOW_LOGS"
      | ROUTE53_RESOLVER_QUERY_LOGS -> "ROUTE53_RESOLVER_QUERY_LOGS"
      | EKS_AUDIT_LOGS -> "EKS_AUDIT_LOGS"
      | EKS_AUTHENTICATOR_LOGS -> "EKS_AUTHENTICATOR_LOGS"
      | EKS_CONTROLLER_MANAGER_LOGS -> "EKS_CONTROLLER_MANAGER_LOGS"
      | EKS_SCHEDULER_LOGS -> "EKS_SCHEDULER_LOGS"
      | EKS_API_LOGS -> "EKS_API_LOGS"
      | Non_static_id s -> s
    let of_string =
      function
      | "VPC_FLOW_LOGS" -> VPC_FLOW_LOGS
      | "ROUTE53_RESOLVER_QUERY_LOGS" -> ROUTE53_RESOLVER_QUERY_LOGS
      | "EKS_AUDIT_LOGS" -> EKS_AUDIT_LOGS
      | "EKS_AUTHENTICATOR_LOGS" -> EKS_AUTHENTICATOR_LOGS
      | "EKS_CONTROLLER_MANAGER_LOGS" -> EKS_CONTROLLER_MANAGER_LOGS
      | "EKS_SCHEDULER_LOGS" -> EKS_SCHEDULER_LOGS
      | "EKS_API_LOGS" -> EKS_API_LOGS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration TelemetrySourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TelemetrySourceType" j)
    let to_json = simple_to_json to_value
  end
module DataSources =
  struct
    type nonrec t = DataSource.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DataSource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DataSource.of_xml)
    let of_json j =
      list_of_json ~kind:"DataSources" ~of_json:DataSource.of_json j
    let to_json v = composed_to_json to_value v
  end
module Processors =
  struct
    type nonrec t = String_.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"Processors" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module Sinks =
  struct
    type nonrec t = String_.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j = list_of_json ~kind:"Sinks" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module Sources =
  struct
    type nonrec t = Source.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Source.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Source.of_xml)
    let of_json j = list_of_json ~kind:"Sources" ~of_json:Source.of_json j
    let to_json v = composed_to_json to_value v
  end
module TelemetryState =
  struct
    type nonrec t =
      | Enabled 
      | Disabled 
      | NotApplicable 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Enabled -> "Enabled"
      | Disabled -> "Disabled"
      | NotApplicable -> "NotApplicable"
      | Non_static_id s -> s
    let of_string =
      function
      | "Enabled" -> Enabled
      | "Disabled" -> Disabled
      | "NotApplicable" -> NotApplicable
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TelemetryState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TelemetryState" j)
    let to_json = simple_to_json to_value
  end
module TelemetryType =
  struct
    type nonrec t =
      | Logs 
      | Metrics 
      | Traces 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Logs -> "Logs"
      | Metrics -> "Metrics"
      | Traces -> "Traces"
      | Non_static_id s -> s
    let of_string =
      function
      | "Logs" -> Logs
      | "Metrics" -> Metrics
      | "Traces" -> Traces
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TelemetryType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TelemetryType" j)
    let to_json = simple_to_json to_value
  end
module ValidationError =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc "The error message describing the validation issue."];
      reason: String_.t option
        [@ocaml.doc "The reason code or category for the validation error."];
      fieldMap: FieldMap.t option
        [@ocaml.doc
          "A mapping of field names to specific validation issues within the configuration."]}
    let make ?message =
      fun ?reason -> fun ?fieldMap -> fun () -> { message; reason; fieldMap }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("Reason", (Option.map x.reason ~f:String_.to_value));
        ("FieldMap", (Option.map x.fieldMap ~f:FieldMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fieldMap =
        (Option.map ~f:FieldMap.of_xml) (Xml.child xml_arg0 "FieldMap") in
      let reason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Reason") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?fieldMap ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fieldMap = field_map json__ "FieldMap" FieldMap.of_json in
      let reason = field_map json__ "Reason" String_.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?fieldMap ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a detailed validation error with message, reason, and field mapping for comprehensive error reporting."]
module CloudtrailParameters =
  struct
    type nonrec t =
      {
      advancedEventSelectors: AdvancedEventSelectors.t
        [@ocaml.doc
          "The advanced event selectors to use for filtering Amazon Web Services CloudTrail events."]}
    let context_ = "CloudtrailParameters"
    let make ~advancedEventSelectors = fun () -> { advancedEventSelectors }
    let to_value x =
      structure_to_value
        [("AdvancedEventSelectors",
           (Some (AdvancedEventSelectors.to_value x.advancedEventSelectors)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let advancedEventSelectors =
        AdvancedEventSelectors.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AdvancedEventSelectors") in
      make ~advancedEventSelectors ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let advancedEventSelectors =
        field_map_exn json__ "AdvancedEventSelectors"
          AdvancedEventSelectors.of_json in
      make ~advancedEventSelectors ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Parameters specific to Amazon Web Services CloudTrail telemetry configuration."]
module DestinationType =
  struct
    type nonrec t =
      | Cloud_watch_logs 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Cloud_watch_logs -> "cloud-watch-logs"
      | Non_static_id s -> s
    let of_string =
      function
      | "cloud-watch-logs" -> Cloud_watch_logs
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DestinationType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DestinationType" j)
    let to_json = simple_to_json to_value
  end
module ELBLoadBalancerLoggingParameters =
  struct
    type nonrec t =
      {
      outputFormat: OutputFormat.t option
        [@ocaml.doc
          "The format for ELB access log entries (plain text or JSON format)."];
      fieldDelimiter: String_.t option
        [@ocaml.doc
          "The delimiter character used to separate fields in ELB access log entries when using plain text format."]}
    let make ?outputFormat =
      fun ?fieldDelimiter -> fun () -> { outputFormat; fieldDelimiter }
    let to_value x =
      structure_to_value
        [("OutputFormat",
           (Option.map x.outputFormat ~f:OutputFormat.to_value));
        ("FieldDelimiter", (Option.map x.fieldDelimiter ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fieldDelimiter =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "FieldDelimiter") in
      let outputFormat =
        (Option.map ~f:OutputFormat.of_xml)
          (Xml.child xml_arg0 "OutputFormat") in
      make ?fieldDelimiter ?outputFormat ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fieldDelimiter = field_map json__ "FieldDelimiter" String_.of_json in
      let outputFormat = field_map json__ "OutputFormat" OutputFormat.of_json in
      make ?fieldDelimiter ?outputFormat ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration parameters for ELB load balancer logging, including output format and field delimiter settings."]
module LogDeliveryParameters =
  struct
    type nonrec t =
      {
      logTypes: LogTypes.t option
        [@ocaml.doc "The type of log that the source is sending."]}
    let make ?logTypes = fun () -> { logTypes }
    let to_value x =
      structure_to_value
        [("LogTypes", (Option.map x.logTypes ~f:LogTypes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logTypes =
        (Option.map ~f:LogTypes.of_xml) (Xml.child xml_arg0 "LogTypes") in
      make ?logTypes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logTypes = field_map json__ "LogTypes" LogTypes.of_json in
      make ?logTypes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration parameters for Amazon Bedrock AgentCore logging, including logType settings."]
module MskMonitoringParameters =
  struct
    type nonrec t =
      {
      enhancedMonitoring: MskEnhancedMonitoringLevel.t option
        [@ocaml.doc "The level of enhanced monitoring for the MSK cluster."]}
    let make ?enhancedMonitoring = fun () -> { enhancedMonitoring }
    let to_value x =
      structure_to_value
        [("EnhancedMonitoring",
           (Option.map x.enhancedMonitoring
              ~f:MskEnhancedMonitoringLevel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enhancedMonitoring =
        (Option.map ~f:MskEnhancedMonitoringLevel.of_xml)
          (Xml.child xml_arg0 "EnhancedMonitoring") in
      make ?enhancedMonitoring ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let enhancedMonitoring =
        field_map json__ "EnhancedMonitoring"
          MskEnhancedMonitoringLevel.of_json in
      make ?enhancedMonitoring ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration parameters for Amazon MSK cluster monitoring, including enhanced monitoring level settings."]
module RetentionPeriodInDays =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:3653) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for RetentionPeriodInDays" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module VPCFlowLogParameters =
  struct
    type nonrec t =
      {
      logFormat: String_.t option
        [@ocaml.doc
          "The format in which VPC Flow Log entries should be logged."];
      trafficType: String_.t option
        [@ocaml.doc "The type of traffic to log (ACCEPT, REJECT, or ALL)."];
      maxAggregationInterval: Integer.t option
        [@ocaml.doc
          "The maximum interval in seconds between the capture of flow log records."]}
    let make ?logFormat =
      fun ?trafficType ->
        fun ?maxAggregationInterval ->
          fun () -> { logFormat; trafficType; maxAggregationInterval }
    let to_value x =
      structure_to_value
        [("LogFormat", (Option.map x.logFormat ~f:String_.to_value));
        ("TrafficType", (Option.map x.trafficType ~f:String_.to_value));
        ("MaxAggregationInterval",
          (Option.map x.maxAggregationInterval ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxAggregationInterval =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MaxAggregationInterval") in
      let trafficType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "TrafficType") in
      let logFormat =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "LogFormat") in
      make ?maxAggregationInterval ?trafficType ?logFormat ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxAggregationInterval =
        field_map json__ "MaxAggregationInterval" Integer.of_json in
      let trafficType = field_map json__ "TrafficType" String_.of_json in
      let logFormat = field_map json__ "LogFormat" String_.of_json in
      make ?maxAggregationInterval ?trafficType ?logFormat ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Configuration parameters specific to VPC Flow Logs."]
module WAFLoggingParameters =
  struct
    type nonrec t =
      {
      redactedFields: RedactedFields.t option
        [@ocaml.doc
          "The fields to redact from WAF logs to protect sensitive information."];
      loggingFilter: LoggingFilter.t option
        [@ocaml.doc
          "A filter configuration that determines which WAF log records to include or exclude."];
      logType: WAFLogType.t option
        [@ocaml.doc
          "The type of WAF logs to collect (currently supports WAF_LOGS)."]}
    let make ?redactedFields =
      fun ?loggingFilter ->
        fun ?logType -> fun () -> { redactedFields; loggingFilter; logType }
    let to_value x =
      structure_to_value
        [("RedactedFields",
           (Option.map x.redactedFields ~f:RedactedFields.to_value));
        ("LoggingFilter",
          (Option.map x.loggingFilter ~f:LoggingFilter.to_value));
        ("LogType", (Option.map x.logType ~f:WAFLogType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logType =
        (Option.map ~f:WAFLogType.of_xml) (Xml.child xml_arg0 "LogType") in
      let loggingFilter =
        (Option.map ~f:LoggingFilter.of_xml)
          (Xml.child xml_arg0 "LoggingFilter") in
      let redactedFields =
        (Option.map ~f:RedactedFields.of_xml)
          (Xml.child xml_arg0 "RedactedFields") in
      make ?logType ?loggingFilter ?redactedFields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logType = field_map json__ "LogType" WAFLogType.of_json in
      let loggingFilter =
        field_map json__ "LoggingFilter" LoggingFilter.of_json in
      let redactedFields =
        field_map json__ "RedactedFields" RedactedFields.of_json in
      make ?logType ?loggingFilter ?redactedFields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration parameters for WAF logging, including redacted fields and logging filters."]
module AccountIdentifier =
  struct
    type nonrec t = string
    let context_ = "AccountIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:12) >>=
             (fun () ->
                (check_string_max i ~max:12) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9]{12}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AccountIdentifier" j
    let to_json = simple_to_json to_value
  end
module DestinationLogsConfiguration =
  struct
    type nonrec t =
      {
      logsEncryptionConfiguration: LogsEncryptionConfiguration.t option
        [@ocaml.doc
          "The encryption configuration for centralization destination log groups."];
      backupConfiguration: LogsBackupConfiguration.t option
        [@ocaml.doc
          "Configuration defining the backup region and an optional KMS key for the backup destination."];
      logGroupNameConfiguration: LogGroupNameConfiguration.t option
        [@ocaml.doc
          "Configuration that specifies a naming pattern for destination log groups created during centralization. The pattern supports static text and dynamic variables that are replaced with source attributes when log groups are created."]}
    let make ?logsEncryptionConfiguration =
      fun ?backupConfiguration ->
        fun ?logGroupNameConfiguration ->
          fun () ->
            {
              logsEncryptionConfiguration;
              backupConfiguration;
              logGroupNameConfiguration
            }
    let to_value x =
      structure_to_value
        [("LogsEncryptionConfiguration",
           (Option.map x.logsEncryptionConfiguration
              ~f:LogsEncryptionConfiguration.to_value));
        ("BackupConfiguration",
          (Option.map x.backupConfiguration
             ~f:LogsBackupConfiguration.to_value));
        ("LogGroupNameConfiguration",
          (Option.map x.logGroupNameConfiguration
             ~f:LogGroupNameConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logGroupNameConfiguration =
        (Option.map ~f:LogGroupNameConfiguration.of_xml)
          (Xml.child xml_arg0 "LogGroupNameConfiguration") in
      let backupConfiguration =
        (Option.map ~f:LogsBackupConfiguration.of_xml)
          (Xml.child xml_arg0 "BackupConfiguration") in
      let logsEncryptionConfiguration =
        (Option.map ~f:LogsEncryptionConfiguration.of_xml)
          (Xml.child xml_arg0 "LogsEncryptionConfiguration") in
      make ?logGroupNameConfiguration ?backupConfiguration
        ?logsEncryptionConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logGroupNameConfiguration =
        field_map json__ "LogGroupNameConfiguration"
          LogGroupNameConfiguration.of_json in
      let backupConfiguration =
        field_map json__ "BackupConfiguration"
          LogsBackupConfiguration.of_json in
      let logsEncryptionConfiguration =
        field_map json__ "LogsEncryptionConfiguration"
          LogsEncryptionConfiguration.of_json in
      make ?logGroupNameConfiguration ?backupConfiguration
        ?logsEncryptionConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration for centralization destination log groups, including encryption and backup settings."]
module Regions =
  struct
    type nonrec t = Region.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Region.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Region.of_xml)
    let of_json j = list_of_json ~kind:"Regions" ~of_json:Region.of_json j
    let to_json v = composed_to_json to_value v
  end
module SourceFilterString =
  struct
    type nonrec t = string
    let context_ = "SourceFilterString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SourceFilterString" j
    let to_json = simple_to_json to_value
  end
module SourceLogsConfiguration =
  struct
    type nonrec t =
      {
      logGroupSelectionCriteria: LogsFilterString.t option
        [@ocaml.doc
          "The selection criteria that specifies which source log groups to centralize. The selection criteria uses the same format as OAM link filters."];
      dataSourceSelectionCriteria: DataSourceFilterString.t option
        [@ocaml.doc
          "The selection criteria that specifies which data sources to centralize. The selection criteria uses the same filter expression format as LogGroupSelectionCriteria, but operates on DataSourceName and DataSourceType operands. When both LogGroupSelectionCriteria and DataSourceSelectionCriteria are specified, a log event must match both criteria to be centralized."];
      encryptedLogGroupStrategy: EncryptedLogGroupStrategy.t
        [@ocaml.doc
          "A strategy determining whether to centralize source log groups that are encrypted with customer managed KMS keys (CMK). ALLOW will consider CMK encrypted source log groups for centralization while SKIP will skip CMK encrypted source log groups from centralization."]}
    let context_ = "SourceLogsConfiguration"
    let make ?logGroupSelectionCriteria =
      fun ?dataSourceSelectionCriteria ->
        fun ~encryptedLogGroupStrategy ->
          fun () ->
            {
              logGroupSelectionCriteria;
              dataSourceSelectionCriteria;
              encryptedLogGroupStrategy
            }
    let to_value x =
      structure_to_value
        [("LogGroupSelectionCriteria",
           (Option.map x.logGroupSelectionCriteria
              ~f:LogsFilterString.to_value));
        ("DataSourceSelectionCriteria",
          (Option.map x.dataSourceSelectionCriteria
             ~f:DataSourceFilterString.to_value));
        ("EncryptedLogGroupStrategy",
          (Some
             (EncryptedLogGroupStrategy.to_value x.encryptedLogGroupStrategy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptedLogGroupStrategy =
        EncryptedLogGroupStrategy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "EncryptedLogGroupStrategy") in
      let dataSourceSelectionCriteria =
        (Option.map ~f:DataSourceFilterString.of_xml)
          (Xml.child xml_arg0 "DataSourceSelectionCriteria") in
      let logGroupSelectionCriteria =
        (Option.map ~f:LogsFilterString.of_xml)
          (Xml.child xml_arg0 "LogGroupSelectionCriteria") in
      make ~encryptedLogGroupStrategy ?dataSourceSelectionCriteria
        ?logGroupSelectionCriteria ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryptedLogGroupStrategy =
        field_map_exn json__ "EncryptedLogGroupStrategy"
          EncryptedLogGroupStrategy.of_json in
      let dataSourceSelectionCriteria =
        field_map json__ "DataSourceSelectionCriteria"
          DataSourceFilterString.of_json in
      let logGroupSelectionCriteria =
        field_map json__ "LogGroupSelectionCriteria" LogsFilterString.of_json in
      make ~encryptedLogGroupStrategy ?dataSourceSelectionCriteria
        ?logGroupSelectionCriteria ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration for selecting and handling source log groups for centralization."]
module PipelineOutputError =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "The detailed error message describing what went wrong during the pipeline test operation for this record."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains detailed error information from pipeline test operations, providing structured error responses for better debugging and troubleshooting capabilities."]
module Record =
  struct
    type nonrec t =
      {
      data: String_.t option
        [@ocaml.doc
          "The data content of the test record used for pipeline validation."];
      type_: RecordFormat.t option
        [@ocaml.doc
          "The type of the test record, indicating the format or category of the data."]}
    let make ?data = fun ?type_ -> fun () -> { data; type_ }
    let to_value x =
      structure_to_value
        [("Data", (Option.map x.data ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:RecordFormat.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:RecordFormat.of_xml) (Xml.child xml_arg0 "Type") in
      let data = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Data") in
      make ?type_ ?data ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "Type" RecordFormat.of_json in
      let data = field_map json__ "Data" String_.of_json in
      make ?type_ ?data ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a test record structure used for pipeline testing operations to validate data processing."]
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module ResourceType =
  struct
    type nonrec t =
      | AWS__EC2__Instance 
      | AWS__EC2__VPC 
      | AWS__Lambda__Function 
      | AWS__CloudTrail 
      | AWS__EKS__Cluster 
      | AWS__WAFv2__WebACL 
      | AWS__ElasticLoadBalancingV2__LoadBalancer 
      | AWS__Route53Resolver__ResolverEndpoint 
      | AWS__BedrockAgentCore__Runtime 
      | AWS__BedrockAgentCore__Browser 
      | AWS__BedrockAgentCore__CodeInterpreter 
      | AWS__BedrockAgentCore__Gateway 
      | AWS__BedrockAgentCore__Memory 
      | AWS__BedrockAgentCore__WorkloadIdentity 
      | AWS__SecurityHub__Hub 
      | AWS__CloudFront__Distribution 
      | AWS__SecurityHub__HubV2 
      | AWS__CloudWatch__OTelEnrichment 
      | AWS__MSK__Cluster 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AWS__EC2__Instance -> "AWS::EC2::Instance"
      | AWS__EC2__VPC -> "AWS::EC2::VPC"
      | AWS__Lambda__Function -> "AWS::Lambda::Function"
      | AWS__CloudTrail -> "AWS::CloudTrail"
      | AWS__EKS__Cluster -> "AWS::EKS::Cluster"
      | AWS__WAFv2__WebACL -> "AWS::WAFv2::WebACL"
      | AWS__ElasticLoadBalancingV2__LoadBalancer ->
          "AWS::ElasticLoadBalancingV2::LoadBalancer"
      | AWS__Route53Resolver__ResolverEndpoint ->
          "AWS::Route53Resolver::ResolverEndpoint"
      | AWS__BedrockAgentCore__Runtime -> "AWS::BedrockAgentCore::Runtime"
      | AWS__BedrockAgentCore__Browser -> "AWS::BedrockAgentCore::Browser"
      | AWS__BedrockAgentCore__CodeInterpreter ->
          "AWS::BedrockAgentCore::CodeInterpreter"
      | AWS__BedrockAgentCore__Gateway -> "AWS::BedrockAgentCore::Gateway"
      | AWS__BedrockAgentCore__Memory -> "AWS::BedrockAgentCore::Memory"
      | AWS__BedrockAgentCore__WorkloadIdentity ->
          "AWS::BedrockAgentCore::WorkloadIdentity"
      | AWS__SecurityHub__Hub -> "AWS::SecurityHub::Hub"
      | AWS__CloudFront__Distribution -> "AWS::CloudFront::Distribution"
      | AWS__SecurityHub__HubV2 -> "AWS::SecurityHub::HubV2"
      | AWS__CloudWatch__OTelEnrichment -> "AWS::CloudWatch::OTelEnrichment"
      | AWS__MSK__Cluster -> "AWS::MSK::Cluster"
      | Non_static_id s -> s
    let of_string =
      function
      | "AWS::EC2::Instance" -> AWS__EC2__Instance
      | "AWS::EC2::VPC" -> AWS__EC2__VPC
      | "AWS::Lambda::Function" -> AWS__Lambda__Function
      | "AWS::CloudTrail" -> AWS__CloudTrail
      | "AWS::EKS::Cluster" -> AWS__EKS__Cluster
      | "AWS::WAFv2::WebACL" -> AWS__WAFv2__WebACL
      | "AWS::ElasticLoadBalancingV2::LoadBalancer" ->
          AWS__ElasticLoadBalancingV2__LoadBalancer
      | "AWS::Route53Resolver::ResolverEndpoint" ->
          AWS__Route53Resolver__ResolverEndpoint
      | "AWS::BedrockAgentCore::Runtime" -> AWS__BedrockAgentCore__Runtime
      | "AWS::BedrockAgentCore::Browser" -> AWS__BedrockAgentCore__Browser
      | "AWS::BedrockAgentCore::CodeInterpreter" ->
          AWS__BedrockAgentCore__CodeInterpreter
      | "AWS::BedrockAgentCore::Gateway" -> AWS__BedrockAgentCore__Gateway
      | "AWS::BedrockAgentCore::Memory" -> AWS__BedrockAgentCore__Memory
      | "AWS::BedrockAgentCore::WorkloadIdentity" ->
          AWS__BedrockAgentCore__WorkloadIdentity
      | "AWS::SecurityHub::Hub" -> AWS__SecurityHub__Hub
      | "AWS::CloudFront::Distribution" -> AWS__CloudFront__Distribution
      | "AWS::SecurityHub::HubV2" -> AWS__SecurityHub__HubV2
      | "AWS::CloudWatch::OTelEnrichment" -> AWS__CloudWatch__OTelEnrichment
      | "AWS::MSK::Cluster" -> AWS__MSK__Cluster
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ResourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ResourceType" j)
    let to_json = simple_to_json to_value
  end
module RuleName =
  struct
    type nonrec t = string
    let context_ = "RuleName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9A-Za-z-_.#/]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RuleName" j
    let to_json = simple_to_json to_value
  end
module TelemetrySourceTypes =
  struct
    type nonrec t = TelemetrySourceType.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TelemetrySourceType.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TelemetrySourceType.of_xml)
    let of_json j =
      list_of_json ~kind:"TelemetrySourceTypes"
        ~of_json:TelemetrySourceType.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConfigurationSummary =
  struct
    type nonrec t =
      {
      sources: Sources.t option
        [@ocaml.doc "The list of data sources configured in the pipeline."];
      dataSources: DataSources.t option
        [@ocaml.doc
          "The list of data sources that provide telemetry data to the pipeline."];
      processors: Processors.t option
        [@ocaml.doc
          "The list of processors configured in the pipeline for data transformation."];
      processorCount: Integer.t option
        [@ocaml.doc
          "The total number of processors configured in the pipeline."];
      sinks: Sinks.t option
        [@ocaml.doc "The list of destinations where processed data is sent."]}
    let make ?sources =
      fun ?dataSources ->
        fun ?processors ->
          fun ?processorCount ->
            fun ?sinks ->
              fun () ->
                { sources; dataSources; processors; processorCount; sinks }
    let to_value x =
      structure_to_value
        [("Sources", (Option.map x.sources ~f:Sources.to_value));
        ("DataSources", (Option.map x.dataSources ~f:DataSources.to_value));
        ("Processors", (Option.map x.processors ~f:Processors.to_value));
        ("ProcessorCount", (Option.map x.processorCount ~f:Integer.to_value));
        ("Sinks", (Option.map x.sinks ~f:Sinks.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sinks = (Option.map ~f:Sinks.of_xml) (Xml.child xml_arg0 "Sinks") in
      let processorCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "ProcessorCount") in
      let processors =
        (Option.map ~f:Processors.of_xml) (Xml.child xml_arg0 "Processors") in
      let dataSources =
        (Option.map ~f:DataSources.of_xml) (Xml.child xml_arg0 "DataSources") in
      let sources =
        (Option.map ~f:Sources.of_xml) (Xml.child xml_arg0 "Sources") in
      make ?sinks ?processorCount ?processors ?dataSources ?sources ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sinks = field_map json__ "Sinks" Sinks.of_json in
      let processorCount = field_map json__ "ProcessorCount" Integer.of_json in
      let processors = field_map json__ "Processors" Processors.of_json in
      let dataSources = field_map json__ "DataSources" DataSources.of_json in
      let sources = field_map json__ "Sources" Sources.of_json in
      make ?sinks ?processorCount ?processors ?dataSources ?sources ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a summary of pipeline configuration components including sources, processors, and destinations."]
module TagMapOutput =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module TelemetryPipelineName =
  struct
    type nonrec t = string
    let context_ = "TelemetryPipelineName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:28) >>=
                  (fun () -> check_pattern i ~pattern:".*[a-z][a-z0-9\\-]+.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TelemetryPipelineName" j
    let to_json = simple_to_json to_value
  end
module TelemetryPipelineStatus =
  struct
    type nonrec t =
      | CREATING 
      | ACTIVE 
      | UPDATING 
      | DELETING 
      | CREATE_FAILED 
      | UPDATE_FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATING -> "CREATING"
      | ACTIVE -> "ACTIVE"
      | UPDATING -> "UPDATING"
      | DELETING -> "DELETING"
      | CREATE_FAILED -> "CREATE_FAILED"
      | UPDATE_FAILED -> "UPDATE_FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATING" -> CREATING
      | "ACTIVE" -> ACTIVE
      | "UPDATING" -> UPDATING
      | "DELETING" -> DELETING
      | "CREATE_FAILED" -> CREATE_FAILED
      | "UPDATE_FAILED" -> UPDATE_FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration TelemetryPipelineStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"TelemetryPipelineStatus" j)
    let to_json = simple_to_json to_value
  end
module IntegrationStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | DELETING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | DELETING -> "DELETING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "DELETING" -> DELETING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration IntegrationStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"IntegrationStatus" j)
    let to_json = simple_to_json to_value
  end
module ResourceIdentifier =
  struct
    type nonrec t = string
    let context_ = "ResourceIdentifier"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceIdentifier" j
    let to_json = simple_to_json to_value
  end
module TelemetryConfigurationState =
  struct
    type nonrec t = (TelemetryType.t * TelemetryState.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TelemetryType.of_string chopped),
                              (TelemetryState.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TelemetryType.to_value x) |>
                    (fun x ->
                       (TelemetryState.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TelemetryType.of_string
        ~of_json:TelemetryState.of_json j
    let to_json v = composed_to_json to_value v
  end
module CentralizationFailureReason =
  struct
    type nonrec t =
      | TRUSTED_ACCESS_NOT_ENABLED 
      | DESTINATION_ACCOUNT_NOT_IN_ORGANIZATION 
      | INTERNAL_SERVER_ERROR 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TRUSTED_ACCESS_NOT_ENABLED -> "TRUSTED_ACCESS_NOT_ENABLED"
      | DESTINATION_ACCOUNT_NOT_IN_ORGANIZATION ->
          "DESTINATION_ACCOUNT_NOT_IN_ORGANIZATION"
      | INTERNAL_SERVER_ERROR -> "INTERNAL_SERVER_ERROR"
      | Non_static_id s -> s
    let of_string =
      function
      | "TRUSTED_ACCESS_NOT_ENABLED" -> TRUSTED_ACCESS_NOT_ENABLED
      | "DESTINATION_ACCOUNT_NOT_IN_ORGANIZATION" ->
          DESTINATION_ACCOUNT_NOT_IN_ORGANIZATION
      | "INTERNAL_SERVER_ERROR" -> INTERNAL_SERVER_ERROR
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration CentralizationFailureReason"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"CentralizationFailureReason" j)
    let to_json = simple_to_json to_value
  end
module RuleHealth =
  struct
    type nonrec t =
      | Healthy 
      | Unhealthy 
      | Provisioning 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Healthy -> "Healthy"
      | Unhealthy -> "Unhealthy"
      | Provisioning -> "Provisioning"
      | Non_static_id s -> s
    let of_string =
      function
      | "Healthy" -> Healthy
      | "Unhealthy" -> Unhealthy
      | "Provisioning" -> Provisioning
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration RuleHealth" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RuleHealth" j)
    let to_json = simple_to_json to_value
  end
module TelemetryPipelineConfigurationBody =
  struct
    type nonrec t = string
    let context_ = "TelemetryPipelineConfigurationBody"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:24000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"TelemetryPipelineConfigurationBody" j
    let to_json = simple_to_json to_value
  end
module ValidationErrors =
  struct
    type nonrec t = ValidationError.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ValidationError.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ValidationError.of_xml)
    let of_json j =
      list_of_json ~kind:"ValidationErrors" ~of_json:ValidationError.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module AllRegions =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module TelemetryDestinationConfiguration =
  struct
    type nonrec t =
      {
      destinationType: DestinationType.t option
        [@ocaml.doc
          "The type of destination for the telemetry data (e.g., \"Amazon CloudWatch Logs\", \"S3\")."];
      destinationPattern: String_.t option
        [@ocaml.doc
          "The pattern used to generate the destination path or name, supporting macros like <resourceId> and <accountId>."];
      retentionInDays: RetentionPeriodInDays.t option
        [@ocaml.doc
          "The number of days to retain the telemetry data in the destination."];
      vPCFlowLogParameters: VPCFlowLogParameters.t option
        [@ocaml.doc
          "Configuration parameters specific to VPC Flow Logs when VPC is the resource type."];
      cloudtrailParameters: CloudtrailParameters.t option
        [@ocaml.doc
          "Configuration parameters specific to Amazon Web Services CloudTrail when CloudTrail is the source type."];
      eLBLoadBalancerLoggingParameters:
        ELBLoadBalancerLoggingParameters.t option
        [@ocaml.doc
          "Configuration parameters specific to ELB load balancer logging when ELB is the resource type."];
      wAFLoggingParameters: WAFLoggingParameters.t option
        [@ocaml.doc
          "Configuration parameters specific to WAF logging when WAF is the resource type."];
      logDeliveryParameters: LogDeliveryParameters.t option
        [@ocaml.doc
          "Configuration parameters specific to Amazon Bedrock AgentCore logging when Amazon Bedrock AgentCore is the resource type."];
      mskMonitoringParameters: MskMonitoringParameters.t option
        [@ocaml.doc
          "Configuration parameters specific to MSK monitoring when MSK is the resource type."]}
    let make ?destinationType =
      fun ?destinationPattern ->
        fun ?retentionInDays ->
          fun ?vPCFlowLogParameters ->
            fun ?cloudtrailParameters ->
              fun ?eLBLoadBalancerLoggingParameters ->
                fun ?wAFLoggingParameters ->
                  fun ?logDeliveryParameters ->
                    fun ?mskMonitoringParameters ->
                      fun () ->
                        {
                          destinationType;
                          destinationPattern;
                          retentionInDays;
                          vPCFlowLogParameters;
                          cloudtrailParameters;
                          eLBLoadBalancerLoggingParameters;
                          wAFLoggingParameters;
                          logDeliveryParameters;
                          mskMonitoringParameters
                        }
    let to_value x =
      structure_to_value
        [("DestinationType",
           (Option.map x.destinationType ~f:DestinationType.to_value));
        ("DestinationPattern",
          (Option.map x.destinationPattern ~f:String_.to_value));
        ("RetentionInDays",
          (Option.map x.retentionInDays ~f:RetentionPeriodInDays.to_value));
        ("VPCFlowLogParameters",
          (Option.map x.vPCFlowLogParameters ~f:VPCFlowLogParameters.to_value));
        ("CloudtrailParameters",
          (Option.map x.cloudtrailParameters ~f:CloudtrailParameters.to_value));
        ("ELBLoadBalancerLoggingParameters",
          (Option.map x.eLBLoadBalancerLoggingParameters
             ~f:ELBLoadBalancerLoggingParameters.to_value));
        ("WAFLoggingParameters",
          (Option.map x.wAFLoggingParameters ~f:WAFLoggingParameters.to_value));
        ("LogDeliveryParameters",
          (Option.map x.logDeliveryParameters
             ~f:LogDeliveryParameters.to_value));
        ("MskMonitoringParameters",
          (Option.map x.mskMonitoringParameters
             ~f:MskMonitoringParameters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let mskMonitoringParameters =
        (Option.map ~f:MskMonitoringParameters.of_xml)
          (Xml.child xml_arg0 "MskMonitoringParameters") in
      let logDeliveryParameters =
        (Option.map ~f:LogDeliveryParameters.of_xml)
          (Xml.child xml_arg0 "LogDeliveryParameters") in
      let wAFLoggingParameters =
        (Option.map ~f:WAFLoggingParameters.of_xml)
          (Xml.child xml_arg0 "WAFLoggingParameters") in
      let eLBLoadBalancerLoggingParameters =
        (Option.map ~f:ELBLoadBalancerLoggingParameters.of_xml)
          (Xml.child xml_arg0 "ELBLoadBalancerLoggingParameters") in
      let cloudtrailParameters =
        (Option.map ~f:CloudtrailParameters.of_xml)
          (Xml.child xml_arg0 "CloudtrailParameters") in
      let vPCFlowLogParameters =
        (Option.map ~f:VPCFlowLogParameters.of_xml)
          (Xml.child xml_arg0 "VPCFlowLogParameters") in
      let retentionInDays =
        (Option.map ~f:RetentionPeriodInDays.of_xml)
          (Xml.child xml_arg0 "RetentionInDays") in
      let destinationPattern =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationPattern") in
      let destinationType =
        (Option.map ~f:DestinationType.of_xml)
          (Xml.child xml_arg0 "DestinationType") in
      make ?mskMonitoringParameters ?logDeliveryParameters
        ?wAFLoggingParameters ?eLBLoadBalancerLoggingParameters
        ?cloudtrailParameters ?vPCFlowLogParameters ?retentionInDays
        ?destinationPattern ?destinationType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let mskMonitoringParameters =
        field_map json__ "MskMonitoringParameters"
          MskMonitoringParameters.of_json in
      let logDeliveryParameters =
        field_map json__ "LogDeliveryParameters"
          LogDeliveryParameters.of_json in
      let wAFLoggingParameters =
        field_map json__ "WAFLoggingParameters" WAFLoggingParameters.of_json in
      let eLBLoadBalancerLoggingParameters =
        field_map json__ "ELBLoadBalancerLoggingParameters"
          ELBLoadBalancerLoggingParameters.of_json in
      let cloudtrailParameters =
        field_map json__ "CloudtrailParameters" CloudtrailParameters.of_json in
      let vPCFlowLogParameters =
        field_map json__ "VPCFlowLogParameters" VPCFlowLogParameters.of_json in
      let retentionInDays =
        field_map json__ "RetentionInDays" RetentionPeriodInDays.of_json in
      let destinationPattern =
        field_map json__ "DestinationPattern" String_.of_json in
      let destinationType =
        field_map json__ "DestinationType" DestinationType.of_json in
      make ?mskMonitoringParameters ?logDeliveryParameters
        ?wAFLoggingParameters ?eLBLoadBalancerLoggingParameters
        ?cloudtrailParameters ?vPCFlowLogParameters ?retentionInDays
        ?destinationPattern ?destinationType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration specifying where and how telemetry data should be delivered for Amazon Web Services resources."]
module CentralizationRuleDestination =
  struct
    type nonrec t =
      {
      region: Region.t
        [@ocaml.doc
          "The primary destination region to which telemetry data should be centralized."];
      account: AccountIdentifier.t option
        [@ocaml.doc
          "The destination account (within the organization) to which the telemetry data should be centralized."];
      destinationLogsConfiguration: DestinationLogsConfiguration.t option
        [@ocaml.doc
          "Log specific configuration for centralization destination log groups."]}
    let context_ = "CentralizationRuleDestination"
    let make ?account =
      fun ?destinationLogsConfiguration ->
        fun ~region ->
          fun () -> { account; destinationLogsConfiguration; region }
    let to_value x =
      structure_to_value
        [("Region", (Some (Region.to_value x.region)));
        ("Account", (Option.map x.account ~f:AccountIdentifier.to_value));
        ("DestinationLogsConfiguration",
          (Option.map x.destinationLogsConfiguration
             ~f:DestinationLogsConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let destinationLogsConfiguration =
        (Option.map ~f:DestinationLogsConfiguration.of_xml)
          (Xml.child xml_arg0 "DestinationLogsConfiguration") in
      let account =
        (Option.map ~f:AccountIdentifier.of_xml)
          (Xml.child xml_arg0 "Account") in
      let region =
        Region.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Region") in
      make ?destinationLogsConfiguration ?account ~region ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let destinationLogsConfiguration =
        field_map json__ "DestinationLogsConfiguration"
          DestinationLogsConfiguration.of_json in
      let account = field_map json__ "Account" AccountIdentifier.of_json in
      let region = field_map_exn json__ "Region" Region.of_json in
      make ?destinationLogsConfiguration ?account ~region ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration specifying the primary destination for centralized telemetry data."]
module CentralizationRuleSource =
  struct
    type nonrec t =
      {
      regions: Regions.t
        [@ocaml.doc
          "The list of source regions from which telemetry data should be centralized."];
      scope: SourceFilterString.t option
        [@ocaml.doc
          "The organizational scope from which telemetry data should be centralized, specified using organization id, accounts or organizational unit ids."];
      sourceLogsConfiguration: SourceLogsConfiguration.t option
        [@ocaml.doc
          "Log specific configuration for centralization source log groups."]}
    let context_ = "CentralizationRuleSource"
    let make ?scope =
      fun ?sourceLogsConfiguration ->
        fun ~regions -> fun () -> { scope; sourceLogsConfiguration; regions }
    let to_value x =
      structure_to_value
        [("Regions", (Some (Regions.to_value x.regions)));
        ("Scope", (Option.map x.scope ~f:SourceFilterString.to_value));
        ("SourceLogsConfiguration",
          (Option.map x.sourceLogsConfiguration
             ~f:SourceLogsConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourceLogsConfiguration =
        (Option.map ~f:SourceLogsConfiguration.of_xml)
          (Xml.child xml_arg0 "SourceLogsConfiguration") in
      let scope =
        (Option.map ~f:SourceFilterString.of_xml)
          (Xml.child xml_arg0 "Scope") in
      let regions =
        Regions.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Regions") in
      make ?sourceLogsConfiguration ?scope ~regions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sourceLogsConfiguration =
        field_map json__ "SourceLogsConfiguration"
          SourceLogsConfiguration.of_json in
      let scope = field_map json__ "Scope" SourceFilterString.of_json in
      let regions = field_map_exn json__ "Regions" Regions.of_json in
      make ?sourceLogsConfiguration ?scope ~regions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration specifying the source of telemetry data to be centralized."]
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module PipelineOutput =
  struct
    type nonrec t =
      {
      record: Record.t option
        [@ocaml.doc
          "The processed record output from the pipeline test operation."];
      error: PipelineOutputError.t option
        [@ocaml.doc
          "Any error that occurred during the pipeline test operation for this record."]}
    let make ?record = fun ?error -> fun () -> { record; error }
    let to_value x =
      structure_to_value
        [("Record", (Option.map x.record ~f:Record.to_value));
        ("Error", (Option.map x.error ~f:PipelineOutputError.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let error =
        (Option.map ~f:PipelineOutputError.of_xml)
          (Xml.child xml_arg0 "Error") in
      let record =
        (Option.map ~f:Record.of_xml) (Xml.child xml_arg0 "Record") in
      make ?error ?record ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let error = field_map json__ "Error" PipelineOutputError.of_json in
      let record = field_map json__ "Record" Record.of_json in
      make ?error ?record ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the output from pipeline test operations, including processed records and any errors encountered."]
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module TelemetryRuleSummary =
  struct
    type nonrec t =
      {
      ruleName: RuleName.t option
        [@ocaml.doc "The name of the telemetry rule."];
      ruleArn: ResourceArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the telemetry rule."];
      createdTimeStamp: Long.t option
        [@ocaml.doc "The timestamp when the telemetry rule was created."];
      lastUpdateTimeStamp: Long.t option
        [@ocaml.doc
          "The timestamp when the telemetry rule was last modified."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The type of Amazon Web Services resource the rule applies to."];
      telemetryType: TelemetryType.t option
        [@ocaml.doc
          "The type of telemetry (Logs, Metrics, or Traces) the rule configures."];
      telemetrySourceTypes: TelemetrySourceTypes.t option
        [@ocaml.doc
          "The types of telemetry sources configured for this rule, such as VPC Flow Logs or EKS audit logs. TelemetrySourceTypes must be correlated with the specific resource type."]}
    let make ?ruleName =
      fun ?ruleArn ->
        fun ?createdTimeStamp ->
          fun ?lastUpdateTimeStamp ->
            fun ?resourceType ->
              fun ?telemetryType ->
                fun ?telemetrySourceTypes ->
                  fun () ->
                    {
                      ruleName;
                      ruleArn;
                      createdTimeStamp;
                      lastUpdateTimeStamp;
                      resourceType;
                      telemetryType;
                      telemetrySourceTypes
                    }
    let to_value x =
      structure_to_value
        [("RuleName", (Option.map x.ruleName ~f:RuleName.to_value));
        ("RuleArn", (Option.map x.ruleArn ~f:ResourceArn.to_value));
        ("CreatedTimeStamp",
          (Option.map x.createdTimeStamp ~f:Long.to_value));
        ("LastUpdateTimeStamp",
          (Option.map x.lastUpdateTimeStamp ~f:Long.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("TelemetryType",
          (Option.map x.telemetryType ~f:TelemetryType.to_value));
        ("TelemetrySourceTypes",
          (Option.map x.telemetrySourceTypes ~f:TelemetrySourceTypes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let telemetrySourceTypes =
        (Option.map ~f:TelemetrySourceTypes.of_xml)
          (Xml.child xml_arg0 "TelemetrySourceTypes") in
      let telemetryType =
        (Option.map ~f:TelemetryType.of_xml)
          (Xml.child xml_arg0 "TelemetryType") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let lastUpdateTimeStamp =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "LastUpdateTimeStamp") in
      let createdTimeStamp =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "CreatedTimeStamp") in
      let ruleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RuleArn") in
      let ruleName =
        (Option.map ~f:RuleName.of_xml) (Xml.child xml_arg0 "RuleName") in
      make ?telemetrySourceTypes ?telemetryType ?resourceType
        ?lastUpdateTimeStamp ?createdTimeStamp ?ruleArn ?ruleName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let telemetrySourceTypes =
        field_map json__ "TelemetrySourceTypes" TelemetrySourceTypes.of_json in
      let telemetryType =
        field_map json__ "TelemetryType" TelemetryType.of_json in
      let resourceType = field_map json__ "ResourceType" ResourceType.of_json in
      let lastUpdateTimeStamp =
        field_map json__ "LastUpdateTimeStamp" Long.of_json in
      let createdTimeStamp = field_map json__ "CreatedTimeStamp" Long.of_json in
      let ruleArn = field_map json__ "RuleArn" ResourceArn.of_json in
      let ruleName = field_map json__ "RuleName" RuleName.of_json in
      make ?telemetrySourceTypes ?telemetryType ?resourceType
        ?lastUpdateTimeStamp ?createdTimeStamp ?ruleArn ?ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A summary of a telemetry rule's key properties."]
module OrganizationUnitIdentifier =
  struct
    type nonrec t = string
    let context_ = "OrganizationUnitIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"ou-[0-9a-z]{4,32}-[a-z0-9]{8,32}");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"OrganizationUnitIdentifier" j
    let to_json = simple_to_json to_value
  end
module TelemetryPipelineSummary =
  struct
    type nonrec t =
      {
      createdTimeStamp: Long.t option
        [@ocaml.doc "The timestamp when the telemetry pipeline was created."];
      lastUpdateTimeStamp: Long.t option
        [@ocaml.doc
          "The timestamp when the telemetry pipeline was last updated."];
      arn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the telemetry pipeline."];
      name: TelemetryPipelineName.t option
        [@ocaml.doc "The name of the telemetry pipeline."];
      status: TelemetryPipelineStatus.t option
        [@ocaml.doc "The current status of the telemetry pipeline."];
      tags: TagMapOutput.t option
        [@ocaml.doc
          "The key-value pairs associated with the telemetry pipeline resource."];
      configurationSummary: ConfigurationSummary.t option
        [@ocaml.doc "A summary of the pipeline configuration components."]}
    let make ?createdTimeStamp =
      fun ?lastUpdateTimeStamp ->
        fun ?arn ->
          fun ?name ->
            fun ?status ->
              fun ?tags ->
                fun ?configurationSummary ->
                  fun () ->
                    {
                      createdTimeStamp;
                      lastUpdateTimeStamp;
                      arn;
                      name;
                      status;
                      tags;
                      configurationSummary
                    }
    let to_value x =
      structure_to_value
        [("CreatedTimeStamp",
           (Option.map x.createdTimeStamp ~f:Long.to_value));
        ("LastUpdateTimeStamp",
          (Option.map x.lastUpdateTimeStamp ~f:Long.to_value));
        ("Arn", (Option.map x.arn ~f:ResourceArn.to_value));
        ("Name", (Option.map x.name ~f:TelemetryPipelineName.to_value));
        ("Status", (Option.map x.status ~f:TelemetryPipelineStatus.to_value));
        ("Tags", (Option.map x.tags ~f:TagMapOutput.to_value));
        ("ConfigurationSummary",
          (Option.map x.configurationSummary ~f:ConfigurationSummary.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurationSummary =
        (Option.map ~f:ConfigurationSummary.of_xml)
          (Xml.child xml_arg0 "ConfigurationSummary") in
      let tags =
        (Option.map ~f:TagMapOutput.of_xml) (Xml.child xml_arg0 "Tags") in
      let status =
        (Option.map ~f:TelemetryPipelineStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let name =
        (Option.map ~f:TelemetryPipelineName.of_xml)
          (Xml.child xml_arg0 "Name") in
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "Arn") in
      let lastUpdateTimeStamp =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "LastUpdateTimeStamp") in
      let createdTimeStamp =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "CreatedTimeStamp") in
      make ?configurationSummary ?tags ?status ?name ?arn
        ?lastUpdateTimeStamp ?createdTimeStamp ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let configurationSummary =
        field_map json__ "ConfigurationSummary" ConfigurationSummary.of_json in
      let tags = field_map json__ "Tags" TagMapOutput.of_json in
      let status = field_map json__ "Status" TelemetryPipelineStatus.of_json in
      let name = field_map json__ "Name" TelemetryPipelineName.of_json in
      let arn = field_map json__ "Arn" ResourceArn.of_json in
      let lastUpdateTimeStamp =
        field_map json__ "LastUpdateTimeStamp" Long.of_json in
      let createdTimeStamp = field_map json__ "CreatedTimeStamp" Long.of_json in
      make ?configurationSummary ?tags ?status ?name ?arn
        ?lastUpdateTimeStamp ?createdTimeStamp ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains summary information about a telemetry pipeline for listing operations."]
module IntegrationSummary =
  struct
    type nonrec t =
      {
      arn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the S3 Table integration."];
      status: IntegrationStatus.t option
        [@ocaml.doc "The current status of the S3 Table integration."]}
    let make ?arn = fun ?status -> fun () -> { arn; status }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ResourceArn.to_value));
        ("Status", (Option.map x.status ~f:IntegrationStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:IntegrationStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?status ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "Status" IntegrationStatus.of_json in
      let arn = field_map json__ "Arn" ResourceArn.of_json in
      make ?status ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains summary information about an S3 Table integration for listing operations."]
module TelemetryConfiguration =
  struct
    type nonrec t =
      {
      accountIdentifier: AccountIdentifier.t option
        [@ocaml.doc
          "The account ID which contains the resource managed in telemetry configuration. An example of a valid account ID is 012345678901."];
      telemetryConfigurationState: TelemetryConfigurationState.t option
        [@ocaml.doc
          "The configuration state for the resource, for example \\{ Logs: NotApplicable; Metrics: Enabled; Traces: NotApplicable; \\}."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The type of resource, for example Amazon Web Services::EC2::Instance, or Amazon Web Services::EKS::Cluster, etc."];
      resourceIdentifier: ResourceIdentifier.t option
        [@ocaml.doc
          "The identifier of the resource, for example for Amazon VPC, it would be vpc-1a2b3c4d5e6f1a2b3."];
      resourceTags: TagMapOutput.t option
        [@ocaml.doc
          "Tags associated with the resource, for example \\{ Name: \"ExampleInstance\", Environment: \"Development\" \\}."];
      lastUpdateTimeStamp: Long.t option
        [@ocaml.doc
          "The timestamp of the last change to the telemetry configuration for the resource. For example, 1728679196318."];
      telemetrySourceType: TelemetrySourceType.t option }
    let make ?accountIdentifier =
      fun ?telemetryConfigurationState ->
        fun ?resourceType ->
          fun ?resourceIdentifier ->
            fun ?resourceTags ->
              fun ?lastUpdateTimeStamp ->
                fun ?telemetrySourceType ->
                  fun () ->
                    {
                      accountIdentifier;
                      telemetryConfigurationState;
                      resourceType;
                      resourceIdentifier;
                      resourceTags;
                      lastUpdateTimeStamp;
                      telemetrySourceType
                    }
    let to_value x =
      structure_to_value
        [("AccountIdentifier",
           (Option.map x.accountIdentifier ~f:AccountIdentifier.to_value));
        ("TelemetryConfigurationState",
          (Option.map x.telemetryConfigurationState
             ~f:TelemetryConfigurationState.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("ResourceIdentifier",
          (Option.map x.resourceIdentifier ~f:ResourceIdentifier.to_value));
        ("ResourceTags",
          (Option.map x.resourceTags ~f:TagMapOutput.to_value));
        ("LastUpdateTimeStamp",
          (Option.map x.lastUpdateTimeStamp ~f:Long.to_value));
        ("TelemetrySourceType",
          (Option.map x.telemetrySourceType ~f:TelemetrySourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let telemetrySourceType =
        (Option.map ~f:TelemetrySourceType.of_xml)
          (Xml.child xml_arg0 "TelemetrySourceType") in
      let lastUpdateTimeStamp =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "LastUpdateTimeStamp") in
      let resourceTags =
        (Option.map ~f:TagMapOutput.of_xml)
          (Xml.child xml_arg0 "ResourceTags") in
      let resourceIdentifier =
        (Option.map ~f:ResourceIdentifier.of_xml)
          (Xml.child xml_arg0 "ResourceIdentifier") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let telemetryConfigurationState =
        (Option.map ~f:TelemetryConfigurationState.of_xml)
          (Xml.child xml_arg0 "TelemetryConfigurationState") in
      let accountIdentifier =
        (Option.map ~f:AccountIdentifier.of_xml)
          (Xml.child xml_arg0 "AccountIdentifier") in
      make ?telemetrySourceType ?lastUpdateTimeStamp ?resourceTags
        ?resourceIdentifier ?resourceType ?telemetryConfigurationState
        ?accountIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let telemetrySourceType =
        field_map json__ "TelemetrySourceType" TelemetrySourceType.of_json in
      let lastUpdateTimeStamp =
        field_map json__ "LastUpdateTimeStamp" Long.of_json in
      let resourceTags = field_map json__ "ResourceTags" TagMapOutput.of_json in
      let resourceIdentifier =
        field_map json__ "ResourceIdentifier" ResourceIdentifier.of_json in
      let resourceType = field_map json__ "ResourceType" ResourceType.of_json in
      let telemetryConfigurationState =
        field_map json__ "TelemetryConfigurationState"
          TelemetryConfigurationState.of_json in
      let accountIdentifier =
        field_map json__ "AccountIdentifier" AccountIdentifier.of_json in
      make ?telemetrySourceType ?lastUpdateTimeStamp ?resourceTags
        ?resourceIdentifier ?resourceType ?telemetryConfigurationState
        ?accountIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A model representing the state of a resource within an account according to telemetry config."]
module CentralizationRuleSummary =
  struct
    type nonrec t =
      {
      ruleName: RuleName.t option
        [@ocaml.doc "The name of the organization centralization rule."];
      ruleArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the organization centralization rule."];
      creatorAccountId: String_.t option
        [@ocaml.doc
          "The Amazon Web Services Account that created the organization centralization rule."];
      createdTimeStamp: Long.t option
        [@ocaml.doc
          "The timestamp when the organization centralization rule was created."];
      createdRegion: Region.t option
        [@ocaml.doc
          "The Amazon Web Services region where the organization centralization rule was created."];
      lastUpdateTimeStamp: Long.t option
        [@ocaml.doc
          "The timestamp when the organization centralization rule was last updated."];
      ruleHealth: RuleHealth.t option
        [@ocaml.doc
          "The health status of the organization centralization rule."];
      failureReason: CentralizationFailureReason.t option
        [@ocaml.doc
          "The reason why an organization centralization rule is marked UNHEALTHY."];
      destinationAccountId: String_.t option
        [@ocaml.doc
          "The primary destination account of the organization centralization rule."];
      destinationRegion: Region.t option
        [@ocaml.doc
          "The primary destination region of the organization centralization rule."]}
    let make ?ruleName =
      fun ?ruleArn ->
        fun ?creatorAccountId ->
          fun ?createdTimeStamp ->
            fun ?createdRegion ->
              fun ?lastUpdateTimeStamp ->
                fun ?ruleHealth ->
                  fun ?failureReason ->
                    fun ?destinationAccountId ->
                      fun ?destinationRegion ->
                        fun () ->
                          {
                            ruleName;
                            ruleArn;
                            creatorAccountId;
                            createdTimeStamp;
                            createdRegion;
                            lastUpdateTimeStamp;
                            ruleHealth;
                            failureReason;
                            destinationAccountId;
                            destinationRegion
                          }
    let to_value x =
      structure_to_value
        [("RuleName", (Option.map x.ruleName ~f:RuleName.to_value));
        ("RuleArn", (Option.map x.ruleArn ~f:ResourceArn.to_value));
        ("CreatorAccountId",
          (Option.map x.creatorAccountId ~f:String_.to_value));
        ("CreatedTimeStamp",
          (Option.map x.createdTimeStamp ~f:Long.to_value));
        ("CreatedRegion", (Option.map x.createdRegion ~f:Region.to_value));
        ("LastUpdateTimeStamp",
          (Option.map x.lastUpdateTimeStamp ~f:Long.to_value));
        ("RuleHealth", (Option.map x.ruleHealth ~f:RuleHealth.to_value));
        ("FailureReason",
          (Option.map x.failureReason ~f:CentralizationFailureReason.to_value));
        ("DestinationAccountId",
          (Option.map x.destinationAccountId ~f:String_.to_value));
        ("DestinationRegion",
          (Option.map x.destinationRegion ~f:Region.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let destinationRegion =
        (Option.map ~f:Region.of_xml)
          (Xml.child xml_arg0 "DestinationRegion") in
      let destinationAccountId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DestinationAccountId") in
      let failureReason =
        (Option.map ~f:CentralizationFailureReason.of_xml)
          (Xml.child xml_arg0 "FailureReason") in
      let ruleHealth =
        (Option.map ~f:RuleHealth.of_xml) (Xml.child xml_arg0 "RuleHealth") in
      let lastUpdateTimeStamp =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "LastUpdateTimeStamp") in
      let createdRegion =
        (Option.map ~f:Region.of_xml) (Xml.child xml_arg0 "CreatedRegion") in
      let createdTimeStamp =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "CreatedTimeStamp") in
      let creatorAccountId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CreatorAccountId") in
      let ruleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RuleArn") in
      let ruleName =
        (Option.map ~f:RuleName.of_xml) (Xml.child xml_arg0 "RuleName") in
      make ?destinationRegion ?destinationAccountId ?failureReason
        ?ruleHealth ?lastUpdateTimeStamp ?createdRegion ?createdTimeStamp
        ?creatorAccountId ?ruleArn ?ruleName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let destinationRegion =
        field_map json__ "DestinationRegion" Region.of_json in
      let destinationAccountId =
        field_map json__ "DestinationAccountId" String_.of_json in
      let failureReason =
        field_map json__ "FailureReason" CentralizationFailureReason.of_json in
      let ruleHealth = field_map json__ "RuleHealth" RuleHealth.of_json in
      let lastUpdateTimeStamp =
        field_map json__ "LastUpdateTimeStamp" Long.of_json in
      let createdRegion = field_map json__ "CreatedRegion" Region.of_json in
      let createdTimeStamp = field_map json__ "CreatedTimeStamp" Long.of_json in
      let creatorAccountId =
        field_map json__ "CreatorAccountId" String_.of_json in
      let ruleArn = field_map json__ "RuleArn" ResourceArn.of_json in
      let ruleName = field_map json__ "RuleName" RuleName.of_json in
      make ?destinationRegion ?destinationAccountId ?failureReason
        ?ruleHealth ?lastUpdateTimeStamp ?createdRegion ?createdTimeStamp
        ?creatorAccountId ?ruleArn ?ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A summary of a centralization rule's key properties and status."]
module RegionStatus =
  struct
    type nonrec t =
      {
      region: Region.t option
        [@ocaml.doc
          "The Amazon Web Services Region code (for example, eu-west-1 or us-west-2) that this status applies to."];
      status: String_.t option
        [@ocaml.doc
          "The status of the operation in this region. For telemetry evaluation, valid values include STARTING, RUNNING, and FAILED_START. For telemetry rules, valid values include PENDING, ACTIVE, and FAILED."];
      failureReason: String_.t option
        [@ocaml.doc
          "The reason for a failure status in this region. This field is only populated when Status indicates a failure."];
      ruleArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the telemetry rule in this spoke region. This field is only present for telemetry rule region statuses and is populated when the rule has been successfully created in the spoke region (status is ACTIVE)."]}
    let make ?region =
      fun ?status ->
        fun ?failureReason ->
          fun ?ruleArn ->
            fun () -> { region; status; failureReason; ruleArn }
    let to_value x =
      structure_to_value
        [("Region", (Option.map x.region ~f:Region.to_value));
        ("Status", (Option.map x.status ~f:String_.to_value));
        ("FailureReason", (Option.map x.failureReason ~f:String_.to_value));
        ("RuleArn", (Option.map x.ruleArn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RuleArn") in
      let failureReason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "FailureReason") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Status") in
      let region =
        (Option.map ~f:Region.of_xml) (Xml.child xml_arg0 "Region") in
      make ?ruleArn ?failureReason ?status ?region ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleArn = field_map json__ "RuleArn" ResourceArn.of_json in
      let failureReason = field_map json__ "FailureReason" String_.of_json in
      let status = field_map json__ "Status" String_.of_json in
      let region = field_map json__ "Region" Region.of_json in
      make ?ruleArn ?failureReason ?status ?region ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the status of a multi-region operation in a specific Amazon Web Services Region. This structure is used to report per-region progress for both telemetry evaluation and telemetry rule replication."]
module TelemetryPipelineConfiguration =
  struct
    type nonrec t =
      {
      body: TelemetryPipelineConfigurationBody.t
        [@ocaml.doc
          "The pipeline configuration body that defines the data processing rules and transformations."]}
    let context_ = "TelemetryPipelineConfiguration"
    let make ~body = fun () -> { body }
    let to_value x =
      structure_to_value
        [("Body",
           (Some (TelemetryPipelineConfigurationBody.to_value x.body)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let body =
        TelemetryPipelineConfigurationBody.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Body") in
      make ~body ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let body =
        field_map_exn json__ "Body"
          TelemetryPipelineConfigurationBody.of_json in
      make ~body ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines the configuration for a pipeline, including how data flows from sources through processors to destinations. The configuration is specified in YAML format and must include a valid pipeline definition with required source and sink components. This pipeline enables end-to-end telemetry data collection, transformation, and delivery while supporting optional processing steps and extensions for enhanced functionality. The primary pipeline configuration section are: Source: Defines where log data originates from (S3 buckets, CloudWatch Logs, third-party APIs). Each pipeline must have exactly one source. Processors (optional): Transform, parse, and enrich log data as it flows through the pipeline. Processors are applied sequentially in the order they are defined. Sink: Defines the destination where processed log data is sent. Each pipeline must have exactly one sink. Extensions (optional): Provide additional functionality such as Amazon Web Services Secrets Manager integration for credential management. For more details on each configuration section see CloudWatch pipelines User Guide. Additional comprehensive configuration examples can be found in the CreateTelemetryPipeline API docs."]
module TelemetryPipelineStatusReason =
  struct
    type nonrec t =
      {
      description: String_.t option
        [@ocaml.doc
          "A description of the pipeline status reason, providing additional context about the current state."]}
    let make ?description = fun () -> { description }
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      make ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description = field_map json__ "Description" String_.of_json in
      make ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides detailed information about the status of a telemetry pipeline, including reasons for specific states."]
module SSEAlgorithm =
  struct
    type nonrec t =
      | Aws_kms 
      | AES256 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Aws_kms -> "aws:kms"
      | AES256 -> "AES256"
      | Non_static_id s -> s
    let of_string =
      function
      | "aws:kms" -> Aws_kms
      | "AES256" -> AES256
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SSEAlgorithm" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SSEAlgorithm" j)
    let to_json = simple_to_json to_value
  end
module AccessDeniedException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      amznErrorType: String_.t option
        [@ocaml.doc "The name of the exception."]}
    let make ?message =
      fun ?amznErrorType -> fun () -> { message; amznErrorType }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("x-amzn-ErrorType",
          (Option.map x.amznErrorType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let amznErrorType =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amzn-ErrorType") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?amznErrorType ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let amznErrorType = field_map json__ "amznErrorType" String_.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?amznErrorType ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates you don't have permissions to perform the requested operation. The user or role that is making the request must have at least one IAM permissions policy attached that grants the required permissions. For more information, see Access management for Amazon Web Services resources in the IAM user guide."]
module InternalServerException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      amznErrorType: String_.t option
        [@ocaml.doc "The name of the exception."];
      retryAfterSeconds: Integer.t option
        [@ocaml.doc
          "The number of seconds to wait before retrying the request."]}
    let make ?message =
      fun ?amznErrorType ->
        fun ?retryAfterSeconds ->
          fun () -> { message; amznErrorType; retryAfterSeconds }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("x-amzn-ErrorType",
          (Option.map x.amznErrorType ~f:String_.to_value));
        ("Retry-After", (Option.map x.retryAfterSeconds ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retryAfterSeconds =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Retry-After") in
      let amznErrorType =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amzn-ErrorType") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?retryAfterSeconds ?amznErrorType ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retryAfterSeconds =
        field_map json__ "retryAfterSeconds" Integer.of_json in
      let amznErrorType = field_map json__ "amznErrorType" String_.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?retryAfterSeconds ?amznErrorType ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates the request has failed to process because of an unknown server error, exception, or failure."]
module TooManyRequestsException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request throughput limit was exceeded."]
module ValidationException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      errors: ValidationErrors.t option
        [@ocaml.doc "The errors in the input which caused the exception."]}
    let make ?message = fun ?errors -> fun () -> { message; errors }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("Errors", (Option.map x.errors ~f:ValidationErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errors =
        (Option.map ~f:ValidationErrors.of_xml) (Xml.child xml_arg0 "Errors") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?errors ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let errors = field_map json__ "Errors" ValidationErrors.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?errors ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates input validation failed. Check your request parameters and retry the request."]
module ConflictException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      resourceId: String_.t option
        [@ocaml.doc
          "The identifier of the resource which is in conflict with the requested operation."];
      resourceType: String_.t option
        [@ocaml.doc
          "The type of the resource which is in conflict with the requested operation."]}
    let make ?message =
      fun ?resourceId ->
        fun ?resourceType -> fun () -> { message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("ResourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("ResourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceType ?resourceId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "ResourceType" String_.of_json in
      let resourceId = field_map json__ "ResourceId" String_.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?resourceType ?resourceId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested operation conflicts with the current state of the specified resource or with another request."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      resourceId: String_.t option
        [@ocaml.doc
          "The identifier of the resource which could not be found."];
      resourceType: String_.t option
        [@ocaml.doc "The type of the resource which could not be found."]}
    let make ?message =
      fun ?resourceId ->
        fun ?resourceType -> fun () -> { message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("ResourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("ResourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceType ?resourceId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "ResourceType" String_.of_json in
      let resourceId = field_map json__ "ResourceId" String_.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?resourceType ?resourceId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified resource (such as a telemetry rule) could not be found."]
module ServiceQuotaExceededException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      resourceId: String_.t option
        [@ocaml.doc
          "The identifier of the resource which exceeds the service quota."];
      resourceType: String_.t option
        [@ocaml.doc
          "The type of the resource which exceeds the service quota."];
      serviceCode: String_.t option
        [@ocaml.doc "The code for the service of the exceeded quota."];
      quotaCode: String_.t option
        [@ocaml.doc "The code for the exceeded service quota."];
      amznErrorType: String_.t option
        [@ocaml.doc "The name of the exception."]}
    let make ?message =
      fun ?resourceId ->
        fun ?resourceType ->
          fun ?serviceCode ->
            fun ?quotaCode ->
              fun ?amznErrorType ->
                fun () ->
                  {
                    message;
                    resourceId;
                    resourceType;
                    serviceCode;
                    quotaCode;
                    amznErrorType
                  }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("ResourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("ResourceType", (Option.map x.resourceType ~f:String_.to_value));
        ("ServiceCode", (Option.map x.serviceCode ~f:String_.to_value));
        ("QuotaCode", (Option.map x.quotaCode ~f:String_.to_value));
        ("x-amzn-ErrorType",
          (Option.map x.amznErrorType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let amznErrorType =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amzn-ErrorType") in
      let quotaCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "QuotaCode") in
      let serviceCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ServiceCode") in
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?amznErrorType ?quotaCode ?serviceCode ?resourceType ?resourceId
        ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let amznErrorType = field_map json__ "amznErrorType" String_.of_json in
      let quotaCode = field_map json__ "QuotaCode" String_.of_json in
      let serviceCode = field_map json__ "ServiceCode" String_.of_json in
      let resourceType = field_map json__ "ResourceType" String_.of_json in
      let resourceId = field_map json__ "ResourceId" String_.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?amznErrorType ?quotaCode ?serviceCode ?resourceType ?resourceId
        ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested operation would exceed the allowed quota for the specified resource type."]
module RuleIdentifier =
  struct
    type nonrec t = string
    let context_ = "RuleIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1011) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RuleIdentifier" j
    let to_json = simple_to_json to_value
  end
module TelemetryRule =
  struct
    type nonrec t =
      {
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The type of Amazon Web Services resource to configure telemetry for (e.g., \"AWS::EC2::VPC\", \"AWS::EKS::Cluster\", \"AWS::WAFv2::WebACL\")."];
      telemetryType: TelemetryType.t
        [@ocaml.doc
          "The type of telemetry to collect (Logs, Metrics, or Traces)."];
      telemetrySourceTypes: TelemetrySourceTypes.t option
        [@ocaml.doc
          "The specific telemetry source types to configure for the resource, such as VPC_FLOW_LOGS or EKS_AUDIT_LOGS. TelemetrySourceTypes must be correlated with the specific resource type."];
      destinationConfiguration: TelemetryDestinationConfiguration.t option
        [@ocaml.doc
          "Configuration specifying where and how the telemetry data should be delivered."];
      scope: String_.t option
        [@ocaml.doc
          "The organizational scope to which the rule applies, specified using accounts or organizational units."];
      selectionCriteria: String_.t option
        [@ocaml.doc
          "Criteria for selecting which resources the rule applies to, such as resource tags."];
      allowFieldUpdates: Boolean.t option
        [@ocaml.doc
          "If set to true, Amazon CloudWatch Observability Admin detects and remediates configuration drift in telemetry resources that it manages. For example, if a VPC flow log's format, traffic type, or aggregation interval no longer matches the rule's destination configuration, the flow log is replaced with one that matches. Only Observability Admin-managed resources are updated; customer-created resources are never modified. Currently supported for AWS::EC2::VPC resources (VPC flow logs)."];
      regions: Regions.t option
        [@ocaml.doc
          "An optional list of Amazon Web Services Regions where this telemetry rule should be replicated. When specified, the rule is created in the home region and automatically replicated to all listed regions. Mutually exclusive with AllRegions."];
      allRegions: AllRegions.t option
        [@ocaml.doc
          "If set to true, the telemetry rule is replicated to all Amazon Web Services Regions where Amazon CloudWatch Observability Admin is available in the current partition. When new regions become available, the rule automatically replicates to them. Mutually exclusive with Regions."]}
    let context_ = "TelemetryRule"
    let make ?resourceType =
      fun ?telemetrySourceTypes ->
        fun ?destinationConfiguration ->
          fun ?scope ->
            fun ?selectionCriteria ->
              fun ?allowFieldUpdates ->
                fun ?regions ->
                  fun ?allRegions ->
                    fun ~telemetryType ->
                      fun () ->
                        {
                          resourceType;
                          telemetrySourceTypes;
                          destinationConfiguration;
                          scope;
                          selectionCriteria;
                          allowFieldUpdates;
                          regions;
                          allRegions;
                          telemetryType
                        }
    let to_value x =
      structure_to_value
        [("ResourceType",
           (Option.map x.resourceType ~f:ResourceType.to_value));
        ("TelemetryType", (Some (TelemetryType.to_value x.telemetryType)));
        ("TelemetrySourceTypes",
          (Option.map x.telemetrySourceTypes ~f:TelemetrySourceTypes.to_value));
        ("DestinationConfiguration",
          (Option.map x.destinationConfiguration
             ~f:TelemetryDestinationConfiguration.to_value));
        ("Scope", (Option.map x.scope ~f:String_.to_value));
        ("SelectionCriteria",
          (Option.map x.selectionCriteria ~f:String_.to_value));
        ("AllowFieldUpdates",
          (Option.map x.allowFieldUpdates ~f:Boolean.to_value));
        ("Regions", (Option.map x.regions ~f:Regions.to_value));
        ("AllRegions", (Option.map x.allRegions ~f:AllRegions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let allRegions =
        (Option.map ~f:AllRegions.of_xml) (Xml.child xml_arg0 "AllRegions") in
      let regions =
        (Option.map ~f:Regions.of_xml) (Xml.child xml_arg0 "Regions") in
      let allowFieldUpdates =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "AllowFieldUpdates") in
      let selectionCriteria =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "SelectionCriteria") in
      let scope = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Scope") in
      let destinationConfiguration =
        (Option.map ~f:TelemetryDestinationConfiguration.of_xml)
          (Xml.child xml_arg0 "DestinationConfiguration") in
      let telemetrySourceTypes =
        (Option.map ~f:TelemetrySourceTypes.of_xml)
          (Xml.child xml_arg0 "TelemetrySourceTypes") in
      let telemetryType =
        TelemetryType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TelemetryType") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      make ?allRegions ?regions ?allowFieldUpdates ?selectionCriteria ?scope
        ?destinationConfiguration ?telemetrySourceTypes ~telemetryType
        ?resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let allRegions = field_map json__ "AllRegions" AllRegions.of_json in
      let regions = field_map json__ "Regions" Regions.of_json in
      let allowFieldUpdates =
        field_map json__ "AllowFieldUpdates" Boolean.of_json in
      let selectionCriteria =
        field_map json__ "SelectionCriteria" String_.of_json in
      let scope = field_map json__ "Scope" String_.of_json in
      let destinationConfiguration =
        field_map json__ "DestinationConfiguration"
          TelemetryDestinationConfiguration.of_json in
      let telemetrySourceTypes =
        field_map json__ "TelemetrySourceTypes" TelemetrySourceTypes.of_json in
      let telemetryType =
        field_map_exn json__ "TelemetryType" TelemetryType.of_json in
      let resourceType = field_map json__ "ResourceType" ResourceType.of_json in
      make ?allRegions ?regions ?allowFieldUpdates ?selectionCriteria ?scope
        ?destinationConfiguration ?telemetrySourceTypes ~telemetryType
        ?resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines how telemetry should be configured for specific Amazon Web Services resources."]
module TelemetryPipelineIdentifier =
  struct
    type nonrec t = string
    let context_ = "TelemetryPipelineIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TelemetryPipelineIdentifier" j
    let to_json = simple_to_json to_value
  end
module CentralizationRule =
  struct
    type nonrec t =
      {
      source: CentralizationRuleSource.t
        [@ocaml.doc
          "Configuration determining the source of the telemetry data to be centralized."];
      destination: CentralizationRuleDestination.t
        [@ocaml.doc
          "Configuration determining where the telemetry data should be centralized, backed up, as well as encryption configuration for the primary and backup destinations."]}
    let context_ = "CentralizationRule"
    let make ~source = fun ~destination -> fun () -> { source; destination }
    let to_value x =
      structure_to_value
        [("Source", (Some (CentralizationRuleSource.to_value x.source)));
        ("Destination",
          (Some (CentralizationRuleDestination.to_value x.destination)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let destination =
        CentralizationRuleDestination.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Destination") in
      let source =
        CentralizationRuleSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Source") in
      make ~destination ~source ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let destination =
        field_map_exn json__ "Destination"
          CentralizationRuleDestination.of_json in
      let source =
        field_map_exn json__ "Source" CentralizationRuleSource.of_json in
      make ~destination ~source ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines how telemetry data should be centralized across an Amazon Web Services Organization, including source and destination configurations."]
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module PipelineOutputs =
  struct
    type nonrec t = PipelineOutput.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PipelineOutput.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PipelineOutput.of_xml)
    let of_json j =
      list_of_json ~kind:"PipelineOutputs" ~of_json:PipelineOutput.of_json j
    let to_json v = composed_to_json to_value v
  end
module Records =
  struct
    type nonrec t = Record.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Record.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Record.of_xml)
    let of_json j = list_of_json ~kind:"Records" ~of_json:Record.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagMapInput =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module TelemetryEnrichmentStatus =
  struct
    type nonrec t =
      | Running 
      | Stopped 
      | Impaired 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Running -> "Running"
      | Stopped -> "Stopped"
      | Impaired -> "Impaired"
      | Non_static_id s -> s
    let of_string =
      function
      | "Running" -> Running
      | "Stopped" -> Stopped
      | "Impaired" -> Impaired
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration TelemetryEnrichmentStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"TelemetryEnrichmentStatus" j)
    let to_json = simple_to_json to_value
  end
module AwsResourceExplorerManagedViewArn =
  struct
    type nonrec t = string
    let context_ = "AwsResourceExplorerManagedViewArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws([a-z0-9\\-]+)?:resource-explorer-2:([a-z0-9\\-]+)?:([0-9]{12})?:managed-view/(.+)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"AwsResourceExplorerManagedViewArn" j
    let to_json = simple_to_json to_value
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module TelemetryRuleSummaries =
  struct
    type nonrec t = TelemetryRuleSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TelemetryRuleSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TelemetryRuleSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"TelemetryRuleSummaries"
        ~of_json:TelemetryRuleSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListTelemetryRulesMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ListTelemetryRulesMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AccountIdentifiers =
  struct
    type nonrec t = AccountIdentifier.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AccountIdentifier.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccountIdentifier.of_xml)
    let of_json j =
      list_of_json ~kind:"AccountIdentifiers"
        ~of_json:AccountIdentifier.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListTelemetryRulesForOrganizationMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListTelemetryRulesForOrganizationMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module OrganizationUnitIdentifiers =
  struct
    type nonrec t = OrganizationUnitIdentifier.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:OrganizationUnitIdentifier.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:OrganizationUnitIdentifier.of_xml)
    let of_json j =
      list_of_json ~kind:"OrganizationUnitIdentifiers"
        ~of_json:OrganizationUnitIdentifier.of_json j
    let to_json v = composed_to_json to_value v
  end
module TelemetryPipelineSummaries =
  struct
    type nonrec t = TelemetryPipelineSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TelemetryPipelineSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TelemetryPipelineSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"TelemetryPipelineSummaries"
        ~of_json:TelemetryPipelineSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListTelemetryPipelinesMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListTelemetryPipelinesMaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module IntegrationSummaries =
  struct
    type nonrec t = IntegrationSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:IntegrationSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:IntegrationSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"IntegrationSummaries"
        ~of_json:IntegrationSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListS3TableIntegrationsMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListS3TableIntegrationsMaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TelemetryConfigurations =
  struct
    type nonrec t = TelemetryConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TelemetryConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TelemetryConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"TelemetryConfigurations"
        ~of_json:TelemetryConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListResourceTelemetryMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ListResourceTelemetryMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ResourceIdentifierPrefix =
  struct
    type nonrec t = string
    let context_ = "ResourceIdentifierPrefix"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:768) >>=
             (fun () -> check_string_min i ~min:3));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceIdentifierPrefix" j
    let to_json = simple_to_json to_value
  end
module ResourceTypes =
  struct
    type nonrec t = ResourceType.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:9) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ResourceType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceType.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceTypes" ~of_json:ResourceType.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListResourceTelemetryForOrganizationMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListResourceTelemetryForOrganizationMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CentralizationRuleSummaries =
  struct
    type nonrec t = CentralizationRuleSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CentralizationRuleSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CentralizationRuleSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"CentralizationRuleSummaries"
        ~of_json:CentralizationRuleSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListCentralizationRulesForOrganizationInputRuleNamePrefixString =
  struct
    type nonrec t = string
    let context_ =
      "ListCentralizationRulesForOrganizationInputRuleNamePrefixString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json
        ~kind:"ListCentralizationRulesForOrganizationInputRuleNamePrefixString"
        j
    let to_json = simple_to_json to_value
  end
module ListCentralizationRulesForOrganizationMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListCentralizationRulesForOrganizationMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module IsReplicated =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module RegionStatuses =
  struct
    type nonrec t = RegionStatus.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:RegionStatus.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RegionStatus.of_xml)
    let of_json j =
      list_of_json ~kind:"RegionStatuses" ~of_json:RegionStatus.of_json j
    let to_json v = composed_to_json to_value v
  end
module TelemetryPipeline =
  struct
    type nonrec t =
      {
      createdTimeStamp: Long.t option
        [@ocaml.doc "The timestamp when the telemetry pipeline was created."];
      lastUpdateTimeStamp: Long.t option
        [@ocaml.doc
          "The timestamp when the telemetry pipeline was last updated."];
      arn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the telemetry pipeline."];
      name: TelemetryPipelineName.t option
        [@ocaml.doc "The name of the telemetry pipeline."];
      configuration: TelemetryPipelineConfiguration.t option
        [@ocaml.doc
          "The configuration that defines how the telemetry pipeline processes data."];
      status: TelemetryPipelineStatus.t option
        [@ocaml.doc "The current status of the telemetry pipeline."];
      statusReason: TelemetryPipelineStatusReason.t option
        [@ocaml.doc
          "Additional information about the pipeline status, including reasons for failure states."];
      tags: TagMapOutput.t option
        [@ocaml.doc
          "The key-value pairs associated with the telemetry pipeline resource."]}
    let make ?createdTimeStamp =
      fun ?lastUpdateTimeStamp ->
        fun ?arn ->
          fun ?name ->
            fun ?configuration ->
              fun ?status ->
                fun ?statusReason ->
                  fun ?tags ->
                    fun () ->
                      {
                        createdTimeStamp;
                        lastUpdateTimeStamp;
                        arn;
                        name;
                        configuration;
                        status;
                        statusReason;
                        tags
                      }
    let to_value x =
      structure_to_value
        [("CreatedTimeStamp",
           (Option.map x.createdTimeStamp ~f:Long.to_value));
        ("LastUpdateTimeStamp",
          (Option.map x.lastUpdateTimeStamp ~f:Long.to_value));
        ("Arn", (Option.map x.arn ~f:ResourceArn.to_value));
        ("Name", (Option.map x.name ~f:TelemetryPipelineName.to_value));
        ("Configuration",
          (Option.map x.configuration
             ~f:TelemetryPipelineConfiguration.to_value));
        ("Status", (Option.map x.status ~f:TelemetryPipelineStatus.to_value));
        ("StatusReason",
          (Option.map x.statusReason
             ~f:TelemetryPipelineStatusReason.to_value));
        ("Tags", (Option.map x.tags ~f:TagMapOutput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        (Option.map ~f:TagMapOutput.of_xml) (Xml.child xml_arg0 "Tags") in
      let statusReason =
        (Option.map ~f:TelemetryPipelineStatusReason.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:TelemetryPipelineStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let configuration =
        (Option.map ~f:TelemetryPipelineConfiguration.of_xml)
          (Xml.child xml_arg0 "Configuration") in
      let name =
        (Option.map ~f:TelemetryPipelineName.of_xml)
          (Xml.child xml_arg0 "Name") in
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "Arn") in
      let lastUpdateTimeStamp =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "LastUpdateTimeStamp") in
      let createdTimeStamp =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "CreatedTimeStamp") in
      make ?tags ?statusReason ?status ?configuration ?name ?arn
        ?lastUpdateTimeStamp ?createdTimeStamp ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMapOutput.of_json in
      let statusReason =
        field_map json__ "StatusReason" TelemetryPipelineStatusReason.of_json in
      let status = field_map json__ "Status" TelemetryPipelineStatus.of_json in
      let configuration =
        field_map json__ "Configuration"
          TelemetryPipelineConfiguration.of_json in
      let name = field_map json__ "Name" TelemetryPipelineName.of_json in
      let arn = field_map json__ "Arn" ResourceArn.of_json in
      let lastUpdateTimeStamp =
        field_map json__ "LastUpdateTimeStamp" Long.of_json in
      let createdTimeStamp = field_map json__ "CreatedTimeStamp" Long.of_json in
      make ?tags ?statusReason ?status ?configuration ?name ?arn
        ?lastUpdateTimeStamp ?createdTimeStamp ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a complete telemetry pipeline resource with configuration, status, and metadata for data processing and transformation."]
module FailureReason =
  struct
    type nonrec t = string
    let context_ = "FailureReason"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FailureReason" j
    let to_json = simple_to_json to_value
  end
module Status =
  struct
    type nonrec t =
      | NOT_STARTED 
      | STARTING 
      | FAILED_START 
      | RUNNING 
      | STOPPING 
      | FAILED_STOP 
      | STOPPED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NOT_STARTED -> "NOT_STARTED"
      | STARTING -> "STARTING"
      | FAILED_START -> "FAILED_START"
      | RUNNING -> "RUNNING"
      | STOPPING -> "STOPPING"
      | FAILED_STOP -> "FAILED_STOP"
      | STOPPED -> "STOPPED"
      | Non_static_id s -> s
    let of_string =
      function
      | "NOT_STARTED" -> NOT_STARTED
      | "STARTING" -> STARTING
      | "FAILED_START" -> FAILED_START
      | "RUNNING" -> RUNNING
      | "STOPPING" -> STOPPING
      | "FAILED_STOP" -> FAILED_STOP
      | "STOPPED" -> STOPPED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Status" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Status" j)
    let to_json = simple_to_json to_value
  end
module Encryption =
  struct
    type nonrec t =
      {
      sseAlgorithm: SSEAlgorithm.t
        [@ocaml.doc
          "The server-side encryption algorithm used for encrypting data in the S3 Table integration."];
      kmsKeyArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the KMS key used for encryption when using customer-managed keys."]}
    let context_ = "Encryption"
    let make ?kmsKeyArn =
      fun ~sseAlgorithm -> fun () -> { kmsKeyArn; sseAlgorithm }
    let to_value x =
      structure_to_value
        [("SseAlgorithm", (Some (SSEAlgorithm.to_value x.sseAlgorithm)));
        ("KmsKeyArn", (Option.map x.kmsKeyArn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKeyArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "KmsKeyArn") in
      let sseAlgorithm =
        SSEAlgorithm.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SseAlgorithm") in
      make ?kmsKeyArn ~sseAlgorithm ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let kmsKeyArn = field_map json__ "KmsKeyArn" ResourceArn.of_json in
      let sseAlgorithm =
        field_map_exn json__ "SseAlgorithm" SSEAlgorithm.of_json in
      make ?kmsKeyArn ~sseAlgorithm ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines the encryption configuration for S3 Table integrations, including the encryption algorithm and KMS key settings."]
module ValidateTelemetryPipelineConfigurationOutput =
  struct
    type nonrec t =
      {
      errors: ValidationErrors.t option
        [@ocaml.doc
          "A list of validation errors found in the pipeline configuration, if any."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?errors = fun () -> { errors }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Errors", (Option.map x.errors ~f:ValidationErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errors =
        (Option.map ~f:ValidationErrors.of_xml) (Xml.child xml_arg0 "Errors") in
      make ?errors ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let errors = field_map json__ "Errors" ValidationErrors.of_json in
      make ?errors ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Validates a pipeline configuration without creating the pipeline. This operation checks the configuration for syntax errors and compatibility issues."]
module ValidateTelemetryPipelineConfigurationInput =
  struct
    type nonrec t =
      {
      configuration: TelemetryPipelineConfiguration.t
        [@ocaml.doc
          "The pipeline configuration to validate for syntax and compatibility."]}
    let context_ = "ValidateTelemetryPipelineConfigurationInput"
    let make ~configuration = fun () -> { configuration }
    let to_value x =
      structure_to_value
        [("Configuration",
           (Some (TelemetryPipelineConfiguration.to_value x.configuration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuration =
        TelemetryPipelineConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Configuration") in
      make ~configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let configuration =
        field_map_exn json__ "Configuration"
          TelemetryPipelineConfiguration.of_json in
      make ~configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Validates a pipeline configuration without creating the pipeline. This operation checks the configuration for syntax errors and compatibility issues."]
module UpdateTelemetryRuleOutput =
  struct
    type nonrec t =
      {
      ruleArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the updated telemetry rule."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ruleArn = fun () -> { ruleArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RuleArn", (Option.map x.ruleArn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RuleArn") in
      make ?ruleArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleArn = field_map json__ "RuleArn" ResourceArn.of_json in
      make ?ruleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing telemetry rule in your account. If multiple users attempt to modify the same telemetry rule simultaneously, a ConflictException is returned to provide specific error information for concurrent modification scenarios."]
module UpdateTelemetryRuleInput =
  struct
    type nonrec t =
      {
      ruleIdentifier: RuleIdentifier.t
        [@ocaml.doc
          "The identifier (name or ARN) of the telemetry rule to update."];
      rule: TelemetryRule.t
        [@ocaml.doc "The new configuration details for the telemetry rule."]}
    let context_ = "UpdateTelemetryRuleInput"
    let make ~ruleIdentifier =
      fun ~rule -> fun () -> { ruleIdentifier; rule }
    let to_value x =
      structure_to_value
        [("RuleIdentifier",
           (Some (RuleIdentifier.to_value x.ruleIdentifier)));
        ("Rule", (Some (TelemetryRule.to_value x.rule)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rule =
        TelemetryRule.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Rule") in
      let ruleIdentifier =
        RuleIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleIdentifier") in
      make ~rule ~ruleIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let rule = field_map_exn json__ "Rule" TelemetryRule.of_json in
      let ruleIdentifier =
        field_map_exn json__ "RuleIdentifier" RuleIdentifier.of_json in
      make ~rule ~ruleIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing telemetry rule in your account. If multiple users attempt to modify the same telemetry rule simultaneously, a ConflictException is returned to provide specific error information for concurrent modification scenarios."]
module UpdateTelemetryRuleForOrganizationOutput =
  struct
    type nonrec t =
      {
      ruleArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the updated organization telemetry rule."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ruleArn = fun () -> { ruleArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RuleArn", (Option.map x.ruleArn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RuleArn") in
      make ?ruleArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleArn = field_map json__ "RuleArn" ResourceArn.of_json in
      make ?ruleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing telemetry rule that applies across an Amazon Web Services Organization. This operation can only be called by the organization's management account or a delegated administrator account."]
module UpdateTelemetryRuleForOrganizationInput =
  struct
    type nonrec t =
      {
      ruleIdentifier: RuleIdentifier.t
        [@ocaml.doc
          "The identifier (name or ARN) of the organization telemetry rule to update."];
      rule: TelemetryRule.t
        [@ocaml.doc
          "The new configuration details for the organization telemetry rule, including resource type, telemetry type, and destination configuration."]}
    let context_ = "UpdateTelemetryRuleForOrganizationInput"
    let make ~ruleIdentifier =
      fun ~rule -> fun () -> { ruleIdentifier; rule }
    let to_value x =
      structure_to_value
        [("RuleIdentifier",
           (Some (RuleIdentifier.to_value x.ruleIdentifier)));
        ("Rule", (Some (TelemetryRule.to_value x.rule)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rule =
        TelemetryRule.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Rule") in
      let ruleIdentifier =
        RuleIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleIdentifier") in
      make ~rule ~ruleIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let rule = field_map_exn json__ "Rule" TelemetryRule.of_json in
      let ruleIdentifier =
        field_map_exn json__ "RuleIdentifier" RuleIdentifier.of_json in
      make ~rule ~ruleIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing telemetry rule that applies across an Amazon Web Services Organization. This operation can only be called by the organization's management account or a delegated administrator account."]
module UpdateTelemetryPipelineOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the configuration of an existing telemetry pipeline. The following attributes cannot be updated after pipeline creation: Pipeline name - The pipeline name is immutable Pipeline ARN - The ARN is automatically generated and cannot be changed Source type - Once a pipeline is created with a specific source type (such as S3, CloudWatch Logs, GitHub, or third-party sources), it cannot be changed to a different source type Processors can be added, removed, or modified. However, some processors are not supported for third-party pipelines and cannot be added through updates. Source-Specific Update Rules CloudWatch Logs Sources (Vended and Custom) Updatable: sts_role_arn Fixed: data_source_name, data_source_type, sink (must remain \\@original) S3 Sources (Crowdstrike, Zscaler, SentinelOne, Custom) Updatable: All SQS configuration parameters, sts_role_arn, codec settings, compression type, bucket ownership settings, sink log group Fixed: notification_type, aws.region GitHub Audit Logs Updatable: All Amazon Web Services Secrets Manager attributes, scope (can switch between ORGANIZATION/ENTERPRISE), organization or enterprise name, range, authentication credentials (PAT or GitHub App) Microsoft Sources (Entra ID, Office365, Windows) Updatable: All Amazon Web Services Secrets Manager attributes, tenant_id, workspace_id (Windows only), OAuth2 credentials (client_id, client_secret) Okta Sources (SSO, Auth0) Updatable: All Amazon Web Services Secrets Manager attributes, domain, range, OAuth2 credentials (client_id, client_secret) Palo Alto Networks Updatable: All Amazon Web Services Secrets Manager attributes, hostname, basic authentication credentials (username, password) ServiceNow CMDB Updatable: All Amazon Web Services Secrets Manager attributes, instance_url, range, OAuth2 credentials (client_id, client_secret) Wiz CNAPP Updatable: All Amazon Web Services Secrets Manager attributes, region, range, OAuth2 credentials (client_id, client_secret)"]
module UpdateTelemetryPipelineInput =
  struct
    type nonrec t =
      {
      pipelineIdentifier: TelemetryPipelineIdentifier.t
        [@ocaml.doc "The ARN of the telemetry pipeline to update."];
      configuration: TelemetryPipelineConfiguration.t
        [@ocaml.doc
          "The new configuration for the telemetry pipeline, including updated sources, processors, and destinations."]}
    let context_ = "UpdateTelemetryPipelineInput"
    let make ~pipelineIdentifier =
      fun ~configuration -> fun () -> { pipelineIdentifier; configuration }
    let to_value x =
      structure_to_value
        [("PipelineIdentifier",
           (Some (TelemetryPipelineIdentifier.to_value x.pipelineIdentifier)));
        ("Configuration",
          (Some (TelemetryPipelineConfiguration.to_value x.configuration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuration =
        TelemetryPipelineConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Configuration") in
      let pipelineIdentifier =
        TelemetryPipelineIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PipelineIdentifier") in
      make ~configuration ~pipelineIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let configuration =
        field_map_exn json__ "Configuration"
          TelemetryPipelineConfiguration.of_json in
      let pipelineIdentifier =
        field_map_exn json__ "PipelineIdentifier"
          TelemetryPipelineIdentifier.of_json in
      make ~configuration ~pipelineIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the configuration of an existing telemetry pipeline. The following attributes cannot be updated after pipeline creation: Pipeline name - The pipeline name is immutable Pipeline ARN - The ARN is automatically generated and cannot be changed Source type - Once a pipeline is created with a specific source type (such as S3, CloudWatch Logs, GitHub, or third-party sources), it cannot be changed to a different source type Processors can be added, removed, or modified. However, some processors are not supported for third-party pipelines and cannot be added through updates. Source-Specific Update Rules CloudWatch Logs Sources (Vended and Custom) Updatable: sts_role_arn Fixed: data_source_name, data_source_type, sink (must remain \\@original) S3 Sources (Crowdstrike, Zscaler, SentinelOne, Custom) Updatable: All SQS configuration parameters, sts_role_arn, codec settings, compression type, bucket ownership settings, sink log group Fixed: notification_type, aws.region GitHub Audit Logs Updatable: All Amazon Web Services Secrets Manager attributes, scope (can switch between ORGANIZATION/ENTERPRISE), organization or enterprise name, range, authentication credentials (PAT or GitHub App) Microsoft Sources (Entra ID, Office365, Windows) Updatable: All Amazon Web Services Secrets Manager attributes, tenant_id, workspace_id (Windows only), OAuth2 credentials (client_id, client_secret) Okta Sources (SSO, Auth0) Updatable: All Amazon Web Services Secrets Manager attributes, domain, range, OAuth2 credentials (client_id, client_secret) Palo Alto Networks Updatable: All Amazon Web Services Secrets Manager attributes, hostname, basic authentication credentials (username, password) ServiceNow CMDB Updatable: All Amazon Web Services Secrets Manager attributes, instance_url, range, OAuth2 credentials (client_id, client_secret) Wiz CNAPP Updatable: All Amazon Web Services Secrets Manager attributes, region, range, OAuth2 credentials (client_id, client_secret)"]
module UpdateCentralizationRuleForOrganizationOutput =
  struct
    type nonrec t =
      {
      ruleArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the updated organization centralization rule."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ruleArn = fun () -> { ruleArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RuleArn", (Option.map x.ruleArn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RuleArn") in
      make ?ruleArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleArn = field_map json__ "RuleArn" ResourceArn.of_json in
      make ?ruleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing centralization rule that applies across an Amazon Web Services Organization. This operation can only be called by the organization's management account or a delegated administrator account."]
module UpdateCentralizationRuleForOrganizationInput =
  struct
    type nonrec t =
      {
      ruleIdentifier: RuleIdentifier.t
        [@ocaml.doc
          "The identifier (name or ARN) of the organization centralization rule to update."];
      rule: CentralizationRule.t
        [@ocaml.doc
          "The configuration details for the organization-wide centralization rule, including the source configuration and the destination configuration to centralize telemetry data across the organization."]}
    let context_ = "UpdateCentralizationRuleForOrganizationInput"
    let make ~ruleIdentifier =
      fun ~rule -> fun () -> { ruleIdentifier; rule }
    let to_value x =
      structure_to_value
        [("RuleIdentifier",
           (Some (RuleIdentifier.to_value x.ruleIdentifier)));
        ("Rule", (Some (CentralizationRule.to_value x.rule)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rule =
        CentralizationRule.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Rule") in
      let ruleIdentifier =
        RuleIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleIdentifier") in
      make ~rule ~ruleIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let rule = field_map_exn json__ "Rule" CentralizationRule.of_json in
      let ruleIdentifier =
        field_map_exn json__ "RuleIdentifier" RuleIdentifier.of_json in
      make ~rule ~ruleIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing centralization rule that applies across an Amazon Web Services Organization. This operation can only be called by the organization's management account or a delegated administrator account."]
module UntagResourceInput =
  struct
    type nonrec t =
      {
      resourceARN: ResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the telemetry rule resource to remove tags from."];
      tagKeys: TagKeyList.t
        [@ocaml.doc
          "The list of tag keys to remove from the telemetry rule resource."]}
    let context_ = "UntagResourceInput"
    let make ~resourceARN =
      fun ~tagKeys -> fun () -> { resourceARN; tagKeys }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (ResourceArn.to_value x.resourceARN)));
        ("TagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TagKeys") in
      let resourceARN =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~tagKeys ~resourceARN ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map_exn json__ "TagKeys" TagKeyList.of_json in
      let resourceARN =
        field_map_exn json__ "ResourceARN" ResourceArn.of_json in
      make ~tagKeys ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes tags from a resource. Supports telemetry rule resources and telemetry pipeline resources."]
module TestTelemetryPipelineOutput =
  struct
    type nonrec t =
      {
      results: PipelineOutputs.t option
        [@ocaml.doc
          "The results of processing the test records through the pipeline configuration, including any outputs or errors."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?results = fun () -> { results }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Results", (Option.map x.results ~f:PipelineOutputs.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let results =
        (Option.map ~f:PipelineOutputs.of_xml) (Xml.child xml_arg0 "Results") in
      make ?results ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let results = field_map json__ "Results" PipelineOutputs.of_json in
      make ?results ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Tests a pipeline configuration with sample records to validate data processing before deployment. This operation helps ensure your pipeline configuration works as expected."]
module TestTelemetryPipelineInput =
  struct
    type nonrec t =
      {
      records: Records.t
        [@ocaml.doc
          "The sample records to process through the pipeline configuration for testing purposes."];
      configuration: TelemetryPipelineConfiguration.t
        [@ocaml.doc
          "The pipeline configuration to test with the provided sample records."]}
    let context_ = "TestTelemetryPipelineInput"
    let make ~records =
      fun ~configuration -> fun () -> { records; configuration }
    let to_value x =
      structure_to_value
        [("Records", (Some (Records.to_value x.records)));
        ("Configuration",
          (Some (TelemetryPipelineConfiguration.to_value x.configuration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuration =
        TelemetryPipelineConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Configuration") in
      let records =
        Records.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Records") in
      make ~configuration ~records ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let configuration =
        field_map_exn json__ "Configuration"
          TelemetryPipelineConfiguration.of_json in
      let records = field_map_exn json__ "Records" Records.of_json in
      make ~configuration ~records ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Tests a pipeline configuration with sample records to validate data processing before deployment. This operation helps ensure your pipeline configuration works as expected."]
module TagResourceInput =
  struct
    type nonrec t =
      {
      resourceARN: ResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the telemetry rule resource to tag."];
      tags: TagMapInput.t
        [@ocaml.doc
          "The key-value pairs to add or update for the telemetry rule resource."]}
    let context_ = "TagResourceInput"
    let make ~resourceARN = fun ~tags -> fun () -> { resourceARN; tags }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (ResourceArn.to_value x.resourceARN)));
        ("Tags", (Some (TagMapInput.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagMapInput.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resourceARN =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~tags ~resourceARN ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "Tags" TagMapInput.of_json in
      let resourceARN =
        field_map_exn json__ "ResourceARN" ResourceArn.of_json in
      make ~tags ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds or updates tags for a resource. Supports telemetry rule resources and telemetry pipeline resources."]
module StopTelemetryEnrichmentOutput =
  struct
    type nonrec t =
      {
      status: TelemetryEnrichmentStatus.t option
        [@ocaml.doc
          "The status of the resource tags for telemetry feature after the stop operation (Running, Stopped, or Impaired)."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Status",
           (Option.map x.status ~f:TelemetryEnrichmentStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:TelemetryEnrichmentStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      make ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status =
        field_map json__ "Status" TelemetryEnrichmentStatus.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Disables the resource tags for telemetry feature for your account, stopping the enhancement of telemetry data with additional resource metadata."]
module StartTelemetryEvaluationInput =
  struct
    type nonrec t =
      {
      regions: Regions.t option
        [@ocaml.doc
          "An optional list of Amazon Web Services Regions to include in multi-region telemetry evaluation. The current region is always implicitly included and must not be specified in this list. When provided, telemetry evaluation starts in the current region and propagates to all specified regions. Mutually exclusive with AllRegions. If neither Regions nor AllRegions is provided, the operation applies only to the current region."];
      allRegions: AllRegions.t option
        [@ocaml.doc
          "If set to true, telemetry evaluation starts in all Amazon Web Services Regions where Amazon CloudWatch Observability Admin is available in the current partition. The current region becomes the home region for managing multi-region evaluation. When new regions become available, evaluation automatically expands to include them. Mutually exclusive with Regions."]}
    let make ?regions = fun ?allRegions -> fun () -> { regions; allRegions }
    let to_value x =
      structure_to_value
        [("Regions", (Option.map x.regions ~f:Regions.to_value));
        ("AllRegions", (Option.map x.allRegions ~f:AllRegions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let allRegions =
        (Option.map ~f:AllRegions.of_xml) (Xml.child xml_arg0 "AllRegions") in
      let regions =
        (Option.map ~f:Regions.of_xml) (Xml.child xml_arg0 "Regions") in
      make ?allRegions ?regions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let allRegions = field_map json__ "AllRegions" AllRegions.of_json in
      let regions = field_map json__ "Regions" Regions.of_json in
      make ?allRegions ?regions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action begins onboarding the caller Amazon Web Services account to the telemetry config feature."]
module StartTelemetryEvaluationForOrganizationInput =
  struct
    type nonrec t =
      {
      regions: Regions.t option
        [@ocaml.doc
          "An optional list of Amazon Web Services Regions to include in multi-region telemetry evaluation for the organization. The current region is always implicitly included and must not be specified in this list. When provided, telemetry evaluation starts in the current region and propagates to all specified regions for the organization. Mutually exclusive with AllRegions. If neither Regions nor AllRegions is provided, the operation applies only to the current region."];
      allRegions: AllRegions.t option
        [@ocaml.doc
          "If set to true, telemetry evaluation for the organization starts in all Amazon Web Services Regions where Amazon CloudWatch Observability Admin is available in the current partition. The current region becomes the home region for managing multi-region evaluation for the organization. When new regions become available, evaluation automatically expands to include them. Mutually exclusive with Regions."]}
    let make ?regions = fun ?allRegions -> fun () -> { regions; allRegions }
    let to_value x =
      structure_to_value
        [("Regions", (Option.map x.regions ~f:Regions.to_value));
        ("AllRegions", (Option.map x.allRegions ~f:AllRegions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let allRegions =
        (Option.map ~f:AllRegions.of_xml) (Xml.child xml_arg0 "AllRegions") in
      let regions =
        (Option.map ~f:Regions.of_xml) (Xml.child xml_arg0 "Regions") in
      make ?allRegions ?regions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let allRegions = field_map json__ "AllRegions" AllRegions.of_json in
      let regions = field_map json__ "Regions" Regions.of_json in
      make ?allRegions ?regions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This actions begins onboarding the organization and all member accounts to the telemetry config feature."]
module StartTelemetryEnrichmentOutput =
  struct
    type nonrec t =
      {
      status: TelemetryEnrichmentStatus.t option
        [@ocaml.doc
          "The status of the resource tags for telemetry feature after the start operation (Running, Stopped, or Impaired)."];
      awsResourceExplorerManagedViewArn:
        AwsResourceExplorerManagedViewArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Resource Explorer managed view created for resource tags for telemetry."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?status =
      fun ?awsResourceExplorerManagedViewArn ->
        fun () -> { status; awsResourceExplorerManagedViewArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Status",
           (Option.map x.status ~f:TelemetryEnrichmentStatus.to_value));
        ("AwsResourceExplorerManagedViewArn",
          (Option.map x.awsResourceExplorerManagedViewArn
             ~f:AwsResourceExplorerManagedViewArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let awsResourceExplorerManagedViewArn =
        (Option.map ~f:AwsResourceExplorerManagedViewArn.of_xml)
          (Xml.child xml_arg0 "AwsResourceExplorerManagedViewArn") in
      let status =
        (Option.map ~f:TelemetryEnrichmentStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      make ?awsResourceExplorerManagedViewArn ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let awsResourceExplorerManagedViewArn =
        field_map json__ "AwsResourceExplorerManagedViewArn"
          AwsResourceExplorerManagedViewArn.of_json in
      let status =
        field_map json__ "Status" TelemetryEnrichmentStatus.of_json in
      make ?awsResourceExplorerManagedViewArn ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Enables the resource tags for telemetry feature for your account, which enhances telemetry data with additional resource metadata from Resource Explorer to provide richer context for monitoring and observability."]
module ListTelemetryRulesOutput =
  struct
    type nonrec t =
      {
      telemetryRuleSummaries: TelemetryRuleSummaries.t option
        [@ocaml.doc "A list of telemetry rule summaries."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token to resume pagination of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?telemetryRuleSummaries =
      fun ?nextToken -> fun () -> { telemetryRuleSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TelemetryRuleSummaries",
           (Option.map x.telemetryRuleSummaries
              ~f:TelemetryRuleSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let telemetryRuleSummaries =
        (Option.map ~f:TelemetryRuleSummaries.of_xml)
          (Xml.child xml_arg0 "TelemetryRuleSummaries") in
      make ?nextToken ?telemetryRuleSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let telemetryRuleSummaries =
        field_map json__ "TelemetryRuleSummaries"
          TelemetryRuleSummaries.of_json in
      make ?nextToken ?telemetryRuleSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all telemetry rules in your account. You can filter the results by specifying a rule name prefix."]
module ListTelemetryRulesInput =
  struct
    type nonrec t =
      {
      ruleNamePrefix: String_.t option
        [@ocaml.doc
          "A string to filter telemetry rules whose names begin with the specified prefix."];
      maxResults: ListTelemetryRulesMaxResults.t option
        [@ocaml.doc
          "The maximum number of telemetry rules to return in a single call."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token for the next set of results. A previous call generates this token."]}
    let make ?ruleNamePrefix =
      fun ?maxResults ->
        fun ?nextToken -> fun () -> { ruleNamePrefix; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("RuleNamePrefix",
           (Option.map x.ruleNamePrefix ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:ListTelemetryRulesMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:ListTelemetryRulesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let ruleNamePrefix =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RuleNamePrefix") in
      make ?nextToken ?maxResults ?ruleNamePrefix ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults" ListTelemetryRulesMaxResults.of_json in
      let ruleNamePrefix = field_map json__ "RuleNamePrefix" String_.of_json in
      make ?nextToken ?maxResults ?ruleNamePrefix ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all telemetry rules in your account. You can filter the results by specifying a rule name prefix."]
module ListTelemetryRulesForOrganizationOutput =
  struct
    type nonrec t =
      {
      telemetryRuleSummaries: TelemetryRuleSummaries.t option
        [@ocaml.doc "A list of organization telemetry rule summaries."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token to resume pagination of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?telemetryRuleSummaries =
      fun ?nextToken -> fun () -> { telemetryRuleSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TelemetryRuleSummaries",
           (Option.map x.telemetryRuleSummaries
              ~f:TelemetryRuleSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let telemetryRuleSummaries =
        (Option.map ~f:TelemetryRuleSummaries.of_xml)
          (Xml.child xml_arg0 "TelemetryRuleSummaries") in
      make ?nextToken ?telemetryRuleSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let telemetryRuleSummaries =
        field_map json__ "TelemetryRuleSummaries"
          TelemetryRuleSummaries.of_json in
      make ?nextToken ?telemetryRuleSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all telemetry rules in your organization. This operation can only be called by the organization's management account or a delegated administrator account."]
module ListTelemetryRulesForOrganizationInput =
  struct
    type nonrec t =
      {
      ruleNamePrefix: String_.t option
        [@ocaml.doc
          "A string to filter organization telemetry rules whose names begin with the specified prefix."];
      sourceAccountIds: AccountIdentifiers.t option
        [@ocaml.doc
          "The list of account IDs to filter organization telemetry rules by their source accounts."];
      sourceOrganizationUnitIds: OrganizationUnitIdentifiers.t option
        [@ocaml.doc
          "The list of organizational unit IDs to filter organization telemetry rules by their source organizational units."];
      maxResults: ListTelemetryRulesForOrganizationMaxResults.t option
        [@ocaml.doc
          "The maximum number of organization telemetry rules to return in a single call."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token for the next set of results. A previous call generates this token."]}
    let make ?ruleNamePrefix =
      fun ?sourceAccountIds ->
        fun ?sourceOrganizationUnitIds ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                {
                  ruleNamePrefix;
                  sourceAccountIds;
                  sourceOrganizationUnitIds;
                  maxResults;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("RuleNamePrefix",
           (Option.map x.ruleNamePrefix ~f:String_.to_value));
        ("SourceAccountIds",
          (Option.map x.sourceAccountIds ~f:AccountIdentifiers.to_value));
        ("SourceOrganizationUnitIds",
          (Option.map x.sourceOrganizationUnitIds
             ~f:OrganizationUnitIdentifiers.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:ListTelemetryRulesForOrganizationMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:ListTelemetryRulesForOrganizationMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let sourceOrganizationUnitIds =
        (Option.map ~f:OrganizationUnitIdentifiers.of_xml)
          (Xml.child xml_arg0 "SourceOrganizationUnitIds") in
      let sourceAccountIds =
        (Option.map ~f:AccountIdentifiers.of_xml)
          (Xml.child xml_arg0 "SourceAccountIds") in
      let ruleNamePrefix =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RuleNamePrefix") in
      make ?nextToken ?maxResults ?sourceOrganizationUnitIds
        ?sourceAccountIds ?ruleNamePrefix ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          ListTelemetryRulesForOrganizationMaxResults.of_json in
      let sourceOrganizationUnitIds =
        field_map json__ "SourceOrganizationUnitIds"
          OrganizationUnitIdentifiers.of_json in
      let sourceAccountIds =
        field_map json__ "SourceAccountIds" AccountIdentifiers.of_json in
      let ruleNamePrefix = field_map json__ "RuleNamePrefix" String_.of_json in
      make ?nextToken ?maxResults ?sourceOrganizationUnitIds
        ?sourceAccountIds ?ruleNamePrefix ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all telemetry rules in your organization. This operation can only be called by the organization's management account or a delegated administrator account."]
module ListTelemetryPipelinesOutput =
  struct
    type nonrec t =
      {
      pipelineSummaries: TelemetryPipelineSummaries.t option
        [@ocaml.doc
          "A list of telemetry pipeline summaries containing key information about each pipeline."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token to resume pagination of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?pipelineSummaries =
      fun ?nextToken -> fun () -> { pipelineSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PipelineSummaries",
           (Option.map x.pipelineSummaries
              ~f:TelemetryPipelineSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let pipelineSummaries =
        (Option.map ~f:TelemetryPipelineSummaries.of_xml)
          (Xml.child xml_arg0 "PipelineSummaries") in
      make ?nextToken ?pipelineSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let pipelineSummaries =
        field_map json__ "PipelineSummaries"
          TelemetryPipelineSummaries.of_json in
      make ?nextToken ?pipelineSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of telemetry pipelines in your account. Returns up to 100 results. If more than 100 telemetry pipelines exist, include the NextToken value from the response to retrieve the next set of results."]
module ListTelemetryPipelinesInput =
  struct
    type nonrec t =
      {
      maxResults: ListTelemetryPipelinesMaxResults.t option
        [@ocaml.doc
          "The maximum number of telemetry pipelines to return in a single call."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token for the next set of results. A previous call generates this token."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("MaxResults",
           (Option.map x.maxResults
              ~f:ListTelemetryPipelinesMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:ListTelemetryPipelinesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          ListTelemetryPipelinesMaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of telemetry pipelines in your account. Returns up to 100 results. If more than 100 telemetry pipelines exist, include the NextToken value from the response to retrieve the next set of results."]
module ListTagsForResourceOutput =
  struct
    type nonrec t =
      {
      tags: TagMapOutput.t option
        [@ocaml.doc
          "The list of tags associated with the telemetry rule resource."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Tags", (Option.map x.tags ~f:TagMapOutput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        (Option.map ~f:TagMapOutput.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMapOutput.of_json in
      make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all tags attached to the specified resource. Supports telemetry rule resources and telemetry pipeline resources."]
module ListTagsForResourceInput =
  struct
    type nonrec t =
      {
      resourceARN: ResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the telemetry rule resource whose tags you want to list."]}
    let context_ = "ListTagsForResourceInput"
    let make ~resourceARN = fun () -> { resourceARN }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (ResourceArn.to_value x.resourceARN)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceARN =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~resourceARN ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceARN =
        field_map_exn json__ "ResourceARN" ResourceArn.of_json in
      make ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all tags attached to the specified resource. Supports telemetry rule resources and telemetry pipeline resources."]
module ListS3TableIntegrationsOutput =
  struct
    type nonrec t =
      {
      integrationSummaries: IntegrationSummaries.t option
        [@ocaml.doc
          "A list of S3 Table integration summaries containing key information about each integration."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token to resume pagination of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?integrationSummaries =
      fun ?nextToken -> fun () -> { integrationSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("IntegrationSummaries",
           (Option.map x.integrationSummaries
              ~f:IntegrationSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let integrationSummaries =
        (Option.map ~f:IntegrationSummaries.of_xml)
          (Xml.child xml_arg0 "IntegrationSummaries") in
      make ?nextToken ?integrationSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let integrationSummaries =
        field_map json__ "IntegrationSummaries" IntegrationSummaries.of_json in
      make ?nextToken ?integrationSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all S3 Table integrations in your account. We recommend using pagination to ensure that the operation returns quickly and successfully."]
module ListS3TableIntegrationsInput =
  struct
    type nonrec t =
      {
      maxResults: ListS3TableIntegrationsMaxResults.t option
        [@ocaml.doc
          "The maximum number of S3 Table integrations to return in a single call."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token for the next set of results. A previous call generates this token."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("MaxResults",
           (Option.map x.maxResults
              ~f:ListS3TableIntegrationsMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:ListS3TableIntegrationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          ListS3TableIntegrationsMaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all S3 Table integrations in your account. We recommend using pagination to ensure that the operation returns quickly and successfully."]
module ListResourceTelemetryOutput =
  struct
    type nonrec t =
      {
      telemetryConfigurations: TelemetryConfigurations.t option
        [@ocaml.doc
          "A list of telemetry configurations for Amazon Web Services resources supported by telemetry config in the caller's account."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token for the next set of items to return. A previous call generates this token."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?telemetryConfigurations =
      fun ?nextToken -> fun () -> { telemetryConfigurations; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TelemetryConfigurations",
           (Option.map x.telemetryConfigurations
              ~f:TelemetryConfigurations.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let telemetryConfigurations =
        (Option.map ~f:TelemetryConfigurations.of_xml)
          (Xml.child xml_arg0 "TelemetryConfigurations") in
      make ?nextToken ?telemetryConfigurations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let telemetryConfigurations =
        field_map json__ "TelemetryConfigurations"
          TelemetryConfigurations.of_json in
      make ?nextToken ?telemetryConfigurations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of telemetry configurations for Amazon Web Services resources supported by telemetry config. For more information, see Auditing CloudWatch telemetry configurations."]
module ListResourceTelemetryInput =
  struct
    type nonrec t =
      {
      resourceIdentifierPrefix: ResourceIdentifierPrefix.t option
        [@ocaml.doc
          "A string used to filter resources which have a ResourceIdentifier starting with the ResourceIdentifierPrefix."];
      resourceTypes: ResourceTypes.t option
        [@ocaml.doc
          "A list of resource types used to filter resources supported by telemetry config. If this parameter is provided, the resources will be returned in the same order used in the request."];
      telemetryConfigurationState: TelemetryConfigurationState.t option
        [@ocaml.doc
          "A key-value pair to filter resources based on the telemetry type and the state of the telemetry configuration. The key is the telemetry type and the value is the state."];
      resourceTags: TagMapInput.t option
        [@ocaml.doc
          "A key-value pair to filter resources based on tags associated with the resource. For more information about tags, see What are tags?"];
      maxResults: ListResourceTelemetryMaxResults.t option
        [@ocaml.doc
          "A number field used to limit the number of results within the returned list."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token for the next set of items to return. A previous call generates this token."]}
    let make ?resourceIdentifierPrefix =
      fun ?resourceTypes ->
        fun ?telemetryConfigurationState ->
          fun ?resourceTags ->
            fun ?maxResults ->
              fun ?nextToken ->
                fun () ->
                  {
                    resourceIdentifierPrefix;
                    resourceTypes;
                    telemetryConfigurationState;
                    resourceTags;
                    maxResults;
                    nextToken
                  }
    let to_value x =
      structure_to_value
        [("ResourceIdentifierPrefix",
           (Option.map x.resourceIdentifierPrefix
              ~f:ResourceIdentifierPrefix.to_value));
        ("ResourceTypes",
          (Option.map x.resourceTypes ~f:ResourceTypes.to_value));
        ("TelemetryConfigurationState",
          (Option.map x.telemetryConfigurationState
             ~f:TelemetryConfigurationState.to_value));
        ("ResourceTags", (Option.map x.resourceTags ~f:TagMapInput.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:ListResourceTelemetryMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:ListResourceTelemetryMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let resourceTags =
        (Option.map ~f:TagMapInput.of_xml)
          (Xml.child xml_arg0 "ResourceTags") in
      let telemetryConfigurationState =
        (Option.map ~f:TelemetryConfigurationState.of_xml)
          (Xml.child xml_arg0 "TelemetryConfigurationState") in
      let resourceTypes =
        (Option.map ~f:ResourceTypes.of_xml)
          (Xml.child xml_arg0 "ResourceTypes") in
      let resourceIdentifierPrefix =
        (Option.map ~f:ResourceIdentifierPrefix.of_xml)
          (Xml.child xml_arg0 "ResourceIdentifierPrefix") in
      make ?nextToken ?maxResults ?resourceTags ?telemetryConfigurationState
        ?resourceTypes ?resourceIdentifierPrefix ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults" ListResourceTelemetryMaxResults.of_json in
      let resourceTags = field_map json__ "ResourceTags" TagMapInput.of_json in
      let telemetryConfigurationState =
        field_map json__ "TelemetryConfigurationState"
          TelemetryConfigurationState.of_json in
      let resourceTypes =
        field_map json__ "ResourceTypes" ResourceTypes.of_json in
      let resourceIdentifierPrefix =
        field_map json__ "ResourceIdentifierPrefix"
          ResourceIdentifierPrefix.of_json in
      make ?nextToken ?maxResults ?resourceTags ?telemetryConfigurationState
        ?resourceTypes ?resourceIdentifierPrefix ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of telemetry configurations for Amazon Web Services resources supported by telemetry config. For more information, see Auditing CloudWatch telemetry configurations."]
module ListResourceTelemetryForOrganizationOutput =
  struct
    type nonrec t =
      {
      telemetryConfigurations: TelemetryConfigurations.t option
        [@ocaml.doc
          "A list of telemetry configurations for Amazon Web Services resources supported by telemetry config in the organization."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token for the next set of items to return. A previous call provides this token."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?telemetryConfigurations =
      fun ?nextToken -> fun () -> { telemetryConfigurations; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TelemetryConfigurations",
           (Option.map x.telemetryConfigurations
              ~f:TelemetryConfigurations.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let telemetryConfigurations =
        (Option.map ~f:TelemetryConfigurations.of_xml)
          (Xml.child xml_arg0 "TelemetryConfigurations") in
      make ?nextToken ?telemetryConfigurations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let telemetryConfigurations =
        field_map json__ "TelemetryConfigurations"
          TelemetryConfigurations.of_json in
      make ?nextToken ?telemetryConfigurations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of telemetry configurations for Amazon Web Services resources supported by telemetry config in the organization."]
module ListResourceTelemetryForOrganizationInput =
  struct
    type nonrec t =
      {
      accountIdentifiers: AccountIdentifiers.t option
        [@ocaml.doc
          "A list of Amazon Web Services accounts used to filter the resources to those associated with the specified accounts."];
      resourceIdentifierPrefix: ResourceIdentifierPrefix.t option
        [@ocaml.doc
          "A string used to filter resources in the organization which have a ResourceIdentifier starting with the ResourceIdentifierPrefix."];
      resourceTypes: ResourceTypes.t option
        [@ocaml.doc
          "A list of resource types used to filter resources in the organization. If this parameter is provided, the resources will be returned in the same order used in the request."];
      telemetryConfigurationState: TelemetryConfigurationState.t option
        [@ocaml.doc
          "A key-value pair to filter resources in the organization based on the telemetry type and the state of the telemetry configuration. The key is the telemetry type and the value is the state."];
      resourceTags: TagMapInput.t option
        [@ocaml.doc
          "A key-value pair to filter resources in the organization based on tags associated with the resource. Fore more information about tags, see What are tags?"];
      maxResults: ListResourceTelemetryForOrganizationMaxResults.t option
        [@ocaml.doc
          "A number field used to limit the number of results within the returned list."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token for the next set of items to return. A previous call provides this token."]}
    let make ?accountIdentifiers =
      fun ?resourceIdentifierPrefix ->
        fun ?resourceTypes ->
          fun ?telemetryConfigurationState ->
            fun ?resourceTags ->
              fun ?maxResults ->
                fun ?nextToken ->
                  fun () ->
                    {
                      accountIdentifiers;
                      resourceIdentifierPrefix;
                      resourceTypes;
                      telemetryConfigurationState;
                      resourceTags;
                      maxResults;
                      nextToken
                    }
    let to_value x =
      structure_to_value
        [("AccountIdentifiers",
           (Option.map x.accountIdentifiers ~f:AccountIdentifiers.to_value));
        ("ResourceIdentifierPrefix",
          (Option.map x.resourceIdentifierPrefix
             ~f:ResourceIdentifierPrefix.to_value));
        ("ResourceTypes",
          (Option.map x.resourceTypes ~f:ResourceTypes.to_value));
        ("TelemetryConfigurationState",
          (Option.map x.telemetryConfigurationState
             ~f:TelemetryConfigurationState.to_value));
        ("ResourceTags", (Option.map x.resourceTags ~f:TagMapInput.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:ListResourceTelemetryForOrganizationMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:ListResourceTelemetryForOrganizationMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let resourceTags =
        (Option.map ~f:TagMapInput.of_xml)
          (Xml.child xml_arg0 "ResourceTags") in
      let telemetryConfigurationState =
        (Option.map ~f:TelemetryConfigurationState.of_xml)
          (Xml.child xml_arg0 "TelemetryConfigurationState") in
      let resourceTypes =
        (Option.map ~f:ResourceTypes.of_xml)
          (Xml.child xml_arg0 "ResourceTypes") in
      let resourceIdentifierPrefix =
        (Option.map ~f:ResourceIdentifierPrefix.of_xml)
          (Xml.child xml_arg0 "ResourceIdentifierPrefix") in
      let accountIdentifiers =
        (Option.map ~f:AccountIdentifiers.of_xml)
          (Xml.child xml_arg0 "AccountIdentifiers") in
      make ?nextToken ?maxResults ?resourceTags ?telemetryConfigurationState
        ?resourceTypes ?resourceIdentifierPrefix ?accountIdentifiers ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          ListResourceTelemetryForOrganizationMaxResults.of_json in
      let resourceTags = field_map json__ "ResourceTags" TagMapInput.of_json in
      let telemetryConfigurationState =
        field_map json__ "TelemetryConfigurationState"
          TelemetryConfigurationState.of_json in
      let resourceTypes =
        field_map json__ "ResourceTypes" ResourceTypes.of_json in
      let resourceIdentifierPrefix =
        field_map json__ "ResourceIdentifierPrefix"
          ResourceIdentifierPrefix.of_json in
      let accountIdentifiers =
        field_map json__ "AccountIdentifiers" AccountIdentifiers.of_json in
      make ?nextToken ?maxResults ?resourceTags ?telemetryConfigurationState
        ?resourceTypes ?resourceIdentifierPrefix ?accountIdentifiers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of telemetry configurations for Amazon Web Services resources supported by telemetry config in the organization."]
module ListCentralizationRulesForOrganizationOutput =
  struct
    type nonrec t =
      {
      centralizationRuleSummaries: CentralizationRuleSummaries.t option
        [@ocaml.doc "A list of centralization rule summaries."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token to resume pagination of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?centralizationRuleSummaries =
      fun ?nextToken -> fun () -> { centralizationRuleSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("CentralizationRuleSummaries",
           (Option.map x.centralizationRuleSummaries
              ~f:CentralizationRuleSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let centralizationRuleSummaries =
        (Option.map ~f:CentralizationRuleSummaries.of_xml)
          (Xml.child xml_arg0 "CentralizationRuleSummaries") in
      make ?nextToken ?centralizationRuleSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let centralizationRuleSummaries =
        field_map json__ "CentralizationRuleSummaries"
          CentralizationRuleSummaries.of_json in
      make ?nextToken ?centralizationRuleSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all centralization rules in your organization. This operation can only be called by the organization's management account or a delegated administrator account."]
module ListCentralizationRulesForOrganizationInput =
  struct
    type nonrec t =
      {
      ruleNamePrefix:
        ListCentralizationRulesForOrganizationInputRuleNamePrefixString.t
          option
        [@ocaml.doc
          "A string to filter organization centralization rules whose names begin with the specified prefix."];
      allRegions: Boolean.t option
        [@ocaml.doc
          "A flag determining whether to return organization centralization rules from all regions or only the current region."];
      maxResults: ListCentralizationRulesForOrganizationMaxResults.t option
        [@ocaml.doc
          "The maximum number of organization centralization rules to return in a single call."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token for the next set of results. A previous call generates this token."]}
    let make ?ruleNamePrefix =
      fun ?allRegions ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { ruleNamePrefix; allRegions; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("RuleNamePrefix",
           (Option.map x.ruleNamePrefix
              ~f:ListCentralizationRulesForOrganizationInputRuleNamePrefixString.to_value));
        ("AllRegions", (Option.map x.allRegions ~f:Boolean.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:ListCentralizationRulesForOrganizationMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map
           ~f:ListCentralizationRulesForOrganizationMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let allRegions =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "AllRegions") in
      let ruleNamePrefix =
        (Option.map
           ~f:ListCentralizationRulesForOrganizationInputRuleNamePrefixString.of_xml)
          (Xml.child xml_arg0 "RuleNamePrefix") in
      make ?nextToken ?maxResults ?allRegions ?ruleNamePrefix ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          ListCentralizationRulesForOrganizationMaxResults.of_json in
      let allRegions = field_map json__ "AllRegions" Boolean.of_json in
      let ruleNamePrefix =
        field_map json__ "RuleNamePrefix"
          ListCentralizationRulesForOrganizationInputRuleNamePrefixString.of_json in
      make ?nextToken ?maxResults ?allRegions ?ruleNamePrefix ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all centralization rules in your organization. This operation can only be called by the organization's management account or a delegated administrator account."]
module InvalidStateException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested operation cannot be completed on the specified resource in the current state."]
module GetTelemetryRuleOutput =
  struct
    type nonrec t =
      {
      ruleName: RuleName.t option
        [@ocaml.doc "The name of the telemetry rule."];
      ruleArn: ResourceArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the telemetry rule."];
      createdTimeStamp: Long.t option
        [@ocaml.doc "The timestamp when the telemetry rule was created."];
      lastUpdateTimeStamp: Long.t option
        [@ocaml.doc
          "The timestamp when the telemetry rule was last updated."];
      telemetryRule: TelemetryRule.t option
        [@ocaml.doc "The configuration details of the telemetry rule."];
      homeRegion: Region.t option
        [@ocaml.doc
          "The Amazon Web Services Region where the telemetry rule was originally created. For replicated rules in spoke regions, this indicates the region that manages the rule. For rules created without multi-region scope, this field is not present."];
      isReplicated: IsReplicated.t option
        [@ocaml.doc
          "Indicates whether this telemetry rule is a replica that was created in this region through multi-region fan-out from the home region. Replicated rules cannot be directly updated or deleted in the spoke region. To modify a replicated rule, make changes in the home region."];
      regionStatuses: RegionStatuses.t option
        [@ocaml.doc
          "A list of per-region replication statuses for the telemetry rule. Each entry indicates the replication status of the rule in a specific spoke region. This field is only present for rules created with multi-region scope."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ruleName =
      fun ?ruleArn ->
        fun ?createdTimeStamp ->
          fun ?lastUpdateTimeStamp ->
            fun ?telemetryRule ->
              fun ?homeRegion ->
                fun ?isReplicated ->
                  fun ?regionStatuses ->
                    fun () ->
                      {
                        ruleName;
                        ruleArn;
                        createdTimeStamp;
                        lastUpdateTimeStamp;
                        telemetryRule;
                        homeRegion;
                        isReplicated;
                        regionStatuses
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RuleName", (Option.map x.ruleName ~f:RuleName.to_value));
        ("RuleArn", (Option.map x.ruleArn ~f:ResourceArn.to_value));
        ("CreatedTimeStamp",
          (Option.map x.createdTimeStamp ~f:Long.to_value));
        ("LastUpdateTimeStamp",
          (Option.map x.lastUpdateTimeStamp ~f:Long.to_value));
        ("TelemetryRule",
          (Option.map x.telemetryRule ~f:TelemetryRule.to_value));
        ("HomeRegion", (Option.map x.homeRegion ~f:Region.to_value));
        ("IsReplicated",
          (Option.map x.isReplicated ~f:IsReplicated.to_value));
        ("RegionStatuses",
          (Option.map x.regionStatuses ~f:RegionStatuses.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let regionStatuses =
        (Option.map ~f:RegionStatuses.of_xml)
          (Xml.child xml_arg0 "RegionStatuses") in
      let isReplicated =
        (Option.map ~f:IsReplicated.of_xml)
          (Xml.child xml_arg0 "IsReplicated") in
      let homeRegion =
        (Option.map ~f:Region.of_xml) (Xml.child xml_arg0 "HomeRegion") in
      let telemetryRule =
        (Option.map ~f:TelemetryRule.of_xml)
          (Xml.child xml_arg0 "TelemetryRule") in
      let lastUpdateTimeStamp =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "LastUpdateTimeStamp") in
      let createdTimeStamp =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "CreatedTimeStamp") in
      let ruleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RuleArn") in
      let ruleName =
        (Option.map ~f:RuleName.of_xml) (Xml.child xml_arg0 "RuleName") in
      make ?regionStatuses ?isReplicated ?homeRegion ?telemetryRule
        ?lastUpdateTimeStamp ?createdTimeStamp ?ruleArn ?ruleName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let regionStatuses =
        field_map json__ "RegionStatuses" RegionStatuses.of_json in
      let isReplicated = field_map json__ "IsReplicated" IsReplicated.of_json in
      let homeRegion = field_map json__ "HomeRegion" Region.of_json in
      let telemetryRule =
        field_map json__ "TelemetryRule" TelemetryRule.of_json in
      let lastUpdateTimeStamp =
        field_map json__ "LastUpdateTimeStamp" Long.of_json in
      let createdTimeStamp = field_map json__ "CreatedTimeStamp" Long.of_json in
      let ruleArn = field_map json__ "RuleArn" ResourceArn.of_json in
      let ruleName = field_map json__ "RuleName" RuleName.of_json in
      make ?regionStatuses ?isReplicated ?homeRegion ?telemetryRule
        ?lastUpdateTimeStamp ?createdTimeStamp ?ruleArn ?ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the details of a specific telemetry rule in your account."]
module GetTelemetryRuleInput =
  struct
    type nonrec t =
      {
      ruleIdentifier: RuleIdentifier.t
        [@ocaml.doc
          "The identifier (name or ARN) of the telemetry rule to retrieve."]}
    let context_ = "GetTelemetryRuleInput"
    let make ~ruleIdentifier = fun () -> { ruleIdentifier }
    let to_value x =
      structure_to_value
        [("RuleIdentifier",
           (Some (RuleIdentifier.to_value x.ruleIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleIdentifier =
        RuleIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleIdentifier") in
      make ~ruleIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleIdentifier =
        field_map_exn json__ "RuleIdentifier" RuleIdentifier.of_json in
      make ~ruleIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the details of a specific telemetry rule in your account."]
module GetTelemetryRuleForOrganizationOutput =
  struct
    type nonrec t =
      {
      ruleName: RuleName.t option
        [@ocaml.doc "The name of the organization telemetry rule."];
      ruleArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the organization telemetry rule."];
      createdTimeStamp: Long.t option
        [@ocaml.doc
          "The timestamp when the organization telemetry rule was created."];
      lastUpdateTimeStamp: Long.t option
        [@ocaml.doc
          "The timestamp when the organization telemetry rule was last updated."];
      telemetryRule: TelemetryRule.t option
        [@ocaml.doc
          "The configuration details of the organization telemetry rule."];
      homeRegion: Region.t option
        [@ocaml.doc
          "The Amazon Web Services Region where the organization telemetry rule was originally created. For replicated rules in spoke regions, this indicates the region that manages the rule. For rules created without multi-region scope, this field is not present."];
      isReplicated: IsReplicated.t option
        [@ocaml.doc
          "Indicates whether this organization telemetry rule is a replica that was created in this region through multi-region fan-out from the home region. Replicated rules cannot be directly updated or deleted in the spoke region. To modify a replicated rule, make changes in the home region."];
      regionStatuses: RegionStatuses.t option
        [@ocaml.doc
          "A list of per-region replication statuses for the organization telemetry rule. Each entry indicates the replication status of the rule in a specific spoke region. This field is only present for rules created with multi-region scope."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ruleName =
      fun ?ruleArn ->
        fun ?createdTimeStamp ->
          fun ?lastUpdateTimeStamp ->
            fun ?telemetryRule ->
              fun ?homeRegion ->
                fun ?isReplicated ->
                  fun ?regionStatuses ->
                    fun () ->
                      {
                        ruleName;
                        ruleArn;
                        createdTimeStamp;
                        lastUpdateTimeStamp;
                        telemetryRule;
                        homeRegion;
                        isReplicated;
                        regionStatuses
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RuleName", (Option.map x.ruleName ~f:RuleName.to_value));
        ("RuleArn", (Option.map x.ruleArn ~f:ResourceArn.to_value));
        ("CreatedTimeStamp",
          (Option.map x.createdTimeStamp ~f:Long.to_value));
        ("LastUpdateTimeStamp",
          (Option.map x.lastUpdateTimeStamp ~f:Long.to_value));
        ("TelemetryRule",
          (Option.map x.telemetryRule ~f:TelemetryRule.to_value));
        ("HomeRegion", (Option.map x.homeRegion ~f:Region.to_value));
        ("IsReplicated",
          (Option.map x.isReplicated ~f:IsReplicated.to_value));
        ("RegionStatuses",
          (Option.map x.regionStatuses ~f:RegionStatuses.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let regionStatuses =
        (Option.map ~f:RegionStatuses.of_xml)
          (Xml.child xml_arg0 "RegionStatuses") in
      let isReplicated =
        (Option.map ~f:IsReplicated.of_xml)
          (Xml.child xml_arg0 "IsReplicated") in
      let homeRegion =
        (Option.map ~f:Region.of_xml) (Xml.child xml_arg0 "HomeRegion") in
      let telemetryRule =
        (Option.map ~f:TelemetryRule.of_xml)
          (Xml.child xml_arg0 "TelemetryRule") in
      let lastUpdateTimeStamp =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "LastUpdateTimeStamp") in
      let createdTimeStamp =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "CreatedTimeStamp") in
      let ruleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RuleArn") in
      let ruleName =
        (Option.map ~f:RuleName.of_xml) (Xml.child xml_arg0 "RuleName") in
      make ?regionStatuses ?isReplicated ?homeRegion ?telemetryRule
        ?lastUpdateTimeStamp ?createdTimeStamp ?ruleArn ?ruleName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let regionStatuses =
        field_map json__ "RegionStatuses" RegionStatuses.of_json in
      let isReplicated = field_map json__ "IsReplicated" IsReplicated.of_json in
      let homeRegion = field_map json__ "HomeRegion" Region.of_json in
      let telemetryRule =
        field_map json__ "TelemetryRule" TelemetryRule.of_json in
      let lastUpdateTimeStamp =
        field_map json__ "LastUpdateTimeStamp" Long.of_json in
      let createdTimeStamp = field_map json__ "CreatedTimeStamp" Long.of_json in
      let ruleArn = field_map json__ "RuleArn" ResourceArn.of_json in
      let ruleName = field_map json__ "RuleName" RuleName.of_json in
      make ?regionStatuses ?isReplicated ?homeRegion ?telemetryRule
        ?lastUpdateTimeStamp ?createdTimeStamp ?ruleArn ?ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the details of a specific organization telemetry rule. This operation can only be called by the organization's management account or a delegated administrator account."]
module GetTelemetryRuleForOrganizationInput =
  struct
    type nonrec t =
      {
      ruleIdentifier: RuleIdentifier.t
        [@ocaml.doc
          "The identifier (name or ARN) of the organization telemetry rule to retrieve."]}
    let context_ = "GetTelemetryRuleForOrganizationInput"
    let make ~ruleIdentifier = fun () -> { ruleIdentifier }
    let to_value x =
      structure_to_value
        [("RuleIdentifier",
           (Some (RuleIdentifier.to_value x.ruleIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleIdentifier =
        RuleIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleIdentifier") in
      make ~ruleIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleIdentifier =
        field_map_exn json__ "RuleIdentifier" RuleIdentifier.of_json in
      make ~ruleIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the details of a specific organization telemetry rule. This operation can only be called by the organization's management account or a delegated administrator account."]
module GetTelemetryPipelineOutput =
  struct
    type nonrec t =
      {
      pipeline: TelemetryPipeline.t option
        [@ocaml.doc
          "The complete telemetry pipeline resource information, including configuration, status, and metadata."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?pipeline = fun () -> { pipeline }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Pipeline", (Option.map x.pipeline ~f:TelemetryPipeline.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pipeline =
        (Option.map ~f:TelemetryPipeline.of_xml)
          (Xml.child xml_arg0 "Pipeline") in
      make ?pipeline ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pipeline = field_map json__ "Pipeline" TelemetryPipeline.of_json in
      make ?pipeline ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves information about a specific telemetry pipeline, including its configuration, status, and metadata."]
module GetTelemetryPipelineInput =
  struct
    type nonrec t =
      {
      pipelineIdentifier: TelemetryPipelineIdentifier.t
        [@ocaml.doc
          "The identifier (name or ARN) of the telemetry pipeline to retrieve."]}
    let context_ = "GetTelemetryPipelineInput"
    let make ~pipelineIdentifier = fun () -> { pipelineIdentifier }
    let to_value x =
      structure_to_value
        [("PipelineIdentifier",
           (Some (TelemetryPipelineIdentifier.to_value x.pipelineIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pipelineIdentifier =
        TelemetryPipelineIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PipelineIdentifier") in
      make ~pipelineIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pipelineIdentifier =
        field_map_exn json__ "PipelineIdentifier"
          TelemetryPipelineIdentifier.of_json in
      make ~pipelineIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves information about a specific telemetry pipeline, including its configuration, status, and metadata."]
module GetTelemetryEvaluationStatusOutput =
  struct
    type nonrec t =
      {
      status: Status.t option
        [@ocaml.doc "The onboarding status of the telemetry config feature."];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "Describes the reason for the failure status. The field will only be populated if Status is FAILED_START or FAILED_STOP."];
      homeRegion: Region.t option
        [@ocaml.doc
          "The Amazon Web Services Region that is designated as the home region for multi-region telemetry evaluation. The home region is the single management point for all multi-region operations on this account. This field is only present when multi-region telemetry evaluation is active."];
      regionStatuses: RegionStatuses.t option
        [@ocaml.doc
          "A list of per-region telemetry evaluation statuses. Each entry indicates the evaluation status for a specific spoke region included in the multi-region configuration. This field is only present when multi-region telemetry evaluation is active."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?status =
      fun ?failureReason ->
        fun ?homeRegion ->
          fun ?regionStatuses ->
            fun () -> { status; failureReason; homeRegion; regionStatuses }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Status", (Option.map x.status ~f:Status.to_value));
        ("FailureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("HomeRegion", (Option.map x.homeRegion ~f:Region.to_value));
        ("RegionStatuses",
          (Option.map x.regionStatuses ~f:RegionStatuses.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let regionStatuses =
        (Option.map ~f:RegionStatuses.of_xml)
          (Xml.child xml_arg0 "RegionStatuses") in
      let homeRegion =
        (Option.map ~f:Region.of_xml) (Xml.child xml_arg0 "HomeRegion") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "FailureReason") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "Status") in
      make ?regionStatuses ?homeRegion ?failureReason ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let regionStatuses =
        field_map json__ "RegionStatuses" RegionStatuses.of_json in
      let homeRegion = field_map json__ "HomeRegion" Region.of_json in
      let failureReason =
        field_map json__ "FailureReason" FailureReason.of_json in
      let status = field_map json__ "Status" Status.of_json in
      make ?regionStatuses ?homeRegion ?failureReason ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the current onboarding status of the telemetry config feature, including the status of the feature and reason the feature failed to start or stop."]
module GetTelemetryEvaluationStatusForOrganizationOutput =
  struct
    type nonrec t =
      {
      status: Status.t option
        [@ocaml.doc
          "The onboarding status of the telemetry config feature for the organization."];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "This field describes the reason for the failure status. The field will only be populated if Status is FAILED_START or FAILED_STOP."];
      homeRegion: Region.t option
        [@ocaml.doc
          "The Amazon Web Services Region that is designated as the home region for multi-region telemetry evaluation for the organization. The home region is the single management point for all multi-region operations on this organization. This field is only present when multi-region telemetry evaluation is active."];
      regionStatuses: RegionStatuses.t option
        [@ocaml.doc
          "A list of per-region telemetry evaluation statuses for the organization. Each entry indicates the evaluation status for a specific spoke region included in the multi-region configuration. This field is only present when multi-region telemetry evaluation is active."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?status =
      fun ?failureReason ->
        fun ?homeRegion ->
          fun ?regionStatuses ->
            fun () -> { status; failureReason; homeRegion; regionStatuses }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Status", (Option.map x.status ~f:Status.to_value));
        ("FailureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("HomeRegion", (Option.map x.homeRegion ~f:Region.to_value));
        ("RegionStatuses",
          (Option.map x.regionStatuses ~f:RegionStatuses.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let regionStatuses =
        (Option.map ~f:RegionStatuses.of_xml)
          (Xml.child xml_arg0 "RegionStatuses") in
      let homeRegion =
        (Option.map ~f:Region.of_xml) (Xml.child xml_arg0 "HomeRegion") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "FailureReason") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "Status") in
      make ?regionStatuses ?homeRegion ?failureReason ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let regionStatuses =
        field_map json__ "RegionStatuses" RegionStatuses.of_json in
      let homeRegion = field_map json__ "HomeRegion" Region.of_json in
      let failureReason =
        field_map json__ "FailureReason" FailureReason.of_json in
      let status = field_map json__ "Status" Status.of_json in
      make ?regionStatuses ?homeRegion ?failureReason ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This returns the onboarding status of the telemetry configuration feature for the organization. It can only be called by a Management Account of an Amazon Web Services Organization or an assigned Delegated Admin Account of Amazon CloudWatch telemetry config."]
module GetTelemetryEnrichmentStatusOutput =
  struct
    type nonrec t =
      {
      status: TelemetryEnrichmentStatus.t option
        [@ocaml.doc
          "The current status of the resource tags for telemetry feature (Running, Stopped, or Impaired)."];
      awsResourceExplorerManagedViewArn:
        AwsResourceExplorerManagedViewArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Resource Explorer managed view used for resource tags for telemetry, if the feature is enabled."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?status =
      fun ?awsResourceExplorerManagedViewArn ->
        fun () -> { status; awsResourceExplorerManagedViewArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Status",
           (Option.map x.status ~f:TelemetryEnrichmentStatus.to_value));
        ("AwsResourceExplorerManagedViewArn",
          (Option.map x.awsResourceExplorerManagedViewArn
             ~f:AwsResourceExplorerManagedViewArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let awsResourceExplorerManagedViewArn =
        (Option.map ~f:AwsResourceExplorerManagedViewArn.of_xml)
          (Xml.child xml_arg0 "AwsResourceExplorerManagedViewArn") in
      let status =
        (Option.map ~f:TelemetryEnrichmentStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      make ?awsResourceExplorerManagedViewArn ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let awsResourceExplorerManagedViewArn =
        field_map json__ "AwsResourceExplorerManagedViewArn"
          AwsResourceExplorerManagedViewArn.of_json in
      let status =
        field_map json__ "Status" TelemetryEnrichmentStatus.of_json in
      make ?awsResourceExplorerManagedViewArn ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the current status of the resource tags for telemetry feature, which enhances telemetry data with additional resource metadata from Resource Explorer."]
module GetS3TableIntegrationOutput =
  struct
    type nonrec t =
      {
      arn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the S3 Table integration."];
      roleArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM role used by the S3 Table integration."];
      status: IntegrationStatus.t option
        [@ocaml.doc "The current status of the S3 Table integration."];
      encryption: Encryption.t option
        [@ocaml.doc
          "The encryption configuration for the S3 Table integration."];
      destinationTableBucketArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the S3 bucket used as the destination for the table data."];
      createdTimeStamp: Long.t option
        [@ocaml.doc
          "The timestamp when the S3 Table integration was created."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn =
      fun ?roleArn ->
        fun ?status ->
          fun ?encryption ->
            fun ?destinationTableBucketArn ->
              fun ?createdTimeStamp ->
                fun () ->
                  {
                    arn;
                    roleArn;
                    status;
                    encryption;
                    destinationTableBucketArn;
                    createdTimeStamp
                  }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ResourceArn.to_value));
        ("RoleArn", (Option.map x.roleArn ~f:ResourceArn.to_value));
        ("Status", (Option.map x.status ~f:IntegrationStatus.to_value));
        ("Encryption", (Option.map x.encryption ~f:Encryption.to_value));
        ("DestinationTableBucketArn",
          (Option.map x.destinationTableBucketArn ~f:ResourceArn.to_value));
        ("CreatedTimeStamp",
          (Option.map x.createdTimeStamp ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let createdTimeStamp =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "CreatedTimeStamp") in
      let destinationTableBucketArn =
        (Option.map ~f:ResourceArn.of_xml)
          (Xml.child xml_arg0 "DestinationTableBucketArn") in
      let encryption =
        (Option.map ~f:Encryption.of_xml) (Xml.child xml_arg0 "Encryption") in
      let status =
        (Option.map ~f:IntegrationStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let roleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RoleArn") in
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?createdTimeStamp ?destinationTableBucketArn ?encryption ?status
        ?roleArn ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let createdTimeStamp = field_map json__ "CreatedTimeStamp" Long.of_json in
      let destinationTableBucketArn =
        field_map json__ "DestinationTableBucketArn" ResourceArn.of_json in
      let encryption = field_map json__ "Encryption" Encryption.of_json in
      let status = field_map json__ "Status" IntegrationStatus.of_json in
      let roleArn = field_map json__ "RoleArn" ResourceArn.of_json in
      let arn = field_map json__ "Arn" ResourceArn.of_json in
      make ?createdTimeStamp ?destinationTableBucketArn ?encryption ?status
        ?roleArn ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves information about a specific S3 Table integration, including its configuration, status, and metadata."]
module GetS3TableIntegrationInput =
  struct
    type nonrec t =
      {
      arn: ResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the S3 Table integration to retrieve."]}
    let context_ = "GetS3TableIntegrationInput"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value [("Arn", (Some (ResourceArn.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        ResourceArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Arn") in
      make ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let arn = field_map_exn json__ "Arn" ResourceArn.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves information about a specific S3 Table integration, including its configuration, status, and metadata."]
module GetCentralizationRuleForOrganizationOutput =
  struct
    type nonrec t =
      {
      ruleName: RuleName.t option
        [@ocaml.doc "The name of the organization centralization rule."];
      ruleArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the organization centralization rule."];
      creatorAccountId: String_.t option
        [@ocaml.doc
          "The Amazon Web Services Account that created the organization centralization rule."];
      createdTimeStamp: Long.t option
        [@ocaml.doc
          "The timestamp when the organization centralization rule was created."];
      createdRegion: Region.t option
        [@ocaml.doc
          "The Amazon Web Services region where the organization centralization rule was created."];
      lastUpdateTimeStamp: Long.t option
        [@ocaml.doc
          "The timestamp when the organization centralization rule was last updated."];
      ruleHealth: RuleHealth.t option
        [@ocaml.doc
          "The health status of the organization centralization rule."];
      failureReason: CentralizationFailureReason.t option
        [@ocaml.doc
          "The reason why an organization centralization rule is marked UNHEALTHY."];
      centralizationRule: CentralizationRule.t option
        [@ocaml.doc
          "The configuration details for the organization centralization rule."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ruleName =
      fun ?ruleArn ->
        fun ?creatorAccountId ->
          fun ?createdTimeStamp ->
            fun ?createdRegion ->
              fun ?lastUpdateTimeStamp ->
                fun ?ruleHealth ->
                  fun ?failureReason ->
                    fun ?centralizationRule ->
                      fun () ->
                        {
                          ruleName;
                          ruleArn;
                          creatorAccountId;
                          createdTimeStamp;
                          createdRegion;
                          lastUpdateTimeStamp;
                          ruleHealth;
                          failureReason;
                          centralizationRule
                        }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RuleName", (Option.map x.ruleName ~f:RuleName.to_value));
        ("RuleArn", (Option.map x.ruleArn ~f:ResourceArn.to_value));
        ("CreatorAccountId",
          (Option.map x.creatorAccountId ~f:String_.to_value));
        ("CreatedTimeStamp",
          (Option.map x.createdTimeStamp ~f:Long.to_value));
        ("CreatedRegion", (Option.map x.createdRegion ~f:Region.to_value));
        ("LastUpdateTimeStamp",
          (Option.map x.lastUpdateTimeStamp ~f:Long.to_value));
        ("RuleHealth", (Option.map x.ruleHealth ~f:RuleHealth.to_value));
        ("FailureReason",
          (Option.map x.failureReason ~f:CentralizationFailureReason.to_value));
        ("CentralizationRule",
          (Option.map x.centralizationRule ~f:CentralizationRule.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let centralizationRule =
        (Option.map ~f:CentralizationRule.of_xml)
          (Xml.child xml_arg0 "CentralizationRule") in
      let failureReason =
        (Option.map ~f:CentralizationFailureReason.of_xml)
          (Xml.child xml_arg0 "FailureReason") in
      let ruleHealth =
        (Option.map ~f:RuleHealth.of_xml) (Xml.child xml_arg0 "RuleHealth") in
      let lastUpdateTimeStamp =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "LastUpdateTimeStamp") in
      let createdRegion =
        (Option.map ~f:Region.of_xml) (Xml.child xml_arg0 "CreatedRegion") in
      let createdTimeStamp =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "CreatedTimeStamp") in
      let creatorAccountId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CreatorAccountId") in
      let ruleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RuleArn") in
      let ruleName =
        (Option.map ~f:RuleName.of_xml) (Xml.child xml_arg0 "RuleName") in
      make ?centralizationRule ?failureReason ?ruleHealth
        ?lastUpdateTimeStamp ?createdRegion ?createdTimeStamp
        ?creatorAccountId ?ruleArn ?ruleName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let centralizationRule =
        field_map json__ "CentralizationRule" CentralizationRule.of_json in
      let failureReason =
        field_map json__ "FailureReason" CentralizationFailureReason.of_json in
      let ruleHealth = field_map json__ "RuleHealth" RuleHealth.of_json in
      let lastUpdateTimeStamp =
        field_map json__ "LastUpdateTimeStamp" Long.of_json in
      let createdRegion = field_map json__ "CreatedRegion" Region.of_json in
      let createdTimeStamp = field_map json__ "CreatedTimeStamp" Long.of_json in
      let creatorAccountId =
        field_map json__ "CreatorAccountId" String_.of_json in
      let ruleArn = field_map json__ "RuleArn" ResourceArn.of_json in
      let ruleName = field_map json__ "RuleName" RuleName.of_json in
      make ?centralizationRule ?failureReason ?ruleHealth
        ?lastUpdateTimeStamp ?createdRegion ?createdTimeStamp
        ?creatorAccountId ?ruleArn ?ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the details of a specific organization centralization rule. This operation can only be called by the organization's management account or a delegated administrator account."]
module GetCentralizationRuleForOrganizationInput =
  struct
    type nonrec t =
      {
      ruleIdentifier: RuleIdentifier.t
        [@ocaml.doc
          "The identifier (name or ARN) of the organization centralization rule to retrieve."]}
    let context_ = "GetCentralizationRuleForOrganizationInput"
    let make ~ruleIdentifier = fun () -> { ruleIdentifier }
    let to_value x =
      structure_to_value
        [("RuleIdentifier",
           (Some (RuleIdentifier.to_value x.ruleIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleIdentifier =
        RuleIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleIdentifier") in
      make ~ruleIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleIdentifier =
        field_map_exn json__ "RuleIdentifier" RuleIdentifier.of_json in
      make ~ruleIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the details of a specific organization centralization rule. This operation can only be called by the organization's management account or a delegated administrator account."]
module DeleteTelemetryRuleInput =
  struct
    type nonrec t =
      {
      ruleIdentifier: RuleIdentifier.t
        [@ocaml.doc
          "The identifier (name or ARN) of the telemetry rule to delete."]}
    let context_ = "DeleteTelemetryRuleInput"
    let make ~ruleIdentifier = fun () -> { ruleIdentifier }
    let to_value x =
      structure_to_value
        [("RuleIdentifier",
           (Some (RuleIdentifier.to_value x.ruleIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleIdentifier =
        RuleIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleIdentifier") in
      make ~ruleIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleIdentifier =
        field_map_exn json__ "RuleIdentifier" RuleIdentifier.of_json in
      make ~ruleIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a telemetry rule from your account. Any telemetry configurations previously created by the rule will remain but no new resources will be configured by this rule."]
module DeleteTelemetryRuleForOrganizationInput =
  struct
    type nonrec t =
      {
      ruleIdentifier: RuleIdentifier.t
        [@ocaml.doc
          "The identifier (name or ARN) of the organization telemetry rule to delete."]}
    let context_ = "DeleteTelemetryRuleForOrganizationInput"
    let make ~ruleIdentifier = fun () -> { ruleIdentifier }
    let to_value x =
      structure_to_value
        [("RuleIdentifier",
           (Some (RuleIdentifier.to_value x.ruleIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleIdentifier =
        RuleIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleIdentifier") in
      make ~ruleIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleIdentifier =
        field_map_exn json__ "RuleIdentifier" RuleIdentifier.of_json in
      make ~ruleIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an organization-wide telemetry rule. This operation can only be called by the organization's management account or a delegated administrator account."]
module DeleteTelemetryPipelineOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a telemetry pipeline and its associated resources. This operation stops data processing and removes the pipeline configuration."]
module DeleteTelemetryPipelineInput =
  struct
    type nonrec t =
      {
      pipelineIdentifier: TelemetryPipelineIdentifier.t
        [@ocaml.doc "The ARN of the telemetry pipeline to delete."]}
    let context_ = "DeleteTelemetryPipelineInput"
    let make ~pipelineIdentifier = fun () -> { pipelineIdentifier }
    let to_value x =
      structure_to_value
        [("PipelineIdentifier",
           (Some (TelemetryPipelineIdentifier.to_value x.pipelineIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pipelineIdentifier =
        TelemetryPipelineIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PipelineIdentifier") in
      make ~pipelineIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pipelineIdentifier =
        field_map_exn json__ "PipelineIdentifier"
          TelemetryPipelineIdentifier.of_json in
      make ~pipelineIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a telemetry pipeline and its associated resources. This operation stops data processing and removes the pipeline configuration."]
module DeleteS3TableIntegrationInput =
  struct
    type nonrec t =
      {
      arn: ResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the S3 Table integration to delete."]}
    let context_ = "DeleteS3TableIntegrationInput"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value [("Arn", (Some (ResourceArn.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        ResourceArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Arn") in
      make ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let arn = field_map_exn json__ "Arn" ResourceArn.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an S3 Table integration and its associated data. This operation removes the connection between CloudWatch Observability Admin and S3 Tables."]
module DeleteCentralizationRuleForOrganizationInput =
  struct
    type nonrec t =
      {
      ruleIdentifier: RuleIdentifier.t
        [@ocaml.doc
          "The identifier (name or ARN) of the organization centralization rule to delete."]}
    let context_ = "DeleteCentralizationRuleForOrganizationInput"
    let make ~ruleIdentifier = fun () -> { ruleIdentifier }
    let to_value x =
      structure_to_value
        [("RuleIdentifier",
           (Some (RuleIdentifier.to_value x.ruleIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleIdentifier =
        RuleIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleIdentifier") in
      make ~ruleIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleIdentifier =
        field_map_exn json__ "RuleIdentifier" RuleIdentifier.of_json in
      make ~ruleIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an organization-wide centralization rule. This operation can only be called by the organization's management account or a delegated administrator account."]
module CreateTelemetryRuleOutput =
  struct
    type nonrec t =
      {
      ruleArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the created telemetry rule."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ruleArn = fun () -> { ruleArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RuleArn", (Option.map x.ruleArn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RuleArn") in
      make ?ruleArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleArn = field_map json__ "RuleArn" ResourceArn.of_json in
      make ?ruleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a telemetry rule that defines how telemetry should be configured for Amazon Web Services resources in your account. The rule specifies which resources should have telemetry enabled and how that telemetry data should be collected based on resource type, telemetry type, and selection criteria."]
module CreateTelemetryRuleInput =
  struct
    type nonrec t =
      {
      ruleName: RuleName.t
        [@ocaml.doc "A unique name for the telemetry rule being created."];
      rule: TelemetryRule.t
        [@ocaml.doc
          "The configuration details for the telemetry rule, including the resource type, telemetry type, destination configuration, and selection criteria for which resources the rule applies to."];
      tags: TagMapInput.t option
        [@ocaml.doc
          "The key-value pairs to associate with the telemetry rule resource for categorization and management purposes."]}
    let context_ = "CreateTelemetryRuleInput"
    let make ?tags =
      fun ~ruleName -> fun ~rule -> fun () -> { tags; ruleName; rule }
    let to_value x =
      structure_to_value
        [("RuleName", (Some (RuleName.to_value x.ruleName)));
        ("Rule", (Some (TelemetryRule.to_value x.rule)));
        ("Tags", (Option.map x.tags ~f:TagMapInput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        (Option.map ~f:TagMapInput.of_xml) (Xml.child xml_arg0 "Tags") in
      let rule =
        TelemetryRule.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Rule") in
      let ruleName =
        RuleName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "RuleName") in
      make ?tags ~rule ~ruleName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMapInput.of_json in
      let rule = field_map_exn json__ "Rule" TelemetryRule.of_json in
      let ruleName = field_map_exn json__ "RuleName" RuleName.of_json in
      make ?tags ~rule ~ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a telemetry rule that defines how telemetry should be configured for Amazon Web Services resources in your account. The rule specifies which resources should have telemetry enabled and how that telemetry data should be collected based on resource type, telemetry type, and selection criteria."]
module CreateTelemetryRuleForOrganizationOutput =
  struct
    type nonrec t =
      {
      ruleArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the created organization telemetry rule."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ruleArn = fun () -> { ruleArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RuleArn", (Option.map x.ruleArn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RuleArn") in
      make ?ruleArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleArn = field_map json__ "RuleArn" ResourceArn.of_json in
      make ?ruleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a telemetry rule that applies across an Amazon Web Services Organization. This operation can only be called by the organization's management account or a delegated administrator account."]
module CreateTelemetryRuleForOrganizationInput =
  struct
    type nonrec t =
      {
      ruleName: RuleName.t
        [@ocaml.doc
          "A unique name for the organization-wide telemetry rule being created."];
      rule: TelemetryRule.t
        [@ocaml.doc
          "The configuration details for the organization-wide telemetry rule, including the resource type, telemetry type, destination configuration, and selection criteria for which resources the rule applies to across the organization."];
      tags: TagMapInput.t option
        [@ocaml.doc
          "The key-value pairs to associate with the organization telemetry rule resource for categorization and management purposes."]}
    let context_ = "CreateTelemetryRuleForOrganizationInput"
    let make ?tags =
      fun ~ruleName -> fun ~rule -> fun () -> { tags; ruleName; rule }
    let to_value x =
      structure_to_value
        [("RuleName", (Some (RuleName.to_value x.ruleName)));
        ("Rule", (Some (TelemetryRule.to_value x.rule)));
        ("Tags", (Option.map x.tags ~f:TagMapInput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        (Option.map ~f:TagMapInput.of_xml) (Xml.child xml_arg0 "Tags") in
      let rule =
        TelemetryRule.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Rule") in
      let ruleName =
        RuleName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "RuleName") in
      make ?tags ~rule ~ruleName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMapInput.of_json in
      let rule = field_map_exn json__ "Rule" TelemetryRule.of_json in
      let ruleName = field_map_exn json__ "RuleName" RuleName.of_json in
      make ?tags ~rule ~ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a telemetry rule that applies across an Amazon Web Services Organization. This operation can only be called by the organization's management account or a delegated administrator account."]
module CreateTelemetryPipelineOutput =
  struct
    type nonrec t =
      {
      arn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the created telemetry pipeline."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn = fun () -> { arn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let arn = field_map json__ "Arn" ResourceArn.of_json in make ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a telemetry pipeline for processing and transforming telemetry data. The pipeline defines how data flows from sources through processors to destinations, enabling data transformation and delivering capabilities."]
module CreateTelemetryPipelineInput =
  struct
    type nonrec t =
      {
      name: TelemetryPipelineName.t
        [@ocaml.doc
          "The name of the telemetry pipeline to create. The name must be unique within your account."];
      configuration: TelemetryPipelineConfiguration.t
        [@ocaml.doc
          "The configuration that defines how the telemetry pipeline processes data, including sources, processors, and destinations. For more information about pipeline components, see the Amazon CloudWatch User Guide"];
      tags: TagMapInput.t option
        [@ocaml.doc
          "The key-value pairs to associate with the telemetry pipeline resource for categorization and management purposes."]}
    let context_ = "CreateTelemetryPipelineInput"
    let make ?tags =
      fun ~name ->
        fun ~configuration -> fun () -> { tags; name; configuration }
    let to_value x =
      structure_to_value
        [("Name", (Some (TelemetryPipelineName.to_value x.name)));
        ("Configuration",
          (Some (TelemetryPipelineConfiguration.to_value x.configuration)));
        ("Tags", (Option.map x.tags ~f:TagMapInput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        (Option.map ~f:TagMapInput.of_xml) (Xml.child xml_arg0 "Tags") in
      let configuration =
        TelemetryPipelineConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Configuration") in
      let name =
        TelemetryPipelineName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?tags ~configuration ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMapInput.of_json in
      let configuration =
        field_map_exn json__ "Configuration"
          TelemetryPipelineConfiguration.of_json in
      let name = field_map_exn json__ "Name" TelemetryPipelineName.of_json in
      make ?tags ~configuration ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a telemetry pipeline for processing and transforming telemetry data. The pipeline defines how data flows from sources through processors to destinations, enabling data transformation and delivering capabilities."]
module CreateS3TableIntegrationOutput =
  struct
    type nonrec t =
      {
      arn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the created S3 Table integration."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn = fun () -> { arn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let arn = field_map json__ "Arn" ResourceArn.of_json in make ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an integration between CloudWatch and S3 Tables for analytics. This integration enables querying CloudWatch telemetry data using analytics engines like Amazon Athena, Amazon Redshift, and Apache Spark."]
module CreateS3TableIntegrationInput =
  struct
    type nonrec t =
      {
      encryption: Encryption.t
        [@ocaml.doc
          "The encryption configuration for the S3 Table integration, including the encryption algorithm and KMS key settings."];
      roleArn: ResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM role that grants permissions for the S3 Table integration to access necessary resources."];
      tags: TagMapInput.t option
        [@ocaml.doc
          "The key-value pairs to associate with the S3 Table integration resource for categorization and management purposes."]}
    let context_ = "CreateS3TableIntegrationInput"
    let make ?tags =
      fun ~encryption ->
        fun ~roleArn -> fun () -> { tags; encryption; roleArn }
    let to_value x =
      structure_to_value
        [("Encryption", (Some (Encryption.to_value x.encryption)));
        ("RoleArn", (Some (ResourceArn.to_value x.roleArn)));
        ("Tags", (Option.map x.tags ~f:TagMapInput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        (Option.map ~f:TagMapInput.of_xml) (Xml.child xml_arg0 "Tags") in
      let roleArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RoleArn") in
      let encryption =
        Encryption.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Encryption") in
      make ?tags ~roleArn ~encryption ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMapInput.of_json in
      let roleArn = field_map_exn json__ "RoleArn" ResourceArn.of_json in
      let encryption = field_map_exn json__ "Encryption" Encryption.of_json in
      make ?tags ~roleArn ~encryption ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an integration between CloudWatch and S3 Tables for analytics. This integration enables querying CloudWatch telemetry data using analytics engines like Amazon Athena, Amazon Redshift, and Apache Spark."]
module CreateCentralizationRuleForOrganizationOutput =
  struct
    type nonrec t =
      {
      ruleArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the created organization centralization rule."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ruleArn = fun () -> { ruleArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RuleArn", (Option.map x.ruleArn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "RuleArn") in
      make ?ruleArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleArn = field_map json__ "RuleArn" ResourceArn.of_json in
      make ?ruleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a centralization rule that applies across an Amazon Web Services Organization. This operation can only be called by the organization's management account or a delegated administrator account."]
module CreateCentralizationRuleForOrganizationInput =
  struct
    type nonrec t =
      {
      ruleName: RuleName.t
        [@ocaml.doc
          "A unique name for the organization-wide centralization rule being created."];
      rule: CentralizationRule.t
        [@ocaml.doc
          "The configuration details for the organization-wide centralization rule, including the source configuration and the destination configuration to centralize telemetry data across the organization."];
      tags: TagMapInput.t option
        [@ocaml.doc
          "The key-value pairs to associate with the organization telemetry rule resource for categorization and management purposes."]}
    let context_ = "CreateCentralizationRuleForOrganizationInput"
    let make ?tags =
      fun ~ruleName -> fun ~rule -> fun () -> { tags; ruleName; rule }
    let to_value x =
      structure_to_value
        [("RuleName", (Some (RuleName.to_value x.ruleName)));
        ("Rule", (Some (CentralizationRule.to_value x.rule)));
        ("Tags", (Option.map x.tags ~f:TagMapInput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        (Option.map ~f:TagMapInput.of_xml) (Xml.child xml_arg0 "Tags") in
      let rule =
        CentralizationRule.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Rule") in
      let ruleName =
        RuleName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "RuleName") in
      make ?tags ~rule ~ruleName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMapInput.of_json in
      let rule = field_map_exn json__ "Rule" CentralizationRule.of_json in
      let ruleName = field_map_exn json__ "RuleName" RuleName.of_json in
      make ?tags ~rule ~ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a centralization rule that applies across an Amazon Web Services Organization. This operation can only be called by the organization's management account or a delegated administrator account."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let activate_event_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING EventSourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.activate_event_source
           (Values.ActivateEventSourceRequest.make ~name ()) None None])
let cancel_replay =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replayName =
         flag "replay-name" (required string) ~doc:"STRING ReplayName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_replay (Values.CancelReplayRequest.make ~replayName ())
           (Some Values.CancelReplayResponse.to_json)
           (Some Values.CancelReplayResponse.error_to_json)])
let create_api_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ApiDestinationDescription"
       and invocationRateLimitPerSecond =
         flag "invocation-rate-limit-per-second" (optional int)
           ~doc:"INT ApiDestinationInvocationRateLimitPerSecond"
       and name =
         flag "name" (required string) ~doc:"STRING ApiDestinationName"
       and connectionArn =
         flag "connection-arn" (required string) ~doc:"STRING ConnectionArn"
       and invocationEndpoint =
         flag "invocation-endpoint" (required string)
           ~doc:"STRING HttpsEndpoint"
       and httpMethod =
         flag "http-method" (required json_arg)
           ~doc:"JSON ApiDestinationHttpMethod" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_api_destination
           (Values.CreateApiDestinationRequest.make ?description
              ?invocationRateLimitPerSecond ~name ~connectionArn
              ~invocationEndpoint
              ~httpMethod:(Values.ApiDestinationHttpMethod.of_json httpMethod)
              ()) (Some Values.CreateApiDestinationResponse.to_json)
           (Some Values.CreateApiDestinationResponse.error_to_json)])
let create_archive =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ArchiveDescription"
       and eventPattern =
         flag "event-pattern" (optional string) ~doc:"STRING EventPattern"
       and retentionDays =
         flag "retention-days" (optional int) ~doc:"INT RetentionDays"
       and archiveName =
         flag "archive-name" (required string) ~doc:"STRING ArchiveName"
       and eventSourceArn =
         flag "event-source-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_archive
           (Values.CreateArchiveRequest.make ?description ?eventPattern
              ?retentionDays ~archiveName ~eventSourceArn ())
           (Some Values.CreateArchiveResponse.to_json)
           (Some Values.CreateArchiveResponse.error_to_json)])
let create_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ConnectionDescription"
       and name = flag "name" (required string) ~doc:"STRING ConnectionName"
       and authorizationType =
         flag "authorization-type" (required json_arg)
           ~doc:"JSON ConnectionAuthorizationType"
       and authParameters =
         flag "auth-parameters" (required json_arg)
           ~doc:"JSON CreateConnectionAuthRequestParameters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connection
           (Values.CreateConnectionRequest.make ?description ~name
              ~authorizationType:(Values.ConnectionAuthorizationType.of_json
                                    authorizationType)
              ~authParameters:(Values.CreateConnectionAuthRequestParameters.of_json
                                 authParameters) ())
           (Some Values.CreateConnectionResponse.to_json)
           (Some Values.CreateConnectionResponse.error_to_json)])
let create_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING EndpointDescription"
       and replicationConfig =
         flag "replication-config" (optional json_arg)
           ~doc:"JSON ReplicationConfig"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and name = flag "name" (required string) ~doc:"STRING EndpointName"
       and routingConfig =
         flag "routing-config" (required json_arg) ~doc:"JSON RoutingConfig"
       and eventBuses =
         flag "event-buses" (required json_arg)
           ~doc:"JSON EndpointEventBusList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_endpoint
           (Values.CreateEndpointRequest.make ?description
              ?replicationConfig:(Option.map
                                    ~f:Values.ReplicationConfig.of_json
                                    replicationConfig) ?roleArn ~name
              ~routingConfig:(Values.RoutingConfig.of_json routingConfig)
              ~eventBuses:(Values.EndpointEventBusList.of_json eventBuses) ())
           (Some Values.CreateEndpointResponse.to_json)
           (Some Values.CreateEndpointResponse.error_to_json)])
let create_event_bus =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventSourceName =
         flag "event-source-name" (optional string)
           ~doc:"STRING EventSourceName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING EventBusName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_event_bus
           (Values.CreateEventBusRequest.make ?eventSourceName
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.CreateEventBusResponse.to_json)
           (Some Values.CreateEventBusResponse.error_to_json)])
let create_partner_event_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING EventSourceName"
       and account = flag "account" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_partner_event_source
           (Values.CreatePartnerEventSourceRequest.make ~name ~account ())
           (Some Values.CreatePartnerEventSourceResponse.to_json)
           (Some Values.CreatePartnerEventSourceResponse.error_to_json)])
let deactivate_event_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING EventSourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deactivate_event_source
           (Values.DeactivateEventSourceRequest.make ~name ()) None None])
let deauthorize_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ConnectionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deauthorize_connection
           (Values.DeauthorizeConnectionRequest.make ~name ())
           (Some Values.DeauthorizeConnectionResponse.to_json)
           (Some Values.DeauthorizeConnectionResponse.error_to_json)])
let delete_api_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING ApiDestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_api_destination
           (Values.DeleteApiDestinationRequest.make ~name ())
           (Some Values.DeleteApiDestinationResponse.to_json)
           (Some Values.DeleteApiDestinationResponse.error_to_json)])
let delete_archive =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and archiveName =
         flag "archive-name" (required string) ~doc:"STRING ArchiveName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_archive
           (Values.DeleteArchiveRequest.make ~archiveName ())
           (Some Values.DeleteArchiveResponse.to_json)
           (Some Values.DeleteArchiveResponse.error_to_json)])
let delete_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ConnectionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connection
           (Values.DeleteConnectionRequest.make ~name ())
           (Some Values.DeleteConnectionResponse.to_json)
           (Some Values.DeleteConnectionResponse.error_to_json)])
let delete_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING EndpointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_endpoint (Values.DeleteEndpointRequest.make ~name ())
           (Some Values.DeleteEndpointResponse.to_json)
           (Some Values.DeleteEndpointResponse.error_to_json)])
let delete_event_bus =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING EventBusName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event_bus (Values.DeleteEventBusRequest.make ~name ())
           None None])
let delete_partner_event_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING EventSourceName"
       and account = flag "account" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_partner_event_source
           (Values.DeletePartnerEventSourceRequest.make ~name ~account ())
           None None])
let delete_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventBusName =
         flag "event-bus-name" (optional string)
           ~doc:"STRING EventBusNameOrArn"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and name = flag "name" (required string) ~doc:"STRING RuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_rule
           (Values.DeleteRuleRequest.make ?eventBusName ?force ~name ()) None
           None])
let describe_api_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING ApiDestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_api_destination
           (Values.DescribeApiDestinationRequest.make ~name ())
           (Some Values.DescribeApiDestinationResponse.to_json)
           (Some Values.DescribeApiDestinationResponse.error_to_json)])
let describe_archive =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and archiveName =
         flag "archive-name" (required string) ~doc:"STRING ArchiveName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_archive
           (Values.DescribeArchiveRequest.make ~archiveName ())
           (Some Values.DescribeArchiveResponse.to_json)
           (Some Values.DescribeArchiveResponse.error_to_json)])
let describe_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ConnectionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_connection
           (Values.DescribeConnectionRequest.make ~name ())
           (Some Values.DescribeConnectionResponse.to_json)
           (Some Values.DescribeConnectionResponse.error_to_json)])
let describe_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and homeRegion =
         flag "home-region" (optional string) ~doc:"STRING HomeRegion"
       and name = flag "name" (required string) ~doc:"STRING EndpointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoint
           (Values.DescribeEndpointRequest.make ?homeRegion ~name ())
           (Some Values.DescribeEndpointResponse.to_json)
           (Some Values.DescribeEndpointResponse.error_to_json)])
let describe_event_bus =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING EventBusNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_bus
           (Values.DescribeEventBusRequest.make ?name ())
           (Some Values.DescribeEventBusResponse.to_json)
           (Some Values.DescribeEventBusResponse.error_to_json)])
let describe_event_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING EventSourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_source
           (Values.DescribeEventSourceRequest.make ~name ())
           (Some Values.DescribeEventSourceResponse.to_json)
           (Some Values.DescribeEventSourceResponse.error_to_json)])
let describe_partner_event_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING EventSourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_partner_event_source
           (Values.DescribePartnerEventSourceRequest.make ~name ())
           (Some Values.DescribePartnerEventSourceResponse.to_json)
           (Some Values.DescribePartnerEventSourceResponse.error_to_json)])
let describe_replay =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replayName =
         flag "replay-name" (required string) ~doc:"STRING ReplayName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_replay
           (Values.DescribeReplayRequest.make ~replayName ())
           (Some Values.DescribeReplayResponse.to_json)
           (Some Values.DescribeReplayResponse.error_to_json)])
let describe_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventBusName =
         flag "event-bus-name" (optional string)
           ~doc:"STRING EventBusNameOrArn"
       and name = flag "name" (required string) ~doc:"STRING RuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_rule
           (Values.DescribeRuleRequest.make ?eventBusName ~name ())
           (Some Values.DescribeRuleResponse.to_json)
           (Some Values.DescribeRuleResponse.error_to_json)])
let disable_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventBusName =
         flag "event-bus-name" (optional string)
           ~doc:"STRING EventBusNameOrArn"
       and name = flag "name" (required string) ~doc:"STRING RuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_rule
           (Values.DisableRuleRequest.make ?eventBusName ~name ()) None None])
let enable_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventBusName =
         flag "event-bus-name" (optional string)
           ~doc:"STRING EventBusNameOrArn"
       and name = flag "name" (required string) ~doc:"STRING RuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_rule
           (Values.EnableRuleRequest.make ?eventBusName ~name ()) None None])
let list_api_destinations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namePrefix =
         flag "name-prefix" (optional string)
           ~doc:"STRING ApiDestinationName"
       and connectionArn =
         flag "connection-arn" (optional string) ~doc:"STRING ConnectionArn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT LimitMax100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_api_destinations
           (Values.ListApiDestinationsRequest.make ?namePrefix ?connectionArn
              ?nextToken ?limit ())
           (Some Values.ListApiDestinationsResponse.to_json)
           (Some Values.ListApiDestinationsResponse.error_to_json)])
let list_archives =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namePrefix =
         flag "name-prefix" (optional string) ~doc:"STRING ArchiveName"
       and eventSourceArn =
         flag "event-source-arn" (optional string) ~doc:"STRING Arn"
       and state = flag "state" (optional json_arg) ~doc:"JSON ArchiveState"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT LimitMax100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_archives
           (Values.ListArchivesRequest.make ?namePrefix ?eventSourceArn
              ?state:(Option.map ~f:Values.ArchiveState.of_json state)
              ?nextToken ?limit ())
           (Some Values.ListArchivesResponse.to_json)
           (Some Values.ListArchivesResponse.error_to_json)])
let list_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namePrefix =
         flag "name-prefix" (optional string) ~doc:"STRING ConnectionName"
       and connectionState =
         flag "connection-state" (optional json_arg)
           ~doc:"JSON ConnectionState"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT LimitMax100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connections
           (Values.ListConnectionsRequest.make ?namePrefix
              ?connectionState:(Option.map ~f:Values.ConnectionState.of_json
                                  connectionState) ?nextToken ?limit ())
           (Some Values.ListConnectionsResponse.to_json)
           (Some Values.ListConnectionsResponse.error_to_json)])
let list_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namePrefix =
         flag "name-prefix" (optional string) ~doc:"STRING EndpointName"
       and homeRegion =
         flag "home-region" (optional string) ~doc:"STRING HomeRegion"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT LimitMax100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_endpoints
           (Values.ListEndpointsRequest.make ?namePrefix ?homeRegion
              ?nextToken ?maxResults ())
           (Some Values.ListEndpointsResponse.to_json)
           (Some Values.ListEndpointsResponse.error_to_json)])
let list_event_buses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namePrefix =
         flag "name-prefix" (optional string) ~doc:"STRING EventBusName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT LimitMax100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_event_buses
           (Values.ListEventBusesRequest.make ?namePrefix ?nextToken ?limit
              ()) (Some Values.ListEventBusesResponse.to_json)
           (Some Values.ListEventBusesResponse.error_to_json)])
let list_event_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namePrefix =
         flag "name-prefix" (optional string)
           ~doc:"STRING EventSourceNamePrefix"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT LimitMax100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_event_sources
           (Values.ListEventSourcesRequest.make ?namePrefix ?nextToken ?limit
              ()) (Some Values.ListEventSourcesResponse.to_json)
           (Some Values.ListEventSourcesResponse.error_to_json)])
let list_partner_event_source_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT LimitMax100"
       and eventSourceName =
         flag "event-source-name" (required string)
           ~doc:"STRING EventSourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_partner_event_source_accounts
           (Values.ListPartnerEventSourceAccountsRequest.make ?nextToken
              ?limit ~eventSourceName ())
           (Some Values.ListPartnerEventSourceAccountsResponse.to_json)
           (Some Values.ListPartnerEventSourceAccountsResponse.error_to_json)])
let list_partner_event_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT LimitMax100"
       and namePrefix =
         flag "name-prefix" (required string)
           ~doc:"STRING PartnerEventSourceNamePrefix" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_partner_event_sources
           (Values.ListPartnerEventSourcesRequest.make ?nextToken ?limit
              ~namePrefix ())
           (Some Values.ListPartnerEventSourcesResponse.to_json)
           (Some Values.ListPartnerEventSourcesResponse.error_to_json)])
let list_replays =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namePrefix =
         flag "name-prefix" (optional string) ~doc:"STRING ReplayName"
       and state = flag "state" (optional json_arg) ~doc:"JSON ReplayState"
       and eventSourceArn =
         flag "event-source-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT LimitMax100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_replays
           (Values.ListReplaysRequest.make ?namePrefix
              ?state:(Option.map ~f:Values.ReplayState.of_json state)
              ?eventSourceArn ?nextToken ?limit ())
           (Some Values.ListReplaysResponse.to_json)
           (Some Values.ListReplaysResponse.error_to_json)])
let list_rule_names_by_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventBusName =
         flag "event-bus-name" (optional string)
           ~doc:"STRING EventBusNameOrArn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT LimitMax100"
       and targetArn =
         flag "target-arn" (required string) ~doc:"STRING TargetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rule_names_by_target
           (Values.ListRuleNamesByTargetRequest.make ?eventBusName ?nextToken
              ?limit ~targetArn ())
           (Some Values.ListRuleNamesByTargetResponse.to_json)
           (Some Values.ListRuleNamesByTargetResponse.error_to_json)])
let list_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namePrefix =
         flag "name-prefix" (optional string) ~doc:"STRING RuleName"
       and eventBusName =
         flag "event-bus-name" (optional string)
           ~doc:"STRING EventBusNameOrArn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT LimitMax100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rules
           (Values.ListRulesRequest.make ?namePrefix ?eventBusName ?nextToken
              ?limit ()) (Some Values.ListRulesResponse.to_json)
           (Some Values.ListRulesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_targets_by_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventBusName =
         flag "event-bus-name" (optional string)
           ~doc:"STRING EventBusNameOrArn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT LimitMax100"
       and rule = flag "rule" (required string) ~doc:"STRING RuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_targets_by_rule
           (Values.ListTargetsByRuleRequest.make ?eventBusName ?nextToken
              ?limit ~rule ())
           (Some Values.ListTargetsByRuleResponse.to_json)
           (Some Values.ListTargetsByRuleResponse.error_to_json)])
let put_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointId =
         flag "endpoint-id" (optional string) ~doc:"STRING EndpointId"
       and entries =
         flag "entries" (required json_arg)
           ~doc:"JSON PutEventsRequestEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_events
           (Values.PutEventsRequest.make ?endpointId
              ~entries:(Values.PutEventsRequestEntryList.of_json entries) ())
           (Some Values.PutEventsResponse.to_json)
           (Some Values.PutEventsResponse.error_to_json)])
let put_partner_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entries =
         flag "entries" (required json_arg)
           ~doc:"JSON PutPartnerEventsRequestEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_partner_events
           (Values.PutPartnerEventsRequest.make
              ~entries:(Values.PutPartnerEventsRequestEntryList.of_json
                          entries) ())
           (Some Values.PutPartnerEventsResponse.to_json)
           (Some Values.PutPartnerEventsResponse.error_to_json)])
let put_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventBusName =
         flag "event-bus-name" (optional string)
           ~doc:"STRING NonPartnerEventBusName"
       and action = flag "action" (optional string) ~doc:"STRING Action"
       and principal =
         flag "principal" (optional string) ~doc:"STRING Principal"
       and statementId =
         flag "statement-id" (optional string) ~doc:"STRING StatementId"
       and condition =
         flag "condition" (optional json_arg) ~doc:"JSON Condition"
       and policy = flag "policy" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_permission
           (Values.PutPermissionRequest.make ?eventBusName ?action ?principal
              ?statementId
              ?condition:(Option.map ~f:Values.Condition.of_json condition)
              ?policy ()) None None])
let put_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduleExpression =
         flag "schedule-expression" (optional string)
           ~doc:"STRING ScheduleExpression"
       and eventPattern =
         flag "event-pattern" (optional string) ~doc:"STRING EventPattern"
       and state = flag "state" (optional json_arg) ~doc:"JSON RuleState"
       and description =
         flag "description" (optional string) ~doc:"STRING RuleDescription"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and eventBusName =
         flag "event-bus-name" (optional string)
           ~doc:"STRING EventBusNameOrArn"
       and name = flag "name" (required string) ~doc:"STRING RuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_rule
           (Values.PutRuleRequest.make ?scheduleExpression ?eventPattern
              ?state:(Option.map ~f:Values.RuleState.of_json state)
              ?description ?roleArn
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?eventBusName
              ~name ()) (Some Values.PutRuleResponse.to_json)
           (Some Values.PutRuleResponse.error_to_json)])
let put_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventBusName =
         flag "event-bus-name" (optional string)
           ~doc:"STRING EventBusNameOrArn"
       and rule = flag "rule" (required string) ~doc:"STRING RuleName"
       and targets =
         flag "targets" (required json_arg) ~doc:"JSON TargetList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_targets
           (Values.PutTargetsRequest.make ?eventBusName ~rule
              ~targets:(Values.TargetList.of_json targets) ())
           (Some Values.PutTargetsResponse.to_json)
           (Some Values.PutTargetsResponse.error_to_json)])
let remove_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statementId =
         flag "statement-id" (optional string) ~doc:"STRING StatementId"
       and removeAllPermissions =
         flag "remove-all-permissions" (optional bool) ~doc:"BOOL Boolean"
       and eventBusName =
         flag "event-bus-name" (optional string)
           ~doc:"STRING NonPartnerEventBusName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_permission
           (Values.RemovePermissionRequest.make ?statementId
              ?removeAllPermissions ?eventBusName ()) None None])
let remove_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventBusName =
         flag "event-bus-name" (optional string)
           ~doc:"STRING EventBusNameOrArn"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and rule = flag "rule" (required string) ~doc:"STRING RuleName"
       and ids = flag "ids" (required json_arg) ~doc:"JSON TargetIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_targets
           (Values.RemoveTargetsRequest.make ?eventBusName ?force ~rule
              ~ids:(Values.TargetIdList.of_json ids) ())
           (Some Values.RemoveTargetsResponse.to_json)
           (Some Values.RemoveTargetsResponse.error_to_json)])
let start_replay =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING ReplayDescription"
       and replayName =
         flag "replay-name" (required string) ~doc:"STRING ReplayName"
       and eventSourceArn =
         flag "event-source-arn" (required string) ~doc:"STRING Arn"
       and eventStartTime =
         flag "event-start-time" (required json_arg) ~doc:"JSON Timestamp"
       and eventEndTime =
         flag "event-end-time" (required json_arg) ~doc:"JSON Timestamp"
       and destination =
         flag "destination" (required json_arg) ~doc:"JSON ReplayDestination" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_replay
           (Values.StartReplayRequest.make ?description ~replayName
              ~eventSourceArn
              ~eventStartTime:(Values.Timestamp.of_json eventStartTime)
              ~eventEndTime:(Values.Timestamp.of_json eventEndTime)
              ~destination:(Values.ReplayDestination.of_json destination) ())
           (Some Values.StartReplayResponse.to_json)
           (Some Values.StartReplayResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let test_event_pattern =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventPattern =
         flag "event-pattern" (required string) ~doc:"STRING EventPattern"
       and event = flag "event" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_event_pattern
           (Values.TestEventPatternRequest.make ~eventPattern ~event ())
           (Some Values.TestEventPatternResponse.to_json)
           (Some Values.TestEventPatternResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_api_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ApiDestinationDescription"
       and connectionArn =
         flag "connection-arn" (optional string) ~doc:"STRING ConnectionArn"
       and invocationEndpoint =
         flag "invocation-endpoint" (optional string)
           ~doc:"STRING HttpsEndpoint"
       and httpMethod =
         flag "http-method" (optional json_arg)
           ~doc:"JSON ApiDestinationHttpMethod"
       and invocationRateLimitPerSecond =
         flag "invocation-rate-limit-per-second" (optional int)
           ~doc:"INT ApiDestinationInvocationRateLimitPerSecond"
       and name =
         flag "name" (required string) ~doc:"STRING ApiDestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_api_destination
           (Values.UpdateApiDestinationRequest.make ?description
              ?connectionArn ?invocationEndpoint
              ?httpMethod:(Option.map
                             ~f:Values.ApiDestinationHttpMethod.of_json
                             httpMethod) ?invocationRateLimitPerSecond ~name
              ()) (Some Values.UpdateApiDestinationResponse.to_json)
           (Some Values.UpdateApiDestinationResponse.error_to_json)])
let update_archive =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ArchiveDescription"
       and eventPattern =
         flag "event-pattern" (optional string) ~doc:"STRING EventPattern"
       and retentionDays =
         flag "retention-days" (optional int) ~doc:"INT RetentionDays"
       and archiveName =
         flag "archive-name" (required string) ~doc:"STRING ArchiveName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_archive
           (Values.UpdateArchiveRequest.make ?description ?eventPattern
              ?retentionDays ~archiveName ())
           (Some Values.UpdateArchiveResponse.to_json)
           (Some Values.UpdateArchiveResponse.error_to_json)])
let update_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ConnectionDescription"
       and authorizationType =
         flag "authorization-type" (optional json_arg)
           ~doc:"JSON ConnectionAuthorizationType"
       and authParameters =
         flag "auth-parameters" (optional json_arg)
           ~doc:"JSON UpdateConnectionAuthRequestParameters"
       and name = flag "name" (required string) ~doc:"STRING ConnectionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connection
           (Values.UpdateConnectionRequest.make ?description
              ?authorizationType:(Option.map
                                    ~f:Values.ConnectionAuthorizationType.of_json
                                    authorizationType)
              ?authParameters:(Option.map
                                 ~f:Values.UpdateConnectionAuthRequestParameters.of_json
                                 authParameters) ~name ())
           (Some Values.UpdateConnectionResponse.to_json)
           (Some Values.UpdateConnectionResponse.error_to_json)])
let update_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING EndpointDescription"
       and routingConfig =
         flag "routing-config" (optional json_arg) ~doc:"JSON RoutingConfig"
       and replicationConfig =
         flag "replication-config" (optional json_arg)
           ~doc:"JSON ReplicationConfig"
       and eventBuses =
         flag "event-buses" (optional json_arg)
           ~doc:"JSON EndpointEventBusList"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and name = flag "name" (required string) ~doc:"STRING EndpointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_endpoint
           (Values.UpdateEndpointRequest.make ?description
              ?routingConfig:(Option.map ~f:Values.RoutingConfig.of_json
                                routingConfig)
              ?replicationConfig:(Option.map
                                    ~f:Values.ReplicationConfig.of_json
                                    replicationConfig)
              ?eventBuses:(Option.map ~f:Values.EndpointEventBusList.of_json
                             eventBuses) ?roleArn ~name ())
           (Some Values.UpdateEndpointResponse.to_json)
           (Some Values.UpdateEndpointResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("activate-event-source", activate_event_source);
    ("cancel-replay", cancel_replay);
    ("create-api-destination", create_api_destination);
    ("create-archive", create_archive);
    ("create-connection", create_connection);
    ("create-endpoint", create_endpoint);
    ("create-event-bus", create_event_bus);
    ("create-partner-event-source", create_partner_event_source);
    ("deactivate-event-source", deactivate_event_source);
    ("deauthorize-connection", deauthorize_connection);
    ("delete-api-destination", delete_api_destination);
    ("delete-archive", delete_archive);
    ("delete-connection", delete_connection);
    ("delete-endpoint", delete_endpoint);
    ("delete-event-bus", delete_event_bus);
    ("delete-partner-event-source", delete_partner_event_source);
    ("delete-rule", delete_rule);
    ("describe-api-destination", describe_api_destination);
    ("describe-archive", describe_archive);
    ("describe-connection", describe_connection);
    ("describe-endpoint", describe_endpoint);
    ("describe-event-bus", describe_event_bus);
    ("describe-event-source", describe_event_source);
    ("describe-partner-event-source", describe_partner_event_source);
    ("describe-replay", describe_replay);
    ("describe-rule", describe_rule);
    ("disable-rule", disable_rule);
    ("enable-rule", enable_rule);
    ("list-api-destinations", list_api_destinations);
    ("list-archives", list_archives);
    ("list-connections", list_connections);
    ("list-endpoints", list_endpoints);
    ("list-event-buses", list_event_buses);
    ("list-event-sources", list_event_sources);
    ("list-partner-event-source-accounts",
      list_partner_event_source_accounts);
    ("list-partner-event-sources", list_partner_event_sources);
    ("list-replays", list_replays);
    ("list-rule-names-by-target", list_rule_names_by_target);
    ("list-rules", list_rules);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-targets-by-rule", list_targets_by_rule);
    ("put-events", put_events);
    ("put-partner-events", put_partner_events);
    ("put-permission", put_permission);
    ("put-rule", put_rule);
    ("put-targets", put_targets);
    ("remove-permission", remove_permission);
    ("remove-targets", remove_targets);
    ("start-replay", start_replay);
    ("tag-resource", tag_resource);
    ("test-event-pattern", test_event_pattern);
    ("untag-resource", untag_resource);
    ("update-api-destination", update_api_destination);
    ("update-archive", update_archive);
    ("update-connection", update_connection);
    ("update-endpoint", update_endpoint)]
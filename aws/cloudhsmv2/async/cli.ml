(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let copy_backup_to_region =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagList = flag "tag-list" (optional json_arg) ~doc:"JSON TagList"
       and destinationRegion =
         flag "destination-region" (required string) ~doc:"STRING Region"
       and backupId =
         flag "backup-id" (required string) ~doc:"STRING BackupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_backup_to_region
           (Values.CopyBackupToRegionRequest.make
              ?tagList:(Option.map ~f:Values.TagList.of_json tagList)
              ~destinationRegion ~backupId ())
           (Some Values.CopyBackupToRegionResponse.to_json)
           (Some Values.CopyBackupToRegionResponse.error_to_json)])
let create_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backupRetentionPolicy =
         flag "backup-retention-policy" (optional json_arg)
           ~doc:"JSON BackupRetentionPolicy"
       and sourceBackupId =
         flag "source-backup-id" (optional string) ~doc:"STRING BackupArn"
       and networkType =
         flag "network-type" (optional json_arg) ~doc:"JSON NetworkType"
       and tagList = flag "tag-list" (optional json_arg) ~doc:"JSON TagList"
       and mode = flag "mode" (optional json_arg) ~doc:"JSON ClusterMode"
       and hsmType = flag "hsm-type" (required string) ~doc:"STRING HsmType"
       and subnetIds =
         flag "subnet-ids" (required json_arg) ~doc:"JSON SubnetIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster
           (Values.CreateClusterRequest.make
              ?backupRetentionPolicy:(Option.map
                                        ~f:Values.BackupRetentionPolicy.of_json
                                        backupRetentionPolicy)
              ?sourceBackupId
              ?networkType:(Option.map ~f:Values.NetworkType.of_json
                              networkType)
              ?tagList:(Option.map ~f:Values.TagList.of_json tagList)
              ?mode:(Option.map ~f:Values.ClusterMode.of_json mode) ~hsmType
              ~subnetIds:(Values.SubnetIds.of_json subnetIds) ())
           (Some Values.CreateClusterResponse.to_json)
           (Some Values.CreateClusterResponse.error_to_json)])
let create_hsm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ipAddress =
         flag "ip-address" (optional string) ~doc:"STRING IpAddress"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING ClusterId"
       and availabilityZone =
         flag "availability-zone" (required string) ~doc:"STRING ExternalAz" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_hsm
           (Values.CreateHsmRequest.make ?ipAddress ~clusterId
              ~availabilityZone ()) (Some Values.CreateHsmResponse.to_json)
           (Some Values.CreateHsmResponse.error_to_json)])
let delete_backup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backupId =
         flag "backup-id" (required string) ~doc:"STRING BackupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_backup (Values.DeleteBackupRequest.make ~backupId ())
           (Some Values.DeleteBackupResponse.to_json)
           (Some Values.DeleteBackupResponse.error_to_json)])
let delete_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster (Values.DeleteClusterRequest.make ~clusterId ())
           (Some Values.DeleteClusterResponse.to_json)
           (Some Values.DeleteClusterResponse.error_to_json)])
let delete_hsm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hsmId = flag "hsm-id" (optional string) ~doc:"STRING HsmId"
       and eniId = flag "eni-id" (optional string) ~doc:"STRING EniId"
       and eniIp = flag "eni-ip" (optional string) ~doc:"STRING IpAddress"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_hsm
           (Values.DeleteHsmRequest.make ?hsmId ?eniId ?eniIp ~clusterId ())
           (Some Values.DeleteHsmResponse.to_json)
           (Some Values.DeleteHsmResponse.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING CloudHsmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyRequest.make ?resourceArn ())
           (Some Values.DeleteResourcePolicyResponse.to_json)
           (Some Values.DeleteResourcePolicyResponse.error_to_json)])
let describe_backups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BackupsMaxSize"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and shared = flag "shared" (optional bool) ~doc:"BOOL Boolean"
       and sortAscending =
         flag "sort-ascending" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_backups
           (Values.DescribeBackupsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ?shared
              ?sortAscending ())
           (Some Values.DescribeBackupsResponse.to_json)
           (Some Values.DescribeBackupsResponse.error_to_json)])
let describe_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ClustersMaxSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_clusters
           (Values.DescribeClustersRequest.make
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.DescribeClustersResponse.to_json)
           (Some Values.DescribeClustersResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING CloudHsmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ?resourceArn ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let initialize_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING ClusterId"
       and signedCert =
         flag "signed-cert" (required string) ~doc:"STRING Cert"
       and trustAnchor =
         flag "trust-anchor" (required string) ~doc:"STRING Cert" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.initialize_cluster
           (Values.InitializeClusterRequest.make ~clusterId ~signedCert
              ~trustAnchor ())
           (Some Values.InitializeClusterResponse.to_json)
           (Some Values.InitializeClusterResponse.error_to_json)])
let list_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT MaxSize"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags
           (Values.ListTagsRequest.make ?nextToken ?maxResults ~resourceId ())
           (Some Values.ListTagsResponse.to_json)
           (Some Values.ListTagsResponse.error_to_json)])
let modify_backup_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backupId =
         flag "backup-id" (required string) ~doc:"STRING BackupId"
       and neverExpires =
         flag "never-expires" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_backup_attributes
           (Values.ModifyBackupAttributesRequest.make ~backupId ~neverExpires
              ()) (Some Values.ModifyBackupAttributesResponse.to_json)
           (Some Values.ModifyBackupAttributesResponse.error_to_json)])
let modify_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hsmType = flag "hsm-type" (optional string) ~doc:"STRING HsmType"
       and backupRetentionPolicy =
         flag "backup-retention-policy" (optional json_arg)
           ~doc:"JSON BackupRetentionPolicy"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_cluster
           (Values.ModifyClusterRequest.make ?hsmType
              ?backupRetentionPolicy:(Option.map
                                        ~f:Values.BackupRetentionPolicy.of_json
                                        backupRetentionPolicy) ~clusterId ())
           (Some Values.ModifyClusterResponse.to_json)
           (Some Values.ModifyClusterResponse.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING CloudHsmArn"
       and policy =
         flag "policy" (optional string) ~doc:"STRING ResourcePolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyRequest.make ?resourceArn ?policy ())
           (Some Values.PutResourcePolicyResponse.to_json)
           (Some Values.PutResourcePolicyResponse.error_to_json)])
let restore_backup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backupId =
         flag "backup-id" (required string) ~doc:"STRING BackupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_backup (Values.RestoreBackupRequest.make ~backupId ())
           (Some Values.RestoreBackupResponse.to_json)
           (Some Values.RestoreBackupResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and tagList = flag "tag-list" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceId
              ~tagList:(Values.TagList.of_json tagList) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and tagKeyList =
         flag "tag-key-list" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceId
              ~tagKeyList:(Values.TagKeyList.of_json tagKeyList) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("copy-backup-to-region", copy_backup_to_region);
    ("create-cluster", create_cluster);
    ("create-hsm", create_hsm);
    ("delete-backup", delete_backup);
    ("delete-cluster", delete_cluster);
    ("delete-hsm", delete_hsm);
    ("delete-resource-policy", delete_resource_policy);
    ("describe-backups", describe_backups);
    ("describe-clusters", describe_clusters);
    ("get-resource-policy", get_resource_policy);
    ("initialize-cluster", initialize_cluster);
    ("list-tags", list_tags);
    ("modify-backup-attributes", modify_backup_attributes);
    ("modify-cluster", modify_cluster);
    ("put-resource-policy", put_resource_policy);
    ("restore-backup", restore_backup);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_admin_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and adminAccount =
         flag "admin-account" (required string) ~doc:"STRING AWSAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_admin_account
           (Values.AssociateAdminAccountRequest.make ~adminAccount ()) None
           None])
let associate_third_party_firewall =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thirdPartyFirewall =
         flag "third-party-firewall" (required json_arg)
           ~doc:"JSON ThirdPartyFirewall" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_third_party_firewall
           (Values.AssociateThirdPartyFirewallRequest.make
              ~thirdPartyFirewall:(Values.ThirdPartyFirewall.of_json
                                     thirdPartyFirewall) ())
           (Some Values.AssociateThirdPartyFirewallResponse.to_json)
           (Some Values.AssociateThirdPartyFirewallResponse.error_to_json)])
let delete_apps_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listId = flag "list-id" (required string) ~doc:"STRING ListId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_apps_list (Values.DeleteAppsListRequest.make ~listId ())
           None None])
let delete_notification_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_notification_channel
           (Values.DeleteNotificationChannelRequest.make ()) None None])
let delete_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deleteAllPolicyResources =
         flag "delete-all-policy-resources" (optional bool)
           ~doc:"BOOL Boolean"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy
           (Values.DeletePolicyRequest.make ?deleteAllPolicyResources
              ~policyId ()) None None])
let delete_protocols_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listId = flag "list-id" (required string) ~doc:"STRING ListId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_protocols_list
           (Values.DeleteProtocolsListRequest.make ~listId ()) None None])
let disassociate_admin_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_admin_account
           (Values.DisassociateAdminAccountRequest.make ()) None None])
let disassociate_third_party_firewall =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thirdPartyFirewall =
         flag "third-party-firewall" (required json_arg)
           ~doc:"JSON ThirdPartyFirewall" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_third_party_firewall
           (Values.DisassociateThirdPartyFirewallRequest.make
              ~thirdPartyFirewall:(Values.ThirdPartyFirewall.of_json
                                     thirdPartyFirewall) ())
           (Some Values.DisassociateThirdPartyFirewallResponse.to_json)
           (Some Values.DisassociateThirdPartyFirewallResponse.error_to_json)])
let get_admin_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_admin_account (Values.GetAdminAccountRequest.make ())
           (Some Values.GetAdminAccountResponse.to_json)
           (Some Values.GetAdminAccountResponse.error_to_json)])
let get_apps_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and defaultList =
         flag "default-list" (optional bool) ~doc:"BOOL Boolean"
       and listId = flag "list-id" (required string) ~doc:"STRING ListId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_apps_list
           (Values.GetAppsListRequest.make ?defaultList ~listId ())
           (Some Values.GetAppsListResponse.to_json)
           (Some Values.GetAppsListResponse.error_to_json)])
let get_compliance_detail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId"
       and memberAccount =
         flag "member-account" (required string) ~doc:"STRING AWSAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_compliance_detail
           (Values.GetComplianceDetailRequest.make ~policyId ~memberAccount
              ()) (Some Values.GetComplianceDetailResponse.to_json)
           (Some Values.GetComplianceDetailResponse.error_to_json)])
let get_notification_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_notification_channel
           (Values.GetNotificationChannelRequest.make ())
           (Some Values.GetNotificationChannelResponse.to_json)
           (Some Values.GetNotificationChannelResponse.error_to_json)])
let get_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy (Values.GetPolicyRequest.make ~policyId ())
           (Some Values.GetPolicyResponse.to_json)
           (Some Values.GetPolicyResponse.error_to_json)])
let get_protection_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberAccountId =
         flag "member-account-id" (optional string)
           ~doc:"STRING AWSAccountId"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TimeStamp"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON TimeStamp"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PaginationMaxResults"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_protection_status
           (Values.GetProtectionStatusRequest.make ?memberAccountId
              ?startTime:(Option.map ~f:Values.TimeStamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.TimeStamp.of_json endTime)
              ?nextToken ?maxResults ~policyId ())
           (Some Values.GetProtectionStatusResponse.to_json)
           (Some Values.GetProtectionStatusResponse.error_to_json)])
let get_protocols_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and defaultList =
         flag "default-list" (optional bool) ~doc:"BOOL Boolean"
       and listId = flag "list-id" (required string) ~doc:"STRING ListId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_protocols_list
           (Values.GetProtocolsListRequest.make ?defaultList ~listId ())
           (Some Values.GetProtocolsListResponse.to_json)
           (Some Values.GetProtocolsListResponse.error_to_json)])
let get_third_party_firewall_association_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thirdPartyFirewall =
         flag "third-party-firewall" (required json_arg)
           ~doc:"JSON ThirdPartyFirewall" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_third_party_firewall_association_status
           (Values.GetThirdPartyFirewallAssociationStatusRequest.make
              ~thirdPartyFirewall:(Values.ThirdPartyFirewall.of_json
                                     thirdPartyFirewall) ())
           (Some
              Values.GetThirdPartyFirewallAssociationStatusResponse.to_json)
           (Some
              Values.GetThirdPartyFirewallAssociationStatusResponse.error_to_json)])
let get_violation_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId"
       and memberAccount =
         flag "member-account" (required string) ~doc:"STRING AWSAccountId"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and resourceType =
         flag "resource-type" (required string) ~doc:"STRING ResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_violation_details
           (Values.GetViolationDetailsRequest.make ~policyId ~memberAccount
              ~resourceId ~resourceType ())
           (Some Values.GetViolationDetailsResponse.to_json)
           (Some Values.GetViolationDetailsResponse.error_to_json)])
let list_apps_lists =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and defaultLists =
         flag "default-lists" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (required int) ~doc:"INT PaginationMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_apps_lists
           (Values.ListAppsListsRequest.make ?defaultLists ?nextToken
              ~maxResults ()) (Some Values.ListAppsListsResponse.to_json)
           (Some Values.ListAppsListsResponse.error_to_json)])
let list_compliance_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PaginationMaxResults"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_compliance_status
           (Values.ListComplianceStatusRequest.make ?nextToken ?maxResults
              ~policyId ())
           (Some Values.ListComplianceStatusResponse.to_json)
           (Some Values.ListComplianceStatusResponse.error_to_json)])
let list_member_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PaginationMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_member_accounts
           (Values.ListMemberAccountsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListMemberAccountsResponse.to_json)
           (Some Values.ListMemberAccountsResponse.error_to_json)])
let list_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PaginationMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policies
           (Values.ListPoliciesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListPoliciesResponse.to_json)
           (Some Values.ListPoliciesResponse.error_to_json)])
let list_protocols_lists =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and defaultLists =
         flag "default-lists" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (required int) ~doc:"INT PaginationMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_protocols_lists
           (Values.ListProtocolsListsRequest.make ?defaultLists ?nextToken
              ~maxResults ())
           (Some Values.ListProtocolsListsResponse.to_json)
           (Some Values.ListProtocolsListsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_third_party_firewall_firewall_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and thirdPartyFirewall =
         flag "third-party-firewall" (required json_arg)
           ~doc:"JSON ThirdPartyFirewall"
       and maxResults =
         flag "max-results" (required int) ~doc:"INT PaginationMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_third_party_firewall_firewall_policies
           (Values.ListThirdPartyFirewallFirewallPoliciesRequest.make
              ?nextToken
              ~thirdPartyFirewall:(Values.ThirdPartyFirewall.of_json
                                     thirdPartyFirewall) ~maxResults ())
           (Some
              Values.ListThirdPartyFirewallFirewallPoliciesResponse.to_json)
           (Some
              Values.ListThirdPartyFirewallFirewallPoliciesResponse.error_to_json)])
let put_apps_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagList = flag "tag-list" (optional json_arg) ~doc:"JSON TagList"
       and appsList =
         flag "apps-list" (required json_arg) ~doc:"JSON AppsListData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_apps_list
           (Values.PutAppsListRequest.make
              ?tagList:(Option.map ~f:Values.TagList.of_json tagList)
              ~appsList:(Values.AppsListData.of_json appsList) ())
           (Some Values.PutAppsListResponse.to_json)
           (Some Values.PutAppsListResponse.error_to_json)])
let put_notification_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snsTopicArn =
         flag "sns-topic-arn" (required string) ~doc:"STRING ResourceArn"
       and snsRoleName =
         flag "sns-role-name" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_notification_channel
           (Values.PutNotificationChannelRequest.make ~snsTopicArn
              ~snsRoleName ()) None None])
let put_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagList = flag "tag-list" (optional json_arg) ~doc:"JSON TagList"
       and policy = flag "policy" (required json_arg) ~doc:"JSON Policy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_policy
           (Values.PutPolicyRequest.make
              ?tagList:(Option.map ~f:Values.TagList.of_json tagList)
              ~policy:(Values.Policy.of_json policy) ())
           (Some Values.PutPolicyResponse.to_json)
           (Some Values.PutPolicyResponse.error_to_json)])
let put_protocols_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagList = flag "tag-list" (optional json_arg) ~doc:"JSON TagList"
       and protocolsList =
         flag "protocols-list" (required json_arg)
           ~doc:"JSON ProtocolsListData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_protocols_list
           (Values.PutProtocolsListRequest.make
              ?tagList:(Option.map ~f:Values.TagList.of_json tagList)
              ~protocolsList:(Values.ProtocolsListData.of_json protocolsList)
              ()) (Some Values.PutProtocolsListResponse.to_json)
           (Some Values.PutProtocolsListResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagList = flag "tag-list" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tagList:(Values.TagList.of_json tagList) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-admin-account", associate_admin_account);
    ("associate-third-party-firewall", associate_third_party_firewall);
    ("delete-apps-list", delete_apps_list);
    ("delete-notification-channel", delete_notification_channel);
    ("delete-policy", delete_policy);
    ("delete-protocols-list", delete_protocols_list);
    ("disassociate-admin-account", disassociate_admin_account);
    ("disassociate-third-party-firewall", disassociate_third_party_firewall);
    ("get-admin-account", get_admin_account);
    ("get-apps-list", get_apps_list);
    ("get-compliance-detail", get_compliance_detail);
    ("get-notification-channel", get_notification_channel);
    ("get-policy", get_policy);
    ("get-protection-status", get_protection_status);
    ("get-protocols-list", get_protocols_list);
    ("get-third-party-firewall-association-status",
      get_third_party_firewall_association_status);
    ("get-violation-details", get_violation_details);
    ("list-apps-lists", list_apps_lists);
    ("list-compliance-status", list_compliance_status);
    ("list-member-accounts", list_member_accounts);
    ("list-policies", list_policies);
    ("list-protocols-lists", list_protocols_lists);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-third-party-firewall-firewall-policies",
      list_third_party_firewall_firewall_policies);
    ("put-apps-list", put_apps_list);
    ("put-notification-channel", put_notification_channel);
    ("put-policy", put_policy);
    ("put-protocols-list", put_protocols_list);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
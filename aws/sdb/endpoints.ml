(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | BatchDeleteAttributes: (BatchDeleteAttributesRequest.t, unit, unit) t 
  | BatchPutAttributes: (BatchPutAttributesRequest.t, unit, unit) t 
  | CreateDomain: (CreateDomainRequest.t, unit, unit) t 
  | DeleteAttributes: (DeleteAttributesRequest.t, unit, unit) t 
  | DeleteDomain: (DeleteDomainRequest.t, unit, unit) t 
  | DomainMetadata: (DomainMetadataRequest.t, DomainMetadataResult.t,
  DomainMetadataResult.error) t 
  | GetAttributes: (GetAttributesRequest.t, GetAttributesResult.t,
  GetAttributesResult.error) t 
  | ListDomains: (ListDomainsRequest.t, ListDomainsResult.t,
  ListDomainsResult.error) t 
  | PutAttributes: (PutAttributesRequest.t, unit, unit) t 
  | Select: (SelectRequest.t, SelectResult.t, SelectResult.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | BatchDeleteAttributes -> `POST
  | BatchPutAttributes -> `POST
  | CreateDomain -> `POST
  | DeleteAttributes -> `POST
  | DeleteDomain -> `POST
  | DomainMetadata -> `POST
  | GetAttributes -> `POST
  | ListDomains -> `POST
  | PutAttributes -> `POST
  | Select -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | BatchDeleteAttributes -> (Format.kasprintf Uri.of_string) "/"
      | BatchPutAttributes -> (Format.kasprintf Uri.of_string) "/"
      | CreateDomain -> (Format.kasprintf Uri.of_string) "/"
      | DeleteAttributes -> (Format.kasprintf Uri.of_string) "/"
      | DeleteDomain -> (Format.kasprintf Uri.of_string) "/"
      | DomainMetadata -> (Format.kasprintf Uri.of_string) "/"
      | GetAttributes -> (Format.kasprintf Uri.of_string) "/"
      | ListDomains -> (Format.kasprintf Uri.of_string) "/"
      | PutAttributes -> (Format.kasprintf Uri.of_string) "/"
      | Select -> (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | BatchDeleteAttributes ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["BatchDeleteAttributes"]); ("Version", [apiVersion])] in
        let query =
          (BatchDeleteAttributesRequest.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | BatchPutAttributes ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["BatchPutAttributes"]); ("Version", [apiVersion])] in
        let query =
          (BatchPutAttributesRequest.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateDomain ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["CreateDomain"]); ("Version", [apiVersion])] in
        let query =
          (CreateDomainRequest.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeleteAttributes ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeleteAttributes"]); ("Version", [apiVersion])] in
        let query =
          (DeleteAttributesRequest.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeleteDomain ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["DeleteDomain"]); ("Version", [apiVersion])] in
        let query =
          (DeleteDomainRequest.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DomainMetadata ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DomainMetadata"]); ("Version", [apiVersion])] in
        let query =
          (DomainMetadataRequest.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | GetAttributes ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["GetAttributes"]); ("Version", [apiVersion])] in
        let query =
          (GetAttributesRequest.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ListDomains ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["ListDomains"]); ("Version", [apiVersion])] in
        let query =
          (ListDomainsRequest.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | PutAttributes ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["PutAttributes"]); ("Version", [apiVersion])] in
        let query =
          (PutAttributesRequest.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | Select ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["Select"]); ("Version", [apiVersion])] in
        let query = (SelectRequest.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let parse_aws_error error_of_xml =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = None }) in
    match (error_of_xml, ((code >= 400) && (code <= 599))) with
    | (None, _) | (_, false) -> bail ()
    | (Some error_of_xml, true) ->
        (match Awso.Xml.parse_response body with
         | `Data _ -> bail ()
         | `El (((_, "ErrorResponse"), _), _) as error_response_xml ->
             let error_xml = Awso.Xml.child_exn error_response_xml "Error" in
             (try
                let error_code =
                  match Awso.Xml.child_exn error_xml "Code" with
                  | `Data error_code -> error_code
                  | `El (_, children) ->
                      (List.map children
                         ~f:(function | `Data s -> s | `El _ -> ""))
                        |> (String.concat ~sep:"") in
                error_of_xml (String.strip error_code) error_xml
              with | Failure _ -> bail ())
         | `El _ -> bail ()) in
  let _ = parse_aws_error in
  let _ = resp in
  match endpoint with
  | BatchDeleteAttributes ->
      if is_success then Ok () else Error (parse_aws_error None)
  | BatchPutAttributes ->
      if is_success then Ok () else Error (parse_aws_error None)
  | CreateDomain ->
      if is_success then Ok () else Error (parse_aws_error None)
  | DeleteAttributes ->
      if is_success then Ok () else Error (parse_aws_error None)
  | DeleteDomain ->
      if is_success then Ok () else Error (parse_aws_error None)
  | DomainMetadata ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DomainMetadataResult.of_xml xml)
      else Error (parse_aws_error (Some DomainMetadataResult.error_of_xml))
  | GetAttributes ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (GetAttributesResult.of_xml xml)
      else Error (parse_aws_error (Some GetAttributesResult.error_of_xml))
  | ListDomains ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ListDomainsResult.of_xml xml)
      else Error (parse_aws_error (Some ListDomainsResult.error_of_xml))
  | PutAttributes ->
      if is_success then Ok () else Error (parse_aws_error None)
  | Select ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (SelectResult.of_xml xml)
      else Error (parse_aws_error (Some SelectResult.error_of_xml))
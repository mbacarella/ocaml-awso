(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_delete_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String"
       and items =
         flag "items" (required json_arg) ~doc:"JSON DeletableItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_attributes
           (Values.BatchDeleteAttributesRequest.make ~domainName
              ~items:(Values.DeletableItemList.of_json items) ()) None None])
let batch_put_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String"
       and items =
         flag "items" (required json_arg) ~doc:"JSON ReplaceableItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_put_attributes
           (Values.BatchPutAttributesRequest.make ~domainName
              ~items:(Values.ReplaceableItemList.of_json items) ()) None None])
let create_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain (Values.CreateDomainRequest.make ~domainName ())
           None None])
let delete_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON AttributeList"
       and expected =
         flag "expected" (optional json_arg) ~doc:"JSON UpdateCondition"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String"
       and itemName = flag "item-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_attributes
           (Values.DeleteAttributesRequest.make
              ?attributes:(Option.map ~f:Values.AttributeList.of_json
                             attributes)
              ?expected:(Option.map ~f:Values.UpdateCondition.of_json
                           expected) ~domainName ~itemName ()) None None])
let delete_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain (Values.DeleteDomainRequest.make ~domainName ())
           None None])
let domain_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.domain_metadata
           (Values.DomainMetadataRequest.make ~domainName ())
           (Some Values.DomainMetadataResult.to_json)
           (Some Values.DomainMetadataResult.error_to_json)])
let get_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeNames =
         flag "attribute-names" (optional json_arg)
           ~doc:"JSON AttributeNameList"
       and consistentRead =
         flag "consistent-read" (optional bool) ~doc:"BOOL Boolean"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String"
       and itemName = flag "item-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_attributes
           (Values.GetAttributesRequest.make
              ?attributeNames:(Option.map ~f:Values.AttributeNameList.of_json
                                 attributeNames) ?consistentRead ~domainName
              ~itemName ()) (Some Values.GetAttributesResult.to_json)
           (Some Values.GetAttributesResult.error_to_json)])
let list_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxNumberOfDomains =
         flag "max-number-of-domains" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domains
           (Values.ListDomainsRequest.make ?maxNumberOfDomains ?nextToken ())
           (Some Values.ListDomainsResult.to_json)
           (Some Values.ListDomainsResult.error_to_json)])
let put_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expected =
         flag "expected" (optional json_arg) ~doc:"JSON UpdateCondition"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING String"
       and itemName = flag "item-name" (required string) ~doc:"STRING String"
       and attributes =
         flag "attributes" (required json_arg)
           ~doc:"JSON ReplaceableAttributeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_attributes
           (Values.PutAttributesRequest.make
              ?expected:(Option.map ~f:Values.UpdateCondition.of_json
                           expected) ~domainName ~itemName
              ~attributes:(Values.ReplaceableAttributeList.of_json attributes)
              ()) None None])
let select =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and consistentRead =
         flag "consistent-read" (optional bool) ~doc:"BOOL Boolean"
       and selectExpression =
         flag "select-expression" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.select
           (Values.SelectRequest.make ?nextToken ?consistentRead
              ~selectExpression ()) (Some Values.SelectResult.to_json)
           (Some Values.SelectResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("batch-delete-attributes", batch_delete_attributes);
    ("batch-put-attributes", batch_put_attributes);
    ("create-domain", create_domain);
    ("delete-attributes", delete_attributes);
    ("delete-domain", delete_domain);
    ("domain-metadata", domain_metadata);
    ("get-attributes", get_attributes);
    ("list-domains", list_domains);
    ("put-attributes", put_attributes);
    ("select", select)]
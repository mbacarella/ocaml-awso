(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverSideKmsKeyId =
         flag "server-side-kms-key-id" (optional string)
           ~doc:"STRING NameOrArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName"
       and datasetSchema =
         flag "dataset-schema" (required json_arg) ~doc:"JSON DatasetSchema"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING IdempotenceToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dataset
           (Values.CreateDatasetRequest.make ?serverSideKmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~datasetName
              ~datasetSchema:(Values.DatasetSchema.of_json datasetSchema)
              ~clientToken ()) (Some Values.CreateDatasetResponse.to_json)
           (Some Values.CreateDatasetResponse.error_to_json)])
let create_inference_scheduler =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataDelayOffsetInMinutes =
         flag "data-delay-offset-in-minutes" (optional json_arg)
           ~doc:"JSON DataDelayOffsetInMinutes"
       and serverSideKmsKeyId =
         flag "server-side-kms-key-id" (optional string)
           ~doc:"STRING NameOrArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING ModelName"
       and inferenceSchedulerName =
         flag "inference-scheduler-name" (required string)
           ~doc:"STRING InferenceSchedulerName"
       and dataUploadFrequency =
         flag "data-upload-frequency" (required json_arg)
           ~doc:"JSON DataUploadFrequency"
       and dataInputConfiguration =
         flag "data-input-configuration" (required json_arg)
           ~doc:"JSON InferenceInputConfiguration"
       and dataOutputConfiguration =
         flag "data-output-configuration" (required json_arg)
           ~doc:"JSON InferenceOutputConfiguration"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING IamRoleArn"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING IdempotenceToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_inference_scheduler
           (Values.CreateInferenceSchedulerRequest.make
              ?dataDelayOffsetInMinutes:(Option.map
                                           ~f:Values.DataDelayOffsetInMinutes.of_json
                                           dataDelayOffsetInMinutes)
              ?serverSideKmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~modelName
              ~inferenceSchedulerName
              ~dataUploadFrequency:(Values.DataUploadFrequency.of_json
                                      dataUploadFrequency)
              ~dataInputConfiguration:(Values.InferenceInputConfiguration.of_json
                                         dataInputConfiguration)
              ~dataOutputConfiguration:(Values.InferenceOutputConfiguration.of_json
                                          dataOutputConfiguration) ~roleArn
              ~clientToken ())
           (Some Values.CreateInferenceSchedulerResponse.to_json)
           (Some Values.CreateInferenceSchedulerResponse.error_to_json)])
let create_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetSchema =
         flag "dataset-schema" (optional json_arg) ~doc:"JSON DatasetSchema"
       and labelsInputConfiguration =
         flag "labels-input-configuration" (optional json_arg)
           ~doc:"JSON LabelsInputConfiguration"
       and trainingDataStartTime =
         flag "training-data-start-time" (optional json_arg)
           ~doc:"JSON Timestamp"
       and trainingDataEndTime =
         flag "training-data-end-time" (optional json_arg)
           ~doc:"JSON Timestamp"
       and evaluationDataStartTime =
         flag "evaluation-data-start-time" (optional json_arg)
           ~doc:"JSON Timestamp"
       and evaluationDataEndTime =
         flag "evaluation-data-end-time" (optional json_arg)
           ~doc:"JSON Timestamp"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and dataPreProcessingConfiguration =
         flag "data-pre-processing-configuration" (optional json_arg)
           ~doc:"JSON DataPreProcessingConfiguration"
       and serverSideKmsKeyId =
         flag "server-side-kms-key-id" (optional string)
           ~doc:"STRING NameOrArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and offCondition =
         flag "off-condition" (optional string) ~doc:"STRING OffCondition"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING ModelName"
       and datasetName =
         flag "dataset-name" (required string)
           ~doc:"STRING DatasetIdentifier"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING IdempotenceToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model
           (Values.CreateModelRequest.make
              ?datasetSchema:(Option.map ~f:Values.DatasetSchema.of_json
                                datasetSchema)
              ?labelsInputConfiguration:(Option.map
                                           ~f:Values.LabelsInputConfiguration.of_json
                                           labelsInputConfiguration)
              ?trainingDataStartTime:(Option.map ~f:Values.Timestamp.of_json
                                        trainingDataStartTime)
              ?trainingDataEndTime:(Option.map ~f:Values.Timestamp.of_json
                                      trainingDataEndTime)
              ?evaluationDataStartTime:(Option.map
                                          ~f:Values.Timestamp.of_json
                                          evaluationDataStartTime)
              ?evaluationDataEndTime:(Option.map ~f:Values.Timestamp.of_json
                                        evaluationDataEndTime) ?roleArn
              ?dataPreProcessingConfiguration:(Option.map
                                                 ~f:Values.DataPreProcessingConfiguration.of_json
                                                 dataPreProcessingConfiguration)
              ?serverSideKmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?offCondition
              ~modelName ~datasetName ~clientToken ())
           (Some Values.CreateModelResponse.to_json)
           (Some Values.CreateModelResponse.error_to_json)])
let delete_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetName =
         flag "dataset-name" (required string)
           ~doc:"STRING DatasetIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dataset
           (Values.DeleteDatasetRequest.make ~datasetName ()) None None])
let delete_inference_scheduler =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inferenceSchedulerName =
         flag "inference-scheduler-name" (required string)
           ~doc:"STRING InferenceSchedulerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_inference_scheduler
           (Values.DeleteInferenceSchedulerRequest.make
              ~inferenceSchedulerName ()) None None])
let delete_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING ModelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model (Values.DeleteModelRequest.make ~modelName ())
           None None])
let describe_data_ingestion_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId =
         flag "job-id" (required string) ~doc:"STRING IngestionJobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_data_ingestion_job
           (Values.DescribeDataIngestionJobRequest.make ~jobId ())
           (Some Values.DescribeDataIngestionJobResponse.to_json)
           (Some Values.DescribeDataIngestionJobResponse.error_to_json)])
let describe_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetName =
         flag "dataset-name" (required string)
           ~doc:"STRING DatasetIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dataset
           (Values.DescribeDatasetRequest.make ~datasetName ())
           (Some Values.DescribeDatasetResponse.to_json)
           (Some Values.DescribeDatasetResponse.error_to_json)])
let describe_inference_scheduler =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inferenceSchedulerName =
         flag "inference-scheduler-name" (required string)
           ~doc:"STRING InferenceSchedulerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_inference_scheduler
           (Values.DescribeInferenceSchedulerRequest.make
              ~inferenceSchedulerName ())
           (Some Values.DescribeInferenceSchedulerResponse.to_json)
           (Some Values.DescribeInferenceSchedulerResponse.error_to_json)])
let describe_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING ModelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_model (Values.DescribeModelRequest.make ~modelName ())
           (Some Values.DescribeModelResponse.to_json)
           (Some Values.DescribeModelResponse.error_to_json)])
let list_data_ingestion_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetName =
         flag "dataset-name" (optional string) ~doc:"STRING DatasetName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON IngestionJobStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_ingestion_jobs
           (Values.ListDataIngestionJobsRequest.make ?datasetName ?nextToken
              ?maxResults
              ?status:(Option.map ~f:Values.IngestionJobStatus.of_json status)
              ()) (Some Values.ListDataIngestionJobsResponse.to_json)
           (Some Values.ListDataIngestionJobsResponse.error_to_json)])
let list_datasets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and datasetNameBeginsWith =
         flag "dataset-name-begins-with" (optional string)
           ~doc:"STRING DatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_datasets
           (Values.ListDatasetsRequest.make ?nextToken ?maxResults
              ?datasetNameBeginsWith ())
           (Some Values.ListDatasetsResponse.to_json)
           (Some Values.ListDatasetsResponse.error_to_json)])
let list_inference_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and dataStartTimeAfter =
         flag "data-start-time-after" (optional json_arg)
           ~doc:"JSON Timestamp"
       and dataEndTimeBefore =
         flag "data-end-time-before" (optional json_arg)
           ~doc:"JSON Timestamp"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON InferenceExecutionStatus"
       and inferenceSchedulerName =
         flag "inference-scheduler-name" (required string)
           ~doc:"STRING InferenceSchedulerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_inference_executions
           (Values.ListInferenceExecutionsRequest.make ?nextToken ?maxResults
              ?dataStartTimeAfter:(Option.map ~f:Values.Timestamp.of_json
                                     dataStartTimeAfter)
              ?dataEndTimeBefore:(Option.map ~f:Values.Timestamp.of_json
                                    dataEndTimeBefore)
              ?status:(Option.map ~f:Values.InferenceExecutionStatus.of_json
                         status) ~inferenceSchedulerName ())
           (Some Values.ListInferenceExecutionsResponse.to_json)
           (Some Values.ListInferenceExecutionsResponse.error_to_json)])
let list_inference_schedulers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and inferenceSchedulerNameBeginsWith =
         flag "inference-scheduler-name-begins-with" (optional string)
           ~doc:"STRING InferenceSchedulerIdentifier"
       and modelName =
         flag "model-name" (optional string) ~doc:"STRING ModelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_inference_schedulers
           (Values.ListInferenceSchedulersRequest.make ?nextToken ?maxResults
              ?inferenceSchedulerNameBeginsWith ?modelName ())
           (Some Values.ListInferenceSchedulersResponse.to_json)
           (Some Values.ListInferenceSchedulersResponse.error_to_json)])
let list_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and status = flag "status" (optional json_arg) ~doc:"JSON ModelStatus"
       and modelNameBeginsWith =
         flag "model-name-begins-with" (optional string)
           ~doc:"STRING ModelName"
       and datasetNameBeginsWith =
         flag "dataset-name-begins-with" (optional string)
           ~doc:"STRING DatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_models
           (Values.ListModelsRequest.make ?nextToken ?maxResults
              ?status:(Option.map ~f:Values.ModelStatus.of_json status)
              ?modelNameBeginsWith ?datasetNameBeginsWith ())
           (Some Values.ListModelsResponse.to_json)
           (Some Values.ListModelsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_data_ingestion_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetName =
         flag "dataset-name" (required string)
           ~doc:"STRING DatasetIdentifier"
       and ingestionInputConfiguration =
         flag "ingestion-input-configuration" (required json_arg)
           ~doc:"JSON IngestionInputConfiguration"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING IamRoleArn"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING IdempotenceToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_data_ingestion_job
           (Values.StartDataIngestionJobRequest.make ~datasetName
              ~ingestionInputConfiguration:(Values.IngestionInputConfiguration.of_json
                                              ingestionInputConfiguration)
              ~roleArn ~clientToken ())
           (Some Values.StartDataIngestionJobResponse.to_json)
           (Some Values.StartDataIngestionJobResponse.error_to_json)])
let start_inference_scheduler =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inferenceSchedulerName =
         flag "inference-scheduler-name" (required string)
           ~doc:"STRING InferenceSchedulerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_inference_scheduler
           (Values.StartInferenceSchedulerRequest.make
              ~inferenceSchedulerName ())
           (Some Values.StartInferenceSchedulerResponse.to_json)
           (Some Values.StartInferenceSchedulerResponse.error_to_json)])
let stop_inference_scheduler =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inferenceSchedulerName =
         flag "inference-scheduler-name" (required string)
           ~doc:"STRING InferenceSchedulerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_inference_scheduler
           (Values.StopInferenceSchedulerRequest.make ~inferenceSchedulerName
              ()) (Some Values.StopInferenceSchedulerResponse.to_json)
           (Some Values.StopInferenceSchedulerResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_inference_scheduler =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataDelayOffsetInMinutes =
         flag "data-delay-offset-in-minutes" (optional json_arg)
           ~doc:"JSON DataDelayOffsetInMinutes"
       and dataUploadFrequency =
         flag "data-upload-frequency" (optional json_arg)
           ~doc:"JSON DataUploadFrequency"
       and dataInputConfiguration =
         flag "data-input-configuration" (optional json_arg)
           ~doc:"JSON InferenceInputConfiguration"
       and dataOutputConfiguration =
         flag "data-output-configuration" (optional json_arg)
           ~doc:"JSON InferenceOutputConfiguration"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and inferenceSchedulerName =
         flag "inference-scheduler-name" (required string)
           ~doc:"STRING InferenceSchedulerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_inference_scheduler
           (Values.UpdateInferenceSchedulerRequest.make
              ?dataDelayOffsetInMinutes:(Option.map
                                           ~f:Values.DataDelayOffsetInMinutes.of_json
                                           dataDelayOffsetInMinutes)
              ?dataUploadFrequency:(Option.map
                                      ~f:Values.DataUploadFrequency.of_json
                                      dataUploadFrequency)
              ?dataInputConfiguration:(Option.map
                                         ~f:Values.InferenceInputConfiguration.of_json
                                         dataInputConfiguration)
              ?dataOutputConfiguration:(Option.map
                                          ~f:Values.InferenceOutputConfiguration.of_json
                                          dataOutputConfiguration) ?roleArn
              ~inferenceSchedulerName ()) None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-dataset", create_dataset);
    ("create-inference-scheduler", create_inference_scheduler);
    ("create-model", create_model);
    ("delete-dataset", delete_dataset);
    ("delete-inference-scheduler", delete_inference_scheduler);
    ("delete-model", delete_model);
    ("describe-data-ingestion-job", describe_data_ingestion_job);
    ("describe-dataset", describe_dataset);
    ("describe-inference-scheduler", describe_inference_scheduler);
    ("describe-model", describe_model);
    ("list-data-ingestion-jobs", list_data_ingestion_jobs);
    ("list-datasets", list_datasets);
    ("list-inference-executions", list_inference_executions);
    ("list-inference-schedulers", list_inference_schedulers);
    ("list-models", list_models);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-data-ingestion-job", start_data_ingestion_job);
    ("start-inference-scheduler", start_inference_scheduler);
    ("stop-inference-scheduler", stop_inference_scheduler);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-inference-scheduler", update_inference_scheduler)]
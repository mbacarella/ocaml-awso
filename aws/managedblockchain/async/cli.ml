(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_accessor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagMap"
       and networkType =
         flag "network-type" (optional json_arg)
           ~doc:"JSON AccessorNetworkType"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestTokenString"
       and accessorType =
         flag "accessor-type" (required json_arg) ~doc:"JSON AccessorType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_accessor
           (Values.CreateAccessorInput.make
              ?tags:(Option.map ~f:Values.InputTagMap.of_json tags)
              ?networkType:(Option.map ~f:Values.AccessorNetworkType.of_json
                              networkType) ~clientRequestToken
              ~accessorType:(Values.AccessorType.of_json accessorType) ())
           (Some Values.CreateAccessorOutput.to_json)
           (Some Values.CreateAccessorOutput.error_to_json)])
let create_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestTokenString"
       and invitationId =
         flag "invitation-id" (required string)
           ~doc:"STRING ResourceIdString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString"
       and memberConfiguration =
         flag "member-configuration" (required json_arg)
           ~doc:"JSON MemberConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_member
           (Values.CreateMemberInput.make ~clientRequestToken ~invitationId
              ~networkId
              ~memberConfiguration:(Values.MemberConfiguration.of_json
                                      memberConfiguration) ())
           (Some Values.CreateMemberOutput.to_json)
           (Some Values.CreateMemberOutput.error_to_json)])
let create_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and frameworkConfiguration =
         flag "framework-configuration" (optional json_arg)
           ~doc:"JSON NetworkFrameworkConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagMap"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestTokenString"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and framework =
         flag "framework" (required json_arg) ~doc:"JSON Framework"
       and frameworkVersion =
         flag "framework-version" (required string)
           ~doc:"STRING FrameworkVersionString"
       and votingPolicy =
         flag "voting-policy" (required json_arg) ~doc:"JSON VotingPolicy"
       and memberConfiguration =
         flag "member-configuration" (required json_arg)
           ~doc:"JSON MemberConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_network
           (Values.CreateNetworkInput.make ?description
              ?frameworkConfiguration:(Option.map
                                         ~f:Values.NetworkFrameworkConfiguration.of_json
                                         frameworkConfiguration)
              ?tags:(Option.map ~f:Values.InputTagMap.of_json tags)
              ~clientRequestToken ~name
              ~framework:(Values.Framework.of_json framework)
              ~frameworkVersion
              ~votingPolicy:(Values.VotingPolicy.of_json votingPolicy)
              ~memberConfiguration:(Values.MemberConfiguration.of_json
                                      memberConfiguration) ())
           (Some Values.CreateNetworkOutput.to_json)
           (Some Values.CreateNetworkOutput.error_to_json)])
let create_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberId =
         flag "member-id" (optional string) ~doc:"STRING ResourceIdString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagMap"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestTokenString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString"
       and nodeConfiguration =
         flag "node-configuration" (required json_arg)
           ~doc:"JSON NodeConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_node
           (Values.CreateNodeInput.make ?memberId
              ?tags:(Option.map ~f:Values.InputTagMap.of_json tags)
              ~clientRequestToken ~networkId
              ~nodeConfiguration:(Values.NodeConfiguration.of_json
                                    nodeConfiguration) ())
           (Some Values.CreateNodeOutput.to_json)
           (Some Values.CreateNodeOutput.error_to_json)])
let create_proposal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagMap"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestTokenString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString"
       and memberId =
         flag "member-id" (required string) ~doc:"STRING ResourceIdString"
       and actions =
         flag "actions" (required json_arg) ~doc:"JSON ProposalActions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_proposal
           (Values.CreateProposalInput.make ?description
              ?tags:(Option.map ~f:Values.InputTagMap.of_json tags)
              ~clientRequestToken ~networkId ~memberId
              ~actions:(Values.ProposalActions.of_json actions) ())
           (Some Values.CreateProposalOutput.to_json)
           (Some Values.CreateProposalOutput.error_to_json)])
let delete_accessor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessorId =
         flag "accessor-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_accessor
           (Values.DeleteAccessorInput.make ~accessorId ())
           (Some Values.DeleteAccessorOutput.to_json)
           (Some Values.DeleteAccessorOutput.error_to_json)])
let delete_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString"
       and memberId =
         flag "member-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_member
           (Values.DeleteMemberInput.make ~networkId ~memberId ())
           (Some Values.DeleteMemberOutput.to_json)
           (Some Values.DeleteMemberOutput.error_to_json)])
let delete_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberId =
         flag "member-id" (optional string) ~doc:"STRING ResourceIdString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString"
       and nodeId =
         flag "node-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_node
           (Values.DeleteNodeInput.make ?memberId ~networkId ~nodeId ())
           (Some Values.DeleteNodeOutput.to_json)
           (Some Values.DeleteNodeOutput.error_to_json)])
let get_accessor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessorId =
         flag "accessor-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_accessor (Values.GetAccessorInput.make ~accessorId ())
           (Some Values.GetAccessorOutput.to_json)
           (Some Values.GetAccessorOutput.error_to_json)])
let get_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString"
       and memberId =
         flag "member-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_member (Values.GetMemberInput.make ~networkId ~memberId ())
           (Some Values.GetMemberOutput.to_json)
           (Some Values.GetMemberOutput.error_to_json)])
let get_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_network (Values.GetNetworkInput.make ~networkId ())
           (Some Values.GetNetworkOutput.to_json)
           (Some Values.GetNetworkOutput.error_to_json)])
let get_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberId =
         flag "member-id" (optional string) ~doc:"STRING ResourceIdString"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString"
       and nodeId =
         flag "node-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_node
           (Values.GetNodeInput.make ?memberId ~networkId ~nodeId ())
           (Some Values.GetNodeOutput.to_json)
           (Some Values.GetNodeOutput.error_to_json)])
let get_proposal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString"
       and proposalId =
         flag "proposal-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_proposal
           (Values.GetProposalInput.make ~networkId ~proposalId ())
           (Some Values.GetProposalOutput.to_json)
           (Some Values.GetProposalOutput.error_to_json)])
let list_accessors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT AccessorListMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and networkType =
         flag "network-type" (optional json_arg)
           ~doc:"JSON AccessorNetworkType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_accessors
           (Values.ListAccessorsInput.make ?maxResults ?nextToken
              ?networkType:(Option.map ~f:Values.AccessorNetworkType.of_json
                              networkType) ())
           (Some Values.ListAccessorsOutput.to_json)
           (Some Values.ListAccessorsOutput.error_to_json)])
let list_invitations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ProposalListMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_invitations
           (Values.ListInvitationsInput.make ?maxResults ?nextToken ())
           (Some Values.ListInvitationsOutput.to_json)
           (Some Values.ListInvitationsOutput.error_to_json)])
let list_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON MemberStatus"
       and isOwned = flag "is-owned" (optional bool) ~doc:"BOOL IsOwned"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MemberListMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_members
           (Values.ListMembersInput.make ?name
              ?status:(Option.map ~f:Values.MemberStatus.of_json status)
              ?isOwned ?maxResults ?nextToken ~networkId ())
           (Some Values.ListMembersOutput.to_json)
           (Some Values.ListMembersOutput.error_to_json)])
let list_networks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and framework =
         flag "framework" (optional json_arg) ~doc:"JSON Framework"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON NetworkStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NetworkListMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_networks
           (Values.ListNetworksInput.make ?name
              ?framework:(Option.map ~f:Values.Framework.of_json framework)
              ?status:(Option.map ~f:Values.NetworkStatus.of_json status)
              ?maxResults ?nextToken ())
           (Some Values.ListNetworksOutput.to_json)
           (Some Values.ListNetworksOutput.error_to_json)])
let list_nodes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberId =
         flag "member-id" (optional string) ~doc:"STRING ResourceIdString"
       and status = flag "status" (optional json_arg) ~doc:"JSON NodeStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT NodeListMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_nodes
           (Values.ListNodesInput.make ?memberId
              ?status:(Option.map ~f:Values.NodeStatus.of_json status)
              ?maxResults ?nextToken ~networkId ())
           (Some Values.ListNodesOutput.to_json)
           (Some Values.ListNodesOutput.error_to_json)])
let list_proposal_votes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ProposalListMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString"
       and proposalId =
         flag "proposal-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_proposal_votes
           (Values.ListProposalVotesInput.make ?maxResults ?nextToken
              ~networkId ~proposalId ())
           (Some Values.ListProposalVotesOutput.to_json)
           (Some Values.ListProposalVotesOutput.error_to_json)])
let list_proposals =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ProposalListMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_proposals
           (Values.ListProposalsInput.make ?maxResults ?nextToken ~networkId
              ()) (Some Values.ListProposalsOutput.to_json)
           (Some Values.ListProposalsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let reject_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and invitationId =
         flag "invitation-id" (required string)
           ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_invitation
           (Values.RejectInvitationInput.make ~invitationId ())
           (Some Values.RejectInvitationOutput.to_json)
           (Some Values.RejectInvitationOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ArnString"
       and tags = flag "tags" (required json_arg) ~doc:"JSON InputTagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.InputTagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ArnString"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and logPublishingConfiguration =
         flag "log-publishing-configuration" (optional json_arg)
           ~doc:"JSON MemberLogPublishingConfiguration"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString"
       and memberId =
         flag "member-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_member
           (Values.UpdateMemberInput.make
              ?logPublishingConfiguration:(Option.map
                                             ~f:Values.MemberLogPublishingConfiguration.of_json
                                             logPublishingConfiguration)
              ~networkId ~memberId ())
           (Some Values.UpdateMemberOutput.to_json)
           (Some Values.UpdateMemberOutput.error_to_json)])
let update_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberId =
         flag "member-id" (optional string) ~doc:"STRING ResourceIdString"
       and logPublishingConfiguration =
         flag "log-publishing-configuration" (optional json_arg)
           ~doc:"JSON NodeLogPublishingConfiguration"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString"
       and nodeId =
         flag "node-id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_node
           (Values.UpdateNodeInput.make ?memberId
              ?logPublishingConfiguration:(Option.map
                                             ~f:Values.NodeLogPublishingConfiguration.of_json
                                             logPublishingConfiguration)
              ~networkId ~nodeId ()) (Some Values.UpdateNodeOutput.to_json)
           (Some Values.UpdateNodeOutput.error_to_json)])
let vote_on_proposal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING ResourceIdString"
       and proposalId =
         flag "proposal-id" (required string) ~doc:"STRING ResourceIdString"
       and voterMemberId =
         flag "voter-member-id" (required string)
           ~doc:"STRING ResourceIdString"
       and vote = flag "vote" (required json_arg) ~doc:"JSON VoteValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.vote_on_proposal
           (Values.VoteOnProposalInput.make ~networkId ~proposalId
              ~voterMemberId ~vote:(Values.VoteValue.of_json vote) ())
           (Some Values.VoteOnProposalOutput.to_json)
           (Some Values.VoteOnProposalOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-accessor", create_accessor);
    ("create-member", create_member);
    ("create-network", create_network);
    ("create-node", create_node);
    ("create-proposal", create_proposal);
    ("delete-accessor", delete_accessor);
    ("delete-member", delete_member);
    ("delete-node", delete_node);
    ("get-accessor", get_accessor);
    ("get-member", get_member);
    ("get-network", get_network);
    ("get-node", get_node);
    ("get-proposal", get_proposal);
    ("list-accessors", list_accessors);
    ("list-invitations", list_invitations);
    ("list-members", list_members);
    ("list-networks", list_networks);
    ("list-nodes", list_nodes);
    ("list-proposal-votes", list_proposal_votes);
    ("list-proposals", list_proposals);
    ("list-tags-for-resource", list_tags_for_resource);
    ("reject-invitation", reject_invitation);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-member", update_member);
    ("update-node", update_node);
    ("vote-on-proposal", vote_on_proposal)]
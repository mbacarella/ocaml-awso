(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_anomaly_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceTags =
         flag "resource-tags" (optional json_arg) ~doc:"JSON ResourceTagList"
       and anomalyMonitor =
         flag "anomaly-monitor" (required json_arg)
           ~doc:"JSON AnomalyMonitor" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_anomaly_monitor
           (Values.CreateAnomalyMonitorRequest.make
              ?resourceTags:(Option.map ~f:Values.ResourceTagList.of_json
                               resourceTags)
              ~anomalyMonitor:(Values.AnomalyMonitor.of_json anomalyMonitor)
              ()) (Some Values.CreateAnomalyMonitorResponse.to_json)
           (Some Values.CreateAnomalyMonitorResponse.error_to_json)])
let create_anomaly_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceTags =
         flag "resource-tags" (optional json_arg) ~doc:"JSON ResourceTagList"
       and anomalySubscription =
         flag "anomaly-subscription" (required json_arg)
           ~doc:"JSON AnomalySubscription" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_anomaly_subscription
           (Values.CreateAnomalySubscriptionRequest.make
              ?resourceTags:(Option.map ~f:Values.ResourceTagList.of_json
                               resourceTags)
              ~anomalySubscription:(Values.AnomalySubscription.of_json
                                      anomalySubscription) ())
           (Some Values.CreateAnomalySubscriptionResponse.to_json)
           (Some Values.CreateAnomalySubscriptionResponse.error_to_json)])
let create_cost_category_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and defaultValue =
         flag "default-value" (optional string)
           ~doc:"STRING CostCategoryValue"
       and splitChargeRules =
         flag "split-charge-rules" (optional json_arg)
           ~doc:"JSON CostCategorySplitChargeRulesList"
       and resourceTags =
         flag "resource-tags" (optional json_arg) ~doc:"JSON ResourceTagList"
       and name =
         flag "name" (required string) ~doc:"STRING CostCategoryName"
       and ruleVersion =
         flag "rule-version" (required json_arg)
           ~doc:"JSON CostCategoryRuleVersion"
       and rules =
         flag "rules" (required json_arg) ~doc:"JSON CostCategoryRulesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cost_category_definition
           (Values.CreateCostCategoryDefinitionRequest.make ?defaultValue
              ?splitChargeRules:(Option.map
                                   ~f:Values.CostCategorySplitChargeRulesList.of_json
                                   splitChargeRules)
              ?resourceTags:(Option.map ~f:Values.ResourceTagList.of_json
                               resourceTags) ~name
              ~ruleVersion:(Values.CostCategoryRuleVersion.of_json
                              ruleVersion)
              ~rules:(Values.CostCategoryRulesList.of_json rules) ())
           (Some Values.CreateCostCategoryDefinitionResponse.to_json)
           (Some Values.CreateCostCategoryDefinitionResponse.error_to_json)])
let delete_anomaly_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorArn =
         flag "monitor-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_anomaly_monitor
           (Values.DeleteAnomalyMonitorRequest.make ~monitorArn ())
           (Some Values.DeleteAnomalyMonitorResponse.to_json)
           (Some Values.DeleteAnomalyMonitorResponse.error_to_json)])
let delete_anomaly_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionArn =
         flag "subscription-arn" (required string)
           ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_anomaly_subscription
           (Values.DeleteAnomalySubscriptionRequest.make ~subscriptionArn ())
           (Some Values.DeleteAnomalySubscriptionResponse.to_json)
           (Some Values.DeleteAnomalySubscriptionResponse.error_to_json)])
let delete_cost_category_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and costCategoryArn =
         flag "cost-category-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cost_category_definition
           (Values.DeleteCostCategoryDefinitionRequest.make ~costCategoryArn
              ()) (Some Values.DeleteCostCategoryDefinitionResponse.to_json)
           (Some Values.DeleteCostCategoryDefinitionResponse.error_to_json)])
let describe_cost_category_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and effectiveOn =
         flag "effective-on" (optional string) ~doc:"STRING ZonedDateTime"
       and costCategoryArn =
         flag "cost-category-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cost_category_definition
           (Values.DescribeCostCategoryDefinitionRequest.make ?effectiveOn
              ~costCategoryArn ())
           (Some Values.DescribeCostCategoryDefinitionResponse.to_json)
           (Some Values.DescribeCostCategoryDefinitionResponse.error_to_json)])
let get_anomalies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorArn =
         flag "monitor-arn" (optional string) ~doc:"STRING GenericString"
       and feedback =
         flag "feedback" (optional json_arg) ~doc:"JSON AnomalyFeedbackType"
       and totalImpact =
         flag "total-impact" (optional json_arg)
           ~doc:"JSON TotalImpactFilter"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and dateInterval =
         flag "date-interval" (required json_arg)
           ~doc:"JSON AnomalyDateInterval" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_anomalies
           (Values.GetAnomaliesRequest.make ?monitorArn
              ?feedback:(Option.map ~f:Values.AnomalyFeedbackType.of_json
                           feedback)
              ?totalImpact:(Option.map ~f:Values.TotalImpactFilter.of_json
                              totalImpact) ?nextPageToken ?maxResults
              ~dateInterval:(Values.AnomalyDateInterval.of_json dateInterval)
              ()) (Some Values.GetAnomaliesResponse.to_json)
           (Some Values.GetAnomaliesResponse.error_to_json)])
let get_anomaly_monitors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorArnList =
         flag "monitor-arn-list" (optional json_arg) ~doc:"JSON Values"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_anomaly_monitors
           (Values.GetAnomalyMonitorsRequest.make
              ?monitorArnList:(Option.map ~f:Values.Values.of_json
                                 monitorArnList) ?nextPageToken ?maxResults
              ()) (Some Values.GetAnomalyMonitorsResponse.to_json)
           (Some Values.GetAnomalyMonitorsResponse.error_to_json)])
let get_anomaly_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionArnList =
         flag "subscription-arn-list" (optional json_arg) ~doc:"JSON Values"
       and monitorArn =
         flag "monitor-arn" (optional string) ~doc:"STRING GenericString"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_anomaly_subscriptions
           (Values.GetAnomalySubscriptionsRequest.make
              ?subscriptionArnList:(Option.map ~f:Values.Values.of_json
                                      subscriptionArnList) ?monitorArn
              ?nextPageToken ?maxResults ())
           (Some Values.GetAnomalySubscriptionsResponse.to_json)
           (Some Values.GetAnomalySubscriptionsResponse.error_to_json)])
let get_cost_and_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and groupBy =
         flag "group-by" (optional json_arg) ~doc:"JSON GroupDefinitions"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING NextPageToken"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON DateInterval"
       and granularity =
         flag "granularity" (required json_arg) ~doc:"JSON Granularity"
       and metrics =
         flag "metrics" (required json_arg) ~doc:"JSON MetricNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cost_and_usage
           (Values.GetCostAndUsageRequest.make
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?groupBy:(Option.map ~f:Values.GroupDefinitions.of_json groupBy)
              ?nextPageToken
              ~timePeriod:(Values.DateInterval.of_json timePeriod)
              ~granularity:(Values.Granularity.of_json granularity)
              ~metrics:(Values.MetricNames.of_json metrics) ())
           (Some Values.GetCostAndUsageResponse.to_json)
           (Some Values.GetCostAndUsageResponse.error_to_json)])
let get_cost_and_usage_with_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metrics =
         flag "metrics" (optional json_arg) ~doc:"JSON MetricNames"
       and groupBy =
         flag "group-by" (optional json_arg) ~doc:"JSON GroupDefinitions"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING NextPageToken"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON DateInterval"
       and granularity =
         flag "granularity" (required json_arg) ~doc:"JSON Granularity"
       and filter = flag "filter" (required json_arg) ~doc:"JSON Expression" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cost_and_usage_with_resources
           (Values.GetCostAndUsageWithResourcesRequest.make
              ?metrics:(Option.map ~f:Values.MetricNames.of_json metrics)
              ?groupBy:(Option.map ~f:Values.GroupDefinitions.of_json groupBy)
              ?nextPageToken
              ~timePeriod:(Values.DateInterval.of_json timePeriod)
              ~granularity:(Values.Granularity.of_json granularity)
              ~filter:(Values.Expression.of_json filter) ())
           (Some Values.GetCostAndUsageWithResourcesResponse.to_json)
           (Some Values.GetCostAndUsageWithResourcesResponse.error_to_json)])
let get_cost_categories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchString =
         flag "search-string" (optional string) ~doc:"STRING SearchString"
       and costCategoryName =
         flag "cost-category-name" (optional string)
           ~doc:"STRING CostCategoryName"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortDefinitions"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING NextPageToken"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON DateInterval" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cost_categories
           (Values.GetCostCategoriesRequest.make ?searchString
              ?costCategoryName
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?sortBy:(Option.map ~f:Values.SortDefinitions.of_json sortBy)
              ?maxResults ?nextPageToken
              ~timePeriod:(Values.DateInterval.of_json timePeriod) ())
           (Some Values.GetCostCategoriesResponse.to_json)
           (Some Values.GetCostCategoriesResponse.error_to_json)])
let get_cost_forecast =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and predictionIntervalLevel =
         flag "prediction-interval-level" (optional int)
           ~doc:"INT PredictionIntervalLevel"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON DateInterval"
       and metric = flag "metric" (required json_arg) ~doc:"JSON Metric"
       and granularity =
         flag "granularity" (required json_arg) ~doc:"JSON Granularity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cost_forecast
           (Values.GetCostForecastRequest.make
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?predictionIntervalLevel
              ~timePeriod:(Values.DateInterval.of_json timePeriod)
              ~metric:(Values.Metric.of_json metric)
              ~granularity:(Values.Granularity.of_json granularity) ())
           (Some Values.GetCostForecastResponse.to_json)
           (Some Values.GetCostForecastResponse.error_to_json)])
let get_dimension_values =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchString =
         flag "search-string" (optional string) ~doc:"STRING SearchString"
       and context = flag "context" (optional json_arg) ~doc:"JSON Context"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortDefinitions"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING NextPageToken"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON DateInterval"
       and dimension =
         flag "dimension" (required json_arg) ~doc:"JSON Dimension" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dimension_values
           (Values.GetDimensionValuesRequest.make ?searchString
              ?context:(Option.map ~f:Values.Context.of_json context)
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?sortBy:(Option.map ~f:Values.SortDefinitions.of_json sortBy)
              ?maxResults ?nextPageToken
              ~timePeriod:(Values.DateInterval.of_json timePeriod)
              ~dimension:(Values.Dimension.of_json dimension) ())
           (Some Values.GetDimensionValuesResponse.to_json)
           (Some Values.GetDimensionValuesResponse.error_to_json)])
let get_reservation_coverage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupBy =
         flag "group-by" (optional json_arg) ~doc:"JSON GroupDefinitions"
       and granularity =
         flag "granularity" (optional json_arg) ~doc:"JSON Granularity"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and metrics =
         flag "metrics" (optional json_arg) ~doc:"JSON MetricNames"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING NextPageToken"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortDefinition"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON DateInterval" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_reservation_coverage
           (Values.GetReservationCoverageRequest.make
              ?groupBy:(Option.map ~f:Values.GroupDefinitions.of_json groupBy)
              ?granularity:(Option.map ~f:Values.Granularity.of_json
                              granularity)
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?metrics:(Option.map ~f:Values.MetricNames.of_json metrics)
              ?nextPageToken
              ?sortBy:(Option.map ~f:Values.SortDefinition.of_json sortBy)
              ?maxResults
              ~timePeriod:(Values.DateInterval.of_json timePeriod) ())
           (Some Values.GetReservationCoverageResponse.to_json)
           (Some Values.GetReservationCoverageResponse.error_to_json)])
let get_reservation_purchase_recommendation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING GenericString"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and accountScope =
         flag "account-scope" (optional json_arg) ~doc:"JSON AccountScope"
       and lookbackPeriodInDays =
         flag "lookback-period-in-days" (optional json_arg)
           ~doc:"JSON LookbackPeriodInDays"
       and termInYears =
         flag "term-in-years" (optional json_arg) ~doc:"JSON TermInYears"
       and paymentOption =
         flag "payment-option" (optional json_arg) ~doc:"JSON PaymentOption"
       and serviceSpecification =
         flag "service-specification" (optional json_arg)
           ~doc:"JSON ServiceSpecification"
       and pageSize =
         flag "page-size" (optional int) ~doc:"INT NonNegativeInteger"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING NextPageToken"
       and service =
         flag "service" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_reservation_purchase_recommendation
           (Values.GetReservationPurchaseRecommendationRequest.make
              ?accountId
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?accountScope:(Option.map ~f:Values.AccountScope.of_json
                               accountScope)
              ?lookbackPeriodInDays:(Option.map
                                       ~f:Values.LookbackPeriodInDays.of_json
                                       lookbackPeriodInDays)
              ?termInYears:(Option.map ~f:Values.TermInYears.of_json
                              termInYears)
              ?paymentOption:(Option.map ~f:Values.PaymentOption.of_json
                                paymentOption)
              ?serviceSpecification:(Option.map
                                       ~f:Values.ServiceSpecification.of_json
                                       serviceSpecification) ?pageSize
              ?nextPageToken ~service ())
           (Some Values.GetReservationPurchaseRecommendationResponse.to_json)
           (Some
              Values.GetReservationPurchaseRecommendationResponse.error_to_json)])
let get_reservation_utilization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupBy =
         flag "group-by" (optional json_arg) ~doc:"JSON GroupDefinitions"
       and granularity =
         flag "granularity" (optional json_arg) ~doc:"JSON Granularity"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortDefinition"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON DateInterval" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_reservation_utilization
           (Values.GetReservationUtilizationRequest.make
              ?groupBy:(Option.map ~f:Values.GroupDefinitions.of_json groupBy)
              ?granularity:(Option.map ~f:Values.Granularity.of_json
                              granularity)
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?sortBy:(Option.map ~f:Values.SortDefinition.of_json sortBy)
              ?nextPageToken ?maxResults
              ~timePeriod:(Values.DateInterval.of_json timePeriod) ())
           (Some Values.GetReservationUtilizationResponse.to_json)
           (Some Values.GetReservationUtilizationResponse.error_to_json)])
let get_rightsizing_recommendation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON RightsizingRecommendationConfiguration"
       and pageSize =
         flag "page-size" (optional int) ~doc:"INT NonNegativeInteger"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING NextPageToken"
       and service =
         flag "service" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_rightsizing_recommendation
           (Values.GetRightsizingRecommendationRequest.make
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?configuration:(Option.map
                                ~f:Values.RightsizingRecommendationConfiguration.of_json
                                configuration) ?pageSize ?nextPageToken
              ~service ())
           (Some Values.GetRightsizingRecommendationResponse.to_json)
           (Some Values.GetRightsizingRecommendationResponse.error_to_json)])
let get_savings_plans_coverage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupBy =
         flag "group-by" (optional json_arg) ~doc:"JSON GroupDefinitions"
       and granularity =
         flag "granularity" (optional json_arg) ~doc:"JSON Granularity"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and metrics =
         flag "metrics" (optional json_arg) ~doc:"JSON MetricNames"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortDefinition"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON DateInterval" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_savings_plans_coverage
           (Values.GetSavingsPlansCoverageRequest.make
              ?groupBy:(Option.map ~f:Values.GroupDefinitions.of_json groupBy)
              ?granularity:(Option.map ~f:Values.Granularity.of_json
                              granularity)
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?metrics:(Option.map ~f:Values.MetricNames.of_json metrics)
              ?nextToken ?maxResults
              ?sortBy:(Option.map ~f:Values.SortDefinition.of_json sortBy)
              ~timePeriod:(Values.DateInterval.of_json timePeriod) ())
           (Some Values.GetSavingsPlansCoverageResponse.to_json)
           (Some Values.GetSavingsPlansCoverageResponse.error_to_json)])
let get_savings_plans_purchase_recommendation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountScope =
         flag "account-scope" (optional json_arg) ~doc:"JSON AccountScope"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING NextPageToken"
       and pageSize =
         flag "page-size" (optional int) ~doc:"INT NonNegativeInteger"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and savingsPlansType =
         flag "savings-plans-type" (required json_arg)
           ~doc:"JSON SupportedSavingsPlansType"
       and termInYears =
         flag "term-in-years" (required json_arg) ~doc:"JSON TermInYears"
       and paymentOption =
         flag "payment-option" (required json_arg) ~doc:"JSON PaymentOption"
       and lookbackPeriodInDays =
         flag "lookback-period-in-days" (required json_arg)
           ~doc:"JSON LookbackPeriodInDays" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_savings_plans_purchase_recommendation
           (Values.GetSavingsPlansPurchaseRecommendationRequest.make
              ?accountScope:(Option.map ~f:Values.AccountScope.of_json
                               accountScope) ?nextPageToken ?pageSize
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ~savingsPlansType:(Values.SupportedSavingsPlansType.of_json
                                   savingsPlansType)
              ~termInYears:(Values.TermInYears.of_json termInYears)
              ~paymentOption:(Values.PaymentOption.of_json paymentOption)
              ~lookbackPeriodInDays:(Values.LookbackPeriodInDays.of_json
                                       lookbackPeriodInDays) ())
           (Some Values.GetSavingsPlansPurchaseRecommendationResponse.to_json)
           (Some
              Values.GetSavingsPlansPurchaseRecommendationResponse.error_to_json)])
let get_savings_plans_utilization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and granularity =
         flag "granularity" (optional json_arg) ~doc:"JSON Granularity"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortDefinition"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON DateInterval" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_savings_plans_utilization
           (Values.GetSavingsPlansUtilizationRequest.make
              ?granularity:(Option.map ~f:Values.Granularity.of_json
                              granularity)
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?sortBy:(Option.map ~f:Values.SortDefinition.of_json sortBy)
              ~timePeriod:(Values.DateInterval.of_json timePeriod) ())
           (Some Values.GetSavingsPlansUtilizationResponse.to_json)
           (Some Values.GetSavingsPlansUtilizationResponse.error_to_json)])
let get_savings_plans_utilization_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and dataType =
         flag "data-type" (optional json_arg)
           ~doc:"JSON SavingsPlansDataTypes"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortDefinition"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON DateInterval" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_savings_plans_utilization_details
           (Values.GetSavingsPlansUtilizationDetailsRequest.make
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?dataType:(Option.map ~f:Values.SavingsPlansDataTypes.of_json
                           dataType) ?nextToken ?maxResults
              ?sortBy:(Option.map ~f:Values.SortDefinition.of_json sortBy)
              ~timePeriod:(Values.DateInterval.of_json timePeriod) ())
           (Some Values.GetSavingsPlansUtilizationDetailsResponse.to_json)
           (Some
              Values.GetSavingsPlansUtilizationDetailsResponse.error_to_json)])
let get_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchString =
         flag "search-string" (optional string) ~doc:"STRING SearchString"
       and tagKey = flag "tag-key" (optional string) ~doc:"STRING TagKey"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortDefinitions"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextPageToken =
         flag "next-page-token" (optional string) ~doc:"STRING NextPageToken"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON DateInterval" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tags
           (Values.GetTagsRequest.make ?searchString ?tagKey
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?sortBy:(Option.map ~f:Values.SortDefinitions.of_json sortBy)
              ?maxResults ?nextPageToken
              ~timePeriod:(Values.DateInterval.of_json timePeriod) ())
           (Some Values.GetTagsResponse.to_json)
           (Some Values.GetTagsResponse.error_to_json)])
let get_usage_forecast =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and predictionIntervalLevel =
         flag "prediction-interval-level" (optional int)
           ~doc:"INT PredictionIntervalLevel"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON DateInterval"
       and metric = flag "metric" (required json_arg) ~doc:"JSON Metric"
       and granularity =
         flag "granularity" (required json_arg) ~doc:"JSON Granularity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_usage_forecast
           (Values.GetUsageForecastRequest.make
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?predictionIntervalLevel
              ~timePeriod:(Values.DateInterval.of_json timePeriod)
              ~metric:(Values.Metric.of_json metric)
              ~granularity:(Values.Granularity.of_json granularity) ())
           (Some Values.GetUsageForecastResponse.to_json)
           (Some Values.GetUsageForecastResponse.error_to_json)])
let list_cost_category_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and effectiveOn =
         flag "effective-on" (optional string) ~doc:"STRING ZonedDateTime"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT CostCategoryMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_cost_category_definitions
           (Values.ListCostCategoryDefinitionsRequest.make ?effectiveOn
              ?nextToken ?maxResults ())
           (Some Values.ListCostCategoryDefinitionsResponse.to_json)
           (Some Values.ListCostCategoryDefinitionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let provide_anomaly_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and anomalyId =
         flag "anomaly-id" (required string) ~doc:"STRING GenericString"
       and feedback =
         flag "feedback" (required json_arg) ~doc:"JSON AnomalyFeedbackType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.provide_anomaly_feedback
           (Values.ProvideAnomalyFeedbackRequest.make ~anomalyId
              ~feedback:(Values.AnomalyFeedbackType.of_json feedback) ())
           (Some Values.ProvideAnomalyFeedbackResponse.to_json)
           (Some Values.ProvideAnomalyFeedbackResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and resourceTags =
         flag "resource-tags" (required json_arg) ~doc:"JSON ResourceTagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~resourceTags:(Values.ResourceTagList.of_json resourceTags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and resourceTagKeys =
         flag "resource-tag-keys" (required json_arg)
           ~doc:"JSON ResourceTagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~resourceTagKeys:(Values.ResourceTagKeyList.of_json
                                  resourceTagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_anomaly_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorName =
         flag "monitor-name" (optional string) ~doc:"STRING GenericString"
       and monitorArn =
         flag "monitor-arn" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_anomaly_monitor
           (Values.UpdateAnomalyMonitorRequest.make ?monitorName ~monitorArn
              ()) (Some Values.UpdateAnomalyMonitorResponse.to_json)
           (Some Values.UpdateAnomalyMonitorResponse.error_to_json)])
let update_anomaly_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and threshold =
         flag "threshold" (optional float)
           ~doc:"FLOAT NullableNonNegativeDouble"
       and frequency =
         flag "frequency" (optional json_arg)
           ~doc:"JSON AnomalySubscriptionFrequency"
       and monitorArnList =
         flag "monitor-arn-list" (optional json_arg)
           ~doc:"JSON MonitorArnList"
       and subscribers =
         flag "subscribers" (optional json_arg) ~doc:"JSON Subscribers"
       and subscriptionName =
         flag "subscription-name" (optional string)
           ~doc:"STRING GenericString"
       and subscriptionArn =
         flag "subscription-arn" (required string)
           ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_anomaly_subscription
           (Values.UpdateAnomalySubscriptionRequest.make ?threshold
              ?frequency:(Option.map
                            ~f:Values.AnomalySubscriptionFrequency.of_json
                            frequency)
              ?monitorArnList:(Option.map ~f:Values.MonitorArnList.of_json
                                 monitorArnList)
              ?subscribers:(Option.map ~f:Values.Subscribers.of_json
                              subscribers) ?subscriptionName ~subscriptionArn
              ()) (Some Values.UpdateAnomalySubscriptionResponse.to_json)
           (Some Values.UpdateAnomalySubscriptionResponse.error_to_json)])
let update_cost_category_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and defaultValue =
         flag "default-value" (optional string)
           ~doc:"STRING CostCategoryValue"
       and splitChargeRules =
         flag "split-charge-rules" (optional json_arg)
           ~doc:"JSON CostCategorySplitChargeRulesList"
       and costCategoryArn =
         flag "cost-category-arn" (required string) ~doc:"STRING Arn"
       and ruleVersion =
         flag "rule-version" (required json_arg)
           ~doc:"JSON CostCategoryRuleVersion"
       and rules =
         flag "rules" (required json_arg) ~doc:"JSON CostCategoryRulesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cost_category_definition
           (Values.UpdateCostCategoryDefinitionRequest.make ?defaultValue
              ?splitChargeRules:(Option.map
                                   ~f:Values.CostCategorySplitChargeRulesList.of_json
                                   splitChargeRules) ~costCategoryArn
              ~ruleVersion:(Values.CostCategoryRuleVersion.of_json
                              ruleVersion)
              ~rules:(Values.CostCategoryRulesList.of_json rules) ())
           (Some Values.UpdateCostCategoryDefinitionResponse.to_json)
           (Some Values.UpdateCostCategoryDefinitionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-anomaly-monitor", create_anomaly_monitor);
    ("create-anomaly-subscription", create_anomaly_subscription);
    ("create-cost-category-definition", create_cost_category_definition);
    ("delete-anomaly-monitor", delete_anomaly_monitor);
    ("delete-anomaly-subscription", delete_anomaly_subscription);
    ("delete-cost-category-definition", delete_cost_category_definition);
    ("describe-cost-category-definition", describe_cost_category_definition);
    ("get-anomalies", get_anomalies);
    ("get-anomaly-monitors", get_anomaly_monitors);
    ("get-anomaly-subscriptions", get_anomaly_subscriptions);
    ("get-cost-and-usage", get_cost_and_usage);
    ("get-cost-and-usage-with-resources", get_cost_and_usage_with_resources);
    ("get-cost-categories", get_cost_categories);
    ("get-cost-forecast", get_cost_forecast);
    ("get-dimension-values", get_dimension_values);
    ("get-reservation-coverage", get_reservation_coverage);
    ("get-reservation-purchase-recommendation",
      get_reservation_purchase_recommendation);
    ("get-reservation-utilization", get_reservation_utilization);
    ("get-rightsizing-recommendation", get_rightsizing_recommendation);
    ("get-savings-plans-coverage", get_savings_plans_coverage);
    ("get-savings-plans-purchase-recommendation",
      get_savings_plans_purchase_recommendation);
    ("get-savings-plans-utilization", get_savings_plans_utilization);
    ("get-savings-plans-utilization-details",
      get_savings_plans_utilization_details);
    ("get-tags", get_tags);
    ("get-usage-forecast", get_usage_forecast);
    ("list-cost-category-definitions", list_cost_category_definitions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("provide-anomaly-feedback", provide_anomaly_feedback);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-anomaly-monitor", update_anomaly_monitor);
    ("update-anomaly-subscription", update_anomaly_subscription);
    ("update-cost-category-definition", update_cost_category_definition)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_gateway_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and gatewayRouteName =
         flag "gateway-route-name" (required string)
           ~doc:"STRING ResourceName"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and spec =
         flag "spec" (required json_arg) ~doc:"JSON GatewayRouteSpec"
       and virtualGatewayName =
         flag "virtual-gateway-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_gateway_route
           (Values.CreateGatewayRouteInput.make ?clientToken ?meshOwner
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~gatewayRouteName ~meshName
              ~spec:(Values.GatewayRouteSpec.of_json spec)
              ~virtualGatewayName ())
           (Some Values.CreateGatewayRouteOutput.to_json)
           (Some Values.CreateGatewayRouteOutput.error_to_json)])
let create_mesh =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and spec = flag "spec" (optional json_arg) ~doc:"JSON MeshSpec"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_mesh
           (Values.CreateMeshInput.make ?clientToken
              ?spec:(Option.map ~f:Values.MeshSpec.of_json spec)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~meshName ())
           (Some Values.CreateMeshOutput.to_json)
           (Some Values.CreateMeshOutput.error_to_json)])
let create_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and routeName =
         flag "route-name" (required string) ~doc:"STRING ResourceName"
       and spec = flag "spec" (required json_arg) ~doc:"JSON RouteSpec"
       and virtualRouterName =
         flag "virtual-router-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_route
           (Values.CreateRouteInput.make ?clientToken ?meshOwner
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~meshName
              ~routeName ~spec:(Values.RouteSpec.of_json spec)
              ~virtualRouterName ()) (Some Values.CreateRouteOutput.to_json)
           (Some Values.CreateRouteOutput.error_to_json)])
let create_virtual_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and spec =
         flag "spec" (required json_arg) ~doc:"JSON VirtualGatewaySpec"
       and virtualGatewayName =
         flag "virtual-gateway-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_virtual_gateway
           (Values.CreateVirtualGatewayInput.make ?clientToken ?meshOwner
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~meshName
              ~spec:(Values.VirtualGatewaySpec.of_json spec)
              ~virtualGatewayName ())
           (Some Values.CreateVirtualGatewayOutput.to_json)
           (Some Values.CreateVirtualGatewayOutput.error_to_json)])
let create_virtual_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and spec = flag "spec" (required json_arg) ~doc:"JSON VirtualNodeSpec"
       and virtualNodeName =
         flag "virtual-node-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_virtual_node
           (Values.CreateVirtualNodeInput.make ?clientToken ?meshOwner
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~meshName
              ~spec:(Values.VirtualNodeSpec.of_json spec) ~virtualNodeName ())
           (Some Values.CreateVirtualNodeOutput.to_json)
           (Some Values.CreateVirtualNodeOutput.error_to_json)])
let create_virtual_router =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and spec =
         flag "spec" (required json_arg) ~doc:"JSON VirtualRouterSpec"
       and virtualRouterName =
         flag "virtual-router-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_virtual_router
           (Values.CreateVirtualRouterInput.make ?clientToken ?meshOwner
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~meshName
              ~spec:(Values.VirtualRouterSpec.of_json spec)
              ~virtualRouterName ())
           (Some Values.CreateVirtualRouterOutput.to_json)
           (Some Values.CreateVirtualRouterOutput.error_to_json)])
let create_virtual_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and spec =
         flag "spec" (required json_arg) ~doc:"JSON VirtualServiceSpec"
       and virtualServiceName =
         flag "virtual-service-name" (required string)
           ~doc:"STRING ServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_virtual_service
           (Values.CreateVirtualServiceInput.make ?clientToken ?meshOwner
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~meshName
              ~spec:(Values.VirtualServiceSpec.of_json spec)
              ~virtualServiceName ())
           (Some Values.CreateVirtualServiceOutput.to_json)
           (Some Values.CreateVirtualServiceOutput.error_to_json)])
let delete_gateway_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and gatewayRouteName =
         flag "gateway-route-name" (required string)
           ~doc:"STRING ResourceName"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and virtualGatewayName =
         flag "virtual-gateway-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_gateway_route
           (Values.DeleteGatewayRouteInput.make ?meshOwner ~gatewayRouteName
              ~meshName ~virtualGatewayName ())
           (Some Values.DeleteGatewayRouteOutput.to_json)
           (Some Values.DeleteGatewayRouteOutput.error_to_json)])
let delete_mesh =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_mesh (Values.DeleteMeshInput.make ~meshName ())
           (Some Values.DeleteMeshOutput.to_json)
           (Some Values.DeleteMeshOutput.error_to_json)])
let delete_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and routeName =
         flag "route-name" (required string) ~doc:"STRING ResourceName"
       and virtualRouterName =
         flag "virtual-router-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route
           (Values.DeleteRouteInput.make ?meshOwner ~meshName ~routeName
              ~virtualRouterName ()) (Some Values.DeleteRouteOutput.to_json)
           (Some Values.DeleteRouteOutput.error_to_json)])
let delete_virtual_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and virtualGatewayName =
         flag "virtual-gateway-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_virtual_gateway
           (Values.DeleteVirtualGatewayInput.make ?meshOwner ~meshName
              ~virtualGatewayName ())
           (Some Values.DeleteVirtualGatewayOutput.to_json)
           (Some Values.DeleteVirtualGatewayOutput.error_to_json)])
let delete_virtual_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and virtualNodeName =
         flag "virtual-node-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_virtual_node
           (Values.DeleteVirtualNodeInput.make ?meshOwner ~meshName
              ~virtualNodeName ())
           (Some Values.DeleteVirtualNodeOutput.to_json)
           (Some Values.DeleteVirtualNodeOutput.error_to_json)])
let delete_virtual_router =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and virtualRouterName =
         flag "virtual-router-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_virtual_router
           (Values.DeleteVirtualRouterInput.make ?meshOwner ~meshName
              ~virtualRouterName ())
           (Some Values.DeleteVirtualRouterOutput.to_json)
           (Some Values.DeleteVirtualRouterOutput.error_to_json)])
let delete_virtual_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and virtualServiceName =
         flag "virtual-service-name" (required string)
           ~doc:"STRING ServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_virtual_service
           (Values.DeleteVirtualServiceInput.make ?meshOwner ~meshName
              ~virtualServiceName ())
           (Some Values.DeleteVirtualServiceOutput.to_json)
           (Some Values.DeleteVirtualServiceOutput.error_to_json)])
let describe_gateway_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and gatewayRouteName =
         flag "gateway-route-name" (required string)
           ~doc:"STRING ResourceName"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and virtualGatewayName =
         flag "virtual-gateway-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_gateway_route
           (Values.DescribeGatewayRouteInput.make ?meshOwner
              ~gatewayRouteName ~meshName ~virtualGatewayName ())
           (Some Values.DescribeGatewayRouteOutput.to_json)
           (Some Values.DescribeGatewayRouteOutput.error_to_json)])
let describe_mesh =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_mesh
           (Values.DescribeMeshInput.make ?meshOwner ~meshName ())
           (Some Values.DescribeMeshOutput.to_json)
           (Some Values.DescribeMeshOutput.error_to_json)])
let describe_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and routeName =
         flag "route-name" (required string) ~doc:"STRING ResourceName"
       and virtualRouterName =
         flag "virtual-router-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_route
           (Values.DescribeRouteInput.make ?meshOwner ~meshName ~routeName
              ~virtualRouterName ())
           (Some Values.DescribeRouteOutput.to_json)
           (Some Values.DescribeRouteOutput.error_to_json)])
let describe_virtual_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and virtualGatewayName =
         flag "virtual-gateway-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_virtual_gateway
           (Values.DescribeVirtualGatewayInput.make ?meshOwner ~meshName
              ~virtualGatewayName ())
           (Some Values.DescribeVirtualGatewayOutput.to_json)
           (Some Values.DescribeVirtualGatewayOutput.error_to_json)])
let describe_virtual_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and virtualNodeName =
         flag "virtual-node-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_virtual_node
           (Values.DescribeVirtualNodeInput.make ?meshOwner ~meshName
              ~virtualNodeName ())
           (Some Values.DescribeVirtualNodeOutput.to_json)
           (Some Values.DescribeVirtualNodeOutput.error_to_json)])
let describe_virtual_router =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and virtualRouterName =
         flag "virtual-router-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_virtual_router
           (Values.DescribeVirtualRouterInput.make ?meshOwner ~meshName
              ~virtualRouterName ())
           (Some Values.DescribeVirtualRouterOutput.to_json)
           (Some Values.DescribeVirtualRouterOutput.error_to_json)])
let describe_virtual_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and virtualServiceName =
         flag "virtual-service-name" (required string)
           ~doc:"STRING ServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_virtual_service
           (Values.DescribeVirtualServiceInput.make ?meshOwner ~meshName
              ~virtualServiceName ())
           (Some Values.DescribeVirtualServiceOutput.to_json)
           (Some Values.DescribeVirtualServiceOutput.error_to_json)])
let list_gateway_routes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit =
         flag "limit" (optional int) ~doc:"INT ListGatewayRoutesLimit"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and virtualGatewayName =
         flag "virtual-gateway-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_gateway_routes
           (Values.ListGatewayRoutesInput.make ?limit ?meshOwner ?nextToken
              ~meshName ~virtualGatewayName ())
           (Some Values.ListGatewayRoutesOutput.to_json)
           (Some Values.ListGatewayRoutesOutput.error_to_json)])
let list_meshes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT ListMeshesLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_meshes (Values.ListMeshesInput.make ?limit ?nextToken ())
           (Some Values.ListMeshesOutput.to_json)
           (Some Values.ListMeshesOutput.error_to_json)])
let list_routes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT ListRoutesLimit"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and virtualRouterName =
         flag "virtual-router-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_routes
           (Values.ListRoutesInput.make ?limit ?meshOwner ?nextToken
              ~meshName ~virtualRouterName ())
           (Some Values.ListRoutesOutput.to_json)
           (Some Values.ListRoutesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT TagsLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ?limit ?nextToken
              ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_virtual_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit =
         flag "limit" (optional int) ~doc:"INT ListVirtualGatewaysLimit"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_virtual_gateways
           (Values.ListVirtualGatewaysInput.make ?limit ?meshOwner ?nextToken
              ~meshName ()) (Some Values.ListVirtualGatewaysOutput.to_json)
           (Some Values.ListVirtualGatewaysOutput.error_to_json)])
let list_virtual_nodes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit =
         flag "limit" (optional int) ~doc:"INT ListVirtualNodesLimit"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_virtual_nodes
           (Values.ListVirtualNodesInput.make ?limit ?meshOwner ?nextToken
              ~meshName ()) (Some Values.ListVirtualNodesOutput.to_json)
           (Some Values.ListVirtualNodesOutput.error_to_json)])
let list_virtual_routers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit =
         flag "limit" (optional int) ~doc:"INT ListVirtualRoutersLimit"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_virtual_routers
           (Values.ListVirtualRoutersInput.make ?limit ?meshOwner ?nextToken
              ~meshName ()) (Some Values.ListVirtualRoutersOutput.to_json)
           (Some Values.ListVirtualRoutersOutput.error_to_json)])
let list_virtual_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit =
         flag "limit" (optional int) ~doc:"INT ListVirtualServicesLimit"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_virtual_services
           (Values.ListVirtualServicesInput.make ?limit ?meshOwner ?nextToken
              ~meshName ()) (Some Values.ListVirtualServicesOutput.to_json)
           (Some Values.ListVirtualServicesOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_gateway_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and gatewayRouteName =
         flag "gateway-route-name" (required string)
           ~doc:"STRING ResourceName"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and spec =
         flag "spec" (required json_arg) ~doc:"JSON GatewayRouteSpec"
       and virtualGatewayName =
         flag "virtual-gateway-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_gateway_route
           (Values.UpdateGatewayRouteInput.make ?clientToken ?meshOwner
              ~gatewayRouteName ~meshName
              ~spec:(Values.GatewayRouteSpec.of_json spec)
              ~virtualGatewayName ())
           (Some Values.UpdateGatewayRouteOutput.to_json)
           (Some Values.UpdateGatewayRouteOutput.error_to_json)])
let update_mesh =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and spec = flag "spec" (optional json_arg) ~doc:"JSON MeshSpec"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_mesh
           (Values.UpdateMeshInput.make ?clientToken
              ?spec:(Option.map ~f:Values.MeshSpec.of_json spec) ~meshName ())
           (Some Values.UpdateMeshOutput.to_json)
           (Some Values.UpdateMeshOutput.error_to_json)])
let update_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and routeName =
         flag "route-name" (required string) ~doc:"STRING ResourceName"
       and spec = flag "spec" (required json_arg) ~doc:"JSON RouteSpec"
       and virtualRouterName =
         flag "virtual-router-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_route
           (Values.UpdateRouteInput.make ?clientToken ?meshOwner ~meshName
              ~routeName ~spec:(Values.RouteSpec.of_json spec)
              ~virtualRouterName ()) (Some Values.UpdateRouteOutput.to_json)
           (Some Values.UpdateRouteOutput.error_to_json)])
let update_virtual_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and spec =
         flag "spec" (required json_arg) ~doc:"JSON VirtualGatewaySpec"
       and virtualGatewayName =
         flag "virtual-gateway-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_virtual_gateway
           (Values.UpdateVirtualGatewayInput.make ?clientToken ?meshOwner
              ~meshName ~spec:(Values.VirtualGatewaySpec.of_json spec)
              ~virtualGatewayName ())
           (Some Values.UpdateVirtualGatewayOutput.to_json)
           (Some Values.UpdateVirtualGatewayOutput.error_to_json)])
let update_virtual_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and spec = flag "spec" (required json_arg) ~doc:"JSON VirtualNodeSpec"
       and virtualNodeName =
         flag "virtual-node-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_virtual_node
           (Values.UpdateVirtualNodeInput.make ?clientToken ?meshOwner
              ~meshName ~spec:(Values.VirtualNodeSpec.of_json spec)
              ~virtualNodeName ())
           (Some Values.UpdateVirtualNodeOutput.to_json)
           (Some Values.UpdateVirtualNodeOutput.error_to_json)])
let update_virtual_router =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and spec =
         flag "spec" (required json_arg) ~doc:"JSON VirtualRouterSpec"
       and virtualRouterName =
         flag "virtual-router-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_virtual_router
           (Values.UpdateVirtualRouterInput.make ?clientToken ?meshOwner
              ~meshName ~spec:(Values.VirtualRouterSpec.of_json spec)
              ~virtualRouterName ())
           (Some Values.UpdateVirtualRouterOutput.to_json)
           (Some Values.UpdateVirtualRouterOutput.error_to_json)])
let update_virtual_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and meshOwner =
         flag "mesh-owner" (optional string) ~doc:"STRING AccountId"
       and meshName =
         flag "mesh-name" (required string) ~doc:"STRING ResourceName"
       and spec =
         flag "spec" (required json_arg) ~doc:"JSON VirtualServiceSpec"
       and virtualServiceName =
         flag "virtual-service-name" (required string)
           ~doc:"STRING ServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_virtual_service
           (Values.UpdateVirtualServiceInput.make ?clientToken ?meshOwner
              ~meshName ~spec:(Values.VirtualServiceSpec.of_json spec)
              ~virtualServiceName ())
           (Some Values.UpdateVirtualServiceOutput.to_json)
           (Some Values.UpdateVirtualServiceOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-gateway-route", create_gateway_route);
    ("create-mesh", create_mesh);
    ("create-route", create_route);
    ("create-virtual-gateway", create_virtual_gateway);
    ("create-virtual-node", create_virtual_node);
    ("create-virtual-router", create_virtual_router);
    ("create-virtual-service", create_virtual_service);
    ("delete-gateway-route", delete_gateway_route);
    ("delete-mesh", delete_mesh);
    ("delete-route", delete_route);
    ("delete-virtual-gateway", delete_virtual_gateway);
    ("delete-virtual-node", delete_virtual_node);
    ("delete-virtual-router", delete_virtual_router);
    ("delete-virtual-service", delete_virtual_service);
    ("describe-gateway-route", describe_gateway_route);
    ("describe-mesh", describe_mesh);
    ("describe-route", describe_route);
    ("describe-virtual-gateway", describe_virtual_gateway);
    ("describe-virtual-node", describe_virtual_node);
    ("describe-virtual-router", describe_virtual_router);
    ("describe-virtual-service", describe_virtual_service);
    ("list-gateway-routes", list_gateway_routes);
    ("list-meshes", list_meshes);
    ("list-routes", list_routes);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-virtual-gateways", list_virtual_gateways);
    ("list-virtual-nodes", list_virtual_nodes);
    ("list-virtual-routers", list_virtual_routers);
    ("list-virtual-services", list_virtual_services);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-gateway-route", update_gateway_route);
    ("update-mesh", update_mesh);
    ("update-route", update_route);
    ("update-virtual-gateway", update_virtual_gateway);
    ("update-virtual-node", update_virtual_node);
    ("update-virtual-router", update_virtual_router);
    ("update-virtual-service", update_virtual_service)]
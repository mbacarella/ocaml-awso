(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_bill_of_materials_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and s3uri =
         flag "s3uri" (required string) ~doc:"STRING ConfigurationS3Uri" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bill_of_materials_import_job
           (Values.CreateBillOfMaterialsImportJobRequest.make ?clientToken
              ~instanceId ~s3uri ())
           (Some Values.CreateBillOfMaterialsImportJobResponse.to_json)
           (Some Values.CreateBillOfMaterialsImportJobResponse.error_to_json)])
let create_data_integration_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and name =
         flag "name" (required string) ~doc:"STRING DataIntegrationFlowName"
       and sources =
         flag "sources" (required json_arg)
           ~doc:"JSON DataIntegrationFlowSourceList"
       and transformation =
         flag "transformation" (required json_arg)
           ~doc:"JSON DataIntegrationFlowTransformation"
       and target =
         flag "target" (required json_arg)
           ~doc:"JSON DataIntegrationFlowTarget" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_integration_flow
           (Values.CreateDataIntegrationFlowRequest.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~name
              ~sources:(Values.DataIntegrationFlowSourceList.of_json sources)
              ~transformation:(Values.DataIntegrationFlowTransformation.of_json
                                 transformation)
              ~target:(Values.DataIntegrationFlowTarget.of_json target) ())
           (Some Values.CreateDataIntegrationFlowResponse.to_json)
           (Some Values.CreateDataIntegrationFlowResponse.error_to_json)])
let create_data_lake_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schema =
         flag "schema" (optional json_arg) ~doc:"JSON DataLakeDatasetSchema"
       and description =
         flag "description" (optional string)
           ~doc:"STRING DataLakeDatasetDescription"
       and partitionSpec =
         flag "partition-spec" (optional json_arg)
           ~doc:"JSON DataLakeDatasetPartitionSpec"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and namespace =
         flag "namespace" (required string)
           ~doc:"STRING DataLakeNamespaceName"
       and name =
         flag "name" (required string) ~doc:"STRING DataLakeDatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_lake_dataset
           (Values.CreateDataLakeDatasetRequest.make
              ?schema:(Option.map ~f:Values.DataLakeDatasetSchema.of_json
                         schema) ?description
              ?partitionSpec:(Option.map
                                ~f:Values.DataLakeDatasetPartitionSpec.of_json
                                partitionSpec)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~namespace ~name ())
           (Some Values.CreateDataLakeDatasetResponse.to_json)
           (Some Values.CreateDataLakeDatasetResponse.error_to_json)])
let create_data_lake_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING DataLakeNamespaceDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and name =
         flag "name" (required string) ~doc:"STRING DataLakeNamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_lake_namespace
           (Values.CreateDataLakeNamespaceRequest.make ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~name ()) (Some Values.CreateDataLakeNamespaceResponse.to_json)
           (Some Values.CreateDataLakeNamespaceResponse.error_to_json)])
let create_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceName =
         flag "instance-name" (optional string) ~doc:"STRING InstanceName"
       and instanceDescription =
         flag "instance-description" (optional string)
           ~doc:"STRING InstanceDescription"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and webAppDnsDomain =
         flag "web-app-dns-domain" (optional string)
           ~doc:"STRING InstanceWebAppDnsDomain"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_instance
           (Values.CreateInstanceRequest.make ?instanceName
              ?instanceDescription ?kmsKeyArn ?webAppDnsDomain
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?clientToken
              ()) (Some Values.CreateInstanceResponse.to_json)
           (Some Values.CreateInstanceResponse.error_to_json)])
let delete_data_integration_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and name =
         flag "name" (required string) ~doc:"STRING DataIntegrationFlowName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_integration_flow
           (Values.DeleteDataIntegrationFlowRequest.make ~instanceId ~name ())
           (Some Values.DeleteDataIntegrationFlowResponse.to_json)
           (Some Values.DeleteDataIntegrationFlowResponse.error_to_json)])
let delete_data_lake_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and namespace =
         flag "namespace" (required string)
           ~doc:"STRING DataLakeNamespaceName"
       and name =
         flag "name" (required string) ~doc:"STRING DataLakeDatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_lake_dataset
           (Values.DeleteDataLakeDatasetRequest.make ~instanceId ~namespace
              ~name ()) (Some Values.DeleteDataLakeDatasetResponse.to_json)
           (Some Values.DeleteDataLakeDatasetResponse.error_to_json)])
let delete_data_lake_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and name =
         flag "name" (required string) ~doc:"STRING DataLakeNamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_lake_namespace
           (Values.DeleteDataLakeNamespaceRequest.make ~instanceId ~name ())
           (Some Values.DeleteDataLakeNamespaceResponse.to_json)
           (Some Values.DeleteDataLakeNamespaceResponse.error_to_json)])
let delete_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_instance
           (Values.DeleteInstanceRequest.make ~instanceId ())
           (Some Values.DeleteInstanceResponse.to_json)
           (Some Values.DeleteInstanceResponse.error_to_json)])
let get_bill_of_materials_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and jobId = flag "job-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bill_of_materials_import_job
           (Values.GetBillOfMaterialsImportJobRequest.make ~instanceId ~jobId
              ()) (Some Values.GetBillOfMaterialsImportJobResponse.to_json)
           (Some Values.GetBillOfMaterialsImportJobResponse.error_to_json)])
let get_data_integration_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and eventId = flag "event-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_integration_event
           (Values.GetDataIntegrationEventRequest.make ~instanceId ~eventId
              ()) (Some Values.GetDataIntegrationEventResponse.to_json)
           (Some Values.GetDataIntegrationEventResponse.error_to_json)])
let get_data_integration_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and name =
         flag "name" (required string) ~doc:"STRING DataIntegrationFlowName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_integration_flow
           (Values.GetDataIntegrationFlowRequest.make ~instanceId ~name ())
           (Some Values.GetDataIntegrationFlowResponse.to_json)
           (Some Values.GetDataIntegrationFlowResponse.error_to_json)])
let get_data_integration_flow_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and flowName =
         flag "flow-name" (required string)
           ~doc:"STRING DataIntegrationFlowName"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_integration_flow_execution
           (Values.GetDataIntegrationFlowExecutionRequest.make ~instanceId
              ~flowName ~executionId ())
           (Some Values.GetDataIntegrationFlowExecutionResponse.to_json)
           (Some Values.GetDataIntegrationFlowExecutionResponse.error_to_json)])
let get_data_lake_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and namespace =
         flag "namespace" (required string)
           ~doc:"STRING DataLakeNamespaceName"
       and name =
         flag "name" (required string) ~doc:"STRING DataLakeDatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_lake_dataset
           (Values.GetDataLakeDatasetRequest.make ~instanceId ~namespace
              ~name ()) (Some Values.GetDataLakeDatasetResponse.to_json)
           (Some Values.GetDataLakeDatasetResponse.error_to_json)])
let get_data_lake_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and name =
         flag "name" (required string) ~doc:"STRING DataLakeNamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_lake_namespace
           (Values.GetDataLakeNamespaceRequest.make ~instanceId ~name ())
           (Some Values.GetDataLakeNamespaceResponse.to_json)
           (Some Values.GetDataLakeNamespaceResponse.error_to_json)])
let get_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance (Values.GetInstanceRequest.make ~instanceId ())
           (Some Values.GetInstanceResponse.to_json)
           (Some Values.GetInstanceResponse.error_to_json)])
let list_data_integration_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventType =
         flag "event-type" (optional json_arg)
           ~doc:"JSON DataIntegrationEventType"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING DataIntegrationEventNextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DataIntegrationEventMaxResults"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_integration_events
           (Values.ListDataIntegrationEventsRequest.make
              ?eventType:(Option.map
                            ~f:Values.DataIntegrationEventType.of_json
                            eventType) ?nextToken ?maxResults ~instanceId ())
           (Some Values.ListDataIntegrationEventsResponse.to_json)
           (Some Values.ListDataIntegrationEventsResponse.error_to_json)])
let list_data_integration_flow_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING DataIntegrationFlowExecutionNextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DataIntegrationFlowExecutionMaxResults"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and flowName =
         flag "flow-name" (required string)
           ~doc:"STRING DataIntegrationFlowName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_integration_flow_executions
           (Values.ListDataIntegrationFlowExecutionsRequest.make ?nextToken
              ?maxResults ~instanceId ~flowName ())
           (Some Values.ListDataIntegrationFlowExecutionsResponse.to_json)
           (Some
              Values.ListDataIntegrationFlowExecutionsResponse.error_to_json)])
let list_data_integration_flows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING DataIntegrationFlowNextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DataIntegrationFlowMaxResults"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_integration_flows
           (Values.ListDataIntegrationFlowsRequest.make ?nextToken
              ?maxResults ~instanceId ())
           (Some Values.ListDataIntegrationFlowsResponse.to_json)
           (Some Values.ListDataIntegrationFlowsResponse.error_to_json)])
let list_data_lake_datasets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING DataLakeDatasetNextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DataLakeDatasetMaxResults"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and namespace =
         flag "namespace" (required string)
           ~doc:"STRING DataLakeNamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_lake_datasets
           (Values.ListDataLakeDatasetsRequest.make ?nextToken ?maxResults
              ~instanceId ~namespace ())
           (Some Values.ListDataLakeDatasetsResponse.to_json)
           (Some Values.ListDataLakeDatasetsResponse.error_to_json)])
let list_data_lake_namespaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING DataLakeNamespaceNextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DataLakeNamespaceMaxResults"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_lake_namespaces
           (Values.ListDataLakeNamespacesRequest.make ?nextToken ?maxResults
              ~instanceId ())
           (Some Values.ListDataLakeNamespacesResponse.to_json)
           (Some Values.ListDataLakeNamespacesResponse.error_to_json)])
let list_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING InstanceNextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT InstanceMaxResults"
       and instanceNameFilter =
         flag "instance-name-filter" (optional json_arg)
           ~doc:"JSON InstanceNameList"
       and instanceStateFilter =
         flag "instance-state-filter" (optional json_arg)
           ~doc:"JSON InstanceStateList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_instances
           (Values.ListInstancesRequest.make ?nextToken ?maxResults
              ?instanceNameFilter:(Option.map
                                     ~f:Values.InstanceNameList.of_json
                                     instanceNameFilter)
              ?instanceStateFilter:(Option.map
                                      ~f:Values.InstanceStateList.of_json
                                      instanceStateFilter) ())
           (Some Values.ListInstancesResponse.to_json)
           (Some Values.ListInstancesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING AscResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let send_data_integration_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventTimestamp =
         flag "event-timestamp" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_epoch_seconds"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and datasetTarget =
         flag "dataset-target" (optional json_arg)
           ~doc:"JSON DataIntegrationEventDatasetTargetConfiguration"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and eventType =
         flag "event-type" (required json_arg)
           ~doc:"JSON DataIntegrationEventType"
       and data =
         flag "data" (required string) ~doc:"STRING DataIntegrationEventData"
       and eventGroupId =
         flag "event-group-id" (required string)
           ~doc:"STRING DataIntegrationEventGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_data_integration_event
           (Values.SendDataIntegrationEventRequest.make
              ?eventTimestamp:(Option.map
                                 ~f:Values.SyntheticTimestamp_epoch_seconds.of_json
                                 eventTimestamp) ?clientToken
              ?datasetTarget:(Option.map
                                ~f:Values.DataIntegrationEventDatasetTargetConfiguration.of_json
                                datasetTarget) ~instanceId
              ~eventType:(Values.DataIntegrationEventType.of_json eventType)
              ~data ~eventGroupId ())
           (Some Values.SendDataIntegrationEventResponse.to_json)
           (Some Values.SendDataIntegrationEventResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING AscResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING AscResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_data_integration_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sources =
         flag "sources" (optional json_arg)
           ~doc:"JSON DataIntegrationFlowSourceList"
       and transformation =
         flag "transformation" (optional json_arg)
           ~doc:"JSON DataIntegrationFlowTransformation"
       and target =
         flag "target" (optional json_arg)
           ~doc:"JSON DataIntegrationFlowTarget"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and name =
         flag "name" (required string) ~doc:"STRING DataIntegrationFlowName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_integration_flow
           (Values.UpdateDataIntegrationFlowRequest.make
              ?sources:(Option.map
                          ~f:Values.DataIntegrationFlowSourceList.of_json
                          sources)
              ?transformation:(Option.map
                                 ~f:Values.DataIntegrationFlowTransformation.of_json
                                 transformation)
              ?target:(Option.map ~f:Values.DataIntegrationFlowTarget.of_json
                         target) ~instanceId ~name ())
           (Some Values.UpdateDataIntegrationFlowResponse.to_json)
           (Some Values.UpdateDataIntegrationFlowResponse.error_to_json)])
let update_data_lake_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING DataLakeDatasetDescription"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and namespace =
         flag "namespace" (required string)
           ~doc:"STRING DataLakeNamespaceName"
       and name =
         flag "name" (required string) ~doc:"STRING DataLakeDatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_lake_dataset
           (Values.UpdateDataLakeDatasetRequest.make ?description ~instanceId
              ~namespace ~name ())
           (Some Values.UpdateDataLakeDatasetResponse.to_json)
           (Some Values.UpdateDataLakeDatasetResponse.error_to_json)])
let update_data_lake_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING DataLakeNamespaceDescription"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID"
       and name =
         flag "name" (required string) ~doc:"STRING DataLakeNamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_lake_namespace
           (Values.UpdateDataLakeNamespaceRequest.make ?description
              ~instanceId ~name ())
           (Some Values.UpdateDataLakeNamespaceResponse.to_json)
           (Some Values.UpdateDataLakeNamespaceResponse.error_to_json)])
let update_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceName =
         flag "instance-name" (optional string) ~doc:"STRING InstanceName"
       and instanceDescription =
         flag "instance-description" (optional string)
           ~doc:"STRING InstanceDescription"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_instance
           (Values.UpdateInstanceRequest.make ?instanceName
              ?instanceDescription ~instanceId ())
           (Some Values.UpdateInstanceResponse.to_json)
           (Some Values.UpdateInstanceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-bill-of-materials-import-job",
       create_bill_of_materials_import_job);
    ("create-data-integration-flow", create_data_integration_flow);
    ("create-data-lake-dataset", create_data_lake_dataset);
    ("create-data-lake-namespace", create_data_lake_namespace);
    ("create-instance", create_instance);
    ("delete-data-integration-flow", delete_data_integration_flow);
    ("delete-data-lake-dataset", delete_data_lake_dataset);
    ("delete-data-lake-namespace", delete_data_lake_namespace);
    ("delete-instance", delete_instance);
    ("get-bill-of-materials-import-job", get_bill_of_materials_import_job);
    ("get-data-integration-event", get_data_integration_event);
    ("get-data-integration-flow", get_data_integration_flow);
    ("get-data-integration-flow-execution",
      get_data_integration_flow_execution);
    ("get-data-lake-dataset", get_data_lake_dataset);
    ("get-data-lake-namespace", get_data_lake_namespace);
    ("get-instance", get_instance);
    ("list-data-integration-events", list_data_integration_events);
    ("list-data-integration-flow-executions",
      list_data_integration_flow_executions);
    ("list-data-integration-flows", list_data_integration_flows);
    ("list-data-lake-datasets", list_data_lake_datasets);
    ("list-data-lake-namespaces", list_data_lake_namespaces);
    ("list-instances", list_instances);
    ("list-tags-for-resource", list_tags_for_resource);
    ("send-data-integration-event", send_data_integration_event);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-data-integration-flow", update_data_integration_flow);
    ("update-data-lake-dataset", update_data_lake_dataset);
    ("update-data-lake-namespace", update_data_lake_namespace);
    ("update-instance", update_instance)]
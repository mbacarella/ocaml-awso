(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_environment_account_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string)
           ~doc:"STRING EnvironmentAccountConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_environment_account_connection
           (Values.AcceptEnvironmentAccountConnectionInput.make ~id ())
           (Some Values.AcceptEnvironmentAccountConnectionOutput.to_json)
           (Some
              Values.AcceptEnvironmentAccountConnectionOutput.error_to_json)])
let cancel_environment_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_environment_deployment
           (Values.CancelEnvironmentDeploymentInput.make ~environmentName ())
           (Some Values.CancelEnvironmentDeploymentOutput.to_json)
           (Some Values.CancelEnvironmentDeploymentOutput.error_to_json)])
let cancel_service_instance_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceInstanceName =
         flag "service-instance-name" (required string)
           ~doc:"STRING ResourceName"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_service_instance_deployment
           (Values.CancelServiceInstanceDeploymentInput.make
              ~serviceInstanceName ~serviceName ())
           (Some Values.CancelServiceInstanceDeploymentOutput.to_json)
           (Some Values.CancelServiceInstanceDeploymentOutput.error_to_json)])
let cancel_service_pipeline_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_service_pipeline_deployment
           (Values.CancelServicePipelineDeploymentInput.make ~serviceName ())
           (Some Values.CancelServicePipelineDeploymentOutput.to_json)
           (Some Values.CancelServicePipelineDeploymentOutput.error_to_json)])
let create_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and environmentAccountConnectionId =
         flag "environment-account-connection-id" (optional string)
           ~doc:"STRING EnvironmentAccountConnectionId"
       and protonServiceRoleArn =
         flag "proton-service-role-arn" (optional string) ~doc:"STRING Arn"
       and provisioningRepository =
         flag "provisioning-repository" (optional json_arg)
           ~doc:"JSON RepositoryBranchInput"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and templateMinorVersion =
         flag "template-minor-version" (optional string)
           ~doc:"STRING TemplateVersionPart"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and spec = flag "spec" (required string) ~doc:"STRING SpecContents"
       and templateMajorVersion =
         flag "template-major-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment
           (Values.CreateEnvironmentInput.make ?description
              ?environmentAccountConnectionId ?protonServiceRoleArn
              ?provisioningRepository:(Option.map
                                         ~f:Values.RepositoryBranchInput.of_json
                                         provisioningRepository)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?templateMinorVersion ~name ~spec ~templateMajorVersion
              ~templateName ()) (Some Values.CreateEnvironmentOutput.to_json)
           (Some Values.CreateEnvironmentOutput.error_to_json)])
let create_environment_account_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING ResourceName"
       and managementAccountId =
         flag "management-account-id" (required string)
           ~doc:"STRING AwsAccountId"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment_account_connection
           (Values.CreateEnvironmentAccountConnectionInput.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~environmentName ~managementAccountId ~roleArn ())
           (Some Values.CreateEnvironmentAccountConnectionOutput.to_json)
           (Some
              Values.CreateEnvironmentAccountConnectionOutput.error_to_json)])
let create_environment_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and encryptionKey =
         flag "encryption-key" (optional string) ~doc:"STRING Arn"
       and provisioning =
         flag "provisioning" (optional json_arg) ~doc:"JSON Provisioning"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment_template
           (Values.CreateEnvironmentTemplateInput.make ?description
              ?displayName ?encryptionKey
              ?provisioning:(Option.map ~f:Values.Provisioning.of_json
                               provisioning)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.CreateEnvironmentTemplateOutput.to_json)
           (Some Values.CreateEnvironmentTemplateOutput.error_to_json)])
let create_environment_template_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and majorVersion =
         flag "major-version" (optional string)
           ~doc:"STRING TemplateVersionPart"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and source =
         flag "source" (required json_arg)
           ~doc:"JSON TemplateVersionSourceInput"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment_template_version
           (Values.CreateEnvironmentTemplateVersionInput.make ?clientToken
              ?description ?majorVersion
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~source:(Values.TemplateVersionSourceInput.of_json source)
              ~templateName ())
           (Some Values.CreateEnvironmentTemplateVersionOutput.to_json)
           (Some Values.CreateEnvironmentTemplateVersionOutput.error_to_json)])
let create_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionKey =
         flag "encryption-key" (optional string) ~doc:"STRING Arn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and connectionArn =
         flag "connection-arn" (required string) ~doc:"STRING Arn"
       and name = flag "name" (required string) ~doc:"STRING RepositoryName"
       and provider =
         flag "provider" (required json_arg) ~doc:"JSON RepositoryProvider" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_repository
           (Values.CreateRepositoryInput.make ?encryptionKey
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~connectionArn ~name
              ~provider:(Values.RepositoryProvider.of_json provider) ())
           (Some Values.CreateRepositoryOutput.to_json)
           (Some Values.CreateRepositoryOutput.error_to_json)])
let create_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and branchName =
         flag "branch-name" (optional string) ~doc:"STRING GitBranchName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and repositoryConnectionArn =
         flag "repository-connection-arn" (optional string) ~doc:"STRING Arn"
       and repositoryId =
         flag "repository-id" (optional string) ~doc:"STRING RepositoryId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and templateMinorVersion =
         flag "template-minor-version" (optional string)
           ~doc:"STRING TemplateVersionPart"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and spec = flag "spec" (required string) ~doc:"STRING SpecContents"
       and templateMajorVersion =
         flag "template-major-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service
           (Values.CreateServiceInput.make ?branchName ?description
              ?repositoryConnectionArn ?repositoryId
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?templateMinorVersion ~name ~spec ~templateMajorVersion
              ~templateName ()) (Some Values.CreateServiceOutput.to_json)
           (Some Values.CreateServiceOutput.error_to_json)])
let create_service_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and encryptionKey =
         flag "encryption-key" (optional string) ~doc:"STRING Arn"
       and pipelineProvisioning =
         flag "pipeline-provisioning" (optional json_arg)
           ~doc:"JSON Provisioning"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service_template
           (Values.CreateServiceTemplateInput.make ?description ?displayName
              ?encryptionKey
              ?pipelineProvisioning:(Option.map
                                       ~f:Values.Provisioning.of_json
                                       pipelineProvisioning)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.CreateServiceTemplateOutput.to_json)
           (Some Values.CreateServiceTemplateOutput.error_to_json)])
let create_service_template_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and majorVersion =
         flag "major-version" (optional string)
           ~doc:"STRING TemplateVersionPart"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and compatibleEnvironmentTemplates =
         flag "compatible-environment-templates" (required json_arg)
           ~doc:"JSON CompatibleEnvironmentTemplateInputList"
       and source =
         flag "source" (required json_arg)
           ~doc:"JSON TemplateVersionSourceInput"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service_template_version
           (Values.CreateServiceTemplateVersionInput.make ?clientToken
              ?description ?majorVersion
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~compatibleEnvironmentTemplates:(Values.CompatibleEnvironmentTemplateInputList.of_json
                                                 compatibleEnvironmentTemplates)
              ~source:(Values.TemplateVersionSourceInput.of_json source)
              ~templateName ())
           (Some Values.CreateServiceTemplateVersionOutput.to_json)
           (Some Values.CreateServiceTemplateVersionOutput.error_to_json)])
let create_template_sync_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING Subdirectory"
       and branch =
         flag "branch" (required string) ~doc:"STRING GitBranchName"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and repositoryProvider =
         flag "repository-provider" (required json_arg)
           ~doc:"JSON RepositoryProvider"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName"
       and templateType =
         flag "template-type" (required json_arg) ~doc:"JSON TemplateType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_template_sync_config
           (Values.CreateTemplateSyncConfigInput.make ?subdirectory ~branch
              ~repositoryName
              ~repositoryProvider:(Values.RepositoryProvider.of_json
                                     repositoryProvider) ~templateName
              ~templateType:(Values.TemplateType.of_json templateType) ())
           (Some Values.CreateTemplateSyncConfigOutput.to_json)
           (Some Values.CreateTemplateSyncConfigOutput.error_to_json)])
let delete_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment
           (Values.DeleteEnvironmentInput.make ~name ())
           (Some Values.DeleteEnvironmentOutput.to_json)
           (Some Values.DeleteEnvironmentOutput.error_to_json)])
let delete_environment_account_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string)
           ~doc:"STRING EnvironmentAccountConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment_account_connection
           (Values.DeleteEnvironmentAccountConnectionInput.make ~id ())
           (Some Values.DeleteEnvironmentAccountConnectionOutput.to_json)
           (Some
              Values.DeleteEnvironmentAccountConnectionOutput.error_to_json)])
let delete_environment_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment_template
           (Values.DeleteEnvironmentTemplateInput.make ~name ())
           (Some Values.DeleteEnvironmentTemplateOutput.to_json)
           (Some Values.DeleteEnvironmentTemplateOutput.error_to_json)])
let delete_environment_template_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and majorVersion =
         flag "major-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and minorVersion =
         flag "minor-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment_template_version
           (Values.DeleteEnvironmentTemplateVersionInput.make ~majorVersion
              ~minorVersion ~templateName ())
           (Some Values.DeleteEnvironmentTemplateVersionOutput.to_json)
           (Some Values.DeleteEnvironmentTemplateVersionOutput.error_to_json)])
let delete_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING RepositoryName"
       and provider =
         flag "provider" (required json_arg) ~doc:"JSON RepositoryProvider" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_repository
           (Values.DeleteRepositoryInput.make ~name
              ~provider:(Values.RepositoryProvider.of_json provider) ())
           (Some Values.DeleteRepositoryOutput.to_json)
           (Some Values.DeleteRepositoryOutput.error_to_json)])
let delete_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service (Values.DeleteServiceInput.make ~name ())
           (Some Values.DeleteServiceOutput.to_json)
           (Some Values.DeleteServiceOutput.error_to_json)])
let delete_service_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service_template
           (Values.DeleteServiceTemplateInput.make ~name ())
           (Some Values.DeleteServiceTemplateOutput.to_json)
           (Some Values.DeleteServiceTemplateOutput.error_to_json)])
let delete_service_template_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and majorVersion =
         flag "major-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and minorVersion =
         flag "minor-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service_template_version
           (Values.DeleteServiceTemplateVersionInput.make ~majorVersion
              ~minorVersion ~templateName ())
           (Some Values.DeleteServiceTemplateVersionOutput.to_json)
           (Some Values.DeleteServiceTemplateVersionOutput.error_to_json)])
let delete_template_sync_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName"
       and templateType =
         flag "template-type" (required json_arg) ~doc:"JSON TemplateType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_template_sync_config
           (Values.DeleteTemplateSyncConfigInput.make ~templateName
              ~templateType:(Values.TemplateType.of_json templateType) ())
           (Some Values.DeleteTemplateSyncConfigOutput.to_json)
           (Some Values.DeleteTemplateSyncConfigOutput.error_to_json)])
let get_account_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_settings (Values.GetAccountSettingsInput.make ())
           (Some Values.GetAccountSettingsOutput.to_json)
           (Some Values.GetAccountSettingsOutput.error_to_json)])
let get_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment (Values.GetEnvironmentInput.make ~name ())
           (Some Values.GetEnvironmentOutput.to_json)
           (Some Values.GetEnvironmentOutput.error_to_json)])
let get_environment_account_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string)
           ~doc:"STRING EnvironmentAccountConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment_account_connection
           (Values.GetEnvironmentAccountConnectionInput.make ~id ())
           (Some Values.GetEnvironmentAccountConnectionOutput.to_json)
           (Some Values.GetEnvironmentAccountConnectionOutput.error_to_json)])
let get_environment_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment_template
           (Values.GetEnvironmentTemplateInput.make ~name ())
           (Some Values.GetEnvironmentTemplateOutput.to_json)
           (Some Values.GetEnvironmentTemplateOutput.error_to_json)])
let get_environment_template_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and majorVersion =
         flag "major-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and minorVersion =
         flag "minor-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment_template_version
           (Values.GetEnvironmentTemplateVersionInput.make ~majorVersion
              ~minorVersion ~templateName ())
           (Some Values.GetEnvironmentTemplateVersionOutput.to_json)
           (Some Values.GetEnvironmentTemplateVersionOutput.error_to_json)])
let get_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING RepositoryName"
       and provider =
         flag "provider" (required json_arg) ~doc:"JSON RepositoryProvider" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_repository
           (Values.GetRepositoryInput.make ~name
              ~provider:(Values.RepositoryProvider.of_json provider) ())
           (Some Values.GetRepositoryOutput.to_json)
           (Some Values.GetRepositoryOutput.error_to_json)])
let get_repository_sync_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and branch =
         flag "branch" (required string) ~doc:"STRING GitBranchName"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and repositoryProvider =
         flag "repository-provider" (required json_arg)
           ~doc:"JSON RepositoryProvider"
       and syncType =
         flag "sync-type" (required json_arg) ~doc:"JSON SyncType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_repository_sync_status
           (Values.GetRepositorySyncStatusInput.make ~branch ~repositoryName
              ~repositoryProvider:(Values.RepositoryProvider.of_json
                                     repositoryProvider)
              ~syncType:(Values.SyncType.of_json syncType) ())
           (Some Values.GetRepositorySyncStatusOutput.to_json)
           (Some Values.GetRepositorySyncStatusOutput.error_to_json)])
let get_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service (Values.GetServiceInput.make ~name ())
           (Some Values.GetServiceOutput.to_json)
           (Some Values.GetServiceOutput.error_to_json)])
let get_service_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_instance
           (Values.GetServiceInstanceInput.make ~name ~serviceName ())
           (Some Values.GetServiceInstanceOutput.to_json)
           (Some Values.GetServiceInstanceOutput.error_to_json)])
let get_service_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_template
           (Values.GetServiceTemplateInput.make ~name ())
           (Some Values.GetServiceTemplateOutput.to_json)
           (Some Values.GetServiceTemplateOutput.error_to_json)])
let get_service_template_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and majorVersion =
         flag "major-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and minorVersion =
         flag "minor-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_template_version
           (Values.GetServiceTemplateVersionInput.make ~majorVersion
              ~minorVersion ~templateName ())
           (Some Values.GetServiceTemplateVersionOutput.to_json)
           (Some Values.GetServiceTemplateVersionOutput.error_to_json)])
let get_template_sync_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName"
       and templateType =
         flag "template-type" (required json_arg) ~doc:"JSON TemplateType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_template_sync_config
           (Values.GetTemplateSyncConfigInput.make ~templateName
              ~templateType:(Values.TemplateType.of_json templateType) ())
           (Some Values.GetTemplateSyncConfigOutput.to_json)
           (Some Values.GetTemplateSyncConfigOutput.error_to_json)])
let get_template_sync_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName"
       and templateType =
         flag "template-type" (required json_arg) ~doc:"JSON TemplateType"
       and templateVersion =
         flag "template-version" (required string)
           ~doc:"STRING TemplateVersionPart" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_template_sync_status
           (Values.GetTemplateSyncStatusInput.make ~templateName
              ~templateType:(Values.TemplateType.of_json templateType)
              ~templateVersion ())
           (Some Values.GetTemplateSyncStatusOutput.to_json)
           (Some Values.GetTemplateSyncStatusOutput.error_to_json)])
let list_environment_account_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentName =
         flag "environment-name" (optional string) ~doc:"STRING ResourceName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPageResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and statuses =
         flag "statuses" (optional json_arg)
           ~doc:"JSON EnvironmentAccountConnectionStatusList"
       and requestedBy =
         flag "requested-by" (required json_arg)
           ~doc:"JSON EnvironmentAccountConnectionRequesterAccountType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environment_account_connections
           (Values.ListEnvironmentAccountConnectionsInput.make
              ?environmentName ?maxResults ?nextToken
              ?statuses:(Option.map
                           ~f:Values.EnvironmentAccountConnectionStatusList.of_json
                           statuses)
              ~requestedBy:(Values.EnvironmentAccountConnectionRequesterAccountType.of_json
                              requestedBy) ())
           (Some Values.ListEnvironmentAccountConnectionsOutput.to_json)
           (Some Values.ListEnvironmentAccountConnectionsOutput.error_to_json)])
let list_environment_outputs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING EmptyNextToken"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environment_outputs
           (Values.ListEnvironmentOutputsInput.make ?nextToken
              ~environmentName ())
           (Some Values.ListEnvironmentOutputsOutput.to_json)
           (Some Values.ListEnvironmentOutputsOutput.error_to_json)])
let list_environment_provisioned_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING EmptyNextToken"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environment_provisioned_resources
           (Values.ListEnvironmentProvisionedResourcesInput.make ?nextToken
              ~environmentName ())
           (Some Values.ListEnvironmentProvisionedResourcesOutput.to_json)
           (Some
              Values.ListEnvironmentProvisionedResourcesOutput.error_to_json)])
let list_environment_template_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and majorVersion =
         flag "major-version" (optional string)
           ~doc:"STRING TemplateVersionPart"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPageResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environment_template_versions
           (Values.ListEnvironmentTemplateVersionsInput.make ?majorVersion
              ?maxResults ?nextToken ~templateName ())
           (Some Values.ListEnvironmentTemplateVersionsOutput.to_json)
           (Some Values.ListEnvironmentTemplateVersionsOutput.error_to_json)])
let list_environment_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPageResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environment_templates
           (Values.ListEnvironmentTemplatesInput.make ?maxResults ?nextToken
              ()) (Some Values.ListEnvironmentTemplatesOutput.to_json)
           (Some Values.ListEnvironmentTemplatesOutput.error_to_json)])
let list_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentTemplates =
         flag "environment-templates" (optional json_arg)
           ~doc:"JSON EnvironmentTemplateFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPageResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environments
           (Values.ListEnvironmentsInput.make
              ?environmentTemplates:(Option.map
                                       ~f:Values.EnvironmentTemplateFilterList.of_json
                                       environmentTemplates) ?maxResults
              ?nextToken ()) (Some Values.ListEnvironmentsOutput.to_json)
           (Some Values.ListEnvironmentsOutput.error_to_json)])
let list_repositories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPageResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_repositories
           (Values.ListRepositoriesInput.make ?maxResults ?nextToken ())
           (Some Values.ListRepositoriesOutput.to_json)
           (Some Values.ListRepositoriesOutput.error_to_json)])
let list_repository_sync_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING EmptyNextToken"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and repositoryProvider =
         flag "repository-provider" (required json_arg)
           ~doc:"JSON RepositoryProvider"
       and syncType =
         flag "sync-type" (required json_arg) ~doc:"JSON SyncType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_repository_sync_definitions
           (Values.ListRepositorySyncDefinitionsInput.make ?nextToken
              ~repositoryName
              ~repositoryProvider:(Values.RepositoryProvider.of_json
                                     repositoryProvider)
              ~syncType:(Values.SyncType.of_json syncType) ())
           (Some Values.ListRepositorySyncDefinitionsOutput.to_json)
           (Some Values.ListRepositorySyncDefinitionsOutput.error_to_json)])
let list_service_instance_outputs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING EmptyNextToken"
       and serviceInstanceName =
         flag "service-instance-name" (required string)
           ~doc:"STRING ResourceName"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_instance_outputs
           (Values.ListServiceInstanceOutputsInput.make ?nextToken
              ~serviceInstanceName ~serviceName ())
           (Some Values.ListServiceInstanceOutputsOutput.to_json)
           (Some Values.ListServiceInstanceOutputsOutput.error_to_json)])
let list_service_instance_provisioned_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING EmptyNextToken"
       and serviceInstanceName =
         flag "service-instance-name" (required string)
           ~doc:"STRING ResourceName"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_instance_provisioned_resources
           (Values.ListServiceInstanceProvisionedResourcesInput.make
              ?nextToken ~serviceInstanceName ~serviceName ())
           (Some Values.ListServiceInstanceProvisionedResourcesOutput.to_json)
           (Some
              Values.ListServiceInstanceProvisionedResourcesOutput.error_to_json)])
let list_service_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPageResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and serviceName =
         flag "service-name" (optional string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_instances
           (Values.ListServiceInstancesInput.make ?maxResults ?nextToken
              ?serviceName ())
           (Some Values.ListServiceInstancesOutput.to_json)
           (Some Values.ListServiceInstancesOutput.error_to_json)])
let list_service_pipeline_outputs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING EmptyNextToken"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_pipeline_outputs
           (Values.ListServicePipelineOutputsInput.make ?nextToken
              ~serviceName ())
           (Some Values.ListServicePipelineOutputsOutput.to_json)
           (Some Values.ListServicePipelineOutputsOutput.error_to_json)])
let list_service_pipeline_provisioned_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING EmptyNextToken"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_pipeline_provisioned_resources
           (Values.ListServicePipelineProvisionedResourcesInput.make
              ?nextToken ~serviceName ())
           (Some Values.ListServicePipelineProvisionedResourcesOutput.to_json)
           (Some
              Values.ListServicePipelineProvisionedResourcesOutput.error_to_json)])
let list_service_template_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and majorVersion =
         flag "major-version" (optional string)
           ~doc:"STRING TemplateVersionPart"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPageResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_template_versions
           (Values.ListServiceTemplateVersionsInput.make ?majorVersion
              ?maxResults ?nextToken ~templateName ())
           (Some Values.ListServiceTemplateVersionsOutput.to_json)
           (Some Values.ListServiceTemplateVersionsOutput.error_to_json)])
let list_service_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPageResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_templates
           (Values.ListServiceTemplatesInput.make ?maxResults ?nextToken ())
           (Some Values.ListServiceTemplatesOutput.to_json)
           (Some Values.ListServiceTemplatesOutput.error_to_json)])
let list_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPageResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_services
           (Values.ListServicesInput.make ?maxResults ?nextToken ())
           (Some Values.ListServicesOutput.to_json)
           (Some Values.ListServicesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPageResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ?maxResults ?nextToken
              ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let notify_resource_deployment_status_change =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deploymentId =
         flag "deployment-id" (optional string) ~doc:"STRING DeploymentId"
       and outputs =
         flag "outputs" (optional json_arg)
           ~doc:"JSON NotifyResourceDeploymentStatusChangeInputOutputsList"
       and statusMessage =
         flag "status-message" (optional string)
           ~doc:"STRING SyntheticNotifyResourceDeploymentStatusChangeInputString"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and status =
         flag "status" (required json_arg)
           ~doc:"JSON ResourceDeploymentStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.notify_resource_deployment_status_change
           (Values.NotifyResourceDeploymentStatusChangeInput.make
              ?deploymentId
              ?outputs:(Option.map
                          ~f:Values.NotifyResourceDeploymentStatusChangeInputOutputsList.of_json
                          outputs) ?statusMessage ~resourceArn
              ~status:(Values.ResourceDeploymentStatus.of_json status) ())
           (Some Values.NotifyResourceDeploymentStatusChangeOutput.to_json)
           (Some
              Values.NotifyResourceDeploymentStatusChangeOutput.error_to_json)])
let reject_environment_account_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string)
           ~doc:"STRING EnvironmentAccountConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_environment_account_connection
           (Values.RejectEnvironmentAccountConnectionInput.make ~id ())
           (Some Values.RejectEnvironmentAccountConnectionOutput.to_json)
           (Some
              Values.RejectEnvironmentAccountConnectionOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_account_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineProvisioningRepository =
         flag "pipeline-provisioning-repository" (optional json_arg)
           ~doc:"JSON RepositoryBranchInput"
       and pipelineServiceRoleArn =
         flag "pipeline-service-role-arn" (optional string)
           ~doc:"STRING PipelineRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account_settings
           (Values.UpdateAccountSettingsInput.make
              ?pipelineProvisioningRepository:(Option.map
                                                 ~f:Values.RepositoryBranchInput.of_json
                                                 pipelineProvisioningRepository)
              ?pipelineServiceRoleArn ())
           (Some Values.UpdateAccountSettingsOutput.to_json)
           (Some Values.UpdateAccountSettingsOutput.error_to_json)])
let update_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and environmentAccountConnectionId =
         flag "environment-account-connection-id" (optional string)
           ~doc:"STRING EnvironmentAccountConnectionId"
       and protonServiceRoleArn =
         flag "proton-service-role-arn" (optional string) ~doc:"STRING Arn"
       and provisioningRepository =
         flag "provisioning-repository" (optional json_arg)
           ~doc:"JSON RepositoryBranchInput"
       and spec = flag "spec" (optional string) ~doc:"STRING SpecContents"
       and templateMajorVersion =
         flag "template-major-version" (optional string)
           ~doc:"STRING TemplateVersionPart"
       and templateMinorVersion =
         flag "template-minor-version" (optional string)
           ~doc:"STRING TemplateVersionPart"
       and deploymentType =
         flag "deployment-type" (required json_arg)
           ~doc:"JSON DeploymentUpdateType"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment
           (Values.UpdateEnvironmentInput.make ?description
              ?environmentAccountConnectionId ?protonServiceRoleArn
              ?provisioningRepository:(Option.map
                                         ~f:Values.RepositoryBranchInput.of_json
                                         provisioningRepository) ?spec
              ?templateMajorVersion ?templateMinorVersion
              ~deploymentType:(Values.DeploymentUpdateType.of_json
                                 deploymentType) ~name ())
           (Some Values.UpdateEnvironmentOutput.to_json)
           (Some Values.UpdateEnvironmentOutput.error_to_json)])
let update_environment_account_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string)
           ~doc:"STRING EnvironmentAccountConnectionId"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment_account_connection
           (Values.UpdateEnvironmentAccountConnectionInput.make ~id ~roleArn
              ())
           (Some Values.UpdateEnvironmentAccountConnectionOutput.to_json)
           (Some
              Values.UpdateEnvironmentAccountConnectionOutput.error_to_json)])
let update_environment_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment_template
           (Values.UpdateEnvironmentTemplateInput.make ?description
              ?displayName ~name ())
           (Some Values.UpdateEnvironmentTemplateOutput.to_json)
           (Some Values.UpdateEnvironmentTemplateOutput.error_to_json)])
let update_environment_template_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON TemplateVersionStatus"
       and majorVersion =
         flag "major-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and minorVersion =
         flag "minor-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment_template_version
           (Values.UpdateEnvironmentTemplateVersionInput.make ?description
              ?status:(Option.map ~f:Values.TemplateVersionStatus.of_json
                         status) ~majorVersion ~minorVersion ~templateName ())
           (Some Values.UpdateEnvironmentTemplateVersionOutput.to_json)
           (Some Values.UpdateEnvironmentTemplateVersionOutput.error_to_json)])
let update_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and spec = flag "spec" (optional string) ~doc:"STRING SpecContents"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service
           (Values.UpdateServiceInput.make ?description ?spec ~name ())
           (Some Values.UpdateServiceOutput.to_json)
           (Some Values.UpdateServiceOutput.error_to_json)])
let update_service_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spec = flag "spec" (optional string) ~doc:"STRING SpecContents"
       and templateMajorVersion =
         flag "template-major-version" (optional string)
           ~doc:"STRING TemplateVersionPart"
       and templateMinorVersion =
         flag "template-minor-version" (optional string)
           ~doc:"STRING TemplateVersionPart"
       and deploymentType =
         flag "deployment-type" (required json_arg)
           ~doc:"JSON DeploymentUpdateType"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_instance
           (Values.UpdateServiceInstanceInput.make ?spec
              ?templateMajorVersion ?templateMinorVersion
              ~deploymentType:(Values.DeploymentUpdateType.of_json
                                 deploymentType) ~name ~serviceName ())
           (Some Values.UpdateServiceInstanceOutput.to_json)
           (Some Values.UpdateServiceInstanceOutput.error_to_json)])
let update_service_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateMajorVersion =
         flag "template-major-version" (optional string)
           ~doc:"STRING TemplateVersionPart"
       and templateMinorVersion =
         flag "template-minor-version" (optional string)
           ~doc:"STRING TemplateVersionPart"
       and deploymentType =
         flag "deployment-type" (required json_arg)
           ~doc:"JSON DeploymentUpdateType"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING ResourceName"
       and spec = flag "spec" (required string) ~doc:"STRING SpecContents" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_pipeline
           (Values.UpdateServicePipelineInput.make ?templateMajorVersion
              ?templateMinorVersion
              ~deploymentType:(Values.DeploymentUpdateType.of_json
                                 deploymentType) ~serviceName ~spec ())
           (Some Values.UpdateServicePipelineOutput.to_json)
           (Some Values.UpdateServicePipelineOutput.error_to_json)])
let update_service_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_template
           (Values.UpdateServiceTemplateInput.make ?description ?displayName
              ~name ()) (Some Values.UpdateServiceTemplateOutput.to_json)
           (Some Values.UpdateServiceTemplateOutput.error_to_json)])
let update_service_template_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and compatibleEnvironmentTemplates =
         flag "compatible-environment-templates" (optional json_arg)
           ~doc:"JSON CompatibleEnvironmentTemplateInputList"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON TemplateVersionStatus"
       and majorVersion =
         flag "major-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and minorVersion =
         flag "minor-version" (required string)
           ~doc:"STRING TemplateVersionPart"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_template_version
           (Values.UpdateServiceTemplateVersionInput.make
              ?compatibleEnvironmentTemplates:(Option.map
                                                 ~f:Values.CompatibleEnvironmentTemplateInputList.of_json
                                                 compatibleEnvironmentTemplates)
              ?description
              ?status:(Option.map ~f:Values.TemplateVersionStatus.of_json
                         status) ~majorVersion ~minorVersion ~templateName ())
           (Some Values.UpdateServiceTemplateVersionOutput.to_json)
           (Some Values.UpdateServiceTemplateVersionOutput.error_to_json)])
let update_template_sync_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING Subdirectory"
       and branch =
         flag "branch" (required string) ~doc:"STRING GitBranchName"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and repositoryProvider =
         flag "repository-provider" (required json_arg)
           ~doc:"JSON RepositoryProvider"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING ResourceName"
       and templateType =
         flag "template-type" (required json_arg) ~doc:"JSON TemplateType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_template_sync_config
           (Values.UpdateTemplateSyncConfigInput.make ?subdirectory ~branch
              ~repositoryName
              ~repositoryProvider:(Values.RepositoryProvider.of_json
                                     repositoryProvider) ~templateName
              ~templateType:(Values.TemplateType.of_json templateType) ())
           (Some Values.UpdateTemplateSyncConfigOutput.to_json)
           (Some Values.UpdateTemplateSyncConfigOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("accept-environment-account-connection",
       accept_environment_account_connection);
    ("cancel-environment-deployment", cancel_environment_deployment);
    ("cancel-service-instance-deployment",
      cancel_service_instance_deployment);
    ("cancel-service-pipeline-deployment",
      cancel_service_pipeline_deployment);
    ("create-environment", create_environment);
    ("create-environment-account-connection",
      create_environment_account_connection);
    ("create-environment-template", create_environment_template);
    ("create-environment-template-version",
      create_environment_template_version);
    ("create-repository", create_repository);
    ("create-service", create_service);
    ("create-service-template", create_service_template);
    ("create-service-template-version", create_service_template_version);
    ("create-template-sync-config", create_template_sync_config);
    ("delete-environment", delete_environment);
    ("delete-environment-account-connection",
      delete_environment_account_connection);
    ("delete-environment-template", delete_environment_template);
    ("delete-environment-template-version",
      delete_environment_template_version);
    ("delete-repository", delete_repository);
    ("delete-service", delete_service);
    ("delete-service-template", delete_service_template);
    ("delete-service-template-version", delete_service_template_version);
    ("delete-template-sync-config", delete_template_sync_config);
    ("get-account-settings", get_account_settings);
    ("get-environment", get_environment);
    ("get-environment-account-connection",
      get_environment_account_connection);
    ("get-environment-template", get_environment_template);
    ("get-environment-template-version", get_environment_template_version);
    ("get-repository", get_repository);
    ("get-repository-sync-status", get_repository_sync_status);
    ("get-service", get_service);
    ("get-service-instance", get_service_instance);
    ("get-service-template", get_service_template);
    ("get-service-template-version", get_service_template_version);
    ("get-template-sync-config", get_template_sync_config);
    ("get-template-sync-status", get_template_sync_status);
    ("list-environment-account-connections",
      list_environment_account_connections);
    ("list-environment-outputs", list_environment_outputs);
    ("list-environment-provisioned-resources",
      list_environment_provisioned_resources);
    ("list-environment-template-versions",
      list_environment_template_versions);
    ("list-environment-templates", list_environment_templates);
    ("list-environments", list_environments);
    ("list-repositories", list_repositories);
    ("list-repository-sync-definitions", list_repository_sync_definitions);
    ("list-service-instance-outputs", list_service_instance_outputs);
    ("list-service-instance-provisioned-resources",
      list_service_instance_provisioned_resources);
    ("list-service-instances", list_service_instances);
    ("list-service-pipeline-outputs", list_service_pipeline_outputs);
    ("list-service-pipeline-provisioned-resources",
      list_service_pipeline_provisioned_resources);
    ("list-service-template-versions", list_service_template_versions);
    ("list-service-templates", list_service_templates);
    ("list-services", list_services);
    ("list-tags-for-resource", list_tags_for_resource);
    ("notify-resource-deployment-status-change",
      notify_resource_deployment_status_change);
    ("reject-environment-account-connection",
      reject_environment_account_connection);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-account-settings", update_account_settings);
    ("update-environment", update_environment);
    ("update-environment-account-connection",
      update_environment_account_connection);
    ("update-environment-template", update_environment_template);
    ("update-environment-template-version",
      update_environment_template_version);
    ("update-service", update_service);
    ("update-service-instance", update_service_instance);
    ("update-service-pipeline", update_service_pipeline);
    ("update-service-template", update_service_template);
    ("update-service-template-version", update_service_template_version);
    ("update-template-sync-config", update_template_sync_config)]
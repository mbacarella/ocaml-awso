(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.dsql
let apiVersion = "2018-05-10"
let endpointPrefix = "dsql"
let serviceFullName = "Amazon Aurora DSQL"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module ValidationExceptionField =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the field."];
      message: String_.t option
        [@ocaml.doc "A message describing why this field failed validation."]}
    let make ?name = fun ?message -> fun () -> { name; message }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?message ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?message ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stores information about a field passed inside a request that resulted in an validation error."]
module ClusterArn =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The Amazon Resource Name of the cluster."]
    let context_ = "ClusterArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"arn:aws(-[^:]+)?:dsql:[a-z0-9-]{1,20}:[0-9]{12}:cluster/[a-z0-9]{26}");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClusterArn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "The Amazon Resource Name of the cluster."]
module ClusterId =
  struct
    type nonrec t = string[@@ocaml.doc "The ID of the cluster."]
    let context_ = "ClusterId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[a-z0-9]{26}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClusterId" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "The ID of the cluster."]
module StreamArn =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The Amazon Resource Name (ARN) of the stream."]
    let context_ = "StreamArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"arn:aws(-[^:]+)?:dsql:[a-z0-9-]{1,20}:[0-9]{12}:cluster/[a-z0-9]{26}/stream/[a-z0-9]{26}");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StreamArn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "The Amazon Resource Name (ARN) of the stream."]
module StreamCreationTime =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The timestamp when the stream was created."]
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "The timestamp when the stream was created."]
module StreamId =
  struct
    type nonrec t = string[@@ocaml.doc "The ID of the stream."]
    let context_ = "StreamId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[a-z0-9]{26}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StreamId" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "The ID of the stream."]
module StreamStatus =
  struct
    type nonrec t =
      | CREATING 
      | ACTIVE 
      | DELETING 
      | DELETED 
      | FAILED 
      | IMPAIRED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATING -> "CREATING"
      | ACTIVE -> "ACTIVE"
      | DELETING -> "DELETING"
      | DELETED -> "DELETED"
      | FAILED -> "FAILED"
      | IMPAIRED -> "IMPAIRED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATING" -> CREATING
      | "ACTIVE" -> ACTIVE
      | "DELETING" -> DELETING
      | "DELETED" -> DELETED
      | "FAILED" -> FAILED
      | "IMPAIRED" -> IMPAIRED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StreamStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StreamStatus" j)
    let to_json = simple_to_json to_value
  end
module KinesisStreamArn =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The Amazon Resource Name (ARN) of an Amazon Kinesis Data Stream."]
    let context_ = "KinesisStreamArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws.*:kinesis:.*:\\d{12}:stream/\\S+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KinesisStreamArn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "The Amazon Resource Name (ARN) of an Amazon Kinesis Data Stream."]
module RoleArn =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The Amazon Resource Name (ARN) of an IAM role."]
    let context_ = "RoleArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(-[^:]+)?:iam::[0-9]{12}:role(/[a-zA-Z0-9+=,.@_-]+)+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RoleArn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "The Amazon Resource Name (ARN) of an IAM role."]
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ValidationExceptionFieldList =
  struct
    type nonrec t = ValidationExceptionField.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ValidationExceptionField.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ValidationExceptionField.of_xml)
    let of_json j =
      list_of_json ~kind:"ValidationExceptionFieldList"
        ~of_json:ValidationExceptionField.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidationExceptionReason =
  struct
    type nonrec t =
      | UnknownOperation 
      | CannotParse 
      | FieldValidationFailed 
      | DeletionProtectionEnabled 
      | Other 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UnknownOperation -> "unknownOperation"
      | CannotParse -> "cannotParse"
      | FieldValidationFailed -> "fieldValidationFailed"
      | DeletionProtectionEnabled -> "deletionProtectionEnabled"
      | Other -> "other"
      | Non_static_id s -> s
    let of_string =
      function
      | "unknownOperation" -> UnknownOperation
      | "cannotParse" -> CannotParse
      | "fieldValidationFailed" -> FieldValidationFailed
      | "deletionProtectionEnabled" -> DeletionProtectionEnabled
      | "other" -> Other
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module ClusterArnList =
  struct
    type nonrec t = ClusterArn.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ClusterArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ClusterArn.of_xml)
    let of_json j =
      list_of_json ~kind:"ClusterArnList" ~of_json:ClusterArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module Region =
  struct
    type nonrec t = string[@@ocaml.doc "Region name."]
    let context_ = "Region"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:50) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Region" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Region name."]
module TagKey =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Unique tag key, maximum 128 Unicode characters in UTF-8."]
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9_.:/=+\\-@ ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Unique tag key, maximum 128 Unicode characters in UTF-8."]
module TagValue =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Tag value, maximum 256 Unicode characters in UTF-8."]
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9_.:/=+\\-@ ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Tag value, maximum 256 Unicode characters in UTF-8."]
module StreamSummary =
  struct
    type nonrec t =
      {
      clusterIdentifier: ClusterId.t option
        [@ocaml.doc "The ID of the cluster."];
      streamIdentifier: StreamId.t option
        [@ocaml.doc "The ID of the stream."];
      arn: StreamArn.t option [@ocaml.doc "The ARN of the stream."];
      creationTime: StreamCreationTime.t option
        [@ocaml.doc "The timestamp when the stream was created."];
      status: StreamStatus.t option
        [@ocaml.doc "The current status of the stream."]}
    let make ?clusterIdentifier =
      fun ?streamIdentifier ->
        fun ?arn ->
          fun ?creationTime ->
            fun ?status ->
              fun () ->
                {
                  clusterIdentifier;
                  streamIdentifier;
                  arn;
                  creationTime;
                  status
                }
    let to_value x =
      structure_to_value
        [("clusterIdentifier",
           (Option.map x.clusterIdentifier ~f:ClusterId.to_value));
        ("streamIdentifier",
          (Option.map x.streamIdentifier ~f:StreamId.to_value));
        ("arn", (Option.map x.arn ~f:StreamArn.to_value));
        ("creationTime",
          (Option.map x.creationTime ~f:StreamCreationTime.to_value));
        ("status", (Option.map x.status ~f:StreamStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:StreamStatus.of_xml) (Xml.child xml_arg0 "status") in
      let creationTime =
        (Option.map ~f:StreamCreationTime.of_xml)
          (Xml.child xml_arg0 "creationTime") in
      let arn = (Option.map ~f:StreamArn.of_xml) (Xml.child xml_arg0 "arn") in
      let streamIdentifier =
        (Option.map ~f:StreamId.of_xml)
          (Xml.child xml_arg0 "streamIdentifier") in
      let clusterIdentifier =
        (Option.map ~f:ClusterId.of_xml)
          (Xml.child xml_arg0 "clusterIdentifier") in
      make ?status ?creationTime ?arn ?streamIdentifier ?clusterIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" StreamStatus.of_json in
      let creationTime =
        field_map json__ "creationTime" StreamCreationTime.of_json in
      let arn = field_map json__ "arn" StreamArn.of_json in
      let streamIdentifier =
        field_map json__ "streamIdentifier" StreamId.of_json in
      let clusterIdentifier =
        field_map json__ "clusterIdentifier" ClusterId.of_json in
      make ?status ?creationTime ?arn ?streamIdentifier ?clusterIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Summary information about a stream."]
module ClusterSummary =
  struct
    type nonrec t =
      {
      identifier: ClusterId.t option [@ocaml.doc "The ID of the cluster."];
      arn: ClusterArn.t option [@ocaml.doc "The ARN of the cluster."]}
    let make ?identifier = fun ?arn -> fun () -> { identifier; arn }
    let to_value x =
      structure_to_value
        [("identifier", (Option.map x.identifier ~f:ClusterId.to_value));
        ("arn", (Option.map x.arn ~f:ClusterArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn = (Option.map ~f:ClusterArn.of_xml) (Xml.child xml_arg0 "arn") in
      let identifier =
        (Option.map ~f:ClusterId.of_xml) (Xml.child xml_arg0 "identifier") in
      make ?arn ?identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let arn = field_map json__ "arn" ClusterArn.of_json in
      let identifier = field_map json__ "identifier" ClusterId.of_json in
      make ?arn ?identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A summary of the properties of a cluster."]
module StreamFailureErrorCode =
  struct
    type nonrec t =
      | KINESIS_THROUGHPUT_EXCEEDED 
      | KINESIS_STREAM_NOT_FOUND 
      | ROLE_ACCESS_DENIED 
      | KINESIS_ACCESS_DENIED 
      | KINESIS_KMS_ACCESS_DENIED 
      | KINESIS_OVERSIZE_RECORD 
      | CLUSTER_CMK_INACCESSIBLE 
      | INTERNAL_ERROR 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | KINESIS_THROUGHPUT_EXCEEDED -> "KINESIS_THROUGHPUT_EXCEEDED"
      | KINESIS_STREAM_NOT_FOUND -> "KINESIS_STREAM_NOT_FOUND"
      | ROLE_ACCESS_DENIED -> "ROLE_ACCESS_DENIED"
      | KINESIS_ACCESS_DENIED -> "KINESIS_ACCESS_DENIED"
      | KINESIS_KMS_ACCESS_DENIED -> "KINESIS_KMS_ACCESS_DENIED"
      | KINESIS_OVERSIZE_RECORD -> "KINESIS_OVERSIZE_RECORD"
      | CLUSTER_CMK_INACCESSIBLE -> "CLUSTER_CMK_INACCESSIBLE"
      | INTERNAL_ERROR -> "INTERNAL_ERROR"
      | Non_static_id s -> s
    let of_string =
      function
      | "KINESIS_THROUGHPUT_EXCEEDED" -> KINESIS_THROUGHPUT_EXCEEDED
      | "KINESIS_STREAM_NOT_FOUND" -> KINESIS_STREAM_NOT_FOUND
      | "ROLE_ACCESS_DENIED" -> ROLE_ACCESS_DENIED
      | "KINESIS_ACCESS_DENIED" -> KINESIS_ACCESS_DENIED
      | "KINESIS_KMS_ACCESS_DENIED" -> KINESIS_KMS_ACCESS_DENIED
      | "KINESIS_OVERSIZE_RECORD" -> KINESIS_OVERSIZE_RECORD
      | "CLUSTER_CMK_INACCESSIBLE" -> CLUSTER_CMK_INACCESSIBLE
      | "INTERNAL_ERROR" -> INTERNAL_ERROR
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration StreamFailureErrorCode" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"StreamFailureErrorCode" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module KinesisTargetDefinition =
  struct
    type nonrec t =
      {
      streamArn: KinesisStreamArn.t
        [@ocaml.doc "The ARN of the Kinesis stream."];
      roleArn: RoleArn.t
        [@ocaml.doc
          "The ARN of the IAM role that grants permission to write to the Kinesis stream. This can be a standard role (arn:aws:iam::account-id:role/role-name) or a role with a path prefix (arn:aws:iam::account-id:role/service-role/role-name), such as roles auto-created by the console."]}
    let context_ = "KinesisTargetDefinition"
    let make ~streamArn = fun ~roleArn -> fun () -> { streamArn; roleArn }
    let to_value x =
      structure_to_value
        [("streamArn", (Some (KinesisStreamArn.to_value x.streamArn)));
        ("roleArn", (Some (RoleArn.to_value x.roleArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let roleArn =
        RoleArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "roleArn") in
      let streamArn =
        KinesisStreamArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "streamArn") in
      make ~roleArn ~streamArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let roleArn = field_map_exn json__ "roleArn" RoleArn.of_json in
      let streamArn =
        field_map_exn json__ "streamArn" KinesisStreamArn.of_json in
      make ~roleArn ~streamArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Kinesis stream target configuration."]
module EncryptionStatus =
  struct
    type nonrec t =
      | ENABLED 
      | UPDATING 
      | KMS_KEY_INACCESSIBLE 
      | ENABLING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | UPDATING -> "UPDATING"
      | KMS_KEY_INACCESSIBLE -> "KMS_KEY_INACCESSIBLE"
      | ENABLING -> "ENABLING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "UPDATING" -> UPDATING
      | "KMS_KEY_INACCESSIBLE" -> KMS_KEY_INACCESSIBLE
      | "ENABLING" -> ENABLING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EncryptionStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EncryptionStatus" j)
    let to_json = simple_to_json to_value
  end
module EncryptionType =
  struct
    type nonrec t =
      | AWS_OWNED_KMS_KEY 
      | CUSTOMER_MANAGED_KMS_KEY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AWS_OWNED_KMS_KEY -> "AWS_OWNED_KMS_KEY"
      | CUSTOMER_MANAGED_KMS_KEY -> "CUSTOMER_MANAGED_KMS_KEY"
      | Non_static_id s -> s
    let of_string =
      function
      | "AWS_OWNED_KMS_KEY" -> AWS_OWNED_KMS_KEY
      | "CUSTOMER_MANAGED_KMS_KEY" -> CUSTOMER_MANAGED_KMS_KEY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EncryptionType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EncryptionType" j)
    let to_json = simple_to_json to_value
  end
module KmsKeyArn =
  struct
    type nonrec t = string
    let context_ = "KmsKeyArn"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KmsKeyArn" j
    let to_json = simple_to_json to_value
  end
module AccessDeniedException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You do not have sufficient access to perform this action."]
module ClusterCreationTime =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The timestamp when the cluster was created."]
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "The timestamp when the cluster was created."]
module ClusterStatus =
  struct
    type nonrec t =
      | CREATING 
      | ACTIVE 
      | IDLE 
      | INACTIVE 
      | UPDATING 
      | DELETING 
      | DELETED 
      | FAILED 
      | PENDING_SETUP 
      | PENDING_DELETE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATING -> "CREATING"
      | ACTIVE -> "ACTIVE"
      | IDLE -> "IDLE"
      | INACTIVE -> "INACTIVE"
      | UPDATING -> "UPDATING"
      | DELETING -> "DELETING"
      | DELETED -> "DELETED"
      | FAILED -> "FAILED"
      | PENDING_SETUP -> "PENDING_SETUP"
      | PENDING_DELETE -> "PENDING_DELETE"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATING" -> CREATING
      | "ACTIVE" -> ACTIVE
      | "IDLE" -> IDLE
      | "INACTIVE" -> INACTIVE
      | "UPDATING" -> UPDATING
      | "DELETING" -> DELETING
      | "DELETED" -> DELETED
      | "FAILED" -> FAILED
      | "PENDING_SETUP" -> PENDING_SETUP
      | "PENDING_DELETE" -> PENDING_DELETE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ClusterStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ClusterStatus" j)
    let to_json = simple_to_json to_value
  end
module ConflictException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      resourceId: String_.t option [@ocaml.doc "Resource Id"];
      resourceType: String_.t option [@ocaml.doc "Resource Type"]}
    let make ?message =
      fun ?resourceId ->
        fun ?resourceType -> fun () -> { message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value));
        ("resourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("resourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?resourceType ?resourceId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "resourceType" String_.of_json in
      let resourceId = field_map json__ "resourceId" String_.of_json in
      let message = field_map json__ "message" String_.of_json in
      make ?resourceType ?resourceId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The submitted action has conflicts."]
module InternalServerException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      retryAfterSeconds: Integer.t option [@ocaml.doc "Retry after seconds."]}
    let make ?message =
      fun ?retryAfterSeconds -> fun () -> { message; retryAfterSeconds }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value));
        ("Retry-After", (Option.map x.retryAfterSeconds ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retryAfterSeconds =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Retry-After") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?retryAfterSeconds ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retryAfterSeconds =
        field_map json__ "retryAfterSeconds" Integer.of_json in
      let message = field_map json__ "message" String_.of_json in
      make ?retryAfterSeconds ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request processing has failed because of an unknown error, exception or failure."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      resourceId: String_.t option
        [@ocaml.doc "The resource ID could not be found."];
      resourceType: String_.t option
        [@ocaml.doc "The resource type could not be found."]}
    let make ?message =
      fun ?resourceId ->
        fun ?resourceType -> fun () -> { message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value));
        ("resourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("resourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?resourceType ?resourceId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "resourceType" String_.of_json in
      let resourceId = field_map json__ "resourceId" String_.of_json in
      let message = field_map json__ "message" String_.of_json in
      make ?resourceType ?resourceId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The resource could not be found."]
module ThrottlingException =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "The message that the request was denied due to request throttling."];
      serviceCode: String_.t option
        [@ocaml.doc "The request exceeds a service quota."];
      quotaCode: String_.t option
        [@ocaml.doc "The request exceeds a request rate quota."];
      retryAfterSeconds: Integer.t option
        [@ocaml.doc
          "The request exceeds a request rate quota. Retry after seconds."]}
    let make ?message =
      fun ?serviceCode ->
        fun ?quotaCode ->
          fun ?retryAfterSeconds ->
            fun () -> { message; serviceCode; quotaCode; retryAfterSeconds }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value));
        ("serviceCode", (Option.map x.serviceCode ~f:String_.to_value));
        ("quotaCode", (Option.map x.quotaCode ~f:String_.to_value));
        ("Retry-After", (Option.map x.retryAfterSeconds ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retryAfterSeconds =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Retry-After") in
      let quotaCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "quotaCode") in
      let serviceCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "serviceCode") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?retryAfterSeconds ?quotaCode ?serviceCode ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retryAfterSeconds =
        field_map json__ "retryAfterSeconds" Integer.of_json in
      let quotaCode = field_map json__ "quotaCode" String_.of_json in
      let serviceCode = field_map json__ "serviceCode" String_.of_json in
      let message = field_map json__ "message" String_.of_json in
      make ?retryAfterSeconds ?quotaCode ?serviceCode ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request was denied due to request throttling."]
module ValidationException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      reason: ValidationExceptionReason.t option
        [@ocaml.doc "The reason for the validation exception."];
      fieldList: ValidationExceptionFieldList.t option
        [@ocaml.doc "A list of fields that didn't validate."]}
    let make ?message =
      fun ?reason ->
        fun ?fieldList -> fun () -> { message; reason; fieldList }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value));
        ("reason",
          (Option.map x.reason ~f:ValidationExceptionReason.to_value));
        ("fieldList",
          (Option.map x.fieldList ~f:ValidationExceptionFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fieldList =
        (Option.map ~f:ValidationExceptionFieldList.of_xml)
          (Xml.child xml_arg0 "fieldList") in
      let reason =
        (Option.map ~f:ValidationExceptionReason.of_xml)
          (Xml.child xml_arg0 "reason") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?fieldList ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fieldList =
        field_map json__ "fieldList" ValidationExceptionFieldList.of_json in
      let reason =
        field_map json__ "reason" ValidationExceptionReason.of_json in
      let message = field_map json__ "message" String_.of_json in
      make ?fieldList ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The input failed to satisfy the constraints specified by an Amazon Web Services service."]
module ClientToken =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Idempotency token so a request is only processed once."]
    let context_ = "ClientToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"[!-~]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientToken" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Idempotency token so a request is only processed once."]
module DeletionProtectionEnabled =
  struct
    type nonrec t = bool[@@ocaml.doc
                          "Indicates whether deletion protection is enabled for a cluster."]
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "Indicates whether deletion protection is enabled for a cluster."]
module KmsEncryptionKey =
  struct
    type nonrec t = string
    let context_ = "KmsEncryptionKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9:/_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KmsEncryptionKey" j
    let to_json = simple_to_json to_value
  end
module MultiRegionProperties =
  struct
    type nonrec t =
      {
      witnessRegion: Region.t option
        [@ocaml.doc
          "The Region that serves as the witness region for a multi-Region cluster. The witness Region helps maintain cluster consistency and quorum."];
      clusters: ClusterArnList.t option
        [@ocaml.doc
          "The set of peered clusters that form the multi-Region cluster configuration. Each peered cluster represents a database instance in a different Region."]}
    let make ?witnessRegion =
      fun ?clusters -> fun () -> { witnessRegion; clusters }
    let to_value x =
      structure_to_value
        [("witnessRegion", (Option.map x.witnessRegion ~f:Region.to_value));
        ("clusters", (Option.map x.clusters ~f:ClusterArnList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clusters =
        (Option.map ~f:ClusterArnList.of_xml) (Xml.child xml_arg0 "clusters") in
      let witnessRegion =
        (Option.map ~f:Region.of_xml) (Xml.child xml_arg0 "witnessRegion") in
      make ?clusters ?witnessRegion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clusters = field_map json__ "clusters" ClusterArnList.of_json in
      let witnessRegion = field_map json__ "witnessRegion" Region.of_json in
      make ?clusters ?witnessRegion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines the structure for multi-Region cluster configurations, containing the witness region and linked cluster settings."]
module Arn =
  struct
    type nonrec t = string[@@ocaml.doc "Amazon Resource Name."]
    let context_ = "Arn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1011) >>=
                  (fun () -> check_pattern i ~pattern:"arn:.+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Arn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Amazon Resource Name."]
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagMap =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module PolicyVersion =
  struct
    type nonrec t = string
    let context_ = "PolicyVersion"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PolicyVersion" j
    let to_json = simple_to_json to_value
  end
module BypassPolicyLockoutSafetyCheck =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module PolicyDocument =
  struct
    type nonrec t = string[@@ocaml.doc
                            "A resource-based policy document in JSON format. Length constraints: Minimum length of 1. Maximum length of 20480 characters (approximately 20KB)."]
    let context_ = "PolicyDocument"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:20480) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PolicyDocument" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "A resource-based policy document in JSON format. Length constraints: Minimum length of 1. Maximum length of 20480 characters (approximately 20KB)."]
module NextToken =
  struct
    type nonrec t = string[@@ocaml.doc "Token used to retrieve next page."]
    let context_ = "NextToken"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Token used to retrieve next page."]
module StreamList =
  struct
    type nonrec t = StreamSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:StreamSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StreamSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"StreamList" ~of_json:StreamSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int[@@ocaml.doc
                         "Max results that will be returned per page."]
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Max results that will be returned per page."]
module ClusterList =
  struct
    type nonrec t = ClusterSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ClusterSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ClusterSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ClusterList" ~of_json:ClusterSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ClusterVpcEndpoint =
  struct
    type nonrec t = string
    let context_ = "ClusterVpcEndpoint"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[a-zA-Z0-9.-]+"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClusterVpcEndpoint" j
    let to_json = simple_to_json to_value
  end
module ServiceName =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The name of the VPC endpoint service that provides access to your cluster. Use this endpoint to establish a private connection between your VPC and the cluster."]
    let context_ = "ServiceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"com\\.amazonaws\\.[a-z0-9-]+\\.dsql-[a-f0-9]{6}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServiceName" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "The name of the VPC endpoint service that provides access to your cluster. Use this endpoint to establish a private connection between your VPC and the cluster."]
module StatusReason =
  struct
    type nonrec t =
      {
      error: StreamFailureErrorCode.t option
        [@ocaml.doc "The error code for the stream failure."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "The timestamp when the status was updated."]}
    let make ?error = fun ?updatedAt -> fun () -> { error; updatedAt }
    let to_value x =
      structure_to_value
        [("error", (Option.map x.error ~f:StreamFailureErrorCode.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let error =
        (Option.map ~f:StreamFailureErrorCode.of_xml)
          (Xml.child xml_arg0 "error") in
      make ?updatedAt ?error ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let error = field_map json__ "error" StreamFailureErrorCode.of_json in
      make ?updatedAt ?error ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Stream status reason with error and timestamp."]
module StreamFormat =
  struct
    type nonrec t =
      | JSON 
      | Non_static_id of string 
    let make i = i
    let to_string = function | JSON -> "JSON" | Non_static_id s -> s
    let of_string = function | "JSON" -> JSON | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StreamFormat" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StreamFormat" j)
    let to_json = simple_to_json to_value
  end
module StreamOrdering =
  struct
    type nonrec t =
      | UNORDERED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | UNORDERED -> "UNORDERED" | Non_static_id s -> s
    let of_string =
      function | "UNORDERED" -> UNORDERED | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StreamOrdering" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StreamOrdering" j)
    let to_json = simple_to_json to_value
  end
module TargetDefinition =
  struct
    type nonrec t =
      {
      kinesis: KinesisTargetDefinition.t option
        [@ocaml.doc "Kinesis stream target configuration."]}
    let make ?kinesis = fun () -> { kinesis }
    let to_value x =
      structure_to_value
        [("kinesis",
           (Option.map x.kinesis ~f:KinesisTargetDefinition.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kinesis =
        (Option.map ~f:KinesisTargetDefinition.of_xml)
          (Xml.child xml_arg0 "kinesis") in
      make ?kinesis ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let kinesis =
        field_map json__ "kinesis" KinesisTargetDefinition.of_json in
      make ?kinesis ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Target definition for stream destination."]
module EncryptionDetails =
  struct
    type nonrec t =
      {
      encryptionType: EncryptionType.t option
        [@ocaml.doc
          "The type of encryption that protects the data on your cluster."];
      kmsKeyArn: KmsKeyArn.t option
        [@ocaml.doc
          "The ARN of the KMS key that encrypts data in the cluster."];
      encryptionStatus: EncryptionStatus.t option
        [@ocaml.doc "The status of encryption for the cluster."]}
    let make ?encryptionType =
      fun ?kmsKeyArn ->
        fun ?encryptionStatus ->
          fun () -> { encryptionType; kmsKeyArn; encryptionStatus }
    let to_value x =
      structure_to_value
        [("encryptionType",
           (Option.map x.encryptionType ~f:EncryptionType.to_value));
        ("kmsKeyArn", (Option.map x.kmsKeyArn ~f:KmsKeyArn.to_value));
        ("encryptionStatus",
          (Option.map x.encryptionStatus ~f:EncryptionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionStatus =
        (Option.map ~f:EncryptionStatus.of_xml)
          (Xml.child xml_arg0 "encryptionStatus") in
      let kmsKeyArn =
        (Option.map ~f:KmsKeyArn.of_xml) (Xml.child xml_arg0 "kmsKeyArn") in
      let encryptionType =
        (Option.map ~f:EncryptionType.of_xml)
          (Xml.child xml_arg0 "encryptionType") in
      make ?encryptionStatus ?kmsKeyArn ?encryptionType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryptionStatus =
        field_map json__ "encryptionStatus" EncryptionStatus.of_json in
      let kmsKeyArn = field_map json__ "kmsKeyArn" KmsKeyArn.of_json in
      let encryptionType =
        field_map json__ "encryptionType" EncryptionType.of_json in
      make ?encryptionStatus ?kmsKeyArn ?encryptionType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration details about encryption for the cluster including the KMS key ARN, encryption type, and encryption status."]
module Endpoint =
  struct
    type nonrec t = string
    let context_ = "Endpoint"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[a-zA-Z0-9.-]+"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Endpoint" j
    let to_json = simple_to_json to_value
  end
module ServiceQuotaExceededException =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc "The service exception for exceeding a quota."];
      resourceId: String_.t option
        [@ocaml.doc "The resource ID exceeds a quota."];
      resourceType: String_.t option
        [@ocaml.doc "The resource type exceeds a quota."];
      serviceCode: String_.t option
        [@ocaml.doc "The request exceeds a service quota."];
      quotaCode: String_.t option [@ocaml.doc "The service exceeds a quota."]}
    let make ?message =
      fun ?resourceId ->
        fun ?resourceType ->
          fun ?serviceCode ->
            fun ?quotaCode ->
              fun () ->
                { message; resourceId; resourceType; serviceCode; quotaCode }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value));
        ("resourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("resourceType", (Option.map x.resourceType ~f:String_.to_value));
        ("serviceCode", (Option.map x.serviceCode ~f:String_.to_value));
        ("quotaCode", (Option.map x.quotaCode ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let quotaCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "quotaCode") in
      let serviceCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "serviceCode") in
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?quotaCode ?serviceCode ?resourceType ?resourceId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let quotaCode = field_map json__ "quotaCode" String_.of_json in
      let serviceCode = field_map json__ "serviceCode" String_.of_json in
      let resourceType = field_map json__ "resourceType" String_.of_json in
      let resourceId = field_map json__ "resourceId" String_.of_json in
      let message = field_map json__ "message" String_.of_json in
      make ?quotaCode ?serviceCode ?resourceType ?resourceId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The service limit was exceeded."]
module UpdateClusterOutput =
  struct
    type nonrec t =
      {
      identifier: ClusterId.t option
        [@ocaml.doc "The ID of the cluster to update."];
      arn: ClusterArn.t option [@ocaml.doc "The ARN of the updated cluster."];
      status: ClusterStatus.t option
        [@ocaml.doc "The status of the updated cluster."];
      creationTime: ClusterCreationTime.t option
        [@ocaml.doc "The time of when the cluster was created."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?identifier =
      fun ?arn ->
        fun ?status ->
          fun ?creationTime ->
            fun () -> { identifier; arn; status; creationTime }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("identifier", (Option.map x.identifier ~f:ClusterId.to_value));
        ("arn", (Option.map x.arn ~f:ClusterArn.to_value));
        ("status", (Option.map x.status ~f:ClusterStatus.to_value));
        ("creationTime",
          (Option.map x.creationTime ~f:ClusterCreationTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationTime =
        (Option.map ~f:ClusterCreationTime.of_xml)
          (Xml.child xml_arg0 "creationTime") in
      let status =
        (Option.map ~f:ClusterStatus.of_xml) (Xml.child xml_arg0 "status") in
      let arn = (Option.map ~f:ClusterArn.of_xml) (Xml.child xml_arg0 "arn") in
      let identifier =
        (Option.map ~f:ClusterId.of_xml) (Xml.child xml_arg0 "identifier") in
      make ?creationTime ?status ?arn ?identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let creationTime =
        field_map json__ "creationTime" ClusterCreationTime.of_json in
      let status = field_map json__ "status" ClusterStatus.of_json in
      let arn = field_map json__ "arn" ClusterArn.of_json in
      let identifier = field_map json__ "identifier" ClusterId.of_json in
      make ?creationTime ?status ?arn ?identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The details of the cluster after it has been updated."]
module UpdateClusterInput =
  struct
    type nonrec t =
      {
      identifier: ClusterId.t
        [@ocaml.doc "The ID of the cluster you want to update."];
      deletionProtectionEnabled: DeletionProtectionEnabled.t option
        [@ocaml.doc
          "Specifies whether to enable deletion protection in your cluster."];
      kmsEncryptionKey: KmsEncryptionKey.t option
        [@ocaml.doc
          "The KMS key that encrypts and protects the data on your cluster. You can specify the ARN, ID, or alias of an existing key or have Amazon Web Services create a default key for you."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully. The subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the Amazon Web Services SDK automatically generates one."];
      multiRegionProperties: MultiRegionProperties.t option
        [@ocaml.doc
          "The new multi-Region cluster configuration settings to be applied during an update operation."]}
    let context_ = "UpdateClusterInput"
    let make ?deletionProtectionEnabled =
      fun ?kmsEncryptionKey ->
        fun ?clientToken ->
          fun ?multiRegionProperties ->
            fun ~identifier ->
              fun () ->
                {
                  deletionProtectionEnabled;
                  kmsEncryptionKey;
                  clientToken;
                  multiRegionProperties;
                  identifier
                }
    let to_value x =
      structure_to_value
        [("identifier", (Some (ClusterId.to_value x.identifier)));
        ("deletionProtectionEnabled",
          (Option.map x.deletionProtectionEnabled
             ~f:DeletionProtectionEnabled.to_value));
        ("kmsEncryptionKey",
          (Option.map x.kmsEncryptionKey ~f:KmsEncryptionKey.to_value));
        ("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("multiRegionProperties",
          (Option.map x.multiRegionProperties
             ~f:MultiRegionProperties.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let multiRegionProperties =
        (Option.map ~f:MultiRegionProperties.of_xml)
          (Xml.child xml_arg0 "multiRegionProperties") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      let kmsEncryptionKey =
        (Option.map ~f:KmsEncryptionKey.of_xml)
          (Xml.child xml_arg0 "kmsEncryptionKey") in
      let deletionProtectionEnabled =
        (Option.map ~f:DeletionProtectionEnabled.of_xml)
          (Xml.child xml_arg0 "deletionProtectionEnabled") in
      let identifier =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "identifier") in
      make ?multiRegionProperties ?clientToken ?kmsEncryptionKey
        ?deletionProtectionEnabled ~identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let multiRegionProperties =
        field_map json__ "multiRegionProperties"
          MultiRegionProperties.of_json in
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let kmsEncryptionKey =
        field_map json__ "kmsEncryptionKey" KmsEncryptionKey.of_json in
      let deletionProtectionEnabled =
        field_map json__ "deletionProtectionEnabled"
          DeletionProtectionEnabled.of_json in
      let identifier = field_map_exn json__ "identifier" ClusterId.of_json in
      make ?multiRegionProperties ?clientToken ?kmsEncryptionKey
        ?deletionProtectionEnabled ~identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The UpdateCluster API allows you to modify both single-Region and multi-Region cluster configurations. With the multiRegionProperties parameter, you can add or modify witness Region support and manage peer relationships with clusters in other Regions. Note that updating multi-Region clusters requires additional IAM permissions beyond those needed for standard cluster updates, as detailed in the Permissions section. Required permissions dsql:UpdateCluster Permission to update a DSQL cluster. Resources: arn:aws:dsql:region:account-id:cluster/cluster-id dsql:PutMultiRegionProperties Permission to configure multi-Region properties for a cluster. Resources: arn:aws:dsql:region:account-id:cluster/cluster-id dsql:GetCluster Permission to retrieve cluster information. Resources: arn:aws:dsql:region:account-id:cluster/cluster-id dsql:AddPeerCluster Permission to add peer clusters. Resources: Local cluster: arn:aws:dsql:region:account-id:cluster/cluster-id Each peer cluster: exact ARN of each specified peer cluster dsql:RemovePeerCluster Permission to remove peer clusters. The dsql:RemovePeerCluster permission uses a wildcard ARN pattern to simplify permission management during updates. Resources: arn:aws:dsql:*:account-id:cluster/* dsql:PutWitnessRegion Permission to set a witness Region. Resources: arn:aws:dsql:region:account-id:cluster/cluster-id Condition Keys: dsql:WitnessRegion (matching the specified witness Region) This permission is checked both in the cluster Region and in the witness Region. The witness region specified in multiRegionProperties.witnessRegion cannot be the same as the cluster's Region. When updating clusters with peer relationships, permissions are checked for both adding and removing peers. The dsql:RemovePeerCluster permission uses a wildcard ARN pattern to simplify permission management during updates."]
module UntagResourceInput =
  struct
    type nonrec t =
      {
      resourceArn: Arn.t
        [@ocaml.doc "The ARN of the resource from which to remove tags."];
      tagKeys: TagKeyList.t
        [@ocaml.doc "The array of keys of the tags that you want to remove."]}
    let context_ = "UntagResourceInput"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map_exn json__ "tagKeys" TagKeyList.of_json in
      let resourceArn = field_map_exn json__ "resourceArn" Arn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes a tag from a resource."]
module TagResourceInput =
  struct
    type nonrec t =
      {
      resourceArn: Arn.t
        [@ocaml.doc "The ARN of the resource that you want to tag."];
      tags: TagMap.t
        [@ocaml.doc
          "A map of key and value pairs to use to tag your resource."]}
    let context_ = "TagResourceInput"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)));
        ("tags", (Some (TagMap.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagMap.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "tags" TagMap.of_json in
      let resourceArn = field_map_exn json__ "resourceArn" Arn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Tags a resource with a map of key and value pairs."]
module PutClusterPolicyOutput =
  struct
    type nonrec t =
      {
      policyVersion: PolicyVersion.t option
        [@ocaml.doc
          "The version of the policy after it has been updated or created."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?policyVersion = fun () -> { policyVersion }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("policyVersion",
           (Option.map x.policyVersion ~f:PolicyVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyVersion =
        (Option.map ~f:PolicyVersion.of_xml)
          (Xml.child xml_arg0 "policyVersion") in
      make ?policyVersion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyVersion =
        field_map json__ "policyVersion" PolicyVersion.of_json in
      make ?policyVersion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches a resource-based policy to a cluster. This policy defines access permissions and conditions for the cluster, allowing you to control which principals can perform actions on the cluster."]
module PutClusterPolicyInput =
  struct
    type nonrec t =
      {
      identifier: ClusterId.t ;
      policy: PolicyDocument.t
        [@ocaml.doc
          "The resource-based policy document to attach to the cluster. This should be a valid JSON policy document that defines permissions and conditions."];
      bypassPolicyLockoutSafetyCheck: BypassPolicyLockoutSafetyCheck.t option
        [@ocaml.doc
          "A flag that allows you to bypass the policy lockout safety check. When set to true, this parameter allows you to apply a policy that might lock you out of the cluster. Use with caution."];
      expectedPolicyVersion: PolicyVersion.t option
        [@ocaml.doc
          "The expected version of the current policy. This parameter ensures that you're updating the correct version of the policy and helps prevent concurrent modification conflicts."];
      clientToken: ClientToken.t option }
    let context_ = "PutClusterPolicyInput"
    let make ?bypassPolicyLockoutSafetyCheck =
      fun ?expectedPolicyVersion ->
        fun ?clientToken ->
          fun ~identifier ->
            fun ~policy ->
              fun () ->
                {
                  bypassPolicyLockoutSafetyCheck;
                  expectedPolicyVersion;
                  clientToken;
                  identifier;
                  policy
                }
    let to_value x =
      structure_to_value
        [("identifier", (Some (ClusterId.to_value x.identifier)));
        ("policy", (Some (PolicyDocument.to_value x.policy)));
        ("bypassPolicyLockoutSafetyCheck",
          (Option.map x.bypassPolicyLockoutSafetyCheck
             ~f:BypassPolicyLockoutSafetyCheck.to_value));
        ("expectedPolicyVersion",
          (Option.map x.expectedPolicyVersion ~f:PolicyVersion.to_value));
        ("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      let expectedPolicyVersion =
        (Option.map ~f:PolicyVersion.of_xml)
          (Xml.child xml_arg0 "expectedPolicyVersion") in
      let bypassPolicyLockoutSafetyCheck =
        (Option.map ~f:BypassPolicyLockoutSafetyCheck.of_xml)
          (Xml.child xml_arg0 "bypassPolicyLockoutSafetyCheck") in
      let policy =
        PolicyDocument.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "policy") in
      let identifier =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "identifier") in
      make ?clientToken ?expectedPolicyVersion
        ?bypassPolicyLockoutSafetyCheck ~policy ~identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let expectedPolicyVersion =
        field_map json__ "expectedPolicyVersion" PolicyVersion.of_json in
      let bypassPolicyLockoutSafetyCheck =
        field_map json__ "bypassPolicyLockoutSafetyCheck"
          BypassPolicyLockoutSafetyCheck.of_json in
      let policy = field_map_exn json__ "policy" PolicyDocument.of_json in
      let identifier = field_map_exn json__ "identifier" ClusterId.of_json in
      make ?clientToken ?expectedPolicyVersion
        ?bypassPolicyLockoutSafetyCheck ~policy ~identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches a resource-based policy to a cluster. This policy defines access permissions and conditions for the cluster, allowing you to control which principals can perform actions on the cluster."]
module ListTagsForResourceOutput =
  struct
    type nonrec t =
      {
      tags: TagMap.t option
        [@ocaml.doc
          "A map of key and value pairs that you used to tag your resource."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists all of the tags for a resource."]
module ListTagsForResourceInput =
  struct
    type nonrec t =
      {
      resourceArn: Arn.t
        [@ocaml.doc
          "The ARN of the resource for which you want to list the tags."]}
    let context_ = "ListTagsForResourceInput"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn = field_map_exn json__ "resourceArn" Arn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists all of the tags for a resource."]
module ListStreamsOutput =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "If nextToken is returned, there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, make the call again using the returned token."];
      streams: StreamList.t option
        [@ocaml.doc "An array of the returned streams."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?streams -> fun () -> { nextToken; streams }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("streams", (Option.map x.streams ~f:StreamList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streams =
        (Option.map ~f:StreamList.of_xml) (Xml.child xml_arg0 "streams") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?streams ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let streams = field_map json__ "streams" StreamList.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?streams ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves information about a list of streams for a cluster."]
module ListStreamsInput =
  struct
    type nonrec t =
      {
      clusterIdentifier: ClusterId.t
        [@ocaml.doc "The ID of the cluster for which to list streams."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "An optional parameter that specifies the maximum number of results to return. You can use nextToken to display the next page of results. Default: 10."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If your initial ListStreams operation returns a nextToken, you can include the returned nextToken in following ListStreams operations, which returns results in the next page."]}
    let context_ = "ListStreamsInput"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~clusterIdentifier ->
          fun () -> { maxResults; nextToken; clusterIdentifier }
    let to_value x =
      structure_to_value
        [("clusterIdentifier",
           (Some (ClusterId.to_value x.clusterIdentifier)));
        ("max-results", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("next-token", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "next-token") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "max-results") in
      let clusterIdentifier =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "clusterIdentifier") in
      make ?nextToken ?maxResults ~clusterIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let clusterIdentifier =
        field_map_exn json__ "clusterIdentifier" ClusterId.of_json in
      make ?nextToken ?maxResults ~clusterIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves information about a list of streams for a cluster."]
module ListClustersOutput =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "If nextToken is returned, there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, make the call again using the returned token."];
      clusters: ClusterList.t option
        [@ocaml.doc "An array of the returned clusters."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?clusters -> fun () -> { nextToken; clusters }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("clusters", (Option.map x.clusters ~f:ClusterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clusters =
        (Option.map ~f:ClusterList.of_xml) (Xml.child xml_arg0 "clusters") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?clusters ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clusters = field_map json__ "clusters" ClusterList.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?clusters ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves information about a list of clusters."]
module ListClustersInput =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc
          "An optional parameter that specifies the maximum number of results to return. You can use nextToken to display the next page of results."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If your initial ListClusters operation returns a nextToken, you can include the returned nextToken in following ListClusters operations, which returns results in the next page."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("max-results", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("next-token", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "next-token") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "max-results") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves information about a list of clusters."]
module GetVpcEndpointServiceNameOutput =
  struct
    type nonrec t =
      {
      serviceName: ServiceName.t option
        [@ocaml.doc "The VPC endpoint service name."];
      clusterVpcEndpoint: ClusterVpcEndpoint.t option
        [@ocaml.doc "The VPC connection endpoint for the cluster."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?serviceName =
      fun ?clusterVpcEndpoint ->
        fun () -> { serviceName; clusterVpcEndpoint }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("serviceName", (Option.map x.serviceName ~f:ServiceName.to_value));
        ("clusterVpcEndpoint",
          (Option.map x.clusterVpcEndpoint ~f:ClusterVpcEndpoint.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clusterVpcEndpoint =
        (Option.map ~f:ClusterVpcEndpoint.of_xml)
          (Xml.child xml_arg0 "clusterVpcEndpoint") in
      let serviceName =
        (Option.map ~f:ServiceName.of_xml) (Xml.child xml_arg0 "serviceName") in
      make ?clusterVpcEndpoint ?serviceName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clusterVpcEndpoint =
        field_map json__ "clusterVpcEndpoint" ClusterVpcEndpoint.of_json in
      let serviceName = field_map json__ "serviceName" ServiceName.of_json in
      make ?clusterVpcEndpoint ?serviceName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves the VPC endpoint service name."]
module GetVpcEndpointServiceNameInput =
  struct
    type nonrec t =
      {
      identifier: ClusterId.t
        [@ocaml.doc "The ID of the cluster to retrieve."]}
    let context_ = "GetVpcEndpointServiceNameInput"
    let make ~identifier = fun () -> { identifier }
    let to_value x =
      structure_to_value
        [("identifier", (Some (ClusterId.to_value x.identifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identifier =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "identifier") in
      make ~identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let identifier = field_map_exn json__ "identifier" ClusterId.of_json in
      make ~identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves the VPC endpoint service name."]
module GetStreamOutput =
  struct
    type nonrec t =
      {
      clusterIdentifier: ClusterId.t option
        [@ocaml.doc "The ID of the cluster for the retrieved stream."];
      streamIdentifier: StreamId.t option
        [@ocaml.doc "The ID of the retrieved stream."];
      arn: StreamArn.t option [@ocaml.doc "The ARN of the retrieved stream."];
      status: StreamStatus.t option
        [@ocaml.doc "The current status of the retrieved stream."];
      creationTime: StreamCreationTime.t option
        [@ocaml.doc "The time when the stream was created."];
      ordering: StreamOrdering.t option
        [@ocaml.doc "The ordering mode of the stream."];
      format: StreamFormat.t option
        [@ocaml.doc "The format of the stream records."];
      targetDefinition: TargetDefinition.t option
        [@ocaml.doc "The target definition for the stream destination."];
      statusReason: StatusReason.t option
        [@ocaml.doc
          "Stream status reason with error code and timestamp (if applicable)."];
      tags: TagMap.t option
        [@ocaml.doc "A map of tags associated with the stream."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?clusterIdentifier =
      fun ?streamIdentifier ->
        fun ?arn ->
          fun ?status ->
            fun ?creationTime ->
              fun ?ordering ->
                fun ?format ->
                  fun ?targetDefinition ->
                    fun ?statusReason ->
                      fun ?tags ->
                        fun () ->
                          {
                            clusterIdentifier;
                            streamIdentifier;
                            arn;
                            status;
                            creationTime;
                            ordering;
                            format;
                            targetDefinition;
                            statusReason;
                            tags
                          }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clusterIdentifier",
           (Option.map x.clusterIdentifier ~f:ClusterId.to_value));
        ("streamIdentifier",
          (Option.map x.streamIdentifier ~f:StreamId.to_value));
        ("arn", (Option.map x.arn ~f:StreamArn.to_value));
        ("status", (Option.map x.status ~f:StreamStatus.to_value));
        ("creationTime",
          (Option.map x.creationTime ~f:StreamCreationTime.to_value));
        ("ordering", (Option.map x.ordering ~f:StreamOrdering.to_value));
        ("format", (Option.map x.format ~f:StreamFormat.to_value));
        ("targetDefinition",
          (Option.map x.targetDefinition ~f:TargetDefinition.to_value));
        ("statusReason",
          (Option.map x.statusReason ~f:StatusReason.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let statusReason =
        (Option.map ~f:StatusReason.of_xml)
          (Xml.child xml_arg0 "statusReason") in
      let targetDefinition =
        (Option.map ~f:TargetDefinition.of_xml)
          (Xml.child xml_arg0 "targetDefinition") in
      let format =
        (Option.map ~f:StreamFormat.of_xml) (Xml.child xml_arg0 "format") in
      let ordering =
        (Option.map ~f:StreamOrdering.of_xml) (Xml.child xml_arg0 "ordering") in
      let creationTime =
        (Option.map ~f:StreamCreationTime.of_xml)
          (Xml.child xml_arg0 "creationTime") in
      let status =
        (Option.map ~f:StreamStatus.of_xml) (Xml.child xml_arg0 "status") in
      let arn = (Option.map ~f:StreamArn.of_xml) (Xml.child xml_arg0 "arn") in
      let streamIdentifier =
        (Option.map ~f:StreamId.of_xml)
          (Xml.child xml_arg0 "streamIdentifier") in
      let clusterIdentifier =
        (Option.map ~f:ClusterId.of_xml)
          (Xml.child xml_arg0 "clusterIdentifier") in
      make ?tags ?statusReason ?targetDefinition ?format ?ordering
        ?creationTime ?status ?arn ?streamIdentifier ?clusterIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let statusReason = field_map json__ "statusReason" StatusReason.of_json in
      let targetDefinition =
        field_map json__ "targetDefinition" TargetDefinition.of_json in
      let format = field_map json__ "format" StreamFormat.of_json in
      let ordering = field_map json__ "ordering" StreamOrdering.of_json in
      let creationTime =
        field_map json__ "creationTime" StreamCreationTime.of_json in
      let status = field_map json__ "status" StreamStatus.of_json in
      let arn = field_map json__ "arn" StreamArn.of_json in
      let streamIdentifier =
        field_map json__ "streamIdentifier" StreamId.of_json in
      let clusterIdentifier =
        field_map json__ "clusterIdentifier" ClusterId.of_json in
      make ?tags ?statusReason ?targetDefinition ?format ?ordering
        ?creationTime ?status ?arn ?streamIdentifier ?clusterIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output of a retrieved stream."]
module GetStreamInput =
  struct
    type nonrec t =
      {
      clusterIdentifier: ClusterId.t
        [@ocaml.doc
          "The ID of the cluster containing the stream to retrieve."];
      streamIdentifier: StreamId.t
        [@ocaml.doc "The ID of the stream to retrieve."]}
    let context_ = "GetStreamInput"
    let make ~clusterIdentifier =
      fun ~streamIdentifier ->
        fun () -> { clusterIdentifier; streamIdentifier }
    let to_value x =
      structure_to_value
        [("clusterIdentifier",
           (Some (ClusterId.to_value x.clusterIdentifier)));
        ("streamIdentifier", (Some (StreamId.to_value x.streamIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamIdentifier =
        StreamId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "streamIdentifier") in
      let clusterIdentifier =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "clusterIdentifier") in
      make ~streamIdentifier ~clusterIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let streamIdentifier =
        field_map_exn json__ "streamIdentifier" StreamId.of_json in
      let clusterIdentifier =
        field_map_exn json__ "clusterIdentifier" ClusterId.of_json in
      make ~streamIdentifier ~clusterIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves information about a stream."]
module GetClusterPolicyOutput =
  struct
    type nonrec t =
      {
      policy: PolicyDocument.t option
        [@ocaml.doc
          "The resource-based policy document attached to the cluster, returned as a JSON string."];
      policyVersion: PolicyVersion.t option
        [@ocaml.doc
          "The version of the policy document. This version number is incremented each time the policy is updated."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?policy =
      fun ?policyVersion -> fun () -> { policy; policyVersion }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("policy", (Option.map x.policy ~f:PolicyDocument.to_value));
        ("policyVersion",
          (Option.map x.policyVersion ~f:PolicyVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyVersion =
        (Option.map ~f:PolicyVersion.of_xml)
          (Xml.child xml_arg0 "policyVersion") in
      let policy =
        (Option.map ~f:PolicyDocument.of_xml) (Xml.child xml_arg0 "policy") in
      make ?policyVersion ?policy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyVersion =
        field_map json__ "policyVersion" PolicyVersion.of_json in
      let policy = field_map json__ "policy" PolicyDocument.of_json in
      make ?policyVersion ?policy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the resource-based policy document attached to a cluster. This policy defines the access permissions and conditions for the cluster."]
module GetClusterPolicyInput =
  struct
    type nonrec t =
      {
      identifier: ClusterId.t
        [@ocaml.doc "The ID of the cluster to retrieve the policy from."]}
    let context_ = "GetClusterPolicyInput"
    let make ~identifier = fun () -> { identifier }
    let to_value x =
      structure_to_value
        [("identifier", (Some (ClusterId.to_value x.identifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identifier =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "identifier") in
      make ~identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let identifier = field_map_exn json__ "identifier" ClusterId.of_json in
      make ~identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the resource-based policy document attached to a cluster. This policy defines the access permissions and conditions for the cluster."]
module GetClusterOutput =
  struct
    type nonrec t =
      {
      identifier: ClusterId.t option
        [@ocaml.doc "The ID of the retrieved cluster."];
      arn: ClusterArn.t option
        [@ocaml.doc "The ARN of the retrieved cluster."];
      status: ClusterStatus.t option
        [@ocaml.doc "The status of the retrieved cluster."];
      creationTime: ClusterCreationTime.t option
        [@ocaml.doc "The time of when the cluster was created."];
      deletionProtectionEnabled: DeletionProtectionEnabled.t option
        [@ocaml.doc
          "Whether deletion protection is enabled in this cluster."];
      multiRegionProperties: MultiRegionProperties.t option
        [@ocaml.doc
          "Returns the current multi-Region cluster configuration, including witness region and linked cluster information."];
      tags: TagMap.t option ;
      encryptionDetails: EncryptionDetails.t option
        [@ocaml.doc
          "The current encryption configuration details for the cluster."];
      endpoint: Endpoint.t option
        [@ocaml.doc "The connection endpoint for the cluster."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?identifier =
      fun ?arn ->
        fun ?status ->
          fun ?creationTime ->
            fun ?deletionProtectionEnabled ->
              fun ?multiRegionProperties ->
                fun ?tags ->
                  fun ?encryptionDetails ->
                    fun ?endpoint ->
                      fun () ->
                        {
                          identifier;
                          arn;
                          status;
                          creationTime;
                          deletionProtectionEnabled;
                          multiRegionProperties;
                          tags;
                          encryptionDetails;
                          endpoint
                        }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("identifier", (Option.map x.identifier ~f:ClusterId.to_value));
        ("arn", (Option.map x.arn ~f:ClusterArn.to_value));
        ("status", (Option.map x.status ~f:ClusterStatus.to_value));
        ("creationTime",
          (Option.map x.creationTime ~f:ClusterCreationTime.to_value));
        ("deletionProtectionEnabled",
          (Option.map x.deletionProtectionEnabled
             ~f:DeletionProtectionEnabled.to_value));
        ("multiRegionProperties",
          (Option.map x.multiRegionProperties
             ~f:MultiRegionProperties.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value));
        ("encryptionDetails",
          (Option.map x.encryptionDetails ~f:EncryptionDetails.to_value));
        ("endpoint", (Option.map x.endpoint ~f:Endpoint.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endpoint =
        (Option.map ~f:Endpoint.of_xml) (Xml.child xml_arg0 "endpoint") in
      let encryptionDetails =
        (Option.map ~f:EncryptionDetails.of_xml)
          (Xml.child xml_arg0 "encryptionDetails") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let multiRegionProperties =
        (Option.map ~f:MultiRegionProperties.of_xml)
          (Xml.child xml_arg0 "multiRegionProperties") in
      let deletionProtectionEnabled =
        (Option.map ~f:DeletionProtectionEnabled.of_xml)
          (Xml.child xml_arg0 "deletionProtectionEnabled") in
      let creationTime =
        (Option.map ~f:ClusterCreationTime.of_xml)
          (Xml.child xml_arg0 "creationTime") in
      let status =
        (Option.map ~f:ClusterStatus.of_xml) (Xml.child xml_arg0 "status") in
      let arn = (Option.map ~f:ClusterArn.of_xml) (Xml.child xml_arg0 "arn") in
      let identifier =
        (Option.map ~f:ClusterId.of_xml) (Xml.child xml_arg0 "identifier") in
      make ?endpoint ?encryptionDetails ?tags ?multiRegionProperties
        ?deletionProtectionEnabled ?creationTime ?status ?arn ?identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let endpoint = field_map json__ "endpoint" Endpoint.of_json in
      let encryptionDetails =
        field_map json__ "encryptionDetails" EncryptionDetails.of_json in
      let tags = field_map json__ "tags" TagMap.of_json in
      let multiRegionProperties =
        field_map json__ "multiRegionProperties"
          MultiRegionProperties.of_json in
      let deletionProtectionEnabled =
        field_map json__ "deletionProtectionEnabled"
          DeletionProtectionEnabled.of_json in
      let creationTime =
        field_map json__ "creationTime" ClusterCreationTime.of_json in
      let status = field_map json__ "status" ClusterStatus.of_json in
      let arn = field_map json__ "arn" ClusterArn.of_json in
      let identifier = field_map json__ "identifier" ClusterId.of_json in
      make ?endpoint ?encryptionDetails ?tags ?multiRegionProperties
        ?deletionProtectionEnabled ?creationTime ?status ?arn ?identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output of a cluster."]
module GetClusterInput =
  struct
    type nonrec t =
      {
      identifier: ClusterId.t
        [@ocaml.doc "The ID of the cluster to retrieve."]}
    let context_ = "GetClusterInput"
    let make ~identifier = fun () -> { identifier }
    let to_value x =
      structure_to_value
        [("identifier", (Some (ClusterId.to_value x.identifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identifier =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "identifier") in
      make ~identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let identifier = field_map_exn json__ "identifier" ClusterId.of_json in
      make ~identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves information about a cluster."]
module DeleteStreamOutput =
  struct
    type nonrec t =
      {
      clusterIdentifier: ClusterId.t option
        [@ocaml.doc "The ID of the cluster for the deleted stream."];
      streamIdentifier: StreamId.t option
        [@ocaml.doc "The ID of the deleted stream."];
      arn: StreamArn.t option [@ocaml.doc "The ARN of the deleted stream."];
      status: StreamStatus.t option [@ocaml.doc "The status of the stream."];
      creationTime: StreamCreationTime.t option
        [@ocaml.doc "The time when the stream was created."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?clusterIdentifier =
      fun ?streamIdentifier ->
        fun ?arn ->
          fun ?status ->
            fun ?creationTime ->
              fun () ->
                {
                  clusterIdentifier;
                  streamIdentifier;
                  arn;
                  status;
                  creationTime
                }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clusterIdentifier",
           (Option.map x.clusterIdentifier ~f:ClusterId.to_value));
        ("streamIdentifier",
          (Option.map x.streamIdentifier ~f:StreamId.to_value));
        ("arn", (Option.map x.arn ~f:StreamArn.to_value));
        ("status", (Option.map x.status ~f:StreamStatus.to_value));
        ("creationTime",
          (Option.map x.creationTime ~f:StreamCreationTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationTime =
        (Option.map ~f:StreamCreationTime.of_xml)
          (Xml.child xml_arg0 "creationTime") in
      let status =
        (Option.map ~f:StreamStatus.of_xml) (Xml.child xml_arg0 "status") in
      let arn = (Option.map ~f:StreamArn.of_xml) (Xml.child xml_arg0 "arn") in
      let streamIdentifier =
        (Option.map ~f:StreamId.of_xml)
          (Xml.child xml_arg0 "streamIdentifier") in
      let clusterIdentifier =
        (Option.map ~f:ClusterId.of_xml)
          (Xml.child xml_arg0 "clusterIdentifier") in
      make ?creationTime ?status ?arn ?streamIdentifier ?clusterIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let creationTime =
        field_map json__ "creationTime" StreamCreationTime.of_json in
      let status = field_map json__ "status" StreamStatus.of_json in
      let arn = field_map json__ "arn" StreamArn.of_json in
      let streamIdentifier =
        field_map json__ "streamIdentifier" StreamId.of_json in
      let clusterIdentifier =
        field_map json__ "clusterIdentifier" ClusterId.of_json in
      make ?creationTime ?status ?arn ?streamIdentifier ?clusterIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output from a deleted stream."]
module DeleteStreamInput =
  struct
    type nonrec t =
      {
      clusterIdentifier: ClusterId.t
        [@ocaml.doc "The ID of the cluster containing the stream to delete."];
      streamIdentifier: StreamId.t
        [@ocaml.doc "The ID of the stream to delete."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, the subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the Amazon Web Services SDK automatically generates one."]}
    let context_ = "DeleteStreamInput"
    let make ?clientToken =
      fun ~clusterIdentifier ->
        fun ~streamIdentifier ->
          fun () -> { clientToken; clusterIdentifier; streamIdentifier }
    let to_value x =
      structure_to_value
        [("clusterIdentifier",
           (Some (ClusterId.to_value x.clusterIdentifier)));
        ("streamIdentifier", (Some (StreamId.to_value x.streamIdentifier)));
        ("client-token", (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml)
          (Xml.child xml_arg0 "client-token") in
      let streamIdentifier =
        StreamId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "streamIdentifier") in
      let clusterIdentifier =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "clusterIdentifier") in
      make ?clientToken ~streamIdentifier ~clusterIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let streamIdentifier =
        field_map_exn json__ "streamIdentifier" StreamId.of_json in
      let clusterIdentifier =
        field_map_exn json__ "clusterIdentifier" ClusterId.of_json in
      make ?clientToken ~streamIdentifier ~clusterIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a stream from a cluster."]
module DeleteClusterPolicyOutput =
  struct
    type nonrec t =
      {
      policyVersion: PolicyVersion.t option
        [@ocaml.doc "The version of the policy that was deleted."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?policyVersion = fun () -> { policyVersion }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("policyVersion",
           (Option.map x.policyVersion ~f:PolicyVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyVersion =
        (Option.map ~f:PolicyVersion.of_xml)
          (Xml.child xml_arg0 "policyVersion") in
      make ?policyVersion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyVersion =
        field_map json__ "policyVersion" PolicyVersion.of_json in
      make ?policyVersion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the resource-based policy attached to a cluster. This removes all access permissions defined by the policy, reverting to default access controls."]
module DeleteClusterPolicyInput =
  struct
    type nonrec t =
      {
      identifier: ClusterId.t ;
      expectedPolicyVersion: PolicyVersion.t option
        [@ocaml.doc
          "The expected version of the policy to delete. This parameter ensures that you're deleting the correct version of the policy and helps prevent accidental deletions."];
      clientToken: ClientToken.t option }
    let context_ = "DeleteClusterPolicyInput"
    let make ?expectedPolicyVersion =
      fun ?clientToken ->
        fun ~identifier ->
          fun () -> { expectedPolicyVersion; clientToken; identifier }
    let to_value x =
      structure_to_value
        [("identifier", (Some (ClusterId.to_value x.identifier)));
        ("expected-policy-version",
          (Option.map x.expectedPolicyVersion ~f:PolicyVersion.to_value));
        ("client-token", (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml)
          (Xml.child xml_arg0 "client-token") in
      let expectedPolicyVersion =
        (Option.map ~f:PolicyVersion.of_xml)
          (Xml.child xml_arg0 "expected-policy-version") in
      let identifier =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "identifier") in
      make ?clientToken ?expectedPolicyVersion ~identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let expectedPolicyVersion =
        field_map json__ "expectedPolicyVersion" PolicyVersion.of_json in
      let identifier = field_map_exn json__ "identifier" ClusterId.of_json in
      make ?clientToken ?expectedPolicyVersion ~identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the resource-based policy attached to a cluster. This removes all access permissions defined by the policy, reverting to default access controls."]
module DeleteClusterOutput =
  struct
    type nonrec t =
      {
      identifier: ClusterId.t option
        [@ocaml.doc "The ID of the deleted cluster."];
      arn: ClusterArn.t option [@ocaml.doc "The ARN of the deleted cluster."];
      status: ClusterStatus.t option
        [@ocaml.doc "The status of the cluster."];
      creationTime: ClusterCreationTime.t option
        [@ocaml.doc "The time of when the cluster was created."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?identifier =
      fun ?arn ->
        fun ?status ->
          fun ?creationTime ->
            fun () -> { identifier; arn; status; creationTime }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("identifier", (Option.map x.identifier ~f:ClusterId.to_value));
        ("arn", (Option.map x.arn ~f:ClusterArn.to_value));
        ("status", (Option.map x.status ~f:ClusterStatus.to_value));
        ("creationTime",
          (Option.map x.creationTime ~f:ClusterCreationTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationTime =
        (Option.map ~f:ClusterCreationTime.of_xml)
          (Xml.child xml_arg0 "creationTime") in
      let status =
        (Option.map ~f:ClusterStatus.of_xml) (Xml.child xml_arg0 "status") in
      let arn = (Option.map ~f:ClusterArn.of_xml) (Xml.child xml_arg0 "arn") in
      let identifier =
        (Option.map ~f:ClusterId.of_xml) (Xml.child xml_arg0 "identifier") in
      make ?creationTime ?status ?arn ?identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let creationTime =
        field_map json__ "creationTime" ClusterCreationTime.of_json in
      let status = field_map json__ "status" ClusterStatus.of_json in
      let arn = field_map json__ "arn" ClusterArn.of_json in
      let identifier = field_map json__ "identifier" ClusterId.of_json in
      make ?creationTime ?status ?arn ?identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output from a deleted cluster."]
module DeleteClusterInput =
  struct
    type nonrec t =
      {
      identifier: ClusterId.t [@ocaml.doc "The ID of the cluster to delete."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully. The subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the Amazon Web Services SDK automatically generates one."]}
    let context_ = "DeleteClusterInput"
    let make ?clientToken =
      fun ~identifier -> fun () -> { clientToken; identifier }
    let to_value x =
      structure_to_value
        [("identifier", (Some (ClusterId.to_value x.identifier)));
        ("client-token", (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml)
          (Xml.child xml_arg0 "client-token") in
      let identifier =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "identifier") in
      make ?clientToken ~identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let identifier = field_map_exn json__ "identifier" ClusterId.of_json in
      make ?clientToken ~identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a cluster in Amazon Aurora DSQL."]
module CreateStreamOutput =
  struct
    type nonrec t =
      {
      clusterIdentifier: ClusterId.t option
        [@ocaml.doc "The ID of the cluster for the created stream."];
      streamIdentifier: StreamId.t option
        [@ocaml.doc "The ID of the created stream."];
      arn: StreamArn.t option [@ocaml.doc "The ARN of the created stream."];
      status: StreamStatus.t option
        [@ocaml.doc "The status of the created stream."];
      creationTime: StreamCreationTime.t option
        [@ocaml.doc "The time when created the stream."];
      ordering: StreamOrdering.t option
        [@ocaml.doc "The ordering mode of the created stream."];
      format: StreamFormat.t option
        [@ocaml.doc "The format of the created stream records."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?clusterIdentifier =
      fun ?streamIdentifier ->
        fun ?arn ->
          fun ?status ->
            fun ?creationTime ->
              fun ?ordering ->
                fun ?format ->
                  fun () ->
                    {
                      clusterIdentifier;
                      streamIdentifier;
                      arn;
                      status;
                      creationTime;
                      ordering;
                      format
                    }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clusterIdentifier",
           (Option.map x.clusterIdentifier ~f:ClusterId.to_value));
        ("streamIdentifier",
          (Option.map x.streamIdentifier ~f:StreamId.to_value));
        ("arn", (Option.map x.arn ~f:StreamArn.to_value));
        ("status", (Option.map x.status ~f:StreamStatus.to_value));
        ("creationTime",
          (Option.map x.creationTime ~f:StreamCreationTime.to_value));
        ("ordering", (Option.map x.ordering ~f:StreamOrdering.to_value));
        ("format", (Option.map x.format ~f:StreamFormat.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let format =
        (Option.map ~f:StreamFormat.of_xml) (Xml.child xml_arg0 "format") in
      let ordering =
        (Option.map ~f:StreamOrdering.of_xml) (Xml.child xml_arg0 "ordering") in
      let creationTime =
        (Option.map ~f:StreamCreationTime.of_xml)
          (Xml.child xml_arg0 "creationTime") in
      let status =
        (Option.map ~f:StreamStatus.of_xml) (Xml.child xml_arg0 "status") in
      let arn = (Option.map ~f:StreamArn.of_xml) (Xml.child xml_arg0 "arn") in
      let streamIdentifier =
        (Option.map ~f:StreamId.of_xml)
          (Xml.child xml_arg0 "streamIdentifier") in
      let clusterIdentifier =
        (Option.map ~f:ClusterId.of_xml)
          (Xml.child xml_arg0 "clusterIdentifier") in
      make ?format ?ordering ?creationTime ?status ?arn ?streamIdentifier
        ?clusterIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let format = field_map json__ "format" StreamFormat.of_json in
      let ordering = field_map json__ "ordering" StreamOrdering.of_json in
      let creationTime =
        field_map json__ "creationTime" StreamCreationTime.of_json in
      let status = field_map json__ "status" StreamStatus.of_json in
      let arn = field_map json__ "arn" StreamArn.of_json in
      let streamIdentifier =
        field_map json__ "streamIdentifier" StreamId.of_json in
      let clusterIdentifier =
        field_map json__ "clusterIdentifier" ClusterId.of_json in
      make ?format ?ordering ?creationTime ?status ?arn ?streamIdentifier
        ?clusterIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output of a created stream."]
module CreateStreamInput =
  struct
    type nonrec t =
      {
      clusterIdentifier: ClusterId.t
        [@ocaml.doc "The ID of the cluster for which to create the stream."];
      targetDefinition: TargetDefinition.t
        [@ocaml.doc
          "The target destination configuration for the stream. Contains Kinesis stream configuration including stream ARN and IAM role ARN."];
      ordering: StreamOrdering.t
        [@ocaml.doc
          "The ordering mode for the stream. Determines how change events are ordered when delivered to the target."];
      format: StreamFormat.t [@ocaml.doc "The format of the stream records."];
      tags: TagMap.t option
        [@ocaml.doc
          "A map of key and value pairs to use to tag your stream."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, the subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the Amazon Web Services SDK automatically generates one."]}
    let context_ = "CreateStreamInput"
    let make ?tags =
      fun ?clientToken ->
        fun ~clusterIdentifier ->
          fun ~targetDefinition ->
            fun ~ordering ->
              fun ~format ->
                fun () ->
                  {
                    tags;
                    clientToken;
                    clusterIdentifier;
                    targetDefinition;
                    ordering;
                    format
                  }
    let to_value x =
      structure_to_value
        [("clusterIdentifier",
           (Some (ClusterId.to_value x.clusterIdentifier)));
        ("targetDefinition",
          (Some (TargetDefinition.to_value x.targetDefinition)));
        ("ordering", (Some (StreamOrdering.to_value x.ordering)));
        ("format", (Some (StreamFormat.to_value x.format)));
        ("tags", (Option.map x.tags ~f:TagMap.to_value));
        ("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let format =
        StreamFormat.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "format") in
      let ordering =
        StreamOrdering.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ordering") in
      let targetDefinition =
        TargetDefinition.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "targetDefinition") in
      let clusterIdentifier =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "clusterIdentifier") in
      make ?clientToken ?tags ~format ~ordering ~targetDefinition
        ~clusterIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let tags = field_map json__ "tags" TagMap.of_json in
      let format = field_map_exn json__ "format" StreamFormat.of_json in
      let ordering = field_map_exn json__ "ordering" StreamOrdering.of_json in
      let targetDefinition =
        field_map_exn json__ "targetDefinition" TargetDefinition.of_json in
      let clusterIdentifier =
        field_map_exn json__ "clusterIdentifier" ClusterId.of_json in
      make ?clientToken ?tags ~format ~ordering ~targetDefinition
        ~clusterIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new change data capture (CDC) stream for a cluster. The stream captures database changes and delivers them to the specified target destination. Required permissions dsql:CreateStream Permission to create a new stream. Resources: arn:aws:dsql:region:account-id:cluster/cluster-id iam:PassRole Permission to pass the IAM role specified in the target definition to the service. Resources: ARN of the IAM role specified in targetDefinition.kinesis.roleArn kms:Decrypt Required when the cluster uses a customer managed KMS key (CMK). Permission to decrypt data using the cluster's CMK. Resources: ARN of the KMS key used by the cluster"]
module CreateClusterOutput =
  struct
    type nonrec t =
      {
      identifier: ClusterId.t option
        [@ocaml.doc "The ID of the created cluster."];
      arn: ClusterArn.t option [@ocaml.doc "The ARN of the created cluster."];
      status: ClusterStatus.t option
        [@ocaml.doc "The status of the created cluster."];
      creationTime: ClusterCreationTime.t option
        [@ocaml.doc "The time of when created the cluster."];
      multiRegionProperties: MultiRegionProperties.t option
        [@ocaml.doc
          "The multi-Region cluster configuration details that were set during cluster creation"];
      encryptionDetails: EncryptionDetails.t option
        [@ocaml.doc
          "The encryption configuration for the cluster that was specified during the creation process, including the KMS key identifier and encryption state."];
      deletionProtectionEnabled: DeletionProtectionEnabled.t option
        [@ocaml.doc
          "Whether deletion protection is enabled on this cluster."];
      endpoint: Endpoint.t option
        [@ocaml.doc "The connection endpoint for the created cluster."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?identifier =
      fun ?arn ->
        fun ?status ->
          fun ?creationTime ->
            fun ?multiRegionProperties ->
              fun ?encryptionDetails ->
                fun ?deletionProtectionEnabled ->
                  fun ?endpoint ->
                    fun () ->
                      {
                        identifier;
                        arn;
                        status;
                        creationTime;
                        multiRegionProperties;
                        encryptionDetails;
                        deletionProtectionEnabled;
                        endpoint
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("identifier", (Option.map x.identifier ~f:ClusterId.to_value));
        ("arn", (Option.map x.arn ~f:ClusterArn.to_value));
        ("status", (Option.map x.status ~f:ClusterStatus.to_value));
        ("creationTime",
          (Option.map x.creationTime ~f:ClusterCreationTime.to_value));
        ("multiRegionProperties",
          (Option.map x.multiRegionProperties
             ~f:MultiRegionProperties.to_value));
        ("encryptionDetails",
          (Option.map x.encryptionDetails ~f:EncryptionDetails.to_value));
        ("deletionProtectionEnabled",
          (Option.map x.deletionProtectionEnabled
             ~f:DeletionProtectionEnabled.to_value));
        ("endpoint", (Option.map x.endpoint ~f:Endpoint.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endpoint =
        (Option.map ~f:Endpoint.of_xml) (Xml.child xml_arg0 "endpoint") in
      let deletionProtectionEnabled =
        (Option.map ~f:DeletionProtectionEnabled.of_xml)
          (Xml.child xml_arg0 "deletionProtectionEnabled") in
      let encryptionDetails =
        (Option.map ~f:EncryptionDetails.of_xml)
          (Xml.child xml_arg0 "encryptionDetails") in
      let multiRegionProperties =
        (Option.map ~f:MultiRegionProperties.of_xml)
          (Xml.child xml_arg0 "multiRegionProperties") in
      let creationTime =
        (Option.map ~f:ClusterCreationTime.of_xml)
          (Xml.child xml_arg0 "creationTime") in
      let status =
        (Option.map ~f:ClusterStatus.of_xml) (Xml.child xml_arg0 "status") in
      let arn = (Option.map ~f:ClusterArn.of_xml) (Xml.child xml_arg0 "arn") in
      let identifier =
        (Option.map ~f:ClusterId.of_xml) (Xml.child xml_arg0 "identifier") in
      make ?endpoint ?deletionProtectionEnabled ?encryptionDetails
        ?multiRegionProperties ?creationTime ?status ?arn ?identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let endpoint = field_map json__ "endpoint" Endpoint.of_json in
      let deletionProtectionEnabled =
        field_map json__ "deletionProtectionEnabled"
          DeletionProtectionEnabled.of_json in
      let encryptionDetails =
        field_map json__ "encryptionDetails" EncryptionDetails.of_json in
      let multiRegionProperties =
        field_map json__ "multiRegionProperties"
          MultiRegionProperties.of_json in
      let creationTime =
        field_map json__ "creationTime" ClusterCreationTime.of_json in
      let status = field_map json__ "status" ClusterStatus.of_json in
      let arn = field_map json__ "arn" ClusterArn.of_json in
      let identifier = field_map json__ "identifier" ClusterId.of_json in
      make ?endpoint ?deletionProtectionEnabled ?encryptionDetails
        ?multiRegionProperties ?creationTime ?status ?arn ?identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output of a created cluster."]
module CreateClusterInput =
  struct
    type nonrec t =
      {
      deletionProtectionEnabled: DeletionProtectionEnabled.t option
        [@ocaml.doc
          "If enabled, you can't delete your cluster. You must first disable this property before you can delete your cluster."];
      kmsEncryptionKey: KmsEncryptionKey.t option
        [@ocaml.doc
          "The KMS key that encrypts and protects the data on your cluster. You can specify the ARN, ID, or alias of an existing key or have Amazon Web Services create a default key for you."];
      tags: TagMap.t option
        [@ocaml.doc
          "A map of key and value pairs to use to tag your cluster."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, the subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the Amazon Web Services SDK automatically generates one."];
      multiRegionProperties: MultiRegionProperties.t option
        [@ocaml.doc
          "The configuration settings when creating a multi-Region cluster, including the witness region and linked cluster properties."];
      policy: PolicyDocument.t option
        [@ocaml.doc
          "An optional resource-based policy document in JSON format that defines access permissions for the cluster."];
      bypassPolicyLockoutSafetyCheck: BypassPolicyLockoutSafetyCheck.t option
        [@ocaml.doc
          "An optional field that controls whether to bypass the lockout prevention check. When set to true, this parameter allows you to apply a policy that might lock you out of the cluster. Use with caution."]}
    let make ?deletionProtectionEnabled =
      fun ?kmsEncryptionKey ->
        fun ?tags ->
          fun ?clientToken ->
            fun ?multiRegionProperties ->
              fun ?policy ->
                fun ?bypassPolicyLockoutSafetyCheck ->
                  fun () ->
                    {
                      deletionProtectionEnabled;
                      kmsEncryptionKey;
                      tags;
                      clientToken;
                      multiRegionProperties;
                      policy;
                      bypassPolicyLockoutSafetyCheck
                    }
    let to_value x =
      structure_to_value
        [("deletionProtectionEnabled",
           (Option.map x.deletionProtectionEnabled
              ~f:DeletionProtectionEnabled.to_value));
        ("kmsEncryptionKey",
          (Option.map x.kmsEncryptionKey ~f:KmsEncryptionKey.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value));
        ("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("multiRegionProperties",
          (Option.map x.multiRegionProperties
             ~f:MultiRegionProperties.to_value));
        ("policy", (Option.map x.policy ~f:PolicyDocument.to_value));
        ("bypassPolicyLockoutSafetyCheck",
          (Option.map x.bypassPolicyLockoutSafetyCheck
             ~f:BypassPolicyLockoutSafetyCheck.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bypassPolicyLockoutSafetyCheck =
        (Option.map ~f:BypassPolicyLockoutSafetyCheck.of_xml)
          (Xml.child xml_arg0 "bypassPolicyLockoutSafetyCheck") in
      let policy =
        (Option.map ~f:PolicyDocument.of_xml) (Xml.child xml_arg0 "policy") in
      let multiRegionProperties =
        (Option.map ~f:MultiRegionProperties.of_xml)
          (Xml.child xml_arg0 "multiRegionProperties") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let kmsEncryptionKey =
        (Option.map ~f:KmsEncryptionKey.of_xml)
          (Xml.child xml_arg0 "kmsEncryptionKey") in
      let deletionProtectionEnabled =
        (Option.map ~f:DeletionProtectionEnabled.of_xml)
          (Xml.child xml_arg0 "deletionProtectionEnabled") in
      make ?bypassPolicyLockoutSafetyCheck ?policy ?multiRegionProperties
        ?clientToken ?tags ?kmsEncryptionKey ?deletionProtectionEnabled ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bypassPolicyLockoutSafetyCheck =
        field_map json__ "bypassPolicyLockoutSafetyCheck"
          BypassPolicyLockoutSafetyCheck.of_json in
      let policy = field_map json__ "policy" PolicyDocument.of_json in
      let multiRegionProperties =
        field_map json__ "multiRegionProperties"
          MultiRegionProperties.of_json in
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let tags = field_map json__ "tags" TagMap.of_json in
      let kmsEncryptionKey =
        field_map json__ "kmsEncryptionKey" KmsEncryptionKey.of_json in
      let deletionProtectionEnabled =
        field_map json__ "deletionProtectionEnabled"
          DeletionProtectionEnabled.of_json in
      make ?bypassPolicyLockoutSafetyCheck ?policy ?multiRegionProperties
        ?clientToken ?tags ?kmsEncryptionKey ?deletionProtectionEnabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The CreateCluster API allows you to create both single-Region clusters and multi-Region clusters. With the addition of the multiRegionProperties parameter, you can create a cluster with witness Region support and establish peer relationships with clusters in other Regions during creation. Creating multi-Region clusters requires additional IAM permissions beyond those needed for single-Region clusters, as detailed in the Required permissions section below. Required permissions dsql:CreateCluster Required to create a cluster. Resources: arn:aws:dsql:region:account-id:cluster/* dsql:TagResource Permission to add tags to a resource. Resources: arn:aws:dsql:region:account-id:cluster/* dsql:PutMultiRegionProperties Permission to configure multi-Region properties for a cluster. Resources: arn:aws:dsql:region:account-id:cluster/* dsql:AddPeerCluster When specifying multiRegionProperties.clusters, permission to add peer clusters. Resources: Local cluster: arn:aws:dsql:region:account-id:cluster/* Each peer cluster: exact ARN of each specified peer cluster dsql:PutWitnessRegion When specifying multiRegionProperties.witnessRegion, permission to set a witness Region. This permission is checked both in the cluster Region and in the witness Region. Resources: arn:aws:dsql:region:account-id:cluster/* Condition Keys: dsql:WitnessRegion (matching the specified witness region) The witness Region specified in multiRegionProperties.witnessRegion cannot be the same as the cluster's Region."]
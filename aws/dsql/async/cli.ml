(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deletionProtectionEnabled =
         flag "deletion-protection-enabled" (optional bool)
           ~doc:"BOOL DeletionProtectionEnabled"
       and kmsEncryptionKey =
         flag "kms-encryption-key" (optional string)
           ~doc:"STRING KmsEncryptionKey"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and multiRegionProperties =
         flag "multi-region-properties" (optional json_arg)
           ~doc:"JSON MultiRegionProperties"
       and policy =
         flag "policy" (optional string) ~doc:"STRING PolicyDocument"
       and bypassPolicyLockoutSafetyCheck =
         flag "bypass-policy-lockout-safety-check" (optional bool)
           ~doc:"BOOL BypassPolicyLockoutSafetyCheck" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster
           (Values.CreateClusterInput.make ?deletionProtectionEnabled
              ?kmsEncryptionKey
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?clientToken
              ?multiRegionProperties:(Option.map
                                        ~f:Values.MultiRegionProperties.of_json
                                        multiRegionProperties) ?policy
              ?bypassPolicyLockoutSafetyCheck ())
           (Some Values.CreateClusterOutput.to_json)
           (Some Values.CreateClusterOutput.error_to_json)])
let create_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING ClusterId"
       and targetDefinition =
         flag "target-definition" (required json_arg)
           ~doc:"JSON TargetDefinition"
       and ordering =
         flag "ordering" (required json_arg) ~doc:"JSON StreamOrdering"
       and format =
         flag "format" (required json_arg) ~doc:"JSON StreamFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_stream
           (Values.CreateStreamInput.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?clientToken
              ~clusterIdentifier
              ~targetDefinition:(Values.TargetDefinition.of_json
                                   targetDefinition)
              ~ordering:(Values.StreamOrdering.of_json ordering)
              ~format:(Values.StreamFormat.of_json format) ())
           (Some Values.CreateStreamOutput.to_json)
           (Some Values.CreateStreamOutput.error_to_json)])
let delete_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster
           (Values.DeleteClusterInput.make ?clientToken ~identifier ())
           (Some Values.DeleteClusterOutput.to_json)
           (Some Values.DeleteClusterOutput.error_to_json)])
let delete_cluster_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedPolicyVersion =
         flag "expected-policy-version" (optional string)
           ~doc:"STRING PolicyVersion"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster_policy
           (Values.DeleteClusterPolicyInput.make ?expectedPolicyVersion
              ?clientToken ~identifier ())
           (Some Values.DeleteClusterPolicyOutput.to_json)
           (Some Values.DeleteClusterPolicyOutput.error_to_json)])
let delete_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING ClusterId"
       and streamIdentifier =
         flag "stream-identifier" (required string) ~doc:"STRING StreamId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_stream
           (Values.DeleteStreamInput.make ?clientToken ~clusterIdentifier
              ~streamIdentifier ()) (Some Values.DeleteStreamOutput.to_json)
           (Some Values.DeleteStreamOutput.error_to_json)])
let get_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cluster (Values.GetClusterInput.make ~identifier ())
           (Some Values.GetClusterOutput.to_json)
           (Some Values.GetClusterOutput.error_to_json)])
let get_cluster_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cluster_policy
           (Values.GetClusterPolicyInput.make ~identifier ())
           (Some Values.GetClusterPolicyOutput.to_json)
           (Some Values.GetClusterPolicyOutput.error_to_json)])
let get_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING ClusterId"
       and streamIdentifier =
         flag "stream-identifier" (required string) ~doc:"STRING StreamId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_stream
           (Values.GetStreamInput.make ~clusterIdentifier ~streamIdentifier
              ()) (Some Values.GetStreamOutput.to_json)
           (Some Values.GetStreamOutput.error_to_json)])
let get_vpc_endpoint_service_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vpc_endpoint_service_name
           (Values.GetVpcEndpointServiceNameInput.make ~identifier ())
           (Some Values.GetVpcEndpointServiceNameOutput.to_json)
           (Some Values.GetVpcEndpointServiceNameOutput.error_to_json)])
let list_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_clusters
           (Values.ListClustersInput.make ?maxResults ?nextToken ())
           (Some Values.ListClustersOutput.to_json)
           (Some Values.ListClustersOutput.error_to_json)])
let list_streams =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_streams
           (Values.ListStreamsInput.make ?maxResults ?nextToken
              ~clusterIdentifier ()) (Some Values.ListStreamsOutput.to_json)
           (Some Values.ListStreamsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let put_cluster_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bypassPolicyLockoutSafetyCheck =
         flag "bypass-policy-lockout-safety-check" (optional bool)
           ~doc:"BOOL BypassPolicyLockoutSafetyCheck"
       and expectedPolicyVersion =
         flag "expected-policy-version" (optional string)
           ~doc:"STRING PolicyVersion"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ClusterId"
       and policy =
         flag "policy" (required string) ~doc:"STRING PolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_cluster_policy
           (Values.PutClusterPolicyInput.make ?bypassPolicyLockoutSafetyCheck
              ?expectedPolicyVersion ?clientToken ~identifier ~policy ())
           (Some Values.PutClusterPolicyOutput.to_json)
           (Some Values.PutClusterPolicyOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deletionProtectionEnabled =
         flag "deletion-protection-enabled" (optional bool)
           ~doc:"BOOL DeletionProtectionEnabled"
       and kmsEncryptionKey =
         flag "kms-encryption-key" (optional string)
           ~doc:"STRING KmsEncryptionKey"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and multiRegionProperties =
         flag "multi-region-properties" (optional json_arg)
           ~doc:"JSON MultiRegionProperties"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cluster
           (Values.UpdateClusterInput.make ?deletionProtectionEnabled
              ?kmsEncryptionKey ?clientToken
              ?multiRegionProperties:(Option.map
                                        ~f:Values.MultiRegionProperties.of_json
                                        multiRegionProperties) ~identifier ())
           (Some Values.UpdateClusterOutput.to_json)
           (Some Values.UpdateClusterOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-cluster", create_cluster);
    ("create-stream", create_stream);
    ("delete-cluster", delete_cluster);
    ("delete-cluster-policy", delete_cluster_policy);
    ("delete-stream", delete_stream);
    ("get-cluster", get_cluster);
    ("get-cluster-policy", get_cluster_policy);
    ("get-stream", get_stream);
    ("get-vpc-endpoint-service-name", get_vpc_endpoint_service_name);
    ("list-clusters", list_clusters);
    ("list-streams", list_streams);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-cluster-policy", put_cluster_policy);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-cluster", update_cluster)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | Search: (SearchRequest.t, SearchResponse.t, SearchResponse.error) t 
  | Suggest: (SuggestRequest.t, SuggestResponse.t, SuggestResponse.error) t 
  | UploadDocuments: (UploadDocumentsRequest.t, UploadDocumentsResponse.t,
  UploadDocumentsResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function | Search -> `GET | Suggest -> `GET | UploadDocuments -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | Search ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/2013-01-01/search?format=sdk&pretty=true")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("cursor", (Cursor.to_header v)))
                  x.cursor;
               Option.map ~f:(fun v -> ("expr", (Expr.to_header v))) x.expr;
               Option.map ~f:(fun v -> ("facet", (Facet.to_header v)))
                 x.facet;
               Option.map ~f:(fun v -> ("fq", (FilterQuery.to_header v)))
                 x.filterQuery;
               Option.map
                 ~f:(fun v -> ("highlight", (Highlight.to_header v)))
                 x.highlight;
               Option.map ~f:(fun v -> ("partial", (Partial.to_header v)))
                 x.partial;
               Some ("q", (Query.to_header x.query));
               Option.map
                 ~f:(fun v -> ("q.options", (QueryOptions.to_header v)))
                 x.queryOptions;
               Option.map
                 ~f:(fun v -> ("q.parser", (QueryParser.to_header v)))
                 x.queryParser;
               Option.map ~f:(fun v -> ("return", (Return.to_header v)))
                 x.return;
               Option.map ~f:(fun v -> ("size", (Size.to_header v))) x.size;
               Option.map ~f:(fun v -> ("sort", (Sort.to_header v))) x.sort;
               Option.map ~f:(fun v -> ("start", (Start.to_header v)))
                 x.start;
               Option.map ~f:(fun v -> ("stats", (Stat.to_header v))) x.stats])
      | Suggest ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/2013-01-01/suggest?format=sdk&pretty=true")
            (List.filter_opt
               [Some ("q", (Query.to_header x.query));
               Some ("suggester", (Suggester.to_header x.suggester));
               Option.map
                 ~f:(fun v -> ("size", (SuggestionsSize.to_header v))) 
                 x.size])
      | UploadDocuments ->
          (Format.kasprintf Uri.of_string)
            "/2013-01-01/documents/batch?format=sdk")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | Search ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | Suggest ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UploadDocuments ->
      let headers =
        Some
          ((List.filter_opt
              [Some
                 ("Content-Type",
                   (ContentType.to_header
                      req.UploadDocumentsRequest.contentType))])
             |> Awso.Http.Headers.of_list) in
      let body = Blob.to_header req.documents in
      Awso.Http.Request.make ?headers ~body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | Search ->
      if is_success
      then Ok (SearchResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SearchResponse.error_of_json))
  | Suggest ->
      if is_success
      then Ok (SuggestResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SuggestResponse.error_of_json))
  | UploadDocuments ->
      if is_success
      then Ok (UploadDocumentsResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some UploadDocumentsResponse.error_of_json))
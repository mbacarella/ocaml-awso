(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let deregister_subscription_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionProviderArn =
         flag "subscription-provider-arn" (required string)
           ~doc:"STRING SubscriptionProviderArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_subscription_provider
           (Values.DeregisterSubscriptionProviderRequest.make
              ~subscriptionProviderArn ())
           (Some Values.DeregisterSubscriptionProviderResponse.to_json)
           (Some Values.DeregisterSubscriptionProviderResponse.error_to_json)])
let get_registered_subscription_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionProviderArn =
         flag "subscription-provider-arn" (required string)
           ~doc:"STRING SubscriptionProviderArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_registered_subscription_provider
           (Values.GetRegisteredSubscriptionProviderRequest.make
              ~subscriptionProviderArn ())
           (Some Values.GetRegisteredSubscriptionProviderResponse.to_json)
           (Some
              Values.GetRegisteredSubscriptionProviderResponse.error_to_json)])
let get_service_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_settings (Values.GetServiceSettingsRequest.make ())
           (Some Values.GetServiceSettingsResponse.to_json)
           (Some Values.GetServiceSettingsResponse.error_to_json)])
let list_linux_subscription_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListLinuxSubscriptionInstancesRequestNextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_linux_subscription_instances
           (Values.ListLinuxSubscriptionInstancesRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.ListLinuxSubscriptionInstancesResponse.to_json)
           (Some Values.ListLinuxSubscriptionInstancesResponse.error_to_json)])
let list_linux_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListLinuxSubscriptionsRequestNextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_linux_subscriptions
           (Values.ListLinuxSubscriptionsRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.ListLinuxSubscriptionsResponse.to_json)
           (Some Values.ListLinuxSubscriptionsResponse.error_to_json)])
let list_registered_subscription_providers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRegisteredSubscriptionProvidersRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and subscriptionProviderSources =
         flag "subscription-provider-sources" (optional json_arg)
           ~doc:"JSON SubscriptionProviderSourceList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_registered_subscription_providers
           (Values.ListRegisteredSubscriptionProvidersRequest.make
              ?maxResults ?nextToken
              ?subscriptionProviderSources:(Option.map
                                              ~f:Values.SubscriptionProviderSourceList.of_json
                                              subscriptionProviderSources) ())
           (Some Values.ListRegisteredSubscriptionProvidersResponse.to_json)
           (Some
              Values.ListRegisteredSubscriptionProvidersResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING SubscriptionProviderArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let register_subscription_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and secretArn =
         flag "secret-arn" (required string) ~doc:"STRING SecretArn"
       and subscriptionProviderSource =
         flag "subscription-provider-source" (required json_arg)
           ~doc:"JSON SubscriptionProviderSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_subscription_provider
           (Values.RegisterSubscriptionProviderRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~secretArn
              ~subscriptionProviderSource:(Values.SubscriptionProviderSource.of_json
                                             subscriptionProviderSource) ())
           (Some Values.RegisterSubscriptionProviderResponse.to_json)
           (Some Values.RegisterSubscriptionProviderResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING SubscriptionProviderArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING SubscriptionProviderArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_service_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allowUpdate =
         flag "allow-update" (optional bool) ~doc:"BOOL Boolean"
       and linuxSubscriptionsDiscovery =
         flag "linux-subscriptions-discovery" (required json_arg)
           ~doc:"JSON LinuxSubscriptionsDiscovery"
       and linuxSubscriptionsDiscoverySettings =
         flag "linux-subscriptions-discovery-settings" (required json_arg)
           ~doc:"JSON LinuxSubscriptionsDiscoverySettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_settings
           (Values.UpdateServiceSettingsRequest.make ?allowUpdate
              ~linuxSubscriptionsDiscovery:(Values.LinuxSubscriptionsDiscovery.of_json
                                              linuxSubscriptionsDiscovery)
              ~linuxSubscriptionsDiscoverySettings:(Values.LinuxSubscriptionsDiscoverySettings.of_json
                                                      linuxSubscriptionsDiscoverySettings)
              ()) (Some Values.UpdateServiceSettingsResponse.to_json)
           (Some Values.UpdateServiceSettingsResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("deregister-subscription-provider", deregister_subscription_provider);
    ("get-registered-subscription-provider",
      get_registered_subscription_provider);
    ("get-service-settings", get_service_settings);
    ("list-linux-subscription-instances", list_linux_subscription_instances);
    ("list-linux-subscriptions", list_linux_subscriptions);
    ("list-registered-subscription-providers",
      list_registered_subscription_providers);
    ("list-tags-for-resource", list_tags_for_resource);
    ("register-subscription-provider", register_subscription_provider);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-service-settings", update_service_settings)]
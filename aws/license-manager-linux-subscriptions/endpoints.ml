(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | DeregisterSubscriptionProvider: (DeregisterSubscriptionProviderRequest.t,
  DeregisterSubscriptionProviderResponse.t,
  DeregisterSubscriptionProviderResponse.error) t 
  | GetRegisteredSubscriptionProvider:
  (GetRegisteredSubscriptionProviderRequest.t,
  GetRegisteredSubscriptionProviderResponse.t,
  GetRegisteredSubscriptionProviderResponse.error) t 
  | GetServiceSettings: (GetServiceSettingsRequest.t,
  GetServiceSettingsResponse.t, GetServiceSettingsResponse.error) t 
  | ListLinuxSubscriptionInstances: (ListLinuxSubscriptionInstancesRequest.t,
  ListLinuxSubscriptionInstancesResponse.t,
  ListLinuxSubscriptionInstancesResponse.error) t 
  | ListLinuxSubscriptions: (ListLinuxSubscriptionsRequest.t,
  ListLinuxSubscriptionsResponse.t, ListLinuxSubscriptionsResponse.error) t 
  | ListRegisteredSubscriptionProviders:
  (ListRegisteredSubscriptionProvidersRequest.t,
  ListRegisteredSubscriptionProvidersResponse.t,
  ListRegisteredSubscriptionProvidersResponse.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | RegisterSubscriptionProvider: (RegisterSubscriptionProviderRequest.t,
  RegisterSubscriptionProviderResponse.t,
  RegisterSubscriptionProviderResponse.error) t 
  | TagResource: (TagResourceRequest.t, TagResourceResponse.t,
  TagResourceResponse.error) t 
  | UntagResource: (UntagResourceRequest.t, UntagResourceResponse.t,
  UntagResourceResponse.error) t 
  | UpdateServiceSettings: (UpdateServiceSettingsRequest.t,
  UpdateServiceSettingsResponse.t, UpdateServiceSettingsResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | DeregisterSubscriptionProvider -> `POST
  | GetRegisteredSubscriptionProvider -> `POST
  | GetServiceSettings -> `POST
  | ListLinuxSubscriptionInstances -> `POST
  | ListLinuxSubscriptions -> `POST
  | ListRegisteredSubscriptionProviders -> `POST
  | ListTagsForResource -> `GET
  | RegisterSubscriptionProvider -> `POST
  | TagResource -> `PUT
  | UntagResource -> `DELETE
  | UpdateServiceSettings -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | DeregisterSubscriptionProvider ->
          (Format.kasprintf Uri.of_string)
            "/subscription/DeregisterSubscriptionProvider"
      | GetRegisteredSubscriptionProvider ->
          (Format.kasprintf Uri.of_string)
            "/subscription/GetRegisteredSubscriptionProvider"
      | GetServiceSettings ->
          (Format.kasprintf Uri.of_string) "/subscription/GetServiceSettings"
      | ListLinuxSubscriptionInstances ->
          (Format.kasprintf Uri.of_string)
            "/subscription/ListLinuxSubscriptionInstances"
      | ListLinuxSubscriptions ->
          (Format.kasprintf Uri.of_string)
            "/subscription/ListLinuxSubscriptions"
      | ListRegisteredSubscriptionProviders ->
          (Format.kasprintf Uri.of_string)
            "/subscription/ListRegisteredSubscriptionProviders"
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (SubscriptionProviderArn.to_header
               x.ListTagsForResourceRequest.resourceArn)
      | RegisterSubscriptionProvider ->
          (Format.kasprintf Uri.of_string)
            "/subscription/RegisterSubscriptionProvider"
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (SubscriptionProviderArn.to_header
               x.TagResourceRequest.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (SubscriptionProviderArn.to_header
                  x.UntagResourceRequest.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))])
      | UpdateServiceSettings ->
          (Format.kasprintf Uri.of_string)
            "/subscription/UpdateServiceSettings")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | DeregisterSubscriptionProvider ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("SubscriptionProviderArn",
                           (SubscriptionProviderArn.to_value
                              req.DeregisterSubscriptionProviderRequest.subscriptionProviderArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetRegisteredSubscriptionProvider ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("SubscriptionProviderArn",
                           (SubscriptionProviderArn.to_value
                              req.GetRegisteredSubscriptionProviderRequest.subscriptionProviderArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetServiceSettings ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListLinuxSubscriptionInstances ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListLinuxSubscriptionInstancesRequest.filters
                         ~f:(fun x -> ("Filters", (FilterList.to_value x)));
                      Option.map
                        req.ListLinuxSubscriptionInstancesRequest.maxResults
                        ~f:(fun x -> ("MaxResults", (BoxInteger.to_value x)));
                      Option.map
                        req.ListLinuxSubscriptionInstancesRequest.nextToken
                        ~f:(fun x ->
                              ("NextToken",
                                (ListLinuxSubscriptionInstancesRequestNextTokenString.to_value
                                   x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListLinuxSubscriptions ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListLinuxSubscriptionsRequest.filters
                         ~f:(fun x -> ("Filters", (FilterList.to_value x)));
                      Option.map req.ListLinuxSubscriptionsRequest.maxResults
                        ~f:(fun x -> ("MaxResults", (BoxInteger.to_value x)));
                      Option.map req.ListLinuxSubscriptionsRequest.nextToken
                        ~f:(fun x ->
                              ("NextToken",
                                (ListLinuxSubscriptionsRequestNextTokenString.to_value
                                   x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListRegisteredSubscriptionProviders ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListRegisteredSubscriptionProvidersRequest.maxResults
                         ~f:(fun x ->
                               ("MaxResults",
                                 (ListRegisteredSubscriptionProvidersRequestMaxResultsInteger.to_value
                                    x)));
                      Option.map
                        req.ListRegisteredSubscriptionProvidersRequest.nextToken
                        ~f:(fun x -> ("NextToken", (String_.to_value x)));
                      Option.map
                        req.ListRegisteredSubscriptionProvidersRequest.subscriptionProviderSources
                        ~f:(fun x ->
                              ("SubscriptionProviderSources",
                                (SubscriptionProviderSourceList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | RegisterSubscriptionProvider ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("SecretArn",
                           (SecretArn.to_value
                              req.RegisterSubscriptionProviderRequest.secretArn));
                      Some
                        ("SubscriptionProviderSource",
                          (SubscriptionProviderSource.to_value
                             req.RegisterSubscriptionProviderRequest.subscriptionProviderSource));
                      Option.map req.RegisterSubscriptionProviderRequest.tags
                        ~f:(fun x -> ("Tags", (Tags.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateServiceSettings ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.UpdateServiceSettingsRequest.allowUpdate
                         ~f:(fun x -> ("AllowUpdate", (Boolean.to_value x)));
                      Some
                        ("LinuxSubscriptionsDiscovery",
                          (LinuxSubscriptionsDiscovery.to_value
                             req.UpdateServiceSettingsRequest.linuxSubscriptionsDiscovery));
                      Some
                        ("LinuxSubscriptionsDiscoverySettings",
                          (LinuxSubscriptionsDiscoverySettings.to_value
                             req.UpdateServiceSettingsRequest.linuxSubscriptionsDiscoverySettings))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | DeregisterSubscriptionProvider ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok
          (DeregisterSubscriptionProviderResponse.of_header_and_body
             (headers, ()))
      else
        Error
          (parse_aws_error
             (Some DeregisterSubscriptionProviderResponse.error_of_json))
  | GetRegisteredSubscriptionProvider ->
      if is_success
      then
        Ok
          (GetRegisteredSubscriptionProviderResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetRegisteredSubscriptionProviderResponse.error_of_json))
  | GetServiceSettings ->
      if is_success
      then Ok (GetServiceSettingsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetServiceSettingsResponse.error_of_json))
  | ListLinuxSubscriptionInstances ->
      if is_success
      then
        Ok
          (ListLinuxSubscriptionInstancesResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListLinuxSubscriptionInstancesResponse.error_of_json))
  | ListLinuxSubscriptions ->
      if is_success
      then
        Ok (ListLinuxSubscriptionsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListLinuxSubscriptionsResponse.error_of_json))
  | ListRegisteredSubscriptionProviders ->
      if is_success
      then
        Ok
          (ListRegisteredSubscriptionProvidersResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListRegisteredSubscriptionProvidersResponse.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | RegisterSubscriptionProvider ->
      if is_success
      then
        Ok
          (RegisterSubscriptionProviderResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some RegisterSubscriptionProviderResponse.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceResponse.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceResponse.error_of_json))
  | UpdateServiceSettings ->
      if is_success
      then Ok (UpdateServiceSettingsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some UpdateServiceSettingsResponse.error_of_json))
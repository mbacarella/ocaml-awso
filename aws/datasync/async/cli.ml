(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_task_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskExecutionArn =
         flag "task-execution-arn" (required string)
           ~doc:"STRING TaskExecutionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_task_execution
           (Values.CancelTaskExecutionRequest.make ~taskExecutionArn ())
           (Some Values.CancelTaskExecutionResponse.to_json)
           (Some Values.CancelTaskExecutionResponse.error_to_json)])
let create_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentName =
         flag "agent-name" (optional string) ~doc:"STRING TagValue"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and vpcEndpointId =
         flag "vpc-endpoint-id" (optional string) ~doc:"STRING VpcEndpointId"
       and subnetArns =
         flag "subnet-arns" (optional json_arg) ~doc:"JSON PLSubnetArnList"
       and securityGroupArns =
         flag "security-group-arns" (optional json_arg)
           ~doc:"JSON PLSecurityGroupArnList"
       and activationKey =
         flag "activation-key" (required string) ~doc:"STRING ActivationKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_agent
           (Values.CreateAgentRequest.make ?agentName
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags)
              ?vpcEndpointId
              ?subnetArns:(Option.map ~f:Values.PLSubnetArnList.of_json
                             subnetArns)
              ?securityGroupArns:(Option.map
                                    ~f:Values.PLSecurityGroupArnList.of_json
                                    securityGroupArns) ~activationKey ())
           (Some Values.CreateAgentResponse.to_json)
           (Some Values.CreateAgentResponse.error_to_json)])
let create_location_azure_blob =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sasConfiguration =
         flag "sas-configuration" (optional json_arg)
           ~doc:"JSON AzureBlobSasConfiguration"
       and blobType =
         flag "blob-type" (optional json_arg) ~doc:"JSON AzureBlobType"
       and accessTier =
         flag "access-tier" (optional json_arg) ~doc:"JSON AzureAccessTier"
       and subdirectory =
         flag "subdirectory" (optional string)
           ~doc:"STRING AzureBlobSubdirectory"
       and agentArns =
         flag "agent-arns" (optional json_arg) ~doc:"JSON AgentArnList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and cmkSecretConfig =
         flag "cmk-secret-config" (optional json_arg)
           ~doc:"JSON CmkSecretConfig"
       and customSecretConfig =
         flag "custom-secret-config" (optional json_arg)
           ~doc:"JSON CustomSecretConfig"
       and containerUrl =
         flag "container-url" (required string)
           ~doc:"STRING AzureBlobContainerUrl"
       and authenticationType =
         flag "authentication-type" (required json_arg)
           ~doc:"JSON AzureBlobAuthenticationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_location_azure_blob
           (Values.CreateLocationAzureBlobRequest.make
              ?sasConfiguration:(Option.map
                                   ~f:Values.AzureBlobSasConfiguration.of_json
                                   sasConfiguration)
              ?blobType:(Option.map ~f:Values.AzureBlobType.of_json blobType)
              ?accessTier:(Option.map ~f:Values.AzureAccessTier.of_json
                             accessTier) ?subdirectory
              ?agentArns:(Option.map ~f:Values.AgentArnList.of_json agentArns)
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags)
              ?cmkSecretConfig:(Option.map ~f:Values.CmkSecretConfig.of_json
                                  cmkSecretConfig)
              ?customSecretConfig:(Option.map
                                     ~f:Values.CustomSecretConfig.of_json
                                     customSecretConfig) ~containerUrl
              ~authenticationType:(Values.AzureBlobAuthenticationType.of_json
                                     authenticationType) ())
           (Some Values.CreateLocationAzureBlobResponse.to_json)
           (Some Values.CreateLocationAzureBlobResponse.error_to_json)])
let create_location_efs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING EfsSubdirectory"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and accessPointArn =
         flag "access-point-arn" (optional string)
           ~doc:"STRING EfsAccessPointArn"
       and fileSystemAccessRoleArn =
         flag "file-system-access-role-arn" (optional string)
           ~doc:"STRING IamRoleArn"
       and inTransitEncryption =
         flag "in-transit-encryption" (optional json_arg)
           ~doc:"JSON EfsInTransitEncryption"
       and efsFilesystemArn =
         flag "efs-filesystem-arn" (required string)
           ~doc:"STRING EfsFilesystemArn"
       and ec2Config =
         flag "ec2-config" (required json_arg) ~doc:"JSON Ec2Config" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_location_efs
           (Values.CreateLocationEfsRequest.make ?subdirectory
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags)
              ?accessPointArn ?fileSystemAccessRoleArn
              ?inTransitEncryption:(Option.map
                                      ~f:Values.EfsInTransitEncryption.of_json
                                      inTransitEncryption) ~efsFilesystemArn
              ~ec2Config:(Values.Ec2Config.of_json ec2Config) ())
           (Some Values.CreateLocationEfsResponse.to_json)
           (Some Values.CreateLocationEfsResponse.error_to_json)])
let create_location_fsx_lustre =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string)
           ~doc:"STRING FsxLustreSubdirectory"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and fsxFilesystemArn =
         flag "fsx-filesystem-arn" (required string)
           ~doc:"STRING FsxFilesystemArn"
       and securityGroupArns =
         flag "security-group-arns" (required json_arg)
           ~doc:"JSON Ec2SecurityGroupArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_location_fsx_lustre
           (Values.CreateLocationFsxLustreRequest.make ?subdirectory
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags)
              ~fsxFilesystemArn
              ~securityGroupArns:(Values.Ec2SecurityGroupArnList.of_json
                                    securityGroupArns) ())
           (Some Values.CreateLocationFsxLustreResponse.to_json)
           (Some Values.CreateLocationFsxLustreResponse.error_to_json)])
let create_location_fsx_ontap =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string)
           ~doc:"STRING FsxOntapSubdirectory"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and protocol =
         flag "protocol" (required json_arg) ~doc:"JSON FsxProtocol"
       and securityGroupArns =
         flag "security-group-arns" (required json_arg)
           ~doc:"JSON Ec2SecurityGroupArnList"
       and storageVirtualMachineArn =
         flag "storage-virtual-machine-arn" (required string)
           ~doc:"STRING StorageVirtualMachineArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_location_fsx_ontap
           (Values.CreateLocationFsxOntapRequest.make ?subdirectory
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags)
              ~protocol:(Values.FsxProtocol.of_json protocol)
              ~securityGroupArns:(Values.Ec2SecurityGroupArnList.of_json
                                    securityGroupArns)
              ~storageVirtualMachineArn ())
           (Some Values.CreateLocationFsxOntapResponse.to_json)
           (Some Values.CreateLocationFsxOntapResponse.error_to_json)])
let create_location_fsx_open_zfs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string)
           ~doc:"STRING FsxOpenZfsSubdirectory"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and fsxFilesystemArn =
         flag "fsx-filesystem-arn" (required string)
           ~doc:"STRING FsxFilesystemArn"
       and protocol =
         flag "protocol" (required json_arg) ~doc:"JSON FsxProtocol"
       and securityGroupArns =
         flag "security-group-arns" (required json_arg)
           ~doc:"JSON Ec2SecurityGroupArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_location_fsx_open_zfs
           (Values.CreateLocationFsxOpenZfsRequest.make ?subdirectory
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags)
              ~fsxFilesystemArn
              ~protocol:(Values.FsxProtocol.of_json protocol)
              ~securityGroupArns:(Values.Ec2SecurityGroupArnList.of_json
                                    securityGroupArns) ())
           (Some Values.CreateLocationFsxOpenZfsResponse.to_json)
           (Some Values.CreateLocationFsxOpenZfsResponse.error_to_json)])
let create_location_fsx_windows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string)
           ~doc:"STRING FsxWindowsSubdirectory"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and domain = flag "domain" (optional string) ~doc:"STRING SmbDomain"
       and password =
         flag "password" (optional string) ~doc:"STRING SmbPassword"
       and cmkSecretConfig =
         flag "cmk-secret-config" (optional json_arg)
           ~doc:"JSON CmkSecretConfig"
       and customSecretConfig =
         flag "custom-secret-config" (optional json_arg)
           ~doc:"JSON CustomSecretConfig"
       and fsxFilesystemArn =
         flag "fsx-filesystem-arn" (required string)
           ~doc:"STRING FsxFilesystemArn"
       and securityGroupArns =
         flag "security-group-arns" (required json_arg)
           ~doc:"JSON Ec2SecurityGroupArnList"
       and user = flag "user" (required string) ~doc:"STRING SmbUser" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_location_fsx_windows
           (Values.CreateLocationFsxWindowsRequest.make ?subdirectory
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags) ?domain
              ?password
              ?cmkSecretConfig:(Option.map ~f:Values.CmkSecretConfig.of_json
                                  cmkSecretConfig)
              ?customSecretConfig:(Option.map
                                     ~f:Values.CustomSecretConfig.of_json
                                     customSecretConfig) ~fsxFilesystemArn
              ~securityGroupArns:(Values.Ec2SecurityGroupArnList.of_json
                                    securityGroupArns) ~user ())
           (Some Values.CreateLocationFsxWindowsResponse.to_json)
           (Some Values.CreateLocationFsxWindowsResponse.error_to_json)])
let create_location_hdfs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING HdfsSubdirectory"
       and blockSize =
         flag "block-size" (optional int) ~doc:"INT HdfsBlockSize"
       and replicationFactor =
         flag "replication-factor" (optional int)
           ~doc:"INT HdfsReplicationFactor"
       and kmsKeyProviderUri =
         flag "kms-key-provider-uri" (optional string)
           ~doc:"STRING KmsKeyProviderUri"
       and qopConfiguration =
         flag "qop-configuration" (optional json_arg)
           ~doc:"JSON QopConfiguration"
       and simpleUser =
         flag "simple-user" (optional string) ~doc:"STRING HdfsUser"
       and kerberosPrincipal =
         flag "kerberos-principal" (optional string)
           ~doc:"STRING KerberosPrincipal"
       and kerberosKeytab =
         flag "kerberos-keytab" (optional json_arg)
           ~doc:"JSON KerberosKeytabFile"
       and kerberosKrb5Conf =
         flag "kerberos-krb5-conf" (optional json_arg)
           ~doc:"JSON KerberosKrb5ConfFile"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and cmkSecretConfig =
         flag "cmk-secret-config" (optional json_arg)
           ~doc:"JSON CmkSecretConfig"
       and customSecretConfig =
         flag "custom-secret-config" (optional json_arg)
           ~doc:"JSON CustomSecretConfig"
       and nameNodes =
         flag "name-nodes" (required json_arg) ~doc:"JSON HdfsNameNodeList"
       and authenticationType =
         flag "authentication-type" (required json_arg)
           ~doc:"JSON HdfsAuthenticationType"
       and agentArns =
         flag "agent-arns" (required json_arg) ~doc:"JSON AgentArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_location_hdfs
           (Values.CreateLocationHdfsRequest.make ?subdirectory ?blockSize
              ?replicationFactor ?kmsKeyProviderUri
              ?qopConfiguration:(Option.map
                                   ~f:Values.QopConfiguration.of_json
                                   qopConfiguration) ?simpleUser
              ?kerberosPrincipal
              ?kerberosKeytab:(Option.map
                                 ~f:Values.KerberosKeytabFile.of_json
                                 kerberosKeytab)
              ?kerberosKrb5Conf:(Option.map
                                   ~f:Values.KerberosKrb5ConfFile.of_json
                                   kerberosKrb5Conf)
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags)
              ?cmkSecretConfig:(Option.map ~f:Values.CmkSecretConfig.of_json
                                  cmkSecretConfig)
              ?customSecretConfig:(Option.map
                                     ~f:Values.CustomSecretConfig.of_json
                                     customSecretConfig)
              ~nameNodes:(Values.HdfsNameNodeList.of_json nameNodes)
              ~authenticationType:(Values.HdfsAuthenticationType.of_json
                                     authenticationType)
              ~agentArns:(Values.AgentArnList.of_json agentArns) ())
           (Some Values.CreateLocationHdfsResponse.to_json)
           (Some Values.CreateLocationHdfsResponse.error_to_json)])
let create_location_nfs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mountOptions =
         flag "mount-options" (optional json_arg) ~doc:"JSON NfsMountOptions"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and subdirectory =
         flag "subdirectory" (required string) ~doc:"STRING NfsSubdirectory"
       and serverHostname =
         flag "server-hostname" (required string)
           ~doc:"STRING ServerHostname"
       and onPremConfig =
         flag "on-prem-config" (required json_arg) ~doc:"JSON OnPremConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_location_nfs
           (Values.CreateLocationNfsRequest.make
              ?mountOptions:(Option.map ~f:Values.NfsMountOptions.of_json
                               mountOptions)
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags)
              ~subdirectory ~serverHostname
              ~onPremConfig:(Values.OnPremConfig.of_json onPremConfig) ())
           (Some Values.CreateLocationNfsResponse.to_json)
           (Some Values.CreateLocationNfsResponse.error_to_json)])
let create_location_object_storage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverPort =
         flag "server-port" (optional int) ~doc:"INT ObjectStorageServerPort"
       and serverProtocol =
         flag "server-protocol" (optional json_arg)
           ~doc:"JSON ObjectStorageServerProtocol"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING S3Subdirectory"
       and accessKey =
         flag "access-key" (optional string)
           ~doc:"STRING ObjectStorageAccessKey"
       and secretKey =
         flag "secret-key" (optional string)
           ~doc:"STRING ObjectStorageSecretKey"
       and agentArns =
         flag "agent-arns" (optional json_arg) ~doc:"JSON AgentArnList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and serverCertificate =
         flag "server-certificate" (optional json_arg)
           ~doc:"JSON ObjectStorageCertificate"
       and cmkSecretConfig =
         flag "cmk-secret-config" (optional json_arg)
           ~doc:"JSON CmkSecretConfig"
       and customSecretConfig =
         flag "custom-secret-config" (optional json_arg)
           ~doc:"JSON CustomSecretConfig"
       and serverHostname =
         flag "server-hostname" (required string)
           ~doc:"STRING ServerHostname"
       and bucketName =
         flag "bucket-name" (required string)
           ~doc:"STRING ObjectStorageBucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_location_object_storage
           (Values.CreateLocationObjectStorageRequest.make ?serverPort
              ?serverProtocol:(Option.map
                                 ~f:Values.ObjectStorageServerProtocol.of_json
                                 serverProtocol) ?subdirectory ?accessKey
              ?secretKey
              ?agentArns:(Option.map ~f:Values.AgentArnList.of_json agentArns)
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags)
              ?serverCertificate:(Option.map
                                    ~f:Values.ObjectStorageCertificate.of_json
                                    serverCertificate)
              ?cmkSecretConfig:(Option.map ~f:Values.CmkSecretConfig.of_json
                                  cmkSecretConfig)
              ?customSecretConfig:(Option.map
                                     ~f:Values.CustomSecretConfig.of_json
                                     customSecretConfig) ~serverHostname
              ~bucketName ())
           (Some Values.CreateLocationObjectStorageResponse.to_json)
           (Some Values.CreateLocationObjectStorageResponse.error_to_json)])
let create_location_s3 =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING S3Subdirectory"
       and s3StorageClass =
         flag "s3-storage-class" (optional json_arg)
           ~doc:"JSON S3StorageClass"
       and agentArns =
         flag "agent-arns" (optional json_arg) ~doc:"JSON AgentArnList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and s3BucketArn =
         flag "s3-bucket-arn" (required string) ~doc:"STRING S3BucketArn"
       and s3Config =
         flag "s3-config" (required json_arg) ~doc:"JSON S3Config" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_location_s3
           (Values.CreateLocationS3Request.make ?subdirectory
              ?s3StorageClass:(Option.map ~f:Values.S3StorageClass.of_json
                                 s3StorageClass)
              ?agentArns:(Option.map ~f:Values.AgentArnList.of_json agentArns)
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags)
              ~s3BucketArn ~s3Config:(Values.S3Config.of_json s3Config) ())
           (Some Values.CreateLocationS3Response.to_json)
           (Some Values.CreateLocationS3Response.error_to_json)])
let create_location_smb =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and user = flag "user" (optional string) ~doc:"STRING SmbUser"
       and domain = flag "domain" (optional string) ~doc:"STRING SmbDomain"
       and password =
         flag "password" (optional string) ~doc:"STRING SmbPassword"
       and cmkSecretConfig =
         flag "cmk-secret-config" (optional json_arg)
           ~doc:"JSON CmkSecretConfig"
       and customSecretConfig =
         flag "custom-secret-config" (optional json_arg)
           ~doc:"JSON CustomSecretConfig"
       and mountOptions =
         flag "mount-options" (optional json_arg) ~doc:"JSON SmbMountOptions"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and authenticationType =
         flag "authentication-type" (optional json_arg)
           ~doc:"JSON SmbAuthenticationType"
       and dnsIpAddresses =
         flag "dns-ip-addresses" (optional json_arg) ~doc:"JSON DnsIpList"
       and kerberosPrincipal =
         flag "kerberos-principal" (optional string)
           ~doc:"STRING KerberosPrincipal"
       and kerberosKeytab =
         flag "kerberos-keytab" (optional json_arg)
           ~doc:"JSON KerberosKeytabFile"
       and kerberosKrb5Conf =
         flag "kerberos-krb5-conf" (optional json_arg)
           ~doc:"JSON KerberosKrb5ConfFile"
       and subdirectory =
         flag "subdirectory" (required string) ~doc:"STRING SmbSubdirectory"
       and serverHostname =
         flag "server-hostname" (required string)
           ~doc:"STRING ServerHostname"
       and agentArns =
         flag "agent-arns" (required json_arg) ~doc:"JSON AgentArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_location_smb
           (Values.CreateLocationSmbRequest.make ?user ?domain ?password
              ?cmkSecretConfig:(Option.map ~f:Values.CmkSecretConfig.of_json
                                  cmkSecretConfig)
              ?customSecretConfig:(Option.map
                                     ~f:Values.CustomSecretConfig.of_json
                                     customSecretConfig)
              ?mountOptions:(Option.map ~f:Values.SmbMountOptions.of_json
                               mountOptions)
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags)
              ?authenticationType:(Option.map
                                     ~f:Values.SmbAuthenticationType.of_json
                                     authenticationType)
              ?dnsIpAddresses:(Option.map ~f:Values.DnsIpList.of_json
                                 dnsIpAddresses) ?kerberosPrincipal
              ?kerberosKeytab:(Option.map
                                 ~f:Values.KerberosKeytabFile.of_json
                                 kerberosKeytab)
              ?kerberosKrb5Conf:(Option.map
                                   ~f:Values.KerberosKrb5ConfFile.of_json
                                   kerberosKrb5Conf) ~subdirectory
              ~serverHostname
              ~agentArns:(Values.AgentArnList.of_json agentArns) ())
           (Some Values.CreateLocationSmbResponse.to_json)
           (Some Values.CreateLocationSmbResponse.error_to_json)])
let create_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cloudWatchLogGroupArn =
         flag "cloud-watch-log-group-arn" (optional string)
           ~doc:"STRING LogGroupArn"
       and name = flag "name" (optional string) ~doc:"STRING TagValue"
       and options = flag "options" (optional json_arg) ~doc:"JSON Options"
       and excludes =
         flag "excludes" (optional json_arg) ~doc:"JSON FilterList"
       and schedule =
         flag "schedule" (optional json_arg) ~doc:"JSON TaskSchedule"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and includes =
         flag "includes" (optional json_arg) ~doc:"JSON FilterList"
       and manifestConfig =
         flag "manifest-config" (optional json_arg)
           ~doc:"JSON ManifestConfig"
       and taskReportConfig =
         flag "task-report-config" (optional json_arg)
           ~doc:"JSON TaskReportConfig"
       and taskMode =
         flag "task-mode" (optional json_arg) ~doc:"JSON TaskMode"
       and sourceLocationArn =
         flag "source-location-arn" (required string)
           ~doc:"STRING LocationArn"
       and destinationLocationArn =
         flag "destination-location-arn" (required string)
           ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_task
           (Values.CreateTaskRequest.make ?cloudWatchLogGroupArn ?name
              ?options:(Option.map ~f:Values.Options.of_json options)
              ?excludes:(Option.map ~f:Values.FilterList.of_json excludes)
              ?schedule:(Option.map ~f:Values.TaskSchedule.of_json schedule)
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags)
              ?includes:(Option.map ~f:Values.FilterList.of_json includes)
              ?manifestConfig:(Option.map ~f:Values.ManifestConfig.of_json
                                 manifestConfig)
              ?taskReportConfig:(Option.map
                                   ~f:Values.TaskReportConfig.of_json
                                   taskReportConfig)
              ?taskMode:(Option.map ~f:Values.TaskMode.of_json taskMode)
              ~sourceLocationArn ~destinationLocationArn ())
           (Some Values.CreateTaskResponse.to_json)
           (Some Values.CreateTaskResponse.error_to_json)])
let delete_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentArn =
         flag "agent-arn" (required string) ~doc:"STRING AgentArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_agent (Values.DeleteAgentRequest.make ~agentArn ())
           (Some Values.DeleteAgentResponse.to_json)
           (Some Values.DeleteAgentResponse.error_to_json)])
let delete_location =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_location
           (Values.DeleteLocationRequest.make ~locationArn ())
           (Some Values.DeleteLocationResponse.to_json)
           (Some Values.DeleteLocationResponse.error_to_json)])
let delete_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskArn = flag "task-arn" (required string) ~doc:"STRING TaskArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_task (Values.DeleteTaskRequest.make ~taskArn ())
           (Some Values.DeleteTaskResponse.to_json)
           (Some Values.DeleteTaskResponse.error_to_json)])
let describe_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentArn =
         flag "agent-arn" (required string) ~doc:"STRING AgentArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_agent (Values.DescribeAgentRequest.make ~agentArn ())
           (Some Values.DescribeAgentResponse.to_json)
           (Some Values.DescribeAgentResponse.error_to_json)])
let describe_location_azure_blob =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_location_azure_blob
           (Values.DescribeLocationAzureBlobRequest.make ~locationArn ())
           (Some Values.DescribeLocationAzureBlobResponse.to_json)
           (Some Values.DescribeLocationAzureBlobResponse.error_to_json)])
let describe_location_efs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_location_efs
           (Values.DescribeLocationEfsRequest.make ~locationArn ())
           (Some Values.DescribeLocationEfsResponse.to_json)
           (Some Values.DescribeLocationEfsResponse.error_to_json)])
let describe_location_fsx_lustre =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_location_fsx_lustre
           (Values.DescribeLocationFsxLustreRequest.make ~locationArn ())
           (Some Values.DescribeLocationFsxLustreResponse.to_json)
           (Some Values.DescribeLocationFsxLustreResponse.error_to_json)])
let describe_location_fsx_ontap =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_location_fsx_ontap
           (Values.DescribeLocationFsxOntapRequest.make ~locationArn ())
           (Some Values.DescribeLocationFsxOntapResponse.to_json)
           (Some Values.DescribeLocationFsxOntapResponse.error_to_json)])
let describe_location_fsx_open_zfs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_location_fsx_open_zfs
           (Values.DescribeLocationFsxOpenZfsRequest.make ~locationArn ())
           (Some Values.DescribeLocationFsxOpenZfsResponse.to_json)
           (Some Values.DescribeLocationFsxOpenZfsResponse.error_to_json)])
let describe_location_fsx_windows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_location_fsx_windows
           (Values.DescribeLocationFsxWindowsRequest.make ~locationArn ())
           (Some Values.DescribeLocationFsxWindowsResponse.to_json)
           (Some Values.DescribeLocationFsxWindowsResponse.error_to_json)])
let describe_location_hdfs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_location_hdfs
           (Values.DescribeLocationHdfsRequest.make ~locationArn ())
           (Some Values.DescribeLocationHdfsResponse.to_json)
           (Some Values.DescribeLocationHdfsResponse.error_to_json)])
let describe_location_nfs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_location_nfs
           (Values.DescribeLocationNfsRequest.make ~locationArn ())
           (Some Values.DescribeLocationNfsResponse.to_json)
           (Some Values.DescribeLocationNfsResponse.error_to_json)])
let describe_location_object_storage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_location_object_storage
           (Values.DescribeLocationObjectStorageRequest.make ~locationArn ())
           (Some Values.DescribeLocationObjectStorageResponse.to_json)
           (Some Values.DescribeLocationObjectStorageResponse.error_to_json)])
let describe_location_s3 =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_location_s3
           (Values.DescribeLocationS3Request.make ~locationArn ())
           (Some Values.DescribeLocationS3Response.to_json)
           (Some Values.DescribeLocationS3Response.error_to_json)])
let describe_location_smb =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_location_smb
           (Values.DescribeLocationSmbRequest.make ~locationArn ())
           (Some Values.DescribeLocationSmbResponse.to_json)
           (Some Values.DescribeLocationSmbResponse.error_to_json)])
let describe_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskArn = flag "task-arn" (required string) ~doc:"STRING TaskArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_task (Values.DescribeTaskRequest.make ~taskArn ())
           (Some Values.DescribeTaskResponse.to_json)
           (Some Values.DescribeTaskResponse.error_to_json)])
let describe_task_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskExecutionArn =
         flag "task-execution-arn" (required string)
           ~doc:"STRING TaskExecutionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_task_execution
           (Values.DescribeTaskExecutionRequest.make ~taskExecutionArn ())
           (Some Values.DescribeTaskExecutionResponse.to_json)
           (Some Values.DescribeTaskExecutionResponse.error_to_json)])
let list_agents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_agents
           (Values.ListAgentsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListAgentsResponse.to_json)
           (Some Values.ListAgentsResponse.error_to_json)])
let list_locations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON LocationFilters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_locations
           (Values.ListLocationsRequest.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.LocationFilters.of_json filters)
              ()) (Some Values.ListLocationsResponse.to_json)
           (Some Values.ListLocationsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?maxResults ?nextToken
              ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_task_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskArn = flag "task-arn" (optional string) ~doc:"STRING TaskArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_task_executions
           (Values.ListTaskExecutionsRequest.make ?taskArn ?maxResults
              ?nextToken ()) (Some Values.ListTaskExecutionsResponse.to_json)
           (Some Values.ListTaskExecutionsResponse.error_to_json)])
let list_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON TaskFilters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tasks
           (Values.ListTasksRequest.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.TaskFilters.of_json filters) ())
           (Some Values.ListTasksResponse.to_json)
           (Some Values.ListTasksResponse.error_to_json)])
let start_task_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and overrideOptions =
         flag "override-options" (optional json_arg) ~doc:"JSON Options"
       and includes =
         flag "includes" (optional json_arg) ~doc:"JSON FilterList"
       and excludes =
         flag "excludes" (optional json_arg) ~doc:"JSON FilterList"
       and manifestConfig =
         flag "manifest-config" (optional json_arg)
           ~doc:"JSON ManifestConfig"
       and taskReportConfig =
         flag "task-report-config" (optional json_arg)
           ~doc:"JSON TaskReportConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON InputTagList"
       and taskArn = flag "task-arn" (required string) ~doc:"STRING TaskArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_task_execution
           (Values.StartTaskExecutionRequest.make
              ?overrideOptions:(Option.map ~f:Values.Options.of_json
                                  overrideOptions)
              ?includes:(Option.map ~f:Values.FilterList.of_json includes)
              ?excludes:(Option.map ~f:Values.FilterList.of_json excludes)
              ?manifestConfig:(Option.map ~f:Values.ManifestConfig.of_json
                                 manifestConfig)
              ?taskReportConfig:(Option.map
                                   ~f:Values.TaskReportConfig.of_json
                                   taskReportConfig)
              ?tags:(Option.map ~f:Values.InputTagList.of_json tags) ~taskArn
              ()) (Some Values.StartTaskExecutionResponse.to_json)
           (Some Values.StartTaskExecutionResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON InputTagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.InputTagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn"
       and keys = flag "keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~keys:(Values.TagKeyList.of_json keys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING TagValue"
       and agentArn =
         flag "agent-arn" (required string) ~doc:"STRING AgentArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_agent
           (Values.UpdateAgentRequest.make ?name ~agentArn ())
           (Some Values.UpdateAgentResponse.to_json)
           (Some Values.UpdateAgentResponse.error_to_json)])
let update_location_azure_blob =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string)
           ~doc:"STRING AzureBlobSubdirectory"
       and authenticationType =
         flag "authentication-type" (optional json_arg)
           ~doc:"JSON AzureBlobAuthenticationType"
       and sasConfiguration =
         flag "sas-configuration" (optional json_arg)
           ~doc:"JSON AzureBlobSasConfiguration"
       and blobType =
         flag "blob-type" (optional json_arg) ~doc:"JSON AzureBlobType"
       and accessTier =
         flag "access-tier" (optional json_arg) ~doc:"JSON AzureAccessTier"
       and agentArns =
         flag "agent-arns" (optional json_arg) ~doc:"JSON AgentArnList"
       and cmkSecretConfig =
         flag "cmk-secret-config" (optional json_arg)
           ~doc:"JSON CmkSecretConfig"
       and customSecretConfig =
         flag "custom-secret-config" (optional json_arg)
           ~doc:"JSON CustomSecretConfig"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_location_azure_blob
           (Values.UpdateLocationAzureBlobRequest.make ?subdirectory
              ?authenticationType:(Option.map
                                     ~f:Values.AzureBlobAuthenticationType.of_json
                                     authenticationType)
              ?sasConfiguration:(Option.map
                                   ~f:Values.AzureBlobSasConfiguration.of_json
                                   sasConfiguration)
              ?blobType:(Option.map ~f:Values.AzureBlobType.of_json blobType)
              ?accessTier:(Option.map ~f:Values.AzureAccessTier.of_json
                             accessTier)
              ?agentArns:(Option.map ~f:Values.AgentArnList.of_json agentArns)
              ?cmkSecretConfig:(Option.map ~f:Values.CmkSecretConfig.of_json
                                  cmkSecretConfig)
              ?customSecretConfig:(Option.map
                                     ~f:Values.CustomSecretConfig.of_json
                                     customSecretConfig) ~locationArn ())
           (Some Values.UpdateLocationAzureBlobResponse.to_json)
           (Some Values.UpdateLocationAzureBlobResponse.error_to_json)])
let update_location_efs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING EfsSubdirectory"
       and accessPointArn =
         flag "access-point-arn" (optional string)
           ~doc:"STRING UpdatedEfsAccessPointArn"
       and fileSystemAccessRoleArn =
         flag "file-system-access-role-arn" (optional string)
           ~doc:"STRING UpdatedEfsIamRoleArn"
       and inTransitEncryption =
         flag "in-transit-encryption" (optional json_arg)
           ~doc:"JSON EfsInTransitEncryption"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_location_efs
           (Values.UpdateLocationEfsRequest.make ?subdirectory
              ?accessPointArn ?fileSystemAccessRoleArn
              ?inTransitEncryption:(Option.map
                                      ~f:Values.EfsInTransitEncryption.of_json
                                      inTransitEncryption) ~locationArn ())
           (Some Values.UpdateLocationEfsResponse.to_json)
           (Some Values.UpdateLocationEfsResponse.error_to_json)])
let update_location_fsx_lustre =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING SmbSubdirectory"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_location_fsx_lustre
           (Values.UpdateLocationFsxLustreRequest.make ?subdirectory
              ~locationArn ())
           (Some Values.UpdateLocationFsxLustreResponse.to_json)
           (Some Values.UpdateLocationFsxLustreResponse.error_to_json)])
let update_location_fsx_ontap =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protocol =
         flag "protocol" (optional json_arg) ~doc:"JSON FsxUpdateProtocol"
       and subdirectory =
         flag "subdirectory" (optional string)
           ~doc:"STRING FsxOntapSubdirectory"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_location_fsx_ontap
           (Values.UpdateLocationFsxOntapRequest.make
              ?protocol:(Option.map ~f:Values.FsxUpdateProtocol.of_json
                           protocol) ?subdirectory ~locationArn ())
           (Some Values.UpdateLocationFsxOntapResponse.to_json)
           (Some Values.UpdateLocationFsxOntapResponse.error_to_json)])
let update_location_fsx_open_zfs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protocol =
         flag "protocol" (optional json_arg) ~doc:"JSON FsxProtocol"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING SmbSubdirectory"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_location_fsx_open_zfs
           (Values.UpdateLocationFsxOpenZfsRequest.make
              ?protocol:(Option.map ~f:Values.FsxProtocol.of_json protocol)
              ?subdirectory ~locationArn ())
           (Some Values.UpdateLocationFsxOpenZfsResponse.to_json)
           (Some Values.UpdateLocationFsxOpenZfsResponse.error_to_json)])
let update_location_fsx_windows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string)
           ~doc:"STRING FsxWindowsSubdirectory"
       and domain =
         flag "domain" (optional string) ~doc:"STRING UpdateSmbDomain"
       and user = flag "user" (optional string) ~doc:"STRING SmbUser"
       and password =
         flag "password" (optional string) ~doc:"STRING SmbPassword"
       and cmkSecretConfig =
         flag "cmk-secret-config" (optional json_arg)
           ~doc:"JSON CmkSecretConfig"
       and customSecretConfig =
         flag "custom-secret-config" (optional json_arg)
           ~doc:"JSON CustomSecretConfig"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_location_fsx_windows
           (Values.UpdateLocationFsxWindowsRequest.make ?subdirectory ?domain
              ?user ?password
              ?cmkSecretConfig:(Option.map ~f:Values.CmkSecretConfig.of_json
                                  cmkSecretConfig)
              ?customSecretConfig:(Option.map
                                     ~f:Values.CustomSecretConfig.of_json
                                     customSecretConfig) ~locationArn ())
           (Some Values.UpdateLocationFsxWindowsResponse.to_json)
           (Some Values.UpdateLocationFsxWindowsResponse.error_to_json)])
let update_location_hdfs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING HdfsSubdirectory"
       and nameNodes =
         flag "name-nodes" (optional json_arg) ~doc:"JSON HdfsNameNodeList"
       and blockSize =
         flag "block-size" (optional int) ~doc:"INT HdfsBlockSize"
       and replicationFactor =
         flag "replication-factor" (optional int)
           ~doc:"INT HdfsReplicationFactor"
       and kmsKeyProviderUri =
         flag "kms-key-provider-uri" (optional string)
           ~doc:"STRING KmsKeyProviderUri"
       and qopConfiguration =
         flag "qop-configuration" (optional json_arg)
           ~doc:"JSON QopConfiguration"
       and authenticationType =
         flag "authentication-type" (optional json_arg)
           ~doc:"JSON HdfsAuthenticationType"
       and simpleUser =
         flag "simple-user" (optional string) ~doc:"STRING HdfsUser"
       and kerberosPrincipal =
         flag "kerberos-principal" (optional string)
           ~doc:"STRING KerberosPrincipal"
       and kerberosKeytab =
         flag "kerberos-keytab" (optional json_arg)
           ~doc:"JSON KerberosKeytabFile"
       and kerberosKrb5Conf =
         flag "kerberos-krb5-conf" (optional json_arg)
           ~doc:"JSON KerberosKrb5ConfFile"
       and agentArns =
         flag "agent-arns" (optional json_arg) ~doc:"JSON AgentArnList"
       and cmkSecretConfig =
         flag "cmk-secret-config" (optional json_arg)
           ~doc:"JSON CmkSecretConfig"
       and customSecretConfig =
         flag "custom-secret-config" (optional json_arg)
           ~doc:"JSON CustomSecretConfig"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_location_hdfs
           (Values.UpdateLocationHdfsRequest.make ?subdirectory
              ?nameNodes:(Option.map ~f:Values.HdfsNameNodeList.of_json
                            nameNodes) ?blockSize ?replicationFactor
              ?kmsKeyProviderUri
              ?qopConfiguration:(Option.map
                                   ~f:Values.QopConfiguration.of_json
                                   qopConfiguration)
              ?authenticationType:(Option.map
                                     ~f:Values.HdfsAuthenticationType.of_json
                                     authenticationType) ?simpleUser
              ?kerberosPrincipal
              ?kerberosKeytab:(Option.map
                                 ~f:Values.KerberosKeytabFile.of_json
                                 kerberosKeytab)
              ?kerberosKrb5Conf:(Option.map
                                   ~f:Values.KerberosKrb5ConfFile.of_json
                                   kerberosKrb5Conf)
              ?agentArns:(Option.map ~f:Values.AgentArnList.of_json agentArns)
              ?cmkSecretConfig:(Option.map ~f:Values.CmkSecretConfig.of_json
                                  cmkSecretConfig)
              ?customSecretConfig:(Option.map
                                     ~f:Values.CustomSecretConfig.of_json
                                     customSecretConfig) ~locationArn ())
           (Some Values.UpdateLocationHdfsResponse.to_json)
           (Some Values.UpdateLocationHdfsResponse.error_to_json)])
let update_location_nfs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING NfsSubdirectory"
       and serverHostname =
         flag "server-hostname" (optional string)
           ~doc:"STRING ServerHostname"
       and onPremConfig =
         flag "on-prem-config" (optional json_arg) ~doc:"JSON OnPremConfig"
       and mountOptions =
         flag "mount-options" (optional json_arg) ~doc:"JSON NfsMountOptions"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_location_nfs
           (Values.UpdateLocationNfsRequest.make ?subdirectory
              ?serverHostname
              ?onPremConfig:(Option.map ~f:Values.OnPremConfig.of_json
                               onPremConfig)
              ?mountOptions:(Option.map ~f:Values.NfsMountOptions.of_json
                               mountOptions) ~locationArn ())
           (Some Values.UpdateLocationNfsResponse.to_json)
           (Some Values.UpdateLocationNfsResponse.error_to_json)])
let update_location_object_storage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverPort =
         flag "server-port" (optional int) ~doc:"INT ObjectStorageServerPort"
       and serverProtocol =
         flag "server-protocol" (optional json_arg)
           ~doc:"JSON ObjectStorageServerProtocol"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING S3Subdirectory"
       and serverHostname =
         flag "server-hostname" (optional string)
           ~doc:"STRING ServerHostname"
       and accessKey =
         flag "access-key" (optional string)
           ~doc:"STRING ObjectStorageAccessKey"
       and secretKey =
         flag "secret-key" (optional string)
           ~doc:"STRING ObjectStorageSecretKey"
       and agentArns =
         flag "agent-arns" (optional json_arg) ~doc:"JSON AgentArnList"
       and serverCertificate =
         flag "server-certificate" (optional json_arg)
           ~doc:"JSON ObjectStorageCertificate"
       and cmkSecretConfig =
         flag "cmk-secret-config" (optional json_arg)
           ~doc:"JSON CmkSecretConfig"
       and customSecretConfig =
         flag "custom-secret-config" (optional json_arg)
           ~doc:"JSON CustomSecretConfig"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_location_object_storage
           (Values.UpdateLocationObjectStorageRequest.make ?serverPort
              ?serverProtocol:(Option.map
                                 ~f:Values.ObjectStorageServerProtocol.of_json
                                 serverProtocol) ?subdirectory
              ?serverHostname ?accessKey ?secretKey
              ?agentArns:(Option.map ~f:Values.AgentArnList.of_json agentArns)
              ?serverCertificate:(Option.map
                                    ~f:Values.ObjectStorageCertificate.of_json
                                    serverCertificate)
              ?cmkSecretConfig:(Option.map ~f:Values.CmkSecretConfig.of_json
                                  cmkSecretConfig)
              ?customSecretConfig:(Option.map
                                     ~f:Values.CustomSecretConfig.of_json
                                     customSecretConfig) ~locationArn ())
           (Some Values.UpdateLocationObjectStorageResponse.to_json)
           (Some Values.UpdateLocationObjectStorageResponse.error_to_json)])
let update_location_s3 =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING S3Subdirectory"
       and s3StorageClass =
         flag "s3-storage-class" (optional json_arg)
           ~doc:"JSON S3StorageClass"
       and s3Config =
         flag "s3-config" (optional json_arg) ~doc:"JSON S3Config"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_location_s3
           (Values.UpdateLocationS3Request.make ?subdirectory
              ?s3StorageClass:(Option.map ~f:Values.S3StorageClass.of_json
                                 s3StorageClass)
              ?s3Config:(Option.map ~f:Values.S3Config.of_json s3Config)
              ~locationArn ()) (Some Values.UpdateLocationS3Response.to_json)
           (Some Values.UpdateLocationS3Response.error_to_json)])
let update_location_smb =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subdirectory =
         flag "subdirectory" (optional string) ~doc:"STRING SmbSubdirectory"
       and serverHostname =
         flag "server-hostname" (optional string)
           ~doc:"STRING ServerHostname"
       and user = flag "user" (optional string) ~doc:"STRING SmbUser"
       and domain = flag "domain" (optional string) ~doc:"STRING SmbDomain"
       and password =
         flag "password" (optional string) ~doc:"STRING SmbPassword"
       and cmkSecretConfig =
         flag "cmk-secret-config" (optional json_arg)
           ~doc:"JSON CmkSecretConfig"
       and customSecretConfig =
         flag "custom-secret-config" (optional json_arg)
           ~doc:"JSON CustomSecretConfig"
       and agentArns =
         flag "agent-arns" (optional json_arg) ~doc:"JSON AgentArnList"
       and mountOptions =
         flag "mount-options" (optional json_arg) ~doc:"JSON SmbMountOptions"
       and authenticationType =
         flag "authentication-type" (optional json_arg)
           ~doc:"JSON SmbAuthenticationType"
       and dnsIpAddresses =
         flag "dns-ip-addresses" (optional json_arg) ~doc:"JSON DnsIpList"
       and kerberosPrincipal =
         flag "kerberos-principal" (optional string)
           ~doc:"STRING KerberosPrincipal"
       and kerberosKeytab =
         flag "kerberos-keytab" (optional json_arg)
           ~doc:"JSON KerberosKeytabFile"
       and kerberosKrb5Conf =
         flag "kerberos-krb5-conf" (optional json_arg)
           ~doc:"JSON KerberosKrb5ConfFile"
       and locationArn =
         flag "location-arn" (required string) ~doc:"STRING LocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_location_smb
           (Values.UpdateLocationSmbRequest.make ?subdirectory
              ?serverHostname ?user ?domain ?password
              ?cmkSecretConfig:(Option.map ~f:Values.CmkSecretConfig.of_json
                                  cmkSecretConfig)
              ?customSecretConfig:(Option.map
                                     ~f:Values.CustomSecretConfig.of_json
                                     customSecretConfig)
              ?agentArns:(Option.map ~f:Values.AgentArnList.of_json agentArns)
              ?mountOptions:(Option.map ~f:Values.SmbMountOptions.of_json
                               mountOptions)
              ?authenticationType:(Option.map
                                     ~f:Values.SmbAuthenticationType.of_json
                                     authenticationType)
              ?dnsIpAddresses:(Option.map ~f:Values.DnsIpList.of_json
                                 dnsIpAddresses) ?kerberosPrincipal
              ?kerberosKeytab:(Option.map
                                 ~f:Values.KerberosKeytabFile.of_json
                                 kerberosKeytab)
              ?kerberosKrb5Conf:(Option.map
                                   ~f:Values.KerberosKrb5ConfFile.of_json
                                   kerberosKrb5Conf) ~locationArn ())
           (Some Values.UpdateLocationSmbResponse.to_json)
           (Some Values.UpdateLocationSmbResponse.error_to_json)])
let update_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and options = flag "options" (optional json_arg) ~doc:"JSON Options"
       and excludes =
         flag "excludes" (optional json_arg) ~doc:"JSON FilterList"
       and schedule =
         flag "schedule" (optional json_arg) ~doc:"JSON TaskSchedule"
       and name = flag "name" (optional string) ~doc:"STRING TagValue"
       and cloudWatchLogGroupArn =
         flag "cloud-watch-log-group-arn" (optional string)
           ~doc:"STRING LogGroupArn"
       and includes =
         flag "includes" (optional json_arg) ~doc:"JSON FilterList"
       and manifestConfig =
         flag "manifest-config" (optional json_arg)
           ~doc:"JSON ManifestConfig"
       and taskReportConfig =
         flag "task-report-config" (optional json_arg)
           ~doc:"JSON TaskReportConfig"
       and taskArn = flag "task-arn" (required string) ~doc:"STRING TaskArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_task
           (Values.UpdateTaskRequest.make
              ?options:(Option.map ~f:Values.Options.of_json options)
              ?excludes:(Option.map ~f:Values.FilterList.of_json excludes)
              ?schedule:(Option.map ~f:Values.TaskSchedule.of_json schedule)
              ?name ?cloudWatchLogGroupArn
              ?includes:(Option.map ~f:Values.FilterList.of_json includes)
              ?manifestConfig:(Option.map ~f:Values.ManifestConfig.of_json
                                 manifestConfig)
              ?taskReportConfig:(Option.map
                                   ~f:Values.TaskReportConfig.of_json
                                   taskReportConfig) ~taskArn ())
           (Some Values.UpdateTaskResponse.to_json)
           (Some Values.UpdateTaskResponse.error_to_json)])
let update_task_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskExecutionArn =
         flag "task-execution-arn" (required string)
           ~doc:"STRING TaskExecutionArn"
       and options = flag "options" (required json_arg) ~doc:"JSON Options" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_task_execution
           (Values.UpdateTaskExecutionRequest.make ~taskExecutionArn
              ~options:(Values.Options.of_json options) ())
           (Some Values.UpdateTaskExecutionResponse.to_json)
           (Some Values.UpdateTaskExecutionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("cancel-task-execution", cancel_task_execution);
    ("create-agent", create_agent);
    ("create-location-azure-blob", create_location_azure_blob);
    ("create-location-efs", create_location_efs);
    ("create-location-fsx-lustre", create_location_fsx_lustre);
    ("create-location-fsx-ontap", create_location_fsx_ontap);
    ("create-location-fsx-open-zfs", create_location_fsx_open_zfs);
    ("create-location-fsx-windows", create_location_fsx_windows);
    ("create-location-hdfs", create_location_hdfs);
    ("create-location-nfs", create_location_nfs);
    ("create-location-object-storage", create_location_object_storage);
    ("create-location-s3", create_location_s3);
    ("create-location-smb", create_location_smb);
    ("create-task", create_task);
    ("delete-agent", delete_agent);
    ("delete-location", delete_location);
    ("delete-task", delete_task);
    ("describe-agent", describe_agent);
    ("describe-location-azure-blob", describe_location_azure_blob);
    ("describe-location-efs", describe_location_efs);
    ("describe-location-fsx-lustre", describe_location_fsx_lustre);
    ("describe-location-fsx-ontap", describe_location_fsx_ontap);
    ("describe-location-fsx-open-zfs", describe_location_fsx_open_zfs);
    ("describe-location-fsx-windows", describe_location_fsx_windows);
    ("describe-location-hdfs", describe_location_hdfs);
    ("describe-location-nfs", describe_location_nfs);
    ("describe-location-object-storage", describe_location_object_storage);
    ("describe-location-s3", describe_location_s3);
    ("describe-location-smb", describe_location_smb);
    ("describe-task", describe_task);
    ("describe-task-execution", describe_task_execution);
    ("list-agents", list_agents);
    ("list-locations", list_locations);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-task-executions", list_task_executions);
    ("list-tasks", list_tasks);
    ("start-task-execution", start_task_execution);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-agent", update_agent);
    ("update-location-azure-blob", update_location_azure_blob);
    ("update-location-efs", update_location_efs);
    ("update-location-fsx-lustre", update_location_fsx_lustre);
    ("update-location-fsx-ontap", update_location_fsx_ontap);
    ("update-location-fsx-open-zfs", update_location_fsx_open_zfs);
    ("update-location-fsx-windows", update_location_fsx_windows);
    ("update-location-hdfs", update_location_hdfs);
    ("update-location-nfs", update_location_nfs);
    ("update-location-object-storage", update_location_object_storage);
    ("update-location-s3", update_location_s3);
    ("update-location-smb", update_location_smb);
    ("update-task", update_task);
    ("update-task-execution", update_task_execution)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let configure_logs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and egressAccessLogs =
         flag "egress-access-logs" (optional json_arg)
           ~doc:"JSON EgressAccessLogs"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.configure_logs
           (Values.ConfigureLogsRequest.make
              ?egressAccessLogs:(Option.map
                                   ~f:Values.EgressAccessLogs.of_json
                                   egressAccessLogs) ~id ())
           (Some Values.ConfigureLogsResponse.to_json)
           (Some Values.ConfigureLogsResponse.error_to_json)])
let create_asset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and id = flag "id" (required string) ~doc:"STRING __string"
       and packagingGroupId =
         flag "packaging-group-id" (required string) ~doc:"STRING __string"
       and sourceArn =
         flag "source-arn" (required string) ~doc:"STRING __string"
       and sourceRoleArn =
         flag "source-role-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_asset
           (Values.CreateAssetRequest.make ?resourceId
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~id
              ~packagingGroupId ~sourceArn ~sourceRoleArn ())
           (Some Values.CreateAssetResponse.to_json)
           (Some Values.CreateAssetResponse.error_to_json)])
let create_packaging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cmafPackage =
         flag "cmaf-package" (optional json_arg) ~doc:"JSON CmafPackage"
       and dashPackage =
         flag "dash-package" (optional json_arg) ~doc:"JSON DashPackage"
       and hlsPackage =
         flag "hls-package" (optional json_arg) ~doc:"JSON HlsPackage"
       and mssPackage =
         flag "mss-package" (optional json_arg) ~doc:"JSON MssPackage"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and id = flag "id" (required string) ~doc:"STRING __string"
       and packagingGroupId =
         flag "packaging-group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_packaging_configuration
           (Values.CreatePackagingConfigurationRequest.make
              ?cmafPackage:(Option.map ~f:Values.CmafPackage.of_json
                              cmafPackage)
              ?dashPackage:(Option.map ~f:Values.DashPackage.of_json
                              dashPackage)
              ?hlsPackage:(Option.map ~f:Values.HlsPackage.of_json hlsPackage)
              ?mssPackage:(Option.map ~f:Values.MssPackage.of_json mssPackage)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~id
              ~packagingGroupId ())
           (Some Values.CreatePackagingConfigurationResponse.to_json)
           (Some Values.CreatePackagingConfigurationResponse.error_to_json)])
let create_packaging_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorization =
         flag "authorization" (optional json_arg) ~doc:"JSON Authorization"
       and egressAccessLogs =
         flag "egress-access-logs" (optional json_arg)
           ~doc:"JSON EgressAccessLogs"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_packaging_group
           (Values.CreatePackagingGroupRequest.make
              ?authorization:(Option.map ~f:Values.Authorization.of_json
                                authorization)
              ?egressAccessLogs:(Option.map
                                   ~f:Values.EgressAccessLogs.of_json
                                   egressAccessLogs)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~id ())
           (Some Values.CreatePackagingGroupResponse.to_json)
           (Some Values.CreatePackagingGroupResponse.error_to_json)])
let delete_asset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_asset (Values.DeleteAssetRequest.make ~id ())
           (Some Values.DeleteAssetResponse.to_json)
           (Some Values.DeleteAssetResponse.error_to_json)])
let delete_packaging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_packaging_configuration
           (Values.DeletePackagingConfigurationRequest.make ~id ())
           (Some Values.DeletePackagingConfigurationResponse.to_json)
           (Some Values.DeletePackagingConfigurationResponse.error_to_json)])
let delete_packaging_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_packaging_group
           (Values.DeletePackagingGroupRequest.make ~id ())
           (Some Values.DeletePackagingGroupResponse.to_json)
           (Some Values.DeletePackagingGroupResponse.error_to_json)])
let describe_asset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_asset (Values.DescribeAssetRequest.make ~id ())
           (Some Values.DescribeAssetResponse.to_json)
           (Some Values.DescribeAssetResponse.error_to_json)])
let describe_packaging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_packaging_configuration
           (Values.DescribePackagingConfigurationRequest.make ~id ())
           (Some Values.DescribePackagingConfigurationResponse.to_json)
           (Some Values.DescribePackagingConfigurationResponse.error_to_json)])
let describe_packaging_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_packaging_group
           (Values.DescribePackagingGroupRequest.make ~id ())
           (Some Values.DescribePackagingGroupResponse.to_json)
           (Some Values.DescribePackagingGroupResponse.error_to_json)])
let list_assets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and packagingGroupId =
         flag "packaging-group-id" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_assets
           (Values.ListAssetsRequest.make ?maxResults ?nextToken
              ?packagingGroupId ()) (Some Values.ListAssetsResponse.to_json)
           (Some Values.ListAssetsResponse.error_to_json)])
let list_packaging_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and packagingGroupId =
         flag "packaging-group-id" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_packaging_configurations
           (Values.ListPackagingConfigurationsRequest.make ?maxResults
              ?nextToken ?packagingGroupId ())
           (Some Values.ListPackagingConfigurationsResponse.to_json)
           (Some Values.ListPackagingConfigurationsResponse.error_to_json)])
let list_packaging_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_packaging_groups
           (Values.ListPackagingGroupsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListPackagingGroupsResponse.to_json)
           (Some Values.ListPackagingGroupsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tags = flag "tags" (required json_arg) ~doc:"JSON __mapOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Zz__mapOf__string.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.Zz__listOf__string.of_json tagKeys) ()) None
           None])
let update_packaging_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorization =
         flag "authorization" (optional json_arg) ~doc:"JSON Authorization"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_packaging_group
           (Values.UpdatePackagingGroupRequest.make
              ?authorization:(Option.map ~f:Values.Authorization.of_json
                                authorization) ~id ())
           (Some Values.UpdatePackagingGroupResponse.to_json)
           (Some Values.UpdatePackagingGroupResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("configure-logs", configure_logs);
    ("create-asset", create_asset);
    ("create-packaging-configuration", create_packaging_configuration);
    ("create-packaging-group", create_packaging_group);
    ("delete-asset", delete_asset);
    ("delete-packaging-configuration", delete_packaging_configuration);
    ("delete-packaging-group", delete_packaging_group);
    ("describe-asset", describe_asset);
    ("describe-packaging-configuration", describe_packaging_configuration);
    ("describe-packaging-group", describe_packaging_group);
    ("list-assets", list_assets);
    ("list-packaging-configurations", list_packaging_configurations);
    ("list-packaging-groups", list_packaging_groups);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-packaging-group", update_packaging_group)]
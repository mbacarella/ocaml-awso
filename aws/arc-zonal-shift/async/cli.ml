(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_practice_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and zonalShiftId =
         flag "zonal-shift-id" (required string) ~doc:"STRING ZonalShiftId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_practice_run
           (Values.CancelPracticeRunRequest.make ~zonalShiftId ())
           (Some Values.CancelPracticeRunResponse.to_json)
           (Some Values.CancelPracticeRunResponse.error_to_json)])
let cancel_zonal_shift =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and zonalShiftId =
         flag "zonal-shift-id" (required string) ~doc:"STRING ZonalShiftId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_zonal_shift
           (Values.CancelZonalShiftRequest.make ~zonalShiftId ())
           (Some Values.ZonalShift.to_json)
           (Some Values.ZonalShift.error_to_json)])
let create_practice_run_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and blockedWindows =
         flag "blocked-windows" (optional json_arg)
           ~doc:"JSON BlockedWindows"
       and blockedDates =
         flag "blocked-dates" (optional json_arg) ~doc:"JSON BlockedDates"
       and blockingAlarms =
         flag "blocking-alarms" (optional json_arg)
           ~doc:"JSON BlockingAlarms"
       and allowedWindows =
         flag "allowed-windows" (optional json_arg)
           ~doc:"JSON AllowedWindows"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier"
       and outcomeAlarms =
         flag "outcome-alarms" (required json_arg) ~doc:"JSON OutcomeAlarms" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_practice_run_configuration
           (Values.CreatePracticeRunConfigurationRequest.make
              ?blockedWindows:(Option.map ~f:Values.BlockedWindows.of_json
                                 blockedWindows)
              ?blockedDates:(Option.map ~f:Values.BlockedDates.of_json
                               blockedDates)
              ?blockingAlarms:(Option.map ~f:Values.BlockingAlarms.of_json
                                 blockingAlarms)
              ?allowedWindows:(Option.map ~f:Values.AllowedWindows.of_json
                                 allowedWindows) ~resourceIdentifier
              ~outcomeAlarms:(Values.OutcomeAlarms.of_json outcomeAlarms) ())
           (Some Values.CreatePracticeRunConfigurationResponse.to_json)
           (Some Values.CreatePracticeRunConfigurationResponse.error_to_json)])
let delete_practice_run_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_practice_run_configuration
           (Values.DeletePracticeRunConfigurationRequest.make
              ~resourceIdentifier ())
           (Some Values.DeletePracticeRunConfigurationResponse.to_json)
           (Some Values.DeletePracticeRunConfigurationResponse.error_to_json)])
let get_autoshift_observer_notification_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_autoshift_observer_notification_status
           (Values.GetAutoshiftObserverNotificationStatusRequest.make ())
           (Some
              Values.GetAutoshiftObserverNotificationStatusResponse.to_json)
           (Some
              Values.GetAutoshiftObserverNotificationStatusResponse.error_to_json)])
let get_managed_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_managed_resource
           (Values.GetManagedResourceRequest.make ~resourceIdentifier ())
           (Some Values.GetManagedResourceResponse.to_json)
           (Some Values.GetManagedResourceResponse.error_to_json)])
let list_autoshifts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON AutoshiftExecutionStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_autoshifts
           (Values.ListAutoshiftsRequest.make ?nextToken
              ?status:(Option.map ~f:Values.AutoshiftExecutionStatus.of_json
                         status) ?maxResults ())
           (Some Values.ListAutoshiftsResponse.to_json)
           (Some Values.ListAutoshiftsResponse.error_to_json)])
let list_managed_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_resources
           (Values.ListManagedResourcesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListManagedResourcesResponse.to_json)
           (Some Values.ListManagedResourcesResponse.error_to_json)])
let list_zonal_shifts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON ZonalShiftStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceIdentifier =
         flag "resource-identifier" (optional string)
           ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_zonal_shifts
           (Values.ListZonalShiftsRequest.make ?nextToken
              ?status:(Option.map ~f:Values.ZonalShiftStatus.of_json status)
              ?maxResults ?resourceIdentifier ())
           (Some Values.ListZonalShiftsResponse.to_json)
           (Some Values.ListZonalShiftsResponse.error_to_json)])
let start_practice_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier"
       and awayFrom =
         flag "away-from" (required string) ~doc:"STRING AvailabilityZone"
       and comment =
         flag "comment" (required string) ~doc:"STRING ZonalShiftComment" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_practice_run
           (Values.StartPracticeRunRequest.make ~resourceIdentifier ~awayFrom
              ~comment ()) (Some Values.StartPracticeRunResponse.to_json)
           (Some Values.StartPracticeRunResponse.error_to_json)])
let start_zonal_shift =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier"
       and awayFrom =
         flag "away-from" (required string) ~doc:"STRING AvailabilityZone"
       and expiresIn =
         flag "expires-in" (required string) ~doc:"STRING ExpiresIn"
       and comment =
         flag "comment" (required string) ~doc:"STRING ZonalShiftComment" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_zonal_shift
           (Values.StartZonalShiftRequest.make ~resourceIdentifier ~awayFrom
              ~expiresIn ~comment ()) (Some Values.ZonalShift.to_json)
           (Some Values.ZonalShift.error_to_json)])
let update_autoshift_observer_notification_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (required json_arg)
           ~doc:"JSON AutoshiftObserverNotificationStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_autoshift_observer_notification_status
           (Values.UpdateAutoshiftObserverNotificationStatusRequest.make
              ~status:(Values.AutoshiftObserverNotificationStatus.of_json
                         status) ())
           (Some
              Values.UpdateAutoshiftObserverNotificationStatusResponse.to_json)
           (Some
              Values.UpdateAutoshiftObserverNotificationStatusResponse.error_to_json)])
let update_practice_run_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and blockedWindows =
         flag "blocked-windows" (optional json_arg)
           ~doc:"JSON BlockedWindows"
       and blockedDates =
         flag "blocked-dates" (optional json_arg) ~doc:"JSON BlockedDates"
       and blockingAlarms =
         flag "blocking-alarms" (optional json_arg)
           ~doc:"JSON BlockingAlarms"
       and allowedWindows =
         flag "allowed-windows" (optional json_arg)
           ~doc:"JSON AllowedWindows"
       and outcomeAlarms =
         flag "outcome-alarms" (optional json_arg) ~doc:"JSON OutcomeAlarms"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_practice_run_configuration
           (Values.UpdatePracticeRunConfigurationRequest.make
              ?blockedWindows:(Option.map ~f:Values.BlockedWindows.of_json
                                 blockedWindows)
              ?blockedDates:(Option.map ~f:Values.BlockedDates.of_json
                               blockedDates)
              ?blockingAlarms:(Option.map ~f:Values.BlockingAlarms.of_json
                                 blockingAlarms)
              ?allowedWindows:(Option.map ~f:Values.AllowedWindows.of_json
                                 allowedWindows)
              ?outcomeAlarms:(Option.map ~f:Values.OutcomeAlarms.of_json
                                outcomeAlarms) ~resourceIdentifier ())
           (Some Values.UpdatePracticeRunConfigurationResponse.to_json)
           (Some Values.UpdatePracticeRunConfigurationResponse.error_to_json)])
let update_zonal_autoshift_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier"
       and zonalAutoshiftStatus =
         flag "zonal-autoshift-status" (required json_arg)
           ~doc:"JSON ZonalAutoshiftStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_zonal_autoshift_configuration
           (Values.UpdateZonalAutoshiftConfigurationRequest.make
              ~resourceIdentifier
              ~zonalAutoshiftStatus:(Values.ZonalAutoshiftStatus.of_json
                                       zonalAutoshiftStatus) ())
           (Some Values.UpdateZonalAutoshiftConfigurationResponse.to_json)
           (Some
              Values.UpdateZonalAutoshiftConfigurationResponse.error_to_json)])
let update_zonal_shift =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment =
         flag "comment" (optional string) ~doc:"STRING ZonalShiftComment"
       and expiresIn =
         flag "expires-in" (optional string) ~doc:"STRING ExpiresIn"
       and zonalShiftId =
         flag "zonal-shift-id" (required string) ~doc:"STRING ZonalShiftId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_zonal_shift
           (Values.UpdateZonalShiftRequest.make ?comment ?expiresIn
              ~zonalShiftId ()) (Some Values.ZonalShift.to_json)
           (Some Values.ZonalShift.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("cancel-practice-run", cancel_practice_run);
    ("cancel-zonal-shift", cancel_zonal_shift);
    ("create-practice-run-configuration", create_practice_run_configuration);
    ("delete-practice-run-configuration", delete_practice_run_configuration);
    ("get-autoshift-observer-notification-status",
      get_autoshift_observer_notification_status);
    ("get-managed-resource", get_managed_resource);
    ("list-autoshifts", list_autoshifts);
    ("list-managed-resources", list_managed_resources);
    ("list-zonal-shifts", list_zonal_shifts);
    ("start-practice-run", start_practice_run);
    ("start-zonal-shift", start_zonal_shift);
    ("update-autoshift-observer-notification-status",
      update_autoshift_observer_notification_status);
    ("update-practice-run-configuration", update_practice_run_configuration);
    ("update-zonal-autoshift-configuration",
      update_zonal_autoshift_configuration);
    ("update-zonal-shift", update_zonal_shift)]
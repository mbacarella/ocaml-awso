(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_key_replication_regions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and replicationRegions =
         flag "replication-regions" (required json_arg) ~doc:"JSON Regions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_key_replication_regions
           (Values.AddKeyReplicationRegionsInput.make ~keyIdentifier
              ~replicationRegions:(Values.Regions.of_json replicationRegions)
              ()) (Some Values.AddKeyReplicationRegionsOutput.to_json)
           (Some Values.AddKeyReplicationRegionsOutput.error_to_json)])
let associate_mpa_team =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requesterComment =
         flag "requester-comment" (optional string)
           ~doc:"STRING MpaRequesterComment"
       and action =
         flag "action" (required json_arg) ~doc:"JSON MpaOperation"
       and mpaTeamArn =
         flag "mpa-team-arn" (required string) ~doc:"STRING MpaTeamArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_mpa_team
           (Values.AssociateMpaTeamInput.make ?requesterComment
              ~action:(Values.MpaOperation.of_json action) ~mpaTeamArn ())
           (Some Values.AssociateMpaTeamOutput.to_json)
           (Some Values.AssociateMpaTeamOutput.error_to_json)])
let create_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyArn = flag "key-arn" (optional string) ~doc:"STRING KeyArn"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_alias
           (Values.CreateAliasInput.make ?keyArn ~aliasName ())
           (Some Values.CreateAliasOutput.to_json)
           (Some Values.CreateAliasOutput.error_to_json)])
let create_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyCheckValueAlgorithm =
         flag "key-check-value-algorithm" (optional json_arg)
           ~doc:"JSON KeyCheckValueAlgorithm"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and deriveKeyUsage =
         flag "derive-key-usage" (optional json_arg)
           ~doc:"JSON DeriveKeyUsage"
       and replicationRegions =
         flag "replication-regions" (optional json_arg) ~doc:"JSON Regions"
       and keyAttributes =
         flag "key-attributes" (required json_arg) ~doc:"JSON KeyAttributes"
       and exportable = flag "exportable" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_key
           (Values.CreateKeyInput.make
              ?keyCheckValueAlgorithm:(Option.map
                                         ~f:Values.KeyCheckValueAlgorithm.of_json
                                         keyCheckValueAlgorithm) ?enabled
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?deriveKeyUsage:(Option.map ~f:Values.DeriveKeyUsage.of_json
                                 deriveKeyUsage)
              ?replicationRegions:(Option.map ~f:Values.Regions.of_json
                                     replicationRegions)
              ~keyAttributes:(Values.KeyAttributes.of_json keyAttributes)
              ~exportable ()) (Some Values.CreateKeyOutput.to_json)
           (Some Values.CreateKeyOutput.error_to_json)])
let delete_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_alias (Values.DeleteAliasInput.make ~aliasName ())
           (Some Values.DeleteAliasOutput.to_json)
           (Some Values.DeleteAliasOutput.error_to_json)])
let delete_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deleteKeyInDays =
         flag "delete-key-in-days" (optional int)
           ~doc:"INT DeleteKeyInputDeleteKeyInDaysInteger"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_key
           (Values.DeleteKeyInput.make ?deleteKeyInDays ~keyIdentifier ())
           (Some Values.DeleteKeyOutput.to_json)
           (Some Values.DeleteKeyOutput.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyInput.make ~resourceArn ())
           (Some Values.DeleteResourcePolicyOutput.to_json)
           (Some Values.DeleteResourcePolicyOutput.error_to_json)])
let disable_default_key_replication_regions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationRegions =
         flag "replication-regions" (required json_arg) ~doc:"JSON Regions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_default_key_replication_regions
           (Values.DisableDefaultKeyReplicationRegionsInput.make
              ~replicationRegions:(Values.Regions.of_json replicationRegions)
              ())
           (Some Values.DisableDefaultKeyReplicationRegionsOutput.to_json)
           (Some
              Values.DisableDefaultKeyReplicationRegionsOutput.error_to_json)])
let disassociate_mpa_team =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requesterComment =
         flag "requester-comment" (optional string)
           ~doc:"STRING MpaRequesterComment"
       and action =
         flag "action" (required json_arg) ~doc:"JSON MpaOperation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_mpa_team
           (Values.DisassociateMpaTeamInput.make ?requesterComment
              ~action:(Values.MpaOperation.of_json action) ())
           (Some Values.DisassociateMpaTeamOutput.to_json)
           (Some Values.DisassociateMpaTeamOutput.error_to_json)])
let enable_default_key_replication_regions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationRegions =
         flag "replication-regions" (required json_arg) ~doc:"JSON Regions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_default_key_replication_regions
           (Values.EnableDefaultKeyReplicationRegionsInput.make
              ~replicationRegions:(Values.Regions.of_json replicationRegions)
              ())
           (Some Values.EnableDefaultKeyReplicationRegionsOutput.to_json)
           (Some
              Values.EnableDefaultKeyReplicationRegionsOutput.error_to_json)])
let export_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportAttributes =
         flag "export-attributes" (optional json_arg)
           ~doc:"JSON ExportAttributes"
       and keyMaterial =
         flag "key-material" (required json_arg)
           ~doc:"JSON ExportKeyMaterial"
       and exportKeyIdentifier =
         flag "export-key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_key
           (Values.ExportKeyInput.make
              ?exportAttributes:(Option.map
                                   ~f:Values.ExportAttributes.of_json
                                   exportAttributes)
              ~keyMaterial:(Values.ExportKeyMaterial.of_json keyMaterial)
              ~exportKeyIdentifier ()) (Some Values.ExportKeyOutput.to_json)
           (Some Values.ExportKeyOutput.error_to_json)])
let get_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_alias (Values.GetAliasInput.make ~aliasName ())
           (Some Values.GetAliasOutput.to_json)
           (Some Values.GetAliasOutput.error_to_json)])
let get_certificate_signing_request =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and signingAlgorithm =
         flag "signing-algorithm" (required json_arg)
           ~doc:"JSON SigningAlgorithmType"
       and certificateSubject =
         flag "certificate-subject" (required json_arg)
           ~doc:"JSON CertificateSubjectType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_certificate_signing_request
           (Values.GetCertificateSigningRequestInput.make ~keyIdentifier
              ~signingAlgorithm:(Values.SigningAlgorithmType.of_json
                                   signingAlgorithm)
              ~certificateSubject:(Values.CertificateSubjectType.of_json
                                     certificateSubject) ())
           (Some Values.GetCertificateSigningRequestOutput.to_json)
           (Some Values.GetCertificateSigningRequestOutput.error_to_json)])
let get_default_key_replication_regions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_default_key_replication_regions
           (Values.GetDefaultKeyReplicationRegionsInput.make ())
           (Some Values.GetDefaultKeyReplicationRegionsOutput.to_json)
           (Some Values.GetDefaultKeyReplicationRegionsOutput.error_to_json)])
let get_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_key (Values.GetKeyInput.make ~keyIdentifier ())
           (Some Values.GetKeyOutput.to_json)
           (Some Values.GetKeyOutput.error_to_json)])
let get_mpa_team_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and action =
         flag "action" (required json_arg) ~doc:"JSON MpaOperation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_mpa_team_association
           (Values.GetMpaTeamAssociationInput.make
              ~action:(Values.MpaOperation.of_json action) ())
           (Some Values.GetMpaTeamAssociationOutput.to_json)
           (Some Values.GetMpaTeamAssociationOutput.error_to_json)])
let get_parameters_for_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reuseLastGeneratedToken =
         flag "reuse-last-generated-token" (optional bool)
           ~doc:"BOOL Boolean"
       and keyMaterialType =
         flag "key-material-type" (required json_arg)
           ~doc:"JSON KeyMaterialType"
       and signingKeyAlgorithm =
         flag "signing-key-algorithm" (required json_arg)
           ~doc:"JSON KeyAlgorithm" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_parameters_for_export
           (Values.GetParametersForExportInput.make ?reuseLastGeneratedToken
              ~keyMaterialType:(Values.KeyMaterialType.of_json
                                  keyMaterialType)
              ~signingKeyAlgorithm:(Values.KeyAlgorithm.of_json
                                      signingKeyAlgorithm) ())
           (Some Values.GetParametersForExportOutput.to_json)
           (Some Values.GetParametersForExportOutput.error_to_json)])
let get_parameters_for_import =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reuseLastGeneratedToken =
         flag "reuse-last-generated-token" (optional bool)
           ~doc:"BOOL Boolean"
       and keyMaterialType =
         flag "key-material-type" (required json_arg)
           ~doc:"JSON KeyMaterialType"
       and wrappingKeyAlgorithm =
         flag "wrapping-key-algorithm" (required json_arg)
           ~doc:"JSON KeyAlgorithm" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_parameters_for_import
           (Values.GetParametersForImportInput.make ?reuseLastGeneratedToken
              ~keyMaterialType:(Values.KeyMaterialType.of_json
                                  keyMaterialType)
              ~wrappingKeyAlgorithm:(Values.KeyAlgorithm.of_json
                                       wrappingKeyAlgorithm) ())
           (Some Values.GetParametersForImportOutput.to_json)
           (Some Values.GetParametersForImportOutput.error_to_json)])
let get_public_key_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_public_key_certificate
           (Values.GetPublicKeyCertificateInput.make ~keyIdentifier ())
           (Some Values.GetPublicKeyCertificateOutput.to_json)
           (Some Values.GetPublicKeyCertificateOutput.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyInput.make ~resourceArn ())
           (Some Values.GetResourcePolicyOutput.to_json)
           (Some Values.GetResourcePolicyOutput.error_to_json)])
let import_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyCheckValueAlgorithm =
         flag "key-check-value-algorithm" (optional json_arg)
           ~doc:"JSON KeyCheckValueAlgorithm"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and replicationRegions =
         flag "replication-regions" (optional json_arg) ~doc:"JSON Regions"
       and requesterComment =
         flag "requester-comment" (optional string)
           ~doc:"STRING MpaRequesterComment"
       and keyMaterial =
         flag "key-material" (required json_arg)
           ~doc:"JSON ImportKeyMaterial" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_key
           (Values.ImportKeyInput.make
              ?keyCheckValueAlgorithm:(Option.map
                                         ~f:Values.KeyCheckValueAlgorithm.of_json
                                         keyCheckValueAlgorithm) ?enabled
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?replicationRegions:(Option.map ~f:Values.Regions.of_json
                                     replicationRegions) ?requesterComment
              ~keyMaterial:(Values.ImportKeyMaterial.of_json keyMaterial) ())
           (Some Values.ImportKeyOutput.to_json)
           (Some Values.ImportKeyOutput.error_to_json)])
let list_aliases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyArn = flag "key-arn" (optional string) ~doc:"STRING KeyArn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_aliases
           (Values.ListAliasesInput.make ?keyArn ?nextToken ?maxResults ())
           (Some Values.ListAliasesOutput.to_json)
           (Some Values.ListAliasesOutput.error_to_json)])
let list_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyState =
         flag "key-state" (optional json_arg) ~doc:"JSON KeyState"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_keys
           (Values.ListKeysInput.make
              ?keyState:(Option.map ~f:Values.KeyState.of_json keyState)
              ?nextToken ?maxResults ()) (Some Values.ListKeysOutput.to_json)
           (Some Values.ListKeysOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ?nextToken ?maxResults
              ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and policy =
         flag "policy" (required string) ~doc:"STRING ResourcePolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyInput.make ~resourceArn ~policy ())
           (Some Values.PutResourcePolicyOutput.to_json)
           (Some Values.PutResourcePolicyOutput.error_to_json)])
let remove_key_replication_regions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and replicationRegions =
         flag "replication-regions" (required json_arg) ~doc:"JSON Regions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_key_replication_regions
           (Values.RemoveKeyReplicationRegionsInput.make ~keyIdentifier
              ~replicationRegions:(Values.Regions.of_json replicationRegions)
              ()) (Some Values.RemoveKeyReplicationRegionsOutput.to_json)
           (Some Values.RemoveKeyReplicationRegionsOutput.error_to_json)])
let restore_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_key (Values.RestoreKeyInput.make ~keyIdentifier ())
           (Some Values.RestoreKeyOutput.to_json)
           (Some Values.RestoreKeyOutput.error_to_json)])
let start_key_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_key_usage
           (Values.StartKeyUsageInput.make ~keyIdentifier ())
           (Some Values.StartKeyUsageOutput.to_json)
           (Some Values.StartKeyUsageOutput.error_to_json)])
let stop_key_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_key_usage
           (Values.StopKeyUsageInput.make ~keyIdentifier ())
           (Some Values.StopKeyUsageOutput.to_json)
           (Some Values.StopKeyUsageOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyArn = flag "key-arn" (optional string) ~doc:"STRING KeyArn"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_alias
           (Values.UpdateAliasInput.make ?keyArn ~aliasName ())
           (Some Values.UpdateAliasOutput.to_json)
           (Some Values.UpdateAliasOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-key-replication-regions", add_key_replication_regions);
    ("associate-mpa-team", associate_mpa_team);
    ("create-alias", create_alias);
    ("create-key", create_key);
    ("delete-alias", delete_alias);
    ("delete-key", delete_key);
    ("delete-resource-policy", delete_resource_policy);
    ("disable-default-key-replication-regions",
      disable_default_key_replication_regions);
    ("disassociate-mpa-team", disassociate_mpa_team);
    ("enable-default-key-replication-regions",
      enable_default_key_replication_regions);
    ("export-key", export_key);
    ("get-alias", get_alias);
    ("get-certificate-signing-request", get_certificate_signing_request);
    ("get-default-key-replication-regions",
      get_default_key_replication_regions);
    ("get-key", get_key);
    ("get-mpa-team-association", get_mpa_team_association);
    ("get-parameters-for-export", get_parameters_for_export);
    ("get-parameters-for-import", get_parameters_for_import);
    ("get-public-key-certificate", get_public_key_certificate);
    ("get-resource-policy", get_resource_policy);
    ("import-key", import_key);
    ("list-aliases", list_aliases);
    ("list-keys", list_keys);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-resource-policy", put_resource_policy);
    ("remove-key-replication-regions", remove_key_replication_regions);
    ("restore-key", restore_key);
    ("start-key-usage", start_key_usage);
    ("stop-key-usage", stop_key_usage);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-alias", update_alias)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | DeleteKey: (DeleteKeyRequest.t, DeleteKeyResponse.t,
  DeleteKeyResponse.error) t 
  | DescribeKeyValueStore: (DescribeKeyValueStoreRequest.t,
  DescribeKeyValueStoreResponse.t, DescribeKeyValueStoreResponse.error) t 
  | GetKey: (GetKeyRequest.t, GetKeyResponse.t, GetKeyResponse.error) t 
  | ListKeys: (ListKeysRequest.t, ListKeysResponse.t, ListKeysResponse.error)
  t 
  | PutKey: (PutKeyRequest.t, PutKeyResponse.t, PutKeyResponse.error) t 
  | UpdateKeys: (UpdateKeysRequest.t, UpdateKeysResponse.t,
  UpdateKeysResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | DeleteKey -> `DELETE
  | DescribeKeyValueStore -> `GET
  | GetKey -> `GET
  | ListKeys -> `GET
  | PutKey -> `PUT
  | UpdateKeys -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | DeleteKey ->
          (Format.kasprintf Uri.of_string) "/key-value-stores/%s/keys/%s"
            (KvsARN.to_header x.DeleteKeyRequest.kvsARN)
            (Key.to_header x.DeleteKeyRequest.key)
      | DescribeKeyValueStore ->
          (Format.kasprintf Uri.of_string) "/key-value-stores/%s"
            (KvsARN.to_header x.DescribeKeyValueStoreRequest.kvsARN)
      | GetKey ->
          (Format.kasprintf Uri.of_string) "/key-value-stores/%s/keys/%s"
            (KvsARN.to_header x.GetKeyRequest.kvsARN)
            (Key.to_header x.GetKeyRequest.key)
      | ListKeys ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/key-value-stores/%s/keys"
               (KvsARN.to_header x.ListKeysRequest.kvsARN))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("NextToken", (String_.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v ->
                       ("MaxResults",
                         (ListKeysRequestMaxResultsInteger.to_header v)))
                 x.maxResults])
      | PutKey ->
          (Format.kasprintf Uri.of_string) "/key-value-stores/%s/keys/%s"
            (KvsARN.to_header x.PutKeyRequest.kvsARN)
            (Key.to_header x.PutKeyRequest.key)
      | UpdateKeys ->
          (Format.kasprintf Uri.of_string) "/key-value-stores/%s/keys"
            (KvsARN.to_header x.UpdateKeysRequest.kvsARN))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | DeleteKey -> Awso.Http.Request.make (method_of_endpoint endp)
  | DescribeKeyValueStore ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetKey ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListKeys ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutKey -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateKeys ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("If-Match",
                     (Etag.to_header req.UpdateKeysRequest.ifMatch))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.UpdateKeysRequest.puts
                         ~f:(fun x ->
                               ("Puts", (PutKeyRequestsList.to_value x)));
                      Option.map req.UpdateKeysRequest.deletes
                        ~f:(fun x ->
                              ("Deletes", (DeleteKeyRequestsList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | DeleteKey ->
      if is_success
      then Ok (DeleteKeyResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DeleteKeyResponse.error_of_json))
  | DescribeKeyValueStore ->
      if is_success
      then Ok (DescribeKeyValueStoreResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some DescribeKeyValueStoreResponse.error_of_json))
  | GetKey ->
      if is_success
      then Ok (GetKeyResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetKeyResponse.error_of_json))
  | ListKeys ->
      if is_success
      then Ok (ListKeysResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListKeysResponse.error_of_json))
  | PutKey ->
      if is_success
      then Ok (PutKeyResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some PutKeyResponse.error_of_json))
  | UpdateKeys ->
      if is_success
      then Ok (UpdateKeysResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some UpdateKeysResponse.error_of_json))
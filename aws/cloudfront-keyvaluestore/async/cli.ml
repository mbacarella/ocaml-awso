(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let delete_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kvsARN = flag "kvs-a-r-n" (required string) ~doc:"STRING KvsARN"
       and key = flag "key" (required string) ~doc:"STRING Key"
       and ifMatch = flag "if-match" (required string) ~doc:"STRING Etag" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_key
           (Values.DeleteKeyRequest.make ~kvsARN ~key ~ifMatch ())
           (Some Values.DeleteKeyResponse.to_json)
           (Some Values.DeleteKeyResponse.error_to_json)])
let describe_key_value_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kvsARN = flag "kvs-a-r-n" (required string) ~doc:"STRING KvsARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_key_value_store
           (Values.DescribeKeyValueStoreRequest.make ~kvsARN ())
           (Some Values.DescribeKeyValueStoreResponse.to_json)
           (Some Values.DescribeKeyValueStoreResponse.error_to_json)])
let get_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kvsARN = flag "kvs-a-r-n" (required string) ~doc:"STRING KvsARN"
       and key = flag "key" (required string) ~doc:"STRING Key" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_key (Values.GetKeyRequest.make ~kvsARN ~key ())
           (Some Values.GetKeyResponse.to_json)
           (Some Values.GetKeyResponse.error_to_json)])
let list_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListKeysRequestMaxResultsInteger"
       and kvsARN = flag "kvs-a-r-n" (required string) ~doc:"STRING KvsARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_keys
           (Values.ListKeysRequest.make ?nextToken ?maxResults ~kvsARN ())
           (Some Values.ListKeysResponse.to_json)
           (Some Values.ListKeysResponse.error_to_json)])
let put_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and key = flag "key" (required string) ~doc:"STRING Key"
       and value = flag "value" (required string) ~doc:"STRING Value"
       and kvsARN = flag "kvs-a-r-n" (required string) ~doc:"STRING KvsARN"
       and ifMatch = flag "if-match" (required string) ~doc:"STRING Etag" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_key
           (Values.PutKeyRequest.make ~key ~value ~kvsARN ~ifMatch ())
           (Some Values.PutKeyResponse.to_json)
           (Some Values.PutKeyResponse.error_to_json)])
let update_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and puts =
         flag "puts" (optional json_arg) ~doc:"JSON PutKeyRequestsList"
       and deletes =
         flag "deletes" (optional json_arg) ~doc:"JSON DeleteKeyRequestsList"
       and kvsARN = flag "kvs-a-r-n" (required string) ~doc:"STRING KvsARN"
       and ifMatch = flag "if-match" (required string) ~doc:"STRING Etag" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_keys
           (Values.UpdateKeysRequest.make
              ?puts:(Option.map ~f:Values.PutKeyRequestsList.of_json puts)
              ?deletes:(Option.map ~f:Values.DeleteKeyRequestsList.of_json
                          deletes) ~kvsARN ~ifMatch ())
           (Some Values.UpdateKeysResponse.to_json)
           (Some Values.UpdateKeysResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("delete-key", delete_key);
    ("describe-key-value-store", describe_key_value_store);
    ("get-key", get_key);
    ("list-keys", list_keys);
    ("put-key", put_key);
    ("update-keys", update_keys)]
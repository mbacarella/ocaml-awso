(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_add_channel_role_to_accessors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING ChannelId"
       and accessorIds =
         flag "accessor-ids" (required json_arg) ~doc:"JSON AccessorIdList"
       and channelRole =
         flag "channel-role" (required json_arg) ~doc:"JSON ChannelRole" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_add_channel_role_to_accessors
           (Values.BatchAddChannelRoleToAccessorsInput.make ~spaceId
              ~channelId
              ~accessorIds:(Values.AccessorIdList.of_json accessorIds)
              ~channelRole:(Values.ChannelRole.of_json channelRole) ())
           (Some Values.BatchAddChannelRoleToAccessorsOutput.to_json)
           (Some Values.BatchAddChannelRoleToAccessorsOutput.error_to_json)])
let batch_add_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId"
       and accessorIds =
         flag "accessor-ids" (required json_arg) ~doc:"JSON AccessorIdList"
       and role = flag "role" (required json_arg) ~doc:"JSON Role" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_add_role
           (Values.BatchAddRoleInput.make ~spaceId
              ~accessorIds:(Values.AccessorIdList.of_json accessorIds)
              ~role:(Values.Role.of_json role) ())
           (Some Values.BatchAddRoleOutput.to_json)
           (Some Values.BatchAddRoleOutput.error_to_json)])
let batch_remove_channel_role_from_accessors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING ChannelId"
       and accessorIds =
         flag "accessor-ids" (required json_arg) ~doc:"JSON AccessorIdList"
       and channelRole =
         flag "channel-role" (required json_arg) ~doc:"JSON ChannelRole" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_remove_channel_role_from_accessors
           (Values.BatchRemoveChannelRoleFromAccessorsInput.make ~spaceId
              ~channelId
              ~accessorIds:(Values.AccessorIdList.of_json accessorIds)
              ~channelRole:(Values.ChannelRole.of_json channelRole) ())
           (Some Values.BatchRemoveChannelRoleFromAccessorsOutput.to_json)
           (Some
              Values.BatchRemoveChannelRoleFromAccessorsOutput.error_to_json)])
let batch_remove_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId"
       and accessorIds =
         flag "accessor-ids" (required json_arg) ~doc:"JSON AccessorIdList"
       and role = flag "role" (required json_arg) ~doc:"JSON Role" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_remove_role
           (Values.BatchRemoveRoleInput.make ~spaceId
              ~accessorIds:(Values.AccessorIdList.of_json accessorIds)
              ~role:(Values.Role.of_json role) ())
           (Some Values.BatchRemoveRoleOutput.to_json)
           (Some Values.BatchRemoveRoleOutput.error_to_json)])
let create_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelDescription =
         flag "channel-description" (optional string)
           ~doc:"STRING ChannelDescription"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ChannelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel
           (Values.CreateChannelInput.make ?channelDescription ~spaceId
              ~channelName ()) (Some Values.CreateChannelOutput.to_json)
           (Some Values.CreateChannelOutput.error_to_json)])
let create_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING SpaceDescription"
       and userKMSKey =
         flag "user-k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING Arn"
       and supportedEmailDomains =
         flag "supported-email-domains" (optional json_arg)
           ~doc:"JSON SupportedEmailDomainsParameters"
       and name = flag "name" (required string) ~doc:"STRING SpaceName"
       and subdomain =
         flag "subdomain" (required string) ~doc:"STRING SpaceSubdomain"
       and tier = flag "tier" (required json_arg) ~doc:"JSON TierLevel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_space
           (Values.CreateSpaceInput.make ?description ?userKMSKey
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?roleArn
              ?supportedEmailDomains:(Option.map
                                        ~f:Values.SupportedEmailDomainsParameters.of_json
                                        supportedEmailDomains) ~name
              ~subdomain ~tier:(Values.TierLevel.of_json tier) ())
           (Some Values.CreateSpaceOutput.to_json)
           (Some Values.CreateSpaceOutput.error_to_json)])
let delete_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_space (Values.DeleteSpaceInput.make ~spaceId ()) None
           None])
let deregister_admin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId"
       and adminId = flag "admin-id" (required string) ~doc:"STRING AdminId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_admin
           (Values.DeregisterAdminInput.make ~spaceId ~adminId ()) None None])
let get_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING ChannelId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_channel
           (Values.GetChannelInput.make ~spaceId ~channelId ())
           (Some Values.GetChannelOutput.to_json)
           (Some Values.GetChannelOutput.error_to_json)])
let get_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_space (Values.GetSpaceInput.make ~spaceId ())
           (Some Values.GetSpaceOutput.to_json)
           (Some Values.GetSpaceOutput.error_to_json)])
let list_channels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListChannelsLimit"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channels
           (Values.ListChannelsInput.make ?nextToken ?maxResults ~spaceId ())
           (Some Values.ListChannelsOutput.to_json)
           (Some Values.ListChannelsOutput.error_to_json)])
let list_spaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListSpacesLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_spaces
           (Values.ListSpacesInput.make ?nextToken ?maxResults ())
           (Some Values.ListSpacesOutput.to_json)
           (Some Values.ListSpacesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let register_admin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId"
       and adminId = flag "admin-id" (required string) ~doc:"STRING AdminId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_admin
           (Values.RegisterAdminInput.make ~spaceId ~adminId ()) None None])
let send_invites =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId"
       and accessorIds =
         flag "accessor-ids" (required json_arg) ~doc:"JSON AccessorIdList"
       and title = flag "title" (required string) ~doc:"STRING InviteTitle"
       and body = flag "body" (required string) ~doc:"STRING InviteBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_invites
           (Values.SendInvitesInput.make ~spaceId
              ~accessorIds:(Values.AccessorIdList.of_json accessorIds) ~title
              ~body ()) None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelDescription =
         flag "channel-description" (optional string)
           ~doc:"STRING ChannelDescription"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING ChannelId"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ChannelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel
           (Values.UpdateChannelInput.make ?channelDescription ~spaceId
              ~channelId ~channelName ())
           (Some Values.UpdateChannelOutput.to_json)
           (Some Values.UpdateChannelOutput.error_to_json)])
let update_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING SpaceDescription"
       and tier = flag "tier" (optional json_arg) ~doc:"JSON TierLevel"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING Arn"
       and supportedEmailDomains =
         flag "supported-email-domains" (optional json_arg)
           ~doc:"JSON SupportedEmailDomainsParameters"
       and spaceId = flag "space-id" (required string) ~doc:"STRING SpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_space
           (Values.UpdateSpaceInput.make ?description
              ?tier:(Option.map ~f:Values.TierLevel.of_json tier) ?roleArn
              ?supportedEmailDomains:(Option.map
                                        ~f:Values.SupportedEmailDomainsParameters.of_json
                                        supportedEmailDomains) ~spaceId ())
           None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-add-channel-role-to-accessors",
       batch_add_channel_role_to_accessors);
    ("batch-add-role", batch_add_role);
    ("batch-remove-channel-role-from-accessors",
      batch_remove_channel_role_from_accessors);
    ("batch-remove-role", batch_remove_role);
    ("create-channel", create_channel);
    ("create-space", create_space);
    ("delete-space", delete_space);
    ("deregister-admin", deregister_admin);
    ("get-channel", get_channel);
    ("get-space", get_space);
    ("list-channels", list_channels);
    ("list-spaces", list_spaces);
    ("list-tags-for-resource", list_tags_for_resource);
    ("register-admin", register_admin);
    ("send-invites", send_invites);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-channel", update_channel);
    ("update-space", update_space)]
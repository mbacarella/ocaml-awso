(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | BatchAddChannelRoleToAccessors: (BatchAddChannelRoleToAccessorsInput.t,
  BatchAddChannelRoleToAccessorsOutput.t,
  BatchAddChannelRoleToAccessorsOutput.error) t 
  | BatchAddRole: (BatchAddRoleInput.t, BatchAddRoleOutput.t,
  BatchAddRoleOutput.error) t 
  | BatchRemoveChannelRoleFromAccessors:
  (BatchRemoveChannelRoleFromAccessorsInput.t,
  BatchRemoveChannelRoleFromAccessorsOutput.t,
  BatchRemoveChannelRoleFromAccessorsOutput.error) t 
  | BatchRemoveRole: (BatchRemoveRoleInput.t, BatchRemoveRoleOutput.t,
  BatchRemoveRoleOutput.error) t 
  | CreateChannel: (CreateChannelInput.t, CreateChannelOutput.t,
  CreateChannelOutput.error) t 
  | CreateSpace: (CreateSpaceInput.t, CreateSpaceOutput.t,
  CreateSpaceOutput.error) t 
  | DeleteSpace: (DeleteSpaceInput.t, unit, unit) t 
  | DeregisterAdmin: (DeregisterAdminInput.t, unit, unit) t 
  | GetChannel: (GetChannelInput.t, GetChannelOutput.t,
  GetChannelOutput.error) t 
  | GetSpace: (GetSpaceInput.t, GetSpaceOutput.t, GetSpaceOutput.error) t 
  | ListChannels: (ListChannelsInput.t, ListChannelsOutput.t,
  ListChannelsOutput.error) t 
  | ListSpaces: (ListSpacesInput.t, ListSpacesOutput.t,
  ListSpacesOutput.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | RegisterAdmin: (RegisterAdminInput.t, unit, unit) t 
  | SendInvites: (SendInvitesInput.t, unit, unit) t 
  | TagResource: (TagResourceRequest.t, TagResourceResponse.t,
  TagResourceResponse.error) t 
  | UntagResource: (UntagResourceRequest.t, UntagResourceResponse.t,
  UntagResourceResponse.error) t 
  | UpdateChannel: (UpdateChannelInput.t, UpdateChannelOutput.t,
  UpdateChannelOutput.error) t 
  | UpdateSpace: (UpdateSpaceInput.t, unit, unit) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | BatchAddChannelRoleToAccessors -> `POST
  | BatchAddRole -> `POST
  | BatchRemoveChannelRoleFromAccessors -> `PATCH
  | BatchRemoveRole -> `PATCH
  | CreateChannel -> `POST
  | CreateSpace -> `POST
  | DeleteSpace -> `DELETE
  | DeregisterAdmin -> `DELETE
  | GetChannel -> `GET
  | GetSpace -> `GET
  | ListChannels -> `GET
  | ListSpaces -> `GET
  | ListTagsForResource -> `GET
  | RegisterAdmin -> `POST
  | SendInvites -> `POST
  | TagResource -> `POST
  | UntagResource -> `DELETE
  | UpdateChannel -> `PUT
  | UpdateSpace -> `PUT
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | BatchAddChannelRoleToAccessors ->
          (Format.kasprintf Uri.of_string) "/spaces/%s/channels/%s/roles"
            (SpaceId.to_header x.BatchAddChannelRoleToAccessorsInput.spaceId)
            (ChannelId.to_header
               x.BatchAddChannelRoleToAccessorsInput.channelId)
      | BatchAddRole ->
          (Format.kasprintf Uri.of_string) "/spaces/%s/roles"
            (SpaceId.to_header x.BatchAddRoleInput.spaceId)
      | BatchRemoveChannelRoleFromAccessors ->
          (Format.kasprintf Uri.of_string) "/spaces/%s/channels/%s/roles"
            (SpaceId.to_header
               x.BatchRemoveChannelRoleFromAccessorsInput.spaceId)
            (ChannelId.to_header
               x.BatchRemoveChannelRoleFromAccessorsInput.channelId)
      | BatchRemoveRole ->
          (Format.kasprintf Uri.of_string) "/spaces/%s/roles"
            (SpaceId.to_header x.BatchRemoveRoleInput.spaceId)
      | CreateChannel ->
          (Format.kasprintf Uri.of_string) "/spaces/%s/channels"
            (SpaceId.to_header x.CreateChannelInput.spaceId)
      | CreateSpace -> (Format.kasprintf Uri.of_string) "/spaces"
      | DeleteSpace ->
          (Format.kasprintf Uri.of_string) "/spaces/%s"
            (SpaceId.to_header x.DeleteSpaceInput.spaceId)
      | DeregisterAdmin ->
          (Format.kasprintf Uri.of_string) "/spaces/%s/admins/%s"
            (SpaceId.to_header x.DeregisterAdminInput.spaceId)
            (AdminId.to_header x.DeregisterAdminInput.adminId)
      | GetChannel ->
          (Format.kasprintf Uri.of_string) "/spaces/%s/channels/%s"
            (SpaceId.to_header x.GetChannelInput.spaceId)
            (ChannelId.to_header x.GetChannelInput.channelId)
      | GetSpace ->
          (Format.kasprintf Uri.of_string) "/spaces/%s"
            (SpaceId.to_header x.GetSpaceInput.spaceId)
      | ListChannels ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/spaces/%s/channels"
               (SpaceId.to_header x.ListChannelsInput.spaceId))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("nextToken", (String_.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v -> ("maxResults", (ListChannelsLimit.to_header v)))
                 x.maxResults])
      | ListSpaces ->
          Uri.add_query_params' ((Format.kasprintf Uri.of_string) "/spaces")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("nextToken", (String_.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v -> ("maxResults", (ListSpacesLimit.to_header v)))
                 x.maxResults])
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (Arn.to_header x.ListTagsForResourceRequest.resourceArn)
      | RegisterAdmin ->
          (Format.kasprintf Uri.of_string) "/spaces/%s/admins/%s"
            (SpaceId.to_header x.RegisterAdminInput.spaceId)
            (AdminId.to_header x.RegisterAdminInput.adminId)
      | SendInvites ->
          (Format.kasprintf Uri.of_string) "/spaces/%s/invite"
            (SpaceId.to_header x.SendInvitesInput.spaceId)
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (Arn.to_header x.TagResourceRequest.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (Arn.to_header x.UntagResourceRequest.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))])
      | UpdateChannel ->
          (Format.kasprintf Uri.of_string) "/spaces/%s/channels/%s"
            (SpaceId.to_header x.UpdateChannelInput.spaceId)
            (ChannelId.to_header x.UpdateChannelInput.channelId)
      | UpdateSpace ->
          (Format.kasprintf Uri.of_string) "/spaces/%s"
            (SpaceId.to_header x.UpdateSpaceInput.spaceId))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | BatchAddChannelRoleToAccessors ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("accessorIds",
                           (AccessorIdList.to_value
                              req.BatchAddChannelRoleToAccessorsInput.accessorIds));
                      Some
                        ("channelRole",
                          (ChannelRole.to_value
                             req.BatchAddChannelRoleToAccessorsInput.channelRole))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BatchAddRole ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("accessorIds",
                           (AccessorIdList.to_value
                              req.BatchAddRoleInput.accessorIds));
                      Some
                        ("role", (Role.to_value req.BatchAddRoleInput.role))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BatchRemoveChannelRoleFromAccessors ->
      Awso.Http.Request.make (method_of_endpoint endp)
  | BatchRemoveRole -> Awso.Http.Request.make (method_of_endpoint endp)
  | CreateChannel ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("channelName",
                           (ChannelName.to_value
                              req.CreateChannelInput.channelName));
                      Option.map req.CreateChannelInput.channelDescription
                        ~f:(fun x ->
                              ("channelDescription",
                                (ChannelDescription.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateSpace ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("name",
                           (SpaceName.to_value req.CreateSpaceInput.name));
                      Some
                        ("subdomain",
                          (SpaceSubdomain.to_value
                             req.CreateSpaceInput.subdomain));
                      Some
                        ("tier",
                          (TierLevel.to_value req.CreateSpaceInput.tier));
                      Option.map req.CreateSpaceInput.description
                        ~f:(fun x ->
                              ("description", (SpaceDescription.to_value x)));
                      Option.map req.CreateSpaceInput.userKMSKey
                        ~f:(fun x -> ("userKMSKey", (KMSKey.to_value x)));
                      Option.map req.CreateSpaceInput.tags
                        ~f:(fun x -> ("tags", (Tags.to_value x)));
                      Option.map req.CreateSpaceInput.roleArn
                        ~f:(fun x -> ("roleArn", (Arn.to_value x)));
                      Option.map req.CreateSpaceInput.supportedEmailDomains
                        ~f:(fun x ->
                              ("supportedEmailDomains",
                                (SupportedEmailDomainsParameters.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteSpace -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeregisterAdmin -> Awso.Http.Request.make (method_of_endpoint endp)
  | GetChannel ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetSpace ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListChannels ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSpaces ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | RegisterAdmin ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SendInvites ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("accessorIds",
                           (AccessorIdList.to_value
                              req.SendInvitesInput.accessorIds));
                      Some
                        ("title",
                          (InviteTitle.to_value req.SendInvitesInput.title));
                      Some
                        ("body",
                          (InviteBody.to_value req.SendInvitesInput.body))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tags",
                           (Tags.to_value req.TagResourceRequest.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateChannel -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateSpace -> Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | BatchAddChannelRoleToAccessors ->
      if is_success
      then
        Ok
          (BatchAddChannelRoleToAccessorsOutput.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some BatchAddChannelRoleToAccessorsOutput.error_of_json))
  | BatchAddRole ->
      if is_success
      then Ok (BatchAddRoleOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some BatchAddRoleOutput.error_of_json))
  | BatchRemoveChannelRoleFromAccessors ->
      if is_success
      then
        Ok
          (BatchRemoveChannelRoleFromAccessorsOutput.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some BatchRemoveChannelRoleFromAccessorsOutput.error_of_json))
  | BatchRemoveRole ->
      if is_success
      then Ok (BatchRemoveRoleOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some BatchRemoveRoleOutput.error_of_json))
  | CreateChannel ->
      if is_success
      then Ok (CreateChannelOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateChannelOutput.error_of_json))
  | CreateSpace ->
      if is_success
      then Ok (CreateSpaceOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateSpaceOutput.error_of_json))
  | DeleteSpace -> if is_success then Ok () else Error (parse_aws_error None)
  | DeregisterAdmin ->
      if is_success then Ok () else Error (parse_aws_error None)
  | GetChannel ->
      if is_success
      then Ok (GetChannelOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetChannelOutput.error_of_json))
  | GetSpace ->
      if is_success
      then Ok (GetSpaceOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetSpaceOutput.error_of_json))
  | ListChannels ->
      if is_success
      then Ok (ListChannelsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListChannelsOutput.error_of_json))
  | ListSpaces ->
      if is_success
      then Ok (ListSpacesOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListSpacesOutput.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | RegisterAdmin ->
      if is_success then Ok () else Error (parse_aws_error None)
  | SendInvites -> if is_success then Ok () else Error (parse_aws_error None)
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceResponse.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceResponse.error_of_json))
  | UpdateChannel ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UpdateChannelOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UpdateChannelOutput.error_of_json))
  | UpdateSpace -> if is_success then Ok () else Error (parse_aws_error None)
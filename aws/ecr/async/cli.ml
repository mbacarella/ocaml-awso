(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_check_layer_availability =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and layerDigests =
         flag "layer-digests" (required json_arg)
           ~doc:"JSON BatchedOperationLayerDigestList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_check_layer_availability
           (Values.BatchCheckLayerAvailabilityRequest.make ?registryId
              ~repositoryName
              ~layerDigests:(Values.BatchedOperationLayerDigestList.of_json
                               layerDigests) ())
           (Some Values.BatchCheckLayerAvailabilityResponse.to_json)
           (Some Values.BatchCheckLayerAvailabilityResponse.error_to_json)])
let batch_delete_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and imageIds =
         flag "image-ids" (required json_arg) ~doc:"JSON ImageIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_image
           (Values.BatchDeleteImageRequest.make ?registryId ~repositoryName
              ~imageIds:(Values.ImageIdentifierList.of_json imageIds) ())
           (Some Values.BatchDeleteImageResponse.to_json)
           (Some Values.BatchDeleteImageResponse.error_to_json)])
let batch_get_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and acceptedMediaTypes =
         flag "accepted-media-types" (optional json_arg)
           ~doc:"JSON MediaTypeList"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and imageIds =
         flag "image-ids" (required json_arg) ~doc:"JSON ImageIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_image
           (Values.BatchGetImageRequest.make ?registryId
              ?acceptedMediaTypes:(Option.map ~f:Values.MediaTypeList.of_json
                                     acceptedMediaTypes) ~repositoryName
              ~imageIds:(Values.ImageIdentifierList.of_json imageIds) ())
           (Some Values.BatchGetImageResponse.to_json)
           (Some Values.BatchGetImageResponse.error_to_json)])
let batch_get_repository_scanning_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositoryNames =
         flag "repository-names" (required json_arg)
           ~doc:"JSON ScanningConfigurationRepositoryNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_repository_scanning_configuration
           (Values.BatchGetRepositoryScanningConfigurationRequest.make
              ~repositoryNames:(Values.ScanningConfigurationRepositoryNameList.of_json
                                  repositoryNames) ())
           (Some
              Values.BatchGetRepositoryScanningConfigurationResponse.to_json)
           (Some
              Values.BatchGetRepositoryScanningConfigurationResponse.error_to_json)])
let complete_layer_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING UploadId"
       and layerDigests =
         flag "layer-digests" (required json_arg) ~doc:"JSON LayerDigestList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.complete_layer_upload
           (Values.CompleteLayerUploadRequest.make ?registryId
              ~repositoryName ~uploadId
              ~layerDigests:(Values.LayerDigestList.of_json layerDigests) ())
           (Some Values.CompleteLayerUploadResponse.to_json)
           (Some Values.CompleteLayerUploadResponse.error_to_json)])
let create_pull_through_cache_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and ecrRepositoryPrefix =
         flag "ecr-repository-prefix" (required string)
           ~doc:"STRING PullThroughCacheRuleRepositoryPrefix"
       and upstreamRegistryUrl =
         flag "upstream-registry-url" (required string) ~doc:"STRING Url" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pull_through_cache_rule
           (Values.CreatePullThroughCacheRuleRequest.make ?registryId
              ~ecrRepositoryPrefix ~upstreamRegistryUrl ())
           (Some Values.CreatePullThroughCacheRuleResponse.to_json)
           (Some Values.CreatePullThroughCacheRuleResponse.error_to_json)])
let create_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and imageTagMutability =
         flag "image-tag-mutability" (optional json_arg)
           ~doc:"JSON ImageTagMutability"
       and imageScanningConfiguration =
         flag "image-scanning-configuration" (optional json_arg)
           ~doc:"JSON ImageScanningConfiguration"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_repository
           (Values.CreateRepositoryRequest.make ?registryId
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?imageTagMutability:(Option.map
                                     ~f:Values.ImageTagMutability.of_json
                                     imageTagMutability)
              ?imageScanningConfiguration:(Option.map
                                             ~f:Values.ImageScanningConfiguration.of_json
                                             imageScanningConfiguration)
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ~repositoryName ())
           (Some Values.CreateRepositoryResponse.to_json)
           (Some Values.CreateRepositoryResponse.error_to_json)])
let delete_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_lifecycle_policy
           (Values.DeleteLifecyclePolicyRequest.make ?registryId
              ~repositoryName ())
           (Some Values.DeleteLifecyclePolicyResponse.to_json)
           (Some Values.DeleteLifecyclePolicyResponse.error_to_json)])
let delete_pull_through_cache_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and ecrRepositoryPrefix =
         flag "ecr-repository-prefix" (required string)
           ~doc:"STRING PullThroughCacheRuleRepositoryPrefix" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_pull_through_cache_rule
           (Values.DeletePullThroughCacheRuleRequest.make ?registryId
              ~ecrRepositoryPrefix ())
           (Some Values.DeletePullThroughCacheRuleResponse.to_json)
           (Some Values.DeletePullThroughCacheRuleResponse.error_to_json)])
let delete_registry_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_registry_policy
           (Values.DeleteRegistryPolicyRequest.make ())
           (Some Values.DeleteRegistryPolicyResponse.to_json)
           (Some Values.DeleteRegistryPolicyResponse.error_to_json)])
let delete_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and force = flag "force" (optional bool) ~doc:"BOOL ForceFlag"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_repository
           (Values.DeleteRepositoryRequest.make ?registryId ?force
              ~repositoryName ())
           (Some Values.DeleteRepositoryResponse.to_json)
           (Some Values.DeleteRepositoryResponse.error_to_json)])
let delete_repository_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_repository_policy
           (Values.DeleteRepositoryPolicyRequest.make ?registryId
              ~repositoryName ())
           (Some Values.DeleteRepositoryPolicyResponse.to_json)
           (Some Values.DeleteRepositoryPolicyResponse.error_to_json)])
let describe_image_replication_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and imageId =
         flag "image-id" (required json_arg) ~doc:"JSON ImageIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_image_replication_status
           (Values.DescribeImageReplicationStatusRequest.make ?registryId
              ~repositoryName
              ~imageId:(Values.ImageIdentifier.of_json imageId) ())
           (Some Values.DescribeImageReplicationStatusResponse.to_json)
           (Some Values.DescribeImageReplicationStatusResponse.error_to_json)])
let describe_image_scan_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and imageId =
         flag "image-id" (required json_arg) ~doc:"JSON ImageIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_image_scan_findings
           (Values.DescribeImageScanFindingsRequest.make ?registryId
              ?nextToken ?maxResults ~repositoryName
              ~imageId:(Values.ImageIdentifier.of_json imageId) ())
           (Some Values.DescribeImageScanFindingsResponse.to_json)
           (Some Values.DescribeImageScanFindingsResponse.error_to_json)])
let describe_images =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and imageIds =
         flag "image-ids" (optional json_arg) ~doc:"JSON ImageIdentifierList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON DescribeImagesFilter"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_images
           (Values.DescribeImagesRequest.make ?registryId
              ?imageIds:(Option.map ~f:Values.ImageIdentifierList.of_json
                           imageIds) ?nextToken ?maxResults
              ?filter:(Option.map ~f:Values.DescribeImagesFilter.of_json
                         filter) ~repositoryName ())
           (Some Values.DescribeImagesResponse.to_json)
           (Some Values.DescribeImagesResponse.error_to_json)])
let describe_pull_through_cache_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and ecrRepositoryPrefixes =
         flag "ecr-repository-prefixes" (optional json_arg)
           ~doc:"JSON PullThroughCacheRuleRepositoryPrefixList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_pull_through_cache_rules
           (Values.DescribePullThroughCacheRulesRequest.make ?registryId
              ?ecrRepositoryPrefixes:(Option.map
                                        ~f:Values.PullThroughCacheRuleRepositoryPrefixList.of_json
                                        ecrRepositoryPrefixes) ?nextToken
              ?maxResults ())
           (Some Values.DescribePullThroughCacheRulesResponse.to_json)
           (Some Values.DescribePullThroughCacheRulesResponse.error_to_json)])
let describe_registry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_registry (Values.DescribeRegistryRequest.make ())
           (Some Values.DescribeRegistryResponse.to_json)
           (Some Values.DescribeRegistryResponse.error_to_json)])
let describe_repositories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryNames =
         flag "repository-names" (optional json_arg)
           ~doc:"JSON RepositoryNameList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_repositories
           (Values.DescribeRepositoriesRequest.make ?registryId
              ?repositoryNames:(Option.map
                                  ~f:Values.RepositoryNameList.of_json
                                  repositoryNames) ?nextToken ?maxResults ())
           (Some Values.DescribeRepositoriesResponse.to_json)
           (Some Values.DescribeRepositoriesResponse.error_to_json)])
let get_authorization_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryIds =
         flag "registry-ids" (optional json_arg)
           ~doc:"JSON GetAuthorizationTokenRegistryIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_authorization_token
           (Values.GetAuthorizationTokenRequest.make
              ?registryIds:(Option.map
                              ~f:Values.GetAuthorizationTokenRegistryIdList.of_json
                              registryIds) ())
           (Some Values.GetAuthorizationTokenResponse.to_json)
           (Some Values.GetAuthorizationTokenResponse.error_to_json)])
let get_download_url_for_layer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and layerDigest =
         flag "layer-digest" (required string) ~doc:"STRING LayerDigest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_download_url_for_layer
           (Values.GetDownloadUrlForLayerRequest.make ?registryId
              ~repositoryName ~layerDigest ())
           (Some Values.GetDownloadUrlForLayerResponse.to_json)
           (Some Values.GetDownloadUrlForLayerResponse.error_to_json)])
let get_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_lifecycle_policy
           (Values.GetLifecyclePolicyRequest.make ?registryId ~repositoryName
              ()) (Some Values.GetLifecyclePolicyResponse.to_json)
           (Some Values.GetLifecyclePolicyResponse.error_to_json)])
let get_lifecycle_policy_preview =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and imageIds =
         flag "image-ids" (optional json_arg) ~doc:"JSON ImageIdentifierList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT LifecyclePreviewMaxResults"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON LifecyclePolicyPreviewFilter"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_lifecycle_policy_preview
           (Values.GetLifecyclePolicyPreviewRequest.make ?registryId
              ?imageIds:(Option.map ~f:Values.ImageIdentifierList.of_json
                           imageIds) ?nextToken ?maxResults
              ?filter:(Option.map
                         ~f:Values.LifecyclePolicyPreviewFilter.of_json
                         filter) ~repositoryName ())
           (Some Values.GetLifecyclePolicyPreviewResponse.to_json)
           (Some Values.GetLifecyclePolicyPreviewResponse.error_to_json)])
let get_registry_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_registry_policy (Values.GetRegistryPolicyRequest.make ())
           (Some Values.GetRegistryPolicyResponse.to_json)
           (Some Values.GetRegistryPolicyResponse.error_to_json)])
let get_registry_scanning_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_registry_scanning_configuration
           (Values.GetRegistryScanningConfigurationRequest.make ())
           (Some Values.GetRegistryScanningConfigurationResponse.to_json)
           (Some
              Values.GetRegistryScanningConfigurationResponse.error_to_json)])
let get_repository_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_repository_policy
           (Values.GetRepositoryPolicyRequest.make ?registryId
              ~repositoryName ())
           (Some Values.GetRepositoryPolicyResponse.to_json)
           (Some Values.GetRepositoryPolicyResponse.error_to_json)])
let initiate_layer_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.initiate_layer_upload
           (Values.InitiateLayerUploadRequest.make ?registryId
              ~repositoryName ())
           (Some Values.InitiateLayerUploadResponse.to_json)
           (Some Values.InitiateLayerUploadResponse.error_to_json)])
let list_images =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ListImagesFilter"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_images
           (Values.ListImagesRequest.make ?registryId ?nextToken ?maxResults
              ?filter:(Option.map ~f:Values.ListImagesFilter.of_json filter)
              ~repositoryName ()) (Some Values.ListImagesResponse.to_json)
           (Some Values.ListImagesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and imageManifestMediaType =
         flag "image-manifest-media-type" (optional string)
           ~doc:"STRING MediaType"
       and imageTag =
         flag "image-tag" (optional string) ~doc:"STRING ImageTag"
       and imageDigest =
         flag "image-digest" (optional string) ~doc:"STRING ImageDigest"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and imageManifest =
         flag "image-manifest" (required string) ~doc:"STRING ImageManifest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_image
           (Values.PutImageRequest.make ?registryId ?imageManifestMediaType
              ?imageTag ?imageDigest ~repositoryName ~imageManifest ())
           (Some Values.PutImageResponse.to_json)
           (Some Values.PutImageResponse.error_to_json)])
let put_image_scanning_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and imageScanningConfiguration =
         flag "image-scanning-configuration" (required json_arg)
           ~doc:"JSON ImageScanningConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_image_scanning_configuration
           (Values.PutImageScanningConfigurationRequest.make ?registryId
              ~repositoryName
              ~imageScanningConfiguration:(Values.ImageScanningConfiguration.of_json
                                             imageScanningConfiguration) ())
           (Some Values.PutImageScanningConfigurationResponse.to_json)
           (Some Values.PutImageScanningConfigurationResponse.error_to_json)])
let put_image_tag_mutability =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and imageTagMutability =
         flag "image-tag-mutability" (required json_arg)
           ~doc:"JSON ImageTagMutability" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_image_tag_mutability
           (Values.PutImageTagMutabilityRequest.make ?registryId
              ~repositoryName
              ~imageTagMutability:(Values.ImageTagMutability.of_json
                                     imageTagMutability) ())
           (Some Values.PutImageTagMutabilityResponse.to_json)
           (Some Values.PutImageTagMutabilityResponse.error_to_json)])
let put_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and lifecyclePolicyText =
         flag "lifecycle-policy-text" (required string)
           ~doc:"STRING LifecyclePolicyText" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_lifecycle_policy
           (Values.PutLifecyclePolicyRequest.make ?registryId ~repositoryName
              ~lifecyclePolicyText ())
           (Some Values.PutLifecyclePolicyResponse.to_json)
           (Some Values.PutLifecyclePolicyResponse.error_to_json)])
let put_registry_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyText =
         flag "policy-text" (required string)
           ~doc:"STRING RegistryPolicyText" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_registry_policy
           (Values.PutRegistryPolicyRequest.make ~policyText ())
           (Some Values.PutRegistryPolicyResponse.to_json)
           (Some Values.PutRegistryPolicyResponse.error_to_json)])
let put_registry_scanning_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scanType =
         flag "scan-type" (optional json_arg) ~doc:"JSON ScanType"
       and rules =
         flag "rules" (optional json_arg)
           ~doc:"JSON RegistryScanningRuleList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_registry_scanning_configuration
           (Values.PutRegistryScanningConfigurationRequest.make
              ?scanType:(Option.map ~f:Values.ScanType.of_json scanType)
              ?rules:(Option.map ~f:Values.RegistryScanningRuleList.of_json
                        rules) ())
           (Some Values.PutRegistryScanningConfigurationResponse.to_json)
           (Some
              Values.PutRegistryScanningConfigurationResponse.error_to_json)])
let put_replication_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationConfiguration =
         flag "replication-configuration" (required json_arg)
           ~doc:"JSON ReplicationConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_replication_configuration
           (Values.PutReplicationConfigurationRequest.make
              ~replicationConfiguration:(Values.ReplicationConfiguration.of_json
                                           replicationConfiguration) ())
           (Some Values.PutReplicationConfigurationResponse.to_json)
           (Some Values.PutReplicationConfigurationResponse.error_to_json)])
let set_repository_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and force = flag "force" (optional bool) ~doc:"BOOL ForceFlag"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and policyText =
         flag "policy-text" (required string)
           ~doc:"STRING RepositoryPolicyText" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_repository_policy
           (Values.SetRepositoryPolicyRequest.make ?registryId ?force
              ~repositoryName ~policyText ())
           (Some Values.SetRepositoryPolicyResponse.to_json)
           (Some Values.SetRepositoryPolicyResponse.error_to_json)])
let start_image_scan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and imageId =
         flag "image-id" (required json_arg) ~doc:"JSON ImageIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_image_scan
           (Values.StartImageScanRequest.make ?registryId ~repositoryName
              ~imageId:(Values.ImageIdentifier.of_json imageId) ())
           (Some Values.StartImageScanResponse.to_json)
           (Some Values.StartImageScanResponse.error_to_json)])
let start_lifecycle_policy_preview =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and lifecyclePolicyText =
         flag "lifecycle-policy-text" (optional string)
           ~doc:"STRING LifecyclePolicyText"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_lifecycle_policy_preview
           (Values.StartLifecyclePolicyPreviewRequest.make ?registryId
              ?lifecyclePolicyText ~repositoryName ())
           (Some Values.StartLifecyclePolicyPreviewResponse.to_json)
           (Some Values.StartLifecyclePolicyPreviewResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let upload_layer_part =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (optional string) ~doc:"STRING RegistryId"
       and repositoryName =
         flag "repository-name" (required string)
           ~doc:"STRING RepositoryName"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING UploadId"
       and partFirstByte =
         flag "part-first-byte" (required json_arg) ~doc:"JSON PartSize"
       and partLastByte =
         flag "part-last-byte" (required json_arg) ~doc:"JSON PartSize"
       and layerPartBlob =
         flag "layer-part-blob" (required json_arg) ~doc:"JSON LayerPartBlob" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upload_layer_part
           (Values.UploadLayerPartRequest.make ?registryId ~repositoryName
              ~uploadId
              ~partFirstByte:(Values.PartSize.of_json partFirstByte)
              ~partLastByte:(Values.PartSize.of_json partLastByte)
              ~layerPartBlob:(Values.LayerPartBlob.of_json layerPartBlob) ())
           (Some Values.UploadLayerPartResponse.to_json)
           (Some Values.UploadLayerPartResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("batch-check-layer-availability", batch_check_layer_availability);
    ("batch-delete-image", batch_delete_image);
    ("batch-get-image", batch_get_image);
    ("batch-get-repository-scanning-configuration",
      batch_get_repository_scanning_configuration);
    ("complete-layer-upload", complete_layer_upload);
    ("create-pull-through-cache-rule", create_pull_through_cache_rule);
    ("create-repository", create_repository);
    ("delete-lifecycle-policy", delete_lifecycle_policy);
    ("delete-pull-through-cache-rule", delete_pull_through_cache_rule);
    ("delete-registry-policy", delete_registry_policy);
    ("delete-repository", delete_repository);
    ("delete-repository-policy", delete_repository_policy);
    ("describe-image-replication-status", describe_image_replication_status);
    ("describe-image-scan-findings", describe_image_scan_findings);
    ("describe-images", describe_images);
    ("describe-pull-through-cache-rules", describe_pull_through_cache_rules);
    ("describe-registry", describe_registry);
    ("describe-repositories", describe_repositories);
    ("get-authorization-token", get_authorization_token);
    ("get-download-url-for-layer", get_download_url_for_layer);
    ("get-lifecycle-policy", get_lifecycle_policy);
    ("get-lifecycle-policy-preview", get_lifecycle_policy_preview);
    ("get-registry-policy", get_registry_policy);
    ("get-registry-scanning-configuration",
      get_registry_scanning_configuration);
    ("get-repository-policy", get_repository_policy);
    ("initiate-layer-upload", initiate_layer_upload);
    ("list-images", list_images);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-image", put_image);
    ("put-image-scanning-configuration", put_image_scanning_configuration);
    ("put-image-tag-mutability", put_image_tag_mutability);
    ("put-lifecycle-policy", put_lifecycle_policy);
    ("put-registry-policy", put_registry_policy);
    ("put-registry-scanning-configuration",
      put_registry_scanning_configuration);
    ("put-replication-configuration", put_replication_configuration);
    ("set-repository-policy", set_repository_policy);
    ("start-image-scan", start_image_scan);
    ("start-lifecycle-policy-preview", start_lifecycle_policy_preview);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("upload-layer-part", upload_layer_part)]
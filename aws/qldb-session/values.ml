(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.qldb_session
let apiVersion = "2019-07-11"
let endpointPrefix = "session.qldb"
let serviceFullName = "Amazon QLDB Session"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "QLDB Session"
let targetPrefix = "QLDBSession"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module IonBinary =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module IonText =
  struct
    type nonrec t = string
    let context_ = "IonText"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1048576) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IonText" j
    let to_json = simple_to_json to_value
  end
module ValueHolder =
  struct
    type nonrec t =
      {
      ionBinary: IonBinary.t option
        [@ocaml.doc
          "An Amazon Ion binary value contained in a ValueHolder structure."];
      ionText: IonText.t option
        [@ocaml.doc
          "An Amazon Ion plaintext value contained in a ValueHolder structure."]}
    let make ?ionBinary = fun ?ionText -> fun () -> { ionBinary; ionText }
    let to_value x =
      structure_to_value
        [("IonBinary", (Option.map x.ionBinary ~f:IonBinary.to_value));
        ("IonText", (Option.map x.ionText ~f:IonText.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ionText =
        (Option.map ~f:IonText.of_xml) (Xml.child xml_arg0 "IonText") in
      let ionBinary =
        (Option.map ~f:IonBinary.of_xml) (Xml.child xml_arg0 "IonBinary") in
      make ?ionText ?ionBinary ()
    let of_json json =
      let ionText = field_map json "IonText" IonText.of_json in
      let ionBinary = field_map json "IonBinary" IonBinary.of_json in
      make ?ionText ?ionBinary ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that can contain a value in multiple encoding formats."]
module ProcessingTimeMilliseconds =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module ReadIOs =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module WriteIOs =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module PageToken =
  struct
    type nonrec t = string
    let context_ = "PageToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:4) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"^[A-Za-z-0-9+/=]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PageToken" j
    let to_json = simple_to_json to_value
  end
module ValueHolders =
  struct
    type nonrec t = ValueHolder.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ValueHolder.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ValueHolder.of_xml)
    let of_json j =
      list_of_json ~kind:"ValueHolders" ~of_json:ValueHolder.of_json j
    let to_json v = composed_to_json to_value v
  end
module TimingInformation =
  struct
    type nonrec t =
      {
      processingTimeMilliseconds: ProcessingTimeMilliseconds.t option
        [@ocaml.doc
          "The amount of time that QLDB spent on processing the command, measured in milliseconds."]}
    let make ?processingTimeMilliseconds =
      fun () -> { processingTimeMilliseconds }
    let to_value x =
      structure_to_value
        [("ProcessingTimeMilliseconds",
           (Option.map x.processingTimeMilliseconds
              ~f:ProcessingTimeMilliseconds.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let processingTimeMilliseconds =
        (Option.map ~f:ProcessingTimeMilliseconds.of_xml)
          (Xml.child xml_arg0 "ProcessingTimeMilliseconds") in
      make ?processingTimeMilliseconds ()
    let of_json json =
      let processingTimeMilliseconds =
        field_map json "ProcessingTimeMilliseconds"
          ProcessingTimeMilliseconds.of_json in
      make ?processingTimeMilliseconds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains server-side performance information for a command. Amazon QLDB captures timing information between the times when it receives the request and when it sends the corresponding response."]
module ErrorCode =
  struct
    type nonrec t = string
    let context_ = "ErrorCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorCode" j
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module CommitDigest =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module IOUsage =
  struct
    type nonrec t =
      {
      readIOs: ReadIOs.t option
        [@ocaml.doc "The number of read I/O requests that the command made."];
      writeIOs: WriteIOs.t option
        [@ocaml.doc
          "The number of write I/O requests that the command made."]}
    let make ?readIOs = fun ?writeIOs -> fun () -> { readIOs; writeIOs }
    let to_value x =
      structure_to_value
        [("ReadIOs", (Option.map x.readIOs ~f:ReadIOs.to_value));
        ("WriteIOs", (Option.map x.writeIOs ~f:WriteIOs.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let writeIOs =
        (Option.map ~f:WriteIOs.of_xml) (Xml.child xml_arg0 "WriteIOs") in
      let readIOs =
        (Option.map ~f:ReadIOs.of_xml) (Xml.child xml_arg0 "ReadIOs") in
      make ?writeIOs ?readIOs ()
    let of_json json =
      let writeIOs = field_map json "WriteIOs" WriteIOs.of_json in
      let readIOs = field_map json "ReadIOs" ReadIOs.of_json in
      make ?writeIOs ?readIOs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains I/O usage metrics for a command that was invoked."]
module TransactionId =
  struct
    type nonrec t = string
    let context_ = "TransactionId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:22) >>=
             (fun () ->
                (check_string_max i ~max:22) >>=
                  (fun () -> check_pattern i ~pattern:"^[A-Za-z-0-9]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TransactionId" j
    let to_json = simple_to_json to_value
  end
module Page =
  struct
    type nonrec t =
      {
      values: ValueHolders.t option
        [@ocaml.doc
          "A structure that contains values in multiple encoding formats."];
      nextPageToken: PageToken.t option
        [@ocaml.doc "The token of the next page."]}
    let make ?values =
      fun ?nextPageToken -> fun () -> { values; nextPageToken }
    let to_value x =
      structure_to_value
        [("Values", (Option.map x.values ~f:ValueHolders.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let values =
        (Option.map ~f:ValueHolders.of_xml) (Xml.child xml_arg0 "Values") in
      make ?nextPageToken ?values ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let values = field_map json "Values" ValueHolders.of_json in
      make ?nextPageToken ?values ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains details of the fetched page."]
module SessionToken =
  struct
    type nonrec t = string
    let context_ = "SessionToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:4) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"^[A-Za-z-0-9+/=]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SessionToken" j
    let to_json = simple_to_json to_value
  end
module Statement =
  struct
    type nonrec t = string
    let context_ = "Statement"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Statement" j
    let to_json = simple_to_json to_value
  end
module StatementParameters =
  struct
    type nonrec t = ValueHolder.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ValueHolder.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ValueHolder.of_xml)
    let of_json j =
      list_of_json ~kind:"StatementParameters" ~of_json:ValueHolder.of_json j
    let to_json v = composed_to_json to_value v
  end
module LedgerName =
  struct
    type nonrec t = string
    let context_ = "LedgerName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:32) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(?!^.*--)(?!^[0-9]+$)(?!^-)(?!.*-$)^[A-Za-z0-9-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LedgerName" j
    let to_json = simple_to_json to_value
  end
module AbortTransactionResult =
  struct
    type nonrec t =
      {
      timingInformation: TimingInformation.t option
        [@ocaml.doc
          "Contains server-side performance information for the command."]}
    let make ?timingInformation = fun () -> { timingInformation }
    let to_value x =
      structure_to_value
        [("TimingInformation",
           (Option.map x.timingInformation ~f:TimingInformation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timingInformation =
        (Option.map ~f:TimingInformation.of_xml)
          (Xml.child xml_arg0 "TimingInformation") in
      make ?timingInformation ()
    let of_json json =
      let timingInformation =
        field_map json "TimingInformation" TimingInformation.of_json in
      make ?timingInformation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the details of the aborted transaction."]
module BadRequestException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      code: ErrorCode.t option }
    let make ?message = fun ?code -> fun () -> { message; code }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("Code", (Option.map x.code ~f:ErrorCode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let code = (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "Code") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?code ?message ()
    let of_json json =
      let code = field_map json "Code" ErrorCode.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?code ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returned if the request is malformed or contains an error such as an invalid parameter value or a missing required parameter."]
module CapacityExceededException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returned when the request exceeds the processing capacity of the ledger."]
module CommitTransactionResult =
  struct
    type nonrec t =
      {
      transactionId: TransactionId.t option
        [@ocaml.doc "The transaction ID of the committed transaction."];
      commitDigest: CommitDigest.t option
        [@ocaml.doc "The commit digest of the committed transaction."];
      timingInformation: TimingInformation.t option
        [@ocaml.doc
          "Contains server-side performance information for the command."];
      consumedIOs: IOUsage.t option
        [@ocaml.doc
          "Contains metrics about the number of I/O requests that were consumed."]}
    let make ?transactionId =
      fun ?commitDigest ->
        fun ?timingInformation ->
          fun ?consumedIOs ->
            fun () ->
              { transactionId; commitDigest; timingInformation; consumedIOs }
    let to_value x =
      structure_to_value
        [("TransactionId",
           (Option.map x.transactionId ~f:TransactionId.to_value));
        ("CommitDigest",
          (Option.map x.commitDigest ~f:CommitDigest.to_value));
        ("TimingInformation",
          (Option.map x.timingInformation ~f:TimingInformation.to_value));
        ("ConsumedIOs", (Option.map x.consumedIOs ~f:IOUsage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let consumedIOs =
        (Option.map ~f:IOUsage.of_xml) (Xml.child xml_arg0 "ConsumedIOs") in
      let timingInformation =
        (Option.map ~f:TimingInformation.of_xml)
          (Xml.child xml_arg0 "TimingInformation") in
      let commitDigest =
        (Option.map ~f:CommitDigest.of_xml)
          (Xml.child xml_arg0 "CommitDigest") in
      let transactionId =
        (Option.map ~f:TransactionId.of_xml)
          (Xml.child xml_arg0 "TransactionId") in
      make ?consumedIOs ?timingInformation ?commitDigest ?transactionId ()
    let of_json json =
      let consumedIOs = field_map json "ConsumedIOs" IOUsage.of_json in
      let timingInformation =
        field_map json "TimingInformation" TimingInformation.of_json in
      let commitDigest = field_map json "CommitDigest" CommitDigest.of_json in
      let transactionId =
        field_map json "TransactionId" TransactionId.of_json in
      make ?consumedIOs ?timingInformation ?commitDigest ?transactionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the details of the committed transaction."]
module EndSessionResult =
  struct
    type nonrec t =
      {
      timingInformation: TimingInformation.t option
        [@ocaml.doc
          "Contains server-side performance information for the command."]}
    let make ?timingInformation = fun () -> { timingInformation }
    let to_value x =
      structure_to_value
        [("TimingInformation",
           (Option.map x.timingInformation ~f:TimingInformation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timingInformation =
        (Option.map ~f:TimingInformation.of_xml)
          (Xml.child xml_arg0 "TimingInformation") in
      make ?timingInformation ()
    let of_json json =
      let timingInformation =
        field_map json "TimingInformation" TimingInformation.of_json in
      make ?timingInformation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the details of the ended session."]
module ExecuteStatementResult =
  struct
    type nonrec t =
      {
      firstPage: Page.t option
        [@ocaml.doc "Contains the details of the first fetched page."];
      timingInformation: TimingInformation.t option
        [@ocaml.doc
          "Contains server-side performance information for the command."];
      consumedIOs: IOUsage.t option
        [@ocaml.doc
          "Contains metrics about the number of I/O requests that were consumed."]}
    let make ?firstPage =
      fun ?timingInformation ->
        fun ?consumedIOs ->
          fun () -> { firstPage; timingInformation; consumedIOs }
    let to_value x =
      structure_to_value
        [("FirstPage", (Option.map x.firstPage ~f:Page.to_value));
        ("TimingInformation",
          (Option.map x.timingInformation ~f:TimingInformation.to_value));
        ("ConsumedIOs", (Option.map x.consumedIOs ~f:IOUsage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let consumedIOs =
        (Option.map ~f:IOUsage.of_xml) (Xml.child xml_arg0 "ConsumedIOs") in
      let timingInformation =
        (Option.map ~f:TimingInformation.of_xml)
          (Xml.child xml_arg0 "TimingInformation") in
      let firstPage =
        (Option.map ~f:Page.of_xml) (Xml.child xml_arg0 "FirstPage") in
      make ?consumedIOs ?timingInformation ?firstPage ()
    let of_json json =
      let consumedIOs = field_map json "ConsumedIOs" IOUsage.of_json in
      let timingInformation =
        field_map json "TimingInformation" TimingInformation.of_json in
      let firstPage = field_map json "FirstPage" Page.of_json in
      make ?consumedIOs ?timingInformation ?firstPage ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the details of the executed statement."]
module FetchPageResult =
  struct
    type nonrec t =
      {
      page: Page.t option
        [@ocaml.doc "Contains details of the fetched page."];
      timingInformation: TimingInformation.t option
        [@ocaml.doc
          "Contains server-side performance information for the command."];
      consumedIOs: IOUsage.t option
        [@ocaml.doc
          "Contains metrics about the number of I/O requests that were consumed."]}
    let make ?page =
      fun ?timingInformation ->
        fun ?consumedIOs ->
          fun () -> { page; timingInformation; consumedIOs }
    let to_value x =
      structure_to_value
        [("Page", (Option.map x.page ~f:Page.to_value));
        ("TimingInformation",
          (Option.map x.timingInformation ~f:TimingInformation.to_value));
        ("ConsumedIOs", (Option.map x.consumedIOs ~f:IOUsage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let consumedIOs =
        (Option.map ~f:IOUsage.of_xml) (Xml.child xml_arg0 "ConsumedIOs") in
      let timingInformation =
        (Option.map ~f:TimingInformation.of_xml)
          (Xml.child xml_arg0 "TimingInformation") in
      let page = (Option.map ~f:Page.of_xml) (Xml.child xml_arg0 "Page") in
      make ?consumedIOs ?timingInformation ?page ()
    let of_json json =
      let consumedIOs = field_map json "ConsumedIOs" IOUsage.of_json in
      let timingInformation =
        field_map json "TimingInformation" TimingInformation.of_json in
      let page = field_map json "Page" Page.of_json in
      make ?consumedIOs ?timingInformation ?page ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the page that was fetched."]
module InvalidSessionException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      code: ErrorCode.t option }
    let make ?message = fun ?code -> fun () -> { message; code }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("Code", (Option.map x.code ~f:ErrorCode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let code = (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "Code") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?code ?message ()
    let of_json json =
      let code = field_map json "Code" ErrorCode.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?code ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returned if the session doesn't exist anymore because it timed out or expired."]
module LimitExceededException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returned if a resource limit such as number of active sessions is exceeded."]
module OccConflictException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returned when a transaction cannot be written to the journal due to a failure in the verification phase of optimistic concurrency control (OCC)."]
module RateExceededException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returned when the rate of requests exceeds the allowed throughput."]
module StartSessionResult =
  struct
    type nonrec t =
      {
      sessionToken: SessionToken.t option
        [@ocaml.doc
          "Session token of the started session. This SessionToken is required for every subsequent command that is issued during the current session."];
      timingInformation: TimingInformation.t option
        [@ocaml.doc
          "Contains server-side performance information for the command."]}
    let make ?sessionToken =
      fun ?timingInformation -> fun () -> { sessionToken; timingInformation }
    let to_value x =
      structure_to_value
        [("SessionToken",
           (Option.map x.sessionToken ~f:SessionToken.to_value));
        ("TimingInformation",
          (Option.map x.timingInformation ~f:TimingInformation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timingInformation =
        (Option.map ~f:TimingInformation.of_xml)
          (Xml.child xml_arg0 "TimingInformation") in
      let sessionToken =
        (Option.map ~f:SessionToken.of_xml)
          (Xml.child xml_arg0 "SessionToken") in
      make ?timingInformation ?sessionToken ()
    let of_json json =
      let timingInformation =
        field_map json "TimingInformation" TimingInformation.of_json in
      let sessionToken = field_map json "SessionToken" SessionToken.of_json in
      make ?timingInformation ?sessionToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the details of the started session."]
module StartTransactionResult =
  struct
    type nonrec t =
      {
      transactionId: TransactionId.t option
        [@ocaml.doc "The transaction ID of the started transaction."];
      timingInformation: TimingInformation.t option
        [@ocaml.doc
          "Contains server-side performance information for the command."]}
    let make ?transactionId =
      fun ?timingInformation ->
        fun () -> { transactionId; timingInformation }
    let to_value x =
      structure_to_value
        [("TransactionId",
           (Option.map x.transactionId ~f:TransactionId.to_value));
        ("TimingInformation",
          (Option.map x.timingInformation ~f:TimingInformation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timingInformation =
        (Option.map ~f:TimingInformation.of_xml)
          (Xml.child xml_arg0 "TimingInformation") in
      let transactionId =
        (Option.map ~f:TransactionId.of_xml)
          (Xml.child xml_arg0 "TransactionId") in
      make ?timingInformation ?transactionId ()
    let of_json json =
      let timingInformation =
        field_map json "TimingInformation" TimingInformation.of_json in
      let transactionId =
        field_map json "TransactionId" TransactionId.of_json in
      make ?timingInformation ?transactionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the details of the started transaction."]
module AbortTransactionRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the details of the transaction to abort."]
module CommitTransactionRequest =
  struct
    type nonrec t =
      {
      transactionId: TransactionId.t
        [@ocaml.doc
          "Specifies the transaction ID of the transaction to commit."];
      commitDigest: CommitDigest.t
        [@ocaml.doc
          "Specifies the commit digest for the transaction to commit. For every active transaction, the commit digest must be passed. QLDB validates CommitDigest and rejects the commit with an error if the digest computed on the client does not match the digest computed by QLDB. The purpose of the CommitDigest parameter is to ensure that QLDB commits a transaction if and only if the server has processed the exact set of statements sent by the client, in the same order that client sent them, and with no duplicates."]}
    let context_ = "CommitTransactionRequest"
    let make ~transactionId =
      fun ~commitDigest -> fun () -> { transactionId; commitDigest }
    let to_value x =
      structure_to_value
        [("TransactionId", (Some (TransactionId.to_value x.transactionId)));
        ("CommitDigest", (Some (CommitDigest.to_value x.commitDigest)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let commitDigest =
        CommitDigest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CommitDigest") in
      let transactionId =
        TransactionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransactionId") in
      make ~commitDigest ~transactionId ()
    let of_json json =
      let commitDigest =
        field_map_exn json "CommitDigest" CommitDigest.of_json in
      let transactionId =
        field_map_exn json "TransactionId" TransactionId.of_json in
      make ~commitDigest ~transactionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the details of the transaction to commit."]
module EndSessionRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies a request to end the session."]
module ExecuteStatementRequest =
  struct
    type nonrec t =
      {
      transactionId: TransactionId.t
        [@ocaml.doc "Specifies the transaction ID of the request."];
      statement: Statement.t
        [@ocaml.doc "Specifies the statement of the request."];
      parameters: StatementParameters.t option
        [@ocaml.doc
          "Specifies the parameters for the parameterized statement in the request."]}
    let context_ = "ExecuteStatementRequest"
    let make ?parameters =
      fun ~transactionId ->
        fun ~statement -> fun () -> { parameters; transactionId; statement }
    let to_value x =
      structure_to_value
        [("TransactionId", (Some (TransactionId.to_value x.transactionId)));
        ("Statement", (Some (Statement.to_value x.statement)));
        ("Parameters",
          (Option.map x.parameters ~f:StatementParameters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameters =
        (Option.map ~f:StatementParameters.of_xml)
          (Xml.child xml_arg0 "Parameters") in
      let statement =
        Statement.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Statement") in
      let transactionId =
        TransactionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransactionId") in
      make ?parameters ~statement ~transactionId ()
    let of_json json =
      let parameters =
        field_map json "Parameters" StatementParameters.of_json in
      let statement = field_map_exn json "Statement" Statement.of_json in
      let transactionId =
        field_map_exn json "TransactionId" TransactionId.of_json in
      make ?parameters ~statement ~transactionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies a request to execute a statement."]
module FetchPageRequest =
  struct
    type nonrec t =
      {
      transactionId: TransactionId.t
        [@ocaml.doc
          "Specifies the transaction ID of the page to be fetched."];
      nextPageToken: PageToken.t
        [@ocaml.doc
          "Specifies the next page token of the page to be fetched."]}
    let context_ = "FetchPageRequest"
    let make ~transactionId =
      fun ~nextPageToken -> fun () -> { transactionId; nextPageToken }
    let to_value x =
      structure_to_value
        [("TransactionId", (Some (TransactionId.to_value x.transactionId)));
        ("NextPageToken", (Some (PageToken.to_value x.nextPageToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        PageToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NextPageToken") in
      let transactionId =
        TransactionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TransactionId") in
      make ~nextPageToken ~transactionId ()
    let of_json json =
      let nextPageToken =
        field_map_exn json "NextPageToken" PageToken.of_json in
      let transactionId =
        field_map_exn json "TransactionId" TransactionId.of_json in
      make ~nextPageToken ~transactionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies the details of the page to be fetched."]
module StartSessionRequest =
  struct
    type nonrec t =
      {
      ledgerName: LedgerName.t
        [@ocaml.doc "The name of the ledger to start a new session against."]}
    let context_ = "StartSessionRequest"
    let make ~ledgerName = fun () -> { ledgerName }
    let to_value x =
      structure_to_value
        [("LedgerName", (Some (LedgerName.to_value x.ledgerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ledgerName =
        LedgerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LedgerName") in
      make ~ledgerName ()
    let of_json json =
      let ledgerName = field_map_exn json "LedgerName" LedgerName.of_json in
      make ~ledgerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies a request to start a new session."]
module StartTransactionRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies a request to start a transaction."]
module SendCommandResult =
  struct
    type nonrec t =
      {
      startSession: StartSessionResult.t option
        [@ocaml.doc
          "Contains the details of the started session that includes a session token. This SessionToken is required for every subsequent command that is issued during the current session."];
      startTransaction: StartTransactionResult.t option
        [@ocaml.doc "Contains the details of the started transaction."];
      endSession: EndSessionResult.t option
        [@ocaml.doc "Contains the details of the ended session."];
      commitTransaction: CommitTransactionResult.t option
        [@ocaml.doc "Contains the details of the committed transaction."];
      abortTransaction: AbortTransactionResult.t option
        [@ocaml.doc "Contains the details of the aborted transaction."];
      executeStatement: ExecuteStatementResult.t option
        [@ocaml.doc "Contains the details of the executed statement."];
      fetchPage: FetchPageResult.t option
        [@ocaml.doc "Contains the details of the fetched page."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `CapacityExceededException of CapacityExceededException.t 
      | `InvalidSessionException of InvalidSessionException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `OccConflictException of OccConflictException.t 
      | `RateExceededException of RateExceededException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?startSession =
      fun ?startTransaction ->
        fun ?endSession ->
          fun ?commitTransaction ->
            fun ?abortTransaction ->
              fun ?executeStatement ->
                fun ?fetchPage ->
                  fun () ->
                    {
                      startSession;
                      startTransaction;
                      endSession;
                      commitTransaction;
                      abortTransaction;
                      executeStatement;
                      fetchPage
                    }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "CapacityExceededException" ->
          `CapacityExceededException (CapacityExceededException.of_json json)
      | "InvalidSessionException" ->
          `InvalidSessionException (InvalidSessionException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "OccConflictException" ->
          `OccConflictException (OccConflictException.of_json json)
      | "RateExceededException" ->
          `RateExceededException (RateExceededException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "CapacityExceededException" ->
          `CapacityExceededException (CapacityExceededException.of_xml xml)
      | "InvalidSessionException" ->
          `InvalidSessionException (InvalidSessionException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "OccConflictException" ->
          `OccConflictException (OccConflictException.of_xml xml)
      | "RateExceededException" ->
          `RateExceededException (RateExceededException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `CapacityExceededException e ->
          `Assoc
            [("error", (`String "CapacityExceededException"));
            ("details", (CapacityExceededException.to_json e))]
      | `InvalidSessionException e ->
          `Assoc
            [("error", (`String "InvalidSessionException"));
            ("details", (InvalidSessionException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `OccConflictException e ->
          `Assoc
            [("error", (`String "OccConflictException"));
            ("details", (OccConflictException.to_json e))]
      | `RateExceededException e ->
          `Assoc
            [("error", (`String "RateExceededException"));
            ("details", (RateExceededException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("StartSession",
           (Option.map x.startSession ~f:StartSessionResult.to_value));
        ("StartTransaction",
          (Option.map x.startTransaction ~f:StartTransactionResult.to_value));
        ("EndSession",
          (Option.map x.endSession ~f:EndSessionResult.to_value));
        ("CommitTransaction",
          (Option.map x.commitTransaction ~f:CommitTransactionResult.to_value));
        ("AbortTransaction",
          (Option.map x.abortTransaction ~f:AbortTransactionResult.to_value));
        ("ExecuteStatement",
          (Option.map x.executeStatement ~f:ExecuteStatementResult.to_value));
        ("FetchPage", (Option.map x.fetchPage ~f:FetchPageResult.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fetchPage =
        (Option.map ~f:FetchPageResult.of_xml)
          (Xml.child xml_arg0 "FetchPage") in
      let executeStatement =
        (Option.map ~f:ExecuteStatementResult.of_xml)
          (Xml.child xml_arg0 "ExecuteStatement") in
      let abortTransaction =
        (Option.map ~f:AbortTransactionResult.of_xml)
          (Xml.child xml_arg0 "AbortTransaction") in
      let commitTransaction =
        (Option.map ~f:CommitTransactionResult.of_xml)
          (Xml.child xml_arg0 "CommitTransaction") in
      let endSession =
        (Option.map ~f:EndSessionResult.of_xml)
          (Xml.child xml_arg0 "EndSession") in
      let startTransaction =
        (Option.map ~f:StartTransactionResult.of_xml)
          (Xml.child xml_arg0 "StartTransaction") in
      let startSession =
        (Option.map ~f:StartSessionResult.of_xml)
          (Xml.child xml_arg0 "StartSession") in
      make ?fetchPage ?executeStatement ?abortTransaction ?commitTransaction
        ?endSession ?startTransaction ?startSession ()
    let of_json json =
      let fetchPage = field_map json "FetchPage" FetchPageResult.of_json in
      let executeStatement =
        field_map json "ExecuteStatement" ExecuteStatementResult.of_json in
      let abortTransaction =
        field_map json "AbortTransaction" AbortTransactionResult.of_json in
      let commitTransaction =
        field_map json "CommitTransaction" CommitTransactionResult.of_json in
      let endSession = field_map json "EndSession" EndSessionResult.of_json in
      let startTransaction =
        field_map json "StartTransaction" StartTransactionResult.of_json in
      let startSession =
        field_map json "StartSession" StartSessionResult.of_json in
      make ?fetchPage ?executeStatement ?abortTransaction ?commitTransaction
        ?endSession ?startTransaction ?startSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends a command to an Amazon QLDB ledger. Instead of interacting directly with this API, we recommend using the QLDB driver or the QLDB shell to execute data transactions on a ledger. If you are working with an AWS SDK, use the QLDB driver. The driver provides a high-level abstraction layer above this QLDB Session data plane and manages SendCommand API calls for you. For information and a list of supported programming languages, see Getting started with the driver in the Amazon QLDB Developer Guide. If you are working with the AWS Command Line Interface (AWS CLI), use the QLDB shell. The shell is a command line interface that uses the QLDB driver to interact with a ledger. For information, see Accessing Amazon QLDB using the QLDB shell."]
module SendCommandRequest =
  struct
    type nonrec t =
      {
      sessionToken: SessionToken.t option
        [@ocaml.doc
          "Specifies the session token for the current command. A session token is constant throughout the life of the session. To obtain a session token, run the StartSession command. This SessionToken is required for every subsequent command that is issued during the current session."];
      startSession: StartSessionRequest.t option
        [@ocaml.doc
          "Command to start a new session. A session token is obtained as part of the response."];
      startTransaction: StartTransactionRequest.t option
        [@ocaml.doc "Command to start a new transaction."];
      endSession: EndSessionRequest.t option
        [@ocaml.doc "Command to end the current session."];
      commitTransaction: CommitTransactionRequest.t option
        [@ocaml.doc "Command to commit the specified transaction."];
      abortTransaction: AbortTransactionRequest.t option
        [@ocaml.doc "Command to abort the current transaction."];
      executeStatement: ExecuteStatementRequest.t option
        [@ocaml.doc
          "Command to execute a statement in the specified transaction."];
      fetchPage: FetchPageRequest.t option
        [@ocaml.doc "Command to fetch a page."]}
    let make ?sessionToken =
      fun ?startSession ->
        fun ?startTransaction ->
          fun ?endSession ->
            fun ?commitTransaction ->
              fun ?abortTransaction ->
                fun ?executeStatement ->
                  fun ?fetchPage ->
                    fun () ->
                      {
                        sessionToken;
                        startSession;
                        startTransaction;
                        endSession;
                        commitTransaction;
                        abortTransaction;
                        executeStatement;
                        fetchPage
                      }
    let to_value x =
      structure_to_value
        [("SessionToken",
           (Option.map x.sessionToken ~f:SessionToken.to_value));
        ("StartSession",
          (Option.map x.startSession ~f:StartSessionRequest.to_value));
        ("StartTransaction",
          (Option.map x.startTransaction ~f:StartTransactionRequest.to_value));
        ("EndSession",
          (Option.map x.endSession ~f:EndSessionRequest.to_value));
        ("CommitTransaction",
          (Option.map x.commitTransaction
             ~f:CommitTransactionRequest.to_value));
        ("AbortTransaction",
          (Option.map x.abortTransaction ~f:AbortTransactionRequest.to_value));
        ("ExecuteStatement",
          (Option.map x.executeStatement ~f:ExecuteStatementRequest.to_value));
        ("FetchPage", (Option.map x.fetchPage ~f:FetchPageRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fetchPage =
        (Option.map ~f:FetchPageRequest.of_xml)
          (Xml.child xml_arg0 "FetchPage") in
      let executeStatement =
        (Option.map ~f:ExecuteStatementRequest.of_xml)
          (Xml.child xml_arg0 "ExecuteStatement") in
      let abortTransaction =
        (Option.map ~f:AbortTransactionRequest.of_xml)
          (Xml.child xml_arg0 "AbortTransaction") in
      let commitTransaction =
        (Option.map ~f:CommitTransactionRequest.of_xml)
          (Xml.child xml_arg0 "CommitTransaction") in
      let endSession =
        (Option.map ~f:EndSessionRequest.of_xml)
          (Xml.child xml_arg0 "EndSession") in
      let startTransaction =
        (Option.map ~f:StartTransactionRequest.of_xml)
          (Xml.child xml_arg0 "StartTransaction") in
      let startSession =
        (Option.map ~f:StartSessionRequest.of_xml)
          (Xml.child xml_arg0 "StartSession") in
      let sessionToken =
        (Option.map ~f:SessionToken.of_xml)
          (Xml.child xml_arg0 "SessionToken") in
      make ?fetchPage ?executeStatement ?abortTransaction ?commitTransaction
        ?endSession ?startTransaction ?startSession ?sessionToken ()
    let of_json json =
      let fetchPage = field_map json "FetchPage" FetchPageRequest.of_json in
      let executeStatement =
        field_map json "ExecuteStatement" ExecuteStatementRequest.of_json in
      let abortTransaction =
        field_map json "AbortTransaction" AbortTransactionRequest.of_json in
      let commitTransaction =
        field_map json "CommitTransaction" CommitTransactionRequest.of_json in
      let endSession = field_map json "EndSession" EndSessionRequest.of_json in
      let startTransaction =
        field_map json "StartTransaction" StartTransactionRequest.of_json in
      let startSession =
        field_map json "StartSession" StartSessionRequest.of_json in
      let sessionToken = field_map json "SessionToken" SessionToken.of_json in
      make ?fetchPage ?executeStatement ?abortTransaction ?commitTransaction
        ?endSession ?startTransaction ?startSession ?sessionToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends a command to an Amazon QLDB ledger. Instead of interacting directly with this API, we recommend using the QLDB driver or the QLDB shell to execute data transactions on a ledger. If you are working with an AWS SDK, use the QLDB driver. The driver provides a high-level abstraction layer above this QLDB Session data plane and manages SendCommand API calls for you. For information and a list of supported programming languages, see Getting started with the driver in the Amazon QLDB Developer Guide. If you are working with the AWS Command Line Interface (AWS CLI), use the QLDB shell. The shell is a command line interface that uses the QLDB driver to interact with a ledger. For information, see Accessing Amazon QLDB using the QLDB shell."]
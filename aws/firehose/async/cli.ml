(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_delivery_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deliveryStreamType =
         flag "delivery-stream-type" (optional json_arg)
           ~doc:"JSON DeliveryStreamType"
       and kinesisStreamSourceConfiguration =
         flag "kinesis-stream-source-configuration" (optional json_arg)
           ~doc:"JSON KinesisStreamSourceConfiguration"
       and deliveryStreamEncryptionConfigurationInput =
         flag "delivery-stream-encryption-configuration-input"
           (optional json_arg)
           ~doc:"JSON DeliveryStreamEncryptionConfigurationInput"
       and s3DestinationConfiguration =
         flag "s3-destination-configuration" (optional json_arg)
           ~doc:"JSON S3DestinationConfiguration"
       and extendedS3DestinationConfiguration =
         flag "extended-s3-destination-configuration" (optional json_arg)
           ~doc:"JSON ExtendedS3DestinationConfiguration"
       and redshiftDestinationConfiguration =
         flag "redshift-destination-configuration" (optional json_arg)
           ~doc:"JSON RedshiftDestinationConfiguration"
       and elasticsearchDestinationConfiguration =
         flag "elasticsearch-destination-configuration" (optional json_arg)
           ~doc:"JSON ElasticsearchDestinationConfiguration"
       and amazonopensearchserviceDestinationConfiguration =
         flag "amazonopensearchservice-destination-configuration"
           (optional json_arg)
           ~doc:"JSON AmazonopensearchserviceDestinationConfiguration"
       and splunkDestinationConfiguration =
         flag "splunk-destination-configuration" (optional json_arg)
           ~doc:"JSON SplunkDestinationConfiguration"
       and httpEndpointDestinationConfiguration =
         flag "http-endpoint-destination-configuration" (optional json_arg)
           ~doc:"JSON HttpEndpointDestinationConfiguration"
       and tags =
         flag "tags" (optional json_arg)
           ~doc:"JSON TagDeliveryStreamInputTagList"
       and deliveryStreamName =
         flag "delivery-stream-name" (required string)
           ~doc:"STRING DeliveryStreamName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_delivery_stream
           (Values.CreateDeliveryStreamInput.make
              ?deliveryStreamType:(Option.map
                                     ~f:Values.DeliveryStreamType.of_json
                                     deliveryStreamType)
              ?kinesisStreamSourceConfiguration:(Option.map
                                                   ~f:Values.KinesisStreamSourceConfiguration.of_json
                                                   kinesisStreamSourceConfiguration)
              ?deliveryStreamEncryptionConfigurationInput:(Option.map
                                                             ~f:Values.DeliveryStreamEncryptionConfigurationInput.of_json
                                                             deliveryStreamEncryptionConfigurationInput)
              ?s3DestinationConfiguration:(Option.map
                                             ~f:Values.S3DestinationConfiguration.of_json
                                             s3DestinationConfiguration)
              ?extendedS3DestinationConfiguration:(Option.map
                                                     ~f:Values.ExtendedS3DestinationConfiguration.of_json
                                                     extendedS3DestinationConfiguration)
              ?redshiftDestinationConfiguration:(Option.map
                                                   ~f:Values.RedshiftDestinationConfiguration.of_json
                                                   redshiftDestinationConfiguration)
              ?elasticsearchDestinationConfiguration:(Option.map
                                                        ~f:Values.ElasticsearchDestinationConfiguration.of_json
                                                        elasticsearchDestinationConfiguration)
              ?amazonopensearchserviceDestinationConfiguration:(Option.map
                                                                  ~f:Values.AmazonopensearchserviceDestinationConfiguration.of_json
                                                                  amazonopensearchserviceDestinationConfiguration)
              ?splunkDestinationConfiguration:(Option.map
                                                 ~f:Values.SplunkDestinationConfiguration.of_json
                                                 splunkDestinationConfiguration)
              ?httpEndpointDestinationConfiguration:(Option.map
                                                       ~f:Values.HttpEndpointDestinationConfiguration.of_json
                                                       httpEndpointDestinationConfiguration)
              ?tags:(Option.map
                       ~f:Values.TagDeliveryStreamInputTagList.of_json tags)
              ~deliveryStreamName ())
           (Some Values.CreateDeliveryStreamOutput.to_json)
           (Some Values.CreateDeliveryStreamOutput.error_to_json)])
let delete_delivery_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allowForceDelete =
         flag "allow-force-delete" (optional bool) ~doc:"BOOL BooleanObject"
       and deliveryStreamName =
         flag "delivery-stream-name" (required string)
           ~doc:"STRING DeliveryStreamName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_delivery_stream
           (Values.DeleteDeliveryStreamInput.make ?allowForceDelete
              ~deliveryStreamName ())
           (Some Values.DeleteDeliveryStreamOutput.to_json)
           (Some Values.DeleteDeliveryStreamOutput.error_to_json)])
let describe_delivery_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit =
         flag "limit" (optional int)
           ~doc:"INT DescribeDeliveryStreamInputLimit"
       and exclusiveStartDestinationId =
         flag "exclusive-start-destination-id" (optional string)
           ~doc:"STRING DestinationId"
       and deliveryStreamName =
         flag "delivery-stream-name" (required string)
           ~doc:"STRING DeliveryStreamName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_delivery_stream
           (Values.DescribeDeliveryStreamInput.make ?limit
              ?exclusiveStartDestinationId ~deliveryStreamName ())
           (Some Values.DescribeDeliveryStreamOutput.to_json)
           (Some Values.DescribeDeliveryStreamOutput.error_to_json)])
let list_delivery_streams =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit =
         flag "limit" (optional int) ~doc:"INT ListDeliveryStreamsInputLimit"
       and deliveryStreamType =
         flag "delivery-stream-type" (optional json_arg)
           ~doc:"JSON DeliveryStreamType"
       and exclusiveStartDeliveryStreamName =
         flag "exclusive-start-delivery-stream-name" (optional string)
           ~doc:"STRING DeliveryStreamName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_delivery_streams
           (Values.ListDeliveryStreamsInput.make ?limit
              ?deliveryStreamType:(Option.map
                                     ~f:Values.DeliveryStreamType.of_json
                                     deliveryStreamType)
              ?exclusiveStartDeliveryStreamName ())
           (Some Values.ListDeliveryStreamsOutput.to_json)
           (Some Values.ListDeliveryStreamsOutput.error_to_json)])
let list_tags_for_delivery_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exclusiveStartTagKey =
         flag "exclusive-start-tag-key" (optional string)
           ~doc:"STRING TagKey"
       and limit =
         flag "limit" (optional int)
           ~doc:"INT ListTagsForDeliveryStreamInputLimit"
       and deliveryStreamName =
         flag "delivery-stream-name" (required string)
           ~doc:"STRING DeliveryStreamName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_delivery_stream
           (Values.ListTagsForDeliveryStreamInput.make ?exclusiveStartTagKey
              ?limit ~deliveryStreamName ())
           (Some Values.ListTagsForDeliveryStreamOutput.to_json)
           (Some Values.ListTagsForDeliveryStreamOutput.error_to_json)])
let put_record =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deliveryStreamName =
         flag "delivery-stream-name" (required string)
           ~doc:"STRING DeliveryStreamName"
       and record = flag "record" (required json_arg) ~doc:"JSON Record" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_record
           (Values.PutRecordInput.make ~deliveryStreamName
              ~record:(Values.Record.of_json record) ())
           (Some Values.PutRecordOutput.to_json)
           (Some Values.PutRecordOutput.error_to_json)])
let put_record_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deliveryStreamName =
         flag "delivery-stream-name" (required string)
           ~doc:"STRING DeliveryStreamName"
       and records =
         flag "records" (required json_arg)
           ~doc:"JSON PutRecordBatchRequestEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_record_batch
           (Values.PutRecordBatchInput.make ~deliveryStreamName
              ~records:(Values.PutRecordBatchRequestEntryList.of_json records)
              ()) (Some Values.PutRecordBatchOutput.to_json)
           (Some Values.PutRecordBatchOutput.error_to_json)])
let start_delivery_stream_encryption =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deliveryStreamEncryptionConfigurationInput =
         flag "delivery-stream-encryption-configuration-input"
           (optional json_arg)
           ~doc:"JSON DeliveryStreamEncryptionConfigurationInput"
       and deliveryStreamName =
         flag "delivery-stream-name" (required string)
           ~doc:"STRING DeliveryStreamName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_delivery_stream_encryption
           (Values.StartDeliveryStreamEncryptionInput.make
              ?deliveryStreamEncryptionConfigurationInput:(Option.map
                                                             ~f:Values.DeliveryStreamEncryptionConfigurationInput.of_json
                                                             deliveryStreamEncryptionConfigurationInput)
              ~deliveryStreamName ())
           (Some Values.StartDeliveryStreamEncryptionOutput.to_json)
           (Some Values.StartDeliveryStreamEncryptionOutput.error_to_json)])
let stop_delivery_stream_encryption =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deliveryStreamName =
         flag "delivery-stream-name" (required string)
           ~doc:"STRING DeliveryStreamName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_delivery_stream_encryption
           (Values.StopDeliveryStreamEncryptionInput.make ~deliveryStreamName
              ()) (Some Values.StopDeliveryStreamEncryptionOutput.to_json)
           (Some Values.StopDeliveryStreamEncryptionOutput.error_to_json)])
let tag_delivery_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deliveryStreamName =
         flag "delivery-stream-name" (required string)
           ~doc:"STRING DeliveryStreamName"
       and tags =
         flag "tags" (required json_arg)
           ~doc:"JSON TagDeliveryStreamInputTagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_delivery_stream
           (Values.TagDeliveryStreamInput.make ~deliveryStreamName
              ~tags:(Values.TagDeliveryStreamInputTagList.of_json tags) ())
           (Some Values.TagDeliveryStreamOutput.to_json)
           (Some Values.TagDeliveryStreamOutput.error_to_json)])
let untag_delivery_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deliveryStreamName =
         flag "delivery-stream-name" (required string)
           ~doc:"STRING DeliveryStreamName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_delivery_stream
           (Values.UntagDeliveryStreamInput.make ~deliveryStreamName
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagDeliveryStreamOutput.to_json)
           (Some Values.UntagDeliveryStreamOutput.error_to_json)])
let update_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3DestinationUpdate =
         flag "s3-destination-update" (optional json_arg)
           ~doc:"JSON S3DestinationUpdate"
       and extendedS3DestinationUpdate =
         flag "extended-s3-destination-update" (optional json_arg)
           ~doc:"JSON ExtendedS3DestinationUpdate"
       and redshiftDestinationUpdate =
         flag "redshift-destination-update" (optional json_arg)
           ~doc:"JSON RedshiftDestinationUpdate"
       and elasticsearchDestinationUpdate =
         flag "elasticsearch-destination-update" (optional json_arg)
           ~doc:"JSON ElasticsearchDestinationUpdate"
       and amazonopensearchserviceDestinationUpdate =
         flag "amazonopensearchservice-destination-update"
           (optional json_arg)
           ~doc:"JSON AmazonopensearchserviceDestinationUpdate"
       and splunkDestinationUpdate =
         flag "splunk-destination-update" (optional json_arg)
           ~doc:"JSON SplunkDestinationUpdate"
       and httpEndpointDestinationUpdate =
         flag "http-endpoint-destination-update" (optional json_arg)
           ~doc:"JSON HttpEndpointDestinationUpdate"
       and deliveryStreamName =
         flag "delivery-stream-name" (required string)
           ~doc:"STRING DeliveryStreamName"
       and currentDeliveryStreamVersionId =
         flag "current-delivery-stream-version-id" (required string)
           ~doc:"STRING DeliveryStreamVersionId"
       and destinationId =
         flag "destination-id" (required string) ~doc:"STRING DestinationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_destination
           (Values.UpdateDestinationInput.make
              ?s3DestinationUpdate:(Option.map
                                      ~f:Values.S3DestinationUpdate.of_json
                                      s3DestinationUpdate)
              ?extendedS3DestinationUpdate:(Option.map
                                              ~f:Values.ExtendedS3DestinationUpdate.of_json
                                              extendedS3DestinationUpdate)
              ?redshiftDestinationUpdate:(Option.map
                                            ~f:Values.RedshiftDestinationUpdate.of_json
                                            redshiftDestinationUpdate)
              ?elasticsearchDestinationUpdate:(Option.map
                                                 ~f:Values.ElasticsearchDestinationUpdate.of_json
                                                 elasticsearchDestinationUpdate)
              ?amazonopensearchserviceDestinationUpdate:(Option.map
                                                           ~f:Values.AmazonopensearchserviceDestinationUpdate.of_json
                                                           amazonopensearchserviceDestinationUpdate)
              ?splunkDestinationUpdate:(Option.map
                                          ~f:Values.SplunkDestinationUpdate.of_json
                                          splunkDestinationUpdate)
              ?httpEndpointDestinationUpdate:(Option.map
                                                ~f:Values.HttpEndpointDestinationUpdate.of_json
                                                httpEndpointDestinationUpdate)
              ~deliveryStreamName ~currentDeliveryStreamVersionId
              ~destinationId ())
           (Some Values.UpdateDestinationOutput.to_json)
           (Some Values.UpdateDestinationOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-delivery-stream", create_delivery_stream);
    ("delete-delivery-stream", delete_delivery_stream);
    ("describe-delivery-stream", describe_delivery_stream);
    ("list-delivery-streams", list_delivery_streams);
    ("list-tags-for-delivery-stream", list_tags_for_delivery_stream);
    ("put-record", put_record);
    ("put-record-batch", put_record_batch);
    ("start-delivery-stream-encryption", start_delivery_stream_encryption);
    ("stop-delivery-stream-encryption", stop_delivery_stream_encryption);
    ("tag-delivery-stream", tag_delivery_stream);
    ("untag-delivery-stream", untag_delivery_stream);
    ("update-destination", update_destination)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_get_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ids = flag "ids" (optional json_arg) ~doc:"JSON CollectionIds"
       and names =
         flag "names" (optional json_arg) ~doc:"JSON CollectionNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_collection
           (Values.BatchGetCollectionRequest.make
              ?ids:(Option.map ~f:Values.CollectionIds.of_json ids)
              ?names:(Option.map ~f:Values.CollectionNames.of_json names) ())
           (Some Values.BatchGetCollectionResponse.to_json)
           (Some Values.BatchGetCollectionResponse.error_to_json)])
let batch_get_collection_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ids =
         flag "ids" (optional json_arg) ~doc:"JSON CollectionGroupIds"
       and names =
         flag "names" (optional json_arg) ~doc:"JSON CollectionGroupNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_collection_group
           (Values.BatchGetCollectionGroupRequest.make
              ?ids:(Option.map ~f:Values.CollectionGroupIds.of_json ids)
              ?names:(Option.map ~f:Values.CollectionGroupNames.of_json names)
              ()) (Some Values.BatchGetCollectionGroupResponse.to_json)
           (Some Values.BatchGetCollectionGroupResponse.error_to_json)])
let batch_get_effective_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifiers =
         flag "resource-identifiers" (required json_arg)
           ~doc:"JSON LifecyclePolicyResourceIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_effective_lifecycle_policy
           (Values.BatchGetEffectiveLifecyclePolicyRequest.make
              ~resourceIdentifiers:(Values.LifecyclePolicyResourceIdentifiers.of_json
                                      resourceIdentifiers) ())
           (Some Values.BatchGetEffectiveLifecyclePolicyResponse.to_json)
           (Some
              Values.BatchGetEffectiveLifecyclePolicyResponse.error_to_json)])
let batch_get_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifiers =
         flag "identifiers" (required json_arg)
           ~doc:"JSON LifecyclePolicyIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_lifecycle_policy
           (Values.BatchGetLifecyclePolicyRequest.make
              ~identifiers:(Values.LifecyclePolicyIdentifiers.of_json
                              identifiers) ())
           (Some Values.BatchGetLifecyclePolicyResponse.to_json)
           (Some Values.BatchGetLifecyclePolicyResponse.error_to_json)])
let batch_get_vpc_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ids = flag "ids" (required json_arg) ~doc:"JSON VpcEndpointIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_vpc_endpoint
           (Values.BatchGetVpcEndpointRequest.make
              ~ids:(Values.VpcEndpointIds.of_json ids) ())
           (Some Values.BatchGetVpcEndpointResponse.to_json)
           (Some Values.BatchGetVpcEndpointResponse.error_to_json)])
let create_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING PolicyDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON AccessPolicyType"
       and name = flag "name" (required string) ~doc:"STRING PolicyName"
       and policy =
         flag "policy" (required string) ~doc:"STRING PolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_policy
           (Values.CreateAccessPolicyRequest.make ?description ?clientToken
              ~type_:(Values.AccessPolicyType.of_json type_) ~name ~policy ())
           (Some Values.CreateAccessPolicyResponse.to_json)
           (Some Values.CreateAccessPolicyResponse.error_to_json)])
let create_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON CollectionType"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CreateCollectionRequestDescriptionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and standbyReplicas =
         flag "standby-replicas" (optional json_arg)
           ~doc:"JSON StandbyReplicas"
       and vectorOptions =
         flag "vector-options" (optional json_arg) ~doc:"JSON VectorOptions"
       and collectionGroupName =
         flag "collection-group-name" (optional string)
           ~doc:"STRING CollectionGroupName"
       and encryptionConfig =
         flag "encryption-config" (optional json_arg)
           ~doc:"JSON EncryptionConfig"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and name = flag "name" (required string) ~doc:"STRING CollectionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_collection
           (Values.CreateCollectionRequest.make
              ?type_:(Option.map ~f:Values.CollectionType.of_json type_)
              ?description ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?standbyReplicas:(Option.map ~f:Values.StandbyReplicas.of_json
                                  standbyReplicas)
              ?vectorOptions:(Option.map ~f:Values.VectorOptions.of_json
                                vectorOptions) ?collectionGroupName
              ?encryptionConfig:(Option.map
                                   ~f:Values.EncryptionConfig.of_json
                                   encryptionConfig) ?clientToken ~name ())
           (Some Values.CreateCollectionResponse.to_json)
           (Some Values.CreateCollectionResponse.error_to_json)])
let create_collection_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CreateCollectionGroupRequestDescriptionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and capacityLimits =
         flag "capacity-limits" (optional json_arg)
           ~doc:"JSON CollectionGroupCapacityLimits"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and name =
         flag "name" (required string) ~doc:"STRING CollectionGroupName"
       and standbyReplicas =
         flag "standby-replicas" (required json_arg)
           ~doc:"JSON StandbyReplicas" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_collection_group
           (Values.CreateCollectionGroupRequest.make ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?capacityLimits:(Option.map
                                 ~f:Values.CollectionGroupCapacityLimits.of_json
                                 capacityLimits) ?clientToken ~name
              ~standbyReplicas:(Values.StandbyReplicas.of_json
                                  standbyReplicas) ())
           (Some Values.CreateCollectionGroupResponse.to_json)
           (Some Values.CreateCollectionGroupResponse.error_to_json)])
let create_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexSchema =
         flag "index-schema" (optional json_arg) ~doc:"JSON IndexSchema"
       and id = flag "id" (required string) ~doc:"STRING CollectionId"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING IndexName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_index
           (Values.CreateIndexRequest.make
              ?indexSchema:(Option.map ~f:Values.IndexSchema.of_json
                              indexSchema) ~id ~indexName ())
           (Some Values.CreateIndexResponse.to_json)
           (Some Values.CreateIndexResponse.error_to_json)])
let create_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING PolicyDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON LifecyclePolicyType"
       and name = flag "name" (required string) ~doc:"STRING PolicyName"
       and policy =
         flag "policy" (required string) ~doc:"STRING PolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_lifecycle_policy
           (Values.CreateLifecyclePolicyRequest.make ?description
              ?clientToken ~type_:(Values.LifecyclePolicyType.of_json type_)
              ~name ~policy ())
           (Some Values.CreateLifecyclePolicyResponse.to_json)
           (Some Values.CreateLifecyclePolicyResponse.error_to_json)])
let create_security_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING ConfigDescription"
       and samlOptions =
         flag "saml-options" (optional json_arg)
           ~doc:"JSON SamlConfigOptions"
       and iamIdentityCenterOptions =
         flag "iam-identity-center-options" (optional json_arg)
           ~doc:"JSON CreateIamIdentityCenterConfigOptions"
       and iamFederationOptions =
         flag "iam-federation-options" (optional json_arg)
           ~doc:"JSON IamFederationConfigOptions"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON SecurityConfigType"
       and name = flag "name" (required string) ~doc:"STRING ConfigName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_security_config
           (Values.CreateSecurityConfigRequest.make ?description
              ?samlOptions:(Option.map ~f:Values.SamlConfigOptions.of_json
                              samlOptions)
              ?iamIdentityCenterOptions:(Option.map
                                           ~f:Values.CreateIamIdentityCenterConfigOptions.of_json
                                           iamIdentityCenterOptions)
              ?iamFederationOptions:(Option.map
                                       ~f:Values.IamFederationConfigOptions.of_json
                                       iamFederationOptions) ?clientToken
              ~type_:(Values.SecurityConfigType.of_json type_) ~name ())
           (Some Values.CreateSecurityConfigResponse.to_json)
           (Some Values.CreateSecurityConfigResponse.error_to_json)])
let create_security_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING PolicyDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON SecurityPolicyType"
       and name = flag "name" (required string) ~doc:"STRING PolicyName"
       and policy =
         flag "policy" (required string) ~doc:"STRING PolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_security_policy
           (Values.CreateSecurityPolicyRequest.make ?description ?clientToken
              ~type_:(Values.SecurityPolicyType.of_json type_) ~name ~policy
              ()) (Some Values.CreateSecurityPolicyResponse.to_json)
           (Some Values.CreateSecurityPolicyResponse.error_to_json)])
let create_vpc_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIds"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and name = flag "name" (required string) ~doc:"STRING VpcEndpointName"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId"
       and subnetIds =
         flag "subnet-ids" (required json_arg) ~doc:"JSON SubnetIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpc_endpoint
           (Values.CreateVpcEndpointRequest.make
              ?securityGroupIds:(Option.map
                                   ~f:Values.SecurityGroupIds.of_json
                                   securityGroupIds) ?clientToken ~name
              ~vpcId ~subnetIds:(Values.SubnetIds.of_json subnetIds) ())
           (Some Values.CreateVpcEndpointResponse.to_json)
           (Some Values.CreateVpcEndpointResponse.error_to_json)])
let delete_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON AccessPolicyType"
       and name = flag "name" (required string) ~doc:"STRING PolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_policy
           (Values.DeleteAccessPolicyRequest.make ?clientToken
              ~type_:(Values.AccessPolicyType.of_json type_) ~name ())
           (Some Values.DeleteAccessPolicyResponse.to_json)
           (Some Values.DeleteAccessPolicyResponse.error_to_json)])
let delete_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and id = flag "id" (required string) ~doc:"STRING CollectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_collection
           (Values.DeleteCollectionRequest.make ?clientToken ~id ())
           (Some Values.DeleteCollectionResponse.to_json)
           (Some Values.DeleteCollectionResponse.error_to_json)])
let delete_collection_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and id = flag "id" (required string) ~doc:"STRING CollectionGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_collection_group
           (Values.DeleteCollectionGroupRequest.make ?clientToken ~id ())
           (Some Values.DeleteCollectionGroupResponse.to_json)
           (Some Values.DeleteCollectionGroupResponse.error_to_json)])
let delete_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CollectionId"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING IndexName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_index (Values.DeleteIndexRequest.make ~id ~indexName ())
           (Some Values.DeleteIndexResponse.to_json)
           (Some Values.DeleteIndexResponse.error_to_json)])
let delete_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON LifecyclePolicyType"
       and name = flag "name" (required string) ~doc:"STRING PolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_lifecycle_policy
           (Values.DeleteLifecyclePolicyRequest.make ?clientToken
              ~type_:(Values.LifecyclePolicyType.of_json type_) ~name ())
           (Some Values.DeleteLifecyclePolicyResponse.to_json)
           (Some Values.DeleteLifecyclePolicyResponse.error_to_json)])
let delete_security_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and id = flag "id" (required string) ~doc:"STRING SecurityConfigId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_security_config
           (Values.DeleteSecurityConfigRequest.make ?clientToken ~id ())
           (Some Values.DeleteSecurityConfigResponse.to_json)
           (Some Values.DeleteSecurityConfigResponse.error_to_json)])
let delete_security_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON SecurityPolicyType"
       and name = flag "name" (required string) ~doc:"STRING PolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_security_policy
           (Values.DeleteSecurityPolicyRequest.make ?clientToken
              ~type_:(Values.SecurityPolicyType.of_json type_) ~name ())
           (Some Values.DeleteSecurityPolicyResponse.to_json)
           (Some Values.DeleteSecurityPolicyResponse.error_to_json)])
let delete_vpc_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and id = flag "id" (required string) ~doc:"STRING VpcEndpointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_endpoint
           (Values.DeleteVpcEndpointRequest.make ?clientToken ~id ())
           (Some Values.DeleteVpcEndpointResponse.to_json)
           (Some Values.DeleteVpcEndpointResponse.error_to_json)])
let get_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON AccessPolicyType"
       and name = flag "name" (required string) ~doc:"STRING PolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_policy
           (Values.GetAccessPolicyRequest.make
              ~type_:(Values.AccessPolicyType.of_json type_) ~name ())
           (Some Values.GetAccessPolicyResponse.to_json)
           (Some Values.GetAccessPolicyResponse.error_to_json)])
let get_account_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_settings (Values.GetAccountSettingsRequest.make ())
           (Some Values.GetAccountSettingsResponse.to_json)
           (Some Values.GetAccountSettingsResponse.error_to_json)])
let get_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CollectionId"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING IndexName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_index (Values.GetIndexRequest.make ~id ~indexName ())
           (Some Values.GetIndexResponse.to_json)
           (Some Values.GetIndexResponse.error_to_json)])
let get_policies_stats =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policies_stats (Values.GetPoliciesStatsRequest.make ())
           (Some Values.GetPoliciesStatsResponse.to_json)
           (Some Values.GetPoliciesStatsResponse.error_to_json)])
let get_security_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING SecurityConfigId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_security_config
           (Values.GetSecurityConfigRequest.make ~id ())
           (Some Values.GetSecurityConfigResponse.to_json)
           (Some Values.GetSecurityConfigResponse.error_to_json)])
let get_security_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON SecurityPolicyType"
       and name = flag "name" (required string) ~doc:"STRING PolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_security_policy
           (Values.GetSecurityPolicyRequest.make
              ~type_:(Values.SecurityPolicyType.of_json type_) ~name ())
           (Some Values.GetSecurityPolicyResponse.to_json)
           (Some Values.GetSecurityPolicyResponse.error_to_json)])
let list_access_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resource =
         flag "resource" (optional json_arg)
           ~doc:"JSON ListAccessPoliciesRequestResourceList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAccessPoliciesRequestMaxResultsInteger"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON AccessPolicyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_policies
           (Values.ListAccessPoliciesRequest.make
              ?resource:(Option.map
                           ~f:Values.ListAccessPoliciesRequestResourceList.of_json
                           resource) ?nextToken ?maxResults
              ~type_:(Values.AccessPolicyType.of_json type_) ())
           (Some Values.ListAccessPoliciesResponse.to_json)
           (Some Values.ListAccessPoliciesResponse.error_to_json)])
let list_collection_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListCollectionGroupsRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_collection_groups
           (Values.ListCollectionGroupsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListCollectionGroupsResponse.to_json)
           (Some Values.ListCollectionGroupsResponse.error_to_json)])
let list_collections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and collectionFilters =
         flag "collection-filters" (optional json_arg)
           ~doc:"JSON CollectionFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListCollectionsRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_collections
           (Values.ListCollectionsRequest.make
              ?collectionFilters:(Option.map
                                    ~f:Values.CollectionFilters.of_json
                                    collectionFilters) ?nextToken ?maxResults
              ()) (Some Values.ListCollectionsResponse.to_json)
           (Some Values.ListCollectionsResponse.error_to_json)])
let list_lifecycle_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resources =
         flag "resources" (optional json_arg)
           ~doc:"JSON ListLifecyclePoliciesRequestResourcesList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListLifecyclePoliciesRequestMaxResultsInteger"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON LifecyclePolicyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_lifecycle_policies
           (Values.ListLifecyclePoliciesRequest.make
              ?resources:(Option.map
                            ~f:Values.ListLifecyclePoliciesRequestResourcesList.of_json
                            resources) ?nextToken ?maxResults
              ~type_:(Values.LifecyclePolicyType.of_json type_) ())
           (Some Values.ListLifecyclePoliciesResponse.to_json)
           (Some Values.ListLifecyclePoliciesResponse.error_to_json)])
let list_security_configs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSecurityConfigsRequestMaxResultsInteger"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON SecurityConfigType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_security_configs
           (Values.ListSecurityConfigsRequest.make ?nextToken ?maxResults
              ~type_:(Values.SecurityConfigType.of_json type_) ())
           (Some Values.ListSecurityConfigsResponse.to_json)
           (Some Values.ListSecurityConfigsResponse.error_to_json)])
let list_security_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resource =
         flag "resource" (optional json_arg)
           ~doc:"JSON ListSecurityPoliciesRequestResourceList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSecurityPoliciesRequestMaxResultsInteger"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON SecurityPolicyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_security_policies
           (Values.ListSecurityPoliciesRequest.make
              ?resource:(Option.map
                           ~f:Values.ListSecurityPoliciesRequestResourceList.of_json
                           resource) ?nextToken ?maxResults
              ~type_:(Values.SecurityPolicyType.of_json type_) ())
           (Some Values.ListSecurityPoliciesResponse.to_json)
           (Some Values.ListSecurityPoliciesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_vpc_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcEndpointFilters =
         flag "vpc-endpoint-filters" (optional json_arg)
           ~doc:"JSON VpcEndpointFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListVpcEndpointsRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_vpc_endpoints
           (Values.ListVpcEndpointsRequest.make
              ?vpcEndpointFilters:(Option.map
                                     ~f:Values.VpcEndpointFilters.of_json
                                     vpcEndpointFilters) ?nextToken
              ?maxResults ()) (Some Values.ListVpcEndpointsResponse.to_json)
           (Some Values.ListVpcEndpointsResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING PolicyDescription"
       and policy =
         flag "policy" (optional string) ~doc:"STRING PolicyDocument"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON AccessPolicyType"
       and name = flag "name" (required string) ~doc:"STRING PolicyName"
       and policyVersion =
         flag "policy-version" (required string) ~doc:"STRING PolicyVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_access_policy
           (Values.UpdateAccessPolicyRequest.make ?description ?policy
              ?clientToken ~type_:(Values.AccessPolicyType.of_json type_)
              ~name ~policyVersion ())
           (Some Values.UpdateAccessPolicyResponse.to_json)
           (Some Values.UpdateAccessPolicyResponse.error_to_json)])
let update_account_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityLimits =
         flag "capacity-limits" (optional json_arg)
           ~doc:"JSON CapacityLimits" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account_settings
           (Values.UpdateAccountSettingsRequest.make
              ?capacityLimits:(Option.map ~f:Values.CapacityLimits.of_json
                                 capacityLimits) ())
           (Some Values.UpdateAccountSettingsResponse.to_json)
           (Some Values.UpdateAccountSettingsResponse.error_to_json)])
let update_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING UpdateCollectionRequestDescriptionString"
       and vectorOptions =
         flag "vector-options" (optional json_arg) ~doc:"JSON VectorOptions"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and id = flag "id" (required string) ~doc:"STRING CollectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_collection
           (Values.UpdateCollectionRequest.make ?description
              ?vectorOptions:(Option.map ~f:Values.VectorOptions.of_json
                                vectorOptions) ?clientToken ~id ())
           (Some Values.UpdateCollectionResponse.to_json)
           (Some Values.UpdateCollectionResponse.error_to_json)])
let update_collection_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING UpdateCollectionGroupRequestDescriptionString"
       and capacityLimits =
         flag "capacity-limits" (optional json_arg)
           ~doc:"JSON CollectionGroupCapacityLimits"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and id = flag "id" (required string) ~doc:"STRING CollectionGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_collection_group
           (Values.UpdateCollectionGroupRequest.make ?description
              ?capacityLimits:(Option.map
                                 ~f:Values.CollectionGroupCapacityLimits.of_json
                                 capacityLimits) ?clientToken ~id ())
           (Some Values.UpdateCollectionGroupResponse.to_json)
           (Some Values.UpdateCollectionGroupResponse.error_to_json)])
let update_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexSchema =
         flag "index-schema" (optional json_arg) ~doc:"JSON IndexSchema"
       and id = flag "id" (required string) ~doc:"STRING CollectionId"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING IndexName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_index
           (Values.UpdateIndexRequest.make
              ?indexSchema:(Option.map ~f:Values.IndexSchema.of_json
                              indexSchema) ~id ~indexName ())
           (Some Values.UpdateIndexResponse.to_json)
           (Some Values.UpdateIndexResponse.error_to_json)])
let update_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING PolicyDescription"
       and policy =
         flag "policy" (optional string) ~doc:"STRING PolicyDocument"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON LifecyclePolicyType"
       and name = flag "name" (required string) ~doc:"STRING PolicyName"
       and policyVersion =
         flag "policy-version" (required string) ~doc:"STRING PolicyVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_lifecycle_policy
           (Values.UpdateLifecyclePolicyRequest.make ?description ?policy
              ?clientToken ~type_:(Values.LifecyclePolicyType.of_json type_)
              ~name ~policyVersion ())
           (Some Values.UpdateLifecyclePolicyResponse.to_json)
           (Some Values.UpdateLifecyclePolicyResponse.error_to_json)])
let update_security_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING ConfigDescription"
       and samlOptions =
         flag "saml-options" (optional json_arg)
           ~doc:"JSON SamlConfigOptions"
       and iamIdentityCenterOptionsUpdates =
         flag "iam-identity-center-options-updates" (optional json_arg)
           ~doc:"JSON UpdateIamIdentityCenterConfigOptions"
       and iamFederationOptions =
         flag "iam-federation-options" (optional json_arg)
           ~doc:"JSON IamFederationConfigOptions"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and id = flag "id" (required string) ~doc:"STRING SecurityConfigId"
       and configVersion =
         flag "config-version" (required string) ~doc:"STRING PolicyVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_security_config
           (Values.UpdateSecurityConfigRequest.make ?description
              ?samlOptions:(Option.map ~f:Values.SamlConfigOptions.of_json
                              samlOptions)
              ?iamIdentityCenterOptionsUpdates:(Option.map
                                                  ~f:Values.UpdateIamIdentityCenterConfigOptions.of_json
                                                  iamIdentityCenterOptionsUpdates)
              ?iamFederationOptions:(Option.map
                                       ~f:Values.IamFederationConfigOptions.of_json
                                       iamFederationOptions) ?clientToken ~id
              ~configVersion ())
           (Some Values.UpdateSecurityConfigResponse.to_json)
           (Some Values.UpdateSecurityConfigResponse.error_to_json)])
let update_security_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING PolicyDescription"
       and policy =
         flag "policy" (optional string) ~doc:"STRING PolicyDocument"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON SecurityPolicyType"
       and name = flag "name" (required string) ~doc:"STRING PolicyName"
       and policyVersion =
         flag "policy-version" (required string) ~doc:"STRING PolicyVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_security_policy
           (Values.UpdateSecurityPolicyRequest.make ?description ?policy
              ?clientToken ~type_:(Values.SecurityPolicyType.of_json type_)
              ~name ~policyVersion ())
           (Some Values.UpdateSecurityPolicyResponse.to_json)
           (Some Values.UpdateSecurityPolicyResponse.error_to_json)])
let update_vpc_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addSubnetIds =
         flag "add-subnet-ids" (optional json_arg) ~doc:"JSON SubnetIds"
       and removeSubnetIds =
         flag "remove-subnet-ids" (optional json_arg) ~doc:"JSON SubnetIds"
       and addSecurityGroupIds =
         flag "add-security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIds"
       and removeSecurityGroupIds =
         flag "remove-security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIds"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and id = flag "id" (required string) ~doc:"STRING VpcEndpointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_vpc_endpoint
           (Values.UpdateVpcEndpointRequest.make
              ?addSubnetIds:(Option.map ~f:Values.SubnetIds.of_json
                               addSubnetIds)
              ?removeSubnetIds:(Option.map ~f:Values.SubnetIds.of_json
                                  removeSubnetIds)
              ?addSecurityGroupIds:(Option.map
                                      ~f:Values.SecurityGroupIds.of_json
                                      addSecurityGroupIds)
              ?removeSecurityGroupIds:(Option.map
                                         ~f:Values.SecurityGroupIds.of_json
                                         removeSecurityGroupIds) ?clientToken
              ~id ()) (Some Values.UpdateVpcEndpointResponse.to_json)
           (Some Values.UpdateVpcEndpointResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-get-collection", batch_get_collection);
    ("batch-get-collection-group", batch_get_collection_group);
    ("batch-get-effective-lifecycle-policy",
      batch_get_effective_lifecycle_policy);
    ("batch-get-lifecycle-policy", batch_get_lifecycle_policy);
    ("batch-get-vpc-endpoint", batch_get_vpc_endpoint);
    ("create-access-policy", create_access_policy);
    ("create-collection", create_collection);
    ("create-collection-group", create_collection_group);
    ("create-index", create_index);
    ("create-lifecycle-policy", create_lifecycle_policy);
    ("create-security-config", create_security_config);
    ("create-security-policy", create_security_policy);
    ("create-vpc-endpoint", create_vpc_endpoint);
    ("delete-access-policy", delete_access_policy);
    ("delete-collection", delete_collection);
    ("delete-collection-group", delete_collection_group);
    ("delete-index", delete_index);
    ("delete-lifecycle-policy", delete_lifecycle_policy);
    ("delete-security-config", delete_security_config);
    ("delete-security-policy", delete_security_policy);
    ("delete-vpc-endpoint", delete_vpc_endpoint);
    ("get-access-policy", get_access_policy);
    ("get-account-settings", get_account_settings);
    ("get-index", get_index);
    ("get-policies-stats", get_policies_stats);
    ("get-security-config", get_security_config);
    ("get-security-policy", get_security_policy);
    ("list-access-policies", list_access_policies);
    ("list-collection-groups", list_collection_groups);
    ("list-collections", list_collections);
    ("list-lifecycle-policies", list_lifecycle_policies);
    ("list-security-configs", list_security_configs);
    ("list-security-policies", list_security_policies);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-vpc-endpoints", list_vpc_endpoints);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-access-policy", update_access_policy);
    ("update-account-settings", update_account_settings);
    ("update-collection", update_collection);
    ("update-collection-group", update_collection_group);
    ("update-index", update_index);
    ("update-lifecycle-policy", update_lifecycle_policy);
    ("update-security-config", update_security_config);
    ("update-security-policy", update_security_policy);
    ("update-vpc-endpoint", update_vpc_endpoint)]
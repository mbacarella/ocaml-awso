(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_configuration_items_to_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationConfigurationId =
         flag "application-configuration-id" (required string)
           ~doc:"STRING ApplicationId"
       and configurationIds =
         flag "configuration-ids" (required json_arg)
           ~doc:"JSON ConfigurationIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_configuration_items_to_application
           (Values.AssociateConfigurationItemsToApplicationRequest.make
              ~applicationConfigurationId
              ~configurationIds:(Values.ConfigurationIdList.of_json
                                   configurationIds) ())
           (Some
              Values.AssociateConfigurationItemsToApplicationResponse.to_json)
           (Some
              Values.AssociateConfigurationItemsToApplicationResponse.error_to_json)])
let batch_delete_import_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and importTaskIds =
         flag "import-task-ids" (required json_arg)
           ~doc:"JSON ToDeleteIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_import_data
           (Values.BatchDeleteImportDataRequest.make
              ~importTaskIds:(Values.ToDeleteIdentifierList.of_json
                                importTaskIds) ())
           (Some Values.BatchDeleteImportDataResponse.to_json)
           (Some Values.BatchDeleteImportDataResponse.error_to_json)])
let create_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application
           (Values.CreateApplicationRequest.make ?description ~name ())
           (Some Values.CreateApplicationResponse.to_json)
           (Some Values.CreateApplicationResponse.error_to_json)])
let create_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationIds =
         flag "configuration-ids" (required json_arg)
           ~doc:"JSON ConfigurationIdList"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagSet" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_tags
           (Values.CreateTagsRequest.make
              ~configurationIds:(Values.ConfigurationIdList.of_json
                                   configurationIds)
              ~tags:(Values.TagSet.of_json tags) ())
           (Some Values.CreateTagsResponse.to_json)
           (Some Values.CreateTagsResponse.error_to_json)])
let delete_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationIds =
         flag "configuration-ids" (required json_arg)
           ~doc:"JSON ApplicationIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_applications
           (Values.DeleteApplicationsRequest.make
              ~configurationIds:(Values.ApplicationIdsList.of_json
                                   configurationIds) ())
           (Some Values.DeleteApplicationsResponse.to_json)
           (Some Values.DeleteApplicationsResponse.error_to_json)])
let delete_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagSet"
       and configurationIds =
         flag "configuration-ids" (required json_arg)
           ~doc:"JSON ConfigurationIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tags
           (Values.DeleteTagsRequest.make
              ?tags:(Option.map ~f:Values.TagSet.of_json tags)
              ~configurationIds:(Values.ConfigurationIdList.of_json
                                   configurationIds) ())
           (Some Values.DeleteTagsResponse.to_json)
           (Some Values.DeleteTagsResponse.error_to_json)])
let describe_agents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentIds =
         flag "agent-ids" (optional json_arg) ~doc:"JSON AgentIds"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_agents
           (Values.DescribeAgentsRequest.make
              ?agentIds:(Option.map ~f:Values.AgentIds.of_json agentIds)
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.DescribeAgentsResponse.to_json)
           (Some Values.DescribeAgentsResponse.error_to_json)])
let describe_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationIds =
         flag "configuration-ids" (required json_arg)
           ~doc:"JSON ConfigurationIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_configurations
           (Values.DescribeConfigurationsRequest.make
              ~configurationIds:(Values.ConfigurationIdList.of_json
                                   configurationIds) ())
           (Some Values.DescribeConfigurationsResponse.to_json)
           (Some Values.DescribeConfigurationsResponse.error_to_json)])
let describe_continuous_exports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportIds =
         flag "export-ids" (optional json_arg)
           ~doc:"JSON ContinuousExportIds"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeContinuousExportsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_continuous_exports
           (Values.DescribeContinuousExportsRequest.make
              ?exportIds:(Option.map ~f:Values.ContinuousExportIds.of_json
                            exportIds) ?maxResults ?nextToken ())
           (Some Values.DescribeContinuousExportsResponse.to_json)
           (Some Values.DescribeContinuousExportsResponse.error_to_json)])
let describe_export_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportIds =
         flag "export-ids" (optional json_arg) ~doc:"JSON ExportIds"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_export_configurations
           (Values.DescribeExportConfigurationsRequest.make
              ?exportIds:(Option.map ~f:Values.ExportIds.of_json exportIds)
              ?maxResults ?nextToken ())
           (Some Values.DescribeExportConfigurationsResponse.to_json)
           (Some Values.DescribeExportConfigurationsResponse.error_to_json)])
let describe_export_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportIds =
         flag "export-ids" (optional json_arg) ~doc:"JSON ExportIds"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ExportFilters"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_export_tasks
           (Values.DescribeExportTasksRequest.make
              ?exportIds:(Option.map ~f:Values.ExportIds.of_json exportIds)
              ?filters:(Option.map ~f:Values.ExportFilters.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.DescribeExportTasksResponse.to_json)
           (Some Values.DescribeExportTasksResponse.error_to_json)])
let describe_import_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON DescribeImportTasksFilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeImportTasksMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_import_tasks
           (Values.DescribeImportTasksRequest.make
              ?filters:(Option.map
                          ~f:Values.DescribeImportTasksFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.DescribeImportTasksResponse.to_json)
           (Some Values.DescribeImportTasksResponse.error_to_json)])
let describe_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON TagFilters"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_tags
           (Values.DescribeTagsRequest.make
              ?filters:(Option.map ~f:Values.TagFilters.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.DescribeTagsResponse.to_json)
           (Some Values.DescribeTagsResponse.error_to_json)])
let disassociate_configuration_items_from_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationConfigurationId =
         flag "application-configuration-id" (required string)
           ~doc:"STRING ApplicationId"
       and configurationIds =
         flag "configuration-ids" (required json_arg)
           ~doc:"JSON ConfigurationIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_configuration_items_from_application
           (Values.DisassociateConfigurationItemsFromApplicationRequest.make
              ~applicationConfigurationId
              ~configurationIds:(Values.ConfigurationIdList.of_json
                                   configurationIds) ())
           (Some
              Values.DisassociateConfigurationItemsFromApplicationResponse.to_json)
           (Some
              Values.DisassociateConfigurationItemsFromApplicationResponse.error_to_json)])
let export_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_configurations (Fn.id ())
           (Some Values.ExportConfigurationsResponse.to_json)
           (Some Values.ExportConfigurationsResponse.error_to_json)])
let get_discovery_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_discovery_summary
           (Values.GetDiscoverySummaryRequest.make ())
           (Some Values.GetDiscoverySummaryResponse.to_json)
           (Some Values.GetDiscoverySummaryResponse.error_to_json)])
let list_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and orderBy =
         flag "order-by" (optional json_arg) ~doc:"JSON OrderByList"
       and configurationType =
         flag "configuration-type" (required json_arg)
           ~doc:"JSON ConfigurationItemType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configurations
           (Values.ListConfigurationsRequest.make
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ?maxResults ?nextToken
              ?orderBy:(Option.map ~f:Values.OrderByList.of_json orderBy)
              ~configurationType:(Values.ConfigurationItemType.of_json
                                    configurationType) ())
           (Some Values.ListConfigurationsResponse.to_json)
           (Some Values.ListConfigurationsResponse.error_to_json)])
let list_server_neighbors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portInformationNeeded =
         flag "port-information-needed" (optional bool) ~doc:"BOOL Boolean"
       and neighborConfigurationIds =
         flag "neighbor-configuration-ids" (optional json_arg)
           ~doc:"JSON ConfigurationIdList"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and configurationId =
         flag "configuration-id" (required string)
           ~doc:"STRING ConfigurationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_server_neighbors
           (Values.ListServerNeighborsRequest.make ?portInformationNeeded
              ?neighborConfigurationIds:(Option.map
                                           ~f:Values.ConfigurationIdList.of_json
                                           neighborConfigurationIds)
              ?maxResults ?nextToken ~configurationId ())
           (Some Values.ListServerNeighborsResponse.to_json)
           (Some Values.ListServerNeighborsResponse.error_to_json)])
let start_continuous_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_continuous_export
           (Values.StartContinuousExportRequest.make ())
           (Some Values.StartContinuousExportResponse.to_json)
           (Some Values.StartContinuousExportResponse.error_to_json)])
let start_data_collection_by_agent_ids =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentIds =
         flag "agent-ids" (required json_arg) ~doc:"JSON AgentIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_data_collection_by_agent_ids
           (Values.StartDataCollectionByAgentIdsRequest.make
              ~agentIds:(Values.AgentIds.of_json agentIds) ())
           (Some Values.StartDataCollectionByAgentIdsResponse.to_json)
           (Some Values.StartDataCollectionByAgentIdsResponse.error_to_json)])
let start_export_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportDataFormat =
         flag "export-data-format" (optional json_arg)
           ~doc:"JSON ExportDataFormats"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ExportFilters"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TimeStamp"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON TimeStamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_export_task
           (Values.StartExportTaskRequest.make
              ?exportDataFormat:(Option.map
                                   ~f:Values.ExportDataFormats.of_json
                                   exportDataFormat)
              ?filters:(Option.map ~f:Values.ExportFilters.of_json filters)
              ?startTime:(Option.map ~f:Values.TimeStamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.TimeStamp.of_json endTime) ())
           (Some Values.StartExportTaskResponse.to_json)
           (Some Values.StartExportTaskResponse.error_to_json)])
let start_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and name = flag "name" (required string) ~doc:"STRING ImportTaskName"
       and importUrl =
         flag "import-url" (required string) ~doc:"STRING ImportURL" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_import_task
           (Values.StartImportTaskRequest.make ?clientRequestToken ~name
              ~importUrl ()) (Some Values.StartImportTaskResponse.to_json)
           (Some Values.StartImportTaskResponse.error_to_json)])
let stop_continuous_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportId =
         flag "export-id" (required string)
           ~doc:"STRING ConfigurationsExportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_continuous_export
           (Values.StopContinuousExportRequest.make ~exportId ())
           (Some Values.StopContinuousExportResponse.to_json)
           (Some Values.StopContinuousExportResponse.error_to_json)])
let stop_data_collection_by_agent_ids =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentIds =
         flag "agent-ids" (required json_arg) ~doc:"JSON AgentIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_data_collection_by_agent_ids
           (Values.StopDataCollectionByAgentIdsRequest.make
              ~agentIds:(Values.AgentIds.of_json agentIds) ())
           (Some Values.StopDataCollectionByAgentIdsResponse.to_json)
           (Some Values.StopDataCollectionByAgentIdsResponse.error_to_json)])
let update_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and configurationId =
         flag "configuration-id" (required string)
           ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application
           (Values.UpdateApplicationRequest.make ?name ?description
              ~configurationId ())
           (Some Values.UpdateApplicationResponse.to_json)
           (Some Values.UpdateApplicationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-configuration-items-to-application",
       associate_configuration_items_to_application);
    ("batch-delete-import-data", batch_delete_import_data);
    ("create-application", create_application);
    ("create-tags", create_tags);
    ("delete-applications", delete_applications);
    ("delete-tags", delete_tags);
    ("describe-agents", describe_agents);
    ("describe-configurations", describe_configurations);
    ("describe-continuous-exports", describe_continuous_exports);
    ("describe-export-configurations", describe_export_configurations);
    ("describe-export-tasks", describe_export_tasks);
    ("describe-import-tasks", describe_import_tasks);
    ("describe-tags", describe_tags);
    ("disassociate-configuration-items-from-application",
      disassociate_configuration_items_from_application);
    ("export-configurations", export_configurations);
    ("get-discovery-summary", get_discovery_summary);
    ("list-configurations", list_configurations);
    ("list-server-neighbors", list_server_neighbors);
    ("start-continuous-export", start_continuous_export);
    ("start-data-collection-by-agent-ids",
      start_data_collection_by_agent_ids);
    ("start-export-task", start_export_task);
    ("start-import-task", start_import_task);
    ("stop-continuous-export", stop_continuous_export);
    ("stop-data-collection-by-agent-ids", stop_data_collection_by_agent_ids);
    ("update-application", update_application)]
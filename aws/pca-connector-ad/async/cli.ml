(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (required string)
           ~doc:"STRING CertificateAuthorityArn"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and vpcInformation =
         flag "vpc-information" (required json_arg)
           ~doc:"JSON VpcInformation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connector
           (Values.CreateConnectorRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~certificateAuthorityArn ~directoryId
              ~vpcInformation:(Values.VpcInformation.of_json vpcInformation)
              ()) (Some Values.CreateConnectorResponse.to_json)
           (Some Values.CreateConnectorResponse.error_to_json)])
let create_directory_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_directory_registration
           (Values.CreateDirectoryRegistrationRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~directoryId ())
           (Some Values.CreateDirectoryRegistrationResponse.to_json)
           (Some Values.CreateDirectoryRegistrationResponse.error_to_json)])
let create_service_principal_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and connectorArn =
         flag "connector-arn" (required string) ~doc:"STRING ConnectorArn"
       and directoryRegistrationArn =
         flag "directory-registration-arn" (required string)
           ~doc:"STRING DirectoryRegistrationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service_principal_name
           (Values.CreateServicePrincipalNameRequest.make ?clientToken
              ~connectorArn ~directoryRegistrationArn ()) None None])
let create_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and connectorArn =
         flag "connector-arn" (required string) ~doc:"STRING ConnectorArn"
       and definition =
         flag "definition" (required json_arg) ~doc:"JSON TemplateDefinition"
       and name = flag "name" (required string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_template
           (Values.CreateTemplateRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~connectorArn
              ~definition:(Values.TemplateDefinition.of_json definition)
              ~name ()) (Some Values.CreateTemplateResponse.to_json)
           (Some Values.CreateTemplateResponse.error_to_json)])
let create_template_group_access_control_entry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and accessRights =
         flag "access-rights" (required json_arg) ~doc:"JSON AccessRights"
       and groupDisplayName =
         flag "group-display-name" (required string)
           ~doc:"STRING DisplayName"
       and groupSecurityIdentifier =
         flag "group-security-identifier" (required string)
           ~doc:"STRING GroupSecurityIdentifier"
       and templateArn =
         flag "template-arn" (required string) ~doc:"STRING TemplateArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_template_group_access_control_entry
           (Values.CreateTemplateGroupAccessControlEntryRequest.make
              ?clientToken
              ~accessRights:(Values.AccessRights.of_json accessRights)
              ~groupDisplayName ~groupSecurityIdentifier ~templateArn ())
           None None])
let delete_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorArn =
         flag "connector-arn" (required string) ~doc:"STRING ConnectorArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connector
           (Values.DeleteConnectorRequest.make ~connectorArn ()) None None])
let delete_directory_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryRegistrationArn =
         flag "directory-registration-arn" (required string)
           ~doc:"STRING DirectoryRegistrationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_directory_registration
           (Values.DeleteDirectoryRegistrationRequest.make
              ~directoryRegistrationArn ()) None None])
let delete_service_principal_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorArn =
         flag "connector-arn" (required string) ~doc:"STRING ConnectorArn"
       and directoryRegistrationArn =
         flag "directory-registration-arn" (required string)
           ~doc:"STRING DirectoryRegistrationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service_principal_name
           (Values.DeleteServicePrincipalNameRequest.make ~connectorArn
              ~directoryRegistrationArn ()) None None])
let delete_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateArn =
         flag "template-arn" (required string) ~doc:"STRING TemplateArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_template
           (Values.DeleteTemplateRequest.make ~templateArn ()) None None])
let delete_template_group_access_control_entry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupSecurityIdentifier =
         flag "group-security-identifier" (required string)
           ~doc:"STRING GroupSecurityIdentifier"
       and templateArn =
         flag "template-arn" (required string) ~doc:"STRING TemplateArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_template_group_access_control_entry
           (Values.DeleteTemplateGroupAccessControlEntryRequest.make
              ~groupSecurityIdentifier ~templateArn ()) None None])
let get_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorArn =
         flag "connector-arn" (required string) ~doc:"STRING ConnectorArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connector
           (Values.GetConnectorRequest.make ~connectorArn ())
           (Some Values.GetConnectorResponse.to_json)
           (Some Values.GetConnectorResponse.error_to_json)])
let get_directory_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryRegistrationArn =
         flag "directory-registration-arn" (required string)
           ~doc:"STRING DirectoryRegistrationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_directory_registration
           (Values.GetDirectoryRegistrationRequest.make
              ~directoryRegistrationArn ())
           (Some Values.GetDirectoryRegistrationResponse.to_json)
           (Some Values.GetDirectoryRegistrationResponse.error_to_json)])
let get_service_principal_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorArn =
         flag "connector-arn" (required string) ~doc:"STRING ConnectorArn"
       and directoryRegistrationArn =
         flag "directory-registration-arn" (required string)
           ~doc:"STRING DirectoryRegistrationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_principal_name
           (Values.GetServicePrincipalNameRequest.make ~connectorArn
              ~directoryRegistrationArn ())
           (Some Values.GetServicePrincipalNameResponse.to_json)
           (Some Values.GetServicePrincipalNameResponse.error_to_json)])
let get_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateArn =
         flag "template-arn" (required string) ~doc:"STRING TemplateArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_template (Values.GetTemplateRequest.make ~templateArn ())
           (Some Values.GetTemplateResponse.to_json)
           (Some Values.GetTemplateResponse.error_to_json)])
let get_template_group_access_control_entry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupSecurityIdentifier =
         flag "group-security-identifier" (required string)
           ~doc:"STRING GroupSecurityIdentifier"
       and templateArn =
         flag "template-arn" (required string) ~doc:"STRING TemplateArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_template_group_access_control_entry
           (Values.GetTemplateGroupAccessControlEntryRequest.make
              ~groupSecurityIdentifier ~templateArn ())
           (Some Values.GetTemplateGroupAccessControlEntryResponse.to_json)
           (Some
              Values.GetTemplateGroupAccessControlEntryResponse.error_to_json)])
let list_connectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connectors
           (Values.ListConnectorsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListConnectorsResponse.to_json)
           (Some Values.ListConnectorsResponse.error_to_json)])
let list_directory_registrations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_directory_registrations
           (Values.ListDirectoryRegistrationsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListDirectoryRegistrationsResponse.to_json)
           (Some Values.ListDirectoryRegistrationsResponse.error_to_json)])
let list_service_principal_names =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and directoryRegistrationArn =
         flag "directory-registration-arn" (required string)
           ~doc:"STRING DirectoryRegistrationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_principal_names
           (Values.ListServicePrincipalNamesRequest.make ?maxResults
              ?nextToken ~directoryRegistrationArn ())
           (Some Values.ListServicePrincipalNamesResponse.to_json)
           (Some Values.ListServicePrincipalNamesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_template_group_access_control_entries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and templateArn =
         flag "template-arn" (required string) ~doc:"STRING TemplateArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_template_group_access_control_entries
           (Values.ListTemplateGroupAccessControlEntriesRequest.make
              ?maxResults ?nextToken ~templateArn ())
           (Some Values.ListTemplateGroupAccessControlEntriesResponse.to_json)
           (Some
              Values.ListTemplateGroupAccessControlEntriesResponse.error_to_json)])
let list_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and connectorArn =
         flag "connector-arn" (required string) ~doc:"STRING ConnectorArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_templates
           (Values.ListTemplatesRequest.make ?maxResults ?nextToken
              ~connectorArn ()) (Some Values.ListTemplatesResponse.to_json)
           (Some Values.ListTemplatesResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and definition =
         flag "definition" (optional json_arg) ~doc:"JSON TemplateDefinition"
       and reenrollAllCertificateHolders =
         flag "reenroll-all-certificate-holders" (optional bool)
           ~doc:"BOOL Boolean"
       and templateArn =
         flag "template-arn" (required string) ~doc:"STRING TemplateArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_template
           (Values.UpdateTemplateRequest.make
              ?definition:(Option.map ~f:Values.TemplateDefinition.of_json
                             definition) ?reenrollAllCertificateHolders
              ~templateArn ()) None None])
let update_template_group_access_control_entry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessRights =
         flag "access-rights" (optional json_arg) ~doc:"JSON AccessRights"
       and groupDisplayName =
         flag "group-display-name" (optional string)
           ~doc:"STRING DisplayName"
       and groupSecurityIdentifier =
         flag "group-security-identifier" (required string)
           ~doc:"STRING GroupSecurityIdentifier"
       and templateArn =
         flag "template-arn" (required string) ~doc:"STRING TemplateArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_template_group_access_control_entry
           (Values.UpdateTemplateGroupAccessControlEntryRequest.make
              ?accessRights:(Option.map ~f:Values.AccessRights.of_json
                               accessRights) ?groupDisplayName
              ~groupSecurityIdentifier ~templateArn ()) None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-connector", create_connector);
    ("create-directory-registration", create_directory_registration);
    ("create-service-principal-name", create_service_principal_name);
    ("create-template", create_template);
    ("create-template-group-access-control-entry",
      create_template_group_access_control_entry);
    ("delete-connector", delete_connector);
    ("delete-directory-registration", delete_directory_registration);
    ("delete-service-principal-name", delete_service_principal_name);
    ("delete-template", delete_template);
    ("delete-template-group-access-control-entry",
      delete_template_group_access_control_entry);
    ("get-connector", get_connector);
    ("get-directory-registration", get_directory_registration);
    ("get-service-principal-name", get_service_principal_name);
    ("get-template", get_template);
    ("get-template-group-access-control-entry",
      get_template_group_access_control_entry);
    ("list-connectors", list_connectors);
    ("list-directory-registrations", list_directory_registrations);
    ("list-service-principal-names", list_service_principal_names);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-template-group-access-control-entries",
      list_template_group_access_control_entries);
    ("list-templates", list_templates);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-template", update_template);
    ("update-template-group-access-control-entry",
      update_template_group_access_control_entry)]
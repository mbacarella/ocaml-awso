(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.pca_connector_ad
let apiVersion = "2018-05-10"
let endpointPrefix = "pca-connector-ad"
let serviceFullName = "PcaConnectorAd"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module ApplicationPolicyType =
  struct
    type nonrec t =
      | ALL_APPLICATION_POLICIES 
      | ANY_PURPOSE 
      | ATTESTATION_IDENTITY_KEY_CERTIFICATE 
      | CERTIFICATE_REQUEST_AGENT 
      | CLIENT_AUTHENTICATION 
      | CODE_SIGNING 
      | CTL_USAGE 
      | DIGITAL_RIGHTS 
      | DIRECTORY_SERVICE_EMAIL_REPLICATION 
      | DISALLOWED_LIST 
      | DNS_SERVER_TRUST 
      | DOCUMENT_ENCRYPTION 
      | DOCUMENT_SIGNING 
      | DYNAMIC_CODE_GENERATOR 
      | EARLY_LAUNCH_ANTIMALWARE_DRIVER 
      | EMBEDDED_WINDOWS_SYSTEM_COMPONENT_VERIFICATION 
      | ENCLAVE 
      | ENCRYPTING_FILE_SYSTEM 
      | ENDORSEMENT_KEY_CERTIFICATE 
      | FILE_RECOVERY 
      | HAL_EXTENSION 
      | IP_SECURITY_END_SYSTEM 
      | IP_SECURITY_IKE_INTERMEDIATE 
      | IP_SECURITY_TUNNEL_TERMINATION 
      | IP_SECURITY_USER 
      | ISOLATED_USER_MODE 
      | KDC_AUTHENTICATION 
      | KERNEL_MODE_CODE_SIGNING 
      | KEY_PACK_LICENSES 
      | KEY_RECOVERY 
      | KEY_RECOVERY_AGENT 
      | LICENSE_SERVER_VERIFICATION 
      | LIFETIME_SIGNING 
      | MICROSOFT_PUBLISHER 
      | MICROSOFT_TIME_STAMPING 
      | MICROSOFT_TRUST_LIST_SIGNING 
      | OCSP_SIGNING 
      | OEM_WINDOWS_SYSTEM_COMPONENT_VERIFICATION 
      | PLATFORM_CERTIFICATE 
      | PREVIEW_BUILD_SIGNING 
      | PRIVATE_KEY_ARCHIVAL 
      | PROTECTED_PROCESS_LIGHT_VERIFICATION 
      | PROTECTED_PROCESS_VERIFICATION 
      | QUALIFIED_SUBORDINATION 
      | REVOKED_LIST_SIGNER 
      | ROOT_PROGRAM_AUTO_UPDATE_CA_REVOCATION 
      | ROOT_PROGRAM_AUTO_UPDATE_END_REVOCATION 
      | ROOT_PROGRAM_NO_OSCP_FAILOVER_TO_CRL 
      | ROOT_LIST_SIGNER 
      | SECURE_EMAIL 
      | SERVER_AUTHENTICATION 
      | SMART_CARD_LOGIN 
      | SPC_ENCRYPTED_DIGEST_RETRY_COUNT 
      | SPC_RELAXED_PE_MARKER_CHECK 
      | TIME_STAMPING 
      | WINDOWS_HARDWARE_DRIVER_ATTESTED_VERIFICATION 
      | WINDOWS_HARDWARE_DRIVER_EXTENDED_VERIFICATION 
      | WINDOWS_HARDWARE_DRIVER_VERIFICATION 
      | WINDOWS_HELLO_RECOVERY_KEY_ENCRYPTION 
      | WINDOWS_KITS_COMPONENT 
      | WINDOWS_RT_VERIFICATION 
      | WINDOWS_SOFTWARE_EXTENSION_VERIFICATION 
      | WINDOWS_STORE 
      | WINDOWS_SYSTEM_COMPONENT_VERIFICATION 
      | WINDOWS_TCB_COMPONENT 
      | WINDOWS_THIRD_PARTY_APPLICATION_COMPONENT 
      | WINDOWS_UPDATE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ALL_APPLICATION_POLICIES -> "ALL_APPLICATION_POLICIES"
      | ANY_PURPOSE -> "ANY_PURPOSE"
      | ATTESTATION_IDENTITY_KEY_CERTIFICATE ->
          "ATTESTATION_IDENTITY_KEY_CERTIFICATE"
      | CERTIFICATE_REQUEST_AGENT -> "CERTIFICATE_REQUEST_AGENT"
      | CLIENT_AUTHENTICATION -> "CLIENT_AUTHENTICATION"
      | CODE_SIGNING -> "CODE_SIGNING"
      | CTL_USAGE -> "CTL_USAGE"
      | DIGITAL_RIGHTS -> "DIGITAL_RIGHTS"
      | DIRECTORY_SERVICE_EMAIL_REPLICATION ->
          "DIRECTORY_SERVICE_EMAIL_REPLICATION"
      | DISALLOWED_LIST -> "DISALLOWED_LIST"
      | DNS_SERVER_TRUST -> "DNS_SERVER_TRUST"
      | DOCUMENT_ENCRYPTION -> "DOCUMENT_ENCRYPTION"
      | DOCUMENT_SIGNING -> "DOCUMENT_SIGNING"
      | DYNAMIC_CODE_GENERATOR -> "DYNAMIC_CODE_GENERATOR"
      | EARLY_LAUNCH_ANTIMALWARE_DRIVER -> "EARLY_LAUNCH_ANTIMALWARE_DRIVER"
      | EMBEDDED_WINDOWS_SYSTEM_COMPONENT_VERIFICATION ->
          "EMBEDDED_WINDOWS_SYSTEM_COMPONENT_VERIFICATION"
      | ENCLAVE -> "ENCLAVE"
      | ENCRYPTING_FILE_SYSTEM -> "ENCRYPTING_FILE_SYSTEM"
      | ENDORSEMENT_KEY_CERTIFICATE -> "ENDORSEMENT_KEY_CERTIFICATE"
      | FILE_RECOVERY -> "FILE_RECOVERY"
      | HAL_EXTENSION -> "HAL_EXTENSION"
      | IP_SECURITY_END_SYSTEM -> "IP_SECURITY_END_SYSTEM"
      | IP_SECURITY_IKE_INTERMEDIATE -> "IP_SECURITY_IKE_INTERMEDIATE"
      | IP_SECURITY_TUNNEL_TERMINATION -> "IP_SECURITY_TUNNEL_TERMINATION"
      | IP_SECURITY_USER -> "IP_SECURITY_USER"
      | ISOLATED_USER_MODE -> "ISOLATED_USER_MODE"
      | KDC_AUTHENTICATION -> "KDC_AUTHENTICATION"
      | KERNEL_MODE_CODE_SIGNING -> "KERNEL_MODE_CODE_SIGNING"
      | KEY_PACK_LICENSES -> "KEY_PACK_LICENSES"
      | KEY_RECOVERY -> "KEY_RECOVERY"
      | KEY_RECOVERY_AGENT -> "KEY_RECOVERY_AGENT"
      | LICENSE_SERVER_VERIFICATION -> "LICENSE_SERVER_VERIFICATION"
      | LIFETIME_SIGNING -> "LIFETIME_SIGNING"
      | MICROSOFT_PUBLISHER -> "MICROSOFT_PUBLISHER"
      | MICROSOFT_TIME_STAMPING -> "MICROSOFT_TIME_STAMPING"
      | MICROSOFT_TRUST_LIST_SIGNING -> "MICROSOFT_TRUST_LIST_SIGNING"
      | OCSP_SIGNING -> "OCSP_SIGNING"
      | OEM_WINDOWS_SYSTEM_COMPONENT_VERIFICATION ->
          "OEM_WINDOWS_SYSTEM_COMPONENT_VERIFICATION"
      | PLATFORM_CERTIFICATE -> "PLATFORM_CERTIFICATE"
      | PREVIEW_BUILD_SIGNING -> "PREVIEW_BUILD_SIGNING"
      | PRIVATE_KEY_ARCHIVAL -> "PRIVATE_KEY_ARCHIVAL"
      | PROTECTED_PROCESS_LIGHT_VERIFICATION ->
          "PROTECTED_PROCESS_LIGHT_VERIFICATION"
      | PROTECTED_PROCESS_VERIFICATION -> "PROTECTED_PROCESS_VERIFICATION"
      | QUALIFIED_SUBORDINATION -> "QUALIFIED_SUBORDINATION"
      | REVOKED_LIST_SIGNER -> "REVOKED_LIST_SIGNER"
      | ROOT_PROGRAM_AUTO_UPDATE_CA_REVOCATION ->
          "ROOT_PROGRAM_AUTO_UPDATE_CA_REVOCATION"
      | ROOT_PROGRAM_AUTO_UPDATE_END_REVOCATION ->
          "ROOT_PROGRAM_AUTO_UPDATE_END_REVOCATION"
      | ROOT_PROGRAM_NO_OSCP_FAILOVER_TO_CRL ->
          "ROOT_PROGRAM_NO_OSCP_FAILOVER_TO_CRL"
      | ROOT_LIST_SIGNER -> "ROOT_LIST_SIGNER"
      | SECURE_EMAIL -> "SECURE_EMAIL"
      | SERVER_AUTHENTICATION -> "SERVER_AUTHENTICATION"
      | SMART_CARD_LOGIN -> "SMART_CARD_LOGIN"
      | SPC_ENCRYPTED_DIGEST_RETRY_COUNT ->
          "SPC_ENCRYPTED_DIGEST_RETRY_COUNT"
      | SPC_RELAXED_PE_MARKER_CHECK -> "SPC_RELAXED_PE_MARKER_CHECK"
      | TIME_STAMPING -> "TIME_STAMPING"
      | WINDOWS_HARDWARE_DRIVER_ATTESTED_VERIFICATION ->
          "WINDOWS_HARDWARE_DRIVER_ATTESTED_VERIFICATION"
      | WINDOWS_HARDWARE_DRIVER_EXTENDED_VERIFICATION ->
          "WINDOWS_HARDWARE_DRIVER_EXTENDED_VERIFICATION"
      | WINDOWS_HARDWARE_DRIVER_VERIFICATION ->
          "WINDOWS_HARDWARE_DRIVER_VERIFICATION"
      | WINDOWS_HELLO_RECOVERY_KEY_ENCRYPTION ->
          "WINDOWS_HELLO_RECOVERY_KEY_ENCRYPTION"
      | WINDOWS_KITS_COMPONENT -> "WINDOWS_KITS_COMPONENT"
      | WINDOWS_RT_VERIFICATION -> "WINDOWS_RT_VERIFICATION"
      | WINDOWS_SOFTWARE_EXTENSION_VERIFICATION ->
          "WINDOWS_SOFTWARE_EXTENSION_VERIFICATION"
      | WINDOWS_STORE -> "WINDOWS_STORE"
      | WINDOWS_SYSTEM_COMPONENT_VERIFICATION ->
          "WINDOWS_SYSTEM_COMPONENT_VERIFICATION"
      | WINDOWS_TCB_COMPONENT -> "WINDOWS_TCB_COMPONENT"
      | WINDOWS_THIRD_PARTY_APPLICATION_COMPONENT ->
          "WINDOWS_THIRD_PARTY_APPLICATION_COMPONENT"
      | WINDOWS_UPDATE -> "WINDOWS_UPDATE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ALL_APPLICATION_POLICIES" -> ALL_APPLICATION_POLICIES
      | "ANY_PURPOSE" -> ANY_PURPOSE
      | "ATTESTATION_IDENTITY_KEY_CERTIFICATE" ->
          ATTESTATION_IDENTITY_KEY_CERTIFICATE
      | "CERTIFICATE_REQUEST_AGENT" -> CERTIFICATE_REQUEST_AGENT
      | "CLIENT_AUTHENTICATION" -> CLIENT_AUTHENTICATION
      | "CODE_SIGNING" -> CODE_SIGNING
      | "CTL_USAGE" -> CTL_USAGE
      | "DIGITAL_RIGHTS" -> DIGITAL_RIGHTS
      | "DIRECTORY_SERVICE_EMAIL_REPLICATION" ->
          DIRECTORY_SERVICE_EMAIL_REPLICATION
      | "DISALLOWED_LIST" -> DISALLOWED_LIST
      | "DNS_SERVER_TRUST" -> DNS_SERVER_TRUST
      | "DOCUMENT_ENCRYPTION" -> DOCUMENT_ENCRYPTION
      | "DOCUMENT_SIGNING" -> DOCUMENT_SIGNING
      | "DYNAMIC_CODE_GENERATOR" -> DYNAMIC_CODE_GENERATOR
      | "EARLY_LAUNCH_ANTIMALWARE_DRIVER" -> EARLY_LAUNCH_ANTIMALWARE_DRIVER
      | "EMBEDDED_WINDOWS_SYSTEM_COMPONENT_VERIFICATION" ->
          EMBEDDED_WINDOWS_SYSTEM_COMPONENT_VERIFICATION
      | "ENCLAVE" -> ENCLAVE
      | "ENCRYPTING_FILE_SYSTEM" -> ENCRYPTING_FILE_SYSTEM
      | "ENDORSEMENT_KEY_CERTIFICATE" -> ENDORSEMENT_KEY_CERTIFICATE
      | "FILE_RECOVERY" -> FILE_RECOVERY
      | "HAL_EXTENSION" -> HAL_EXTENSION
      | "IP_SECURITY_END_SYSTEM" -> IP_SECURITY_END_SYSTEM
      | "IP_SECURITY_IKE_INTERMEDIATE" -> IP_SECURITY_IKE_INTERMEDIATE
      | "IP_SECURITY_TUNNEL_TERMINATION" -> IP_SECURITY_TUNNEL_TERMINATION
      | "IP_SECURITY_USER" -> IP_SECURITY_USER
      | "ISOLATED_USER_MODE" -> ISOLATED_USER_MODE
      | "KDC_AUTHENTICATION" -> KDC_AUTHENTICATION
      | "KERNEL_MODE_CODE_SIGNING" -> KERNEL_MODE_CODE_SIGNING
      | "KEY_PACK_LICENSES" -> KEY_PACK_LICENSES
      | "KEY_RECOVERY" -> KEY_RECOVERY
      | "KEY_RECOVERY_AGENT" -> KEY_RECOVERY_AGENT
      | "LICENSE_SERVER_VERIFICATION" -> LICENSE_SERVER_VERIFICATION
      | "LIFETIME_SIGNING" -> LIFETIME_SIGNING
      | "MICROSOFT_PUBLISHER" -> MICROSOFT_PUBLISHER
      | "MICROSOFT_TIME_STAMPING" -> MICROSOFT_TIME_STAMPING
      | "MICROSOFT_TRUST_LIST_SIGNING" -> MICROSOFT_TRUST_LIST_SIGNING
      | "OCSP_SIGNING" -> OCSP_SIGNING
      | "OEM_WINDOWS_SYSTEM_COMPONENT_VERIFICATION" ->
          OEM_WINDOWS_SYSTEM_COMPONENT_VERIFICATION
      | "PLATFORM_CERTIFICATE" -> PLATFORM_CERTIFICATE
      | "PREVIEW_BUILD_SIGNING" -> PREVIEW_BUILD_SIGNING
      | "PRIVATE_KEY_ARCHIVAL" -> PRIVATE_KEY_ARCHIVAL
      | "PROTECTED_PROCESS_LIGHT_VERIFICATION" ->
          PROTECTED_PROCESS_LIGHT_VERIFICATION
      | "PROTECTED_PROCESS_VERIFICATION" -> PROTECTED_PROCESS_VERIFICATION
      | "QUALIFIED_SUBORDINATION" -> QUALIFIED_SUBORDINATION
      | "REVOKED_LIST_SIGNER" -> REVOKED_LIST_SIGNER
      | "ROOT_PROGRAM_AUTO_UPDATE_CA_REVOCATION" ->
          ROOT_PROGRAM_AUTO_UPDATE_CA_REVOCATION
      | "ROOT_PROGRAM_AUTO_UPDATE_END_REVOCATION" ->
          ROOT_PROGRAM_AUTO_UPDATE_END_REVOCATION
      | "ROOT_PROGRAM_NO_OSCP_FAILOVER_TO_CRL" ->
          ROOT_PROGRAM_NO_OSCP_FAILOVER_TO_CRL
      | "ROOT_LIST_SIGNER" -> ROOT_LIST_SIGNER
      | "SECURE_EMAIL" -> SECURE_EMAIL
      | "SERVER_AUTHENTICATION" -> SERVER_AUTHENTICATION
      | "SMART_CARD_LOGIN" -> SMART_CARD_LOGIN
      | "SPC_ENCRYPTED_DIGEST_RETRY_COUNT" ->
          SPC_ENCRYPTED_DIGEST_RETRY_COUNT
      | "SPC_RELAXED_PE_MARKER_CHECK" -> SPC_RELAXED_PE_MARKER_CHECK
      | "TIME_STAMPING" -> TIME_STAMPING
      | "WINDOWS_HARDWARE_DRIVER_ATTESTED_VERIFICATION" ->
          WINDOWS_HARDWARE_DRIVER_ATTESTED_VERIFICATION
      | "WINDOWS_HARDWARE_DRIVER_EXTENDED_VERIFICATION" ->
          WINDOWS_HARDWARE_DRIVER_EXTENDED_VERIFICATION
      | "WINDOWS_HARDWARE_DRIVER_VERIFICATION" ->
          WINDOWS_HARDWARE_DRIVER_VERIFICATION
      | "WINDOWS_HELLO_RECOVERY_KEY_ENCRYPTION" ->
          WINDOWS_HELLO_RECOVERY_KEY_ENCRYPTION
      | "WINDOWS_KITS_COMPONENT" -> WINDOWS_KITS_COMPONENT
      | "WINDOWS_RT_VERIFICATION" -> WINDOWS_RT_VERIFICATION
      | "WINDOWS_SOFTWARE_EXTENSION_VERIFICATION" ->
          WINDOWS_SOFTWARE_EXTENSION_VERIFICATION
      | "WINDOWS_STORE" -> WINDOWS_STORE
      | "WINDOWS_SYSTEM_COMPONENT_VERIFICATION" ->
          WINDOWS_SYSTEM_COMPONENT_VERIFICATION
      | "WINDOWS_TCB_COMPONENT" -> WINDOWS_TCB_COMPONENT
      | "WINDOWS_THIRD_PARTY_APPLICATION_COMPONENT" ->
          WINDOWS_THIRD_PARTY_APPLICATION_COMPONENT
      | "WINDOWS_UPDATE" -> WINDOWS_UPDATE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ApplicationPolicyType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ApplicationPolicyType" j)
    let to_json = simple_to_json to_value
  end
module CustomObjectIdentifier =
  struct
    type nonrec t = string
    let context_ = "CustomObjectIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([0-2])\\.([0-9]|([0-3][0-9]))(\\.([0-9]+)){0,126}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CustomObjectIdentifier" j
    let to_json = simple_to_json to_value
  end
module ApplicationPolicy =
  struct
    type nonrec t =
      {
      policyObjectIdentifier: CustomObjectIdentifier.t option
        [@ocaml.doc "The object identifier (OID) of an application policy."];
      policyType: ApplicationPolicyType.t option
        [@ocaml.doc "The type of application policy"]}
    let make ?policyObjectIdentifier =
      fun ?policyType -> fun () -> { policyObjectIdentifier; policyType }
    let to_value x =
      structure_to_value
        [("PolicyObjectIdentifier",
           (Option.map x.policyObjectIdentifier
              ~f:CustomObjectIdentifier.to_value));
        ("PolicyType",
          (Option.map x.policyType ~f:ApplicationPolicyType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyType =
        (Option.map ~f:ApplicationPolicyType.of_xml)
          (Xml.child xml_arg0 "PolicyType") in
      let policyObjectIdentifier =
        (Option.map ~f:CustomObjectIdentifier.of_xml)
          (Xml.child xml_arg0 "PolicyObjectIdentifier") in
      make ?policyType ?policyObjectIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyType =
        field_map json__ "PolicyType" ApplicationPolicyType.of_json in
      let policyObjectIdentifier =
        field_map json__ "PolicyObjectIdentifier"
          CustomObjectIdentifier.of_json in
      make ?policyType ?policyObjectIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Application policies describe what the certificate can be used for."]
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ValidityPeriodPeriodLong =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int64_max i ~max:8766000L) >>=
             (fun () -> check_int64_min i ~min:1L));
        i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module ValidityPeriodType =
  struct
    type nonrec t =
      | HOURS 
      | DAYS 
      | WEEKS 
      | MONTHS 
      | YEARS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | HOURS -> "HOURS"
      | DAYS -> "DAYS"
      | WEEKS -> "WEEKS"
      | MONTHS -> "MONTHS"
      | YEARS -> "YEARS"
      | Non_static_id s -> s
    let of_string =
      function
      | "HOURS" -> HOURS
      | "DAYS" -> DAYS
      | "WEEKS" -> WEEKS
      | "MONTHS" -> MONTHS
      | "YEARS" -> YEARS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidityPeriodType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ValidityPeriodType" j)
    let to_json = simple_to_json to_value
  end
module ApplicationPolicyList =
  struct
    type nonrec t = ApplicationPolicy.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ApplicationPolicy.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ApplicationPolicy.of_xml)
    let of_json j =
      list_of_json ~kind:"ApplicationPolicyList"
        ~of_json:ApplicationPolicy.of_json j
    let to_json v = composed_to_json to_value v
  end
module KeyUsageFlags =
  struct
    type nonrec t =
      {
      dataEncipherment: Boolean.t option
        [@ocaml.doc
          "DataEncipherment is asserted when the subject public key is used for directly enciphering raw user data without the use of an intermediate symmetric cipher."];
      digitalSignature: Boolean.t option
        [@ocaml.doc
          "The digitalSignature is asserted when the subject public key is used for verifying digital signatures."];
      keyAgreement: Boolean.t option
        [@ocaml.doc
          "KeyAgreement is asserted when the subject public key is used for key agreement."];
      keyEncipherment: Boolean.t option
        [@ocaml.doc
          "KeyEncipherment is asserted when the subject public key is used for enciphering private or secret keys, i.e., for key transport."];
      nonRepudiation: Boolean.t option
        [@ocaml.doc
          "NonRepudiation is asserted when the subject public key is used to verify digital signatures."]}
    let make ?dataEncipherment =
      fun ?digitalSignature ->
        fun ?keyAgreement ->
          fun ?keyEncipherment ->
            fun ?nonRepudiation ->
              fun () ->
                {
                  dataEncipherment;
                  digitalSignature;
                  keyAgreement;
                  keyEncipherment;
                  nonRepudiation
                }
    let to_value x =
      structure_to_value
        [("DataEncipherment",
           (Option.map x.dataEncipherment ~f:Boolean.to_value));
        ("DigitalSignature",
          (Option.map x.digitalSignature ~f:Boolean.to_value));
        ("KeyAgreement", (Option.map x.keyAgreement ~f:Boolean.to_value));
        ("KeyEncipherment",
          (Option.map x.keyEncipherment ~f:Boolean.to_value));
        ("NonRepudiation", (Option.map x.nonRepudiation ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nonRepudiation =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "NonRepudiation") in
      let keyEncipherment =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "KeyEncipherment") in
      let keyAgreement =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "KeyAgreement") in
      let digitalSignature =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "DigitalSignature") in
      let dataEncipherment =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "DataEncipherment") in
      make ?nonRepudiation ?keyEncipherment ?keyAgreement ?digitalSignature
        ?dataEncipherment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nonRepudiation = field_map json__ "NonRepudiation" Boolean.of_json in
      let keyEncipherment =
        field_map json__ "KeyEncipherment" Boolean.of_json in
      let keyAgreement = field_map json__ "KeyAgreement" Boolean.of_json in
      let digitalSignature =
        field_map json__ "DigitalSignature" Boolean.of_json in
      let dataEncipherment =
        field_map json__ "DataEncipherment" Boolean.of_json in
      make ?nonRepudiation ?keyEncipherment ?keyAgreement ?digitalSignature
        ?dataEncipherment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The key usage flags represent the purpose (e.g., encipherment, signature) of the key contained in the certificate."]
module CryptoProvidersListMemberString =
  struct
    type nonrec t = string
    let context_ = "CryptoProvidersListMemberString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CryptoProvidersListMemberString" j
    let to_json = simple_to_json to_value
  end
module KeyUsagePropertyFlags =
  struct
    type nonrec t =
      {
      decrypt: Boolean.t option
        [@ocaml.doc "Allows key for encryption and decryption."];
      keyAgreement: Boolean.t option
        [@ocaml.doc "Allows key exchange without encryption."];
      sign: Boolean.t option
        [@ocaml.doc "Allow key use for digital signature."]}
    let make ?decrypt =
      fun ?keyAgreement ->
        fun ?sign -> fun () -> { decrypt; keyAgreement; sign }
    let to_value x =
      structure_to_value
        [("Decrypt", (Option.map x.decrypt ~f:Boolean.to_value));
        ("KeyAgreement", (Option.map x.keyAgreement ~f:Boolean.to_value));
        ("Sign", (Option.map x.sign ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sign = (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Sign") in
      let keyAgreement =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "KeyAgreement") in
      let decrypt =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Decrypt") in
      make ?sign ?keyAgreement ?decrypt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sign = field_map json__ "Sign" Boolean.of_json in
      let keyAgreement = field_map json__ "KeyAgreement" Boolean.of_json in
      let decrypt = field_map json__ "Decrypt" Boolean.of_json in
      make ?sign ?keyAgreement ?decrypt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies key usage."]
module KeyUsagePropertyType =
  struct
    type nonrec t =
      | ALL 
      | Non_static_id of string 
    let make i = i
    let to_string = function | ALL -> "ALL" | Non_static_id s -> s
    let of_string = function | "ALL" -> ALL | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration KeyUsagePropertyType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"KeyUsagePropertyType" j)
    let to_json = simple_to_json to_value
  end
module ValidityPeriod =
  struct
    type nonrec t =
      {
      period: ValidityPeriodPeriodLong.t
        [@ocaml.doc "The numeric value for the validity period."];
      periodType: ValidityPeriodType.t
        [@ocaml.doc
          "The unit of time. You can select hours, days, weeks, months, and years."]}
    let context_ = "ValidityPeriod"
    let make ~period = fun ~periodType -> fun () -> { period; periodType }
    let to_value x =
      structure_to_value
        [("Period", (Some (ValidityPeriodPeriodLong.to_value x.period)));
        ("PeriodType", (Some (ValidityPeriodType.to_value x.periodType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let periodType =
        ValidityPeriodType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PeriodType") in
      let period =
        ValidityPeriodPeriodLong.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Period") in
      make ~periodType ~period ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let periodType =
        field_map_exn json__ "PeriodType" ValidityPeriodType.of_json in
      let period =
        field_map_exn json__ "Period" ValidityPeriodPeriodLong.of_json in
      make ~periodType ~period ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information describing the end of the validity period of the certificate. This parameter sets the \226\128\156Not After\226\128\157 date for the certificate. Certificate validity is the period of time during which a certificate is valid. Validity can be expressed as an explicit date and time when the certificate expires, or as a span of time after issuance, stated in hours, days, months, or years. For more information, see Validity in RFC 5280. This value is unaffected when ValidityNotBefore is also specified. For example, if Validity is set to 20 days in the future, the certificate will expire 20 days from issuance time regardless of the ValidityNotBefore value."]
module ApplicationPolicies =
  struct
    type nonrec t =
      {
      critical: Boolean.t option
        [@ocaml.doc "Marks the application policy extension as critical."];
      policies: ApplicationPolicyList.t
        [@ocaml.doc
          "Application policies describe what the certificate can be used for."]}
    let context_ = "ApplicationPolicies"
    let make ?critical = fun ~policies -> fun () -> { critical; policies }
    let to_value x =
      structure_to_value
        [("Critical", (Option.map x.critical ~f:Boolean.to_value));
        ("Policies", (Some (ApplicationPolicyList.to_value x.policies)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policies =
        ApplicationPolicyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Policies") in
      let critical =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Critical") in
      make ~policies ?critical ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policies =
        field_map_exn json__ "Policies" ApplicationPolicyList.of_json in
      let critical = field_map json__ "Critical" Boolean.of_json in
      make ~policies ?critical ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Application policies describe what the certificate can be used for."]
module KeyUsage =
  struct
    type nonrec t =
      {
      critical: Boolean.t option
        [@ocaml.doc "Sets the key usage extension to critical."];
      usageFlags: KeyUsageFlags.t
        [@ocaml.doc
          "The key usage flags represent the purpose (e.g., encipherment, signature) of the key contained in the certificate."]}
    let context_ = "KeyUsage"
    let make ?critical =
      fun ~usageFlags -> fun () -> { critical; usageFlags }
    let to_value x =
      structure_to_value
        [("Critical", (Option.map x.critical ~f:Boolean.to_value));
        ("UsageFlags", (Some (KeyUsageFlags.to_value x.usageFlags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let usageFlags =
        KeyUsageFlags.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "UsageFlags") in
      let critical =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Critical") in
      make ~usageFlags ?critical ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let usageFlags =
        field_map_exn json__ "UsageFlags" KeyUsageFlags.of_json in
      let critical = field_map json__ "Critical" Boolean.of_json in
      make ~usageFlags ?critical ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The key usage extension defines the purpose (e.g., encipherment, signature) of the key contained in the certificate."]
module CryptoProvidersList =
  struct
    type nonrec t = CryptoProvidersListMemberString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CryptoProvidersListMemberString.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CryptoProvidersListMemberString.of_xml)
    let of_json j =
      list_of_json ~kind:"CryptoProvidersList"
        ~of_json:CryptoProvidersListMemberString.of_json j
    let to_json v = composed_to_json to_value v
  end
module KeySpec =
  struct
    type nonrec t =
      | KEY_EXCHANGE 
      | SIGNATURE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | KEY_EXCHANGE -> "KEY_EXCHANGE"
      | SIGNATURE -> "SIGNATURE"
      | Non_static_id s -> s
    let of_string =
      function
      | "KEY_EXCHANGE" -> KEY_EXCHANGE
      | "SIGNATURE" -> SIGNATURE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration KeySpec" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"KeySpec" j)
    let to_json = simple_to_json to_value
  end
module PrivateKeyAttributesV2MinimalKeyLengthInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for PrivateKeyAttributesV2MinimalKeyLengthInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ClientCompatibilityV2 =
  struct
    type nonrec t =
      | WINDOWS_SERVER_2003 
      | WINDOWS_SERVER_2008 
      | WINDOWS_SERVER_2008_R2 
      | WINDOWS_SERVER_2012 
      | WINDOWS_SERVER_2012_R2 
      | WINDOWS_SERVER_2016 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | WINDOWS_SERVER_2003 -> "WINDOWS_SERVER_2003"
      | WINDOWS_SERVER_2008 -> "WINDOWS_SERVER_2008"
      | WINDOWS_SERVER_2008_R2 -> "WINDOWS_SERVER_2008_R2"
      | WINDOWS_SERVER_2012 -> "WINDOWS_SERVER_2012"
      | WINDOWS_SERVER_2012_R2 -> "WINDOWS_SERVER_2012_R2"
      | WINDOWS_SERVER_2016 -> "WINDOWS_SERVER_2016"
      | Non_static_id s -> s
    let of_string =
      function
      | "WINDOWS_SERVER_2003" -> WINDOWS_SERVER_2003
      | "WINDOWS_SERVER_2008" -> WINDOWS_SERVER_2008
      | "WINDOWS_SERVER_2008_R2" -> WINDOWS_SERVER_2008_R2
      | "WINDOWS_SERVER_2012" -> WINDOWS_SERVER_2012
      | "WINDOWS_SERVER_2012_R2" -> WINDOWS_SERVER_2012_R2
      | "WINDOWS_SERVER_2016" -> WINDOWS_SERVER_2016
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ClientCompatibilityV2" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ClientCompatibilityV2" j)
    let to_json = simple_to_json to_value
  end
module TemplateName =
  struct
    type nonrec t = string
    let context_ = "TemplateName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(?!^\\s+$)((?![\\x5c'\\x2b,;<=>#\\x22])([\\x20-\\x7E]))+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TemplateName" j
    let to_json = simple_to_json to_value
  end
module KeyUsageProperty =
  struct
    type nonrec t =
      {
      propertyFlags: KeyUsagePropertyFlags.t option
        [@ocaml.doc
          "You can specify key usage for encryption, key agreement, and signature. You can use property flags or property type but not both."];
      propertyType: KeyUsagePropertyType.t option
        [@ocaml.doc
          "You can specify all key usages using property type ALL. You can use property type or property flags but not both."]}
    let make ?propertyFlags =
      fun ?propertyType -> fun () -> { propertyFlags; propertyType }
    let to_value x =
      structure_to_value
        [("PropertyFlags",
           (Option.map x.propertyFlags ~f:KeyUsagePropertyFlags.to_value));
        ("PropertyType",
          (Option.map x.propertyType ~f:KeyUsagePropertyType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let propertyType =
        (Option.map ~f:KeyUsagePropertyType.of_xml)
          (Xml.child xml_arg0 "PropertyType") in
      let propertyFlags =
        (Option.map ~f:KeyUsagePropertyFlags.of_xml)
          (Xml.child xml_arg0 "PropertyFlags") in
      make ?propertyType ?propertyFlags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let propertyType =
        field_map json__ "PropertyType" KeyUsagePropertyType.of_json in
      let propertyFlags =
        field_map json__ "PropertyFlags" KeyUsagePropertyFlags.of_json in
      make ?propertyType ?propertyFlags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The key usage property defines the purpose of the private key contained in the certificate. You can specify specific purposes using property flags or all by using property type ALL."]
module PrivateKeyAlgorithm =
  struct
    type nonrec t =
      | RSA 
      | ECDH_P256 
      | ECDH_P384 
      | ECDH_P521 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | RSA -> "RSA"
      | ECDH_P256 -> "ECDH_P256"
      | ECDH_P384 -> "ECDH_P384"
      | ECDH_P521 -> "ECDH_P521"
      | Non_static_id s -> s
    let of_string =
      function
      | "RSA" -> RSA
      | "ECDH_P256" -> ECDH_P256
      | "ECDH_P384" -> ECDH_P384
      | "ECDH_P521" -> ECDH_P521
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PrivateKeyAlgorithm" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PrivateKeyAlgorithm" j)
    let to_json = simple_to_json to_value
  end
module PrivateKeyAttributesV3MinimalKeyLengthInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for PrivateKeyAttributesV3MinimalKeyLengthInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ClientCompatibilityV3 =
  struct
    type nonrec t =
      | WINDOWS_SERVER_2008 
      | WINDOWS_SERVER_2008_R2 
      | WINDOWS_SERVER_2012 
      | WINDOWS_SERVER_2012_R2 
      | WINDOWS_SERVER_2016 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | WINDOWS_SERVER_2008 -> "WINDOWS_SERVER_2008"
      | WINDOWS_SERVER_2008_R2 -> "WINDOWS_SERVER_2008_R2"
      | WINDOWS_SERVER_2012 -> "WINDOWS_SERVER_2012"
      | WINDOWS_SERVER_2012_R2 -> "WINDOWS_SERVER_2012_R2"
      | WINDOWS_SERVER_2016 -> "WINDOWS_SERVER_2016"
      | Non_static_id s -> s
    let of_string =
      function
      | "WINDOWS_SERVER_2008" -> WINDOWS_SERVER_2008
      | "WINDOWS_SERVER_2008_R2" -> WINDOWS_SERVER_2008_R2
      | "WINDOWS_SERVER_2012" -> WINDOWS_SERVER_2012
      | "WINDOWS_SERVER_2012_R2" -> WINDOWS_SERVER_2012_R2
      | "WINDOWS_SERVER_2016" -> WINDOWS_SERVER_2016
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ClientCompatibilityV3" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ClientCompatibilityV3" j)
    let to_json = simple_to_json to_value
  end
module PrivateKeyAttributesV4MinimalKeyLengthInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for PrivateKeyAttributesV4MinimalKeyLengthInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ClientCompatibilityV4 =
  struct
    type nonrec t =
      | WINDOWS_SERVER_2012 
      | WINDOWS_SERVER_2012_R2 
      | WINDOWS_SERVER_2016 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | WINDOWS_SERVER_2012 -> "WINDOWS_SERVER_2012"
      | WINDOWS_SERVER_2012_R2 -> "WINDOWS_SERVER_2012_R2"
      | WINDOWS_SERVER_2016 -> "WINDOWS_SERVER_2016"
      | Non_static_id s -> s
    let of_string =
      function
      | "WINDOWS_SERVER_2012" -> WINDOWS_SERVER_2012
      | "WINDOWS_SERVER_2012_R2" -> WINDOWS_SERVER_2012_R2
      | "WINDOWS_SERVER_2016" -> WINDOWS_SERVER_2016
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ClientCompatibilityV4" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ClientCompatibilityV4" j)
    let to_json = simple_to_json to_value
  end
module CertificateValidity =
  struct
    type nonrec t =
      {
      renewalPeriod: ValidityPeriod.t
        [@ocaml.doc
          "Renewal period is the period of time before certificate expiration when a new certificate will be requested."];
      validityPeriod: ValidityPeriod.t
        [@ocaml.doc
          "Information describing the end of the validity period of the certificate. This parameter sets the \226\128\156Not After\226\128\157 date for the certificate. Certificate validity is the period of time during which a certificate is valid. Validity can be expressed as an explicit date and time when the certificate expires, or as a span of time after issuance, stated in days, months, or years. For more information, see Validity in RFC 5280. This value is unaffected when ValidityNotBefore is also specified. For example, if Validity is set to 20 days in the future, the certificate will expire 20 days from issuance time regardless of the ValidityNotBefore value."]}
    let context_ = "CertificateValidity"
    let make ~renewalPeriod =
      fun ~validityPeriod -> fun () -> { renewalPeriod; validityPeriod }
    let to_value x =
      structure_to_value
        [("RenewalPeriod", (Some (ValidityPeriod.to_value x.renewalPeriod)));
        ("ValidityPeriod", (Some (ValidityPeriod.to_value x.validityPeriod)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let validityPeriod =
        ValidityPeriod.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ValidityPeriod") in
      let renewalPeriod =
        ValidityPeriod.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RenewalPeriod") in
      make ~validityPeriod ~renewalPeriod ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let validityPeriod =
        field_map_exn json__ "ValidityPeriod" ValidityPeriod.of_json in
      let renewalPeriod =
        field_map_exn json__ "RenewalPeriod" ValidityPeriod.of_json in
      make ~validityPeriod ~renewalPeriod ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information describing the end of the validity period of the certificate. This parameter sets the \226\128\156Not After\226\128\157 date for the certificate. Certificate validity is the period of time during which a certificate is valid. Validity can be expressed as an explicit date and time when the certificate expires, or as a span of time after issuance, stated in days, months, or years. For more information, see Validity in RFC 5280. This value is unaffected when ValidityNotBefore is also specified. For example, if Validity is set to 20 days in the future, the certificate will expire 20 days from issuance time regardless of the ValidityNotBefore value."]
module EnrollmentFlagsV2 =
  struct
    type nonrec t =
      {
      enableKeyReuseOnNtTokenKeysetStorageFull: Boolean.t option
        [@ocaml.doc "Allow renewal using the same key."];
      includeSymmetricAlgorithms: Boolean.t option
        [@ocaml.doc "Include symmetric algorithms allowed by the subject."];
      noSecurityExtension: Boolean.t option
        [@ocaml.doc
          "This flag instructs the CA to not include the security extension szOID_NTDS_CA_SECURITY_EXT (OID:1.3.6.1.4.1.311.25.2), as specified in \\[MS-WCCE\\] sections 2.2.2.7.7.4 and 3.2.2.6.2.1.4.5.9, in the issued certificate. This addresses a Windows Kerberos elevation-of-privilege vulnerability."];
      removeInvalidCertificateFromPersonalStore: Boolean.t option
        [@ocaml.doc
          "Delete expired or revoked certificates instead of archiving them."];
      userInteractionRequired: Boolean.t option
        [@ocaml.doc
          "Require user interaction when the subject is enrolled and the private key associated with the certificate is used."]}
    let make ?enableKeyReuseOnNtTokenKeysetStorageFull =
      fun ?includeSymmetricAlgorithms ->
        fun ?noSecurityExtension ->
          fun ?removeInvalidCertificateFromPersonalStore ->
            fun ?userInteractionRequired ->
              fun () ->
                {
                  enableKeyReuseOnNtTokenKeysetStorageFull;
                  includeSymmetricAlgorithms;
                  noSecurityExtension;
                  removeInvalidCertificateFromPersonalStore;
                  userInteractionRequired
                }
    let to_value x =
      structure_to_value
        [("EnableKeyReuseOnNtTokenKeysetStorageFull",
           (Option.map x.enableKeyReuseOnNtTokenKeysetStorageFull
              ~f:Boolean.to_value));
        ("IncludeSymmetricAlgorithms",
          (Option.map x.includeSymmetricAlgorithms ~f:Boolean.to_value));
        ("NoSecurityExtension",
          (Option.map x.noSecurityExtension ~f:Boolean.to_value));
        ("RemoveInvalidCertificateFromPersonalStore",
          (Option.map x.removeInvalidCertificateFromPersonalStore
             ~f:Boolean.to_value));
        ("UserInteractionRequired",
          (Option.map x.userInteractionRequired ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userInteractionRequired =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "UserInteractionRequired") in
      let removeInvalidCertificateFromPersonalStore =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RemoveInvalidCertificateFromPersonalStore") in
      let noSecurityExtension =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "NoSecurityExtension") in
      let includeSymmetricAlgorithms =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeSymmetricAlgorithms") in
      let enableKeyReuseOnNtTokenKeysetStorageFull =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "EnableKeyReuseOnNtTokenKeysetStorageFull") in
      make ?userInteractionRequired
        ?removeInvalidCertificateFromPersonalStore ?noSecurityExtension
        ?includeSymmetricAlgorithms ?enableKeyReuseOnNtTokenKeysetStorageFull
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let userInteractionRequired =
        field_map json__ "UserInteractionRequired" Boolean.of_json in
      let removeInvalidCertificateFromPersonalStore =
        field_map json__ "RemoveInvalidCertificateFromPersonalStore"
          Boolean.of_json in
      let noSecurityExtension =
        field_map json__ "NoSecurityExtension" Boolean.of_json in
      let includeSymmetricAlgorithms =
        field_map json__ "IncludeSymmetricAlgorithms" Boolean.of_json in
      let enableKeyReuseOnNtTokenKeysetStorageFull =
        field_map json__ "EnableKeyReuseOnNtTokenKeysetStorageFull"
          Boolean.of_json in
      make ?userInteractionRequired
        ?removeInvalidCertificateFromPersonalStore ?noSecurityExtension
        ?includeSymmetricAlgorithms ?enableKeyReuseOnNtTokenKeysetStorageFull
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Template configurations for v2 template schema."]
module ExtensionsV2 =
  struct
    type nonrec t =
      {
      applicationPolicies: ApplicationPolicies.t option
        [@ocaml.doc
          "Application policies specify what the certificate is used for and its purpose."];
      keyUsage: KeyUsage.t
        [@ocaml.doc
          "The key usage extension defines the purpose (e.g., encipherment, signature, certificate signing) of the key contained in the certificate."]}
    let context_ = "ExtensionsV2"
    let make ?applicationPolicies =
      fun ~keyUsage -> fun () -> { applicationPolicies; keyUsage }
    let to_value x =
      structure_to_value
        [("ApplicationPolicies",
           (Option.map x.applicationPolicies ~f:ApplicationPolicies.to_value));
        ("KeyUsage", (Some (KeyUsage.to_value x.keyUsage)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keyUsage =
        KeyUsage.of_xml (Xml.child_exn ~context:context_ xml_arg0 "KeyUsage") in
      let applicationPolicies =
        (Option.map ~f:ApplicationPolicies.of_xml)
          (Xml.child xml_arg0 "ApplicationPolicies") in
      make ~keyUsage ?applicationPolicies ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let keyUsage = field_map_exn json__ "KeyUsage" KeyUsage.of_json in
      let applicationPolicies =
        field_map json__ "ApplicationPolicies" ApplicationPolicies.of_json in
      make ~keyUsage ?applicationPolicies ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Certificate extensions for v2 template schema"]
module GeneralFlagsV2 =
  struct
    type nonrec t =
      {
      autoEnrollment: Boolean.t option
        [@ocaml.doc
          "Allows certificate issuance using autoenrollment. Set to TRUE to allow autoenrollment."];
      machineType: Boolean.t option
        [@ocaml.doc
          "Defines if the template is for machines or users. Set to TRUE if the template is for machines. Set to FALSE if the template is for users."]}
    let make ?autoEnrollment =
      fun ?machineType -> fun () -> { autoEnrollment; machineType }
    let to_value x =
      structure_to_value
        [("AutoEnrollment",
           (Option.map x.autoEnrollment ~f:Boolean.to_value));
        ("MachineType", (Option.map x.machineType ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let machineType =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "MachineType") in
      let autoEnrollment =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "AutoEnrollment") in
      make ?machineType ?autoEnrollment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let machineType = field_map json__ "MachineType" Boolean.of_json in
      let autoEnrollment = field_map json__ "AutoEnrollment" Boolean.of_json in
      make ?machineType ?autoEnrollment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "General flags for v2 template schema that defines if the template is for a machine or a user and if the template can be issued using autoenrollment."]
module PrivateKeyAttributesV2 =
  struct
    type nonrec t =
      {
      cryptoProviders: CryptoProvidersList.t option
        [@ocaml.doc
          "Defines the cryptographic providers used to generate the private key."];
      keySpec: KeySpec.t
        [@ocaml.doc
          "Defines the purpose of the private key. Set it to \"KEY_EXCHANGE\" or \"SIGNATURE\" value."];
      minimalKeyLength: PrivateKeyAttributesV2MinimalKeyLengthInteger.t
        [@ocaml.doc "Set the minimum key length of the private key."]}
    let context_ = "PrivateKeyAttributesV2"
    let make ?cryptoProviders =
      fun ~keySpec ->
        fun ~minimalKeyLength ->
          fun () -> { cryptoProviders; keySpec; minimalKeyLength }
    let to_value x =
      structure_to_value
        [("CryptoProviders",
           (Option.map x.cryptoProviders ~f:CryptoProvidersList.to_value));
        ("KeySpec", (Some (KeySpec.to_value x.keySpec)));
        ("MinimalKeyLength",
          (Some
             (PrivateKeyAttributesV2MinimalKeyLengthInteger.to_value
                x.minimalKeyLength)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let minimalKeyLength =
        PrivateKeyAttributesV2MinimalKeyLengthInteger.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MinimalKeyLength") in
      let keySpec =
        KeySpec.of_xml (Xml.child_exn ~context:context_ xml_arg0 "KeySpec") in
      let cryptoProviders =
        (Option.map ~f:CryptoProvidersList.of_xml)
          (Xml.child xml_arg0 "CryptoProviders") in
      make ~minimalKeyLength ~keySpec ?cryptoProviders ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let minimalKeyLength =
        field_map_exn json__ "MinimalKeyLength"
          PrivateKeyAttributesV2MinimalKeyLengthInteger.of_json in
      let keySpec = field_map_exn json__ "KeySpec" KeySpec.of_json in
      let cryptoProviders =
        field_map json__ "CryptoProviders" CryptoProvidersList.of_json in
      make ~minimalKeyLength ~keySpec ?cryptoProviders ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Defines the attributes of the private key."]
module PrivateKeyFlagsV2 =
  struct
    type nonrec t =
      {
      clientVersion: ClientCompatibilityV2.t
        [@ocaml.doc "Defines the minimum client compatibility."];
      exportableKey: Boolean.t option
        [@ocaml.doc "Allows the private key to be exported."];
      strongKeyProtectionRequired: Boolean.t option
        [@ocaml.doc
          "Require user input when using the private key for enrollment."]}
    let context_ = "PrivateKeyFlagsV2"
    let make ?exportableKey =
      fun ?strongKeyProtectionRequired ->
        fun ~clientVersion ->
          fun () ->
            { exportableKey; strongKeyProtectionRequired; clientVersion }
    let to_value x =
      structure_to_value
        [("ClientVersion",
           (Some (ClientCompatibilityV2.to_value x.clientVersion)));
        ("ExportableKey", (Option.map x.exportableKey ~f:Boolean.to_value));
        ("StrongKeyProtectionRequired",
          (Option.map x.strongKeyProtectionRequired ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let strongKeyProtectionRequired =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "StrongKeyProtectionRequired") in
      let exportableKey =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ExportableKey") in
      let clientVersion =
        ClientCompatibilityV2.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVersion") in
      make ?strongKeyProtectionRequired ?exportableKey ~clientVersion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let strongKeyProtectionRequired =
        field_map json__ "StrongKeyProtectionRequired" Boolean.of_json in
      let exportableKey = field_map json__ "ExportableKey" Boolean.of_json in
      let clientVersion =
        field_map_exn json__ "ClientVersion" ClientCompatibilityV2.of_json in
      make ?strongKeyProtectionRequired ?exportableKey ~clientVersion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Private key flags for v2 templates specify the client compatibility, if the private key can be exported, and if user input is required when using a private key."]
module SubjectNameFlagsV2 =
  struct
    type nonrec t =
      {
      requireCommonName: Boolean.t option
        [@ocaml.doc "Include the common name in the subject name."];
      requireDirectoryPath: Boolean.t option
        [@ocaml.doc "Include the directory path in the subject name."];
      requireDnsAsCn: Boolean.t option
        [@ocaml.doc "Include the DNS as common name in the subject name."];
      requireEmail: Boolean.t option
        [@ocaml.doc "Include the subject's email in the subject name."];
      sanRequireDirectoryGuid: Boolean.t option
        [@ocaml.doc
          "Include the globally unique identifier (GUID) in the subject alternate name."];
      sanRequireDns: Boolean.t option
        [@ocaml.doc "Include the DNS in the subject alternate name."];
      sanRequireDomainDns: Boolean.t option
        [@ocaml.doc "Include the domain DNS in the subject alternate name."];
      sanRequireEmail: Boolean.t option
        [@ocaml.doc
          "Include the subject's email in the subject alternate name."];
      sanRequireSpn: Boolean.t option
        [@ocaml.doc
          "Include the service principal name (SPN) in the subject alternate name."];
      sanRequireUpn: Boolean.t option
        [@ocaml.doc
          "Include the user principal name (UPN) in the subject alternate name."]}
    let make ?requireCommonName =
      fun ?requireDirectoryPath ->
        fun ?requireDnsAsCn ->
          fun ?requireEmail ->
            fun ?sanRequireDirectoryGuid ->
              fun ?sanRequireDns ->
                fun ?sanRequireDomainDns ->
                  fun ?sanRequireEmail ->
                    fun ?sanRequireSpn ->
                      fun ?sanRequireUpn ->
                        fun () ->
                          {
                            requireCommonName;
                            requireDirectoryPath;
                            requireDnsAsCn;
                            requireEmail;
                            sanRequireDirectoryGuid;
                            sanRequireDns;
                            sanRequireDomainDns;
                            sanRequireEmail;
                            sanRequireSpn;
                            sanRequireUpn
                          }
    let to_value x =
      structure_to_value
        [("RequireCommonName",
           (Option.map x.requireCommonName ~f:Boolean.to_value));
        ("RequireDirectoryPath",
          (Option.map x.requireDirectoryPath ~f:Boolean.to_value));
        ("RequireDnsAsCn", (Option.map x.requireDnsAsCn ~f:Boolean.to_value));
        ("RequireEmail", (Option.map x.requireEmail ~f:Boolean.to_value));
        ("SanRequireDirectoryGuid",
          (Option.map x.sanRequireDirectoryGuid ~f:Boolean.to_value));
        ("SanRequireDns", (Option.map x.sanRequireDns ~f:Boolean.to_value));
        ("SanRequireDomainDns",
          (Option.map x.sanRequireDomainDns ~f:Boolean.to_value));
        ("SanRequireEmail",
          (Option.map x.sanRequireEmail ~f:Boolean.to_value));
        ("SanRequireSpn", (Option.map x.sanRequireSpn ~f:Boolean.to_value));
        ("SanRequireUpn", (Option.map x.sanRequireUpn ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sanRequireUpn =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SanRequireUpn") in
      let sanRequireSpn =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SanRequireSpn") in
      let sanRequireEmail =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SanRequireEmail") in
      let sanRequireDomainDns =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "SanRequireDomainDns") in
      let sanRequireDns =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SanRequireDns") in
      let sanRequireDirectoryGuid =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "SanRequireDirectoryGuid") in
      let requireEmail =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "RequireEmail") in
      let requireDnsAsCn =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "RequireDnsAsCn") in
      let requireDirectoryPath =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RequireDirectoryPath") in
      let requireCommonName =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RequireCommonName") in
      make ?sanRequireUpn ?sanRequireSpn ?sanRequireEmail
        ?sanRequireDomainDns ?sanRequireDns ?sanRequireDirectoryGuid
        ?requireEmail ?requireDnsAsCn ?requireDirectoryPath
        ?requireCommonName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sanRequireUpn = field_map json__ "SanRequireUpn" Boolean.of_json in
      let sanRequireSpn = field_map json__ "SanRequireSpn" Boolean.of_json in
      let sanRequireEmail =
        field_map json__ "SanRequireEmail" Boolean.of_json in
      let sanRequireDomainDns =
        field_map json__ "SanRequireDomainDns" Boolean.of_json in
      let sanRequireDns = field_map json__ "SanRequireDns" Boolean.of_json in
      let sanRequireDirectoryGuid =
        field_map json__ "SanRequireDirectoryGuid" Boolean.of_json in
      let requireEmail = field_map json__ "RequireEmail" Boolean.of_json in
      let requireDnsAsCn = field_map json__ "RequireDnsAsCn" Boolean.of_json in
      let requireDirectoryPath =
        field_map json__ "RequireDirectoryPath" Boolean.of_json in
      let requireCommonName =
        field_map json__ "RequireCommonName" Boolean.of_json in
      make ?sanRequireUpn ?sanRequireSpn ?sanRequireEmail
        ?sanRequireDomainDns ?sanRequireDns ?sanRequireDirectoryGuid
        ?requireEmail ?requireDnsAsCn ?requireDirectoryPath
        ?requireCommonName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information to include in the subject name and alternate subject name of the certificate. The subject name can be common name, directory path, DNS as common name, or left blank. You can optionally include email to the subject name for user templates. If you leave the subject name blank then you must set a subject alternate name. The subject alternate name (SAN) can include globally unique identifier (GUID), DNS, domain DNS, email, service principal name (SPN), and user principal name (UPN). You can leave the SAN blank. If you leave the SAN blank, then you must set a subject name."]
module TemplateNameList =
  struct
    type nonrec t = TemplateName.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TemplateName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TemplateName.of_xml)
    let of_json j =
      list_of_json ~kind:"TemplateNameList" ~of_json:TemplateName.of_json j
    let to_json v = composed_to_json to_value v
  end
module EnrollmentFlagsV3 =
  struct
    type nonrec t =
      {
      enableKeyReuseOnNtTokenKeysetStorageFull: Boolean.t option
        [@ocaml.doc "Allow renewal using the same key."];
      includeSymmetricAlgorithms: Boolean.t option
        [@ocaml.doc "Include symmetric algorithms allowed by the subject."];
      noSecurityExtension: Boolean.t option
        [@ocaml.doc
          "This flag instructs the CA to not include the security extension szOID_NTDS_CA_SECURITY_EXT (OID:1.3.6.1.4.1.311.25.2), as specified in \\[MS-WCCE\\] sections 2.2.2.7.7.4 and 3.2.2.6.2.1.4.5.9, in the issued certificate. This addresses a Windows Kerberos elevation-of-privilege vulnerability."];
      removeInvalidCertificateFromPersonalStore: Boolean.t option
        [@ocaml.doc
          "Delete expired or revoked certificates instead of archiving them."];
      userInteractionRequired: Boolean.t option
        [@ocaml.doc
          "Require user interaction when the subject is enrolled and the private key associated with the certificate is used."]}
    let make ?enableKeyReuseOnNtTokenKeysetStorageFull =
      fun ?includeSymmetricAlgorithms ->
        fun ?noSecurityExtension ->
          fun ?removeInvalidCertificateFromPersonalStore ->
            fun ?userInteractionRequired ->
              fun () ->
                {
                  enableKeyReuseOnNtTokenKeysetStorageFull;
                  includeSymmetricAlgorithms;
                  noSecurityExtension;
                  removeInvalidCertificateFromPersonalStore;
                  userInteractionRequired
                }
    let to_value x =
      structure_to_value
        [("EnableKeyReuseOnNtTokenKeysetStorageFull",
           (Option.map x.enableKeyReuseOnNtTokenKeysetStorageFull
              ~f:Boolean.to_value));
        ("IncludeSymmetricAlgorithms",
          (Option.map x.includeSymmetricAlgorithms ~f:Boolean.to_value));
        ("NoSecurityExtension",
          (Option.map x.noSecurityExtension ~f:Boolean.to_value));
        ("RemoveInvalidCertificateFromPersonalStore",
          (Option.map x.removeInvalidCertificateFromPersonalStore
             ~f:Boolean.to_value));
        ("UserInteractionRequired",
          (Option.map x.userInteractionRequired ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userInteractionRequired =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "UserInteractionRequired") in
      let removeInvalidCertificateFromPersonalStore =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RemoveInvalidCertificateFromPersonalStore") in
      let noSecurityExtension =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "NoSecurityExtension") in
      let includeSymmetricAlgorithms =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeSymmetricAlgorithms") in
      let enableKeyReuseOnNtTokenKeysetStorageFull =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "EnableKeyReuseOnNtTokenKeysetStorageFull") in
      make ?userInteractionRequired
        ?removeInvalidCertificateFromPersonalStore ?noSecurityExtension
        ?includeSymmetricAlgorithms ?enableKeyReuseOnNtTokenKeysetStorageFull
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let userInteractionRequired =
        field_map json__ "UserInteractionRequired" Boolean.of_json in
      let removeInvalidCertificateFromPersonalStore =
        field_map json__ "RemoveInvalidCertificateFromPersonalStore"
          Boolean.of_json in
      let noSecurityExtension =
        field_map json__ "NoSecurityExtension" Boolean.of_json in
      let includeSymmetricAlgorithms =
        field_map json__ "IncludeSymmetricAlgorithms" Boolean.of_json in
      let enableKeyReuseOnNtTokenKeysetStorageFull =
        field_map json__ "EnableKeyReuseOnNtTokenKeysetStorageFull"
          Boolean.of_json in
      make ?userInteractionRequired
        ?removeInvalidCertificateFromPersonalStore ?noSecurityExtension
        ?includeSymmetricAlgorithms ?enableKeyReuseOnNtTokenKeysetStorageFull
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Template configurations for v3 template schema."]
module ExtensionsV3 =
  struct
    type nonrec t =
      {
      applicationPolicies: ApplicationPolicies.t option
        [@ocaml.doc
          "Application policies specify what the certificate is used for and its purpose."];
      keyUsage: KeyUsage.t
        [@ocaml.doc
          "The key usage extension defines the purpose (e.g., encipherment, signature, certificate signing) of the key contained in the certificate."]}
    let context_ = "ExtensionsV3"
    let make ?applicationPolicies =
      fun ~keyUsage -> fun () -> { applicationPolicies; keyUsage }
    let to_value x =
      structure_to_value
        [("ApplicationPolicies",
           (Option.map x.applicationPolicies ~f:ApplicationPolicies.to_value));
        ("KeyUsage", (Some (KeyUsage.to_value x.keyUsage)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keyUsage =
        KeyUsage.of_xml (Xml.child_exn ~context:context_ xml_arg0 "KeyUsage") in
      let applicationPolicies =
        (Option.map ~f:ApplicationPolicies.of_xml)
          (Xml.child xml_arg0 "ApplicationPolicies") in
      make ~keyUsage ?applicationPolicies ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let keyUsage = field_map_exn json__ "KeyUsage" KeyUsage.of_json in
      let applicationPolicies =
        field_map json__ "ApplicationPolicies" ApplicationPolicies.of_json in
      make ~keyUsage ?applicationPolicies ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Certificate extensions for v3 template schema"]
module GeneralFlagsV3 =
  struct
    type nonrec t =
      {
      autoEnrollment: Boolean.t option
        [@ocaml.doc
          "Allows certificate issuance using autoenrollment. Set to TRUE to allow autoenrollment."];
      machineType: Boolean.t option
        [@ocaml.doc
          "Defines if the template is for machines or users. Set to TRUE if the template is for machines. Set to FALSE if the template is for users"]}
    let make ?autoEnrollment =
      fun ?machineType -> fun () -> { autoEnrollment; machineType }
    let to_value x =
      structure_to_value
        [("AutoEnrollment",
           (Option.map x.autoEnrollment ~f:Boolean.to_value));
        ("MachineType", (Option.map x.machineType ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let machineType =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "MachineType") in
      let autoEnrollment =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "AutoEnrollment") in
      make ?machineType ?autoEnrollment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let machineType = field_map json__ "MachineType" Boolean.of_json in
      let autoEnrollment = field_map json__ "AutoEnrollment" Boolean.of_json in
      make ?machineType ?autoEnrollment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "General flags for v3 template schema that defines if the template is for a machine or a user and if the template can be issued using autoenrollment."]
module HashAlgorithm =
  struct
    type nonrec t =
      | SHA256 
      | SHA384 
      | SHA512 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SHA256 -> "SHA256"
      | SHA384 -> "SHA384"
      | SHA512 -> "SHA512"
      | Non_static_id s -> s
    let of_string =
      function
      | "SHA256" -> SHA256
      | "SHA384" -> SHA384
      | "SHA512" -> SHA512
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration HashAlgorithm" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"HashAlgorithm" j)
    let to_json = simple_to_json to_value
  end
module PrivateKeyAttributesV3 =
  struct
    type nonrec t =
      {
      algorithm: PrivateKeyAlgorithm.t
        [@ocaml.doc
          "Defines the algorithm used to generate the private key."];
      cryptoProviders: CryptoProvidersList.t option
        [@ocaml.doc
          "Defines the cryptographic providers used to generate the private key."];
      keySpec: KeySpec.t
        [@ocaml.doc
          "Defines the purpose of the private key. Set it to \"KEY_EXCHANGE\" or \"SIGNATURE\" value."];
      keyUsageProperty: KeyUsageProperty.t
        [@ocaml.doc
          "The key usage property defines the purpose of the private key contained in the certificate. You can specify specific purposes using property flags or all by using property type ALL."];
      minimalKeyLength: PrivateKeyAttributesV3MinimalKeyLengthInteger.t
        [@ocaml.doc "Set the minimum key length of the private key."]}
    let context_ = "PrivateKeyAttributesV3"
    let make ?cryptoProviders =
      fun ~algorithm ->
        fun ~keySpec ->
          fun ~keyUsageProperty ->
            fun ~minimalKeyLength ->
              fun () ->
                {
                  cryptoProviders;
                  algorithm;
                  keySpec;
                  keyUsageProperty;
                  minimalKeyLength
                }
    let to_value x =
      structure_to_value
        [("Algorithm", (Some (PrivateKeyAlgorithm.to_value x.algorithm)));
        ("CryptoProviders",
          (Option.map x.cryptoProviders ~f:CryptoProvidersList.to_value));
        ("KeySpec", (Some (KeySpec.to_value x.keySpec)));
        ("KeyUsageProperty",
          (Some (KeyUsageProperty.to_value x.keyUsageProperty)));
        ("MinimalKeyLength",
          (Some
             (PrivateKeyAttributesV3MinimalKeyLengthInteger.to_value
                x.minimalKeyLength)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let minimalKeyLength =
        PrivateKeyAttributesV3MinimalKeyLengthInteger.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MinimalKeyLength") in
      let keyUsageProperty =
        KeyUsageProperty.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "KeyUsageProperty") in
      let keySpec =
        KeySpec.of_xml (Xml.child_exn ~context:context_ xml_arg0 "KeySpec") in
      let cryptoProviders =
        (Option.map ~f:CryptoProvidersList.of_xml)
          (Xml.child xml_arg0 "CryptoProviders") in
      let algorithm =
        PrivateKeyAlgorithm.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Algorithm") in
      make ~minimalKeyLength ~keyUsageProperty ~keySpec ?cryptoProviders
        ~algorithm ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let minimalKeyLength =
        field_map_exn json__ "MinimalKeyLength"
          PrivateKeyAttributesV3MinimalKeyLengthInteger.of_json in
      let keyUsageProperty =
        field_map_exn json__ "KeyUsageProperty" KeyUsageProperty.of_json in
      let keySpec = field_map_exn json__ "KeySpec" KeySpec.of_json in
      let cryptoProviders =
        field_map json__ "CryptoProviders" CryptoProvidersList.of_json in
      let algorithm =
        field_map_exn json__ "Algorithm" PrivateKeyAlgorithm.of_json in
      make ~minimalKeyLength ~keyUsageProperty ~keySpec ?cryptoProviders
        ~algorithm ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Defines the attributes of the private key."]
module PrivateKeyFlagsV3 =
  struct
    type nonrec t =
      {
      clientVersion: ClientCompatibilityV3.t
        [@ocaml.doc "Defines the minimum client compatibility."];
      exportableKey: Boolean.t option
        [@ocaml.doc "Allows the private key to be exported."];
      requireAlternateSignatureAlgorithm: Boolean.t option
        [@ocaml.doc
          "Reguires the PKCS #1 v2.1 signature format for certificates. You should verify that your CA, objects, and applications can accept this signature format."];
      strongKeyProtectionRequired: Boolean.t option
        [@ocaml.doc
          "Requirer user input when using the private key for enrollment."]}
    let context_ = "PrivateKeyFlagsV3"
    let make ?exportableKey =
      fun ?requireAlternateSignatureAlgorithm ->
        fun ?strongKeyProtectionRequired ->
          fun ~clientVersion ->
            fun () ->
              {
                exportableKey;
                requireAlternateSignatureAlgorithm;
                strongKeyProtectionRequired;
                clientVersion
              }
    let to_value x =
      structure_to_value
        [("ClientVersion",
           (Some (ClientCompatibilityV3.to_value x.clientVersion)));
        ("ExportableKey", (Option.map x.exportableKey ~f:Boolean.to_value));
        ("RequireAlternateSignatureAlgorithm",
          (Option.map x.requireAlternateSignatureAlgorithm
             ~f:Boolean.to_value));
        ("StrongKeyProtectionRequired",
          (Option.map x.strongKeyProtectionRequired ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let strongKeyProtectionRequired =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "StrongKeyProtectionRequired") in
      let requireAlternateSignatureAlgorithm =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RequireAlternateSignatureAlgorithm") in
      let exportableKey =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ExportableKey") in
      let clientVersion =
        ClientCompatibilityV3.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVersion") in
      make ?strongKeyProtectionRequired ?requireAlternateSignatureAlgorithm
        ?exportableKey ~clientVersion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let strongKeyProtectionRequired =
        field_map json__ "StrongKeyProtectionRequired" Boolean.of_json in
      let requireAlternateSignatureAlgorithm =
        field_map json__ "RequireAlternateSignatureAlgorithm" Boolean.of_json in
      let exportableKey = field_map json__ "ExportableKey" Boolean.of_json in
      let clientVersion =
        field_map_exn json__ "ClientVersion" ClientCompatibilityV3.of_json in
      make ?strongKeyProtectionRequired ?requireAlternateSignatureAlgorithm
        ?exportableKey ~clientVersion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Private key flags for v3 templates specify the client compatibility, if the private key can be exported, if user input is required when using a private key, and if an alternate signature algorithm should be used."]
module SubjectNameFlagsV3 =
  struct
    type nonrec t =
      {
      requireCommonName: Boolean.t option
        [@ocaml.doc "Include the common name in the subject name."];
      requireDirectoryPath: Boolean.t option
        [@ocaml.doc "Include the directory path in the subject name."];
      requireDnsAsCn: Boolean.t option
        [@ocaml.doc "Include the DNS as common name in the subject name."];
      requireEmail: Boolean.t option
        [@ocaml.doc "Include the subject's email in the subject name."];
      sanRequireDirectoryGuid: Boolean.t option
        [@ocaml.doc
          "Include the globally unique identifier (GUID) in the subject alternate name."];
      sanRequireDns: Boolean.t option
        [@ocaml.doc "Include the DNS in the subject alternate name."];
      sanRequireDomainDns: Boolean.t option
        [@ocaml.doc "Include the domain DNS in the subject alternate name."];
      sanRequireEmail: Boolean.t option
        [@ocaml.doc
          "Include the subject's email in the subject alternate name."];
      sanRequireSpn: Boolean.t option
        [@ocaml.doc
          "Include the service principal name (SPN) in the subject alternate name."];
      sanRequireUpn: Boolean.t option
        [@ocaml.doc
          "Include the user principal name (UPN) in the subject alternate name."]}
    let make ?requireCommonName =
      fun ?requireDirectoryPath ->
        fun ?requireDnsAsCn ->
          fun ?requireEmail ->
            fun ?sanRequireDirectoryGuid ->
              fun ?sanRequireDns ->
                fun ?sanRequireDomainDns ->
                  fun ?sanRequireEmail ->
                    fun ?sanRequireSpn ->
                      fun ?sanRequireUpn ->
                        fun () ->
                          {
                            requireCommonName;
                            requireDirectoryPath;
                            requireDnsAsCn;
                            requireEmail;
                            sanRequireDirectoryGuid;
                            sanRequireDns;
                            sanRequireDomainDns;
                            sanRequireEmail;
                            sanRequireSpn;
                            sanRequireUpn
                          }
    let to_value x =
      structure_to_value
        [("RequireCommonName",
           (Option.map x.requireCommonName ~f:Boolean.to_value));
        ("RequireDirectoryPath",
          (Option.map x.requireDirectoryPath ~f:Boolean.to_value));
        ("RequireDnsAsCn", (Option.map x.requireDnsAsCn ~f:Boolean.to_value));
        ("RequireEmail", (Option.map x.requireEmail ~f:Boolean.to_value));
        ("SanRequireDirectoryGuid",
          (Option.map x.sanRequireDirectoryGuid ~f:Boolean.to_value));
        ("SanRequireDns", (Option.map x.sanRequireDns ~f:Boolean.to_value));
        ("SanRequireDomainDns",
          (Option.map x.sanRequireDomainDns ~f:Boolean.to_value));
        ("SanRequireEmail",
          (Option.map x.sanRequireEmail ~f:Boolean.to_value));
        ("SanRequireSpn", (Option.map x.sanRequireSpn ~f:Boolean.to_value));
        ("SanRequireUpn", (Option.map x.sanRequireUpn ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sanRequireUpn =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SanRequireUpn") in
      let sanRequireSpn =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SanRequireSpn") in
      let sanRequireEmail =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SanRequireEmail") in
      let sanRequireDomainDns =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "SanRequireDomainDns") in
      let sanRequireDns =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SanRequireDns") in
      let sanRequireDirectoryGuid =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "SanRequireDirectoryGuid") in
      let requireEmail =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "RequireEmail") in
      let requireDnsAsCn =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "RequireDnsAsCn") in
      let requireDirectoryPath =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RequireDirectoryPath") in
      let requireCommonName =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RequireCommonName") in
      make ?sanRequireUpn ?sanRequireSpn ?sanRequireEmail
        ?sanRequireDomainDns ?sanRequireDns ?sanRequireDirectoryGuid
        ?requireEmail ?requireDnsAsCn ?requireDirectoryPath
        ?requireCommonName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sanRequireUpn = field_map json__ "SanRequireUpn" Boolean.of_json in
      let sanRequireSpn = field_map json__ "SanRequireSpn" Boolean.of_json in
      let sanRequireEmail =
        field_map json__ "SanRequireEmail" Boolean.of_json in
      let sanRequireDomainDns =
        field_map json__ "SanRequireDomainDns" Boolean.of_json in
      let sanRequireDns = field_map json__ "SanRequireDns" Boolean.of_json in
      let sanRequireDirectoryGuid =
        field_map json__ "SanRequireDirectoryGuid" Boolean.of_json in
      let requireEmail = field_map json__ "RequireEmail" Boolean.of_json in
      let requireDnsAsCn = field_map json__ "RequireDnsAsCn" Boolean.of_json in
      let requireDirectoryPath =
        field_map json__ "RequireDirectoryPath" Boolean.of_json in
      let requireCommonName =
        field_map json__ "RequireCommonName" Boolean.of_json in
      make ?sanRequireUpn ?sanRequireSpn ?sanRequireEmail
        ?sanRequireDomainDns ?sanRequireDns ?sanRequireDirectoryGuid
        ?requireEmail ?requireDnsAsCn ?requireDirectoryPath
        ?requireCommonName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information to include in the subject name and alternate subject name of the certificate. The subject name can be common name, directory path, DNS as common name, or left blank. You can optionally include email to the subject name for user templates. If you leave the subject name blank then you must set a subject alternate name. The subject alternate name (SAN) can include globally unique identifier (GUID), DNS, domain DNS, email, service principal name (SPN), and user principal name (UPN). You can leave the SAN blank. If you leave the SAN blank, then you must set a subject name."]
module EnrollmentFlagsV4 =
  struct
    type nonrec t =
      {
      enableKeyReuseOnNtTokenKeysetStorageFull: Boolean.t option
        [@ocaml.doc "Allow renewal using the same key."];
      includeSymmetricAlgorithms: Boolean.t option
        [@ocaml.doc "Include symmetric algorithms allowed by the subject."];
      noSecurityExtension: Boolean.t option
        [@ocaml.doc
          "This flag instructs the CA to not include the security extension szOID_NTDS_CA_SECURITY_EXT (OID:1.3.6.1.4.1.311.25.2), as specified in \\[MS-WCCE\\] sections 2.2.2.7.7.4 and 3.2.2.6.2.1.4.5.9, in the issued certificate. This addresses a Windows Kerberos elevation-of-privilege vulnerability."];
      removeInvalidCertificateFromPersonalStore: Boolean.t option
        [@ocaml.doc
          "Delete expired or revoked certificates instead of archiving them."];
      userInteractionRequired: Boolean.t option
        [@ocaml.doc
          "Require user interaction when the subject is enrolled and the private key associated with the certificate is used."]}
    let make ?enableKeyReuseOnNtTokenKeysetStorageFull =
      fun ?includeSymmetricAlgorithms ->
        fun ?noSecurityExtension ->
          fun ?removeInvalidCertificateFromPersonalStore ->
            fun ?userInteractionRequired ->
              fun () ->
                {
                  enableKeyReuseOnNtTokenKeysetStorageFull;
                  includeSymmetricAlgorithms;
                  noSecurityExtension;
                  removeInvalidCertificateFromPersonalStore;
                  userInteractionRequired
                }
    let to_value x =
      structure_to_value
        [("EnableKeyReuseOnNtTokenKeysetStorageFull",
           (Option.map x.enableKeyReuseOnNtTokenKeysetStorageFull
              ~f:Boolean.to_value));
        ("IncludeSymmetricAlgorithms",
          (Option.map x.includeSymmetricAlgorithms ~f:Boolean.to_value));
        ("NoSecurityExtension",
          (Option.map x.noSecurityExtension ~f:Boolean.to_value));
        ("RemoveInvalidCertificateFromPersonalStore",
          (Option.map x.removeInvalidCertificateFromPersonalStore
             ~f:Boolean.to_value));
        ("UserInteractionRequired",
          (Option.map x.userInteractionRequired ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userInteractionRequired =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "UserInteractionRequired") in
      let removeInvalidCertificateFromPersonalStore =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RemoveInvalidCertificateFromPersonalStore") in
      let noSecurityExtension =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "NoSecurityExtension") in
      let includeSymmetricAlgorithms =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeSymmetricAlgorithms") in
      let enableKeyReuseOnNtTokenKeysetStorageFull =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "EnableKeyReuseOnNtTokenKeysetStorageFull") in
      make ?userInteractionRequired
        ?removeInvalidCertificateFromPersonalStore ?noSecurityExtension
        ?includeSymmetricAlgorithms ?enableKeyReuseOnNtTokenKeysetStorageFull
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let userInteractionRequired =
        field_map json__ "UserInteractionRequired" Boolean.of_json in
      let removeInvalidCertificateFromPersonalStore =
        field_map json__ "RemoveInvalidCertificateFromPersonalStore"
          Boolean.of_json in
      let noSecurityExtension =
        field_map json__ "NoSecurityExtension" Boolean.of_json in
      let includeSymmetricAlgorithms =
        field_map json__ "IncludeSymmetricAlgorithms" Boolean.of_json in
      let enableKeyReuseOnNtTokenKeysetStorageFull =
        field_map json__ "EnableKeyReuseOnNtTokenKeysetStorageFull"
          Boolean.of_json in
      make ?userInteractionRequired
        ?removeInvalidCertificateFromPersonalStore ?noSecurityExtension
        ?includeSymmetricAlgorithms ?enableKeyReuseOnNtTokenKeysetStorageFull
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Template configurations for v4 template schema."]
module ExtensionsV4 =
  struct
    type nonrec t =
      {
      applicationPolicies: ApplicationPolicies.t option
        [@ocaml.doc
          "Application policies specify what the certificate is used for and its purpose."];
      keyUsage: KeyUsage.t
        [@ocaml.doc
          "The key usage extension defines the purpose (e.g., encipherment, signature) of the key contained in the certificate."]}
    let context_ = "ExtensionsV4"
    let make ?applicationPolicies =
      fun ~keyUsage -> fun () -> { applicationPolicies; keyUsage }
    let to_value x =
      structure_to_value
        [("ApplicationPolicies",
           (Option.map x.applicationPolicies ~f:ApplicationPolicies.to_value));
        ("KeyUsage", (Some (KeyUsage.to_value x.keyUsage)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keyUsage =
        KeyUsage.of_xml (Xml.child_exn ~context:context_ xml_arg0 "KeyUsage") in
      let applicationPolicies =
        (Option.map ~f:ApplicationPolicies.of_xml)
          (Xml.child xml_arg0 "ApplicationPolicies") in
      make ~keyUsage ?applicationPolicies ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let keyUsage = field_map_exn json__ "KeyUsage" KeyUsage.of_json in
      let applicationPolicies =
        field_map json__ "ApplicationPolicies" ApplicationPolicies.of_json in
      make ~keyUsage ?applicationPolicies ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Certificate extensions for v4 template schema"]
module GeneralFlagsV4 =
  struct
    type nonrec t =
      {
      autoEnrollment: Boolean.t option
        [@ocaml.doc
          "Allows certificate issuance using autoenrollment. Set to TRUE to allow autoenrollment."];
      machineType: Boolean.t option
        [@ocaml.doc
          "Defines if the template is for machines or users. Set to TRUE if the template is for machines. Set to FALSE if the template is for users"]}
    let make ?autoEnrollment =
      fun ?machineType -> fun () -> { autoEnrollment; machineType }
    let to_value x =
      structure_to_value
        [("AutoEnrollment",
           (Option.map x.autoEnrollment ~f:Boolean.to_value));
        ("MachineType", (Option.map x.machineType ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let machineType =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "MachineType") in
      let autoEnrollment =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "AutoEnrollment") in
      make ?machineType ?autoEnrollment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let machineType = field_map json__ "MachineType" Boolean.of_json in
      let autoEnrollment = field_map json__ "AutoEnrollment" Boolean.of_json in
      make ?machineType ?autoEnrollment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "General flags for v4 template schema that defines if the template is for a machine or a user and if the template can be issued using autoenrollment."]
module PrivateKeyAttributesV4 =
  struct
    type nonrec t =
      {
      algorithm: PrivateKeyAlgorithm.t option
        [@ocaml.doc
          "Defines the algorithm used to generate the private key."];
      cryptoProviders: CryptoProvidersList.t option
        [@ocaml.doc
          "Defines the cryptographic providers used to generate the private key."];
      keySpec: KeySpec.t
        [@ocaml.doc
          "Defines the purpose of the private key. Set it to \"KEY_EXCHANGE\" or \"SIGNATURE\" value."];
      keyUsageProperty: KeyUsageProperty.t option
        [@ocaml.doc
          "The key usage property defines the purpose of the private key contained in the certificate. You can specify specific purposes using property flags or all by using property type ALL."];
      minimalKeyLength: PrivateKeyAttributesV4MinimalKeyLengthInteger.t
        [@ocaml.doc "Set the minimum key length of the private key."]}
    let context_ = "PrivateKeyAttributesV4"
    let make ?algorithm =
      fun ?cryptoProviders ->
        fun ?keyUsageProperty ->
          fun ~keySpec ->
            fun ~minimalKeyLength ->
              fun () ->
                {
                  algorithm;
                  cryptoProviders;
                  keyUsageProperty;
                  keySpec;
                  minimalKeyLength
                }
    let to_value x =
      structure_to_value
        [("Algorithm",
           (Option.map x.algorithm ~f:PrivateKeyAlgorithm.to_value));
        ("CryptoProviders",
          (Option.map x.cryptoProviders ~f:CryptoProvidersList.to_value));
        ("KeySpec", (Some (KeySpec.to_value x.keySpec)));
        ("KeyUsageProperty",
          (Option.map x.keyUsageProperty ~f:KeyUsageProperty.to_value));
        ("MinimalKeyLength",
          (Some
             (PrivateKeyAttributesV4MinimalKeyLengthInteger.to_value
                x.minimalKeyLength)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let minimalKeyLength =
        PrivateKeyAttributesV4MinimalKeyLengthInteger.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MinimalKeyLength") in
      let keyUsageProperty =
        (Option.map ~f:KeyUsageProperty.of_xml)
          (Xml.child xml_arg0 "KeyUsageProperty") in
      let keySpec =
        KeySpec.of_xml (Xml.child_exn ~context:context_ xml_arg0 "KeySpec") in
      let cryptoProviders =
        (Option.map ~f:CryptoProvidersList.of_xml)
          (Xml.child xml_arg0 "CryptoProviders") in
      let algorithm =
        (Option.map ~f:PrivateKeyAlgorithm.of_xml)
          (Xml.child xml_arg0 "Algorithm") in
      make ~minimalKeyLength ?keyUsageProperty ~keySpec ?cryptoProviders
        ?algorithm ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let minimalKeyLength =
        field_map_exn json__ "MinimalKeyLength"
          PrivateKeyAttributesV4MinimalKeyLengthInteger.of_json in
      let keyUsageProperty =
        field_map json__ "KeyUsageProperty" KeyUsageProperty.of_json in
      let keySpec = field_map_exn json__ "KeySpec" KeySpec.of_json in
      let cryptoProviders =
        field_map json__ "CryptoProviders" CryptoProvidersList.of_json in
      let algorithm =
        field_map json__ "Algorithm" PrivateKeyAlgorithm.of_json in
      make ~minimalKeyLength ?keyUsageProperty ~keySpec ?cryptoProviders
        ?algorithm ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Defines the attributes of the private key."]
module PrivateKeyFlagsV4 =
  struct
    type nonrec t =
      {
      clientVersion: ClientCompatibilityV4.t
        [@ocaml.doc "Defines the minimum client compatibility."];
      exportableKey: Boolean.t option
        [@ocaml.doc "Allows the private key to be exported."];
      requireAlternateSignatureAlgorithm: Boolean.t option
        [@ocaml.doc
          "Requires the PKCS #1 v2.1 signature format for certificates. You should verify that your CA, objects, and applications can accept this signature format."];
      requireSameKeyRenewal: Boolean.t option
        [@ocaml.doc "Renew certificate using the same private key."];
      strongKeyProtectionRequired: Boolean.t option
        [@ocaml.doc
          "Require user input when using the private key for enrollment."];
      useLegacyProvider: Boolean.t option
        [@ocaml.doc
          "Specifies the cryptographic service provider category used to generate private keys. Set to TRUE to use Legacy Cryptographic Service Providers and FALSE to use Key Storage Providers."]}
    let context_ = "PrivateKeyFlagsV4"
    let make ?exportableKey =
      fun ?requireAlternateSignatureAlgorithm ->
        fun ?requireSameKeyRenewal ->
          fun ?strongKeyProtectionRequired ->
            fun ?useLegacyProvider ->
              fun ~clientVersion ->
                fun () ->
                  {
                    exportableKey;
                    requireAlternateSignatureAlgorithm;
                    requireSameKeyRenewal;
                    strongKeyProtectionRequired;
                    useLegacyProvider;
                    clientVersion
                  }
    let to_value x =
      structure_to_value
        [("ClientVersion",
           (Some (ClientCompatibilityV4.to_value x.clientVersion)));
        ("ExportableKey", (Option.map x.exportableKey ~f:Boolean.to_value));
        ("RequireAlternateSignatureAlgorithm",
          (Option.map x.requireAlternateSignatureAlgorithm
             ~f:Boolean.to_value));
        ("RequireSameKeyRenewal",
          (Option.map x.requireSameKeyRenewal ~f:Boolean.to_value));
        ("StrongKeyProtectionRequired",
          (Option.map x.strongKeyProtectionRequired ~f:Boolean.to_value));
        ("UseLegacyProvider",
          (Option.map x.useLegacyProvider ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let useLegacyProvider =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "UseLegacyProvider") in
      let strongKeyProtectionRequired =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "StrongKeyProtectionRequired") in
      let requireSameKeyRenewal =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RequireSameKeyRenewal") in
      let requireAlternateSignatureAlgorithm =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RequireAlternateSignatureAlgorithm") in
      let exportableKey =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ExportableKey") in
      let clientVersion =
        ClientCompatibilityV4.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientVersion") in
      make ?useLegacyProvider ?strongKeyProtectionRequired
        ?requireSameKeyRenewal ?requireAlternateSignatureAlgorithm
        ?exportableKey ~clientVersion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let useLegacyProvider =
        field_map json__ "UseLegacyProvider" Boolean.of_json in
      let strongKeyProtectionRequired =
        field_map json__ "StrongKeyProtectionRequired" Boolean.of_json in
      let requireSameKeyRenewal =
        field_map json__ "RequireSameKeyRenewal" Boolean.of_json in
      let requireAlternateSignatureAlgorithm =
        field_map json__ "RequireAlternateSignatureAlgorithm" Boolean.of_json in
      let exportableKey = field_map json__ "ExportableKey" Boolean.of_json in
      let clientVersion =
        field_map_exn json__ "ClientVersion" ClientCompatibilityV4.of_json in
      make ?useLegacyProvider ?strongKeyProtectionRequired
        ?requireSameKeyRenewal ?requireAlternateSignatureAlgorithm
        ?exportableKey ~clientVersion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Private key flags for v4 templates specify the client compatibility, if the private key can be exported, if user input is required when using a private key, if an alternate signature algorithm should be used, and if certificates are renewed using the same private key."]
module SubjectNameFlagsV4 =
  struct
    type nonrec t =
      {
      requireCommonName: Boolean.t option
        [@ocaml.doc "Include the common name in the subject name."];
      requireDirectoryPath: Boolean.t option
        [@ocaml.doc "Include the directory path in the subject name."];
      requireDnsAsCn: Boolean.t option
        [@ocaml.doc "Include the DNS as common name in the subject name."];
      requireEmail: Boolean.t option
        [@ocaml.doc "Include the subject's email in the subject name."];
      sanRequireDirectoryGuid: Boolean.t option
        [@ocaml.doc
          "Include the globally unique identifier (GUID) in the subject alternate name."];
      sanRequireDns: Boolean.t option
        [@ocaml.doc "Include the DNS in the subject alternate name."];
      sanRequireDomainDns: Boolean.t option
        [@ocaml.doc "Include the domain DNS in the subject alternate name."];
      sanRequireEmail: Boolean.t option
        [@ocaml.doc
          "Include the subject's email in the subject alternate name."];
      sanRequireSpn: Boolean.t option
        [@ocaml.doc
          "Include the service principal name (SPN) in the subject alternate name."];
      sanRequireUpn: Boolean.t option
        [@ocaml.doc
          "Include the user principal name (UPN) in the subject alternate name."]}
    let make ?requireCommonName =
      fun ?requireDirectoryPath ->
        fun ?requireDnsAsCn ->
          fun ?requireEmail ->
            fun ?sanRequireDirectoryGuid ->
              fun ?sanRequireDns ->
                fun ?sanRequireDomainDns ->
                  fun ?sanRequireEmail ->
                    fun ?sanRequireSpn ->
                      fun ?sanRequireUpn ->
                        fun () ->
                          {
                            requireCommonName;
                            requireDirectoryPath;
                            requireDnsAsCn;
                            requireEmail;
                            sanRequireDirectoryGuid;
                            sanRequireDns;
                            sanRequireDomainDns;
                            sanRequireEmail;
                            sanRequireSpn;
                            sanRequireUpn
                          }
    let to_value x =
      structure_to_value
        [("RequireCommonName",
           (Option.map x.requireCommonName ~f:Boolean.to_value));
        ("RequireDirectoryPath",
          (Option.map x.requireDirectoryPath ~f:Boolean.to_value));
        ("RequireDnsAsCn", (Option.map x.requireDnsAsCn ~f:Boolean.to_value));
        ("RequireEmail", (Option.map x.requireEmail ~f:Boolean.to_value));
        ("SanRequireDirectoryGuid",
          (Option.map x.sanRequireDirectoryGuid ~f:Boolean.to_value));
        ("SanRequireDns", (Option.map x.sanRequireDns ~f:Boolean.to_value));
        ("SanRequireDomainDns",
          (Option.map x.sanRequireDomainDns ~f:Boolean.to_value));
        ("SanRequireEmail",
          (Option.map x.sanRequireEmail ~f:Boolean.to_value));
        ("SanRequireSpn", (Option.map x.sanRequireSpn ~f:Boolean.to_value));
        ("SanRequireUpn", (Option.map x.sanRequireUpn ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sanRequireUpn =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SanRequireUpn") in
      let sanRequireSpn =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SanRequireSpn") in
      let sanRequireEmail =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SanRequireEmail") in
      let sanRequireDomainDns =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "SanRequireDomainDns") in
      let sanRequireDns =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SanRequireDns") in
      let sanRequireDirectoryGuid =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "SanRequireDirectoryGuid") in
      let requireEmail =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "RequireEmail") in
      let requireDnsAsCn =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "RequireDnsAsCn") in
      let requireDirectoryPath =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RequireDirectoryPath") in
      let requireCommonName =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RequireCommonName") in
      make ?sanRequireUpn ?sanRequireSpn ?sanRequireEmail
        ?sanRequireDomainDns ?sanRequireDns ?sanRequireDirectoryGuid
        ?requireEmail ?requireDnsAsCn ?requireDirectoryPath
        ?requireCommonName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sanRequireUpn = field_map json__ "SanRequireUpn" Boolean.of_json in
      let sanRequireSpn = field_map json__ "SanRequireSpn" Boolean.of_json in
      let sanRequireEmail =
        field_map json__ "SanRequireEmail" Boolean.of_json in
      let sanRequireDomainDns =
        field_map json__ "SanRequireDomainDns" Boolean.of_json in
      let sanRequireDns = field_map json__ "SanRequireDns" Boolean.of_json in
      let sanRequireDirectoryGuid =
        field_map json__ "SanRequireDirectoryGuid" Boolean.of_json in
      let requireEmail = field_map json__ "RequireEmail" Boolean.of_json in
      let requireDnsAsCn = field_map json__ "RequireDnsAsCn" Boolean.of_json in
      let requireDirectoryPath =
        field_map json__ "RequireDirectoryPath" Boolean.of_json in
      let requireCommonName =
        field_map json__ "RequireCommonName" Boolean.of_json in
      make ?sanRequireUpn ?sanRequireSpn ?sanRequireEmail
        ?sanRequireDomainDns ?sanRequireDns ?sanRequireDirectoryGuid
        ?requireEmail ?requireDnsAsCn ?requireDirectoryPath
        ?requireCommonName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information to include in the subject name and alternate subject name of the certificate. The subject name can be common name, directory path, DNS as common name, or left blank. You can optionally include email to the subject name for user templates. If you leave the subject name blank then you must set a subject alternate name. The subject alternate name (SAN) can include globally unique identifier (GUID), DNS, domain DNS, email, service principal name (SPN), and user principal name (UPN). You can leave the SAN blank. If you leave the SAN blank, then you must set a subject name."]
module SecurityGroupId =
  struct
    type nonrec t = string
    let context_ = "SecurityGroupId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:11) >>=
             (fun () ->
                (check_string_max i ~max:20) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(?:sg-[0-9a-f]{8}|sg-[0-9a-f]{17})$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SecurityGroupId" j
    let to_json = simple_to_json to_value
  end
module TemplateV2 =
  struct
    type nonrec t =
      {
      certificateValidity: CertificateValidity.t
        [@ocaml.doc
          "Certificate validity describes the validity and renewal periods of a certificate."];
      enrollmentFlags: EnrollmentFlagsV2.t
        [@ocaml.doc
          "Enrollment flags describe the enrollment settings for certificates such as using the existing private key and deleting expired or revoked certificates."];
      extensions: ExtensionsV2.t
        [@ocaml.doc
          "Extensions describe the key usage extensions and application policies for a template."];
      generalFlags: GeneralFlagsV2.t
        [@ocaml.doc
          "General flags describe whether the template is used for computers or users and if the template can be used with autoenrollment."];
      privateKeyAttributes: PrivateKeyAttributesV2.t
        [@ocaml.doc
          "Private key attributes allow you to specify the minimal key length, key spec, and cryptographic providers for the private key of a certificate for v2 templates. V2 templates allow you to use Legacy Cryptographic Service Providers."];
      privateKeyFlags: PrivateKeyFlagsV2.t
        [@ocaml.doc
          "Private key flags for v2 templates specify the client compatibility, if the private key can be exported, and if user input is required when using a private key."];
      subjectNameFlags: SubjectNameFlagsV2.t
        [@ocaml.doc
          "Subject name flags describe the subject name and subject alternate name that is included in a certificate."];
      supersededTemplates: TemplateNameList.t option
        [@ocaml.doc
          "List of templates in Active Directory that are superseded by this template."]}
    let context_ = "TemplateV2"
    let make ?supersededTemplates =
      fun ~certificateValidity ->
        fun ~enrollmentFlags ->
          fun ~extensions ->
            fun ~generalFlags ->
              fun ~privateKeyAttributes ->
                fun ~privateKeyFlags ->
                  fun ~subjectNameFlags ->
                    fun () ->
                      {
                        supersededTemplates;
                        certificateValidity;
                        enrollmentFlags;
                        extensions;
                        generalFlags;
                        privateKeyAttributes;
                        privateKeyFlags;
                        subjectNameFlags
                      }
    let to_value x =
      structure_to_value
        [("CertificateValidity",
           (Some (CertificateValidity.to_value x.certificateValidity)));
        ("EnrollmentFlags",
          (Some (EnrollmentFlagsV2.to_value x.enrollmentFlags)));
        ("Extensions", (Some (ExtensionsV2.to_value x.extensions)));
        ("GeneralFlags", (Some (GeneralFlagsV2.to_value x.generalFlags)));
        ("PrivateKeyAttributes",
          (Some (PrivateKeyAttributesV2.to_value x.privateKeyAttributes)));
        ("PrivateKeyFlags",
          (Some (PrivateKeyFlagsV2.to_value x.privateKeyFlags)));
        ("SubjectNameFlags",
          (Some (SubjectNameFlagsV2.to_value x.subjectNameFlags)));
        ("SupersededTemplates",
          (Option.map x.supersededTemplates ~f:TemplateNameList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let supersededTemplates =
        (Option.map ~f:TemplateNameList.of_xml)
          (Xml.child xml_arg0 "SupersededTemplates") in
      let subjectNameFlags =
        SubjectNameFlagsV2.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SubjectNameFlags") in
      let privateKeyFlags =
        PrivateKeyFlagsV2.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrivateKeyFlags") in
      let privateKeyAttributes =
        PrivateKeyAttributesV2.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrivateKeyAttributes") in
      let generalFlags =
        GeneralFlagsV2.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GeneralFlags") in
      let extensions =
        ExtensionsV2.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Extensions") in
      let enrollmentFlags =
        EnrollmentFlagsV2.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EnrollmentFlags") in
      let certificateValidity =
        CertificateValidity.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CertificateValidity") in
      make ?supersededTemplates ~subjectNameFlags ~privateKeyFlags
        ~privateKeyAttributes ~generalFlags ~extensions ~enrollmentFlags
        ~certificateValidity ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let supersededTemplates =
        field_map json__ "SupersededTemplates" TemplateNameList.of_json in
      let subjectNameFlags =
        field_map_exn json__ "SubjectNameFlags" SubjectNameFlagsV2.of_json in
      let privateKeyFlags =
        field_map_exn json__ "PrivateKeyFlags" PrivateKeyFlagsV2.of_json in
      let privateKeyAttributes =
        field_map_exn json__ "PrivateKeyAttributes"
          PrivateKeyAttributesV2.of_json in
      let generalFlags =
        field_map_exn json__ "GeneralFlags" GeneralFlagsV2.of_json in
      let extensions = field_map_exn json__ "Extensions" ExtensionsV2.of_json in
      let enrollmentFlags =
        field_map_exn json__ "EnrollmentFlags" EnrollmentFlagsV2.of_json in
      let certificateValidity =
        field_map_exn json__ "CertificateValidity"
          CertificateValidity.of_json in
      make ?supersededTemplates ~subjectNameFlags ~privateKeyFlags
        ~privateKeyAttributes ~generalFlags ~extensions ~enrollmentFlags
        ~certificateValidity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "v2 template schema that uses Legacy Cryptographic Providers."]
module TemplateV3 =
  struct
    type nonrec t =
      {
      certificateValidity: CertificateValidity.t
        [@ocaml.doc
          "Certificate validity describes the validity and renewal periods of a certificate."];
      enrollmentFlags: EnrollmentFlagsV3.t
        [@ocaml.doc
          "Enrollment flags describe the enrollment settings for certificates such as using the existing private key and deleting expired or revoked certificates."];
      extensions: ExtensionsV3.t
        [@ocaml.doc
          "Extensions describe the key usage extensions and application policies for a template."];
      generalFlags: GeneralFlagsV3.t
        [@ocaml.doc
          "General flags describe whether the template is used for computers or users and if the template can be used with autoenrollment."];
      hashAlgorithm: HashAlgorithm.t
        [@ocaml.doc
          "Specifies the hash algorithm used to hash the private key."];
      privateKeyAttributes: PrivateKeyAttributesV3.t
        [@ocaml.doc
          "Private key attributes allow you to specify the algorithm, minimal key length, key spec, key usage, and cryptographic providers for the private key of a certificate for v3 templates. V3 templates allow you to use Key Storage Providers."];
      privateKeyFlags: PrivateKeyFlagsV3.t
        [@ocaml.doc
          "Private key flags for v3 templates specify the client compatibility, if the private key can be exported, if user input is required when using a private key, and if an alternate signature algorithm should be used."];
      subjectNameFlags: SubjectNameFlagsV3.t
        [@ocaml.doc
          "Subject name flags describe the subject name and subject alternate name that is included in a certificate."];
      supersededTemplates: TemplateNameList.t option
        [@ocaml.doc
          "List of templates in Active Directory that are superseded by this template."]}
    let context_ = "TemplateV3"
    let make ?supersededTemplates =
      fun ~certificateValidity ->
        fun ~enrollmentFlags ->
          fun ~extensions ->
            fun ~generalFlags ->
              fun ~hashAlgorithm ->
                fun ~privateKeyAttributes ->
                  fun ~privateKeyFlags ->
                    fun ~subjectNameFlags ->
                      fun () ->
                        {
                          supersededTemplates;
                          certificateValidity;
                          enrollmentFlags;
                          extensions;
                          generalFlags;
                          hashAlgorithm;
                          privateKeyAttributes;
                          privateKeyFlags;
                          subjectNameFlags
                        }
    let to_value x =
      structure_to_value
        [("CertificateValidity",
           (Some (CertificateValidity.to_value x.certificateValidity)));
        ("EnrollmentFlags",
          (Some (EnrollmentFlagsV3.to_value x.enrollmentFlags)));
        ("Extensions", (Some (ExtensionsV3.to_value x.extensions)));
        ("GeneralFlags", (Some (GeneralFlagsV3.to_value x.generalFlags)));
        ("HashAlgorithm", (Some (HashAlgorithm.to_value x.hashAlgorithm)));
        ("PrivateKeyAttributes",
          (Some (PrivateKeyAttributesV3.to_value x.privateKeyAttributes)));
        ("PrivateKeyFlags",
          (Some (PrivateKeyFlagsV3.to_value x.privateKeyFlags)));
        ("SubjectNameFlags",
          (Some (SubjectNameFlagsV3.to_value x.subjectNameFlags)));
        ("SupersededTemplates",
          (Option.map x.supersededTemplates ~f:TemplateNameList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let supersededTemplates =
        (Option.map ~f:TemplateNameList.of_xml)
          (Xml.child xml_arg0 "SupersededTemplates") in
      let subjectNameFlags =
        SubjectNameFlagsV3.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SubjectNameFlags") in
      let privateKeyFlags =
        PrivateKeyFlagsV3.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrivateKeyFlags") in
      let privateKeyAttributes =
        PrivateKeyAttributesV3.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrivateKeyAttributes") in
      let hashAlgorithm =
        HashAlgorithm.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HashAlgorithm") in
      let generalFlags =
        GeneralFlagsV3.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GeneralFlags") in
      let extensions =
        ExtensionsV3.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Extensions") in
      let enrollmentFlags =
        EnrollmentFlagsV3.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EnrollmentFlags") in
      let certificateValidity =
        CertificateValidity.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CertificateValidity") in
      make ?supersededTemplates ~subjectNameFlags ~privateKeyFlags
        ~privateKeyAttributes ~hashAlgorithm ~generalFlags ~extensions
        ~enrollmentFlags ~certificateValidity ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let supersededTemplates =
        field_map json__ "SupersededTemplates" TemplateNameList.of_json in
      let subjectNameFlags =
        field_map_exn json__ "SubjectNameFlags" SubjectNameFlagsV3.of_json in
      let privateKeyFlags =
        field_map_exn json__ "PrivateKeyFlags" PrivateKeyFlagsV3.of_json in
      let privateKeyAttributes =
        field_map_exn json__ "PrivateKeyAttributes"
          PrivateKeyAttributesV3.of_json in
      let hashAlgorithm =
        field_map_exn json__ "HashAlgorithm" HashAlgorithm.of_json in
      let generalFlags =
        field_map_exn json__ "GeneralFlags" GeneralFlagsV3.of_json in
      let extensions = field_map_exn json__ "Extensions" ExtensionsV3.of_json in
      let enrollmentFlags =
        field_map_exn json__ "EnrollmentFlags" EnrollmentFlagsV3.of_json in
      let certificateValidity =
        field_map_exn json__ "CertificateValidity"
          CertificateValidity.of_json in
      make ?supersededTemplates ~subjectNameFlags ~privateKeyFlags
        ~privateKeyAttributes ~hashAlgorithm ~generalFlags ~extensions
        ~enrollmentFlags ~certificateValidity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "v3 template schema that uses Key Storage Providers."]
module TemplateV4 =
  struct
    type nonrec t =
      {
      certificateValidity: CertificateValidity.t
        [@ocaml.doc
          "Certificate validity describes the validity and renewal periods of a certificate."];
      enrollmentFlags: EnrollmentFlagsV4.t
        [@ocaml.doc
          "Enrollment flags describe the enrollment settings for certificates using the existing private key and deleting expired or revoked certificates."];
      extensions: ExtensionsV4.t
        [@ocaml.doc
          "Extensions describe the key usage extensions and application policies for a template."];
      generalFlags: GeneralFlagsV4.t
        [@ocaml.doc
          "General flags describe whether the template is used for computers or users and if the template can be used with autoenrollment."];
      hashAlgorithm: HashAlgorithm.t option
        [@ocaml.doc
          "Specifies the hash algorithm used to hash the private key. Hash algorithm can only be specified when using Key Storage Providers."];
      privateKeyAttributes: PrivateKeyAttributesV4.t
        [@ocaml.doc
          "Private key attributes allow you to specify the minimal key length, key spec, key usage, and cryptographic providers for the private key of a certificate for v4 templates. V4 templates allow you to use either Key Storage Providers or Legacy Cryptographic Service Providers. You specify the cryptography provider category in private key flags."];
      privateKeyFlags: PrivateKeyFlagsV4.t
        [@ocaml.doc
          "Private key flags for v4 templates specify the client compatibility, if the private key can be exported, if user input is required when using a private key, if an alternate signature algorithm should be used, and if certificates are renewed using the same private key."];
      subjectNameFlags: SubjectNameFlagsV4.t
        [@ocaml.doc
          "Subject name flags describe the subject name and subject alternate name that is included in a certificate."];
      supersededTemplates: TemplateNameList.t option
        [@ocaml.doc
          "List of templates in Active Directory that are superseded by this template."]}
    let context_ = "TemplateV4"
    let make ?hashAlgorithm =
      fun ?supersededTemplates ->
        fun ~certificateValidity ->
          fun ~enrollmentFlags ->
            fun ~extensions ->
              fun ~generalFlags ->
                fun ~privateKeyAttributes ->
                  fun ~privateKeyFlags ->
                    fun ~subjectNameFlags ->
                      fun () ->
                        {
                          hashAlgorithm;
                          supersededTemplates;
                          certificateValidity;
                          enrollmentFlags;
                          extensions;
                          generalFlags;
                          privateKeyAttributes;
                          privateKeyFlags;
                          subjectNameFlags
                        }
    let to_value x =
      structure_to_value
        [("CertificateValidity",
           (Some (CertificateValidity.to_value x.certificateValidity)));
        ("EnrollmentFlags",
          (Some (EnrollmentFlagsV4.to_value x.enrollmentFlags)));
        ("Extensions", (Some (ExtensionsV4.to_value x.extensions)));
        ("GeneralFlags", (Some (GeneralFlagsV4.to_value x.generalFlags)));
        ("HashAlgorithm",
          (Option.map x.hashAlgorithm ~f:HashAlgorithm.to_value));
        ("PrivateKeyAttributes",
          (Some (PrivateKeyAttributesV4.to_value x.privateKeyAttributes)));
        ("PrivateKeyFlags",
          (Some (PrivateKeyFlagsV4.to_value x.privateKeyFlags)));
        ("SubjectNameFlags",
          (Some (SubjectNameFlagsV4.to_value x.subjectNameFlags)));
        ("SupersededTemplates",
          (Option.map x.supersededTemplates ~f:TemplateNameList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let supersededTemplates =
        (Option.map ~f:TemplateNameList.of_xml)
          (Xml.child xml_arg0 "SupersededTemplates") in
      let subjectNameFlags =
        SubjectNameFlagsV4.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SubjectNameFlags") in
      let privateKeyFlags =
        PrivateKeyFlagsV4.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrivateKeyFlags") in
      let privateKeyAttributes =
        PrivateKeyAttributesV4.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrivateKeyAttributes") in
      let hashAlgorithm =
        (Option.map ~f:HashAlgorithm.of_xml)
          (Xml.child xml_arg0 "HashAlgorithm") in
      let generalFlags =
        GeneralFlagsV4.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GeneralFlags") in
      let extensions =
        ExtensionsV4.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Extensions") in
      let enrollmentFlags =
        EnrollmentFlagsV4.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EnrollmentFlags") in
      let certificateValidity =
        CertificateValidity.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CertificateValidity") in
      make ?supersededTemplates ~subjectNameFlags ~privateKeyFlags
        ~privateKeyAttributes ?hashAlgorithm ~generalFlags ~extensions
        ~enrollmentFlags ~certificateValidity ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let supersededTemplates =
        field_map json__ "SupersededTemplates" TemplateNameList.of_json in
      let subjectNameFlags =
        field_map_exn json__ "SubjectNameFlags" SubjectNameFlagsV4.of_json in
      let privateKeyFlags =
        field_map_exn json__ "PrivateKeyFlags" PrivateKeyFlagsV4.of_json in
      let privateKeyAttributes =
        field_map_exn json__ "PrivateKeyAttributes"
          PrivateKeyAttributesV4.of_json in
      let hashAlgorithm =
        field_map json__ "HashAlgorithm" HashAlgorithm.of_json in
      let generalFlags =
        field_map_exn json__ "GeneralFlags" GeneralFlagsV4.of_json in
      let extensions = field_map_exn json__ "Extensions" ExtensionsV4.of_json in
      let enrollmentFlags =
        field_map_exn json__ "EnrollmentFlags" EnrollmentFlagsV4.of_json in
      let certificateValidity =
        field_map_exn json__ "CertificateValidity"
          CertificateValidity.of_json in
      make ?supersededTemplates ~subjectNameFlags ~privateKeyFlags
        ~privateKeyAttributes ?hashAlgorithm ~generalFlags ~extensions
        ~enrollmentFlags ~certificateValidity ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "v4 template schema that can use either Legacy Cryptographic Providers or Key Storage Providers."]
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AccessRight =
  struct
    type nonrec t =
      | ALLOW 
      | DENY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | ALLOW -> "ALLOW" | DENY -> "DENY" | Non_static_id s -> s
    let of_string =
      function | "ALLOW" -> ALLOW | "DENY" -> DENY | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AccessRight" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AccessRight" j)
    let to_json = simple_to_json to_value
  end
module IpAddressType =
  struct
    type nonrec t =
      | IPV4 
      | DUALSTACK 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | IPV4 -> "IPV4"
      | DUALSTACK -> "DUALSTACK"
      | Non_static_id s -> s
    let of_string =
      function
      | "IPV4" -> IPV4
      | "DUALSTACK" -> DUALSTACK
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration IpAddressType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"IpAddressType" j)
    let to_json = simple_to_json to_value
  end
module SecurityGroupIdList =
  struct
    type nonrec t = SecurityGroupId.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:4) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SecurityGroupId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SecurityGroupId.of_xml)
    let of_json j =
      list_of_json ~kind:"SecurityGroupIdList"
        ~of_json:SecurityGroupId.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConnectorArn =
  struct
    type nonrec t = string
    let context_ = "ConnectorArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:5) >>=
             (fun () ->
                (check_string_max i ~max:200) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:[\\w-]+:pca-connector-ad:[\\w-]+:[0-9]+:connector\\/[0-9a-f]{8}(-[0-9a-f]{4}){3}-[0-9a-f]{12}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ConnectorArn" j
    let to_json = simple_to_json to_value
  end
module TemplateArn =
  struct
    type nonrec t = string
    let context_ = "TemplateArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:5) >>=
             (fun () ->
                (check_string_max i ~max:200) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:[\\w-]+:pca-connector-ad:[\\w-]+:[0-9]+:connector\\/[0-9a-f]{8}(-[0-9a-f]{4}){3}-[0-9a-f]{12}\\/template\\/[0-9a-f]{8}(-[0-9a-f]{4}){3}-[0-9a-f]{12}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TemplateArn" j
    let to_json = simple_to_json to_value
  end
module TemplateDefinition =
  struct
    type nonrec t =
      {
      templateV2: TemplateV2.t option
        [@ocaml.doc
          "Template configuration to define the information included in certificates. Define certificate validity and renewal periods, certificate request handling and enrollment options, key usage extensions, application policies, and cryptography settings."];
      templateV3: TemplateV3.t option
        [@ocaml.doc
          "Template configuration to define the information included in certificates. Define certificate validity and renewal periods, certificate request handling and enrollment options, key usage extensions, application policies, and cryptography settings."];
      templateV4: TemplateV4.t option
        [@ocaml.doc
          "Template configuration to define the information included in certificates. Define certificate validity and renewal periods, certificate request handling and enrollment options, key usage extensions, application policies, and cryptography settings."]}
    let make ?templateV2 =
      fun ?templateV3 ->
        fun ?templateV4 -> fun () -> { templateV2; templateV3; templateV4 }
    let to_value x =
      structure_to_value
        [("TemplateV2", (Option.map x.templateV2 ~f:TemplateV2.to_value));
        ("TemplateV3", (Option.map x.templateV3 ~f:TemplateV3.to_value));
        ("TemplateV4", (Option.map x.templateV4 ~f:TemplateV4.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateV4 =
        (Option.map ~f:TemplateV4.of_xml) (Xml.child xml_arg0 "TemplateV4") in
      let templateV3 =
        (Option.map ~f:TemplateV3.of_xml) (Xml.child xml_arg0 "TemplateV3") in
      let templateV2 =
        (Option.map ~f:TemplateV2.of_xml) (Xml.child xml_arg0 "TemplateV2") in
      make ?templateV4 ?templateV3 ?templateV2 ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateV4 = field_map json__ "TemplateV4" TemplateV4.of_json in
      let templateV3 = field_map json__ "TemplateV3" TemplateV3.of_json in
      let templateV2 = field_map json__ "TemplateV2" TemplateV2.of_json in
      make ?templateV4 ?templateV3 ?templateV2 ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Template configuration to define the information included in certificates. Define certificate validity and renewal periods, certificate request handling and enrollment options, key usage extensions, application policies, and cryptography settings."]
module TemplateRevision =
  struct
    type nonrec t =
      {
      majorRevision: Integer.t option
        [@ocaml.doc
          "The revision version of the template. Re-enrolling all certificate holders will increment the major revision."];
      minorRevision: Integer.t option
        [@ocaml.doc
          "The revision version of the template. Re-enrolling all certificate holders will increment the major revision."]}
    let make ?majorRevision =
      fun ?minorRevision -> fun () -> { majorRevision; minorRevision }
    let to_value x =
      structure_to_value
        [("MajorRevision", (Option.map x.majorRevision ~f:Integer.to_value));
        ("MinorRevision", (Option.map x.minorRevision ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let minorRevision =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MinorRevision") in
      let majorRevision =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "MajorRevision") in
      make ?minorRevision ?majorRevision ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let minorRevision = field_map json__ "MinorRevision" Integer.of_json in
      let majorRevision = field_map json__ "MajorRevision" Integer.of_json in
      make ?minorRevision ?majorRevision ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The revision version of the template. Template updates will increment the minor revision. Re-enrolling all certificate holders will increment the major revision."]
module TemplateStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | DELETING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | DELETING -> "DELETING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "DELETING" -> DELETING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TemplateStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TemplateStatus" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module AccessRights =
  struct
    type nonrec t =
      {
      autoEnroll: AccessRight.t option
        [@ocaml.doc
          "Allow or deny an Active Directory group from autoenrolling certificates issued against a template. The Active Directory group must be allowed to enroll to allow autoenrollment"];
      enroll: AccessRight.t option
        [@ocaml.doc
          "Allow or deny an Active Directory group from enrolling certificates issued against a template."]}
    let make ?autoEnroll = fun ?enroll -> fun () -> { autoEnroll; enroll }
    let to_value x =
      structure_to_value
        [("AutoEnroll", (Option.map x.autoEnroll ~f:AccessRight.to_value));
        ("Enroll", (Option.map x.enroll ~f:AccessRight.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enroll =
        (Option.map ~f:AccessRight.of_xml) (Xml.child xml_arg0 "Enroll") in
      let autoEnroll =
        (Option.map ~f:AccessRight.of_xml) (Xml.child xml_arg0 "AutoEnroll") in
      make ?enroll ?autoEnroll ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let enroll = field_map json__ "Enroll" AccessRight.of_json in
      let autoEnroll = field_map json__ "AutoEnroll" AccessRight.of_json in
      make ?enroll ?autoEnroll ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allow or deny permissions for an Active Directory group to enroll or autoenroll certificates for a template."]
module DisplayName =
  struct
    type nonrec t = string
    let context_ = "DisplayName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"^[\\x20-\\x7E]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DisplayName" j
    let to_json = simple_to_json to_value
  end
module GroupSecurityIdentifier =
  struct
    type nonrec t = string
    let context_ = "GroupSecurityIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:7) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^S-[0-9]-([0-9]+-){1,14}[0-9]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GroupSecurityIdentifier" j
    let to_json = simple_to_json to_value
  end
module DirectoryRegistrationArn =
  struct
    type nonrec t = string
    let context_ = "DirectoryRegistrationArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:5) >>=
             (fun () ->
                (check_string_max i ~max:200) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:[\\w-]+:pca-connector-ad:[\\w-]+:[0-9]+:directory-registration\\/d-[0-9a-f]{10}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DirectoryRegistrationArn" j
    let to_json = simple_to_json to_value
  end
module ServicePrincipalNameStatus =
  struct
    type nonrec t =
      | CREATING 
      | ACTIVE 
      | DELETING 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATING -> "CREATING"
      | ACTIVE -> "ACTIVE"
      | DELETING -> "DELETING"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATING" -> CREATING
      | "ACTIVE" -> ACTIVE
      | "DELETING" -> DELETING
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ServicePrincipalNameStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ServicePrincipalNameStatus" j)
    let to_json = simple_to_json to_value
  end
module ServicePrincipalNameStatusReason =
  struct
    type nonrec t =
      | DIRECTORY_ACCESS_DENIED 
      | DIRECTORY_NOT_REACHABLE 
      | DIRECTORY_RESOURCE_NOT_FOUND 
      | SPN_EXISTS_ON_DIFFERENT_AD_OBJECT 
      | SPN_LIMIT_EXCEEDED 
      | INTERNAL_FAILURE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DIRECTORY_ACCESS_DENIED -> "DIRECTORY_ACCESS_DENIED"
      | DIRECTORY_NOT_REACHABLE -> "DIRECTORY_NOT_REACHABLE"
      | DIRECTORY_RESOURCE_NOT_FOUND -> "DIRECTORY_RESOURCE_NOT_FOUND"
      | SPN_EXISTS_ON_DIFFERENT_AD_OBJECT ->
          "SPN_EXISTS_ON_DIFFERENT_AD_OBJECT"
      | SPN_LIMIT_EXCEEDED -> "SPN_LIMIT_EXCEEDED"
      | INTERNAL_FAILURE -> "INTERNAL_FAILURE"
      | Non_static_id s -> s
    let of_string =
      function
      | "DIRECTORY_ACCESS_DENIED" -> DIRECTORY_ACCESS_DENIED
      | "DIRECTORY_NOT_REACHABLE" -> DIRECTORY_NOT_REACHABLE
      | "DIRECTORY_RESOURCE_NOT_FOUND" -> DIRECTORY_RESOURCE_NOT_FOUND
      | "SPN_EXISTS_ON_DIFFERENT_AD_OBJECT" ->
          SPN_EXISTS_ON_DIFFERENT_AD_OBJECT
      | "SPN_LIMIT_EXCEEDED" -> SPN_LIMIT_EXCEEDED
      | "INTERNAL_FAILURE" -> INTERNAL_FAILURE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ServicePrincipalNameStatusReason"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ServicePrincipalNameStatusReason" j)
    let to_json = simple_to_json to_value
  end
module DirectoryId =
  struct
    type nonrec t = string
    let context_ = "DirectoryId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"^d-[0-9a-f]{10}$"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DirectoryId" j
    let to_json = simple_to_json to_value
  end
module DirectoryRegistrationStatus =
  struct
    type nonrec t =
      | CREATING 
      | ACTIVE 
      | DELETING 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATING -> "CREATING"
      | ACTIVE -> "ACTIVE"
      | DELETING -> "DELETING"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATING" -> CREATING
      | "ACTIVE" -> ACTIVE
      | "DELETING" -> DELETING
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DirectoryRegistrationStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"DirectoryRegistrationStatus" j)
    let to_json = simple_to_json to_value
  end
module DirectoryRegistrationStatusReason =
  struct
    type nonrec t =
      | DIRECTORY_ACCESS_DENIED 
      | DIRECTORY_RESOURCE_NOT_FOUND 
      | DIRECTORY_NOT_ACTIVE 
      | DIRECTORY_NOT_REACHABLE 
      | DIRECTORY_TYPE_NOT_SUPPORTED 
      | INTERNAL_FAILURE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DIRECTORY_ACCESS_DENIED -> "DIRECTORY_ACCESS_DENIED"
      | DIRECTORY_RESOURCE_NOT_FOUND -> "DIRECTORY_RESOURCE_NOT_FOUND"
      | DIRECTORY_NOT_ACTIVE -> "DIRECTORY_NOT_ACTIVE"
      | DIRECTORY_NOT_REACHABLE -> "DIRECTORY_NOT_REACHABLE"
      | DIRECTORY_TYPE_NOT_SUPPORTED -> "DIRECTORY_TYPE_NOT_SUPPORTED"
      | INTERNAL_FAILURE -> "INTERNAL_FAILURE"
      | Non_static_id s -> s
    let of_string =
      function
      | "DIRECTORY_ACCESS_DENIED" -> DIRECTORY_ACCESS_DENIED
      | "DIRECTORY_RESOURCE_NOT_FOUND" -> DIRECTORY_RESOURCE_NOT_FOUND
      | "DIRECTORY_NOT_ACTIVE" -> DIRECTORY_NOT_ACTIVE
      | "DIRECTORY_NOT_REACHABLE" -> DIRECTORY_NOT_REACHABLE
      | "DIRECTORY_TYPE_NOT_SUPPORTED" -> DIRECTORY_TYPE_NOT_SUPPORTED
      | "INTERNAL_FAILURE" -> INTERNAL_FAILURE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DirectoryRegistrationStatusReason"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"DirectoryRegistrationStatusReason" j)
    let to_json = simple_to_json to_value
  end
module CertificateAuthorityArn =
  struct
    type nonrec t = string
    let context_ = "CertificateAuthorityArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:5) >>=
             (fun () ->
                (check_string_max i ~max:200) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:[\\w-]+:acm-pca:[\\w-]+:[0-9]+:certificate-authority\\/[0-9a-f]{8}(-[0-9a-f]{4}){3}-[0-9a-f]{12}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CertificateAuthorityArn" j
    let to_json = simple_to_json to_value
  end
module ConnectorStatus =
  struct
    type nonrec t =
      | CREATING 
      | ACTIVE 
      | DELETING 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATING -> "CREATING"
      | ACTIVE -> "ACTIVE"
      | DELETING -> "DELETING"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATING" -> CREATING
      | "ACTIVE" -> ACTIVE
      | "DELETING" -> DELETING
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ConnectorStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ConnectorStatus" j)
    let to_json = simple_to_json to_value
  end
module ConnectorStatusReason =
  struct
    type nonrec t =
      | CA_CERTIFICATE_REGISTRATION_FAILED 
      | DIRECTORY_ACCESS_DENIED 
      | INTERNAL_FAILURE 
      | INSUFFICIENT_FREE_ADDRESSES 
      | INVALID_SUBNET_IP_PROTOCOL 
      | PRIVATECA_ACCESS_DENIED 
      | PRIVATECA_RESOURCE_NOT_FOUND 
      | SECURITY_GROUP_NOT_IN_VPC 
      | VPC_ACCESS_DENIED 
      | VPC_ENDPOINT_LIMIT_EXCEEDED 
      | VPC_RESOURCE_NOT_FOUND 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CA_CERTIFICATE_REGISTRATION_FAILED ->
          "CA_CERTIFICATE_REGISTRATION_FAILED"
      | DIRECTORY_ACCESS_DENIED -> "DIRECTORY_ACCESS_DENIED"
      | INTERNAL_FAILURE -> "INTERNAL_FAILURE"
      | INSUFFICIENT_FREE_ADDRESSES -> "INSUFFICIENT_FREE_ADDRESSES"
      | INVALID_SUBNET_IP_PROTOCOL -> "INVALID_SUBNET_IP_PROTOCOL"
      | PRIVATECA_ACCESS_DENIED -> "PRIVATECA_ACCESS_DENIED"
      | PRIVATECA_RESOURCE_NOT_FOUND -> "PRIVATECA_RESOURCE_NOT_FOUND"
      | SECURITY_GROUP_NOT_IN_VPC -> "SECURITY_GROUP_NOT_IN_VPC"
      | VPC_ACCESS_DENIED -> "VPC_ACCESS_DENIED"
      | VPC_ENDPOINT_LIMIT_EXCEEDED -> "VPC_ENDPOINT_LIMIT_EXCEEDED"
      | VPC_RESOURCE_NOT_FOUND -> "VPC_RESOURCE_NOT_FOUND"
      | Non_static_id s -> s
    let of_string =
      function
      | "CA_CERTIFICATE_REGISTRATION_FAILED" ->
          CA_CERTIFICATE_REGISTRATION_FAILED
      | "DIRECTORY_ACCESS_DENIED" -> DIRECTORY_ACCESS_DENIED
      | "INTERNAL_FAILURE" -> INTERNAL_FAILURE
      | "INSUFFICIENT_FREE_ADDRESSES" -> INSUFFICIENT_FREE_ADDRESSES
      | "INVALID_SUBNET_IP_PROTOCOL" -> INVALID_SUBNET_IP_PROTOCOL
      | "PRIVATECA_ACCESS_DENIED" -> PRIVATECA_ACCESS_DENIED
      | "PRIVATECA_RESOURCE_NOT_FOUND" -> PRIVATECA_RESOURCE_NOT_FOUND
      | "SECURITY_GROUP_NOT_IN_VPC" -> SECURITY_GROUP_NOT_IN_VPC
      | "VPC_ACCESS_DENIED" -> VPC_ACCESS_DENIED
      | "VPC_ENDPOINT_LIMIT_EXCEEDED" -> VPC_ENDPOINT_LIMIT_EXCEEDED
      | "VPC_RESOURCE_NOT_FOUND" -> VPC_RESOURCE_NOT_FOUND
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ConnectorStatusReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ConnectorStatusReason" j)
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module VpcInformation =
  struct
    type nonrec t =
      {
      ipAddressType: IpAddressType.t option
        [@ocaml.doc "The VPC IP address type."];
      securityGroupIds: SecurityGroupIdList.t
        [@ocaml.doc
          "The security groups used with the connector. You can use a maximum of 4 security groups with a connector."]}
    let context_ = "VpcInformation"
    let make ?ipAddressType =
      fun ~securityGroupIds -> fun () -> { ipAddressType; securityGroupIds }
    let to_value x =
      structure_to_value
        [("IpAddressType",
           (Option.map x.ipAddressType ~f:IpAddressType.to_value));
        ("SecurityGroupIds",
          (Some (SecurityGroupIdList.to_value x.securityGroupIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityGroupIds =
        SecurityGroupIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SecurityGroupIds") in
      let ipAddressType =
        (Option.map ~f:IpAddressType.of_xml)
          (Xml.child xml_arg0 "IpAddressType") in
      make ~securityGroupIds ?ipAddressType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let securityGroupIds =
        field_map_exn json__ "SecurityGroupIds" SecurityGroupIdList.of_json in
      let ipAddressType =
        field_map json__ "IpAddressType" IpAddressType.of_json in
      make ~securityGroupIds ?ipAddressType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about your VPC and security groups used with the connector."]
module TemplateSummary =
  struct
    type nonrec t =
      {
      arn: TemplateArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateTemplate."];
      connectorArn: ConnectorArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateConnector."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time that the template was created."];
      definition: TemplateDefinition.t option
        [@ocaml.doc
          "Template configuration to define the information included in certificates. Define certificate validity and renewal periods, certificate request handling and enrollment options, key usage extensions, application policies, and cryptography settings."];
      name: TemplateName.t option
        [@ocaml.doc
          "Name of the template. The template name must be unique."];
      objectIdentifier: CustomObjectIdentifier.t option
        [@ocaml.doc "Object identifier of a template."];
      policySchema: Integer.t option
        [@ocaml.doc
          "The template schema version. Template schema versions can be v2, v3, or v4. The template configuration options change based on the template schema version."];
      revision: TemplateRevision.t option
        [@ocaml.doc
          "The revision version of the template. Template updates will increment the minor revision. Re-enrolling all certificate holders will increment the major revision."];
      status: TemplateStatus.t option
        [@ocaml.doc
          "Status of the template. Status can be creating, active, deleting, or failed."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "The date and time that the template was updated."]}
    let make ?arn =
      fun ?connectorArn ->
        fun ?createdAt ->
          fun ?definition ->
            fun ?name ->
              fun ?objectIdentifier ->
                fun ?policySchema ->
                  fun ?revision ->
                    fun ?status ->
                      fun ?updatedAt ->
                        fun () ->
                          {
                            arn;
                            connectorArn;
                            createdAt;
                            definition;
                            name;
                            objectIdentifier;
                            policySchema;
                            revision;
                            status;
                            updatedAt
                          }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:TemplateArn.to_value));
        ("ConnectorArn",
          (Option.map x.connectorArn ~f:ConnectorArn.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("Definition",
          (Option.map x.definition ~f:TemplateDefinition.to_value));
        ("Name", (Option.map x.name ~f:TemplateName.to_value));
        ("ObjectIdentifier",
          (Option.map x.objectIdentifier ~f:CustomObjectIdentifier.to_value));
        ("PolicySchema", (Option.map x.policySchema ~f:Integer.to_value));
        ("Revision", (Option.map x.revision ~f:TemplateRevision.to_value));
        ("Status", (Option.map x.status ~f:TemplateStatus.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let status =
        (Option.map ~f:TemplateStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let revision =
        (Option.map ~f:TemplateRevision.of_xml)
          (Xml.child xml_arg0 "Revision") in
      let policySchema =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "PolicySchema") in
      let objectIdentifier =
        (Option.map ~f:CustomObjectIdentifier.of_xml)
          (Xml.child xml_arg0 "ObjectIdentifier") in
      let name =
        (Option.map ~f:TemplateName.of_xml) (Xml.child xml_arg0 "Name") in
      let definition =
        (Option.map ~f:TemplateDefinition.of_xml)
          (Xml.child xml_arg0 "Definition") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let connectorArn =
        (Option.map ~f:ConnectorArn.of_xml)
          (Xml.child xml_arg0 "ConnectorArn") in
      let arn = (Option.map ~f:TemplateArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?updatedAt ?status ?revision ?policySchema ?objectIdentifier ?name
        ?definition ?createdAt ?connectorArn ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let status = field_map json__ "Status" TemplateStatus.of_json in
      let revision = field_map json__ "Revision" TemplateRevision.of_json in
      let policySchema = field_map json__ "PolicySchema" Integer.of_json in
      let objectIdentifier =
        field_map json__ "ObjectIdentifier" CustomObjectIdentifier.of_json in
      let name = field_map json__ "Name" TemplateName.of_json in
      let definition =
        field_map json__ "Definition" TemplateDefinition.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let connectorArn = field_map json__ "ConnectorArn" ConnectorArn.of_json in
      let arn = field_map json__ "Arn" TemplateArn.of_json in
      make ?updatedAt ?status ?revision ?policySchema ?objectIdentifier ?name
        ?definition ?createdAt ?connectorArn ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An Active Directory compatible certificate template. Connectors issue certificates against these templates based on the requestor's Active Directory group membership."]
module ValidationExceptionReason =
  struct
    type nonrec t =
      | FIELD_VALIDATION_FAILED 
      | INVALID_CA_SUBJECT 
      | INVALID_PERMISSION 
      | INVALID_STATE 
      | MISMATCHED_CONNECTOR 
      | MISMATCHED_VPC 
      | NO_CLIENT_TOKEN 
      | UNKNOWN_OPERATION 
      | OTHER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | FIELD_VALIDATION_FAILED -> "FIELD_VALIDATION_FAILED"
      | INVALID_CA_SUBJECT -> "INVALID_CA_SUBJECT"
      | INVALID_PERMISSION -> "INVALID_PERMISSION"
      | INVALID_STATE -> "INVALID_STATE"
      | MISMATCHED_CONNECTOR -> "MISMATCHED_CONNECTOR"
      | MISMATCHED_VPC -> "MISMATCHED_VPC"
      | NO_CLIENT_TOKEN -> "NO_CLIENT_TOKEN"
      | UNKNOWN_OPERATION -> "UNKNOWN_OPERATION"
      | OTHER -> "OTHER"
      | Non_static_id s -> s
    let of_string =
      function
      | "FIELD_VALIDATION_FAILED" -> FIELD_VALIDATION_FAILED
      | "INVALID_CA_SUBJECT" -> INVALID_CA_SUBJECT
      | "INVALID_PERMISSION" -> INVALID_PERMISSION
      | "INVALID_STATE" -> INVALID_STATE
      | "MISMATCHED_CONNECTOR" -> MISMATCHED_CONNECTOR
      | "MISMATCHED_VPC" -> MISMATCHED_VPC
      | "NO_CLIENT_TOKEN" -> NO_CLIENT_TOKEN
      | "UNKNOWN_OPERATION" -> UNKNOWN_OPERATION
      | "OTHER" -> OTHER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module AccessControlEntrySummary =
  struct
    type nonrec t =
      {
      accessRights: AccessRights.t option
        [@ocaml.doc
          "Allow or deny an Active Directory group from enrolling and autoenrolling certificates issued against a template."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time that the Access Control Entry was created."];
      groupDisplayName: DisplayName.t option
        [@ocaml.doc
          "Name of the Active Directory group. This name does not need to match the group name in Active Directory."];
      groupSecurityIdentifier: GroupSecurityIdentifier.t option
        [@ocaml.doc
          "Security identifier (SID) of the group object from Active Directory. The SID starts with \"S-\"."];
      templateArn: TemplateArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateTemplate."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time that the Access Control Entry was updated."]}
    let make ?accessRights =
      fun ?createdAt ->
        fun ?groupDisplayName ->
          fun ?groupSecurityIdentifier ->
            fun ?templateArn ->
              fun ?updatedAt ->
                fun () ->
                  {
                    accessRights;
                    createdAt;
                    groupDisplayName;
                    groupSecurityIdentifier;
                    templateArn;
                    updatedAt
                  }
    let to_value x =
      structure_to_value
        [("AccessRights",
           (Option.map x.accessRights ~f:AccessRights.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("GroupDisplayName",
          (Option.map x.groupDisplayName ~f:DisplayName.to_value));
        ("GroupSecurityIdentifier",
          (Option.map x.groupSecurityIdentifier
             ~f:GroupSecurityIdentifier.to_value));
        ("TemplateArn", (Option.map x.templateArn ~f:TemplateArn.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let templateArn =
        (Option.map ~f:TemplateArn.of_xml) (Xml.child xml_arg0 "TemplateArn") in
      let groupSecurityIdentifier =
        (Option.map ~f:GroupSecurityIdentifier.of_xml)
          (Xml.child xml_arg0 "GroupSecurityIdentifier") in
      let groupDisplayName =
        (Option.map ~f:DisplayName.of_xml)
          (Xml.child xml_arg0 "GroupDisplayName") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let accessRights =
        (Option.map ~f:AccessRights.of_xml)
          (Xml.child xml_arg0 "AccessRights") in
      make ?updatedAt ?templateArn ?groupSecurityIdentifier ?groupDisplayName
        ?createdAt ?accessRights ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let templateArn = field_map json__ "TemplateArn" TemplateArn.of_json in
      let groupSecurityIdentifier =
        field_map json__ "GroupSecurityIdentifier"
          GroupSecurityIdentifier.of_json in
      let groupDisplayName =
        field_map json__ "GroupDisplayName" DisplayName.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let accessRights = field_map json__ "AccessRights" AccessRights.of_json in
      make ?updatedAt ?templateArn ?groupSecurityIdentifier ?groupDisplayName
        ?createdAt ?accessRights ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Summary of group access control entries that allow or deny Active Directory groups based on their security identifiers (SIDs) from enrolling and/or autofenrolling with the template."]
module ServicePrincipalNameSummary =
  struct
    type nonrec t =
      {
      connectorArn: ConnectorArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateConnector."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time that the service principal name was created."];
      directoryRegistrationArn: DirectoryRegistrationArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration."];
      status: ServicePrincipalNameStatus.t option
        [@ocaml.doc "The status of a service principal name."];
      statusReason: ServicePrincipalNameStatusReason.t option
        [@ocaml.doc
          "Additional information for the status of a service principal name if the status is failed."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "Time when the service principal name was updated."]}
    let make ?connectorArn =
      fun ?createdAt ->
        fun ?directoryRegistrationArn ->
          fun ?status ->
            fun ?statusReason ->
              fun ?updatedAt ->
                fun () ->
                  {
                    connectorArn;
                    createdAt;
                    directoryRegistrationArn;
                    status;
                    statusReason;
                    updatedAt
                  }
    let to_value x =
      structure_to_value
        [("ConnectorArn",
           (Option.map x.connectorArn ~f:ConnectorArn.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("DirectoryRegistrationArn",
          (Option.map x.directoryRegistrationArn
             ~f:DirectoryRegistrationArn.to_value));
        ("Status",
          (Option.map x.status ~f:ServicePrincipalNameStatus.to_value));
        ("StatusReason",
          (Option.map x.statusReason
             ~f:ServicePrincipalNameStatusReason.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let statusReason =
        (Option.map ~f:ServicePrincipalNameStatusReason.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:ServicePrincipalNameStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let directoryRegistrationArn =
        (Option.map ~f:DirectoryRegistrationArn.of_xml)
          (Xml.child xml_arg0 "DirectoryRegistrationArn") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let connectorArn =
        (Option.map ~f:ConnectorArn.of_xml)
          (Xml.child xml_arg0 "ConnectorArn") in
      make ?updatedAt ?statusReason ?status ?directoryRegistrationArn
        ?createdAt ?connectorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let statusReason =
        field_map json__ "StatusReason"
          ServicePrincipalNameStatusReason.of_json in
      let status =
        field_map json__ "Status" ServicePrincipalNameStatus.of_json in
      let directoryRegistrationArn =
        field_map json__ "DirectoryRegistrationArn"
          DirectoryRegistrationArn.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let connectorArn = field_map json__ "ConnectorArn" ConnectorArn.of_json in
      make ?updatedAt ?statusReason ?status ?directoryRegistrationArn
        ?createdAt ?connectorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The service principal name that the connector uses to authenticate with Active Directory."]
module DirectoryRegistrationSummary =
  struct
    type nonrec t =
      {
      arn: DirectoryRegistrationArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time that the directory registration was created."];
      directoryId: DirectoryId.t option
        [@ocaml.doc "The identifier of the Active Directory."];
      status: DirectoryRegistrationStatus.t option
        [@ocaml.doc "Status of the directory registration."];
      statusReason: DirectoryRegistrationStatusReason.t option
        [@ocaml.doc
          "Additional information about the directory registration status if the status is failed."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time that the directory registration was updated."]}
    let make ?arn =
      fun ?createdAt ->
        fun ?directoryId ->
          fun ?status ->
            fun ?statusReason ->
              fun ?updatedAt ->
                fun () ->
                  {
                    arn;
                    createdAt;
                    directoryId;
                    status;
                    statusReason;
                    updatedAt
                  }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:DirectoryRegistrationArn.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("Status",
          (Option.map x.status ~f:DirectoryRegistrationStatus.to_value));
        ("StatusReason",
          (Option.map x.statusReason
             ~f:DirectoryRegistrationStatusReason.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let statusReason =
        (Option.map ~f:DirectoryRegistrationStatusReason.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:DirectoryRegistrationStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let arn =
        (Option.map ~f:DirectoryRegistrationArn.of_xml)
          (Xml.child xml_arg0 "Arn") in
      make ?updatedAt ?statusReason ?status ?directoryId ?createdAt ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let statusReason =
        field_map json__ "StatusReason"
          DirectoryRegistrationStatusReason.of_json in
      let status =
        field_map json__ "Status" DirectoryRegistrationStatus.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let arn = field_map json__ "Arn" DirectoryRegistrationArn.of_json in
      make ?updatedAt ?statusReason ?status ?directoryId ?createdAt ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The directory registration represents the authorization of the connector service with the Active Directory."]
module ConnectorSummary =
  struct
    type nonrec t =
      {
      arn: ConnectorArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateConnector."];
      certificateAuthorityArn: CertificateAuthorityArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the certificate authority being used."];
      certificateEnrollmentPolicyServerEndpoint: String_.t option
        [@ocaml.doc
          "Certificate enrollment endpoint for Active Directory domain-joined objects to request certificates."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time that the connector was created."];
      directoryId: DirectoryId.t option
        [@ocaml.doc "The identifier of the Active Directory."];
      status: ConnectorStatus.t option
        [@ocaml.doc
          "Status of the connector. Status can be creating, active, deleting, or failed."];
      statusReason: ConnectorStatusReason.t option
        [@ocaml.doc
          "Additional information about the connector status if the status is failed."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "The date and time that the connector was updated."];
      vpcInformation: VpcInformation.t option
        [@ocaml.doc
          "Information of the VPC and security group(s) used with the connector."]}
    let make ?arn =
      fun ?certificateAuthorityArn ->
        fun ?certificateEnrollmentPolicyServerEndpoint ->
          fun ?createdAt ->
            fun ?directoryId ->
              fun ?status ->
                fun ?statusReason ->
                  fun ?updatedAt ->
                    fun ?vpcInformation ->
                      fun () ->
                        {
                          arn;
                          certificateAuthorityArn;
                          certificateEnrollmentPolicyServerEndpoint;
                          createdAt;
                          directoryId;
                          status;
                          statusReason;
                          updatedAt;
                          vpcInformation
                        }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ConnectorArn.to_value));
        ("CertificateAuthorityArn",
          (Option.map x.certificateAuthorityArn
             ~f:CertificateAuthorityArn.to_value));
        ("CertificateEnrollmentPolicyServerEndpoint",
          (Option.map x.certificateEnrollmentPolicyServerEndpoint
             ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("Status", (Option.map x.status ~f:ConnectorStatus.to_value));
        ("StatusReason",
          (Option.map x.statusReason ~f:ConnectorStatusReason.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("VpcInformation",
          (Option.map x.vpcInformation ~f:VpcInformation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcInformation =
        (Option.map ~f:VpcInformation.of_xml)
          (Xml.child xml_arg0 "VpcInformation") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let statusReason =
        (Option.map ~f:ConnectorStatusReason.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:ConnectorStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let certificateEnrollmentPolicyServerEndpoint =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CertificateEnrollmentPolicyServerEndpoint") in
      let certificateAuthorityArn =
        (Option.map ~f:CertificateAuthorityArn.of_xml)
          (Xml.child xml_arg0 "CertificateAuthorityArn") in
      let arn =
        (Option.map ~f:ConnectorArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?vpcInformation ?updatedAt ?statusReason ?status ?directoryId
        ?createdAt ?certificateEnrollmentPolicyServerEndpoint
        ?certificateAuthorityArn ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcInformation =
        field_map json__ "VpcInformation" VpcInformation.of_json in
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let statusReason =
        field_map json__ "StatusReason" ConnectorStatusReason.of_json in
      let status = field_map json__ "Status" ConnectorStatus.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let certificateEnrollmentPolicyServerEndpoint =
        field_map json__ "CertificateEnrollmentPolicyServerEndpoint"
          String_.of_json in
      let certificateAuthorityArn =
        field_map json__ "CertificateAuthorityArn"
          CertificateAuthorityArn.of_json in
      let arn = field_map json__ "Arn" ConnectorArn.of_json in
      make ?vpcInformation ?updatedAt ?statusReason ?status ?directoryId
        ?createdAt ?certificateEnrollmentPolicyServerEndpoint
        ?certificateAuthorityArn ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Summary description of the Amazon Web Services Private CA AD connectors belonging to an Amazon Web Services account."]
module TagKeyList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"TagKeyList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module Tags =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module AccessDeniedException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You can receive this error if you attempt to create a resource share when you don't have the required permissions. This can be caused by insufficient permissions in policies attached to your Amazon Web Services Identity and Access Management (IAM) principal. It can also happen because of restrictions in place from an Amazon Web Services Organizations service control policy (SCP) that affects your Amazon Web Services account."]
module InternalServerException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request processing has failed because of an unknown error, exception or failure with an internal server."]
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1000) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(?:[A-Za-z0-9_-]{4})*(?:[A-Za-z0-9_-]{2}==|[A-Za-z0-9_-]{3}=)?$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      resourceId: String_.t option
        [@ocaml.doc "The identifier of the Amazon Web Services resource."];
      resourceType: String_.t option
        [@ocaml.doc
          "The resource type, which can be one of Connector, Template, TemplateGroupAccessControlEntry, ServicePrincipalName, or DirectoryRegistration."]}
    let make ?message =
      fun ?resourceId ->
        fun ?resourceType -> fun () -> { message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("ResourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("ResourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceType ?resourceId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "ResourceType" String_.of_json in
      let resourceId = field_map json__ "ResourceId" String_.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?resourceType ?resourceId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The operation tried to access a nonexistent resource. The resource might not be specified correctly, or its status might not be ACTIVE."]
module TemplateList =
  struct
    type nonrec t = TemplateSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TemplateSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TemplateSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"TemplateList" ~of_json:TemplateSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ThrottlingException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      quotaCode: String_.t option
        [@ocaml.doc "The code associated with the quota."];
      serviceCode: String_.t option
        [@ocaml.doc "Identifies the originating service."]}
    let make ?message =
      fun ?quotaCode ->
        fun ?serviceCode -> fun () -> { message; quotaCode; serviceCode }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("QuotaCode", (Option.map x.quotaCode ~f:String_.to_value));
        ("ServiceCode", (Option.map x.serviceCode ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ServiceCode") in
      let quotaCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "QuotaCode") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?serviceCode ?quotaCode ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serviceCode = field_map json__ "ServiceCode" String_.of_json in
      let quotaCode = field_map json__ "QuotaCode" String_.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?serviceCode ?quotaCode ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The limit on the number of requests per second was exceeded."]
module ValidationException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      reason: ValidationExceptionReason.t option
        [@ocaml.doc
          "The reason for the validation error. This won't be return for every validation exception."]}
    let make ?message = fun ?reason -> fun () -> { message; reason }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("Reason",
          (Option.map x.reason ~f:ValidationExceptionReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:ValidationExceptionReason.of_xml)
          (Xml.child xml_arg0 "Reason") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reason =
        field_map json__ "Reason" ValidationExceptionReason.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An input validation error occurred. For example, invalid characters in a template name, or if a pagination token is invalid."]
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AccessControlEntryList =
  struct
    type nonrec t = AccessControlEntrySummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AccessControlEntrySummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccessControlEntrySummary.of_xml)
    let of_json j =
      list_of_json ~kind:"AccessControlEntryList"
        ~of_json:AccessControlEntrySummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServicePrincipalNameList =
  struct
    type nonrec t = ServicePrincipalNameSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ServicePrincipalNameSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ServicePrincipalNameSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ServicePrincipalNameList"
        ~of_json:ServicePrincipalNameSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module DirectoryRegistrationList =
  struct
    type nonrec t = DirectoryRegistrationSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DirectoryRegistrationSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DirectoryRegistrationSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"DirectoryRegistrationList"
        ~of_json:DirectoryRegistrationSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConnectorList =
  struct
    type nonrec t = ConnectorSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ConnectorSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ConnectorSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ConnectorList" ~of_json:ConnectorSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module Template =
  struct
    type nonrec t =
      {
      arn: TemplateArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateTemplate."];
      connectorArn: ConnectorArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateConnector."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time that the template was created."];
      definition: TemplateDefinition.t option
        [@ocaml.doc
          "Template configuration to define the information included in certificates. Define certificate validity and renewal periods, certificate request handling and enrollment options, key usage extensions, application policies, and cryptography settings."];
      name: TemplateName.t option
        [@ocaml.doc "Name of the templates. Template names must be unique."];
      objectIdentifier: CustomObjectIdentifier.t option
        [@ocaml.doc "Object identifier of a template."];
      policySchema: Integer.t option
        [@ocaml.doc
          "The template schema version. Template schema versions can be v2, v3, or v4. The template configuration options change based on the template schema version."];
      revision: TemplateRevision.t option
        [@ocaml.doc
          "The version of the template. Template updates will increment the minor revision. Re-enrolling all certificate holders will increment the major revision."];
      status: TemplateStatus.t option
        [@ocaml.doc
          "Status of the template. Status can be creating, active, deleting, or failed."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "The date and time that the template was updated."]}
    let make ?arn =
      fun ?connectorArn ->
        fun ?createdAt ->
          fun ?definition ->
            fun ?name ->
              fun ?objectIdentifier ->
                fun ?policySchema ->
                  fun ?revision ->
                    fun ?status ->
                      fun ?updatedAt ->
                        fun () ->
                          {
                            arn;
                            connectorArn;
                            createdAt;
                            definition;
                            name;
                            objectIdentifier;
                            policySchema;
                            revision;
                            status;
                            updatedAt
                          }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:TemplateArn.to_value));
        ("ConnectorArn",
          (Option.map x.connectorArn ~f:ConnectorArn.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("Definition",
          (Option.map x.definition ~f:TemplateDefinition.to_value));
        ("Name", (Option.map x.name ~f:TemplateName.to_value));
        ("ObjectIdentifier",
          (Option.map x.objectIdentifier ~f:CustomObjectIdentifier.to_value));
        ("PolicySchema", (Option.map x.policySchema ~f:Integer.to_value));
        ("Revision", (Option.map x.revision ~f:TemplateRevision.to_value));
        ("Status", (Option.map x.status ~f:TemplateStatus.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let status =
        (Option.map ~f:TemplateStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let revision =
        (Option.map ~f:TemplateRevision.of_xml)
          (Xml.child xml_arg0 "Revision") in
      let policySchema =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "PolicySchema") in
      let objectIdentifier =
        (Option.map ~f:CustomObjectIdentifier.of_xml)
          (Xml.child xml_arg0 "ObjectIdentifier") in
      let name =
        (Option.map ~f:TemplateName.of_xml) (Xml.child xml_arg0 "Name") in
      let definition =
        (Option.map ~f:TemplateDefinition.of_xml)
          (Xml.child xml_arg0 "Definition") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let connectorArn =
        (Option.map ~f:ConnectorArn.of_xml)
          (Xml.child xml_arg0 "ConnectorArn") in
      let arn = (Option.map ~f:TemplateArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?updatedAt ?status ?revision ?policySchema ?objectIdentifier ?name
        ?definition ?createdAt ?connectorArn ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let status = field_map json__ "Status" TemplateStatus.of_json in
      let revision = field_map json__ "Revision" TemplateRevision.of_json in
      let policySchema = field_map json__ "PolicySchema" Integer.of_json in
      let objectIdentifier =
        field_map json__ "ObjectIdentifier" CustomObjectIdentifier.of_json in
      let name = field_map json__ "Name" TemplateName.of_json in
      let definition =
        field_map json__ "Definition" TemplateDefinition.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let connectorArn = field_map json__ "ConnectorArn" ConnectorArn.of_json in
      let arn = field_map json__ "Arn" TemplateArn.of_json in
      make ?updatedAt ?status ?revision ?policySchema ?objectIdentifier ?name
        ?definition ?createdAt ?connectorArn ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An Active Directory compatible certificate template. Connectors issue certificates against these templates based on the requestor's Active Directory group membership."]
module AccessControlEntry =
  struct
    type nonrec t =
      {
      accessRights: AccessRights.t option
        [@ocaml.doc
          "Permissions to allow or deny an Active Directory group to enroll or autoenroll certificates issued against a template."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time that the Access Control Entry was created."];
      groupDisplayName: DisplayName.t option
        [@ocaml.doc
          "Name of the Active Directory group. This name does not need to match the group name in Active Directory."];
      groupSecurityIdentifier: GroupSecurityIdentifier.t option
        [@ocaml.doc
          "Security identifier (SID) of the group object from Active Directory. The SID starts with \"S-\"."];
      templateArn: TemplateArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateTemplate."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time that the Access Control Entry was updated."]}
    let make ?accessRights =
      fun ?createdAt ->
        fun ?groupDisplayName ->
          fun ?groupSecurityIdentifier ->
            fun ?templateArn ->
              fun ?updatedAt ->
                fun () ->
                  {
                    accessRights;
                    createdAt;
                    groupDisplayName;
                    groupSecurityIdentifier;
                    templateArn;
                    updatedAt
                  }
    let to_value x =
      structure_to_value
        [("AccessRights",
           (Option.map x.accessRights ~f:AccessRights.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("GroupDisplayName",
          (Option.map x.groupDisplayName ~f:DisplayName.to_value));
        ("GroupSecurityIdentifier",
          (Option.map x.groupSecurityIdentifier
             ~f:GroupSecurityIdentifier.to_value));
        ("TemplateArn", (Option.map x.templateArn ~f:TemplateArn.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let templateArn =
        (Option.map ~f:TemplateArn.of_xml) (Xml.child xml_arg0 "TemplateArn") in
      let groupSecurityIdentifier =
        (Option.map ~f:GroupSecurityIdentifier.of_xml)
          (Xml.child xml_arg0 "GroupSecurityIdentifier") in
      let groupDisplayName =
        (Option.map ~f:DisplayName.of_xml)
          (Xml.child xml_arg0 "GroupDisplayName") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let accessRights =
        (Option.map ~f:AccessRights.of_xml)
          (Xml.child xml_arg0 "AccessRights") in
      make ?updatedAt ?templateArn ?groupSecurityIdentifier ?groupDisplayName
        ?createdAt ?accessRights ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let templateArn = field_map json__ "TemplateArn" TemplateArn.of_json in
      let groupSecurityIdentifier =
        field_map json__ "GroupSecurityIdentifier"
          GroupSecurityIdentifier.of_json in
      let groupDisplayName =
        field_map json__ "GroupDisplayName" DisplayName.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let accessRights = field_map json__ "AccessRights" AccessRights.of_json in
      make ?updatedAt ?templateArn ?groupSecurityIdentifier ?groupDisplayName
        ?createdAt ?accessRights ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An access control entry allows or denies Active Directory groups based on their security identifiers (SIDs) from enrolling and/or autoenrolling with the template."]
module ServicePrincipalName =
  struct
    type nonrec t =
      {
      connectorArn: ConnectorArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateConnector.html."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time that the service principal name was created."];
      directoryRegistrationArn: DirectoryRegistrationArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration."];
      status: ServicePrincipalNameStatus.t option
        [@ocaml.doc "The status of a service principal name."];
      statusReason: ServicePrincipalNameStatusReason.t option
        [@ocaml.doc
          "Additional information for the status of a service principal name if the status is failed."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time that the service principal name was updated."]}
    let make ?connectorArn =
      fun ?createdAt ->
        fun ?directoryRegistrationArn ->
          fun ?status ->
            fun ?statusReason ->
              fun ?updatedAt ->
                fun () ->
                  {
                    connectorArn;
                    createdAt;
                    directoryRegistrationArn;
                    status;
                    statusReason;
                    updatedAt
                  }
    let to_value x =
      structure_to_value
        [("ConnectorArn",
           (Option.map x.connectorArn ~f:ConnectorArn.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("DirectoryRegistrationArn",
          (Option.map x.directoryRegistrationArn
             ~f:DirectoryRegistrationArn.to_value));
        ("Status",
          (Option.map x.status ~f:ServicePrincipalNameStatus.to_value));
        ("StatusReason",
          (Option.map x.statusReason
             ~f:ServicePrincipalNameStatusReason.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let statusReason =
        (Option.map ~f:ServicePrincipalNameStatusReason.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:ServicePrincipalNameStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let directoryRegistrationArn =
        (Option.map ~f:DirectoryRegistrationArn.of_xml)
          (Xml.child xml_arg0 "DirectoryRegistrationArn") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let connectorArn =
        (Option.map ~f:ConnectorArn.of_xml)
          (Xml.child xml_arg0 "ConnectorArn") in
      make ?updatedAt ?statusReason ?status ?directoryRegistrationArn
        ?createdAt ?connectorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let statusReason =
        field_map json__ "StatusReason"
          ServicePrincipalNameStatusReason.of_json in
      let status =
        field_map json__ "Status" ServicePrincipalNameStatus.of_json in
      let directoryRegistrationArn =
        field_map json__ "DirectoryRegistrationArn"
          DirectoryRegistrationArn.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let connectorArn = field_map json__ "ConnectorArn" ConnectorArn.of_json in
      make ?updatedAt ?statusReason ?status ?directoryRegistrationArn
        ?createdAt ?connectorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The service principal name that the connector uses to authenticate with Active Directory."]
module DirectoryRegistration =
  struct
    type nonrec t =
      {
      arn: DirectoryRegistrationArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time that the directory registration was created."];
      directoryId: DirectoryId.t option
        [@ocaml.doc "The identifier of the Active Directory."];
      status: DirectoryRegistrationStatus.t option
        [@ocaml.doc "Status of the directory registration."];
      statusReason: DirectoryRegistrationStatusReason.t option
        [@ocaml.doc
          "Additional information about the directory registration status if the status is failed."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time that the directory registration was updated."]}
    let make ?arn =
      fun ?createdAt ->
        fun ?directoryId ->
          fun ?status ->
            fun ?statusReason ->
              fun ?updatedAt ->
                fun () ->
                  {
                    arn;
                    createdAt;
                    directoryId;
                    status;
                    statusReason;
                    updatedAt
                  }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:DirectoryRegistrationArn.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("Status",
          (Option.map x.status ~f:DirectoryRegistrationStatus.to_value));
        ("StatusReason",
          (Option.map x.statusReason
             ~f:DirectoryRegistrationStatusReason.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let statusReason =
        (Option.map ~f:DirectoryRegistrationStatusReason.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:DirectoryRegistrationStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let arn =
        (Option.map ~f:DirectoryRegistrationArn.of_xml)
          (Xml.child xml_arg0 "Arn") in
      make ?updatedAt ?statusReason ?status ?directoryId ?createdAt ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let statusReason =
        field_map json__ "StatusReason"
          DirectoryRegistrationStatusReason.of_json in
      let status =
        field_map json__ "Status" DirectoryRegistrationStatus.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let arn = field_map json__ "Arn" DirectoryRegistrationArn.of_json in
      make ?updatedAt ?statusReason ?status ?directoryId ?createdAt ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The directory registration represents the authorization of the connector service with a directory."]
module Connector =
  struct
    type nonrec t =
      {
      arn: ConnectorArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateConnector."];
      certificateAuthorityArn: CertificateAuthorityArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the certificate authority being used."];
      certificateEnrollmentPolicyServerEndpoint: String_.t option
        [@ocaml.doc
          "Certificate enrollment endpoint for Active Directory domain-joined objects reach out to when requesting certificates."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time that the connector was created."];
      directoryId: DirectoryId.t option
        [@ocaml.doc "The identifier of the Active Directory."];
      status: ConnectorStatus.t option
        [@ocaml.doc
          "Status of the connector. Status can be creating, active, deleting, or failed."];
      statusReason: ConnectorStatusReason.t option
        [@ocaml.doc
          "Additional information about the connector status if the status is failed."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "The date and time that the connector was updated."];
      vpcInformation: VpcInformation.t option
        [@ocaml.doc
          "Information of the VPC and security group(s) used with the connector."]}
    let make ?arn =
      fun ?certificateAuthorityArn ->
        fun ?certificateEnrollmentPolicyServerEndpoint ->
          fun ?createdAt ->
            fun ?directoryId ->
              fun ?status ->
                fun ?statusReason ->
                  fun ?updatedAt ->
                    fun ?vpcInformation ->
                      fun () ->
                        {
                          arn;
                          certificateAuthorityArn;
                          certificateEnrollmentPolicyServerEndpoint;
                          createdAt;
                          directoryId;
                          status;
                          statusReason;
                          updatedAt;
                          vpcInformation
                        }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ConnectorArn.to_value));
        ("CertificateAuthorityArn",
          (Option.map x.certificateAuthorityArn
             ~f:CertificateAuthorityArn.to_value));
        ("CertificateEnrollmentPolicyServerEndpoint",
          (Option.map x.certificateEnrollmentPolicyServerEndpoint
             ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("DirectoryId", (Option.map x.directoryId ~f:DirectoryId.to_value));
        ("Status", (Option.map x.status ~f:ConnectorStatus.to_value));
        ("StatusReason",
          (Option.map x.statusReason ~f:ConnectorStatusReason.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("VpcInformation",
          (Option.map x.vpcInformation ~f:VpcInformation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcInformation =
        (Option.map ~f:VpcInformation.of_xml)
          (Xml.child xml_arg0 "VpcInformation") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let statusReason =
        (Option.map ~f:ConnectorStatusReason.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:ConnectorStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let directoryId =
        (Option.map ~f:DirectoryId.of_xml) (Xml.child xml_arg0 "DirectoryId") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let certificateEnrollmentPolicyServerEndpoint =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CertificateEnrollmentPolicyServerEndpoint") in
      let certificateAuthorityArn =
        (Option.map ~f:CertificateAuthorityArn.of_xml)
          (Xml.child xml_arg0 "CertificateAuthorityArn") in
      let arn =
        (Option.map ~f:ConnectorArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?vpcInformation ?updatedAt ?statusReason ?status ?directoryId
        ?createdAt ?certificateEnrollmentPolicyServerEndpoint
        ?certificateAuthorityArn ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcInformation =
        field_map json__ "VpcInformation" VpcInformation.of_json in
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let statusReason =
        field_map json__ "StatusReason" ConnectorStatusReason.of_json in
      let status = field_map json__ "Status" ConnectorStatus.of_json in
      let directoryId = field_map json__ "DirectoryId" DirectoryId.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let certificateEnrollmentPolicyServerEndpoint =
        field_map json__ "CertificateEnrollmentPolicyServerEndpoint"
          String_.of_json in
      let certificateAuthorityArn =
        field_map json__ "CertificateAuthorityArn"
          CertificateAuthorityArn.of_json in
      let arn = field_map json__ "Arn" ConnectorArn.of_json in
      make ?vpcInformation ?updatedAt ?statusReason ?status ?directoryId
        ?createdAt ?certificateEnrollmentPolicyServerEndpoint
        ?certificateAuthorityArn ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Amazon Web Services Private CA Connector for Active Directory is a service that links your Active Directory with Amazon Web Services Private CA. The connector brokers the exchange of certificates from Amazon Web Services Private CA to domain-joined users and machines managed with Active Directory."]
module ConflictException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      resourceId: String_.t option
        [@ocaml.doc "The identifier of the Amazon Web Services resource."];
      resourceType: String_.t option
        [@ocaml.doc
          "The resource type, which can be one of Connector, Template, TemplateGroupAccessControlEntry, ServicePrincipalName, or DirectoryRegistration."]}
    let make ?message =
      fun ?resourceId ->
        fun ?resourceType -> fun () -> { message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("ResourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("ResourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceType ?resourceId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "ResourceType" String_.of_json in
      let resourceId = field_map json__ "ResourceId" String_.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?resourceType ?resourceId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This request cannot be completed for one of the following reasons because the requested resource was being concurrently modified by another request."]
module ServiceQuotaExceededException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      quotaCode: String_.t option
        [@ocaml.doc "The code associated with the service quota."];
      resourceId: String_.t option
        [@ocaml.doc "The identifier of the Amazon Web Services resource."];
      resourceType: String_.t option
        [@ocaml.doc
          "The resource type, which can be one of Connector, Template, TemplateGroupAccessControlEntry, ServicePrincipalName, or DirectoryRegistration."];
      serviceCode: String_.t option
        [@ocaml.doc "Identifies the originating service."]}
    let make ?message =
      fun ?quotaCode ->
        fun ?resourceId ->
          fun ?resourceType ->
            fun ?serviceCode ->
              fun () ->
                { message; quotaCode; resourceId; resourceType; serviceCode }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("QuotaCode", (Option.map x.quotaCode ~f:String_.to_value));
        ("ResourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("ResourceType", (Option.map x.resourceType ~f:String_.to_value));
        ("ServiceCode", (Option.map x.serviceCode ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ServiceCode") in
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceId") in
      let quotaCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "QuotaCode") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?serviceCode ?resourceType ?resourceId ?quotaCode ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serviceCode = field_map json__ "ServiceCode" String_.of_json in
      let resourceType = field_map json__ "ResourceType" String_.of_json in
      let resourceId = field_map json__ "ResourceId" String_.of_json in
      let quotaCode = field_map json__ "QuotaCode" String_.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?serviceCode ?resourceType ?resourceId ?quotaCode ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Request would cause a service quota to be exceeded."]
module ClientToken =
  struct
    type nonrec t = string
    let context_ = "ClientToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"^[!-~]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientToken" j
    let to_json = simple_to_json to_value
  end
module UpdateTemplateRequest =
  struct
    type nonrec t =
      {
      definition: TemplateDefinition.t option
        [@ocaml.doc
          "Template configuration to define the information included in certificates. Define certificate validity and renewal periods, certificate request handling and enrollment options, key usage extensions, application policies, and cryptography settings."];
      reenrollAllCertificateHolders: Boolean.t option
        [@ocaml.doc
          "This setting allows the major version of a template to be increased automatically. All members of Active Directory groups that are allowed to enroll with a template will receive a new certificate issued using that template."];
      templateArn: TemplateArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateTemplate."]}
    let context_ = "UpdateTemplateRequest"
    let make ?definition =
      fun ?reenrollAllCertificateHolders ->
        fun ~templateArn ->
          fun () ->
            { definition; reenrollAllCertificateHolders; templateArn }
    let to_value x =
      structure_to_value
        [("Definition",
           (Option.map x.definition ~f:TemplateDefinition.to_value));
        ("ReenrollAllCertificateHolders",
          (Option.map x.reenrollAllCertificateHolders ~f:Boolean.to_value));
        ("TemplateArn", (Some (TemplateArn.to_value x.templateArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateArn =
        TemplateArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateArn") in
      let reenrollAllCertificateHolders =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "ReenrollAllCertificateHolders") in
      let definition =
        (Option.map ~f:TemplateDefinition.of_xml)
          (Xml.child xml_arg0 "Definition") in
      make ~templateArn ?reenrollAllCertificateHolders ?definition ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateArn =
        field_map_exn json__ "TemplateArn" TemplateArn.of_json in
      let reenrollAllCertificateHolders =
        field_map json__ "ReenrollAllCertificateHolders" Boolean.of_json in
      let definition =
        field_map json__ "Definition" TemplateDefinition.of_json in
      make ~templateArn ?reenrollAllCertificateHolders ?definition ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Update template configuration to define the information included in certificates."]
module UpdateTemplateGroupAccessControlEntryRequest =
  struct
    type nonrec t =
      {
      accessRights: AccessRights.t option
        [@ocaml.doc
          "Allow or deny permissions for an Active Directory group to enroll or autoenroll certificates for a template."];
      groupDisplayName: DisplayName.t option
        [@ocaml.doc
          "Name of the Active Directory group. This name does not need to match the group name in Active Directory."];
      groupSecurityIdentifier: GroupSecurityIdentifier.t
        [@ocaml.doc
          "Security identifier (SID) of the group object from Active Directory. The SID starts with \"S-\"."];
      templateArn: TemplateArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateTemplate."]}
    let context_ = "UpdateTemplateGroupAccessControlEntryRequest"
    let make ?accessRights =
      fun ?groupDisplayName ->
        fun ~groupSecurityIdentifier ->
          fun ~templateArn ->
            fun () ->
              {
                accessRights;
                groupDisplayName;
                groupSecurityIdentifier;
                templateArn
              }
    let to_value x =
      structure_to_value
        [("AccessRights",
           (Option.map x.accessRights ~f:AccessRights.to_value));
        ("GroupDisplayName",
          (Option.map x.groupDisplayName ~f:DisplayName.to_value));
        ("GroupSecurityIdentifier",
          (Some (GroupSecurityIdentifier.to_value x.groupSecurityIdentifier)));
        ("TemplateArn", (Some (TemplateArn.to_value x.templateArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateArn =
        TemplateArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateArn") in
      let groupSecurityIdentifier =
        GroupSecurityIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GroupSecurityIdentifier") in
      let groupDisplayName =
        (Option.map ~f:DisplayName.of_xml)
          (Xml.child xml_arg0 "GroupDisplayName") in
      let accessRights =
        (Option.map ~f:AccessRights.of_xml)
          (Xml.child xml_arg0 "AccessRights") in
      make ~templateArn ~groupSecurityIdentifier ?groupDisplayName
        ?accessRights ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateArn =
        field_map_exn json__ "TemplateArn" TemplateArn.of_json in
      let groupSecurityIdentifier =
        field_map_exn json__ "GroupSecurityIdentifier"
          GroupSecurityIdentifier.of_json in
      let groupDisplayName =
        field_map json__ "GroupDisplayName" DisplayName.of_json in
      let accessRights = field_map json__ "AccessRights" AccessRights.of_json in
      make ~templateArn ~groupSecurityIdentifier ?groupDisplayName
        ?accessRights ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Update a group access control entry you created using CreateTemplateGroupAccessControlEntry."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: String_.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you created the resource."];
      tagKeys: TagKeyList.t
        [@ocaml.doc
          "Specifies a list of tag keys that you want to remove from the specified resources."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (String_.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map_exn json__ "TagKeys" TagKeyList.of_json in
      let resourceArn = field_map_exn json__ "ResourceArn" String_.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes one or more tags from your resource."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: String_.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you created the resource."];
      tags: Tags.t
        [@ocaml.doc
          "Metadata assigned to a directory registration consisting of a key-value pair."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (String_.to_value x.resourceArn)));
        ("Tags", (Some (Tags.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        Tags.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resourceArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "Tags" Tags.of_json in
      let resourceArn = field_map_exn json__ "ResourceArn" String_.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Adds one or more tags to your resource."]
module ListTemplatesResponse =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "Use this parameter when paginating results in a subsequent request after you receive a response with truncated results. Set it to the value of the NextToken parameter from the response you just received."];
      templates: TemplateList.t option
        [@ocaml.doc
          "Custom configuration templates used when issuing a certificate."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?templates -> fun () -> { nextToken; templates }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("Templates", (Option.map x.templates ~f:TemplateList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templates =
        (Option.map ~f:TemplateList.of_xml) (Xml.child xml_arg0 "Templates") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?templates ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templates = field_map json__ "Templates" TemplateList.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      make ?templates ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the templates, if any, that are associated with a connector."]
module ListTemplatesRequest =
  struct
    type nonrec t =
      {
      connectorArn: ConnectorArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateConnector."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "Use this parameter when paginating results to specify the maximum number of items to return in the response on each page. If additional items exist beyond the number you specify, the NextToken element is sent in the response. Use this NextToken value in a subsequent request to retrieve additional items."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "Use this parameter when paginating results in a subsequent request after you receive a response with truncated results. Set it to the value of the NextToken parameter from the response you just received."]}
    let context_ = "ListTemplatesRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~connectorArn ->
          fun () -> { maxResults; nextToken; connectorArn }
    let to_value x =
      structure_to_value
        [("ConnectorArn", (Some (ConnectorArn.to_value x.connectorArn)));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let connectorArn =
        ConnectorArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectorArn") in
      make ?nextToken ?maxResults ~connectorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResults.of_json in
      let connectorArn =
        field_map_exn json__ "ConnectorArn" ConnectorArn.of_json in
      make ?nextToken ?maxResults ~connectorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the templates, if any, that are associated with a connector."]
module ListTemplateGroupAccessControlEntriesResponse =
  struct
    type nonrec t =
      {
      accessControlEntries: AccessControlEntryList.t option
        [@ocaml.doc
          "An access control entry grants or denies permission to an Active Directory group to enroll certificates for a template."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "Use this parameter when paginating results in a subsequent request after you receive a response with truncated results. Set it to the value of the NextToken parameter from the response you just received."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?accessControlEntries =
      fun ?nextToken -> fun () -> { accessControlEntries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("AccessControlEntries",
           (Option.map x.accessControlEntries
              ~f:AccessControlEntryList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let accessControlEntries =
        (Option.map ~f:AccessControlEntryList.of_xml)
          (Xml.child xml_arg0 "AccessControlEntries") in
      make ?nextToken ?accessControlEntries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let accessControlEntries =
        field_map json__ "AccessControlEntries"
          AccessControlEntryList.of_json in
      make ?nextToken ?accessControlEntries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists group access control entries you created."]
module ListTemplateGroupAccessControlEntriesRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc
          "Use this parameter when paginating results to specify the maximum number of items to return in the response on each page. If additional items exist beyond the number you specify, the NextToken element is sent in the response. Use this NextToken value in a subsequent request to retrieve additional items."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "Use this parameter when paginating results in a subsequent request after you receive a response with truncated results. Set it to the value of the NextToken parameter from the response you just received."];
      templateArn: TemplateArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateTemplate."]}
    let context_ = "ListTemplateGroupAccessControlEntriesRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~templateArn -> fun () -> { maxResults; nextToken; templateArn }
    let to_value x =
      structure_to_value
        [("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("TemplateArn", (Some (TemplateArn.to_value x.templateArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateArn =
        TemplateArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateArn") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      make ~templateArn ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateArn =
        field_map_exn json__ "TemplateArn" TemplateArn.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResults.of_json in
      make ~templateArn ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists group access control entries you created."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: Tags.t option
        [@ocaml.doc
          "The tags, if any, that are associated with your resource."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" Tags.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the tags, if any, that are associated with your resource."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: String_.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you created the resource."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (String_.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn = field_map_exn json__ "ResourceArn" String_.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the tags, if any, that are associated with your resource."]
module ListServicePrincipalNamesResponse =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "Use this parameter when paginating results in a subsequent request after you receive a response with truncated results. Set it to the value of the NextToken parameter from the response you just received."];
      servicePrincipalNames: ServicePrincipalNameList.t option
        [@ocaml.doc
          "The service principal name, if any, that the connector uses to authenticate with Active Directory."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?servicePrincipalNames ->
        fun () -> { nextToken; servicePrincipalNames }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("ServicePrincipalNames",
          (Option.map x.servicePrincipalNames
             ~f:ServicePrincipalNameList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let servicePrincipalNames =
        (Option.map ~f:ServicePrincipalNameList.of_xml)
          (Xml.child xml_arg0 "ServicePrincipalNames") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?servicePrincipalNames ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let servicePrincipalNames =
        field_map json__ "ServicePrincipalNames"
          ServicePrincipalNameList.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      make ?servicePrincipalNames ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the service principal names that the connector uses to authenticate with Active Directory."]
module ListServicePrincipalNamesRequest =
  struct
    type nonrec t =
      {
      directoryRegistrationArn: DirectoryRegistrationArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "Use this parameter when paginating results to specify the maximum number of items to return in the response on each page. If additional items exist beyond the number you specify, the NextToken element is sent in the response. Use this NextToken value in a subsequent request to retrieve additional items."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "Use this parameter when paginating results in a subsequent request after you receive a response with truncated results. Set it to the value of the NextToken parameter from the response you just received."]}
    let context_ = "ListServicePrincipalNamesRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~directoryRegistrationArn ->
          fun () -> { maxResults; nextToken; directoryRegistrationArn }
    let to_value x =
      structure_to_value
        [("DirectoryRegistrationArn",
           (Some
              (DirectoryRegistrationArn.to_value x.directoryRegistrationArn)));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let directoryRegistrationArn =
        DirectoryRegistrationArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "DirectoryRegistrationArn") in
      make ?nextToken ?maxResults ~directoryRegistrationArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResults.of_json in
      let directoryRegistrationArn =
        field_map_exn json__ "DirectoryRegistrationArn"
          DirectoryRegistrationArn.of_json in
      make ?nextToken ?maxResults ~directoryRegistrationArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the service principal names that the connector uses to authenticate with Active Directory."]
module ListDirectoryRegistrationsResponse =
  struct
    type nonrec t =
      {
      directoryRegistrations: DirectoryRegistrationList.t option
        [@ocaml.doc
          "Summary information about each directory registration you have created."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "Use this parameter when paginating results in a subsequent request after you receive a response with truncated results. Set it to the value of the NextToken parameter from the response you just received."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?directoryRegistrations =
      fun ?nextToken -> fun () -> { directoryRegistrations; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DirectoryRegistrations",
           (Option.map x.directoryRegistrations
              ~f:DirectoryRegistrationList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let directoryRegistrations =
        (Option.map ~f:DirectoryRegistrationList.of_xml)
          (Xml.child xml_arg0 "DirectoryRegistrations") in
      make ?nextToken ?directoryRegistrations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let directoryRegistrations =
        field_map json__ "DirectoryRegistrations"
          DirectoryRegistrationList.of_json in
      make ?nextToken ?directoryRegistrations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the directory registrations that you created by using the https://docs.aws.amazon.com/pca-connector-ad/latest/APIReference/API_CreateDirectoryRegistration action."]
module ListDirectoryRegistrationsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc
          "Use this parameter when paginating results to specify the maximum number of items to return in the response on each page. If additional items exist beyond the number you specify, the NextToken element is sent in the response. Use this NextToken value in a subsequent request to retrieve additional items."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "Use this parameter when paginating results in a subsequent request after you receive a response with truncated results. Set it to the value of the NextToken parameter from the response you just received."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the directory registrations that you created by using the https://docs.aws.amazon.com/pca-connector-ad/latest/APIReference/API_CreateDirectoryRegistration action."]
module ListConnectorsResponse =
  struct
    type nonrec t =
      {
      connectors: ConnectorList.t option
        [@ocaml.doc
          "Summary information about each connector you have created."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "Use this parameter when paginating results in a subsequent request after you receive a response with truncated results. Set it to the value of the NextToken parameter from the response you just received."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?connectors =
      fun ?nextToken -> fun () -> { connectors; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Connectors", (Option.map x.connectors ~f:ConnectorList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let connectors =
        (Option.map ~f:ConnectorList.of_xml)
          (Xml.child xml_arg0 "Connectors") in
      make ?nextToken ?connectors ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let connectors = field_map json__ "Connectors" ConnectorList.of_json in
      make ?nextToken ?connectors ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the connectors that you created by using the https://docs.aws.amazon.com/pca-connector-ad/latest/APIReference/API_CreateConnector action."]
module ListConnectorsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc
          "Use this parameter when paginating results to specify the maximum number of items to return in the response on each page. If additional items exist beyond the number you specify, the NextToken element is sent in the response. Use this NextToken value in a subsequent request to retrieve additional items."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "Use this parameter when paginating results in a subsequent request after you receive a response with truncated results. Set it to the value of the NextToken parameter from the response you just received."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the connectors that you created by using the https://docs.aws.amazon.com/pca-connector-ad/latest/APIReference/API_CreateConnector action."]
module GetTemplateResponse =
  struct
    type nonrec t =
      {
      template: Template.t option
        [@ocaml.doc
          "A certificate template that the connector uses to issue certificates from a private CA."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?template = fun () -> { template }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Template", (Option.map x.template ~f:Template.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let template =
        (Option.map ~f:Template.of_xml) (Xml.child xml_arg0 "Template") in
      make ?template ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let template = field_map json__ "Template" Template.of_json in
      make ?template ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a certificate template that the connector uses to issue certificates from a private CA."]
module GetTemplateRequest =
  struct
    type nonrec t =
      {
      templateArn: TemplateArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateTemplate."]}
    let context_ = "GetTemplateRequest"
    let make ~templateArn = fun () -> { templateArn }
    let to_value x =
      structure_to_value
        [("TemplateArn", (Some (TemplateArn.to_value x.templateArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateArn =
        TemplateArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateArn") in
      make ~templateArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateArn =
        field_map_exn json__ "TemplateArn" TemplateArn.of_json in
      make ~templateArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a certificate template that the connector uses to issue certificates from a private CA."]
module GetTemplateGroupAccessControlEntryResponse =
  struct
    type nonrec t =
      {
      accessControlEntry: AccessControlEntry.t option
        [@ocaml.doc
          "An access control entry allows or denies an Active Directory group from enrolling and/or autoenrolling with a template."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?accessControlEntry = fun () -> { accessControlEntry }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("AccessControlEntry",
           (Option.map x.accessControlEntry ~f:AccessControlEntry.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accessControlEntry =
        (Option.map ~f:AccessControlEntry.of_xml)
          (Xml.child xml_arg0 "AccessControlEntry") in
      make ?accessControlEntry ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let accessControlEntry =
        field_map json__ "AccessControlEntry" AccessControlEntry.of_json in
      make ?accessControlEntry ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the group access control entries for a template."]
module GetTemplateGroupAccessControlEntryRequest =
  struct
    type nonrec t =
      {
      groupSecurityIdentifier: GroupSecurityIdentifier.t
        [@ocaml.doc
          "Security identifier (SID) of the group object from Active Directory. The SID starts with \"S-\"."];
      templateArn: TemplateArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateTemplate."]}
    let context_ = "GetTemplateGroupAccessControlEntryRequest"
    let make ~groupSecurityIdentifier =
      fun ~templateArn -> fun () -> { groupSecurityIdentifier; templateArn }
    let to_value x =
      structure_to_value
        [("GroupSecurityIdentifier",
           (Some (GroupSecurityIdentifier.to_value x.groupSecurityIdentifier)));
        ("TemplateArn", (Some (TemplateArn.to_value x.templateArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateArn =
        TemplateArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateArn") in
      let groupSecurityIdentifier =
        GroupSecurityIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GroupSecurityIdentifier") in
      make ~templateArn ~groupSecurityIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateArn =
        field_map_exn json__ "TemplateArn" TemplateArn.of_json in
      let groupSecurityIdentifier =
        field_map_exn json__ "GroupSecurityIdentifier"
          GroupSecurityIdentifier.of_json in
      make ~templateArn ~groupSecurityIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the group access control entries for a template."]
module GetServicePrincipalNameResponse =
  struct
    type nonrec t =
      {
      servicePrincipalName: ServicePrincipalName.t option
        [@ocaml.doc
          "The service principal name that the connector uses to authenticate with Active Directory."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?servicePrincipalName = fun () -> { servicePrincipalName }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ServicePrincipalName",
           (Option.map x.servicePrincipalName
              ~f:ServicePrincipalName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let servicePrincipalName =
        (Option.map ~f:ServicePrincipalName.of_xml)
          (Xml.child xml_arg0 "ServicePrincipalName") in
      make ?servicePrincipalName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let servicePrincipalName =
        field_map json__ "ServicePrincipalName" ServicePrincipalName.of_json in
      make ?servicePrincipalName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the service principal name that the connector uses to authenticate with Active Directory."]
module GetServicePrincipalNameRequest =
  struct
    type nonrec t =
      {
      connectorArn: ConnectorArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateConnector."];
      directoryRegistrationArn: DirectoryRegistrationArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration."]}
    let context_ = "GetServicePrincipalNameRequest"
    let make ~connectorArn =
      fun ~directoryRegistrationArn ->
        fun () -> { connectorArn; directoryRegistrationArn }
    let to_value x =
      structure_to_value
        [("ConnectorArn", (Some (ConnectorArn.to_value x.connectorArn)));
        ("DirectoryRegistrationArn",
          (Some
             (DirectoryRegistrationArn.to_value x.directoryRegistrationArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let directoryRegistrationArn =
        DirectoryRegistrationArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "DirectoryRegistrationArn") in
      let connectorArn =
        ConnectorArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectorArn") in
      make ~directoryRegistrationArn ~connectorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let directoryRegistrationArn =
        field_map_exn json__ "DirectoryRegistrationArn"
          DirectoryRegistrationArn.of_json in
      let connectorArn =
        field_map_exn json__ "ConnectorArn" ConnectorArn.of_json in
      make ~directoryRegistrationArn ~connectorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the service principal name that the connector uses to authenticate with Active Directory."]
module GetDirectoryRegistrationResponse =
  struct
    type nonrec t =
      {
      directoryRegistration: DirectoryRegistration.t option
        [@ocaml.doc
          "The directory registration represents the authorization of the connector service with a directory."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?directoryRegistration = fun () -> { directoryRegistration }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DirectoryRegistration",
           (Option.map x.directoryRegistration
              ~f:DirectoryRegistration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let directoryRegistration =
        (Option.map ~f:DirectoryRegistration.of_xml)
          (Xml.child xml_arg0 "DirectoryRegistration") in
      make ?directoryRegistration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let directoryRegistration =
        field_map json__ "DirectoryRegistration"
          DirectoryRegistration.of_json in
      make ?directoryRegistration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that contains information about your directory registration."]
module GetDirectoryRegistrationRequest =
  struct
    type nonrec t =
      {
      directoryRegistrationArn: DirectoryRegistrationArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration."]}
    let context_ = "GetDirectoryRegistrationRequest"
    let make ~directoryRegistrationArn =
      fun () -> { directoryRegistrationArn }
    let to_value x =
      structure_to_value
        [("DirectoryRegistrationArn",
           (Some
              (DirectoryRegistrationArn.to_value x.directoryRegistrationArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let directoryRegistrationArn =
        DirectoryRegistrationArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "DirectoryRegistrationArn") in
      make ~directoryRegistrationArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let directoryRegistrationArn =
        field_map_exn json__ "DirectoryRegistrationArn"
          DirectoryRegistrationArn.of_json in
      make ~directoryRegistrationArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that contains information about your directory registration."]
module GetConnectorResponse =
  struct
    type nonrec t =
      {
      connector: Connector.t option
        [@ocaml.doc
          "A structure that contains information about your connector."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?connector = fun () -> { connector }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Connector", (Option.map x.connector ~f:Connector.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connector =
        (Option.map ~f:Connector.of_xml) (Xml.child xml_arg0 "Connector") in
      make ?connector ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let connector = field_map json__ "Connector" Connector.of_json in
      make ?connector ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists information about your connector. You specify the connector on input by its ARN (Amazon Resource Name)."]
module GetConnectorRequest =
  struct
    type nonrec t =
      {
      connectorArn: ConnectorArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateConnector."]}
    let context_ = "GetConnectorRequest"
    let make ~connectorArn = fun () -> { connectorArn }
    let to_value x =
      structure_to_value
        [("ConnectorArn", (Some (ConnectorArn.to_value x.connectorArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connectorArn =
        ConnectorArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectorArn") in
      make ~connectorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let connectorArn =
        field_map_exn json__ "ConnectorArn" ConnectorArn.of_json in
      make ~connectorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists information about your connector. You specify the connector on input by its ARN (Amazon Resource Name)."]
module DeleteTemplateRequest =
  struct
    type nonrec t =
      {
      templateArn: TemplateArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateTemplate."]}
    let context_ = "DeleteTemplateRequest"
    let make ~templateArn = fun () -> { templateArn }
    let to_value x =
      structure_to_value
        [("TemplateArn", (Some (TemplateArn.to_value x.templateArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateArn =
        TemplateArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateArn") in
      make ~templateArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateArn =
        field_map_exn json__ "TemplateArn" TemplateArn.of_json in
      make ~templateArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a template. Certificates issued using the template are still valid until they are revoked or expired."]
module DeleteTemplateGroupAccessControlEntryRequest =
  struct
    type nonrec t =
      {
      groupSecurityIdentifier: GroupSecurityIdentifier.t
        [@ocaml.doc
          "Security identifier (SID) of the group object from Active Directory. The SID starts with \"S-\"."];
      templateArn: TemplateArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateTemplate."]}
    let context_ = "DeleteTemplateGroupAccessControlEntryRequest"
    let make ~groupSecurityIdentifier =
      fun ~templateArn -> fun () -> { groupSecurityIdentifier; templateArn }
    let to_value x =
      structure_to_value
        [("GroupSecurityIdentifier",
           (Some (GroupSecurityIdentifier.to_value x.groupSecurityIdentifier)));
        ("TemplateArn", (Some (TemplateArn.to_value x.templateArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateArn =
        TemplateArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateArn") in
      let groupSecurityIdentifier =
        GroupSecurityIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GroupSecurityIdentifier") in
      make ~templateArn ~groupSecurityIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateArn =
        field_map_exn json__ "TemplateArn" TemplateArn.of_json in
      let groupSecurityIdentifier =
        field_map_exn json__ "GroupSecurityIdentifier"
          GroupSecurityIdentifier.of_json in
      make ~templateArn ~groupSecurityIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a group access control entry."]
module DeleteServicePrincipalNameRequest =
  struct
    type nonrec t =
      {
      connectorArn: ConnectorArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateConnector."];
      directoryRegistrationArn: DirectoryRegistrationArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration."]}
    let context_ = "DeleteServicePrincipalNameRequest"
    let make ~connectorArn =
      fun ~directoryRegistrationArn ->
        fun () -> { connectorArn; directoryRegistrationArn }
    let to_value x =
      structure_to_value
        [("ConnectorArn", (Some (ConnectorArn.to_value x.connectorArn)));
        ("DirectoryRegistrationArn",
          (Some
             (DirectoryRegistrationArn.to_value x.directoryRegistrationArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let directoryRegistrationArn =
        DirectoryRegistrationArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "DirectoryRegistrationArn") in
      let connectorArn =
        ConnectorArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectorArn") in
      make ~directoryRegistrationArn ~connectorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let directoryRegistrationArn =
        field_map_exn json__ "DirectoryRegistrationArn"
          DirectoryRegistrationArn.of_json in
      let connectorArn =
        field_map_exn json__ "ConnectorArn" ConnectorArn.of_json in
      make ~directoryRegistrationArn ~connectorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the service principal name (SPN) used by a connector to authenticate with your Active Directory."]
module DeleteDirectoryRegistrationRequest =
  struct
    type nonrec t =
      {
      directoryRegistrationArn: DirectoryRegistrationArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration."]}
    let context_ = "DeleteDirectoryRegistrationRequest"
    let make ~directoryRegistrationArn =
      fun () -> { directoryRegistrationArn }
    let to_value x =
      structure_to_value
        [("DirectoryRegistrationArn",
           (Some
              (DirectoryRegistrationArn.to_value x.directoryRegistrationArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let directoryRegistrationArn =
        DirectoryRegistrationArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "DirectoryRegistrationArn") in
      make ~directoryRegistrationArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let directoryRegistrationArn =
        field_map_exn json__ "DirectoryRegistrationArn"
          DirectoryRegistrationArn.of_json in
      make ~directoryRegistrationArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a directory registration. Deleting a directory registration deauthorizes Amazon Web Services Private CA with the directory."]
module DeleteConnectorRequest =
  struct
    type nonrec t =
      {
      connectorArn: ConnectorArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateConnector."]}
    let context_ = "DeleteConnectorRequest"
    let make ~connectorArn = fun () -> { connectorArn }
    let to_value x =
      structure_to_value
        [("ConnectorArn", (Some (ConnectorArn.to_value x.connectorArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connectorArn =
        ConnectorArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectorArn") in
      make ~connectorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let connectorArn =
        field_map_exn json__ "ConnectorArn" ConnectorArn.of_json in
      make ~connectorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a connector for Active Directory. You must provide the Amazon Resource Name (ARN) of the connector that you want to delete. You can find the ARN by calling the https://docs.aws.amazon.com/pca-connector-ad/latest/APIReference/API_ListConnectors action. Deleting a connector does not deregister your directory with Amazon Web Services Private CA. You can deregister your directory by calling the https://docs.aws.amazon.com/pca-connector-ad/latest/APIReference/API_DeleteDirectoryRegistration action."]
module CreateTemplateResponse =
  struct
    type nonrec t =
      {
      templateArn: TemplateArn.t option
        [@ocaml.doc
          "If successful, the Amazon Resource Name (ARN) of the template."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?templateArn = fun () -> { templateArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TemplateArn", (Option.map x.templateArn ~f:TemplateArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateArn =
        (Option.map ~f:TemplateArn.of_xml) (Xml.child xml_arg0 "TemplateArn") in
      make ?templateArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateArn = field_map json__ "TemplateArn" TemplateArn.of_json in
      make ?templateArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an Active Directory compatible certificate template. The connectors issues certificates using these templates based on the requester\226\128\153s Active Directory group membership."]
module CreateTemplateRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option [@ocaml.doc "Idempotency token."];
      connectorArn: ConnectorArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateConnector."];
      definition: TemplateDefinition.t
        [@ocaml.doc
          "Template configuration to define the information included in certificates. Define certificate validity and renewal periods, certificate request handling and enrollment options, key usage extensions, application policies, and cryptography settings."];
      name: TemplateName.t
        [@ocaml.doc
          "Name of the template. The template name must be unique."];
      tags: Tags.t option
        [@ocaml.doc
          "Metadata assigned to a template consisting of a key-value pair."]}
    let context_ = "CreateTemplateRequest"
    let make ?clientToken =
      fun ?tags ->
        fun ~connectorArn ->
          fun ~definition ->
            fun ~name ->
              fun () -> { clientToken; tags; connectorArn; definition; name }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("ConnectorArn", (Some (ConnectorArn.to_value x.connectorArn)));
        ("Definition", (Some (TemplateDefinition.to_value x.definition)));
        ("Name", (Some (TemplateName.to_value x.name)));
        ("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let name =
        TemplateName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let definition =
        TemplateDefinition.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Definition") in
      let connectorArn =
        ConnectorArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectorArn") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?tags ~name ~definition ~connectorArn ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" Tags.of_json in
      let name = field_map_exn json__ "Name" TemplateName.of_json in
      let definition =
        field_map_exn json__ "Definition" TemplateDefinition.of_json in
      let connectorArn =
        field_map_exn json__ "ConnectorArn" ConnectorArn.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      make ?tags ~name ~definition ~connectorArn ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an Active Directory compatible certificate template. The connectors issues certificates using these templates based on the requester\226\128\153s Active Directory group membership."]
module CreateTemplateGroupAccessControlEntryRequest =
  struct
    type nonrec t =
      {
      accessRights: AccessRights.t
        [@ocaml.doc
          "Allow or deny permissions for an Active Directory group to enroll or autoenroll certificates for a template."];
      clientToken: ClientToken.t option [@ocaml.doc "Idempotency token."];
      groupDisplayName: DisplayName.t
        [@ocaml.doc
          "Name of the Active Directory group. This name does not need to match the group name in Active Directory."];
      groupSecurityIdentifier: GroupSecurityIdentifier.t
        [@ocaml.doc
          "Security identifier (SID) of the group object from Active Directory. The SID starts with \"S-\"."];
      templateArn: TemplateArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateTemplate."]}
    let context_ = "CreateTemplateGroupAccessControlEntryRequest"
    let make ?clientToken =
      fun ~accessRights ->
        fun ~groupDisplayName ->
          fun ~groupSecurityIdentifier ->
            fun ~templateArn ->
              fun () ->
                {
                  clientToken;
                  accessRights;
                  groupDisplayName;
                  groupSecurityIdentifier;
                  templateArn
                }
    let to_value x =
      structure_to_value
        [("AccessRights", (Some (AccessRights.to_value x.accessRights)));
        ("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("GroupDisplayName",
          (Some (DisplayName.to_value x.groupDisplayName)));
        ("GroupSecurityIdentifier",
          (Some (GroupSecurityIdentifier.to_value x.groupSecurityIdentifier)));
        ("TemplateArn", (Some (TemplateArn.to_value x.templateArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateArn =
        TemplateArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TemplateArn") in
      let groupSecurityIdentifier =
        GroupSecurityIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GroupSecurityIdentifier") in
      let groupDisplayName =
        DisplayName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GroupDisplayName") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let accessRights =
        AccessRights.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AccessRights") in
      make ~templateArn ~groupSecurityIdentifier ~groupDisplayName
        ?clientToken ~accessRights ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateArn =
        field_map_exn json__ "TemplateArn" TemplateArn.of_json in
      let groupSecurityIdentifier =
        field_map_exn json__ "GroupSecurityIdentifier"
          GroupSecurityIdentifier.of_json in
      let groupDisplayName =
        field_map_exn json__ "GroupDisplayName" DisplayName.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      let accessRights =
        field_map_exn json__ "AccessRights" AccessRights.of_json in
      make ~templateArn ~groupSecurityIdentifier ~groupDisplayName
        ?clientToken ~accessRights ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a group access control entry. Allow or deny Active Directory groups from enrolling and/or autoenrolling with the template based on the group security identifiers (SIDs)."]
module CreateServicePrincipalNameRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option [@ocaml.doc "Idempotency token."];
      connectorArn: ConnectorArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateConnector."];
      directoryRegistrationArn: DirectoryRegistrationArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration."]}
    let context_ = "CreateServicePrincipalNameRequest"
    let make ?clientToken =
      fun ~connectorArn ->
        fun ~directoryRegistrationArn ->
          fun () -> { clientToken; connectorArn; directoryRegistrationArn }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("ConnectorArn", (Some (ConnectorArn.to_value x.connectorArn)));
        ("DirectoryRegistrationArn",
          (Some
             (DirectoryRegistrationArn.to_value x.directoryRegistrationArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let directoryRegistrationArn =
        DirectoryRegistrationArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "DirectoryRegistrationArn") in
      let connectorArn =
        ConnectorArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectorArn") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ~directoryRegistrationArn ~connectorArn ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let directoryRegistrationArn =
        field_map_exn json__ "DirectoryRegistrationArn"
          DirectoryRegistrationArn.of_json in
      let connectorArn =
        field_map_exn json__ "ConnectorArn" ConnectorArn.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      make ~directoryRegistrationArn ~connectorArn ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a service principal name (SPN) for the service account in Active Directory. Kerberos authentication uses SPNs to associate a service instance with a service sign-in account."]
module CreateDirectoryRegistrationResponse =
  struct
    type nonrec t =
      {
      directoryRegistrationArn: DirectoryRegistrationArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?directoryRegistrationArn =
      fun () -> { directoryRegistrationArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DirectoryRegistrationArn",
           (Option.map x.directoryRegistrationArn
              ~f:DirectoryRegistrationArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let directoryRegistrationArn =
        (Option.map ~f:DirectoryRegistrationArn.of_xml)
          (Xml.child xml_arg0 "DirectoryRegistrationArn") in
      make ?directoryRegistrationArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let directoryRegistrationArn =
        field_map json__ "DirectoryRegistrationArn"
          DirectoryRegistrationArn.of_json in
      make ?directoryRegistrationArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a directory registration that authorizes communication between Amazon Web Services Private CA and an Active Directory"]
module CreateDirectoryRegistrationRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option [@ocaml.doc "Idempotency token."];
      directoryId: DirectoryId.t
        [@ocaml.doc "The identifier of the Active Directory."];
      tags: Tags.t option
        [@ocaml.doc
          "Metadata assigned to a directory registration consisting of a key-value pair."]}
    let context_ = "CreateDirectoryRegistrationRequest"
    let make ?clientToken =
      fun ?tags ->
        fun ~directoryId -> fun () -> { clientToken; tags; directoryId }
    let to_value x =
      structure_to_value
        [("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      make ?tags ~directoryId ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" Tags.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      make ?tags ~directoryId ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a directory registration that authorizes communication between Amazon Web Services Private CA and an Active Directory"]
module CreateConnectorResponse =
  struct
    type nonrec t =
      {
      connectorArn: ConnectorArn.t option
        [@ocaml.doc
          "If successful, the Amazon Resource Name (ARN) of the connector for Active Directory."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?connectorArn = fun () -> { connectorArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ConnectorArn",
           (Option.map x.connectorArn ~f:ConnectorArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connectorArn =
        (Option.map ~f:ConnectorArn.of_xml)
          (Xml.child xml_arg0 "ConnectorArn") in
      make ?connectorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let connectorArn = field_map json__ "ConnectorArn" ConnectorArn.of_json in
      make ?connectorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a connector between Amazon Web Services Private CA and an Active Directory. You must specify the private CA, directory ID, and security groups."]
module CreateConnectorRequest =
  struct
    type nonrec t =
      {
      certificateAuthorityArn: CertificateAuthorityArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the certificate authority being used."];
      clientToken: ClientToken.t option [@ocaml.doc "Idempotency token."];
      directoryId: DirectoryId.t
        [@ocaml.doc "The identifier of the Active Directory."];
      tags: Tags.t option
        [@ocaml.doc
          "Metadata assigned to a connector consisting of a key-value pair."];
      vpcInformation: VpcInformation.t
        [@ocaml.doc
          "Information about your VPC and security groups used with the connector."]}
    let context_ = "CreateConnectorRequest"
    let make ?clientToken =
      fun ?tags ->
        fun ~certificateAuthorityArn ->
          fun ~directoryId ->
            fun ~vpcInformation ->
              fun () ->
                {
                  clientToken;
                  tags;
                  certificateAuthorityArn;
                  directoryId;
                  vpcInformation
                }
    let to_value x =
      structure_to_value
        [("CertificateAuthorityArn",
           (Some (CertificateAuthorityArn.to_value x.certificateAuthorityArn)));
        ("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("DirectoryId", (Some (DirectoryId.to_value x.directoryId)));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("VpcInformation", (Some (VpcInformation.to_value x.vpcInformation)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcInformation =
        VpcInformation.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpcInformation") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let directoryId =
        DirectoryId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DirectoryId") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let certificateAuthorityArn =
        CertificateAuthorityArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CertificateAuthorityArn") in
      make ~vpcInformation ?tags ~directoryId ?clientToken
        ~certificateAuthorityArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vpcInformation =
        field_map_exn json__ "VpcInformation" VpcInformation.of_json in
      let tags = field_map json__ "Tags" Tags.of_json in
      let directoryId =
        field_map_exn json__ "DirectoryId" DirectoryId.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      let certificateAuthorityArn =
        field_map_exn json__ "CertificateAuthorityArn"
          CertificateAuthorityArn.of_json in
      make ~vpcInformation ?tags ~directoryId ?clientToken
        ~certificateAuthorityArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a connector between Amazon Web Services Private CA and an Active Directory. You must specify the private CA, directory ID, and security groups."]
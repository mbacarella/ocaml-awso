(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_domain_transfer_from_another_aws_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and password = flag "password" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_domain_transfer_from_another_aws_account
           (Values.AcceptDomainTransferFromAnotherAwsAccountRequest.make
              ~domainName ~password ())
           (Some
              Values.AcceptDomainTransferFromAnotherAwsAccountResponse.to_json)
           (Some
              Values.AcceptDomainTransferFromAnotherAwsAccountResponse.error_to_json)])
let cancel_domain_transfer_to_another_aws_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_domain_transfer_to_another_aws_account
           (Values.CancelDomainTransferToAnotherAwsAccountRequest.make
              ~domainName ())
           (Some
              Values.CancelDomainTransferToAnotherAwsAccountResponse.to_json)
           (Some
              Values.CancelDomainTransferToAnotherAwsAccountResponse.error_to_json)])
let check_domain_availability =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and idnLangCode =
         flag "idn-lang-code" (optional string) ~doc:"STRING LangCode"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.check_domain_availability
           (Values.CheckDomainAvailabilityRequest.make ?idnLangCode
              ~domainName ())
           (Some Values.CheckDomainAvailabilityResponse.to_json)
           (Some Values.CheckDomainAvailabilityResponse.error_to_json)])
let check_domain_transferability =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authCode =
         flag "auth-code" (optional string) ~doc:"STRING DomainAuthCode"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.check_domain_transferability
           (Values.CheckDomainTransferabilityRequest.make ?authCode
              ~domainName ())
           (Some Values.CheckDomainTransferabilityResponse.to_json)
           (Some Values.CheckDomainTransferabilityResponse.error_to_json)])
let delete_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain (Values.DeleteDomainRequest.make ~domainName ())
           (Some Values.DeleteDomainResponse.to_json)
           (Some Values.DeleteDomainResponse.error_to_json)])
let delete_tags_for_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and tagsToDelete =
         flag "tags-to-delete" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tags_for_domain
           (Values.DeleteTagsForDomainRequest.make ~domainName
              ~tagsToDelete:(Values.TagKeyList.of_json tagsToDelete) ())
           (Some Values.DeleteTagsForDomainResponse.to_json)
           (Some Values.DeleteTagsForDomainResponse.error_to_json)])
let disable_domain_auto_renew =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_domain_auto_renew
           (Values.DisableDomainAutoRenewRequest.make ~domainName ())
           (Some Values.DisableDomainAutoRenewResponse.to_json)
           (Some Values.DisableDomainAutoRenewResponse.error_to_json)])
let disable_domain_transfer_lock =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_domain_transfer_lock
           (Values.DisableDomainTransferLockRequest.make ~domainName ())
           (Some Values.DisableDomainTransferLockResponse.to_json)
           (Some Values.DisableDomainTransferLockResponse.error_to_json)])
let enable_domain_auto_renew =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_domain_auto_renew
           (Values.EnableDomainAutoRenewRequest.make ~domainName ())
           (Some Values.EnableDomainAutoRenewResponse.to_json)
           (Some Values.EnableDomainAutoRenewResponse.error_to_json)])
let enable_domain_transfer_lock =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_domain_transfer_lock
           (Values.EnableDomainTransferLockRequest.make ~domainName ())
           (Some Values.EnableDomainTransferLockResponse.to_json)
           (Some Values.EnableDomainTransferLockResponse.error_to_json)])
let get_contact_reachability_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_contact_reachability_status
           (Values.GetContactReachabilityStatusRequest.make ?domainName ())
           (Some Values.GetContactReachabilityStatusResponse.to_json)
           (Some Values.GetContactReachabilityStatusResponse.error_to_json)])
let get_domain_detail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_detail
           (Values.GetDomainDetailRequest.make ~domainName ())
           (Some Values.GetDomainDetailResponse.to_json)
           (Some Values.GetDomainDetailResponse.error_to_json)])
let get_domain_suggestions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and suggestionCount =
         flag "suggestion-count" (required int) ~doc:"INT Integer"
       and onlyAvailable =
         flag "only-available" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_suggestions
           (Values.GetDomainSuggestionsRequest.make ~domainName
              ~suggestionCount ~onlyAvailable ())
           (Some Values.GetDomainSuggestionsResponse.to_json)
           (Some Values.GetDomainSuggestionsResponse.error_to_json)])
let get_operation_detail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operationId =
         flag "operation-id" (required string) ~doc:"STRING OperationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_operation_detail
           (Values.GetOperationDetailRequest.make ~operationId ())
           (Some Values.GetOperationDetailResponse.to_json)
           (Some Values.GetOperationDetailResponse.error_to_json)])
let list_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterConditions =
         flag "filter-conditions" (optional json_arg)
           ~doc:"JSON FilterConditions"
       and sortCondition =
         flag "sort-condition" (optional json_arg) ~doc:"JSON SortCondition"
       and marker = flag "marker" (optional string) ~doc:"STRING PageMarker"
       and maxItems = flag "max-items" (optional int) ~doc:"INT PageMaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domains
           (Values.ListDomainsRequest.make
              ?filterConditions:(Option.map
                                   ~f:Values.FilterConditions.of_json
                                   filterConditions)
              ?sortCondition:(Option.map ~f:Values.SortCondition.of_json
                                sortCondition) ?marker ?maxItems ())
           (Some Values.ListDomainsResponse.to_json)
           (Some Values.ListDomainsResponse.error_to_json)])
let list_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and submittedSince =
         flag "submitted-since" (optional json_arg) ~doc:"JSON Timestamp"
       and marker = flag "marker" (optional string) ~doc:"STRING PageMarker"
       and maxItems = flag "max-items" (optional int) ~doc:"INT PageMaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_operations
           (Values.ListOperationsRequest.make
              ?submittedSince:(Option.map ~f:Values.Timestamp.of_json
                                 submittedSince) ?marker ?maxItems ())
           (Some Values.ListOperationsResponse.to_json)
           (Some Values.ListOperationsResponse.error_to_json)])
let list_prices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tld = flag "tld" (optional string) ~doc:"STRING TldName"
       and marker = flag "marker" (optional string) ~doc:"STRING PageMarker"
       and maxItems = flag "max-items" (optional int) ~doc:"INT PageMaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_prices
           (Values.ListPricesRequest.make ?tld ?marker ?maxItems ())
           (Some Values.ListPricesResponse.to_json)
           (Some Values.ListPricesResponse.error_to_json)])
let list_tags_for_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_domain
           (Values.ListTagsForDomainRequest.make ~domainName ())
           (Some Values.ListTagsForDomainResponse.to_json)
           (Some Values.ListTagsForDomainResponse.error_to_json)])
let register_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and idnLangCode =
         flag "idn-lang-code" (optional string) ~doc:"STRING LangCode"
       and autoRenew = flag "auto-renew" (optional bool) ~doc:"BOOL Boolean"
       and privacyProtectAdminContact =
         flag "privacy-protect-admin-contact" (optional bool)
           ~doc:"BOOL Boolean"
       and privacyProtectRegistrantContact =
         flag "privacy-protect-registrant-contact" (optional bool)
           ~doc:"BOOL Boolean"
       and privacyProtectTechContact =
         flag "privacy-protect-tech-contact" (optional bool)
           ~doc:"BOOL Boolean"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and durationInYears =
         flag "duration-in-years" (required int) ~doc:"INT DurationInYears"
       and adminContact =
         flag "admin-contact" (required json_arg) ~doc:"JSON ContactDetail"
       and registrantContact =
         flag "registrant-contact" (required json_arg)
           ~doc:"JSON ContactDetail"
       and techContact =
         flag "tech-contact" (required json_arg) ~doc:"JSON ContactDetail" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_domain
           (Values.RegisterDomainRequest.make ?idnLangCode ?autoRenew
              ?privacyProtectAdminContact ?privacyProtectRegistrantContact
              ?privacyProtectTechContact ~domainName ~durationInYears
              ~adminContact:(Values.ContactDetail.of_json adminContact)
              ~registrantContact:(Values.ContactDetail.of_json
                                    registrantContact)
              ~techContact:(Values.ContactDetail.of_json techContact) ())
           (Some Values.RegisterDomainResponse.to_json)
           (Some Values.RegisterDomainResponse.error_to_json)])
let reject_domain_transfer_from_another_aws_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_domain_transfer_from_another_aws_account
           (Values.RejectDomainTransferFromAnotherAwsAccountRequest.make
              ~domainName ())
           (Some
              Values.RejectDomainTransferFromAnotherAwsAccountResponse.to_json)
           (Some
              Values.RejectDomainTransferFromAnotherAwsAccountResponse.error_to_json)])
let renew_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and durationInYears =
         flag "duration-in-years" (optional int) ~doc:"INT DurationInYears"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and currentExpiryYear =
         flag "current-expiry-year" (required int)
           ~doc:"INT CurrentExpiryYear" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.renew_domain
           (Values.RenewDomainRequest.make ?durationInYears ~domainName
              ~currentExpiryYear ())
           (Some Values.RenewDomainResponse.to_json)
           (Some Values.RenewDomainResponse.error_to_json)])
let resend_contact_reachability_email =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resend_contact_reachability_email
           (Values.ResendContactReachabilityEmailRequest.make ?domainName ())
           (Some Values.ResendContactReachabilityEmailResponse.to_json)
           (Some Values.ResendContactReachabilityEmailResponse.error_to_json)])
let retrieve_domain_auth_code =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.retrieve_domain_auth_code
           (Values.RetrieveDomainAuthCodeRequest.make ~domainName ())
           (Some Values.RetrieveDomainAuthCodeResponse.to_json)
           (Some Values.RetrieveDomainAuthCodeResponse.error_to_json)])
let transfer_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and idnLangCode =
         flag "idn-lang-code" (optional string) ~doc:"STRING LangCode"
       and nameservers =
         flag "nameservers" (optional json_arg) ~doc:"JSON NameserverList"
       and authCode =
         flag "auth-code" (optional string) ~doc:"STRING DomainAuthCode"
       and autoRenew = flag "auto-renew" (optional bool) ~doc:"BOOL Boolean"
       and privacyProtectAdminContact =
         flag "privacy-protect-admin-contact" (optional bool)
           ~doc:"BOOL Boolean"
       and privacyProtectRegistrantContact =
         flag "privacy-protect-registrant-contact" (optional bool)
           ~doc:"BOOL Boolean"
       and privacyProtectTechContact =
         flag "privacy-protect-tech-contact" (optional bool)
           ~doc:"BOOL Boolean"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and durationInYears =
         flag "duration-in-years" (required int) ~doc:"INT DurationInYears"
       and adminContact =
         flag "admin-contact" (required json_arg) ~doc:"JSON ContactDetail"
       and registrantContact =
         flag "registrant-contact" (required json_arg)
           ~doc:"JSON ContactDetail"
       and techContact =
         flag "tech-contact" (required json_arg) ~doc:"JSON ContactDetail" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.transfer_domain
           (Values.TransferDomainRequest.make ?idnLangCode
              ?nameservers:(Option.map ~f:Values.NameserverList.of_json
                              nameservers) ?authCode ?autoRenew
              ?privacyProtectAdminContact ?privacyProtectRegistrantContact
              ?privacyProtectTechContact ~domainName ~durationInYears
              ~adminContact:(Values.ContactDetail.of_json adminContact)
              ~registrantContact:(Values.ContactDetail.of_json
                                    registrantContact)
              ~techContact:(Values.ContactDetail.of_json techContact) ())
           (Some Values.TransferDomainResponse.to_json)
           (Some Values.TransferDomainResponse.error_to_json)])
let transfer_domain_to_another_aws_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.transfer_domain_to_another_aws_account
           (Values.TransferDomainToAnotherAwsAccountRequest.make ~domainName
              ~accountId ())
           (Some Values.TransferDomainToAnotherAwsAccountResponse.to_json)
           (Some
              Values.TransferDomainToAnotherAwsAccountResponse.error_to_json)])
let update_domain_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and adminContact =
         flag "admin-contact" (optional json_arg) ~doc:"JSON ContactDetail"
       and registrantContact =
         flag "registrant-contact" (optional json_arg)
           ~doc:"JSON ContactDetail"
       and techContact =
         flag "tech-contact" (optional json_arg) ~doc:"JSON ContactDetail"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain_contact
           (Values.UpdateDomainContactRequest.make
              ?adminContact:(Option.map ~f:Values.ContactDetail.of_json
                               adminContact)
              ?registrantContact:(Option.map ~f:Values.ContactDetail.of_json
                                    registrantContact)
              ?techContact:(Option.map ~f:Values.ContactDetail.of_json
                              techContact) ~domainName ())
           (Some Values.UpdateDomainContactResponse.to_json)
           (Some Values.UpdateDomainContactResponse.error_to_json)])
let update_domain_contact_privacy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and adminPrivacy =
         flag "admin-privacy" (optional bool) ~doc:"BOOL Boolean"
       and registrantPrivacy =
         flag "registrant-privacy" (optional bool) ~doc:"BOOL Boolean"
       and techPrivacy =
         flag "tech-privacy" (optional bool) ~doc:"BOOL Boolean"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain_contact_privacy
           (Values.UpdateDomainContactPrivacyRequest.make ?adminPrivacy
              ?registrantPrivacy ?techPrivacy ~domainName ())
           (Some Values.UpdateDomainContactPrivacyResponse.to_json)
           (Some Values.UpdateDomainContactPrivacyResponse.error_to_json)])
let update_domain_nameservers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fIAuthKey =
         flag "f-i-auth-key" (optional string) ~doc:"STRING FIAuthKey"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and nameservers =
         flag "nameservers" (required json_arg) ~doc:"JSON NameserverList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain_nameservers
           (Values.UpdateDomainNameserversRequest.make ?fIAuthKey ~domainName
              ~nameservers:(Values.NameserverList.of_json nameservers) ())
           (Some Values.UpdateDomainNameserversResponse.to_json)
           (Some Values.UpdateDomainNameserversResponse.error_to_json)])
let update_tags_for_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagsToUpdate =
         flag "tags-to-update" (optional json_arg) ~doc:"JSON TagList"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_tags_for_domain
           (Values.UpdateTagsForDomainRequest.make
              ?tagsToUpdate:(Option.map ~f:Values.TagList.of_json
                               tagsToUpdate) ~domainName ())
           (Some Values.UpdateTagsForDomainResponse.to_json)
           (Some Values.UpdateTagsForDomainResponse.error_to_json)])
let view_billing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and start = flag "start" (optional json_arg) ~doc:"JSON Timestamp"
       and end_ = flag "end-" (optional json_arg) ~doc:"JSON Timestamp"
       and marker = flag "marker" (optional string) ~doc:"STRING PageMarker"
       and maxItems = flag "max-items" (optional int) ~doc:"INT PageMaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.view_billing
           (Values.ViewBillingRequest.make
              ?start:(Option.map ~f:Values.Timestamp.of_json start)
              ?end_:(Option.map ~f:Values.Timestamp.of_json end_) ?marker
              ?maxItems ()) (Some Values.ViewBillingResponse.to_json)
           (Some Values.ViewBillingResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-domain-transfer-from-another-aws-account",
       accept_domain_transfer_from_another_aws_account);
    ("cancel-domain-transfer-to-another-aws-account",
      cancel_domain_transfer_to_another_aws_account);
    ("check-domain-availability", check_domain_availability);
    ("check-domain-transferability", check_domain_transferability);
    ("delete-domain", delete_domain);
    ("delete-tags-for-domain", delete_tags_for_domain);
    ("disable-domain-auto-renew", disable_domain_auto_renew);
    ("disable-domain-transfer-lock", disable_domain_transfer_lock);
    ("enable-domain-auto-renew", enable_domain_auto_renew);
    ("enable-domain-transfer-lock", enable_domain_transfer_lock);
    ("get-contact-reachability-status", get_contact_reachability_status);
    ("get-domain-detail", get_domain_detail);
    ("get-domain-suggestions", get_domain_suggestions);
    ("get-operation-detail", get_operation_detail);
    ("list-domains", list_domains);
    ("list-operations", list_operations);
    ("list-prices", list_prices);
    ("list-tags-for-domain", list_tags_for_domain);
    ("register-domain", register_domain);
    ("reject-domain-transfer-from-another-aws-account",
      reject_domain_transfer_from_another_aws_account);
    ("renew-domain", renew_domain);
    ("resend-contact-reachability-email", resend_contact_reachability_email);
    ("retrieve-domain-auth-code", retrieve_domain_auth_code);
    ("transfer-domain", transfer_domain);
    ("transfer-domain-to-another-aws-account",
      transfer_domain_to_another_aws_account);
    ("update-domain-contact", update_domain_contact);
    ("update-domain-contact-privacy", update_domain_contact_privacy);
    ("update-domain-nameservers", update_domain_nameservers);
    ("update-tags-for-domain", update_tags_for_domain);
    ("view-billing", view_billing)]
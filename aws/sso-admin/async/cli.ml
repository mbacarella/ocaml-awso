(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let attach_managed_policy_to_permission_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn"
       and managedPolicyArn =
         flag "managed-policy-arn" (required string)
           ~doc:"STRING ManagedPolicyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_managed_policy_to_permission_set
           (Values.AttachManagedPolicyToPermissionSetRequest.make
              ~instanceArn ~permissionSetArn ~managedPolicyArn ())
           (Some Values.AttachManagedPolicyToPermissionSetResponse.to_json)
           (Some
              Values.AttachManagedPolicyToPermissionSetResponse.error_to_json)])
let create_account_assignment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and targetId =
         flag "target-id" (required string) ~doc:"STRING TargetId"
       and targetType =
         flag "target-type" (required json_arg) ~doc:"JSON TargetType"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn"
       and principalType =
         flag "principal-type" (required json_arg) ~doc:"JSON PrincipalType"
       and principalId =
         flag "principal-id" (required string) ~doc:"STRING PrincipalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_account_assignment
           (Values.CreateAccountAssignmentRequest.make ~instanceArn ~targetId
              ~targetType:(Values.TargetType.of_json targetType)
              ~permissionSetArn
              ~principalType:(Values.PrincipalType.of_json principalType)
              ~principalId ())
           (Some Values.CreateAccountAssignmentResponse.to_json)
           (Some Values.CreateAccountAssignmentResponse.error_to_json)])
let create_instance_access_control_attribute_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and instanceAccessControlAttributeConfiguration =
         flag "instance-access-control-attribute-configuration"
           (required json_arg)
           ~doc:"JSON InstanceAccessControlAttributeConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_instance_access_control_attribute_configuration
           (Values.CreateInstanceAccessControlAttributeConfigurationRequest.make
              ~instanceArn
              ~instanceAccessControlAttributeConfiguration:(Values.InstanceAccessControlAttributeConfiguration.of_json
                                                              instanceAccessControlAttributeConfiguration)
              ())
           (Some
              Values.CreateInstanceAccessControlAttributeConfigurationResponse.to_json)
           (Some
              Values.CreateInstanceAccessControlAttributeConfigurationResponse.error_to_json)])
let create_permission_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PermissionSetDescription"
       and sessionDuration =
         flag "session-duration" (optional string) ~doc:"STRING Duration"
       and relayState =
         flag "relay-state" (optional string) ~doc:"STRING RelayState"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string) ~doc:"STRING PermissionSetName"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_permission_set
           (Values.CreatePermissionSetRequest.make ?description
              ?sessionDuration ?relayState
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~instanceArn ())
           (Some Values.CreatePermissionSetResponse.to_json)
           (Some Values.CreatePermissionSetResponse.error_to_json)])
let delete_account_assignment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and targetId =
         flag "target-id" (required string) ~doc:"STRING TargetId"
       and targetType =
         flag "target-type" (required json_arg) ~doc:"JSON TargetType"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn"
       and principalType =
         flag "principal-type" (required json_arg) ~doc:"JSON PrincipalType"
       and principalId =
         flag "principal-id" (required string) ~doc:"STRING PrincipalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_account_assignment
           (Values.DeleteAccountAssignmentRequest.make ~instanceArn ~targetId
              ~targetType:(Values.TargetType.of_json targetType)
              ~permissionSetArn
              ~principalType:(Values.PrincipalType.of_json principalType)
              ~principalId ())
           (Some Values.DeleteAccountAssignmentResponse.to_json)
           (Some Values.DeleteAccountAssignmentResponse.error_to_json)])
let delete_inline_policy_from_permission_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_inline_policy_from_permission_set
           (Values.DeleteInlinePolicyFromPermissionSetRequest.make
              ~instanceArn ~permissionSetArn ())
           (Some Values.DeleteInlinePolicyFromPermissionSetResponse.to_json)
           (Some
              Values.DeleteInlinePolicyFromPermissionSetResponse.error_to_json)])
let delete_instance_access_control_attribute_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_instance_access_control_attribute_configuration
           (Values.DeleteInstanceAccessControlAttributeConfigurationRequest.make
              ~instanceArn ())
           (Some
              Values.DeleteInstanceAccessControlAttributeConfigurationResponse.to_json)
           (Some
              Values.DeleteInstanceAccessControlAttributeConfigurationResponse.error_to_json)])
let delete_permission_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_permission_set
           (Values.DeletePermissionSetRequest.make ~instanceArn
              ~permissionSetArn ())
           (Some Values.DeletePermissionSetResponse.to_json)
           (Some Values.DeletePermissionSetResponse.error_to_json)])
let describe_account_assignment_creation_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and accountAssignmentCreationRequestId =
         flag "account-assignment-creation-request-id" (required string)
           ~doc:"STRING UUId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_assignment_creation_status
           (Values.DescribeAccountAssignmentCreationStatusRequest.make
              ~instanceArn ~accountAssignmentCreationRequestId ())
           (Some
              Values.DescribeAccountAssignmentCreationStatusResponse.to_json)
           (Some
              Values.DescribeAccountAssignmentCreationStatusResponse.error_to_json)])
let describe_account_assignment_deletion_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and accountAssignmentDeletionRequestId =
         flag "account-assignment-deletion-request-id" (required string)
           ~doc:"STRING UUId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_assignment_deletion_status
           (Values.DescribeAccountAssignmentDeletionStatusRequest.make
              ~instanceArn ~accountAssignmentDeletionRequestId ())
           (Some
              Values.DescribeAccountAssignmentDeletionStatusResponse.to_json)
           (Some
              Values.DescribeAccountAssignmentDeletionStatusResponse.error_to_json)])
let describe_instance_access_control_attribute_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_access_control_attribute_configuration
           (Values.DescribeInstanceAccessControlAttributeConfigurationRequest.make
              ~instanceArn ())
           (Some
              Values.DescribeInstanceAccessControlAttributeConfigurationResponse.to_json)
           (Some
              Values.DescribeInstanceAccessControlAttributeConfigurationResponse.error_to_json)])
let describe_permission_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_permission_set
           (Values.DescribePermissionSetRequest.make ~instanceArn
              ~permissionSetArn ())
           (Some Values.DescribePermissionSetResponse.to_json)
           (Some Values.DescribePermissionSetResponse.error_to_json)])
let describe_permission_set_provisioning_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and provisionPermissionSetRequestId =
         flag "provision-permission-set-request-id" (required string)
           ~doc:"STRING UUId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_permission_set_provisioning_status
           (Values.DescribePermissionSetProvisioningStatusRequest.make
              ~instanceArn ~provisionPermissionSetRequestId ())
           (Some
              Values.DescribePermissionSetProvisioningStatusResponse.to_json)
           (Some
              Values.DescribePermissionSetProvisioningStatusResponse.error_to_json)])
let detach_managed_policy_from_permission_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn"
       and managedPolicyArn =
         flag "managed-policy-arn" (required string)
           ~doc:"STRING ManagedPolicyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_managed_policy_from_permission_set
           (Values.DetachManagedPolicyFromPermissionSetRequest.make
              ~instanceArn ~permissionSetArn ~managedPolicyArn ())
           (Some Values.DetachManagedPolicyFromPermissionSetResponse.to_json)
           (Some
              Values.DetachManagedPolicyFromPermissionSetResponse.error_to_json)])
let get_inline_policy_for_permission_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_inline_policy_for_permission_set
           (Values.GetInlinePolicyForPermissionSetRequest.make ~instanceArn
              ~permissionSetArn ())
           (Some Values.GetInlinePolicyForPermissionSetResponse.to_json)
           (Some Values.GetInlinePolicyForPermissionSetResponse.error_to_json)])
let list_account_assignment_creation_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON OperationStatusFilter"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_account_assignment_creation_status
           (Values.ListAccountAssignmentCreationStatusRequest.make
              ?maxResults ?nextToken
              ?filter:(Option.map ~f:Values.OperationStatusFilter.of_json
                         filter) ~instanceArn ())
           (Some Values.ListAccountAssignmentCreationStatusResponse.to_json)
           (Some
              Values.ListAccountAssignmentCreationStatusResponse.error_to_json)])
let list_account_assignment_deletion_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON OperationStatusFilter"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_account_assignment_deletion_status
           (Values.ListAccountAssignmentDeletionStatusRequest.make
              ?maxResults ?nextToken
              ?filter:(Option.map ~f:Values.OperationStatusFilter.of_json
                         filter) ~instanceArn ())
           (Some Values.ListAccountAssignmentDeletionStatusResponse.to_json)
           (Some
              Values.ListAccountAssignmentDeletionStatusResponse.error_to_json)])
let list_account_assignments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING TargetId"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_account_assignments
           (Values.ListAccountAssignmentsRequest.make ?maxResults ?nextToken
              ~instanceArn ~accountId ~permissionSetArn ())
           (Some Values.ListAccountAssignmentsResponse.to_json)
           (Some Values.ListAccountAssignmentsResponse.error_to_json)])
let list_accounts_for_provisioned_permission_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and provisioningStatus =
         flag "provisioning-status" (optional json_arg)
           ~doc:"JSON ProvisioningStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_accounts_for_provisioned_permission_set
           (Values.ListAccountsForProvisionedPermissionSetRequest.make
              ?provisioningStatus:(Option.map
                                     ~f:Values.ProvisioningStatus.of_json
                                     provisioningStatus) ?maxResults
              ?nextToken ~instanceArn ~permissionSetArn ())
           (Some
              Values.ListAccountsForProvisionedPermissionSetResponse.to_json)
           (Some
              Values.ListAccountsForProvisionedPermissionSetResponse.error_to_json)])
let list_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_instances
           (Values.ListInstancesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListInstancesResponse.to_json)
           (Some Values.ListInstancesResponse.error_to_json)])
let list_managed_policies_in_permission_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_policies_in_permission_set
           (Values.ListManagedPoliciesInPermissionSetRequest.make ?maxResults
              ?nextToken ~instanceArn ~permissionSetArn ())
           (Some Values.ListManagedPoliciesInPermissionSetResponse.to_json)
           (Some
              Values.ListManagedPoliciesInPermissionSetResponse.error_to_json)])
let list_permission_set_provisioning_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON OperationStatusFilter"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_permission_set_provisioning_status
           (Values.ListPermissionSetProvisioningStatusRequest.make
              ?maxResults ?nextToken
              ?filter:(Option.map ~f:Values.OperationStatusFilter.of_json
                         filter) ~instanceArn ())
           (Some Values.ListPermissionSetProvisioningStatusResponse.to_json)
           (Some
              Values.ListPermissionSetProvisioningStatusResponse.error_to_json)])
let list_permission_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_permission_sets
           (Values.ListPermissionSetsRequest.make ?nextToken ?maxResults
              ~instanceArn ())
           (Some Values.ListPermissionSetsResponse.to_json)
           (Some Values.ListPermissionSetsResponse.error_to_json)])
let list_permission_sets_provisioned_to_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and provisioningStatus =
         flag "provisioning-status" (optional json_arg)
           ~doc:"JSON ProvisioningStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_permission_sets_provisioned_to_account
           (Values.ListPermissionSetsProvisionedToAccountRequest.make
              ?provisioningStatus:(Option.map
                                     ~f:Values.ProvisioningStatus.of_json
                                     provisioningStatus) ?maxResults
              ?nextToken ~instanceArn ~accountId ())
           (Some
              Values.ListPermissionSetsProvisionedToAccountResponse.to_json)
           (Some
              Values.ListPermissionSetsProvisionedToAccountResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING GeneralArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?nextToken ~instanceArn
              ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let provision_permission_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetId =
         flag "target-id" (optional string) ~doc:"STRING TargetId"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn"
       and targetType =
         flag "target-type" (required json_arg)
           ~doc:"JSON ProvisionTargetType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.provision_permission_set
           (Values.ProvisionPermissionSetRequest.make ?targetId ~instanceArn
              ~permissionSetArn
              ~targetType:(Values.ProvisionTargetType.of_json targetType) ())
           (Some Values.ProvisionPermissionSetResponse.to_json)
           (Some Values.ProvisionPermissionSetResponse.error_to_json)])
let put_inline_policy_to_permission_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn"
       and inlinePolicy =
         flag "inline-policy" (required string)
           ~doc:"STRING PermissionSetPolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_inline_policy_to_permission_set
           (Values.PutInlinePolicyToPermissionSetRequest.make ~instanceArn
              ~permissionSetArn ~inlinePolicy ())
           (Some Values.PutInlinePolicyToPermissionSetResponse.to_json)
           (Some Values.PutInlinePolicyToPermissionSetResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING GeneralArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~instanceArn ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING GeneralArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~instanceArn ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_instance_access_control_attribute_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and instanceAccessControlAttributeConfiguration =
         flag "instance-access-control-attribute-configuration"
           (required json_arg)
           ~doc:"JSON InstanceAccessControlAttributeConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_instance_access_control_attribute_configuration
           (Values.UpdateInstanceAccessControlAttributeConfigurationRequest.make
              ~instanceArn
              ~instanceAccessControlAttributeConfiguration:(Values.InstanceAccessControlAttributeConfiguration.of_json
                                                              instanceAccessControlAttributeConfiguration)
              ())
           (Some
              Values.UpdateInstanceAccessControlAttributeConfigurationResponse.to_json)
           (Some
              Values.UpdateInstanceAccessControlAttributeConfigurationResponse.error_to_json)])
let update_permission_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PermissionSetDescription"
       and sessionDuration =
         flag "session-duration" (optional string) ~doc:"STRING Duration"
       and relayState =
         flag "relay-state" (optional string) ~doc:"STRING RelayState"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and permissionSetArn =
         flag "permission-set-arn" (required string)
           ~doc:"STRING PermissionSetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_permission_set
           (Values.UpdatePermissionSetRequest.make ?description
              ?sessionDuration ?relayState ~instanceArn ~permissionSetArn ())
           (Some Values.UpdatePermissionSetResponse.to_json)
           (Some Values.UpdatePermissionSetResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("attach-managed-policy-to-permission-set",
       attach_managed_policy_to_permission_set);
    ("create-account-assignment", create_account_assignment);
    ("create-instance-access-control-attribute-configuration",
      create_instance_access_control_attribute_configuration);
    ("create-permission-set", create_permission_set);
    ("delete-account-assignment", delete_account_assignment);
    ("delete-inline-policy-from-permission-set",
      delete_inline_policy_from_permission_set);
    ("delete-instance-access-control-attribute-configuration",
      delete_instance_access_control_attribute_configuration);
    ("delete-permission-set", delete_permission_set);
    ("describe-account-assignment-creation-status",
      describe_account_assignment_creation_status);
    ("describe-account-assignment-deletion-status",
      describe_account_assignment_deletion_status);
    ("describe-instance-access-control-attribute-configuration",
      describe_instance_access_control_attribute_configuration);
    ("describe-permission-set", describe_permission_set);
    ("describe-permission-set-provisioning-status",
      describe_permission_set_provisioning_status);
    ("detach-managed-policy-from-permission-set",
      detach_managed_policy_from_permission_set);
    ("get-inline-policy-for-permission-set",
      get_inline_policy_for_permission_set);
    ("list-account-assignment-creation-status",
      list_account_assignment_creation_status);
    ("list-account-assignment-deletion-status",
      list_account_assignment_deletion_status);
    ("list-account-assignments", list_account_assignments);
    ("list-accounts-for-provisioned-permission-set",
      list_accounts_for_provisioned_permission_set);
    ("list-instances", list_instances);
    ("list-managed-policies-in-permission-set",
      list_managed_policies_in_permission_set);
    ("list-permission-set-provisioning-status",
      list_permission_set_provisioning_status);
    ("list-permission-sets", list_permission_sets);
    ("list-permission-sets-provisioned-to-account",
      list_permission_sets_provisioned_to_account);
    ("list-tags-for-resource", list_tags_for_resource);
    ("provision-permission-set", provision_permission_set);
    ("put-inline-policy-to-permission-set",
      put_inline_policy_to_permission_set);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-instance-access-control-attribute-configuration",
      update_instance_access_control_attribute_configuration);
    ("update-permission-set", update_permission_set)]
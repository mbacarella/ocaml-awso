(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_service_quota_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_service_quota_template
           (Values.AssociateServiceQuotaTemplateRequest.make ())
           (Some Values.AssociateServiceQuotaTemplateResponse.to_json)
           (Some Values.AssociateServiceQuotaTemplateResponse.error_to_json)])
let create_support_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestId =
         flag "request-id" (required string) ~doc:"STRING RequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_support_case
           (Values.CreateSupportCaseRequest.make ~requestId ())
           (Some Values.CreateSupportCaseResponse.to_json)
           (Some Values.CreateSupportCaseResponse.error_to_json)])
let delete_service_quota_increase_request_from_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceCode =
         flag "service-code" (required string) ~doc:"STRING ServiceCode"
       and quotaCode =
         flag "quota-code" (required string) ~doc:"STRING QuotaCode"
       and awsRegion =
         flag "aws-region" (required string) ~doc:"STRING AwsRegion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service_quota_increase_request_from_template
           (Values.DeleteServiceQuotaIncreaseRequestFromTemplateRequest.make
              ~serviceCode ~quotaCode ~awsRegion ())
           (Some
              Values.DeleteServiceQuotaIncreaseRequestFromTemplateResponse.to_json)
           (Some
              Values.DeleteServiceQuotaIncreaseRequestFromTemplateResponse.error_to_json)])
let disassociate_service_quota_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_service_quota_template
           (Values.DisassociateServiceQuotaTemplateRequest.make ())
           (Some Values.DisassociateServiceQuotaTemplateResponse.to_json)
           (Some
              Values.DisassociateServiceQuotaTemplateResponse.error_to_json)])
let get_a_w_s_default_service_quota =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceCode =
         flag "service-code" (required string) ~doc:"STRING ServiceCode"
       and quotaCode =
         flag "quota-code" (required string) ~doc:"STRING QuotaCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_a_w_s_default_service_quota
           (Values.GetAWSDefaultServiceQuotaRequest.make ~serviceCode
              ~quotaCode ())
           (Some Values.GetAWSDefaultServiceQuotaResponse.to_json)
           (Some Values.GetAWSDefaultServiceQuotaResponse.error_to_json)])
let get_association_for_service_quota_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_association_for_service_quota_template
           (Values.GetAssociationForServiceQuotaTemplateRequest.make ())
           (Some Values.GetAssociationForServiceQuotaTemplateResponse.to_json)
           (Some
              Values.GetAssociationForServiceQuotaTemplateResponse.error_to_json)])
let get_auto_management_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_auto_management_configuration
           (Values.GetAutoManagementConfigurationRequest.make ())
           (Some Values.GetAutoManagementConfigurationResponse.to_json)
           (Some Values.GetAutoManagementConfigurationResponse.error_to_json)])
let get_quota_utilization_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsUtilization"
       and reportId =
         flag "report-id" (required string) ~doc:"STRING ReportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_quota_utilization_report
           (Values.GetQuotaUtilizationReportRequest.make ?nextToken
              ?maxResults ~reportId ())
           (Some Values.GetQuotaUtilizationReportResponse.to_json)
           (Some Values.GetQuotaUtilizationReportResponse.error_to_json)])
let get_requested_service_quota_change =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestId =
         flag "request-id" (required string) ~doc:"STRING RequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_requested_service_quota_change
           (Values.GetRequestedServiceQuotaChangeRequest.make ~requestId ())
           (Some Values.GetRequestedServiceQuotaChangeResponse.to_json)
           (Some Values.GetRequestedServiceQuotaChangeResponse.error_to_json)])
let get_service_quota =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contextId =
         flag "context-id" (optional string) ~doc:"STRING QuotaContextId"
       and serviceCode =
         flag "service-code" (required string) ~doc:"STRING ServiceCode"
       and quotaCode =
         flag "quota-code" (required string) ~doc:"STRING QuotaCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_quota
           (Values.GetServiceQuotaRequest.make ?contextId ~serviceCode
              ~quotaCode ()) (Some Values.GetServiceQuotaResponse.to_json)
           (Some Values.GetServiceQuotaResponse.error_to_json)])
let get_service_quota_increase_request_from_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceCode =
         flag "service-code" (required string) ~doc:"STRING ServiceCode"
       and quotaCode =
         flag "quota-code" (required string) ~doc:"STRING QuotaCode"
       and awsRegion =
         flag "aws-region" (required string) ~doc:"STRING AwsRegion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_quota_increase_request_from_template
           (Values.GetServiceQuotaIncreaseRequestFromTemplateRequest.make
              ~serviceCode ~quotaCode ~awsRegion ())
           (Some
              Values.GetServiceQuotaIncreaseRequestFromTemplateResponse.to_json)
           (Some
              Values.GetServiceQuotaIncreaseRequestFromTemplateResponse.error_to_json)])
let list_a_w_s_default_service_quotas =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and serviceCode =
         flag "service-code" (required string) ~doc:"STRING ServiceCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_a_w_s_default_service_quotas
           (Values.ListAWSDefaultServiceQuotasRequest.make ?nextToken
              ?maxResults ~serviceCode ())
           (Some Values.ListAWSDefaultServiceQuotasResponse.to_json)
           (Some Values.ListAWSDefaultServiceQuotasResponse.error_to_json)])
let list_requested_service_quota_change_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceCode =
         flag "service-code" (optional string) ~doc:"STRING ServiceCode"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON RequestStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and quotaRequestedAtLevel =
         flag "quota-requested-at-level" (optional json_arg)
           ~doc:"JSON AppliedLevelEnum" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_requested_service_quota_change_history
           (Values.ListRequestedServiceQuotaChangeHistoryRequest.make
              ?serviceCode
              ?status:(Option.map ~f:Values.RequestStatus.of_json status)
              ?nextToken ?maxResults
              ?quotaRequestedAtLevel:(Option.map
                                        ~f:Values.AppliedLevelEnum.of_json
                                        quotaRequestedAtLevel) ())
           (Some
              Values.ListRequestedServiceQuotaChangeHistoryResponse.to_json)
           (Some
              Values.ListRequestedServiceQuotaChangeHistoryResponse.error_to_json)])
let list_requested_service_quota_change_history_by_quota =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON RequestStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and quotaRequestedAtLevel =
         flag "quota-requested-at-level" (optional json_arg)
           ~doc:"JSON AppliedLevelEnum"
       and serviceCode =
         flag "service-code" (required string) ~doc:"STRING ServiceCode"
       and quotaCode =
         flag "quota-code" (required string) ~doc:"STRING QuotaCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_requested_service_quota_change_history_by_quota
           (Values.ListRequestedServiceQuotaChangeHistoryByQuotaRequest.make
              ?status:(Option.map ~f:Values.RequestStatus.of_json status)
              ?nextToken ?maxResults
              ?quotaRequestedAtLevel:(Option.map
                                        ~f:Values.AppliedLevelEnum.of_json
                                        quotaRequestedAtLevel) ~serviceCode
              ~quotaCode ())
           (Some
              Values.ListRequestedServiceQuotaChangeHistoryByQuotaResponse.to_json)
           (Some
              Values.ListRequestedServiceQuotaChangeHistoryByQuotaResponse.error_to_json)])
let list_service_quota_increase_requests_in_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceCode =
         flag "service-code" (optional string) ~doc:"STRING ServiceCode"
       and awsRegion =
         flag "aws-region" (optional string) ~doc:"STRING AwsRegion"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_quota_increase_requests_in_template
           (Values.ListServiceQuotaIncreaseRequestsInTemplateRequest.make
              ?serviceCode ?awsRegion ?nextToken ?maxResults ())
           (Some
              Values.ListServiceQuotaIncreaseRequestsInTemplateResponse.to_json)
           (Some
              Values.ListServiceQuotaIncreaseRequestsInTemplateResponse.error_to_json)])
let list_service_quotas =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and quotaCode =
         flag "quota-code" (optional string) ~doc:"STRING QuotaCode"
       and quotaAppliedAtLevel =
         flag "quota-applied-at-level" (optional json_arg)
           ~doc:"JSON AppliedLevelEnum"
       and serviceCode =
         flag "service-code" (required string) ~doc:"STRING ServiceCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_quotas
           (Values.ListServiceQuotasRequest.make ?nextToken ?maxResults
              ?quotaCode
              ?quotaAppliedAtLevel:(Option.map
                                      ~f:Values.AppliedLevelEnum.of_json
                                      quotaAppliedAtLevel) ~serviceCode ())
           (Some Values.ListServiceQuotasResponse.to_json)
           (Some Values.ListServiceQuotasResponse.error_to_json)])
let list_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_services
           (Values.ListServicesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListServicesResponse.to_json)
           (Some Values.ListServicesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_service_quota_increase_request_into_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and quotaCode =
         flag "quota-code" (required string) ~doc:"STRING QuotaCode"
       and serviceCode =
         flag "service-code" (required string) ~doc:"STRING ServiceCode"
       and awsRegion =
         flag "aws-region" (required string) ~doc:"STRING AwsRegion"
       and desiredValue =
         flag "desired-value" (required float) ~doc:"FLOAT QuotaValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_service_quota_increase_request_into_template
           (Values.PutServiceQuotaIncreaseRequestIntoTemplateRequest.make
              ~quotaCode ~serviceCode ~awsRegion ~desiredValue ())
           (Some
              Values.PutServiceQuotaIncreaseRequestIntoTemplateResponse.to_json)
           (Some
              Values.PutServiceQuotaIncreaseRequestIntoTemplateResponse.error_to_json)])
let request_service_quota_increase =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contextId =
         flag "context-id" (optional string) ~doc:"STRING QuotaContextId"
       and supportCaseAllowed =
         flag "support-case-allowed" (optional bool)
           ~doc:"BOOL SupportCaseAllowed"
       and serviceCode =
         flag "service-code" (required string) ~doc:"STRING ServiceCode"
       and quotaCode =
         flag "quota-code" (required string) ~doc:"STRING QuotaCode"
       and desiredValue =
         flag "desired-value" (required float) ~doc:"FLOAT QuotaValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.request_service_quota_increase
           (Values.RequestServiceQuotaIncreaseRequest.make ?contextId
              ?supportCaseAllowed ~serviceCode ~quotaCode ~desiredValue ())
           (Some Values.RequestServiceQuotaIncreaseResponse.to_json)
           (Some Values.RequestServiceQuotaIncreaseResponse.error_to_json)])
let start_auto_management =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notificationArn =
         flag "notification-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and exclusionList =
         flag "exclusion-list" (optional json_arg) ~doc:"JSON ExclusionList"
       and optInLevel =
         flag "opt-in-level" (required json_arg) ~doc:"JSON OptInLevel"
       and optInType =
         flag "opt-in-type" (required json_arg) ~doc:"JSON OptInType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_auto_management
           (Values.StartAutoManagementRequest.make ?notificationArn
              ?exclusionList:(Option.map ~f:Values.ExclusionList.of_json
                                exclusionList)
              ~optInLevel:(Values.OptInLevel.of_json optInLevel)
              ~optInType:(Values.OptInType.of_json optInType) ())
           (Some Values.StartAutoManagementResponse.to_json)
           (Some Values.StartAutoManagementResponse.error_to_json)])
let start_quota_utilization_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_quota_utilization_report
           (Values.StartQuotaUtilizationReportRequest.make ())
           (Some Values.StartQuotaUtilizationReportResponse.to_json)
           (Some Values.StartQuotaUtilizationReportResponse.error_to_json)])
let stop_auto_management =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_auto_management (Values.StopAutoManagementRequest.make ())
           (Some Values.StopAutoManagementResponse.to_json)
           (Some Values.StopAutoManagementResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON InputTags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.InputTags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON InputTagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.InputTagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_auto_management =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and optInType =
         flag "opt-in-type" (optional json_arg) ~doc:"JSON OptInType"
       and notificationArn =
         flag "notification-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and exclusionList =
         flag "exclusion-list" (optional json_arg) ~doc:"JSON ExclusionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_auto_management
           (Values.UpdateAutoManagementRequest.make
              ?optInType:(Option.map ~f:Values.OptInType.of_json optInType)
              ?notificationArn
              ?exclusionList:(Option.map ~f:Values.ExclusionList.of_json
                                exclusionList) ())
           (Some Values.UpdateAutoManagementResponse.to_json)
           (Some Values.UpdateAutoManagementResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-service-quota-template", associate_service_quota_template);
    ("create-support-case", create_support_case);
    ("delete-service-quota-increase-request-from-template",
      delete_service_quota_increase_request_from_template);
    ("disassociate-service-quota-template",
      disassociate_service_quota_template);
    ("get-a-w-s-default-service-quota", get_a_w_s_default_service_quota);
    ("get-association-for-service-quota-template",
      get_association_for_service_quota_template);
    ("get-auto-management-configuration", get_auto_management_configuration);
    ("get-quota-utilization-report", get_quota_utilization_report);
    ("get-requested-service-quota-change",
      get_requested_service_quota_change);
    ("get-service-quota", get_service_quota);
    ("get-service-quota-increase-request-from-template",
      get_service_quota_increase_request_from_template);
    ("list-a-w-s-default-service-quotas", list_a_w_s_default_service_quotas);
    ("list-requested-service-quota-change-history",
      list_requested_service_quota_change_history);
    ("list-requested-service-quota-change-history-by-quota",
      list_requested_service_quota_change_history_by_quota);
    ("list-service-quota-increase-requests-in-template",
      list_service_quota_increase_requests_in_template);
    ("list-service-quotas", list_service_quotas);
    ("list-services", list_services);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-service-quota-increase-request-into-template",
      put_service_quota_increase_request_into_template);
    ("request-service-quota-increase", request_service_quota_increase);
    ("start-auto-management", start_auto_management);
    ("start-quota-utilization-report", start_quota_utilization_report);
    ("stop-auto-management", stop_auto_management);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-auto-management", update_auto_management)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_predictions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revision =
         flag "revision" (optional string) ~doc:"STRING Revision"
       and acceptRule =
         flag "accept-rule" (optional json_arg) ~doc:"JSON AcceptRule"
       and acceptChoices =
         flag "accept-choices" (optional json_arg) ~doc:"JSON AcceptChoices"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING AssetIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_predictions
           (Values.AcceptPredictionsInput.make ?revision
              ?acceptRule:(Option.map ~f:Values.AcceptRule.of_json acceptRule)
              ?acceptChoices:(Option.map ~f:Values.AcceptChoices.of_json
                                acceptChoices) ?clientToken ~domainIdentifier
              ~identifier ()) (Some Values.AcceptPredictionsOutput.to_json)
           (Some Values.AcceptPredictionsOutput.error_to_json)])
let accept_subscription_request =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and decisionComment =
         flag "decision-comment" (optional string)
           ~doc:"STRING DecisionComment"
       and assetScopes =
         flag "asset-scopes" (optional json_arg)
           ~doc:"JSON AcceptedAssetScopes"
       and assetPermissions =
         flag "asset-permissions" (optional json_arg)
           ~doc:"JSON AssetPermissions"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING SubscriptionRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_subscription_request
           (Values.AcceptSubscriptionRequestInput.make ?decisionComment
              ?assetScopes:(Option.map ~f:Values.AcceptedAssetScopes.of_json
                              assetScopes)
              ?assetPermissions:(Option.map
                                   ~f:Values.AssetPermissions.of_json
                                   assetPermissions) ~domainIdentifier
              ~identifier ())
           (Some Values.AcceptSubscriptionRequestOutput.to_json)
           (Some Values.AcceptSubscriptionRequestOutput.error_to_json)])
let add_entity_owner =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityType =
         flag "entity-type" (required json_arg)
           ~doc:"JSON DataZoneEntityType"
       and entityIdentifier =
         flag "entity-identifier" (required string) ~doc:"STRING String"
       and owner =
         flag "owner" (required json_arg) ~doc:"JSON OwnerProperties" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_entity_owner
           (Values.AddEntityOwnerInput.make ?clientToken ~domainIdentifier
              ~entityType:(Values.DataZoneEntityType.of_json entityType)
              ~entityIdentifier ~owner:(Values.OwnerProperties.of_json owner)
              ()) (Some Values.AddEntityOwnerOutput.to_json)
           (Some Values.AddEntityOwnerOutput.error_to_json)])
let add_policy_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityType =
         flag "entity-type" (required json_arg) ~doc:"JSON TargetEntityType"
       and entityIdentifier =
         flag "entity-identifier" (required string) ~doc:"STRING String"
       and policyType =
         flag "policy-type" (required json_arg) ~doc:"JSON ManagedPolicyType"
       and principal =
         flag "principal" (required json_arg)
           ~doc:"JSON PolicyGrantPrincipal"
       and detail =
         flag "detail" (required json_arg) ~doc:"JSON PolicyGrantDetail" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_policy_grant
           (Values.AddPolicyGrantInput.make ?clientToken ~domainIdentifier
              ~entityType:(Values.TargetEntityType.of_json entityType)
              ~entityIdentifier
              ~policyType:(Values.ManagedPolicyType.of_json policyType)
              ~principal:(Values.PolicyGrantPrincipal.of_json principal)
              ~detail:(Values.PolicyGrantDetail.of_json detail) ())
           (Some Values.AddPolicyGrantOutput.to_json)
           (Some Values.AddPolicyGrantOutput.error_to_json)])
let associate_environment_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and environmentRoleArn =
         flag "environment-role-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_environment_role
           (Values.AssociateEnvironmentRoleInput.make ~domainIdentifier
              ~environmentIdentifier ~environmentRoleArn ())
           (Some Values.AssociateEnvironmentRoleOutput.to_json)
           (Some Values.AssociateEnvironmentRoleOutput.error_to_json)])
let associate_governed_terms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityIdentifier =
         flag "entity-identifier" (required string)
           ~doc:"STRING EntityIdentifier"
       and entityType =
         flag "entity-type" (required json_arg)
           ~doc:"JSON GovernedEntityType"
       and governedGlossaryTerms =
         flag "governed-glossary-terms" (required json_arg)
           ~doc:"JSON AssociateGovernedTermsInputGovernedGlossaryTermsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_governed_terms
           (Values.AssociateGovernedTermsInput.make ~domainIdentifier
              ~entityIdentifier
              ~entityType:(Values.GovernedEntityType.of_json entityType)
              ~governedGlossaryTerms:(Values.AssociateGovernedTermsInputGovernedGlossaryTermsList.of_json
                                        governedGlossaryTerms) ())
           (Some Values.AssociateGovernedTermsOutput.to_json)
           (Some Values.AssociateGovernedTermsOutput.error_to_json)])
let batch_get_attributes_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entityRevision =
         flag "entity-revision" (optional string) ~doc:"STRING Revision"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityType =
         flag "entity-type" (required json_arg)
           ~doc:"JSON AttributeEntityType"
       and entityIdentifier =
         flag "entity-identifier" (required string) ~doc:"STRING EntityId"
       and attributeIdentifiers =
         flag "attribute-identifiers" (required json_arg)
           ~doc:"JSON AttributesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_attributes_metadata
           (Values.BatchGetAttributesMetadataInput.make ?entityRevision
              ~domainIdentifier
              ~entityType:(Values.AttributeEntityType.of_json entityType)
              ~entityIdentifier
              ~attributeIdentifiers:(Values.AttributesList.of_json
                                       attributeIdentifiers) ())
           (Some Values.BatchGetAttributesMetadataOutput.to_json)
           (Some Values.BatchGetAttributesMetadataOutput.error_to_json)])
let batch_put_attributes_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityType =
         flag "entity-type" (required json_arg)
           ~doc:"JSON AttributeEntityType"
       and entityIdentifier =
         flag "entity-identifier" (required string) ~doc:"STRING EntityId"
       and attributes =
         flag "attributes" (required json_arg) ~doc:"JSON Attributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_put_attributes_metadata
           (Values.BatchPutAttributesMetadataInput.make ?clientToken
              ~domainIdentifier
              ~entityType:(Values.AttributeEntityType.of_json entityType)
              ~entityIdentifier
              ~attributes:(Values.Attributes.of_json attributes) ())
           (Some Values.BatchPutAttributesMetadataOutput.to_json)
           (Some Values.BatchPutAttributesMetadataOutput.error_to_json)])
let cancel_metadata_generation_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING MetadataGenerationRunIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_metadata_generation_run
           (Values.CancelMetadataGenerationRunInput.make ~domainIdentifier
              ~identifier ())
           (Some Values.CancelMetadataGenerationRunOutput.to_json)
           (Some Values.CancelMetadataGenerationRunOutput.error_to_json)])
let cancel_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING SubscriptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_subscription
           (Values.CancelSubscriptionInput.make ~domainIdentifier ~identifier
              ()) (Some Values.CancelSubscriptionOutput.to_json)
           (Some Values.CancelSubscriptionOutput.error_to_json)])
let create_account_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING AccountPoolName"
       and resolutionStrategy =
         flag "resolution-strategy" (required json_arg)
           ~doc:"JSON ResolutionStrategy"
       and accountSource =
         flag "account-source" (required json_arg) ~doc:"JSON AccountSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_account_pool
           (Values.CreateAccountPoolInput.make ?description ~domainIdentifier
              ~name
              ~resolutionStrategy:(Values.ResolutionStrategy.of_json
                                     resolutionStrategy)
              ~accountSource:(Values.AccountSource.of_json accountSource) ())
           (Some Values.CreateAccountPoolOutput.to_json)
           (Some Values.CreateAccountPoolOutput.error_to_json)])
let create_asset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and externalIdentifier =
         flag "external-identifier" (optional string)
           ~doc:"STRING ExternalIdentifier"
       and typeRevision =
         flag "type-revision" (optional string) ~doc:"STRING Revision"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and glossaryTerms =
         flag "glossary-terms" (optional json_arg) ~doc:"JSON GlossaryTerms"
       and formsInput =
         flag "forms-input" (optional json_arg) ~doc:"JSON FormInputList"
       and predictionConfiguration =
         flag "prediction-configuration" (optional json_arg)
           ~doc:"JSON PredictionConfiguration"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and name = flag "name" (required string) ~doc:"STRING AssetName"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and typeIdentifier =
         flag "type-identifier" (required string)
           ~doc:"STRING AssetTypeIdentifier"
       and owningProjectIdentifier =
         flag "owning-project-identifier" (required string)
           ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_asset
           (Values.CreateAssetInput.make ?externalIdentifier ?typeRevision
              ?description
              ?glossaryTerms:(Option.map ~f:Values.GlossaryTerms.of_json
                                glossaryTerms)
              ?formsInput:(Option.map ~f:Values.FormInputList.of_json
                             formsInput)
              ?predictionConfiguration:(Option.map
                                          ~f:Values.PredictionConfiguration.of_json
                                          predictionConfiguration)
              ?clientToken ~name ~domainIdentifier ~typeIdentifier
              ~owningProjectIdentifier ())
           (Some Values.CreateAssetOutput.to_json)
           (Some Values.CreateAssetOutput.error_to_json)])
let create_asset_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and assetIdentifier =
         flag "asset-identifier" (required string) ~doc:"STRING AssetId"
       and name = flag "name" (required string) ~doc:"STRING FilterName"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON AssetFilterConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_asset_filter
           (Values.CreateAssetFilterInput.make ?description ?clientToken
              ~domainIdentifier ~assetIdentifier ~name
              ~configuration:(Values.AssetFilterConfiguration.of_json
                                configuration) ())
           (Some Values.CreateAssetFilterOutput.to_json)
           (Some Values.CreateAssetFilterOutput.error_to_json)])
let create_asset_revision =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and typeRevision =
         flag "type-revision" (optional string) ~doc:"STRING Revision"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and glossaryTerms =
         flag "glossary-terms" (optional json_arg) ~doc:"JSON GlossaryTerms"
       and formsInput =
         flag "forms-input" (optional json_arg) ~doc:"JSON FormInputList"
       and predictionConfiguration =
         flag "prediction-configuration" (optional json_arg)
           ~doc:"JSON PredictionConfiguration"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and name = flag "name" (required string) ~doc:"STRING AssetName"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING AssetIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_asset_revision
           (Values.CreateAssetRevisionInput.make ?typeRevision ?description
              ?glossaryTerms:(Option.map ~f:Values.GlossaryTerms.of_json
                                glossaryTerms)
              ?formsInput:(Option.map ~f:Values.FormInputList.of_json
                             formsInput)
              ?predictionConfiguration:(Option.map
                                          ~f:Values.PredictionConfiguration.of_json
                                          predictionConfiguration)
              ?clientToken ~name ~domainIdentifier ~identifier ())
           (Some Values.CreateAssetRevisionOutput.to_json)
           (Some Values.CreateAssetRevisionOutput.error_to_json)])
let create_asset_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING TypeName"
       and formsInput =
         flag "forms-input" (required json_arg) ~doc:"JSON FormsInputMap"
       and owningProjectIdentifier =
         flag "owning-project-identifier" (required string)
           ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_asset_type
           (Values.CreateAssetTypeInput.make ?description ~domainIdentifier
              ~name ~formsInput:(Values.FormsInputMap.of_json formsInput)
              ~owningProjectIdentifier ())
           (Some Values.CreateAssetTypeOutput.to_json)
           (Some Values.CreateAssetTypeOutput.error_to_json)])
let create_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsLocation =
         flag "aws-location" (optional json_arg) ~doc:"JSON AwsLocation"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and configurations =
         flag "configurations" (optional json_arg) ~doc:"JSON Configurations"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CreateConnectionInputDescriptionString"
       and environmentIdentifier =
         flag "environment-identifier" (optional string)
           ~doc:"STRING EnvironmentId"
       and props =
         flag "props" (optional json_arg)
           ~doc:"JSON ConnectionPropertiesInput"
       and enableTrustedIdentityPropagation =
         flag "enable-trusted-identity-propagation" (optional bool)
           ~doc:"BOOL Boolean"
       and scope =
         flag "scope" (optional json_arg) ~doc:"JSON ConnectionScope"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING ConnectionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connection
           (Values.CreateConnectionInput.make
              ?awsLocation:(Option.map ~f:Values.AwsLocation.of_json
                              awsLocation) ?clientToken
              ?configurations:(Option.map ~f:Values.Configurations.of_json
                                 configurations) ?description
              ?environmentIdentifier
              ?props:(Option.map ~f:Values.ConnectionPropertiesInput.of_json
                        props) ?enableTrustedIdentityPropagation
              ?scope:(Option.map ~f:Values.ConnectionScope.of_json scope)
              ~domainIdentifier ~name ())
           (Some Values.CreateConnectionOutput.to_json)
           (Some Values.CreateConnectionOutput.error_to_json)])
let create_data_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING DataProductDescription"
       and glossaryTerms =
         flag "glossary-terms" (optional json_arg) ~doc:"JSON GlossaryTerms"
       and formsInput =
         flag "forms-input" (optional json_arg) ~doc:"JSON FormInputList"
       and items =
         flag "items" (optional json_arg) ~doc:"JSON DataProductItems"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING DataProductName"
       and owningProjectIdentifier =
         flag "owning-project-identifier" (required string)
           ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_product
           (Values.CreateDataProductInput.make ?description
              ?glossaryTerms:(Option.map ~f:Values.GlossaryTerms.of_json
                                glossaryTerms)
              ?formsInput:(Option.map ~f:Values.FormInputList.of_json
                             formsInput)
              ?items:(Option.map ~f:Values.DataProductItems.of_json items)
              ?clientToken ~domainIdentifier ~name ~owningProjectIdentifier
              ()) (Some Values.CreateDataProductOutput.to_json)
           (Some Values.CreateDataProductOutput.error_to_json)])
let create_data_product_revision =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING DataProductDescription"
       and glossaryTerms =
         flag "glossary-terms" (optional json_arg) ~doc:"JSON GlossaryTerms"
       and items =
         flag "items" (optional json_arg) ~doc:"JSON DataProductItems"
       and formsInput =
         flag "forms-input" (optional json_arg) ~doc:"JSON FormInputList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DataProductId"
       and name = flag "name" (required string) ~doc:"STRING DataProductName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_product_revision
           (Values.CreateDataProductRevisionInput.make ?description
              ?glossaryTerms:(Option.map ~f:Values.GlossaryTerms.of_json
                                glossaryTerms)
              ?items:(Option.map ~f:Values.DataProductItems.of_json items)
              ?formsInput:(Option.map ~f:Values.FormInputList.of_json
                             formsInput) ?clientToken ~domainIdentifier
              ~identifier ~name ())
           (Some Values.CreateDataProductRevisionOutput.to_json)
           (Some Values.CreateDataProductRevisionOutput.error_to_json)])
let create_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and environmentIdentifier =
         flag "environment-identifier" (optional string)
           ~doc:"STRING CreateDataSourceInputEnvironmentIdentifierString"
       and connectionIdentifier =
         flag "connection-identifier" (optional string)
           ~doc:"STRING CreateDataSourceInputConnectionIdentifierString"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON DataSourceConfigurationInput"
       and recommendation =
         flag "recommendation" (optional json_arg)
           ~doc:"JSON RecommendationConfiguration"
       and enableSetting =
         flag "enable-setting" (optional json_arg) ~doc:"JSON EnableSetting"
       and schedule =
         flag "schedule" (optional json_arg)
           ~doc:"JSON ScheduleConfiguration"
       and publishOnImport =
         flag "publish-on-import" (optional bool) ~doc:"BOOL Boolean"
       and assetFormsInput =
         flag "asset-forms-input" (optional json_arg)
           ~doc:"JSON FormInputList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and projectIdentifier =
         flag "project-identifier" (required string)
           ~doc:"STRING CreateDataSourceInputProjectIdentifierString"
       and type_ =
         flag "type-" (required string) ~doc:"STRING DataSourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_source
           (Values.CreateDataSourceInput.make ?description
              ?environmentIdentifier ?connectionIdentifier
              ?configuration:(Option.map
                                ~f:Values.DataSourceConfigurationInput.of_json
                                configuration)
              ?recommendation:(Option.map
                                 ~f:Values.RecommendationConfiguration.of_json
                                 recommendation)
              ?enableSetting:(Option.map ~f:Values.EnableSetting.of_json
                                enableSetting)
              ?schedule:(Option.map ~f:Values.ScheduleConfiguration.of_json
                           schedule) ?publishOnImport
              ?assetFormsInput:(Option.map ~f:Values.FormInputList.of_json
                                  assetFormsInput) ?clientToken ~name
              ~domainIdentifier ~projectIdentifier ~type_ ())
           (Some Values.CreateDataSourceOutput.to_json)
           (Some Values.CreateDataSourceOutput.error_to_json)])
let create_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and singleSignOn =
         flag "single-sign-on" (optional json_arg) ~doc:"JSON SingleSignOn"
       and domainExecutionRole =
         flag "domain-execution-role" (optional string) ~doc:"STRING RoleArn"
       and kmsKeyIdentifier =
         flag "kms-key-identifier" (optional string) ~doc:"STRING KmsKeyArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and domainVersion =
         flag "domain-version" (optional json_arg) ~doc:"JSON DomainVersion"
       and serviceRole =
         flag "service-role" (optional string) ~doc:"STRING RoleArn"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain
           (Values.CreateDomainInput.make ?description
              ?singleSignOn:(Option.map ~f:Values.SingleSignOn.of_json
                               singleSignOn) ?domainExecutionRole
              ?kmsKeyIdentifier
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?domainVersion:(Option.map ~f:Values.DomainVersion.of_json
                                domainVersion) ?serviceRole ?clientToken
              ~name ()) (Some Values.CreateDomainOutput.to_json)
           (Some Values.CreateDomainOutput.error_to_json)])
let create_domain_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING DomainUnitDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING DomainUnitName"
       and parentDomainUnitIdentifier =
         flag "parent-domain-unit-identifier" (required string)
           ~doc:"STRING DomainUnitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain_unit
           (Values.CreateDomainUnitInput.make ?description ?clientToken
              ~domainIdentifier ~name ~parentDomainUnitIdentifier ())
           (Some Values.CreateDomainUnitOutput.to_json)
           (Some Values.CreateDomainUnitOutput.error_to_json)])
let create_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and environmentProfileIdentifier =
         flag "environment-profile-identifier" (optional string)
           ~doc:"STRING EnvironmentProfileId"
       and userParameters =
         flag "user-parameters" (optional json_arg)
           ~doc:"JSON EnvironmentParametersList"
       and glossaryTerms =
         flag "glossary-terms" (optional json_arg) ~doc:"JSON GlossaryTerms"
       and environmentAccountIdentifier =
         flag "environment-account-identifier" (optional string)
           ~doc:"STRING String"
       and environmentAccountRegion =
         flag "environment-account-region" (optional string)
           ~doc:"STRING String"
       and environmentBlueprintIdentifier =
         flag "environment-blueprint-identifier" (optional string)
           ~doc:"STRING String"
       and deploymentOrder =
         flag "deployment-order" (optional int) ~doc:"INT Integer"
       and environmentConfigurationId =
         flag "environment-configuration-id" (optional string)
           ~doc:"STRING String"
       and environmentConfigurationName =
         flag "environment-configuration-name" (optional string)
           ~doc:"STRING EnvironmentConfigurationName"
       and projectIdentifier =
         flag "project-identifier" (required string) ~doc:"STRING ProjectId"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment
           (Values.CreateEnvironmentInput.make ?description
              ?environmentProfileIdentifier
              ?userParameters:(Option.map
                                 ~f:Values.EnvironmentParametersList.of_json
                                 userParameters)
              ?glossaryTerms:(Option.map ~f:Values.GlossaryTerms.of_json
                                glossaryTerms) ?environmentAccountIdentifier
              ?environmentAccountRegion ?environmentBlueprintIdentifier
              ?deploymentOrder ?environmentConfigurationId
              ?environmentConfigurationName ~projectIdentifier
              ~domainIdentifier ~name ())
           (Some Values.CreateEnvironmentOutput.to_json)
           (Some Values.CreateEnvironmentOutput.error_to_json)])
let create_environment_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and name = flag "name" (required string) ~doc:"STRING String"
       and parameters =
         flag "parameters" (required json_arg) ~doc:"JSON ActionParameters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment_action
           (Values.CreateEnvironmentActionInput.make ?description
              ~domainIdentifier ~environmentIdentifier ~name
              ~parameters:(Values.ActionParameters.of_json parameters) ())
           (Some Values.CreateEnvironmentActionOutput.to_json)
           (Some Values.CreateEnvironmentActionOutput.error_to_json)])
let create_environment_blueprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and userParameters =
         flag "user-parameters" (optional json_arg)
           ~doc:"JSON CustomParameterList"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and name =
         flag "name" (required string) ~doc:"STRING EnvironmentBlueprintName"
       and provisioningProperties =
         flag "provisioning-properties" (required json_arg)
           ~doc:"JSON ProvisioningProperties" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment_blueprint
           (Values.CreateEnvironmentBlueprintInput.make ?description
              ?userParameters:(Option.map
                                 ~f:Values.CustomParameterList.of_json
                                 userParameters) ~domainIdentifier ~name
              ~provisioningProperties:(Values.ProvisioningProperties.of_json
                                         provisioningProperties) ())
           (Some Values.CreateEnvironmentBlueprintOutput.to_json)
           (Some Values.CreateEnvironmentBlueprintOutput.error_to_json)])
let create_environment_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and userParameters =
         flag "user-parameters" (optional json_arg)
           ~doc:"JSON EnvironmentParametersList"
       and awsAccountId =
         flag "aws-account-id" (optional string) ~doc:"STRING AwsAccountId"
       and awsAccountRegion =
         flag "aws-account-region" (optional string) ~doc:"STRING AwsRegion"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and name =
         flag "name" (required string) ~doc:"STRING EnvironmentProfileName"
       and environmentBlueprintIdentifier =
         flag "environment-blueprint-identifier" (required string)
           ~doc:"STRING EnvironmentBlueprintId"
       and projectIdentifier =
         flag "project-identifier" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment_profile
           (Values.CreateEnvironmentProfileInput.make ?description
              ?userParameters:(Option.map
                                 ~f:Values.EnvironmentParametersList.of_json
                                 userParameters) ?awsAccountId
              ?awsAccountRegion ~domainIdentifier ~name
              ~environmentBlueprintIdentifier ~projectIdentifier ())
           (Some Values.CreateEnvironmentProfileOutput.to_json)
           (Some Values.CreateEnvironmentProfileOutput.error_to_json)])
let create_form_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON FormTypeStatus"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING FormTypeName"
       and model = flag "model" (required json_arg) ~doc:"JSON Model"
       and owningProjectIdentifier =
         flag "owning-project-identifier" (required string)
           ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_form_type
           (Values.CreateFormTypeInput.make
              ?status:(Option.map ~f:Values.FormTypeStatus.of_json status)
              ?description ~domainIdentifier ~name
              ~model:(Values.Model.of_json model) ~owningProjectIdentifier ())
           (Some Values.CreateFormTypeOutput.to_json)
           (Some Values.CreateFormTypeOutput.error_to_json)])
let create_glossary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING GlossaryDescription"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON GlossaryStatus"
       and usageRestrictions =
         flag "usage-restrictions" (optional json_arg)
           ~doc:"JSON GlossaryUsageRestrictions"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING GlossaryName"
       and owningProjectIdentifier =
         flag "owning-project-identifier" (required string)
           ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_glossary
           (Values.CreateGlossaryInput.make ?description
              ?status:(Option.map ~f:Values.GlossaryStatus.of_json status)
              ?usageRestrictions:(Option.map
                                    ~f:Values.GlossaryUsageRestrictions.of_json
                                    usageRestrictions) ?clientToken
              ~domainIdentifier ~name ~owningProjectIdentifier ())
           (Some Values.CreateGlossaryOutput.to_json)
           (Some Values.CreateGlossaryOutput.error_to_json)])
let create_glossary_term =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON GlossaryTermStatus"
       and shortDescription =
         flag "short-description" (optional string)
           ~doc:"STRING ShortDescription"
       and longDescription =
         flag "long-description" (optional string)
           ~doc:"STRING LongDescription"
       and termRelations =
         flag "term-relations" (optional json_arg) ~doc:"JSON TermRelations"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and glossaryIdentifier =
         flag "glossary-identifier" (required string)
           ~doc:"STRING GlossaryTermId"
       and name =
         flag "name" (required string) ~doc:"STRING GlossaryTermName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_glossary_term
           (Values.CreateGlossaryTermInput.make
              ?status:(Option.map ~f:Values.GlossaryTermStatus.of_json status)
              ?shortDescription ?longDescription
              ?termRelations:(Option.map ~f:Values.TermRelations.of_json
                                termRelations) ?clientToken ~domainIdentifier
              ~glossaryIdentifier ~name ())
           (Some Values.CreateGlossaryTermOutput.to_json)
           (Some Values.CreateGlossaryTermOutput.error_to_json)])
let create_group_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupIdentifier =
         flag "group-identifier" (optional string)
           ~doc:"STRING GroupIdentifier"
       and rolePrincipalArn =
         flag "role-principal-arn" (optional string) ~doc:"STRING String"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_group_profile
           (Values.CreateGroupProfileInput.make ?groupIdentifier
              ?rolePrincipalArn ?clientToken ~domainIdentifier ())
           (Some Values.CreateGroupProfileOutput.to_json)
           (Some Values.CreateGroupProfileOutput.error_to_json)])
let create_listing_change_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entityRevision =
         flag "entity-revision" (optional string) ~doc:"STRING Revision"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityIdentifier =
         flag "entity-identifier" (required string)
           ~doc:"STRING EntityIdentifier"
       and entityType =
         flag "entity-type" (required json_arg) ~doc:"JSON EntityType"
       and action =
         flag "action" (required json_arg) ~doc:"JSON ChangeAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_listing_change_set
           (Values.CreateListingChangeSetInput.make ?entityRevision
              ?clientToken ~domainIdentifier ~entityIdentifier
              ~entityType:(Values.EntityType.of_json entityType)
              ~action:(Values.ChangeAction.of_json action) ())
           (Some Values.CreateListingChangeSetOutput.to_json)
           (Some Values.CreateListingChangeSetOutput.error_to_json)])
let create_notebook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON Metadata"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and owningProjectIdentifier =
         flag "owning-project-identifier" (required string)
           ~doc:"STRING ProjectId"
       and name = flag "name" (required string) ~doc:"STRING NotebookName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_notebook
           (Values.CreateNotebookInput.make ?description
              ?metadata:(Option.map ~f:Values.Metadata.of_json metadata)
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?clientToken ~domainIdentifier ~owningProjectIdentifier ~name
              ()) (Some Values.CreateNotebookOutput.to_json)
           (Some Values.CreateNotebookOutput.error_to_json)])
let create_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and resourceTags =
         flag "resource-tags" (optional json_arg)
           ~doc:"JSON CreateProjectInputResourceTagsMap"
       and glossaryTerms =
         flag "glossary-terms" (optional json_arg) ~doc:"JSON GlossaryTerms"
       and domainUnitId =
         flag "domain-unit-id" (optional string) ~doc:"STRING DomainUnitId"
       and projectProfileId =
         flag "project-profile-id" (optional string)
           ~doc:"STRING ProjectProfileId"
       and userParameters =
         flag "user-parameters" (optional json_arg)
           ~doc:"JSON EnvironmentConfigurationUserParametersList"
       and projectCategory =
         flag "project-category" (optional string) ~doc:"STRING String"
       and projectExecutionRole =
         flag "project-execution-role" (optional string)
           ~doc:"STRING RoleArn"
       and membershipAssignments =
         flag "membership-assignments" (optional json_arg)
           ~doc:"JSON ProjectMembershipAssignments"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_project
           (Values.CreateProjectInput.make ?description
              ?resourceTags:(Option.map
                               ~f:Values.CreateProjectInputResourceTagsMap.of_json
                               resourceTags)
              ?glossaryTerms:(Option.map ~f:Values.GlossaryTerms.of_json
                                glossaryTerms) ?domainUnitId
              ?projectProfileId
              ?userParameters:(Option.map
                                 ~f:Values.EnvironmentConfigurationUserParametersList.of_json
                                 userParameters) ?projectCategory
              ?projectExecutionRole
              ?membershipAssignments:(Option.map
                                        ~f:Values.ProjectMembershipAssignments.of_json
                                        membershipAssignments)
              ~domainIdentifier ~name ())
           (Some Values.CreateProjectOutput.to_json)
           (Some Values.CreateProjectOutput.error_to_json)])
let create_project_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and projectIdentifier =
         flag "project-identifier" (required string) ~doc:"STRING ProjectId"
       and member = flag "member" (required json_arg) ~doc:"JSON Member"
       and designation =
         flag "designation" (required json_arg) ~doc:"JSON UserDesignation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_project_membership
           (Values.CreateProjectMembershipInput.make ~domainIdentifier
              ~projectIdentifier ~member:(Values.Member.of_json member)
              ~designation:(Values.UserDesignation.of_json designation) ())
           (Some Values.CreateProjectMembershipOutput.to_json)
           (Some Values.CreateProjectMembershipOutput.error_to_json)])
let create_project_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and status = flag "status" (optional json_arg) ~doc:"JSON Status"
       and projectResourceTags =
         flag "project-resource-tags" (optional json_arg)
           ~doc:"JSON ProjectResourceTagParameters"
       and allowCustomProjectResourceTags =
         flag "allow-custom-project-resource-tags" (optional bool)
           ~doc:"BOOL Boolean"
       and projectResourceTagsDescription =
         flag "project-resource-tags-description" (optional string)
           ~doc:"STRING Description"
       and environmentConfigurations =
         flag "environment-configurations" (optional json_arg)
           ~doc:"JSON EnvironmentConfigurationsList"
       and domainUnitIdentifier =
         flag "domain-unit-identifier" (optional string)
           ~doc:"STRING DomainUnitId"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and name =
         flag "name" (required string) ~doc:"STRING ProjectProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_project_profile
           (Values.CreateProjectProfileInput.make ?description
              ?status:(Option.map ~f:Values.Status.of_json status)
              ?projectResourceTags:(Option.map
                                      ~f:Values.ProjectResourceTagParameters.of_json
                                      projectResourceTags)
              ?allowCustomProjectResourceTags ?projectResourceTagsDescription
              ?environmentConfigurations:(Option.map
                                            ~f:Values.EnvironmentConfigurationsList.of_json
                                            environmentConfigurations)
              ?domainUnitIdentifier ~domainIdentifier ~name ())
           (Some Values.CreateProjectProfileOutput.to_json)
           (Some Values.CreateProjectProfileOutput.error_to_json)])
let create_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and name = flag "name" (required string) ~doc:"STRING RuleName"
       and target = flag "target" (required json_arg) ~doc:"JSON RuleTarget"
       and action = flag "action" (required json_arg) ~doc:"JSON RuleAction"
       and scope = flag "scope" (required json_arg) ~doc:"JSON RuleScope"
       and detail = flag "detail" (required json_arg) ~doc:"JSON RuleDetail" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_rule
           (Values.CreateRuleInput.make ?description ?clientToken
              ~domainIdentifier ~name
              ~target:(Values.RuleTarget.of_json target)
              ~action:(Values.RuleAction.of_json action)
              ~scope:(Values.RuleScope.of_json scope)
              ~detail:(Values.RuleDetail.of_json detail) ())
           (Some Values.CreateRuleOutput.to_json)
           (Some Values.CreateRuleOutput.error_to_json)])
let create_subscription_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionTargetIdentifier =
         flag "subscription-target-identifier" (optional string)
           ~doc:"STRING SubscriptionTargetId"
       and assetTargetNames =
         flag "asset-target-names" (optional json_arg)
           ~doc:"JSON AssetTargetNames"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and grantedEntity =
         flag "granted-entity" (required json_arg)
           ~doc:"JSON GrantedEntityInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subscription_grant
           (Values.CreateSubscriptionGrantInput.make
              ?subscriptionTargetIdentifier
              ?assetTargetNames:(Option.map
                                   ~f:Values.AssetTargetNames.of_json
                                   assetTargetNames) ?clientToken
              ~domainIdentifier ~environmentIdentifier
              ~grantedEntity:(Values.GrantedEntityInput.of_json grantedEntity)
              ()) (Some Values.CreateSubscriptionGrantOutput.to_json)
           (Some Values.CreateSubscriptionGrantOutput.error_to_json)])
let create_subscription_request =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and metadataForms =
         flag "metadata-forms" (optional json_arg)
           ~doc:"JSON MetadataFormInputs"
       and assetPermissions =
         flag "asset-permissions" (optional json_arg)
           ~doc:"JSON AssetPermissions"
       and assetScopes =
         flag "asset-scopes" (optional json_arg)
           ~doc:"JSON AcceptedAssetScopes"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and subscribedPrincipals =
         flag "subscribed-principals" (required json_arg)
           ~doc:"JSON SubscribedPrincipalInputs"
       and subscribedListings =
         flag "subscribed-listings" (required json_arg)
           ~doc:"JSON SubscribedListingInputs"
       and requestReason =
         flag "request-reason" (required string) ~doc:"STRING RequestReason" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subscription_request
           (Values.CreateSubscriptionRequestInput.make ?clientToken
              ?metadataForms:(Option.map ~f:Values.MetadataFormInputs.of_json
                                metadataForms)
              ?assetPermissions:(Option.map
                                   ~f:Values.AssetPermissions.of_json
                                   assetPermissions)
              ?assetScopes:(Option.map ~f:Values.AcceptedAssetScopes.of_json
                              assetScopes) ~domainIdentifier
              ~subscribedPrincipals:(Values.SubscribedPrincipalInputs.of_json
                                       subscribedPrincipals)
              ~subscribedListings:(Values.SubscribedListingInputs.of_json
                                     subscribedListings) ~requestReason ())
           (Some Values.CreateSubscriptionRequestOutput.to_json)
           (Some Values.CreateSubscriptionRequestOutput.error_to_json)])
let create_subscription_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and provider = flag "provider" (optional string) ~doc:"STRING String"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and subscriptionGrantCreationMode =
         flag "subscription-grant-creation-mode" (optional json_arg)
           ~doc:"JSON SubscriptionGrantCreationMode"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and name =
         flag "name" (required string) ~doc:"STRING SubscriptionTargetName"
       and type_ = flag "type-" (required string) ~doc:"STRING String"
       and subscriptionTargetConfig =
         flag "subscription-target-config" (required json_arg)
           ~doc:"JSON SubscriptionTargetForms"
       and authorizedPrincipals =
         flag "authorized-principals" (required json_arg)
           ~doc:"JSON AuthorizedPrincipalIdentifiers"
       and manageAccessRole =
         flag "manage-access-role" (required string) ~doc:"STRING IamRoleArn"
       and applicableAssetTypes =
         flag "applicable-asset-types" (required json_arg)
           ~doc:"JSON ApplicableAssetTypes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subscription_target
           (Values.CreateSubscriptionTargetInput.make ?provider ?clientToken
              ?subscriptionGrantCreationMode:(Option.map
                                                ~f:Values.SubscriptionGrantCreationMode.of_json
                                                subscriptionGrantCreationMode)
              ~domainIdentifier ~environmentIdentifier ~name ~type_
              ~subscriptionTargetConfig:(Values.SubscriptionTargetForms.of_json
                                           subscriptionTargetConfig)
              ~authorizedPrincipals:(Values.AuthorizedPrincipalIdentifiers.of_json
                                       authorizedPrincipals)
              ~manageAccessRole
              ~applicableAssetTypes:(Values.ApplicableAssetTypes.of_json
                                       applicableAssetTypes) ())
           (Some Values.CreateSubscriptionTargetOutput.to_json)
           (Some Values.CreateSubscriptionTargetOutput.error_to_json)])
let create_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userType =
         flag "user-type" (optional json_arg) ~doc:"JSON UserType"
       and sessionName =
         flag "session-name" (optional string)
           ~doc:"STRING CreateUserProfileInputSessionNameString"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and userIdentifier =
         flag "user-identifier" (required string)
           ~doc:"STRING UserIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user_profile
           (Values.CreateUserProfileInput.make
              ?userType:(Option.map ~f:Values.UserType.of_json userType)
              ?sessionName ?clientToken ~domainIdentifier ~userIdentifier ())
           (Some Values.CreateUserProfileOutput.to_json)
           (Some Values.CreateUserProfileOutput.error_to_json)])
let delete_account_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING AccountPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_account_pool
           (Values.DeleteAccountPoolInput.make ~domainIdentifier ~identifier
              ()) (Some Values.DeleteAccountPoolOutput.to_json)
           (Some Values.DeleteAccountPoolOutput.error_to_json)])
let delete_asset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING AssetIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_asset
           (Values.DeleteAssetInput.make ~domainIdentifier ~identifier ())
           (Some Values.DeleteAssetOutput.to_json)
           (Some Values.DeleteAssetOutput.error_to_json)])
let delete_asset_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and assetIdentifier =
         flag "asset-identifier" (required string) ~doc:"STRING AssetId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING FilterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_asset_filter
           (Values.DeleteAssetFilterInput.make ~domainIdentifier
              ~assetIdentifier ~identifier ()) None None])
let delete_asset_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING AssetTypeIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_asset_type
           (Values.DeleteAssetTypeInput.make ~domainIdentifier ~identifier ())
           (Some Values.DeleteAssetTypeOutput.to_json)
           (Some Values.DeleteAssetTypeOutput.error_to_json)])
let delete_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connection
           (Values.DeleteConnectionInput.make ~domainIdentifier ~identifier
              ()) (Some Values.DeleteConnectionOutput.to_json)
           (Some Values.DeleteConnectionOutput.error_to_json)])
let delete_data_export_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_export_configuration
           (Values.DeleteDataExportConfigurationInput.make ~domainIdentifier
              ()) (Some Values.DeleteDataExportConfigurationOutput.to_json)
           (Some Values.DeleteDataExportConfigurationOutput.error_to_json)])
let delete_data_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DataProductId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_product
           (Values.DeleteDataProductInput.make ~domainIdentifier ~identifier
              ()) (Some Values.DeleteDataProductOutput.to_json)
           (Some Values.DeleteDataProductOutput.error_to_json)])
let delete_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and retainPermissionsOnRevokeFailure =
         flag "retain-permissions-on-revoke-failure" (optional bool)
           ~doc:"BOOL Boolean"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DataSourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_source
           (Values.DeleteDataSourceInput.make ?clientToken
              ?retainPermissionsOnRevokeFailure ~domainIdentifier ~identifier
              ()) (Some Values.DeleteDataSourceOutput.to_json)
           (Some Values.DeleteDataSourceOutput.error_to_json)])
let delete_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and skipDeletionCheck =
         flag "skip-deletion-check" (optional bool) ~doc:"BOOL Boolean"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain
           (Values.DeleteDomainInput.make ?clientToken ?skipDeletionCheck
              ~identifier ()) (Some Values.DeleteDomainOutput.to_json)
           (Some Values.DeleteDomainOutput.error_to_json)])
let delete_domain_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DomainUnitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain_unit
           (Values.DeleteDomainUnitInput.make ~domainIdentifier ~identifier
              ()) (Some Values.DeleteDomainUnitOutput.to_json)
           (Some Values.DeleteDomainUnitOutput.error_to_json)])
let delete_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment
           (Values.DeleteEnvironmentInput.make ~domainIdentifier ~identifier
              ()) None None])
let delete_environment_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment_action
           (Values.DeleteEnvironmentActionInput.make ~domainIdentifier
              ~environmentIdentifier ~identifier ()) None None])
let delete_environment_blueprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING EnvironmentBlueprintId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment_blueprint
           (Values.DeleteEnvironmentBlueprintInput.make ~domainIdentifier
              ~identifier ()) None None])
let delete_environment_blueprint_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentBlueprintIdentifier =
         flag "environment-blueprint-identifier" (required string)
           ~doc:"STRING EnvironmentBlueprintId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment_blueprint_configuration
           (Values.DeleteEnvironmentBlueprintConfigurationInput.make
              ~domainIdentifier ~environmentBlueprintIdentifier ())
           (Some Values.DeleteEnvironmentBlueprintConfigurationOutput.to_json)
           (Some
              Values.DeleteEnvironmentBlueprintConfigurationOutput.error_to_json)])
let delete_environment_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING EnvironmentProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment_profile
           (Values.DeleteEnvironmentProfileInput.make ~domainIdentifier
              ~identifier ()) None None])
let delete_form_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and formTypeIdentifier =
         flag "form-type-identifier" (required string)
           ~doc:"STRING FormTypeIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_form_type
           (Values.DeleteFormTypeInput.make ~domainIdentifier
              ~formTypeIdentifier ())
           (Some Values.DeleteFormTypeOutput.to_json)
           (Some Values.DeleteFormTypeOutput.error_to_json)])
let delete_glossary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING GlossaryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_glossary
           (Values.DeleteGlossaryInput.make ~domainIdentifier ~identifier ())
           (Some Values.DeleteGlossaryOutput.to_json)
           (Some Values.DeleteGlossaryOutput.error_to_json)])
let delete_glossary_term =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING GlossaryTermId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_glossary_term
           (Values.DeleteGlossaryTermInput.make ~domainIdentifier ~identifier
              ()) (Some Values.DeleteGlossaryTermOutput.to_json)
           (Some Values.DeleteGlossaryTermOutput.error_to_json)])
let delete_listing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ListingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_listing
           (Values.DeleteListingInput.make ~domainIdentifier ~identifier ())
           (Some Values.DeleteListingOutput.to_json)
           (Some Values.DeleteListingOutput.error_to_json)])
let delete_notebook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING NotebookId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_notebook
           (Values.DeleteNotebookInput.make ~domainIdentifier ~identifier ())
           (Some Values.DeleteNotebookOutput.to_json)
           (Some Values.DeleteNotebookOutput.error_to_json)])
let delete_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipDeletionCheck =
         flag "skip-deletion-check" (optional bool) ~doc:"BOOL Boolean"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_project
           (Values.DeleteProjectInput.make ?skipDeletionCheck
              ~domainIdentifier ~identifier ())
           (Some Values.DeleteProjectOutput.to_json)
           (Some Values.DeleteProjectOutput.error_to_json)])
let delete_project_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and projectIdentifier =
         flag "project-identifier" (required string) ~doc:"STRING ProjectId"
       and member = flag "member" (required json_arg) ~doc:"JSON Member" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_project_membership
           (Values.DeleteProjectMembershipInput.make ~domainIdentifier
              ~projectIdentifier ~member:(Values.Member.of_json member) ())
           (Some Values.DeleteProjectMembershipOutput.to_json)
           (Some Values.DeleteProjectMembershipOutput.error_to_json)])
let delete_project_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ProjectProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_project_profile
           (Values.DeleteProjectProfileInput.make ~domainIdentifier
              ~identifier ())
           (Some Values.DeleteProjectProfileOutput.to_json)
           (Some Values.DeleteProjectProfileOutput.error_to_json)])
let delete_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING RuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_rule
           (Values.DeleteRuleInput.make ~domainIdentifier ~identifier ())
           (Some Values.DeleteRuleOutput.to_json)
           (Some Values.DeleteRuleOutput.error_to_json)])
let delete_subscription_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING SubscriptionGrantId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_subscription_grant
           (Values.DeleteSubscriptionGrantInput.make ~domainIdentifier
              ~identifier ())
           (Some Values.DeleteSubscriptionGrantOutput.to_json)
           (Some Values.DeleteSubscriptionGrantOutput.error_to_json)])
let delete_subscription_request =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING SubscriptionRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_subscription_request
           (Values.DeleteSubscriptionRequestInput.make ~domainIdentifier
              ~identifier ()) None None])
let delete_subscription_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING SubscriptionTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_subscription_target
           (Values.DeleteSubscriptionTargetInput.make ~domainIdentifier
              ~environmentIdentifier ~identifier ()) None None])
let delete_time_series_data_points =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityIdentifier =
         flag "entity-identifier" (required string)
           ~doc:"STRING EntityIdentifier"
       and entityType =
         flag "entity-type" (required json_arg)
           ~doc:"JSON TimeSeriesEntityType"
       and formName =
         flag "form-name" (required string) ~doc:"STRING TimeSeriesFormName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_time_series_data_points
           (Values.DeleteTimeSeriesDataPointsInput.make ?clientToken
              ~domainIdentifier ~entityIdentifier
              ~entityType:(Values.TimeSeriesEntityType.of_json entityType)
              ~formName ())
           (Some Values.DeleteTimeSeriesDataPointsOutput.to_json)
           (Some Values.DeleteTimeSeriesDataPointsOutput.error_to_json)])
let disassociate_environment_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and environmentRoleArn =
         flag "environment-role-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_environment_role
           (Values.DisassociateEnvironmentRoleInput.make ~domainIdentifier
              ~environmentIdentifier ~environmentRoleArn ())
           (Some Values.DisassociateEnvironmentRoleOutput.to_json)
           (Some Values.DisassociateEnvironmentRoleOutput.error_to_json)])
let disassociate_governed_terms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityIdentifier =
         flag "entity-identifier" (required string)
           ~doc:"STRING EntityIdentifier"
       and entityType =
         flag "entity-type" (required json_arg)
           ~doc:"JSON GovernedEntityType"
       and governedGlossaryTerms =
         flag "governed-glossary-terms" (required json_arg)
           ~doc:"JSON DisassociateGovernedTermsInputGovernedGlossaryTermsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_governed_terms
           (Values.DisassociateGovernedTermsInput.make ~domainIdentifier
              ~entityIdentifier
              ~entityType:(Values.GovernedEntityType.of_json entityType)
              ~governedGlossaryTerms:(Values.DisassociateGovernedTermsInputGovernedGlossaryTermsList.of_json
                                        governedGlossaryTerms) ())
           (Some Values.DisassociateGovernedTermsOutput.to_json)
           (Some Values.DisassociateGovernedTermsOutput.error_to_json)])
let get_account_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING AccountPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_pool
           (Values.GetAccountPoolInput.make ~domainIdentifier ~identifier ())
           (Some Values.GetAccountPoolOutput.to_json)
           (Some Values.GetAccountPoolOutput.error_to_json)])
let get_asset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revision =
         flag "revision" (optional string) ~doc:"STRING Revision"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING AssetIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_asset
           (Values.GetAssetInput.make ?revision ~domainIdentifier ~identifier
              ()) (Some Values.GetAssetOutput.to_json)
           (Some Values.GetAssetOutput.error_to_json)])
let get_asset_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and assetIdentifier =
         flag "asset-identifier" (required string) ~doc:"STRING AssetId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING FilterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_asset_filter
           (Values.GetAssetFilterInput.make ~domainIdentifier
              ~assetIdentifier ~identifier ())
           (Some Values.GetAssetFilterOutput.to_json)
           (Some Values.GetAssetFilterOutput.error_to_json)])
let get_asset_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revision =
         flag "revision" (optional string) ~doc:"STRING Revision"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING AssetTypeIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_asset_type
           (Values.GetAssetTypeInput.make ?revision ~domainIdentifier
              ~identifier ()) (Some Values.GetAssetTypeOutput.to_json)
           (Some Values.GetAssetTypeOutput.error_to_json)])
let get_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and withSecret =
         flag "with-secret" (optional bool) ~doc:"BOOL Boolean"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connection
           (Values.GetConnectionInput.make ?withSecret ~domainIdentifier
              ~identifier ()) (Some Values.GetConnectionOutput.to_json)
           (Some Values.GetConnectionOutput.error_to_json)])
let get_data_export_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_export_configuration
           (Values.GetDataExportConfigurationInput.make ~domainIdentifier ())
           (Some Values.GetDataExportConfigurationOutput.to_json)
           (Some Values.GetDataExportConfigurationOutput.error_to_json)])
let get_data_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revision =
         flag "revision" (optional string) ~doc:"STRING Revision"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DataProductId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_product
           (Values.GetDataProductInput.make ?revision ~domainIdentifier
              ~identifier ()) (Some Values.GetDataProductOutput.to_json)
           (Some Values.GetDataProductOutput.error_to_json)])
let get_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DataSourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_source
           (Values.GetDataSourceInput.make ~domainIdentifier ~identifier ())
           (Some Values.GetDataSourceOutput.to_json)
           (Some Values.GetDataSourceOutput.error_to_json)])
let get_data_source_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DataSourceRunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_source_run
           (Values.GetDataSourceRunInput.make ~domainIdentifier ~identifier
              ()) (Some Values.GetDataSourceRunOutput.to_json)
           (Some Values.GetDataSourceRunOutput.error_to_json)])
let get_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain (Values.GetDomainInput.make ~identifier ())
           (Some Values.GetDomainOutput.to_json)
           (Some Values.GetDomainOutput.error_to_json)])
let get_domain_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DomainUnitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_unit
           (Values.GetDomainUnitInput.make ~domainIdentifier ~identifier ())
           (Some Values.GetDomainUnitOutput.to_json)
           (Some Values.GetDomainUnitOutput.error_to_json)])
let get_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment
           (Values.GetEnvironmentInput.make ~domainIdentifier ~identifier ())
           (Some Values.GetEnvironmentOutput.to_json)
           (Some Values.GetEnvironmentOutput.error_to_json)])
let get_environment_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment_action
           (Values.GetEnvironmentActionInput.make ~domainIdentifier
              ~environmentIdentifier ~identifier ())
           (Some Values.GetEnvironmentActionOutput.to_json)
           (Some Values.GetEnvironmentActionOutput.error_to_json)])
let get_environment_blueprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING EnvironmentBlueprintId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment_blueprint
           (Values.GetEnvironmentBlueprintInput.make ~domainIdentifier
              ~identifier ())
           (Some Values.GetEnvironmentBlueprintOutput.to_json)
           (Some Values.GetEnvironmentBlueprintOutput.error_to_json)])
let get_environment_blueprint_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentBlueprintIdentifier =
         flag "environment-blueprint-identifier" (required string)
           ~doc:"STRING EnvironmentBlueprintId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment_blueprint_configuration
           (Values.GetEnvironmentBlueprintConfigurationInput.make
              ~domainIdentifier ~environmentBlueprintIdentifier ())
           (Some Values.GetEnvironmentBlueprintConfigurationOutput.to_json)
           (Some
              Values.GetEnvironmentBlueprintConfigurationOutput.error_to_json)])
let get_environment_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment_credentials
           (Values.GetEnvironmentCredentialsInput.make ~domainIdentifier
              ~environmentIdentifier ())
           (Some Values.GetEnvironmentCredentialsOutput.to_json)
           (Some Values.GetEnvironmentCredentialsOutput.error_to_json)])
let get_environment_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING EnvironmentProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment_profile
           (Values.GetEnvironmentProfileInput.make ~domainIdentifier
              ~identifier ())
           (Some Values.GetEnvironmentProfileOutput.to_json)
           (Some Values.GetEnvironmentProfileOutput.error_to_json)])
let get_form_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revision =
         flag "revision" (optional string) ~doc:"STRING Revision"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and formTypeIdentifier =
         flag "form-type-identifier" (required string)
           ~doc:"STRING FormTypeIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_form_type
           (Values.GetFormTypeInput.make ?revision ~domainIdentifier
              ~formTypeIdentifier ()) (Some Values.GetFormTypeOutput.to_json)
           (Some Values.GetFormTypeOutput.error_to_json)])
let get_glossary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING GlossaryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_glossary
           (Values.GetGlossaryInput.make ~domainIdentifier ~identifier ())
           (Some Values.GetGlossaryOutput.to_json)
           (Some Values.GetGlossaryOutput.error_to_json)])
let get_glossary_term =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING GlossaryTermId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_glossary_term
           (Values.GetGlossaryTermInput.make ~domainIdentifier ~identifier ())
           (Some Values.GetGlossaryTermOutput.to_json)
           (Some Values.GetGlossaryTermOutput.error_to_json)])
let get_group_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and groupIdentifier =
         flag "group-identifier" (required string)
           ~doc:"STRING GroupIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_group_profile
           (Values.GetGroupProfileInput.make ~domainIdentifier
              ~groupIdentifier ())
           (Some Values.GetGroupProfileOutput.to_json)
           (Some Values.GetGroupProfileOutput.error_to_json)])
let get_iam_portal_login_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_iam_portal_login_url
           (Values.GetIamPortalLoginUrlInput.make ~domainIdentifier ())
           (Some Values.GetIamPortalLoginUrlOutput.to_json)
           (Some Values.GetIamPortalLoginUrlOutput.error_to_json)])
let get_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING RunIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job_run
           (Values.GetJobRunInput.make ~domainIdentifier ~identifier ())
           (Some Values.GetJobRunOutput.to_json)
           (Some Values.GetJobRunOutput.error_to_json)])
let get_lineage_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING LineageEventIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_lineage_event
           (Values.GetLineageEventInput.make ~domainIdentifier ~identifier ())
           (Some Values.GetLineageEventOutput.to_json)
           (Some Values.GetLineageEventOutput.error_to_json)])
let get_lineage_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventTimestamp =
         flag "event-timestamp" (optional json_arg) ~doc:"JSON Timestamp"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING LineageNodeIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_lineage_node
           (Values.GetLineageNodeInput.make
              ?eventTimestamp:(Option.map ~f:Values.Timestamp.of_json
                                 eventTimestamp) ~domainIdentifier
              ~identifier ()) (Some Values.GetLineageNodeOutput.to_json)
           (Some Values.GetLineageNodeOutput.error_to_json)])
let get_listing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listingRevision =
         flag "listing-revision" (optional string) ~doc:"STRING Revision"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ListingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_listing
           (Values.GetListingInput.make ?listingRevision ~domainIdentifier
              ~identifier ()) (Some Values.GetListingOutput.to_json)
           (Some Values.GetListingOutput.error_to_json)])
let get_metadata_generation_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg)
           ~doc:"JSON MetadataGenerationRunType"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING MetadataGenerationRunIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_metadata_generation_run
           (Values.GetMetadataGenerationRunInput.make
              ?type_:(Option.map ~f:Values.MetadataGenerationRunType.of_json
                        type_) ~domainIdentifier ~identifier ())
           (Some Values.GetMetadataGenerationRunOutput.to_json)
           (Some Values.GetMetadataGenerationRunOutput.error_to_json)])
let get_notebook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING NotebookId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_notebook
           (Values.GetNotebookInput.make ~domainIdentifier ~identifier ())
           (Some Values.GetNotebookOutput.to_json)
           (Some Values.GetNotebookOutput.error_to_json)])
let get_notebook_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ExportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_notebook_export
           (Values.GetNotebookExportInput.make ~domainIdentifier ~identifier
              ()) (Some Values.GetNotebookExportOutput.to_json)
           (Some Values.GetNotebookExportOutput.error_to_json)])
let get_notebook_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING NotebookRunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_notebook_run
           (Values.GetNotebookRunInput.make ~domainIdentifier ~identifier ())
           (Some Values.GetNotebookRunOutput.to_json)
           (Some Values.GetNotebookRunOutput.error_to_json)])
let get_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_project
           (Values.GetProjectInput.make ~domainIdentifier ~identifier ())
           (Some Values.GetProjectOutput.to_json)
           (Some Values.GetProjectOutput.error_to_json)])
let get_project_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ProjectProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_project_profile
           (Values.GetProjectProfileInput.make ~domainIdentifier ~identifier
              ()) (Some Values.GetProjectProfileOutput.to_json)
           (Some Values.GetProjectProfileOutput.error_to_json)])
let get_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revision =
         flag "revision" (optional string) ~doc:"STRING Revision"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING RuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_rule
           (Values.GetRuleInput.make ?revision ~domainIdentifier ~identifier
              ()) (Some Values.GetRuleOutput.to_json)
           (Some Values.GetRuleOutput.error_to_json)])
let get_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING SubscriptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_subscription
           (Values.GetSubscriptionInput.make ~domainIdentifier ~identifier ())
           (Some Values.GetSubscriptionOutput.to_json)
           (Some Values.GetSubscriptionOutput.error_to_json)])
let get_subscription_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING SubscriptionGrantId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_subscription_grant
           (Values.GetSubscriptionGrantInput.make ~domainIdentifier
              ~identifier ())
           (Some Values.GetSubscriptionGrantOutput.to_json)
           (Some Values.GetSubscriptionGrantOutput.error_to_json)])
let get_subscription_request_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING SubscriptionRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_subscription_request_details
           (Values.GetSubscriptionRequestDetailsInput.make ~domainIdentifier
              ~identifier ())
           (Some Values.GetSubscriptionRequestDetailsOutput.to_json)
           (Some Values.GetSubscriptionRequestDetailsOutput.error_to_json)])
let get_subscription_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING SubscriptionTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_subscription_target
           (Values.GetSubscriptionTargetInput.make ~domainIdentifier
              ~environmentIdentifier ~identifier ())
           (Some Values.GetSubscriptionTargetOutput.to_json)
           (Some Values.GetSubscriptionTargetOutput.error_to_json)])
let get_time_series_data_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityIdentifier =
         flag "entity-identifier" (required string)
           ~doc:"STRING EntityIdentifier"
       and entityType =
         flag "entity-type" (required json_arg)
           ~doc:"JSON TimeSeriesEntityType"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING TimeSeriesDataPointIdentifier"
       and formName =
         flag "form-name" (required string) ~doc:"STRING TimeSeriesFormName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_time_series_data_point
           (Values.GetTimeSeriesDataPointInput.make ~domainIdentifier
              ~entityIdentifier
              ~entityType:(Values.TimeSeriesEntityType.of_json entityType)
              ~identifier ~formName ())
           (Some Values.GetTimeSeriesDataPointOutput.to_json)
           (Some Values.GetTimeSeriesDataPointOutput.error_to_json)])
let get_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON UserProfileType"
       and sessionName =
         flag "session-name" (optional string)
           ~doc:"STRING GetUserProfileInputSessionNameString"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and userIdentifier =
         flag "user-identifier" (required string)
           ~doc:"STRING UserIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user_profile
           (Values.GetUserProfileInput.make
              ?type_:(Option.map ~f:Values.UserProfileType.of_json type_)
              ?sessionName ~domainIdentifier ~userIdentifier ())
           (Some Values.GetUserProfileOutput.to_json)
           (Some Values.GetUserProfileOutput.error_to_json)])
let list_account_pools =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING AccountPoolName"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortFieldAccountPool"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_account_pools
           (Values.ListAccountPoolsInput.make ?name
              ?sortBy:(Option.map ~f:Values.SortFieldAccountPool.of_json
                         sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?nextToken ?maxResults ~domainIdentifier ())
           (Some Values.ListAccountPoolsOutput.to_json)
           (Some Values.ListAccountPoolsOutput.error_to_json)])
let list_accounts_in_account_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING AccountPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_accounts_in_account_pool
           (Values.ListAccountsInAccountPoolInput.make ?nextToken ?maxResults
              ~domainIdentifier ~identifier ())
           (Some Values.ListAccountsInAccountPoolOutput.to_json)
           (Some Values.ListAccountsInAccountPoolOutput.error_to_json)])
let list_asset_filters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON FilterStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and assetIdentifier =
         flag "asset-identifier" (required string) ~doc:"STRING AssetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_asset_filters
           (Values.ListAssetFiltersInput.make
              ?status:(Option.map ~f:Values.FilterStatus.of_json status)
              ?nextToken ?maxResults ~domainIdentifier ~assetIdentifier ())
           (Some Values.ListAssetFiltersOutput.to_json)
           (Some Values.ListAssetFiltersOutput.error_to_json)])
let list_asset_revisions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING AssetIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_asset_revisions
           (Values.ListAssetRevisionsInput.make ?nextToken ?maxResults
              ~domainIdentifier ~identifier ())
           (Some Values.ListAssetRevisionsOutput.to_json)
           (Some Values.ListAssetRevisionsOutput.error_to_json)])
let list_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortFieldConnection"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and name = flag "name" (optional string) ~doc:"STRING ConnectionName"
       and environmentIdentifier =
         flag "environment-identifier" (optional string)
           ~doc:"STRING EnvironmentId"
       and projectIdentifier =
         flag "project-identifier" (optional string) ~doc:"STRING ProjectId"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON ConnectionType"
       and scope =
         flag "scope" (optional json_arg) ~doc:"JSON ConnectionScope"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connections
           (Values.ListConnectionsInput.make ?maxResults ?nextToken
              ?sortBy:(Option.map ~f:Values.SortFieldConnection.of_json
                         sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?name ?environmentIdentifier ?projectIdentifier
              ?type_:(Option.map ~f:Values.ConnectionType.of_json type_)
              ?scope:(Option.map ~f:Values.ConnectionScope.of_json scope)
              ~domainIdentifier ())
           (Some Values.ListConnectionsOutput.to_json)
           (Some Values.ListConnectionsOutput.error_to_json)])
let list_data_product_revisions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DataProductId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_product_revisions
           (Values.ListDataProductRevisionsInput.make ?maxResults ?nextToken
              ~domainIdentifier ~identifier ())
           (Some Values.ListDataProductRevisionsOutput.to_json)
           (Some Values.ListDataProductRevisionsOutput.error_to_json)])
let list_data_source_run_activities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON DataAssetActivityStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DataSourceRunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_source_run_activities
           (Values.ListDataSourceRunActivitiesInput.make
              ?status:(Option.map ~f:Values.DataAssetActivityStatus.of_json
                         status) ?nextToken ?maxResults ~domainIdentifier
              ~identifier ())
           (Some Values.ListDataSourceRunActivitiesOutput.to_json)
           (Some Values.ListDataSourceRunActivitiesOutput.error_to_json)])
let list_data_source_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON DataSourceRunStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and dataSourceIdentifier =
         flag "data-source-identifier" (required string)
           ~doc:"STRING DataSourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_source_runs
           (Values.ListDataSourceRunsInput.make
              ?status:(Option.map ~f:Values.DataSourceRunStatus.of_json
                         status) ?nextToken ?maxResults ~domainIdentifier
              ~dataSourceIdentifier ())
           (Some Values.ListDataSourceRunsOutput.to_json)
           (Some Values.ListDataSourceRunsOutput.error_to_json)])
let list_data_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentIdentifier =
         flag "environment-identifier" (optional string) ~doc:"STRING String"
       and connectionIdentifier =
         flag "connection-identifier" (optional string) ~doc:"STRING String"
       and type_ =
         flag "type-" (optional string) ~doc:"STRING DataSourceType"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON DataSourceStatus"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and projectIdentifier =
         flag "project-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_sources
           (Values.ListDataSourcesInput.make ?environmentIdentifier
              ?connectionIdentifier ?type_
              ?status:(Option.map ~f:Values.DataSourceStatus.of_json status)
              ?name ?nextToken ?maxResults ~domainIdentifier
              ~projectIdentifier ())
           (Some Values.ListDataSourcesOutput.to_json)
           (Some Values.ListDataSourcesOutput.error_to_json)])
let list_domain_units_for_parent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsForListDomains"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and parentDomainUnitIdentifier =
         flag "parent-domain-unit-identifier" (required string)
           ~doc:"STRING DomainUnitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domain_units_for_parent
           (Values.ListDomainUnitsForParentInput.make ?maxResults ?nextToken
              ~domainIdentifier ~parentDomainUnitIdentifier ())
           (Some Values.ListDomainUnitsForParentOutput.to_json)
           (Some Values.ListDomainUnitsForParentOutput.error_to_json)])
let list_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON DomainStatus"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsForListDomains"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domains
           (Values.ListDomainsInput.make
              ?status:(Option.map ~f:Values.DomainStatus.of_json status)
              ?maxResults ?nextToken ())
           (Some Values.ListDomainsOutput.to_json)
           (Some Values.ListDomainsOutput.error_to_json)])
let list_entity_owners =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsForListDomains"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityType =
         flag "entity-type" (required json_arg)
           ~doc:"JSON DataZoneEntityType"
       and entityIdentifier =
         flag "entity-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_entity_owners
           (Values.ListEntityOwnersInput.make ?maxResults ?nextToken
              ~domainIdentifier
              ~entityType:(Values.DataZoneEntityType.of_json entityType)
              ~entityIdentifier ())
           (Some Values.ListEntityOwnersOutput.to_json)
           (Some Values.ListEntityOwnersOutput.error_to_json)])
let list_environment_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environment_actions
           (Values.ListEnvironmentActionsInput.make ?nextToken ?maxResults
              ~domainIdentifier ~environmentIdentifier ())
           (Some Values.ListEnvironmentActionsOutput.to_json)
           (Some Values.ListEnvironmentActionsOutput.error_to_json)])
let list_environment_blueprint_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environment_blueprint_configurations
           (Values.ListEnvironmentBlueprintConfigurationsInput.make
              ?maxResults ?nextToken ~domainIdentifier ())
           (Some Values.ListEnvironmentBlueprintConfigurationsOutput.to_json)
           (Some
              Values.ListEnvironmentBlueprintConfigurationsOutput.error_to_json)])
let list_environment_blueprints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and name =
         flag "name" (optional string) ~doc:"STRING EnvironmentBlueprintName"
       and managed = flag "managed" (optional bool) ~doc:"BOOL Boolean"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environment_blueprints
           (Values.ListEnvironmentBlueprintsInput.make ?maxResults ?nextToken
              ?name ?managed ~domainIdentifier ())
           (Some Values.ListEnvironmentBlueprintsOutput.to_json)
           (Some Values.ListEnvironmentBlueprintsOutput.error_to_json)])
let list_environment_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (optional string) ~doc:"STRING AwsAccountId"
       and awsAccountRegion =
         flag "aws-account-region" (optional string) ~doc:"STRING AwsRegion"
       and environmentBlueprintIdentifier =
         flag "environment-blueprint-identifier" (optional string)
           ~doc:"STRING EnvironmentBlueprintId"
       and projectIdentifier =
         flag "project-identifier" (optional string) ~doc:"STRING ProjectId"
       and name =
         flag "name" (optional string) ~doc:"STRING EnvironmentProfileName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environment_profiles
           (Values.ListEnvironmentProfilesInput.make ?awsAccountId
              ?awsAccountRegion ?environmentBlueprintIdentifier
              ?projectIdentifier ?name ?nextToken ?maxResults
              ~domainIdentifier ())
           (Some Values.ListEnvironmentProfilesOutput.to_json)
           (Some Values.ListEnvironmentProfilesOutput.error_to_json)])
let list_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (optional string) ~doc:"STRING AwsAccountId"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON EnvironmentStatus"
       and awsAccountRegion =
         flag "aws-account-region" (optional string) ~doc:"STRING AwsRegion"
       and environmentProfileIdentifier =
         flag "environment-profile-identifier" (optional string)
           ~doc:"STRING EnvironmentProfileId"
       and environmentBlueprintIdentifier =
         flag "environment-blueprint-identifier" (optional string)
           ~doc:"STRING EnvironmentBlueprintId"
       and provider = flag "provider" (optional string) ~doc:"STRING String"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and projectIdentifier =
         flag "project-identifier" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environments
           (Values.ListEnvironmentsInput.make ?awsAccountId
              ?status:(Option.map ~f:Values.EnvironmentStatus.of_json status)
              ?awsAccountRegion ?environmentProfileIdentifier
              ?environmentBlueprintIdentifier ?provider ?name ?maxResults
              ?nextToken ~domainIdentifier ~projectIdentifier ())
           (Some Values.ListEnvironmentsOutput.to_json)
           (Some Values.ListEnvironmentsOutput.error_to_json)])
let list_job_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON JobRunStatus"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and jobIdentifier =
         flag "job-identifier" (required string)
           ~doc:"STRING ListJobRunsInputJobIdentifierString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_job_runs
           (Values.ListJobRunsInput.make
              ?status:(Option.map ~f:Values.JobRunStatus.of_json status)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?nextToken ?maxResults ~domainIdentifier ~jobIdentifier ())
           (Some Values.ListJobRunsOutput.to_json)
           (Some Values.ListJobRunsOutput.error_to_json)])
let list_lineage_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and timestampAfter =
         flag "timestamp-after" (optional json_arg) ~doc:"JSON Timestamp"
       and timestampBefore =
         flag "timestamp-before" (optional json_arg) ~doc:"JSON Timestamp"
       and processingStatus =
         flag "processing-status" (optional json_arg)
           ~doc:"JSON LineageEventProcessingStatus"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_lineage_events
           (Values.ListLineageEventsInput.make ?maxResults
              ?timestampAfter:(Option.map ~f:Values.Timestamp.of_json
                                 timestampAfter)
              ?timestampBefore:(Option.map ~f:Values.Timestamp.of_json
                                  timestampBefore)
              ?processingStatus:(Option.map
                                   ~f:Values.LineageEventProcessingStatus.of_json
                                   processingStatus)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?nextToken ~domainIdentifier ())
           (Some Values.ListLineageEventsOutput.to_json)
           (Some Values.ListLineageEventsOutput.error_to_json)])
let list_lineage_node_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and direction =
         flag "direction" (optional json_arg) ~doc:"JSON EdgeDirection"
       and eventTimestampGTE =
         flag "event-timestamp-g-t-e" (optional json_arg)
           ~doc:"JSON Timestamp"
       and eventTimestampLTE =
         flag "event-timestamp-l-t-e" (optional json_arg)
           ~doc:"JSON Timestamp"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING LineageNodeIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_lineage_node_history
           (Values.ListLineageNodeHistoryInput.make ?maxResults ?nextToken
              ?direction:(Option.map ~f:Values.EdgeDirection.of_json
                            direction)
              ?eventTimestampGTE:(Option.map ~f:Values.Timestamp.of_json
                                    eventTimestampGTE)
              ?eventTimestampLTE:(Option.map ~f:Values.Timestamp.of_json
                                    eventTimestampLTE)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ~domainIdentifier ~identifier ())
           (Some Values.ListLineageNodeHistoryOutput.to_json)
           (Some Values.ListLineageNodeHistoryOutput.error_to_json)])
let list_metadata_generation_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON MetadataGenerationRunStatus"
       and type_ =
         flag "type-" (optional json_arg)
           ~doc:"JSON MetadataGenerationRunType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and targetIdentifier =
         flag "target-identifier" (optional string) ~doc:"STRING EntityId"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_metadata_generation_runs
           (Values.ListMetadataGenerationRunsInput.make
              ?status:(Option.map
                         ~f:Values.MetadataGenerationRunStatus.of_json status)
              ?type_:(Option.map ~f:Values.MetadataGenerationRunType.of_json
                        type_) ?nextToken ?maxResults ?targetIdentifier
              ~domainIdentifier ())
           (Some Values.ListMetadataGenerationRunsOutput.to_json)
           (Some Values.ListMetadataGenerationRunsOutput.error_to_json)])
let list_notebook_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notebookIdentifier =
         flag "notebook-identifier" (optional string)
           ~doc:"STRING NotebookId"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON NotebookRunStatus"
       and scheduleIdentifier =
         flag "schedule-identifier" (optional string)
           ~doc:"STRING ScheduleId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and owningProjectIdentifier =
         flag "owning-project-identifier" (required string)
           ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_notebook_runs
           (Values.ListNotebookRunsInput.make ?notebookIdentifier
              ?status:(Option.map ~f:Values.NotebookRunStatus.of_json status)
              ?scheduleIdentifier ?maxResults
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?nextToken ~domainIdentifier ~owningProjectIdentifier ())
           (Some Values.ListNotebookRunsOutput.to_json)
           (Some Values.ListNotebookRunsOutput.error_to_json)])
let list_notebooks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SortKey"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON NotebookStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and owningProjectIdentifier =
         flag "owning-project-identifier" (required string)
           ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_notebooks
           (Values.ListNotebooksInput.make ?maxResults
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?sortBy:(Option.map ~f:Values.SortKey.of_json sortBy)
              ?status:(Option.map ~f:Values.NotebookStatus.of_json status)
              ?nextToken ~domainIdentifier ~owningProjectIdentifier ())
           (Some Values.ListNotebooksOutput.to_json)
           (Some Values.ListNotebooksOutput.error_to_json)])
let list_notifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and afterTimestamp =
         flag "after-timestamp" (optional json_arg) ~doc:"JSON Timestamp"
       and beforeTimestamp =
         flag "before-timestamp" (optional json_arg) ~doc:"JSON Timestamp"
       and subjects =
         flag "subjects" (optional json_arg) ~doc:"JSON NotificationSubjects"
       and taskStatus =
         flag "task-status" (optional json_arg) ~doc:"JSON TaskStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON NotificationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_notifications
           (Values.ListNotificationsInput.make
              ?afterTimestamp:(Option.map ~f:Values.Timestamp.of_json
                                 afterTimestamp)
              ?beforeTimestamp:(Option.map ~f:Values.Timestamp.of_json
                                  beforeTimestamp)
              ?subjects:(Option.map ~f:Values.NotificationSubjects.of_json
                           subjects)
              ?taskStatus:(Option.map ~f:Values.TaskStatus.of_json taskStatus)
              ?maxResults ?nextToken ~domainIdentifier
              ~type_:(Values.NotificationType.of_json type_) ())
           (Some Values.ListNotificationsOutput.to_json)
           (Some Values.ListNotificationsOutput.error_to_json)])
let list_policy_grants =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsForListDomains"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityType =
         flag "entity-type" (required json_arg) ~doc:"JSON TargetEntityType"
       and entityIdentifier =
         flag "entity-identifier" (required string) ~doc:"STRING String"
       and policyType =
         flag "policy-type" (required json_arg) ~doc:"JSON ManagedPolicyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_grants
           (Values.ListPolicyGrantsInput.make ?maxResults ?nextToken
              ~domainIdentifier
              ~entityType:(Values.TargetEntityType.of_json entityType)
              ~entityIdentifier
              ~policyType:(Values.ManagedPolicyType.of_json policyType) ())
           (Some Values.ListPolicyGrantsOutput.to_json)
           (Some Values.ListPolicyGrantsOutput.error_to_json)])
let list_project_memberships =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortFieldProject"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and projectIdentifier =
         flag "project-identifier" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_project_memberships
           (Values.ListProjectMembershipsInput.make
              ?sortBy:(Option.map ~f:Values.SortFieldProject.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?nextToken ?maxResults ~domainIdentifier ~projectIdentifier ())
           (Some Values.ListProjectMembershipsOutput.to_json)
           (Some Values.ListProjectMembershipsOutput.error_to_json)])
let list_project_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING ProjectProfileName"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortFieldProject"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_project_profiles
           (Values.ListProjectProfilesInput.make ?name
              ?sortBy:(Option.map ~f:Values.SortFieldProject.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?nextToken ?maxResults ~domainIdentifier ())
           (Some Values.ListProjectProfilesOutput.to_json)
           (Some Values.ListProjectProfilesOutput.error_to_json)])
let list_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userIdentifier =
         flag "user-identifier" (optional string) ~doc:"STRING String"
       and groupIdentifier =
         flag "group-identifier" (optional string) ~doc:"STRING String"
       and name = flag "name" (optional string) ~doc:"STRING ProjectName"
       and projectCategory =
         flag "project-category" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_projects
           (Values.ListProjectsInput.make ?userIdentifier ?groupIdentifier
              ?name ?projectCategory ?nextToken ?maxResults ~domainIdentifier
              ()) (Some Values.ListProjectsOutput.to_json)
           (Some Values.ListProjectsOutput.error_to_json)])
let list_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleType =
         flag "rule-type" (optional json_arg) ~doc:"JSON RuleType"
       and action = flag "action" (optional json_arg) ~doc:"JSON RuleAction"
       and projectIds =
         flag "project-ids" (optional json_arg) ~doc:"JSON ProjectIds"
       and assetTypes =
         flag "asset-types" (optional json_arg)
           ~doc:"JSON AssetTypeIdentifiers"
       and dataProduct =
         flag "data-product" (optional bool) ~doc:"BOOL Boolean"
       and includeCascaded =
         flag "include-cascaded" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRulesInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and targetType =
         flag "target-type" (required json_arg) ~doc:"JSON RuleTargetType"
       and targetIdentifier =
         flag "target-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rules
           (Values.ListRulesInput.make
              ?ruleType:(Option.map ~f:Values.RuleType.of_json ruleType)
              ?action:(Option.map ~f:Values.RuleAction.of_json action)
              ?projectIds:(Option.map ~f:Values.ProjectIds.of_json projectIds)
              ?assetTypes:(Option.map ~f:Values.AssetTypeIdentifiers.of_json
                             assetTypes) ?dataProduct ?includeCascaded
              ?maxResults ?nextToken ~domainIdentifier
              ~targetType:(Values.RuleTargetType.of_json targetType)
              ~targetIdentifier ()) (Some Values.ListRulesOutput.to_json)
           (Some Values.ListRulesOutput.error_to_json)])
let list_subscription_grants =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and subscriptionTargetId =
         flag "subscription-target-id" (optional string)
           ~doc:"STRING SubscriptionTargetId"
       and subscribedListingId =
         flag "subscribed-listing-id" (optional string)
           ~doc:"STRING ListingId"
       and subscriptionId =
         flag "subscription-id" (optional string)
           ~doc:"STRING SubscriptionId"
       and owningProjectId =
         flag "owning-project-id" (optional string) ~doc:"STRING ProjectId"
       and owningIamPrincipalArn =
         flag "owning-iam-principal-arn" (optional string)
           ~doc:"STRING IamPrincipalArn"
       and owningUserId =
         flag "owning-user-id" (optional string) ~doc:"STRING UserProfileId"
       and owningGroupId =
         flag "owning-group-id" (optional string)
           ~doc:"STRING GroupProfileId"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SortKey"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_subscription_grants
           (Values.ListSubscriptionGrantsInput.make ?environmentId
              ?subscriptionTargetId ?subscribedListingId ?subscriptionId
              ?owningProjectId ?owningIamPrincipalArn ?owningUserId
              ?owningGroupId
              ?sortBy:(Option.map ~f:Values.SortKey.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?maxResults ?nextToken ~domainIdentifier ())
           (Some Values.ListSubscriptionGrantsOutput.to_json)
           (Some Values.ListSubscriptionGrantsOutput.error_to_json)])
let list_subscription_requests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON SubscriptionRequestStatus"
       and subscribedListingId =
         flag "subscribed-listing-id" (optional string)
           ~doc:"STRING ListingId"
       and owningProjectId =
         flag "owning-project-id" (optional string) ~doc:"STRING ProjectId"
       and owningIamPrincipalArn =
         flag "owning-iam-principal-arn" (optional string)
           ~doc:"STRING IamPrincipalArn"
       and approverProjectId =
         flag "approver-project-id" (optional string) ~doc:"STRING ProjectId"
       and owningUserId =
         flag "owning-user-id" (optional string) ~doc:"STRING UserProfileId"
       and owningGroupId =
         flag "owning-group-id" (optional string)
           ~doc:"STRING GroupProfileId"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SortKey"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_subscription_requests
           (Values.ListSubscriptionRequestsInput.make
              ?status:(Option.map ~f:Values.SubscriptionRequestStatus.of_json
                         status) ?subscribedListingId ?owningProjectId
              ?owningIamPrincipalArn ?approverProjectId ?owningUserId
              ?owningGroupId
              ?sortBy:(Option.map ~f:Values.SortKey.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?maxResults ?nextToken ~domainIdentifier ())
           (Some Values.ListSubscriptionRequestsOutput.to_json)
           (Some Values.ListSubscriptionRequestsOutput.error_to_json)])
let list_subscription_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SortKey"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_subscription_targets
           (Values.ListSubscriptionTargetsInput.make
              ?sortBy:(Option.map ~f:Values.SortKey.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?maxResults ?nextToken ~domainIdentifier ~environmentIdentifier
              ()) (Some Values.ListSubscriptionTargetsOutput.to_json)
           (Some Values.ListSubscriptionTargetsOutput.error_to_json)])
let list_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionRequestIdentifier =
         flag "subscription-request-identifier" (optional string)
           ~doc:"STRING SubscriptionRequestId"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON SubscriptionStatus"
       and subscribedListingId =
         flag "subscribed-listing-id" (optional string)
           ~doc:"STRING ListingId"
       and owningProjectId =
         flag "owning-project-id" (optional string) ~doc:"STRING ProjectId"
       and owningIamPrincipalArn =
         flag "owning-iam-principal-arn" (optional string)
           ~doc:"STRING IamPrincipalArn"
       and owningUserId =
         flag "owning-user-id" (optional string) ~doc:"STRING UserProfileId"
       and owningGroupId =
         flag "owning-group-id" (optional string)
           ~doc:"STRING GroupProfileId"
       and approverProjectId =
         flag "approver-project-id" (optional string) ~doc:"STRING ProjectId"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SortKey"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_subscriptions
           (Values.ListSubscriptionsInput.make ?subscriptionRequestIdentifier
              ?status:(Option.map ~f:Values.SubscriptionStatus.of_json status)
              ?subscribedListingId ?owningProjectId ?owningIamPrincipalArn
              ?owningUserId ?owningGroupId ?approverProjectId
              ?sortBy:(Option.map ~f:Values.SortKey.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?maxResults ?nextToken ~domainIdentifier ())
           (Some Values.ListSubscriptionsOutput.to_json)
           (Some Values.ListSubscriptionsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_time_series_data_points =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startedAt =
         flag "started-at" (optional json_arg) ~doc:"JSON Timestamp"
       and endedAt =
         flag "ended-at" (optional json_arg) ~doc:"JSON Timestamp"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityIdentifier =
         flag "entity-identifier" (required string)
           ~doc:"STRING EntityIdentifier"
       and entityType =
         flag "entity-type" (required json_arg)
           ~doc:"JSON TimeSeriesEntityType"
       and formName =
         flag "form-name" (required string) ~doc:"STRING TimeSeriesFormName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_time_series_data_points
           (Values.ListTimeSeriesDataPointsInput.make
              ?startedAt:(Option.map ~f:Values.Timestamp.of_json startedAt)
              ?endedAt:(Option.map ~f:Values.Timestamp.of_json endedAt)
              ?nextToken ?maxResults ~domainIdentifier ~entityIdentifier
              ~entityType:(Values.TimeSeriesEntityType.of_json entityType)
              ~formName ())
           (Some Values.ListTimeSeriesDataPointsOutput.to_json)
           (Some Values.ListTimeSeriesDataPointsOutput.error_to_json)])
let post_lineage_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and event = flag "event" (required json_arg) ~doc:"JSON LineageEvent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.post_lineage_event
           (Values.PostLineageEventInput.make ?clientToken ~domainIdentifier
              ~event:(Values.LineageEvent.of_json event) ())
           (Some Values.PostLineageEventOutput.to_json)
           (Some Values.PostLineageEventOutput.error_to_json)])
let post_time_series_data_points =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityIdentifier =
         flag "entity-identifier" (required string)
           ~doc:"STRING EntityIdentifier"
       and entityType =
         flag "entity-type" (required json_arg)
           ~doc:"JSON TimeSeriesEntityType"
       and forms =
         flag "forms" (required json_arg)
           ~doc:"JSON TimeSeriesDataPointFormInputList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.post_time_series_data_points
           (Values.PostTimeSeriesDataPointsInput.make ?clientToken
              ~domainIdentifier ~entityIdentifier
              ~entityType:(Values.TimeSeriesEntityType.of_json entityType)
              ~forms:(Values.TimeSeriesDataPointFormInputList.of_json forms)
              ()) (Some Values.PostTimeSeriesDataPointsOutput.to_json)
           (Some Values.PostTimeSeriesDataPointsOutput.error_to_json)])
let put_data_export_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and enableExport =
         flag "enable-export" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_data_export_configuration
           (Values.PutDataExportConfigurationInput.make
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?clientToken ~domainIdentifier ~enableExport ())
           (Some Values.PutDataExportConfigurationOutput.to_json)
           (Some Values.PutDataExportConfigurationOutput.error_to_json)])
let put_environment_blueprint_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and provisioningRoleArn =
         flag "provisioning-role-arn" (optional string) ~doc:"STRING RoleArn"
       and manageAccessRoleArn =
         flag "manage-access-role-arn" (optional string)
           ~doc:"STRING RoleArn"
       and environmentRolePermissionBoundary =
         flag "environment-role-permission-boundary" (optional string)
           ~doc:"STRING PolicyArn"
       and regionalParameters =
         flag "regional-parameters" (optional json_arg)
           ~doc:"JSON RegionalParameterMap"
       and globalParameters =
         flag "global-parameters" (optional json_arg)
           ~doc:"JSON GlobalParameterMap"
       and provisioningConfigurations =
         flag "provisioning-configurations" (optional json_arg)
           ~doc:"JSON ProvisioningConfigurationList"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentBlueprintIdentifier =
         flag "environment-blueprint-identifier" (required string)
           ~doc:"STRING EnvironmentBlueprintId"
       and enabledRegions =
         flag "enabled-regions" (required json_arg)
           ~doc:"JSON EnabledRegionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_environment_blueprint_configuration
           (Values.PutEnvironmentBlueprintConfigurationInput.make
              ?provisioningRoleArn ?manageAccessRoleArn
              ?environmentRolePermissionBoundary
              ?regionalParameters:(Option.map
                                     ~f:Values.RegionalParameterMap.of_json
                                     regionalParameters)
              ?globalParameters:(Option.map
                                   ~f:Values.GlobalParameterMap.of_json
                                   globalParameters)
              ?provisioningConfigurations:(Option.map
                                             ~f:Values.ProvisioningConfigurationList.of_json
                                             provisioningConfigurations)
              ~domainIdentifier ~environmentBlueprintIdentifier
              ~enabledRegions:(Values.EnabledRegionList.of_json
                                 enabledRegions) ())
           (Some Values.PutEnvironmentBlueprintConfigurationOutput.to_json)
           (Some
              Values.PutEnvironmentBlueprintConfigurationOutput.error_to_json)])
let query_graph =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and additionalAttributes =
         flag "additional-attributes" (optional json_arg)
           ~doc:"JSON AdditionalAttributes"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and match_ =
         flag "match-" (required json_arg) ~doc:"JSON MatchClauses" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.query_graph
           (Values.QueryGraphInput.make ?maxResults ?nextToken
              ?additionalAttributes:(Option.map
                                       ~f:Values.AdditionalAttributes.of_json
                                       additionalAttributes)
              ~domainIdentifier ~match_:(Values.MatchClauses.of_json match_)
              ()) (Some Values.QueryGraphOutput.to_json)
           (Some Values.QueryGraphOutput.error_to_json)])
let reject_predictions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revision =
         flag "revision" (optional string) ~doc:"STRING Revision"
       and rejectRule =
         flag "reject-rule" (optional json_arg) ~doc:"JSON RejectRule"
       and rejectChoices =
         flag "reject-choices" (optional json_arg) ~doc:"JSON RejectChoices"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING AssetIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_predictions
           (Values.RejectPredictionsInput.make ?revision
              ?rejectRule:(Option.map ~f:Values.RejectRule.of_json rejectRule)
              ?rejectChoices:(Option.map ~f:Values.RejectChoices.of_json
                                rejectChoices) ?clientToken ~domainIdentifier
              ~identifier ()) (Some Values.RejectPredictionsOutput.to_json)
           (Some Values.RejectPredictionsOutput.error_to_json)])
let reject_subscription_request =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and decisionComment =
         flag "decision-comment" (optional string)
           ~doc:"STRING DecisionComment"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING SubscriptionRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_subscription_request
           (Values.RejectSubscriptionRequestInput.make ?decisionComment
              ~domainIdentifier ~identifier ())
           (Some Values.RejectSubscriptionRequestOutput.to_json)
           (Some Values.RejectSubscriptionRequestOutput.error_to_json)])
let remove_entity_owner =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityType =
         flag "entity-type" (required json_arg)
           ~doc:"JSON DataZoneEntityType"
       and entityIdentifier =
         flag "entity-identifier" (required string) ~doc:"STRING String"
       and owner =
         flag "owner" (required json_arg) ~doc:"JSON OwnerProperties" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_entity_owner
           (Values.RemoveEntityOwnerInput.make ?clientToken ~domainIdentifier
              ~entityType:(Values.DataZoneEntityType.of_json entityType)
              ~entityIdentifier ~owner:(Values.OwnerProperties.of_json owner)
              ()) (Some Values.RemoveEntityOwnerOutput.to_json)
           (Some Values.RemoveEntityOwnerOutput.error_to_json)])
let remove_policy_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantIdentifier =
         flag "grant-identifier" (optional string)
           ~doc:"STRING GrantIdentifier"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and entityType =
         flag "entity-type" (required json_arg) ~doc:"JSON TargetEntityType"
       and entityIdentifier =
         flag "entity-identifier" (required string) ~doc:"STRING String"
       and policyType =
         flag "policy-type" (required json_arg) ~doc:"JSON ManagedPolicyType"
       and principal =
         flag "principal" (required json_arg)
           ~doc:"JSON PolicyGrantPrincipal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_policy_grant
           (Values.RemovePolicyGrantInput.make ?grantIdentifier ?clientToken
              ~domainIdentifier
              ~entityType:(Values.TargetEntityType.of_json entityType)
              ~entityIdentifier
              ~policyType:(Values.ManagedPolicyType.of_json policyType)
              ~principal:(Values.PolicyGrantPrincipal.of_json principal) ())
           (Some Values.RemovePolicyGrantOutput.to_json)
           (Some Values.RemovePolicyGrantOutput.error_to_json)])
let revoke_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retainPermissions =
         flag "retain-permissions" (optional bool) ~doc:"BOOL Boolean"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING SubscriptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_subscription
           (Values.RevokeSubscriptionInput.make ?retainPermissions
              ~domainIdentifier ~identifier ())
           (Some Values.RevokeSubscriptionOutput.to_json)
           (Some Values.RevokeSubscriptionOutput.error_to_json)])
let search =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and owningProjectIdentifier =
         flag "owning-project-identifier" (optional string)
           ~doc:"STRING ProjectId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and searchText =
         flag "search-text" (optional string) ~doc:"STRING SearchText"
       and searchIn =
         flag "search-in" (optional json_arg) ~doc:"JSON SearchInList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterClause"
       and sort = flag "sort" (optional json_arg) ~doc:"JSON SearchSort"
       and additionalAttributes =
         flag "additional-attributes" (optional json_arg)
           ~doc:"JSON SearchOutputAdditionalAttributes"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and searchScope =
         flag "search-scope" (required json_arg)
           ~doc:"JSON InventorySearchScope" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.search
           (Values.SearchInput.make ?owningProjectIdentifier ?maxResults
              ?nextToken ?searchText
              ?searchIn:(Option.map ~f:Values.SearchInList.of_json searchIn)
              ?filters:(Option.map ~f:Values.FilterClause.of_json filters)
              ?sort:(Option.map ~f:Values.SearchSort.of_json sort)
              ?additionalAttributes:(Option.map
                                       ~f:Values.SearchOutputAdditionalAttributes.of_json
                                       additionalAttributes)
              ~domainIdentifier
              ~searchScope:(Values.InventorySearchScope.of_json searchScope)
              ()) (Some Values.SearchOutput.to_json)
           (Some Values.SearchOutput.error_to_json)])
let search_group_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchText =
         flag "search-text" (optional string) ~doc:"STRING GroupSearchText"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and groupType =
         flag "group-type" (required json_arg) ~doc:"JSON GroupSearchType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_group_profiles
           (Values.SearchGroupProfilesInput.make ?searchText ?maxResults
              ?nextToken ~domainIdentifier
              ~groupType:(Values.GroupSearchType.of_json groupType) ())
           (Some Values.SearchGroupProfilesOutput.to_json)
           (Some Values.SearchGroupProfilesOutput.error_to_json)])
let search_listings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchText =
         flag "search-text" (optional string)
           ~doc:"STRING SearchListingsInputSearchTextString"
       and searchIn =
         flag "search-in" (optional json_arg) ~doc:"JSON SearchInList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterClause"
       and aggregations =
         flag "aggregations" (optional json_arg) ~doc:"JSON AggregationList"
       and sort = flag "sort" (optional json_arg) ~doc:"JSON SearchSort"
       and additionalAttributes =
         flag "additional-attributes" (optional json_arg)
           ~doc:"JSON SearchOutputAdditionalAttributes"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_listings
           (Values.SearchListingsInput.make ?searchText
              ?searchIn:(Option.map ~f:Values.SearchInList.of_json searchIn)
              ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.FilterClause.of_json filters)
              ?aggregations:(Option.map ~f:Values.AggregationList.of_json
                               aggregations)
              ?sort:(Option.map ~f:Values.SearchSort.of_json sort)
              ?additionalAttributes:(Option.map
                                       ~f:Values.SearchOutputAdditionalAttributes.of_json
                                       additionalAttributes)
              ~domainIdentifier ())
           (Some Values.SearchListingsOutput.to_json)
           (Some Values.SearchListingsOutput.error_to_json)])
let search_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and searchText =
         flag "search-text" (optional string) ~doc:"STRING SearchText"
       and searchIn =
         flag "search-in" (optional json_arg) ~doc:"JSON SearchInList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterClause"
       and sort = flag "sort" (optional json_arg) ~doc:"JSON SearchSort"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and searchScope =
         flag "search-scope" (required json_arg) ~doc:"JSON TypesSearchScope"
       and managed = flag "managed" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_types
           (Values.SearchTypesInput.make ?maxResults ?nextToken ?searchText
              ?searchIn:(Option.map ~f:Values.SearchInList.of_json searchIn)
              ?filters:(Option.map ~f:Values.FilterClause.of_json filters)
              ?sort:(Option.map ~f:Values.SearchSort.of_json sort)
              ~domainIdentifier
              ~searchScope:(Values.TypesSearchScope.of_json searchScope)
              ~managed ()) (Some Values.SearchTypesOutput.to_json)
           (Some Values.SearchTypesOutput.error_to_json)])
let search_user_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchText =
         flag "search-text" (optional string) ~doc:"STRING UserSearchText"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and userType =
         flag "user-type" (required json_arg) ~doc:"JSON UserSearchType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_user_profiles
           (Values.SearchUserProfilesInput.make ?searchText ?maxResults
              ?nextToken ~domainIdentifier
              ~userType:(Values.UserSearchType.of_json userType) ())
           (Some Values.SearchUserProfilesOutput.to_json)
           (Some Values.SearchUserProfilesOutput.error_to_json)])
let start_data_source_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and dataSourceIdentifier =
         flag "data-source-identifier" (required string)
           ~doc:"STRING DataSourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_data_source_run
           (Values.StartDataSourceRunInput.make ?clientToken
              ~domainIdentifier ~dataSourceIdentifier ())
           (Some Values.StartDataSourceRunOutput.to_json)
           (Some Values.StartDataSourceRunOutput.error_to_json)])
let start_metadata_generation_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg)
           ~doc:"JSON MetadataGenerationRunType"
       and types =
         flag "types" (optional json_arg)
           ~doc:"JSON MetadataGenerationRunTypes"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and target =
         flag "target" (required json_arg)
           ~doc:"JSON MetadataGenerationRunTarget"
       and owningProjectIdentifier =
         flag "owning-project-identifier" (required string)
           ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_metadata_generation_run
           (Values.StartMetadataGenerationRunInput.make
              ?type_:(Option.map ~f:Values.MetadataGenerationRunType.of_json
                        type_)
              ?types:(Option.map ~f:Values.MetadataGenerationRunTypes.of_json
                        types) ?clientToken ~domainIdentifier
              ~target:(Values.MetadataGenerationRunTarget.of_json target)
              ~owningProjectIdentifier ())
           (Some Values.StartMetadataGenerationRunOutput.to_json)
           (Some Values.StartMetadataGenerationRunOutput.error_to_json)])
let start_notebook_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and notebookIdentifier =
         flag "notebook-identifier" (required string)
           ~doc:"STRING NotebookId"
       and owningProjectIdentifier =
         flag "owning-project-identifier" (required string)
           ~doc:"STRING ProjectId"
       and fileFormat =
         flag "file-format" (required json_arg) ~doc:"JSON FileFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_notebook_export
           (Values.StartNotebookExportInput.make ?clientToken
              ~domainIdentifier ~notebookIdentifier ~owningProjectIdentifier
              ~fileFormat:(Values.FileFormat.of_json fileFormat) ())
           (Some Values.StartNotebookExportOutput.to_json)
           (Some Values.StartNotebookExportOutput.error_to_json)])
let start_notebook_import =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and owningProjectIdentifier =
         flag "owning-project-identifier" (required string)
           ~doc:"STRING ProjectId"
       and sourceLocation =
         flag "source-location" (required json_arg)
           ~doc:"JSON SourceLocation"
       and name = flag "name" (required string) ~doc:"STRING NotebookName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_notebook_import
           (Values.StartNotebookImportInput.make ?description ?clientToken
              ~domainIdentifier ~owningProjectIdentifier
              ~sourceLocation:(Values.SourceLocation.of_json sourceLocation)
              ~name ()) (Some Values.StartNotebookImportOutput.to_json)
           (Some Values.StartNotebookImportOutput.error_to_json)])
let start_notebook_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduleIdentifier =
         flag "schedule-identifier" (optional string)
           ~doc:"STRING ScheduleId"
       and computeConfiguration =
         flag "compute-configuration" (optional json_arg)
           ~doc:"JSON ComputeConfig"
       and networkConfiguration =
         flag "network-configuration" (optional json_arg)
           ~doc:"JSON NetworkConfig"
       and timeoutConfiguration =
         flag "timeout-configuration" (optional json_arg)
           ~doc:"JSON TimeoutConfig"
       and triggerSource =
         flag "trigger-source" (optional json_arg) ~doc:"JSON TriggerSource"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON Metadata"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and owningProjectIdentifier =
         flag "owning-project-identifier" (required string)
           ~doc:"STRING ProjectId"
       and notebookIdentifier =
         flag "notebook-identifier" (required string)
           ~doc:"STRING NotebookId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_notebook_run
           (Values.StartNotebookRunInput.make ?scheduleIdentifier
              ?computeConfiguration:(Option.map
                                       ~f:Values.ComputeConfig.of_json
                                       computeConfiguration)
              ?networkConfiguration:(Option.map
                                       ~f:Values.NetworkConfig.of_json
                                       networkConfiguration)
              ?timeoutConfiguration:(Option.map
                                       ~f:Values.TimeoutConfig.of_json
                                       timeoutConfiguration)
              ?triggerSource:(Option.map ~f:Values.TriggerSource.of_json
                                triggerSource)
              ?metadata:(Option.map ~f:Values.Metadata.of_json metadata)
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?clientToken ~domainIdentifier ~owningProjectIdentifier
              ~notebookIdentifier ())
           (Some Values.StartNotebookRunOutput.to_json)
           (Some Values.StartNotebookRunOutput.error_to_json)])
let stop_notebook_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING NotebookRunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_notebook_run
           (Values.StopNotebookRunInput.make ?clientToken ~domainIdentifier
              ~identifier ()) (Some Values.StopNotebookRunOutput.to_json)
           (Some Values.StopNotebookRunOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_account_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING AccountPoolName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and resolutionStrategy =
         flag "resolution-strategy" (optional json_arg)
           ~doc:"JSON ResolutionStrategy"
       and accountSource =
         flag "account-source" (optional json_arg) ~doc:"JSON AccountSource"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING AccountPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account_pool
           (Values.UpdateAccountPoolInput.make ?name ?description
              ?resolutionStrategy:(Option.map
                                     ~f:Values.ResolutionStrategy.of_json
                                     resolutionStrategy)
              ?accountSource:(Option.map ~f:Values.AccountSource.of_json
                                accountSource) ~domainIdentifier ~identifier
              ()) (Some Values.UpdateAccountPoolOutput.to_json)
           (Some Values.UpdateAccountPoolOutput.error_to_json)])
let update_asset_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON AssetFilterConfiguration"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and assetIdentifier =
         flag "asset-identifier" (required string) ~doc:"STRING AssetId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING FilterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_asset_filter
           (Values.UpdateAssetFilterInput.make ?name ?description
              ?configuration:(Option.map
                                ~f:Values.AssetFilterConfiguration.of_json
                                configuration) ~domainIdentifier
              ~assetIdentifier ~identifier ())
           (Some Values.UpdateAssetFilterOutput.to_json)
           (Some Values.UpdateAssetFilterOutput.error_to_json)])
let update_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurations =
         flag "configurations" (optional json_arg) ~doc:"JSON Configurations"
       and description =
         flag "description" (optional string)
           ~doc:"STRING UpdateConnectionInputDescriptionString"
       and awsLocation =
         flag "aws-location" (optional json_arg) ~doc:"JSON AwsLocation"
       and props =
         flag "props" (optional json_arg)
           ~doc:"JSON ConnectionPropertiesPatch"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connection
           (Values.UpdateConnectionInput.make
              ?configurations:(Option.map ~f:Values.Configurations.of_json
                                 configurations) ?description
              ?awsLocation:(Option.map ~f:Values.AwsLocation.of_json
                              awsLocation)
              ?props:(Option.map ~f:Values.ConnectionPropertiesPatch.of_json
                        props) ~domainIdentifier ~identifier ())
           (Some Values.UpdateConnectionOutput.to_json)
           (Some Values.UpdateConnectionOutput.error_to_json)])
let update_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and enableSetting =
         flag "enable-setting" (optional json_arg) ~doc:"JSON EnableSetting"
       and publishOnImport =
         flag "publish-on-import" (optional bool) ~doc:"BOOL Boolean"
       and assetFormsInput =
         flag "asset-forms-input" (optional json_arg)
           ~doc:"JSON FormInputList"
       and schedule =
         flag "schedule" (optional json_arg)
           ~doc:"JSON ScheduleConfiguration"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON DataSourceConfigurationInput"
       and recommendation =
         flag "recommendation" (optional json_arg)
           ~doc:"JSON RecommendationConfiguration"
       and retainPermissionsOnRevokeFailure =
         flag "retain-permissions-on-revoke-failure" (optional bool)
           ~doc:"BOOL Boolean"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DataSourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_source
           (Values.UpdateDataSourceInput.make ?name ?description
              ?enableSetting:(Option.map ~f:Values.EnableSetting.of_json
                                enableSetting) ?publishOnImport
              ?assetFormsInput:(Option.map ~f:Values.FormInputList.of_json
                                  assetFormsInput)
              ?schedule:(Option.map ~f:Values.ScheduleConfiguration.of_json
                           schedule)
              ?configuration:(Option.map
                                ~f:Values.DataSourceConfigurationInput.of_json
                                configuration)
              ?recommendation:(Option.map
                                 ~f:Values.RecommendationConfiguration.of_json
                                 recommendation)
              ?retainPermissionsOnRevokeFailure ~domainIdentifier ~identifier
              ()) (Some Values.UpdateDataSourceOutput.to_json)
           (Some Values.UpdateDataSourceOutput.error_to_json)])
let update_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and singleSignOn =
         flag "single-sign-on" (optional json_arg) ~doc:"JSON SingleSignOn"
       and domainExecutionRole =
         flag "domain-execution-role" (optional string) ~doc:"STRING RoleArn"
       and serviceRole =
         flag "service-role" (optional string) ~doc:"STRING RoleArn"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain
           (Values.UpdateDomainInput.make ?description
              ?singleSignOn:(Option.map ~f:Values.SingleSignOn.of_json
                               singleSignOn) ?domainExecutionRole
              ?serviceRole ?name ?clientToken ~identifier ())
           (Some Values.UpdateDomainOutput.to_json)
           (Some Values.UpdateDomainOutput.error_to_json)])
let update_domain_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING DomainUnitDescription"
       and name = flag "name" (optional string) ~doc:"STRING DomainUnitName"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING DomainUnitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain_unit
           (Values.UpdateDomainUnitInput.make ?description ?name
              ~domainIdentifier ~identifier ())
           (Some Values.UpdateDomainUnitOutput.to_json)
           (Some Values.UpdateDomainUnitOutput.error_to_json)])
let update_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and glossaryTerms =
         flag "glossary-terms" (optional json_arg) ~doc:"JSON GlossaryTerms"
       and blueprintVersion =
         flag "blueprint-version" (optional string) ~doc:"STRING String"
       and userParameters =
         flag "user-parameters" (optional json_arg)
           ~doc:"JSON EnvironmentParametersList"
       and environmentConfigurationName =
         flag "environment-configuration-name" (optional string)
           ~doc:"STRING EnvironmentConfigurationName"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment
           (Values.UpdateEnvironmentInput.make ?name ?description
              ?glossaryTerms:(Option.map ~f:Values.GlossaryTerms.of_json
                                glossaryTerms) ?blueprintVersion
              ?userParameters:(Option.map
                                 ~f:Values.EnvironmentParametersList.of_json
                                 userParameters)
              ?environmentConfigurationName ~domainIdentifier ~identifier ())
           (Some Values.UpdateEnvironmentOutput.to_json)
           (Some Values.UpdateEnvironmentOutput.error_to_json)])
let update_environment_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON ActionParameters"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment_action
           (Values.UpdateEnvironmentActionInput.make
              ?parameters:(Option.map ~f:Values.ActionParameters.of_json
                             parameters) ?name ?description ~domainIdentifier
              ~environmentIdentifier ~identifier ())
           (Some Values.UpdateEnvironmentActionOutput.to_json)
           (Some Values.UpdateEnvironmentActionOutput.error_to_json)])
let update_environment_blueprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and provisioningProperties =
         flag "provisioning-properties" (optional json_arg)
           ~doc:"JSON ProvisioningProperties"
       and userParameters =
         flag "user-parameters" (optional json_arg)
           ~doc:"JSON CustomParameterList"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING EnvironmentBlueprintId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment_blueprint
           (Values.UpdateEnvironmentBlueprintInput.make ?description
              ?provisioningProperties:(Option.map
                                         ~f:Values.ProvisioningProperties.of_json
                                         provisioningProperties)
              ?userParameters:(Option.map
                                 ~f:Values.CustomParameterList.of_json
                                 userParameters) ~domainIdentifier
              ~identifier ())
           (Some Values.UpdateEnvironmentBlueprintOutput.to_json)
           (Some Values.UpdateEnvironmentBlueprintOutput.error_to_json)])
let update_environment_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING EnvironmentProfileName"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and userParameters =
         flag "user-parameters" (optional json_arg)
           ~doc:"JSON EnvironmentParametersList"
       and awsAccountId =
         flag "aws-account-id" (optional string) ~doc:"STRING AwsAccountId"
       and awsAccountRegion =
         flag "aws-account-region" (optional string) ~doc:"STRING AwsRegion"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING EnvironmentProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment_profile
           (Values.UpdateEnvironmentProfileInput.make ?name ?description
              ?userParameters:(Option.map
                                 ~f:Values.EnvironmentParametersList.of_json
                                 userParameters) ?awsAccountId
              ?awsAccountRegion ~domainIdentifier ~identifier ())
           (Some Values.UpdateEnvironmentProfileOutput.to_json)
           (Some Values.UpdateEnvironmentProfileOutput.error_to_json)])
let update_glossary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING GlossaryName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING GlossaryDescription"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON GlossaryStatus"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING GlossaryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_glossary
           (Values.UpdateGlossaryInput.make ?name ?description
              ?status:(Option.map ~f:Values.GlossaryStatus.of_json status)
              ?clientToken ~domainIdentifier ~identifier ())
           (Some Values.UpdateGlossaryOutput.to_json)
           (Some Values.UpdateGlossaryOutput.error_to_json)])
let update_glossary_term =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and glossaryIdentifier =
         flag "glossary-identifier" (optional string)
           ~doc:"STRING GlossaryTermId"
       and name =
         flag "name" (optional string) ~doc:"STRING GlossaryTermName"
       and shortDescription =
         flag "short-description" (optional string)
           ~doc:"STRING ShortDescription"
       and longDescription =
         flag "long-description" (optional string)
           ~doc:"STRING LongDescription"
       and termRelations =
         flag "term-relations" (optional json_arg) ~doc:"JSON TermRelations"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON GlossaryTermStatus"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING GlossaryTermId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_glossary_term
           (Values.UpdateGlossaryTermInput.make ?glossaryIdentifier ?name
              ?shortDescription ?longDescription
              ?termRelations:(Option.map ~f:Values.TermRelations.of_json
                                termRelations)
              ?status:(Option.map ~f:Values.GlossaryTermStatus.of_json status)
              ~domainIdentifier ~identifier ())
           (Some Values.UpdateGlossaryTermOutput.to_json)
           (Some Values.UpdateGlossaryTermOutput.error_to_json)])
let update_group_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and groupIdentifier =
         flag "group-identifier" (required string)
           ~doc:"STRING GroupIdentifier"
       and status =
         flag "status" (required json_arg) ~doc:"JSON GroupProfileStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_group_profile
           (Values.UpdateGroupProfileInput.make ~domainIdentifier
              ~groupIdentifier
              ~status:(Values.GroupProfileStatus.of_json status) ())
           (Some Values.UpdateGroupProfileOutput.to_json)
           (Some Values.UpdateGroupProfileOutput.error_to_json)])
let update_notebook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON NotebookStatus"
       and name = flag "name" (optional string) ~doc:"STRING NotebookName"
       and cellOrder =
         flag "cell-order" (optional json_arg) ~doc:"JSON CellOrder"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON Metadata"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and environmentConfiguration =
         flag "environment-configuration" (optional json_arg)
           ~doc:"JSON EnvironmentConfig"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING NotebookId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_notebook
           (Values.UpdateNotebookInput.make ?description
              ?status:(Option.map ~f:Values.NotebookStatus.of_json status)
              ?name
              ?cellOrder:(Option.map ~f:Values.CellOrder.of_json cellOrder)
              ?metadata:(Option.map ~f:Values.Metadata.of_json metadata)
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?environmentConfiguration:(Option.map
                                           ~f:Values.EnvironmentConfig.of_json
                                           environmentConfiguration)
              ?clientToken ~domainIdentifier ~identifier ())
           (Some Values.UpdateNotebookOutput.to_json)
           (Some Values.UpdateNotebookOutput.error_to_json)])
let update_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ProjectName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and resourceTags =
         flag "resource-tags" (optional json_arg)
           ~doc:"JSON UpdateProjectInputResourceTagsMap"
       and glossaryTerms =
         flag "glossary-terms" (optional json_arg) ~doc:"JSON GlossaryTerms"
       and domainUnitId =
         flag "domain-unit-id" (optional string) ~doc:"STRING DomainUnitId"
       and environmentDeploymentDetails =
         flag "environment-deployment-details" (optional json_arg)
           ~doc:"JSON EnvironmentDeploymentDetails"
       and userParameters =
         flag "user-parameters" (optional json_arg)
           ~doc:"JSON EnvironmentConfigurationUserParametersList"
       and projectProfileVersion =
         flag "project-profile-version" (optional string)
           ~doc:"STRING String"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_project
           (Values.UpdateProjectInput.make ?name ?description
              ?resourceTags:(Option.map
                               ~f:Values.UpdateProjectInputResourceTagsMap.of_json
                               resourceTags)
              ?glossaryTerms:(Option.map ~f:Values.GlossaryTerms.of_json
                                glossaryTerms) ?domainUnitId
              ?environmentDeploymentDetails:(Option.map
                                               ~f:Values.EnvironmentDeploymentDetails.of_json
                                               environmentDeploymentDetails)
              ?userParameters:(Option.map
                                 ~f:Values.EnvironmentConfigurationUserParametersList.of_json
                                 userParameters) ?projectProfileVersion
              ~domainIdentifier ~identifier ())
           (Some Values.UpdateProjectOutput.to_json)
           (Some Values.UpdateProjectOutput.error_to_json)])
let update_project_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING ProjectProfileName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and status = flag "status" (optional json_arg) ~doc:"JSON Status"
       and projectResourceTags =
         flag "project-resource-tags" (optional json_arg)
           ~doc:"JSON ProjectResourceTagParameters"
       and allowCustomProjectResourceTags =
         flag "allow-custom-project-resource-tags" (optional bool)
           ~doc:"BOOL Boolean"
       and projectResourceTagsDescription =
         flag "project-resource-tags-description" (optional string)
           ~doc:"STRING Description"
       and environmentConfigurations =
         flag "environment-configurations" (optional json_arg)
           ~doc:"JSON EnvironmentConfigurationsList"
       and domainUnitIdentifier =
         flag "domain-unit-identifier" (optional string)
           ~doc:"STRING DomainUnitId"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ProjectProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_project_profile
           (Values.UpdateProjectProfileInput.make ?name ?description
              ?status:(Option.map ~f:Values.Status.of_json status)
              ?projectResourceTags:(Option.map
                                      ~f:Values.ProjectResourceTagParameters.of_json
                                      projectResourceTags)
              ?allowCustomProjectResourceTags ?projectResourceTagsDescription
              ?environmentConfigurations:(Option.map
                                            ~f:Values.EnvironmentConfigurationsList.of_json
                                            environmentConfigurations)
              ?domainUnitIdentifier ~domainIdentifier ~identifier ())
           (Some Values.UpdateProjectProfileOutput.to_json)
           (Some Values.UpdateProjectProfileOutput.error_to_json)])
let update_root_domain_unit_owner =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and currentOwner =
         flag "current-owner" (required string) ~doc:"STRING UserIdentifier"
       and newOwner = flag "new-owner" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_root_domain_unit_owner
           (Values.UpdateRootDomainUnitOwnerInput.make ?clientToken
              ~domainIdentifier ~currentOwner ~newOwner ())
           (Some Values.UpdateRootDomainUnitOwnerOutput.to_json)
           (Some Values.UpdateRootDomainUnitOwnerOutput.error_to_json)])
let update_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING RuleName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and scope = flag "scope" (optional json_arg) ~doc:"JSON RuleScope"
       and detail = flag "detail" (optional json_arg) ~doc:"JSON RuleDetail"
       and includeChildDomainUnits =
         flag "include-child-domain-units" (optional bool)
           ~doc:"BOOL Boolean"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING RuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_rule
           (Values.UpdateRuleInput.make ?name ?description
              ?scope:(Option.map ~f:Values.RuleScope.of_json scope)
              ?detail:(Option.map ~f:Values.RuleDetail.of_json detail)
              ?includeChildDomainUnits ~domainIdentifier ~identifier ())
           (Some Values.UpdateRuleOutput.to_json)
           (Some Values.UpdateRuleOutput.error_to_json)])
let update_subscription_grant_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and failureCause =
         flag "failure-cause" (optional json_arg) ~doc:"JSON FailureCause"
       and targetName =
         flag "target-name" (optional string) ~doc:"STRING String"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING SubscriptionGrantId"
       and assetIdentifier =
         flag "asset-identifier" (required string) ~doc:"STRING AssetId"
       and status =
         flag "status" (required json_arg)
           ~doc:"JSON SubscriptionGrantStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_subscription_grant_status
           (Values.UpdateSubscriptionGrantStatusInput.make
              ?failureCause:(Option.map ~f:Values.FailureCause.of_json
                               failureCause) ?targetName ~domainIdentifier
              ~identifier ~assetIdentifier
              ~status:(Values.SubscriptionGrantStatus.of_json status) ())
           (Some Values.UpdateSubscriptionGrantStatusOutput.to_json)
           (Some Values.UpdateSubscriptionGrantStatusOutput.error_to_json)])
let update_subscription_request =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING SubscriptionRequestId"
       and requestReason =
         flag "request-reason" (required string) ~doc:"STRING RequestReason" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_subscription_request
           (Values.UpdateSubscriptionRequestInput.make ~domainIdentifier
              ~identifier ~requestReason ())
           (Some Values.UpdateSubscriptionRequestOutput.to_json)
           (Some Values.UpdateSubscriptionRequestOutput.error_to_json)])
let update_subscription_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING SubscriptionTargetName"
       and authorizedPrincipals =
         flag "authorized-principals" (optional json_arg)
           ~doc:"JSON AuthorizedPrincipalIdentifiers"
       and applicableAssetTypes =
         flag "applicable-asset-types" (optional json_arg)
           ~doc:"JSON ApplicableAssetTypes"
       and subscriptionTargetConfig =
         flag "subscription-target-config" (optional json_arg)
           ~doc:"JSON SubscriptionTargetForms"
       and manageAccessRole =
         flag "manage-access-role" (optional string) ~doc:"STRING IamRoleArn"
       and provider = flag "provider" (optional string) ~doc:"STRING String"
       and subscriptionGrantCreationMode =
         flag "subscription-grant-creation-mode" (optional json_arg)
           ~doc:"JSON SubscriptionGrantCreationMode"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING SubscriptionTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_subscription_target
           (Values.UpdateSubscriptionTargetInput.make ?name
              ?authorizedPrincipals:(Option.map
                                       ~f:Values.AuthorizedPrincipalIdentifiers.of_json
                                       authorizedPrincipals)
              ?applicableAssetTypes:(Option.map
                                       ~f:Values.ApplicableAssetTypes.of_json
                                       applicableAssetTypes)
              ?subscriptionTargetConfig:(Option.map
                                           ~f:Values.SubscriptionTargetForms.of_json
                                           subscriptionTargetConfig)
              ?manageAccessRole ?provider
              ?subscriptionGrantCreationMode:(Option.map
                                                ~f:Values.SubscriptionGrantCreationMode.of_json
                                                subscriptionGrantCreationMode)
              ~domainIdentifier ~environmentIdentifier ~identifier ())
           (Some Values.UpdateSubscriptionTargetOutput.to_json)
           (Some Values.UpdateSubscriptionTargetOutput.error_to_json)])
let update_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON UserProfileType"
       and sessionName =
         flag "session-name" (optional string)
           ~doc:"STRING UpdateUserProfileInputSessionNameString"
       and domainIdentifier =
         flag "domain-identifier" (required string) ~doc:"STRING DomainId"
       and userIdentifier =
         flag "user-identifier" (required string)
           ~doc:"STRING UserIdentifier"
       and status =
         flag "status" (required json_arg) ~doc:"JSON UserProfileStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_profile
           (Values.UpdateUserProfileInput.make
              ?type_:(Option.map ~f:Values.UserProfileType.of_json type_)
              ?sessionName ~domainIdentifier ~userIdentifier
              ~status:(Values.UserProfileStatus.of_json status) ())
           (Some Values.UpdateUserProfileOutput.to_json)
           (Some Values.UpdateUserProfileOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-predictions", accept_predictions);
    ("accept-subscription-request", accept_subscription_request);
    ("add-entity-owner", add_entity_owner);
    ("add-policy-grant", add_policy_grant);
    ("associate-environment-role", associate_environment_role);
    ("associate-governed-terms", associate_governed_terms);
    ("batch-get-attributes-metadata", batch_get_attributes_metadata);
    ("batch-put-attributes-metadata", batch_put_attributes_metadata);
    ("cancel-metadata-generation-run", cancel_metadata_generation_run);
    ("cancel-subscription", cancel_subscription);
    ("create-account-pool", create_account_pool);
    ("create-asset", create_asset);
    ("create-asset-filter", create_asset_filter);
    ("create-asset-revision", create_asset_revision);
    ("create-asset-type", create_asset_type);
    ("create-connection", create_connection);
    ("create-data-product", create_data_product);
    ("create-data-product-revision", create_data_product_revision);
    ("create-data-source", create_data_source);
    ("create-domain", create_domain);
    ("create-domain-unit", create_domain_unit);
    ("create-environment", create_environment);
    ("create-environment-action", create_environment_action);
    ("create-environment-blueprint", create_environment_blueprint);
    ("create-environment-profile", create_environment_profile);
    ("create-form-type", create_form_type);
    ("create-glossary", create_glossary);
    ("create-glossary-term", create_glossary_term);
    ("create-group-profile", create_group_profile);
    ("create-listing-change-set", create_listing_change_set);
    ("create-notebook", create_notebook);
    ("create-project", create_project);
    ("create-project-membership", create_project_membership);
    ("create-project-profile", create_project_profile);
    ("create-rule", create_rule);
    ("create-subscription-grant", create_subscription_grant);
    ("create-subscription-request", create_subscription_request);
    ("create-subscription-target", create_subscription_target);
    ("create-user-profile", create_user_profile);
    ("delete-account-pool", delete_account_pool);
    ("delete-asset", delete_asset);
    ("delete-asset-filter", delete_asset_filter);
    ("delete-asset-type", delete_asset_type);
    ("delete-connection", delete_connection);
    ("delete-data-export-configuration", delete_data_export_configuration);
    ("delete-data-product", delete_data_product);
    ("delete-data-source", delete_data_source);
    ("delete-domain", delete_domain);
    ("delete-domain-unit", delete_domain_unit);
    ("delete-environment", delete_environment);
    ("delete-environment-action", delete_environment_action);
    ("delete-environment-blueprint", delete_environment_blueprint);
    ("delete-environment-blueprint-configuration",
      delete_environment_blueprint_configuration);
    ("delete-environment-profile", delete_environment_profile);
    ("delete-form-type", delete_form_type);
    ("delete-glossary", delete_glossary);
    ("delete-glossary-term", delete_glossary_term);
    ("delete-listing", delete_listing);
    ("delete-notebook", delete_notebook);
    ("delete-project", delete_project);
    ("delete-project-membership", delete_project_membership);
    ("delete-project-profile", delete_project_profile);
    ("delete-rule", delete_rule);
    ("delete-subscription-grant", delete_subscription_grant);
    ("delete-subscription-request", delete_subscription_request);
    ("delete-subscription-target", delete_subscription_target);
    ("delete-time-series-data-points", delete_time_series_data_points);
    ("disassociate-environment-role", disassociate_environment_role);
    ("disassociate-governed-terms", disassociate_governed_terms);
    ("get-account-pool", get_account_pool);
    ("get-asset", get_asset);
    ("get-asset-filter", get_asset_filter);
    ("get-asset-type", get_asset_type);
    ("get-connection", get_connection);
    ("get-data-export-configuration", get_data_export_configuration);
    ("get-data-product", get_data_product);
    ("get-data-source", get_data_source);
    ("get-data-source-run", get_data_source_run);
    ("get-domain", get_domain);
    ("get-domain-unit", get_domain_unit);
    ("get-environment", get_environment);
    ("get-environment-action", get_environment_action);
    ("get-environment-blueprint", get_environment_blueprint);
    ("get-environment-blueprint-configuration",
      get_environment_blueprint_configuration);
    ("get-environment-credentials", get_environment_credentials);
    ("get-environment-profile", get_environment_profile);
    ("get-form-type", get_form_type);
    ("get-glossary", get_glossary);
    ("get-glossary-term", get_glossary_term);
    ("get-group-profile", get_group_profile);
    ("get-iam-portal-login-url", get_iam_portal_login_url);
    ("get-job-run", get_job_run);
    ("get-lineage-event", get_lineage_event);
    ("get-lineage-node", get_lineage_node);
    ("get-listing", get_listing);
    ("get-metadata-generation-run", get_metadata_generation_run);
    ("get-notebook", get_notebook);
    ("get-notebook-export", get_notebook_export);
    ("get-notebook-run", get_notebook_run);
    ("get-project", get_project);
    ("get-project-profile", get_project_profile);
    ("get-rule", get_rule);
    ("get-subscription", get_subscription);
    ("get-subscription-grant", get_subscription_grant);
    ("get-subscription-request-details", get_subscription_request_details);
    ("get-subscription-target", get_subscription_target);
    ("get-time-series-data-point", get_time_series_data_point);
    ("get-user-profile", get_user_profile);
    ("list-account-pools", list_account_pools);
    ("list-accounts-in-account-pool", list_accounts_in_account_pool);
    ("list-asset-filters", list_asset_filters);
    ("list-asset-revisions", list_asset_revisions);
    ("list-connections", list_connections);
    ("list-data-product-revisions", list_data_product_revisions);
    ("list-data-source-run-activities", list_data_source_run_activities);
    ("list-data-source-runs", list_data_source_runs);
    ("list-data-sources", list_data_sources);
    ("list-domain-units-for-parent", list_domain_units_for_parent);
    ("list-domains", list_domains);
    ("list-entity-owners", list_entity_owners);
    ("list-environment-actions", list_environment_actions);
    ("list-environment-blueprint-configurations",
      list_environment_blueprint_configurations);
    ("list-environment-blueprints", list_environment_blueprints);
    ("list-environment-profiles", list_environment_profiles);
    ("list-environments", list_environments);
    ("list-job-runs", list_job_runs);
    ("list-lineage-events", list_lineage_events);
    ("list-lineage-node-history", list_lineage_node_history);
    ("list-metadata-generation-runs", list_metadata_generation_runs);
    ("list-notebook-runs", list_notebook_runs);
    ("list-notebooks", list_notebooks);
    ("list-notifications", list_notifications);
    ("list-policy-grants", list_policy_grants);
    ("list-project-memberships", list_project_memberships);
    ("list-project-profiles", list_project_profiles);
    ("list-projects", list_projects);
    ("list-rules", list_rules);
    ("list-subscription-grants", list_subscription_grants);
    ("list-subscription-requests", list_subscription_requests);
    ("list-subscription-targets", list_subscription_targets);
    ("list-subscriptions", list_subscriptions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-time-series-data-points", list_time_series_data_points);
    ("post-lineage-event", post_lineage_event);
    ("post-time-series-data-points", post_time_series_data_points);
    ("put-data-export-configuration", put_data_export_configuration);
    ("put-environment-blueprint-configuration",
      put_environment_blueprint_configuration);
    ("query-graph", query_graph);
    ("reject-predictions", reject_predictions);
    ("reject-subscription-request", reject_subscription_request);
    ("remove-entity-owner", remove_entity_owner);
    ("remove-policy-grant", remove_policy_grant);
    ("revoke-subscription", revoke_subscription);
    ("search", search);
    ("search-group-profiles", search_group_profiles);
    ("search-listings", search_listings);
    ("search-types", search_types);
    ("search-user-profiles", search_user_profiles);
    ("start-data-source-run", start_data_source_run);
    ("start-metadata-generation-run", start_metadata_generation_run);
    ("start-notebook-export", start_notebook_export);
    ("start-notebook-import", start_notebook_import);
    ("start-notebook-run", start_notebook_run);
    ("stop-notebook-run", stop_notebook_run);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-account-pool", update_account_pool);
    ("update-asset-filter", update_asset_filter);
    ("update-connection", update_connection);
    ("update-data-source", update_data_source);
    ("update-domain", update_domain);
    ("update-domain-unit", update_domain_unit);
    ("update-environment", update_environment);
    ("update-environment-action", update_environment_action);
    ("update-environment-blueprint", update_environment_blueprint);
    ("update-environment-profile", update_environment_profile);
    ("update-glossary", update_glossary);
    ("update-glossary-term", update_glossary_term);
    ("update-group-profile", update_group_profile);
    ("update-notebook", update_notebook);
    ("update-project", update_project);
    ("update-project-profile", update_project_profile);
    ("update-root-domain-unit-owner", update_root_domain_unit_owner);
    ("update-rule", update_rule);
    ("update-subscription-grant-status", update_subscription_grant_status);
    ("update-subscription-request", update_subscription_request);
    ("update-subscription-target", update_subscription_target);
    ("update-user-profile", update_user_profile)]
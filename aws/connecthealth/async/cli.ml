(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let activate_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and subscriptionId =
         flag "subscription-id" (required string)
           ~doc:"STRING SubscriptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.activate_subscription
           (Values.ActivateSubscriptionInput.make ~domainId ~subscriptionId
              ()) (Some Values.ActivateSubscriptionOutput.to_json)
           (Some Values.ActivateSubscriptionOutput.error_to_json)])
let create_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and webAppSetupConfiguration =
         flag "web-app-setup-configuration" (optional json_arg)
           ~doc:"JSON CreateWebAppConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain
           (Values.CreateDomainInput.make ?kmsKeyArn
              ?webAppSetupConfiguration:(Option.map
                                           ~f:Values.CreateWebAppConfiguration.of_json
                                           webAppSetupConfiguration)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name ())
           (Some Values.CreateDomainOutput.to_json)
           (Some Values.CreateDomainOutput.error_to_json)])
let create_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subscription
           (Values.CreateSubscriptionInput.make ~domainId ())
           (Some Values.CreateSubscriptionOutput.to_json)
           (Some Values.CreateSubscriptionOutput.error_to_json)])
let deactivate_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and subscriptionId =
         flag "subscription-id" (required string)
           ~doc:"STRING SubscriptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deactivate_subscription
           (Values.DeactivateSubscriptionInput.make ~domainId ~subscriptionId
              ()) (Some Values.DeactivateSubscriptionOutput.to_json)
           (Some Values.DeactivateSubscriptionOutput.error_to_json)])
let delete_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain (Values.DeleteDomainInput.make ~domainId ())
           (Some Values.DeleteDomainOutput.to_json)
           (Some Values.DeleteDomainOutput.error_to_json)])
let get_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain (Values.GetDomainInput.make ~domainId ())
           (Some Values.GetDomainOutput.to_json)
           (Some Values.GetDomainOutput.error_to_json)])
let get_medical_scribe_listening_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING SessionId"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and subscriptionId =
         flag "subscription-id" (required string)
           ~doc:"STRING SubscriptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_medical_scribe_listening_session
           (Values.GetMedicalScribeListeningSessionInput.make ~sessionId
              ~domainId ~subscriptionId ())
           (Some Values.GetMedicalScribeListeningSessionOutput.to_json)
           (Some Values.GetMedicalScribeListeningSessionOutput.error_to_json)])
let get_patient_insights_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_patient_insights_job
           (Values.GetPatientInsightsJobRequest.make ~domainId ~jobId ())
           (Some Values.GetPatientInsightsJobResponse.to_json)
           (Some Values.GetPatientInsightsJobResponse.error_to_json)])
let get_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and subscriptionId =
         flag "subscription-id" (required string)
           ~doc:"STRING SubscriptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_subscription
           (Values.GetSubscriptionInput.make ~domainId ~subscriptionId ())
           (Some Values.GetSubscriptionOutput.to_json)
           (Some Values.GetSubscriptionOutput.error_to_json)])
let list_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON DomainStatus"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListDomainsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domains
           (Values.ListDomainsInput.make
              ?status:(Option.map ~f:Values.DomainStatus.of_json status)
              ?maxResults ?nextToken ())
           (Some Values.ListDomainsOutput.to_json)
           (Some Values.ListDomainsOutput.error_to_json)])
let list_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSubscriptionsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_subscriptions
           (Values.ListSubscriptionsInput.make ?maxResults ?nextToken
              ~domainId ()) (Some Values.ListSubscriptionsOutput.to_json)
           (Some Values.ListSubscriptionsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let start_medical_scribe_listening_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputStream =
         flag "input-stream" (optional json_arg)
           ~doc:"JSON MedicalScribeInputStream"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING SessionId"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and subscriptionId =
         flag "subscription-id" (required string)
           ~doc:"STRING SubscriptionId"
       and languageCode =
         flag "language-code" (required json_arg)
           ~doc:"JSON MedicalScribeLanguageCode"
       and mediaSampleRateHertz =
         flag "media-sample-rate-hertz" (required int)
           ~doc:"INT MedicalScribeMediaSampleRateHertz"
       and mediaEncoding =
         flag "media-encoding" (required json_arg)
           ~doc:"JSON MedicalScribeMediaEncoding" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_medical_scribe_listening_session
           (Values.StartMedicalScribeListeningSessionInput.make
              ?inputStream:(Option.map
                              ~f:Values.MedicalScribeInputStream.of_json
                              inputStream) ~sessionId ~domainId
              ~subscriptionId
              ~languageCode:(Values.MedicalScribeLanguageCode.of_json
                               languageCode) ~mediaSampleRateHertz
              ~mediaEncoding:(Values.MedicalScribeMediaEncoding.of_json
                                mediaEncoding) ())
           (Some Values.StartMedicalScribeListeningSessionOutput.to_json)
           (Some
              Values.StartMedicalScribeListeningSessionOutput.error_to_json)])
let start_patient_insights_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING NonEmptyString"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and patientContext =
         flag "patient-context" (required json_arg)
           ~doc:"JSON PatientInsightsPatientContext"
       and insightsContext =
         flag "insights-context" (required json_arg)
           ~doc:"JSON InsightsContext"
       and encounterContext =
         flag "encounter-context" (required json_arg)
           ~doc:"JSON PatientInsightsEncounterContext"
       and userContext =
         flag "user-context" (required json_arg) ~doc:"JSON UserContext"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_patient_insights_job
           (Values.StartPatientInsightsJobRequest.make ?clientToken ~domainId
              ~patientContext:(Values.PatientInsightsPatientContext.of_json
                                 patientContext)
              ~insightsContext:(Values.InsightsContext.of_json
                                  insightsContext)
              ~encounterContext:(Values.PatientInsightsEncounterContext.of_json
                                   encounterContext)
              ~userContext:(Values.UserContext.of_json userContext)
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ())
           (Some Values.StartPatientInsightsJobResponse.to_json)
           (Some Values.StartPatientInsightsJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("activate-subscription", activate_subscription);
    ("create-domain", create_domain);
    ("create-subscription", create_subscription);
    ("deactivate-subscription", deactivate_subscription);
    ("delete-domain", delete_domain);
    ("get-domain", get_domain);
    ("get-medical-scribe-listening-session",
      get_medical_scribe_listening_session);
    ("get-patient-insights-job", get_patient_insights_job);
    ("get-subscription", get_subscription);
    ("list-domains", list_domains);
    ("list-subscriptions", list_subscriptions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-medical-scribe-listening-session",
      start_medical_scribe_listening_session);
    ("start-patient-insights-job", start_patient_insights_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
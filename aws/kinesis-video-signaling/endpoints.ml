(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | GetIceServerConfig: (GetIceServerConfigRequest.t,
  GetIceServerConfigResponse.t, GetIceServerConfigResponse.error) t 
  | SendAlexaOfferToMaster: (SendAlexaOfferToMasterRequest.t,
  SendAlexaOfferToMasterResponse.t, SendAlexaOfferToMasterResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function | GetIceServerConfig -> `POST | SendAlexaOfferToMaster -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | GetIceServerConfig ->
          (Format.kasprintf Uri.of_string) "/v1/get-ice-server-config"
      | SendAlexaOfferToMaster ->
          (Format.kasprintf Uri.of_string) "/v1/send-alexa-offer-to-master")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | GetIceServerConfig ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ChannelARN",
                           (ResourceARN.to_value
                              req.GetIceServerConfigRequest.channelARN));
                      Option.map req.GetIceServerConfigRequest.clientId
                        ~f:(fun x -> ("ClientId", (ClientId.to_value x)));
                      Option.map req.GetIceServerConfigRequest.service
                        ~f:(fun x -> ("Service", (Service.to_value x)));
                      Option.map req.GetIceServerConfigRequest.username
                        ~f:(fun x -> ("Username", (Username.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SendAlexaOfferToMaster ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ChannelARN",
                           (ResourceARN.to_value
                              req.SendAlexaOfferToMasterRequest.channelARN));
                      Some
                        ("SenderClientId",
                          (ClientId.to_value
                             req.SendAlexaOfferToMasterRequest.senderClientId));
                      Some
                        ("MessagePayload",
                          (MessagePayload.to_value
                             req.SendAlexaOfferToMasterRequest.messagePayload))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : (Awso.Http.Response.t, Awso.Http.Io.Error.call) result) :
  (o, [ `AWS of e  | `Transport of Awso.Http.Io.Error.call ]) result=
  let handle_error err error_of_json =
    match err with
    | `Too_many_redirects -> Error (`Transport `Too_many_redirects)
    | `Bad_response
        { Awso.Http.Io.Error.code = code; body; x_amzn_error_type } ->
        let generic_error () =
          Error
            (`Transport
               (`Bad_response
                  { Awso.Http.Io.Error.code = code; body; x_amzn_error_type })) in
        (match (x_amzn_error_type, error_of_json,
                 ((code >= 400) && (code <= 599)))
         with
         | (Some error_type, Some error_of_json, true) ->
             let json = Yojson.Safe.from_string body in
             Error (`AWS (error_of_json error_type json))
         | (None, Some error_of_json, true) ->
             (try
                let json = Yojson.Safe.from_string body in
                match json |> (Yojson.Safe.Util.member "__type") with
                | `String error_type ->
                    let error_type =
                      match String.lsplit2 error_type ~on:'#' with
                      | Some (_, s) -> s
                      | None -> error_type in
                    Error (`AWS (error_of_json error_type json))
                | `Null -> generic_error ()
                | _ ->
                    failwithf "Error '__type' did not have string type: %s"
                      body ()
              with | _ -> generic_error ())
         | (None, _, _) | (_, None, _) | (_, _, false) -> generic_error ()) in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = resp in
  let _ = handle_error in
  let _ = response_to_json in
  match endpoint with
  | GetIceServerConfig ->
      (match resp with
       | Error err ->
           handle_error err (Some GetIceServerConfigResponse.error_of_json)
       | Ok resp ->
           Ok (GetIceServerConfigResponse.of_json (response_to_json resp)))
  | SendAlexaOfferToMaster ->
      (match resp with
       | Error err ->
           handle_error err
             (Some SendAlexaOfferToMasterResponse.error_of_json)
       | Ok resp ->
           Ok
             (SendAlexaOfferToMasterResponse.of_json (response_to_json resp)))
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_tags_to_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags_to_resource
           (Values.AddTagsToResourceMessage.make ~resourceName
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagListMessage.to_json)
           (Some Values.TagListMessage.error_to_json)])
let authorize_cache_security_group_ingress =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheSecurityGroupName =
         flag "cache-security-group-name" (required string)
           ~doc:"STRING String"
       and eC2SecurityGroupName =
         flag "e-c2-security-group-name" (required string)
           ~doc:"STRING String"
       and eC2SecurityGroupOwnerId =
         flag "e-c2-security-group-owner-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.authorize_cache_security_group_ingress
           (Values.AuthorizeCacheSecurityGroupIngressMessage.make
              ~cacheSecurityGroupName ~eC2SecurityGroupName
              ~eC2SecurityGroupOwnerId ())
           (Some Values.AuthorizeCacheSecurityGroupIngressResult.to_json)
           (Some
              Values.AuthorizeCacheSecurityGroupIngressResult.error_to_json)])
let batch_apply_update_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationGroupIds =
         flag "replication-group-ids" (optional json_arg)
           ~doc:"JSON ReplicationGroupIdList"
       and cacheClusterIds =
         flag "cache-cluster-ids" (optional json_arg)
           ~doc:"JSON CacheClusterIdList"
       and serviceUpdateName =
         flag "service-update-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_apply_update_action
           (Values.BatchApplyUpdateActionMessage.make
              ?replicationGroupIds:(Option.map
                                      ~f:Values.ReplicationGroupIdList.of_json
                                      replicationGroupIds)
              ?cacheClusterIds:(Option.map
                                  ~f:Values.CacheClusterIdList.of_json
                                  cacheClusterIds) ~serviceUpdateName ())
           (Some Values.UpdateActionResultsMessage.to_json)
           (Some Values.UpdateActionResultsMessage.error_to_json)])
let batch_stop_update_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationGroupIds =
         flag "replication-group-ids" (optional json_arg)
           ~doc:"JSON ReplicationGroupIdList"
       and cacheClusterIds =
         flag "cache-cluster-ids" (optional json_arg)
           ~doc:"JSON CacheClusterIdList"
       and serviceUpdateName =
         flag "service-update-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_stop_update_action
           (Values.BatchStopUpdateActionMessage.make
              ?replicationGroupIds:(Option.map
                                      ~f:Values.ReplicationGroupIdList.of_json
                                      replicationGroupIds)
              ?cacheClusterIds:(Option.map
                                  ~f:Values.CacheClusterIdList.of_json
                                  cacheClusterIds) ~serviceUpdateName ())
           (Some Values.UpdateActionResultsMessage.to_json)
           (Some Values.UpdateActionResultsMessage.error_to_json)])
let complete_migration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and replicationGroupId =
         flag "replication-group-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.complete_migration
           (Values.CompleteMigrationMessage.make ?force ~replicationGroupId
              ()) (Some Values.CompleteMigrationResponse.to_json)
           (Some Values.CompleteMigrationResponse.error_to_json)])
let copy_serverless_cache_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and sourceServerlessCacheSnapshotName =
         flag "source-serverless-cache-snapshot-name" (required string)
           ~doc:"STRING String"
       and targetServerlessCacheSnapshotName =
         flag "target-serverless-cache-snapshot-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_serverless_cache_snapshot
           (Values.CopyServerlessCacheSnapshotRequest.make ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~sourceServerlessCacheSnapshotName
              ~targetServerlessCacheSnapshotName ())
           (Some Values.CopyServerlessCacheSnapshotResponse.to_json)
           (Some Values.CopyServerlessCacheSnapshotResponse.error_to_json)])
let copy_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetBucket =
         flag "target-bucket" (optional string) ~doc:"STRING String"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and sourceSnapshotName =
         flag "source-snapshot-name" (required string) ~doc:"STRING String"
       and targetSnapshotName =
         flag "target-snapshot-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_snapshot
           (Values.CopySnapshotMessage.make ?targetBucket ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~sourceSnapshotName ~targetSnapshotName ())
           (Some Values.CopySnapshotResult.to_json)
           (Some Values.CopySnapshotResult.error_to_json)])
let create_cache_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationGroupId =
         flag "replication-group-id" (optional string) ~doc:"STRING String"
       and aZMode = flag "a-z-mode" (optional json_arg) ~doc:"JSON AZMode"
       and preferredAvailabilityZone =
         flag "preferred-availability-zone" (optional string)
           ~doc:"STRING String"
       and preferredAvailabilityZones =
         flag "preferred-availability-zones" (optional json_arg)
           ~doc:"JSON PreferredAvailabilityZoneList"
       and numCacheNodes =
         flag "num-cache-nodes" (optional int) ~doc:"INT IntegerOptional"
       and cacheNodeType =
         flag "cache-node-type" (optional string) ~doc:"STRING String"
       and engine = flag "engine" (optional string) ~doc:"STRING String"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and cacheParameterGroupName =
         flag "cache-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and cacheSubnetGroupName =
         flag "cache-subnet-group-name" (optional string)
           ~doc:"STRING String"
       and cacheSecurityGroupNames =
         flag "cache-security-group-names" (optional json_arg)
           ~doc:"JSON CacheSecurityGroupNameList"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIdsList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and snapshotArns =
         flag "snapshot-arns" (optional json_arg)
           ~doc:"JSON SnapshotArnsList"
       and snapshotName =
         flag "snapshot-name" (optional string) ~doc:"STRING String"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and notificationTopicArn =
         flag "notification-topic-arn" (optional string) ~doc:"STRING String"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and snapshotRetentionLimit =
         flag "snapshot-retention-limit" (optional int)
           ~doc:"INT IntegerOptional"
       and snapshotWindow =
         flag "snapshot-window" (optional string) ~doc:"STRING String"
       and authToken =
         flag "auth-token" (optional string) ~doc:"STRING String"
       and outpostMode =
         flag "outpost-mode" (optional json_arg) ~doc:"JSON OutpostMode"
       and preferredOutpostArn =
         flag "preferred-outpost-arn" (optional string) ~doc:"STRING String"
       and preferredOutpostArns =
         flag "preferred-outpost-arns" (optional json_arg)
           ~doc:"JSON PreferredOutpostArnList"
       and logDeliveryConfigurations =
         flag "log-delivery-configurations" (optional json_arg)
           ~doc:"JSON LogDeliveryConfigurationRequestList"
       and transitEncryptionEnabled =
         flag "transit-encryption-enabled" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and networkType =
         flag "network-type" (optional json_arg) ~doc:"JSON NetworkType"
       and ipDiscovery =
         flag "ip-discovery" (optional json_arg) ~doc:"JSON IpDiscovery"
       and cacheClusterId =
         flag "cache-cluster-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cache_cluster
           (Values.CreateCacheClusterMessage.make ?replicationGroupId
              ?aZMode:(Option.map ~f:Values.AZMode.of_json aZMode)
              ?preferredAvailabilityZone
              ?preferredAvailabilityZones:(Option.map
                                             ~f:Values.PreferredAvailabilityZoneList.of_json
                                             preferredAvailabilityZones)
              ?numCacheNodes ?cacheNodeType ?engine ?engineVersion
              ?cacheParameterGroupName ?cacheSubnetGroupName
              ?cacheSecurityGroupNames:(Option.map
                                          ~f:Values.CacheSecurityGroupNameList.of_json
                                          cacheSecurityGroupNames)
              ?securityGroupIds:(Option.map
                                   ~f:Values.SecurityGroupIdsList.of_json
                                   securityGroupIds)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?snapshotArns:(Option.map ~f:Values.SnapshotArnsList.of_json
                               snapshotArns) ?snapshotName
              ?preferredMaintenanceWindow ?port ?notificationTopicArn
              ?autoMinorVersionUpgrade ?snapshotRetentionLimit
              ?snapshotWindow ?authToken
              ?outpostMode:(Option.map ~f:Values.OutpostMode.of_json
                              outpostMode) ?preferredOutpostArn
              ?preferredOutpostArns:(Option.map
                                       ~f:Values.PreferredOutpostArnList.of_json
                                       preferredOutpostArns)
              ?logDeliveryConfigurations:(Option.map
                                            ~f:Values.LogDeliveryConfigurationRequestList.of_json
                                            logDeliveryConfigurations)
              ?transitEncryptionEnabled
              ?networkType:(Option.map ~f:Values.NetworkType.of_json
                              networkType)
              ?ipDiscovery:(Option.map ~f:Values.IpDiscovery.of_json
                              ipDiscovery) ~cacheClusterId ())
           (Some Values.CreateCacheClusterResult.to_json)
           (Some Values.CreateCacheClusterResult.error_to_json)])
let create_cache_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and cacheParameterGroupName =
         flag "cache-parameter-group-name" (required string)
           ~doc:"STRING String"
       and cacheParameterGroupFamily =
         flag "cache-parameter-group-family" (required string)
           ~doc:"STRING String"
       and description =
         flag "description" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cache_parameter_group
           (Values.CreateCacheParameterGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~cacheParameterGroupName ~cacheParameterGroupFamily
              ~description ())
           (Some Values.CreateCacheParameterGroupResult.to_json)
           (Some Values.CreateCacheParameterGroupResult.error_to_json)])
let create_cache_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and cacheSecurityGroupName =
         flag "cache-security-group-name" (required string)
           ~doc:"STRING String"
       and description =
         flag "description" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cache_security_group
           (Values.CreateCacheSecurityGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~cacheSecurityGroupName ~description ())
           (Some Values.CreateCacheSecurityGroupResult.to_json)
           (Some Values.CreateCacheSecurityGroupResult.error_to_json)])
let create_cache_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and cacheSubnetGroupName =
         flag "cache-subnet-group-name" (required string)
           ~doc:"STRING String"
       and cacheSubnetGroupDescription =
         flag "cache-subnet-group-description" (required string)
           ~doc:"STRING String"
       and subnetIds =
         flag "subnet-ids" (required json_arg)
           ~doc:"JSON SubnetIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cache_subnet_group
           (Values.CreateCacheSubnetGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~cacheSubnetGroupName ~cacheSubnetGroupDescription
              ~subnetIds:(Values.SubnetIdentifierList.of_json subnetIds) ())
           (Some Values.CreateCacheSubnetGroupResult.to_json)
           (Some Values.CreateCacheSubnetGroupResult.error_to_json)])
let create_global_replication_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalReplicationGroupDescription =
         flag "global-replication-group-description" (optional string)
           ~doc:"STRING String"
       and globalReplicationGroupIdSuffix =
         flag "global-replication-group-id-suffix" (required string)
           ~doc:"STRING String"
       and primaryReplicationGroupId =
         flag "primary-replication-group-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_global_replication_group
           (Values.CreateGlobalReplicationGroupMessage.make
              ?globalReplicationGroupDescription
              ~globalReplicationGroupIdSuffix ~primaryReplicationGroupId ())
           (Some Values.CreateGlobalReplicationGroupResult.to_json)
           (Some Values.CreateGlobalReplicationGroupResult.error_to_json)])
let create_replication_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalReplicationGroupId =
         flag "global-replication-group-id" (optional string)
           ~doc:"STRING String"
       and primaryClusterId =
         flag "primary-cluster-id" (optional string) ~doc:"STRING String"
       and automaticFailoverEnabled =
         flag "automatic-failover-enabled" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and multiAZEnabled =
         flag "multi-a-z-enabled" (optional bool) ~doc:"BOOL BooleanOptional"
       and numCacheClusters =
         flag "num-cache-clusters" (optional int) ~doc:"INT IntegerOptional"
       and preferredCacheClusterAZs =
         flag "preferred-cache-cluster-a-zs" (optional json_arg)
           ~doc:"JSON AvailabilityZonesList"
       and numNodeGroups =
         flag "num-node-groups" (optional int) ~doc:"INT IntegerOptional"
       and replicasPerNodeGroup =
         flag "replicas-per-node-group" (optional int)
           ~doc:"INT IntegerOptional"
       and nodeGroupConfiguration =
         flag "node-group-configuration" (optional json_arg)
           ~doc:"JSON NodeGroupConfigurationList"
       and cacheNodeType =
         flag "cache-node-type" (optional string) ~doc:"STRING String"
       and engine = flag "engine" (optional string) ~doc:"STRING String"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and cacheParameterGroupName =
         flag "cache-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and cacheSubnetGroupName =
         flag "cache-subnet-group-name" (optional string)
           ~doc:"STRING String"
       and cacheSecurityGroupNames =
         flag "cache-security-group-names" (optional json_arg)
           ~doc:"JSON CacheSecurityGroupNameList"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIdsList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and snapshotArns =
         flag "snapshot-arns" (optional json_arg)
           ~doc:"JSON SnapshotArnsList"
       and snapshotName =
         flag "snapshot-name" (optional string) ~doc:"STRING String"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and notificationTopicArn =
         flag "notification-topic-arn" (optional string) ~doc:"STRING String"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and snapshotRetentionLimit =
         flag "snapshot-retention-limit" (optional int)
           ~doc:"INT IntegerOptional"
       and snapshotWindow =
         flag "snapshot-window" (optional string) ~doc:"STRING String"
       and authToken =
         flag "auth-token" (optional string) ~doc:"STRING String"
       and transitEncryptionEnabled =
         flag "transit-encryption-enabled" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and atRestEncryptionEnabled =
         flag "at-rest-encryption-enabled" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and userGroupIds =
         flag "user-group-ids" (optional json_arg)
           ~doc:"JSON UserGroupIdListInput"
       and logDeliveryConfigurations =
         flag "log-delivery-configurations" (optional json_arg)
           ~doc:"JSON LogDeliveryConfigurationRequestList"
       and dataTieringEnabled =
         flag "data-tiering-enabled" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and networkType =
         flag "network-type" (optional json_arg) ~doc:"JSON NetworkType"
       and ipDiscovery =
         flag "ip-discovery" (optional json_arg) ~doc:"JSON IpDiscovery"
       and transitEncryptionMode =
         flag "transit-encryption-mode" (optional json_arg)
           ~doc:"JSON TransitEncryptionMode"
       and clusterMode =
         flag "cluster-mode" (optional json_arg) ~doc:"JSON ClusterMode"
       and serverlessCacheSnapshotName =
         flag "serverless-cache-snapshot-name" (optional string)
           ~doc:"STRING String"
       and replicationGroupId =
         flag "replication-group-id" (required string) ~doc:"STRING String"
       and replicationGroupDescription =
         flag "replication-group-description" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_replication_group
           (Values.CreateReplicationGroupMessage.make
              ?globalReplicationGroupId ?primaryClusterId
              ?automaticFailoverEnabled ?multiAZEnabled ?numCacheClusters
              ?preferredCacheClusterAZs:(Option.map
                                           ~f:Values.AvailabilityZonesList.of_json
                                           preferredCacheClusterAZs)
              ?numNodeGroups ?replicasPerNodeGroup
              ?nodeGroupConfiguration:(Option.map
                                         ~f:Values.NodeGroupConfigurationList.of_json
                                         nodeGroupConfiguration)
              ?cacheNodeType ?engine ?engineVersion ?cacheParameterGroupName
              ?cacheSubnetGroupName
              ?cacheSecurityGroupNames:(Option.map
                                          ~f:Values.CacheSecurityGroupNameList.of_json
                                          cacheSecurityGroupNames)
              ?securityGroupIds:(Option.map
                                   ~f:Values.SecurityGroupIdsList.of_json
                                   securityGroupIds)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?snapshotArns:(Option.map ~f:Values.SnapshotArnsList.of_json
                               snapshotArns) ?snapshotName
              ?preferredMaintenanceWindow ?port ?notificationTopicArn
              ?autoMinorVersionUpgrade ?snapshotRetentionLimit
              ?snapshotWindow ?authToken ?transitEncryptionEnabled
              ?atRestEncryptionEnabled ?kmsKeyId
              ?userGroupIds:(Option.map
                               ~f:Values.UserGroupIdListInput.of_json
                               userGroupIds)
              ?logDeliveryConfigurations:(Option.map
                                            ~f:Values.LogDeliveryConfigurationRequestList.of_json
                                            logDeliveryConfigurations)
              ?dataTieringEnabled
              ?networkType:(Option.map ~f:Values.NetworkType.of_json
                              networkType)
              ?ipDiscovery:(Option.map ~f:Values.IpDiscovery.of_json
                              ipDiscovery)
              ?transitEncryptionMode:(Option.map
                                        ~f:Values.TransitEncryptionMode.of_json
                                        transitEncryptionMode)
              ?clusterMode:(Option.map ~f:Values.ClusterMode.of_json
                              clusterMode) ?serverlessCacheSnapshotName
              ~replicationGroupId ~replicationGroupDescription ())
           (Some Values.CreateReplicationGroupResult.to_json)
           (Some Values.CreateReplicationGroupResult.error_to_json)])
let create_serverless_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and majorEngineVersion =
         flag "major-engine-version" (optional string) ~doc:"STRING String"
       and cacheUsageLimits =
         flag "cache-usage-limits" (optional json_arg)
           ~doc:"JSON CacheUsageLimits"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIdsList"
       and snapshotArnsToRestore =
         flag "snapshot-arns-to-restore" (optional json_arg)
           ~doc:"JSON SnapshotArnsList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and userGroupId =
         flag "user-group-id" (optional string) ~doc:"STRING String"
       and subnetIds =
         flag "subnet-ids" (optional json_arg) ~doc:"JSON SubnetIdsList"
       and snapshotRetentionLimit =
         flag "snapshot-retention-limit" (optional int)
           ~doc:"INT IntegerOptional"
       and dailySnapshotTime =
         flag "daily-snapshot-time" (optional string) ~doc:"STRING String"
       and networkType =
         flag "network-type" (optional json_arg) ~doc:"JSON NetworkType"
       and serverlessCacheName =
         flag "serverless-cache-name" (required string) ~doc:"STRING String"
       and engine = flag "engine" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_serverless_cache
           (Values.CreateServerlessCacheRequest.make ?description
              ?majorEngineVersion
              ?cacheUsageLimits:(Option.map
                                   ~f:Values.CacheUsageLimits.of_json
                                   cacheUsageLimits) ?kmsKeyId
              ?securityGroupIds:(Option.map
                                   ~f:Values.SecurityGroupIdsList.of_json
                                   securityGroupIds)
              ?snapshotArnsToRestore:(Option.map
                                        ~f:Values.SnapshotArnsList.of_json
                                        snapshotArnsToRestore)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?userGroupId
              ?subnetIds:(Option.map ~f:Values.SubnetIdsList.of_json
                            subnetIds) ?snapshotRetentionLimit
              ?dailySnapshotTime
              ?networkType:(Option.map ~f:Values.NetworkType.of_json
                              networkType) ~serverlessCacheName ~engine ())
           (Some Values.CreateServerlessCacheResponse.to_json)
           (Some Values.CreateServerlessCacheResponse.error_to_json)])
let create_serverless_cache_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and serverlessCacheSnapshotName =
         flag "serverless-cache-snapshot-name" (required string)
           ~doc:"STRING String"
       and serverlessCacheName =
         flag "serverless-cache-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_serverless_cache_snapshot
           (Values.CreateServerlessCacheSnapshotRequest.make ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~serverlessCacheSnapshotName ~serverlessCacheName ())
           (Some Values.CreateServerlessCacheSnapshotResponse.to_json)
           (Some Values.CreateServerlessCacheSnapshotResponse.error_to_json)])
let create_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationGroupId =
         flag "replication-group-id" (optional string) ~doc:"STRING String"
       and cacheClusterId =
         flag "cache-cluster-id" (optional string) ~doc:"STRING String"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and snapshotName =
         flag "snapshot-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_snapshot
           (Values.CreateSnapshotMessage.make ?replicationGroupId
              ?cacheClusterId ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~snapshotName
              ()) (Some Values.CreateSnapshotResult.to_json)
           (Some Values.CreateSnapshotResult.error_to_json)])
let create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and passwords =
         flag "passwords" (optional json_arg) ~doc:"JSON PasswordListInput"
       and noPasswordRequired =
         flag "no-password-required" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and authenticationMode =
         flag "authentication-mode" (optional json_arg)
           ~doc:"JSON AuthenticationMode"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId"
       and userName =
         flag "user-name" (required string) ~doc:"STRING UserName"
       and engine = flag "engine" (required string) ~doc:"STRING EngineType"
       and accessString =
         flag "access-string" (required string) ~doc:"STRING AccessString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user
           (Values.CreateUserMessage.make
              ?passwords:(Option.map ~f:Values.PasswordListInput.of_json
                            passwords) ?noPasswordRequired
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?authenticationMode:(Option.map
                                     ~f:Values.AuthenticationMode.of_json
                                     authenticationMode) ~userId ~userName
              ~engine ~accessString ()) (Some Values.User.to_json)
           (Some Values.User.error_to_json)])
let create_user_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userIds =
         flag "user-ids" (optional json_arg) ~doc:"JSON UserIdListInput"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and userGroupId =
         flag "user-group-id" (required string) ~doc:"STRING String"
       and engine = flag "engine" (required string) ~doc:"STRING EngineType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user_group
           (Values.CreateUserGroupMessage.make
              ?userIds:(Option.map ~f:Values.UserIdListInput.of_json userIds)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~userGroupId
              ~engine ()) (Some Values.UserGroup.to_json)
           (Some Values.UserGroup.error_to_json)])
let decrease_node_groups_in_global_replication_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNodeGroupsToRemove =
         flag "global-node-groups-to-remove" (optional json_arg)
           ~doc:"JSON GlobalNodeGroupIdList"
       and globalNodeGroupsToRetain =
         flag "global-node-groups-to-retain" (optional json_arg)
           ~doc:"JSON GlobalNodeGroupIdList"
       and globalReplicationGroupId =
         flag "global-replication-group-id" (required string)
           ~doc:"STRING String"
       and nodeGroupCount =
         flag "node-group-count" (required int) ~doc:"INT Integer"
       and applyImmediately =
         flag "apply-immediately" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.decrease_node_groups_in_global_replication_group
           (Values.DecreaseNodeGroupsInGlobalReplicationGroupMessage.make
              ?globalNodeGroupsToRemove:(Option.map
                                           ~f:Values.GlobalNodeGroupIdList.of_json
                                           globalNodeGroupsToRemove)
              ?globalNodeGroupsToRetain:(Option.map
                                           ~f:Values.GlobalNodeGroupIdList.of_json
                                           globalNodeGroupsToRetain)
              ~globalReplicationGroupId ~nodeGroupCount ~applyImmediately ())
           (Some
              Values.DecreaseNodeGroupsInGlobalReplicationGroupResult.to_json)
           (Some
              Values.DecreaseNodeGroupsInGlobalReplicationGroupResult.error_to_json)])
let decrease_replica_count =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and newReplicaCount =
         flag "new-replica-count" (optional int) ~doc:"INT IntegerOptional"
       and replicaConfiguration =
         flag "replica-configuration" (optional json_arg)
           ~doc:"JSON ReplicaConfigurationList"
       and replicasToRemove =
         flag "replicas-to-remove" (optional json_arg)
           ~doc:"JSON RemoveReplicasList"
       and replicationGroupId =
         flag "replication-group-id" (required string) ~doc:"STRING String"
       and applyImmediately =
         flag "apply-immediately" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.decrease_replica_count
           (Values.DecreaseReplicaCountMessage.make ?newReplicaCount
              ?replicaConfiguration:(Option.map
                                       ~f:Values.ReplicaConfigurationList.of_json
                                       replicaConfiguration)
              ?replicasToRemove:(Option.map
                                   ~f:Values.RemoveReplicasList.of_json
                                   replicasToRemove) ~replicationGroupId
              ~applyImmediately ())
           (Some Values.DecreaseReplicaCountResult.to_json)
           (Some Values.DecreaseReplicaCountResult.error_to_json)])
let delete_cache_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and finalSnapshotIdentifier =
         flag "final-snapshot-identifier" (optional string)
           ~doc:"STRING String"
       and cacheClusterId =
         flag "cache-cluster-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cache_cluster
           (Values.DeleteCacheClusterMessage.make ?finalSnapshotIdentifier
              ~cacheClusterId ())
           (Some Values.DeleteCacheClusterResult.to_json)
           (Some Values.DeleteCacheClusterResult.error_to_json)])
let delete_cache_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheParameterGroupName =
         flag "cache-parameter-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cache_parameter_group
           (Values.DeleteCacheParameterGroupMessage.make
              ~cacheParameterGroupName ()) None None])
let delete_cache_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheSecurityGroupName =
         flag "cache-security-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cache_security_group
           (Values.DeleteCacheSecurityGroupMessage.make
              ~cacheSecurityGroupName ()) None None])
let delete_cache_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheSubnetGroupName =
         flag "cache-subnet-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cache_subnet_group
           (Values.DeleteCacheSubnetGroupMessage.make ~cacheSubnetGroupName
              ()) None None])
let delete_global_replication_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalReplicationGroupId =
         flag "global-replication-group-id" (required string)
           ~doc:"STRING String"
       and retainPrimaryReplicationGroup =
         flag "retain-primary-replication-group" (required bool)
           ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_global_replication_group
           (Values.DeleteGlobalReplicationGroupMessage.make
              ~globalReplicationGroupId ~retainPrimaryReplicationGroup ())
           (Some Values.DeleteGlobalReplicationGroupResult.to_json)
           (Some Values.DeleteGlobalReplicationGroupResult.error_to_json)])
let delete_replication_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retainPrimaryCluster =
         flag "retain-primary-cluster" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and finalSnapshotIdentifier =
         flag "final-snapshot-identifier" (optional string)
           ~doc:"STRING String"
       and replicationGroupId =
         flag "replication-group-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_replication_group
           (Values.DeleteReplicationGroupMessage.make ?retainPrimaryCluster
              ?finalSnapshotIdentifier ~replicationGroupId ())
           (Some Values.DeleteReplicationGroupResult.to_json)
           (Some Values.DeleteReplicationGroupResult.error_to_json)])
let delete_serverless_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and finalSnapshotName =
         flag "final-snapshot-name" (optional string) ~doc:"STRING String"
       and serverlessCacheName =
         flag "serverless-cache-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_serverless_cache
           (Values.DeleteServerlessCacheRequest.make ?finalSnapshotName
              ~serverlessCacheName ())
           (Some Values.DeleteServerlessCacheResponse.to_json)
           (Some Values.DeleteServerlessCacheResponse.error_to_json)])
let delete_serverless_cache_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverlessCacheSnapshotName =
         flag "serverless-cache-snapshot-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_serverless_cache_snapshot
           (Values.DeleteServerlessCacheSnapshotRequest.make
              ~serverlessCacheSnapshotName ())
           (Some Values.DeleteServerlessCacheSnapshotResponse.to_json)
           (Some Values.DeleteServerlessCacheSnapshotResponse.error_to_json)])
let delete_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotName =
         flag "snapshot-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_snapshot
           (Values.DeleteSnapshotMessage.make ~snapshotName ())
           (Some Values.DeleteSnapshotResult.to_json)
           (Some Values.DeleteSnapshotResult.error_to_json)])
let delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user (Values.DeleteUserMessage.make ~userId ())
           (Some Values.User.to_json) (Some Values.User.error_to_json)])
let delete_user_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userGroupId =
         flag "user-group-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user_group
           (Values.DeleteUserGroupMessage.make ~userGroupId ())
           (Some Values.UserGroup.to_json)
           (Some Values.UserGroup.error_to_json)])
let describe_cache_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheClusterId =
         flag "cache-cluster-id" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and showCacheNodeInfo =
         flag "show-cache-node-info" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and showCacheClustersNotInReplicationGroups =
         flag "show-cache-clusters-not-in-replication-groups" (optional bool)
           ~doc:"BOOL BooleanOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cache_clusters
           (Values.DescribeCacheClustersMessage.make ?cacheClusterId
              ?maxRecords ?marker ?showCacheNodeInfo
              ?showCacheClustersNotInReplicationGroups ())
           (Some Values.CacheClusterMessage.to_json)
           (Some Values.CacheClusterMessage.error_to_json)])
let describe_cache_engine_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engine = flag "engine" (optional string) ~doc:"STRING String"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and cacheParameterGroupFamily =
         flag "cache-parameter-group-family" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and defaultOnly =
         flag "default-only" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cache_engine_versions
           (Values.DescribeCacheEngineVersionsMessage.make ?engine
              ?engineVersion ?cacheParameterGroupFamily ?maxRecords ?marker
              ?defaultOnly ())
           (Some Values.CacheEngineVersionMessage.to_json)
           (Some Values.CacheEngineVersionMessage.error_to_json)])
let describe_cache_parameter_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheParameterGroupName =
         flag "cache-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cache_parameter_groups
           (Values.DescribeCacheParameterGroupsMessage.make
              ?cacheParameterGroupName ?maxRecords ?marker ())
           (Some Values.CacheParameterGroupsMessage.to_json)
           (Some Values.CacheParameterGroupsMessage.error_to_json)])
let describe_cache_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and source = flag "source" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and cacheParameterGroupName =
         flag "cache-parameter-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cache_parameters
           (Values.DescribeCacheParametersMessage.make ?source ?maxRecords
              ?marker ~cacheParameterGroupName ())
           (Some Values.CacheParameterGroupDetails.to_json)
           (Some Values.CacheParameterGroupDetails.error_to_json)])
let describe_cache_security_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheSecurityGroupName =
         flag "cache-security-group-name" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cache_security_groups
           (Values.DescribeCacheSecurityGroupsMessage.make
              ?cacheSecurityGroupName ?maxRecords ?marker ())
           (Some Values.CacheSecurityGroupMessage.to_json)
           (Some Values.CacheSecurityGroupMessage.error_to_json)])
let describe_cache_subnet_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheSubnetGroupName =
         flag "cache-subnet-group-name" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cache_subnet_groups
           (Values.DescribeCacheSubnetGroupsMessage.make
              ?cacheSubnetGroupName ?maxRecords ?marker ())
           (Some Values.CacheSubnetGroupMessage.to_json)
           (Some Values.CacheSubnetGroupMessage.error_to_json)])
let describe_engine_default_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and cacheParameterGroupFamily =
         flag "cache-parameter-group-family" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_engine_default_parameters
           (Values.DescribeEngineDefaultParametersMessage.make ?maxRecords
              ?marker ~cacheParameterGroupFamily ())
           (Some Values.DescribeEngineDefaultParametersResult.to_json)
           (Some Values.DescribeEngineDefaultParametersResult.error_to_json)])
let describe_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceIdentifier =
         flag "source-identifier" (optional string) ~doc:"STRING String"
       and sourceType =
         flag "source-type" (optional json_arg) ~doc:"JSON SourceType"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TStamp"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON TStamp"
       and duration =
         flag "duration" (optional int) ~doc:"INT IntegerOptional"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_events
           (Values.DescribeEventsMessage.make ?sourceIdentifier
              ?sourceType:(Option.map ~f:Values.SourceType.of_json sourceType)
              ?startTime:(Option.map ~f:Values.TStamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.TStamp.of_json endTime)
              ?duration ?maxRecords ?marker ())
           (Some Values.EventsMessage.to_json)
           (Some Values.EventsMessage.error_to_json)])
let describe_global_replication_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalReplicationGroupId =
         flag "global-replication-group-id" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and showMemberInfo =
         flag "show-member-info" (optional bool) ~doc:"BOOL BooleanOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_global_replication_groups
           (Values.DescribeGlobalReplicationGroupsMessage.make
              ?globalReplicationGroupId ?maxRecords ?marker ?showMemberInfo
              ()) (Some Values.DescribeGlobalReplicationGroupsResult.to_json)
           (Some Values.DescribeGlobalReplicationGroupsResult.error_to_json)])
let describe_replication_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationGroupId =
         flag "replication-group-id" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_replication_groups
           (Values.DescribeReplicationGroupsMessage.make ?replicationGroupId
              ?maxRecords ?marker ())
           (Some Values.ReplicationGroupMessage.to_json)
           (Some Values.ReplicationGroupMessage.error_to_json)])
let describe_reserved_cache_nodes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedCacheNodeId =
         flag "reserved-cache-node-id" (optional string) ~doc:"STRING String"
       and reservedCacheNodesOfferingId =
         flag "reserved-cache-nodes-offering-id" (optional string)
           ~doc:"STRING String"
       and cacheNodeType =
         flag "cache-node-type" (optional string) ~doc:"STRING String"
       and duration = flag "duration" (optional string) ~doc:"STRING String"
       and productDescription =
         flag "product-description" (optional string) ~doc:"STRING String"
       and offeringType =
         flag "offering-type" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_cache_nodes
           (Values.DescribeReservedCacheNodesMessage.make
              ?reservedCacheNodeId ?reservedCacheNodesOfferingId
              ?cacheNodeType ?duration ?productDescription ?offeringType
              ?maxRecords ?marker ())
           (Some Values.ReservedCacheNodeMessage.to_json)
           (Some Values.ReservedCacheNodeMessage.error_to_json)])
let describe_reserved_cache_nodes_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedCacheNodesOfferingId =
         flag "reserved-cache-nodes-offering-id" (optional string)
           ~doc:"STRING String"
       and cacheNodeType =
         flag "cache-node-type" (optional string) ~doc:"STRING String"
       and duration = flag "duration" (optional string) ~doc:"STRING String"
       and productDescription =
         flag "product-description" (optional string) ~doc:"STRING String"
       and offeringType =
         flag "offering-type" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_cache_nodes_offerings
           (Values.DescribeReservedCacheNodesOfferingsMessage.make
              ?reservedCacheNodesOfferingId ?cacheNodeType ?duration
              ?productDescription ?offeringType ?maxRecords ?marker ())
           (Some Values.ReservedCacheNodesOfferingMessage.to_json)
           (Some Values.ReservedCacheNodesOfferingMessage.error_to_json)])
let describe_serverless_cache_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverlessCacheName =
         flag "serverless-cache-name" (optional string) ~doc:"STRING String"
       and serverlessCacheSnapshotName =
         flag "serverless-cache-snapshot-name" (optional string)
           ~doc:"STRING String"
       and snapshotType =
         flag "snapshot-type" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IntegerOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_serverless_cache_snapshots
           (Values.DescribeServerlessCacheSnapshotsRequest.make
              ?serverlessCacheName ?serverlessCacheSnapshotName ?snapshotType
              ?nextToken ?maxResults ())
           (Some Values.DescribeServerlessCacheSnapshotsResponse.to_json)
           (Some
              Values.DescribeServerlessCacheSnapshotsResponse.error_to_json)])
let describe_serverless_caches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverlessCacheName =
         flag "serverless-cache-name" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IntegerOptional"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_serverless_caches
           (Values.DescribeServerlessCachesRequest.make ?serverlessCacheName
              ?maxResults ?nextToken ())
           (Some Values.DescribeServerlessCachesResponse.to_json)
           (Some Values.DescribeServerlessCachesResponse.error_to_json)])
let describe_service_updates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceUpdateName =
         flag "service-update-name" (optional string) ~doc:"STRING String"
       and serviceUpdateStatus =
         flag "service-update-status" (optional json_arg)
           ~doc:"JSON ServiceUpdateStatusList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_service_updates
           (Values.DescribeServiceUpdatesMessage.make ?serviceUpdateName
              ?serviceUpdateStatus:(Option.map
                                      ~f:Values.ServiceUpdateStatusList.of_json
                                      serviceUpdateStatus) ?maxRecords
              ?marker ()) (Some Values.ServiceUpdatesMessage.to_json)
           (Some Values.ServiceUpdatesMessage.error_to_json)])
let describe_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationGroupId =
         flag "replication-group-id" (optional string) ~doc:"STRING String"
       and cacheClusterId =
         flag "cache-cluster-id" (optional string) ~doc:"STRING String"
       and snapshotName =
         flag "snapshot-name" (optional string) ~doc:"STRING String"
       and snapshotSource =
         flag "snapshot-source" (optional string) ~doc:"STRING String"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and showNodeGroupConfig =
         flag "show-node-group-config" (optional bool)
           ~doc:"BOOL BooleanOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_snapshots
           (Values.DescribeSnapshotsMessage.make ?replicationGroupId
              ?cacheClusterId ?snapshotName ?snapshotSource ?marker
              ?maxRecords ?showNodeGroupConfig ())
           (Some Values.DescribeSnapshotsListMessage.to_json)
           (Some Values.DescribeSnapshotsListMessage.error_to_json)])
let describe_update_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceUpdateName =
         flag "service-update-name" (optional string) ~doc:"STRING String"
       and replicationGroupIds =
         flag "replication-group-ids" (optional json_arg)
           ~doc:"JSON ReplicationGroupIdList"
       and cacheClusterIds =
         flag "cache-cluster-ids" (optional json_arg)
           ~doc:"JSON CacheClusterIdList"
       and engine = flag "engine" (optional string) ~doc:"STRING String"
       and serviceUpdateStatus =
         flag "service-update-status" (optional json_arg)
           ~doc:"JSON ServiceUpdateStatusList"
       and serviceUpdateTimeRange =
         flag "service-update-time-range" (optional json_arg)
           ~doc:"JSON TimeRangeFilter"
       and updateActionStatus =
         flag "update-action-status" (optional json_arg)
           ~doc:"JSON UpdateActionStatusList"
       and showNodeLevelUpdateStatus =
         flag "show-node-level-update-status" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_update_actions
           (Values.DescribeUpdateActionsMessage.make ?serviceUpdateName
              ?replicationGroupIds:(Option.map
                                      ~f:Values.ReplicationGroupIdList.of_json
                                      replicationGroupIds)
              ?cacheClusterIds:(Option.map
                                  ~f:Values.CacheClusterIdList.of_json
                                  cacheClusterIds) ?engine
              ?serviceUpdateStatus:(Option.map
                                      ~f:Values.ServiceUpdateStatusList.of_json
                                      serviceUpdateStatus)
              ?serviceUpdateTimeRange:(Option.map
                                         ~f:Values.TimeRangeFilter.of_json
                                         serviceUpdateTimeRange)
              ?updateActionStatus:(Option.map
                                     ~f:Values.UpdateActionStatusList.of_json
                                     updateActionStatus)
              ?showNodeLevelUpdateStatus ?maxRecords ?marker ())
           (Some Values.UpdateActionsMessage.to_json)
           (Some Values.UpdateActionsMessage.error_to_json)])
let describe_user_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userGroupId =
         flag "user-group-id" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user_groups
           (Values.DescribeUserGroupsMessage.make ?userGroupId ?maxRecords
              ?marker ()) (Some Values.DescribeUserGroupsResult.to_json)
           (Some Values.DescribeUserGroupsResult.error_to_json)])
let describe_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engine = flag "engine" (optional string) ~doc:"STRING EngineType"
       and userId = flag "user-id" (optional string) ~doc:"STRING UserId"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_users
           (Values.DescribeUsersMessage.make ?engine ?userId
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DescribeUsersResult.to_json)
           (Some Values.DescribeUsersResult.error_to_json)])
let disassociate_global_replication_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalReplicationGroupId =
         flag "global-replication-group-id" (required string)
           ~doc:"STRING String"
       and replicationGroupId =
         flag "replication-group-id" (required string) ~doc:"STRING String"
       and replicationGroupRegion =
         flag "replication-group-region" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_global_replication_group
           (Values.DisassociateGlobalReplicationGroupMessage.make
              ~globalReplicationGroupId ~replicationGroupId
              ~replicationGroupRegion ())
           (Some Values.DisassociateGlobalReplicationGroupResult.to_json)
           (Some
              Values.DisassociateGlobalReplicationGroupResult.error_to_json)])
let export_serverless_cache_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverlessCacheSnapshotName =
         flag "serverless-cache-snapshot-name" (required string)
           ~doc:"STRING String"
       and s3BucketName =
         flag "s3-bucket-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_serverless_cache_snapshot
           (Values.ExportServerlessCacheSnapshotRequest.make
              ~serverlessCacheSnapshotName ~s3BucketName ())
           (Some Values.ExportServerlessCacheSnapshotResponse.to_json)
           (Some Values.ExportServerlessCacheSnapshotResponse.error_to_json)])
let failover_global_replication_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalReplicationGroupId =
         flag "global-replication-group-id" (required string)
           ~doc:"STRING String"
       and primaryRegion =
         flag "primary-region" (required string) ~doc:"STRING String"
       and primaryReplicationGroupId =
         flag "primary-replication-group-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.failover_global_replication_group
           (Values.FailoverGlobalReplicationGroupMessage.make
              ~globalReplicationGroupId ~primaryRegion
              ~primaryReplicationGroupId ())
           (Some Values.FailoverGlobalReplicationGroupResult.to_json)
           (Some Values.FailoverGlobalReplicationGroupResult.error_to_json)])
let increase_node_groups_in_global_replication_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regionalConfigurations =
         flag "regional-configurations" (optional json_arg)
           ~doc:"JSON RegionalConfigurationList"
       and globalReplicationGroupId =
         flag "global-replication-group-id" (required string)
           ~doc:"STRING String"
       and nodeGroupCount =
         flag "node-group-count" (required int) ~doc:"INT Integer"
       and applyImmediately =
         flag "apply-immediately" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.increase_node_groups_in_global_replication_group
           (Values.IncreaseNodeGroupsInGlobalReplicationGroupMessage.make
              ?regionalConfigurations:(Option.map
                                         ~f:Values.RegionalConfigurationList.of_json
                                         regionalConfigurations)
              ~globalReplicationGroupId ~nodeGroupCount ~applyImmediately ())
           (Some
              Values.IncreaseNodeGroupsInGlobalReplicationGroupResult.to_json)
           (Some
              Values.IncreaseNodeGroupsInGlobalReplicationGroupResult.error_to_json)])
let increase_replica_count =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and newReplicaCount =
         flag "new-replica-count" (optional int) ~doc:"INT IntegerOptional"
       and replicaConfiguration =
         flag "replica-configuration" (optional json_arg)
           ~doc:"JSON ReplicaConfigurationList"
       and replicationGroupId =
         flag "replication-group-id" (required string) ~doc:"STRING String"
       and applyImmediately =
         flag "apply-immediately" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.increase_replica_count
           (Values.IncreaseReplicaCountMessage.make ?newReplicaCount
              ?replicaConfiguration:(Option.map
                                       ~f:Values.ReplicaConfigurationList.of_json
                                       replicaConfiguration)
              ~replicationGroupId ~applyImmediately ())
           (Some Values.IncreaseReplicaCountResult.to_json)
           (Some Values.IncreaseReplicaCountResult.error_to_json)])
let list_allowed_node_type_modifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheClusterId =
         flag "cache-cluster-id" (optional string) ~doc:"STRING String"
       and replicationGroupId =
         flag "replication-group-id" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_allowed_node_type_modifications
           (Values.ListAllowedNodeTypeModificationsMessage.make
              ?cacheClusterId ?replicationGroupId ())
           (Some Values.AllowedNodeTypeModificationsMessage.to_json)
           (Some Values.AllowedNodeTypeModificationsMessage.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceMessage.make ~resourceName ())
           (Some Values.TagListMessage.to_json)
           (Some Values.TagListMessage.error_to_json)])
let modify_cache_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and numCacheNodes =
         flag "num-cache-nodes" (optional int) ~doc:"INT IntegerOptional"
       and cacheNodeIdsToRemove =
         flag "cache-node-ids-to-remove" (optional json_arg)
           ~doc:"JSON CacheNodeIdsList"
       and aZMode = flag "a-z-mode" (optional json_arg) ~doc:"JSON AZMode"
       and newAvailabilityZones =
         flag "new-availability-zones" (optional json_arg)
           ~doc:"JSON PreferredAvailabilityZoneList"
       and cacheSecurityGroupNames =
         flag "cache-security-group-names" (optional json_arg)
           ~doc:"JSON CacheSecurityGroupNameList"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIdsList"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and notificationTopicArn =
         flag "notification-topic-arn" (optional string) ~doc:"STRING String"
       and cacheParameterGroupName =
         flag "cache-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and notificationTopicStatus =
         flag "notification-topic-status" (optional string)
           ~doc:"STRING String"
       and applyImmediately =
         flag "apply-immediately" (optional bool) ~doc:"BOOL Boolean"
       and engine = flag "engine" (optional string) ~doc:"STRING String"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and snapshotRetentionLimit =
         flag "snapshot-retention-limit" (optional int)
           ~doc:"INT IntegerOptional"
       and snapshotWindow =
         flag "snapshot-window" (optional string) ~doc:"STRING String"
       and cacheNodeType =
         flag "cache-node-type" (optional string) ~doc:"STRING String"
       and authToken =
         flag "auth-token" (optional string) ~doc:"STRING String"
       and authTokenUpdateStrategy =
         flag "auth-token-update-strategy" (optional json_arg)
           ~doc:"JSON AuthTokenUpdateStrategyType"
       and logDeliveryConfigurations =
         flag "log-delivery-configurations" (optional json_arg)
           ~doc:"JSON LogDeliveryConfigurationRequestList"
       and ipDiscovery =
         flag "ip-discovery" (optional json_arg) ~doc:"JSON IpDiscovery"
       and scaleConfig =
         flag "scale-config" (optional json_arg) ~doc:"JSON ScaleConfig"
       and cacheClusterId =
         flag "cache-cluster-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_cache_cluster
           (Values.ModifyCacheClusterMessage.make ?numCacheNodes
              ?cacheNodeIdsToRemove:(Option.map
                                       ~f:Values.CacheNodeIdsList.of_json
                                       cacheNodeIdsToRemove)
              ?aZMode:(Option.map ~f:Values.AZMode.of_json aZMode)
              ?newAvailabilityZones:(Option.map
                                       ~f:Values.PreferredAvailabilityZoneList.of_json
                                       newAvailabilityZones)
              ?cacheSecurityGroupNames:(Option.map
                                          ~f:Values.CacheSecurityGroupNameList.of_json
                                          cacheSecurityGroupNames)
              ?securityGroupIds:(Option.map
                                   ~f:Values.SecurityGroupIdsList.of_json
                                   securityGroupIds)
              ?preferredMaintenanceWindow ?notificationTopicArn
              ?cacheParameterGroupName ?notificationTopicStatus
              ?applyImmediately ?engine ?engineVersion
              ?autoMinorVersionUpgrade ?snapshotRetentionLimit
              ?snapshotWindow ?cacheNodeType ?authToken
              ?authTokenUpdateStrategy:(Option.map
                                          ~f:Values.AuthTokenUpdateStrategyType.of_json
                                          authTokenUpdateStrategy)
              ?logDeliveryConfigurations:(Option.map
                                            ~f:Values.LogDeliveryConfigurationRequestList.of_json
                                            logDeliveryConfigurations)
              ?ipDiscovery:(Option.map ~f:Values.IpDiscovery.of_json
                              ipDiscovery)
              ?scaleConfig:(Option.map ~f:Values.ScaleConfig.of_json
                              scaleConfig) ~cacheClusterId ())
           (Some Values.ModifyCacheClusterResult.to_json)
           (Some Values.ModifyCacheClusterResult.error_to_json)])
let modify_cache_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheParameterGroupName =
         flag "cache-parameter-group-name" (required string)
           ~doc:"STRING String"
       and parameterNameValues =
         flag "parameter-name-values" (required json_arg)
           ~doc:"JSON ParameterNameValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_cache_parameter_group
           (Values.ModifyCacheParameterGroupMessage.make
              ~cacheParameterGroupName
              ~parameterNameValues:(Values.ParameterNameValueList.of_json
                                      parameterNameValues) ())
           (Some Values.CacheParameterGroupNameMessage.to_json)
           (Some Values.CacheParameterGroupNameMessage.error_to_json)])
let modify_cache_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheSubnetGroupDescription =
         flag "cache-subnet-group-description" (optional string)
           ~doc:"STRING String"
       and subnetIds =
         flag "subnet-ids" (optional json_arg)
           ~doc:"JSON SubnetIdentifierList"
       and cacheSubnetGroupName =
         flag "cache-subnet-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_cache_subnet_group
           (Values.ModifyCacheSubnetGroupMessage.make
              ?cacheSubnetGroupDescription
              ?subnetIds:(Option.map ~f:Values.SubnetIdentifierList.of_json
                            subnetIds) ~cacheSubnetGroupName ())
           (Some Values.ModifyCacheSubnetGroupResult.to_json)
           (Some Values.ModifyCacheSubnetGroupResult.error_to_json)])
let modify_global_replication_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheNodeType =
         flag "cache-node-type" (optional string) ~doc:"STRING String"
       and engine = flag "engine" (optional string) ~doc:"STRING String"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and cacheParameterGroupName =
         flag "cache-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and globalReplicationGroupDescription =
         flag "global-replication-group-description" (optional string)
           ~doc:"STRING String"
       and automaticFailoverEnabled =
         flag "automatic-failover-enabled" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and globalReplicationGroupId =
         flag "global-replication-group-id" (required string)
           ~doc:"STRING String"
       and applyImmediately =
         flag "apply-immediately" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_global_replication_group
           (Values.ModifyGlobalReplicationGroupMessage.make ?cacheNodeType
              ?engine ?engineVersion ?cacheParameterGroupName
              ?globalReplicationGroupDescription ?automaticFailoverEnabled
              ~globalReplicationGroupId ~applyImmediately ())
           (Some Values.ModifyGlobalReplicationGroupResult.to_json)
           (Some Values.ModifyGlobalReplicationGroupResult.error_to_json)])
let modify_replication_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationGroupDescription =
         flag "replication-group-description" (optional string)
           ~doc:"STRING String"
       and primaryClusterId =
         flag "primary-cluster-id" (optional string) ~doc:"STRING String"
       and snapshottingClusterId =
         flag "snapshotting-cluster-id" (optional string)
           ~doc:"STRING String"
       and automaticFailoverEnabled =
         flag "automatic-failover-enabled" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and multiAZEnabled =
         flag "multi-a-z-enabled" (optional bool) ~doc:"BOOL BooleanOptional"
       and nodeGroupId =
         flag "node-group-id" (optional string) ~doc:"STRING String"
       and cacheSecurityGroupNames =
         flag "cache-security-group-names" (optional json_arg)
           ~doc:"JSON CacheSecurityGroupNameList"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIdsList"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and notificationTopicArn =
         flag "notification-topic-arn" (optional string) ~doc:"STRING String"
       and cacheParameterGroupName =
         flag "cache-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and notificationTopicStatus =
         flag "notification-topic-status" (optional string)
           ~doc:"STRING String"
       and applyImmediately =
         flag "apply-immediately" (optional bool) ~doc:"BOOL Boolean"
       and engine = flag "engine" (optional string) ~doc:"STRING String"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and snapshotRetentionLimit =
         flag "snapshot-retention-limit" (optional int)
           ~doc:"INT IntegerOptional"
       and snapshotWindow =
         flag "snapshot-window" (optional string) ~doc:"STRING String"
       and cacheNodeType =
         flag "cache-node-type" (optional string) ~doc:"STRING String"
       and authToken =
         flag "auth-token" (optional string) ~doc:"STRING String"
       and authTokenUpdateStrategy =
         flag "auth-token-update-strategy" (optional json_arg)
           ~doc:"JSON AuthTokenUpdateStrategyType"
       and userGroupIdsToAdd =
         flag "user-group-ids-to-add" (optional json_arg)
           ~doc:"JSON UserGroupIdList"
       and userGroupIdsToRemove =
         flag "user-group-ids-to-remove" (optional json_arg)
           ~doc:"JSON UserGroupIdList"
       and removeUserGroups =
         flag "remove-user-groups" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and logDeliveryConfigurations =
         flag "log-delivery-configurations" (optional json_arg)
           ~doc:"JSON LogDeliveryConfigurationRequestList"
       and ipDiscovery =
         flag "ip-discovery" (optional json_arg) ~doc:"JSON IpDiscovery"
       and transitEncryptionEnabled =
         flag "transit-encryption-enabled" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and transitEncryptionMode =
         flag "transit-encryption-mode" (optional json_arg)
           ~doc:"JSON TransitEncryptionMode"
       and clusterMode =
         flag "cluster-mode" (optional json_arg) ~doc:"JSON ClusterMode"
       and replicationGroupId =
         flag "replication-group-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_replication_group
           (Values.ModifyReplicationGroupMessage.make
              ?replicationGroupDescription ?primaryClusterId
              ?snapshottingClusterId ?automaticFailoverEnabled
              ?multiAZEnabled ?nodeGroupId
              ?cacheSecurityGroupNames:(Option.map
                                          ~f:Values.CacheSecurityGroupNameList.of_json
                                          cacheSecurityGroupNames)
              ?securityGroupIds:(Option.map
                                   ~f:Values.SecurityGroupIdsList.of_json
                                   securityGroupIds)
              ?preferredMaintenanceWindow ?notificationTopicArn
              ?cacheParameterGroupName ?notificationTopicStatus
              ?applyImmediately ?engine ?engineVersion
              ?autoMinorVersionUpgrade ?snapshotRetentionLimit
              ?snapshotWindow ?cacheNodeType ?authToken
              ?authTokenUpdateStrategy:(Option.map
                                          ~f:Values.AuthTokenUpdateStrategyType.of_json
                                          authTokenUpdateStrategy)
              ?userGroupIdsToAdd:(Option.map
                                    ~f:Values.UserGroupIdList.of_json
                                    userGroupIdsToAdd)
              ?userGroupIdsToRemove:(Option.map
                                       ~f:Values.UserGroupIdList.of_json
                                       userGroupIdsToRemove)
              ?removeUserGroups
              ?logDeliveryConfigurations:(Option.map
                                            ~f:Values.LogDeliveryConfigurationRequestList.of_json
                                            logDeliveryConfigurations)
              ?ipDiscovery:(Option.map ~f:Values.IpDiscovery.of_json
                              ipDiscovery) ?transitEncryptionEnabled
              ?transitEncryptionMode:(Option.map
                                        ~f:Values.TransitEncryptionMode.of_json
                                        transitEncryptionMode)
              ?clusterMode:(Option.map ~f:Values.ClusterMode.of_json
                              clusterMode) ~replicationGroupId ())
           (Some Values.ModifyReplicationGroupResult.to_json)
           (Some Values.ModifyReplicationGroupResult.error_to_json)])
let modify_replication_group_shard_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reshardingConfiguration =
         flag "resharding-configuration" (optional json_arg)
           ~doc:"JSON ReshardingConfigurationList"
       and nodeGroupsToRemove =
         flag "node-groups-to-remove" (optional json_arg)
           ~doc:"JSON NodeGroupsToRemoveList"
       and nodeGroupsToRetain =
         flag "node-groups-to-retain" (optional json_arg)
           ~doc:"JSON NodeGroupsToRetainList"
       and replicationGroupId =
         flag "replication-group-id" (required string) ~doc:"STRING String"
       and nodeGroupCount =
         flag "node-group-count" (required int) ~doc:"INT Integer"
       and applyImmediately =
         flag "apply-immediately" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_replication_group_shard_configuration
           (Values.ModifyReplicationGroupShardConfigurationMessage.make
              ?reshardingConfiguration:(Option.map
                                          ~f:Values.ReshardingConfigurationList.of_json
                                          reshardingConfiguration)
              ?nodeGroupsToRemove:(Option.map
                                     ~f:Values.NodeGroupsToRemoveList.of_json
                                     nodeGroupsToRemove)
              ?nodeGroupsToRetain:(Option.map
                                     ~f:Values.NodeGroupsToRetainList.of_json
                                     nodeGroupsToRetain) ~replicationGroupId
              ~nodeGroupCount ~applyImmediately ())
           (Some
              Values.ModifyReplicationGroupShardConfigurationResult.to_json)
           (Some
              Values.ModifyReplicationGroupShardConfigurationResult.error_to_json)])
let modify_serverless_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and cacheUsageLimits =
         flag "cache-usage-limits" (optional json_arg)
           ~doc:"JSON CacheUsageLimits"
       and removeUserGroup =
         flag "remove-user-group" (optional bool) ~doc:"BOOL BooleanOptional"
       and userGroupId =
         flag "user-group-id" (optional string) ~doc:"STRING String"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIdsList"
       and snapshotRetentionLimit =
         flag "snapshot-retention-limit" (optional int)
           ~doc:"INT IntegerOptional"
       and dailySnapshotTime =
         flag "daily-snapshot-time" (optional string) ~doc:"STRING String"
       and engine = flag "engine" (optional string) ~doc:"STRING String"
       and majorEngineVersion =
         flag "major-engine-version" (optional string) ~doc:"STRING String"
       and serverlessCacheName =
         flag "serverless-cache-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_serverless_cache
           (Values.ModifyServerlessCacheRequest.make ?description
              ?cacheUsageLimits:(Option.map
                                   ~f:Values.CacheUsageLimits.of_json
                                   cacheUsageLimits) ?removeUserGroup
              ?userGroupId
              ?securityGroupIds:(Option.map
                                   ~f:Values.SecurityGroupIdsList.of_json
                                   securityGroupIds) ?snapshotRetentionLimit
              ?dailySnapshotTime ?engine ?majorEngineVersion
              ~serverlessCacheName ())
           (Some Values.ModifyServerlessCacheResponse.to_json)
           (Some Values.ModifyServerlessCacheResponse.error_to_json)])
let modify_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessString =
         flag "access-string" (optional string) ~doc:"STRING AccessString"
       and appendAccessString =
         flag "append-access-string" (optional string)
           ~doc:"STRING AccessString"
       and passwords =
         flag "passwords" (optional json_arg) ~doc:"JSON PasswordListInput"
       and noPasswordRequired =
         flag "no-password-required" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and authenticationMode =
         flag "authentication-mode" (optional json_arg)
           ~doc:"JSON AuthenticationMode"
       and engine = flag "engine" (optional string) ~doc:"STRING EngineType"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_user
           (Values.ModifyUserMessage.make ?accessString ?appendAccessString
              ?passwords:(Option.map ~f:Values.PasswordListInput.of_json
                            passwords) ?noPasswordRequired
              ?authenticationMode:(Option.map
                                     ~f:Values.AuthenticationMode.of_json
                                     authenticationMode) ?engine ~userId ())
           (Some Values.User.to_json) (Some Values.User.error_to_json)])
let modify_user_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userIdsToAdd =
         flag "user-ids-to-add" (optional json_arg)
           ~doc:"JSON UserIdListInput"
       and userIdsToRemove =
         flag "user-ids-to-remove" (optional json_arg)
           ~doc:"JSON UserIdListInput"
       and engine = flag "engine" (optional string) ~doc:"STRING EngineType"
       and userGroupId =
         flag "user-group-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_user_group
           (Values.ModifyUserGroupMessage.make
              ?userIdsToAdd:(Option.map ~f:Values.UserIdListInput.of_json
                               userIdsToAdd)
              ?userIdsToRemove:(Option.map ~f:Values.UserIdListInput.of_json
                                  userIdsToRemove) ?engine ~userGroupId ())
           (Some Values.UserGroup.to_json)
           (Some Values.UserGroup.error_to_json)])
let purchase_reserved_cache_nodes_offering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedCacheNodeId =
         flag "reserved-cache-node-id" (optional string) ~doc:"STRING String"
       and cacheNodeCount =
         flag "cache-node-count" (optional int) ~doc:"INT IntegerOptional"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and reservedCacheNodesOfferingId =
         flag "reserved-cache-nodes-offering-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.purchase_reserved_cache_nodes_offering
           (Values.PurchaseReservedCacheNodesOfferingMessage.make
              ?reservedCacheNodeId ?cacheNodeCount
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~reservedCacheNodesOfferingId ())
           (Some Values.PurchaseReservedCacheNodesOfferingResult.to_json)
           (Some
              Values.PurchaseReservedCacheNodesOfferingResult.error_to_json)])
let rebalance_slots_in_global_replication_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalReplicationGroupId =
         flag "global-replication-group-id" (required string)
           ~doc:"STRING String"
       and applyImmediately =
         flag "apply-immediately" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.rebalance_slots_in_global_replication_group
           (Values.RebalanceSlotsInGlobalReplicationGroupMessage.make
              ~globalReplicationGroupId ~applyImmediately ())
           (Some Values.RebalanceSlotsInGlobalReplicationGroupResult.to_json)
           (Some
              Values.RebalanceSlotsInGlobalReplicationGroupResult.error_to_json)])
let reboot_cache_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheClusterId =
         flag "cache-cluster-id" (required string) ~doc:"STRING String"
       and cacheNodeIdsToReboot =
         flag "cache-node-ids-to-reboot" (required json_arg)
           ~doc:"JSON CacheNodeIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_cache_cluster
           (Values.RebootCacheClusterMessage.make ~cacheClusterId
              ~cacheNodeIdsToReboot:(Values.CacheNodeIdsList.of_json
                                       cacheNodeIdsToReboot) ())
           (Some Values.RebootCacheClusterResult.to_json)
           (Some Values.RebootCacheClusterResult.error_to_json)])
let remove_tags_from_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING String"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON KeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_tags_from_resource
           (Values.RemoveTagsFromResourceMessage.make ~resourceName
              ~tagKeys:(Values.KeyList.of_json tagKeys) ())
           (Some Values.TagListMessage.to_json)
           (Some Values.TagListMessage.error_to_json)])
let reset_cache_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resetAllParameters =
         flag "reset-all-parameters" (optional bool) ~doc:"BOOL Boolean"
       and parameterNameValues =
         flag "parameter-name-values" (optional json_arg)
           ~doc:"JSON ParameterNameValueList"
       and cacheParameterGroupName =
         flag "cache-parameter-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_cache_parameter_group
           (Values.ResetCacheParameterGroupMessage.make ?resetAllParameters
              ?parameterNameValues:(Option.map
                                      ~f:Values.ParameterNameValueList.of_json
                                      parameterNameValues)
              ~cacheParameterGroupName ())
           (Some Values.CacheParameterGroupNameMessage.to_json)
           (Some Values.CacheParameterGroupNameMessage.error_to_json)])
let revoke_cache_security_group_ingress =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheSecurityGroupName =
         flag "cache-security-group-name" (required string)
           ~doc:"STRING String"
       and eC2SecurityGroupName =
         flag "e-c2-security-group-name" (required string)
           ~doc:"STRING String"
       and eC2SecurityGroupOwnerId =
         flag "e-c2-security-group-owner-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_cache_security_group_ingress
           (Values.RevokeCacheSecurityGroupIngressMessage.make
              ~cacheSecurityGroupName ~eC2SecurityGroupName
              ~eC2SecurityGroupOwnerId ())
           (Some Values.RevokeCacheSecurityGroupIngressResult.to_json)
           (Some Values.RevokeCacheSecurityGroupIngressResult.error_to_json)])
let start_migration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationGroupId =
         flag "replication-group-id" (required string) ~doc:"STRING String"
       and customerNodeEndpointList =
         flag "customer-node-endpoint-list" (required json_arg)
           ~doc:"JSON CustomerNodeEndpointList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_migration
           (Values.StartMigrationMessage.make ~replicationGroupId
              ~customerNodeEndpointList:(Values.CustomerNodeEndpointList.of_json
                                           customerNodeEndpointList) ())
           (Some Values.StartMigrationResponse.to_json)
           (Some Values.StartMigrationResponse.error_to_json)])
let test_failover =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationGroupId =
         flag "replication-group-id" (required string) ~doc:"STRING String"
       and nodeGroupId =
         flag "node-group-id" (required string)
           ~doc:"STRING AllowedNodeGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_failover
           (Values.TestFailoverMessage.make ~replicationGroupId ~nodeGroupId
              ()) (Some Values.TestFailoverResult.to_json)
           (Some Values.TestFailoverResult.error_to_json)])
let test_migration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replicationGroupId =
         flag "replication-group-id" (required string) ~doc:"STRING String"
       and customerNodeEndpointList =
         flag "customer-node-endpoint-list" (required json_arg)
           ~doc:"JSON CustomerNodeEndpointList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_migration
           (Values.TestMigrationMessage.make ~replicationGroupId
              ~customerNodeEndpointList:(Values.CustomerNodeEndpointList.of_json
                                           customerNodeEndpointList) ())
           (Some Values.TestMigrationResponse.to_json)
           (Some Values.TestMigrationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-tags-to-resource", add_tags_to_resource);
    ("authorize-cache-security-group-ingress",
      authorize_cache_security_group_ingress);
    ("batch-apply-update-action", batch_apply_update_action);
    ("batch-stop-update-action", batch_stop_update_action);
    ("complete-migration", complete_migration);
    ("copy-serverless-cache-snapshot", copy_serverless_cache_snapshot);
    ("copy-snapshot", copy_snapshot);
    ("create-cache-cluster", create_cache_cluster);
    ("create-cache-parameter-group", create_cache_parameter_group);
    ("create-cache-security-group", create_cache_security_group);
    ("create-cache-subnet-group", create_cache_subnet_group);
    ("create-global-replication-group", create_global_replication_group);
    ("create-replication-group", create_replication_group);
    ("create-serverless-cache", create_serverless_cache);
    ("create-serverless-cache-snapshot", create_serverless_cache_snapshot);
    ("create-snapshot", create_snapshot);
    ("create-user", create_user);
    ("create-user-group", create_user_group);
    ("decrease-node-groups-in-global-replication-group",
      decrease_node_groups_in_global_replication_group);
    ("decrease-replica-count", decrease_replica_count);
    ("delete-cache-cluster", delete_cache_cluster);
    ("delete-cache-parameter-group", delete_cache_parameter_group);
    ("delete-cache-security-group", delete_cache_security_group);
    ("delete-cache-subnet-group", delete_cache_subnet_group);
    ("delete-global-replication-group", delete_global_replication_group);
    ("delete-replication-group", delete_replication_group);
    ("delete-serverless-cache", delete_serverless_cache);
    ("delete-serverless-cache-snapshot", delete_serverless_cache_snapshot);
    ("delete-snapshot", delete_snapshot);
    ("delete-user", delete_user);
    ("delete-user-group", delete_user_group);
    ("describe-cache-clusters", describe_cache_clusters);
    ("describe-cache-engine-versions", describe_cache_engine_versions);
    ("describe-cache-parameter-groups", describe_cache_parameter_groups);
    ("describe-cache-parameters", describe_cache_parameters);
    ("describe-cache-security-groups", describe_cache_security_groups);
    ("describe-cache-subnet-groups", describe_cache_subnet_groups);
    ("describe-engine-default-parameters",
      describe_engine_default_parameters);
    ("describe-events", describe_events);
    ("describe-global-replication-groups",
      describe_global_replication_groups);
    ("describe-replication-groups", describe_replication_groups);
    ("describe-reserved-cache-nodes", describe_reserved_cache_nodes);
    ("describe-reserved-cache-nodes-offerings",
      describe_reserved_cache_nodes_offerings);
    ("describe-serverless-cache-snapshots",
      describe_serverless_cache_snapshots);
    ("describe-serverless-caches", describe_serverless_caches);
    ("describe-service-updates", describe_service_updates);
    ("describe-snapshots", describe_snapshots);
    ("describe-update-actions", describe_update_actions);
    ("describe-user-groups", describe_user_groups);
    ("describe-users", describe_users);
    ("disassociate-global-replication-group",
      disassociate_global_replication_group);
    ("export-serverless-cache-snapshot", export_serverless_cache_snapshot);
    ("failover-global-replication-group", failover_global_replication_group);
    ("increase-node-groups-in-global-replication-group",
      increase_node_groups_in_global_replication_group);
    ("increase-replica-count", increase_replica_count);
    ("list-allowed-node-type-modifications",
      list_allowed_node_type_modifications);
    ("list-tags-for-resource", list_tags_for_resource);
    ("modify-cache-cluster", modify_cache_cluster);
    ("modify-cache-parameter-group", modify_cache_parameter_group);
    ("modify-cache-subnet-group", modify_cache_subnet_group);
    ("modify-global-replication-group", modify_global_replication_group);
    ("modify-replication-group", modify_replication_group);
    ("modify-replication-group-shard-configuration",
      modify_replication_group_shard_configuration);
    ("modify-serverless-cache", modify_serverless_cache);
    ("modify-user", modify_user);
    ("modify-user-group", modify_user_group);
    ("purchase-reserved-cache-nodes-offering",
      purchase_reserved_cache_nodes_offering);
    ("rebalance-slots-in-global-replication-group",
      rebalance_slots_in_global_replication_group);
    ("reboot-cache-cluster", reboot_cache_cluster);
    ("remove-tags-from-resource", remove_tags_from_resource);
    ("reset-cache-parameter-group", reset_cache_parameter_group);
    ("revoke-cache-security-group-ingress",
      revoke_cache_security_group_ingress);
    ("start-migration", start_migration);
    ("test-failover", test_failover);
    ("test-migration", test_migration)]
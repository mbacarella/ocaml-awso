(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_draft_app_version_resource_mappings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn"
       and resourceMappings =
         flag "resource-mappings" (required json_arg)
           ~doc:"JSON ResourceMappingList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_draft_app_version_resource_mappings
           (Values.AddDraftAppVersionResourceMappingsRequest.make ~appArn
              ~resourceMappings:(Values.ResourceMappingList.of_json
                                   resourceMappings) ())
           (Some Values.AddDraftAppVersionResourceMappingsResponse.to_json)
           (Some
              Values.AddDraftAppVersionResourceMappingsResponse.error_to_json)])
let create_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING EntityDescription"
       and policyArn = flag "policy-arn" (optional string) ~doc:"STRING Arn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app
           (Values.CreateAppRequest.make ?clientToken ?description ?policyArn
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name ())
           (Some Values.CreateAppResponse.to_json)
           (Some Values.CreateAppResponse.error_to_json)])
let create_recommendation_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bucketName =
         flag "bucket-name" (optional string) ~doc:"STRING EntityName"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and format =
         flag "format" (optional json_arg) ~doc:"JSON TemplateFormat"
       and recommendationIds =
         flag "recommendation-ids" (optional json_arg)
           ~doc:"JSON RecommendationIdList"
       and recommendationTypes =
         flag "recommendation-types" (optional json_arg)
           ~doc:"JSON RenderRecommendationTypeList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and assessmentArn =
         flag "assessment-arn" (required string) ~doc:"STRING Arn"
       and name = flag "name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_recommendation_template
           (Values.CreateRecommendationTemplateRequest.make ?bucketName
              ?clientToken
              ?format:(Option.map ~f:Values.TemplateFormat.of_json format)
              ?recommendationIds:(Option.map
                                    ~f:Values.RecommendationIdList.of_json
                                    recommendationIds)
              ?recommendationTypes:(Option.map
                                      ~f:Values.RenderRecommendationTypeList.of_json
                                      recommendationTypes)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~assessmentArn
              ~name ())
           (Some Values.CreateRecommendationTemplateResponse.to_json)
           (Some Values.CreateRecommendationTemplateResponse.error_to_json)])
let create_resiliency_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and dataLocationConstraint =
         flag "data-location-constraint" (optional json_arg)
           ~doc:"JSON DataLocationConstraint"
       and policyDescription =
         flag "policy-description" (optional string)
           ~doc:"STRING EntityDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and policy =
         flag "policy" (required json_arg) ~doc:"JSON DisruptionPolicy"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING EntityName"
       and tier =
         flag "tier" (required json_arg) ~doc:"JSON ResiliencyPolicyTier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resiliency_policy
           (Values.CreateResiliencyPolicyRequest.make ?clientToken
              ?dataLocationConstraint:(Option.map
                                         ~f:Values.DataLocationConstraint.of_json
                                         dataLocationConstraint)
              ?policyDescription
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~policy:(Values.DisruptionPolicy.of_json policy) ~policyName
              ~tier:(Values.ResiliencyPolicyTier.of_json tier) ())
           (Some Values.CreateResiliencyPolicyResponse.to_json)
           (Some Values.CreateResiliencyPolicyResponse.error_to_json)])
let delete_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL BooleanOptional"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app
           (Values.DeleteAppRequest.make ?clientToken ?forceDelete ~appArn ())
           (Some Values.DeleteAppResponse.to_json)
           (Some Values.DeleteAppResponse.error_to_json)])
let delete_app_assessment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and assessmentArn =
         flag "assessment-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_assessment
           (Values.DeleteAppAssessmentRequest.make ?clientToken
              ~assessmentArn ())
           (Some Values.DeleteAppAssessmentResponse.to_json)
           (Some Values.DeleteAppAssessmentResponse.error_to_json)])
let delete_recommendation_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and recommendationTemplateArn =
         flag "recommendation-template-arn" (required string)
           ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_recommendation_template
           (Values.DeleteRecommendationTemplateRequest.make ?clientToken
              ~recommendationTemplateArn ())
           (Some Values.DeleteRecommendationTemplateResponse.to_json)
           (Some Values.DeleteRecommendationTemplateResponse.error_to_json)])
let delete_resiliency_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and policyArn = flag "policy-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resiliency_policy
           (Values.DeleteResiliencyPolicyRequest.make ?clientToken ~policyArn
              ()) (Some Values.DeleteResiliencyPolicyResponse.to_json)
           (Some Values.DeleteResiliencyPolicyResponse.error_to_json)])
let describe_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app (Values.DescribeAppRequest.make ~appArn ())
           (Some Values.DescribeAppResponse.to_json)
           (Some Values.DescribeAppResponse.error_to_json)])
let describe_app_assessment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assessmentArn =
         flag "assessment-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app_assessment
           (Values.DescribeAppAssessmentRequest.make ~assessmentArn ())
           (Some Values.DescribeAppAssessmentResponse.to_json)
           (Some Values.DescribeAppAssessmentResponse.error_to_json)])
let describe_app_version_resources_resolution_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resolutionId =
         flag "resolution-id" (optional string) ~doc:"STRING String255"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn"
       and appVersion =
         flag "app-version" (required string) ~doc:"STRING EntityVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app_version_resources_resolution_status
           (Values.DescribeAppVersionResourcesResolutionStatusRequest.make
              ?resolutionId ~appArn ~appVersion ())
           (Some
              Values.DescribeAppVersionResourcesResolutionStatusResponse.to_json)
           (Some
              Values.DescribeAppVersionResourcesResolutionStatusResponse.error_to_json)])
let describe_app_version_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn"
       and appVersion =
         flag "app-version" (required string) ~doc:"STRING EntityVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app_version_template
           (Values.DescribeAppVersionTemplateRequest.make ~appArn ~appVersion
              ()) (Some Values.DescribeAppVersionTemplateResponse.to_json)
           (Some Values.DescribeAppVersionTemplateResponse.error_to_json)])
let describe_draft_app_version_resources_import_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_draft_app_version_resources_import_status
           (Values.DescribeDraftAppVersionResourcesImportStatusRequest.make
              ~appArn ())
           (Some
              Values.DescribeDraftAppVersionResourcesImportStatusResponse.to_json)
           (Some
              Values.DescribeDraftAppVersionResourcesImportStatusResponse.error_to_json)])
let describe_resiliency_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn = flag "policy-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_resiliency_policy
           (Values.DescribeResiliencyPolicyRequest.make ~policyArn ())
           (Some Values.DescribeResiliencyPolicyResponse.to_json)
           (Some Values.DescribeResiliencyPolicyResponse.error_to_json)])
let import_resources_to_draft_app_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn"
       and sourceArns =
         flag "source-arns" (required json_arg) ~doc:"JSON ArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_resources_to_draft_app_version
           (Values.ImportResourcesToDraftAppVersionRequest.make ~appArn
              ~sourceArns:(Values.ArnList.of_json sourceArns) ())
           (Some Values.ImportResourcesToDraftAppVersionResponse.to_json)
           (Some
              Values.ImportResourcesToDraftAppVersionResponse.error_to_json)])
let list_alarm_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and assessmentArn =
         flag "assessment-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_alarm_recommendations
           (Values.ListAlarmRecommendationsRequest.make ?maxResults
              ?nextToken ~assessmentArn ())
           (Some Values.ListAlarmRecommendationsResponse.to_json)
           (Some Values.ListAlarmRecommendationsResponse.error_to_json)])
let list_app_assessments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appArn = flag "app-arn" (optional string) ~doc:"STRING Arn"
       and assessmentName =
         flag "assessment-name" (optional string) ~doc:"STRING EntityName"
       and assessmentStatus =
         flag "assessment-status" (optional json_arg)
           ~doc:"JSON AssessmentStatusList"
       and complianceStatus =
         flag "compliance-status" (optional json_arg)
           ~doc:"JSON ComplianceStatus"
       and invoker =
         flag "invoker" (optional json_arg) ~doc:"JSON AssessmentInvoker"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and reverseOrder =
         flag "reverse-order" (optional bool) ~doc:"BOOL BooleanOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_assessments
           (Values.ListAppAssessmentsRequest.make ?appArn ?assessmentName
              ?assessmentStatus:(Option.map
                                   ~f:Values.AssessmentStatusList.of_json
                                   assessmentStatus)
              ?complianceStatus:(Option.map
                                   ~f:Values.ComplianceStatus.of_json
                                   complianceStatus)
              ?invoker:(Option.map ~f:Values.AssessmentInvoker.of_json
                          invoker) ?maxResults ?nextToken ?reverseOrder ())
           (Some Values.ListAppAssessmentsResponse.to_json)
           (Some Values.ListAppAssessmentsResponse.error_to_json)])
let list_app_component_compliances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and assessmentArn =
         flag "assessment-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_component_compliances
           (Values.ListAppComponentCompliancesRequest.make ?maxResults
              ?nextToken ~assessmentArn ())
           (Some Values.ListAppComponentCompliancesResponse.to_json)
           (Some Values.ListAppComponentCompliancesResponse.error_to_json)])
let list_app_component_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and assessmentArn =
         flag "assessment-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_component_recommendations
           (Values.ListAppComponentRecommendationsRequest.make ?maxResults
              ?nextToken ~assessmentArn ())
           (Some Values.ListAppComponentRecommendationsResponse.to_json)
           (Some Values.ListAppComponentRecommendationsResponse.error_to_json)])
let list_app_version_resource_mappings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn"
       and appVersion =
         flag "app-version" (required string) ~doc:"STRING EntityVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_version_resource_mappings
           (Values.ListAppVersionResourceMappingsRequest.make ?maxResults
              ?nextToken ~appArn ~appVersion ())
           (Some Values.ListAppVersionResourceMappingsResponse.to_json)
           (Some Values.ListAppVersionResourceMappingsResponse.error_to_json)])
let list_app_version_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and resolutionId =
         flag "resolution-id" (optional string) ~doc:"STRING String255"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn"
       and appVersion =
         flag "app-version" (required string) ~doc:"STRING EntityVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_version_resources
           (Values.ListAppVersionResourcesRequest.make ?maxResults ?nextToken
              ?resolutionId ~appArn ~appVersion ())
           (Some Values.ListAppVersionResourcesResponse.to_json)
           (Some Values.ListAppVersionResourcesResponse.error_to_json)])
let list_app_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_versions
           (Values.ListAppVersionsRequest.make ?maxResults ?nextToken ~appArn
              ()) (Some Values.ListAppVersionsResponse.to_json)
           (Some Values.ListAppVersionsResponse.error_to_json)])
let list_apps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appArn = flag "app-arn" (optional string) ~doc:"STRING Arn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and name = flag "name" (optional string) ~doc:"STRING EntityName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_apps
           (Values.ListAppsRequest.make ?appArn ?maxResults ?name ?nextToken
              ()) (Some Values.ListAppsResponse.to_json)
           (Some Values.ListAppsResponse.error_to_json)])
let list_recommendation_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and name = flag "name" (optional string) ~doc:"STRING EntityName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and recommendationTemplateArn =
         flag "recommendation-template-arn" (optional string)
           ~doc:"STRING Arn"
       and reverseOrder =
         flag "reverse-order" (optional bool) ~doc:"BOOL BooleanOptional"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON RecommendationTemplateStatusList"
       and assessmentArn =
         flag "assessment-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recommendation_templates
           (Values.ListRecommendationTemplatesRequest.make ?maxResults ?name
              ?nextToken ?recommendationTemplateArn ?reverseOrder
              ?status:(Option.map
                         ~f:Values.RecommendationTemplateStatusList.of_json
                         status) ~assessmentArn ())
           (Some Values.ListRecommendationTemplatesResponse.to_json)
           (Some Values.ListRecommendationTemplatesResponse.error_to_json)])
let list_resiliency_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and policyName =
         flag "policy-name" (optional string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resiliency_policies
           (Values.ListResiliencyPoliciesRequest.make ?maxResults ?nextToken
              ?policyName ())
           (Some Values.ListResiliencyPoliciesResponse.to_json)
           (Some Values.ListResiliencyPoliciesResponse.error_to_json)])
let list_sop_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and assessmentArn =
         flag "assessment-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sop_recommendations
           (Values.ListSopRecommendationsRequest.make ?maxResults ?nextToken
              ~assessmentArn ())
           (Some Values.ListSopRecommendationsResponse.to_json)
           (Some Values.ListSopRecommendationsResponse.error_to_json)])
let list_suggested_resiliency_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_suggested_resiliency_policies
           (Values.ListSuggestedResiliencyPoliciesRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListSuggestedResiliencyPoliciesResponse.to_json)
           (Some Values.ListSuggestedResiliencyPoliciesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_test_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and assessmentArn =
         flag "assessment-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_test_recommendations
           (Values.ListTestRecommendationsRequest.make ?maxResults ?nextToken
              ~assessmentArn ())
           (Some Values.ListTestRecommendationsResponse.to_json)
           (Some Values.ListTestRecommendationsResponse.error_to_json)])
let list_unsupported_app_version_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and resolutionId =
         flag "resolution-id" (optional string) ~doc:"STRING String255"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn"
       and appVersion =
         flag "app-version" (required string) ~doc:"STRING EntityVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_unsupported_app_version_resources
           (Values.ListUnsupportedAppVersionResourcesRequest.make ?maxResults
              ?nextToken ?resolutionId ~appArn ~appVersion ())
           (Some Values.ListUnsupportedAppVersionResourcesResponse.to_json)
           (Some
              Values.ListUnsupportedAppVersionResourcesResponse.error_to_json)])
let publish_app_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.publish_app_version
           (Values.PublishAppVersionRequest.make ~appArn ())
           (Some Values.PublishAppVersionResponse.to_json)
           (Some Values.PublishAppVersionResponse.error_to_json)])
let put_draft_app_version_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn"
       and appTemplateBody =
         flag "app-template-body" (required string)
           ~doc:"STRING AppTemplateBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_draft_app_version_template
           (Values.PutDraftAppVersionTemplateRequest.make ~appArn
              ~appTemplateBody ())
           (Some Values.PutDraftAppVersionTemplateResponse.to_json)
           (Some Values.PutDraftAppVersionTemplateResponse.error_to_json)])
let remove_draft_app_version_resource_mappings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appRegistryAppNames =
         flag "app-registry-app-names" (optional json_arg)
           ~doc:"JSON EntityNameList"
       and logicalStackNames =
         flag "logical-stack-names" (optional json_arg)
           ~doc:"JSON String255List"
       and resourceGroupNames =
         flag "resource-group-names" (optional json_arg)
           ~doc:"JSON EntityNameList"
       and resourceNames =
         flag "resource-names" (optional json_arg) ~doc:"JSON EntityNameList"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_draft_app_version_resource_mappings
           (Values.RemoveDraftAppVersionResourceMappingsRequest.make
              ?appRegistryAppNames:(Option.map
                                      ~f:Values.EntityNameList.of_json
                                      appRegistryAppNames)
              ?logicalStackNames:(Option.map ~f:Values.String255List.of_json
                                    logicalStackNames)
              ?resourceGroupNames:(Option.map
                                     ~f:Values.EntityNameList.of_json
                                     resourceGroupNames)
              ?resourceNames:(Option.map ~f:Values.EntityNameList.of_json
                                resourceNames) ~appArn ())
           (Some Values.RemoveDraftAppVersionResourceMappingsResponse.to_json)
           (Some
              Values.RemoveDraftAppVersionResourceMappingsResponse.error_to_json)])
let resolve_app_version_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn"
       and appVersion =
         flag "app-version" (required string) ~doc:"STRING EntityVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resolve_app_version_resources
           (Values.ResolveAppVersionResourcesRequest.make ~appArn ~appVersion
              ()) (Some Values.ResolveAppVersionResourcesResponse.to_json)
           (Some Values.ResolveAppVersionResourcesResponse.error_to_json)])
let start_app_assessment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn"
       and appVersion =
         flag "app-version" (required string) ~doc:"STRING EntityVersion"
       and assessmentName =
         flag "assessment-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_app_assessment
           (Values.StartAppAssessmentRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~appArn
              ~appVersion ~assessmentName ())
           (Some Values.StartAppAssessmentResponse.to_json)
           (Some Values.StartAppAssessmentResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clearResiliencyPolicyArn =
         flag "clear-resiliency-policy-arn" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and description =
         flag "description" (optional string) ~doc:"STRING EntityDescription"
       and policyArn = flag "policy-arn" (optional string) ~doc:"STRING Arn"
       and appArn = flag "app-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_app
           (Values.UpdateAppRequest.make ?clearResiliencyPolicyArn
              ?description ?policyArn ~appArn ())
           (Some Values.UpdateAppResponse.to_json)
           (Some Values.UpdateAppResponse.error_to_json)])
let update_resiliency_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataLocationConstraint =
         flag "data-location-constraint" (optional json_arg)
           ~doc:"JSON DataLocationConstraint"
       and policy =
         flag "policy" (optional json_arg) ~doc:"JSON DisruptionPolicy"
       and policyDescription =
         flag "policy-description" (optional string)
           ~doc:"STRING EntityDescription"
       and policyName =
         flag "policy-name" (optional string) ~doc:"STRING EntityName"
       and tier =
         flag "tier" (optional json_arg) ~doc:"JSON ResiliencyPolicyTier"
       and policyArn = flag "policy-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resiliency_policy
           (Values.UpdateResiliencyPolicyRequest.make
              ?dataLocationConstraint:(Option.map
                                         ~f:Values.DataLocationConstraint.of_json
                                         dataLocationConstraint)
              ?policy:(Option.map ~f:Values.DisruptionPolicy.of_json policy)
              ?policyDescription ?policyName
              ?tier:(Option.map ~f:Values.ResiliencyPolicyTier.of_json tier)
              ~policyArn ())
           (Some Values.UpdateResiliencyPolicyResponse.to_json)
           (Some Values.UpdateResiliencyPolicyResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("add-draft-app-version-resource-mappings",
       add_draft_app_version_resource_mappings);
    ("create-app", create_app);
    ("create-recommendation-template", create_recommendation_template);
    ("create-resiliency-policy", create_resiliency_policy);
    ("delete-app", delete_app);
    ("delete-app-assessment", delete_app_assessment);
    ("delete-recommendation-template", delete_recommendation_template);
    ("delete-resiliency-policy", delete_resiliency_policy);
    ("describe-app", describe_app);
    ("describe-app-assessment", describe_app_assessment);
    ("describe-app-version-resources-resolution-status",
      describe_app_version_resources_resolution_status);
    ("describe-app-version-template", describe_app_version_template);
    ("describe-draft-app-version-resources-import-status",
      describe_draft_app_version_resources_import_status);
    ("describe-resiliency-policy", describe_resiliency_policy);
    ("import-resources-to-draft-app-version",
      import_resources_to_draft_app_version);
    ("list-alarm-recommendations", list_alarm_recommendations);
    ("list-app-assessments", list_app_assessments);
    ("list-app-component-compliances", list_app_component_compliances);
    ("list-app-component-recommendations",
      list_app_component_recommendations);
    ("list-app-version-resource-mappings",
      list_app_version_resource_mappings);
    ("list-app-version-resources", list_app_version_resources);
    ("list-app-versions", list_app_versions);
    ("list-apps", list_apps);
    ("list-recommendation-templates", list_recommendation_templates);
    ("list-resiliency-policies", list_resiliency_policies);
    ("list-sop-recommendations", list_sop_recommendations);
    ("list-suggested-resiliency-policies",
      list_suggested_resiliency_policies);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-test-recommendations", list_test_recommendations);
    ("list-unsupported-app-version-resources",
      list_unsupported_app_version_resources);
    ("publish-app-version", publish_app_version);
    ("put-draft-app-version-template", put_draft_app_version_template);
    ("remove-draft-app-version-resource-mappings",
      remove_draft_app_version_resource_mappings);
    ("resolve-app-version-resources", resolve_app_version_resources);
    ("start-app-assessment", start_app_assessment);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-app", update_app);
    ("update-resiliency-policy", update_resiliency_policy)]
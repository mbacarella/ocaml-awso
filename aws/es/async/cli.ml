(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_inbound_cross_cluster_search_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crossClusterSearchConnectionId =
         flag "cross-cluster-search-connection-id" (required string)
           ~doc:"STRING CrossClusterSearchConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_inbound_cross_cluster_search_connection
           (Values.AcceptInboundCrossClusterSearchConnectionRequest.make
              ~crossClusterSearchConnectionId ())
           (Some
              Values.AcceptInboundCrossClusterSearchConnectionResponse.to_json)
           (Some
              Values.AcceptInboundCrossClusterSearchConnectionResponse.error_to_json)])
let add_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aRN = flag "a-r-n" (required string) ~doc:"STRING ARN"
       and tagList = flag "tag-list" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags
           (Values.AddTagsRequest.make ~aRN
              ~tagList:(Values.TagList.of_json tagList) ()) None None])
let associate_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and packageID =
         flag "package-i-d" (required string) ~doc:"STRING PackageID"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_package
           (Values.AssociatePackageRequest.make ~packageID ~domainName ())
           (Some Values.AssociatePackageResponse.to_json)
           (Some Values.AssociatePackageResponse.error_to_json)])
let cancel_elasticsearch_service_software_update =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_elasticsearch_service_software_update
           (Values.CancelElasticsearchServiceSoftwareUpdateRequest.make
              ~domainName ())
           (Some
              Values.CancelElasticsearchServiceSoftwareUpdateResponse.to_json)
           (Some
              Values.CancelElasticsearchServiceSoftwareUpdateResponse.error_to_json)])
let create_elasticsearch_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and elasticsearchVersion =
         flag "elasticsearch-version" (optional string)
           ~doc:"STRING ElasticsearchVersionString"
       and elasticsearchClusterConfig =
         flag "elasticsearch-cluster-config" (optional json_arg)
           ~doc:"JSON ElasticsearchClusterConfig"
       and eBSOptions =
         flag "e-b-s-options" (optional json_arg) ~doc:"JSON EBSOptions"
       and accessPolicies =
         flag "access-policies" (optional string)
           ~doc:"STRING PolicyDocument"
       and snapshotOptions =
         flag "snapshot-options" (optional json_arg)
           ~doc:"JSON SnapshotOptions"
       and vPCOptions =
         flag "v-p-c-options" (optional json_arg) ~doc:"JSON VPCOptions"
       and cognitoOptions =
         flag "cognito-options" (optional json_arg)
           ~doc:"JSON CognitoOptions"
       and encryptionAtRestOptions =
         flag "encryption-at-rest-options" (optional json_arg)
           ~doc:"JSON EncryptionAtRestOptions"
       and nodeToNodeEncryptionOptions =
         flag "node-to-node-encryption-options" (optional json_arg)
           ~doc:"JSON NodeToNodeEncryptionOptions"
       and advancedOptions =
         flag "advanced-options" (optional json_arg)
           ~doc:"JSON AdvancedOptions"
       and logPublishingOptions =
         flag "log-publishing-options" (optional json_arg)
           ~doc:"JSON LogPublishingOptions"
       and domainEndpointOptions =
         flag "domain-endpoint-options" (optional json_arg)
           ~doc:"JSON DomainEndpointOptions"
       and advancedSecurityOptions =
         flag "advanced-security-options" (optional json_arg)
           ~doc:"JSON AdvancedSecurityOptionsInput"
       and autoTuneOptions =
         flag "auto-tune-options" (optional json_arg)
           ~doc:"JSON AutoTuneOptionsInput"
       and tagList = flag "tag-list" (optional json_arg) ~doc:"JSON TagList"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_elasticsearch_domain
           (Values.CreateElasticsearchDomainRequest.make
              ?elasticsearchVersion
              ?elasticsearchClusterConfig:(Option.map
                                             ~f:Values.ElasticsearchClusterConfig.of_json
                                             elasticsearchClusterConfig)
              ?eBSOptions:(Option.map ~f:Values.EBSOptions.of_json eBSOptions)
              ?accessPolicies
              ?snapshotOptions:(Option.map ~f:Values.SnapshotOptions.of_json
                                  snapshotOptions)
              ?vPCOptions:(Option.map ~f:Values.VPCOptions.of_json vPCOptions)
              ?cognitoOptions:(Option.map ~f:Values.CognitoOptions.of_json
                                 cognitoOptions)
              ?encryptionAtRestOptions:(Option.map
                                          ~f:Values.EncryptionAtRestOptions.of_json
                                          encryptionAtRestOptions)
              ?nodeToNodeEncryptionOptions:(Option.map
                                              ~f:Values.NodeToNodeEncryptionOptions.of_json
                                              nodeToNodeEncryptionOptions)
              ?advancedOptions:(Option.map ~f:Values.AdvancedOptions.of_json
                                  advancedOptions)
              ?logPublishingOptions:(Option.map
                                       ~f:Values.LogPublishingOptions.of_json
                                       logPublishingOptions)
              ?domainEndpointOptions:(Option.map
                                        ~f:Values.DomainEndpointOptions.of_json
                                        domainEndpointOptions)
              ?advancedSecurityOptions:(Option.map
                                          ~f:Values.AdvancedSecurityOptionsInput.of_json
                                          advancedSecurityOptions)
              ?autoTuneOptions:(Option.map
                                  ~f:Values.AutoTuneOptionsInput.of_json
                                  autoTuneOptions)
              ?tagList:(Option.map ~f:Values.TagList.of_json tagList)
              ~domainName ())
           (Some Values.CreateElasticsearchDomainResponse.to_json)
           (Some Values.CreateElasticsearchDomainResponse.error_to_json)])
let create_outbound_cross_cluster_search_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceDomainInfo =
         flag "source-domain-info" (required json_arg)
           ~doc:"JSON DomainInformation"
       and destinationDomainInfo =
         flag "destination-domain-info" (required json_arg)
           ~doc:"JSON DomainInformation"
       and connectionAlias =
         flag "connection-alias" (required string)
           ~doc:"STRING ConnectionAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_outbound_cross_cluster_search_connection
           (Values.CreateOutboundCrossClusterSearchConnectionRequest.make
              ~sourceDomainInfo:(Values.DomainInformation.of_json
                                   sourceDomainInfo)
              ~destinationDomainInfo:(Values.DomainInformation.of_json
                                        destinationDomainInfo)
              ~connectionAlias ())
           (Some
              Values.CreateOutboundCrossClusterSearchConnectionResponse.to_json)
           (Some
              Values.CreateOutboundCrossClusterSearchConnectionResponse.error_to_json)])
let create_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and packageDescription =
         flag "package-description" (optional string)
           ~doc:"STRING PackageDescription"
       and packageName =
         flag "package-name" (required string) ~doc:"STRING PackageName"
       and packageType =
         flag "package-type" (required json_arg) ~doc:"JSON PackageType"
       and packageSource =
         flag "package-source" (required json_arg) ~doc:"JSON PackageSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_package
           (Values.CreatePackageRequest.make ?packageDescription ~packageName
              ~packageType:(Values.PackageType.of_json packageType)
              ~packageSource:(Values.PackageSource.of_json packageSource) ())
           (Some Values.CreatePackageResponse.to_json)
           (Some Values.CreatePackageResponse.error_to_json)])
let delete_elasticsearch_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_elasticsearch_domain
           (Values.DeleteElasticsearchDomainRequest.make ~domainName ())
           (Some Values.DeleteElasticsearchDomainResponse.to_json)
           (Some Values.DeleteElasticsearchDomainResponse.error_to_json)])
let delete_elasticsearch_service_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_elasticsearch_service_role (Fn.id ()) None None])
let delete_inbound_cross_cluster_search_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crossClusterSearchConnectionId =
         flag "cross-cluster-search-connection-id" (required string)
           ~doc:"STRING CrossClusterSearchConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_inbound_cross_cluster_search_connection
           (Values.DeleteInboundCrossClusterSearchConnectionRequest.make
              ~crossClusterSearchConnectionId ())
           (Some
              Values.DeleteInboundCrossClusterSearchConnectionResponse.to_json)
           (Some
              Values.DeleteInboundCrossClusterSearchConnectionResponse.error_to_json)])
let delete_outbound_cross_cluster_search_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crossClusterSearchConnectionId =
         flag "cross-cluster-search-connection-id" (required string)
           ~doc:"STRING CrossClusterSearchConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_outbound_cross_cluster_search_connection
           (Values.DeleteOutboundCrossClusterSearchConnectionRequest.make
              ~crossClusterSearchConnectionId ())
           (Some
              Values.DeleteOutboundCrossClusterSearchConnectionResponse.to_json)
           (Some
              Values.DeleteOutboundCrossClusterSearchConnectionResponse.error_to_json)])
let delete_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and packageID =
         flag "package-i-d" (required string) ~doc:"STRING PackageID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_package (Values.DeletePackageRequest.make ~packageID ())
           (Some Values.DeletePackageResponse.to_json)
           (Some Values.DeletePackageResponse.error_to_json)])
let describe_domain_auto_tunes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain_auto_tunes
           (Values.DescribeDomainAutoTunesRequest.make ?maxResults ?nextToken
              ~domainName ())
           (Some Values.DescribeDomainAutoTunesResponse.to_json)
           (Some Values.DescribeDomainAutoTunesResponse.error_to_json)])
let describe_domain_change_progress =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and changeId = flag "change-id" (optional string) ~doc:"STRING GUID"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain_change_progress
           (Values.DescribeDomainChangeProgressRequest.make ?changeId
              ~domainName ())
           (Some Values.DescribeDomainChangeProgressResponse.to_json)
           (Some Values.DescribeDomainChangeProgressResponse.error_to_json)])
let describe_elasticsearch_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_elasticsearch_domain
           (Values.DescribeElasticsearchDomainRequest.make ~domainName ())
           (Some Values.DescribeElasticsearchDomainResponse.to_json)
           (Some Values.DescribeElasticsearchDomainResponse.error_to_json)])
let describe_elasticsearch_domain_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_elasticsearch_domain_config
           (Values.DescribeElasticsearchDomainConfigRequest.make ~domainName
              ())
           (Some Values.DescribeElasticsearchDomainConfigResponse.to_json)
           (Some
              Values.DescribeElasticsearchDomainConfigResponse.error_to_json)])
let describe_elasticsearch_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNames =
         flag "domain-names" (required json_arg) ~doc:"JSON DomainNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_elasticsearch_domains
           (Values.DescribeElasticsearchDomainsRequest.make
              ~domainNames:(Values.DomainNameList.of_json domainNames) ())
           (Some Values.DescribeElasticsearchDomainsResponse.to_json)
           (Some Values.DescribeElasticsearchDomainsResponse.error_to_json)])
let describe_elasticsearch_instance_type_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName"
       and instanceType =
         flag "instance-type" (required json_arg)
           ~doc:"JSON ESPartitionInstanceType"
       and elasticsearchVersion =
         flag "elasticsearch-version" (required string)
           ~doc:"STRING ElasticsearchVersionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_elasticsearch_instance_type_limits
           (Values.DescribeElasticsearchInstanceTypeLimitsRequest.make
              ?domainName
              ~instanceType:(Values.ESPartitionInstanceType.of_json
                               instanceType) ~elasticsearchVersion ())
           (Some
              Values.DescribeElasticsearchInstanceTypeLimitsResponse.to_json)
           (Some
              Values.DescribeElasticsearchInstanceTypeLimitsResponse.error_to_json)])
let describe_inbound_cross_cluster_search_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_inbound_cross_cluster_search_connections
           (Values.DescribeInboundCrossClusterSearchConnectionsRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ())
           (Some
              Values.DescribeInboundCrossClusterSearchConnectionsResponse.to_json)
           (Some
              Values.DescribeInboundCrossClusterSearchConnectionsResponse.error_to_json)])
let describe_outbound_cross_cluster_search_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_outbound_cross_cluster_search_connections
           (Values.DescribeOutboundCrossClusterSearchConnectionsRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ())
           (Some
              Values.DescribeOutboundCrossClusterSearchConnectionsResponse.to_json)
           (Some
              Values.DescribeOutboundCrossClusterSearchConnectionsResponse.error_to_json)])
let describe_packages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON DescribePackagesFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_packages
           (Values.DescribePackagesRequest.make
              ?filters:(Option.map
                          ~f:Values.DescribePackagesFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.DescribePackagesResponse.to_json)
           (Some Values.DescribePackagesResponse.error_to_json)])
let describe_reserved_elasticsearch_instance_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedElasticsearchInstanceOfferingId =
         flag "reserved-elasticsearch-instance-offering-id" (optional string)
           ~doc:"STRING GUID"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_elasticsearch_instance_offerings
           (Values.DescribeReservedElasticsearchInstanceOfferingsRequest.make
              ?reservedElasticsearchInstanceOfferingId ?maxResults ?nextToken
              ())
           (Some
              Values.DescribeReservedElasticsearchInstanceOfferingsResponse.to_json)
           (Some
              Values.DescribeReservedElasticsearchInstanceOfferingsResponse.error_to_json)])
let describe_reserved_elasticsearch_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedElasticsearchInstanceId =
         flag "reserved-elasticsearch-instance-id" (optional string)
           ~doc:"STRING GUID"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_elasticsearch_instances
           (Values.DescribeReservedElasticsearchInstancesRequest.make
              ?reservedElasticsearchInstanceId ?maxResults ?nextToken ())
           (Some
              Values.DescribeReservedElasticsearchInstancesResponse.to_json)
           (Some
              Values.DescribeReservedElasticsearchInstancesResponse.error_to_json)])
let dissociate_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and packageID =
         flag "package-i-d" (required string) ~doc:"STRING PackageID"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.dissociate_package
           (Values.DissociatePackageRequest.make ~packageID ~domainName ())
           (Some Values.DissociatePackageResponse.to_json)
           (Some Values.DissociatePackageResponse.error_to_json)])
let get_compatible_elasticsearch_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_compatible_elasticsearch_versions
           (Values.GetCompatibleElasticsearchVersionsRequest.make ?domainName
              ())
           (Some Values.GetCompatibleElasticsearchVersionsResponse.to_json)
           (Some
              Values.GetCompatibleElasticsearchVersionsResponse.error_to_json)])
let get_package_version_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and packageID =
         flag "package-i-d" (required string) ~doc:"STRING PackageID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_package_version_history
           (Values.GetPackageVersionHistoryRequest.make ?maxResults
              ?nextToken ~packageID ())
           (Some Values.GetPackageVersionHistoryResponse.to_json)
           (Some Values.GetPackageVersionHistoryResponse.error_to_json)])
let get_upgrade_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_upgrade_history
           (Values.GetUpgradeHistoryRequest.make ?maxResults ?nextToken
              ~domainName ()) (Some Values.GetUpgradeHistoryResponse.to_json)
           (Some Values.GetUpgradeHistoryResponse.error_to_json)])
let get_upgrade_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_upgrade_status
           (Values.GetUpgradeStatusRequest.make ~domainName ())
           (Some Values.GetUpgradeStatusResponse.to_json)
           (Some Values.GetUpgradeStatusResponse.error_to_json)])
let list_domain_names =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engineType =
         flag "engine-type" (optional json_arg) ~doc:"JSON EngineType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domain_names
           (Values.ListDomainNamesRequest.make
              ?engineType:(Option.map ~f:Values.EngineType.of_json engineType)
              ()) (Some Values.ListDomainNamesResponse.to_json)
           (Some Values.ListDomainNamesResponse.error_to_json)])
let list_domains_for_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and packageID =
         flag "package-i-d" (required string) ~doc:"STRING PackageID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domains_for_package
           (Values.ListDomainsForPackageRequest.make ?maxResults ?nextToken
              ~packageID ())
           (Some Values.ListDomainsForPackageResponse.to_json)
           (Some Values.ListDomainsForPackageResponse.error_to_json)])
let list_elasticsearch_instance_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and elasticsearchVersion =
         flag "elasticsearch-version" (required string)
           ~doc:"STRING ElasticsearchVersionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_elasticsearch_instance_types
           (Values.ListElasticsearchInstanceTypesRequest.make ?domainName
              ?maxResults ?nextToken ~elasticsearchVersion ())
           (Some Values.ListElasticsearchInstanceTypesResponse.to_json)
           (Some Values.ListElasticsearchInstanceTypesResponse.error_to_json)])
let list_elasticsearch_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_elasticsearch_versions
           (Values.ListElasticsearchVersionsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListElasticsearchVersionsResponse.to_json)
           (Some Values.ListElasticsearchVersionsResponse.error_to_json)])
let list_packages_for_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_packages_for_domain
           (Values.ListPackagesForDomainRequest.make ?maxResults ?nextToken
              ~domainName ())
           (Some Values.ListPackagesForDomainResponse.to_json)
           (Some Values.ListPackagesForDomainResponse.error_to_json)])
let list_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aRN = flag "a-r-n" (required string) ~doc:"STRING ARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags (Values.ListTagsRequest.make ~aRN ())
           (Some Values.ListTagsResponse.to_json)
           (Some Values.ListTagsResponse.error_to_json)])
let purchase_reserved_elasticsearch_instance_offering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceCount =
         flag "instance-count" (optional int) ~doc:"INT InstanceCount"
       and reservedElasticsearchInstanceOfferingId =
         flag "reserved-elasticsearch-instance-offering-id" (required string)
           ~doc:"STRING GUID"
       and reservationName =
         flag "reservation-name" (required string)
           ~doc:"STRING ReservationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.purchase_reserved_elasticsearch_instance_offering
           (Values.PurchaseReservedElasticsearchInstanceOfferingRequest.make
              ?instanceCount ~reservedElasticsearchInstanceOfferingId
              ~reservationName ())
           (Some
              Values.PurchaseReservedElasticsearchInstanceOfferingResponse.to_json)
           (Some
              Values.PurchaseReservedElasticsearchInstanceOfferingResponse.error_to_json)])
let reject_inbound_cross_cluster_search_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crossClusterSearchConnectionId =
         flag "cross-cluster-search-connection-id" (required string)
           ~doc:"STRING CrossClusterSearchConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_inbound_cross_cluster_search_connection
           (Values.RejectInboundCrossClusterSearchConnectionRequest.make
              ~crossClusterSearchConnectionId ())
           (Some
              Values.RejectInboundCrossClusterSearchConnectionResponse.to_json)
           (Some
              Values.RejectInboundCrossClusterSearchConnectionResponse.error_to_json)])
let remove_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aRN = flag "a-r-n" (required string) ~doc:"STRING ARN"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_tags
           (Values.RemoveTagsRequest.make ~aRN
              ~tagKeys:(Values.StringList.of_json tagKeys) ()) None None])
let start_elasticsearch_service_software_update =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_elasticsearch_service_software_update
           (Values.StartElasticsearchServiceSoftwareUpdateRequest.make
              ~domainName ())
           (Some
              Values.StartElasticsearchServiceSoftwareUpdateResponse.to_json)
           (Some
              Values.StartElasticsearchServiceSoftwareUpdateResponse.error_to_json)])
let update_elasticsearch_domain_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and elasticsearchClusterConfig =
         flag "elasticsearch-cluster-config" (optional json_arg)
           ~doc:"JSON ElasticsearchClusterConfig"
       and eBSOptions =
         flag "e-b-s-options" (optional json_arg) ~doc:"JSON EBSOptions"
       and snapshotOptions =
         flag "snapshot-options" (optional json_arg)
           ~doc:"JSON SnapshotOptions"
       and vPCOptions =
         flag "v-p-c-options" (optional json_arg) ~doc:"JSON VPCOptions"
       and cognitoOptions =
         flag "cognito-options" (optional json_arg)
           ~doc:"JSON CognitoOptions"
       and advancedOptions =
         flag "advanced-options" (optional json_arg)
           ~doc:"JSON AdvancedOptions"
       and accessPolicies =
         flag "access-policies" (optional string)
           ~doc:"STRING PolicyDocument"
       and logPublishingOptions =
         flag "log-publishing-options" (optional json_arg)
           ~doc:"JSON LogPublishingOptions"
       and domainEndpointOptions =
         flag "domain-endpoint-options" (optional json_arg)
           ~doc:"JSON DomainEndpointOptions"
       and advancedSecurityOptions =
         flag "advanced-security-options" (optional json_arg)
           ~doc:"JSON AdvancedSecurityOptionsInput"
       and nodeToNodeEncryptionOptions =
         flag "node-to-node-encryption-options" (optional json_arg)
           ~doc:"JSON NodeToNodeEncryptionOptions"
       and encryptionAtRestOptions =
         flag "encryption-at-rest-options" (optional json_arg)
           ~doc:"JSON EncryptionAtRestOptions"
       and autoTuneOptions =
         flag "auto-tune-options" (optional json_arg)
           ~doc:"JSON AutoTuneOptions"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL DryRun"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_elasticsearch_domain_config
           (Values.UpdateElasticsearchDomainConfigRequest.make
              ?elasticsearchClusterConfig:(Option.map
                                             ~f:Values.ElasticsearchClusterConfig.of_json
                                             elasticsearchClusterConfig)
              ?eBSOptions:(Option.map ~f:Values.EBSOptions.of_json eBSOptions)
              ?snapshotOptions:(Option.map ~f:Values.SnapshotOptions.of_json
                                  snapshotOptions)
              ?vPCOptions:(Option.map ~f:Values.VPCOptions.of_json vPCOptions)
              ?cognitoOptions:(Option.map ~f:Values.CognitoOptions.of_json
                                 cognitoOptions)
              ?advancedOptions:(Option.map ~f:Values.AdvancedOptions.of_json
                                  advancedOptions) ?accessPolicies
              ?logPublishingOptions:(Option.map
                                       ~f:Values.LogPublishingOptions.of_json
                                       logPublishingOptions)
              ?domainEndpointOptions:(Option.map
                                        ~f:Values.DomainEndpointOptions.of_json
                                        domainEndpointOptions)
              ?advancedSecurityOptions:(Option.map
                                          ~f:Values.AdvancedSecurityOptionsInput.of_json
                                          advancedSecurityOptions)
              ?nodeToNodeEncryptionOptions:(Option.map
                                              ~f:Values.NodeToNodeEncryptionOptions.of_json
                                              nodeToNodeEncryptionOptions)
              ?encryptionAtRestOptions:(Option.map
                                          ~f:Values.EncryptionAtRestOptions.of_json
                                          encryptionAtRestOptions)
              ?autoTuneOptions:(Option.map ~f:Values.AutoTuneOptions.of_json
                                  autoTuneOptions) ?dryRun ~domainName ())
           (Some Values.UpdateElasticsearchDomainConfigResponse.to_json)
           (Some Values.UpdateElasticsearchDomainConfigResponse.error_to_json)])
let update_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and packageDescription =
         flag "package-description" (optional string)
           ~doc:"STRING PackageDescription"
       and commitMessage =
         flag "commit-message" (optional string) ~doc:"STRING CommitMessage"
       and packageID =
         flag "package-i-d" (required string) ~doc:"STRING PackageID"
       and packageSource =
         flag "package-source" (required json_arg) ~doc:"JSON PackageSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_package
           (Values.UpdatePackageRequest.make ?packageDescription
              ?commitMessage ~packageID
              ~packageSource:(Values.PackageSource.of_json packageSource) ())
           (Some Values.UpdatePackageResponse.to_json)
           (Some Values.UpdatePackageResponse.error_to_json)])
let upgrade_elasticsearch_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and performCheckOnly =
         flag "perform-check-only" (optional bool) ~doc:"BOOL Boolean"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and targetVersion =
         flag "target-version" (required string)
           ~doc:"STRING ElasticsearchVersionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upgrade_elasticsearch_domain
           (Values.UpgradeElasticsearchDomainRequest.make ?performCheckOnly
              ~domainName ~targetVersion ())
           (Some Values.UpgradeElasticsearchDomainResponse.to_json)
           (Some Values.UpgradeElasticsearchDomainResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-inbound-cross-cluster-search-connection",
       accept_inbound_cross_cluster_search_connection);
    ("add-tags", add_tags);
    ("associate-package", associate_package);
    ("cancel-elasticsearch-service-software-update",
      cancel_elasticsearch_service_software_update);
    ("create-elasticsearch-domain", create_elasticsearch_domain);
    ("create-outbound-cross-cluster-search-connection",
      create_outbound_cross_cluster_search_connection);
    ("create-package", create_package);
    ("delete-elasticsearch-domain", delete_elasticsearch_domain);
    ("delete-elasticsearch-service-role", delete_elasticsearch_service_role);
    ("delete-inbound-cross-cluster-search-connection",
      delete_inbound_cross_cluster_search_connection);
    ("delete-outbound-cross-cluster-search-connection",
      delete_outbound_cross_cluster_search_connection);
    ("delete-package", delete_package);
    ("describe-domain-auto-tunes", describe_domain_auto_tunes);
    ("describe-domain-change-progress", describe_domain_change_progress);
    ("describe-elasticsearch-domain", describe_elasticsearch_domain);
    ("describe-elasticsearch-domain-config",
      describe_elasticsearch_domain_config);
    ("describe-elasticsearch-domains", describe_elasticsearch_domains);
    ("describe-elasticsearch-instance-type-limits",
      describe_elasticsearch_instance_type_limits);
    ("describe-inbound-cross-cluster-search-connections",
      describe_inbound_cross_cluster_search_connections);
    ("describe-outbound-cross-cluster-search-connections",
      describe_outbound_cross_cluster_search_connections);
    ("describe-packages", describe_packages);
    ("describe-reserved-elasticsearch-instance-offerings",
      describe_reserved_elasticsearch_instance_offerings);
    ("describe-reserved-elasticsearch-instances",
      describe_reserved_elasticsearch_instances);
    ("dissociate-package", dissociate_package);
    ("get-compatible-elasticsearch-versions",
      get_compatible_elasticsearch_versions);
    ("get-package-version-history", get_package_version_history);
    ("get-upgrade-history", get_upgrade_history);
    ("get-upgrade-status", get_upgrade_status);
    ("list-domain-names", list_domain_names);
    ("list-domains-for-package", list_domains_for_package);
    ("list-elasticsearch-instance-types", list_elasticsearch_instance_types);
    ("list-elasticsearch-versions", list_elasticsearch_versions);
    ("list-packages-for-domain", list_packages_for_domain);
    ("list-tags", list_tags);
    ("purchase-reserved-elasticsearch-instance-offering",
      purchase_reserved_elasticsearch_instance_offering);
    ("reject-inbound-cross-cluster-search-connection",
      reject_inbound_cross_cluster_search_connection);
    ("remove-tags", remove_tags);
    ("start-elasticsearch-service-software-update",
      start_elasticsearch_service_software_update);
    ("update-elasticsearch-domain-config",
      update_elasticsearch_domain_config);
    ("update-package", update_package);
    ("upgrade-elasticsearch-domain", upgrade_elasticsearch_domain)]
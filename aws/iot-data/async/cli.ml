(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let delete_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cleanSession =
         flag "clean-session" (optional bool) ~doc:"BOOL CleanSession"
       and preventWillMessage =
         flag "prevent-will-message" (optional bool)
           ~doc:"BOOL PreventWillMessage"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connection
           (Values.DeleteConnectionRequest.make ?cleanSession
              ?preventWillMessage ~clientId ()) None None])
let delete_thing_shadow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shadowName =
         flag "shadow-name" (optional string) ~doc:"STRING ShadowName"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_thing_shadow
           (Values.DeleteThingShadowRequest.make ?shadowName ~thingName ())
           (Some Values.DeleteThingShadowResponse.to_json)
           (Some Values.DeleteThingShadowResponse.error_to_json)])
let get_retained_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and topic = flag "topic" (required string) ~doc:"STRING Topic" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_retained_message
           (Values.GetRetainedMessageRequest.make ~topic ())
           (Some Values.GetRetainedMessageResponse.to_json)
           (Some Values.GetRetainedMessageResponse.error_to_json)])
let get_thing_shadow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shadowName =
         flag "shadow-name" (optional string) ~doc:"STRING ShadowName"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_thing_shadow
           (Values.GetThingShadowRequest.make ?shadowName ~thingName ())
           (Some Values.GetThingShadowResponse.to_json)
           (Some Values.GetThingShadowResponse.error_to_json)])
let list_named_shadows_for_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_named_shadows_for_thing
           (Values.ListNamedShadowsForThingRequest.make ?nextToken ?pageSize
              ~thingName ())
           (Some Values.ListNamedShadowsForThingResponse.to_json)
           (Some Values.ListNamedShadowsForThingResponse.error_to_json)])
let list_retained_messages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_retained_messages
           (Values.ListRetainedMessagesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListRetainedMessagesResponse.to_json)
           (Some Values.ListRetainedMessagesResponse.error_to_json)])
let publish =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qos = flag "qos" (optional int) ~doc:"INT Qos"
       and retain = flag "retain" (optional bool) ~doc:"BOOL Retain"
       and payload = flag "payload" (optional json_arg) ~doc:"JSON Payload"
       and userProperties =
         flag "user-properties" (optional string)
           ~doc:"STRING UserProperties"
       and payloadFormatIndicator =
         flag "payload-format-indicator" (optional json_arg)
           ~doc:"JSON PayloadFormatIndicator"
       and contentType =
         flag "content-type" (optional string) ~doc:"STRING ContentType"
       and responseTopic =
         flag "response-topic" (optional string) ~doc:"STRING ResponseTopic"
       and correlationData =
         flag "correlation-data" (optional string)
           ~doc:"STRING CorrelationData"
       and messageExpiry =
         flag "message-expiry" (optional json_arg) ~doc:"JSON MessageExpiry"
       and topic = flag "topic" (required string) ~doc:"STRING Topic" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.publish
           (Values.PublishRequest.make ?qos ?retain
              ?payload:(Option.map ~f:Values.Payload.of_json payload)
              ?userProperties
              ?payloadFormatIndicator:(Option.map
                                         ~f:Values.PayloadFormatIndicator.of_json
                                         payloadFormatIndicator) ?contentType
              ?responseTopic ?correlationData
              ?messageExpiry:(Option.map ~f:Values.MessageExpiry.of_json
                                messageExpiry) ~topic ()) None None])
let update_thing_shadow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shadowName =
         flag "shadow-name" (optional string) ~doc:"STRING ShadowName"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName"
       and payload =
         flag "payload" (required json_arg) ~doc:"JSON JsonDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_thing_shadow
           (Values.UpdateThingShadowRequest.make ?shadowName ~thingName
              ~payload:(Values.JsonDocument.of_json payload) ())
           (Some Values.UpdateThingShadowResponse.to_json)
           (Some Values.UpdateThingShadowResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("delete-connection", delete_connection);
    ("delete-thing-shadow", delete_thing_shadow);
    ("get-retained-message", get_retained_message);
    ("get-thing-shadow", get_thing_shadow);
    ("list-named-shadows-for-thing", list_named_shadows_for_thing);
    ("list-retained-messages", list_retained_messages);
    ("publish", publish);
    ("update-thing-shadow", update_thing_shadow)]
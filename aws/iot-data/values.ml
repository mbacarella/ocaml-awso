(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.iot_data
let apiVersion = "2015-05-28"
let endpointPrefix = "data-ats.iot"
let serviceFullName = "AWS IoT Data Plane"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module PayloadSize =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module Qos =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Qos" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module Topic =
  struct
    type nonrec t = string
    let context_ = "Topic"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Topic" j
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "errorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"errorMessage" j
    let to_json = simple_to_json to_value
  end
module RetainedMessageSummary =
  struct
    type nonrec t =
      {
      topic: Topic.t option
        [@ocaml.doc
          "The topic name to which the retained message was published."];
      payloadSize: PayloadSize.t option
        [@ocaml.doc "The size of the retained message's payload in bytes."];
      qos: Qos.t option
        [@ocaml.doc
          "The quality of service (QoS) level used to publish the retained message."];
      lastModifiedTime: Timestamp.t option
        [@ocaml.doc
          "The Epoch date and time, in milliseconds, when the retained message was stored by IoT."]}
    let make ?topic =
      fun ?payloadSize ->
        fun ?qos ->
          fun ?lastModifiedTime ->
            fun () -> { topic; payloadSize; qos; lastModifiedTime }
    let to_value x =
      structure_to_value
        [("topic", (Option.map x.topic ~f:Topic.to_value));
        ("payloadSize", (Option.map x.payloadSize ~f:PayloadSize.to_value));
        ("qos", (Option.map x.qos ~f:Qos.to_value));
        ("lastModifiedTime",
          (Option.map x.lastModifiedTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastModifiedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastModifiedTime") in
      let qos = (Option.map ~f:Qos.of_xml) (Xml.child xml_arg0 "qos") in
      let payloadSize =
        (Option.map ~f:PayloadSize.of_xml) (Xml.child xml_arg0 "payloadSize") in
      let topic = (Option.map ~f:Topic.of_xml) (Xml.child xml_arg0 "topic") in
      make ?lastModifiedTime ?qos ?payloadSize ?topic ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastModifiedTime =
        field_map json "lastModifiedTime" Timestamp.of_json in
      let qos = field_map json "qos" Qos.of_json in
      let payloadSize = field_map json "payloadSize" PayloadSize.of_json in
      let topic = field_map json "topic" Topic.of_json in
      make ?lastModifiedTime ?qos ?payloadSize ?topic ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a single retained message."]
module ShadowName =
  struct
    type nonrec t = string
    let context_ = "ShadowName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9:_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ShadowName" j
    let to_json = simple_to_json to_value
  end
module ConflictException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc "The message for the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified version does not match the version of the document."]
module InternalFailureException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc "The message for the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An unexpected error has occurred."]
module InvalidRequestException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc "The message for the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request is not valid."]
module JsonDocument =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module MethodNotAllowedException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc "The message for the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified combination of HTTP verb and URI is not supported."]
module RequestEntityTooLargeException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc "The message for the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The payload exceeds the maximum size allowed."]
module ServiceUnavailableException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc "The message for the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The service is temporarily unavailable."]
module ThrottlingException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc "The message for the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The rate exceeds the limit."]
module UnauthorizedException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc "The message for the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "You are not authorized to perform this operation."]
module UnsupportedDocumentEncodingException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc "The message for the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The document encoding is not supported."]
module ThingName =
  struct
    type nonrec t = string
    let context_ = "ThingName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9:_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ThingName" j
    let to_json = simple_to_json to_value
  end
module Payload =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module Retain =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module RetainedMessageList =
  struct
    type nonrec t = RetainedMessageSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:RetainedMessageSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RetainedMessageSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"RetainedMessageList"
        ~of_json:RetainedMessageSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:200) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module NamedShadowList =
  struct
    type nonrec t = ShadowName.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ShadowName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ShadowName.of_xml)
    let of_json j =
      list_of_json ~kind:"NamedShadowList" ~of_json:ShadowName.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc "The message for the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified resource does not exist."]
module PageSize =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for PageSize" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module UpdateThingShadowResponse =
  struct
    type nonrec t =
      {
      payload: JsonDocument.t option
        [@ocaml.doc "The state information, in JSON format."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `RequestEntityTooLargeException of RequestEntityTooLargeException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedDocumentEncodingException of
          UnsupportedDocumentEncodingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?payload = fun () -> { payload }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "RequestEntityTooLargeException" ->
          `RequestEntityTooLargeException
            (RequestEntityTooLargeException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedDocumentEncodingException" ->
          `UnsupportedDocumentEncodingException
            (UnsupportedDocumentEncodingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "RequestEntityTooLargeException" ->
          `RequestEntityTooLargeException
            (RequestEntityTooLargeException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedDocumentEncodingException" ->
          `UnsupportedDocumentEncodingException
            (UnsupportedDocumentEncodingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `RequestEntityTooLargeException e ->
          `Assoc
            [("error", (`String "RequestEntityTooLargeException"));
            ("details", (RequestEntityTooLargeException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedDocumentEncodingException e ->
          `Assoc
            [("error", (`String "UnsupportedDocumentEncodingException"));
            ("details", (UnsupportedDocumentEncodingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) -> make ?payload:(Some pipe) ())[@warning "-27"])
    let to_value x =
      structure_to_value
        [("payload", (Option.map x.payload ~f:JsonDocument.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let payload =
        (Option.map ~f:JsonDocument.of_xml) (Xml.child xml_arg0 "payload") in
      make ?payload ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let payload = field_map json "payload" JsonDocument.of_json in
      make ?payload ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output from the UpdateThingShadow operation."]
module UpdateThingShadowRequest =
  struct
    type nonrec t =
      {
      thingName: ThingName.t [@ocaml.doc "The name of the thing."];
      shadowName: ShadowName.t option [@ocaml.doc "The name of the shadow."];
      payload: JsonDocument.t
        [@ocaml.doc "The state information, in JSON format."]}
    let context_ = "UpdateThingShadowRequest"
    let make ?shadowName =
      fun ~thingName ->
        fun ~payload -> fun () -> { shadowName; thingName; payload }
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ~thingName:(ThingName.of_string
                          ((List.Assoc.find_exn ~equal:String.Caseless.equal)
                             xs "thingName"))
            ?shadowName:(Option.map
                           ((List.Assoc.find ~equal:String.Caseless.equal) xs
                              "name") ~f:ShadowName.of_string) ~payload:pipe
            ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("thingName", (Some (ThingName.to_value x.thingName)));
        ("name", (Option.map x.shadowName ~f:ShadowName.to_value));
        ("payload", (Some (JsonDocument.to_value x.payload)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let payload =
        JsonDocument.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "payload") in
      let shadowName =
        (Option.map ~f:ShadowName.of_xml) (Xml.child xml_arg0 "name") in
      let thingName =
        ThingName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "thingName") in
      make ~payload ?shadowName ~thingName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let payload = field_map_exn json "payload" JsonDocument.of_json in
      let shadowName = field_map json "shadowName" ShadowName.of_json in
      let thingName = field_map_exn json "thingName" ThingName.of_json in
      make ~payload ?shadowName ~thingName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The input for the UpdateThingShadow operation."]
module PublishRequest =
  struct
    type nonrec t =
      {
      topic: Topic.t [@ocaml.doc "The name of the MQTT topic."];
      qos: Qos.t option [@ocaml.doc "The Quality of Service (QoS) level."];
      retain: Retain.t option
        [@ocaml.doc
          "A Boolean value that determines whether to set the RETAIN flag when the message is published. Setting the RETAIN flag causes the message to be retained and sent to new subscribers to the topic. Valid values: true | false Default value: false"];
      payload: Payload.t option
        [@ocaml.doc
          "The message body. MQTT accepts text, binary, and empty (null) message payloads. Publishing an empty (null) payload with retain = true deletes the retained message identified by topic from Amazon Web Services IoT Core."]}
    let context_ = "PublishRequest"
    let make ?qos =
      fun ?retain ->
        fun ?payload ->
          fun ~topic -> fun () -> { qos; retain; payload; topic }
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ~topic:(Topic.of_string
                      ((List.Assoc.find_exn ~equal:String.Caseless.equal) xs
                         "topic"))
            ?qos:(Option.map
                    ((List.Assoc.find ~equal:String.Caseless.equal) xs "qos")
                    ~f:Qos.of_string)
            ?retain:(Option.map
                       ((List.Assoc.find ~equal:String.Caseless.equal) xs
                          "retain") ~f:Retain.of_string) ?payload:(Some pipe)
            ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("topic", (Some (Topic.to_value x.topic)));
        ("qos", (Option.map x.qos ~f:Qos.to_value));
        ("retain", (Option.map x.retain ~f:Retain.to_value));
        ("payload", (Option.map x.payload ~f:Payload.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let payload =
        (Option.map ~f:Payload.of_xml) (Xml.child xml_arg0 "payload") in
      let retain =
        (Option.map ~f:Retain.of_xml) (Xml.child xml_arg0 "retain") in
      let qos = (Option.map ~f:Qos.of_xml) (Xml.child xml_arg0 "qos") in
      let topic =
        Topic.of_xml (Xml.child_exn ~context:context_ xml_arg0 "topic") in
      make ?payload ?retain ?qos ~topic ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let payload = field_map json "payload" Payload.of_json in
      let retain = field_map json "retain" Retain.of_json in
      let qos = field_map json "qos" Qos.of_json in
      let topic = field_map_exn json "topic" Topic.of_json in
      make ?payload ?retain ?qos ~topic ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The input for the Publish operation."]
module ListRetainedMessagesResponse =
  struct
    type nonrec t =
      {
      retainedTopics: RetainedMessageList.t option
        [@ocaml.doc
          "A summary list the account's retained messages. The information returned doesn't include the message payloads of the retained messages."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token for the next set of results, or null if there are no additional results."]}
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?retainedTopics =
      fun ?nextToken -> fun () -> { retainedTopics; nextToken }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("retainedTopics",
           (Option.map x.retainedTopics ~f:RetainedMessageList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let retainedTopics =
        (Option.map ~f:RetainedMessageList.of_xml)
          (Xml.child xml_arg0 "retainedTopics") in
      make ?nextToken ?retainedTopics ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let retainedTopics =
        field_map json "retainedTopics" RetainedMessageList.of_json in
      make ?nextToken ?retainedTopics ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists summary information about the retained messages stored for the account. This action returns only the topic names of the retained messages. It doesn't return any message payloads. Although this action doesn't return a message payload, it can still incur messaging costs. To get the message payload of a retained message, call GetRetainedMessage with the topic name of the retained message. Requires permission to access the ListRetainedMessages action. For more information about messaging costs, see Amazon Web Services IoT Core pricing - Messaging."]
module ListRetainedMessagesRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "To retrieve the next set of results, the nextToken value from a previous response; otherwise null to receive the first set of results."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of results to return at one time."]}
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists summary information about the retained messages stored for the account. This action returns only the topic names of the retained messages. It doesn't return any message payloads. Although this action doesn't return a message payload, it can still incur messaging costs. To get the message payload of a retained message, call GetRetainedMessage with the topic name of the retained message. Requires permission to access the ListRetainedMessages action. For more information about messaging costs, see Amazon Web Services IoT Core pricing - Messaging."]
module ListNamedShadowsForThingResponse =
  struct
    type nonrec t =
      {
      results: NamedShadowList.t option
        [@ocaml.doc "The list of shadows for the specified thing."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to get the next set of results, or null if there are no additional results."];
      timestamp: Timestamp.t option
        [@ocaml.doc
          "The Epoch date and time the response was generated by IoT."]}
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?results =
      fun ?nextToken ->
        fun ?timestamp -> fun () -> { results; nextToken; timestamp }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("results", (Option.map x.results ~f:NamedShadowList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("timestamp", (Option.map x.timestamp ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let results =
        (Option.map ~f:NamedShadowList.of_xml) (Xml.child xml_arg0 "results") in
      make ?timestamp ?nextToken ?results ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let timestamp = field_map json "timestamp" Timestamp.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let results = field_map json "results" NamedShadowList.of_json in
      make ?timestamp ?nextToken ?results ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the shadows for the specified thing. Requires permission to access the ListNamedShadowsForThing action."]
module ListNamedShadowsForThingRequest =
  struct
    type nonrec t =
      {
      thingName: ThingName.t [@ocaml.doc "The name of the thing."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token to retrieve the next set of results."];
      pageSize: PageSize.t option [@ocaml.doc "The result page size."]}
    let context_ = "ListNamedShadowsForThingRequest"
    let make ?nextToken =
      fun ?pageSize ->
        fun ~thingName -> fun () -> { nextToken; pageSize; thingName }
    let to_value x =
      structure_to_value
        [("thingName", (Some (ThingName.to_value x.thingName)));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("pageSize", (Option.map x.pageSize ~f:PageSize.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "pageSize") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let thingName =
        ThingName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "thingName") in
      make ?pageSize ?nextToken ~thingName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let pageSize = field_map json "pageSize" PageSize.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let thingName = field_map_exn json "thingName" ThingName.of_json in
      make ?pageSize ?nextToken ~thingName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the shadows for the specified thing. Requires permission to access the ListNamedShadowsForThing action."]
module GetThingShadowResponse =
  struct
    type nonrec t =
      {
      payload: JsonDocument.t option
        [@ocaml.doc "The state information, in JSON format."]}
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedDocumentEncodingException of
          UnsupportedDocumentEncodingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?payload = fun () -> { payload }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedDocumentEncodingException" ->
          `UnsupportedDocumentEncodingException
            (UnsupportedDocumentEncodingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedDocumentEncodingException" ->
          `UnsupportedDocumentEncodingException
            (UnsupportedDocumentEncodingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedDocumentEncodingException e ->
          `Assoc
            [("error", (`String "UnsupportedDocumentEncodingException"));
            ("details", (UnsupportedDocumentEncodingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) -> make ?payload:(Some pipe) ())[@warning "-27"])
    let to_value x =
      structure_to_value
        [("payload", (Option.map x.payload ~f:JsonDocument.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let payload =
        (Option.map ~f:JsonDocument.of_xml) (Xml.child xml_arg0 "payload") in
      make ?payload ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let payload = field_map json "payload" JsonDocument.of_json in
      make ?payload ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output from the GetThingShadow operation."]
module GetThingShadowRequest =
  struct
    type nonrec t =
      {
      thingName: ThingName.t [@ocaml.doc "The name of the thing."];
      shadowName: ShadowName.t option [@ocaml.doc "The name of the shadow."]}
    let context_ = "GetThingShadowRequest"
    let make ?shadowName =
      fun ~thingName -> fun () -> { shadowName; thingName }
    let to_value x =
      structure_to_value
        [("thingName", (Some (ThingName.to_value x.thingName)));
        ("name", (Option.map x.shadowName ~f:ShadowName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shadowName =
        (Option.map ~f:ShadowName.of_xml) (Xml.child xml_arg0 "name") in
      let thingName =
        ThingName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "thingName") in
      make ?shadowName ~thingName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let shadowName = field_map json "shadowName" ShadowName.of_json in
      let thingName = field_map_exn json "thingName" ThingName.of_json in
      make ?shadowName ~thingName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The input for the GetThingShadow operation."]
module GetRetainedMessageResponse =
  struct
    type nonrec t =
      {
      topic: Topic.t option
        [@ocaml.doc
          "The topic name to which the retained message was published."];
      payload: Payload.t option
        [@ocaml.doc
          "The Base64-encoded message payload of the retained message body."];
      qos: Qos.t option
        [@ocaml.doc
          "The quality of service (QoS) level used to publish the retained message."];
      lastModifiedTime: Timestamp.t option
        [@ocaml.doc
          "The Epoch date and time, in milliseconds, when the retained message was stored by IoT."]}
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?topic =
      fun ?payload ->
        fun ?qos ->
          fun ?lastModifiedTime ->
            fun () -> { topic; payload; qos; lastModifiedTime }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("topic", (Option.map x.topic ~f:Topic.to_value));
        ("payload", (Option.map x.payload ~f:Payload.to_value));
        ("qos", (Option.map x.qos ~f:Qos.to_value));
        ("lastModifiedTime",
          (Option.map x.lastModifiedTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastModifiedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastModifiedTime") in
      let qos = (Option.map ~f:Qos.of_xml) (Xml.child xml_arg0 "qos") in
      let payload =
        (Option.map ~f:Payload.of_xml) (Xml.child xml_arg0 "payload") in
      let topic = (Option.map ~f:Topic.of_xml) (Xml.child xml_arg0 "topic") in
      make ?lastModifiedTime ?qos ?payload ?topic ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastModifiedTime =
        field_map json "lastModifiedTime" Timestamp.of_json in
      let qos = field_map json "qos" Qos.of_json in
      let payload = field_map json "payload" Payload.of_json in
      let topic = field_map json "topic" Topic.of_json in
      make ?lastModifiedTime ?qos ?payload ?topic ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output from the GetRetainedMessage operation."]
module GetRetainedMessageRequest =
  struct
    type nonrec t =
      {
      topic: Topic.t
        [@ocaml.doc "The topic name of the retained message to retrieve."]}
    let context_ = "GetRetainedMessageRequest"
    let make ~topic = fun () -> { topic }
    let to_value x =
      structure_to_value [("topic", (Some (Topic.to_value x.topic)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let topic =
        Topic.of_xml (Xml.child_exn ~context:context_ xml_arg0 "topic") in
      make ~topic ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let topic = field_map_exn json "topic" Topic.of_json in make ~topic ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The input for the GetRetainedMessage operation."]
module DeleteThingShadowResponse =
  struct
    type nonrec t =
      {
      payload: JsonDocument.t
        [@ocaml.doc "The state information, in JSON format."]}
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `UnsupportedDocumentEncodingException of
          UnsupportedDocumentEncodingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DeleteThingShadowResponse"
    let make ~payload = fun () -> { payload }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | "UnsupportedDocumentEncodingException" ->
          `UnsupportedDocumentEncodingException
            (UnsupportedDocumentEncodingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | "UnsupportedDocumentEncodingException" ->
          `UnsupportedDocumentEncodingException
            (UnsupportedDocumentEncodingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `UnsupportedDocumentEncodingException e ->
          `Assoc
            [("error", (`String "UnsupportedDocumentEncodingException"));
            ("details", (UnsupportedDocumentEncodingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ~payload:pipe ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("payload", (Some (JsonDocument.to_value x.payload)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let payload =
        JsonDocument.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "payload") in
      make ~payload ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let payload = field_map_exn json "payload" JsonDocument.of_json in
      make ~payload ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output from the DeleteThingShadow operation."]
module DeleteThingShadowRequest =
  struct
    type nonrec t =
      {
      thingName: ThingName.t [@ocaml.doc "The name of the thing."];
      shadowName: ShadowName.t option [@ocaml.doc "The name of the shadow."]}
    let context_ = "DeleteThingShadowRequest"
    let make ?shadowName =
      fun ~thingName -> fun () -> { shadowName; thingName }
    let to_value x =
      structure_to_value
        [("thingName", (Some (ThingName.to_value x.thingName)));
        ("name", (Option.map x.shadowName ~f:ShadowName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shadowName =
        (Option.map ~f:ShadowName.of_xml) (Xml.child xml_arg0 "name") in
      let thingName =
        ThingName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "thingName") in
      make ?shadowName ~thingName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let shadowName = field_map json "shadowName" ShadowName.of_json in
      let thingName = field_map_exn json "thingName" ThingName.of_json in
      make ?shadowName ~thingName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The input for the DeleteThingShadow operation."]
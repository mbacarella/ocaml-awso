(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | DeleteThingShadow: (DeleteThingShadowRequest.t,
  DeleteThingShadowResponse.t, DeleteThingShadowResponse.error) t 
  | GetRetainedMessage: (GetRetainedMessageRequest.t,
  GetRetainedMessageResponse.t, GetRetainedMessageResponse.error) t 
  | GetThingShadow: (GetThingShadowRequest.t, GetThingShadowResponse.t,
  GetThingShadowResponse.error) t 
  | ListNamedShadowsForThing: (ListNamedShadowsForThingRequest.t,
  ListNamedShadowsForThingResponse.t, ListNamedShadowsForThingResponse.error)
  t 
  | ListRetainedMessages: (ListRetainedMessagesRequest.t,
  ListRetainedMessagesResponse.t, ListRetainedMessagesResponse.error) t 
  | Publish: (PublishRequest.t, unit, unit) t 
  | UpdateThingShadow: (UpdateThingShadowRequest.t,
  UpdateThingShadowResponse.t, UpdateThingShadowResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | DeleteThingShadow -> `DELETE
  | GetRetainedMessage -> `GET
  | GetThingShadow -> `GET
  | ListNamedShadowsForThing -> `GET
  | ListRetainedMessages -> `GET
  | Publish -> `POST
  | UpdateThingShadow -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | DeleteThingShadow ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/things/%s/shadow"
               (ThingName.to_header x.DeleteThingShadowRequest.thingName))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("name", (ShadowName.to_header v)))
                  x.shadowName])
      | GetRetainedMessage ->
          (Format.kasprintf Uri.of_string) "/retainedMessage/%s"
            (Topic.to_header x.GetRetainedMessageRequest.topic)
      | GetThingShadow ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/things/%s/shadow"
               (ThingName.to_header x.GetThingShadowRequest.thingName))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("name", (ShadowName.to_header v)))
                  x.shadowName])
      | ListNamedShadowsForThing ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/api/things/shadow/ListNamedShadowsForThing/%s"
               (ThingName.to_header
                  x.ListNamedShadowsForThingRequest.thingName))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                  x.nextToken;
               Option.map ~f:(fun v -> ("pageSize", (PageSize.to_header v)))
                 x.pageSize])
      | ListRetainedMessages ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/retainedMessage")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults])
      | Publish ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/topics/%s"
               (Topic.to_header x.PublishRequest.topic))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("qos", (Qos.to_header v))) x.qos;
               Option.map ~f:(fun v -> ("retain", (Retain.to_header v)))
                 x.retain])
      | UpdateThingShadow ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/things/%s/shadow"
               (ThingName.to_header x.UpdateThingShadowRequest.thingName))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("name", (ShadowName.to_header v)))
                  x.shadowName]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | DeleteThingShadow -> Awso.Http.Request.make (method_of_endpoint endp)
  | GetRetainedMessage ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetThingShadow ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListNamedShadowsForThing ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListRetainedMessages ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | Publish ->
      let headers = Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
      let body = Option.map req.payload ~f:Payload.to_header in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateThingShadow ->
      let headers = Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
      let body = JsonDocument.to_header req.payload in
      Awso.Http.Request.make ?headers ~body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : (Awso.Http.Response.t, Awso.Http.Io.Error.call) result) :
  (o, [ `AWS of e  | `Transport of Awso.Http.Io.Error.call ]) result=
  let handle_error err error_of_json =
    match err with
    | `Too_many_redirects -> Error (`Transport `Too_many_redirects)
    | `Bad_response
        { Awso.Http.Io.Error.code = code; body; x_amzn_error_type } ->
        let generic_error () =
          Error
            (`Transport
               (`Bad_response
                  { Awso.Http.Io.Error.code = code; body; x_amzn_error_type })) in
        (match (x_amzn_error_type, error_of_json,
                 ((code >= 400) && (code <= 599)))
         with
         | (Some error_type, Some error_of_json, true) ->
             let json = Yojson.Safe.from_string body in
             Error (`AWS (error_of_json error_type json))
         | (None, Some error_of_json, true) ->
             (try
                let json = Yojson.Safe.from_string body in
                match json |> (Yojson.Safe.Util.member "__type") with
                | `String error_type ->
                    let error_type =
                      match String.lsplit2 error_type ~on:'#' with
                      | Some (_, s) -> s
                      | None -> error_type in
                    Error (`AWS (error_of_json error_type json))
                | `Null -> generic_error ()
                | _ ->
                    failwithf "Error '__type' did not have string type: %s"
                      body ()
              with | _ -> generic_error ())
         | (None, _, _) | (_, None, _) | (_, _, false) -> generic_error ()) in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = resp in
  let _ = handle_error in
  let _ = response_to_json in
  match endpoint with
  | DeleteThingShadow ->
      (match resp with
       | Error err ->
           handle_error err (Some DeleteThingShadowResponse.error_of_json)
       | Ok resp ->
           let body = Payload.of_string (Awso.Http.Response.body resp) in
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (DeleteThingShadowResponse.of_header_and_body (headers, body)))
  | GetRetainedMessage ->
      (match resp with
       | Error err ->
           handle_error err (Some GetRetainedMessageResponse.error_of_json)
       | Ok resp ->
           Ok (GetRetainedMessageResponse.of_json (response_to_json resp)))
  | GetThingShadow ->
      (match resp with
       | Error err ->
           handle_error err (Some GetThingShadowResponse.error_of_json)
       | Ok resp ->
           let body = Payload.of_string (Awso.Http.Response.body resp) in
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (GetThingShadowResponse.of_header_and_body (headers, body)))
  | ListNamedShadowsForThing ->
      (match resp with
       | Error err ->
           handle_error err
             (Some ListNamedShadowsForThingResponse.error_of_json)
       | Ok resp ->
           Ok
             (ListNamedShadowsForThingResponse.of_json
                (response_to_json resp)))
  | ListRetainedMessages ->
      (match resp with
       | Error err ->
           handle_error err (Some ListRetainedMessagesResponse.error_of_json)
       | Ok resp ->
           Ok (ListRetainedMessagesResponse.of_json (response_to_json resp)))
  | Publish -> Ok ()
  | UpdateThingShadow ->
      (match resp with
       | Error err ->
           handle_error err (Some UpdateThingShadowResponse.error_of_json)
       | Ok resp ->
           let body = Payload.of_string (Awso.Http.Response.body resp) in
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (UpdateThingShadowResponse.of_header_and_body (headers, body)))
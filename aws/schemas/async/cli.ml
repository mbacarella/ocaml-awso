(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_discoverer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max256"
       and crossAccount =
         flag "cross-account" (optional bool) ~doc:"BOOL __boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and sourceArn =
         flag "source-arn" (required string)
           ~doc:"STRING __stringMin20Max1600" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_discoverer
           (Values.CreateDiscovererRequest.make ?description ?crossAccount
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~sourceArn ())
           (Some Values.CreateDiscovererResponse.to_json)
           (Some Values.CreateDiscovererResponse.error_to_json)])
let create_registry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max256"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_registry
           (Values.CreateRegistryRequest.make ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~registryName ())
           (Some Values.CreateRegistryResponse.to_json)
           (Some Values.CreateRegistryResponse.error_to_json)])
let create_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max256"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and content =
         flag "content" (required string) ~doc:"STRING __stringMin1Max100000"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING __string"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON Type" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_schema
           (Values.CreateSchemaRequest.make ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~content
              ~registryName ~schemaName ~type_:(Values.Type.of_json type_) ())
           (Some Values.CreateSchemaResponse.to_json)
           (Some Values.CreateSchemaResponse.error_to_json)])
let delete_discoverer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and discovererId =
         flag "discoverer-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_discoverer
           (Values.DeleteDiscovererRequest.make ~discovererId ()) None None])
let delete_registry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_registry
           (Values.DeleteRegistryRequest.make ~registryName ()) None None])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryName =
         flag "registry-name" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyRequest.make ?registryName ()) None
           None])
let delete_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_schema
           (Values.DeleteSchemaRequest.make ~registryName ~schemaName ())
           None None])
let delete_schema_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING __string"
       and schemaVersion =
         flag "schema-version" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_schema_version
           (Values.DeleteSchemaVersionRequest.make ~registryName ~schemaName
              ~schemaVersion ()) None None])
let describe_code_binding =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaVersion =
         flag "schema-version" (optional string) ~doc:"STRING __string"
       and language =
         flag "language" (required string) ~doc:"STRING __string"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_code_binding
           (Values.DescribeCodeBindingRequest.make ?schemaVersion ~language
              ~registryName ~schemaName ())
           (Some Values.DescribeCodeBindingResponse.to_json)
           (Some Values.DescribeCodeBindingResponse.error_to_json)])
let describe_discoverer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and discovererId =
         flag "discoverer-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_discoverer
           (Values.DescribeDiscovererRequest.make ~discovererId ())
           (Some Values.DescribeDiscovererResponse.to_json)
           (Some Values.DescribeDiscovererResponse.error_to_json)])
let describe_registry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_registry
           (Values.DescribeRegistryRequest.make ~registryName ())
           (Some Values.DescribeRegistryResponse.to_json)
           (Some Values.DescribeRegistryResponse.error_to_json)])
let describe_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaVersion =
         flag "schema-version" (optional string) ~doc:"STRING __string"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_schema
           (Values.DescribeSchemaRequest.make ?schemaVersion ~registryName
              ~schemaName ()) (Some Values.DescribeSchemaResponse.to_json)
           (Some Values.DescribeSchemaResponse.error_to_json)])
let export_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaVersion =
         flag "schema-version" (optional string) ~doc:"STRING __string"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING __string"
       and type_ = flag "type-" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_schema
           (Values.ExportSchemaRequest.make ?schemaVersion ~registryName
              ~schemaName ~type_ ())
           (Some Values.ExportSchemaResponse.to_json)
           (Some Values.ExportSchemaResponse.error_to_json)])
let get_code_binding_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaVersion =
         flag "schema-version" (optional string) ~doc:"STRING __string"
       and language =
         flag "language" (required string) ~doc:"STRING __string"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_code_binding_source
           (Values.GetCodeBindingSourceRequest.make ?schemaVersion ~language
              ~registryName ~schemaName ())
           (Some Values.GetCodeBindingSourceResponse.to_json)
           (Some Values.GetCodeBindingSourceResponse.error_to_json)])
let get_discovered_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and events =
         flag "events" (required json_arg)
           ~doc:"JSON __listOfGetDiscoveredSchemaVersionItemInput"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON Type" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_discovered_schema
           (Values.GetDiscoveredSchemaRequest.make
              ~events:(Values.Zz__listOfGetDiscoveredSchemaVersionItemInput.of_json
                         events) ~type_:(Values.Type.of_json type_) ())
           (Some Values.GetDiscoveredSchemaResponse.to_json)
           (Some Values.GetDiscoveredSchemaResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryName =
         flag "registry-name" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ?registryName ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let list_discoverers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and discovererIdPrefix =
         flag "discoverer-id-prefix" (optional string) ~doc:"STRING __string"
       and limit = flag "limit" (optional int) ~doc:"INT __integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and sourceArnPrefix =
         flag "source-arn-prefix" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_discoverers
           (Values.ListDiscoverersRequest.make ?discovererIdPrefix ?limit
              ?nextToken ?sourceArnPrefix ())
           (Some Values.ListDiscoverersResponse.to_json)
           (Some Values.ListDiscoverersResponse.error_to_json)])
let list_registries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT __integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and registryNamePrefix =
         flag "registry-name-prefix" (optional string) ~doc:"STRING __string"
       and scope = flag "scope" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_registries
           (Values.ListRegistriesRequest.make ?limit ?nextToken
              ?registryNamePrefix ?scope ())
           (Some Values.ListRegistriesResponse.to_json)
           (Some Values.ListRegistriesResponse.error_to_json)])
let list_schema_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT __integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_schema_versions
           (Values.ListSchemaVersionsRequest.make ?limit ?nextToken
              ~registryName ~schemaName ())
           (Some Values.ListSchemaVersionsResponse.to_json)
           (Some Values.ListSchemaVersionsResponse.error_to_json)])
let list_schemas =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT __integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and schemaNamePrefix =
         flag "schema-name-prefix" (optional string) ~doc:"STRING __string"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_schemas
           (Values.ListSchemasRequest.make ?limit ?nextToken
              ?schemaNamePrefix ~registryName ())
           (Some Values.ListSchemasResponse.to_json)
           (Some Values.ListSchemasResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_code_binding =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaVersion =
         flag "schema-version" (optional string) ~doc:"STRING __string"
       and language =
         flag "language" (required string) ~doc:"STRING __string"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_code_binding
           (Values.PutCodeBindingRequest.make ?schemaVersion ~language
              ~registryName ~schemaName ())
           (Some Values.PutCodeBindingResponse.to_json)
           (Some Values.PutCodeBindingResponse.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryName =
         flag "registry-name" (optional string) ~doc:"STRING __string"
       and revisionId =
         flag "revision-id" (optional string) ~doc:"STRING __string"
       and policy = flag "policy" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyRequest.make ?registryName ?revisionId
              ~policy ()) (Some Values.PutResourcePolicyResponse.to_json)
           (Some Values.PutResourcePolicyResponse.error_to_json)])
let search_schemas =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT __integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and keywords =
         flag "keywords" (required string) ~doc:"STRING __string"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_schemas
           (Values.SearchSchemasRequest.make ?limit ?nextToken ~keywords
              ~registryName ()) (Some Values.SearchSchemasResponse.to_json)
           (Some Values.SearchSchemasResponse.error_to_json)])
let start_discoverer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and discovererId =
         flag "discoverer-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_discoverer
           (Values.StartDiscovererRequest.make ~discovererId ())
           (Some Values.StartDiscovererResponse.to_json)
           (Some Values.StartDiscovererResponse.error_to_json)])
let stop_discoverer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and discovererId =
         flag "discoverer-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_discoverer
           (Values.StopDiscovererRequest.make ~discovererId ())
           (Some Values.StopDiscovererResponse.to_json)
           (Some Values.StopDiscovererResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.Zz__listOf__string.of_json tagKeys) ()) None
           None])
let update_discoverer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max256"
       and crossAccount =
         flag "cross-account" (optional bool) ~doc:"BOOL __boolean"
       and discovererId =
         flag "discoverer-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_discoverer
           (Values.UpdateDiscovererRequest.make ?description ?crossAccount
              ~discovererId ())
           (Some Values.UpdateDiscovererResponse.to_json)
           (Some Values.UpdateDiscovererResponse.error_to_json)])
let update_registry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max256"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_registry
           (Values.UpdateRegistryRequest.make ?description ~registryName ())
           (Some Values.UpdateRegistryResponse.to_json)
           (Some Values.UpdateRegistryResponse.error_to_json)])
let update_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientTokenId =
         flag "client-token-id" (optional string)
           ~doc:"STRING __stringMin0Max36"
       and content =
         flag "content" (optional string) ~doc:"STRING __stringMin1Max100000"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max256"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON Type"
       and registryName =
         flag "registry-name" (required string) ~doc:"STRING __string"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_schema
           (Values.UpdateSchemaRequest.make ?clientTokenId ?content
              ?description ?type_:(Option.map ~f:Values.Type.of_json type_)
              ~registryName ~schemaName ())
           (Some Values.UpdateSchemaResponse.to_json)
           (Some Values.UpdateSchemaResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-discoverer", create_discoverer);
    ("create-registry", create_registry);
    ("create-schema", create_schema);
    ("delete-discoverer", delete_discoverer);
    ("delete-registry", delete_registry);
    ("delete-resource-policy", delete_resource_policy);
    ("delete-schema", delete_schema);
    ("delete-schema-version", delete_schema_version);
    ("describe-code-binding", describe_code_binding);
    ("describe-discoverer", describe_discoverer);
    ("describe-registry", describe_registry);
    ("describe-schema", describe_schema);
    ("export-schema", export_schema);
    ("get-code-binding-source", get_code_binding_source);
    ("get-discovered-schema", get_discovered_schema);
    ("get-resource-policy", get_resource_policy);
    ("list-discoverers", list_discoverers);
    ("list-registries", list_registries);
    ("list-schema-versions", list_schema_versions);
    ("list-schemas", list_schemas);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-code-binding", put_code_binding);
    ("put-resource-policy", put_resource_policy);
    ("search-schemas", search_schemas);
    ("start-discoverer", start_discoverer);
    ("stop-discoverer", stop_discoverer);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-discoverer", update_discoverer);
    ("update-registry", update_registry);
    ("update-schema", update_schema)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let approve_plan_execution_step =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment =
         flag "comment" (optional string) ~doc:"STRING ExecutionComment"
       and planArn = flag "plan-arn" (required string) ~doc:"STRING PlanArn"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING ExecutionId"
       and stepName =
         flag "step-name" (required string) ~doc:"STRING StepName"
       and approval =
         flag "approval" (required json_arg) ~doc:"JSON Approval" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.approve_plan_execution_step
           (Values.ApprovePlanExecutionStepRequest.make ?comment ~planArn
              ~executionId ~stepName
              ~approval:(Values.Approval.of_json approval) ())
           (Some Values.ApprovePlanExecutionStepResponse.to_json)
           (Some Values.ApprovePlanExecutionStepResponse.error_to_json)])
let cancel_plan_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment =
         flag "comment" (optional string) ~doc:"STRING ExecutionComment"
       and planArn = flag "plan-arn" (required string) ~doc:"STRING PlanArn"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING ExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_plan_execution
           (Values.CancelPlanExecutionRequest.make ?comment ~planArn
              ~executionId ())
           (Some Values.CancelPlanExecutionResponse.to_json)
           (Some Values.CancelPlanExecutionResponse.error_to_json)])
let create_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and recoveryTimeObjectiveMinutes =
         flag "recovery-time-objective-minutes" (optional int)
           ~doc:"INT CreatePlanRequestRecoveryTimeObjectiveMinutesInteger"
       and associatedAlarms =
         flag "associated-alarms" (optional json_arg)
           ~doc:"JSON AssociatedAlarmMap"
       and triggers =
         flag "triggers" (optional json_arg) ~doc:"JSON TriggerList"
       and reportConfiguration =
         flag "report-configuration" (optional json_arg)
           ~doc:"JSON ReportConfiguration"
       and primaryRegion =
         flag "primary-region" (optional string) ~doc:"STRING Region"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and workflows =
         flag "workflows" (required json_arg) ~doc:"JSON WorkflowList"
       and executionRole =
         flag "execution-role" (required string) ~doc:"STRING IamRoleArn"
       and name = flag "name" (required string) ~doc:"STRING PlanName"
       and regions =
         flag "regions" (required json_arg) ~doc:"JSON RegionList"
       and recoveryApproach =
         flag "recovery-approach" (required json_arg)
           ~doc:"JSON RecoveryApproach" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_plan
           (Values.CreatePlanRequest.make ?description
              ?recoveryTimeObjectiveMinutes
              ?associatedAlarms:(Option.map
                                   ~f:Values.AssociatedAlarmMap.of_json
                                   associatedAlarms)
              ?triggers:(Option.map ~f:Values.TriggerList.of_json triggers)
              ?reportConfiguration:(Option.map
                                      ~f:Values.ReportConfiguration.of_json
                                      reportConfiguration) ?primaryRegion
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~workflows:(Values.WorkflowList.of_json workflows)
              ~executionRole ~name
              ~regions:(Values.RegionList.of_json regions)
              ~recoveryApproach:(Values.RecoveryApproach.of_json
                                   recoveryApproach) ())
           (Some Values.CreatePlanResponse.to_json)
           (Some Values.CreatePlanResponse.error_to_json)])
let delete_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PlanArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_plan (Values.DeletePlanRequest.make ~arn ())
           (Some Values.DeletePlanResponse.to_json)
           (Some Values.DeletePlanResponse.error_to_json)])
let get_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PlanArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_plan (Values.GetPlanRequest.make ~arn ())
           (Some Values.GetPlanResponse.to_json)
           (Some Values.GetPlanResponse.error_to_json)])
let get_plan_evaluation_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and planArn = flag "plan-arn" (required string) ~doc:"STRING PlanArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_plan_evaluation_status
           (Values.GetPlanEvaluationStatusRequest.make ?maxResults ?nextToken
              ~planArn ())
           (Some Values.GetPlanEvaluationStatusResponse.to_json)
           (Some Values.GetPlanEvaluationStatusResponse.error_to_json)])
let get_plan_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT GetPlanExecutionStepStatesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and planArn = flag "plan-arn" (required string) ~doc:"STRING PlanArn"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING ExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_plan_execution
           (Values.GetPlanExecutionRequest.make ?maxResults ?nextToken
              ~planArn ~executionId ())
           (Some Values.GetPlanExecutionResponse.to_json)
           (Some Values.GetPlanExecutionResponse.error_to_json)])
let get_plan_in_region =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PlanArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_plan_in_region (Values.GetPlanInRegionRequest.make ~arn ())
           (Some Values.GetPlanInRegionResponse.to_json)
           (Some Values.GetPlanInRegionResponse.error_to_json)])
let list_plan_execution_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListExecutionEventsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and name = flag "name" (optional string) ~doc:"STRING StepName"
       and planArn = flag "plan-arn" (required string) ~doc:"STRING PlanArn"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING ExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_plan_execution_events
           (Values.ListPlanExecutionEventsRequest.make ?maxResults ?nextToken
              ?name ~planArn ~executionId ())
           (Some Values.ListPlanExecutionEventsResponse.to_json)
           (Some Values.ListPlanExecutionEventsResponse.error_to_json)])
let list_plan_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListExecutionsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and state =
         flag "state" (optional json_arg) ~doc:"JSON ExecutionState"
       and planArn = flag "plan-arn" (required string) ~doc:"STRING PlanArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_plan_executions
           (Values.ListPlanExecutionsRequest.make ?maxResults ?nextToken
              ?state:(Option.map ~f:Values.ExecutionState.of_json state)
              ~planArn ()) (Some Values.ListPlanExecutionsResponse.to_json)
           (Some Values.ListPlanExecutionsResponse.error_to_json)])
let list_plans =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_plans
           (Values.ListPlansRequest.make ?maxResults ?nextToken ())
           (Some Values.ListPlansResponse.to_json)
           (Some Values.ListPlansResponse.error_to_json)])
let list_plans_in_region =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_plans_in_region
           (Values.ListPlansInRegionRequest.make ?maxResults ?nextToken ())
           (Some Values.ListPlansInRegionResponse.to_json)
           (Some Values.ListPlansInRegionResponse.error_to_json)])
let list_route53_health_checks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (optional string)
           ~doc:"STRING Route53HostedZoneId"
       and recordName =
         flag "record-name" (optional string) ~doc:"STRING Route53RecordName"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRoute53HealthChecksRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and arn = flag "arn" (required string) ~doc:"STRING PlanArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_route53_health_checks
           (Values.ListRoute53HealthChecksRequest.make ?hostedZoneId
              ?recordName ?maxResults ?nextToken ~arn ())
           (Some Values.ListRoute53HealthChecksResponse.to_json)
           (Some Values.ListRoute53HealthChecksResponse.error_to_json)])
let list_route53_health_checks_in_region =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (optional string)
           ~doc:"STRING Route53HostedZoneId"
       and recordName =
         flag "record-name" (optional string) ~doc:"STRING Route53RecordName"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRoute53HealthChecksInRegionRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and arn = flag "arn" (required string) ~doc:"STRING PlanArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_route53_health_checks_in_region
           (Values.ListRoute53HealthChecksInRegionRequest.make ?hostedZoneId
              ?recordName ?maxResults ?nextToken ~arn ())
           (Some Values.ListRoute53HealthChecksInRegionResponse.to_json)
           (Some Values.ListRoute53HealthChecksInRegionResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PlanArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~arn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_plan_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mode = flag "mode" (optional json_arg) ~doc:"JSON ExecutionMode"
       and comment =
         flag "comment" (optional string) ~doc:"STRING ExecutionComment"
       and latestVersion =
         flag "latest-version" (optional string) ~doc:"STRING String"
       and recoveryExecutionId =
         flag "recovery-execution-id" (optional string)
           ~doc:"STRING RecoveryExecutionId"
       and planArn = flag "plan-arn" (required string) ~doc:"STRING PlanArn"
       and targetRegion =
         flag "target-region" (required string) ~doc:"STRING String"
       and action =
         flag "action" (required json_arg) ~doc:"JSON ExecutionAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_plan_execution
           (Values.StartPlanExecutionRequest.make
              ?mode:(Option.map ~f:Values.ExecutionMode.of_json mode)
              ?comment ?latestVersion ?recoveryExecutionId ~planArn
              ~targetRegion ~action:(Values.ExecutionAction.of_json action)
              ()) (Some Values.StartPlanExecutionResponse.to_json)
           (Some Values.StartPlanExecutionResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PlanArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~arn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PlanArn"
       and resourceTagKeys =
         flag "resource-tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~arn
              ~resourceTagKeys:(Values.TagKeys.of_json resourceTagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and recoveryTimeObjectiveMinutes =
         flag "recovery-time-objective-minutes" (optional int)
           ~doc:"INT UpdatePlanRequestRecoveryTimeObjectiveMinutesInteger"
       and associatedAlarms =
         flag "associated-alarms" (optional json_arg)
           ~doc:"JSON AssociatedAlarmMap"
       and triggers =
         flag "triggers" (optional json_arg) ~doc:"JSON TriggerList"
       and reportConfiguration =
         flag "report-configuration" (optional json_arg)
           ~doc:"JSON ReportConfiguration"
       and arn = flag "arn" (required string) ~doc:"STRING PlanArn"
       and workflows =
         flag "workflows" (required json_arg) ~doc:"JSON WorkflowList"
       and executionRole =
         flag "execution-role" (required string) ~doc:"STRING IamRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_plan
           (Values.UpdatePlanRequest.make ?description
              ?recoveryTimeObjectiveMinutes
              ?associatedAlarms:(Option.map
                                   ~f:Values.AssociatedAlarmMap.of_json
                                   associatedAlarms)
              ?triggers:(Option.map ~f:Values.TriggerList.of_json triggers)
              ?reportConfiguration:(Option.map
                                      ~f:Values.ReportConfiguration.of_json
                                      reportConfiguration) ~arn
              ~workflows:(Values.WorkflowList.of_json workflows)
              ~executionRole ()) (Some Values.UpdatePlanResponse.to_json)
           (Some Values.UpdatePlanResponse.error_to_json)])
let update_plan_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment =
         flag "comment" (optional string) ~doc:"STRING ExecutionComment"
       and planArn = flag "plan-arn" (required string) ~doc:"STRING PlanArn"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING ExecutionId"
       and action =
         flag "action" (required json_arg)
           ~doc:"JSON UpdatePlanExecutionAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_plan_execution
           (Values.UpdatePlanExecutionRequest.make ?comment ~planArn
              ~executionId
              ~action:(Values.UpdatePlanExecutionAction.of_json action) ())
           (Some Values.UpdatePlanExecutionResponse.to_json)
           (Some Values.UpdatePlanExecutionResponse.error_to_json)])
let update_plan_execution_step =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and planArn = flag "plan-arn" (required string) ~doc:"STRING PlanArn"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING ExecutionId"
       and comment =
         flag "comment" (required string) ~doc:"STRING ExecutionComment"
       and stepName = flag "step-name" (required string) ~doc:"STRING String"
       and actionToTake =
         flag "action-to-take" (required json_arg)
           ~doc:"JSON UpdatePlanExecutionStepAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_plan_execution_step
           (Values.UpdatePlanExecutionStepRequest.make ~planArn ~executionId
              ~comment ~stepName
              ~actionToTake:(Values.UpdatePlanExecutionStepAction.of_json
                               actionToTake) ())
           (Some Values.UpdatePlanExecutionStepResponse.to_json)
           (Some Values.UpdatePlanExecutionStepResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("approve-plan-execution-step", approve_plan_execution_step);
    ("cancel-plan-execution", cancel_plan_execution);
    ("create-plan", create_plan);
    ("delete-plan", delete_plan);
    ("get-plan", get_plan);
    ("get-plan-evaluation-status", get_plan_evaluation_status);
    ("get-plan-execution", get_plan_execution);
    ("get-plan-in-region", get_plan_in_region);
    ("list-plan-execution-events", list_plan_execution_events);
    ("list-plan-executions", list_plan_executions);
    ("list-plans", list_plans);
    ("list-plans-in-region", list_plans_in_region);
    ("list-route53-health-checks", list_route53_health_checks);
    ("list-route53-health-checks-in-region",
      list_route53_health_checks_in_region);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-plan-execution", start_plan_execution);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-plan", update_plan);
    ("update-plan-execution", update_plan_execution);
    ("update-plan-execution-step", update_plan_execution_step)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_policy_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and condition =
         flag "condition" (optional string) ~doc:"STRING StatementCondition"
       and arn = flag "arn" (required string) ~doc:"STRING VeniceGlobalArn"
       and statementId =
         flag "statement-id" (required string) ~doc:"STRING StatementId"
       and effect_ =
         flag "effect-" (required json_arg) ~doc:"JSON StatementEffect"
       and action =
         flag "action" (required json_arg) ~doc:"JSON StatementActionList"
       and principal =
         flag "principal" (required json_arg)
           ~doc:"JSON StatementPrincipalList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_policy_statement
           (Values.AddPolicyStatementInput.make ?condition ~arn ~statementId
              ~effect_:(Values.StatementEffect.of_json effect_)
              ~action:(Values.StatementActionList.of_json action)
              ~principal:(Values.StatementPrincipalList.of_json principal) ())
           (Some Values.AddPolicyStatementOutput.to_json)
           (Some Values.AddPolicyStatementOutput.error_to_json)])
let batch_delete_unique_id =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputSource =
         flag "input-source" (optional string)
           ~doc:"STRING BatchDeleteUniqueIdInputInputSourceString"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName"
       and uniqueIds =
         flag "unique-ids" (required json_arg) ~doc:"JSON UniqueIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_unique_id
           (Values.BatchDeleteUniqueIdInput.make ?inputSource ~workflowName
              ~uniqueIds:(Values.UniqueIdList.of_json uniqueIds) ())
           (Some Values.BatchDeleteUniqueIdOutput.to_json)
           (Some Values.BatchDeleteUniqueIdOutput.error_to_json)])
let create_id_mapping_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and outputSourceConfig =
         flag "output-source-config" (optional json_arg)
           ~doc:"JSON IdMappingWorkflowOutputSourceConfig"
       and incrementalRunConfig =
         flag "incremental-run-config" (optional json_arg)
           ~doc:"JSON IdMappingIncrementalRunConfig"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IdMappingRoleArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName"
       and inputSourceConfig =
         flag "input-source-config" (required json_arg)
           ~doc:"JSON IdMappingWorkflowInputSourceConfig"
       and idMappingTechniques =
         flag "id-mapping-techniques" (required json_arg)
           ~doc:"JSON IdMappingTechniques" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_id_mapping_workflow
           (Values.CreateIdMappingWorkflowInput.make ?description
              ?outputSourceConfig:(Option.map
                                     ~f:Values.IdMappingWorkflowOutputSourceConfig.of_json
                                     outputSourceConfig)
              ?incrementalRunConfig:(Option.map
                                       ~f:Values.IdMappingIncrementalRunConfig.of_json
                                       incrementalRunConfig) ?roleArn
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~workflowName
              ~inputSourceConfig:(Values.IdMappingWorkflowInputSourceConfig.of_json
                                    inputSourceConfig)
              ~idMappingTechniques:(Values.IdMappingTechniques.of_json
                                      idMappingTechniques) ())
           (Some Values.CreateIdMappingWorkflowOutput.to_json)
           (Some Values.CreateIdMappingWorkflowOutput.error_to_json)])
let create_id_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and inputSourceConfig =
         flag "input-source-config" (optional json_arg)
           ~doc:"JSON IdNamespaceInputSourceConfig"
       and idMappingWorkflowProperties =
         flag "id-mapping-workflow-properties" (optional json_arg)
           ~doc:"JSON IdNamespaceIdMappingWorkflowPropertiesList"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and idNamespaceName =
         flag "id-namespace-name" (required string) ~doc:"STRING EntityName"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON IdNamespaceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_id_namespace
           (Values.CreateIdNamespaceInput.make ?description
              ?inputSourceConfig:(Option.map
                                    ~f:Values.IdNamespaceInputSourceConfig.of_json
                                    inputSourceConfig)
              ?idMappingWorkflowProperties:(Option.map
                                              ~f:Values.IdNamespaceIdMappingWorkflowPropertiesList.of_json
                                              idMappingWorkflowProperties)
              ?roleArn ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~idNamespaceName ~type_:(Values.IdNamespaceType.of_json type_)
              ()) (Some Values.CreateIdNamespaceOutput.to_json)
           (Some Values.CreateIdNamespaceOutput.error_to_json)])
let create_matching_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and incrementalRunConfig =
         flag "incremental-run-config" (optional json_arg)
           ~doc:"JSON IncrementalRunConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName"
       and inputSourceConfig =
         flag "input-source-config" (required json_arg)
           ~doc:"JSON InputSourceConfig"
       and outputSourceConfig =
         flag "output-source-config" (required json_arg)
           ~doc:"JSON OutputSourceConfig"
       and resolutionTechniques =
         flag "resolution-techniques" (required json_arg)
           ~doc:"JSON ResolutionTechniques"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_matching_workflow
           (Values.CreateMatchingWorkflowInput.make ?description
              ?incrementalRunConfig:(Option.map
                                       ~f:Values.IncrementalRunConfig.of_json
                                       incrementalRunConfig)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~workflowName
              ~inputSourceConfig:(Values.InputSourceConfig.of_json
                                    inputSourceConfig)
              ~outputSourceConfig:(Values.OutputSourceConfig.of_json
                                     outputSourceConfig)
              ~resolutionTechniques:(Values.ResolutionTechniques.of_json
                                       resolutionTechniques) ~roleArn ())
           (Some Values.CreateMatchingWorkflowOutput.to_json)
           (Some Values.CreateMatchingWorkflowOutput.error_to_json)])
let create_schema_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING EntityName"
       and mappedInputFields =
         flag "mapped-input-fields" (required json_arg)
           ~doc:"JSON SchemaInputAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_schema_mapping
           (Values.CreateSchemaMappingInput.make ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~schemaName
              ~mappedInputFields:(Values.SchemaInputAttributes.of_json
                                    mappedInputFields) ())
           (Some Values.CreateSchemaMappingOutput.to_json)
           (Some Values.CreateSchemaMappingOutput.error_to_json)])
let delete_id_mapping_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_id_mapping_workflow
           (Values.DeleteIdMappingWorkflowInput.make ~workflowName ())
           (Some Values.DeleteIdMappingWorkflowOutput.to_json)
           (Some Values.DeleteIdMappingWorkflowOutput.error_to_json)])
let delete_id_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and idNamespaceName =
         flag "id-namespace-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_id_namespace
           (Values.DeleteIdNamespaceInput.make ~idNamespaceName ())
           (Some Values.DeleteIdNamespaceOutput.to_json)
           (Some Values.DeleteIdNamespaceOutput.error_to_json)])
let delete_matching_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_matching_workflow
           (Values.DeleteMatchingWorkflowInput.make ~workflowName ())
           (Some Values.DeleteMatchingWorkflowOutput.to_json)
           (Some Values.DeleteMatchingWorkflowOutput.error_to_json)])
let delete_policy_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING VeniceGlobalArn"
       and statementId =
         flag "statement-id" (required string) ~doc:"STRING StatementId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy_statement
           (Values.DeletePolicyStatementInput.make ~arn ~statementId ())
           (Some Values.DeletePolicyStatementOutput.to_json)
           (Some Values.DeletePolicyStatementOutput.error_to_json)])
let delete_schema_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_schema_mapping
           (Values.DeleteSchemaMappingInput.make ~schemaName ())
           (Some Values.DeleteSchemaMappingOutput.to_json)
           (Some Values.DeleteSchemaMappingOutput.error_to_json)])
let generate_match_id =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and processingType =
         flag "processing-type" (optional json_arg)
           ~doc:"JSON ProcessingType"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName"
       and records =
         flag "records" (required json_arg)
           ~doc:"JSON GenerateMatchIdInputRecordsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_match_id
           (Values.GenerateMatchIdInput.make
              ?processingType:(Option.map ~f:Values.ProcessingType.of_json
                                 processingType) ~workflowName
              ~records:(Values.GenerateMatchIdInputRecordsList.of_json
                          records) ())
           (Some Values.GenerateMatchIdOutput.to_json)
           (Some Values.GenerateMatchIdOutput.error_to_json)])
let get_id_mapping_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowName =
         flag "workflow-name" (required string)
           ~doc:"STRING EntityNameOrIdMappingWorkflowArn"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_id_mapping_job
           (Values.GetIdMappingJobInput.make ~workflowName ~jobId ())
           (Some Values.GetIdMappingJobOutput.to_json)
           (Some Values.GetIdMappingJobOutput.error_to_json)])
let get_id_mapping_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_id_mapping_workflow
           (Values.GetIdMappingWorkflowInput.make ~workflowName ())
           (Some Values.GetIdMappingWorkflowOutput.to_json)
           (Some Values.GetIdMappingWorkflowOutput.error_to_json)])
let get_id_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and idNamespaceName =
         flag "id-namespace-name" (required string)
           ~doc:"STRING EntityNameOrIdNamespaceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_id_namespace
           (Values.GetIdNamespaceInput.make ~idNamespaceName ())
           (Some Values.GetIdNamespaceOutput.to_json)
           (Some Values.GetIdNamespaceOutput.error_to_json)])
let get_match_id =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applyNormalization =
         flag "apply-normalization" (optional bool) ~doc:"BOOL Boolean"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName"
       and record =
         flag "record" (required json_arg) ~doc:"JSON RecordAttributeMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_match_id
           (Values.GetMatchIdInput.make ?applyNormalization ~workflowName
              ~record:(Values.RecordAttributeMap.of_json record) ())
           (Some Values.GetMatchIdOutput.to_json)
           (Some Values.GetMatchIdOutput.error_to_json)])
let get_matching_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_matching_job
           (Values.GetMatchingJobInput.make ~workflowName ~jobId ())
           (Some Values.GetMatchingJobOutput.to_json)
           (Some Values.GetMatchingJobOutput.error_to_json)])
let get_matching_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_matching_workflow
           (Values.GetMatchingWorkflowInput.make ~workflowName ())
           (Some Values.GetMatchingWorkflowOutput.to_json)
           (Some Values.GetMatchingWorkflowOutput.error_to_json)])
let get_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING VeniceGlobalArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy (Values.GetPolicyInput.make ~arn ())
           (Some Values.GetPolicyOutput.to_json)
           (Some Values.GetPolicyOutput.error_to_json)])
let get_provider_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and providerName =
         flag "provider-name" (required string) ~doc:"STRING EntityName"
       and providerServiceName =
         flag "provider-service-name" (required string)
           ~doc:"STRING ProviderServiceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_provider_service
           (Values.GetProviderServiceInput.make ~providerName
              ~providerServiceName ())
           (Some Values.GetProviderServiceOutput.to_json)
           (Some Values.GetProviderServiceOutput.error_to_json)])
let get_schema_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_schema_mapping
           (Values.GetSchemaMappingInput.make ~schemaName ())
           (Some Values.GetSchemaMappingOutput.to_json)
           (Some Values.GetSchemaMappingOutput.error_to_json)])
let list_id_mapping_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListIdMappingJobsInputMaxResultsInteger"
       and workflowName =
         flag "workflow-name" (required string)
           ~doc:"STRING EntityNameOrIdMappingWorkflowArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_id_mapping_jobs
           (Values.ListIdMappingJobsInput.make ?nextToken ?maxResults
              ~workflowName ()) (Some Values.ListIdMappingJobsOutput.to_json)
           (Some Values.ListIdMappingJobsOutput.error_to_json)])
let list_id_mapping_workflows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListIdMappingWorkflowsInputMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_id_mapping_workflows
           (Values.ListIdMappingWorkflowsInput.make ?nextToken ?maxResults ())
           (Some Values.ListIdMappingWorkflowsOutput.to_json)
           (Some Values.ListIdMappingWorkflowsOutput.error_to_json)])
let list_id_namespaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListIdNamespacesInputMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_id_namespaces
           (Values.ListIdNamespacesInput.make ?nextToken ?maxResults ())
           (Some Values.ListIdNamespacesOutput.to_json)
           (Some Values.ListIdNamespacesOutput.error_to_json)])
let list_matching_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListMatchingJobsInputMaxResultsInteger"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_matching_jobs
           (Values.ListMatchingJobsInput.make ?nextToken ?maxResults
              ~workflowName ()) (Some Values.ListMatchingJobsOutput.to_json)
           (Some Values.ListMatchingJobsOutput.error_to_json)])
let list_matching_workflows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListMatchingWorkflowsInputMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_matching_workflows
           (Values.ListMatchingWorkflowsInput.make ?nextToken ?maxResults ())
           (Some Values.ListMatchingWorkflowsOutput.to_json)
           (Some Values.ListMatchingWorkflowsOutput.error_to_json)])
let list_provider_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListProviderServicesInputMaxResultsInteger"
       and providerName =
         flag "provider-name" (optional string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_provider_services
           (Values.ListProviderServicesInput.make ?nextToken ?maxResults
              ?providerName ())
           (Some Values.ListProviderServicesOutput.to_json)
           (Some Values.ListProviderServicesOutput.error_to_json)])
let list_schema_mappings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSchemaMappingsInputMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_schema_mappings
           (Values.ListSchemaMappingsInput.make ?nextToken ?maxResults ())
           (Some Values.ListSchemaMappingsOutput.to_json)
           (Some Values.ListSchemaMappingsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING VeniceGlobalArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let put_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and token = flag "token" (optional string) ~doc:"STRING PolicyToken"
       and arn = flag "arn" (required string) ~doc:"STRING VeniceGlobalArn"
       and policy =
         flag "policy" (required string) ~doc:"STRING PolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_policy (Values.PutPolicyInput.make ?token ~arn ~policy ())
           (Some Values.PutPolicyOutput.to_json)
           (Some Values.PutPolicyOutput.error_to_json)])
let start_id_mapping_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and outputSourceConfig =
         flag "output-source-config" (optional json_arg)
           ~doc:"JSON IdMappingJobOutputSourceConfig"
       and jobType = flag "job-type" (optional json_arg) ~doc:"JSON JobType"
       and workflowName =
         flag "workflow-name" (required string)
           ~doc:"STRING EntityNameOrIdMappingWorkflowArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_id_mapping_job
           (Values.StartIdMappingJobInput.make
              ?outputSourceConfig:(Option.map
                                     ~f:Values.IdMappingJobOutputSourceConfig.of_json
                                     outputSourceConfig)
              ?jobType:(Option.map ~f:Values.JobType.of_json jobType)
              ~workflowName ()) (Some Values.StartIdMappingJobOutput.to_json)
           (Some Values.StartIdMappingJobOutput.error_to_json)])
let start_matching_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_matching_job
           (Values.StartMatchingJobInput.make ~workflowName ())
           (Some Values.StartMatchingJobOutput.to_json)
           (Some Values.StartMatchingJobOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING VeniceGlobalArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING VeniceGlobalArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_id_mapping_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and outputSourceConfig =
         flag "output-source-config" (optional json_arg)
           ~doc:"JSON IdMappingWorkflowOutputSourceConfig"
       and incrementalRunConfig =
         flag "incremental-run-config" (optional json_arg)
           ~doc:"JSON IdMappingIncrementalRunConfig"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IdMappingRoleArn"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName"
       and inputSourceConfig =
         flag "input-source-config" (required json_arg)
           ~doc:"JSON IdMappingWorkflowInputSourceConfig"
       and idMappingTechniques =
         flag "id-mapping-techniques" (required json_arg)
           ~doc:"JSON IdMappingTechniques" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_id_mapping_workflow
           (Values.UpdateIdMappingWorkflowInput.make ?description
              ?outputSourceConfig:(Option.map
                                     ~f:Values.IdMappingWorkflowOutputSourceConfig.of_json
                                     outputSourceConfig)
              ?incrementalRunConfig:(Option.map
                                       ~f:Values.IdMappingIncrementalRunConfig.of_json
                                       incrementalRunConfig) ?roleArn
              ~workflowName
              ~inputSourceConfig:(Values.IdMappingWorkflowInputSourceConfig.of_json
                                    inputSourceConfig)
              ~idMappingTechniques:(Values.IdMappingTechniques.of_json
                                      idMappingTechniques) ())
           (Some Values.UpdateIdMappingWorkflowOutput.to_json)
           (Some Values.UpdateIdMappingWorkflowOutput.error_to_json)])
let update_id_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and inputSourceConfig =
         flag "input-source-config" (optional json_arg)
           ~doc:"JSON IdNamespaceInputSourceConfig"
       and idMappingWorkflowProperties =
         flag "id-mapping-workflow-properties" (optional json_arg)
           ~doc:"JSON IdNamespaceIdMappingWorkflowPropertiesList"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and idNamespaceName =
         flag "id-namespace-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_id_namespace
           (Values.UpdateIdNamespaceInput.make ?description
              ?inputSourceConfig:(Option.map
                                    ~f:Values.IdNamespaceInputSourceConfig.of_json
                                    inputSourceConfig)
              ?idMappingWorkflowProperties:(Option.map
                                              ~f:Values.IdNamespaceIdMappingWorkflowPropertiesList.of_json
                                              idMappingWorkflowProperties)
              ?roleArn ~idNamespaceName ())
           (Some Values.UpdateIdNamespaceOutput.to_json)
           (Some Values.UpdateIdNamespaceOutput.error_to_json)])
let update_matching_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and incrementalRunConfig =
         flag "incremental-run-config" (optional json_arg)
           ~doc:"JSON IncrementalRunConfig"
       and workflowName =
         flag "workflow-name" (required string) ~doc:"STRING EntityName"
       and inputSourceConfig =
         flag "input-source-config" (required json_arg)
           ~doc:"JSON InputSourceConfig"
       and outputSourceConfig =
         flag "output-source-config" (required json_arg)
           ~doc:"JSON OutputSourceConfig"
       and resolutionTechniques =
         flag "resolution-techniques" (required json_arg)
           ~doc:"JSON ResolutionTechniques"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_matching_workflow
           (Values.UpdateMatchingWorkflowInput.make ?description
              ?incrementalRunConfig:(Option.map
                                       ~f:Values.IncrementalRunConfig.of_json
                                       incrementalRunConfig) ~workflowName
              ~inputSourceConfig:(Values.InputSourceConfig.of_json
                                    inputSourceConfig)
              ~outputSourceConfig:(Values.OutputSourceConfig.of_json
                                     outputSourceConfig)
              ~resolutionTechniques:(Values.ResolutionTechniques.of_json
                                       resolutionTechniques) ~roleArn ())
           (Some Values.UpdateMatchingWorkflowOutput.to_json)
           (Some Values.UpdateMatchingWorkflowOutput.error_to_json)])
let update_schema_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and schemaName =
         flag "schema-name" (required string) ~doc:"STRING EntityName"
       and mappedInputFields =
         flag "mapped-input-fields" (required json_arg)
           ~doc:"JSON SchemaInputAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_schema_mapping
           (Values.UpdateSchemaMappingInput.make ?description ~schemaName
              ~mappedInputFields:(Values.SchemaInputAttributes.of_json
                                    mappedInputFields) ())
           (Some Values.UpdateSchemaMappingOutput.to_json)
           (Some Values.UpdateSchemaMappingOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-policy-statement", add_policy_statement);
    ("batch-delete-unique-id", batch_delete_unique_id);
    ("create-id-mapping-workflow", create_id_mapping_workflow);
    ("create-id-namespace", create_id_namespace);
    ("create-matching-workflow", create_matching_workflow);
    ("create-schema-mapping", create_schema_mapping);
    ("delete-id-mapping-workflow", delete_id_mapping_workflow);
    ("delete-id-namespace", delete_id_namespace);
    ("delete-matching-workflow", delete_matching_workflow);
    ("delete-policy-statement", delete_policy_statement);
    ("delete-schema-mapping", delete_schema_mapping);
    ("generate-match-id", generate_match_id);
    ("get-id-mapping-job", get_id_mapping_job);
    ("get-id-mapping-workflow", get_id_mapping_workflow);
    ("get-id-namespace", get_id_namespace);
    ("get-match-id", get_match_id);
    ("get-matching-job", get_matching_job);
    ("get-matching-workflow", get_matching_workflow);
    ("get-policy", get_policy);
    ("get-provider-service", get_provider_service);
    ("get-schema-mapping", get_schema_mapping);
    ("list-id-mapping-jobs", list_id_mapping_jobs);
    ("list-id-mapping-workflows", list_id_mapping_workflows);
    ("list-id-namespaces", list_id_namespaces);
    ("list-matching-jobs", list_matching_jobs);
    ("list-matching-workflows", list_matching_workflows);
    ("list-provider-services", list_provider_services);
    ("list-schema-mappings", list_schema_mappings);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-policy", put_policy);
    ("start-id-mapping-job", start_id_mapping_job);
    ("start-matching-job", start_matching_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-id-mapping-workflow", update_id_mapping_workflow);
    ("update-id-namespace", update_id_namespace);
    ("update-matching-workflow", update_matching_workflow);
    ("update-schema-mapping", update_schema_mapping)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_delete_worlds =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and worlds = flag "worlds" (required json_arg) ~doc:"JSON Arns" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_worlds
           (Values.BatchDeleteWorldsRequest.make
              ~worlds:(Values.Arns.of_json worlds) ())
           (Some Values.BatchDeleteWorldsResponse.to_json)
           (Some Values.BatchDeleteWorldsResponse.error_to_json)])
let batch_describe_simulation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobs = flag "jobs" (required json_arg) ~doc:"JSON Arns" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_describe_simulation_job
           (Values.BatchDescribeSimulationJobRequest.make
              ~jobs:(Values.Arns.of_json jobs) ())
           (Some Values.BatchDescribeSimulationJobResponse.to_json)
           (Some Values.BatchDescribeSimulationJobResponse.error_to_json)])
let cancel_deployment_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and job = flag "job" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_deployment_job
           (Values.CancelDeploymentJobRequest.make ~job ())
           (Some Values.CancelDeploymentJobResponse.to_json)
           (Some Values.CancelDeploymentJobResponse.error_to_json)])
let cancel_simulation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and job = flag "job" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_simulation_job
           (Values.CancelSimulationJobRequest.make ~job ())
           (Some Values.CancelSimulationJobResponse.to_json)
           (Some Values.CancelSimulationJobResponse.error_to_json)])
let cancel_simulation_job_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and batch = flag "batch" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_simulation_job_batch
           (Values.CancelSimulationJobBatchRequest.make ~batch ())
           (Some Values.CancelSimulationJobBatchResponse.to_json)
           (Some Values.CancelSimulationJobBatchResponse.error_to_json)])
let cancel_world_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and job = flag "job" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_world_export_job
           (Values.CancelWorldExportJobRequest.make ~job ())
           (Some Values.CancelWorldExportJobResponse.to_json)
           (Some Values.CancelWorldExportJobResponse.error_to_json)])
let cancel_world_generation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and job = flag "job" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_world_generation_job
           (Values.CancelWorldGenerationJobRequest.make ~job ())
           (Some Values.CancelWorldGenerationJobResponse.to_json)
           (Some Values.CancelWorldGenerationJobResponse.error_to_json)])
let create_deployment_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deploymentConfig =
         flag "deployment-config" (optional json_arg)
           ~doc:"JSON DeploymentConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken"
       and fleet = flag "fleet" (required string) ~doc:"STRING Arn"
       and deploymentApplicationConfigs =
         flag "deployment-application-configs" (required json_arg)
           ~doc:"JSON DeploymentApplicationConfigs" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_deployment_job
           (Values.CreateDeploymentJobRequest.make
              ?deploymentConfig:(Option.map
                                   ~f:Values.DeploymentConfig.of_json
                                   deploymentConfig)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~clientRequestToken ~fleet
              ~deploymentApplicationConfigs:(Values.DeploymentApplicationConfigs.of_json
                                               deploymentApplicationConfigs)
              ()) (Some Values.CreateDeploymentJobResponse.to_json)
           (Some Values.CreateDeploymentJobResponse.error_to_json)])
let create_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_fleet
           (Values.CreateFleetRequest.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name ())
           (Some Values.CreateFleetResponse.to_json)
           (Some Values.CreateFleetResponse.error_to_json)])
let create_robot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and architecture =
         flag "architecture" (required json_arg) ~doc:"JSON Architecture"
       and greengrassGroupId =
         flag "greengrass-group-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_robot
           (Values.CreateRobotRequest.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~architecture:(Values.Architecture.of_json architecture)
              ~greengrassGroupId ())
           (Some Values.CreateRobotResponse.to_json)
           (Some Values.CreateRobotResponse.error_to_json)])
let create_robot_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sources =
         flag "sources" (optional json_arg) ~doc:"JSON SourceConfigs"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and environment =
         flag "environment" (optional json_arg) ~doc:"JSON Environment"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and robotSoftwareSuite =
         flag "robot-software-suite" (required json_arg)
           ~doc:"JSON RobotSoftwareSuite" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_robot_application
           (Values.CreateRobotApplicationRequest.make
              ?sources:(Option.map ~f:Values.SourceConfigs.of_json sources)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?environment:(Option.map ~f:Values.Environment.of_json
                              environment) ~name
              ~robotSoftwareSuite:(Values.RobotSoftwareSuite.of_json
                                     robotSoftwareSuite) ())
           (Some Values.CreateRobotApplicationResponse.to_json)
           (Some Values.CreateRobotApplicationResponse.error_to_json)])
let create_robot_application_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and currentRevisionId =
         flag "current-revision-id" (optional string)
           ~doc:"STRING RevisionId"
       and s3Etags = flag "s3-etags" (optional json_arg) ~doc:"JSON S3Etags"
       and imageDigest =
         flag "image-digest" (optional string) ~doc:"STRING ImageDigest"
       and application =
         flag "application" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_robot_application_version
           (Values.CreateRobotApplicationVersionRequest.make
              ?currentRevisionId
              ?s3Etags:(Option.map ~f:Values.S3Etags.of_json s3Etags)
              ?imageDigest ~application ())
           (Some Values.CreateRobotApplicationVersionResponse.to_json)
           (Some Values.CreateRobotApplicationVersionResponse.error_to_json)])
let create_simulation_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sources =
         flag "sources" (optional json_arg) ~doc:"JSON SourceConfigs"
       and renderingEngine =
         flag "rendering-engine" (optional json_arg)
           ~doc:"JSON RenderingEngine"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and environment =
         flag "environment" (optional json_arg) ~doc:"JSON Environment"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and simulationSoftwareSuite =
         flag "simulation-software-suite" (required json_arg)
           ~doc:"JSON SimulationSoftwareSuite"
       and robotSoftwareSuite =
         flag "robot-software-suite" (required json_arg)
           ~doc:"JSON RobotSoftwareSuite" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_simulation_application
           (Values.CreateSimulationApplicationRequest.make
              ?sources:(Option.map ~f:Values.SourceConfigs.of_json sources)
              ?renderingEngine:(Option.map ~f:Values.RenderingEngine.of_json
                                  renderingEngine)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?environment:(Option.map ~f:Values.Environment.of_json
                              environment) ~name
              ~simulationSoftwareSuite:(Values.SimulationSoftwareSuite.of_json
                                          simulationSoftwareSuite)
              ~robotSoftwareSuite:(Values.RobotSoftwareSuite.of_json
                                     robotSoftwareSuite) ())
           (Some Values.CreateSimulationApplicationResponse.to_json)
           (Some Values.CreateSimulationApplicationResponse.error_to_json)])
let create_simulation_application_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and currentRevisionId =
         flag "current-revision-id" (optional string)
           ~doc:"STRING RevisionId"
       and s3Etags = flag "s3-etags" (optional json_arg) ~doc:"JSON S3Etags"
       and imageDigest =
         flag "image-digest" (optional string) ~doc:"STRING ImageDigest"
       and application =
         flag "application" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_simulation_application_version
           (Values.CreateSimulationApplicationVersionRequest.make
              ?currentRevisionId
              ?s3Etags:(Option.map ~f:Values.S3Etags.of_json s3Etags)
              ?imageDigest ~application ())
           (Some Values.CreateSimulationApplicationVersionResponse.to_json)
           (Some
              Values.CreateSimulationApplicationVersionResponse.error_to_json)])
let create_simulation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and outputLocation =
         flag "output-location" (optional json_arg)
           ~doc:"JSON OutputLocation"
       and loggingConfig =
         flag "logging-config" (optional json_arg) ~doc:"JSON LoggingConfig"
       and failureBehavior =
         flag "failure-behavior" (optional json_arg)
           ~doc:"JSON FailureBehavior"
       and robotApplications =
         flag "robot-applications" (optional json_arg)
           ~doc:"JSON RobotApplicationConfigs"
       and simulationApplications =
         flag "simulation-applications" (optional json_arg)
           ~doc:"JSON SimulationApplicationConfigs"
       and dataSources =
         flag "data-sources" (optional json_arg)
           ~doc:"JSON DataSourceConfigs"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VPCConfig"
       and compute = flag "compute" (optional json_arg) ~doc:"JSON Compute"
       and maxJobDurationInSeconds =
         flag "max-job-duration-in-seconds" (required json_arg)
           ~doc:"JSON JobDuration"
       and iamRole = flag "iam-role" (required string) ~doc:"STRING IamRole" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_simulation_job
           (Values.CreateSimulationJobRequest.make ?clientRequestToken
              ?outputLocation:(Option.map ~f:Values.OutputLocation.of_json
                                 outputLocation)
              ?loggingConfig:(Option.map ~f:Values.LoggingConfig.of_json
                                loggingConfig)
              ?failureBehavior:(Option.map ~f:Values.FailureBehavior.of_json
                                  failureBehavior)
              ?robotApplications:(Option.map
                                    ~f:Values.RobotApplicationConfigs.of_json
                                    robotApplications)
              ?simulationApplications:(Option.map
                                         ~f:Values.SimulationApplicationConfigs.of_json
                                         simulationApplications)
              ?dataSources:(Option.map ~f:Values.DataSourceConfigs.of_json
                              dataSources)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?vpcConfig:(Option.map ~f:Values.VPCConfig.of_json vpcConfig)
              ?compute:(Option.map ~f:Values.Compute.of_json compute)
              ~maxJobDurationInSeconds:(Values.JobDuration.of_json
                                          maxJobDurationInSeconds) ~iamRole
              ()) (Some Values.CreateSimulationJobResponse.to_json)
           (Some Values.CreateSimulationJobResponse.error_to_json)])
let create_world_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and worlds = flag "worlds" (required json_arg) ~doc:"JSON Arns"
       and outputLocation =
         flag "output-location" (required json_arg)
           ~doc:"JSON OutputLocation"
       and iamRole = flag "iam-role" (required string) ~doc:"STRING IamRole" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_world_export_job
           (Values.CreateWorldExportJobRequest.make ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~worlds:(Values.Arns.of_json worlds)
              ~outputLocation:(Values.OutputLocation.of_json outputLocation)
              ~iamRole ()) (Some Values.CreateWorldExportJobResponse.to_json)
           (Some Values.CreateWorldExportJobResponse.error_to_json)])
let create_world_generation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and worldTags =
         flag "world-tags" (optional json_arg) ~doc:"JSON TagMap"
       and template = flag "template" (required string) ~doc:"STRING Arn"
       and worldCount =
         flag "world-count" (required json_arg) ~doc:"JSON WorldCount" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_world_generation_job
           (Values.CreateWorldGenerationJobRequest.make ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?worldTags:(Option.map ~f:Values.TagMap.of_json worldTags)
              ~template ~worldCount:(Values.WorldCount.of_json worldCount) ())
           (Some Values.CreateWorldGenerationJobResponse.to_json)
           (Some Values.CreateWorldGenerationJobResponse.error_to_json)])
let create_world_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and name = flag "name" (optional string) ~doc:"STRING TemplateName"
       and templateBody =
         flag "template-body" (optional string) ~doc:"STRING Json"
       and templateLocation =
         flag "template-location" (optional json_arg)
           ~doc:"JSON TemplateLocation"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_world_template
           (Values.CreateWorldTemplateRequest.make ?clientRequestToken ?name
              ?templateBody
              ?templateLocation:(Option.map
                                   ~f:Values.TemplateLocation.of_json
                                   templateLocation)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ())
           (Some Values.CreateWorldTemplateResponse.to_json)
           (Some Values.CreateWorldTemplateResponse.error_to_json)])
let delete_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleet = flag "fleet" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_fleet (Values.DeleteFleetRequest.make ~fleet ())
           (Some Values.DeleteFleetResponse.to_json)
           (Some Values.DeleteFleetResponse.error_to_json)])
let delete_robot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and robot = flag "robot" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_robot (Values.DeleteRobotRequest.make ~robot ())
           (Some Values.DeleteRobotResponse.to_json)
           (Some Values.DeleteRobotResponse.error_to_json)])
let delete_robot_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationVersion =
         flag "application-version" (optional string) ~doc:"STRING Version"
       and application =
         flag "application" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_robot_application
           (Values.DeleteRobotApplicationRequest.make ?applicationVersion
              ~application ())
           (Some Values.DeleteRobotApplicationResponse.to_json)
           (Some Values.DeleteRobotApplicationResponse.error_to_json)])
let delete_simulation_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationVersion =
         flag "application-version" (optional string) ~doc:"STRING Version"
       and application =
         flag "application" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_simulation_application
           (Values.DeleteSimulationApplicationRequest.make
              ?applicationVersion ~application ())
           (Some Values.DeleteSimulationApplicationResponse.to_json)
           (Some Values.DeleteSimulationApplicationResponse.error_to_json)])
let delete_world_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and template = flag "template" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_world_template
           (Values.DeleteWorldTemplateRequest.make ~template ())
           (Some Values.DeleteWorldTemplateResponse.to_json)
           (Some Values.DeleteWorldTemplateResponse.error_to_json)])
let deregister_robot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleet = flag "fleet" (required string) ~doc:"STRING Arn"
       and robot = flag "robot" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_robot
           (Values.DeregisterRobotRequest.make ~fleet ~robot ())
           (Some Values.DeregisterRobotResponse.to_json)
           (Some Values.DeregisterRobotResponse.error_to_json)])
let describe_deployment_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and job = flag "job" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_deployment_job
           (Values.DescribeDeploymentJobRequest.make ~job ())
           (Some Values.DescribeDeploymentJobResponse.to_json)
           (Some Values.DescribeDeploymentJobResponse.error_to_json)])
let describe_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleet = flag "fleet" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleet (Values.DescribeFleetRequest.make ~fleet ())
           (Some Values.DescribeFleetResponse.to_json)
           (Some Values.DescribeFleetResponse.error_to_json)])
let describe_robot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and robot = flag "robot" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_robot (Values.DescribeRobotRequest.make ~robot ())
           (Some Values.DescribeRobotResponse.to_json)
           (Some Values.DescribeRobotResponse.error_to_json)])
let describe_robot_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationVersion =
         flag "application-version" (optional string) ~doc:"STRING Version"
       and application =
         flag "application" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_robot_application
           (Values.DescribeRobotApplicationRequest.make ?applicationVersion
              ~application ())
           (Some Values.DescribeRobotApplicationResponse.to_json)
           (Some Values.DescribeRobotApplicationResponse.error_to_json)])
let describe_simulation_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationVersion =
         flag "application-version" (optional string) ~doc:"STRING Version"
       and application =
         flag "application" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_simulation_application
           (Values.DescribeSimulationApplicationRequest.make
              ?applicationVersion ~application ())
           (Some Values.DescribeSimulationApplicationResponse.to_json)
           (Some Values.DescribeSimulationApplicationResponse.error_to_json)])
let describe_simulation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and job = flag "job" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_simulation_job
           (Values.DescribeSimulationJobRequest.make ~job ())
           (Some Values.DescribeSimulationJobResponse.to_json)
           (Some Values.DescribeSimulationJobResponse.error_to_json)])
let describe_simulation_job_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and batch = flag "batch" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_simulation_job_batch
           (Values.DescribeSimulationJobBatchRequest.make ~batch ())
           (Some Values.DescribeSimulationJobBatchResponse.to_json)
           (Some Values.DescribeSimulationJobBatchResponse.error_to_json)])
let describe_world =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and world = flag "world" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_world (Values.DescribeWorldRequest.make ~world ())
           (Some Values.DescribeWorldResponse.to_json)
           (Some Values.DescribeWorldResponse.error_to_json)])
let describe_world_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and job = flag "job" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_world_export_job
           (Values.DescribeWorldExportJobRequest.make ~job ())
           (Some Values.DescribeWorldExportJobResponse.to_json)
           (Some Values.DescribeWorldExportJobResponse.error_to_json)])
let describe_world_generation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and job = flag "job" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_world_generation_job
           (Values.DescribeWorldGenerationJobRequest.make ~job ())
           (Some Values.DescribeWorldGenerationJobResponse.to_json)
           (Some Values.DescribeWorldGenerationJobResponse.error_to_json)])
let describe_world_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and template = flag "template" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_world_template
           (Values.DescribeWorldTemplateRequest.make ~template ())
           (Some Values.DescribeWorldTemplateResponse.to_json)
           (Some Values.DescribeWorldTemplateResponse.error_to_json)])
let get_world_template_body =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and template = flag "template" (optional string) ~doc:"STRING Arn"
       and generationJob =
         flag "generation-job" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_world_template_body
           (Values.GetWorldTemplateBodyRequest.make ?template ?generationJob
              ()) (Some Values.GetWorldTemplateBodyResponse.to_json)
           (Some Values.GetWorldTemplateBodyResponse.error_to_json)])
let list_deployment_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_deployment_jobs
           (Values.ListDeploymentJobsRequest.make
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.ListDeploymentJobsResponse.to_json)
           (Some Values.ListDeploymentJobsResponse.error_to_json)])
let list_fleets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_fleets
           (Values.ListFleetsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListFleetsResponse.to_json)
           (Some Values.ListFleetsResponse.error_to_json)])
let list_robot_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionQualifier =
         flag "version-qualifier" (optional string)
           ~doc:"STRING VersionQualifier"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_robot_applications
           (Values.ListRobotApplicationsRequest.make ?versionQualifier
              ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListRobotApplicationsResponse.to_json)
           (Some Values.ListRobotApplicationsResponse.error_to_json)])
let list_robots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_robots
           (Values.ListRobotsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListRobotsResponse.to_json)
           (Some Values.ListRobotsResponse.error_to_json)])
let list_simulation_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionQualifier =
         flag "version-qualifier" (optional string)
           ~doc:"STRING VersionQualifier"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_simulation_applications
           (Values.ListSimulationApplicationsRequest.make ?versionQualifier
              ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListSimulationApplicationsResponse.to_json)
           (Some Values.ListSimulationApplicationsResponse.error_to_json)])
let list_simulation_job_batches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_simulation_job_batches
           (Values.ListSimulationJobBatchesRequest.make ?nextToken
              ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListSimulationJobBatchesResponse.to_json)
           (Some Values.ListSimulationJobBatchesResponse.error_to_json)])
let list_simulation_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_simulation_jobs
           (Values.ListSimulationJobsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListSimulationJobsResponse.to_json)
           (Some Values.ListSimulationJobsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_world_export_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_world_export_jobs
           (Values.ListWorldExportJobsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListWorldExportJobsResponse.to_json)
           (Some Values.ListWorldExportJobsResponse.error_to_json)])
let list_world_generation_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_world_generation_jobs
           (Values.ListWorldGenerationJobsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListWorldGenerationJobsResponse.to_json)
           (Some Values.ListWorldGenerationJobsResponse.error_to_json)])
let list_world_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_world_templates
           (Values.ListWorldTemplatesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListWorldTemplatesResponse.to_json)
           (Some Values.ListWorldTemplatesResponse.error_to_json)])
let list_worlds =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_worlds
           (Values.ListWorldsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListWorldsResponse.to_json)
           (Some Values.ListWorldsResponse.error_to_json)])
let register_robot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleet = flag "fleet" (required string) ~doc:"STRING Arn"
       and robot = flag "robot" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_robot
           (Values.RegisterRobotRequest.make ~fleet ~robot ())
           (Some Values.RegisterRobotResponse.to_json)
           (Some Values.RegisterRobotResponse.error_to_json)])
let restart_simulation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and job = flag "job" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restart_simulation_job
           (Values.RestartSimulationJobRequest.make ~job ())
           (Some Values.RestartSimulationJobResponse.to_json)
           (Some Values.RestartSimulationJobResponse.error_to_json)])
let start_simulation_job_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and batchPolicy =
         flag "batch-policy" (optional json_arg) ~doc:"JSON BatchPolicy"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and createSimulationJobRequests =
         flag "create-simulation-job-requests" (required json_arg)
           ~doc:"JSON CreateSimulationJobRequests" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_simulation_job_batch
           (Values.StartSimulationJobBatchRequest.make ?clientRequestToken
              ?batchPolicy:(Option.map ~f:Values.BatchPolicy.of_json
                              batchPolicy)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~createSimulationJobRequests:(Values.CreateSimulationJobRequests.of_json
                                              createSimulationJobRequests) ())
           (Some Values.StartSimulationJobBatchResponse.to_json)
           (Some Values.StartSimulationJobBatchResponse.error_to_json)])
let sync_deployment_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken"
       and fleet = flag "fleet" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.sync_deployment_job
           (Values.SyncDeploymentJobRequest.make ~clientRequestToken ~fleet
              ()) (Some Values.SyncDeploymentJobResponse.to_json)
           (Some Values.SyncDeploymentJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_robot_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sources =
         flag "sources" (optional json_arg) ~doc:"JSON SourceConfigs"
       and currentRevisionId =
         flag "current-revision-id" (optional string)
           ~doc:"STRING RevisionId"
       and environment =
         flag "environment" (optional json_arg) ~doc:"JSON Environment"
       and application =
         flag "application" (required string) ~doc:"STRING Arn"
       and robotSoftwareSuite =
         flag "robot-software-suite" (required json_arg)
           ~doc:"JSON RobotSoftwareSuite" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_robot_application
           (Values.UpdateRobotApplicationRequest.make
              ?sources:(Option.map ~f:Values.SourceConfigs.of_json sources)
              ?currentRevisionId
              ?environment:(Option.map ~f:Values.Environment.of_json
                              environment) ~application
              ~robotSoftwareSuite:(Values.RobotSoftwareSuite.of_json
                                     robotSoftwareSuite) ())
           (Some Values.UpdateRobotApplicationResponse.to_json)
           (Some Values.UpdateRobotApplicationResponse.error_to_json)])
let update_simulation_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sources =
         flag "sources" (optional json_arg) ~doc:"JSON SourceConfigs"
       and renderingEngine =
         flag "rendering-engine" (optional json_arg)
           ~doc:"JSON RenderingEngine"
       and currentRevisionId =
         flag "current-revision-id" (optional string)
           ~doc:"STRING RevisionId"
       and environment =
         flag "environment" (optional json_arg) ~doc:"JSON Environment"
       and application =
         flag "application" (required string) ~doc:"STRING Arn"
       and simulationSoftwareSuite =
         flag "simulation-software-suite" (required json_arg)
           ~doc:"JSON SimulationSoftwareSuite"
       and robotSoftwareSuite =
         flag "robot-software-suite" (required json_arg)
           ~doc:"JSON RobotSoftwareSuite" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_simulation_application
           (Values.UpdateSimulationApplicationRequest.make
              ?sources:(Option.map ~f:Values.SourceConfigs.of_json sources)
              ?renderingEngine:(Option.map ~f:Values.RenderingEngine.of_json
                                  renderingEngine) ?currentRevisionId
              ?environment:(Option.map ~f:Values.Environment.of_json
                              environment) ~application
              ~simulationSoftwareSuite:(Values.SimulationSoftwareSuite.of_json
                                          simulationSoftwareSuite)
              ~robotSoftwareSuite:(Values.RobotSoftwareSuite.of_json
                                     robotSoftwareSuite) ())
           (Some Values.UpdateSimulationApplicationResponse.to_json)
           (Some Values.UpdateSimulationApplicationResponse.error_to_json)])
let update_world_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING TemplateName"
       and templateBody =
         flag "template-body" (optional string) ~doc:"STRING Json"
       and templateLocation =
         flag "template-location" (optional json_arg)
           ~doc:"JSON TemplateLocation"
       and template = flag "template" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_world_template
           (Values.UpdateWorldTemplateRequest.make ?name ?templateBody
              ?templateLocation:(Option.map
                                   ~f:Values.TemplateLocation.of_json
                                   templateLocation) ~template ())
           (Some Values.UpdateWorldTemplateResponse.to_json)
           (Some Values.UpdateWorldTemplateResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("batch-delete-worlds", batch_delete_worlds);
    ("batch-describe-simulation-job", batch_describe_simulation_job);
    ("cancel-deployment-job", cancel_deployment_job);
    ("cancel-simulation-job", cancel_simulation_job);
    ("cancel-simulation-job-batch", cancel_simulation_job_batch);
    ("cancel-world-export-job", cancel_world_export_job);
    ("cancel-world-generation-job", cancel_world_generation_job);
    ("create-deployment-job", create_deployment_job);
    ("create-fleet", create_fleet);
    ("create-robot", create_robot);
    ("create-robot-application", create_robot_application);
    ("create-robot-application-version", create_robot_application_version);
    ("create-simulation-application", create_simulation_application);
    ("create-simulation-application-version",
      create_simulation_application_version);
    ("create-simulation-job", create_simulation_job);
    ("create-world-export-job", create_world_export_job);
    ("create-world-generation-job", create_world_generation_job);
    ("create-world-template", create_world_template);
    ("delete-fleet", delete_fleet);
    ("delete-robot", delete_robot);
    ("delete-robot-application", delete_robot_application);
    ("delete-simulation-application", delete_simulation_application);
    ("delete-world-template", delete_world_template);
    ("deregister-robot", deregister_robot);
    ("describe-deployment-job", describe_deployment_job);
    ("describe-fleet", describe_fleet);
    ("describe-robot", describe_robot);
    ("describe-robot-application", describe_robot_application);
    ("describe-simulation-application", describe_simulation_application);
    ("describe-simulation-job", describe_simulation_job);
    ("describe-simulation-job-batch", describe_simulation_job_batch);
    ("describe-world", describe_world);
    ("describe-world-export-job", describe_world_export_job);
    ("describe-world-generation-job", describe_world_generation_job);
    ("describe-world-template", describe_world_template);
    ("get-world-template-body", get_world_template_body);
    ("list-deployment-jobs", list_deployment_jobs);
    ("list-fleets", list_fleets);
    ("list-robot-applications", list_robot_applications);
    ("list-robots", list_robots);
    ("list-simulation-applications", list_simulation_applications);
    ("list-simulation-job-batches", list_simulation_job_batches);
    ("list-simulation-jobs", list_simulation_jobs);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-world-export-jobs", list_world_export_jobs);
    ("list-world-generation-jobs", list_world_generation_jobs);
    ("list-world-templates", list_world_templates);
    ("list-worlds", list_worlds);
    ("register-robot", register_robot);
    ("restart-simulation-job", restart_simulation_job);
    ("start-simulation-job-batch", start_simulation_job_batch);
    ("sync-deployment-job", sync_deployment_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-robot-application", update_robot_application);
    ("update-simulation-application", update_simulation_application);
    ("update-world-template", update_world_template)]
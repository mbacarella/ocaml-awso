(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_associate_scram_secret =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string"
       and secretArnList =
         flag "secret-arn-list" (required json_arg)
           ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_associate_scram_secret
           (Values.BatchAssociateScramSecretRequest.make ~clusterArn
              ~secretArnList:(Values.Zz__listOf__string.of_json secretArnList)
              ()) (Some Values.BatchAssociateScramSecretResponse.to_json)
           (Some Values.BatchAssociateScramSecretResponse.error_to_json)])
let create_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientAuthentication =
         flag "client-authentication" (optional json_arg)
           ~doc:"JSON ClientAuthentication"
       and configurationInfo =
         flag "configuration-info" (optional json_arg)
           ~doc:"JSON ConfigurationInfo"
       and encryptionInfo =
         flag "encryption-info" (optional json_arg)
           ~doc:"JSON EncryptionInfo"
       and enhancedMonitoring =
         flag "enhanced-monitoring" (optional json_arg)
           ~doc:"JSON EnhancedMonitoring"
       and openMonitoring =
         flag "open-monitoring" (optional json_arg)
           ~doc:"JSON OpenMonitoringInfo"
       and loggingInfo =
         flag "logging-info" (optional json_arg) ~doc:"JSON LoggingInfo"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON __mapOf__string"
       and brokerNodeGroupInfo =
         flag "broker-node-group-info" (required json_arg)
           ~doc:"JSON BrokerNodeGroupInfo"
       and clusterName =
         flag "cluster-name" (required string)
           ~doc:"STRING __stringMin1Max64"
       and kafkaVersion =
         flag "kafka-version" (required string)
           ~doc:"STRING __stringMin1Max128"
       and numberOfBrokerNodes =
         flag "number-of-broker-nodes" (required int)
           ~doc:"INT __integerMin1Max15" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster
           (Values.CreateClusterRequest.make
              ?clientAuthentication:(Option.map
                                       ~f:Values.ClientAuthentication.of_json
                                       clientAuthentication)
              ?configurationInfo:(Option.map
                                    ~f:Values.ConfigurationInfo.of_json
                                    configurationInfo)
              ?encryptionInfo:(Option.map ~f:Values.EncryptionInfo.of_json
                                 encryptionInfo)
              ?enhancedMonitoring:(Option.map
                                     ~f:Values.EnhancedMonitoring.of_json
                                     enhancedMonitoring)
              ?openMonitoring:(Option.map
                                 ~f:Values.OpenMonitoringInfo.of_json
                                 openMonitoring)
              ?loggingInfo:(Option.map ~f:Values.LoggingInfo.of_json
                              loggingInfo)
              ?tags:(Option.map ~f:Values.Zz__mapOf__string.of_json tags)
              ~brokerNodeGroupInfo:(Values.BrokerNodeGroupInfo.of_json
                                      brokerNodeGroupInfo) ~clusterName
              ~kafkaVersion ~numberOfBrokerNodes ())
           (Some Values.CreateClusterResponse.to_json)
           (Some Values.CreateClusterResponse.error_to_json)])
let create_cluster_v2 =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON __mapOf__string"
       and provisioned =
         flag "provisioned" (optional json_arg)
           ~doc:"JSON ProvisionedRequest"
       and serverless =
         flag "serverless" (optional json_arg) ~doc:"JSON ServerlessRequest"
       and clusterName =
         flag "cluster-name" (required string)
           ~doc:"STRING __stringMin1Max64" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster_v2
           (Values.CreateClusterV2Request.make
              ?tags:(Option.map ~f:Values.Zz__mapOf__string.of_json tags)
              ?provisioned:(Option.map ~f:Values.ProvisionedRequest.of_json
                              provisioned)
              ?serverless:(Option.map ~f:Values.ServerlessRequest.of_json
                             serverless) ~clusterName ())
           (Some Values.CreateClusterV2Response.to_json)
           (Some Values.CreateClusterV2Response.error_to_json)])
let create_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and kafkaVersions =
         flag "kafka-versions" (optional json_arg)
           ~doc:"JSON __listOf__string"
       and name = flag "name" (required string) ~doc:"STRING __string"
       and serverProperties =
         flag "server-properties" (required json_arg) ~doc:"JSON __blob" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration
           (Values.CreateConfigurationRequest.make ?description
              ?kafkaVersions:(Option.map ~f:Values.Zz__listOf__string.of_json
                                kafkaVersions) ~name
              ~serverProperties:(Values.Zz__blob.of_json serverProperties) ())
           (Some Values.CreateConfigurationResponse.to_json)
           (Some Values.CreateConfigurationResponse.error_to_json)])
let delete_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and currentVersion =
         flag "current-version" (optional string) ~doc:"STRING __string"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster
           (Values.DeleteClusterRequest.make ?currentVersion ~clusterArn ())
           (Some Values.DeleteClusterResponse.to_json)
           (Some Values.DeleteClusterResponse.error_to_json)])
let delete_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration
           (Values.DeleteConfigurationRequest.make ~arn ())
           (Some Values.DeleteConfigurationResponse.to_json)
           (Some Values.DeleteConfigurationResponse.error_to_json)])
let describe_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster
           (Values.DescribeClusterRequest.make ~clusterArn ())
           (Some Values.DescribeClusterResponse.to_json)
           (Some Values.DescribeClusterResponse.error_to_json)])
let describe_cluster_v2 =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster_v2
           (Values.DescribeClusterV2Request.make ~clusterArn ())
           (Some Values.DescribeClusterV2Response.to_json)
           (Some Values.DescribeClusterV2Response.error_to_json)])
let describe_cluster_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterOperationArn =
         flag "cluster-operation-arn" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster_operation
           (Values.DescribeClusterOperationRequest.make ~clusterOperationArn
              ()) (Some Values.DescribeClusterOperationResponse.to_json)
           (Some Values.DescribeClusterOperationResponse.error_to_json)])
let describe_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_configuration
           (Values.DescribeConfigurationRequest.make ~arn ())
           (Some Values.DescribeConfigurationResponse.to_json)
           (Some Values.DescribeConfigurationResponse.error_to_json)])
let describe_configuration_revision =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING __string"
       and revision = flag "revision" (required json_arg) ~doc:"JSON __long" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_configuration_revision
           (Values.DescribeConfigurationRevisionRequest.make ~arn
              ~revision:(Values.Zz__long.of_json revision) ())
           (Some Values.DescribeConfigurationRevisionResponse.to_json)
           (Some Values.DescribeConfigurationRevisionResponse.error_to_json)])
let batch_disassociate_scram_secret =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string"
       and secretArnList =
         flag "secret-arn-list" (required json_arg)
           ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_disassociate_scram_secret
           (Values.BatchDisassociateScramSecretRequest.make ~clusterArn
              ~secretArnList:(Values.Zz__listOf__string.of_json secretArnList)
              ()) (Some Values.BatchDisassociateScramSecretResponse.to_json)
           (Some Values.BatchDisassociateScramSecretResponse.error_to_json)])
let get_bootstrap_brokers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bootstrap_brokers
           (Values.GetBootstrapBrokersRequest.make ~clusterArn ())
           (Some Values.GetBootstrapBrokersResponse.to_json)
           (Some Values.GetBootstrapBrokersResponse.error_to_json)])
let get_compatible_kafka_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_compatible_kafka_versions
           (Values.GetCompatibleKafkaVersionsRequest.make ?clusterArn ())
           (Some Values.GetCompatibleKafkaVersionsResponse.to_json)
           (Some Values.GetCompatibleKafkaVersionsResponse.error_to_json)])
let list_cluster_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_cluster_operations
           (Values.ListClusterOperationsRequest.make ?maxResults ?nextToken
              ~clusterArn ())
           (Some Values.ListClusterOperationsResponse.to_json)
           (Some Values.ListClusterOperationsResponse.error_to_json)])
let list_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterNameFilter =
         flag "cluster-name-filter" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_clusters
           (Values.ListClustersRequest.make ?clusterNameFilter ?maxResults
              ?nextToken ()) (Some Values.ListClustersResponse.to_json)
           (Some Values.ListClustersResponse.error_to_json)])
let list_clusters_v2 =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterNameFilter =
         flag "cluster-name-filter" (optional string) ~doc:"STRING __string"
       and clusterTypeFilter =
         flag "cluster-type-filter" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_clusters_v2
           (Values.ListClustersV2Request.make ?clusterNameFilter
              ?clusterTypeFilter ?maxResults ?nextToken ())
           (Some Values.ListClustersV2Response.to_json)
           (Some Values.ListClustersV2Response.error_to_json)])
let list_configuration_revisions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and arn = flag "arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configuration_revisions
           (Values.ListConfigurationRevisionsRequest.make ?maxResults
              ?nextToken ~arn ())
           (Some Values.ListConfigurationRevisionsResponse.to_json)
           (Some Values.ListConfigurationRevisionsResponse.error_to_json)])
let list_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configurations
           (Values.ListConfigurationsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListConfigurationsResponse.to_json)
           (Some Values.ListConfigurationsResponse.error_to_json)])
let list_kafka_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_kafka_versions
           (Values.ListKafkaVersionsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListKafkaVersionsResponse.to_json)
           (Some Values.ListKafkaVersionsResponse.error_to_json)])
let list_nodes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_nodes
           (Values.ListNodesRequest.make ?maxResults ?nextToken ~clusterArn
              ()) (Some Values.ListNodesResponse.to_json)
           (Some Values.ListNodesResponse.error_to_json)])
let list_scram_secrets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_scram_secrets
           (Values.ListScramSecretsRequest.make ?maxResults ?nextToken
              ~clusterArn ()) (Some Values.ListScramSecretsResponse.to_json)
           (Some Values.ListScramSecretsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let reboot_broker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and brokerIds =
         flag "broker-ids" (required json_arg) ~doc:"JSON __listOf__string"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_broker
           (Values.RebootBrokerRequest.make
              ~brokerIds:(Values.Zz__listOf__string.of_json brokerIds)
              ~clusterArn ()) (Some Values.RebootBrokerResponse.to_json)
           (Some Values.RebootBrokerResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tags = flag "tags" (required json_arg) ~doc:"JSON __mapOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Zz__mapOf__string.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.Zz__listOf__string.of_json tagKeys) ()) None
           None])
let update_broker_count =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string"
       and currentVersion =
         flag "current-version" (required string) ~doc:"STRING __string"
       and targetNumberOfBrokerNodes =
         flag "target-number-of-broker-nodes" (required int)
           ~doc:"INT __integerMin1Max15" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_broker_count
           (Values.UpdateBrokerCountRequest.make ~clusterArn ~currentVersion
              ~targetNumberOfBrokerNodes ())
           (Some Values.UpdateBrokerCountResponse.to_json)
           (Some Values.UpdateBrokerCountResponse.error_to_json)])
let update_broker_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string"
       and currentVersion =
         flag "current-version" (required string) ~doc:"STRING __string"
       and targetInstanceType =
         flag "target-instance-type" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_broker_type
           (Values.UpdateBrokerTypeRequest.make ~clusterArn ~currentVersion
              ~targetInstanceType ())
           (Some Values.UpdateBrokerTypeResponse.to_json)
           (Some Values.UpdateBrokerTypeResponse.error_to_json)])
let update_broker_storage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string"
       and currentVersion =
         flag "current-version" (required string) ~doc:"STRING __string"
       and targetBrokerEBSVolumeInfo =
         flag "target-broker-e-b-s-volume-info" (required json_arg)
           ~doc:"JSON __listOfBrokerEBSVolumeInfo" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_broker_storage
           (Values.UpdateBrokerStorageRequest.make ~clusterArn
              ~currentVersion
              ~targetBrokerEBSVolumeInfo:(Values.Zz__listOfBrokerEBSVolumeInfo.of_json
                                            targetBrokerEBSVolumeInfo) ())
           (Some Values.UpdateBrokerStorageResponse.to_json)
           (Some Values.UpdateBrokerStorageResponse.error_to_json)])
let update_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and arn = flag "arn" (required string) ~doc:"STRING __string"
       and serverProperties =
         flag "server-properties" (required json_arg) ~doc:"JSON __blob" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_configuration
           (Values.UpdateConfigurationRequest.make ?description ~arn
              ~serverProperties:(Values.Zz__blob.of_json serverProperties) ())
           (Some Values.UpdateConfigurationResponse.to_json)
           (Some Values.UpdateConfigurationResponse.error_to_json)])
let update_connectivity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string"
       and connectivityInfo =
         flag "connectivity-info" (required json_arg)
           ~doc:"JSON ConnectivityInfo"
       and currentVersion =
         flag "current-version" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connectivity
           (Values.UpdateConnectivityRequest.make ~clusterArn
              ~connectivityInfo:(Values.ConnectivityInfo.of_json
                                   connectivityInfo) ~currentVersion ())
           (Some Values.UpdateConnectivityResponse.to_json)
           (Some Values.UpdateConnectivityResponse.error_to_json)])
let update_cluster_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string"
       and configurationInfo =
         flag "configuration-info" (required json_arg)
           ~doc:"JSON ConfigurationInfo"
       and currentVersion =
         flag "current-version" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cluster_configuration
           (Values.UpdateClusterConfigurationRequest.make ~clusterArn
              ~configurationInfo:(Values.ConfigurationInfo.of_json
                                    configurationInfo) ~currentVersion ())
           (Some Values.UpdateClusterConfigurationResponse.to_json)
           (Some Values.UpdateClusterConfigurationResponse.error_to_json)])
let update_cluster_kafka_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationInfo =
         flag "configuration-info" (optional json_arg)
           ~doc:"JSON ConfigurationInfo"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string"
       and currentVersion =
         flag "current-version" (required string) ~doc:"STRING __string"
       and targetKafkaVersion =
         flag "target-kafka-version" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cluster_kafka_version
           (Values.UpdateClusterKafkaVersionRequest.make
              ?configurationInfo:(Option.map
                                    ~f:Values.ConfigurationInfo.of_json
                                    configurationInfo) ~clusterArn
              ~currentVersion ~targetKafkaVersion ())
           (Some Values.UpdateClusterKafkaVersionResponse.to_json)
           (Some Values.UpdateClusterKafkaVersionResponse.error_to_json)])
let update_monitoring =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enhancedMonitoring =
         flag "enhanced-monitoring" (optional json_arg)
           ~doc:"JSON EnhancedMonitoring"
       and openMonitoring =
         flag "open-monitoring" (optional json_arg)
           ~doc:"JSON OpenMonitoringInfo"
       and loggingInfo =
         flag "logging-info" (optional json_arg) ~doc:"JSON LoggingInfo"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string"
       and currentVersion =
         flag "current-version" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_monitoring
           (Values.UpdateMonitoringRequest.make
              ?enhancedMonitoring:(Option.map
                                     ~f:Values.EnhancedMonitoring.of_json
                                     enhancedMonitoring)
              ?openMonitoring:(Option.map
                                 ~f:Values.OpenMonitoringInfo.of_json
                                 openMonitoring)
              ?loggingInfo:(Option.map ~f:Values.LoggingInfo.of_json
                              loggingInfo) ~clusterArn ~currentVersion ())
           (Some Values.UpdateMonitoringResponse.to_json)
           (Some Values.UpdateMonitoringResponse.error_to_json)])
let update_security =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientAuthentication =
         flag "client-authentication" (optional json_arg)
           ~doc:"JSON ClientAuthentication"
       and encryptionInfo =
         flag "encryption-info" (optional json_arg)
           ~doc:"JSON EncryptionInfo"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string"
       and currentVersion =
         flag "current-version" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_security
           (Values.UpdateSecurityRequest.make
              ?clientAuthentication:(Option.map
                                       ~f:Values.ClientAuthentication.of_json
                                       clientAuthentication)
              ?encryptionInfo:(Option.map ~f:Values.EncryptionInfo.of_json
                                 encryptionInfo) ~clusterArn ~currentVersion
              ()) (Some Values.UpdateSecurityResponse.to_json)
           (Some Values.UpdateSecurityResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("batch-associate-scram-secret", batch_associate_scram_secret);
    ("create-cluster", create_cluster);
    ("create-cluster-v2", create_cluster_v2);
    ("create-configuration", create_configuration);
    ("delete-cluster", delete_cluster);
    ("delete-configuration", delete_configuration);
    ("describe-cluster", describe_cluster);
    ("describe-cluster-v2", describe_cluster_v2);
    ("describe-cluster-operation", describe_cluster_operation);
    ("describe-configuration", describe_configuration);
    ("describe-configuration-revision", describe_configuration_revision);
    ("batch-disassociate-scram-secret", batch_disassociate_scram_secret);
    ("get-bootstrap-brokers", get_bootstrap_brokers);
    ("get-compatible-kafka-versions", get_compatible_kafka_versions);
    ("list-cluster-operations", list_cluster_operations);
    ("list-clusters", list_clusters);
    ("list-clusters-v2", list_clusters_v2);
    ("list-configuration-revisions", list_configuration_revisions);
    ("list-configurations", list_configurations);
    ("list-kafka-versions", list_kafka_versions);
    ("list-nodes", list_nodes);
    ("list-scram-secrets", list_scram_secrets);
    ("list-tags-for-resource", list_tags_for_resource);
    ("reboot-broker", reboot_broker);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-broker-count", update_broker_count);
    ("update-broker-type", update_broker_type);
    ("update-broker-storage", update_broker_storage);
    ("update-configuration", update_configuration);
    ("update-connectivity", update_connectivity);
    ("update-cluster-configuration", update_cluster_configuration);
    ("update-cluster-kafka-version", update_cluster_kafka_version);
    ("update-monitoring", update_monitoring);
    ("update-security", update_security)]
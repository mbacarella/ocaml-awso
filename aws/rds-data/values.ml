(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.rds_data
let apiVersion = "2018-08-01"
let endpointPrefix = "rds-data"
let serviceFullName = "AWS RDS DataService"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module Arn =
  struct
    type nonrec t = string
    let context_ = "Arn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:11));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Arn" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module StringArray =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"StringArray" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module BoxedLong =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module LongArray =
  struct
    type nonrec t = BoxedLong.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BoxedLong.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BoxedLong.of_xml)
    let of_json j =
      list_of_json ~kind:"LongArray" ~of_json:BoxedLong.of_json j
    let to_json v = composed_to_json to_value v
  end
module BoxedDouble =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module DoubleArray =
  struct
    type nonrec t = BoxedDouble.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BoxedDouble.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BoxedDouble.of_xml)
    let of_json j =
      list_of_json ~kind:"DoubleArray" ~of_json:BoxedDouble.of_json j
    let to_json v = composed_to_json to_value v
  end
module BoxedBoolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module BooleanArray =
  struct
    type nonrec t = BoxedBoolean.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BoxedBoolean.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BoxedBoolean.of_xml)
    let of_json j =
      list_of_json ~kind:"BooleanArray" ~of_json:BoxedBoolean.of_json j
    let to_json v = composed_to_json to_value v
  end
module rec
  ArrayOfArray:sig
                 type nonrec t = ArrayValue.t list
                 val make : ArrayValue.t list -> t
                 val to_value : t -> Botodata.value
                 val to_query : t -> Client.Query.t
                 val of_xml : Xml.t -> ArrayValue.t list
                 val of_json : Yojson.Safe.t -> t
                 val to_json : t -> Yojson.Safe.t
                 val to_header : t -> string
               end =
  struct
    type nonrec t = ArrayValue.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ArrayValue.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ArrayValue.of_xml)
    let of_json j =
      list_of_json ~kind:"ArrayOfArray" ~of_json:ArrayValue.of_json j
    let to_json v = composed_to_json to_value v
  end
 and
  ArrayValue:sig
               type nonrec t =
                 {
                 arrayValues: ArrayOfArray.t option ;
                 booleanValues: BooleanArray.t option ;
                 doubleValues: DoubleArray.t option ;
                 longValues: LongArray.t option ;
                 stringValues: StringArray.t option }
               val make :
                 ?arrayValues:ArrayOfArray.t ->
                   ?booleanValues:BooleanArray.t ->
                     ?doubleValues:DoubleArray.t ->
                       ?longValues:LongArray.t ->
                         ?stringValues:StringArray.t -> unit -> t
               val to_value : t -> Botodata.value
               val to_query : t -> Client.Query.t
               val of_xml : Xml.t -> t
               val of_json : Yojson.Safe.t -> t
               val to_json : t -> Yojson.Safe.t
             end =
  struct
    type nonrec t =
      {
      arrayValues: ArrayOfArray.t option ;
      booleanValues: BooleanArray.t option ;
      doubleValues: DoubleArray.t option ;
      longValues: LongArray.t option ;
      stringValues: StringArray.t option }
    let make ?arrayValues =
      fun ?booleanValues ->
        fun ?doubleValues ->
          fun ?longValues ->
            fun ?stringValues ->
              fun () ->
                {
                  arrayValues;
                  booleanValues;
                  doubleValues;
                  longValues;
                  stringValues
                }
    let to_value x =
      structure_to_value
        [("arrayValues", (Option.map x.arrayValues ~f:ArrayOfArray.to_value));
        ("booleanValues",
          (Option.map x.booleanValues ~f:BooleanArray.to_value));
        ("doubleValues", (Option.map x.doubleValues ~f:DoubleArray.to_value));
        ("longValues", (Option.map x.longValues ~f:LongArray.to_value));
        ("stringValues", (Option.map x.stringValues ~f:StringArray.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stringValues =
        (Option.map ~f:StringArray.of_xml)
          (Xml.child xml_arg0 "stringValues") in
      let longValues =
        (Option.map ~f:LongArray.of_xml) (Xml.child xml_arg0 "longValues") in
      let doubleValues =
        (Option.map ~f:DoubleArray.of_xml)
          (Xml.child xml_arg0 "doubleValues") in
      let booleanValues =
        (Option.map ~f:BooleanArray.of_xml)
          (Xml.child xml_arg0 "booleanValues") in
      let arrayValues =
        (Option.map ~f:ArrayOfArray.of_xml)
          (Xml.child xml_arg0 "arrayValues") in
      make ?stringValues ?longValues ?doubleValues ?booleanValues
        ?arrayValues ()
    let of_json json =
      let stringValues = field_map json "stringValues" StringArray.of_json in
      let longValues = field_map json "longValues" LongArray.of_json in
      let doubleValues = field_map json "doubleValues" DoubleArray.of_json in
      let booleanValues = field_map json "booleanValues" BooleanArray.of_json in
      let arrayValues = field_map json "arrayValues" ArrayOfArray.of_json in
      make ?stringValues ?longValues ?doubleValues ?booleanValues
        ?arrayValues ()
    let to_json v = composed_to_json to_value v
  end
module BoxedInteger =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for BoxedInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module BoxedFloat =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Float x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a float" xml_arg0)
    let of_json j = float_of_json ~kind:"a float" j
    let to_json = simple_to_json to_value
  end
module Blob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module rec
  ArrayValueList:sig
                   type nonrec t = Value.t list
                   val make : Value.t list -> t
                   val to_value : t -> Botodata.value
                   val to_query : t -> Client.Query.t
                   val of_xml : Xml.t -> Value.t list
                   val of_json : Yojson.Safe.t -> t
                   val to_json : t -> Yojson.Safe.t
                   val to_header : t -> string
                 end =
  struct
    type nonrec t = Value.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Value.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Value.of_xml)
    let of_json j =
      list_of_json ~kind:"ArrayValueList" ~of_json:Value.of_json j
    let to_json v = composed_to_json to_value v
  end
 and
  StructValue:sig
                type nonrec t = {
                  attributes: ArrayValueList.t option }
                val make : ?attributes:ArrayValueList.t -> unit -> t
                val to_value : t -> Botodata.value
                val to_query : t -> Client.Query.t
                val of_xml : Xml.t -> t
                val of_json : Yojson.Safe.t -> t
                val to_json : t -> Yojson.Safe.t
              end =
  struct
    type nonrec t = {
      attributes: ArrayValueList.t option }
    let make ?attributes = fun () -> { attributes }
    let to_value x =
      structure_to_value
        [("attributes", (Option.map x.attributes ~f:ArrayValueList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributes =
        (Option.map ~f:ArrayValueList.of_xml)
          (Xml.child xml_arg0 "attributes") in
      make ?attributes ()
    let of_json json =
      let attributes = field_map json "attributes" ArrayValueList.of_json in
      make ?attributes ()
    let to_json v = composed_to_json to_value v
  end and
       Value:sig
               type nonrec t =
                 {
                 arrayValues: ArrayValueList.t option ;
                 bigIntValue: BoxedLong.t option ;
                 bitValue: BoxedBoolean.t option ;
                 blobValue: Blob.t option ;
                 doubleValue: BoxedDouble.t option ;
                 intValue: BoxedInteger.t option ;
                 isNull: BoxedBoolean.t option ;
                 realValue: BoxedFloat.t option ;
                 stringValue: String_.t option ;
                 structValue: StructValue.t option }
               val make :
                 ?arrayValues:ArrayValueList.t ->
                   ?bigIntValue:BoxedLong.t ->
                     ?bitValue:BoxedBoolean.t ->
                       ?blobValue:Blob.t ->
                         ?doubleValue:BoxedDouble.t ->
                           ?intValue:BoxedInteger.t ->
                             ?isNull:BoxedBoolean.t ->
                               ?realValue:BoxedFloat.t ->
                                 ?stringValue:String_.t ->
                                   ?structValue:StructValue.t -> unit -> t
               val to_value : t -> Botodata.value
               val to_query : t -> Client.Query.t
               val of_xml : Xml.t -> t
               val of_json : Yojson.Safe.t -> t
               val to_json : t -> Yojson.Safe.t
             end =
       struct
         type nonrec t =
           {
           arrayValues: ArrayValueList.t option ;
           bigIntValue: BoxedLong.t option ;
           bitValue: BoxedBoolean.t option ;
           blobValue: Blob.t option ;
           doubleValue: BoxedDouble.t option ;
           intValue: BoxedInteger.t option ;
           isNull: BoxedBoolean.t option ;
           realValue: BoxedFloat.t option ;
           stringValue: String_.t option ;
           structValue: StructValue.t option }
         let make ?arrayValues =
           fun ?bigIntValue ->
             fun ?bitValue ->
               fun ?blobValue ->
                 fun ?doubleValue ->
                   fun ?intValue ->
                     fun ?isNull ->
                       fun ?realValue ->
                         fun ?stringValue ->
                           fun ?structValue ->
                             fun () ->
                               {
                                 arrayValues;
                                 bigIntValue;
                                 bitValue;
                                 blobValue;
                                 doubleValue;
                                 intValue;
                                 isNull;
                                 realValue;
                                 stringValue;
                                 structValue
                               }
         let to_value x =
           structure_to_value
             [("arrayValues",
                (Option.map x.arrayValues ~f:ArrayValueList.to_value));
             ("bigIntValue",
               (Option.map x.bigIntValue ~f:BoxedLong.to_value));
             ("bitValue", (Option.map x.bitValue ~f:BoxedBoolean.to_value));
             ("blobValue", (Option.map x.blobValue ~f:Blob.to_value));
             ("doubleValue",
               (Option.map x.doubleValue ~f:BoxedDouble.to_value));
             ("intValue", (Option.map x.intValue ~f:BoxedInteger.to_value));
             ("isNull", (Option.map x.isNull ~f:BoxedBoolean.to_value));
             ("realValue", (Option.map x.realValue ~f:BoxedFloat.to_value));
             ("stringValue", (Option.map x.stringValue ~f:String_.to_value));
             ("structValue",
               (Option.map x.structValue ~f:StructValue.to_value))]
         let to_query v = to_query to_value v
         let of_xml xml_arg0 =
           let structValue =
             (Option.map ~f:StructValue.of_xml)
               (Xml.child xml_arg0 "structValue") in
           let stringValue =
             (Option.map ~f:String_.of_xml)
               (Xml.child xml_arg0 "stringValue") in
           let realValue =
             (Option.map ~f:BoxedFloat.of_xml)
               (Xml.child xml_arg0 "realValue") in
           let isNull =
             (Option.map ~f:BoxedBoolean.of_xml)
               (Xml.child xml_arg0 "isNull") in
           let intValue =
             (Option.map ~f:BoxedInteger.of_xml)
               (Xml.child xml_arg0 "intValue") in
           let doubleValue =
             (Option.map ~f:BoxedDouble.of_xml)
               (Xml.child xml_arg0 "doubleValue") in
           let blobValue =
             (Option.map ~f:Blob.of_xml) (Xml.child xml_arg0 "blobValue") in
           let bitValue =
             (Option.map ~f:BoxedBoolean.of_xml)
               (Xml.child xml_arg0 "bitValue") in
           let bigIntValue =
             (Option.map ~f:BoxedLong.of_xml)
               (Xml.child xml_arg0 "bigIntValue") in
           let arrayValues =
             (Option.map ~f:ArrayValueList.of_xml)
               (Xml.child xml_arg0 "arrayValues") in
           make ?structValue ?stringValue ?realValue ?isNull ?intValue
             ?doubleValue ?blobValue ?bitValue ?bigIntValue ?arrayValues ()
         let of_json json =
           let structValue = field_map json "structValue" StructValue.of_json in
           let stringValue = field_map json "stringValue" String_.of_json in
           let realValue = field_map json "realValue" BoxedFloat.of_json in
           let isNull = field_map json "isNull" BoxedBoolean.of_json in
           let intValue = field_map json "intValue" BoxedInteger.of_json in
           let doubleValue = field_map json "doubleValue" BoxedDouble.of_json in
           let blobValue = field_map json "blobValue" Blob.of_json in
           let bitValue = field_map json "bitValue" BoxedBoolean.of_json in
           let bigIntValue = field_map json "bigIntValue" BoxedLong.of_json in
           let arrayValues =
             field_map json "arrayValues" ArrayValueList.of_json in
           make ?structValue ?stringValue ?realValue ?isNull ?intValue
             ?doubleValue ?blobValue ?bitValue ?bigIntValue ?arrayValues ()
         let to_json v = composed_to_json to_value v
       end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module BadRequestException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module SqlStatement =
  struct
    type nonrec t = string
    let context_ = "SqlStatement"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:65536) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SqlStatement" j
    let to_json = simple_to_json to_value
  end
module TypeHint =
  struct
    type nonrec t =
      | JSON 
      | UUID 
      | TIMESTAMP 
      | DATE 
      | TIME 
      | DECIMAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | JSON -> "JSON"
      | UUID -> "UUID"
      | TIMESTAMP -> "TIMESTAMP"
      | DATE -> "DATE"
      | TIME -> "TIME"
      | DECIMAL -> "DECIMAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "JSON" -> JSON
      | "UUID" -> UUID
      | "TIMESTAMP" -> TIMESTAMP
      | "DATE" -> DATE
      | "TIME" -> TIME
      | "DECIMAL" -> DECIMAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TypeHint" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TypeHint" j)
    let to_json = simple_to_json to_value
  end
module ParameterName =
  struct
    type nonrec t = string
    let context_ = "ParameterName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ParameterName" j
    let to_json = simple_to_json to_value
  end
module Field =
  struct
    type nonrec t =
      {
      arrayValue: ArrayValue.t option ;
      blobValue: Blob.t option ;
      booleanValue: BoxedBoolean.t option ;
      doubleValue: BoxedDouble.t option ;
      isNull: BoxedBoolean.t option ;
      longValue: BoxedLong.t option ;
      stringValue: String_.t option }
    let make ?arrayValue =
      fun ?blobValue ->
        fun ?booleanValue ->
          fun ?doubleValue ->
            fun ?isNull ->
              fun ?longValue ->
                fun ?stringValue ->
                  fun () ->
                    {
                      arrayValue;
                      blobValue;
                      booleanValue;
                      doubleValue;
                      isNull;
                      longValue;
                      stringValue
                    }
    let to_value x =
      structure_to_value
        [("arrayValue", (Option.map x.arrayValue ~f:ArrayValue.to_value));
        ("blobValue", (Option.map x.blobValue ~f:Blob.to_value));
        ("booleanValue",
          (Option.map x.booleanValue ~f:BoxedBoolean.to_value));
        ("doubleValue", (Option.map x.doubleValue ~f:BoxedDouble.to_value));
        ("isNull", (Option.map x.isNull ~f:BoxedBoolean.to_value));
        ("longValue", (Option.map x.longValue ~f:BoxedLong.to_value));
        ("stringValue", (Option.map x.stringValue ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stringValue =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "stringValue") in
      let longValue =
        (Option.map ~f:BoxedLong.of_xml) (Xml.child xml_arg0 "longValue") in
      let isNull =
        (Option.map ~f:BoxedBoolean.of_xml) (Xml.child xml_arg0 "isNull") in
      let doubleValue =
        (Option.map ~f:BoxedDouble.of_xml) (Xml.child xml_arg0 "doubleValue") in
      let booleanValue =
        (Option.map ~f:BoxedBoolean.of_xml)
          (Xml.child xml_arg0 "booleanValue") in
      let blobValue =
        (Option.map ~f:Blob.of_xml) (Xml.child xml_arg0 "blobValue") in
      let arrayValue =
        (Option.map ~f:ArrayValue.of_xml) (Xml.child xml_arg0 "arrayValue") in
      make ?stringValue ?longValue ?isNull ?doubleValue ?booleanValue
        ?blobValue ?arrayValue ()
    let of_json json =
      let stringValue = field_map json "stringValue" String_.of_json in
      let longValue = field_map json "longValue" BoxedLong.of_json in
      let isNull = field_map json "isNull" BoxedBoolean.of_json in
      let doubleValue = field_map json "doubleValue" BoxedDouble.of_json in
      let booleanValue = field_map json "booleanValue" BoxedBoolean.of_json in
      let blobValue = field_map json "blobValue" Blob.of_json in
      let arrayValue = field_map json "arrayValue" ArrayValue.of_json in
      make ?stringValue ?longValue ?isNull ?doubleValue ?booleanValue
        ?blobValue ?arrayValue ()
    let to_json v = composed_to_json to_value v
  end
module SqlParameter =
  struct
    type nonrec t =
      {
      name: ParameterName.t option ;
      typeHint: TypeHint.t option ;
      value: Field.t option }
    let make ?name =
      fun ?typeHint -> fun ?value -> fun () -> { name; typeHint; value }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:ParameterName.to_value));
        ("typeHint", (Option.map x.typeHint ~f:TypeHint.to_value));
        ("value", (Option.map x.value ~f:Field.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:Field.of_xml) (Xml.child xml_arg0 "value") in
      let typeHint =
        (Option.map ~f:TypeHint.of_xml) (Xml.child xml_arg0 "typeHint") in
      let name =
        (Option.map ~f:ParameterName.of_xml) (Xml.child xml_arg0 "name") in
      make ?value ?typeHint ?name ()
    let of_json json =
      let value = field_map json "value" Field.of_json in
      let typeHint = field_map json "typeHint" TypeHint.of_json in
      let name = field_map json "name" ParameterName.of_json in
      make ?value ?typeHint ?name ()
    let to_json v = composed_to_json to_value v
  end
module SqlParametersList =
  struct
    type nonrec t = SqlParameter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SqlParameter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SqlParameter.of_xml)
    let of_json j =
      list_of_json ~kind:"SqlParametersList" ~of_json:SqlParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module SqlParameterSets =
  struct
    type nonrec t = SqlParametersList.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SqlParametersList.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SqlParametersList.of_xml)
    let of_json j =
      list_of_json ~kind:"SqlParameterSets"
        ~of_json:SqlParametersList.of_json j
    let to_json v = composed_to_json to_value v
  end
module Id =
  struct
    type nonrec t = string
    let context_ = "Id"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:192) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Id" j
    let to_json = simple_to_json to_value
  end
module DbName =
  struct
    type nonrec t = string
    let context_ = "DbName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DbName" j
    let to_json = simple_to_json to_value
  end
module BatchExecuteStatementRequest =
  struct
    type nonrec t =
      {
      database: DbName.t option ;
      parameterSets: SqlParameterSets.t option ;
      resourceArn: Arn.t ;
      schema: DbName.t option ;
      secretArn: Arn.t ;
      sql: SqlStatement.t ;
      transactionId: Id.t option }
    let context_ = "BatchExecuteStatementRequest"
    let make ?database =
      fun ?parameterSets ->
        fun ?schema ->
          fun ?transactionId ->
            fun ~resourceArn ->
              fun ~secretArn ->
                fun ~sql ->
                  fun () ->
                    {
                      database;
                      parameterSets;
                      schema;
                      transactionId;
                      resourceArn;
                      secretArn;
                      sql
                    }
    let to_value x =
      structure_to_value
        [("database", (Option.map x.database ~f:DbName.to_value));
        ("parameterSets",
          (Option.map x.parameterSets ~f:SqlParameterSets.to_value));
        ("resourceArn", (Some (Arn.to_value x.resourceArn)));
        ("schema", (Option.map x.schema ~f:DbName.to_value));
        ("secretArn", (Some (Arn.to_value x.secretArn)));
        ("sql", (Some (SqlStatement.to_value x.sql)));
        ("transactionId", (Option.map x.transactionId ~f:Id.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transactionId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "transactionId") in
      let sql =
        SqlStatement.of_xml (Xml.child_exn ~context:context_ xml_arg0 "sql") in
      let secretArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "secretArn") in
      let schema =
        (Option.map ~f:DbName.of_xml) (Xml.child xml_arg0 "schema") in
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      let parameterSets =
        (Option.map ~f:SqlParameterSets.of_xml)
          (Xml.child xml_arg0 "parameterSets") in
      let database =
        (Option.map ~f:DbName.of_xml) (Xml.child xml_arg0 "database") in
      make ?transactionId ~sql ~secretArn ?schema ~resourceArn ?parameterSets
        ?database ()
    let of_json json =
      let transactionId = field_map json "transactionId" Id.of_json in
      let sql = field_map_exn json "sql" SqlStatement.of_json in
      let secretArn = field_map_exn json "secretArn" Arn.of_json in
      let schema = field_map json "schema" DbName.of_json in
      let resourceArn = field_map_exn json "resourceArn" Arn.of_json in
      let parameterSets =
        field_map json "parameterSets" SqlParameterSets.of_json in
      let database = field_map json "database" DbName.of_json in
      make ?transactionId ~sql ~secretArn ?schema ~resourceArn ?parameterSets
        ?database ()
    let to_json v = composed_to_json to_value v
  end
module FieldList =
  struct
    type nonrec t = Field.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Field.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Field.of_xml)
    let of_json j = list_of_json ~kind:"FieldList" ~of_json:Field.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateResult =
  struct
    type nonrec t = {
      generatedFields: FieldList.t option }
    let make ?generatedFields = fun () -> { generatedFields }
    let to_value x =
      structure_to_value
        [("generatedFields",
           (Option.map x.generatedFields ~f:FieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let generatedFields =
        (Option.map ~f:FieldList.of_xml)
          (Xml.child xml_arg0 "generatedFields") in
      make ?generatedFields ()
    let of_json json =
      let generatedFields =
        field_map json "generatedFields" FieldList.of_json in
      make ?generatedFields ()
    let to_json v = composed_to_json to_value v
  end
module UpdateResults =
  struct
    type nonrec t = UpdateResult.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:UpdateResult.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UpdateResult.of_xml)
    let of_json j =
      list_of_json ~kind:"UpdateResults" ~of_json:UpdateResult.of_json j
    let to_json v = composed_to_json to_value v
  end
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module StatementTimeoutException =
  struct
    type nonrec t =
      {
      dbConnectionId: Long.t option ;
      message: ErrorMessage.t option }
    let make ?dbConnectionId =
      fun ?message -> fun () -> { dbConnectionId; message }
    let to_value x =
      structure_to_value
        [("dbConnectionId", (Option.map x.dbConnectionId ~f:Long.to_value));
        ("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      let dbConnectionId =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "dbConnectionId") in
      make ?message ?dbConnectionId ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      let dbConnectionId = field_map json "dbConnectionId" Long.of_json in
      make ?message ?dbConnectionId ()
    let to_json v = composed_to_json to_value v
  end
module ServiceUnavailableError =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module InternalServerErrorException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module ForbiddenException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module BatchExecuteStatementResponse =
  struct
    type nonrec t = {
      updateResults: UpdateResults.t option }
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `ServiceUnavailableError of ServiceUnavailableError.t 
      | `StatementTimeoutException of StatementTimeoutException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?updateResults = fun () -> { updateResults }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "ServiceUnavailableError" ->
          `ServiceUnavailableError (ServiceUnavailableError.of_json json)
      | "StatementTimeoutException" ->
          `StatementTimeoutException (StatementTimeoutException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "ServiceUnavailableError" ->
          `ServiceUnavailableError (ServiceUnavailableError.of_xml xml)
      | "StatementTimeoutException" ->
          `StatementTimeoutException (StatementTimeoutException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `ServiceUnavailableError e ->
          `Assoc
            [("error", (`String "ServiceUnavailableError"));
            ("details", (ServiceUnavailableError.to_json e))]
      | `StatementTimeoutException e ->
          `Assoc
            [("error", (`String "StatementTimeoutException"));
            ("details", (StatementTimeoutException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("updateResults",
           (Option.map x.updateResults ~f:UpdateResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updateResults =
        (Option.map ~f:UpdateResults.of_xml)
          (Xml.child xml_arg0 "updateResults") in
      make ?updateResults ()
    let of_json json =
      let updateResults =
        field_map json "updateResults" UpdateResults.of_json in
      make ?updateResults ()
    let to_json v = composed_to_json to_value v
  end
module BeginTransactionRequest =
  struct
    type nonrec t =
      {
      database: DbName.t option ;
      resourceArn: Arn.t ;
      schema: DbName.t option ;
      secretArn: Arn.t }
    let context_ = "BeginTransactionRequest"
    let make ?database =
      fun ?schema ->
        fun ~resourceArn ->
          fun ~secretArn ->
            fun () -> { database; schema; resourceArn; secretArn }
    let to_value x =
      structure_to_value
        [("database", (Option.map x.database ~f:DbName.to_value));
        ("resourceArn", (Some (Arn.to_value x.resourceArn)));
        ("schema", (Option.map x.schema ~f:DbName.to_value));
        ("secretArn", (Some (Arn.to_value x.secretArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let secretArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "secretArn") in
      let schema =
        (Option.map ~f:DbName.of_xml) (Xml.child xml_arg0 "schema") in
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      let database =
        (Option.map ~f:DbName.of_xml) (Xml.child xml_arg0 "database") in
      make ~secretArn ?schema ~resourceArn ?database ()
    let of_json json =
      let secretArn = field_map_exn json "secretArn" Arn.of_json in
      let schema = field_map json "schema" DbName.of_json in
      let resourceArn = field_map_exn json "resourceArn" Arn.of_json in
      let database = field_map json "database" DbName.of_json in
      make ~secretArn ?schema ~resourceArn ?database ()
    let to_json v = composed_to_json to_value v
  end
module BeginTransactionResponse =
  struct
    type nonrec t = {
      transactionId: Id.t option }
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `ServiceUnavailableError of ServiceUnavailableError.t 
      | `StatementTimeoutException of StatementTimeoutException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?transactionId = fun () -> { transactionId }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "ServiceUnavailableError" ->
          `ServiceUnavailableError (ServiceUnavailableError.of_json json)
      | "StatementTimeoutException" ->
          `StatementTimeoutException (StatementTimeoutException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "ServiceUnavailableError" ->
          `ServiceUnavailableError (ServiceUnavailableError.of_xml xml)
      | "StatementTimeoutException" ->
          `StatementTimeoutException (StatementTimeoutException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `ServiceUnavailableError e ->
          `Assoc
            [("error", (`String "ServiceUnavailableError"));
            ("details", (ServiceUnavailableError.to_json e))]
      | `StatementTimeoutException e ->
          `Assoc
            [("error", (`String "StatementTimeoutException"));
            ("details", (StatementTimeoutException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transactionId", (Option.map x.transactionId ~f:Id.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transactionId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "transactionId") in
      make ?transactionId ()
    let of_json json =
      let transactionId = field_map json "transactionId" Id.of_json in
      make ?transactionId ()
    let to_json v = composed_to_json to_value v
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ColumnMetadata =
  struct
    type nonrec t =
      {
      arrayBaseColumnType: Integer.t option ;
      isAutoIncrement: Boolean.t option ;
      isCaseSensitive: Boolean.t option ;
      isCurrency: Boolean.t option ;
      isSigned: Boolean.t option ;
      label: String_.t option ;
      name: String_.t option ;
      nullable: Integer.t option ;
      precision: Integer.t option ;
      scale: Integer.t option ;
      schemaName: String_.t option ;
      tableName: String_.t option ;
      type_: Integer.t option ;
      typeName: String_.t option }
    let make ?arrayBaseColumnType =
      fun ?isAutoIncrement ->
        fun ?isCaseSensitive ->
          fun ?isCurrency ->
            fun ?isSigned ->
              fun ?label ->
                fun ?name ->
                  fun ?nullable ->
                    fun ?precision ->
                      fun ?scale ->
                        fun ?schemaName ->
                          fun ?tableName ->
                            fun ?type_ ->
                              fun ?typeName ->
                                fun () ->
                                  {
                                    arrayBaseColumnType;
                                    isAutoIncrement;
                                    isCaseSensitive;
                                    isCurrency;
                                    isSigned;
                                    label;
                                    name;
                                    nullable;
                                    precision;
                                    scale;
                                    schemaName;
                                    tableName;
                                    type_;
                                    typeName
                                  }
    let to_value x =
      structure_to_value
        [("arrayBaseColumnType",
           (Option.map x.arrayBaseColumnType ~f:Integer.to_value));
        ("isAutoIncrement",
          (Option.map x.isAutoIncrement ~f:Boolean.to_value));
        ("isCaseSensitive",
          (Option.map x.isCaseSensitive ~f:Boolean.to_value));
        ("isCurrency", (Option.map x.isCurrency ~f:Boolean.to_value));
        ("isSigned", (Option.map x.isSigned ~f:Boolean.to_value));
        ("label", (Option.map x.label ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("nullable", (Option.map x.nullable ~f:Integer.to_value));
        ("precision", (Option.map x.precision ~f:Integer.to_value));
        ("scale", (Option.map x.scale ~f:Integer.to_value));
        ("schemaName", (Option.map x.schemaName ~f:String_.to_value));
        ("tableName", (Option.map x.tableName ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:Integer.to_value));
        ("typeName", (Option.map x.typeName ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let typeName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "typeName") in
      let type_ = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "type") in
      let tableName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "tableName") in
      let schemaName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "schemaName") in
      let scale = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "scale") in
      let precision =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "precision") in
      let nullable =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "nullable") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let label = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "label") in
      let isSigned =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isSigned") in
      let isCurrency =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isCurrency") in
      let isCaseSensitive =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isCaseSensitive") in
      let isAutoIncrement =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isAutoIncrement") in
      let arrayBaseColumnType =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "arrayBaseColumnType") in
      make ?typeName ?type_ ?tableName ?schemaName ?scale ?precision
        ?nullable ?name ?label ?isSigned ?isCurrency ?isCaseSensitive
        ?isAutoIncrement ?arrayBaseColumnType ()
    let of_json json =
      let typeName = field_map json "typeName" String_.of_json in
      let type_ = field_map json "type" Integer.of_json in
      let tableName = field_map json "tableName" String_.of_json in
      let schemaName = field_map json "schemaName" String_.of_json in
      let scale = field_map json "scale" Integer.of_json in
      let precision = field_map json "precision" Integer.of_json in
      let nullable = field_map json "nullable" Integer.of_json in
      let name = field_map json "name" String_.of_json in
      let label = field_map json "label" String_.of_json in
      let isSigned = field_map json "isSigned" Boolean.of_json in
      let isCurrency = field_map json "isCurrency" Boolean.of_json in
      let isCaseSensitive = field_map json "isCaseSensitive" Boolean.of_json in
      let isAutoIncrement = field_map json "isAutoIncrement" Boolean.of_json in
      let arrayBaseColumnType =
        field_map json "arrayBaseColumnType" Integer.of_json in
      make ?typeName ?type_ ?tableName ?schemaName ?scale ?precision
        ?nullable ?name ?label ?isSigned ?isCurrency ?isCaseSensitive
        ?isAutoIncrement ?arrayBaseColumnType ()
    let to_json v = composed_to_json to_value v
  end
module CommitTransactionRequest =
  struct
    type nonrec t =
      {
      resourceArn: Arn.t ;
      secretArn: Arn.t ;
      transactionId: Id.t }
    let context_ = "CommitTransactionRequest"
    let make ~resourceArn =
      fun ~secretArn ->
        fun ~transactionId ->
          fun () -> { resourceArn; secretArn; transactionId }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)));
        ("secretArn", (Some (Arn.to_value x.secretArn)));
        ("transactionId", (Some (Id.to_value x.transactionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transactionId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "transactionId") in
      let secretArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "secretArn") in
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~transactionId ~secretArn ~resourceArn ()
    let of_json json =
      let transactionId = field_map_exn json "transactionId" Id.of_json in
      let secretArn = field_map_exn json "secretArn" Arn.of_json in
      let resourceArn = field_map_exn json "resourceArn" Arn.of_json in
      make ~transactionId ~secretArn ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end
module TransactionStatus =
  struct
    type nonrec t = string
    let context_ = "TransactionStatus"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TransactionStatus" j
    let to_json = simple_to_json to_value
  end
module NotFoundException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module CommitTransactionResponse =
  struct
    type nonrec t = {
      transactionStatus: TransactionStatus.t option }
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceUnavailableError of ServiceUnavailableError.t 
      | `StatementTimeoutException of StatementTimeoutException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?transactionStatus = fun () -> { transactionStatus }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceUnavailableError" ->
          `ServiceUnavailableError (ServiceUnavailableError.of_json json)
      | "StatementTimeoutException" ->
          `StatementTimeoutException (StatementTimeoutException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceUnavailableError" ->
          `ServiceUnavailableError (ServiceUnavailableError.of_xml xml)
      | "StatementTimeoutException" ->
          `StatementTimeoutException (StatementTimeoutException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceUnavailableError e ->
          `Assoc
            [("error", (`String "ServiceUnavailableError"));
            ("details", (ServiceUnavailableError.to_json e))]
      | `StatementTimeoutException e ->
          `Assoc
            [("error", (`String "StatementTimeoutException"));
            ("details", (StatementTimeoutException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transactionStatus",
           (Option.map x.transactionStatus ~f:TransactionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transactionStatus =
        (Option.map ~f:TransactionStatus.of_xml)
          (Xml.child xml_arg0 "transactionStatus") in
      make ?transactionStatus ()
    let of_json json =
      let transactionStatus =
        field_map json "transactionStatus" TransactionStatus.of_json in
      make ?transactionStatus ()
    let to_json v = composed_to_json to_value v
  end
module DecimalReturnType =
  struct
    type nonrec t =
      | STRING 
      | DOUBLE_OR_LONG 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | STRING -> "STRING"
      | DOUBLE_OR_LONG -> "DOUBLE_OR_LONG"
      | Non_static_id s -> s
    let of_string =
      function
      | "STRING" -> STRING
      | "DOUBLE_OR_LONG" -> DOUBLE_OR_LONG
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DecimalReturnType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DecimalReturnType" j)
    let to_json = simple_to_json to_value
  end
module ExecuteSqlRequest =
  struct
    type nonrec t =
      {
      awsSecretStoreArn: Arn.t ;
      database: DbName.t option ;
      dbClusterOrInstanceArn: Arn.t ;
      schema: DbName.t option ;
      sqlStatements: SqlStatement.t }
    let context_ = "ExecuteSqlRequest"
    let make ?database =
      fun ?schema ->
        fun ~awsSecretStoreArn ->
          fun ~dbClusterOrInstanceArn ->
            fun ~sqlStatements ->
              fun () ->
                {
                  database;
                  schema;
                  awsSecretStoreArn;
                  dbClusterOrInstanceArn;
                  sqlStatements
                }
    let to_value x =
      structure_to_value
        [("awsSecretStoreArn", (Some (Arn.to_value x.awsSecretStoreArn)));
        ("database", (Option.map x.database ~f:DbName.to_value));
        ("dbClusterOrInstanceArn",
          (Some (Arn.to_value x.dbClusterOrInstanceArn)));
        ("schema", (Option.map x.schema ~f:DbName.to_value));
        ("sqlStatements", (Some (SqlStatement.to_value x.sqlStatements)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sqlStatements =
        SqlStatement.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sqlStatements") in
      let schema =
        (Option.map ~f:DbName.of_xml) (Xml.child xml_arg0 "schema") in
      let dbClusterOrInstanceArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "dbClusterOrInstanceArn") in
      let database =
        (Option.map ~f:DbName.of_xml) (Xml.child xml_arg0 "database") in
      let awsSecretStoreArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "awsSecretStoreArn") in
      make ~sqlStatements ?schema ~dbClusterOrInstanceArn ?database
        ~awsSecretStoreArn ()
    let of_json json =
      let sqlStatements =
        field_map_exn json "sqlStatements" SqlStatement.of_json in
      let schema = field_map json "schema" DbName.of_json in
      let dbClusterOrInstanceArn =
        field_map_exn json "dbClusterOrInstanceArn" Arn.of_json in
      let database = field_map json "database" DbName.of_json in
      let awsSecretStoreArn =
        field_map_exn json "awsSecretStoreArn" Arn.of_json in
      make ~sqlStatements ?schema ~dbClusterOrInstanceArn ?database
        ~awsSecretStoreArn ()
    let to_json v = composed_to_json to_value v
  end
module Metadata =
  struct
    type nonrec t = ColumnMetadata.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ColumnMetadata.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ColumnMetadata.of_xml)
    let of_json j =
      list_of_json ~kind:"Metadata" ~of_json:ColumnMetadata.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResultSetMetadata =
  struct
    type nonrec t =
      {
      columnCount: Long.t option ;
      columnMetadata: Metadata.t option }
    let make ?columnCount =
      fun ?columnMetadata -> fun () -> { columnCount; columnMetadata }
    let to_value x =
      structure_to_value
        [("columnCount", (Option.map x.columnCount ~f:Long.to_value));
        ("columnMetadata",
          (Option.map x.columnMetadata ~f:Metadata.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let columnMetadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "columnMetadata") in
      let columnCount =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "columnCount") in
      make ?columnMetadata ?columnCount ()
    let of_json json =
      let columnMetadata = field_map json "columnMetadata" Metadata.of_json in
      let columnCount = field_map json "columnCount" Long.of_json in
      make ?columnMetadata ?columnCount ()
    let to_json v = composed_to_json to_value v
  end
module Row =
  struct
    type nonrec t = Value.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Value.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Value.of_xml)
    let of_json j = list_of_json ~kind:"Row" ~of_json:Value.of_json j
    let to_json v = composed_to_json to_value v
  end
module Record =
  struct
    type nonrec t = {
      values: Row.t option }
    let make ?values = fun () -> { values }
    let to_value x =
      structure_to_value [("values", (Option.map x.values ~f:Row.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values = (Option.map ~f:Row.of_xml) (Xml.child xml_arg0 "values") in
      make ?values ()
    let of_json json =
      let values = field_map json "values" Row.of_json in make ?values ()
    let to_json v = composed_to_json to_value v
  end
module Records =
  struct
    type nonrec t = Record.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Record.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Record.of_xml)
    let of_json j = list_of_json ~kind:"Records" ~of_json:Record.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResultFrame =
  struct
    type nonrec t =
      {
      records: Records.t option ;
      resultSetMetadata: ResultSetMetadata.t option }
    let make ?records =
      fun ?resultSetMetadata -> fun () -> { records; resultSetMetadata }
    let to_value x =
      structure_to_value
        [("records", (Option.map x.records ~f:Records.to_value));
        ("resultSetMetadata",
          (Option.map x.resultSetMetadata ~f:ResultSetMetadata.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resultSetMetadata =
        (Option.map ~f:ResultSetMetadata.of_xml)
          (Xml.child xml_arg0 "resultSetMetadata") in
      let records =
        (Option.map ~f:Records.of_xml) (Xml.child xml_arg0 "records") in
      make ?resultSetMetadata ?records ()
    let of_json json =
      let resultSetMetadata =
        field_map json "resultSetMetadata" ResultSetMetadata.of_json in
      let records = field_map json "records" Records.of_json in
      make ?resultSetMetadata ?records ()
    let to_json v = composed_to_json to_value v
  end
module RecordsUpdated =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module SqlStatementResult =
  struct
    type nonrec t =
      {
      numberOfRecordsUpdated: RecordsUpdated.t option ;
      resultFrame: ResultFrame.t option }
    let make ?numberOfRecordsUpdated =
      fun ?resultFrame -> fun () -> { numberOfRecordsUpdated; resultFrame }
    let to_value x =
      structure_to_value
        [("numberOfRecordsUpdated",
           (Option.map x.numberOfRecordsUpdated ~f:RecordsUpdated.to_value));
        ("resultFrame", (Option.map x.resultFrame ~f:ResultFrame.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resultFrame =
        (Option.map ~f:ResultFrame.of_xml) (Xml.child xml_arg0 "resultFrame") in
      let numberOfRecordsUpdated =
        (Option.map ~f:RecordsUpdated.of_xml)
          (Xml.child xml_arg0 "numberOfRecordsUpdated") in
      make ?resultFrame ?numberOfRecordsUpdated ()
    let of_json json =
      let resultFrame = field_map json "resultFrame" ResultFrame.of_json in
      let numberOfRecordsUpdated =
        field_map json "numberOfRecordsUpdated" RecordsUpdated.of_json in
      make ?resultFrame ?numberOfRecordsUpdated ()
    let to_json v = composed_to_json to_value v
  end
module SqlStatementResults =
  struct
    type nonrec t = SqlStatementResult.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SqlStatementResult.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SqlStatementResult.of_xml)
    let of_json j =
      list_of_json ~kind:"SqlStatementResults"
        ~of_json:SqlStatementResult.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExecuteSqlResponse =
  struct
    type nonrec t = {
      sqlStatementResults: SqlStatementResults.t option }
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `ServiceUnavailableError of ServiceUnavailableError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?sqlStatementResults = fun () -> { sqlStatementResults }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "ServiceUnavailableError" ->
          `ServiceUnavailableError (ServiceUnavailableError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "ServiceUnavailableError" ->
          `ServiceUnavailableError (ServiceUnavailableError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `ServiceUnavailableError e ->
          `Assoc
            [("error", (`String "ServiceUnavailableError"));
            ("details", (ServiceUnavailableError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("sqlStatementResults",
           (Option.map x.sqlStatementResults ~f:SqlStatementResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sqlStatementResults =
        (Option.map ~f:SqlStatementResults.of_xml)
          (Xml.child xml_arg0 "sqlStatementResults") in
      make ?sqlStatementResults ()
    let of_json json =
      let sqlStatementResults =
        field_map json "sqlStatementResults" SqlStatementResults.of_json in
      make ?sqlStatementResults ()
    let to_json v = composed_to_json to_value v
  end
module ResultSetOptions =
  struct
    type nonrec t = {
      decimalReturnType: DecimalReturnType.t option }
    let make ?decimalReturnType = fun () -> { decimalReturnType }
    let to_value x =
      structure_to_value
        [("decimalReturnType",
           (Option.map x.decimalReturnType ~f:DecimalReturnType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let decimalReturnType =
        (Option.map ~f:DecimalReturnType.of_xml)
          (Xml.child xml_arg0 "decimalReturnType") in
      make ?decimalReturnType ()
    let of_json json =
      let decimalReturnType =
        field_map json "decimalReturnType" DecimalReturnType.of_json in
      make ?decimalReturnType ()
    let to_json v = composed_to_json to_value v
  end
module ExecuteStatementRequest =
  struct
    type nonrec t =
      {
      continueAfterTimeout: Boolean.t option ;
      database: DbName.t option ;
      includeResultMetadata: Boolean.t option ;
      parameters: SqlParametersList.t option ;
      resourceArn: Arn.t ;
      resultSetOptions: ResultSetOptions.t option ;
      schema: DbName.t option ;
      secretArn: Arn.t ;
      sql: SqlStatement.t ;
      transactionId: Id.t option }
    let context_ = "ExecuteStatementRequest"
    let make ?continueAfterTimeout =
      fun ?database ->
        fun ?includeResultMetadata ->
          fun ?parameters ->
            fun ?resultSetOptions ->
              fun ?schema ->
                fun ?transactionId ->
                  fun ~resourceArn ->
                    fun ~secretArn ->
                      fun ~sql ->
                        fun () ->
                          {
                            continueAfterTimeout;
                            database;
                            includeResultMetadata;
                            parameters;
                            resultSetOptions;
                            schema;
                            transactionId;
                            resourceArn;
                            secretArn;
                            sql
                          }
    let to_value x =
      structure_to_value
        [("continueAfterTimeout",
           (Option.map x.continueAfterTimeout ~f:Boolean.to_value));
        ("database", (Option.map x.database ~f:DbName.to_value));
        ("includeResultMetadata",
          (Option.map x.includeResultMetadata ~f:Boolean.to_value));
        ("parameters",
          (Option.map x.parameters ~f:SqlParametersList.to_value));
        ("resourceArn", (Some (Arn.to_value x.resourceArn)));
        ("resultSetOptions",
          (Option.map x.resultSetOptions ~f:ResultSetOptions.to_value));
        ("schema", (Option.map x.schema ~f:DbName.to_value));
        ("secretArn", (Some (Arn.to_value x.secretArn)));
        ("sql", (Some (SqlStatement.to_value x.sql)));
        ("transactionId", (Option.map x.transactionId ~f:Id.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transactionId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "transactionId") in
      let sql =
        SqlStatement.of_xml (Xml.child_exn ~context:context_ xml_arg0 "sql") in
      let secretArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "secretArn") in
      let schema =
        (Option.map ~f:DbName.of_xml) (Xml.child xml_arg0 "schema") in
      let resultSetOptions =
        (Option.map ~f:ResultSetOptions.of_xml)
          (Xml.child xml_arg0 "resultSetOptions") in
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      let parameters =
        (Option.map ~f:SqlParametersList.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let includeResultMetadata =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "includeResultMetadata") in
      let database =
        (Option.map ~f:DbName.of_xml) (Xml.child xml_arg0 "database") in
      let continueAfterTimeout =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "continueAfterTimeout") in
      make ?transactionId ~sql ~secretArn ?schema ?resultSetOptions
        ~resourceArn ?parameters ?includeResultMetadata ?database
        ?continueAfterTimeout ()
    let of_json json =
      let transactionId = field_map json "transactionId" Id.of_json in
      let sql = field_map_exn json "sql" SqlStatement.of_json in
      let secretArn = field_map_exn json "secretArn" Arn.of_json in
      let schema = field_map json "schema" DbName.of_json in
      let resultSetOptions =
        field_map json "resultSetOptions" ResultSetOptions.of_json in
      let resourceArn = field_map_exn json "resourceArn" Arn.of_json in
      let parameters = field_map json "parameters" SqlParametersList.of_json in
      let includeResultMetadata =
        field_map json "includeResultMetadata" Boolean.of_json in
      let database = field_map json "database" DbName.of_json in
      let continueAfterTimeout =
        field_map json "continueAfterTimeout" Boolean.of_json in
      make ?transactionId ~sql ~secretArn ?schema ?resultSetOptions
        ~resourceArn ?parameters ?includeResultMetadata ?database
        ?continueAfterTimeout ()
    let to_json v = composed_to_json to_value v
  end
module SqlRecords =
  struct
    type nonrec t = FieldList.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:FieldList.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FieldList.of_xml)
    let of_json j =
      list_of_json ~kind:"SqlRecords" ~of_json:FieldList.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExecuteStatementResponse =
  struct
    type nonrec t =
      {
      columnMetadata: Metadata.t option ;
      generatedFields: FieldList.t option ;
      numberOfRecordsUpdated: RecordsUpdated.t option ;
      records: SqlRecords.t option }
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `ServiceUnavailableError of ServiceUnavailableError.t 
      | `StatementTimeoutException of StatementTimeoutException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?columnMetadata =
      fun ?generatedFields ->
        fun ?numberOfRecordsUpdated ->
          fun ?records ->
            fun () ->
              {
                columnMetadata;
                generatedFields;
                numberOfRecordsUpdated;
                records
              }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "ServiceUnavailableError" ->
          `ServiceUnavailableError (ServiceUnavailableError.of_json json)
      | "StatementTimeoutException" ->
          `StatementTimeoutException (StatementTimeoutException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "ServiceUnavailableError" ->
          `ServiceUnavailableError (ServiceUnavailableError.of_xml xml)
      | "StatementTimeoutException" ->
          `StatementTimeoutException (StatementTimeoutException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `ServiceUnavailableError e ->
          `Assoc
            [("error", (`String "ServiceUnavailableError"));
            ("details", (ServiceUnavailableError.to_json e))]
      | `StatementTimeoutException e ->
          `Assoc
            [("error", (`String "StatementTimeoutException"));
            ("details", (StatementTimeoutException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("columnMetadata",
           (Option.map x.columnMetadata ~f:Metadata.to_value));
        ("generatedFields",
          (Option.map x.generatedFields ~f:FieldList.to_value));
        ("numberOfRecordsUpdated",
          (Option.map x.numberOfRecordsUpdated ~f:RecordsUpdated.to_value));
        ("records", (Option.map x.records ~f:SqlRecords.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let records =
        (Option.map ~f:SqlRecords.of_xml) (Xml.child xml_arg0 "records") in
      let numberOfRecordsUpdated =
        (Option.map ~f:RecordsUpdated.of_xml)
          (Xml.child xml_arg0 "numberOfRecordsUpdated") in
      let generatedFields =
        (Option.map ~f:FieldList.of_xml)
          (Xml.child xml_arg0 "generatedFields") in
      let columnMetadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "columnMetadata") in
      make ?records ?numberOfRecordsUpdated ?generatedFields ?columnMetadata
        ()
    let of_json json =
      let records = field_map json "records" SqlRecords.of_json in
      let numberOfRecordsUpdated =
        field_map json "numberOfRecordsUpdated" RecordsUpdated.of_json in
      let generatedFields =
        field_map json "generatedFields" FieldList.of_json in
      let columnMetadata = field_map json "columnMetadata" Metadata.of_json in
      make ?records ?numberOfRecordsUpdated ?generatedFields ?columnMetadata
        ()
    let to_json v = composed_to_json to_value v
  end
module RollbackTransactionRequest =
  struct
    type nonrec t =
      {
      resourceArn: Arn.t ;
      secretArn: Arn.t ;
      transactionId: Id.t }
    let context_ = "RollbackTransactionRequest"
    let make ~resourceArn =
      fun ~secretArn ->
        fun ~transactionId ->
          fun () -> { resourceArn; secretArn; transactionId }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)));
        ("secretArn", (Some (Arn.to_value x.secretArn)));
        ("transactionId", (Some (Id.to_value x.transactionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transactionId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "transactionId") in
      let secretArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "secretArn") in
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~transactionId ~secretArn ~resourceArn ()
    let of_json json =
      let transactionId = field_map_exn json "transactionId" Id.of_json in
      let secretArn = field_map_exn json "secretArn" Arn.of_json in
      let resourceArn = field_map_exn json "resourceArn" Arn.of_json in
      make ~transactionId ~secretArn ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end
module RollbackTransactionResponse =
  struct
    type nonrec t = {
      transactionStatus: TransactionStatus.t option }
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceUnavailableError of ServiceUnavailableError.t 
      | `StatementTimeoutException of StatementTimeoutException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?transactionStatus = fun () -> { transactionStatus }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceUnavailableError" ->
          `ServiceUnavailableError (ServiceUnavailableError.of_json json)
      | "StatementTimeoutException" ->
          `StatementTimeoutException (StatementTimeoutException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceUnavailableError" ->
          `ServiceUnavailableError (ServiceUnavailableError.of_xml xml)
      | "StatementTimeoutException" ->
          `StatementTimeoutException (StatementTimeoutException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceUnavailableError e ->
          `Assoc
            [("error", (`String "ServiceUnavailableError"));
            ("details", (ServiceUnavailableError.to_json e))]
      | `StatementTimeoutException e ->
          `Assoc
            [("error", (`String "StatementTimeoutException"));
            ("details", (StatementTimeoutException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transactionStatus",
           (Option.map x.transactionStatus ~f:TransactionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transactionStatus =
        (Option.map ~f:TransactionStatus.of_xml)
          (Xml.child xml_arg0 "transactionStatus") in
      make ?transactionStatus ()
    let of_json json =
      let transactionStatus =
        field_map json "transactionStatus" TransactionStatus.of_json in
      make ?transactionStatus ()
    let to_json v = composed_to_json to_value v
  end
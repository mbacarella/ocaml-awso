(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | BatchExecuteStatement: (BatchExecuteStatementRequest.t,
  BatchExecuteStatementResponse.t, BatchExecuteStatementResponse.error) t 
  | BeginTransaction: (BeginTransactionRequest.t, BeginTransactionResponse.t,
  BeginTransactionResponse.error) t 
  | CommitTransaction: (CommitTransactionRequest.t,
  CommitTransactionResponse.t, CommitTransactionResponse.error) t 
  | ExecuteSql: (ExecuteSqlRequest.t, ExecuteSqlResponse.t,
  ExecuteSqlResponse.error) t 
  | ExecuteStatement: (ExecuteStatementRequest.t, ExecuteStatementResponse.t,
  ExecuteStatementResponse.error) t 
  | RollbackTransaction: (RollbackTransactionRequest.t,
  RollbackTransactionResponse.t, RollbackTransactionResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | BatchExecuteStatement -> `POST
  | BeginTransaction -> `POST
  | CommitTransaction -> `POST
  | ExecuteSql -> `POST
  | ExecuteStatement -> `POST
  | RollbackTransaction -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | BatchExecuteStatement ->
          (Format.kasprintf Uri.of_string) "/BatchExecute"
      | BeginTransaction ->
          (Format.kasprintf Uri.of_string) "/BeginTransaction"
      | CommitTransaction ->
          (Format.kasprintf Uri.of_string) "/CommitTransaction"
      | ExecuteSql -> (Format.kasprintf Uri.of_string) "/ExecuteSql"
      | ExecuteStatement -> (Format.kasprintf Uri.of_string) "/Execute"
      | RollbackTransaction ->
          (Format.kasprintf Uri.of_string) "/RollbackTransaction")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | BatchExecuteStatement ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.BatchExecuteStatementRequest.database
                         ~f:(fun x -> ("database", (DbName.to_value x)));
                      Option.map
                        req.BatchExecuteStatementRequest.parameterSets
                        ~f:(fun x ->
                              ("parameterSets",
                                (SqlParameterSets.to_value x)));
                      Some
                        ("resourceArn",
                          (Arn.to_value
                             req.BatchExecuteStatementRequest.resourceArn));
                      Option.map req.BatchExecuteStatementRequest.schema
                        ~f:(fun x -> ("schema", (DbName.to_value x)));
                      Some
                        ("secretArn",
                          (Arn.to_value
                             req.BatchExecuteStatementRequest.secretArn));
                      Some
                        ("sql",
                          (SqlStatement.to_value
                             req.BatchExecuteStatementRequest.sql));
                      Option.map
                        req.BatchExecuteStatementRequest.transactionId
                        ~f:(fun x -> ("transactionId", (Id.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BeginTransaction ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.BeginTransactionRequest.database
                         ~f:(fun x -> ("database", (DbName.to_value x)));
                      Some
                        ("resourceArn",
                          (Arn.to_value
                             req.BeginTransactionRequest.resourceArn));
                      Option.map req.BeginTransactionRequest.schema
                        ~f:(fun x -> ("schema", (DbName.to_value x)));
                      Some
                        ("secretArn",
                          (Arn.to_value req.BeginTransactionRequest.secretArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CommitTransaction ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("resourceArn",
                           (Arn.to_value
                              req.CommitTransactionRequest.resourceArn));
                      Some
                        ("secretArn",
                          (Arn.to_value
                             req.CommitTransactionRequest.secretArn));
                      Some
                        ("transactionId",
                          (Id.to_value
                             req.CommitTransactionRequest.transactionId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ExecuteSql ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("awsSecretStoreArn",
                           (Arn.to_value
                              req.ExecuteSqlRequest.awsSecretStoreArn));
                      Option.map req.ExecuteSqlRequest.database
                        ~f:(fun x -> ("database", (DbName.to_value x)));
                      Some
                        ("dbClusterOrInstanceArn",
                          (Arn.to_value
                             req.ExecuteSqlRequest.dbClusterOrInstanceArn));
                      Option.map req.ExecuteSqlRequest.schema
                        ~f:(fun x -> ("schema", (DbName.to_value x)));
                      Some
                        ("sqlStatements",
                          (SqlStatement.to_value
                             req.ExecuteSqlRequest.sqlStatements))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ExecuteStatement ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ExecuteStatementRequest.continueAfterTimeout
                         ~f:(fun x ->
                               ("continueAfterTimeout", (Boolean.to_value x)));
                      Option.map req.ExecuteStatementRequest.database
                        ~f:(fun x -> ("database", (DbName.to_value x)));
                      Option.map
                        req.ExecuteStatementRequest.includeResultMetadata
                        ~f:(fun x ->
                              ("includeResultMetadata", (Boolean.to_value x)));
                      Option.map req.ExecuteStatementRequest.parameters
                        ~f:(fun x ->
                              ("parameters", (SqlParametersList.to_value x)));
                      Some
                        ("resourceArn",
                          (Arn.to_value
                             req.ExecuteStatementRequest.resourceArn));
                      Option.map req.ExecuteStatementRequest.resultSetOptions
                        ~f:(fun x ->
                              ("resultSetOptions",
                                (ResultSetOptions.to_value x)));
                      Option.map req.ExecuteStatementRequest.schema
                        ~f:(fun x -> ("schema", (DbName.to_value x)));
                      Some
                        ("secretArn",
                          (Arn.to_value req.ExecuteStatementRequest.secretArn));
                      Some
                        ("sql",
                          (SqlStatement.to_value
                             req.ExecuteStatementRequest.sql));
                      Option.map req.ExecuteStatementRequest.transactionId
                        ~f:(fun x -> ("transactionId", (Id.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | RollbackTransaction ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("resourceArn",
                           (Arn.to_value
                              req.RollbackTransactionRequest.resourceArn));
                      Some
                        ("secretArn",
                          (Arn.to_value
                             req.RollbackTransactionRequest.secretArn));
                      Some
                        ("transactionId",
                          (Id.to_value
                             req.RollbackTransactionRequest.transactionId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | BatchExecuteStatement ->
      if is_success
      then Ok (BatchExecuteStatementResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some BatchExecuteStatementResponse.error_of_json))
  | BeginTransaction ->
      if is_success
      then Ok (BeginTransactionResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some BeginTransactionResponse.error_of_json))
  | CommitTransaction ->
      if is_success
      then Ok (CommitTransactionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some CommitTransactionResponse.error_of_json))
  | ExecuteSql ->
      if is_success
      then Ok (ExecuteSqlResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ExecuteSqlResponse.error_of_json))
  | ExecuteStatement ->
      if is_success
      then Ok (ExecuteStatementResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ExecuteStatementResponse.error_of_json))
  | RollbackTransaction ->
      if is_success
      then Ok (RollbackTransactionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some RollbackTransactionResponse.error_of_json))
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_execute_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and database = flag "database" (optional string) ~doc:"STRING DbName"
       and parameterSets =
         flag "parameter-sets" (optional json_arg)
           ~doc:"JSON SqlParameterSets"
       and schema = flag "schema" (optional string) ~doc:"STRING DbName"
       and transactionId =
         flag "transaction-id" (optional string) ~doc:"STRING Id"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and secretArn = flag "secret-arn" (required string) ~doc:"STRING Arn"
       and sql = flag "sql" (required string) ~doc:"STRING SqlStatement" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_execute_statement
           (Values.BatchExecuteStatementRequest.make ?database
              ?parameterSets:(Option.map ~f:Values.SqlParameterSets.of_json
                                parameterSets) ?schema ?transactionId
              ~resourceArn ~secretArn ~sql ())
           (Some Values.BatchExecuteStatementResponse.to_json)
           (Some Values.BatchExecuteStatementResponse.error_to_json)])
let begin_transaction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and database = flag "database" (optional string) ~doc:"STRING DbName"
       and schema = flag "schema" (optional string) ~doc:"STRING DbName"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and secretArn = flag "secret-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.begin_transaction
           (Values.BeginTransactionRequest.make ?database ?schema
              ~resourceArn ~secretArn ())
           (Some Values.BeginTransactionResponse.to_json)
           (Some Values.BeginTransactionResponse.error_to_json)])
let commit_transaction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and secretArn = flag "secret-arn" (required string) ~doc:"STRING Arn"
       and transactionId =
         flag "transaction-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.commit_transaction
           (Values.CommitTransactionRequest.make ~resourceArn ~secretArn
              ~transactionId ())
           (Some Values.CommitTransactionResponse.to_json)
           (Some Values.CommitTransactionResponse.error_to_json)])
let execute_sql =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and database = flag "database" (optional string) ~doc:"STRING DbName"
       and schema = flag "schema" (optional string) ~doc:"STRING DbName"
       and awsSecretStoreArn =
         flag "aws-secret-store-arn" (required string) ~doc:"STRING Arn"
       and dbClusterOrInstanceArn =
         flag "db-cluster-or-instance-arn" (required string)
           ~doc:"STRING Arn"
       and sqlStatements =
         flag "sql-statements" (required string) ~doc:"STRING SqlStatement" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_sql
           (Values.ExecuteSqlRequest.make ?database ?schema
              ~awsSecretStoreArn ~dbClusterOrInstanceArn ~sqlStatements ())
           (Some Values.ExecuteSqlResponse.to_json)
           (Some Values.ExecuteSqlResponse.error_to_json)])
let execute_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and continueAfterTimeout =
         flag "continue-after-timeout" (optional bool) ~doc:"BOOL Boolean"
       and database = flag "database" (optional string) ~doc:"STRING DbName"
       and includeResultMetadata =
         flag "include-result-metadata" (optional bool) ~doc:"BOOL Boolean"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON SqlParametersList"
       and resultSetOptions =
         flag "result-set-options" (optional json_arg)
           ~doc:"JSON ResultSetOptions"
       and schema = flag "schema" (optional string) ~doc:"STRING DbName"
       and transactionId =
         flag "transaction-id" (optional string) ~doc:"STRING Id"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and secretArn = flag "secret-arn" (required string) ~doc:"STRING Arn"
       and sql = flag "sql" (required string) ~doc:"STRING SqlStatement" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_statement
           (Values.ExecuteStatementRequest.make ?continueAfterTimeout
              ?database ?includeResultMetadata
              ?parameters:(Option.map ~f:Values.SqlParametersList.of_json
                             parameters)
              ?resultSetOptions:(Option.map
                                   ~f:Values.ResultSetOptions.of_json
                                   resultSetOptions) ?schema ?transactionId
              ~resourceArn ~secretArn ~sql ())
           (Some Values.ExecuteStatementResponse.to_json)
           (Some Values.ExecuteStatementResponse.error_to_json)])
let rollback_transaction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and secretArn = flag "secret-arn" (required string) ~doc:"STRING Arn"
       and transactionId =
         flag "transaction-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.rollback_transaction
           (Values.RollbackTransactionRequest.make ~resourceArn ~secretArn
              ~transactionId ())
           (Some Values.RollbackTransactionResponse.to_json)
           (Some Values.RollbackTransactionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("batch-execute-statement", batch_execute_statement);
    ("begin-transaction", begin_transaction);
    ("commit-transaction", commit_transaction);
    ("execute-sql", execute_sql);
    ("execute-statement", execute_statement);
    ("rollback-transaction", rollback_transaction)]
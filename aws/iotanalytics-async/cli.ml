(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_put_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ChannelName"
       and messages =
         flag "messages" (required json_arg) ~doc:"JSON Messages" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_put_message
           (Values.BatchPutMessageRequest.make ~channelName
              ~messages:(Values.Messages.of_json messages) ())
           (Some Values.BatchPutMessageResponse.to_json)
           (Some Values.BatchPutMessageResponse.error_to_json)])
let cancel_pipeline_reprocessing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName"
       and reprocessingId =
         flag "reprocessing-id" (required string)
           ~doc:"STRING ReprocessingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_pipeline_reprocessing
           (Values.CancelPipelineReprocessingRequest.make ~pipelineName
              ~reprocessingId ())
           (Some Values.CancelPipelineReprocessingResponse.to_json)
           (Some Values.CancelPipelineReprocessingResponse.error_to_json)])
let create_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelStorage =
         flag "channel-storage" (optional json_arg)
           ~doc:"JSON ChannelStorage"
       and retentionPeriod =
         flag "retention-period" (optional json_arg)
           ~doc:"JSON RetentionPeriod"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ChannelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel
           (Values.CreateChannelRequest.make
              ?channelStorage:(Option.map ~f:Values.ChannelStorage.of_json
                                 channelStorage)
              ?retentionPeriod:(Option.map ~f:Values.RetentionPeriod.of_json
                                  retentionPeriod)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~channelName
              ()) (Some Values.CreateChannelResponse.to_json)
           (Some Values.CreateChannelResponse.error_to_json)])
let create_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and triggers =
         flag "triggers" (optional json_arg) ~doc:"JSON DatasetTriggers"
       and contentDeliveryRules =
         flag "content-delivery-rules" (optional json_arg)
           ~doc:"JSON DatasetContentDeliveryRules"
       and retentionPeriod =
         flag "retention-period" (optional json_arg)
           ~doc:"JSON RetentionPeriod"
       and versioningConfiguration =
         flag "versioning-configuration" (optional json_arg)
           ~doc:"JSON VersioningConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and lateDataRules =
         flag "late-data-rules" (optional json_arg) ~doc:"JSON LateDataRules"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName"
       and actions =
         flag "actions" (required json_arg) ~doc:"JSON DatasetActions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dataset
           (Values.CreateDatasetRequest.make
              ?triggers:(Option.map ~f:Values.DatasetTriggers.of_json
                           triggers)
              ?contentDeliveryRules:(Option.map
                                       ~f:Values.DatasetContentDeliveryRules.of_json
                                       contentDeliveryRules)
              ?retentionPeriod:(Option.map ~f:Values.RetentionPeriod.of_json
                                  retentionPeriod)
              ?versioningConfiguration:(Option.map
                                          ~f:Values.VersioningConfiguration.of_json
                                          versioningConfiguration)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?lateDataRules:(Option.map ~f:Values.LateDataRules.of_json
                                lateDataRules) ~datasetName
              ~actions:(Values.DatasetActions.of_json actions) ())
           (Some Values.CreateDatasetResponse.to_json)
           (Some Values.CreateDatasetResponse.error_to_json)])
let create_dataset_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionId =
         flag "version-id" (optional string)
           ~doc:"STRING DatasetContentVersion"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dataset_content
           (Values.CreateDatasetContentRequest.make ?versionId ~datasetName
              ()) (Some Values.CreateDatasetContentResponse.to_json)
           (Some Values.CreateDatasetContentResponse.error_to_json)])
let create_datastore =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreStorage =
         flag "datastore-storage" (optional json_arg)
           ~doc:"JSON DatastoreStorage"
       and retentionPeriod =
         flag "retention-period" (optional json_arg)
           ~doc:"JSON RetentionPeriod"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and fileFormatConfiguration =
         flag "file-format-configuration" (optional json_arg)
           ~doc:"JSON FileFormatConfiguration"
       and datastorePartitions =
         flag "datastore-partitions" (optional json_arg)
           ~doc:"JSON DatastorePartitions"
       and datastoreName =
         flag "datastore-name" (required string) ~doc:"STRING DatastoreName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_datastore
           (Values.CreateDatastoreRequest.make
              ?datastoreStorage:(Option.map
                                   ~f:Values.DatastoreStorage.of_json
                                   datastoreStorage)
              ?retentionPeriod:(Option.map ~f:Values.RetentionPeriod.of_json
                                  retentionPeriod)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?fileFormatConfiguration:(Option.map
                                          ~f:Values.FileFormatConfiguration.of_json
                                          fileFormatConfiguration)
              ?datastorePartitions:(Option.map
                                      ~f:Values.DatastorePartitions.of_json
                                      datastorePartitions) ~datastoreName ())
           (Some Values.CreateDatastoreResponse.to_json)
           (Some Values.CreateDatastoreResponse.error_to_json)])
let create_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName"
       and pipelineActivities =
         flag "pipeline-activities" (required json_arg)
           ~doc:"JSON PipelineActivities" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pipeline
           (Values.CreatePipelineRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~pipelineName
              ~pipelineActivities:(Values.PipelineActivities.of_json
                                     pipelineActivities) ())
           (Some Values.CreatePipelineResponse.to_json)
           (Some Values.CreatePipelineResponse.error_to_json)])
let delete_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ChannelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel
           (Values.DeleteChannelRequest.make ~channelName ()) None None])
let delete_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dataset
           (Values.DeleteDatasetRequest.make ~datasetName ()) None None])
let delete_dataset_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionId =
         flag "version-id" (optional string)
           ~doc:"STRING DatasetContentVersion"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dataset_content
           (Values.DeleteDatasetContentRequest.make ?versionId ~datasetName
              ()) None None])
let delete_datastore =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreName =
         flag "datastore-name" (required string) ~doc:"STRING DatastoreName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_datastore
           (Values.DeleteDatastoreRequest.make ~datastoreName ()) None None])
let delete_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_pipeline
           (Values.DeletePipelineRequest.make ~pipelineName ()) None None])
let describe_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeStatistics =
         flag "include-statistics" (optional bool)
           ~doc:"BOOL IncludeStatisticsFlag"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ChannelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel
           (Values.DescribeChannelRequest.make ?includeStatistics
              ~channelName ()) (Some Values.DescribeChannelResponse.to_json)
           (Some Values.DescribeChannelResponse.error_to_json)])
let describe_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dataset
           (Values.DescribeDatasetRequest.make ~datasetName ())
           (Some Values.DescribeDatasetResponse.to_json)
           (Some Values.DescribeDatasetResponse.error_to_json)])
let describe_datastore =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeStatistics =
         flag "include-statistics" (optional bool)
           ~doc:"BOOL IncludeStatisticsFlag"
       and datastoreName =
         flag "datastore-name" (required string) ~doc:"STRING DatastoreName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_datastore
           (Values.DescribeDatastoreRequest.make ?includeStatistics
              ~datastoreName ())
           (Some Values.DescribeDatastoreResponse.to_json)
           (Some Values.DescribeDatastoreResponse.error_to_json)])
let describe_logging_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_logging_options
           (Values.DescribeLoggingOptionsRequest.make ())
           (Some Values.DescribeLoggingOptionsResponse.to_json)
           (Some Values.DescribeLoggingOptionsResponse.error_to_json)])
let describe_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_pipeline
           (Values.DescribePipelineRequest.make ~pipelineName ())
           (Some Values.DescribePipelineResponse.to_json)
           (Some Values.DescribePipelineResponse.error_to_json)])
let get_dataset_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionId =
         flag "version-id" (optional string)
           ~doc:"STRING DatasetContentVersion"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dataset_content
           (Values.GetDatasetContentRequest.make ?versionId ~datasetName ())
           (Some Values.GetDatasetContentResponse.to_json)
           (Some Values.GetDatasetContentResponse.error_to_json)])
let list_channels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channels
           (Values.ListChannelsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListChannelsResponse.to_json)
           (Some Values.ListChannelsResponse.error_to_json)])
let list_dataset_contents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and scheduledOnOrAfter =
         flag "scheduled-on-or-after" (optional json_arg)
           ~doc:"JSON Timestamp"
       and scheduledBefore =
         flag "scheduled-before" (optional json_arg) ~doc:"JSON Timestamp"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dataset_contents
           (Values.ListDatasetContentsRequest.make ?nextToken ?maxResults
              ?scheduledOnOrAfter:(Option.map ~f:Values.Timestamp.of_json
                                     scheduledOnOrAfter)
              ?scheduledBefore:(Option.map ~f:Values.Timestamp.of_json
                                  scheduledBefore) ~datasetName ())
           (Some Values.ListDatasetContentsResponse.to_json)
           (Some Values.ListDatasetContentsResponse.error_to_json)])
let list_datasets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_datasets
           (Values.ListDatasetsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListDatasetsResponse.to_json)
           (Some Values.ListDatasetsResponse.error_to_json)])
let list_datastores =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_datastores
           (Values.ListDatastoresRequest.make ?nextToken ?maxResults ())
           (Some Values.ListDatastoresResponse.to_json)
           (Some Values.ListDatastoresResponse.error_to_json)])
let list_pipelines =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pipelines
           (Values.ListPipelinesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListPipelinesResponse.to_json)
           (Some Values.ListPipelinesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn__lc1 =
         flag "resource-arn--lc1" (optional string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?resourceArn__lc1 ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_logging_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loggingOptions =
         flag "logging-options" (required json_arg)
           ~doc:"JSON LoggingOptions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_logging_options
           (Values.PutLoggingOptionsRequest.make
              ~loggingOptions:(Values.LoggingOptions.of_json loggingOptions)
              ()) None None])
let run_pipeline_activity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineActivity =
         flag "pipeline-activity" (required json_arg)
           ~doc:"JSON PipelineActivity"
       and payloads =
         flag "payloads" (required json_arg) ~doc:"JSON MessagePayloads" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.run_pipeline_activity
           (Values.RunPipelineActivityRequest.make
              ~pipelineActivity:(Values.PipelineActivity.of_json
                                   pipelineActivity)
              ~payloads:(Values.MessagePayloads.of_json payloads) ())
           (Some Values.RunPipelineActivityResponse.to_json)
           (Some Values.RunPipelineActivityResponse.error_to_json)])
let sample_channel_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxMessages =
         flag "max-messages" (optional int) ~doc:"INT MaxMessages"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON StartTime"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON EndTime"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ChannelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.sample_channel_data
           (Values.SampleChannelDataRequest.make ?maxMessages
              ?startTime:(Option.map ~f:Values.StartTime.of_json startTime)
              ?endTime:(Option.map ~f:Values.EndTime.of_json endTime)
              ~channelName ())
           (Some Values.SampleChannelDataResponse.to_json)
           (Some Values.SampleChannelDataResponse.error_to_json)])
let start_pipeline_reprocessing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON StartTime"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON EndTime"
       and channelMessages =
         flag "channel-messages" (optional json_arg)
           ~doc:"JSON ChannelMessages"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_pipeline_reprocessing
           (Values.StartPipelineReprocessingRequest.make
              ?startTime:(Option.map ~f:Values.StartTime.of_json startTime)
              ?endTime:(Option.map ~f:Values.EndTime.of_json endTime)
              ?channelMessages:(Option.map ~f:Values.ChannelMessages.of_json
                                  channelMessages) ~pipelineName ())
           (Some Values.StartPipelineReprocessingResponse.to_json)
           (Some Values.StartPipelineReprocessingResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn__lc1 =
         flag "resource-arn--lc1" (optional string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ?resourceArn__lc1
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn__lc1 =
         flag "resource-arn--lc1" (optional string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ?resourceArn__lc1
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelStorage =
         flag "channel-storage" (optional json_arg)
           ~doc:"JSON ChannelStorage"
       and retentionPeriod =
         flag "retention-period" (optional json_arg)
           ~doc:"JSON RetentionPeriod"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ChannelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel
           (Values.UpdateChannelRequest.make
              ?channelStorage:(Option.map ~f:Values.ChannelStorage.of_json
                                 channelStorage)
              ?retentionPeriod:(Option.map ~f:Values.RetentionPeriod.of_json
                                  retentionPeriod) ~channelName ()) None None])
let update_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and triggers =
         flag "triggers" (optional json_arg) ~doc:"JSON DatasetTriggers"
       and contentDeliveryRules =
         flag "content-delivery-rules" (optional json_arg)
           ~doc:"JSON DatasetContentDeliveryRules"
       and retentionPeriod =
         flag "retention-period" (optional json_arg)
           ~doc:"JSON RetentionPeriod"
       and versioningConfiguration =
         flag "versioning-configuration" (optional json_arg)
           ~doc:"JSON VersioningConfiguration"
       and lateDataRules =
         flag "late-data-rules" (optional json_arg) ~doc:"JSON LateDataRules"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING DatasetName"
       and actions =
         flag "actions" (required json_arg) ~doc:"JSON DatasetActions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_dataset
           (Values.UpdateDatasetRequest.make
              ?triggers:(Option.map ~f:Values.DatasetTriggers.of_json
                           triggers)
              ?contentDeliveryRules:(Option.map
                                       ~f:Values.DatasetContentDeliveryRules.of_json
                                       contentDeliveryRules)
              ?retentionPeriod:(Option.map ~f:Values.RetentionPeriod.of_json
                                  retentionPeriod)
              ?versioningConfiguration:(Option.map
                                          ~f:Values.VersioningConfiguration.of_json
                                          versioningConfiguration)
              ?lateDataRules:(Option.map ~f:Values.LateDataRules.of_json
                                lateDataRules) ~datasetName
              ~actions:(Values.DatasetActions.of_json actions) ()) None None])
let update_datastore =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retentionPeriod =
         flag "retention-period" (optional json_arg)
           ~doc:"JSON RetentionPeriod"
       and datastoreStorage =
         flag "datastore-storage" (optional json_arg)
           ~doc:"JSON DatastoreStorage"
       and fileFormatConfiguration =
         flag "file-format-configuration" (optional json_arg)
           ~doc:"JSON FileFormatConfiguration"
       and datastoreName =
         flag "datastore-name" (required string) ~doc:"STRING DatastoreName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_datastore
           (Values.UpdateDatastoreRequest.make
              ?retentionPeriod:(Option.map ~f:Values.RetentionPeriod.of_json
                                  retentionPeriod)
              ?datastoreStorage:(Option.map
                                   ~f:Values.DatastoreStorage.of_json
                                   datastoreStorage)
              ?fileFormatConfiguration:(Option.map
                                          ~f:Values.FileFormatConfiguration.of_json
                                          fileFormatConfiguration)
              ~datastoreName ()) None None])
let update_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName"
       and pipelineActivities =
         flag "pipeline-activities" (required json_arg)
           ~doc:"JSON PipelineActivities" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pipeline
           (Values.UpdatePipelineRequest.make ~pipelineName
              ~pipelineActivities:(Values.PipelineActivities.of_json
                                     pipelineActivities) ()) None None])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("batch-put-message", batch_put_message);
    ("cancel-pipeline-reprocessing", cancel_pipeline_reprocessing);
    ("create-channel", create_channel);
    ("create-dataset", create_dataset);
    ("create-dataset-content", create_dataset_content);
    ("create-datastore", create_datastore);
    ("create-pipeline", create_pipeline);
    ("delete-channel", delete_channel);
    ("delete-dataset", delete_dataset);
    ("delete-dataset-content", delete_dataset_content);
    ("delete-datastore", delete_datastore);
    ("delete-pipeline", delete_pipeline);
    ("describe-channel", describe_channel);
    ("describe-dataset", describe_dataset);
    ("describe-datastore", describe_datastore);
    ("describe-logging-options", describe_logging_options);
    ("describe-pipeline", describe_pipeline);
    ("get-dataset-content", get_dataset_content);
    ("list-channels", list_channels);
    ("list-dataset-contents", list_dataset_contents);
    ("list-datasets", list_datasets);
    ("list-datastores", list_datastores);
    ("list-pipelines", list_pipelines);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-logging-options", put_logging_options);
    ("run-pipeline-activity", run_pipeline_activity);
    ("sample-channel-data", sample_channel_data);
    ("start-pipeline-reprocessing", start_pipeline_reprocessing);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-channel", update_channel);
    ("update-dataset", update_dataset);
    ("update-datastore", update_datastore);
    ("update-pipeline", update_pipeline)]
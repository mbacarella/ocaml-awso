(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING BillingGroupArn"
       and accountIds =
         flag "account-ids" (required json_arg) ~doc:"JSON AccountIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_accounts
           (Values.AssociateAccountsInput.make ~arn
              ~accountIds:(Values.AccountIdList.of_json accountIds) ())
           (Some Values.AssociateAccountsOutput.to_json)
           (Some Values.AssociateAccountsOutput.error_to_json)])
let associate_pricing_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PricingPlanArn"
       and pricingRuleArns =
         flag "pricing-rule-arns" (required json_arg)
           ~doc:"JSON PricingRuleArnsNonEmptyInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_pricing_rules
           (Values.AssociatePricingRulesInput.make ~arn
              ~pricingRuleArns:(Values.PricingRuleArnsNonEmptyInput.of_json
                                  pricingRuleArns) ())
           (Some Values.AssociatePricingRulesOutput.to_json)
           (Some Values.AssociatePricingRulesOutput.error_to_json)])
let batch_associate_resources_to_custom_line_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingPeriodRange =
         flag "billing-period-range" (optional json_arg)
           ~doc:"JSON CustomLineItemBillingPeriodRange"
       and targetArn =
         flag "target-arn" (required string) ~doc:"STRING CustomLineItemArn"
       and resourceArns =
         flag "resource-arns" (required json_arg)
           ~doc:"JSON CustomLineItemBatchAssociationsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_associate_resources_to_custom_line_item
           (Values.BatchAssociateResourcesToCustomLineItemInput.make
              ?billingPeriodRange:(Option.map
                                     ~f:Values.CustomLineItemBillingPeriodRange.of_json
                                     billingPeriodRange) ~targetArn
              ~resourceArns:(Values.CustomLineItemBatchAssociationsList.of_json
                               resourceArns) ())
           (Some Values.BatchAssociateResourcesToCustomLineItemOutput.to_json)
           (Some
              Values.BatchAssociateResourcesToCustomLineItemOutput.error_to_json)])
let batch_disassociate_resources_from_custom_line_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingPeriodRange =
         flag "billing-period-range" (optional json_arg)
           ~doc:"JSON CustomLineItemBillingPeriodRange"
       and targetArn =
         flag "target-arn" (required string) ~doc:"STRING CustomLineItemArn"
       and resourceArns =
         flag "resource-arns" (required json_arg)
           ~doc:"JSON CustomLineItemBatchDisassociationsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_disassociate_resources_from_custom_line_item
           (Values.BatchDisassociateResourcesFromCustomLineItemInput.make
              ?billingPeriodRange:(Option.map
                                     ~f:Values.CustomLineItemBillingPeriodRange.of_json
                                     billingPeriodRange) ~targetArn
              ~resourceArns:(Values.CustomLineItemBatchDisassociationsList.of_json
                               resourceArns) ())
           (Some
              Values.BatchDisassociateResourcesFromCustomLineItemOutput.to_json)
           (Some
              Values.BatchDisassociateResourcesFromCustomLineItemOutput.error_to_json)])
let create_billing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and primaryAccountId =
         flag "primary-account-id" (optional string) ~doc:"STRING AccountId"
       and description =
         flag "description" (optional string)
           ~doc:"STRING BillingGroupDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name =
         flag "name" (required string) ~doc:"STRING BillingGroupName"
       and accountGrouping =
         flag "account-grouping" (required json_arg)
           ~doc:"JSON AccountGrouping"
       and computationPreference =
         flag "computation-preference" (required json_arg)
           ~doc:"JSON ComputationPreference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_billing_group
           (Values.CreateBillingGroupInput.make ?clientToken
              ?primaryAccountId ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~accountGrouping:(Values.AccountGrouping.of_json
                                  accountGrouping)
              ~computationPreference:(Values.ComputationPreference.of_json
                                        computationPreference) ())
           (Some Values.CreateBillingGroupOutput.to_json)
           (Some Values.CreateBillingGroupOutput.error_to_json)])
let create_custom_line_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and billingPeriodRange =
         flag "billing-period-range" (optional json_arg)
           ~doc:"JSON CustomLineItemBillingPeriodRange"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name =
         flag "name" (required string) ~doc:"STRING CustomLineItemName"
       and description =
         flag "description" (required string)
           ~doc:"STRING CustomLineItemDescription"
       and billingGroupArn =
         flag "billing-group-arn" (required string)
           ~doc:"STRING BillingGroupArn"
       and chargeDetails =
         flag "charge-details" (required json_arg)
           ~doc:"JSON CustomLineItemChargeDetails" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_line_item
           (Values.CreateCustomLineItemInput.make ?clientToken
              ?billingPeriodRange:(Option.map
                                     ~f:Values.CustomLineItemBillingPeriodRange.of_json
                                     billingPeriodRange)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~description ~billingGroupArn
              ~chargeDetails:(Values.CustomLineItemChargeDetails.of_json
                                chargeDetails) ())
           (Some Values.CreateCustomLineItemOutput.to_json)
           (Some Values.CreateCustomLineItemOutput.error_to_json)])
let create_pricing_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PricingPlanDescription"
       and pricingRuleArns =
         flag "pricing-rule-arns" (optional json_arg)
           ~doc:"JSON PricingRuleArnsInput"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING PricingPlanName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pricing_plan
           (Values.CreatePricingPlanInput.make ?clientToken ?description
              ?pricingRuleArns:(Option.map
                                  ~f:Values.PricingRuleArnsInput.of_json
                                  pricingRuleArns)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name ())
           (Some Values.CreatePricingPlanOutput.to_json)
           (Some Values.CreatePricingPlanOutput.error_to_json)])
let create_pricing_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PricingRuleDescription"
       and service = flag "service" (optional string) ~doc:"STRING Service"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING PricingRuleName"
       and scope =
         flag "scope" (required json_arg) ~doc:"JSON PricingRuleScope"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON PricingRuleType"
       and modifierPercentage =
         flag "modifier-percentage" (required float)
           ~doc:"FLOAT ModifierPercentage" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pricing_rule
           (Values.CreatePricingRuleInput.make ?clientToken ?description
              ?service ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~scope:(Values.PricingRuleScope.of_json scope)
              ~type_:(Values.PricingRuleType.of_json type_)
              ~modifierPercentage ())
           (Some Values.CreatePricingRuleOutput.to_json)
           (Some Values.CreatePricingRuleOutput.error_to_json)])
let delete_billing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING BillingGroupArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_billing_group
           (Values.DeleteBillingGroupInput.make ~arn ())
           (Some Values.DeleteBillingGroupOutput.to_json)
           (Some Values.DeleteBillingGroupOutput.error_to_json)])
let delete_custom_line_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingPeriodRange =
         flag "billing-period-range" (optional json_arg)
           ~doc:"JSON CustomLineItemBillingPeriodRange"
       and arn = flag "arn" (required string) ~doc:"STRING CustomLineItemArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_line_item
           (Values.DeleteCustomLineItemInput.make
              ?billingPeriodRange:(Option.map
                                     ~f:Values.CustomLineItemBillingPeriodRange.of_json
                                     billingPeriodRange) ~arn ())
           (Some Values.DeleteCustomLineItemOutput.to_json)
           (Some Values.DeleteCustomLineItemOutput.error_to_json)])
let delete_pricing_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PricingPlanArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_pricing_plan
           (Values.DeletePricingPlanInput.make ~arn ())
           (Some Values.DeletePricingPlanOutput.to_json)
           (Some Values.DeletePricingPlanOutput.error_to_json)])
let delete_pricing_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PricingRuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_pricing_rule
           (Values.DeletePricingRuleInput.make ~arn ())
           (Some Values.DeletePricingRuleOutput.to_json)
           (Some Values.DeletePricingRuleOutput.error_to_json)])
let disassociate_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING BillingGroupArn"
       and accountIds =
         flag "account-ids" (required json_arg) ~doc:"JSON AccountIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_accounts
           (Values.DisassociateAccountsInput.make ~arn
              ~accountIds:(Values.AccountIdList.of_json accountIds) ())
           (Some Values.DisassociateAccountsOutput.to_json)
           (Some Values.DisassociateAccountsOutput.error_to_json)])
let disassociate_pricing_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PricingPlanArn"
       and pricingRuleArns =
         flag "pricing-rule-arns" (required json_arg)
           ~doc:"JSON PricingRuleArnsNonEmptyInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_pricing_rules
           (Values.DisassociatePricingRulesInput.make ~arn
              ~pricingRuleArns:(Values.PricingRuleArnsNonEmptyInput.of_json
                                  pricingRuleArns) ())
           (Some Values.DisassociatePricingRulesOutput.to_json)
           (Some Values.DisassociatePricingRulesOutput.error_to_json)])
let list_account_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingPeriod =
         flag "billing-period" (optional string) ~doc:"STRING BillingPeriod"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ListAccountAssociationsFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_account_associations
           (Values.ListAccountAssociationsInput.make ?billingPeriod
              ?filters:(Option.map
                          ~f:Values.ListAccountAssociationsFilter.of_json
                          filters) ?nextToken ())
           (Some Values.ListAccountAssociationsOutput.to_json)
           (Some Values.ListAccountAssociationsOutput.error_to_json)])
let list_billing_group_cost_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingPeriod =
         flag "billing-period" (optional string) ~doc:"STRING BillingPeriod"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxBillingGroupResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ListBillingGroupCostReportsFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_billing_group_cost_reports
           (Values.ListBillingGroupCostReportsInput.make ?billingPeriod
              ?maxResults ?nextToken
              ?filters:(Option.map
                          ~f:Values.ListBillingGroupCostReportsFilter.of_json
                          filters) ())
           (Some Values.ListBillingGroupCostReportsOutput.to_json)
           (Some Values.ListBillingGroupCostReportsOutput.error_to_json)])
let list_billing_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingPeriod =
         flag "billing-period" (optional string) ~doc:"STRING BillingPeriod"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxBillingGroupResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ListBillingGroupsFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_billing_groups
           (Values.ListBillingGroupsInput.make ?billingPeriod ?maxResults
              ?nextToken
              ?filters:(Option.map ~f:Values.ListBillingGroupsFilter.of_json
                          filters) ())
           (Some Values.ListBillingGroupsOutput.to_json)
           (Some Values.ListBillingGroupsOutput.error_to_json)])
let list_custom_line_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingPeriod =
         flag "billing-period" (optional string) ~doc:"STRING BillingPeriod"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxBillingGroupResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ListCustomLineItemsFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_line_items
           (Values.ListCustomLineItemsInput.make ?billingPeriod ?maxResults
              ?nextToken
              ?filters:(Option.map
                          ~f:Values.ListCustomLineItemsFilter.of_json filters)
              ()) (Some Values.ListCustomLineItemsOutput.to_json)
           (Some Values.ListCustomLineItemsOutput.error_to_json)])
let list_pricing_plans =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingPeriod =
         flag "billing-period" (optional string) ~doc:"STRING BillingPeriod"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ListPricingPlansFilter"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPricingPlanResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pricing_plans
           (Values.ListPricingPlansInput.make ?billingPeriod
              ?filters:(Option.map ~f:Values.ListPricingPlansFilter.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.ListPricingPlansOutput.to_json)
           (Some Values.ListPricingPlansOutput.error_to_json)])
let list_pricing_plans_associated_with_pricing_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingPeriod =
         flag "billing-period" (optional string) ~doc:"STRING BillingPeriod"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPricingRuleResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and pricingRuleArn =
         flag "pricing-rule-arn" (required string)
           ~doc:"STRING PricingRuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pricing_plans_associated_with_pricing_rule
           (Values.ListPricingPlansAssociatedWithPricingRuleInput.make
              ?billingPeriod ?maxResults ?nextToken ~pricingRuleArn ())
           (Some
              Values.ListPricingPlansAssociatedWithPricingRuleOutput.to_json)
           (Some
              Values.ListPricingPlansAssociatedWithPricingRuleOutput.error_to_json)])
let list_pricing_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingPeriod =
         flag "billing-period" (optional string) ~doc:"STRING BillingPeriod"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ListPricingRulesFilter"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPricingRuleResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pricing_rules
           (Values.ListPricingRulesInput.make ?billingPeriod
              ?filters:(Option.map ~f:Values.ListPricingRulesFilter.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.ListPricingRulesOutput.to_json)
           (Some Values.ListPricingRulesOutput.error_to_json)])
let list_pricing_rules_associated_to_pricing_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingPeriod =
         flag "billing-period" (optional string) ~doc:"STRING BillingPeriod"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxPricingPlanResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and pricingPlanArn =
         flag "pricing-plan-arn" (required string)
           ~doc:"STRING PricingPlanArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pricing_rules_associated_to_pricing_plan
           (Values.ListPricingRulesAssociatedToPricingPlanInput.make
              ?billingPeriod ?maxResults ?nextToken ~pricingPlanArn ())
           (Some Values.ListPricingRulesAssociatedToPricingPlanOutput.to_json)
           (Some
              Values.ListPricingRulesAssociatedToPricingPlanOutput.error_to_json)])
let list_resources_associated_to_custom_line_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingPeriod =
         flag "billing-period" (optional string) ~doc:"STRING BillingPeriod"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxCustomLineItemResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ListResourcesAssociatedToCustomLineItemFilter"
       and arn = flag "arn" (required string) ~doc:"STRING CustomLineItemArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resources_associated_to_custom_line_item
           (Values.ListResourcesAssociatedToCustomLineItemInput.make
              ?billingPeriod ?maxResults ?nextToken
              ?filters:(Option.map
                          ~f:Values.ListResourcesAssociatedToCustomLineItemFilter.of_json
                          filters) ~arn ())
           (Some Values.ListResourcesAssociatedToCustomLineItemOutput.to_json)
           (Some
              Values.ListResourcesAssociatedToCustomLineItemOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_billing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING BillingGroupName"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON BillingGroupStatus"
       and computationPreference =
         flag "computation-preference" (optional json_arg)
           ~doc:"JSON ComputationPreference"
       and description =
         flag "description" (optional string)
           ~doc:"STRING BillingGroupDescription"
       and arn = flag "arn" (required string) ~doc:"STRING BillingGroupArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_billing_group
           (Values.UpdateBillingGroupInput.make ?name
              ?status:(Option.map ~f:Values.BillingGroupStatus.of_json status)
              ?computationPreference:(Option.map
                                        ~f:Values.ComputationPreference.of_json
                                        computationPreference) ?description
              ~arn ()) (Some Values.UpdateBillingGroupOutput.to_json)
           (Some Values.UpdateBillingGroupOutput.error_to_json)])
let update_custom_line_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING BillingGroupName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CustomLineItemDescription"
       and chargeDetails =
         flag "charge-details" (optional json_arg)
           ~doc:"JSON UpdateCustomLineItemChargeDetails"
       and billingPeriodRange =
         flag "billing-period-range" (optional json_arg)
           ~doc:"JSON CustomLineItemBillingPeriodRange"
       and arn = flag "arn" (required string) ~doc:"STRING CustomLineItemArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_custom_line_item
           (Values.UpdateCustomLineItemInput.make ?name ?description
              ?chargeDetails:(Option.map
                                ~f:Values.UpdateCustomLineItemChargeDetails.of_json
                                chargeDetails)
              ?billingPeriodRange:(Option.map
                                     ~f:Values.CustomLineItemBillingPeriodRange.of_json
                                     billingPeriodRange) ~arn ())
           (Some Values.UpdateCustomLineItemOutput.to_json)
           (Some Values.UpdateCustomLineItemOutput.error_to_json)])
let update_pricing_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING PricingPlanName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PricingPlanDescription"
       and arn = flag "arn" (required string) ~doc:"STRING PricingPlanArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pricing_plan
           (Values.UpdatePricingPlanInput.make ?name ?description ~arn ())
           (Some Values.UpdatePricingPlanOutput.to_json)
           (Some Values.UpdatePricingPlanOutput.error_to_json)])
let update_pricing_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING PricingRuleName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PricingRuleDescription"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON PricingRuleType"
       and modifierPercentage =
         flag "modifier-percentage" (optional float)
           ~doc:"FLOAT ModifierPercentage"
       and arn = flag "arn" (required string) ~doc:"STRING PricingRuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pricing_rule
           (Values.UpdatePricingRuleInput.make ?name ?description
              ?type_:(Option.map ~f:Values.PricingRuleType.of_json type_)
              ?modifierPercentage ~arn ())
           (Some Values.UpdatePricingRuleOutput.to_json)
           (Some Values.UpdatePricingRuleOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-accounts", associate_accounts);
    ("associate-pricing-rules", associate_pricing_rules);
    ("batch-associate-resources-to-custom-line-item",
      batch_associate_resources_to_custom_line_item);
    ("batch-disassociate-resources-from-custom-line-item",
      batch_disassociate_resources_from_custom_line_item);
    ("create-billing-group", create_billing_group);
    ("create-custom-line-item", create_custom_line_item);
    ("create-pricing-plan", create_pricing_plan);
    ("create-pricing-rule", create_pricing_rule);
    ("delete-billing-group", delete_billing_group);
    ("delete-custom-line-item", delete_custom_line_item);
    ("delete-pricing-plan", delete_pricing_plan);
    ("delete-pricing-rule", delete_pricing_rule);
    ("disassociate-accounts", disassociate_accounts);
    ("disassociate-pricing-rules", disassociate_pricing_rules);
    ("list-account-associations", list_account_associations);
    ("list-billing-group-cost-reports", list_billing_group_cost_reports);
    ("list-billing-groups", list_billing_groups);
    ("list-custom-line-items", list_custom_line_items);
    ("list-pricing-plans", list_pricing_plans);
    ("list-pricing-plans-associated-with-pricing-rule",
      list_pricing_plans_associated_with_pricing_rule);
    ("list-pricing-rules", list_pricing_rules);
    ("list-pricing-rules-associated-to-pricing-plan",
      list_pricing_rules_associated_to_pricing_plan);
    ("list-resources-associated-to-custom-line-item",
      list_resources_associated_to_custom_line_item);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-billing-group", update_billing_group);
    ("update-custom-line-item", update_custom_line_item);
    ("update-pricing-plan", update_pricing_plan);
    ("update-pricing-rule", update_pricing_rule)]
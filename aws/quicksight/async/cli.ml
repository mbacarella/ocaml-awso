(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_ingestion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSetId =
         flag "data-set-id" (required string) ~doc:"STRING String"
       and ingestionId =
         flag "ingestion-id" (required string) ~doc:"STRING IngestionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_ingestion
           (Values.CancelIngestionRequest.make ~awsAccountId ~dataSetId
              ~ingestionId ()) (Some Values.CancelIngestionResponse.to_json)
           (Some Values.CancelIngestionResponse.error_to_json)])
let create_account_customization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING Namespace"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and accountCustomization =
         flag "account-customization" (required json_arg)
           ~doc:"JSON AccountCustomization" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_account_customization
           (Values.CreateAccountCustomizationRequest.make ?namespace
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~awsAccountId
              ~accountCustomization:(Values.AccountCustomization.of_json
                                       accountCustomization) ())
           (Some Values.CreateAccountCustomizationResponse.to_json)
           (Some Values.CreateAccountCustomizationResponse.error_to_json)])
let create_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and permissions =
         flag "permissions" (optional json_arg)
           ~doc:"JSON ResourcePermissionList"
       and themeArn = flag "theme-arn" (optional string) ~doc:"STRING Arn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and analysisId =
         flag "analysis-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and name = flag "name" (required string) ~doc:"STRING AnalysisName"
       and sourceEntity =
         flag "source-entity" (required json_arg)
           ~doc:"JSON AnalysisSourceEntity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_analysis
           (Values.CreateAnalysisRequest.make
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?permissions:(Option.map
                              ~f:Values.ResourcePermissionList.of_json
                              permissions) ?themeArn
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~awsAccountId
              ~analysisId ~name
              ~sourceEntity:(Values.AnalysisSourceEntity.of_json sourceEntity)
              ()) (Some Values.CreateAnalysisResponse.to_json)
           (Some Values.CreateAnalysisResponse.error_to_json)])
let create_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and permissions =
         flag "permissions" (optional json_arg)
           ~doc:"JSON ResourcePermissionList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and versionDescription =
         flag "version-description" (optional string)
           ~doc:"STRING VersionDescription"
       and dashboardPublishOptions =
         flag "dashboard-publish-options" (optional json_arg)
           ~doc:"JSON DashboardPublishOptions"
       and themeArn = flag "theme-arn" (optional string) ~doc:"STRING Arn"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dashboardId =
         flag "dashboard-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and name = flag "name" (required string) ~doc:"STRING DashboardName"
       and sourceEntity =
         flag "source-entity" (required json_arg)
           ~doc:"JSON DashboardSourceEntity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dashboard
           (Values.CreateDashboardRequest.make
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?permissions:(Option.map
                              ~f:Values.ResourcePermissionList.of_json
                              permissions)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?versionDescription
              ?dashboardPublishOptions:(Option.map
                                          ~f:Values.DashboardPublishOptions.of_json
                                          dashboardPublishOptions) ?themeArn
              ~awsAccountId ~dashboardId ~name
              ~sourceEntity:(Values.DashboardSourceEntity.of_json
                               sourceEntity) ())
           (Some Values.CreateDashboardResponse.to_json)
           (Some Values.CreateDashboardResponse.error_to_json)])
let create_data_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and logicalTableMap =
         flag "logical-table-map" (optional json_arg)
           ~doc:"JSON LogicalTableMap"
       and columnGroups =
         flag "column-groups" (optional json_arg) ~doc:"JSON ColumnGroupList"
       and fieldFolders =
         flag "field-folders" (optional json_arg) ~doc:"JSON FieldFolderMap"
       and permissions =
         flag "permissions" (optional json_arg)
           ~doc:"JSON ResourcePermissionList"
       and rowLevelPermissionDataSet =
         flag "row-level-permission-data-set" (optional json_arg)
           ~doc:"JSON RowLevelPermissionDataSet"
       and rowLevelPermissionTagConfiguration =
         flag "row-level-permission-tag-configuration" (optional json_arg)
           ~doc:"JSON RowLevelPermissionTagConfiguration"
       and columnLevelPermissionRules =
         flag "column-level-permission-rules" (optional json_arg)
           ~doc:"JSON ColumnLevelPermissionRuleList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dataSetUsageConfiguration =
         flag "data-set-usage-configuration" (optional json_arg)
           ~doc:"JSON DataSetUsageConfiguration"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSetId =
         flag "data-set-id" (required string) ~doc:"STRING ResourceId"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and physicalTableMap =
         flag "physical-table-map" (required json_arg)
           ~doc:"JSON PhysicalTableMap"
       and importMode =
         flag "import-mode" (required json_arg) ~doc:"JSON DataSetImportMode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_set
           (Values.CreateDataSetRequest.make
              ?logicalTableMap:(Option.map ~f:Values.LogicalTableMap.of_json
                                  logicalTableMap)
              ?columnGroups:(Option.map ~f:Values.ColumnGroupList.of_json
                               columnGroups)
              ?fieldFolders:(Option.map ~f:Values.FieldFolderMap.of_json
                               fieldFolders)
              ?permissions:(Option.map
                              ~f:Values.ResourcePermissionList.of_json
                              permissions)
              ?rowLevelPermissionDataSet:(Option.map
                                            ~f:Values.RowLevelPermissionDataSet.of_json
                                            rowLevelPermissionDataSet)
              ?rowLevelPermissionTagConfiguration:(Option.map
                                                     ~f:Values.RowLevelPermissionTagConfiguration.of_json
                                                     rowLevelPermissionTagConfiguration)
              ?columnLevelPermissionRules:(Option.map
                                             ~f:Values.ColumnLevelPermissionRuleList.of_json
                                             columnLevelPermissionRules)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?dataSetUsageConfiguration:(Option.map
                                            ~f:Values.DataSetUsageConfiguration.of_json
                                            dataSetUsageConfiguration)
              ~awsAccountId ~dataSetId ~name
              ~physicalTableMap:(Values.PhysicalTableMap.of_json
                                   physicalTableMap)
              ~importMode:(Values.DataSetImportMode.of_json importMode) ())
           (Some Values.CreateDataSetResponse.to_json)
           (Some Values.CreateDataSetResponse.error_to_json)])
let create_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceParameters =
         flag "data-source-parameters" (optional json_arg)
           ~doc:"JSON DataSourceParameters"
       and credentials =
         flag "credentials" (optional json_arg)
           ~doc:"JSON DataSourceCredentials"
       and permissions =
         flag "permissions" (optional json_arg)
           ~doc:"JSON ResourcePermissionList"
       and vpcConnectionProperties =
         flag "vpc-connection-properties" (optional json_arg)
           ~doc:"JSON VpcConnectionProperties"
       and sslProperties =
         flag "ssl-properties" (optional json_arg) ~doc:"JSON SslProperties"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING ResourceId"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON DataSourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_source
           (Values.CreateDataSourceRequest.make
              ?dataSourceParameters:(Option.map
                                       ~f:Values.DataSourceParameters.of_json
                                       dataSourceParameters)
              ?credentials:(Option.map
                              ~f:Values.DataSourceCredentials.of_json
                              credentials)
              ?permissions:(Option.map
                              ~f:Values.ResourcePermissionList.of_json
                              permissions)
              ?vpcConnectionProperties:(Option.map
                                          ~f:Values.VpcConnectionProperties.of_json
                                          vpcConnectionProperties)
              ?sslProperties:(Option.map ~f:Values.SslProperties.of_json
                                sslProperties)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~awsAccountId
              ~dataSourceId ~name
              ~type_:(Values.DataSourceType.of_json type_) ())
           (Some Values.CreateDataSourceResponse.to_json)
           (Some Values.CreateDataSourceResponse.error_to_json)])
let create_folder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING FolderName"
       and folderType =
         flag "folder-type" (optional json_arg) ~doc:"JSON FolderType"
       and parentFolderArn =
         flag "parent-folder-arn" (optional string) ~doc:"STRING Arn"
       and permissions =
         flag "permissions" (optional json_arg)
           ~doc:"JSON ResourcePermissionList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and folderId =
         flag "folder-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_folder
           (Values.CreateFolderRequest.make ?name
              ?folderType:(Option.map ~f:Values.FolderType.of_json folderType)
              ?parentFolderArn
              ?permissions:(Option.map
                              ~f:Values.ResourcePermissionList.of_json
                              permissions)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~awsAccountId
              ~folderId ()) (Some Values.CreateFolderResponse.to_json)
           (Some Values.CreateFolderResponse.error_to_json)])
let create_folder_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and folderId =
         flag "folder-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and memberId =
         flag "member-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and memberType =
         flag "member-type" (required json_arg) ~doc:"JSON MemberType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_folder_membership
           (Values.CreateFolderMembershipRequest.make ~awsAccountId ~folderId
              ~memberId ~memberType:(Values.MemberType.of_json memberType) ())
           (Some Values.CreateFolderMembershipResponse.to_json)
           (Some Values.CreateFolderMembershipResponse.error_to_json)])
let create_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING GroupDescription"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_group
           (Values.CreateGroupRequest.make ?description ~groupName
              ~awsAccountId ~namespace ())
           (Some Values.CreateGroupResponse.to_json)
           (Some Values.CreateGroupResponse.error_to_json)])
let create_group_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberName =
         flag "member-name" (required string) ~doc:"STRING GroupMemberName"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_group_membership
           (Values.CreateGroupMembershipRequest.make ~memberName ~groupName
              ~awsAccountId ~namespace ())
           (Some Values.CreateGroupMembershipResponse.to_json)
           (Some Values.CreateGroupMembershipResponse.error_to_json)])
let create_i_a_m_policy_assignment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn = flag "policy-arn" (optional string) ~doc:"STRING Arn"
       and identities =
         flag "identities" (optional json_arg) ~doc:"JSON IdentityMap"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and assignmentName =
         flag "assignment-name" (required string)
           ~doc:"STRING IAMPolicyAssignmentName"
       and assignmentStatus =
         flag "assignment-status" (required json_arg)
           ~doc:"JSON AssignmentStatus"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_i_a_m_policy_assignment
           (Values.CreateIAMPolicyAssignmentRequest.make ?policyArn
              ?identities:(Option.map ~f:Values.IdentityMap.of_json
                             identities) ~awsAccountId ~assignmentName
              ~assignmentStatus:(Values.AssignmentStatus.of_json
                                   assignmentStatus) ~namespace ())
           (Some Values.CreateIAMPolicyAssignmentResponse.to_json)
           (Some Values.CreateIAMPolicyAssignmentResponse.error_to_json)])
let create_ingestion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ingestionType =
         flag "ingestion-type" (optional json_arg) ~doc:"JSON IngestionType"
       and dataSetId =
         flag "data-set-id" (required string) ~doc:"STRING String"
       and ingestionId =
         flag "ingestion-id" (required string) ~doc:"STRING IngestionId"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ingestion
           (Values.CreateIngestionRequest.make
              ?ingestionType:(Option.map ~f:Values.IngestionType.of_json
                                ingestionType) ~dataSetId ~ingestionId
              ~awsAccountId ()) (Some Values.CreateIngestionResponse.to_json)
           (Some Values.CreateIngestionResponse.error_to_json)])
let create_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace"
       and identityStore =
         flag "identity-store" (required json_arg) ~doc:"JSON IdentityStore" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_namespace
           (Values.CreateNamespaceRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~awsAccountId
              ~namespace
              ~identityStore:(Values.IdentityStore.of_json identityStore) ())
           (Some Values.CreateNamespaceResponse.to_json)
           (Some Values.CreateNamespaceResponse.error_to_json)])
let create_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING TemplateName"
       and permissions =
         flag "permissions" (optional json_arg)
           ~doc:"JSON ResourcePermissionList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and versionDescription =
         flag "version-description" (optional string)
           ~doc:"STRING VersionDescription"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and templateId =
         flag "template-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and sourceEntity =
         flag "source-entity" (required json_arg)
           ~doc:"JSON TemplateSourceEntity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_template
           (Values.CreateTemplateRequest.make ?name
              ?permissions:(Option.map
                              ~f:Values.ResourcePermissionList.of_json
                              permissions)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?versionDescription ~awsAccountId ~templateId
              ~sourceEntity:(Values.TemplateSourceEntity.of_json sourceEntity)
              ()) (Some Values.CreateTemplateResponse.to_json)
           (Some Values.CreateTemplateResponse.error_to_json)])
let create_template_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and templateId =
         flag "template-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasName"
       and templateVersionNumber =
         flag "template-version-number" (required json_arg)
           ~doc:"JSON VersionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_template_alias
           (Values.CreateTemplateAliasRequest.make ~awsAccountId ~templateId
              ~aliasName
              ~templateVersionNumber:(Values.VersionNumber.of_json
                                        templateVersionNumber) ())
           (Some Values.CreateTemplateAliasResponse.to_json)
           (Some Values.CreateTemplateAliasResponse.error_to_json)])
let create_theme =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionDescription =
         flag "version-description" (optional string)
           ~doc:"STRING VersionDescription"
       and permissions =
         flag "permissions" (optional json_arg)
           ~doc:"JSON ResourcePermissionList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and themeId =
         flag "theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and name = flag "name" (required string) ~doc:"STRING ThemeName"
       and baseThemeId =
         flag "base-theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON ThemeConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_theme
           (Values.CreateThemeRequest.make ?versionDescription
              ?permissions:(Option.map
                              ~f:Values.ResourcePermissionList.of_json
                              permissions)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~awsAccountId
              ~themeId ~name ~baseThemeId
              ~configuration:(Values.ThemeConfiguration.of_json configuration)
              ()) (Some Values.CreateThemeResponse.to_json)
           (Some Values.CreateThemeResponse.error_to_json)])
let create_theme_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and themeId =
         flag "theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasName"
       and themeVersionNumber =
         flag "theme-version-number" (required json_arg)
           ~doc:"JSON VersionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_theme_alias
           (Values.CreateThemeAliasRequest.make ~awsAccountId ~themeId
              ~aliasName
              ~themeVersionNumber:(Values.VersionNumber.of_json
                                     themeVersionNumber) ())
           (Some Values.CreateThemeAliasResponse.to_json)
           (Some Values.CreateThemeAliasResponse.error_to_json)])
let delete_account_customization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING Namespace"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_account_customization
           (Values.DeleteAccountCustomizationRequest.make ?namespace
              ~awsAccountId ())
           (Some Values.DeleteAccountCustomizationResponse.to_json)
           (Some Values.DeleteAccountCustomizationResponse.error_to_json)])
let delete_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recoveryWindowInDays =
         flag "recovery-window-in-days" (optional json_arg)
           ~doc:"JSON RecoveryWindowInDays"
       and forceDeleteWithoutRecovery =
         flag "force-delete-without-recovery" (optional bool)
           ~doc:"BOOL Boolean__lc1"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and analysisId =
         flag "analysis-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_analysis
           (Values.DeleteAnalysisRequest.make
              ?recoveryWindowInDays:(Option.map
                                       ~f:Values.RecoveryWindowInDays.of_json
                                       recoveryWindowInDays)
              ?forceDeleteWithoutRecovery ~awsAccountId ~analysisId ())
           (Some Values.DeleteAnalysisResponse.to_json)
           (Some Values.DeleteAnalysisResponse.error_to_json)])
let delete_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionNumber =
         flag "version-number" (optional json_arg) ~doc:"JSON VersionNumber"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dashboardId =
         flag "dashboard-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dashboard
           (Values.DeleteDashboardRequest.make
              ?versionNumber:(Option.map ~f:Values.VersionNumber.of_json
                                versionNumber) ~awsAccountId ~dashboardId ())
           (Some Values.DeleteDashboardResponse.to_json)
           (Some Values.DeleteDashboardResponse.error_to_json)])
let delete_data_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSetId =
         flag "data-set-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_set
           (Values.DeleteDataSetRequest.make ~awsAccountId ~dataSetId ())
           (Some Values.DeleteDataSetResponse.to_json)
           (Some Values.DeleteDataSetResponse.error_to_json)])
let delete_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_source
           (Values.DeleteDataSourceRequest.make ~awsAccountId ~dataSourceId
              ()) (Some Values.DeleteDataSourceResponse.to_json)
           (Some Values.DeleteDataSourceResponse.error_to_json)])
let delete_folder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and folderId =
         flag "folder-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_folder
           (Values.DeleteFolderRequest.make ~awsAccountId ~folderId ())
           (Some Values.DeleteFolderResponse.to_json)
           (Some Values.DeleteFolderResponse.error_to_json)])
let delete_folder_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and folderId =
         flag "folder-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and memberId =
         flag "member-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and memberType =
         flag "member-type" (required json_arg) ~doc:"JSON MemberType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_folder_membership
           (Values.DeleteFolderMembershipRequest.make ~awsAccountId ~folderId
              ~memberId ~memberType:(Values.MemberType.of_json memberType) ())
           (Some Values.DeleteFolderMembershipResponse.to_json)
           (Some Values.DeleteFolderMembershipResponse.error_to_json)])
let delete_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_group
           (Values.DeleteGroupRequest.make ~groupName ~awsAccountId
              ~namespace ()) (Some Values.DeleteGroupResponse.to_json)
           (Some Values.DeleteGroupResponse.error_to_json)])
let delete_group_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberName =
         flag "member-name" (required string) ~doc:"STRING GroupMemberName"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_group_membership
           (Values.DeleteGroupMembershipRequest.make ~memberName ~groupName
              ~awsAccountId ~namespace ())
           (Some Values.DeleteGroupMembershipResponse.to_json)
           (Some Values.DeleteGroupMembershipResponse.error_to_json)])
let delete_i_a_m_policy_assignment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and assignmentName =
         flag "assignment-name" (required string)
           ~doc:"STRING IAMPolicyAssignmentName"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_i_a_m_policy_assignment
           (Values.DeleteIAMPolicyAssignmentRequest.make ~awsAccountId
              ~assignmentName ~namespace ())
           (Some Values.DeleteIAMPolicyAssignmentResponse.to_json)
           (Some Values.DeleteIAMPolicyAssignmentResponse.error_to_json)])
let delete_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_namespace
           (Values.DeleteNamespaceRequest.make ~awsAccountId ~namespace ())
           (Some Values.DeleteNamespaceResponse.to_json)
           (Some Values.DeleteNamespaceResponse.error_to_json)])
let delete_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionNumber =
         flag "version-number" (optional json_arg) ~doc:"JSON VersionNumber"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and templateId =
         flag "template-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_template
           (Values.DeleteTemplateRequest.make
              ?versionNumber:(Option.map ~f:Values.VersionNumber.of_json
                                versionNumber) ~awsAccountId ~templateId ())
           (Some Values.DeleteTemplateResponse.to_json)
           (Some Values.DeleteTemplateResponse.error_to_json)])
let delete_template_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and templateId =
         flag "template-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_template_alias
           (Values.DeleteTemplateAliasRequest.make ~awsAccountId ~templateId
              ~aliasName ())
           (Some Values.DeleteTemplateAliasResponse.to_json)
           (Some Values.DeleteTemplateAliasResponse.error_to_json)])
let delete_theme =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionNumber =
         flag "version-number" (optional json_arg) ~doc:"JSON VersionNumber"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and themeId =
         flag "theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_theme
           (Values.DeleteThemeRequest.make
              ?versionNumber:(Option.map ~f:Values.VersionNumber.of_json
                                versionNumber) ~awsAccountId ~themeId ())
           (Some Values.DeleteThemeResponse.to_json)
           (Some Values.DeleteThemeResponse.error_to_json)])
let delete_theme_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and themeId =
         flag "theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_theme_alias
           (Values.DeleteThemeAliasRequest.make ~awsAccountId ~themeId
              ~aliasName ()) (Some Values.DeleteThemeAliasResponse.to_json)
           (Some Values.DeleteThemeAliasResponse.error_to_json)])
let delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING UserName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user
           (Values.DeleteUserRequest.make ~userName ~awsAccountId ~namespace
              ()) (Some Values.DeleteUserResponse.to_json)
           (Some Values.DeleteUserResponse.error_to_json)])
let delete_user_by_principal_id =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and principalId =
         flag "principal-id" (required string) ~doc:"STRING String"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user_by_principal_id
           (Values.DeleteUserByPrincipalIdRequest.make ~principalId
              ~awsAccountId ~namespace ())
           (Some Values.DeleteUserByPrincipalIdResponse.to_json)
           (Some Values.DeleteUserByPrincipalIdResponse.error_to_json)])
let describe_account_customization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING Namespace"
       and resolved =
         flag "resolved" (optional bool) ~doc:"BOOL Boolean__lc1"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_customization
           (Values.DescribeAccountCustomizationRequest.make ?namespace
              ?resolved ~awsAccountId ())
           (Some Values.DescribeAccountCustomizationResponse.to_json)
           (Some Values.DescribeAccountCustomizationResponse.error_to_json)])
let describe_account_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_settings
           (Values.DescribeAccountSettingsRequest.make ~awsAccountId ())
           (Some Values.DescribeAccountSettingsResponse.to_json)
           (Some Values.DescribeAccountSettingsResponse.error_to_json)])
let describe_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and analysisId =
         flag "analysis-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_analysis
           (Values.DescribeAnalysisRequest.make ~awsAccountId ~analysisId ())
           (Some Values.DescribeAnalysisResponse.to_json)
           (Some Values.DescribeAnalysisResponse.error_to_json)])
let describe_analysis_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and analysisId =
         flag "analysis-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_analysis_permissions
           (Values.DescribeAnalysisPermissionsRequest.make ~awsAccountId
              ~analysisId ())
           (Some Values.DescribeAnalysisPermissionsResponse.to_json)
           (Some Values.DescribeAnalysisPermissionsResponse.error_to_json)])
let describe_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionNumber =
         flag "version-number" (optional json_arg) ~doc:"JSON VersionNumber"
       and aliasName =
         flag "alias-name" (optional string) ~doc:"STRING AliasName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dashboardId =
         flag "dashboard-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dashboard
           (Values.DescribeDashboardRequest.make
              ?versionNumber:(Option.map ~f:Values.VersionNumber.of_json
                                versionNumber) ?aliasName ~awsAccountId
              ~dashboardId ())
           (Some Values.DescribeDashboardResponse.to_json)
           (Some Values.DescribeDashboardResponse.error_to_json)])
let describe_dashboard_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dashboardId =
         flag "dashboard-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dashboard_permissions
           (Values.DescribeDashboardPermissionsRequest.make ~awsAccountId
              ~dashboardId ())
           (Some Values.DescribeDashboardPermissionsResponse.to_json)
           (Some Values.DescribeDashboardPermissionsResponse.error_to_json)])
let describe_data_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSetId =
         flag "data-set-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_data_set
           (Values.DescribeDataSetRequest.make ~awsAccountId ~dataSetId ())
           (Some Values.DescribeDataSetResponse.to_json)
           (Some Values.DescribeDataSetResponse.error_to_json)])
let describe_data_set_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSetId =
         flag "data-set-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_data_set_permissions
           (Values.DescribeDataSetPermissionsRequest.make ~awsAccountId
              ~dataSetId ())
           (Some Values.DescribeDataSetPermissionsResponse.to_json)
           (Some Values.DescribeDataSetPermissionsResponse.error_to_json)])
let describe_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_data_source
           (Values.DescribeDataSourceRequest.make ~awsAccountId ~dataSourceId
              ()) (Some Values.DescribeDataSourceResponse.to_json)
           (Some Values.DescribeDataSourceResponse.error_to_json)])
let describe_data_source_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_data_source_permissions
           (Values.DescribeDataSourcePermissionsRequest.make ~awsAccountId
              ~dataSourceId ())
           (Some Values.DescribeDataSourcePermissionsResponse.to_json)
           (Some Values.DescribeDataSourcePermissionsResponse.error_to_json)])
let describe_folder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and folderId =
         flag "folder-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_folder
           (Values.DescribeFolderRequest.make ~awsAccountId ~folderId ())
           (Some Values.DescribeFolderResponse.to_json)
           (Some Values.DescribeFolderResponse.error_to_json)])
let describe_folder_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and folderId =
         flag "folder-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_folder_permissions
           (Values.DescribeFolderPermissionsRequest.make ~awsAccountId
              ~folderId ())
           (Some Values.DescribeFolderPermissionsResponse.to_json)
           (Some Values.DescribeFolderPermissionsResponse.error_to_json)])
let describe_folder_resolved_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and folderId =
         flag "folder-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_folder_resolved_permissions
           (Values.DescribeFolderResolvedPermissionsRequest.make
              ~awsAccountId ~folderId ())
           (Some Values.DescribeFolderResolvedPermissionsResponse.to_json)
           (Some
              Values.DescribeFolderResolvedPermissionsResponse.error_to_json)])
let describe_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_group
           (Values.DescribeGroupRequest.make ~groupName ~awsAccountId
              ~namespace ()) (Some Values.DescribeGroupResponse.to_json)
           (Some Values.DescribeGroupResponse.error_to_json)])
let describe_group_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberName =
         flag "member-name" (required string) ~doc:"STRING GroupMemberName"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_group_membership
           (Values.DescribeGroupMembershipRequest.make ~memberName ~groupName
              ~awsAccountId ~namespace ())
           (Some Values.DescribeGroupMembershipResponse.to_json)
           (Some Values.DescribeGroupMembershipResponse.error_to_json)])
let describe_i_a_m_policy_assignment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and assignmentName =
         flag "assignment-name" (required string)
           ~doc:"STRING IAMPolicyAssignmentName"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_i_a_m_policy_assignment
           (Values.DescribeIAMPolicyAssignmentRequest.make ~awsAccountId
              ~assignmentName ~namespace ())
           (Some Values.DescribeIAMPolicyAssignmentResponse.to_json)
           (Some Values.DescribeIAMPolicyAssignmentResponse.error_to_json)])
let describe_ingestion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSetId =
         flag "data-set-id" (required string) ~doc:"STRING String"
       and ingestionId =
         flag "ingestion-id" (required string) ~doc:"STRING IngestionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ingestion
           (Values.DescribeIngestionRequest.make ~awsAccountId ~dataSetId
              ~ingestionId ())
           (Some Values.DescribeIngestionResponse.to_json)
           (Some Values.DescribeIngestionResponse.error_to_json)])
let describe_ip_restriction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ip_restriction
           (Values.DescribeIpRestrictionRequest.make ~awsAccountId ())
           (Some Values.DescribeIpRestrictionResponse.to_json)
           (Some Values.DescribeIpRestrictionResponse.error_to_json)])
let describe_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_namespace
           (Values.DescribeNamespaceRequest.make ~awsAccountId ~namespace ())
           (Some Values.DescribeNamespaceResponse.to_json)
           (Some Values.DescribeNamespaceResponse.error_to_json)])
let describe_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionNumber =
         flag "version-number" (optional json_arg) ~doc:"JSON VersionNumber"
       and aliasName =
         flag "alias-name" (optional string) ~doc:"STRING AliasName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and templateId =
         flag "template-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_template
           (Values.DescribeTemplateRequest.make
              ?versionNumber:(Option.map ~f:Values.VersionNumber.of_json
                                versionNumber) ?aliasName ~awsAccountId
              ~templateId ()) (Some Values.DescribeTemplateResponse.to_json)
           (Some Values.DescribeTemplateResponse.error_to_json)])
let describe_template_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and templateId =
         flag "template-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_template_alias
           (Values.DescribeTemplateAliasRequest.make ~awsAccountId
              ~templateId ~aliasName ())
           (Some Values.DescribeTemplateAliasResponse.to_json)
           (Some Values.DescribeTemplateAliasResponse.error_to_json)])
let describe_template_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and templateId =
         flag "template-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_template_permissions
           (Values.DescribeTemplatePermissionsRequest.make ~awsAccountId
              ~templateId ())
           (Some Values.DescribeTemplatePermissionsResponse.to_json)
           (Some Values.DescribeTemplatePermissionsResponse.error_to_json)])
let describe_theme =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionNumber =
         flag "version-number" (optional json_arg) ~doc:"JSON VersionNumber"
       and aliasName =
         flag "alias-name" (optional string) ~doc:"STRING AliasName"
       and awsAccountId =
         flag "aws-account-id" (required string)
           ~doc:"STRING AwsAndAccountId"
       and themeId =
         flag "theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_theme
           (Values.DescribeThemeRequest.make
              ?versionNumber:(Option.map ~f:Values.VersionNumber.of_json
                                versionNumber) ?aliasName ~awsAccountId
              ~themeId ()) (Some Values.DescribeThemeResponse.to_json)
           (Some Values.DescribeThemeResponse.error_to_json)])
let describe_theme_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and themeId =
         flag "theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_theme_alias
           (Values.DescribeThemeAliasRequest.make ~awsAccountId ~themeId
              ~aliasName ()) (Some Values.DescribeThemeAliasResponse.to_json)
           (Some Values.DescribeThemeAliasResponse.error_to_json)])
let describe_theme_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and themeId =
         flag "theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_theme_permissions
           (Values.DescribeThemePermissionsRequest.make ~awsAccountId
              ~themeId ())
           (Some Values.DescribeThemePermissionsResponse.to_json)
           (Some Values.DescribeThemePermissionsResponse.error_to_json)])
let describe_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING UserName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user
           (Values.DescribeUserRequest.make ~userName ~awsAccountId
              ~namespace ()) (Some Values.DescribeUserResponse.to_json)
           (Some Values.DescribeUserResponse.error_to_json)])
let generate_embed_url_for_anonymous_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionLifetimeInMinutes =
         flag "session-lifetime-in-minutes" (optional json_arg)
           ~doc:"JSON SessionLifetimeInMinutes"
       and sessionTags =
         flag "session-tags" (optional json_arg) ~doc:"JSON SessionTagList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace"
       and authorizedResourceArns =
         flag "authorized-resource-arns" (required json_arg)
           ~doc:"JSON ArnList"
       and experienceConfiguration =
         flag "experience-configuration" (required json_arg)
           ~doc:"JSON AnonymousUserEmbeddingExperienceConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_embed_url_for_anonymous_user
           (Values.GenerateEmbedUrlForAnonymousUserRequest.make
              ?sessionLifetimeInMinutes:(Option.map
                                           ~f:Values.SessionLifetimeInMinutes.of_json
                                           sessionLifetimeInMinutes)
              ?sessionTags:(Option.map ~f:Values.SessionTagList.of_json
                              sessionTags) ~awsAccountId ~namespace
              ~authorizedResourceArns:(Values.ArnList.of_json
                                         authorizedResourceArns)
              ~experienceConfiguration:(Values.AnonymousUserEmbeddingExperienceConfiguration.of_json
                                          experienceConfiguration) ())
           (Some Values.GenerateEmbedUrlForAnonymousUserResponse.to_json)
           (Some
              Values.GenerateEmbedUrlForAnonymousUserResponse.error_to_json)])
let generate_embed_url_for_registered_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionLifetimeInMinutes =
         flag "session-lifetime-in-minutes" (optional json_arg)
           ~doc:"JSON SessionLifetimeInMinutes"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and userArn = flag "user-arn" (required string) ~doc:"STRING Arn"
       and experienceConfiguration =
         flag "experience-configuration" (required json_arg)
           ~doc:"JSON RegisteredUserEmbeddingExperienceConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_embed_url_for_registered_user
           (Values.GenerateEmbedUrlForRegisteredUserRequest.make
              ?sessionLifetimeInMinutes:(Option.map
                                           ~f:Values.SessionLifetimeInMinutes.of_json
                                           sessionLifetimeInMinutes)
              ~awsAccountId ~userArn
              ~experienceConfiguration:(Values.RegisteredUserEmbeddingExperienceConfiguration.of_json
                                          experienceConfiguration) ())
           (Some Values.GenerateEmbedUrlForRegisteredUserResponse.to_json)
           (Some
              Values.GenerateEmbedUrlForRegisteredUserResponse.error_to_json)])
let get_dashboard_embed_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionLifetimeInMinutes =
         flag "session-lifetime-in-minutes" (optional json_arg)
           ~doc:"JSON SessionLifetimeInMinutes"
       and undoRedoDisabled =
         flag "undo-redo-disabled" (optional bool) ~doc:"BOOL Boolean"
       and resetDisabled =
         flag "reset-disabled" (optional bool) ~doc:"BOOL Boolean"
       and statePersistenceEnabled =
         flag "state-persistence-enabled" (optional bool) ~doc:"BOOL Boolean"
       and userArn = flag "user-arn" (optional string) ~doc:"STRING Arn"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING Namespace"
       and additionalDashboardIds =
         flag "additional-dashboard-ids" (optional json_arg)
           ~doc:"JSON AdditionalDashboardIdList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dashboardId =
         flag "dashboard-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and identityType =
         flag "identity-type" (required json_arg)
           ~doc:"JSON EmbeddingIdentityType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dashboard_embed_url
           (Values.GetDashboardEmbedUrlRequest.make
              ?sessionLifetimeInMinutes:(Option.map
                                           ~f:Values.SessionLifetimeInMinutes.of_json
                                           sessionLifetimeInMinutes)
              ?undoRedoDisabled ?resetDisabled ?statePersistenceEnabled
              ?userArn ?namespace
              ?additionalDashboardIds:(Option.map
                                         ~f:Values.AdditionalDashboardIdList.of_json
                                         additionalDashboardIds)
              ~awsAccountId ~dashboardId
              ~identityType:(Values.EmbeddingIdentityType.of_json
                               identityType) ())
           (Some Values.GetDashboardEmbedUrlResponse.to_json)
           (Some Values.GetDashboardEmbedUrlResponse.error_to_json)])
let get_session_embed_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entryPoint =
         flag "entry-point" (optional string) ~doc:"STRING EntryPoint"
       and sessionLifetimeInMinutes =
         flag "session-lifetime-in-minutes" (optional json_arg)
           ~doc:"JSON SessionLifetimeInMinutes"
       and userArn = flag "user-arn" (optional string) ~doc:"STRING Arn"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_session_embed_url
           (Values.GetSessionEmbedUrlRequest.make ?entryPoint
              ?sessionLifetimeInMinutes:(Option.map
                                           ~f:Values.SessionLifetimeInMinutes.of_json
                                           sessionLifetimeInMinutes) ?userArn
              ~awsAccountId ())
           (Some Values.GetSessionEmbedUrlResponse.to_json)
           (Some Values.GetSessionEmbedUrlResponse.error_to_json)])
let list_analyses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_analyses
           (Values.ListAnalysesRequest.make ?nextToken ?maxResults
              ~awsAccountId ()) (Some Values.ListAnalysesResponse.to_json)
           (Some Values.ListAnalysesResponse.error_to_json)])
let list_dashboard_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dashboardId =
         flag "dashboard-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dashboard_versions
           (Values.ListDashboardVersionsRequest.make ?nextToken ?maxResults
              ~awsAccountId ~dashboardId ())
           (Some Values.ListDashboardVersionsResponse.to_json)
           (Some Values.ListDashboardVersionsResponse.error_to_json)])
let list_dashboards =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dashboards
           (Values.ListDashboardsRequest.make ?nextToken ?maxResults
              ~awsAccountId ()) (Some Values.ListDashboardsResponse.to_json)
           (Some Values.ListDashboardsResponse.error_to_json)])
let list_data_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_sets
           (Values.ListDataSetsRequest.make ?nextToken ?maxResults
              ~awsAccountId ()) (Some Values.ListDataSetsResponse.to_json)
           (Some Values.ListDataSetsResponse.error_to_json)])
let list_data_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_sources
           (Values.ListDataSourcesRequest.make ?nextToken ?maxResults
              ~awsAccountId ()) (Some Values.ListDataSourcesResponse.to_json)
           (Some Values.ListDataSourcesResponse.error_to_json)])
let list_folder_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and folderId =
         flag "folder-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_folder_members
           (Values.ListFolderMembersRequest.make ?nextToken ?maxResults
              ~awsAccountId ~folderId ())
           (Some Values.ListFolderMembersResponse.to_json)
           (Some Values.ListFolderMembersResponse.error_to_json)])
let list_folders =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_folders
           (Values.ListFoldersRequest.make ?nextToken ?maxResults
              ~awsAccountId ()) (Some Values.ListFoldersResponse.to_json)
           (Some Values.ListFoldersResponse.error_to_json)])
let list_group_memberships =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_group_memberships
           (Values.ListGroupMembershipsRequest.make ?nextToken ?maxResults
              ~groupName ~awsAccountId ~namespace ())
           (Some Values.ListGroupMembershipsResponse.to_json)
           (Some Values.ListGroupMembershipsResponse.error_to_json)])
let list_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_groups
           (Values.ListGroupsRequest.make ?nextToken ?maxResults
              ~awsAccountId ~namespace ())
           (Some Values.ListGroupsResponse.to_json)
           (Some Values.ListGroupsResponse.error_to_json)])
let list_i_a_m_policy_assignments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assignmentStatus =
         flag "assignment-status" (optional json_arg)
           ~doc:"JSON AssignmentStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_i_a_m_policy_assignments
           (Values.ListIAMPolicyAssignmentsRequest.make
              ?assignmentStatus:(Option.map
                                   ~f:Values.AssignmentStatus.of_json
                                   assignmentStatus) ?nextToken ?maxResults
              ~awsAccountId ~namespace ())
           (Some Values.ListIAMPolicyAssignmentsResponse.to_json)
           (Some Values.ListIAMPolicyAssignmentsResponse.error_to_json)])
let list_i_a_m_policy_assignments_for_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and userName =
         flag "user-name" (required string) ~doc:"STRING UserName"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_i_a_m_policy_assignments_for_user
           (Values.ListIAMPolicyAssignmentsForUserRequest.make ?nextToken
              ?maxResults ~awsAccountId ~userName ~namespace ())
           (Some Values.ListIAMPolicyAssignmentsForUserResponse.to_json)
           (Some Values.ListIAMPolicyAssignmentsForUserResponse.error_to_json)])
let list_ingestions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IngestionMaxResults"
       and dataSetId =
         flag "data-set-id" (required string) ~doc:"STRING String"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ingestions
           (Values.ListIngestionsRequest.make ?nextToken ?maxResults
              ~dataSetId ~awsAccountId ())
           (Some Values.ListIngestionsResponse.to_json)
           (Some Values.ListIngestionsResponse.error_to_json)])
let list_namespaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_namespaces
           (Values.ListNamespacesRequest.make ?nextToken ?maxResults
              ~awsAccountId ()) (Some Values.ListNamespacesResponse.to_json)
           (Some Values.ListNamespacesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_template_aliases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and templateId =
         flag "template-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_template_aliases
           (Values.ListTemplateAliasesRequest.make ?nextToken ?maxResults
              ~awsAccountId ~templateId ())
           (Some Values.ListTemplateAliasesResponse.to_json)
           (Some Values.ListTemplateAliasesResponse.error_to_json)])
let list_template_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and templateId =
         flag "template-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_template_versions
           (Values.ListTemplateVersionsRequest.make ?nextToken ?maxResults
              ~awsAccountId ~templateId ())
           (Some Values.ListTemplateVersionsResponse.to_json)
           (Some Values.ListTemplateVersionsResponse.error_to_json)])
let list_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_templates
           (Values.ListTemplatesRequest.make ?nextToken ?maxResults
              ~awsAccountId ()) (Some Values.ListTemplatesResponse.to_json)
           (Some Values.ListTemplatesResponse.error_to_json)])
let list_theme_aliases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and themeId =
         flag "theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_theme_aliases
           (Values.ListThemeAliasesRequest.make ?nextToken ?maxResults
              ~awsAccountId ~themeId ())
           (Some Values.ListThemeAliasesResponse.to_json)
           (Some Values.ListThemeAliasesResponse.error_to_json)])
let list_theme_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and themeId =
         flag "theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_theme_versions
           (Values.ListThemeVersionsRequest.make ?nextToken ?maxResults
              ~awsAccountId ~themeId ())
           (Some Values.ListThemeVersionsResponse.to_json)
           (Some Values.ListThemeVersionsResponse.error_to_json)])
let list_themes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON ThemeType"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_themes
           (Values.ListThemesRequest.make ?nextToken ?maxResults
              ?type_:(Option.map ~f:Values.ThemeType.of_json type_)
              ~awsAccountId ()) (Some Values.ListThemesResponse.to_json)
           (Some Values.ListThemesResponse.error_to_json)])
let list_user_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and userName =
         flag "user-name" (required string) ~doc:"STRING UserName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_user_groups
           (Values.ListUserGroupsRequest.make ?nextToken ?maxResults
              ~userName ~awsAccountId ~namespace ())
           (Some Values.ListUserGroupsResponse.to_json)
           (Some Values.ListUserGroupsResponse.error_to_json)])
let list_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_users
           (Values.ListUsersRequest.make ?nextToken ?maxResults ~awsAccountId
              ~namespace ()) (Some Values.ListUsersResponse.to_json)
           (Some Values.ListUsersResponse.error_to_json)])
let register_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and iamArn = flag "iam-arn" (optional string) ~doc:"STRING String"
       and sessionName =
         flag "session-name" (optional string) ~doc:"STRING RoleSessionName"
       and userName =
         flag "user-name" (optional string) ~doc:"STRING UserName"
       and customPermissionsName =
         flag "custom-permissions-name" (optional string)
           ~doc:"STRING RoleName"
       and externalLoginFederationProviderType =
         flag "external-login-federation-provider-type" (optional string)
           ~doc:"STRING String"
       and customFederationProviderUrl =
         flag "custom-federation-provider-url" (optional string)
           ~doc:"STRING String"
       and externalLoginId =
         flag "external-login-id" (optional string) ~doc:"STRING String"
       and identityType =
         flag "identity-type" (required json_arg) ~doc:"JSON IdentityType"
       and email = flag "email" (required string) ~doc:"STRING String"
       and userRole =
         flag "user-role" (required json_arg) ~doc:"JSON UserRole"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_user
           (Values.RegisterUserRequest.make ?iamArn ?sessionName ?userName
              ?customPermissionsName ?externalLoginFederationProviderType
              ?customFederationProviderUrl ?externalLoginId
              ~identityType:(Values.IdentityType.of_json identityType) ~email
              ~userRole:(Values.UserRole.of_json userRole) ~awsAccountId
              ~namespace ()) (Some Values.RegisterUserResponse.to_json)
           (Some Values.RegisterUserResponse.error_to_json)])
let restore_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and analysisId =
         flag "analysis-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_analysis
           (Values.RestoreAnalysisRequest.make ~awsAccountId ~analysisId ())
           (Some Values.RestoreAnalysisResponse.to_json)
           (Some Values.RestoreAnalysisResponse.error_to_json)])
let search_analyses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and filters =
         flag "filters" (required json_arg)
           ~doc:"JSON AnalysisSearchFilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_analyses
           (Values.SearchAnalysesRequest.make ?nextToken ?maxResults
              ~awsAccountId
              ~filters:(Values.AnalysisSearchFilterList.of_json filters) ())
           (Some Values.SearchAnalysesResponse.to_json)
           (Some Values.SearchAnalysesResponse.error_to_json)])
let search_dashboards =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and filters =
         flag "filters" (required json_arg)
           ~doc:"JSON DashboardSearchFilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_dashboards
           (Values.SearchDashboardsRequest.make ?nextToken ?maxResults
              ~awsAccountId
              ~filters:(Values.DashboardSearchFilterList.of_json filters) ())
           (Some Values.SearchDashboardsResponse.to_json)
           (Some Values.SearchDashboardsResponse.error_to_json)])
let search_folders =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and filters =
         flag "filters" (required json_arg)
           ~doc:"JSON FolderSearchFilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_folders
           (Values.SearchFoldersRequest.make ?nextToken ?maxResults
              ~awsAccountId
              ~filters:(Values.FolderSearchFilterList.of_json filters) ())
           (Some Values.SearchFoldersResponse.to_json)
           (Some Values.SearchFoldersResponse.error_to_json)])
let search_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace"
       and filters =
         flag "filters" (required json_arg) ~doc:"JSON GroupSearchFilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_groups
           (Values.SearchGroupsRequest.make ?nextToken ?maxResults
              ~awsAccountId ~namespace
              ~filters:(Values.GroupSearchFilterList.of_json filters) ())
           (Some Values.SearchGroupsResponse.to_json)
           (Some Values.SearchGroupsResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_account_customization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING Namespace"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and accountCustomization =
         flag "account-customization" (required json_arg)
           ~doc:"JSON AccountCustomization" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account_customization
           (Values.UpdateAccountCustomizationRequest.make ?namespace
              ~awsAccountId
              ~accountCustomization:(Values.AccountCustomization.of_json
                                       accountCustomization) ())
           (Some Values.UpdateAccountCustomizationResponse.to_json)
           (Some Values.UpdateAccountCustomizationResponse.error_to_json)])
let update_account_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notificationEmail =
         flag "notification-email" (optional string) ~doc:"STRING String"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and defaultNamespace =
         flag "default-namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account_settings
           (Values.UpdateAccountSettingsRequest.make ?notificationEmail
              ~awsAccountId ~defaultNamespace ())
           (Some Values.UpdateAccountSettingsResponse.to_json)
           (Some Values.UpdateAccountSettingsResponse.error_to_json)])
let update_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and themeArn = flag "theme-arn" (optional string) ~doc:"STRING Arn"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and analysisId =
         flag "analysis-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and name = flag "name" (required string) ~doc:"STRING AnalysisName"
       and sourceEntity =
         flag "source-entity" (required json_arg)
           ~doc:"JSON AnalysisSourceEntity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_analysis
           (Values.UpdateAnalysisRequest.make
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?themeArn ~awsAccountId ~analysisId ~name
              ~sourceEntity:(Values.AnalysisSourceEntity.of_json sourceEntity)
              ()) (Some Values.UpdateAnalysisResponse.to_json)
           (Some Values.UpdateAnalysisResponse.error_to_json)])
let update_analysis_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantPermissions =
         flag "grant-permissions" (optional json_arg)
           ~doc:"JSON UpdateResourcePermissionList"
       and revokePermissions =
         flag "revoke-permissions" (optional json_arg)
           ~doc:"JSON UpdateResourcePermissionList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and analysisId =
         flag "analysis-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_analysis_permissions
           (Values.UpdateAnalysisPermissionsRequest.make
              ?grantPermissions:(Option.map
                                   ~f:Values.UpdateResourcePermissionList.of_json
                                   grantPermissions)
              ?revokePermissions:(Option.map
                                    ~f:Values.UpdateResourcePermissionList.of_json
                                    revokePermissions) ~awsAccountId
              ~analysisId ())
           (Some Values.UpdateAnalysisPermissionsResponse.to_json)
           (Some Values.UpdateAnalysisPermissionsResponse.error_to_json)])
let update_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and versionDescription =
         flag "version-description" (optional string)
           ~doc:"STRING VersionDescription"
       and dashboardPublishOptions =
         flag "dashboard-publish-options" (optional json_arg)
           ~doc:"JSON DashboardPublishOptions"
       and themeArn = flag "theme-arn" (optional string) ~doc:"STRING Arn"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dashboardId =
         flag "dashboard-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and name = flag "name" (required string) ~doc:"STRING DashboardName"
       and sourceEntity =
         flag "source-entity" (required json_arg)
           ~doc:"JSON DashboardSourceEntity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_dashboard
           (Values.UpdateDashboardRequest.make
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?versionDescription
              ?dashboardPublishOptions:(Option.map
                                          ~f:Values.DashboardPublishOptions.of_json
                                          dashboardPublishOptions) ?themeArn
              ~awsAccountId ~dashboardId ~name
              ~sourceEntity:(Values.DashboardSourceEntity.of_json
                               sourceEntity) ())
           (Some Values.UpdateDashboardResponse.to_json)
           (Some Values.UpdateDashboardResponse.error_to_json)])
let update_dashboard_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantPermissions =
         flag "grant-permissions" (optional json_arg)
           ~doc:"JSON UpdateResourcePermissionList"
       and revokePermissions =
         flag "revoke-permissions" (optional json_arg)
           ~doc:"JSON UpdateResourcePermissionList"
       and grantLinkPermissions =
         flag "grant-link-permissions" (optional json_arg)
           ~doc:"JSON UpdateLinkPermissionList"
       and revokeLinkPermissions =
         flag "revoke-link-permissions" (optional json_arg)
           ~doc:"JSON UpdateLinkPermissionList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dashboardId =
         flag "dashboard-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_dashboard_permissions
           (Values.UpdateDashboardPermissionsRequest.make
              ?grantPermissions:(Option.map
                                   ~f:Values.UpdateResourcePermissionList.of_json
                                   grantPermissions)
              ?revokePermissions:(Option.map
                                    ~f:Values.UpdateResourcePermissionList.of_json
                                    revokePermissions)
              ?grantLinkPermissions:(Option.map
                                       ~f:Values.UpdateLinkPermissionList.of_json
                                       grantLinkPermissions)
              ?revokeLinkPermissions:(Option.map
                                        ~f:Values.UpdateLinkPermissionList.of_json
                                        revokeLinkPermissions) ~awsAccountId
              ~dashboardId ())
           (Some Values.UpdateDashboardPermissionsResponse.to_json)
           (Some Values.UpdateDashboardPermissionsResponse.error_to_json)])
let update_dashboard_published_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dashboardId =
         flag "dashboard-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and versionNumber =
         flag "version-number" (required json_arg) ~doc:"JSON VersionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_dashboard_published_version
           (Values.UpdateDashboardPublishedVersionRequest.make ~awsAccountId
              ~dashboardId
              ~versionNumber:(Values.VersionNumber.of_json versionNumber) ())
           (Some Values.UpdateDashboardPublishedVersionResponse.to_json)
           (Some Values.UpdateDashboardPublishedVersionResponse.error_to_json)])
let update_data_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and logicalTableMap =
         flag "logical-table-map" (optional json_arg)
           ~doc:"JSON LogicalTableMap"
       and columnGroups =
         flag "column-groups" (optional json_arg) ~doc:"JSON ColumnGroupList"
       and fieldFolders =
         flag "field-folders" (optional json_arg) ~doc:"JSON FieldFolderMap"
       and rowLevelPermissionDataSet =
         flag "row-level-permission-data-set" (optional json_arg)
           ~doc:"JSON RowLevelPermissionDataSet"
       and rowLevelPermissionTagConfiguration =
         flag "row-level-permission-tag-configuration" (optional json_arg)
           ~doc:"JSON RowLevelPermissionTagConfiguration"
       and columnLevelPermissionRules =
         flag "column-level-permission-rules" (optional json_arg)
           ~doc:"JSON ColumnLevelPermissionRuleList"
       and dataSetUsageConfiguration =
         flag "data-set-usage-configuration" (optional json_arg)
           ~doc:"JSON DataSetUsageConfiguration"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSetId =
         flag "data-set-id" (required string) ~doc:"STRING ResourceId"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and physicalTableMap =
         flag "physical-table-map" (required json_arg)
           ~doc:"JSON PhysicalTableMap"
       and importMode =
         flag "import-mode" (required json_arg) ~doc:"JSON DataSetImportMode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_set
           (Values.UpdateDataSetRequest.make
              ?logicalTableMap:(Option.map ~f:Values.LogicalTableMap.of_json
                                  logicalTableMap)
              ?columnGroups:(Option.map ~f:Values.ColumnGroupList.of_json
                               columnGroups)
              ?fieldFolders:(Option.map ~f:Values.FieldFolderMap.of_json
                               fieldFolders)
              ?rowLevelPermissionDataSet:(Option.map
                                            ~f:Values.RowLevelPermissionDataSet.of_json
                                            rowLevelPermissionDataSet)
              ?rowLevelPermissionTagConfiguration:(Option.map
                                                     ~f:Values.RowLevelPermissionTagConfiguration.of_json
                                                     rowLevelPermissionTagConfiguration)
              ?columnLevelPermissionRules:(Option.map
                                             ~f:Values.ColumnLevelPermissionRuleList.of_json
                                             columnLevelPermissionRules)
              ?dataSetUsageConfiguration:(Option.map
                                            ~f:Values.DataSetUsageConfiguration.of_json
                                            dataSetUsageConfiguration)
              ~awsAccountId ~dataSetId ~name
              ~physicalTableMap:(Values.PhysicalTableMap.of_json
                                   physicalTableMap)
              ~importMode:(Values.DataSetImportMode.of_json importMode) ())
           (Some Values.UpdateDataSetResponse.to_json)
           (Some Values.UpdateDataSetResponse.error_to_json)])
let update_data_set_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantPermissions =
         flag "grant-permissions" (optional json_arg)
           ~doc:"JSON ResourcePermissionList"
       and revokePermissions =
         flag "revoke-permissions" (optional json_arg)
           ~doc:"JSON ResourcePermissionList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSetId =
         flag "data-set-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_set_permissions
           (Values.UpdateDataSetPermissionsRequest.make
              ?grantPermissions:(Option.map
                                   ~f:Values.ResourcePermissionList.of_json
                                   grantPermissions)
              ?revokePermissions:(Option.map
                                    ~f:Values.ResourcePermissionList.of_json
                                    revokePermissions) ~awsAccountId
              ~dataSetId ())
           (Some Values.UpdateDataSetPermissionsResponse.to_json)
           (Some Values.UpdateDataSetPermissionsResponse.error_to_json)])
let update_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceParameters =
         flag "data-source-parameters" (optional json_arg)
           ~doc:"JSON DataSourceParameters"
       and credentials =
         flag "credentials" (optional json_arg)
           ~doc:"JSON DataSourceCredentials"
       and vpcConnectionProperties =
         flag "vpc-connection-properties" (optional json_arg)
           ~doc:"JSON VpcConnectionProperties"
       and sslProperties =
         flag "ssl-properties" (optional json_arg) ~doc:"JSON SslProperties"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING ResourceId"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_source
           (Values.UpdateDataSourceRequest.make
              ?dataSourceParameters:(Option.map
                                       ~f:Values.DataSourceParameters.of_json
                                       dataSourceParameters)
              ?credentials:(Option.map
                              ~f:Values.DataSourceCredentials.of_json
                              credentials)
              ?vpcConnectionProperties:(Option.map
                                          ~f:Values.VpcConnectionProperties.of_json
                                          vpcConnectionProperties)
              ?sslProperties:(Option.map ~f:Values.SslProperties.of_json
                                sslProperties) ~awsAccountId ~dataSourceId
              ~name ()) (Some Values.UpdateDataSourceResponse.to_json)
           (Some Values.UpdateDataSourceResponse.error_to_json)])
let update_data_source_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantPermissions =
         flag "grant-permissions" (optional json_arg)
           ~doc:"JSON ResourcePermissionList"
       and revokePermissions =
         flag "revoke-permissions" (optional json_arg)
           ~doc:"JSON ResourcePermissionList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_source_permissions
           (Values.UpdateDataSourcePermissionsRequest.make
              ?grantPermissions:(Option.map
                                   ~f:Values.ResourcePermissionList.of_json
                                   grantPermissions)
              ?revokePermissions:(Option.map
                                    ~f:Values.ResourcePermissionList.of_json
                                    revokePermissions) ~awsAccountId
              ~dataSourceId ())
           (Some Values.UpdateDataSourcePermissionsResponse.to_json)
           (Some Values.UpdateDataSourcePermissionsResponse.error_to_json)])
let update_folder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and folderId =
         flag "folder-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and name = flag "name" (required string) ~doc:"STRING FolderName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_folder
           (Values.UpdateFolderRequest.make ~awsAccountId ~folderId ~name ())
           (Some Values.UpdateFolderResponse.to_json)
           (Some Values.UpdateFolderResponse.error_to_json)])
let update_folder_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantPermissions =
         flag "grant-permissions" (optional json_arg)
           ~doc:"JSON ResourcePermissionList"
       and revokePermissions =
         flag "revoke-permissions" (optional json_arg)
           ~doc:"JSON ResourcePermissionList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and folderId =
         flag "folder-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_folder_permissions
           (Values.UpdateFolderPermissionsRequest.make
              ?grantPermissions:(Option.map
                                   ~f:Values.ResourcePermissionList.of_json
                                   grantPermissions)
              ?revokePermissions:(Option.map
                                    ~f:Values.ResourcePermissionList.of_json
                                    revokePermissions) ~awsAccountId
              ~folderId ())
           (Some Values.UpdateFolderPermissionsResponse.to_json)
           (Some Values.UpdateFolderPermissionsResponse.error_to_json)])
let update_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING GroupDescription"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_group
           (Values.UpdateGroupRequest.make ?description ~groupName
              ~awsAccountId ~namespace ())
           (Some Values.UpdateGroupResponse.to_json)
           (Some Values.UpdateGroupResponse.error_to_json)])
let update_i_a_m_policy_assignment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assignmentStatus =
         flag "assignment-status" (optional json_arg)
           ~doc:"JSON AssignmentStatus"
       and policyArn = flag "policy-arn" (optional string) ~doc:"STRING Arn"
       and identities =
         flag "identities" (optional json_arg) ~doc:"JSON IdentityMap"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and assignmentName =
         flag "assignment-name" (required string)
           ~doc:"STRING IAMPolicyAssignmentName"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_i_a_m_policy_assignment
           (Values.UpdateIAMPolicyAssignmentRequest.make
              ?assignmentStatus:(Option.map
                                   ~f:Values.AssignmentStatus.of_json
                                   assignmentStatus) ?policyArn
              ?identities:(Option.map ~f:Values.IdentityMap.of_json
                             identities) ~awsAccountId ~assignmentName
              ~namespace ())
           (Some Values.UpdateIAMPolicyAssignmentResponse.to_json)
           (Some Values.UpdateIAMPolicyAssignmentResponse.error_to_json)])
let update_ip_restriction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ipRestrictionRuleMap =
         flag "ip-restriction-rule-map" (optional json_arg)
           ~doc:"JSON IpRestrictionRuleMap"
       and enabled =
         flag "enabled" (optional bool) ~doc:"BOOL NullableBoolean"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_ip_restriction
           (Values.UpdateIpRestrictionRequest.make
              ?ipRestrictionRuleMap:(Option.map
                                       ~f:Values.IpRestrictionRuleMap.of_json
                                       ipRestrictionRuleMap) ?enabled
              ~awsAccountId ())
           (Some Values.UpdateIpRestrictionResponse.to_json)
           (Some Values.UpdateIpRestrictionResponse.error_to_json)])
let update_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionDescription =
         flag "version-description" (optional string)
           ~doc:"STRING VersionDescription"
       and name = flag "name" (optional string) ~doc:"STRING TemplateName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and templateId =
         flag "template-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and sourceEntity =
         flag "source-entity" (required json_arg)
           ~doc:"JSON TemplateSourceEntity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_template
           (Values.UpdateTemplateRequest.make ?versionDescription ?name
              ~awsAccountId ~templateId
              ~sourceEntity:(Values.TemplateSourceEntity.of_json sourceEntity)
              ()) (Some Values.UpdateTemplateResponse.to_json)
           (Some Values.UpdateTemplateResponse.error_to_json)])
let update_template_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and templateId =
         flag "template-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasName"
       and templateVersionNumber =
         flag "template-version-number" (required json_arg)
           ~doc:"JSON VersionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_template_alias
           (Values.UpdateTemplateAliasRequest.make ~awsAccountId ~templateId
              ~aliasName
              ~templateVersionNumber:(Values.VersionNumber.of_json
                                        templateVersionNumber) ())
           (Some Values.UpdateTemplateAliasResponse.to_json)
           (Some Values.UpdateTemplateAliasResponse.error_to_json)])
let update_template_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantPermissions =
         flag "grant-permissions" (optional json_arg)
           ~doc:"JSON UpdateResourcePermissionList"
       and revokePermissions =
         flag "revoke-permissions" (optional json_arg)
           ~doc:"JSON UpdateResourcePermissionList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and templateId =
         flag "template-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_template_permissions
           (Values.UpdateTemplatePermissionsRequest.make
              ?grantPermissions:(Option.map
                                   ~f:Values.UpdateResourcePermissionList.of_json
                                   grantPermissions)
              ?revokePermissions:(Option.map
                                    ~f:Values.UpdateResourcePermissionList.of_json
                                    revokePermissions) ~awsAccountId
              ~templateId ())
           (Some Values.UpdateTemplatePermissionsResponse.to_json)
           (Some Values.UpdateTemplatePermissionsResponse.error_to_json)])
let update_theme =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ThemeName"
       and versionDescription =
         flag "version-description" (optional string)
           ~doc:"STRING VersionDescription"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON ThemeConfiguration"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and themeId =
         flag "theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and baseThemeId =
         flag "base-theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_theme
           (Values.UpdateThemeRequest.make ?name ?versionDescription
              ?configuration:(Option.map ~f:Values.ThemeConfiguration.of_json
                                configuration) ~awsAccountId ~themeId
              ~baseThemeId ()) (Some Values.UpdateThemeResponse.to_json)
           (Some Values.UpdateThemeResponse.error_to_json)])
let update_theme_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and themeId =
         flag "theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasName"
       and themeVersionNumber =
         flag "theme-version-number" (required json_arg)
           ~doc:"JSON VersionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_theme_alias
           (Values.UpdateThemeAliasRequest.make ~awsAccountId ~themeId
              ~aliasName
              ~themeVersionNumber:(Values.VersionNumber.of_json
                                     themeVersionNumber) ())
           (Some Values.UpdateThemeAliasResponse.to_json)
           (Some Values.UpdateThemeAliasResponse.error_to_json)])
let update_theme_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantPermissions =
         flag "grant-permissions" (optional json_arg)
           ~doc:"JSON UpdateResourcePermissionList"
       and revokePermissions =
         flag "revoke-permissions" (optional json_arg)
           ~doc:"JSON UpdateResourcePermissionList"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and themeId =
         flag "theme-id" (required string)
           ~doc:"STRING RestrictiveResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_theme_permissions
           (Values.UpdateThemePermissionsRequest.make
              ?grantPermissions:(Option.map
                                   ~f:Values.UpdateResourcePermissionList.of_json
                                   grantPermissions)
              ?revokePermissions:(Option.map
                                    ~f:Values.UpdateResourcePermissionList.of_json
                                    revokePermissions) ~awsAccountId ~themeId
              ()) (Some Values.UpdateThemePermissionsResponse.to_json)
           (Some Values.UpdateThemePermissionsResponse.error_to_json)])
let update_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customPermissionsName =
         flag "custom-permissions-name" (optional string)
           ~doc:"STRING RoleName"
       and unapplyCustomPermissions =
         flag "unapply-custom-permissions" (optional bool)
           ~doc:"BOOL Boolean"
       and externalLoginFederationProviderType =
         flag "external-login-federation-provider-type" (optional string)
           ~doc:"STRING String"
       and customFederationProviderUrl =
         flag "custom-federation-provider-url" (optional string)
           ~doc:"STRING String"
       and externalLoginId =
         flag "external-login-id" (optional string) ~doc:"STRING String"
       and userName =
         flag "user-name" (required string) ~doc:"STRING UserName"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING AwsAccountId"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace"
       and email = flag "email" (required string) ~doc:"STRING String"
       and role = flag "role" (required json_arg) ~doc:"JSON UserRole" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user
           (Values.UpdateUserRequest.make ?customPermissionsName
              ?unapplyCustomPermissions ?externalLoginFederationProviderType
              ?customFederationProviderUrl ?externalLoginId ~userName
              ~awsAccountId ~namespace ~email
              ~role:(Values.UserRole.of_json role) ())
           (Some Values.UpdateUserResponse.to_json)
           (Some Values.UpdateUserResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("cancel-ingestion", cancel_ingestion);
    ("create-account-customization", create_account_customization);
    ("create-analysis", create_analysis);
    ("create-dashboard", create_dashboard);
    ("create-data-set", create_data_set);
    ("create-data-source", create_data_source);
    ("create-folder", create_folder);
    ("create-folder-membership", create_folder_membership);
    ("create-group", create_group);
    ("create-group-membership", create_group_membership);
    ("create-i-a-m-policy-assignment", create_i_a_m_policy_assignment);
    ("create-ingestion", create_ingestion);
    ("create-namespace", create_namespace);
    ("create-template", create_template);
    ("create-template-alias", create_template_alias);
    ("create-theme", create_theme);
    ("create-theme-alias", create_theme_alias);
    ("delete-account-customization", delete_account_customization);
    ("delete-analysis", delete_analysis);
    ("delete-dashboard", delete_dashboard);
    ("delete-data-set", delete_data_set);
    ("delete-data-source", delete_data_source);
    ("delete-folder", delete_folder);
    ("delete-folder-membership", delete_folder_membership);
    ("delete-group", delete_group);
    ("delete-group-membership", delete_group_membership);
    ("delete-i-a-m-policy-assignment", delete_i_a_m_policy_assignment);
    ("delete-namespace", delete_namespace);
    ("delete-template", delete_template);
    ("delete-template-alias", delete_template_alias);
    ("delete-theme", delete_theme);
    ("delete-theme-alias", delete_theme_alias);
    ("delete-user", delete_user);
    ("delete-user-by-principal-id", delete_user_by_principal_id);
    ("describe-account-customization", describe_account_customization);
    ("describe-account-settings", describe_account_settings);
    ("describe-analysis", describe_analysis);
    ("describe-analysis-permissions", describe_analysis_permissions);
    ("describe-dashboard", describe_dashboard);
    ("describe-dashboard-permissions", describe_dashboard_permissions);
    ("describe-data-set", describe_data_set);
    ("describe-data-set-permissions", describe_data_set_permissions);
    ("describe-data-source", describe_data_source);
    ("describe-data-source-permissions", describe_data_source_permissions);
    ("describe-folder", describe_folder);
    ("describe-folder-permissions", describe_folder_permissions);
    ("describe-folder-resolved-permissions",
      describe_folder_resolved_permissions);
    ("describe-group", describe_group);
    ("describe-group-membership", describe_group_membership);
    ("describe-i-a-m-policy-assignment", describe_i_a_m_policy_assignment);
    ("describe-ingestion", describe_ingestion);
    ("describe-ip-restriction", describe_ip_restriction);
    ("describe-namespace", describe_namespace);
    ("describe-template", describe_template);
    ("describe-template-alias", describe_template_alias);
    ("describe-template-permissions", describe_template_permissions);
    ("describe-theme", describe_theme);
    ("describe-theme-alias", describe_theme_alias);
    ("describe-theme-permissions", describe_theme_permissions);
    ("describe-user", describe_user);
    ("generate-embed-url-for-anonymous-user",
      generate_embed_url_for_anonymous_user);
    ("generate-embed-url-for-registered-user",
      generate_embed_url_for_registered_user);
    ("get-dashboard-embed-url", get_dashboard_embed_url);
    ("get-session-embed-url", get_session_embed_url);
    ("list-analyses", list_analyses);
    ("list-dashboard-versions", list_dashboard_versions);
    ("list-dashboards", list_dashboards);
    ("list-data-sets", list_data_sets);
    ("list-data-sources", list_data_sources);
    ("list-folder-members", list_folder_members);
    ("list-folders", list_folders);
    ("list-group-memberships", list_group_memberships);
    ("list-groups", list_groups);
    ("list-i-a-m-policy-assignments", list_i_a_m_policy_assignments);
    ("list-i-a-m-policy-assignments-for-user",
      list_i_a_m_policy_assignments_for_user);
    ("list-ingestions", list_ingestions);
    ("list-namespaces", list_namespaces);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-template-aliases", list_template_aliases);
    ("list-template-versions", list_template_versions);
    ("list-templates", list_templates);
    ("list-theme-aliases", list_theme_aliases);
    ("list-theme-versions", list_theme_versions);
    ("list-themes", list_themes);
    ("list-user-groups", list_user_groups);
    ("list-users", list_users);
    ("register-user", register_user);
    ("restore-analysis", restore_analysis);
    ("search-analyses", search_analyses);
    ("search-dashboards", search_dashboards);
    ("search-folders", search_folders);
    ("search-groups", search_groups);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-account-customization", update_account_customization);
    ("update-account-settings", update_account_settings);
    ("update-analysis", update_analysis);
    ("update-analysis-permissions", update_analysis_permissions);
    ("update-dashboard", update_dashboard);
    ("update-dashboard-permissions", update_dashboard_permissions);
    ("update-dashboard-published-version",
      update_dashboard_published_version);
    ("update-data-set", update_data_set);
    ("update-data-set-permissions", update_data_set_permissions);
    ("update-data-source", update_data_source);
    ("update-data-source-permissions", update_data_source_permissions);
    ("update-folder", update_folder);
    ("update-folder-permissions", update_folder_permissions);
    ("update-group", update_group);
    ("update-i-a-m-policy-assignment", update_i_a_m_policy_assignment);
    ("update-ip-restriction", update_ip_restriction);
    ("update-template", update_template);
    ("update-template-alias", update_template_alias);
    ("update-template-permissions", update_template_permissions);
    ("update-theme", update_theme);
    ("update-theme-alias", update_theme_alias);
    ("update-theme-permissions", update_theme_permissions);
    ("update-user", update_user)]